# Инфраструктура и грабли облачной среды

#инфра #грабли

## Среда Claude Code (web)
- Облачный контейнер, репо клонируется свежим. Всё ценное — коммитить и пушить сразу.
- Ветка работы: `claude/new-branch-setup-mq8llw` (зоопт+сайт). OKO-app — `claude/new-session-w2ptqy`.
- Исходящий HTTPS через прокси `$HTTPS_PROXY`. curl работает, node fetch мимо прокси НЕ ходит.
- apt: сначала `apt-get update`, иначе 404 на пакетах. Установлены: ffmpeg, poppler-utils, potrace.

## Playwright / скриншоты / видео
- Python playwright + системный Chromium: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`.
- Args для WebGL в headless: `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader --no-sandbox`.
- Прокси браузеру: `proxy={"server":$HTTPS_PROXY,"bypass":"localhost,127.0.0.1"}` — БЕЗ bypass localhost не откроется.
- Видео: record_video_dir в new_context, скролл РЕАЛЬНЫМИ mouse.wheel (Lenis перехватывает), webm→mp4 через ffmpeg.

## Генерация (бесплатно, HF Spaces)
- MCP dynamic_space invoke ОТКЛЮЧЁН (gradio=none). Обход: `pip install gradio_client`, ходить напрямую.
- Живые спейсы (проверено 07.07.2026):
  - Картинки: `mcp-tools/FLUX.1-Krea-dev` (/infer), `Tongyi-MAI/Z-Image-Turbo` (/generate, есть 1280x720, seed, random_seed=False)
  - Редактирование: `Qwen/Qwen-Image-Edit` (/infer)
  - Видео A→Б: `multimodalart/wan-2-2-first-last-frame` (/generate_video, duration_seconds, seed)
  - Видео из 1 кадра: `zerogpu-aoti/wan2-2-fp8da-aoti-faster`; LTX: `Lightricks/ltx-video-distilled`
- МЁРТВЫЕ: `mcp-tools/wan-2-2-first-last-frame` (RUNTIME_ERROR), `mcp-tools/Qwen-Image-Edit-2509-LoRAs-Fast` (401), `prithivMLmods/...2511...` (закрыт).
- ZeroGPU аноним: мало квоты на видео. HF_TOKEN в env поднимает квоты (пока НЕ подключён).
- Higgsfield: баланс 0.5 кредита (free) — генерация недоступна, ждём пополнения.

## Хостинг Higgsfield (сайты)
- ЗооОпт site: website_id `5c654873-f2e0-43c1-a330-e2575a340700`, slug `spicy-panther-317`, URL https://spicy-panther-317.higgsfield.app
- OKO прототип: website_id `5426760c-49ec-46c4-b3ff-b22a6dd598a5`, URL https://true-journey-418.higgsfield.app
- Статика кладётся в `app/public/` шаблона (asset-first: public/index.html отдаётся на «/»).
- deploy_website ЧАСТО таймаутит 60с — деплой ДОХОДИТ, проверять curl'ом по маркеру в HTML (retry 3-8 раз по 20-25с).
- Repo-token скоуплен, пуш: `git -c http.extraHeader="Authorization: token <token>" push origin main`.

## Шрифты (кириллица!)
- Unbounded: github googlefonts/unbounded — полная кириллица.
- Onest: сабсеты fontsource БЕЗ кириллицы+цифр вместе! Брать VF: `cdn.jsdelivr.net/gh/google/fonts@main/ofl/onest/Onest%5Bwght%5D.ttf`, инстанцировать fonttools.
- reportlab 5: canvas.setCharSpace НЕТ — текст-объект t.setCharSpace, и ВСЕГДА сбрасывать (утечка Tc сдвигает следующий текст!).

## Печать (зоопт-пайплайн)
- CMYK-вектора, bleed 5мм, метки реза — `zoopt/src/design_system.py`. Helvetica вычищается pikepdf-постпроцессом.
- Векторизация potrace лого с AA-краями = грязь. Не повторять, ждать вектор от клиента.

## Сайт-стек (wow)
- Все либы ВЕНДОРИТЬ локально (CDN через прокси флапает): three 0.160 + jsm (bloom/GLTF/Orbit), gsap, ScrollTrigger, lenis — уже в `zoopt/site/js/vendor/`.
- CatmullRom НЕ проходит контрольные точки на равных параметрах — прекомпьют anchorsOf+remap (см. zoopt/site/js/app.js).
- CC0 GLB: Khronos glTF-Sample-Assets (Fox анимированный, Duck). Sketchfab API требует токен.

## Ключи и интеграции (проверено 07.07.2026)
Значения лежат в env-переменных окружения code.claude.com (и на сессию — ~/.oko/secrets.env, chmod 600, ВНЕ git).
- HF_TOKEN — РАБОТАЕТ (whoami: okoteam). Использовать в gradio_client(hf_token=...) для квот ZeroGPU.
- FAL_KEY — РАБОТАЕТ (формат uuid:hex, auth 404 на фейк-запрос = ок). Генерация без очередей, платно за факт.
- TWENTY_FIRST_API_KEY — РАБОТАЕТ (api.21st.dev/api/search, header x-api-key) — поиск wow-компонентов.
- CLOUDFLARE_API_TOKEN (cfat_, АККАУНТ-токен!) — РАБОТАЕТ. Проверка ТОЛЬКО через
  /accounts/$CLOUDFLARE_ACCOUNT_ID/tokens/verify (обычный /user/tokens/verify даёт Invalid).
- CLOUDFLARE_ACCOUNT_ID, R2_ACCESS_KEY_ID/SECRET/ENDPOINT — сохранены, НО домен
  *.r2.cloudflarestorage.com ЗАБЛОКИРОВАН network policy окружения (curl code 000). Добавить домен в policy.
- SKETCHFAB_TOKEN — Invalid: со скрина токен обрезан (26 символов, надо 32) — попросить полный.
- Rive: API-ключа НЕ существует. Рантайм вендорен: factory/vendor/rive.min.js (canvas-single, WASM внутри,
  1.7MB) + демо factory/rive/vehicles.riv. Смоук пройден в headless. Обычный @rive-app/canvas НЕ работает
  оффлайн (тянет rive.wasm с CDN) — брать только canvas-single.

## Remotion (видео кодом) — подключён 07.07.2026
Скилл: .claude/skills/remotion-video/ (wshuyi/remotion-video-skill + наш рецепт среды).
Рендер: ТОЛЬКО headless_shell обёртка /tmp/chrome-ns.sh (--no-sandbox) + --gl=swangle.
Обычный chrome падает: «Old Headless mode removed». Версии: remotion 4.0.245, react 18.3.1. Смоук: 3с MP4 отрендерен.

## Боевые скиллы кино-сайтов (установлены 08.07, GitHub → .claude/skills/)
- **scroll-world** (oso95, 121★): «полёт сквозь мир» без склеек = ТА САМАЯ техника с референсов. Higgsfield gpt_image_2 сцены + seedance_2_0 dive-клипы + коннекторы ИЗ РЕАЛЬНЫХ КАДРОВ соседей (правило бесшовности — иначе «поп»). Портативный scrub-engine с blob-seek (подтвердил мою граблю Range). Архитектура A (форвард-тэйк, без коннекторов) vs B (диорама+аэро-коннектор). Энкод -g 8 -crf 20. NSFW-фильтр Seedance ложно срабатывает на bed/pool/spa — ре-роллить. Адаптер CLI→MCP дописан в конец SKILL.md.
- **forja-landing** (Carlos-Dominguez): кино-лендинг + анти-AI-slop критика (10 осей) + аудит + Next.js шаблоны. Палитра ember (dark+amber), Fraunces+Geist.
- Дизайн-рефы: brain/Claude/refs/claude-design/ (адаптация внутреннего Design-промпта Claude).
- Вывод: механику кино-сайтов мы ТЕПЕРЬ знаем полностью и подтверждённо. Осталось топливо — кредиты Higgsfield (Ultra) для реальных сцен.
