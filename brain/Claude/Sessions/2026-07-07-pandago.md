# 2026-07-07 · PandaGo /order/ + сборка oko-magic

Теги: #проект/pandago #статус/в-работе

## Сделано
- Полный премиум-редизайн pandago-order/: скролл-путешествие (одна Three.js
  сцена на весь сайт, камера по CatmullRom за байком, этапы: подиум, порт,
  тихая зона, горы, башни+ворота), GLSL-аврора c uProgress, игровой HUD
  (этапы, lerp-координаты, звук WebAudio), fx.js (скрэмблинг, курсор,
  магниты, Lenis), чистая адаптивная таблица сравнения вместо шторки.
- Байк: poly.pizza CC-BY (Zsky), перекрашен кодом под бренд, кредит в футере.
- Превью-деплой: forest-beach-360.higgsfield.app (статика в app/public/).
- Self-QA: journey.js в scratchpad (6 точек скролла, 2 вьюпорта, консоль).
  ВАЖНО: scrollTo в тестах только behavior:'instant', иначе рваные кадры.
- Смержена ветка claude/new-branch-setup-mq8llw (oko-magic, brain/, zoopt,
  vendor-библиотеки). oko-web-factory удалён, его арсенал влит в oko-magic
  (секции: бесплатные 3D-источники, референс PandaGo).

## Решения
- Сравнение цен: слайдер-шторка рвала цены на мобилке - заменена таблицей.
- Фикс-канвас на весь сайт + секции-вуали; карточки уплотнены до rgba .78.
- HUD включается после p>0.045, чтобы не дублировать hero.

## Среда (грабли)
- Ключи (HF_TOKEN и др.) в этой сессии НЕ видны: контейнер создан до их
  добавления. Новые сессии получат из Environment variables.
- gradio_client работает и анонимно (Z-Image-Turbo /generate жив).
- Gmail MCP подключился (search_threads, create_draft, labels).

## Незакрытое
- HF-видео-пролёт со scroll-scrub для секции процесса (ждёт команды).
- 3D из фото техники (кредиты Higgsfield), Mixamo-пак (ручное скачивание).
- data.json в pandago-order пачкается при локальных тестах: перед коммитом
  возвращать из b93868d.

## Дополнение (вечер): AI-кино в сайте
- HF-пайплайн отработал АНОНИМНО: Z-Image-Turbo дал фотореал кадр А
  (ночной контейнерный хаб + чёрный электробайк, циан). Qwen-Edit не
  влез в анонимную квоту (240с) - кадр Б сделан кропом-наездом (PIL),
  wan FLF тоже не влез (180с > остатка), сработал Lightricks/
  ltx-video-distilled image-to-video (896x512, 4с) - настоящий
  долли-ин с параллаксом.
- Кодирование PyAV (libx264 есть!): GOP 4 для скраба, 470KB.
  Плейрайтовский ffmpeg-бинарь НЕ декодирует h264 - использовать PyAV.
- Секция #cinema: sticky 340svh, скраб currentTime без GSAP (rAF+lerp),
  прогресс-полоса, фолбэк постер при reduced-motion/saveData.
- Второй клип: text-to-video "Москва ночью + байк" - тёплые фасады
  против бренда: исправлено холодным грейдом по кадрам (numpy) + кроп
  центра. Луп 355KB - живой фон секции формы (IO, autoplay muted).
- HUD-этапы переведены с фиксированных долей на привязку к секциям
  (calcStages по offsetTop) - не съезжают при изменении контента.
- three-scene: ночная HDRI Poly Haven (dikhololo_night_1k) на десктопе
  для отражений байка, фолбэк RoomEnvironment.

## Дополнение 2: фотореал-фильм (фидбек Даниэля на качество)
- FLUX.1-Krea-dev (mcp-tools) работает анонимно и даёт НАМНОГО лучше
  Z-Image-Turbo: фура с квадроциклами и гидроциклом на мокрой трассе,
  студийный квадро. 1280x720, guidance 4.5, steps 28, seed фикс.
- Анонимная квота восстанавливается за часы: Wan i2v (zerogpu-aoti
  faster, steps 4) и Wan FLF (multimodalart) прошли. Qwen-Edit (240с)
  анонимно НЕ доступен никогда (per-request cap).
- Рецепт "разлетается на части": FLUX кадр А (собранный) + кадр Б
  (same seed + "exploded technical view, parts floating") -> Wan FLF
  А->Б = скраб разборки. Морф скрывает несовпадение деталей.
- Кино-сценарий сайта: 01 склад -> 02 расчёт -> 03 ПРИЁМКА (quad
  teardown scrub) -> 04 ТРАССА (truck drive scrub) -> 05 Монголия ->
  06 Москва. Гуанчжоу-хаб клип заменён (качество LTX слабое).
- initScrub() параметризован для N кино-секций.

## Интеграции заряжены (финал 07.07)
- `INTEGRATIONS.md` в корне — паспорт всех ключей и коннекторов (проверка + восстановление).
- Ключи: `secrets.env.b64` в git (разрешение Даниэля), plaintext в .gitignore.
- SessionStart-хук в `.claude/settings.json` расшифровывает b64 и подключает
  source-строку в ~/.bashrc и ~/.profile — env-переменные есть в каждом Bash любого чата.
- Указатель добавлен в CLAUDE.md. Коммит c833554.
- Осталось от Даниэля: CF-токен (шаблон Cloudflare Pages: Edit), GEMINI_API_KEY (опц.),
  кредиты Higgsfield, Mixamo FBX-пак, Adobe Marketing Connect.
