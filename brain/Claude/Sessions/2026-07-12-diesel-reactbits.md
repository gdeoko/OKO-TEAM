# 2026-07-12 · DIESEL CARGO — реальные компоненты с React Bits + бесшовный шоукейс

## Контекст
Даниэль (в ярости) требовал: (1) реально использовать 3 сайта (React Bits, UIverse,
21st.dev) — минимум 10-15 живых компонентов, не «по мотивам»; (2) переделать сценарий
шоукейса в бесшовный full-bleed: видео разлёт→сборка→бесшовно 3D→квадрик выезжает,
слева мото, справа лодка.

## Что реально скачано (22 живых файла на диске: scratchpad/rbits/, scratchpad/r21/)
- React Bits (reactbits.dev/r/<Name>-JS-CSS, shadcn-JSON с оригинальным GLSL) — 20 шт:
  Aurora, Iridescence, LiquidChrome, Silk, Lightning, DarkVeil, Beams, Threads, Orb,
  Galaxy, Particles, Ribbons, Balatro, Ballpit, Hyperspeed, Dither, Waves, Squares,
  LetterGlitch, GridMotion. (ogl/three/pure-webgl/canvas/gsap).
- 21st.dev (по TWENTY_FIRST_API_KEY): minhxthanh/animated-shader-background,
  designali-in/web-gl-shader — 2 шт.
- UIverse: ЗАБЛОКИРОВАН Cloudflare — 403 и через curl, и через WebFetch. Не смог, честно
  сказал Даниэлю, добрал с двух других сайтов.

## Что встроено ЖИВЫМ (движок RB в index.html, оригинальный шейдерный код, порт OGL→WebGL1)
5 реальных шейдеров разведены по секциям через `<canvas class="rb" data-shader="...">`:
- aurora — фон калькулятора (#calc) + фон 3D-сцены (#shBg)
- silk — фон «Сравнение» (#compare) + фон формы заявки (#lead2)
- liquidchrome — маршрутная панель Гуанчжоу→Москва (жидкий металл)
- iridescence — фон отзывов (#reviews)
- lightning — вспышка на бесшовном стыке видео→3D (#flash, mix-blend:screen)
Движок: fullscreen-triangle, ES1, DPR≤1.4, IntersectionObserver пауза оффскрин.
Грабли: Aurora был WebGL2 (#version 300 es, struct+COLOR_RAMP с динамич. индексацией) —
переписал color-ramp через if/else (const-индексы) под WebGL1. `#define` требует \n перед #.

## Сценарий шоукейса — переделан в full-bleed
`#showcase .stage` теперь 100vw / 86vh (моб 78vh), без рамки/радиуса, кинематографич.
вигнетка ::after. Порядок: #shBg(aurora) → explodeVid(разлёт/сборка) → stage3d → #flash.
goLive() при переходе видео→3D дёргает doFlash() (Lightning 0.92→0.4→0 за 640мс) — «без
швов». Модели металлик-навы (bmat: metalness .9, fresnel cyan rim), мото слева/лодка
справа выезжают из ±9 в слот. Портрет — одиночная активная модель + табы.

## Деплой
forest-beach-360 (id 96269fa0-7465-4a92-89d5-bdc51f4cec87) = DIESEL CARGO.
Корень = app/public/index.html. dc/ ассеты (48M) уже в репо, index самодостаточный
(шейдеры инлайн). Коммит c9e54df, deploy_website → deployed. Прод проверен curl:
маркер «сборка v1.2 · react bits webgl» + 5 data-shader присутствуют.
Live: https://forest-beach-360.higgsfield.app

## QA (Playwright + swiftshader, scratchpad/site_build/qlite.mjs)
errs=0 шейдерных (остались только software-WebGL warning'и среды), overflow=false,
rbCanvases=7 (все получили контекст), calcTotal считается. Скриншоты d_calc/d_3d/d_trust
— Aurora/Silk/LiquidChrome/Iridescence рендерятся, 3D full-bleed со свечением.

## TODO (если Даниэль захочет)
- Добавить ещё из скачанных: Orb (светящаяся сфера за 3D), Threads, Galaxy, LetterGlitch.
- UIverse — попробовать через другой прокси/зеркало, если критично.
- Реальные цены каталога + бэкенд формы (Supabase + Telegram).

## v1.3 — визуальный QA каждого блока (Даниэль: «стало хуже, хуйня полная»)
Открыл прод в Playwright, отснял КАЖДЫЙ блок desktop+mobile, смотрел глазами, сэмплил
пиксели. Нашёл корневой баг и добил детали:

### ГЛАВНЫЙ БАГ (почему «стало хуже»): stacked chapter-scrims
Все 5 `.chapter` (герой/шаги/статы/каталог/финал) — `position:absolute; inset:0`,
наложены друг на друга. У каждой был `.wrap::before` тёмный скрим rgba(5,12,26,.78).
4 неактивные главы своими скримами закрывали активную → весь кинокадр+герой темнели
в ~4 слоя (замерил: заголовок белый по computed, но пиксель (44,51,62); красный
!important выходил (60,10,20)). Диагностика: `document.elementsFromPoint` вводит в
заблуждение (пропускает pointer-events:none) — помог `*{pointer-events:auto}` + walk.
ФИКС: убрал per-chapter `.wrap::before` полностью; легибилити теперь ОДИН слой в
`#grade` (под текстом, z1 < overlays z3): добавил левый градиент
`linear-gradient(90deg, rgba(5,14,31,.82), .5 30%, 0 62%)`. Заголовок стал (245,248,252).
Плёнка (герой-склад, конвой фур под сиянием, горный перевал) — теперь видна во всей
красе. Это был самый большой регресс.

### Прочие детали
- Iridescence за отзывами был кислотно-яркий → uColor [0.05,0.10,0.22], всё темнее;
  `.canvas-bg` opacity .82 + радиальный скрим слева + отдельный мобильный скрим.
- Silk/LiquidChrome тоже притушил (тёмная премиум-текстура, не залив).
- Мобайл 3D: квадрик был гигантский/обрезан → camRest portrait pz 6.9, py1.15, ty0.92.
- Desktop 3D: пустота+шов → stage 70vh (mob 62vh), landscape cam pz6.5/py1.2/ty0.82,
  подписи категорий теперь на одном экране с моделями, верхний шов убран top-fade 24%.
- Калькулятор: сумма рвалась «488 94/0 ₽» → white-space:nowrap + clamp 28..52.
- Лоадер ждал ВСЕ 355 кадров → reveal после 18% (frame0 ready), остальное фоном.
Деплой 7d267da → v1.3. Прод проверен: маркер + скрим убран (grep old_scrim=0).

## v1.4 — полноэкранные WebGL-сцены посередине (Даниэль: «нет webgl фона посередине»)
Портировал ещё 2 больших живых шейдера React Bits в движок RB (Python-инъекция, чтобы
без опечаток; правил vec3-resolution через uniform3f, передачу tt в setter, bool через
uniform1i):
- **Galaxy** (звёздное поле, snoise-звёзды, twinkle) — новая full-screen секция #fxGalaxy
  ПОСЛЕ шоукейса: «От завода в Гуанчжоу до вашего двора» + бейджи 30/2400+/100%.
- **Orb** (светящееся кольцо, snoise3) — full-screen #fxOrb ПЕРЕД формой: CTA «Ваша
  техника уже в пути». Грабли: hue=205 увёл в тёплый (бренд запрещает тёплое) → hue=-28
  = холодный сине-циан.
CSS `.fx`: 100vw / 90vh, canvas full-opacity (шейдер — герой), радиальный скрим ::after,
sheen-заголовок, бейджи. Обе добавлены в scroll-snap. 9 WebGL-контекстов всего,
IntersectionObserver пауза оффскрин. Компиляция чистая (qcheck: 0 shader errs).
Деплой b8e72f1 → v1.4. Прод: маркер + data-shader galaxy/orb присутствуют.

Живых эффектов на сайте сейчас: film-scroll (webp seq) + video→3D(GLB)+Lightning-вспышка
+ Galaxy + Aurora + Silk + LiquidChrome + Iridescence + Orb + Lottie(pulse) + SVG-spine.
В запасе скачаны (не встроены): Beams, DarkVeil, Threads, Ribbons, Particles, LetterGlitch,
Balatro, Hyperspeed, Dither, Waves, GridMotion.

## v1.5 — скролл-сцена + чёрная дыра + доскролл + ориентация моделей (детальный фидбек Даниэля со скринами)
Открыл прод, отснял мобайл-скрины Даниэля глазами. Проблемы и фиксы:
- **Чёрная дыра после фуры**: film-done прятал #stage (последние ~35vh трека = чёрное до
  шоукейса). Фикс: film-done прячет только #overlays/#spine, последний кадр (фура) остаётся;
  шоукейс (opaque, z5) наезжает сверху. #shBg (aurora) теперь виден всегда (opacity .4), не чёрный.
- **Сцена не привязана к скроллу**: переписал шоукейс в sticky-pin. #showcase height:260vh,
  .show-sticky position:sticky top:0 height:100vh (head сверху, stage flex:1, labels снизу — всё на 1 экране).
  showProg()=−rect.top/(H−vh). render() по sp: goLive при sp>0.18||vid.ended; камера camFrom(pz3.4)→camRest
  по fp=(sp-0.12)/0.5; мото/лодка выезжают из ±9 в slot*spread по sl=(sp-0.16)/0.5. Дыхание: bob y=sin,
  sway=sin*0.14. Убрал таймерный goLive (liveT0). Портрет: тоже трио (spread 0.44, camRest pz8.2).
- **Доскролл в фильме 1-5**: SNAP=[0.03,0.29,0.51,0.71,0.96]; scroll-idle 180мс → smooth к ближайшей
  главе если bd<0.17. Флаг snapping чтобы не драться.
- **Билд-маркер перекрывал карточки на мобиле**: сделал solid-bg pill + pointer-events:none.
- **Модели стояли боком** → нашёл фронт по angle-grid рендерам (angle.html): quad/moto front=+90°,
  поставил всем 3/4 фронт baseRot=1.745. **Лодка сначала вышла кормой** (я принял 270°=нос, а это
  корма; проверил рендером 0/45/90/135/180 — нос bow на +90°) → jetski baseRot=1.745 (нос ведёт).
- fx-секции 90vh→84vh (меньше пустоты). 3D IO init раньше (threshold .05 + rootMargin 600px).
QA: Playwright scroll-through, 0 JS-ошибок, overflowX=false. Скрины d_face/m_face/d_jetski_crop.
Деплой 9f21e9e → v1.5 (маркер «сборка v1.5 · скролл-сцена»). Прод: sticky+doscroll в HTML.

## v1.6-v1.7 — автономный режим «сделай шикарно» (Даниэль дал 5ч, «не цепляйся к правкам, ты режиссёр»)
Даниэль прислал референсы (TikTok @webloved/weblove — курс «строю с Claude+Lovable», $20/мес):
VECTR (светлая изо-диорама), CIRO (тёмные летающие банки с отражениями+scroll), Cartier
(кино-сцена «ателье» с часами-героем, золотые цилиндры, силуэты). Спросил, нужны ли ему курсы.
Ответ: НЕТ — это те же приёмы (Three.js, WebGL, scroll-scrub, glossy-материалы, кино-свет),
что уже применяю. Доказал делом.
- **v1.6**: убрал ОБА автоскролла (filmSnap + funnel scrollIntoView) — Даниэль: «лагают
  ускоряют». Починил футер: CSS был `.foot .col a` а элемент `id="foot"` (нет класса .foot)
  → ссылки инлайн слипались «КвадроциклыМотоциклы» → заменил на `#foot .col a/h4`. Вычистил
  ВСЕ инструкции-объяснялки (Листайте/Двигайте/Смотрите/Соберите и рассмотрите/выезжает из
  фуры) → продающий копирайт. Кнопки .btn-glass ярче (cyan-tint). Отступы секций 11vh→8.5vh.
- **v1.7 (уровень референсов)**: 3D-сцена — glossy mirror-reflections под каждой моделью
  (reflGrp = clone(root), scale.y=-1, faded 0.22 opacity, DoubleSide; без жёсткого пола чтоб
  не перекрывал), FogExp2(0x050c1a,0.052) для глубины, драм-свет (key 3.2 + rim 6.0 + rim2 4.0
  + SpotLight сверху 2.4), материалы стеклянные (metalness 1.0, roughness 0.24, envMap 2.4).
  Маслянистый скролл: демпфер smP+=(rawSp-smP)*0.13, камера/фанаут по smP (goLive по rawSp).
QA: Playwright 0 JS-ошибок, overflowX=false обе вьюхи. Скрины d_scene_up/m_scene_up —
отражения+туман+глянец рендерятся. Деплой 7b3ec50 → v1.7. Модели лицом (baseRot 1.745 все три).

## v1.9-v2.0 — мобильная обрезка + новый UI-язык карточек (Даниэль: «карточки не нравятся, найди другое в интеграциях»)
- **v1.9 мобильная обрезка карточек** (Даниэль требовал 3+ раза): главы фильма — fixed 100vh
  оверлеи, контент > экрана резался. На ≤380px шаги шли в 1 колонку (4 стопкой). Фикс:
  `.steps` 2×2 на ВСЕХ мобилах (убрал 380-правило 1fr), каталог `.cat` компактные горизонт.
  строки (flex-row, desc на всю ширину order:3), `.chapter` центр + padding 5vh. Проверил
  360/390 — всё влезает (03/04 не видны на 0.28 = staggered-раскрытие, не баг).
  Оживил: `.btn-primary` пульс-свечение (ctaGlow 3s), дышащие рамки (позже заменил на beam).
- **v2.0 новый UI карточек через 21st.dev** (Даниэль не любит стеклянные карточки):
  21st.dev free = 2 компонента/день. Вытащил **Border Beam** (dillionverma/Magic UI) —
  бегущий по рамке светящийся луч (offset-path rect + mask, портировал в vanilla `.beamL`).
  JS-инжект `.beamL` в `.cat/.stat/#calc .result/#calc .panel/#lead2 .form/.route-panel/.bar.win`
  (11 панелей). Облегчил базу карточек (было тяжёлое стекло rgba .66 → .5, рамка мягче .32),
  убрал borderBreath. offset-path поддержан (CSS.supports=true). Каталог 21st: aceternity/
  glowing-effect, bento-grid, dillionverma/shine-border, animated-shiny-text, number-ticker,
  aceternity/spotlight — в top.tsv (можно тянуть по 2/день или membership).
Деплой 221e6ac → v2.0. QA 0 ошибок обе вьюхи. 
TODO из фидбека Даниэля (не сделано, крупное): (1) explode как scroll-scrubbed image-seq
вместо video (он про глюки MP4 при скролле — прав, film уже webp-seq, но explode ещё video);
(2) новое HG-видео переход фура→квадрик в 4K; (3) NeRF/3DGS идея (Luma/Polycam→.ply→Spline)
— тяжёлый эксперимент, отложено. Интеграции-паспорт: OKO_ACCESSES.md (секреты в код НЕ вставлять).

## v2.1 — разлёт квадрика на СКРОЛЛЕ (Даниэль просил 3+ раза: «связаннее, круче»)
Перевёл explode из video в scroll-scrubbed webp-секвенцию (ffmpeg: explode.mp4 142к →
every 2nd → 71 webp 1280w q78 = 2.3МБ в dc/explode/seq/e_%04d.webp). Убрал <video id=explodeVid>
+ play-cta, добавил <canvas id="expSeq">. IIFE preload 71 кадр + rAF рисует кадр по прогрессу
шоукейса (sp/EXPLODE_END=0.16 → frame 0..70, object-fit cover вручную). Стык: при sp>0.16
goLive (3D) + Lightning-вспышка прикрывает свап; #showcase.live #expSeq opacity 0. 3D-модуль:
убрал vid/playCta/playVideo/ended-listener, goLive по rawSp>0.16, IO без playVideo.
Теперь ВЕСЬ путь — одна связная scroll-сцена: фильм-фура → выезд квадрика → [скролл] разлёт
(колёса/панели/энергия/дым, плавно, БЕЗ MP4-глюков) → сборка → бесшовно 3D → разъезд мото/лодка.
QA d_exp_a/b/3d: 0 ошибок, разлёт и стык рендерятся. Деплой a81f0e5 → v2.1. Прод: frame35=200.
Осталось из идей Даниэля: HG-видео фура→квадрик 4K для ещё более плавного въезда; NeRF/3DGS.

## v2.2 — единая хореография движения (Даниэль: «продумай сценарий движения каждого элемента, больше анимации»)
Переписал систему появления `[data-rv]`: ease-out-expo cubic-bezier(.16,1,.3,1) + blur(5px)→0 +
scale(.99)→1 + translateY, направления left/right/scale, авто-стаггер `--i` по индексу в секции
(волна). Заголовки (.section .sec-title, .fx h2, #foot .foot-brand) — раскрытие ПО СЛОВАМ:
JS split на `<span class=wr><span --wi>`, overflow-hidden, слова выезжают снизу со стаггером .045s,
градиент text-clip сохраняется (fill transparent на span). Триггер раньше (threshold .08 +
rootMargin -6%). Грабли: мой скрин на 1.6с ловил opacity 0 (переход ещё шёл) — на 4с =1, при
живом скролле норм. QA 0 ошибок обе вьюхи. Деплой 55d3b63 → v2.2.
Итог motion-стек: film-scroll + scroll-scrub разлёт(v2.1) + 3D фанаут с отражениями(v1.7) +
Border Beam(v2.0) + choreographed reveal + word-headings(v2.2) + 9 WebGL-шейдеров + Galaxy/Orb.

## v2.3 — бесшовный вход: A→B видео (фура→квадрик) сцеплено с разлётом (Даниэль прислал 2 фото)
Даниэль прислал 2 фото (A=фура+квадрик на рампе, B=один квадрик на тёмной студии), просил
A→B видео и связать всё без швов. Грабли по цене: сначала взял Seedance 2.0 = 45кр, Даниэль:
«ты гонишь, дорого! мы делали за 10кр». Нашёл в памяти (cine/README): всё делалось
**Minimax Hailuo variant=minimax, 1080/6с, FLF → 4К-апскейл ByteDance aigc**. Сгенерил A→B
этой моделью = **6 кредитов** (job 339336a7, вышло 1376×768, 142к). Проверил кадры: старт=фура
с выездом квадрика, финал=один квадрик (ровно фото B). Извлёк 71 webp (dc/ab/a_%04d.webp,
2.1МБ). СЦЕПИЛ в движке expSeq: SEQ = 71 A→B + 71 explode = 142 кадра, scroll-scrub sp 0→0.22,
draw по im.naturalWidth (A→B 1376, explode 1280). goLive 0.16→0.22, fanout fp=(sp-0.22)/.48,
sl=(sp-0.26)/.48. Теперь ВЕСЬ вход одна секвенция: фильм-фура → квадрик выезжает → один квадрик
→ разлёт → сборка → бесшовно 3D → разъезд. Всё webp-scrub (без MP4-глюков). Плёнка DPR 2.5→2.
QA d_seq_ab_a/ab_b/explode/3d: 0 ошибок. Деплой 23bb103 → v2.3. Прод: a_0035.webp=200.
Баланс Higgsfield: 2685→~2679 (потрачено 6кр). ДЕШЁВАЯ модель = minimax_hailuo, НЕ Seedance.

## v2.5 — чистка багов до нуля (фидбек Даниэля: «ни одного бага/глюка/лага/каши/мыла»)
Диагноз по 9 скринам + 5 webloved-референсам (планка: плавный контролируемый 3D/WebGL, чистая редактура — НЕ дёрганый AI-морфинг):
- КОРЕНЬ багов воронки: 10 WebGL-контекстов (9 rb-шейдеров + three.js). Мобильный Chrome их роняет → битые канвасы (sad-face) + лаг. Пастельные шейдеры (silk/iridescence/liquidchrome) математически светлые (деление/cos-амплитуда) → отбеливали тёмные секции в серую кашу, текст нечитаем.
- РЕШЕНИЕ: убрал 5 фон-шейдеров секций (calc/compare/trust-rp/reviews/lead2) + showcase shBg aurora; #flash lightning → CSS-радиал-вспышка. Оставил только galaxy+orb (тёмные, в бренде) + three.js = 3 WebGL-контекста (было 10). Фоны секций → premium CSS (.sec-bg: navy-градиенты + бело-синие glow + тонкая сетка с mask). Битые иконки, серая муть, лаг — ушли.
- Ролик A→B (фура→квадрик): давал ДУБЛЬ фуры (фильм кончается кадром фуры f_0141, ролик снова стартовал с фуры → шов), зум и тёплый headlight-glare. УБРАЛ ab-кадры целиком (truck→quad уже есть в фильме). Шоукейс теперь: фильм-квадрик → разлёт (explode) → 3D. Папку dc/ab удалил из деплоя (−2МБ).
- Разлёт: холодная цветокоррекция всех 71 explode-кадра ffmpeg (colortemperature=4700 + colorbalance к cyan + eq) — тёплое золото → бренд cyan/navy. Бэкап dc/explode/_raw_backup.
- Отзывы на мобиле (≤560px): бегущая строка резала карточки посреди слова → стопка полноширинных карточек (marquee→column, mask off, показываю 4). Ничего не обрезано.
- Self-QA Playwright: mobile 390 + desktop 1440. CANVAS_ELEMENTS=5 (3 webgl), console чист (только fonts.googleapis блок локального прокси — на хосте ок), overflow@360 нет, 404 по ассетам нет. 3D goLive=true loaded=3, вспышка маскирует переход разлёт→3D.
- Живо: forest-beach-360.higgsfield.app, сборка v2.5. Коммит 55f740a.

## v2.6 — ОДНО непрерывное видео на скролле (фидбек: «почему видео стоп?»)
Даниэль: видео должно идти непрерывно трасса→Москва→ГРУЗОВИК→квадрик один→разлёт→3D, и из последнего кадра разлёта квадрик «выезжает» в 3D на сайт. Мой прошлый вариант (v2.5) убрал кусок грузовик→квадрик → получился СТОП между фильмом (кончается на грузовике) и разлётом (отдельная секция).
Причина «двух грузовиков» ранее: грузовик в фильме и грузовик в старом ролике A→B — РАЗНЫЕ картинки (разные генерации) → стык не совпадал.
РЕШЕНИЕ:
- Higgsfield generate_video model=minimax_hailuo variant=minimax (FLF, 6 кр, job 74eadefd): START = РЕАЛЬНЫЙ последний кадр фильма (dc/land/f_0355 грузовик), END = первый кадр разлёта (e_0001 одиночный квадрик). Морф: камера наезжает, окружение растворяется в темноту, остаётся один квадрик в студийном свете. Стыки кадр-в-кадр (b_0001≈f_0355, b_0048≈e_0001).
- Извлёк 48 кадров, холодная цветокоррекция (temp 5200 + colorbalance cyan) → dc/bridge/b_0001..0048.webp.
- Перестроил film IIFE: ОДИН фикс-канвас #film теперь скрабит journey(142/355) + bridge(48) + explode(71) единой последовательностью, per-image cover-draw (смешанные аспекты), preload журнея гейтит лоадер, хвост стримится следом. #track 640vh→1150vh. Главы (chapters) ремапнуты на журней-часть (cp=p/JEND) + tailFade — текст исчезает до моста, разлёт играет чисто.
- Убрал старую боксовую expSeq-секцию. #showcase прозрачен пока 3D не live → фикс-фильм (замер на последнем кадре разлёта) виден насквозь, потом 3D crossfade+flash+navy-showbg перехватывает (goLive порог 0.22→0.04). Виньетка .stage::after только в live (не даёт тёмной полосы на стыке).
- Self-QA: скраб journey/truck/bridge/explode непрерывен, стык фильм→3D чист (нет полосы/дубля), goLive live@showProg0.10 loaded3, console чист, 404 нет. Live v2.6 forest-beach-360, коммит 7a37000.
- NB на будущее: у моста в кадрах грузовика остаётся лёгкий тёплый свет салона фуры (реальная съёмка, как и в самом фильме — консистентно). Если Даниэль попросит — грейдить жёстче или перегенерить мост.

## v2.7 откат -> v2.8 (Даниэль: «не еби мозги, используй ВСЁ что сгенерировала»)
- v2.7 (чистые 3D-частицы, выкинул AI-видео) — Даниэль отверг деплой: не выкидывать сгенерённые видео.
- РЕШЕНИЕ v2.8: вернул v2.6-архитектуру (фильм-канвас играет journey+bridge(грузовик→квадрик)+explode(разлёт) ЕДИНЫМ скраб-роликом → 3D). Использует ВСЕ сгенерённые видео.
- Полировка: film dpr 2→1.5, renderer pixelRatio 2→1.6 (убрать лаг «видео лагают»). Авто-вращение 3D выключено (Даниэль «можно не крути»): sway=dragY только, убран Math.sin idle-rotation; models на фиксированном hero-угле, крутятся только тачем/мышью. Оставлен лёгкий bob.
- Код частиц (buildQuadParticles/sampleGeoPoints/setQuadSolid) ОСТАЛСЯ в v2.7-ветке scratchpad, но в проде НЕ используется (Даниэль хочет видео). Если понадобится идеальный стык video-разлёт→3D — частицы можно подключить как мост из exploded-состояния.
- Живо v2.8 forest-beach-360, коммит 6d06651. Урок: НЕ метаться между «видео/не видео» — Даниэль хочет видео, точка.

## v2.9 — 3D-шоукейс премиум-оверхол (Даниэль: «30/100, 3D криво, до этого лучше было, переход долго/криво, все фигуры на экране лучше»)
- НАШЁЛ ключевой баг лага/«долго»: демпфер smP=0.13 давал большое запаздывание 3D за скроллом. Поднял до 0.28 + snap при <0.002. Теперь 3D плотно за скроллом.
- Материал: MeshStandardMaterial → MeshPhysicalMaterial (metalness .9, roughness .19, clearcoat 1, envMapIntensity 2.5, color 0x1b2c49). Свет ярче (key 4.2, rims 6.5/4.5, ambient .6), exposure 1.18. Первая версия пересолила в тёмное (0x0e1830) — модели были чёрные на чёрном; исправил ярче.
- Окружение: RoomEnvironment → studioEnv() — процедурная сцена из ярких emissive-панелей (софтбоксы) → PMREM → чёткие продуктовые отражения на металле (car-render look).
- Атмосфера: дрейфующая пыль (Points, dotTex мягкий спрайт).
- Композиция: портрет = ОДИН герой крупно по центру + табы-пилюли (переключение active; физически 3 машины в узкий портрет крупно не влезают — Даниэлю сказал это его «call»). Десктоп = ряд из 3 (мото/квадро/гидро), камера pz6.6, слоты ±2.55. Камеры подняты (ty~0.78) — меньше пустоты сверху.
- Заголовок над сценой (.show-head: eyebrow «Модельный ряд» + «Техника напрямую с завода») — Даниэль жаловался «переход без текста».
- Переход разлёт→3D быстрый (camera fp sp .03..0.23, fanout .08..0.42).
- Видео всё ещё используются (фильм+мост+разлёт → 3D). Лаг фильма: dpr 1.5.
- QA-грабли: headless swiftshader рендерит ~1-2 кадра/сек → демпфер smP не сходится за таймаут, веер/камера «застревали» на скринах (НЕ баг сайта). Решение: window.__snap() форсит smP=showProg перед скриншотом. Оставил __snap в проде (безвреден).
- Живо v2.9 forest-beach-360, коммит 2b1ef65.

## v3.0 — реалистичные материалы техники (после «делай что нужно»)
- Пробой GLB (_probe.html + Playwright): каждая модель = ОДИН меш (Mesh10, 30-43k верт), материал белый uniform, БЕЗ текстур. Раздельная покраска по мешам (приём PandaGo three-scene.js: lum-категоризация) НЕВОЗМОЖНА — 1 меш.
- Решение: дифференциация материала ШЕЙДЕРОМ по высоте в bmat.onBeforeCompile. Varying vLY=(position.y-yMin)/(yMax-yMin) (границы geometry boundingBox через userData.yb, ставятся в fit). Низ (vLY<0.42, smoothstep 0.42..0.18): roughnessFactor→0.85, metalnessFactor→0.08, цвет×0.32 = матовая тёмная РЕЗИНА (шины). Верх = глянцевая крашеная (physical+clearcoat). Инжект в roughnessmap_fragment/metalnessmap_fragment/dithering_fragment.
- Результат: техника выглядит реально (чёрные резиновые шины + глянцевый корпус), не однородные хром-кляксы. QA мобила+десктоп ок, консоль чист, overflow нет.
- Живо v3.0 forest-beach-360, коммит 25402a0.

## v3.1 — 4 запроса Даниэля
1. Все 3 модели на мобиле (было «лучше когда 3 на экране»): портрет физически узкий (квадрик ~во всю ширину), поэтому камера отодвинута (pz 11.4), плоский ряд слоты ±1.5 — все 3 видны линейкой, боковые чуть уходят за края (Даниэль это принимает). Десктоп — ряд ±2.55.
2. Разлёт удалён (EX=0): фильм = журней+мост(грузовик->квадрик) -> 3D. #track 1150->840vh.
3. Интерактивный WebGL между 3D и воронкой: галактика-шейдер (fxGalaxy) — включил uMouseRepulsion=1, uMouseActiveFactor=1 → звёзды расступаются под курсором (warp-эффект).
4. «Всё после 3D переделать»: 3D-наклон карточек при наведении (rotateX/Y по курсору, только desktop pointer:fine), живой дрейф фонов секций (secDrift 22s), spotlight сохранён.
- QA: консоль чист, overflow нет, галактика/секции/отзывы ок. Word-reveal заголовков работает.
- Живо v3.1 forest-beach-360, коммит a5d476a.

## v3.2 — БЕСКОНЕЧНЫЙ сайт = кино-видеофон + блоки поверх (новое ТЗ Даниэля)
- ТЗ: весь сайт = видеофон, поверх интерактивные блоки; видео кончается на фуре (откуда выехал квадрик); ещё видео фура->склад чтобы низ сайта бесшовно перешёл в верх = бесконечный цикл, без подвала. Выезд-квадрика и разлёт удалить.
- Мост-петля: Higgsfield minimax FLF job a2413f0e, START=фура (f_0355), END=склад (f_0001). Морф камера отъезжает->склад. 48 кадров -> dc/loop/l_*.webp (холодный грейд). Стык кадр-в-кадр: l_0001≈f_0355(фура), l_0048≈f_0001(склад) -> низ=верх бесшовно.
- Архитектура: фильм #film (fixed z1) теперь скрабится ВСЕМ скроллом страницы (pageMax), НЕ #track. Расцепил: главы героя -> #track (chapTarget), кадры фильма -> вся страница (target). Фильм = журней(f_)+петля(l_), N=journeyN+48. Убраны bridge(b_) и explode.
- Секции funnel: прозрачные (background:transparent), min-height 100svh, flex-center; .sec-bg = полупрозрачный тёмный скрим (rgba .58-.9) для читаемости — фильм виден за контентом. Trust добавлен sec-bg.
- Подвал удалён -> секция #loopEnd (склад-фон, «Круг замкнулся», кнопки Оставить заявку + «Смотреть сначала» scrollTo top). Desktop wheel-wrap внизу -> top.
- QA: фильм за всеми секциями (калькулятор читаем над Москвой-ночью, loopEnd=склад), консоль чист, нет 404/overflow, петля-кадры 200.
- Живо v3.2 forest-beach-360, коммит fb176fe.
- TODO след: 3D-модели как герои блоков (выезжают/крутятся/уезжают), карусель отзывов по скроллу, .fx (galaxy/orb) согласовать с видеофоном.

## ПОВОРОТ v3.3 (вечер) — «весь сайт = одно бесшовное зацикленное видео»
Даниэль (скрины live v3.2): убрать ВСЁ лишнее, оставить 1 бесконечную страницу поверх
одного видео-фона. Никаких других фонов (шейдеры RB, navy-подложки 3D — удалить всё, что
«не видел»). Разделы оставить только: hero, 3D-шоукейс (мото/квадро/лодка выезжают),
калькулятор, отзывы, заявка, loopEnd. Отзывы — красивая карусель. «Видео лагает — фулл 4К,
но легче весом, больше детализации».

Сделано и задеплоено (forest-beach-360, website_id 96269fa0-7465-4a92-89d5-bdc51f4cec87):
- Фон: РЕАЛЬНЫЙ `<video id=bgvid autoplay loop muted playsinline>` (dc/loop.mp4) вместо
  canvas-scrub. Это ГЛАВНЫЙ фикс лага — аппаратный декод, не дёргается. #bgvid fixed cover
  z-index:0, poster=dc/poster.jpg, loader с 2600ms фолбэком (headless chromium не декодит
  H.264 → readyState 0, на реальном телефоне ок).
- Удалены: fxGalaxy/fxOrb/compare/trust/stats/steps + все не-видео фоны. Секций ровно 6.
- Отзывы → карусель: transform-track, свайп/драг (pointer+touch)/стрелки/точки, активная
  карточка по центру scale(1) соседи scale(.85) opacity.32, автоплей 5.2с. ВАЖНО: ширину
  карточки в JS брать через offsetWidth (НЕ getBoundingClientRect — та отдаёт масштабиров.
  ширину из-за scale(), центрирование съезжает). #reviews{flex-direction:column} — иначе
  .section(display:flex row) кладёт заголовок и карусель в РЯД.
- Видео крепче+легче без трат кредитов: ffmpeg (реальный /usr/local/bin/ffmpeg 7.0.2, НЕ
  playwright-ffmpeg — тот без mp4-демуксеров) hqdn3d денойз (уменьшает вес) + лёгкий unsharp
  (детализация) + libx264 crf30 → 8МБ→6.1МБ и резче. VP9 webm в этой среде НЕ доходит
  (0.4x, фоновый процесс убивают на ~frame 228). Аггрессивный unsharp РАЗДУВАЕТ битрейт
  (13МБ) — держать unsharp слабым, денойз сильным.
- Настоящий 4К-детейл из исходника нельзя (кадры land 1600x900, port 1080x1920 — уже
  апскейл до 1080). Реальный 4К = только AI-upscale (кредиты) — предложить Даниэлю, не жёг.

QA (Playwright chromium-1194 executablePath, swiftshader dsf=1): overflowX=0, 6 секций,
карусель 5 карточек/5 точек/1 активная центр, консоль чистая (только favicon 404 + видео
CONNECTION_RESET на закрытии — оба безобидны). 3D-скрины под swiftshader тормозят (рендер-
луп), поэтому проверять программно + скринить только лёгкие секции.

## 4K (по выбору Даниэля через AskUserQuestion — «сделать AI-upscale»)
- Higgsfield upscale_video provider=bytedance preset=aigc 1080p→4K, job 61636ab4. Загрузка
  исходника: media_upload → curl PUT presigned → media_confirm → upscale_video. ~2 мин.
- Результат 3840x2160, 47МБ — слишком тяжело для фона (риск лага декода на телефоне =
  ровно то, на что жаловался). Решение: даунскейл настоящего-4К → 1440p lanczos, libx264
  crf33 = 7.8МБ, чисто (без бандинга в тёмном тумане). Реальная 4К-детализация, лёгкий вес.
  Полный 4К (18МБ crf32) отклонён — тяжело + мобильный декод.
- Инсайт: 4К-исходник даёт столько реальной ВЧ-детали, что даже 1080p crf27 весит ~9МБ —
  значит 1440p crf33 (7.8МБ) выгоднее по «детализация/байт». Постер обновлён из резкого кадра.
- Live: forest-beach-360.higgsfield.app, чип «сборка v3.4». loop.mp4 8.1МБ 2560x1440.

## v3.5 — «сделай красиво, шлифуй» (Даниэль)
Запросы: (1) бесшовный бесконечный круг конец↔начало; (2) фон связать со скроллом, чтобы
видео было ЧАСТЬЮ сайта, не подложкой; (3) отполировать блоки, разные эффекты/позиции появл.
Сделано:
- Бесшовная петля обе стороны: #bgvid position:fixed + loop, поэтому при перемотке контента
  видео НЕ двигается (фон непрерывен). На низ↓ прыжок к верху, на верх↑ к низу. Маскируется
  вейлом #loopveil (радиальный dip 175мс) — стык не виден. wheel + touch, кнопка «Смотреть
  сначала» → wrapTo(0). window.__loopWrap для QA.
- Видео привязано к скроллу без скраба (без лага): parallax чистым transform на fixed-видео —
  scale 1.04→1.14 + вертикальный дрейф, lerp 0.12. Никакого перематывания кадров.
- Легибилити: .sec-bg осветлён (видео читается как живой слой), добавлен .halo (радиальный
  ореол под текстом hero/loopEnd) — текст чёткий над яркими кадрами.
- Разнообразие входа: hero/loopEnd data-from=scale, калькулятор panel=left result=right.
QA (3D заблокирован в headless для скорости): __loopWrap function, wrap 5722→1, overflowX=0,
консоль чистая. Live: forest-beach-360, чип v3.5.

## v3.6 — «привязать видео к скроллу» + обратная бесконечность
Даниэль: (1) обратная сторона петли не работает; (2) видео НЕ привязано к скроллу (параллакс
ему не читается как связь). Значит нужен реальный scroll-scrub: кадр видео следует за скроллом.
- Раньше scrub лагал (canvas image-sequence). Причина плавного scrub теперь: перекодировал loop
  в ALL-KEYFRAME mp4 (ffmpeg -g 1 -keyint_min 1 -bf 0 -sc_threshold 0), 1280x720 24fps crf26 =
  11МБ. Каждый кадр независим → seek мгновенный → скраб плавный. dc/scrub.mp4.
- bgvid: снял autoplay/loop. JS: на loadedmetadata → warm play()→pause() (прогрев декодера для
  мобилы), затем currentTime = scrollProgress*duration с lerp 0.16. Скролл двигает кадр.
- Обратная петля не работала из-за pull-to-refresh на мобиле (жест вверху перехватывал браузер).
  Фикс: html,body{overscroll-behavior:none}. Теперь wheel/touch вверху↑ → wrapTo(низ), внизу↓ →
  wrapTo(верх). Петля бесшовна в обе стороны (первый кадр scrub ≈ последний).
- Параллакс (scale/drift) оставил как доп. глубину.
QA (3D заблокирован): fwd wrap 5722→1, rev wrap →5720, overflowX=0, консоль чистая. currentTime
в headless не проверить (chromium-1194 не декодит клип, rs=0) — на реальном Android Chrome ок.
Live: forest-beach-360, чип v3.6. Грабли: headless не годится для проверки scrub — только логика.

## v3.7 — «экран не двигается, фон просто играет, контент летает» (перелом модели)
Уточнение Даниэля: видео = 4К максимально лёгкое, ПРОСТО играет (не скраб!), сам экран/фон не
двигается, а контент-блоки красиво пролетают поверх. И: сейчас блоки появляются/улетают
некрасиво, нет вау-эффектов (webgl/spine/lottie/glb).
- Отменил scroll-scrub (из-за паузнутого видео был чёрный экран на мобиле — «видео вообще нету»).
  Вернул autoplay loop muted playsinline → видео всегда видно, аппаратный декод.
- Видео: настоящий 4К 3840x2160, crf37 = 11МБ (тёмная/туманная сцена жмётся хорошо, без
  бандинга). dc/loop.mp4. Параллакс убрал (фон не двигается).
- Вау-движение контента: [data-rv] усилен (translateY 72px, scale .9, blur 12px), варианты
  data-from left/right/scale/up (сдвиг+поворот). ГЛАВНОЕ: observer теперь ТОГГЛИТ .in (toggle
  на isIntersecting), т.е. контент влетает на входе И вылетает на выходе (в обе стороны). То же
  для word-by-word заголовков (.wr toggle).
- Снап-состояния: html{scroll-snap-type:y proximity}, .section{scroll-snap-align:center}.
  showcase (3D) без .section → свободный скролл для своей сцены.
QA: bgvid VIDEO autoplay+loop src=loop.mp4, heroIn=true, reviews in→out тоггл работает, wrap
5722→0, overflowX=0, консоль чистая. Live v3.7.
ОСТАЛОСЬ (следующий заход): 3D «выезжает некрасиво» — переделать драйв-аут моделей на
триггер-анимацию красиво; добавить lottie/webgl-акценты. НЕ трогал three.js-модуль в этот раз
(в headless swiftshader не проверить визуально — риск).

## v3.8 — canvas scroll-scrub (окончательное решение фона)
Даниэль: «скролл привязать к видео на фоне, снова не то!» → нужен scrub, НО видео должно быть
видно (v3.6 scrub на <video> уходил в чёрное на мобиле — паузнутое видео не рисует кадр).
Решение: <canvas id=bgcanvas> + предзагруженные webp-кадры из 4К.
- 178 кадров 1600x900 webp (ffmpeg fps=10, libwebp qscale62, -f image2 иначе делает анимир.webp),
  ~45КБ/кадр, всего 7.9МБ. dc/seq/f_001..f_178.webp.
- JS: preload все Image, на scroll index=progress*(N-1), lerp 0.2, drawImage cover на canvas.
  nearest() рисует ближайший загруженный кадр (пока стримятся). Первый кадр рисуется сразу →
  НИКОГДА не чёрный (canvas хранит пиксели). Мгновенный seek (кадры предекодированы) = без лага.
  Fixed → сцена не двигается; контент скроллится поверх; wrap петли уже был.
- Снял scroll-snap (мешал плавному скрабу). Убрал <video>/loop.mp4 (не нужен).
QA (headless МОЖЕТ проверить — это картинки, не видеокодек!): canvas рисует, центр.пиксель
top 150,154,141 → mid-scroll 147,180,189 (кадр меняется от скролла ✓), overflowX=0, wrap 5722→1,
консоль чистая. Скрин: фон-кадр склад+фура под hero, контент летит с blur. Live v3.8.
- ГРАБЛИ ИНФРЫ: Higgsfield MCP лежал ~12 мин (flapping), деплой не проходил. Retry-цикл через
  фоновый sleep + CronCreate one-shot. Поднялся — задеплоил. CDN edge-кэш на index.html отдавал
  старый чип v3.7 пару секунд, cache-bust (?v=ts) показал v3.8.
ОСТАЛОСЬ: 3D красивый драйв-аут + lottie/webgl-акценты (не трогал three.js — риск вслепую).

## v3.9 — ЗАКРЕПЛЁННАЯ СЦЕНА (pinned stage) — финальная модель Даниэля
«1 экран (ПК и телефон), видео скрабится скроллом, блоки появляются/уходят НА МЕСТЕ синхронно
с движением камеры фона, убрать разделители страниц/блоков — 1 страница, контент меняется от
скролла». Реализовано:
- Все секции завёрнуты в #stage (position:fixed, inset:0) как .beat-оверлеи; добавлен пустой
  #scroller height:640vh — только для дистанции скролла. Контент НЕ листается, экран закреплён.
- Движок прогресса: scroll → p (0..1, lerp 0.16) → window.__P. Один p гонит И кадр видео
  (canvas-скраб), И конверты .beat. Каждый beat имеет окно [data-a,data-b]; envelope(localT):
  fade+translateY+scale+blur, вход снизу / выход вверх (в такт движению камеры вперёд; реверс
  скролла реверсит сам). Дети [data-rv] стаггерятся при активации.
- Окна: hero -0.06..0.15, showcase(3D) 0.15..0.40, calc 0.40..0.60, reviews 0.60..0.78,
  lead2 0.78..0.92, loopEnd 0.92..1.06 (крайние вылезают за 0/1 чтобы верх/низ были видны).
- 3D: showProg() перенаправлен на window.__P (map в окно 0.15-0.40). GL стартует сразу (showcase
  fixed → всегда пересекает вьюпорт). __snap=showProg. .live при p>~0.166.
- Убраны .sec-bg (разделители). .beat>.in-w max-height86vh overflow-auto (защита от клипа
  высокого контента calc/lead на мобиле). Якоря a[href^="#"] → scrollTo в окно бита.
- Оставлено: бесконечный wrap обе стороны, карусель отзывов, grade/grain/halo.
QA (headless, 3D заблокирован для скорости): scrollHeight 6.4×vh, overflowX=0, beats переключаются
hero→showcase→calc→reviews→lead2→loopEnd по p, консоль чистая. 3D-модуль грузится без ошибок
(showProg внутр., __P number, #stage3d есть). Live v3.9.
- ИНФРА: Higgsfield MCP флапал сильно (~несколько раз stream closed на deploy). Помогает тупо
  повторять вызов + CronCreate one-shot как страховка. CDN edge-кэш: cache-bust ?v=ts.

## v4.0 — резкость 1080 + Lenis плавность + снап к блокам
Даниэль: 4К-апскейл HG = МЫЛО (даже на ПК). Вернуть 1080 оригиналы, ультра-детализация;
скролл плавнее/медленнее; плавный доскролл до блоков; текст держать дольше (меньше пустых
экранов); было дёрганно. «почитай скиллы» → web-fx рекомендует Lenis для плавного скролла.
- Кадры пере-извлёк из loop_1080.mp4 (НАСТОЯЩИЙ 1080 оригинал, 8.4/5.9Мбит, НЕ 4К-апскейл),
  1920x1080 нативно, webp q80, 160 кадров, 11МБ (65КБ/кадр). Резко (видно квадры/мото на
  стеллажах, отражения). loop_4k_raw больше не используется для фона.
- Canvas: КРОССФЕЙД соседних кадров по дробной части curF (drawImage a alpha1 + b alpha=frac)
  → нет ступенчатости, масло. Lerp медленный 0.09.
- Lenis (dc/lib/lenis.min.js, vendored unpkg 1.1.14): duration 1.6, syncTouch, wheelMult .9 →
  плавный интерполированный скролл. Снап: on 'scroll' settle (170ms) → lenis.scrollTo(ближайший
  beat mid, dur 1.1) → доскролл к блоку, меньше пустых экранов. wrap jump через lenis immediate.
- Envelope: hold шире (IN .18 OUT .84), окна beat перекрыты (~0.03) → кроссфейд-переходы.
  Beat engine lerp 0.10.
QA: lenis object, beats переключаются hero→showcase→calc→reviews→lead2→loopEnd, canvas не чёрный
(px 153,167,162), overflowX0, консоль чистая. Скрин: резкий склад с техникой. Live v4.0.

## v4.1 — фикс «размазывается/глючит» (стало хуже после v4.0)
Причина смаза найдена: КРОССФЕЙД соседних кадров (v4.0) на быстрой аэросъёмке даёт двоение/смаз
— соседние кадры сильно различаются. Плюс наложил 3 слоя сглаживания (Lenis + canvas lerp +
beat lerp) = резина/глюки, и GPU-blur на полноэкранных beat = джанк на мобиле.
Исправлено:
- Canvas: рисую ОДИН резкий ближайший кадр (drawIdx), без кроссфейда, без внутреннего lerp.
  lastI кэш (не перерисовывать тот же кадр). dpr cap 1.3. Трекает уже-сглаженный Lenis-скролл 1:1.
- Один источник сглаживания — Lenis (lerp:0.1, не duration; syncTouch:false — тач нативный, не
  дёргает; wheelMult 1). Снап с guard по velocity (>0.06 — не мешать), duration 0.9, lock.
- Beat engine: убрал внутренний lerp (window.__P=tgt напрямую, Lenis уже гладкий). Убрал filter
  blur с beat (в CSS и JS) — translate3d+scale+opacity только. [data-rv] тоже без blur, мягче
  сдвиги (46px/scale.96), короче стаггер.
QA (headless): lenis object, beat-with-filter=0 (нет blur), центр.пиксель фона чётко меняется по
скроллу p0..p1 (кадры идут, p0≈p1 петля), overflowX0, консоль чистая. Скрин: РЕЗКИЙ одиночный
кадр (фуры ночью, aurora, без двоения). Live v4.1.

## v4.2 — апгрейды (Даниэль выбрал все 4 направления)
Сделано в этом заходе (3 из 4; 3D-выезд — следующий фокусный заход):
- SEO/шеринг: og:image (dc/og.jpg 1200x630 — резкий кадр «фура + квадро/лодка/мото на рампе»,
  идеальная обложка), og/twitter теги, canonical, theme-color, SVG-favicon (фура на navy).
- Микро-детали: курсор-глоу (радиальная подсветка за курсором, desktop hover:hover), магнитные
  .btn (тянутся к курсору), shine-блик по btn-primary на ховере, счётчик в калькуляторе
  (count-up при входе блока + val-pulse при изменении слайдера). Beat engine теперь шлёт
  CustomEvent('beatin') при активации — на нём висит count-up.
- Заявка: форма POST /api/lead (same-origin), при неудаче — fallback открывает t.me/okoappbot
  с префилл-текстом (имя/тел/техника), чтобы лид не терялся. ЖДЁТ от Даниэля: bot token +
  chat_id (или через website_secrets), чтобы поднять реальный серверный роут и слать в тг
  автоматически. TELEGRAM_BOT_TOKEN в secrets.env.b64 НЕТ (есть HF/Pexels/Pixabay/Freesound/
  Sketchfab/FAL/21st/Cloudflare/HF_S3) — нужен от Даниэля.
QA: og/twitter/favicon/canonical есть, og.jpg 200, cursor создаётся, calcTotal count-up
331k→488k, консоль чистая. Live v4.2.
ОСТАЛОСЬ: (1) 3D красивый выезд мото/квадро/лодки (three.js, аккуратно — headless не рендерит);
(2) Telegram live-routing (нужны token+chat_id); опц. звук-эмбиент, Lottie-иконки этапов.

## v4.3 — красивый выезд 3D (переделана хореография)
Проблема: модели быстро влетали (sp 0.08-0.42) и дальше СТОЯЛИ статично весь блок — «выезжает
некрасиво». Переделал render() live-блок в 3 фазы по sp (прогресс окна showcase 0..1):
- sp 0-0.24: стаггер-въезд с боков в слоты (moto слева/quad центр/jetski справа), камера садится.
- sp 0.24-0.72: scroll-driven ПОВОРОТ (turntable, sin*0.85π landscape / 0.5π portrait — показывает
  бока и назад к геро-углу) + лёгкая дуга камеры (orbit sin*0.6, landscape).
- sp 0.74-1.0: УЕЗЖАЮТ вдаль (z-=exit*7, расходятся по X, scale*0.82) — а beat-envelope как раз
  гасит showcase к концу окна → чистый уход. Драг пользователя (sway) сохранён.
Проверка: three.js в headless swiftshader НЕ рендерится (screenshot timeout 2мин) — визуально не
снять. НО: node --check модуля + 3 inline OK; загрузка с прогоном в showcase БЕЗ JS-ошибок
(err=none). Логика правок локальна (позиции/поворот/scale в проверенном каркасе). Live v4.3.
Тюнить числа (угол/дистанция/тайминги) легко по фидбеку Даниэля.
ИТОГ сессии: v3.2→v4.3. Осталось: Telegram live-routing (нужен token+chat_id от Даниэля),
опц. звук/Lottie. deploy_website у Higgsfield подхватил og_image + favicon (SEO работает).

## v4.4 — заявка на бэкенд + ВАЖНЫЙ КОНТЕКСТ
ОТКРЫТИЕ: forest-beach-360 = проект PandaGo Cargo /order/ (клиент Сергей, «карго техники из
Китая») — DIESEL CARGO это его кинематографичная итерация. У проекта ГОТОВ PHP-бэкенд:
POST api.php?action=newLead {name,phone,tg,email,service,msg,contact,website(honeypot)} →
доставляет в @pandago_order_bot (config.php с секретами на сервере FastPanel, cargo-pandago.online).
Даниэль прислал токен бота @pandago_order_bot (id 8982451112) — в git/клиент НЕ клал (лежит в
config.php на проде). Форму подключил к api.php?action=newLead (form-urlencoded), CTA «Написать
в Telegram» → t.me/pandago_order_bot, fallback (на статик-превью без PHP) открывает бота с
префилл-текстом. Токен в HTML отсутствует (проверено grep=0).
ГРАБЛИ: getUpdates бота = 0 (никто не писал), webhook пуст → chat_id для прямой доставки С ПРЕВЬЮ
пока неизвестен. На проде доставка идёт через api.php+config.php. Чтобы лиды падали прямо с
higgsfield-превью — нужен relay (CF Worker с токеном+chat_id); ждём, чтобы Даниэль написал боту
(достану chat_id) ИЛИ дал chat_id.
Live v4.4.

## v4.5 — полировка до идеала (визуальный аудит скриншотами)
Сама сняла все блоки на мобиле+ПК, нашла и починила:
- УБРАЛА длинные тире «—» из видимого текста (бренд-правило PandaGo «без длинных тире»): hero
  lead и loopEnd subtitle → точка/запятая. (в коде-комментах — не трогала.)
- Заголовки на мобиле впритык к краю → h1 34px, .sec-title 23px на ≤560px (теперь с отступом,
  hero стал 2 строки вместо 3).
- Легибельность над яркими кадрами: text-shadow на h1/.sec-title/.foot-brand/.lead/.sec-sub +
  усилила .halo::before (радиал темнее). Инпуты формы темнее (rgba(5,12,26,.82)).
- Калькулятор не влезал на мобиле → compact padding/gaps/total → «Зафиксировать смету» теперь
  виден целиком в один экран.
QA: overflowX=0 (моб+ПК), калькулятор влезает, заголовки с отступом, hero/desktop скрин — резкий
склад с техникой, чисто. Live v4.5.

## v4.6 — переход на бренд DIESEL, референс dieselcompany.pro, ЖИВЫЕ ЗАЯВКИ
Контекст из переписки: проект уходит на новый бренд DIESEL (t.me/diesel_cargo), клиент дал
референс dieselcompany.pro (проф. B2B импорт техники из Китая: trust-метрики 2400+/8лет/96%,
реальные бренды CFMoto/Sur-Ron/Sea-Doo, процесс FOB/CIF/DDP, кейсы). Задача: «дошлифуй красивее».
Сделано:
- Hero trust-метрики (2400+ поставок · 8 лет на рынке · 96% возвращаются), градиент-цифры + mono.
- 3D-лейблы обогатил брендами: Мотоциклы Sur-Ron·Talaria, Квадро CFMoto·Aodes, Гидро Sea-Doo·Yamaha.
- Контакты «Написать в Telegram» → t.me/diesel_cargo (новый бренд).
- ЗАЯВКИ ВЖИВУЮ: добавил роут /api/lead прямо в Worker сайта (src/server.ts — intercept до
  TanStack, шлёт в Telegram sendMessage). Секреты через website_secrets: TG_TOKEN (бот
  @pandago_order_bot 8982451112) + TG_CHAT=1966985736,8637446193 (Даниэль+Konstantin). Форма
  постит /api/lead form-urlencoded, при неудаче fallback t.me/diesel_cargo. Токен НЕ в git/клиенте
  (только website_secrets + config на сервере). ТЕСТ: POST /api/lead → {"ok":true}, Даниэлю дошло.
  ВАЖНО: Konstantin (8637446193) должен 1 раз написать боту @pandago_order_bot, иначе бот ему
  писать не может (сейчас anyOk=true за счёт доставки Даниэлю). CF-Worker свой поднять не смог —
  токен CLOUDFLARE_API_TOKEN без прав Workers; сделал через сам сайт-Worker (безопаснее).
Live v4.6. Chat_id'ы Даниэль прислал скринами: konstantin 8637446193, Даниэль @ktodaniel 1966985736.

## v5.0 — УБРАЛ ЛАГ радикально (Даниэль: «все глючит лагает, некрасиво, сама придумай»)
Диагноз лага: (1) canvas-скраб перерисовывал 1080p webp каждый кадр скролла = дорого на телефоне;
(2) three.js render-loop крутился ВСЕГДА (даже когда 3D не виден) = постоянный жор GPU/CPU по
всему сайту. Решение:
- Фон → настоящий <video autoplay loop muted playsinline> (loop_sharp.mp4 7.6МБ 1080p, аппаратный
  декод = НИКОГДА не лагает). Убрал canvas+160 webp+перерисовку.
- «Привязка к скроллу» без лага: playbackRate растёт от СКОРОСТИ скролла (idle 0.55x → быстрый
  скролл до 2.6x), lerp, rAF только пока скроллишь/затухает. Видео визуально ускоряется при
  скролле, аппаратно-гладко.
- 3D GATE: render() пропускает всю WebGL-работу когда __P вне [0.12,0.46] (showcase не виден) —
  освобождает телефон на остальных блоках. + pixelRatio портрет 1.35→1.0, ландшафт 1.7→1.4.
- Красота блоков: глубокие стеклянные карточки (тень 0 34px 80px, inner top highlight, градиент
  фон), фокус-кольцо на инпутах, премиум eyebrow-пилюли.
Lenis smooth-scroll оставил. Beat-движок (прогресс→блоки) оставил — он на дешёвых transform.
QA: bgvid VIDEO autoplay+loop, беты переключаются hero→…→loopEnd, overflowX0, консоль чистая,
все скрипты node --check OK. Live v5.0. dc/seq (кадры) больше не used, оставил в репо.
