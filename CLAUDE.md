# Память проекта OKO (читать в каждой сессии)

Владелец: Даниэль (@ktodaniel, okoteam.top@gmail.com). Общение — на русском.

**ЖЁСТКОЕ ПРАВИЛО — ЕДИНОЕ ХРАНИЛИЩЕ `okoteam.top/key`.**
Всё НОВОЕ из любого чата — ключи, логины, пароли, API, токены, аккаунты, доступы,
паспорта, реквизиты, документы, новые сайты/библиотеки/источники, механики, правила,
скиллы — СРАЗУ отправляем в единое хранилище, ничего не оставляем только в переписке.
Секреты → `OKO_MASTER_VAULT.md` + `secrets.env` на сервере; знания → репозиторий + пуш.
Страница `okoteam.top/key` собирается из этого. Правило действует в КАЖДОМ чате, при
создании новых веток и репозиториев — CLAUDE.md с ним в каждом репозитории.

**Первым делом читать `СЕРВИСЫ.md`** — карта на один экран: чем генерим картинки,
видео и звук, что подключено, где паспорта. Почти всё уже есть, обходные пути
изобретать не нужно. Коротко: картинки ChatGPT, видео Runway, звук ElevenLabs,
всё через браузер агента на сервере, единая дверь `core/generate.py` в oko-agents.

## Два проекта в этом репозитории
- Корень — сайт DUCK'S GAME SPACE (клиентский, не трогать без запроса).
- `oko-app/` — приложение OKO. Вся работа по OKO — на ветке `claude/new-session-w2ptqy`.
- Мастер-документы OKO: `oko-app/docs/OKO_APP_CONTEXT.txt` и `oko-app/docs/OKO_APP_TZ.md`.
- **Контент-стратегия: `OKO_CONTENT_STRATEGY.md` (корень)** — цели, ЦА, форматы, KPI, реф-воронка. Читать перед производством контента.

## Жёсткие правила от Даниэля
1. **Никаких окон подтверждения.** Разрешения выданы на всё (`.claude/settings.json`).
   Не спрашивать подтверждений на инструменты, деплой и правки.
1а. **НИКОГДА не вызывать AskUserQuestion (всплывающая форма с вариантами).**
   В веб-интерфейсе она виснет и повторяется каждую секунду — Даниэль читает с телефона.
   Нужен выбор — задать вопрос ОБЫЧНЫМ ТЕКСТОМ в конце ответа, пронумерованными
   вариантами, и продолжать работу по своему лучшему варианту, не блокируясь.
   Правило постоянное, для всех сессий и всех репозиториев.
2. **НЕ публиковать Claude-артефакты.** Даниэль смотрит прогресс только по публичной
   ссылке. Артефакт-зеркало отключено навсегда.
3. Правки применяются как постоянные правила, не разово.
4. В интерфейсе OKO — никаких эмодзи, только SVG-иконки в стиле бренда.
   Бренд: чёрный + лайм #9AFF00, Bebas Neue + Montserrat, тёмная и светлая темы.
5. Работать автономно: делать, деплоить, отчитываться результатом.
6. **ЛОГО — ТОЛЬКО НАСТОЯЩЕЕ, ВЕЗДЕ И ВСЕГДА, в base64, не искажая пропорции.**
   Файлы: `brand/oko-logo.png` (оригинал 1000×1000), `brand/oko-logo-256.png`,
   `brand/oko-logo-b64.txt` (готовый data-URI для вставки в `<img src=...>`).
   В агентах — `oko-agents/core/brand.py` (`LOGO_DATA_URI`). НЕ рисовать SVG-заглушки,
   НЕ растягивать, НЕ менять цвета лого. Обновлять эти файлы при новой версии лого.

7. **ПЛАТНЫЕ КРЕДИТЫ/ГЕНЕРАЦИИ — ТОЛЬКО С ЯВНОГО РАЗРЕШЕНИЯ ДАНИЭЛЯ.** Правило №1
   (никаких подтверждений) НЕ распространяется на трату денег. СПРАШИВАТЬ перед:
   генерациями Higgsfield (`generate_image/video/audio/3d`, upscale, Marketing Studio —
   жгут кредиты), рендерами Shotstack **prod** (`SHOTSTACK_PROD_KEY` платный — для тестов
   ТОЛЬКО sandbox), fal.ai, платными API. Бесплатное можно без спроса: Shotstack sandbox
   (вотермарк), стоки Pexels/Pixabay/Shutterstock-preview, edge-tts, HF ZeroGPU,
   локальный ffmpeg/Playwright. Для черновиков/тестов — всегда бесплатный путь.

8. **ССЫЛКА НА ОПЛАТУ — ТОЛЬКО НАШ ДОМЕН, И СРАЗУ ДВА ВАРИАНТА.** Клиенту уходит
   страница `okoteam.top/p/<код>`, собранная `core.shortlink.pay_page` (репо
   `gdeoko/oko-agents`), и на ней всегда обе кассы: карта через Lava и счёт на
   юрлицо через ЮKassa. Эталон, с которого списываем:
   https://okoteam.top/p/klubnyy-dom-noty-mesyac-2/
   Прямые `app.lava.top/...` и `yoomoney.ru/checkout/...` не отправлять НИКОГДА:
   чужой домен и строка параметров на пол-экрана читаются как скам, человек не
   платит. Это касается переписки, КП, договоров и писем. На сервере проверка
   `core.outbound_guard.check` не пропустит сообщение с сырой ссылкой кассы
   (причина `raw_payment_link`). Собрать страницу:
   `vexec 'cd /opt/oko-agents && .venv/bin/python -c "from core import lava, yookassa, shortlink; ..."'`
   (пример целиком — `ETALONS.md`, раздел 3, в репо `gdeoko/oko-agents`).

9. **ПРОЕКТ КЛИЕНТА ЖИВЁТ У КЛИЕНТА.** Сайты, приложения и файлы клиентов
   разворачиваем на ИХ хостинге или сервере, с полным доступом у них. Наш VPS —
   не хостинг для клиентских проектов: на сотне проектов он ляжет, а клиент
   останется без доступа к своему же продукту. Если у клиента нет подходящей
   площадки, говорим прямо, что нужно взять (хостинг для сайта, VPS для бота или
   приложения), и переносим бесплатно. Держать у себя допустимо только временно и
   с явной пометкой в чате клиента, что это временно и почему.

## Публичная ссылка (единственная витрина прогресса)
https://true-journey-418.higgsfield.app

Как обновлять (после каждого значимого изменения прототипа):
1. Источник правды — `oko-app/prototype/index.html` (самодостаточный файл).
2. Higgsfield website_id: `5426760c-49ec-46c4-b3ff-b22a6dd598a5`.
   `mcp__Higgsfield__website_repo_access` → клонировать репо сайта →
   скопировать прототип в `app/src/oko-prototype.html` → commit+push →
   `mcp__Higgsfield__deploy_website` env=production. Таймаут 60с не ошибка —
   деплой доходит, проверять curl'ом по маркеру версии.
3. Версия — чип «сборка vX.Y» в шапке прототипа, поднимать при каждом деплое.
4. В прототипе есть экран «Прогресс сборки» (PROGRESS в JS) — обновлять статусы этапов.

## Инфраструктура (подключено и проверено)
- Supabase: проект `tkjewndtlzhnmqwmrnil`, база 28 таблиц развёрнута.
  SQL выполнять через Management API `api.supabase.com` (PAT) — прямой порт 5432 из
  облака закрыт. Node fetch мимо прокси не ходит — использовать curl.
- Telegram-бот: @okoappbot. S3: `https://s3.twcstorage.ru`, бакеты `oko-media`,
  `oko-tmp` (lifecycle 24ч настроен). Gemini — 3 ключа. Claude API — ключ есть,
  баланс пополняется Даниэлем.
- Все ключи Даниэль прислал в чат сессии от 05.07.2026; должны лежать в
  Environment variables окружения. Если их нет — спросить у Даниэля, в git не класть.
- Сервер-каркас: `oko-app/server` (Fastify + Supabase, новые ключи sb_/JWKS).

## Стиль ответов
Кратко, по делу, mobile-first (Даниэль читает с телефона). Скриншоты — только
когда есть что показать визуально нового.

## МАСТЕР-ХРАНИЛИЩЕ (все ключи/пароли/доступы — читать в начале ЛЮБОЙ сессии)
Полный свод ВСЕХ доступов (инфра, VPS, AI, соцсети, оплата, паспорт/банк, аккаунты
клиентов, сервисы сборки) авто-загружается SessionStart-хуком в `~/OKO_MASTER_VAULT.md`
в каждой сессии любого репозитория. Читать его при старте — там всё, что нужно, спрашивать
у Даниэля пароли/ключи НЕ нужно. Если файла нет: `vexec 'cat /opt/oko-poster/cfg/OKO_MASTER_VAULT.md'`
(обёртка vexec — в `OKO_BOOTSTRAP.md`). Добавил новый ключ — допиши и в vault на VPS.

### ЯНДЕКС 360 / UNISENDER / СЕРВЕРЫ — доступ есть всегда, спрашивать не нужно
Бизнес-аккаунт Яндекс 360 «Музыкальный Мир» (почта, домен, DNS), Unisender Go,
root на обоих серверах, панель регистратора — всё лежит в vault (раздел 13) и
приезжает переменными окружения: `YANDEX360_*`, `UNISENDER_*`, `NETHOUSE_*`,
`MUZMIR_VPS_*`, `OKO_VPS_*`. Вход в Яндекс закрыт 2FA по SMS Даниэля, поэтому на
мосту `104.171.132.45` крутится Chrome с уже выполненным входом:
`/opt/oko-poster/yandex.sh <URL>` (профиль `/opt/oko-poster/browser/live`,
бэкап + сторож в cron, CDP `127.0.0.1:9222`). Подробности и грабли —
`INTEGRATIONS.md`, раздел 5б. Просить у Даниэля логины/пароли/SMS-коды по этим
сервисам не нужно — только если сторож сообщит, что профиль потерян.

## УПРАВЛЕНИЕ АГЕНТОМ ИЗ ЛЮБОГО ЧАТА
Полный мануал — `OKO_AGENT_CONTROL.md` (в каждом репо). Как из любой сессии: командовать
агентом (аудит/воронка/диалоги через `CONTROL_URL`+`vexec`), заставить агента отправить
клиенту сообщение/фото/файл (мост задач `taskqueue`), гонять браузер (Chromium в сессии),
пользоваться интеграциями и MCP, читать базу знаний по продажам, деплоить агента. Агент,
его браузер и интеграции доступны в КАЖДОМ чате для работы с клиентами.

## Интеграции и API-ключи (для ЛЮБОГО чата)
**Паспорт: `INTEGRATIONS.md` — ЕДИНЫЙ файл всех интеграций.** Читать в начале
каждой сессии и АКТИВНО ИСПОЛЬЗОВАТЬ перечисленное по необходимости — не изобретать
обходные пути, если инструмент уже подключён. Ключи в `secrets.env.b64` (base64)
автозагружаются SessionStart-хуком в каждый shell. Вручную:
`. ~/.oko/secrets.env`.
Главный производственный скилл: `/oko-magic` (.claude/skills/oko-magic/SKILL.md).
Видеоролики: скилл `/reels-machine` + раздел 1а паспорта.
Ключи не вписывать в код сайтов и не отдавать в браузер.

**Правило пополнения (обязательное, для всех чатов):** подключил новую
интеграцию/сервис/ключ — СРАЗУ допиши её в `INTEGRATIONS.md` (что даёт, как
проверить, грабли); новый ключ → дописать на VPS в
`/opt/oko-poster/cfg/secrets.env` и в мастер-хранилище там же (через `vexec`).
В git ключи НЕ кладём, даже в base64 (почему - в `РОТАЦИЯ_КЛЮЧЕЙ.md`).
Паспорт `INTEGRATIONS.md` по-прежнему пушим в текущую и дефолтную ветку.

## Правило ГЕНЕРАЦИИ: картинка, видео, звук (для ЛЮБОГО чата Claude - обязательно)

0. **МАРШРУТ ГЕНЕРАЦИИ, слово владельца от 23.08.2026. Отменяет всё, что было раньше.**

   | что делаем | чем | где живёт |
   |---|---|---|
   | **картинки, обложки, баннеры, карточки, инфографика** | **ChatGPT**, безлимит по подписке | браузер агента на VPS |
   | **видео, ролики, анимация** | **Runway** Max, аккаунт `claude.okoteam@gmail.com`, безлимит | **отдельный** браузер rwbg на болгарском IP |
   | **озвучка, голос, речь, звуковые эффекты** | **ElevenLabs**, модель Flash v2.5 | ключ `ELEVEN_API_KEY` |

   Всё это оплачено помесячно, поэтому каждая генерация стоит ноль сверх подписки.

   **RUNWAY = БОЛГАРСКИЙ IP (жёстко, 25.08.2026). Полная инструкция - `oko-agents/docs/RUNWAY.md` раздел «Болгарский IP».**
   Старый аккаунт `okoteam.top` забанен за датацентр-IP и машинный залп. Новый
   боевой: `claude.okoteam@gmail.com` (Personal · Max, безлимит в режиме Explore).
   Ролики Runway идут ТОЛЬКО через отдельный браузер **rwbg** (экран :99, отладчик
   **9251**) с выходом на болгарский жилой IP `176.222.2.133` (mobileproxy.space,
   конфиг `cfg/bg_proxy.json`), НИКОГДА через oko-chrome (9222) и НИКОГДА через
   прямой IP сервера. Маршрут зашит: `core/generate.ролик()` берёт
   `RUNWAY_CDP=http://127.0.0.1:9251`. Правила против бана (в коде): пауза 60-180с
   между роликами, объём не режем (100-200/день ок, `RW_DAILY_CAP=0`), один аккаунт,
   режим Explore/Relaxed, вход делает человек (логин не автоматизируем).
   Поднять браузер: `DISPLAY=:99 setsid bash -c "cd /opt/oko-poster && node runway_bg_up.cjs" &`.
   Посмотреть глазами: `<VNC рабочего стола: секретная ссылка + VNC-пароль в vault на сервере (OKO_MASTER_VAULT.md, раздел desktop/Runway)>`.

   **ФОЛБЭК НА БОЛГАРСКИЙ IP (правило владельца 25.08.2026). Паспорт - `oko-agents/docs/БОЛГАРСКИЙ_IP.md`.**
   Болгарский жилой IP это ОБЩИЙ выход, не только под Runway. Если площадка не
   открывается и обычный VPN (Pantera) её не берёт - Gemini, Instagram, Facebook,
   Threads, WhatsApp и подобные - подключаем через болгарский IP (`fproxy.site:13047`).
   Порядок: сначала перебрать выходы Pantera, не помогло - болгарский IP. Трафик
   безлимитный, аренда до 24.09.2026. Каждую площадку логиним в СВОЁМ профиле браузера,
   иначе аккаунты слинкуются. Telegram и ВК на болгарский IP НЕ переводим (российские,
   вход из Болгарии = риск заморозки). Для масштаба соцсетей - свой IP на каждую личность.

1. **Вход только через браузер агента, ключи OpenAI не нужны вообще.** На сервере
   настоящий Chrome (`oko-chrome`, экран :99, профиль `/opt/oko-poster/profiles/google`),
   вход в `okoteam.top@gmail.com`. Отладчик `127.0.0.1:9222`, из облака - через `vexec`.
   **ВЫХОД БРАУЗЕРА КАРТИНОК - БОЛГАРСКИЙ ЖИЛОЙ IP (25.08.2026):** выходы Pantera
   (датацентр) нестабильны, Cloudflare режет ChatGPT, поэтому браузер ChatGPT переведён
   на болгарский резидентный IP `--proxy-server="http://aus.mobileproxy.space:2413"`
   (whitelist по IP сервера, без пароля). Поднять/перевести: `bash /opt/oko-poster/gpt_bg.sh`.
   Вход в ChatGPT владелец делает через noVNC один раз.

   Единая дверь в коде - **`core/generate.py`**:

       from core import generate
       generate.картинка("подробный английский запрос", "/путь/файл.png")
       generate.ролик("подробный английский запрос", "/путь/ролик.mp4", формат="9:16")
       generate.голос("текст по-русски", "/путь/озвучка.mp3")
       generate.чем_генерим()      # короткая справка прямо в чат

   Драйверы браузера: `tools/chatgpt_web/chatgpt_web.mjs` (картинки),
   `tools/runway_web/runway_web.mjs` (видео). На сервере они лежат в
   `/opt/oko-poster/`. Gemini-драйвер для визуала не используем.

   Руками с сервера:

       node /opt/oko-poster/chatgpt_web.mjs "<английский промпт>" /файл.png
       RW_METKA=кадр node /opt/oko-poster/runway_web.mjs minimax "<промпт>" /ролик.mp4 5 9:16

2. **Платные API с поштучной оплатой не трогаем.** Ни `GEMINI_KEY_PAID`, ни ключи
   OpenAI, ни Higgsfield: там каждая картинка и каждая секунда ролика стоят денег,
   а через браузер то же самое уже оплачено. Higgsfield отключён совсем, двести
   долларов в месяц без безлимита. Ключ Gemini остаётся мозгу агента для текста,
   к визуалу отношения не имеет. Ключ Runway API годится под точечные задачи, но
   у него отдельный кошелёк, безлимит живёт только в веб-кабинете.

3. **Картинки ТОЛЬКО ChatGPT. Gemini для визуала не используем совсем** (слово
   владельца 25.08.2026). Ключ Gemini остаётся мозгу агента для ТЕКСТА (API), к
   картинкам и роликам отношения не имеет. Запасного пути через Gemini-браузер нет:
   если ChatGPT не открылся - чиним выход/вход браузера, а не переключаемся на Gemini.

4. **Выход в сеть решает.** Отказ «не поддерживается в вашей стране» лечится сменой
   выхода, а не выводами. Главный выход Pantera ОБХОД № 16, Нидерланды, socks5 на
   `127.0.0.1:10840`, через него открывается всё. Дубай `10809`, Франкфурт `10808`,
   США `10811` держим запасными. Поднять заново:
   `python3 /opt/oko-poster/pantera_up.py "ОБХОД № 16"`. Перебирать надо ВСЕ
   шестнадцать обходов, а не три поднятых.

5. **Промпт картинки - ВСЕГДА по-английски и от 2000 знаков.** Сцена, объект,
   материалы и фактура, свет с направлением и мягкостью, объектив и глубина,
   композиция и ракурс, цвета в HEX по бренду, настроение, стиль, свободное место
   под текст, качество (8K, ultra sharp, commercial product photography). Русский
   текст, который должен появиться НА картинке, пишем в кавычках внутри английского
   промпта. Драйвер сам не пропустит промпт короче двух тысяч знаков.
   К промпту можно приложить до десяти файлов: настоящий логотип, товар, лицо
   человека в нескольких ракурсах.

6. **Объём упрётся в лимиты аккаунта** - поднимаем второй и третий профиль браузера
   со своими аккаунтами и своими выходами. Для контент-завода так масштабируется
   хоть до сотни аккаунтов.

