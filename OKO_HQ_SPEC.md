# OKO HQ — командный центр (полный build-спек)

> Живой 3D-офис + мозг + командный чат для управления ВСЕМИ агентами OKO из одного экрана.
> Всё рендерится КОДОМ в реальном времени (Three.js/WebGL) — никаких фото/видео-подложек,
> поэтому зум чёткий, без каши и лагов. Стиль: тёмный IT/хакер, бренд OKO (чёрный + лайм #9AFF00).
> Собирать в отдельном чате OKO App на FABLE 5 (долгий автономный заход). Этот файл — ТЗ.

---

## 0. Концепция за 30 секунд
Открываешь → тёмный офис OKO сверху/сбоку. За столами сидят агенты-сотрудники (продажи,
монтажёр, дизайнер, маркетолог, юрист, копирайтер, саппорт, контент-завод, помощники, ты —
гендиректор). От каждого ноутбука тянется светящийся лаймовый «нерв» к МОЗГУ в центре зала —
пульсирующему анимационному ядру. Каждый нерв = файл/материал/память; на нём точки-узлы,
кликаешь → открыть/скачать/редактировать/удалить/заменить. Слева — рабочий чат: лента ВСЕХ
действий агентов в реальном времени + поле ввода (текст/файл/видео/голос). Даёшь задачу
конкретному агенту или в общий чат → диспетчер раскидывает → агенты идут к столам и работают
→ зумишь в ноутбук → видишь ЖИВОЙ экран (браузер/код/диалог). Когда агент не занят — ходит по
офису, пьёт воду у кулера, залипает в телефон, болтает — как NPC в игре. Дал задачу — вернулся,
сел, работает.

---

## 1. Технологический стек (всё кодом, без Higgsfield)

| Слой | Технология | Зачем |
|---|---|---|
| 3D-движок | **Three.js (WebGL2)** | реал-тайм рендер, чёткий зум, тени, анимации |
| Пост-обработка | **postprocessing** (Bloom, SSAO, FXAA/SMAA, Vignette) | лаймовое свечение, мягкие тени, хакер-настроение |
| UI-оверлей | **React 18 + Zustand** (стейт) | чат, панели, HUD поверх канваса |
| Сборка | **Vite + TypeScript** | быстрый билд, типобезопасность |
| Анимации UI | **GSAP** + **Framer Motion** | плавные переходы панелей, счётчиков |
| 3D-анимации | Three **AnimationMixer** + скелетные клипы | ходьба/печать/питьё агентов |
| Пути NPC | лёгкий **A\*** по nav-grid (или waypoints) | агенты ходят, обходят столы |
| Данные | **WebSocket** (live) + REST fallback | статусы/действия/команды в реальном времени |
| Голос-вход | **faster-whisper** (локально, уже заведён) | голосовые команды без квоты |
| Звук | **Howler.js** | эмбиент офиса, клики, пульс мозга (тумблер) |
| Хостинг | Higgsfield website / OKO App (embed) | подключение ко всем чатам + в приложение |

**Философия производительности:** НЕ фотореализм (он лагает), а **стилизованный реализм** —
low-poly модели + PBR-материалы + качественный свет + Bloom/SSAO. Выглядит премиально, держит
60 fps. Инстансинг повторов (столы/стулья), LOD, frustum culling, render-target экранов только
когда в кадре, троттлинг апдейтов NPC вне зоны видимости.

---

## 2. 3D-ОФИС (сцена)

### 2.1 Комната
- Open-space: пол (тёмный бетон/эпокси с лёгким отражением лайма), стены, потолок со световыми
  панелями (излучают мягкий свет + чуть лайма по кромкам).
- **Окна** во всю стену → за ними ночной город (параллакс-скайбокс или лёгкая гео + bloom-огни).
  Опционально день/ночь-цикл (солнце/луна двигаются, свет в комнате меняется).
- **Двери** (входная, серверная-мозг, переговорка) — открываются на анимации, когда агент проходит.
- Зоны: рабочая (столы), лаунж (диван, кресла), кухня (кулер с водой, кофемашина), переговорка
  (стол + голо-доска), серверная в центре (МОЗГ).

### 2.2 Столы и ноутбуки
- Каждый стол = 1 роль. На столе: ноутбук (экран — живой render-target), кружка, стикеры,
  мелочь. У продажников — второй монитор с воронкой.
- **Экран ноутбука** — отдельная текстура-цель (RenderTarget или CSS3D-плоскость), на которой:
  режим агента (браузер-скрин / строки кода / диалог с клиентом / рендер макета). Обновляется
  по данным с бэка. При зуме камера подлетает — текстура догружается в высоком разрешении.
- От ноутбука вниз-к-полу и по полу к центру — **лаймовый нерв** (см. §4).

### 2.3 Пропсы и жизнь
Кулер с водой (пузырёк всплывает при питье), кофемашина (пар), растения (лёгкое колыхание),
телевизор на стене с метриками, часы, доска с задачами, аквариум (пасхалка), робот-пылесос
ездит. Всё мелко анимировано — офис «дышит».

### 2.4 Свет и настроение
Тёмная база (#0a0a0a), холодный ambient, тёплые точки от экранов, **лаймовые акценты** (#9AFF00)
на кромках, проводах, логотипе, статусах. Bloom на всё светящееся. Мягкие контактные тени (SSAO).
Лёгкий туман/дымка у пола для глубины. Настроение: «ночной штаб хакеров», премиум-IT.

---

## 3. АГЕНТЫ (NPC)

### 3.1 Роли (v1)
1. **Гендиректор (Даниэль)** — угловой стол, обзор всего, «раздаёт задачи».
2. **Отдел продаж** (можно 2-3: acc1/acc2/acc3 ТГ + ВК + YouTube) — за мониторами воронки.
3. **Монтажёр** — стол с таймлайном на экране.
4. **Дизайнер** — стол с макетами/Figma.
5. **Маркетолог** — графики/аналитика.
6. **Юрист** — договоры/161-ФЗ.
7. **Копирайтер** — тексты/офферы.
8. **Саппорт OKO App** — чаты поддержки.
9. **Контент-завод** — продвижение услуг + приложения (рилсы/постинг).
10. **Помощники** (1-2) — универсалы, берут разное.

### 3.1а ДИЗАЙН ПЕРСОНАЖА — мини-робот OKO (референс: AXIAL STUDIO, но в бренде OKO)
Милый округлый низкополигональный робот (как в референсе — soft-body, стоит ~«по колено» столу).
**Лицо = ОДИН ГЛАЗ-ЛОГОТИП OKO**: лаймовая дуга-веко сверху + светящийся лаймовый круг-зрачок
(#9AFF00, emissive+bloom) — это и есть лого OKO (оно и означает «око/глаз»). Глаз ЖИВОЙ и
выразительный:
- **моргает** (дуга-веко опускается), **смотрит** в сторону задачи/собеседника (зрачок следит),
- **пульсирует** ярче когда работает, **сужается** при фокусе,
- **краснеет/дрожит** при затыке (BLOCKED), **прищур+искра** при успехе/сделке,
- лёгкое свечение отражается на столе/полу.
Тело: тёмный матовый корпус (бренд-чёрный) с лаймовыми кромками + **у каждой роли свой акцент-цвет**
(как в референсе синий/оранж/зелёный/фиолет/жёлтый) на «шарфе»/полосе/наушниках — чтобы различать,
но глаз-лого у всех единый лаймовый (все — агенты OKO). Мелкое лого OKO ещё и на груди/спине корпуса.
Пропорции: большая голова-глаз + компактное тело + короткие ручки (берёт кружку, машет, печатает).
Материалы: matte PBR + soft-shadow + чуть SSS для «игрушечности». Анимации: idle-дыхание, ходьба
вразвалочку, печать, питьё воды, «залип в телефон», болтовня (два робота, глаза друг на друга).
Над головой — бейдж: роль + статус-огонёк + текущая задача (моно-шрифт).

### 3.1б Элементы офиса из референса (берём в дело)
- **VAULT (сейф)** у стены = физический вход в МОЗГ-память (открывается, внутри — узлы/файлы).
- **KANBAN WALL** = доска задач (Backlog/In Progress/Review/Done) — стеклянные карточки, задачи
  переезжают анимацией когда агент двигает статус.
- **SECURITY GATE** = турникет: визуал «доступа» (кто из агентов активен/залогинен).
- **SYSTEM OVERVIEW** панель справа = HUD-метрики (агенты онлайн, задачи, воронка, выручка).
- **Карточка агента** снизу (как в референсе): `TASK / FEATURE / RUNTIME / TOKENS / STATUS + прогресс`.
Стиль оставляем OKO: темнее и «хакернее» референса (чёрный+лайм, неон, стекло), но роботы такие же
милые.

### 3.2 Стейт-машина поведения
```
IDLE ──(задача)──▶ GOING_TO_DESK ──▶ WORKING ──(готово)──▶ IDLE
  │                                     │
  │                                     └─(затык)─▶ BLOCKED (красный, рука вверх «?»)
  └─ бродит: WATER_COOLER / LOUNGE / PHONE / STRETCH / CHAT_WITH(agent) / WINDOW_GAZE
```
- **IDLE**: A\*-путь к случайной точке интереса, проигрывает клип (пьёт воду, тянется, смотрит в
  телефон, болтает с соседним агентом — два подходят, «говорят»). «Делает что хочет».
- **WORKING**: сидит, клип «печатает», экран активен, нерв к мозгу пульсирует чаще.
- **BLOCKED**: встаёт, красный статус, значок «?» — сигнал тебе (и в чат «агент застрял: …»).
- Плавные переходы, IK-посадка на стул, анимация «сел/встал».

### 3.3 Две природы агентов (честно в модели данных)
- **Живые 24/7 на VPS** (продажи ТГ/ВК/YT) — их статус/действия берём с control-эндпоинта; «экран»
  = живой диалог/действие.
- **Роли-исполнители** (монтажёр/дизайнер/юрист/копирайтер/контент-завод) — это Claude Code (ты)
  через мост: берёшь задачу, делаешь (обложка/договор/текст/ролик), пушишь на экран скрин
  браузера/кода/рендера. В офисе обе природы выглядят одинаково — как сотрудники.

---

## 4. МОЗГ (центр зала)

### 4.1 Ядро
Пульсирующая анимационная структура в центре (нейро-сфера / пучок волокон / раздвижной кристалл).
Медленно вращается, «дышит» (раздвигается-сжимается). Внутри — частицы/светлячки (лайм+бирюза),
объёмное свечение (bloom). При активности (агент пишет в память) — вспышка + волна по волокнам.

### 4.2 Нервы-волокна = файлы/память
- От каждого ноутбука к ядру идёт **лаймовое волокно** (кривая Безье по полу и вверх в ядро).
- Каждое волокно = категория/файл памяти. По волокну бегут **импульсы**: ноутбук→ядро (агент
  что-то сохранил), ядро→ноутбук (агент читает знание).
- На волокнах — **узлы-точки** (glow-сферы). Каждый узел = конкретный файл/материал/запись.
- Категории (цвет волокна): база продаж (лайм), клиенты (бирюза), бренд-ассеты (золото), договоры
  (красный), обучение (фиолет), загруженные файлы (белый).

### 4.3 Взаимодействие с памятью
Клик по узлу → **панель памяти**:
- Превью (текст/картинка/pdf/аудио-волна).
- Кнопки: **Открыть · Скачать · Редактировать · Заменить · Удалить**.
- Метаданные: тип, размер, дата, кто создал (агент/ты), теги.
- Поиск по всей памяти (строка сверху) → волокна/узлы подсвечиваются по совпадению.
Наведение на узел → тултип + свечение; клик → зум камеры к узлу + панель.

### 4.4 Загрузка в мозг
Кидаешь файл/видео/голос в чат → он «летит» частицей от поля ввода к ядру → я через мост
структурирую/анализирую (транскрибация/OCR/разбор, как сейчас с обучением) → появляется новый
узел на нужном волокне с анимацией «врастания». Всё видно вживую.

---

## 5. КАМЕРА И УПРАВЛЕНИЕ
- **Орбита + зум + пан** (OrbitControls с ограничениями, плавный дэмпинг).
- **Пресеты камеры** (кнопки/хоткеи): `1` Обзор зала · `2` Отдел продаж · `3` Мозг · `4` CEO-угол ·
  `5` Контент-завод. Плавный облёт (GSAP tween камеры).
- **Зум в ноутбук**: даблклик по столу → камера подлетает, экран на весь кадр, живой контент,
  оверлей «что делает агент» + кнопка «вмешаться».
- **Клик по агенту** → карточка (роль/статус/задача/лог действий/кнопка «дать задачу»).
- **Клик по узлу мозга** → панель памяти.
- **Ховер** везде: подсветка контуром (outline pass) + тултип.
- **Миникарта** угол экрана (топ-вид зала, точки агентов). **Поиск** (Ctrl+K, командная палитра).

---

## 6. UI-ОВЕРЛЕЙ (рабочий чат + HUD)

### 6.1 Рабочий чат (док слева или снизу)
- **Лента действий** — реал-тайм, ВСЕ действия агентов, по цвету роли, с таймкодом:
  `14:22 · Продажи(acc3) → отправил Ольге тестовое (2 макета)` ·
  `14:23 · Дизайнер → рендер обложки MedCraft (готово)` ·
  `14:25 · Мозг ← загружен файл brief.pdf (разобран, 3 узла)`.
- **Поле ввода**: текст · 📎 файл · 🎥 видео · 🎙 голос (mic→whisper→текст). Без эмодзи в проде —
  SVG-иконки.
- **Адресат**: тумблер «в общий чат» (диспетчер раскидает) / выбрать агента (клик по нему или дропдаун).
- **Командная палитра (хакер-терминал)**: `/status`, `/funnel`, `/warm <лид>`, `/send <клиент> <ассет>`,
  `/deploy`, `/agent <роль> <задача>`, `/brain search <запрос>`. Автодополнение.
- Я (через мост) выполняю → отчёт той же лентой.

### 6.2 HUD-метрики (верх/углы, стиль терминала, моноширинный)
Выручка дня (реальная, счётчик-одометр), лиды/ответили, горячие, агенты онлайн, PeerFlood-статус
аккаунтов, задачи в очереди. Мигающие алерты: новый горячий лид, закрыта сделка, агент застрял.

### 6.3 Панели (выезжают, стекло+неон)
- Карточка агента. Панель памяти. Экран-ноутбука (fullscreen live). Настройки (звук, день/ночь,
  тема-reskin, качество графики low/med/ultra).

---

## 7. ДИСПЕТЧЕР (логика мозга)
Задача в общий чат → парс интента (что нужно) → маршрут по ролям (одна или несколько, дробит на
подзадачи) → в очередь мосту (`taskqueue`) → агенты выполняют → отчёт + запись в память. Визуал:
задача-частица летит от чата к агенту(ам); очередь видна над столом.

Пример: «сделай тест для клиента X и подготовь договор» → Дизайнер (макет) + Юрист (договор) +
Продажи (отправка) — три частицы к трём столам, три отчёта.

---

## 8. ДАННЫЕ И API

### 8.1 Dashboard API (тонкий слой над VPS control-plane)
```
GET  /api/agents            → [{id, role, nature, status, task, lastActions[]}]
GET  /api/feed?since=ts     → live-лента действий (или WS)
GET  /api/funnel            → {stages, leads, revenueToday, hot[]}
GET  /api/brain             → волокна+узлы (память): [{id, cat, title, type, size, mtime}]
GET  /api/brain/:id         → содержимое узла (файл/текст)
POST /api/brain             → загрузка файла в память (→ мост структурирует)
PUT/DELETE /api/brain/:id   → редактировать/заменить/удалить
POST /api/command           → {target: 'general'|agentId, text, files[], voice}
GET  /api/agent/:id/screen  → живой скрин (браузер/код/диалог)
WS   /ws                    → push: feed, статусы, импульсы мозга, алерты
```
Всё это оборачивает уже существующее: `CONTROL_URL`+`CONTROL_TOKEN`, `taskqueue`-мост,
`client_memory`, логи `dialogs.jsonl`/`cant_do`/`ai_flags`, база `sales_knowledge_artem.md`.

### 8.2 Схема агента (пример)
```json
{ "id":"sales_acc3", "role":"Отдел продаж", "nature":"live-vps",
  "status":"working", "task":"дожим Ольги MedCraft",
  "screen":{"type":"dialog","peer":"Ольга","last":"жду фидбек по макетам"},
  "stats":{"deals":3,"revenue":45000}, "desk":[x,y,z], "color":"#9AFF00" }
```
### 8.3 Живой экран
- live-vps (продажи): экран = виджет диалога (последние сообщения, статус воронки).
- role-claude (дизайнер/монтажёр…): экран = скрин браузера/кода/рендера, который я пушу в
  `/api/agent/:id/screen` во время задачи (Playwright-скрин или код-фрейм).

---

## 9. ВХОДЫ (файл/видео/голос → мозг)
- **Файл** (pdf/doc/img/zip): drag&drop или 📎 → загрузка → мост разбирает (OCR/парс) → узлы.
- **Видео**: загрузка → извлечение кадров/аудио → транскрибация (whisper) + разбор → узлы.
- **Голос**: зажал mic → whisper локально → текст в поле → как команда ИЛИ в память.
- Прогресс разбора виден частицей, летящей в мозг + тост «разобрано: N узлов».

---

## 10. ФИШКИ (игровой максимум)
- **День/ночь + погода** за окном; свет комнаты подстраивается.
- **Счёт как в игре**: выручка = «очки», у агентов XP/уровни за закрытые сделки, ачивки
  («первый $300/день», «10 сделок»).
- **Реплей дня**: таймлайн-скраб — прокрутить действия агентов за день.
- **War-room режим**: горячий лид → прожектор на продажника, музыка нарастает.
- **Звук**: эмбиент офиса, клавиши, пульс мозга, блипы алертов (тумблер).
- **Пасхалки**: агенты иногда играют (мини-игра на ноуте), робот-пылесос, аквариум, кот ходит.
- **Фото-режим**: скрин красивого кадра офиса для соцсетей OKO.
- **Reskin по клиенту**: темы (смена палитры/бренда) — как у Knox «fork per client».
- **Мобилка**: на телефоне — упрощённый 2D-режим (список агентов + лента + метрики), 3D на десктопе.
  (Ты читаешь с телефона — этот режим обязателен, лёгкий.)
- **Уведомления**: плавающие тосты (лид/сделка/затык) + опц. пуш.

---

## 11. БРЕНД И ЭСТЕТИКА
- Цвета: чёрный #0a0a0a / лайм #9AFF00 / бирюза-акцент / золото (премиум-детали) / красный (алерт).
- Шрифты: **Bebas Neue** (заголовки), **Montserrat** (текст), **моноширинный** (JetBrains Mono —
  терминал/данные/экраны). Реальное лого OKO (base64), не искажать.
- Стиль: тёмный «хакерский штаб» — стеклянные панели, неон-кромки, скан-линии (тонко), матрица-импульсы
  по проводам. Без эмодзи в интерфейсе — только SVG-иконки в стиле бренда.

---

## 12. ПРОИЗВОДИТЕЛЬНОСТЬ (не лагать!)
Инстансинг (столы/стулья/растения), LOD агентов и мебели, frustum+occlusion culling, экраны-ноутов
рендерятся только в кадре и по запросу, троттлинг NPC-логики вне видимости, атласы текстур, baked
lighting где статично + few dynamic lights, DPR-cap, quality-пресеты (low/med/ultra) c авто-даунгрейдом
по FPS. Цель: 60 fps десктоп, плавно на среднем железе, лёгкий 2D-fallback на мобиле.

---

## 13. СТРУКТУРА ПРОЕКТА
```
oko-hq/
  index.html
  src/
    main.tsx
    scene/            room.ts desks.ts laptops.ts props.ts lights.ts brain.ts wires.ts
    agents/           Agent.ts stateMachine.ts pathfind.ts animations.ts roles.ts
    camera/           controls.ts presets.ts
    ui/               Chat.tsx Feed.tsx CommandPalette.tsx AgentCard.tsx MemoryPanel.tsx
                      LaptopScreen.tsx HUD.tsx Notifications.tsx Settings.tsx MiniMap.tsx
    data/             api.ts ws.ts store.ts (zustand) dispatcher.ts
    voice/            whisper.ts
    audio/            sound.ts
    assets/           models/ textures/ fonts/ logo_b64.ts
  server/             dashboard-api (обёртка control-plane) — или serverless
  public/
```

## 14. ФАЗЫ СБОРКИ (для FABLE 5, один длинный заход)
0. Скелет (Vite+TS+Three+React), API-стаб, бренд-токены.
1. Комната + столы + свет + камера + Bloom/SSAO (статика, красиво). **Проверка: 60 fps.**
2. Агенты: модели + стейт-машина + A\* + idle-анимации (ходят, пьют воду). **Проверка: 10 NPC плавно.**
3. Мозг: ядро + волокна + узлы + импульсы. **Проверка: клик по узлу открывает панель.**
4. UI: чат + лента + палитра + карточки + панели. **Проверка: команда уходит в API-стаб.**
5. Данные: Dashboard API поверх control-plane + WebSocket. **Проверка: реальные статусы/воронка.**
6. Команды: чат → мост → выполнение → отчёт + диспетчер. **Проверка: «отправь клиенту X» реально шлёт.**
7. Живые экраны ноутов (скрин браузера/кода/диалога) + зум. **Проверка: видно что делает агент.**
8. Входы: файл/видео/голос → мозг (разбор). **Проверка: загрузил pdf → появился узел.**
9. Фишки: звук, день/ночь, ачивки, реплей, war-room, мобил-2D. 
10. Полировка, перф-пасс, reskin, деплой (Higgsfield/OKO App), подключение ко всем чатам Claude Code.

После каждой фазы — **самопроверка Playwright/Chromium** (скрин + FPS + клики), чтобы не копить баги.

---

## 15. ЧТО НУЖНО ДЛЯ СБОРКИ (ключи/интеграции)
Все ключи/доступы автозагружаются в чате (SessionStart-хук) и лежат в `~/OKO_MASTER_VAULT.md`
(или `oko-setup.sh` в старом чате). Для этого билда конкретно нужны:
- **VPS control-plane**: `CONTROL_URL`+`CONTROL_TOKEN`, `OKO_POSTER_URL`+`OKO_POSTER_TOKEN` (мост,
  статусы, команды, база знаний).
- **Мост задач** (`taskqueue` на VPS) — отправка результатов клиентам/агентам.
- **Higgsfield** (MCP) — если для мозга/деталей нужны текстуры/HDRI/иконки (но геометрия — кодом).
- **Соцключи** (VK/YT/IG/TG, Gemini) — для живых данных агентов и их экранов.
- **Хостинг**: Higgsfield website (`website_repo_access` → deploy) или OKO App embed.
- **Playwright/Chromium** (`/opt/pw-browsers`) — самопроверка сцены и скрины экранов агентов.
Полный список значений — файл `OKO_MASTER_VAULT.md` (прислан отдельно) + `OKO_AGENT_CONTROL.md`
(как дёргать агента/мост/базу).

---

## 15а. СВЯЗАННЫЕ ФАЙЛЫ (в этом же ЗИПе)
- **`OKO_HQ_AGENTS.md`** — все 10 ролей детально (внешность, атрибут, поведение, экран, команды, данные) + диспетчер.
- **`OKO_HQ_BRAIN.md`** — мозг/память: категории-волокна, модель узла, визуал, загрузка/разбор, API, поиск.
- **`OKO_HQ_ROOMS.md`** — комнаты-отделы полноценной ИИ-компании: ФИНАНСЫ (бухгалтер, НДФЛ/УСН/ИП,
  декларации, казна), ЮРИДИЧЕСКИЙ, БЕЗОПАСНОСТЬ/ИНФРА (DevOps, антибан, антидетект, мониторинг),
  RESEARCH LAB, WAR ROOM, COMMS, PUBLISHING, HR/НАЙМ, МОЗГ, ЛАУНЖ — с честной пометкой что авто, что через сервис/ЭЦП.
- **`concept/scene.html` + `three.module.js` + `render.py`** — РАБОЧИЙ старт-код: 3 робота-агента OKO
  с глазом-логотипом, рендерится через Chromium (проверено). Отсюда стартует сборка персонажа.
- **`OKO_MASTER_VAULT.md`** — все ключи/токены/доступы.
- **`OKO_AGENT_CONTROL.md`** — как дёргать агента/мост/базу из кода.
- **`oko-setup.sh`** — one-file бутстрап (ключи+мост).
- **`brand/`** — лого OKO (png + base64) для глаза/бейджей.
- **`knowledge/sales_knowledge_artem.md`** — база продаж (память мозга).

## 15б. QUICKSTART для сборки в чате OKO App (FABLE 5)
1. Распаковать ЗИП, запустить `bash oko-setup.sh` (подтянет ключи+мост+vault).
2. `npm create vite@latest oko-hq -- --template react-ts && cd oko-hq && npm i three postprocessing gsap zustand howler`.
3. Взять за основу персонажа `concept/scene.html` (робот+глаз-лого уже работают), перенести в
   `src/agents/Agent.ts`, добавить скелет-анимации.
4. Идти ПО ФАЗАМ (§14), после каждой — самопроверка Playwright/Chromium (скрин+FPS):
   `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader`, сервить через
   `python3 -m http.server` (ES-модули по file:// режет CORS).
5. Данные — через `OKO_AGENT_CONTROL.md` (`CONTROL_URL`/`vexec`/`taskqueue`). Живой агент — не трогать
   Gemini-ключи батчами (локально whisper/tesseract).
6. Деплой: Higgsfield website (`website_repo_access`→`deploy_website`) или embed в OKO App.

**Стартовый минимум персонажа (из рабочего concept/scene.html):** округлый корпус (Sphere squished)
+ голова (Sphere) + лицо-глаз: дуга-веко (`TorusGeometry(...,Math.PI)`) + зрачок (`CircleGeometry`,
emissive #9AFF00) + вырез-полумесяц (тёмный `CircleGeometry` со смещением) + antenna + руки-капсулы +
акцент-кольцо роли. Свет: Hemisphere + Directional(shadow) + лаймовый PointLight. Tonemap ACES, Bloom.

## 16. ИТОГ
Это не «дашборд», а **игровой командный центр OKO** — живой 3D-штаб, где ты видишь всех агентов,
их экраны и мысли, кидаешь задачи голосом/файлом/текстом, а мозг в центре хранит и растит всю
память компании. Красиво, по-хакерски, без лагов, подключается ко всем чатам и в приложение.
