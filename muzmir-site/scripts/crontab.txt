# ЕДИНЫЙ CRONTAB САЙТА КЦ «МУЗЫКАЛЬНЫЙ МИР» — ИСТОЧНИК ПРАВДЫ.
#
# Этот файл — полный список заданий. Раньше их было два и оба неполные: здесь
# лежали 8 строк, в deploy/DEPLOY.md — 9 «полного набора», а реально в cron/
# двадцать с лишним рабочих скриптов. Ключевые (launch_scheduler — исполнитель
# волн запуска, monthly_launch — открытие нового месяца) не значились нигде,
# и при восстановлении crontab после переустановки сервера кампания просто
# не запускалась бы, причём молча.
#
# ЗАМЕНА СТИРАЕТ ВСЁ, ЧЕГО В ЭТОМ ФАЙЛЕ НЕТ. Так уже расходилось: здесь стоял 31
# задание, на сервере крутилось 49, и «восстановление по документации» молча
# снесло бы постинг ВК, приглашения педагогам, партнёрские триггеры и всю
# почтовую гигиену. Поэтому порядок действий именно такой, а не одна команда:
#   crontab -l > /var/www/muzmir/data/backups/crontab-$(date +%F).txt   # копия живого
#   php /var/www/muzmir/scripts/check_crontab.php                      # что разошлось
#   crontab /var/www/muzmir/scripts/crontab.txt                        # только теперь замена
# Появилось новое задание на сервере — дописать сюда в тот же день. Сторож
# расхождений стоит ниже в самом расписании и пишет владельцу в Telegram.
#
# ЧАСОВОЙ ПОЯС. Все времена ниже — МОСКОВСКИЕ. CRON_TZ ниже фиксирует это на
# уровне самого крона, чтобы часы не «поехали» при переустановке сервера с
# другой зоной по умолчанию. PHP переводится в Москву отдельно (config.php).
CRON_TZ=Europe/Moscow
MAILTO=""

# ── Каждую минуту ───────────────────────────────────────────────────────────
# Очередь писем: личные — сразу, массовые — по дневной норме и в окне 09–18.
* * * * * php /var/www/muzmir/cron/process_newsletter_queue.php >/dev/null 2>&1
# Исполнитель волн запуска (пульт «Запуск»). БЕЗ НЕГО КАМПАНИЯ НЕ ИДЁТ ВООБЩЕ.
* * * * * php /var/www/muzmir/cron/launch_scheduler.php >/dev/null 2>&1
# Автоматическая аттестация конкурсных работ. Задание само проверяет режим в
# админке (раздел «Аттестация») и при выключенном режиме сразу выходит, поэтому
# строка стоит всегда: включение и выключение делается человеком, а не правкой
# расписания. Раз в 10 минут: сервис оценки имеет минутные лимиты, а работы
# идут небольшими пачками.
*/10 * * * * php /var/www/muzmir/cron/ai_grade.php >> /var/www/muzmir/data/logs/cron.log 2>&1

# Дипломы: генерация и отправка по расписанию рабочих дней.
* * * * * php /var/www/muzmir/cron/send_diplomas.php >>/var/log/muzmir_send_diplomas.log 2>&1
# Дожим неоплаченных заявок (первое письмо через 3 минуты после подачи).
* * * * * php /var/www/muzmir/cron/dunning.php >/dev/null 2>&1
# Личные сообщения сообщества ВК (очередь vk_dm_queue) — только открытые диалоги.
* * * * * php /var/www/muzmir/cron/vk_dm_worker.php >/dev/null 2>&1

# ── Чаще раза в час ─────────────────────────────────────────────────────────
# Сверка платежей с ЮKassa (pull-реконсиляция — работает и без вебхуков).
*/2 * * * * php /var/www/muzmir/cron/reconcile_payments.php >/dev/null 2>&1
# Доступность сайта.
*/5 * * * * php /var/www/muzmir/cron/health_check.php >/dev/null 2>&1
# Отложенные афиши в соцсети.
*/5 * * * * php /var/www/muzmir/cron/publish_scheduled_posts.php >/dev/null 2>&1
# Ответы на обращения, пришедшие в нерабочее время («отвечу, как начнётся день»).
*/10 * * * * php /var/www/muzmir/cron/chat_offhours_flush.php >/dev/null 2>&1
# Закрытие «повисших» диалогов бота ВК.
*/10 * * * * php /var/www/muzmir/cron/vk_bot_close.php >/dev/null 2>&1
# Разбор возвратов почты (bounce).
*/30 * * * * php /var/www/muzmir/cron/process_bounces.php >> /var/www/muzmir/data/logs/bounces.log 2>&1
# Разбор событий доставки от сервиса рассылок: жёсткий отказ, жалоба, отписка
# снимают адрес с рассылки. Приёмник только складывает события, снимает — это.
*/5 * * * * php /var/www/muzmir/cron/mail_events_apply.php >> /var/www/muzmir/data/logs/cron.log 2>&1
# Сбор базы учреждений (школы искусств, дома культуры, центры творчества) из
# открытых источников. Только днём: ночью чужие сайты трогать незачем. Порциями,
# с паузами — источники чужие, и валить их запросами нельзя.
*/10 8-22 * * * php /var/www/muzmir/cron/harvest_institutions.php >/dev/null 2>&1
# Сбор адресов ведомств: реестр Минкультуры (один раз) и обход порталов регионов
# по одному за прогон. Государственные сайты слабые — темп подчёркнуто медленный.
*/20 9-20 * * * php /var/www/muzmir/cron/harvest_ministries.php >/dev/null 2>&1
# Очередь приглашений учреждениям: держится глубиной в два дневных объёма и
# добирается по мере отправки. Разом тридцать пять тысяч писем не ставим — они
# ушли бы через две недели и звали на уже закрытый конкурс.
#
# ВКЛЮЧЕНО ВЛАДЕЛЬЦЕМ 19.08.2026 вместе с партнёрской рассылкой. Задание само
# писем не отправляет, а наполняет очередь; окно 8-18 совпадает с окном отправки
# (nl_window_hour_from/to), иначе очередь копилась бы в часы, когда её не разбирают.
*/15 8-18 * * * php /var/www/muzmir/cron/queue_institutions.php >/dev/null 2>&1

# ── Раз в час ───────────────────────────────────────────────────────────────
# Ответы ведомств на обращения: скан письма поддержки сам попадает в раздел
# «Поддержка» на сайте, а владелец получает уведомление.
5 * * * * php /var/www/muzmir/cron/ministry_replies.php >/dev/null 2>&1
# Маркетинговые волны конкурсов (открытие приёма, «3 дня», «последний день»).
0 * * * * php /var/www/muzmir/cron/mailings.php >/dev/null 2>&1
# Продление подписок ВИП-клуба по сохранённому способу оплаты.
0 * * * * php /var/www/muzmir/cron/club_billing.php >/dev/null 2>&1
# Партнёрские триггеры: благодарности педагогам от 5 заявок и выдача заказанных.
15 * * * * php /var/www/muzmir/cron/partner_triggers.php >/dev/null 2>&1
# Уборка диска: превью, временные файлы, старые вложения писем. Час, а не шесть:
# в разгар кампании диск забивался за сутки и отправка вставала.
0 * * * * php /var/www/muzmir/cron/disk_cleanup.php >> /var/www/muzmir/data/logs/disk_cleanup.log 2>&1

# ── Раз в сутки ─────────────────────────────────────────────────────────────
# Бэкап БД.
0 3 * * * php /var/www/muzmir/cron/daily_backup.php >/dev/null 2>&1
# Свободное место на диске: предупредить владельца до того, как встанет отправка.
30 4 * * * php /var/www/muzmir/cron/disk_watch.php >/dev/null 2>&1
# Расход дневной нормы сервиса рассылок — считаем ДО открытия окна отправки.
0 8 * * * php /var/www/muzmir/cron/quota_watch.php >/dev/null 2>&1
# Подтверждение доменов и адресов ведомств (проверка отправителя).
20 8 * * * php /var/www/muzmir/cron/ministry_verify_watch.php >/dev/null 2>&1
# Даты конкурсов: автозакрытие приёма и генерация афиш.
0 9 * * * php /var/www/muzmir/cron/check_competitions_dates.php >/dev/null 2>&1
# ЧАС ЗАПУСКА ОБЯЗАН БЫТЬ ВНУТРИ РАБОЧЕГО ОКНА (пн-сб 09:00-19:00 МСК).
# Оба задания сами проверяют окно (core/outreach_window.php) и на 08:00 выходили
# на первой строке, не сделав ничего: письма о заказе наград не уходили вовсе, а
# окно заказа всего 60 дней. Вывод идёт в журнал, а не в /dev/null — иначе отказ
# по окну не виден нигде.
# Напоминания об оплате заказов наградных материалов.
0 11 * * 1-6 php /var/www/muzmir/cron/award_order_reminders.php >> /var/www/muzmir/data/logs/cron.log 2>&1
# Напоминания о датах конкурсов подписчикам календаря.
30 9 * * 1-6 php /var/www/muzmir/cron/calendar_reminders.php >> /var/www/muzmir/data/logs/cron.log 2>&1
# Напоминания участникам (оплата / награды / дедлайн).
0 10 * * * php /var/www/muzmir/cron/send_reminders.php >/dev/null 2>&1
# Цепочка касаний по базе (drip).
30 10 * * * php /var/www/muzmir/cron/drip.php >/dev/null 2>&1
# Возврат «спящих» участников.
0 12 * * * php /var/www/muzmir/cron/engagement.php >/dev/null 2>&1

# ── Раз в неделю / месяц / год ──────────────────────────────────────────────
# Полный бэкап (БД + uploads + дипломы), ночь с воскресенья на понедельник.
0 4 * * 1 php /var/www/muzmir/cron/weekly_backup.php >/dev/null 2>&1
# ОТКРЫТИЕ НОВОГО МЕСЯЦА: продление конкурсов и план волн. 1-го в 09:00 МСК,
# за час до запуска в 10:00. БЕЗ ЭТОЙ СТРОКИ НОВЫЙ СЕЗОН НЕ НАЧНЁТСЯ.
0 9 1 * * php /var/www/muzmir/cron/monthly_launch.php >> /var/www/muzmir/data/logs/launch.log 2>&1
# Месячная сводка владельцу.
0 8 1 * * php /var/www/muzmir/cron/monthly_report.php >/dev/null 2>&1
# ОБРАЩЕНИЯ В ВЕДОМСТВА В ДЕНЬ ЗАПУСКА. Скрипт запускается ежедневно и сам
# решает, его сегодня день или нет: письма готовятся только 1-го числа и только
# когда открыт хотя бы один бесплатный конкурс. Пока стоп-кран обращений опущен,
# письма ложатся в очередь приостановленными и никуда не уходят.
10 9 * * * php /var/www/muzmir/cron/ministry_letters.php >> /var/www/muzmir/data/logs/ministry.log 2>&1
# Итоги бесплатных конкурсов в ВК + письма участникам — 28-го в 10:00 МСК.
0 10 28 * * php /var/www/muzmir/cron/publish_results_vk.php >/dev/null 2>&1
# Годовой отчёт — 5 января.
0 6 5 1 * php /var/www/muzmir/cron/annual_report.php >/dev/null 2>&1

# ── Наружные каналы: только пн-сб и только в рабочее окно ───────────────────
# Правило владельца: наружу ничего не уходит ночью и в воскресенье. Каждое из
# трёх заданий проверяет окно и внутри себя (core/outreach_window.php), но час
# в расписании должен совпадать с окном, иначе задание просто не делает ничего.
# Промо-волны в чужие сообщества ВК.
0 9,11,13,15,17,18 * * 1-6 php /var/www/muzmir/cron/vk_promo.php >/dev/null 2>&1
# Личные сообщения администраторам сообществ.
42 10-20 * * * php /var/www/muzmir/cron/vk_admin_dm.php >/dev/null 2>&1
# Приглашения педагогам.
0 11 * * 1-6 php /var/www/muzmir/cron/teacher_invite.php >/dev/null 2>&1

# ── Почтовая гигиена и наблюдение (scripts/) ────────────────────────────────
# Сторож самого расписания: сверяет живой crontab с этим файлом и пишет
# владельцу в Telegram, если состав разошёлся в любую сторону.
0 7 * * * php /var/www/muzmir/scripts/check_crontab.php >> /var/www/muzmir/data/logs/cron.log 2>&1
# Домены с закрытым рассылочным каналом: список должен стареть сам.
40 7 * * * php /var/www/muzmir/scripts/mail_domain_policy.php >> /var/www/muzmir/data/logs/cron.log 2>&1
# Доля открытий по каждой почтовой службе — наш собственный постмастер.
50 7 * * * php /var/www/muzmir/scripts/mail_health.php >> /var/www/muzmir/data/logs/mail_health.log 2>&1

# ── Почтовое хозяйство после инцидента 17.08.2026 ──────────────────────────
# ЭТИ ЗАДАНИЯ РАБОТАЮТ НА СЕРВЕРЕ, И ЗДЕСЬ ОНИ ДОЛЖНЫ БЫТЬ ЖИВЫМИ СТРОКАМИ.
# Раньше они стояли комментариями «для памяти», и это оказалось ловушкой: сторож
# считал закомментированную строку записанной, печатал «совпадает полностью», а
# следующая же команда `crontab scripts/crontab.txt` стирала их с сервера. Среди
# них сторож разблокировки почты и придержка подавленных адресов, без которых
# рассылка встаёт молча.
# Ждёт, когда почтовая служба снимет 554 с рабочих ящиков, и возвращает канал.
35 * * * * php /var/www/muzmir/scripts/mail_unblock_watch.php >> /var/www/muzmir/data/logs/cron.log 2>&1
# Убирает из головы очереди адреса, которые сервис рассылок всё равно не отправит.
10 7 * * * php /var/www/muzmir/scripts/hold_suppressed.php --apply >> /var/www/muzmir/data/logs/cron.log 2>&1
# Снятие ложных подавлений у сервиса. Окно скользящее: зашитая дата через месяц
# означала бы «перебирать месячный список ради полусотни снятий в сутки».
15 7 * * * php /var/www/muzmir/scripts/unisender_suppression.php --clear-days=10 >> /var/www/muzmir/data/logs/cron.log 2>&1
# Один повтор письмам, которые почтовик отбил без объяснения.
30 7 * * 1-6 php /var/www/muzmir/scripts/requeue_false_bounces.php --days=2 >> /var/www/muzmir/data/logs/cron.log 2>&1
# Ответы ведомств, пришедшие на чужие ящики центра.
25 * * * * php /var/www/muzmir/scripts/process_ministry_inbox.php >> /var/www/muzmir/data/logs/ministry.log 2>&1
# Порядок в галерее писем поддержки: новые сверху, внутри региона по дате.
50 7 * * * php /var/www/muzmir/scripts/fix_ministry_letters.php >> /var/www/muzmir/data/logs/cron.log 2>&1
# Отметка отправленных обращений в ведомства — отдельным заданием (режим sync).
*/10 * * * * php /var/www/muzmir/cron/ministry_letters.php sync >> /var/www/muzmir/data/logs/ministry.log 2>&1

# ── Запускаются вручную, в crontab не нужны ─────────────────────────────────
#   cron/regen_regulations.php  — перегенерация положений-DOCX (all | <id>)
#   cron/vk_send_delayed.php    — отложенный ответ бота, запускается из вебхука ВК

# Входящая почта: читалка четырёх ящиков и разбор с автодействиями.
*/5 * * * * timeout 540 php /var/www/muzmir/cron/inbox_read.php >> /var/www/muzmir/data/logs/cron.log 2>&1
*/2 * * * * php /var/www/muzmir/cron/inbox_actions.php >> /var/www/muzmir/data/logs/cron.log 2>&1

# Гигиена базы рассылки: домены, которых нет, и адреса с повторными отказами.
# Раз в неделю: почтовые службы судят отправителя по тому, куда он пишет, и
# каждое письмо в мёртвый ящик портит доставку всем остальным.
0 6 * * 1 php /var/www/muzmir/scripts/mail_base_hygiene.php --apply >> /var/www/muzmir/data/logs/cron.log 2>&1
# Просьба указать адрес доставки: оплаченный заказ оригиналов без адреса.
0 12 * * 1-6 php /var/www/muzmir/cron/order_address_request.php --send >> /var/www/muzmir/data/logs/cron.log 2>&1
# Разбор расхождений с жюри: пересмотр записи и урок на будущее.
*/15 * * * * php /var/www/muzmir/cron/grade_learn.php >> /var/www/muzmir/data/logs/cron.log 2>&1
