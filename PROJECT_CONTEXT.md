# DUCK'S GAME SPACE — полный контекст сборки сайта

> Этот файл — память проекта. Здесь: что за проект, как устроен код, что уже сделано,
> какие ошибки были и как их НЕ повторять, как собирать/проверять/отдавать.
> Читать ПЕРЕД любой новой правкой.

---

## 1. О проекте

- **Что:** кинематографичный «магический» 3D-сайт для реального гейм-клуба DUCK'S GAME SPACE
  в Москве (покер, дартс, бильярд, бар). Клуб уже работает.
- **Эталон качества:** ~70–80% от https://www.igloo.inc (анимации, частицы, переходы камеры, звук,
  ощущение «бесконечного живого мира»).
- **Концепция (НЕ МЕНЯТЬ):** единый непрерывный 3D-мир, камера летит вперёд. Утка-маскот
  (в чёрном худи и очках) → распадается на частицы → собирается в МОЗГ (слоган «играй мозгами»)
  → при клике мозг превращается в ТУННЕЛЬ из частиц, летящий НА зрителя → внутри текст.
- **Фон:** Sketchfab «Sci-fi bar» GLB. **ЗАФИКСИРОВАН — НЕ ТРОГАТЬ.** Камера только едет вперёд.
- **Слоган:** «Клуб для своих.» Бренд: красный `#CC0000` на чёрном.
- **Заказчик:** Caterina (через OKO TEAM). Деплой — сам клиент: ZIP → ISPmanager → `/www/ducks.games/`.

### Жёсткие запреты (НИКОГДА)
- **Стоп-слова (юридически нельзя нигде — текст, alt, meta, даже в отрицании):**
  азартные игры / азарт / ставки / ставить / выигрыш / выплаты / казино / casino / gambling /
  bet / betting / покер на деньги / призовой фонд / банк.
- **НЕТ копирайта OKO TEAM** на сайте.
- **Секреты НЕ коммитить** (см. ниже).
- **Идентификатор модели** (`claude-opus-*`) не писать в коммиты/код/PR — только в чат.

### Секреты (НЕ в репозиторий, НЕ в dist)
- `DUCKS_SITE_CONTEXT.txt` содержит Google App Password и SMTP/ISPmanager креды.
  Должен оставаться вне git и вне сборки. Проверять `git ls-files | grep -i context` = пусто.

---

## 2. Стек и архитектура

- **Сборка:** Vite 5 (`vite.config.js`: `base: './'` — относительные пути, работает в любой папке хостинга;
  `assetsInlineLimit: 0`, `target: es2022`).
- **3D:** Three.js **0.164.1** (строго эта версия).
- **Скролл:** Lenis (плавный). **Анимации:** GSAP (timeline для интро).
- **Звук:** Web Audio API — ВСЁ синтезом, без файлов (`src/audio/sound-system.js`).
- **Загрузка моделей:** GLTFLoader + DRACOLoader. Декодер Draco **локальный** в `public/draco/`
  (`setDecoderPath('draco/')`) — НЕ с gstatic CDN (VPN/блокировки ломали загрузку).

### Файлы
```
index.html              # разметка: nav, #content (hero, about), #brain-detail, #loader, #sound-ind
src/main.js             # ВСЯ логика (~790 строк): сцена, частицы, камера, интро, скролл, туннель
src/style.css           # стили + адаптив (@media max-width:768px)
src/audio/sound-system.js   # SoundSystem: пэд + слои + эффекты (синтез)
src/three/environment.js    # ClubEnvironment: загрузка scifi_bar.glb, неон, PMREM-свет
src/three/poker.js          # PokerStation: овальный стол + 4 карты-формата (станция 3)
src/three/darts.js          # DartsStation: процедурная мишень + мини-игра броска (станция 4)
vite.config.js
public/draco/           # draco_decoder.wasm + draco_wasm_wrapper.js (ЛОКАЛЬНО)
public/models/          # duck.glb (~1MB), brain.glb (~1.4MB), scifi_bar.glb (~0.17MB) — Draco+сжатые
public/.htaccess        # MIME для wasm/glb/webp + SPA fallback
dev-assets/             # НЕиспользуемые тяжёлые ассеты (ball8.glb, dart.glb, references/) — НЕ в сборку
verify.cjs              # headless-проверка (puppeteer), в git; НЕ попадает в dist
```

### Рельса станций (важно для правок камеры/скролла) — `src/main.js`
`scrollProgress` 0..1 поделён на сегменты, `STATIONS = [0, HOLL_END, POKER_END, 1.0]`:
- `0..HOLL_END(0.34)` — ХОЛЛ (утка→взрыв→мозг), `tp = sp/HOLL_END`.
- `HOLL_END..POKER_END(0.67)` — ПОКЕР, `pk` (морф частиц мозга→карты, поворот камеры к столу).
- `POKER_END..1.0` — ДАРТС, `dk` (камера «наезжает вперёд» в темноту → мишень).
Камера лерпится: скролл-поза → туннель(tb) → POKER_CAM/LOOK (pkc) → DARTS_CAM/LOOK (dkc).
`env.fade` дополнительно гасит клуб на Дартсе (≈×0.25) → «пустота», скрывает бар-геометрию.
Добавляешь станцию (Бильярд) → расширь `STATIONS`, добавь её прогресс и блок лерпа камеры тем же приёмом.

### Ключевые параметры в `src/main.js` (верх файла)
- `PCOUNT = isMobile ? 65000 : 115000` — число частиц.
- `HERO_Y`, `SUBJ_SCALE`, `DUCK_FACE = 270°` — позиция/разворот утки (клювом в камеру).
- `DUCK_DIST` — расстояние фигуры от камеры (моб 6.5 / ПК 4.6).
- Камера (подобрана клиентом, НЕ менять без спроса):
  `CAM0 = {x:0.39, y:0.10, z:20.69}`, `LOOK = {x:-0.49, y:-5.52, z:-8.27}`.

### Debug-параметры URL
- `?fast` — пропустить интро (для скриншотов финальной композиции).
- `?tune` — OrbitControls + HUD с координатами (для подбора ракурса фона).
- `?duck=град`, `?camy ?look ?camz ?roty ?dx ?dy ?dist` — тонкая подстройка.
- Тест-хуки (в коде, безвредны): `window.__setScroll(v)`, `window.__teleport`,
  `window.__openBrain()/__closeBrain()`, `window.__lenis`, `window.__scene`, `window.__camera`.

---

## 3. Как устроена анимация частиц (важно для правок)

Один `THREE.Points`. `sampleModel()` сэмплирует треугольники GLB по площади и **заполняет объём**
(cbrt-распределение → мозг плотный, не просвечивает).

Состояния по `tp` (scrollProgress 0..1):
- `tp 0..0.5`: утка → «взрыв»/облако (`explodePos`).
- `tp 0.5..1`: облако → мозг (`brainPos`).
- `tp > 0.75`: `onBrain` — мозг собран, можно кликать.

**Туннель** (`tunnelBlend` 0→1 плавно): кольца частиц (угол+радиус+фаза глубины), летят по −Z НА камеру.
Труба **предповёрнута на −brainYaw**, чтобы при любом угле вращения мозга лететь РОВНО на зрителя
(без «увода вбок» и без рывка-доворота на входе/выходе).

**Физика (текущая, рабочая):**
- `RETURN_SHAPE/RETURN_TOUCH` — насколько держится форма / мягкий возврат после касания.
- Касание = мягкое «раздвигание песка» (НЕ пружина, НЕ взрыв) + свечение внутри + плавное оседание.
- Само-движение «живое» — в ШЕЙДЕРЕ, привязано к точке формы (масса не расплывается).
- `reform` — окно сильной пересборки мозга после выхода из туннеля (чинит «дырку»).
- `brainBurst` — лёгкий толчок при входе (0.35, не взрыв).

**Свет/материал частиц:** монохром ледяной-белый, мягкое круглое зерно, объёмная подсветка,
свечение от скорости/касания. Фон клуба гаснет у мозга (`env.fade`), чтобы неон не просвечивал.

---

## 4. Что СДЕЛАНО и работает (на момент этого отката, commit 154434c)

- ✅ Загрузка моделей (Draco локально), относительные пути — грузится на хостинге.
- ✅ Интро: кубики падают с отскоками + звук → утка приходит из глубины, клювом в камеру → текст.
- ✅ Hero: заголовок/кнопки фиксированы, при скролле растворяются на месте + наплыв (не уезжают вверх).
- ✅ Утка строго фронтально, ровно, не налезает на текст/кнопки.
- ✅ Частицы: плотный объём (не просвечивает), живое движение, касание как «песок» + свечение, БЕЗ пружины.
- ✅ Мозг: собирается по центру, крутится на 360.
- ✅ Туннель: летит строго на зрителя, без поворота на входе/выходе; текст приближается из точки;
  затемнение плавное (не резкое); подложка текста без видимой рамки.
- ✅ Дырка после туннеля — чинится окном `reform` (проверено замером: центр восстанавливается).
- ✅ Карточки About: ровная сетка 2×2, номера не налезают, светящаяся «жила».
- ✅ Звук: пэд + слои (шелест частиц на мозге, гул), эффекты; уход со вкладки → fade; индикатор «SOUND: ON».
- ✅ **Покер (станция 3):** овальный стол DUCK'S, 4 карты-формата, клик по карте → подъём/переворот, анкета.
- ✅ **Дартс (станция 4):** процедурная мишень (20 секторов, дабл/трипл, булл/яблочко, неон-обвод #CC0000),
  медленно вращается. Мини-игра броска: ТАП по мишени → дротик летит дугой из-за камеры → втыкается →
  очки по зоне (замером: центр=ЯБЛОЧКО 50, секторы верно). Серия 3 дротика → суммарный счёт (HUD `#darts-score`),
  следующий тап после серии — новая. Звуки: свист броска + втык (`playDartThrow`/`playDartHit` wood/wire/bull).
  Переход Покер→Дартс: камера наезжает вперёд, клуб гаснет в «пустоту». Easter egg `tipsy` (0..1) готов в коде
  (`window.__setTipsy`, прицел плывёт) — подключить к Бару, когда он будет. На ПК прицел следует за курсором.
- ✅ Вес сборки: ~3.6 MB (убраны неиспользуемые ассеты в dev-assets/).

### Что ОТКАЧЕНО (НЕ делать так же снова без переработки)
- ❌ **Экран «нажми чтобы войти» (enter-screen)** — сломал сайт: прелоадер перекрывал кнопку,
  лишнее правило `pointer-events:none`, и (по фото клиента) **пропадала утка** + мозг налезал на текст.
  Откатили к 154434c. Если делать снова — только аккуратно, с полной headless-проверкой реального тапа
  И проверкой, что утка появляется и мозг на месте.

---

## 5. ОШИБКИ, которые уже были (НЕ ПОВТОРЯТЬ)

1. **`Object.assign(new THREE.PointLight(...), {position:...})`** → `position` read-only → краш всей сцены
   → вечная загрузка. **Фикс:** `mkLight()` с `l.position.set()`. (Виновата была НЕ хостинг, а код.)
2. **Draco-декодер с gstatic.com** блокировался VPN → модели не декодировались. **Фикс:** локально в `public/draco/`.
3. **Абсолютные пути `/assets/`,`/models/`** ломались на хостинге. **Фикс:** vite `base: './'`.
4. **Импорты вперемешку с кодом** → "Cannot access 'ri' before initialization". **Фикс:** все import сверху,
   `let orbit=null` рядом с верхом.
5. **Тяжёлые модели (29 MB)** → часы загрузки. **Фикс:** gltf-transform Draco+сжатие до 1–1.4 MB.
6. **Сэмплинг 70k+ точек** морозил headless. Держать разумный PCOUNT.
7. **Скриншоты headless ловили середину анимации** (низкий FPS) → казалось, что сломано.
   **Фикс:** `?fast` + тест-хуки `__teleport/__setScroll`, давать время на сходимость, мерить ЧИСЛАМИ.
8. **Трогала фон** (прятала меши вентилятора) — клиент против. Фон ЗАФИКСИРОВАН.
9. **`?roty=` в адресной строке** делал вид «сломано» (серая стена) — открывать чистый ducks.games.
10. **JS-«поток» со слабым удержанием** → масса расплывалась, теряла форму мозга.
    **Урок:** форму держать жёстко (JS), «жизнь» — в шейдере, привязанной к точке.
11. **Частицы additive на тёмном фоне** → серость и просвечивание. **Фикс:** плотное непрозрачное зерно + cbrt-объём.
12. **Вращение трубы вокруг Z** → «вправо-влево». **Фикс:** труба предповёрнута на −brainYaw, летит по −Z.
13. **Дырка после туннеля** — частицы догоняли трубу/застывали кольцом. **Фикс:** окно `reform` + сброс vel/glow/disturb.
14. **enter-screen** перекрыт прелоадером + `pointer-events:none` → некликабельно, утка пропадала. ОТКАЧЕНО.
15. **Edit падал на смайл/юникод-комментариях** — копировать точную строку из файла, не «на память».
16. **Дамп сырых байтов в терминал** (`od -c` на кириллице) однажды вызвал сбой API. НЕ дампить бинарь/мусор.
17. **Ложь в коммите** («tool output tampered») — так НЕ делать. Сообщения честные.
18. **Пустые/битые вызовы инструментов** (SendUserFile без files, AskUserQuestion без questions) — проверять аргументы.

---

## 6. Рабочий процесс (ОБЯЗАТЕЛЬНО перед отдачей)

1. Правка → `npm run build` (должен пройти без ошибок).
2. **Проверить в headless-браузере** (`node verify.cjs mobile`) — скриншоты hero/explode/brain/tunnel.
   Для физики/туннеля — мерить ЧИСЛАМИ (плотность центра, расстояние до формы), а не на глаз.
3. Убедиться: **утка видна** в hero, мозг на месте (не на заголовке), нет console-ошибок (PAGEERR).
4. Только потом паковать ZIP и отдавать. **Не отдавать непроверенное.**
5. Если тест показал баг — НЕ отдавать, чинить и перепроверять.

### Сборка ZIP для клиента
```
rm -rf dist && npm run build
cd dist && zip -r -q ../ducks_FULL.zip . -x '.*' && zip -q ../ducks_FULL.zip .htaccess && cd ..
# проверить целостность: md5 зипнутого JS/CSS == свежесобранного dist
```
Клиент распаковывает и заливает ВСЁ содержимое в `/www/ducks.games/` с заменой.

### Git
- Ветка разработки: `claude/adoring-tesla-fFKEd`. Пуш: `git push -u origin <branch>`.
- `.gitignore`: `node_modules/`, `dist/`, `*.zip`, `_v_*.png`, `dbg*.cjs`, `*.log`.
- PR не создавать без явной просьбы.

---

## 7. Открытые «на глаз» моменты (проверяет только клиент на телефоне)

Headless не передаёт: реальную скорость/плавность движения частиц, ощущение касания, ЗВУК.
Это настраивается по видео/фидбеку клиента точечно (числа: амплитуда потока, сила касания,
громкость/тайминг звуков). Композицию, плотность, форму, отсутствие дырки — проверяю замером.

### Нерешённое/спорное
- Автозвук «с самого начала»: браузеры запрещают звук до первого жеста. Варианты — экран-вход
  (был, откатили из-за бага) ИЛИ старт на первом скролле/касании. Согласовать с клиентом заново
  и сделать аккуратно с полной проверкой.
- Сайт задуман как единый «фильм» из 7 станций по кругу (бесконечная петля). Полный режиссёрский
  сценарий, карта 3D-моделей, спека анкеты и бренд-правила — в `SCENARIO.md`. Готовы 4 станции
  (Холл: hero + мозг/туннель → Покер → Дартс). Следующая по сценарию — **Бильярд** (без мини-игры,
  шар-8 + инфо), затем Бар (мини-игра «глоток», связать с `tipsy` Дартса), FAQ, CTA + бесшовная петля.

---

## 8. История фидбэка клиента (суть)

Клиент очень внимателен к деталям и устал от итераций. Ключевые требования, повторённые много раз:
- Частицы как у igloo: **полностью заполненный мозг** (не просвечивает), **каждая частица бегает**
  по всей форме (внутри и снаружи, иногда за контур), движение быстрое но **плавное**, не «дрожит на месте».
- Касание: **не пружина/резина/жвачка**, а «палец в песке» — мягко раздвигает, светится, плавно стекается.
- Туннель: строго **НА зрителя**, без увода вбок и без поворота мозга на входе/выходе; после выхода
  мозг **полностью собирается** (была «дырка»); текст приближается из точки; затемнение плавное.
- Звук на каждый смысловой момент (загрузка, кубики, утка, запуск, туннель), шелест частиц на мозге.
- 1 экран: текст появляется ПОСЛЕ кубиков; утка ровно, не налезает; кнопки под лапками.

**Тон работы:** отдавать клиенту ТОЛЬКО проверенный, рабочий результат. Не гонять по кругу.
Честно говорить, что проверено замером, а что — только клиент увидит вживую.

---
---

# СЕССИЯ 2 ИЮНЯ 2026 — ДЕТАЛЬНЫЙ ЛОГ (читать ПЕРЕД новой задачей!)

> Очень длинная сессия (~20+ итераций). Клиент вылизывал Покер, страницу Мозга, физику касания, интро,
> туннель, производительность. ГЛАВНЫЕ УРОКИ: (1) НЕ менять утверждённое; (2) физику касания НЕ трогать
> без явной просьбы — клиент ею дорожит; (3) отдавать ТОЛЬКО проверенное ЗАМЕРОМ (числа); (4) «ничего
> не меняется» = КЭШ HTML; (5) не делать «дыру/пузырь/взрыв» при касании.

## 9. ПОКЕР (стр. 3) — УТВЕРЖДЕНО ✅
Правки по фото клиента (всё в `src/three/poker.js`, `index.html`, `src/style.css`):
- Карты — ровный ряд над окошками: `cardXs` шаг 0.82, `card.position z=0` (окошки точно под картами).
- Стол приблизить, овал шире/менее вытянутый: `HALF_L 2.6→2.4, HALF_W 1.4→1.7`; камера ближе (`POKER_CAM z`).
- Подпись «НАШИ ТУРНИРЫ · НАЖМИ НА КАРТУ» поднята к картам, ярче/крупнее, масштаб по ширине сукна (`maxW`).
- Заголовок «ПОКЕР» ниже шапки (`.poker-top top:13vh`), кнопка «Записаться» вынесена ВНИЗ (`.poker-bottom bottom`).
- Кнопка Покера кликабельна ТОЛЬКО когда станция проявлена (pointer-events драйв в JS) — баг клика над мозгом убран.
- Белые поля карт убраны: тёмный торец `edge = 0x140a0e`, зум арта `coverDraw(...,1.26)`.
- Цитата клиента после: всё устроило, перешли дальше.

## 10. МОЗГ — САМАЯ ДЛИННАЯ САГА. ФОРМА УТВЕРЖДЕНА в v43 — НЕ ПЕРЕДЕЛЫВАТЬ! 🔒
Клиент 20+ раз: «не похоже на мозг», «отрисуй хоть 100 раз». Что перебирали и ЧЕМ кончилось:
- Красный сплошной (как глянцевый референс) → «красный вообще плохо смотрится» → откат.
- Искусственный пинч-борозда `sculptBrain()` → «грецкий орех» → УБРАЛ ПОЛНОСТЬЮ. brain.glb = 219k верш/413k tri,
  детальная анатомия — её НЕ деформировать.
- Рельеф через НОРМАЛИ граней (`sampleModel` отдаёт `__nrm`): `nDotR = dot(нормаль, радиаль)` →
  гребни ярко, борозды/стенки темно (`fold`, `fs`). + глубина по радиусу (нутро темнее → силуэт).
- Продольная борозда: затемнение `fissure = exp(-(bx²)/0.03) * smoothstep(by)` (центр, верх).
- ❗ИТОГ, ЧТО КЛИЕНТ УТВЕРДИЛ И ПОПРОСИЛ «СОХРАНИ И ЗАКРЕПИ!!!!» (v43):
  - `brainPos = sampleModel(brain, PCOUNT, 0.9)` — ТОЛЬКО ПОВЕРХНОСТЬ (оболочка, без заливки).
  - размер частиц 2× мельче: `sizes[i] = 0.017 + random*0.015`.
  - рельеф ТОЛЬКО ВНУТРЬ: `disp = Math.min(0, (nDotR-0.5)*0.14)` — гребни НЕ торчат наружу.
  - мозг 2% меньше вокруг центра: `for(...) brainPos[i] *= 0.98`.
  - цвет голубой/циан + постоянное свечение (фрагмент-шейдер).
- Цитата: «форма идеальная сохрани пожалуйста и закрепи!!!!». → ПОНРАВИЛОСЬ. Больше форму НЕ трогать.

## 11. УТКА
- Ошибка: сделала утку сразу частицами → «не понятно что это, а не утка» → ВЕРНУЛ твёрдую цветную GLB.
- Утверждено: твёрдая цветная утка + слежение за пальцем/наклоном + кряк по тапу.
- Локальный распад утки в месте касания (дыра в меше через `onBeforeCompile discard`) — НЕ вышло чисто,
  клиент дал fallback (убрать частицы у утки при касании). Сейчас распад — только при скролле 1→2.
- ИНТРО (утверждено, проверено vintro.cjs): кубики падают ДО НИЗА → растворяются в частицы → частицы
  собираются в утку СНИЗУ ВВЕРХ → цветная утка проявляется снизу вверх (Y-обрезка меша `uDuckReveal` по
  МИРОВОМУ Y). Текст СКРЫТ пока кубики видны, появляется ПЛАВНО во время сборки.
  ❗БАГ, который чинили: текст был виден сразу из-за `updateUIByScroll` ставившего inline `opacity=1` при tp=0.
  ФИКС: ввёл `heroReveal` (0 в начале → 1 в интро), opacity = `(1-e)*heroReveal`.

## 12. ФИЗИКА КАСАНИЯ — КРИТИЧЕСКОЕ ПРАВИЛО (не нарушать!)
Клиент МНОГО раз и с возмущением: «НЕ трогай физику касания, она была шикарная, я не просил менять»,
«не нужен пузырь», «не нужна дыра/круг вокруг касания», «не взрыв».
- ❌ РАДИАЛЬНЫЙ выброс наружу (`vel += (dx/dist)*push`) = ДЫРА/КОЛЬЦО/ПУЗЫРЬ — клиент ЭТО НЕНАВИДИТ.
- ❌ scatter/«рассыпание» в стороны — тоже не зашло.
- ✅ УТВЕРЖДЕНО (v43): касание = ВИХРЬ вокруг пальца (тангенциаль `(-ry, rx)*sw`) + ПОТОК за движением
  пальца (`pointerMove * fall * 0.38`). БЕЗ радиали наружу → НЕТ дыры. Возврат МЕДЛЕННЫЙ и плавный
  (`TOUCH_DAMP=0.90`, `RETURN_TOUCH=0.01`, `disturb *= touchOn?0.975:0.95`). Цитата клиента про возврат:
  «когда отпускаю так быстро не возвращались, плавный возврат».
- ПРАВИЛО: «АНИМАЦИЯ КАСАНИЯ МОЗГА РАБОТАЕТ ВСЕГДА» — тап, свайп вверх/вниз/вбок/в угол, ЛЮБОЕ касание =
  анимация в месте касания, ОДНОВРЕМЕННО со скроллом.

## 13. КАСАНИЕ + СКРОЛЛ ОДНОВРЕМЕННО (мучили ~10 раз — РЕШЕНО, проверено)
Корни бага и фиксы:
- (a) Вертикальный драг по мозгу срабатывал как КЛИК → `openBrain()` (вход в туннель + `lenis.stop`) →
  «скролл не работает, только анимация входа». ФИКС: флаг `_dragged` (движение >8px → НЕ клик; в обработчике
  click: `if(_dragged) return`).
- (b) При скролле 2→3 морф мозг→карты (`inTrans`) ПЕРЕТИРАЛ позиции каждый кадр → касание давало только
  СВЕЧЕНИЕ+звук БЕЗ движения частиц. ФИКС: в `inTrans` смещение касания копится в `vel` и плавно затухает
  ПОВЕРХ морфа (`arr = morphPos + vel`) → частицы РЕАЛЬНО двигаются.
- (c) `brainGrab` — защёлка: палец на мозге (`hitSubject`) → анимация ВКЛ на весь жест, любая сторона.
- (d) `overscroll-behavior:none` (html,body) — запрет браузерного свайпа-назад (это был «глюк горизонтали»).
- ПРОВЕРЕНО `vtouch.cjs` (хуки `__glowArr/__velMax/__scrollState/__setStation`): свайп вверх→Покер (движение
  vel~0.39 + навигация 1→2), вниз→1стр (vel + 1→0), горизонт (vel, без навигации), brainOpen=false везде.
- ❗СКРОЛЛ ЗАКРЕПЛЁН — работает везде при вертикальном жесте. НЕ ЛОМАТЬ (Lenis НЕ стопить при касании мозга).

## 14. «НИЧЕГО НЕ МЕНЯЕТСЯ» = КЭШ HTML (важнейший системный фикс)
Несколько раз клиент: «ничего не поменялось» по ВСЕМ пунктам сразу → причина: браузер кэшировал старый
`index.html`, который ссылается на старый хэш JS → грузилась СТАРАЯ сборка.
ФИКС в `public/.htaccess`: no-cache для HTML
(`<FilesMatch "\.html$"> Header set Cache-Control "no-cache, no-store, must-revalidate"`,
`ExpiresByType text/html "access plus 0 seconds"`).
+ Метка сборки в консоли: `console.log('... build vNN ...')` — клиент проверяет, что загрузилась НОВАЯ версия.
ПРАВИЛО: при отдаче напоминать — залить с заменой `.htaccess` + ХАРД-РЕФРЕШ/инкогнито; проверить плашку buildNN.

## 15. ПРОИЗВОДИТЕЛЬНОСТЬ (клиент: «лагает, виснет, сделай легче без изменения картинки»)
idle-оптимизация в цикле частиц: когда фигура в ПОКОЕ (станция устоялась `scrollStill`, не трогают, нет
морфа/туннеля/интро, `_glowMax<0.012`) — НЕ пересчитываем и НЕ грузим 65k точек в GPU (`if(!idle){ ...loop...
needsUpdate }`). Шейдер сам «течёт» (uTime) + объект крутится (transform). Визуально 1:1, CPU/шина разгружены.

## 16. ТУННЕЛЬ
- Вход/выход ускорены по просьбе: `TUNNEL_DUR 3.0→2.0→1.3`; тайминг текста CSS ускорен на ~1с
  (`.bd-inner` enter `0.5s` delay, exit `1.9s`).
- Выход = инверсия входа: класс `tunnel-exit` держим по ТАЙМЕРУ (`_exitTimer 2200ms`), НЕ снимаем при
  `tunnelBlend≈0` — иначе текст обрывался (туннель закрывается за 1.3с, текст улетает дольше).
- Текст 2 стр. СКРЫТ во время выхода (`body.exit-textwait`, снимается ~0.7с) → появляется ПЛАВНО во время
  сборки мозга, а не после паузы.

## 17. ПРОЧЕЕ (утверждено)
- Заголовок 2 стр.: **«ИГРАЙ УМОМ / ОТДЫХАЯ ТЕЛОМ»** (`index.html .about-title`).
- Убрала один дальний вентилятор клуба (узел `Fan` в `scifi_bar.glb`, у окна), ближний `Fan3` оставлен.
- Тёмная полоса сверху на стр. мозга — ВЕРОЯТНО рамка окна 3D-клуба (на стр.1 такой нет). Уменьшила тень
  заголовка. ОЖИДАЕТ подтверждения клиента (подсветить/убрать прицельно).

## 18. ИНСТРУМЕНТЫ ПРОВЕРКИ (созданы, в .gitignore — НЕ в сборку)
- `vtouch.cjs` — замер касания (glow + velMax) во все стороны + навигация + brainOpen.
- `vintro.cjs` — РЕАЛЬНОЕ интро (кликает вход, ловит ПОРЯДОК событий: кубики→формирование→текст→цвет снизу-вверх).
- `vscroll.cjs` — симуляция драга пальцем, проверка навигации между станциями.
- Хуки в коде (безвредны): `window.__introState/__scrollState/__glowArr/__velMax/__setStation`.
- ❗gsap в headless идёт ~×8 МЕДЛЕННЕЕ — проверять ПОРЯДОК событий, не абсолютные секунды.
- ❗headless часто падает на тяжёлом морфе (swiftshader) — это НЕ баг сайта; снимать hero/brain до краша,
  мерить числами.

---

## 19. МЕТА-ПРАВИЛА после этой сессии (соблюдать ВСЕГДА)
1. **ПЕРЕД любой новой задачей — ПЕРЕЧИТАТЬ этот файл целиком.**
2. **НЕ трогать утверждённое:** форма мозга (v43), физика касания (v43), скролл. Меняя рядом — не ломать.
3. **Физику/анимацию касания НЕ менять без явной просьбы.** Нет радиали-наружу (дыра/пузырь/кольцо/взрыв).
4. **Касание мозга = ВСЕГДА**, при любом событии, одновременно со скроллом.
5. **Проверять ЗАМЕРОМ** (glow, velMax, scrollProgress, порядок событий) — не «на глаз». Не отдавать непроверенное.
6. **«Ничего не меняется» = кэш** → no-cache HTML стоит; напоминать про хард-рефреш + метку build.
7. **Отдавать ZIP САЙТА + этот .txt** каждый раз (клиент попросил отдельно файл контекста каждую сессию).
8. **Фон не трогать. Стоп-слова не писать. Идентификатор модели — только в чат.**

---

## 20. УТВЕРЖДЁННЫЙ ПЛАН САЙТА (кольцо, бесконечная петля)
| Стр. | Станция | Статус |
|---|---|---|
| 1 | ХОЛЛ — утка (интро, slоган) | ✅ ГОТОВО (утверждено) |
| 2 | МОЗГ + ТУННЕЛЬ внутрь | ✅ ГОТОВО (утверждено) |
| 3 | ПОКЕР — стол, 4 карты, АНКЕТА (уже встроена в Покер) | ✅ ГОТОВО (утверждено) |
| 4 | **ДАРТС** — мишень по центру, мини-игра броска дротика | 🔜 СЛЕДУЮЩАЯ |
| 5 | БИЛЬЯРД — шар-8 крутится + инфо (БЕЗ мини-игры — решение клиента) | ⬜ |
| 6 | БАР — бокал, мини-игра «глоток», «без кальянов/чистый воздух» | ⬜ |
| 7 | FAQ — карты-вопросы в пространстве | ⬜ |
| 8 | CTA + контакты | ⬜ |
| — | ПЕТЛЯ: утка крякает → распад в частицы → чёрная пауза → бесшовно обратно в ХОЛЛ | ⬜ |

(Полная режиссура каждой станции, связки, звук, easter-egg «опьянение» tipsy — в `SCENARIO.md`.)

**ТЕХНИЧЕСКОЕ (бэкенд анкеты `mail.php` + Телеграм-бот) — В САМОМ КОНЦЕ** (клиент: «техническое за пару минут,
сейчас сайт и визуал»). Анкета (фронт) уже встроена в Покер: поля имя/email/телефон/игра/формат + экран «спасибо».

### СЛЕДУЮЩАЯ ЗАДАЧА: СТРАНИЦА 4 — ДАРТС
По `SCENARIO.md` (разд. 3–4): связка Покер→Дартс (пролёт сквозь темноту/градиент), мишень по центру,
заголовок «ДАРТС», карточки снизу (атмосфера, «каждый бросок — твой»), дротик прилетает в цель;
мини-игра броска. Easter egg «опьянение» (`tipsy 0..1`) — связь бар↔дартс.
ПЕРЕД стартом — согласовать с клиентом конкретику: полноценная мини-игра броска ИЛИ мишень + инфо + анимация дротика?
