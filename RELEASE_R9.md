# Rocket r9 — опубликован, готов к клиентской приёмке

Дата: 9 сентября 2026. Опубликован `20260909-r9`; исходники — коммит `d7d45406ba29510c813a7d8b67dfa14eee0cccb3`. Предыдущий выпуск `20260908-r8` сохранён для отката.

## Что исправлено

- Аналитика CDN и игры повторяет отправку после сетевой ошибки и отказа `sendBeacon`. Стабильные идентификаторы защищают от дублей; пакеты ограничены реальным лимитом API — 25 событий. Возврат из bfcache восстанавливает сбор. Очередь хранит до 250 событий в памяти вкладки; закрытие браузера без сети не гарантирует доставку.
- Редактор контента сохраняет черновик при неудачном сбросе. Сохранение, сброс и переключение продукта защищены от запоздалых ответов. Сброс требует подтверждения. Перед сохранением проверяется объект JSON.
- Кабина сохраняет авторские карты и свойства материалов при переходе Phong → Standard. Карта шероховатости больше не делает поверхности излишне зеркальными; сила мелкого рельефа уменьшена. На новых кадрах устранены резкие точечные блики обшивки.
- Модуль кабины CDN и VPN синхронизирован: одинаковые материалы, повышенная чёткость экранов и совместимость текстур с WebGL 1. Особенности VPN остаются в его отдельном модуле сцены.
- Кэш CDN обновлён меткой `20260909r9`; VPN использует время изменения файлов.

## Результаты проверки

| Проверка | Результат |
|---|---:|
| JavaScript и геометрия | 54 прошли |
| Изолированный API | 13 прошли |
| Изолированное хранилище | 12 прошли |
| Изолированная доставка | 7 прошли |
| Развёртывание, откат, перенос | 10 прошли |
| Манифест выпуска | 24 файла совпадают |
| Нативный PHP 8.3.6 на клиентском сервере | 24 проверки прошли |
| Параллельная запись PHP | Все 240 изменений сохранены |
| CDN и полёт: новые кадры | 46, без ошибок GL/шейдеров |
| VPN: финал вперёд и назад | 12 кадров, без ошибок GL и событий |
| Проверка сервера после активации | 30 прошли |
| Прежние клиентские заявки | Все 5 сохранены |

Проверки PHP на сервере использовали отдельную временную папку и искусственные данные. Реальные уведомления не отправлялись. Часть нативных проверок повторяет локальные: это разные среды, а не 120 уникальных сценариев.

Протокол: `review/r9-verification.json`. Отчёты и выбранные кадры: `review/native-flight-r9/`. Папки `desktop` и `portrait` показывают первоначальный r9; `refined-*` — окончательную правку материалов. SHA-256 исходных модулей в новых отчётах совпадают с файлами этого выпуска.

## Публикация

Доступ к существующему клиенту восстановлен после точного разрешения пользователя. Изолированный графический стенд запущен и проверен. Выпуск r9 активирован на `rocketvpn.top` и `rocketcdn.ru`.

Проверка сервера подтвердила главные страницы, API, все 23 непосредственно выдаваемых файла манифеста и активный таймер доставки — 30 успешных проверок. Сверка идентификаторов подтвердила сохранность всех пяти существовавших заявок; новых заявок проверка не создавала. Протоколы: `review/r9-server-receipt.json` и `review/r9-publication.json`.

Повторная внешняя HTTP-проверка из среды исполнителя не завершилась: сетевые запросы были отменены до решения о доступе, а веб-сервис не разрешил открыть адреса. Поэтому внешний повторный прогон после публикации не заявляется. Серверные проверки выполнены по TLS с правильными Host-заголовками и проверкой отдаваемых байтов.

Манифест содержит 24 производственных файла; относительно r8 меняются шесть. Скрипт проверяет основу, готовит отдельный каталог, переключает оба сайта и возвращает r8 при неудаче проверок. Предыдущий выпуск сохраняется для отката. PHP приложения, рабочая конфигурация и клиентские записи не перезаписываются.

Проверка и откат на клиенте:

```sh
sudo python3 /var/www/rocket-releases/20260909-r9/deploy/followup.py health
sudo python3 /var/www/rocket-releases/20260909-r9/deploy/followup.py rollback
```

Откат возвращает код r8, сохраняя рабочие записи. Это инструкция на случай необходимости; откат не выполнялся.

## Границы приёмки

Новые кадры получены через нативный WebGL 1 в двух размерах, 640×400 и 390×844. Это фиксированные точки исходных сцен, а не измерение FPS на телефоне. Облачный браузер отключает WebGL. В нём проверены загрузка CDN, RU/EN и искусственная админка; после открытия подтверждения сброса транспорт автоматизации перестал отвечать, поэтому повторную полную проверку всех 17 разделов в этом проходе не заявляем. Предыдущий проход всех разделов сохранён в отчёте r8.

Непрерывное движение, звук, WebGL 2/HDR и FPS на физических устройствах, точное совпадение с Igloo, фотореализм и 8K остаются неподтверждёнными критериями приёмки. Публикация r9 не равна безусловной гарантии «100% на любом устройстве».

Для переноса в исходный монорепозиторий использовать только `deploy/rocket-overlay.json` и `deploy/integrate.py`. Не переносить всю независимую ветку поверх монорепозитория.
