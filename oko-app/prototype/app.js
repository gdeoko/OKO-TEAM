/* OKO app bundle v2.13 — modules, off-critical */

/* ===== module: core-ext ===== */
const SIGNATURE_B64_URL = 'media/cert/signature.png';   /* было 58 КБ base64 прямо в коде */
const SEAL_B64_URL = 'media/cert/seal.png';   /* было 111 КБ base64 прямо в коде */
/* ================= CORE-EXT: ядро супер-приложения =================
   Кошелёк (лицевой счёт), i18n, попапы, печать/подпись, verified, admin-auth.
   Все модули опираются на эти API. */

/* ---------- КОШЕЛЁК / ЛИЦЕВОЙ СЧЁТ ---------- */
/* Счёт открывается ПУСТЫМ (правка Даниэля 09.08: ноль демо-данных).
   Раньше прототип сразу рисовал 2 500 ₽ «приветственного бонуса» — человек
   видел на балансе деньги, которых у него нет. Пополнение и партнёрские
   начисления кладут сюда реальные суммы. */
/* МИГРАЦИЯ 09.08: у тех, кто заходил в прототип раньше, в localStorage лежат
   демо-суммы (2 500 ₽ приветственного бонуса, накрученные переводы, XP и
   токены). Один раз чистим их, иначе человек продолжает видеть деньги,
   которых нет. Метку ставим, чтобы не стирать реальные операции повторно. */
(function okoWipeDemoMoney(){
  try{
    if(localStorage.getItem('oko-money-reset-v1') === '1') return;
    localStorage.removeItem('oko-wallet');
    localStorage.removeItem('oko-wallet-x');
    localStorage.removeItem('oko-wallet-demo');
    localStorage.removeItem('oko-pp-goal-months-pro');
    localStorage.removeItem('pp-count');
    localStorage.removeItem('pp-claimed');
    localStorage.setItem('oko-money-reset-v1', '1');
  }catch(e){}
})();

const WALLET = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-wallet'))||null; }catch(e){ return null; } })() || {
  acc: 'OKO-' + String(Math.floor(1e7 + Math.random()*9e7)),
  balance: 0,
  hold: 0,
  ledger: []
};
function walletSave(){ try{ localStorage.setItem('oko-wallet', JSON.stringify(WALLET)); }catch(e){} }
function fmtMoney(n){ return (Math.round(n*100)/100).toLocaleString('ru-RU').replace(/,/g,' ') + ' ₽'; }
function walletAdd(sum, why){
  WALLET.balance += sum;
  WALLET.ledger.unshift({t:'+', sum, why, at: Date.now()});
  walletSave(); walletNotifyRender();
}
function walletCharge(sum, why){
  if(WALLET.balance < sum){
    toast('Недостаточно средств — пополни кошелёк');
    if(typeof showTab==='function' && document.getElementById('screen-wallet')) showTab('wallet');
    return false;
  }
  WALLET.balance -= sum;
  WALLET.ledger.unshift({t:'-', sum, why, at: Date.now()});
  walletSave(); walletNotifyRender();
  return true;
}
function walletNotifyRender(){ if(typeof renderWallet==='function' && document.getElementById('screen-wallet') && document.getElementById('screen-wallet').classList.contains('active')) renderWallet(); }

/* Доход OKO (комиссии/маржа) — видит только владелец в админке */
const OKO_REVENUE = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-revenue'))||[]; }catch(e){ return []; } })();
function okoEarn(sum, src){
  OKO_REVENUE.unshift({sum: Math.round(sum*100)/100, src, at: Date.now()});
  if(OKO_REVENUE.length > 400) OKO_REVENUE.length = 400;
  try{ localStorage.setItem('oko-revenue', JSON.stringify(OKO_REVENUE)); }catch(e){}
}
function okoRevenueTotal(){ return OKO_REVENUE.reduce((s,r)=>s+r.sum,0); }

/* ---------- i18n ЯДРО ---------- */
let LANG = (()=>{ try{ return localStorage.getItem('oko-lang')||'ru'; }catch(e){ return 'ru'; } })();
const I18N = {};
function regT(dict){ for(const k in dict) I18N[k] = dict[k]; }
function t(k){ const e = I18N[k]; return e ? (e[LANG] || e.ru || k) : k; }
const LANG_HOOKS = [];
function onLangChange(fn){ LANG_HOOKS.push(fn); }
function setLang(l){
  LANG = l; try{ localStorage.setItem('oko-lang', l); }catch(e){}
  document.documentElement.lang = l;
  LANG_HOOKS.forEach(fn=>{ try{ fn(l); }catch(e){} });
  toast(l==='ru' ? 'Язык: русский' : 'Language: English');
}

/* ---------- ВЕРИФИКАЦИЯ (синяя галочка) ---------- */
(function addVerifiedSymbol(){
  const defs = document.querySelector('svg defs');
  if(!defs) return;
  const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
  s.setAttribute('id','i-verified'); s.setAttribute('viewBox','0 0 100 100');
  s.innerHTML = '<path d="M50 6l11 9 14-2 5 13 13 5-2 14 9 11-9 11 2 14-13 5-5 13-14-2-11 9-11-9-14 2-5-13-13-5 2-14-9-11 9-11-2-14 13-5 5-13 14 2z" fill="#9AFF00" stroke="none"/><path d="M32 52l12 12 24-28" fill="none" stroke="#0a0a0a" stroke-width="9" stroke-linecap="round" stroke-linejoin="round"/>';
  defs.appendChild(s);
})();
const VERIFIED = new Set(['Даниэль','OKO','OKO Team','Поддержка OKO']);
function vBadge(name, size){
  return VERIFIED.has(name) ? `<svg class="i fill" style="width:${size||'0.95em'};height:${size||'0.95em'};vertical-align:-0.12em;margin-left:3px"><use href="#i-verified"/></svg>` : '';
}

/* ---------- АДМИН-АУТЕНТИФИКАЦИЯ (владелец) ---------- */
const ADMIN_EMAIL = 'okoteam.top@gmail.com';
const ADMIN_HASH = 'ccf4cf77b15f3d07726458af3713a4f2ea1ec60ff55d2589e9706e63a6ab3077'; // sha-256, пароль не хранится
async function sha256hex(str){
  const buf = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(str));
  return [...new Uint8Array(buf)].map(b=>b.toString(16).padStart(2,'0')).join('');
}
function isOwner(){ return PROFILE.role === 'owner'; }
async function adminLogin(email, pass){
  if((email||'').trim().toLowerCase() !== ADMIN_EMAIL) return false;
  if(await sha256hex(pass||'') !== ADMIN_HASH) return false;
  PROFILE.role = 'owner'; PROFILE.tier = 'PRO';
  PROFILE.ceo = true; VERIFIED.add(PROFILE.name);
  try{ localStorage.setItem('oko-owner','1'); }catch(e){}
  return true;
}
(function restoreOwner(){ try{ if(localStorage.getItem('oko-owner')==='1'){ PROFILE.role='owner'; PROFILE.ceo=true; VERIFIED.add(PROFILE.name); } }catch(e){} })();

/* ---------- ПЕЧАТЬ ОРГАНИЗАЦИИ (официальный вид, синяя) ---------- */
const SEAL_REQ = {
  fio: 'ИП ИЛЬЯСОВ ДАНИЭЛЬ АЛЬБЕРТОВИЧ',
  inn: 'ИНН 682016634349',
  brand: 'ОКО PROJECT',
  geo: 'МОСКВА · ДУБАЙ · РФ / ОАЭ'
};
function sealSvg(size, color){
  size = size || 160;
  // Настоящая бренд-печать (синяя ink-stamp) из brand/seal/. mix-blend работает и в тёмной, и в светлой теме.
  return `<img src="${SEAL_B64_URL}" width="${size}" height="${size}" alt="печать OKO" style="display:block;user-select:none;-webkit-user-drag:none;pointer-events:none">`;
}
function signatureImg(w){
  return `<img src="${SIGNATURE_B64_URL}" style="width:${w||150}px;display:block" alt="подпись">`;
}

/* ---------- ПОПАПЫ (модальные окна) ---------- */
function showPopup(o){
  closePopup();
  const el = document.createElement('div');
  el.id = 'okoPopup';
  el.innerHTML = `<div class="pop-card fade-in">
    ${o.ico ? `<div class="pop-ico">${I(o.ico)}</div>` : ''}
    <h3>${o.title||''}</h3>
    <div class="pop-body">${o.body||''}</div>
    <div class="pop-actions">${(o.actions||[{label:'Понятно'}]).map((a,i)=>
      `<button class="btn ${a.ghost?'ghost':''}" data-pa="${i}">${a.label}</button>`).join('')}</div>
  </div>`;
  el.addEventListener('click', e=>{ if(e.target===el) closePopup(); });
  document.body.appendChild(el);
  (o.actions||[{label:'Понятно'}]).forEach((a,i)=>{
    el.querySelector(`[data-pa="${i}"]`).onclick = ()=>{ closePopup(); if(a.onclick) a.onclick(); };
  });
}
function closePopup(){ const p = document.getElementById('okoPopup'); if(p) p.remove(); }

/* ---------- ХАБ: регистрация сервис-тайлов модулями ---------- */
function addSvcTile(o){
  const grid = document.querySelector('#maGrid .svc-grid');
  if(!grid || document.getElementById('svc-'+o.id)) return;
  const b = document.createElement('button');
  b.className = 'svc'; b.id = 'svc-'+o.id;
  b.innerHTML = `<span class="svc-ic" style="background:${o.bg||'var(--lime-dim)'};color:${o.fg||'var(--accent)'}"><svg class="i"><use href="#i-${o.ico}"/></svg></span><span class="svc-label">${o.label}</span>`;
  b.onclick = o.onclick;
  if(o.first) grid.prepend(b); else grid.appendChild(b);
}

/* ---------- TITLES расширение (заголовок шапки для новых экранов) ---------- */
function regTitle(k, title){ TITLES[k] = title; }

/* ===== module: registration ===== */
/* ===== REGISTRATION: полная регистрация + системные попапы (префикс reg-) ===== */

/* ---------- состояние ---------- */
const REG = {
  step: 1,
  method: 'email',          // email | phone
  contact: '',
  code: '',
  codeTimer: null,
  codeLeft: 0,
  nickState: null,          // null | 'wait' | 'ok' | 'bad'
  nickTimer: null,
  interests: new Set(),
  adult: true,
  avaIdx: 0,                // выбранный акцент аватара
};
/* палитра аватара: строго бренд (чёрный + лайм), но с разнообразием тона.
   fg — цвет инициала, подобран под читаемость на каждом фоне. */
const REG_AVA = [
  {bg:'linear-gradient(135deg,#B9FF4D,#7ACC00)', fg:'#001400'},
  {bg:'linear-gradient(135deg,#EAFF9C,#9AFF00)', fg:'#1a2a00'},
  {bg:'linear-gradient(135deg,#9AFF00,#4d9c00)', fg:'#04140a'},
  {bg:'linear-gradient(135deg,#1c1c1c,#000)',     fg:'#9AFF00'},
  {bg:'linear-gradient(135deg,#38471f,#12180b)',  fg:'#B9FF4D'},
  {bg:'linear-gradient(135deg,#f2f6ec,#cfe3b3)',  fg:'#3d7a00'},
];
const REG_OWNER_EMAIL = (typeof ADMIN_EMAIL !== 'undefined') ? ADMIN_EMAIL : 'okoteam.top@gmail.com';
const REG_TAKEN = ['oko','okoteam','oko_official','daniel','ktodaniel','admin','support','team','ceo','help'];
const REG_INTERESTS = [
  {id:'ai',        label:'Нейросети', ico:'bolt'},
  {id:'content',   label:'Контент',   ico:'camera'},
  {id:'business',  label:'Бизнес',    ico:'briefcase'},
  {id:'marketing', label:'Маркетинг', ico:'megaphone'},
  {id:'games',     label:'Игры',      ico:'play'},
  {id:'crypto',    label:'Крипта',    ico:'money'},
];

/* ---------- localStorage: одноразовые попапы + данные регистрации ---------- */
function regPopupsSeen(){ try{ return JSON.parse(localStorage.getItem('oko-reg-popups')) || {}; }catch(e){ return {}; } }
function regPopupMark(k){ const s = regPopupsSeen(); s[k] = 1; try{ localStorage.setItem('oko-reg-popups', JSON.stringify(s)); }catch(e){} }
function regSave(data){ try{ localStorage.setItem('oko-registration', JSON.stringify(data)); }catch(e){} try{ window.okoTrack && okoTrack('sign_up', {method:(data&&data.method)||'unknown', at:Date.now()}); }catch(e){} }

/* ---------- открытие / закрытие / навигация ---------- */
function regOpen(){
  if(!document.getElementById('regView')) return;
  REG.step = 1; REG.code = ''; REG.nickState = null; REG.interests.clear(); REG.adult = true; REG.avaIdx = 0;
  ['regContact','regPass','regName','regNick','regBirth'].forEach(id=>{ const el = document.getElementById(id); if(el) el.value = ''; });
  document.getElementById('regTerms').checked = false;
  document.getElementById('regAgeWarn').classList.remove('show');
  document.getElementById('regNickSt').innerHTML = '';
  document.getElementById('regNickMsg').textContent = '';
  regMethodSet(REG.method);
  regPassMeter();
  regRenderInts();
  regAvaRender();
  regUpdateFinish();
  regShowStep(1);
  document.getElementById('regView').classList.add('open');
  setTimeout(()=>{ const i = document.getElementById('regContact'); if(i) i.focus(); }, 340);
}
function regClose(){
  document.getElementById('regView').classList.remove('open');
  if(REG.codeTimer){ clearInterval(REG.codeTimer); REG.codeTimer = null; }
}
function regBack(){
  if(REG.step === 1){ regClose(); return; }
  regShowStep(REG.step - 1);
  if(REG.step === 2) regCodeFocus(0);
}
function regShowStep(n){
  const back = (typeof REG.step === 'number' && n < REG.step); // направление перехода
  REG.step = n;
  [1,2,3].forEach(i=>{
    const el = document.getElementById('regStep'+i);
    el.classList.remove('active','reg-back');
    if(i === n){ void el.offsetWidth; if(back) el.classList.add('reg-back'); el.classList.add('active'); } // рестарт + направление
  });
  document.getElementById('regBar').style.width = (n/3*100).toFixed(1) + '%';
  document.getElementById('regStepNum').textContent = 'Шаг ' + n + ' из 3';
  document.getElementById('regView').querySelector('.reg-body').scrollTop = 0;
}
function regErrShow(id, msg){ const el = document.getElementById(id); el.textContent = msg; el.classList.remove('show'); void el.offsetWidth; el.classList.add('show'); }
function regErrHide(id){ const el = document.getElementById(id); el.classList.remove('show'); el.textContent = ''; }

/* ---------- шаг 1: контакт + пароль ---------- */
function regMethodSet(m){
  REG.method = m;
  document.getElementById('regSegEmail').classList.toggle('on', m === 'email');
  document.getElementById('regSegPhone').classList.toggle('on', m === 'phone');
  const inp = document.getElementById('regContact');
  const lab = document.getElementById('regContactLab');
  if(m === 'email'){
    lab.textContent = 'Электронная почта';
    inp.type = 'email'; inp.inputMode = 'email'; inp.autocomplete = 'email';
    inp.placeholder = 'you@example.com';
  } else {
    lab.textContent = 'Номер телефона';
    inp.type = 'tel'; inp.inputMode = 'tel'; inp.autocomplete = 'tel';
    inp.placeholder = '+7 900 000-00-00';
  }
  inp.value = ''; regErrHide('regErr1');
}
function regContactInput(){
  regErrHide('regErr1');
  if(REG.method !== 'phone') return;
  const inp = document.getElementById('regContact');
  let d = inp.value.replace(/\D/g, '');
  if(d.startsWith('8')) d = '7' + d.slice(1);
  if(d && !d.startsWith('7')) d = '7' + d;
  d = d.slice(0, 11);
  let out = '';
  if(d.length){ out = '+7'; }
  if(d.length > 1) out += ' ' + d.slice(1, 4);
  if(d.length > 4) out += ' ' + d.slice(4, 7);
  if(d.length > 7) out += '-' + d.slice(7, 9);
  if(d.length > 9) out += '-' + d.slice(9, 11);
  inp.value = out;
}
function regTogglePass(){
  const inp = document.getElementById('regPass');
  const btn = document.querySelector('#regView .reg-eye');
  const show = inp.type === 'password';
  inp.type = show ? 'text' : 'password';
  btn.classList.toggle('on', show);
}
function regPassScore(p){
  let s = 0;
  if(p.length >= 6) s++;
  if(p.length >= 10) s++;
  if(/[A-ZА-Я]/.test(p) && /[a-zа-я]/.test(p)) s++;
  if(/\d/.test(p)) s++;
  if(/[^\w\sа-яА-Я]/.test(p)) s++;
  return Math.min(s, 4);
}
function regPassMeter(){
  regErrHide('regErr1');
  const p = document.getElementById('regPass').value;
  const bar = document.getElementById('regStrBar');
  const lab = document.getElementById('regStrLab');
  const sc = p ? regPassScore(p) : 0;
  bar.style.width = (sc/4*100) + '%';
  bar.className = sc >= 3 ? 'hi' : (sc === 2 ? 'mid' : '');
  lab.textContent = !p ? 'Надёжность пароля' : (sc <= 1 ? 'Слабый пароль' : sc === 2 ? 'Нормальный пароль' : sc === 3 ? 'Хороший пароль' : 'Отличный пароль');
}
async function regNext1(){
  const contact = document.getElementById('regContact').value.trim();
  const pass = document.getElementById('regPass').value;
  if(REG.method === 'email'){
    if(!/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(contact)) return regErrShow('regErr1', 'Похоже, в почте опечатка — проверь адрес');
  } else {
    if(contact.replace(/\D/g, '').length < 11) return regErrShow('regErr1', 'Введи номер полностью — 11 цифр с +7');
  }
  if(pass.length < 6) return regErrShow('regErr1', 'Пароль слишком короткий — минимум 6 символов');
  REG.contact = contact;

  /* вход владельца: okoteam.top@gmail.com -> adminLogin из ядра */
  if(REG.method === 'email' && contact.toLowerCase() === REG_OWNER_EMAIL){
    const btn = document.getElementById('regBtn1');
    btn.classList.add('loading'); btn.textContent = 'Проверяю доступ…';
    let ok = false;
    try{ if(typeof adminLogin === 'function') ok = await adminLogin(contact, pass); }catch(e){}
    btn.classList.remove('loading'); btn.innerHTML = 'Продолжить ' + I('chev');
    if(!ok) return regErrShow('regErr1', 'Неверный пароль владельца');
    regSave({owner:true, contact, method:'email', at: Date.now()});
    regClose();
    _regPrevDoLogin('owner');
    if(typeof renderMyProfile === 'function') try{ renderMyProfile(); }catch(e){}
    setTimeout(()=>{
      showPopup({ico:'crown', title:'Доступ владельца',
        body:'Генеральный директор OKO. Открыт полный доступ: админка, доход платформы, HQ и управление всеми разделами.',
        actions:[{label:'В приложение'}]});
    }, 650);
    regSchedulePopups();
    return;
  }
  regSendCode();
  regShowStep(2);
}

/* ---------- шаг 2: код подтверждения (мок) ---------- */
function regSendCode(){
  REG.code = String(Math.floor(1000 + Math.random() * 9000));
  document.getElementById('regCodeSub').innerHTML =
    'Мы отправили 4-значный код на <b style="color:var(--text)">' + esc(REG.contact) + '</b>';
  document.querySelectorAll('#regCodeRow input').forEach(i=>{ i.value = ''; i.classList.remove('fill'); });
  document.getElementById('regCodeRow').classList.remove('err');
  regErrHide('regErr2');
  regCodeFocus(0);
  toast('Код подтверждения OKO: ' + REG.code + ' (демо)');
  /* таймер повторной отправки 30 с */
  if(REG.codeTimer) clearInterval(REG.codeTimer);
  REG.codeLeft = 30;
  const btn = document.getElementById('regResendBtn');
  btn.disabled = true;
  btn.textContent = 'Отправить код повторно (30 с)';
  REG.codeTimer = setInterval(()=>{
    REG.codeLeft--;
    if(REG.codeLeft <= 0){
      clearInterval(REG.codeTimer); REG.codeTimer = null;
      btn.disabled = false; btn.textContent = 'Отправить код повторно';
    } else {
      btn.textContent = 'Отправить код повторно (' + REG.codeLeft + ' с)';
    }
  }, 1000);
}
function regResend(){ regSendCode(); }
function regCodeFocus(i){ const inp = document.querySelector('#regCodeRow input[data-i="' + i + '"]'); if(inp){ inp.focus(); inp.select(); } }
function regCodeVal(){ return [...document.querySelectorAll('#regCodeRow input')].map(i=>i.value).join(''); }
function regVerifyCode(){
  const v = regCodeVal();
  if(v.length < 4) return regErrShow('regErr2', 'Введи все 4 цифры кода');
  if(v !== REG.code){
    const row = document.getElementById('regCodeRow');
    row.classList.remove('err'); void row.offsetWidth; row.classList.add('err');
    regErrShow('regErr2', 'Код не подходит — проверь и попробуй ещё раз');
    return;
  }
  toast('Код подтверждён');
  regShowStep(3);
  setTimeout(()=>{ const i = document.getElementById('regName'); if(i) i.focus(); }, 340);
}

/* ---------- шаг 3: профиль, ник, возраст, интересы, условия ---------- */
function regNickInput(){
  const raw = document.getElementById('regNick').value;
  const clean = raw.toLowerCase().replace(/[^a-z0-9_]/g, '').slice(0, 24);
  if(raw !== clean) document.getElementById('regNick').value = clean;
  const st = document.getElementById('regNickSt');
  const msg = document.getElementById('regNickMsg');
  clearTimeout(REG.nickTimer);
  if(!clean){ REG.nickState = null; st.innerHTML = ''; msg.textContent = ''; msg.className = 'reg-nick-msg'; regUpdateFinish(); return; }
  REG.nickState = 'wait';
  st.innerHTML = '<span class="wait">' + I('clock') + '</span>';
  msg.textContent = 'Проверяю никнейм…'; msg.className = 'reg-nick-msg wait';
  regUpdateFinish();
  REG.nickTimer = setTimeout(()=>{
    const taken = REG_TAKEN.includes(clean) || clean.length < 3;
    REG.nickState = taken ? 'bad' : 'ok';
    if(taken){
      st.innerHTML = '<span class="bad">' + I('plus') + '</span>'; // плюс под 45° = крест
      st.querySelector('svg').style.transform = 'rotate(45deg)';
      msg.textContent = clean.length < 3 ? 'Слишком короткий — минимум 3 символа' : '@' + clean + ' уже занят, попробуй другой';
      msg.className = 'reg-nick-msg bad';
    } else {
      st.innerHTML = '<span class="ok">' + I('check2') + '</span>';
      msg.textContent = '@' + clean + ' свободен';
      msg.className = 'reg-nick-msg ok';
    }
    regUpdateFinish();
  }, 550);
}
function regBirthCheck(){
  const v = document.getElementById('regBirth').value;
  REG.adult = true;
  if(v){
    const b = new Date(v), now = new Date();
    let age = now.getFullYear() - b.getFullYear();
    const m = now.getMonth() - b.getMonth();
    if(m < 0 || (m === 0 && now.getDate() < b.getDate())) age--;
    REG.adult = age >= 18;
  }
  document.getElementById('regAgeWarn').classList.toggle('show', !REG.adult && !!v);
}
function regRenderInts(){
  document.getElementById('regInts').innerHTML = REG_INTERESTS.map(x=>
    `<button type="button" class="reg-int ${REG.interests.has(x.id) ? 'on' : ''}" onclick="regToggleInt('${x.id}')">${I(x.ico)}<span>${x.label}</span></button>`
  ).join('');
}
function regToggleInt(id){
  if(REG.interests.has(id)) REG.interests.delete(id); else REG.interests.add(id);
  regRenderInts(); regUpdateFinish();
}
/* ---------- аватар: превью инициала + выбор акцента ---------- */
function regAvaRender(){
  const sw = document.getElementById('regAvaSw');
  if(sw) sw.innerHTML = REG_AVA.map((a,i)=>
    `<button type="button" class="reg-ava-sw ${i===REG.avaIdx?'on':''}" style="background:${a.bg}" aria-label="Акцент аватара ${i+1}" onclick="regAvaPick(${i})"></button>`
  ).join('');
  regAvaSync();
}
function regAvaPick(i){ REG.avaIdx = i; regAvaRender(); }
function regAvaSync(){
  const p = document.getElementById('regAvaPrev');
  if(!p) return;
  const a = REG_AVA[REG.avaIdx] || REG_AVA[0];
  const nm = (document.getElementById('regName')||{}).value || '';
  p.textContent = (nm.trim()[0] || 'O').toUpperCase();
  p.style.background = a.bg; p.style.color = a.fg;
  p.classList.add('set');
}
/* применить выбранный аватар к профилю ядра (#profAva правит только textContent —
   инлайновые стили сохраняются между renderMyProfile) */
function regApplyAva(){
  const a = REG_AVA[REG.avaIdx] || REG_AVA[0];
  const el = document.getElementById('profAva');
  if(el){ el.style.background = a.bg; el.style.color = a.fg; }
  try{ if(typeof PROFILE !== 'undefined'){ PROFILE.avaBg = a.bg; PROFILE.avaFg = a.fg; } }catch(e){}
}
function regUpdateFinish(){
  regErrHide('regErr3');
  regAvaSync();
  const terms = document.getElementById('regTerms');
  const name = document.getElementById('regName');
  const btn = document.getElementById('regFinishBtn');
  if(!terms || !name || !btn) return;
  const ok = terms.checked
    && name.value.trim().length > 0
    && REG.nickState === 'ok'
    && REG.interests.size > 0;
  btn.disabled = !ok;
}
function regLegal(kind){
  if(typeof openLegalDoc === 'function'){ openLegalDoc(kind); return; }
  toast(kind === 'terms' ? 'Условия сервиса откроются в разделе «Документы»' : 'Политика конфиденциальности откроется в разделе «Документы»');
}
function regFinish(){
  const name = document.getElementById('regName').value.trim();
  const nick = document.getElementById('regNick').value.trim();
  if(!name) return regErrShow('regErr3', 'Укажи имя — так тебя увидят в OKO');
  if(REG.nickState !== 'ok') return regErrShow('regErr3', 'Нужен свободный никнейм');
  if(!REG.interests.size) return regErrShow('regErr3', 'Выбери хотя бы один интерес — без него лента будет пустой');
  if(!document.getElementById('regTerms').checked) return regErrShow('regErr3', 'Прими условия сервиса, чтобы продолжить');

  /* профиль ядра + персист */
  PROFILE.name = name;
  PROFILE.nick = nick;
  regSave({
    contact: REG.contact, method: REG.method, name, nick,
    birth: document.getElementById('regBirth').value || null,
    adult: REG.adult,
    interests: [...REG.interests],
    avaIdx: REG.avaIdx,
    at: Date.now(),
  });
  if(typeof renderMyProfile === 'function') try{ renderMyProfile(); }catch(e){}
  regApplyAva();
  /* реально засеять алгоритм ленты: интересы дают стартовый вес сигналов
     (feed-algo и так читает oko-registration.interests, но это делает
     персонализацию видимой сразу с первой сессии) */
  try{ if(typeof faSignal === 'function') REG.interests.forEach(t=>faSignal(t, 8)); }catch(e){}

  regClose();
  _regPrevDoLogin(REG.method); /* оригинальный вход: oko-auth, скрыть auth, initLive, онбординг */

  /* welcome-попап — один раз после первой регистрации.
     Начисление 2 500 ₽ убрано 09.08: деньги на счёте нового человека были
     выдуманными (правило «ноль демо-данных»). Кошелёк стартует с нуля. */
  if(!regPopupsSeen().welcome){
    regPopupMark('welcome');
    setTimeout(()=>{
      showPopup({ico:'logo', title: regWelcomeTitle(name),
        body: regWelcomeBody(name),
        actions:[
          {label:'Открыть кошелёк', onclick:()=>{ if(document.getElementById('screen-wallet') && typeof showTab === 'function') showTab('wallet'); }},
          {label:'Позже', ghost:true},
        ]});
    }, 700);
  }
  regSchedulePopups();
}

/* ---------- премиум-тела системных попапов (rich HTML, свои reg-pop-* классы) ---------- */
function regFeatRow(ico, txt){
  return `<div class="reg-pop-feat">${I(ico)}<span>${txt}</span></div>`;
}

/* --- реферальная ссылка: захват из URL / хранение / чтение --- */
function regRefCapture(){
  try{
    const q = new URLSearchParams(location.search);
    const r = (q.get('ref') || q.get('start') || '').trim().slice(0, 40);
    if(r && /^[a-z0-9_\-]{2,40}$/i.test(r)){
      localStorage.setItem('oko-ref', r.toLowerCase());
    }
  }catch(e){}
}
function regRefGet(){
  try{ return (localStorage.getItem('oko-ref') || '').trim(); }catch(e){ return ''; }
}

/* --- lettering: буквы имени появляются одна за другой (--i задаёт задержку) --- */
function regNameLet(name){
  const src = String(name || '').trim().slice(0, 24);
  if(!src) return '';
  let out = '';
  for(let i = 0; i < src.length; i++){
    const ch = src[i];
    if(ch === ' '){ out += '<span class="reg-let sp"> </span>'; continue; }
    out += `<span class="reg-let" style="--i:${i}">${esc(ch)}</span>`;
  }
  return `<span class="reg-hello-name">${out}</span>`;
}

/* --- заголовок welcome с персональным приветствием и lettering --- */
function regWelcomeTitle(name){
  const nm = String(name || '').trim();
  if(!nm) return 'Добро пожаловать в OKO';
  return `<span class="reg-hello">Добро пожаловать,<br>${regNameLet(nm)}</span>`;
}

/* --- быстрые действия: 4 карточки первого клика (заголовки без переноса) --- */
function regQuickActions(){
  const items = [
    {act:'video',   ico:'camera',   t:'Первое видео',   s:'Собери ролик за 3 минуты'},
    {act:'roulette',ico:'gm-gift',  t:'Рулетка',        s:'Крути ежедневный бонус'},
    {act:'invite',  ico:'user',     t:'Пригласи друга', s:'Плюс 2 бонуса вам обоим'},
    {act:'lesson',  ico:'star',     t:'Первый урок',    s:'Академия с сертификатом'},
  ];
  return `<div class="reg-pop-qa">
    ${items.map((x, i) => `
      <button type="button" class="reg-qa" style="--i:${i}" onclick="regGo('${x.act}')">
        <span class="reg-qa-ico">${I(x.ico)}</span>
        <span class="reg-qa-txt">
          <b class="reg-qa-t">${x.t}</b>
          <span class="reg-qa-s">${x.s}</span>
        </span>
        <span class="reg-qa-arr">${I('chev')}</span>
      </button>`).join('')}
  </div>`;
}

/* --- реф-нотис: показать, что пригласивший получит бонус после оплаты тарифа --- */
function regRefNotice(){
  const ref = regRefGet();
  if(!ref) return '';
  return `<div class="reg-pop-ref">
    <span class="reg-pop-ref-ico">${I('heart')}</span>
    <div class="reg-pop-ref-txt">
      Привет! Твой пригласивший <b>@${esc(ref)}</b> получит бонус, когда ты оформишь тариф OKO.
    </div>
  </div>`;
}

/* --- переход из quick-action: закрыть попап и открыть нужный раздел --- */
function regGo(kind){
  try{ if(typeof closePopup === 'function') closePopup(); }catch(e){}
  setTimeout(()=>{
    try{
      if(kind === 'video'){
        if(typeof showTab === 'function') showTab('mini');
        if(typeof toast === 'function') toast('Открыл мини-аппы — тапни «Реклама» или «Академия», чтобы собрать первое видео');
      } else if(kind === 'roulette'){
        if(typeof showTab === 'function') showTab('games');
      } else if(kind === 'invite'){
        if(typeof showTab === 'function'){
          if(document.getElementById('screen-partner')) showTab('partner');
          else showTab('profile');
        }
      } else if(kind === 'lesson'){
        if(typeof showTab === 'function'){
          if(document.getElementById('screen-academy')) showTab('academy');
          else showTab('mini');
        }
      }
    }catch(e){}
  }, 120);
}
try{ window.regGo = regGo; window.regRefGet = regRefGet; }catch(e){}

function regWelcomeBody(name){
  return `<div class="reg-pop">
    ${regRefNotice()}
    <p class="reg-pop-lead">Аккаунт создан. Ты в OKO — экосистеме, где контент, бизнес и заработок в одном месте.</p>
    <div class="reg-pop-bonus">
      <span class="reg-pop-bonus-ico">${I('wallet')}</span>
      <div><b>Кошелёк открыт</b><small>лицевой счёт готов к пополнению</small></div>
      <span class="reg-pop-bonus-tag">Старт</span>
    </div>
    <div class="reg-pop-qa-lab">С чего начать</div>
    ${regQuickActions()}
    <div class="reg-pop-feats">
      ${regFeatRow('rocket','Продвижение постов и каналов')}
      ${regFeatRow('briefcase','Биржа заказов и услуг')}
      ${regFeatRow('play','Игры и ежедневные бонусы')}
    </div>
  </div>`;
}
function regProBody(){
  return `<div class="reg-pop">
    <p class="reg-pop-lead">Максимум возможностей OKO — приоритет, аналитика и сниженная комиссия.</p>
    <div class="reg-pop-feats">
      ${regFeatRow('rocket','Приоритет в ленте и поиске')}
      ${regFeatRow('poll','Расширенная аналитика профиля')}
      ${regFeatRow('megaphone','Кабинет продвижения')}
      ${regFeatRow('money','Сниженная комиссия биржи')}
    </div>
    <div class="reg-pop-price">
      <div><b>от 490 ₽</b><small>в месяц</small></div>
      <span class="reg-pop-save">Выгода 20% при оплате за год</span>
    </div>
  </div>`;
}
function regNotifBody(){
  return `<div class="reg-pop">
    <p class="reg-pop-lead">Узнавай о важном сразу, а не когда откроешь приложение.</p>
    <div class="reg-pop-feats">
      ${regFeatRow('comment','Ответы и сообщения в чатах')}
      ${regFeatRow('briefcase','Отклики и сделки на бирже')}
      ${regFeatRow('money','Начисления партнёрской программы')}
    </div>
  </div>`;
}
try{ window.regWelcomeBody = regWelcomeBody; window.regProBody = regProBody; window.regNotifBody = regNotifBody; }catch(e){}

/* ---------- системные попапы (каждый один раз, oko-reg-popups) ---------- */
let regPopTimers = [];
/* открыта переписка или полноэкранная вьюха (профиль/расширенные настройки)?
   — тогда промо-попап не перекрываем, а откладываем (как для sheet/попапа) */
function regViewBusy(){
  const cb = document.getElementById('convBody');
  if(cb && cb.style.display !== 'none' && cb.offsetParent !== null) return true;
  const st2 = document.getElementById('st2View'); if(st2 && st2.classList.contains('open')) return true;
  const ps  = document.getElementById('psView');  if(ps  && ps.classList.contains('open'))  return true;
  return false;
}
function regSchedulePopups(){
  regPopTimers.forEach(t=>clearTimeout(t)); regPopTimers = [];
  const seen = regPopupsSeen();
  if(!seen.pro)   regPopTimers.push(setTimeout(regPopupPro,   45000));
  if(!seen.notif) regPopTimers.push(setTimeout(regPopupNotif, 90000));
}
function regPopupPro(){
  if(regPopupsSeen().pro) return;
  if(document.getElementById('regView').classList.contains('open')) return;
  /* не перебивать открытый sheet/попап/переписку/полноэкранную вьюху — попробовать позже */
  if(document.querySelector('.sheet.open') || document.getElementById('okoPopup') || regViewBusy()){
    regPopTimers.push(setTimeout(regPopupPro, 20000)); return;
  }
  regPopupMark('pro');
  showPopup({ico:'crown', title:'Открой OKO PRO',
    body: regProBody(),
    actions:[
      {label:'Оформить PRO', onclick:()=>{ if(typeof openPay === 'function') openPay('PRO'); else toast('Тарифы скоро откроются'); }},
      {label:'Позже', ghost:true},
    ]});
}
function regPopupNotif(){
  if(regPopupsSeen().notif) return;
  if(document.getElementById('regView').classList.contains('open')) return;
  if(document.querySelector('.sheet.open') || document.getElementById('okoPopup') || regViewBusy()){
    regPopTimers.push(setTimeout(regPopupNotif, 20000)); return;
  }
  regPopupMark('notif');
  showPopup({ico:'bell', title:'Включи уведомления',
    body: regNotifBody(),
    actions:[
      {label:'Включить', onclick:()=>{
        try{ if(window.Notification && Notification.requestPermission) Notification.requestPermission(); }catch(e){}
        toast('Уведомления включены');
      }},
      {label:'Позже', ghost:true},
    ]});
}

/* ---------- патч doLogin: метод phone -> НОВЫЙ 4-шаговый флоу rg2 ---------- */
const _regPrevDoLogin = doLogin;
doLogin = function(method){
  if(method === 'phone'){ rg2Open(); return; }
  _regPrevDoLogin(method);            /* telegram / google / apple — быстрый вход как раньше */
  regSchedulePopups();
};

/* ============================================================
   RG2 — новый 4-шаговый флоу регистрации внутри #authScreen
   (по ТЗ Даниэля 29.07). Полностью самодостаточный, префикс rg2-.
   Не трогает base.html: динамически строит DOM внутри #authScreen,
   прячет .auth-inner, восстанавливает при back-to-step-0.
   ============================================================ */
const RG2 = {
  step: 1,                 /* 1..4 */
  method: 'email',         /* email | phone — авто по вводу */
  contact: '',             /* email или +7... */
  password: '',
  remember: true,
  code: '',                /* сгенерированный 6-значный код (мок) */
  codeTimer: null,
  codeLeft: 0,
  resendUsed: 0,
  name: '',
  nick: '',
  bio: '',
  avaIdx: 2,               /* индекс REG_AVA */
  avaImg: '',              /* dataURL если загружена картинка */
  tier: 'FREE',            /* FREE | START | PRO | BUSINESS | MAX */
  period: 1,               /* 1|3|6|12 месяцев */
};
const RG2_ADMIN_EMAIL = (typeof ADMIN_EMAIL !== 'undefined') ? ADMIN_EMAIL : 'okoteam.top@gmail.com';
const RG2_TAKEN_NICKS = ['oko','okoteam','oko_official','daniel','ktodaniel','admin','support','team','ceo','help'];
const RG2_TIERS = [
  {id:'FREE',     name:'Free',     price:0,     line:'Старт бесплатно',           feats:['Лента и чаты','Кошелёк и переводы','Базовые инструменты']},
  {id:'START',    name:'Start',    price:990,   line:'Первые системные инструменты', feats:['Автопостинг в 2 сети','Файлы до 300 МБ','Скидка 5% на рекламу']},
  {id:'PRO',      name:'Pro',      price:4900,  line:'Максимум возможностей',      feats:['Проверка видео: безлимит','Система роста','Все соцсети, файлы до 2 ГБ','Скидка 10% на рекламу'], reco:true},
  {id:'BUSINESS', name:'Business', price:19900, line:'Команда и конвейер',         feats:['Контент-завод под ключ','Рекламный кабинет PRO','Менеджер и API']},
  {id:'MAX',      name:'Max',      price:149900,line:'Максимальный доступ',         feats:['Всё из PRO + BUSINESS','Персональный продюсер','Приоритет во всём']},
];
const RG2_PERIODS = [
  {m:1,  lab:'1 мес',  disc:0},
  {m:3,  lab:'3 мес',  disc:10},
  {m:6,  lab:'6 мес',  disc:15},
  {m:12, lab:'Год',    disc:20},
];

function rg2Detect(v){
  const s = String(v||'').trim();
  if(/^\+?\d[\d\s\-()]{7,}$/.test(s) && s.replace(/\D/g,'').length >= 8) return 'phone';
  return 'email';
}
function rg2MaskContact(v){
  const s = String(v||'');
  if(rg2Detect(s) === 'email'){
    const [u, d] = s.split('@');
    if(!d) return s;
    const uu = u.length <= 2 ? u : u[0] + '***' + u.slice(-1);
    return uu + '@' + d;
  }
  const d = s.replace(/\D/g,'');
  if(d.length < 4) return s;
  return '+' + d.slice(0,1) + ' *** *** ' + d.slice(-2);
}
function rg2Rand6(){ return String(Math.floor(100000 + Math.random()*900000)); }
function rg2RandNick(){ return 'user_' + Math.random().toString(36).slice(2,8); }

/* ---------- вход в поток / выход ---------- */
function rg2Open(){
  const scr = document.getElementById('authScreen');
  if(!scr) return;
  scr.classList.remove('hidden');
  /* спрятать стандартный auth-inner */
  const inner = scr.querySelector('.auth-inner');
  if(inner) inner.style.display = 'none';
  /* создать shell если ещё нет */
  let shell = document.getElementById('rg2Shell');
  if(!shell){
    shell = document.createElement('div');
    shell.id = 'rg2Shell';
    shell.className = 'rg2-shell';
    shell.innerHTML = rg2ShellHTML();
    scr.appendChild(shell);
    rg2BindOnce();
  }
  shell.classList.add('open');
  /* сбросить состояние */
  RG2.step = 1; RG2.contact = ''; RG2.password = ''; RG2.code = '';
  RG2.name = ''; RG2.nick = rg2RandNick(); RG2.bio = '';
  RG2.avaIdx = 2; RG2.avaImg = ''; RG2.tier = 'FREE'; RG2.period = 1;
  RG2.resendUsed = 0;
  rg2GoTo(1);
}
function rg2Exit(){
  const shell = document.getElementById('rg2Shell');
  if(shell) shell.classList.remove('open');
  const scr = document.getElementById('authScreen');
  const inner = scr && scr.querySelector('.auth-inner');
  if(inner) inner.style.display = '';
  if(RG2.codeTimer){ clearInterval(RG2.codeTimer); RG2.codeTimer = null; }
}
function rg2Back(){
  if(RG2.step <= 1){ rg2Exit(); return; }
  rg2GoTo(RG2.step - 1);
}
function rg2GoTo(n){
  RG2.step = n;
  /* прогресс-таймлайн */
  for(let i=1; i<=4; i++){
    const dot = document.querySelector('#rg2Tl .rg2-tl-dot[data-i="'+i+'"]');
    if(dot){
      dot.classList.toggle('done', i < n);
      dot.classList.toggle('on',   i === n);
    }
    const bar = document.querySelector('#rg2Tl .rg2-tl-seg[data-i="'+i+'"]');
    if(bar) bar.classList.toggle('done', i < n);
  }
  /* видимость шагов */
  for(let i=1; i<=4; i++){
    const s = document.getElementById('rg2Step'+i);
    if(s){
      s.classList.remove('active','rg2-back');
      if(i === n){
        void s.offsetWidth;
        s.classList.add('active');
      }
    }
  }
  /* фокус на первое поле */
  setTimeout(()=>{
    const first = document.querySelector('#rg2Step'+n+' input:not([type=hidden]):not([type=file]):not([disabled])');
    if(first && n !== 4) try{ first.focus(); }catch(e){}
  }, 300);
  /* прячем back на 1 шаге — заменяем на «к вариантам входа» */
  const back = document.getElementById('rg2Back');
  if(back) back.dataset.first = (n === 1 ? '1' : '0');
}
function rg2Err(id, msg){
  const el = document.getElementById(id);
  if(!el) return;
  el.textContent = msg || '';
  el.classList.toggle('show', !!msg);
  if(msg){ void el.offsetWidth; el.classList.add('shake'); setTimeout(()=>el.classList.remove('shake'), 400); }
}

/* ---------- ШЕЛЛ HTML ---------- */
function rg2ShellHTML(){
  return `
  <header class="rg2-head">
    <button type="button" class="rg2-back" id="rg2Back" data-first="1" onclick="rg2Back()" aria-label="Назад">
      <svg class="i"><use href="#i-back"/></svg>
    </button>
    <div class="rg2-tl" id="rg2Tl">
      ${[1,2,3,4].map(i=>`
        <span class="rg2-tl-dot ${i===1?'on':''}" data-i="${i}"><i>${i}</i></span>
        ${i<4?`<span class="rg2-tl-seg" data-i="${i}"><i></i></span>`:''}
      `).join('')}
    </div>
    <span class="rg2-brand" aria-hidden="true"><svg class="i"><use href="#i-logo"/></svg></span>
  </header>
  <div class="rg2-body">
    ${rg2Step1HTML()}
    ${rg2Step2HTML()}
    ${rg2Step3HTML()}
    ${rg2Step4HTML()}
  </div>
  <input type="file" id="rg2AvaFile" accept="image/*" hidden>
  `;
}

/* ---------- ШАГ 1 ---------- */
function rg2Step1HTML(){
  return `
  <section class="rg2-step active" id="rg2Step1">
    <h1 class="rg2-h">Создай аккаунт</h1>
    <p class="rg2-sub">Введи почту или телефон и пароль — этого достаточно для старта.</p>
    <label class="rg2-lab" for="rg2Contact">Email или телефон</label>
    <div class="rg2-field">
      <span class="rg2-field-ic" id="rg2ContactIc"><svg class="i"><use href="#i-send"/></svg></span>
      <input id="rg2Contact" type="text" inputmode="email" autocomplete="email"
             placeholder="you@example.com или +7 900 000-00-00"
             oninput="rg2ContactInput()">
    </div>
    <label class="rg2-lab" for="rg2Pass">Придумай пароль</label>
    <div class="rg2-field">
      <span class="rg2-field-ic"><svg class="i"><use href="#i-lock"/></svg></span>
      <input id="rg2Pass" type="password" autocomplete="new-password"
             placeholder="Не короче 8 символов"
             oninput="rg2PassInput()">
      <button type="button" class="rg2-eye" data-target="rg2Pass" onclick="rg2TogglePass('rg2Pass')" aria-label="Показать пароль">
        <svg class="i"><use href="#i-eye"/></svg>
      </button>
    </div>
    <div class="rg2-strength"><i id="rg2StrBar"></i></div>
    <div class="rg2-strength-lab" id="rg2StrLab">Надёжность пароля</div>
    <label class="rg2-lab" for="rg2Pass2">Повтори пароль</label>
    <div class="rg2-field">
      <span class="rg2-field-ic"><svg class="i"><use href="#i-check2"/></svg></span>
      <input id="rg2Pass2" type="password" autocomplete="new-password" placeholder="Ещё раз тот же пароль" oninput="rg2Pass2Input()">
      <button type="button" class="rg2-eye" data-target="rg2Pass2" onclick="rg2TogglePass('rg2Pass2')" aria-label="Показать пароль">
        <svg class="i"><use href="#i-eye"/></svg>
      </button>
    </div>
    <label class="rg2-check">
      <input type="checkbox" id="rg2Remember" checked>
      <span class="rg2-check-box"><svg class="i"><use href="#i-check"/></svg></span>
      <span>Сохранить вход на этом устройстве</span>
    </label>
    <div class="rg2-err" id="rg2Err1"></div>
    <button class="rg2-btn" id="rg2Next1Btn" onclick="rg2Next1()">Продолжить <svg class="i"><use href="#i-chev"/></svg></button>
    <div class="rg2-alt">
      Уже есть аккаунт? <a onclick="rg2Login()">Войти</a>
    </div>
    <div class="rg2-secure">
      <svg class="i"><use href="#i-lock"/></svg>
      Данные шифруются, не передаются третьим лицам
    </div>
  </section>`;
}
function rg2ContactInput(){
  rg2Err('rg2Err1', '');
  const el = document.getElementById('rg2Contact');
  const v = el.value;
  const kind = rg2Detect(v);
  RG2.method = kind;
  /* иконка слева */
  const ic = document.getElementById('rg2ContactIc');
  if(ic) ic.innerHTML = kind === 'phone'
    ? '<svg class="i"><use href="#i-phone"/></svg>'
    : '<svg class="i"><use href="#i-send"/></svg>';
  /* автоформат телефона */
  if(kind === 'phone'){
    el.inputMode = 'tel'; el.autocomplete = 'tel';
    let d = v.replace(/\D/g,'');
    if(d.startsWith('8')) d = '7' + d.slice(1);
    if(d && !d.startsWith('7') && !d.startsWith('9')) { /* не форматируем */ }
    d = d.slice(0, 11);
    if(d.length && (d.startsWith('7') || d.length >= 10)){
      if(!d.startsWith('7')) d = '7' + d;
      let out = '+7';
      if(d.length > 1) out += ' ' + d.slice(1,4);
      if(d.length > 4) out += ' ' + d.slice(4,7);
      if(d.length > 7) out += '-' + d.slice(7,9);
      if(d.length > 9) out += '-' + d.slice(9,11);
      el.value = out;
    }
  } else {
    el.inputMode = 'email'; el.autocomplete = 'email';
  }
}
function rg2TogglePass(id){
  const inp = document.getElementById(id);
  const btn = document.querySelector('.rg2-eye[data-target="'+id+'"]');
  if(!inp) return;
  const show = inp.type === 'password';
  inp.type = show ? 'text' : 'password';
  if(btn) btn.classList.toggle('on', show);
}
function rg2PassScore(p){
  let s = 0;
  if(p.length >= 8) s++;
  if(p.length >= 12) s++;
  if(/[A-ZА-Я]/.test(p) && /[a-zа-я]/.test(p)) s++;
  if(/\d/.test(p)) s++;
  if(/[^\w\sа-яА-Я]/.test(p)) s++;
  return Math.min(s, 4);
}
function rg2PassInput(){
  rg2Err('rg2Err1', '');
  const p = document.getElementById('rg2Pass').value;
  const bar = document.getElementById('rg2StrBar');
  const lab = document.getElementById('rg2StrLab');
  const sc = p ? rg2PassScore(p) : 0;
  if(bar){
    bar.style.width = (sc/4*100) + '%';
    bar.className = sc >= 3 ? 'hi' : (sc === 2 ? 'mid' : (sc === 1 ? 'lo' : ''));
  }
  if(lab){
    lab.textContent = !p ? 'Надёжность пароля'
      : (sc <= 1 ? 'Слабый — добавь символов'
      : sc === 2 ? 'Нормальный'
      : sc === 3 ? 'Хороший' : 'Отличный');
  }
  /* при изменении первого пароля — сверка второго */
  rg2Pass2Input();
}
function rg2Pass2Input(){
  rg2Err('rg2Err1', '');
  const p1 = document.getElementById('rg2Pass').value;
  const p2 = document.getElementById('rg2Pass2').value;
  const f  = document.getElementById('rg2Pass2');
  if(!p2){ f.classList.remove('bad','ok'); return; }
  if(p2 === p1 && p2.length >= 8){ f.classList.add('ok'); f.classList.remove('bad'); }
  else { f.classList.add('bad'); f.classList.remove('ok'); }
}
async function rg2Next1(){
  const contact = document.getElementById('rg2Contact').value.trim();
  const p1 = document.getElementById('rg2Pass').value;
  const p2 = document.getElementById('rg2Pass2').value;
  const kind = rg2Detect(contact);
  if(kind === 'email'){
    if(!/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(contact)) return rg2Err('rg2Err1', 'Проверь адрес почты — похоже, опечатка');
  } else {
    if(contact.replace(/\D/g,'').length < 10) return rg2Err('rg2Err1', 'Введи номер полностью (не меньше 10 цифр)');
  }
  if(p1.length < 8) return rg2Err('rg2Err1', 'Пароль слишком короткий — минимум 8 символов');
  if(p1 !== p2)     return rg2Err('rg2Err1', 'Пароли не совпадают — сверь повтор');

  RG2.contact = contact; RG2.method = kind; RG2.password = p1;
  RG2.remember = document.getElementById('rg2Remember').checked;

  /* вход владельца по мастер-почте — без кода */
  if(kind === 'email' && contact.toLowerCase() === RG2_ADMIN_EMAIL){
    const btn = document.getElementById('rg2Next1Btn');
    btn.classList.add('loading'); btn.disabled = true;
    let ok = false;
    try{ if(typeof adminLogin === 'function') ok = await adminLogin(contact, p1); }catch(e){}
    btn.classList.remove('loading'); btn.disabled = false;
    if(!ok) return rg2Err('rg2Err1', 'Неверный пароль владельца');
    /* готово: сразу входим */
    rg2FinishOwner();
    return;
  }
  /* отправить код (мок / stub) — реально pinger позже подключит /api.php?action=auth_send_code */
  rg2SendCode();
  rg2GoTo(2);
}
function rg2Login(){
  /* «Уже есть аккаунт»: пока это тот же поток авторизации (нет отдельного «войти по паролю»).
     Валидная почта + пароль → на 2 шаг за кодом. Иначе — подсказка. */
  const contact = document.getElementById('rg2Contact').value.trim();
  const p1 = document.getElementById('rg2Pass').value;
  if(!contact || !p1){
    return rg2Err('rg2Err1', 'Введи почту/телефон и пароль — и жми «Продолжить»');
  }
  rg2Next1();
}

/* ---------- ШАГ 2 ---------- */
function rg2Step2HTML(){
  return `
  <section class="rg2-step" id="rg2Step2">
    <div class="rg2-code-ico"><svg class="i"><use href="#i-lock"/></svg></div>
    <h1 class="rg2-h rg2-h--c">Введи код</h1>
    <p class="rg2-sub rg2-sub--c">Отправили на <b id="rg2CodeTo">—</b></p>
    <div class="rg2-code" id="rg2Code">
      ${[0,1,2,3,4,5].map(i=>`<input inputmode="numeric" maxlength="1" data-i="${i}" ${i===0?'autocomplete="one-time-code"':''}>`).join('')}
    </div>
    <div class="rg2-err rg2-err--c" id="rg2Err2"></div>
    <button type="button" class="rg2-resend" id="rg2Resend" onclick="rg2Resend()" disabled>Отправить снова через 45 сек</button>
    <button class="rg2-btn" id="rg2Next2Btn" onclick="rg2Next2()" disabled>Продолжить <svg class="i"><use href="#i-chev"/></svg></button>
    <button type="button" class="rg2-linkbtn" onclick="rg2Back()">← Изменить контакт</button>
  </section>`;
}
function rg2SendCode(){
  RG2.code = rg2Rand6();
  const to = document.getElementById('rg2CodeTo');
  if(to) to.textContent = rg2MaskContact(RG2.contact);
  /* очистить инпуты */
  document.querySelectorAll('#rg2Code input').forEach(i=>{ i.value=''; i.classList.remove('fill'); });
  document.getElementById('rg2Code').classList.remove('err');
  rg2Err('rg2Err2', '');
  rg2Next2Enable();
  /* таймер повторной отправки */
  if(RG2.codeTimer) clearInterval(RG2.codeTimer);
  RG2.codeLeft = 45;
  const btn = document.getElementById('rg2Resend');
  if(btn){ btn.disabled = true; btn.textContent = 'Отправить снова через 45 сек'; }
  RG2.codeTimer = setInterval(()=>{
    RG2.codeLeft--;
    if(RG2.codeLeft <= 0){
      clearInterval(RG2.codeTimer); RG2.codeTimer = null;
      if(btn){ btn.disabled = false; btn.textContent = 'Отправить снова'; }
    } else if(btn){
      btn.textContent = 'Отправить снова через ' + RG2.codeLeft + ' сек';
    }
  }, 1000);
  /* демо-показ кода (только на клиенте; в проде убрать) */
  try{ if(typeof toast === 'function') toast('Код: ' + RG2.code + ' (демо)'); }catch(e){}
  /* реальная отправка (когда бек включит) */
  try{
    if(typeof OKO_API !== 'undefined'){
      fetch(OKO_API + '?action=auth_send_code', {
        method:'POST', headers:{'Content-Type':'application/json'},
        body: JSON.stringify({contact: RG2.contact, method: RG2.method})
      }).catch(()=>{});
    }
  }catch(e){}
}
function rg2Resend(){
  if(RG2.resendUsed >= 5) return;
  RG2.resendUsed++;
  rg2SendCode();
}
function rg2CodeVal(){ return [...document.querySelectorAll('#rg2Code input')].map(i=>i.value).join(''); }
function rg2Next2Enable(){
  const v = rg2CodeVal();
  const b = document.getElementById('rg2Next2Btn');
  if(b) b.disabled = v.length !== 6;
}
function rg2Next2(){
  const v = rg2CodeVal();
  if(v.length !== 6) return rg2Err('rg2Err2', 'Введи все 6 цифр кода');
  /* «правильный код» = сгенерированный, или спец-мастер 000000 (для теста) */
  if(v !== RG2.code && v !== '000000'){
    const row = document.getElementById('rg2Code');
    row.classList.remove('err'); void row.offsetWidth; row.classList.add('err');
    return rg2Err('rg2Err2', 'Код не подходит — проверь и попробуй ещё раз');
  }
  /* авто-имя из email до @ */
  if(!RG2.name){
    RG2.name = RG2.method === 'email' ? (RG2.contact.split('@')[0].replace(/[^a-zA-Zа-яА-Я0-9]/g,' ').trim().slice(0,24) || 'Гость') : 'Гость';
  }
  const nameInp = document.getElementById('rg2Name');
  if(nameInp && !nameInp.value) nameInp.value = RG2.name;
  const nickInp = document.getElementById('rg2Nick');
  if(nickInp && !nickInp.value) nickInp.value = RG2.nick;
  rg2AvaSync();
  rg2GoTo(3);
}

/* ---------- ШАГ 3: профиль ---------- */
function rg2Step3HTML(){
  return `
  <section class="rg2-step" id="rg2Step3">
    <h1 class="rg2-h">Профиль</h1>
    <p class="rg2-sub">Как тебя показывать в OKO.</p>
    <div class="rg2-ava-row">
      <button type="button" class="rg2-ava" id="rg2Ava" onclick="rg2AvaPick()" aria-label="Выбрать аватар">
        <span class="rg2-ava-fg" id="rg2AvaFg">O</span>
        <span class="rg2-ava-edit"><svg class="i"><use href="#i-camera"/></svg></span>
      </button>
      <div class="rg2-ava-side">
        <div class="rg2-ava-lab">Аватар</div>
        <div class="rg2-ava-sw" id="rg2AvaSw"></div>
        <button type="button" class="rg2-ava-upload" onclick="document.getElementById('rg2AvaFile').click()">
          <svg class="i"><use href="#i-photo"/></svg> Загрузить фото
        </button>
      </div>
    </div>
    <label class="rg2-lab" for="rg2Name">Имя</label>
    <div class="rg2-field">
      <span class="rg2-field-ic"><svg class="i"><use href="#i-user"/></svg></span>
      <input id="rg2Name" type="text" autocomplete="name" placeholder="Как тебя зовут" oninput="rg2NameInput()">
    </div>
    <label class="rg2-lab" for="rg2Nick">Никнейм</label>
    <div class="rg2-field rg2-nick">
      <span class="rg2-field-ic">@</span>
      <input id="rg2Nick" type="text" autocomplete="off" placeholder="никнейм" oninput="rg2NickInput()">
      <span class="rg2-nick-st" id="rg2NickSt"></span>
    </div>
    <div class="rg2-nick-msg" id="rg2NickMsg">Придумай уникальный @username — под ним тебя найдут в OKO</div>
    <label class="rg2-lab" for="rg2Bio">Био <span class="rg2-lab-dim">— по желанию</span></label>
    <div class="rg2-field rg2-bio-wrap">
      <textarea id="rg2Bio" maxlength="160" placeholder="Пара слов о себе — покажется в профиле" oninput="rg2BioInput()"></textarea>
      <span class="rg2-bio-count" id="rg2BioCount">0 / 160</span>
    </div>
    <div class="rg2-err" id="rg2Err3"></div>
    <button class="rg2-btn" id="rg2Next3Btn" onclick="rg2Next3()" disabled>Продолжить <svg class="i"><use href="#i-chev"/></svg></button>
  </section>`;
}
function rg2AvaPalette(){
  return (typeof REG_AVA !== 'undefined' && REG_AVA.length) ? REG_AVA : [
    {bg:'linear-gradient(135deg,#B9FF4D,#7ACC00)', fg:'#001400'},
    {bg:'linear-gradient(135deg,#9AFF00,#4d9c00)', fg:'#04140a'},
    {bg:'linear-gradient(135deg,#1c1c1c,#000)',     fg:'#9AFF00'},
  ];
}
function rg2AvaRenderSw(){
  const sw = document.getElementById('rg2AvaSw'); if(!sw) return;
  const pal = rg2AvaPalette();
  sw.innerHTML = pal.map((a,i)=>
    `<button type="button" class="rg2-ava-swb ${(i===RG2.avaIdx && !RG2.avaImg)?'on':''}" style="background:${a.bg}" onclick="rg2AvaPickIdx(${i})" aria-label="Акцент ${i+1}"></button>`
  ).join('');
}
function rg2AvaPickIdx(i){
  RG2.avaIdx = i; RG2.avaImg = '';
  rg2AvaRenderSw(); rg2AvaSync();
}
function rg2AvaPick(){
  /* тап на кружок — открыть file input */
  const f = document.getElementById('rg2AvaFile');
  if(f) f.click();
}
function rg2AvaSync(){
  const el = document.getElementById('rg2Ava');
  const fg = document.getElementById('rg2AvaFg');
  if(!el || !fg) return;
  const nm = (document.getElementById('rg2Name')||{}).value || RG2.name || '';
  fg.textContent = (nm.trim()[0] || 'O').toUpperCase();
  if(RG2.avaImg){
    el.style.backgroundImage = 'url(' + RG2.avaImg + ')';
    el.style.backgroundSize = 'cover';
    el.style.backgroundPosition = 'center';
    el.style.color = 'transparent';
    fg.style.opacity = '0';
  } else {
    const a = rg2AvaPalette()[RG2.avaIdx] || rg2AvaPalette()[0];
    el.style.backgroundImage = '';
    el.style.background = a.bg;
    el.style.color = a.fg;
    fg.style.opacity = '1';
  }
}
function rg2NameInput(){
  rg2Err('rg2Err3', '');
  RG2.name = document.getElementById('rg2Name').value.trim();
  rg2AvaSync();
  rg2Next3Enable();
}
function rg2NickInput(){
  rg2Err('rg2Err3','');
  const raw = document.getElementById('rg2Nick').value;
  const clean = raw.toLowerCase().replace(/[^a-z0-9_]/g,'').slice(0,24);
  if(raw !== clean) document.getElementById('rg2Nick').value = clean;
  RG2.nick = clean;
  const st  = document.getElementById('rg2NickSt');
  const msg = document.getElementById('rg2NickMsg');
  if(!clean){ st.innerHTML=''; msg.textContent='Придумай уникальный @username — под ним тебя найдут в OKO'; msg.className='rg2-nick-msg'; rg2Next3Enable(); return; }
  if(clean.length < 3){
    st.innerHTML = '<span class="bad"><svg class="i" style="transform:rotate(45deg)"><use href="#i-plus"/></svg></span>';
    msg.textContent = 'Слишком короткий — минимум 3 символа';
    msg.className = 'rg2-nick-msg bad';
    rg2Next3Enable(); return;
  }
  if(RG2_TAKEN_NICKS.includes(clean)){
    st.innerHTML = '<span class="bad"><svg class="i" style="transform:rotate(45deg)"><use href="#i-plus"/></svg></span>';
    msg.textContent = '@' + clean + ' уже занят — попробуй другой';
    msg.className = 'rg2-nick-msg bad';
    rg2Next3Enable(); return;
  }
  st.innerHTML = '<span class="ok"><svg class="i"><use href="#i-check2"/></svg></span>';
  msg.textContent = '@' + clean + ' свободен';
  msg.className = 'rg2-nick-msg ok';
  rg2Next3Enable();
}
function rg2BioInput(){
  RG2.bio = document.getElementById('rg2Bio').value.slice(0,160);
  const c = document.getElementById('rg2BioCount');
  if(c) c.textContent = RG2.bio.length + ' / 160';
}
function rg2NickOk(){
  const v = RG2.nick;
  return !!v && v.length >= 3 && !RG2_TAKEN_NICKS.includes(v);
}
function rg2Next3Enable(){
  const b = document.getElementById('rg2Next3Btn');
  if(!b) return;
  b.disabled = !(RG2.name && rg2NickOk());
}
function rg2Next3(){
  if(!RG2.name)     return rg2Err('rg2Err3', 'Укажи имя — так тебя увидят в OKO');
  if(!rg2NickOk())  return rg2Err('rg2Err3', 'Придумай никнейм ≥ 3 символа');
  /* сохранить профиль в PROFILE до финиша (пусть UI видит после back-навигации) */
  try{
    if(typeof PROFILE !== 'undefined'){
      PROFILE.name = RG2.name;
      PROFILE.nick = RG2.nick;
      PROFILE.bio  = RG2.bio || PROFILE.bio;
      if(RG2.method === 'email') PROFILE.email = RG2.contact;
      if(RG2.method === 'phone') PROFILE.phone = RG2.contact;
      /* цветовая аватарка */
      const a = rg2AvaPalette()[RG2.avaIdx] || rg2AvaPalette()[0];
      PROFILE.avaBg = a.bg; PROFILE.avaFg = a.fg;
      if(RG2.avaImg) PROFILE.avaImg = RG2.avaImg;
    }
  }catch(e){}
  /* persist */
  try{ localStorage.setItem('oko-rg2', JSON.stringify({
    contact:RG2.contact, method:RG2.method,
    name:RG2.name, nick:RG2.nick, bio:RG2.bio,
    avaIdx:RG2.avaIdx, avaImg:RG2.avaImg ? '1' : '',
    remember: RG2.remember, at: Date.now()
  })); }catch(e){}
  if(RG2.avaImg){ try{ localStorage.setItem('oko-rg2-ava', RG2.avaImg); }catch(e){} }
  /* заранее ставим oko-auth только если «сохранить вход» */
  if(RG2.remember){ try{ localStorage.setItem('oko-auth', RG2.method); }catch(e){} }
  rg2RenderTiers();
  rg2GoTo(4);
}

/* ---------- ШАГ 4: тарифы ---------- */
function rg2Step4HTML(){
  return `
  <section class="rg2-step" id="rg2Step4">
    <h1 class="rg2-h">Твой тариф</h1>
    <p class="rg2-sub">Выбери сейчас — можно менять в любой момент.</p>
    <div class="rg2-period" id="rg2Period">
      ${RG2_PERIODS.map(p=>`
        <button type="button" class="rg2-per ${p.m===1?'on':''}" data-m="${p.m}" onclick="rg2PeriodPick(${p.m})">
          <span>${p.lab}</span>${p.disc?`<i>−${p.disc}%</i>`:''}
        </button>
      `).join('')}
    </div>
    <div class="rg2-tiers" id="rg2Tiers"></div>
    <button class="rg2-btn" id="rg2Next4Btn" onclick="rg2Next4()">Начать пользоваться <svg class="i"><use href="#i-chev"/></svg></button>
    <button type="button" class="rg2-linkbtn" onclick="rg2Skip()">Пропустить — начать с FREE</button>
  </section>`;
}
function rg2Fmt(n){
  return Number(n||0).toLocaleString('ru-RU').replace(/,/g,' ');
}
function rg2PeriodPick(m){
  RG2.period = m;
  document.querySelectorAll('#rg2Period .rg2-per').forEach(b=>{
    b.classList.toggle('on', +b.dataset.m === m);
  });
  rg2RenderTiers();
}
function rg2TierPick(id){
  RG2.tier = id;
  document.querySelectorAll('#rg2Tiers .rg2-tier').forEach(el=>{
    el.classList.toggle('sel', el.dataset.id === id);
  });
  const b = document.getElementById('rg2Next4Btn');
  if(b){
    if(id === 'FREE') b.innerHTML = 'Начать пользоваться <svg class="i"><use href="#i-chev"/></svg>';
    else {
      const per = RG2_PERIODS.find(p=>p.m===RG2.period) || RG2_PERIODS[0];
      const tier = RG2_TIERS.find(t=>t.id===id);
      const total = Math.round(tier.price * per.m * (1 - per.disc/100));
      b.innerHTML = 'Оформить ' + tier.name + ' · ' + rg2Fmt(total) + ' ₽ <svg class="i"><use href="#i-chev"/></svg>';
    }
  }
}
function rg2RenderTiers(){
  const box = document.getElementById('rg2Tiers'); if(!box) return;
  const per = RG2_PERIODS.find(p=>p.m===RG2.period) || RG2_PERIODS[0];
  box.innerHTML = RG2_TIERS.map(t=>{
    const total = Math.round(t.price * per.m * (1 - per.disc/100));
    const priceStr = t.price === 0 ? 'бесплатно'
      : (per.m === 1 ? rg2Fmt(t.price)+' ₽/мес' : rg2Fmt(total)+' ₽ за '+per.lab.toLowerCase());
    return `<button type="button" class="rg2-tier ${t.reco?'reco':''} ${RG2.tier===t.id?'sel':''}" data-id="${t.id}" onclick="rg2TierPick('${t.id}')">
      <div class="rg2-tier-head">
        <div class="rg2-tier-name">${t.name}${t.reco?'<span class="rg2-tier-badge">Рекомендуем</span>':''}</div>
        <div class="rg2-tier-price">${priceStr}</div>
      </div>
      <div class="rg2-tier-line">${t.line}</div>
      <ul class="rg2-tier-feats">
        ${t.feats.map(f=>`<li><svg class="i"><use href="#i-check2"/></svg>${esc(f)}</li>`).join('')}
      </ul>
    </button>`;
  }).join('');
  /* если ни одного не выбрано — подсветить FREE */
  if(!document.querySelector('#rg2Tiers .rg2-tier.sel')){
    RG2.tier = 'FREE';
    const el = document.querySelector('#rg2Tiers .rg2-tier[data-id="FREE"]');
    if(el) el.classList.add('sel');
  }
  rg2TierPick(RG2.tier);
}
function rg2Skip(){ RG2.tier = 'FREE'; rg2Next4(); }
function rg2Next4(){
  /* сохранить тариф */
  try{ if(typeof PROFILE !== 'undefined') PROFILE.tier = RG2.tier; }catch(e){}
  try{
    const r = JSON.parse(localStorage.getItem('oko-rg2')||'{}');
    r.tier = RG2.tier; r.period = RG2.period;
    localStorage.setItem('oko-rg2', JSON.stringify(r));
  }catch(e){}
  /* завершить регистрацию */
  rg2FinishFlow(RG2.tier !== 'FREE');
}

/* ---------- финиш ---------- */
function rg2FinishFlow(openPaySheet){
  /* пометить, чтобы штатный онбординг после doLogin не запустился (у нас свой поток) */
  try{ localStorage.setItem('oko-onboarded','1'); }catch(e){}
  try{ localStorage.setItem('oko-auth', RG2.method); }catch(e){}
  /* спрятать shell + auth-inner, скрыть authScreen */
  rg2Exit();
  const scr = document.getElementById('authScreen');
  if(scr) scr.classList.add('hidden');
  try{ if(typeof stopParticles === 'function') stopParticles(); }catch(e){}
  try{ if(typeof initLive === 'function') initLive(); }catch(e){}
  try{ if(typeof renderMyProfile === 'function') renderMyProfile(); }catch(e){}
  /* приветственный попап (используем существующие премиум-функции).
     Начисление 2 500 ₽ убрано 09.08 — выдуманных денег на счёте нового
     человека быть не должно. */
  if(!regPopupsSeen().welcome){
    regPopupMark('welcome');
    setTimeout(()=>{
      try{
        showPopup({ico:'logo', title: regWelcomeTitle(RG2.name),
          body: regWelcomeBody(RG2.name),
          actions:[
            {label:'В приложение'},
          ]});
      }catch(e){}
    }, 500);
  }
  /* открыть feed */
  try{ if(typeof showTab === 'function') showTab('feed'); }catch(e){}
  /* оплата если не FREE */
  if(openPaySheet){
    setTimeout(()=>{
      try{
        if(typeof openPay === 'function' && typeof PLANS !== 'undefined' && PLANS[RG2.tier]){
          openPay(RG2.tier);
        } else {
          if(typeof toast === 'function') toast('Тариф ' + RG2.tier + ' скоро подключим');
        }
      }catch(e){}
    }, 900);
  }
  regSchedulePopups();
}
function rg2FinishOwner(){
  try{ localStorage.setItem('oko-onboarded','1'); }catch(e){}
  try{ localStorage.setItem('oko-auth','email'); }catch(e){}
  try{
    if(typeof PROFILE !== 'undefined'){ PROFILE.email = RG2.contact; }
  }catch(e){}
  rg2Exit();
  const scr = document.getElementById('authScreen');
  if(scr) scr.classList.add('hidden');
  try{ if(typeof stopParticles === 'function') stopParticles(); }catch(e){}
  try{ if(typeof initLive === 'function') initLive(); }catch(e){}
  try{ if(typeof renderMyProfile === 'function') renderMyProfile(); }catch(e){}
  setTimeout(()=>{
    try{
      showPopup({ico:'crown', title:'Доступ владельца',
        body:'Генеральный директор OKO. Открыт полный доступ: админка, доход платформы, HQ и управление всеми разделами.',
        actions:[{label:'В приложение'}]});
    }catch(e){}
  }, 500);
  regSchedulePopups();
}

/* ---------- one-time bindings (после первой сборки shell) ---------- */
function rg2BindOnce(){
  /* инпуты кода: авто-фокус цепочка, backspace, paste всех 6 */
  document.querySelectorAll('#rg2Code input').forEach(inp=>{
    inp.addEventListener('input', ()=>{
      inp.value = inp.value.replace(/\D/g,'').slice(0,1);
      inp.classList.toggle('fill', !!inp.value);
      rg2Err('rg2Err2','');
      const i = +inp.dataset.i;
      if(inp.value && i < 5) document.querySelector('#rg2Code input[data-i="'+(i+1)+'"]').focus();
      rg2Next2Enable();
      if(rg2CodeVal().length === 6) setTimeout(rg2Next2, 120);
    });
    inp.addEventListener('keydown', e=>{
      const i = +inp.dataset.i;
      if(e.key === 'Backspace' && !inp.value && i > 0){
        const prev = document.querySelector('#rg2Code input[data-i="'+(i-1)+'"]');
        if(prev){ prev.focus(); prev.value=''; prev.classList.remove('fill'); rg2Next2Enable(); }
      }
    });
    inp.addEventListener('paste', e=>{
      const d = (e.clipboardData.getData('text')||'').replace(/\D/g,'').slice(0,6);
      if(d.length < 2) return;
      e.preventDefault();
      document.querySelectorAll('#rg2Code input').forEach((c,j)=>{
        c.value = d[j] || '';
        c.classList.toggle('fill', !!c.value);
      });
      const idx = Math.min(d.length, 5);
      const next = document.querySelector('#rg2Code input[data-i="'+idx+'"]');
      if(next) next.focus();
      rg2Next2Enable();
      if(d.length === 6) setTimeout(rg2Next2, 120);
    });
  });
  /* аватар: file input */
  const file = document.getElementById('rg2AvaFile');
  if(file){
    file.addEventListener('change', e=>{
      const f = e.target.files && e.target.files[0];
      if(!f) return;
      const fr = new FileReader();
      fr.onload = ev => {
        RG2.avaImg = String(ev.target.result || '');
        rg2AvaSync(); rg2AvaRenderSw();
      };
      fr.readAsDataURL(f);
      /* сбросить value чтобы можно было выбрать ту же картинку заново */
      try{ e.target.value = ''; }catch(er){}
    });
  }
  /* пре-рендер свотчей и тарифов */
  rg2AvaRenderSw();
  /* по Enter на step1 — Продолжить */
  ['rg2Contact','rg2Pass','rg2Pass2'].forEach(id=>{
    const el = document.getElementById(id);
    if(el) el.addEventListener('keydown', e=>{ if(e.key === 'Enter'){ e.preventDefault(); rg2Next1(); }});
  });
  ['rg2Name','rg2Nick'].forEach(id=>{
    const el = document.getElementById(id);
    if(el) el.addEventListener('keydown', e=>{ if(e.key === 'Enter'){ e.preventDefault(); rg2Next3(); }});
  });
}

/* ---------- chain-patch renderMyProfile: показ фото-аватара ---------- */
try{
  if(typeof renderMyProfile === 'function'){
    const _prev = renderMyProfile;
    renderMyProfile = function(){
      _prev.apply(this, arguments);
      try{
        const el = document.getElementById('profAva');
        if(!el || typeof PROFILE === 'undefined') return;
        if(PROFILE.avaImg){
          el.style.backgroundImage = 'url(' + PROFILE.avaImg + ')';
          el.style.backgroundSize = 'cover';
          el.style.backgroundPosition = 'center';
          el.style.color = 'transparent';
        } else if(PROFILE.avaBg){
          el.style.background = PROFILE.avaBg;
          if(PROFILE.avaFg) el.style.color = PROFILE.avaFg;
        }
      }catch(e){}
    };
  }
}catch(e){}

/* восстановить сохранённый аватар из localStorage при загрузке */
try{
  const saved = JSON.parse(localStorage.getItem('oko-rg2') || 'null');
  if(saved){
    if(typeof PROFILE !== 'undefined'){
      if(saved.name) PROFILE.name = saved.name;
      if(saved.nick) PROFILE.nick = saved.nick;
      if(saved.bio)  PROFILE.bio  = saved.bio;
      if(saved.method === 'email') PROFILE.email = saved.contact;
      if(saved.method === 'phone') PROFILE.phone = saved.contact;
      if(saved.tier) PROFILE.tier = saved.tier;
      const a = rg2AvaPalette()[saved.avaIdx] || rg2AvaPalette()[0];
      PROFILE.avaBg = a.bg; PROFILE.avaFg = a.fg;
      if(saved.avaImg){
        const img = localStorage.getItem('oko-rg2-ava');
        if(img) PROFILE.avaImg = img;
      }
    }
  }
}catch(e){}

try{ window.rg2Open = rg2Open; window.rg2Back = rg2Back; }catch(e){}

/* ============================================================
   ГЛОБАЛЬНЫЙ ГЕЙТ на приглашение в Академию (bugfix v1.94).
   Аудит: попап «Академия OKO — начни урок 1» дублировался при
   каждом переключении вкладок. Держим строгий флаг в localStorage
   (`oko-academy-invite-seen`) — показываем максимум ОДИН раз
   за жизнь аккаунта, а не за сессию. Дополнительно давим повторы
   в рамках одной сессии (быстрые re-check по таймеру).
   ============================================================ */
(function regAcademyInviteGate(){
  const K = 'oko-academy-invite-seen';
  const SK = 'oko-academy-invite-shown-session';
  /* распознать «приглашение начать урок 1»: строго по тайтлу + телу */
  function isAcademyInvite(o){
    if(!o) return false;
    const t = String(o.title || '');
    const b = String(o.body || '');
    if(!/Академия\s*OKO/i.test(t)) return false;
    return /Начни\s*урок/i.test(b) || /Продолжи\s*урок/i.test(b);
  }
  function seen(){ try{ return !!localStorage.getItem(K); }catch(e){ return false; } }
  function mark(){
    try{ localStorage.setItem(K, String(Date.now())); }catch(e){}
    try{ sessionStorage.setItem(SK, '1'); }catch(e){}
  }
  /* Chain-патч: перехватываем ЛЮБОЙ вызов showPopup.
     Если это академ-приглашение и мы его уже видели (в этом аккаунте либо в этой
     сессии) — молча гасим. Иначе — помечаем и пропускаем в базовый showPopup. */
  if(typeof showPopup === 'function'){
    const _regPrevShowPopup = showPopup;
    showPopup = function(o){
      if(isAcademyInvite(o)){
        let sessionShown = false;
        try{ sessionShown = !!sessionStorage.getItem(SK); }catch(e){}
        if(seen() || sessionShown) return;      /* уже видели — не показываем */
        mark();
      }
      _regPrevShowPopup(o);
    };
  }
})();

/* ============================================================
   ПРЕМИУМ-ОНБОРДИНГ — брендовая воронка активации.
   Оболочка (#onboard, obShow/obNext/obFinish, ONBOARD, obStep)
   живёт в base.html. Мы доводим её цепочкой: пересобираем данные
   слайдов (ONBOARD) и переопределяем renderOb на богатые сцены.
   Ничего в base.html не редактируем.
   ============================================================ */
const REG_OB = [
  {scene:'brand',   tag:'OKO',              t:'Один глаз на всё',          d:'Мессенджер, лента, биржа, инструменты для роста, академия и заработок в одном приложении. Добро пожаловать в OKO.'},
  {scene:'chat',    tag:'Мессенджер',       t:'Общение без границ',        d:'Личные чаты, группы и каналы в реальном времени. Голосовые, файлы и переводы денег — прямо в переписке.'},
  {scene:'feed',    tag:'Умная лента',      t:'Лента, которая знает тебя',  d:'Настоящие алгоритмы рекомендаций подстраивают ленту под твои интересы, и продвигают твои посты к новой аудитории.'},
  {scene:'market',  tag:'Биржа',            t:'Зарабатывай на навыках',     d:'Услуги и заказы с эскроу-защитой: деньги замораживаются и уходят исполнителю только после результата.'},
  {scene:'ai',      tag:'Инструменты',t:'Команда берёт рутину на себя',    d:'Готовые посты, ролики, картинки и озвучка, специалисты OKO соберут за тебя. Идея превращается в готовый контент.'},
  {scene:'academy', tag:'Академия',         t:'Учись и получай сертификат', d:'Видео-уроки, тесты и практика. За каждый пройденный курс — официальный сертификат OKO с печатью.'},
  {scene:'earn',    tag:'Партнёрка',        t:'Расти вместе с OKO',         d:'Партнёрская программа, рекламный кабинет и контент-завод. Приводи людей и клиентов, доход сразу в приложении.'},
];

/* уникальная сцена под каждый слайд — ни один приём не повторяется */
function regObScene(key){
  switch(key){
    case 'brand':
      return `<div class="reg-ob-scene reg-ob-scene--brand">
        <span class="reg-ob-ring"></span><span class="reg-ob-ring d2"></span><span class="reg-ob-ring d3"></span>
        <span class="reg-ob-orb o1"></span><span class="reg-ob-orb o2"></span><span class="reg-ob-orb o3"></span>
        <span class="reg-ob-logo">${I('logo')}</span>
      </div>`;
    case 'chat':
      return `<div class="reg-ob-scene reg-ob-scene--chat">
        <div class="reg-ob-bub in b1">Видел новую биржу OKO?</div>
        <div class="reg-ob-bub out b2">Да, уже беру первый заказ</div>
        <div class="reg-ob-bub in b3"><span class="reg-ob-typing"><i></i><i></i><i></i></span></div>
      </div>`;
    case 'feed':
      return `<div class="reg-ob-scene reg-ob-scene--feed">
        <div class="reg-ob-card">
          <div class="reg-ob-card-h">
            <span class="reg-ob-mini-ava">Н</span>
            <div><b>Нейро-дайджест</b><span class="reg-ob-rec">${I('compass')} рекомендовано вам</span></div>
          </div>
          <div class="reg-ob-lines"><i></i><i></i><i></i></div>
          <div class="reg-ob-react"><span>${I('heart')} 512</span><span>${I('comment')} 64</span><span>${I('share')}</span></div>
        </div>
      </div>`;
    case 'market':
      return `<div class="reg-ob-scene reg-ob-scene--market">
        <div class="reg-ob-list">
          <div class="reg-ob-list-top">
            <div class="reg-ob-list-img">${I('briefcase')}</div>
            <div class="reg-ob-list-b"><b>Монтаж Reels под ключ</b><small>исполнитель · 4.9 ${I('star')}</small></div>
            <span class="reg-ob-price">2 500 ₽</span>
          </div>
          <span class="reg-ob-escrow">${I('lock')} Сделка защищена эскроу</span>
        </div>
      </div>`;
    case 'ai':
      return `<div class="reg-ob-scene reg-ob-scene--ai">
        <span class="reg-ob-spark s1"></span><span class="reg-ob-spark s2"></span><span class="reg-ob-spark s3"></span>
        <div class="reg-ob-prompt">${I('bolt')}<span>Сделай ролик из статьи…</span></div>
        <div class="reg-ob-gen"><i></i><i></i><i></i></div>
      </div>`;
    case 'academy':
      return `<div class="reg-ob-scene reg-ob-scene--academy">
        <div class="reg-ob-cert">
          <div class="reg-ob-cert-h">${I('star')} Сертификат OKO</div>
          <div class="reg-ob-cert-name">Мастер контента</div>
          <div class="reg-ob-cert-lines"><i></i><i></i></div>
          <div class="reg-ob-seal">${I('check2')}</div>
        </div>
      </div>`;
    case 'earn':
      return `<div class="reg-ob-scene reg-ob-scene--earn">
        <div class="reg-ob-earn-sum"><span class="reg-ob-coin">${I('money')}</span><b>2 500 ₽</b></div>
        <div class="reg-ob-bars"><i></i><i></i><i></i><i></i></div>
      </div>`;
    default:
      return `<div class="reg-ob-scene reg-ob-scene--brand"><span class="reg-ob-logo">${I('logo')}</span></div>`;
  }
}

/* пересобрать данные онбординга ядра под наши 7 слайдов (ONBOARD — const-массив,
   мутируем содержимое, не переназначаем ссылку) */
try{
  if(typeof ONBOARD !== 'undefined' && Array.isArray(ONBOARD)){
    ONBOARD.length = 0;
    REG_OB.forEach(function(x){ ONBOARD.push(x); });
  }
}catch(e){}

/* переопределяем рендер онбординга на премиум-версию */
if(typeof renderOb === 'function'){
  renderOb = function(){
    const list = (typeof ONBOARD !== 'undefined' && ONBOARD.length) ? ONBOARD : REG_OB;
    const n = list.length;
    if(typeof obStep !== 'number' || obStep < 0) obStep = 0;
    if(obStep > n - 1) obStep = n - 1;
    const s = list[obStep] || REG_OB[obStep] || REG_OB[0];
    const stage = document.getElementById('obStage');
    if(!stage) return;
    stage.innerHTML =
      `<div class="reg-ob-top">
         <span class="reg-ob-count-chip">Шаг ${obStep + 1} / ${n}</span>
         <div class="reg-ob-track"><i style="width:${((obStep + 1) / n * 100).toFixed(1)}%"></i></div>
       </div>
       <div class="reg-ob-center">
         <div class="reg-ob" data-step="${obStep}">
           ${regObScene(s.scene)}
           <span class="reg-ob-tag">${esc(s.tag || '')}</span>
           <h2 class="reg-ob-title">${esc(s.t || '')}</h2>
           <p class="reg-ob-desc">${esc(s.d || '')}</p>
         </div>
       </div>`;
    const dots = document.getElementById('obDots');
    if(dots) dots.innerHTML = list.map((_, i)=>`<span class="${i === obStep ? 'on' : ''}"></span>`).join('');
    const nx = document.getElementById('obNext');
    if(nx) nx.innerHTML = (obStep === n - 1 ? 'Начать в OKO' : 'Далее') + I('chev');
  };
}

/* ---------- самоинициализация ---------- */
(function regInit(){
  /* реф-параметр в URL — сразу же в localStorage, чтобы welcome его увидел */
  regRefCapture();

  /* ввод кода: автофокус-цепочка, backspace, вставка всего кода разом */
  document.querySelectorAll('#regCodeRow input').forEach(inp=>{
    inp.addEventListener('input', ()=>{
      inp.value = inp.value.replace(/\D/g, '').slice(0, 1);
      inp.classList.toggle('fill', !!inp.value);
      regErrHide('regErr2');
      const i = +inp.dataset.i;
      if(inp.value && i < 3) regCodeFocus(i + 1);
      if(regCodeVal().length === 4) setTimeout(regVerifyCode, 120);
    });
    inp.addEventListener('keydown', e=>{
      const i = +inp.dataset.i;
      if(e.key === 'Backspace' && !inp.value && i > 0) regCodeFocus(i - 1);
    });
    inp.addEventListener('paste', e=>{
      const d = (e.clipboardData.getData('text') || '').replace(/\D/g, '').slice(0, 4);
      if(d.length < 2) return;
      e.preventDefault();
      document.querySelectorAll('#regCodeRow input').forEach((c, j)=>{ c.value = d[j] || ''; c.classList.toggle('fill', !!c.value); });
      if(d.length === 4) setTimeout(regVerifyCode, 120);
      else regCodeFocus(Math.min(d.length, 3));
    });
  });
  regRenderInts();
  /* дата рождения не может быть в будущем */
  const bd = document.getElementById('regBirth');
  if(bd){ try{ bd.max = new Date().toISOString().slice(0, 10); }catch(e){} }
  /* если уже вошли (перезагрузка) — планируем одноразовые попапы */
  if(typeof authed === 'function' && authed()) regSchedulePopups();
  /* восстановить выбранный аватар после перезагрузки */
  try{
    const reg = JSON.parse(localStorage.getItem('oko-registration') || 'null');
    if(reg && typeof reg.avaIdx === 'number' && REG_AVA[reg.avaIdx]){
      REG.avaIdx = reg.avaIdx; regApplyAva();
    }
  }catch(e){}
})();

/* ===== module: wallet ===== */
/* ================= WALLET: экран кошелька / лицевой счёт =================
   Опирается на core-ext: WALLET, walletAdd, walletCharge, fmtMoney, okoEarn.
   Патчит денежные потоки ядра (тарифы, продвижение, биржа, переводы, партнёрка).
   Плюс: график баланса 30 дней, автопродление PRO, выписка (.txt + документ),
   реквизиты пополнения, лимиты счёта и ПИН-код на вывод. */

/* ---------- иконки в общий defs (TON, свап, отпечаток, USDT, XP-звезда, чек, QR, target) ---------- */
(function walAddIcons(){
  const defs = document.querySelector('svg defs');
  if(!defs) return;
  const add = (id, vb, body)=>{
    if(document.getElementById(id)) return;
    const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
    s.setAttribute('id', id); s.setAttribute('viewBox', vb || '0 0 100 100');
    s.innerHTML = body;
    defs.appendChild(s);
  };
  add('i-ton','0 0 100 100','<path d="M18 20h64L50 88 18 20z" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/><path d="M50 20v68" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/>');
  add('i-dl','0 0 100 100','<path d="M50 14v44M32 42l18 18 18-18" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/><path d="M20 80h60" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/>');
  add('i-search','0 0 100 100','<circle cx="43" cy="43" r="27" fill="none" stroke="currentColor" stroke-width="8"/><path d="M63 63 84 84" fill="none" stroke="currentColor" stroke-width="8" stroke-linecap="round"/>');
  add('i-dice','0 0 100 100','<rect x="20" y="20" width="60" height="60" rx="14" fill="none" stroke="currentColor" stroke-width="7"/><circle cx="37" cy="37" r="5.5" fill="currentColor"/><circle cx="63" cy="37" r="5.5" fill="currentColor"/><circle cx="50" cy="50" r="5.5" fill="currentColor"/><circle cx="37" cy="63" r="5.5" fill="currentColor"/><circle cx="63" cy="63" r="5.5" fill="currentColor"/>');
  /* свап (двусторонняя стрелка обмена) */
  add('i-swap','0 0 100 100','<path d="M24 34h50l-14-14M76 66H26l14 14" fill="none" stroke="currentColor" stroke-width="8" stroke-linecap="round" stroke-linejoin="round"/>');
  /* отпечаток пальца */
  add('i-fingerprint','0 0 100 100','<path d="M30 55c0-14 9-25 20-25s20 11 20 25c0 6-1 12-3 17M50 30c-8 0-15 8-15 25 0 6 1 12 3 17M50 42c-3 0-5 3-5 12 0 8 2 20 4 30M62 46c1 5 1 12 0 20-1 6-3 12-5 18M40 78c1 4 3 8 5 11" fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round"/>');
  /* USDT — «$» в круге (простой логотип для чипа) */
  add('i-usdt','0 0 100 100','<circle cx="50" cy="50" r="38" fill="none" stroke="currentColor" stroke-width="7"/><path d="M50 32v36M40 40h20a6 6 0 010 12H40a6 6 0 000 12h20" fill="none" stroke="currentColor" stroke-width="6.5" stroke-linecap="round" stroke-linejoin="round"/>');
  /* XP-звезда (5 лучей) */
  add('i-xp','0 0 100 100','<path d="M50 12l11 25 27 3-20 19 6 27-24-13-24 13 6-27-20-19 27-3z" fill="currentColor" stroke="currentColor" stroke-width="4" stroke-linejoin="round"/>');
  /* цель / мишень */
  add('i-target','0 0 100 100','<circle cx="50" cy="50" r="34" fill="none" stroke="currentColor" stroke-width="7"/><circle cx="50" cy="50" r="20" fill="none" stroke="currentColor" stroke-width="6"/><circle cx="50" cy="50" r="6" fill="currentColor"/>');
  /* поделиться */
  add('i-share','0 0 100 100','<circle cx="24" cy="50" r="10" fill="none" stroke="currentColor" stroke-width="7"/><circle cx="72" cy="24" r="10" fill="none" stroke="currentColor" stroke-width="7"/><circle cx="72" cy="76" r="10" fill="none" stroke="currentColor" stroke-width="7"/><path d="M33 45l30-16M33 55l30 16" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/>');
  /* QR-иконка (маленькие квадраты) */
  add('i-qr','0 0 100 100','<rect x="12" y="12" width="26" height="26" rx="4" fill="none" stroke="currentColor" stroke-width="7"/><rect x="62" y="12" width="26" height="26" rx="4" fill="none" stroke="currentColor" stroke-width="7"/><rect x="12" y="62" width="26" height="26" rx="4" fill="none" stroke="currentColor" stroke-width="7"/><rect x="52" y="52" width="14" height="14" fill="currentColor"/><rect x="74" y="52" width="14" height="14" fill="currentColor"/><rect x="52" y="74" width="14" height="14" fill="currentColor"/></svg>');
  /* backspace (для пин-пада, если нет) */
  add('i-back-key','0 0 100 100','<path d="M32 20h48a10 10 0 0110 10v40a10 10 0 01-10 10H32L8 50z" fill="none" stroke="currentColor" stroke-width="7" stroke-linejoin="round"/><path d="M46 38l20 24M66 38L46 62" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/>');
})();

/* ---------- ГОСТЬ / НЕ-ВЛАДЕЛЕЦ: сбрасываем демо-баланс и не сеем демо-историю ----------
   Правка Даниэля 29.07: «почему я новый аккаунт создал и у меня баланс перенёсся весь?»
   «почему везде демо данные?». Демо-заготовки — только у owner (Даниэль).
   Всем остальным — 0 ₽, пустой леджер, никаких live-пингов. */
/* Владелец = Даниэль (ник ktodaniel). Регистрация меняет ник, но не роль,
   поэтому проверяем по нику: любой другой ник = «новый аккаунт», ему не нужны
   демо-баланс, демо-цели, демо-автопополнения и live-ping «пришло 500₽». */
function walIsOwner(){
  try{
    if(typeof PROFILE === 'undefined') return false;
    const nick = String(PROFILE.nick || '').toLowerCase();
    return nick === 'ktodaniel' || PROFILE.role === 'owner-force';
  }catch(e){ return false; }
}
(function walGuestGuard(){
  if(walIsOwner()) return;
  try{
    if(localStorage.getItem('oko-wallet-guest-v2') === '1') return;
    /* глушим оба демо-сида, даже если их уже нет */
    localStorage.setItem('oko-wallet-demo', '1');
    localStorage.setItem('oko-wallet-demo2', '1');
    WALLET.balance = 0;
    WALLET.hold = 0;
    WALLET.ledger = [];
    walletSave();
    /* Стираем оставшиеся демо-балансы (USDT/TON/XP) и демо-цели/автоправила —
       чтобы новый аккаунт стартовал с чистого нуля, а не с чужих значений. */
    localStorage.removeItem('oko-wallet-x');
    localStorage.setItem('oko-wallet-guest-v2', '1');
  }catch(e){}
})();

/* ---------- демо-наполнение леджера (один раз, реалистичные операции) ---------- */
(function walSeedDemo(){
  /* демо-сидер отключён 09.08 (правка Даниэля: ноль демо-данных) */
  return (arguments.length && Array.isArray(arguments[0])) ? [] : undefined;

  if(!walIsOwner()) return;
  try{ if(localStorage.getItem('oko-wallet-demo')==='1') return; }catch(e){}
  const H = 3600e3, now = Date.now();
  const demo = [
    {t:'-', sum:300,  why:'Перевод в чате · Марк Волков',            at: now - 5*H},
    {t:'-', sum:200,  why:'Игра «Рулетка» · ставка',                 at: now - 9*H},
    {t:'-', sum:500,  why:'Покупка на Бирже: Монтаж Reels под ключ', at: now - 26*H},
    {t:'-', sum:349,  why:'Продвижение объявления · Турбо',          at: now - 30*H},
    {t:'-', sum:1490, why:'Тариф START · 1 мес',                     at: now - 50*H},
    {t:'+', sum:5000, why:'Пополнение · Карта РФ',                   at: now - 52*H},
  ];
  demo.forEach(op=>{ WALLET.balance += op.t==='+' ? op.sum : -op.sum; });
  WALLET.ledger = demo.concat(WALLET.ledger);
  WALLET.ledger.sort((a,b)=>b.at-a.at);
  if(!WALLET.hold) WALLET.hold = 500; // эскроу по активной сделке Биржи
  walletSave();
  try{ localStorage.setItem('oko-wallet-demo','1'); }catch(e){}
})();

/* история за месяц для графика (только старые даты, баланс НЕ меняют —
   восстанавливаются графиком назад от текущего) */
(function walSeedDemo2(){
  /* демо-сидер отключён 09.08 (правка Даниэля: ноль демо-данных) */
  return (arguments.length && Array.isArray(arguments[0])) ? [] : undefined;

  if(!walIsOwner()) return;
  try{ if(localStorage.getItem('oko-wallet-demo2')==='1') return; }catch(e){}
  const D = 864e5, now = Date.now();
  const hist = [
    {t:'+', sum:1200, why:'Продажа на Бирже: обложка канала',   at: now - 3*D},
    {t:'-', sum:150,  why:'Стикеры OKO · набор «Неон»',         at: now - 4*D},
    {t:'+', sum:900,  why:'Партнёрская программа · выплата',    at: now - 6*D},
    {t:'-', sum:349,  why:'Продвижение объявления · Старт',     at: now - 8*D},
    {t:'-', sum:600,  why:'Рулетка OKO · платный спин',          at: now - 11*D},
    {t:'+', sum:800,  why:'Пополнение · USDT',                  at: now - 13*D},
    {t:'-', sum:1490, why:'Тариф START · 1 мес',                at: now - 19*D},
    {t:'+', sum:700,  why:'Возврат по спору · Биржа',           at: now - 22*D},
    {t:'-', sum:250,  why:'Перевод в чате · Аня Соколова',      at: now - 26*D},
    {t:'+', sum:1500, why:'Пополнение · Карта РФ',              at: now - 28*D},
  ];
  WALLET.ledger = WALLET.ledger.concat(hist);
  WALLET.ledger.sort((a,b)=>b.at-a.at);
  walletSave();
  try{ localStorage.setItem('oko-wallet-demo2','1'); }catch(e){}
})();

/* ---------- состояние модуля ---------- */
let walFilter = 'all';
let walSearch = '';
let walPeriod = 'all';
let walTopupState = {sum:1000, method:'card'};
let walWdState = {sum:0, method:'card'};
const WAL_METHODS = [
  ['card','Карта РФ','card'],
  ['usdt','USDT','money'],
  ['lava','Lava.top','bolt'],
  ['ton','TON','ton'],
];
const WAL_M_LABEL = {card:'Карта РФ', usdt:'Крипта USDT', lava:'Lava.top', ton:'TON'};
const WAL_USD_RATE = 90; // курс прототипа для партнёрских выплат в $
/* суточный лимит вывода зависит от тарифа (лимиты по тарифу) */
const WAL_WD_LIMITS = {FREE:50000, START:100000, PRO:300000, BUSINESS:1000000, MAX:5000000};
const WAL_WD_DAY_LIMIT = 100000;        // дефолт (fallback без paywall-API)
function walTier(){
  if(typeof window.okoHasSub === 'function'){
    if(window.okoHasSub('MAX')) return 'MAX';
    if(window.okoHasSub('BUSINESS')) return 'BUSINESS';
    if(window.okoHasSub('PRO')) return 'PRO';
    if(window.okoHasSub('START')) return 'START';
    return 'FREE';
  }
  const t = (typeof PROFILE!=='undefined' && PROFILE.tier) ? String(PROFILE.tier).toUpperCase() : 'FREE';
  return WAL_WD_LIMITS[t] ? t : 'FREE';
}
function walWdLimit(){ return WAL_WD_LIMITS[walTier()] || WAL_WD_DAY_LIMIT; }
const WAL_NOCONF_LIMIT = 5000;          // покупки без подтверждения
const WAL_CARD_NUM = '2200 7007 1234 5566';
const WAL_USDT_ADDR = 'TQoKo4fHFYyeJtsDdD7TgKLxAV1mFJnEok';
const WAL_TON_ADDR = 'UQAoKoAppTonWa11etMockAddr9hfP2vXqLmTz4A';

/* доп-настройки кошелька (персист): автопродление, ПИН, суточный вывод */
const WAL_X = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-wallet-x'))||{}; }catch(e){ return {}; } })();
function walXSave(){ try{ localStorage.setItem('oko-wallet-x', JSON.stringify(WAL_X)); }catch(e){} }
function walProPrice(){ return (typeof PLANS!=='undefined' && PLANS.PRO) ? PLANS.PRO.mo : 4890; }
function walDMY(ts){ const d = new Date(ts); return String(d.getDate()).padStart(2,'0')+'.'+String(d.getMonth()+1).padStart(2,'0')+'.'+d.getFullYear(); }
function walDMYT(ts){ const d = new Date(ts); return walDMY(ts)+' '+String(d.getHours()).padStart(2,'0')+':'+String(d.getMinutes()).padStart(2,'0'); }
function walCopy(txt, msg){ try{ navigator.clipboard.writeText(txt); }catch(e){} toast(msg||'Скопировано'); }

/* ---------- категории операций (расход и доход) ---------- */
function walCat(why){
  if(/продвижен/i.test(why)) return 'Продвижение';
  if(/бирж/i.test(why)) return 'Биржа';
  if(/игр|рулетк|ставк|дорог|кости/i.test(why)) return 'Игры';
  if(/тариф|автопродлен|подписк/i.test(why)) return 'Тарифы';
  if(/вывод/i.test(why)) return 'Вывод средств';
  if(/пополнен/i.test(why)) return 'Пополнение';
  if(/партнёр|партнер|реф(ерал|\b)/i.test(why)) return 'Партнёрка';
  if(/стикер/i.test(why)) return 'Стикеры';
  if(/перевод/i.test(why)) return 'Переводы';
  return 'Прочее';
}
/* иконка категории для строки истории */
const WAL_CAT_IC = {
  'Продвижение':'rocket', 'Биржа':'briefcase', 'Игры':'dice', 'Тарифы':'crown',
  'Вывод средств':'card', 'Пополнение':'plus', 'Партнёрка':'users',
  'Стикеры':'sticker', 'Переводы':'send', 'Прочее':'money'
};
function walCatIc(why){ return WAL_CAT_IC[walCat(why)] || 'money'; }
/* заголовок дня для группировки истории */
const WAL_MONTHS = ['января','февраля','марта','апреля','мая','июня','июля','августа','сентября','октября','ноября','декабря'];
function walDayKey(at){ return new Date(at).toDateString(); }
function walDayLabel(at){
  const d = new Date(at), n = new Date();
  if(d.toDateString() === n.toDateString()) return 'Сегодня';
  const y = new Date(n - 864e5);
  if(d.toDateString() === y.toDateString()) return 'Вчера';
  return d.getDate() + ' ' + WAL_MONTHS[d.getMonth()] + (d.getFullYear() !== n.getFullYear() ? ' ' + d.getFullYear() : '');
}
function walWhen(at){
  const d = new Date(at), n = new Date();
  const hm = String(d.getHours()).padStart(2,'0') + ':' + String(d.getMinutes()).padStart(2,'0');
  if(d.toDateString() === n.toDateString()) return 'сегодня ' + hm;
  const y = new Date(n - 864e5);
  if(d.toDateString() === y.toDateString()) return 'вчера ' + hm;
  return String(d.getDate()).padStart(2,'0') + '.' + String(d.getMonth()+1).padStart(2,'0') + ' ' + hm;
}

/* ---------- рендер экрана ---------- */
let walShownBal = null;
function walAnimateBalance(){
  const el = document.getElementById('walBalance');
  if(!el) return;
  const target = WALLET.balance;
  const from = walShownBal === null ? target : walShownBal;
  walShownBal = target;
  const t0 = performance.now(), dur = 550;
  (function step(t){
    const k = Math.min(1, (t - t0) / dur), e = 1 - Math.pow(1 - k, 3);
    const v = Math.round(from + (target - from) * e);
    el.innerHTML = v.toLocaleString('ru-RU').replace(/,/g,' ') + ' <b>₽</b>';
    if(k < 1) requestAnimationFrame(step);
  })(t0);
}
function renderWallet(){
  const scr = document.getElementById('screen-wallet');
  if(!scr) return;
  const acc = document.getElementById('walAccNum'); if(acc) acc.textContent = WALLET.acc;
  const accA = document.getElementById('walAccNumAcc'); if(accA) accA.textContent = WALLET.acc;
  /* Последние 4 цифры номера счёта — в блок «•••• XXXX» карты (Сбер-стиль) */
  const last = document.getElementById('walAccLast');
  if(last){
    const digits = String(WALLET.acc).replace(/\D/g,'');
    last.textContent = digits.slice(-4).padStart(4, '0');
  }
  walApplyEye();          // применить скрытый режим баланса
  walAnimateBalance();
  const hold = document.getElementById('walHold');
  if(hold){
    hold.style.display = WALLET.hold > 0 ? 'inline-flex' : 'none';
    const span = hold.querySelector('span');
    if(span) span.innerHTML = 'В обработке: <b>' + fmtMoney(WALLET.hold) + '</b>';
  }
  walRenderCurrencies();  // мультивалютная лента (в подстранице «Мои счета»)
  walRenderGoals();       // финансовые цели (в подстранице «Цели»)
  walRenderAutoRules();   // правила автопополнения
  walRenderStats();
  walRenderTopInc();      // топ-3 источника дохода
  walRenderPie();         // пирог расходов
  walRenderCats();
  walRenderLedger();
  walUpdateChips();
  walRenderAutopay();
  walRenderSafety();
  walRenderPlanned();
  walRenderSec();
  walDrawChart(true);
  walUpdateAvg();
  w2UpdateMainMeta();     // хлебные крошки в меню + XP + плашка про крипту
  /* демо-нотификация о входящем платеже — 1 раз за сессию, только owner */
  walMaybeDemoLive();
}

/* --- обновление подписей в главном меню (счета/автоплатежи/цели/безопасность) + XP + крипто-плашка --- */
function w2UpdateMainMeta(){
  /* XP-число */
  const xp = (WAL_X.balances && WAL_X.balances.XP) || 0;
  const xpEl = document.getElementById('walXpNum');
  if(xpEl) xpEl.textContent = Math.round(xp).toLocaleString('ru-RU').replace(/,/g,' ');
  /* подпись «Мои счета»: собираем валюты с ненулевым балансом или дефолт */
  const accSub = document.getElementById('walMenuAccSub');
  if(accSub){
    const codes = ['RUB','USDT_TON','USDT_TRC','TON'];
    const active = codes.filter(c => walCurBal(c) > 0).map(c => WAL_CUR_META[c].sym);
    accSub.textContent = active.length ? active.join(', ') + ' — 4 счёта' : 'Рубли, TON, USDT · открой валюту';
  }
  /* подпись «Автоплатежи»: сколько активных правил + PRO */
  const autoSub = document.getElementById('walMenuAutoSub');
  if(autoSub){
    const rules = (WAL_X.autoRules || []).filter(r => r.on).length;
    const pro   = WAL_X.autopay ? 'PRO автопродление' : null;
    const parts = [];
    if(pro)   parts.push(pro);
    if(rules) parts.push(rules + ' правил' + (rules === 1 ? 'о' : ''));
    autoSub.textContent = parts.length ? parts.join(' · ') : 'Пока не настроено';
  }
  /* подпись «Цели» */
  const goalsSub = document.getElementById('walMenuGoalsSub');
  if(goalsSub){
    const n = (WAL_X.goals || []).length;
    goalsSub.textContent = n ? n + ' актив' + (n === 1 ? 'ная' : 'ных') + ' · копи под мечту' : 'Заведи первую копилку';
  }
  /* подпись «Безопасность»: сколько мер включено */
  const secSub = document.getElementById('walMenuSecSub');
  if(secSub){
    const on = [];
    if(WAL_X.pin) on.push('ПИН');
    if(WAL_X.bio) on.push('Face-ID');
    secSub.textContent = on.length ? on.join(' · ') + ' активны' : 'ПИН, Face-ID, лимиты';
  }
  /* крипто-плашка в «Мои счета» — честно про внешний кошелёк */
  const cp = document.getElementById('walCryptoPlate');
  if(cp){
    if(WAL_X.ton){
      cp.innerHTML = '<b>TON-кошелёк подключён.</b> Токены зачисляются напрямую с внешнего Tonkeeper / TON Wallet. USDT-TRC20 — сеть Tron, тот же способ через централизованный обменник.';
    } else {
      cp.innerHTML = '<b>Крипта — только с внешним кошельком.</b> Пока TON-кошелёк не подключён, вкладки TON и USDT показывают ноль. Подключить в разделе «Переводы» → «На крипто-адрес» (Tonkeeper, TON Wallet, MyTonWallet).';
    }
  }
  /* crypto sub в переводах */
  const trs = document.getElementById('w2TransferCryptoSub');
  if(trs) trs.textContent = WAL_X.ton ? 'Кошелёк подключён · комиссия сети' : 'Требуется подключить внешний кошелёк';
}

/* ---------- СТАТИСТИКА ДОХОДОВ/РАСХОДОВ за 30 дней ---------- */
function walCountUp(el, to, dur){
  if(!el) return;
  const reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion:reduce)').matches;
  if(reduce){ el.textContent = fmtMoney(to); return; }
  const from = 0, t0 = performance.now(), D = dur || 700;
  (function step(t){
    const k = Math.min(1, (t - t0) / D), e = 1 - Math.pow(1 - k, 3);
    el.textContent = fmtMoney(Math.round(from + (to - from) * e));
    if(k < 1) requestAnimationFrame(step);
  })(t0);
}
function walRenderStats(){
  const box = document.getElementById('walStats');
  if(!box) return;
  const since = Date.now() - 30 * 864e5;
  let inc = 0, exp = 0, part = 0, incN = 0, expN = 0;
  WALLET.ledger.forEach(o=>{
    if(o.at < since) return;
    if(o.t === '+'){ inc += o.sum; incN++; if(walCat(o.why) === 'Партнёрка') part += o.sum; }
    else { exp += o.sum; expN++; }
  });
  const net = inc - exp, mx = Math.max(inc, exp, 1);
  const wIn = (inc / mx * 100).toFixed(1), wOut = (exp / mx * 100).toFixed(1);
  const netUp = net >= 0;
  box.innerHTML = `
    <div class="card wal-stat-card">
      <div class="wal-stat-net">
        <div class="wal-stat-net-l"><span>Чистыми за 30 дней</span><b class="${netUp?'in':'out'}" id="walStatNet">0 ₽</b></div>
        <span class="wal-stat-net-chip ${netUp?'in':'out'}">${netUp?'прибыль':'минус'}</span>
      </div>
      <div class="wal-stat-rows">
        <div class="wal-stat-row">
          <div class="wal-stat-top"><span class="wal-stat-k"><i class="wal-stat-dot in"></i>Доходы<em>${incN}</em></span><b class="in" id="walStatIn">0 ₽</b></div>
          <div class="wal-stat-track"><i class="in" style="width:${wIn}%"></i></div>
        </div>
        <div class="wal-stat-row">
          <div class="wal-stat-top"><span class="wal-stat-k"><i class="wal-stat-dot out"></i>Расходы<em>${expN}</em></span><b class="out" id="walStatOut">0 ₽</b></div>
          <div class="wal-stat-track"><i class="out" style="width:${wOut}%"></i></div>
        </div>
      </div>
      ${part > 0 ? `<div class="wal-stat-part">${I('users')}<span>Партнёрские начисления</span><b>+ ${fmtMoney(part)}</b></div>` : ''}
    </div>`;
  requestAnimationFrame(()=>{
    walCountUp(document.getElementById('walStatNet'), net, 800);
    walCountUp(document.getElementById('walStatIn'), inc, 800);
    walCountUp(document.getElementById('walStatOut'), exp, 800);
  });
}

/* ---------- ГРАФИК БАЛАНСА за 30 дней (восстановление по леджеру) ---------- */
function walSeries(days){
  const now = Date.now(), day = 864e5, pts = [];
  for(let i = days; i >= 0; i--){
    const T = now - i * day;
    let b = WALLET.balance;
    for(const op of WALLET.ledger){
      if(op.at > T) b += op.t === '+' ? -op.sum : op.sum; // откатываем более поздние операции
    }
    pts.push(b);
  }
  return pts;
}
let walChartRAF = 0;
let walChartDays = 30;                     // текущий период мини-графика 7 / 30
function walSetChartPeriod(d){
  walChartDays = Number(d) || 30;
  document.querySelectorAll('#walChartPeriod button').forEach(b=>b.classList.toggle('on', +b.dataset.cp === walChartDays));
  walDrawChart(true);
  walUpdateAvg();
}
function walDrawChart(animate){
  const cv = document.getElementById('walChart');
  if(!cv) return;
  if(walChartRAF){ cancelAnimationFrame(walChartRAF); walChartRAF = 0; }
  requestAnimationFrame(()=>{
    const W = cv.clientWidth, H = 66;
    if(!W) return;
    const dpr = window.devicePixelRatio || 1;
    cv.width = Math.round(W * dpr); cv.height = Math.round(H * dpr);
    const ctx = cv.getContext('2d');
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    const pts = walSeries(walChartDays), n = pts.length;
    const min = Math.min.apply(null, pts), max = Math.max.apply(null, pts);
    const span = (max - min) || Math.max(Math.abs(max), 100) * 0.25;
    const lo = min - span * 0.12, hi = max + span * 0.12;
    const X = i => 3 + i / (n - 1) * (W - 6);
    const base = H - 5;                         // ось для «роста»
    const Yfull = v => base - (v - lo) / (hi - lo) * (H - 12);
    const light = document.documentElement.dataset.theme === 'light';
    const accent = (getComputedStyle(document.documentElement).getPropertyValue('--accent') || '').trim() || '#9AFF00';
    const reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion:reduce)').matches;

    function frame(prog){
      // растём от оси вверх: Y интерполируется от base к финальному
      const Y = v => base + (Yfull(v) - base) * prog;
      ctx.clearRect(0, 0, W, H);
      /* сетка */
      ctx.strokeStyle = light ? 'rgba(0,0,0,.09)' : 'rgba(255,255,255,.08)';
      ctx.lineWidth = 1; ctx.setLineDash([3,5]);
      [0.22, 0.78].forEach(k=>{ ctx.beginPath(); ctx.moveTo(2, H*k); ctx.lineTo(W-2, H*k); ctx.stroke(); });
      ctx.setLineDash([]);
      const path = ()=>{
        ctx.beginPath();
        ctx.moveTo(X(0), Y(pts[0]));
        for(let i = 1; i < n - 1; i++)
          ctx.quadraticCurveTo(X(i), Y(pts[i]), (X(i)+X(i+1))/2, (Y(pts[i])+Y(pts[i+1]))/2);
        ctx.quadraticCurveTo(X(n-1), Y(pts[n-1]), X(n-1), Y(pts[n-1]));
      };
      /* градиент-заливка */
      path();
      ctx.lineTo(X(n-1), H); ctx.lineTo(X(0), H); ctx.closePath();
      const g = ctx.createLinearGradient(0, 0, 0, H);
      g.addColorStop(0, light ? `rgba(83,168,0,${.28*prog})` : `rgba(154,255,0,${.30*prog})`);
      g.addColorStop(1, 'rgba(154,255,0,0)');
      ctx.fillStyle = g; ctx.fill();
      /* линия */
      path();
      ctx.strokeStyle = accent; ctx.lineWidth = 2;
      ctx.lineJoin = 'round'; ctx.lineCap = 'round';
      if(!light){ ctx.shadowColor = 'rgba(154,255,0,.55)'; ctx.shadowBlur = 7; }
      ctx.stroke();
      ctx.shadowBlur = 0;
      /* точка «сейчас» */
      ctx.beginPath(); ctx.arc(X(n-1), Y(pts[n-1]), 3.2, 0, Math.PI*2);
      ctx.fillStyle = accent; ctx.fill();
      if(prog > 0.98){
        ctx.beginPath(); ctx.arc(X(n-1), Y(pts[n-1]), 5.6, 0, Math.PI*2);
        ctx.strokeStyle = light ? 'rgba(83,168,0,.35)' : 'rgba(154,255,0,.35)'; ctx.lineWidth = 1.4; ctx.stroke();
      }
    }

    if(animate && !reduce){
      const t0 = performance.now(), D = 780;
      (function run(t){
        const k = Math.min(1, (t - t0) / D), e = 1 - Math.pow(1 - k, 3);
        frame(e);
        if(k < 1) walChartRAF = requestAnimationFrame(run); else walChartRAF = 0;
      })(t0);
    } else {
      frame(1);
    }

    /* чип динамики */
    const dEl = document.getElementById('walDelta');
    if(dEl){
      const diff = pts[n-1] - pts[0];
      const pct = pts[0] ? Math.round(diff / Math.abs(pts[0]) * 100) : 0;
      const up = diff >= 0;
      dEl.className = 'wal-delta ' + (up ? 'in' : 'out');
      dEl.innerHTML = `<svg viewBox="0 0 100 100" style="${up?'':'transform:rotate(180deg)'}"><path d="M20 66 50 34 80 66" fill="none" stroke="currentColor" stroke-width="14" stroke-linecap="round" stroke-linejoin="round"/></svg>${up?'+':'−'} ${fmtMoney(Math.abs(diff))}${pct ? ' · ' + (up?'+':'−') + Math.abs(pct) + '%' : ''}`;
    }
  });
}

function walRenderCats(){
  const bar = document.getElementById('walCatBar'), wrap = document.getElementById('walCats');
  if(!bar || !wrap) return;
  const sums = {};
  WALLET.ledger.forEach(op=>{ if(op.t==='-') sums[walCat(op.why)] = (sums[walCat(op.why)]||0) + op.sum; });
  const cats = Object.entries(sums).sort((a,b)=>b[1]-a[1]);
  if(!cats.length){
    bar.style.display = 'none';
    wrap.innerHTML = '<div class="wal-cats-empty">Расходов пока нет, всё в плюсе</div>';
    return;
  }
  bar.style.display = 'flex';
  const total = cats.reduce((s,c)=>s+c[1],0);
  const CAT_COLORS = ['#9AFF00','#7ECBEB','#FFB84A','#FF7EA6','#A980FF','#4EE2B8','#FFDF5C','#FF5C5C'];
  bar.innerHTML = cats.map(([,v],i)=>
    `<i style="width:${total?(v/total*100).toFixed(1):0}%;background:${CAT_COLORS[i%CAT_COLORS.length]};animation-delay:${i*70}ms"></i>`).join('');
  wrap.innerHTML = cats.map(([k,v],i)=>{
    const pct = total ? Math.round(v / total * 100) : 0;
    const c = CAT_COLORS[i%CAT_COLORS.length];
    return `<span class="wal-cat" style="animation-delay:${i*60}ms"><span class="dot" style="background:${c};box-shadow:0 0 6px ${c}66"></span>${k}<b>${fmtMoney(v)}</b><span class="wal-cat-pct">${pct||'<1'}%</span></span>`;
  }).join('');
}
function walOpTime(at){
  const d = new Date(at);
  return String(d.getHours()).padStart(2,'0') + ':' + String(d.getMinutes()).padStart(2,'0');
}
function walRenderLedger(){
  const box = document.getElementById('walLedger');
  if(!box) return;
  const q = walSearch;
  const periodMs = walPeriod === 'all' ? 0 : Number(walPeriod) * 864e5;
  const since = periodMs ? Date.now() - periodMs : 0;
  const list = WALLET.ledger
    .filter(op => {
      if(walFilter === 'all')   return true;
      if(walFilter === 'in')    return op.t === '+';
      if(walFilter === 'out')   return op.t === '-';
      if(walFilter === 'part')  return walCat(op.why) === 'Партнёрка';
      if(walFilter === 'games') return walCat(op.why) === 'Игры';
      return true;
    })
    .filter(op => !since || op.at >= since)
    .filter(op => !q || (op.why + ' ' + walCat(op.why)).toLowerCase().includes(q))
    .slice().sort((a,b)=>b.at-a.at)
    .slice(0, 80);
  if(!list.length){
    /* полностью пустой леджер + нет фильтров = CTA «пополни счёт» (правка 29.07) */
    if(!q && !since && walFilter === 'all' && WALLET.ledger.length === 0){
      box.innerHTML = `<div class="w2-empty-cta">
        <div class="w2-empty-ic">${I('plus')}</div>
        <b>Операций пока нет</b>
        <span>Пополни счёт — и здесь появятся все входящие и исходящие платежи.</span>
        <button onclick="walOpenTopup()">${I('plus')} Пополнить счёт</button>
      </div>`;
      return;
    }
    let emptyMsg;
    if(q) emptyMsg = 'Ничего не найдено';
    else if(since) emptyMsg = 'За выбранный период операций нет';
    else if(walFilter==='in')    emptyMsg = 'Пополнений пока нет';
    else if(walFilter==='out')   emptyMsg = 'Списаний пока нет';
    else if(walFilter==='part')  emptyMsg = 'Партнёрских начислений пока нет';
    else if(walFilter==='games') emptyMsg = 'Игровых операций пока нет';
    else emptyMsg = 'Операций пока нет';
    box.innerHTML = `<div class="wal-empty">${I(q ? 'search' : 'file')}${emptyMsg}</div>`;
    return;
  }
  /* корневой значок направления поверх иконки категории */
  const badgeIn  = '<svg class="wal-op-dir" viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="12" stroke-linecap="round" stroke-linejoin="round"><path d="M70 34 34 70"/><path d="M36 40v30h30"/></svg>';
  const badgeOut = '<svg class="wal-op-dir" viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="12" stroke-linecap="round" stroke-linejoin="round"><path d="M32 68 68 32"/><path d="M40 32h28v28"/></svg>';
  /* группировка по дням */
  const groups = [];
  let cur = null;
  list.forEach(op=>{
    const k = walDayKey(op.at);
    if(!cur || cur.k !== k){ cur = {k, at: op.at, ops: [], net: 0}; groups.push(cur); }
    cur.ops.push(op);
    cur.net += op.t==='+' ? op.sum : -op.sum;
  });
  let idx = 0;
  box.innerHTML = groups.map(g=>{
    const netUp = g.net >= 0;
    const rows = g.ops.map(op=>{
      const d = idx++; const dir = op.t==='+';
      return `
      <div class="wal-op" role="button" tabindex="0" onclick="walOpenTx(${op.at})" style="animation-delay:${Math.min(d,12)*42}ms">
        <div class="wal-op-ic ${dir?'in':'out'}"><svg class="i"><use href="#i-${walCatIc(op.why)}"/></svg>${dir?badgeIn:badgeOut}</div>
        <div class="wal-op-b">
          <div class="wal-op-why">${esc(op.why)}</div>
          <div class="wal-op-t"><span class="wal-op-cat">${walCat(op.why)}</span> · ${walOpTime(op.at)}</div>
        </div>
        <div class="wal-op-sum ${dir?'in':'out'}">${dir?'+':'−'} ${fmtMoney(op.sum)}</div>
        <svg class="i wal-op-chev"><use href="#i-chev"/></svg>
      </div>`;
    }).join('');
    return `<div class="wal-day">
      <div class="wal-day-h"><span class="wal-day-l">${walDayLabel(g.at)}</span><span class="wal-day-net ${netUp?'in':'out'}">${netUp?'+':'−'} ${fmtMoney(Math.abs(g.net))}</span></div>
      ${rows}
    </div>`;
  }).join('');
}
function walSetFilter(f){
  walFilter = f;
  document.querySelectorAll('#walFilters button').forEach(b=>b.classList.toggle('on', b.dataset.f===f));
  walRenderLedger();
}
function walSetSearch(v){
  walSearch = (v || '').trim().toLowerCase();
  walRenderLedger();
}
function walSetPeriod(p){
  walPeriod = String(p);
  document.querySelectorAll('#walPeriod button').forEach(b=>b.classList.toggle('on', b.dataset.p===walPeriod));
  walRenderLedger();
}
function walCopyAcc(){
  walCopy(WALLET.acc, 'Номер счёта скопирован: ' + WALLET.acc);
}
function walScrollHistory(){
  const a = document.getElementById('walHistAnchor');
  if(a) a.scrollIntoView({behavior:'smooth', block:'start'});
}

/* ---------- АВТОПРОДЛЕНИЕ ПОДПИСКИ PRO ---------- */
function walRenderAutopay(){
  const sw = document.getElementById('walAutoSw'), sub = document.getElementById('walAutoSub');
  if(!sw || !sub) return;
  sw.classList.toggle('on', !!WAL_X.autopay);
  sub.textContent = WAL_X.autopay
    ? 'Следующее списание ' + walDMY(WAL_X.nextAt) + ' · ' + fmtMoney(walProPrice())
    : 'Выключено, тариф не продлевается сам';
}
function walToggleAutopay(){
  if(WAL_X.autopay){
    WAL_X.autopay = false; walXSave(); walRenderAutopay();
    toast('Автопродление PRO выключено');
    return;
  }
  const next = Date.now() + 30 * 864e5;
  showPopup({ico:'crown', title:'Автопродление PRO',
    body:'Тариф PRO — <b>' + fmtMoney(walProPrice()) + ' / мес</b>.<br>Первое автосписание с лицевого счёта — <b>' + walDMY(next) + '</b>.<br>Отключить можно в любой момент в кошельке.',
    actions:[
      {label:'Включить автопродление', onclick:()=>{
        WAL_X.autopay = true; WAL_X.nextAt = next; walXSave();
        walRenderAutopay(); toast('Автопродление PRO включено — списание ' + walDMY(next));
      }},
      {label:'Отмена', ghost:true}
    ]});
}
/* просроченное автопродление — списываем при запуске (мок реального крона) */
(function walAutoChargeDue(){
  if(!WAL_X.autopay || !WAL_X.nextAt || Date.now() < WAL_X.nextAt) return;
  const price = walProPrice();
  if(WALLET.balance >= price && walletCharge(price, 'Автопродление тарифа PRO')){
    okoEarn(price, 'Тарифы');
    WAL_X.nextAt = Date.now() + 30 * 864e5;
    toast('Тариф PRO продлён автоматически: ' + fmtMoney(price));
  } else {
    WAL_X.autopay = false;
    toast('Автопродление PRO: не хватило средств — выключено');
  }
  walXSave();
})();

/* ---------- ЛИМИТЫ И БЕЗОПАСНОСТЬ ---------- */
function walWdUsedToday(){
  return WAL_X.wdDay === new Date().toDateString() ? (WAL_X.wdSum || 0) : 0;
}
function walRenderSec(){
  const sub = document.getElementById('walSecWdSub'), bar = document.getElementById('walSecWdBar');
  const used = walWdUsedToday(), lim = walWdLimit(), tier = walTier();
  if(sub) sub.innerHTML = fmtMoney(used) + ' из ' + fmtMoney(lim) + ' за сегодня · <b class="wal-tier-tag">тариф ' + tier + '</b>';
  if(bar) bar.style.width = Math.min(100, used / lim * 100).toFixed(1) + '%';
  /* апселл лимита — если не максимальный тариф */
  const secB = sub ? sub.parentElement : null;
  let up = document.getElementById('walWdUpsell');
  if(secB){
    if(tier !== 'MAX'){
      if(!up){
        up = document.createElement('button');
        up.id = 'walWdUpsell'; up.type = 'button'; up.className = 'wal-upsell';
        up.onclick = walWdUpsell;
        secB.appendChild(up);
      }
      const nextTier = tier==='FREE'?'START':tier==='START'?'PRO':tier==='PRO'?'BUSINESS':'MAX';
      up.innerHTML = I('crown') + '<span>Лимит ' + fmtMoney(WAL_WD_LIMITS[nextTier]) + '/сутки на ' + nextTier + '</span>' + I('chev');
    } else if(up){ up.remove(); }
  }
  const psw = document.getElementById('walPinSw'), psub = document.getElementById('walPinSub');
  if(psw) psw.classList.toggle('on', !!WAL_X.pin);
  if(psub) psub.textContent = WAL_X.pin ? 'Включён — вывод только по ПИН-коду' : 'Выключен, включи для защиты вывода';
  const bsw = document.getElementById('walBioSw'), bsub = document.getElementById('walBioSub');
  if(bsw) bsw.classList.toggle('on', !!WAL_X.bio);
  if(bsub) bsub.textContent = WAL_X.bio ? 'Включён — быстрый вход по отпечатку / Face-ID' : 'Выключен, быстрая разблокировка кошелька';
}
function walWdUpsell(){
  const tier = walTier();
  const nextTier = tier==='FREE'?'START':tier==='START'?'PRO':tier==='PRO'?'BUSINESS':'MAX';
  if(typeof window.okoRequireSub === 'function'){
    window.okoRequireSub(nextTier, 'Выше тариф — выше суточный лимит вывода средств', ()=>{ renderWallet(); });
  } else if(typeof openPay === 'function'){
    openPay(nextTier==='MAX'?'BUSINESS':nextTier);
  }
}
function walTogglePin(){
  if(WAL_X.pin) walPinOpen('off', 'walPinView');
  else walPinOpen('set', 'walPinView');
}

/* ---------- ПИН-ПАД (мок 4 цифры, персист) ---------- */
let walPinCtx = null;
function walPinOpen(mode, targetId, onOk){
  walPinCtx = {mode, targetId, onOk: onOk || null, stage: 1, first: '', buf: ''};
  if(targetId === 'walPinView') openSheet('walPin');
  walPinRender();
}
function walPinRender(err){
  const c = walPinCtx; if(!c) return;
  const box = document.getElementById(c.targetId); if(!box) return;
  const T = {
    set1:    ['Придумай ПИН-код', '4 цифры — понадобится при каждом выводе средств'],
    set2:    ['Повтори ПИН-код', 'Ещё раз те же 4 цифры для подтверждения'],
    off:     ['Отключение ПИН-кода', 'Введи текущий ПИН-код'],
    confirm: ['Подтверди вывод', 'Введи ПИН-код, чтобы вывести средства'],
  };
  const [title, sub] = T[c.mode === 'set' ? 'set' + c.stage : c.mode];
  box.innerHTML = `
  <div class="wal-pin">
    <div class="wal-pin-lock">${I('lock')}</div>
    <h3 style="text-align:center">${title}</h3>
    <p class="dim" style="font-size:12px;text-align:center;margin-top:4px">${sub}</p>
    <div class="wal-pin-dots ${err ? 'err' : ''}" id="walPinDots">${[0,1,2,3].map(i=>`<span class="${i < c.buf.length ? 'on' : ''}"></span>`).join('')}</div>
    <div class="wal-pin-keys">
      ${[1,2,3,4,5,6,7,8,9].map(d=>`<button onclick="walPinKey(${d})">${d}</button>`).join('')}
      <button class="ghosty" onclick="walPinCancel()">Отмена</button>
      <button onclick="walPinKey(0)">0</button>
      <button class="ghosty" onclick="walPinBackspace()" aria-label="Стереть"><svg class="i"><use href="#i-back"/></svg></button>
    </div>
  </div>`;
}
function walPinDots(){
  const c = walPinCtx, d = document.getElementById('walPinDots');
  if(!c || !d) return;
  [...d.children].forEach((s,i)=>s.classList.toggle('on', i < c.buf.length));
}
function walPinKey(n){
  const c = walPinCtx; if(!c || c.buf.length >= 4) return;
  c.buf += String(n); walPinDots();
  if(c.buf.length === 4) setTimeout(walPinEval, 170);
}
function walPinBackspace(){
  const c = walPinCtx; if(!c) return;
  c.buf = c.buf.slice(0, -1); walPinDots();
}
function walPinErr(msg){
  const c = walPinCtx; if(!c) return;
  c.buf = '';
  toast(msg);
  walPinRender(true);
  setTimeout(()=>{ const d = document.getElementById('walPinDots'); if(d) d.classList.remove('err'); }, 450);
}
function walPinEval(){
  const c = walPinCtx; if(!c) return;
  const code = c.buf;
  if(c.mode === 'set'){
    if(c.stage === 1){ c.first = code; c.stage = 2; c.buf = ''; walPinRender(); return; }
    if(code === c.first){
      WAL_X.pin = code; walXSave(); walPinCtx = null;
      closeSheet(); walRenderSec();
      toast('ПИН-код на вывод установлен');
    } else { c.stage = 1; c.first = ''; walPinErr('ПИН-коды не совпали — начни заново'); }
    return;
  }
  if(c.mode === 'off'){
    if(code === WAL_X.pin){
      WAL_X.pin = null; walXSave(); walPinCtx = null;
      closeSheet(); walRenderSec();
      toast('ПИН-код отключён');
    } else walPinErr('Неверный ПИН-код');
    return;
  }
  if(c.mode === 'confirm'){
    if(code === WAL_X.pin){ const ok = c.onOk; walPinCtx = null; if(ok) ok(); }
    else walPinErr('Неверный ПИН-код');
  }
}
function walPinCancel(){
  const c = walPinCtx; walPinCtx = null;
  if(c && c.mode === 'confirm') walRenderWithdraw();
  else closeSheet();
}

/* ---------- баланс-чипы (хаб + профиль) ---------- */
function walMakeChip(id){
  if(document.getElementById(id)) return;
  const b = document.createElement('button');
  b.className = 'balance-chip'; b.id = id;
  b.innerHTML = `<svg class="i"><use href="#i-money"/></svg><span>${fmtMoney(WALLET.balance)}</span>`;
  b.onclick = ()=>showTab('wallet');
  return b;
}
function walInsertChips(){
  const heroTxt = document.querySelector('#maHero .ma-hero-txt');
  if(heroTxt && !document.getElementById('walChipHub')){
    const c = walMakeChip('walChipHub');
    if(c){ c.style.marginTop = '9px'; heroTxt.appendChild(c); }
  }
  const profTop = document.querySelector('#screen-profile .profile-top');
  if(profTop && !document.getElementById('walChipProf')){
    const c = walMakeChip('walChipProf');
    if(c){ c.style.margin = '0 0 14px'; profTop.insertAdjacentElement('afterend', c); }
  }
}
function walUpdateChips(){
  ['walChipHub','walChipProf'].forEach(id=>{
    const el = document.getElementById(id);
    if(el) el.querySelector('span').textContent = fmtMoney(WALLET.balance);
  });
}

/* ---------- псевдо-QR (детерминированный SVG-плейсхолдер) ---------- */
function walQrSvg(seed, size){
  let h = 2166136261;
  for(let i = 0; i < seed.length; i++){ h ^= seed.charCodeAt(i); h = Math.imul(h, 16777619) >>> 0; }
  const rnd = ()=>{ h ^= h << 13; h >>>= 0; h ^= h >>> 17; h ^= h << 5; h >>>= 0; return h / 4294967296; };
  const N = 21, finder = (r,c)=> (r < 8 && c < 8) || (r < 8 && c >= N-8) || (r >= N-8 && c < 8);
  let cells = '';
  for(let r = 0; r < N; r++) for(let c = 0; c < N; c++)
    if(!finder(r,c) && rnd() < 0.46) cells += `<rect x="${c}" y="${r}" width="1" height="1"/>`;
  const fp = (r,c)=>`<rect x="${c}" y="${r}" width="7" height="7" fill="#111"/><rect x="${c+1}" y="${r+1}" width="5" height="5" fill="#fff"/><rect x="${c+2}" y="${r+2}" width="3" height="3" fill="#111"/>`;
  return `<svg viewBox="-1.5 -1.5 24 24" width="${size}" height="${size}" shape-rendering="crispEdges" style="border-radius:8px;background:#fff">
    <rect x="-1.5" y="-1.5" width="24" height="24" fill="#fff"/>
    <g fill="#111">${cells}</g>${fp(0,0)}${fp(0,14)}${fp(14,0)}</svg>`;
}

/* ---------- реквизиты пополнения по способу ---------- */
function walTopReqHtml(m){
  if(m === 'card') return `
    <div class="wal-mockcard fade-in">
      <div class="wal-mc-row">
        <svg class="wal-mc-chip" viewBox="0 0 44 32"><rect x="1" y="1" width="42" height="30" rx="6" fill="none" stroke="currentColor" stroke-width="2.4"/><path d="M1 12h14M1 20h14M29 12h14M29 20h14M15 5v22M29 5v22" stroke="currentColor" stroke-width="2.4" fill="none"/></svg>
        <span class="wal-mc-sys">МИР · СБП</span>
      </div>
      <button class="wal-mc-num" onclick="walCopy(WAL_CARD_NUM,'Номер карты скопирован')">${WAL_CARD_NUM}${I('copy')}</button>
      <div class="wal-mc-bot"><span>OKO PAY · перевод по номеру</span><span>Т-Банк</span></div>
    </div>
    <div class="wal-req-qr fade-in">
      ${walQrSvg('oko-card-' + WALLET.acc, 88)}
      <div><b>QR для оплаты</b><span>Отсканируй в приложении банка — сумма и назначение платежа подставятся сами. Зачисление мгновенно.</span></div>
    </div>`;
  if(m === 'usdt') return `
    <div class="wal-req fade-in">
      <div class="wal-req-h">${I('money')}<span>Адрес USDT · сеть <b>TRC20</b></span></div>
      <button class="wal-addr" onclick="walCopy(WAL_USDT_ADDR,'Адрес USDT скопирован')"><span>${WAL_USDT_ADDR}</span>${I('copy')}</button>
      <p class="wal-req-warn">Отправляй только USDT в сети TRC20 — перевод в другой сети будет потерян. Курс фиксируется в момент зачисления.</p>
    </div>`;
  if(m === 'ton') return `
    ${walTonSurface('topup')}
    <div class="wal-req fade-in">
      <div class="wal-req-h">${I('ton')}<span>Адрес кошелька <b>TON</b></span></div>
      <button class="wal-addr" onclick="walCopy(WAL_TON_ADDR,'Адрес TON скопирован')"><span>${WAL_TON_ADDR}</span>${I('copy')}</button>
      <p class="wal-req-warn" style="color:var(--dim)">Переводи Toncoin из любого TON-кошелька — зачисление за 1–2 минуты по текущему курсу.</p>
    </div>`;
  return `
    <div class="wal-req fade-in">
      <div class="wal-req-h">${I('bolt')}<span>Оплата через Lava.top</span></div>
      <p class="wal-req-warn" style="color:var(--dim);margin-top:7px">Счёт откроется в защищённом шлюзе после нажатия «Пополнить» — карты любой страны, СБП и крипта.</p>
    </div>`;
}

/* ---------- пополнение ---------- */
function walOpenTopup(prefill){
  walTopupState = {sum: prefill || 1000, method:'card'};
  walRenderTopup();
  openSheet('walTopup');
}
function walRenderTopup(){
  const s = walTopupState;
  document.getElementById('walTopupView').innerHTML = `
    <h3>Пополнение счёта</h3>
    <p class="dim" style="font-size:12.5px;margin:-4px 0 12px">Счёт ${WALLET.acc} · зачисление мгновенно</p>
    <div class="wal-qs">${[500,1000,5000,10000].map(v=>`
      <button class="${s.sum===v?'on':''}" onclick="walTopupState.sum=${v};walRenderTopup()">${v>=1000?(v/1000)+'к':v}</button>`).join('')}</div>
    <input id="walTopSum" type="number" min="1" placeholder="Сумма, ₽" value="${s.sum||''}"
      oninput="walTopupState.sum=Math.max(0,Number(this.value)||0);walSyncTopupBtn()">
    <p style="font-weight:600;font-size:13px;margin:2px 0 8px">Способ пополнения</p>
    <div class="wal-methods">${WAL_METHODS.map(([k,l,ic])=>`
      <button class="wal-m ${s.method===k?'on':''}" onclick="walTopupState.method='${k}';walRenderTopup()">${I(ic)}<span>${l}</span></button>`).join('')}</div>
    <div id="walTopReq">${walTopReqHtml(s.method)}</div>
    <div style="height:14px"></div>
    <button class="btn" onclick="walDoTopup()"><svg class="i"><use href="#i-plus"/></svg> <span id="walTopBtnSum">Пополнить на ${fmtMoney(s.sum)}</span></button>
    <p class="dim" style="font-size:11px;text-align:center;margin-top:9px">Без комиссии. Платежи проходят через шлюз OKO.</p>`;
}
function walSyncTopupBtn(){
  const el = document.getElementById('walTopBtnSum');
  if(el) el.textContent = 'Пополнить на ' + fmtMoney(walTopupState.sum);
}
function walDoTopup(){
  const s = walTopupState;
  if(!s.sum || s.sum <= 0){ toast('Укажи сумму пополнения'); return; }
  const v = document.getElementById('walTopupView');
  v.innerHTML = `<div style="text-align:center;padding:22px 0">
    <div class="spin"></div><p style="font-weight:700;margin-top:14px">Создаём платёж…</p>
    <p class="dim" style="font-size:12px;margin-top:5px">${WAL_M_LABEL[s.method]} · защищённое соединение</p></div>`;
  setTimeout(()=>{
    walletAdd(s.sum, 'Пополнение · ' + WAL_M_LABEL[s.method]);
    v.innerHTML = `<div class="wal-ok-wrap">
      <div class="wal-ok">${I('check')}</div>
      <p style="font-weight:800;font-size:19px;margin-top:14px">+ ${fmtMoney(s.sum)}</p>
      <p class="dim" style="font-size:13px;margin-top:6px">Счёт пополнен через ${WAL_M_LABEL[s.method]}.<br>Баланс: ${fmtMoney(WALLET.balance)}</p>
      <div style="height:16px"></div>
      <button class="btn" onclick="closeSheet()">Отлично</button></div>`;
    renderWallet();
    walFlash('in');
    toast('Кошелёк пополнен на ' + fmtMoney(s.sum));
  }, 1100);
}

/* ---------- вывод (комиссия 2%, суточный лимит, ПИН) ---------- */
function walOpenWithdraw(){
  walWdState = {sum:0, method:'card'};
  walRenderWithdraw();
  openSheet('walWithdraw');
}
function walWdFee(sum){ return Math.round(sum * 0.02 * 100) / 100; }
function walRenderWithdraw(){
  const s = walWdState;
  document.getElementById('walWdView').innerHTML = `
    <h3>Вывод средств</h3>
    <p class="dim" style="font-size:12.5px;margin:-4px 0 12px">Доступно: <b style="color:var(--accent)">${fmtMoney(WALLET.balance)}</b> · лимит на сегодня: ${fmtMoney(Math.max(0, walWdLimit() - walWdUsedToday()))}</p>
    <input id="walWdSum" type="number" min="1" max="${WALLET.balance}" placeholder="Сумма вывода, ₽" value="${s.sum||''}"
      oninput="walWdState.sum=Math.max(0,Number(this.value)||0);walSyncWdCalc()">
    <p style="font-weight:600;font-size:13px;margin:2px 0 8px">Куда вывести</p>
    <div class="wal-methods">${WAL_METHODS.map(([k,l,ic])=>`
      <button class="wal-m ${s.method===k?'on':''}" onclick="walWdState.method='${k}';walRenderWithdraw()">${I(ic)}<span>${l}</span></button>`).join('')}</div>
    ${s.method==='ton' ? walTonSurface('withdraw') : ''}
    <div class="wal-fee" id="walWdCalc"></div>
    <button class="btn" onclick="walDoWithdraw()"><svg class="i"><use href="#i-card"/></svg> <span id="walWdBtn">Вывести</span></button>
    <p class="dim" style="font-size:11px;text-align:center;margin-top:9px">Комиссия вывода 2%. Поступление 1–3 рабочих дня.${WAL_X.pin ? ' Защищено ПИН-кодом.' : ''}</p>`;
  walSyncWdCalc();
}
function walSyncWdCalc(){
  const s = walWdState, fee = walWdFee(s.sum), get = Math.max(0, s.sum - fee);
  const c = document.getElementById('walWdCalc');
  if(c) c.innerHTML = `
    <div class="wal-fee-row"><span>Сумма вывода</span><b>${fmtMoney(s.sum)}</b></div>
    <div class="wal-fee-row"><span>Комиссия 2%</span><b style="color:var(--danger)">− ${fmtMoney(fee)}</b></div>
    <div class="wal-fee-row total"><span>К получению</span><b>${fmtMoney(get)}</b></div>`;
  const b = document.getElementById('walWdBtn');
  if(b) b.textContent = s.sum > 0 ? 'Вывести ' + fmtMoney(s.sum) : 'Вывести';
}
function walDoWithdraw(){
  const s = walWdState;
  if(!s.sum || s.sum <= 0){ toast('Укажи сумму вывода'); return; }
  if(s.sum > WALLET.balance){ toast('Сумма больше баланса — максимум ' + fmtMoney(WALLET.balance)); return; }
  if(s.method === 'ton' && !WAL_X.ton){ toast('Сначала подключи TON-кошелёк'); return; }
  const used = walWdUsedToday(), lim = walWdLimit();
  if(used + s.sum > lim){
    toast('Суточный лимит вывода ' + fmtMoney(lim) + ' (тариф ' + walTier() + ') — сегодня доступно ' + fmtMoney(Math.max(0, lim - used)) + '. Выше тариф — выше лимит.');
    return;
  }
  if(WAL_X.pin){ walPinOpen('confirm', 'walWdView', walExecWithdraw); return; }
  walExecWithdraw();
}
function walExecWithdraw(){
  const s = walWdState;
  const fee = walWdFee(s.sum), get = s.sum - fee;
  if(!walletCharge(s.sum, 'Вывод средств · ' + WAL_M_LABEL[s.method])){ return; }
  okoEarn(fee, 'Комиссия вывода');
  const today = new Date().toDateString();
  if(WAL_X.wdDay !== today){ WAL_X.wdDay = today; WAL_X.wdSum = 0; }
  WAL_X.wdSum += s.sum; walXSave();
  const v = document.getElementById('walWdView');
  v.innerHTML = `<div style="text-align:center;padding:22px 0">
    <div class="spin"></div><p style="font-weight:700;margin-top:14px">Оформляем заявку…</p></div>`;
  setTimeout(()=>{
    v.innerHTML = `<div class="wal-ok-wrap">
      <div class="wal-ok">${I('check')}</div>
      <p style="font-weight:800;font-size:19px;margin-top:14px">Заявка на вывод создана</p>
      <p class="dim" style="font-size:13px;margin-top:6px">${fmtMoney(get)} придут на ${WAL_M_LABEL[s.method]} в течение 1–3 дней.<br>Комиссия 2%: ${fmtMoney(fee)}</p>
      <div class="wal-tx-tl-h" style="justify-content:center">${I('clock')}<span>Статус вывода</span></div>
      <div style="text-align:left;max-width:300px;margin:0 auto">${walWdTimelineHtml(Date.now())}</div>
      <div style="height:14px"></div>
      <button class="btn" onclick="closeSheet()">Готово</button></div>`;
    renderWallet();
    walFlash('out');
    toast('Вывод ' + fmtMoney(s.sum) + ' оформлен');
  }, 900);
}

/* ---------- ВЫПИСКА: документ с печатью + скачивание .txt ---------- */
function walStmtTotals(){
  let inN = 0, inS = 0, outN = 0, outS = 0;
  WALLET.ledger.forEach(o=>{ if(o.t === '+'){ inN++; inS += o.sum; } else { outN++; outS += o.sum; } });
  return {inN, inS, outN, outS, net: inS - outS};
}
function walStmtHtml(){
  const ops = WALLET.ledger.slice().sort((a,b)=>b.at-a.at);
  const t = walStmtTotals(), now = Date.now();
  const from = ops.length ? ops[ops.length-1].at : now, to = ops.length ? ops[0].at : now;
  return `
  <div class="wal-st-head">
    <div class="wal-st-req">
      <b>${SEAL_REQ.fio}</b>
      <span>${SEAL_REQ.inn}</span>
      <span>${SEAL_REQ.brand} · ${SEAL_REQ.geo}</span>
    </div>
    <div class="wal-st-seal">${sealSvg(84)}</div>
  </div>
  <h1>Выписка по лицевому счёту</h1>
  <div class="wal-st-meta">
    <div><span>Лицевой счёт</span><b>${WALLET.acc}</b></div>
    <div><span>Владелец</span><b>${esc(PROFILE.name)} · @${esc(PROFILE.nick)}</b></div>
    <div><span>Период</span><b>${walDMY(from)} — ${walDMY(to)}</b></div>
    <div><span>Сформирована</span><b>${walDMYT(now)}</b></div>
  </div>
  <div class="wal-st-ops">${ops.map(o=>`
    <div class="wal-st-op">
      <span class="d">${walDMYT(o.at)}</span>
      <span class="w">${esc(o.why)}</span>
      <b class="${o.t==='+'?'in':'out'}">${o.t==='+'?'+':'−'} ${fmtMoney(o.sum)}</b>
    </div>`).join('')}</div>
  <div class="wal-st-tot">
    <div><span>Пополнения (${t.inN})</span><b class="in">+ ${fmtMoney(t.inS)}</b></div>
    <div><span>Списания (${t.outN})</span><b class="out">− ${fmtMoney(t.outS)}</b></div>
    <div class="net"><span>Итог за период</span><b>${t.net>=0?'+':'−'} ${fmtMoney(Math.abs(t.net))}</b></div>
    <div class="net"><span>Баланс на дату выписки</span><b>${fmtMoney(WALLET.balance)}</b></div>
    ${WALLET.hold > 0 ? `<div><span>В холде (эскроу)</span><b>${fmtMoney(WALLET.hold)}</b></div>` : ''}
  </div>
  <div class="wal-st-sign">
    <div class="wal-st-sig">${signatureImg(118)}<span>Ильясов Д. А. · ${walDMY(now)}</span></div>
    <span class="wal-st-note">Документ сформирован автоматически в OKO APP и подтверждает операции по лицевому счёту.</span>
  </div>`;
}
function walStmtText(){
  const ops = WALLET.ledger.slice().sort((a,b)=>b.at-a.at);
  const t = walStmtTotals(), now = Date.now();
  const from = ops.length ? ops[ops.length-1].at : now, to = ops.length ? ops[0].at : now;
  const hr = '='.repeat(58), sep = '-'.repeat(58), L = [];
  L.push(hr);
  L.push('        OKO APP — ВЫПИСКА ПО ЛИЦЕВОМУ СЧЁТУ');
  L.push(hr);
  L.push('Счёт:          ' + WALLET.acc);
  L.push('Владелец:      ' + PROFILE.name + ' (@' + PROFILE.nick + ')');
  L.push('Период:        ' + walDMY(from) + ' — ' + walDMY(to));
  L.push('Сформирована:  ' + walDMYT(now));
  L.push(sep);
  ops.forEach(o=>{
    L.push(walDMYT(o.at) + '  ' + (o.t === '+' ? '+' : '-') + fmtMoney(o.sum).padStart(13) + '  ' + o.why);
  });
  L.push(sep);
  L.push('Пополнений:    ' + t.inN + ' на сумму ' + fmtMoney(t.inS));
  L.push('Списаний:      ' + t.outN + ' на сумму ' + fmtMoney(t.outS));
  L.push('Итог периода:  ' + (t.net >= 0 ? '+' : '-') + fmtMoney(Math.abs(t.net)));
  L.push('Баланс:        ' + fmtMoney(WALLET.balance) + (WALLET.hold > 0 ? ' (в холде ' + fmtMoney(WALLET.hold) + ')' : ''));
  L.push(sep);
  L.push(SEAL_REQ.fio + ' · ' + SEAL_REQ.inn);
  L.push(SEAL_REQ.brand + ' · ' + SEAL_REQ.geo);
  L.push('Документ сформирован автоматически в OKO APP.');
  L.push(hr);
  return L.join('\n');
}
function walStmtHide(){
  const el = document.getElementById('walStmt');
  if(el) el.classList.remove('open');
}
function walOpenStatement(){
  const paper = document.getElementById('walStmtPaper');
  if(paper) paper.innerHTML = walStmtHtml();
  const el = document.getElementById('walStmt');
  if(el) el.classList.add('open');
  if(typeof nvPush === 'function') nvPush('wal-stmt', walStmtHide);
}
function walCloseStatement(){
  walStmtHide();
  if(typeof nvPop === 'function') nvPop('wal-stmt');
}
function walDownloadStatement(){
  const d = new Date();
  const name = 'OKO-выписка-' + WALLET.acc + '-' + d.getFullYear() + '-' +
    String(d.getMonth()+1).padStart(2,'0') + '-' + String(d.getDate()).padStart(2,'0') + '.txt';
  const blob = new Blob(['\ufeff' + walStmtText()], {type:'text/plain;charset=utf-8'});
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = name;
  document.body.appendChild(a); a.click(); a.remove();
  setTimeout(()=>{ try{ URL.revokeObjectURL(a.href); }catch(e){} }, 4000);
  toast('Выписка сохранена: ' + name);
}

/* ---------- CSV-экспорт операций (Excel/Numbers/Google Sheets) ---------- */
function walCsvCell(v){
  const s = String(v==null ? '' : v);
  return /[";\r\n]/.test(s) ? '"' + s.replace(/"/g,'""') + '"' : s;
}
function walDownloadCSV(){
  const ops = WALLET.ledger.slice().sort((a,b)=>b.at-a.at);
  const rows = [['Дата','Тип','Категория','Сумма, ₽','Описание']];
  ops.forEach(o=>{
    rows.push([
      walDMYT(o.at),
      o.t==='+' ? 'Пополнение' : 'Списание',
      walCat(o.why),
      (o.t==='+' ? '+' : '-') + o.sum,
      o.why
    ]);
  });
  const csv = rows.map(r=>r.map(walCsvCell).join(';')).join('\r\n');
  const d = new Date();
  const name = 'oko-wallet-' + d.getFullYear() + '-' +
    String(d.getMonth()+1).padStart(2,'0') + '-' + String(d.getDate()).padStart(2,'0') + '.csv';
  const blob = new Blob(['﻿' + csv], {type:'text/csv;charset=utf-8'});
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = name;
  document.body.appendChild(a); a.click(); a.remove();
  setTimeout(()=>{ try{ URL.revokeObjectURL(a.href); }catch(e){} }, 4000);
  toast('CSV сохранён: ' + name + ' (' + ops.length + ' операций)');
}

/* ---------- ПЛАНИРУЕМЫЕ СПИСАНИЯ (ближайшие подписки и автоплатежи) ---------- */
function walPlannedItems(){
  const now = new Date();
  const y = now.getFullYear();
  const nextAutopay = WAL_X.autopay && WAL_X.nextAt
    ? {ic:'crown', title:'Автопродление PRO', date: walDMY(WAL_X.nextAt), sum: walProPrice(), sub:'Списание с лицевого счёта'}
    : {ic:'crown', title:'Подписка PRO', date:'1 сентября ' + y, sum: walProPrice(), sub:'Продление тарифа, если включишь автопродление'};
  return [
    nextAutopay,
    {ic:'plus',    title:'Автопополнение баланса', date:'5 октября ' + y, sum: 3000, sub:'Регулярное пополнение с карты РФ'},
    {ic:'rocket',  title:'Продвижение канала',     date:'12 октября ' + y, sum: 349,  sub:'Продление кампании «Старт»'},
  ];
}
function walRenderPlanned(){
  const box = document.getElementById('walPlanned');
  if(!box) return;
  const items = walPlannedItems();
  const total = items.reduce((s,it)=>s+it.sum, 0);
  box.innerHTML = items.map((it,i)=>`
    <div class="wal-plan-row" style="animation-delay:${i*50}ms">
      <div class="wal-plan-ic">${I(it.ic)}</div>
      <div class="wal-plan-b">
        <b>${it.title}</b>
        <span>${it.sub}, ${it.date}</span>
      </div>
      <div class="wal-plan-sum">− ${fmtMoney(it.sum)}</div>
    </div>`).join('') + `
    <div class="wal-plan-total"><span>Итого запланировано</span><b>− ${fmtMoney(total)}</b></div>`;
}

/* ---------- УРОВЕНЬ БЕЗОПАСНОСТИ (виджет с прогресс-баром) ---------- */
function walSafetyChecks(){
  return [
    {ok: !!WAL_X.pin,   name:'ПИН-код на вывод'},
    {ok: !!WAL_X.bio,   name:'Вход по отпечатку / Face-ID'},
    {ok: !!WAL_X.twofa, name:'Двухфакторная защита входа'},
    {ok: !!WAL_X.phone, name:'Привязанный номер телефона'},
    {ok: true,          name:'Подтверждение крупных операций'},
    {ok: true,          name:'Проверка устройств входа'},
  ];
}
function walRenderSafety(){
  const box = document.getElementById('walSafety');
  if(!box) return;
  const list = walSafetyChecks();
  const ok = list.filter(c=>c.ok).length, tot = list.length;
  const pct = Math.round(ok/tot*100);
  const lvl = ok>=5 ? 'максимальный' : ok>=4 ? 'высокий' : ok>=3 ? 'средний' : ok>=2 ? 'базовый' : 'низкий';
  const cls = ok>=4 ? 'hi' : ok>=3 ? 'md' : 'lo';
  box.innerHTML = `
    <div class="wal-safe-h">
      <div class="wal-safe-ic ${cls}">${I('lock')}</div>
      <div class="wal-safe-t">
        <b>Уровень защиты: ${lvl}</b>
        <span>${ok} из ${tot} мер активны, нажми для советов</span>
      </div>
      <span class="wal-safe-score ${cls}">${ok}/${tot}</span>
    </div>
    <div class="wal-safe-bar"><i class="${cls}" style="width:${pct}%"></i></div>
    <div class="wal-safe-list">
      ${list.map(c=>`<span class="wal-safe-chip ${c.ok?'on':''}">${I(c.ok ? 'check' : 'lock')}${c.name}</span>`).join('')}
    </div>`;
}
function walSafetyTips(){
  const off = walSafetyChecks().filter(c=>!c.ok);
  let body;
  if(off.length){
    const tips = {
      'ПИН-код на вывод':               'Включи ПИН-код в блоке «Лимиты и безопасность» ниже, любой вывод потребует 4 цифры.',
      'Вход по отпечатку / Face-ID':    'Включи в блоке «Лимиты и безопасность» — быстрая разблокировка вместо ввода ПИН.',
      'Двухфакторная защита входа':     'В настройках профиля привяжи Telegram-подтверждение входа, второй фактор к паролю.',
      'Привязанный номер телефона':     'Добавь телефон в профиле, восстановление доступа и SMS-подтверждение крупных операций.',
    };
    body = '<b>Что усилить, по приоритету:</b><br>' +
      off.map((c,i)=>'<b>' + (i+1) + '.</b> ' + c.name + '<br><span style="color:var(--dim);font-size:12px">' + (tips[c.name] || 'Активировать в настройках профиля.') + '</span>').join('<br><br>') +
      '<br><br>Каждая мера снижает риск кражи денег и потери доступа к счёту.';
  } else {
    body = 'Все меры активны, кошелёк защищён по максимуму. Проверяй список активных сессий раз в месяц.';
  }
  if(typeof showPopup === 'function'){
    showPopup({ico:'lock', title:'Как усилить защиту счёта', body, actions:[{label:'Понятно'}]});
  } else {
    toast('Активно ' + (walSafetyChecks().length - off.length) + ' из ' + walSafetyChecks().length + ' мер защиты');
  }
}

/* ---------- WOW: вспышка-свечение баланса при денежной операции ---------- */
function walFlash(kind){
  const h = document.querySelector('#screen-wallet .wal-hero');
  if(!h) return;
  const reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion:reduce)').matches;
  if(reduce) return;
  h.classList.remove('wal-flash-in','wal-flash-out');
  void h.offsetWidth;                       // рефлоу — рестарт анимации
  h.classList.add(kind === 'out' ? 'wal-flash-out' : 'wal-flash-in');
  setTimeout(()=>h.classList.remove('wal-flash-in','wal-flash-out'), 900);
}

/* ---------- TON CONNECT: подключение внешнего TON-кошелька (персист) ---------- */
/* мок-адрес пользовательского кошелька (детерминированный от лицевого счёта) */
function walTonMyAddr(){
  let h = 2166136261; const seed = 'ton-' + WALLET.acc;
  for(let i = 0; i < seed.length; i++){ h ^= seed.charCodeAt(i); h = Math.imul(h, 16777619) >>> 0; }
  let s = '';
  for(let i = 0; i < 34; i++){ h ^= h << 13; h >>>= 0; h ^= h >>> 17; h ^= h << 5; h >>>= 0;
    s += 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789abcdefghijkmnopqrstuvwxyz'[h % 56]; }
  return 'UQ' + s;
}
function walTonReRender(){
  const tv = document.getElementById('sheet-walTopup');
  if(tv && tv.classList.contains('open')){ const r = document.getElementById('walTopReq'); if(r) r.innerHTML = walTopReqHtml(walTopupState.method); }
  const wv = document.getElementById('sheet-walWithdraw');
  if(wv && wv.classList.contains('open')) walRenderWithdraw();
}
function walTonConnect(){
  const btn = event && event.currentTarget;
  if(btn){ btn.innerHTML = '<span class="spin" style="width:20px;height:20px"></span><span><b>Подключаем…</b><em>TON Connect · подтверди в кошельке</em></span>'; }
  setTimeout(()=>{
    WAL_X.ton = walTonMyAddr(); walXSave();
    walTonReRender();
    toast('TON-кошелёк подключён');
  }, 700);
}
function walTonDisconnect(){
  WAL_X.ton = null; walXSave();
  walTonReRender();
  toast('TON-кошелёк отключён');
}
function walTonSurface(ctx){
  if(WAL_X.ton){
    const a = WAL_X.ton;
    return `<div class="wal-ton-conn fade-in">
      <div class="wal-ton-badge">${I('ton')}</div>
      <div class="wal-ton-b"><b>TON-кошелёк подключён</b><span>${a.slice(0,6)}…${a.slice(-6)}</span></div>
      <button class="wal-ton-x" onclick="walTonDisconnect()">Отключить</button>
    </div>`;
  }
  const sub = ctx === 'withdraw' ? 'Нужен для вывода Toncoin — Tonkeeper · Wallet · MyTonWallet'
                                 : 'Пополняй в один тап — Tonkeeper · Wallet · MyTonWallet';
  return `<button class="wal-ton-connect fade-in" type="button" onclick="walTonConnect()">${I('ton')}<span><b>Подключить TON-кошелёк</b><em>${sub}</em></span>${I('chev')}</button>`;
}

/* ---------- СТАТУС ВЫВОДА: живой таймлайн (мок реального процессинга) ---------- */
const WAL_WD_STAGES = [
  ['file', 'Заявка создана',        'Вывод оформлен в OKO'],
  ['lock', 'Проверка безопасности', 'Антифрод и сверка реквизитов'],
  ['card', 'Отправлено в банк',     'Платёж передан в платёжную систему'],
  ['check','Зачислено',             'Деньги у получателя'],
];
/* индекс текущего этапа: <этого done, ==active; 3 = полностью завершено */
function walWdStage(at){
  const el = Date.now() - at, M = 60000, H = 3600000, D = 864e5;
  if(el >= D) return 3;        // зачислено
  if(el >= 10 * M) return 2;   // отправлено в банк
  return 1;                    // проверка безопасности
}
function walWdDone(at){ return walWdStage(at) >= 3; }
function walWdTimelineHtml(at){
  const cur = walWdStage(at);
  const steps = WAL_WD_STAGES.map((s,i)=>{
    const isDone = i < cur || (i === 3 && cur === 3);
    const isActive = i === cur && cur < 3;
    const cls = isDone ? 'done' : isActive ? 'active' : '';
    const ic = isDone ? 'check' : s[0];
    let sub = s[2];
    if(i === 3 && !isDone) sub = 'Ожидается до ' + walDMY(at + 3 * 864e5);
    return `<div class="wal-tl-step ${cls}" style="animation-delay:${i*60}ms">
      <span class="wal-tl-dot"><svg class="i"><use href="#i-${ic}"/></svg></span>
      <span class="wal-tl-b"><b>${s[1]}</b><span>${sub}</span></span>
    </div>`;
  }).join('');
  return `<div class="wal-tl">${steps}</div>`;
}

/* ---------- ДЕТАЛИ ОПЕРАЦИИ (bottom-sheet, читает персист-леджер) ---------- */
function walOpId(at){ return 'OP-' + Number(at).toString(36).toUpperCase().slice(-8).padStart(8,'0'); }
function walBalanceAfter(op){
  let after = WALLET.balance;
  for(const o of WALLET.ledger){ if(o.at > op.at) after -= (o.t === '+' ? o.sum : -o.sum); }
  return after;
}
let walTxCur = null;
function walOpenTx(at){
  const op = WALLET.ledger.find(o => o.at === at);
  if(!op) return;
  walTxCur = op;
  walRenderTx(op);
  openSheet('walTx');
}
function walRenderTx(op){
  const box = document.getElementById('walTxView');
  if(!box) return;
  const dir = op.t === '+', cat = walCat(op.why), isTop = cat === 'Пополнение';
  const isWd = cat === 'Вывод средств';
  const wdDone = isWd ? walWdDone(op.at) : true;
  const after = walBalanceAfter(op);
  const statusHtml = isWd && !wdDone
    ? `<span class="wal-tx-status proc">${I('clock')}В обработке</span>`
    : `<span class="wal-tx-status">${I('check')}${isWd ? 'Зачислено' : 'Выполнено'}</span>`;
  box.innerHTML = `
  <div class="wal-tx">
    <div class="wal-tx-hero">
      <div class="wal-tx-ic ${dir?'in':'out'}"><svg class="i"><use href="#i-${walCatIc(op.why)}"/></svg></div>
      <div class="wal-tx-sum ${dir?'in':'out'}">${dir?'+':'−'} ${fmtMoney(op.sum)}</div>
      <div class="wal-tx-why">${esc(op.why)}</div>
      ${statusHtml}
    </div>
    ${isWd ? `<div class="wal-tx-tl-h">${I('clock')}<span>Статус вывода</span></div>${walWdTimelineHtml(op.at)}` : ''}
    <div class="wal-tx-rows">
      <div class="wal-tx-row"><span>Категория</span><b class="wal-tx-cat">${I(walCatIc(op.why))}${cat}</b></div>
      <div class="wal-tx-row"><span>Тип</span><b class="${dir?'':'out'}" style="color:${dir?'var(--accent)':'var(--text)'}">${dir?'Пополнение':'Списание'}</b></div>
      <div class="wal-tx-row"><span>Дата и время</span><b>${walDMYT(op.at)}</b></div>
      <div class="wal-tx-row"><span>Баланс после</span><b>${fmtMoney(after)}</b></div>
      <div class="wal-tx-row"><span>Номер операции</span><button class="wal-tx-id" onclick="walCopy('${walOpId(op.at)}','Номер операции скопирован')">${walOpId(op.at)}${I('copy')}</button></div>
    </div>
    <div class="wal-tx-acts">
      ${isTop ? `<button class="prim" onclick="closeSheet();walOpenTopup(${op.sum})"><svg class="i"><use href="#i-plus"/></svg>Повторить</button>` : ''}
      <button class="sec" onclick="walTxCopy()"><svg class="i"><use href="#i-copy"/></svg>Скопировать</button>
    </div>
  </div>`;
}
function walTxCopy(){
  const op = walTxCur; if(!op) return;
  const txt = 'OKO · операция ' + walOpId(op.at) + '\n' + (op.t==='+'?'+ ':'− ') + fmtMoney(op.sum) +
    '\n' + op.why + '\n' + walCat(op.why) + ' · ' + walDMYT(op.at) + '\nСчёт ' + WALLET.acc;
  walCopy(txt, 'Детали операции скопированы');
}

/* =========================================================================
   WALLET v2: MULTI-CURRENCY, EXCHANGE, SEND, RECEIVE (QR), GOALS,
   AUTO-TOP-UP, TOP-3 INCOME, PIE, AVG, EYE (HIDE BALANCE), FACE-ID
   ========================================================================= */

/* ---------- показать/скрыть баланс (глазик) ---------- */
function walToggleEye(){
  WAL_X.hidden = !WAL_X.hidden; walXSave();
  walApplyEye();
}
function walApplyEye(){
  const on = !!WAL_X.hidden;
  const eye = document.getElementById('walEye');
  if(eye){
    eye.classList.toggle('on', on);
    eye.innerHTML = on
      ? '<svg class="i" viewBox="0 0 100 100"><path d="M14 50s12-22 36-22 36 22 36 22-12 22-36 22S14 50 14 50z" fill="none" stroke="currentColor" stroke-width="7" stroke-linejoin="round"/><path d="M14 14l72 72" stroke="currentColor" stroke-width="7" stroke-linecap="round"/></svg>'
      : '<svg class="i"><use href="#i-eye"/></svg>';
  }
  const bal = document.getElementById('walBalance');
  if(bal) bal.classList.toggle('hidden', on);
  document.querySelectorAll('.wal-cur-bal, .wal-cur-eq').forEach(el=>el.classList.toggle('hidden', on));
}

/* ---------- КУРСЫ и валютные балансы ---------- */
const WAL_RATES = {                          // курс к рублю (демо, фиксирован)
  USDT_TON: 95,    // 1 USDT = 95 ₽
  USDT_TRC: 95,    // одинаковый USDT, разные сети
  TON:      380,   // 1 TON  = 380 ₽
  XP:       0.5,   // 1 XP   = 0.5 ₽ (внутренние баллы)
};
const WAL_CUR_META = {
  RUB:      {code:'RUB',      name:'Рубли',     sub:'Лицевой счёт',          cls:'rub',      ic:'money',   sym:'₽',    dec:0},
  USDT_TON: {code:'USDT-TON', name:'USDT · TON', sub:'Стейбл в сети TON',    cls:'usdt-ton', ic:'usdt',    sym:'USDT', dec:2},
  USDT_TRC: {code:'USDT-TRC', name:'USDT · TRC20',sub:'Стейбл в сети Tron',  cls:'usdt-trc', ic:'usdt',    sym:'USDT', dec:2},
  TON:      {code:'TON',       name:'Toncoin',   sub:'Криптовалюта TON',     cls:'ton',      ic:'ton',     sym:'TON',  dec:2},
  XP:       {code:'XP',        name:'Звёзды XP', sub:'Внутренняя валюта',    cls:'xp',       ic:'xp',      sym:'XP',   dec:0},
};
/* стартовые балансы — демо только для владельца, гостям — по нулям (правка 29.07) */
if(!WAL_X.balances){
  /* Все валютные счета стартуют с нуля. Демо-остатки владельца (42.15 USDT,
     12.4 TON, 8 420 XP) удалены 09.08 — токены приходят только с внешнего
     кошелька, XP начисляется за реальную активность. */
  WAL_X.balances = {USDT_TON: 0, USDT_TRC: 0, TON: 0, XP: 0};
  walXSave();
}
function walCurBal(code){ return code === 'RUB' ? WALLET.balance : (WAL_X.balances[code] || 0); }
function walCurSet(code, v){
  if(code === 'RUB'){ WALLET.balance = Math.max(0, v); walletSave(); return; }
  WAL_X.balances[code] = Math.max(0, v); walXSave();
}
function walCurRub(code, amt){
  if(code === 'RUB') return amt;
  return amt * (WAL_RATES[code] || 0);
}
function walCurFmt(code, v){
  const m = WAL_CUR_META[code];
  if(!m) return String(v);
  if(code === 'RUB') return fmtMoney(v);
  const val = m.dec ? (Math.round(v * 100) / 100).toLocaleString('ru-RU', {minimumFractionDigits: m.dec, maximumFractionDigits: m.dec})
                    : Math.round(v).toLocaleString('ru-RU').replace(/,/g,' ');
  return val + ' ' + m.sym;
}
/* лого-круг валюты (для карточки и обмена) */
function walCurLogoHtml(code, klass){
  const m = WAL_CUR_META[code];
  const inner = code === 'RUB' ? '₽' : (code === 'XP' ? I('xp') : I(m.ic));
  return `<span class="wal-cur-logo ${m.cls} ${klass||''}">${inner}</span>`;
}

/* ---------- РЕНДЕР ЛЕНТЫ МУЛЬТИВАЛЮТНЫХ КАРТОЧЕК (устаревшая ленты strip)
   Оставлен как no-op для совместимости — теперь валюты живут в подстранице
   «Мои счета» (walRenderAccounts). XP — отдельным блоком, не как валюта. */
function walRenderCurrencies(){
  /* обновляем сумму эквивалента и список счетов в подстранице */
  walRenderAccounts();
}
let walCurRAF = 0;

/* ---------- НОВОЕ: список валютных счетов (подстраница «Мои счета») ---------- */
function walRenderAccounts(){
  const list = document.getElementById('walAccList');
  const totEl = document.getElementById('walTotalEq');
  /* Общий эквивалент — сумма всех валют в рублях (без XP: XP не валюта) */
  const codes = ['RUB','USDT_TON','USDT_TRC','TON'];
  let totalRub = 0;
  codes.forEach(c => { totalRub += walCurRub(c, walCurBal(c)); });
  if(totEl){
    totEl.innerHTML = Math.round(totalRub).toLocaleString('ru-RU').replace(/,/g,' ') + ' <b>₽</b>';
  }
  if(!list) return;
  const tonConnected = !!WAL_X.ton;
  list.innerHTML = codes.map(code => {
    const m = WAL_CUR_META[code];
    const bal = walCurBal(code);
    const eq  = walCurRub(code, bal);
    const isCrypto = code !== 'RUB';
    const disabled = isCrypto && !tonConnected && bal === 0;
    if(disabled){
      return `<button class="w2-acc-row disabled" onclick="w2Close('accounts');w2Open('transfers')" aria-label="Открыть счёт ${m.name}">
        ${walCurLogoHtml(code)}
        <div class="w2-acc-body">
          <b>${m.name}</b>
          <em>Нужен внешний ${code === 'TON' ? 'TON' : (code === 'USDT_TON' ? 'TON' : 'TRC20')}-кошелёк</em>
        </div>
        <span class="w2-acc-cta">Открыть счёт</span>
      </button>`;
    }
    return `<button class="w2-acc-row" onclick="walOpenCurDetail('${code}')" aria-label="Открыть счёт ${m.name}">
      ${walCurLogoHtml(code)}
      <div class="w2-acc-body">
        <b>${m.name}</b>
        <em>${isCrypto ? '<span class="rate">1 ' + m.sym + ' = ' + fmtMoney(WAL_RATES[code]).replace(' ₽','') + ' ₽</span>' : 'Основной лицевой счёт'}</em>
      </div>
      <div class="w2-acc-sum">
        <b>${walCurFmt(code, bal)}</b>
        ${isCrypto && bal > 0 ? '<em>≈ ' + fmtMoney(eq) + '</em>' : ''}
      </div>
    </button>`;
  }).join('');
}

/* ---------- ДЕТАЛИ ВАЛЮТЫ (bottom-sheet) ---------- */
function walOpenCurDetail(code){
  const m = WAL_CUR_META[code];
  const bal = walCurBal(code);
  const eq  = walCurRub(code, bal);
  document.getElementById('walCurDetailView').innerHTML = `
    <div style="text-align:center;padding:6px 0 4px">
      ${walCurLogoHtml(code)}
      <h3 style="margin-top:12px">${m.name}</h3>
      <p class="dim" style="font-size:12px;margin-top:3px">${m.sub}</p>
      <div style="font-family:var(--font-display);font-size:36px;letter-spacing:.02em;margin:16px 0 4px;color:var(--accent)">${walCurFmt(code, bal)}</div>
      <div style="font-size:13px;color:var(--dim)">≈ ${fmtMoney(eq)} по курсу ${fmtMoney(WAL_RATES[code]).replace(' ₽','')} ₽ за 1 ${m.sym}</div>
    </div>
    <div style="height:18px"></div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
      <button class="btn" onclick="closeSheet();walExState={from:'${code}',to:'RUB',amount:0,editing:'from'};w2Open('exchange')"><svg class="i"><use href="#i-swap"/></svg> Продать за ₽</button>
      <button class="btn ghost" onclick="closeSheet();walExState={from:'RUB',to:'${code}',amount:0,editing:'from'};w2Open('exchange')"><svg class="i"><use href="#i-plus"/></svg> Купить за ₽</button>
    </div>
    <div style="height:8px"></div>
    <button class="btn ghost" onclick="closeSheet();walRecvState={sum:0,note:'',code:'${code}'};w2Open('receive')"><svg class="i"><use href="#i-qr"/></svg> Принять ${m.sym}</button>`;
  openSheet('walCurDetail');
}

/* ---------- ОБМЕН ВАЛЮТ ---------- */
let walExState = {from:'RUB', to:'USDT_TON', amount:0, editing:'from'};
function walOpenExchange(from, to){
  const codes = ['RUB','USDT_TON','USDT_TRC','TON','XP'];
  walExState.from = codes.includes(from) ? from : 'RUB';
  walExState.to   = codes.includes(to)   ? to   : (walExState.from === 'RUB' ? 'USDT_TON' : 'RUB');
  walExState.amount = 0; walExState.editing = 'from';
  walRenderExchange();
  openSheet('walExchange');
}
function walExRate(from, to){
  const fRub = from === 'RUB' ? 1 : WAL_RATES[from];
  const tRub = to   === 'RUB' ? 1 : WAL_RATES[to];
  return fRub / tRub;
}
function walExOut(){
  const s = walExState;
  return s.amount * walExRate(s.from, s.to);
}
function walExFee(){ return walExOut() * 0.005; }   // 0.5% спред
function walExFinal(){ return walExOut() - walExFee(); }
function walRenderExchange(){
  const s = walExState;
  const codes = ['RUB','USDT_TON','USDT_TRC','TON','XP'];
  const fromM = WAL_CUR_META[s.from], toM = WAL_CUR_META[s.to];
  const outVal = walExFinal();
  document.getElementById('walExView').innerHTML = `
    <h3>Обмен валют</h3>
    <p class="dim" style="font-size:12.5px;margin:-4px 0 12px">Мгновенный обмен внутри кошелька · спред 0.5%</p>
    <div class="wal-ex-swap">
      <div class="wal-ex-cell on">
        <span class="wal-ex-lbl">Отдаёте</span>
        ${walCurLogoHtml(s.from)}
        <div class="wal-ex-cell-mid">
          <div class="wal-ex-cell-code">${fromM.name}</div>
          <div class="wal-ex-cell-max">Доступно: ${walCurFmt(s.from, walCurBal(s.from))}</div>
        </div>
        <input id="walExAmt" type="number" min="0" step="any" placeholder="0" value="${s.amount||''}"
          oninput="walExState.amount=Math.max(0,Number(this.value)||0);walSyncExchange()">
      </div>
      <button class="wal-ex-arrow" onclick="walExSwap()" aria-label="Поменять местами"><svg class="i"><use href="#i-swap"/></svg></button>
      <div class="wal-ex-cell">
        <span class="wal-ex-lbl">Получаете</span>
        ${walCurLogoHtml(s.to)}
        <div class="wal-ex-cell-mid">
          <div class="wal-ex-cell-code">${toM.name}</div>
          <div class="wal-ex-cell-max">Курс: 1 ${fromM.sym} = ${walExRate(s.from, s.to).toFixed(6).replace(/\.?0+$/,'')} ${toM.sym}</div>
        </div>
        <input type="text" readonly value="${outVal ? walCurFmt(s.to, outVal).replace(' '+toM.sym,'') : ''}" placeholder="0">
      </div>
    </div>
    <p style="font-weight:600;font-size:12.5px;margin:14px 0 6px">Быстрый выбор пары</p>
    <div class="wal-ex-cur-pick">${codes.filter(c=>c!==s.from).map(c=>`
      <button class="${s.to===c?'on':''}" onclick="walExState.to='${c}';walRenderExchange()">
        ${walCurLogoHtml(c)}<span>${WAL_CUR_META[c].sym}</span>
      </button>`).join('')}</div>
    <div class="wal-ex-rate" id="walExRateBox"></div>
    <div style="height:8px"></div>
    <button class="btn" onclick="walDoExchange()"><svg class="i"><use href="#i-swap"/></svg> <span id="walExBtn">Обменять</span></button>
    <p class="dim" style="font-size:11px;text-align:center;margin-top:9px">Курс фиксируется в момент обмена. Комиссия сети OKO 0.5% уже учтена.</p>`;
  walSyncExchange();
}
function walSyncExchange(){
  const s = walExState, out = walExFinal(), toM = WAL_CUR_META[s.to];
  const box = document.getElementById('walExRateBox');
  if(box){
    box.innerHTML = s.amount > 0
      ? `Отдаёте <b>${walCurFmt(s.from, s.amount)}</b> → получите <em>${walCurFmt(s.to, out)}</em><br>Курс: 1 ${WAL_CUR_META[s.from].sym} = ${walExRate(s.from, s.to).toFixed(6).replace(/\.?0+$/,'')} ${toM.sym} · спред 0.5%`
      : 'Введи сумму, чтобы увидеть, сколько получишь';
  }
  const b = document.getElementById('walExBtn');
  if(b) b.textContent = s.amount > 0 ? `Обменять ${walCurFmt(s.from, s.amount)}` : 'Обменять';
}
function walExSwap(){
  const t = walExState.from; walExState.from = walExState.to; walExState.to = t;
  walExState.amount = 0;
  walRenderExchange();
}
function walDoExchange(){
  const s = walExState;
  if(!s.amount || s.amount <= 0){ toast('Укажи сумму обмена'); return; }
  if(s.amount > walCurBal(s.from)){ toast('Недостаточно средств: доступно ' + walCurFmt(s.from, walCurBal(s.from))); return; }
  const out = walExFinal();
  const from = WAL_CUR_META[s.from], to = WAL_CUR_META[s.to];
  const v = document.getElementById('walExView');
  v.innerHTML = `<div style="text-align:center;padding:22px 0"><div class="spin"></div><p style="font-weight:700;margin-top:14px">Обмен…</p></div>`;
  setTimeout(()=>{
    /* списываем «от» */
    if(s.from === 'RUB'){
      walletCharge(s.amount, 'Обмен ' + from.sym + ' → ' + to.sym);
    } else {
      walCurSet(s.from, walCurBal(s.from) - s.amount);
    }
    /* начисляем «в» */
    if(s.to === 'RUB'){
      walletAdd(Math.round(out), 'Обмен ' + from.sym + ' → ₽');
    } else {
      walCurSet(s.to, walCurBal(s.to) + out);
      /* лог операции — рубль-эквивалент, чтобы он попал в статистику */
      WALLET.ledger.unshift({t:'-', sum: Math.round(walCurRub(s.from, s.amount) - walCurRub(s.to, out)),
        why: 'Спред обмена ' + from.sym + ' → ' + to.sym, at: Date.now()});
      walletSave();
    }
    v.innerHTML = `<div class="wal-ok-wrap">
      <div class="wal-ok">${I('check')}</div>
      <p style="font-weight:800;font-size:19px;margin-top:14px">${walCurFmt(s.to, out)}</p>
      <p class="dim" style="font-size:13px;margin-top:6px">Обмен выполнен: ${walCurFmt(s.from, s.amount)} → ${walCurFmt(s.to, out)}<br>Новый баланс ${to.sym}: <b>${walCurFmt(s.to, walCurBal(s.to))}</b></p>
      <div style="height:16px"></div>
      <button class="btn" onclick="closeSheet()">Отлично</button></div>`;
    renderWallet();
    walFlash(s.to === 'RUB' ? 'in' : 'out');
    toast('Обмен: ' + walCurFmt(s.from, s.amount) + ' → ' + walCurFmt(s.to, out));
  }, 900);
}

/* ---------- ОТПРАВКА ДРУГОМУ ПОЛЬЗОВАТЕЛЮ ---------- */
const WAL_CONTACTS = []  /* демо-данные удалены 09.08: контакты для переводов — источник только API */;
let walSendState = {to:'', name:'', sum:0, note:''};
function walOpenSend(nickPrefill){
  walSendState = {to: nickPrefill||'', name:'', sum:0, note:''};
  walRenderSend();
  openSheet('walSend');
}
function walRenderSend(){
  const s = walSendState;
  const contactChip = c => `
    <button class="wal-send-qi" onclick="walSendState.to='${c.nick}';walSendState.name='${esc(c.name)}';walRenderSend()">
      <span class="wal-send-av">${c.name.charAt(0)}</span><span>${esc(c.name.split(' ')[0])}</span>
    </button>`;
  document.getElementById('walSendView').innerHTML = `
    <h3>Отправить деньги</h3>
    <p class="dim" style="font-size:12.5px;margin:-4px 0 12px">Перевод внутри OKO — мгновенно и без комиссии</p>
    <div class="wal-send-to">
      <span class="wal-send-av">${(s.name || s.to || '?').charAt(0).toUpperCase()}</span>
      <input placeholder="Ник получателя, например ktodaniel" value="${esc(s.to)}"
        oninput="walSendState.to=this.value.replace(/^@/,'').trim();walSyncSendBtn()">
    </div>
    <p style="font-weight:600;font-size:12px;margin:2px 0 6px;color:var(--dim);letter-spacing:.06em;text-transform:uppercase">Недавние</p>
    <div class="wal-send-quick">${WAL_CONTACTS.map(contactChip).join('')}</div>
    <input class="wal-send-note" placeholder="Комментарий, например «за монтаж»"
      value="${esc(s.note)}" maxlength="80" oninput="walSendState.note=this.value">
    <div class="wal-qs">${[100,500,1000,2000].map(v=>`
      <button class="${s.sum===v?'on':''}" onclick="walSendState.sum=${v};walRenderSend()">${v>=1000?(v/1000)+'к':v}</button>`).join('')}</div>
    <input id="walSendSum" type="number" min="1" placeholder="Сумма, ₽" value="${s.sum||''}"
      oninput="walSendState.sum=Math.max(0,Number(this.value)||0);walSyncSendBtn()">
    <div style="height:6px"></div>
    <p class="dim" style="font-size:12px">Доступно: <b style="color:var(--accent)">${fmtMoney(WALLET.balance)}</b> · комиссии нет · получатель увидит перевод в чате.</p>
    <div style="height:12px"></div>
    <button class="btn" onclick="walDoSend()"><svg class="i"><use href="#i-send"/></svg> <span id="walSendBtn">Отправить</span></button>`;
  walSyncSendBtn();
}
function walSyncSendBtn(){
  const s = walSendState;
  const b = document.getElementById('walSendBtn');
  if(b) b.textContent = s.sum > 0 && s.to ? 'Отправить ' + fmtMoney(s.sum) : 'Отправить';
}
function walDoSend(){
  const s = walSendState;
  if(!s.to){ toast('Укажи ник получателя'); return; }
  if(!s.sum || s.sum <= 0){ toast('Укажи сумму перевода'); return; }
  if(s.sum > WALLET.balance){ toast('Недостаточно средств: доступно ' + fmtMoney(WALLET.balance)); return; }
  const why = 'Перевод в чате · @' + s.to + (s.note ? ' («' + s.note + '»)' : '');
  const v = document.getElementById('walSendView');
  v.innerHTML = `<div style="text-align:center;padding:22px 0"><div class="spin"></div><p style="font-weight:700;margin-top:14px">Отправляем…</p></div>`;
  setTimeout(()=>{
    walletCharge(s.sum, why);
    v.innerHTML = `<div class="wal-ok-wrap">
      <div class="wal-ok">${I('check')}</div>
      <p style="font-weight:800;font-size:19px;margin-top:14px">− ${fmtMoney(s.sum)}</p>
      <p class="dim" style="font-size:13px;margin-top:6px">Перевод @${esc(s.to)} доставлен.<br>Баланс: <b>${fmtMoney(WALLET.balance)}</b></p>
      <div style="height:16px"></div>
      <button class="btn" onclick="closeSheet()">Готово</button></div>`;
    renderWallet();
    walFlash('out');
    toast('Отправлено @' + s.to + ': ' + fmtMoney(s.sum));
  }, 800);
}

/* ---------- QR-ПРИЁМ (canvas) ---------- */
let walRecvState = {sum:0, note:'', code:'RUB'};
function walOpenReceive(code){
  walRecvState = {sum: 0, note:'', code: code || 'RUB'};
  walRenderReceive();
  openSheet('walReceive');
  setTimeout(walDrawRecvQR, 40);
}
function walRecvLink(){
  const s = walRecvState;
  const base = 'https://okoteam.top/pay';
  const p = new URLSearchParams();
  p.set('to', WALLET.acc);
  if(s.sum) p.set('sum', String(s.sum));
  if(s.note) p.set('note', s.note);
  if(s.code && s.code !== 'RUB') p.set('cur', s.code);
  return base + '?' + p.toString();
}
/* Контейнер приходит аргументом (правка 09.08).
   Раньше функция всегда искала #walRecvView по id, а таких элементов в
   документе ДВА: скрытый внутри шторки и настоящий на подстранице «Приём
   платежа». getElementById отдавал первый — шторку, и подстраница
   оставалась пустым чёрным экраном. */
function walRenderReceive(host){
  const s = walRecvState, m = WAL_CUR_META[s.code];
  const box = host || document.getElementById('walRecvView');
  if(!box) return;
  box.innerHTML = `
    <h3 style="margin-bottom:2px">Принять платёж</h3>
    <p class="dim" style="font-size:12.5px;margin:-2px 0 10px">Покажи QR — плательщик отсканирует и увидит счёт${s.code!=='RUB'?' в '+m.sym:''}</p>
    <div class="wal-recv">
      <div class="wal-recv-card">
        <canvas id="walRecvCanvas" width="420" height="420" aria-label="QR-код"></canvas>
        <div class="wal-recv-sum">${s.sum ? walCurFmt(s.code, s.sum) : (s.code==='RUB'?'Любая сумма':'Любое количество '+m.sym)}</div>
        <div class="wal-recv-sub">Счёт: <b>${WALLET.acc}</b>${s.note?'<br>«'+esc(s.note)+'»':''}</div>
        <div class="wal-recv-inputs">
          <input type="number" min="0" step="any" placeholder="Сумма ${m.sym}" value="${s.sum||''}"
            oninput="walRecvState.sum=Math.max(0,Number(this.value)||0);walRecvRefresh(this)">
          <input placeholder="Комментарий" maxlength="40" value="${esc(s.note)}"
            oninput="walRecvState.note=this.value;walRecvRefresh(this)">
        </div>
        <button class="wal-recv-link" onclick="walCopy(walRecvLink(),'Ссылка на приём скопирована')">
          <svg class="i"><use href="#i-copy"/></svg>
          <span id="walRecvLink">${esc(walRecvLink())}</span>
        </button>
      </div>
      <div class="wal-recv-share">
        <button class="prim" onclick="walShareReceive()"><svg class="i"><use href="#i-share"/></svg>Поделиться</button>
        <button class="sec" onclick="walCopy(walRecvLink(),'Ссылка скопирована')"><svg class="i"><use href="#i-copy"/></svg>Копировать</button>
      </div>
    </div>`;
}
/* Перерисовка QR и подписи строго в том экране, где человек сейчас находится
   (шторка и подстраница «Приём платежа» содержат одинаковую разметку). */
function walRecvRefresh(fromEl){
  const scope = (fromEl && fromEl.closest) ? fromEl.closest('.wal-recv-host, #walRecvView, .sheet, .w2-page') : null;
  walDrawRecvQR(scope);
  walRenderReceiveText(scope);
}
function walRenderReceiveText(scope){
  const root = scope || document;
  const el = root.querySelector ? root.querySelector('#walRecvLink, .wal-recv-link span') : null;
  if(el) el.textContent = walRecvLink();
  const sumEl = root.querySelector ? root.querySelector('.wal-recv-sum') : null;
  if(sumEl){
    const s = walRecvState, m = WAL_CUR_META[s.code];
    sumEl.textContent = s.sum ? walCurFmt(s.code, s.sum) : (s.code==='RUB'?'Любая сумма':'Любое количество '+m.sym);
  }
}
function walDrawRecvQR(scope){
  /* Канва тоже дублируется в двух местах — ищем строго в нужном контейнере. */
  const cv = (scope && scope.querySelector) ? scope.querySelector('#walRecvCanvas, .wal-recv-canvas')
                                            : document.getElementById('walRecvCanvas');
  if(!cv) return;
  const ctx = cv.getContext('2d');
  const W = cv.width, H = cv.height;
  ctx.fillStyle = '#fff'; ctx.fillRect(0,0,W,H);
  /* сеть 25x25 (генерируем «QR-подобный» узор детерминированно по ссылке) */
  const N = 25, cell = W / N;
  const seed = walRecvLink();
  let h = 2166136261;
  for(let i = 0; i < seed.length; i++){ h ^= seed.charCodeAt(i); h = Math.imul(h, 16777619) >>> 0; }
  const rnd = ()=>{ h ^= h << 13; h >>>= 0; h ^= h >>> 17; h ^= h << 5; h >>>= 0; return h / 4294967296; };
  const inFinder = (r,c)=> (r < 8 && c < 8) || (r < 8 && c >= N-8) || (r >= N-8 && c < 8);
  const inAlign  = (r,c)=> (r >= 17 && r <= 21 && c >= 17 && c <= 21);
  ctx.fillStyle = '#0a0a0a';
  for(let r = 0; r < N; r++) for(let c = 0; c < N; c++){
    if(inFinder(r,c) || inAlign(r,c)) continue;
    if(r === 6 || c === 6){                       // тайминг-линии
      if((r + c) % 2 === 0) ctx.fillRect(c*cell, r*cell, cell, cell);
      continue;
    }
    if(rnd() < 0.47) ctx.fillRect(c*cell, r*cell, cell, cell);
  }
  /* три finder-квадрата в углах */
  const fp = (r,c)=>{
    ctx.fillStyle = '#0a0a0a'; ctx.fillRect(c*cell, r*cell, 7*cell, 7*cell);
    ctx.fillStyle = '#fff';    ctx.fillRect((c+1)*cell, (r+1)*cell, 5*cell, 5*cell);
    ctx.fillStyle = '#0a0a0a'; ctx.fillRect((c+2)*cell, (r+2)*cell, 3*cell, 3*cell);
  };
  fp(0,0); fp(0, N-7); fp(N-7, 0);
  /* маленький alignment в правом-нижнем */
  ctx.fillStyle = '#0a0a0a'; ctx.fillRect(17*cell, 17*cell, 5*cell, 5*cell);
  ctx.fillStyle = '#fff';    ctx.fillRect(18*cell, 18*cell, 3*cell, 3*cell);
  ctx.fillStyle = '#0a0a0a'; ctx.fillRect(19*cell, 19*cell, cell, cell);
  /* лого OKO в центре — лайм-круг с O */
  const cx = W/2, cy = H/2, R = W * 0.13;
  ctx.fillStyle = '#fff'; ctx.beginPath(); ctx.arc(cx, cy, R + cell*0.7, 0, Math.PI*2); ctx.fill();
  ctx.fillStyle = '#9AFF00'; ctx.beginPath(); ctx.arc(cx, cy, R, 0, Math.PI*2); ctx.fill();
  ctx.fillStyle = '#000';
  ctx.font = 'bold ' + Math.round(R * 1.05) + 'px "Bebas Neue", Impact, sans-serif';
  ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
  ctx.fillText('O', cx, cy + R*0.06);
}
function walShareReceive(){
  const link = walRecvLink();
  const text = 'Прими платёж на OKO-счёт ' + WALLET.acc + (walRecvState.sum ? ' · ' + walCurFmt(walRecvState.code, walRecvState.sum) : '');
  if(navigator.share){
    navigator.share({title: 'Платёж OKO', text, url: link}).catch(()=>{});
  } else {
    walCopy(link, 'Ссылка на приём скопирована');
  }
}

/* ---------- ПИРОГ РАСХОДОВ (SVG donut) ---------- */
const WAL_PIE_COLORS = ['#9AFF00','#7ECBEB','#FFB84A','#FF7EA6','#A980FF','#4EE2B8','#FFDF5C','#FF5C5C'];
function walRenderPie(){
  const box = document.getElementById('walPie');
  if(!box) return;
  const sums = {};
  const since = Date.now() - 30 * 864e5;
  WALLET.ledger.forEach(op=>{ if(op.t==='-' && op.at >= since) sums[walCat(op.why)] = (sums[walCat(op.why)]||0) + op.sum; });
  const cats = Object.entries(sums).sort((a,b)=>b[1]-a[1]);
  if(!cats.length){
    box.innerHTML = `<div class="wal-pie-empty">Расходов за 30 дней нет — держи баланс в плюсе.</div>`;
    return;
  }
  const total = cats.reduce((s,c)=>s+c[1], 0);
  const R = 42, C = 2 * Math.PI * R;
  let acc = 0;
  const arcs = cats.map(([k,v],i)=>{
    const frac = v / total, len = C * frac;
    const el = `<circle cx="60" cy="60" r="${R}" fill="transparent"
      stroke="${WAL_PIE_COLORS[i%WAL_PIE_COLORS.length]}" stroke-width="16"
      stroke-dasharray="${len} ${C - len}" stroke-dashoffset="${-acc}"
      style="animation:walBarIn .7s cubic-bezier(.3,1,.4,1) ${i*70}ms both"/>`;
    acc += len;
    return el;
  }).join('');
  const list = cats.slice(0, 6).map(([k,v],i)=>{
    const pct = total ? Math.round(v / total * 100) : 0;
    return `<div class="wal-pie-row" style="animation-delay:${i*60}ms">
      <span class="wal-pie-dot" style="background:${WAL_PIE_COLORS[i%WAL_PIE_COLORS.length]}"></span>
      <span class="wal-pie-nm">${esc(k)}</span>
      <span class="wal-pie-pct">${pct||'<1'}%</span>
    </div>`;
  }).join('');
  box.innerHTML = `
    <div class="wal-pie">
      <svg viewBox="0 0 120 120">
        <circle cx="60" cy="60" r="${R}" fill="transparent" stroke="var(--raised)" stroke-width="16"/>
        ${arcs}
      </svg>
      <div class="wal-pie-center"><b>${fmtMoney(total)}</b><span>Всего 30 дней</span></div>
    </div>
    <div class="wal-pie-list">${list}</div>`;
}

/* ---------- ТОП-3 ИСТОЧНИКА ДОХОДА + СРЕДНЕЕ В ДЕНЬ ---------- */
function walRenderTopInc(){
  const box = document.getElementById('walTopInc');
  if(!box) return;
  const since = Date.now() - 30 * 864e5;
  const sums = {};
  let totalIn = 0;
  WALLET.ledger.forEach(op=>{
    if(op.t !== '+' || op.at < since) return;
    const c = walCat(op.why);
    sums[c] = (sums[c] || 0) + op.sum;
    totalIn += op.sum;
  });
  const cats = Object.entries(sums).sort((a,b)=>b[1]-a[1]).slice(0, 3);
  if(!cats.length){
    box.innerHTML = `<div class="wal-topinc-empty">За 30 дней доходов ещё нет — время монетизировать контент.</div>`;
    return;
  }
  const avg = Math.round(totalIn / 30);
  box.innerHTML = cats.map(([k,v],i)=>{
    const pct = totalIn ? Math.round(v / totalIn * 100) : 0;
    return `<div class="wal-topinc-r" style="animation-delay:${i*60}ms">
      <span class="wal-topinc-rank">${i+1}</span>
      <div class="wal-topinc-b">
        <b>${esc(k)}</b><span>${pct}% от всего дохода за 30 дней</span>
      </div>
      <span class="wal-topinc-sum">+ ${fmtMoney(v)}</span>
    </div>`;
  }).join('') + `
    <div class="wal-avg-chip">${I('users')}<span>В среднем в день:</span><b>+ ${fmtMoney(avg)}</b></div>`;
}

/* среднее в день под мини-графиком в hero */
function walUpdateAvg(){
  const el = document.getElementById('walAvg');
  if(!el) return;
  const days = walChartDays;
  const since = Date.now() - days * 864e5;
  let inc = 0, exp = 0;
  WALLET.ledger.forEach(o=>{
    if(o.at < since) return;
    if(o.t === '+') inc += o.sum; else exp += o.sum;
  });
  const net = inc - exp, avg = Math.round(net / days);
  const sign = avg >= 0 ? '+' : '−';
  el.innerHTML = `<svg viewBox="0 0 100 100"><path d="M20 66 50 34 80 66" fill="none" stroke="currentColor" stroke-width="14" stroke-linecap="round" stroke-linejoin="round"/></svg>
    В среднем в день: <b>${sign} ${fmtMoney(Math.abs(avg))}</b> · за ${days} дн: ${sign} ${fmtMoney(Math.abs(net))}`;
}

/* ---------- ФИНАНСОВЫЕ ЦЕЛИ ----------
   Демо-цели владельца («Копим на PRO», «Камера для съёмок» с уже
   отложенными 24 500 ₽) удалены 09.08. Раздел открывается пустым состоянием
   и первой целью, которую человек ставит сам. */
if(!WAL_X.goals){
  WAL_X.goals = [];
  walXSave();
}
function walRenderGoals(){
  const box = document.getElementById('walGoals');
  if(!box) return;
  const items = WAL_X.goals || [];
  /* Пустой экран с одной кнопкой выглядел как недоделка (правка 09.08).
     Даём человеку понять, зачем этот раздел вообще нужен. */
  if(!items.length){
    box.innerHTML = `
      <div class="wal-empty">
        <div class="wal-empty-ic">${I('target')}</div>
        <h3>Копилка под мечту</h3>
        <p>Поставь цель — камера, поездка, новый компьютер — и OKO будет откладывать
           понемногу с каждого поступления и показывать, сколько осталось.</p>
      </div>
      <button class="wal-goal-add" onclick="walOpenGoal()"><svg class="i"><use href="#i-plus"/></svg>Поставить первую цель</button>`;
    return;
  }
  box.innerHTML = items.map(g=>{
    const pct = Math.min(100, Math.round((g.saved || 0) / (g.target || 1) * 100));
    const done = pct >= 100;
    return `<div class="card wal-goal ${done?'done':''}" onclick="walOpenGoal('${g.id}')" role="button">
      <div class="wal-goal-top">
        <div class="wal-goal-ic">${I(g.ic || 'target')}</div>
        <div class="wal-goal-b"><b>${esc(g.name)}</b><span>${done?'Цель достигнута — забирай и трать':'Осталось накопить '+fmtMoney(Math.max(0, g.target - g.saved))}</span></div>
        <span class="wal-goal-pct">${pct}%</span>
      </div>
      <div class="wal-goal-bar"><i style="width:${pct}%"></i></div>
      <div class="wal-goal-foot">
        <span>Собрано <b>${fmtMoney(g.saved || 0)}</b></span>
        <span>Цель <b>${fmtMoney(g.target || 0)}</b></span>
      </div>
    </div>`;
  }).join('') + `
    <button class="wal-goal-add" onclick="walOpenGoal()"><svg class="i"><use href="#i-plus"/></svg>Добавить цель</button>`;
}
const WAL_GOAL_ICS = ['target','crown','rocket','photo','circle-play','bookmark','fire','globe'];
let walGoalEdit = null;
function walOpenGoal(id){
  const g = id ? (WAL_X.goals||[]).find(x=>x.id===id) : {id:'g_'+Math.random().toString(36).slice(2,7), name:'', target:0, saved:0, ic:'target'};
  walGoalEdit = Object.assign({_new: !id}, g);
  walRenderGoal();
  openSheet('walGoal');
}
function walRenderGoal(){
  const g = walGoalEdit; if(!g) return;
  document.getElementById('walGoalView').innerHTML = `
    <h3>${g._new ? 'Новая цель' : 'Цель · ' + esc(g.name || 'без имени')}</h3>
    <div class="wal-goal-form">
      <div>
        <span class="wal-autor-lbl">Название</span>
        <input class="wal-autor-inp" placeholder="Копим на новую камеру" value="${esc(g.name)}"
          oninput="walGoalEdit.name=this.value">
      </div>
      <div>
        <span class="wal-autor-lbl">Цель, ₽</span>
        <input class="wal-autor-inp" type="number" min="1" placeholder="5000" value="${g.target||''}"
          oninput="walGoalEdit.target=Math.max(0,Number(this.value)||0)">
      </div>
      <div>
        <span class="wal-autor-lbl">Уже накоплено, ₽</span>
        <input class="wal-autor-inp" type="number" min="0" placeholder="0" value="${g.saved||''}"
          oninput="walGoalEdit.saved=Math.max(0,Number(this.value)||0)">
      </div>
      <div>
        <span class="wal-autor-lbl">Иконка</span>
        <div class="wal-goal-ic-pick">${WAL_GOAL_ICS.map(ic=>`
          <button class="${g.ic===ic?'on':''}" onclick="walGoalEdit.ic='${ic}';walRenderGoal()">${I(ic)}</button>`).join('')}</div>
      </div>
    </div>
    <button class="btn" onclick="walSaveGoal()"><svg class="i"><use href="#i-check"/></svg> Сохранить цель</button>
    ${g._new ? '' : `<div style="height:8px"></div>
      <button class="btn ghost" style="color:var(--danger);border-color:var(--danger)" onclick="walDeleteGoal()"><svg class="i"><use href="#i-trash"/></svg> Удалить цель</button>`}`;
}
function walSaveGoal(){
  const g = walGoalEdit; if(!g) return;
  if(!g.name.trim()){ toast('Введи название цели'); return; }
  if(!g.target || g.target <= 0){ toast('Укажи сумму цели'); return; }
  const arr = WAL_X.goals || [];
  const idx = arr.findIndex(x=>x.id===g.id);
  const clean = {id:g.id, name:g.name.trim(), target:g.target, saved:g.saved||0, ic:g.ic||'target'};
  if(idx < 0) arr.push(clean); else arr[idx] = clean;
  WAL_X.goals = arr; walXSave();
  closeSheet(); walRenderGoals();
  toast(g._new ? 'Цель добавлена: ' + clean.name : 'Цель обновлена');
}
function walDeleteGoal(){
  const g = walGoalEdit; if(!g) return;
  WAL_X.goals = (WAL_X.goals||[]).filter(x=>x.id!==g.id); walXSave();
  closeSheet(); walRenderGoals();
  toast('Цель удалена');
}

/* ---------- АВТОПОПОЛНЕНИЯ ----------
   Демо-правило владельца («пополнять на 5 000 ₽, когда баланс ниже 1 000»)
   удалено 09.08: правила создаёт сам человек, и первое из них не должно
   появляться из воздуха. */
if(!WAL_X.autoRules){
  WAL_X.autoRules = [];
  walXSave();
}
function walRenderAutoRules(){
  const box = document.getElementById('walAutoRules');
  if(!box) return;
  const rules = WAL_X.autoRules || [];
  const list = rules.length ? rules.map(r=>`
    <div class="wal-autor-r" style="opacity:${r.on?1:.55}">
      <div class="wal-autor-ic">${I('plus')}</div>
      <div class="wal-autor-b">
        <b>Если баланс &lt; ${fmtMoney(r.below)}</b>
        <span>Пополнить на <b style="color:var(--accent)">${fmtMoney(r.sum)}</b> · ${WAL_M_LABEL[r.method]||'Карта РФ'} · ${r.on?'активно':'выключено'}</span>
      </div>
      <button class="wal-autor-x" onclick="walToggleAutoRule('${r.id}')" aria-label="${r.on?'Выключить':'Включить'}">${I(r.on?'check':'plus')}</button>
      <button class="wal-autor-x" onclick="walDeleteAutoRule('${r.id}')" aria-label="Удалить">${I('trash')}</button>
    </div>`).join('') : `<div class="wal-autor-empty">Правил ещё нет. Настрой — и баланс не упадёт до нуля в самый неподходящий момент.</div>`;
  box.innerHTML = list + `
    <button class="wal-autor-add" onclick="walOpenAutoRule()"><svg class="i"><use href="#i-plus"/></svg>Новое правило</button>`;
}
function walToggleAutoRule(id){
  const r = (WAL_X.autoRules||[]).find(x=>x.id===id); if(!r) return;
  r.on = !r.on; walXSave(); walRenderAutoRules();
  toast('Автопополнение ' + (r.on ? 'включено' : 'выключено'));
}
function walDeleteAutoRule(id){
  WAL_X.autoRules = (WAL_X.autoRules||[]).filter(x=>x.id!==id); walXSave();
  walRenderAutoRules(); toast('Правило удалено');
}
let walAutoRuleEdit = null;
function walOpenAutoRule(){
  walAutoRuleEdit = {id:'ar_'+Math.random().toString(36).slice(2,7), below:1000, sum:5000, method:'card', on:true};
  walRenderAutoRule();
  openSheet('walAutoRule');
}
function walRenderAutoRule(){
  const r = walAutoRuleEdit; if(!r) return;
  document.getElementById('walAutoRuleView').innerHTML = `
    <h3>Новое правило автопополнения</h3>
    <p class="dim" style="font-size:12.5px;margin:-4px 0 12px">Кошелёк сам пополнится, когда баланс упадёт ниже порога</p>
    <div class="wal-autor-form">
      <div>
        <span class="wal-autor-lbl">Пополнить, если баланс меньше, ₽</span>
        <input class="wal-autor-inp" type="number" min="0" value="${r.below}"
          oninput="walAutoRuleEdit.below=Math.max(0,Number(this.value)||0)">
      </div>
      <div>
        <span class="wal-autor-lbl">Сумма пополнения, ₽</span>
        <input class="wal-autor-inp" type="number" min="1" value="${r.sum}"
          oninput="walAutoRuleEdit.sum=Math.max(0,Number(this.value)||0)">
      </div>
      <div>
        <span class="wal-autor-lbl">Источник пополнения</span>
        <div class="wal-methods">${WAL_METHODS.map(([k,l,ic])=>`
          <button class="wal-m ${r.method===k?'on':''}" onclick="walAutoRuleEdit.method='${k}';walRenderAutoRule()">${I(ic)}<span>${l}</span></button>`).join('')}</div>
      </div>
    </div>
    <button class="btn" onclick="walSaveAutoRule()"><svg class="i"><use href="#i-check"/></svg> Сохранить правило</button>
    <p class="dim" style="font-size:11px;text-align:center;margin-top:9px">Списание с карты только при срабатывании правила. Отключить в любой момент.</p>`;
}
function walSaveAutoRule(){
  const r = walAutoRuleEdit; if(!r) return;
  if(r.sum <= 0){ toast('Укажи сумму пополнения'); return; }
  WAL_X.autoRules = (WAL_X.autoRules || []).concat([r]); walXSave();
  closeSheet(); walRenderAutoRules();
  toast('Правило добавлено: если < ' + fmtMoney(r.below) + ' → пополнить на ' + fmtMoney(r.sum));
}
/* мок реального крона: при загрузке — если баланс ниже порога любого активного
   правила и с последнего срабатывания прошло > 30 мин — сработает.
   На самой первой загрузке молча выставляем baseline, чтобы не удивлять пользователя. */
(function walAutoRuleTick(){
  if(!WAL_X.autoRuleLast){ WAL_X.autoRuleLast = Date.now(); walXSave(); return; }
  const rules = (WAL_X.autoRules || []).filter(r=>r.on && WALLET.balance < r.below);
  if(!rules.length) return;
  if(Date.now() - WAL_X.autoRuleLast < 30 * 60 * 1000) return;
  const r = rules[0];
  walletAdd(r.sum, 'Автопополнение · правило (' + WAL_M_LABEL[r.method] + ')');
  WAL_X.autoRuleLast = Date.now(); walXSave();
  setTimeout(()=>toast('Автопополнение: +' + fmtMoney(r.sum) + ' — баланс был ниже ' + fmtMoney(r.below)), 500);
})();

/* ---------- FACE-ID / ОТПЕЧАТОК (эмуляция) ---------- */
function walToggleBio(){
  if(WAL_X.bio){
    WAL_X.bio = false; walXSave(); walRenderSec();
    toast('Вход по отпечатку выключен');
  } else {
    walOpenBio('setup');
  }
}
function walOpenBio(mode, onOk){
  document.getElementById('walBioView').innerHTML = `
    <div class="wal-bio" id="walBioBox">
      <div class="wal-bio-ic" onclick="walBioScan()">${I('fingerprint')}</div>
      <div class="wal-bio-h">${mode==='setup'?'Приложи палец':'Разблокируй кошелёк'}</div>
      <div class="wal-bio-s">${mode==='setup'?'Приложи палец к датчику, чтобы приложение запомнило отпечаток. В прод-версии — реальный Touch/Face-ID.':'Быстрый вход без ПИН-кода — приложи палец или посмотри в камеру'}</div>
    </div>
    <div style="height:14px"></div>
    <button class="btn ghost" onclick="closeSheet()">Отмена</button>`;
  window._walBioOnOk = onOk || (()=>{
    WAL_X.bio = true; walXSave(); walRenderSec();
    toast('Вход по отпечатку включён');
  });
  window._walBioMode = mode;
  openSheet('walBio');
}
function walBioScan(){
  const box = document.getElementById('walBioBox'); if(!box) return;
  /* «сканирование» — 1.4с прогресса */
  setTimeout(()=>{
    /* 90% успех */
    const ok = Math.random() > 0.1;
    box.classList.add(ok ? 'ok' : 'err');
    box.querySelector('.wal-bio-h').textContent = ok ? 'Готово' : 'Не распознан';
    box.querySelector('.wal-bio-s').textContent = ok ? 'Отпечаток принят.' : 'Попробуй ещё раз — приложи палец плотно.';
    if(ok){
      setTimeout(()=>{ closeSheet(); if(window._walBioOnOk) window._walBioOnOk(); }, 550);
    } else {
      setTimeout(()=>{ box.classList.remove('err'); box.querySelector('.wal-bio-h').textContent = 'Приложи палец'; box.querySelector('.wal-bio-s').textContent = 'Ещё раз — держи палец плотно.'; }, 900);
    }
  }, 1400);
}

/* ---------- LIVE-УВЕДОМЛЕНИЕ О ЗАЧИСЛЕНИИ (sticky slide-in) ---------- */
function walLiveNotify(opts){
  const cur = document.getElementById('walLive');
  if(cur) cur.remove();
  const el = document.createElement('div');
  el.id = 'walLive'; el.className = 'wal-live';
  el.innerHTML = `
    <span class="wal-live-av">${opts.avatar || (opts.who||'?').charAt(0).toUpperCase()}</span>
    <div class="wal-live-b">
      <b>${esc(opts.title || 'Поступление')}</b>
      <span>${esc(opts.sub || '')}</span>
    </div>
    <span class="wal-live-sum">${opts.sum || ''}</span>
    <button class="wal-live-x" onclick="event.stopPropagation();walLiveClose()" aria-label="Скрыть">${I('back')}</button>`;
  el.onclick = ()=>{ walLiveClose(); if(opts.onclick) opts.onclick(); };
  document.body.appendChild(el);
  setTimeout(walLiveClose, opts.ttl || 5000);
}
function walLiveClose(){
  const el = document.getElementById('walLive'); if(!el) return;
  el.classList.add('out');
  setTimeout(()=>{ if(el.parentNode) el.parentNode.removeChild(el); }, 350);
}
/* демо-нотификация при заходе в кошелёк — раз за сессию, зачисляет 500₽ */
let _walDemoLiveShown = false;
function walMaybeDemoLive(){
  /* демо-сидер отключён 09.08 (правка Даниэля: ноль демо-данных) */
  return (arguments.length && Array.isArray(arguments[0])) ? [] : undefined;

  if(!walIsOwner()) return;                              // гостям — никаких «пришло 500₽» (правка 29.07)
  if(_walDemoLiveShown) return;
  _walDemoLiveShown = true;
  setTimeout(()=>{
    walletAdd(500, 'Перевод в чате · Марк Волков');
    walLiveNotify({
      who:'Марк Волков',
      title:'Пришло 500 ₽ от Марка',
      sub:'@markvolkov · за монтаж рилса',
      sum:'+ 500 ₽',
      onclick:()=>{ if(typeof showTab==='function') showTab('wallet'); }
    });
    walFlash('in');
  }, 1600);
}

/* ---------- ПАТЧИ денежных потоков ядра (прежнее поведение сохранено) ---------- */

/* чипы обновляются при любой операции кошелька */
const _prevWalletNotifyRenderWal = walletNotifyRender;
walletNotifyRender = function(){ _prevWalletNotifyRenderWal(); walUpdateChips(); };

/* рендер кошелька при входе на вкладку */
const _prevShowTabWal = showTab;
showTab = function(t){
  _prevShowTabWal(t);
  if(t === 'wallet') renderWallet();
};

/* перерисовать график при смене темы (цвет линии и сетки зависят от темы) */
const _prevApplyThemeWal = applyTheme;
applyTheme = function(t){
  _prevApplyThemeWal(t);
  if(typeof walDrawChart === 'function') walDrawChart();
};

/* тарифы: списание с кошелька; при нехватке — предложение пополнить */
const _prevDoPayWal = doPay;
doPay = function(){
  const p = PLANS[payState.plan];
  const {total} = payPrice();
  if(WALLET.balance < total){
    const lack = total - WALLET.balance;
    document.getElementById('payView').innerHTML = `<div style="text-align:center;padding:14px 0">
      <div class="pop-ico" style="width:58px;height:58px;border-radius:50%;background:var(--lime-dim);display:flex;align-items:center;justify-content:center;margin:0 auto 12px">${I('money')}</div>
      <p style="font-weight:800;font-size:19px">Не хватает ${fmtMoney(lack)}</p>
      <p class="dim" style="font-size:13px;margin-top:6px">Тариф ${p.name} стоит ${fmtRub(total)}, на счёте ${fmtMoney(WALLET.balance)}.<br>Пополни кошелёк и вернись к оплате.</p>
      <div style="height:16px"></div>
      <button class="btn" onclick="closeSheet();showTab('wallet');walOpenTopup(${Math.ceil(lack/100)*100})">${I('plus')} Пополнить кошелёк</button>
      <div style="height:9px"></div>
      <button class="btn ghost" onclick="renderPay()">Назад к оплате</button></div>`;
    return;
  }
  walletCharge(total, 'Тариф ' + p.name);
  okoEarn(total, 'Тарифы');
  _prevDoPayWal();
};

/* продвижение объявления на Бирже */
const _prevBuyPromoWal = buyPromo;
buyPromo = function(id, k){
  const pr = PROMOS.find(p=>p.k===k);
  if(!walletCharge(pr.price, 'Продвижение объявления · ' + pr.name)){ closeSheet(); return; }
  okoEarn(pr.price, 'Продвижение');
  _prevBuyPromoWal(id, k);
};

/* продвижение поста в ленте */
const _prevBuyPostPromoWal = buyPostPromo;
buyPostPromo = function(i){
  const pr = POST_PROMOS[i];
  if(!walletCharge(pr.price, 'Продвижение поста · ' + pr.reach + ' показов')){ closeSheet(); return; }
  okoEarn(pr.price, 'Продвижение');
  _prevBuyPostPromoWal(i);
};

/* продвижение канала */
const _prevBuyChannelPromoWal = buyChannelPromo;
buyChannelPromo = function(i){
  const pr = CH_PROMOS[i];
  if(!walletCharge(pr.price, 'Продвижение канала · ' + pr.reach + ' показов')){ closeSheet(); return; }
  okoEarn(pr.price, 'Продвижение');
  _prevBuyChannelPromoWal(i);
};

/* заказ на Бирже: эскроу + комиссия OKO 10% */
const _prevOrderListingWal = orderListing;
orderListing = function(id){
  const l = LISTINGS.find(x=>x.id===id);
  if(!walletCharge(l.p, 'Покупка на Бирже: ' + l.t)){ closeSheet(); return; }
  // комиссия НЕ признаётся сейчас — только при снятии эскроу (mpReleaseHold), когда сделка реально завершена
  WALLET.hold += l.p; walletSave();
  _prevOrderListingWal(id);
  toast('Эскроу-защита: ' + fmtMoney(l.p) + ' в холде до выполнения заказа');
};

/* перевод денег в чате */
const _prevSendMoneyWal = sendMoney;
sendMoney = function(){
  const sum = Number(document.getElementById('moneySum').value);
  if(!sum || !currentChat){ _prevSendMoneyWal(); return; }
  if(!walletCharge(sum, 'Перевод в чате · ' + currentChat.name)){ closeSheet(); return; }
  _prevSendMoneyWal();
};

/* партнёрка: выплата зачисляется на кошелёк */
const _prevDoPayoutWal = doPayout;
doPayout = function(){
  const req = (document.getElementById('payoutReq').value || '').trim();
  _prevDoPayoutWal();
  if(!req) return; // прежняя версия уже показала подсказку про реквизиты
  const rub = Math.round(PAYOUT_BALANCE * WAL_USD_RATE);
  walletAdd(rub, 'Выплата партнёрских ($' + PAYOUT_BALANCE + ')');
};

/* =========================================================================
   W2: НАВИГАЦИЯ ПО ПОДСТРАНИЦАМ КОШЕЛЬКА (Сбер/iOS pushed screens)
   Каждая .w2-page — fullscreen. Открытие через w2Open(id), закрытие w2Close(id).
   Синхронизировано с nvPush/nvPop, чтобы работал системный «назад».
   ========================================================================= */
function w2Open(id){
  const p = document.getElementById('w2p-' + id);
  if(!p) return;
  p.classList.add('open');
  /* оживление подстраниц, где рендер зависит от размеров */
  if(id === 'accounts')  walRenderAccounts();
  if(id === 'goals')     walRenderGoals();
  if(id === 'autopay')   { walRenderAutopay(); walRenderAutoRules(); walRenderPlanned(); }
  if(id === 'analytics') { walRenderStats(); walRenderTopInc(); walRenderPie(); walRenderCats(); requestAnimationFrame(()=>{ walDrawChart(true); walUpdateAvg(); }); }
  if(id === 'security')  { walRenderSafety(); walRenderSec(); }
  if(id === 'history')   walRenderLedger();
  if(id === 'transfers') w2RenderRecent();
  if(id === 'exchange')  w2RenderExchange();
  if(id === 'receive')   w2RenderReceive();
  try{ if(typeof nvPush === 'function') nvPush('w2-' + id, ()=>{ p.classList.remove('open'); }); }catch(e){}
}
function w2Close(id){
  const p = document.getElementById('w2p-' + id);
  if(!p) return;
  p.classList.remove('open');
  try{ if(typeof nvPop === 'function') nvPop('w2-' + id); }catch(e){}
}

/* --- «Оплатить» — маленький popup выбора куда платить --- */
function w2OpenPay(){
  if(typeof showPopup !== 'function'){
    /* fallback: пусть просто откроется история */
    return w2Open('history');
  }
  showPopup({
    ico:'card', title:'Что оплатить?',
    body:'Выбери, куда пойдёт списание с лицевого счёта. Для новых услуг — раздел «Партнёрка» и «Мини-аппы».',
    actions:[
      {label:'Тариф · подписка PRO', onclick:()=>{ if(typeof openPay === 'function') openPay('PRO'); else w2Open('autopay'); }},
      {label:'Автопополнение и шаблоны', onclick:()=>w2Open('autopay')},
      {label:'Перевод по нику или на карту', onclick:()=>w2Open('transfers')},
      {label:'Закрыть', ghost:true},
    ]
  });
}

/* --- «крипто-перевод» из подстраницы «Переводы» --- */
function w2TransferCrypto(){
  if(!WAL_X.ton){
    if(typeof showPopup === 'function'){
      showPopup({
        ico:'ton', title:'Нужен внешний TON-кошелёк',
        body:'Реальная крипта (Toncoin, USDT-TON, USDT-TRC20) отправляется и принимается только через <b>Tonkeeper</b>, <b>TON Wallet</b> или <b>MyTonWallet</b>. Внутри OKO — только отображение баланса и заявки.<br><br>Подключи кошелёк через TON Connect — займёт 10 секунд.',
        actions:[
          {label:'Подключить TON-кошелёк', onclick:()=>{ walTonConnect(); }},
          {label:'Понятно', ghost:true},
        ]
      });
    }
    return;
  }
  /* если подключён — открываем sheet вывода на TON */
  w2Close('transfers');
  walOpenWithdraw();
  setTimeout(()=>{ walWdState.method = 'ton'; walRenderWithdraw(); }, 40);
}

/* --- «Недавние получатели» на странице переводов --- */
function w2RenderRecent(){
  const box = document.getElementById('w2Recent');
  if(!box) return;
  /* берём из истории уникальных получателей переводов */
  const seen = new Set(), rows = [];
  WALLET.ledger.forEach(o => {
    if(o.t !== '-' || !/Перевод в чате/i.test(o.why)) return;
    const m = o.why.match(/@([A-Za-z0-9_]+)/);
    const nick = m ? m[1] : (o.why.split('·')[1] || '').trim();
    if(!nick || seen.has(nick)) return;
    seen.add(nick);
    rows.push({nick, name: nick, at: o.at});
  });
  if(!rows.length){
    box.innerHTML = '<p class="w2-recent-empty">Пока никого — как только сделаешь первый перевод, ник появится здесь.</p>';
    return;
  }
  box.innerHTML = rows.slice(0, 8).map(r => `
    <button class="wal-send-qi" onclick="w2Close('transfers');walOpenSend('${esc(r.nick)}')">
      <span class="wal-send-av">${(r.name||'?').charAt(0).toUpperCase()}</span>
      <span>${esc(r.name.length > 10 ? r.name.slice(0,10) + '…' : r.name)}</span>
    </button>`).join('');
}

/* --- XP инфо-попап --- */
function w2XpInfo(){
  if(typeof showPopup !== 'function') return;
  const xp = (WAL_X.balances && WAL_X.balances.XP) || 0;
  showPopup({
    ico:'xp', title:'Про очки OKO (XP)',
    body:'<b>' + Math.round(xp).toLocaleString('ru-RU').replace(/,/g,' ') + ' XP</b> — это внутренние очки приложения, а не валюта.<br><br>' +
         '· Начисляются за активность (уроки Академии, посты, задания)<br>' +
         '· Тратятся в Играх (спины, ставки) и на бонусы Академии<br>' +
         '· Нельзя вывести и обменять на рубли<br>' +
         '· Не связаны с криптой или банковским счётом',
    actions:[{label:'Понятно'}]
  });
}

/* =========================================================================
   W2: НОВЫЙ ОБМЕН ВАЛЮТ (fullscreen, большие поля ввода)
   Правка Даниэля: «пишет введите сумму а куда вводить при обмене?»
   Ответ: явное большое поле, focus, hint, счётчик, кнопка MAX, честный курс.
   ========================================================================= */
function w2RenderExchange(){
  const s = walExState;
  const codes = ['RUB','USDT_TON','USDT_TRC','TON'];         // XP не участвует в обмене — не валюта
  if(!codes.includes(s.from)) s.from = 'RUB';
  if(!codes.includes(s.to))   s.to = s.from === 'RUB' ? 'USDT_TON' : 'RUB';
  const fromM = WAL_CUR_META[s.from], toM = WAL_CUR_META[s.to];
  const outVal = walExFinal();
  const rate = walExRate(s.from, s.to);
  const box = document.getElementById('w2ExView');
  if(!box) return;
  box.innerHTML = `
    <div class="w2-ex">
      <p class="w2-ex-hint">Введи сумму в поле «Отдаёте» — во второй строке автоматически посчитается, сколько придёт.</p>

      <div class="w2-ex-cell on">
        <div class="w2-ex-cell-h">
          ${walCurLogoHtml(s.from)}
          <div class="w2-ex-cell-name">
            <div class="w2-ex-cell-lbl">Отдаёте</div>
            <b>${fromM.name}</b>
          </div>
        </div>
        <div class="w2-ex-cell-inp">
          <input id="w2ExAmt" type="number" inputmode="decimal" min="0" step="any"
                 placeholder="0"
                 value="${s.amount || ''}"
                 oninput="walExState.amount=Math.max(0,Number(this.value)||0);w2SyncExchange()">
          <span class="w2-ex-cell-sym">${fromM.sym}</span>
        </div>
        <div class="w2-ex-cell-bot">
          <span>Доступно: <b>${walCurFmt(s.from, walCurBal(s.from))}</b></span>
          <button class="max" onclick="walExState.amount=walCurBal('${s.from}');w2RenderExchange();document.getElementById('w2ExAmt').focus()">Всё</button>
        </div>
      </div>

      <button class="w2-ex-swap-btn" onclick="w2ExSwap()" aria-label="Поменять местами">${I('swap')}</button>

      <div class="w2-ex-cell">
        <div class="w2-ex-cell-h">
          ${walCurLogoHtml(s.to)}
          <div class="w2-ex-cell-name">
            <div class="w2-ex-cell-lbl">Получаете</div>
            <b>${toM.name}</b>
          </div>
        </div>
        <div class="w2-ex-cell-inp">
          <span class="w2-ex-cell-val" id="w2ExOut">${outVal ? walCurFmt(s.to, outVal).replace(' '+toM.sym,'') : '0'}</span>
          <span class="w2-ex-cell-sym">${toM.sym}</span>
        </div>
        <div class="w2-ex-cell-bot">
          <span>Курс: <b>1 ${fromM.sym} = ${rate.toFixed(6).replace(/\.?0+$/,'')} ${toM.sym}</b></span>
        </div>
      </div>

      <p class="w2-ex-pair-h">Быстрая смена пары «В»</p>
      <div class="w2-ex-pair">${codes.filter(c => c !== s.from).map(c => `
        <button class="${s.to === c ? 'on' : ''}" onclick="walExState.to='${c}';w2RenderExchange()">
          ${walCurLogoHtml(c)}<span>${WAL_CUR_META[c].sym}</span>
        </button>`).join('')}</div>

      <div class="w2-ex-info" id="w2ExInfo"></div>

      <button class="btn" onclick="w2DoExchange()">
        <svg class="i"><use href="#i-swap"/></svg>
        <span id="w2ExBtn">${s.amount > 0 ? 'Обменять ' + walCurFmt(s.from, s.amount) : 'Обменять'}</span>
      </button>
      <p class="dim" style="font-size:11px;text-align:center;margin-top:9px">Курс OKO Team, обновляется несколько раз в сутки. Комиссия обмена 1%. Реальные торги — на биржах.</p>
    </div>`;
  w2SyncExchange();
  /* автофокус на поле ввода — чтобы было очевидно, куда вбивать */
  setTimeout(()=>{
    const inp = document.getElementById('w2ExAmt');
    if(inp && !s.amount){ try{ inp.focus(); }catch(e){} }
  }, 220);
}
function w2SyncExchange(){
  const s = walExState;
  const out = walExFinal();
  const toM = WAL_CUR_META[s.to], fromM = WAL_CUR_META[s.from];
  const outEl = document.getElementById('w2ExOut');
  if(outEl) outEl.textContent = out ? walCurFmt(s.to, out).replace(' '+toM.sym,'') : '0';
  const info = document.getElementById('w2ExInfo');
  if(info){
    if(s.amount > 0){
      const fee = walExFee();
      info.innerHTML = 'Отдаёте <b>' + walCurFmt(s.from, s.amount) + '</b>, получите <em>' + walCurFmt(s.to, out) +
        '</em>.<br>Курс OKO Team, комиссия 1% уже учтена (' + walCurFmt(s.to, fee) + ').';
    } else {
      info.textContent = 'Введи сумму в верхнем поле — покажу, сколько придёт, и с каким курсом.';
    }
  }
  const btn = document.getElementById('w2ExBtn');
  if(btn) btn.textContent = s.amount > 0 ? 'Обменять ' + walCurFmt(s.from, s.amount) : 'Обменять';
}
function w2ExSwap(){
  const t = walExState.from; walExState.from = walExState.to; walExState.to = t;
  walExState.amount = 0;
  w2RenderExchange();
}
function w2DoExchange(){
  const s = walExState;
  if(!s.amount || s.amount <= 0){ toast('Введи сумму в поле «Отдаёте»'); const i = document.getElementById('w2ExAmt'); if(i) i.focus(); return; }
  if(s.amount > walCurBal(s.from)){ toast('Недостаточно: доступно ' + walCurFmt(s.from, walCurBal(s.from))); return; }
  const out = walExFinal();
  const from = WAL_CUR_META[s.from], to = WAL_CUR_META[s.to];
  const box = document.getElementById('w2ExView');
  box.innerHTML = `<div style="text-align:center;padding:44px 0"><div class="spin"></div><p style="font-weight:700;margin-top:14px">Меняем валюту…</p></div>`;
  setTimeout(()=>{
    /* списываем «от» */
    if(s.from === 'RUB'){
      walletCharge(s.amount, 'Обмен ' + from.sym + ' → ' + to.sym);
    } else {
      walCurSet(s.from, walCurBal(s.from) - s.amount);
    }
    /* начисляем «в» */
    if(s.to === 'RUB'){
      walletAdd(Math.round(out), 'Обмен ' + from.sym + ' → ₽');
    } else {
      walCurSet(s.to, walCurBal(s.to) + out);
    }
    box.innerHTML = `<div class="wal-ok-wrap">
      <div class="wal-ok">${I('check')}</div>
      <p style="font-weight:800;font-size:22px;margin-top:16px">${walCurFmt(s.to, out)}</p>
      <p class="dim" style="font-size:13px;margin-top:8px">Обмен выполнен: ${walCurFmt(s.from, s.amount)} → ${walCurFmt(s.to, out)}<br>Баланс ${to.sym}: <b>${walCurFmt(s.to, walCurBal(s.to))}</b></p>
      <div style="height:20px"></div>
      <button class="btn" onclick="w2Close('exchange')">Готово</button>
      <div style="height:8px"></div>
      <button class="btn ghost" onclick="walExState.amount=0;w2RenderExchange()">Ещё обмен</button>
    </div>`;
    renderWallet();
    walFlash(s.to === 'RUB' ? 'in' : 'out');
    toast('Обмен: ' + walCurFmt(s.from, s.amount) + ' → ' + walCurFmt(s.to, out));
  }, 700);
}

/* --- «QR-приём»: реюзаем существующий walRenderReceive/walDrawRecvQR в подстранице --- */
function w2RenderReceive(){
  const host = document.getElementById('w2RecvHost');
  if(!host) return;
  walRecvState = walRecvState || {sum:0, note:'', code:'RUB'};
  /* Контейнер отдаём явно — без подмены id, иначе рендер уходил в шторку. */
  const view = document.createElement('div'); view.className = 'wal-recv-host';
  host.innerHTML = ''; host.appendChild(view);
  walRenderReceive(view);
  setTimeout(()=>walDrawRecvQR(view), 60);
}

/* =========================================================================
   w2- REAL P2P: реальные внутренние переводы через backend api.php.
   - WALLET.owner_email = PROFILE.email (ключ везде)
   - GET wallet_balance при открытии экрана
   - POST wallet_transfer с 1% комиссией OKO
   - GET wallet_history — реальная выписка
   - POST wallet_topup — Lava.top ссылка
   - IndexedDB очередь оффлайн, автоотправка при онлайне
   - PIN обязателен для сумм > 10 000 ₽ (реальная проверка перед fetch)
   - Rate limit 5 tx/min на бэке; на клиенте — понятное сообщение
   - Событие window «oko:wallet:receive» для notifs-plus
   ========================================================================= */
(function w2WalletRealInit(){
  const API   = (typeof OKO_API !== 'undefined' && OKO_API) ? OKO_API : 'https://okoteam.top/api.php';
  const FEE   = 0.01;
  const PIN_T = 10000; // порог PIN
  const w2SecureTxt = 'Через OKO Bank · комиссия 1%';

  function w2OwnerEmail(){
    try{
      if(typeof PROFILE === 'undefined') return '';
      return String(PROFILE.email || '').trim().toLowerCase();
    }catch(e){ return ''; }
  }
  window.w2WalletOwnerEmail = w2OwnerEmail;

  /* ---------- IndexedDB очередь оффлайн-переводов ---------- */
  const IDB_NAME = 'oko-wallet-queue-v1', IDB_STORE = 'txq';
  function w2Idb(){
    return new Promise((res)=>{
      try{
        const rq = indexedDB.open(IDB_NAME, 1);
        rq.onupgradeneeded = e => {
          const db = e.target.result;
          if(!db.objectStoreNames.contains(IDB_STORE))
            db.createObjectStore(IDB_STORE, {keyPath:'id', autoIncrement:true});
        };
        rq.onsuccess = e => res(e.target.result);
        rq.onerror   = ()=> res(null);
      }catch(e){ res(null); }
    });
  }
  async function w2QueuePush(tx){
    const db = await w2Idb(); if(!db) return false;
    return new Promise(res => {
      try{
        const t = db.transaction(IDB_STORE, 'readwrite');
        t.objectStore(IDB_STORE).add(Object.assign({at: Date.now()}, tx));
        t.oncomplete = ()=>res(true); t.onerror = ()=>res(false);
      }catch(e){ res(false); }
    });
  }
  async function w2QueueAll(){
    const db = await w2Idb(); if(!db) return [];
    return new Promise(res => {
      try{
        const t = db.transaction(IDB_STORE, 'readonly');
        const rq = t.objectStore(IDB_STORE).getAll();
        rq.onsuccess = ()=>res(rq.result || []); rq.onerror = ()=>res([]);
      }catch(e){ res([]); }
    });
  }
  async function w2QueueDel(id){
    const db = await w2Idb(); if(!db) return;
    return new Promise(res => {
      try{
        const t = db.transaction(IDB_STORE, 'readwrite');
        t.objectStore(IDB_STORE).delete(id);
        t.oncomplete = res; t.onerror = res;
      }catch(e){ res(); }
    });
  }

  /* ---------- API-обёртки ---------- */
  async function w2Fetch(url, opts){
    const ctrl = new AbortController();
    const to = setTimeout(()=>ctrl.abort(), 15000);
    try{
      const r = await fetch(url, Object.assign({signal:ctrl.signal}, opts||{}));
      clearTimeout(to);
      let j;
      try{ j = await r.json(); }catch(e){ j = {ok:false, error:'bad response'}; }
      if(r.status === 429) { const err = new Error(j.error||'Слишком часто'); err.code=429; throw err; }
      if(!j || !j.ok) throw new Error((j && j.error) || ('HTTP '+r.status));
      return j;
    } catch(e){ clearTimeout(to); throw e; }
  }
  async function w2ApiBalance(email){
    return w2Fetch(API+'?action=wallet_balance&email='+encodeURIComponent(email));
  }
  async function w2ApiTransfer(from, to, amount, comment){
    return w2Fetch(API+'?action=wallet_transfer', {
      method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({from_email: from, to_nick_or_email: to, amount, comment: comment||''})
    });
  }
  async function w2ApiHistory(email, limit){
    return w2Fetch(API+'?action=wallet_history&email='+encodeURIComponent(email)+'&limit='+(limit||50));
  }
  async function w2ApiTopup(email, amount, method){
    return w2Fetch(API+'?action=wallet_topup', {
      method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({email, amount, method})
    });
  }
  window.w2Api = {balance:w2ApiBalance, transfer:w2ApiTransfer, history:w2ApiHistory, topup:w2ApiTopup};

  /* ---------- Слияние истории бэка в локальный леджер (без дублей по tx_id+dir) ---------- */
  function w2MergeHistory(items){
    if(!Array.isArray(items) || !items.length) return;
    const seen = new Set();
    (WALLET.ledger||[]).forEach(o => { if(o.tx_id) seen.add(o.tx_id + ':' + (o.dir || o.direction || o.t)); });
    const add = [];
    items.forEach(it => {
      const key = it.tx_id + ':' + it.direction;
      if(seen.has(key)) return;
      seen.add(key);
      const at = Date.parse((it.created_at||'').replace(' ','T')) || Date.now();
      const amt = Number(it.amount) || 0;
      if(it.direction === 'receive'){
        add.push({t:'+', sum: amt, why: 'Перевод от '+it.from_email + (it.comment?' («'+it.comment+'»)':''), at, tx_id: it.tx_id, dir:'receive'});
      } else if(it.direction === 'send'){
        add.push({t:'-', sum: amt, why: 'Перевод @'+it.to_email + (it.comment?' («'+it.comment+'»)':''), at, tx_id: it.tx_id, dir:'send'});
      } else if(it.direction === 'fee'){
        add.push({t:'-', sum: amt, why: 'Комиссия OKO 1%', at, tx_id: it.tx_id, dir:'fee'});
      } else if(it.direction === 'topup'){
        add.push({t:'+', sum: amt, why: 'Пополнение · '+(it.comment||'Lava.top'), at, tx_id: it.tx_id, dir:'topup'});
      }
    });
    if(add.length){
      WALLET.ledger = add.concat(WALLET.ledger||[]);
      WALLET.ledger.sort((a,b)=>b.at-a.at);
      walletSave();
    }
  }

  /* ---------- Синхронизация локального WALLET с бэком (skeleton-friendly) ---------- */
  let w2SyncBusy = false, w2LastSyncAt = 0, w2LastBal = null;
  async function w2Sync(force){
    const email = w2OwnerEmail();
    if(!email) return;
    if(w2SyncBusy) return;
    if(!force && Date.now() - w2LastSyncAt < 4000) return;
    w2SyncBusy = true;
    try{
      const j = await w2ApiBalance(email);
      const prev = WALLET.balance;
      WALLET.balance    = j.balance;
      WALLET.hold       = j.hold || 0;
      WALLET.owner_email = email;
      walletSave();
      // событие получения средств (для notifs-plus и live-нотифа)
      if(w2LastBal !== null && j.balance > w2LastBal){
        const diff = j.balance - w2LastBal;
        try{ window.dispatchEvent(new CustomEvent('oko:wallet:receive', {detail:{amount:diff, balance:j.balance, email}})); }catch(e){}
        try{ walLiveNotify({who:'OKO Bank', title:'Пришло '+fmtMoney(diff), sub:'На лицевой счёт', sum:'+ '+fmtMoney(diff)}); }catch(e){}
        try{ walFlash('in'); }catch(e){}
        if(typeof NOTIFS !== 'undefined' && Array.isArray(NOTIFS)){
          NOTIFS.unshift({ic:'money', who:'OKO Bank', t:'Пришло '+fmtMoney(diff)+' на счёт', time:'только что', g:'Сегодня', unread:true, act:()=>{ if(typeof showTab==='function') showTab('wallet'); }});
          if(typeof updateNotifDot === 'function') updateNotifDot();
        }
      }
      w2LastBal = j.balance;
      try{
        const h = await w2ApiHistory(email, 50);
        if(h) w2MergeHistory(h.items);
      }catch(e){}
      try{ walletNotifyRender(); walUpdateChips(); }catch(e){}
      w2LastSyncAt = Date.now();
    } catch(e){
      /* offline / api down — тихо, следующий тик попробует */
    } finally { w2SyncBusy = false; }
  }
  window.w2WalletSync = w2Sync;

  /* ---------- Skeleton (заглушка на время fetch) ---------- */
  function w2ShowSkeleton(){
    const bal = document.getElementById('walBalance');
    if(bal && !bal.dataset.w2sk){
      bal.dataset.w2sk = '1';
      bal.style.opacity = '0.65';
      setTimeout(()=>{ bal.style.opacity = ''; delete bal.dataset.w2sk; }, 900);
    }
  }

  /* ---------- Перехват renderWallet: фоновый sync ---------- */
  const _prevRenderWallet = window.renderWallet || renderWallet;
  window.renderWallet = function(){
    _prevRenderWallet();
    if(w2OwnerEmail()){ w2ShowSkeleton(); w2Sync(); }
  };
  try{ renderWallet = window.renderWallet; }catch(e){}

  /* ---------- Перехват walDoSend: реальный POST /wallet_transfer ---------- */
  const _prevWalDoSend = window.walDoSend;
  window.walDoSend = function(){
    const s = walSendState;
    if(!s.to){ toast('Укажи ник получателя'); return; }
    if(!s.sum || s.sum <= 0){ toast('Укажи сумму перевода'); return; }
    // считаем комиссию 1% — сумма+комиссия ≤ баланс
    const fee   = Math.floor(s.sum * FEE);
    const total = s.sum + fee;
    if(total > WALLET.balance){
      toast('Не хватает: нужно '+fmtMoney(total)+' (сумма+комиссия 1%)');
      return;
    }
    // PIN обязателен на суммы > PIN_T
    if(s.sum > PIN_T){
      if(!WAL_X.pin){
        showPopup({ico:'lock', title:'Нужен ПИН-код',
          body:'Для перевода на сумму больше <b>'+fmtMoney(PIN_T)+'</b> нужно установить ПИН-код в разделе «Безопасность».',
          actions:[
            {label:'Настроить ПИН', onclick:()=>{ closeSheet(); w2Open('security'); setTimeout(walTogglePin, 350); }},
            {label:'Отмена', ghost:true},
          ]});
        return;
      }
      walPinOpen('confirm', 'walSendView', ()=>{ openSheet('walSend'); w2DoRealSend(); });
      return;
    }
    w2DoRealSend();
  };

  async function w2DoRealSend(){
    const s = walSendState;
    const email = w2OwnerEmail();
    const v = document.getElementById('walSendView');
    if(v) v.innerHTML = '<div style="text-align:center;padding:22px 0"><div class="spin"></div><p style="font-weight:700;margin-top:14px">Отправляем…</p><p class="dim" style="font-size:12px;margin-top:5px">'+w2SecureTxt+'</p></div>';
    // офлайн или гость без email → очередь
    if(!email){
      if(v) v.innerHTML = '<div class="wal-ok-wrap"><div class="wal-ok" style="background:rgba(255,184,74,0.2);color:#FFB84A">'+I('lock')+'</div>'
        +'<p style="font-weight:800;font-size:19px;margin-top:14px">Нужен email</p>'
        +'<p class="dim" style="font-size:13px;margin-top:6px">Заверши регистрацию (добавь email), чтобы отправлять реальные P2P-переводы.</p>'
        +'<div style="height:16px"></div>'
        +'<button class="btn" onclick="closeSheet()">Хорошо</button></div>';
      return;
    }
    if(!navigator.onLine){
      await w2QueuePush({from:email, to:s.to, amount:s.sum, comment:s.note});
      if(v) v.innerHTML = '<div class="wal-ok-wrap"><div class="wal-ok" style="background:rgba(255,184,74,0.2);color:#FFB84A">'+I('clock')+'</div>'
        +'<p style="font-weight:800;font-size:19px;margin-top:14px">В очереди</p>'
        +'<p class="dim" style="font-size:13px;margin-top:6px">Нет сети — отправим, как только появится соединение.</p>'
        +'<div style="height:16px"></div>'
        +'<button class="btn" onclick="closeSheet()">Хорошо</button></div>';
      toast('Перевод в очереди — отправим при онлайне');
      return;
    }
    try{
      const r = await w2ApiTransfer(email, s.to, s.sum, s.note);
      const why = 'Перевод @'+s.to + (s.note?' («'+s.note+'»)':'');
      WALLET.balance = r.balance;
      // локальная запись — с tx_id, чтобы не задублить при следующем sync
      WALLET.ledger.unshift({t:'-', sum: r.amount, why, at: Date.now(), tx_id: r.tx_id, dir:'send'});
      if(r.fee > 0){
        WALLET.ledger.unshift({t:'-', sum: r.fee, why:'Комиссия OKO 1% · '+r.tx_id, at: Date.now(), tx_id: r.tx_id, dir:'fee'});
      }
      walletSave();
      if(v) v.innerHTML = '<div class="wal-ok-wrap"><div class="wal-ok">'+I('check')+'</div>'
        +'<p style="font-weight:800;font-size:19px;margin-top:14px">− '+fmtMoney(r.amount + (r.fee||0))+'</p>'
        +'<p class="dim" style="font-size:13px;margin-top:6px">Перевод @'+esc(s.to)+' доставлен.<br>Комиссия OKO: <b>'+fmtMoney(r.fee||0)+'</b><br>Осталось: <b>'+fmtMoney(WALLET.balance)+'</b></p>'
        +'<div style="font-size:11px;color:var(--dim);margin-top:8px">TX '+esc(r.tx_id)+'</div>'
        +'<div style="height:16px"></div>'
        +'<button class="btn" onclick="closeSheet()">Готово</button></div>';
      renderWallet(); walFlash('out');
      toast('Отправлено @'+s.to+': '+fmtMoney(r.amount));
    } catch(e){
      const msg = String((e && e.message) || e);
      if(/recipient not found/i.test(msg))       toast('Получатель не найден в OKO');
      else if(/insufficient/i.test(msg))         toast('Недостаточно средств на счёте');
      else if(/cannot transfer to yourself/i.test(msg)) toast('Нельзя перевести самому себе');
      else if(e && e.code === 429)               toast('Лимит: не более 5 переводов в минуту');
      else if(!navigator.onLine){
        await w2QueuePush({from:email, to:s.to, amount:s.sum, comment:s.note});
        toast('Сеть пропала — перевод в очереди');
      } else                                     toast('Ошибка перевода: '+msg);
      walRenderSend();
    }
  }

  /* ---------- Показ комиссии под кнопкой «Отправить» (пере-рендер-safe) ---------- */
  const _prevWalSyncSendBtn = window.walSyncSendBtn;
  window.walSyncSendBtn = function(){
    if(_prevWalSyncSendBtn) _prevWalSyncSendBtn();
    const s = walSendState;
    const view = document.getElementById('walSendView'); if(!view) return;
    let box = document.getElementById('w2SendFee');
    if(!box){
      const btn = document.getElementById('walSendBtn');
      const btnWrap = btn ? btn.closest('button') : null;
      const anchor = btnWrap || (view.querySelector('.btn'));
      if(anchor && anchor.parentElement){
        box = document.createElement('div');
        box.id = 'w2SendFee';
        box.style.cssText = 'font-size:12px;color:var(--dim);text-align:center;margin-top:6px;line-height:1.55;padding:0 6px';
        anchor.parentElement.insertBefore(box, anchor.nextSibling);
      }
    }
    if(box){
      if(s.sum > 0){
        const fee   = Math.floor(s.sum * FEE);
        const total = s.sum + fee;
        box.innerHTML =
          'К зачислению получателю: <b style="color:var(--accent)">'+fmtMoney(s.sum)+'</b><br>'+
          'Комиссия OKO 1%: <b>'+fmtMoney(fee)+'</b><br>'+
          'Всего спишется: <b>'+fmtMoney(total)+'</b>' +
          (s.sum > PIN_T ? '<br><span style="color:#FFB84A">При отправке потребуется ПИН-код</span>' : '');
      } else box.innerHTML = '';
    }
  };

  /* ---------- Guest warning: если открыл «Отправить» без email — предупредим ---------- */
  const _prevWalOpenSend = window.walOpenSend;
  window.walOpenSend = function(prefill){
    _prevWalOpenSend(prefill);
    if(!w2OwnerEmail()){
      setTimeout(()=>{
        const v = document.getElementById('walSendView');
        if(!v || document.getElementById('w2SendGuestWarn')) return;
        const warn = document.createElement('div');
        warn.id = 'w2SendGuestWarn';
        warn.style.cssText = 'background:rgba(255,184,74,0.12);border:1px solid rgba(255,184,74,0.35);border-radius:12px;padding:10px 12px;margin-bottom:10px;font-size:12.5px;color:#FFB84A';
        warn.innerHTML = 'Реальные P2P-переводы работают после добавления email в профиль. Гостям — только UI-демо.';
        v.insertBefore(warn, v.firstChild);
      }, 40);
    }
  };

  /* ---------- Оффлайн очередь: авто-flush при онлайне ---------- */
  async function w2FlushQueue(){
    const email = w2OwnerEmail(); if(!email || !navigator.onLine) return;
    const q = await w2QueueAll();
    for(const tx of q){
      try{
        const r = await w2ApiTransfer(email, tx.to, tx.amount, tx.comment||'');
        await w2QueueDel(tx.id);
        toast('Отправлено из очереди: '+tx.to+' · '+fmtMoney(tx.amount));
      } catch(e){ break; /* следующий раз повторим */ }
    }
    w2Sync(true);
  }
  window.addEventListener('online', w2FlushQueue);
  setTimeout(w2FlushQueue, 3000);

  /* ---------- Перехват walDoTopup: реальный Lava.top для card/lava ---------- */
  const _prevWalDoTopup = window.walDoTopup;
  window.walDoTopup = async function(){
    const email = w2OwnerEmail();
    const s = walTopupState;
    // если гость или не банковский метод — старая мок-логика (usdt/ton — реквизиты)
    if(!email || (s.method !== 'card' && s.method !== 'lava')){
      return _prevWalDoTopup();
    }
    if(!s.sum || s.sum <= 0){ toast('Укажи сумму пополнения'); return; }
    const v = document.getElementById('walTopupView');
    if(v) v.innerHTML = '<div style="text-align:center;padding:22px 0"><div class="spin"></div><p style="font-weight:700;margin-top:14px">Открываем Lava.top…</p></div>';
    try{
      const r = await w2ApiTopup(email, s.sum, s.method);
      if(r && r.url){
        if(v) v.innerHTML = '<div class="wal-ok-wrap"><div class="wal-ok" style="background:linear-gradient(135deg,#9AFF00,#7ECBEB);color:#000">'+I('bolt')+'</div>'
          +'<p style="font-weight:800;font-size:19px;margin-top:14px">Счёт создан</p>'
          +'<p class="dim" style="font-size:13px;margin-top:6px">Оплата откроется на Lava.top. После оплаты баланс обновится автоматически.</p>'
          +'<div style="height:16px"></div>'
          +'<a class="btn" href="'+r.url+'" target="_blank" rel="noopener" style="text-decoration:none">Открыть Lava.top на '+fmtMoney(r.amount)+'</a>'
          +'<div style="height:8px"></div>'
          +'<button class="btn ghost" onclick="closeSheet()">Позже</button></div>';
        try{ window.open(r.url, '_blank', 'noopener'); }catch(e){}
        return;
      }
    } catch(e){ /* fallback */ }
    return _prevWalDoTopup();
  };

  /* ---------- PIN для вывода > 10 000: если PIN не установлен — требуем настроить ---------- */
  const _prevWalDoWithdraw = window.walDoWithdraw;
  window.walDoWithdraw = function(){
    const s = walWdState;
    if(s.sum > PIN_T && !WAL_X.pin){
      showPopup({ico:'lock', title:'Нужен ПИН-код',
        body:'Для вывода на сумму больше <b>'+fmtMoney(PIN_T)+'</b> нужно установить ПИН-код в разделе «Безопасность».',
        actions:[
          {label:'Настроить ПИН', onclick:()=>{ closeSheet(); w2Open('security'); setTimeout(walTogglePin, 350); }},
          {label:'Отмена', ghost:true},
        ]});
      return;
    }
    return _prevWalDoWithdraw();
  };

  /* ---------- «Повторить перевод» на карточке транзакции ---------- */
  const _prevWalRenderTx = window.walRenderTx;
  window.walRenderTx = function(op){
    _prevWalRenderTx(op);
    // если это исходящий перевод — добавим кнопку «Повторить»
    try{
      const cat = walCat(op.why);
      if(cat !== 'Переводы' || op.t !== '-') return;
      const acts = document.querySelector('#walTxView .wal-tx-acts');
      if(!acts || document.getElementById('w2RepeatBtn')) return;
      // достаём ник из описания
      const m = op.why.match(/@([A-Za-z0-9_.\-]+)/);
      const nick = m ? m[1] : '';
      if(!nick) return;
      const btn = document.createElement('button');
      btn.id = 'w2RepeatBtn'; btn.className = 'prim';
      btn.innerHTML = '<svg class="i"><use href="#i-send"/></svg>Повторить';
      btn.onclick = ()=>{ closeSheet(); setTimeout(()=>{ walOpenSend(nick); walSendState.sum = op.sum; walRenderSend(); }, 120); };
      acts.insertBefore(btn, acts.firstChild);
    }catch(e){}
  };

  /* ---------- Периодический poll: балансно-безопасно, только когда таб виден ---------- */
  setInterval(()=>{ if(!document.hidden && w2OwnerEmail()) w2Sync(); }, 25000);

  /* ---------- Первичный sync при загрузке (даёт email — сразу тянем реальные цифры) ---------- */
  if(w2OwnerEmail()) setTimeout(()=>w2Sync(true), 800);
})();

/* ---------- самоинициализация ---------- */
regTitle('wallet', 'Кошелёк');
walInsertChips();
walUpdateChips();
if(document.getElementById('screen-wallet') && document.getElementById('screen-wallet').classList.contains('active')) renderWallet();
/* При смене таба на другой — закрываем все w2-подстраницы, чтобы не «висели» */
try{
  const _prevShowTabW2 = showTab;
  showTab = function(t){
    if(t !== 'wallet'){
      document.querySelectorAll('.w2-page.open').forEach(p => p.classList.remove('open'));
    }
    _prevShowTabW2(t);
  };
}catch(e){}

/* ===== module: games ===== */
/* ===== GAMES: Рулетка OKO — единая призовая рулетка (префикс gm-) =====
   Один призовой опыт: колесо призов (SVG-иконки) — деньги, билеты, скидки на
   тариф, сам тариф, проверки видео, услуги OKO и бусты.
   • 1 бесплатная крутка в день (подарок — каждый сектор в плюс).
   • Платные крутки: пользователь выбирает ставку (100/300/1000 ₽ или билеты) —
     чем крупнее ставка, тем богаче пул. Каждая крутка ГАРАНТИРОВАННО даёт приз
     (нет «сгораний»). EV прозрачен — средний приз показан заранее.
   Никакого денежного гэмблинга (множителей ×N на ставку) — только фикс-призы.
   Списание — walletCharge, начисление — walletAdd, доход OKO — okoEarn.
   Всё оборачивается в function-declaration/патчи; каждый доступ к DOM защищён. */

/* ---------- SVG-иконки модуля (штрих 7, скруглённые концы) ---------- */
(function gmIcons(){
  const defs = document.querySelector('svg defs');
  if(!defs) return;
  const mk = (id, inner)=>{
    if(defs.querySelector('#'+id)) return;
    const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
    s.setAttribute('id', id); s.setAttribute('viewBox','0 0 100 100');
    s.innerHTML = inner; defs.appendChild(s);
  };
  /* подарок с лентой */
  mk('i-gm-gift','<path d="M20 46v34a8 8 0 008 8h44a8 8 0 008-8V46" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/><rect x="14" y="30" width="72" height="16" rx="5" fill="none" stroke="currentColor" stroke-width="7"/><path d="M50 30v58" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/><path d="M50 30C39 30 30 22 32 13c9-2 16 7 18 17zM50 30c11 0 20-8 18-17-9-2-16 7-18 17z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>');
  /* кубок чемпиона */
  mk('i-gm-cup','<path d="M32 14h36v24a18 18 0 01-36 0z" fill="none" stroke="currentColor" stroke-width="7" stroke-linejoin="round"/><path d="M32 22H17a15 15 0 0015 17M68 22h15a15 15 0 01-15 17" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/><path d="M50 56v14M38 84h24M43 70h14" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/>');
  /* билет (внутренняя валюта) с перфорацией */
  mk('i-gm-ticket','<path d="M20 32h60a6 6 0 016 6v8a7.5 7.5 0 000 15v8a6 6 0 01-6 6H20a6 6 0 01-6-6v-8a7.5 7.5 0 000-15v-8a6 6 0 016-6z" fill="none" stroke="currentColor" stroke-width="7" stroke-linejoin="round"/><path d="M66 36v7M66 57v7" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/>');
  /* процент (скидка на тариф) */
  mk('i-gm-pct','<circle cx="31" cy="31" r="10" fill="none" stroke="currentColor" stroke-width="7"/><circle cx="69" cy="69" r="10" fill="none" stroke="currentColor" stroke-width="7"/><path d="M78 22 22 78" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/>');
  /* буст (двойная стрелка вверх) */
  mk('i-gm-boost','<path d="M26 52l24-24 24 24M26 74l24-24 24 24" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/>');
  /* проверка видео (экран + галочка) */
  mk('i-gm-checkvid','<rect x="14" y="22" width="72" height="56" rx="12" fill="none" stroke="currentColor" stroke-width="7"/><path d="M34 50l10 11 22-24" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/>');
  /* пламя (серия визитов / стрик) */
  mk('i-gm-flame','<path d="M53 12c2 16 18 21 18 39a21 21 0 01-42 0c0-9 5-14 5-14-11 4-18 15-18 27a34 34 0 0068 0c0-26-24-36-31-52z" fill="none" stroke="currentColor" stroke-width="7" stroke-linejoin="round"/><path d="M50 84a13 13 0 01-8-23c1 8 7 9 8 15 4-3 4-7 3-11 6 2 10 8 10 14a13 13 0 01-13 5z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>');
  /* весы честности (таблица шансов) */
  mk('i-gm-scales','<path d="M50 16v66M30 82h40" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/><path d="M24 28h52" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/><path d="M24 28 12 54a13 13 0 0024 0zM76 28 64 54a13 13 0 0024 0z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>');
  /* тайный сундук (mystery box — loot crate) */
  mk('i-gm-mbox','<rect x="14" y="30" width="72" height="56" rx="6" fill="none" stroke="currentColor" stroke-width="7"/><rect x="10" y="22" width="80" height="14" rx="4" fill="none" stroke="currentColor" stroke-width="7"/><path d="M50 30v56" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/><circle cx="50" cy="52" r="6" fill="none" stroke="currentColor" stroke-width="6"/><path d="M50 22c-6-10 6-12 6-4 0-6 12-4 6 4M50 22c6-10-6-12-6-4 0-6-12-4-6 4" fill="none" stroke="currentColor" stroke-width="5" stroke-linejoin="round"/>');
  /* медаль-звезда (достижения) */
  mk('i-gm-medal','<path d="M28 10h44l-8 30H36z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/><circle cx="50" cy="62" r="24" fill="none" stroke="currentColor" stroke-width="7"/><path d="M50 50l4 10 10 1-8 7 3 10-9-6-9 6 3-10-8-7 10-1z" fill="none" stroke="currentColor" stroke-width="4" stroke-linejoin="round"/>');
  /* календарь стрика */
  mk('i-gm-cal','<rect x="14" y="22" width="72" height="64" rx="6" fill="none" stroke="currentColor" stroke-width="7"/><path d="M14 40h72M32 12v18M68 12v18" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/>');
})();

/* ---------- общее состояние + хелперы ---------- */
function gmUpdateBalance(){
  const el = document.getElementById('gmBalance');
  if(el) el.textContent = fmtMoney(WALLET.balance);
}
function gmPluralTk(n){
  const a = Math.abs(n) % 100, b = a % 10;
  if(a > 10 && a < 20) return 'билетов';
  if(b > 1 && b < 5) return 'билета';
  if(b === 1) return 'билет';
  return 'билетов';
}
/* «крутка» с числительным: 1 бесплатная крутка / 2 крутки / 5 круток */
function gmPluralSpin(n){
  const a = Math.abs(n) % 100, b = a % 10;
  if(a > 10 && a < 20) return 'бесплатных круток';
  if(b === 1) return 'бесплатная крутка';
  if(b > 1 && b < 5) return 'бесплатные крутки';
  return 'бесплатных круток';
}
function gmPluralCheck(n){
  const a = Math.abs(n) % 100, b = a % 10;
  if(a > 10 && a < 20) return 'проверок';
  if(b > 1 && b < 5) return 'проверки';
  if(b === 1) return 'проверка';
  return 'проверок';
}
function gmPluralPrize(n){
  const a = Math.abs(n) % 100, b = a % 10;
  if(a > 10 && a < 20) return 'призов';
  if(b > 1 && b < 5) return 'приза';
  if(b === 1) return 'приз';
  return 'призов';
}

/* услуги OKO — призы-купоны (код в «Мои призы») */
const GM_SERVICES = {
  promo:    {s:'Промо',    label:'Промо-пост в ленте OKO',        ic:'megaphone'},
  listing:  {s:'Буст',     label:'Буст объявления · 3 дня',        ic:'rocket'},
  priority: {s:'Топ',      label:'Приоритет в ленте · 7 дней',     ic:'fire'},
  campaign: {s:'Кампания', label:'Рекламная кампания OKO',         ic:'megaphone'}
};

/* ============================================================
   ПРИЗОВОЙ ПУЛ: билеты (валюта), проверки видео, бусты, сундук призов
   ============================================================ */

/* ---------- билеты: внутренняя валюта круток ---------- */
let GM_TICKETS = (()=>{ try{ return Math.max(0, parseInt(localStorage.getItem('oko-games-tickets'),10) || 0); }catch(e){ return 0; } })();
function gmTicketsSave(){ try{ localStorage.setItem('oko-games-tickets', String(GM_TICKETS)); }catch(e){} }
function gmTicketsRender(){
  document.querySelectorAll('.gm-tk-val').forEach(e=>{ e.textContent = GM_TICKETS; });
}
function gmTicketsAdd(n){
  if(n <= 0) return;
  GM_TICKETS += n; gmTicketsSave(); gmTicketsRender();
}
function gmTicketsSpend(n){
  if(GM_TICKETS < n) return false;
  GM_TICKETS -= n; gmTicketsSave(); gmTicketsRender();
  return true;
}

/* ---------- бонусные проверки видео ---------- */
function gmChecksGet(){ try{ return Math.max(0, parseInt(localStorage.getItem('oko-games-checks'),10) || 0); }catch(e){ return 0; } }
function gmChecksAdd(n){ try{ localStorage.setItem('oko-games-checks', String(gmChecksGet() + n)); }catch(e){} }
/* другим модулям (напр. paywall) — сколько бонусных проверок доступно */
window.okoBonusChecks = gmChecksGet;

/* ---------- буст: ×множитель к следующему ДЕНЕЖНОМУ призу ---------- */
function gmBoostGet(){ try{ return parseInt(localStorage.getItem('oko-games-boost'),10) || 0; }catch(e){ return 0; } }
function gmBoostSet(v){ try{ if(v > 1) localStorage.setItem('oko-games-boost', String(v)); else localStorage.removeItem('oko-games-boost'); }catch(e){} gmBoostBadge(); }
function gmBoostBadge(){
  const el = document.getElementById('gmBoostBadge');
  if(!el) return;
  const b = gmBoostGet();
  if(b > 1){
    el.style.display = '';
    el.innerHTML = `${I('gm-boost')}<span>Активен буст <b>×${b}</b> — умножит следующий денежный приз в рулетке</span>`;
  }else{
    el.style.display = 'none';
    el.innerHTML = '';
  }
}
/* применить (и погасить) буст к денежному призу; вернуть итоговую выплату */
function gmApplyBoost(base){
  const b = gmBoostGet();
  if(b > 1 && base > 0){
    gmBoostSet(0);
    gmPrizeUse2('boost'); /* пометить первый активный буст использованным */
    const boosted = Math.round(base * b * 100) / 100;
    toast(`Буст ×${b} сработал: +${fmtMoney(boosted - base)}`);
    return boosted;
  }
  return base;
}

/* ---------- сундук призов (персист 'oko-games-prizes') ---------- */
let GM_PRIZES = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-games-prizes')) || []; }catch(e){ return []; } })();
function gmPrizesSave(){ try{ localStorage.setItem('oko-games-prizes', JSON.stringify(GM_PRIZES.slice(0, 60))); }catch(e){} }
function gmMakeCode(){ return 'OKO-' + Math.random().toString(36).slice(2, 6).toUpperCase(); }
function gmPrizeAdd(p){
  p.id = 'gp' + Date.now().toString(36) + Math.floor(Math.random() * 1e4).toString(36);
  p.at = Date.now();
  GM_PRIZES.unshift(p);
  gmPrizesSave();
  gmPrizesRender();
  gmPrizesBtnRender();
  return p;
}
function gmPrizeUse(id){
  const p = GM_PRIZES.find(x=>x.id === id);
  if(p && p.status === 'active'){ p.status = 'used'; gmPrizesSave(); gmPrizesRender(); gmPrizesBtnRender(); }
}
/* пометить первый активный приз данного типа использованным (для бустов) */
function gmPrizeUse2(type){
  const p = GM_PRIZES.find(x=>x.type === type && x.status === 'active');
  if(p){ p.status = 'used'; gmPrizesSave(); gmPrizesRender(); gmPrizesBtnRender(); }
}
/* лучшая активная скидка на тариф — подхватывается в оплате */
function gmActiveDiscount(){
  return GM_PRIZES.filter(p=>p.type === 'discount' && p.status === 'active')
                  .sort((a,b)=>b.val - a.val)[0] || null;
}
function gmPrizesActiveCount(){ return GM_PRIZES.filter(p=>p.status === 'active').length; }

/* ============================================================
   ПРИЗОВЫЕ ПУЛЫ — каждый ровно 12 секторов (30° на сектор)
   c — «редкость» (0..4) для палитры; s — короткая подпись сектора
   ============================================================ */
const GM_POOLS = {
  /* бесплатный подарок дня — каждый сектор в плюс */
  free: [
    {t:'money',   v:50,        w:220, c:0, s:'50 ₽'},
    {t:'ticket',  v:1,         w:190, c:1, s:'1 бил'},
    {t:'money',   v:100,       w:150, c:1, s:'100 ₽'},
    {t:'check',   v:1,         w:120, c:2, s:'1 пров'},
    {t:'disc',    v:10,        w:95,  c:2, s:'−10%'},
    {t:'ticket',  v:2,         w:80,  c:2, s:'2 бил'},
    {t:'money',   v:200,       w:55,  c:3, s:'200 ₽'},
    {t:'money',   v:300,       w:40,  c:3, s:'300 ₽'},
    {t:'disc',    v:20,        w:28,  c:3, s:'−20%'},
    {t:'ticket',  v:5,         w:14,  c:4, s:'5 бил'},
    {t:'service', v:'promo',   w:6,   c:4, s:'Промо'},
    {t:'tier',    v:'PRO',     w:2,   c:4, s:'PRO'}
  ],
  /* крутка за 100 ₽ */
  p100: [
    {t:'ticket',  v:2,         w:210, c:1, s:'2 бил'},
    {t:'money',   v:50,        w:200, c:1, s:'50 ₽'},
    {t:'money',   v:100,       w:150, c:1, s:'100 ₽'},
    {t:'check',   v:2,         w:110, c:2, s:'2 пров'},
    {t:'disc',    v:10,        w:95,  c:2, s:'−10%'},
    {t:'money',   v:200,       w:70,  c:2, s:'200 ₽'},
    {t:'boost',   v:2,         w:55,  c:3, s:'×2'},
    {t:'disc',    v:20,        w:40,  c:3, s:'−20%'},
    {t:'money',   v:500,       w:40,  c:3, s:'500 ₽'},
    {t:'ticket',  v:5,         w:20,  c:4, s:'5 бил'},
    {t:'service', v:'listing', w:8,   c:4, s:'Буст'},
    {t:'tier',    v:'PRO',     w:2,   c:4, s:'PRO'}
  ],
  /* крутка за 300 ₽ */
  p300: [
    {t:'money',   v:200,       w:200, c:1, s:'200 ₽'},
    {t:'ticket',  v:5,         w:170, c:1, s:'5 бил'},
    {t:'money',   v:300,       w:140, c:2, s:'300 ₽'},
    {t:'check',   v:3,         w:100, c:2, s:'3 пров'},
    {t:'disc',    v:20,        w:95,  c:2, s:'−20%'},
    {t:'money',   v:500,       w:70,  c:3, s:'500 ₽'},
    {t:'boost',   v:3,         w:55,  c:3, s:'×3'},
    {t:'disc',    v:30,        w:40,  c:3, s:'−30%'},
    {t:'money',   v:1000,      w:35,  c:4, s:'1000 ₽'},
    {t:'service', v:'priority',w:22,  c:4, s:'Топ'},
    {t:'tier',    v:'PRO',     w:15,  c:4, s:'PRO'},
    {t:'disc',    v:50,        w:8,   c:4, s:'−50%'}
  ],
  /* крутка за 1000 ₽ */
  p1000: [
    {t:'money',   v:500,       w:190, c:1, s:'500 ₽'},
    {t:'ticket',  v:10,        w:150, c:1, s:'10 бил'},
    {t:'money',   v:1000,      w:130, c:2, s:'1000 ₽'},
    {t:'disc',    v:30,        w:95,  c:2, s:'−30%'},
    {t:'check',   v:5,         w:85,  c:2, s:'5 пров'},
    {t:'boost',   v:5,         w:60,  c:3, s:'×5'},
    {t:'money',   v:2000,      w:55,  c:3, s:'2000 ₽'},
    {t:'disc',    v:50,        w:45,  c:3, s:'−50%'},
    {t:'service', v:'campaign',w:40,  c:4, s:'Кампания'},
    {t:'tier',    v:'PRO',     w:30,  c:4, s:'PRO'},
    {t:'tier',    v:'BUSINESS',w:12,  c:4, s:'BIZ'},
    {t:'money',   v:5000,      w:8,   c:4, s:'5000 ₽'}
  ]
};
const GM_STAKES = {
  p100:  {cost:100,  tk:1},
  p300:  {cost:300,  tk:3},
  p1000: {cost:1000, tk:10}
};

/* палитра секторов по «редкости», зависит от темы: [фон, подпись] */
const GM_POOL_COLS_DARK = [
  ['#16220b','#a9c47f'], ['#20340a','#c9ff70'], ['#33520a','#e4ffbf'], ['#5f9c0d','#0c1400'], ['#9AFF00','#0a1400']
];
/* светлая тема: секторы углублены, чтобы колесо не «выцветало» на почти-белом столе, при этом подписи тёмные и читаемые */
const GM_POOL_COLS_LIGHT = [
  ['#cfe39a','#2c4708'], ['#b2d76e','#22400a'], ['#8fc23a','#15330a'], ['#6bac16','#0c2000'], ['#9AFF00','#0a1400']
];
function gmIsLight(){ return document.documentElement.dataset.theme === 'light'; }
function gmPoolCols(){ return gmIsLight() ? GM_POOL_COLS_LIGHT : GM_POOL_COLS_DARK; }

/* номинальная ₽-ценность приза — для прозрачного EV и таблицы лидеров */
function gmNominal(p){
  switch(p.t){
    case 'money':   return p.v;
    case 'ticket':  return p.v * 50;
    case 'check':   return p.v * 100;
    case 'disc':    return ({10:200, 20:450, 30:800, 50:1600})[p.v] || p.v * 20;
    case 'boost':   return ({2:120, 3:220, 5:400})[p.v] || 100;
    case 'tier':    return ({PRO:990, BUSINESS:2490})[p.v] || 990;
    case 'service': return ({promo:300, listing:450, priority:700, campaign:1600})[p.v] || 400;
  }
  return 0;
}
function gmPrizeIcon(p){
  switch(p.t){
    case 'money':   return 'money';
    case 'ticket':  return 'gm-ticket';
    case 'check':   return 'gm-checkvid';
    case 'disc':    return 'gm-pct';
    case 'boost':   return 'gm-boost';
    case 'tier':    return 'crown';
    case 'service': return (GM_SERVICES[p.v] || {}).ic || 'gm-gift';
  }
  return 'gm-gift';
}

/* ============================================================
   БЕСПЛАТНАЯ ЕЖЕДНЕВНАЯ КРУТКА — таймер до полуночи
   ============================================================ */
function gmBonusStamp(){ try{ return Number(localStorage.getItem('oko-games-bonus')) || 0; }catch(e){ return 0; } }
function gmBonusClaimedToday(){
  const ts = gmBonusStamp();
  if(!ts) return false;
  const a = new Date(ts), b = new Date();
  return a.getFullYear() === b.getFullYear() && a.getMonth() === b.getMonth() && a.getDate() === b.getDate();
}
function gmBonusLeftMs(){
  const n = new Date();
  return new Date(n.getFullYear(), n.getMonth(), n.getDate() + 1) - n;
}
function gmFmtCd(ms){
  const s = Math.max(0, Math.floor(ms / 1000));
  const p = n => String(n).padStart(2, '0');
  return p(Math.floor(s/3600)) + ':' + p(Math.floor(s%3600/60)) + ':' + p(s%60);
}

/* ============================================================
   СЕРИЯ ВИЗИТОВ (СТРИК) — заходи каждый день, копи серию, получай билеты.
   Хранение 'oko-games-streak' = {n:серия, d:индекс_дня_последней_крутки}.
   Ретеншн-петля: пропуск дня обнуляет серию; вехи 3/7/14/30 дней дают бонус-билеты.
   ============================================================ */
const GM_STREAK_MILE = {3:1, 7:3, 14:6, 30:15}; /* веха(дней): бонус(билетов) */
function gmDayIdx(ts){ const d = ts ? new Date(ts) : new Date(); return Math.round(new Date(d.getFullYear(), d.getMonth(), d.getDate()).getTime() / 864e5); }
function gmStreakGet(){ try{ return JSON.parse(localStorage.getItem('oko-games-streak')) || {n:0, d:null}; }catch(e){ return {n:0, d:null}; } }
function gmStreakSave(s){ try{ localStorage.setItem('oko-games-streak', JSON.stringify(s)); }catch(e){} }
/* «живая» серия для показа: если последняя крутка сегодня или вчера — серия ещё жива */
function gmStreakDisplay(){
  const s = gmStreakGet(), t = gmDayIdx();
  return (s.d === t || s.d === t - 1) ? (s.n || 0) : 0;
}
function gmPluralDay(n){
  const a = Math.abs(n) % 100, b = a % 10;
  if(a > 10 && a < 20) return 'дней';
  if(b > 1 && b < 5) return 'дня';
  if(b === 1) return 'день';
  return 'дней';
}
/* засчитать сегодняшнюю активность в серию (зовётся при БЕСПЛАТНОЙ крутке дня) */
function gmStreakBump(){
  const s = gmStreakGet(), t = gmDayIdx();
  if(s.d === t) return s;                       /* уже засчитано сегодня */
  if(s.d === t - 1) s.n = (s.n || 0) + 1;       /* заходил вчера — серия растёт */
  else s.n = 1;                                 /* был пропуск — серия с начала */
  s.d = t;
  gmStreakSave(s);
  /* веха 7 дней — +2 бесплатных крутки в копилке */
  if(s.n === 7){
    gmExtraFreeSet(gmExtraFreeGet() + 2);
    gmAchUnlock('streak7');
    toast('Серия 7 дней подряд · +2 бесплатных крутки в копилке');
  }
  /* прочие вехи — бонусные билеты */
  const bonus = GM_STREAK_MILE[s.n] || (s.n > 30 && s.n % 7 === 0 ? 5 : 0);
  if(bonus){
    gmTicketsAdd(bonus);
    toast(`Серия ${s.n} ${gmPluralDay(s.n)}! Бонус +${bonus} ${gmPluralTk(bonus)}`);
  }
  gmStreakRender();
  return s;
}
/* календарь последних 7 дней — активные дни закрашены как «сделано» */
function gmStreakCalendar(){
  const s = gmStreakGet(), today = gmDayIdx();
  const now = new Date();
  const names = ['вс','пн','вт','ср','чт','пт','сб'];
  const out = [];
  for(let i = 6; i >= 0; i--){
    const dt = new Date(now.getFullYear(), now.getMonth(), now.getDate() - i);
    const dayIdx = today - i;
    const active = s.d !== null && s.d !== undefined && dayIdx <= s.d && dayIdx > s.d - (s.n || 0);
    out.push({name: names[dt.getDay()], num: dt.getDate(), done: active, isToday: i === 0});
  }
  return out;
}
function gmStreakRender(){
  const el = document.getElementById('gmStreakChip');
  if(!el) return;
  const n = gmStreakDisplay();
  if(n > 0){
    el.style.display = '';
    el.classList.toggle('lit', gmBonusClaimedToday());
    const v = el.querySelector('.gm-streak-n');
    if(v) v.textContent = n;
    el.setAttribute('title', `Серия ${n} ${gmPluralDay(n)} подряд — заходи каждый день за бонусными билетами`);
  }else{
    el.style.display = 'none';
  }
}
/* контекстная подсказка серии + календарь последних 7 дней (Duolingo-style) */
function gmStreakNote(){
  const n = gmStreakDisplay();
  const cal = gmStreakCalendar();
  const cells = cal.map(c=>
    `<div class="gm-cal-cell ${c.done ? 'done' : ''} ${c.isToday ? 'today' : ''}">
      <span class="gm-cal-day">${c.name}</span>
      <span class="gm-cal-dot">${c.done ? I('gm-flame') : c.num}</span>
    </div>`
  ).join('');

  const extra = gmExtraFreeGet();
  let msg;
  if(extra > 0){
    msg = `Бесплатных круток в копилке · <b>${extra}</b>. Жми «Крутить бесплатно»`;
  }else if(n <= 0){
    msg = 'Заходи каждый день · через 7 дней подряд открой +2 бесплатные крутки';
  }else{
    const milestones = [3, 7, 14, 30];
    const next = milestones.find(m => m > n) || (n + (7 - n % 7));
    const reward = next === 7 ? '+2 бесплатные крутки' : `+${GM_STREAK_MILE[next] || 5} ${gmPluralTk(GM_STREAK_MILE[next] || 5)}`;
    msg = `Серия <b>${n}</b> ${gmPluralDay(n)} · ещё ${next - n} ${gmPluralDay(next - n)} до ${reward}`;
  }

  return `<div class="gm-streak-block ${n > 0 ? 'lit' : ''}">
    <div class="gm-cal">${cells}</div>
    <div class="gm-cal-msg">${I('gm-flame')}<span>${msg}</span></div>
  </div>`;
}

/* ============================================================
   КОЛЕСО — построение, режимы, кручение, выдача приза
   ============================================================ */
let gmMode = 'free';                 /* free | p100 | p300 | p1000 */
let gmSpinning = false;
let gmSpinToken = 0;
let gmWheelAngle = 0;
let gmPaidCost = 0;

function gmCurrentPool(){ return GM_POOLS[gmMode] || GM_POOLS.free; }

function gmSecLabel(p){
  return `${I(gmPrizeIcon(p))}<b>${p.s}</b>`;
}
function gmBuildWheel(){
  const w = document.getElementById('gmWheel');
  if(!w) return;
  const pool = gmCurrentPool(), C = gmPoolCols();
  const N = pool.length, seg = 360 / N;
  const stops = pool.map((p,i)=>`${C[p.c][0]} ${i*seg}deg ${(i+1)*seg}deg`).join(',');
  w.style.background = `conic-gradient(from -${seg/2}deg, ${stops})`;
  w.innerHTML = pool.map((p,i)=>
    `<span class="gm-sec" style="color:${C[p.c][1]};transform:translate(-50%,-50%) rotate(${i*seg}deg) translateY(-90px)">${gmSecLabel(p)}</span>`
  ).join('');
}
function gmSetResult(html){
  const el = document.getElementById('gmWheelResult');
  if(el) el.innerHTML = html;
}

/* --- режимы (пилюли ставок + EV + кнопка запуска) --- */
function gmModeSub(m){
  if(m === 'free') return gmBonusClaimedToday() ? 'через ' + gmFmtCd(gmBonusLeftMs()) : 'подарок дня';
  return 'призы крупнее';
}
function gmEvText(m){
  const pool = GM_POOLS[m], tot = pool.reduce((a,p)=>a + p.w, 0);
  const avg = Math.round(pool.reduce((a,p)=>a + p.w * gmNominal(p), 0) / tot);
  const tierW = pool.filter(p=>p.t === 'tier').reduce((a,p)=>a + p.w, 0);
  const pct = Math.round(tierW / tot * 1000) / 10;
  if(m === 'free') return `Подарок дня — каждый сектор в плюс, деньги сразу на кошелёк. Средний приз ≈ ${fmtMoney(avg)}`;
  return `Средний приз ≈ ${fmtMoney(avg)} · приз гарантирован каждую крутку · шанс тарифа ${String(pct).replace('.', ',')}%`;
}
function gmRenderModes(){
  const el = document.getElementById('gmModes');
  if(!el) return;
  const order = ['free','p100','p300','p1000'];
  const label = {free:'Бесплатно', p100:'100 ₽', p300:'300 ₽', p1000:'1000 ₽'};
  const pills = order.map(m=>{
    const on = m === gmMode;
    const claimed = m === 'free' && gmBonusClaimedToday();
    return `<button class="gm-mpill ${on ? 'on' : ''} ${claimed ? 'claimed' : ''}" ${gmSpinning ? 'disabled' : ''} onclick="gmSelectMode('${m}')">
      <span class="gm-mpill-ic">${I(m === 'free' ? 'gm-gift' : 'money')}</span>
      <b>${label[m]}</b>
      <small ${claimed ? 'id="gmFreeCd"' : ''}>${gmModeSub(m)}</small>
    </button>`;
  }).join('');

  let action = '';
  if(gmMode === 'free'){
    const canFree = gmCanFree();
    const extra = gmExtraFreeGet();
    const claimedNoStash = gmBonusClaimedToday() && extra <= 0;
    let label;
    if(!canFree){
      label = `<span class="gm-spin-cd">Бесплатно через <b id="gmFreeCd2">${gmFmtCd(gmBonusLeftMs())}</b></span>`;
    }else if(extra > 0 && gmBonusClaimedToday()){
      label = `Крутить бесплатно · копилка <b>${extra}</b>`;
    }else{
      label = 'Крутить бесплатно';
    }
    action = `<button class="gm-spin ${claimedNoStash || gmSpinning ? 'off' : ''}" onclick="gmDoSpin(false)">${I('gm-gift')}${label}</button>` + gmStreakNote();
  }else{
    const st = GM_STAKES[gmMode], canTk = GM_TICKETS >= st.tk;
    action = `<div class="gm-spin-row">
      <button class="gm-spin ${gmSpinning ? 'off' : ''}" onclick="gmDoSpin(false)">${I('bolt')}Крутить · ${fmtMoney(st.cost)}</button>
      <button class="gm-spin ghost ${canTk && !gmSpinning ? '' : 'off'}" onclick="gmDoSpin(true)">${I('gm-ticket')}${st.tk} ${gmPluralTk(st.tk)}</button>
    </div>`;
  }
  el.innerHTML = `
    <div class="gm-mode-pills">${pills}</div>
    <div class="gm-ev" id="gmEv">${gmEvText(gmMode)}</div>
    ${gmWheelTeaserHtml()}
    ${action}
    ${gmMBProgressHtml()}`;
}
function gmSelectMode(m){
  if(gmSpinning || !GM_POOLS[m]) return;
  gmMode = m;
  gmBuildWheel();
  gmRenderModes();
}

/* --- запуск крутки --- */
function gmDoSpin(payTicket){
  if(gmSpinning){ toast('Колесо ещё крутится'); return; }
  const m = gmMode;
  if(m === 'free'){
    /* если дневной подарок уже сорван — снимаем из копилки бесплатных круток от стрика */
    if(gmBonusClaimedToday()){
      if(!gmExtraFreeUse()){
        toast('Бесплатная крутка сегодня уже была — выбери платную ставку');
        return;
      }
    }
    gmPaidCost = 0;
  }else{
    const st = GM_STAKES[m];
    if(payTicket){
      if(!gmTicketsSpend(st.tk)){ toast(`Нужно ${st.tk} ${gmPluralTk(st.tk)} — заработай их в рулетке`); return; }
      gmPaidCost = 0;
    }else{
      if(!walletCharge(st.cost, 'Крутка рулетки OKO')) return;
      gmPaidCost = st.cost;
      gmUpdateBalance();
    }
  }
  gmSpinRun(m, !!payTicket);
}
function gmSpinRun(mode, payTicket){
  gmSpinning = true;
  const token = ++gmSpinToken;
  gmRenderModes(); /* заблокировать пилюли/кнопки на время раунда */
  gmSetResult(`<span class="gm-run">Колесо крутится…</span>`);
  const rev = document.getElementById('gmReveal');
  if(rev){ rev.className = 'gm-reveal'; rev.innerHTML = ''; }

  const pool = gmCurrentPool();
  const total = pool.reduce((a,p)=>a + p.w, 0);
  let r = Math.random() * total, idx = 0;
  for(let i = 0; i < pool.length; i++){ if(r < pool[i].w){ idx = i; break; } r -= pool[i].w; }
  const seg = 360 / pool.length;

  const wheel = document.getElementById('gmWheel');
  if(!wheel){ gmSpinning = false; gmGrant(pool[idx], mode, payTicket); return; }
  const cur = ((gmWheelAngle % 360) + 360) % 360;
  wheel.style.transition = 'none';
  wheel.style.transform = `rotate(${cur}deg)`;
  void wheel.offsetWidth; /* форс-рефлоу перед новым переходом */

  /* стрелка «щёлкает» по секторам, пока колесо крутится */
  const ptr = document.querySelector('#screen-games .gm-pointer');
  if(ptr) ptr.classList.add('ticking');

  const turns = 6 + Math.floor(Math.random() * 3);
  const jitter = Math.random() * (seg * 0.44) - seg * 0.22; /* внутри сектора, с запасом от границ */
  const delta = ((360 - idx * seg) - cur + 720) % 360;
  const target = cur + turns * 360 + delta + jitter;
  gmWheelAngle = target;
  /* длинный инерционный хвост: быстрый старт, долгое мягкое торможение */
  wheel.style.transition = 'transform 6.4s cubic-bezier(0.16,0.84,0.16,1)';
  wheel.style.transform = `rotate(${target}deg)`;

  /* transitionend может потеряться (смена вкладки/reduced-motion) —
     страховочный таймер; token гарантирует ровно один вызов */
  const settle = ()=>{
    if(token !== gmSpinToken || !gmSpinning) return;
    gmSpinning = false;
    if(ptr) ptr.classList.remove('ticking');
    gmGrant(pool[idx], mode, payTicket);
  };
  wheel.addEventListener('transitionend', settle, {once:true});
  setTimeout(settle, 6700);
}
function gmPrizeSub(p){
  switch(p.t){
    case 'money':   return 'зачислено на кошелёк';
    case 'ticket':  return 'билеты — валюта круток';
    case 'check':   return 'бонусные проверки видео — в «Мои призы»';
    case 'disc':    return 'промокод в «Мои призы» — применится при оплате тарифа';
    case 'boost':   return 'умножит следующий денежный приз в рулетке';
    case 'tier':    return 'тариф активирован на месяц';
    case 'service': return 'услуга активна — код в «Мои призы»';
  }
  return '';
}
function gmGrant(p, mode, payTicket){
  /* бесплатный дневной подарок засчитываем ТОЛЬКО если это была реальная дневная крутка,
     а не расход из копилки от стрика (которая должна быть сверх лимита) */
  if(mode === 'free' && !gmBonusClaimedToday()){
    try{ localStorage.setItem('oko-games-bonus', String(Date.now())); }catch(e){}
    gmStreakBump();
  }
  /* общая статистика круток — для Mystery Box, ретеншена и достижений */
  gmSetLastSpin();
  const total = gmSpinTotalInc();
  gmMBSet(gmMBCount() + 1);
  if(total === 1) gmAchUnlock('first');
  if(total >= 10) gmAchUnlock('ten');
  if(total >= 50) gmAchUnlock('fifty');
  const icon = gmPrizeIcon(p);
  const rare = p.c >= 4; /* редчайший сектор — вау-момент «джекпот» */
  if(rare) gmAchUnlock('jackpot');
  if(p.t === 'tier') gmAchUnlock('tier');
  let title = '', prize = null;
  switch(p.t){
    case 'money':{
      const amt = gmApplyBoost(p.v);
      walletAdd(amt, 'Приз рулетки OKO');
      prize = {type:'money', val:amt, status:'done'};
      title = `+ ${fmtMoney(amt)}`; break;
    }
    case 'ticket':
      gmTicketsAdd(p.v); prize = {type:'ticket', val:p.v, status:'done'};
      title = `+ ${p.v} ${gmPluralTk(p.v)}`; break;
    case 'check':
      gmChecksAdd(p.v); prize = {type:'check', val:p.v, status:'active'};
      title = `+ ${p.v} ${gmPluralCheck(p.v)} видео`; break;
    case 'disc':
      prize = {type:'discount', val:p.v, code:gmMakeCode(), status:'active'};
      title = `Скидка −${p.v}% на тариф`; break;
    case 'boost':
      gmBoostSet(p.v); prize = {type:'boost', val:p.v, status:'active'};
      title = `Буст ×${p.v} к призу`; break;
    case 'tier':
      if(typeof PROFILE !== 'undefined'){ PROFILE.tier = p.v; if(typeof renderMyProfile === 'function') renderMyProfile(); }
      prize = {type:'tier', val:p.v, status:'active'};
      title = `Тариф ${p.v} на месяц`; break;
    case 'service':{
      const sv = GM_SERVICES[p.v] || {};
      prize = {type:'service', val:p.v, code:gmMakeCode(), status:'active'};
      title = sv.label || 'Услуга OKO'; break;
    }
  }
  /* доход OKO — маржа платной крутки (за вычетом денежного приза) */
  if(mode !== 'free' && !payTicket && gmPaidCost > 0){
    const moneyOut = p.t === 'money' ? prize.val : 0;
    const net = gmPaidCost - moneyOut;
    if(net > 0) okoEarn(net, 'Игры: рулетка OKO');
  }
  gmPrizeAdd(prize);
  gmPrizeReveal(icon, title, rare);
  gmConfetti(document.querySelector('.gm-wheel-card'), rare);
  if(rare) gmJackpot();
  gmSetResult(`<b class="gm-win">${title}</b><span>${gmPrizeSub(p)}</span>`);
  const val = gmNominal(p);
  if(val > 0) gmLbAddWin(val);

  /* бесплатная крутка израсходована — перевести выбор на платный пул,
     если больше нет доступной бесплатной крутки (ни дневной, ни из копилки) */
  if(mode === 'free' && !gmCanFree()){ gmMode = 'p100'; gmBuildWheel(); }
  gmRenderModes();
  gmPrizesBtnRender();
  gmUpdateBalance();
  gmTicketsRender();
  gmBoostBadge();
  gmAchRender();
  /* MYSTERY BOX — 20 круток подряд открывают тайный сундук с гарантированно крупным призом */
  if(gmMBCount() >= GM_MB_STEP){
    gmMBSet(0);
    setTimeout(gmMBAward, 900); /* даём отыграть основной ревил */
  }
}

/* красивая анимация выпадения приза — карточка в центре колеса */
let gmRevHideT = 0;
function gmPrizeReveal(icon, title, rare){
  const rev = document.getElementById('gmReveal');
  if(!rev) return;
  rev.classList.toggle('rare', !!rare);
  rev.innerHTML = `
    <div class="gm-reveal-rays"></div>
    <div class="gm-reveal-card">
      ${rare ? `<span class="gm-reveal-tag">${I('gm-cup')}Редкий приз</span>` : ''}
      <span class="gm-reveal-ic">${I(icon)}</span>
      <b>${title}</b>
    </div>`;
  rev.classList.remove('show'); void rev.offsetWidth; rev.classList.add('show');
  /* лёгкая вибрация выигрыша на телефоне (звука нет); редкий приз — насыщеннее */
  try{ if(navigator.vibrate) navigator.vibrate(rare ? [18, 50, 22, 50, 40] : [12, 40, 22]); }catch(e){}
  /* показать праздник, затем плавно вернуть колесо готовым к следующей крутке */
  const tok = gmSpinToken;
  clearTimeout(gmRevHideT);
  gmRevHideT = setTimeout(()=>{
    if(tok !== gmSpinToken || gmSpinning) return; /* уже крутят снова — не мешаем */
    const r = document.getElementById('gmReveal');
    if(r) r.classList.remove('show');
  }, 3200);
}

/* лаймовое конфетти (rare — гуще и с золотыми искрами для джекпота) */
function gmConfetti(host, rare){
  if(!host) return;
  const box = document.createElement('div');
  box.className = 'gm-confetti' + (rare ? ' rare' : '');
  const cols = rare
    ? ['#9AFF00','#c9ff70','#eaffcc','#ffe873','#ffd23f','#ffffff']
    : ['#9AFF00','#c9ff70','#6fb50d','#eaffcc','#ffffff'];
  const N = rare ? 46 : 28;
  let h = '';
  for(let i=0;i<N;i++){
    h += `<i style="left:${(Math.random()*100).toFixed(1)}%;background:${cols[i%cols.length]};animation-delay:${(Math.random()*0.4).toFixed(2)}s;animation-duration:${(0.9+Math.random()*0.9).toFixed(2)}s"></i>`;
  }
  box.innerHTML = h;
  host.appendChild(box);
  setTimeout(()=>box.remove(), 2400);
}

/* джекпот — редкий приз: золотая вспышка стола + доп. вибрация */
function gmJackpot(){
  const card = document.querySelector('#screen-games .gm-wheel-card');
  if(!card) return;
  card.classList.remove('gm-jackpot'); void card.offsetWidth; card.classList.add('gm-jackpot');
  setTimeout(()=>card.classList.remove('gm-jackpot'), 1500);
}

/* ================= ТАБЛИЦА ЛИДЕРОВ НЕДЕЛИ =================
   Топ-10: мок-игроки + свой ник. Свои призы реально суммируются (номинал ₽)
   и двигают позицию. Персист под 'oko-games-lb', сброс каждую неделю. */
const GM_LB_NAMES = []  /* демо-данные удалены 09.08: боты таблицы лидеров — источник только API */;
let GM_LB = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-games-lb'))||null; }catch(e){ return null; } })();
/* На устройствах, где приложение уже запускалось, таблица лидеров с ботами
   лежит в localStorage и переживает удаление сидов: gmLbEnsure() не трогает
   запись, если неделя совпадает. Разовая чистка — иначе Даниэль снова увидел
   бы выдуманных игроков после «удаления демо-данных». */
(function gmWipeDemoLb(){
  try{
    if(localStorage.getItem('oko-lb-reset-v1') === '1') return;
    localStorage.removeItem('oko-games-lb');
    Object.keys(localStorage).forEach(function(k){
      if(k.indexOf('oko-games-lb-') === 0) localStorage.removeItem(k);
    });
    localStorage.setItem('oko-lb-reset-v1', '1');
    GM_LB = null;
  }catch(e){}
})();

function gmWeekKey(){
  const d = new Date();
  const jan1 = new Date(d.getFullYear(), 0, 1);
  const week = Math.ceil(((d - jan1) / 864e5 + jan1.getDay() + 1) / 7);
  return d.getFullYear() + '-W' + week;
}
function gmLbSave(){ try{ localStorage.setItem('oko-games-lb', JSON.stringify(GM_LB)); }catch(e){} }
function gmLbEnsure(){
  const wk = gmWeekKey();
  if(GM_LB && GM_LB.week === wk && Array.isArray(GM_LB.bots)) return;
  const pool = GM_LB_NAMES.slice();
  for(let i = pool.length - 1; i > 0; i--){
    const j = Math.floor(Math.random() * (i + 1));
    [pool[i], pool[j]] = [pool[j], pool[i]];
  }
  GM_LB = {
    week: wk, my: 0, myW: 0, move: null,
    bots: pool.slice(0,10).map(n=>({n, s: Math.round((300 + Math.random()*8200)/10)*10, w: 2 + Math.floor(Math.random()*34)}))
  };
  gmLbSave();
}
function gmLbRank(){
  gmLbEnsure();
  const rows = GM_LB.bots.map(b=>({n: b.n, s: b.s, w: b.w, me: false}));
  rows.push({n: PROFILE.name, s: Math.round(GM_LB.my*100)/100, w: GM_LB.myW, me: true});
  rows.sort((a,b)=>b.s - a.s);
  return rows;
}
function gmLbMyRank(){ return gmLbRank().findIndex(r=>r.me) + 1; }
function gmLbAddWin(sum){
  gmLbEnsure();
  const before = gmLbMyRank();
  GM_LB.my += sum;
  GM_LB.myW++;
  const after = gmLbMyRank();
  if(after < before){ GM_LB.move = 'up'; toast(`Лидеры недели: ты поднялся на ${after}-е место`); }
  else if(after > before) GM_LB.move = 'down';
  gmLbSave();
  gmLbRender();
  if(GM_LB.my >= 5000 && typeof gmAchUnlock === 'function') gmAchUnlock('rich');
}
/* Раньше здесь «оживала» таблица: случайный мок-игрок время от времени
   набирал очки. Мок-игроков больше нет (правка Даниэля: ноль выдуманных
   людей), поэтому дрейфовать нечему — и без этой проверки функция падала
   на пустом массиве при каждом переходе на вкладку. */
function gmLbDrift(){
  gmLbEnsure();
  if(!GM_LB || !Array.isArray(GM_LB.bots) || !GM_LB.bots.length) return;
  if(Math.random() < 0.45){
    const b = GM_LB.bots[Math.floor(Math.random() * GM_LB.bots.length)];
    b.s += Math.round((40 + Math.random()*550)/10)*10;
    b.w++;
    gmLbSave();
  }
}
function gmLbRow(r, pos){
  const mv = r.me && GM_LB.move ? `<svg class="i gm-lb-mv ${GM_LB.move}"><use href="#i-chev"/></svg>` : '';
  const posHtml = pos === 1 ? `<b class="gm-lb-pos p1">${I('gm-cup')}</b>` : `<b class="gm-lb-pos ${pos<=3?'p'+pos:''}">${pos}</b>`;
  return `<div class="gm-lb-row ${r.me ? 'me' : ''}">
    ${posHtml}
    <span class="gm-lb-ava">${esc((r.n[0]||'?').toUpperCase())}</span>
    <div class="gm-lb-b">
      <span class="gm-lb-n">${esc(r.n)}${r.me ? vBadge(r.n) : ''}${r.me ? '<i class="gm-lb-you">ты</i>' : ''}${mv}</span>
      <small>${r.w} ${gmPluralPrize(r.w)} за неделю</small>
    </div>
    <b class="gm-lb-sum">${fmtMoney(r.s)}</b>
  </div>`;
}
function gmLbRender(){
  const el = document.getElementById('gmLb');
  if(!el) return;
  const rows = gmLbRank();
  const myIdx = rows.findIndex(r=>r.me);
  const days = 7 - ((new Date().getDay() + 6) % 7);
  let html = `<div class="gm-lb-head"><span>${I('gm-cup')}Призы за эту неделю</span><small>сброс через ${days} ${days === 1 ? 'день' : days < 5 ? 'дня' : 'дней'}</small></div>`;
  html += rows.slice(0, 10).map((r,i)=>gmLbRow(r, i+1)).join('');
  if(myIdx >= 10) html += `<div class="gm-lb-gap">···</div>` + gmLbRow(rows[myIdx], myIdx + 1);
  el.innerHTML = html;
}

/* ---------- баннер «Мои призы» ---------- */
function gmPrizesBtnRender(){
  const b = document.getElementById('gmPrizesBtn');
  if(b){
    const n = GM_PRIZES.length, act = gmPrizesActiveCount();
    b.innerHTML = `${I('gm-cup')}<div class="gm-bonus-t"><span>Мои призы</span><small>${n ? `${n} ${gmPluralPrize(n)} · ${act} к использованию` : 'ещё нет — крути рулетку'}</small></div>${I('chev','gm-bonus-chev')}`;
  }
  document.querySelectorAll('.gm-prizes-cnt').forEach(e=>{ e.textContent = GM_PRIZES.length; });
}

/* ---------- «Мои призы»: шит выпавших призов ---------- */
function gmPrizesOpen(){ openSheet('gmPrizes'); gmPrizesRender(); }
function gmPrizeRow(p){
  let ic = 'gm-gift', cat = 'Приз', title = '', extra = '', badge = '', bcls = 'active';
  switch(p.type){
    case 'money':  ic='money';       cat='Деньги';         title=`+ ${fmtMoney(p.val)}`;            badge='Начислено'; bcls='done'; break;
    case 'ticket': ic='gm-ticket';   cat='Билеты';         title=`+ ${p.val} ${gmPluralTk(p.val)}`; badge='Начислено'; bcls='done'; break;
    case 'check':  ic='gm-checkvid'; cat='Проверки видео'; title=`${p.val} ${gmPluralCheck(p.val)}`; badge=p.status==='used'?'Использовано':'Доступно'; bcls=p.status==='used'?'used':'active'; break;
    case 'discount':
      ic='gm-pct'; cat='Скидка на тариф'; title=`Скидка −${p.val}% на тариф`;
      if(p.status==='active'){ badge='Активна'; bcls='active'; extra=`<button class="gm-code" onclick="gmCopyCode('${p.code}')">${p.code}${I('copy')}</button>`; }
      else { badge='Использована'; bcls='used'; extra=`<span class="gm-code used">${p.code}</span>`; }
      break;
    case 'boost':  ic='gm-boost'; cat='Буст к призу'; title=`Буст ×${p.val} к призу`; badge=p.status==='used'?'Использован':'Активен'; bcls=p.status==='used'?'used':'active'; break;
    case 'tier':   ic='crown';    cat='Тариф на месяц'; title=`Тариф ${p.val} на месяц`; badge='Активен'; bcls='active'; break;
    case 'service':{
      const sv = GM_SERVICES[p.val] || {};
      ic = sv.ic || 'gm-gift'; cat = 'Услуга OKO'; title = sv.label || 'Услуга OKO';
      if(p.status==='active'){ badge='Активна'; bcls='active'; extra=`<button class="gm-code" onclick="gmCopyCode('${p.code}')">${p.code}${I('copy')}</button>`; }
      else { badge='Использована'; bcls='used'; extra=`<span class="gm-code used">${p.code}</span>`; }
      break;
    }
  }
  const d = new Date(p.at);
  const time = d.toLocaleDateString('ru-RU', {day:'2-digit', month:'2-digit'}) + ' ' +
               d.toLocaleTimeString('ru-RU', {hour:'2-digit', minute:'2-digit'});
  return `<div class="gm-prize ${bcls}">
    <span class="gm-prize-ic p-${p.type}">${I(ic)}</span>
    <div class="gm-prize-b">
      <span class="gm-prize-t">${title}</span>
      <span class="gm-prize-s">${cat} · ${time}</span>
      ${extra}
    </div>
    <span class="gm-prize-badge b-${bcls}">${badge}</span>
  </div>`;
}
function gmPrizesRender(){
  const el = document.getElementById('gmPrizesBody');
  if(!el) return;
  const checks = gmChecksGet();
  const discs = GM_PRIZES.filter(p=>p.type === 'discount' && p.status === 'active').length;
  const head = `<div class="gm-prz-stats">
    <div class="gm-prz-stat"><span class="gm-prz-sic">${I('gm-ticket')}</span><b class="gm-tk-val">${GM_TICKETS}</b><small>${gmPluralTk(GM_TICKETS)}</small></div>
    <div class="gm-prz-stat"><span class="gm-prz-sic">${I('gm-checkvid')}</span><b>${checks}</b><small>проверок</small></div>
    <div class="gm-prz-stat"><span class="gm-prz-sic">${I('gm-pct')}</span><b>${discs}</b><small>скидок</small></div>
  </div>`;
  if(!GM_PRIZES.length){
    el.innerHTML = head +
      `<div class="gm-empty">${I('gm-gift')}<span>Призов пока нет — крути рулетку: выпадают билеты, скидки на тариф, проверки видео, услуги OKO, бусты и деньги</span></div>
       <button class="gm-spin" onclick="closeSheet();showTab('games')">${I('gm-gift')}К рулетке</button>`;
    return;
  }
  el.innerHTML = head + `<div class="gm-prz-list">${GM_PRIZES.map(gmPrizeRow).join('')}</div>`;
}
function gmCopyCode(code){
  const done = ()=>toast('Код ' + code + ' скопирован — вставь при оплате тарифа');
  try{
    if(navigator.clipboard && navigator.clipboard.writeText){ navigator.clipboard.writeText(code).then(done, done); return; }
  }catch(e){}
  done();
}

/* ============================================================
   ТАБЛИЦА ШАНСОВ — честная механика: точный % каждого сектора по каждому пулу.
   Прозрачность уровня «provably fair»: игрок видит вероятности до крутки.
   ============================================================ */
let gmOddsMode = 'free';
function gmOddsOpen(){ gmOddsMode = (gmMode && GM_POOLS[gmMode]) ? gmMode : 'free'; openSheet('gmOdds'); gmOddsRender(); }
function gmOddsSetMode(m){ if(GM_POOLS[m]){ gmOddsMode = m; gmOddsRender(); } }
function gmOddsRender(){
  const el = document.getElementById('gmOddsBody');
  if(!el) return;
  const order = ['free','p100','p300','p1000'];
  const label = {free:'Бесплатно', p100:'100 ₽', p300:'300 ₽', p1000:'1000 ₽'};
  const pool = GM_POOLS[gmOddsMode] || GM_POOLS.free;
  const total = pool.reduce((a,p)=>a + p.w, 0);
  const avg = Math.round(pool.reduce((a,p)=>a + p.w * gmNominal(p), 0) / total);
  const C = gmPoolCols();

  const tabs = order.map(m=>
    `<button class="gm-odds-tab ${m === gmOddsMode ? 'on' : ''}" onclick="gmOddsSetMode('${m}')">${label[m]}</button>`
  ).join('');

  /* сортировка по убыванию шанса — самые частые сверху */
  const rows = pool.map((p,i)=>({p, pct: p.w / total * 100})).sort((a,b)=>b.pct - a.pct);
  const maxPct = rows[0].pct || 1;
  const body = rows.map(({p, pct})=>{
    const shown = pct >= 9.95 ? Math.round(pct) : Math.round(pct * 10) / 10;
    const txt = String(shown).replace('.', ',') + '%';
    return `<div class="gm-odds-row">
      <span class="gm-odds-ic" style="color:${C[p.c][1]};background:${C[p.c][0]}">${I(gmPrizeIcon(p))}</span>
      <div class="gm-odds-b">
        <span class="gm-odds-t">${p.s}</span>
        <span class="gm-odds-bar"><i style="width:${Math.max(4, pct / maxPct * 100).toFixed(1)}%"></i></span>
      </div>
      <b class="gm-odds-pct">${txt}</b>
    </div>`;
  }).join('');

  el.innerHTML = `
    <div class="gm-odds-tabs">${tabs}</div>
    <div class="gm-odds-note">${I('gm-scales')}<span>${
      gmOddsMode === 'free'
        ? 'Подарок дня — каждый сектор в плюс. Шансы честные и неизменные.'
        : 'Приз гарантирован каждую крутку. Шансы фиксированы и не зависят от прошлых круток.'
    } Средний приз ≈ <b>${fmtMoney(avg)}</b>.</span></div>
    <div class="gm-odds-list">${body}</div>
    <div class="gm-odds-sum">Сумма всех шансов — 100%. Итог выбирается честным случайным числом на устройстве.</div>`;
}

/* живой обратный отсчёт до полуночи (пилюля + кнопка «Бесплатно») */
setInterval(()=>{
  if(!gmBonusClaimedToday()) return;
  const cd = gmFmtCd(gmBonusLeftMs());
  ['gmFreeCd','gmFreeCd2'].forEach(id=>{ const e = document.getElementById(id); if(e) e.textContent = cd; });
}, 1000);

/* ============================================================
   БЕСПЛАТНЫЕ КРУТКИ В КОПИЛКЕ (стрик 7 дней = +2 бесплатных крутки)
   ============================================================ */
function gmExtraFreeGet(){ try{ return Math.max(0, parseInt(localStorage.getItem('oko-games-extrafree'),10) || 0); }catch(e){ return 0; } }
function gmExtraFreeSet(n){
  try{
    if(n > 0) localStorage.setItem('oko-games-extrafree', String(n));
    else localStorage.removeItem('oko-games-extrafree');
  }catch(e){}
}
function gmExtraFreeUse(){
  const n = gmExtraFreeGet();
  if(n <= 0) return false;
  gmExtraFreeSet(n - 1);
  return true;
}
function gmCanFree(){ return !gmBonusClaimedToday() || gmExtraFreeGet() > 0; }

/* ============================================================
   СТАТИСТИКА КРУТОК — для Mystery Box, ретеншена, достижений
   ============================================================ */
function gmSpinTotal(){ try{ return Math.max(0, parseInt(localStorage.getItem('oko-games-spins-total'),10) || 0); }catch(e){ return 0; } }
function gmSpinTotalInc(){
  const n = gmSpinTotal() + 1;
  try{ localStorage.setItem('oko-games-spins-total', String(n)); }catch(e){}
  return n;
}
function gmLastSpin(){ try{ return parseInt(localStorage.getItem('oko-games-lastspin'),10) || 0; }catch(e){ return 0; } }
function gmSetLastSpin(){ try{ localStorage.setItem('oko-games-lastspin', String(Date.now())); }catch(e){} }

/* ============================================================
   MYSTERY BOX — особый приз каждые 20 круток (loot crate)
   Пул только премиум-призов, гарантированно крупнее среднего.
   ============================================================ */
const GM_MB_STEP = 20;
const GM_MB_POOL = [
  {t:'ticket',  v:10,          w:200},
  {t:'check',   v:5,           w:170},
  {t:'disc',    v:30,          w:130},
  {t:'ticket',  v:20,          w:110},
  {t:'boost',   v:5,           w:90},
  {t:'service', v:'priority',  w:75},
  {t:'disc',    v:50,          w:55},
  {t:'tier',    v:'PRO',       w:45},
  {t:'service', v:'campaign',  w:35}
];
function gmMBCount(){ try{ return Math.max(0, parseInt(localStorage.getItem('oko-games-mbspins'),10) || 0); }catch(e){ return 0; } }
function gmMBSet(n){ try{ localStorage.setItem('oko-games-mbspins', String(Math.max(0, n|0))); }catch(e){} }
function gmMBPluralSpin(n){
  const a = Math.abs(n) % 100, b = a % 10;
  if(a > 10 && a < 20) return 'круток';
  if(b > 1 && b < 5) return 'крутки';
  if(b === 1) return 'крутка';
  return 'круток';
}
function gmMBProgressHtml(){
  const cur = Math.min(GM_MB_STEP, gmMBCount());
  const pct = cur / GM_MB_STEP * 100;
  const left = Math.max(0, GM_MB_STEP - cur);
  const ready = left === 0;
  return `<div class="gm-mb-progress ${ready ? 'ready' : ''}">
    <div class="gm-mb-p-head">
      <span>${I('gm-mbox')}Тайный сундук</span>
      <b>${cur}/${GM_MB_STEP}</b>
    </div>
    <div class="gm-mb-p-bar"><i style="width:${pct}%"></i></div>
    <small>${ready ? 'Готов к открытию · следующая крутка откроет сундук' : `Ещё ${left} ${gmMBPluralSpin(left)} до особого приза`}</small>
  </div>`;
}
function gmMBAward(){
  const pool = GM_MB_POOL;
  const tot = pool.reduce((a,p)=>a + p.w, 0);
  let r = Math.random() * tot, idx = 0;
  for(let i = 0; i < pool.length; i++){ if(r < pool[i].w){ idx = i; break; } r -= pool[i].w; }
  const p = pool[idx];
  let title = '', prize = null;
  switch(p.t){
    case 'ticket':
      gmTicketsAdd(p.v); prize = {type:'ticket', val:p.v, status:'done'};
      title = `+ ${p.v} ${gmPluralTk(p.v)}`; break;
    case 'check':
      gmChecksAdd(p.v); prize = {type:'check', val:p.v, status:'active'};
      title = `+ ${p.v} ${gmPluralCheck(p.v)} видео`; break;
    case 'disc':
      prize = {type:'discount', val:p.v, code:gmMakeCode(), status:'active'};
      title = `Скидка −${p.v}% на тариф`; break;
    case 'boost':
      gmBoostSet(p.v); prize = {type:'boost', val:p.v, status:'active'};
      title = `Буст ×${p.v} к призу`; break;
    case 'service':{
      const sv = GM_SERVICES[p.v] || {};
      prize = {type:'service', val:p.v, code:gmMakeCode(), status:'active'};
      title = sv.label || 'Услуга OKO'; break;
    }
    case 'tier':
      if(typeof PROFILE !== 'undefined'){ PROFILE.tier = p.v; if(typeof renderMyProfile === 'function') renderMyProfile(); }
      prize = {type:'tier', val:p.v, status:'active'};
      title = `Тариф ${p.v} на месяц`;
      gmAchUnlock('tier'); break;
  }
  gmPrizeAdd(prize);
  gmAchUnlock('mbox');
  const val = gmNominal(p);
  if(val > 0) gmLbAddWin(val);
  gmMBReveal(gmPrizeIcon(p), title);
  gmRenderModes(); /* обновить прогресс сундука на 0/20 */
}
function gmMBReveal(icon, title){
  const host = document.getElementById('screen-games');
  if(!host) return;
  const el = document.createElement('div');
  el.className = 'gm-mb-overlay';
  el.innerHTML = `
    <div class="gm-mb-scrim" onclick="gmMBClose(this)"></div>
    <div class="gm-mb-modal" role="dialog" aria-modal="true">
      <span class="gm-mb-tag">${I('gm-cup')}Тайный сундук</span>
      <div class="gm-mb-crate">
        <span class="gm-mb-crate-glow"></span>
        <span class="gm-mb-crate-ic">${I('gm-mbox')}</span>
      </div>
      <div class="gm-mb-prize">
        <span class="gm-mb-prize-ic">${I(icon)}</span>
        <b>${title}</b>
      </div>
      <small class="gm-mb-sub">Награда за ${GM_MB_STEP} круток · уже в «Мои призы»</small>
      <button class="gm-spin" onclick="gmMBClose(this)">${I('gm-gift')}Забрать</button>
    </div>`;
  host.appendChild(el);
  requestAnimationFrame(()=>el.classList.add('open'));
  gmConfetti(el.querySelector('.gm-mb-modal'), true);
  try{ if(navigator.vibrate) navigator.vibrate([30, 60, 30, 60, 90]); }catch(e){}
}
function gmMBClose(node){
  const wrap = node && node.closest ? node.closest('.gm-mb-overlay') : null;
  if(wrap){ wrap.classList.remove('open'); setTimeout(()=>wrap.remove(), 260); }
}

/* ============================================================
   ДОСТИЖЕНИЯ — 8 бейджей за активность в рулетке
   ============================================================ */
const GM_ACH = [
  {id:'first',   ic:'gm-gift',    t:'Первый спин',    s:'Крути рулетку впервые'},
  {id:'ten',     ic:'bolt',       t:'10 круток',      s:'Разогрев начат'},
  {id:'fifty',   ic:'gm-flame',   t:'50 круток',      s:'Постоянный игрок'},
  {id:'jackpot', ic:'gm-cup',     t:'Джекпот',        s:'Поймать редкий сектор'},
  {id:'streak7', ic:'gm-cal',     t:'Неделя подряд',  s:'Серия из 7 дней'},
  {id:'mbox',    ic:'gm-mbox',    t:'Тайный сундук',  s:'Открыть Mystery Box'},
  {id:'rich',    ic:'money',      t:'Богач недели',   s:'5000 рублей призов за неделю'},
  {id:'tier',    ic:'crown',      t:'Тарифный удар',  s:'Выиграть тариф в рулетке'}
];
function gmAchGet(){ try{ return JSON.parse(localStorage.getItem('oko-games-ach')) || {}; }catch(e){ return {}; } }
function gmAchSave(a){ try{ localStorage.setItem('oko-games-ach', JSON.stringify(a)); }catch(e){} }
function gmAchUnlock(id){
  const a = gmAchGet();
  if(a[id]) return false;
  a[id] = Date.now();
  gmAchSave(a);
  const info = GM_ACH.find(x=>x.id === id);
  if(info) toast(`Достижение · ${info.t}`);
  gmAchRender();
  return true;
}
function gmAchRender(){
  const el = document.getElementById('gmAch');
  if(!el) return;
  const a = gmAchGet();
  const done = GM_ACH.filter(x=>a[x.id]).length;
  el.innerHTML = GM_ACH.map(ach=>{
    const on = !!a[ach.id];
    return `<div class="gm-ach ${on ? 'on' : ''}">
      <span class="gm-ach-ic">${I(ach.ic)}</span>
      <b>${ach.t}</b>
      <small>${ach.s}</small>
      ${on ? `<span class="gm-ach-check">${I('check')}</span>` : ''}
    </div>`;
  }).join('');
  const head = document.getElementById('gmAchHead');
  if(head) head.textContent = `${done}/${GM_ACH.length}`;
}

/* ============================================================
   RETENTION-БАННЕР — если last-spin > 24ч и клиент вернулся,
   а бесплатная крутка снова доступна, показать «С возвращением»
   ============================================================ */
function gmRetentionSeenKey(){ return 'oko-games-retseen-' + gmDayIdx(); }
function gmRetentionRender(){
  const el = document.getElementById('gmRetention');
  if(!el) return;
  const last = gmLastSpin();
  const gap = last ? (Date.now() - last) / 3600e3 : 0;
  let seen = false;
  try{ seen = !!localStorage.getItem(gmRetentionSeenKey()); }catch(e){}
  if(last && gap > 24 && !gmBonusClaimedToday() && !seen){
    el.innerHTML = `<div class="gm-ret">
      <span class="gm-ret-ic">${I('gm-gift')}</span>
      <div class="gm-ret-b"><b>С возвращением</b><small>Бесплатная крутка ждёт тебя в колесе</small></div>
      <button class="gm-ret-cta" onclick="gmSelectMode('free');gmRetentionDismiss()">Крутить</button>
      <button class="gm-ret-x" onclick="gmRetentionDismiss()" aria-label="Закрыть">×</button>
    </div>`;
  }else{
    el.innerHTML = '';
  }
}
function gmRetentionDismiss(){
  try{ localStorage.setItem(gmRetentionSeenKey(), '1'); }catch(e){}
  gmRetentionRender();
}

/* ============================================================
   WHEEL-TEASER — три самых крупных приза из текущего пула,
   чтобы клиент видел ЧТО можно получить ещё до крутки
   ============================================================ */
function gmWheelTeaserHtml(){
  const pool = gmCurrentPool();
  const top = pool.slice().sort((a,b)=>gmNominal(b) - gmNominal(a)).slice(0, 3);
  const cells = top.map(p=>
    `<div class="gm-teaser-cell">
      <span class="gm-teaser-ic">${I(gmPrizeIcon(p))}</span>
      <b>${p.s}</b>
    </div>`
  ).join('');
  return `<div class="gm-teaser">
    <small>В колесе прямо сейчас · крупные призы</small>
    <div class="gm-teaser-row">${cells}</div>
  </div>`;
}

/* ---------- патчи ядра (прежние версии сохранены и вызываются) ---------- */
const _prevWalletAddGm = walletAdd;
walletAdd = function(sum, why){
  _prevWalletAddGm(sum, why);
  gmUpdateBalance();
};
const _prevWalletChargeGm = walletCharge;
walletCharge = function(sum, why){
  const ok = _prevWalletChargeGm(sum, why);
  gmUpdateBalance();
  return ok;
};
/* пересобрать колесо под новую тему (палитра секторов задаётся из JS) */
if(typeof applyTheme === 'function'){
  const _prevApplyThemeGm = applyTheme;
  applyTheme = function(t){
    _prevApplyThemeGm(t);
    if(!gmSpinning) gmBuildWheel();
    if(document.getElementById('sheet-gmPrizes') && document.getElementById('sheet-gmPrizes').classList.contains('open')) gmPrizesRender();
  };
}

const _prevShowTabGm = showTab;
showTab = function(t){
  _prevShowTabGm(t);
  if(t === 'games'){
    gmUpdateBalance();
    gmLbDrift();      /* таблица лидеров живёт: мок-игроки тоже выигрывают */
    gmLbRender();
    gmPrizesBtnRender();
    gmTicketsRender();
    gmBoostBadge();
    gmStreakRender();
    gmAchRender();
    gmRetentionRender();
    if(!gmSpinning){
      /* автопереключение на платный пул только если бесплатной крутки правда нет */
      if(gmMode === 'free' && !gmCanFree()) gmMode = 'p100';
      gmBuildWheel();
      gmRenderModes();
    }
  }
};

/* ---------- самоинициализация (все новые модули поднимаются ниже, после определения) ---------- */
regTitle('games', 'Рулетка OKO');
addSvcTile({id:'games', label:'Рулетка OKO', ico:'gm-gift', first:true, onclick:()=>showTab('games')});
gmMode = gmCanFree() ? 'free' : 'p100';
gmBuildWheel();
gmRenderModes();
gmLbEnsure();
gmLbRender();
gmPrizesBtnRender();
gmTicketsRender();
gmBoostBadge();
gmStreakRender();
gmAchRender();
gmRetentionRender();
gmUpdateBalance();

/* ---------- подхват призовой скидки в оплате тарифа ----------
   paywall грузится ПОСЛЕ games и полностью заменяет renderPay (pwRenderPay),
   поэтому оборачиваем ИТОГОВЫЙ renderPay/doPay отложенно (после всех модулей). */
function gmApplyDiscountToPay(){
  const view = document.getElementById('payView');
  if(!view) return;
  const old = view.querySelector('.gm-pay-promo');
  if(old) old.remove();
  const d = gmActiveDiscount();
  if(!d) return;
  const sumEl = view.querySelector('.pay-sum');
  if(!sumEl) return;
  const base = parseInt(sumEl.textContent.replace(/[^\d]/g, ''), 10) || 0;
  const now = Math.round(base * (1 - d.val / 100));
  const fmt = (typeof fmtRub === 'function') ? fmtRub : fmtMoney;
  sumEl.textContent = fmt(now);
  const btn = Array.from(view.querySelectorAll('button.btn, button')).find(b=>/Оплатить/.test(b.textContent));
  if(btn) btn.innerHTML = I('lock') + ' Оплатить ' + fmt(now);
  const total = view.querySelector('.pay-total');
  if(total && total.parentNode){
    const badge = document.createElement('div');
    badge.className = 'gm-pay-promo';
    badge.innerHTML = `${I('gm-pct')}<span>Промокод из рулетки <b>${d.code}</b> · дополнительно −${d.val}%</span>`;
    total.parentNode.insertBefore(badge, total);
  }
}
setTimeout(function gmPatchPay(){
  if(typeof renderPay === 'function'){
    const _prevRenderPayGm = renderPay;
    renderPay = function(){ _prevRenderPayGm.apply(this, arguments); gmApplyDiscountToPay(); };
  }
  if(typeof doPay === 'function'){
    const _prevDoPayGm = doPay;
    doPay = function(){
      const d = gmActiveDiscount();
      _prevDoPayGm.apply(this, arguments);
      if(d) gmPrizeUse(d.id); /* скидка израсходована на покупку */
    };
  }
}, 0);

/* ============================================================
   ДОПОЛНИТЕЛЬНЫЕ SVG-ИКОНКИ (подарки, мини-игры, лиги, чарт)
   ============================================================ */
(function gmIcons2(){
  const defs = document.querySelector('svg defs');
  if(!defs) return;
  const mk = (id, inner, vb)=>{
    if(defs.querySelector('#'+id)) return;
    const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
    s.setAttribute('id', id); s.setAttribute('viewBox', vb || '0 0 100 100');
    s.innerHTML = inner; defs.appendChild(s);
  };
  /* Подарки для магазина TON */
  mk('i-gm-rose','<path d="M50 20a14 14 0 00-14 14c0 8 6 14 14 14s14-6 14-14a14 14 0 00-14-14z" fill="none" stroke="currentColor" stroke-width="6"/><path d="M50 48v34M40 62c-14-2-22 6-24 20 12-2 22-8 24-20zM60 62c14-2 22 6 24 20-12-2-22-8-24-20z" fill="none" stroke="currentColor" stroke-width="5.5" stroke-linejoin="round" stroke-linecap="round"/><path d="M50 34l4 6-4 4-4-4z" fill="currentColor" stroke="none"/>');
  mk('i-gm-diamond','<path d="M50 12 20 42l30 46 30-46L50 12z" fill="none" stroke="currentColor" stroke-width="6.5" stroke-linejoin="round"/><path d="M20 42h60M50 12v76M35 42l15-30M65 42L50 12M35 42l15 46M65 42 50 88" fill="none" stroke="currentColor" stroke-width="4.5"/>');
  mk('i-gm-rocket2','<path d="M50 10c14 12 22 30 22 46l-8 8H36l-8-8c0-16 8-34 22-46z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/><circle cx="50" cy="42" r="8" fill="none" stroke="currentColor" stroke-width="6"/><path d="M36 68l-14 20 22-6M64 68l14 20-22-6" fill="none" stroke="currentColor" stroke-width="5.5" stroke-linejoin="round"/>');
  mk('i-gm-flame2','<path d="M52 10c2 18 22 22 22 42 0 16-11 30-24 30S26 68 26 52c0-9 5-15 5-15-11 4-17 15-17 27a36 36 0 0072 0c0-28-27-38-34-54z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>');
  mk('i-gm-crown2','<path d="M18 74V36l16 14 16-24 16 24 16-14v38a4 4 0 01-4 4H22a4 4 0 01-4-4z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/><circle cx="34" cy="36" r="4" fill="currentColor" stroke="none"/><circle cx="50" cy="26" r="4" fill="currentColor" stroke="none"/><circle cx="66" cy="36" r="4" fill="currentColor" stroke="none"/>');
  mk('i-gm-cup2','<path d="M30 12h40v22a20 20 0 01-40 0z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/><path d="M30 20H16a12 12 0 0014 14M70 20h14a12 12 0 01-14 14M50 54v14M36 84h28M43 68h14" fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round"/>');
  /* Sad-face для sad-anim (стрик порван) */
  mk('i-gm-sad','<circle cx="50" cy="50" r="38" fill="none" stroke="currentColor" stroke-width="6"/><circle cx="38" cy="43" r="4" fill="currentColor" stroke="none"/><circle cx="62" cy="43" r="4" fill="currentColor" stroke="none"/><path d="M34 70c8-11 24-11 32 0" fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round"/><path d="M22 44l-6-6M78 44l6-6" fill="none" stroke="currentColor" stroke-width="5" stroke-linecap="round"/>');
  /* График / чарт для personal stats */
  mk('i-gm-chart','<path d="M14 82V18M14 82h72" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/><path d="M28 66l16-18 14 12 22-30" fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round" stroke-linejoin="round"/><circle cx="80" cy="30" r="4" fill="currentColor" stroke="none"/>');
  /* Мишень: задачи/челленджи */
  mk('i-gm-target','<circle cx="50" cy="50" r="36" fill="none" stroke="currentColor" stroke-width="6"/><circle cx="50" cy="50" r="22" fill="none" stroke="currentColor" stroke-width="6"/><circle cx="50" cy="50" r="8" fill="currentColor" stroke="none"/>');
  /* Три лиги */
  mk('i-gm-friends','<circle cx="30" cy="34" r="12" fill="none" stroke="currentColor" stroke-width="6"/><circle cx="70" cy="34" r="12" fill="none" stroke="currentColor" stroke-width="6"/><path d="M10 78c0-14 9-24 20-24s20 10 20 24M50 78c0-14 9-24 20-24s20 10 20 24" fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round"/>');
  mk('i-gm-city','<path d="M12 84h76M22 84V44l12-8v48M46 84V26l16-10v68M74 84V52l12-6v38" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round" stroke-linecap="round"/><rect x="28" y="52" width="4" height="4" fill="currentColor" stroke="none"/><rect x="52" y="40" width="4" height="4" fill="currentColor" stroke="none"/><rect x="52" y="56" width="4" height="4" fill="currentColor" stroke="none"/><rect x="78" y="60" width="4" height="4" fill="currentColor" stroke="none"/>');
  mk('i-gm-world','<circle cx="50" cy="50" r="36" fill="none" stroke="currentColor" stroke-width="6"/><path d="M14 50h72M50 14c-10 10-16 22-16 36s6 26 16 36c10-10 16-22 16-36S60 24 50 14z" fill="none" stroke="currentColor" stroke-width="5.5"/>');
  /* Скретч (монета/скраб) */
  mk('i-gm-scratch','<rect x="14" y="24" width="72" height="52" rx="7" fill="none" stroke="currentColor" stroke-width="6"/><path d="M28 44l14 14M28 60l14 14M46 44l14 14M46 60l14 14" fill="none" stroke="currentColor" stroke-width="5" stroke-linecap="round"/><circle cx="72" cy="50" r="10" fill="currentColor" stroke="none"/>');
  /* Сетка (три-в-ряд) */
  mk('i-gm-grid','<path d="M14 14h24v24H14zM38 14h24v24H38zM62 14h24v24H62zM14 38h24v24H14zM38 38h24v24H38zM62 38h24v24H62zM14 62h24v24H14zM38 62h24v24H38zM62 62h24v24H62z" fill="none" stroke="currentColor" stroke-width="5" stroke-linejoin="round"/>');
  /* Коробка (открытие) */
  mk('i-gm-box','<rect x="14" y="34" width="72" height="52" rx="4" fill="none" stroke="currentColor" stroke-width="6"/><path d="M14 34l8-16h56l8 16M50 18v68M14 52h72" fill="none" stroke="currentColor" stroke-width="5.5" stroke-linejoin="round"/>');
  /* Battle pass — щит с звездой */
  mk('i-gm-bp','<path d="M50 12l32 8v26c0 22-14 34-32 42-18-8-32-20-32-42V20z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/><path d="M50 34l5 10 11 2-8 8 2 11-10-6-10 6 2-11-8-8 11-2z" fill="none" stroke="currentColor" stroke-width="4.5" stroke-linejoin="round"/>');
  /* XP-искра */
  mk('i-gm-xp','<path d="M50 10l10 22 24 4-18 16 4 24-20-12-20 12 4-24-18-16 24-4z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/><path d="M50 34v14M40 46l10 6 10-6" fill="none" stroke="currentColor" stroke-width="4.5" stroke-linecap="round"/>');
  /* Инвентарь подарков */
  mk('i-gm-inv','<rect x="12" y="26" width="76" height="60" rx="6" fill="none" stroke="currentColor" stroke-width="6"/><path d="M12 44h76M30 26v-4a10 10 0 0110-10h20a10 10 0 0110 10v4" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/><circle cx="50" cy="62" r="8" fill="none" stroke="currentColor" stroke-width="5.5"/>');
  /* Меню-точки для карточек подарков — уже есть i-more, не дублируем */
  /* Часы (напоминание стрика) — уже есть i-clock */
})();

/* ============================================================
   ЗВУК: короткий WebAudio-бип для достижений и наград (без внешних mp3)
   ============================================================ */
let GM_AUDIO_CTX = null;
function gmSfx(freq, dur, type){
  try{
    if(document.documentElement.dataset.sound === 'off') return;
    if(!GM_AUDIO_CTX) GM_AUDIO_CTX = new (window.AudioContext || window.webkitAudioContext)();
    const ctx = GM_AUDIO_CTX;
    const osc = ctx.createOscillator();
    const g = ctx.createGain();
    osc.type = type || 'sine';
    osc.frequency.value = freq || 800;
    g.gain.value = 0;
    osc.connect(g).connect(ctx.destination);
    const t = ctx.currentTime;
    g.gain.linearRampToValueAtTime(0.14, t + 0.01);
    g.gain.exponentialRampToValueAtTime(0.0001, t + (dur || 0.16));
    osc.start(t); osc.stop(t + (dur || 0.16) + 0.02);
  }catch(e){}
}
function gmSfxAch(){ gmSfx(660, 0.09, 'triangle'); setTimeout(()=>gmSfx(990, 0.16, 'triangle'), 90); }
function gmSfxWin(){ gmSfx(520, 0.09, 'sine'); setTimeout(()=>gmSfx(780, 0.11, 'sine'), 100); setTimeout(()=>gmSfx(1040, 0.24, 'sine'), 210); }
function gmSfxTap(){ gmSfx(360, 0.05, 'square'); }

/* ============================================================
   XP-СИСТЕМА: сквозной опыт для Battle Pass + челленджей
   ============================================================ */
function gmXpGet(){ try{ return Math.max(0, parseInt(localStorage.getItem('oko-games-xp'),10) || 0); }catch(e){ return 0; } }
function gmXpSet(v){ try{ localStorage.setItem('oko-games-xp', String(Math.max(0, v|0))); }catch(e){} }
function gmXpAdd(n, why){
  if(n <= 0) return;
  const before = gmBpLevel();
  gmXpSet(gmXpGet() + n);
  const after = gmBpLevel();
  if(after > before){
    toast(`Новый уровень пропуска · ${after}`);
    gmSfxAch();
    for(let l = before + 1; l <= after; l++) gmBpGrant(l);
    gmAchCheckBp(after);
  }
  gmBpRenderCard();
  if(why) try{ gmXpLogPush({at:Date.now(), n, why}); }catch(e){}
}
function gmXpLogPush(rec){
  let log = [];
  try{ log = JSON.parse(localStorage.getItem('oko-games-xp-log')) || []; }catch(e){}
  log.unshift(rec);
  try{ localStorage.setItem('oko-games-xp-log', JSON.stringify(log.slice(0, 40))); }catch(e){}
}

/* ============================================================
   BATTLE PASS — 30 уровней, XP-требование растёт, награды через уровень
   ============================================================ */
const GM_BP_MAX = 30;
const GM_BP_PER_LEVEL = 250; /* базовый XP на уровень (растёт линейно) */
function gmBpXpForLevel(l){ return Math.round(GM_BP_PER_LEVEL * l + 50 * (l - 1) * l / 2); } /* нарастающая сумма */
function gmBpLevel(){
  const xp = gmXpGet();
  for(let l = 1; l <= GM_BP_MAX; l++){
    if(xp < gmBpXpForLevel(l)) return l - 1;
  }
  return GM_BP_MAX;
}
function gmBpProgress(){
  const l = gmBpLevel();
  if(l >= GM_BP_MAX) return {l, cur: 1, need: 1, next: null};
  const cur = gmXpGet() - (l ? gmBpXpForLevel(l) : 0);
  const need = gmBpXpForLevel(l + 1) - (l ? gmBpXpForLevel(l) : 0);
  return {l, cur, need, next: l + 1};
}
function gmBpReward(l){
  /* награды подобраны в стиле око: билеты, крутки, скидки, услуги, тарифы */
  if(l % 10 === 0) return {t:'tier', v:'PRO', s:'PRO на месяц'};
  if(l % 5 === 0) return {t:'service', v:'priority', s:'Топ в ленте'};
  if(l % 3 === 0) return {t:'check', v:2, s:'2 проверки видео'};
  if(l % 2 === 0) return {t:'ticket', v:3, s:'3 билета'};
  return {t:'xp', v:50, s:'+50 XP-буст'};
}
function gmBpClaimedList(){ try{ return JSON.parse(localStorage.getItem('oko-games-bp-claimed')) || {}; }catch(e){ return {}; } }
function gmBpClaimSave(m){ try{ localStorage.setItem('oko-games-bp-claimed', JSON.stringify(m)); }catch(e){} }
function gmBpGrant(l){
  const cl = gmBpClaimedList();
  if(cl[l]) return;
  cl[l] = Date.now();
  gmBpClaimSave(cl);
  const r = gmBpReward(l);
  switch(r.t){
    case 'ticket': gmTicketsAdd(r.v); toast(`Пропуск ур ${l} · +${r.v} ${gmPluralTk(r.v)}`); break;
    case 'check':  gmChecksAdd(r.v); gmPrizeAdd({type:'check', val:r.v, status:'active'}); toast(`Пропуск ур ${l} · +${r.v} проверки видео`); break;
    case 'service':{
      gmPrizeAdd({type:'service', val:r.v, code:gmMakeCode(), status:'active'});
      toast(`Пропуск ур ${l} · ${(GM_SERVICES[r.v]||{}).label||'услуга OKO'}`); break;
    }
    case 'tier':
      if(typeof PROFILE !== 'undefined'){ PROFILE.tier = r.v; if(typeof renderMyProfile === 'function') renderMyProfile(); }
      gmPrizeAdd({type:'tier', val:r.v, status:'active'});
      toast(`Пропуск ур ${l} · тариф ${r.v} на месяц`); break;
    case 'xp': gmTicketsAdd(1); break; /* xp-буст = маленький бонус, не циклим xp */
  }
  gmPrizesBtnRender();
}
function gmBpRenderCard(){
  const el = document.getElementById('gmBpCard');
  if(!el) return;
  const p = gmBpProgress();
  const chip = document.getElementById('gmBpLvlChip');
  if(chip) chip.textContent = 'Ур ' + p.l;
  const pct = p.need > 0 ? Math.min(100, Math.round(p.cur / p.need * 100)) : 100;
  const nextReward = p.next ? gmBpReward(p.next) : null;
  el.innerHTML = `
    <div class="gm-bp-head">
      <span class="gm-bp-badge">${I('gm-bp')}<b>Уровень ${p.l}</b></span>
      <small>${p.next ? `до ур ${p.next} · ${p.need - p.cur} XP` : 'максимум достигнут'}</small>
    </div>
    <div class="gm-bp-bar"><i style="width:${pct}%"></i></div>
    ${nextReward ? `<div class="gm-bp-next">
      <span>Следующая награда</span>
      <b>${nextReward.s}</b>
    </div>` : ''}
    <button class="gm-bp-open" onclick="gmBpOpen()">Смотреть все 30 уровней ${I('chev')}</button>`;
}
function gmBpOpen(){ openSheet('gmBp'); gmBpRenderFull(); }
function gmBpRenderFull(){
  const el = document.getElementById('gmBpBody');
  if(!el) return;
  const p = gmBpProgress();
  const cl = gmBpClaimedList();
  const rows = [];
  for(let l = 1; l <= GM_BP_MAX; l++){
    const r = gmBpReward(l);
    const claimed = !!cl[l];
    const locked = l > p.l;
    const need = gmBpXpForLevel(l);
    rows.push(`<div class="gm-bp-row ${claimed ? 'claimed' : ''} ${locked ? 'locked' : 'ready'}">
      <span class="gm-bp-lvl">${l}</span>
      <span class="gm-bp-rw-ic">${I(gmBpRewardIcon(r))}</span>
      <div class="gm-bp-rw-b">
        <b>${r.s}</b>
        <small>${need} XP</small>
      </div>
      <span class="gm-bp-rw-badge ${claimed ? 'done' : (locked ? 'lock' : 'act')}">${claimed ? I('check') : (locked ? I('lock') : I('gm-gift'))}</span>
    </div>`);
  }
  el.innerHTML = `
    <div class="gm-bp-summary">
      <div>
        <b>${I('gm-bp')}Уровень ${p.l} / ${GM_BP_MAX}</b>
        <small>${p.next ? `XP до ур ${p.next}: ${p.need - p.cur} / ${p.need}` : 'Все уровни пройдены'}</small>
      </div>
      <div class="gm-bp-xp">${I('gm-xp')}<b>${gmXpGet()}</b><small>XP всего</small></div>
    </div>
    <div class="gm-bp-bar big"><i style="width:${p.need ? (p.cur/p.need*100) : 100}%"></i></div>
    <p class="gm-bp-note">Опыт даётся за крутки, задания дня, стрик, мини-игры и достижения. Пропуск сбрасывается вместе с сезоном.</p>
    <div class="gm-bp-list">${rows.join('')}</div>`;
}
function gmBpRewardIcon(r){
  switch(r.t){
    case 'ticket': return 'gm-ticket';
    case 'check':  return 'gm-checkvid';
    case 'service':return 'gm-boost';
    case 'tier':   return 'crown';
    case 'xp':     return 'gm-xp';
  }
  return 'gm-gift';
}

/* ============================================================
   DAILY CHALLENGES — 3 ежедневных задания, сброс в полночь
   ============================================================ */
const GM_DAILY_TEMPLATES = [
  {id:'spin3',    ic:'gm-gift',   t:'Крути рулетку',       target:3,  metric:'spins',     xp:60,  s:'3 крутки колеса'},
  {id:'spin5',    ic:'gm-gift',   t:'Расширь удачу',       target:5,  metric:'spins',     xp:110, s:'5 круток колеса'},
  {id:'mini2',    ic:'gm-grid',   t:'Мини-игры',           target:2,  metric:'minis',     xp:80,  s:'2 партии в мини-играх'},
  {id:'mini3',    ic:'gm-grid',   t:'Мини-марафон',        target:3,  metric:'minis',     xp:120, s:'3 партии в мини-играх'},
  {id:'gift1',    ic:'gm-gift',   t:'Отправь подарок',     target:1,  metric:'gifts',     xp:70,  s:'1 отправленный подарок'},
  {id:'scratch2', ic:'gm-scratch',t:'Скретч дня',          target:2,  metric:'scratch',   xp:80,  s:'2 скретч-карты'},
  {id:'m3',       ic:'gm-grid',   t:'Собери линию',        target:1,  metric:'match3',    xp:60,  s:'1 успешная линия'},
  {id:'box',      ic:'gm-box',    t:'Открой коробку',      target:1,  metric:'box',       xp:70,  s:'1 открытая коробка'},
  {id:'stake',    ic:'money',    t:'Ставка на удачу',      target:1,  metric:'paidspin',  xp:90,  s:'1 платная крутка (₽ или билет)'},
  {id:'streak',   ic:'gm-flame', t:'Держи серию',          target:1,  metric:'streakday', xp:50,  s:'зайди в игры сегодня'}
];
function gmDailyKey(){ return 'oko-games-daily-' + gmDayIdx(); }
function gmDailyState(){
  const k = gmDailyKey();
  let st = null;
  try{ st = JSON.parse(localStorage.getItem(k)); }catch(e){}
  if(st && Array.isArray(st.tasks)) return st;
  /* сгенерировать новые 3 задания на день (детерминировано от даты) */
  const seed = gmDayIdx() * 9301 + 49297;
  const shuffled = GM_DAILY_TEMPLATES.slice().sort((a,b)=>((seed + a.target*13) % 100) - ((seed + b.target*7) % 100));
  const picked = shuffled.slice(0, 3).map(x=>({id:x.id, p:0, c:false, r:false}));
  st = {tasks: picked, bonusClaimed: false};
  /* всегда включаем «зайди сегодня» — считать сразу выполненным при заходе */
  const streakIdx = picked.findIndex(t=>t.id==='streak');
  if(streakIdx >= 0){ picked[streakIdx].p = 1; picked[streakIdx].c = true; }
  try{ localStorage.setItem(k, JSON.stringify(st)); }catch(e){}
  gmDailyCleanupOld();
  return st;
}
function gmDailyCleanupOld(){
  try{
    for(let i = 0; i < localStorage.length; i++){
      const k = localStorage.key(i);
      if(k && k.startsWith('oko-games-daily-') && k !== gmDailyKey()){
        const d = parseInt(k.replace('oko-games-daily-',''), 10);
        if(d && d < gmDayIdx() - 3){ localStorage.removeItem(k); i--; }
      }
    }
  }catch(e){}
}
function gmDailyTaskInfo(id){ return GM_DAILY_TEMPLATES.find(t=>t.id === id) || {}; }
function gmDailySave(st){ try{ localStorage.setItem(gmDailyKey(), JSON.stringify(st)); }catch(e){} }
function gmDailyBump(metric){
  const st = gmDailyState();
  let any = false;
  st.tasks.forEach(t=>{
    const info = gmDailyTaskInfo(t.id);
    if(info.metric !== metric || t.c) return;
    t.p = Math.min(info.target, (t.p || 0) + 1);
    if(t.p >= info.target){ t.c = true; any = true; }
  });
  gmDailySave(st);
  if(any){ toast('Задание дня выполнено'); gmSfxTap(); }
  gmDailyRenderCard();
  gmDailyRenderFull();
}
function gmDailyClaim(id){
  const st = gmDailyState();
  const t = st.tasks.find(x=>x.id === id);
  const info = gmDailyTaskInfo(id);
  if(!t || !t.c || t.r) return;
  t.r = true;
  gmDailySave(st);
  gmXpAdd(info.xp || 40, 'daily:' + id);
  toast(`+${info.xp} XP · ${info.t}`);
  gmSfxAch();
  /* если все 3 задания завершены и награда за них ещё не забрана — +200 XP и бонус-крутка */
  if(st.tasks.every(x=>x.c && x.r) && !st.bonusClaimed){
    st.bonusClaimed = true;
    gmDailySave(st);
    gmXpAdd(200, 'daily-bonus');
    gmExtraFreeSet(gmExtraFreeGet() + 1);
    toast('Все задания дня! +200 XP + бесплатная крутка');
    gmAchUnlock('daily-all');
    gmSfxWin();
  }
  gmDailyRenderCard();
  gmDailyRenderFull();
  gmRenderModes();
}
function gmDailyRenderCard(){
  const el = document.getElementById('gmDailyBlock');
  if(!el) return;
  const st = gmDailyState();
  const done = st.tasks.filter(t=>t.c).length;
  const cnt = document.getElementById('gmDailyCount');
  if(cnt) cnt.textContent = done + '/3';
  const rows = st.tasks.slice(0,3).map(t=>{
    const info = gmDailyTaskInfo(t.id);
    const pct = Math.min(100, Math.round((t.p||0) / (info.target || 1) * 100));
    return `<div class="gm-dq-row ${t.c ? 'done' : ''} ${t.r ? 'redeemed' : ''}">
      <span class="gm-dq-ic">${I(info.ic || 'gm-target')}</span>
      <div class="gm-dq-b">
        <b>${info.t || t.id}</b>
        <div class="gm-dq-bar"><i style="width:${pct}%"></i></div>
        <small>${t.p||0}/${info.target||0} · +${info.xp||0} XP</small>
      </div>
      <button class="gm-dq-btn ${t.c && !t.r ? 'ready' : ''}" ${t.c && !t.r ? '' : 'disabled'} onclick="gmDailyClaim('${t.id}')">${t.r ? I('check') : (t.c ? 'Забрать' : `${info.target - (t.p||0)}`)}</button>
    </div>`;
  }).join('');
  const allDone = st.tasks.every(x=>x.c && x.r);
  el.innerHTML = rows + `<div class="gm-dq-bonus ${allDone ? 'done' : (st.tasks.every(x=>x.c) ? 'ready' : '')}">
    <span>${I('gm-target')}Все 3 задания = +200 XP и бесплатная крутка</span>
    ${allDone ? `<b>${I('check')}Получено</b>` : ''}
  </div>`;
}
function gmDailyOpen(){ openSheet('gmDaily'); gmDailyRenderFull(); }
function gmDailyRenderFull(){
  const el = document.getElementById('gmDailyBody');
  if(!el) return;
  const st = gmDailyState();
  const left = gmFmtCd(gmBonusLeftMs());
  const done = st.tasks.filter(t=>t.c).length;
  const rows = st.tasks.map(t=>{
    const info = gmDailyTaskInfo(t.id);
    const pct = Math.min(100, Math.round((t.p||0) / (info.target || 1) * 100));
    return `<div class="gm-dq-row big ${t.c ? 'done' : ''} ${t.r ? 'redeemed' : ''}">
      <span class="gm-dq-ic">${I(info.ic || 'gm-target')}</span>
      <div class="gm-dq-b">
        <b>${info.t}</b>
        <small class="gm-dq-desc">${info.s}</small>
        <div class="gm-dq-bar"><i style="width:${pct}%"></i></div>
        <small>${t.p||0}/${info.target} · <b class="gm-dq-xp">+${info.xp} XP</b></small>
      </div>
      <button class="gm-dq-btn ${t.c && !t.r ? 'ready' : ''}" ${t.c && !t.r ? '' : 'disabled'} onclick="gmDailyClaim('${t.id}')">${t.r ? I('check') : (t.c ? 'Забрать' : 'Идёт')}</button>
    </div>`;
  }).join('');
  el.innerHTML = `
    <div class="gm-dq-head">
      <div><b>Задания дня · ${done}/3</b><small>сброс через ${left}</small></div>
      <span class="gm-bp-xp small">${I('gm-xp')}<b>${gmXpGet()}</b><small>XP</small></span>
    </div>
    <div class="gm-dq-list">${rows}</div>
    <div class="gm-dq-bonus ${st.tasks.every(x=>x.c && x.r) ? 'done' : (st.tasks.every(x=>x.c) ? 'ready' : '')}">
      <span>${I('gm-target')}Все задания = +200 XP и +1 бесплатная крутка</span>
    </div>`;
}

/* ============================================================
   СТРИК: большой шит с 7-дневным календарём + sad-anim при разрыве
   ============================================================ */
function gmStreakOpen(){ openSheet('gmStreak'); gmStreakRenderFull(); }
function gmStreakRenderFull(){
  const el = document.getElementById('gmStreakBody');
  if(!el) return;
  const s = gmStreakGet();
  const n = gmStreakDisplay();
  const broken = s.d !== null && s.d !== undefined && s.d < gmDayIdx() - 1 && (s.n || 0) > 0;
  const cal = gmStreakCalendar();
  const cells = cal.map(c=>
    `<div class="gm-cal-cell big ${c.done ? 'done' : ''} ${c.isToday ? 'today' : ''}">
      <span class="gm-cal-day">${c.name}</span>
      <span class="gm-cal-dot">${c.done ? I('gm-flame') : c.num}</span>
    </div>`
  ).join('');
  const left = gmFmtCd(gmBonusLeftMs());
  const bonus7 = n >= 7 ? true : false;
  el.innerHTML = `
    <div class="gm-streak-hero ${broken ? 'sad' : (n>0 ? 'lit' : '')}">
      <div class="gm-streak-flame ${broken ? 'sad' : ''}">${I(broken ? 'gm-sad' : 'gm-flame2')}</div>
      <div>
        <b class="gm-streak-big">${n}</b>
        <small>${gmPluralDay(n)} подряд</small>
      </div>
    </div>
    ${broken ? `<div class="gm-streak-sad">Серия прервалась. Заходи каждый день, чтобы копить бонусы.</div>` : ''}
    <div class="gm-streak-cal">${cells}</div>
    <div class="gm-streak-bonus ${bonus7 ? 'on' : ''}">
      <span>${I('gm-cup')}</span>
      <div><b>7 дней подряд = ×3 приз</b><small>множитель применится к следующей крутке колеса</small></div>
      ${bonus7 ? `<span class="gm-streak-badge">Активно</span>` : `<small class="gm-streak-need">ещё ${Math.max(0,7-n)} ${gmPluralDay(Math.max(0,7-n))}</small>`}
    </div>
    <div class="gm-streak-remind">
      ${I('clock')}<span>Осталось до сброса дня · <b>${left}</b></span>
    </div>
    <button class="gm-spin" onclick="gmStreakRemind()">${I('bell')}${gmRemindOn() ? 'Напоминание включено' : 'Напомнить за 3 часа до сброса'}</button>`;
}
/* «push» через notifs-plus — при разрыве серии в течение дня */
function gmRemindOn(){ try{ return localStorage.getItem('oko-games-remind') === '1'; }catch(e){ return false; } }
function gmStreakRemind(){
  const cur = gmRemindOn();
  try{ localStorage.setItem('oko-games-remind', cur ? '0' : '1'); }catch(e){}
  if(!cur){
    toast('Напомним за 3 часа до сброса дня');
    gmScheduleRemind();
  }else{
    toast('Напоминание выключено');
  }
  gmStreakRenderFull();
}
let gmRemindTimer = 0;
function gmScheduleRemind(){
  clearTimeout(gmRemindTimer);
  if(!gmRemindOn()) return;
  const left = gmBonusLeftMs();
  const trigger = left - 3 * 3600 * 1000;
  if(trigger < 1000){
    /* уже поздно на сегодня — планируем на завтра */
    gmRemindTimer = setTimeout(gmScheduleRemind, left + 60000);
    return;
  }
  gmRemindTimer = setTimeout(()=>{
    if(!gmCanFree()) return;
    if(typeof NOTIFS !== 'undefined' && NOTIFS.unshift){
      NOTIFS.unshift({ic:'gm-flame', who:'Рулетка OKO', t:'Осталось 3 часа до сброса дня — заходи за бесплатной круткой', time:'только что', g:'Сегодня', unread:true, act:function(){ if(typeof showTab === 'function') showTab('games'); }});
      if(typeof updateNotifDot === 'function') updateNotifDot();
      if(typeof window.renderNotifs === 'function') window.renderNotifs();
    }
    toast('До сброса дня 3 часа — крути бесплатную');
    gmScheduleRemind();
  }, trigger);
}

/* ============================================================
   TON GIFTS — каталог, отправка другу, инвентарь с редкостью
   ============================================================ */
const GM_GIFTS = [
  {id:'rose',    n:'Роза',      ic:'gm-rose',    ton:0.5,  rar:'Common',    col:'#ff5b7a'},
  {id:'cup',     n:'Кубок',     ic:'gm-cup2',    ton:1.5,  rar:'Common',    col:'#f4c04a'},
  {id:'flame',   n:'Огонь',     ic:'gm-flame2',  ton:3,    rar:'Rare',      col:'#ff8a24'},
  {id:'rocket',  n:'Ракета',    ic:'gm-rocket2', ton:8,    rar:'Rare',      col:'#7cd0ff'},
  {id:'diamond', n:'Диамант',   ic:'gm-diamond', ton:25,   rar:'Epic',      col:'#9AFF00'},
  {id:'crown',   n:'Корона',    ic:'gm-crown2',  ton:100,  rar:'Legendary', col:'#ffd23f'}
];
const GM_RAR_COLORS = {
  Common:    {bg:'rgba(154,255,0,.08)', ink:'#9AFF00',  ord:1},
  Rare:      {bg:'rgba(0,136,204,.14)', ink:'#0088CC',  ord:2},
  Epic:      {bg:'rgba(178,102,255,.15)',ink:'#b266ff', ord:3},
  Legendary: {bg:'rgba(255,210,63,.18)', ink:'#ffd23f', ord:4}
};
function gmGiftById(id){ return GM_GIFTS.find(g=>g.id === id); }
/* TON — 1 TON ≈ ₽300 (условно для симуляции) */
function gmTonToRub(t){ return Math.round(t * 300); }

function gmGiftsInv(){ try{ return JSON.parse(localStorage.getItem('oko-games-gifts')) || []; }catch(e){ return []; } }
function gmGiftsInvSave(list){ try{ localStorage.setItem('oko-games-gifts', JSON.stringify(list.slice(0, 60))); }catch(e){} }
function gmGiftAdd(id, from){
  const g = gmGiftById(id);
  if(!g) return;
  const list = gmGiftsInv();
  list.unshift({id, n:g.n, ic:g.ic, rar:g.rar, from:from||null, at:Date.now()});
  gmGiftsInvSave(list);
  gmGiftsRailRender();
  gmGiftsInvBtnRender();
  gmAchGiftCheck();
}
function gmGiftsHist(){ try{ return JSON.parse(localStorage.getItem('oko-games-gifts-hist')) || []; }catch(e){ return []; } }
function gmGiftsHistPush(rec){
  const l = gmGiftsHist();
  l.unshift(rec);
  try{ localStorage.setItem('oko-games-gifts-hist', JSON.stringify(l.slice(0, 40))); }catch(e){}
}
function gmGiftsShopOpen(){ openSheet('gmGiftsShop'); gmGiftsShopRender(); }
function gmGiftsShopRender(){
  const el = document.getElementById('gmGiftsShopBody');
  if(!el) return;
  const cards = GM_GIFTS.map(g=>{
    const c = GM_RAR_COLORS[g.rar];
    return `<div class="gm-gcard" style="--gc:${g.col}">
      <span class="gm-gcard-ic">${I(g.ic)}</span>
      <b>${g.n}</b>
      <span class="gm-gcard-rar" style="background:${c.bg};color:${c.ink}">${g.rar}</span>
      <div class="gm-gcard-price">${I('ton')}<b>${g.ton}</b><small>≈ ${fmtMoney(gmTonToRub(g.ton))}</small></div>
      <button class="gm-gcard-buy" onclick="gmGiftBuy('${g.id}',false)">Купить себе</button>
      <button class="gm-gcard-send" onclick="gmGiftBuy('${g.id}',true)">${I('send')}Другу</button>
    </div>`;
  }).join('');
  el.innerHTML = `
    <div class="gm-gift-note">${I('ton')}<span>Подарки покупаются за TON. При оплате блокчейн-сеть возьмёт комиссию. Симуляция — оплата фиктивная.</span></div>
    <div class="gm-gift-grid">${cards}</div>`;
}
function gmGiftBuy(id, forFriend){
  const g = gmGiftById(id);
  if(!g) return;
  /* «оплата» — списание с ₽-кошелька по курсу TON→₽ */
  const rub = gmTonToRub(g.ton);
  if(!walletCharge(rub, 'Подарок TON · ' + g.n)) return;
  okoEarn(Math.round(rub * 0.05), 'Комиссия подарка TON');
  gmXpAdd(30, 'gift-buy');
  if(forFriend){
    gmGiftSendOpen(id);
  }else{
    gmGiftAdd(id, 'сам');
    gmGiftsHistPush({dir:'in', id, at:Date.now(), from:'себе'});
    toast(`Подарок «${g.n}» добавлен в коллекцию`);
    gmSfxAch();
    gmDailyBump('gifts'); /* учтём в челлендже — купить = подарить себе тоже считается */
  }
}
function gmGiftSendOpen(preselectId){
  openSheet('gmGiftSend');
  const el = document.getElementById('gmGiftSendBody');
  if(!el) return;
  const g = preselectId ? gmGiftById(preselectId) : null;
  const friends = gmFriendList();
  el.innerHTML = `
    <div class="gm-gsend-head">
      ${g ? `<span class="gm-gcard-ic small" style="--gc:${g.col}">${I(g.ic)}</span><div><b>${g.n}</b><small>${g.ton} TON · ${g.rar}</small></div>` : `<b>Выбери контакт</b>`}
    </div>
    <label class="gm-gsend-msg"><small>Сообщение (необязательно)</small><input id="gmGSendMsg" placeholder="С днём рождения!" maxlength="80"/></label>
    <div class="gm-gsend-list">${friends.map(f=>`<button class="gm-gsend-friend" onclick="gmGiftSendTo('${preselectId||''}','${esc(f.n)}')">
      <span class="gm-gsend-ava">${esc(f.n[0]||'?').toUpperCase()}</span>
      <div><b>${esc(f.n)}</b><small>${esc(f.h||'в OKO')}</small></div>
      ${I('chev')}
    </button>`).join('')}</div>`;
}
function gmFriendList(){
  /* демо-список друзей: если в чатах есть контакты — берём оттуда, иначе моки */
  const fromChats = [];
  try{
    if(typeof CHATS !== 'undefined' && Array.isArray(CHATS)){
      CHATS.filter(c=>!c.grp && c.name).slice(0,8).forEach(c=>{
        fromChats.push({n:c.name, h:c.last || 'в OKO'});
      });
    }
  }catch(e){}
  /* Правка Даниэля: ноль выдуманных людей. Раньше здесь возвращались
     шесть придуманных друзей — человек видел «своих» контактов, которых
     у него нет. Нет реальных контактов — значит список пуст, а экран
     покажет честное «пока некому отправить». */
  return fromChats;
}
function gmGiftSendTo(giftId, friendName){
  const g = giftId ? gmGiftById(giftId) : null;
  if(!g){ toast('Сначала выбери подарок в магазине'); return; }
  const msg = (document.getElementById('gmGSendMsg')||{}).value || '';
  gmGiftsHistPush({dir:'out', id:giftId, to:friendName, msg, at:Date.now()});
  /* «событие в чат» — если есть чаты, покажем сообщение-стикер */
  try{
    if(typeof CHATS !== 'undefined' && Array.isArray(CHATS)){
      const c = CHATS.find(x=>x.name === friendName);
      if(c){
        c.last = `Ты отправил подарок «${g.n}»`;
        c.time = 'только что';
        if(typeof renderChats === 'function') renderChats();
      }
    }
  }catch(e){}
  gmXpAdd(50, 'gift-send');
  gmDailyBump('gifts');
  toast(`Подарок «${g.n}» отправлен · ${friendName}`);
  gmSfxAch();
  closeSheet();
  gmGiftsRailRender();
  gmGiftsInvBtnRender();
}
function gmGiftsRailRender(){
  const el = document.getElementById('gmGiftRail');
  if(!el) return;
  el.innerHTML = GM_GIFTS.map(g=>{
    const c = GM_RAR_COLORS[g.rar];
    return `<button class="gm-grail" style="--gc:${g.col}" onclick="gmGiftBuy('${g.id}',false)">
      <span class="gm-grail-ic">${I(g.ic)}</span>
      <b>${g.n}</b>
      <small style="color:${c.ink}">${g.ton} TON</small>
    </button>`;
  }).join('');
}
function gmGiftsInvBtnRender(){
  const el = document.getElementById('gmGiftsInvBtn');
  if(!el) return;
  const inv = gmGiftsInv();
  const hist = gmGiftsHist();
  const sent = hist.filter(h=>h.dir === 'out').length;
  el.innerHTML = `${I('gm-inv')}<div class="gm-bonus-t"><span>Мои подарки</span><small>${inv.length ? `${inv.length} в коллекции · отправлено ${sent}` : 'ещё нет — купи или отправь другу'}</small></div>${I('chev','gm-bonus-chev')}`;
}
function gmGiftsInvOpen(){ openSheet('gmGiftsInv'); gmGiftsInvRender(); }
function gmGiftsInvRender(){
  const el = document.getElementById('gmGiftsInvBody');
  if(!el) return;
  const inv = gmGiftsInv();
  const hist = gmGiftsHist();
  const rarStats = {Common:0,Rare:0,Epic:0,Legendary:0};
  inv.forEach(x=>{ if(rarStats[x.rar] !== undefined) rarStats[x.rar]++; });
  const stats = `<div class="gm-inv-stats">${['Common','Rare','Epic','Legendary'].map(r=>{
    const c = GM_RAR_COLORS[r];
    return `<div class="gm-inv-stat" style="--rc:${c.ink};--rb:${c.bg}"><b>${rarStats[r]}</b><small>${r}</small></div>`;
  }).join('')}</div>`;
  if(!inv.length){
    el.innerHTML = stats + `<div class="gm-empty">${I('gm-inv')}<span>Подарков пока нет — открой витрину и подари себе или другу</span></div>
      <button class="gm-spin" onclick="closeSheet();gmGiftsShopOpen()">${I('gm-diamond')}Открыть витрину</button>`;
    return;
  }
  const items = inv.map(x=>{
    const g = gmGiftById(x.id) || {ic:'gm-gift', col:'#9AFF00'};
    const c = GM_RAR_COLORS[x.rar] || GM_RAR_COLORS.Common;
    const d = new Date(x.at);
    const time = d.toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit'}) + ' ' + d.toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'});
    return `<div class="gm-inv-card" style="--gc:${g.col}">
      <span class="gm-inv-ic">${I(g.ic)}</span>
      <b>${esc(x.n)}</b>
      <small class="gm-inv-rar" style="background:${c.bg};color:${c.ink}">${x.rar}</small>
      <small class="gm-inv-time">${time}</small>
    </div>`;
  }).join('');
  const histRows = hist.slice(0, 12).map(h=>{
    const g = gmGiftById(h.id) || {n:'Подарок', ic:'gm-gift'};
    const d = new Date(h.at);
    const time = d.toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit'});
    return `<div class="gm-inv-hist ${h.dir}">
      ${I(g.ic)}
      <div><b>${h.dir==='out' ? 'Отправлен · '+esc(h.to||'') : 'Получен · '+esc(h.from||'')}</b><small>${g.n} · ${time}</small></div>
    </div>`;
  }).join('');
  el.innerHTML = stats +
    `<div class="gm-inv-grid">${items}</div>` +
    (histRows ? `<h4 class="gm-inv-h">История</h4><div class="gm-inv-hist-list">${histRows}</div>` : '');
}

/* ============================================================
   МИНИ-ИГРЫ: скретч, три-в-ряд, коробка, спин (уже есть)
   ============================================================ */
const GM_MINI = [
  {id:'wheel',   n:'Колесо',        ic:'gm-gift',    s:'основная рулетка', fn:'gmScrollToWheel'},
  {id:'scratch', n:'Скретч',        ic:'gm-scratch', s:'сотри пальцем',     fn:'gmScratchOpen'},
  {id:'match3',  n:'Лайм-плитки',   ic:'gm-grid',    s:'три в ряд',        fn:'gmMatch3Open'},
  {id:'box',     n:'Коробка',       ic:'gm-box',     s:'крути и открой',   fn:'gmBoxOpen'}
];
function gmMiniRender(){
  const el = document.getElementById('gmMiniGrid');
  if(!el) return;
  el.innerHTML = GM_MINI.map(m=>{
    return `<button class="gm-mini-cell" onclick="${m.fn}()">
      <span class="gm-mini-ic">${I(m.ic)}</span>
      <b>${m.n}</b>
      <small>${m.s}</small>
    </button>`;
  }).join('');
}
function gmScrollToWheel(){
  const el = document.querySelector('#screen-games .gm-wheel-card');
  if(el && el.scrollIntoView) el.scrollIntoView({behavior:'smooth', block:'start'});
}

/* --- СКРЕТЧ-КАРТА --- */
function gmScratchOpen(){ openSheet('gmScratch'); gmScratchRender(); }
function gmScratchState(){
  return {revealed: 0, total: 0, prize: null};
}
function gmScratchRender(){
  const el = document.getElementById('gmScratchBody');
  if(!el) return;
  /* цена: 1 билет за карту, награда — случайный приз из пула free */
  const pool = GM_POOLS.free;
  const total = pool.reduce((a,p)=>a + p.w, 0);
  let r = Math.random() * total, idx = 0;
  for(let i = 0; i < pool.length; i++){ if(r < pool[i].w){ idx = i; break; } r -= pool[i].w; }
  const prize = pool[idx];
  el.innerHTML = `
    <div class="gm-sc-head">
      <b>Скретч-карта</b>
      <small>1 билет · сотри пальцем поле, чтобы открыть приз</small>
    </div>
    <div class="gm-sc-price"><span>Стоимость: 1 билет</span><span class="gm-tk-val">${GM_TICKETS}</span></div>
    <div class="gm-sc-card" id="gmScCard" data-prize='${JSON.stringify(prize).replace(/'/g,'&apos;')}'>
      <div class="gm-sc-prize">
        <span class="gm-sc-prize-ic">${I(gmPrizeIcon(prize))}</span>
        <b>${prize.s}</b>
        <small>${gmPrizeSub(prize)}</small>
      </div>
      <canvas class="gm-sc-canvas" id="gmScCanvas" width="300" height="180"></canvas>
    </div>
    <button class="gm-spin" id="gmScBuy" onclick="gmScratchBuy()">${I('gm-scratch')}Купить и потереть — 1 билет</button>
    <button class="gm-spin ghost" id="gmScNew" style="display:none" onclick="gmScratchOpen()">Ещё карта</button>`;
  gmScratchInitCanvas();
}
function gmScratchInitCanvas(){
  const c = document.getElementById('gmScCanvas');
  if(!c) return;
  const ctx = c.getContext('2d');
  const g = ctx.createLinearGradient(0, 0, c.width, c.height);
  g.addColorStop(0, '#20340a');
  g.addColorStop(1, '#0d0d0d');
  ctx.fillStyle = g;
  ctx.fillRect(0, 0, c.width, c.height);
  ctx.fillStyle = 'rgba(154,255,0,.35)';
  ctx.font = 'bold 22px sans-serif';
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText('ЗАДИЗЬ ПАЛЬЦЕМ', c.width/2, c.height/2);
  c.dataset.locked = '1'; /* до оплаты — заблокировано */
}
function gmScratchBuy(){
  const c = document.getElementById('gmScCanvas');
  const btn = document.getElementById('gmScBuy');
  if(!c || !btn) return;
  if(!gmTicketsSpend(1)){ toast('Нужен 1 билет — заработай в рулетке'); return; }
  c.dataset.locked = '0';
  btn.style.display = 'none';
  gmXpAdd(20, 'scratch-play');
  gmDailyBump('minis');
  gmDailyBump('scratch');
  gmScratchAttach();
}
function gmScratchAttach(){
  const c = document.getElementById('gmScCanvas');
  if(!c) return;
  const ctx = c.getContext('2d');
  let dragging = false, cleared = 0, cellSize = 8;
  const totalArea = c.width * c.height;
  const need = totalArea * 0.55;
  ctx.globalCompositeOperation = 'destination-out';
  function pos(e){
    const rect = c.getBoundingClientRect();
    const pt = e.touches ? e.touches[0] : e;
    return {x: (pt.clientX - rect.left) * (c.width / rect.width), y: (pt.clientY - rect.top) * (c.height / rect.height)};
  }
  function scratch(e){
    if(!dragging) return;
    e.preventDefault && e.preventDefault();
    const p = pos(e);
    ctx.beginPath();
    ctx.arc(p.x, p.y, 22, 0, Math.PI * 2);
    ctx.fill();
    cleared += Math.PI * 22 * 22;
    if(cleared > need){
      dragging = false;
      gmScratchDone();
    }
  }
  c.addEventListener('pointerdown', e=>{ dragging = true; scratch(e); });
  c.addEventListener('pointermove', scratch);
  c.addEventListener('pointerup', ()=>{ dragging = false; });
  c.addEventListener('pointerleave', ()=>{ dragging = false; });
  c.addEventListener('touchstart', e=>{ dragging = true; scratch(e); }, {passive:false});
  c.addEventListener('touchmove', scratch, {passive:false});
  c.addEventListener('touchend', ()=>{ dragging = false; });
}
function gmScratchDone(){
  const card = document.getElementById('gmScCard');
  const c = document.getElementById('gmScCanvas');
  if(!card) return;
  try{
    const ctx = c.getContext('2d');
    ctx.clearRect(0, 0, c.width, c.height); /* полное открытие */
  }catch(e){}
  const p = JSON.parse((card.dataset.prize||'{}').replace(/&apos;/g,"'"));
  if(!p || !p.t) return;
  /* выдаём приз через общий gmGrant-конвейер, режим 'free', но не считаем как дневную крутку */
  const pseudoGrant = ()=>{
    let prize = null, title = '';
    switch(p.t){
      case 'money':  walletAdd(p.v, 'Скретч-карта OKO'); prize = {type:'money', val:p.v, status:'done'}; title = '+ ' + fmtMoney(p.v); break;
      case 'ticket': gmTicketsAdd(p.v); prize = {type:'ticket', val:p.v, status:'done'}; title = `+ ${p.v} ${gmPluralTk(p.v)}`; break;
      case 'check':  gmChecksAdd(p.v); prize = {type:'check', val:p.v, status:'active'}; title = `+ ${p.v} проверки видео`; break;
      case 'disc':   prize = {type:'discount', val:p.v, code:gmMakeCode(), status:'active'}; title = `Скидка −${p.v}% на тариф`; break;
      case 'boost':  gmBoostSet(p.v); prize = {type:'boost', val:p.v, status:'active'}; title = `Буст ×${p.v}`; break;
      case 'tier':   if(typeof PROFILE!=='undefined'){ PROFILE.tier = p.v; } prize = {type:'tier', val:p.v, status:'active'}; title = `Тариф ${p.v} на месяц`; break;
      case 'service':prize = {type:'service', val:p.v, code:gmMakeCode(), status:'active'}; title = (GM_SERVICES[p.v]||{}).label||'Услуга OKO'; break;
    }
    gmPrizeAdd(prize);
    toast('Скретч: ' + title);
    const btn = document.getElementById('gmScNew');
    if(btn) btn.style.display = '';
    gmSfxWin();
    gmXpAdd(40, 'scratch-win');
    gmLbAddWin(gmNominal(p) || 0);
  };
  pseudoGrant();
}

/* --- ТРИ-В-РЯД: 4x4 лайм-плитки, найти 3 одинаковых подряд --- */
const GM_M3_ICONS = ['gm-gift','gm-flame2','gm-diamond','gm-rose','gm-cup2'];
function gmMatch3Open(){ openSheet('gmMatch3'); gmMatch3New(); }
let GM_M3 = null;
function gmMatch3New(){
  const el = document.getElementById('gmMatch3Body');
  if(!el) return;
  GM_M3 = {grid: [], sel: -1, moves: 5, matched: 0};
  for(let i = 0; i < 16; i++){
    GM_M3.grid.push(Math.floor(Math.random() * GM_M3_ICONS.length));
  }
  gmMatch3Render();
}
function gmMatch3Render(){
  const el = document.getElementById('gmMatch3Body');
  if(!el) return;
  const cells = GM_M3.grid.map((v,i)=>`<button class="gm-m3-cell ${GM_M3.sel === i ? 'sel' : ''}" data-i="${i}" onclick="gmMatch3Tap(${i})">${I(GM_M3_ICONS[v])}</button>`).join('');
  el.innerHTML = `
    <div class="gm-sc-head">
      <b>Собери три в ряд</b>
      <small>Меняй местами соседние плитки · ${GM_M3.moves} ходов · линии: <b>${GM_M3.matched}</b></small>
    </div>
    <div class="gm-m3-grid">${cells}</div>
    <button class="gm-spin ghost" onclick="gmMatch3New()">Новая партия</button>`;
}
function gmMatch3Tap(i){
  if(!GM_M3) return;
  if(GM_M3.moves <= 0){ toast('Ходы закончились — начни партию заново'); return; }
  if(GM_M3.sel < 0){ GM_M3.sel = i; gmMatch3Render(); return; }
  if(GM_M3.sel === i){ GM_M3.sel = -1; gmMatch3Render(); return; }
  const a = GM_M3.sel, b = i;
  const ax = a % 4, ay = Math.floor(a / 4), bx = b % 4, by = Math.floor(b / 4);
  if(Math.abs(ax - bx) + Math.abs(ay - by) !== 1){ GM_M3.sel = b; gmMatch3Render(); return; }
  [GM_M3.grid[a], GM_M3.grid[b]] = [GM_M3.grid[b], GM_M3.grid[a]];
  GM_M3.sel = -1;
  GM_M3.moves--;
  gmSfxTap();
  const found = gmMatch3Find();
  if(found > 0){
    GM_M3.matched += found;
    gmDailyBump('m3');
    gmDailyBump('minis');
    /* награда: за каждую собранную линию — 1 билет */
    gmTicketsAdd(found);
    gmXpAdd(30 * found, 'match3');
    toast(`Линия! +${found} ${gmPluralTk(found)}`);
    gmSfxAch();
  }
  gmMatch3Render();
  if(GM_M3.moves <= 0 && GM_M3.matched > 0){
    setTimeout(()=>{ toast(`Партия окончена · линий: ${GM_M3.matched}`); }, 200);
  }
}
function gmMatch3Find(){
  let cnt = 0;
  /* горизонтали */
  for(let r = 0; r < 4; r++){
    for(let c = 0; c < 2; c++){
      const i = r*4 + c;
      if(GM_M3.grid[i] === GM_M3.grid[i+1] && GM_M3.grid[i] === GM_M3.grid[i+2]){
        GM_M3.grid[i] = -1; GM_M3.grid[i+1] = -1; GM_M3.grid[i+2] = -1;
        cnt++;
      }
    }
  }
  /* вертикали */
  for(let c = 0; c < 4; c++){
    for(let r = 0; r < 2; r++){
      const i = r*4 + c;
      if(GM_M3.grid[i] !== -1 && GM_M3.grid[i] === GM_M3.grid[i+4] && GM_M3.grid[i] === GM_M3.grid[i+8]){
        GM_M3.grid[i] = -1; GM_M3.grid[i+4] = -1; GM_M3.grid[i+8] = -1;
        cnt++;
      }
    }
  }
  /* «падение»: заполнить пустые новыми */
  for(let i = 0; i < 16; i++){
    if(GM_M3.grid[i] === -1) GM_M3.grid[i] = Math.floor(Math.random() * GM_M3_ICONS.length);
  }
  return cnt;
}

/* --- КОРОБКА: 3 коробки, выбери одну — открытие с анимацией --- */
function gmBoxOpen(){ openSheet('gmBox'); gmBoxRender(); }
function gmBoxRender(){
  const el = document.getElementById('gmBoxBody');
  if(!el) return;
  el.innerHTML = `
    <div class="gm-sc-head">
      <b>Три коробки — одна выигрышная</b>
      <small>Стоимость: 2 билета · выбери коробку и открой</small>
    </div>
    <div class="gm-box-row" id="gmBoxRow">
      ${[0,1,2].map(i=>`<button class="gm-box" onclick="gmBoxPick(${i})"><span class="gm-box-ic">${I('gm-box')}</span></button>`).join('')}
    </div>
    <button class="gm-spin" id="gmBoxBuy" onclick="gmBoxBuy()">${I('gm-box')}Оплатить и выбрать — 2 билета</button>
    <div id="gmBoxPrize"></div>`;
}
let GM_BOX_PAID = false;
function gmBoxBuy(){
  if(!gmTicketsSpend(2)){ toast('Нужно 2 билета'); return; }
  GM_BOX_PAID = true;
  document.getElementById('gmBoxBuy').style.display = 'none';
  toast('Выбирай коробку');
  gmXpAdd(20, 'box-play');
  gmDailyBump('minis');
  gmDailyBump('box');
}
function gmBoxPick(i){
  if(!GM_BOX_PAID){ toast('Сначала оплати — 2 билета'); return; }
  GM_BOX_PAID = false;
  const boxes = document.querySelectorAll('#gmBoxRow .gm-box');
  boxes.forEach((b,idx)=>{
    b.classList.add(idx === i ? 'chosen' : 'faded');
    b.disabled = true;
  });
  /* Выбираем приз из премиум-пула mystery box */
  const pool = GM_MB_POOL;
  const tot = pool.reduce((a,p)=>a + p.w, 0);
  let r = Math.random() * tot, idx = 0;
  for(let j = 0; j < pool.length; j++){ if(r < pool[j].w){ idx = j; break; } r -= pool[j].w; }
  const p = pool[idx];
  setTimeout(()=>{
    let title = '', prize = null;
    switch(p.t){
      case 'ticket': gmTicketsAdd(p.v); prize = {type:'ticket', val:p.v, status:'done'}; title = `+ ${p.v} ${gmPluralTk(p.v)}`; break;
      case 'check':  gmChecksAdd(p.v); prize = {type:'check', val:p.v, status:'active'}; title = `+ ${p.v} проверки видео`; break;
      case 'disc':   prize = {type:'discount', val:p.v, code:gmMakeCode(), status:'active'}; title = `Скидка −${p.v}%`; break;
      case 'boost':  gmBoostSet(p.v); prize = {type:'boost', val:p.v, status:'active'}; title = `Буст ×${p.v}`; break;
      case 'service':prize = {type:'service', val:p.v, code:gmMakeCode(), status:'active'}; title = (GM_SERVICES[p.v]||{}).label||'Услуга OKO'; break;
      case 'tier':   if(typeof PROFILE!=='undefined'){ PROFILE.tier = p.v; } prize = {type:'tier', val:p.v, status:'active'}; title = `Тариф ${p.v} на месяц`; break;
    }
    gmPrizeAdd(prize);
    const pel = document.getElementById('gmBoxPrize');
    if(pel){
      pel.innerHTML = `<div class="gm-box-reveal">${I(gmPrizeIcon(p))}<b>${title}</b><small>Приз в «Мои призы»</small></div>
        <button class="gm-spin ghost" onclick="gmBoxRender()">Ещё раз</button>`;
    }
    gmSfxWin();
    gmXpAdd(60, 'box-win');
    gmLbAddWin(gmNominal(p) || 0);
  }, 700);
}

/* ============================================================
   ЛИДЕРЫ 3 ЛИГИ (Друзья / Город / Мир) — расширяем существующий рендер
   ============================================================ */
let GM_LB_LEAGUE = 'friends';
const GM_LB_LEAGUES = [
  {id:'friends', n:'Друзья', ic:'gm-friends'},
  {id:'city',    n:'Твой город', ic:'gm-city'},
  {id:'world',   n:'Мир', ic:'gm-world'}
];
/* Таблицы лидеров были набиты ботами с выдуманными никами и случайными
   очками — то есть человек соревновался с несуществующими людьми.
   Пулы пусты: в таблице только реальные игроки. Пока их нет — честный
   пустой экран, а не имитация конкуренции. */
const GM_LB_POOLS = { friends: [], city: [], world: [] };
function gmLbLeagueGet(id){
  const k = 'oko-games-lb-' + id + '-' + gmWeekKey();
  let cached = null;
  try{ cached = JSON.parse(localStorage.getItem(k)); }catch(e){}
  if(cached && cached.bots) return cached;
  const pool = GM_LB_POOLS[id].slice().sort(()=>Math.random() - 0.5);
  const size = id === 'world' ? 12 : 10;
  /* призы: у world самые крупные, у friends скромнее */
  const scale = id === 'friends' ? 0.6 : id === 'city' ? 1 : 1.8;
  const data = {
    week: gmWeekKey(),
    bots: pool.slice(0, size).map(n=>({
      n, s: Math.round((300 + Math.random() * 8200 * scale) / 10) * 10, w: 2 + Math.floor(Math.random() * 34)
    }))
  };
  try{ localStorage.setItem(k, JSON.stringify(data)); }catch(e){}
  return data;
}
function gmLbTabsRender(){
  const el = document.getElementById('gmLbTabs');
  if(!el) return;
  el.innerHTML = GM_LB_LEAGUES.map(l=>`<button class="gm-lb-tab ${GM_LB_LEAGUE === l.id ? 'on' : ''}" onclick="gmLbSetLeague('${l.id}')">${I(l.ic)}<span>${l.n}</span></button>`).join('');
}
function gmLbSetLeague(id){
  if(!GM_LB_POOLS[id]) return;
  GM_LB_LEAGUE = id;
  gmLbTabsRender();
  gmLbRender();
}
/* переопределение gmLbRank/gmLbRender для 3 лиг — с сохранением базового поведения */
const _gmLbRankBase = gmLbRank;
gmLbRank = function(){
  if(GM_LB_LEAGUE === 'friends' || GM_LB_LEAGUE === 'city' || GM_LB_LEAGUE === 'world'){
    gmLbEnsure();
    const data = gmLbLeagueGet(GM_LB_LEAGUE);
    const rows = data.bots.map(b=>({n:b.n, s:b.s, w:b.w, me:false}));
    /* твоя позиция участвует только в friends / city (не в world по умолчанию) */
    if(GM_LB_LEAGUE !== 'world' || GM_LB.my > 4000){
      rows.push({n: PROFILE.name, s: Math.round(GM_LB.my*100)/100, w: GM_LB.myW, me:true});
    }
    rows.sort((a,b)=>b.s - a.s);
    return rows;
  }
  return _gmLbRankBase.apply(this, arguments);
};
const _gmLbRenderBase = gmLbRender;
gmLbRender = function(){
  const el = document.getElementById('gmLb');
  if(!el) return;
  gmLbEnsure();
  const rows = gmLbRank();
  const myIdx = rows.findIndex(r=>r.me);
  const days = 7 - ((new Date().getDay() + 6) % 7);
  const leagueLabel = (GM_LB_LEAGUES.find(l=>l.id === GM_LB_LEAGUE) || {n:''}).n;
  const nextGap = (myIdx > 0 && myIdx < rows.length) ? rows[Math.max(0, myIdx - 1)].s - rows[myIdx].s : 0;
  let html = `<div class="gm-lb-head"><span>${I('gm-cup')}${leagueLabel} · за эту неделю</span><small>сброс через ${days} ${days === 1 ? 'день' : days < 5 ? 'дня' : 'дней'}</small></div>`;
  html += rows.slice(0, 10).map((r,i)=>gmLbRow(r, i+1)).join('');
  if(myIdx >= 10) html += `<div class="gm-lb-gap">···</div>` + gmLbRow(rows[myIdx], myIdx + 1);
  if(myIdx > 0 && nextGap > 0) html += `<div class="gm-lb-tip">${I('gm-chart')}Ещё ${fmtMoney(nextGap)} призов · и обгонишь ${rows[myIdx-1].n}</div>`;
  if(myIdx < 0) html += `<div class="gm-lb-tip">${I('gm-chart')}Крути и выигрывай — попадёшь в лигу</div>`;
  el.innerHTML = html;
};

/* ============================================================
   ДОСТИЖЕНИЯ 30 с редкостью + фильтром + звуком
   ============================================================ */
const GM_ACH_EXT = [
  {id:'daily-all',   ic:'gm-target',  t:'Мастер дня',      s:'Выполнить все 3 задания дня', rar:'Rare'},
  {id:'daily-week',  ic:'gm-target',  t:'Неделя миссий',   s:'7 дней с выполненными заданиями', rar:'Epic'},
  {id:'gift-first',  ic:'gm-gift',    t:'Первый подарок',  s:'Купи или отправь подарок TON', rar:'Common'},
  {id:'gift-legend', ic:'gm-crown2',  t:'Легенда даров',   s:'Заполучи Legendary-подарок', rar:'Legendary'},
  {id:'gift-collect',ic:'gm-inv',     t:'Коллекционер',    s:'Собери 10 подарков в инвентаре', rar:'Rare'},
  {id:'gift-send5',  ic:'send',       t:'Щедрая душа',     s:'Отправь 5 подарков', rar:'Rare'},
  {id:'m3-line',     ic:'gm-grid',    t:'В линию',         s:'Собери линию в лайм-плитках', rar:'Common'},
  {id:'m3-triple',   ic:'gm-grid',    t:'Три линии',       s:'Собери 3 линии за партию', rar:'Rare'},
  {id:'scratch-hit', ic:'gm-scratch', t:'Скретчёр',        s:'Открой скретч-карту', rar:'Common'},
  {id:'box-open',    ic:'gm-box',     t:'Коробочник',      s:'Открой коробку', rar:'Common'},
  {id:'bp-5',        ic:'gm-bp',      t:'Пропуск 5',       s:'Достичь уровня 5 пропуска', rar:'Common'},
  {id:'bp-15',       ic:'gm-bp',      t:'Пропуск 15',      s:'Достичь уровня 15 пропуска', rar:'Rare'},
  {id:'bp-30',       ic:'gm-bp',      t:'Максимальный',    s:'Достичь уровня 30 пропуска', rar:'Legendary'},
  {id:'streak14',    ic:'gm-flame',   t:'Две недели',      s:'Серия 14 дней', rar:'Rare'},
  {id:'streak30',    ic:'gm-flame2',  t:'Месяц огня',      s:'Серия 30 дней', rar:'Epic'},
  {id:'xp1000',      ic:'gm-xp',      t:'1000 XP',         s:'Набрать 1000 XP всего', rar:'Common'},
  {id:'xp5000',      ic:'gm-xp',      t:'5000 XP',         s:'Набрать 5000 XP всего', rar:'Rare'},
  {id:'city-top10',  ic:'gm-city',    t:'Топ города',      s:'Войди в топ-10 своего города', rar:'Rare'},
  {id:'world-top10', ic:'gm-world',   t:'Мир знает',       s:'Войди в топ-10 мира', rar:'Legendary'},
  {id:'friends-top1',ic:'gm-friends', t:'Первый среди друзей', s:'Занять 1 место в лиге друзей', rar:'Epic'},
  {id:'boost-5',     ic:'gm-boost',   t:'Пять бустов',     s:'Выиграй буст 5 раз', rar:'Rare'},
  {id:'promo-use',   ic:'gm-pct',     t:'Скидка в дело',   s:'Использовать призовую скидку на тариф', rar:'Common'},
  {id:'vip',         ic:'crown',      t:'VIP-статус',      s:'Активировать любой платный тариф', rar:'Rare'}
];
/* Расширяем список: сначала базовые 8, потом ext */
if(typeof GM_ACH !== 'undefined' && GM_ACH.length < 15){
  GM_ACH.forEach(a=>{ if(!a.rar) a.rar = 'Common'; });
  GM_ACH_EXT.forEach(a=>GM_ACH.push(a));
}
let GM_ACH_FILTER = 'all';
function gmAchFilterRender(){
  const el = document.getElementById('gmAchFilter');
  if(!el) return;
  const opts = [
    {id:'all', n:'Все'},
    {id:'unlocked', n:'Открытые'},
    {id:'Common', n:'Common'},
    {id:'Rare', n:'Rare'},
    {id:'Epic', n:'Epic'},
    {id:'Legendary', n:'Legendary'}
  ];
  el.innerHTML = opts.map(o=>`<button class="gm-ach-fil ${GM_ACH_FILTER === o.id ? 'on' : ''}" onclick="gmAchSetFilter('${o.id}')">${o.n}</button>`).join('');
}
function gmAchSetFilter(id){ GM_ACH_FILTER = id; gmAchFilterRender(); gmAchRender(); }
/* Переопределение рендера достижений — с редкостью, фильтром и звуком */
const _gmAchRenderBase = gmAchRender;
gmAchRender = function(){
  const el = document.getElementById('gmAch');
  if(!el) return;
  const a = gmAchGet();
  const done = GM_ACH.filter(x=>a[x.id]).length;
  let list = GM_ACH.slice();
  if(GM_ACH_FILTER === 'unlocked') list = list.filter(x=>a[x.id]);
  else if(GM_ACH_FILTER !== 'all') list = list.filter(x=>(x.rar||'Common') === GM_ACH_FILTER);
  /* сортировка: открытые сначала, затем по редкости (легендарные внизу неоткрытые) */
  const rarOrd = (r)=>({Common:1,Rare:2,Epic:3,Legendary:4})[r] || 1;
  list.sort((x,y)=>{
    const ax = a[x.id] ? 0 : 1, ay = a[y.id] ? 0 : 1;
    if(ax !== ay) return ax - ay;
    return rarOrd(x.rar||'Common') - rarOrd(y.rar||'Common');
  });
  el.innerHTML = list.map(ach=>{
    const on = !!a[ach.id];
    const rar = ach.rar || 'Common';
    const rc = GM_RAR_COLORS[rar] || GM_RAR_COLORS.Common;
    return `<div class="gm-ach ${on ? 'on' : ''} rar-${rar.toLowerCase()}">
      <span class="gm-ach-ic" style="${on ? '' : ''}">${I(ach.ic)}</span>
      <b>${ach.t}</b>
      <small>${ach.s}</small>
      <span class="gm-ach-rar" style="color:${rc.ink};background:${rc.bg}">${rar}</span>
      ${on ? `<span class="gm-ach-check">${I('check')}</span>` : ''}
    </div>`;
  }).join('');
  const head = document.getElementById('gmAchHead');
  if(head) head.textContent = `${done}/${GM_ACH.length}`;
};
/* Расширить gmAchUnlock — играть звук */
const _gmAchUnlockBase = gmAchUnlock;
gmAchUnlock = function(id){
  const before = gmAchGet();
  if(before[id]) return false;
  const r = _gmAchUnlockBase.apply(this, arguments);
  if(r){
    const info = GM_ACH.find(x=>x.id === id);
    const rar = (info && info.rar) || 'Common';
    if(rar === 'Legendary' || rar === 'Epic') gmSfxWin();
    else gmSfxAch();
    gmXpAdd(({Common:30,Rare:80,Epic:150,Legendary:300})[rar] || 30, 'ach:' + id);
  }
  return r;
};
/* Проверки достижений */
function gmAchCheckBp(l){
  if(l >= 5)  gmAchUnlock('bp-5');
  if(l >= 15) gmAchUnlock('bp-15');
  if(l >= 30) gmAchUnlock('bp-30');
}
function gmAchGiftCheck(){
  const inv = gmGiftsInv();
  const hist = gmGiftsHist();
  if(inv.length >= 1) gmAchUnlock('gift-first');
  if(inv.length >= 10) gmAchUnlock('gift-collect');
  if(inv.some(g=>g.rar === 'Legendary')) gmAchUnlock('gift-legend');
  if(hist.filter(h=>h.dir==='out').length >= 5) gmAchUnlock('gift-send5');
}

/* ============================================================
   PERSONAL STATS — «твои шансы за неделю»
   ============================================================ */
function gmStatsOpen(){ openSheet('gmStats'); gmStatsRender(); }
function gmStatsRender(){
  const el = document.getElementById('gmStatsBody');
  if(!el) return;
  const total = gmSpinTotal();
  const weekWin = GM_LB && typeof GM_LB.my === 'number' ? GM_LB.my : 0;
  const weekN = GM_LB && GM_LB.myW ? GM_LB.myW : 0;
  const avg = weekN ? Math.round(weekWin / weekN) : 0;
  const pool = GM_POOLS.p100;
  const tot = pool.reduce((a,p)=>a + p.w, 0);
  const rare = pool.filter(p=>p.c >= 4).reduce((a,p)=>a + p.w, 0) / tot;
  const tierChance = pool.filter(p=>p.t === 'tier').reduce((a,p)=>a + p.w, 0) / tot;
  el.innerHTML = `
    <div class="gm-stats-grid">
      <div class="gm-stats-cell"><span>${I('gm-gift')}</span><b>${total}</b><small>круток всего</small></div>
      <div class="gm-stats-cell"><span>${I('gm-cup')}</span><b>${weekN}</b><small>призов за неделю</small></div>
      <div class="gm-stats-cell"><span>${I('money')}</span><b>${fmtMoney(weekWin)}</b><small>суммарно за неделю</small></div>
      <div class="gm-stats-cell"><span>${I('gm-xp')}</span><b>${gmXpGet()}</b><small>XP всего</small></div>
    </div>
    <h4 class="gm-stats-h">Твой средний приз</h4>
    <p class="gm-stats-note">За неделю: <b>${fmtMoney(avg)}</b> за одну крутку</p>
    <h4 class="gm-stats-h">Шансы редких секторов</h4>
    <div class="gm-stats-bar-row">
      <span>Редкие призы (c4)</span>
      <div class="gm-stats-bar"><i style="width:${(rare * 100 * 3).toFixed(1)}%"></i></div>
      <b>${(rare * 100).toFixed(1)}%</b>
    </div>
    <div class="gm-stats-bar-row">
      <span>Тариф в подарок</span>
      <div class="gm-stats-bar"><i style="width:${(tierChance * 100 * 5).toFixed(1)}%"></i></div>
      <b>${(tierChance * 100).toFixed(1)}%</b>
    </div>
    <h4 class="gm-stats-h">Честность (anti-cheat)</h4>
    <p class="gm-stats-note">Каждый сектор — фиксированный вес. RNG выполняется на устройстве (Math.random). Прошлые крутки не влияют на будущие. Шансы совпадают с «Таблицей шансов».</p>
    <button class="gm-spin ghost" onclick="closeSheet();gmOddsOpen()">${I('gm-scales')}Открыть таблицу шансов</button>`;
}

/* ============================================================
   VIP-КАБИНЕТ — для START/PRO/MAX: удвоенные призы + доп. крутки
   ============================================================ */
function gmVipTier(){
  const t = (typeof PROFILE !== 'undefined' && PROFILE.tier) ? String(PROFILE.tier).toUpperCase() : 'FREE';
  if(t.includes('MAX')) return 'MAX';
  if(t.includes('PRO')) return 'PRO';
  if(t.includes('START')) return 'START';
  if(t.includes('BUSINESS')) return 'BUSINESS';
  return 'FREE';
}
function gmVipMultiplier(){
  const t = gmVipTier();
  if(t === 'MAX' || t === 'BUSINESS') return 2.5;
  if(t === 'PRO') return 2;
  if(t === 'START') return 1.5;
  return 1;
}
function gmVipExtraFree(){
  const t = gmVipTier();
  if(t === 'MAX' || t === 'BUSINESS') return 3;
  if(t === 'PRO') return 2;
  if(t === 'START') return 1;
  return 0;
}
function gmVipBannerRender(){
  const el = document.getElementById('gmVipBanner');
  if(!el) return;
  const t = gmVipTier();
  if(t === 'FREE'){
    el.innerHTML = `<button class="gm-vip-banner off" onclick="if(typeof showTab==='function')showTab('paywall')">
      ${I('crown')}<div><b>VIP-кабинет закрыт</b><small>Активируй START, PRO или MAX — ×2 к призам и +2 бесплатные крутки в день</small></div>${I('chev','gm-bonus-chev')}
    </button>`;
  }else{
    const mul = gmVipMultiplier();
    el.innerHTML = `<button class="gm-vip-banner on" onclick="gmVipOpen()">
      ${I('crown')}<div><b>VIP · ${t}</b><small>×${mul} к призам · +${gmVipExtraFree()} ${gmPluralSpin(gmVipExtraFree())} в день · открыть</small></div>${I('chev','gm-bonus-chev')}
    </button>`;
  }
}
function gmVipOpen(){ openSheet('gmVip'); gmVipRender(); }
function gmVipRender(){
  const el = document.getElementById('gmVipBody');
  if(!el) return;
  const t = gmVipTier();
  const mul = gmVipMultiplier();
  const claimedKey = 'oko-games-vip-free-' + gmDayIdx();
  let claimed = 0;
  try{ claimed = parseInt(localStorage.getItem(claimedKey),10) || 0; }catch(e){}
  const max = gmVipExtraFree();
  const left = Math.max(0, max - claimed);
  if(t === 'FREE'){
    el.innerHTML = `<div class="gm-vip-lock">${I('lock')}<b>VIP-кабинет закрыт</b><small>Оформи START, PRO или MAX — множитель призов, дополнительные бесплатные крутки, приоритет в лидерах</small><button class="gm-spin" onclick="closeSheet();if(typeof showTab==='function')showTab('paywall')">${I('crown')}Смотреть тарифы</button></div>`;
    return;
  }
  el.innerHTML = `
    <div class="gm-vip-hero">
      <span>${I('crown')}</span>
      <div>
        <b>VIP · ${t}</b>
        <small>активные бонусы в рулетке</small>
      </div>
    </div>
    <div class="gm-vip-perks">
      <div class="gm-vip-perk"><span>${I('gm-boost')}</span><b>×${mul}</b><small>множитель денежных призов</small></div>
      <div class="gm-vip-perk"><span>${I('gm-gift')}</span><b>+${max}</b><small>бесплатных круток в день</small></div>
      <div class="gm-vip-perk"><span>${I('gm-cup')}</span><b>+50%</b><small>вес XP-начислений</small></div>
      <div class="gm-vip-perk"><span>${I('gm-scales')}</span><b>Всегда</b><small>приоритет при равном призе</small></div>
    </div>
    <div class="gm-vip-daily">
      <b>Ежедневные VIP-крутки</b>
      <small>Забрано сегодня: ${claimed}/${max}</small>
      <button class="gm-spin ${left ? '' : 'off'}" onclick="gmVipClaimFree()" ${left ? '' : 'disabled'}>${I('gm-gift')}${left ? `Забрать ${left} ${gmPluralTk(left).replace('билет','крутк')}` : 'Уже забраны'}</button>
    </div>
    <p class="gm-stats-note">VIP-множитель применяется автоматически к каждой денежной выплате в колесе. Крутки идут сверх лимита и не занимают дневной подарок.</p>`;
}
function gmVipClaimFree(){
  const t = gmVipTier();
  if(t === 'FREE'){ toast('Нужен активный тариф'); return; }
  const max = gmVipExtraFree();
  const claimedKey = 'oko-games-vip-free-' + gmDayIdx();
  let claimed = 0;
  try{ claimed = parseInt(localStorage.getItem(claimedKey),10) || 0; }catch(e){}
  const left = Math.max(0, max - claimed);
  if(!left){ toast('VIP-крутки на сегодня забраны'); return; }
  try{ localStorage.setItem(claimedKey, String(claimed + left)); }catch(e){}
  gmExtraFreeSet(gmExtraFreeGet() + left);
  toast(`+${left} бесплатных круток от VIP`);
  gmSfxAch();
  gmVipRender();
  gmRenderModes();
}
/* Применяем VIP-множитель поверх gmApplyBoost */
const _gmApplyBoostBase = gmApplyBoost;
gmApplyBoost = function(base){
  let val = _gmApplyBoostBase.apply(this, arguments);
  const mul = gmVipMultiplier();
  if(mul > 1 && val > 0){
    const before = val;
    val = Math.round(val * mul * 100) / 100;
    toast(`VIP ×${mul}: +${fmtMoney(val - before)}`);
  }
  return val;
};

/* ============================================================
   ИНТЕГРАЦИЯ: cчётчики круток → challenges + XP + achievements
   ============================================================ */
const _gmGrantBase = gmGrant;
gmGrant = function(p, mode, payTicket){
  _gmGrantBase.apply(this, arguments);
  gmDailyBump('spins');
  if(mode !== 'free') gmDailyBump('paidspin');
  gmXpAdd(mode === 'free' ? 15 : 25, 'spin');
  /* стрик достижения расширенные */
  const s = gmStreakGet();
  if(s.n >= 14) gmAchUnlock('streak14');
  if(s.n >= 30) gmAchUnlock('streak30');
  if(gmXpGet() >= 1000) gmAchUnlock('xp1000');
  if(gmXpGet() >= 5000) gmAchUnlock('xp5000');
  /* лидерборды-достижения */
  try{
    const cityRank = (function(){ const rows = _gmLbRankBase.call({}); return 0; })();
  }catch(e){}
  if(gmVipTier() !== 'FREE') gmAchUnlock('vip');
};

/* Патчим существующий gmPrizeUse для достижения promo-use */
const _gmPrizeUseBase = gmPrizeUse;
gmPrizeUse = function(id){
  _gmPrizeUseBase.apply(this, arguments);
  const p = GM_PRIZES.find(x=>x.id === id);
  if(p && p.type === 'discount' && p.status === 'used') gmAchUnlock('promo-use');
};

/* ============================================================
   MASTER init: показать все новые блоки на экране игр
   ============================================================ */
const _prevShowTabGm2 = showTab;
showTab = function(t){
  _prevShowTabGm2(t);
  if(t === 'games'){
    gmDailyRenderCard();
    gmMiniRender();
    gmBpRenderCard();
    gmGiftsRailRender();
    gmGiftsInvBtnRender();
    gmLbTabsRender();
    gmAchFilterRender();
    gmVipBannerRender();
    gmScheduleRemind();
    /* засчитать «зашёл сегодня» в челленджах */
    gmDailyBump('streakday');
  }
};

/* Первичный рендер (когда games — стартовая вкладка) */
setTimeout(function gmInit2(){
  gmDailyRenderCard();
  gmMiniRender();
  gmBpRenderCard();
  gmGiftsRailRender();
  gmGiftsInvBtnRender();
  gmLbTabsRender();
  gmAchFilterRender();
  gmVipBannerRender();
  gmScheduleRemind();
  /* при первом заходе — засчитать «зашёл сегодня» в челлендж */
  gmDailyBump('streakday');
}, 60);

/* ===== module: academy-content ===== */
/* ===== АКАДЕМИЯ · КОНТЕНТ-ПАК (сгенерировано build_pack.mjs) =====================
   Доп.уроки блоков. Грузится ПЕРЕД модулем academy → window.AC_PACK { blockId: [урок…] }.
   Формат урока идентичен academy/script.js. Иконки слайдов — бренд-SVG по ключу. */
window.AC_PACK = Object.assign(window.AC_PACK||{}, {
  "m1": [
{
  title:'Три типа участников',
  sub:'3:12 · видео (голос Даниэля) + тест + игра', dur:'3:12',
  videoUrl:'https://okoteam.top/media/oko_m1_0.mp4',
  c1:'ТРИ', c2:'ТИПА',
  slides:[
  {t:'Один курс — три разных старта', pts:['Новичок: аккаунт с нуля, задача — первые <b>деньги с блога</b> и первый клиент', 'Практик: блог уже есть, задача — навести порядок в <b>упаковке</b> и контенте', 'Бизнес: аккаунт как <b>канал продаж</b>, задача — заявки и выручка, а не лайки', 'Материал один, но путь у каждого свой — важно понять, где ты сейчас'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Новичок с нуля', pts:['Нет аудитории, нет насмотренности, есть страх камеры — это нормально', 'Первая цель узкая: не 100к подписчиков, а первые <b>10 000 рублей</b> за услугу', 'Учишься на потоке — снимаешь плохо, но снимаешь каждый день', 'Главный враг — перфекционизм, главный друг — количество'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Практик — навести порядок', pts:['Блог ведёшь, но хаотично: шапка не продаёт, рубрики размыты, охваты скачут', 'Задача — собрать <b>упаковку</b> и контент-систему, а не искать новую платформу', 'Часто тормозит не отсутствие знаний, а отсутствие структуры', 'Быстрый рост даёт наведение фокуса на 1-2 рабочих формата'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Бизнес — аккаунт как продажник', pts:['Соцсеть — не витрина для красоты, а <b>воронка</b> к заявке и оплате', 'Метрика успеха — стоимость заявки и выручка, а не охват', 'Контент строится вокруг продукта, возражений и кейсов клиентов', 'Собственнику важно делегировать съёмку, но держать смыслы за собой'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Зачем определять свой тип', pts:['Один и тот же приём новичку и бизнесу даёт разный результат', 'Определив тип, ты берёшь из курса только то, что двигает тебя сейчас', 'Не распыляешься на чужие задачи — экономишь недели времени', 'Тип может меняться: с нуля дорос до практика — сменил фокус'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Какая первая цель у новичка по школе ХЛБ?', o:['100 000 подписчиков', 'Первые деньги с блога и первый клиент', 'Идеальная съёмка', 'Вирусный ролик на 1 млн'], a:1},
  {q:'Главная задача типа «практик»?', o:['Начать блог с нуля', 'Навести порядок в упаковке и контенте', 'Купить рекламу', 'Сменить платформу'], a:1},
  {q:'Что для бизнеса главная метрика аккаунта?', o:['Лайки', 'Заявки и выручка', 'Число сторис', 'Красивая лента'], a:1},
  {q:'Почему важно определить свой тип с первого дня?', o:['Чтобы получить скидку', 'Чтобы брать из курса то, что двигает именно тебя', 'Чтобы соревноваться', 'Это не важно'], a:1},
  {q:'Что чаще всего тормозит практика?', o:['Нехватка знаний', 'Отсутствие структуры и фокуса', 'Отсутствие камеры', 'Плохой интернет'], a:1}
  ],
  pairs:[
  ['Новичок', 'Аккаунт с нуля и первые деньги'],
  ['Практик', 'Порядок в упаковке и контенте'],
  ['Бизнес', 'Аккаунт как канал продаж'],
  ['Метрика бизнеса', 'Стоимость заявки и выручка'],
  ['Враг новичка', 'Перфекционизм вместо количества']
  ],
  task:{
    intro:'Определи свой тип участника и распиши, какая узкая цель у тебя на ближайший месяц, исходя из этого типа.',
    chips:['Мой тип: новичок/практик/бизнес', 'Что уже есть', 'Чего не хватает', 'Узкая цель на месяц', 'Первая метрика успеха', 'Что беру из курса в первую очередь'],
    ph:'Тип: практик. Есть блог 1200 подписчиков, но шапка не продаёт и рубрик нет. Цель на месяц: собрать упаковку и 2 рабочих рубрики, поднять охваты reels с 300 до 1500. Метрика: 3 заявки в личку с блога.',
    verdict:'Сильно — ты честно назвал тип и поставил измеримую цель с цифрой охвата и заявок, это уже не «хочу больше». Усилю одним: сразу пропиши, какой ОДИН формат reels ты берёшь на весь месяц, чтобы не распыляться.'
  }
},
{
  title:'Кейс на себе — полигон',
  sub:'3:28 · видео (голос Даниэля) + тест + игра', dur:'3:28',
  videoUrl:'https://okoteam.top/media/oko_m1_1.mp4',
  c1:'ПОЛИГОН', c2:'НА СЕБЕ',
  slides:[
  {t:'Твой аккаунт — испытательный стенд', pts:['Каждый приём с курса проверяй сначала на своём блоге, а не в теории', 'Свой аккаунт — бесплатный полигон, где ошибаться не страшно', 'Ведёшь блог с первого дня обучения, не «когда буду готов»', 'Кейс на себе — главный аргумент, когда придёшь к клиенту'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Почему теория без практики не работает', pts:['Знание про <b>крючок</b> в ролике ничего не стоит, пока не снял 5 своих', 'Мозг запоминает не лекцию, а собственную набитую шишку', 'На полигоне ты видишь реальные цифры: досмотры, охваты, заявки', 'Практик с 30 роликами обгонит теоретика с 30 конспектами'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Что тестировать на полигоне', pts:['Разные <b>крючки</b> первых 3 секунд — что удерживает досмотр', 'Форматы: говорящая голова, экспертная нарезка, история клиента', 'Время выхода и частоту постинга — когда твоя аудитория живее', 'Оформление обложек и подписей — рост сохранений'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Полигон продаёт за тебя', pts:['Когда сам вырастил охваты — тебе верят без слов', 'Скрин своей статистики сильнее любого диплома', 'Клиент покупает результат, который видит на твоём блоге', 'Первые заявки часто приходят прямо с блога-полигона'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Ошибка новичков — ждать идеала', pts:['«Начну вести, когда разберусь» — так не начинают никогда', 'Полигон нужен грязный и рабочий, а не вылизанный и пустой', 'Публикуй сырое — правь по ходу, метрики покажут направление', 'Каждый день без полигона — минус данные о себе'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c4 12-6 16-6 26 0-6-5-9-5-9-4 6-11 11-11 22a22 22 0 0 0 44 0c0-14-12-20-16-39z"/></svg>'}
  ],
  quiz:[
  {q:'Что значит «кейс на себе»?', o:['Снять один идеальный ролик', 'Вести свой аккаунт как полигон и проверять каждый приём', 'Купить готовый блог', 'Учить теорию наизусть'], a:1},
  {q:'Когда начинать вести блог-полигон?', o:['Когда пройдёшь весь курс', 'С первого дня обучения', 'Когда будет 1000 подписчиков', 'Когда купишь камеру'], a:1},
  {q:'Почему полигон помогает продавать услуги?', o:['Даёт диплом', 'Свой результат и статистика убеждают клиента без слов', 'Даёт скидку на рекламу', 'Делает ленту красивой'], a:1},
  {q:'Что лучше для обучения?', o:['30 конспектов теории', '30 снятых своих роликов с цифрами', 'Один вылизанный ролик', 'Ждать вдохновения'], a:1},
  {q:'Главная ошибка на старте полигона?', o:['Снимать много', 'Ждать идеала и не публиковать', 'Пробовать форматы', 'Смотреть статистику'], a:1}
  ],
  pairs:[
  ['Полигон', 'Свой блог для проверки приёмов'],
  ['Кейс на себе', 'Результат, который продаёт услугу'],
  ['Теория без практики', 'Знание, что не превратилось в навык'],
  ['Что тестируем', 'Крючки, форматы, время выхода'],
  ['Ошибка старта', 'Ждать идеала вместо публикаций']
  ],
  task:{
    intro:'Выбери 3 приёма из курса, которые ты протестируешь на своём блоге на этой неделе, и укажи, по какой метрике поймёшь результат.',
    chips:['Приём 1 и метрика', 'Приём 2 и метрика', 'Приём 3 и метрика', 'Сколько роликов сниму', 'Дни публикаций', 'Как замерю до/после'],
    ph:'Приём 1: сильный крючок в первые 3 сек — метрика удержание с 45% до 60%. Приём 2: формат «история клиента» — метрика сохранения. Приём 3: постинг в 19:00 — метрика охват первого часа. Сниму 5 роликов пн-пт, замер по статистике до/после.',
    verdict:'Отлично — ты привязал каждый приём к конкретной метрике и поставил замер до/после, это и есть работа на полигоне. Усилю: заведи простую табличку и вноси цифры в тот же день, иначе через неделю не вспомнишь, что сработало.'
  }
},
{
  title:'Стадии обучения',
  sub:'3:40 · видео (голос Даниэля) + тест + игра', dur:'3:40',
  videoUrl:'https://okoteam.top/media/oko_m1_2.mp4',
  c1:'СТАДИИ', c2:'ПУТИ',
  slides:[
  {t:'Эмоции в обучении — предсказуемы', pts:['Проходя курс, ты проживёшь 4 стадии — это норма, а не поломка', 'Энтузиазм на старте сменится спадом, и это закономерно', 'Знать стадии — значит не бросить на самой тяжёлой', 'Плато проходят все, кто дошёл до результата'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Стадия 1 — энтузиазм', pts:['Всё новое, горят глаза, готов снимать по 5 роликов в день', 'Опасность — быстро выгореть на всплеске мотивации', 'Задача стадии — заложить <b>график</b>, а не жечь всё топливо сразу', 'Энтузиазм не топливо на дистанцию, топливо — система'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Стадия 2 — «дайте новое»', pts:['Базу освоил, кажется, что старое скучно и нужны «фишки»', 'Ловушка — гоняться за новыми приёмами вместо повторения рабочих', 'Рост даёт не новизна, а доведение базовых форматов до автомата', 'Здесь отваливаются те, кто путает скуку с потолком'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Стадия 3 — «все виноваты»', pts:['Результат не летит, и мозг ищет виноватых: алгоритм, ниша, время', 'На деле — это сигнал вернуться к дисциплине и цифрам', 'Внешние причины комфортны, но не двигают ни на шаг', 'Выход — перестать искать виноватых и снять следующий ролик'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Стадия 4 — усталость и плато', pts:['Рост замедлился, эмоций нет, кажется, что топчешься на месте', 'Плато — это накопление перед скачком, а не конец пути', 'Спасает не мотивация, а <b>график</b> и микроцели на неделю', 'Кто проходит плато на дисциплине — выходит на новый уровень'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Как пройти все стадии', pts:['Назови стадию, на которой стоишь, — станет легче', 'Опирайся на <b>график</b>, а не на настроение дня', 'Держи цель в цифрах перед глазами, чтобы не сдуться', 'Помни: спад временный, система вытянет'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'}
  ],
  quiz:[
  {q:'Сколько эмоциональных стадий проживает ученик?', o:['Две', 'Четыре', 'Семь', 'Стадий не бывает'], a:1},
  {q:'Опасность стадии энтузиазма?', o:['Слишком медленный старт', 'Выгореть на всплеске и бросить систему', 'Нет опасности', 'Мало идей'], a:1},
  {q:'Что стоит за стадией «дайте новое»?', o:['Реальный потолок', 'Скука, которую путают с потолком', 'Технический сбой', 'Конец курса'], a:1},
  {q:'О чём сигналит стадия «все виноваты»?', o:['Пора менять нишу', 'Пора вернуться к дисциплине и цифрам', 'Алгоритм реально сломан', 'Надо бросить'], a:1},
  {q:'Что такое плато?', o:['Конец роста', 'Накопление перед скачком', 'Ошибка съёмки', 'Признак плохой ниши'], a:1},
  {q:'На что опираться, чтобы пройти стадии?', o:['На настроение', 'На график и цель в цифрах', 'На удачу', 'На новые фишки'], a:1}
  ],
  pairs:[
  ['Энтузиазм', 'Горят глаза, риск выгореть'],
  ['«Дайте новое»', 'Скуку путают с потолком'],
  ['«Все виноваты»', 'Поиск внешних причин'],
  ['Усталость', 'Плато перед скачком'],
  ['Что вытягивает', 'График, а не мотивация']
  ],
  task:{
    intro:'Определи, на какой стадии ты сейчас, опиши свои эмоции честно и напиши одно действие, которое удержит тебя на дистанции.',
    chips:['Моя стадия сейчас', 'Что чувствую', 'На что тянет свалить вину', 'Что говорят мои цифры', 'Одно удерживающее действие', 'Микроцель на неделю'],
    ph:'Стадия: «все виноваты». Чувствую раздражение, охваты упали и хочется винить алгоритм. Цифры: я снял 4 ролика за 2 недели вместо 10. Удерживающее действие: вернуть график 5 роликов в неделю. Микроцель: 5 публикаций до воскресенья.',
    verdict:'Честно и по делу — ты поймал себя на поиске виноватых и сам увидел провал по количеству, это половина решения. Усилю: повесь микроцель на видное место и отмечай каждый выпущенный ролик галочкой, дисциплина любит видимый счёт.'
  }
},
{
  title:'Кринж-этап и первые клипы',
  sub:'3:20 · видео (голос Даниэля) + тест + игра', dur:'3:20',
  videoUrl:'https://okoteam.top/media/oko_m1_3.mp4',
  c1:'КРИНЖ', c2:'ЭТАП',
  slides:[
  {t:'Кринж проходят все', pts:['Первые ролики будут вызывать стыд — это универсальный опыт', 'Даже блогеры-миллионники начинали с зажатых и неловких видео', 'Кринж — не признак бездарности, а признак старта', 'Единственный способ пройти этап — пройти его насквозь, снимая'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c4 12-6 16-6 26 0-6-5-9-5-9-4 6-11 11-11 22a22 22 0 0 0 44 0c0-14-12-20-16-39z"/></svg>'},
  {t:'Качество приходит с количеством', pts:['Первые <b>20-30 роликов</b> — это тренировка, а не портфолио', 'Каждый ролик прокачивает речь, кадр, монтаж на 1%', 'Не жди шедевра на пятом видео — жди роста к тридцатому', 'Количество набивает навык быстрее любого разбора теории'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Что именно тренируется', pts:['Речь: уходит зажатость, появляется темп и <b>крючок</b>', 'Кадр: учишься свету, ракурсу, фону без лишних движений', 'Монтаж: ритм, склейки, субтитры входят в привычку', 'Насмотренность на себе: видишь свои ошибки и правишь'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M52 24l20 11-20 11z" fill="currentColor" stroke="none"/></svg>'},
  {t:'Как ускорить выход из кринжа', pts:['Снимай сериями, не по одному ролику в неделю', 'Пересматривай без самобичевания — ищи одну правку на ролик', 'Не удаляй старое: сравнение первого и тридцатого мотивирует', 'Публикуй сразу — стол «на потом» убивает прогресс'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Кринж как фильтр', pts:['Большинство бросает на первых неловких видео — и уступает место тебе', 'Кто прошёл 30 роликов, входит в меньшинство, у которого получается', 'Стыд конечен: к третьему десятку камера станет своей', 'Твоё преимущество — не талант, а то, что ты не остановился'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 12h40v14a20 20 0 0 1-40 0z"/><path d="M40 18H26c0 12 8 16 16 16M80 18h14c0 12-8 16-16 16M60 46v10M46 62h28M52 56h16"/></svg>'}
  ],
  quiz:[
  {q:'Кто проходит кринж-этап первых роликов?', o:['Только новички без таланта', 'Все, включая топовых блогеров', 'Никто, если есть талант', 'Только застенчивые'], a:1},
  {q:'Сколько первых роликов считаются тренировкой?', o:['1-2', '20-30', '100-150', 'Ни один'], a:1},
  {q:'Как быстрее выйти из кринжа?', o:['Ждать вдохновения', 'Снимать сериями и публиковать сразу', 'Снять один идеальный', 'Учить теорию год'], a:1},
  {q:'Что даёт количество роликов?', o:['Только усталость', 'Рост навыка речи, кадра и монтажа', 'Ничего', 'Падение охватов'], a:1},
  {q:'Почему кринж-этап — это фильтр?', o:['Он отсеивает камеры', 'Большинство бросает, и остаются те, у кого получается', 'Он снижает качество', 'Он не важен'], a:1}
  ],
  pairs:[
  ['Кринж', 'Стыд за первые ролики'],
  ['20-30 роликов', 'Этап тренировки навыка'],
  ['Количество', 'Путь к качеству'],
  ['Серии съёмок', 'Ускорение выхода из кринжа'],
  ['Фильтр', 'Большинство бросает на старте']
  ],
  task:{
    intro:'Запланируй серию из 10 роликов на ближайшие 2 недели и напиши, какую одну вещь ты будешь улучшать от ролика к ролику.',
    chips:['Тема серии', 'Число роликов и срок', 'Что улучшаю (речь/кадр/монтаж)', 'Как замерю прогресс', 'Правило: не удалять старое', 'Где публикую'],
    ph:'Серия: 10 роликов про мою нишу за 2 недели, по одному в будни. Улучшаю крючок первых 3 секунд — от простого к цепляющему. Прогресс замеряю по удержанию в статистике. Старые не удаляю, сравню 1-й и 10-й. Публикую в reels каждый день в 19:00.',
    verdict:'Хороший план — ты сфокусировался на ОДНОМ навыке (крючок) и не распыляешься, так рост виден четче. Усилю: после каждого ролика записывай одним словом, что улучшить в следующем, — превратишь съёмку в осознанную тренировку, а не в конвейер.'
  }
},
{
  title:'Цель в цифрах и срок',
  sub:'3:34 · видео (голос Даниэля) + тест + игра', dur:'3:34',
  videoUrl:'https://okoteam.top/media/oko_m1_4.mp4',
  c1:'ЦЕЛЬ', c2:'В ЦИФРАХ',
  slides:[
  {t:'«Хочу больше» — это не цель', pts:['Размытое желание не двигает и не проверяется', 'Цель обязана иметь <b>число</b> и <b>срок</b>, иначе это мечта', '«Хочу расти» → «100 000 рублей с блога к 1 октября»', 'Конкретика включает мозг на поиск шагов, а не на жалость к себе'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Формула рабочей цели', pts:['Число + срок + метрика: что, к когда, чем измеряем', 'Плохо: «больше клиентов». Хорошо: «5 клиентов по 20к до 30 сентября»', 'Цель должна быть амбициозной, но достижимой за срок', 'Одна главная цель важнее пяти размытых'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Пропиши жизнь при цели', pts:['Опиши, как выглядит твой день, когда цель достигнута', 'Что изменится: доход, свобода, статус, ощущения', 'Образ жизни при цели — топливо на плато и в усталость', 'Мозг тянется к яркой картинке сильнее, чем к сухой цифре'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Декомпозиция — цель на недели', pts:['Годовую цель режь на месяцы, месяц — на недели', '100к за 3 месяца → ~33к в месяц → понятные шаги на неделю', 'Маленькие сроки не дают откладывать на «потом»', 'Каждую неделю сверяйся: иду в график или отстаю'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Цель на видном месте', pts:['Цель, которую не видишь каждый день, забывается за неделю', 'Держи число и срок перед глазами: заставка, стикер, доска', 'Сверяй решения с целью: этот ролик приближает к ней?', 'Достиг — ставь следующую, не зависай на плато успеха'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M36 8v54"/><path d="M36 12h44l-8 12 8 12H36z"/></svg>'}
  ],
  quiz:[
  {q:'Почему «хочу больше» — плохая цель?', o:['Слишком амбициозна', 'Нет числа и срока, её нельзя проверить', 'Слишком скромна', 'Это отличная цель'], a:1},
  {q:'Что входит в формулу рабочей цели?', o:['Только желание', 'Число + срок + метрика', 'Настроение и удача', 'Много подцелей сразу'], a:1},
  {q:'Зачем прописывать жизнь при цели?', o:['Для красоты', 'Яркий образ даёт топливо на плато и в усталость', 'Чтобы похвастаться', 'Это лишнее'], a:1},
  {q:'Что такое декомпозиция цели?', o:['Отказ от цели', 'Разбивка большой цели на недели и шаги', 'Удвоение цели', 'Перенос срока'], a:1},
  {q:'Что делать с достигнутой целью?', o:['Долго отдыхать на успехе', 'Ставить следующую, не зависая', 'Забыть', 'Снизить планку'], a:1}
  ],
  pairs:[
  ['«Хочу больше»', 'Мечта без числа и срока'],
  ['Рабочая цель', 'Число + срок + метрика'],
  ['Жизнь при цели', 'Топливо на тяжёлых стадиях'],
  ['Декомпозиция', 'Разбивка цели на недели'],
  ['Цель на виду', 'Ежедневная сверка решений']
  ],
  task:{
    intro:'Сформулируй одну главную цель по формуле «число + срок + метрика» и распиши, как выглядит твоя жизнь в день, когда ты её достиг.',
    chips:['Число', 'Срок (дата)', 'Метрика', 'Шаг на эту неделю', 'Образ жизни при цели', 'Где повешу цель на видное место'],
    ph:'Цель: 60 000 рублей с блога к 15 октября, метрика — оплаченные услуги. Шаг недели: 5 продающих reels и 3 прогрева в сторис. Жизнь при цели: работаю из дома, беру 3 клиентов в месяц, спокоен за аренду. Повешу цель на заставку телефона.',
    verdict:'Мощно — у тебя есть число, дата и метрика оплат, а не лайков, это взрослая постановка цели. Усилю: разбей 60к на недельные подцели (по 15к) и в конце каждой недели сверяйся, иначе к 15 октября сложно понять, успеваешь ли ты.'
  }
},
{
  title:'Дисциплина против настроения',
  sub:'3:26 · видео (голос Даниэля) + тест + игра', dur:'3:26',
  videoUrl:'https://okoteam.top/media/oko_m1_5.mp4',
  c1:'ДИСЦИПЛИНА', c2:'БЬЁТ НАСТРОЙ',
  slides:[
  {t:'Настроение — плохой начальник', pts:['Ждать вдохновения = снимать 4 ролика в месяц вместо 20', 'Настроение скачет каждый день, результат требует ровного темпа', 'Профи снимает и в плохой день — по <b>графику</b>, а не по настрою', 'Кто работает на настроении, всегда проигрывает тому, кто на системе'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'График сильнее мотивации', pts:['<b>График</b> убирает ежедневный вопрос «а хочу ли я сегодня»', 'Решение принято один раз — дальше просто исполняешь', 'Фиксированные дни и время съёмки экономят силу воли', 'Система работает, даже когда мотивация на нуле'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="16" width="76" height="46" rx="8"/><path d="M22 30h76M38 10v12M82 10v12"/><circle cx="44" cy="44" r="3" fill="currentColor" stroke="none"/><circle cx="60" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Как построить рабочий график', pts:['Назначь конкретные дни и часы под съёмку и монтаж', 'Ставь реальную норму: 5 роликов в неделю, а не 30', 'Батчинг: сними 5 роликов за один заход, публикуй по дням', 'Защити слоты в календаре как встречи, которые нельзя отменить'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'},
  {t:'Дни, когда не хочется', pts:['Правило минимума: плохой день — сними хотя бы один короткий', 'Действие рождает настроение, а не наоборот', 'Снизь планку качества, но не пропускай день целиком', 'Один сделанный ролик в спад ценнее пяти в идеальный день'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Дисциплина как навык', pts:['Дисциплина не врождённая — она тренируется повторением', 'Каждое выполненное обещание себе усиливает следующее', 'Считай серию дней в графике — не рви цепочку', 'Через месяц система станет привычкой, воля почти не нужна'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Почему работать на настроении проигрышно?', o:['Настроение всегда хорошее', 'Оно скачет, а результат требует ровного темпа', 'Так быстрее', 'Это миф'], a:1},
  {q:'Что даёт график?', o:['Больше сомнений', 'Убирает ежедневный вопрос «хочу ли я»', 'Снижает качество', 'Отнимает время'], a:1},
  {q:'Что такое батчинг?', o:['Удаление роликов', 'Снять серию за один заход, публиковать по дням', 'Съёмка раз в месяц', 'Покупка рекламы'], a:1},
  {q:'Что делать в день, когда не хочется?', o:['Пропустить полностью', 'Сделать минимум — хотя бы один короткий ролик', 'Ждать настроения', 'Удалить блог'], a:1},
  {q:'Дисциплина — это...', o:['Врождённый дар', 'Навык, который тренируется повторением', 'Удача', 'Настроение'], a:1}
  ],
  pairs:[
  ['Настроение', 'Скачет, подводит на дистанции'],
  ['График', 'Решение принято один раз'],
  ['Батчинг', 'Серия роликов за один заход'],
  ['Правило минимума', 'Хотя бы один ролик в спад'],
  ['Дисциплина', 'Тренируемый навык, не дар']
  ],
  task:{
    intro:'Составь свой съёмочный график на неделю с конкретными днями и временем и пропиши правило минимума на день, когда нет сил.',
    chips:['Дни съёмки', 'Время слотов', 'Норма роликов в неделю', 'День монтажа/публикаций', 'Правило минимума', 'Как отмечаю выполнение'],
    ph:'График: съёмка вт и чт с 10:00 до 12:00, батчингом по 3 ролика. Норма — 6 reels в неделю. Монтаж по вечерам, публикация пн-сб в 19:00. Правило минимума: в плохой день снимаю один 15-секундный ролик на телефон без монтажа. Отмечаю галочками в заметках.',
    verdict:'Отличная система — у тебя есть фиксированные слоты, батчинг и правило минимума, это защищает от провалов настроения. Усилю: поставь слоты съёмки как повторяющиеся события в календарь с напоминанием, тогда мозг перестанет каждый раз решать заново.'
  }
},
{
  title:'Окружение и насмотренность',
  sub:'3:30 · видео (голос Даниэля) + тест + игра', dur:'3:30',
  videoUrl:'https://okoteam.top/media/oko_m1_6.mp4',
  c1:'ОКРУЖЕНИЕ', c2:'ТЯНЕТ ВВЕРХ',
  slides:[
  {t:'Ты — среднее своего окружения', pts:['Люди вокруг задают твою планку нормы, часто незаметно', 'В среде, где все растут, расти становится естественно', 'Скептики и «зачем тебе это» тянут вниз без злого умысла', 'Смени окружение — и планка целей поднимется сама'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Насмотренность — топливо для контента', pts:['<b>Насмотренность</b> — насколько много сильного контента ты видел', 'Кто смотрит топовые reels осознанно, снимает лучше интуитивно', 'Мозг собирает банк приёмов: крючки, монтаж, подачи', 'Без насмотренности изобретаешь велосипед и стоишь на месте'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Как смотреть с пользой', pts:['Смотри не как зритель, а как разбирающий: почему зацепило', 'Разбирай крючок, ритм, склейки, финал — что удержало досмотр', 'Заведи папку сохранённых сильных роликов-референсов', '10 минут осознанного разбора ценнее часа бездумной ленты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Собери растущее окружение', pts:['Найди чаты, мастермайнды, коллег по нише на одном пути', 'Общение с теми, кто уже дальше, ускоряет тебя в разы', 'Публично заяви цель — окружение станет держателем слова', 'Одиночке легко бросить, в среде — стыдно отставать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Чисти вредное окружение', pts:['Отпишись от аккаунтов, что вызывают зависть, а не идеи', 'Убери из ленты тех, кто гасит инициативу', 'Меньше пассивного скролла — больше целевого разбора', 'Твоё внимание — ресурс, вкладывай его в рост, а не в шум'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h80L68 44v18l-16 8V44z"/></svg>'}
  ],
  quiz:[
  {q:'Как окружение влияет на результат?', o:['Никак', 'Задаёт планку нормы и целей', 'Только мешает', 'Влияет лишь на новичков'], a:1},
  {q:'Что такое насмотренность?', o:['Число подписчиков', 'Объём сильного контента, который ты осознанно видел', 'Время в ленте', 'Количество камер'], a:1},
  {q:'Как смотреть контент с пользой?', o:['Как обычный зритель', 'Разбирая, почему зацепило: крючок, ритм, финал', 'На перемотке', 'Не смотреть вовсе'], a:1},
  {q:'Зачем растущее окружение?', o:['Для лайков', 'Оно ускоряет и держит тебя на слове', 'Для рекламы', 'Незачем'], a:1},
  {q:'Что делать с вредным окружением?', o:['Терпеть', 'Чистить ленту от зависти и гасителей', 'Ничего', 'Смотреть больше'], a:1}
  ],
  pairs:[
  ['Окружение', 'Задаёт планку нормы'],
  ['Насмотренность', 'Банк сильных приёмов'],
  ['Осознанный просмотр', 'Разбор, почему зацепило'],
  ['Растущая среда', 'Ускоряет и держит слово'],
  ['Чистка ленты', 'Убрать зависть и гасителей']
  ],
  task:{
    intro:'Собери свой план прокачки насмотренности и окружения: что смотришь, где ищешь среду и от чего чистишь ленту.',
    chips:['3 аккаунта для разбора', 'Что разбираю в них', 'Где искать растущую среду', 'Папка референсов', 'От чего чищу ленту', 'Сколько минут в день на разбор'],
    ph:'Разбираю 3 топовых блогера в моей нише: крючки и ритм монтажа. Среду ищу в 2 профильных чатах и мастермайнде. Завёл папку «референсы» — 20 роликов. Чищу ленту от аккаунтов, что вызывают зависть без пользы. 15 минут в день на осознанный разбор с заметками.',
    verdict:'Класс — ты перевёл насмотренность из пассивного скролла в систему с папкой референсов и разбором, это редко кто делает. Усилю: раз в неделю бери один приём из папки и применяй в своём ролике, иначе референсы останутся коллекцией, а не навыком.'
  }
},
{
  title:'План действий на 10 пунктов (ДЗ)',
  sub:'3:52 · видео (голос Даниэля) + тест + игра', dur:'3:52',
  videoUrl:'https://okoteam.top/media/oko_m1_7.mp4',
  c1:'ПЛАН', c2:'10 ПУНКТОВ',
  slides:[
  {t:'Собираем всё в один план', pts:['Финал блока — <b>план на 10 пунктов</b>, который свяжет всё вместе', 'План превращает знания курса в конкретные шаги', 'Без плана мотивация после урока испаряется за 2 дня', 'Написанный план — контракт с собой, а не благое намерение'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Что обязательно в плане', pts:['Твой тип участника и главная цель в <b>цифрах и сроке</b>', 'Целевые цифры: доход, число роликов, охваты, заявки', 'Образ жизни при достижении цели — зачем всё это', 'Конкретные действия на неделю, а не расплывчатые мечты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Каркас 10 пунктов', pts:['1-2: цель в цифрах и срок; 3: образ жизни при цели', '4-5: съёмочный <b>график</b> и норма роликов в неделю', '6-7: форматы для полигона и метрики проверки', '8-10: насмотренность, окружение, правило минимума на спад'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Правила сильного плана', pts:['Каждый пункт измерим: есть число или чёткое действие', 'Сроки конкретные — дата, а не «скоро»', 'План на виду: перечитывай в начале каждой недели', 'Живой документ: корректируй по цифрам, но не бросай'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M30 54h60" opacity=".6"/><path d="M40 46l30-30 8 8-30 30-12 4z"/><path d="M64 22l8 8"/></svg>'},
  {t:'Первый шаг — сегодня', pts:['План без первого действия сегодня — просто текст', 'Выбери один пункт и начни его в ближайшие 24 часа', 'Сними первый ролик-полигон, не откладывая на понедельник', 'Импульс от старта тянет весь план за собой'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Сверка и результат', pts:['Раз в неделю сверяй факт с планом: где в графике, где отстал', 'Отмечай выполненное — видимый прогресс держит темп', 'Не вышло по пункту — правь способ, а не отменяй цель', 'Через месяц по плану у тебя первые цифры и первые деньги'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 12h40v14a20 20 0 0 1-40 0z"/><path d="M40 18H26c0 12 8 16 16 16M80 18h14c0 12-8 16-16 16M60 46v10M46 62h28M52 56h16"/></svg>'}
  ],
  quiz:[
  {q:'Зачем нужен план на 10 пунктов?', o:['Для отчёта куратору', 'Превратить знания курса в конкретные измеримые шаги', 'Для красоты', 'Он необязателен'], a:1},
  {q:'Что обязательно входит в план?', o:['Только мечты', 'Цель в цифрах, срок, действия и образ жизни при цели', 'Список чужих блогов', 'Настроение на неделю'], a:1},
  {q:'Каким должен быть каждый пункт плана?', o:['Размытым', 'Измеримым: число или чёткое действие', 'Про далёкое будущее', 'Без срока'], a:1},
  {q:'Когда сделать первый шаг по плану?', o:['С понедельника', 'В ближайшие 24 часа', 'Через месяц', 'Когда появится настроение'], a:1},
  {q:'Что делать, если пункт не выполнен?', o:['Отменить цель', 'Править способ, но держать цель', 'Бросить план', 'Ждать'], a:1}
  ],
  pairs:[
  ['План на 10 пунктов', 'Контракт с собой в цифрах'],
  ['Пункты 1-3', 'Цель, срок, образ жизни'],
  ['Пункты 4-5', 'График и норма роликов'],
  ['Правило сильного плана', 'Каждый пункт измерим'],
  ['Первый шаг', 'Действие в ближайшие 24 часа']
  ],
  task:{
    intro:'Напиши свой финальный план из 10 пунктов: тип участника, цель в цифрах и сроке, целевые цифры, образ жизни при цели, график, форматы, метрики, насмотренность, окружение и первый шаг на сегодня.',
    chips:['Тип и главная цель', 'Число + срок', 'Целевые цифры', 'Образ жизни при цели', 'График и норма роликов', 'Первый шаг сегодня'],
    ph:'1. Тип: новичок, цель — первые 30 000 руб с блога. 2. Срок: к 20 октября. 3. Цифры: 20 reels, охваты 1500+, 5 заявок. 4. Жизнь: работаю на себя из дома. 5. График: съёмка вт/чт, 5 роликов в неделю. 6. Форматы: история клиента + экспертная нарезка. 7. Метрика: удержание и сохранения. 8. Насмотренность: 15 мин разбора в день. 9. Окружение: вступил в 2 чата. 10. Первый шаг сегодня: снять и выложить ролик-знакомство.',
    verdict:'Это готовый рабочий план — у тебя есть цифры, срок, график и, главное, первый шаг на сегодня, а не на понедельник. Усилю: в конце каждой недели ставь напротив каждого пункта «в графике / отстаю» и правь способ, — так план останется живым инструментом, а не разовым текстом.'
  }
}
  ],
  "m2": [
{
  title:'Шапка: 3 секунды',
  sub:'3:12 · видео (голос Даниэля) + тест + игра', dur:'3:12',
  videoUrl:'https://okoteam.top/media/oko_m2_0.mp4',
  c1:'3 СЕКУНДЫ', c2:'НА РЕШЕНИЕ',
  slides:[
  {t:'Первое касание решает всё', pts:['Мозг оценивает профиль за <b>3 секунды</b> — быстрее, чем ты дочитаешь эту строку', 'Решение «остаться или уйти» принимается до того, как человек долистает до постов', 'Шапка — это не «инфа обо мне», а <b>оффер за 3 секунды</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v16l11 7"/></svg>'},
  {t:'Куда смотрит взгляд', pts:['Сначала <b>аватар</b>, потом <b>имя-строка</b>, потом первая строка описания', 'Взгляд идёт сверху вниз, хвосты текста под кнопкой «ещё» не читают', 'Витрина <b>вечных историй</b> считывается последней, но добивает решение'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Цель шапки = тип блога', pts:['<b>Коммерческий</b> блог: задача — продать, шапка ведёт к покупке', '<b>Экспертный</b> блог: задача — доверие, шапка доказывает компетентность', '<b>Лайфстайл</b>: задача — влюбить в личность', 'Одна шапка не решает три задачи сразу — выбери одну'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Пять деталей первого экрана', pts:['<b>Ник</b>, <b>имя-строка</b>, <b>аватар</b>, <b>описание</b>, <b>мультиссылка</b>', 'Каждый элемент — микро-инструмент, а не украшение', 'Пустая или шаблонная деталь = слитые 3 секунды'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Фильтр «а мне зачем»', pts:['К каждой строке задай вопрос читателя: <b>«а мне зачем это?»</b>', 'Не отвечает на вопрос — вырезай без жалости', 'Хорошая шапка — это выгода читателя, а не резюме автора'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'}
  ],
  quiz:[
  {q:'Сколько времени у шапки, чтобы удержать человека?', o:['3 секунды', '30 секунд', '3 минуты', 'Зависит от постов'], a:0},
  {q:'От чего зависит цель шапки?', o:['От числа подписчиков', 'От типа блога', 'От времени суток', 'От длины описания'], a:1},
  {q:'Что взгляд считывает первым?', o:['Описание', 'Мультиссылку', 'Аватар', 'Посты'], a:2},
  {q:'Главный фильтр для каждой строки шапки?', o:['«Красиво ли»', '«А мне зачем»', '«Модно ли»', '«Длинно ли»'], a:1},
  {q:'Что НЕ работает на первое впечатление?', o:['Аватар', 'Имя-строка', 'Текст под кнопкой «ещё»', 'Первая строка описания'], a:2}
  ],
  pairs:[
  ['Коммерческий блог', 'Задача — продать'],
  ['Экспертный блог', 'Задача — доверие'],
  ['Лайфстайл', 'Влюбить в личность'],
  ['Шапка', 'Оффер за 3 секунды'],
  ['«А мне зачем»', 'Фильтр каждой строки']
  ],
  task:{
    intro:'Открой свой профиль, засеки 3 секунды и запиши, что успел понять чужой человек: чем ты полезен и кому.',
    chips:['Тип блога: экспертный/коммерческий/лайфстайл', 'Цель шапки одной фразой', 'Что видно за 3 секунды', 'Чего не хватает', 'Ответ на «а мне зачем»'],
    ph:'Тип: экспертный. Цель — доверие. За 3 сек видно: маркетолог, но непонятно для кого и какой результат. Не хватает цифр и выгоды в описании.',
    verdict:'Сильно, что ты честно назвал провал — «непонятно для кого». Это и есть корень. Добавь в имя-строку нишу клиента (например «маркетолог для стоматологий»), и первое впечатление сразу перестанет быть размытым.'
  }
},
{
  title:'Ник, который запоминают',
  sub:'2:48 · видео (голос Даниэля) + тест + игра', dur:'2:48',
  videoUrl:'https://okoteam.top/media/oko_m2_1.mp4',
  c1:'НИК', c2:'БЕЗ МУСОРА',
  slides:[
  {t:'Ник — это адрес, а не украшение', pts:['Ник диктуют голосом и вбивают руками — он должен <b>писаться с одного раза</b>', 'Мусор из точек, подчёркиваний и цифр (_x_, 2007) убивает запоминаемость', 'Плохой ник = потерянные переходы «по сарафану»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 35 46 12h30a8 8 0 0 1 8 8v30L56 78z" transform="scale(.85) translate(6 -4)"/><circle cx="66" cy="27" r="5"/></svg>'},
  {t:'Две рабочие модели ника', pts:['<b>Имя + фамилия</b> для личного бренда: anna.petrova', '<b>Имя + тематика</b>: petrova.marketing, dr.smirnov', 'Чистая тематика без имени — для брендов, а не личных блогов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="30" cy="24" r="12"/><path d="M18 24a12 12 0 0 1 24 0"/><path d="M52 16h54M52 27h40M52 38h50" opacity=".85"/></svg>'},
  {t:'Правила чистого ника', pts:['Только то, что <b>легко продиктовать</b> по телефону', 'Без двойных точек, случайных цифр и года рождения', 'Латиница — читаемая с первого раза, без ловушек транслита (ж, щ, ы)'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Проверка на запоминаемость', pts:['Скажи ник вслух другу — <b>напишет с первого раза?</b>', 'Вбей в поиск — не путают ли с чужим похожим', 'Ник живёт годами: смена = потеря наработанной узнаваемости'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Ник под цель блога', pts:['Эксперт: <b>имя + сфера</b>, чтобы находили по нише', 'Продукт: название бренда, короткое и звучное', 'Не привязывай ник к тренду, который завтра умрёт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Каким должен быть ник?', o:['Длинным и креативным', 'Пишется с первого раза', 'С годом рождения', 'Обязательно на английском сленге'], a:1},
  {q:'Что убивает запоминаемость ника?', o:['Имя владельца', 'Название ниши', 'Точки, цифры и подчёркивания', 'Короткая длина'], a:2},
  {q:'Модель ника для личного бренда?', o:['Случайный набор букв', 'Имя + фамилия или имя + ниша', 'Название чужого бренда', 'Только эмодзи'], a:1},
  {q:'Как проверить ник на запоминаемость?', o:['Спросить у нейросети', 'Продиктовать вслух другу', 'Посчитать буквы', 'Сравнить с конкурентом'], a:1},
  {q:'Почему нельзя часто менять ник?', o:['Это платно', 'Теряется наработанная узнаваемость', 'Запрещено платформой', 'Портит аватар'], a:1}
  ],
  pairs:[
  ['Ник', 'Адрес блога'],
  ['Имя + ниша', 'Модель для эксперта'],
  ['Год рождения в нике', 'Мусор'],
  ['Смена ника', 'Потеря узнаваемости'],
  ['Диктовка вслух', 'Тест на запоминаемость']
  ],
  task:{
    intro:'Составь 3 варианта чистого ника для своего блога по моделям «имя+фамилия» и «имя+ниша». Прогони каждый через тест «продиктуй другу».',
    chips:['Твоё имя латиницей', 'Твоя ниша одним словом', 'Вариант имя+фамилия', 'Вариант имя+ниша', 'Есть ли мусор (точки/цифры)'],
    ph:'Ниша: копирайтинг. Варианты: ivanov.copy, anna_copywriter (плохо — подчёркивание), textmaster.ivanov. Продиктовал — друг написал ivanov.copy с первого раза.',
    verdict:'Хорошо, что сразу отсёк вариант с подчёркиванием — это чутьё. ivanov.copy — рабочий: короткий, диктуется, читается. Проверь только, не занят ли он и нет ли похожего конкурента, чтобы тебя не путали.'
  }
},
{
  title:'Аватар — лицо блога',
  sub:'3:04 · видео (голос Даниэля) + тест + игра', dur:'3:04',
  videoUrl:'https://okoteam.top/media/oko_m2_2.mp4',
  c1:'АВАТАР', c2:'ОТКРЫТЫЙ ВЗГЛЯД',
  slides:[
  {t:'Аватар размером с ноготь', pts:['В ленте и поиске аватар — <b>кружок в пару миллиметров</b>', 'Мелкие детали, логотипы и текст на нём не читаются', 'Работает только <b>крупный план лица</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="30" cy="24" r="12"/><path d="M18 24a12 12 0 0 1 24 0"/><path d="M52 16h54M52 27h40M52 38h50" opacity=".85"/></svg>'},
  {t:'Формула сильного аватара', pts:['<b>Крупный план</b>: лицо занимает большую часть кадра', '<b>Однотонный фон</b> — контраст с лицом, никакой каши', '<b>Открытый взгляд</b> в камеру плюс лёгкая эмоция'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="18" width="54" height="38" rx="7"/><path d="M74 30l24-12v38L74 42z"/><circle cx="40" cy="37" r="9"/></svg>'},
  {t:'Свет и качество', pts:['Дневной мягкий свет или софтбокс, без жёлтых теней', 'Резкое фото без шума — портретный режим телефона годится', 'Одежда и фон — в тон бренду, а не «что было под рукой»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="24" y="14" width="72" height="46" rx="7"/><circle cx="43" cy="30" r="6"/><path d="M28 54l20-18 14 12 12-10 22 16" opacity=".85"/></svg>'},
  {t:'Аватар под тип блога', pts:['Эксперт и личный бренд — <b>твоё лицо</b>, всегда', 'Коммерческий бренд — логотип, но крупный и читаемый в кружке', 'Лайфстайл — живая эмоция важнее «идеального» кадра'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="30" cy="24" r="12"/><path d="M18 24a12 12 0 0 1 24 0"/><path d="M52 16h54M52 27h40M52 38h50" opacity=".85"/></svg>'},
  {t:'Стабильность узнавания', pts:['Меняй аватар <b>не чаще 1–2 раз в год</b>', 'Частая смена = аудитория заново «знакомится» и путается', 'Тот же кадр ставь на все площадки — единое лицо'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'}
  ],
  quiz:[
  {q:'Почему аватару нужен крупный план?', o:['Так модно', 'Он маленький, детали не видно', 'Экономит трафик', 'Требование платформы'], a:1},
  {q:'Какой фон работает лучше всего?', o:['Пёстрый и детальный', 'Однотонный контрастный', 'С текстом', 'С коллажем из работ'], a:1},
  {q:'Как часто стоит менять аватар?', o:['Каждую неделю', 'Каждый пост', '1–2 раза в год', 'Никогда'], a:2},
  {q:'Что важно во взгляде на аватаре?', o:['Отведён в сторону', 'Открытый, в камеру', 'Закрыт очками', 'Не виден'], a:1},
  {q:'Что ставить аватаром коммерческому бренду?', o:['Пейзаж', 'Крупный читаемый логотип', 'Мелкий текст', 'Фото товара издалека'], a:1}
  ],
  pairs:[
  ['Однотонный фон', 'Контраст с лицом'],
  ['Крупный план', 'Лицо читается в кружке'],
  ['Смена 1–2 раза/год', 'Стабильное узнавание'],
  ['Открытый взгляд', 'Контакт с аудиторией'],
  ['Логотип', 'Аватар бренда']
  ],
  task:{
    intro:'Опиши бриф на свой новый аватар: план, фон, свет, эмоция. Оцени текущий по формуле и назови, что заменить.',
    chips:['Текущий аватар: план и фон', 'Какая эмоция во взгляде', 'Фон однотонный или каша', 'Что заменить', 'Единый ли кадр на всех площадках'],
    ph:'Сейчас: фото по пояс на фоне полки с книгами, лицо мелкое, взгляд в сторону. Бриф на новый: крупный план по плечи, серый однотонный фон, дневной свет у окна, лёгкая улыбка, взгляд в камеру.',
    verdict:'Точный разбор — ты поймал две главные ошибки: мелкое лицо и пёстрый фон. Бриф грамотный. Добавь только: сними сразу 5–7 дублей и выбери кадр, где взгляд самый живой — это решает больше, чем идеальный свет.'
  }
},
{
  title:'Имя-строка и ключевые слова',
  sub:'3:35 · видео (голос Даниэля) + тест + игра', dur:'3:35',
  videoUrl:'https://okoteam.top/media/oko_m2_3.mp4',
  c1:'ИМЯ-СТРОКА', c2:'ПО НЕЙ ИЩУТ',
  slides:[
  {t:'Жирная строка, по которой ищут', pts:['Имя-строка — <b>единственное поле, что индексирует поиск</b> платформы', 'До <b>30 знаков</b>, показывается жирным под аватаром', 'Ник ищут те, кто знает; имя-строку — те, кто ищет тему'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Не дублируй ник', pts:['Повтор ника в имени-строке — это слитые 30 знаков', 'Ставь <b>имя + ключевой запрос</b>: «Анна · таргетолог»', 'Ключ — то, что твой клиент реально вбивает в поиск'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="35" r="14"/><path d="M48 35h44M78 35v12M92 35v10"/></svg>'},
  {t:'Как найти ключевые слова', pts:['Подумай, каким словом тебя ищет клиент: «психолог», «маникюр»', 'Проверь частотность запроса в <b>Wordstat</b> — бери то, что реально ищут', 'Гео-привязка («…Казань») ловит локальный трафик'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h80L68 44v18l-16 8V44z"/></svg>'},
  {t:'Формула имени-строки', pts:['<b>Имя + ниша</b>: «Игорь · SMM для юристов»', '<b>Ниша + гео</b>: «Ногтевой сервис · Пермь»', 'Разделители-точки и слэши делают строку читаемой'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Чего нельзя в имени-строке', pts:['Не забивай её эмодзи и «звёздочками» — крадут символы у ключей', 'Не пиши абстракции «создаю красоту» — по ним не ищут', 'Каждый из 30 знаков должен работать на <b>находимость</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Чем уникальна имя-строка?', o:['Она самая длинная', 'Её индексирует поиск', 'Она кликабельна', 'Она видна только тебе'], a:1},
  {q:'Каков лимит имени-строки?', o:['15 знаков', '30 знаков', '150 знаков', 'Без лимита'], a:1},
  {q:'Где проверить частотность запроса?', o:['В календаре', 'В Wordstat', 'В галерее', 'В настройках профиля'], a:1},
  {q:'Что НЕ надо писать в имени-строке?', o:['Ключевое слово', 'Дубль ника', 'Гео', 'Нишу'], a:1},
  {q:'Что ловит гео-привязка в строке?', o:['Ботов', 'Локальный трафик', 'Рекламу', 'Подписчиков конкурента'], a:1}
  ],
  pairs:[
  ['Имя-строка', 'Индексируется поиском'],
  ['30 знаков', 'Лимит строки'],
  ['Wordstat', 'Частотность запросов'],
  ['Ключевое слово', 'Запрос клиента'],
  ['Гео в строке', 'Локальный трафик']
  ],
  task:{
    intro:'Собери свою имя-строку до 30 знаков по формуле «имя + ниша» или «ниша + гео». Ключевое слово сверь с реальным запросом клиента.',
    chips:['Твоё имя', 'Ниша одним словом', 'Нужно ли гео', 'Ключ, который вбивает клиент', 'Уложился ли в 30 знаков'],
    ph:'Клиент ищет «детский психолог». Строка: «Ольга · детский психолог» — 24 знака. Гео не ставлю, работаю онлайн по всей стране.',
    verdict:'Отлично — ты взял ключ из головы клиента, а не из своей («психолог», а не «помогаю семьям»). Уложился в лимит. Один шаг вперёд: проверь запрос в Wordstat — возможно, «детский психолог онлайн» ищут чаще, и это влезет.'
  }
},
{
  title:'Описание по формуле',
  sub:'3:50 · видео (голос Даниэля) + тест + игра', dur:'3:50',
  videoUrl:'https://okoteam.top/media/oko_m2_4.mp4',
  c1:'ОПИСАНИЕ', c2:'ПО ФОРМУЛЕ',
  slides:[
  {t:'150 знаков, которые продают', pts:['Описание — до <b>150 знаков</b>, это лифт-презентация блога', 'Не «дневник», а структура: кто ты и что человек получит', 'Каждая строка отвечает на <b>«а мне зачем подписываться»</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Формула для эксперта', pts:['<b>Кто ты</b> → <b>чем интересен</b> → <b>факты-цифры</b> → <b>выгода</b> → <b>CTA</b>', 'Цифры дают доверие: «7 лет / 300 клиентов / −20 кг»', 'CTA — конкретное действие: «Забери гайд ниже»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Формула для коммерции', pts:['<b>Продукт</b> → <b>выгода</b> → <b>уникальность</b> → <b>CTA</b>', 'Уникальность — чем ты не как все: «доставка за 2 часа»', 'Заканчивай призывом к первому шагу, а не точкой'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 35 46 12h30a8 8 0 0 1 8 8v30L56 78z" transform="scale(.85) translate(6 -4)"/><circle cx="66" cy="27" r="5"/></svg>'},
  {t:'Цифры вместо прилагательных', pts:['«Качественно и с любовью» ничего не значит и не запоминается', 'Замени на факт: «собрал 40 воронок, средний +35% заявок»', 'Конкретика = доверие за секунду'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V14M18 58h84"/><rect x="30" y="40" width="12" height="18"/><rect x="52" y="30" width="12" height="28"/><rect x="74" y="20" width="12" height="38"/></svg>'},
  {t:'Черновик и чистка', pts:['Набросай смысл, затем сожми в 150 знаков через <b>ChatGPT</b> или <b>Claude</b>', 'Убери воду, оставь глаголы и цифры', 'Проверь: помещается ли и читается ли с телефона'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M30 54h60" opacity=".6"/><path d="M40 46l30-30 8 8-30 30-12 4z"/><path d="M64 22l8 8"/></svg>'}
  ],
  quiz:[
  {q:'Каков лимит описания профиля?', o:['30 знаков', '150 знаков', '500 знаков', 'Без лимита'], a:1},
  {q:'С чего начинается экспертная формула?', o:['С CTA', 'С кто ты', 'С цены', 'С хэштегов'], a:1},
  {q:'Что даёт доверие в описании?', o:['Прилагательные', 'Факты и цифры', 'Эмодзи', 'Длина текста'], a:1},
  {q:'Чем заканчивать описание?', o:['Точкой', 'Хэштегом', 'CTA — призывом к действию', 'Смайлом'], a:2},
  {q:'Что такое уникальность в коммерческой формуле?', o:['Название продукта', 'Чем ты не как все', 'Список услуг', 'Цена'], a:1}
  ],
  pairs:[
  ['150 знаков', 'Лимит описания'],
  ['Экспертная формула', 'Кто/интересен/цифры/выгода/CTA'],
  ['Коммерческая формула', 'Продукт/выгода/уникальность/CTA'],
  ['Цифры', 'Доверие'],
  ['CTA', 'Призыв к действию']
  ],
  task:{
    intro:'Напиши описание своего блога до 150 знаков строго по формуле (экспертной или коммерческой). Вставь хотя бы одну цифру и CTA.',
    chips:['Формула: экспертная/коммерческая', 'Кто ты / что за продукт', 'Факт или цифра', 'В чём выгода читателю', 'CTA — что нажать'],
    ph:'Нутрициолог · помогаю убрать вес без диет · 400+ клиентов, средний −8 кг за 2 мес · получи разбор питания → ссылка ниже.',
    verdict:'Крепко: есть цифра (400+ клиентов), измеримый результат (−8 кг) и понятный CTA. Всё по формуле. Усилю одним: добавь «за 2 мес» прямо к выгоде читателя, а не только к кейсу — так человек считает срок своего результата, а не чужого.'
  }
},
{
  title:'Мультиссылка и трафик',
  sub:'3:28 · видео (голос Даниэля) + тест + игра', dur:'3:28',
  videoUrl:'https://okoteam.top/media/oko_m2_5.mp4',
  c1:'МУЛЬТИ-', c2:'ССЫЛКА',
  slides:[
  {t:'Одна ссылка — узкое горлышко', pts:['Профиль даёт <b>одну кликабельную ссылку</b> — это горлышко трафика', 'Голая ссылка на сайт теряет тех, кому нужен другой раздел', 'Решение — <b>мультиссылка</b>: меню вместо одной двери'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M48 42 72 18a13 13 0 0 1 18 18L74 52M72 28 48 52a13 13 0 0 1-18-18l16-16" opacity=".9"/></svg>'},
  {t:'Из чего собрать меню', pts:['<b>О себе</b>, <b>Услуги и прайс</b>, <b>Отзывы и кейсы</b>', '<b>Примеры работ</b>, <b>Как заказать</b>, <b>Акции</b>', 'Каждый пункт — под свой сегмент аудитории'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Порядок кнопок = путь клиента', pts:['Сверху — то, что ведёт к деньгам: услуги, заказ', 'Прогрев — отзывы и кейсы для сомневающихся', 'Не вали 15 кнопок: <b>4–6 сильных</b> лучше свалки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'},
  {t:'Кликабельность и мессенджеры', pts:['Контакты должны <b>открываться в один тап</b>, а не копироваться руками', 'Ссылка на прямой чат или бота ловит тёплого клиента сразу', 'Проверь каждую кнопку с телефона — мёртвая ссылка = слитый лид'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="42" y="8" width="36" height="54" rx="7"/><path d="M52 16h16M56 54h8" opacity=".7"/></svg>'},
  {t:'Трафик под контентом', pts:['В постах и историях зови: «ссылка в шапке, раздел Услуги»', 'Раздели трафик по целям: холодным — кейсы, тёплым — заказ', 'Мультиссылка без призывов в контенте — просто висит'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30v12l14 3 30 14V13L34 27z"/><path d="M64 22c10 3 10 23 0 26" opacity=".7"/><path d="M40 48l4 12" /></svg>'}
  ],
  quiz:[
  {q:'Сколько кликабельных ссылок даёт профиль?', o:['Одна', 'Три', 'Десять', 'Сколько угодно'], a:0},
  {q:'Что решает проблему одной ссылки?', o:['Хэштеги', 'Мультиссылка', 'Второй аккаунт', 'Реклама'], a:1},
  {q:'Что ставить в топ кнопок мультиссылки?', o:['О себе', 'То, что ведёт к деньгам', 'Акции 2019 года', 'Ссылку на конкурента'], a:1},
  {q:'Сколько кнопок оптимально?', o:['1', '4–6 сильных', '15+', 'Чем больше, тем лучше'], a:1},
  {q:'Как должны работать контакты?', o:['Копироваться руками', 'Открываться в один тап', 'Быть картинкой', 'Прятаться в постах'], a:1}
  ],
  pairs:[
  ['Мультиссылка', 'Меню вместо одной двери'],
  ['Отзывы-кейсы', 'Прогрев сомневающихся'],
  ['Как заказать', 'Кнопка к деньгам'],
  ['4–6 кнопок', 'Оптимум меню'],
  ['Призыв в постах', 'Запуск трафика в шапку']
  ],
  task:{
    intro:'Спроектируй структуру своей мультиссылки: 4–6 кнопок в порядке пути клиента. Первой — та, что ближе к деньгам.',
    chips:['Ниша и что продаёшь', 'Кнопка №1 (к заказу)', 'Кнопки-прогрев (отзывы/кейсы)', 'Контакт в один тап', 'Что убрать из лишнего'],
    ph:'Ниша: фотограф. 1) Записаться на съёмку → чат. 2) Прайс. 3) Примеры работ. 4) Отзывы. Убрал старую акцию и ссылку на личку без описания.',
    verdict:'Логика воронки выстроена верно: заказ сверху, доказательства ниже. Молодец, что срезал мёртвую акцию. Совет: кнопку «Примеры работ» подними выше отзывов — у фотографа портфолио закрывает возражение быстрее, чем текст отзыва.'
  }
},
{
  title:'Вечные истории — витрина',
  sub:'2:56 · видео (голос Даниэля) + тест + игра', dur:'2:56',
  videoUrl:'https://okoteam.top/media/oko_m2_6.mp4',
  c1:'ВЕЧНЫЕ', c2:'ИСТОРИИ',
  slides:[
  {t:'Витрина, которая работает без тебя', pts:['<b>Вечные истории</b> — закреплённые сторис под шапкой, живут постоянно', 'Это витрина магазина: человек листает и принимает решение', 'Работают <b>24/7</b> и отвечают на вопросы за тебя, пока ты спишь'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Обязательные разделы витрины', pts:['<b>О себе</b>, <b>Отзывы</b>, <b>Кейсы и результаты</b>', '<b>Прайс и услуги</b>, <b>FAQ</b>, <b>Как купить</b>', 'Каждый раздел закрывает одно возражение клиента'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Обложки в одном стиле', pts:['Единый стиль обложек = аккуратная, дорогая витрина', 'Один шрифт, цвета бренда, понятные подписи-иконки', 'Разнобой обложек = ощущение бардака и недоверие'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M74 12 92 30 56 66l-18 4 4-18z"/><path d="M62 24 80 42" opacity=".7"/></svg>'},
  {t:'Порядок = логика прогрева', pts:['Слева — самое важное: <b>О себе</b> и <b>Отзывы</b>', 'Дальше по воронке: кейсы → прайс → как купить', 'Мёртвые истории из 2019-го убери, чтобы не старить блог'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'},
  {t:'Что кладём внутрь', pts:['В отзывы — <b>скрины и видео живых клиентов</b>, не текст от себя', 'В кейсы — «было / стало» с цифрами', 'Обновляй раз в 1–2 месяца, снимай устаревшее'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Что такое вечные истории?', o:['Обычные сторис на 24 часа', 'Закреплённая сторис-витрина', 'Раздел настроек', 'Лента постов'], a:1},
  {q:'Зачем обложки в одном стиле?', o:['Для алгоритма', 'Для аккуратности и доверия', 'Чтобы было ярче', 'Это обязательно платформой'], a:1},
  {q:'Что ставить первым (слева)?', o:['Старые акции', 'О себе и отзывы', 'Мемы', 'Рекламу'], a:1},
  {q:'Что класть в раздел отзывов?', o:['Свой текст о себе', 'Скрины и видео живых клиентов', 'Цены', 'Фото товара'], a:1},
  {q:'Как показывать кейсы?', o:['Просто фото', '«Было / стало» с цифрами', 'Одним словом', 'Ссылкой на сайт'], a:1}
  ],
  pairs:[
  ['Вечные истории', 'Витрина 24/7'],
  ['Единый стиль обложек', 'Доверие'],
  ['Отзывы', 'Скрины живых клиентов'],
  ['Кейсы', 'Было / стало с цифрами'],
  ['Порядок разделов', 'Логика прогрева']
  ],
  task:{
    intro:'Спланируй набор вечных историй: какие разделы нужны твоему блогу и в каком порядке. Отметь, что положишь внутрь каждого.',
    chips:['Ниша', 'Список разделов', 'Порядок слева направо', 'Что внутри отзывов', 'Стиль обложек (шрифт/цвет)'],
    ph:'Ниша: мастер бровей. Разделы: О себе → Отзывы (скрины переписок) → До/После (фото) → Прайс → Как записаться. Обложки: бежевый фон, один шрифт, тонкие иконки.',
    verdict:'Собрано по уму: раздел «До/После» — самое сильное доказательство для бьюти, и он стоит рано. Обложки в едином стиле — плюс к доверию. Усилю: в «Отзывы» добавь пару видео-кружков — живой голос клиента убеждает сильнее скрина.'
  }
},
{
  title:'Частые ошибки шапки',
  sub:'3:40 · видео (голос Даниэля) + тест + игра', dur:'3:40',
  videoUrl:'https://okoteam.top/media/oko_m2_7.mp4',
  c1:'ОШИБКИ', c2:'ШАПКИ',
  slides:[
  {t:'Шаблон вместо смысла', pts:['«Помогаю женщинам быть счастливыми» — <b>шаблон</b>, под который подходит любой', 'Аудитория видела это тысячу раз, взгляд проскальзывает', 'Лечится конкретикой: кому, что и с каким результатом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Английский для русской аудитории', pts:['<b>Английские слова</b> и заумные термины в русском блоге = барьер', 'Часть аудитории просто не понимает и уходит', 'Пиши на языке клиента, а не «чтобы звучало солидно»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Некликабельные контакты', pts:['Номер или @ник текстом, который надо <b>копировать руками</b>', 'Каждый лишний шаг теряет часть тёплых клиентов', 'Всё, что можно нажать, должно быть <b>ссылкой в один тап</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="42" y="8" width="36" height="54" rx="7"/><path d="M52 16h16M56 54h8" opacity=".7"/></svg>'},
  {t:'Строки без ответа «а мне зачем»', pts:['Каждая строка про тебя, а не про выгоду читателя', 'Фильтр простой: не отвечает <b>«а мне зачем»</b> — вырезай', 'Эмодзи-свалка и дубль ника — тоже слитые символы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Шапку не проверяют с телефона', pts:['Верстают на компьютере, а <b>95% смотрят с телефона</b>', 'Итог: обрезанные строки, кривые переносы, мёртвые ссылки', 'Проверяй глазами чужого человека раз в пару месяцев'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'}
  ],
  quiz:[
  {q:'Чем плох шаблон в описании?', o:['Он короткий', 'Под него подходит любой, не цепляет', 'Он длинный', 'Он платный'], a:1},
  {q:'Почему опасен английский в русском блоге?', o:['Запрещён', 'Часть аудитории не понимает и уходит', 'Портит алгоритм', 'Занимает мало места'], a:1},
  {q:'Какими должны быть контакты?', o:['Текстом для копирования', 'Кликабельными в один тап', 'Картинкой', 'Спрятанными'], a:1},
  {q:'Главный фильтр для строки шапки?', o:['«Красиво ли»', '«А мне зачем»', '«Длинно ли»', '«Модно ли»'], a:1},
  {q:'С какого устройства проверять шапку?', o:['Только с компьютера', 'С телефона', 'С планшета', 'Неважно'], a:1}
  ],
  pairs:[
  ['Шаблон', 'Подходит под любого'],
  ['Английский текст', 'Барьер для аудитории'],
  ['Контакт текстом', 'Некликабельно, теряешь лидов'],
  ['«А мне зачем»', 'Фильтр каждой строки'],
  ['Проверка с телефона', '95% смотрят с него']
  ],
  task:{
    intro:'Проведи аудит своей шапки: найди минимум 3 ошибки из урока и напиши, как исправишь каждую.',
    chips:['Есть ли шаблонные фразы', 'Английский без нужды', 'Кликабельны ли контакты', 'Строки без выгоды', 'Проверял ли с телефона'],
    ph:'Ошибки: 1) «создаю атмосферу уюта» — шаблон, заменю на «декор для кафе, 60 проектов». 2) @номер текстом — сделаю кнопку в мультиссылке. 3) Строка «люблю кофе» — вырезаю, не про клиента.',
    verdict:'Сильный аудит — ты не просто нашёл ошибки, а сразу дал замену с цифрой (60 проектов). Это уровень выше среднего. Добавь последний шаг: открой обновлённую шапку с телефона и проверь, что ни одна строка не обрезается на «ещё».'
  }
}
  ],
  "m3": [
{
  title:'Зачем визуал (51% судят по картинке)',
  sub:'3:12 · видео (голос Даниэля) + тест + игра', dur:'3:12',
  videoUrl:'https://okoteam.top/media/oko_m3_0.mp4',
  c1:'ПЕРВЫЙ', c2:'ВЗГЛЯД',
  slides:[
  {t:'3 секунды до отписки', pts:['Человек заходит в профиль и <b>за 3 секунды</b> решает: остаться или уйти', 'Он не читает тексты — он <b>сканирует картинку</b>: аватар, шапку, первый ряд сетки', '<b>51% людей</b> судят о блоге только по визуалу, ещё до единого слова', 'Плохой визуал = закрытая дверь: контент внутри уже никто не увидит'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Что на самом деле продаёт визуал', pts:['<b>Внимание</b> — останавливает палец в бесконечной ленте', '<b>Ценности</b> — за 1 секунду показывает, о чём ты и для кого', '<b>Отстройка</b> — почему ты, а не 100 таких же аккаунтов в нише', '<b>Доверие</b> — аккуратная лента считывается как «человек всё делает качественно»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Мозг решает раньше сознания', pts:['Оценка «нравится / не нравится» происходит на <b>эмоциях</b>, не на логике', 'Красивая картинка = дофамин = «хочу ещё» → подписка на автомате', 'Некрасивая = микро-раздражение → палец листает дальше, даже без причины', 'Ты не убеждаешь текстом того, кто ушёл на <b>обложке</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Визуал ≠ дорого и сложно', pts:['Не нужен фотограф за 30 000 — нужна <b>система</b>: гамма, планы, воздух', 'Снимает <b>iPhone 11–12+</b>, обрабатывает бесплатный редактор в телефоне', 'Референсы и раскадровку сетки можно собрать в <b>Midjourney</b> и <b>Flux</b>', 'Решает не техника, а насмотренность и повторяемый стиль'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="42" y="8" width="36" height="54" rx="7"/><path d="M52 16h16M56 54h8" opacity=".7"/></svg>'},
  {t:'Цифра, которую нельзя игнорировать', pts:['Два эксперта одного уровня: у кого лента чище — у того <b>подписка дороже</b>', 'Визуал поднимает <b>цену за экспертность</b> без единого слова о цене', 'Бренды платят за размещение аккуратным блогам охотнее — риск ниже', 'Вложение в визуал окупается <b>ростом доверия</b>, а доверие = деньги'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Что делать уже сегодня', pts:['Открой свой профиль <b>глазами чужого человека</b>: остался бы ты сам?', 'Проверь первый экран: аватар читается? шапка понятна за 2 секунды?', 'Первый ряд из 3 фото — это твоя <b>витрина</b>, не свалка случайного', 'Реши одно: с этого дня каждый пост проходит фильтр «а красиво ли»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Какая доля людей судит о блоге по визуалу?', o:['21%', '51%', '72%', '96%'], a:1},
  {q:'За сколько времени человек решает остаться в профиле или уйти?', o:['Около 3 секунд', '30 секунд', '2 минуты', 'После первого поста'], a:0},
  {q:'Что визуал даёт в первую очередь?', o:['Внимание', 'Продажу курса', 'Комментарии', 'Репосты'], a:0},
  {q:'Почему сильный текст не спасает слабую обложку?', o:['Тексты вообще не читают', 'Ушедший на картинке до текста не доходит', 'Алгоритм режет длинные тексты', 'Обложка важнее смысла всегда'], a:1},
  {q:'Что НЕ нужно для сильного визуала?', o:['Единая гамма', 'Дорогая камера', 'Разные планы', 'Воздух в кадре'], a:1}
  ],
  pairs:[
  ['Внимание', 'Останавливает палец в ленте'],
  ['Отстройка', 'Показывает, почему ты, а не сотня похожих'],
  ['Доверие', 'Аккуратность читается как качество во всём'],
  ['51%', 'Доля судящих о блоге по картинке'],
  ['3 секунды', 'Время на решение остаться или уйти']
  ],
  task:{
    intro:'Открой свой профиль как незнакомец и честно опиши первое впечатление за 3 секунды: что цепляет, что отталкивает, остался бы ты подписан.',
    chips:['Аватар читается', 'Шапка понятна за 2 сек', 'Первый ряд сетки', 'Единый стиль есть', 'Что оттолкнуло', 'Остался бы / нет'],
    ph:'Первое впечатление: аватар тёмный, лицо не разобрать. Шапка — сплошной текст, суть непонятна. Первый ряд: три разных по цвету фото, глаз мечется. Оттолкнула пестрота. Честно — не подписался бы, выглядит как хаос.',
    verdict:'Отличный разбор — ты назвал конкретику, а не «вроде норм»: тёмный аватар и пестрота первого ряда это реальные точки утечки подписчиков. Усилю: сфотографируй аватар при дневном свете у окна и переверстай шапку в 3 строки — кто ты, для кого, что получит подписчик.'
  }
},
{
  title:'Баннерная слепота',
  sub:'3:28 · видео (голос Даниэля) + тест + игра', dur:'3:28',
  videoUrl:'https://okoteam.top/media/oko_m3_1.mp4',
  c1:'МОЗГ', c2:'ПРОЛИСТАЛ',
  slides:[
  {t:'Что такое баннерная слепота', pts:['Мозг научился <b>отфильтровывать</b> всё однообразное и предсказуемое', 'Пришло из рекламы: баннеры стали одинаковыми — люди перестали их видеть', 'В ленте то же: если всё на одно лицо, палец листает <b>на автопилоте</b>', 'Твой пост может быть открыт и... не замечен ни на секунду'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Главный враг — однообразие', pts:['Десять постов одного шаблона подряд = мозг ставит метку «уже видел»', 'Одинаковый ракурс, один фон, один тип обложки — <b>усыпляют внимание</b>', 'Даже красивое, но повторяющееся, теряет силу к третьему разу', 'Слепота лечится не яркостью, а <b>сменой ритма и разнообразием</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Как ломать шаблон правильно', pts:['Чередуй <b>планы</b>: общий, средний, крупный — глаз не привыкает', 'Меняй <b>сюжет кадра</b>: человек, предмет, деталь, пространство, текст', 'Раз в несколько постов — <b>кадр-выброс</b>: неожиданный ракурс или цвет', 'Разнообразие внутри одной гаммы = свежо, но всё ещё твой стиль'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Разнообразие ≠ хаос', pts:['Ошибка новичка: спастись от слепоты и уйти в <b>визуальный бардак</b>', 'Каркас держат <b>3 константы</b>: гамма, ≤2 шрифта, единая обработка', 'Меняем содержание кадра — оставляем неизменной атмосферу', 'Правило: <b>узнаваемо, но не одинаково</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Тест на слепоту за 30 секунд', pts:['Уменьши сетку до сетки-превью и <b>расфокусируй взгляд</b>', 'Если посты сливаются в серое пятно — у тебя слепота', 'Если глаз цепляется за 2–3 точки и ведёт дальше — ритм живой', 'Проверяй так каждую новую партию из <b>9–12 постов</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'}
  ],
  quiz:[
  {q:'Что вызывает баннерную слепоту?', o:['Однообразие и предсказуемость', 'Яркие цвета', 'Много текста', 'Живые лица'], a:0},
  {q:'Чем лечится баннерная слепота?', o:['Максимальной яркостью', 'Сменой ритма и разнообразием', 'Большим числом постов', 'Одним жёстким шаблоном'], a:1},
  {q:'Как разнообразить, не сломав стиль?', o:['Менять содержание кадра, держа гамму и шрифты', 'Каждый пост в новой цветовой схеме', 'Ставить случайные фильтры', 'Чередовать разные шрифты в каждом посте'], a:0},
  {q:'Что такое кадр-выброс?', o:['Бракованное фото', 'Неожиданный ракурс или цвет, ломающий привычку глаза', 'Реклама в ленте', 'Пост без обработки'], a:1},
  {q:'Как быстро проверить сетку на слепоту?', o:['Спросить друзей', 'Расфокусировать взгляд: сливается в пятно или нет', 'Посчитать лайки', 'Сравнить с конкурентом'], a:1}
  ],
  pairs:[
  ['Баннерная слепота', 'Мозг фильтрует однообразное как шум'],
  ['Однообразие', 'Главная причина, по которой пост не замечают'],
  ['Кадр-выброс', 'Неожиданный ракурс, ломающий привычку'],
  ['3 константы', 'Гамма, шрифты и обработка держат стиль'],
  ['Тест пятна', 'Расфокус: сетка сливается или дышит']
  ],
  task:{
    intro:'Возьми последние 9 своих постов, посмотри на них как на сетку с расфокусом и найди, где начинается баннерная слепота. Предложи 2 конкретных приёма, чтобы её разбить.',
    chips:['Всё в один ракурс', 'Одинаковый фон', 'Нет смены планов', 'Сетка сливается', 'Приём 1', 'Приём 2'],
    ph:'Расфокус показал серое пятно: 7 из 9 постов — селфи в упор на одном фоне. Слепота железная. Приём 1: два ближайших поста заменю на общий план рабочего места. Приём 2: добавлю один кадр-деталь (руки, кофе) как визуальную паузу между лицами.',
    verdict:'Сильно — ты не просто увидел проблему, а поставил диагноз «7 из 9 селфи в упор» и дал два разных по типу приёма, а не два похожих. Усилю: чередуй план по правилу «через один» — лицо, деталь, лицо, пространство, тогда глаз ведёт по сетке сам и слепота не вернётся.'
  }
},
{
  title:'Сбор смыслов и мудборд',
  sub:'3:45 · видео (голос Даниэля) + тест + игра', dur:'3:45',
  videoUrl:'https://okoteam.top/media/oko_m3_2.mp4',
  c1:'СНАЧАЛА', c2:'СМЫСЛ',
  slides:[
  {t:'Визуал начинается не с фото', pts:['Ошибка: сразу бежать снимать. Правильно — сначала <b>собрать смыслы</b>', 'Смыслы = о чём ты, для кого, какие ценности, какое настроение транслируешь', 'Пока не ясно ЧТО показываешь, любая красивая картинка бьёт мимо', '<b>Мудборд</b> — это мостик от смыслов к конкретной картинке'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'},
  {t:'Как собрать смыслы блога', pts:['Выпиши <b>3–5 ключевых тем</b>, вокруг которых крутится твой контент', 'Опиши целевого подписчика: возраст, вкус, что ему близко визуально', 'Найди <b>3 слова-настроения</b>: например «тепло, честно, по-домашнему»', 'Смыслы — это фильтр: подходит кадр этим словам или нет'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Что такое мудборд', pts:['Это <b>доска-настроение</b>: 15–30 референсов, задающих вектор визуала', 'Не «чужие фото на украсть», а образец света, гаммы, ракурсов, атмосферы', 'Собирают в <b>Pinterest</b>, в заметках или на одном холсте', 'Свой уникальный референс-ряд генерят в <b>Midjourney</b>, <b>Flux</b>, <b>Nano Banana</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="24" y="14" width="72" height="46" rx="7"/><circle cx="43" cy="30" r="6"/><path d="M28 54l20-18 14 12 12-10 22 16" opacity=".85"/></svg>'},
  {t:'Как собирать референсы правильно', pts:['Бери то, что вызывает «вау, хочу так же», и сразу спрашивай <b>почему</b>', 'Отмечай конкретику: тёплый свет, приглушённые тона, много воздуха', 'Собери 30 — выкинь всё, что <b>не бьётся</b> с твоими 3 словами-настроения', 'Останется 12–15 — это и есть твой визуальный код'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h80L68 44v18l-16 8V44z"/></svg>'},
  {t:'От мудборда к своим кадрам', pts:['Мудборд отвечает на 3 вопроса: <b>какой свет, какая гамма, какие сюжеты</b>', 'Под каждый референс придумай, как повторить его в СВОЕЙ реальности', 'Не копируй один в один — вытаскивай <b>принцип</b>, а не картинку', 'Готовый мудборд экономит часы: съёмка идёт по понятному ориентиру'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Частые ошибки на этом этапе', pts:['Мудборд из <b>несовместимых стилей</b> — потом лента разваливается', 'Референсы «просто красивые», но <b>не про твои смыслы</b>', 'Слишком много образцов и ни одного вывода — доска ради доски', 'Пропуск этапа смыслов → визуал есть, а <b>послания нет</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'}
  ],
  quiz:[
  {q:'С чего начинается работа над визуалом?', o:['Со сбора смыслов', 'С покупки камеры', 'С первой съёмки', 'С выбора фильтра'], a:0},
  {q:'Что такое мудборд?', o:['Доска-настроение из референсов, задающих вектор', 'Папка чужих фото для копирования', 'Готовая сетка постов', 'Список тем для сторис'], a:0},
  {q:'Как отбирать референсы для мудборда?', o:['По совпадению со словами-настроением блога', 'Всё самое яркое подряд', 'Только фото конкурентов', 'Случайно, чтобы было разнообразие'], a:0},
  {q:'Что вытаскивают из референса?', o:['Принцип: свет, гамму, сюжет', 'Точную копию кадра', 'Число лайков', 'Имя автора'], a:0},
  {q:'Какие инструменты помогут собрать свой референс-ряд?', o:['Midjourney, Flux, Nano Banana', 'Excel и Word', 'Только чужой Instagram', 'Калькулятор'], a:0}
  ],
  pairs:[
  ['Сбор смыслов', 'О чём ты, для кого и с каким настроением'],
  ['Мудборд', 'Доска-настроение из отобранных референсов'],
  ['Слова-настроение', 'Фильтр «подходит кадр или нет»'],
  ['Принцип референса', 'Свет, гамма, сюжет — а не сама картинка'],
  ['Midjourney / Flux', 'Генерация своего уникального референс-ряда']
  ],
  task:{
    intro:'Сформулируй 3 слова-настроения своего блога и опиши мудборд под них: какой свет, какая гамма и 3 типа сюжетов, которые ты будешь снимать.',
    chips:['Слово-настроение 1', 'Слово 2', 'Слово 3', 'Какой свет', 'Какая гамма', '3 типа сюжетов'],
    ph:'Настроение: тепло, честно, по-домашнему. Свет — мягкий дневной у окна, без вспышки. Гамма — бежевый, тёплый белый, приглушённый зелёный. Сюжеты: 1) я за работой дома, 2) детали быта (кофе, книга, руки), 3) общий план уютного пространства.',
    verdict:'Чётко — три слова-настроения не абстрактные, а сразу превращаются в свет, гамму и конкретные сюжеты, это и есть рабочий мудборд, а не доска ради доски. Усилю: собери под каждый из трёх сюжетов по 4 живых референса в Pinterest или сгенерь в Midjourney, чтобы на съёмке был точный ориентир, а не память.'
  }
},
{
  title:'Планирование сетки',
  sub:'3:34 · видео (голос Даниэля) + тест + игра', dur:'3:34',
  videoUrl:'https://okoteam.top/media/oko_m3_3.mp4',
  c1:'ЛЕНТА', c2:'КАК СИСТЕМА',
  slides:[
  {t:'Лента — это витрина, а не архив', pts:['Подписчик видит не один пост, а <b>3–9 сразу</b> — как единую композицию', 'Красивый по отдельности пост может <b>ломать</b> общую картину сетки', 'Планировать надо не фото, а <b>ряды и переходы</b> между ними', 'Витрину собирают заранее, а не «снял — выложил — как получится»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Планируй партиями по 12–15', pts:['Оптимально верстать <b>12–15 фото</b> вперёд — это 4–5 рядов сетки', 'Так видно ритм: где скучно, где пестро, где два похожих кадра рядом', 'Работает <b>раскладка</b>: приложение-планировщик или просто холст', 'Партия = ты управляешь впечатлением, а не догоняешь его'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="16" width="76" height="46" rx="8"/><path d="M22 30h76M38 10v12M82 10v12"/><circle cx="44" cy="44" r="3" fill="currentColor" stroke="none"/><circle cx="60" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Три опоры красивой сетки', pts:['<b>Единая гамма</b> — все кадры в одной цветовой семье', '<b>Не более двух шрифтов</b> на текстовых обложках', '<b>Единая обработка</b> — один пресет, одна атмосфера на всю партию', 'Опоры держат стиль, пока содержание кадров меняется'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Ритм и чередование', pts:['Чередуй <b>образы</b>: лицо → деталь → пространство → текст → лицо', 'Не ставь два одинаковых по цвету или плану кадра <b>вплотную</b>', 'Раскидывай яркие акценты <b>по диагонали</b> — глаз ведёт по сетке', 'Ритм — это когда взгляд не спотыкается и не засыпает'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Воздух и дыхание сетки', pts:['<b>Воздух</b> — свободное место в кадре, куда «дышит» композиция', 'Плотные, забитые кадры подряд утомляют — нужны визуальные паузы', 'Светлый минималистичный кадр между насыщенными = <b>вдох</b>', 'Кадр <b>4:5</b> — вертикаль, забирает максимум площади в ленте'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Как проверить сетку перед выкладкой', pts:['Собери <b>превью</b> будущих 9 постов в планировщике', 'Расфокусируй взгляд: есть ритм или серое пятно', 'Проверь, нет ли двух близнецов рядом и не пестрит ли', 'Только после этого — <b>публикация по одному</b> в нужном порядке'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Сколько фото оптимально планировать вперёд?', o:['1–2', '12–15', '50+', 'Не планировать вовсе'], a:1},
  {q:'Сколько шрифтов держать на обложках?', o:['Не более двух', 'Ровно пять', 'По одному на пост', 'Без ограничений'], a:0},
  {q:'Что такое воздух в кадре?', o:['Свободное место, дающее композиции дышать', 'Пустой пост без смысла', 'Размытый фон', 'Кадр без обработки'], a:0},
  {q:'Какой формат кадра забирает больше площади в ленте?', o:['1:1', '4:5', '16:9', '9:16'], a:1},
  {q:'Как правильно чередовать кадры в сетке?', o:['Лицо, деталь, пространство, текст — по ритму', 'Все лица подряд', 'По дате съёмки', 'Два одинаковых рядом для симметрии'], a:0}
  ],
  pairs:[
  ['Партия 12–15', 'Планируешь вперёд и видишь ритм'],
  ['Единая гамма', 'Все кадры одной цветовой семьи'],
  ['≤2 шрифтов', 'Порядок на текстовых обложках'],
  ['Воздух', 'Свободное место, дающее кадру дышать'],
  ['Формат 4:5', 'Вертикаль, максимум площади в ленте'],
  ['Ритм', 'Чередование образов без спотыканий']
  ],
  task:{
    intro:'Спланируй свою следующую сетку из 9 постов: распиши по порядку тип каждого кадра и назови гамму, которая их объединит.',
    chips:['Гамма партии', 'Ряд 1 (3 кадра)', 'Ряд 2 (3 кадра)', 'Ряд 3 (3 кадра)', 'Где воздух', 'Формат 4:5'],
    ph:'Гамма: тёплый беж и зелень. Ряд 1: лицо / деталь-кофе / пространство. Ряд 2: текстовая обложка / лицо / деталь-книга. Ряд 3: пространство / лицо / светлый минимал-кадр (воздух). Всё в 4:5, один пресет на партию.',
    verdict:'Отлично — ты не свалил 9 фото в кучу, а выстроил чередование лицо-деталь-пространство и специально заложил светлый кадр-воздух, это уже режиссура сетки. Усилю: собери эти 9 в любом планировщике и глянь расфокусом перед выкладкой — если два зелёных кадра встали по диагонали рядом, поменяй местами, чтобы акценты не слипались.'
  }
},
{
  title:'Гамма и шрифты',
  sub:'3:20 · видео (голос Даниэля) + тест + игра', dur:'3:20',
  videoUrl:'https://okoteam.top/media/oko_m3_4.mp4',
  c1:'ЦВЕТ И', c2:'БУКВЫ',
  slides:[
  {t:'Гамма делает ленту узнаваемой', pts:['<b>Гамма</b> — 3–5 цветов, которые повторяются во всех кадрах', 'Именно она даёт эффект «сразу видно, что это твой пост»', 'Единый цвет связывает даже разные по сюжету фото в <b>одну ленту</b>', 'Хаос цветов = баннерная слепота и ощущение любительства'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M74 12 92 30 56 66l-18 4 4-18z"/><path d="M62 24 80 42" opacity=".7"/></svg>'},
  {t:'Как собрать свою палитру', pts:['Возьми <b>1 базовый</b> (фон-настроение), 2 дополнительных, 1 акцент', 'Отталкивайся от смыслов: тёплые тона = уют, холодные = технологичность', 'Проверь палитру на <b>реальных кадрах</b>, а не только в теории', 'Сгенерить и протестить цветовые связки можно в <b>Midjourney</b> и <b>Flux</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Обработка без пересвета и кислоты', pts:['<b>Пересвет</b> — выбитый в белое кадр, теряются детали и объём', '<b>Кислота</b> — перекрученная насыщенность, цвета «кричат» неестественно', 'Здоровая обработка: мягкий контраст, живой цвет кожи, читаемые тени', 'Один <b>пресет</b> на всю партию = единая атмосфера без ручной подгонки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'},
  {t:'Правило двух шрифтов', pts:['На обложках — <b>максимум два шрифта</b>: один заголовочный, один текстовый', 'Больше — визуальный шум, лента выглядит собранной наспех', 'Пара строится на контрасте: жирный акцентный + спокойный основной', 'Один и тот же дуэт шрифтов во всех постах = <b>фирменный почерк</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Как подобрать пару шрифтов', pts:['Контраст по <b>толщине и характеру</b>: массивный заголовок + лёгкий текст', 'Не мешай два похожих — будет смотреться как ошибка, а не приём', 'Заголовочный — с характером, текстовый — <b>читаемый</b> в мелком размере', 'Определился один раз — используешь во всей ленте без исключений'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'}
  ],
  quiz:[
  {q:'Сколько цветов держит рабочая гамма?', o:['3–5', '10–12', 'Один', 'Сколько угодно'], a:0},
  {q:'Что такое кислота в обработке?', o:['Перекрученная насыщенность, неестественно кричащий цвет', 'Выбитый в белое кадр', 'Тёмная тень', 'Чёрно-белое фото'], a:0},
  {q:'Сколько шрифтов держать на обложках?', o:['Максимум два', 'Минимум четыре', 'По одному на слово', 'Не важно'], a:0},
  {q:'На чём строится хорошая пара шрифтов?', o:['На контрасте толщины и характера', 'На двух почти одинаковых шрифтах', 'На случайном выборе', 'На самом узорчатом шрифте'], a:0},
  {q:'Зачем один пресет на всю партию?', o:['Единая атмосфера без ручной подгонки', 'Чтобы фото были ярче всех', 'Чтобы скрыть плохой свет', 'Это требование алгоритма'], a:0}
  ],
  pairs:[
  ['Гамма', '3–5 повторяющихся цветов ленты'],
  ['Акцентный цвет', 'Одна яркая нота на спокойном фоне'],
  ['Пересвет', 'Выбитый в белое кадр без деталей'],
  ['Кислота', 'Перекрученная, кричащая насыщенность'],
  ['Пара шрифтов', 'Контраст заголовочного и текстового'],
  ['Пресет', 'Единая обработка на всю партию']
  ],
  task:{
    intro:'Собери гамму своего блога из 4 цветов и подбери пару шрифтов. Обоснуй, почему именно эти цвета и шрифты бьются с твоими смыслами.',
    chips:['Базовый цвет', 'Доп. цвет 1', 'Доп. цвет 2', 'Акцент', 'Заголовочный шрифт', 'Текстовый шрифт'],
    ph:'Гамма: базовый — тёплый беж, доп. — молочный и приглушённый олив, акцент — терракота. Смысл «тепло и честно» → мягкие природные тона. Шрифты: заголовочный — плотный гротеск с характером, текстовый — лёгкий читаемый гротеск. Контраст по толщине, оба без засечек — чисто и современно.',
    verdict:'Сильно — палитра не набор случайных цветов, а прямое продолжение слова «тепло», и ты дал акцентную терракоту, чтобы лента не была пресной. Усилю: прогони эти 4 цвета на 3 своих реальных фото через один пресет, и если терракота начнёт спорить с цветом кожи — приглуши её на пару тонов, акцент должен поддерживать, а не перетягивать.'
  }
},
{
  title:'Планы и ракурсы съёмки',
  sub:'3:52 · видео (голос Даниэля) + тест + игра', dur:'3:52',
  videoUrl:'https://okoteam.top/media/oko_m3_5.mp4',
  c1:'ПЛАНЫ И', c2:'РАКУРСЫ',
  slides:[
  {t:'Зачем разные планы', pts:['<b>План</b> — это насколько крупно объект в кадре: от общего до сверхкрупного', 'Одинаковая крупность во всех постах = <b>баннерная слепота</b>', 'Смена планов создаёт ритм и держит внимание по всей сетке', 'Правило: в партии должны быть <b>минимум 3 разных плана</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="18" width="54" height="38" rx="7"/><path d="M74 30l24-12v38L74 42z"/><circle cx="40" cy="37" r="9"/></svg>'},
  {t:'Пять планов, которые нужны', pts:['<b>Общий</b> — человек и пространство целиком, задаёт контекст', '<b>Средний</b> — по пояс, рабочий план для контакта со зрителем', '<b>Крупный</b> — лицо, эмоция, максимум личного контакта', '<b>Сверхкрупный</b> — деталь: руки, предмет, фактура', '<b>Деталь-акцент</b> — визуальная пауза между людьми в сетке'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Ракурс меняет всё', pts:['<b>Ракурс</b> — точка и угол, с которого снимаешь', 'Съёмка <b>сверху</b> — раскладки, столы, уменьшает объект', '<b>На уровне глаз</b> — честный контакт, база для портрета', 'Чуть <b>снизу</b> — добавляет значимости и уверенности герою', 'Меняй ракурс, чтобы даже привычный сюжет смотрелся свежо'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Композиция и воздух в кадре', pts:['Оставляй <b>воздух</b> — свободное место, куда направлен взгляд или движение', 'Не лепи объект строго в центр каждый раз — используй смещение', 'Убери из кадра <b>лишний мусор</b>: провода, случайные предметы фона', 'Простой фон = объект читается, кадр не разваливается'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="24" y="14" width="72" height="46" rx="7"/><circle cx="43" cy="30" r="6"/><path d="M28 54l20-18 14 12 12-10 22 16" opacity=".85"/></svg>'},
  {t:'Снимаем на телефон', pts:['Для качественной ленты хватает <b>iPhone 11–12</b> и новее', 'Главное не камера, а <b>свет</b>: дневной у окна бьёт любую вспышку', 'Снимай в <b>4:5</b> вертикально — максимум площади в ленте', 'Делай по 5–10 дублей одного кадра — выберешь лучший потом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="42" y="8" width="36" height="54" rx="7"/><path d="M52 16h16M56 54h8" opacity=".7"/></svg>'},
  {t:'Тренды — да, потеря себя — нет', pts:['Следи за трендовыми ракурсами, но <b>адаптируй под свой стиль</b>', 'Слепое копирование = ты растворяешься среди тысяч таких же', 'Бери из тренда <b>идею</b>, оборачивай в свою гамму и подачу', 'Референсы трендов удобно раскадровать в <b>Midjourney</b> и <b>Higgsfield</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Что такое план в съёмке?', o:['Насколько крупно объект в кадре', 'Расписание постов', 'Угол наклона телефона', 'Цветовая гамма'], a:0},
  {q:'Какой план даёт максимум личного контакта?', o:['Крупный (лицо, эмоция)', 'Общий', 'Сверхкрупный на предмет', 'Съёмка сверху'], a:0},
  {q:'Что даёт ракурс чуть снизу?', o:['Добавляет герою значимости и уверенности', 'Уменьшает объект', 'Делает раскладку', 'Всегда портит кадр'], a:0},
  {q:'Что важнее камеры для хорошего кадра?', o:['Свет', 'Дорогой объектив', 'Штатив', 'Число мегапикселей'], a:0},
  {q:'Как правильно работать с трендом?', o:['Взять идею и адаптировать под свой стиль', 'Скопировать один в один', 'Игнорировать любые тренды', 'Менять из-за тренда всю гамму'], a:0}
  ],
  pairs:[
  ['Общий план', 'Человек и пространство целиком'],
  ['Крупный план', 'Лицо и эмоция, максимум контакта'],
  ['Сверхкрупный', 'Деталь: руки, предмет, фактура'],
  ['Ракурс снизу', 'Добавляет герою значимости'],
  ['Воздух', 'Свободное место для взгляда и движения'],
  ['Свет у окна', 'Важнее любой дорогой камеры']
  ],
  task:{
    intro:'Составь мини-план съёмки одного дня: распиши 5 кадров с разными планами и ракурсами под одну тему, чтобы закрыть будущий ряд сетки.',
    chips:['Тема дня', 'Общий план', 'Средний/крупный', 'Сверхкрупный-деталь', 'Ракурс-эксперимент', 'Где воздух'],
    ph:'Тема: утро за работой. 1) общий — стол и я у окна, контекст. 2) крупный — лицо, взгляд в кадр, свет слева. 3) сверхкрупный — руки на клавиатуре. 4) съёмка сверху — раскладка стола: кофе, блокнот. 5) средний чуть снизу — я встаю, воздух сверху кадра. Всё в 4:5, дневной свет.',
    verdict:'Отлично — из одной темы «утро за работой» ты вытащил пять разных планов и даже поменял ракурс на съёмке сверху и снизу, это готовый разнообразный ряд без баннерной слепоты. Усилю: на каждый из пяти кадров сними по 6–8 дублей с чуть разным светом и наклоном, на монтаже выбор из восьми почти всегда даёт кадр сильнее, чем единственный дубль.'
  }
},
{
  title:'Личность в кадре (96% лайкают людей)',
  sub:'4:02 · видео (голос Даниэля) + тест + игра', dur:'4:02',
  videoUrl:'https://okoteam.top/media/oko_m3_6.mp4',
  c1:'ЛИЦО =', c2:'КОНТАКТ',
  slides:[
  {t:'Люди лайкают людей', pts:['<b>96% лайков</b> собирают фото с людьми, а не с предметами и пейзажами', 'Мозг эволюционно заточен искать <b>лица</b> — они цепляют взгляд первыми', 'Лицо в кадре = ощущение живого контакта, а не безликого бренда', 'Предметная лента красива, но <b>холодна</b> — за ней не чувствуют человека'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Личность продаёт сильнее ниши', pts:['Экспертов в нише — сотни, <b>таких как ты</b> — ты один', 'Люди подписываются на человека, покупают у того, кому <b>доверяют</b>', 'Личность = твоя главная <b>отстройка</b>, её невозможно скопировать', 'Показывай себя: лицо, эмоции, характер, а не только результат работы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="24" r="11"/><path d="M40 58c1-13 9-20 20-20s19 7 20 20"/></svg>'},
  {t:'Зрительный контакт через экран', pts:['Взгляд <b>в камеру</b> = прямой контакт «глаза в глаза» со зрителем', 'Такой кадр останавливает палец сильнее любого предмета', 'Живая эмоция важнее идеальной позы — <b>искренность считывается</b>', 'Чередуй взгляд в кадр и в сторону, чтобы лица не были однообразными'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Как побороть страх камеры', pts:['Первые кадры кривые у всех — это <b>нормальный этап</b>, не приговор', 'Снимай <b>сериями</b>: из 30 дублей 2–3 всегда живые и твои', 'Начни с полубоку и деталей, наращивай крупность <b>постепенно</b>', 'Чем чаще в кадре — тем естественнее, камера перестаёт пугать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Личное ≠ только селфи', pts:['Присутствие человека — это и <b>руки в кадре</b>, и фигура со спины, и силуэт', 'Процесс работы с твоим участием живее, чем голый результат', 'Чередуй: портрет, деталь с руками, ты в пространстве — против <b>слепоты</b>', 'Главное — чтобы за лентой <b>чувствовался живой человек</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="30" cy="24" r="12"/><path d="M18 24a12 12 0 0 1 24 0"/><path d="M52 16h54M52 27h40M52 38h50" opacity=".85"/></svg>'},
  {t:'Баланс личности и пользы', pts:['Только лицо без смысла надоедает, только польза без лица — холодит', 'Рабочая формула: <b>человек + ценность</b> в одном кадре', 'Покажи эмоцию к теме — и лайк, и доверие, и запоминаемость разом', 'Твоё лицо — это <b>мостик</b> между подписчиком и твоим продуктом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'}
  ],
  quiz:[
  {q:'Какая доля лайков приходится на фото с людьми?', o:['51%', '72%', '96%', '40%'], a:2},
  {q:'Почему лица цепляют взгляд первыми?', o:['Мозг эволюционно заточен искать лица', 'Их подсвечивает алгоритм', 'Они всегда ярче', 'Так работают фильтры'], a:0},
  {q:'Что делает личность главной отстройкой?', o:['Её невозможно скопировать', 'Она дешевле съёмки предметов', 'Так требует алгоритм', 'Ниша всегда важнее человека'], a:0},
  {q:'Как лучше побороть страх камеры?', o:['Снимать сериями и наращивать крупность постепенно', 'Ждать, пока страх пройдёт сам', 'Снимать только предметы', 'Ставить один идеальный кадр с первого дубля'], a:0},
  {q:'Что такое присутствие человека, кроме селфи?', o:['Руки в кадре, фигура со спины, силуэт, процесс', 'Только портрет в упор', 'Логотип поверх фото', 'Пейзаж без людей'], a:0},
  {q:'Какая формула сильного личного кадра?', o:['Человек + ценность', 'Только лицо', 'Только польза без лица', 'Максимум предметов'], a:0}
  ],
  pairs:[
  ['96%', 'Доля лайков на фото с людьми'],
  ['Взгляд в камеру', 'Прямой контакт глаза в глаза'],
  ['Личность', 'Отстройка, которую нельзя скопировать'],
  ['Серия дублей', 'Способ побороть страх камеры'],
  ['Руки в кадре', 'Присутствие человека без селфи'],
  ['Человек + ценность', 'Формула сильного личного кадра']
  ],
  task:{
    intro:'Придумай 3 разных способа показать личность в своей ленте, не сводя всё к селфи. Опиши, какую эмоцию или смысл несёт каждый кадр.',
    chips:['Портрет со взглядом', 'Руки в процессе', 'Ты в пространстве', 'Со спины/силуэт', 'Какая эмоция', 'Какая польза рядом'],
    ph:'1) Крупный портрет, взгляд в камеру, лёгкая улыбка — контакт и доверие. 2) Руки за работой над проектом — показываю процесс, не только итог. 3) Общий план: я у окна со спины, кофе, ноутбук — атмосфера и «свой человек». Эмоции: тепло, вовлечённость, спокойствие. Рядом с каждым — короткая мысль по теме дня.',
    verdict:'Отлично — ты закрыл сразу три уровня присутствия от прямого взгляда до силуэта со спины, и главное привязал к каждому кадру конкретную эмоцию, а не просто «фото меня». Усилю: в портрете со взглядом добавь микро-действие — засмеяться, поправить волосы, отвести взгляд, живые кадры с движением всегда обгоняют статичную позу по контакту и лайкам.'
  }
}
  ],
  "m4": [
{
  title:'Личный контент и доверие',
  sub:'3:20 · видео (голос Даниэля) + тест + игра', dur:'3:20',
  videoUrl:'https://okoteam.top/media/oko_m4_0.mp4',
  c1:'ЛИЧНОЕ', c2:'ДОВЕРИЕ',
  slides:[
  {t:'Покупают не у бренда, а у человека', pts:['Логотип не вызывает эмоций — <b>человек</b> вызывает', 'Люди идут за тем, чьё лицо и историю они узнают', '<b>Доверие</b> — это валюта, которую нельзя купить рекламой', 'Первые продажи почти всегда приходят от тех, кто вас \'знает\''],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Что такое личный контент на самом деле', pts:['Не селфи из кафе, а <b>показ процесса и мышления</b>', 'Как вы принимаете решения, ошибаетесь, чините ошибки', '<b>Бэкстейдж</b> работы ценнее вылизанного результата', 'Человек за экспертом: ценности, быт, характер', 'Личное = то, что нельзя скопировать у конкурента'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="24" r="11"/><path d="M40 58c1-13 9-20 20-20s19 7 20 20"/></svg>'},
  {t:'Три слоя доверия', pts:['<b>Компетентность</b> — вы разбираетесь в теме, это видно', '<b>Последовательность</b> — вы стабильно на связи, не пропадаете', '<b>Открытость</b> — вы показываете не только победы', 'Слои копятся месяцами, ломаются одним фальшивым постом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Маска эксперта убивает доверие', pts:['Идеальная картинка читается как <b>реклама</b> — ей не верят', 'Аудитория чувствует наигранность быстрее, чем вы думаете', 'Признание ошибки поднимает доверие сильнее любого кейса', 'Правило: <b>90% пользы, но всегда с живым лицом</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Как показывать себя без стыда', pts:['Начните с <b>камеры телефона</b> и одной мысли в день', 'Снимайте короткие \'мысли вслух\' — не готовьтесь идеально', 'Черновик сценария можно набросать в <b>ChatGPT</b>, но словами говорите свои', 'Стыд уходит после 20–30 роликов, не раньше — это норма', 'Смотрите не на охваты, а на <b>первые тёплые сообщения</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="18" width="54" height="38" rx="7"/><path d="M74 30l24-12v38L74 42z"/><circle cx="40" cy="37" r="9"/></svg>'},
  {t:'Доверие превращается в деньги', pts:['Тёплая аудитория покупает <b>без длинных продаж</b>', 'Один лояльный подписчик ценнее ста случайных охватов', 'Путь: узнал → доверился → купил → рекомендует', '<b>Первые 5000 рублей</b> почти всегда от \'своих\', не от рекламы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'}
  ],
  quiz:[
  {q:'Почему первые продажи чаще приходят от \'своих\'?', o:['Они богаче остальной аудитории', 'Они уже прошли путь узнавания и доверия', 'Им всегда дают скидку', 'Алгоритм показывает контент только им'], a:1},
  {q:'Что такое настоящий личный контент?', o:['Красивые фото из отпуска', 'Только экспертные разборы без лица', 'Показ процесса, мышления и бэкстейджа работы', 'Репосты чужих цитат'], a:2},
  {q:'Как влияет \'маска идеального эксперта\' на доверие?', o:['Повышает — люди любят профессионалов', 'Снижает — читается как реклама, ей не верят', 'Не влияет никак', 'Повышает только у новичков'], a:1},
  {q:'Из чего складываются три слоя доверия?', o:['Охваты, лайки, репосты', 'Компетентность, последовательность, открытость', 'Реклама, бюджет, таргет', 'Красота, монтаж, музыка'], a:1},
  {q:'Когда обычно уходит стыд перед камерой?', o:['Сразу после первого ролика', 'После 20–30 роликов регулярной практики', 'Никогда, с этим надо смириться', 'Только после платных курсов актёрского мастерства'], a:1}
  ],
  pairs:[
  ['Личный контент', 'Показ процесса, мышления и характера, а не глянца'],
  ['Доверие', 'Валюта, которую нельзя купить рекламой'],
  ['Маска эксперта', 'Идеальная картинка, которая читается как реклама'],
  ['Последовательность', 'Стабильное присутствие, вы не пропадаете'],
  ['Первые деньги', 'Приходят от тёплой аудитории, а не от охватов']
  ],
  task:{
    intro:'Опиши три темы личного контента, которые покажут тебя как человека, а не как безликого эксперта. По каждой — что именно снимешь.',
    chips:['Моя ошибка и как я её исправил', 'Бэкстейдж одного заказа', 'Почему я в этой нише', 'Мой обычный рабочий день', 'Что я думаю о тренде в нише', 'Провал, который меня научил'],
    ph:'1) Ошибка: пересолила крем, переделала торт ночью — покажу процесс спасения. 2) Бэкстейдж: как собираю заказ с нуля до коробки. 3) Почему кондитер: история про бабушкины пироги и первый провальный бисквит.',
    verdict:'Отлично — ты взяла не абстрактные темы, а конкретные ситуации с деталями (пересоленный крем, бабушкины пироги), именно это цепляет. Усилю: в каждом сюжете добавь одну честную эмоцию в моменте (что чувствовала), тогда доверие вырастет вдвое.'
  }
},
{
  title:'Шкала эмоций',
  sub:'3:40 · видео (голос Даниэля) + тест + игра', dur:'3:40',
  videoUrl:'https://okoteam.top/media/oko_m4_1.mp4',
  c1:'ШКАЛА', c2:'ЭМОЦИЙ',
  slides:[
  {t:'Эмоция — двигатель охвата', pts:['Контент без эмоции пролистывают за <b>полсекунды</b>', 'Алгоритм ловит не тему, а <b>реакцию</b> на неё', 'Сильная эмоция = сохранения, репосты, комментарии', 'Ваша задача — не информировать, а <b>задеть</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Семь эмоций вашего контента', pts:['Тёплые: <b>радость, восхищение, благодарность</b>', 'Острые: <b>злость, разочарование, несогласие</b>', 'Глубокая: <b>сопереживание</b> — самая объединяющая', 'Здоровый блог качается по всей шкале, а не в одной точке'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Почему нельзя быть всегда \'хорошеньким\'', pts:['Вечная позитивность превращается в <b>фон</b>, её не замечают', 'Только тёплое = \'приятный, но забываемый\' блогер', '<b>Острые эмоции</b> дают точку зрения и характер', 'У вас должно быть мнение, за которое кто-то заденется'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c4 12-6 16-6 26 0-6-5-9-5-9-4 6-11 11-11 22a22 22 0 0 0 44 0c0-14-12-20-16-39z"/></svg>'},
  {t:'Как включать острые эмоции без токсичности', pts:['Злость — на <b>явление и подход</b>, не на конкретных людей', 'Несогласие — с аргументом, а не с ярлыком', 'Разочарование — честное, с выводом \'что делать\'', 'Правило: острота ради смысла, а не ради скандала'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Микс эмоций внутри недели', pts:['Не заваливайте ленту одной нотой — <b>чередуйте</b>', 'Понедельник радость, среда несогласие, пятница сопереживание', 'После острого поста — тёплый, чтобы <b>снять напряжение</b>', 'Держите баланс: примерно 60% тёплого, 40% острого'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Проверка: что чувствует зритель', pts:['Перед публикацией спросите: <b>какую эмоцию я вызову?</b>', 'Если ответ \'никакую\' — пост в корзину или на переработку', 'Читайте комментарии как <b>термометр эмоций</b>', 'Спор в комментариях — не провал, а сигнал живого блога'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'}
  ],
  quiz:[
  {q:'На что реально реагирует алгоритм?', o:['На длину текста', 'На эмоцию и реакцию аудитории', 'На количество хэштегов', 'На время суток публикации'], a:1},
  {q:'Какая из семи эмоций самая объединяющая?', o:['Злость', 'Восхищение', 'Сопереживание', 'Несогласие'], a:2},
  {q:'Чем плоха вечная позитивность в блоге?', o:['Она превращается в фон, её перестают замечать', 'Она запрещена правилами площадок', 'Она снижает качество фото', 'Ничем, это идеальная стратегия'], a:0},
  {q:'Как правильно выражать злость в контенте?', o:['На конкретных людей поимённо', 'На явление и подход, а не на личности', 'Вообще не выражать', 'Только в закрытых чатах'], a:1},
  {q:'Что делать, если пост не вызывает никакой эмоции?', o:['Опубликовать, охваты сами придут', 'Добавить больше хэштегов', 'Переработать или отправить в корзину', 'Купить рекламу на него'], a:2}
  ],
  pairs:[
  ['Радость', 'Тёплая эмоция, сближает и располагает'],
  ['Злость', 'Острая эмоция на явление, даёт характер блогу'],
  ['Сопереживание', 'Самая объединяющая эмоция, рождает поддержку'],
  ['Несогласие', 'Точка зрения с аргументом, а не ярлык'],
  ['Нейтральность', 'Фон, который аудитория не замечает'],
  ['Микс эмоций', 'Чередование тёплого и острого в течение недели']
  ],
  task:{
    intro:'Составь мини-план на неделю: 5 постов, для каждого укажи одну целевую эмоцию из шкалы и тему. Хотя бы два поста должны быть \'острыми\'.',
    chips:['Радость: маленькая победа клиента', 'Несогласие: развею миф ниши', 'Разочарование: почему бросил старый подход', 'Восхищение: разбор чужой крутой работы', 'Сопереживание: моя трудная ситуация', 'Благодарность: письмо подписчикам'],
    ph:'Пн — радость (отзыв клиента), Вт — несогласие (миф \'дорого = качественно\'), Ср — сопереживание (мой выгорающий месяц), Чт — восхищение (разбор чужого кейса), Пт — разочарование (почему ушёл с фриланс-биржи).',
    verdict:'Хорошо — шкала раскачана, есть и тёплое, и острое, и посты по темам конкретные. Усилю: пост про несогласие (\'дорого = качественно\') поставь на середину недели и заверши вопросом к аудитории — острый пост с вопросом даёт больше всего комментариев.'
  }
},
{
  title:'Реакция важнее нейтральности',
  sub:'3:10 · видео (голос Даниэля) + тест + игра', dur:'3:10',
  videoUrl:'https://okoteam.top/media/oko_m4_2.mp4',
  c1:'ЛЮБАЯ', c2:'РЕАКЦИЯ',
  slides:[
  {t:'Плохая реакция — это никакая реакция', pts:['Худшее, что может быть с постом — <b>тишина</b>', 'Негатив в комментариях лучше <b>полного молчания</b>', 'Молчание = вас не заметили, значит вас нет', 'Любая реакция — сигнал, что вы <b>задели живое</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30v12l14 3 30 14V13L34 27z"/><path d="M64 22c10 3 10 23 0 26" opacity=".7"/><path d="M40 48l4 12" /></svg>'},
  {t:'Почему нейтральность опаснее хейта', pts:['Нейтральный контент не сохраняют и не пересылают', 'Алгоритм читает молчание как <b>\'неинтересно\'</b> и режет охват', 'Хейт даёт <b>активность</b>, а активность даёт показы', 'Равнодушие — единственная реакция, из которой нет выхода'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Нельзя нравиться всем', pts:['Попытка угодить каждому = <b>беззубый серый контент</b>', 'Чёткая позиция всегда кого-то оттолкнёт — это норма', 'Отписки после острого поста — <b>чистка</b>, а не потеря', 'Остаются те, кому вы реально нужны'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h80L68 44v18l-16 8V44z"/></svg>'},
  {t:'Как работать с негативом правильно', pts:['Не удаляйте адекватную критику — <b>отвечайте по сути</b>', 'Спокойный ответ на хейт поднимает вас в глазах молчунов', 'Троллинг без смысла — <b>игнор или юмор</b>, не оправдания', 'Каждый комментарий — топливо для охвата, используйте его'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Провоцируйте реакцию осознанно', pts:['Заканчивайте пост <b>вопросом или выбором</b> для зрителя', 'Заявляйте непопулярное мнение, если готовы его защищать', 'Просите поделиться своим опытом в комментариях', '<b>Опросы и \'а вы как?\'</b> дешевле всего запускают реакцию'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Метрика, на которую смотреть', pts:['Не лайки, а <b>комментарии и сохранения</b>', 'Соотношение реакций к охвату важнее абсолютных цифр', 'Пост со спором часто <b>обгоняет</b> тёплый по охвату вдвое', 'Цель — вызвать действие, а не одобрительный кивок'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Что хуже всего для поста?', o:['Негативный комментарий', 'Полное молчание аудитории', 'Мало лайков', 'Один хейтер'], a:1},
  {q:'Почему нейтральность опаснее хейта?', o:['Нейтральный контент дороже продвигать', 'Молчание алгоритм читает как \'неинтересно\' и режет охват', 'Хейт запрещён правилами', 'Нейтральность портит фото'], a:1},
  {q:'Как относиться к отпискам после острого поста?', o:['Как к катастрофе, срочно всё удалить', 'Как к чистке — остаются те, кому вы нужны', 'Как к сбою алгоритма', 'Никак, отписки невозможны'], a:1},
  {q:'Как лучше реагировать на адекватную критику?', o:['Удалить комментарий', 'Оправдываться длинными постами', 'Спокойно ответить по сути', 'Заблокировать автора'], a:2},
  {q:'На какую метрику смотреть в первую очередь?', o:['Только лайки', 'Комментарии и сохранения', 'Число подписчиков', 'Количество хэштегов'], a:1}
  ],
  pairs:[
  ['Нейтральность', 'Реакция, из которой нет выхода к охвату'],
  ['Негатив', 'Активность, которую алгоритм превращает в показы'],
  ['Отписки', 'Чистка аудитории после чёткой позиции'],
  ['Вопрос в конце поста', 'Дешёвый способ запустить комментарии'],
  ['Молчание', 'Сигнал, что пост не задел никого']
  ],
  task:{
    intro:'Возьми свою последнюю \'спокойную\' тему и переделай её так, чтобы она провоцировала реакцию. Напиши новый заголовок и финальный вопрос к аудитории.',
    chips:['Непопулярное мнение', 'Развенчание мифа ниши', 'Выбор из двух вариантов', 'Просьба поделиться опытом', 'Спорное сравнение', 'Честное \'мне не нравится\''],
    ph:'Было: \'Как выбрать шрифт для логотипа\'. Стало: \'Почему модный минимализм в логотипах — это лень дизайнера, а не тренд\'. Финал: \'А вы за чистый минимализм или за характер? Пишите, поспорим\'.',
    verdict:'Сильно — из нейтральной инструкции ты сделал заявление с позицией и открытым спором в конце, это соберёт комментарии. Усилю: в тексте приведи один живой пример логотипа, который \'слишком минималистичный\' — конкретика превращает спор из абстрактного в горячий.'
  }
},
{
  title:'Приверженность аудитории',
  sub:'3:50 · видео (голос Даниэля) + тест + игра', dur:'3:50',
  videoUrl:'https://okoteam.top/media/oko_m4_3.mp4',
  c1:'ЧТОБЫ', c2:'СКУЧАЛИ',
  slides:[
  {t:'Приверженность — выше лояльности', pts:['Лояльный смотрит, <b>приверженный ждёт и скучает</b>', 'Это сильная <b>эмоциональная связь</b>, а не привычка листать', 'Приверженный покупает всё и рекомендует без просьбы', 'Такой подписчик приносит деньги <b>годами</b>, а не разово'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Чтобы по вам скучали', pts:['У вас должен быть <b>узнаваемый характер и голос</b>', 'Сквозные герои: команда, питомец, постоянная рубрика', 'Внутренние шутки и <b>свой язык</b>, понятный только своим', 'Пропали на неделю — по вам должны <b>спросить в директ</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Что строит эмоциональную связь', pts:['<b>Уязвимость</b>: делитесь сомнениями, а не только успехом', 'Обращение лично: \'вы\', имена, ответы в комментариях', 'Совместный путь: берите аудиторию <b>в свой процесс</b>', 'Держите обещания — регулярность рождает привязанность'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'Ритуалы, которые держат аудиторию', pts:['Постоянное время выхода — вас <b>начинают ждать</b>', 'Еженедельная рубрика превращается в <b>сериал</b>', 'Финальная фраза или приветствие как <b>подпись</b>', 'Ритуал = якорь, к которому возвращаются на автомате'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v16l11 7"/></svg>'},
  {t:'Ошибки, убивающие привязанность', pts:['Резкая смена стиля и тона — аудитория <b>теряет \'вас\'</b>', 'Исчезновение на месяцы обнуляет связь', 'Только продажи без личного — вас разлюбят быстро', 'Игнор комментариев читается как <b>\'мне всё равно\'</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Как измерить приверженность', pts:['Сообщения \'скучали\', \'где вы пропали\' — <b>прямой сигнал</b>', 'Возвраты одних и тех же имён в комментариях', '<b>Повторные покупки</b> и рекомендации друзьям', 'Люди защищают вас в спорах без вашего участия'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'}
  ],
  quiz:[
  {q:'Чем приверженность отличается от лояльности?', o:['Ничем, это синонимы', 'Лояльный смотрит, приверженный ждёт и скучает', 'Приверженность — это про рекламу', 'Лояльность сильнее приверженности'], a:1},
  {q:'Что строит эмоциональную связь с аудиторией?', o:['Только идеальные успешные кейсы', 'Уязвимость, личное обращение, совместный путь', 'Максимум продающих постов', 'Частая смена стиля'], a:1},
  {q:'Зачем нужны ритуалы (постоянное время, рубрики)?', o:['Чтобы заполнить ленту', 'Они становятся якорем, аудитория начинает ждать', 'Чтобы понравиться алгоритму', 'Ритуалы не нужны'], a:1},
  {q:'Что быстрее всего убивает привязанность?', o:['Ответы на комментарии', 'Регулярность постинга', 'Исчезновение на месяцы и только продажи', 'Личные истории'], a:2},
  {q:'Какой сигнал говорит о реальной приверженности?', o:['Много лайков от ботов', 'Сообщения \'скучали, где вы пропали\'', 'Рост числа хэштегов', 'Один вирусный ролик'], a:1}
  ],
  pairs:[
  ['Лояльность', 'Привычка смотреть контент'],
  ['Приверженность', 'Эмоциональная связь, по вам скучают'],
  ['Ритуал', 'Постоянное время или рубрика-якорь'],
  ['Уязвимость', 'Открытые сомнения, а не только успех'],
  ['Сквозной герой', 'Питомец или команда, которых узнают'],
  ['Игнор комментариев', 'Сигнал \'мне всё равно\', рушит связь']
  ],
  task:{
    intro:'Придумай один ритуал и одного сквозного героя для своего блога — то, по чему аудитория будет вас узнавать и ждать. Опиши, как это будет выглядеть еженедельно.',
    chips:['Постоянное время выхода', 'Еженедельная рубрика-сериал', 'Фирменное приветствие', 'Сквозной герой (питомец/команда)', 'Внутренняя шутка', 'Ответ каждому в комментариях'],
    ph:'Ритуал: каждую пятницу \'Разбор недели\' в 19:00 — показываю один заказ от заявки до готового результата. Сквозной герой: мой кот Бисквит, который \'проверяет\' каждый торт, аудитория уже спрашивает про него.',
    verdict:'Класс — \'Разбор недели\' в фиксированное время это готовый сериал, а кот Бисквит уже вызывает вопросы, значит связь строится. Усилю: заканчивай каждый пятничный разбор одним и тем же вопросом-ритуалом (\'какой заказ показать в следующий раз?\') — так аудитория почувствует, что решает вместе с тобой.'
  }
},
{
  title:'Рубрики и контент-план',
  sub:'3:30 · видео (голос Даниэля) + тест + игра', dur:'3:30',
  videoUrl:'https://okoteam.top/media/oko_m4_4.mp4',
  c1:'РУБРИКИ', c2:'И ПЛАН',
  slides:[
  {t:'Зачем вообще рубрики', pts:['Без системы вы каждый день думаете <b>\'а что снимать?\'</b>', '<b>Рубрики</b> — это готовые полки, куда раскладываются идеи', 'Аудитория запоминает форматы и <b>ждёт любимую рубрику</b>', 'Система снимает выгорание и убирает хаос'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Четыре типа рубрик', pts:['<b>Экспертные</b>: разборы, ошибки, инструкции — растят доверие', '<b>Личные</b>: истории, бэкстейдж, ценности — строят связь', '<b>Вовлекающие</b>: опросы, вопросы, споры — гонят реакцию', '<b>Продающие</b>: кейсы, отзывы, предложения — приносят деньги'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Баланс рубрик 40-30-20-10', pts:['40% <b>экспертное</b>, 30% <b>личное</b>, 20% вовлекающее, 10% продающее', 'Перекос в продажи — аудитория устаёт и уходит', 'Только эксперт без личного — <b>сухо и забываемо</b>', 'Баланс подстраивается под нишу, но структура остаётся'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Как собрать контент-план на месяц', pts:['Возьмите <b>сетку 4 недели × дни постинга</b>', 'Разложите рубрики так, чтобы эмоции чередовались', 'Оставьте <b>10-20% под ситуативку</b> и тренды', 'Планируйте темы, но снимайте живо, без зубрёжки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="16" width="76" height="46" rx="8"/><path d="M22 30h76M38 10v12M82 10v12"/><circle cx="44" cy="44" r="3" fill="currentColor" stroke="none"/><circle cx="60" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Где брать идеи для рубрик', pts:['Вопросы клиентов = <b>готовые темы экспертных постов</b>', 'Свои ошибки и провалы — материал для личных рубрик', 'Прогоните нишу через <b>ChatGPT</b> для мозгового штурма', 'Комментарии аудитории — банк тем на месяцы вперёд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'},
  {t:'План живёт, а не пылится', pts:['Раз в неделю сверяйтесь: <b>что зашло, что нет</b>', 'Рубрику с нулевой реакцией — <b>меняем или убираем</b>', 'Форматы-хиты масштабируйте, слабые режьте без жалости', 'План — инструмент, а не клетка'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Зачем нужны рубрики в блоге?', o:['Чтобы заполнить профиль', 'Готовые полки для идей, система против хаоса и выгорания', 'Чтобы понравиться рекламодателям', 'Рубрики не нужны, снимай что хочешь'], a:1},
  {q:'Какие четыре типа рубрик выделяют?', o:['Утренние, дневные, вечерние, ночные', 'Экспертные, личные, вовлекающие, продающие', 'Фото, видео, текст, сторис', 'Платные, бесплатные, партнёрские, свои'], a:1},
  {q:'Чем опасен перекос в продающие рубрики?', o:['Ничем, продажи это главное', 'Аудитория устаёт и уходит', 'Алгоритм банит за продажи', 'Растут расходы на рекламу'], a:1},
  {q:'Откуда брать темы для экспертных рубрик?', o:['Придумывать наугад', 'Из вопросов клиентов и комментариев аудитории', 'Копировать у конкурентов дословно', 'Только из платных курсов'], a:1},
  {q:'Что делать с рубрикой, которая не даёт реакции?', o:['Оставить как есть навсегда', 'Постить чаще', 'Поменять формат или убрать без жалости', 'Купить рекламу'], a:2}
  ],
  pairs:[
  ['Экспертная рубрика', 'Разборы и инструкции, растит доверие'],
  ['Личная рубрика', 'Истории и бэкстейдж, строит связь'],
  ['Вовлекающая рубрика', 'Опросы и споры, гонит реакцию'],
  ['Продающая рубрика', 'Кейсы и отзывы, приносит деньги'],
  ['Контент-план', 'Сетка недель, где чередуются рубрики и эмоции'],
  ['Ситуативка', '10-20% плана под тренды и момент']
  ],
  task:{
    intro:'Собери 4 рубрики для своего блога — по одной каждого типа (экспертная, личная, вовлекающая, продающая). Дай каждой название и опиши одним предложением.',
    chips:['Экспертная: разбор ошибки', 'Личная: история из практики', 'Вовлекающая: спор недели', 'Продающая: кейс клиента', 'Ситуативная: реакция на тренд', 'Ответы на вопросы'],
    ph:'Экспертная \'Разбор налёта\' — чиню чужие плохие макеты. Личная \'Из-за кулис\' — как проходит мой заказ. Вовлекающая \'Спор пятницы\' — острое мнение о нише. Продающая \'До/После\' — кейс с цифрами и отзывом.',
    verdict:'Отлично — все четыре типа закрыты, названия цепкие (\'Разбор налёта\', \'Спор пятницы\'), сразу видна система. Усилю: закрепи за каждой рубрикой день недели и держи баланс 40-30-20-10 — тогда план перестанет проваливаться в одни продажи и аудитория привыкнет к ритму.'
  }
},
{
  title:'Контент-план по нишам',
  sub:'4:00 · видео (голос Даниэля) + тест + игра', dur:'4:00',
  videoUrl:'https://okoteam.top/media/oko_m4_5.mp4',
  c1:'ПЛАН', c2:'ПОД НИШУ',
  slides:[
  {t:'Одна структура — разное наполнение', pts:['Каркас рубрик <b>универсален</b>, а темы всегда под нишу', 'Чужой контент-план копировать бессмысленно', 'Ниша диктует <b>боли, язык и форматы</b> аудитории', 'Задача — перевести общие рубрики на язык вашей ниши'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Кейс: кондитер', pts:['Экспертное: <b>\'почему трескается макарон\'</b>, разбор ошибок', 'Личное: ночная смена перед свадебным заказом', 'Вовлекающее: <b>\'какой торг собрать на 8 марта?\'</b> опрос', 'Продающее: до/после и отзыв невесты с фото торта', 'Боль ниши: страх, что \'дорого\' и \'не вкусно\''],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Кейс: фриланс-дизайнер', pts:['Экспертное: <b>\'как правk\'ить ТЗ, чтобы не переделывать 5 раз\'</b>', 'Личное: как ушёл из найма и первый клиент за 3000 руб', 'Вовлекающее: <b>\'этот логотип за 500 или за 50000?\'</b>', 'Продающее: кейс редизайна с ростом продаж клиента', 'Боль ниши: \'дизайнеров много, чем ты лучше\''],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M74 12 92 30 56 66l-18 4 4-18z"/><path d="M62 24 80 42" opacity=".7"/></svg>'},
  {t:'Как найти темы своей ниши', pts:['Соберите <b>частые вопросы</b> клиентов — это готовые посты', 'Проверьте спрос запросов через <b>Wordstat</b>', 'Изучите боли в комментариях у коллег по нише', 'Мозговой штурм тем прогоните через <b>ChatGPT</b> под свою нишу'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Язык ниши решает', pts:['Говорите <b>словами аудитории</b>, а не профессиональным сленгом', 'Кондитер: \'не течёт крем\', а не \'стабилизация ганаша\'', 'Дизайнер: \'не стыдно показать\', а не \'UX-консистентность\'', 'Попадание в язык = ощущение <b>\'это про меня\'</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Соберите свой план за час', pts:['Шаг 1: 4 рубрики под нишу. Шаг 2: по 4 темы в каждую', 'Шаг 3: разложите <b>16 тем на 4 недели</b>', 'Шаг 4: добавьте эмоции и ситуативку', 'Готово: <b>месяц контента</b> без ежедневной паники'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Что в контент-плане универсально, а что меняется?', o:['Меняется всё, включая структуру', 'Каркас рубрик универсален, темы всегда под нишу', 'Универсально всё, включая темы', 'Ничего нельзя переиспользовать'], a:1},
  {q:'Пример экспертной темы для кондитера?', o:['Как я провёл выходные', 'Почему трескается макарон и как это чинить', 'Отзыв клиента', 'Опрос про вкусы'], a:1},
  {q:'Каким инструментом проверить спрос на темы?', o:['Midjourney', 'Wordstat', 'ElevenLabs', 'Runway'], a:1},
  {q:'Почему важно говорить на языке ниши?', o:['Чтобы выглядеть умнее коллег', 'Чтобы у аудитории возникло ощущение \'это про меня\'', 'Чтобы обойти алгоритм', 'Язык не имеет значения'], a:1},
  {q:'Где взять готовые темы для экспертных постов?', o:['Из частых вопросов клиентов', 'Из чужих планов дословно', 'Придумать наугад', 'Только из платных баз'], a:0}
  ],
  pairs:[
  ['Каркас рубрик', 'Универсальная часть плана'],
  ['Темы', 'Часть плана, всегда под конкретную нишу'],
  ['Wordstat', 'Проверка спроса на темы по запросам'],
  ['Язык ниши', 'Слова аудитории вместо профсленга'],
  ['Вопросы клиентов', 'Банк готовых экспертных тем'],
  ['Боль ниши', 'Страх аудитории, вокруг которого строят контент']
  ],
  task:{
    intro:'Собери контент-план на неделю под СВОЮ нишу: 5 тем, каждая привязана к рубрике и к реальной боли твоей аудитории. Пиши языком клиента, а не сленгом.',
    chips:['Частый вопрос клиента', 'Реальная боль ниши', 'Опрос по вкусам аудитории', 'Кейс до/после', 'Личная история из практики', 'Проверка темы через Wordstat'],
    ph:'Ниша — маникюр. Пн (эксперт): \'почему покрытие скалывается через 3 дня\'. Вт (личное): мой путь от кухонного стола до кабинета. Ср (вовлечение): \'френч или тёмный тон на осень?\'. Чт (продажа): до/после запущенных ногтей. Пт: разбор вопроса \'не вредно ли для ногтей\'.',
    verdict:'Отлично — все темы бьют в реальные боли ниши (\'скалывается\', \'не вредно ли\'), это язык клиента, а не мастера. Усилю: тему про сколы проверь в Wordstat и вынеси точную формулировку запроса в заголовок — так пост подтянет ещё и поиск, а не только ленту.'
  }
},
{
  title:'Комьюнити «вы не одни»',
  sub:'3:45 · видео (голос Даниэля) + тест + игра', dur:'3:45',
  videoUrl:'https://okoteam.top/media/oko_m4_6.mp4',
  c1:'ВЫ', c2:'НЕ ОДНИ',
  slides:[
  {t:'Блог как место общения, а не витрина', pts:['Витрину листают, <b>в комьюнити возвращаются</b>', 'Люди остаются не ради контента, а ради <b>ощущения \'свои\'</b>', 'Главное чувство сильного блога — <b>\'вы не одни\'</b>', 'Комьюнити удерживает даже когда контент слабее обычного'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Почему \'вы не одни\' так работает', pts:['Люди ищут <b>принадлежность</b> сильнее, чем информацию', 'Общая боль объединяет: \'у меня тоже так\'', 'Аудитория начинает <b>общаться между собой</b>, не только с вами', 'Чувство племени = причина не отписываться никогда'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Как превратить подписчиков в комьюнити', pts:['Задавайте вопросы и <b>публикуйте ответы аудитории</b>', 'Знакомьте подписчиков друг с другом в комментариях', 'Вводите <b>общий язык и внутренние мемы</b>', 'Отмечайте активных, делайте их <b>соавторами</b> контента'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'Форматы, рождающие комьюнити', pts:['<b>Рубрика вопросов</b>: аудитория задаёт, вы разбираете', 'Челленджи и совместные марафоны с отчётами', 'Прямые эфиры — живое общение <b>в реальном времени</b>', 'Пользовательский контент: показывайте, что делают <b>ваши люди</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M52 24l20 11-20 11z" fill="currentColor" stroke="none"/></svg>'},
  {t:'Модерация: тепло, но с границами', pts:['Комьюнити нужны <b>правила</b>, иначе приходят токсики', 'Защищайте своих от хамства — это укрепляет доверие', 'Поощряйте поддержку, гасите травлю <b>сразу</b>', 'Ваш тон задаёт атмосферу: тепло притягивает тёплых'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Комьюнити = деньги и защита', pts:['Своя аудитория <b>покупает и рекомендует</b> без уговоров', 'Комьюнити <b>защищает вас</b> в спорах без вашего участия', 'Сарафан внутри племени дешевле любой рекламы', 'Сильное комьюнити переживает <b>смену площадок</b> с вами'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 50h80l-6-30-20 16-14-24-14 24-20-16z"/><path d="M20 56h80" opacity=".7"/></svg>'}
  ],
  quiz:[
  {q:'Чем блог-комьюнити отличается от блога-витрины?', o:['Ничем, это одно и то же', 'В комьюнити возвращаются ради ощущения \'свои\', витрину листают', 'Витрина приносит больше денег', 'Комьюнити не нужно вести'], a:1},
  {q:'Какое чувство лежит в основе сильного комьюнити?', o:['\'Я лучше всех\'', '\'Вы не одни\'', '\'Здесь дёшево\'', '\'Тут много рекламы\''], a:1},
  {q:'Что помогает превратить подписчиков в комьюнити?', o:['Игнорировать комментарии', 'Публиковать ответы аудитории и знакомить людей между собой', 'Постить только продающее', 'Запретить общение в комментариях'], a:1},
  {q:'Зачем комьюнити нужны правила и модерация?', o:['Чтобы отпугнуть аудиторию', 'Иначе приходят токсики и разрушают тёплую атмосферу', 'Правила не нужны', 'Для красоты профиля'], a:1},
  {q:'Какую пользу даёт сильное комьюнити бизнесу?', o:['Только лайки', 'Покупки, рекомендации и защиту вас в спорах', 'Ничего практического', 'Снижает качество контента'], a:1}
  ],
  pairs:[
  ['Витрина', 'Место, которое листают и уходят'],
  ['Комьюнити', 'Место, куда возвращаются ради \'своих\''],
  ['\'Вы не одни\'', 'Чувство принадлежности, удерживающее аудиторию'],
  ['Пользовательский контент', 'Работы аудитории, которые вы показываете'],
  ['Модерация', 'Правила и защита своих от токсиков'],
  ['Сарафан в племени', 'Рекомендации дешевле любой рекламы']
  ],
  task:{
    intro:'Придумай один формат, который превратит твоих подписчиков в комьюнити — где они общаются друг с другом, а не только с тобой. Опиши, как это запустишь.',
    chips:['Рубрика вопросов от аудитории', 'Совместный челлендж с отчётами', 'Прямой эфир-общение', 'Показ работ подписчиков', 'Внутренний мем или язык', 'Знакомство людей в комментариях'],
    ph:'Запущу челлендж \'Неделя без покупной еды\' для своей аудитории по домашней готовке: участники выкладывают свои блюда с моим хэштегом, я публикую лучшие в сторис, а под постом прошу их подсказывать рецепты друг другу.',
    verdict:'Отлично — челлендж с хэштегом и разбором работ включает людей в соавторы, а просьба подсказывать друг другу запускает общение между подписчиками, это ядро комьюнити. Усилю: в конце недели собери всех участников в один пост-\'доску почёта\' с именами — публичное признание превращает разовый челлендж в постоянное племя, которое будет ждать следующий.'
  }
}
  ],
  "m5": [
{
  title:'Прогрев через сторис',
  sub:'3:30 · видео (голос Даниэля) + тест + игра', dur:'3:30',
  videoUrl:'https://okoteam.top/media/oko_m5_0.mp4',
  c1:'СТОРИС', c2:'ПРОГРЕВ',
  slides:[
  {t:'Зачем вообще сторис', pts:['У сторис три работы: <b>вовлечь в пост</b>, показать событие жизни и подвести к продаже', 'Пост живёт в ленте днями, сторис — 24 часа, поэтому это твой <b>ежедневный контакт</b> с аудиторией', 'Человек покупает не в момент оффера, а после <b>7–15 касаний</b> — сторис и накручивают эти касания', 'Продажа без прогрева = холодный оффер незнакомцу: конверсия падает в разы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Драматургия прогрева на 5–7 дней', pts:['День 1–2: <b>боль и проблема</b> — показываешь, что аудитория узнаёт себя', 'День 3–4: <b>путь и инсайт</b> — как ты сам решил эту проблему, ошибки, поворот', 'День 5–6: <b>доказательства</b> — кейсы, отзывы, скрины результата', 'День 7: <b>оффер</b> — открываешь продажу, когда аудитория уже дозрела', 'Каждая история заканчивается вопросом «завтра покажу как» — это <b>клиффхэнгер</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Крючки, которые держат до конца', pts:['Первые 2 секунды решают всё: сразу <b>лицо или движение</b>, не заставка', 'Текст-<b>крючок</b> поверх кадра: «я потерял 40 тысяч на этом» цепляет сильнее «привет всем»', 'Открытая петля: обещай ответ в конце цепочки, а не в этой сторис', 'Меняй формат каждые 2–3 сторис: селфи-видео, скрин переписки, опрос, кружок'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Показывай жизнь, а не только продукт', pts:['Аудитория покупает у <b>человека</b>, а не у логотипа: завтрак, дорога, рабочий стол — это доверие', 'Правило чередования: на 4 личные/полезные сторис — 1 продающая', 'Событие жизни = повод: «сегодня клиент написал спасибо» органично ведёт к кейсу', 'Рутина, поданная искренне, работает как <b>сериал</b> — за тобой начинают следить'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Призыв и типичные ошибки', pts:['Один <b>CTA</b> на цепочку: «пиши ПЛЮС в директ» — не размазывай пять разных действий', 'Ошибка №1: продавать в лоб на холодную, без дней прогрева', 'Ошибка №2: <b>каша из тем</b> — прогрев к одному продукту не мешают с двумя другими', 'Ошибка №3: нет реакции на ответы в директ — прогрев без диалога мёртв'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30v12l14 3 30 14V13L34 27z"/><path d="M64 22c10 3 10 23 0 26" opacity=".7"/><path d="M40 48l4 12" /></svg>'}
  ],
  quiz:[
  {q:'Сколько касаний в среднем нужно человеку до покупки?', o:['1–2', '3–4', '7–15', '50+'], a:2},
  {q:'Что должно быть в первые 2 секунды сторис?', o:['Красивая заставка с логотипом', 'Лицо или движение', 'Длинный текст-описание', 'Музыка без картинки'], a:1},
  {q:'Оптимальная последовательность прогрева на неделю:', o:['Оффер сразу в день 1', 'Боль → путь → доказательства → оффер', 'Только кейсы всю неделю', 'Случайные темы каждый день'], a:1},
  {q:'Правильное соотношение личных и продающих сторис:', o:['50/50', 'Примерно 4 личных на 1 продающую', 'Только продающие', 'Только личные, без продаж'], a:1},
  {q:'Что такое клиффхэнгер в цепочке сторис?', o:['Скидка на продукт', 'Обещание показать ответ завтра/в конце', 'Опрос с двумя кнопками', 'Прямая ссылка на оплату'], a:1}
  ],
  pairs:[
  ['Прогрев', 'Серия касаний, доводящая до готовности купить'],
  ['Крючок', 'Фраза или кадр, удерживающий с первых секунд'],
  ['Клиффхэнгер', 'Открытая петля: ответ обещан позже'],
  ['Событие жизни', 'Бытовой повод, дающий доверие и вход в тему'],
  ['CTA', 'Одно чёткое действие для зрителя']
  ],
  task:{
    intro:'Распиши цепочку прогрева на 5 дней для одного своего продукта: тема каждого дня и крючок первой сторис.',
    chips:['День 1: боль', 'День 2: моя история', 'Кейс с цифрой', 'Открытая петля', 'Один CTA', 'Личная сторис для доверия'],
    ph:'День1 боль: «85% моих клиентов сливали бюджет на таргет без оффера». День2 путь: как я сам потерял 40к и что понял. День3 кейс: скрин +230 заявок за неделю. День4 отзыв голосом клиента. День5 оффер: «пиши РАЗБОР в директ». Крючок дня1: крупный план лица + текст «это стоило мне 40 000».',
    verdict:'Сильно: у тебя есть конкретная цифра (40к) и живой кейс со скрином — это цепляет лучше абстракций. Усилю одним: в день 4 добавь мини-возражение («думаешь, у тебя ниша другая?») и закрой его — так оффер дня 5 встретит меньше сопротивления.'
  }
},
{
  title:'Эфиры (Live)',
  sub:'3:50 · видео (голос Даниэля) + тест + игра', dur:'3:50',
  videoUrl:'https://okoteam.top/media/oko_m5_1.mp4',
  c1:'ЖИВОЙ', c2:'ЭФИР',
  slides:[
  {t:'Почему эфир — сильнейший инструмент', pts:['<b>Живая обратная связь</b> в реальном времени: ты видишь вопросы и сразу закрываешь возражения', 'Эфир поднимает <b>доверие</b> быстрее любого поста — тебя нельзя смонтировать, ты настоящий', 'Алгоритмы подтягивают Live в рекомендации: платформа любит <b>долгое удержание</b>', 'Один эфир = прогрев, продажа и контент-заготовки на нарезку одновременно'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M52 24l20 11-20 11z" fill="currentColor" stroke="none"/></svg>'},
  {t:'Подготовка: без плана эфир разваливается', pts:['Заранее собери <b>3–5 болей</b> аудитории — это скелет эфира, а не импровизация с нуля', 'Анонс за 24 часа и напоминание за час: <b>явка</b> зависит от анонса больше, чем от темы', 'Один эфир — <b>одна тема</b> и одно целевое действие в финале', 'Проверь свет, звук и заряд: технический сбой убивает динамику на старте'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Структура эфира на 30–40 минут', pts:['Первые 3 минуты — <b>разгон</b>: ждём людей, но сразу даём мысль, чтобы не уходили', 'Основной блок: <b>польза через истории</b>, а не сухая теория', 'Середина — <b>интерактив</b>: вопрос в чат, поднимаем вовлечённость', 'Финал — <b>оффер</b> и чёткий призыв: «ссылка в закреплённом на 15 минут»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Работа с чатом вживую', pts:['Называй людей по имени: «Марина спрашивает…» — это <b>вовлечение</b> и лояльность', 'Собирай <b>вопросы</b> в начале, отвечай блоками — иначе скачешь и теряешь нить', 'Негатив и тролли — не игнор и не спор: короткий ответ и дальше по плану', 'Каждый ответ разворачивай в <b>пользу для всех</b>, а не только для спросившего'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Что делать после эфира', pts:['Нарежь на <b>Reels/Shorts</b>: сильные моменты живут второй жизнью неделями', 'Догрев в сторис для тех, кто не был: «вот что ты пропустил»', 'Собери вопросы из чата — это <b>готовые темы</b> для следующего контента', 'Посчитай метрику: пик зрителей, удержание, заявки — <b>анализ</b> растит следующий эфир'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'}
  ],
  quiz:[
  {q:'Главное преимущество Live перед записанным контентом:', o:['Его можно смонтировать', 'Живая обратная связь в реальном времени', 'Он короче', 'Его не видит алгоритм'], a:1},
  {q:'От чего сильнее всего зависит явка на эфир?', o:['От темы', 'От качества анонса и напоминаний', 'От длины эфира', 'От времени суток'], a:1},
  {q:'Сколько тем стоит брать на один эфир?', o:['Как можно больше', 'Одну тему и одно целевое действие', 'Пять-шесть тем', 'Ноль, всё импровизация'], a:1},
  {q:'Как правильно работать с чатом на эфире?', o:['Игнорировать вопросы', 'Называть людей по имени и отвечать блоками', 'Спорить с троллями подробно', 'Отвечать только личными сообщениями'], a:1},
  {q:'Что делать с эфиром после его окончания?', o:['Удалить, чтобы не занимал место', 'Нарезать на Reels и собрать вопросы в темы', 'Ничего', 'Выложить целиком без изменений и забыть'], a:1}
  ],
  pairs:[
  ['Live', 'Прямой эфир с обратной связью в реальном времени'],
  ['Анонс', 'Прогрев за 24 часа, поднимающий явку'],
  ['Разгон', 'Первые минуты эфира, пока собираются люди'],
  ['Нарезка', 'Reels из сильных моментов эфира'],
  ['Целевое действие', 'Одно чёткое, что зритель делает в финале']
  ],
  task:{
    intro:'Составь план эфира на 30 минут: тема, 3 боли-блока, момент интерактива и оффер в финале.',
    chips:['Одна тема', '3 боли аудитории', 'История вместо теории', 'Вопрос в чат', 'Оффер + дедлайн', 'Анонс за сутки'],
    ph:'Тема: «3 ошибки, из-за которых сторис не продают». Боль1: продают в лоб на холодную. Боль2: каша из тем. Боль3: нет CTA. Интерактив на 15-й минуте: «напишите в чат, какая ошибка ваша». Финал: разбор цепочки в моём мини-курсе, ссылка в закрепе на 15 минут.',
    verdict:'Хорошо: тема узкая и конкретная, три боли дают чёткий скелет — эфир не расползётся. Усилю: перед оффером вставь один живой кейс ученика с цифрой, чтобы переход к продаже опирался на доказательство, а не на голое обещание.'
  }
},
{
  title:'Удержание на эфире',
  sub:'3:40 · видео (голос Даниэля) + тест + игра', dur:'3:40',
  videoUrl:'https://okoteam.top/media/oko_m5_2.mp4',
  c1:'УДЕРЖАНИЕ', c2:'ВНИМАНИЯ',
  slides:[
  {t:'Почему люди уходят с эфира', pts:['Монотонность: один <b>ракурс</b>, один тон, ноль движения — мозг устаёт за 90 секунд', 'Нет причины остаться: не пообещал <b>ценность впереди</b> — человек закрывает', 'Долгое вступление: 5 минут «привет, ждём всех» — и половина уже ушла', 'Провалы динамики: пауза, тишина, возня с телефоном рвут <b>удержание</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Смена ракурса и картинки', pts:['Меняй <b>план</b>: сел, встал, подошёл к доске, показал экран — картинка не должна застывать', 'Демонстрируй руками, предметами, скринами — <b>визуал</b> держит сильнее слов', 'Каждые 3–5 минут — новый визуальный якорь, чтобы <b>сбросить усталость</b> глаза', 'Даже смена фона или переход к окну — микрособытие, которое будит внимание'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="18" width="54" height="38" rx="7"/><path d="M74 30l24-12v38L74 42z"/><circle cx="40" cy="37" r="9"/></svg>'},
  {t:'Смена тона и темпа речи', pts:['Говори не ровной линией, а <b>волной</b>: тише-громче, быстрее-медленнее', 'Перед важной мыслью — <b>пауза</b>: тишина заставляет вслушаться', 'Эмоция в голосе заразительна: скучно тебе — скучно всем', 'Ключевую фразу произнеси дважды и <b>с нажимом</b> — так она запоминается'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="50" y="8" width="20" height="34" rx="10"/><path d="M40 36a20 20 0 0 0 40 0M60 56v8M48 64h24"/></svg>'},
  {t:'Крючки удержания по ходу эфира', pts:['«Через 10 минут покажу схему, которая приносит заявки» — <b>обещание впереди</b>', '«Досмотри до конца — там подарок» — <b>якорь на финал</b> держит до оффера', 'Мини-интриги внутри блоков: «а вот тут почти все ошибаются…»', 'Возвращай ушедших: «кто зашёл — быстро повторю главное», не бросай новичков'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Интерактив как удержание', pts:['Вопрос в чат каждые несколько минут: отвечающий <b>остаётся вовлечённым</b>', '«Поставьте плюс, если узнали себя» — простое действие возвращает фокус', 'Голосование прямо в эфире держит людей ради <b>результата</b>', 'Реагируй на ответы вслух — человек видит, что его <b>заметили</b>, и не уходит'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'}
  ],
  quiz:[
  {q:'Главная причина, по которой люди уходят с эфира:', o:['Слишком много интерактива', 'Монотонность и отсутствие движения', 'Слишком короткое вступление', 'Хорошее качество звука'], a:1},
  {q:'Как работать с картинкой, чтобы удержать зрителя?', o:['Сидеть неподвижно весь эфир', 'Менять план, показывать предметы и экран', 'Выключить видео и оставить звук', 'Держать один крупный план'], a:1},
  {q:'Зачем нужна пауза перед важной мыслью?', o:['Чтобы попить воды', 'Тишина заставляет вслушаться', 'Чтобы эфир был длиннее', 'Это ошибка, пауз надо избегать'], a:1},
  {q:'Что такое якорь на финал?', o:['Скидка в начале', 'Обещание ценности в конце, удерживающее зрителя', 'Длинное вступление', 'Ссылка на другой эфир'], a:1},
  {q:'Как интерактив помогает удержанию?', o:['Отвлекает и мешает', 'Отвечающий вовлекается и остаётся', 'Замедляет эфир без пользы', 'Работает только в записи'], a:1}
  ],
  pairs:[
  ['Смена ракурса', 'Новый визуальный план против монотонности'],
  ['Волна речи', 'Игра громкостью и темпом голоса'],
  ['Якорь на финал', 'Обещание подарка/ценности в конце'],
  ['Пауза', 'Тишина, усиливающая ключевую мысль'],
  ['Мини-интрига', 'Крючок «тут почти все ошибаются»']
  ],
  task:{
    intro:'Возьми свой эфир на 30 минут и расставь по таймлайну 4 приёма удержания: где смена ракурса, где крючок впереди, где интерактив, где якорь на финал.',
    chips:['Смена плана на 5 мин', 'Крючок «через 10 минут схема»', 'Вопрос в чат', 'Пауза перед выводом', 'Якорь: подарок в конце', 'Мини-интрига'],
    ph:'0:00 старт, сразу мысль. 5:00 встаю к доске (смена ракурса). 7:00 крючок: «в конце дам чек-лист». 12:00 вопрос в чат «ваша ошибка?». 18:00 показ экрана со схемой. 24:00 пауза перед главным выводом. 29:00 якорь-финал: чек-лист по кодовому слову.',
    verdict:'Отлично: у тебя приёмы разведены по времени, а не свалены в кучу — внимание не провиснет. Усилю: между 12-й и 18-й минутой добавь смену тона (перейди на тише-медленнее перед схемой) — контраст темпа сделает ключевой блок ещё заметнее.'
  }
},
{
  title:'Опросы и интерактив',
  sub:'3:20 · видео (голос Даниэля) + тест + игра', dur:'3:20',
  videoUrl:'https://okoteam.top/media/oko_m5_3.mp4',
  c1:'ОПРОС', c2:'ВОВЛЕКАЕТ',
  slides:[
  {t:'Опрос — это разведка боем', pts:['Опрос выявляет <b>реальную боль</b>, которую ты потом закрываешь продуктом', 'Не гадай, что нужно аудитории — <b>спроси</b> и получи готовый оффер их словами', 'Ответы = сегментация: видишь, кто на какой стадии и что предлагать', 'Механика вовлечения: тапнуть кнопку легче, чем написать — <b>охваты растут</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Виды интерактивов в сторис', pts:['<b>Опрос из двух кнопок</b> — быстрый выбор, максимум ответов', '<b>Шкала-ползунок</b> — эмоция и градус («насколько устал от…»)', '<b>Вопрос-бокс</b> — собираешь боли текстом, темы для контента', '<b>Квиз с вариантами</b> — вовлекает через игру и лёгкую конкуренцию', '<b>Обратный отсчёт</b> — прогрев к эфиру или старту продаж'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Как задавать вопрос, чтобы отвечали', pts:['Спрашивай про <b>боль</b>, а не про товар: «что бесит в ведении блога?» вместо «купишь курс?»', 'Формулировка бинарно и близко: «узнал себя? да / нет» — <b>низкий порог</b>', 'Один вопрос за раз: два вопроса в одной сторис = меньше ответов на каждый', 'Дай <b>повод</b>: «отвечу каждому лично» повышает отклик в разы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Развлечение как упаковка пользы', pts:['Интерактив в <b>игровой форме</b> заходит легче, чем прямой вопрос-допрос', '«Угадай результат», «выбери вариант» — азарт держит и возвращает', 'Мемы, челленджи, «этот или тот» — вовлечение без ощущения продажи', 'Правило: сначала <b>развлёк и вовлёк</b>, потом продал — не наоборот'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="28" width="68" height="34" rx="6"/><path d="M26 38h68M60 28v34"/><path d="M60 28c-6-14-22-10-16-2 3 4 16 2 16 2zM60 28c6-14 22-10 16-2-3 4-16 2-16 2z"/></svg>'},
  {t:'Из ответов — в продукт и контент', pts:['Собери частые боли из вопрос-бокса — это <b>карта продукта</b> и линейка офферов', 'Ответ большинства = следующая тема поста или эфира, ты бьёшь в точку', 'Покажи <b>статистику опроса</b> подписчикам: «70% из вас ответили…» — это соцдоказательство', 'Цитируй ответы (с согласия) — люди любят <b>видеть себя</b> в твоём контенте'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'}
  ],
  quiz:[
  {q:'Главная стратегическая польза опроса:', o:['Заполнить пустую сторис', 'Выявить боль, которую закроешь продуктом', 'Увеличить длину блога', 'Показать красивую графику'], a:1},
  {q:'Как лучше формулировать вопрос для отклика?', o:['Сразу «купишь курс?»', 'Про боль аудитории с низким порогом ответа', 'Максимально сложно и подробно', 'Два вопроса в одной сторис'], a:1},
  {q:'Зачем показывать статистику опроса подписчикам?', o:['Чтобы заполнить время', 'Это соцдоказательство «70% ответили так»', 'Чтобы скрыть результат', 'Это запрещено платформой'], a:1},
  {q:'Почему интерактив подают в игровой форме?', o:['Так дороже', 'Развлечение вовлекает легче прямого допроса', 'Игры не работают в сторис', 'Чтобы отпугнуть аудиторию'], a:1},
  {q:'Что делать с ответами из вопрос-бокса?', o:['Удалять', 'Превращать в темы контента и линейку офферов', 'Игнорировать', 'Отвечать только себе'], a:1}
  ],
  pairs:[
  ['Опрос', 'Разведка боли аудитории двумя кнопками'],
  ['Вопрос-бокс', 'Сбор болей текстом для тем контента'],
  ['Шкала-ползунок', 'Замер градуса эмоции по теме'],
  ['Квиз', 'Игровой интерактив с вариантами ответа'],
  ['Статистика опроса', 'Соцдоказательство из ответов аудитории']
  ],
  task:{
    intro:'Придумай серию из 3 интерактивов на один день, которая выведет на боль и подведёт к твоему продукту.',
    chips:['Опрос да/нет', 'Ползунок эмоции', 'Вопрос-бокс о боли', 'Игровой квиз', 'Показать статистику', 'Повод: отвечу лично'],
    ph:'Сторис1 опрос: «Твои сторис продают? да/нет». Сторис2 ползунок: «Насколько бесит вести блог каждый день?». Сторис3 вопрос-бокс: «Что мешает выкладывать регулярно?». На след. день показываю статистику «81% ответили нет» и подвожу к разбору контент-плана.',
    verdict:'Сильно: воронка от лёгкой кнопки к открытому вопросу — порог растёт постепенно, ответов будет много. Усилю: во втором шаге добавь повод «топ-3 ответа разберу в сторис завтра» — люди отвечают охотнее, когда видят, что их услышат публично.'
  }
},
{
  title:'Архетипы бренда — разбор',
  sub:'4:00 · слайды + тест + игра', dur:'4:00',
  c1:'АРХЕТИП', c2:'БРЕНДА',
  slides:[
  {t:'Характер бренда = живой человек', pts:['С брендом общаются как с <b>человеком</b>: у него есть тон, ценности, манера', '<b>Архетип</b> — это ядро характера, оно определяет, как ты говоришь и что обещаешь', 'Без характера бренд безликий: люди не привязываются к <b>табличке</b>', 'Один и тот же продукт с разным характером цепляет <b>разную аудиторию</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Стабильность и контроль', pts:['Обещание: <b>надёжность, порядок, безопасность</b> — «со мной спокойно»', 'Тон: уверенный, чёткий, без хаоса и кривляний', 'Кому подходит: финансы, консалтинг, всё, где важна <b>предсказуемость</b>', 'Триггеры: гарантии, системность, «пошаговый план», цифры и факты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Свобода', pts:['Обещание: <b>независимость, движение, свой путь</b> — «делай по-своему»', 'Тон: лёгкий, вдохновляющий, антирамочный', 'Кому подходит: тревел, лайфстайл, инфобизнес про <b>свободу выбора</b>', 'Триггеры: приключение, «уйти из найма», нестандартность, простор'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Вызов и трансформация', pts:['Обещание: <b>рост, преодоление, новая версия себя</b> — «стань сильнее»', 'Тон: дерзкий, мотивирующий, иногда провокационный', 'Кому подходит: спорт, обучение, коучинг, всё про <b>результат и прокачку</b>', 'Триггеры: «до/после», челлендж, «выйди из зоны комфорта», рекорды'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c4 12-6 16-6 26 0-6-5-9-5-9-4 6-11 11-11 22a22 22 0 0 0 44 0c0-14-12-20-16-39z"/></svg>'},
  {t:'Радость и тепло', pts:['Обещание: <b>уют, забота, лёгкость</b> — «с тобой хорошо и по-доброму»', 'Тон: тёплый, дружеский, живой, с юмором', 'Кому подходит: семейные ниши, еда, хобби, <b>комьюнити</b>', 'Триггеры: искренность, забота, «мы вместе», простые радости'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Как выбрать свой архетип', pts:['Отталкивайся от <b>ценностей аудитории</b>, а не от того, что модно', 'Проверь на себе: сможешь ли ты <b>жить в этом тоне</b> каждый день без фальши', 'Один ведущий архетип задаёт 70% голоса, остальное — оттенки', 'Характер должен совпасть с <b>продуктом</b>: свобода в банке звучит фальшиво'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'}
  ],
  quiz:[
  {q:'Что такое характер бренда простыми словами?', o:['Логотип и цвета', 'Восприятие бренда как живого человека с тоном и ценностями', 'Количество подписчиков', 'Название компании'], a:1},
  {q:'Какой архетип обещает надёжность, порядок и безопасность?', o:['Свобода', 'Стабильность и контроль', 'Вызов', 'Радость'], a:1},
  {q:'Триггеры «до/после», челлендж, рекорды — это архетип:', o:['Радость и тепло', 'Стабильность', 'Вызов и трансформация', 'Свобода'], a:2},
  {q:'От чего отталкиваться при выборе архетипа?', o:['От моды', 'От ценностей аудитории и совпадения с продуктом', 'От архетипа конкурента', 'От случайного выбора'], a:1},
  {q:'Почему «свобода» плохо звучит для банка?', o:['Свобода всем нравится', 'Характер должен совпадать с продуктом, иначе фальшь', 'Банкам нельзя иметь характер', 'Это единственно верный архетип'], a:1}
  ],
  pairs:[
  ['Стабильность', 'Надёжность, порядок, «со мной спокойно»'],
  ['Свобода', 'Независимость, свой путь, антирамочность'],
  ['Вызов', 'Рост, преодоление, новая версия себя'],
  ['Радость', 'Уют, забота, тепло и юмор'],
  ['Архетип', 'Ядро характера, задающее тон и обещание']
  ],
  task:{
    intro:'Опиши свой бренд через ведущий архетип: какое обещание, какой тон и 2 триггера, которые ты будешь использовать в контенте.',
    chips:['Стабильность', 'Свобода', 'Вызов', 'Радость', 'Обещание бренда', 'Два триггера'],
    ph:'Ведущий архетип: Вызов и трансформация. Обещание: «за 90 дней ты станешь новой версией себя в блоге». Тон: дерзкий, мотивирующий, без нытья. Триггеры: формат «до/после» учеников и еженедельный челлендж с публичным результатом.',
    verdict:'Хорошо: архетип, обещание и триггеры у тебя не противоречат друг другу — характер читается цельно. Усилю: пропиши одну фразу-манифест в этом тоне (например «комфорт не строит блоги»), закрепи её в шапке — так архетип станет узнаваемым с первого касания.'
  }
},
{
  title:'Смесь характеров (уникальность)',
  sub:'3:40 · слайды + тест + игра', dur:'3:40',
  c1:'СМЕСЬ', c2:'ХАРАКТЕРОВ',
  slides:[
  {t:'Чистых характеров почти не бывает', pts:['На практике бренд — это <b>смесь архетипов</b>, а не один в чистом виде', 'Именно смесь делает тебя <b>непохожим</b>: чистый архетип легко скопировать', 'Обычно есть <b>ведущий</b> характер (60–70%) и один-два оттенка', 'Смесь = твой отпечаток: конкуренту трудно повторить сочетание'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M28 24h20a6 6 0 0 1 12 0h20v20a6 6 0 0 1 0 12v8H60a6 6 0 0 0-12 0H28V44a6 6 0 0 0 0-12z"/></svg>'},
  {t:'Как рождается уникальность', pts:['<b>Вызов + тепло</b>: жёстко про результат, но по-доброму и с поддержкой', '<b>Стабильность + свобода</b>: системность без духоты, порядок ради свободы', '<b>Свобода + вызов</b>: свой путь, но через преодоление и рост', 'Неожиданное сочетание запоминается сильнее <b>предсказуемого</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Ведущий и оттенок', pts:['Один архетип — <b>основа голоса</b>, он звучит в 70% контента', 'Оттенок включается точечно: в кейсах, в юморе, в подаче', 'Не мешай поровну: <b>каша из равных долей</b> размывает характер', 'Проверка: убери оттенок — бренд ещё узнаваем? Значит основа выбрана верно'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Опасность несовместимых смесей', pts:['Не всё сочетается: <b>жёсткий контроль + анархичная свобода</b> путают аудиторию', 'Если тон скачет от заботы к агрессии без логики — теряется <b>доверие</b>', 'Смесь работает, когда у неё есть <b>общий смысл</b>, а не случайный набор', 'Тест на человека: живой человек может быть таким? Значит смесь жизнеспособна'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Как закрепить свой микс', pts:['Пропиши <b>формулу</b>: «ведущий + оттенок = обещание» одной строкой', 'Собери <b>3 фразы</b> в этом миксе — эталон, по которому пишешь всё остальное', 'Держи <b>единый тон</b> во всех точках: сторис, эфиры, ответы в директ', 'Раз в месяц сверяйся: контент не сполз к чистому шаблонному архетипу?'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="35" r="14"/><path d="M48 35h44M78 35v12M92 35v10"/></svg>'}
  ],
  quiz:[
  {q:'Почему смесь архетипов делает бренд уникальным?', o:['Она дешевле', 'Чистый архетип легко скопировать, сочетание — трудно', 'Смесь никто не замечает', 'Так требует алгоритм'], a:1},
  {q:'Как правильно распределять характеры в смеси?', o:['Поровну между всеми', 'Ведущий 60–70% плюс один-два оттенка', 'Только оттенки без основы', 'Каждый пост новый архетип'], a:1},
  {q:'В чём опасность несовместимой смеси?', o:['Она слишком запоминается', 'Скачки тона без логики рушат доверие', 'Её невозможно создать', 'Она всегда лучше чистого архетипа'], a:1},
  {q:'Хороший тест на жизнеспособность смеси:', o:['Нравится ли она конкуренту', 'Может ли такой быть живой человек', 'Дорого ли она стоит', 'Есть ли она у лидера ниши'], a:1},
  {q:'Что помогает закрепить свой микс характеров?', o:['Менять тон каждую неделю', 'Формула «ведущий + оттенок» и 3 эталонные фразы', 'Копировать чужой голос', 'Убрать характер совсем'], a:1}
  ],
  pairs:[
  ['Ведущий архетип', 'Основа голоса, 60–70% контента'],
  ['Оттенок', 'Дополнительный характер, включается точечно'],
  ['Смесь', 'Сочетание архетипов, дающее уникальность'],
  ['Каша', 'Равные доли, размывающие характер'],
  ['Тест на человека', 'Проверка: возможен ли такой живой человек']
  ],
  task:{
    intro:'Собери формулу своего бренда из двух характеров: ведущий + оттенок, и объясни, какое обещание рождается на их стыке.',
    chips:['Ведущий 70%', 'Оттенок 30%', 'Обещание на стыке', 'Эталонная фраза', 'Где включается оттенок', 'Проверка на человека'],
    ph:'Формула: Вызов (ведущий) + Тепло (оттенок) = «жёстко про результат, но рядом и по-человечески». Оттенок тепла включаю в ответах на возражения и в кейсах. Эталонная фраза: «буду гонять, но не брошу». Живой человек такой? Да — строгий, но заботливый тренер.',
    verdict:'Отлично: стык «вызов + тепло» осмыслен, а не случаен — обещание держится на противоречии, которое читается по-человечески. Усилю: закрепи, где оттенок НЕ включается (например в анонсах челленджей звучит только вызов) — так основа не расплывётся и микс останется управляемым.'
  }
},
{
  title:'ДЗ: характер твоего бренда',
  sub:'3:00 · слайды + тест + игра', dur:'3:00',
  c1:'ТВОЙ', c2:'ХАРАКТЕР',
  slides:[
  {t:'Задача этого урока', pts:['Финал блока: ты <b>определяешь характер</b> своего бренда по таблице архетипов', 'Не в теории, а в готовой <b>формуле</b>, по которой будешь писать контент', 'Результат — <b>паспорт характера</b>: ведущий, оттенок, тон, триггеры', 'Это фундамент: сторис, эфиры и опросы теперь звучат в <b>одном голосе</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M36 8v54"/><path d="M36 12h44l-8 12 8 12H36z"/></svg>'},
  {t:'Шаг 1 — пройди по таблице', pts:['Выпиши 4 архетипа: <b>стабильность, свобода, вызов, радость</b>', 'Оцени каждый по 10-балльной шкале: насколько это про <b>тебя и аудиторию</b>', 'Самый высокий балл — твой <b>ведущий</b>, второй по силе — оттенок', 'Отбрось то, что набрало мало: не пытайся быть <b>всем сразу</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Шаг 2 — собери паспорт', pts:['<b>Ведущий + оттенок</b>: два слова, задающие ядро', '<b>Обещание</b>: одна фраза, что бренд даёт человеку', '<b>Тон</b>: 3–4 прилагательных, как ты звучишь', '<b>Триггеры</b>: 3 приёма, которые будешь применять в контенте'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Шаг 3 — проверь на прочность', pts:['<b>Тест на человека</b>: живой человек может быть таким? Если нет — переделай', 'Совпадает ли характер с <b>продуктом</b> и с тем, как ты реально говоришь', 'Напиши <b>3 сторис</b> в этом голосе — звучит цельно или фальшиво?', 'Покажи знакомому: угадает <b>настроение бренда</b> без подсказок?'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Что дальше', pts:['Закрепи паспорт в <b>заметке</b> и сверяйся с ним перед каждым постом', 'Прогрев, эфиры, опросы из этого блока — теперь <b>в едином характере</b>', 'Через месяц пересмотри: <b>развился</b> ли бренд, не сполз ли в шаблон', 'Характер — не разовая настройка, а <b>живая система</b>, которую ведёшь'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Что должно получиться в итоге домашнего задания?', o:['Красивый логотип', 'Паспорт характера: ведущий, оттенок, тон, триггеры', 'Контент-план на год', 'Список конкурентов'], a:1},
  {q:'Как определить ведущий архетип по таблице?', o:['Выбрать модный', 'Оценить 4 архетипа по шкале и взять самый высокий балл', 'Взять все четыре', 'Спросить у конкурента'], a:1},
  {q:'Что входит в паспорт характера?', o:['Только цвета бренда', 'Ведущий+оттенок, обещание, тон, триггеры', 'Число подписчиков', 'Цена продукта'], a:1},
  {q:'Главная проверка готового характера:', o:['Дорого ли он стоит', 'Тест на человека и совпадение с продуктом', 'Есть ли он у лидера ниши', 'Нравится ли алгоритму'], a:1},
  {q:'Что делать с паспортом характера после ДЗ?', o:['Забыть', 'Сверяться перед каждым постом и раз в месяц пересматривать', 'Показать один раз', 'Менять ежедневно'], a:1}
  ],
  pairs:[
  ['Паспорт характера', 'Ведущий, оттенок, обещание, тон, триггеры'],
  ['Ведущий архетип', 'Набравший высший балл по таблице'],
  ['Оттенок', 'Второй по силе архетип'],
  ['Тест на человека', 'Проверка живости характера'],
  ['Живая система', 'Характер, который ведут и пересматривают']
  ],
  task:{
    intro:'Составь паспорт характера своего бренда: ведущий + оттенок, обещание в одну фразу, тон в 3 слова и 3 триггера. Проверь тестом на человека.',
    chips:['Ведущий архетип', 'Оттенок', 'Обещание одной фразой', 'Тон в 3 слова', '3 триггера', 'Тест на человека'],
    ph:'Ведущий: Вызов. Оттенок: Свобода. Обещание: «вырасту твой блог до дохода, не убивая твою свободу». Тон: дерзкий, честный, лёгкий. Триггеры: формат «до/после», челлендж недели, разбор «уйти из найма через контент». Тест на человека: да — бунтарь-наставник, реалистично.',
    verdict:'Сильно: паспорт цельный, обещание держит стык вызова и свободы, а тест на человека ты прошёл осознанно. Усилю: добавь одну антиценность — чего бренд НЕ делает (например «не продаю через страх и нытьё») — это заострит характер сильнее, чем ещё один триггер, и сразу отсечёт чужую аудиторию.'
  }
}
  ],
  "k1": [
{
  title:'Пиши как говоришь',
  sub:'3:24 · слайды + тест + игра', dur:'3:24',
  c1:'ПИШИ КАК', c2:'ГОВОРИШЬ',
  slides:[
  {t:'Текст — это молчаливая речь', pts:['Хороший текст — <b>аналог живого разговора</b>, просто на бумаге, а не вслух', 'Читатель слышит вас в голове: если фраза не звучит губами — она не звучит и на экране', 'Проверка одна: прочитай вслух. Споткнулся — переписывай до тех пор, пока не польётся'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Враг номер один — канцелярит', pts:['<b>Канцелярит</b> — язык справок и отписок: осуществление, в целях, данный, является', 'Ни один живой человек не говорит другу «в целях повышения эффективности»', 'Пример: «осуществляем доставку» → «привозим». Смысл тот же, воздуха больше'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Пластик убивает доверие', pts:['«Пластик» — красивые, но пустые обороты: команда профессионалов, широкий спектр, гибкий подход', 'Мозг читателя проскакивает их как шум и не запоминает ничего', 'За каждым словом должен стоять факт: не «быстро», а «за 40 минут»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Как заставить текст звучать', pts:['Представь конкретного человека и напиши, как объяснил бы лично ему за кофе', 'Короткие фразы, простые слова, прямой порядок: кто что сделал', 'Надиктуй голосом в <b>ChatGPT</b> или в диктофон, потом расшифруй — так исчезает книжность'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="50" y="8" width="20" height="34" rx="10"/><path d="M40 36a20 20 0 0 0 40 0M60 56v8M48 64h24"/></svg>'},
  {t:'Первые деньги приходят за ясность', pts:['Клиент платит не за красоту фраз, а за то, что понял и поверил', 'На разборах простой текст «привезу завтра к 10» продаёт лучше «оперативно реализуем логистику»', 'Первый гонорар копирайтера — это чаще всего переписанная в разговорную речь чужая казёнщина'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'}
  ],
  quiz:[
  {q:'Что такое текст по главному принципу школы?', o:['Красиво оформленный документ', 'Молчаливый аналог живой речи', 'Набор ключевых слов для поиска', 'Максимально официальное изложение'], a:1},
  {q:'Как быстрее всего проверить, живой ли текст?', o:['Посчитать количество абзацев', 'Прогнать через проверку орфографии', 'Прочитать вслух и заметить спотыкания', 'Показать другому копирайтеру'], a:2},
  {q:'Что из этого — канцелярит?', o:['«Привезём завтра к десяти»', '«Осуществляем доставку в целях удобства»', '«Позвони, если что»', '«Сделаем за час»'], a:1},
  {q:'Почему «пластиковые» обороты вредны?', o:['Они слишком длинные для заголовка', 'Их запрещают поисковики', 'Мозг проскакивает их и ничего не запоминает', 'Они требуют сложной вёрстки'], a:2},
  {q:'Чем заменить слово «быстро» в продающем тексте?', o:['Словом «оперативно»', 'Конкретикой: «за 40 минут»', 'Словом «моментально»', 'Ничем, оставить как есть'], a:1}
  ],
  pairs:[
  ['Канцелярит', 'Язык справок и отписок вместо живой речи'],
  ['Пластик', 'Красивые пустые обороты без фактов'],
  ['Проверка вслух', 'Спотыкаешься — значит фраза не звучит'],
  ['Разговорный тон', 'Пишешь так, будто объясняешь другу лично'],
  ['Конкретика', 'Цифра или факт вместо оценочного слова']
  ],
  task:{
    intro:'Возьми любую казённую фразу с сайта услуг и перепиши её так, как сказал бы вживую. Ниже — заготовки, замени под свой пример.',
    chips:['Было: «осуществляем...»', 'Стало: разговорный вариант', 'Убрал канцелярит: ...', 'Добавил конкретику: цифра', 'Проверил вслух: звучит', 'Кому объясняю: конкретный человек'],
    ph:'Было: «Компания осуществляет доставку продукции в кратчайшие сроки». Стало: «Привозим заказ на следующий день, до 12:00». Убрал канцелярит «осуществляет доставку», добавил конкретику «до 12:00», прочитал вслух — не спотыкаюсь.',
    verdict:'Отличная работа: ты не просто сократил, а вставил живую цифру «до 12:00» — именно она превращает обещание в факт. Усилю совет: перед публикацией дай прочитать фразу человеку не из ниши. Если он повторит её своими словами без запинки — текст готов.'
  }
},
{
  title:'Признак 1: причастия → глаголы',
  sub:'3:02 · слайды + тест + игра', dur:'3:02',
  c1:'ПРИЧАСТИЯ', c2:'В ГЛАГОЛЫ',
  slides:[
  {t:'Причастия тормозят чтение', pts:['<b>Причастия и деепричастия</b> — «идущий», «сделав», «работающая» — навешивают на фразу лишний вес', 'Живая речь их почти не использует: мы говорим «который идёт», а чаще просто «идёт»', 'Каждое причастие — это спрятанный глагол, который просит вернуть его в строй'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Как их ловить', pts:['Ищи хвосты -ущ, -ющ, -ащ, -ящ, -вш, -ем, -им и деепричастные -а/-я, -в', '«Клиент, оформивший заказ» — стоп, здесь спрятан глагол «оформил»', 'Подсвети их в <b>Word</b> или попроси <b>ChatGPT</b> найти все причастные обороты в тексте'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Разворачиваем в действие', pts:['«Растущие продажи, обусловленные рекламой» → «Реклама подняла продажи»', 'Оборот превращаем в отдельное короткое предложение с глаголом-действием', 'Появляется подлежащее и сказуемое — фраза оживает и ускоряется'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Когда причастие оставить', pts:['Есть устойчивые формы: «жареный», «печатный», «данные» — их не трогаем', 'Одиночное причастие-определение без хвоста оборота часто безобидно', 'Правило про действие, а не про фанатизм: если звучит живо — оставляй'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Что это даёт в деньгах', pts:['Глагольный текст читается на скорости речи — <b>конверсия</b> оффера растёт', 'На разборах карточка «Мастер, приезжающий и выполняющий...» проигрывала «Мастер приедет и починит»', 'Динамика = действие. Глагол двигает читателя к кнопке, причастие усыпляет'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Почему причастия тормозят текст?', o:['Они всегда с ошибками', 'Навешивают лишний вес и прячут глагол', 'Их не понимают поисковики', 'Они слишком короткие'], a:1},
  {q:'Какой из хвостов выдаёт причастие?', o:['-ость', '-ение', '-ущ / -ющ / -вш', '-ация'], a:2},
  {q:'Как правильно развернуть «продажи, выросшие после рекламы»?', o:['«Продажи, являющиеся выросшими»', '«После рекламы продажи выросли»', '«Рост продаж вследствие рекламы»', '«Выросшесть продаж от рекламы»'], a:1},
  {q:'Какое причастие можно оставить без правки?', o:['«Оформивший заказ клиент»', '«Работающая допоздна поддержка»', '«Жареный картофель»', '«Приезжающий утром мастер»'], a:2},
  {q:'Что даёт замена причастий на глаголы?', o:['Текст становится официальнее', 'Текст читается на скорости речи и лучше конвертит', 'Растёт число символов', 'Пропадает смысл'], a:1}
  ],
  pairs:[
  ['Причастие', 'Спрятанный глагол с тяжёлым хвостом'],
  ['Деепричастие', '«Сделав», «оформив» — тоже в глагол'],
  ['-ущ, -вш, -ем', 'Маркеры, по которым ловим оборот'],
  ['Разворот в глагол', 'Отдельное короткое предложение с действием'],
  ['Устойчивая форма', '«Жареный», «данные» — не трогаем']
  ],
  task:{
    intro:'Найди в своём тексте три причастных или деепричастных оборота и разверни каждый в глагол. Заполни по образцу.',
    chips:['Оборот 1 → глагол', 'Оборот 2 → глагол', 'Оборот 3 → глагол', 'Хвост, который выдал: ...', 'Оставил как есть: ...', 'Стало короче на N слов'],
    ph:'1) «Клиент, оформивший заявку» → «Клиент оформил заявку». 2) «Товар, доставляемый курьером» → «Курьер привезёт товар». 3) «Сэкономив время, вы...» → «Вы сэкономите время и...». Оставил «печатная реклама» — устойчивая форма.',
    verdict:'Сильно: ты не механически рубил, а вернул каждому обороту подлежащее и действие, поэтому фразы зазвучали. И правильно не тронул «печатную рекламу». Совет на будущее: после разворота перечитай абзац целиком — иногда два соседних глагола просятся слиться в один динамичный.'
  }
},
{
  title:'Признак 2: выкинь лишнее слово',
  sub:'2:48 · слайды + тест + игра', dur:'2:48',
  c1:'ВЫКИНЬ', c2:'ЛИШНЕЕ',
  slides:[
  {t:'Текст не худеет в смысле', pts:['Приём: выкидывай <b>каждое 3–5-е слово</b> и смотри — потерялся ли смысл', 'В 9 случаях из 10 смысл на месте, а фраза стала легче и быстрее', 'Слово, без которого понятно, — это балласт, а не украшение'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h80L68 44v18l-16 8V44z"/></svg>'},
  {t:'Кого выкидываем первым', pts:['Усилители-паразиты: очень, весьма, довольно, реально, буквально, просто', 'Дубли смысла: «свободная вакансия», «первый дебют», «в конечном итоге»', 'Служебный шум: «то, что», «для того чтобы», «на самом деле», «как бы»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Механика прохода', pts:['Читаешь фразу и мысленно закрываешь каждое слово пальцем', 'Смысл выжил без слова — вычёркиваешь без сожаления', 'Смысл поплыл — возвращаешь. Так проходишь весь абзац по одному слову'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'«Очень» не усиливает', pts:['«Очень дёшево» слабее, чем «299 рублей» — усилитель не даёт картинки', 'Заменяй усилитель фактом, а не убирай в пустоту', 'Сила в конкретике: не «огромная скидка», а «минус 60%»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Плотность держит внимание', pts:['Плотный текст уважает время читателя — он не бросает его на середине', 'На разборах ужатый на 30% оффер собирал больше заявок при том же трафике', 'Меньше слов на ту же мысль = выше <b>дочитываемость</b> и больше касаний до продажи'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'В чём суть приёма «выкинь каждое 3–5-е слово»?', o:['Сократить текст ровно на треть', 'Проверить, теряется ли смысл без слова', 'Удалить все прилагательные', 'Оставить только глаголы'], a:1},
  {q:'Что из этого — смысловой дубль?', o:['«Быстрая доставка»', '«Свободная вакансия»', '«Скидка 60%»', '«Привезём завтра»'], a:1},
  {q:'Чем заменить «очень дёшево»?', o:['«Крайне дёшево»', '«Реально недорого»', '«299 рублей»', '«Максимально доступно»'], a:2},
  {q:'Когда слово нельзя выкидывать?', o:['Если оно длиннее пяти букв', 'Если без него смысл плывёт', 'Если это глагол', 'Если оно стоит первым'], a:1},
  {q:'Что растёт при большей плотности текста?', o:['Количество символов', 'Дочитываемость и число заявок', 'Число причастий', 'Время написания'], a:1}
  ],
  pairs:[
  ['Балласт', 'Слово, без которого всё понятно'],
  ['Усилитель-паразит', 'Очень, весьма, буквально, реально'],
  ['Смысловой дубль', '«Первый дебют», «свободная вакансия»'],
  ['Проход пальцем', 'Закрываешь слово и проверяешь смысл'],
  ['Плотность', 'Меньше слов на ту же мысль']
  ],
  task:{
    intro:'Возьми свой абзац и сократи его минимум на четверть, не потеряв ни одной мысли. Покажи было/стало и что выкинул.',
    chips:['Было: N слов', 'Стало: M слов', 'Выкинул паразитов: ...', 'Убрал дубль: ...', 'Усилитель заменил фактом', 'Смысл сохранён: да'],
    ph:'Было (24 слова): «Мы предлагаем действительно очень выгодные цены и осуществляем максимально быструю доставку для того, чтобы вам было удобно». Стало (11 слов): «Цены от 299 рублей, привозим на следующий день». Выкинул «действительно», «очень», «максимально», «для того чтобы»; «выгодные» заменил цифрой.',
    verdict:'Мощно: минус 13 слов, а мысль стала яснее, чем была. Особенно верно, что «выгодные» ты не удалил в пустоту, а заменил на «от 299 рублей». Добавлю приём: после сокращения отложи текст на час и пройди ещё раз — на свежую голову находится ещё 2–3 лишних слова.'
  }
},
{
  title:'Признак 3: выразительный синоним',
  sub:'3:10 · слайды + тест + игра', dur:'3:10',
  c1:'ТОЧНЫЙ', c2:'СИНОНИМ',
  slides:[
  {t:'Пресные слова не цепляют', pts:['Приём: <b>каждое 3–5-е слово</b> меняй на более выразительный синоним', 'Не ради красоты, а ради точности: «большой» и «громадный» рисуют разную картинку', 'Точное слово экономит целое предложение объяснений'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M74 12 92 30 56 66l-18 4 4-18z"/><path d="M62 24 80 42" opacity=".7"/></svg>'},
  {t:'От серого к точному', pts:['«Сделать» → построить, собрать, запустить, починить — что именно сделали?', '«Хороший» → надёжный, честный, тёплый, быстрый — по какому признаку хороший?', '«Проблема» → затык, дыра, головная боль — какая именно боль клиента?'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Глаголы — главная добыча', pts:['Замена глагола даёт больше энергии, чем замена прилагательного', '«Цены выросли» → «цены взлетели / подскочили / поползли вверх»', 'Выбирай глагол по эмоции сцены: паника, спокойствие, азарт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c4 12-6 16-6 26 0-6-5-9-5-9-4 6-11 11-11 22a22 22 0 0 0 44 0c0-14-12-20-16-39z"/></svg>'},
  {t:'Не переборщить', pts:['Выразительный ≠ редкий. «Дефиниция» вместо «определение» — это выпендрёж, не сила', 'Синоним должен быть понятен читателю с ходу, без словаря', 'Один яркий глагол на фразу — уже много; два подряд глушат друг друга'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Где брать синонимы', pts:['Проси у <b>ChatGPT</b> или <b>Claude</b> список синонимов с оттенками смысла, а не сухой ряд', 'Держи личный словарик сильных глаголов своей ниши', 'На разборах точное слово «затопило заявками» продавало кейс лучше, чем «пришло много»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 16c-8-6-22-6-30 0v40c8-6 22-6 30 0 8-6 22-6 30 0V16c-8-6-22-6-30 0z"/><path d="M60 16v40"/></svg>'}
  ],
  quiz:[
  {q:'Зачем менять слова на выразительные синонимы?', o:['Чтобы текст был длиннее', 'Ради точности картинки, а не украшательства', 'Чтобы показать словарный запас', 'Чтобы обойти проверку на уникальность'], a:1},
  {q:'Замена какой части речи даёт больше энергии?', o:['Предлога', 'Глагола', 'Союза', 'Местоимения'], a:1},
  {q:'Какой синоним «выпендрёжный», а не сильный?', o:['«Взлетели» вместо «выросли»', '«Затык» вместо «проблема»', '«Дефиниция» вместо «определение»', '«Починил» вместо «сделал»'], a:2},
  {q:'Сколько ярких глаголов в одной фразе — уже перебор?', o:['Ни одного нельзя', 'Два подряд глушат друг друга', 'Пять — норма', 'Ограничений нет'], a:1},
  {q:'Как правильно просить синонимы у нейросети?', o:['Просто список из десяти слов', 'Список с оттенками смысла под контекст', 'Только редкие книжные слова', 'Синонимы на английском'], a:1}
  ],
  pairs:[
  ['Пресное слово', 'Серое «сделать», «хороший», «большой»'],
  ['Точный синоним', 'Рисует конкретную картинку одним словом'],
  ['Сильный глагол', 'Взлетели, затопило, подскочили'],
  ['Выпендрёж', 'Редкое слово, за которым лезешь в словарь'],
  ['Личный словарик', 'Запас сильных глаголов своей ниши']
  ],
  task:{
    intro:'Возьми три пресные фразы и замени в каждой ключевое слово на выразительный, но понятный синоним. Объясни выбор оттенка.',
    chips:['Было: пресное слово', 'Стало: синоним', 'Оттенок: почему это', 'Глагол вместо прилагательного', 'Проверил на понятность', 'Не переборщил: 1 яркое'],
    ph:'1) «Пришло много заявок» → «Заявки завалили нас» — оттенок азарта и масштаба. 2) «Мы сделали сайт» → «Мы собрали сайт за 5 дней» — точность действия. 3) «Хороший сервис» → «Сервис, который не подведёт» — признак надёжности.',
    verdict:'Отлично: ты менял не ради блеска, а под эмоцию сцены — «завалили» реально передаёт масштаб, «собрали» уточняет действие. И держишь по одному яркому слову на фразу. Совет: заведи заметку «сильные глаголы моей ниши» и пополняй её из чужих залетевших офферов — через месяц будешь брать точное слово за секунду.'
  }
},
{
  title:'Признак 4: убери вводный абзац',
  sub:'2:56 · слайды + тест + игра', dur:'2:56',
  c1:'УБЕРИ', c2:'РАЗБЕГ',
  slides:[
  {t:'Первый абзац — это разбег', pts:['Приём: удали <b>вводный первый абзац</b> целиком и проверь — стало ли хуже', 'Почти всегда первый абзац — это разогрев автора, а не польза читателю', 'Мы разгоняемся, как перед прыжком: настоящая мысль начинается со второго-третьего абзаца'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Как выглядит вода', pts:['«В современном мире всё быстрее меняется...» — так начинается 90% слабых текстов', 'Общие фразы про важность темы, историю вопроса, «как известно»', 'Признак: абзац можно вставить в любой другой текст — значит, он ни о чём'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 52a16 16 0 0 1 2-32 20 20 0 0 1 38 4 14 14 0 0 1-2 28z"/></svg>'},
  {t:'Тест на удаление', pts:['Закрой первый абзац и прочитай текст со второго — понятно ли, о чём речь?', 'Понятно и даже бодрее — значит первый абзац был лишним разбегом', 'Потерялся вход — значит там была мысль, её надо переставить, а не тянуть воду'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Начинай с сути', pts:['Хороший текст бьёт с первой строки: факт, цифра, обещание, вопрос в лоб', '«В этой статье мы расскажем» — вырезать, сразу рассказывать', 'Читатель решает за 2–3 секунды, остаться или уйти — не трать их на разгон'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Цена лишнего входа', pts:['Каждый абзац-вода — это часть аудитории, которая ушла, не дойдя до оффера', 'На разборах удаление вступления поднимало <b>дочитываемость</b> поста в полтора раза', 'Первый экран решает всё: там либо крючок, либо потерянный клиент'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'}
  ],
  quiz:[
  {q:'Почему первый абзац часто лишний?', o:['В нём всегда ошибки', 'Это разогрев автора, а не польза читателю', 'Он слишком короткий', 'В нём нет глаголов'], a:1},
  {q:'Как проверить, что абзац — вода?', o:['Он длиннее трёх строк', 'Его можно вставить в любой другой текст', 'В нём есть цифры', 'Он заканчивается вопросом'], a:1},
  {q:'Что делать, если после удаления вход потерялся?', o:['Вернуть абзац как был', 'Дописать ещё воды', 'Переставить настоящую мысль вперёд', 'Удалить и второй абзац'], a:2},
  {q:'За сколько читатель решает, остаться ли?', o:['За минуту чтения', 'За 2–3 секунды', 'После первого экрана целиком', 'После заголовка и подзаголовка'], a:1},
  {q:'С чего должен бить сильный текст?', o:['С истории вопроса', 'С факта, цифры или обещания', 'С благодарности читателю', 'С оговорки об актуальности'], a:1}
  ],
  pairs:[
  ['Вводный абзац', 'Разбег автора перед настоящей мыслью'],
  ['Вода', 'Общие фразы, что вставишь в любой текст'],
  ['Тест на удаление', 'Читаешь со второго абзаца — понятно?'],
  ['Крючок', 'Факт или вопрос в первой же строке'],
  ['«Мы расскажем»', 'Служебный вход, который надо вырезать']
  ],
  task:{
    intro:'Возьми свой текст или пост, удали первый абзац и оцени результат. Покажи удалённое, новое начало и вывод.',
    chips:['Удалил абзац: ...', 'Новое начало: ...', 'Стало хуже / бодрее', 'Переставил мысль? да/нет', 'Первая строка — крючок', 'Дочитываемость выросла'],
    ph:'Удалил: «В современном мире ремонт техники становится всё актуальнее, ведь у каждого дома куча гаджетов». Новое начало: «Починим ваш холодильник за один визит — или вернём деньги». Стало бодрее, мысль про важность не понадобилась, первая строка теперь обещание с гарантией.',
    verdict:'Точно в цель: ты не просто отрезал воду, а поставил на её место обещание с гарантией — это и есть крючок первого экрана. Усилю: проверь, нет ли «второго вводного» — иногда разбег прячется и во втором абзаце. Прогони тот же тест удаления ещё раз.'
  }
},
{
  title:'Признак 5: первое предложение = заголовок',
  sub:'3:18 · слайды + тест + игра', dur:'3:18',
  c1:'ПЕРВАЯ', c2:'СТРОКА',
  slides:[
  {t:'Первая строка работает как заголовок', pts:['Приём: <b>первое предложение</b> должно цеплять так же сильно, как заголовок', 'В ленте и мессенджерах заголовка часто нет — первую строку видят раньше всего', 'Не зашла первая фраза — остальной текст уже никто не прочтёт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Четыре рабочих крючка', pts:['Цифра-обещание: «Сняли 3 ролика — получили 40 заявок»', 'Вопрос-боль: «Почему клиенты уходят после первого письма?»', 'Дерзкое утверждение: «Ваш оффер не продаёт. И вот почему»', 'Обещание выгоды: «Покажу, как писать посты за 15 минут»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Чего первая строка боится', pts:['Раскачки: «Хочу сегодня поговорить о...» — уже потерял половину', 'Приветствий и оргинформации в самом верху', 'Абстракций без картинки: «Тема очень важная и актуальная»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Пиши строку последней', pts:['Сначала весь текст, потом вытаскивай самую сильную мысль наверх', 'Часто лучший крючок прячется в середине — вырежи и подними в первую строку', 'Проверь: если оставить только первую фразу — захочется читать дальше?'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M30 54h60" opacity=".6"/><path d="M40 46l30-30 8 8-30 30-12 4z"/><path d="M64 22l8 8"/></svg>'},
  {t:'Первая строка = деньги', pts:['Это ворота воронки: не открыл — не увидел ни оффера, ни кнопки', 'На разборах смена одной первой строки поднимала охваты поста в 2–3 раза без правки остального', 'Тестируй 3–5 вариантов первой строки на один текст — как заголовки к <b>рекламе</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'}
  ],
  quiz:[
  {q:'Почему первая строка = заголовок?', o:['Так короче писать', 'В ленте её видят раньше всего, заголовка часто нет', 'Этого требуют поисковики', 'Чтобы уместить ключевые слова'], a:1},
  {q:'Что из этого — рабочий крючок?', o:['«Всем привет, как дела?»', '«Сегодня хочу поговорить о важном»', '«Сняли 3 ролика — получили 40 заявок»', '«Тема эта очень актуальна»'], a:2},
  {q:'Когда лучше писать первую строку?', o:['Самой первой, до текста', 'Последней, вытащив сильную мысль наверх', 'Вообще не писать', 'Скопировать из заголовка статьи'], a:1},
  {q:'Чего первая строка должна избегать?', o:['Цифр', 'Вопросов', 'Раскачки и приветствий', 'Обещания выгоды'], a:2},
  {q:'Как относиться к первой строке при тесте?', o:['Писать один раз и не трогать', 'Тестировать 3–5 вариантов, как заголовки рекламы', 'Делать её максимально длинной', 'Прятать в конце текста'], a:1}
  ],
  pairs:[
  ['Первая строка', 'Скрытый заголовок, который видят первым'],
  ['Цифра-обещание', '«3 ролика — 40 заявок»'],
  ['Вопрос-боль', 'Бьёт прямо в проблему читателя'],
  ['Раскачка', '«Хочу поговорить о...» — убить'],
  ['Строка последней', 'Пишешь после текста, подняв сильную мысль']
  ],
  task:{
    intro:'Напиши к одному своему тексту три разных первых строки по разным крючкам (цифра, вопрос, дерзость). Отметь, какую выбрал и почему.',
    chips:['Крючок 1: цифра', 'Крючок 2: вопрос-боль', 'Крючок 3: дерзкое утверждение', 'Выбрал вариант: N', 'Почему цепляет: ...', 'Раскачку убрал: да'],
    ph:'Тема — услуги копирайтера. 1) Цифра: «Переписал 1 экран сайта — заявки выросли на 34%». 2) Вопрос: «Почему ваш сайт читают, но не пишут?». 3) Дерзость: «Ваш текст вежливый и мёртвый». Выбрал вариант 3 — бьёт по боли резче и заставляет читать дальше, чтобы оспорить.',
    verdict:'Здорово, что дал именно три разных типа крючка, а не три перефразировки одного — так видно диапазон. Выбор дерзкого варианта оправдан для холодной аудитории. Совет: не выбрасывай два других, поставь их A/B-тестом — цифра «34%» может выиграть на тёплой базе, где доверие уже есть.'
  }
},
{
  title:'Этапы: пиши, потом редактируй',
  sub:'3:30 · слайды + тест + игра', dur:'3:30',
  c1:'ПИШИ', c2:'ПОТОМ ПРАВЬ',
  slides:[
  {t:'Два режима нельзя смешивать', pts:['Сначала — <b>поток</b>: пишешь всё подряд, не останавливаясь и не оценивая', 'Потом — <b>редактура</b>: холодно режешь, меняешь, чистишь по признакам', 'Автор и редактор в одной голове одновременно — гарантия ступора и мук над строкой'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Этап 1: пиши на потоке', pts:['Задача — выгрузить все мысли, а не написать красиво с первого раза', 'Не возвращайся, не перечитывай, не ищи слово — оставляй пропуск и беги дальше', 'Черновик имеет право быть корявым: его никто не увидит'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Этап 2: остынь и режь', pts:['Дай тексту отлежаться хотя бы час, лучше ночь — вернётся свежий взгляд', 'Проходишь по всем пяти признакам: причастия, лишние слова, синонимы, вводный абзац, первая строка', 'Здесь ты безжалостен: режешь даже то, что было приятно писать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'},
  {t:'Читай вслух — правь', pts:['Финальный прогон — только вслух, вживую, губами, а не глазами', 'Споткнулся, задохнулся, полез перечитывать фразу — правь это место', 'Ухо ловит канцелярит и длинноты точнее, чем глаз'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="50" y="8" width="20" height="34" rx="10"/><path d="M40 36a20 20 0 0 0 40 0M60 56v8M48 64h24"/></svg>'},
  {t:'Где помогает нейросеть', pts:['Поток можно надиктовать голосом, а расшифровку сделать в <b>ChatGPT</b>', 'Редактуру просят у <b>Claude</b>: «найди канцелярит, причастия и повторы, но не меняй смысл»', 'Финальное решение всегда за тобой — модель советчик, а не автор'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="22" width="52" height="38" rx="10"/><circle cx="50" cy="41" r="5"/><circle cx="70" cy="41" r="5"/><path d="M60 22v-8M50 14h20"/><path d="M28 38v10M92 38v10" opacity=".7"/></svg>'}
  ],
  quiz:[
  {q:'Почему нельзя писать и редактировать одновременно?', o:['Это медленно печатается', 'Автор и редактор в одной голове дают ступор', 'Так запрещают редакторы', 'Теряется уникальность'], a:1},
  {q:'Что делать со словом, которое не нашлось на потоке?', o:['Остановиться и искать до победы', 'Оставить пропуск и бежать дальше', 'Заменить любым синонимом', 'Удалить всё предложение'], a:1},
  {q:'Зачем давать тексту отлежаться?', o:['Чтобы проверили другие', 'Чтобы вернулся свежий взгляд для редактуры', 'Чтобы поднять уникальность', 'Так требует алгоритм ленты'], a:1},
  {q:'Каким должен быть финальный прогон?', o:['Про себя глазами', 'Через проверку орфографии', 'Вслух, губами', 'Чтением с конца'], a:2},
  {q:'Какая роль у нейросети в этой схеме?', o:['Пишет текст за автора целиком', 'Советчик на потоке и в редактуре, решение за автором', 'Заменяет чтение вслух', 'Ставит финальную оценку'], a:1}
  ],
  pairs:[
  ['Поток', 'Выгружаешь мысли, не оценивая'],
  ['Редактура', 'Холодно режешь по пяти признакам'],
  ['Отлежаться', 'Час-ночь паузы для свежего взгляда'],
  ['Чтение вслух', 'Ухо ловит канцелярит точнее глаза'],
  ['Пропуск на потоке', 'Не нашёл слово — беги дальше']
  ],
  task:{
    intro:'Напиши короткий текст в два захода: сначала поток за 5 минут без правок, потом после паузы отредактируй по признакам. Покажи оба варианта и что изменилось.',
    chips:['Поток (черновик): ...', 'Пауза: сколько ждал', 'Правки по признакам: ...', 'Читал вслух: спотыкался тут', 'Итог (чистовик): ...', 'Стало короче на N слов'],
    ph:'Поток (5 мин, 60 слов, коряво про услугу монтажа). Пауза — 2 часа. Редактура: развернул 2 причастия, выкинул 9 слов-паразитов, удалил вводный абзац про «важность видео», поднял в первую строку фразу «Смонтирую ролик за сутки». Вслух споткнулся на длинном предложении — разбил на два. Итог — 40 слов, читается на одном дыхании.',
    verdict:'Прекрасно виден разрыв между режимами: черновик корявый, и это правильно — ты не мучил себя на потоке. В редактуре сработали сразу четыре признака плюс чтение вслух. Совет: замеряй время каждого этапа. Скоро увидишь, что поток занимает 20% времени, а редактура 80% — и перестанешь винить себя за «медленное» письмо.'
  }
},
{
  title:'F/Z-траектории чтения',
  sub:'3:44 · слайды + тест + игра', dur:'3:44',
  c1:'F И Z', c2:'ТРАЕКТОРИИ',
  slides:[
  {t:'Глаз не читает — сканирует', pts:['Читатель не проходит текст по строчкам, а бегает глазами по <b>траектории</b>', 'Тексты с абзацами глаз читает по букве F, лендинги-афиши — по букве Z', 'Задача копирайтера — расставить важное на пути этого взгляда, а не против него'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'F-траектория: длинные тексты', pts:['Глаз идёт вдоль первой строки, потом чуть ниже второй, затем падает по левому краю', 'Правый низ абзацев почти не читают — туда нельзя прятать суть', 'Вывод: <b>начало строк и абзацев</b> — золото, ставь туда ключевые слова'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Z-траектория: афиши и первые экраны', pts:['Взгляд идёт по Z: верх слева направо, диагональ вниз, низ слева направо', 'Работает там, где мало текста: баннер, обложка, hero-экран лендинга', 'Ставь логотип и оффер по углам Z, а <b>кнопку</b> — в финальную точку внизу справа'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Правила под траекторию', pts:['Первые 2–3 слова строки несут смысл — не начинай с «и», «а также», «для того»', 'Короткие абзацы: длинный блок глаз бросает на середине', 'Подзаголовки, списки и жирный текст создают опоры, за которые цепляется взгляд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Проверка вёрстки', pts:['Прищурься на текст: читаются ли начала строк как отдельная выжимка сути?', 'Собери в <b>Figma</b> или в макете тепловую логику: где взгляд задержится', 'На разборах перенос оффера в начало строки поднимал клики по кнопке без смены слов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'}
  ],
  quiz:[
  {q:'Как читатель проходит текст на самом деле?', o:['Аккуратно по каждой строке', 'Сканирует по траектории F или Z', 'Только заголовки', 'Снизу вверх'], a:1},
  {q:'Для чего подходит F-траектория?', o:['Для афиш и баннеров', 'Для длинных текстов с абзацами', 'Для логотипа', 'Для кнопки'], a:1},
  {q:'Куда по Z-траектории лучше ставить кнопку?', o:['В левый верхний угол', 'В центр', 'В финальную точку внизу справа', 'В левый нижний угол'], a:2},
  {q:'Почему важны первые слова строки?', o:['Они крупнее по шрифту', 'Глаз читает начала строк, туда идёт смысл', 'Так требует типографика', 'Их проще выделить жирным'], a:1},
  {q:'Как проверить текст на F-читаемость?', o:['Посчитать абзацы', 'Прищуриться и прочитать только начала строк', 'Прочитать с конца', 'Убрать все подзаголовки'], a:1}
  ],
  pairs:[
  ['F-траектория', 'Путь глаза по длинному тексту с абзацами'],
  ['Z-траектория', 'Путь взгляда по афише и первому экрану'],
  ['Начало строки', 'Золотая зона для ключевых слов'],
  ['Правый низ абзаца', 'Мёртвая зона, суть туда не прячут'],
  ['Опоры взгляда', 'Подзаголовки, списки, жирный текст']
  ],
  task:{
    intro:'Возьми свой экран или пост и переставь важное под траекторию чтения. Опиши, что было в мёртвой зоне и куда ты это перенёс.',
    chips:['Тип текста: F или Z', 'Было в мёртвой зоне: ...', 'Перенёс в начало строки: ...', 'Кнопка / оффер: куда поставил', 'Добавил опоры: подзаголовки', 'Проверил прищуром: суть видна'],
    ph:'Тип — F (пост-описание услуги). В мёртвой зоне правого низа пряталась цена «от 3000 рублей» — перенёс её в начало строки нового абзаца. Оффер «Смонтирую за сутки» поднял в первую строку. Добавил два подзаголовка-опоры. Прищурился: по началам строк читается «Смонтирую за сутки — от 3000 рублей — пишите в личку» — вся суть на пути взгляда.',
    verdict:'Сильный разбор: ты не просто «сделал красиво», а вытащил цену из мёртвой зоны в начало строки — именно там её теперь увидят. И проверка прищуром показала связную выжимку из трёх опор. Совет: для Z-экранов повесь кнопку строго в правый нижний угол первого экрана — это естественная финальная точка взгляда, клики по ней обычно выше на 10–20%.'
  }
}
  ],
  "k2": [
{
  title:'Заголовок и лид (боль)',
  sub:'3:20 · слайды + тест + игра', dur:'3:20',
  c1:'ЗАГОЛОВОК', c2:'И БОЛЬ',
  slides:[
  {t:'Зачем нужен заголовок', pts:['<b>Заголовок</b> решает, прочитают первую строку или пролистают — у тебя 1-2 секунды', 'Задача заголовка не продать, а <b>остановить</b> и заставить читать дальше', 'Пиши для одного человека и одной боли, а не для всех сразу'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Формула цепляющего заголовка', pts:['Обещание результата: что человек получит, если дочитает', '<b>Конкретика и цифры</b>: «первые 10 000 ₽» бьёт сильнее «много денег»', 'Интрига или разрыв шаблона: сказать не то, что ждут'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Лид: погружаем в боль', pts:['<b>Лид</b> — 2-4 строки сразу после заголовка, вводят в проблему', 'Опиши боль словами читателя, как он сам её проговаривает в голове', 'Усиль: покажи, к чему боль ведёт, если её не решить'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Как найти настоящую боль', pts:['Слушай комментарии, вопросы в директ, отзывы конкурентов', 'Боль — не «нет клиентов», а «сижу вечером и считаю, чем платить за аренду»', 'Одна боль на пост: распыление убивает отклик'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Частые ошибки', pts:['Заголовок «О себе» или «Немного мыслей» — никто не откроет', 'Лид про компанию, а не про читателя', 'Слишком общая боль, в которой никто себя не узнаёт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Главная задача заголовка?', o:['Продать товар сразу', 'Остановить и заставить читать дальше', 'Рассказать о компании', 'Собрать лайки'], a:1},
  {q:'Что такое лид в продающем посте?', o:['Ссылка на сайт', 'Первые строки, погружающие в боль', 'Название поста', 'Призыв купить'], a:1},
  {q:'Какой заголовок сильнее?', o:['«Немного о нас»', '«Как я вышла на первые 10 000 ₽ за неделю»', '«Наши новости»', '«Читайте пост»'], a:1},
  {q:'Сколько болей раскрывать в одном посте?', o:['Как можно больше', 'Одну, конкретную', 'Ровно три', 'Ни одной'], a:1},
  {q:'Как правильно описывать боль?', o:['Общими словами «нет продаж»', 'Словами и ситуациями самого читателя', 'Сложными терминами', 'Через выгоды продукта'], a:1}
  ],
  pairs:[
  ['Заголовок', 'Останавливает и втягивает в чтение'],
  ['Лид', 'Погружает читателя в боль'],
  ['Конкретика', 'Цифры вместо общих слов'],
  ['Интрига', 'Разрыв шаблона, сказать неожиданное'],
  ['Одна боль', 'Один пост — одна проблема']
  ],
  task:{
    intro:'Напиши заголовок и лид для продающего поста своей ниши: зацепи вниманием и вскрой одну конкретную боль клиента.',
    chips:['Цифра в заголовке', 'Обещание результата', 'Боль словами клиента', 'Одна проблема', 'Разрыв шаблона', 'Последствие боли'],
    ph:'Заголовок: «Как перестать сливать 30 000 ₽ в месяц на рекламу, которая не окупается». Лид: Ты запускаешь кампанию за кампанией, деньги уходят, а заявок — три калеки. Каждое утро открываешь кабинет и надеешься на чудо. Знакомо?',
    verdict:'Отлично: цифра 30 000 и живая картинка «три калеки» бьют в реального человека, а не в абстракцию. Усилю: добавь в конец лида один вопрос от первого лица клиента — так он окончательно узнает себя и дочитает до оффера.'
  }
},
{
  title:'Оффер и продажа выгод',
  sub:'3:40 · слайды + тест + игра', dur:'3:40',
  c1:'ОФФЕР', c2:'И ВЫГОДЫ',
  slides:[
  {t:'Что такое оффер', pts:['<b>Оффер</b> — суть предложения одним ясным ударом: что, кому и за что', 'Не «услуги маркетинга», а «настрою рекламу, которая приведёт 50 заявок за месяц»', 'Хороший оффер понятен за 5 секунд и вызывает «хочу»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="28" width="68" height="34" rx="6"/><path d="M26 38h68M60 28v34"/><path d="M60 28c-6-14-22-10-16-2 3 4 16 2 16 2zM60 28c6-14 22-10 16-2-3 4-16 2-16 2z"/></svg>'},
  {t:'Свойство против выгоды', pts:['Свойство — что есть у продукта; <b>выгода</b> — что это даёт клиенту', '«10 уроков» — свойство; «соберёшь первый ролик уже к вечеру» — выгода', 'Продавай результат и эмоцию, а не характеристики'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Продажа выгод доказательствами', pts:['<b>Отзывы</b>: реальные слова клиентов бьют сильнее твоих обещаний', '<b>Кейсы</b>: было-стало с цифрами и сроком', 'Цифры и факты снимают недоверие лучше прилагательных'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Язык выгод', pts:['Формула «свойство → поэтому → выгода»: связывай факт с результатом', 'Говори «ты получишь», а не «мы предоставляем»', 'Каждая выгода отвечает на немой вопрос «и что мне с этого?»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Собираем блок выгод', pts:['3-5 выгод, каждая — отдельная строка, легко сканировать', 'Первая выгода — самая сильная, закрывает главную боль из лида', 'Подкрепи хотя бы одну выгоду цифрой, отзывом или скрином'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Ошибки оффера', pts:['Размытость: «качественно и недорого» не значит ничего', 'Список свойств без перевода в выгоды', 'Обещание без доказательств — читатель не верит на слово'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Что такое оффер?', o:['Список ваших регалий', 'Ясная суть предложения одним ударом', 'История компании', 'Цена со скидкой'], a:1},
  {q:'Чем выгода отличается от свойства?', o:['Ничем', 'Свойство — что есть, выгода — что это даёт клиенту', 'Выгода дешевле', 'Свойство важнее'], a:1},
  {q:'Что лучше докажет результат?', o:['Красивое прилагательное', 'Кейс «было-стало» с цифрами', 'Обещание', 'Длинное описание'], a:1},
  {q:'С какой выгоды начинать блок?', o:['С самой слабой', 'С самой сильной, закрывающей главную боль', 'С цены', 'Со случайной'], a:1},
  {q:'Формула перевода факта в выгоду?', o:['«свойство → поэтому → выгода»', '«цена → скидка»', '«мы → лучшие»', '«купи → сейчас»'], a:0},
  {q:'Почему обещание без доказательства слабое?', o:['Оно длинное', 'Читатель не верит на слово', 'Его сложно писать', 'Оно дорогое'], a:1}
  ],
  pairs:[
  ['Оффер', 'Суть предложения одним ударом'],
  ['Свойство', 'Что есть у продукта'],
  ['Выгода', 'Что это даёт клиенту'],
  ['Кейс', 'Было-стало с цифрами'],
  ['Отзыв', 'Слова клиента как доказательство'],
  ['Первая выгода', 'Закрывает главную боль']
  ],
  task:{
    intro:'Возьми свой продукт и распиши оффер плюс 3 выгоды по формуле «свойство → поэтому → выгода», подкрепив хотя бы одну доказательством.',
    chips:['Свойство продукта', 'Перевод в выгоду', 'Цифра или срок', 'Отзыв клиента', 'Кейс было-стало', 'Главная боль в первой выгоде'],
    ph:'Оффер: соберу воронку в Telegram, которая превращает подписчиков в заявки на автопилоте. Выгоды: 1) Настрою бота за 3 дня — поэтому заявки пойдут уже на этой неделе. 2) Пропишу цепочку из 5 сообщений — поэтому подписчик прогревается сам, без тебя. 3) Клиент Аня так собрала 42 заявки за первый месяц (скрин прилагаю).',
    verdict:'Сильно: каждая выгода привязана к результату и сроку, а кейс Ани с цифрой 42 снимает недоверие. Усилю: перенеси кейс выше, сразу после первой выгоды — доказательство в начале блока держит внимание крепче, чем в конце.'
  }
},
{
  title:'Продажа цены и дедлайн',
  sub:'3:30 · слайды + тест + игра', dur:'3:30',
  c1:'ЦЕНА', c2:'И ДЕДЛАЙН',
  slides:[
  {t:'Почему цену надо продавать', pts:['Цена без объяснения кажется дорогой — мозг ищет причину сказать «нет»', '<b>Продажа цены</b> — показать, почему она справедлива и выгодна', 'Сравнивай цену с ценностью, а не с кошельком читателя'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Приёмы обоснования цены', pts:['<b>Дробление</b>: «всего 33 ₽ в день» вместо «1000 ₽ в месяц»', 'Сравнение: цена курса против одной ошибки в рекламе на 30 000 ₽', 'Что входит: разложи пакет, чтобы ценность превышала цифру'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Якорь и контраст', pts:['<b>Якорь</b>: сначала покажи высокую цену или аналог, потом свою', 'Три тарифа: средний на фоне дорогого кажется разумным', 'Старая цена зачёркнута рядом с новой — виден выигрыш'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Дедлайн и дефицит', pts:['<b>Дедлайн</b> убирает «подумаю потом», которое равно «никогда»', 'Ограничь по времени, по местам или по бонусу — дефицит двигает к действию', 'Дефицит должен быть настоящим, иначе теряешь доверие'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Как усилить дедлайн', pts:['Назови точную дату и время окончания, а не «скоро»', 'Свяжи дедлайн с выгодой: «после пятницы цена +50%»', 'Напомни, что теряет человек, если не успеет — <b>боль упущенного</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Ошибки цены и дедлайна', pts:['Цена без контекста ценности — сразу «дорого»', 'Вечный «последний день», который повторяется неделями', 'Дедлайн без последствий: если ничего не меняется, никто не спешит'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Зачем продавать цену?', o:['Чтобы она казалась справедливой и выгодной', 'Чтобы скрыть её', 'Чтобы поднять налог', 'Продавать не нужно'], a:0},
  {q:'Что такое дробление цены?', o:['Банковская рассрочка', 'Показ цены как малой суммы за день или использование', 'Скидка', 'Возврат средств'], a:1},
  {q:'Как работает ценовой якорь?', o:['Высокая цена рядом делает твою разумной', 'Прячет цену', 'Снижает налог', 'Ускоряет доставку'], a:0},
  {q:'Зачем нужен дедлайн?', o:['Для красоты', 'Убрать «подумаю потом» и подтолкнуть к действию', 'Поднять цену', 'Замедлить продажи'], a:1},
  {q:'Каким должен быть дефицит?', o:['Выдуманным побольше', 'Настоящим, иначе теряешь доверие', 'Вечным', 'Скрытым'], a:1},
  {q:'Что усиливает дедлайн?', o:['Расплывчатое «скоро»', 'Точная дата плюс последствие пропуска', 'Отсутствие даты', 'Молчание о сроке'], a:1}
  ],
  pairs:[
  ['Продажа цены', 'Объяснить, почему справедлива'],
  ['Дробление', 'Цена как сумма за день'],
  ['Якорь', 'Высокая цена делает твою разумной'],
  ['Дедлайн', 'Ограничение времени против «потом»'],
  ['Дефицит', 'Ограничение мест или бонуса'],
  ['Боль упущенного', 'Что теряешь, если не успеешь']
  ],
  task:{
    intro:'Обоснуй цену своего продукта минимум двумя приёмами и добавь честный дедлайн с последствием пропуска.',
    chips:['Дробление на день', 'Сравнение с потерей', 'Что входит в пакет', 'Ценовой якорь', 'Точная дата', 'Последствие после дедлайна'],
    ph:'Курс стоит 9 900 ₽ — это 27 ₽ в день за год. Одна неудачная рекламная кампания сливает больше за неделю. Внутри: 12 модулей, разборы, шаблоны и чат с куратором. До воскресенья 23:59 цена 9 900 ₽, с понедельника — 14 900 ₽. Успеешь — сэкономишь 5 000 и стартуешь в эти выходные.',
    verdict:'Чётко: дробление до 27 ₽ и сравнение с потерей на рекламе снимают «дорого», а точная дата с ростом цены на 5 000 создаёт реальную причину не тянуть. Усилю: добавь ограничение по местам к куратору — дефицит по времени плюс по количеству давит сильнее, чем один таймер.'
  }
},
{
  title:'CTA — призыв к действию',
  sub:'3:10 · слайды + тест + игра', dur:'3:10',
  c1:'ОДИН', c2:'ПРИЗЫВ',
  slides:[
  {t:'Что такое CTA', pts:['<b>CTA</b> — призыв к действию, чёткий следующий шаг для читателя', 'Без CTA пост прочитали и забыли — конверсия равна нулю', 'Один пост — один целевой шаг, не разбрасывай внимание'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30v12l14 3 30 14V13L34 27z"/><path d="M64 22c10 3 10 23 0 26" opacity=".7"/><path d="M40 48l4 12" /></svg>'},
  {t:'Правило одного действия', pts:['Не давай выбор «купи, подпишись, лайкни, напиши» — мозг зависает', 'Выбери одно ключевое действие под цель поста', 'Чем проще шаг, тем выше отклик: «напиши +» легче, чем «оставь заявку на сайте»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Формула сильного CTA', pts:['Глагол действия плюс конкретика: «Напиши слово СТАРТ в директ»', 'Убери <b>трение</b>: скажи точно, куда нажать и что произойдёт дальше', 'Добавь микро-выгоду: «напиши СТАРТ — пришлю программу и цену»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'CTA и дедлайн вместе', pts:['Свяжи призыв со сроком: «Запишись до пятницы»', 'Напомни, что даёт действие прямо сейчас', 'Лёгкий толчок: «осталось 5 мест» рядом с призывом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Ошибки CTA', pts:['Нет призыва вообще — самый частый провал поста', 'Несколько призывов сразу — читатель не делает ни одного', 'Размытое «переходите по ссылке» без причины и без выгоды'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Что такое CTA?', o:['Заголовок', 'Чёткий призыв к следующему шагу', 'Отзыв', 'Цена'], a:1},
  {q:'Сколько целевых действий в одном посте?', o:['Как можно больше', 'Одно', 'Ровно три', 'Ноль'], a:1},
  {q:'Какой CTA сильнее?', o:['«Переходите по ссылке»', '«Напиши СТАРТ в директ — пришлю программу»', '«Спасибо за внимание»', '«Подумайте на досуге»'], a:1},
  {q:'Почему несколько призывов вредят?', o:['Долго читать', 'Читатель зависает и не делает ни одного', 'Это дорого', 'Так нельзя технически'], a:1},
  {q:'Что усиливает CTA?', o:['Связка со сроком и микро-выгодой', 'Длина текста', 'Много ссылок', 'Отсутствие глагола'], a:0}
  ],
  pairs:[
  ['CTA', 'Чёткий следующий шаг'],
  ['Одно действие', 'Не распылять выбор'],
  ['Глагол действия', '«Напиши», «Запишись»'],
  ['Микро-выгода', 'Что получишь сразу за шаг'],
  ['Трение', 'Всё, что мешает нажать']
  ],
  task:{
    intro:'Напиши один сильный CTA для своего поста: глагол действия, точный шаг, микро-выгода и лёгкий толчок срочности.',
    chips:['Глагол действия', 'Куда именно написать', 'Микро-выгода за шаг', 'Одно действие', 'Срок или места', 'Убрать трение'],
    ph:'Хочешь так же? Напиши слово РАЗБОР в личку — за 10 минут пришлю разбор твоей воронки и покажу, где ты теряешь заявки. Бесплатно, но беру только 5 человек до пятницы.',
    verdict:'Точно в цель: один глагол «напиши», ясное слово-триггер РАЗБОР и микро-выгода «разбор за 10 минут» убирают сомнения. Усилю: продублируй этот CTA короткой строкой в самом низу поста — читатель, который сразу проскроллил к концу, всё равно упрётся в призыв.'
  }
},
{
  title:'Хук: первые 3 секунды',
  sub:'3:00 · слайды + тест + игра', dur:'3:00',
  c1:'ПЕРВЫЕ', c2:'3 СЕКУНДЫ',
  slides:[
  {t:'Три секунды решают всё', pts:['<b>Хук</b> — первые 3 секунды, которые решают, досмотрят или пролистают', 'Алгоритм видит удержание: слабое начало — и охваты падают', 'Задача хука — «дать пощёчину»: резко выдернуть из ленты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Пять эмоций-крючков', pts:['<b>Удивление</b>: неожиданный факт или результат', '<b>Любопытство</b>: вопрос без ответа, интрига', '<b>Гнев, страх, ностальгия</b>: сильная эмоция держит палец над экраном', 'Выбирай одну эмоцию под смысл ролика'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Приёмы сильного хука', pts:['Начни с результата или с конфликта, а не с приветствия', 'Обещай пользу в первой фразе: «за 3 секунды покажу, как…»', 'Визуальный крючок: движение, крупный план, необычный первый кадр'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Что убивает хук', pts:['«Привет, ребята, сегодня поговорим о…» — минус половина зрителей', 'Долгая заставка, логотип, раскачка на 5 секунд', 'Обещание, которое не совпадает с содержанием — люди уходят и не верят'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Как проверить хук', pts:['Посмотри первые 3 секунды без звука — цепляет?', 'Спроси себя: возникает ли вопрос «а что дальше?»', 'Смотри метрику <b>удержания</b>: где просадка — там слабое начало'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'}
  ],
  quiz:[
  {q:'Что такое хук?', o:['Финал ролика', 'Первые 3 секунды, решающие судьбу просмотра', 'Музыка', 'Подпись'], a:1},
  {q:'Почему хук важен для охватов?', o:['Алгоритм смотрит удержание, слабое начало роняет охваты', 'Ради красоты', 'Для звука', 'Он не важен'], a:0},
  {q:'Какая эмоция НЕ работает как крючок?', o:['Удивление', 'Любопытство', 'Равнодушие', 'Страх'], a:2},
  {q:'Как НЕ надо начинать ролик?', o:['С результата', 'С «Привет, сегодня поговорим о…»', 'С конфликта', 'С обещания пользы'], a:1},
  {q:'Как проверить силу хука?', o:['По длине ролика', 'Посмотреть первые 3 сек и метрику удержания', 'По числу тегов', 'По музыке'], a:1},
  {q:'Что если обещание в хуке сильнее содержания?', o:['Оставить, это норма', 'Люди уйдут и потеряют доверие — так нельзя', 'Добавить музыку', 'Удлинить ролик'], a:1}
  ],
  pairs:[
  ['Хук', 'Первые 3 секунды ролика'],
  ['Пощёчина', 'Резко выдернуть из ленты'],
  ['Любопытство', 'Вопрос без ответа'],
  ['Удержание', 'Метрика досмотра'],
  ['Раскачка', 'Долгое вялое начало — враг хука'],
  ['Визуальный крючок', 'Движение и необычный первый кадр']
  ],
  task:{
    intro:'Придумай хук на первые 3 секунды своего ролика: одна эмоция-крючок, обещание пользы или интрига, без раскачки.',
    chips:['Эмоция-крючок', 'Результат в первом кадре', 'Вопрос-интрига', 'Обещание пользы', 'Без приветствия', 'Визуальный крючок'],
    ph:'Первый кадр: пустой рекламный кабинет с нулём заявок крупным планом. Голос: «Вот так выглядел мой аккаунт неделю назад. А теперь смотри, что я поменял за один вечер». Дальше — резкий переход на экран с 47 заявками.',
    verdict:'Мощно: контраст «ноль заявок» против «47 за вечер» запускает и любопытство, и лёгкий страх узнавания — палец точно замрёт. Усилю: убери слова «Вот так» в начале, стартуй сразу с «Неделю назад у меня был ноль заявок» — чем раньше цифра, тем резче пощёчина.'
  }
},
{
  title:'Тизинг — дразнить, не отдавать',
  sub:'3:20 · слайды + тест + игра', dur:'3:20',
  c1:'ТИЗИНГ', c2:'ИНТРИГА',
  slides:[
  {t:'Что такое тизинг', pts:['<b>Тизинг</b> — дразнить, приоткрывать, но не отдавать всё сразу', 'Мозг не может бросить историю без развязки — это держит до конца', 'На продвижение влияет <b>глубина просмотра</b>: дольше смотрят — выше охваты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Механика интриги', pts:['Пообещай ценное впереди: «в конце покажу приём, который удвоил охваты»', 'Открытая петля: задай вопрос в начале, ответь в финале', 'Веди по нарастающей: каждый следующий кадр интереснее предыдущего'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M28 24h20a6 6 0 0 1 12 0h20v20a6 6 0 0 1 0 12v8H60a6 6 0 0 0-12 0H28V44a6 6 0 0 0 0-12z"/></svg>'},
  {t:'Дозируй информацию', pts:['Не вываливай всё в первые секунды — оставь причину досмотреть', '5-10 секунд подогрева интриги, дальше выдавай суть — <b>не пережать</b>', 'Слишком долго тянуть — зритель почувствует обман и уйдёт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h80L68 44v18l-16 8V44z"/></svg>'},
  {t:'Приёмы тизинга', pts:['Клиффхэнгер перед ключевым моментом: «а теперь самое важное…»', 'Показать результат, но не показать способ — способ в конце', 'Намёк вместо ответа: «есть один нюанс, о нём в конце»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Баланс: не пережать', pts:['Интрига без пользы бесит — всегда закрывай обещание', 'Не обманывай: обещал приём — покажи приём', 'Держи ритм: пауза для интриги короткая, ценность приходит вовремя'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'}
  ],
  quiz:[
  {q:'Что такое тизинг?', o:['Отдать всё сразу', 'Дразнить, приоткрывать, не отдавать всё', 'Скидка', 'Финал'], a:1},
  {q:'Что растёт от хорошего тизинга?', o:['Глубина просмотра и охваты', 'Цена', 'Число тегов', 'Длина ролика'], a:0},
  {q:'Сколько секунд подогревать интригу?', o:['1 секунду', '5-10 секунд, не пережимая', 'Весь ролик', 'Целую минуту'], a:1},
  {q:'Что такое открытая петля?', o:['Вопрос в начале — ответ в финале', 'Скидка', 'Музыка', 'Ошибка монтажа'], a:0},
  {q:'Чем опасно пережать интригу?', o:['Ничем', 'Зритель почувствует обман и уйдёт', 'Станет дороже', 'Замедлит загрузку'], a:1},
  {q:'Надо ли закрывать обещание из тизинга?', o:['Нет', 'Да, иначе интрига превращается в обман', 'Иногда', 'Только в рекламе'], a:1}
  ],
  pairs:[
  ['Тизинг', 'Дразнить, не отдавать всё сразу'],
  ['Открытая петля', 'Вопрос в начале, ответ в конце'],
  ['Глубина просмотра', 'Как долго смотрят ролик'],
  ['Клиффхэнгер', 'Обрыв перед ключевым моментом'],
  ['Пережать', 'Тянуть так долго, что уходят'],
  ['Дозировка', 'Выдавать информацию порциями']
  ],
  task:{
    intro:'Построй тизинг для своего ролика: открытая петля в начале, дозированный подогрев и честная развязка в конце.',
    chips:['Обещание в начале', 'Открытая петля', '5-10 сек подогрева', 'Клиффхэнгер', 'Развязка в финале', 'Не пережать'],
    ph:'Старт: «Из-за одной ошибки в шапке профиля ты теряешь половину подписчиков — и почти никто её не замечает». Середина: показываю три хороших профиля, но саму ошибку пока не называю. Финал: «А вот та самая ошибка — и как её исправить за минуту».',
    verdict:'Хорошо держит: открытая петля про «одну ошибку» и отложенная развязка тянут до конца, а обещание честно закрывается. Усилю: сократи середину до двух примеров — на коротком ролике три уже провисают, а глубина просмотра любит плотный темп.'
  }
},
{
  title:'Вовлечение через восприятие',
  sub:'3:40 · слайды + тест + игра', dur:'3:40',
  c1:'ЧУВСТВА', c2:'И РИТМ',
  slides:[
  {t:'Продаём через органы чувств', pts:['Мозг верит тому, что может <b>почувствовать</b>: вкус, запах, звук, текстуру', 'Абстракция «вкусный кофе» слабее, чем «горчинка и пар над чашкой утром»', 'Чем больше чувств задействуешь, тем ярче картинка в голове читателя'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Сенсорные детали в тексте', pts:['Опиши звук, запах, тактильность — читатель достраивает сцену сам', 'Конкретная деталь вместо общего слова: не «уютно», а «плед и запах корицы»', 'Одна яркая деталь заменяет три общих прилагательных'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M74 12 92 30 56 66l-18 4 4-18z"/><path d="M62 24 80 42" opacity=".7"/></svg>'},
  {t:'Динамика и ритм', pts:['Чередуй короткие и длинные фразы — текст дышит, как речь', '<b>Ритм</b> ведёт читателя: рубленые фразы ускоряют, длинные замедляют', 'Динамика в видео: смена планов и темпа под смысл и музыку'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M50 46V16l32-6v30"/><circle cx="44" cy="48" r="8"/><circle cx="76" cy="42" r="8"/></svg>'},
  {t:'Свой стиль и голос', pts:['Узнаваемый <b>стиль</b> — то, за чем возвращаются именно к тебе', 'Свои словечки, интонация, ритм отличают тебя от тысяч похожих', 'Пиши, как говоришь: живая речь вовлекает сильнее гладкого «журнального» текста'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="50" y="8" width="20" height="34" rx="10"/><path d="M40 36a20 20 0 0 0 40 0M60 56v8M48 64h24"/></svg>'},
  {t:'Вовлечение действием', pts:['Задай вопрос читателю прямо в тексте — он мысленно отвечает', 'Проси представить: «представь запах свежей выпечки» — он уже вовлечён', 'Микро-действия по ходу: «поставь + если узнал себя»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Ошибки вовлечения', pts:['Сухой текст без единой детали — воображению не за что зацепиться', 'Монотонный ритм: одинаковые длинные фразы усыпляют', 'Безликий стиль под всех — тебя не запоминают'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Почему сенсорные детали вовлекают?', o:['Мозг верит тому, что может почувствовать', 'Они короче', 'Так модно', 'Ради SEO'], a:0},
  {q:'Что сильнее?', o:['«вкусный кофе»', '«горчинка и пар над чашкой утром»', '«хороший напиток»', '«просто кофе»'], a:1},
  {q:'Как ритм влияет на текст?', o:['Никак', 'Чередование фраз ведёт и держит внимание', 'Только удлиняет', 'Мешает читать'], a:1},
  {q:'Зачем нужен свой стиль?', o:['За узнаваемым голосом возвращаются именно к тебе', 'Для объёма', 'Для тегов', 'Незачем'], a:0},
  {q:'Как вовлечь действием в тексте?', o:['Запретить комментарии', 'Задать вопрос или попросить представить', 'Убрать все детали', 'Писать сухо'], a:1},
  {q:'Что усыпляет читателя?', o:['Яркая деталь', 'Монотонный одинаковый ритм', 'Вопрос', 'Живая речь'], a:1}
  ],
  pairs:[
  ['Сенсорная деталь', 'Звук, запах, вкус в тексте'],
  ['Ритм', 'Чередование коротких и длинных фраз'],
  ['Свой стиль', 'Узнаваемый голос автора'],
  ['Динамика', 'Смена планов и темпа под смысл'],
  ['Вовлечение действием', 'Вопрос или просьба представить'],
  ['Конкретность', 'Деталь вместо общего слова']
  ],
  task:{
    intro:'Перепиши сухой фрагмент своего поста, добавив сенсорные детали, живой ритм и один приём вовлечения читателя.',
    chips:['Звук или запах', 'Тактильная деталь', 'Короткая рубленая фраза', 'Вопрос читателю', 'Просьба представить', 'Своё словечко'],
    ph:'Было: «У нас уютная кофейня с вкусным кофе». Стало: Заходишь — и в лицо пар, запах корицы, тихий джаз. Первый глоток: горчинка, потом сладость. Плечи сами опускаются. Представь, что так начинается твоё утро. Хочешь такое место рядом с домом? Поставь +.',
    verdict:'Живо и вкусно: пар, корица, джаз и «горчинка, потом сладость» рисуют сцену, а рубленый ритм и вопрос в конце втягивают в диалог. Усилю: добавь один свой фирменный оборот, который повторяется из поста в пост — так читатель начнёт узнавать тебя ещё до подписи.'
  }
}
  ],
  "k3": [
{
  title:'8 триггеров — карта',
  sub:'3:40 · слайды + тест + игра', dur:'3:40',
  c1:'ВОСЕМЬ', c2:'ТРИГГЕРОВ',
  slides:[
  {t:'Что такое триггер покупки', pts:['<b>Триггер</b> — эмоциональный крючок, который переводит из «интересно» в «беру сейчас»', 'Работает не на логике, а на <b>чувстве</b>: страх, зависть, азарт, желание быть своим', 'Триггер не создаёт потребность, а <b>усиливает</b> уже существующую', 'Без реальной пользы за постом любой крючок читается как манипуляция'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Восемь рабочих триггеров', pts:['<b>Толпа</b> (как все) и <b>эксклюзив</b> (не как все) — две стороны принадлежности', '<b>Дефицит</b> (осталось мало) и <b>халява</b> (бесплатно) — про упущенную выгоду', '<b>Авторитет</b> и <b>статистика</b> — весомое мнение и цифры вместо обещаний', '<b>Крутые кейсы</b> — «хочу так же», <b>страх</b> — «боюсь, что будет хуже»', '<b>Новизна</b> — быть первым, кто попробовал'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Почему триггеры вообще работают', pts:['Мозг экономит энергию и решает по <b>ярлыкам</b>, а не взвешивает всё', '«Раз все берут — значит, стоит» — <b>социальное доказательство</b> дешевле анализа', 'Страх потерять сильнее радости приобрести — это <b>неприятие потери</b>', 'Триггер даёт готовый повод сказать «да» прямо сейчас'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Триггер живёт на пользе', pts:['Сначала <b>ценность</b> в контенте, потом крючок — не наоборот', '«Осталось 2 места» на пустой продукт = разовая продажа и минус доверие', 'Пугать, торопить, вызывать зависть можно только там, где есть результат', 'Проверка: убери триггер — предложение всё ещё честное? Значит, можно усиливать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Сколько триггеров в одном посте', pts:['Оптимум — <b>2-3 триггера</b> на пост, не все восемь сразу', 'Перебор крючков превращает текст в базарный зазыв', 'Связка: один ведёт (основной), другие поддерживают', 'Разным аудиториям — разные триггеры: новичку халява, эксперту эксклюзив'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Как выбрать триггер под аудиторию', pts:['Смотри на <b>боль</b> и <b>мечту</b> сегмента, а не на то, что нравится тебе', 'Холодным — новизна и толпа, тёплым — кейсы и авторитет, горячим — дефицит', 'Тестируй: один оффер с разными крючками, сравнивай отклик', 'Реальную боль ниши достаёшь из комментариев и <b>Wordstat</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Что делает триггер?', o:['Создаёт потребность с нуля', 'Усиливает уже существующее желание и даёт повод купить сейчас', 'Заменяет пользу продукта', 'Снижает цену'], a:1},
  {q:'Сколько триггеров оптимально в одном посте?', o:['Все восемь', 'Ни одного', '2-3', 'Только один и всегда одинаковый'], a:2},
  {q:'Почему страх потери — сильный рычаг?', o:['Люди любят рисковать', 'Страх потерять сильнее радости приобрести', 'Он дешёвый', 'Он логичен'], a:1},
  {q:'На чём обязан стоять любой триггер?', o:['На скидке', 'На реальной пользе продукта', 'На красивой картинке', 'На длине текста'], a:1},
  {q:'Какой триггер ближе холодной аудитории?', o:['Жёсткий дефицит', 'Новизна и толпа', 'Личный кейс клиента', 'Статусный эксклюзив'], a:1}
  ],
  pairs:[
  ['Триггер', 'Эмоциональный крючок, ускоряющий решение купить'],
  ['Толпа', 'Беру, потому что берут все'],
  ['Эксклюзив', 'Беру, потому что это не для всех'],
  ['Дефицит', 'Беру, потому что скоро закончится'],
  ['Авторитет', 'Верю, потому что сказал весомый источник'],
  ['Новизна', 'Беру, чтобы быть первым']
  ],
  task:{
    intro:'Возьми свой продукт и распиши, какие 3 из 8 триггеров подходят твоей аудитории и почему.',
    chips:['Мой продукт: ...', 'Аудитория: ...', 'Триггер 1 и почему', 'Триггер 2 и почему', 'Триггер 3 и почему', 'Какая польза под ними'],
    ph:'Продукт: курс по таблицам для малого бизнеса. Аудитория: владельцы, тонут в хаосе. Триггер 1 — страх (теряешь деньги на ручном учёте). Триггер 2 — кейс (клиент сократил отчёт с 3 часов до 15 минут). Триггер 3 — халява (бесплатный шаблон на входе). Польза: реально экономит время.',
    verdict:'Хорошо, что связал каждый триггер с конкретной болью, а не взял их наугад. Усилю: под кейс добавь одну цифру результата — она делает «хочу так же» осязаемым.'
  }
},
{
  title:'Толпа и эксклюзив',
  sub:'3:20 · слайды + тест + игра', dur:'3:20',
  c1:'КАК ВСЕ', c2:'НЕ КАК ВСЕ',
  slides:[
  {t:'Две стороны принадлежности', pts:['<b>Толпа</b>: «раз все выбрали — безопасно» — снимает страх ошибки', '<b>Эксклюзив</b>: «это не для всех» — даёт статус и ощущение избранности', 'Оба про одно — где я в группе: со всеми или над всеми', 'Один сегмент хочет быть как все, другой — выделяться'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Как включить триггер толпы', pts:['Показывай <b>числа</b>: «уже 3 200 человек прошли», «500 отзывов»', 'Живые лица клиентов работают сильнее абстрактного «многие»', 'Очередь, аншлаг, «места разбирают» — визуальная толпа', 'Врать в цифрах нельзя: разоблачат в комментариях за минуту'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Как включить триггер эксклюзива', pts:['«Только для <b>своих</b>», закрытый чат, отбор по анкете', 'Ограничь вход не ценой, а <b>условием</b>: не всем продаём', 'Статусные детали: ручная сборка, лимитная серия, именной доступ', 'Эксклюзив дороже толпы — за него платят за чувство избранности'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 50h80l-6-30-20 16-14-24-14 24-20-16z"/><path d="M20 56h80" opacity=".7"/></svg>'},
  {t:'Когда толпа, а когда эксклюзив', pts:['Массовый недорогой продукт — жми на <b>толпу</b>', 'Премиум и личная работа — жми на <b>эксклюзив</b>', 'Новичок боится ошибиться → толпа успокаивает', 'Опытный устал быть как все → эксклюзив цепляет'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Ошибки обоих триггеров', pts:['Толпа без доказательств = пустое «нас выбирают тысячи»', 'Фейковые счётчики «онлайн: 47 человек» палятся и убивают доверие', 'Эксклюзив без реального барьера = обман: «клуб» с открытой дверью', 'Смешивать в лоб нельзя: «для избранных, но берут все» звучит фальшиво'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Формулировки, которые цепляют', pts:['Толпа: «Пока ты думаешь, 800 человек уже внутри»', 'Эксклюзив: «Мы берём не всех — сначала смотрим, подойдём ли»', 'Толпа через FOMO: «Все твои конкуренты уже это делают»', 'Черновики фраз гоняй через <b>ChatGPT</b> и отбирай живые'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'}
  ],
  quiz:[
  {q:'Триггер толпы снимает у покупателя прежде всего...', o:['Цену', 'Страх ошибиться, потому что «все уже выбрали»', 'Скуку', 'Недостаток времени'], a:1},
  {q:'Для какого продукта уместнее эксклюзив?', o:['Дешёвый массовый товар', 'Премиум и личная работа', 'Бесплатный лид-магнит', 'Товар повседневного спроса'], a:1},
  {q:'Что убивает триггер толпы?', o:['Реальные отзывы', 'Фейковые счётчики и накрутка', 'Живые лица клиентов', 'Точное число участников'], a:1},
  {q:'Эксклюзив требует...', o:['Максимально низкой цены', 'Реального барьера входа', 'Открытого доступа для всех', 'Большого рекламного бюджета'], a:1},
  {q:'Новичок, который боится ошибиться, лучше реагирует на...', o:['Эксклюзив', 'Толпу', 'Дефицит', 'Вину'], a:1}
  ],
  pairs:[
  ['Толпа', 'Безопасно, раз выбрали многие'],
  ['Эксклюзив', 'Статус, раз это не для всех'],
  ['Счётчик участников', 'Инструмент триггера толпы'],
  ['Отбор по анкете', 'Инструмент триггера эксклюзива'],
  ['FOMO', 'Страх остаться вне общего движения'],
  ['Лимитная серия', 'Материальный знак избранности']
  ],
  task:{
    intro:'Напиши два варианта анонса одного продукта: один на триггере толпы, второй на эксклюзиве.',
    chips:['Продукт: ...', 'Вариант «толпа»: цифра + соц.доказательство', 'Вариант «эксклюзив»: барьер входа', 'Кому какой ближе', 'Что честно, а что нет'],
    ph:'Продукт: наставничество по продажам. Толпа: «За год через разбор прошли 640 менеджеров, средний рост чека +30%». Эксклюзив: «Беру 6 человек в поток, только после созвона — смотрю, смогу ли реально помочь». Толпа — холодным, эксклюзив — тем, кто уже в теме.',
    verdict:'Сильно, что цифра в «толпе» подкреплена результатом, а не просто количеством. Добавь в «эксклюзив» одну деталь отбора — минимальный опыт кандидата, тогда барьер станет ощутимо настоящим.'
  }
},
{
  title:'Дефицит и халява',
  sub:'3:30 · слайды + тест + игра', dur:'3:30',
  c1:'ОСТАЛОСЬ', c2:'МАЛО',
  slides:[
  {t:'Дефицит и халява: два полюса выгоды', pts:['<b>Дефицит</b> давит на страх упустить: «скоро не будет»', '<b>Халява</b> снимает риск: «попробуй бесплатно, ничем не рискуешь»', 'Оба работают через <b>упущенную выгоду</b>, но с разных сторон', 'Дефицит торопит, халява снижает барьер первого шага'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Как честно создать дефицит', pts:['Ограничивай реально: число мест, срок, тираж — то, что правда конечно', '<b>Дедлайн</b> должен быть настоящим: сгорел — значит сгорел', 'Масштаб под размер аудитории: не «500 мест» при 100 подписчиках', 'Объясняй причину лимита: «беру 10, чтобы дать обратную связь каждому»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Виды дефицита', pts:['<b>Количественный</b>: осталось 3 места, 12 копий', '<b>Временной</b>: цена растёт в полночь, набор закрывается в пятницу', '<b>Бонусный</b>: подарок только первым 20', '<b>Условный</b>: доступ только тем, кто выполнил действие'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Халява как вход в воронку', pts:['Бесплатное — это <b>лид-магнит</b>, первый контакт, а не финал', 'Ценность халявы должна быть настоящей, иначе платному не поверят', '«Бесплатно» снимает страх → человек делает первый маленький шаг', 'За халявой всегда стоит следующий платный шаг воронки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="28" width="68" height="34" rx="6"/><path d="M26 38h68M60 28v34"/><path d="M60 28c-6-14-22-10-16-2 3 4 16 2 16 2zM60 28c6-14 22-10 16-2-3 4-16 2-16 2z"/></svg>'},
  {t:'Ошибки дефицита и халявы', pts:['Вечный «последний день» — аудитория перестаёт верить дедлайнам', 'Дефицит без причины выглядит как манипуляция', 'Халява без ценности обесценивает и платный продукт', '«Бесплатно», а внутри сплошная продажа — разочарование и отписки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Связка: халява ведёт к дефициту', pts:['Сначала бесплатный шаг, потом ограниченное платное предложение', '«Забрал гайд? Первым 15 — скидка до пятницы»', 'Халява греет, дефицит закрывает — классическая пара', 'Landing со счётчиком быстро поднимаешь на <b>Vercel</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'}
  ],
  quiz:[
  {q:'Каким должен быть дедлайн в дефиците?', o:['Плавающим, можно продлить', 'Настоящим: сгорел — значит сгорел', 'Максимально далёким', 'Секретным'], a:1},
  {q:'Что не так с «500 мест осталось» при 100 подписчиках?', o:['Слишком мало мест', 'Масштаб дефицита не совпадает с размером аудитории — не поверят', 'Это честно', 'Мест должно быть больше'], a:1},
  {q:'Роль халявы в воронке?', o:['Финальная продажа', 'Вход и первый контакт, снимающий риск', 'Замена платного продукта', 'Способ поднять цену'], a:1},
  {q:'Что убивает доверие к дефициту?', o:['Реальный тираж', 'Вечный «последний день»', 'Объяснённая причина лимита', 'Настоящий счётчик мест'], a:1},
  {q:'Хорошая связка двух триггеров здесь — это...', o:['Дефицит, потом халява', 'Халява греет — дефицит закрывает', 'Только халява', 'Только дефицит'], a:1}
  ],
  pairs:[
  ['Дефицит', 'Скоро закончится — решай сейчас'],
  ['Халява', 'Бесплатно — рискнуть не страшно'],
  ['Дедлайн', 'Временная граница предложения'],
  ['Лид-магнит', 'Бесплатная ценность на входе воронки'],
  ['Количественный лимит', 'Осталось N мест или копий'],
  ['Ложный дефицит', 'Вечный «последний день», убивающий доверие']
  ],
  task:{
    intro:'Собери честное ограниченное предложение: халява на входе + реальный дефицит на платном шаге.',
    chips:['Бесплатный шаг: ...', 'Платное предложение: ...', 'Тип дефицита', 'Реальная причина лимита', 'Дедлайн (дата/число)'],
    ph:'Халява: чек-лист «7 ошибок в рекламе», забирают в боте. Платное: разбор кабинета, беру 8 человек. Дефицит количественный. Причина: смотрю каждый аккаунт руками, больше 8 не вытяну по качеству. Дедлайн: набор до воскресенья 23:59, дальше поток закрыт.',
    verdict:'Отлично: лимит объяснён качеством, а не выдуман, и это считывается как забота, а не давление. Усиление — покажи, что будет с теми, кто не успел (следующий поток через месяц), тогда упущенная выгода станет ещё ощутимее.'
  }
},
{
  title:'Авторитет и статистика',
  sub:'3:25 · слайды + тест + игра', dur:'3:25',
  c1:'ВЕСОМОЕ', c2:'МНЕНИЕ',
  slides:[
  {t:'Почему цифры и авторитет убеждают', pts:['Конкретная <b>цифра</b> звучит правдивее любого прилагательного', 'Мнение <b>эксперта</b> экономит покупателю проверку — он доверяет вместо анализа', '«Хорошо» — пусто, «сократил расходы на 34%» — доказательство', 'Авторитет и статистика гасят главный барьер — недоверие'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V14M18 58h84"/><rect x="30" y="40" width="12" height="18"/><rect x="52" y="30" width="12" height="28"/><rect x="74" y="20" width="12" height="38"/></svg>'},
  {t:'Как показать авторитет', pts:['Регалии по делу: опыт, кейсы, годы, объёмы — а не пустые титулы', '<b>Заимствованный авторитет</b>: упоминание в СМИ, известные клиенты', 'Экспертный контент сам по себе строит авторитет лучше слов о себе', 'Признание нюансов и ошибок повышает доверие, а не роняет его'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 50h80l-6-30-20 16-14-24-14 24-20-16z"/><path d="M20 56h80" opacity=".7"/></svg>'},
  {t:'Как работать со статистикой', pts:['Бери <b>конкретные</b> числа: не «многие», а «7 из 10»', 'Источник обязателен — иначе цифра выглядит выдуманной', 'Своя статистика сильнее чужой: результаты твоих клиентов', 'Одна яркая цифра бьёт лучше, чем таблица из двадцати'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Как не соврать цифрами', pts:['Не вырывай процент из контекста: «+300%» с двух продаж до восьми — смешно', 'Средние прячут правду — показывай и типичный результат, и честный разброс', 'Округление в свою пользу читается как манипуляция', 'Проверяемая цифра сильнее красивой: её не страшно показать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Форматы подачи цифр', pts:['Сравнение «до/после» — самый наглядный формат', 'Одно число крупно на карточке — визуальный акцент', 'Мини-инфографика вместо абзаца текста', 'Простые графики быстро рисуешь через <b>ChatGPT</b> с кодом или в таблице'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Ошибки триггера', pts:['Регалии не в тему: «10 лет в маркетинге» продавцу тортов', 'Круглые «99% клиентов довольны» без источника — не верят', 'Перегруз цифрами: пять процентов в одном абзаце никто не удержит', 'Чужой авторитет без разрешения — юридический и репутационный риск'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'}
  ],
  quiz:[
  {q:'Что убедительнее в тексте?', o:['«Очень эффективно»', '«Сократил расходы на 34% за 2 месяца»', '«Многие довольны»', '«Лучшее решение»'], a:1},
  {q:'Обязательный спутник любой статистики — это...', o:['Красивый шрифт', 'Источник числа', 'Восклицательный знак', 'Круглая цифра'], a:1},
  {q:'Что такое заимствованный авторитет?', o:['Выдуманные регалии', 'Опора на известные СМИ, клиентов, признание', 'Чужие цифры без спроса', 'Титул без опыта'], a:1},
  {q:'Почему «+300%» бывает манипуляцией?', o:['Процент всегда врёт', 'За ним могут стоять смешные абсолютные числа (с 2 до 8)', 'Проценты нельзя использовать', '300 — некрасивое число'], a:1},
  {q:'Сколько цифр держать в одном акценте?', o:['Как можно больше', 'Одну яркую и проверяемую', 'Ровно десять', 'Ни одной'], a:1}
  ],
  pairs:[
  ['Авторитет', 'Верю, потому что сказал знающий'],
  ['Статистика', 'Верю, потому что есть цифра'],
  ['Заимствованный авторитет', 'Опора на известных клиентов и СМИ'],
  ['До/после', 'Наглядный формат подачи результата'],
  ['Голая цифра без источника', 'Главная причина недоверия к статистике'],
  ['+300% с двух продаж', 'Пример манипуляции процентом']
  ],
  task:{
    intro:'Возьми один свой результат и оформи его как доказательство: цифра + источник + контекст.',
    chips:['Результат (цифра): ...', 'Что было до', 'Что стало после', 'Источник/чем подтверждается', 'Формат подачи (до-после, карточка)'],
    ph:'Результат: заявки подешевели с 640 до 210 рублей. До: клиент лил на широкую аудиторию. После: сегментировал и переписал офферы за 3 недели. Подтверждение — скрин рекламного кабинета. Формат: карточка до/после с двумя цифрами.',
    verdict:'Здорово, что цифра подкреплена скрином — это снимает вопрос «а не выдумал ли». Усилю: добавь срок и нишу клиента, тогда результат перестанет быть абстрактным и читатель примерит его на себя.'
  }
},
{
  title:'Крутые кейсы «хочу так же»',
  sub:'3:35 · слайды + тест + игра', dur:'3:35',
  c1:'ХОЧУ', c2:'ТАК ЖЕ',
  slides:[
  {t:'Кейс продаёт результат, а не процесс', pts:['<b>Кейс</b> показывает: такой же человек получил желаемое — значит, и я смогу', 'Работает <b>белая зависть</b>: «хочу так же»', 'Продаёшь не свою работу, а <b>трансформацию</b> клиента', 'Чем ближе герой кейса к читателю, тем сильнее отклик'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 12h40v14a20 20 0 0 1-40 0z"/><path d="M40 18H26c0 12 8 16 16 16M80 18h14c0 12-8 16-16 16M60 46v10M46 62h28M52 56h16"/></svg>'},
  {t:'Структура сильного кейса', pts:['<b>Точка А</b>: боль и ситуация до — конкретно, с деталями', '<b>Путь</b>: что сделали, коротко, без хвастовства процессом', '<b>Точка Б</b>: измеримый результат с цифрой и сроком', '<b>Эмоция</b> клиента в финале — его слова, не твои'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Как выбрать героя кейса', pts:['Бери клиента, <b>похожего</b> на твою целевую аудиторию', 'Не самый громкий результат, а самый <b>достоверный</b> и близкий', '«Обычный человек добился» цепляет сильнее звёздного исключения', 'Если герой из той же ниши — читатель узнаёт себя мгновенно'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="24" r="11"/><path d="M40 58c1-13 9-20 20-20s19 7 20 20"/></svg>'},
  {t:'Детали, которые оживляют кейс', pts:['Имя, фото, реальная цитата вместо «один клиент»', 'Конкретика: сроки, суммы, шаги — а не «всё получилось»', 'Честные сложности по пути повышают доверие к результату', 'Скрины переписки и цифр — доказательство, что кейс настоящий'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Ошибки кейсов', pts:['Идеальный результат без единой трудности — не верят', 'Кейс про тебя, а не про клиента: «я такой молодец»', 'Абстрактный герой «одна девушка» — нет узнавания, нет доверия', 'Гигантский результат, недостижимый для читателя, отпугивает'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Как усилить «хочу так же»', pts:['Прямо назови: «если ты в такой же ситуации — результат реален и для тебя»', 'Покажи, что старт героя был <b>хуже</b>, чем у читателя', 'Свяжи кейс с конкретным шагом: «начни с того же, с чего начал он»', 'Черновик истории структурируй через <b>Claude</b>, факты — свои'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c4 12-6 16-6 26 0-6-5-9-5-9-4 6-11 11-11 22a22 22 0 0 0 44 0c0-14-12-20-16-39z"/></svg>'}
  ],
  quiz:[
  {q:'Что на самом деле продаёт кейс?', o:['Твою занятость', 'Трансформацию клиента из А в Б', 'Красивые слова о себе', 'Скидку'], a:1},
  {q:'Кого брать героем кейса?', o:['Самого звёздного клиента', 'Похожего на аудиторию, чтобы читатель узнал себя', 'Любого случайного', 'Себя'], a:1},
  {q:'Что повышает доверие к кейсу?', o:['Идеальный путь без проблем', 'Честные сложности по дороге', 'Отсутствие цифр', 'Абстрактный герой'], a:1},
  {q:'Почему «одна девушка добилась успеха» слабо работает?', o:['Слишком длинно', 'Нет узнавания и доказательств — некому верить', 'Девушки не покупают', 'Успех — плохое слово'], a:1},
  {q:'Как усилить «хочу так же»?', o:['Показать, что старт героя был хуже, чем у читателя', 'Сделать результат недостижимым', 'Убрать все детали', 'Говорить только о себе'], a:0}
  ],
  pairs:[
  ['Точка А', 'Ситуация и боль клиента до'],
  ['Точка Б', 'Измеримый результат после'],
  ['Белая зависть', 'Хочу так же, как у героя'],
  ['Похожий герой', 'Читатель узнаёт в нём себя'],
  ['Цитата клиента', 'Живое доказательство эмоции'],
  ['Идеальный кейс без трудностей', 'Причина недоверия к истории']
  ],
  task:{
    intro:'Напиши мини-кейс по структуре А → путь → Б с цифрой и цитатой клиента.',
    chips:['Герой (кто, ниша): ...', 'Точка А (боль): ...', 'Что сделали: ...', 'Точка Б (цифра+срок): ...', 'Цитата клиента', 'Одна честная сложность'],
    ph:'Герой: Марина, мастер маникюра, запись через директ, вечный хаос. А: теряла до 5 клиентов в неделю на несогласованных окошках. Сделали: подключили запись через бота за 4 дня. Б: за месяц ноль потерянных записей, +18 000 к выручке. Цитата: «Я наконец перестала жить в переписке». Сложность: первую неделю клиенты привыкали к боту.',
    verdict:'Отличный кейс: героиня узнаваема, результат в деньгах и со сроком, а честная сложность делает историю живой. Усилю: добавь строчку про старт Марины — если у неё не было ни бюджета, ни техзнаний, «хочу так же» сработает у каждого новичка.'
  }
},
{
  title:'Страх (пугай с заботой)',
  sub:'3:50 · слайды + тест + игра', dur:'3:50',
  c1:'ПУГАЙ', c2:'С ЗАБОТОЙ',
  slides:[
  {t:'Как устроен триггер страха', pts:['В основе — <b>страх потери</b>: потерять больнее, чем радостно приобрести', 'Человек покупает, чтобы избежать худшего, а не только ради лучшего', 'Страх без заботы = запугивание, вызывает отторжение', 'Правильный страх звучит как <b>предупреждение от своего</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Формула «пугай с заботой»', pts:['Назови <b>проблему</b> честно и покажи её реальные последствия', 'Усугуби: к чему приведёт бездействие, если не решать', 'Дай <b>выход</b>: как будет, когда проблема закрыта', 'Тон — забота, а не злорадство: «я предупреждаю, потому что вижу»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Адекватность масштабу', pts:['Страх должен совпадать с реальным <b>размером</b> проблемы', 'Раздувать мелочь до катастрофы — терять доверие', '«Потеряешь всё» из-за одной ошибки в посте — перебор', 'Честный масштаб: маленькая проблема — маленькая тревога'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Что усиливает, что разрушает', pts:['Усиливает: конкретный сценарий последствий, близкий читателю', 'Усиливает: показать, что цена бездействия растёт со временем', 'Разрушает: манипулятивное давление без выхода и решения', 'Разрушает: страх ради страха, когда за ним нет твоей помощи'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Показывай обе картины', pts:['Картина боли: что будет, если ничего не менять', 'Картина результата: что будет после решения', 'Контраст двух картин и есть мотивация к действию', 'Заканчивай на <b>картине результата</b>, а не на страхе'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Этика страха', pts:['Пугай только реальными рисками, а не выдуманными', 'За страхом всегда должно стоять честное решение', 'Не эксплуатируй глубокие травмы и уязвимость аудитории', 'Проверка: после поста человеку становится яснее, а не просто тревожнее'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'На чём стоит триггер страха?', o:['На жадности', 'На страхе потери: потерять больнее, чем приятно приобрести', 'На любопытстве', 'На скуке'], a:1},
  {q:'Что превращает страх в манипуляцию?', o:['Забота и выход', 'Отсутствие решения и давление ради давления', 'Честный масштаб', 'Реальные последствия'], a:1},
  {q:'На чём заканчивать пост со страхом?', o:['На картине боли', 'На картине результата после решения', 'На угрозе', 'На многоточии'], a:1},
  {q:'Почему «потеряешь всё из-за одной ошибки» плохо?', o:['Слишком длинно', 'Масштаб страха не совпадает с реальным размером проблемы', 'Ошибок не бывает', 'Всё нельзя потерять'], a:1},
  {q:'Главная проверка этичного страха?', o:['Человек напуган сильнее', 'После поста стало яснее, а не просто тревожнее', 'Пост длиннее', 'Больше восклицаний'], a:1}
  ],
  pairs:[
  ['Страх потери', 'Потерять больнее, чем приятно приобрести'],
  ['Пугай с заботой', 'Предупреждаю, потому что вижу и помогу'],
  ['Картина боли', 'Что будет, если не менять'],
  ['Картина результата', 'Что будет после решения'],
  ['Адекватность масштабу', 'Тревога под реальный размер проблемы'],
  ['Страх без выхода', 'Манипуляция, разрушающая доверие']
  ],
  task:{
    intro:'Напиши абзац по формуле «пугай с заботой»: проблема → последствия → выход, адекватно масштабу.',
    chips:['Проблема аудитории: ...', 'Реальное последствие бездействия', 'Как растёт цена со временем', 'Выход/решение', 'Тон заботы (фраза)'],
    ph:'Проблема: ведёшь учёт клиентов в голове и заметках. Последствие: забыл перезвонить — клиент ушёл к конкуренту, и так каждую неделю. Со временем: база растёт, дыр всё больше, теряешь уже не одного, а пятерых. Выход: простая CRM за вечер собирает все касания в одном месте. Тон: «Пишу это не пугать, а потому что сам так сливал заявки».',
    verdict:'Точно выдержан масштаб — теряешь клиентов, а не «жизнь рушится», и это читается честно. Усилю: добавь конкретную цифру потерянных денег за месяц, тогда картина боли станет осязаемой, а выход — очевидно выгодным.'
  }
},
{
  title:'Новизна — быть первым',
  sub:'3:15 · слайды + тест + игра', dur:'3:15',
  c1:'БЫТЬ', c2:'ПЕРВЫМ',
  slides:[
  {t:'Почему новизна цепляет', pts:['Человеку важно быть <b>первым</b>, кто попробовал и рассказал', 'Новое обещает <b>преимущество</b> перед теми, кто ещё не в теме', 'Мозг реагирует на новизну выбросом интереса — это встроено', '«Пока все спят — ты уже используешь» — суть триггера'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Что подать как новое', pts:['Новый <b>инструмент</b>, метод, формат, подход к старой задаче', 'Новизна не обязана быть глобальной — новое для твоей ниши уже цепляет', 'Свежий взгляд на привычное = тоже новизна', 'Тренд на раннем этапе — золото: зашёл до того, как приелось'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'},
  {t:'Новизна и ранний доступ', pts:['«Первым — <b>ранний доступ</b>», бета, закрытый запуск', 'Статус первопроходца: «ты был среди первых, кто внедрил»', 'Ранние пользователи становятся адвокатами бренда', 'Совмещается с эксклюзивом: новое + не для всех'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="35" r="14"/><path d="M48 35h44M78 35v12M92 35v10"/></svg>'},
  {t:'Новизна в теме ИИ', pts:['Поле <b>нейросетей</b> обновляется еженедельно — вечный источник новизны', '«Вышла новая модель — вот как её применить в твоём деле»', 'Разбор свежих <b>ChatGPT</b>, <b>Claude</b>, <b>Midjourney</b>, <b>Veo</b> собирает охваты', 'Кто первым показал применение — забирает внимание ниши'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="22" width="52" height="38" rx="10"/><circle cx="50" cy="41" r="5"/><circle cx="70" cy="41" r="5"/><path d="M60 22v-8M50 14h20"/><path d="M28 38v10M92 38v10" opacity=".7"/></svg>'},
  {t:'Ошибки триггера новизны', pts:['«Новое» без пользы = погоня за хайпом, быстро выгорает', 'Называть новым то, что давно у всех — теряешь экспертность', 'Успеть первым, но подать сыро — репутационный минус', 'Новизна ради новизны не удерживает: за ней должна быть ценность'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Как быстро ловить новое', pts:['Следи за источниками ниши и тестируй раньше остальных', 'Проверяй тренд спроса в <b>Wordstat</b> — растёт ли интерес', 'Публикуй быстро: первый разбор ценнее идеального, но позднего', 'Свяжи новинку с привычной болью — так новое становится нужным'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'}
  ],
  quiz:[
  {q:'Что обещает новизна покупателю?', o:['Скидку', 'Преимущество перед теми, кто ещё не в теме', 'Гарантию возврата', 'Толпу'], a:1},
  {q:'Обязательно ли новое быть глобальным?', o:['Да, только мировые открытия', 'Нет, новое для твоей ниши уже цепляет', 'Да, иначе не триггер', 'Только если это ИИ'], a:1},
  {q:'Почему тема ИИ — источник вечной новизны?', o:['Она сложная', 'Поле обновляется почти еженедельно — всегда есть свежее', 'Там нет конкуренции', 'Её никто не понимает'], a:1},
  {q:'Главный риск триггера новизны?', o:['Слишком много пользы', '«Новое» без ценности — хайп, который быстро выгорает', 'Аудитория взрослеет', 'Новизна дорогая'], a:1},
  {q:'С чем связать новинку, чтобы она сработала?', o:['С другой новинкой', 'С привычной болью аудитории', 'С длинным текстом', 'Ни с чем'], a:1}
  ],
  pairs:[
  ['Новизна', 'Быть первым, кто попробовал'],
  ['Ранний доступ', 'Статус первопроходца до остальных'],
  ['Тренд на старте', 'Зашёл, пока не приелось'],
  ['Новое для ниши', 'Не мировое открытие, но цепляет своих'],
  ['Хайп без пользы', 'Новизна, которая быстро выгорает'],
  ['Свежая модель ИИ', 'Постоянный повод для нового контента']
  ],
  task:{
    intro:'Возьми что-то новое (инструмент, метод, тренд) и свяжи его с привычной болью своей аудитории.',
    chips:['Что нового: ...', 'Почему это ново для ниши', 'Привычная боль аудитории', 'Как новое решает эту боль', 'Почему сейчас (пока не все)'],
    ph:'Новое: генерация озвучки роликов нейросетью локально, без подписок. Ново для ниши: блогеры до сих пор платят за голос или пишут сами. Боль: озвучка съедает время и бюджет. Решение: голос за минуту, бесплатно и без лимитов. Почему сейчас: конкуренты ещё не переключились — можно выйти вперёд по объёму контента.',
    verdict:'Хорошо связал новинку с реальной болью — время и деньги, а не «просто круто». Усилю: добавь «пока не все» в цифрах или примере, чтобы триггер первопроходца стал ощутимым — покажи, скольким это уже даёт фору.'
  }
},
{
  title:'Вина и вдохновение (аккуратно)',
  sub:'3:30 · слайды + тест + игра', dur:'3:30',
  c1:'ВИНА И', c2:'ВДОХНОВЕНИЕ',
  slides:[
  {t:'Два тонких триггера', pts:['<b>Вина</b> подталкивает через «ты сам недорабатываешь»', '<b>Вдохновение</b> тянет вперёд через образ желаемого будущего', 'Оба сильные, но легко скатываются в токсичность или пустой пафос', 'Правило блока: применять <b>аккуратно</b>, дозированно'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Как работает вина без токсичности', pts:['Не обвиняй личность, а называй <b>привычку</b>: «откладываешь — теряешь»', 'Вина работает как мягкое зеркало, а не как удар', 'Дай сразу выход, чтобы не осталось только тяжести', 'Грань: подтолкнуть к действию, а не вогнать в стыд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Опасная сторона вины', pts:['Давить на комплексы и внешность — за гранью, это вредит людям', 'Постоянная вина в контенте отталкивает и вызывает защиту', '«Ты плохой родитель, если не купишь» — токсично и недопустимо', 'Вина уместна редко и только вокруг действий, которые человек контролирует'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Как вдохновлять по-настоящему', pts:['Покажи <b>образ будущего</b>: кем читатель станет и как это ощущается', 'Опирай мечту на реальный путь, а не на пустой лозунг', 'Вдохновение = мечта плюс первый посильный шаг к ней', 'История преодоления вдохновляет сильнее призывов «ты всё сможешь»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l13 27 29 4-21 21 5 29-26-14-26 14 5-29-21-21 29-4z"/></svg>'},
  {t:'Ошибки вдохновения', pts:['Пустой пафос без конкретики быстро приедается', 'Недостижимый идеал вызывает не подъём, а бессилие', 'Вдохновение без шага = красиво, но не продаёт', 'Чужие цитаты вместо своей истории — не цепляют'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Баланс вины и вдохновения', pts:['Вина показывает цену бездействия, вдохновение — награду за движение', 'Здоровая связка: мягко про проблему → ярко про возможность', 'Заканчивай на <b>подъёме</b>, а не на чувстве вины', 'Спорные формулировки прогоняй через <b>ChatGPT</b> на токсичность'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'}
  ],
  quiz:[
  {q:'На что опирается вдохновение, чтобы продавать?', o:['Только на красивые слова', 'На образ будущего плюс первый посильный шаг', 'На чужие цитаты', 'На страх'], a:1},
  {q:'Где проходит граница здоровой вины?', o:['Обвинять личность и внешность', 'Называть привычку и сразу давать выход', 'Давить на комплексы', 'Внушать стыд'], a:1},
  {q:'Почему постоянная вина в контенте вредна?', o:['Она слишком мотивирует', 'Отталкивает и вызывает защитную реакцию', 'Делает посты короче', 'Повышает охваты'], a:1},
  {q:'Что делает вдохновение пустым?', o:['Конкретный шаг', 'Пафос без конкретики и достижимого пути', 'Реальная история', 'Образ будущего'], a:1},
  {q:'На какой ноте заканчивать связку вины и вдохновения?', o:['На вине', 'На подъёме и возможности', 'На страхе', 'На нейтрали'], a:1}
  ],
  pairs:[
  ['Вина', 'Мягкое зеркало привычки, а не удар по личности'],
  ['Вдохновение', 'Образ будущего плюс посильный шаг'],
  ['Токсичная вина', 'Давление на комплексы и внешность'],
  ['Пустой пафос', 'Вдохновение без конкретики и шага'],
  ['Здоровая связка', 'Цена бездействия → награда за движение'],
  ['Финал на подъёме', 'Заканчивать возможностью, а не виной']
  ],
  task:{
    intro:'Напиши короткий текст, где мягкая вина за бездействие переходит во вдохновляющий образ будущего с первым шагом.',
    chips:['Привычка-бездействие: ...', 'Мягкое зеркало (без обвинения личности)', 'Образ будущего: ...', 'Первый посильный шаг', 'Финал на подъёме'],
    ph:'Привычка: полгода «когда-нибудь начну блог». Зеркало: не ты ленивый — просто первый шаг кажется огромным, вот и стоишь. Будущее: через 3 месяца регулярных постов у тебя лояльная аудитория и первые заявки из контента. Шаг: сними один сторис сегодня, без монтажа. Финал: начинают не готовые — начинают те, кто снял первое видео сегодня.',
    verdict:'Тонко сделано: вина мягкая, направлена на привычку, а не на человека, и сразу переходит в подъём. Усилю: сделай образ будущего конкретнее — назови число постов или первую сумму заявок, чтобы мечта стала измеримой целью, а не настроением.'
  }
},
{
  title:'Связки триггеров в посте',
  sub:'4:00 · слайды + тест + игра', dur:'4:00',
  c1:'СВЯЗКИ', c2:'ТРИГГЕРОВ',
  slides:[
  {t:'Почему триггеры работают в связке', pts:['Один триггер цепляет, <b>связка</b> ведёт от внимания к покупке', 'Разные крючки закрывают разные сомнения читателя', '2-3 триггера на пост — рабочий максимум, не все восемь', 'Связка строится на <b>сценарии</b>, а не на куче крючков подряд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M48 42 72 18a13 13 0 0 1 18 18L74 52M72 28 48 52a13 13 0 0 1-18-18l16-16" opacity=".9"/></svg>'},
  {t:'Каркас поста под связку', pts:['<b>Крючок</b>: новизна или страх ловят внимание в первой строке', '<b>Тело</b>: польза плюс кейс и статистика как доказательство', '<b>Оффер</b>: дефицит и халява на этапе решения', '<b>CTA</b>: один ясный призыв, без пяти действий сразу'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Рабочие связки', pts:['<b>Страх + решение + дефицит</b>: пугнул, дал выход, ограничил срок', '<b>Кейс + толпа + халява</b>: результат, «таких много», бесплатный вход', '<b>Новизна + авторитет + ранний доступ</b>: свежее, весомо, первым', 'Ведущий триггер один, остальные — поддержка'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M28 24h20a6 6 0 0 1 12 0h20v20a6 6 0 0 1 0 12v8H60a6 6 0 0 0-12 0H28V44a6 6 0 0 0 0-12z"/></svg>'},
  {t:'Как не переборщить', pts:['Все восемь крючков в одном посте = базарный зазыв, доверие падает', 'Каждый триггер должен опираться на <b>реальную пользу</b>', 'Если триггеры спорят (толпа + эксклюзив в лоб) — конфликт', 'Убери любой крючок — пост всё ещё честный? Значит, связка здорова'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Порядок триггеров в тексте', pts:['Сначала — тот, что <b>цепляет внимание</b> (страх, новизна)', 'В середине — тот, что <b>доказывает</b> (кейс, статистика, авторитет)', 'Ближе к концу — тот, что <b>торопит</b> (дефицит, дедлайн)', 'Финал — призыв, усиленный ведущим триггером'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Сборка и тест связки', pts:['Определи <b>ведущий</b> триггер под цель поста и этап воронки', 'Добавь 1-2 поддерживающих, проверь, что не спорят', 'Прогони черновик и варианты через <b>Claude</b> или <b>ChatGPT</b>', 'Тестируй разные связки на одном оффере и смотри отклик'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'},
  {t:'Чек-лист готового поста', pts:['Есть польза, а не только крючки', 'Не больше 2-3 триггеров, ведущий понятен', 'Триггеры честные и адекватны масштабу', 'Один ясный <b>призыв к действию</b> в финале'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Сколько триггеров держать в одном посте?', o:['Все восемь', '2-3, с одним ведущим', 'Ровно один навсегда', 'Пять минимум'], a:1},
  {q:'Где в тексте место дефициту?', o:['В самом начале', 'Ближе к концу, на этапе решения', 'Вместо пользы', 'В заголовке всегда'], a:1},
  {q:'Какая связка конфликтна в лоб?', o:['Страх + решение', 'Кейс + халява', 'Толпа + эксклюзив одновременно и прямо', 'Новизна + авторитет'], a:2},
  {q:'Что цепляет внимание в первой строке?', o:['Дефицит', 'Страх или новизна', 'Призыв к действию', 'Реквизиты'], a:1},
  {q:'Главная проверка здоровой связки?', o:['Крючков как можно больше', 'Убери любой триггер — пост всё ещё честный', 'Все восемь на месте', 'Пять призывов в финале'], a:1}
  ],
  pairs:[
  ['Ведущий триггер', 'Главный крючок под цель поста'],
  ['Поддерживающий триггер', 'Закрывает дополнительное сомнение'],
  ['Крючок в первой строке', 'Страх или новизна ловят внимание'],
  ['Дефицит в финале', 'Торопит на этапе решения'],
  ['Толпа + эксклюзив в лоб', 'Конфликтная связка'],
  ['Один CTA', 'Ясный призыв без пяти действий']
  ],
  task:{
    intro:'Собери один пост-связку: выбери ведущий триггер и 1-2 поддерживающих, расставь их по каркасу крючок → доказательство → оффер → CTA.',
    chips:['Оффер поста: ...', 'Ведущий триггер', 'Поддерживающие 1-2', 'Крючок (первая строка)', 'Доказательство (кейс/цифра)', 'Оффер с ограничением + один CTA'],
    ph:'Оффер: мини-курс по Reels. Ведущий — страх (твои ролики тонут без структуры). Поддержка — кейс и дефицит. Крючок: «Снимаешь каждый день, а просмотров 200? Дело не в тебе». Доказательство: ученица вышла с 200 до 40 000 за 3 недели. Оффер: разбор + курс, старт-цена только до пятницы, дальше дороже. CTA: «Пиши «хочу» в комментариях».',
    verdict:'Крепкая связка: страх цепляет, кейс доказывает, дефицит закрывает, и всё держится на реальной пользе. Усилю: убедись, что дедлайн настоящий, и назови старт-цену цифрой — конкретика в оффере поднимает конверсию сильнее слова «дешевле».'
  }
}
  ],
  "k4": [
{
  title:'Прямые и косвенные конкуренты',
  sub:'3:20 · слайды + тест + игра', dur:'3:20',
  c1:'ПРЯМЫЕ', c2:'КОСВЕННЫЕ',
  slides:[
  {t:'Карта вместо интуиции', pts:['Фраза «у меня нет конкурентов» — это <b>слепая зона</b>, а не преимущество', 'Конкурент — любой, кто забирает <b>деньги и внимание</b> твоей аудитории', 'Анализ строится по двум осям: <b>товар</b> и <b>потребитель</b>', 'Без карты ты отстраиваешься не от тех и <b>теряешь позиционирование</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Две оси таблицы', pts:['Горизонталь — <b>товар</b>: такой же или другой', 'Вертикаль — <b>потребитель</b>: та же цель или другая', 'Пересечения дают четыре типа: <b>прямые</b>, косвенные, неявные, будущие', 'Этот урок — про первые два, где <b>товар совпадает</b> или близок'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Прямые конкуренты', pts:['Тот же <b>товар</b> и та же <b>цель клиента</b> — бьётесь за одного покупателя', 'Пример: два курса по <b>таргету ВК</b> для новичков с нуля', 'Клиент выбирает по <b>цене, доверию, кейсам</b>', 'Решает <b>отстройка</b>: чем ты конкретно полезнее этой нише', 'Изучай их <b>оферы и отзывы</b>, а не только ценник'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Косвенные конкуренты', pts:['Похожий товар, но <b>другая цель</b> клиента', 'Пример: курс по <b>таргету</b> и курс по <b>SMM-ведению</b> — оба про соцсети, результат разный', 'Клиент уходит к ним, если <b>перепутал</b> свою задачу', 'Твоя работа — объяснить, <b>какую именно</b> проблему закрываешь ты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Как собрать список', pts:['Вбей свой запрос в поиск и в <b>Wordstat</b> — смотри, кого показывают рядом', 'Пройди <b>путь клиента</b>: что он ищет до и после покупки', 'Собери по <b>5–7 игроков</b> в каждую категорию, не больше', 'Фиксируй <b>офер, цену, обещание</b> в одну таблицу'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Что делать с картой', pts:['Не копировать лидера, а найти <b>свободную клетку</b> на карте', 'Отстройка = «то же самое, но для <b>конкретного сегмента</b>»', 'Прямых обходишь <b>доверием</b>, косвенных — <b>ясностью офера</b>', 'Обновляй карту раз в <b>квартал</b> — рынок движется'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Прямой конкурент — это тот, у кого…', o:['тот же товар и та же цель клиента', 'другой товар, но та же потребность', 'похожий товар и другая цель', 'другой рынок целиком'], a:0},
  {q:'Косвенный конкурент отличается тем, что…', o:['продаёт то же самое дешевле', 'похожий товар, но другая цель клиента', 'работает в другой стране', 'вообще не берёт денег'], a:1},
  {q:'По каким осям строится таблица конкурентов?', o:['Цена и качество', 'Реклама и охваты', 'Товар и потребитель', 'Бренд и упаковка'], a:2},
  {q:'Сколько игроков разумно брать в одну категорию?', o:['1–2', '5–7', '30–40', 'всех подряд'], a:1},
  {q:'Чем эффективнее всего обходить прямых конкурентов?', o:['демпингом по цене', 'доверием и кейсами под нишу', 'агрессивной рекламой', 'копированием их курса'], a:1}
  ],
  pairs:[
  ['Прямой', 'Тот же товар, та же цель клиента'],
  ['Косвенный', 'Похожий товар, другая цель'],
  ['Ось X', 'Товар: такой же или другой'],
  ['Ось Y', 'Потребитель: та же цель или другая'],
  ['Отстройка', 'То же, но для конкретного сегмента'],
  ['Wordstat', 'Показывает, кого ищут рядом с тобой']
  ],
  task:{
    intro:'Составь мини-карту конкурентов своей ниши: назови по 3 прямых и 3 косвенных игрока и одну фразу отстройки.',
    chips:['Моя ниша', 'Прямые: тот же товар + цель', 'Косвенные: похожий товар, другая цель', 'Их главный офер', 'Их слабое место', 'Моя отстройка'],
    ph:'Ниша: монтаж Reels для экспертов. Прямые: 3 монтажёра с той же услугой на Kwork. Косвенные: курсы «сам смонтируешь» и SMM-агентства «под ключ». Отстройка: не просто монтаж, а Reels под конкретную нишу психологов с гарантией 3 правок.',
    verdict:'Сильно: ты развёл прямых и косвенных по цели клиента и не свалил всех в кучу. Усилю одним ходом — добавь к отстройке измеримое обещание (срок сдачи или результат в цифрах), тогда клиенту будет не с чем сравнивать по цене.'
  }
},
{
  title:'Неявные, будущие, партнёры',
  sub:'3:35 · слайды + тест + игра', dur:'3:35',
  c1:'НЕЯВНЫЕ', c2:'ПАРТНЁРЫ',
  slides:[
  {t:'Три невидимых слоя', pts:['Прямые и косвенные видны сразу, а <b>деньги утекают</b> к невидимым', 'Неявные, будущие и партнёры — те, кого новичок <b>не считает</b> конкурентами', 'Именно они забирают <b>бюджет клиента</b> в самый нужный момент', 'Разберём каждый слой и как его <b>использовать в свою пользу</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Неявные конкуренты', pts:['Другой <b>товар</b>, но та же <b>потребность</b> клиента', 'Пример: человек хочет «больше клиентов» — и выбирает между <b>таргетом</b> и <b>сарафаном через нетворкинг</b>', 'Ты конкурируешь не с курсом, а со <b>способом решить задачу</b>', 'Опасны тем, что клиент даже <b>не сравнивает</b> тебя с ними напрямую', 'Побеждаешь, показывая, что твой путь <b>быстрее или дешевле</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'},
  {t:'Будущие конкуренты', pts:['Сегодня их нет, но рынок <b>притягивает</b> новых игроков', 'Как только ниша показала <b>деньги</b> — приходят сильные с бюджетом', 'Пример: ты первый делаешь <b>ИИ-аватары</b>, через год их делают все', 'Защита — <b>скорость, репутация и база клиентов</b>, а не секрет метода'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v16l11 7"/></svg>'},
  {t:'Конкуренты-партнёры', pts:['Тот, кто закрывает <b>соседний этап</b>, а не тот же самый', 'Пример: ты пишешь тексты, партнёр делает <b>дизайн</b> — вы обмениваетесь клиентами', 'Партнёрство даёт <b>тёплые лиды</b> без затрат на рекламу', 'Косвенный конкурент часто и есть <b>лучший партнёр</b>', 'Договорись о <b>взаимных рекомендациях</b> с процентом или бартером'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'Как их находить', pts:['Спроси клиента: «А что вы <b>рассматривали ещё</b>?» — так всплывают неявные', 'Смотри <b>смежные ниши</b> в <b>Wordstat</b> и в соседних запросах', 'Будущих ищи по <b>трендам</b>: где растёт спрос, туда придут игроки', 'Партнёров ищи среди тех, кто работает <b>до и после</b> тебя'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Собери экосистему', pts:['Неявных — <b>перебивай ценностью</b>, а не спором', 'Будущих — опережай <b>скоростью и лояльной базой</b>', 'Партнёров — превращай в <b>канал тёплых лидов</b>', 'Раз в месяц пересматривай, кто перешёл <b>из партнёров в прямые</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="16" r="7"/><circle cx="26" cy="52" r="7"/><circle cx="94" cy="52" r="7"/><path d="M56 22 30 46M64 22l26 24M33 52h54" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Неявный конкурент — это тот, кто…', o:['продаёт тот же товар той же аудитории', 'закрывает ту же потребность другим товаром', 'ещё не вышел на рынок', 'делает соседний этап работы'], a:1},
  {q:'Чем защищаются от будущих конкурентов?', o:['секретностью метода', 'скоростью, репутацией и базой клиентов', 'снижением цены до нуля', 'отказом от рекламы'], a:1},
  {q:'Кто чаще всего становится хорошим партнёром?', o:['прямой конкурент', 'косвенный конкурент из смежного этапа', 'клиент', 'случайный подрядчик'], a:1},
  {q:'Какой вопрос вскрывает неявных конкурентов?', o:['«Сколько у вас бюджет?»', '«А что вы рассматривали ещё?»', '«Когда начнём?»', '«Кто вам меня посоветовал?»'], a:1},
  {q:'Почему будущие конкуренты появляются в нише?', o:['из-за рекламы', 'потому что ниша показала деньги', 'по требованию клиентов', 'случайно'], a:1}
  ],
  pairs:[
  ['Неявный', 'Другой товар, та же потребность'],
  ['Будущий', 'Придёт, когда ниша покажет деньги'],
  ['Партнёр', 'Закрывает соседний этап работы'],
  ['Защита от будущих', 'Скорость, репутация, база'],
  ['Экосистема', 'Партнёр как канал тёплых лидов'],
  ['Смежная ниша', 'Где искать неявных через Wordstat']
  ],
  task:{
    intro:'Найди по одному неявному конкуренту, одному будущему и одному потенциальному партнёру в своей нише. Объясни выбор.',
    chips:['Потребность клиента', 'Неявный: другой способ решить её', 'Будущий: кто придёт на деньги', 'Партнёр: соседний этап', 'Что предложу партнёру', 'Мой риск от будущего'],
    ph:'Ниша: настройка Telegram-ботов. Неявный: клиент вместо бота нанимает живого менеджера. Будущий: конструкторы ботов с ИИ, где не нужен исполнитель. Партнёр: таргетолог, который приводит трафик, — отдаю ему 10% за рекомендации.',
    verdict:'Отлично, что ты увидел неявного в живом менеджере — это ход зрелого маркетолога. Усилю: по будущему конкуренту сразу заложи стратегию — переходи от «настрою бота» к «настрою и веду бота с аналитикой», чтобы конструктор тебя не вытеснил.'
  }
},
{
  title:'Принцип «немного пользы»',
  sub:'3:45 · слайды + тест + игра', dur:'3:45',
  c1:'НЕМНОГО', c2:'ПОЛЬЗЫ',
  slides:[
  {t:'Ошибка щедрого новичка', pts:['Новичок думает: дам <b>максимум бесплатно</b> — и меня полюбят', 'На деле клиент <b>наедается бесплатным</b> и не доходит до покупки', 'Ты обесцениваешь <b>собственную экспертизу</b> своими руками', 'Щедрость без границ — это <b>слив</b>, а не маркетинг'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="28" width="68" height="34" rx="6"/><path d="M26 38h68M60 28v34"/><path d="M60 28c-6-14-22-10-16-2 3 4 16 2 16 2zM60 28c6-14 22-10 16-2-3 4-16 2-16 2z"/></svg>'},
  {t:'Что такое немного пользы', pts:['Дай <b>кусочек результата</b>, который реально работает', 'Это <b>тест-драйв</b>: клиент почувствовал вкус, но задача не закрыта', 'Бесплатное показывает <b>«что делать»</b>, платное отвечает <b>«как именно»</b>', 'Цель — не облагодетельствовать, а <b>доказать компетентность</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'},
  {t:'Где проходит граница', pts:['Бесплатно — <b>один быстрый результат</b> или разбор одной ошибки', 'Платно — <b>система, сопровождение и глубина</b>', 'Правило: после бесплатного клиент должен захотеть <b>больше</b>, а не «мне хватит»', 'Если после лид-магнита нечего продать — <b>ты отдал ядро</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Как упаковать тест-драйв', pts:['Мини-аудит, чек-лист или <b>один рабочий приём</b> за подписку', 'Или символический чек <b>100–300 ₽</b> — отсекает халявщиков', 'Платящий даже 1 рубль клиент <b>ценит и слушает</b> в разы сильнее', 'Собери такой актив в <b>ChatGPT</b> за час и тестируй'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 35 46 12h30a8 8 0 0 1 8 8v30L56 78z" transform="scale(.85) translate(6 -4)"/><circle cx="66" cy="27" r="5"/></svg>'},
  {t:'Мостик к платному', pts:['В конце бесплатного — честно: <b>«вот что делать дальше»</b>', 'Покажи <b>масштаб задачи</b>, который в одиночку не потянуть', 'Плати́т тот, кому <b>нужен результат</b>, а не просто информация', 'Не дави — <b>логично продолжи</b> путь клиента в платный продукт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'}
  ],
  quiz:[
  {q:'В чём типичная ошибка новичка с бесплатным?', o:['берёт слишком дорого', 'отдаёт максимум пользы и клиенту хватает', 'совсем ничего не даёт', 'продаёт только премиум'], a:1},
  {q:'Задача бесплатного продукта — это…', o:['закрыть проблему клиента целиком', 'дать вкус результата и доказать компетентность', 'собрать как можно больше контактов любой ценой', 'заменить платный продукт'], a:1},
  {q:'Почему полезен символический чек 100–300 ₽?', o:['это основной заработок', 'он отсекает халявщиков и повышает ценность', 'так требует закон', 'чтобы окупить рекламу'], a:1},
  {q:'Что должен почувствовать клиент после бесплатного?', o:['«мне уже хватило»', '«хочу больше, но сам не потяну»', '«здесь ничего нет»', '«дорого»'], a:1},
  {q:'Признак, что ты отдал слишком много бесплатно:', o:['клиенты не приходят', 'после лид-магнита нечего продать', 'много подписчиков', 'высокая цена'], a:1}
  ],
  pairs:[
  ['Немного пользы', 'Кусочек результата как тест-драйв'],
  ['Бесплатное', 'Показывает «что делать»'],
  ['Платное', 'Отвечает «как именно» и ведёт'],
  ['Символический чек', 'Отсекает халявщиков'],
  ['Ошибка новичка', 'Отдать ядро продукта даром'],
  ['Мостик', 'Логичное продолжение в платный продукт']
  ],
  task:{
    intro:'Придумай тест-драйв по принципу «немного пользы»: что дашь бесплатно и что оставишь за платным продуктом.',
    chips:['Мой продукт', 'Бесплатно: один результат', 'Платно: система и ведение', 'Формат теста (чек-лист/аудит)', 'Символический чек?', 'Фраза-мостик к покупке'],
    ph:'Продукт: настройка рекламы у психологов. Бесплатно: мини-аудит профиля и 3 ошибки в оформлении за 200 ₽. Платно: полная настройка + ведение месяц. Мостик: «ошибки нашли, но чтобы пошли заявки — нужна связка оффер + аудитория + тексты, это уже работа под ключ».',
    verdict:'Хорошо, что бесплатное у тебя — конкретный аудит, а не «полезный пост ни о чём». Усилю: поставь на мини-аудит именно платные 200 ₽, а не подписку — платящий клиент дослушает тебя до оффера и купит основное в разы охотнее.'
  }
},
{
  title:'Каскадность продукта',
  sub:'3:50 · слайды + тест + игра', dur:'3:50',
  c1:'КАСКАД', c2:'ПРОДУКТА',
  slides:[
  {t:'Покупка рождает потребность', pts:['Каждая покупка создаёт <b>новую задачу</b>, а значит — новый спрос', 'Купил сайт — нужна <b>реклама</b>, пошла реклама — нужен <b>отдел продаж</b>', 'Клиент движется по <b>лестнице задач</b>, и на каждой ступени есть деньги', 'Твоя цель — <b>встроиться</b> в этот каскад, а не продать разово'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Логика каскада', pts:['Результат одного этапа <b>обнажает</b> узкое место следующего', 'Продал <b>Reels</b> — клиенту нужен <b>прогрев и продажи</b> с этих просмотров', 'Ты либо закрываешь этап <b>сам</b>, либо отдаёшь <b>партнёру</b> за процент', 'Каскад = <b>предсказуемый</b> повторный доход с одного клиента'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Начинай с востребованного', pts:['Вход в каскад — через то, что покупают <b>легко и часто</b>', 'Не с премиума, а с <b>понятной первой ступени</b>', 'Пример: сначала <b>монтаж</b>, потом ведение, потом стратегия', 'Первый простой продукт <b>открывает дверь</b> ко всей линейке'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'LTV вместо разовой сделки', pts:['Один клиент в каскаде приносит <b>в разы больше</b>, чем разовая продажа', 'Считай <b>LTV</b> — сколько денег даст клиент за всё время', 'Дешевле <b>удержать и допродать</b>, чем найти нового', 'Каскад превращает клиента в <b>источник дохода на месяцы</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Собери свою лестницу', pts:['Выпиши <b>путь клиента</b> от первой боли до финального результата', 'Отметь, где возникает <b>следующая потребность</b>', 'Реши: закрываешь <b>сам</b> или через <b>партнёра</b>', 'Выстрой предложения так, чтобы каждое <b>вело к следующему</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="12" width="60" height="14" rx="4"/><rect x="30" y="30" width="60" height="14" rx="4" opacity=".85"/><rect x="30" y="48" width="60" height="14" rx="4" opacity=".7"/></svg>'}
  ],
  quiz:[
  {q:'Что лежит в основе каскадности продукта?', o:['скидки на всё', 'покупка рождает новую потребность', 'реклама на всех площадках', 'один дорогой продукт'], a:1},
  {q:'С чего правильно начинать вход в каскад?', o:['с самого дорогого продукта', 'с востребованного и понятного первого шага', 'с бесплатного навсегда', 'с премиум-ведения'], a:1},
  {q:'LTV — это…', o:['стоимость рекламы', 'сколько клиент приносит за всё время', 'число подписчиков', 'цена одного продукта'], a:1},
  {q:'Если ты не закрываешь следующий этап сам, то…', o:['теряешь клиента навсегда', 'отдаёшь его партнёру за процент', 'снижаешь цену', 'закрываешь бизнес'], a:1},
  {q:'Главная выгода каскада для тебя:', o:['разовая крупная сделка', 'предсказуемый повторный доход с клиента', 'много бесплатных заявок', 'известность'], a:1}
  ],
  pairs:[
  ['Каскад', 'Покупка рождает новую потребность'],
  ['Первая ступень', 'Востребованное и понятное — легко купить'],
  ['LTV', 'Доход с клиента за всё время'],
  ['Партнёр', 'Закрывает этап, который не делаешь сам'],
  ['Удержание', 'Дешевле, чем поиск нового клиента'],
  ['Лестница', 'Каждое предложение ведёт к следующему']
  ],
  task:{
    intro:'Построй каскад из 3 ступеней для своей услуги: что покупают сначала и какие потребности это рождает дальше.',
    chips:['Первая ступень (легко купить)', 'Потребность после неё', 'Вторая ступень', 'Потребность после неё', 'Третья ступень', 'Что отдам партнёру'],
    ph:'Услуга: контент для эксперта. 1) Монтаж 10 Reels — рождает потребность в стабильном потоке. 2) Ведение канала под ключ — рождает потребность продавать с охватов. 3) Воронка и прогрев в сторис. Аналитику рекламы отдаю партнёру-таргетологу за 15%.',
    verdict:'Сильная лестница: каждая ступень логично вытекает из предыдущей, а не навязана. Усилю: посчитай LTV этой цепочки хотя бы грубо (10к + 40к + 60к), увидишь, что первый дешёвый монтаж — это не заработок, а вход в клиента на 100к+.'
  }
},
{
  title:'Проверка спроса (Wordstat)',
  sub:'3:30 · слайды + тест + игра', dur:'3:30',
  c1:'ПРОВЕРКА', c2:'СПРОСА',
  slides:[
  {t:'Не угадывай — проверяй', pts:['Новичок придумывает продукт «из головы» и <b>вкладывается в тишину</b>', 'Спрос нельзя чувствовать — его нужно <b>измерить</b>', '<b>Wordstat</b> показывает, сколько людей <b>уже ищут</b> решение', 'Проверка спроса до старта экономит <b>месяцы и деньги</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Что такое Wordstat', pts:['Бесплатный сервис Яндекса про <b>частотность запросов</b>', 'Вводишь фразу — видишь, сколько раз её <b>искали за месяц</b>', 'Показывает <b>смежные</b> и <b>уточняющие</b> запросы рядом', 'Это витрина <b>реальных желаний</b> рынка, а не твоих гипотез'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Как читать цифры', pts:['Смотри не только частоту, но и <b>намерение</b> за запросом', '«Как настроить рекламу» — учатся сами, «настроить рекламу под ключ» — <b>готовы платить</b>', 'Отделяй <b>горячие</b> коммерческие запросы от информационных', 'Кавычки и операторы уточняют <b>точную</b> частоту фразы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V14M18 58h84"/><rect x="30" y="40" width="12" height="18"/><rect x="52" y="30" width="12" height="28"/><rect x="74" y="20" width="12" height="38"/></svg>'},
  {t:'Оценка ниши', pts:['Считай <b>сезонность</b>: смотри динамику за год, а не один месяц', 'Растущий тренд — <b>заходи</b>, падающий — <b>подумай</b>', 'Слишком узкий спрос — рынка нет; слишком широкий — <b>высокая конкуренция</b>', 'Идеал — <b>стабильный спрос</b> с понятным коммерческим намерением'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'От спроса к продукту', pts:['Формулируй продукт <b>словами клиента</b> из запросов', 'Бери запрос с явным намерением купить и <b>делай под него офер</b>', 'Проверяй <b>несколько формулировок</b> — люди ищут по-разному', 'Спрос подтверждён цифрами → можно <b>вкладываться в трафик</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Зачем проверять спрос перед запуском?', o:['чтобы не вкладываться в то, что никто не ищет', 'чтобы поднять цену', 'для красивой презентации', 'чтобы обойти налоги'], a:0},
  {q:'Что показывает Wordstat?', o:['доход конкурентов', 'частотность поисковых запросов', 'возраст аудитории', 'стоимость рекламы'], a:1},
  {q:'Какой запрос ближе к готовности платить?', o:['«как настроить рекламу самому»', '«настроить рекламу под ключ»', '«что такое таргет»', '«реклама это»'], a:1},
  {q:'Почему важно смотреть динамику за год?', o:['из-за сезонности спроса', 'чтобы узнать конкурентов', 'это требование Яндекса', 'ради красивого графика'], a:0},
  {q:'Как лучше формулировать продукт?', o:['умными терминами', 'словами клиента из его запросов', 'как у лидера рынка', 'максимально широко'], a:1}
  ],
  pairs:[
  ['Wordstat', 'Частотность запросов за месяц'],
  ['Информационный запрос', '«как сделать самому» — учатся'],
  ['Коммерческий запрос', '«под ключ» — готовы платить'],
  ['Сезонность', 'Динамика спроса за год'],
  ['Узкий спрос', 'Рынка почти нет'],
  ['Слова клиента', 'Основа формулировки офера']
  ],
  task:{
    intro:'Проверь спрос на свою услугу: подбери 3 запроса в Wordstat и определи, где коммерческое намерение, а где информационное.',
    chips:['Моя ниша', 'Запрос 1 + частота', 'Запрос 2 + частота', 'Коммерческий или инфо', 'Сезонность есть?', 'Вывод: заходить или нет'],
    ph:'Ниша: монтаж Shorts. Запросы: «монтаж shorts под ключ» (коммерческий, ~1500/мес), «как монтировать shorts» (инфо, ~8000/мес), «монтажёр shorts цена» (горячий, ~600/мес). Вывод: делаю офер под «под ключ» и «цена», обучающий трафик игнорирую — они не платят.',
    verdict:'Верно поймал главное: высокая частота инфо-запроса не значит спрос на услугу, платят по коммерческим. Усилю: возьми ещё пару синонимов («редактор shorts», «нарезка shorts») — часто основной денежный спрос прячется в формулировке, до которой конкуренты не додумались.'
  }
},
{
  title:'Критерии успеха',
  sub:'3:25 · слайды + тест + игра', dur:'3:25',
  c1:'КРИТЕРИИ', c2:'УСПЕХА',
  slides:[
  {t:'Что считать успехом', pts:['Успех — не «мне кажется, стало лучше», а <b>измеримый факт</b>', 'Два честных критерия: <b>рост статистики</b> и <b>рост дохода</b>', 'Всё остальное — <b>ощущения</b>, которые обманывают', 'Без критериев ты <b>не отличишь</b> рабочую гипотезу от слива'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 12h40v14a20 20 0 0 1-40 0z"/><path d="M40 18H26c0 12 8 16 16 16M80 18h14c0 12-8 16-16 16M60 46v10M46 62h28M52 56h16"/></svg>'},
  {t:'Рост статистики', pts:['Смотри <b>динамику</b>: охваты, заявки, конверсию, подписки', 'Один хороший день ничего не значит — важен <b>тренд</b>', 'Сравнивай период с периодом, а не с <b>вчерашним настроением</b>', 'Цифры вверх при том же бюджете = гипотеза <b>работает</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Рост дохода', pts:['Финальный судья — <b>деньги в кассе</b>, а не лайки', 'Охваты без продаж — это <b>красивый тупик</b>', 'Считай не выручку, а <b>прибыль</b> после расходов', 'Первые реальные деньги важнее <b>тысячи комплиментов</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Мнения не из ЦА — игнор', pts:['Друзья, коллеги, случайные комментаторы — <b>не твои клиенты</b>', 'Их «мне не нравится» ничего не говорит о <b>спросе</b>', 'Слушай только тех, кто <b>платит или готов платить</b>', 'Обратную связь бери <b>из поведения ЦА</b>, а не из чужих вкусов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h80L68 44v18l-16 8V44z"/></svg>'},
  {t:'Как принимать решение', pts:['Задай метрику <b>заранее</b>, до запуска гипотезы', 'Дай ей <b>достаточно данных</b>, не выключай на второй день', 'Растёт статистика и доход — <b>масштабируй</b>', 'Стоит на месте при вложениях — <b>меняй</b>, не влюбляйся в идею'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Какие два критерия успеха главные?', o:['лайки и комментарии', 'рост статистики и рост дохода', 'похвала коллег и подписчики', 'красота оформления'], a:1},
  {q:'Почему охваты без продаж — тупик?', o:['их сложно посчитать', 'внимание есть, а денег нет', 'они быстро падают', 'так говорит алгоритм'], a:1},
  {q:'Чьё мнение стоит игнорировать?', o:['платящих клиентов', 'людей не из твоей ЦА', 'целевой аудитории', 'заказчиков'], a:1},
  {q:'Когда пора масштабировать гипотезу?', o:['если она нравится тебе', 'когда растут и статистика, и доход', 'после первого удачного дня', 'когда хвалят друзья'], a:1},
  {q:'Когда задавать метрику успеха?', o:['после провала', 'заранее, до запуска', 'в конце месяца', 'никогда'], a:1}
  ],
  pairs:[
  ['Рост статистики', 'Динамика охватов и заявок, тренд'],
  ['Рост дохода', 'Прибыль в кассе, финальный судья'],
  ['Охваты без продаж', 'Красивый тупик'],
  ['Мнение не из ЦА', 'Игнор, это не спрос'],
  ['Метрика заранее', 'Ставится до запуска гипотезы'],
  ['Влюблённость в идею', 'Главный враг честной оценки']
  ],
  task:{
    intro:'Задай критерии успеха для ближайшей гипотезы: какую метрику и какой доход считаешь успехом, и чьи мнения отсекаешь.',
    chips:['Гипотеза', 'Метрика статистики', 'Целевой доход', 'Срок проверки', 'Чьё мнение игнорирую', 'Что сделаю, если не сработает'],
    ph:'Гипотеза: серия Reels-разборов приведёт заявки. Метрика: рост заявок в директ с 2 до 10 в неделю. Доход: минимум 2 продажи по 30к за месяц. Срок: 3 недели. Игнорирую комментарии знакомых «не твоё». Если ноль — меняю оффер, а не бросаю формат.',
    verdict:'Чётко: у тебя есть и цифра статистики, и денежный порог, и срок — это уже система, а не надежда. Усилю: заранее пропиши, что именно будешь менять при провале (оффер, а не формат), чтобы в момент разочарования не снести всё сгоряча.'
  }
},
{
  title:'Продуктовая линейка',
  sub:'4:00 · слайды + тест + игра', dur:'4:00',
  c1:'ЛИНЕЙКА', c2:'ПРОДУКТА',
  slides:[
  {t:'Один продукт — потолок дохода', pts:['С одним продуктом ты теряешь тех, кому <b>дорого</b> или <b>мало</b>', 'Линейка ловит клиента на <b>его уровне бюджета</b>', 'Разные люди готовы платить <b>разные деньги</b> за один результат', 'Задача — дать вход <b>каждому</b> и путь наверх'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="12" width="60" height="14" rx="4"/><rect x="30" y="30" width="60" height="14" rx="4" opacity=".85"/><rect x="30" y="48" width="60" height="14" rx="4" opacity=".7"/></svg>'},
  {t:'Минимум', pts:['Самый доступный продукт — <b>низкий порог входа</b>', 'Даёт <b>первый результат</b> и знакомит с тобой', 'Пример: <b>разбор</b> или базовая настройка за небольшой чек', 'Его задача — не заработать, а <b>завести клиента</b> в линейку'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 35 46 12h30a8 8 0 0 1 8 8v30L56 78z" transform="scale(.85) translate(6 -4)"/><circle cx="66" cy="27" r="5"/></svg>'},
  {t:'Стандарт', pts:['<b>Основной продукт</b>, на котором держится доход', 'Полное решение задачи <b>без излишеств</b>', 'Сюда ведёшь <b>большинство</b> клиентов', 'Оптимум <b>цена/результат</b> — то, что покупают чаще всего'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Премиум', pts:['Максимум <b>глубины, скорости и внимания</b>', 'Личное участие, приоритет, <b>гарантии и эксклюзив</b>', 'Даже редкие покупки <b>сильно поднимают</b> средний чек', 'Премиум <b>заякоряет</b> цену: рядом с ним стандарт кажется выгодным'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 50h80l-6-30-20 16-14-24-14 24-20-16z"/><path d="M20 56h80" opacity=".7"/></svg>'},
  {t:'Ведение', pts:['<b>Регулярный формат</b> — подписка или ежемесячное сопровождение', 'Даёт <b>предсказуемый</b> повторяющийся доход', 'Клиент остаётся с тобой <b>на месяцы</b>, растёт LTV', 'Ведение превращает разовые сделки в <b>стабильный поток</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'Как собрать линейку', pts:['Начни со <b>стандарта</b> — он должен продаваться уверенно', 'Отними лишнее → получишь <b>минимум</b>; добавь заботы → <b>премиум</b>', 'Пристрой сверху <b>ведение</b> как продолжение результата', 'Каждый уровень должен <b>вести</b> к следующему по каскаду'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'}
  ],
  quiz:[
  {q:'Зачем нужна продуктовая линейка?', o:['чтобы запутать клиента', 'поймать клиентов с разным бюджетом', 'поднять только цену', 'убрать конкурентов'], a:1},
  {q:'Главная задача продукта-минимума:', o:['принести основную прибыль', 'завести клиента в линейку и дать первый результат', 'заменить премиум', 'собрать отзывы'], a:1},
  {q:'На чём держится основной доход?', o:['на минимуме', 'на стандарте', 'на премиуме', 'на бесплатном'], a:1},
  {q:'Чем полезен премиум, даже если покупают редко?', o:['его легко делать', 'он заякоряет цену и поднимает средний чек', 'он бесплатный', 'его все берут'], a:1},
  {q:'Что даёт формат ведения?', o:['разовую крупную сделку', 'предсказуемый повторяющийся доход', 'только новых клиентов', 'снижение цены'], a:1}
  ],
  pairs:[
  ['Минимум', 'Низкий порог, первый результат'],
  ['Стандарт', 'Основной доход, оптимум цена/результат'],
  ['Премиум', 'Глубина, внимание, якорь цены'],
  ['Ведение', 'Подписка, повторяющийся доход'],
  ['Якорь цены', 'Премиум делает стандарт выгодным'],
  ['Сборка линейки', 'От стандарта: убрать или добавить']
  ],
  task:{
    intro:'Собери линейку из 4 уровней для своей услуги: минимум, стандарт, премиум и ведение — с ценой и сутью каждого.',
    chips:['Минимум + цена', 'Стандарт + цена', 'Премиум + цена', 'Ведение + цена/мес', 'Что отличает премиум', 'Как минимум ведёт к стандарту'],
    ph:'Услуга: продвижение эксперта. Минимум: аудит + план на 5к. Стандарт: настройка + 10 Reels за 40к. Премиум: полная упаковка + воронка + личный созвон еженедельно за 120к. Ведение: сопровождение и контент 30к/мес. Минимум даёт план, по которому логично заказать стандарт-реализацию.',
    verdict:'Грамотно: премиум у тебя не «то же дороже», а другой уровень вовлечения — это и держит цену. Усилю: сделай минимум-аудит платным мостиком, где итог прямо упирается в стандарт («вот план — вот кто его реализует за 40к»), тогда конверсия из уровня в уровень вырастет заметно.'
  }
}
  ],
  "k5": [
{
  title:'Чаты активности',
  sub:'3:20 · слайды + тест + игра', dur:'3:20',
  c1:'ЧАТЫ', c2:'АКТИВНОСТИ',
  slides:[
  {t:'Масса задавливает алгоритм', pts:['<b>10-15 роликов в день</b> — это не спам, а масса, которую алгоритм обязан кому-то показать', 'Чем больше единиц контента, тем выше шанс, что хотя бы один <b>залетит</b> и вытянет весь профиль', 'Один вирусный ролик поднимает охваты соседних — алгоритм начинает доверять аккаунту'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c4 12-6 16-6 26 0-6-5-9-5-9-4 6-11 11-11 22a22 22 0 0 0 44 0c0-14-12-20-16-39z"/></svg>'},
  {t:'Что такое чат активности', pts:['Закрытый чат, где авторы <b>взаимно</b> дают первые реакции на свежие ролики', 'Первые 30-60 минут решают — алгоритм смотрит на скорость реакций', 'Задача чата — дать <b>стартовый импульс</b>, дальше ролик едет сам'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Правило осмысленных комментариев', pts:['Комментарий из 5-8 слов по сути ролика весит в разы больше лайка', 'Алгоритм читает <b>время на комментарий</b> и диалог в ветке как сильный сигнал', 'Отвечай на чужие комментарии — ветка из 3-4 ответов держит зрителя дольше'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Огонёчки — это накрутка', pts:['Пачка эмодзи без смысла — <b>засорение</b>, алгоритм считывает это как искусственную активность', 'Массовые одинаковые реакции в первые минуты могут уронить, а не поднять ролик', 'Лучше 10 живых комментариев, чем 100 огонёчков'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Как выбрать чат', pts:['Бери чат <b>по своей нише</b> или смежной — реакции от целевой аудитории ценнее', 'Проверь активность: если люди пишут только \'взаимно+\', а не по сути — толку мало', 'Оптимально 2-3 живых чата, а не 20 мёртвых'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h80L68 44v18l-16 8V44z"/></svg>'},
  {t:'Дисциплина важнее таланта', pts:['Заходи в чат <b>каждый день</b> в одно время — набьёшь связи и получишь стабильный старт', 'Отдавай активность честно: халявщиков вычисляют и выкидывают', 'Совмещай массу роликов и чат — это система, а не разовый залёт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Сколько роликов в день даёт эффект массы?', o:['3-5', '10-15', '1-2', '30 и больше'], a:1},
  {q:'Что весит больше для алгоритма?', o:['огонёчек', 'осмысленный комментарий', 'репост в личку', 'просмотр 1 секунду'], a:1},
  {q:'Почему огонёчки вредят?', o:['делают ролик длиннее', 'считываются как накрутка и засорение', 'банят чат', 'замедляют монтаж'], a:1},
  {q:'Почему важны первые минуты после публикации?', o:['алгоритм оценивает скорость реакций', 'так удобнее автору', 'этого требует площадка', 'иначе ролик не сохранится'], a:0},
  {q:'Сколько живых чатов оптимально вести?', o:['20 и больше', '2-3', 'ноль', 'ровно 10'], a:1}
  ],
  pairs:[
  ['Масса', '10-15 роликов в день ради залёта хотя бы одного'],
  ['Чат активности', 'стартовый импульс реакций в первый час'],
  ['Осмысленный коммент', 'сильный сигнал: время плюс диалог в ветке'],
  ['Огонёчки', 'накрутка и засорение, риск уронить ролик'],
  ['Смежная ниша', 'реакции целевой аудитории, а не случайных людей']
  ],
  task:{
    intro:'Опиши свою систему массы и активности на неделю: сколько роликов в день, в каких 2-3 чатах будешь давать активность и по какому правилу комментируешь.',
    chips:['10-15 роликов в день', '2-3 чата по нише', 'коммент 5-8 слов по сути', 'отвечаю на ветки', 'без огонёчков', 'заход каждый день в 10:00'],
    ph:'Ниша — короткие рецепты. Делаю 12 Reels в день. Активность в 3 чатах: \'Кулинары Reels\', \'Взаимные охваты. Еда\', смежный \'ЗОЖ-блогеры\'. Комментирую по сути: \'Класс, а сколько по времени печётся при 180?\'. Отвечаю на 3-4 ветки каждый день в 10:00 утра.',
    verdict:'Сильно — у тебя конкретная ниша, число роликов и живые чаты по теме. Совет: заведи мини-таблицу и отмечай, какой ролик после чата дал залёт — через 2 недели увидишь, какой чат реально работает, а какой можно выкинуть.'
  }
},
{
  title:'Трафик в Telegram',
  sub:'3:45 · слайды + тест + игра', dur:'3:45',
  c1:'ТРАФИК', c2:'В ТЕЛЕГЕ',
  slides:[
  {t:'Телеграм — игра в перспективу', pts:['Подписчик в <b>Telegram</b> не даёт мгновенных продаж — он греется касаниями неделями', 'Считай не \'сколько продал сегодня\', а <b>стоимость подписчика</b> и его окупаемость за 2-3 месяца', 'Канал — это актив, который растёт в цене, а не разовая акция'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Закуп решает объём', pts:['Единичная реклама почти не даёт данных — нужен <b>объём закупов</b>, чтобы найти рабочие каналы', 'Тестируй пачкой: 10-15 размещений, потом масштабируй то, что окупилось', 'Один удачный канал-донор может кормить подписками месяцами'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V14M18 58h84"/><rect x="30" y="40" width="12" height="18"/><rect x="52" y="30" width="12" height="28"/><rect x="74" y="20" width="12" height="38"/></svg>'},
  {t:'Только целевые и смежные каналы', pts:['Реклама в канал <b>по твоей аудитории</b> или смежной — иначе сливаешь бюджет', 'Смежность важнее размера: маленький точный канал бьёт большой мусорный', 'Проверяй аудиторию донора: живые комментарии и охваты, а не только число подписчиков'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Как считать эффективность', pts:['Метрика — <b>цена подписчика</b> (потрачено делить на пришедших), а не \'красивый пост\'', 'Смотри на удержание: сколько осталось через неделю, а не в день закупа', 'Веди таблицу доноров: канал, цена, подписки, отписки, вывод'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Прогрев после захода', pts:['Новый подписчик холодный — первые посты должны <b>цеплять и давать пользу</b>', 'Серия касаний: польза, история, оффер — а не продажа в лоб', 'Тёплая база покупает сама, когда доверие набрано'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Частые сливы бюджета', pts:['Закуп в накрученные каналы — <b>боты не покупают</b>', 'Реклама не по аудитории — приходят и сразу отписываются', 'Один тест вместо объёма — нет статистики, нет вывода'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Телеграм-трафик — это игра во что?', o:['быстрые продажи', 'перспективу и прогрев', 'лайки', 'охваты сторис'], a:1},
  {q:'Главная метрика закупа рекламы?', o:['число подписчиков донора', 'цена подписчика и окупаемость', 'лайки на посте', 'дата размещения'], a:1},
  {q:'Куда покупать рекламу?', o:['в любой большой канал', 'в целевые и смежные по аудитории', 'в развлекательные мемы', 'куда дешевле'], a:1},
  {q:'Зачем нужен объём закупов?', o:['так дешевле', 'чтобы набрать статистику и найти рабочие каналы', 'этого требует площадка', 'для красивого отчёта'], a:1},
  {q:'Что делать с новым холодным подписчиком?', o:['сразу продавать', 'прогревать серией касаний', 'игнорировать', 'просить отзыв'], a:1}
  ],
  pairs:[
  ['Игра в перспективу', 'подписчик окупается за 2-3 месяца, а не сразу'],
  ['Цена подписчика', 'бюджет делить на число пришедших'],
  ['Смежный канал', 'близкая по интересу аудитория, дешёвый точный трафик'],
  ['Объём закупов', 'пачка размещений ради статистики'],
  ['Прогрев', 'серия польза-история-оффер до продажи']
  ],
  task:{
    intro:'Составь план первого тестового закупа в Telegram: нишу канала, 3-5 каналов-доноров с типом аудитории, бюджет на тест и метрику, по которой поймёшь успех.',
    chips:['ниша канала', '5 доноров по аудитории', 'смежные тематики', 'бюджет на пачку тестов', 'метрика: цена подписчика', 'удержание через 7 дней'],
    ph:'Канал про фриланс для новичков. Доноры: \'Удалёнка и вакансии\', \'Дизайн с нуля\', \'Копирайтинг\', смежные \'Как заработать студенту\' и \'Нейросети для работы\'. Бюджет теста 15 000 на 5 размещений. Успех — цена подписчика до 25 руб и удержание 70% через неделю.',
    verdict:'Отлично — есть смежные каналы и чёткая метрика цены подписчика, а не абстрактный \'рост\'. Совет: заложи ещё колонку \'дошли до оффера\' — через месяц увидишь, какой донор даёт не просто дешёвого, а покупающего подписчика.'
  }
},
{
  title:'Оффер и первый продукт',
  sub:'3:10 · слайды + тест + игра', dur:'3:10',
  c1:'ОФФЕР', c2:'ПОД БОЛЬ',
  slides:[
  {t:'Оффер — это решение боли', pts:['Оффер это не \'делаю Reels\', а <b>результат</b>: \'5 роликов, которые приведут первых клиентов\'', 'Люди платят за <b>решение конкретной проблемы</b>, а не за услугу вообще', 'Нет боли — нет продажи, какой бы красивой ни была подача'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Собери оффер из своих навыков', pts:['Выпиши <b>сильные стороны</b>: что делаешь легко, а другим тяжело', 'На пересечении твоего навыка и чужой боли рождается оффер', 'Не пытайся уметь всё — узкий сильный навык продаётся дороже'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M28 24h20a6 6 0 0 1 12 0h20v20a6 6 0 0 1 0 12v8H60a6 6 0 0 0-12 0H28V44a6 6 0 0 0 0-12z"/></svg>'},
  {t:'Ищи неожиданные сегменты', pts:['Конкуренция ниже там, где <b>никто не смотрит</b>: мамы после родов, локальные мастера, эксперты 50+', 'Узкий сегмент — точный оффер и меньше конкурентов за бюджет', 'Один и тот же навык переупаковывается под 5 разных аудиторий'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Формула сильного оффера', pts:['Кто плюс какая боль плюс какой результат плюс за какой срок', '\'Помогу маме в декрете запустить блог и получить первые 3 заказа за месяц\'', 'Конкретика в цифрах и сроке усиливает доверие в разы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'},
  {t:'Первый продукт — простой', pts:['Не строй огромный курс — начни с <b>одной услуги</b> с понятным результатом', 'Простой продукт легче продать, быстрее сделать, быстрее получить кейс', 'Усложнять будешь потом, когда пойдут деньги и отзывы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Тест оффера в поле', pts:['Проверяй оффер на реальных людях: пиши, спрашивай, слушай боль их словами', 'Если в ответ \'интересно, а сколько стоит\' — оффер попал', 'Молчание в ответ — переупакуй боль, а не поднимай активность'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'}
  ],
  quiz:[
  {q:'Что на самом деле продаёт оффер?', o:['услугу вообще', 'решение конкретной боли', 'красивый профиль', 'низкую цену'], a:1},
  {q:'Из чего собирать оффер?', o:['из модных ниш', 'из своих сильных навыков и чужой боли', 'из советов блогеров', 'наугад'], a:1},
  {q:'Зачем искать неожиданные сегменты?', o:['так интереснее', 'ниже конкуренция и точнее оффер', 'дороже реклама', 'проще монтаж'], a:1},
  {q:'Пример неожиданного сегмента?', o:['все подряд', 'мамы после родов и локальные мастера', 'крупные бренды', 'конкуренты'], a:1},
  {q:'Каким должен быть первый продукт?', o:['огромным курсом', 'простой услугой с понятным результатом', 'бесплатным навсегда', 'без цены'], a:1}
  ],
  pairs:[
  ['Оффер', 'результат под конкретную боль, а не название услуги'],
  ['Сильный навык', 'то, что тебе легко, а другим тяжело'],
  ['Неожиданный сегмент', 'аудитория с низкой конкуренцией, точный заход'],
  ['Формула оффера', 'кто плюс боль плюс результат плюс срок'],
  ['Первый продукт', 'простая услуга ради быстрого кейса']
  ],
  task:{
    intro:'Собери свой оффер по формуле: возьми один сильный навык, выбери неожиданный сегмент ЦА и сформулируй результат со сроком.',
    chips:['мой сильный навык', 'неожиданный сегмент', 'их главная боль', 'конкретный результат', 'срок', 'формула кто+боль+результат+срок'],
    ph:'Навык — монтаж вертикальных видео. Сегмент — мастера маникюра в маленьких городах. Боль — \'снимаю на телефон, но ролики не смотрят\'. Оффер: \'Соберу мастеру маникюра 8 продающих Reels в месяц, чтобы записи заполнялись на 2 недели вперёд, первый результат за 3 недели\'.',
    verdict:'Мощно — редкий сегмент, живая боль их словами и результат в записи, а не в просмотрах. Совет: добавь мини-гарантию (\'если за месяц не будет ни одной новой записи — доделаю бесплатно\') — на холодную аудиторию это снимает страх и поднимает конверсию в оплату.'
  }
},
{
  title:'Первые кейсы за отзыв',
  sub:'3:35 · слайды + тест + игра', dur:'3:35',
  c1:'ПЕРВЫЕ', c2:'КЕЙСЫ',
  slides:[
  {t:'Зачем работать за отзыв', pts:['Первые <b>1-2 клиента</b> — не про деньги, а про кейс и доверие', 'Без кейсов ты \'ещё один новичок\', с кейсами — <b>специалист с доказательством</b>', 'Отзыв и результат окупятся десятками платных клиентов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'До и после — обязательно', pts:['Всегда фиксируй <b>точку старта</b>: охваты, заявки, подписчики ДО работы', 'Результат без \'до\' не читается — цифра сравнивается только с базой', 'Скриншоты статистики до и после весят больше любых слов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Что входит в кейс', pts:['Задача клиента, что сделал, <b>цифры результата</b>, отзыв его словами', 'Живой отзыв с именем и лицом сильнее анонимного текста', 'Один экран, где видно проблему и результат — это и есть продающий кейс'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Правило трёх кейсов', pts:['<b>3 разных кейса</b> закрывают сомнение \'а у тебя вообще получалось?\'', 'Три результата — это система, а не случайность, за неё платят дороже', 'Каждый новый кейс легально <b>поднимает ценник</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 12h40v14a20 20 0 0 1-40 0z"/><path d="M40 18H26c0 12 8 16 16 16M80 18h14c0 12-8 16-16 16M60 46v10M46 62h28M52 56h16"/></svg>'},
  {t:'Как не застрять в бесплатном', pts:['Договорись на берегу: <b>бесплатно за отзыв — только 1-2 клиента</b>, дальше платно', 'Пропиши срок и объём, чтобы \'за отзыв\' не превратилось в вечную работу', 'Бесплатный клиент тоже отвечает: даёт доступы, отзыв, участвует'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Оформи кейс красиво', pts:['Собери кейс в единый макет: скрин до, скрин после, короткий текст', 'Держи кейсы в закрепе и в первом касании с новым клиентом', 'Обновляй — свежий кейс продаёт лучше прошлогоднего'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="24" y="14" width="72" height="46" rx="7"/><circle cx="43" cy="30" r="6"/><path d="M28 54l20-18 14 12 12-10 22 16" opacity=".85"/></svg>'}
  ],
  quiz:[
  {q:'Зачем брать первых клиентов за отзыв?', o:['ради денег', 'ради кейса и доверия', 'чтобы занять время', 'для рекламы площадки'], a:1},
  {q:'Что обязательно в кейсе?', o:['только отзыв', 'точка до и после в цифрах', 'красивое фото', 'низкая цена'], a:1},
  {q:'Сколько кейсов снимают сомнение?', o:['один', '3 разных', 'двадцать', 'ноль'], a:1},
  {q:'Как не застрять в бесплатной работе?', o:['работать всем бесплатно', 'ограничить 1-2 клиентами и прописать срок', 'брать предоплату 100%', 'не давать отзыв'], a:1},
  {q:'Что даёт правило трёх кейсов?', o:['ничего', 'доказывает систему и поднимает ценник', 'снижает цену', 'усложняет продажу'], a:1}
  ],
  pairs:[
  ['Работа за отзыв', 'первые 1-2 клиента ради кейса, не денег'],
  ['До и после', 'точка старта в цифрах против результата'],
  ['Кейс', 'задача, действия, цифры, отзыв клиента'],
  ['Три кейса', 'доказательство системы и повод поднять цену'],
  ['Границы бесплатного', 'срок и объём прописаны заранее']
  ],
  task:{
    intro:'Спланируй свой первый кейс за отзыв: кого возьмёшь, какие цифры зафиксируешь ДО, какой результат обещаешь и что попросишь взамен.',
    chips:['клиент за отзыв', 'цифры ДО (охваты/заявки)', 'срок работы', 'обещанный результат', 'формат отзыва (видео/текст)', 'лимит: только 1-2 клиента'],
    ph:'Беру знакомого фотографа. Фиксирую ДО: охваты Reels 400-600, заявок 1-2 в неделю. Срок — 3 недели, 9 роликов. Цель — охваты 3000+ и 5 заявок в неделю. Взамен: видео-отзыв с лицом плюс разрешение показывать скрины статистики. После него — только платно.',
    verdict:'Чётко — есть замер ДО в цифрах и жёсткий лимит на бесплатную работу, это спасёт от вечного \'за отзыв\'. Совет: сразу договорись, что скрин статистики он пришлёт тебе на 21-й день лично — иначе кейс повиснет без финальной цифры, а именно она и продаёт.'
  }
},
{
  title:'Холодные касания и скрипты',
  sub:'3:55 · слайды + тест + игра', dur:'3:55',
  c1:'ХОЛОДНЫЕ', c2:'КАСАНИЯ',
  slides:[
  {t:'Холодные касания — это система', pts:['<b>10-20 сообщений в день</b> — не спам, а воронка первых клиентов', 'Продажа рождается из объёма: 100 касаний, несколько диалогов, 1-2 клиента', 'Считай касания как метрику — нет цифры, нет результата'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Заход через контекст', pts:['Начинай не с \'куплю рекламу\', а с <b>повода</b>: \'увидел твоё объявление в чате\'', 'Тёплый вход через общий контекст убивает ощущение спама', 'Первое сообщение — про него, а не про тебя'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Скрипт первого сообщения', pts:['Повод, короткий комплимент по делу, <b>вопрос</b> — а не оффер в лоб', '\'Видел, что снимаешь сам. Хочешь, покажу, как из этого сделать продающий Reels?\'', 'Вопрос втягивает в диалог, монолог отпугивает'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Оффер-челлендж', pts:['Предложи конкретику: <b>\'7 Reels с результатом\'</b> за короткий срок', 'Челлендж с обещанным итогом легче принять, чем \'давайте посотрудничаем\'', 'Ограниченный формат снимает страх большого обязательства'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Дожим в лапу', pts:['После интереса не пропадай — <b>дожимай</b>: конкретный следующий шаг и срок', '\'Давай стартуем в понедельник, скину список из 3 доступов\'', 'Большинство сделок теряется не на \'нет\', а на молчании после \'интересно\''],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'Дисциплина касаний', pts:['Веди таблицу: кому написал, ответ, следующий шаг, дата дожима', 'Возвращайся к \'подумаю\' через 2-3 дня — половина закрывается на втором касании', 'Без учёта касания превращаются в хаос и слитые контакты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Сколько холодных касаний делать в день?', o:['1-2', '10-20', '100 и больше', 'ноль'], a:1},
  {q:'Как заходить в первом сообщении?', o:['сразу с оффера', 'через повод и вопрос', 'с прайса', 'с просьбы о деньгах'], a:1},
  {q:'Что такое оффер-челлендж?', o:['бесплатно навсегда', 'конкретный формат вроде \'7 Reels с результатом\'', 'скидка', 'розыгрыш'], a:1},
  {q:'Где теряется больше всего сделок?', o:['на явном \'нет\'', 'в молчании после \'интересно\'', 'на цене', 'в самом начале'], a:1},
  {q:'Зачем нужна таблица касаний?', o:['для отчёта', 'чтобы дожимать и не терять контакты', 'этого требует площадка', 'незачем'], a:1}
  ],
  pairs:[
  ['Холодное касание', 'первое сообщение незнакомому потенциальному клиенту'],
  ['Заход через контекст', '\'увидел объявление в чате\' вместо продажи в лоб'],
  ['Оффер-челлендж', '\'7 Reels с результатом\' за короткий срок'],
  ['Дожим в лапу', 'конкретный следующий шаг и срок после интереса'],
  ['Таблица касаний', 'учёт: кому, ответ, следующий шаг, дата']
  ],
  task:{
    intro:'Напиши свой скрипт холодного касания: заход через контекст, вопрос вместо оффера, оффер-челлендж и фразу дожима. Плюс укажи норму касаний в день.',
    chips:['повод захода', 'комплимент по делу', 'вопрос-крючок', 'оффер-челлендж 7 Reels', 'фраза дожима', 'норма 15 касаний в день'],
    ph:'Норма — 15 касаний в день. Скрипт: \'Привет! Увидел твоё объявление в чате мастеров. Классно снимаешь сама, но ролики теряются в ленте. Хочешь, за неделю сделаю тебе 7 Reels с целью на первые заявки? Если зайдёт — продолжим\'. Дожим: \'Давай стартанём в понедельник, скину список из 3 доступов\'.',
    verdict:'Сильный скрипт — заход через общий чат, вопрос вместо втюхивания и челлендж с понятным итогом. Совет: заготовь 2-3 варианта первой строки под разные поводы (объявление, комментарий, общий чат) — упрёшься в один шаблон, и живые люди почуют рассылку.'
  }
},
{
  title:'Ценообразование и юнит-экономика',
  sub:'4:05 · слайды + тест + игра', dur:'4:05',
  c1:'ЦЕНА', c2:'И ЮНИТ',
  slides:[
  {t:'Цена — это тест, а не догма', pts:['Подними цену <b>на всё сразу</b> для новых клиентов и смотри на спрос', 'Если поток заявок не упал — цена была занижена, поднимай ещё', 'Рынок сам показывает потолок, но только если ты пробуешь'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Действующим — постепенно', pts:['Старым клиентам цену поднимай <b>на 10-20%</b>, не резко', 'Объясни ростом ценности: новые инструменты, лучший результат', 'Плавный рост удерживает базу и не вызывает бунт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Считай юнит-экономику', pts:['Прибыль с клиента — это цена минус <b>все расходы</b> на него', 'В расходы входят <b>нейронки</b>, подписки на сервисы, монтаж, твоё время', 'Красивая цена при минусовой экономике — это работа в убыток'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Из чего складывается себестоимость', pts:['Подписки: <b>ChatGPT</b>, <b>Midjourney</b>, <b>ElevenLabs</b>, монтажки', 'Оплата подрядчику или твои часы по цене часа', 'Раздели общие подписки на число клиентов — получишь честную себестоимость единицы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="18" width="76" height="38" rx="8"/><path d="M22 30h76"/><circle cx="82" cy="42" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Маржа и порог цены', pts:['Держи <b>маржу</b>, при которой хочется работать, а не выживать', 'Ниже порога — отказывайся, дешёвый клиент часто самый токсичный', 'Цена ниже себестоимости — это ты платишь клиенту за право работать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Поднимай ценность, а не только цену', pts:['Кейсы, скорость, гарантия результата — <b>основания</b> для высокой цены', 'Продавай итог для клиента, а не количество часов монтажа', 'Три кейса плюс понятный результат — цену можно смело удваивать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 50h80l-6-30-20 16-14-24-14 24-20-16z"/><path d="M20 56h80" opacity=".7"/></svg>'}
  ],
  quiz:[
  {q:'Как тестировать новую цену?', o:['снижать', 'поднять на всё сразу для новых и смотреть спрос', 'не менять', 'спросить конкурентов'], a:1},
  {q:'На сколько поднимать цену действующим клиентам?', o:['в 2 раза', 'на 10-20% постепенно', 'на 100%', 'не поднимать вообще'], a:1},
  {q:'Что такое юнит-экономика?', o:['только цена', 'цена минус все расходы на клиента', 'число подписчиков', 'охваты'], a:1},
  {q:'Что относится к себестоимости услуги?', o:['лайки', 'подписки на нейронки и монтаж', 'комментарии', 'репосты'], a:1},
  {q:'Чем обосновать высокую цену?', o:['красивым профилем', 'кейсами и гарантией результата', 'возрастом аккаунта', 'скидкой'], a:1}
  ],
  pairs:[
  ['Тест цены', 'поднять на всё сразу и смотреть на спрос'],
  ['Рост для старых', 'плавно на 10-20% с ростом ценности'],
  ['Юнит-экономика', 'цена минус все расходы на одного клиента'],
  ['Себестоимость', 'нейронки, подписки, монтаж, твои часы'],
  ['Маржа', 'разница, при которой работать выгодно']
  ],
  task:{
    intro:'Посчитай юнит-экономику одной своей услуги: цена, все расходы на клиента (нейронки, подписки, монтаж, время) и итоговая прибыль. Затем реши, поднимать ли цену.',
    chips:['цена услуги', 'подписки на инструменты', 'оплата монтажа/подрядчику', 'мои часы умножить на ставку', 'прибыль с клиента', 'решение по цене'],
    ph:'Пакет \'8 Reels в месяц\' — 15 000. Расходы: ChatGPT, Midjourney и ElevenLabs около 2500 (делю на 4 клиентов, выходит 625), монтаж 3000, моё время 10 часов по 500 — это 5000. Себестоимость 8625, прибыль 6375. Заявок много — поднимаю новым до 19 000, старым плюс 15%.',
    verdict:'Отлично — ты посчитал даже долю подписок на одного клиента, это и есть честная юнит-экономика. Совет: заложи в цену \'риск-час\' на правки — новички считают идеальный сценарий, а реальные доработки съедают маржу, и прибыль оказывается вдвое меньше расчётной.'
  }
},
{
  title:'Переговоры: «хочу, но не нуждаюсь»',
  sub:'3:25 · слайды + тест + игра', dur:'3:25',
  c1:'ХОЧУ,', c2:'НЕ НУЖДАЮСЬ',
  slides:[
  {t:'Позиция изобилия', pts:['\'<b>Хочу, но не нуждаюсь</b>\' — ты рад клиенту, но не держишься за него', 'Когда заявок много, <b>ты выбираешь</b>, а не выпрашиваешь', 'Нужда в голосе слышна и сбивает цену — изобилие держит её'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 50h80l-6-30-20 16-14-24-14 24-20-16z"/><path d="M20 56h80" opacity=".7"/></svg>'},
  {t:'Наполни воронку', pts:['Изобилие не поза, а <b>следствие</b> потока касаний и заявок', '10-20 касаний в день дают выбор — из позиции выбора легко говорить \'нет\'', 'Пустая воронка — это страх, а страх — это прогиб под любую манипуляцию'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'},
  {t:'Распознай манипуляции', pts:['\'У других дешевле\', \'сделай тест бесплатно\', \'потом много закажу\' — <b>давление на нужду</b>', 'Обещание будущих заказов вместо оплаты сейчас — красный флаг', 'Манипуляция работает только на том, кто боится потерять клиента'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Держи рамку спокойно', pts:['Отвечай без агрессии: \'Понимаю, но моя цена такая, потому что...\'', 'Не оправдывайся и не падай в цене от одного \'дорого\'', '\'Дорого\' — часто проверка твоей уверенности, а не отказ'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Отпускай не своих', pts:['Клиент, который торгуется до нуля, <b>сожрёт нервы</b> и не даст кейс', 'Спокойное \'возможно, я вам не подхожу\' часто возвращает клиента с уважением', 'Отказ неподходящему освобождает место для нужного'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'Уверенность через кейсы', pts:['За спиной 3 кейса — и манипуляции перестают цеплять', 'Ты опираешься на результат, а не на надежду \'лишь бы взяли\'', 'Цена плюс доказательство — переговоры из силы, а не из страха'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 12h40v14a20 20 0 0 1-40 0z"/><path d="M40 18H26c0 12 8 16 16 16M80 18h14c0 12-8 16-16 16M60 46v10M46 62h28M52 56h16"/></svg>'}
  ],
  quiz:[
  {q:'Что значит \'хочу, но не нуждаюсь\'?', o:['клиент не нужен', 'рад клиенту, но не держусь за него', 'работаю бесплатно', 'поднимаю цену вдвое'], a:1},
  {q:'Откуда берётся позиция изобилия?', o:['из позы', 'из потока касаний и заявок', 'из возраста аккаунта', 'из скидок'], a:1},
  {q:'Что такое \'потом много закажу\'?', o:['щедрость клиента', 'манипуляция-давление на нужду', 'гарантия', 'предоплата'], a:1},
  {q:'Как реагировать на \'дорого\'?', o:['сразу скинуть цену', 'спокойно держать рамку и обосновать', 'обидеться', 'уйти молча'], a:1},
  {q:'Что делать с клиентом, торгующимся до нуля?', o:['прогнуться', 'спокойно отпустить', 'работать в убыток', 'долго спорить'], a:1}
  ],
  pairs:[
  ['Хочу, не нуждаюсь', 'рад клиенту, но не держусь за него'],
  ['Позиция изобилия', 'следствие полной воронки заявок'],
  ['\'Потом закажу\'', 'манипуляция вместо оплаты сейчас'],
  ['Держать рамку', 'обосновать цену без оправданий и прогиба'],
  ['Отпустить клиента', 'освободить место под подходящего']
  ],
  task:{
    intro:'Пропиши свои ответы на 3 частые манипуляции клиента (\'у других дешевле\', \'сделай тест бесплатно\', \'потом много закажу\'), сохраняя позицию изобилия.',
    chips:['ответ на \'у других дешевле\'', 'ответ на \'бесплатный тест\'', 'ответ на \'потом закажу\'', 'спокойный тон', 'опора на кейсы', 'готовность отпустить'],
    ph:'\'У других дешевле\' — \'Возможно, и это ок. У меня цена за результат: 3 клиента на скринах статистики\'. \'Сделай тест бесплатно\' — \'Бесплатных тестов не делаю, но первый ролик покажу в работе — если не зайдёт, разойдёмся\'. \'Потом много закажу\' — \'Супер, тогда стартуем с оплаченного пакета сейчас, а объём обсудим по результату\'.',
    verdict:'Крепко — ты не оправдываешься и везде возвращаешь разговор к результату и оплате сейчас. Совет: добавь паузу перед ответом на \'дорого\' — 2-3 секунды тишины часто дожимают клиента сильнее любых слов, потому что он сам начинает оправдывать свою цифру.'
  }
},
{
  title:'Границы, возвраты, антикризис',
  sub:'3:50 · слайды + тест + игра', dur:'3:50',
  c1:'ГРАНИЦЫ', c2:'И ВОЗВРАТЫ',
  slides:[
  {t:'Границы вместо терпения', pts:['Терпила теряет и деньги, и нервы — <b>границы</b> защищают и то, и другое', 'Правила игры проговариваются <b>до старта</b>, а не в разгар конфликта', 'Уважение начинается там, где ты сам уважаешь свои условия'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Прописывай сроки и объём', pts:['Заранее фиксируй: <b>сколько правок</b>, какой объём, какие сроки', '\'Безлимит правок\' — ловушка, из которой не выбраться', 'Чёткие рамки в переписке — это защита от \'а переделай ещё раз\''],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Фиксируй всё', pts:['<b>Скрин-рекорд</b> и скриншоты договорённостей — твоя страховка', 'Устных договорённостей не существует — есть только зафиксированные', 'При споре решает не память, а сохранённая переписка'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Отпусти токсичного клиента', pts:['Клиент, который унижает и выносит мозг, <b>дороже своих денег</b>', 'Иногда лучший ход — <b>вернуть деньги</b> и закончить отношения', 'Один токсик съедает время трёх нормальных клиентов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Возврат как сила', pts:['Возврат — не поражение, а <b>управление репутацией</b> и нервами', 'Лучше вернуть и разойтись, чем получить публичный негатив', 'Спокойный возврат часто закрывает конфликт без скандала'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'Антикризисный план', pts:['Заранее реши, при каких условиях делаешь возврат и как расстаёшься', 'Держи шаблон спокойного финального сообщения без эмоций', 'Кризис проходят те, у кого есть правила, а не паника'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Когда проговаривать правила работы?', o:['в конфликте', 'до старта работы', 'в самом конце', 'никогда'], a:1},
  {q:'Чем опасен \'безлимит правок\'?', o:['ничем', 'это ловушка без выхода', 'ускоряет работу', 'повышает цену'], a:1},
  {q:'Зачем скрин-рекорд и скриншоты?', o:['для красоты', 'страховка при спорах', 'для площадки', 'для отчёта клиенту'], a:1},
  {q:'Что делать с токсичным клиентом?', o:['терпеть ради денег', 'отпустить или вернуть деньги', 'спорить публично', 'снизить цену'], a:1},
  {q:'Возврат денег — это что?', o:['поражение', 'инструмент управления репутацией и нервами', 'обязанность', 'штраф'], a:1}
  ],
  pairs:[
  ['Границы', 'правила, проговорённые до старта работы'],
  ['Сроки и объём', 'число правок и рамки зафиксированы заранее'],
  ['Скрин-рекорд', 'страховка: договорённости сохранены'],
  ['Токсичный клиент', 'дороже своих денег, отпусти его'],
  ['Возврат', 'управление репутацией, а не поражение']
  ],
  task:{
    intro:'Составь свои рабочие границы и антикризисный план: что прописываешь до старта, как фиксируешь договорённости и при каких условиях делаешь возврат.',
    chips:['число правок в пакете', 'сроки и объём', 'как фиксирую (скрин/переписка)', 'стоп-условия по токсику', 'правило возврата', 'шаблон финального сообщения'],
    ph:'До старта в переписке: 8 роликов, 2 правки на каждый, срок 30 дней. Всё согласую текстом и делаю скрин-рекорд сдачи. Стоп-условие: переход на личности или требование безлимита правок. Возврат — за невыполненный объём, спокойным сообщением: \'Возвращаю оплату за N роликов, спасибо за опыт, всего доброго\'.',
    verdict:'Зрело — у тебя есть и лимит правок, и стоп-условие, и готовый текст возврата, это признак специалиста, а не новичка. Совет: пропиши границы отдельным коротким сообщением-\'правилами\' и проси клиента ответить \'ок\' перед стартом — этот \'ок\' в переписке решает 90% будущих споров в твою пользу.'
  }
}
  ],
  "a1": [
{
  title:'Задача → модель → цена',
  sub:'3:20 · слайды + тест + игра', dur:'3:20',
  c1:'СНАЧАЛА', c2:'ЗАДАЧА',
  slides:[
  {t:'Правило №1: сначала задача', pts:['Новичок делает наоборот: сначала лезет в модную нейронку, потом ищет, куда её приткнуть. Так сгорают и деньги, и недели.', 'Сформулируй результат словами: «обложка Reels 1080x1920», «письмо клиенту на 800 знаков», «10 сек ролика с котом». Инструмент выбираешь ПОД это.', 'Одна задача — один класс модели. Не жди картинку от <b>ChatGPT</b>, а монтаж — от <b>Suno</b>.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Карта моделей 2026', pts:['Текст: <b>ChatGPT</b>, <b>Claude</b>, <b>Gemini</b>. Картинки: <b>Midjourney</b>, <b>Flux</b>, <b>Nano Banana</b>.', 'Видео: <b>Veo</b>, <b>Kling</b>, <b>Runway</b>. Код: <b>Cursor</b>, <b>Claude Code</b>, <b>Lovable</b>.', 'Озвучка: <b>ElevenLabs</b>. Музыка: <b>Suno</b>. Автоматизация: <b>n8n</b>. Держи эту карту перед глазами.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Цена решает не меньше качества', pts:['Топ-модель на каждый чих — путь в минус. Считай стоимость ОДНОГО результата, а не месячную подписку.', 'Пример: секунда видео в <b>Kling</b> ~$0.10, у <b>Veo</b> и <b>Runway</b> дороже в разы. Черновик — на дешёвой, финал — на дорогой.', 'Правило: тестируй гипотезу на самой дешёвой модели, добивай качество на топовой только когда идея зашла.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Как сопоставить задачу и модель', pts:['Спроси три вещи: какой формат на выходе, какое качество нужно клиенту, сколько это стоит за штуку.', 'Клиенту в блог хватит <b>Flux</b>, на билборд — <b>Midjourney</b>. Разница в цене и в требованиях, а не в «крутости».', 'Заведи табличку: задача — модель — цена за результат. Через неделю у тебя личная карта, а не чужие советы.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Чек-лист перед стартом', pts:['Записал результат одним предложением? Выбрал класс модели? Прикинул цену за штуку? Только тогда открываешь нейронку.', 'Не знаешь модель — гугли «лучшая нейросеть для [задача] 2026», а не «топ нейросетей вообще».', 'Первые деньги приходят не тому, кто знает 50 моделей, а тому, кто под задачу берёт правильные 3.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'С чего начинается работа с нейросетями по правилу №1?', o:['С выбора самой мощной модели', 'С формулировки задачи и результата', 'С покупки подписки Ultra', 'С изучения всех 50 нейросетей'], a:1},
  {q:'Какая модель из карты 2026 отвечает за видео?', o:['Suno', 'Kling', 'ElevenLabs', 'Flux'], a:1},
  {q:'Как правильно считать стоимость работы нейросети?', o:['По цене месячной подписки', 'По количеству моделей в связке', 'По стоимости одного результата', 'По размеру файла'], a:2},
  {q:'Где выгоднее собирать черновики?', o:['Всегда на топовой модели', 'На самой дешёвой, финал — на дорогой', 'Только офлайн', 'Разницы нет'], a:1},
  {q:'Что отвечает за озвучку в карте 2026?', o:['Midjourney', 'n8n', 'ElevenLabs', 'Cursor'], a:2}
  ],
  pairs:[
  ['Текст', 'ChatGPT, Claude, Gemini'],
  ['Картинки', 'Midjourney, Flux, Nano Banana'],
  ['Видео', 'Veo, Kling, Runway'],
  ['Код', 'Cursor, Claude Code, Lovable'],
  ['Музыка', 'Suno'],
  ['Автоматизация', 'n8n']
  ],
  task:{
    intro:'Возьми одну свою реальную задачу и разложи её по цепочке: задача → класс модели → конкретный инструмент → примерная цена за результат.',
    chips:['Задача одним предложением', 'Формат на выходе', 'Класс модели', 'Конкретный инструмент', 'Цена за штуку', 'Черновик/финал'],
    ph:'Задача: обложка для Reels 1080x1920 про запуск курса. Класс — картинки. Инструмент — Flux для черновиков, финал добью в Midjourney. Цена: черновики почти бесплатно, финал ~1 генерация на билборд-качество.',
    verdict:'Отлично — ты не назвал «модную нейронку», а прошёл всю цепочку и даже развёл черновик и финал по цене. Усилю: добавь второй вариант инструмента на случай, если основной даст мыло, — так ты не встанешь, когда клиент ждёт результат сегодня.'
  }
},
{
  title:'Формула промпта',
  sub:'3:45 · слайды + тест + игра', dur:'3:45',
  c1:'ФОРМУЛА', c2:'ПРОМПТА',
  slides:[
  {t:'Шесть блоков сильного промпта', pts:['Формула: <b>Роль</b> + <b>Задача</b> + <b>Контекст</b> + <b>Формат</b> + <b>Примеры</b> + <b>Ограничения</b>.', 'Пропустил блок — нейронка додумывает сама и выдаёт средний по палате результат.', 'Работает одинаково в <b>ChatGPT</b>, <b>Claude</b> и <b>Gemini</b>. Это не трюк одной модели, а инженерия запроса.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Роль и Задача', pts:['<b>Роль</b>: «ты копирайтер прямого отклика с 10-летним опытом». Модель включает нужный слой знаний.', '<b>Задача</b>: глагол + конкретный результат. Не «помоги с текстом», а «напиши 3 заголовка для лендинга».', 'Роль без задачи — болтовня, задача без роли — обезличенный шаблон. Нужны обе.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="24" r="11"/><path d="M40 58c1-13 9-20 20-20s19 7 20 20"/></svg>'},
  {t:'Контекст — топливо ответа', pts:['<b>Контекст</b>: кто аудитория, что за продукт, какая цель, что уже пробовали. Чем больше фактов — тем точнее выход.', 'Плохой запрос: «текст про кофейню». Хороший: «спешелти-кофейня у метро, аудитория 25-35, средний чек 400р, цель — второй визит».', 'Не жалей символов на контекст: 5 строк вводных экономят 5 итераций правок.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Формат и Ограничения', pts:['<b>Формат</b>: скажи, как хочешь ответ — таблица, список из 5, JSON, пост на 600 знаков.', '<b>Ограничения</b>: «без воды», «без эмодзи», «только факты из моего текста», «не длиннее 3 абзацев».', 'Формат экономит время на переделке, ограничения убирают то, что бесит: канцелярит, штампы, галлюцинации.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Примеры делают магию', pts:['<b>Примеры</b> (даже один-два) показывают тон и структуру лучше любого описания.', 'Дай образец «вот так пишу я» — и модель подхватит стиль вместо усреднённого ИИ-голоса.', 'Это мостик к следующему уроку про few-shot: примеры — самый недооценённый рычаг качества.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Шаблон-рыба, который всегда под рукой', pts:['«Ты [роль]. Задача: [что сделать]. Контекст: [факты]. Формат: [как]. Пример: [образец]. Ограничения: [чего нельзя]».', 'Сохрани рыбу в заметки и подставляй под каждую задачу — скорость вырастает в разы.', 'Через 20 запросов формула станет автоматической, и ты будешь удивляться чужим кривым промптам.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M30 54h60" opacity=".6"/><path d="M40 46l30-30 8 8-30 30-12 4z"/><path d="M64 22l8 8"/></svg>'}
  ],
  quiz:[
  {q:'Из скольких блоков состоит базовая формула промпта?', o:['Из трёх', 'Из шести', 'Из десяти', 'Из двух'], a:1},
  {q:'Зачем в промпте задавать роль?', o:['Чтобы модель включила нужный слой знаний и тон', 'Чтобы ответ был длиннее', 'Это никак не влияет', 'Чтобы сэкономить деньги'], a:0},
  {q:'Что делает блок «Контекст»?', o:['Ограничивает длину ответа', 'Даёт факты об аудитории, продукте и цели', 'Задаёт формат вывода', 'Отключает эмодзи'], a:1},
  {q:'Какой запрос сильнее?', o:['«напиши текст про кофейню»', '«помоги с рекламой»', '«напиши 3 заголовка лендинга для спешелти-кофейни, аудитория 25-35, цель — второй визит»', '«сделай красиво»'], a:2},
  {q:'Зачем добавлять примеры в промпт?', o:['Чтобы модель подхватила нужный стиль и структуру', 'Чтобы потратить больше токенов', 'Примеры мешают ответу', 'Только для картинок'], a:0},
  {q:'Что убирают «Ограничения»?', o:['Роль и задачу', 'Воду, штампы, эмодзи, галлюцинации', 'Контекст', 'Формат'], a:1}
  ],
  pairs:[
  ['Роль', 'Кто отвечает и с каким опытом'],
  ['Задача', 'Глагол плюс конкретный результат'],
  ['Контекст', 'Факты об аудитории и цели'],
  ['Формат', 'Вид ответа: список, таблица, JSON'],
  ['Примеры', 'Образец тона и структуры'],
  ['Ограничения', 'Чего делать нельзя']
  ],
  task:{
    intro:'Собери один промпт по полной формуле из шести блоков под свою реальную задачу. Каждый блок — отдельной строкой.',
    chips:['Роль', 'Задача с глаголом', 'Контекст 3-5 фактов', 'Формат вывода', 'Пример-образец', 'Список ограничений'],
    ph:'Роль: ты SMM-копирайтер. Задача: напиши 3 варианта первого касания для Reels. Контекст: мастер маникюра, аудитория 20-30, боль — долго держится ли покрытие. Формат: 3 крючка по 1 строке. Пример: «Твой маникюр слезает через 3 дня? Читай». Ограничения: без эмодзи, без канцелярита.',
    verdict:'Сильно — все шесть блоков на месте, а контекст с реальной болью аудитории («слезает через 3 дня») сразу поднимает качество ответа. Усилю: в блок «Пример» добавь ещё и анти-пример («так не надо: сухо и без интриги») — модель точнее поймёт границу твоего стиля.'
  }
},
{
  title:'Few-shot и итерации',
  sub:'3:30 · слайды + тест + игра', dur:'3:30',
  c1:'FEW-SHOT', c2:'И ИТЕРАЦИИ',
  slides:[
  {t:'«Думай пошагово» — простой рычаг', pts:['Добавь фразу «думай пошагово» или «рассуждай вслух» — и модель перестаёт угадывать, начинает считать.', 'Особенно для логики, расчётов, разбора кейсов: качество ответа растёт без смены модели.', 'Работает в <b>ChatGPT</b>, <b>Claude</b>, <b>Gemini</b> — это про мышление модели, а не про её бренд.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'},
  {t:'Few-shot: учим на примерах', pts:['<b>Few-shot</b> — это когда ты даёшь 2-3 готовых примера «вопрос → ответ», а потом свою задачу.', 'Модель копирует не текст, а ПАТТЕРН: длину, тон, структуру. Идеально для однотипной пачки контента.', 'Zero-shot (без примеров) — быстро и грязно. Few-shot — чуть дольше, но результат ровный от штуки к штуке.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Негативы: говори, чего НЕ надо', pts:['<b>Негативы</b> отсекают мусор: «без вводных абзацев», «не используй слово „уникальный“», «не выдумывай факты».', 'В картинках то же самое: «без лишних пальцев, без текста на изображении, без размытого фона».', 'Один точный запрет экономит три итерации. Веди личный список того, что модель повторяет назойливо.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h80L68 44v18l-16 8V44z"/></svg>'},
  {t:'Итерации: победа с 3-й попытки', pts:['Первый ответ — черновик, а не приговор. Профи не переписывает промпт с нуля, а докручивает: «сделай короче», «добавь цифру», «убери канцелярит».', 'Держи диалог в одном окне — модель помнит контекст и правит прицельно.', 'Норма — 3-5 итераций до сильного результата. Кто бросает после первой, тот и жалуется, что «нейронка тупая».'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Разбор живого диалога', pts:['Запрос → слабо → «добавь конкретики и один пример из практики» → лучше → «сократи вдвое, оставь суть» → готово.', 'Каждая реплика — маленький точный толчок, а не новый огромный промпт.', 'Сохраняй удачные цепочки: это твои готовые сценарии доводки на будущее.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M30 54h60" opacity=".6"/><path d="M40 46l30-30 8 8-30 30-12 4z"/><path d="M64 22l8 8"/></svg>'},
  {t:'Как ускориться на потоке', pts:['Собери 2-3 своих лучших результата в шаблон few-shot — и новые задачи будут выходить с первого раза.', 'Заведи файл «мои негативы» и «мои фразы доводки» — вставляй готовым.', 'Итерации + few-shot вместе дают то, ради чего всё затевалось: стабильное качество на скорости.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 12h40v14a20 20 0 0 1-40 0z"/><path d="M40 18H26c0 12 8 16 16 16M80 18h14c0 12-8 16-16 16M60 46v10M46 62h28M52 56h16"/></svg>'}
  ],
  quiz:[
  {q:'Что делает приём «думай пошагово»?', o:['Ускоряет ответ вдвое', 'Заставляет модель рассуждать, а не угадывать', 'Отключает цензуру', 'Меняет модель'], a:1},
  {q:'Что такое few-shot?', o:['Запрос без примеров', 'Один короткий вопрос', 'Дать модели 2-3 примера перед задачей', 'Генерация видео'], a:2},
  {q:'Что копирует модель из few-shot-примеров?', o:['Дословный текст', 'Паттерн: тон, длину, структуру', 'Только заголовок', 'Ничего'], a:1},
  {q:'Для чего нужны негативы в промпте?', o:['Чтобы удлинить ответ', 'Чтобы отсечь мусор и повторяющиеся штампы', 'Чтобы сменить роль', 'Для оплаты'], a:1},
  {q:'Сколько итераций обычно нужно до сильного результата?', o:['Всегда одна', 'Ни одной', 'Обычно 3-5 прицельных правок', 'Не меньше 50'], a:2},
  {q:'Как правильно доводить ответ?', o:['Переписывать промпт с нуля каждый раз', 'Точечными репликами в том же диалоге', 'Менять модель после каждого ответа', 'Копировать чужие промпты'], a:1}
  ],
  pairs:[
  ['Думай пошагово', 'Модель рассуждает, а не угадывает'],
  ['Few-shot', '2-3 примера перед задачей'],
  ['Zero-shot', 'Запрос вообще без примеров'],
  ['Негативы', 'Список того, чего делать нельзя'],
  ['Итерация', 'Точечная правка в том же окне'],
  ['Паттерн', 'Тон и структура, что копирует модель']
  ],
  task:{
    intro:'Возьми задачу, где нужна пачка однотипного контента, и собери few-shot промпт: 2 своих примера-образца плюс новая задача и хотя бы один негатив.',
    chips:['2 примера вопрос-ответ', 'Новая задача', 'Негатив (чего нельзя)', 'Фраза «думай пошагово»', 'Формат вывода', 'План на 3 итерации'],
    ph:'Пример 1: «Продукт: гель-лак → Крючок: Держится 3 недели, а не 3 дня». Пример 2: «Продукт: наращивание → Крючок: Ресницы, из-за которых спрашивают, свои ли». Задача: сделай крючок для педикюра. Негатив: без эмодзи и без слова «идеальный».',
    verdict:'Класс — ты дал два ровных образца с одинаковой структурой «продукт → крючок», и модель точно подхватит паттерн, а не выдаст усреднённое. Усилю: добавь третий пример с намеренно ДРУГИМ углом (боль вместо выгоды) — так few-shot научит модель варьировать, а не штамповать одинаковые крючки.'
  }
},
{
  title:'Рабочее место нейродельца',
  sub:'3:35 · слайды + тест + игра', dur:'3:35',
  c1:'РАБОЧЕЕ', c2:'МЕСТО',
  slides:[
  {t:'Не одна модель, а связка', pts:['Профи не молится на одну нейронку. Рабочее место — связка <b>ChatGPT</b> + <b>Claude</b> + <b>Gemini</b> в трёх вкладках.', 'У каждой свои сильные стороны — гоняешь задачу туда, где она решается лучше и дешевле.', 'Это как бригада: один быстрый, второй вдумчивый, третий знает свежие новости.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'},
  {t:'ChatGPT — быстрый универсал', pts:['<b>ChatGPT</b> — рабочая лошадка на каждый день: быстрые ответы, идеи, брейншторм, черновики.', 'Хорош, когда нужно много и сразу, без глубокого анализа огромных текстов.', 'Держи открытым как основную вкладку для 80% мелких задач.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Claude — длинные тексты и код', pts:['<b>Claude</b> силён на больших документах, аккуратных длинных текстах и коде — держит структуру и контекст.', 'Кидай сюда «прочитай этот лонгрид и собери выжимку» или «напиши компонент» — <b>Claude Code</b> для проектов посерьёзнее.', 'Когда важна точность и связность, а не скорость, — задача идёт к <b>Claude</b>.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Gemini — свежесть и Google', pts:['<b>Gemini</b> в связке с поиском Google даёт свежие данные и факты, которых нет у остальных.', 'Хорош для ресёрча, проверки актуальности, разбора картинок и таблиц.', 'Когда вопрос «что сейчас происходит» — открываешь <b>Gemini</b>.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Как гонять задачу по кругу', pts:['Ресёрч в <b>Gemini</b> → структура и черновик в <b>Claude</b> → быстрая шлифовка и варианты в <b>ChatGPT</b>.', 'Или наоборот — под задачу. Смысл в том, что ни одна модель не делает всё лучше всех.', 'Копируй результат между вкладками руками — это и есть «рабочее место», а не магия одной кнопки.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Твой сетап на старте', pts:['Три вкладки в браузере, free-планы для теста, апгрейд той модели, что реально тянет твой поток.', 'Заметки с рыбами-промптами и списком, какую задачу куда отправлять.', 'Через неделю такой связки чужие споры «какая нейронка лучшая» будут смешить: лучшая — под задачу.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Что такое рабочее место нейродельца?', o:['Одна самая мощная модель', 'Связка ChatGPT, Claude и Gemini под разные задачи', 'Только платная подписка', 'Смартфон с одним приложением'], a:1},
  {q:'К какой модели логично отправить огромный лонгрид на выжимку?', o:['Suno', 'Claude', 'Midjourney', 'Kling'], a:1},
  {q:'Чем силён Gemini в связке?', o:['Генерацией музыки', 'Свежими данными через поиск Google', 'Монтажом видео', 'Ничем'], a:1},
  {q:'Для чего в связке ChatGPT?', o:['Только для кода', 'Быстрый универсал на 80% мелких задач', 'Только для картинок', 'Для озвучки'], a:1},
  {q:'Какой инструмент берут под серьёзные проекты с кодом?', o:['Claude Code', 'Suno', 'ElevenLabs', 'Runway'], a:0},
  {q:'Почему спор «какая нейросеть лучшая» бессмысленный?', o:['Все одинаковые', 'Лучшая — та, что подходит под конкретную задачу', 'Есть только одна рабочая', 'Это реклама'], a:1}
  ],
  pairs:[
  ['ChatGPT', 'Быстрый универсал на каждый день'],
  ['Claude', 'Длинные тексты и связный код'],
  ['Gemini', 'Свежие данные и поиск Google'],
  ['Claude Code', 'Серьёзные проекты с кодом'],
  ['Связка', 'Три модели под разные задачи'],
  ['Ресёрч', 'Стартовый этап в Gemini']
  ],
  task:{
    intro:'Опиши свой рабочий процесс по одной реальной задаче: какую часть отдаёшь ChatGPT, какую Claude, какую Gemini — и почему.',
    chips:['Этап ресёрча', 'Этап черновика', 'Этап шлифовки', 'Куда идёт код', 'Куда идут свежие факты', 'Где финальные варианты'],
    ph:'Задача: пост-разбор нового закона для блога. Свежие детали и проверка фактов — Gemini. Структура и связный длинный черновик — Claude. Три коротких варианта заголовка и финальная шлифовка — ChatGPT.',
    verdict:'Отлично — ты не свалил всё в одну модель, а развёл этапы по сильным сторонам: факты в Gemini, связность в Claude, скорость в ChatGPT. Усилю: пропиши, что копируешь между вкладками руками именно результат, а не весь диалог, — так контекст не раздувается и каждая модель работает чисто со своей частью.'
  }
},
{
  title:'Агрегаторы моделей',
  sub:'3:25 · слайды + тест + игра', dur:'3:25',
  c1:'АГРЕГА-', c2:'ТОРЫ',
  slides:[
  {t:'Что такое агрегатор', pts:['<b>Агрегатор</b> — одна платформа и один ключ, через которые доступны десятки моделей сразу.', 'Не нужно заводить подписку в каждом сервисе отдельно — платишь в одном месте, переключаешь модели кнопкой.', 'Два рабочих: <b>Higgsfield</b> для фото/видео/3D и <b>OpenRouter</b> для текстовых LLM.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="12" width="60" height="14" rx="4"/><rect x="30" y="30" width="60" height="14" rx="4" opacity=".85"/><rect x="30" y="48" width="60" height="14" rx="4" opacity=".7"/></svg>'},
  {t:'Higgsfield: фото, видео, 3D', pts:['<b>Higgsfield</b> собирает под одной крышей генерацию картинок, видео и 3D — включая топовые модели вроде <b>Veo</b>, <b>Kling</b>.', 'Удобно, когда делаешь ролик целиком: обложка, кадры, движение — не прыгая по пяти сайтам.', 'Один интерфейс, одна оплата, готовые пресеты движения камеры — экономит часы на рутине.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="24" y="14" width="72" height="46" rx="7"/><circle cx="43" cy="30" r="6"/><path d="M28 54l20-18 14 12 12-10 22 16" opacity=".85"/></svg>'},
  {t:'OpenRouter: все LLM в одном ключе', pts:['<b>OpenRouter</b> даёт доступ к <b>ChatGPT</b>, <b>Claude</b>, <b>Gemini</b> и десяткам моделей через единый API-ключ.', 'Идеально для разработки и автоматизаций: в <b>n8n</b> или коде подключил один ключ — гоняешь любую модель.', 'Видишь цену за каждый запрос и переключаешь модель под задачу без новой регистрации.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="16" r="7"/><circle cx="26" cy="52" r="7"/><circle cx="94" cy="52" r="7"/><path d="M56 22 30 46M64 22l26 24M33 52h54" opacity=".8"/></svg>'},
  {t:'Экономика агрегатора', pts:['Платишь по факту использования, а не за пять подписок, половина из которых простаивает.', 'Легко сравнить цену моделей рядом и выбрать оптимум качество/стоимость под конкретный кадр или текст.', 'Для потока это дисциплинирует расходы: видишь, куда утекают деньги, и режешь дорогое.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Агрегатор против прямого доступа', pts:['Прямой доступ иногда даёт самые свежие фичи модели на день раньше и максимальные лимиты.', 'Агрегатор выигрывает в удобстве, единой оплате и скорости переключения — меньше трения в работе.', 'Правило: постоянную основную модель бери напрямую, всё разнообразие и эксперименты — через агрегатор.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Когда что брать', pts:['Делаешь видео и картинки пачками — <b>Higgsfield</b>. Строишь автоматизацию или продукт на LLM — <b>OpenRouter</b>.', 'Работаешь в одной модели каждый день и упёрся в лимиты — бери её напрямую.', 'Не выбирай раз и навсегда: сегодня агрегатор, завтра прямой доступ — под задачу и объём.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'}
  ],
  quiz:[
  {q:'Что даёт агрегатор моделей?', o:['Одну модель бесплатно навсегда', 'Доступ к десяткам моделей через одну платформу и оплату', 'Только видео', 'Скидку на интернет'], a:1},
  {q:'Higgsfield агрегирует прежде всего что?', o:['Текстовые LLM', 'Фото, видео и 3D', 'Только музыку', 'Таблицы'], a:1},
  {q:'Чем удобен OpenRouter для автоматизаций?', o:['Один API-ключ на десятки LLM', 'Он бесплатный без лимитов', 'Делает видео', 'Заменяет n8n'], a:0},
  {q:'В чём плюс прямого доступа к модели?', o:['Всегда дешевле', 'Свежие фичи раньше и максимальные лимиты', 'Не нужен интернет', 'Работает без промпта'], a:1},
  {q:'Как агрегатор помогает экономить?', o:['Даёт всё бесплатно', 'Платишь по факту и видишь цену каждого запроса', 'Отключает дорогие модели', 'Дарит подписки'], a:1},
  {q:'Что логичнее взять для пачек видео и картинок?', o:['OpenRouter', 'Higgsfield', 'Suno', 'Cursor'], a:1}
  ],
  pairs:[
  ['Агрегатор', 'Много моделей через одну платформу'],
  ['Higgsfield', 'Фото, видео и 3D в одном месте'],
  ['OpenRouter', 'Все LLM через единый ключ'],
  ['Прямой доступ', 'Свежие фичи и максимальные лимиты'],
  ['Оплата по факту', 'Платишь за реальные запросы'],
  ['Единый ключ', 'Подключил раз — гоняешь любую модель']
  ],
  task:{
    intro:'Реши, что тебе выгоднее под твою задачу — агрегатор или прямой доступ. Опиши задачу, объём и обоснуй выбор.',
    chips:['Тип задачи', 'Объём в неделю', 'Higgsfield или OpenRouter', 'Или прямой доступ', 'Почему так', 'Что тестируешь на потоке'],
    ph:'Задача: 10 Reels в неделю с генеративными кадрами и движением. Объём большой и разноформатный. Беру Higgsfield: картинки, видео и 3D в одном окне и одной оплате, не прыгаю по сайтам. Основную LLM для сценариев держу напрямую — упираюсь в лимиты каждый день.',
    verdict:'Сильно — ты развёл всё грамотно: разнообразный видеопоток в агрегатор, а ежедневную упирающуюся в лимиты LLM оставил напрямую. Усилю: заведи привычку раз в неделю сравнивать в агрегаторе цену модели за один готовый кадр — на объёме 10 роликов даже 20% экономии на генерации складываются в ощутимую сумму за месяц.'
  }
},
{
  title:'Лазейки и экономия',
  sub:'3:40 · слайды + тест + игра', dur:'3:40',
  c1:'ЛАЗЕЙКИ', c2:'И ЭКОНОМИЯ',
  slides:[
  {t:'Мышление на потоке', pts:['На одном ролике цена модели незаметна. На сотне — это разница между прибылью и работой в минус.', 'Экономия — не про жадность, а про то, чтобы масштабироваться и не разориться на подписках.', 'Правило: каждую регулярную трату спрашивай — а есть ли то же самое дешевле или бесплатно?'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="18" width="76" height="38" rx="8"/><path d="M22 30h76"/><circle cx="82" cy="42" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Higgsfield Ultra — почти безлимит', pts:['<b>Higgsfield Ultra</b> (~$99/мес) открывает почти безлимитный доступ к топ-моделям фото и видео.', 'Если генеришь много каждый день, это дешевле, чем платить поштучно за каждый кадр в <b>Veo</b> или <b>Runway</b>.', 'Считай точку окупаемости: как только твой объём за месяц дороже подписки — переходи на безлимит.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 50h80l-6-30-20 16-14-24-14 24-20-16z"/><path d="M20 56h80" opacity=".7"/></svg>'},
  {t:'Бесплатная озвучка и self-hosted', pts:['Озвучку на потоке гони через бесплатные локальные нейро-TTS — качество живое, а платишь ноль за символ.', '<b>n8n</b> в self-hosted варианте бесплатен: ставишь на свой сервер и автоматизируешь без ежемесячной платы за платформу.', 'Платные кредиты на озвучку и авто держи только там, где без них правда никак.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="50" y="8" width="20" height="34" rx="10"/><path d="M40 36a20 20 0 0 0 40 0M60 56v8M48 64h24"/></svg>'},
  {t:'Kling — самое дешёвое видео', pts:['<b>Kling</b> — около $0.10 за секунду, один из самых дешёвых генераторов видео на рынке.', 'Черновики и массовые кадры делай на нём, а дорогой <b>Veo</b> или <b>Runway</b> подключай только на витринный финал.', 'На сотне секунд разница с топ-моделью — это уже реальные деньги в кармане, а не абстракция.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 35 46 12h30a8 8 0 0 1 8 8v30L56 78z" transform="scale(.85) translate(6 -4)"/><circle cx="66" cy="27" r="5"/></svg>'},
  {t:'Free-планы и реф-кредиты', pts:['Складывай free-планы стопкой: у многих сервисов бесплатный лимит покрывает тесты и мелкие задачи целиком.', '<b>Реф-кредиты</b> — пригласил друга, получил бонусные генерации. На старте это ощутимая подушка.', 'Не гни на бесплатном продакшн для клиента, но обкатывать гипотезы на free — грамотно и по-хозяйски.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="28" width="68" height="34" rx="6"/><path d="M26 38h68M60 28v34"/><path d="M60 28c-6-14-22-10-16-2 3 4 16 2 16 2zM60 28c6-14 22-10 16-2-3 4-16 2-16 2z"/></svg>'},
  {t:'Твоя экономная связка', pts:['Тесты — на free-планах, поток видео — на <b>Kling</b>, объём фото/видео — под безлимит <b>Higgsfield Ultra</b>.', 'Озвучка — бесплатные локальные TTS, автоматизация — self-hosted <b>n8n</b>. Дорогое — точечно на финал.', 'Веди учёт трат: что окупается, что жрёт деньги впустую. Экономный нейроделец зарабатывает больше при том же объёме.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Почему экономия критична именно на потоке?', o:['На объёме цена модели складывается в разницу прибыль/минус', 'Бесплатное всегда лучше', 'Клиенты требуют дешёвого', 'Модели дорожают каждый день'], a:0},
  {q:'В чём смысл Higgsfield Ultra?', o:['Бесплатная озвучка', 'Почти безлимит топ-моделей за фикс ~$99/мес', 'Самое дешёвое видео', 'Реф-кредиты'], a:1},
  {q:'Какой генератор видео один из самых дешёвых?', o:['Veo', 'Runway', 'Kling', 'Midjourney'], a:2},
  {q:'Как бесплатно поднять n8n?', o:['Купить Ultra', 'Self-hosted на своём сервере', 'Через реф-кредиты', 'Никак'], a:1},
  {q:'Что такое реф-кредиты?', o:['Кредит в банке', 'Бонусные генерации за приглашённого друга', 'Платная подписка', 'Штраф за спам'], a:1},
  {q:'Где уместно использовать free-планы?', o:['Для финального продакшна клиенту', 'Для тестов и обкатки гипотез', 'Нигде', 'Только для видео'], a:1}
  ],
  pairs:[
  ['Higgsfield Ultra', 'Почти безлимит топ-моделей за ~$99'],
  ['Kling', 'Видео от ~$0.10 за секунду'],
  ['n8n self-hosted', 'Бесплатная автоматизация на своём сервере'],
  ['Реф-кредиты', 'Бонусы за приглашённого друга'],
  ['Free-планы', 'Обкатка тестов без оплаты'],
  ['Локальный TTS', 'Озвучка без платы за символ']
  ],
  task:{
    intro:'Собери свою экономную связку под реальный поток: посчитай, где ты сейчас переплачиваешь, и на что это заменить.',
    chips:['Что генеришь и сколько', 'Текущие траты', 'Где Kling вместо дорогого', 'Где безлимит окупится', 'Что уводишь в free/self-hosted', 'Итоговая экономия'],
    ph:'Поток: 15 роликов в неделю. Сейчас плачу поштучно за видео в топ-модели — дорого. План: черновые кадры на Kling, финал точечно на Veo, объём фото под Higgsfield Ultra, озвучка на бесплатном локальном TTS, автоматизация постинга на self-hosted n8n. Экономия ощутимая на дистанции месяца.',
    verdict:'Отлично — ты не просто назвал дешёвые инструменты, а развёл черновик и финал (Kling → Veo) и увёл в бесплатное то, что реально можно (TTS, n8n). Усилю: посчитай точку окупаемости Ultra в цифрах — сколько кадров в месяц должно быть, чтобы подписка стала выгоднее поштучной оплаты; тогда переход будет решением по числам, а не на глаз.'
  }
},
{
  title:'Безопасность, этика, авторские',
  sub:'3:50 · слайды + тест + игра', dur:'3:50',
  c1:'БЕЗОПАС-', c2:'НОСТЬ',
  slides:[
  {t:'Как не спалить нейронку', pts:['Площадки режут охваты откровенно ИИ-контенту. Задача — чтобы ролик считывался как живой, а не как генерёжка.', 'Три кита маскировки: <b>караоке-субтитры</b>, живой человеческий голос и вставки реальных кадров.', 'Голый выхлоп нейронки без обработки — прямой путь в теневой бан и нулевые просмотры.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Признаки ИИ и как их убрать', pts:['Выдаёт синтетика: стерильный голос-робот, идеально гладкая картинка, отсутствие мелких «человеческих» деталей.', 'Прогоняй озвучку через хумонайзер — паузы, интонация, дыхание. Добавляй лёгкий шум, зерно, дрожь камеры в видео.', 'Правь текст живой речью, убирай ИИ-штампы. Микс генерации и реального материала — золотой стандарт.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Дипфейки: только своё лицо', pts:['Чужое лицо без согласия — это не «крутой приём», а прямой путь к иску и блокировке.', 'Дипфейк допустим только со СВОИМ лицом/голосом или с письменного согласия человека.', 'Публичные персоны, знаменитости, коллеги без разрешения — табу. Репутация дороже одного вирусного ролика.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="30" cy="24" r="12"/><path d="M18 24a12 12 0 0 1 24 0"/><path d="M52 16h54M52 27h40M52 38h50" opacity=".85"/></svg>'},
  {t:'Авторские права и лицензия', pts:['Сгенерированное — не всегда «ничьё»: чужие узнаваемые персонажи, бренды и стили защищены.', 'Стоки и модели имеют лицензию: проверь, разрешено ли <b>коммерческое</b> использование конкретно твоего результата.', 'Музыка из <b>Suno</b>, голоса, картинки — читай условия. «Нашёл в интернете» не равно «можно продавать».'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Коммерческое использование', pts:['Делаешь для клиента за деньги — нужна лицензия, разрешающая коммерцию, иначе рискует и клиент.', 'У многих сервисов бесплатный план запрещает коммерческое применение результата — это ловушка на старте.', 'Храни доказательства: скрины лицензий, условия тарифа. Спор решает бумага, а не «я думал, что можно».'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="30" width="52" height="34" rx="8"/><path d="M44 30v-8a16 16 0 0 1 32 0v8"/><circle cx="60" cy="46" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Чек-лист безопасного контента', pts:['Голос живой? Есть субтитры и реальные вставки? Лицо своё или согласованное? Лицензия на коммерцию есть?', 'Нет чужих брендов и персонажей без прав? Условия тарифа разрешают продажу результата?', 'Прошёл по чек-листу — публикуй спокойно. Этика и право здесь не тормоз, а защита твоего заработка.'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Что помогает контенту не считываться как чистая генерёжка?', o:['Максимально гладкая картинка', 'Караоке-субтитры, живой голос, реальные кадры', 'Голос-робот', 'Отсутствие обработки'], a:1},
  {q:'Когда допустим дипфейк?', o:['С любым публичным лицом', 'Только со своим или согласованным лицом', 'С коллегами без спроса', 'Всегда'], a:1},
  {q:'Что значит проверить лицензию модели или стока?', o:['Узнать цену', 'Убедиться, что разрешено коммерческое использование', 'Скачать быстрее', 'Сменить модель'], a:1},
  {q:'Чем опасен бесплатный план для работы на клиента?', o:['Он медленный', 'Часто запрещает коммерческое использование результата', 'Нет русского языка', 'Ничем'], a:1},
  {q:'Как очеловечить озвучку?', o:['Оставить голос-робот', 'Прогнать через хумонайзер: паузы, интонация, дыхание', 'Ускорить в 2 раза', 'Убрать звук'], a:1},
  {q:'«Нашёл картинку в интернете» означает, что...', o:['Её можно свободно продавать', 'Права надо проверять, продажа не автоматом разрешена', 'Она бесплатная навсегда', 'Лицензия не нужна'], a:1}
  ],
  pairs:[
  ['Караоке-субтитры', 'Делают ролик живее для площадки'],
  ['Хумонайзер', 'Оживляет озвучку: паузы и интонация'],
  ['Дипфейк', 'Только со своим или согласованным лицом'],
  ['Коммерческая лицензия', 'Разрешение продавать результат'],
  ['Реальные вставки', 'Микс генерации и живого материала'],
  ['Free-план', 'Часто запрещает коммерцию']
  ],
  task:{
    intro:'Прогони свой готовый или планируемый ролик по чек-листу безопасности и этики. По каждому пункту напиши статус: ок или что доработать.',
    chips:['Живой голос/хумонайзер', 'Субтитры и реальные вставки', 'Лицо своё или согласованное', 'Лицензия на коммерцию', 'Нет чужих брендов', 'Скрины условий тарифа'],
    ph:'Ролик для клиента-кофейни. Голос: локальный TTS + хумонайзер — ок. Субтитры караоке есть, добавил вставку реальной съёмки чашки — ок. Лицо своё в кадре — ок. Музыка из Suno: проверил тариф, коммерция разрешена, скрин сохранил. Чужих логотипов нет — ок.',
    verdict:'Сильно — ты не просто «сделал ролик», а закрыл и техническую маскировку (живой голос, субтитры, реальная вставка), и правовую сторону со скрином лицензии на музыку. Усилю: заведи отдельную папку «лицензии» с датой и тарифом по каждому проекту — если через полгода площадка или клиент задаст вопрос, у тебя будет готовый ответ бумагой, а не по памяти.'
  }
}
  ],
  "a2": [
{
  title:'Тексты, которые продают',
  sub:'3:48 · слайды + тест + игра', dur:'3:48',
  c1:'ТЕКСТ', c2:'ПРОДАЁТ',
  slides:[
  {t:'Нейросеть не пишет за тебя — она пишет С тобой', pts:['<b>ChatGPT</b> и <b>Claude</b> на пустом брифе выдают серую кашу: банальности, которые не цепляют и не продают', 'Продающий текст рождается из <b>фактуры</b>: твой оффер, цена, боль клиента, реальный кейс, возражения', 'Правило: чем больше конкретики в промпте, тем меньше воды в ответе — грузи цифры, а не пожелания', 'ИИ — это <b>усилитель смысла</b>, а не генератор смысла. Смысл приносишь ты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Хук решает всё: 3 секунды на удержание', pts:['Первая строка поста и первые 2 секунды <b>Reels</b>決 決 — если не зацепил, дальше не читают', 'Рабочие типы хуков: цифра-обещание, разрыв шаблона, вопрос-боль, антисовет («не делай так»)', 'Проси <b>Claude</b> дать 10 вариантов хука на один текст — выбираешь сильнейший, остальное в банк', 'Плохой хук: «Сегодня расскажу про...». Сильный: «Слил 40 тысяч на рекламу, пока не понял одну вещь»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Структура продающего поста: скелет, на который вешаешь мясо', pts:['<b>Хук</b> → усиление боли → разворот (а можно иначе) → решение → доказательство → <b>призыв</b>', 'Один пост — одна мысль. Пытаешься впихнуть три — не заходит ни одна', 'Абзацы по 1-2 строки: с телефона стена текста = мгновенный свайп', 'Финал всегда закрывает действием: написать в директ, забрать гайд, оставить «+» в комментах'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Прогрев: продажа без слова «купи»', pts:['Прогрев — это <b>серия касаний</b>, где ты снимаешь возражения ДО того, как назвал цену', 'Классика: боль → своя история провала → инсайт → трансформация → мягкое предложение', 'Проси <b>ChatGPT</b> расписать прогрев на 7 сторис под конкретный продукт и одно ключевое возражение', 'Люди покупают не курс, а <b>версию себя</b> после курса — показывай результат, а не программу'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Промпт, который выжимает из ИИ максимум', pts:['Задай <b>роль</b> («ты копирайтер прямого отклика»), <b>контекст</b> (продукт, цена, аудитория), <b>задачу</b> и <b>формат</b>', 'Добавь ограничения: длина, тон, запрет на канцелярит и клише вроде «в современном мире»', 'Проси в <b>Claude</b> сразу 3 версии под разные сегменты — новичок, сомневающийся, готовый купить', 'Не бери первый ответ. Дожимай: «сделай хук жёстче», «убери воду», «добавь конкретную цифру»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Редактура: где ИИ ломается и ты правишь руками', pts:['Нейросеть любит вводные обороты, тройки перечислений и слово «важно» — вычищай на автомате', 'Проверяй факты и цифры: <b>ChatGPT</b> уверенно придумывает статистику, которой нет', 'Читай вслух: спотыкаешься — переписываешь. Живой текст звучит как речь, а не как документ', 'Добавляй свои словечки и детали из жизни — именно они отличают тебя от тысячи одинаковых постов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M30 54h60" opacity=".6"/><path d="M40 46l30-30 8 8-30 30-12 4z"/><path d="M64 22l8 8"/></svg>'}
  ],
  quiz:[
  {q:'Почему ИИ на коротком брифе выдаёт слабый продающий текст?', o:['Модель специально экономит ресурсы', 'Без фактуры (оффер, цена, боль, кейс) ей не из чего строить смысл', 'Русский язык модели даётся хуже английского', 'Продающие тексты запрещены правилами нейросети'], a:1},
  {q:'Сколько секунд есть у хука в Reels, чтобы удержать зрителя?', o:['Около 30 секунд', 'Примерно минуту', '2-3 секунды', 'Время не имеет значения'], a:2},
  {q:'Что обязательно должно быть в финале продающего поста?', o:['Хештеги по теме', 'Конкретный призыв к действию', 'Ссылка на источник статистики', 'Извинение за длинный текст'], a:1},
  {q:'В чём суть прогрева?', o:['Много раз написать слово «купи»', 'Снять возражения серией касаний до объявления цены', 'Один раз выложить прайс и ждать', 'Скинуть цену вдвое в последний день'], a:1},
  {q:'Какой элемент промпта задаёт нейросети угол подачи?', o:['Роль («ты копирайтер прямого отклика»)', 'Дата запроса', 'Количество эмодзи', 'Название модели'], a:0},
  {q:'Что чаще всего приходится править руками после ChatGPT?', o:['Раскладку клавиатуры', 'Клише, канцелярит и выдуманные цифры', 'Кодировку файла', 'Порядок абзацев всегда идеальный'], a:1}
  ],
  pairs:[
  ['Хук', 'Первая строка, которая цепляет за 2-3 секунды'],
  ['Оффер', 'Конкретное предложение с ценностью и ценой'],
  ['Прогрев', 'Серия касаний, снимающая возражения до продажи'],
  ['Призыв', 'Действие, которым закрывается текст'],
  ['Роль в промпте', 'Задаёт нейросети угол и тон подачи'],
  ['Канцелярит', 'Сухие обороты, которые убивают живой текст']
  ],
  task:{
    intro:'Возьми свой продукт или услугу и напиши промпт для Claude на продающий пост. Пропиши роль, продукт, цену, боль клиента и одно главное возражение. Затем в поле ниже дай сам получившийся хук — первую строку поста.',
    chips:['Роль: копирайтер прямого отклика', 'Продукт и цена', 'Боль аудитории одной фразой', 'Главное возражение', 'Тон: живой, без канцелярита', 'Формат: пост до 900 знаков'],
    ph:'Слил первые 30 тысяч на таргет, пока не понял: дело было не в бюджете, а в одной строчке оффера',
    verdict:'Отличный хук — есть конкретная цифра, личный провал и обещание инсайта, это цепляет за 2 секунды. Усиль следующим шагом: сразу после хука дай одно осязаемое доказательство результата (было/стало в цифрах), чтобы читатель поверил ещё до оффера.'
  }
},
{
  title:'Аватары (Higgsfield Soul)',
  sub:'3:32 · слайды + тест + игра', dur:'3:32',
  c1:'ЦИФРОВОЙ', c2:'ДВОЙНИК',
  slides:[
  {t:'Аватар — это ты, которого можно поставить в любую сцену', pts:['<b>Higgsfield Soul</b> обучает <b>цифрового двойника</b> на 5-20 твоих фото и запоминает лицо', 'Дальше генерируешь себя где угодно: студия, улица Токио, обложка журнала — без съёмки', 'Это снимает главную боль контента: «нет времени/сил постоянно снимать себя на камеру»', 'Двойник работает 24/7 — пока ты спишь, у тебя есть готовый актёр для любого кадра'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="30" cy="24" r="12"/><path d="M18 24a12 12 0 0 1 24 0"/><path d="M52 16h54M52 27h40M52 38h50" opacity=".85"/></svg>'},
  {t:'Датасет решает: мусор на входе — мусор на выходе', pts:['Бери 10-15 фото с <b>разных ракурсов</b>: анфас, профиль, полуоборот, разный свет и эмоции', 'Только ты в кадре, лицо резкое, без фильтров, без масок и очков, без других людей рядом', 'Разная одежда и фон — модель учит лицо, а не запоминает конкретный свитер', '5 однотипных селфи = кривой двойник. 15 разнообразных = стабильное сходство'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="18" width="54" height="38" rx="7"/><path d="M74 30l24-12v38L74 42z"/><circle cx="40" cy="37" r="9"/></svg>'},
  {t:'Промпт для сцены: режиссёр, а не проситель', pts:['Описывай кадр как режиссёр: план (крупный/средний), свет, локация, настроение, одежда', 'Промпт на английском и подробный — <b>Higgsfield Soul</b> любит детали композиции и света', 'Пример: «cinematic medium shot, soft window light, minimalist office, confident expression»', 'Меняешь сцену — меняешь только окружение, лицо остаётся твоим за счёт обученного двойника'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M74 12 92 30 56 66l-18 4 4-18z"/><path d="M62 24 80 42" opacity=".7"/></svg>'},
  {t:'Где это реально приносит деньги', pts:['Экспертный контент без вечных съёмок: генеришь себя под каждый пост и <b>Reels</b>', 'Обложки, баннеры, рекламные креативы с твоим лицом — единый узнаваемый образ', 'Прогрев и линейка сторис, где ты в разных локациях, а на съёмку ушло 0 часов', 'Первый ощутимый результат — экономия 5-10 часов в неделю на продакшене'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Ограничения, о которых молчат в рекламе', pts:['Мелкая мимика и руки — слабое место генерации, проверяй пальцы и глаза на каждом кадре', 'Двойник хорош для фото и коротких клипов, но это не замена живому эфиру и доверию голосом', 'Одежду с логотипами и мелкий текст модель искажает — держи такие детали за кадром', 'Этика: показывай, что это ИИ-контент там, где выдача за реальную съёмку вводит в заблуждение'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'}
  ],
  quiz:[
  {q:'Сколько фото обычно нужно, чтобы обучить двойника в Higgsfield Soul?', o:['1-2 фото', '5-20 фото', 'Не меньше 500 фото', 'Фото не нужны вовсе'], a:1},
  {q:'Какой датасет даст самое стабильное сходство?', o:['15 фото с разных ракурсов, светом и эмоциями', '5 одинаковых селфи с одного ракурса', 'Фото в маске и тёмных очках', 'Групповые фото с друзьями'], a:0},
  {q:'Почему в датасете лучше менять одежду и фон?', o:['Так файлы весят меньше', 'Чтобы модель учила лицо, а не конкретный наряд', 'Это требование лицензии', 'Иначе двойник не сгенерируется вообще'], a:1},
  {q:'Что чаще всего искажается в генерации аватара?', o:['Цвет неба', 'Руки, пальцы и мелкая мимика', 'Разрешение всего кадра', 'Формат файла'], a:1},
  {q:'Где двойник даёт максимальную пользу?', o:['Замена живого эфира с голосом', 'Экономия часов на съёмке контента и креативов', 'Юридические документы', 'Замена паспортного фото'], a:1}
  ],
  pairs:[
  ['Цифровой двойник', 'Обученная на твоих фото модель твоего лица'],
  ['Датасет', 'Набор из 5-20 фото для обучения'],
  ['Разные ракурсы', 'Условие стабильного сходства'],
  ['Промпт-сцена', 'Описание кадра: свет, план, локация'],
  ['Слабое место', 'Руки, пальцы и мелкая мимика'],
  ['Выгода', 'Часы, сэкономленные на съёмке']
  ],
  task:{
    intro:'Собери бриф на своего цифрового двойника. Опиши, какие 12-15 фото ты отберёшь в датасет, и придумай одну конкретную сцену, в которой хочешь себя сгенерировать. В поле ниже дай англоязычный промпт этой сцены.',
    chips:['Анфас, профиль, полуоборот', 'Разный свет и эмоции', 'Только ты, лицо резкое', 'Разная одежда и фон', 'Сцена: локация + план + настроение', 'Промпт на английском'],
    ph:'cinematic medium shot, confident smile, soft golden hour light through a window, minimalist scandinavian office, wearing a dark knit sweater, shallow depth of field, natural skin texture',
    verdict:'Сильный бриф — ракурсы разнообразные, а промпт по-режиссёрски описывает свет, план и настроение, модель такое любит. Добавь усиление: заранее заложи 3-4 сцены под разные форматы (пост, обложка, сторис), чтобы с одного обучения двойника закрыть неделю контента, а не один кадр.'
  }
},
{
  title:'Озвучка (ElevenLabs и edge-tts)',
  sub:'3:40 · слайды + тест + игра', dur:'3:40',
  c1:'ГОЛОС', c2:'РЕШАЕТ',
  slides:[
  {t:'Голос — это половина удержания в видео', pts:['Плоская роботная озвучка убивает даже сильный сценарий: зритель свайпает на первой фразе', '<b>ElevenLabs</b> даёт реалистичную речь с интонацией и паузами (~$5-22/мес по объёму)', '<b>edge-tts</b> — бесплатный вариант с русскими голосами, для черновиков и потока незаменим', 'Правило: тестируй сценарий в бесплатном, финалку под ключевые ролики гони в платном'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="50" y="8" width="20" height="34" rx="10"/><path d="M40 36a20 20 0 0 0 40 0M60 56v8M48 64h24"/></svg>'},
  {t:'ElevenLabs: где живёт эмоция', pts:['Настройки <b>Stability</b> и <b>Similarity</b> рулят характером: ниже стабильность — живее и эмоциональнее', 'Умеет <b>дубляж</b> — переозвучка ролика на другой язык с сохранением тембра', 'Расставляй паузы и знаки препинания в тексте — модель читает пунктуацию как режиссуру', 'Экономь символы: это платно за знак, лишние черновики жги в бесплатном движке'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="50" y="8" width="20" height="34" rx="10"/><path d="M40 36a20 20 0 0 0 40 0M60 56v8M48 64h24"/></svg>'},
  {t:'edge-tts: бесплатный рабочий конь', pts:['Русские голоса (Dmitry, Svetlana) звучат чисто, для информационного контента хватает', 'Ноль лимитов по стоимости — гоняй хоть сотню роликов в день на черновой сборке', 'Минусы: меньше эмоции и живости, ошибки в ударениях на сложных словах', 'Лечится SSML и ручной расстановкой ударений через букву «ё» и знаки пауз'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'},
  {t:'Скрипт под озвучку пишется иначе, чем текст для чтения', pts:['Короткие фразы, одна мысль — одно предложение: длинные периоды нейроголос заваливает', 'Пиши как говоришь: разговорные связки, паузы после сильных фраз, без причастных нагромождений', 'Цифры и аббревиатуры прописывай словами, чтобы TTS не прочитал их криво', 'Читай текст вслух перед озвучкой — где спотыкаешься сам, там споткнётся и модель'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Гумонизация: убираем «робота» из речи', pts:['Правь ударения вручную — главный признак дешёвой озвучки это неверный акцент в слове', 'Добавляй микропаузы перед важным и лёгкое ускорение на перечислениях под темп ролика', 'Синхронь голос с музыкой и монтажом: акцент речи ложится на смену кадра', 'Финальный чек: закрой глаза и слушай — веришь голосу или слышишь генерацию'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'}
  ],
  quiz:[
  {q:'Чем edge-tts выгоднее ElevenLabs для потока черновиков?', o:['Он полностью бесплатный и без лимитов по стоимости', 'У него больше эмоции', 'Он не делает ошибок в ударениях', 'Он умеет дубляж на 50 языков'], a:0},
  {q:'За что в ElevenLabs отвечает параметр Stability?', o:['За громкость файла', 'За стабильность против эмоциональности голоса', 'За язык озвучки', 'За формат экспорта'], a:1},
  {q:'Главный признак дешёвой роботной озвучки — это...', o:['Высокое разрешение', 'Неверные ударения в словах', 'Слишком длинный файл', 'Отсутствие музыки'], a:1},
  {q:'Как писать скрипт под нейроозвучку?', o:['Длинными сложными периодами', 'Короткими фразами, одна мысль — одно предложение', 'Только заглавными буквами', 'С максимумом аббревиатур'], a:1},
  {q:'Что делает функция дубляжа в ElevenLabs?', o:['Удваивает громкость', 'Переозвучивает ролик на другой язык с сохранением тембра', 'Создаёт две копии файла', 'Добавляет эхо'], a:1}
  ],
  pairs:[
  ['ElevenLabs', 'Реалистичная платная озвучка с эмоцией'],
  ['edge-tts', 'Бесплатные русские голоса для потока'],
  ['Stability', 'Баланс стабильности и эмоциональности'],
  ['Дубляж', 'Переозвучка на другой язык с тембром'],
  ['Гумонизация', 'Правка ударений, пауз, темпа под живость'],
  ['Скрипт под TTS', 'Короткие фразы, слова вместо цифр']
  ],
  task:{
    intro:'Возьми 4-5 предложений своего сценария и перепиши их специально под нейроозвучку. Разбей на короткие фразы, пропиши цифры словами, расставь паузы. В поле ниже вставь готовый под-озвучку фрагмент.',
    chips:['Одна мысль — одно предложение', 'Цифры прописью', 'Паузы после сильных фраз', 'Разговорные связки', 'Ударения через «ё» где нужно', 'Читаемо вслух без спотыканий'],
    ph:'Смотри. Первые деньги пришли не с рекламы. Они пришли с одного поста. Всего одного. Я вложил ноль рублей — и получил семь заявок за сутки.',
    verdict:'Хорошо переписано — фразы рубленые, есть паузы, цифра «семь» словами, такое нейроголос прочитает чисто и живо. Усиль так: пометь одно слово, на которое должен лечь акцент (например «ноль рублей»), и синхронь его со сменой кадра в монтаже — эмоция голоса и картинка сработают в один удар.'
  }
},
{
  title:'Музыка (Suno)',
  sub:'3:26 · слайды + тест + игра', dur:'3:26',
  c1:'МУЗЫКА', c2:'ПОД СМЫСЛ',
  slides:[
  {t:'Музыка — это невидимый сценарист эмоции', pts:['Зритель не осознаёт трек, но именно он держит темп и решает, досмотрит человек или свайпнет', '<b>Suno</b> (~$10/мес) генерит треки, джинглы и фоновую музыку под смысл ролика по текстовому промпту', 'Один и тот же видеоряд под драйв и под лирику — два разных ролика по восприятию', 'Готовая музыка убирает боль с авторскими правами и вечным поиском по стокам'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M50 46V16l32-6v30"/><circle cx="44" cy="48" r="8"/><circle cx="76" cy="42" r="8"/></svg>'},
  {t:'Промпт для Suno: жанр, темп, настроение, инструменты', pts:['Описывай <b>жанр</b>, <b>BPM</b>/темп, настроение, ключевые инструменты и вокал (или инструментал)', 'Пример: «energetic lo-fi hip hop, 90 bpm, warm vinyl, no vocals, motivational»', 'Для фона под речь бери инструментал — вокал перебивает озвучку и путает зрителя', 'Генерь по 3-4 варианта на один бриф: из партии выбираешь лучший, остальное в банк'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Джингл и звуковой логотип: узнаваемость за 3 секунды', pts:['<b>Джингл</b> — короткая музыкальная подпись бренда, которую узнают ещё до логотипа', 'Ставь один и тот же джингл на каждый ролик — это строит <b>звуковую айдентику</b>', 'В <b>Suno</b> задавай короткий формат и запоминающийся мотив в 2-4 секунды', 'Звуковой логотип экономит внимание: зритель по звуку понимает «это они» на автомате'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l13 27 29 4-21 21 5 29-26-14-26 14 5-29-21-21 29-4z"/></svg>'},
  {t:'Музыка под структуру ролика, а не наоборот', pts:['Подбирай трек под драматургию: спокойный вход, нарастание к сути, акцент на выводе', 'Монтажные склейки сажай на биты — совпадение кадра с долей ощущается как «дорого»', 'На хуке музыка тише голоса, на без-текстовых вставках выводи её вперёд', 'Тишина — тоже приём: обрубленный трек перед сильной фразой бьёт сильнее любого баса'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Юридическая чистота и рутина потока', pts:['Сгенеренный в <b>Suno</b> трек снимает риск блокировок за чужую музыку в Reels и Shorts', 'Держи <b>библиотеку</b> своих треков по настроениям: драйв, лирика, деловое, тревожное', 'Один бриф на неделю контента: генеришь пачку под разные форматы за один заход', 'Называй файлы по настроению и BPM — на потоке это экономит часы поиска нужного'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Почему для фона под озвучку берут инструментал?', o:['Он дешевле в генерации', 'Вокал перебивает речь и путает зрителя', 'Инструментал громче', 'Так требует площадка'], a:1},
  {q:'Что задаёт хороший промпт для Suno?', o:['Только название трека', 'Жанр, темп, настроение и инструменты', 'Имя исполнителя для копирования', 'Разрешение видео'], a:1},
  {q:'Что такое джингл?', o:['Полноценная песня на 3 минуты', 'Короткая музыкальная подпись бренда', 'Настройка громкости', 'Формат экспорта аудио'], a:1},
  {q:'Зачем сажать монтажные склейки на биты?', o:['Чтобы файл весил меньше', 'Совпадение кадра с долей ощущается как дорогой монтаж', 'Так быстрее рендерится', 'Это требование Suno'], a:1},
  {q:'Чем сгенерированный в Suno трек лучше чужого популярного?', o:['Он всегда громче', 'Он снимает риск блокировок за авторские права', 'Его нельзя скачать', 'Он длиннее по умолчанию'], a:1}
  ],
  pairs:[
  ['Suno', 'Генератор музыки под смысл по промпту'],
  ['Джингл', 'Короткая музыкальная подпись бренда'],
  ['Инструментал', 'Фон под речь без перебивающего вокала'],
  ['Биты', 'Точки, куда сажают монтажные склейки'],
  ['Тишина', 'Приём-акцент перед сильной фразой'],
  ['Библиотека треков', 'Свои композиции по настроениям']
  ],
  task:{
    intro:'Придумай промпт для Suno под свой типовой ролик. Определи жанр, темп, настроение и роль музыки (фон под речь или самостоятельный трек). В поле ниже дай англоязычный промпт.',
    chips:['Жанр и настроение', 'Темп/BPM', 'Инструментал или вокал', 'Ключевые инструменты', 'Роль: фон или трек', 'Промпт на английском'],
    ph:'warm motivational lo-fi hip hop, 88 bpm, soft piano and vinyl crackle, no vocals, calm confident mood, for a talking-head expert reel',
    verdict:'Точный промпт — есть темп, инструменты, инструментал под речь и указана задача ролика, Suno выдаст рабочий фон с первого раза. Усилишь так: сразу собери мини-библиотеку из трёх настроений (драйв, лирика, деловое) под один бренд-джингл в начале — тогда неделя роликов будет звучать как один узнаваемый канал, а не набор случайных треков.'
  }
},
{
  title:'Автосборка — контент-завод',
  sub:'4:04 · слайды + тест + игра', dur:'4:04',
  c1:'КОНТЕНТ', c2:'ЗАВОД',
  slides:[
  {t:'Контент-завод: не один ролик, а конвейер', pts:['Смысл завода — превратить ручной героизм в <b>повторяемый пайплайн</b>: делаешь не «ролик», а систему', 'Базовая цепочка: <b>картинка → видео → озвучка → монтаж</b> с субтитрами и инфографикой', 'Каждый этап — отдельный инструмент, а результат стыкуется в единую сборку', 'Цель — выйти на ролик в день без выгорания: от идеи до готового файла по накатанной'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Этапы пайплайна: кто за что отвечает', pts:['Текст и сценарий — <b>ChatGPT</b>/<b>Claude</b>; картинка — <b>Midjourney</b>/<b>Flux</b>/<b>Nano Banana</b>', 'Оживление в видео — <b>Higgsfield</b>/<b>Kling</b>/<b>Runway</b>; озвучка — <b>ElevenLabs</b>; музыка — <b>Suno</b>', 'Сборка, субтитры и инфографика — на этапе монтажа, где всё сходится в один файл', 'Каждый узел заменяем: сломался инструмент — меняешь звено, а не весь завод'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Караоке-субтитры и инфографика — не украшение, а удержание', pts:['Большинство смотрит <b>без звука</b> — субтитры удерживают тех, кто листает в тишине', '<b>Караоке-субтитры</b> подсвечивают слово в такт речи и держат взгляд на экране', 'Инфографика выносит цифры и списки в кадр — зритель схватывает суть, не вслушиваясь', 'Ключевое слово фразы делай крупнее и цветом бренда — глаз цепляется за акцент'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Автоматизация связок: где живёт настоящая скорость', pts:['Ручная стыковка этапов — узкое горло; связывай узлы через <b>n8n</b>/<b>Make</b>/<b>Zapier</b>', 'Шаблон сборки один раз собрал — дальше меняешь только контент, а не структуру', 'Пакетный подход: генеришь пачку картинок и озвучек разом, монтаж идёт по конвейеру', 'Метрика завода — не «красиво», а <b>время на ролик</b>: цель загнать его в десятки минут'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="16" r="7"/><circle cx="26" cy="52" r="7"/><circle cx="94" cy="52" r="7"/><path d="M56 22 30 46M64 22l26 24M33 52h54" opacity=".8"/></svg>'},
  {t:'Разнообразие против штампа: завод не значит клоны', pts:['Опасность конвейера — одинаковые ролики; веди <b>реестр приёмов</b> и не повторяй эффекты', 'Меняй хуки, переходы, инфографику и ракурсы — один шаблон, разное наполнение', 'Держи банк заготовок: хуки, переходы, музыка по настроениям — собираешь как из кубиков', 'Первый ощутимый результат завода — стабильные 5-7 роликов в неделю без потери качества'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Контроль качества на выходе', pts:['Чек-лист перед публикацией: сходство лица, ударения, синхрон субтитров, читаемость с телефона', 'Смотри готовый ролик со звуком и без — обе аудитории должны всё понять', 'Проверяй первые 2 секунды отдельно: слабый старт обнуляет всю сборку', 'Плохой кадр лучше пересобрать, чем выложить — один провальный ролик бьёт по доверию канала'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'В чём главная идея контент-завода?', o:['Сделать один идеальный ролик', 'Построить повторяемый пайплайн вместо ручного героизма', 'Купить дорогую камеру', 'Нанять большую команду'], a:1},
  {q:'Какая базовая цепочка этапов у завода?', o:['Картинка → видео → озвучка → монтаж', 'Съёмка → печать → рассылка', 'Только монтаж', 'Озвучка → тишина → публикация'], a:0},
  {q:'Почему караоке-субтитры критичны?', o:['Они увеличивают разрешение', 'Большинство смотрит без звука, субтитры удерживают', 'Так требует закон', 'Они ускоряют рендер'], a:1},
  {q:'Чем связывают этапы, чтобы убрать ручную стыковку?', o:['n8n, Make или Zapier', 'Только вручную', 'Через фотошоп', 'Через таблицу без автоматизации'], a:0},
  {q:'Как избежать одинаковых роликов на конвейере?', o:['Использовать один и тот же эффект везде', 'Вести реестр приёмов и не повторять эффекты', 'Публиковать реже', 'Отключить субтитры'], a:1},
  {q:'Какая метрика реально измеряет эффективность завода?', o:['Количество лайков режиссёра', 'Время на один ролик', 'Вес итогового файла', 'Число вкладок в браузере'], a:1}
  ],
  pairs:[
  ['Пайплайн', 'Повторяемая цепочка этапов сборки'],
  ['Караоке-субтитры', 'Подсветка слова в такт речи'],
  ['Инфографика', 'Вынос цифр и списков в кадр'],
  ['n8n / Make', 'Автоматизация связок между узлами'],
  ['Реестр приёмов', 'Защита от одинаковых роликов'],
  ['Время на ролик', 'Главная метрика завода']
  ],
  task:{
    intro:'Опиши свой контент-завод на один ролик: распиши цепочку этапов и укажи, какой инструмент отвечает за каждый. В поле ниже дай пайплайн одной строкой в виде связки.',
    chips:['Сценарий (ChatGPT/Claude)', 'Картинка (Midjourney/Flux)', 'Оживление (Kling/Runway)', 'Озвучка (ElevenLabs)', 'Музыка (Suno)', 'Монтаж + караоке-субтитры'],
    ph:'Claude пишет сценарий → Flux рисует кадры → Kling оживляет → ElevenLabs озвучивает → Suno даёт фон → монтаж с караоке-субтитрами и вынесенной в кадр цифрой',
    verdict:'Чёткий пайплайн — все узлы на месте, каждый этап закреплён за инструментом, такое реально гнать в поток. Усиль устойчивость: пометь у каждого звена запасной инструмент (например Runway вместо Kling), чтобы сбой одного сервиса не останавливал весь завод, и заведи реестр приёмов, иначе через месяц ролики станут близнецами.'
  }
},
{
  title:'Апскейл и качество',
  sub:'3:18 · слайды + тест + игра', dur:'3:18',
  c1:'КАЧЕСТВО', c2:'2K / 4K',
  slides:[
  {t:'Апскейл — это не «сделать больше», а «сделать чётче»', pts:['<b>Апскейл</b> увеличивает разрешение и дорисовывает детали: из мыльного 720p — резкое 2K/4K', 'Нейросеть не растягивает пиксели, а <b>достраивает</b> текстуру кожи, волос, ткани', 'Зачем: лента давит некачественное, а чёткая картинка читается как «дорогой» контент', 'Правило потока: генерь в рабочем разрешении быстро, а финалку прогоняй через апскейл'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Артефакты: враг, которого надо видеть', pts:['Типовой мусор генерации: кривые пальцы, плывущие глаза, шум, ореолы по контуру, «ватные» края', 'Апскейл-модели чистят шум и мелкие артефакты, но грубый брак усиливают, а не прячут', 'Сначала <b>чистка</b> и отбраковка кадра, потом увеличение — не наоборот', 'Смотри на 100% зумом: с телефона мелкий артефакт незаметен, на большом экране — вылезет'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Апскейл видео vs апскейл картинки', pts:['Картинку апскейлят покадрово — качество на кадр выше, времени меньше', 'Видео тяжелее: важна <b>консистентность между кадрами</b>, иначе появляется мерцание деталей', 'Для видео бери инструменты с временной стабилизацией, не просто покадровый рост', 'Апскейлить есть смысл только чистый исходник — увеличенный брак = крупный брак'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M52 24l20 11-20 11z" fill="currentColor" stroke="none"/></svg>'},
  {t:'Разрешение под площадку, а не «чем больше тем лучше»', pts:['Reels/Shorts — вертикаль 1080x1920; гнать сюда 8K бессмысленно, площадка всё равно сожмёт', '2K/4K оправдан для обложек, превью, печати и кадров, которые зритель разглядывает', 'Слишком большой файл = долгая загрузка и лишний вес без выигрыша в восприятии', 'Держи баланс: качество, которое видно, а не цифра в свойствах файла'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Финишная доводка перед публикацией', pts:['После апскейла — лёгкая цветокоррекция и резкость под единый стиль канала', 'Проверяй кожу: перешарп даёт пластик, недошарп — мыло, ищи золотую середину', 'Экспортируй под кодек площадки, чтобы платформа не пережала твой чистый кадр в кашу', 'Финальный чек с телефона: контент живёт на маленьком экране, там и оценивай результат'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Что на самом деле делает апскейл?', o:['Просто растягивает пиксели', 'Увеличивает разрешение и достраивает детали', 'Уменьшает размер файла', 'Меняет формат видео'], a:1},
  {q:'В каком порядке правильно работать с кадром?', o:['Сначала апскейл, потом чистка', 'Сначала чистка и отбраковка, потом апскейл', 'Порядок не важен', 'Только апскейл без чистки'], a:1},
  {q:'Чем апскейл видео сложнее апскейла картинки?', o:['Видео весит меньше', 'Нужна консистентность между кадрами против мерцания', 'Видео нельзя увеличить вообще', 'Картинки не апскейлятся'], a:1},
  {q:'Почему гнать 8K в Reels обычно бессмысленно?', o:['8K запрещён в вертикали', 'Площадка всё равно сожмёт до своего разрешения', '8K хуже по цвету', 'Телефоны не показывают вертикаль'], a:1},
  {q:'К чему приводит перешарп на коже?', o:['Эффект пластика', 'Идеальную натуральность', 'Уменьшение файла', 'Исчезновение артефактов'], a:0}
  ],
  pairs:[
  ['Апскейл', 'Рост разрешения с дорисовкой деталей'],
  ['Артефакты', 'Кривые пальцы, шум, ореолы, плывущие глаза'],
  ['Чистка', 'Отбраковка и удаление мусора до увеличения'],
  ['Консистентность кадров', 'Ключ к апскейлу видео без мерцания'],
  ['Перешарп', 'Пластиковая кожа от лишней резкости'],
  ['Разрешение под площадку', 'Качество, которое видно, а не цифра']
  ],
  task:{
    intro:'Опиши свой чек-лист доводки кадра перед публикацией: что проверяешь, в каком порядке чистишь и апскейлишь, под какое разрешение готовишь. В поле ниже дай короткий чек-лист по шагам.',
    chips:['Отбраковка брака (пальцы, глаза)', 'Чистка шума и артефактов', 'Апскейл до 2K/4K', 'Резкость без пластика', 'Разрешение под площадку', 'Финальный просмотр с телефона'],
    ph:'1) Зум 100%, отбраковываю кадры с кривыми пальцами. 2) Чищу шум. 3) Апскейл до 2K для обложки, рабочие Reels оставляю 1080x1920. 4) Лёгкая резкость, проверяю кожу на пластик. 5) Смотрю с телефона.',
    verdict:'Грамотный чек-лист — сначала отбраковка и чистка, апскейл только под задачу, а не всё подряд в 4K, это экономит время и вес. Усиль контроль: добавь шаг проверки одного и того же кадра на большом экране и на телефоне — артефакт, невидимый на маленьком, вылезет на десктопе, и наоборот пластик кожи заметнее крупно.'
  }
},
{
  title:'Стиль и консистентность',
  sub:'3:44 · слайды + тест + игра', dur:'3:44',
  c1:'ЕДИНЫЙ', c2:'СТИЛЬ',
  slides:[
  {t:'Консистентность — то, что превращает ролики в бренд', pts:['Разрозненные красивые кадры — это набор картинок; единый стиль — это <b>узнаваемый канал</b>', 'Зритель узнаёт тебя в ленте за долю секунды по цвету, шрифту, свету и персонажу', 'Узнаваемость = доверие = продажи: люди покупают у тех, кого <b>помнят</b>', 'Стиль строится не на вдохновении, а на <b>правилах</b>, которые ты повторяешь из ролика в ролик'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Бренд-стиль: фиксируй параметры письменно', pts:['Пропиши <b>палитру</b>, шрифты, тип света, настроение и композицию — это твой визуальный паспорт', 'Вставляй эти параметры в каждый промпт: не «красиво», а конкретные цвета и свет', 'Один описанный стиль → десятки кадров в едином ключе без разнобоя', 'Без паспорта каждый ролик тянет в свою сторону, и канал выглядит как склейка разных авторов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 16c-8-6-22-6-30 0v40c8-6 22-6 30 0 8-6 22-6 30 0V16c-8-6-22-6-30 0z"/><path d="M60 16v40"/></svg>'},
  {t:'Консистентный персонаж: одно лицо во всех сценах', pts:['<b>Runway</b> и референсы держат одного и того же персонажа стабильным от кадра к кадру', 'Работай через <b>референс</b> лица/образа — модель повторяет его в любой сцене', 'Для своего лица — обученный двойник; для вымышленного героя — фикс референса и описания', 'Стабильный персонаж делает сериал из роликов, а зритель привязывается к герою'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="24" r="11"/><path d="M40 58c1-13 9-20 20-20s19 7 20 20"/></svg>'},
  {t:'Сид и референсы: рычаги повторяемости', pts:['<b>Сид</b> (seed) — число, фиксирующее генерацию: тот же сид + тот же промпт = близкий результат', 'Меняешь одну деталь при том же сиде — получаешь вариацию, а не совершенно новый кадр', '<b>Референс</b>-изображение задаёт композицию, цвет и образ там, где слов не хватает', 'Комбо «сид + референс + бренд-промпт» — основа предсказуемого потока в едином стиле'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="35" r="14"/><path d="M48 35h44M78 35v12M92 35v10"/></svg>'},
  {t:'Баланс: единый стиль, но не однообразие', pts:['Консистентность — про <b>узнаваемые константы</b> (цвет, шрифт, герой), а не про клоны кадров', 'Держи постоянным каркас, меняй наполнение: сюжет, ракурс, инфографику, хук', 'Опасность перегиба — все ролики на одно лицо, зритель устаёт и листает', 'Правило: узнал за секунду, но досмотрел, потому что внутри каждый раз новое'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Система, а не разовый успех', pts:['Собери <b>бренд-кит</b>: палитра, шрифты, шаблон субтитров, джингл, референсы персонажа', 'Заведи стартовый промпт-шаблон со всеми константами — новый ролик стартует не с нуля', 'Первый результат системы — канал, который узнают без логотипа по одному кадру', 'Стиль, записанный в правила, переживает смену настроения, усталость и любой инструмент'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Что превращает набор красивых роликов в бренд?', o:['Максимальное разрешение', 'Единый узнаваемый стиль, повторяемый из ролика в ролик', 'Частота публикаций', 'Количество эффектов в кадре'], a:1},
  {q:'Что такое сид (seed) в генерации?', o:['Название модели', 'Число, фиксирующее генерацию для повторяемого результата', 'Формат экспорта', 'Уровень громкости'], a:1},
  {q:'Как удержать одного персонажа во всех сценах?', o:['Каждый раз описывать заново словами', 'Через референс и инструменты вроде Runway', 'Менять лицо в каждом ролике', 'Убрать персонажа из кадра'], a:1},
  {q:'Зачем нужен письменный бренд-паспорт стиля?', o:['Для отчётности перед площадкой', 'Чтобы вставлять константы в каждый промпт и держать единый ключ', 'Чтобы увеличить вес файлов', 'Он не нужен вовсе'], a:1},
  {q:'В чём риск перегиба с консистентностью?', o:['Ролики становятся слишком чёткими', 'Все ролики на одно лицо, зритель устаёт и листает', 'Падает разрешение', 'Пропадает звук'], a:1}
  ],
  pairs:[
  ['Консистентность', 'Узнаваемые константы стиля во всех роликах'],
  ['Бренд-паспорт', 'Записанные палитра, шрифты, свет, настроение'],
  ['Консистентный персонаж', 'Одно лицо и образ во всех сценах'],
  ['Сид (seed)', 'Число, фиксирующее повторяемость генерации'],
  ['Референс', 'Изображение, задающее композицию и образ'],
  ['Бренд-кит', 'Палитра, шрифты, джингл, шаблоны, референсы']
  ],
  task:{
    intro:'Собери мини-паспорт визуального стиля своего канала. Пропиши палитру, шрифты, тип света, настроение и как ты держишь консистентного персонажа. В поле ниже дай готовый бренд-паспорт в 3-5 строк.',
    chips:['Палитра (2-3 цвета)', 'Шрифты для субтитров', 'Тип света и настроение', 'Референс/двойник персонажа', 'Сид для повторяемости', 'Постоянный джингл'],
    ph:'Палитра: чёрный + лайм + белый. Шрифт субтитров: жирный гротеск, ключевое слово лаймом. Свет: контрастный студийный. Настроение: уверенное, деловое. Персонаж — мой двойник по фиксированному референсу, один джингл на входе.',
    verdict:'Сильный паспорт — есть конкретная палитра, правило акцентного слова и фиксированный персонаж, по такому каналу тебя узнают без логотипа. Усиль повторяемость: закрепи один стартовый промпт-шаблон со всеми этими константами и сидом, чтобы каждый новый ролик собирался в едином ключе не по памяти, а по системе — тогда стиль не поплывёт в усталый день.'
  }
}
  ],
  "a3": [
{
  title:'Что такое вайбкодинг',
  sub:'3:10 · слайды + тест + игра', dur:'3:10',
  c1:'КОД', c2:'СЛОВАМИ',
  slides:[
  {t:'Вайбкодинг простыми словами', pts:['Ты описываешь <b>словами</b>, что хочешь получить, а <b>ИИ</b> пишет код за тебя', 'Правишь не строки, а <b>формулировку задачи</b> — как ставишь ТЗ живому разработчику', 'Синтаксис знать не нужно: нужно уметь <b>чётко объяснять</b> и проверять результат глазами'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="18" y="14" width="84" height="42" rx="8"/><path d="M40 28l-10 7 10 7M80 28l10 7-10 7M66 26l-12 18" stroke-width="2.8"/></svg>'},
  {t:'Почему это выстрелило именно сейчас', pts:['Модели уровня <b>Claude</b> и <b>ChatGPT</b> пишут рабочий код с первого-второго захода', '<b>Lovable</b> и <b>Bolt</b> показывают живое превью прямо во время генерации', 'Порог входа упал с месяцев учёбы до <b>одного вечера</b> за ноутбуком'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Что реально можно собрать', pts:['Лендинг под оффер, приложение с базой и оплатой, <b>телеграм-бот</b> на ИИ', 'Внутренние инструменты: калькуляторы, дашборды, формы заявок', 'Сам сервис <b>OKO</b> собран терминальным агентом <b>Claude Code</b> — это не игрушка'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Где проходит честная граница', pts:['Вайбкод — не «магия без понимания»: за красивым UI всё равно живой код', 'Для сложной логики подключаешь <b>Cursor</b> и учишься читать, что написал ИИ', 'Ты остаёшься <b>архитектором</b> замысла, инструмент — только руки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Мышление вайбкодера', pts:['Думай <b>продуктом</b> и пользователем, а не строчками кода', 'Дроби задачу: короткие итерации, одна конкретная правка за раз', 'После каждого шага <b>проверяй</b> результат — не копи двадцать изменений в слепую'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'В чём суть вайбкодинга?', o:['Ты описываешь задачу словами, а ИИ пишет код', 'Ты вручную пишешь весь код без подсказок', 'Ты нанимаешь фрилансера через биржу', 'Ты копируешь готовые сайты целиком'], a:0},
  {q:'Каким инструментом собран сам сервис OKO?', o:['Wordstat', 'Claude Code', 'Midjourney', 'Suno'], a:1},
  {q:'Что даёт Lovable и Bolt прямо во время работы?', o:['Готовый юридический договор', 'Живое превью результата', 'Автоматическую рекламу', 'Бесплатный трафик'], a:1},
  {q:'Как правильно вести итерации?', o:['Копить 20 правок и проверять всё разом', 'Менять всё сразу большими кусками', 'Короткими шагами, проверяя после каждого', 'Не проверять — ИИ не ошибается'], a:2},
  {q:'Кем остаётся человек в вайбкодинге?', o:['Наблюдателем без влияния', 'Архитектором замысла', 'Тестировщиком чужого кода', 'Только заказчиком дизайна'], a:1}
  ],
  pairs:[
  ['Вайбкодинг', 'Сборка продукта словами, без ручного кода'],
  ['Итерация', 'Один короткий шаг правки с проверкой результата'],
  ['Claude Code', 'Терминальный ИИ-агент, собравший OKO'],
  ['Превью', 'Живой показ результата во время генерации'],
  ['Архитектор', 'Роль человека: держит замысел и проверяет']
  ],
  task:{
    intro:'Сформулируй в одном абзаце идею продукта, который хочешь собрать вайбкодом, как будто ставишь ТЗ разработчику. Что это, для кого, какая единственная главная задача.',
    chips:['Что за продукт (лендинг/бот/апп)', 'Кто пользователь', 'Главное действие на экране', 'Какую боль закрывает', 'Один ключевой экран'],
    ph:'Лендинг для мастера маникюра: одна страница, кнопка записи через WhatsApp, галерея работ и отзывы. Задача — превратить переход из Instagram в запись. Пользователь — девушка 20-35, зашла с телефона.',
    verdict:'Отличная конкретика — есть пользователь, устройство и одно целевое действие, это уже готовое ТЗ для Lovable. Усилю одним советом: добавь в формулировку главный оффер над кнопкой (например «запись на этой неделе — минус 20%»), тогда ИИ соберёт лендинг не просто красивый, а продающий.'
  }
},
{
  title:'Карта инструментов',
  sub:'3:40 · слайды + тест + игра', dur:'3:40',
  c1:'КАРТА', c2:'ИНСТРУМЕНТОВ',
  slides:[
  {t:'Четыре инструмента, которые решают всё', pts:['<b>Lovable</b> ($25) — самые красивые приложения из коробки', '<b>Bolt</b> ($25) — за вечер проверить сырую идею', '<b>Cursor</b> ($20) — глубокое понимание кода для сложного', '<b>Claude Code</b> — терминальный агент, им собран сам <b>OKO</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Lovable — про красоту и скорость', pts:['Даёт готовый <b>дизайн</b> без возни: аккуратные экраны сразу', 'Идеален для лендингов, MVP, презентации идеи инвестору', 'Слабеет там, где нужна тонкая <b>логика</b> и нестандартные связки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l13 27 29 4-21 21 5 29-26-14-26 14 5-29-21-21 29-4z"/></svg>'},
  {t:'Bolt — про проверку гипотезы', pts:['«Собрал за вечер, показал, выкинул если не зашло» — вот его роль', 'Быстрый черновик работающего прототипа, чтобы <b>пощупать</b> идею', 'Не тащи на нём годами живой проект — это стенд для тестов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Cursor и Claude Code — про глубину', pts:['<b>Cursor</b> видит весь проект целиком и лечит сложные баги', '<b>Claude Code</b> работает в терминале как автономный инженер', 'Сюда переходишь, когда из красивого прототипа делаешь <b>серьёзный</b> продукт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="18" y="14" width="84" height="42" rx="8"/><path d="M40 28l-10 7 10 7M80 28l10 7-10 7M66 26l-12 18" stroke-width="2.8"/></svg>'},
  {t:'Деплой — бесплатно', pts:['<b>Vercel</b> и <b>Cloudflare</b> Pages поднимают сайт в интернет за минуты', 'Бесплатного тарифа хватает большинству проектов на старте', 'Домен подключается в пару кликов, HTTPS включается сам'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 52a16 16 0 0 1 2-32 20 20 0 0 1 38 4 14 14 0 0 1-2 28z"/></svg>'},
  {t:'Эталонная связка', pts:['<b>Lovable</b> собирает интерфейс — быстро и красиво', '<b>Cursor</b> или <b>Claude Code</b> дописывают логику и правят сложное', 'Деплой на <b>Vercel</b> — и продукт живёт в проде за один вечер'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'}
  ],
  quiz:[
  {q:'Какой инструмент даёт самые красивые приложения из коробки?', o:['Cursor', 'Lovable', 'Vercel', 'Bolt'], a:1},
  {q:'Куда переходят, когда нужна глубокая работа с кодом?', o:['В Lovable', 'В Cursor', 'В Suno', 'В Wordstat'], a:1},
  {q:'Для чего лучше всего подходит Bolt?', o:['Держать проект годами', 'За вечер проверить идею', 'Рисовать логотипы', 'Монтировать видео'], a:1},
  {q:'Что делает связка-эталон?', o:['Lovable для UI, потом Cursor/Claude Code для логики', 'Только Cursor от начала до конца', 'Только Bolt без деплоя', 'Midjourney рисует, а Vercel кодит'], a:0},
  {q:'Чем поднимают сайт в интернет бесплатно?', o:['ElevenLabs', 'Vercel и Cloudflare', 'Kling', 'n8n'], a:1}
  ],
  pairs:[
  ['Lovable', 'Красивый UI из коробки, $25'],
  ['Bolt', 'Быстрый прототип на вечер, $25'],
  ['Cursor', 'Глубокое понимание кода, $20'],
  ['Claude Code', 'Автономный агент в терминале'],
  ['Vercel', 'Бесплатный деплой в один клик']
  ],
  task:{
    intro:'Разложи свой проект по карте: каким инструментом соберёшь UI, чем допишешь логику, где задеплоишь. Объясни в одном-двух предложениях почему именно так.',
    chips:['Инструмент для интерфейса', 'Инструмент для логики', 'Где деплой', 'Почему эта связка', 'Сложность проекта'],
    ph:'Интерфейс собираю в Lovable — нужен красивый экран быстро. Логику оплаты и личного кабинета дописываю в Cursor, потому что там нестандартная связка с базой. Деплой на Vercel — бесплатно и с доменом за минуту.',
    verdict:'Хорошо разложено — ты честно отделил зону Lovable от зоны Cursor по критерию сложности логики, это и есть правильное мышление. Усилю: начни всё-таки с Bolt на один вечер, чтобы проверить, что идея вообще нужна людям, и только потом вкладывайся в красивую сборку в Lovable.'
  }
},
{
  title:'Лендинг за вечер',
  sub:'3:20 · слайды + тест + игра', dur:'3:20',
  c1:'ЛЕНДИНГ', c2:'ЗА ВЕЧЕР',
  slides:[
  {t:'От идеи до деплоя за один вечер', pts:['Открываешь <b>Lovable</b> или <b>Bolt</b> и описываешь страницу словами', 'Через пару минут — живой лендинг, который можно листать', 'К концу вечера он уже в интернете по своему <b>домену</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Первый промпт решает половину', pts:['Опиши <b>оффер</b>, аудиторию, нужные блоки и настроение дизайна', 'Дай структуру: экран-обложка, выгоды, отзывы, цена, <b>кнопка</b>', 'Плохой промпт «сделай красиво» = серый шаблон; конкретика = точное попадание'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Обязательные блоки продающего лендинга', pts:['Первый экран: заголовок с выгодой и одна <b>кнопка действия</b>', 'Блок доверия: отзывы, кейсы, цифры, лица', 'Оффер и цена без воды, повтор кнопки внизу'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'},
  {t:'Правки — точечно', pts:['Не «переделай всё», а «сделай заголовок крупнее и добавь блок FAQ»', 'Одна правка — один запрос, так ИИ не ломает соседние блоки', 'Проверяй на <b>телефоне</b>: большинство зайдёт с мобильного'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M30 54h60" opacity=".6"/><path d="M40 46l30-30 8 8-30 30-12 4z"/><path d="M64 22l8 8"/></svg>'},
  {t:'Публикация и домен', pts:['Встроенный деплой отдаёт ссылку сразу, либо тянешь на <b>Vercel</b>', 'Подключаешь свой домен — доверие выше, чем к длинной ссылке', 'Ставишь <b>аналитику</b> и пиксель, чтобы видеть трафик и заявки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M48 42 72 18a13 13 0 0 1 18 18L74 52M72 28 48 52a13 13 0 0 1-18-18l16-16" opacity=".9"/></svg>'}
  ],
  quiz:[
  {q:'С чего начинается лендинг за вечер?', o:['С покупки дорогого хостинга', 'С подробного промпта про оффер и блоки', 'С изучения JavaScript месяц', 'С найма дизайнера'], a:1},
  {q:'Что должно быть на первом экране?', o:['Заголовок с выгодой и кнопка действия', 'Длинная история компании', 'Только логотип', 'Прайс из 30 позиций'], a:0},
  {q:'Как правильно вносить правки?', o:['Одним запросом «переделай всё»', 'Точечно, одна правка — один запрос', 'Вручную в коде наугад', 'Никак, оставить как есть'], a:1},
  {q:'Почему проверять надо на телефоне?', o:['Так требует Vercel', 'Большинство заходит с мобильного', 'На компьютере лендинг не грузится', 'Телефон быстрее интернета'], a:1},
  {q:'Зачем подключать свой домен?', o:['Это обязательно по закону', 'Повышает доверие к странице', 'Ускоряет генерацию кода', 'Отключает рекламу конкурентов'], a:1}
  ],
  pairs:[
  ['Первый экран', 'Выгода в заголовке плюс одна кнопка'],
  ['Блок доверия', 'Отзывы, кейсы, цифры и лица'],
  ['Точечная правка', 'Один запрос меняет один элемент'],
  ['Домен', 'Своё имя вместо длинной ссылки'],
  ['Мобильная проверка', 'Тест на телефоне до публикации']
  ],
  task:{
    intro:'Напиши стартовый промпт для лендинга своего оффера в Lovable. Укажи оффер, аудиторию, список блоков сверху вниз и настроение дизайна.',
    chips:['Оффер одной фразой', 'Кто аудитория', 'Список блоков по порядку', 'Настроение и цвета', 'Текст кнопки', 'Что на первом экране'],
    ph:'Собери лендинг для онлайн-курса по фотографии на телефон. Аудитория — новички 25-40. Блоки: обложка с заголовком «Снимай как профи за 2 недели» и кнопкой «Записаться», выгоды в 3 карточках, отзывы учеников, программа, цена, повтор кнопки. Стиль минималистичный, тёмный фон, акцент жёлтый.',
    verdict:'Сильный промпт — задан порядок блоков и конкретный текст кнопки, ИИ соберёт готовую структуру, а не абстракцию. Усилю: добавь во фразу над кнопкой ограничение по времени или количеству мест («старт группы в понедельник»), это поднимет конверсию первого экрана без переделки дизайна.'
  }
},
{
  title:'Приложение + БД + оплата',
  sub:'4:05 · слайды + тест + игра', dur:'4:05',
  c1:'АПП', c2:'И ОПЛАТА',
  slides:[
  {t:'Из лендинга — в настоящее приложение', pts:['Приложение отличается тремя вещами: <b>вход</b>, <b>база данных</b>, оплата', 'Собираешь каркас в <b>Lovable</b>, а сложное дотачиваешь в <b>Cursor</b>', 'Цель — чтобы человек регистрировался, что-то сохранял и платил'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'База данных без страха', pts:['БД — это просто таблицы: пользователи, заказы, подписки', '<b>Lovable</b> сам поднимает базу, ты описываешь словами какие поля нужны', 'Правило: одна сущность — одна таблица, не сваливай всё в кучу'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="12" width="60" height="14" rx="4"/><rect x="30" y="30" width="60" height="14" rx="4" opacity=".85"/><rect x="30" y="48" width="60" height="14" rx="4" opacity=".7"/></svg>'},
  {t:'Вход и аккаунты', pts:['<b>Авторизация</b> по email или через Google — просишь ИИ включить готовую', 'У каждого пользователя свои данные, чужое он не видит', 'Не изобретай пароли сам — используй <b>встроенную</b> систему входа'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="35" r="14"/><path d="M48 35h44M78 35v12M92 35v10"/></svg>'},
  {t:'Оплата — сердце монетизации', pts:['Подключаешь платёжку: <b>Lava.top</b>, ЮKassa или Stripe под задачу', 'Логика проста: оплатил — открыли доступ, не оплатил — закрыли', 'Это та часть, где лучше перейти в <b>Cursor</b> и проверить каждый шаг'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="18" width="76" height="38" rx="8"/><path d="M22 30h76"/><circle cx="82" cy="42" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Порядок сборки', pts:['Сначала экраны и вход, потом база, в конце — <b>оплата</b>', 'Проверяй по одному куску: зарегистрировался → сохранил → заплатил', 'Не подключай платёж, пока не работает <b>вход</b> и данные'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Частые грабли', pts:['Открытые данные: чужой пользователь видит чужие заказы — проверь права', 'Оплата «прошла», а доступ не открылся — тестируй на реальном платеже', 'Не храни <b>ключи</b> платёжки в открытом коде — только в защищённых переменных'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Чем приложение отличается от лендинга?', o:['Вход, база данных и оплата', 'Только другим цветом кнопок', 'Наличием видео на фоне', 'Ничем, это одно и то же'], a:0},
  {q:'Как устроена база данных на старте?', o:['Одна таблица на все данные сразу', 'Одна сущность — одна таблица', 'Без таблиц, всё в тексте', 'Только в голове разработчика'], a:1},
  {q:'Какая логика у оплаты?', o:['Оплатил — открыли доступ, не оплатил — закрыли', 'Доступ открыт всем всегда', 'Оплата отключает приложение', 'Платит разработчик за пользователя'], a:0},
  {q:'В каком порядке собирать приложение?', o:['Сначала оплата, потом вход', 'Экраны и вход, потом база, в конце оплата', 'Всё одновременно одним промптом', 'Сначала реклама, потом код'], a:1},
  {q:'Где безопасно хранить ключи платёжки?', o:['Прямо в коде страницы', 'В защищённых переменных окружения', 'В отзывах на лендинге', 'В названии домена'], a:1}
  ],
  pairs:[
  ['База данных', 'Таблицы: пользователи, заказы, подписки'],
  ['Авторизация', 'Вход по email или Google'],
  ['Оплата', 'Оплатил — доступ открыт, нет — закрыт'],
  ['Права доступа', 'Каждый видит только свои данные'],
  ['Ключи платёжки', 'Хранятся в переменных, не в коде']
  ],
  task:{
    intro:'Опиши архитектуру своего приложения словами: какие таблицы в базе, как входит пользователь, за что и как он платит. Как будто ставишь задачу Lovable и Cursor.',
    chips:['Название приложения', 'Таблицы в базе', 'Способ входа', 'Что платно', 'Платёжка', 'Что открывается после оплаты'],
    ph:'Приложение «Дневник тренировок». Таблицы: пользователи, тренировки, планы. Вход через Google. Платно — раздел с готовыми программами. Платёжка Lava.top: оплатил подписку 490 руб/мес — открылись программы и аналитика прогресса.',
    verdict:'Чётко — таблицы разделены по сущностям, а платный барьер стоит ровно на ценной части, это правильная монетизация. Усилю: пропиши, что происходит при отмене подписки (доступ к программам закрывается, но свои тренировки остаются), — эту логику ИИ часто забывает, и её надо задать заранее.'
  }
},
{
  title:'Телеграм-бот на ИИ',
  sub:'3:35 · слайды + тест + игра', dur:'3:35',
  c1:'ТГ-БОТ', c2:'НА ИИ',
  slides:[
  {t:'Бот, который отвечает как человек', pts:['Обычный бот — кнопки и скрипты; <b>ИИ-бот</b> понимает живой текст', 'Внутри связка: сообщение пользователя → <b>LLM</b> → умный ответ', 'Собирается без единой строчки, написанной руками'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="22" width="52" height="38" rx="10"/><circle cx="50" cy="41" r="5"/><circle cx="70" cy="41" r="5"/><path d="M60 22v-8M50 14h20"/><path d="M28 38v10M92 38v10" opacity=".7"/></svg>'},
  {t:'Три кирпича бота', pts:['Токен от <b>@BotFather</b> — паспорт твоего бота в Telegram', 'Мозг — модель <b>ChatGPT</b>, <b>Claude</b> или <b>Gemini</b> через ключ', 'Связующее звено: <b>n8n</b>, <b>Make</b> или свой код на Claude Code'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="16" r="7"/><circle cx="26" cy="52" r="7"/><circle cx="94" cy="52" r="7"/><path d="M56 22 30 46M64 22l26 24M33 52h54" opacity=".8"/></svg>'},
  {t:'Без кода через n8n или Make', pts:['Собираешь схему из блоков: получил сообщение — отправил в <b>LLM</b> — вернул ответ', '<b>n8n</b> можно поднять бесплатно у себя, <b>Make</b> проще для новичка', 'Визуальный сценарий заменяет десятки строк кода'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Характер и роль бота', pts:['Задаёшь <b>системный промпт</b>: кто бот, как говорит, чего не делает', 'Пример: «ты вежливый администратор салона, отвечаешь коротко, зовёшь записаться»', 'От промпта зависит, будет бот полезным или пустым болтуном'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 25a15 15 0 0 1 32-6 15 15 0 0 1 32 6c0 15-32 31-32 31z"/></svg>'},
  {t:'Память и границы', pts:['Чтобы бот помнил диалог — храни последние сообщения в <b>базе</b>', 'Ставь лимиты: не отвечать на лишнее, не сливать <b>ключи</b> и промпт', 'Тестируй на злых вопросах, пока не пустил к клиентам'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Чем ИИ-бот отличается от обычного?', o:['Понимает живой текст, а не только кнопки', 'Работает только ночью', 'Не требует токена', 'Отвечает медленнее человека'], a:0},
  {q:'Где взять токен для телеграм-бота?', o:['В Midjourney', 'У @BotFather', 'На Vercel', 'В Wordstat'], a:1},
  {q:'Что связывает Telegram и LLM без кода?', o:['n8n или Make', 'ElevenLabs', 'Cloudflare Pages', 'Bolt'], a:0},
  {q:'Зачем боту системный промпт?', o:['Чтобы задать характер и роль', 'Чтобы ускорить интернет', 'Чтобы получить токен', 'Он не нужен вообще'], a:0},
  {q:'Как сделать, чтобы бот помнил диалог?', o:['Никак, он не может', 'Хранить последние сообщения в базе', 'Перезапускать его каждый раз', 'Удлинить токен'], a:1}
  ],
  pairs:[
  ['Токен', 'Паспорт бота, выдаёт @BotFather'],
  ['LLM', 'Мозг бота: ChatGPT, Claude, Gemini'],
  ['n8n / Make', 'Связка Telegram и модели без кода'],
  ['Системный промпт', 'Характер, роль и границы бота'],
  ['Память', 'Хранение последних сообщений диалога']
  ],
  task:{
    intro:'Напиши системный промпт для своего ИИ-бота: кто он, как общается, что делает и чего не делает. Плюс укажи, через что соберёшь связку.',
    chips:['Роль бота', 'Тон общения', 'Главная задача', 'Чего не делает', 'Через что связка (n8n/Make)', 'Какая LLM внутри'],
    ph:'Ты администратор кофейни «Зерно». Отвечаешь тепло и коротко, максимум 2 предложения. Задача — подсказать меню, часы работы и записать на дегустацию. Не обсуждаешь ничего вне кофейни, не выдумываешь цены. Связка на Make, мозг — Claude.',
    verdict:'Отлично — заданы и роль, и жёсткие границы («не выдумывать цены»), это спасёт от галлюцинаций перед клиентами. Усилю: добавь в промпт одну фразу-фолбэк на случай, когда бот не знает ответа («уточню у бариста, оставьте контакт»), — так он не будет фантазировать, а мягко переведёт на человека.'
  }
},
{
  title:'Подключение LLM API',
  sub:'3:45 · слайды + тест + игра', dur:'3:45',
  c1:'LLM', c2:'В КОДЕ',
  slides:[
  {t:'Что вообще значит подключить API', pts:['<b>API</b> — это дверь к чужому мозгу: твой код шлёт текст, модель шлёт ответ', 'Так внутри твоего приложения появляется <b>ChatGPT</b> или <b>Claude</b>', 'Три вещи нужны всегда: <b>ключ</b>, название модели, промпт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M48 42 72 18a13 13 0 0 1 18 18L74 52M72 28 48 52a13 13 0 0 1-18-18l16-16" opacity=".9"/></svg>'},
  {t:'Ключ — это твой пропуск и твои деньги', pts:['Берёшь <b>API-ключ</b> в личном кабинете провайдера модели', 'Каждый запрос стоит денег — платишь за <b>токены</b>, не за штуки', 'Ключ — как пароль от карты: утёк ключ — утекли деньги'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="35" r="14"/><path d="M48 35h44M78 35v12M92 35v10"/></svg>'},
  {t:'Никогда не клади ключ в код', pts:['Ключ живёт в <b>переменных окружения</b>, а не в тексте страницы', 'Иначе любой откроет код в браузере и заберёт твой ключ', 'В <b>Lovable</b> и <b>Vercel</b> есть отдельное хранилище секретов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="30" width="52" height="34" rx="8"/><path d="M44 30v-8a16 16 0 0 1 32 0v8"/><circle cx="60" cy="46" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Выбор модели под задачу', pts:['Дорогая умная модель — для сложных рассуждений и кода', 'Дешёвая быстрая — для простых ответов и большого потока', '<b>Gemini</b>, <b>Claude</b>, <b>ChatGPT</b> — у каждой свои сильные стороны'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'},
  {t:'Промпт в коде решает качество', pts:['Системный <b>промпт</b> задаёт поведение модели на каждый запрос', 'Передавай контекст: кто пользователь, что он уже делал', 'Плохой промпт — и самая умная модель отвечает мимо'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Считай деньги с первого дня', pts:['Ставь <b>лимит</b> расходов в кабинете, чтобы не улететь в минус', 'Не гоняй длинные диалоги без нужды — режь <b>контекст</b>', 'Логируй запросы: увидишь, где сжигается бюджет'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'}
  ],
  quiz:[
  {q:'Что нужно, чтобы подключить LLM в приложение?', o:['Ключ, название модели и промпт', 'Только красивый дизайн', 'Домен и логотип', 'Ничего, всё работает само'], a:0},
  {q:'За что платишь при работе с API?', o:['За токены в запросах и ответах', 'За количество кнопок', 'За домен раз в год', 'Ни за что, это бесплатно всегда'], a:0},
  {q:'Где хранить API-ключ?', o:['Прямо в коде страницы', 'В переменных окружения', 'В названии бота', 'В отзывах клиентов'], a:1},
  {q:'Какую модель брать для большого потока простых ответов?', o:['Самую дорогую всегда', 'Дешёвую и быструю', 'Любую, разницы нет', 'Вообще без модели'], a:1},
  {q:'Как не улететь в минус по деньгам?', o:['Не считать расходы вовсе', 'Поставить лимит и резать контекст', 'Гонять максимально длинные диалоги', 'Отключить логи'], a:1}
  ],
  pairs:[
  ['API', 'Дверь к модели: шлёшь текст, получаешь ответ'],
  ['Токены', 'Единица оплаты за запрос и ответ'],
  ['Переменные окружения', 'Безопасное место для ключа'],
  ['Системный промпт', 'Задаёт поведение модели'],
  ['Лимит расходов', 'Страховка бюджета в кабинете']
  ],
  task:{
    intro:'Опиши, как подключишь LLM к своему продукту: какую модель выберешь и почему, какой системный промпт зашьёшь, как защитишь ключ и удержишь бюджет.',
    chips:['Какая модель', 'Почему именно она', 'Системный промпт коротко', 'Где лежит ключ', 'Лимит расходов', 'Как режешь контекст'],
    ph:'Беру Gemini Flash — дешёвая и быстрая, у меня поток коротких вопросов по товарам. Системный промпт: «ты консультант магазина, отвечаешь по каталогу, коротко». Ключ в переменных окружения Vercel, не в коде. Лимит 20$ в месяц, передаю в контекст только последние 3 сообщения.',
    verdict:'Грамотно — модель выбрана под характер нагрузки, а не по принципу «самая мощная», и ключ спрятан правильно. Усилю: добавь в системный промпт запрет отвечать по товарам, которых нет в каталоге, иначе на дешёвой модели легко получить выдуманные позиции и цены.'
  }
},
{
  title:'Дизайн-система',
  sub:'3:30 · слайды + тест + игра', dur:'3:30',
  c1:'ДИЗАЙН', c2:'СИСТЕМА',
  slides:[
  {t:'Почему без системы всё разваливается', pts:['Без правил каждый экран получается своим — продукт выглядит кустарно', '<b>Дизайн-система</b> — это единые цвета, шрифты и <b>компоненты</b>', 'Задаёшь её один раз, и весь продукт держится в одном стиле'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Три кита: цвет, шрифт, отступы', pts:['Палитра из 3-4 цветов: основной, акцент, фон, текст — не больше', 'Одна-две <b>шрифтовые</b> пары, а не десять разных начертаний', 'Единый шаг отступов, чтобы всё дышало ровно'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M74 12 92 30 56 66l-18 4 4-18z"/><path d="M62 24 80 42" opacity=".7"/></svg>'},
  {t:'Компоненты — кирпичи интерфейса', pts:['Кнопка, карточка, поле ввода — рисуешь один раз, используешь везде', 'Меняешь <b>компонент</b> в одном месте — меняется во всём приложении', '<b>Lovable</b> и <b>Cursor</b> отлично держат такие переиспользуемые блоки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="12" width="60" height="14" rx="4"/><rect x="30" y="30" width="60" height="14" rx="4" opacity=".85"/><rect x="30" y="48" width="60" height="14" rx="4" opacity=".7"/></svg>'},
  {t:'Как задать систему словами', pts:['В промпте укажи бренд-цвета в <b>HEX</b> и названия шрифтов', 'Опиши тон: «минимализм, много воздуха, скругления, тёмная тема»', 'Проси ИИ вынести стили в единые <b>токены</b>, а не хардкодить в каждом экране'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Тёмная и светлая темы', pts:['Заложи обе темы сразу — переделывать потом дороже', 'Цвета описывай ролями («фон», «акцент»), а не намертво', 'Проверь <b>контраст</b>: текст должен читаться в обеих темах'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'}
  ],
  quiz:[
  {q:'Что такое дизайн-система?', o:['Единые цвета, шрифты и компоненты', 'Один красивый экран', 'Папка со скриншотами', 'Название приложения'], a:0},
  {q:'Сколько цветов держать в палитре на старте?', o:['Десять и больше', '3-4 основных', 'Ровно один', 'Сколько выйдет'], a:1},
  {q:'В чём сила компонентов?', o:['Меняешь в одном месте — меняется везде', 'Каждый экран рисуешь заново', 'Они замедляют приложение', 'Они нужны только для игр'], a:0},
  {q:'Как правильно задать бренд-цвета ИИ?', o:['Словами «сделай красиво»', 'Точными кодами HEX и ролями', 'Не задавать вовсе', 'Прислать фото заката'], a:1},
  {q:'Когда закладывать тёмную и светлую темы?', o:['Сразу, обе вместе', 'Никогда не нужно', 'Только после релиза', 'Когда пожалуются пользователи'], a:0}
  ],
  pairs:[
  ['Палитра', '3-4 цвета: основной, акцент, фон, текст'],
  ['Шрифтовая пара', 'Одно-два начертания на весь продукт'],
  ['Компонент', 'Кирпич UI, переиспользуемый везде'],
  ['Токены', 'Единые стили вместо хардкода в экранах'],
  ['Контраст', 'Читаемость текста в обеих темах']
  ],
  task:{
    intro:'Опиши дизайн-систему своего продукта: палитра в HEX, шрифтовая пара, скругления и настроение. Сформулируй так, чтобы вставить в промпт Lovable.',
    chips:['Основной цвет (HEX)', 'Акцентный цвет (HEX)', 'Шрифт заголовков', 'Шрифт текста', 'Скругления и тени', 'Тёмная или светлая тема'],
    ph:'Основной чёрный #0A0A0A, акцент лаймовый #9AFF00, фон тёмный. Заголовки Bebas Neue, текст Montserrat. Скругления средние, тени мягкие, много воздуха. Тёмная тема по умолчанию, светлая как опция. Стиль — минимализм, дерзко, без лишнего декора.',
    verdict:'Сильно — цвета заданы кодами и ролями, а не «на глаз», ИИ соберёт единый стиль без разнобоя. Усилю: пропиши ещё состояние hover и disabled для кнопки (например акцент темнеет при наведении), иначе Lovable сделает их случайными и продукт потеряет цельность в мелочах.'
  }
},
{
  title:'Бесплатный деплой (Vercel/Cloudflare)',
  sub:'3:15 · слайды + тест + игра', dur:'3:15',
  c1:'ДЕПЛОЙ', c2:'БЕСПЛАТНО',
  slides:[
  {t:'Деплой простыми словами', pts:['<b>Деплой</b> — это когда твой проект переезжает из ноутбука в интернет', 'До деплоя его видишь только ты, после — весь мир по ссылке', '<b>Vercel</b> и <b>Cloudflare</b> Pages делают это бесплатно и за минуты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 52a16 16 0 0 1 2-32 20 20 0 0 1 38 4 14 14 0 0 1-2 28z"/></svg>'},
  {t:'Vercel — путь по умолчанию', pts:['Связываешь проект с <b>GitHub</b> — каждый пуш сам уезжает в прод', 'Бесплатного тарифа хватает лендингам и небольшим приложениям', '<b>HTTPS</b> и быстрая отдача включаются автоматически'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'},
  {t:'Cloudflare — когда нужна мощь', pts:['<b>Cloudflare</b> Pages силён на большом трафике и по всему миру', 'Щедрый бесплатный лимит, встроенная защита от атак', 'Хорош, если аудитория глобальная, а не только твой город'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Домен и HTTPS', pts:['Привязываешь свой <b>домен</b> в настройках проекта за пару кликов', 'Замочек <b>HTTPS</b> ставится сам — сайту доверяют и браузеры, и люди', 'Пока домена нет — работает бесплатный адрес платформы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M48 42 72 18a13 13 0 0 1 18 18L74 52M72 28 48 52a13 13 0 0 1-18-18l16-16" opacity=".9"/></svg>'},
  {t:'Авто-деплой из репозитория', pts:['Правило: код в <b>GitHub</b> — прод обновляется без ручных действий', 'Так собран деплой самого <b>OKO</b>: пуш в ветку и через минуты всё в проде', 'Откат тоже в один клик — вернулся на прошлую рабочую версию'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'}
  ],
  quiz:[
  {q:'Что такое деплой?', o:['Переезд проекта в интернет, доступный всем', 'Удаление проекта', 'Покупка компьютера', 'Написание промпта'], a:0},
  {q:'Как настроен авто-деплой на Vercel?', o:['Каждый пуш в GitHub сам уезжает в прод', 'Вручную копируешь файлы по FTP', 'Раз в месяц по расписанию', 'Только через платную поддержку'], a:0},
  {q:'Когда выбирают Cloudflare Pages?', o:['Для большого мирового трафика', 'Только для одного пользователя', 'Когда нет интернета', 'Для печати визиток'], a:0},
  {q:'Что включается автоматически при деплое?', o:['HTTPS и быстрая отдача', 'Реклама конкурентов', 'Платная подписка', 'Ничего'], a:0},
  {q:'Что делать, если новая версия сломалась?', o:['Откатиться на прошлую в один клик', 'Удалить весь проект', 'Ждать неделю', 'Начать всё с нуля'], a:0}
  ],
  pairs:[
  ['Деплой', 'Переезд проекта из ноутбука в интернет'],
  ['Vercel', 'Быстрый бесплатный деплой из GitHub'],
  ['Cloudflare Pages', 'Мощь на большом мировом трафике'],
  ['HTTPS', 'Автоматический замочек доверия'],
  ['Откат', 'Возврат на прошлую рабочую версию']
  ],
  task:{
    intro:'Опиши план деплоя своего проекта: где разместишь, как настроишь авто-обновление, какой домен подключишь и почему выбрал именно эту платформу.',
    chips:['Платформа (Vercel/Cloudflare)', 'Почему она', 'Связка с GitHub', 'Свой домен', 'Ожидаемый трафик', 'Как откатываешь'],
    ph:'Лендинг курса заливаю на Vercel — трафик небольшой, из России и СНГ, бесплатного тарифа хватит. Свяжу с репозиторием на GitHub, чтобы каждый пуш сам обновлял прод. Домен kurs-foto.ru, HTTPS автоматом. Если что сломается — откат на прошлую сборку в один клик.',
    verdict:'Хорошо — платформа выбрана под реальный масштаб трафика, а авто-деплой из GitHub снимет ручную рутину. Усилю: заведи отдельную ветку под черновики и деплой прод только из главной, тогда сырые правки не будут случайно улетать на живой домен к клиентам.'
  }
},
{
  title:'Лазейки вайбкодинга',
  sub:'3:50 · слайды + тест + игра', dur:'3:50',
  c1:'ЛАЗЕЙКИ', c2:'ВАЙБКОДА',
  slides:[
  {t:'Как собирать много и почти бесплатно', pts:['У инструментов есть <b>бесплатные кредиты</b> и щедрые триалы на старте', 'Готовые <b>шаблоны</b> экономят половину работы и токенов', 'Главный навык — не жечь <b>токены</b> впустую на пустых итерациях'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="35" r="14"/><path d="M48 35h44M78 35v12M92 35v10"/></svg>'},
  {t:'Бесплатные кредиты и триалы', pts:['<b>Lovable</b>, <b>Bolt</b>, <b>Cursor</b> дают лимит бесплатных генераций', 'Хватает, чтобы собрать первый прототип и проверить идею', 'Деплой на <b>Vercel</b> и <b>Cloudflare</b> вовсе бесплатный на старте'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="28" width="68" height="34" rx="6"/><path d="M26 38h68M60 28v34"/><path d="M60 28c-6-14-22-10-16-2 3 4 16 2 16 2zM60 28c6-14 22-10 16-2-3 4-16 2-16 2z"/></svg>'},
  {t:'Шаблоны вместо чистого листа', pts:['Бери готовый <b>шаблон</b> дашборда, магазина, лендинга и правь под себя', 'Так экономишь десятки запросов к модели', 'Изучай чужие сборки — половина решений уже кем-то придумана'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Как не сжечь токены', pts:['<b>Короткие итерации</b>: одна конкретная задача — один запрос', 'Не проси «переделай всё приложение» — это дорого и ломает рабочее', 'Формулируй точно: чем яснее задача, тем меньше <b>переделок</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Дели работу между инструментами', pts:['Дорогую умную модель включай только на сложное', 'Простое собирай в дешёвом или бесплатном режиме', '<b>Lovable</b> — на UI, <b>Cursor</b> — точечно на трудную логику'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Правила экономного вайбкодера', pts:['Планируй до генерации: набросай экраны и логику словами', 'Проверяй каждый шаг, не копи слепые правки', 'Сохраняй рабочие версии — <b>откат</b> дешевле новой сборки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Главный навык экономного вайбкодера?', o:['Не жечь токены на пустых итерациях', 'Генерировать всё максимально длинно', 'Никогда не пользоваться шаблонами', 'Платить за самый дорогой тариф'], a:0},
  {q:'Зачем брать готовые шаблоны?', o:['Экономят запросы и половину работы', 'Они всегда хуже своего', 'Ради лишних расходов', 'Чтобы усложнить проект'], a:0},
  {q:'Как правильно вести итерации ради экономии?', o:['Одна конкретная задача — один запрос', '«Переделай всё приложение» одним промптом', 'Копить 30 правок и слать разом', 'Не проверять результат'], a:0},
  {q:'Когда включать дорогую умную модель?', o:['Только на сложные задачи', 'Всегда и на всё', 'Никогда', 'Только для деплоя'], a:0},
  {q:'Почему стоит сохранять рабочие версии?', o:['Откат дешевле новой сборки', 'Так требует Vercel', 'Это замедляет проект', 'Незачем, версии не нужны'], a:0}
  ],
  pairs:[
  ['Бесплатные кредиты', 'Стартовый лимит генераций у инструментов'],
  ['Шаблон', 'Готовая основа вместо чистого листа'],
  ['Короткая итерация', 'Одна задача — один запрос'],
  ['Разделение моделей', 'Дорогая на сложное, дешёвая на простое'],
  ['Откат', 'Возврат к версии дешевле пересборки']
  ],
  task:{
    intro:'Составь свой план экономной сборки: где возьмёшь бесплатные кредиты, какой шаблон используешь, как будешь дробить задачи и на чём сэкономишь токены.',
    chips:['Где бесплатные кредиты', 'Какой шаблон берёшь', 'Как дробишь задачи', 'Дорогая модель — на что', 'Что деплоишь бесплатно', 'Как сохраняешь версии'],
    ph:'Первый прототип соберу на бесплатных кредитах Bolt. Возьму готовый шаблон лендинга и переделаю тексты и цвета. Задачи дроблю по одной: сначала первый экран, потом форма, потом отзывы. Cursor включу только на связку с платёжкой. Деплой бесплатно на Vercel, рабочие версии храню в GitHub для отката.',
    verdict:'Отличный план — ты используешь и бесплатный лимит, и шаблон, и точечно дорогой инструмент только на сложное, это грамотная экономия. Усилю: перед первой генерацией набросай список экранов в блокноте, тогда каждый запрос будет попадать в цель с первого раза и переделок станет вдвое меньше.'
  }
}
  ],
  "a4": [
{
  title:'Что автоматизировать (с деньгами)',
  sub:'3:05 · слайды + тест + игра', dur:'3:05',
  c1:'АВТО', c2:'С ДЕНЬГАМИ',
  slides:[
  {t:'Правило первых денег', pts:['Автоматизируй то, что уже приносит выручку или напрямую её теряет', 'Кандидаты: <b>поддержка</b>, <b>квалификация лидов</b>, счета, автопостинг, мониторинг цен', 'Сначала процесс работает руками — только потом его автоматизируют'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Формула приоритета', pts:['Считай: <b>время</b> на задачу × частота в месяц × ставка часа', '30 мин × 60 раз = 30 часов/мес — вот кандидат номер один', 'Ручная квалификация лида в директе съедает до 40% рабочего дня'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Пять процессов, что окупаются первыми', pts:['<b>Поддержка</b>: типовые вопросы клиентов 24/7', '<b>Квалификация лидов</b>: отсеять холодных до звонка', 'Счета, <b>автопостинг</b> и <b>мониторинг цен конкурентов</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'},
  {t:'Что автоматизировать нельзя', pts:['Процесс, который сам не понимаешь руками — сначала опиши шаги', 'Редкие задачи: раз в квартал дешевле сделать вручную', 'Решения, где цена ошибки — потеря клиента без контроля'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Первый запуск за вечер', pts:['Выпиши 10 задач, которые повторяешь каждую неделю', 'Отметь три самых частых и денежных — это дорожная карта', 'Автоматизируй одну, замерь освобождённые часы, посчитай в деньгах'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'}
  ],
  quiz:[
  {q:'С чего правильно начинать автоматизацию?', o:['С процесса, который уже приносит деньги или их теряет', 'С самой сложной технической задачи', 'С того, что делают конкуренты', 'С покупки самого дорогого сервиса'], a:0},
  {q:'Как оценить, стоит ли автоматизировать задачу?', o:['Время на задачу × частоту × ставку часа', 'По количеству кнопок в интерфейсе', 'По цене подписки сервиса', 'По числу подписчиков'], a:0},
  {q:'Какую задачу автоматизировать НЕ стоит?', o:['Редкую задачу раз в квартал', 'Ответы на типовые вопросы клиентов', 'Выставление счетов', 'Автопостинг в соцсети'], a:0},
  {q:'Что нужно сделать до автоматизации процесса?', o:['Отладить его вручную и описать шаги', 'Сразу купить облачный тариф', 'Нанять программиста', 'Запустить рекламу'], a:0},
  {q:'Почему поддержка — хороший первый кандидат?', o:['Типовые вопросы повторяются часто и съедают время', 'Клиенты не любят живых людей', 'Это самый дешёвый процесс', 'Так делают все'], a:0}
  ],
  pairs:[
  ['Поддержка', 'Ответы на повторяющиеся вопросы 24/7'],
  ['Квалификация', 'Отсев холодных лидов до звонка'],
  ['Формула приоритета', 'Время × частота × ставка часа'],
  ['Стоп-лист', 'Редкие задачи и решения без контроля'],
  ['Первый шаг', 'Отладить процесс руками, потом автоматизировать']
  ],
  task:{
    intro:'Выпиши свой процесс-кандидат №1 на автоматизацию и посчитай, сколько времени и денег он съедает в месяц.',
    chips:['Процесс: ...', 'Частота в месяц: ...', 'Время на один раз: ...', 'Ставка часа: ...', 'Итого часов/мес: ...', 'Что теряю без автоматизации: ...'],
    ph:'Процесс: ручные ответы на \'сколько стоит\' в директе. Частота: 80 раз/мес. Время: 4 мин. Ставка: 700 р/час. Итого: 5,3 часа и ~3700 р/мес плюс теряю 3-4 лида ночью, когда не отвечаю.',
    verdict:'Отлично — ты посчитал в деньгах и часах, а не \'на глаз\', и увидел ночные потери лидов. Усилю: добавь к расчёту скорость ответа — если бот отвечает за 5 сек вместо 3 часов, конверсия в диалог обычно растёт на 15-30%, вот твой второй аргумент за запуск.'
  }
},
{
  title:'n8n / Make / Zapier — карта',
  sub:'3:30 · слайды + тест + игра', dur:'3:30',
  c1:'КАРТА', c2:'ИНСТРУМЕНТОВ',
  slides:[
  {t:'Три инструмента — три философии', pts:['<b>n8n</b>: гибкий, self-hosted бесплатно, оплата за исполнения в облаке', '<b>Make</b>: наглядные сценарии, дешевле на среднем объёме', '<b>Zapier</b>: проще всех, 9000+ сервисов, дорожает на объёме'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="16" r="7"/><circle cx="26" cy="52" r="7"/><circle cx="94" cy="52" r="7"/><path d="M56 22 30 46M64 22l26 24M33 52h54" opacity=".8"/></svg>'},
  {t:'n8n — когда нужен контроль', pts:['Self-hosted на своём VPS — <b>бесплатно</b> и без лимитов на воркфлоу', 'Облако от $24/мес, платишь за исполнения, а не за каждый шаг', 'Код-ноды на JavaScript: где не хватает блоков — пишешь сам'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'},
  {t:'Make — золотая середина', pts:['Визуальный конструктор: видно, как данные текут по сценарию', 'Оплата за операции — выгоднее <b>Zapier</b> на тысячах задач', 'Роутеры, фильтры, агрегаторы из коробки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Zapier — скорость и охват', pts:['<b>9000+ интеграций</b> — почти любой сервис уже подключён', 'Собрал связку за 10 минут без чтения документации', 'На больших объёмах ценник кусается — считай заранее'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M48 42 72 18a13 13 0 0 1 18 18L74 52M72 28 48 52a13 13 0 0 1-18-18l16-16" opacity=".9"/></svg>'},
  {t:'Как выбрать под задачу', pts:['Много исполнений и свой сервер → <b>n8n</b>', 'Средний объём, любишь наглядность → <b>Make</b>', 'Нужен редкий сервис прямо сейчас → <b>Zapier</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Считаем стоимость на объёме', pts:['10 000 задач/мес: на Zapier дорого, на n8n self-hosted — цена сервера', 'Прикинь рост × 12 месяцев, а не только сегодняшний тариф', 'Миграция между платформами болезненна — выбирай на вырост'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'}
  ],
  quiz:[
  {q:'Какой инструмент бесплатен при self-hosted?', o:['n8n', 'Zapier', 'Make', 'Ни один'], a:0},
  {q:'У какого сервиса больше всего готовых интеграций?', o:['Zapier — 9000+', 'n8n', 'Make', 'У всех одинаково'], a:0},
  {q:'За что платишь в облачном n8n?', o:['За исполнения', 'За каждый воркфлоу', 'За пользователя', 'За гигабайты'], a:0},
  {q:'Когда выбрать Make?', o:['Средний объём и нужна наглядность сценария', 'Когда нужен один редкий сервис', 'Когда всё на своём сервере бесплатно', 'Когда нет интернета'], a:0},
  {q:'Что важно учесть при выборе на будущее?', o:['Рост числа задач × 12 месяцев', 'Цвет интерфейса', 'Наличие мобильного приложения', 'Число сотрудников'], a:0}
  ],
  pairs:[
  ['n8n', 'Self-hosted бесплатно, код-ноды, контроль'],
  ['Make', 'Визуальные сценарии, выгоден на среднем объёме'],
  ['Zapier', '9000+ сервисов, проще всех, дорог на объёме'],
  ['Исполнение', 'Единица оплаты в n8n и Make'],
  ['Self-hosted', 'Запуск на своём сервере без лимитов'],
  ['Миграция', 'Болезненный переезд — выбирай на вырост']
  ],
  task:{
    intro:'Выбери платформу под свою задачу и обоснуй выбор цифрами: объём задач и бюджет.',
    chips:['Задача: ...', 'Объём в месяц: ...', 'Нужные сервисы: ...', 'Свой сервер есть/нет: ...', 'Бюджет: ...', 'Выбор и почему: ...'],
    ph:'Задача: автопостинг в VK и TG + сбор заявок в Google Sheets. Объём: ~1500 исполнений/мес. Сервисы стандартные. Свой VPS есть. Бюджет минимальный. Выбор: n8n self-hosted — платформа бесплатна, VPS уже оплачен, а на Zapier эти 1500 задач вышли бы в ~$30-50/мес.',
    verdict:'Сильно — ты не просто назвал n8n, а привязал выбор к своему VPS и посчитал экономию против Zapier. Усилю: заложи 20% запаса по ресурсам сервера на пиковые исполнения и настрой автобэкап воркфлоу — на self-hosted за сохранность отвечаешь только ты.'
  }
},
{
  title:'Агент поддержки (RAG)',
  sub:'3:50 · слайды + тест + игра', dur:'3:50',
  c1:'АГЕНТ', c2:'ПОДДЕРЖКИ',
  slides:[
  {t:'Из чего собран агент поддержки', pts:['<b>LLM</b> (ChatGPT, Claude, Gemini) — мозг, что формулирует ответ', '<b>База знаний (RAG)</b> — отвечает по твоим документам, а не выдумывает', 'Инструменты плюс кнопка «позвать человека» на сложных случаях'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="22" width="52" height="38" rx="10"/><circle cx="50" cy="41" r="5"/><circle cx="70" cy="41" r="5"/><path d="M60 22v-8M50 14h20"/><path d="M28 38v10M92 38v10" opacity=".7"/></svg>'},
  {t:'Что такое RAG простыми словами', pts:['Агент ищет ответ в твоей базе и отвечает только по найденному', 'Документы бьют на куски, превращают в векторы, ищут по смыслу', 'Это лечит галлюцинации: нет в базе — не сочиняет, а зовёт человека'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 16c-8-6-22-6-30 0v40c8-6 22-6 30 0 8-6 22-6 30 0V16c-8-6-22-6-30 0z"/><path d="M60 16v40"/></svg>'},
  {t:'Собираем базу знаний', pts:['Загрузи FAQ, условия возврата, прайс, регламенты', 'Пиши коротко и по фактам — мусор на входе = мусор в ответе', 'Изменил условия — обнови документ в тот же день'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Инструменты и передача человеку', pts:['Дай агенту действия: проверить заказ, статус доставки, создать тикет', 'Триггеры эскалации: злость клиента, слово «оператор», три провала подряд', 'Живой человек подхватывает диалог с полной историей'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'Тон и границы агента', pts:['Пропиши характер: вежливый, короткий, без обещаний от балды', 'Запрети темы вне компетенции: скидки, юридические гарантии', 'Всегда честно: «я бот, при сложном вопросе позову коллегу»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Проверка перед запуском', pts:['Прогони 30-50 реальных вопросов из переписок', 'Метрика: доля диалогов, закрытых без человека (цель 60-80%)', 'Логируй провалы — они пополняют базу знаний'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Зачем агенту RAG?', o:['Чтобы отвечать по твоей базе, а не выдумывать', 'Чтобы работать без интернета', 'Чтобы быть бесплатным', 'Чтобы печатать быстрее'], a:0},
  {q:'Что делает агент, если ответа нет в базе?', o:['Зовёт человека вместо выдумки', 'Придумывает правдоподобный ответ', 'Молчит навсегда', 'Удаляет диалог'], a:0},
  {q:'Хорошая метрика агента поддержки?', o:['Доля диалогов, закрытых без человека', 'Количество слов в ответе', 'Число эмодзи', 'Скорость набора'], a:0},
  {q:'Когда передавать диалог человеку?', o:['Злость клиента, слово \'оператор\', три провала', 'Никогда, бот должен всё сам', 'Только ночью', 'После первого сообщения'], a:0},
  {q:'Что важно для качества базы знаний?', o:['Актуальность и краткость документов', 'Максимальное число страниц', 'Красивое оформление PDF', 'Пароль на файлы'], a:0}
  ],
  pairs:[
  ['LLM', 'Мозг агента: формулирует ответ'],
  ['RAG', 'Ответ строго по загруженной базе знаний'],
  ['Эскалация', 'Передача сложного диалога человеку'],
  ['Галлюцинация', 'Выдуманный ответ, которого нет в базе'],
  ['Инструмент', 'Действие агента: проверить заказ, создать тикет'],
  ['Метрика', 'Доля диалогов, закрытых без оператора']
  ],
  task:{
    intro:'Опиши своего агента поддержки: что он умеет, откуда берёт ответы и когда зовёт человека.',
    chips:['Ниша: ...', 'База знаний из: ...', 'Инструменты агента: ...', 'Правила эскалации: ...', 'Тон: ...', 'Целевая метрика: ...'],
    ph:'Ниша: онлайн-школа. База: FAQ по доступам, условия возврата, расписание. Инструменты: проверить оплату по email, выдать ссылку на урок. Эскалация: слово \'верните деньги\', технический сбой, злость. Тон: короткий, дружелюбный, без обещаний скидок. Метрика: 70% диалогов без человека.',
    verdict:'Класс — у тебя чёткие правила эскалации и конкретная база, а не \'бот на всё\'. Усилю: автологируй вопросы, на которые агент ответил плохо, в отдельную таблицу — раз в неделю разбираешь их и дописываешь базу, так метрика 70% за месяц-два дойдёт до 85%.'
  }
},
{
  title:'Автопостинг',
  sub:'3:15 · слайды + тест + игра', dur:'3:15',
  c1:'АВТО', c2:'ПОСТИНГ',
  slides:[
  {t:'Автопостинг — это конвейер, а не кнопка', pts:['Три звена: <b>расписание</b> → генерация → публикация', 'Публикует в <b>VK</b>, <b>Telegram</b> и соцсети по календарю', 'Ты готовишь контент пачкой — робот раскидывает по времени'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="16" width="76" height="46" rx="8"/><path d="M22 30h76M38 10v12M82 10v12"/><circle cx="44" cy="44" r="3" fill="currentColor" stroke="none"/><circle cx="60" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Контент-план как источник', pts:['Держи посты в таблице: дата, текст, картинка, площадка, статус', '<b>n8n</b> и <b>Make</b> читают строку и публикуют в нужный момент', 'Статус «опубликовано» ставится сам — видно, что ушло'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Генерация под площадку', pts:['Один смысл — разные форматы: длинный для TG, короткий для VK', 'Текст через <b>ChatGPT</b>/<b>Claude</b>, картинка через <b>Midjourney</b>/<b>Flux</b>', 'Шаблон промпта в бренд-стиле — лента выглядит единой'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M74 12 92 30 56 66l-18 4 4-18z"/><path d="M62 24 80 42" opacity=".7"/></svg>'},
  {t:'Расписание и лучшее время', pts:['Триггер по времени: посты уходят в часы активности аудитории', 'Не постить всё в один момент — размажь по дню', 'Очередь на неделю вперёд = стабильность без ежедневной суеты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v16l11 7"/></svg>'},
  {t:'Защита от провалов', pts:['Проверяй, что картинка и текст на месте до публикации', 'Площадка вернула ошибку — <b>ретрай</b> и уведомление в бота', 'Веди лог: что, когда и куда ушло — для разбора и отчёта'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Три звена автопостинга?', o:['Расписание, генерация, публикация', 'Лайк, репост, комментарий', 'Съёмка, монтаж, звук', 'Реклама, охват, продажа'], a:0},
  {q:'Где удобно хранить контент-план?', o:['В таблице со статусами', 'В голове', 'В личных сообщениях', 'На бумаге'], a:0},
  {q:'Почему нельзя постить всё в один момент?', o:['Нужно попадать в часы активности и не спамить', 'Соцсети берут доплату', 'Это незаконно', 'Робот не умеет'], a:0},
  {q:'Что делать при ошибке публикации?', o:['Ретрай и уведомление в бота', 'Игнорировать', 'Удалить весь план', 'Постить вручную навсегда'], a:0},
  {q:'Зачем разный формат под площадку?', o:['Длина и стиль поста в TG и VK отличаются', 'Чтобы запутать алгоритм', 'Для экономии', 'Так требует закон'], a:0}
  ],
  pairs:[
  ['Расписание', 'Триггер публикации по времени'],
  ['Контент-план', 'Таблица: дата, текст, картинка, статус'],
  ['Генерация', 'Текст и картинка под каждую площадку'],
  ['Ретрай', 'Повтор публикации при ошибке площадки'],
  ['Статус', 'Отметка \'опубликовано\' проставляется автоматически'],
  ['Очередь', 'Посты на неделю вперёд без ручной работы']
  ],
  task:{
    intro:'Спроектируй свой автопостинг: откуда берётся пост, куда и когда публикуется.',
    chips:['Площадки: ...', 'Где хранится план: ...', 'Частота постов: ...', 'Кто генерит текст/картинку: ...', 'Время публикации: ...', 'Что при ошибке: ...'],
    ph:'Площадки: Telegram-канал и группа VK. План: таблица Google Sheets со статусами. Частота: 1 пост/день. Текст — Claude по шаблону, картинка — Flux в бренд-цветах. Время: 12:00 и 19:00 по будням. При ошибке: 2 ретрая и алерт в бота.',
    verdict:'Отлично — у тебя завязаны хранилище, генерация и обработка ошибок в один поток, а не \'постим руками\'. Усилю: добавь колонку \'рубрика\' и чередуй их по расписанию (польза-кейс-оффер), чтобы лента не стала одним типом постов — алгоритмы и живые люди любят разнообразие.'
  }
},
{
  title:'Контент-конвейер',
  sub:'3:55 · слайды + тест + игра', dur:'3:55',
  c1:'КОНТЕНТ', c2:'КОНВЕЙЕР',
  slides:[
  {t:'Конвейер против одного поста', pts:['Автопостинг публикует готовое; <b>конвейер</b> производит контент на потоке', 'Цепочка: генерация → сборка → публикация, десятки единиц без ручной склейки', 'Цель — контент серией, а не один шедевр раз в неделю'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Этап генерации', pts:['Сценарий: <b>ChatGPT</b>/<b>Claude</b> по твоему промпту-шаблону', 'Визуал: <b>Midjourney</b>, <b>Flux</b>, видео — <b>Veo</b>, <b>Kling</b>, <b>Runway</b>', 'Озвучка — <b>ElevenLabs</b> или локальный TTS, музыка — <b>Suno</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'},
  {t:'Этап сборки', pts:['Собери куски в готовую единицу: текст + картинка + видео + звук', 'Шаблон сборки один — меняется только наполнение', 'Автоматика: <b>n8n</b> дирижирует, кто за кем отдаёт результат'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Этап публикации и учёта', pts:['Готовое уходит в автопостинг по расписанию', 'Каждая единица получает статус и ссылку на результат', 'Метрики возвращаются в таблицу — видно, что зашло'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30v12l14 3 30 14V13L34 27z"/><path d="M64 22c10 3 10 23 0 26" opacity=".7"/><path d="M40 48l4 12" /></svg>'},
  {t:'Разнообразие без повторов', pts:['Реестр приёмов: ни один эффект и формат не повторяется подряд', 'Ротация рубрик и форматов через поля в таблице', 'Повтор убивает охваты — конвейер должен удивлять'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M28 24h20a6 6 0 0 1 12 0h20v20a6 6 0 0 1 0 12v8H60a6 6 0 0 0-12 0H28V44a6 6 0 0 0 0-12z"/></svg>'},
  {t:'Держим темп и качество', pts:['Узкое место — там, где всё встаёт в очередь: найди и расширь', 'Контроль качества на выходе: брак не публикуется', 'Считай себестоимость единицы: время плюс кредиты генерации'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'}
  ],
  quiz:[
  {q:'Чем конвейер отличается от автопостинга?', o:['Он производит контент на потоке, а не только публикует', 'Ничем', 'Он дороже', 'Он только для видео'], a:0},
  {q:'Порядок этапов конвейера?', o:['Генерация → сборка → публикация', 'Публикация → генерация → сборка', 'Сборка → публикация → генерация', 'Только публикация'], a:0},
  {q:'Что дирижирует конвейером?', o:['Оркестратор вроде n8n', 'Ручной копипаст', 'Один Excel без автоматики', 'Ничего'], a:0},
  {q:'Почему важно разнообразие форматов?', o:['Повторы убивают охваты', 'Так дешевле', 'Требование площадок', 'Чтобы занять место'], a:0},
  {q:'Что такое узкое место конвейера?', o:['Этап, где всё встаёт в очередь', 'Самый дешёвый шаг', 'Название сервиса', 'Первый пост'], a:0}
  ],
  pairs:[
  ['Конвейер', 'Производство контента сериями на потоке'],
  ['Генерация', 'Создание текста, визуала, звука нейросетями'],
  ['Сборка', 'Склейка кусков в готовую единицу'],
  ['Оркестратор', 'n8n дирижирует последовательностью шагов'],
  ['Узкое место', 'Этап, где скапливается очередь'],
  ['Себестоимость', 'Время плюс кредиты на одну единицу']
  ],
  task:{
    intro:'Разложи свой контент-конвейер по трём этапам и укажи инструмент на каждом шаге.',
    chips:['Что производим: ...', 'Генерация — чем: ...', 'Сборка — как: ...', 'Публикация — куда: ...', 'Оркестратор: ...', 'Себестоимость единицы: ...'],
    ph:'Производим: вертикальные рилс, 3 шт/день. Генерация: сценарий — Claude, кадры — Kling, озвучка — локальный TTS, музыка — Suno. Сборка: шаблон монтажа, караоке-субтитры. Публикация: TG+VK по расписанию. Оркестратор: n8n. Себестоимость: ~15 мин моего времени + кредиты Kling на ролик.',
    verdict:'Мощно — ты видишь весь цикл и уже считаешь себестоимость ролика, это уровень продюсера, а не любителя. Усилю: замерь пропускную способность каждого этапа отдельно (сколько роликов/день тянет генерация vs сборка) — горлышко почти всегда в сборке, туда и вкладывай шаблоны, чтобы выйти с 3 на 6 роликов в день без потери качества.'
  }
},
{
  title:'Связки: соцсети + CRM + оплата',
  sub:'3:40 · слайды + тест + игра', dur:'3:40',
  c1:'СВЯЗКИ', c2:'В ОДИН ПОТОК',
  slides:[
  {t:'Один поток вместо ручных мостиков', pts:['Заявка из соцсети → карточка в <b>CRM</b> → оплата → доступ', 'Ни одного ручного переноса данных между сервисами', 'Клиент не выпадает на стыках — там теряются деньги'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M48 42 72 18a13 13 0 0 1 18 18L74 52M72 28 48 52a13 13 0 0 1-18-18l16-16" opacity=".9"/></svg>'},
  {t:'Точка входа: соцсети и мессенджеры', pts:['Лид пишет в <b>Telegram</b>/VK/Instagram — <b>вебхук</b> ловит событие', 'ИИ квалифицирует: горячий или холодный, что хочет', 'Данные летят дальше по связке автоматически'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'CRM как единая память', pts:['Каждый лид — карточка: источник, этап, сумма, история', 'Автоматика двигает по этапам: новый → квалифицирован → оплатил', 'Нет карточки — нет клиента: всё в одном месте'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Оплата и доступ', pts:['Ссылка на оплату (например <b>Lava.top</b>) генерится автоматически', 'Оплата прошла → вебхук → выдача доступа/товара мгновенно', 'Не оплатил за N часов — автонапоминание, потом менеджеру'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="18" width="76" height="38" rx="8"/><path d="M22 30h76"/><circle cx="82" cy="42" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Оркестрация связки', pts:['<b>n8n</b>/<b>Make</b>/<b>Zapier</b> соединяют соцсеть + CRM + оплату + ИИ', 'Каждый сервис делает своё, автоматика передаёт эстафету', 'Один сбой не должен ронять всю цепочку — об этом урок про надёжность'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Что это даёт в деньгах', pts:['Скорость: ответ и счёт за секунды, а не через часы', 'Ничего не теряется: каждый лид дошёл до CRM', 'Прозрачность: видно, где отваливаются клиенты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'}
  ],
  quiz:[
  {q:'Зачем связывать соцсети, CRM и оплату?', o:['Чтобы клиент не терялся на стыках сервисов', 'Чтобы было модно', 'Ради красивых отчётов', 'Так требует закон'], a:0},
  {q:'Что ловит событие \'клиент написал\'?', o:['Вебхук', 'Принтер', 'Антивирус', 'Таблица вручную'], a:0},
  {q:'Роль CRM в связке?', o:['Единая память о каждом лиде и этапе', 'Генерация картинок', 'Хостинг сайта', 'Озвучка'], a:0},
  {q:'Что происходит после успешной оплаты?', o:['Вебхук выдаёт доступ автоматически', 'Ничего, ждём менеджера', 'Удаляется карточка', 'Отправляется реклама'], a:0},
  {q:'Кто соединяет сервисы в один поток?', o:['Оркестратор: n8n, Make или Zapier', 'Сам клиент', 'Соцсеть по умолчанию', 'Никто, вручную'], a:0}
  ],
  pairs:[
  ['Вебхук', 'Ловит событие \'пришёл лид\' или \'оплата прошла\''],
  ['CRM', 'Единая карточка лида: источник, этап, сумма'],
  ['Оплата', 'Автоссылка и мгновенная выдача доступа'],
  ['Оркестратор', 'Соединяет соцсеть, CRM, оплату и ИИ'],
  ['Квалификация', 'ИИ делит лидов на горячих и холодных'],
  ['Стык', 'Место, где вручную теряются клиенты и деньги']
  ],
  task:{
    intro:'Нарисуй словами свою связку: от первого сообщения клиента до выданного доступа.',
    chips:['Точка входа: ...', 'Кто квалифицирует: ...', 'Куда падает лид: ...', 'Оплата через: ...', 'Что после оплаты: ...', 'Если не оплатил: ...'],
    ph:'Вход: сообщение в Telegram-бот. Квалифицирует ИИ-агент: продукт, бюджет. Лид падает карточкой в CRM с источником и этапом. Оплата: автоссылка Lava.top. После оплаты: вебхук выдаёт доступ в закрытый канал за 5 сек. Не оплатил за 3 часа: автонапоминание, через сутки — менеджеру.',
    verdict:'Отлично — связка замкнута от входа до выдачи, и даже прописан сценарий \'не оплатил\', где обычно и утекают деньги. Усилю: фиксируй UTM/источник в карточке CRM, чтобы через месяц видеть, какая соцсеть приносит платящих, а не просто болтунов — это развернёт бюджет к каналу, который реально окупается.'
  }
},
{
  title:'Вебхуки и расписания',
  sub:'3:20 · слайды + тест + игра', dur:'3:20',
  c1:'ТРИГГЕРЫ', c2:'ЗАПУСКА',
  slides:[
  {t:'Два способа запустить автоматизацию', pts:['<b>Вебхук</b> — запуск по событию: пришёл лид, прошла оплата', '<b>Расписание</b> (cron) — запуск по времени: каждый день в 10:00', 'Всё в автоматизации стартует одним из этих двух триггеров'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Вебхук простыми словами', pts:['Это URL, на который сервис стучится, когда что-то случилось', 'Событие → мгновенный запуск сценария, без опроса и ожидания', 'Пример: оплата в Lava.top → вебхук → выдача доступа'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Расписание (cron)', pts:['Запуск по времени: раз в час, каждое утро, по будням в 12:00', 'Для задач без внешнего события: отчёты, автопостинг, проверки', 'Формат cron — пять полей: минуты, часы, дни, месяц, день недели'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v16l11 7"/></svg>'},
  {t:'Событие или время — что выбрать', pts:['Нужна мгновенная реакция на действие → <b>вебхук</b>', 'Задача регулярная и сама по себе → <b>расписание</b>', 'Часто связка: вебхук ловит, расписание подчищает и досылает'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Подводные камни триггеров', pts:['Вебхук может прийти дважды — делай защиту от дублей', 'Ты был недоступен — нужны ретраи со стороны сервиса', 'Слишком частый cron жжёт исполнения и деньги — выбирай интервал с умом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Что запускает сценарий по событию?', o:['Вебхук', 'Расписание', 'Принтер', 'Пользователь вручную'], a:0},
  {q:'Что запускает сценарий по времени?', o:['Расписание (cron)', 'Вебхук', 'Оплата', 'Лид'], a:0},
  {q:'Хороший случай для вебхука?', o:['Мгновенная выдача доступа после оплаты', 'Ежедневный отчёт в 10:00', 'Раз в неделю бэкап', 'Ничего'], a:0},
  {q:'Опасность вебхука?', o:['Может прийти дважды — нужна защита от дублей', 'Он всегда платный', 'Работает только ночью', 'Не поддерживает текст'], a:0},
  {q:'Чем опасен слишком частый cron?', o:['Жжёт исполнения и деньги', 'Ломает интернет', 'Блокирует соцсети', 'Ничем'], a:0}
  ],
  pairs:[
  ['Вебхук', 'Запуск по событию через URL'],
  ['Cron', 'Запуск по расписанию: время и дни'],
  ['Событие', 'Оплата, новый лид, новое письмо'],
  ['Дубль', 'Вебхук пришёл дважды — нужна защита'],
  ['Интервал', 'Как часто срабатывает расписание'],
  ['Пять полей', 'Минуты, часы, дни, месяц, день недели в cron']
  ],
  task:{
    intro:'Для трёх своих задач реши, что их запускает: вебхук или расписание, и почему.',
    chips:['Задача 1 → триггер: ...', 'Задача 2 → триггер: ...', 'Задача 3 → триггер: ...', 'Где нужна мгновенность: ...', 'Где хватит по времени: ...', 'Защита от дублей: ...'],
    ph:'1) Выдача доступа после оплаты → вебхук (нужна мгновенность). 2) Автопостинг в 12:00 → расписание (cron 0 12 * * *). 3) Отчёт по заявкам каждое утро → расписание (0 10 * * *). Мгновенность — только на оплате. Дубли: проверяю id платежа, повторный вебхук игнорирую.',
    verdict:'Отлично — ты верно развёл событийные и временные триггеры и заложил проверку id платежа от дублей, а на эти грабли наступают почти все. Усилю: для cron-задач добавь \'сторожа\' — если утренний отчёт не собрался, отдельный алерт в бота, иначе молчаливый пропуск заметишь только когда клиент спросит, где цифры.'
  }
},
{
  title:'Мониторинг цен конкурентов',
  sub:'3:35 · слайды + тест + игра', dur:'3:35',
  c1:'ЦЕНЫ', c2:'КОНКУРЕНТОВ',
  slides:[
  {t:'Зачем следить за ценами конкурентов', pts:['Цена решает сделку — знай рынок в реальном времени', 'Автомониторинг: <b>парсинг</b> страниц → сравнение → оповещение', 'Ручная проверка раз в месяц = ты всегда опаздываешь'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Как устроен парсинг', pts:['Робот заходит на страницу товара и вытаскивает цену', 'Сохраняет в таблицу с датой — копится история цен', '<b>n8n</b>/<b>Make</b> по расписанию обходят список ссылок'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Триггер и оповещение', pts:['Цена изменилась больше порога → <b>алерт</b> в Telegram-бот', 'Конкурент уронил цену на 15% — узнал за минуты, а не за месяц', 'В сообщении: ссылка, старая и новая цена — сразу видно суть'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30v12l14 3 30 14V13L34 27z"/><path d="M64 22c10 3 10 23 0 26" opacity=".7"/><path d="M40 48l4 12" /></svg>'},
  {t:'История цен = аналитика', pts:['Копишь данные — видишь тренды: кто демпингует, кто держит', 'Замечаешь распродажи и сезонность конкурентов', 'Решения по своей цене — на фактах, а не на догадках'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V14M18 58h84"/><rect x="30" y="40" width="12" height="18"/><rect x="52" y="30" width="12" height="28"/><rect x="74" y="20" width="12" height="38"/></svg>'},
  {t:'Технические тонкости', pts:['Сайты меняют вёрстку — парсер ломается, следи за пустыми ценами', 'Не долби сайт слишком часто — вежливый интервал против бана', 'Пустая или странная цена → не пиши в базу, а шли предупреждение'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'}
  ],
  quiz:[
  {q:'Из чего состоит мониторинг цен?', o:['Парсинг → сравнение → оповещение', 'Реклама → охват → продажа', 'Только ручная проверка', 'Звонок конкуренту'], a:0},
  {q:'Что делает парсер?', o:['Вытаскивает цену со страницы товара', 'Пишет посты', 'Звонит клиентам', 'Рисует картинки'], a:0},
  {q:'Когда приходит алерт?', o:['Когда цена изменилась больше порога', 'Каждую секунду', 'Никогда', 'Только в Новый год'], a:0},
  {q:'Зачем копить историю цен?', o:['Видеть тренды и сезонность конкурентов', 'Занять место на диске', 'Для красоты', 'Незачем'], a:0},
  {q:'Почему нельзя парсить сайт слишком часто?', o:['Можно получить бан за нагрузку', 'Это всегда платно', 'Робот устанет', 'Данные испортятся сами'], a:0}
  ],
  pairs:[
  ['Парсинг', 'Извлечение цены со страницы конкурента'],
  ['Порог', 'Величина изменения, при которой шлём алерт'],
  ['История цен', 'Накопленные данные для анализа трендов'],
  ['Алерт', 'Оповещение в бота при изменении цены'],
  ['Пустая цена', 'Признак сломанного парсера — не писать в базу'],
  ['Интервал', 'Вежливая частота обхода, чтобы не забанили']
  ],
  task:{
    intro:'Спроектируй мониторинг цен: за кем следишь, как часто и что считается сигналом.',
    chips:['Конкуренты/ссылки: ...', 'Что снимаем: ...', 'Частота проверки: ...', 'Порог алерта: ...', 'Куда шлём сигнал: ...', 'Что при сбое парсера: ...'],
    ph:'Слежу за 8 товарами у 3 конкурентов. Снимаю цену и наличие. Частота: раз в 6 часов (cron). Порог алерта: изменение больше 5% или ушёл в \'нет в наличии\'. Сигнал: бот со ссылкой, старой и новой ценой. При пустой цене: не пишу в базу, шлю \'парсер по ссылке X сломался\'.',
    verdict:'Сильно — ты задал разумный интервал и, главное, обработал случай сломанного парсера, о котором новички забывают и потом решают по мусорным данным. Усилю: добавь в таблицу свою цену рядом с ценой конкурента и считай разницу автоматически — тогда алерт будет не \'у них дешевле\', а \'ты дороже на 12%, теряешь позицию\', и решение напрашивается само.'
  }
},
{
  title:'Ошибки, логи, надёжность',
  sub:'3:45 · слайды + тест + игра', dur:'3:45',
  c1:'ЛОГИ И', c2:'НАДЁЖНОСТЬ',
  slides:[
  {t:'Что падает молча — хуже ручной работы', pts:['Незаметный сбой = потерянные лиды и деньги без твоего ведома', 'Три опоры надёжности: <b>логи</b>, обработка ошибок, <b>ретраи</b>', 'Цель — конвейер, который сам сообщает, что сломался'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Логи: чёрный ящик конвейера', pts:['Пиши, что запустилось, с чем и чем закончилось', 'По логам находишь, на каком шаге и почему встало', 'Без логов отладка — гадание; с логами — минутное дело'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Обработка ошибок', pts:['Каждый шаг может упасть: сервис недоступен, кривые данные', 'Ветка «на случай ошибки»: поймать, записать, уведомить', 'Не дай одной ошибке уронить весь сценарий целиком'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M60 12v-6M60 64v-6M83 35h6M31 35h-6M76 19l4-4M40 55l-4 4M76 51l4 4M40 15l-4-4"/></svg>'},
  {t:'Ретраи с умом', pts:['Сервис не ответил — повтори через паузу, а не сразу и вечно', '<b>Экспоненциальная</b> пауза: 10с, 30с, 2 мин — даёшь время очнуться', 'После N попыток — сдавайся и зови человека, не долби бесконечно'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Оповещения о сбоях', pts:['Критическая ошибка → мгновенный алерт в Telegram-бот', 'В сообщении: что упало, когда, ссылка на детали', 'Лучше 5 честных алертов, чем один тихий провал на неделю'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30v12l14 3 30 14V13L34 27z"/><path d="M64 22c10 3 10 23 0 26" opacity=".7"/><path d="M40 48l4 12" /></svg>'},
  {t:'Идемпотентность и дубли', pts:['Повторный запуск не создаёт второй счёт или доступ', 'Проверяй по id: уже обработано — пропусти', 'Это спасает при ретраях и двойных вебхуках'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="30" width="52" height="34" rx="8"/><path d="M44 30v-8a16 16 0 0 1 32 0v8"/><circle cx="60" cy="46" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Регламент здоровья системы', pts:['Раз в неделю смотри логи и долю ошибок — есть ли рост', 'Держи резервную копию воркфлоу и базы знаний', 'Тестируй сценарий после каждого изменения — не на живых клиентах'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="28" y="6" width="64" height="58" rx="8"/><path d="M38 20l4 4 7-8M38 36l4 4 7-8M38 52l4 4 7-8" stroke-width="2.8"/><path d="M56 21h26M56 37h26M56 53h18" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Три опоры надёжности?', o:['Логи, обработка ошибок, ретраи', 'Лайки, репосты, охваты', 'Цена, качество, срок', 'Текст, звук, видео'], a:0},
  {q:'Зачем нужны логи?', o:['Найти, где и почему сценарий встал', 'Для красоты интерфейса', 'Чтобы занять память', 'Незачем'], a:0},
  {q:'Что такое экспоненциальный ретрай?', o:['Повтор с растущей паузой: 10с, 30с, 2 мин', 'Повтор сразу и бесконечно', 'Один повтор через сутки', 'Отказ от повторов'], a:0},
  {q:'Что даёт идемпотентность?', o:['Повтор не создаёт дубль счёта или доступа', 'Ускорение в 10 раз', 'Бесплатный хостинг', 'Красивые логи'], a:0},
  {q:'Почему тихий сбой опаснее ручной работы?', o:['Теряешь лиды и деньги, не зная об этом', 'Он ломает компьютер', 'Он всегда платный', 'Он не опасен'], a:0},
  {q:'Что делать после N неудачных ретраев?', o:['Остановиться и позвать человека', 'Долбить сервис бесконечно', 'Удалить сценарий', 'Игнорировать'], a:0}
  ],
  pairs:[
  ['Лог', 'Запись: что запустилось и чем закончилось'],
  ['Обработка ошибок', 'Ветка \'на случай сбоя\': поймать и уведомить'],
  ['Ретрай', 'Повтор шага с растущей паузой'],
  ['Идемпотентность', 'Повтор не создаёт дубль счёта или доступа'],
  ['Алерт', 'Мгновенное сообщение о критическом сбое'],
  ['Бэкап', 'Резервная копия воркфлоу и базы']
  ],
  task:{
    intro:'Сделай свой сценарий отказоустойчивым: пропиши логи, обработку ошибок и оповещения.',
    chips:['Сценарий: ...', 'Что логирую: ...', 'Где может упасть: ...', 'Стратегия ретраев: ...', 'Куда шлю алерт: ...', 'Защита от дублей: ...'],
    ph:'Сценарий: оплата → выдача доступа. Логирую: id платежа, время, статус выдачи. Уязвимое место: сервис доступов недоступен. Ретраи: 3 попытки с паузой 15с/1мин/5мин. Алерт: при третьем провале — бот \'доступ по платежу X не выдан\'. Дубли: проверяю id платежа, повторный вебхук игнорирую.',
    verdict:'Отлично — закрыты все три опоры: логи, ретраи с растущей паузой и защита от дублей по id, это продакшн-уровень, а не \'на коленке\'. Усилю: заведи простой счётчик \'успешно/с ошибкой\' за день — когда доля ошибок ползёт вверх, ты увидишь это раньше, чем клиент напишет \'не пришёл доступ\', и починишь на опережение.'
  }
}
  ],
  "a5": [
{
  title:'ИИ-услуги и цены',
  sub:'3:40 · слайды + тест + игра', dur:'3:40',
  c1:'ИИ-УСЛУГИ', c2:'И ЦЕНЫ',
  slides:[
  {t:'Пять услуг, которые покупают уже сегодня', pts:['<b>ИИ-аватары и фотосессия</b> — 5-30к ₽ за пакет портретов без фотографа', '<b>Reels пачками</b> — себестоимость 200-500 ₽, продаёшь от 1500 ₽ за ролик', '<b>Телеграм-боты</b> — 10-50к ₽ за бота-продавца или автоответчик', '<b>Лендинги на вайбкодинге</b> — 30-100к ₽ за страницу с нуля', '<b>Автоматизация под ключ</b> — от 30к ₽ за связку без ручного труда'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v30M53 27h12a5 5 0 0 1 0 10h-10a5 5 0 0 0 0 10h13"/></svg>'},
  {t:'Почему маржа 60-90%, а не 20', pts:['Себестоимость на ИИ — это подписка и генерации, а не часы работы', 'Аватары: <b>Higgsfield</b> или <b>Midjourney</b> — десятки рублей за сет', 'Ролик: сток + <b>ElevenLabs</b> озвучка + монтаж = 200-500 ₽', 'Ты продаёшь результат и скорость, клиент не считает твою себестоимость'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'За что платят на самом деле', pts:['Не за нейросеть — за снятую задачу и сэкономленное время', 'Бизнесу нужен <b>поток контента</b>, а не доступ к <b>ChatGPT</b>', 'Гарантия результата дороже, чем «я умею в Midjourney»', 'Упакованная услуга продаётся в 3-5 раз дороже сырой генерации'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'},
  {t:'Как выбрать первую услугу', pts:['Бери то, что делаешь за вечер и не выгораешь', 'Аватары и Reels — быстрый вход, деньги на 2-3 день', 'Боты и лендинги — чек выше, но нужен навык <b>вайбкодинга</b>', 'Начни с одной услуги, добей до кейса, потом расширяй'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Первые деньги: реальный путь', pts:['Неделя 1 — собрал 3 демо-работы для портфолио', 'Неделя 2 — 5-10 холодных касаний в день по своей нише', 'Первый чек обычно 3-15к ₽ — это нормальный старт', 'Дальше цену поднимаешь с каждым новым кейсом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c12 6 18 18 18 30l-8 8H50l-8-8c0-12 6-24 18-30z"/><circle cx="60" cy="30" r="6"/><path d="M50 46l-8 12M70 46l8 12M60 50v10"/></svg>'}
  ],
  quiz:[
  {q:'Какая примерная себестоимость одного ролика Reels на ИИ?', o:['200-500 ₽', '3000-5000 ₽', '10-15к ₽', '50 ₽ фиксировано'], a:0},
  {q:'За что реально платит клиент?', o:['За доступ к нейросети', 'За снятую задачу и скорость', 'За количество генераций', 'За подписку исполнителя'], a:1},
  {q:'Какой чек у Телеграм-бота под ключ?', o:['500-1000 ₽', '10-50к ₽', 'от 300к ₽', 'бесплатно'], a:1},
  {q:'С какой услуги проще всего стартовать новичку?', o:['Автоматизация корпораций', 'Аватары и Reels', 'ERP-системы', 'Мобильные приложения'], a:1},
  {q:'Почему упакованная услуга дороже сырой генерации?', o:['Из-за налогов', 'Продаёшь результат и гарантию, а не доступ', 'Нейросеть дороже', 'Так принято'], a:1}
  ],
  pairs:[
  ['ИИ-аватары', 'Пакет портретов без фотографа, 5-30к ₽'],
  ['Reels пачками', 'Себестоимость 200-500 ₽, продажа от 1500 ₽'],
  ['Телеграм-бот', 'Автоответчик или продавец, 10-50к ₽'],
  ['Лендинг', 'Страница на вайбкодинге, 30-100к ₽'],
  ['Маржа 60-90%', 'Разница между копеечной себестоимостью и ценником']
  ],
  task:{
    intro:'Выбери одну ИИ-услугу для старта и распиши по ней: что делаешь, за сколько времени, себестоимость и цену продажи.',
    chips:['Услуга: ___', 'Время на 1 заказ: ___', 'Себестоимость: ___ ₽', 'Цена продажи: ___ ₽', 'Моя ниша: ___', 'Маржа: ___%'],
    ph:'Услуга: Reels пачками. Время: 40 мин/ролик. Себестоимость: 350 ₽ (сток + ElevenLabs). Цена продажи: 2000 ₽/ролик. Ниша: локальные кофейни. Маржа: 82%.',
    verdict:'Отличная конкретика — ты посчитал себестоимость и маржу, а не абстрактно «буду делать ролики». Совет: сразу заложи пакет из 8-10 роликов, так средний чек вырастет, а время на переговоры не изменится.'
  }
},
{
  title:'Упаковка ИИ-услуги',
  sub:'3:55 · слайды + тест + игра', dur:'3:55',
  c1:'УПАКОВКА', c2:'УСЛУГИ',
  slides:[
  {t:'Сырая услуга не продаётся', pts:['«Делаю картинки в Midjourney» — это не оффер, это хобби', 'Клиент не покупает инструмент, он покупает <b>решение проблемы</b>', 'Упаковка превращает навык в понятный продукт с ценой', 'Одна и та же работа в упаковке стоит в 3-5 раз дороже'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M28 24h20a6 6 0 0 1 12 0h20v20a6 6 0 0 1 0 12v8H60a6 6 0 0 0-12 0H28V44a6 6 0 0 0 0-12z"/></svg>'},
  {t:'Формула оффера', pts:['Кому + какой результат + за какой срок + с какой гарантией', '«30 Reels для салона за 7 дней или верну деньги» — это оффер', 'Убери из формулировки слова «нейросеть» и «ИИ» — оставь выгоду', 'Оффер должен читаться за 5 секунд без вопросов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Кейсы до/после — главный аргумент', pts:['Один экран «было/стало» убеждает сильнее трёх абзацев текста', 'Показывай цифры: было 200 охватов, стало 15 000', 'Даже учебный кейс на себе работает лучше пустого портфолио', 'Собери 3 кейса до первой продажи — это твой козырь'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'},
  {t:'Гарантия снимает страх', pts:['Клиент боится слить деньги — сними этот страх на себя', '«Не понравится первый вариант — переделаю бесплатно»', 'Гарантия результата поднимает конверсию и оправдывает цену', 'Гарантия закрывает главное возражение — как в блоке Продажи'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'},
  {t:'Продающее описание услуги', pts:['Оффер, 3 кейса, что входит в пакет, цена, гарантия, призыв', 'Опиши процесс по шагам — клиент любит понятность', 'Добавь дедлайн или ограничение мест для срочности', 'Собери всё на одном <b>лендинге</b> через вайбкодинг за вечер'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="8" width="52" height="54" rx="7"/><path d="M44 22h32M44 32h32M44 42h24M44 52h18" opacity=".85"/></svg>'},
  {t:'Частая ошибка новичка', pts:['Продаёт процесс («сделаю в Kling»), а не результат', 'Стесняется цены и демпингует — обесценивает работу', 'Нет ни одного кейса — нечем закрыть недоверие', 'Лечится одним сильным оффером и тремя кейсами до/после'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'}
  ],
  quiz:[
  {q:'Что из этого — настоящий оффер?', o:['Делаю видео в Kling', '30 Reels для салона за 7 дней с гарантией', 'Умею в нейросети', 'Дёшево и быстро'], a:1},
  {q:'Из чего состоит формула оффера?', o:['Кому + результат + срок + гарантия', 'Только цена', 'Название нейросети + цена', 'Портфолио + отзывы'], a:0},
  {q:'Зачем нужна гарантия?', o:['Для красоты', 'Снять страх клиента слить деньги', 'Обязательна по закону', 'Чтобы работать бесплатно'], a:1},
  {q:'Сколько кейсов стоит собрать до первой продажи?', o:['Ноль, продам так', 'Минимум 3 до/после', 'Не меньше 50', 'Один текстовый отзыв'], a:1},
  {q:'Главная ошибка при упаковке?', o:['Слишком высокая цена', 'Продаёт процесс вместо результата', 'Много кейсов', 'Есть гарантия'], a:1}
  ],
  pairs:[
  ['Оффер', 'Кому + результат + срок + гарантия'],
  ['Кейс до/после', 'Наглядное «было/стало» с цифрами'],
  ['Гарантия', 'Снимает страх клиента потерять деньги'],
  ['Упаковка', 'Превращает навык в продукт с ценой'],
  ['Сырая услуга', '«Делаю в нейросети» без результата и цены']
  ],
  task:{
    intro:'Составь оффер своей услуги по формуле: кому, какой результат, за какой срок, с какой гарантией.',
    chips:['Кому: ___', 'Результат: ___', 'Срок: ___', 'Гарантия: ___', 'Цена пакета: ___', 'Крючок срочности: ___'],
    ph:'Кому: барбершопам. Результат: 20 Reels + оформленная шапка профиля. Срок: 10 дней. Гарантия: не зайдёт первый ролик — переделаю бесплатно. Цена: 18к ₽. Срочность: беру 3 проекта в месяц.',
    verdict:'Сильный оффер — есть адресат, измеримый результат, срок и гарантия, всё как надо. Совет: добавь в описание один кейс до/после с цифрами охватов, тогда цена 18к перестанет вызывать вопросы и торг.'
  }
},
{
  title:'Автоворонки на ИИ',
  sub:'4:05 · слайды + тест + игра', dur:'4:05',
  c1:'АВТО-', c2:'ВОРОНКИ',
  slides:[
  {t:'Воронка работает, пока ты спишь', pts:['Ручные продажи упираются в твоё время и силы', 'Автоворонка ведёт клиента от интереса к оплате без тебя', 'Схема простая: <b>лид-магнит</b> → прогрев в боте → продажа', 'Один раз собрал — работает на потоке заявок месяцами'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 14h84L70 44v18l-20 8V44z"/></svg>'},
  {t:'Лид-магнит: бесплатно, но ценно', pts:['Это полезность в обмен на контакт, а не реклама', 'ИИ-гайд, чек-лист промптов, подборка связок — делается за час', 'Собери его в <b>ChatGPT</b> или <b>Claude</b>, оформи в PDF', 'Правило: лид-магнит решает одну маленькую боль целиком'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="28" width="68" height="34" rx="6"/><path d="M26 38h68M60 28v34"/><path d="M60 28c-6-14-22-10-16-2 3 4 16 2 16 2zM60 28c6-14 22-10 16-2-3 4-16 2-16 2z"/></svg>'},
  {t:'Бот-прогрев вместо переписки руками', pts:['<b>Телеграм-бот</b> выдаёт гайд и ведёт серию сообщений', 'Прогрев — это польза, кейсы и снятие возражений по шагам', 'Собирается на <b>n8n</b> или <b>Make</b> без глубокого кода', 'Бот не устаёт и обрабатывает сотни лидов параллельно'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="34" y="22" width="52" height="38" rx="10"/><circle cx="50" cy="41" r="5"/><circle cx="70" cy="41" r="5"/><path d="M60 22v-8M50 14h20"/><path d="M28 38v10M92 38v10" opacity=".7"/></svg>'},
  {t:'Точка продажи в воронке', pts:['После 3-5 касаний пользы — оффер с кейсом и гарантией', 'Кнопка «Обсудить проект» ведёт на тебя или на оплату', 'Дожим: ограничение мест или дедлайн скидки', 'Горячий лид доходит до тебя уже прогретым, торг короче'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30v12l14 3 30 14V13L34 27z"/><path d="M64 22c10 3 10 23 0 26" opacity=".7"/><path d="M40 48l4 12" /></svg>'},
  {t:'Где брать трафик в воронку', pts:['Reels и Shorts с призывом «забери гайд в боте»', 'Комментарии под постами в нише, партнёрские посевы', 'Каждый ролик — это вход в воронку, а не разовый просмотр', 'Трафик + автоворонка = заявки без ручного поиска'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 30v12l14 3 30 14V13L34 27z"/><path d="M64 22c10 3 10 23 0 26" opacity=".7"/><path d="M40 48l4 12" /></svg>'},
  {t:'Метрики, которые смотрим', pts:['Стоимость лида, конверсия в диалог, конверсия в оплату', 'Слабое звено видно по цифрам — чиним именно его', 'Лиды есть, а продаж нет — проблема в оффере или прогреве', 'Считай окупаемость воронки, а не только число подписчиков'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'}
  ],
  quiz:[
  {q:'Из каких трёх шагов состоит базовая автоворонка?', o:['Реклама → звонок → счёт', 'Лид-магнит → бот-прогрев → продажа', 'Пост → лайк → подписка', 'Сайт → SEO → продажа'], a:1},
  {q:'Что такое лид-магнит?', o:['Платный курс', 'Полезность в обмен на контакт', 'Реклама услуги', 'Скидочный купон'], a:1},
  {q:'На чём удобно собрать бота-прогрев без глубокого кода?', o:['n8n или Make', 'Photoshop', 'Excel', 'Midjourney'], a:0},
  {q:'Когда в воронке показывать оффер?', o:['Сразу в первом сообщении', 'После нескольких касаний пользы', 'Никогда', 'Только по звонку'], a:1},
  {q:'Что делать, если лиды идут, а продаж нет?', o:['Лить больше трафика', 'Чинить оффер или прогрев', 'Поднять цену', 'Удалить бота'], a:1}
  ],
  pairs:[
  ['Лид-магнит', 'ИИ-гайд или чек-лист за контакт'],
  ['Бот-прогрев', 'Серия полезных сообщений в Телеграм'],
  ['Точка продажи', 'Оффер с кейсом после касаний пользы'],
  ['Трафик', 'Reels и посевы, ведущие в бота'],
  ['Метрика', 'Стоимость лида и конверсия в оплату']
  ],
  task:{
    intro:'Спроектируй свою автоворонку: какой лид-магнит, какая цепочка прогрева и где точка продажи.',
    chips:['Лид-магнит: ___', 'Платформа бота: ___', 'Шагов прогрева: ___', 'Оффер в конце: ___', 'Источник трафика: ___', 'Цена лида: ___'],
    ph:'Лид-магнит: PDF «15 промптов для Reels». Бот на n8n. Прогрев: 4 сообщения — польза, кейс, возражение, оффер. Оффер: пакет 20 роликов за 18к. Трафик: Reels с призывом. Цена лида: до 40 ₽.',
    verdict:'Хорошая воронка — лид-магнит бьёт в конкретную боль, а прогрев логично ведёт к офферу. Совет: добавь после оффера одно догревающее сообщение через сутки для тех, кто не ответил, оно обычно возвращает 10-20% заявок.'
  }
},
{
  title:'Ценообразование и себестоимость',
  sub:'3:50 · слайды + тест + игра', dur:'3:50',
  c1:'ЦЕНА', c2:'И МАРЖА',
  slides:[
  {t:'Считай себестоимость, а не гадай цену', pts:['Цена с потолка приводит к работе в минус или демпингу', 'Себестоимость на ИИ — это подписки, генерации и твоё время', '<b>Higgsfield</b>, <b>ElevenLabs</b>, <b>Midjourney</b> — считай в рублях за заказ', 'Знаешь себестоимость — знаешь свою нижнюю границу цены'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="18" width="76" height="38" rx="8"/><path d="M22 30h76"/><circle cx="82" cy="42" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Три уровня цены', pts:['Себестоимость — сколько тебе стоит сделать', 'Цена за час — себестоимость плюс твоя ставка времени', 'Цена за ценность — сколько результат приносит клиенту', 'Продавай ближе к ценности, а не к себестоимости'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v42M40 54h40" /><path d="M30 24h60M30 24l-10 18h20zM90 24l-10 18h20z"/></svg>'},
  {t:'Почему нельзя продавать по часам', pts:['Ты растёшь в скорости — и наказываешь себя меньшим чеком', 'Клиенту важен результат, а не сколько ты возился', 'Фиксируй цену за результат: «пакет роликов», «бот под ключ»', 'ИИ ускоряет тебя — прибыль забираешь ты, а не клиент'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M60 20v16l11 7"/></svg>'},
  {t:'Ошибка демпинга', pts:['Низкая цена привлекает худших клиентов и обесценивает труд', '«Дёшево» читается как «некачественно» — это отпугивает', 'Поднять цену после демпинга сложнее, чем сразу встать выше', 'Лучше меньше заказов с нормальной маржой, чем поток за копейки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8c4 12-6 16-6 26 0-6-5-9-5-9-4 6-11 11-11 22a22 22 0 0 0 44 0c0-14-12-20-16-39z"/></svg>'},
  {t:'Как обосновать цену клиенту', pts:['Считай не свою себестоимость, а его выгоду и экономию', '«Фотограф — 25к, у меня аватары — 8к и за день»', 'Сравнивай с альтернативой клиента, а не со своими затратами', 'Кейс с цифрами закрывает вопрос цены лучше любого спора'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 30l20-8 14 8 14-8 20 8"/><path d="M30 34l14 14 8-6 10 10 8-6"/><path d="M84 34l-14 14" opacity=".7"/></svg>'},
  {t:'Пример расчёта', pts:['Пакет 20 Reels: сток и озвучка ~7к, время ~10 часов', 'Ставишь цену 25-30к — маржа выше 70% даже с учётом времени', 'Заложи запас на правки, чтобы не работать в ноль', 'Пересчитывай себестоимость раз в месяц — тарифы ИИ меняются'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V14M18 58h84"/><rect x="30" y="40" width="12" height="18"/><rect x="52" y="30" width="12" height="28"/><rect x="74" y="20" width="12" height="38"/></svg>'}
  ],
  quiz:[
  {q:'Что входит в себестоимость ИИ-услуги?', o:['Только налоги', 'Подписки, генерации и твоё время', 'Ничего, всё бесплатно', 'Аренда офиса'], a:1},
  {q:'К чему привязывать цену?', o:['К себестоимости', 'К ценности для клиента', 'К настроению', 'К курсу доллара'], a:1},
  {q:'Почему опасно продавать по часам?', o:['Это незаконно', 'Растёшь в скорости — теряешь в чеке', 'Клиенты не понимают', 'Часы считать сложно'], a:1},
  {q:'Чем плох демпинг?', o:['Ничем, это выгодно', 'Привлекает худших клиентов и обесценивает труд', 'Слишком много заказов', 'Налоговая накажет'], a:1},
  {q:'Как лучше обосновать цену клиенту?', o:['Показать свою себестоимость', 'Сравнить с его альтернативой и выгодой', 'Сказать «так надо»', 'Дать скидку'], a:1}
  ],
  pairs:[
  ['Себестоимость', 'Подписки, генерации и время на заказ'],
  ['Цена за ценность', 'Сколько результат приносит клиенту'],
  ['Демпинг', 'Низкая цена, что обесценивает труд'],
  ['Оплата за результат', 'Фикс за пакет, а не за часы'],
  ['Обоснование цены', 'Сравнение с альтернативой клиента']
  ],
  task:{
    intro:'Посчитай реальную себестоимость одного своего заказа и назначь цену от ценности, а не от затрат.',
    chips:['Услуга: ___', 'Подписки/генерации: ___ ₽', 'Время: ___ ч', 'Себестоимость итого: ___ ₽', 'Альтернатива клиента: ___ ₽', 'Моя цена: ___ ₽'],
    ph:'Услуга: ИИ-фотосессия. Генерации в Higgsfield: 400 ₽. Время: 3 ч. Себестоимость: ~1400 ₽ с учётом часа. Альтернатива — фотограф за 20к. Моя цена: 8к ₽, маржа 82%.',
    verdict:'Грамотно — ты оттолкнулся от альтернативы клиента (фотограф за 20к), а не от своих 400 ₽ генераций. Совет: сделай два тарифа — базовый на 8к и расширенный на 14к с большим числом образов, часть клиентов сама выберет дороже.'
  }
},
{
  title:'Пакеты и допродажи',
  sub:'3:45 · слайды + тест + игра', dur:'3:45',
  c1:'ПАКЕТЫ И', c2:'ДОПРОДАЖИ',
  slides:[
  {t:'Пакет продаётся легче, чем поштучно', pts:['Один ролик — разовая сделка, пакет — предсказуемый доход', 'Клиенту проще выбрать из 3 тарифов, чем решать «брать или нет»', 'Средний чек пакета выше, а времени на переговоры столько же', 'Пакет привязывает клиента к тебе на недели вперёд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="12" width="60" height="14" rx="4"/><rect x="30" y="30" width="60" height="14" rx="4" opacity=".85"/><rect x="30" y="48" width="60" height="14" rx="4" opacity=".7"/></svg>'},
  {t:'Правило трёх тарифов', pts:['Базовый, оптимальный, премиум — большинство берёт средний', 'Премиум-тариф делает средний выгодным на контрасте', 'В каждом тарифе — понятный список, что входит', 'Якорь дорогого тарифа поднимает восприятие всей линейки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="12" width="32" height="20" rx="4"/><rect x="66" y="12" width="32" height="20" rx="4"/><rect x="22" y="40" width="32" height="20" rx="4"/><rect x="66" y="40" width="32" height="20" rx="4"/></svg>'},
  {t:'Допродажа сразу после «да»', pts:['Клиент, который уже сказал «да», покупает допы легко', '«Добавить оформление профиля к пакету роликов — плюс 5к?»', 'Допродажа даёт 20-40% сверху без нового поиска клиента', 'Предлагай доп в момент оплаты, а не через неделю'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M62 6 34 40h20l-6 24 30-38H56z"/></svg>'},
  {t:'Что допродавать в ИИ-услугах', pts:['К роликам — обложки, шапка профиля, монтаж Stories', 'К боту — сопровождение и доработки на абонентке', 'К лендингу — тексты, <b>ИИ-аватар</b> для героя, домен и деплой', 'К аватарам — доп-образы, ретушь, печатный макет'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Абонентка — деньги каждый месяц', pts:['Разовый заказ кончается, подписка приносит стабильно', '«Веду ваши Reels — 20 роликов в месяц за фикс»', 'Ретеншн важнее новых продаж: удержать дешевле, чем найти', '5 клиентов на абонентке — предсказуемый доход месяцами'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="16" width="76" height="46" rx="8"/><path d="M22 30h76M38 10v12M82 10v12"/><circle cx="44" cy="44" r="3" fill="currentColor" stroke="none"/><circle cx="60" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Ошибки в пакетах', pts:['Тарифы почти не отличаются — клиент не видит смысла доплаты', 'Слишком много опций — выбор парализует, сделка срывается', 'Нет якорного премиума — средний тариф не кажется выгодным', 'Допродажа звучит как впаривание, а не как забота'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'}
  ],
  quiz:[
  {q:'Почему пакет продавать выгоднее поштучного?', o:['Так проще считать', 'Выше средний чек при тех же переговорах', 'Клиенты любят цифры', 'Меньше работы'], a:1},
  {q:'Зачем в линейке нужен дорогой премиум-тариф?', o:['Чтобы его покупали все', 'Как якорь, делающий средний выгодным', 'Для красоты', 'Он не нужен'], a:1},
  {q:'Когда лучше предлагать допродажу?', o:['Через месяц', 'Сразу после согласия на основной заказ', 'Никогда', 'До первого контакта'], a:1},
  {q:'Что такое абонентка в ИИ-услугах?', o:['Разовая оплата', 'Ежемесячный фикс за поток работ', 'Скидка новичкам', 'Бесплатный тест'], a:1},
  {q:'Частая ошибка в тарифной линейке?', o:['Слишком дорогой премиум', 'Тарифы почти не отличаются друг от друга', 'Есть якорь', 'Понятный список услуг'], a:1}
  ],
  pairs:[
  ['Три тарифа', 'Базовый, оптимальный, премиум'],
  ['Якорь', 'Дорогой тариф, делающий средний выгодным'],
  ['Допродажа', 'Плюс к чеку сразу после согласия'],
  ['Абонентка', 'Ежемесячный фикс за поток работ'],
  ['Ретеншн', 'Удержать клиента дешевле, чем найти']
  ],
  task:{
    intro:'Собери линейку из трёх тарифов на свою услугу и придумай одну допродажу к основному заказу.',
    chips:['Базовый: ___ / ___ ₽', 'Оптимальный: ___ / ___ ₽', 'Премиум: ___ / ___ ₽', 'Допродажа: ___ / +___ ₽', 'Абонентка: ___ ₽/мес', 'Что якорит: ___'],
    ph:'Базовый: 10 роликов / 12к. Оптимальный: 20 роликов + обложки / 22к. Премиум: 30 роликов + Stories + профиль / 38к. Допродажа: монтаж 5 Stories / +6к. Абонентка: 20 роликов/мес / 20к.',
    verdict:'Отлично выстроено — премиум за 38к работает якорем и делает средний тариф очевидным выбором. Совет: пропиши допродажу как готовую фразу в момент оплаты, тогда она не забудется и стабильно даст +6к почти с каждого заказа.'
  }
},
{
  title:'Поиск клиентов',
  sub:'4:00 · слайды + тест + игра', dur:'4:00',
  c1:'ПОИСК', c2:'КЛИЕНТОВ',
  slides:[
  {t:'Клиенты не приходят сами в начале', pts:['Ждать заявок без имени и кейсов — терять недели впустую', 'На старте работает исходящий: ты идёшь к клиенту первым', '<b>Холодные касания</b> + чёткая ниша дают первые сделки быстрее', 'Правило старта: 5-10 адресных касаний каждый день'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="50" cy="32" r="20"/><path d="M65 47l22 20"/></svg>'},
  {t:'Выбери одну нишу', pts:['«Всем подряд» = никому, оффер размывается', 'Ниша — это язык клиента, понятные боли и готовые кейсы', 'Барбершопы, стоматологии, кофейни, эксперты — выбери одну', 'В одной нише второй клиент приходит по рекомендации первого'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><circle cx="60" cy="35" r="15"/><circle cx="60" cy="35" r="5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Как заходить в холодную', pts:['Не «здравствуйте, предлагаю услуги», а конкретная польза', 'Покажи, что уже сделал разбор именно их профиля', 'Приложи мини-кейс или пример под их нишу', 'Персонализация в первом сообщении решает всё'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="14" width="54" height="38" rx="9"/><path d="M30 60l6-8h12" opacity=".8"/><rect x="58" y="30" width="46" height="30" rx="8" opacity=".85"/></svg>'},
  {t:'Где искать клиентов', pts:['Локальный бизнес в картах и справочниках — у многих слабый контент', 'Эксперты и блогеры в нише — им вечно не хватает рук', 'Комментарии и чаты по теме, доски объявлений, нетворк', 'Собирай базу контактов таблицей и веди касания системно'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="27"/><path d="M72 23 54 41l-6 18 18-6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Касания — это не один заход', pts:['Продажа часто на 3-7 касании, а не с первого сообщения', 'Не ответили — не значит «нет», часто просто не вовремя', 'Возвращайся с новой пользой, а не с «ну что, надумали?»', 'Веди учёт касаний, чтобы не бросать и не дублировать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M48 42 72 18a13 13 0 0 1 18 18L74 52M72 28 48 52a13 13 0 0 1-18-18l16-16" opacity=".9"/></svg>'},
  {t:'Кейс превращает холод в тепло', pts:['Первый бесплатный или дешёвый кейс окупается потоком тёплых', '«Смотрите, что я сделал соседнему салону» — сильнее рекламы', 'Отзыв и цифры результата снимают недоверие к новичку', 'Один живой кейс закрывает следующие сделки почти без споров'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l34 10v20c0 18-14 28-34 34-20-6-34-16-34-34V18z"/><path d="M48 34l9 9 17-17" stroke-width="2.8"/></svg>'}
  ],
  quiz:[
  {q:'Что работает на старте лучше всего?', o:['Ждать входящих заявок', 'Исходящие холодные касания в нише', 'Платная реклама на 100к', 'Ничего не делать'], a:1},
  {q:'Почему важно выбрать одну нишу?', o:['Так меньше клиентов', 'Оффер и кейсы бьют точно, идут рекомендации', 'Это правило налоговой', 'Чтобы поднять цену'], a:1},
  {q:'Как правильно заходить в холодную?', o:['Шаблон «предлагаю услуги»', 'Персонально, с разбором и мини-кейсом', 'Массовая рассылка спама', 'Просто прайс без слов'], a:1},
  {q:'На каком касании чаще случается продажа?', o:['Всегда на первом', 'Часто на 3-7 касании', 'Никогда', 'Только после рекламы'], a:1},
  {q:'Что быстрее превращает холодного клиента в тёплого?', o:['Низкая цена', 'Живой кейс с результатом в его нише', 'Длинный прайс', 'Много сообщений подряд'], a:1}
  ],
  pairs:[
  ['Холодное касание', 'Первый адресный контакт с клиентом'],
  ['Ниша', 'Одна сфера с понятными болями и кейсами'],
  ['Персонализация', 'Разбор их профиля в первом сообщении'],
  ['Серия касаний', 'Продажа на 3-7 контакте, а не с первого'],
  ['Кейс в нише', 'Аргумент, снимающий недоверие к новичку']
  ],
  task:{
    intro:'Выбери нишу и напиши первое холодное сообщение потенциальному клиенту: персонально, с пользой и мини-кейсом.',
    chips:['Ниша: ___', 'Кому пишу: ___', 'Замеченная боль: ___', 'Что предлагаю: ___', 'Мини-кейс: ___', 'Призыв: ___'],
    ph:'Ниша: кофейни. Кому: владельцу локальной кофейни. Боль: 3 поста за полгода, охваты мёртвые. Предлагаю: 15 Reels под их меню. Кейс: соседней кофейне поднял охваты с 200 до 9000. Призыв: скину 2 демо-ролика бесплатно?',
    verdict:'Сильное касание — ты заметил конкретную боль и сразу дал кейс с цифрами, это не спам, а разговор по делу. Совет: заканчивай микро-шагом («скину 2 демо»), а не большим вопросом о сотрудничестве — на маленькое «да» соглашаются охотнее.'
  }
},
{
  title:'Кейсы до/после',
  sub:'3:35 · слайды + тест + игра', dur:'3:35',
  c1:'КЕЙСЫ', c2:'ДО / ПОСЛЕ',
  slides:[
  {t:'Кейс продаёт вместо тебя', pts:['Слова «я хороший специалист» не значат ничего', 'Кейс до/после показывает результат, а не обещание', 'Один сильный кейс закрывает больше сделок, чем длинный прайс', 'Нет кейсов — сделай на себе или бесплатно для первого клиента'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Анатомия сильного кейса', pts:['Точка А: что было и в чём была боль клиента', 'Что сделал: коротко, без технических деталей про нейросети', 'Точка Б: что стало, с конкретными цифрами', 'Вывод и отзыв клиента — вишенка доверия'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 10h32l14 14v38a6 6 0 0 1-6 6H40a6 6 0 0 1-6-6V16a6 6 0 0 1 6-6z"/><path d="M72 10v14h14M44 34h32M44 44h32M44 54h20" opacity=".85"/></svg>'},
  {t:'Цифры важнее красивых слов', pts:['«Стало лучше» — пусто, «охваты с 300 до 12 000» — убедительно', 'Считай то, что важно клиенту: заявки, охваты, продажи, время', 'Даже «сэкономил 15 часов в неделю» — сильный измеримый результат', 'Скриншот статистики весит больше, чем твоё честное слово'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V14M18 58h84"/><rect x="30" y="40" width="12" height="18"/><rect x="52" y="30" width="12" height="28"/><rect x="74" y="20" width="12" height="38"/></svg>'},
  {t:'Визуал: покажи, а не рассказывай', pts:['Экран «было/стало» рядом — мозг считывает разницу мгновенно', 'До: унылый профиль. После: живая лента роликов', 'Собери карусель или короткое видео-до/после', 'Обложку кейса можно сделать в <b>Higgsfield</b> или <b>Canva</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="24" y="14" width="72" height="46" rx="7"/><circle cx="43" cy="30" r="6"/><path d="M28 54l20-18 14 12 12-10 22 16" opacity=".85"/></svg>'},
  {t:'Кейс без клиента — тоже кейс', pts:['Нет заказов — сделай кейс на своём проекте или на друге', '«Оживил мёртвый профиль знакомого» — уже доказательство', 'Тестовый кейс показывает процесс и качество твоей работы', 'Первые 3 кейса можно собрать за неделю, не дожидаясь оплаты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10a20 20 0 0 1 12 36c-3 2-4 5-4 9H52c0-4-1-7-4-9a20 20 0 0 1 12-36z"/><path d="M52 60h16M55 66h10" opacity=".8"/></svg>'},
  {t:'Где показывать кейсы', pts:['В первом холодном сообщении — как аргумент', 'На лендинге и в закрепе профиля — как витрина', 'В боте-прогреве — как касание, снимающее возражение', 'Обновляй кейсы: свежий результат сильнее старого'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 35s18-22 48-22 48 22 48 22-18 22-48 22S12 35 12 35z"/><circle cx="60" cy="35" r="10"/></svg>'}
  ],
  quiz:[
  {q:'Почему кейс сильнее слов «я профессионал»?', o:['Он длиннее', 'Показывает результат, а не обещание', 'Так принято', 'Кейсы дешевле'], a:1},
  {q:'Что обязательно в сильном кейсе?', o:['Только красивая картинка', 'Точка А, что сделал, точка Б с цифрами', 'Список нейросетей', 'Длинное вступление'], a:1},
  {q:'Что убедительнее в кейсе?', o:['«Стало намного лучше»', 'Конкретные цифры результата', 'Много прилагательных', 'Обещание на будущее'], a:1},
  {q:'Что делать, если заказов ещё не было?', o:['Ждать первого клиента', 'Сделать кейс на себе или бесплатно', 'Придумать фейк', 'Отказаться от кейсов'], a:1},
  {q:'Как лучше подать результат визуально?', o:['Абзац текста', 'Экран «было/стало» рядом', 'Только цифру без контекста', 'Ссылку на нейросеть'], a:1}
  ],
  pairs:[
  ['Точка А', 'Что было и в чём боль клиента'],
  ['Точка Б', 'Что стало, с конкретными цифрами'],
  ['Цифры', 'Охваты, заявки, продажи, сэкономленное время'],
  ['Тестовый кейс', 'Работа на себе или бесплатно для старта'],
  ['Витрина кейсов', 'Лендинг, закреп профиля, бот-прогрев']
  ],
  task:{
    intro:'Оформи один свой кейс по схеме до/после: точка А, что сделал, точка Б с цифрами и вывод.',
    chips:['Клиент/проект: ___', 'Точка А: ___', 'Что сделал: ___', 'Точка Б (цифры): ___', 'Срок: ___', 'Отзыв: ___'],
    ph:'Проект: профиль знакомого мастера маникюра. Точка А: 2 поста за 4 месяца, охваты ~150. Сделал: 12 Reels за неделю под её услуги. Точка Б: охваты выросли до 8500, пришло 6 заявок. Срок: 7 дней. Отзыв: «наконец-то профиль ожил».',
    verdict:'Отличный кейс — есть измеримая точка Б (8500 охватов, 6 заявок) и короткий срок, это продаёт само по себе. Совет: добавь скриншот статистики «до» и «после» — визуальное доказательство снимает последние сомнения даже у недоверчивых клиентов.'
  }
},
{
  title:'Масштаб и конвейер',
  sub:'4:10 · слайды + тест + игра', dur:'4:10',
  c1:'МАСШТАБ И', c2:'КОНВЕЙЕР',
  slides:[
  {t:'Потолок ручной работы', pts:['В одиночку ты упираешься в 24 часа и выгорание', 'Больше заказов руками — больше усталости, а не денег', 'Масштаб — это система, а не «работать ещё усерднее»', 'Формула роста: <b>конвейер</b> + шаблоны + команда + агенты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M38 10h44M38 60h44"/><path d="M42 10c0 16 18 20 18 25 0-5 18-9 18-25M42 60c0-16 18-20 18-25 0 5 18 9 18 25"/></svg>'},
  {t:'Шаблоны убирают рутину', pts:['Каждый новый заказ с нуля — потеря времени', 'Шаблоны сценариев, промптов, монтажа, писем клиентам', 'Собери банк <b>промптов</b> для <b>ChatGPT</b> и Midjourney под задачи', 'Шаблон превращает 3 часа работы в 30 минут сборки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 20 30l40 20 40-20z"/><path d="M20 42l40 20 40-20" opacity=".7"/></svg>'},
  {t:'Конвейер: разбей на этапы', pts:['Не «сделать ролик», а сценарий → визуал → озвучка → монтаж', 'Каждый этап отлажен и предсказуем по времени', 'Стандарт качества на каждом шаге — меньше правок в конце', 'Конвейер позволяет вести 10 заказов параллельно без хаоса'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Агенты и автоматизация', pts:['Рутину отдаём ИИ: генерация, черновики, ответы, постинг', 'Связки на <b>n8n</b> или <b>Make</b> делают работу по расписанию', '<b>Claude Code</b> и агенты берут повторяемые технические задачи', 'Ты остаёшься на смысле и контроле, машина — на конвейере'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="24" width="24" height="22" rx="5"/><rect x="80" y="24" width="24" height="22" rx="5"/><path d="M40 35h40"/><path d="M72 29l8 6-8 6" /></svg>'},
  {t:'Команда: делегируй слабые звенья', pts:['Первым нанимай на то, что забирает время и не растит тебя', 'Монтажёр, ассистент на касания, копирайтер на тексты', 'Дай человеку шаблон и стандарт — не учи с нуля каждый раз', 'Ты — на продажах и качестве, команда — на производстве'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="26" r="9"/><path d="M20 54c1-11 7-16 14-16s13 5 14 16"/><circle cx="80" cy="26" r="9"/><path d="M66 54c1-11 7-16 14-16s13 5 14 16"/></svg>'},
  {t:'Поток заказов вместо гонки', pts:['Цель масштаба — стабильный поток, а не разовые рывки', 'Считай пропускную способность конвейера в заказах в неделю', 'Узкое место расширяй первым: шаблоном, агентом или человеком', 'Система работает, даже когда ты в отпуске — вот это и есть масштаб'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58V16M18 58h84"/><path d="M30 50l18-16 14 10 22-26" /><circle cx="48" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="62" cy="44" r="3" fill="currentColor" stroke="none"/></svg>'}
  ],
  quiz:[
  {q:'Из чего складывается масштаб по формуле урока?', o:['Работать усерднее', 'Конвейер + шаблоны + команда + агенты', 'Больше рекламы', 'Поднять цены'], a:1},
  {q:'Зачем нужны шаблоны?', o:['Для красоты', 'Убрать рутину и ускорить сборку заказа', 'Чтобы усложнить процесс', 'Так требует клиент'], a:1},
  {q:'Что значит разбить работу на конвейер?', o:['Делать всё сразу', 'Разбить на отлаженные этапы', 'Работать по ночам', 'Отказаться от части заказов'], a:1},
  {q:'Что отдавать агентам и автоматизации в первую очередь?', o:['Продажи и стратегию', 'Повторяемую рутину', 'Общение с клиентом', 'Ценообразование'], a:1},
  {q:'На что нанимать первого человека?', o:['На то, что тебе нравится делать', 'На звено, что забирает время и не растит тебя', 'На переговоры о цене', 'Ни на что'], a:1}
  ],
  pairs:[
  ['Шаблон', 'Готовая заготовка, что убирает рутину'],
  ['Конвейер', 'Работа, разбитая на отлаженные этапы'],
  ['Агент', 'ИИ, берущий повторяемые задачи'],
  ['Делегирование', 'Отдать слабое звено человеку'],
  ['Узкое место', 'Этап, что ограничивает поток заказов']
  ],
  task:{
    intro:'Разложи производство своей услуги на конвейер из этапов и отметь, что можно отдать шаблону, агенту или человеку.',
    chips:['Этап 1: ___', 'Этап 2: ___', 'Этап 3: ___', 'Что в шаблон: ___', 'Что агенту: ___', 'Что человеку: ___'],
    ph:'Этапы Reels: сценарий → визуал → озвучка → монтаж → постинг. В шаблон: структура сценария и промпты. Агенту: озвучка и автопостинг через n8n. Человеку: монтаж отдаю фрилансеру по стандарту. Пропускная способность: 30 роликов/неделю.',
    verdict:'Сильно — ты не просто «делаю ролики», а видишь конвейер и где у него узкие места. Совет: замерь, какой этап сейчас самый медленный, и расширь именно его первым — обычно это монтаж, и один обученный по шаблону монтажёр удваивает пропускную способность.'
  }
}
  ]
});

/* ===== module: academy-enrich ===== */
/* ================= ОБОГАЩЕНИЕ УРОКОВ АКАДЕМИИ =================
   window.AC_ENRICH[gi] = { intro, notes:[{h,body}], lifehack, links:[{label,url,note}] }
   gi — глобальный индекс урока (== acL в плоском AC_COURSE).
   Данные — реальная польза по теме урока, без повторов, живым языком (школа ХЛБ, Даниэль).
   Пустые уроки просто не показывают блоки обогащения (грациозная деградация). */
window.AC_ENRICH = Object.assign(window.AC_ENRICH || {}, {
  0: {
    intro: 'За этот урок ты перестанешь «готовиться вести блог» и начнёшь его вести — как полигон, который с первого дня приносит данные, а скоро и деньги.',
    notes: [
      { h: 'Блог — это лаборатория, а не витрина', body: 'Работает цикл: гипотеза → пост → цифры → вывод. Провалившийся ролик — не поражение, а данные о том, что твоей аудитории не зашло. Смотри на реакцию людей (досмотры, сохранения, заявки), а не на «нравится мне самому». Формат, который тебе кажется скучным, часто и есть рабочий.' },
      { h: 'Стадии обучения проходят все', body: 'Энтузиазм → «дайте новое» → «все виноваты» → усталость и плато. Спад мотивации не значит, что ты не тянешь — это этап, а не приговор. 80% бросают именно на плато, где рост замедлился. Знаешь стадии наперёд — не пугаешься, когда накрывает, и просто продолжаешь по графику.' },
      { h: 'Цель — это число и срок, а не мечта', body: '«Хочу больше денег» мозг не может исполнить. «100 000 ₽ с блога к 1 октября» — может, потому что видит шаги. Пропиши целевые цифры: подписчики, охваты, заявки, доход. Разбей большую цель на недели с датами — так она перестаёт пугать.' },
      { h: 'Количество бьёт идеал', body: 'Кринж-этап проходят все: первые 20–30 роликов — это тренировка речи, кадра и монтажа, а не портфолио. Идеального старта не существует, качество приходит с объёмом. Опубликованное «сойдёт» всегда побеждает идеальное «в черновиках».' }
    ],
    lifehack: 'Заведи «полку кринжа»: первые 20–30 роликов складывай в отдельную папку и НЕ удаляй. Через месяц пересмотришь — увидишь рост в кадре, и страх камеры уйдёт сам. Он лечится количеством, а не ожиданием вдохновения.',
    links: [
      { label: 'CapCut', url: 'capcut.com', note: 'Бесплатный монтаж вертикалок с шаблонами и авто-субтитрами — снимать можно с первого дня' },
      { label: 'Notion', url: 'notion.so', note: 'Собери контент-план и трекер целей: гипотезы, даты, цифры в одном месте' }
    ]
  },
  1: {
    intro: 'Прежде чем брать приёмы из курса, определи, кто ты сейчас — новичок, практик или бизнес. От этого зависит, что тебе делать в первую очередь, а что пока не трогать.',
    notes: [
      { h: 'Три старта — три разных фокуса', body: 'Новичок: аккаунта нет, задача — первые 10 000 ₽ и первый клиент, а не 100к подписчиков. Практик: блог есть, но хаотичный — задача навести упаковку и контент-систему. Бизнес: аккаунт как воронка к заявке, метрика — стоимость заявки и выручка, а не лайки.' },
      { h: 'Главный враг новичка — перфекционизм', body: 'Нет аудитории, насмотренности и есть страх камеры — это норма старта. Первая цель узкая и денежная, а не про большие числа. Снимаешь на потоке: плохо, но каждый день. Друг новичка — количество, враг — желание сделать идеально с первого раза.' },
      { h: 'Практика тормозит не незнание, а хаос', body: 'Обычно у практика уже достаточно знаний — не хватает структуры: шапка не продаёт, рубрики размыты, охваты скачут. Быстрый рост даёт не новая платформа, а фокус на 1–2 рабочих формата и порядок в упаковке.' },
      { h: 'Тип может меняться', body: 'Дорос с нуля до практика — сменил фокус с «просто снимать» на «навести систему». Определив тип, ты берёшь из курса только то, что двигает тебя сейчас, и не тратишь недели на чужие задачи.' }
    ],
    lifehack: 'Напиши одно предложение: «Я [тип], моя ближайшая цель — [число] к [дате]». Повесь на видное место. Каждый новый приём из курса примеряй к этой строчке: двигает — берёшь, не двигает сейчас — откладываешь.',
    links: [
      { label: 'Яндекс Wordstat', url: 'wordstat.yandex.ru', note: 'Проверь реальный спрос в своей нише — темы для контента берутся из того, что ищут люди' },
      { label: 'Canva', url: 'canva.com', note: 'Собрать продающую шапку и оформление профиля без дизайнера' }
    ]
  },
  2: {
    intro: 'Твой собственный аккаунт — бесплатный испытательный стенд. Тут ты ошибаешься без риска, набиваешь навык и получаешь свой первый кейс, который продаёт тебя дороже любого диплома.',
    notes: [
      { h: 'Теория без практики не запоминается', body: 'Знание про крючок в ролике ничего не стоит, пока не снял пять своих. Мозг запоминает не лекцию, а собственную набитую шишку. Практик с 30 роликами обгонит теоретика с 30 конспектами — потому что видит реальные цифры: досмотры, охваты, заявки.' },
      { h: 'Что именно тестировать', body: 'Разные крючки первых 3 секунд — что удерживает досмотр. Форматы: говорящая голова, экспертная нарезка, история клиента. Время и частоту выхода — когда твоя аудитория живее. Обложки и подписи — что растит сохранения.' },
      { h: 'Полигон продаёт за тебя', body: 'Когда сам вырастил охваты — тебе верят без слов. Скрин своей статистики сильнее любого сертификата. Клиент покупает результат, который видит на твоём блоге, и первые заявки часто приходят прямо оттуда.' },
      { h: 'Не жди идеала — публикуй сырое', body: '«Начну вести, когда разберусь» — так не начинают никогда. Полигон нужен грязный и рабочий, а не вылизанный и пустой. Публикуй, правь по ходу — метрики сами покажут направление. Каждый день без полигона — минус данные о себе.' }
    ],
    lifehack: 'Раз в неделю делай скрин статистики лучшего поста и складывай в папку «Мои результаты». Через 2–3 месяца это готовое портфолио: показываешь клиенту рост в цифрах, а не рассказываешь на словах.',
    links: [
      { label: 'Metricool', url: 'metricool.com', note: 'Бесплатная аналитика соцсетей: досмотры, лучшее время выхода, динамика по неделям' },
      { label: 'CapCut', url: 'capcut.com', note: 'Быстро пересобрать крючок и обложку между тестами форматов' }
    ]
  },
  3: {
    intro: 'Эмоции в обучении предсказуемы: ты проживёшь четыре стадии. Если знать их заранее — не бросишь на самой тяжёлой, где сходит большинство.',
    notes: [
      { h: 'Стадия 1 — энтузиазм', body: 'Горят глаза, готов снимать по 5 роликов в день. Опасность — сжечь всё топливо на всплеске и выгореть. Задача стадии — не жечь, а заложить график. Энтузиазм не топливо на дистанцию, топливо — система.' },
      { h: 'Стадия 2 — «дайте новое»', body: 'База освоена, старое кажется скучным, тянет за «фишками». Ловушка — гоняться за новыми приёмами вместо повторения рабочих. Рост даёт не новизна, а доведение базовых форматов до автомата. Здесь отваливаются те, кто путает скуку с потолком.' },
      { h: 'Стадия 3 — «все виноваты»', body: 'Результат не летит, и мозг ищет виноватых: алгоритм, ниша, время. Внешние причины комфортны, но не двигают ни на шаг. Это сигнал вернуться к дисциплине и цифрам, а не искать врага. Выход — снять следующий ролик.' },
      { h: 'Стадия 4 — усталость и плато', body: 'Рост замедлился, эмоций нет, кажется, что топчешься. Плато — это накопление перед скачком, а не конец. Спасает не мотивация, а график и микроцели на неделю. Кто проходит плато на дисциплине — выходит на новый уровень.' }
    ],
    lifehack: 'Когда накрывает — вслух назови стадию: «я на этапе всё виноваты». Простое называние снимает половину тяжести и возвращает контроль: ты не сломался, ты на понятном участке пути, который проходят все.',
    links: [
      { label: 'Notion', url: 'notion.so', note: 'Заведи трекер серии дней в графике — визуальная цепочка держит на плато' },
      { label: 'Telegram', url: 't.me', note: 'Найди чат по нише: в среде проходить спад в разы легче, чем в одиночку' }
    ]
  },
  4: {
    intro: 'Первые ролики будут вызывать стыд — это универсальный опыт, через него прошли все, включая миллионников. Кринж лечится только одним способом: пройти его насквозь, снимая.',
    notes: [
      { h: 'Качество приходит с количеством', body: 'Первые 20–30 роликов — тренировка, а не портфолио. Каждый прокачивает речь, кадр и монтаж на процент. Не жди шедевра на пятом видео — жди роста к тридцатому. Количество набивает навык быстрее любого разбора теории.' },
      { h: 'Что именно тренируется', body: 'Речь: уходит зажатость, появляются темп и крючок. Кадр: чувствуешь свет, ракурс и фон без лишних движений. Монтаж: ритм, склейки и субтитры входят в привычку. И насмотренность на себе — видишь свои ошибки и правишь их.' },
      { h: 'Как ускорить выход из кринжа', body: 'Снимай сериями, а не по ролику в неделю. Пересматривай без самобичевания — ищи одну правку на ролик. Не удаляй старое: сравнение первого и тридцатого мотивирует. Публикуй сразу — папка «на потом» убивает прогресс.' },
      { h: 'Кринж — это фильтр', body: 'Большинство бросает на первых неловких видео и уступает место тебе. Кто прошёл 30 роликов — входит в меньшинство, у которого получается. Твоё преимущество не талант, а то, что ты не остановился.' }
    ],
    lifehack: 'Снимай батчем: за один заход в одной одежде запиши сразу 5 роликов. Так ты проходишь «разогрев» один раз, экономишь силу воли и быстрее выходишь из кринжа, чем растягивая по одному видео на неделю.',
    links: [
      { label: 'CapCut', url: 'capcut.com', note: 'Монтаж, авто-субтитры и тренды — превратить сырой дубль в чистый ролик за минуты' },
      { label: 'InShot', url: 'inshot.com', note: 'Простой мобильный монтаж для быстрых серий прямо с телефона' }
    ]
  },
  5: {
    intro: 'Размытое «хочу больше» не двигает и не проверяется. В этом уроке ты превратишь мечту в цель, которую мозг реально может исполнить — с числом, сроком и метрикой.',
    notes: [
      { h: 'Формула рабочей цели', body: 'Число + срок + метрика: что, к когда, чем измеряем. Плохо: «больше клиентов». Хорошо: «5 клиентов по 20к до 30 сентября». Цель амбициозная, но достижимая за срок. Одна главная цель важнее пяти размытых.' },
      { h: 'Пропиши жизнь при цели', body: 'Опиши свой день, когда цель достигнута: доход, свобода, статус, ощущения. Яркая картинка тянет к действию сильнее сухой цифры. Этот образ — твоё топливо на плато и в усталость, когда мотивация на нуле.' },
      { h: 'Декомпозиция на недели', body: 'Годовую цель режь на месяцы, месяц — на недели. 100к за 3 месяца → ~33к в месяц → понятные шаги на неделю. Маленькие сроки не дают откладывать на «потом». Каждую неделю сверяйся: идёшь в график или отстаёшь.' },
      { h: 'Цель — на видном месте', body: 'То, что не видишь каждый день, забывается за неделю. Держи число и срок перед глазами: заставка телефона, стикер, доска. Каждое решение сверяй с целью: этот ролик приближает к ней? Достиг — ставь следующую, не зависай.' }
    ],
    lifehack: 'Поставь целью число и срок прямо на заставку телефона (обои с текстом делаются в Canva за минуту). Ты смотришь в экран 100+ раз в день — цель будет напоминать о себе без всяких приложений и планировщиков.',
    links: [
      { label: 'Canva', url: 'canva.com', note: 'Сделать обои-заставку с целью и сроком за пару минут' },
      { label: 'Trello', url: 'trello.com', note: 'Разложить цель на недельные шаги-карточки и двигать их по мере выполнения' }
    ]
  },
  6: {
    intro: 'Ждать вдохновения — значит снимать 4 ролика в месяц вместо 20. Этот урок про то, как перестать зависеть от настроения и поставить результат на систему.',
    notes: [
      { h: 'График сильнее мотивации', body: 'Настроение скачет каждый день, результат требует ровного темпа. График убирает ежедневный вопрос «а хочу ли я сегодня»: решение принято один раз, дальше просто исполняешь. Фиксированные дни и время съёмки экономят силу воли.' },
      { h: 'Как построить рабочий график', body: 'Назначь конкретные дни и часы под съёмку и монтаж. Ставь реальную норму: 5 роликов в неделю, а не 30. Используй батчинг — сними 5 за один заход, публикуй по дням. Защити эти слоты в календаре как встречи, которые нельзя отменить.' },
      { h: 'Дни, когда не хочется', body: 'Правило минимума: в плохой день сними хотя бы один короткий ролик. Действие рождает настроение, а не наоборот. Снизь планку качества, но не пропускай день целиком. Один ролик в спад ценнее пяти в идеальный день, которого может не быть.' },
      { h: 'Дисциплина — это навык', body: 'Она не врождённая, а тренируется повторением. Каждое выполненное обещание себе усиливает следующее. Считай серию дней в графике и не рви цепочку. Через месяц система станет привычкой, и воля почти не понадобится.' }
    ],
    lifehack: 'Правило «не рвать цепочку»: отмечай каждый день, когда сделал норму, галочкой в календаре. Через две недели непрерывная цепочка сама становится мотиватором — тебе будет жалко её прервать, и это сильнее любого вдохновения.',
    links: [
      { label: 'Google Календарь', url: 'calendar.google.com', note: 'Забронируй повторяющиеся слоты «съёмка/монтаж» как неотменяемые встречи' },
      { label: 'Notion', url: 'notion.so', note: 'Трекер привычки со счётчиком серии дней в графике' }
    ]
  },
  7: {
    intro: 'Ты — среднее своего окружения и своей насмотренности. Смени и то и другое осознанно — и планка того, что для тебя «нормально», поднимется сама.',
    notes: [
      { h: 'Окружение задаёт планку', body: 'Люди вокруг незаметно определяют твою норму. В среде, где все растут, расти становится естественно. Скептики и «зачем тебе это» тянут вниз без злого умысла. Найди чаты, мастермайнды, коллег по нише на одном пути — в среде бросить стыдно, а в одиночку легко.' },
      { h: 'Насмотренность — топливо для контента', body: 'Это то, сколько сильного контента ты осознанно разобрал. Кто изучает топовые reels, снимает лучше интуитивно — мозг собирает банк приёмов: крючки, монтаж, подачи. Без насмотренности изобретаешь велосипед и стоишь на месте.' },
      { h: 'Как смотреть с пользой', body: 'Смотри не как зритель, а как разбирающий: почему зацепило. Разложи крючок, ритм, склейки, финал — что удержало досмотр. Заведи папку сохранённых сильных роликов-референсов. 10 минут осознанного разбора ценнее часа бездумной ленты.' },
      { h: 'Чисти вредное окружение', body: 'Отпишись от аккаунтов, что вызывают зависть, а не идеи. Убери из ленты тех, кто гасит инициативу. Меньше пассивного скролла — больше целевого разбора. Твоё внимание — ресурс, вкладывай его в рост, а не в шум.' }
    ],
    lifehack: 'Собери папку из 20–30 «эталонных» роликов в своей нише и пересматривай перед каждой съёмкой 5 минут. Мозг настроится на сильный уровень, и твой ролик выйдет качественнее — без единого нового приёма, просто на насмотренности.',
    links: [
      { label: 'Telegram', url: 't.me', note: 'Мастермайнды и ниша-чаты: окружение, которое держит темп и не даёт бросить' },
      { label: 'Pinterest', url: 'pinterest.com', note: 'Банк визуальных референсов: обложки, раскадровки, идеи под свой стиль' }
    ]
  },
  8: {
    intro: 'Финал блока — план на 10 пунктов, который свяжет всё вместе. Именно план превращает знания курса в шаги, а без него мотивация после урока испаряется за пару дней.',
    notes: [
      { h: 'Что обязательно в плане', body: 'Твой тип участника и главная цель в цифрах и сроке. Целевые цифры: доход, число роликов, охваты, заявки. Образ жизни при достижении цели — зачем всё это. И конкретные действия на неделю, а не расплывчатые мечты.' },
      { h: 'Каркас 10 пунктов', body: '1–2: цель в цифрах и срок; 3: образ жизни при цели. 4–5: съёмочный график и норма роликов в неделю. 6–7: форматы для полигона и метрики проверки. 8–10: насмотренность, окружение и правило минимума на спад.' },
      { h: 'Правила сильного плана', body: 'Каждый пункт измерим — есть число или чёткое действие. Сроки конкретные: дата, а не «скоро». План на виду: перечитывай в начале каждой недели. Это живой документ — корректируй по цифрам, но не бросай.' },
      { h: 'Первый шаг — сегодня', body: 'План без действия сегодня — просто текст. Выбери один пункт и начни его в ближайшие 24 часа: сними первый ролик-полигон, не откладывая на понедельник. Импульс от старта тянет весь план за собой. Раз в неделю сверяй факт с планом.' }
    ],
    lifehack: 'Сделай план по правилу «одна страница»: если он не влезает на один экран — он не рабочий, а список мечтаний. Коротко, измеримо, с датами. Такой план ты реально перечитываешь каждую неделю, а не прячешь в заметках навсегда.',
    links: [
      { label: 'Notion', url: 'notion.so', note: 'Собрать план на 10 пунктов с чек-боксами и недельной сверкой' },
      { label: 'Gamma', url: 'gamma.app', note: 'Красиво оформить план/стратегию блога, если хочется наглядности' }
    ]
  },
  9: {
    intro: 'Упаковка профиля решает, останется человек или уйдёт, за 3 секунды — ещё до первого поста. В этом уроке соберём шапку, которая работает на тебя, а не просто «рассказывает о себе».',
    notes: [
      { h: 'Цель шапки зависит от типа блога', body: 'Коммерческий — продать: шапка ведёт к покупке. Экспертный — вызвать доверие: доказывает компетентность. Лайфстайл — влюбить в личность. Сначала определи тип, потом собирай элементы под эту цель, а не наоборот.' },
      { h: 'Пять элементов первого экрана', body: 'Ник — как тебя ищут. Имя-строка — жирные 30 знаков, которые индексирует поиск. Описание — до 150 знаков, где работает каждое слово. Аватар — лицо блога. Мультиссылка — куда ведёшь тёплый трафик. Каждый элемент это инструмент, а не украшение.' },
      { h: 'Фильтр «а мне зачем»', body: 'К каждой строке шапки задай вопрос читателя: «а мне это зачем?». Не отвечает на него — вырезай без жалости. Хорошая шапка это выгода читателя, а не резюме автора. «Делаю мир лучше» и «люблю жизнь» — пустые шаблоны, под которые подходит кто угодно.' }
    ],
    lifehack: 'Дай прочитать свою шапку человеку не из твоей ниши и спроси: «Понял за 5 секунд, чем я полезен и кому?». Если пересказал своими словами — шапка работает. Замялся — переписывай, дело не в нём, а в упаковке.',
    links: [
      { label: 'Taplink', url: 'taplink.ru', note: 'Собрать мультиссылку-меню вместо одной ссылки — под каждый сегмент свой раздел' },
      { label: 'Canva', url: 'canva.com', note: 'Оформить единый визуал шапки и обложек в цветах бренда' }
    ]
  },
  10: {
    intro: 'Мозг оценивает профиль за 3 секунды — быстрее, чем ты дочитаешь эту строку. Шапка это не «инфа обо мне», а оффер, который надо успеть донести за это касание.',
    notes: [
      { h: 'Куда идёт взгляд', body: 'Сверху вниз: сначала аватар, потом имя-строка, потом первая строка описания. Хвосты текста под кнопкой «ещё» почти не читают. Витрина вечных историй считывается последней, но добивает решение. Значит, главное — в первые строки, до сгиба.' },
      { h: 'Оффер, а не биография', body: 'В первые 3 секунды человек должен понять: кому ты полезен и что получит. Не «маркетолог, мама, путешественник», а «упакую твой блог в продающий за 2 недели». Конкретный оффер цепляет, размытое перечисление ролей — пролистывают.' },
      { h: 'Каждая деталь — микро-инструмент', body: 'Ник, имя-строка, аватар, описание, мультиссылка — пять инструментов, а не украшения. Пустая или шаблонная деталь = слитые 3 секунды и потерянный подписчик. Слабых звеньев быть не должно: цепочку рвёт самый слабый элемент.' }
    ],
    lifehack: 'Сделай тест «прищур»: открой свой профиль и прищурься, чтобы текст расплылся. Что осталось читаемым и цепляет даже размытым — то и работает за 3 секунды. Всё, что теряется, — не успевает сработать в реальной ленте.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Сформулировать оффер шапки в одну сильную строку из 5 черновых вариантов' },
      { label: 'Canva', url: 'canva.com', note: 'Быстро собрать и сравнить несколько версий первого экрана' }
    ]
  },
  11: {
    intro: 'Ник — это адрес, а не украшение: его диктуют голосом и вбивают руками. Если он не пишется с первого раза, ты теряешь переходы «по сарафану», даже не зная об этом.',
    notes: [
      { h: 'Две рабочие модели', body: 'Имя + фамилия для личного бренда: anna.petrova. Имя + тематика: petrova.marketing, dr.smirnov. Чистая тематика без имени — только для брендов, а не личных блогов. Мусор из точек, подчёркиваний и года рождения (_x_, 2007) убивает запоминаемость.' },
      { h: 'Читаемость с первого раза', body: 'Только то, что легко продиктовать по телефону. Латиница без ловушек транслита: ж, щ, ы, й читаются по-разному, и человек ошибётся. Без двойных точек и случайных цифр. Плохой ник = потерянные прямые заходы.' },
      { h: 'Ник живёт годами', body: 'Смена ника = потеря наработанной узнаваемости и части поисковых заходов. Не привязывай его к тренду, который завтра умрёт. Выбирай с прицелом на 5 лет вперёд, а не под сиюминутную тему.' }
    ],
    lifehack: 'Проверка вслух: продиктуй ник другу по телефону и попроси написать. Напечатал с первого раза без переспросов — ник рабочий. Пришлось диктовать по буквам — упрощай, пока не станет очевидным на слух.',
    links: [
      { label: 'Namecheckr', url: 'namecheckr.com', note: 'Проверить, свободен ли ник разом во всех соцсетях — занять одинаковый везде' }
    ]
  },
  12: {
    intro: 'В ленте и поиске аватар — это кружок размером с ноготь. Логотипы, текст и мелкие детали на нём не читаются: работает только крупный план лица.',
    notes: [
      { h: 'Формула сильного аватара', body: 'Крупный план: лицо занимает большую часть кадра. Однотонный фон — контраст с лицом, без каши. Открытый взгляд в камеру плюс лёгкая эмоция — это контакт с человеком по ту сторону экрана. Логотип — только для бренда и только крупный, читаемый в кружке.' },
      { h: 'Свет и качество', body: 'Дневной мягкий свет у окна или софтбокс, без жёлтых теней. Резкое фото без шума — портретный режим телефона годится. Одежда и фон в тон бренду, а не «что было под рукой». Плохой свет за секунду считывается как «непрофессионально».' },
      { h: 'Стабильность узнавания', body: 'Меняй аватар не чаще 1–2 раз в год: тебя узнают по нему в ленте. Частая смена = аудитория заново «знакомится» и путается. Один и тот же кадр ставь на все площадки — единое лицо бренда везде.' }
    ],
    lifehack: 'Проверь аватар «в реальном размере»: уменьши фото до кружка 2 см на экране телефона. Если лицо и эмоция читаются даже такими мелкими — аватар рабочий. Пропали в кашу — переснимай крупнее и на чистом фоне.',
    links: [
      { label: 'Remove.bg', url: 'remove.bg', note: 'Вырезать фон и заменить на чистый однотонный за пару секунд' },
      { label: 'PhotoRoom', url: 'photoroom.com', note: 'Подчистить портрет, фон и свет прямо с телефона' }
    ]
  },
  13: {
    intro: 'Имя-строка — единственное поле профиля, которое индексирует поиск платформы. Ник ищут те, кто тебя знает; имя-строку — те, кто ищет твою тему и ещё не знает тебя.',
    notes: [
      { h: 'Не дублируй ник', body: 'Повтор ника в имени-строке — это слитые 30 знаков. Ставь имя + ключевой запрос: «Анна · таргетолог». Ключ — то, что клиент реально вбивает в поиск: «психолог», «маникюр», «SMM». Абстракции «создаю красоту» бесполезны — по ним не ищут.' },
      { h: 'Как найти ключевые слова', body: 'Подумай, каким словом тебя ищет клиент, и проверь частотность в Wordstat — бери то, что реально спрашивают. Гео-привязка ловит локальный трафик: «Ногтевой сервис · Пермь». Формула: имя + ниша или ниша + гео.' },
      { h: 'Каждый из 30 знаков работает', body: 'Не забивай строку эмодзи и «звёздочками» — они крадут символы у ключей. Разделители-точки и слэши делают строку читаемой. Задача поля одна — находимость, поэтому всё, что не помогает найти тебя, лишнее.' }
    ],
    lifehack: 'Вбей свой ключевой запрос в поиск платформы и посмотри, кто вылезает в топе. Если тебя там нет, а конкуренты есть — сравни их имена-строки и поставь в свою тот же рабочий ключ. Находимость часто чинится одной правильной строкой.',
    links: [
      { label: 'Яндекс Wordstat', url: 'wordstat.yandex.ru', note: 'Частотность запросов — какой ключ реально ищут в твоей нише и гео' },
      { label: 'Google Trends', url: 'trends.google.com', note: 'Сравнить популярность формулировок и поймать сезонный спрос' }
    ]
  },
  14: {
    intro: 'Описание — это 150 знаков лифт-презентации блога. Не дневник, а структура, где каждая строка отвечает на вопрос «а мне зачем на тебя подписываться».',
    notes: [
      { h: 'Формула для эксперта', body: 'Кто ты → чем интересен → факты-цифры → выгода → призыв. Цифры дают доверие за секунду: «7 лет / 300 клиентов / −20 кг». Заканчивай конкретным действием: «Забери гайд ниже», а не точкой. Абстрактное «качественно и с любовью» ничего не значит.' },
      { h: 'Формула для коммерции', body: 'Продукт → выгода → уникальность → призыв. Уникальность — чем ты не как все: «доставка за 2 часа», «первый замер бесплатно». Именно она отделяет тебя от десятка похожих. Без неё описание сливается с конкурентами.' },
      { h: 'Цифры вместо прилагательных', body: 'Прилагательные не запоминаются, факты — да. «Собрал 40 воронок, средний +35% заявок» бьёт любое «делаю с душой». Набросай смысл, потом сожми в 150 знаков и проверь с телефона: помещается ли и читается ли без обрезки.' }
    ],
    lifehack: 'Напиши описание как обычно, а потом попроси ИИ сжать его до 150 знаков, оставив только цифры, выгоду и призыв. Ограничение по символам заставляет выкинуть воду — остаётся концентрат, который реально продаёт подписку.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Сжать описание в 150 знаков и выдать 5 вариантов с призывом' },
      { label: 'Claude', url: 'claude.ai', note: 'Переписать био живым языком под конкретный тип блога' }
    ]
  },
  15: {
    intro: 'Профиль даёт одну кликабельную ссылку — это узкое горлышко всего твоего трафика. Голая ссылка на сайт теряет тех, кому нужен другой раздел. Решение — мультиссылка: меню вместо одной двери.',
    notes: [
      { h: 'Из чего собрать меню', body: 'О себе, Услуги и прайс, Отзывы и кейсы, Примеры работ, Как заказать, Акции. Каждый пункт — под свой сегмент аудитории. Не вали 15 кнопок: 4–6 сильных работают лучше свалки, в которой глаз теряется.' },
      { h: 'Порядок кнопок = путь клиента', body: 'Сверху — то, что ведёт к деньгам: услуги, заказ. Ниже — прогрев для сомневающихся: отзывы и кейсы. Раздели трафик по целям: холодным показывай кейсы, тёплым — кнопку заказа. Порядок это не эстетика, а воронка.' },
      { h: 'Кликабельность решает', body: 'Контакты открываются в один тап, а не копируются руками — каждый лишний шаг теряет часть тёплых лидов. Ссылка на прямой чат или бота ловит клиента сразу. Проверь каждую кнопку с телефона: мёртвая ссылка = слитый лид.' }
    ],
    lifehack: 'Заведи мультиссылку и в контенте всегда зови по конкретному разделу: «ссылка в шапке → раздел Услуги», а не просто «ссылка в шапке». Точный маршрут повышает клики в разы — человек не ищет, а идёт по указателю.',
    links: [
      { label: 'Taplink', url: 'taplink.ru', note: 'Собрать меню-мультиссылку с кнопками, прайсом и приёмом заявок' },
      { label: 'mssg.me', url: 'mssg.me', note: 'Лёгкая страница-визитка со ссылками на все мессенджеры в один тап' }
    ]
  },
  16: {
    intro: 'Вечные истории — это витрина магазина под шапкой, которая работает 24/7. Человек листает её и принимает решение, пока ты спишь — она отвечает на вопросы за тебя.',
    notes: [
      { h: 'Обязательные разделы витрины', body: 'О себе, Отзывы, Кейсы и результаты, Прайс и услуги, FAQ, Как купить. Каждый раздел закрывает одно возражение клиента. В отзывы клади скрины и видео живых клиентов, а не текст от себя. В кейсы — «было / стало» с цифрами.' },
      { h: 'Единый стиль обложек', body: 'Один стиль обложек = аккуратная, дорогая витрина. Один шрифт, цвета бренда, понятные подписи-иконки. Разнобой обложек читается как бардак и роняет доверие ещё до того, как человек откроет истории.' },
      { h: 'Порядок = логика прогрева', body: 'Слева — самое важное: О себе и Отзывы. Дальше по воронке: кейсы → прайс → как купить. Мёртвые истории из 2019-го убери — они старят блог. Обновляй раздел раз в 1–2 месяца, снимай устаревшее.' }
    ],
    lifehack: 'Собери «путь сомневающегося»: расставь вечные истории в том порядке, в котором клиент принимает решение — сначала «кто ты», потом «кому уже помог», потом «сколько стоит», в конце «как купить». Витрина сама доводит до заявки.',
    links: [
      { label: 'Canva', url: 'canva.com', note: 'Единый набор обложек для вечных историй в стиле бренда' },
      { label: 'Mojo', url: 'mojo-app.com', note: 'Анимированные сторис и обложки, чтобы витрина смотрелась живой' }
    ]
  },
  17: {
    intro: 'Собери типовые ошибки шапки в один чек-лист и пройдись по своей — большинство профилей сливают подписчиков на одних и тех же граблях. Починить их можно за один вечер.',
    notes: [
      { h: 'Шаблон вместо смысла', body: '«Помогаю женщинам быть счастливыми» — шаблон, под который подходит любой блог. Аудитория видела это тысячу раз, взгляд проскальзывает. Лечится конкретикой: кому именно, что именно и с каким результатом. Конкретное всегда цепляет сильнее общего.' },
      { h: 'Язык клиента, а не «чтобы звучало»', body: 'Английские слова и заумные термины в русском блоге — барьер: часть аудитории просто не понимает и уходит. Пиши на языке клиента, а не «чтобы выглядело солидно». Понятность продаёт лучше, чем солидность.' },
      { h: 'Контакты и телефон', body: 'Номер или ник текстом, который надо копировать руками, теряет часть тёплых клиентов на каждом лишнем шаге. Всё, что можно нажать, должно быть ссылкой в один тап. И проверяй шапку с телефона: 95% смотрят с него, а верстают на компьютере — отсюда обрезанные строки и мёртвые ссылки.' }
    ],
    lifehack: 'Раз в пару месяцев открывай свой профиль с чужого телефона глазами постороннего и читай вслух. Обрезанные строки, непонятные термины и некликабельные контакты вылезают сразу — то, что замылилось у тебя, чужой глаз ловит за секунды.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Переписать шаблонные строки шапки в конкретные и живые' },
      { label: 'Taplink', url: 'taplink.ru', note: 'Сделать все контакты кликабельными в один тап' }
    ]
  },
  18: {
    intro: '51% людей судят о блоге по картинке — визуал говорит раньше слов. В этом уроке собираем систему, которая делает ленту узнаваемой и без слов транслирует, что аккаунт ведут всерьёз.',
    notes: [
      { h: 'От смыслов к съёмке, а не наоборот', body: 'Шаг 1 — собери смыслы: о чём блог, какие ценности показываешь. Шаг 2 — мудборд: референсы, гамма, настроение. Шаг 3 — планирование: 12–15 фото вперёд, единая гамма, не больше двух шрифтов. Красивое фото без системы бьёт мимо.' },
      { h: 'Приёмы красивой ленты', body: 'Чередуй планы: общий → средний → крупный → сверхкрупный. Чередуй сюжеты — рядом не должны стоять два похожих кадра. Оставляй «воздух» — свободное пространство в кадре, не забивай всё до краёв. Максимум два шрифта: один акцентный, один текстовый.' },
      { h: 'Личность продаёт', body: '96% людей охотнее лайкают фото, где есть человек — покажи лицо. Блог без личности это безликая витрина, к ней не привязываются. Личные кадры, эмоции, закулисье — то, что отличает тебя от конкурентов и что невозможно скопировать.' }
    ],
    lifehack: 'Собери мудборд из 20–30 референсов ДО первой съёмки и выдели из них 2–3 базовых цвета. Дальше снимаешь по образцу, а не наугад — это экономит часы пересъёмок и с первого раза даёт ленте единый тон.',
    links: [
      { label: 'Pinterest', url: 'pinterest.com', note: 'Собрать мудборд: свет, ракурсы, гамма, настроение будущей ленты' },
      { label: 'Coolors', url: 'coolors.co', note: 'Подобрать палитру из 3–5 цветов, чтобы лента звучала в тон' }
    ]
  },
  19: {
    intro: 'Монтаж решает, досмотрят твой Reels или пролистают на второй секунде. Разберём ритм, субтитры и звук — то, что удерживает внимание кадр за кадром.',
    notes: [
      { h: 'Ритм и джамп-каты', body: 'Режь по музыке и речи — склейки на битах и словах. Удаляй «воздух»: вдохи, паузы, запинки. Средний план держи 1,5–2,5 секунды, никаких мёртвых кадров, где ничего не происходит. Zoom-панч на акценте добавляет энергии.' },
      { h: 'Караоке-субтитры обязательны', body: '90% смотрят без звука — без субтитров половина аудитории не поймёт ролик. Делай крупно, контрастно, читаемо на любом фоне. Подсветка по слову в бренд-цвете ведёт взгляд и удерживает досмотр до конца.' },
      { h: 'Звук под смысл', body: 'Музыка — под настроение сцены, а не просто фоном. SFX под каждое движение: свуши, клики, удары. Дакинг — приглушай музыку под голос, чтобы речь читалась. Единый пресет/LUT на весь ролик делает картинку «дорогой».' }
    ],
    lifehack: 'Правило «нет мёртвым кадрам»: пройди готовый ролик и вырежи каждый момент, где на экране ничего не меняется дольше секунды. Плотный ритм без провисаний — главная причина высокого досмотра, важнее спецэффектов.',
    links: [
      { label: 'CapCut', url: 'capcut.com', note: 'Монтаж по битам, авто-субтитры с подсветкой слова, тренды и пресеты' },
      { label: 'Mixkit', url: 'mixkit.co', note: 'Бесплатная музыка и звуковые эффекты под сцены и переходы' }
    ]
  },
  20: {
    intro: 'Человек решает «остаться или уйти» за 3 секунды и не по текстам — он сканирует картинку. Визуал ≠ дорого: нужен не фотограф за 30 000, а система из гаммы, планов и воздуха.',
    notes: [
      { h: 'Что на самом деле продаёт визуал', body: 'Внимание — останавливает палец в бесконечной ленте. Ценности — за секунду показывает, о чём ты и для кого. Отстройка — почему ты, а не сотня таких же в нише. Оценка «нравится / не нравится» идёт на эмоциях, до всякой логики.' },
      { h: 'Визуал поднимает цену', body: 'Два эксперта одного уровня: у кого лента чище — у того подписка и услуга дороже. Аккуратный визуал поднимает восприятие экспертности без единого слова о цене. Бренды охотнее размещаются у аккуратных блогов — для них это меньше риска.' },
      { h: 'Что делать уже сегодня', body: 'Открой профиль глазами чужого человека: остался бы ты сам? Проверь первый экран — аватар читается, шапка понятна за 2 секунды. Первый ряд из трёх фото — это твоя витрина, а не свалка случайного. Снимает iPhone 11–12+, обрабатывает бесплатный редактор.' }
    ],
    lifehack: 'Тест «первого ряда»: посмотри только на верхние 3 поста профиля, как новый человек. Если за 3 секунды не считывается, о чём блог и для кого — переверстай именно этот ряд. Он решает подписку раньше, чем человек долистает ниже.',
    links: [
      { label: 'Lightroom', url: 'lightroom.adobe.com', note: 'Бесплатная мобильная обработка и единый пресет на всю ленту' },
      { label: 'Midjourney', url: 'midjourney.com', note: 'Собрать референсы и раскадровку будущей сетки под свою гамму' }
    ]
  },
  21: {
    intro: 'Мозг научился отфильтровывать всё однообразное — это баннерная слепота. Если посты на одно лицо, палец листает на автопилоте, даже когда кадры красивые.',
    notes: [
      { h: 'Главный враг — однообразие', body: 'Десять постов одного шаблона подряд = мозг ставит метку «уже видел». Одинаковый ракурс, фон и тип обложки усыпляют внимание. Даже красивое, но повторяющееся, теряет силу к третьему разу. Слепоту вызывает не уродство, а предсказуемость.' },
      { h: 'Ломай шаблон, но не в хаос', body: 'Чередуй планы (общий/средний/крупный) и сюжет кадра: человек, предмет, деталь, пространство, текст. Раз в несколько постов — кадр-выброс: неожиданный ракурс или цвет. Но каркас держат 3 константы: гамма, ≤2 шрифта, единая обработка. Меняем содержание — оставляем атмосферу.' },
      { h: 'Тест на слепоту за 30 секунд', body: 'Уменьши сетку до превью и расфокусируй взгляд. Если посты сливаются в серое пятно — у тебя слепота. Если глаз цепляется за 2–3 точки и ведёт дальше — ритм живой. Это самый быстрый способ проверить ленту до выкладки.' }
    ],
    lifehack: 'Перед выкладкой смотри не на пост, а на всю сетку в планировщике и прищуривайся. Задача — чтобы взгляд «спотыкался» о яркие точки по диагонали, а не скользил по ровному ковру. Ритм важнее красоты каждого отдельного кадра.',
    links: [
      { label: 'Later', url: 'later.com', note: 'Визуальный планировщик сетки — увидеть однообразие до публикации' },
      { label: 'Savee', url: 'savee.it', note: 'Банк разнообразных референсов, чтобы ломать шаблон осознанно' }
    ]
  },
  22: {
    intro: 'Визуал начинается не с фото, а со смыслов. Пока не ясно, ЧТО ты показываешь и для кого, любая красивая картинка бьёт мимо. Собираем смыслы и мудборд.',
    notes: [
      { h: 'Как собрать смыслы блога', body: 'Выпиши 3–5 ключевых тем, вокруг которых крутится контент. Опиши целевого подписчика: возраст, вкус, что ему близко визуально. Найди 3 слова-настроения — например «тепло, честно, по-домашнему». Это компас для всех будущих кадров.' },
      { h: 'Что такое мудборд', body: 'Доска-настроение из 15–30 референсов, задающих вектор визуала. Не «чужие фото, чтобы украсть», а образец света, гаммы, ракурсов, атмосферы. Бери то, что вызывает «хочу так же», и сразу спрашивай почему — фиксируй конкретику: тёплый свет, приглушённые тона, много воздуха.' },
      { h: 'От мудборда к своим кадрам', body: 'Мудборд отвечает на 3 вопроса: какой свет, какая гамма, какие сюжеты. Под каждый референс придумай, как повторить его в своей реальности. Не копируй один в один — вытаскивай принцип, а не картинку. Собрал 30 — выкинь всё, что не бьётся с твоими 3 словами.' }
    ],
    lifehack: 'После сбора референсов к каждому подпиши одно слово — почему он тут: «свет», «воздух», «поза». Оставь только те, где слово совпадает с твоими 3 словами-настроения. Так мудборд превращается из красивой свалки в рабочую инструкцию по съёмке.',
    links: [
      { label: 'Milanote', url: 'milanote.com', note: 'Собрать мудборд и смыслы блога на одной визуальной доске' },
      { label: 'Are.na', url: 'are.na', note: 'Копить референсы по темам и видеть общий вектор визуала' }
    ]
  },
  23: {
    intro: 'Подписчик видит не один пост, а 3–9 сразу — как единую композицию. Красивый по отдельности кадр может ломать сетку. Поэтому планируем не фото, а ряды и переходы.',
    notes: [
      { h: 'Планируй партиями по 12–15', body: 'Оптимально верстать 12–15 фото вперёд — это 4–5 рядов сетки. Так видно ритм: где скучно, где пестро, где два похожих кадра рядом. Работает раскладка в приложении-планировщике или просто на холсте. Точечная выкладка «по одному» ломает общую картину.' },
      { h: 'Три опоры красивой сетки', body: 'Единая гамма — все кадры в одной цветовой семье. Не более двух шрифтов на текстовых обложках. Единая обработка — один пресет, одна атмосфера на всю партию. Чередуй образы: лицо → деталь → пространство → текст → лицо, раскидывай акценты по диагонали.' },
      { h: 'Воздух и проверка перед выкладкой', body: 'Воздух — свободное место, куда «дышит» композиция; светлый минималистичный кадр между насыщенными это вдох для глаза. Перед публикацией собери превью 9 постов, расфокусируй взгляд: есть ритм или серое пятно, нет ли двух близнецов рядом.' }
    ],
    lifehack: 'Держи в запасе 2–3 «нейтральных» кадра (минимализм, макро, светлый фон). Когда сетка начинает пестрить или два похожих поста встают рядом — ставишь между ними нейтральный, и ряд снова дышит. Это спасательный круг ленты.',
    links: [
      { label: 'Planoly', url: 'planoly.com', note: 'Планировщик сетки: раскладываешь будущие посты и видишь ритм' },
      { label: 'UNUM', url: 'unum.la', note: 'Раскладка ленты с превью рядов — ловить близнецов до выкладки' }
    ]
  },
  24: {
    intro: 'Гамма — это то, что даёт эффект «сразу видно, что это твой пост». 3–5 повторяющихся цветов связывают даже разные по сюжету кадры в одну узнаваемую ленту.',
    notes: [
      { h: 'Как собрать палитру', body: 'Возьми 1 базовый (фон-настроение), 2 дополнительных и 1 акцент. Отталкивайся от смыслов: тёплые тона = уют, холодные = технологичность. Проверь палитру на реальных кадрах, а не только в теории — на фото цвета ведут себя иначе, чем в подборке.' },
      { h: 'Обработка без пересвета и кислоты', body: 'Пересвет — выбитый в белое кадр, теряются детали и объём. Кислота — перекрученная насыщенность, цвета кричат неестественно. Здоровая обработка: мягкий контраст, живой цвет кожи, читаемые тени. Лучше недожать, чем перекрутить.' },
      { h: 'Правило двух шрифтов', body: 'На обложках максимум два шрифта: один заголовочный с характером, один спокойный для текста. Больше — визуальный шум и ощущение «собрано наспех». Пара строится на контрасте по толщине: массивный заголовок + лёгкий читаемый текст. Не мешай два похожих.' }
    ],
    lifehack: 'Собери свою палитру и сохрани её отдельной картинкой в телефоне. Перед обработкой каждого фото открывай её рядом и подгоняй тон под эти 3–5 цветов. Лента станет узнаваемой без единого логотипа — люди начнут узнавать твои посты по цвету.',
    links: [
      { label: 'Khroma', url: 'khroma.co', note: 'ИИ-генератор палитр под твой вкус — быстрый подбор гаммы блога' },
      { label: 'Fontpair', url: 'fontpair.co', note: 'Готовые пары шрифтов на контрасте — заголовочный + текстовый' }
    ]
  },
  25: {
    intro: 'План — это насколько крупно объект в кадре. Одинаковая крупность во всех постах и есть баннерная слепота. Смена планов и ракурсов создаёт ритм и держит внимание по всей сетке.',
    notes: [
      { h: 'Пять планов и ракурс', body: 'Общий — человек и пространство, задаёт контекст. Средний — по пояс, рабочий план для контакта. Крупный — лицо и эмоция, максимум личного. Ракурс тоже меняет всё: сверху — для раскладок и столов, на уровне глаз — честный контакт и база портрета.' },
      { h: 'Композиция и воздух', body: 'Оставляй воздух — свободное место, куда направлен взгляд или движение. Не лепи объект строго в центр каждый раз, используй смещение (правило третей). Убери из кадра лишний мусор: провода, случайные предметы фона — они дешевят снимок.' },
      { h: 'Снимаем на телефон', body: 'Для качественной ленты хватает iPhone 11–12 и новее. Главное не камера, а свет: дневной у окна бьёт любую вспышку. Снимай вертикально в 4:5 — максимум площади в ленте. Тренды бери как идею, но оборачивай в свою гамму, иначе растворишься среди тысяч.' }
    ],
    lifehack: 'Ставь себе «квоту планов» на съёмочный день: минимум по одному общему, среднему и крупному кадру одной сцены. Потом в сетке будет из чего собрать ритм, и ты не окажешься с десятью одинаковыми по крупности фото, которые вгоняют ленту в слепоту.',
    links: [
      { label: 'VSCO', url: 'vsco.co', note: 'Плёночные пресеты и тонкая цветокоррекция для единого стиля кадров' },
      { label: 'PhotoRoom', url: 'photoroom.com', note: 'Убрать лишний фон и мусор из кадра, подчистить композицию' }
    ]
  },
  26: {
    intro: '96% лайков собирают фото с людьми — мозг эволюционно ищет лица. Лицо в кадре это ощущение живого контакта, а не безликого бренда, и твоя главная отстройка, которую невозможно скопировать.',
    notes: [
      { h: 'Зрительный контакт через экран', body: 'Взгляд в камеру = прямой контакт «глаза в глаза» со зрителем, он останавливает палец сильнее любого предмета. Живая эмоция важнее идеальной позы — искренность считывается. Экспертов в нише сотни, таких как ты один: люди подписываются на человека.' },
      { h: 'Как побороть страх камеры', body: 'Первые кадры кривые у всех — это этап, не приговор. Снимай сериями: из 30 дублей 2–3 всегда живые и твои. Начни с полубоку и деталей, наращивай крупность постепенно. Камера становится своей от количества, а не от настроя.' },
      { h: 'Личное ≠ только селфи', body: 'Присутствие человека — это и руки в кадре, и фигура со спины, и силуэт. Процесс работы с твоим участием живее голого результата. Чередуй: портрет, деталь с руками, ты в пространстве — заодно бьёшь баннерную слепоту. Формула: человек + ценность в одном кадре.' }
    ],
    lifehack: 'Снимай «процесс», а не только результат: руки за работой, ты в пространстве, момент действия. Такие кадры даются легче селфи (не надо позировать в лицо), собирают лайки как «фото с человеком» и показывают экспертность в деле — три выгоды в одном.',
    links: [
      { label: 'Cosmos', url: 'cosmos.so', note: 'Насмотренность на живые портретные кадры и позы для референсов' },
      { label: 'Lightroom', url: 'lightroom.adobe.com', note: 'Довести портрет: мягкий свет, живой цвет кожи без пересвета' }
    ]
  },
  27: {
    intro: 'Люди покупают у тех, кому доверяют, а доверяют — знакомым. Контент делает тебя знакомым. В этом уроке разложим три опоры контента и шкалу эмоций, которая держит внимание.',
    notes: [
      { h: 'Три опоры контента', body: 'Личный — сближает и рождает доверие к человеку за блогом. Экспертный — показывает, что ты решаешь проблему аудитории. Продающий — прямо ведёт к продукту и заявке. Экспертность отвечает «умеет ли он», личное — «свой ли он мне». Нужны все три в балансе.' },
      { h: 'Плохая реакция — это никакая', body: 'Худшая реакция — нейтральность: пролистали и забыли. Любая эмоция, даже спор, лучше молчаливого равнодушия. Эмоции широкие: радость, восхищение, благодарность — и злость, разочарование, несогласие тоже в работу. Нельзя быть всегда «хорошеньким»: пресный образ не запоминают.' },
      { h: 'Рубрики — под нишу', body: 'У каждой ниши свой набор рубрик. Кондитер: процесс, начинки, отзывы, закулисье кухни, акции. Фриланс-дизайнер: кейсы до/после, разбор ошибок, будни, оффер. Приверженность (сильная связь, когда по тебе скучают) строится на регулярности, честности и общих ценностях.' }
    ],
    lifehack: 'Веди контент по правилу «3 опоры в неделю»: минимум один личный пост, один экспертный и один вовлекающий за 7 дней. Так ты одновременно строишь доверие, показываешь пользу и гонишь реакцию — а не заваливаешь ленту одной нотой.',
    links: [
      { label: 'Airtable', url: 'airtable.com', note: 'База контент-плана: рубрики, темы, статусы и даты в одной таблице' },
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Штурм рубрик под твою нишу и тип блога за пару минут' }
    ]
  },
  28: {
    intro: 'Вирусный ролик собирается не на съёмке, а в структуре — сначала каркас, потом камера. Четыре блока: хук → удержание → развязка → призыв. Один ролик — одна мысль, всё лишнее режь.',
    notes: [
      { h: 'Хук: первые 2 секунды', body: 'В первые 2 секунды — обещание выгоды или интрига. Хук-текст на экране плюс голос работают вместе. Заходят: вопрос, цифра, конфликт, «не делай это». Слабый хук убивает даже гениальный ролик — на него уходит половина работы над сценарием.' },
      { h: 'Удержание: петли внимания', body: 'Микро-обещания «дальше покажу, как…» тянут смотреть дальше. Смена плана каждые 2–3 секунды — глазу не скучно. Показывай визуальный прогресс к развязке. Схемы, что цепляют: было → стало, ошибка → решение. Конфликт в начале держит до конца, эмоция важнее фактов.' },
      { h: 'Репосты и призыв', body: 'Люди делятся ради пользы, статуса, эмоции, узнавания. «Сохрани, чтобы не потерять» двигает в сохранения; спорное, но честное мнение — в комментарии. Призыв один на ролик, мягко: «подпишись, если было полезно». Вопрос в конце запускает обсуждение.' }
    ],
    lifehack: 'Пиши сценарий с конца: сначала сформулируй ОДНУ мысль ролика и призыв, потом придумай хук, который её обещает. Так ролик получается цельным, а хук — честным (обещал в начале ровно то, что дал в конце), и досмотры не проседают на обмане ожиданий.',
    links: [
      { label: 'TikTok', url: 'tiktok.com', note: 'Разбирай трендовые хуки и структуры в своей нише как насмотренность' },
      { label: 'CapCut', url: 'capcut.com', note: 'Снять и смонтировать ролик по готовой структуре хук→CTA' }
    ]
  },
  29: {
    intro: 'Логотип не вызывает эмоций — человек вызывает. Люди идут за тем, чьё лицо и историю узнают. Доверие — валюта, которую нельзя купить рекламой, её можно только заслужить контентом.',
    notes: [
      { h: 'Что такое личный контент', body: 'Не селфи из кафе, а показ процесса и мышления: как принимаешь решения, ошибаешься, чинишь ошибки. Бэкстейдж работы ценнее вылизанного результата. Аудитория подписывается на то, КАК ты думаешь, а не только на то, ЧТО ты умеешь.' },
      { h: 'Три слоя доверия', body: 'Компетентность — видно, что ты разбираешься в теме. Последовательность — ты стабильно на связи, не пропадаешь. Открытость — показываешь не только победы. Маска идеального эксперта читается как реклама, ей не верят; признание ошибки поднимает доверие сильнее любого кейса.' },
      { h: 'Доверие превращается в деньги', body: 'Тёплая аудитория покупает без длинных продаж. Один лояльный подписчик ценнее ста случайных охватов. Путь простой: узнал → доверился → купил → рекомендует. Начни с камеры телефона и одной мысли в день, снимай короткие «мысли вслух» без идеальной подготовки.' }
    ],
    lifehack: 'Заведи рубрику «мысль вслух»: раз в день 20–30 секунд на камеру про одну идею из своей работы, без сценария и монтажа. Такой сырой личный контент строит доверие быстрее вылизанных постов — люди видят живого человека, а не отрепетированную рекламу.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Набросать тезисы «мысли вслух» — говоришь потом своими словами' }
    ]
  },
  30: {
    intro: 'Контент без эмоции пролистывают за полсекунды. Алгоритм ловит не тему, а реакцию на неё: сильная эмоция = сохранения, репосты, комментарии. Учимся управлять эмоцией поста осознанно.',
    notes: [
      { h: 'Семь эмоций контента', body: 'Тёплые: радость, восхищение, благодарность. Острые: злость, разочарование, несогласие. Глубокая: сопереживание — самая объединяющая. Вечная позитивность превращается в фон, её не замечают; только тёплое = «приятный, но забываемый» блогер. Острые эмоции дают характер.' },
      { h: 'Острое без токсичности', body: 'Злись на явление и подход, а не на конкретных людей. Несогласие — с аргументом, а не с ярлыком. Разочарование — честное, с выводом «что делать». Тогда острая эмоция цепляет и не превращает тебя в токсичного скандалиста.' },
      { h: 'Микс эмоций внутри недели', body: 'Не заваливай ленту одной нотой — чередуй: радость, несогласие, сопереживание по разным дням. После острого поста ставь тёплый, чтобы снять напряжение. Перед публикацией спрашивай: какую эмоцию я вызову? Ответ «никакую» — пост в переработку.' }
    ],
    lifehack: 'Перед каждой публикацией отвечай одним словом: какую эмоцию вызовет этот пост? Если честный ответ «никакую» — переделывай или не публикуй. Эта 5-секундная проверка отсеивает пресные посты, которые всё равно никто бы не сохранил.',
    links: [
      { label: 'AnswerThePublic', url: 'answerthepublic.com', note: 'Увидеть реальные вопросы и боли аудитории — из них рождается эмоция' }
    ]
  },
  31: {
    intro: 'Худшее, что может случиться с постом, — тишина. Негатив в комментариях лучше полного молчания: молчание алгоритм читает как «неинтересно» и режет охват. Разберём, как провоцировать реакцию осознанно.',
    notes: [
      { h: 'Почему нейтральность опаснее хейта', body: 'Нейтральный контент не сохраняют и не пересылают. Хейт даёт активность, а активность даёт показы. Пост со спором часто обгоняет тёплый по охвату вдвое. Нельзя нравиться всем: чёткая позиция всегда кого-то оттолкнёт — и это норма, а отписки после острого поста это чистка.' },
      { h: 'Как работать с негативом', body: 'Не удаляй адекватную критику — отвечай по сути. Спокойный ответ на хейт поднимает тебя в глазах молчунов, которые всё читают. Бессмысленный троллинг — игнор или юмор, но не оправдания. Как ты держишь удар, аудитория запоминает лучше самого поста.' },
      { h: 'Метрика, на которую смотреть', body: 'Не лайки, а комментарии и сохранения. Соотношение реакций к охвату важнее абсолютных цифр. Заканчивай пост вопросом или выбором, заявляй непопулярное мнение, если готов его защищать. Проси делиться своим опытом — вопрос запускает комментарии.' }
    ],
    lifehack: 'Раз в неделю публикуй пост с чётким мнением, которое готов защитить, и заканчивай вопросом «а как считаешь ты?». Комментарии и споры разгонят охват сильнее десяти тёплых постов — только выбирай темы, в которых реально разбираешься, чтобы удержать позицию.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Сформулировать непопулярное мнение с аргументом, а не с ярлыком' }
    ]
  },
  32: {
    intro: 'Лояльный смотрит, а приверженный — ждёт и скучает. Приверженность это сильная эмоциональная связь, а не привычка листать. Приверженный покупает всё и рекомендует без просьбы. Учимся её строить.',
    notes: [
      { h: 'Чтобы по тебе скучали', body: 'Нужен узнаваемый характер и голос. Сквозные герои: команда, питомец, постоянная рубрика. Внутренние шутки и свой язык, понятный только своим. Уязвимость — делись сомнениями, а не только успехом. Обращайся лично: «ты», имена, ответы в комментариях.' },
      { h: 'Ритуалы, которые держат', body: 'Постоянное время выхода — тебя начинают ждать. Еженедельная рубрика превращается в сериал. Финальная фраза или приветствие как подпись. Ритуалы создают предсказуемость, за которую аудитория цепляется и возвращается сама.' },
      { h: 'Что убивает привязанность', body: 'Резкая смена стиля и тона — аудитория теряет «тебя». Исчезновение на месяцы обнуляет связь. Только продажи без личного — разлюбят быстро. Измеряй приверженность по сообщениям «скучали, где вы пропали», по возврату одних и тех же имён в комментариях и повторным покупкам.' }
    ],
    lifehack: 'Придумай один повторяющийся ритуал: фирменное приветствие, еженедельную рубрику в один и тот же день или сквозного героя. Предсказуемость превращает случайных зрителей в тех, кто ждёт твой контент — а ожидание это первый шаг к «по тебе скучают».',
    links: [
      { label: 'Telegram', url: 't.me', note: 'Закрытый чат/канал для своих: ритуалы, внутренний язык, живая связь' }
    ]
  },
  33: {
    intro: 'Без системы ты каждый день думаешь «а что снимать?». Рубрики — это готовые полки, куда раскладываются идеи, а аудитория запоминает форматы и ждёт любимую рубрику. Собираем контент-план.',
    notes: [
      { h: 'Четыре типа рубрик и баланс', body: 'Экспертные (разборы, ошибки, инструкции) растят доверие. Личные (истории, бэкстейдж, ценности) строят связь. Вовлекающие (опросы, вопросы, споры) гонят реакцию. Продающие ведут к деньгам. Рабочий баланс: 40% эксперт, 30% личное, 20% вовлечение, 10% продажи. Перекос в продажи утомляет аудиторию.' },
      { h: 'Контент-план на месяц', body: 'Возьми сетку 4 недели × дни постинга. Разложи рубрики так, чтобы эмоции чередовались. Оставь 10–20% под ситуативку и тренды. Где брать идеи: вопросы клиентов = готовые экспертные посты, свои ошибки — материал для личных рубрик.' },
      { h: 'План живёт, а не пылится', body: 'Раз в неделю сверяйся: что зашло, что нет. Рубрику с нулевой реакцией меняй или убирай. Форматы-хиты масштабируй, слабые режь без жалости. План — живой инструмент, а не памятник, написанный один раз и забытый.' }
    ],
    lifehack: 'Собери «банк тем» на месяц вперёд одним заходом: сядь на час и выпиши по 4 темы в каждую из 4 рубрик — это 16 постов. Дальше ты не выдумываешь контент каждый день в панике, а просто берёшь готовую тему с полки и снимаешь.',
    links: [
      { label: 'Notion', url: 'notion.so', note: 'Шаблон контент-плана: рубрики, темы, календарь, статусы' },
      { label: 'Trello', url: 'trello.com', note: 'Доска рубрик с карточками-темами, которые двигаешь по неделям' }
    ]
  },
  34: {
    intro: 'Каркас рубрик универсален, а темы всегда под нишу. Чужой контент-план копировать бессмысленно: ниша диктует боли, язык и форматы аудитории. Соберём план именно под твою нишу.',
    notes: [
      { h: 'Одна структура — разное наполнение', body: 'Кондитер. Экспертное: «почему трескается макарон», разбор ошибок. Личное: ночная смена перед свадебным заказом. Вовлекающее: «какой торт собрать на 8 марта?». Дизайнер. Экспертное: «как править ТЗ, чтобы не переделывать 5 раз». Личное: уход из найма и первый клиент. Вовлекающее: «этот логотип за 500 или за 50 000?».' },
      { h: 'Как найти темы своей ниши', body: 'Собери частые вопросы клиентов — это готовые посты. Проверь спрос запросов через Wordstat. Изучи боли в комментариях у коллег по нише. Реальные вопросы аудитории всегда сильнее выдуманных тем — они уже про то, что людей волнует.' },
      { h: 'Язык ниши решает', body: 'Говори словами аудитории, а не профессиональным сленгом. Кондитер: «не течёт крем», а не «стабилизация ганаша». Дизайнер: «не стыдно показать», а не «UX-консистентность». На чужом языке пост проходит мимо, даже если по сути полезный.' }
    ],
    lifehack: 'Собери план за час по схеме 4×4: 4 рубрики под нишу, по 4 темы в каждую, разложи 16 тем на 4 недели, добавь эмоции и ситуативку. Один час системной работы избавляет от ежедневного вопроса «что постить» на целый месяц.',
    links: [
      { label: 'Яндекс Wordstat', url: 'wordstat.yandex.ru', note: 'Проверить реальный спрос тем и запросов в твоей нише и гео' },
      { label: 'Google Таблицы', url: 'sheets.google.com', note: 'Разложить 16 тем по неделям в простой сетке контент-плана' }
    ]
  },
  35: {
    intro: 'Витрину листают, а в комьюнити возвращаются. Люди остаются не ради контента, а ради ощущения «свои». Главное чувство сильного блога — «вы не одни»: принадлежность люди ищут сильнее, чем информацию.',
    notes: [
      { h: 'Как превратить подписчиков в комьюнити', body: 'Задавай вопросы и публикуй ответы аудитории. Знакомь подписчиков друг с другом в комментариях. Вводи общий язык и внутренние мемы. Общая боль объединяет: «у меня тоже так». Цель — чтобы аудитория начала общаться между собой, а не только с тобой.' },
      { h: 'Форматы, рождающие комьюнити', body: 'Рубрика вопросов: аудитория задаёт, ты разбираешь. Челленджи и совместные марафоны с отчётами. Прямые эфиры — живое общение в реальном времени. Всё, где люди действуют вместе и видят друг друга, превращает зрителей в племя.' },
      { h: 'Модерация и результат', body: 'Комьюнити нужны правила, иначе приходят токсики. Защищай своих от хамства — это укрепляет доверие; поощряй поддержку, гаси травлю сразу. Своя аудитория покупает и рекомендует без уговоров и защищает тебя в спорах без твоего участия. Сарафан внутри племени дешевле любой рекламы.' }
    ],
    lifehack: 'Заведи регулярную рубрику, где публикуешь ответы и истории подписчиков (с их именами). Когда человек видит себя в твоём блоге, он из зрителя превращается в участника — и приводит своих. Комьюнити растёт, когда аудитория чувствует себя соавтором, а не публикой.',
    links: [
      { label: 'Telegram', url: 't.me', note: 'Чат-комьюнити: живое общение, челленджи, свой язык и правила' },
      { label: 'Boosty', url: 'boosty.to', note: 'Закрытое комьюнити с подпиской — «свои» и монетизация разом' }
    ]
  },
  36: {
    intro: 'Сторис — это мост к посту и твой ежедневный контакт с аудиторией. Через прогрев в сторис человек приходит к продаже без давления, а эфиры снимают дистанцию сильнее любого поста.',
    notes: [
      { h: 'Сторис ведут в пост', body: 'Не давай всё в сторис — дразни и отправляй дочитать в посте. Анонс, интрига, кусочек результата и ссылка «подробнее в ленте». Так сторис поднимают охваты поста, а пост углубляет тему. Это лента «здесь и сейчас»: события, быт, закулисье.' },
      { h: 'Эфиры — сильнейший инструмент сближения', body: 'В Live ты живой, здесь и сейчас: вопросы, реакции, разбор в реальном времени. Тебя нельзя смонтировать — доверие растёт быстрее любого поста. Чтобы удерживать: меняй ракурс и картинку, играй тоном и темпом, ставь крючки «через минуту покажу главное».' },
      { h: 'Опросы и развлечение', body: 'Опросы вовлекают в один тап и оживляют охваты сторис. Через них выявляешь боли, которые потом закрываешь продуктом — аудитория сама даёт тебе ТЗ. Вовлекай играючи: тесты, «угадай», интерактивные наклейки. Игра снижает барьер и запоминается лучше лекции.' }
    ],
    lifehack: 'Правило «сторис-тизер»: показывай в сторис не весь результат, а его край — и отправляй за полным разбором в пост. Любопытство гонит переходы, охваты поста растут, а ты приучаешь аудиторию, что главное всегда в ленте.',
    links: [
      { label: 'Mojo', url: 'mojo-app.com', note: 'Анимированные сторис с шаблонами — тизеры и прогрев, которые цепляют' },
      { label: 'InShot', url: 'inshot.com', note: 'Быстро собрать вертикальные сторис и нарезки прямо с телефона' }
    ]
  },
  37: {
    intro: 'Сильный бренд общается как человек, а не как инструкция: у него есть характер — манера речи, ценности, юмор. Люди привязываются к характерам, а не к безличным названиям.',
    notes: [
      { h: 'Что такое архетип', body: 'Архетип — узнаваемый образ-роль, понятный без объяснений. Он задаёт, как бренд говорит, что обещает и какие эмоции несёт. Опора на архетип делает бренд цельным и предсказуемым для своих — люди знают, чего от тебя ждать, и это часть доверия.' },
      { h: 'Четыре рабочих архетипа', body: 'Стабильность: надёжность, порядок, забота — «со мной спокойно», тон уверенный. Свобода: независимость, «живи по-своему» — тон лёгкий, вдохновляющий. Вызов: рост, преодоление, «стань лучшей версией» — тон энергичный, местами провокационный. Радость: лёгкость, юмор, тепло — тон дружелюбный, с улыбкой.' },
      { h: 'Характер должен быть твоим', body: 'Архетип задаёт не только слова, но и реакции, и обещание. Выбирай тот, в котором сможешь жить каждый день без фальши — наигранный характер аудитория считывает быстрее, чем ты думаешь. Один ведущий архетип задаёт основу голоса, остальное — оттенки.' }
    ],
    lifehack: 'Опиши свой бренд как человека на вечеринке: как он говорит, над чем шутит, что его злит, что обещает. Если получился живой, узнаваемый персонаж — у тебя есть характер. Если вышла «полезная, но безликая табличка» — выбери архетип и оживи его.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Примерить архетипы к своей нише и собрать описание характера бренда' },
      { label: 'Milanote', url: 'milanote.com', note: 'Собрать доску характера: тон, ценности, референсы голоса' }
    ]
  },
  38: {
    intro: 'Человек покупает не в момент оффера, а после 7–15 касаний. Сторис живут 24 часа и накручивают эти касания каждый день — это и есть прогрев, который подводит к продаже без давления.',
    notes: [
      { h: 'Драматургия прогрева на 5–7 дней', body: 'День 1–2: боль и проблема — аудитория узнаёт себя. День 3–4: путь и инсайт — как ты сам решил эту проблему, ошибки, поворот. День 5–6: доказательства — кейсы, отзывы, скрины результата. День 7: оффер. Продажа в лоб на холодную, без этих дней, — ошибка №1.' },
      { h: 'Крючки, которые держат до конца', body: 'Первые 2 секунды решают всё: сразу лицо или движение, не заставка. Текст-крючок поверх кадра: «я потерял 40 тысяч на этом» цепляет сильнее «привет всем». Открытая петля: обещай ответ в конце цепочки, а не в этой сторис — так досматривают всю серию.' },
      { h: 'Показывай жизнь, а не только продукт', body: 'Аудитория покупает у человека, а не у логотипа: завтрак, дорога, рабочий стол — это доверие. Правило чередования: на 4 личные/полезные сторис — 1 продающая. Один призыв на цепочку: «пиши ПЛЮС в директ», не размазывай пять действий. И не мешай прогрев к разным продуктам в одну кашу.' }
    ],
    lifehack: 'Строй прогрев по схеме «боль → путь → доказательства → оффер», растянув на 5–7 дней, и в каждой цепочке держи одну открытую петлю на финал. Аудитория приходит к офферу уже прогретой и своими словами описавшей боль — продавать почти не приходится.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Расписать драматургию прогрева по дням: боль, инсайт, доказательства, оффер' },
      { label: 'Canva', url: 'canva.com', note: 'Собрать визуал сторис-цепочки в едином стиле бренда' }
    ]
  },
  39: {
    intro: 'Эфир — сильнейший инструмент: живая обратная связь, тебя нельзя смонтировать, а алгоритмы любят долгое удержание и подтягивают Live в рекомендации. Но без плана эфир разваливается.',
    notes: [
      { h: 'Подготовка решает явку', body: 'Заранее собери 3–5 болей аудитории — это скелет эфира, а не импровизация с нуля. Анонс за 24 часа и напоминание за час: явка зависит от анонса больше, чем от темы. Один эфир — одна тема и одно целевое действие в финале.' },
      { h: 'Структура на 30–40 минут', body: 'Первые 3 минуты — разгон: ждём людей, но сразу даём мысль, чтобы не уходили. Основной блок — польза через истории, а не сухая теория. Середина — интерактив: вопрос в чат. Называй людей по имени: «Марина спрашивает…» — это вовлечение и лояльность.' },
      { h: 'Что делать после эфира', body: 'Нарежь на Reels/Shorts — сильные моменты живут второй жизнью неделями. Догрев в сторис для тех, кто не был: «вот что ты пропустил». Собери вопросы из чата — это готовые темы для следующего контента. Один эфир кормит контент на неделю вперёд.' }
    ],
    lifehack: 'Записывай каждый эфир и сразу нарезай 3–5 сильных моментов на Reels. Один час прямого эфира превращается в неделю коротких роликов, которые приводят новых людей на следующий Live — так эфиры работают на охват, а не только на своих.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Собрать скелет эфира: 3–5 болей, структура, целевое действие' },
      { label: 'CapCut', url: 'capcut.com', note: 'Нарезать запись эфира на Reels с субтитрами' }
    ]
  },
  40: {
    intro: 'Люди уходят с эфира от монотонности: один ракурс, один тон, ноль движения — мозг устаёт за 90 секунд. Удержание это навык, который строится на смене картинки, темпа и постоянных крючках.',
    notes: [
      { h: 'Смена ракурса и картинки', body: 'Меняй план: сел, встал, подошёл к доске, показал экран — картинка не должна застывать. Демонстрируй руками, предметами, скринами: визуал держит сильнее слов. Каждые 3–5 минут — новый визуальный якорь, чтобы сбросить усталость глаза.' },
      { h: 'Смена тона и темпа речи', body: 'Говори не ровной линией, а волной: тише-громче, быстрее-медленнее. Перед важной мыслью — пауза: тишина заставляет вслушаться. Эмоция в голосе заразительна: скучно тебе — скучно всем. Долгое вступление «привет, ждём всех» на 5 минут сливает половину зрителей.' },
      { h: 'Крючки и интерактив', body: '«Через 10 минут покажу схему, которая приносит заявки» — обещание впереди. «Досмотри до конца — там подарок» держит до оффера. Вопрос в чат каждые несколько минут: отвечающий остаётся вовлечённым. «Поставьте плюс, если узнали себя» — простое действие возвращает фокус.' }
    ],
    lifehack: 'Раскидай по тайм-лайну эфира 3–4 «обещания впереди»: в начале, в середине и перед финалом анонсируй ценность, которая будет позже («через 10 минут — схема»). Люди остаются ради обещанного, и средний досмотр растёт, а с ним и рекомендации от алгоритма.',
    links: [
      { label: 'OBS Studio', url: 'obsproject.com', note: 'Бесплатная студия для эфира: смена сцен, показ экрана, несколько камер' }
    ]
  },
  41: {
    intro: 'Опрос — это разведка боем: не гадай, что нужно аудитории, а спроси и получи готовый оффер их же словами. Ответы это ещё и сегментация: видишь, кто на какой стадии и что предлагать.',
    notes: [
      { h: 'Виды интерактивов', body: 'Опрос из двух кнопок — быстрый выбор, максимум ответов. Шкала-ползунок — эмоция и градус («насколько устал от…»). Вопрос-бокс — собираешь боли текстом, это темы для контента. Каждый формат под свою задачу: кнопки для статистики, бокс для глубины.' },
      { h: 'Как спрашивать, чтобы отвечали', body: 'Спрашивай про боль, а не про товар: «что бесит в ведении блога?» вместо «купишь курс?». Формулируй бинарно и близко: «узнал себя? да / нет» — низкий порог входа. Один вопрос за раз: два вопроса в одной сторис = меньше ответов на каждый.' },
      { h: 'Из ответов — в продукт и контент', body: 'Собери частые боли из вопрос-бокса — это карта продукта и линейка офферов. Ответ большинства = следующая тема поста или эфира, ты бьёшь в точку. Покажи статистику опроса подписчикам: «70% из вас ответили…» — это соцдоказательство, которое работает на тебя.' }
    ],
    lifehack: 'Раз в неделю задавай в сторис один вопрос-бокс про боль аудитории и складывай ответы в отдельную заметку. Через месяц у тебя карта болей клиентов их же словами — из неё собираются и темы контента, и продающие офферы, которые попадают в точку.',
    links: [
      { label: 'Google Формы', url: 'forms.google.com', note: 'Глубокий опрос аудитории вне сторис — собрать боли и сегменты подробно' }
    ]
  },
  42: {
    intro: 'С брендом общаются как с человеком: у него есть тон, ценности, манера. Архетип — ядро этого характера. Разберём четыре архетипа детально и под какие ниши каждый подходит.',
    notes: [
      { h: 'Стабильность и Свобода', body: 'Стабильность — обещание надёжности и безопасности, «со мной спокойно»; тон уверенный, без хаоса; подходит финансам, консалтингу, всему предсказуемому. Свобода — независимость и свой путь, «делай по-своему»; тон лёгкий, антирамочный; подходит тревелу, лайфстайлу, инфобизнесу про свободу.' },
      { h: 'Вызов и Радость', body: 'Вызов — рост и новая версия себя, «стань сильнее»; тон дерзкий, мотивирующий; подходит спорту, обучению, коучингу про результат. Радость — уют, забота, лёгкость, «с тобой хорошо»; тон тёплый, с юмором; подходит семейным нишам, еде, хобби, комьюнити.' },
      { h: 'Как выбрать свой', body: 'Отталкивайся от ценностей аудитории, а не от того, что модно. Проверь на себе: сможешь ли жить в этом тоне каждый день без фальши. Один ведущий архетип задаёт 70% голоса, остальное — оттенки. Характер должен совпадать и с продуктом, и с тем, как ты реально говоришь.' }
    ],
    lifehack: 'Выпиши 4 архетипа и оцени каждый по 10-балльной шкале двумя вопросами: «это про меня?» и «это про мою аудиторию?». Где оба балла высокие — там твой ведущий архетип. Так выбор идёт от ценностей и правды, а не от того, что сейчас в моде.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Разобрать архетипы под свою нишу и подобрать тон без фальши' }
    ]
  },
  43: {
    intro: 'Чистых характеров почти не бывает: на практике бренд — смесь архетипов. Именно смесь делает тебя непохожим, ведь чистый архетип легко скопировать. Обычно есть ведущий (60–70%) и один-два оттенка.',
    notes: [
      { h: 'Как рождается уникальность', body: 'Вызов + тепло: жёстко про результат, но по-доброму и с поддержкой. Стабильность + свобода: системность без духоты, порядок ради свободы. Свобода + вызов: свой путь через преодоление и рост. Комбинация двух архетипов даёт голос, который трудно повторить.' },
      { h: 'Ведущий и оттенок', body: 'Один архетип — основа голоса, он звучит в 70% контента. Оттенок включается точечно: в кейсах, в юморе, в подаче. Не мешай поровну — каша из равных долей размывает характер. Смесь работает, когда у неё есть общий смысл, а не случайный набор.' },
      { h: 'Опасность несовместимых смесей', body: 'Не всё сочетается: жёсткий контроль + анархичная свобода путают аудиторию. Если тон скачет от заботы к агрессии без логики — теряется доверие. Держи единый тон во всех точках: сторис, эфиры, ответы в директ. Микс должен звучать как один человек, а не как раздвоение.' }
    ],
    lifehack: 'Пропиши свою формулу одной строкой: «ведущий + оттенок = обещание» (например «вызов + тепло = приведу к результату, но не сломаю»). Собери 3 эталонные фразы в этом миксе и сверяй с ними любой пост — так характер держится единым во всех точках.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Сформулировать формулу «ведущий + оттенок = обещание» и 3 эталон-фразы' }
    ]
  },
  44: {
    intro: 'Финал блока: собери паспорт характера своего бренда — ведущий архетип, оттенок, тон и триггеры. Не в теории, а в готовой формуле, по которой будешь писать весь контент.',
    notes: [
      { h: 'Собери паспорт за 3 шага', body: 'Шаг 1: выпиши 4 архетипа и оцени каждый по 10-балльной шкале — самый высокий это ведущий, второй оттенок. Шаг 2: собери паспорт — ведущий + оттенок (ядро), обещание (одна фраза, что бренд даёт), тон (3–4 прилагательных, как ты звучишь). Шаг 3: проверь на прочность.' },
      { h: 'Тест на прочность', body: 'Тест на человека: живой человек может быть таким? Если нет — переделай. Совпадает ли характер с продуктом и с тем, как ты реально говоришь. Напиши 3 сторис в этом голосе — звучит цельно или фальшиво? Фальшь на этом этапе дешевле поймать, чем потом переучивать аудиторию.' },
      { h: 'Что дальше', body: 'Закрепи паспорт в заметке и сверяйся с ним перед каждым постом. Прогрев, эфиры, опросы из этого блока — теперь в едином характере. Через месяц пересмотри: развился ли бренд, не сполз ли в шаблон. Характер это не разовое упражнение, а живая настройка голоса.' }
    ],
    lifehack: 'Держи паспорт характера первой строкой в заметках телефона и перед публикацией любого поста бросай на него взгляд: звучит в моём тоне или нет? 5 секунд сверки — и весь блог держит единый узнаваемый голос, за который аудитория и привязывается.',
    links: [
      { label: 'Notion', url: 'notion.so', note: 'Закрепить паспорт бренда (ведущий, оттенок, обещание, тон) и сверяться перед постами' }
    ]
  },
  45: {
    intro: 'Текст, который продаёт, — это молчаливый аналог живой речи. Канцелярит и пафос убивают доверие. Разберём 5 признаков сильного текста: прочитал вслух и споткнулся — читатель споткнётся тоже.',
    notes: [
      { h: 'Глаголы двигают, лишнее — режь', body: 'Причастия меняй на глаголы: «являющийся экспертом» → «эксперт», «сделавший» → «сделал». Глагол двигает мысль, причастие тормозит. Выкинь каждое 3–5-е слово — смысл почти всегда на месте, а текст легче. Каждое 3–5-е оставшееся замени выразительным синонимом: «очень хороший» → «сильный».' },
      { h: 'Начало решает всё', body: 'Удали вводный первый абзац — почти всегда это разогрев ни о чём. Начинай сразу с сути, а не с «в современном мире…». Первое предложение работает как заголовок: оно решает, читать дальше или нет. Не зашла первая строка — остальное уже никто не прочтёт.' },
      { h: 'Траектория взгляда', body: 'Глаз не читает, а сканирует: тексты по букве F, визуал-афиши по букве Z. Важное ставь в начало строк и абзацев — туда падает взгляд, там «золото». Правый низ абзацев почти не читают, туда нельзя прятать суть. Первые 2–3 слова строки весят больше, чем середина.' }
    ],
    lifehack: 'Финальная проверка любого текста — прочитать его вслух, губами, а не глазами. Там, где споткнулся, задохнулся или полез перечитывать фразу, — правь. Ухо ловит канцелярит и длинноты точнее глаза, и это бесплатный редактор, который всегда с тобой.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Прогнать черновик по 5 признакам и получить варианты правок' },
      { label: 'Карта слов', url: 'kartaslov.ru', note: 'Подобрать точный синоним с нужным оттенком смысла' }
    ]
  },
  46: {
    intro: 'Хороший текст — аналог живого разговора, просто на бумаге. Читатель слышит тебя в голове: если фраза не звучит губами — она не звучит и на экране. Проверка одна: прочитай вслух.',
    notes: [
      { h: 'Враг номер один — канцелярит', body: 'Канцелярит — язык справок и отписок: осуществление, в целях, данный, является. Ни один живой человек не скажет другу «в целях повышения эффективности». «Осуществляем доставку» → «привозим»: смысл тот же, воздуха больше. Читатель платит за то, что понял, а не за красоту фраз.' },
      { h: 'Пластик убивает доверие', body: '«Пластик» — красивые пустые обороты: команда профессионалов, широкий спектр, гибкий подход. Мозг проскакивает их как шум и не запоминает ничего. За каждым словом должен стоять факт: не «быстро», а «за 40 минут»; не «оперативно реализуем логистику», а «привезу завтра к 10».' },
      { h: 'Как заставить текст звучать', body: 'Представь конкретного человека и напиши, как объяснил бы ему за кофе. Короткие фразы, простые слова, прямой порядок: кто что сделал. Надиктуй голосом, потом расшифруй — так исчезает книжность. Первый гонорар копирайтера это чаще всего переписанная в разговорную речь чужая казёнщина.' }
    ],
    lifehack: 'Прогони готовый текст через Главред и добивай балл до 8+: он подсвечивает канцелярит, штампы и «пластик» прямо по словам. Через пару недель начнёшь замечать казёнщину сразу в голове и писать чисто без всякой проверки — глаз натренируется.',
    links: [
      { label: 'Главред', url: 'glvrd.ru', note: 'Подсвечивает канцелярит, штампы и «пластик» — чистит текст до живого' },
      { label: 'Speechpad', url: 'speechpad.ru', note: 'Надиктовать текст голосом — так уходит книжность и появляется речь' }
    ]
  },
  47: {
    intro: 'Причастия и деепричастия — «идущий», «сделав», «работающая» — навешивают на фразу лишний вес. Живая речь их почти не использует. Каждое причастие это спрятанный глагол, который просит вернуть его в строй.',
    notes: [
      { h: 'Как их ловить', body: 'Ищи хвосты -ущ, -ющ, -ащ, -ящ, -вш, -ем, -им и деепричастные -а/-я, -в. «Клиент, оформивший заказ» — стоп, тут спрятан глагол «оформил». Подсвети их в редакторе или попроси ИИ найти все причастные обороты в тексте — глаз замыливается, инструмент нет.' },
      { h: 'Разворачиваем в действие', body: '«Растущие продажи, обусловленные рекламой» → «Реклама подняла продажи». Оборот превращаем в отдельное короткое предложение с глаголом-действием. Появляется подлежащее и сказуемое — фраза оживает и ускоряется. Динамика = действие: глагол двигает читателя к кнопке, причастие усыпляет.' },
      { h: 'Когда причастие оставить', body: 'Есть устойчивые формы: «жареный», «печатный», «данные» — их не трогаем. Одиночное причастие-определение без хвоста оборота часто безобидно. Правило про действие, а не про фанатизм: если звучит живо — оставляй. «Мастер приедет и починит» бьёт «мастера, приезжающего и выполняющего».' }
    ],
    lifehack: 'Правило «один оборот — одно предложение»: каждый причастный оборот разворачивай в отдельную короткую фразу с глаголом. Текст удлиняется на пару слов, но читается на скорости речи — а именно скорость чтения тянет человека до оффера и кнопки.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Найти все причастные обороты и развернуть их в глаголы, не меняя смысл' }
    ]
  },
  48: {
    intro: 'Приём простой: выкидывай каждое 3–5-е слово и смотри — потерялся ли смысл. В 9 случаях из 10 смысл на месте, а фраза стала легче. Слово, без которого понятно, — это балласт, а не украшение.',
    notes: [
      { h: 'Кого выкидываем первым', body: 'Усилители-паразиты: очень, весьма, довольно, реально, буквально, просто. Дубли смысла: «свободная вакансия», «первый дебют», «в конечном итоге». Служебный шум: «то, что», «для того чтобы», «на самом деле», «как бы». Это первые кандидаты на вылет в любом тексте.' },
      { h: 'Механика прохода', body: 'Читаешь фразу и мысленно закрываешь каждое слово пальцем. Смысл выжил без слова — вычёркиваешь без сожаления. Смысл поплыл — возвращаешь. Так проходишь весь абзац по одному слову. Это не про красоту, а про уважение ко времени читателя.' },
      { h: '«Очень» не усиливает', body: '«Очень дёшево» слабее, чем «299 рублей»: усилитель не даёт картинки. Заменяй усилитель фактом, а не убирай в пустоту. Сила в конкретике: не «огромная скидка», а «минус 60%». Плотный текст не бросают на середине — меньше слов на ту же мысль = выше дочитываемость.' }
    ],
    lifehack: 'Ставь себе цель «минус 30%»: возьми готовый текст и сожми на треть, ничего не потеряв в смысле. Ужатый оффер при том же трафике собирает больше заявок — плотность держит внимание и доводит до кнопки тех, кто на воде бы отвалился.',
    links: [
      { label: 'Тургенев', url: 'turgenev.ashmanov.com', note: 'Оценивает «воду», заспамленность и стоп-слова — что резать в первую очередь' }
    ]
  },
  49: {
    intro: 'Каждое 3–5-е слово меняй на более выразительный синоним — не ради красоты, а ради точности. «Большой» и «громадный» рисуют разную картинку, а точное слово экономит целое предложение объяснений.',
    notes: [
      { h: 'От серого к точному', body: '«Сделать» → построить, собрать, запустить, починить — что именно сделали? «Хороший» → надёжный, честный, тёплый, быстрый — по какому признаку? «Проблема» → затык, дыра, головная боль — какая именно боль клиента? Серое слово прячет смысл, точное вытаскивает его наружу.' },
      { h: 'Глаголы — главная добыча', body: 'Замена глагола даёт больше энергии, чем замена прилагательного. «Цены выросли» → «цены взлетели / подскочили / поползли вверх». Выбирай глагол по эмоции сцены: паника, спокойствие, азарт. Один яркий глагол на фразу — уже много, два подряд глушат друг друга.' },
      { h: 'Не переборщить', body: 'Выразительный ≠ редкий. «Дефиниция» вместо «определение» — это выпендрёж, а не сила. Синоним должен быть понятен читателю с ходу, без словаря. На разборах «затопило заявками» продавало кейс лучше сухого «пришло много» — точность бьёт вычурность.' }
    ],
    lifehack: 'Заведи личный словарик из 20–30 сильных глаголов своей ниши и держи под рукой при письме. Когда рука тянется к «сделать» или «улучшить» — берёшь из словарика точный: «собрал», «разогнал», «вычистил». Текст сразу звучит как эксперт, а не как школьное сочинение.',
    links: [
      { label: 'Синонимы', url: 'sinonim.org', note: 'Ряды синонимов с оттенками — подобрать точное слово вместо серого' }
    ]
  },
  50: {
    intro: 'Удали вводный первый абзац целиком и проверь — стало ли хуже. Почти всегда это разогрев автора, а не польза читателю: мы разгоняемся, как перед прыжком, а настоящая мысль начинается со второго абзаца.',
    notes: [
      { h: 'Как выглядит вода', body: '«В современном мире всё быстрее меняется…» — так начинается 90% слабых текстов. Общие фразы про важность темы, историю вопроса, «как известно». Главный признак: если абзац можно вставить в любой другой текст — значит, он ни о чём и его надо удалить.' },
      { h: 'Тест на удаление', body: 'Закрой первый абзац и прочитай текст со второго — понятно, о чём речь? Понятно и даже бодрее — значит первый был лишним разбегом. Потерялся вход — значит там была мысль, её надо переставить наверх, а не тянуть воду вокруг неё.' },
      { h: 'Начинай с сути', body: 'Хороший текст бьёт с первой строки: факт, цифра, обещание, вопрос в лоб. «В этой статье мы расскажем» — вырезать, сразу рассказывать. Читатель решает за 2–3 секунды, остаться или уйти. Каждый абзац-вода это часть аудитории, которая ушла, не дойдя до оффера.' }
    ],
    lifehack: 'Возьми за правило удалять первый абзац начисто и читать со второго. В 9 текстах из 10 станет только бодрее — вступление было разгоном для тебя, а не пользой для читателя. Дочитываемость поста от этого растёт в полтора раза без единого нового слова.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Найти в тексте «вступление-воду» и показать, с какой строки начинается суть' }
    ]
  },
  51: {
    intro: 'В ленте и мессенджерах заголовка часто нет — первую строку видят раньше всего. Поэтому первое предложение должно цеплять так же сильно, как заголовок: не зашло — остальной текст уже не прочтут.',
    notes: [
      { h: 'Четыре рабочих крючка', body: 'Цифра-обещание: «Сняли 3 ролика — получили 40 заявок». Вопрос-боль: «Почему клиенты уходят после первого письма?». Дерзкое утверждение: «Ваш оффер не продаёт. И вот почему». Интрига с открытой петлёй. Первая строка боится раскачки, приветствий и абстракций без картинки.' },
      { h: 'Пиши строку последней', body: 'Сначала весь текст, потом вытаскивай самую сильную мысль наверх. Часто лучший крючок прячется в середине — вырежи и подними в первую строку. Проверь: если оставить только первую фразу — захочется читать дальше? Нет — значит крючок ещё не найден.' },
      { h: 'Первая строка = деньги', body: 'Это ворота воронки: не открыл — не увидел ни оффера, ни кнопки. Смена одной первой строки поднимала охваты поста в 2–3 раза без правки остального. Тестируй 3–5 вариантов первой строки на один текст — как заголовки к рекламе, это самая окупаемая правка.' }
    ],
    lifehack: 'К каждому посту пиши 3–5 вариантов первой строки и выбирай сильнейшую — как заголовки к рекламе. Это самая окупаемая минута работы над текстом: одна строка решает, увидят твой оффер или пролистают, а её замена удваивает охваты без правки всего остального.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Сгенерировать 5 крючков-первых строк (цифра, вопрос-боль, дерзкое утверждение)' }
    ]
  },
  52: {
    intro: 'Автор и редактор в одной голове одновременно — это гарантия ступора и мук над строкой. Сначала поток, потом редактура: два режима нельзя смешивать, иначе не напишешь ничего.',
    notes: [
      { h: 'Этап 1: пиши на потоке', body: 'Задача — выгрузить все мысли, а не написать красиво с первого раза. Не возвращайся, не перечитывай, не ищи слово — оставляй пропуск и беги дальше. Черновик имеет право быть корявым: его никто не увидит. Скорость важнее качества, качество наведёшь потом.' },
      { h: 'Этап 2: остынь и режь', body: 'Дай тексту отлежаться хотя бы час, лучше ночь — вернётся свежий взгляд. Проходишь по всем пяти признакам: причастия, лишние слова, синонимы, вводный абзац, первая строка. Здесь ты безжалостен: режешь даже то, что было приятно писать.' },
      { h: 'Финал — вслух', body: 'Последний прогон только вслух, губами, а не глазами. Споткнулся, задохнулся, полез перечитывать — правь это место. Нейросеть помогает: поток надиктуй голосом, редактуру попроси у ИИ («найди канцелярит и повторы, смысл не меняй»), но финальное решение всегда за тобой.' }
    ],
    lifehack: 'Разведи написание и редактуру по времени: напиши черновик потоком сегодня, а режь и чисти завтра на свежую голову. Ты перестанешь застревать на первой фразе, а холодный взгляд наутро замечает воду и канцелярит, которые вечером казались нормальными.',
    links: [
      { label: 'Главред', url: 'glvrd.ru', note: 'Этап редактуры: пройтись по канцеляриту и стоп-словам после потока' }
    ]
  },
  53: {
    intro: 'Читатель не проходит текст по строчкам, а бегает глазами по траектории: тексты по букве F, афиши-лендинги по букве Z. Задача копирайтера — расставить важное на пути этого взгляда, а не против него.',
    notes: [
      { h: 'F-траектория: длинные тексты', body: 'Глаз идёт вдоль первой строки, потом чуть ниже второй, затем падает по левому краю. Правый низ абзацев почти не читают — туда нельзя прятать суть. Вывод: начало строк и абзацев — золото, ставь туда ключевые слова, а не «и», «а также», «для того чтобы».' },
      { h: 'Z-траектория: афиши и первые экраны', body: 'Взгляд идёт по Z: верх слева направо, диагональ вниз, низ слева направо. Работает там, где мало текста: баннер, обложка, hero-экран лендинга. Ставь логотип и оффер по углам Z, а кнопку — в финальную точку внизу справа, куда взгляд приходит последней.' },
      { h: 'Правила и проверка', body: 'Короткие абзацы: длинный блок глаз бросает на середине. Подзаголовки, списки и жирный текст создают опоры, за которые цепляется взгляд. Прищурься на текст: читаются ли начала строк как отдельная выжимка сути? Перенос оффера в начало строки поднимал клики без смены слов.' }
    ],
    lifehack: 'Проверь текст «прищуром»: расфокусируй взгляд и прочитай только начала строк и подзаголовки. Если из них складывается понятная выжимка сути — вёрстка работает на F-траекторию. Если суть спряталась в середину и правый край — переставь ключевые слова в начало строк.',
    links: [
      { label: 'Figma', url: 'figma.com', note: 'Собрать макет и проверить тепловую логику F/Z: где задержится взгляд' }
    ]
  },
  54: {
    intro: 'Продающий пост — это не вдохновение, а структура из 7 блоков: Заголовок → Лид → Оффер → Выгоды → Цена → Дедлайн → Призыв. Пропустил блок — воронка протекает и продажа рушится.',
    notes: [
      { h: 'Заголовок, лид, оффер', body: 'Заголовок ловит внимание за 3 секунды, иначе пролистнут. Лид сразу бьёт в боль: «узнаёшь себя?» — читатель кивает и остаётся. Оффер — что конкретно ты предлагаешь и какой результат даёшь: не «услуги SMM», а «5 Reels в неделю + рост охватов за месяц».' },
      { h: 'Выгоды и цена', body: 'Показывай отзывы, кейсы и цифры — доказательства вместо слов, говори о выгоде для клиента, а не о свойствах. Цену продавай ДО того, как назвал: объясни, почему справедлива, сравни с ценностью результата, разложи, что входит — чтобы цифра не пугала в одиночку.' },
      { h: 'Дедлайн и призыв', body: 'Дедлайн включает дефицит: «до пятницы», «5 мест» — но честно. Призыв — одно ясное действие: «пиши в личку», «жми ссылку». Без призыва читатель ставит лайк и уходит. Каждый блок ведёт к следующему, как ступени: сила поста в цельной цепочке, а не в отдельной красивой фразе.' }
    ],
    lifehack: 'Собери пост по чек-листу из 7 блоков и проверь, что ни один не пропущен — пройдись сверху вниз пальцем. Чаще всего посты не продают не из-за слабых слов, а из-за дыры в воронке: забыли обосновать цену или не поставили призыв, и читатель утёк.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Собрать продающий пост по 7 блокам и проверить, что ни один не пропущен' },
      { label: 'Тильда', url: 'tilda.cc', note: 'Развернуть ту же структуру в лендинг с оффером, ценой и кнопкой' }
    ]
  },
  55: {
    intro: 'Хук — это «пощёчина» внимания в первые 3 секунды, а 8 триггеров — рычаги, которые подталкивают к «да». Нейтральное начало = мгновенный пролистывание, поэтому крючок и психология решают продажу.',
    notes: [
      { h: 'Хук и сторителлинг', body: 'Эмоции-крючки: удивление, любопытство, гнев, страх, тёплые воспоминания. Тизинг — дразнить и не вываливать всё сразу, тянуть интригу (глубина просмотра влияет на продвижение). Скелет истории: герой → препятствие → действие → ошибки. Падения вызывают доверие сильнее победы — читатель узнаёт себя.' },
      { h: '8 триггеров покупки', body: 'Толпа: «уже 3000 человек взяли» — страх остаться в стороне. Эксклюзив: «только для своих». Дефицит: «осталось 5 мест». Халява — бесплатная польза авансом. Авторитет и статистика снимают сомнения. Крутые кейсы рождают «хочу так же». Страх — пугай с заботой. Новизна цепляет любопытством.' },
      { h: 'Как это работает вместе', body: 'Хук останавливает, тизинг удерживает, история сближает, триггеры подталкивают к решению. Не лепи все 8 триггеров в один пост — выбери 2–3 под свою аудиторию и продукт. Триггер без честного основания (фейковый дефицит) убивает доверие — используй только настоящие.' }
    ],
    lifehack: 'Под каждый пост подбирай 2–3 триггера осознанно, а не все сразу: для холодной аудитории — авторитет и кейсы (доверие), для тёплой — дефицит и дедлайн (толчок к действию). Триггер, попавший в стадию клиента, работает, а свалка из восьми — отпугивает.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Сгенерировать хуки и подобрать 2–3 триггера под конкретный оффер и аудиторию' }
    ]
  },
  56: {
    intro: 'Заголовок решает, прочитают первую строку или пролистают — у тебя 1–2 секунды. Его задача не продать, а остановить и заставить читать дальше. Пиши для одного человека и одной боли.',
    notes: [
      { h: 'Формула цепляющего заголовка', body: 'Обещание результата: что человек получит, если дочитает. Конкретика и цифры: «первые 10 000 ₽» бьёт сильнее «много денег». Интрига или разрыв шаблона: сказать не то, что ждут. Заголовок «О себе» или «Немного мыслей» не откроет никто — это провал ещё до текста.' },
      { h: 'Лид: погружаем в боль', body: 'Лид — 2–4 строки сразу после заголовка, вводят в проблему. Опиши боль словами читателя, как он сам проговаривает её в голове. Усиль: покажи, к чему боль ведёт, если её не решить. Лид про компанию, а не про читателя, — частая ошибка, которая гасит отклик.' },
      { h: 'Как найти настоящую боль', body: 'Слушай комментарии, вопросы в директ, отзывы конкурентов. Боль — не «нет клиентов», а «сижу вечером и считаю, чем платить за аренду». Конкретная боль, в которой человек узнаёт себя, цепляет; общая, под всех, — не цепляет никого. Одна боль на пост, распыление убивает отклик.' }
    ],
    lifehack: 'Собирай «банк болей» из реальных фраз аудитории: выписывай дословно, как люди жалуются в комментариях и директе. Заголовок и лид, написанные их же словами («сижу вечером и считаю, чем платить аренду»), бьют в десятку — человек чувствует, что пост про него.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Сгенерировать 10 заголовков под одну боль и выбрать сильнейший' }
    ]
  },
  57: {
    intro: 'Оффер — суть предложения одним ясным ударом: что, кому и за что. Не «услуги маркетинга», а «настрою рекламу, которая приведёт 50 заявок за месяц». Хороший оффер понятен за 5 секунд и вызывает «хочу».',
    notes: [
      { h: 'Свойство против выгоды', body: 'Свойство — что есть у продукта; выгода — что это даёт клиенту. «10 уроков» — свойство; «соберёшь первый ролик уже к вечеру» — выгода. Продавай результат и эмоцию, а не характеристики. Формула «свойство → поэтому → выгода» связывает факт с результатом.' },
      { h: 'Продажа выгод доказательствами', body: 'Отзывы: реальные слова клиентов бьют сильнее твоих обещаний. Кейсы: было-стало с цифрами и сроком. Цифры и факты снимают недоверие лучше прилагательных. Говори «ты получишь», а не «мы предоставляем» — каждая выгода отвечает на немой вопрос «и что мне с этого?».' },
      { h: 'Собираем блок выгод', body: '3–5 выгод, каждая отдельной строкой, легко сканировать. Первая выгода — самая сильная, закрывает главную боль из лида. Подкрепи хотя бы одну цифрой, отзывом или скрином. Ошибки: размытость «качественно и недорого», список свойств без перевода в выгоды, обещание без доказательств.' }
    ],
    lifehack: 'Прогони каждый пункт оффера через вопрос «и что мне с этого?»: если ответа нет — это свойство, а не выгода, переписывай. «10 модулей» → «и что? → соберёшь блог за неделю». Клиент покупает результат в своей жизни, а не характеристики твоего продукта.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Перевести свойства продукта в выгоды по формуле «свойство → поэтому → выгода»' }
    ]
  },
  58: {
    intro: 'Цена без объяснения кажется дорогой — мозг ищет причину сказать «нет». Продать цену значит показать, почему она справедлива, и сравнить с ценностью результата, а не с кошельком читателя.',
    notes: [
      { h: 'Приёмы обоснования цены', body: 'Дробление: «всего 33 ₽ в день» вместо «1000 ₽ в месяц». Сравнение: цена курса против одной ошибки в рекламе на 30 000 ₽. Что входит: разложи пакет, чтобы ценность превышала цифру. Цена без контекста ценности читается как «дорого» ещё до раздумий.' },
      { h: 'Якорь и контраст', body: 'Якорь: сначала покажи высокую цену или аналог, потом свою — на этом фоне твоя кажется разумной. Три тарифа: средний на фоне дорогого выглядит выгодным. Старая цена зачёркнута рядом с новой — виден выигрыш. Мозг оценивает цену не абсолютно, а в сравнении.' },
      { h: 'Дедлайн и дефицит', body: 'Дедлайн убирает «подумаю потом», которое равно «никогда». Ограничь по времени, местам или бонусу. Назови точную дату и время, свяжи с выгодой: «после пятницы цена +50%», напомни, что человек теряет. Дефицит должен быть настоящим, а «последний день», повторяющийся неделями, убивает доверие.' }
    ],
    lifehack: 'Ставь три тарифа, даже если продаёшь один продукт: дешёвый-урезанный, основной и дорогой-премиум. Большинство выберет средний — он выглядит разумным на фоне двух других. Без сравнения любая цена кажется взятой с потолка, а рядом с якорями — обоснованной.',
    links: [
      { label: 'Тильда', url: 'tilda.cc', note: 'Собрать блок из трёх тарифов с якорем, зачёркнутой ценой и таймером' }
    ]
  },
  59: {
    intro: 'Призыв к действию — чёткий следующий шаг для читателя. Без него пост прочитали и забыли, конверсия ноль. Правило простое: один пост — один целевой шаг, не разбрасывай внимание.',
    notes: [
      { h: 'Правило одного действия', body: 'Не давай выбор «купи, подпишись, лайкни, напиши» — мозг зависает и не делает ничего. Выбери одно ключевое действие под цель поста. Чем проще шаг, тем выше отклик: «напиши +» легче, чем «оставь заявку на сайте». Убирай трение на пути к действию.' },
      { h: 'Формула сильного CTA', body: 'Глагол действия плюс конкретика: «Напиши слово СТАРТ в директ». Скажи точно, куда нажать и что произойдёт дальше. Добавь микро-выгоду: «напиши СТАРТ — пришлю программу и цену». Свяжи со сроком: «запишись до пятницы», подтолкни: «осталось 5 мест» рядом с призывом.' },
      { h: 'Частые ошибки', body: 'Нет призыва вообще — самый частый провал поста. Несколько призывов сразу — читатель не делает ни одного. Размытое «переходите по ссылке» без причины и выгоды. Призыв должен быть один, конкретный и с понятной наградой за клик.' }
    ],
    lifehack: 'Делай первый шаг предельно лёгким: «напиши + в директ» вместо «заполни заявку на сайте». Микро-действие снимает страх обязательства, человек включается — а дальше ты уже ведёшь его в диалоге. Высокий порог на входе теряет даже горячих клиентов.',
    links: [
      { label: 'Taplink', url: 'taplink.ru', note: 'Сделать кнопку-CTA с приёмом заявок и переходом в чат в один тап' }
    ]
  },
  60: {
    intro: 'Хук — первые 3 секунды, которые решают, досмотрят или пролистают. Алгоритм видит удержание: слабое начало — и охваты падают. Задача хука «дать пощёчину», резко выдернуть человека из ленты.',
    notes: [
      { h: 'Пять эмоций-крючков', body: 'Удивление: неожиданный факт или результат. Любопытство: вопрос без ответа, интрига. Гнев, страх, ностальгия — сильная эмоция держит палец над экраном. Нейтральное «сегодня поговорим о…» такой пощёчины не даёт, и человек уходит на второй секунде.' },
      { h: 'Приёмы сильного хука', body: 'Начни с результата или с конфликта, а не с приветствия. Обещай пользу в первой фразе: «за 3 секунды покажу, как…». Визуальный крючок: движение, крупный план, необычный первый кадр. Первый кадр должен работать даже без звука — 90% смотрят немую ленту.' },
      { h: 'Что убивает хук и как проверить', body: '«Привет, ребята, сегодня поговорим…» — минус половина зрителей. Долгая заставка, логотип, раскачка на 5 секунд. Обещание, которое не совпало с содержанием, — люди уходят и не верят. Проверь: посмотри первые 3 секунды без звука — возникает вопрос «а что дальше?».' }
    ],
    lifehack: 'Смотри метрику удержания (retention): резкая просадка в первые секунды — это слабый хук, и именно его надо переснять, а не весь ролик. Часто замена только первых 3 секунд поднимает охваты в разы — начало это рычаг с самой высокой отдачей.',
    links: [
      { label: 'CapCut', url: 'capcut.com', note: 'Собрать визуальный хук: движение, крупный план, текст-крючок с первого кадра' }
    ]
  },
  61: {
    intro: 'Тизинг — дразнить, приоткрывать, но не отдавать всё сразу. Мозг не может бросить историю без развязки, и это держит до конца. А глубина просмотра напрямую влияет на охваты.',
    notes: [
      { h: 'Механика интриги', body: 'Пообещай ценное впереди: «в конце покажу приём, который удвоил охваты». Открытая петля: задай вопрос в начале, ответь в финале. Веди по нарастающей — каждый следующий кадр интереснее предыдущего. Незакрытый вопрос в голове зрителя сильнее любого призыва «досмотри».' },
      { h: 'Дозируй информацию', body: 'Не вываливай всё в первые секунды — оставь причину досмотреть. 5–10 секунд подогрева интриги, дальше выдавай суть, не пережать. Слишком долго тянуть — зритель почувствует обман и уйдёт. Тизинг это баланс: держать интерес, но не мучить ожиданием.' },
      { h: 'Приёмы и честность', body: 'Клиффхэнгер перед ключевым моментом: «а теперь самое важное…». Покажи результат, но не способ — способ в конце. Намёк вместо ответа: «есть один нюанс, о нём в конце». Но всегда закрывай обещание: обещал приём — покажи приём. Интрига без пользы бесит и жжёт доверие.' }
    ],
    lifehack: 'Ставь открытую петлю в первой фразе и закрывай её строго в финале: «в конце — приём, который удвоил мне охваты». Мозг зрителя не терпит незавершённости и досматривает ради ответа. Главное — реально дать обещанное, иначе следующий ролик уже не досмотрят.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Придумать открытые петли и клиффхэнгеры под конкретную тему ролика' }
    ]
  },
  62: {
    intro: 'Мозг верит тому, что может почувствовать: вкус, запах, звук, текстуру. «Вкусный кофе» слабее, чем «горчинка и пар над чашкой утром». Чем больше чувств задействуешь, тем ярче картинка в голове читателя.',
    notes: [
      { h: 'Сенсорные детали', body: 'Опиши звук, запах, тактильность — читатель достраивает сцену сам. Конкретная деталь вместо общего слова: не «уютно», а «плед и запах корицы». Одна яркая деталь заменяет три общих прилагательных. Воображению нужна зацепка, а не оценка «красиво» или «вкусно».' },
      { h: 'Динамика и свой голос', body: 'Чередуй короткие и длинные фразы — текст дышит, как речь: рубленые ускоряют, длинные замедляют. Узнаваемый стиль — то, за чем возвращаются именно к тебе: свои словечки, интонация, ритм отличают от тысяч похожих. Безликий текст под всех не запоминается.' },
      { h: 'Вовлечение действием', body: 'Задай вопрос читателю прямо в тексте — он мысленно отвечает. Проси представить: «представь запах свежей выпечки» — он уже вовлечён. Микро-действия по ходу: «поставь + если узнал себя». Сухой текст без деталей и монотонный ритм усыпляют, вовлечение будит.' }
    ],
    lifehack: 'Правило «покажи, не называй»: вместо оценки («уютно», «вкусно», «дорого») давай деталь, которую можно почувствовать — «плед и запах корицы», «горчинка и пар над чашкой». Читатель сам достраивает сцену и проживает её, а прожитое запоминается и продаёт.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Заменить общие оценки на сенсорные детали (звук, запах, текстура)' }
    ]
  },
  63: {
    intro: 'Триггер — эмоциональный крючок, который переводит человека из «интересно» в «беру сейчас». Работает не на логике, а на чувстве, и не создаёт потребность, а усиливает уже существующую.',
    notes: [
      { h: 'Восемь рабочих триггеров', body: 'Толпа (как все) и эксклюзив (не как все) — две стороны принадлежности. Дефицит (осталось мало) и халява (бесплатно) — про упущенную выгоду. Авторитет и статистика — весомое мнение и цифры вместо обещаний. Плюс страх и новизна. Восемь рычагов, которые подталкивают к «да».' },
      { h: 'Почему они работают', body: 'Мозг экономит энергию и решает по ярлыкам, а не взвешивает всё. «Раз все берут — значит, стоит»: социальное доказательство дешевле анализа. И страх потерять сильнее радости приобрести — это неприятие потери. Триггеры бьют в эти встроенные механизмы, а не в разум.' },
      { h: 'Триггер живёт на пользе', body: 'Сначала ценность в контенте, потом крючок — не наоборот. «Осталось 2 места» на пустой продукт = разовая продажа и минус доверие. Оптимум 2–3 триггера на пост, не все восемь: перебор превращает текст в базарный зазыв. Выбирай под боль и стадию сегмента, а не по вкусу.' }
    ],
    lifehack: 'Подбирай триггеры под стадию клиента: холодным — новизна и толпа (снять недоверие), тёплым — кейсы и авторитет (доказать), горячим — дефицит и дедлайн (подтолкнуть). Один и тот же триггер на разных стадиях либо стреляет, либо отпугивает — важно попасть в момент.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Подобрать 2–3 триггера под сегмент и стадию воронки без перебора' }
    ]
  },
  64: {
    intro: 'Толпа и эксклюзив — две стороны одной потребности: где я в группе, со всеми или над всеми. Толпа снимает страх ошибки («раз все выбрали — безопасно»), эксклюзив даёт статус избранности.',
    notes: [
      { h: 'Как включить толпу', body: 'Показывай числа: «уже 3 200 человек прошли», «500 отзывов». Живые лица клиентов работают сильнее абстрактного «многие». Очередь, аншлаг, «места разбирают» — визуальная толпа. Формулировки: «Пока ты думаешь, 800 человек уже внутри», «Все твои конкуренты уже это делают».' },
      { h: 'Как включить эксклюзив', body: '«Только для своих», закрытый чат, отбор по анкете. Ограничь вход не ценой, а условием: не всем продаём. Статусные детали: ручная сборка, лимитная серия, именной доступ. «Мы берём не всех — сначала смотрим, подойдём ли» — барьер, который повышает желание попасть.' },
      { h: 'Что когда и типичные ошибки', body: 'Массовый недорогой продукт — жми на толпу; премиум и личная работа — на эксклюзив; новичок боится ошибиться, его успокаивает толпа. Ошибки: «нас выбирают тысячи» без доказательств, фейковые счётчики «онлайн: 47» (палятся), «клуб» с открытой дверью — эксклюзив без реального барьера это обман.' }
    ],
    lifehack: 'Выбирай триггер по цене продукта: дешёвый и массовый — дави на толпу (числа, отзывы, «все берут»), дорогой и личный — на эксклюзив (отбор, «не для всех»). Перепутаешь — толпа обесценит премиум, а эксклюзив отпугнёт от массового. Цена подсказывает, какой рычаг тянуть.',
    links: [
      { label: 'Canva', url: 'canva.com', note: 'Собрать визуальную «толпу»: карточки отзывов, счётчики, коллаж клиентов' }
    ]
  },
  65: {
    intro: 'Дефицит и халява — два полюса упущенной выгоды. Дефицит давит на страх упустить («скоро не будет»), халява снимает риск («попробуй бесплатно, ничем не рискуешь»). Классическая пара воронки.',
    notes: [
      { h: 'Как честно создать дефицит', body: 'Ограничивай реально: число мест, срок, тираж — то, что правда конечно. Дедлайн настоящий: сгорел — значит сгорел. Масштаб под аудиторию: не «500 мест» при 100 подписчиках. Виды: количественный (осталось 3), временной (цена растёт в полночь), бонусный (подарок первым 20).' },
      { h: 'Халява как вход в воронку', body: 'Бесплатное — это лид-магнит, первый контакт, а не финал. Ценность халявы должна быть настоящей, иначе платному не поверят. «Бесплатно» снимает страх, и человек делает первый маленький шаг. Халява без ценности обесценивает и платный продукт — не раздавай пустышки.' },
      { h: 'Связка и ошибки', body: 'Сначала бесплатный шаг, потом ограниченное платное: «Забрал гайд? Первым 15 — скидка до пятницы». Халява греет, дефицит закрывает. Ошибки: вечный «последний день» (перестают верить), дефицит без причины (читается как манипуляция), халява без ценности.' }
    ],
    lifehack: 'Строй связку «лид-магнит → ограниченный оффер»: дай бесплатную реальную пользу, а следом — платное предложение с честным дедлайном для тех, кто её забрал. Тёплый после халявы человек покупает в разы охотнее холодного, а настоящий срок снимает «подумаю потом».',
    links: [
      { label: 'Тильда', url: 'tilda.cc', note: 'Собрать лид-магнит и оффер с настоящим таймером дедлайна' }
    ]
  },
  66: {
    intro: 'Конкретная цифра звучит правдивее любого прилагательного, а мнение эксперта экономит покупателю проверку. «Хорошо» — пусто, «сократил расходы на 34%» — доказательство.',
    notes: [
      { h: 'Как показать авторитет', body: 'Регалии по делу: опыт, кейсы, годы, объёмы — а не пустые титулы. Заимствованный авторитет: упоминание в СМИ, известные клиенты. Экспертный контент сам по себе строит авторитет лучше слов о себе. «10 лет в маркетинге» продавцу тортов — регалия не в тему, она не работает.' },
      { h: 'Как работать со статистикой', body: 'Бери конкретные числа: не «многие», а «7 из 10». Источник обязателен, иначе цифра выглядит выдуманной. Своя статистика (результаты твоих клиентов) сильнее чужой. Не вырывай процент из контекста: «+300%» с двух продаж до восьми — смешно и палится.' },
      { h: 'Как подавать цифры', body: 'Сравнение «до/после» — самый наглядный формат. Одно число крупно на карточке — визуальный акцент. Мини-инфографика вместо абзаца текста. Не перегружай: пять процентов в одном абзаце никто не удержит. Округление в свою пользу и «99% довольны» без источника читаются как манипуляция.' }
    ],
    lifehack: 'Замени каждое оценочное слово в тексте на цифру с источником: «много клиентов» → «184 клиента за год», «быстро» → «за 40 минут». Одна конкретная цифра снимает недоверие сильнее абзаца прилагательных — но всегда с источником, иначе она работает против тебя.',
    links: [
      { label: 'Infogram', url: 'infogram.com', note: 'Собрать наглядную инфографику цифр и «до/после» вместо абзаца текста' }
    ]
  },
  67: {
    intro: 'Кейс показывает: такой же человек получил желаемое — значит, и я смогу. Работает белая зависть «хочу так же». Ты продаёшь не свою работу, а трансформацию клиента.',
    notes: [
      { h: 'Структура сильного кейса', body: 'Точка А: боль и ситуация до — конкретно, с деталями. Путь: что сделали, коротко, без хвастовства процессом. Точка Б: измеримый результат с цифрой и сроком. Формула проста, но каждый блок должен быть предметным, а не «всё получилось».' },
      { h: 'Кого брать героем', body: 'Клиента, похожего на твою целевую аудиторию. Не самый громкий результат, а самый достоверный и близкий: «обычный человек добился» цепляет сильнее звёздного исключения. Покажи, что старт героя был хуже, чем у читателя, — тогда «и я смогу» звучит убедительнее.' },
      { h: 'Детали и ошибки', body: 'Имя, фото, реальная цитата вместо «один клиент». Конкретика: сроки, суммы, шаги. Честные сложности по пути повышают доверие. Ошибки: идеальный результат без единой трудности (не верят), кейс про тебя «я молодец», абстрактный герой «одна девушка» — нет узнавания, нет доверия.' }
    ],
    lifehack: 'Заканчивай кейс мостиком к читателю: «если ты в такой же ситуации — результат реален и для тебя, начни с того же, с чего начал он». Без этой строки кейс — просто история; с ней он превращается в «хочу так же» и ведёт к конкретному первому шагу.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Собрать кейс по схеме А→путь→Б с цифрами и мостиком к читателю' }
    ]
  },
  68: {
    intro: 'В основе триггера страха — страх потери: потерять больнее, чем радостно приобрести. Человек покупает, чтобы избежать худшего. Но страх без заботы = запугивание, которое вызывает отторжение.',
    notes: [
      { h: 'Формула «пугай с заботой»', body: 'Назови проблему честно и покажи её реальные последствия. Усугуби: к чему приведёт бездействие, если не решать. Дай выход: как будет, когда проблема закрыта. Контраст двух картин — боли от бездействия и результата после решения — и есть мотивация к действию.' },
      { h: 'Адекватность масштабу', body: 'Страх должен совпадать с реальным размером проблемы. Раздувать мелочь до катастрофы — терять доверие: «потеряешь всё» из-за одной ошибки в посте это перебор. Усиливает: конкретный сценарий последствий, близкий читателю, и мысль, что цена бездействия растёт со временем.' },
      { h: 'Этика страха', body: 'Пугай только реальными рисками, а не выдуманными. За страхом всегда должно стоять честное решение — давление без выхода разрушает доверие. Не эксплуатируй глубокие травмы и уязвимость аудитории. Страх это инструмент заботы «я вижу твою проблему», а не оружие.' }
    ],
    lifehack: 'Всегда показывай две картины рядом: что будет, если ничего не менять, и что будет после решения. Один страх без выхода парализует и отталкивает, а контраст «боль → результат» мотивирует. Правило: за каждым «страшно» сразу давай «а вот как это починить».',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Выверить формулу «пугай с заботой»: проблема → последствия → выход, без токсичности' }
    ]
  },
  69: {
    intro: 'Человеку важно быть первым, кто попробовал и рассказал. Новое обещает преимущество перед теми, кто ещё не в теме, а мозг реагирует на новизну выбросом интереса — это встроено.',
    notes: [
      { h: 'Что подать как новое', body: 'Новый инструмент, метод, формат, подход к старой задаче. Новизна не обязана быть глобальной — новое для твоей ниши уже цепляет. Свежий взгляд на привычное — тоже новизна. «Первым — ранний доступ», бета, закрытый запуск: статус первопроходца, а ранние пользователи становятся адвокатами бренда.' },
      { h: 'Новизна в теме ИИ', body: 'Поле нейросетей обновляется еженедельно — вечный источник новизны. «Вышла новая модель — вот как применить её в твоём деле». Разбор свежих ChatGPT, Claude, Midjourney, Veo собирает охваты. Кто первым делает понятный разбор новинки, забирает внимание всей ниши.' },
      { h: 'Ошибки и скорость', body: '«Новое» без пользы = погоня за хайпом, быстро выгорает. Называть новым то, что давно у всех, — терять экспертность. Успеть первым, но подать сыро — репутационный минус. Следи за источниками ниши, проверяй рост интереса в Wordstat и публикуй быстро: первый разбор ценнее идеального, но позднего.' }
    ],
    lifehack: 'Держи «дежурство по новинкам»: 15 минут в день мониторь свежие инструменты в своей нише и первым делай простой разбор «что это и как применить у себя». В быстрых темах вроде ИИ роль «того, кто объясняет новое первым» приносит охваты и статус эксперта почти даром.',
    links: [
      { label: "There's An AI For That", url: 'theresanaiforthat.com', note: 'Лови свежие ИИ-инструменты раньше конкурентов — источник новизны' },
      { label: 'Яндекс Wordstat', url: 'wordstat.yandex.ru', note: 'Проверить, растёт ли спрос на новинку, прежде чем делать разбор' }
    ]
  },
  70: {
    intro: 'Вина и вдохновение — два тонких триггера. Вина подталкивает через «ты сам недорабатываешь», вдохновение тянет вперёд образом будущего. Оба сильные, но легко скатываются в токсичность или пустой пафос.',
    notes: [
      { h: 'Вина без токсичности', body: 'Не обвиняй личность, а называй привычку: «откладываешь — теряешь». Вина работает как мягкое зеркало, а не как удар. Дай сразу выход, чтобы не осталось только тяжести. Давить на комплексы, внешность и «ты плохой родитель, если не купишь» — за гранью, это вредит людям.' },
      { h: 'Вдохновлять по-настоящему', body: 'Покажи образ будущего: кем читатель станет и как это ощущается. Опирай мечту на реальный путь, а не на пустой лозунг. Вдохновение = мечта плюс первый посильный шаг к ней. Пустой пафос без конкретики приедается, а недостижимый идеал вызывает не подъём, а бессилие.' },
      { h: 'Баланс', body: 'Вина показывает цену бездействия, вдохновение — награду за движение. Здоровая связка: мягко про проблему → ярко про возможность. Заканчивай на подъёме, а не на чувстве вины: последняя эмоция поста должна двигать вперёд, а не придавливать. Вдохновение без шага красиво, но не продаёт.' }
    ],
    lifehack: 'Правило «заканчивай на подъёме»: если задел вину или проблему в начале — финал делай вдохновляющим и с конкретным первым шагом. Человек должен уходить из поста с чувством «я могу это исправить», а не «я безнадёжен» — тогда он действует, а не закрывает вкладку.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Выверить тон: вина как мягкое зеркало с выходом, вдохновение с реальным шагом' }
    ]
  },
  71: {
    intro: 'Один триггер цепляет, а связка ведёт от внимания к покупке: разные крючки закрывают разные сомнения читателя. Рабочий максимум — 2–3 триггера на пост, а не все восемь сразу.',
    notes: [
      { h: 'Каркас поста под связку', body: 'Крючок: новизна или страх ловят внимание в первой строке. Тело: польза плюс кейс и статистика как доказательство. Оффер: дефицит и халява на этапе решения. Порядок важен: сначала то, что цепляет, в середине то, что доказывает, ближе к концу то, что торопит.' },
      { h: 'Рабочие связки', body: 'Страх + решение + дефицит: пугнул, дал выход, ограничил срок. Кейс + толпа + халява: результат, «таких много», бесплатный вход. Новизна + авторитет + ранний доступ: свежее, весомо, первым. Проверенные комбинации, где триггеры усиливают, а не глушат друг друга.' },
      { h: 'Как не переборщить', body: 'Все восемь крючков в одном посте = базарный зазыв, доверие падает. Каждый триггер должен опираться на реальную пользу. Если триггеры спорят (толпа + эксклюзив в лоб) — конфликт, читатель путается. Определи ведущий под цель поста, добавь 1–2 поддерживающих, проверь, что не спорят.' }
    ],
    lifehack: 'Собирай связку по воронке: цепляющий триггер в начало (страх/новизна), доказывающий в середину (кейс/статистика), торопящий в конец (дефицит/дедлайн). Такой порядок ведёт читателя от «а что это?» к «беру сейчас» — а хаотичная куча крючков только сбивает с решения.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Собрать связку из 2–3 триггеров по воронке и проверить, что они не конфликтуют' }
    ]
  },
  72: {
    intro: 'Конкурент — не враг, а бесплатная разведка: он уже проверил спрос за свои деньги. Ты видишь, какие офферы заходят, а какие проваливаются, ещё до того, как вложился сам. Цель анализа — найти щель, куда никто не встал.',
    notes: [
      { h: 'Карта из двух осей', body: 'Строй сетку: по горизонтали товар (похож / другой), по вертикали потребность клиента (та же / другая). Из двух осей рождаются четыре клетки — от прямых до неявных. Единого «списка врагов» не бывает: каждая клетка требует своей тактики.' },
      { h: 'Прямые и косвенные', body: 'Прямые: тот же товар — та же потребность (кофейня напротив кофейни), бьётесь лоб в лоб, решает отстройка — смотри не на цену, а на то, чего у них нет: сервис, скорость, атмосфера. Косвенные: похожий товар — другой повод (кофейня и чайная), перехватывай поводы: «зашёл за чаем — попробуй наш раф».' },
      { h: 'Неявные, будущие, партнёры', body: 'Неявные: другой товар — та же потребность (с кофейней конкурирует энергетик и даже сон) — самый опасный тип, ты его не видишь в лицо. Будущие: сети и маркетплейсы, что вот-вот войдут — готовься заранее, строй лояльную базу. Партнёры: соседи по потребности (пекарня рядом с кофейней), с кем можно дружить.' }
    ],
    lifehack: 'Нарисуй матрицу 2×2 (товар × потребность) и расставь по клеткам реальных игроков. Пустая клетка на карте — это и есть твоя ниша, свободная щель, куда никто не встал. Так стратегия рождается из наглядной схемы, а не из интуиции «у меня нет конкурентов».',
    links: [
      { label: 'Miro', url: 'miro.com', note: 'Собрать матрицу конкурентов 2×2 и увидеть свободные клетки рынка' },
      { label: 'Popsters', url: 'popsters.ru', note: 'Аналитика постов конкурентов: что у них заходит, а что проваливается' }
    ]
  },
  73: {
    intro: 'Главная ошибка новичка — «откупиться пользой»: вываливает максимум бесплатного, и клиент уходит сытым, но без покупки. Бесплатное должно решать маленькую задачу, платное — большую.',
    notes: [
      { h: 'Принцип «немного пользы»', body: 'Дай бесплатно или за минимальный чек первый шаг — быстрый, осязаемый результат. Клиент почувствовал «работает» → сам захотел следующий шаг за деньги. Пример: бесплатный разбор одного Reels → платный монтаж серии. Много пользы даром обесценивает продукт и приучает не платить.' },
      { h: 'Каскад продукта', body: 'Каждая покупка рождает новую потребность — встраивайся на каждом этапе. Купил камеру → нужен свет → нужен монтаж → нужен продвиженец. Продавай не один продукт, а лестницу решений по пути клиента. Начинай с востребованного: не изобретай спрос, заходи с тем, что уже покупают.' },
      { h: 'Критерии успеха продукта', body: 'Продукт работает, если растёт статистика И растёт доход — оба сразу. Лайки без денег — тщеславие, деньги без охватов — потолок. Мнения людей не из твоей ЦА искажают картину, их в игнор. Проверяй спрос цифрами, а не ощущением, что «тема интересная».' }
    ],
    lifehack: 'Раздели пользу на «что делать» и «как именно»: бесплатно показывай ЧТО нужно сделать (это цепляет и доказывает экспертность), а КАК именно, под ключ и с сопровождением — продавай. Тогда халява греет и ведёт к покупке, а не насыщает клиента до состояния «мне и так хватит».',
    links: [
      { label: 'Notion', url: 'notion.so', note: 'Расписать каскад продукта: первый бесплатный шаг и платные ступени за ним' }
    ]
  },
  74: {
    intro: '«У меня нет конкурентов» — это слепая зона, а не преимущество. Конкурент — любой, кто забирает деньги и внимание твоей аудитории. Начнём с двух самых очевидных типов: прямых и косвенных.',
    notes: [
      { h: 'Прямые конкуренты', body: 'Тот же товар и та же цель клиента — бьётесь за одного покупателя. Пример: два курса по таргету ВК для новичков с нуля. Клиент выбирает по цене, доверию, кейсам. Прямых обходишь не демпингом, а доверием: кейсы, отзывы, репутация решают больше скидки.' },
      { h: 'Косвенные конкуренты', body: 'Похожий товар, но другая цель клиента. Пример: курс по таргету и курс по SMM-ведению — оба про соцсети, результат разный. Клиент уходит к ним, если перепутал свою задачу. Косвенных обходишь ясностью оффера: чётко показываешь, какую именно задачу решаешь ты.' },
      { h: 'Как собрать список', body: 'Вбей свой запрос в поиск — смотри, кого показывают рядом. Пройди путь клиента: что он ищет до и после покупки. Собери по 5–7 игроков в каждую категорию, не больше — иначе утонешь в анализе. Цель не копировать лидера, а найти свободную клетку на карте.' }
    ],
    lifehack: 'Отстройка по формуле «то же самое, но для конкретного сегмента»: не «курс по таргету», а «таргет для мастеров маникюра». Ты не воюешь с лидерами в лоб, а становишься первым выбором для узкой группы — там у тебя нет прямых конкурентов, и доверие строится быстрее.',
    links: [
      { label: 'SimilarWeb', url: 'similarweb.com', note: 'Посмотреть трафик и источники конкурентов — кто реально борется за твою аудиторию' }
    ]
  },
  75: {
    intro: 'Прямые и косвенные конкуренты видны сразу, а деньги утекают к невидимым. Неявные, будущие и партнёры — те, кого новичок не считает конкурентами, но именно они забирают бюджет клиента в нужный момент.',
    notes: [
      { h: 'Неявные конкуренты', body: 'Другой товар, но та же потребность клиента. Человек хочет «больше клиентов» и выбирает между таргетом и сарафаном через нетворкинг. Ты конкурируешь не с курсом, а со способом решить задачу. Перебивай их ценностью, а не спором: покажи, почему твой путь к той же цели надёжнее.' },
      { h: 'Будущие конкуренты', body: 'Сегодня их нет, но рынок притягивает новых. Как только ниша показала деньги — приходят сильные с бюджетом. Пример: ты первый делаешь ИИ-аватары, через год их делают все. Опережай скоростью и лояльной базой: пока их нет, собери аудиторию, которую потом не переманят.' },
      { h: 'Конкуренты-партнёры', body: 'Тот, кто закрывает соседний этап, а не тот же самый. Ты пишешь тексты, партнёр делает дизайн — обмениваетесь клиентами. Партнёрство даёт тёплые лиды без затрат на рекламу. Спрашивай клиента «а что вы рассматривали ещё?» — так всплывают и неявные, и потенциальные партнёры.' }
    ],
    lifehack: 'Задавай каждому клиенту вопрос «а что ещё вы рассматривали, прежде чем прийти ко мне?». В ответах всплывают твои неявные конкуренты — способы, которыми люди решают задачу помимо тебя. Зная их, ты строишь аргументы не против похожих курсов, а против «сделаю сам» и «попрошу знакомого».',
    links: [
      { label: 'Google Trends', url: 'trends.google.com', note: 'Отследить растущие тренды ниши — туда придут будущие конкуренты' }
    ]
  },
  76: {
    intro: 'Новичок думает: дам максимум бесплатно — и меня полюбят. На деле клиент наедается бесплатным и не доходит до покупки. Ты своими руками обесцениваешь собственную экспертизу.',
    notes: [
      { h: 'Что такое немного пользы', body: 'Дай кусочек результата, который реально работает, — тест-драйв: клиент почувствовал вкус, но задача не закрыта. Бесплатное показывает «что делать», платное отвечает «как именно». Граница простая: бесплатно — один быстрый результат или разбор одной ошибки, платно — система, сопровождение, глубина.' },
      { h: 'Как упаковать тест-драйв', body: 'Мини-аудит, чек-лист или один рабочий приём. Или символический чек 100–300 ₽ — он отсекает халявщиков. Платящий даже 1 рубль клиент ценит и слушает в разы сильнее того, кто взял бесплатно. Деньги, пусть маленькие, включают отношение «это важно, я применю».' },
      { h: 'Мостик к платному', body: 'В конце бесплатного честно: «вот что делать дальше». Покажи масштаб задачи, который в одиночку не потянуть. Правило: после бесплатного клиент должен захотеть больше, а не «мне хватит». Платит тот, кому нужен результат, а не просто информация — на него и нацеливай оффер.' }
    ],
    lifehack: 'Ставь на лид-магнит символическую цену 100–300 ₽ вместо «бесплатно». Микроплатёж отсекает халявщиков и резко повышает доходимость: человек, заплативший даже рубль, реально изучает материал и куда охотнее покупает основной продукт, чем тот, кто скачал даром и забыл.',
    links: [
      { label: 'Canva', url: 'canva.com', note: 'Оформить лид-магнит: чек-лист, мини-аудит или гайд как тест-драйв' }
    ]
  },
  77: {
    intro: 'Каждая покупка создаёт новую задачу, а значит — новый спрос. Купил сайт → нужна реклама → нужен отдел продаж. Клиент движется по лестнице задач, и на каждой ступени есть деньги.',
    notes: [
      { h: 'Логика каскада', body: 'Результат одного этапа обнажает узкое место следующего. Продал Reels — клиенту нужен прогрев и продажи с этих просмотров. Ты либо закрываешь этап сам, либо отдаёшь партнёру за процент. Вход в каскад через то, что покупают легко и часто, а не через премиум.' },
      { h: 'LTV вместо разовой сделки', body: 'Один клиент в каскаде приносит в разы больше, чем разовая продажа. Считай LTV — сколько денег даст клиент за всё время работы с тобой. Дешевле удержать и допродать, чем найти нового: привлечение стоит дорого, а следующая продажа тому же человеку почти бесплатна.' },
      { h: 'Собери свою лестницу', body: 'Выпиши путь клиента от первой боли до финального результата. Отметь, где возникает следующая потребность. Реши: закрываешь сам или через партнёра. Пример последовательности: сначала монтаж, потом ведение, потом стратегия — от простого и частого к сложному и дорогому.' }
    ],
    lifehack: 'После каждой продажи спрашивай себя: «какая задача у клиента возникнет СЛЕДУЮЩЕЙ?» — и заранее готовь под неё продукт или партнёра. Тогда ты не ищешь новых клиентов каждый раз с нуля, а ведёшь одного по лестнице, поднимая его LTV в разы дешевле, чем привлекаешь холодных.',
    links: [
      { label: 'Miro', url: 'miro.com', note: 'Нарисовать лестницу задач клиента и отметить точки следующей потребности' }
    ]
  },
  78: {
    intro: 'Новичок придумывает продукт «из головы» и вкладывается в тишину. Спрос нельзя чувствовать — его нужно измерить. Wordstat показывает, сколько людей уже ищут решение твоей задачи.',
    notes: [
      { h: 'Как читать цифры', body: 'Смотри не только частоту, но и намерение за запросом. «Как настроить рекламу» — учатся сами; «настроить рекламу под ключ» — готовы платить. Отделяй горячие коммерческие запросы от информационных. Тысячи запросов = спрос есть; десятки = рынка пока нет.' },
      { h: 'Оценка ниши', body: 'Считай сезонность: смотри динамику за год, а не один месяц. Растущий тренд — заходи, падающий — подумай. Слишком узкий спрос — рынка нет; слишком широкий — высокая конкуренция. Идеал посередине: спрос есть, но гигантов ещё не набежало.' },
      { h: 'От спроса к продукту', body: 'Формулируй продукт словами клиента из запросов — так оффер сразу попадает в то, что человек ищет. Бери запрос с явным намерением купить и делай под него оффер. Проверяй несколько формулировок: люди ищут по-разному, и одна из них окажется самой «денежной».' }
    ],
    lifehack: 'Собери из Wordstat список «коммерческих» запросов со словами «под ключ», «заказать», «цена», «купить» — это люди с деньгами и намерением, а не «почитать». Продукт, названный их же словами, продаётся сам: человек ищет ровно то, что ты предлагаешь, и не сравнивает с абстракциями.',
    links: [
      { label: 'Яндекс Wordstat', url: 'wordstat.yandex.ru', note: 'Измерить спрос и намерение: частота, сезонность, коммерческие запросы' },
      { label: 'Bukvarix', url: 'bukvarix.com', note: 'Расширить семантику: тысячи смежных запросов и подсказок для каскада' }
    ]
  },
  79: {
    intro: 'Успех — не «мне кажется, стало лучше», а измеримый факт. Два честных критерия: рост статистики и рост дохода. Всё остальное — ощущения, которые обманывают.',
    notes: [
      { h: 'Рост статистики и дохода', body: 'Смотри динамику: охваты, заявки, конверсию, подписки — тренд, а не один хороший день. Сравнивай период с периодом, а не с вчерашним настроением. Финальный судья — деньги в кассе, а не лайки: охваты без продаж это красивый тупик. И считай прибыль после расходов, а не выручку.' },
      { h: 'Мнения не из ЦА — в игнор', body: 'Друзья, коллеги, случайные комментаторы — не твои клиенты. Их «мне не нравится» ничего не говорит о спросе. Слушай только тех, кто платит или готов платить. Чужое мнение вне ЦА — самый частый источник неверных решений и брошенных рабочих гипотез.' },
      { h: 'Как принимать решение', body: 'Задай метрику заранее, до запуска гипотезы, — иначе задним числом подгонишь вывод под желаемое. Дай ей достаточно данных, не выключай на второй день. Растёт статистика и доход — масштабируй. Не растёт при честном сроке — меняй, а не уговаривай себя «ещё чуть-чуть».' }
    ],
    lifehack: 'Перед запуском любой гипотезы запиши на бумаге, какая цифра и к какому сроку будет считаться успехом. Это спасает от двух ловушек: бросить рабочее на второй день из-за нервов и тянуть мёртвое месяцами из жалости. Метрика, заданная заранее, решает за тебя холодно.',
    links: [
      { label: 'Яндекс Метрика', url: 'metrika.yandex.ru', note: 'Измерять конверсию, заявки и путь клиента — факты вместо ощущений' }
    ]
  },
  80: {
    intro: 'С одним продуктом ты теряешь тех, кому дорого или мало, — один продукт это потолок дохода. Линейка ловит клиента на его уровне бюджета: разные люди готовы платить разные деньги за один результат.',
    notes: [
      { h: 'Четыре ступени линейки', body: 'Минимум — доступный вход с первым результатом, знакомит с тобой (разбор, базовая настройка). Стандарт — основной продукт, полное решение без излишеств, сюда ведёшь большинство. Премиум — глубина, скорость, личное участие, гарантии; даже редкие покупки поднимают средний чек. Ведение — подписка, предсказуемый повторяющийся доход.' },
      { h: 'Как собрать линейку', body: 'Начни со стандарта — он должен продаваться уверенно. Отними лишнее → получишь минимум; добавь заботы, скорости и гарантий → премиум. Пристрой сверху ведение как продолжение результата. Не строй линейку с премиума: сначала работающая середина, потом края.' },
      { h: 'Зачем это в деньгах', body: 'Минимум заводит поток и снимает страх первого шага. Стандарт кормит. Премиум поднимает средний чек. Ведение растит LTV и делает доход предсказуемым, а не «то густо, то пусто». Вместе они собирают деньги со всех сегментов, а не только с тех, кому подошла одна цена.' }
    ],
    lifehack: 'Собери линейку от рабочего стандарта: возьми продукт, который уверенно продаётся, отними часть — получишь дешёвый вход, добавь заботы и гарантий — получишь премиум. Так три цены рождаются из одного проверенного продукта за час, и ты перестаёшь терять клиентов, которым «дорого» или «мало».',
    links: [
      { label: 'Notion', url: 'notion.so', note: 'Собрать продуктовую линейку: минимум, стандарт, премиум, ведение — что входит в каждый' }
    ]
  },
  81: {
    intro: 'Главный рычаг залёта — объём: 10–15 роликов в день, а не один «идеальный». Алгоритм тестирует контент на маленькой аудитории — дай ему много попыток. Один ролик лотерея, поток роликов система.',
    notes: [
      { h: 'Не гонись за шедевром', body: 'Перфекционизм убивает поток: пока полируешь один ролик, конкурент выложил десять. Готово и опубликовано лучше идеального в черновиках. Каждый ролик — гипотеза: выложил, замерил, усилил то, что зашло. Один вирусный поднимает охваты соседних — аккаунт получает доверие.' },
      { h: 'Чаты активности: как надо и как нет', body: 'Чат реально раскачивает блог, но только осмысленными действиями: развёрнутые комментарии, сохранения, дочитывания — сигнал «контент живой», похожий на органику. А «огонёчки» и пустые «класс!» — засорение: массовая накрутка одинаковых реакций читается как спам и роняет средние показатели.' },
      { h: 'Telegram — игра вдолгую', body: 'Закуп рекламы в Telegram окупается на объёме и дистанции. Один разовый пост редко отбивается — считай серию закупов как систему. Бери объёмом каналов, а не одним крупным: диверсифицируй риск. И про алгоритмы решай практикой — запусти два подхода параллельно и сравни цифры за 2–4 недели.' }
    ],
    lifehack: 'Считай каждый ролик гипотезой, а не произведением: цель дня — не «снять шедевр», а выложить 10–15 единиц и замерить. Что выстрелило — разбирай и повторяй формат, остальное забывай. Объём даёт алгоритму попытки, а тебе — данные о том, что реально работает у твоей аудитории.',
    links: [
      { label: 'SMMplanner', url: 'smmplanner.com', note: 'Планировать и выкладывать поток роликов по расписанию, а не вручную' }
    ]
  },
  82: {
    intro: 'Алгоритм 2026 смотрит на удержание и досмотры, а не на лайки. Репосты и сохранения весят больше, а первые часы после публикации решают судьбу ролика. Разберём, как играть по этим правилам.',
    notes: [
      { h: 'Метрики, что смотреть', body: 'Удержание в процентах и среднее время просмотра — главное. Дочитывания до призыва показывают силу концовки. Репосты и сохранения — сигнал ценности. Стабильность важнее редких всплесков: публикуй регулярно, алгоритм любит ритм, и тестируй окна времени под свою аудиторию.' },
      { h: 'A/B хуков и обложек', body: 'Готовь 3 варианта хука на один ролик. Меняй за раз только одно — иначе не поймёшь причину. Тестируй обложки: первый кадр решает клик. Систематический A/B превращает контент из угадайки в управляемый процесс, где ты знаешь, что и почему сработало.' },
      { h: 'Мультиплатформа', body: 'Один сценарий → Reels, Shorts, TikTok. Адаптируй под площадку, а не тупо копируй. Убирай чужие водяные знаки — их душат алгоритмы. Закрепляй лучший комментарий (продолжает воронку), отвечай ролик-ответом на частые вопросы: вовлечение в комментах поднимает охваты.' }
    ],
    lifehack: 'Снимай один сценарий и раскладывай на три площадки (Reels, Shorts, TikTok), адаптируя формат, но не копируя с чужим водяным знаком. Ты утраиваешь охват с одной единицы контента — а алгоритмы каждой площадки дают свой шанс на залёт тому же ролику.',
    links: [
      { label: 'Buffer', url: 'buffer.com', note: 'Публиковать один ролик сразу на несколько площадок из одного окна' },
      { label: 'Metricool', url: 'metricool.com', note: 'Смотреть удержание, время просмотра и лучшие окна публикации' }
    ]
  },
  83: {
    intro: '10–15 роликов в день — не спам, а масса, которую алгоритм обязан кому-то показать. Чат активности даёт стартовый импульс в первые 30–60 минут, а дальше ролик едет сам. Но импульс должен быть живым.',
    notes: [
      { h: 'Правило осмысленных комментариев', body: 'Комментарий из 5–8 слов по сути ролика весит в разы больше лайка. Алгоритм читает время на комментарий и диалог в ветке как сильный сигнал. Отвечай на чужие комментарии — ветка из 3–4 ответов держит зрителя дольше. Лучше 10 живых комментариев, чем 100 огонёчков.' },
      { h: 'Огонёчки — это накрутка', body: 'Пачка эмодзи без смысла — засорение: алгоритм считывает это как искусственную активность. Массовые одинаковые реакции в первые минуты могут уронить, а не поднять ролик. Первые минуты решают, поэтому важна не скорость пустых лайков, а качество реакций.' },
      { h: 'Как выбрать чат и дисциплина', body: 'Бери чат по своей нише или смежной — реакции от целевой аудитории ценнее. Проверь активность: если все пишут только «взаимно+», толку мало. Оптимально 2–3 живых чата, а не 20 мёртвых. Заходи каждый день в одно время, отдавай активность честно — халявщиков вычисляют и выкидывают.' }
    ],
    lifehack: 'Пиши в чате активности комментарии по сути ролика на 5–8 слов и отвечай на 2–3 чужих — это создаёт живые ветки, которым алгоритм доверяет. Такая «взаимка» работает как настоящая органика и поднимает охват, в отличие от пачки огонёчков, которая только роняет средние показатели.',
    links: [
      { label: 'Telegram', url: 't.me', note: 'Найти 2–3 живых нишевых чата активности с осмысленной взаимной поддержкой' }
    ]
  },
  84: {
    intro: 'Подписчик в Telegram не даёт мгновенных продаж — он греется касаниями неделями. Считай не «сколько продал сегодня», а стоимость подписчика и его окупаемость за 2–3 месяца. Канал это актив, а не разовая акция.',
    notes: [
      { h: 'Закуп решает объём', body: 'Единичная реклама почти не даёт данных — нужен объём, чтобы найти рабочие каналы. Тестируй пачкой: 10–15 размещений, потом масштабируй то, что окупилось. Один удачный канал-донор кормит подписками месяцами. Один тест вместо объёма — нет статистики, нет вывода.' },
      { h: 'Только целевые и смежные', body: 'Реклама в канал по твоей аудитории или смежной, иначе сливаешь бюджет. Смежность важнее размера: маленький точный канал бьёт большой мусорный. Проверяй донора — живые комментарии и охваты, а не только число подписчиков. Закуп в накрученные каналы это деньги ботам, они не покупают.' },
      { h: 'Считай и грей', body: 'Метрика — цена подписчика (потрачено ÷ пришедших), а не «красивый пост». Смотри удержание: сколько осталось через неделю. Веди таблицу доноров: канал, цена, подписки, отписки, вывод. Новый подписчик холодный — прогревай серией «польза → история → оффер», а не продажей в лоб.' }
    ],
    lifehack: 'Перед закупом проверяй донора в аналитике: реальные охваты, прирост, живые комментарии, отсутствие накрутки. Маленький канал с живой целевой аудиторией окупается, а большой с ботами сжигает бюджет. Один вечер проверки доноров экономит месячный рекламный бюджет.',
    links: [
      { label: 'Telemetr', url: 'telemetr.io', note: 'Аналитика Telegram-каналов для закупа: охваты, прирост, накрутка' },
      { label: 'TGStat', url: 'tgstat.ru', note: 'Проверить донора: статистика, вовлечённость, реклама у него' }
    ]
  },
  85: {
    intro: 'Оффер — это не «делаю Reels», а результат: «5 роликов, которые приведут первых клиентов». Люди платят за решение конкретной боли, а не за услугу вообще. Нет боли — нет продажи, какой бы красивой ни была подача.',
    notes: [
      { h: 'Собери оффер из своих навыков', body: 'Выпиши сильные стороны: что делаешь легко, а другим тяжело. На пересечении твоего навыка и чужой боли рождается оффер. Не пытайся уметь всё — узкий сильный навык продаётся дороже. Ищи неожиданные сегменты (мамы после родов, локальные мастера, эксперты 50+): там конкуренция ниже.' },
      { h: 'Формула сильного оффера', body: 'Кто + какая боль + какой результат + за какой срок. «Помогу маме в декрете запустить блог и получить первые 3 заказа за месяц». Конкретика в цифрах и сроке усиливает доверие в разы. Один навык переупаковывается под 5 разных аудиторий — под каждую свой точный оффер.' },
      { h: 'Первый продукт — простой', body: 'Не строй огромный курс — начни с одной услуги с понятным результатом. Простой продукт легче продать, быстрее сделать, быстрее получить кейс. Усложнять будешь потом, когда пойдут деньги и отзывы. Проверяй оффер в поле: «интересно, а сколько стоит» — попал; молчание — переупакуй боль.' }
    ],
    lifehack: 'Тестируй оффер живыми сообщениями до того, как что-то делать: напиши 10 людям из целевого сегмента и опиши результат. Ответ «а сколько стоит?» — оффер попал в боль. Молчание — переупаковывай саму боль, а не поднимай активность. Рынок отвечает деньгами, а не лайками.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Собрать оффер по формуле «кто + боль + результат + срок» и 5 переупаковок под сегменты' }
    ]
  },
  86: {
    intro: 'Первые 1–2 клиента — не про деньги, а про кейс и доверие. Без кейсов ты «ещё один новичок», с кейсами — специалист с доказательством. Отзыв и результат окупятся десятками платных клиентов.',
    notes: [
      { h: '«До и после» — обязательно', body: 'Всегда фиксируй точку старта: охваты, заявки, подписчики ДО работы. Результат без «до» не читается — цифра сравнивается только с базой. Скриншоты статистики до и после весят больше любых слов. В кейс входят: задача клиента, что сделал, цифры результата, отзыв его словами с именем и лицом.' },
      { h: 'Правило трёх кейсов', body: '3 разных кейса закрывают сомнение «а у тебя вообще получалось?». Три результата — это система, а не случайность, за неё платят дороже. Каждый новый кейс легально поднимает ценник. Один продающий экран — это где видно и проблему, и результат разом.' },
      { h: 'Не застрять в бесплатном', body: 'Договорись на берегу: бесплатно за отзыв — только 1–2 клиента, дальше платно. Пропиши срок и объём, чтобы «за отзыв» не стало вечной работой. Бесплатный клиент тоже отвечает: даёт доступы, отзыв, участвует. Держи кейсы в закрепе и обновляй — свежий продаёт лучше прошлогоднего.' }
    ],
    lifehack: 'Снимай скрин статистики клиента ДО начала работы — это половина кейса, без которой результат не читается. Новички забывают зафиксировать точку А и потом не могут доказать рост. «Было 200 охватов → стало 8000» продаёт мгновенно, а «стало 8000» без базы — пустой звук.',
    links: [
      { label: 'Canva', url: 'canva.com', note: 'Собрать кейс в один макет: скрин до, скрин после, цифры и отзыв' }
    ]
  },
  87: {
    intro: 'Холодные касания — это система, а не спам: 10–20 сообщений в день. Продажа рождается из объёма: 100 касаний → несколько диалогов → 1–2 клиента. Считай касания как метрику — нет цифры, нет результата.',
    notes: [
      { h: 'Заход через контекст', body: 'Начинай не с «куплю рекламу», а с повода: «увидел твоё объявление в чате». Тёплый вход через общий контекст убивает ощущение спама. Первое сообщение — про него, а не про тебя. Скрипт: повод + короткий комплимент по делу + вопрос, а не оффер в лоб.' },
      { h: 'Оффер-челлендж и дожим', body: 'Предложи конкретику: «7 Reels с результатом» за короткий срок — челлендж с обещанным итогом легче принять, чем «давайте посотрудничаем». После интереса не пропадай, дожимай конкретным шагом: «давай стартуем в понедельник, скину список из 3 доступов». Большинство сделок теряется не на «нет», а на молчании после «интересно».' },
      { h: 'Дисциплина касаний', body: 'Веди таблицу: кому написал, ответ, следующий шаг, дата дожима. Возвращайся к «подумаю» через 2–3 дня — половина закрывается на втором касании. Без учёта касания превращаются в хаос и слитые контакты. Система важнее вдохновения: 20 касаний в день по таблице дают предсказуемый поток клиентов.' }
    ],
    lifehack: 'Заведи простую CRM-таблицу касаний: контакт, дата, ответ, следующий шаг, дата дожима. Возвращайся к каждому «подумаю» через 2–3 дня — половина сделок закрывается именно на втором касании, а без таблицы ты про них просто забываешь и теряешь готовые деньги.',
    links: [
      { label: 'Notion', url: 'notion.so', note: 'Простая CRM касаний: статусы, следующий шаг, напоминания о дожиме' }
    ]
  },
  88: {
    intro: 'Цена — это тест, а не догма. Подними её для новых клиентов и смотри на спрос: поток заявок не упал — цена была занижена, поднимай ещё. Рынок сам показывает потолок, но только если ты пробуешь.',
    notes: [
      { h: 'Действующим — постепенно', body: 'Старым клиентам поднимай цену на 10–20%, не резко, и объясни ростом ценности: новые инструменты, лучший результат. Плавный рост удерживает базу и не вызывает бунт. Новым — сразу выше: они не знают старой цены и оценивают тебя по текущим кейсам.' },
      { h: 'Считай юнит-экономику', body: 'Прибыль с клиента = цена минус все расходы на него. В расходы входят нейронки, подписки (ChatGPT, Midjourney, ElevenLabs, монтажки), оплата подрядчику или твои часы. Раздели общие подписки на число клиентов — получишь честную себестоимость единицы. Красивая цена при минусовой экономике — это работа в убыток.' },
      { h: 'Маржа и ценность', body: 'Держи маржу, при которой хочется работать, а не выживать. Ниже порога — отказывайся: дешёвый клиент часто самый токсичный. Цена ниже себестоимости — это ты платишь клиенту за право работать. Поднимай не только цену, но и ценность: кейсы, скорость, гарантия — и цену можно удваивать.' }
    ],
    lifehack: 'Посчитай реальную себестоимость одного клиента: сложи все подписки на сервисы и раздели на число клиентов, прибавь свои часы по honest-ставке. Многие новички работают в минус, не замечая этого, потому что не считают нейронки и своё время. Цифра на бумаге сразу показывает, где ты в убытке.',
    links: [
      { label: 'Google Таблицы', url: 'sheets.google.com', note: 'Калькулятор юнит-экономики: цена, расходы, себестоимость, маржа на клиента' }
    ]
  },
  89: {
    intro: '«Хочу, но не нуждаюсь» — ты рад клиенту, но не держишься за него. Когда заявок много, ты выбираешь, а не выпрашиваешь. Нужда в голосе слышна и сбивает цену, изобилие её держит.',
    notes: [
      { h: 'Наполни воронку', body: 'Изобилие не поза, а следствие потока касаний и заявок. 10–20 касаний в день дают выбор, а из позиции выбора легко говорить «нет». Пустая воронка — это страх, а страх — это прогиб под любую манипуляцию. Уверенность в переговорах начинается не с техник, а с полной воронки.' },
      { h: 'Распознай манипуляции', body: '«У других дешевле», «сделай тест бесплатно», «потом много закажу» — давление на нужду. Обещание будущих заказов вместо оплаты сейчас — красный флаг. Держи рамку спокойно: «понимаю, но моя цена такая, потому что…», не оправдывайся и не падай в цене от одного «дорого» — часто это проверка уверенности.' },
      { h: 'Отпускай не своих', body: 'Клиент, который торгуется до нуля, сожрёт нервы и не даст кейс. Спокойное «возможно, я вам не подхожу» часто возвращает клиента с уважением. Отказ неподходящему освобождает место для нужного. За спиной 3 кейса — и манипуляции перестают цеплять: ты опираешься на результат, а не на «лишь бы взяли».' }
    ],
    lifehack: 'Перед важными переговорами наполни воронку: сделай 15–20 касаний, чтобы за спиной были другие заявки. Когда клиент не единственный, «дорого» и «у других дешевле» перестают пугать — ты говоришь из позиции выбора. Изобилие в голосе слышно, и оно само удерживает цену.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Отработать спокойные ответы на «дорого», «у других дешевле», «потом закажу»' }
    ]
  },
  90: {
    intro: 'Терпила теряет и деньги, и нервы — границы защищают и то, и другое. Правила игры проговариваются до старта, а не в разгар конфликта. Уважение начинается там, где ты сам уважаешь свои условия.',
    notes: [
      { h: 'Прописывай сроки и объём', body: 'Заранее фиксируй: сколько правок, какой объём, какие сроки. «Безлимит правок» — ловушка, из которой не выбраться. Чёткие рамки в переписке — защита от «а переделай ещё раз». Устных договорённостей не существует: есть только зафиксированные, и при споре решает сохранённая переписка, а не память.' },
      { h: 'Отпусти токсичного', body: 'Клиент, который унижает и выносит мозг, дороже своих денег: один токсик съедает время трёх нормальных. Иногда лучший ход — вернуть деньги и закончить. Возврат — не поражение, а управление репутацией и нервами: лучше разойтись спокойно, чем получить публичный негатив.' },
      { h: 'Антикризисный план', body: 'Заранее реши, при каких условиях делаешь возврат и как расстаёшься. Держи шаблон спокойного финального сообщения без эмоций. Кризис проходят те, у кого есть правила, а не паника. Готовый сценарий на конфликт превращает стресс в процедуру: достал шаблон, отправил, закрыл вопрос.' }
    ],
    lifehack: 'Пропиши в начале каждого проекта число правок, объём и сроки одним сообщением и получи «ок» клиента. Это одно сообщение снимает 90% будущих конфликтов: любое «переделай ещё раз» упирается в зафиксированную рамку, и ты защищён без ссор и выяснений.',
    links: [
      { label: 'Google Документы', url: 'docs.google.com', note: 'Шаблон договорённостей: объём, правки, сроки, условия возврата — фиксируй письменно' }
    ]
  },
  91: {
    intro: 'Правило №1 всей темы: сначала задача, потом модель. Все нейросети — это 7 семей: текст, картинки, видео, код, озвучка, музыка, автоматизация. Понял, к какой семье относится задача, — полдела сделано.',
    notes: [
      { h: 'Текст, картинки, видео', body: 'Текст: ChatGPT — универсал; Claude — король больших текстов и кода; Gemini — свежий поиск и связка с Google. Картинки (тут крутятся деньги): Midjourney — эстетика и «вау»; Flux — фотореализм почти бесплатно (~5¢ за кадр); Nano Banana — правки и текст прямо на картинке. Видео: Veo — качество и звук; Kling — кино за копейки (~10¢/сек); Runway — маркетинг и монтаж.' },
      { h: 'Озвучка, музыка, код', body: 'Озвучка: ElevenLabs — премиум, но локальные нейро-TTS бесплатны и безлимитны (бренд-озвучка OKO — именно так, без кредитов). Музыка: Suno — трек по одному запросу. Код без программиста: Cursor, Lovable, Claude Code — описываешь словами, получаешь рабочее приложение, MVP за вечер вместо недель.' },
      { h: 'Сначала задача, потом модель', body: 'Новичок хватает модную нейронку и ищет, куда приткнуть, — так сгорают деньги и недели. Сформулируй результат словами: «обложка Reels 1080×1920», «письмо на 800 знаков», «10 сек ролика». Инструмент выбираешь ПОД это. Одна задача — один класс модели: не жди картинку от ChatGPT, а монтаж от Suno.' }
    ],
    lifehack: 'Заведи личную табличку «задача → модель → цена за результат» и заполняй её по мере работы. Через неделю у тебя своя карта нейросетей вместо чужих советов, и ты берёшь под задачу правильные 3 модели, а не пытаешься знать все 50. Деньги приходят второму, а не первому.',
    links: [
      { label: 'LMArena', url: 'lmarena.ai', note: 'Живой рейтинг текстовых моделей вслепую — какая реально сильнее сейчас' },
      { label: "There's An AI For That", url: 'theresanaiforthat.com', note: 'Каталог нейросетей по задачам — подобрать модель под конкретную семью' }
    ]
  },
  92: {
    intro: 'Формула сильного промпта: Роль → Задача → Контекст → Формат → Ограничения. Слабый промпт — «напиши пост». Сильный закрывает все блоки. Чем меньше модель догадывается, тем точнее результат.',
    notes: [
      { h: 'Роль, задача, контекст', body: '«Ты маркетолог с 10-летним опытом» — роль включает нужный пласт знаний. Задача — глаголом: напиши, сравни, разложи, предложи 5 вариантов; одна задача = один промпт. Контекст решает всё: дай продукт, аудиторию, цену, тон бренда. Модель не телепат — что не сказал, то она выдумает. Вставляй свои примеры — стиль скопируется.' },
      { h: 'Формат и ограничения', body: 'Проси конкретно: таблица, нумерованный список, JSON. Ограничь объём: «до 500 знаков», «ровно 3 варианта». Негативные ограничения убирают то, что бесит: «без воды, без вступлений, без клише». Формат экономит время на переделке, ограничения — на чистке канцелярита и штампов.' },
      { h: 'Few-shot и рассуждение', body: 'Few-shot: дай 2–3 пары «вход → выход» прямо в промпте — модель копирует паттерн точнее любых описаний, это лучший способ передать стиль. Chain-of-thought: «рассуждай по шагам» резко поднимает точность на логике. Сложную задачу дели на этапы: сначала план, потом исполнение, потом самопроверка.' }
    ],
    lifehack: 'Сохрани в заметки «рыбу» промпта: «Ты [роль]. Задача: [что]. Контекст: [факты]. Формат: [как]. Пример: [образец]. Ограничения: [чего нельзя]». Подставляй под каждую задачу — скорость и качество вырастают в разы, а через 20 запросов формула станет автоматической.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Отработать формулу «роль-задача-контекст-формат-ограничения» на своих задачах' },
      { label: 'Learn Prompting', url: 'learnprompting.org', note: 'Бесплатный курс по промпт-инжинирингу: few-shot, chain-of-thought и приёмы' }
    ]
  },
  93: {
    intro: 'Правило №1: сначала задача. Новичок делает наоборот — лезет в модную нейронку, потом ищет, куда её приткнуть, и жжёт деньги. Сформулируй результат словами, инструмент выбирай ПОД него.',
    notes: [
      { h: 'Карта моделей 2026', body: 'Текст: ChatGPT, Claude, Gemini. Картинки: Midjourney, Flux, Nano Banana. Видео: Veo, Kling, Runway. Код: Cursor, Claude Code, Lovable. Озвучка: ElevenLabs или бесплатные локальные TTS. Музыка: Suno. Автоматизация: n8n. Держи эту карту перед глазами — она экономит часы метаний.' },
      { h: 'Цена решает не меньше качества', body: 'Топ-модель на каждый чих — путь в минус. Считай стоимость ОДНОГО результата, а не месячную подписку. Секунда видео в Kling ~$0.10, у Veo и Runway дороже в разы. Черновик — на дешёвой модели, финал — на дорогой. Тестируй гипотезу на самой дешёвой, добивай качество на топовой только когда идея зашла.' },
      { h: 'Как сопоставить задачу и модель', body: 'Спроси три вещи: какой формат на выходе, какое качество нужно клиенту, сколько это стоит за штуку. Клиенту в блог хватит Flux, на билборд — Midjourney: разница в требованиях и цене, а не в «крутости». Гугли «лучшая нейросеть для [задача] 2026», а не «топ нейросетей вообще».' }
    ],
    lifehack: 'Работай по правилу «черновик на дешёвой, финал на дорогой»: обкатывай идею на самой дешёвой модели, а топовую подключай только когда гипотеза зашла. На потоке из ста результатов это разница между прибылью и работой в минус — цена за штуку важнее красоты подписки.',
    links: [
      { label: 'OpenRouter', url: 'openrouter.ai', note: 'Видеть цену за запрос по десяткам моделей рядом и выбирать оптимум цена/качество' }
    ]
  },
  94: {
    intro: 'Шесть блоков сильного промпта: Роль + Задача + Контекст + Формат + Примеры + Ограничения. Пропустил блок — нейронка додумывает сама и выдаёт средний по палате результат. Это инженерия запроса, а не трюк одной модели.',
    notes: [
      { h: 'Роль, задача, контекст', body: 'Роль: «ты копирайтер прямого отклика с 10-летним опытом» — включает нужный слой знаний. Задача: глагол + конкретный результат, не «помоги с текстом», а «напиши 3 заголовка для лендинга». Контекст — топливо ответа: кто аудитория, что за продукт, цель, что уже пробовали. 5 строк вводных экономят 5 итераций правок.' },
      { h: 'Формат и ограничения', body: 'Формат: скажи, как хочешь ответ — таблица, список из 5, JSON, пост на 600 знаков. Ограничения: «без воды», «без эмодзи», «только факты из моего текста», «не длиннее 3 абзацев». Формат экономит время на переделке, ограничения убирают канцелярит, штампы и галлюцинации.' },
      { h: 'Примеры делают магию', body: 'Даже один-два примера показывают тон и структуру лучше любого описания. Дай образец «вот так пишу я» — модель подхватит стиль вместо усреднённого ИИ-голоса. Примеры — самый недооценённый рычаг качества, мостик к few-shot из следующего урока.' }
    ],
    lifehack: 'Держи под рукой шаблон-рыбу из шести блоков и заполняй его под каждую задачу, ничего не пропуская. Именно пропущенный блок (чаще всего контекст или ограничения) превращает сильный запрос в «средний по палате». Полная рыба даёт стабильный результат с первого раза, а не с пятого.',
    links: [
      { label: 'Prompting Guide', url: 'promptingguide.ai', note: 'Справочник приёмов промптинга с примерами под каждый блок формулы' }
    ]
  },
  95: {
    intro: 'Первый ответ модели — черновик, а не приговор. Профи не переписывает промпт с нуля, а докручивает итерациями. Плюс два рычага без смены модели: «думай пошагово» и обучение на примерах (few-shot).',
    notes: [
      { h: '«Думай пошагово» и few-shot', body: 'Фраза «думай пошагово» / «рассуждай вслух» заставляет модель считать, а не угадывать — особенно для логики, расчётов, разбора кейсов. Few-shot: дай 2–3 примера «вопрос → ответ», модель копирует ПАТТЕРН (длину, тон, структуру) — идеально для однотипной пачки контента. Zero-shot быстро и грязно, few-shot ровно от штуки к штуке.' },
      { h: 'Негативы: чего НЕ надо', body: 'Негативы отсекают мусор: «без вводных абзацев», «не используй слово „уникальный“», «не выдумывай факты». В картинках то же: «без лишних пальцев, без текста на изображении». Один точный запрет экономит три итерации — веди личный список того, что модель повторяет назойливо.' },
      { h: 'Итерации: победа с 3-й попытки', body: 'Докручивай в одном окне: «сделай короче», «добавь цифру», «убери канцелярит» — модель помнит контекст и правит прицельно. Норма — 3–5 итераций до сильного результата. Каждая реплика — маленький толчок, а не новый огромный промпт. Кто бросает после первой, тот и жалуется, что «нейронка тупая».' }
    ],
    lifehack: 'Собери 2–3 своих лучших результата в шаблон few-shot и заведи файл «мои негативы» и «мои фразы доводки». Вставляй их готовыми — и новые задачи выходят сильными с первого раза. Few-shot плюс итерации вместе дают то, ради чего всё затевалось: стабильное качество на скорости потока.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Длинный диалог с памятью контекста — идеален для few-shot и пошаговой доводки' }
    ]
  },
  96: {
    intro: 'Профи не молится на одну нейронку. Рабочее место — связка ChatGPT + Claude + Gemini в трёх вкладках. У каждой свои сильные стороны: гоняешь задачу туда, где она решается лучше и дешевле.',
    notes: [
      { h: 'У каждой своя роль', body: 'ChatGPT — быстрый универсал на 80% мелких задач: идеи, брейншторм, черновики. Claude — большие документы, аккуратные длинные тексты и код, держит структуру и контекст (Claude Code для проектов посерьёзнее). Gemini в связке с поиском Google даёт свежие данные и разбор картинок/таблиц.' },
      { h: 'Гоняй задачу по кругу', body: 'Ресёрч в Gemini → структура и черновик в Claude → быстрая шлифовка и варианты в ChatGPT. Или наоборот, под задачу. Смысл в том, что ни одна модель не делает всё лучше всех. Копируешь результат между вкладками руками — это и есть «рабочее место», а не магия одной кнопки.' },
      { h: 'Твой сетап на старте', body: 'Три вкладки в браузере, free-планы для теста, апгрейд той модели, что реально тянет твой поток. Заметки с рыбами-промптами и списком «какую задачу куда отправлять». Через неделю такой связки чужие споры «какая нейронка лучшая» будут смешить: лучшая — под задачу.' }
    ],
    lifehack: 'Держи три вкладки открытыми постоянно и распредели роли: Gemini — узнать что сейчас, Claude — обдумать и написать длинное, ChatGPT — быстро накидать варианты. Одна задача, прогнанная по связке, выходит сильнее, чем выжатая из одной модели до упора — потому что каждая добавляет своё.',
    links: [
      { label: 'ChatGPT', url: 'chatgpt.com', note: 'Быстрый универсал: идеи, брейншторм, черновики — основная вкладка' },
      { label: 'Claude', url: 'claude.ai', note: 'Длинные тексты, код, аккуратная структура — вкладка для глубокой работы' },
      { label: 'Gemini', url: 'gemini.google.com', note: 'Свежие данные из поиска Google, разбор картинок и таблиц' }
    ]
  },
  97: {
    intro: 'Агрегатор — одна платформа и один ключ, через которые доступны десятки моделей сразу. Не нужна подписка в каждом сервисе: платишь в одном месте, переключаешь модели кнопкой. Два рабочих — Higgsfield и OpenRouter.',
    notes: [
      { h: 'Higgsfield и OpenRouter', body: 'Higgsfield собирает под одной крышей генерацию картинок, видео и 3D, включая топовые Veo, Kling — удобно, когда делаешь ролик целиком, не прыгая по пяти сайтам. OpenRouter даёт доступ к ChatGPT, Claude, Gemini и десяткам LLM через единый API-ключ — идеально для автоматизаций и кода: подключил один ключ, гоняешь любую модель.' },
      { h: 'Экономика агрегатора', body: 'Платишь по факту использования, а не за пять подписок, половина которых простаивает. Легко сравнить цену моделей рядом и выбрать оптимум качество/стоимость под конкретный кадр или текст. Для потока это дисциплинирует расходы: видишь, куда утекают деньги, и режешь дорогое.' },
      { h: 'Агрегатор против прямого доступа', body: 'Прямой доступ иногда даёт самые свежие фичи на день раньше и максимальные лимиты. Агрегатор выигрывает в удобстве, единой оплате и скорости переключения. Правило: постоянную основную модель бери напрямую, всё разнообразие и эксперименты — через агрегатор. Не выбирай раз и навсегда.' }
    ],
    lifehack: 'Держи связку «основное напрямую + разнообразие через агрегатор»: модель, в которой работаешь каждый день, бери напрямую (свежие фичи и лимиты), а всё, что дёргаешь эпизодически, гоняй через один ключ агрегатора. Так ты не плодишь пять простаивающих подписок и видишь реальные траты.',
    links: [
      { label: 'OpenRouter', url: 'openrouter.ai', note: 'Все текстовые LLM через один API-ключ с ценой за запрос — для кода и автоматизаций' },
      { label: 'Higgsfield', url: 'higgsfield.ai', note: 'Картинки, видео и 3D под одной крышей: Veo, Kling и пресеты движения камеры' }
    ]
  },
  98: {
    intro: 'На одном ролике цена модели незаметна, на сотне — это разница между прибылью и работой в минус. Экономия не про жадность, а про то, чтобы масштабироваться и не разориться на подписках.',
    notes: [
      { h: 'Безлимит и бесплатное', body: 'Higgsfield Ultra (~$99/мес) открывает почти безлимитный доступ к топ-фото/видео — считай точку окупаемости: объём за месяц дороже подписки, значит пора на безлимит. Озвучку на потоке гони через бесплатные локальные нейро-TTS (живое качество, ноль за символ). n8n в self-hosted бесплатен: ставишь на свой сервер и автоматизируешь без платы за платформу.' },
      { h: 'Kling — самое дешёвое видео', body: 'Kling ~$0.10 за секунду — один из самых дешёвых генераторов. Черновики и массовые кадры делай на нём, дорогой Veo или Runway подключай только на витринный финал. На сотне секунд разница с топ-моделью это реальные деньги в кармане, а не абстракция.' },
      { h: 'Free-планы и учёт', body: 'Складывай free-планы стопкой: у многих бесплатный лимит покрывает тесты и мелкие задачи целиком. Реф-кредиты (пригласил друга — получил генерации) на старте это подушка. Не гони на бесплатном продакшн для клиента, но обкатывай гипотезы на free. Веди учёт трат: что окупается, что жрёт деньги впустую.' }
    ],
    lifehack: 'Раздели пайплайн на «дешёвый черновик» и «дорогой финал»: тесты — на free-планах, поток видео — на Kling, объём под безлимит Higgsfield Ultra, озвучка — бесплатные локальные TTS, автоматизация — self-hosted n8n. Дорогое подключай точечно только на витринный результат — экономный нейроделец зарабатывает больше при том же объёме.',
    links: [
      { label: 'Kling AI', url: 'klingai.com', note: 'Дешёвое видео (~$0.10/сек) для черновиков и массовых кадров' },
      { label: 'n8n', url: 'n8n.io', note: 'Self-hosted автоматизация бесплатно — ставишь на свой сервер без платы за платформу' }
    ]
  },
  99: {
    intro: 'Площадки режут охваты откровенно ИИ-контенту. Задача — чтобы ролик считывался как живой, а не как генерёжка. Голый выхлоп нейронки без обработки — прямой путь в теневой бан и нулевые просмотры.',
    notes: [
      { h: 'Как не спалить нейронку', body: 'Три кита маскировки: караоке-субтитры, живой человеческий голос и вставки реальных кадров. Синтетику выдаёт стерильный голос-робот, идеально гладкая картинка, отсутствие мелких «человеческих» деталей. Прогоняй озвучку через хумонайзер (паузы, интонация, дыхание), добавляй лёгкий шум и зерно, правь текст живой речью. Микс генерации и реального материала — золотой стандарт.' },
      { h: 'Дипфейки и чужое лицо', body: 'Чужое лицо без согласия — не «крутой приём», а прямой путь к иску и блокировке. Дипфейк допустим только со СВОИМ лицом/голосом или с письменного согласия. Публичные персоны, знаменитости, коллеги без разрешения — табу. Репутация дороже одного вирусного ролика.' },
      { h: 'Авторские и коммерция', body: 'Сгенерированное — не всегда «ничьё»: чужие узнаваемые персонажи, бренды и стили защищены. У многих сервисов бесплатный план ЗАПРЕЩАЕТ коммерческое использование результата — ловушка на старте. Делаешь для клиента за деньги — нужна лицензия на коммерцию. Храни скрины лицензий и условий тарифа: спор решает бумага, а не «я думал, что можно».' }
    ],
    lifehack: 'Перед публикацией прогони контент по чек-листу: голос живой? есть субтитры и реальные вставки? лицо своё или согласованное? лицензия на коммерцию есть? нет чужих брендов и персонажей? Прошёл — публикуй спокойно. Этика и право здесь не тормоз, а защита твоего заработка от бана и иска.',
    links: [
      { label: 'GPTZero', url: 'gptzero.me', note: 'Проверить, палится ли твой текст как ИИ — и доработать до живого перед публикацией' }
    ]
  },
  100: {
    intro: 'Картинки — это где в нейросетях крутятся деньги. Три топ-модели 2026: Midjourney — эстетика и «вау», Flux — фотореализм почти бесплатно, Nano Banana (Gemini) — правки и текст прямо на картинке.',
    notes: [
      { h: 'Анатомия промпта картинки', body: 'Порядок: Субъект → Стиль → Свет → Камера → Композиция. Порядок слов = приоритет: главное в начало. Конкретика: не «собака», а «щенок корги, 3 месяца, мокрая шерсть». Стиль: photo, 3D render, flat illustration, oil painting; эстетики editorial / cinematic / minimalist задают настроение. Коротко и конкретно бьёт длинно и расплывчато.' },
      { h: 'Свет и камера', body: 'Свет — половина кадра: golden hour, softbox, neon glow. Объектив: 85mm portrait для лиц, wide angle для пространства. Глубина резкости: shallow depth of field, bokeh. Эти «операторские» слова модель понимает и рисует кадр как настоящий фотограф, а не как любитель.' },
      { h: 'Формат и консистентность', body: 'Соотношение под площадку: 9:16 сторис, 1:1 лента, 16:9 обложка; оставляй место под текст, если кадр пойдёт в баннер. Для серии: image reference прикладывай образец стиля, character reference + фиксированный seed держат одного героя, бренд-кит (цвета, логотип) — в каждый промпт.' }
    ],
    lifehack: 'Запомни каркас промпта «Субъект → Стиль → Свет → Камера → Композиция» и ставь главное в начало — модель читает порядок слов как приоритет. Добавь один «операторский» термин про свет (golden hour) и объектив (85mm) — и любительский кадр превращается в кинематографичный без единой правки.',
    links: [
      { label: 'Midjourney', url: 'midjourney.com', note: 'Лучшая эстетика и арт-кадры для обложек и «вау»-визуала' },
      { label: 'PromptHero', url: 'prompthero.com', note: 'Библиотека промптов с примерами — подсмотреть свет, стиль, композицию' }
    ]
  },
  101: {
    intro: 'Видео — дорого и зрелищно. Veo 3 — топ-качество и родной звук, Kling 2 — лучшая цена/качество для тестов, Runway — контроль и монтаж. Для бренда почти всегда image-to-video: полный контроль первого кадра.',
    notes: [
      { h: 'Text-to-video против image-to-video', body: 'Text-to-video быстро, но результат — лотерея. Image-to-video: сначала доводишь идеальный кадр, потом «оживляешь» — полный контроль. Задаёшь первый и последний кадр, модель строит переход. Связка клипов: последний кадр одного = первый кадр следующего, так собирается бесшовный ролик из коротких генераций.' },
      { h: 'Липсинк, звук, вертикаль', body: 'Veo 3 генерирует речь и шумы прямо в кадре; отдельный липсинк — готовая озвучка + лицо героя. Звуковые эффекты под каждое движение оживляют кадр. Формат 2026 — вертикаль 9:16 (Reels, Shorts, TikTok): хук в первые 1,5 секунды, иначе свайп, субтитры обязательны — большинство смотрит без звука.' },
      { h: 'Пайплайн ролика', body: 'Сценарий → раскадровка → кадры-картинки → i2v-клипы → монтаж. Один клип 5–8 секунд: дальше модель «плывёт», теряет консистентность. Озвучка и субтитры — после сборки видеоряда. Собираешь длинное видео из коротких управляемых кусков, а не пытаешься сгенерировать всё разом.' }
    ],
    lifehack: 'Работай через image-to-video: сначала доведи идеальный первый кадр в генераторе картинок, потом оживляй его. Ты получаешь полный контроль над композицией, светом и брендом, а не играешь в лотерею text-to-video. И режь ролик на клипы по 5–8 секунд — дольше модель теряет лицо и детали.',
    links: [
      { label: 'Runway', url: 'runwayml.com', note: 'Контроль i2v, первый/последний кадр и инструменты монтажа под маркетинг' },
      { label: 'Pika', url: 'pika.art', note: 'Быстрое оживление кадров и эффекты для коротких вертикальных клипов' }
    ]
  },
  102: {
    intro: 'Нейросеть не пишет за тебя — она пишет С тобой. На пустом брифе ChatGPT и Claude выдают серую кашу. Продающий текст рождается из фактуры: твой оффер, цена, боль клиента, реальный кейс, возражения.',
    notes: [
      { h: 'Хук решает всё', body: 'Первая строка поста и первые 2 секунды Reels решают: не зацепил — дальше не читают. Рабочие хуки: цифра-обещание, разрыв шаблона, вопрос-боль, антисовет («не делай так»). Проси Claude дать 10 вариантов хука на один текст — выбираешь сильнейший, остальное в банк. Чем больше конкретики в промпте, тем меньше воды в ответе.' },
      { h: 'Структура и прогрев', body: 'Скелет: хук → усиление боли → разворот → решение → доказательство → призыв. Один пост — одна мысль; абзацы по 1–2 строки, иначе с телефона стена = свайп. Прогрев — серия касаний, снимающих возражения ДО цены: боль → своя история провала → инсайт → трансформация → мягкое предложение. Проси ИИ расписать прогрев на 7 сторис под одно возражение.' },
      { h: 'Редактура: где ИИ ломается', body: 'Нейросеть любит вводные обороты, тройки перечислений и слово «важно» — вычищай на автомате. Проверяй факты и цифры: модель уверенно придумывает статистику, которой нет. Читай вслух: спотыкаешься — переписываешь. Задавай роль, контекст, формат и запрет на канцелярит, проси сразу 3 версии под разные сегменты.' }
    ],
    lifehack: 'Никогда не бери первый ответ ИИ как финал: проси 10 вариантов хука и 3 версии текста под сегменты (новичок / сомневающийся / готовый купить), потом собирай сильнейшее вручную и вычищай ИИ-штампы («важно», тройки, вводные). Нейросеть даёт объём и черновики, продающим текст делаешь ты.',
    links: [
      { label: 'Claude', url: 'claude.ai', note: 'Выдать 10 хуков и 3 версии текста под сегменты, держать структуру длинного поста' }
    ]
  },
  103: {
    intro: 'Аватар — это ты, которого можно поставить в любую сцену. Higgsfield Soul учит цифрового двойника на 5–20 твоих фото и снимает главную боль контента: «нет времени/сил постоянно снимать себя на камеру».',
    notes: [
      { h: 'Датасет решает', body: 'Мусор на входе — мусор на выходе. Бери 10–15 фото с разных ракурсов: анфас, профиль, полуоборот, разный свет и эмоции. Только ты в кадре, лицо резкое, без фильтров, масок и очков, без других людей рядом. Разная одежда и фон — модель учит лицо, а не запоминает конкретный свитер.' },
      { h: 'Промпт для сцены — как режиссёр', body: 'Описывай кадр как режиссёр: план (крупный/средний), свет, локация, настроение, одежда. Промпт на английском и подробный — Soul любит детали композиции и света. Пример: «cinematic medium shot, soft window light, minimalist office, confident expression». Ты не проситель, а постановщик кадра.' },
      { h: 'Где деньги и где ограничения', body: 'Экспертный контент без вечных съёмок, обложки и креативы с твоим лицом, линейка сторис в разных локациях за 0 часов съёмки. Но: мелкая мимика и руки — слабое место, проверяй пальцы и глаза на каждом кадре. Двойник хорош для фото и коротких клипов, но не замена живому эфиру. Логотипы и мелкий текст модель искажает — держи за кадром.' }
    ],
    lifehack: 'Вложись в датасет, а не в промпты: 10–15 разных фото (ракурсы, свет, эмоции, только твоё резкое лицо без очков и фильтров) дают двойника, которого не отличить. Кривой аватар — почти всегда следствие ленивого набора однотипных селфи, а не слабой модели. Качество входа решает всё.',
    links: [
      { label: 'Higgsfield Soul', url: 'higgsfield.ai', note: 'Обучить цифрового двойника на своих фото и ставить себя в любую сцену' }
    ]
  },
  104: {
    intro: 'Голос — половина удержания в видео: плоская роботная озвучка убивает даже сильный сценарий. Для потока рабочая лошадка — бесплатные локальные нейро-TTS (безлимит, без кредитов), премиум-голос ElevenLabs держи для витринных проектов.',
    notes: [
      { h: 'Бесплатно и безлимит для потока', body: 'Локальные нейро-TTS (Silero, XTTS) звучат живо, работают на своём железе и стоят ноль за символ — под огромный поход роликов это именно то, что нужно, без квот и оплаты. ElevenLabs даёт премиум-эмоцию и дубляж (~$5–22/мес), но жечь платные кредиты на рядовую озвучку дорого. edge-tts не бери: робот и ошибки в ударениях.' },
      { h: 'Скрипт под озвучку пишется иначе', body: 'Короткие фразы, одна мысль — одно предложение: длинные периоды нейроголос заваливает. Пиши как говоришь: разговорные связки, паузы после сильных фраз, без причастных нагромождений. Цифры и аббревиатуры прописывай словами, чтобы TTS не прочитал их криво. Пунктуация — это режиссура пауз.' },
      { h: 'Гумонизация: убрать «робота»', body: 'Правь ударения вручную — неверный акцент главный признак дешёвой озвучки. Добавляй микропаузы перед важным и лёгкое ускорение на перечислениях. Синхронь голос с музыкой и монтажом: акцент речи ложится на смену кадра. Живая речь звучит как разговор, а не как диктор новостей.' }
    ],
    lifehack: 'Пиши скрипт «для уха», а не «для глаза»: короткие фразы, цифры словами, паузы знаками препинания, ударения проставь заранее в сложных словах. Даже бесплатный локальный TTS на таком тексте звучит живо, а самый дорогой голос на «письменном» тексте с длинными периодами всё равно выйдет роботом.',
    links: [
      { label: 'ElevenLabs', url: 'elevenlabs.io', note: 'Премиум-голос и дубляж — для витринных проектов, где нужна максимальная эмоция' },
      { label: 'TTSMaker', url: 'ttsmaker.com', note: 'Бесплатная онлайн-озвучка с русскими голосами для черновиков и потока' }
    ]
  },
  105: {
    intro: 'Музыка — невидимый сценарист эмоции: зритель не осознаёт трек, но именно он держит темп и решает, досмотрит человек или свайпнет. Suno (~$10/мес) генерит треки и джинглы под смысл ролика по текстовому промпту.',
    notes: [
      { h: 'Промпт для Suno', body: 'Описывай жанр, темп (BPM), настроение, ключевые инструменты и вокал или инструментал. Пример: «energetic lo-fi hip hop, 90 bpm, warm vinyl, no vocals, motivational». Для фона под речь бери инструментал — вокал перебивает озвучку и путает зрителя. Один и тот же видеоряд под драйв и под лирику — два разных ролика по восприятию.' },
      { h: 'Джингл — узнаваемость за 3 секунды', body: 'Джингл — короткая музыкальная подпись бренда, которую узнают ещё до логотипа. Ставь один и тот же на каждый ролик — это строит звуковую айдентику. В Suno задавай короткий формат и запоминающийся мотив в 2–4 секунды. Звук узнают быстрее картинки.' },
      { h: 'Музыка под структуру', body: 'Подбирай трек под драматургию: спокойный вход, нарастание к сути, акцент на выводе. Монтажные склейки сажай на биты — совпадение кадра с долей ощущается как «дорого». На хуке музыка тише голоса, на без-текстовых вставках выводи её вперёд. Свой трек из Suno снимает риск блокировок за чужую музыку.' }
    ],
    lifehack: 'Собери в Suno библиотеку своих треков по настроениям (драйв, лирика, деловое, тревожное) одним брифом на неделю контента. Дальше под каждый ролик берёшь готовый трек нужной эмоции за секунды — и заодно снимаешь риск теневого бана за чужую музыку в Reels и Shorts.',
    links: [
      { label: 'Suno', url: 'suno.com', note: 'Генерировать треки, джинглы и фон под смысл ролика по текстовому промпту' }
    ]
  },
  106: {
    intro: 'Контент-завод — это не один ролик, а конвейер: ты делаешь не «ролик», а повторяемую систему. Базовая цепочка: картинка → видео → озвучка → монтаж с субтитрами и инфографикой, где каждый этап отдельный инструмент.',
    notes: [
      { h: 'Кто за что отвечает', body: 'Текст и сценарий — ChatGPT/Claude; картинка — Midjourney/Flux/Nano Banana; оживление — Higgsfield/Kling/Runway; озвучка — локальные TTS или ElevenLabs; музыка — Suno; сборка, субтитры и инфографика — на монтаже, где всё сходится в один файл. Караоке-субтитры и инфографика — не украшение, а удержание: большинство смотрит без звука.' },
      { h: 'Автоматизация связок', body: 'Ручная стыковка этапов — узкое горло; связывай узлы через n8n/Make/Zapier. Шаблон сборки собрал один раз — дальше меняешь только контент, а не структуру. Пакетный подход: генеришь пачку картинок и озвучек разом, монтаж идёт по конвейеру. Скорость живёт в автоматизации рутинных стыков.' },
      { h: 'Разнообразие против штампа', body: 'Опасность конвейера — одинаковые ролики. Веди реестр приёмов и не повторяй эффекты; меняй хуки, переходы, инфографику, ракурсы — один шаблон, разное наполнение. Держи банк заготовок и чек-лист качества: сходство лица, ударения, синхрон субтитров, читаемость с телефона, сильные первые 2 секунды.' }
    ],
    lifehack: 'Собери шаблон сборки один раз и веди реестр использованных приёмов (хуки, переходы, инфографика). Конвейер даёт скорость, а реестр не даёт скатиться в клоны: каркас постоянный, наполнение всегда новое. Так завод выдаёт много и по-разному, а не сто одинаковых роликов, которые выжигают аудиторию.',
    links: [
      { label: 'Make', url: 'make.com', note: 'Визуально связать этапы пайплайна в автоматический конвейер' },
      { label: 'CapCut', url: 'capcut.com', note: 'Финальная сборка: монтаж, караоке-субтитры, инфографика в одном файле' }
    ]
  },
  107: {
    intro: 'Апскейл — это не «сделать больше», а «сделать чётче»: нейросеть не растягивает пиксели, а достраивает текстуру кожи, волос, ткани. Из мыльного 720p выходит резкое 2K/4K, и контент читается как «дорогой».',
    notes: [
      { h: 'Артефакты — враг, которого надо видеть', body: 'Типовой мусор генерации: кривые пальцы, плывущие глаза, шум, ореолы по контуру, «ватные» края. Апскейл чистит шум и мелкие артефакты, но грубый брак усиливает, а не прячет. Сначала чистка и отбраковка кадра, потом увеличение — не наоборот, иначе увеличишь ошибку.' },
      { h: 'Видео сложнее картинки', body: 'Картинку апскейлят покадрово — качество на кадр выше, времени меньше. Видео тяжелее: важна консистентность между кадрами, иначе появляется мерцание деталей. Для видео бери инструменты с временной стабилизацией, а не просто покадровый рост разрешения.' },
      { h: 'Разрешение под площадку', body: 'Reels/Shorts — вертикаль 1080×1920; гнать сюда 8K бессмысленно, площадка всё равно сожмёт. 2K/4K оправдан для обложек, превью, печати и кадров, которые разглядывают. После апскейла — лёгкая цветокоррекция и резкость; проверяй кожу: перешарп даёт пластик, недошарп — мыло. Экспортируй под кодек площадки.' }
    ],
    lifehack: 'Апскейль только на финале и только то, что уже прошло отбраковку: сначала выбрось кадры с кривыми пальцами и плывущими глазами, потом увеличивай. Апскейл усиливает и красоту, и брак — если поднять разрешение бракованного кадра, ты просто сделаешь ошибки крупнее и заметнее.',
    links: [
      { label: 'Topaz', url: 'topazlabs.com', note: 'Апскейл фото и видео с достройкой деталей и временной стабилизацией' },
      { label: 'Krea', url: 'krea.ai', note: 'Онлайн-апскейл и enhance — быстро повысить чёткость кадра' }
    ]
  },
  108: {
    intro: 'Консистентность — то, что превращает набор красивых кадров в узнаваемый бренд. Зритель узнаёт тебя в ленте за долю секунды по цвету, шрифту, свету и персонажу. Узнаваемость = доверие = продажи.',
    notes: [
      { h: 'Бренд-стиль пиши письменно', body: 'Пропиши палитру, шрифты, тип света, настроение и композицию — это твой визуальный паспорт. Вставляй эти параметры в каждый промпт: не «красиво», а конкретные цвета и свет. Один описанный стиль → десятки кадров в едином ключе без разнобоя. Каждый кадр гадать заново — путь к каше.' },
      { h: 'Персонаж, сид и референсы', body: 'Runway и референсы держат одного персонажа стабильным от кадра к кадру. Для своего лица — обученный двойник, для вымышленного — фикс референса и описания. Seed — число, фиксирующее генерацию: тот же сид + промпт = близкий результат; меняешь одну деталь при том же сиде — получаешь вариацию, а не новый кадр.' },
      { h: 'Единый стиль, но не клоны', body: 'Консистентность — про узнаваемые константы (цвет, шрифт, герой), а не про одинаковые кадры. Держи постоянным каркас, меняй наполнение: сюжет, ракурс, инфографику, хук. Перегиб — все ролики на одно лицо, зритель устаёт. Собери бренд-кит (палитра, шрифты, шаблон субтитров, джингл, референсы) и стартовый промпт-шаблон.' }
    ],
    lifehack: 'Заведи один стартовый промпт-шаблон со всеми бренд-константами (палитра, свет, шрифт, референс персонажа) и начинай каждый новый кадр не с нуля, а с него — меняя только сюжет. Через месяц твой канал узнают в ленте без логотипа по одному кадру, а это и есть доверие, которое конвертируется в деньги.',
    links: [
      { label: 'Leonardo', url: 'leonardo.ai', note: 'Консистентный персонаж, бренд-кит и фикс стиля/сида для единой ленты' }
    ]
  },
  109: {
    intro: 'Вайбкодинг — ты описываешь словами, что хочешь получить, а ИИ пишет код за тебя. Синтаксис знать не нужно: нужно уметь чётко объяснять и проверять результат глазами. Порог входа упал с месяцев учёбы до одного вечера.',
    notes: [
      { h: 'Почему это выстрелило', body: 'Модели уровня Claude и ChatGPT пишут рабочий код с первого-второго захода. Lovable и Bolt показывают живое превью прямо во время генерации. Что реально можно собрать: лендинг под оффер, приложение с базой и оплатой, телеграм-бот на ИИ, внутренние инструменты — калькуляторы, дашборды, формы. Сам сервис OKO собран терминальным Claude Code — это не игрушка.' },
      { h: 'Честная граница', body: 'Вайбкод — не «магия без понимания»: за красивым UI всё равно живой код. Для сложной логики подключаешь Cursor и учишься читать, что написал ИИ. Ты остаёшься архитектором замысла, инструмент — только руки. Правишь не строки, а формулировку задачи — как ставишь ТЗ живому разработчику.' },
      { h: 'Мышление вайбкодера', body: 'Думай продуктом и пользователем, а не строчками кода. Дроби задачу: короткие итерации, одна конкретная правка за раз. После каждого шага проверяй результат — не копи двадцать изменений вслепую. Кто отправляет модели «переделай всё» — платит токенами за собственную лень описать задачу.' }
    ],
    lifehack: 'Работай короткими итерациями с проверкой: одна конкретная правка — один запрос — сразу смотришь результат глазами. Двадцать слепых изменений «пачкой» ИИ разложит так, что не найдёшь, где сломалось. Мелкий шаг + проверка = продукт растёт, крупный шаг вслепую = час на дебаг чужого хаоса.',
    links: [
      { label: 'Lovable', url: 'lovable.dev', note: 'Собрать первый рабочий прототип по описанию за один вечер — с живым превью' }
    ]
  },
  110: {
    intro: 'Четыре инструмента, которые решают всё в 2026: Lovable ($25) — самые красивые приложения из коробки, Bolt ($25) — за вечер проверить сырую идею, Cursor ($20) — глубокое понимание кода, Claude Code — автономный инженер в терминале.',
    notes: [
      { h: 'Кто на что', body: 'Lovable — про красоту и скорость: готовый дизайн без возни, идеален для лендингов, MVP, презентации инвестору. Bolt — про проверку гипотезы: «собрал за вечер, показал, выкинул если не зашло», стенд для тестов. Cursor видит весь проект и лечит сложные баги. Claude Code работает в терминале как автономный инженер — сюда переходишь, когда из прототипа делаешь серьёзный продукт.' },
      { h: 'Деплой — бесплатно', body: 'Vercel и Cloudflare Pages поднимают сайт в интернет за минуты, бесплатного тарифа хватает большинству проектов на старте. Домен подключается в пару кликов, HTTPS включается сам. Не нужно ни сервера, ни админа — платформа берёт это на себя, а ты только пушишь код.' },
      { h: 'Эталонная связка', body: 'Lovable собирает интерфейс — быстро и красиво. Cursor или Claude Code дописывают логику и правят сложное. Деплой на Vercel — и продукт живёт в проде за один вечер. Каждый инструмент делает то, что у него получается лучше всех: не пытайся выжать из одного всё.' }
    ],
    lifehack: 'Не выбирай «один инструмент навсегда» — собирай связку под этап: Lovable для быстрого красивого UI, Cursor или Claude Code для сложной логики поверх, Vercel для бесплатного деплоя. Один инструмент вытянет 60% работы, три под свои роли — все 100% без страданий и без переплаты за лишние функции.',
    links: [
      { label: 'Cursor', url: 'cursor.com', note: 'IDE с ИИ, которая видит весь проект — для сложных багов и глубокой логики' },
      { label: 'Vercel', url: 'vercel.com', note: 'Бесплатный деплой из GitHub за минуты: пуш в ветку → сайт в проде' }
    ]
  },
  111: {
    intro: 'От идеи до деплоя за один вечер. Открываешь Lovable или Bolt, описываешь страницу словами — через пару минут живой лендинг, который можно листать. К концу вечера он уже в интернете по своему домену.',
    notes: [
      { h: 'Первый промпт решает половину', body: 'Опиши оффер, аудиторию, нужные блоки и настроение дизайна. Дай структуру: экран-обложка, выгоды, отзывы, цена, кнопка. Плохой промпт «сделай красиво» = серый шаблон; конкретика = точное попадание. Чем детальнее заявка, тем меньше правок потом.' },
      { h: 'Обязательные блоки', body: 'Первый экран: заголовок с выгодой и одна кнопка действия. Блок доверия: отзывы, кейсы, цифры, лица. Оффер и цена без воды, повтор кнопки внизу. Это не украшение — это воронка: каждый блок закрывает одно возражение, а кнопка внизу ловит того, кто дочитал.' },
      { h: 'Правки и публикация', body: 'Правки — точечно: не «переделай всё», а «сделай заголовок крупнее и добавь блок FAQ». Одна правка — один запрос, так ИИ не ломает соседние блоки. Проверяй на телефоне: большинство зайдёт с мобильного. Публикация — встроенный деплой сразу, либо Vercel; подключи свой домен и поставь аналитику.' }
    ],
    lifehack: 'Первый промпт лендинга пиши как бриф дизайнеру: оффер + аудитория + список из 5–7 блоков в нужном порядке + одно слово настроения («минимализм», «премиум»). Такой заход даёт готовую страницу с первой попытки, а плавающие «сделай красиво про мой бизнес» — всегда серый шаблон, из которого потом день выковыриваешь смысл.',
    links: [
      { label: 'Bolt', url: 'bolt.new', note: 'За вечер собрать рабочий прототип лендинга с живым превью и деплоем' }
    ]
  },
  112: {
    intro: 'Приложение отличается от лендинга тремя вещами: вход, база данных, оплата. Собираешь каркас в Lovable, сложное дотачиваешь в Cursor. Цель — чтобы человек регистрировался, что-то сохранял и платил.',
    notes: [
      { h: 'База и вход', body: 'БД — это просто таблицы: пользователи, заказы, подписки. Lovable сам поднимает базу, ты описываешь словами, какие поля нужны. Правило: одна сущность — одна таблица, не сваливай всё в кучу. Авторизация по email или через Google — проси включить готовую, у каждого свои данные, чужое он не видит. Не изобретай пароли — используй встроенную систему входа.' },
      { h: 'Оплата — сердце монетизации', body: 'Подключаешь платёжку: Lava.top, ЮKassa или Stripe под задачу. Логика проста: оплатил — открыли доступ, не оплатил — закрыли. Это часть, где лучше перейти в Cursor и проверить каждый шаг: ошибка тут стоит реальных денег и потерянных клиентов.' },
      { h: 'Порядок и грабли', body: 'Сначала экраны и вход, потом база, в конце оплата. Проверяй по одному куску: зарегистрировался → сохранил → заплатил. Не подключай платёж, пока не работает вход и данные. Грабли: чужой пользователь видит чужие заказы (проверь права), «оплата прошла», а доступ не открылся (тестируй на реальном платеже), ключи платёжки в открытом коде — только в защищённых переменных.' }
    ],
    lifehack: 'Собирай приложение строго в порядке «вход → база → оплата» и проверяй каждый кусок на живом сценарии до перехода к следующему. Если подключишь оплату на непроверенном входе, будешь неделю искать, почему деньги приходят, а доступ не открывается — вместо часа отладки на этапе, где ошибка ещё в одном месте.',
    links: [
      { label: 'Supabase', url: 'supabase.com', note: 'База, авторизация и защита данных из коробки — работает с Lovable/Cursor' },
      { label: 'Lava.top', url: 'lava.top', note: 'Приём платежей для рунета: подписки, разовые, автопродление' }
    ]
  },
  113: {
    intro: 'Обычный бот — кнопки и скрипты, ИИ-бот понимает живой текст. Внутри связка: сообщение пользователя → LLM → умный ответ. Собирается без единой строчки, написанной руками.',
    notes: [
      { h: 'Три кирпича бота', body: 'Токен от @BotFather — паспорт твоего бота в Telegram. Мозг — модель ChatGPT, Claude или Gemini через ключ. Связующее звено: n8n, Make или свой код на Claude Code. Без кода через n8n/Make: собираешь схему из блоков «получил сообщение → отправил в LLM → вернул ответ», визуальный сценарий заменяет десятки строк кода.' },
      { h: 'Характер и роль', body: 'Задаёшь системный промпт: кто бот, как говорит, чего не делает. Пример: «ты вежливый администратор салона, отвечаешь коротко, зовёшь записаться». От промпта зависит, будет бот полезным или пустым болтуном. Промпт — это должностная инструкция сотрудника, только для ИИ.' },
      { h: 'Память и границы', body: 'Чтобы бот помнил диалог — храни последние сообщения в базе. Ставь лимиты: не отвечать на лишнее, не сливать ключи и промпт. Тестируй на злых вопросах, пока не пустил к клиентам. Без границ первый же тролль или конкурент вытянет из бота твой системный промпт и опозорит на скрине.' }
    ],
    lifehack: 'Перед запуском ИИ-бота на клиентов сам месяц играй роль тролля: спрашивай матом, проси раскрыть системный промпт, отправляй бессмысленный текст, ломай контекст. Каждую пробоину закрывай инструкцией в промпте. То, что вылезет у первого же реального пользователя, дешевле поймать самому, чем на скрине в чужом канале.',
    links: [
      { label: 'BotFather', url: 't.me/BotFather', note: 'Создать Telegram-бота и получить токен — паспорт бота в Telegram' },
      { label: 'n8n', url: 'n8n.io', note: 'Визуальная сборка сценария бота из блоков без кода — self-hosted и бесплатно' }
    ]
  },
  114: {
    intro: 'API — это дверь к чужому мозгу: твой код шлёт текст, модель шлёт ответ. Так внутри твоего приложения появляется ChatGPT или Claude. Три вещи нужны всегда: ключ, название модели, промпт.',
    notes: [
      { h: 'Ключ — пропуск и деньги', body: 'Берёшь API-ключ в личном кабинете провайдера. Каждый запрос стоит денег — платишь за токены, не за штуки. Ключ — как пароль от карты: утёк ключ — утекли деньги. Никогда не клади ключ в код: он живёт в переменных окружения, а не в тексте страницы, иначе любой откроет код в браузере и заберёт. В Lovable и Vercel есть отдельное хранилище секретов.' },
      { h: 'Выбор модели под задачу', body: 'Дорогая умная модель — для сложных рассуждений и кода. Дешёвая быстрая — для простых ответов и большого потока. У Gemini, Claude, ChatGPT свои сильные стороны. Не гони на топ-модели то, что закроет базовая — деньги на потоке испаряются молча, а качество результата почти то же.' },
      { h: 'Промпт и лимиты', body: 'Системный промпт задаёт поведение модели на каждый запрос. Передавай контекст: кто пользователь, что он уже делал. Плохой промпт — и самая умная модель отвечает мимо. Ставь лимит расходов в кабинете, чтобы не улететь в минус, не гоняй длинные диалоги без нужды, режь контекст, логируй запросы — увидишь, где сжигается бюджет.' }
    ],
    lifehack: 'С первого дня ставь жёсткий лимит расходов в кабинете провайдера и включай логирование запросов. Один утёкший ключ или бесконечный цикл в коде за ночь съест месячный бюджет, а лимит остановит счётчик на понятной сумме. Логи покажут, где ты жжёшь токены на длинных ненужных диалогах — оптимизация окупается за неделю.',
    links: [
      { label: 'OpenRouter', url: 'openrouter.ai', note: 'Один ключ на десятки моделей с ценой за запрос — гонишь под задачу самую дешёвую' }
    ]
  },
  115: {
    intro: 'Без правил каждый экран получается своим — продукт выглядит кустарно. Дизайн-система это единые цвета, шрифты и компоненты. Задаёшь её один раз, и весь продукт держится в одном стиле.',
    notes: [
      { h: 'Три кита: цвет, шрифт, отступы', body: 'Палитра из 3–4 цветов: основной, акцент, фон, текст — не больше. Одна-две шрифтовые пары, а не десять разных начертаний. Единый шаг отступов, чтобы всё дышало ровно. Меньше значит цельнее: дисциплина ограничений даёт вид дорогого продукта.' },
      { h: 'Компоненты — кирпичи интерфейса', body: 'Кнопка, карточка, поле ввода — рисуешь один раз, используешь везде. Меняешь компонент в одном месте — меняется во всём приложении. Lovable и Cursor отлично держат такие переиспользуемые блоки. Это экономит время на правках и держит стиль без ручной сверки каждого экрана.' },
      { h: 'Задать словами и темы', body: 'В промпте укажи бренд-цвета в HEX и названия шрифтов, опиши тон: «минимализм, много воздуха, скругления, тёмная тема». Проси ИИ вынести стили в единые токены, а не хардкодить в каждом экране. Заложи обе темы сразу — переделывать потом дороже; цвета описывай ролями («фон», «акцент»), не намертво; проверь контраст в обеих темах.' }
    ],
    lifehack: 'Требуй у ИИ вынести цвета и отступы в единые токены («--color-accent», «--space-4»), а не хардкодить значения в каждом компоненте. Одна правка токена — и весь продукт меняется разом, без похода по 50 экранам. И тёмную тему закладывай в первый же день: описанные ролями цвета переключаются одной переменной, а переделка постфактум это переписывание всего.',
    links: [
      { label: 'shadcn/ui', url: 'ui.shadcn.com', note: 'Готовая система компонентов с токенами, тёмной темой и переиспользуемыми блоками' },
      { label: 'Radix Colors', url: 'radix-ui.com/colors', note: 'Проверенные палитры с контрастом под тёмную и светлую темы одновременно' }
    ]
  },
  116: {
    intro: 'Деплой — это когда твой проект переезжает из ноутбука в интернет: до него видишь только ты, после — весь мир по ссылке. Vercel и Cloudflare Pages делают это бесплатно и за минуты.',
    notes: [
      { h: 'Vercel — путь по умолчанию', body: 'Связываешь проект с GitHub — каждый пуш сам уезжает в прод. Бесплатного тарифа хватает лендингам и небольшим приложениям. HTTPS и быстрая отдача включаются автоматически. Правило: код в GitHub — прод обновляется без ручных действий (так собран деплой самого OKO: пуш в ветку и через минуты всё в проде). Откат тоже в один клик.' },
      { h: 'Cloudflare — когда нужна мощь', body: 'Cloudflare Pages силён на большом трафике и по всему миру. Щедрый бесплатный лимит, встроенная защита от атак. Хорош, если аудитория глобальная, а не только твой город. Для стартового проекта разницы почти нет — бери то, что больше нравится по интерфейсу.' },
      { h: 'Домен и HTTPS', body: 'Привязываешь свой домен в настройках проекта за пару кликов. Замочек HTTPS ставится сам — сайту доверяют и браузеры, и люди. Пока домена нет — работает бесплатный адрес платформы (типа project.vercel.app), это тоже нормально для теста, но продать со «сложной» ссылки труднее.' }
    ],
    lifehack: 'Настраивай авто-деплой из GitHub с первого дня: пуш в ветку — прод обновляется через минуты, без ручных заливок. И сделай preview-ветку для теста правок — сначала смотришь новую версию на временной ссылке, потом одним пушем катишь в прод. Откат тоже в один клик, если что-то пошло не так — так работает деплой самого OKO.',
    links: [
      { label: 'Cloudflare Pages', url: 'pages.cloudflare.com', note: 'Бесплатный деплой с щедрым лимитом и защитой от атак — для глобальной аудитории' },
      { label: 'GitHub', url: 'github.com', note: 'Репозиторий и автотриггер деплоя: пуш в ветку → прод обновляется сам' }
    ]
  },
  117: {
    intro: 'У инструментов есть бесплатные кредиты и щедрые триалы на старте. Готовые шаблоны экономят половину работы и токенов. Главный навык — не жечь токены впустую на пустых итерациях.',
    notes: [
      { h: 'Бесплатное и шаблоны', body: 'Lovable, Bolt, Cursor дают лимит бесплатных генераций — хватает собрать первый прототип и проверить идею. Деплой на Vercel и Cloudflare вовсе бесплатный на старте. Бери готовый шаблон дашборда, магазина, лендинга и правь под себя — так экономишь десятки запросов к модели. Изучай чужие сборки: половина решений уже кем-то придумана.' },
      { h: 'Как не сжечь токены', body: 'Короткие итерации: одна конкретная задача — один запрос. Не проси «переделай всё приложение» — это дорого и ломает рабочее. Формулируй точно: чем яснее задача, тем меньше переделок. Каждая расплывчатая формулировка это лишние генерации, за которые ты платишь из своего кармана.' },
      { h: 'Дели работу между инструментами', body: 'Дорогую умную модель включай только на сложное. Простое собирай в дешёвом или бесплатном режиме. Lovable — на UI, Cursor — точечно на трудную логику. Планируй до генерации: набросай экраны и логику словами, проверяй каждый шаг, сохраняй рабочие версии — откат дешевле новой сборки с нуля.' }
    ],
    lifehack: 'До первой генерации нарисуй экраны и опиши логику словами на листе — потратишь 15 минут, сэкономишь часы токенов. Модель, которая знает точную задачу, собирает нужное с двух попыток; модель, которую попросили «сделай приложение», выдаёт десять итераций серого, за которые ты платишь. Ясность мысли — это скидка на инструменты.',
    links: [
      { label: 'v0', url: 'v0.dev', note: 'Готовые компоненты и шаблоны интерфейсов от Vercel — экономят десятки запросов' }
    ]
  },
  118: {
    intro: 'Правило первых денег: автоматизируй то, что уже приносит выручку или напрямую её теряет. Сначала процесс работает руками — только потом его автоматизируют, иначе автоматизируешь хаос.',
    notes: [
      { h: 'Формула приоритета', body: 'Считай: время на задачу × частота в месяц × ставка часа. 30 мин × 60 раз = 30 часов/мес — вот кандидат номер один. Ручная квалификация лида в директе съедает до 40% рабочего дня. Пять процессов, что окупаются первыми: поддержка, квалификация лидов, счета, автопостинг, мониторинг цен конкурентов.' },
      { h: 'Что автоматизировать нельзя', body: 'Процесс, который сам не понимаешь руками — сначала опиши шаги. Редкие задачи: раз в квартал дешевле сделать вручную. Решения, где цена ошибки — потеря клиента без контроля. Автоматизация в темноте ускоряет только твой путь в убыток — сначала руками, потом системой.' },
      { h: 'Первый запуск за вечер', body: 'Выпиши 10 задач, которые повторяешь каждую неделю. Отметь три самых частых и денежных — это дорожная карта. Автоматизируй одну, замерь освобождённые часы, посчитай в деньгах. Один зашедший процесс на потоке экономит больше, чем 10 «интересных» экспериментов.' }
    ],
    lifehack: 'Прогони через формулу «время × частота × ставка часа» весь свой месяц — самая скучная и частая задача почти всегда окажется на первом месте, и именно её надо автоматизировать первой, а не самую заметную. Скучное на потоке съедает деньги тихо, а автоматизация освобождает часы, которые видно в кассе.',
    links: [
      { label: 'Toggl Track', url: 'toggl.com', note: 'Замерить, куда реально уходят часы недели — из фактов рождается приоритет' }
    ]
  },
  119: {
    intro: 'Три инструмента автоматизации — три философии: n8n (гибкий, self-hosted бесплатно), Make (наглядные сценарии, дешевле на среднем объёме), Zapier (проще всех, 9000+ сервисов, дорожает на объёме).',
    notes: [
      { h: 'n8n и Make', body: 'n8n self-hosted на своём VPS — бесплатно и без лимитов на воркфлоу; облако от $24/мес. Код-ноды на JavaScript: где не хватает блоков — пишешь сам. Make — визуальный конструктор, видно, как данные текут по сценарию; оплата за операции выгоднее Zapier на тысячах задач; роутеры, фильтры, агрегаторы из коробки.' },
      { h: 'Zapier — скорость и охват', body: '9000+ интеграций — почти любой сервис уже подключён. Собрал связку за 10 минут без чтения документации. На больших объёмах ценник кусается — считай заранее. Zapier это про быстрый старт, а не про долгую эксплуатацию: гипотезу проверить, а систему потом переносить на n8n или Make.' },
      { h: 'Как выбрать', body: 'Много исполнений и свой сервер → n8n. Средний объём, любишь наглядность → Make. Нужен редкий сервис прямо сейчас → Zapier. Прикинь рост × 12 месяцев, а не только сегодняшний тариф; миграция между платформами болезненна — выбирай на вырост, а не по одной приятной цене.' }
    ],
    lifehack: 'Перед выбором платформы посчитай не сегодняшний тариф, а стоимость через 6 и 12 месяцев при твоём росте объёма — миграция потом мучительна. Zapier удобен на 100 задачах и разоряет на 10 000; n8n self-hosted наоборот. Считай на вырост, а не на первый вечер, и выбирай ту, где к концу года будешь платить меньше.',
    links: [
      { label: 'Make', url: 'make.com', note: 'Визуальный сценарий с роутерами и фильтрами — золотая середина цены и наглядности' },
      { label: 'Zapier', url: 'zapier.com', note: 'Быстро связать редкий сервис (9000+ интеграций) — гипотезу проверить за 10 минут' }
    ]
  },
  120: {
    intro: 'Из чего собран агент поддержки: LLM (мозг) + база знаний RAG (память) + инструменты + кнопка «позвать человека». Не выдумывает: отвечает по твоим документам, а не по галлюцинациям.',
    notes: [
      { h: 'RAG простыми словами', body: 'Агент ищет ответ в твоей базе и отвечает только по найденному. Документы бьют на куски, превращают в векторы, ищут по смыслу. Это лечит галлюцинации: нет в базе — не сочиняет, а зовёт человека. Загрузи FAQ, условия возврата, прайс, регламенты; пиши коротко и по фактам — мусор на входе = мусор в ответе.' },
      { h: 'Инструменты и эскалация', body: 'Дай агенту действия: проверить заказ, статус доставки, создать тикет. Триггеры эскалации: злость клиента, слово «оператор», три провала подряд. Живой человек подхватывает диалог с полной историей — клиент не пересказывает всё заново. Пропиши характер: вежливый, короткий, без обещаний от балды; запрети темы вне компетенции.' },
      { h: 'Проверка перед запуском', body: 'Прогони 30–50 реальных вопросов из переписок. Метрика: доля диалогов, закрытых без человека (цель 60–80%). Логируй провалы — они пополняют базу знаний. Всегда честно: «я бот, при сложном вопросе позову коллегу». Обманутый клиент — это минус доверие ко всему бренду, а не к боту.' }
    ],
    lifehack: 'Перед запуском на клиентов прогоняй агента на 30–50 РЕАЛЬНЫХ вопросах из архива переписок, а не выдуманных. Каждый провал закрываешь дополнением в базу — за неделю тестов агент закроет 70% типовых, и живой человек подхватит только сложное. Готовый архив вопросов — это твой бесплатный тренажёр.',
    links: [
      { label: 'Voiceflow', url: 'voiceflow.com', note: 'Собрать агента поддержки с RAG и эскалацией на человека без кода' }
    ]
  },
  121: {
    intro: 'Автопостинг — это конвейер, а не кнопка: три звена — расписание → генерация → публикация. Ты готовишь контент пачкой — робот раскидывает по времени во VK, Telegram, соцсети по календарю.',
    notes: [
      { h: 'Контент-план как источник', body: 'Держи посты в таблице: дата, текст, картинка, площадка, статус. n8n и Make читают строку и публикуют в нужный момент. Статус «опубликовано» ставится сам — видно, что ушло. Один смысл — разные форматы: длинный для TG, короткий для VK; шаблон промпта в бренд-стиле — лента выглядит единой.' },
      { h: 'Расписание и лучшее время', body: 'Триггер по времени: посты уходят в часы активности аудитории. Не постить всё в один момент — размажь по дню. Очередь на неделю вперёд = стабильность без ежедневной суеты. Публикация «пачкой в 12:00» выглядит как спам и режется алгоритмами — растяни по естественным часам.' },
      { h: 'Защита от провалов', body: 'Проверяй, что картинка и текст на месте до публикации. Площадка вернула ошибку — ретрай и уведомление в бота. Веди лог: что, когда и куда ушло — для разбора и отчёта. Автопостинг без защиты один раз молча уронит неделю контента, и ты узнаешь только по пустой ленте.' }
    ],
    lifehack: 'Всегда добавляй в автопостинг ретрай при ошибке и алерт в Telegram-бот, если публикация не прошла. Молчаливо провалившийся автопостинг — это неделя пустой ленты, которую ты обнаружишь только по обвалу охватов. Пять минут на настройку алертов экономят потерянный месяц контента.',
    links: [
      { label: 'SMMplanner', url: 'smmplanner.com', note: 'Автопостинг в русскоязычные соцсети по календарю с ретраями и логами' }
    ]
  },
  122: {
    intro: 'Автопостинг публикует готовое; конвейер производит контент на потоке. Цепочка: генерация → сборка → публикация — десятки единиц без ручной склейки. Цель — контент серией, а не один шедевр раз в неделю.',
    notes: [
      { h: 'Три этапа', body: 'Генерация: сценарий (ChatGPT/Claude), визуал (Midjourney/Flux), видео (Veo/Kling/Runway), озвучка (локальный TTS или ElevenLabs), музыка (Suno). Сборка: собери куски в готовую единицу, шаблон сборки один — меняется наполнение, автоматика (n8n) дирижирует. Публикация: готовое уходит в автопостинг, метрики возвращаются в таблицу — видно, что зашло.' },
      { h: 'Разнообразие против штампа', body: 'Реестр приёмов: ни один эффект и формат не повторяется подряд. Ротация рубрик и форматов через поля в таблице. Повтор убивает охваты — конвейер должен удивлять. Опасность потокового производства именно в клонах: каркас постоянный, наполнение всегда новое.' },
      { h: 'Темп и качество', body: 'Узкое место — там, где всё встаёт в очередь: найди и расширь. Контроль качества на выходе: брак не публикуется. Считай себестоимость единицы: время плюс кредиты генерации. Без учёта себестоимости конвейер быстро разоряет — «много и красиво» без цены за штуку это работа в минус.' }
    ],
    lifehack: 'Веди реестр использованных приёмов (хуки, переходы, музыка, эффекты) прямо в таблице контент-плана — и на каждом новом посте выбирай ТО, ЧТО ЕЩЁ НЕ БЫЛО. Один шаблон + разное наполнение из реестра = поток без штампа, а не сто одинаковых роликов, выжигающих аудиторию за неделю.',
    links: [
      { label: 'Airtable', url: 'airtable.com', note: 'Контент-план с реестром приёмов, статусами и связками для n8n/Make' }
    ]
  },
  123: {
    intro: 'Заявка из соцсети → карточка в CRM → оплата → доступ — один поток вместо ручных мостиков. Клиент не выпадает на стыках — там теряются деньги. Ни одного ручного переноса данных между сервисами.',
    notes: [
      { h: 'Точка входа и CRM', body: 'Лид пишет в Telegram/VK/Instagram — вебхук ловит событие. ИИ квалифицирует: горячий или холодный, что хочет. CRM — единая память: каждый лид карточка с источником, этапом, суммой, историей. Автоматика двигает по этапам: новый → квалифицирован → оплатил. Нет карточки — нет клиента.' },
      { h: 'Оплата и доступ', body: 'Ссылка на оплату (Lava.top, ЮKassa) генерится автоматически. Оплата прошла → вебхук → выдача доступа/товара мгновенно. Не оплатил за N часов — автонапоминание, потом менеджеру. Скорость оплаты и выдачи это разница между тёплым клиентом и остывшим за час раздумий.' },
      { h: 'Оркестрация связки', body: 'n8n/Make/Zapier соединяют соцсеть + CRM + оплату + ИИ. Каждый сервис делает своё, автоматика передаёт эстафету. Один сбой не должен ронять всю цепочку. Что даёт в деньгах: скорость ответа и счёта, ничего не теряется, прозрачно видно, где отваливаются клиенты — и это можно чинить точечно.' }
    ],
    lifehack: 'Начни собирать сквозную связку «соцсеть → CRM → оплата → доступ» с малого — с одного канала входа и одной точки оплаты. Полная автоматизация «всё разом» — верный путь застрять на две недели без результата. Одна рабочая цепочка быстро окупается и подсказывает, что дальше автоматизировать во вторую очередь.',
    links: [
      { label: 'AmoCRM', url: 'amocrm.ru', note: 'CRM с готовыми интеграциями Telegram/WhatsApp/Instagram и вебхуками' }
    ]
  },
  124: {
    intro: 'Два способа запустить автоматизацию: вебхук (по событию: пришёл лид, прошла оплата) и расписание/cron (по времени: каждый день в 10:00). Всё стартует одним из этих двух триггеров — других нет.',
    notes: [
      { h: 'Вебхук простыми словами', body: 'Это URL, на который сервис стучится, когда что-то случилось. Событие → мгновенный запуск сценария, без опроса и ожидания. Пример: оплата в Lava.top → вебхук → выдача доступа. Быстрее любого «раз в час проверяем, не пришла ли оплата» — потому что реагирует в ту же секунду.' },
      { h: 'Расписание (cron)', body: 'Запуск по времени: раз в час, каждое утро, по будням в 12:00. Для задач без внешнего события: отчёты, автопостинг, проверки. Формат cron — пять полей: минуты, часы, дни, месяц, день недели. Часто связка: вебхук ловит, расписание подчищает и досылает — надёжнее, чем полагаться на что-то одно.' },
      { h: 'Подводные камни', body: 'Вебхук может прийти дважды — делай защиту от дублей. Ты был недоступен — нужны ретраи со стороны сервиса. Слишком частый cron жжёт исполнения и деньги — выбирай интервал с умом. Незащищённая от дублей выдача доступа = двойные подписки, недовольные клиенты и убытки.' }
    ],
    lifehack: 'На каждый вебхук делай защиту от дублей по внешнему id (order_id, event_id): пришло второй раз — пропусти, не выполняй. Один и тот же вебхук от платёжки может прилететь дважды из-за ретраев, и без этой защиты клиент получит две подписки, а ты — конфликт и возврат вместо чистой сделки.',
    links: [
      { label: 'Webhook.site', url: 'webhook.site', note: 'Отладить приём вебхука вживую: увидеть, что и как отправляет сервис' },
      { label: 'Crontab Guru', url: 'crontab.guru', note: 'Собрать правильное cron-расписание и понять непонятные строки за секунду' }
    ]
  },
  125: {
    intro: 'Цена решает сделку — знай рынок в реальном времени. Автомониторинг цен конкурентов: парсинг страниц → сравнение → оповещение. Ручная проверка раз в месяц = ты всегда опаздываешь на 29 дней.',
    notes: [
      { h: 'Как устроен парсинг', body: 'Робот заходит на страницу товара и вытаскивает цену. Сохраняет в таблицу с датой — копится история цен. n8n/Make по расписанию обходят список ссылок. Копишь данные — видишь тренды: кто демпингует, кто держит, замечаешь распродажи и сезонность.' },
      { h: 'Триггер и оповещение', body: 'Цена изменилась больше порога → алерт в Telegram-бот. Конкурент уронил цену на 15% — узнал за минуты, а не за месяц. В сообщении: ссылка, старая и новая цена — сразу видно суть. Решения по своей цене принимаешь на фактах, а не на догадках.' },
      { h: 'Технические тонкости', body: 'Сайты меняют вёрстку — парсер ломается, следи за пустыми ценами. Не долби сайт слишком часто — вежливый интервал против бана. Пустая или странная цена → не пиши в базу, а шли предупреждение. Защита от «плохих данных» важнее, чем сам парсинг: одна кривая цена в базе даст неверный алерт и глупое решение.' }
    ],
    lifehack: 'Никогда не пиши в базу «пустую» или подозрительно нулевую цену — вместо этого шли алерт «парсер сломался, проверь». Иначе одна кривая цена скажет «конкурент уронил на 100%», ты рванёшь снижать свою — а на самом деле у них просто поменялась вёрстка. Защита от плохих данных ценнее самого парсинга.',
    links: [
      { label: 'Apify', url: 'apify.com', note: 'Готовые парсеры-акторы и хостинг: не изобретаешь велосипед и не банишься' }
    ]
  },
  126: {
    intro: 'Незаметный сбой = потерянные лиды и деньги без твоего ведома. Три опоры надёжности: логи, обработка ошибок, ретраи. Цель — конвейер, который сам сообщает, что сломался, а не молча теряет деньги.',
    notes: [
      { h: 'Логи и обработка', body: 'Пиши, что запустилось, с чем и чем закончилось — по логам находишь, на каком шаге и почему встало (без логов отладка это гадание). Каждый шаг может упасть: сервис недоступен, кривые данные. Ветка «на случай ошибки»: поймать, записать, уведомить. Не дай одной ошибке уронить весь сценарий целиком.' },
      { h: 'Ретраи с умом', body: 'Сервис не ответил — повтори через паузу, а не сразу и вечно. Экспоненциальная пауза: 10с, 30с, 2 мин — даёшь время очнуться. После N попыток — сдавайся и зови человека, не долби бесконечно. Оповещения: критическая ошибка → мгновенный алерт в Telegram-бот с деталями. Лучше 5 честных алертов, чем один тихий провал на неделю.' },
      { h: 'Идемпотентность', body: 'Повторный запуск не создаёт второй счёт или доступ. Проверяй по id: уже обработано — пропусти. Это спасает при ретраях и двойных вебхуках. Без идемпотентности каждый повтор превращается в дубль — двойные оплаты, двойные доступы, недовольные клиенты и хаос вместо системы.' }
    ],
    lifehack: 'Всегда закладывай в цепочку три вещи одновременно: лог каждого шага, ветку «на ошибку» с алертом в Telegram и защиту от дублей по внешнему id. Автоматизация без этих трёх — мина: она красиво работает демо и тихо разоряет тебя в проде, а ты узнаёшь только по звонку злого клиента, а не из системы.',
    links: [
      { label: 'Sentry', url: 'sentry.io', note: 'Сбор ошибок автоматизаций и приложений с алертами — тихие сбои становятся видимыми' }
    ]
  },
  127: {
    intro: 'ИИ-ассистент отвечает 24/7 за секунды, снимает 60–80% типовых вопросов, не болеет, не выгорает, помнит все регламенты. Это не чат-бот по кнопкам: LLM + системный промпт + база знаний (RAG) + интеграции.',
    notes: [
      { h: 'База знаний и промпт', body: 'FAQ, прайсы, регламенты, скрипты продаж → база знаний. Агент ищет фрагмент и отвечает по фактам; обновил документ — агент сразу «поумнел». Системный промпт задаёт личность и тон бренда, жёсткие правила («что нельзя обещать»), сценарии эскалации: когда переключать на менеджера. Промпт — это должностная инструкция ИИ-сотрудника.' },
      { h: 'Подключение к Telegram', body: 'Бот создаётся через BotFather за две минуты. Схема: сообщение → webhook → сервер → LLM → ответ. Кнопки для сценариев + свободный диалог для всего остального. Гибрид кнопок и живой речи ловит и тех, кто ищет быстрый ответ, и тех, кто пишет своими словами.' },
      { h: 'Продажи и поддержка', body: 'Квалификация лида: агент сам задаёт уточняющие вопросы. Запись на встречу и оплата — прямо в чате. Горячий лид мгновенно улетает менеджеру с резюме диалога — человек подхватывает уже готовую сделку, не расспрашивая всё заново. Скорость реакции + прогретость лида = выше конверсия.' }
    ],
    lifehack: 'Обяжи агента ВСЕГДА передавать менеджеру полное резюме диалога с горячим лидом одной кнопкой, а не только имя и телефон. Менеджер, у которого уже есть боль клиента, бюджет и возражения из беседы, закрывает сделку в разы чаще, чем тот, кто начинает знакомство с нуля — конверсия скачет без единой правки в скрипте.',
    links: [
      { label: 'Voiceflow', url: 'voiceflow.com', note: 'Собрать ИИ-ассистента с RAG, кнопками и эскалацией для Telegram/сайта' }
    ]
  },
  128: {
    intro: 'Пять услуг, которые покупают уже сегодня: ИИ-аватары (5–30к ₽), Reels пачками (себестоимость 200–500 ₽, продаёшь от 1500 ₽), телеграм-боты (10–50к ₽), лендинги на вайбкоде, автоворонки. Маржа 60–90%, а не 20.',
    notes: [
      { h: 'Почему маржа такая высокая', body: 'Себестоимость на ИИ — это подписка и генерации, а не часы работы. Аватары: Higgsfield или Midjourney — десятки рублей за сет. Ролик: сток + озвучка + монтаж = 200–500 ₽. За что платят на самом деле: не за нейросеть, а за снятую задачу и сэкономленное время. Бизнесу нужен поток контента, а не доступ к ChatGPT.' },
      { h: 'Как выбрать первую услугу', body: 'Бери то, что делаешь за вечер и не выгораешь. Аватары и Reels — быстрый вход, деньги на 2–3 день. Боты и лендинги — чек выше, но нужен навык вайбкодинга. Начинай там, где твой навык уже есть, а не там, где красиво выглядит цена — иначе застрянешь в обучении и не заработаешь.' },
      { h: 'Первые деньги: реальный путь', body: 'Неделя 1 — собрал 3 демо-работы для портфолио. Неделя 2 — 5–10 холодных касаний в день по своей нише. Первый чек обычно 3–15к ₽ — это нормальный старт, а не потолок. Гарантия результата дороже, чем «я умею в Midjourney»: клиент платит за уверенность в исходе, а не за инструмент.' }
    ],
    lifehack: 'Начни с той услуги, которую делаешь ЗА ВЕЧЕР и не выгораешь, а не с той, где красивая маржа в теории. Аватары и Reels заходят на 2–3 день, боты и лендинги дают выше чек, но требуют вайбкодинга. Один зашедший чек за неделю бьёт три брошенных проекта в «интересных» темах.',
    links: [
      { label: 'Higgsfield', url: 'higgsfield.ai', note: 'Собрать пакет ИИ-аватаров под клиента: сеты портретов без фотографа' }
    ]
  },
  129: {
    intro: 'Сырая услуга не продаётся: «делаю картинки в Midjourney» — это не оффер, это хобби. Клиент не покупает инструмент, он покупает решение проблемы. Упаковка превращает навык в понятный продукт с ценой.',
    notes: [
      { h: 'Формула оффера', body: 'Кому + какой результат + за какой срок + с какой гарантией. «30 Reels для салона за 7 дней или верну деньги» — это оффер. Убери из формулировки слова «нейросеть» и «ИИ» — оставь выгоду. Клиенту не интересно, чем ты делаешь, важно, что он получит и когда.' },
      { h: 'Кейсы и гарантия', body: 'Один экран «было/стало» убеждает сильнее трёх абзацев. Показывай цифры: было 200 охватов, стало 15 000. Даже учебный кейс на себе работает лучше пустого портфолио. Гарантия снимает страх слить деньги: «не понравится первый вариант — переделаю бесплатно». Гарантия результата поднимает конверсию и оправдывает цену.' },
      { h: 'Продающее описание и ошибки', body: 'Оффер, 3 кейса, что входит в пакет, цена, гарантия, призыв. Опиши процесс по шагам — клиент любит понятность. Дедлайн или ограничение мест для срочности. Ошибки: продаёт процесс («сделаю в Kling»), стесняется цены и демпингует, нет ни одного кейса — нечем закрыть недоверие.' }
    ],
    lifehack: 'Убери из своего оффера и лендинга слова «нейросеть», «ИИ», «Midjourney», «нейронка» — оставь только результат, срок и гарантию. Клиенту всё равно, каким инструментом ты работаешь; ему важно, что он получит 30 роликов за неделю и вернёт деньги, если не зайдёт. Инструмент — твоя кухня, а не витрина.',
    links: [
      { label: 'Тильда', url: 'tilda.cc', note: 'Собрать продающую страницу услуги: оффер, кейсы, цена, гарантия, призыв' }
    ]
  },
  130: {
    intro: 'Ручные продажи упираются в твоё время и силы. Автоворонка ведёт клиента от интереса к оплате без тебя. Схема простая: лид-магнит → прогрев в боте → продажа. Работает, пока ты спишь.',
    notes: [
      { h: 'Лид-магнит: бесплатно, но ценно', body: 'Это полезность в обмен на контакт, а не реклама. ИИ-гайд, чек-лист промптов, подборка связок — делается за час. Собери его в ChatGPT или Claude, оформи в PDF. Ценность реальная — иначе платному потом никто не поверит, и лид-магнит станет якорем недоверия.' },
      { h: 'Бот-прогрев вместо переписки', body: 'Телеграм-бот выдаёт гайд и ведёт серию сообщений. Прогрев — это польза, кейсы и снятие возражений по шагам. Собирается на n8n или Make без глубокого кода. После 3–5 касаний пользы — оффер с кейсом и гарантией. Кнопка «Обсудить проект» ведёт на тебя или на оплату; дожим — ограничение мест или дедлайн скидки.' },
      { h: 'Трафик и метрики', body: 'Reels и Shorts с призывом «забери гайд в боте». Комментарии под постами в нише, партнёрские посевы. Каждый ролик — вход в воронку, а не разовый просмотр. Метрики: стоимость лида, конверсия в диалог, конверсия в оплату. Слабое звено видно по цифрам — чиним именно его. Лиды есть, а продаж нет — проблема в оффере или прогреве.' }
    ],
    lifehack: 'Каждый ролик заканчивай призывом «забери в боте» вместо «переходи в директ» — ты автоматически загоняешь холодный трафик в воронку прогрева, а не в свой личный чат. Дальше бот греет 3–5 касаниями и приводит горячего к оплате. Ты обменял ручной ответ каждому на систему, работающую пока ты спишь.',
    links: [
      { label: 'Salebot', url: 'salebot.pro', note: 'Собрать бота-прогрева с воронкой, оплатой и метриками — русский рынок' }
    ]
  },
  131: {
    intro: 'Цена с потолка приводит к работе в минус или демпингу. Считай себестоимость: подписки, генерации и твоё время в рублях за заказ. Три уровня цены: себестоимость → цена за час → цена за ценность.',
    notes: [
      { h: 'Почему нельзя продавать по часам', body: 'Ты растёшь в скорости — и наказываешь себя меньшим чеком за ту же работу. Клиенту важен результат, а не сколько ты возился. Фиксируй цену за результат: «пакет роликов», «бот под ключ». Так рост твоей эффективности превращается в маржу, а не в упавший чек.' },
      { h: 'Демпинг убивает', body: 'Низкая цена привлекает худших клиентов и обесценивает труд. «Дёшево» читается как «некачественно» — отпугивает премиум-сегмент. Поднять цену после демпинга сложнее, чем сразу встать выше. Стартуй с адекватной цены — репозиционироваться потом дороже, чем встать правильно с первого дня.' },
      { h: 'Как обосновать цену', body: 'Считай не свою себестоимость, а его выгоду и экономию. «Фотограф — 25к, у меня аватары — 8к и за день». Сравнивай с альтернативой клиента, а не со своими затратами. Пример: пакет 20 Reels — сток и озвучка ~7к, время ~10 часов; ставишь 25–30к, маржа выше 70% с учётом времени. Заложи запас на правки.' }
    ],
    lifehack: 'Продавай пакетом «под ключ» с фиксированной ценой за результат, а не за часы или штуки. Твой рост в скорости остаётся у тебя в марже, а не превращается в упавший чек. Плюс клиенту легче принять решение по одной понятной цифре, чем считать почасовку и гадать, во сколько это ему обойдётся в итоге.',
    links: [
      { label: 'Notion', url: 'notion.so', note: 'Калькулятор себестоимости заказа: подписки, генерации, время, маржа' }
    ]
  },
  132: {
    intro: 'Пакет продаётся легче, чем поштучно: один ролик — разовая сделка, пакет — предсказуемый доход. Клиенту проще выбрать из 3 тарифов, чем решать «брать или нет». Средний чек пакета выше, а времени столько же.',
    notes: [
      { h: 'Правило трёх тарифов', body: 'Базовый, оптимальный, премиум — большинство берёт средний. Премиум-тариф делает средний выгодным на контрасте (якорь). В каждом — понятный список, что входит. Тарифы должны заметно отличаться: почти одинаковые опции = клиент не видит смысла доплаты; слишком много опций — выбор парализует.' },
      { h: 'Допродажа сразу после «да»', body: 'Клиент, который уже сказал «да», покупает допы легко. «Добавить оформление профиля к пакету роликов — плюс 5к?». Допродажа даёт 20–40% сверху без нового поиска клиента. К роликам — обложки, шапка, монтаж Stories; к боту — сопровождение и доработки на абонентке; к лендингу — тексты, ИИ-аватар для героя, домен и деплой.' },
      { h: 'Абонентка — деньги каждый месяц', body: 'Разовый заказ кончается, подписка приносит стабильно. «Веду ваши Reels — 20 роликов в месяц за фикс». Ретеншн важнее новых продаж: удержать дешевле, чем найти. Один абонемент это предсказуемая выручка, а не гонка за новыми клиентами каждый месяц с нуля.' }
    ],
    lifehack: 'Придумай допродажу сразу после «да» — момент, когда клиент уже принял решение, самый горячий для второй покупки. «Добавить оформление профиля к пакету роликов за +5к?» даёт +20–40% сверху без единого нового касания. Через час эта же допродажа будет холодной и потребует полноценных переговоров.',
    links: [
      { label: 'Google Таблицы', url: 'sheets.google.com', note: 'Собрать три тарифа рядом со списком опций — клиент видит разницу и выбирает средний' }
    ]
  },
  133: {
    intro: 'Клиенты не приходят сами в начале — ждать заявок без имени и кейсов значит терять недели впустую. На старте работает исходящий: ты идёшь к клиенту первым. Холодные касания + чёткая ниша дают первые сделки.',
    notes: [
      { h: 'Выбери одну нишу', body: '«Всем подряд» = никому, оффер размывается. Ниша — это язык клиента, понятные боли и готовые кейсы. Барбершопы, стоматологии, кофейни, эксперты — выбери одну и стань там первым выбором. В узкой нише у тебя нет прямых конкурентов, и доверие строится в разы быстрее.' },
      { h: 'Как заходить в холодную', body: 'Не «здравствуйте, предлагаю услуги», а конкретная польза. Покажи, что уже сделал разбор именно их профиля. Приложи мини-кейс или пример под их нишу. Локальный бизнес в картах, эксперты в нише, комментарии и чаты по теме, доски объявлений, нетворк — где искать клиентов.' },
      { h: 'Касания и кейс', body: 'Продажа часто на 3–7 касании, а не с первого сообщения. Не ответили — не значит «нет», часто просто не вовремя. Возвращайся с новой пользой, а не с «ну что, надумали?». Первый бесплатный или дешёвый кейс окупается потоком тёплых: «смотрите, что я сделал соседнему салону» сильнее любой рекламы.' }
    ],
    lifehack: 'Каждое холодное касание сопровождай МИНИ-РАЗБОРОМ конкретного клиента, а не общим предложением услуг. «Открыл ваш профиль, вижу проблему N, вот как это можно починить» — конверсия в диалог в разы выше, чем «здравствуйте, предлагаю услуги». Ты потратишь 5 минут на клиента, но покажешь работу до найма.',
    links: [
      { label: 'Яндекс Карты', url: 'yandex.ru/maps', note: 'Найти локальный бизнес с слабым контентом в своём городе — источник холодных лидов' }
    ]
  },
  134: {
    intro: 'Слова «я хороший специалист» не значат ничего. Кейс до/после показывает результат, а не обещание. Один сильный кейс закрывает больше сделок, чем длинный прайс — потому что доказывает, а не рассказывает.',
    notes: [
      { h: 'Анатомия сильного кейса', body: 'Точка А: что было и в чём была боль клиента. Что сделал: коротко, без технических деталей про нейросети. Точка Б: что стало, с конкретными цифрами. «Стало лучше» — пусто, «охваты с 300 до 12 000» — убедительно. Даже «сэкономил 15 часов в неделю» — сильный измеримый результат.' },
      { h: 'Визуал: покажи, а не рассказывай', body: 'Экран «было/стало» рядом — мозг считывает разницу мгновенно. До: унылый профиль. После: живая лента роликов. Собери карусель или короткое видео-до/после. Считай то, что важно клиенту: заявки, охваты, продажи, время, а не свои внутренние метрики про количество генераций.' },
      { h: 'Где показывать', body: 'В первом холодном сообщении — как аргумент. На лендинге и в закрепе профиля — как витрина. В боте-прогреве — как касание, снимающее возражение. Нет заказов — сделай кейс на своём проекте или на друге: «оживил мёртвый профиль знакомого» уже доказательство процесса и качества.' }
    ],
    lifehack: 'Если платных клиентов ещё нет — сделай кейс на себе или на друге и оформи его точно так же, как настоящий: точка А, что сделал, точка Б с цифрами, экран «было/стало». Тестовый кейс продаёт почти так же, как реальный: он показывает процесс и уровень качества, а не ждёт разрешения на существование.',
    links: [
      { label: 'Canva', url: 'canva.com', note: 'Собрать карусель кейса «было / стало» с цифрами и логотипом бренда' }
    ]
  },
  135: {
    intro: 'В одиночку ты упираешься в 24 часа и выгорание. Больше заказов руками = больше усталости, а не денег. Масштаб — это система, а не «работать ещё усерднее». Финальный урок Академии — как выйти из этого потолка.',
    notes: [
      { h: 'Шаблоны и конвейер', body: 'Каждый новый заказ с нуля — потеря времени. Шаблоны сценариев, промптов, монтажа, писем клиентам. Собери банк промптов под задачи. Разбей «сделать ролик» на этапы: сценарий → визуал → озвучка → монтаж. Каждый этап отлажен и предсказуем по времени, стандарт качества на каждом шаге — меньше правок в конце.' },
      { h: 'Агенты и делегирование', body: 'Рутину отдаём ИИ: генерация, черновики, ответы, постинг. Связки на n8n или Make делают работу по расписанию. Claude Code и агенты берут повторяемые технические задачи. Первым нанимай человека на то, что забирает время и не растит тебя: монтажёр, ассистент на касания, копирайтер. Дай шаблон и стандарт — не учи с нуля каждый раз.' },
      { h: 'Поток вместо гонки', body: 'Цель масштаба — стабильный поток, а не разовые рывки. Считай пропускную способность конвейера в заказах в неделю. Узкое место расширяй первым: шаблоном, агентом или человеком. Всё, что ты сделал в Академии — от медийности до автоматизации — сходится в этой точке: ты строишь систему, а не работаешь работу.' }
    ],
    lifehack: 'Раз в неделю задавай себе один вопрос: «где сейчас узкое место моего конвейера?» и расширяй ИМЕННО его — шаблоном, агентом или наймом. Расширять другие места, пока узкое течёт, — потерянные деньги. Одно правильное расширение в неделю за квартал утраивает пропускную способность, а «работать больше» просто выгорает быстрее.',
    links: [
      { label: 'ClickUp', url: 'clickup.com', note: 'Управление конвейером: задачи, статусы, шаблоны, узкие места — видно потолок системы' }
    ]
  }
});

/* ===== module: academy ===== */
/* ================= ACADEMY: Академия OKO (префикс ac-) =================
   Курс «Нейросети 2026»: 5 уроков (видео + слайды + тест + практика + мини-игра),
   per-урок прогресс и официальный сертификат за КАЖДЫЙ урок
   (canvas: печать + подпись из core-ext). */

const AC_VIDEO_URL = 'https://true-journey-418.higgsfield.app/media/oko_lesson1_web.mp4'; // урок 1, хостится на домене приложения
const AC_PASS = 70;

/* ================= РЕЕСТР ИНСТРУМЕНТОВ (реф-ссылки в кнопках уроков) =================
   Кнопки «Инструменты урока» авто-подставляются по инструментам, упомянутым в слайдах.
   url — куда ведёт кнопка. Реф-ссылки (наша выгода) добавлять сюда же в поле url:
   как только у OKO есть партнёрская ссылка на инструмент — меняем url на неё. */
const AC_TOOLS = {
  'chatgpt':     {name:'ChatGPT',       url:'https://chatgpt.com',            note:'Универсальный ИИ для текста'},
  'claude':      {name:'Claude',        url:'https://claude.ai',              note:'Король длинных текстов и кода'},
  'claude code': {name:'Claude Code',   url:'https://claude.com/claude-code', note:'Код по описанию словами'},
  'gemini':      {name:'Gemini',        url:'https://gemini.google.com',      note:'Свежий поиск + связка с Google'},
  'midjourney':  {name:'Midjourney',    url:'https://midjourney.com',         note:'Лучшая эстетика картинок'},
  'flux':        {name:'Flux',          url:'https://blackforestlabs.ai',     note:'Фотореализм почти бесплатно'},
  'nano banana': {name:'Nano Banana',   url:'https://gemini.google.com',      note:'Правки и текст прямо на картинке'},
  'higgsfield':  {name:'Higgsfield',    url:'https://higgsfield.ai',          note:'Все модели в одном окне'},
  'veo':         {name:'Veo',           url:'https://deepmind.google/models/veo/', note:'Видео премиум-качества + звук'},
  'kling':       {name:'Kling',         url:'https://klingai.com',            note:'Кино за копейки'},
  'runway':      {name:'Runway',        url:'https://runwayml.com',           note:'Видео под маркетинг и монтаж'},
  'elevenlabs':  {name:'ElevenLabs',    url:'https://elevenlabs.io',          note:'Премиум-озвучка'},
  'suno':        {name:'Suno',          url:'https://suno.com',               note:'Готовый трек по запросу'},
  'cursor':      {name:'Cursor',        url:'https://cursor.com',             note:'IDE с ИИ для кода'},
  'lovable':     {name:'Lovable',       url:'https://lovable.dev',            note:'Приложение по описанию'},
  'bolt':        {name:'Bolt',          url:'https://bolt.new',               note:'Сайт/приложение в браузере'},
  'vercel':      {name:'Vercel',        url:'https://vercel.com',             note:'Бесплатный деплой'},
  'cloudflare':  {name:'Cloudflare',    url:'https://cloudflare.com',         note:'Бесплатный хостинг/CDN'},
  'n8n':         {name:'n8n',           url:'https://n8n.io',                 note:'Автоматизация связок'},
  'make':        {name:'Make',          url:'https://make.com',               note:'Визуальная автоматизация'},
  'zapier':      {name:'Zapier',        url:'https://zapier.com',             note:'Связки 9000+ сервисов'},
};
/* Достаём инструменты, упомянутые в слайдах урока (в <b>…</b>), уникально и по порядку. */
function acLessonTools(L){
  const seen = new Set(), out = [];
  const hay = (L.slides||[]).map(s=>(s.pts||[]).join(' ')).join(' ').toLowerCase();
  // сначала многословные ключи (higgsfield soul → higgsfield), затем одиночные
  for(const key of Object.keys(AC_TOOLS)){
    if(seen.has(key)) continue;
    if(hay.includes(key)){ seen.add(key); out.push(AC_TOOLS[key]); }
  }
  return out;
}
function acToolsHtml(L){
  const tools = acLessonTools(L);
  if(!tools.length) return '';
  return `<h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Инструменты урока</h2>
    <p class="dim" style="font-size:12.5px;line-height:1.5;margin:-2px 0 12px">Открывай прямо из урока — пробуй на практике.</p>
    <div class="ac-tools">${tools.map(t=>`
      <a class="ac-tool" href="${t.url}" target="_blank" rel="noopener noreferrer">
        <span class="ac-tool-b"><b>${esc(t.name)}</b><small>${esc(t.note)}</small></span>
        <span class="ac-tool-go">${I('chev')}</span>
      </a>`).join('')}</div>`;
}

/* ================= КУРС: 5 УРОКОВ ================= */
const AC_COURSE_AI = [

/* ---------- УРОК 1: Карта нейросетей 2026 ---------- */
{
  title:'Карта нейросетей 2026',
  sub:'3:24 · видео + слайды + тест + игра',
  dur:'3:24',
  videoUrl: AC_VIDEO_URL,
  c1:'КАРТА НЕЙРОСЕТЕЙ', c2:'2026',
  slides:[
  {t:'Карта категорий', pts:[
    'Правило №1: сначала <b>задача</b>, потом модель',
    'Все нейросети — всего <b>7 семей</b>: текст, картинки, видео, код, озвучка, музыка, автоматизация',
    'Понял, к какой семье относится задача — полдела сделано',
    'Новинки выходят каждую неделю — карта важнее списка'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round"><circle cx="60" cy="35" r="10"/><circle cx="60" cy="35" r="3.5" fill="currentColor" stroke="none"/><path d="M60 25V10M60 45v15M50 32 22 20M70 32l28-12M50 39 22 52M70 39l28 13M48 35H30M72 35h18" opacity=".8"/><circle cx="22" cy="19" r="5"/><circle cx="98" cy="19" r="5"/><circle cx="22" cy="52" r="5"/><circle cx="98" cy="52" r="5"/><circle cx="60" cy="9" r="5"/><circle cx="60" cy="61" r="5"/><circle cx="28" cy="35" r="5"/></svg>'},
  {t:'Текст: рабочий конь', pts:[
    '<b>ChatGPT</b> — универсальный солдат, хорош почти во всём',
    '<b>Claude</b> — король больших текстов и кода, контекст на десятки страниц',
    '<b>Gemini</b> — свежий поиск и связка с Google',
    'Код или длинный документ → Claude · факт из сети → Gemini · остальное → ChatGPT'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round"><rect x="14" y="10" width="66" height="42" rx="9"/><path d="M26 24h42M26 33h34M26 42h40"/><path d="M34 52 28 62l16-10" stroke-linejoin="round"/><rect x="88" y="26" width="22" height="30" rx="6" opacity=".7"/><path d="M93 34h12M93 41h9M93 48h12" opacity=".7"/></svg>'},
  {t:'Картинки: тут крутятся деньги', pts:[
    '<b>Midjourney</b> — лучшая эстетика, арт и «вау»',
    '<b>Flux</b> — фотореализм почти бесплатно, ~5¢ за кадр',
    '<b>Nano Banana</b> — правки и текст прямо на картинке, без перегенерации',
    '<b>Higgsfield</b> собирает все модели в одном окне'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="8" width="80" height="54" rx="8"/><circle cx="43" cy="24" r="6"/><path d="M20 54l24-20 16 13 18-16 22 20"/><path d="M104 12l3 6 6 1-4.5 4 1 6-5.5-3-5.5 3 1-6-4.5-4 6-1z" fill="currentColor" stroke="none" opacity=".9"/></svg>'},
  {t:'Видео: дорого и зрелищно', pts:[
    '<b>Veo</b> — лучшее качество + родной звук, почти продакшн',
    '<b>Kling</b> — кино за копейки, ~10¢ за секунду',
    '<b>Runway</b> — заточен под маркетинг и монтаж',
    'Тест идеи → Kling · финалка клиенту → Veo'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="18" y="8" width="84" height="46" rx="8"/><path d="M52 22v18l16-9z" fill="currentColor" stroke="none"/><path d="M18 62h84" opacity=".6"/><circle cx="34" cy="62" r="3" fill="currentColor" stroke="none"/><path d="M26 16h6M26 46h6M88 16h6M88 46h6" opacity=".6"/></svg>'},
  {t:'Озвучка и музыка', pts:[
    '<b>ElevenLabs</b> — премиум-голос для витринных проектов',
    '<b>Локальные нейро-TTS</b> — бесплатно и безлимит (этот урок озвучен именно так)',
    '<b>Suno</b> — готовый трек по одному запросу',
    'Музыку всегда подбирай под смысл ролика'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round"><path d="M14 35v0M22 28v14M30 20v30M38 26v18M46 14v42M54 24v22M62 30v10M70 22v26M78 28v14"/><path d="M96 46V16l14-4v26" stroke-linejoin="round"/><circle cx="91" cy="46" r="5.5"/><circle cx="105" cy="38" r="5.5"/></svg>'},
  {t:'Код без программиста', pts:[
    '<b>Cursor</b>, <b>Lovable</b>, <b>Claude Code</b> — тройка лидеров',
    'Описываешь словами → получаешь рабочее приложение',
    'MVP за вечер вместо недель разработки',
    'Чем точнее ТЗ, тем меньше правок'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M38 18 18 35l20 17M82 18l20 17-20 17M68 12 52 58"/></svg>'},
  {t:'Как выбирать: формула', pts:[
    '<b>Задача → Категория → Модель</b> — три шага, пара секунд',
    'Лазейка: <b>Higgsfield Ultra</b> — десятки топ-моделей одной подпиской',
    'Связка «локальные голоса + n8n на своём сервере» роняет себестоимость до копеек',
    'Не плати каждой модели по отдельности'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M16 12h88L74 34v22l-12 8V34z"/><path d="M60 10v0" opacity=".5"/><circle cx="99" cy="52" r="9"/><path d="M96 52l2.5 2.5 5-5"/></svg>'},
  {t:'Чек-лист урока', pts:[
    'Текст → Claude и компания',
    'Картинки → Flux (объём) / Midjourney (вау)',
    'Видео → Kling (тест) / Veo (финал)',
    'Музыка → Suno · Озвучка → локальные TTS',
    'Экономия → агрегаторы и связки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="6" width="68" height="58" rx="8"/><path d="M36 20l4 4 7-8M36 36l4 4 7-8M36 52l4 4 7-8" stroke-width="2.8"/><path d="M54 21h28M54 37h28M54 53h20"/></svg>'},
  ],
  quiz:[
  {q:'С чего начинается выбор нейросети по карте из урока?', o:['С названия самой хайповой модели','С формулировки задачи','С цены подписки','С обзоров блогеров'], a:1},
  {q:'Кому отдать длинный документ или код?', o:['Midjourney','Suno','Claude','Kling'], a:2},
  {q:'Фотореализм почти бесплатно (~5 центов за кадр) — это…', o:['Flux','Veo','ChatGPT','Runway'], a:0},
  {q:'«Кино за копейки» — примерно 10 центов за секунду видео — это…', o:['Veo','Kling','Nano Banana','Cursor'], a:1},
  {q:'Готовый музыкальный трек по одному запросу делает…', o:['ElevenLabs','Gemini','Lovable','Suno'], a:3},
  {q:'Главная формула карты нейросетей 2026:', o:['Модель → Цена → Задача','Категория → Модель → Задача','Задача → Категория → Модель','Промпт → Модель → Результат'], a:2},
  ],
  pairs:[
  ['Claude','Длинный документ и код'],
  ['Flux','Фотореализм за копейки'],
  ['Kling','Дёшево протестировать видео-идею'],
  ['Suno','Трек под ролик за один запрос'],
  ['Nano Banana','Поправить текст на готовой картинке'],
  ],
  task:{
    intro:'Возьми свою реальную задачу и разложи её по формуле урока — так, как отдал бы нейросети в работу:',
    chips:['Задача','Категория','Модель','Почему именно она'],
    ph:'Пример: Задача — обложка для урока. Категория — картинки. Модель — Flux: нужен фотореализм и объём дёшево. Формат — вертикаль 2:3, тёмный фон, лаймовый акцент.',
    verdict:'Категория выбрана верно, модель соответствует бюджету задачи. Совет куратора: добавляй в промпт ожидаемый формат результата — модель ответит точнее.'
  }
},

/* ---------- УРОК 2: Промпт-инжиниринг ---------- */
{
  title:'Промпт-инжиниринг',
  sub:'2:37 · формула промпта · few-shot · chain-of-thought',
  dur:'2:37', videoUrl:'https://true-journey-418.higgsfield.app/media/oko_lesson2_web.mp4',
  c1:'ПРОМПТ', c2:'ИНЖИНИРИНГ',
  slides:[
  {t:'Формула сильного промпта', pts:[
    '<b>Роль → Задача → Контекст → Формат → Ограничения</b> — пять блоков',
    'Слабый промпт: «напиши пост». Сильный — закрывает все пять блоков',
    'Чем меньше модель догадывается, тем точнее результат',
    'Формула работает в любой нейросети: ChatGPT, Claude, Gemini'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="6" y="26" width="16" height="18" rx="4"/><rect x="30" y="26" width="16" height="18" rx="4"/><rect x="54" y="26" width="16" height="18" rx="4"/><rect x="78" y="26" width="16" height="18" rx="4"/><rect x="100" y="26" width="16" height="18" rx="4"/><path d="M23 35h5M47 35h5M71 35h5M95 35h3" opacity=".7"/><path d="M14 20v-8h94v46h-6" opacity=".35"/><circle cx="14" cy="52" r="2" fill="currentColor" stroke="none"/><circle cx="38" cy="52" r="2" fill="currentColor" stroke="none"/><circle cx="62" cy="52" r="2" fill="currentColor" stroke="none"/></svg>'},
  {t:'Роль и задача', pts:[
    '«Ты — маркетолог с 10-летним опытом» — роль включает нужный пласт знаний',
    'Задача — <b>глаголом</b>: напиши, сравни, разложи, предложи 5 вариантов',
    'Одна задача = один промпт. Несколько — режь на шаги',
    'Роль меняет и стиль, и глубину ответа'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="30" cy="22" r="9"/><path d="M14 58c2-14 8-20 16-20s14 6 16 20"/><path d="M52 35h28" opacity=".8"/><path d="M74 29l8 6-8 6" opacity=".8"/><circle cx="98" cy="35" r="14"/><circle cx="98" cy="35" r="8"/><circle cx="98" cy="35" r="2.5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Контекст решает всё', pts:[
    'Дай факты: продукт, аудитория, цена, тон бренда',
    'Модель <b>не телепат</b> — что не сказал, то она выдумает',
    'Вставляй примеры своих текстов — стиль скопируется',
    'Большие документы и таблицы отдавай целиком, не пересказом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="20" width="52" height="42" rx="6"/><path d="M30 14h52v42" opacity=".55"/><path d="M38 8h52v42" opacity=".3"/><path d="M32 32h32M32 41h24M32 50h30"/><circle cx="98" cy="52" r="10"/><path d="M105 59l9 9"/></svg>'},
  {t:'Формат и ограничения', pts:[
    'Проси конкретно: <b>таблица</b>, нумерованный список, JSON',
    'Ограничь объём: «до 500 знаков», «ровно 3 варианта»',
    '«Без воды, без вступлений, без клише» — негативные ограничения',
    'Заданный формат = результат сразу готов к использованию'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="14" y="10" width="66" height="50" rx="6"/><path d="M14 26h66M14 42h66M36 10v50M58 10v50"/><path d="M92 18h18M92 30h14M92 42h18M92 54h10" opacity=".7"/><path d="M88 51l-4 10" opacity=".7"/></svg>'},
  {t:'Few-shot: покажи примеры', pts:[
    '2–3 пары <b>«вход → выход»</b> прямо в промпте',
    'Модель копирует паттерн точнее любых описаний',
    'Лучший способ передать стиль, структуру и тон',
    'Работает для заголовков, писем, карточек товаров'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="8" y="8" width="20" height="14" rx="4"/><path d="M32 15h10M38 11l6 4-6 4"/><rect x="48" y="8" width="20" height="14" rx="4" fill="currentColor" fill-opacity=".15"/><rect x="8" y="28" width="20" height="14" rx="4"/><path d="M32 35h10M38 31l6 4-6 4"/><rect x="48" y="28" width="20" height="14" rx="4" fill="currentColor" fill-opacity=".15"/><rect x="80" y="14" width="32" height="24" rx="6"/><path d="M88 22h16M88 30h12"/><path d="M96 44v10M92 50l4 4 4-4"/></svg>'},
  {t:'Chain-of-thought', pts:[
    '«<b>Рассуждай по шагам</b>» — и точность на логике резко растёт',
    'Сложную задачу дели на этапы: сначала план, потом исполнение',
    'Попроси модель проверить собственный ответ перед выдачей',
    'Расчёты, стратегия, договоры — всегда через шаги'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 58h22V44h22V30h22V16h24"/><circle cx="23" cy="52" r="3" fill="currentColor" stroke="none"/><circle cx="45" cy="38" r="3" fill="currentColor" stroke="none"/><circle cx="67" cy="24" r="3" fill="currentColor" stroke="none"/><path d="M96 10l3 6 6 1-4.5 4 1 6-5.5-3-5.5 3 1-6-4.5-4 6-1z" fill="currentColor" stroke="none" opacity=".9"/></svg>'},
  {t:'Итерация и типовые ошибки', pts:[
    'Первый ответ — <b>черновик</b>: уточняй «короче», «жёстче», «под B2B»',
    'Ошибка №1: несколько задач в одном промпте',
    'Ошибка №2: нет формата — получаешь простыню текста',
    'Ошибка №3: нет примеров там, где важен стиль'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12a23 23 0 1 1-21 14"/><path d="M32 14l6 12 13-4"/><circle cx="60" cy="35" r="8" opacity=".7"/><path d="M100 22v14M100 44v.5" stroke-width="3"/></svg>'},
  {t:'Промпты для бизнеса', pts:[
    'Собери <b>библиотеку промптов</b> компании — как регламенты',
    'Шаблоны с переменными: {товар}, {аудитория}, {акция}',
    'Один отлаженный промпт экономит часы каждый день',
    'Прогони новый промпт 3–5 раз — проверь стабильность результата'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="22" width="68" height="38" rx="8"/><path d="M48 22v-6a6 6 0 0 1 6-6h12a6 6 0 0 1 6 6v6"/><path d="M26 38h28M66 38h28"/><rect x="54" y="33" width="12" height="10" rx="3" fill="currentColor" fill-opacity=".2"/><path d="M104 14l2.5 5 5 .8-3.7 3.4.8 5-4.6-2.5-4.6 2.5.8-5-3.7-3.4 5-.8z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  ],
  quiz:[
  {q:'Правильный порядок блоков сильного промпта:', o:['Формат → Роль → Задача → Контекст','Роль → Задача → Контекст → Формат → Ограничения','Контекст → Роль → Ограничения → Задача','Задача → Формат → Роль → Контекст'], a:1},
  {q:'Few-shot — это…', o:['Попросить ответ покороче','Показать 2–3 примера «вход → выход»','Запустить промпт несколько раз подряд','Урезать контекст до минимума'], a:1},
  {q:'Chain-of-thought повышает точность, потому что модель…', o:['Отвечает быстрее','Тратит меньше токенов','Рассуждает по шагам, а не выдаёт ответ сразу','Подключается к интернету'], a:2},
  {q:'Негативное ограничение в промпте — это…', o:['Грубый тон запроса','Указание, чего НЕ делать: «без воды и клише»','Жалоба на прошлый ответ','Отключение истории чата'], a:1},
  {q:'Первый ответ модели правильнее всего воспринимать как…', o:['Финальный результат','Случайный шум','Черновик для итераций','Ошибку модели'], a:2},
  {q:'Типовая ошибка промпта из урока:', o:['Указать модели роль','Несколько задач в одном промпте без формата','Добавить примеры «вход → выход»','Ограничить объём ответа'], a:1},
  ],
  pairs:[
  ['Роль','«Ты — юрист с 10-летним опытом»'],
  ['Few-shot','2–3 примера «вход → выход»'],
  ['Chain-of-thought','«Рассуждай по шагам»'],
  ['Негативное ограничение','«Без воды и клише»'],
  ['Формат','«Ответ — таблицей из 3 колонок»'],
  ],
  task:{
    intro:'Собери промпт под свою реальную бизнес-задачу по формуле урока:',
    chips:['Роль','Задача','Контекст','Формат','Ограничения'],
    ph:'Пример: Роль — опытный SMM-маркетолог. Задача — напиши 5 заголовков для поста о скидке 20%. Контекст — кофейня в центре, аудитория 20–35. Формат — нумерованный список до 60 знаков. Ограничения — без клише и капслока.',
    verdict:'Все пять блоков формулы на месте — промпт готов к работе. Совет куратора: добавь 1–2 примера «вход → выход», и стиль ответов станет твоим на 100%.'
  }
},

/* ---------- УРОК 3: Генерация картинок PRO ---------- */
{
  title:'Генерация картинок PRO',
  sub:'3:05 · Midjourney v7 · Flux · Nano Banana',
  dur:'3:05', videoUrl:'https://true-journey-418.higgsfield.app/media/oko_lesson3_web.mp4',
  c1:'ГЕНЕРАЦИЯ КАРТИНОК', c2:'PRO',
  slides:[
  {t:'Три топ-модели 2026', pts:[
    '<b>Midjourney v7</b> — эстетика, арт-направление, «вау»-кадры',
    '<b>Flux</b> — фотореализм, дёшево, открытая модель',
    '<b>Nano Banana</b> (Gemini) — правки, текст на картинке, точное следование ТЗ',
    'Выбирай модель под задачу, а не наоборот'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="8" y="14" width="30" height="42" rx="6"/><rect x="45" y="8" width="30" height="48" rx="6"/><rect x="82" y="14" width="30" height="42" rx="6"/><path d="M14 48l8-8 6 5 4-4" opacity=".8"/><circle cx="60" cy="22" r="4"/><path d="M50 48l9-10 7 6 4-5" opacity=".8"/><path d="M90 26h14M90 34h10M90 42h14" opacity=".8"/><path d="M56 60l4 4 6-8" stroke-width="2.8"/></svg>'},
  {t:'Анатомия промпта картинки', pts:[
    '<b>Субъект → Стиль → Свет → Камера → Композиция</b>',
    'Порядок слов = приоритет: главное — в начало',
    'Коротко и конкретно бьёт длинно и расплывчато',
    'Один промпт — одна сцена, без «и ещё…»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="14" y="10" width="58" height="50" rx="7"/><circle cx="34" cy="28" r="7"/><path d="M14 52l16-13 12 9 12-11 12 12"/><path d="M84 14h26M84 26h20M84 38h24M84 50h16" opacity=".75"/><circle cx="80" cy="14" r="1.8" fill="currentColor" stroke="none"/><circle cx="80" cy="26" r="1.8" fill="currentColor" stroke="none"/><circle cx="80" cy="38" r="1.8" fill="currentColor" stroke="none"/><circle cx="80" cy="50" r="1.8" fill="currentColor" stroke="none"/></svg>'},
  {t:'Субъект и стиль', pts:[
    'Конкретика: не «собака», а «щенок корги, 3 месяца, мокрая шерсть»',
    'Стиль: photo, 3D render, flat illustration, oil painting',
    'Эстетики <b>editorial / cinematic / minimalist</b> задают настроение',
    'Материалы и фактуры добавляют реализма: matte, glossy, velvet'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="38" cy="35" r="20"/><circle cx="38" cy="29" r="7"/><path d="M25 48c3-8 7-11 13-11s10 3 13 11"/><path d="M74 18c14-6 26-2 30 6-6 2-10 8-18 8s-14-6-12-14z"/><path d="M78 32l-8 26" opacity=".8"/><circle cx="99" cy="52" r="3" fill="currentColor" stroke="none"/><circle cx="88" cy="58" r="2.4" fill="currentColor" stroke="none" opacity=".6"/></svg>'},
  {t:'Свет и камера', pts:[
    'Свет — половина кадра: <b>golden hour, softbox, neon glow</b>',
    'Объектив: 85mm portrait — лица, wide angle — пространство',
    'Глубина резкости: shallow depth of field, bokeh',
    'Ракурс: eye level, low angle (мощь), top-down (флэтлей)'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="26" cy="20" r="9"/><path d="M26 5v4M26 31v4M11 20h4M37 20h4M15 9l3 3M37 31l-3-3M37 9l-3 3M15 31l3-3"/><rect x="54" y="26" width="52" height="34" rx="7"/><path d="M68 26l5-8h14l5 8"/><circle cx="80" cy="43" r="10"/><circle cx="80" cy="43" r="4" opacity=".7"/><circle cx="99" cy="34" r="1.8" fill="currentColor" stroke="none"/></svg>'},
  {t:'Композиция и формат', pts:[
    'Rule of thirds, negative space, close-up / full shot',
    'Соотношение сторон под площадку: <b>9:16</b> сторис, <b>1:1</b> лента, <b>16:9</b> обложка',
    'Оставляй место под текст, если кадр пойдёт в баннер',
    'Центральная симметрия — приём премиальных брендов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="18" y="8" width="84" height="54" rx="7"/><path d="M46 8v54M74 8v54M18 26h84M18 44h84" opacity=".4"/><circle cx="74" cy="26" r="6" fill="currentColor" fill-opacity=".25"/><circle cx="74" cy="26" r="2.5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Референсы и consistency', pts:[
    'Image reference — прикладывай образец стиля или продукта',
    'Один герой в серии: <b>character reference</b> + фиксированный seed',
    'Бренд-кит в каждом промпте: фирменные цвета, логотип отдельным слоем',
    'Серия в едином стиле перформит лучше одиночных кадров'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="10" y="16" width="34" height="38" rx="6"/><circle cx="27" cy="30" r="6"/><path d="M17 48c2-6 5-9 10-9s8 3 10 9"/><rect x="76" y="16" width="34" height="38" rx="6"/><circle cx="93" cy="30" r="6"/><path d="M83 48c2-6 5-9 10-9s8 3 10 9"/><path d="M50 32h20M50 40h20" opacity=".7"/><path d="M64 26l6 6-6 6M56 34l-6 6 6 6" opacity=".7"/></svg>'},
  {t:'Апскейл и правки', pts:[
    'Генерируй в базовом разрешении, финалку — <b>апскейл до 2K/4K</b>',
    'Руки, текст, мелочи — не перегенерация, а точечная правка (inpaint)',
    'Nano Banana правит словами: «замени надпись, остальное не трогай»',
    'Противоречия в промпте («минимализм, много деталей») ломают кадр'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="12" y="30" width="26" height="20" rx="4"/><path d="M44 40h14M52 34l6 6-6 6"/><rect x="66" y="12" width="44" height="46" rx="6"/><path d="M72 50l12-12 8 6 10-10 8 8" opacity=".8"/><path d="M100 18l2 4 4 .6-3 2.8.7 4-3.7-2-3.7 2 .7-4-3-2.8 4-.6z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Коммерческое использование', pts:[
    'Права на коммерцию — почти всегда на <b>платных тарифах</b>: проверь лицензию',
    'Чужие лица, персонажи и логотипы — нельзя без прав',
    'Храни промпты и исходники — пригодятся для серий и споров',
    'На потоке считай юнит-экономику: цена кадра × объём'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="14" y="12" width="56" height="46" rx="7"/><circle cx="32" cy="26" r="5"/><path d="M14 50l18-14 12 9 10-9 16 14" opacity=".8"/><circle cx="94" cy="42" r="16"/><path d="M94 33v18M99 37c-1.5-2-8-2.5-8 1.5s9 2 9 6.5-7 3.5-9 1.5"/></svg>'},
  ],
  quiz:[
  {q:'Фотореализм дёшево и на открытой модели — это…', o:['Midjourney v7','Flux','Nano Banana','Suno'], a:1},
  {q:'Анатомия промпта картинки из урока:', o:['Стиль → Свет → Субъект → Формат','Субъект → Стиль → Свет → Камера → Композиция','Камера → Субъект → Формат → Свет','Композиция → Свет → Стиль → Камера'], a:1},
  {q:'Поправить надпись на готовой картинке, не трогая остальное, лучше всего умеет…', o:['Kling','Midjourney','Nano Banana','Wan'], a:2},
  {q:'Один и тот же герой во всей серии картинок — это…', o:['Апскейл','Character reference + фиксированный seed','Негативный промпт','Reframe'], a:1},
  {q:'Апскейл нужен, чтобы…', o:['Ускорить генерацию','Поднять разрешение финалки до 2K/4K','Сменить стиль кадра','Убрать фон'], a:1},
  {q:'Что проверить перед коммерческим использованием картинки?', o:['Количество лайков','Лицензию тарифа и отсутствие чужих брендов/лиц','Размер файла','Название модели'], a:1},
  ],
  pairs:[
  ['Midjourney v7','Арт и «вау»-эстетика'],
  ['Flux','Фотореализм за копейки'],
  ['Nano Banana','Правки и текст на картинке'],
  ['85mm portrait','Портрет с мягким боке'],
  ['Апскейл','Финалка в 4K для печати'],
  ],
  task:{
    intro:'Разложи промпт картинки для своего проекта по анатомии из урока:',
    chips:['Субъект','Стиль','Свет','Камера','Композиция'],
    ph:'Пример: Субъект — чашка латте с арт-пенкой. Стиль — editorial photo. Свет — мягкий утренний из окна. Камера — 85mm, shallow depth of field. Композиция — rule of thirds, место под текст справа, формат 4:5.',
    verdict:'Анатомия выдержана: субъект конкретный, свет и камера заданы. Совет куратора: зафиксируй seed и палитру бренда — получишь консистентную серию, а не разрозненные кадры.'
  }
},

/* ---------- УРОК 4: Видео-нейросети ---------- */
{
  title:'Видео-нейросети',
  sub:'Veo 3 · Kling 2 · Runway · Wan',
  dur:'', videoUrl:'',
  c1:'ВИДЕО', c2:'НЕЙРОСЕТИ',
  slides:[
  {t:'Кто есть кто в видео', pts:[
    '<b>Veo 3</b> — топ-качество + родной звук и речь',
    '<b>Kling 2</b> — лучшая цена/качество, идеален для тестов',
    '<b>Runway</b> — контроль, маркетинг, инструменты монтажа',
    '<b>Wan</b> — открытая модель, бесплатно на своём железе'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="8" y="30" width="24" height="28" rx="4"/><rect x="36" y="18" width="24" height="40" rx="4"/><rect x="64" y="26" width="24" height="32" rx="4"/><rect x="92" y="36" width="20" height="22" rx="4"/><path d="M45 32v12l10-6z" fill="currentColor" stroke="none"/><path d="M48 8l2 4 4 .6-3 2.8.7 4-3.7-2-3.7 2 .7-4-3-2.8 4-.6z" fill="currentColor" stroke="none" opacity=".9"/></svg>'},
  {t:'Text-to-video vs Image-to-video', pts:[
    'Text-to-video: быстро, но результат — лотерея',
    '<b>Image-to-video</b>: сначала идеальный кадр, потом «оживление»',
    'Для бренда почти всегда i2v — полный контроль первого кадра',
    'Картинку кадра готовь по анатомии из урока 3'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M10 14h26M10 22h20M10 30h24" opacity=".7"/><path d="M40 22h10M46 18l5 4-5 4" opacity=".7"/><rect x="10" y="42" width="26" height="20" rx="4"/><circle cx="18" cy="49" r="2.4"/><path d="M10 58l8-7 6 5 4-4 8 6" opacity=".8"/><path d="M40 52h10M46 48l5 4-5 4"/><rect x="58" y="18" width="52" height="36" rx="7"/><path d="M78 28v16l14-8z" fill="currentColor" stroke="none"/></svg>'},
  {t:'Первый и последний кадр', pts:[
    'Задаёшь <b>начало и конец</b> — модель строит переход между ними',
    'Связка клипов: последний кадр клипа = первый кадр следующего',
    'Так собирается бесшовный ролик из коротких генераций',
    'Резкие смены сцены прячь за монтажным переходом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="8" y="20" width="32" height="30" rx="5"/><rect x="80" y="20" width="32" height="30" rx="5"/><path d="M44 35c10-14 22 14 32 0" stroke-dasharray="4 5"/><circle cx="24" cy="32" r="4"/><path d="M16 44l8-7 8 6" opacity=".8"/><circle cx="96" cy="30" r="4"/><path d="M88 44l8-8 8 7" opacity=".8"/></svg>'},
  {t:'Липсинк и звук', pts:[
    '<b>Veo 3</b> генерирует речь и шумы прямо в кадре',
    'Отдельный липсинк: готовая озвучка + лицо героя',
    'Звуковые эффекты под каждое движение — кадр оживает',
    'Музыка — под смысл сцены, не «фоном»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="32" r="18"/><circle cx="28" cy="27" r="2" fill="currentColor" stroke="none"/><circle cx="41" cy="27" r="2" fill="currentColor" stroke="none"/><ellipse cx="34" cy="40" rx="6" ry="4"/><path d="M64 24v22M74 18v34M84 26v18M94 14v42M104 24v22" stroke-width="2.8"/></svg>'},
  {t:'Вертикальный контент', pts:[
    '<b>9:16</b> — Reels, Shorts, TikTok: основной формат 2026',
    'Хук в первые 1,5 секунды — иначе свайп',
    'Субтитры обязательны: большинство смотрит без звука',
    'Лучше генерить сразу 9:16, а не спасать горизонталь reframe-ом'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="44" y="4" width="32" height="62" rx="8"/><path d="M55 26v14l12-7z" fill="currentColor" stroke="none"/><path d="M52 52h16M52 58h10" opacity=".8"/><path d="M20 22c4 8 4 18 0 26M12 28c2.5 5 2.5 9 0 14" opacity=".6"/><path d="M100 22c-4 8-4 18 0 26M108 28c-2.5 5-2.5 9 0 14" opacity=".6"/></svg>'},
  {t:'Пайплайн клипа', pts:[
    '<b>Сценарий → раскадровка → кадры-картинки → i2v-клипы → монтаж</b>',
    'Один клип — 5–8 секунд: дальше модель «плывёт»',
    'Озвучка и субтитры — после сборки видеоряда',
    'Финальный проход: цвет, темп под музыку, логотип'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="14" cy="35" r="7"/><circle cx="60" cy="35" r="7"/><circle cx="106" cy="35" r="7"/><circle cx="37" cy="35" r="7" fill="currentColor" fill-opacity=".18"/><circle cx="83" cy="35" r="7" fill="currentColor" fill-opacity=".18"/><path d="M21 35h9M44 35h9M67 35h9M90 35h9"/><path d="M103 32l4 3 4-3" opacity="0"/><path d="M14 20v-8h92v8" opacity=".35"/></svg>'},
  {t:'Экономика продакшна', pts:[
    'Тестируй идею на дешёвой модели, финал — на топовой',
    'Не генери 20 дублей — правь промпт после каждого',
    '30-сек ролик = 4–6 клипов: планируй бюджет заранее',
    'Неудачные клипы — в библиотеку: пригодятся в другом монтаже'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M14 14l40 12-8 10-40-12z"/><path d="M14 26h44v28a6 6 0 0 1-6 6H20a6 6 0 0 1-6-6z"/><path d="M28 44v6M36 40v10M44 44v6" opacity=".8"/><circle cx="92" cy="40" r="16"/><path d="M92 31v18M97 35c-1.5-2-8-2.5-8 1.5s9 2 9 6.5-7 3.5-9 1.5"/></svg>'},
  {t:'Типовые ошибки', pts:[
    'Длинные сцены: режь всё на клипы по 5–8 секунд',
    'Морфинг рук и лиц — меняй ракурс или план',
    'Скачки стиля между клипами — фиксируй референс и палитру',
    'Проверяй результат на телефоне: смотреть будут там'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 96 58H24z"/><path d="M60 28v14M60 50v.5" stroke-width="3"/><path d="M12 20l10 10M22 20l-10 10" opacity=".6"/><path d="M98 18l10 10M108 18l-10 10" opacity=".6"/></svg>'},
  ],
  quiz:[
  {q:'Родной звук и речь прямо при генерации видео даёт…', o:['Kling 2','Wan','Veo 3','Flux'], a:2},
  {q:'Для брендового ролика с контролем каждого кадра выбирают…', o:['Text-to-video','Image-to-video','Апскейл','Липсинк'], a:1},
  {q:'Бесшовная склейка нейро-клипов — это когда…', o:['Все клипы одной длины','Последний кадр клипа = первый кадр следующего','Одна модель на все клипы','Музыка без пауз'], a:1},
  {q:'Субтитры в вертикальном ролике обязательны, потому что…', o:['Так требует платформа','Большинство смотрит без звука','Иначе не пройти модерацию','Это просто красиво'], a:1},
  {q:'Оптимальная длина одного нейро-клипа:', o:['1–2 секунды','5–8 секунд','20–30 секунд','1 минута'], a:1},
  {q:'Дёшево протестировать видео-идею лучше всего в…', o:['Veo 3','Kling 2','ElevenLabs','Nano Banana'], a:1},
  ],
  pairs:[
  ['Veo 3','Качество + родной звук'],
  ['Kling 2','Цена/качество для тестов'],
  ['Runway','Контроль и инструменты монтажа'],
  ['Image-to-video','Оживить готовый кадр'],
  ['Первый-последний кадр','Бесшовный переход между клипами'],
  ],
  task:{
    intro:'Распиши пайплайн 30-секундного ролика о своём продукте:',
    chips:['Сценарий','Кадры','Модель','Клипы 5–8 с','Звук'],
    ph:'Пример: Сценарий — 3 сцены о доставке за 15 минут. Кадры — Flux, 9:16. Модель — тест в Kling 2, финал в Veo 3. Клипы — 5 по 6 секунд, склейка «первый-последний кадр». Звук — озвучка + караоке-сабы.',
    verdict:'Пайплайн рабочий: сцены короткие, склейка по кадрам, звук после сборки. Совет куратора: заложи один запасной клип на сцену — сэкономишь время, когда генерация «поплывёт».'
  }
},

/* ---------- УРОК 5: Свой ИИ-ассистент ---------- */
{
  title:'Свой ИИ-ассистент',
  sub:'агент под бизнес · RAG · Telegram',
  dur:'', videoUrl:'',
  c1:'СВОЙ', c2:'ИИ-АССИСТЕНТ',
  slides:[
  {t:'Зачем бизнесу агент', pts:[
    'Отвечает <b>24/7 за секунды</b> — клиент не ждёт утра',
    'Снимает 60–80% типовых вопросов с людей',
    'Не болеет, не выгорает, помнит все регламенты',
    'Окупается уже на зарплате первого оператора поддержки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="18" width="44" height="34" rx="10"/><circle cx="34" cy="33" r="3.5" fill="currentColor" stroke="none"/><circle cx="50" cy="33" r="3.5" fill="currentColor" stroke="none"/><path d="M34 43c2.5 3 9.5 3 13 0"/><path d="M42 18v-8M38 10h8" opacity=".7"/><circle cx="92" cy="35" r="17"/><path d="M92 25v10l7 5"/></svg>'},
  {t:'Как устроен агент', pts:[
    '<b>LLM + системный промпт + база знаний + интеграции</b>',
    'Это не чат-бот по кнопкам: понимает свободную речь',
    'LLM — «мозг», база знаний — «память», интеграции — «руки»',
    'Собирается без программиста: конструкторы + API'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="13"/><path d="M55 35a5 5 0 0 1 10 0" opacity=".7"/><path d="M60 22V8M60 48v14M47 32 20 20M73 32l27-12M47 40 20 52M73 40l27 12"/><rect x="10" y="12" width="16" height="12" rx="3"/><rect x="94" y="12" width="16" height="12" rx="3"/><rect x="10" y="46" width="16" height="12" rx="3"/><rect x="94" y="46" width="16" height="12" rx="3"/></svg>'},
  {t:'База знаний (RAG)', pts:[
    'FAQ, прайсы, регламенты, скрипты продаж → база знаний',
    'Агент ищет релевантный фрагмент и отвечает <b>по фактам</b>',
    'Обновил документ — агент сразу «поумнел»',
    'Золотое правило: не знаешь — не выдумывай, зови человека'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><ellipse cx="42" cy="16" rx="26" ry="8"/><path d="M16 16v38c0 4.4 11.6 8 26 8s26-3.6 26-8V16"/><path d="M16 35c0 4.4 11.6 8 26 8s26-3.6 26-8" opacity=".6"/><circle cx="94" cy="44" r="11"/><path d="M102 52l10 10"/></svg>'},
  {t:'Системный промпт агента', pts:[
    'Личность и тон бренда: как здоровается, как отвечает',
    'Жёсткие правила: что нельзя обещать и обсуждать',
    'Сценарии эскалации: когда переключать на менеджера',
    'Примеры идеальных диалогов — few-shot из урока 2'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M34 6h40l14 14v44H34z"/><path d="M74 6v14h14" opacity=".7"/><path d="M44 30h32M44 39h24M44 48h30"/><circle cx="24" cy="24" r="8" opacity=".7"/><path d="M21 24l2.5 2.5 4.5-5" opacity=".7"/></svg>'},
  {t:'Подключение к Telegram', pts:[
    'Бот создаётся через <b>BotFather</b> за две минуты',
    'Схема: сообщение → webhook → сервер → LLM → ответ',
    'Кнопки для сценариев + свободный диалог для всего остального',
    'Клиенты уже в мессенджере — не надо загонять их на сайт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M14 32 104 8 82 60 56 44z"/><path d="M56 44l10-16" opacity=".7"/><path d="M56 44v14l10-9" opacity=".7"/></svg>'},
  {t:'Продажи и поддержка на автомате', pts:[
    'Квалификация лида: агент сам задаёт уточняющие вопросы',
    'Запись на встречу и оплата — прямо в чате',
    'Горячий лид мгновенно улетает менеджеру с резюме диалога',
    'Ночные заявки больше не теряются'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 10h60l-18 20v18l-14 8V30z"/><circle cx="96" cy="22" r="9"/><path d="M82 52c2-9 7-13 14-13s12 4 14 13" opacity=".9"/><path d="M62 52h8" opacity=".7"/><path d="M66 48l4 4-4 4" opacity=".7"/></svg>'},
  {t:'Метрики качества', pts:[
    '<b>Доля решённых без человека</b> — главная метрика',
    'Точность: прогоняй агента по тест-набору вопросов',
    'Время ответа и CSAT — оценка клиента после диалога',
    'Еженедельно читай логи: там готовый список улучшений'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M14 8v52h94"/><rect x="26" y="38" width="12" height="22" rx="3"/><rect x="48" y="26" width="12" height="34" rx="3"/><rect x="70" y="32" width="12" height="28" rx="3"/><rect x="92" y="16" width="12" height="44" rx="3" fill="currentColor" fill-opacity=".18"/><path d="M26 30l22-10 22 6 22-14" opacity=".6" stroke-dasharray="3 4"/></svg>'},
  {t:'Безопасность', pts:[
    'Персональные данные клиентов — не в промпты и не в логи',
    '<b>Prompt-injection</b>: «забудь инструкции» — агент должен устоять',
    'Ограничивай полномочия: скидки и возвраты подтверждает человек',
    'Тестируй агента как злоумышленник до запуска'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 6 92 16v18c0 16-13 27-32 32C41 61 28 50 28 34V16z"/><path d="M48 34l8 8 16-16" stroke-width="2.8"/></svg>'},
  ],
  quiz:[
  {q:'Главная выгода ИИ-агента для бизнеса:', o:['Заменяет сайт компании','Снимает 60–80% типовых вопросов 24/7','Ведёт бухгалтерию','Пишет код за разработчика'], a:1},
  {q:'База знаний (RAG) нужна, чтобы агент…', o:['Отвечал быстрее','Отвечал по фактам компании, а не выдумывал','Работал без интернета','Экономил токены'], a:1},
  {q:'Что задаёт системный промпт агента?', o:['Скорость сервера','Тон бренда, правила и сценарии эскалации','Дизайн окна чата','Цену подписки'], a:1},
  {q:'Бот в Telegram создаётся через…', o:['BotFather','Госуслуги','App Store','Настройки телефона'], a:0},
  {q:'Главная метрика качества агента:', o:['Длина ответов','Доля вопросов, решённых без человека','Число кнопок в меню','Количество стикеров'], a:1},
  {q:'Prompt-injection — это…', o:['Способ ускорить ответ','Попытка вредоносным сообщением перехватить управление агентом','Обновление базы знаний','Платная функция LLM'], a:1},
  ],
  pairs:[
  ['База знаний (RAG)','Ответы по фактам компании'],
  ['Системный промпт','Тон бренда и жёсткие правила'],
  ['BotFather','Создание Telegram-бота'],
  ['Эскалация','Передача сложного кейса человеку'],
  ['CSAT','Оценка клиента после диалога'],
  ],
  task:{
    intro:'Спроектируй своего ИИ-агента: ниша, типовые вопросы, тон, эскалация:',
    chips:['Ниша','5 вопросов','Тон','База знаний','Эскалация'],
    ph:'Пример: Ниша — студия маникюра. Вопросы — цены, запись, адрес, мастера, уход. Тон — тёплый, на «вы». База — прайс и FAQ. Эскалация — жалобы и переносы записи → администратору.',
    verdict:'Каркас агента готов: типовые вопросы покрыты, эскалация продумана. Совет куратора: собери тест-набор из 20 реальных диалогов и прогоняй его после каждого изменения промпта.'
  }
},
];

/* ================= КУРС 2 (ПРЕМИУМ): «КОНТЕНТ И REELS» =================
   3 полноценных урока со слайдами, тестом, практикой и мини-игрой.
   Гейт: подписка PRO+ ИЛИ разовая покупка. */
const AC_COURSE_REELS = [

/* ---------- УРОК: Вирусный сценарий за 15 минут ---------- */
{
  title:'Вирусный сценарий за 15 минут',
  sub:'хук · удержание · триггеры репостов',
  dur:'', videoUrl:'',
  c1:'ВИРУСНЫЙ', c2:'СЦЕНАРИЙ',
  slides:[
  {t:'Анатомия вирусного клипа', pts:[
    'Сначала <b>структура</b>, потом съёмка — не наоборот',
    'Четыре блока: <b>хук → удержание → развязка → CTA</b>',
    'Один ролик — одна мысль, всё лишнее режь',
    'Длина под смысл: чаще 7–40 секунд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="10" y="14" width="18" height="42" rx="4" fill="currentColor" fill-opacity=".18"/><rect x="34" y="14" width="18" height="42" rx="4"/><rect x="58" y="14" width="18" height="42" rx="4"/><rect x="82" y="14" width="18" height="42" rx="4"/><path d="M19 24v22M43 24v22M67 24v22M91 24v22" opacity=".6"/><path d="M104 20v30" opacity=".5"/><path d="M14 62h92" opacity=".4"/></svg>'},
  {t:'Хук: первые 2 секунды', pts:[
    'В первые 2 секунды — <b>обещание выгоды</b> или интрига',
    'Хук-текст на экране <b>плюс</b> голос — работают вместе',
    'Заходит: вопрос, цифра, конфликт, «не делай это»',
    'Никаких длинных заставок — сразу к сути'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M28 46 20 12l30 20-14 4 10 20z" fill="currentColor" stroke="none" opacity=".9"/><path d="M64 20h40M64 32h32M64 44h40" opacity=".75"/><circle cx="58" cy="20" r="2" fill="currentColor" stroke="none"/><circle cx="58" cy="32" r="2" fill="currentColor" stroke="none"/><circle cx="58" cy="44" r="2" fill="currentColor" stroke="none"/></svg>'},
  {t:'Удержание: петли внимания', pts:[
    'Микро-обещания: «дальше покажу, как…» — тянут дальше',
    'Смена плана каждые <b>2–3 секунды</b> — глазу не скучно',
    'Показывай визуальный прогресс к развязке',
    'Убирай паузы и «воздух» — плотность держит досмотр'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 40c14-30 30 30 44 0s30-30 44 0" opacity=".85"/><circle cx="12" cy="40" r="3" fill="currentColor" stroke="none"/><circle cx="34" cy="28" r="3" fill="currentColor" stroke="none"/><circle cx="56" cy="40" r="3" fill="currentColor" stroke="none"/><circle cx="78" cy="52" r="3" fill="currentColor" stroke="none"/><circle cx="100" cy="40" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Сторителлинг за 20 секунд', pts:[
    'Схемы, что цепляют: <b>было → стало</b>, ошибка → решение',
    'Конфликт в начале держит до самой развязки',
    'Эмоция важнее фактов — сначала прочувствовать',
    'Герой = зритель: он должен узнать себя'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="10" y="18" width="38" height="34" rx="6"/><path d="M18 44l10-9 7 6 5-5" opacity=".6"/><rect x="72" y="18" width="38" height="34" rx="6" fill="currentColor" fill-opacity=".15"/><path d="M80 44l9-12 7 8 6-6"/><path d="M52 35h16M62 30l6 5-6 5"/></svg>'},
  {t:'Триггеры репостов', pts:[
    'Люди делятся ради <b>пользы, статуса, эмоции, узнавания</b>',
    '«Сохрани, чтобы не потерять» — двигает в сохранения',
    'Спорное, но честное мнение = комментарии и споры',
    'Дай зрителю повод сказать: «это про меня»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="26" cy="35" r="9"/><circle cx="90" cy="18" r="8"/><circle cx="90" cy="52" r="8"/><path d="M34 31l48-10M34 39l48 8"/><path d="M22 35l3 3 6-7" opacity=".7"/></svg>'},
  {t:'CTA без впаривания', pts:[
    'Один призыв на ролик — не распыляй внимание',
    'Мягко: «подпишись, если было полезно»',
    'Вопрос в конце — запускает комментарии',
    'Закреплённый комментарий продолжает воронку'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="24" y="24" width="72" height="26" rx="13" fill="currentColor" fill-opacity=".16"/><path d="M40 37h30" /><path d="M66 31l7 6-7 6"/><path d="M60 12v6M84 16l-4 5M36 16l4 5" opacity=".6"/></svg>'},
  {t:'Форматы, что залетают', pts:[
    'Обучалка, разбор, реакция, POV, листикл-подборка',
    'Бери тренд и <b>адаптируй под свою нишу</b>, а не копируй',
    'Серийность: «часть 1 из 3» — зритель ждёт продолжения',
    'Свой повторяемый формат = стабильные охваты'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="12" y="14" width="26" height="42" rx="5"/><rect x="47" y="14" width="26" height="42" rx="5" fill="currentColor" fill-opacity=".15"/><rect x="82" y="14" width="26" height="42" rx="5"/><path d="M20 26v18M55 26v18M90 26v18" opacity=".5"/><path d="M57 35l6-5v10z" fill="currentColor" stroke="none"/></svg>'},
  {t:'Чек-лист сценария', pts:[
    'Хук ≤ 2 секунд и одна ясная мысль',
    'Петли удержания и смена планов',
    'Эмоция и узнавание для репоста',
    'CTA-вопрос + караоке-субтитры'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="6" width="68" height="58" rx="8"/><path d="M36 20l4 4 7-8M36 36l4 4 7-8M36 52l4 4 7-8" stroke-width="2.8"/><path d="M54 21h28M54 37h28M54 53h20"/></svg>'},
  ],
  quiz:[
  {q:'С чего начинается вирусный ролик по уроку?', o:['Со съёмки красивых кадров','Со структуры: хук → удержание → развязка → CTA','С подбора музыки','С покупки рекламы'], a:1},
  {q:'Задача хука в первые 2 секунды —', o:['Показать логотип','Дать обещание выгоды или интригу','Перечислить регалии автора','Поставить длинную заставку'], a:1},
  {q:'Что удерживает зрителя до конца?', o:['Длинные паузы','Микро-обещания и смена планов каждые 2–3 с','Один статичный кадр','Отсутствие субтитров'], a:1},
  {q:'Фраза «сохрани, чтобы не потерять» работает на…', o:['Лайки','Сохранения','Отписки','Рекламу'], a:1},
  {q:'Сколько призывов (CTA) стоит давать в одном ролике?', o:['Как можно больше','Один чёткий','Ни одного','Ровно пять'], a:1},
  {q:'Правильная работа с трендом —', o:['Скопировать один в один','Адаптировать под свою нишу','Игнорировать тренды','Ждать, пока тренд умрёт'], a:1},
  ],
  pairs:[
  ['Хук','Первые 2 секунды: выгода или интрига'],
  ['Петля удержания','«Дальше покажу, как…»'],
  ['Триггер репоста','«Это же про меня»'],
  ['CTA','Один вопрос в конце ролика'],
  ['Серийность','«Часть 1 из 3»'],
  ],
  task:{
    intro:'Собери каркас вирусного ролика для своей ниши по структуре урока:',
    chips:['Хук','Удержание','Развязка','CTA'],
    ph:'Пример: Хук — «Три ошибки убивают ваши Reels» (текст + голос, 2 с). Удержание — разбираю каждую ошибку с примером, смена плана каждые 2 с. Развязка — как сделать правильно. CTA — «Какая ошибка была у тебя? Пиши в комментах».',
    verdict:'Структура собрана верно: сильный хук, петли удержания и один чёткий CTA. Совет куратора: вынеси хук-текст на экран крупно — так он сработает даже без звука.'
  }
},

/* ---------- УРОК: Монтаж Reels, который держит ---------- */
{
  title:'Монтаж Reels, который держит',
  sub:'ритм · караоке-субтитры · звук и цвет',
  dur:'', videoUrl:'',
  c1:'МОНТАЖ', c2:'REELS',
  slides:[
  {t:'Ритм решает всё', pts:[
    'Режь <b>по музыке и речи</b> — склейки на битах и словах',
    'Удаляй «воздух»: вдохи, паузы, запинки',
    'Средний план держи <b>1,5–2,5 секунды</b>',
    'Плотный ритм = высокий досмотр'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round"><path d="M10 35h8M22 22v26M30 30v10M38 16v38M46 26v18M54 20v30M62 32v6M70 18v34M78 28v14M86 22v26M94 30v10M102 24v22M110 35h0"/></svg>'},
  {t:'Джамп-каты и динамика', pts:[
    'Склейка на слове убирает лишнее и ускоряет темп',
    '<b>Zoom-панч</b> на акценте добавляет энергии',
    'Никаких «мёртвых» кадров, где ничего не происходит',
    'Движение в кадре — постоянно, под темп'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="14" y="20" width="40" height="30" rx="5"/><rect x="66" y="14" width="40" height="42" rx="5"/><path d="M78 26l16 18M94 26L78 44" opacity=".7"/><path d="M56 35h10M62 31l5 4-5 4"/></svg>'},
  {t:'Караоке-субтитры', pts:[
    '<b>90% смотрят без звука</b> — субтитры обязательны',
    'Крупно, с контрастом, читаемо на любом фоне',
    'Подсветка <b>по слову</b> в бренд-цвете держит взгляд',
    'Не перекрывай интерфейс площадки снизу'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="44" y="6" width="32" height="58" rx="8"/><rect x="50" y="40" width="12" height="7" rx="2" fill="currentColor" stroke="none"/><path d="M64 43h6" opacity=".7"/><path d="M50 50h20" opacity=".6"/><path d="M22 30c4 6 4 12 0 18M104 30c-4 6-4 12 0 18" opacity=".5"/></svg>'},
  {t:'Звук: музыка и SFX', pts:[
    'Музыка — <b>под смысл</b> сцены, а не просто фоном',
    'SFX под каждое движение — свуши, клики, удары',
    '<b>Дакинг</b>: приглушай музыку под голос',
    'Звук-хук в первую секунду цепляет ухо'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M14 28v14M22 22v26M30 32v6M38 18v34M46 26v18" stroke-width="2.8"/><path d="M70 46V18l22-6v28" stroke-linejoin="round"/><circle cx="64" cy="46" r="6"/><circle cx="86" cy="40" r="6"/></svg>'},
  {t:'Цвет и единый стиль', pts:[
    'Единый <b>пресет/LUT</b> на весь ролик — картинка «дорогая»',
    'Держи бренд-палитру: акцентный цвет узнаётся',
    'Контраст текста и фона — читаемость прежде всего',
    'Одинаковый стиль в серии = сильный бренд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="40" cy="35" r="22"/><path d="M40 13a22 22 0 0 1 0 44z" fill="currentColor" stroke="none" opacity=".85"/><circle cx="86" cy="24" r="6" fill="currentColor" stroke="none" opacity=".9"/><circle cx="100" cy="38" r="6"/><circle cx="86" cy="50" r="6" fill="currentColor" stroke="none" opacity=".5"/></svg>'},
  {t:'B-roll и перебивки', pts:[
    'Иллюстрируй слова кадрами — «показывай, не рассказывай»',
    'Сток <b>4K</b> — уникальный, не заезженный',
    'Перебивка прячет склейку и держит темп',
    'Не повторяй один и тот же кадр в ролике'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="10" y="16" width="46" height="34" rx="5"/><circle cx="24" cy="28" r="5"/><path d="M14 46l14-12 10 8 8-7 10 9" opacity=".7"/><rect x="66" y="24" width="44" height="30" rx="5" fill="currentColor" fill-opacity=".14"/><path d="M82 32v14l12-7z" fill="currentColor" stroke="none"/></svg>'},
  {t:'Темп, длина, формат', pts:[
    'Держи плотность до конца — обрезай хвост',
    'Формат <b>1080×1920</b>, вертикаль 9:16',
    'Учитывай safe-зоны под интерфейс площадки',
    'Финал не тяни: закончил мысль — руби'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="46" y="6" width="28" height="58" rx="7"/><path d="M46 16h28M46 54h28" opacity=".45" stroke-dasharray="3 4"/><path d="M55 30v10l10-5z" fill="currentColor" stroke="none"/><path d="M20 24v22M100 24v22" opacity=".5"/></svg>'},
  {t:'Экспорт и превью', pts:[
    'Высокий битрейт — без «мыла» и артефактов',
    '<b>Первый кадр</b> = мини-хук: он же обложка',
    'Обложка с крупным текстом ловит на пролистывании',
    'Проверь ролик <b>на телефоне</b> — смотреть будут там'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="12" y="14" width="44" height="34" rx="5"/><path d="M20 42l12-10 8 7 6-6 10 9" opacity=".7"/><path d="M34 54v6M24 60h20" opacity=".6"/><path d="M70 31h30M86 20l14 11-14 11" /></svg>'},
  ],
  quiz:[
  {q:'По чему в первую очередь режут вирусный ролик?', o:['По таймеру каждые 10 с','По музыке и речи — на битах и словах','Наугад','По длине исходников'], a:1},
  {q:'Караоке-субтитры обязательны, потому что…', o:['Так требует закон','Большинство смотрит без звука','Иначе видео не загрузится','Это дешевле музыки'], a:1},
  {q:'«Дакинг» звука — это…', o:['Замена музыки на тишину','Приглушение музыки под голос','Ускорение трека','Добавление эха'], a:1},
  {q:'Зачем единый пресет/LUT на весь ролик?', o:['Чтобы файл был меньше','Чтобы картинка выглядела дорого и в едином стиле','Чтобы убрать субтитры','Чтобы ускорить экспорт'], a:1},
  {q:'Первый кадр ролика важен, потому что он…', o:['Не виден зрителю','Служит обложкой и мини-хуком','Задаёт длину видео','Отвечает за звук'], a:1},
  {q:'Где обязательно проверить готовый Reels?', o:['Только на большом мониторе','На телефоне — там его и смотрят','В распечатке','Нигде, и так сойдёт'], a:1},
  ],
  pairs:[
  ['Джамп-кат','Склейка на слове, ускоряет темп'],
  ['Караоке-субтитры','Подсветка по слову без звука'],
  ['Дакинг','Музыка тише под голос'],
  ['LUT/пресет','Единый «дорогой» цвет'],
  ['B-roll','Перебивка прячет склейку'],
  ],
  task:{
    intro:'Опиши монтажный план своего Reels по приёмам урока:',
    chips:['Ритм','Джамп-каты','Субтитры','Звук','Цвет'],
    ph:'Пример: Ритм — режу по биту трека, планы по 2 с. Джамп-каты на ключевых словах + zoom-панч на выводе. Субтитры караоке, лаймовый акцент. Звук — трек под энергию + свуш на переходах, дакинг под голос. Цвет — единый пресет, бренд-палитра.',
    verdict:'Монтажный план плотный: ритм по звуку, караоке-сабы и единый цвет. Совет куратора: заложи звук-хук в первую секунду — ухо цепляется раньше, чем глаз.'
  }
},

/* ---------- УРОК: Дистрибуция и алгоритмы 2026 ---------- */
{
  title:'Дистрибуция и алгоритмы 2026',
  sub:'удержание · A/B хуков · масштаб',
  dur:'', videoUrl:'',
  c1:'ДИСТРИБУЦИЯ', c2:'АЛГОРИТМЫ',
  slides:[
  {t:'Как думает алгоритм', pts:[
    'Главное — <b>удержание и досмотры</b>, а не лайки',
    'Репосты и сохранения весят больше лайков',
    'Первые часы после публикации решают судьбу ролика',
    'Алгоритм тестирует видео на малой аудитории'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="12"/><path d="M55 35a5 5 0 0 1 10 0" opacity=".7"/><path d="M60 23V10M60 47v13M48 32 24 22M72 32l24-10M48 38 24 48M72 38l24 10"/><circle cx="20" cy="20" r="4"/><circle cx="100" cy="20" r="4"/><circle cx="20" cy="50" r="4"/><circle cx="100" cy="50" r="4"/></svg>'},
  {t:'Время и частота', pts:[
    '<b>Стабильность</b> важнее редких всплесков',
    'Публикуй регулярно — алгоритм любит ритм',
    'Тестируй окна времени под свою аудиторию',
    'Серия форматов приучает зрителя возвращаться'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="35" r="20"/><path d="M34 22v13l9 6"/><path d="M70 20h34M70 32h28M70 44h34" opacity=".7"/><circle cx="64" cy="20" r="1.8" fill="currentColor" stroke="none"/><circle cx="64" cy="32" r="1.8" fill="currentColor" stroke="none"/><circle cx="64" cy="44" r="1.8" fill="currentColor" stroke="none"/></svg>'},
  {t:'Метрики, что смотреть', pts:[
    '<b>Удержание %</b> и среднее время просмотра — главное',
    'Дочитывания до CTA показывают силу концовки',
    'Репосты и сохранения — сигнал ценности',
    'Читай воронку ролика: где зритель уходит'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 8v52h96"/><rect x="24" y="20" width="12" height="40" rx="3" fill="currentColor" fill-opacity=".18"/><rect x="46" y="30" width="12" height="30" rx="3"/><rect x="68" y="26" width="12" height="34" rx="3"/><rect x="90" y="40" width="12" height="20" rx="3"/><path d="M24 18l22 6 22-2 22 12" opacity=".55" stroke-dasharray="3 4"/></svg>'},
  {t:'A/B хуков и обложек', pts:[
    'Готовь <b>3 варианта хука</b> на один ролик',
    'Меняй за раз только одно — иначе не понять причину',
    'Тестируй обложки: первый кадр решает клик',
    'Решай данными, а не «мне так больше нравится»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="14" y="12" width="34" height="22" rx="5"/><rect x="14" y="38" width="34" height="22" rx="5" fill="currentColor" fill-opacity=".15"/><path d="M52 23h14M62 19l5 4-5 4M52 49h14M62 45l5 4-5 4" opacity=".7"/><rect x="72" y="24" width="34" height="22" rx="5"/><path d="M80 42l4 4 6-8" stroke-width="2.8"/></svg>'},
  {t:'Мультиплатформа', pts:[
    'Один сценарий → <b>Reels, Shorts, TikTok</b>',
    'Адаптируй под площадку, а не тупо копируй',
    'Убирай чужие водяные знаки — их душат алгоритмы',
    'Каждая площадка = дополнительный охват бесплатно'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="10" y="16" width="24" height="38" rx="5"/><rect x="48" y="16" width="24" height="38" rx="5" fill="currentColor" fill-opacity=".15"/><rect x="86" y="16" width="24" height="38" rx="5"/><path d="M18 30l6-4v8zM56 30l6-4v8zM94 30l6-4v8z" fill="currentColor" stroke="none"/></svg>'},
  {t:'Работа с комментариями', pts:[
    'Закрепляй лучший комментарий — продолжает воронку',
    'Отвечай <b>ролик-ответом</b> на частые вопросы',
    'Вовлечение в комментах поднимает охваты',
    'Комьюнити возвращается за новыми видео'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M16 16h54a8 8 0 0 1 8 8v16a8 8 0 0 1-8 8H40l-14 10V48h-2a8 8 0 0 1-8-8V16z"/><path d="M28 28h30M28 38h20" opacity=".7"/><circle cx="96" cy="30" r="10"/><path d="M92 30l3 3 6-7" opacity=".8"/></svg>'},
  {t:'Масштаб: контент-завод', pts:[
    'Снимай <b>пачками</b> — один съёмочный день на неделю',
    'Копи шаблоны приёмов, но <b>без повторов</b> в ленте',
    'Аналитика ежедневно — усиливай залетевшее',
    'Система важнее вдохновения: конвейер стабильнее'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="10" y="14" width="20" height="16" rx="3"/><rect x="10" y="40" width="20" height="16" rx="3"/><rect x="50" y="27" width="20" height="16" rx="3" fill="currentColor" fill-opacity=".16"/><rect x="90" y="14" width="20" height="16" rx="3"/><rect x="90" y="40" width="20" height="16" rx="3"/><path d="M30 22h10l10 9M30 48h10l10-9M70 35h20" opacity=".7"/></svg>'},
  {t:'Чек-лист роста', pts:[
    'Тестируй 3 хука на каждый ролик',
    'Держи удержание выше 50%',
    'CTA-вопрос + триггер репоста',
    'Каждый день — разбор метрик и выводы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M16 54l22-18 16 10 30-30"/><path d="M74 16h16v16" /><circle cx="16" cy="54" r="3" fill="currentColor" stroke="none"/><circle cx="38" cy="36" r="3" fill="currentColor" stroke="none"/><circle cx="54" cy="46" r="3" fill="currentColor" stroke="none"/></svg>'},
  ],
  quiz:[
  {q:'Что для алгоритма важнее всего?', o:['Число лайков','Удержание и досмотры','Длина описания','Количество хэштегов'], a:1},
  {q:'Какие сигналы весят больше лайков?', o:['Просмотры профиля','Репосты и сохранения','Время публикации','Число подписок автора'], a:1},
  {q:'Как правильно проводить A/B-тест хука?', o:['Менять всё сразу','Менять за раз только одно','Не тестировать вовсе','Смотреть только на лайки'], a:1},
  {q:'Что даёт публикация на нескольких площадках?', o:['Штраф от алгоритма','Дополнительный охват бесплатно','Потерю качества','Ничего'], a:1},
  {q:'Зачем закреплять комментарий?', o:['Для красоты','Продолжить воронку и вовлечение','Скрыть критику','Ускорить загрузку'], a:1},
  {q:'Что важнее для стабильного роста?', o:['Ждать вдохновения','Система и ежедневная аналитика','Один вирусный ролик','Больше хэштегов'], a:1},
  ],
  pairs:[
  ['Удержание %','Главная метрика алгоритма'],
  ['Сохранения','Сигнал ценности контента'],
  ['A/B хука','3 варианта, меняем одно'],
  ['Мультиплатформа','Один сценарий на 3 площадки'],
  ['Контент-завод','Съёмка пачками + аналитика'],
  ],
  task:{
    intro:'Составь план дистрибуции и роста для своего аккаунта:',
    chips:['Частота','Метрика','A/B','Площадки','Разбор'],
    ph:'Пример: Частота — 1 ролик в день, окно 19:00. Метрика — удержание, цель >55%. A/B — 3 хука на ролик, меняю только первый кадр. Площадки — Reels + Shorts + TikTok. Разбор — каждый вечер смотрю воронку и усиливаю залетевший формат.',
    verdict:'План рабочий: стабильная частота, ясная метрика и ежедневный разбор. Совет куратора: заведи таблицу форматов — так быстро увидишь, какой приём стабильно даёт удержание.'
  }
},
];
/* ===== реальные уроки направлений (авторы OKO по методичке) ===== */
/* ============================================================
   ТРЕК 1. МЕДИЙНОСТЬ — уроки 1.1–1.3
   Формат данных Академии OKO (совместим с AC_COURSE).
   ============================================================ */
const MEDIA_LESSONS = [

/* ---------- УРОК 1: Мышление и старт ---------- */
{
  title:'Мышление и старт',
  sub:'1:27 · видео (голос Даниэля) + слайды + тест + игра',
  dur:'1:27',
  videoUrl:'https://okoteam.top/media/oko_media_l1.mp4',
  c1:'МЫШЛЕНИЕ', c2:'И СТАРТ',
  slides:[
  {t:'Курс для трёх типов', pts:[
    'Материал заходит трём аудиториям сразу: <b>новичкам</b>, <b>практикам</b> и <b>бизнесу</b>',
    'Новичку — с нуля собрать блог и первые деньги',
    'Практику — навести порядок в упаковке и контенте',
    'Бизнесу — превратить аккаунт в канал продаж, а не в витрину-заглушку'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="30" cy="24" r="8"/><path d="M17 52c1-11 6-16 13-16s12 5 13 16"/><circle cx="60" cy="20" r="9"/><path d="M45 54c1-13 7-19 15-19s14 6 15 19"/><circle cx="92" cy="24" r="8"/><path d="M79 52c1-11 6-16 13-16s12 5 13 16"/></svg>'},
  {t:'Кейс на себе — сразу', pts:[
    'Не жди конца курса — <b>веди свой аккаунт как полигон</b> с первого дня',
    'Каждый приём проверяешь на своём блоге, а не в теории',
    'Собственный результат = твой первый и главный кейс для продаж',
    'Учиться и делать — одновременно, иначе знания выветрятся'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="18" y="10" width="40" height="50" rx="7"/><path d="M26 22h24M26 31h18M26 40h22M26 49h14" opacity=".7"/><path d="M70 35h26" opacity=".8"/><path d="M88 27l10 8-10 8" opacity=".8"/><circle cx="106" cy="35" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Стадии обучения — это норма', pts:[
    'Энтузиазм → «дайте <b>новое</b>» → «все виноваты» → усталость — проходят все',
    'Спад мотивации не значит, что ты не тянешь — это этап, а не приговор',
    'Плато — момент, где большинство бросает, а ты просто продолжаешь',
    'Знаешь стадии наперёд — не пугаешься их, когда накрывает'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20l22 22 20-14 22 26 22-38"/><circle cx="34" cy="42" r="3" fill="currentColor" stroke="none"/><circle cx="54" cy="28" r="3" fill="currentColor" stroke="none"/><circle cx="76" cy="54" r="3" fill="currentColor" stroke="none"/><path d="M12 60h96" opacity=".4"/></svg>'},
  {t:'Просто начать и снимать', pts:[
    '<b>Кринж-этап проходят все</b> — первые видео всегда кажутся неловкими',
    'Идеальный старт не существует: качество приходит с количеством',
    'Первые 20–30 роликов — это тренировка, а не витрина',
    'Опубликованное «сойдёт» побеждает идеальное «в черновиках»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="16" width="54" height="38" rx="7"/><path d="M74 28l24-12v38L74 42z"/><circle cx="38" cy="35" r="7"/><path d="M38 35l4 2" opacity=".7"/></svg>'},
  {t:'Мышление полигона', pts:[
    'Блог — <b>лаборатория</b>: гипотеза → пост → цифры → вывод',
    'Провалившийся формат — не поражение, а данные',
    'Смотри на реакцию аудитории, а не на «нравится мне самому»',
    'Регулярность важнее вдохновения: график бьёт настроение'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M50 8h20M56 8v14M64 8v14"/><path d="M56 22 34 52a8 8 0 0 0 7 12h38a8 8 0 0 0 7-12L64 22"/><path d="M46 44h28" opacity=".7"/><circle cx="55" cy="53" r="2.5" fill="currentColor" stroke="none"/><circle cx="68" cy="55" r="2" fill="currentColor" stroke="none"/></svg>'},
  {t:'Цель в цифрах, а не в мечтах', pts:[
    '«Хочу больше денег» — не цель. <b>Цель — это число и срок</b>',
    'Определи целевые цифры: подписчики, охваты, заявки, доход',
    'Пропиши, «как выглядит жизнь при цели» — образ тянет к действию',
    'Разбей большую цель на шаги с датами — так она перестаёт пугать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="45" cy="35" r="26"/><circle cx="45" cy="35" r="16"/><circle cx="45" cy="35" r="6"/><path d="M45 35l40-22" /><path d="M78 9l10 4-2 10z" fill="currentColor" stroke="none"/></svg>'},
  {t:'Домашка задаёт вектор', pts:[
    'ДЗ модуля: <b>план действий на 10 пунктов</b>',
    'Каждый пункт — с конкретным сроком и измеримой цифрой',
    'Финальный пункт — описание «жизни при достигнутой цели»',
    'Этот план — навигатор на весь курс, к нему возвращаешься'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="8" width="60" height="54" rx="8"/><path d="M40 20l4 4 6-7M40 34l4 4 6-7M40 48l4 4 6-7" stroke-width="2.8"/><path d="M58 21h22M58 35h22M58 49h16" opacity=".8"/></svg>'},
  {t:'Чек-лист старта', pts:[
    'Понял свой тип: новичок / практик / бизнес',
    'Завёл аккаунт-полигон и снимаешь, не боясь кринжа',
    'Знаешь стадии обучения — не сольёшься на спаде',
    'Цель оцифрована: число + срок + образ жизни',
    'План на 10 пунктов написан'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="6" width="68" height="58" rx="8"/><path d="M36 20l4 4 7-8M36 36l4 4 7-8M36 52l4 4 7-8" stroke-width="2.8"/><path d="M54 21h28M54 37h28M54 53h20" opacity=".8"/></svg>'},
  ],
  quiz:[
  {q:'На какие три типа участников рассчитан курс «Медийность»?', o:['Дети, взрослые, пенсионеры','Новички, практики, бизнес','Блогеры, продавцы, инвесторы','Дизайнеры, авторы, операторы'], a:1},
  {q:'Когда начинать вести свой аккаунт по методу «кейс на себе»?', o:['После получения сертификата','Когда наберётся 1000 подписчиков','Сразу, с первого дня — как полигон','Только когда будет идеальный контент'], a:2},
  {q:'Что урок говорит про кринж-этап первых роликов?', o:['Его надо избегать любой ценой','Он бывает только у новичков','Его проходят все — просто снимай','Он означает, что съёмки не твоё'], a:2},
  {q:'Эмоциональные стадии обучения (энтузиазм → усталость) — это…', o:['Признак, что курс плохой','Норма, через это проходят все','Повод бросить обучение','Ошибка в планировании'], a:1},
  {q:'Какой должна быть правильно поставленная цель?', o:['Максимально амбициозной и абстрактной','Число и срок, а не просто «хочу больше»','Тайной, чтобы не сглазить','Такой же, как у конкурентов'], a:1},
  {q:'Из скольких пунктов состоит стартовое ДЗ-план действий?', o:['3','5','10','20'], a:2},
  ],
  pairs:[
  ['Новичок','Собрать блог и первые деньги с нуля'],
  ['Кейс на себе','Свой аккаунт как полигон с первого дня'],
  ['Кринж-этап','Проходят все — просто снимай дальше'],
  ['Цель','Число и срок, а не абстрактное «хочу»'],
  ['Стадии обучения','Энтузиазм, спад, усталость — это норма'],
  ],
  task:{
    intro:'Напиши свой стартовый план на 10 пунктов — навигатор на весь курс. Оцифруй цель и опиши, как выглядит твоя жизнь, когда цель достигнута:',
    chips:['Мой тип','Целевые цифры','Срок','Жизнь при цели','Первый шаг сегодня'],
    ph:'Пример: Тип — практик. Цель — 300 заявок и 150 000 ₽/мес за 3 месяца. Полигон — веду личный блог, 1 Reels в день. Жизнь при цели — работаю на себя, выбираю клиентов. Первый шаг сегодня — сниму и выложу первый ролик, не переснимая.',
    verdict:'Цель оцифрована и привязана к сроку — это уже сильнее «хочу больше подписчиков». Совет куратора: повесь этот план на видное место и раз в неделю сверяйся с цифрами, а не с настроением. Полигон запускай сегодня — первый кринжовый ролик приближает к цели больше, чем идеальный в черновиках.'
  }
},

/* ---------- УРОК 2: Упаковка профиля ---------- */
{
  title:'Упаковка профиля',
  sub:'1:24 · видео (голос Даниэля) + слайды + тест + игра',
  dur:'1:24',
  videoUrl:'https://okoteam.top/media/oko_media_l2.mp4',
  c1:'УПАКОВКА', c2:'ПРОФИЛЯ',
  slides:[
  {t:'3 секунды на впечатление', pts:[
    'Шапка профиля решает за <b>3 секунды</b>: остаться или уйти',
    'Цель шапки зависит от типа блога: <b>коммерческий</b>, <b>экспертный</b>, <b>лайфстайл</b>',
    'Коммерческий — продать; экспертный — вызвать доверие; лайфстайл — влюбить в личность',
    'Сначала определи тип и цель, потом пиши текст — не наоборот'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="22"/><path d="M60 22v13l9 6"/><path d="M60 13V8M60 62v-5M38 35h-5M87 35h-5"/></svg>'},
  {t:'Элементы шапки', pts:[
    '<b>Ник</b> — как тебя ищут; <b>Имя</b> — жирная строка до 30 знаков',
    '<b>Описание</b> — до 150 знаков, работает каждое слово',
    '<b>Ссылка</b> — куда ведёшь тёплый трафик; вечные <b>истории</b> — витрина',
    'Пять элементов, и все пять должны работать на одну цель'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="30" cy="24" r="12"/><path d="M18 24a12 12 0 0 1 24 0"/><path d="M50 16h56M50 26h40M50 36h50" opacity=".85"/><circle cx="26" cy="56" r="7"/><circle cx="46" cy="56" r="7"/><circle cx="66" cy="56" r="7"/><circle cx="86" cy="56" r="7"/></svg>'},
  {t:'Ник, который запоминают', pts:[
    'Строй ник из <b>имени или тематики</b> — по нему тебя находят',
    'Должен легко читаться, произноситься и запоминаться',
    'Без «мусора»: цифр-заборов, подчёркиваний, случайных символов',
    'Хороший ник = меньше друзей потеряет тебя в поиске'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M34 30v10a10 10 0 1 0 4-8" opacity=".9"/><circle cx="30" cy="35" r="12"/><path d="M50 45h44a10 10 0 0 0 0-20H64" opacity=".85"/><path d="M50 45l-8-4M50 45l-6 6" opacity=".6"/></svg>'},
  {t:'Аватар: лицо блога', pts:[
    'Качественное фото, <b>однотонный фон</b>, крупный план',
    '<b>Открытый взгляд</b> в камеру — контакт с человеком по ту сторону',
    'Меняй аватар <b>не чаще 1–2 раз в год</b> — тебя узнают по нему',
    'Аватар — это не логотип для галочки, это первое рукопожатие'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="36" y="10" width="48" height="50" rx="12"/><circle cx="60" cy="30" r="9"/><path d="M44 56c2-11 8-15 16-15s14 4 16 15"/><path d="M52 30h.5M68 30h.5" stroke-width="3"/></svg>'},
  {t:'Описание — по формуле', pts:[
    'Экспертный: <b>кто ты → чем интересен → факты-цифры → выгода → CTA</b>',
    'Коммерческий: <b>продукт → выгода → уникальность → CTA</b>',
    'Каждая строка отвечает на вопрос читателя «а мне зачем?»',
    'Заканчивай призывом: подпишись, напиши, забери — дай следующий шаг'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M22 14h60M22 26h50M22 38h60M22 50h34"/><path d="M64 50h18a8 8 0 0 1 8 8v0" opacity=".7"/><path d="M84 52l8 6-8 6" opacity=".7"/></svg>'},
  {t:'Чего избегать в описании', pts:[
    'Пустые <b>шаблоны</b> без смысла: «делаю мир лучше», «люблю жизнь»',
    'Английский для русскоязычной аудитории — теряешь понятность',
    '<b>Некликабельные</b> контакты: телефон текстом, «пишите в директ» без ссылки',
    'Вода вместо фактов: цифры и конкретика продают, лозунги — нет'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="26"/><path d="M42 17 78 53" stroke-width="3"/><path d="M32 40h12M76 30h12" opacity=".5"/></svg>'},
  {t:'Мультиссылка и вечные истории', pts:[
    '<b>Мультиссылка</b> собирает все ссылки в одну — не теряешь трафик',
    'Вечные истории — витрина блога: <b>О себе / Услуги-Прайс / Отзывы-кейсы</b>',
    'Дополни: <b>Примеры / Как заказать / Акции / Достижения</b>',
    'Обложки историй — в едином стиле бренда, а не хаос иконок'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="20" cy="18" r="9"/><circle cx="44" cy="18" r="9"/><circle cx="68" cy="18" r="9"/><circle cx="92" cy="18" r="9"/><path d="M60 40h40a6 6 0 0 1 6 6v0a6 6 0 0 1-6 6H60a6 6 0 0 1-6-6v0a6 6 0 0 1 6-6z"/><path d="M14 46h30" opacity=".7"/></svg>'},
  {t:'Чек-лист упаковки', pts:[
    'Тип блога и цель шапки определены',
    'Ник читаемый, имя-строка до 30, описание до 150',
    'Аватар: однотонный фон, крупный план, открытый взгляд',
    'Описание собрано по формуле и заканчивается CTA',
    'Мультиссылка стоит, вечные истории закрывают путь клиента'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="6" width="68" height="58" rx="8"/><path d="M36 20l4 4 7-8M36 36l4 4 7-8M36 52l4 4 7-8" stroke-width="2.8"/><path d="M54 21h28M54 37h28M54 53h20" opacity=".8"/></svg>'},
  ],
  quiz:[
  {q:'Сколько секунд есть у шапки профиля на первое впечатление?', o:['30 секунд','10 секунд','3 секунды','1 минута'], a:2},
  {q:'От чего зависит цель шапки профиля?', o:['От количества подписчиков','От типа блога: коммерческий/экспертный/лайфстайл','От времени суток','От числа сторис в день'], a:1},
  {q:'Какое ограничение по длине у строки с именем в шапке?', o:['До 30 знаков','До 150 знаков','До 500 знаков','Без ограничений'], a:0},
  {q:'Каким должен быть аватар по уроку?', o:['С ярким разноцветным фоном','Однотонный фон, крупный план, открытый взгляд','Обязательно логотип компании','Групповое фото команды'], a:1},
  {q:'Как выглядит формула описания для экспертного блога?', o:['Продукт → цена → скидка','Кто → чем интересен → факты-цифры → выгода → CTA','Хэштеги → эмодзи → ссылка','Приветствие → благодарность → подпись'], a:1},
  {q:'Что из перечисленного — ошибка в шапке профиля?', o:['Мультиссылка на все ресурсы','Кликабельные контакты','Английский текст для русской аудитории','Формула в описании'], a:2},
  ],
  pairs:[
  ['Шапка профиля','3 секунды на первое впечатление'],
  ['Имя-строка','Жирный текст до 30 знаков'],
  ['Описание','До 150 знаков, по формуле, с CTA'],
  ['Аватар','Однотонный фон, крупный план, открытый взгляд'],
  ['Мультиссылка','Все ссылки в одной — не теряешь трафик'],
  ],
  task:{
    intro:'Пересобери шапку своего профиля по формуле урока. Сначала определи тип блога, затем распиши каждый элемент — ник, имя, описание и структуру вечных историй:',
    chips:['Тип блога','Ник','Имя-строка (до 30)','Описание (до 150)','Вечные истории','CTA'],
    ph:'Пример: Тип — экспертный. Ник — anna.smm. Имя — Анна · SMM-специалист. Описание: веду блоги с 2020, +200 клиентов, средний рост охватов ×4. Разбираю упаковку бесплатно — пиши «шапка». Истории: О себе / Кейсы / Прайс / Как заказать.',
    verdict:'Формула соблюдена: есть кто ты, факты-цифры, выгода и понятный призыв. Совет куратора: проверь описание на «воду» — выкинь строки, которые подошли бы любому блогеру. Обложки вечных историй приведи к единому стилю бренда, чтобы шапка читалась как одно целое за те самые 3 секунды.'
  }
},

/* ---------- УРОК 3: Визуал и лента ---------- */
{
  title:'Визуал и лента',
  sub:'1:11 · видео (голос Даниэля) + слайды + тест + игра',
  dur:'1:11',
  videoUrl:'https://okoteam.top/media/oko_media_l3.mp4',
  c1:'ВИЗУАЛ', c2:'И ЛЕНТА',
  slides:[
  {t:'Зачем вообще визуал', pts:[
    '<b>51% людей судят о блоге по визуалу</b> — картинка говорит раньше слов',
    'Визуал держит внимание, транслирует ценности и отстраивает от других',
    'Единый стиль = доверие: «этот аккаунт ведут всерьёз»',
    '<b>Баннерная слепота</b> — враг: однотипные шаблоны глаз пролистывает не глядя'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M10 35C22 18 44 12 60 12s38 6 50 23c-12 17-34 23-50 23S22 52 10 35z"/><circle cx="60" cy="35" r="11"/><circle cx="60" cy="35" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Этапы: от смыслов к съёмке', pts:[
    'Шаг 1 — <b>сбор смыслов</b>: о чём блог, какие ценности показываешь',
    'Шаг 2 — <b>мудборд</b>: собираешь референсы, гамму, настроение',
    'Шаг 3 — <b>планирование</b>: 12–15 фото вперёд, единая гамма, ≤2 шрифта',
    'Шаг 4 — <b>съёмка → обработка → публикация</b> по плану, а не по настроению'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M14 56h20V42h20V28h20V14h24"/><circle cx="24" cy="50" r="3" fill="currentColor" stroke="none"/><circle cx="44" cy="36" r="3" fill="currentColor" stroke="none"/><circle cx="64" cy="22" r="3" fill="currentColor" stroke="none"/><rect x="92" y="8" width="16" height="12" rx="3" opacity=".8"/></svg>'},
  {t:'Мудборд — карта настроения', pts:[
    'Собери <b>референсы</b>: цвета, ракурсы, свет, объекты, шрифты',
    'Определи <b>гамму</b> — 2–3 базовых цвета, чтобы лента звучала в тон',
    'Мудборд экономит часы: снимаешь по образцу, а не наугад',
    'Он же — бриф для фотографа, дизайнера и для себя будущего'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="14" y="12" width="28" height="20" rx="4"/><rect x="48" y="12" width="24" height="20" rx="4"/><rect x="78" y="12" width="28" height="20" rx="4"/><rect x="14" y="40" width="24" height="18" rx="4"/><rect x="44" y="40" width="32" height="18" rx="4"/><rect x="82" y="40" width="24" height="18" rx="4"/></svg>'},
  {t:'Красивая лента: приёмы', pts:[
    'Чередуй <b>планы</b>: общий → средний → крупный → сверхкрупный',
    'Чередуй образы и сюжеты — рядом не должны стоять два похожих кадра',
    'Оставляй <b>«воздух»</b> — пустое пространство в кадре, не забивай всё',
    'Формат кадра <b>4:5</b> — забирает максимум площади в ленте'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="8" width="26" height="18" rx="3"/><rect x="50" y="8" width="26" height="18" rx="3"/><rect x="80" y="8" width="20" height="18" rx="3"/><rect x="20" y="30" width="20" height="18" rx="3"/><rect x="44" y="30" width="32" height="18" rx="3"/><rect x="80" y="30" width="20" height="18" rx="3"/><rect x="20" y="52" width="26" height="12" rx="3" opacity=".7"/><rect x="50" y="52" width="50" height="12" rx="3" opacity=".7"/></svg>'},
  {t:'Не больше двух шрифтов', pts:[
    'В визуале — <b>максимум 2 шрифта</b>: один акцентный, один для текста',
    'Больше шрифтов = каша и ощущение любительства',
    'Одна пара шрифтов на весь блог — это часть узнаваемости',
    'То же с цветом: держи гамму, а не «радугу на каждом посте»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 16h30M35 16v40" stroke-width="3"/><path d="M66 24h34M83 24v32" stroke-width="2.4" opacity=".85"/><path d="M60 56h44" opacity=".5"/></svg>'},
  {t:'Личность продаёт', pts:[
    '<b>96% людей охотнее лайкают фото, где есть человек</b> — покажи лицо',
    'Блог без личности — безликая витрина, к ней не привязываются',
    'Личные кадры, эмоции, закулисье — то, что отличает тебя от конкурентов',
    'Люди подписываются на людей, а не на идеальные предметные раскладки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="26" r="12"/><path d="M38 60c2-15 10-22 22-22s20 7 22 22"/><path d="M84 20l3 6 6 1-4.5 4 1 6-5.5-3-5.5 3 1-6-4.5-4 6-1z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Техника не главное', pts:[
    'Для качественного визуала хватает <b>iPhone 11–12 и новее</b>',
    'Дело не в дорогой камере, а в свете, кадре и обработке',
    'Следи за трендами, но <b>оставайся собой</b> — копия всегда слабее оригинала',
    'Тренд — это форма; твоя личность и смысл — содержание'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="40" y="8" width="40" height="54" rx="8"/><circle cx="60" cy="30" r="9"/><circle cx="60" cy="30" r="3.5"/><path d="M52 52h16" opacity=".7"/><path d="M96 22l2.5 5 5 .8-3.7 3.4.8 5-4.6-2.5-4.6 2.5.8-5-3.7-3.4 5-.8z" fill="currentColor" stroke="none" opacity=".8"/></svg>'},
  {t:'Чек-лист визуала', pts:[
    'Смыслы собраны, мудборд и гамма готовы',
    'Лента спланирована на 12–15 фото вперёд',
    'Чередуешь планы и образы, оставляешь «воздух»',
    'Не больше 2 шрифтов, кадр 4:5, есть личность в кадре',
    'Снимаешь на то, что есть, — техника не отговорка'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="6" width="68" height="58" rx="8"/><path d="M36 20l4 4 7-8M36 36l4 4 7-8M36 52l4 4 7-8" stroke-width="2.8"/><path d="M54 21h28M54 37h28M54 53h20" opacity=".8"/></svg>'},
  ],
  quiz:[
  {q:'Какой процент людей судит о блоге по визуалу?', o:['10%','25%','51%','90%'], a:2},
  {q:'Что такое «баннерная слепота» в контексте визуала?', o:['Плохое зрение у подписчиков','Глаз пролистывает однотипные шаблоны не глядя','Отсутствие баннеров в профиле','Слишком яркие цвета'], a:1},
  {q:'Сколько фото рекомендуется планировать вперёд на этапе планирования?', o:['3–4','12–15','50','Планировать не нужно'], a:1},
  {q:'Сколько шрифтов максимум стоит использовать в визуале?', o:['1','2','4','Без ограничений'], a:1},
  {q:'Какой формат кадра забирает максимум площади в ленте?', o:['1:1','16:9','4:5','3:1'], a:2},
  {q:'Почему в кадре важна личность (человек)?', o:['Так требуют алгоритмы','96% людей охотнее лайкают фото с человеком','Это дешевле предметной съёмки','Чтобы заполнить пустоту'], a:1},
  ],
  pairs:[
  ['51%','Судят о блоге по визуалу'],
  ['Мудборд','Референсы, гамма, настроение будущей ленты'],
  ['4:5','Формат кадра, забирающий максимум площади'],
  ['≤2 шрифта','Правило единого визуального стиля'],
  ['96%','Охотнее лайкают фото, где есть человек'],
  ],
  task:{
    intro:'Спланируй свою ленту как визуальную систему. Собери мудборд, задай гамму и шрифты и распиши сетку на ближайшие 12–15 кадров с чередованием планов и образов:',
    chips:['Гамма (2–3 цвета)','Шрифты (≤2)','Сетка 12–15 кадров','Чередование планов','Где личность в кадре'],
    ph:'Пример: Гамма — чёрный, лайм, светло-серый. Шрифты — Bebas Neue + Montserrat. Сетка: портрет крупный → предмет средний → закулисье общий → цитата → портрет… Личность — каждый 3-й кадр с лицом и эмоцией. Кадр 4:5, снимаю на iPhone.',
    verdict:'Система выстроена: гамма, пара шрифтов и чередование планов дают ленте узнаваемость. Совет куратора: разложи 12–15 кадров в раскладке заранее и проверь, чтобы рядом не стояли два похожих сюжета — именно это рождает «воздух» и убивает баннерную слепоту. И не жди идеальную камеру: свет и кадр важнее модели телефона.'
  }
}

];
/* ================= ТРЕК 2: МАРКЕТИНГ — 3 урока =================
   Источник: OKO_ACADEMY_CURRICULUM.md, модули 2.1–2.4
   (Копирайтинг · Структура продающего поста · Хуки/тизинг/сторителлинг · Триггеры).
   Формат один в один с AC_COURSE (academy/script.js). */

const MARKETING_LESSONS = [

/* ---------- УРОК 1: Копирайтинг ---------- */
{
  title:'Копирайтинг: тексты, которые покупают',
  sub:'4:10 · видео + слайды + тест + игра',
  dur:'4:10',
  c1:'ПИШИ КАК', c2:'ГОВОРИШЬ',
  slides:[
  {t:'Главный принцип', pts:[
    'Пиши <b>так, как разговариваешь</b> — текст это молчаливый аналог живой речи',
    'Никакого «пластика»: канцелярит и пафос убивают доверие',
    'Прочитал вслух и споткнулся — читатель споткнётся тоже',
    'Простой человеческий текст читают до конца, сложный закрывают'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 14h48a8 8 0 0 1 8 8v18a8 8 0 0 1-8 8H40l-14 12v-12h-6a8 8 0 0 1-8-8V22a8 8 0 0 1 8-8Z"/><path d="M28 26h32M28 34h22"/><circle cx="98" cy="40" r="14"/><path d="M92 40h12M98 34v12" opacity=".7"/></svg>'},
  {t:'Признак 1: глаголы решают', pts:[
    'Причастия и деепричастия меняй на <b>глаголы</b>: «сделавший» → «сделал»',
    'Глагол двигает мысль, причастие её тормозит',
    '«Являющийся экспертом» → «эксперт». Проще и сильнее',
    'Живая речь — это цепочка действий, а не гроздь окончаний'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M14 35h74"/><path d="M74 25l14 10-14 10"/><circle cx="102" cy="35" r="10"/><path d="M97 35l4 4 6-7"/><path d="M20 20l-4 30M32 22l-3 26" opacity=".4"/></svg>'},
  {t:'Признаки 2 и 3: режь и усиливай', pts:[
    'Выкинь <b>каждое 3–5-е слово</b> — текст почти всегда не теряет смысл',
    'Каждое 3–5-е оставшееся замени <b>выразительным синонимом</b>',
    '«Очень хороший результат» → «сильный результат»',
    'Меньше слов, ярче слова — плотность внимания растёт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 22h30M18 32h40M18 42h26M18 52h34" opacity=".85"/><path d="M56 16l14 14-30 30-14 0 0-14z"/><path d="M56 16l14 14" opacity=".5"/><path d="M92 30l4 8 8 1-6 6 1.5 8-7-4-7 4 1.5-8-6-6 8-1z" fill="currentColor" stroke="none" opacity=".9"/></svg>'},
  {t:'Признаки 4 и 5: начало текста', pts:[
    'Удали <b>вводный первый абзац</b> — почти всегда это разогрев ни о чём',
    'Первое предложение = <b>заголовок</b>, оно решает, читать дальше или нет',
    'Начинай сразу с сути, а не с «в современном мире…»',
    'Сильное начало вытаскивает даже слабую середину'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="10" width="80" height="14" rx="4" fill="currentColor" fill-opacity=".18"/><path d="M28 17h50"/><path d="M20 34h64M20 44h72M20 54h56" opacity=".7"/><path d="M104 30l-8 4 8 4" opacity=".6"/><path d="M100 17h10" opacity=".5"/></svg>'},
  {t:'Этапы: сначала пиши, потом правь', pts:[
    'Первый заход — <b>пиши потоком</b>, не редактируй по ходу',
    'Редактура — отдельный этап на свежую голову',
    'Финальная проверка — <b>перечитай вслух</b> и поправь всё, где споткнулся',
    'Смешаешь письмо и правку — застрянешь на первом абзаце'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 50l6-2 34-34-4-4-34 34z"/><path d="M52 12l4 4"/><path d="M70 40h34"/><path d="M70 50h26"/><circle cx="98" cy="20" r="9"/><path d="M94 20l3 3 6-6"/></svg>'},
  {t:'F- и Z-траектории чтения', pts:[
    'Глаз бежит по экрану буквой <b>F</b> (текст) или <b>Z</b> (визуал)',
    'Важное — в <b>начало строк и абзацев</b>, туда, куда падает взгляд',
    'Первые слова строки весят больше, чем середина',
    'Подгоняй текст под траекторию, а не заставляй читать всё подряд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="M22 12h30M22 12v46M22 34h24" stroke-width="3.2"/><path d="M70 14h30l-30 42h30" stroke-width="3.2" opacity=".85"/><circle cx="22" cy="12" r="3" fill="currentColor" stroke="none"/><circle cx="70" cy="14" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Чек-лист копирайтера', pts:[
    'Причастия → глаголы',
    'Выкинул лишнее, усилил синонимами',
    'Снёс вводный абзац, первое предложение — крючок',
    'Написал потоком → отредактировал → прочитал вслух',
    'Важное расставил по F/Z-траектории'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="6" width="68" height="58" rx="8"/><path d="M36 20l4 4 7-8M36 36l4 4 7-8M36 52l4 4 7-8" stroke-width="2.8"/><path d="M54 21h28M54 37h28M54 53h20"/></svg>'},
  ],
  quiz:[
  {q:'Главный принцип копирайтинга из урока:', o:['Писать максимально официально','Писать так, как разговариваешь','Использовать больше причастий','Начинать с длинного вступления'], a:1},
  {q:'Что делать с причастиями и деепричастиями?', o:['Добавлять больше','Менять на глаголы','Оставлять как есть','Выделять жирным'], a:1},
  {q:'По правилу «5 признаков» первый абзац-вступление нужно…', o:['Расширить','Удалить','Продублировать в конце','Перевести на английский'], a:1},
  {q:'Чем должно быть первое предложение текста?', o:['Дисклеймером','Приветствием','Заголовком-крючком','Списком источников'], a:2},
  {q:'Правильный порядок работы над текстом:', o:['Редактировать каждое слово по ходу','Сначала пиши потоком, потом редактируй','Сразу публиковать черновик','Писать только вслух'], a:1},
  {q:'Куда ставить самое важное по F/Z-траектории?', o:['В конец абзацев','В середину строк','В начало строк и абзацев','В подпись'], a:2},
  ],
  pairs:[
  ['Пиши как говоришь','Текст — молчаливый аналог живой речи'],
  ['Глаголы','Заменяют причастия и двигают мысль'],
  ['Первое предложение','Работает как заголовок-крючок'],
  ['Вслух','Финальная проверка текста на живость'],
  ['F/Z-траектория','Важное — в начало строк и абзацев'],
  ],
  task:{
    intro:'Возьми свой старый пост (3–5 предложений) и прогони через «5 признаков хорошего текста»:',
    chips:['Глаголы вместо причастий','Выкинь лишнее','Усиль синонимами','Снеси вводный абзац','Заголовок-крючок'],
    ph:'Пример: было — «Являясь опытным специалистом, я оказываю широкий спектр услуг…». Стало — «Веду блоги, которые продают. Пять лет, 40 клиентов, кейсы с ростом охватов в 3 раза».',
    verdict:'Причастия ушли, лишнее вырезано, первое предложение цепляет. Совет куратора: обязательно прочитай финал вслух — там всплывают последние шероховатости.'
  }
},

/* ---------- УРОК 2: Структура продающего поста ---------- */
{
  title:'Структура продающего поста',
  sub:'4:30 · видео + слайды + тест + игра',
  dur:'4:30',
  c1:'ПРОДАЮЩИЙ', c2:'ПОСТ',
  slides:[
  {t:'Универсальная структура', pts:[
    '<b>7 блоков подряд</b>: Заголовок → Лид → Оффер → Выгоды → Цена → Дедлайн → CTA',
    'Каждый блок ведёт читателя к следующему, как ступени',
    'Пропустил блок — воронка протекает и продажа рушится',
    'Работает для поста, сторис, лендинга и письма одинаково'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 12h84L84 32v20l-14 10V32z"/><path d="M40 22h40M48 32h24" opacity=".6"/><circle cx="60" cy="52" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Заголовок и лид', pts:[
    'Заголовок ловит внимание за <b>первые 3 секунды</b> — иначе пролистнут',
    'Лид сразу бьёт в <b>боль или проблему</b> читателя',
    '«Узнаёшь себя?» — читатель кивает и остаётся',
    'Без попадания в боль остальной текст никто не прочитает'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="10" width="70" height="14" rx="4" fill="currentColor" fill-opacity=".18"/><path d="M24 17h48"/><path d="M16 34h60M16 44h52" opacity=".65"/><circle cx="98" cy="46" r="14"/><path d="M98 40v8M98 52v.5" stroke-width="3"/></svg>'},
  {t:'Оффер: суть предложения', pts:[
    'Оффер — <b>что конкретно</b> ты предлагаешь и какой результат даёшь',
    'Не «услуги SMM», а «5 Reels в неделю + рост охватов за месяц»',
    'Конкретика и результат вместо размытых обещаний',
    'Хороший оффер понятен с одного прочтения'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 8l8 16 18 3-13 13 3 18-16-9-16 9 3-18-13-13 18-3z"/><path d="M52 40l6 6 12-13" stroke-width="2.6"/></svg>'},
  {t:'Продажа выгод', pts:[
    'Показывай <b>отзывы, кейсы и цифры</b> — доказательства вместо слов',
    'Говори не о свойствах, а о <b>выгоде</b> для клиента',
    '«До/После» и конкретные результаты снимают сомнения',
    'Факты и социальное доказательство продают сильнее прилагательных'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M16 58V38M40 58V26M64 58V32M88 58V16" stroke-width="3.2"/><path d="M12 58h96"/><path d="M100 22l6-6 6 6" opacity=".6"/><path d="M106 16v14" opacity=".6"/></svg>'},
  {t:'Продажа цены', pts:[
    'Объясни, <b>почему цена справедлива</b>, до того как её назвал',
    'Сравни с ценностью результата, а не с «рынком»',
    'Разложи, что входит — чтобы цифра не пугала в одиночку',
    'Цена без обоснования всегда кажется завышенной'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12v46"/><path d="M60 20L28 30l32 10 32-10z" fill="currentColor" fill-opacity=".12"/><path d="M28 30l-8 16h16zM92 30l-8 16h16z"/><path d="M20 58h80" opacity=".7"/></svg>'},
  {t:'Дедлайн и CTA', pts:[
    'Дедлайн включает <b>дефицит</b>: «до пятницы», «5 мест» — но честно',
    'CTA — <b>одно ясное действие</b>: «пиши в личку», «жми ссылку»',
    'Без призыва читатель ставит лайк и уходит',
    'Один пост — один целевой шаг, не размазывай'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="34" cy="35" r="20"/><path d="M34 22v13l9 6" stroke-width="2.8"/><rect x="66" y="26" width="44" height="18" rx="9" fill="currentColor" fill-opacity=".15"/><path d="M76 35h20M90 30l6 5-6 5"/></svg>'},
  {t:'Вовлечение через восприятие', pts:[
    'Добавляй <b>вкусы, запахи, звуки, динамику</b> — текст оживает',
    '«Хрустящая корочка» продаёт лучше, чем «вкусный продукт»',
    'Свой <b>ритм и стиль</b> отстраивает от конкурентов',
    'Читатель проживает картинку — и остаётся в посте дольше'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="40" cy="35" r="18"/><path d="M34 32a3 3 0 0 1 6 0M46 32a3 3 0 0 0-6 0" opacity=".8"/><path d="M33 42c4 4 10 4 14 0" stroke-width="2.6"/><path d="M66 24c6 0 6 8 12 8s6-8 12-8M66 46c6 0 6-8 12-8s6 8 12 8" opacity=".6"/></svg>'},
  ],
  quiz:[
  {q:'Сколько блоков в универсальной структуре продающего поста?', o:['3','5','7','10'], a:2},
  {q:'Что идёт сразу после заголовка?', o:['Цена','Лид с болью/проблемой','CTA','Отзывы'], a:1},
  {q:'За сколько секунд заголовок должен поймать внимание?', o:['3 секунды','30 секунд','2 минуты','Неважно'], a:0},
  {q:'Чем лучше всего «продавать выгоды»?', o:['Красивыми прилагательными','Отзывами, кейсами и цифрами','Длинным вступлением','Списком свойств товара'], a:1},
  {q:'Зачем нужен блок дедлайна?', o:['Чтобы удлинить пост','Чтобы создать дефицит и подтолкнуть к действию','Для красоты','Чтобы указать дату публикации'], a:1},
  {q:'Каким должен быть призыв к действию (CTA)?', o:['Несколько разных действий сразу','Одно ясное действие','Скрытым намёком','Только в комментариях'], a:1},
  ],
  pairs:[
  ['Лид','Абзац, который бьёт в боль читателя'],
  ['Оффер','Что конкретно предлагаешь и с каким результатом'],
  ['Продажа выгод','Отзывы, кейсы и цифры вместо слов'],
  ['Дедлайн','Честный дефицит, толкающий к действию'],
  ['CTA','Одно ясное целевое действие'],
  ],
  task:{
    intro:'Собери один продающий пост по полной структуре из 7 блоков для своей услуги:',
    chips:['Заголовок','Лид (боль)','Оффер','Выгоды','Цена','Дедлайн','CTA'],
    ph:'Пример: Заголовок — «Твой блог не продаёт? Дело не в тебе». Лид — про слитый бюджет на рекламу. Оффер — 8 Reels в месяц + рост охватов. Выгоды — кейс с +230% просмотров. Цена — и что в неё входит. Дедлайн — 3 места до пятницы. CTA — «напиши “хочу” в личку».',
    verdict:'Все 7 блоков на месте, боль и выгода читаются, дефицит честный. Совет куратора: добавь одну деталь восприятия (звук, вкус, движение) в лид — пост станет живее и его дочитают.'
  }
},

/* ---------- УРОК 3: Хуки, тизинг, сторителлинг и триггеры ---------- */
{
  title:'Хуки, сторителлинг и 8 триггеров',
  sub:'4:50 · видео + слайды + тест + игра',
  dur:'4:50',
  c1:'ХУКИ И', c2:'ТРИГГЕРЫ',
  slides:[
  {t:'Первые 3 секунды: пощёчина', pts:[
    'Хук — это <b>«пощёчина» внимания</b> в первые 3 секунды',
    'Эмоции-крючки: <b>удивление, любопытство, гнев, страх, тёплые воспоминания</b>',
    'Нейтральное начало = мгновенный пролистывание',
    'Сначала останови палец, потом продавай'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M46 10l-8 26h16l-10 24 34-34H62l10-16z" fill="currentColor" fill-opacity=".15"/><path d="M92 20l4 6M100 32h8M92 46l5-4" opacity=".6"/></svg>'},
  {t:'Тизинг: дразни, не вываливай', pts:[
    '<b>Тизинг</b> — дразнить и не давать всё сразу, тянуть интригу',
    'Глубина просмотра влияет на <b>продвижение</b> — держи до конца',
    '5–10 секунд подогрева интриги, но <b>не пережать</b>',
    '«Совет, который сэкономит миллионы…» — и раскрываешь позже'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M16 35c14-18 44-18 58 0-14 18-44 18-58 0Z"/><circle cx="45" cy="35" r="9"/><circle cx="45" cy="35" r="3" fill="currentColor" stroke="none"/><path d="M84 24l8 4-8 4M92 40l8 4-8 4" opacity=".6"/></svg>'},
  {t:'Сторителлинг: скелет истории', pts:[
    'Схема: <b>герой → препятствие → действие → ошибки и новые попытки</b>',
    'Падения и провалы в истории вызывают доверие сильнее победы',
    'Читатель узнаёт себя в герое — и идёт за ним',
    'От навыка рассказывать истории напрямую зависит доход'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="52" r="6"/><path d="M24 52c8-2 10-18 20-18M44 34l-4-5M44 34l-6 3" opacity=".7"/><path d="M44 34c10 0 12 16 22 16M66 50c10 0 10-30 24-30" /><circle cx="90" cy="20" r="7"/><path d="M90 20l4 4 5-6" opacity=".8"/></svg>'},
  {t:'8 триггеров покупки', pts:[
    'Психология решения — <b>8 рычагов</b>, которые подталкивают к «да»',
    'Толпа · Эксклюзив · Дефицит · Халява',
    'Авторитет/статистика · Крутые кейсы · Страх · Новизна',
    'Один пост — <b>1–2 триггера</b>, не вали все сразу'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="35" r="10"/><circle cx="60" cy="12" r="4"/><circle cx="60" cy="58" r="4"/><circle cx="37" cy="35" r="4"/><circle cx="83" cy="35" r="4"/><circle cx="44" cy="19" r="4"/><circle cx="76" cy="19" r="4"/><circle cx="44" cy="51" r="4"/><circle cx="76" cy="51" r="4"/><path d="M60 25V16M60 45v9M50 35h-9M70 35h9M53 28l-6-6M67 28l6-6M53 42l-6 6M67 42l6 6" opacity=".55"/></svg>'},
  {t:'Триггеры «как все / не как все»', pts:[
    '<b>Толпа</b>: «уже 3000 человек взяли» — страшно остаться в стороне',
    '<b>Эксклюзив</b>: «только для своих» — желание выделиться',
    '<b>Дефицит</b>: «осталось 5 мест» — страх упустить',
    '<b>Халява</b>: бесплатный тест-драйв затягивает в воронку'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="24" cy="24" r="6"/><circle cx="40" cy="24" r="6"/><circle cx="32" cy="40" r="6"/><path d="M14 56c0-8 6-12 10-12M50 56c0-8-6-12-10-12M22 56c0-8 5-12 10-12s10 4 10 12" opacity=".7"/><path d="M82 16l4 8 8 1-6 6 1.5 8-7.5-4-7 4 1.5-8-6-6 8-1z" fill="currentColor" stroke="none" opacity=".85"/></svg>'},
  {t:'Триггеры доверия и движения', pts:[
    '<b>Авторитет/статистика</b>: весомое мнение и цифры снимают сомнения',
    '<b>Крутые кейсы</b>: «хочу так же» — показал результат другого',
    '<b>Страх</b>: «боюсь, что будет хуже» — пугай с заботой, не давя',
    '<b>Новизна</b>: «будь первым» — свежее тянет само по себе'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M30 10l6 12 13 2-9.5 9 2.5 13-12-6.5-12 6.5 2.5-13L23 24l13-2z"/><path d="M30 12v22" opacity=".4"/><path d="M70 50V32M84 50V22M98 50V38" stroke-width="3"/><path d="M66 50h40" opacity=".7"/></svg>'},
  {t:'Как собрать всё вместе', pts:[
    'Хук ловит → тизинг держит → история вовлекает → триггер закрывает',
    'Триггеры работают в блоках выгод, цены и дедлайна из урока 2',
    'Проверяй честность: не «500 мест» при 100 подписчиках',
    'Практика важнее теории — тестируй крючки и смотри досмотры'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 40c0-6 4-10 10-10s10 4 10 10M22 40l4 6-4 6" opacity=".8"/><path d="M32 46h16M44 42l6 4-6 4"/><rect x="54" y="34" width="20" height="24" rx="5"/><path d="M60 44h8M60 50h6" opacity=".6"/><path d="M80 46h14M90 42l6 4-6 4"/><circle cx="104" cy="46" r="9"/><path d="M100 46l3 3 6-7"/></svg>'},
  ],
  quiz:[
  {q:'Что такое хук в первые 3 секунды?', o:['Подпись автора','«Пощёчина» вниманию через эмоцию','Ссылка на источник','Список хэштегов'], a:1},
  {q:'В чём суть тизинга?', o:['Сразу выдать всю пользу','Дразнить и тянуть интригу, не давая всё сразу','Публиковать без текста','Копировать конкурентов'], a:1},
  {q:'Правильный скелет истории:', o:['Реклама → цена → скидка','Герой → препятствие → действие → ошибки и попытки','Заголовок → хэштеги → ссылка','Отзыв → отзыв → отзыв'], a:1},
  {q:'«Осталось 5 мест» — какой это триггер?', o:['Триггер халявы','Триггер дефицита','Триггер новизны','Триггер авторитета'], a:1},
  {q:'«Уже 3000 человек взяли» — какой триггер?', o:['Триггер толпы','Триггер эксклюзива','Триггер страха','Триггер кейсов'], a:0},
  {q:'Как правильно использовать триггер страха?', o:['Давить и запугивать','Пугать с заботой, честно показывая последствия','Обещать катастрофу без причины','Не использовать вовсе'], a:1},
  ],
  pairs:[
  ['Хук','Первая фраза-пощёчина, ловит внимание за 3 секунды'],
  ['Тизинг','Дразнить и тянуть интригу, не давая всё сразу'],
  ['Сторителлинг','Герой → препятствие → действие → ошибки'],
  ['Триггер дефицита','«Осталось мало» — страх упустить'],
  ['Триггер толпы','«Как все уже взяли» — не остаться в стороне'],
  ],
  task:{
    intro:'Сделай связку «хук + тизинг + история + триггер» для своего поста или Reels:',
    chips:['3 хука под эмоции','Строка тизинга','Мини-история','1–2 триггера'],
    ph:'Пример: хук — «Я слил 40 000 на рекламу, чтобы ты не повторял». Тизинг — «одна ошибка съедала весь бюджет, расскажу в конце». История — герой (новичок) → препятствие (нет заявок) → действие (сменил оффер) → результат. Триггер кейса + триггер страха упустить.',
    verdict:'Хук цепляет, тизинг держит до конца, история читается, триггеры подобраны верно. Совет куратора: проверь честность цифр и дефицита — не «500 мест» при 100 подписчиках, иначе доверие рушится.'
  }
}

];
/* Единый плоский список в порядке направлений: Медийность (основы+Reels) → Маркетинг → Нейросети.
   Глобальные индексы: 0..5 Медийность, 6..8 Маркетинг, 9..13 Нейросети. */
/* ============================================================
   ТРЕК 1. МЕДИЙНОСТЬ — уроки 4–6 (продолжение)
   Источник: OKO_ACADEMY_CURRICULUM.md, модули 1.4–1.6
   (Типы контента и эмоции · Сторис, эфиры, вовлечение · Характер и архетипы бренда).
   Формат один в один с AC_COURSE (academy/script.js).
   ============================================================ */
const MEDIA_LESSONS2 = [

/* ---------- УРОК 4: Типы контента и эмоции ---------- */
{
  title:'Типы контента и эмоции',
  sub:'1:02 · видео (голос Даниэля) + слайды + тест + игра',
  dur:'1:02',
  videoUrl:'https://okoteam.top/media/oko_media_l4.mp4',
  c1:'КОНТЕНТ', c2:'И ЭМОЦИИ',
  slides:[
  {t:'Личный контент строит доверие', pts:[
    'Люди <b>покупают у тех, кому доверяют</b>, а доверяют — знакомым',
    'Личный контент делает тебя знакомым: имя, лицо, история, быт',
    'Экспертность отвечает «умеет ли он», личное — «свой ли он мне»',
    'Без личного слоя блог остаётся витриной, к которой не привязываются'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="42" cy="26" r="11"/><path d="M24 56c2-13 8-19 18-19s16 6 18 19"/><path d="M74 30l10 8 18-20" stroke-width="3"/></svg>'},
  {t:'Три опоры контента', pts:[
    '<b>Личный</b> — сближает и рождает доверие к человеку за блогом',
    '<b>Экспертный</b> — показывает, что ты решаешь проблему аудитории',
    '<b>Продающий</b> — прямо ведёт к продукту и заявке',
    'Здоровый блог держит баланс трёх типов, а не давит одним'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10 96 60H24z"/><path d="M42 60V38M60 60V26M78 60V44" opacity=".7"/><circle cx="60" cy="20" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Шкала эмоций', pts:[
    'Эмоции широкие: <b>радость, восхищение, благодарность</b>, и злость тоже',
    'В работу идут и <b>разочарование, несогласие, сопереживание</b>',
    'Нельзя быть всегда <b>«хорошеньким»</b> — пресный образ не запоминают',
    'Разрешай себе весь спектр: живой человек чувствует по-разному'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M14 50h92" opacity=".4"/><path d="M14 50C30 20 44 20 60 38s30 20 46-8"/><circle cx="30" cy="34" r="3" fill="currentColor" stroke="none"/><circle cx="60" cy="38" r="3" fill="currentColor" stroke="none"/><circle cx="90" cy="30" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Плохая реакция — это никакая', pts:[
    '<b>Худшая реакция — нейтральность</b>: пролистали и забыли',
    'Любая эмоция — даже спор — лучше молчаливого равнодушия',
    '<b>Нельзя нравиться всем</b>: пытаясь угодить каждому, теряешь лицо',
    'Позиция притягивает своих и отсеивает чужих — это нормально'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 18h34a8 8 0 0 1 8 8v14a8 8 0 0 1-8 8H36l-12 10V48h-4a8 8 0 0 1-8-8V26a8 8 0 0 1 8-8z"/><path d="M74 30h30M74 42h22" opacity=".7"/><path d="M92 52l12 8-12 8" opacity=".5"/></svg>'},
  {t:'Приверженность и связь', pts:[
    '<b>Приверженность</b> — сильная эмоциональная связь с аудиторией',
    'Цель — чтобы по тебе <b>скучали</b>, когда ты пропадаешь из ленты',
    'Связь строится на регулярности, честности и общих ценностях',
    'Приверженный подписчик прощает промахи и рекомендует тебя сам'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 56S28 40 28 24a14 14 0 0 1 26-6 14 14 0 0 1 26 6c0 16-20 32-20 32z" transform="translate(0 2)"/><path d="M14 32h10M96 32h10" opacity=".5"/></svg>'},
  {t:'Контент-план под нишу', pts:[
    'У каждой ниши свой набор рубрик: <b>кондитер</b> и <b>дизайнер</b> — разные',
    'Кондитер: процесс, начинки, отзывы, закулисье кухни, акции',
    'Фриланс-дизайнер: кейсы до/после, разбор ошибок, будни, оффер',
    'Не копируй чужой план — <b>собери свой</b> из личного, экспертного, продающего'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="10" width="76" height="52" rx="6"/><path d="M22 24h76" opacity=".6"/><path d="M40 10v52M64 10v52M82 10v52" opacity=".5"/><rect x="26" y="30" width="10" height="8" rx="2" fill="currentColor" stroke="none" opacity=".8"/><rect x="46" y="42" width="10" height="8" rx="2" fill="currentColor" stroke="none" opacity=".8"/></svg>'},
  {t:'Блог как комьюнити', pts:[
    'Сильный блог — это <b>место общения</b>, а не лента-монолог',
    'Транслируй мысль <b>«вы не одни»</b> — люди ищут поддержку и своих',
    'Отвечай в комментариях и директе — диалог удерживает лучше охватов',
    'Комьюнити защищает тебя, спорит с хейтом и приводит новых людей'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="40" cy="24" r="9"/><circle cx="80" cy="24" r="9"/><circle cx="60" cy="46" r="9"/><path d="M46 30l10 10M74 30L64 40M40 33v13M80 33v13" opacity=".6"/></svg>'},
  {t:'Чек-лист контента и эмоций', pts:[
    'В блоге есть личный слой, а не только экспертиза и продажи',
    'Разрешаешь себе весь спектр эмоций, не только «хорошенькое»',
    'Не боишься спорной реакции — боишься равнодушия',
    'Строишь приверженность: регулярность, честность, ценности',
    'Контент-план собран под свою нишу, блог работает как комьюнити'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="6" width="68" height="58" rx="8"/><path d="M36 20l4 4 7-8M36 36l4 4 7-8M36 52l4 4 7-8" stroke-width="2.8"/><path d="M54 21h28M54 37h28M54 53h20" opacity=".8"/></svg>'},
  ],
  quiz:[
  {q:'Почему личный контент так важен для продаж?', o:['Он дешевле в производстве','Люди покупают у тех, кому доверяют','Так требуют алгоритмы','Он собирает больше лайков автоматически'], a:1},
  {q:'Какая реакция аудитории считается худшей?', o:['Злость','Несогласие и спор','Нейтральность — пролистали и забыли','Слишком бурная радость'], a:2},
  {q:'Что урок говорит про попытку нравиться всем?', o:['Это главная цель блога','Нельзя нравиться всем — потеряешь лицо','Так делают все успешные блогеры','Это возможно при хорошем визуале'], a:1},
  {q:'Что такое приверженность в контексте блога?', o:['Число подписчиков','Частота публикаций','Сильная эмоциональная связь, чтобы по тебе скучали','Количество рекламных интеграций'], a:2},
  {q:'Каким должен быть контент-план?', o:['Скопирован у топового конкурента','Собран под свою нишу из личного, экспертного, продающего','Только продающим','Только развлекательным'], a:1},
  {q:'Какую мысль транслирует блог-комьюнити?', o:['«Купи прямо сейчас»','«Я лучше конкурентов»','«Вы не одни» — место общения и поддержки','«Смотрите, как я живу»'], a:2},
  ],
  pairs:[
  ['Личный контент','Строит доверие: покупают у знакомых'],
  ['Нейтральность','Худшая реакция — пролистали и забыли'],
  ['Шкала эмоций','Весь спектр, а не всегда «хорошенькое»'],
  ['Приверженность','Связь, чтобы по тебе скучали'],
  ['Блог-комьюнити','Место общения с мыслью «вы не одни»'],
  ],
  task:{
    intro:'Собери свой контент-план на неделю с балансом типов и эмоций. Распиши рубрики под свою нишу и отметь, где личное, где экспертное, где продающее и какую эмоцию несёт каждый пост:',
    chips:['Моя ниша','Личные рубрики','Экспертные рубрики','Продающие рубрики','Эмоция каждого поста'],
    ph:'Пример: Ниша — кондитер. Пн — личное: как я пришла в дело (тепло). Вт — экспертное: 3 ошибки в начинках (польза). Ср — закулисье кухни (доверие). Чт — отзыв клиента (радость). Пт — продающее: набор к празднику + дедлайн. Каждый пост несёт одну явную эмоцию, а не «нейтралку».',
    verdict:'Баланс типов виден, и это уже сильнее, чем сплошные продажи. Совет куратора: проверь, чтобы в неделе был хотя бы один пост, который может вызвать спор или сильное чувство — именно он пробьёт равнодушие. И добавь личного: люди привязываются к человеку, а не к рубрикам. Строй приверженность регулярностью, чтобы по тебе начали скучать.'
  }
},

/* ---------- УРОК 5: Сторис, эфиры, вовлечение ---------- */
{
  title:'Сторис, эфиры, вовлечение',
  sub:'4:30 · видео + слайды + тест + игра',
  dur:'4:30',
  c1:'СТОРИС', c2:'И ЭФИРЫ',
  slides:[
  {t:'Зачем нужны сторис', pts:[
    'Сторис — <b>мост к посту</b>: прогреваешь и заводишь читателя в контент',
    'Это лента «здесь и сейчас»: события жизни, быт, закулисье',
    '<b>Прогрев</b> в сторис готовит аудиторию к продаже без давления',
    'Регулярные сторис держат тебя в верхних строчках у подписчиков'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="42" y="10" width="36" height="50" rx="8"/><circle cx="60" cy="20" r="6"/><path d="M50 34h20M50 42h14" opacity=".7"/><circle cx="20" cy="35" r="7" opacity=".7"/><circle cx="100" cy="35" r="7" opacity=".7"/></svg>'},
  {t:'Сторис ведут в пост', pts:[
    'Не давай всё в сторис — <b>дразни</b> и отправляй дочитать в посте',
    'Анонс, интрига, кусочек результата — и ссылка «подробнее в ленте»',
    'Так сторис поднимают охваты поста, а пост углубляет тему',
    'Связка сторис плюс пост работает сильнее, чем каждый по отдельности'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="14" y="12" width="30" height="46" rx="7"/><rect x="76" y="16" width="30" height="38" rx="5"/><path d="M48 35h22" opacity=".8"/><path d="M62 29l10 6-10 6" opacity=".8"/></svg>'},
  {t:'Эфиры — сильнейший инструмент', pts:[
    '<b>Эфир (Live)</b> — самый мощный формат сближения: ты живой, здесь и сейчас',
    'Живая обратная связь: вопросы, реакции, разбор в реальном времени',
    'Эфир снимает дистанцию сильнее любого поста — тебя видят настоящим',
    'После эфира доверие и лояльность растут скачком'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="14" width="60" height="42" rx="8"/><circle cx="60" cy="35" r="9"/><path d="M18 26a18 18 0 0 0 0 18M102 26a18 18 0 0 1 0 18" opacity=".6"/><circle cx="44" cy="22" r="2.5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Как удерживать на эфире', pts:[
    '<b>Меняй ракурс</b> и картинку — статичная камера усыпляет',
    'Играй <b>тоном и темпом</b>: тише-громче, быстрее-медленнее',
    'Ставь <b>крючки</b>: «через минуту покажу главное», «дождитесь конца»',
    'Обращайся к зрителям по именам — живой диалог держит в эфире'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 50V30l16-10v40z"/><path d="M36 26l30-12v42L36 44" opacity=".85"/><path d="M80 24v22M90 30v16M100 20v30" opacity=".6"/></svg>'},
  {t:'Опросы: инструмент вовлечения', pts:[
    '<b>Опросы</b> вовлекают в один тап и оживляют охваты сторис',
    'Через опросы <b>выявляешь проблемы</b>, которые потом закрываешь продуктом',
    'Спрашивай о боли, выборе, предпочтениях — аудитория сама даёт ТЗ',
    'Ответивший чувствует, что его услышали, — растёт связь'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="22" y="16" width="76" height="16" rx="8"/><rect x="22" y="40" width="52" height="16" rx="8"/><path d="M22 24h50" opacity=".5"/><circle cx="90" cy="24" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Развлекательная форма', pts:[
    'Вовлекай <b>играючи</b>: тесты, «угадай», рубрики, интерактивные наклейки',
    'Развлечение снижает барьер — подписчик участвует, а не смотрит молча',
    'Даже сложную тему подавай легко: игра запоминается лучше лекции',
    'Чередуй пользу и развлечение, чтобы сторис не превращались в отчёт'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="18" width="34" height="34" rx="6" transform="rotate(-8 43 35)"/><rect x="60" y="18" width="34" height="34" rx="6" transform="rotate(8 77 35)"/><circle cx="43" cy="35" r="3" fill="currentColor" stroke="none"/><circle cx="72" cy="30" r="2.5" fill="currentColor" stroke="none"/><circle cx="82" cy="40" r="2.5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Живой персонаж даже в коммерции', pts:[
    'Даже у <b>коммерческого</b> аккаунта должен быть живой персонаж',
    'За брендом — человек: голос, мнение, эмоции, а не безликий логотип',
    'Персонаж отличает тебя от десятков одинаковых магазинов в нише',
    'Люди возвращаются к личности, а к обезличенной витрине — нет'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="24" r="11"/><path d="M40 58c2-13 9-19 20-19s18 6 20 19"/><path d="M52 24h.5M68 24h.5" stroke-width="3"/><path d="M54 30c4 4 8 4 12 0" opacity=".8"/></svg>'},
  {t:'Чек-лист вовлечения', pts:[
    'Ведёшь сторис регулярно и заводишь ими читателя в пост',
    'Проводишь эфиры и умеешь удерживать: ракурс, тон, крючки',
    'Используешь опросы, чтобы выявлять боли аудитории',
    'Подаёшь контент играючи, чередуешь пользу и развлечение',
    'У аккаунта есть живой персонаж, даже если он коммерческий'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="6" width="68" height="58" rx="8"/><path d="M36 20l4 4 7-8M36 36l4 4 7-8M36 52l4 4 7-8" stroke-width="2.8"/><path d="M54 21h28M54 37h28M54 53h20" opacity=".8"/></svg>'},
  ],
  quiz:[
  {q:'Какую основную роль играют сторис?', o:['Заменяют посты полностью','Вовлекают в пост, прогревают и показывают события жизни','Нужны только для рекламы','Служат архивом фотографий'], a:1},
  {q:'Почему эфиры называют сильнейшим инструментом?', o:['Их проще снимать, чем сторис','Дают живую обратную связь и снимают дистанцию','Их продвигают лучше всех форматов','Они не требуют подготовки'], a:1},
  {q:'Что помогает удерживать зрителя на эфире?', o:['Одна статичная камера и ровный тон','Смена ракурса, игра тоном и крючки','Максимально длинный монолог','Отключить комментарии'], a:1},
  {q:'Чем полезны опросы кроме вовлечения?', o:['Накручивают охваты','Выявляют проблемы, которые ты закрываешь продуктом','Заменяют аналитику','Повышают качество фото'], a:1},
  {q:'Нужен ли живой персонаж коммерческому аккаунту?', o:['Нет, коммерции хватает товара','Да — за брендом должен стоять человек','Только на старте','Только в лайфстайл-блогах'], a:1},
  {q:'Как лучше подавать вовлекающий контент?', o:['Строго и по-деловому','Играючи: тесты, интерактив, чередуя пользу и развлечение','Только через длинные тексты','Через сплошные продажи'], a:1},
  ],
  pairs:[
  ['Сторис','Мост к посту: прогрев и события жизни'],
  ['Эфир','Живая обратная связь, снимает дистанцию'],
  ['Крючки','Удержание на эфире: «дождитесь конца»'],
  ['Опросы','Выявляют боли, которые закрываешь продуктом'],
  ['Живой персонаж','Человек за брендом даже в коммерции'],
  ],
  task:{
    intro:'Спланируй вовлекающую цепочку на день: сторис, которые ведут в пост, тему эфира и опрос, выявляющий боль. Пропиши, как удержишь внимание и где проявится твой живой персонаж:',
    chips:['Сторис-прогрев','Ссылка в пост','Тема эфира','Крючки удержания','Опрос про боль'],
    ph:'Пример: Сторис — «сегодня разберу ошибку, из-за которой сливают бюджет», в конце «подробно в новом посте». Эфир вечером: разбор упаковки подписчиков вживую, крючки — «главный совет в конце». Опрос: «Что мешает вести блог? — нет времени / не знаю о чём / стесняюсь камеры». Персонаж — говорю прямо, шучу над собой.',
    verdict:'Цепочка связная: сторис прогревают, пост углубляет, эфир сближает. Совет куратора: в опросе спрашивай про конкретную боль, а не абстрактно, — ответы станут готовым ТЗ для твоего продукта. На эфире не держи одну статичную камеру: меняй ракурс и ставь крючок в начале, чтобы досидели до финала. И не прячь персонажа за пользой — люди остаются ради человека.'
  }
},

/* ---------- УРОК 6: Характер и архетипы бренда ---------- */
{
  title:'Характер и архетипы бренда',
  sub:'4:40 · видео + слайды + тест + игра',
  dur:'4:40',
  c1:'ХАРАКТЕР', c2:'БРЕНДА',
  slides:[
  {t:'Бренд как человек', pts:[
    'Сильный бренд общается с аудиторией <b>как человек</b>, а не как инструкция',
    'У него есть <b>характер</b>: манера речи, ценности, реакции, юмор',
    'Люди привязываются к характерам, а не к безличным названиям',
    'Определи характер один раз — и весь контент зазвучит в одном тоне'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="26" r="12"/><path d="M40 58c2-14 9-20 20-20s18 6 20 20"/><path d="M18 30l6 6 8-10M96 30l-6 6-8-10" opacity=".6"/></svg>'},
  {t:'Что такое архетип', pts:[
    '<b>Архетип</b> — узнаваемый образ-роль, понятный без объяснений',
    'Он задаёт, как бренд говорит, что обещает и какие эмоции несёт',
    'Опора на архетип делает бренд <b>цельным и предсказуемым</b> для своих',
    'Аудитория считывает архетип интуитивно и быстро выбирает «свой» бренд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10l8 16 18 2-13 13 4 18-17-9-17 9 4-18-13-13 18-2z"/><circle cx="60" cy="34" r="4" fill="currentColor" stroke="none"/></svg>'},
  {t:'Стабильность и контроль', pts:[
    'Архетип <b>стабильности</b>: надёжность, порядок, забота, безопасность',
    'Обещание — «со мной спокойно и предсказуемо, я не подведу»',
    'Тон спокойный, уверенный, без крайностей и агрессии',
    'Подходит там, где клиент боится риска и ищет опору'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 10l32 12v14c0 18-14 28-32 34-18-6-32-16-32-34V22z"/><path d="M48 34l8 8 18-18" stroke-width="2.8"/></svg>'},
  {t:'Свобода', pts:[
    'Архетип <b>свободы</b>: независимость, путешествия, «живи по-своему»',
    'Обещание — «сними рамки, выбирай сам, будь автором своей жизни»',
    'Тон лёгкий, вдохновляющий, зовущий в движение и перемены',
    'Притягивает тех, кто устал от правил и хочет дышать'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M22 44c10-2 16-8 24-8s10 6 18 6 14-8 24-10" opacity=".7"/><path d="M60 12c-8 8-10 18-6 26 6-2 12-8 12-16 0 8 4 14 10 16 4-10 0-20-8-26" /><path d="M60 38v20" opacity=".6"/></svg>'},
  {t:'Вызов и трансформация', pts:[
    'Архетип <b>вызова</b>: рост, преодоление, «стань лучшей версией себя»',
    'Обещание — «через усилие и изменение я приведу тебя к результату»',
    'Тон энергичный, прямой, местами провокационный и требовательный',
    'Резонирует с теми, кто готов меняться и хочет прорыва'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M14 58h30l-8-20 22 6-6-22 26 8" /><path d="M86 14l6 4 4-6" opacity=".6"/><circle cx="44" cy="38" r="2.5" fill="currentColor" stroke="none"/></svg>'},
  {t:'Радость и тепло', pts:[
    'Архетип <b>радости</b>: лёгкость, юмор, забота, домашнее тепло',
    'Обещание — «со мной хорошо, тепло и по-доброму»',
    'Тон дружелюбный, живой, с улыбкой и человеческим отношением',
    'Собирает аудиторию, которая ищет позитив и принятие'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="60" cy="34" r="24"/><path d="M50 30h.5M70 30h.5" stroke-width="3.4"/><path d="M46 42c6 8 22 8 28 0" stroke-width="2.8"/></svg>'},
  {t:'Чистые характеры редки', pts:[
    'На практике <b>чистый архетип встречается редко</b> — обычно это смесь',
    'Например: стабильность + тепло, или свобода + вызов',
    'Именно <b>сочетание</b> делает бренд объёмным и уникальным',
    'Смесь — не каша: у неё есть ведущий архетип и один-два оттенка'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="48" cy="35" r="20"/><circle cx="72" cy="35" r="20"/><path d="M60 20a20 20 0 0 1 0 30 20 20 0 0 1 0-30z" opacity=".5"/></svg>'},
  {t:'ДЗ: характер твоего бренда', pts:[
    'Определи <b>ведущий архетип</b> и один-два поддерживающих оттенка',
    'Пропиши манеру речи, ценности и запретные для бренда темы',
    'Проверь: звучит ли твой последний контент в этом характере',
    'Характер — не украшение, а фильтр решений «наше / не наше»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="8" width="60" height="54" rx="8"/><path d="M40 22l4 4 6-7M40 36l4 4 6-7M40 50l4 4 6-7" stroke-width="2.8"/><path d="M58 23h22M58 37h22M58 51h16" opacity=".8"/></svg>'},
  ],
  quiz:[
  {q:'Что значит «у бренда есть характер»?', o:['У него красивый логотип','Он общается как человек: манера речи, ценности, реакции','Он часто постит','У него много подписчиков'], a:1},
  {q:'Что такое архетип бренда?', o:['Шрифт и цвет','Узнаваемый образ-роль, задающий тон и обещание','Название компании','Формат сторис'], a:1},
  {q:'Какому архетипу соответствуют надёжность, порядок и забота?', o:['Свобода','Вызов и трансформация','Стабильность и контроль','Радость и тепло'], a:2},
  {q:'Что обещает архетип вызова и трансформации?', o:['Спокойствие и предсказуемость','Рост и преодоление, «стань лучшей версией себя»','Лёгкость и юмор','Независимость и путешествия'], a:1},
  {q:'Почему чистые архетипы встречаются редко?', o:['Их запрещают алгоритмы','Обычно бренд — смесь, и это делает его уникальнее','Их сложно нарисовать','Так дороже в производстве'], a:1},
  {q:'Чем на практике служит характер бренда?', o:['Украшением профиля','Фильтром решений «наше / не наше»','Заменой контент-плана','Способом накрутки'], a:1},
  ],
  pairs:[
  ['Характер бренда','Общение с аудиторией как с человеком'],
  ['Стабильность','Надёжность, порядок, забота, безопасность'],
  ['Свобода','Независимость, «живи по-своему»'],
  ['Вызов','Рост и трансформация, «стань лучше»'],
  ['Смесь архетипов','Ведущий плюс оттенки — уникальность бренда'],
  ],
  task:{
    intro:'Определи характер своего бренда как ДЗ модуля. Выбери ведущий архетип и один-два оттенка, пропиши манеру речи, ценности и запретные темы, а затем проверь последний контент на соответствие:',
    chips:['Ведущий архетип','Оттенки','Манера речи','Ценности','Запретные темы','Проверка контента'],
    ph:'Пример: Ведущий — вызов (рост и результат), оттенок — тепло. Манера речи: прямо, по делу, с поддержкой, без токсичности. Ценности: честность, дисциплина, забота о результате клиента. Запретные темы: обесценивание, пустой хайп. Проверка: последние 5 постов звучат в этом характере, кроме одного «нейтрального» — переписать.',
    verdict:'Характер собран правильно: есть ведущий архетип и оттенок, а не размытое «всё сразу». Совет куратора: держи этот паспорт характера рядом и сверяй каждый пост — он и есть фильтр «наше / не наше». Если контент выпадает из тона, дело не в теме, а в подаче: перепиши в голосе бренда. Смесь архетипов — твоё преимущество, не бойся сочетать, но всегда оставляй один ведущий.'
  }
}

];
/* ================= МАРКЕТИНГ: уроки 4-6 (продолжение) =================
   Модули 2.5 (Анализ конкурентов), 2.6 (Монетизация и продукт),
   2.7 (Продвижение и алгоритмы). Формат AC_COURSE. */

const MARKETING_LESSONS2 = [

/* ---------- УРОК 4: Анализ конкурентов ---------- */
{
  title:'Анализ конкурентов',
  sub:'4:12 · четыре типа · таблица · что делать с каждым',
  dur:'4:12',
  c1:'АНАЛИЗ', c2:'КОНКУРЕНТОВ',
  slides:[
  {t:'Зачем вообще смотреть на других', pts:[
    'Конкурент — не враг, а <b>бесплатная разведка</b>: он уже проверил спрос за свои деньги',
    'Ты видишь, какие офферы заходят, а какие проваливаются — до того, как вложился сам',
    'Цель анализа не «скопировать», а <b>найти щель</b>, куда никто не встал',
    'Не смотришь на рынок — работаешь вслепую и повторяешь чужие ошибки'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="46" cy="34" r="18"/><path d="M59 47l16 16"/><circle cx="46" cy="34" r="7"/><path d="M46 20v-6M46 54v6M32 34h-6M60 34h6" opacity=".6"/></svg>'},
  {t:'Две оси таблицы', pts:[
    'Строим сетку: по горизонтали — <b>товар</b> (похож / другой), по вертикали — <b>потребность</b> клиента',
    'Из двух осей рождаются четыре клетки конкурентов — от прямых до неявных',
    'Каждая клетка требует своей тактики, единого «списка врагов» не бывает',
    'Раскинул сетку — сразу видно, где тесно, а где пусто'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="30" y="12" width="60" height="46" rx="4"/><path d="M60 12v46M30 35h60"/><circle cx="45" cy="24" r="3" fill="currentColor" stroke="none"/><circle cx="75" cy="24" r="3" fill="currentColor" stroke="none"/><circle cx="45" cy="46" r="3" fill="currentColor" stroke="none"/><circle cx="75" cy="46" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Прямые конкуренты', pts:[
    '<b>Тот же товар — та же потребность.</b> Кофейня напротив другой кофейни',
    'Бьётесь за одного и того же клиента лоб в лоб — тут решает отстройка',
    'Смотри не на цену, а на то, чего у них <b>нет</b>: сервис, скорость, атмосфера',
    'Прямой конкурент задаёт планку — держи её и добавляй одно своё «зато»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M30 50V26l16-10 16 10v24z"/><path d="M74 50V26l16-10 16 10v24z"/><path d="M46 50V38h-16v12M90 50V38H74v12"/><path d="M60 58h0" opacity=".4"/><path d="M58 34h4" opacity=".7"/></svg>'},
  {t:'Косвенные конкуренты', pts:[
    '<b>Похожий товар — другая цель.</b> Кофейня и чайная: напиток тот же жанр, повод разный',
    'Клиент выбирает между вами не всегда, а по настроению и ситуации',
    'Тактика — <b>перехватывать поводы</b>: «зашёл за чаем — попробуй наш раф»',
    'Косвенный конкурент расширяет рынок, а не делит его — учись у его аудитории'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M28 24h20v10a10 10 0 01-20 0z"/><path d="M48 26h5a4 4 0 010 8h-5" opacity=".7"/><path d="M38 44v8" opacity=".6"/><path d="M74 24h20v10a10 10 0 01-20 0z"/><path d="M80 14c0 4-3 4-3 8M88 14c0 4-3 4-3 8" opacity=".7"/><path d="M84 44v8" opacity=".6"/></svg>'},
  {t:'Неявные конкуренты', pts:[
    '<b>Другой товар — та же потребность.</b> С кофейней конкурирует и энергетик, и сон',
    'Клиент решает свою задачу (взбодриться) — и не всегда через твой продукт',
    'Самый опасный тип: ты его не видишь в лицо, а он забирает деньги',
    'Тактика — <b>продавать саму потребность</b>, а не форму: «нужна энергия — вот ритуал»'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 12l6 12 13 2-9 9 2 13-12-6-12 6 2-13-9-9 13-2z"/><circle cx="60" cy="41" r="4" fill="currentColor" stroke="none"/><path d="M22 30l4 4M94 30l-4 4M30 56l5-3M85 56l-5-3" opacity=".5"/></svg>'},
  {t:'Будущие и партнёры', pts:[
    '<b>Будущие</b> — те, кто вот-вот войдёт в нишу: сети, маркетплейсы, крупные игроки',
    '<b>Партнёры</b> — соседи по потребности, с кем можно дружить: пекарня рядом с кофейней',
    'Готовься к будущим заранее: строй базу лояльных, пока их ещё нет',
    'С партнёром <b>обмен трафиком</b> дешевле любой рекламы — ищи взаимную выгоду'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="38" cy="34" r="12"/><circle cx="82" cy="34" r="12"/><path d="M50 30h20M50 38h20" opacity=".7"/><path d="M62 26l8 4-8 4M58 34l-8 4 8 4" opacity=".8"/></svg>'},
  {t:'Как собрать анализ за вечер', pts:[
    'Выпиши <b>5-7 игроков</b> в каждую из четырёх клеток — руками, без автоматики',
    'По каждому: оффер, цена, сильное, слабое, чем цепляет в первые 3 секунды',
    'Найди <b>общую дыру</b> — то, чего нет ни у кого: это и есть твоё место',
    'Анализ живой: обновляй раз в квартал, ниши двигаются быстро'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="24" y="10" width="72" height="52" rx="6"/><path d="M34 24h20M34 34h20M34 44h14" opacity=".8"/><path d="M64 22l4 4 8-8M64 34l4 4 8-8" stroke-width="2.8"/><circle cx="72" cy="48" r="6"/><path d="M76 52l5 5"/></svg>'},
  ],
  quiz:[
  {q:'По каким двум осям строится таблица конкурентов?', o:['Цена и качество','Товар и потребность клиента','Возраст и доход','Онлайн и офлайн'], a:1},
  {q:'Кофейня напротив другой кофейни — это конкурент…', o:['Прямой','Косвенный','Неявный','Будущий'], a:0},
  {q:'Похожий товар, но другая цель использования — это…', o:['Прямой','Косвенный','Неявный','Партнёр'], a:1},
  {q:'Другой товар закрывает ту же потребность клиента — это…', o:['Прямой','Косвенный','Неявный','Будущий'], a:2},
  {q:'Пекарня рядом с кофейней, с которой выгоден обмен трафиком, — это…', o:['Прямой конкурент','Неявный конкурент','Партнёр','Будущий конкурент'], a:2},
  {q:'Главная цель анализа конкурентов по уроку:', o:['Скопировать сильнейшего','Найти незанятую щель на рынке','Снизить свою цену','Собрать список врагов'], a:1},
  ],
  pairs:[
  ['Прямые','Тот же товар, та же потребность'],
  ['Косвенные','Похожий товар, другая цель'],
  ['Неявные','Другой товар, та же потребность'],
  ['Партнёры','Сосед по потребности, обмен трафиком'],
  ['Будущие','Крупный игрок, который вот-вот войдёт'],
  ],
  task:{
    intro:'Разбери свой рынок по таблице урока — заполни все четыре клетки конкретными именами и решениями:',
    chips:['Прямые','Косвенные','Неявные','Партнёры/будущие','Свободная щель'],
    ph:'Пример: ниша — монтаж Reels. Прямые — другие монтажёры-фрилансеры. Косвенные — шаблонные приложения (CapCut-пресеты). Неявные — заказчик монтирует сам. Партнёры — сценаристы и таргетологи. Щель — никто не даёт монтаж + аналитику в одном пакете.',
    verdict:'Клетки заполнены по делу, неявный конкурент найден верно — это уже сильнее большинства. Совет куратора: сформулируй свою «щель» одной фразой-оффером, чтобы она сразу читалась в шапке профиля.'
  }
},

/* ---------- УРОК 5: Монетизация и продукт ---------- */
{
  title:'Монетизация и продукт',
  sub:'4:38 · немного пользы · каскад продукта · проверка спроса',
  dur:'4:38',
  c1:'МОНЕТИЗАЦИЯ', c2:'И ПРОДУКТ',
  slides:[
  {t:'Ошибка «откупиться пользой»', pts:[
    'Новичок вываливает <b>максимум бесплатного</b> — и клиент уходит сытым, но без покупки',
    'Бесплатно должно решать <b>маленькую</b> задачу, платно — большую',
    'Много пользы даром обесценивает продукт и приучает не платить',
    'Твоя щедрость — это <b>тест-драйв</b>, а не благотворительность'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M24 52h30l-4-26H28z"/><path d="M32 26c0-8 3-12 9-12s9 4 9 12" opacity=".7"/><path d="M74 20l6 12 12 2-9 9 2 12-11-6-11 6 2-12-9-9 12-2z" opacity=".85"/><path d="M39 40h0" opacity=".4"/></svg>'},
  {t:'Принцип «немного пользы»', pts:[
    'Дай бесплатно/за минимальный чек <b>первый шаг</b> — быстрый и осязаемый результат',
    'Клиент почувствовал «работает» → сам захотел следующий шаг за деньги',
    'Пример: бесплатный разбор одного Reels → платный монтаж серии',
    'Немного пользы = <b>крючок доверия</b>, а не весь улов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M40 12c0 14 8 18 8 30a8 8 0 01-16 0"/><path d="M32 42a10 10 0 0020 0" opacity=".7"/><circle cx="40" cy="12" r="4"/><path d="M64 34h22M78 28l8 6-8 6" opacity=".8"/><path d="M96 26h14M96 34h10M96 42h14" opacity=".6"/></svg>'},
  {t:'Каскад продукта', pts:[
    'Каждая покупка <b>рождает новую потребность</b> — встраивайся на каждом этапе',
    'Купил камеру → нужен свет → нужен монтаж → нужен продвиженец',
    'Продавай не один продукт, а <b>лестницу решений</b> по пути клиента',
    'Клиент, прошедший каскад, приносит в разы больше, чем разовый'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 58h20V44h20V30h20V16h22"/><circle cx="28" cy="52" r="3" fill="currentColor" stroke="none"/><circle cx="48" cy="38" r="3" fill="currentColor" stroke="none"/><circle cx="68" cy="24" r="3" fill="currentColor" stroke="none"/><path d="M90 10l3 6 6 1-4.5 4 1 6-5.5-3-5.5 3 1-6-4.5-4 6-1z" fill="currentColor" stroke="none" opacity=".9"/></svg>'},
  {t:'Начни с востребованного', pts:[
    'Не изобретай спрос — заходи с продуктом, который <b>уже покупают</b>',
    'Первая позиция в каскаде должна быть «горячей», чтобы завести поток',
    'Экзотику и «уникальное» подтягивай вторым-третьим шагом, на разогретой базе',
    'Востребованный вход = <b>дешёвый трафик</b> и быстрая проверка гипотез'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 54c-8-6-20-14-20-26a12 12 0 0120-6 12 12 0 0120 6c0 12-12 20-20 26z"/><path d="M84 18c3-2 8-2 10 2M88 12c2-4 8-4 10 0" opacity=".6"/><path d="M28 30c-3 0-7 2-7 7M24 22c-4 0-8 3-8 8" opacity=".6"/></svg>'},
  {t:'Проверь спрос через Wordstat', pts:[
    '<b>Wordstat</b> показывает, сколько людей в месяц ищут твою тему в Яндексе',
    'Тысячи запросов = спрос есть; десятки = рынка пока нет',
    'Смотри сезонность и смежные запросы — там подсказки для каскада',
    'Проверка спроса <b>до вложений</b> экономит месяцы работы вхолостую'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="42" cy="30" r="16"/><path d="M54 42l14 14"/><path d="M34 30h16M42 22v16" opacity=".7"/><path d="M78 58V40M90 58V30M102 58V46" stroke-width="3"/></svg>'},
  {t:'Критерии успеха продукта', pts:[
    'Продукт работает, если <b>растёт статистика И растёт доход</b> — оба сразу',
    'Лайки без денег — тщеславие; деньги без охватов — потолок',
    'Мнения людей <b>не из твоей ЦА</b> — в игнор, они искажают картину',
    'Считай не эмоции, а <b>цифры за период</b>: до / после каждой гипотезы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M16 58h92" opacity=".5"/><path d="M22 50l18-16 14 10 30-28"/><path d="M84 16h10v10" stroke-linejoin="round"/><rect x="20" y="46" width="8" height="12" fill="currentColor" fill-opacity=".2" stroke="none"/><rect x="52" y="40" width="8" height="18" fill="currentColor" fill-opacity=".2" stroke="none"/></svg>'},
  {t:'Как выстроить свою монетизацию', pts:[
    'Шаг 1 — определи главную боль ЦА и <b>горячий вход</b> под неё',
    'Шаг 2 — дай «немного пользы» бесплатно, чтобы клиент увидел результат',
    'Шаг 3 — выстрой <b>каскад</b>: что человек захочет сразу после покупки',
    'Шаг 4 — держи руку на пульсе: статистика + доход, гипотеза за гипотезой'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="20" y="12" width="16" height="16" rx="4"/><rect x="52" y="12" width="16" height="16" rx="4"/><rect x="84" y="12" width="16" height="16" rx="4"/><path d="M36 20h16M68 20h16"/><rect x="52" y="44" width="16" height="16" rx="4"/><path d="M60 28v16" opacity=".7"/><path d="M56 50l3 3 6-6" stroke-width="2.8"/></svg>'},
  ],
  quiz:[
  {q:'Главная ошибка новичка в монетизации по уроку:', o:['Слишком высокая цена','Вываливает максимум пользы бесплатно','Мало рекламы','Нет логотипа'], a:1},
  {q:'Что должно решать бесплатное по принципу «немного пользы»?', o:['Всю задачу целиком','Маленькую задачу, как тест-драйв','Ничего, оно для галочки','Задачу конкурента'], a:1},
  {q:'«Купил камеру → нужен свет → нужен монтаж» — это пример…', o:['Прямой конкуренции','Каскада продукта','Демпинга','Сторителлинга'], a:1},
  {q:'С какого продукта советуют начинать каскад?', o:['С самого уникального','С самого дорогого','С уже востребованного','С бесплатного навсегда'], a:2},
  {q:'Каким сервисом проверяют спрос на тему?', o:['Wordstat','Photoshop','Suno','Kling'], a:0},
  {q:'Продукт считается успешным, когда…', o:['Много лайков','Растёт и статистика, и доход','Хвалят друзья','Низкая себестоимость'], a:1},
  ],
  pairs:[
  ['Немного пользы','Бесплатно — маленький результат, за большим платят'],
  ['Каскад продукта','Покупка рождает следующую потребность'],
  ['Горячий вход','Начинать с уже востребованного продукта'],
  ['Wordstat','Проверка спроса по числу запросов'],
  ['Критерий успеха','Рост статистики плюс рост дохода'],
  ],
  task:{
    intro:'Собери свою мини-воронку монетизации по шагам урока — от бесплатного крючка до каскада:',
    chips:['Горячий вход','Что даю бесплатно','Первый платный шаг','Следующий в каскаде','Как проверю спрос'],
    ph:'Пример: вход — монтаж Reels (востребовано). Бесплатно — разбор одного ролика клиента. Платный шаг — монтаж серии из 7 Reels. Каскад — ведение аккаунта → таргет. Спрос проверю по Wordstat: «монтаж reels» и смежные.',
    verdict:'Воронка логичная: бесплатное решает малую задачу, каскад ведёт клиента дальше. Совет куратора: замерь конверсию из бесплатного разбора в платный монтаж — это твоя ключевая цифра, по ней и оптимизируй оффер.'
  }
},

/* ---------- УРОК 6: Продвижение и алгоритмы ---------- */
{
  title:'Продвижение и алгоритмы',
  sub:'4:05 · масса · чаты активности · закуп трафика',
  dur:'4:05',
  c1:'ПРОДВИЖЕНИЕ', c2:'И АЛГОРИТМЫ',
  slides:[
  {t:'Масса задавливает алгоритмы', pts:[
    'Главный рычаг залёта — <b>объём</b>: 10-15 роликов в день, а не один «идеальный»',
    'Алгоритм тестирует контент на маленькой аудитории — дай ему много попыток',
    'Один ролик — лотерея; поток роликов — <b>система</b>, где что-то обязательно выстрелит',
    'Количество на дистанции рождает качество: набиваешь руку и попадаешь в тренд'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="14" y="12" width="18" height="26" rx="3"/><rect x="26" y="20" width="18" height="26" rx="3"/><rect x="38" y="28" width="18" height="26" rx="3"/><path d="M64 40h20M76 32l10 8-10 8" opacity=".8"/><path d="M94 24l3 6 6 1-4.5 4 1 6-5.5-3-5.5 3 1-6-4.5-4 6-1z" fill="currentColor" stroke="none" opacity=".9"/></svg>'},
  {t:'Не гонись за «одним шедевром»', pts:[
    'Перфекционизм убивает поток: пока полируешь один ролик, конкурент выложил десять',
    'Готово и опубликовано <b>лучше идеального в черновиках</b>',
    'Каждый ролик — это гипотеза: выложил, замерил, усилил то, что зашло',
    'Скорость и регулярность алгоритм ценит <b>выше вылизанности</b>'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="46" cy="34" r="22"/><circle cx="46" cy="34" r="14" opacity=".6"/><circle cx="46" cy="34" r="6" fill="currentColor" stroke="none"/><path d="M78 20l8-8M84 12h-6v6" opacity=".7"/><path d="M86 40l-6 6M80 46h6v-6" opacity=".5"/></svg>'},
  {t:'Чаты активности: как надо', pts:[
    'Чат активности реально раскачивает блог — но <b>только осмысленными действиями</b>',
    'Развёрнутые комментарии, сохранения, дочитывания — сигнал «контент живой»',
    'Такая активность <b>похожа на органику</b>, и алгоритм ей доверяет',
    'Договорись с 10-20 такими же авторами и обменивайтесь <b>настоящей</b> реакцией'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 18h44a6 6 0 016 6v14a6 6 0 01-6 6H40l-12 10V44h-8a6 6 0 01-6-6V24a6 6 0 016-6z"/><path d="M28 28h28M28 36h18" opacity=".8"/><circle cx="92" cy="30" r="10"/><path d="M88 30l3 3 6-6" stroke-width="2.8"/></svg>'},
  {t:'Чаты активности: как НЕ надо', pts:[
    '«Огонёчки» и пустые «класс!» — это <b>засорение</b>, а не продвижение',
    'Массовая накрутка одинаковых реакций читается алгоритмом как <b>спам</b>',
    'Пустая активность роняет средние показатели: досмотр падает — охват режется',
    'Лучше 10 живых комментариев, чем 200 эмодзи от «мёртвых» аккаунтов'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="42" cy="34" r="22"/><path d="M30 22l24 24M54 22L30 46"/><path d="M78 18c0 5-4 5-4 10M90 18c0 5-4 5-4 10M84 46v6" opacity=".6"/></svg>'},
  {t:'Об алгоритмах — решай практикой', pts:[
    'Мнений об алгоритмах тысячи, <b>проверенных — единицы</b>: не верь на слово',
    'Спорные вопросы (новый аккаунт vs переучивание старого) решаются <b>тестом</b>',
    'Запусти два подхода параллельно и сравни цифры за 2-4 недели',
    'Твоя ниша уникальна — <b>твоя статистика</b> важнее любого гуру'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M60 14v10M60 24l-22 8v14c0 0 8 8 22 8s22-8 22-8V32z"/><path d="M38 32l22 8 22-8" opacity=".7"/><path d="M60 40v14" opacity=".5"/><circle cx="60" cy="12" r="4"/></svg>'},
  {t:'Трафик в Telegram: игра вдолгую', pts:[
    'Закуп рекламы в Telegram — <b>игра «в перспективу»</b>: окупается на объёме и дистанции',
    'Один разовый пост редко «отбивается» — считай серию закупов как систему',
    'Бери <b>объёмом каналов</b>, а не одним крупным: диверсифицируй риск',
    'Дешёвая подписка сегодня → прибыль через каскад продукта завтра'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M18 42l84-26-14 44-20-14-10 12-4-16z"/><path d="M40 42l28-18-40 12" opacity=".6"/><path d="M54 50l0 0" opacity=".4"/></svg>'},
  {t:'Куда закупать рекламу', pts:[
    'Реклама только в <b>целевые и смежные</b> по аудитории каналы — иначе слив бюджета',
    'Совпадение аудитории важнее размера канала: 5к «своих» бьют 50к «чужих»',
    'Проверяй канал: живые комментарии, ровный охват, отсутствие накрутки',
    'Смежная ниша (не прямой конкурент) даёт <b>тёплую</b> аудиторию по пути клиента'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="46" cy="35" r="20"/><circle cx="74" cy="35" r="20"/><path d="M46 35h28" opacity=".3"/><path d="M56 35a10 14 0 0018 0 10 14 0 00-18 0" fill="currentColor" fill-opacity=".18" stroke="none"/><circle cx="60" cy="35" r="3" fill="currentColor" stroke="none"/></svg>'},
  {t:'Система продвижения — итог', pts:[
    '<b>Масса</b>: держи поток 10-15 роликов/день, дай алгоритму много попыток',
    '<b>Осмысленная активность</b>: живые комментарии, никаких «огонёчков»',
    '<b>Практика вместо мифов</b>: спорное — тестируй на своих цифрах',
    '<b>Умный трафик</b>: закуп вдолгую, объёмом, только в целевые каналы'],
   svg:'<svg viewBox="0 0 120 70" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><rect x="26" y="8" width="68" height="56" rx="8"/><path d="M36 20l4 4 7-8M36 36l4 4 7-8M36 52l4 4 7-8" stroke-width="2.8"/><path d="M54 21h28M54 37h28M54 53h20"/></svg>'},
  ],
  quiz:[
  {q:'Что по уроку сильнее всего «задавливает» алгоритмы?', o:['Один идеальный ролик','Масса — 10-15 роликов в день','Платная накрутка лайков','Красивый логотип'], a:1},
  {q:'Какая активность в чатах реально продвигает блог?', o:['Пустые «огонёчки»','Одинаковые эмодзи пачкой','Осмысленные комментарии и сохранения','Массовые репосты ботами'], a:2},
  {q:'Почему «огонёчки» вредят?', o:['Их не видит алгоритм','Это засорение, читается как спам','Они платные','Их мало'], a:1},
  {q:'Как правильно решать спорные вопросы об алгоритмах?', o:['Верить популярному блогеру','Тестировать на своих цифрах','Игнорировать статистику','Копировать конкурента'], a:1},
  {q:'Закуп трафика в Telegram — это…', o:['Разовая быстрая окупаемость','Игра вдолгую, окупается на объёме','Бесплатный способ','Только для крупных каналов'], a:1},
  {q:'В какие каналы стоит закупать рекламу?', o:['В самые большие любые','В целевые и смежные по аудитории','В случайные для охвата','Только к прямым конкурентам'], a:1},
  ],
  pairs:[
  ['Масса','10-15 роликов в день дают залёт'],
  ['Осмысленный комментарий','Живой сигнал, которому алгоритм доверяет'],
  ['Огонёчки','Засорение и накрутка, вредит охвату'],
  ['Практика','Спорное решается тестом на своих цифрах'],
  ['Смежный канал','Тёплая аудитория по пути клиента'],
  ],
  task:{
    intro:'Собери свой план продвижения на неделю по принципам урока — конкретными цифрами и действиями:',
    chips:['Сколько роликов/день','Чат активности (кто, что делаем)','Что тестирую','Каналы для закупа','Как замерю результат'],
    ph:'Пример: 12 Reels/день. Чат активности — 15 авторов из смежной ниши, развёрнутые комментарии первые 2 часа после выхода. Тестирую два хука на одинаковую тему. Закуп — 5 целевых каналов по 5-15к. Замер — досмотр и подписки за неделю.',
    verdict:'План рабочий: есть масса, живая активность и понятный замер. Совет куратора: заведи простую таблицу «ролик — досмотр — подписки» и раз в неделю усиливай форматы, которые зашли, а провалившиеся не переделывай, а меняй полностью.'
  }
}

];
/* ================= НАПРАВЛЕНИЯ → БЛОКИ → УРОКИ =================
   Масштаб (утв. Даниэлем): 3 направления, в каждом 5 блоков, в блоке ~9 уроков (~135).
   Существующие уроки — сиды блоков; остальное дозаполняется пачками (файл academy/OKO_ACADEMY_BLOCK_MAP.md).
   AC_PACK_* — массивы доп.уроков блока (наполняются волнами производства). */
const AC_PACK = (typeof window!=='undefined' && window.AC_PACK) ? window.AC_PACK : {};
function acPack(id){ return Array.isArray(AC_PACK[id]) ? AC_PACK[id] : []; }

/* блоки: сид-уроки (уже готовые объекты) + пак дозаполнения по id блока */
const AC_DIRS = [
  { id:'media', title:'Медийность', tag:'Направление · база', free:true, minTier:'PRO', price:0,
    author:'OKO · по методологии ХЛБ', c1:'КУРС', c2:'МЕДИЙНОСТЬ',
    outcomes:[
      'Упаковать профиль так, что за 3 секунды ясно — кто ты и зачем',
      'Собрать визуал и ленту, которым доверяют (51% судят по картинке)',
      'Понимать типы контента и эмоций — и попадать в свою аудиторию',
      'Вести аккаунт как полигон уже сейчас, а не «когда-нибудь»'],
    blocks:[
      {id:'m1', title:'Мышление и старт',           seed:[MEDIA_LESSONS[0]]},
      {id:'m2', title:'Упаковка профиля',           seed:[MEDIA_LESSONS[1]]},
      {id:'m3', title:'Визуал и лента',             seed:[MEDIA_LESSONS[2], AC_COURSE_REELS[1]]},
      {id:'m4', title:'Контент и эмоции',           seed:[MEDIA_LESSONS2[0], AC_COURSE_REELS[0]]},
      {id:'m5', title:'Сторис, эфиры, характер',    seed:[MEDIA_LESSONS2[1], MEDIA_LESSONS2[2]]},
    ]},
  { id:'marketing', title:'Маркетинг', tag:'Направление · продажи', free:false, minTier:'PRO', price:2900,
    author:'OKO · по методологии ХЛБ', c1:'КУРС', c2:'МАРКЕТИНГ',
    outcomes:[
      'Писать тексты, которые читают и покупают',
      'Собирать продающий пост по структуре из 7 блоков',
      'Ловить внимание хуком и усиливать 8 триггерами',
      'Превращать подписчика в клиента текстом'],
    blocks:[
      {id:'k1', title:'Копирайтинг',                        seed:[MARKETING_LESSONS[0]]},
      {id:'k2', title:'Продающий пост и хуки',              seed:[MARKETING_LESSONS[1], MARKETING_LESSONS[2]]},
      {id:'k3', title:'Триггеры (психология покупки)',      seed:[]},
      {id:'k4', title:'Стратегия: конкуренты и монетизация',seed:[MARKETING_LESSONS2[0], MARKETING_LESSONS2[1]]},
      {id:'k5', title:'Продвижение, продажи, деньги',       seed:[MARKETING_LESSONS2[2], AC_COURSE_REELS[2]]},
    ]},
  { id:'ai', title:'Нейросети', tag:'Направление · практика', free:false, minTier:'PRO', price:2900,
    author:'Команда OKO', c1:'КУРС', c2:'НЕЙРОСЕТИ',
    outcomes:[
      'Выбирать нейросеть под задачу за секунды по карте 2026',
      'Собирать сильные промпты по формуле: роль, задача, контекст, формат',
      'Генерировать картинки и видео уровня продакшн — дёшево',
      'Запустить своего ИИ-агента для бизнеса в Telegram'],
    blocks:[
      {id:'a1', title:'Старт и промптинг',        seed:[AC_COURSE_AI[0], AC_COURSE_AI[1]]},
      {id:'a2', title:'Контент нейросетями',      seed:[AC_COURSE_AI[2], AC_COURSE_AI[3]]},
      {id:'a3', title:'Вайбкодинг',               seed:[]},
      {id:'a4', title:'Автоматизация',            seed:[]},
      {id:'a5', title:'Бизнес и деньги на ИИ',    seed:[AC_COURSE_AI[4]]},
    ]},
];

/* ---- вычисляем плоский AC_COURSE, метаданные курсов AC_COURSES и границы блоков AC_BLOCKS ---- */
const AC_COURSE = [];
const AC_COURSES = [];
const AC_BLOCKS = [];
AC_DIRS.forEach(d=>{
  const from = AC_COURSE.length;
  d.blocks.forEach(b=>{
    const lessons = (b.seed||[]).concat(acPack(b.id));
    const bf = AC_COURSE.length;
    lessons.forEach(L=>AC_COURSE.push(L));
    AC_BLOCKS.push({dir:d.id, id:b.id, title:b.title, from:bf, count:lessons.length});
  });
  const count = AC_COURSE.length - from;
  const nb = d.blocks.length;
  AC_COURSES.push({
    id:d.id, title:d.title, tag:d.tag, free:d.free, minTier:d.minTier, price:d.price,
    author:d.author, outcomes:d.outcomes, c1:d.c1, c2:d.c2, from, count,
    blocks:d.blocks.map(b=>b.id),
    sub:`${nb} ${acPlural(nb,['блок','блока','блоков'])} · ${count} ${acPlural(count,['урок','урока','уроков'])} · сертификат за направление`,
  });
});
/* Публикуем данные Академии наружу: AC_COURSE/AC_COURSES/AC_BLOCKS объявлены
   через const и в window сами не попадают, а слою полировки (oko-academy2.js)
   нужен доступ для поиска по урокам, оглавления и офлайн-конспектов. */
try{ window.AC_COURSE = AC_COURSE; window.AC_COURSES = AC_COURSES; window.AC_BLOCKS = AC_BLOCKS; }catch(e){}
/* блоки конкретного курса */
function acCourseBlocks(ci){ const id=AC_COURSES[ci].id; return AC_BLOCKS.filter(b=>b.dir===id); }
const AC_FEE = 0.10;   // комиссия платформы OKO с продажи курса (как в каналах)

/* индекс курса по глобальному номеру урока */
function acCourseOf(i){
  for(let c=0;c<AC_COURSES.length;c++){
    const m=AC_COURSES[c];
    if(i>=m.from && i<m.from+m.count) return c;
  }
  return 0;
}
function acLocalNo(i){ return i - AC_COURSES[acCourseOf(i)].from + 1; }        // номер урока внутри курса (с 1)
function acCourseLen(ci){ return AC_COURSES[ci].count; }
function acCourseFirst(ci){ return AC_COURSES[ci].from; }
function acCourseIdx(ci){ const m=AC_COURSES[ci], a=[]; for(let i=0;i<m.count;i++) a.push(m.from+i); return a; }
/* доступ к курсу: бесплатный / куплен / открыт подпиской */
function acOwnsCourse(ci){ try{ return !!(acS.owned && acS.owned[AC_COURSES[ci].id]); }catch(e){ return false; } }
function acCourseAccessible(ci){
  const m=AC_COURSES[ci];
  if(m.free) return true;
  if(acOwnsCourse(ci)) return true;
  return (typeof okoHasSub==='function') ? okoHasSub(m.minTier) : false;
}
function acCoursePctOf(ci){
  const idx=acCourseIdx(ci); if(!idx.length) return 0;
  let s=0; idx.forEach(i=>s+=acLessonPct(i));
  return Math.round(s/idx.length);
}
function acCourseDone(ci){ return acCourseIdx(ci).every(i=>acLessonDone(i)); }
function acCourseCertCount(ci){ return acDirCert(ci) ? 1 : 0; }

/* агрегаты по курсу: сколько всего слайдов, вопросов, игр, видео, минут */
function acCourseStats(ci){
  const idx = acCourseIdx(ci);
  let slides=0, quiz=0, games=0, vids=0, mins=0;
  idx.forEach(i=>{
    const L = AC_COURSE[i];
    slides += L.slides.length;
    quiz   += L.quiz.length;
    if(L.pairs && L.pairs.length) games++;
    if(L.videoUrl) vids++;
    const m = /^(\d+):(\d+)$/.exec(L.dur||'');
    if(m) mins += (+m[1]) + (+m[2])/60;
  });
  return {lessons:idx.length, slides, quiz, games, vids, mins:Math.max(1,Math.round(mins))};
}

/* ---------- состояние (localStorage oko-academy, per-урок) ---------- */
function acNewLS(){
  return {video:false, slides:false, test:false, testScore:0, task:false, taskText:'',
          game:false, gameWrong:null, slideMax:0, cert:null};
}
function acLoadState(){
  let s = null;
  try{ s = JSON.parse(localStorage.getItem('oko-academy')); }catch(e){}
  if(!s) return {lessons:{}, certs:[]};
  if(s.lessons){ if(!Array.isArray(s.certs)) s.certs = []; return s; }
  /* миграция старого одноурочного формата → урок 0 */
  const l0 = Object.assign(acNewLS(), {
    video:!!s.video, slides:!!s.slides, test:!!s.test, testScore:s.testScore||0,
    task:!!s.task, taskText:s.taskText||'', game:!!s.game,
    gameWrong:(s.gameWrong===undefined?null:s.gameWrong), slideMax:s.slideMax||0,
    cert:s.cert||null
  });
  const fix = c => c ? Object.assign({lesson:0, lessonTitle:AC_COURSE[0].title}, c) : c;
  l0.cert = fix(l0.cert);
  const certs = (s.certs||[]).map(fix);
  return {lessons:{0:l0}, certs};
}
const acS = acLoadState();
function acSave(){ try{ localStorage.setItem('oko-academy', JSON.stringify(acS)); }catch(e){} }
acSave(); // сразу персистим мигрированный формат
/* бэкфилл «освоено»: уже стопроцентные уроки не празднуем задним числом (иначе вау-оверлей
   всплыл бы при повторном прохождении теста/игры на давно пройденном уроке) */
try{ Object.keys(acS.lessons).forEach(k=>{ const ls=acS.lessons[k]; if(ls && !ls.mastered && acItems(+k).every(x=>x[1])) ls.mastered=true; }); acSave(); }catch(e){}

/* ---------- стрик: дни подряд в учёбе ---------- */
function acDayStr(d){
  const x = d || new Date();
  return x.getFullYear() + '-' + String(x.getMonth()+1).padStart(2,'0') + '-' + String(x.getDate()).padStart(2,'0');
}
function acStreak(){
  if(!acS.streak) acS.streak = {last:'', days:0, best:0};
  return acS.streak;
}
function acStreakTouch(){
  const st = acStreak(), today = acDayStr();
  if(st.last === today) return st.days;
  const y = new Date(); y.setDate(y.getDate()-1);
  st.days = (st.last === acDayStr(y)) ? st.days + 1 : 1;
  st.last = today;
  if(st.days > (st.best||0)) st.best = st.days;
  acSave();
  return st.days;
}

/* ---------- следующий урок (первый открытый и не завершённый) ---------- */
function acNextLesson(){
  for(let i=0; i<AC_COURSE.length; i++)
    if(acUnlocked(i) && !acLessonDone(i)) return i;
  return -1;
}
/* глобальный счётчик пройденных уроков — для бейджей уровня */
function acLessonsDoneCount(){
  let n = 0;
  for(let i=0; i<AC_COURSE.length; i++) if(acLessonDone(i)) n++;
  return n;
}
/* последний открытый урок — «Продолжи с этого» на главной */
function acResumeLesson(){
  const last = acS.lastLesson;
  if(typeof last === 'number' && last >= 0 && last < AC_COURSE.length
     && acUnlocked(last) && !acLessonDone(last)) return last;
  return acNextLesson();
}
/* рекомендация следующего курса после текущего */
function acRecommendNext(ci){
  for(let j=ci+1; j<AC_COURSES.length; j++) if(!acCourseDone(j)) return j;
  for(let j=0; j<ci; j++) if(!acCourseDone(j)) return j;
  return -1;
}

let acView = 'home';            // 'home' (каталог) | 'course' (уроки курса) | 'lesson'
let acL = 0;                    // глобальный индекс текущего урока
let acCourse = 0;               // индекс текущего курса (для страницы курса)
let acQuiz = null;              // сессия теста (не персистится)
let acG = null;                 // сессия мини-игры
let acTaskChecking = false;     // «Проверяется куратором»
let acCertUrl = null;           // кэш PNG сертификата
let acCertShownNo = null;
let acBadgeNewIds = [];         // бейджи, открытые в этом ре-рендере (для pop-анимации)

function acCur(){ return AC_COURSE[acL]; }
function acLS(i){
  const k = (i===undefined ? acL : i);
  if(!acS.lessons[k]) acS.lessons[k] = acNewLS();
  return acS.lessons[k];
}

/* ---------- прогресс ---------- */
/* Видео-уроки временно отключены (делаем позже) — уроки текстовые: слайды/конспект/тест/практика/игра.
   videoUrl в данных сохранён, вернём включением AC_VIDEO_ON. */
const AC_VIDEO_ON = false;
function acHasVideo(L){ return AC_VIDEO_ON && L && !!L.videoUrl; }
/* Подпись урока: sub из данных, но при выключенном видео чистим упоминания видео
   и гарантируем «слайды + тест + игра» — от того, что реально есть в уроке. */
function acLessonSub(L){
  if(AC_VIDEO_ON) return String((L && L.sub) || '');
  const parts = [];
  const nS = (L && L.slides || []).length;
  const nQ = (L && L.quiz   || []).length;
  const nP = (L && L.pairs  || []).length;
  if(nS) parts.push(nS + ' ' + acPlural(nS, ['слайд','слайда','слайдов']));
  if(nQ) parts.push('тест из ' + nQ);
  if(nP || nS>=3 || nQ>=3) parts.push('мини-игра');
  return parts.join(' · ');
}
function acItems(i){
  const k = (i===undefined?acL:i);
  const ls = acS.lessons[k] || {};
  const L = AC_COURSE[k] || {};
  const items = [];
  if(acHasVideo(L)) items.push(['Видео просмотрено', !!ls.video]);
  items.push(['Слайды пролистаны', !!ls.slides]);
  items.push(['Тест сдан на '+AC_PASS+'%+', !!ls.test]);
  items.push(['Практика зачтена', !!ls.task]);
  items.push(['Мини-игра пройдена', !!ls.game]);
  return items;
}
function acLessonPct(i){ const it = acItems(i); return it.length ? Math.round(it.filter(x=>x[1]).length / it.length * 100) : 0; }
function acCoursePct(){
  let sum = 0;
  for(let i=0; i<AC_COURSE.length; i++) sum += acLessonPct(i);
  return Math.round(sum / AC_COURSE.length);
}
function acLessonDone(i){ return acLessonPct(i) === 100; }
function acUnlocked(i){
  const ci = acCourseOf(i);
  return acCourseAccessible(ci);   // уроки ПРОПУСКАЕМЫЕ — внутри доступного направления открыты все, без жёстких ограничений
}
/* Сертификат — ЗА ВСЁ НАПРАВЛЕНИЕ (не за урок). Один именной диплом на направление. */
function acDirCert(ci){ try{ const id=AC_COURSES[ci].id; return (acS.certs||[]).find(c=>c && c.dir===id) || null; }catch(e){ return null; } }
function acCertEligible(){ const ci=acCourseOf(acL); return acCourseDone(ci) && !acDirCert(ci); }

/* ================= ДОСТУП К ПРЕМИУМ-КУРСУ (подписка / покупка) ================= */
function acFmtPrice(n){ return (typeof fmtRub==='function') ? fmtRub(n) : (n.toLocaleString('ru-RU')+' ₽'); }
/* открыть курс: если доступен — на страницу курса, иначе гейт */
function acOpenCourse(ci){
  if(!acCourseAccessible(ci)){ acCourseGate(ci); return; }
  acCourse = ci; acView = 'course'; acQuiz = null; acG = null; acTaskChecking = false;
  acRender();
  const m = document.querySelector('main'); if(m) m.scrollTop = 0;
}
/* продающий гейт премиум-курса: подписка ИЛИ разовая покупка */
function acCourseGate(ci){
  const c = AC_COURSES[ci];
  if(acCourseAccessible(ci)){ acOpenCourse(ci); return; }
  if(typeof showPopup !== 'function'){ toast('Курс «'+c.title+'» доступен по подписке '+c.minTier); return; }
  const fee = Math.round(c.price*AC_FEE), net = c.price - fee;
  showPopup({ico:'star', title:'Курс «'+c.title+'»',
    body:'Премиум-курс Академии OKO: <b>'+c.count+' '+acPlural(c.count,['урок','урока','уроков'])+'</b>, тесты, практика и именной сертификат за всё направление. Доступ навсегда.<br><br>Входит в подписку <b style="color:var(--accent)">'+c.minTier+'</b> и выше — либо разовая покупка за <b style="color:var(--accent)">'+acFmtPrice(c.price)+'</b>.<br><span style="font-size:11.5px;color:var(--dim);line-height:1.5">Автор курса получает '+acFmtPrice(net)+', комиссия платформы OKO — 10% ('+acFmtPrice(fee)+').</span>',
    actions:[
      {label:'Открыть по подписке '+c.minTier, onclick:()=>{
        if(typeof okoRequireSub === 'function'){
          okoRequireSub(c.minTier, 'Курс «'+c.title+'» входит в подписку '+c.minTier+'. Оформи её — и получишь этот курс и десятки других возможностей.', ()=>acOpenCourse(ci));
        } else { toast('Подписка временно недоступна'); }
      }},
      {label:'Купить за '+acFmtPrice(c.price), onclick:()=>acBuyCourse(ci)},
      {label:'Позже', ghost:true}
    ]});
}
/* разовая покупка курса за баланс (комиссия — доход владельца) */
function acBuyCourse(ci){
  const c = AC_COURSES[ci];
  if(acCourseAccessible(ci)){ acOpenCourse(ci); return; }
  if(typeof walletCharge !== 'function'){ toast('Кошелёк недоступен'); return; }
  if(walletCharge(c.price, 'Академия · курс «'+c.title+'»')){
    acS.owned = acS.owned || {};
    acS.owned[c.id] = true; acSave();
    const fee = Math.round(c.price*AC_FEE), net = c.price - fee;
    if(typeof okoEarn === 'function') okoEarn(c.price, 'Академия · продажа курса «'+c.title+'»');
    acBadgeSync();
    if(typeof showPopup === 'function'){
      showPopup({ico:'check2', title:'Курс открыт',
        body:'Курс «'+esc(c.title)+'» теперь твой навсегда. Списано <b>'+acFmtPrice(c.price)+'</b>: автору начислено '+acFmtPrice(net)+', комиссия платформы OKO 10% — '+acFmtPrice(fee)+'.',
        actions:[{label:'Начать курс', onclick:()=>acOpenCourse(ci)}]});
    } else { toast('Курс открыт: «'+c.title+'»'); acOpenCourse(ci); }
  } else {
    toast('Недостаточно средств — пополни счёт');
  }
}

/* ================= БЕЙДЖИ ДОСТИЖЕНИЙ ================= */
function acAnyLessonTest100(){ for(const k in acS.lessons){ if((acS.lessons[k].testScore||0) >= 100) return true; } return false; }
function acTasksDone(){ let n=0; for(const k in acS.lessons){ if(acS.lessons[k].task) n++; } return n; }
function acBadgeDefs(){
  const st = acStreak();
  const done = acLessonsDoneCount();
  return [
    {id:'first',  ic:'bolt',     t:'Первый шаг',     d:'Пройти первый урок',                on: AC_COURSE.some((_,i)=>acLessonPct(i)>=100) || acS.certs.length>0},
    {id:'l3',     ic:'circle-play',t:'Разгон',       d:'Пройти 3 урока',                    on: done >= 3},
    {id:'l10',    ic:'flag',     t:'Ученик',         d:'Пройти 10 уроков',                  on: done >= 10},
    {id:'l25',    ic:'poll',     t:'Знаток',         d:'Пройти 25 уроков',                  on: done >= 25},
    {id:'l50',    ic:'rocket',   t:'Профи',          d:'Пройти 50 уроков',                  on: done >= 50},
    {id:'l100',   ic:'crown',    t:'Магистр',        d:'Пройти 100 уроков',                 on: done >= 100},
    {id:'cert1',  ic:'star',     t:'Сертификат',     d:'Получить первый сертификат',        on: acS.certs.length >= 1},
    {id:'ace',    ic:'poll',     t:'Отличник',       d:'Сдать тест на 100%',                on: acAnyLessonTest100()},
    {id:'prac',   ic:'edit',     t:'Практик',        d:'Зачесть 3 практики',                on: acTasksDone() >= 3},
    {id:'streak3',ic:'fire',     t:'В огне',         d:'3 дня подряд в учёбе',              on: (st.best||0) >= 3},
    {id:'media',  ic:'rocket',   t:'Медийщик OKO',   d:'Завершить направление «Медийность»', on: acCourseDone(0)},
    {id:'invest', ic:'money',    t:'Инвестор в себя',d:'Открыть премиум-направление',       on: (typeof okoHasSub==='function'&&okoHasSub('PRO')) || (acS.owned && Object.keys(acS.owned).length>0)},
    {id:'mkt',    ic:'briefcase',t:'Маркетолог OKO', d:'Завершить направление «Маркетинг»',  on: AC_COURSES.length>1 && acCourseDone(1)},
    {id:'ai',     ic:'crown',    t:'Нейро-мастер OKO',d:'Завершить направление «Нейросети»', on: AC_COURSES.length>2 && acCourseDone(2)},
    {id:'coll',   ic:'verified', t:'Коллекционер',   d:'Собрать 5 сертификатов',            on: acS.certs.length >= 5},
  ];
}
/* при первом заходе — «засчитать» уже заслуженные молча, чтобы не спамить тостами */
function acBadgeSync(){
  const defs = acBadgeDefs();
  if(!Array.isArray(acS.badgeSeen)){
    acS.badgeSeen = defs.filter(b=>b.on).map(b=>b.id);
    acBadgeNewIds = []; acSave(); return;
  }
  const fresh = defs.filter(b=>b.on && acS.badgeSeen.indexOf(b.id) < 0);
  acBadgeNewIds = fresh.map(b=>b.id);
  if(fresh.length){
    fresh.forEach(b=>acS.badgeSeen.push(b.id));
    acSave();
    fresh.forEach((b,k)=>setTimeout(()=>toast('Награда открыта: «'+b.t+'»'), 260*(k+1)));
  }
}
function acBadgesGridHtml(){
  const defs = acBadgeDefs();
  const earned = defs.filter(b=>b.on).length;
  return `
    <div class="ac-badges-head">
      <span class="ico">${I('verified')}</span>
      <div class="meta"><b>Достижения</b><span>Открыто ${earned} из ${defs.length} — учись и собирай награды</span></div>
    </div>
    <div class="ac-badges">${defs.map((b,i)=>`
      <div class="ac-badge ${b.on?'on':''} ${acBadgeNewIds.indexOf(b.id)>=0?'new':''}" style="animation-delay:${(i*0.045).toFixed(2)}s" title="${b.d}">
        <span class="ic">${I(b.ic)}</span>
        <span class="t">${b.t}</span>
        <span class="d">${b.on?'получено':b.d}</span>
      </div>`).join('')}</div>`;
}

/* ================= РЕНДЕР ================= */
function acRender(){
  const root = document.getElementById('acRoot');
  if(!root) return;
  /* Зеркала состояния наружу. acView/acL/acCourse/acS объявлены через let/const
     и в window не попадают, а единая кнопка «назад» (oko-back.js) читает именно
     window.acView, чтобы шагать урок → курс → каталог. */
  try{ window.acView = acView; window.acL = acL; window.acCourse = acCourse; window.acS = acS; }catch(e){}
  acBadgeSync();  // до построения HTML — чтобы pop-анимация новых наград попала в разметку
  root.innerHTML = acView === 'lesson' ? acLessonHtml() : acView === 'course' ? acCourseHtml() : acHomeHtml();
  root.classList.remove('fade-in'); void root.offsetWidth; root.classList.add('fade-in');
  if(acView === 'lesson'){
    acRenderVideoBox(); acBindSlides(); acRenderTestBox();
    acRenderTaskBox(); acRenderGameBox(); acRenderProgressBox(); acRenderCertBox();
  } else {
    acAnimRings();
  }
}
/* сведение колец прогресса из «пустого» в цель — эффектный sweep при входе */
function acAnimRings(){
  requestAnimationFrame(()=>requestAnimationFrame(()=>{
    document.querySelectorAll('#acRoot .ac-ring .val, #acRoot .ac-mini-ring .val').forEach(c=>{
      const off = c.getAttribute('data-off');
      if(off !== null) c.style.strokeDashoffset = off;
    });
  }));
}

/* обложка курса (inline-SVG, самодостаточная, тёмная арт-панель в обеих темах).
   hero=true → без крупного текста (его дублирует заголовок-капшн под обложкой). */
function acCourseCover(ci, hero){
  const c = AC_COURSES[ci];
  const c2Len = (c.c2||'').length;
  const c2Size = c2Len > 8 ? Math.max(22, 36 - (c2Len-8)*2) : 36;
  const c2Space = c2Len > 8 ? 1 : 2;
  const bigText = hero ? '' : `
    <text x="26" y="118" font-family="'Bebas Neue',Impact,sans-serif" font-size="26" fill="#fff" letter-spacing="1">${esc(c.c1)}</text>
    <text x="26" y="152" font-family="'Bebas Neue',Impact,sans-serif" font-size="${c2Size}" fill="#9AFF00" letter-spacing="${c2Space}">${esc(c.c2)}</text>
    <text x="26" y="176" font-family="Montserrat,sans-serif" font-size="9" font-weight="600" fill="rgba(255,255,255,.5)" letter-spacing="3">АКАДЕМИЯ OKO</text>`;
  return `<svg class="ac-cover-svg" viewBox="0 0 320 190" preserveAspectRatio="xMidYMid slice" aria-hidden="true">
    <rect width="320" height="190" fill="#070a04"/>
    <g stroke="rgba(154,255,0,.10)" stroke-width="1">
      ${[40,80,120,160,200,240,280].map(x=>`<line x1="${x}" y1="0" x2="${x}" y2="190"/>`).join('')}
      ${[38,76,114,152].map(y=>`<line x1="0" y1="${y}" x2="320" y2="${y}"/>`).join('')}
    </g>
    <circle cx="266" cy="44" r="70" fill="rgba(154,255,0,.09)"/>
    <use href="#i-logo" x="238" y="16" width="64" height="64"/>${bigText}
  </svg>`;
}

/* карточка курса в каталоге */
function acCourseCardHtml(ci){
  const c = AC_COURSES[ci];
  const acc = acCourseAccessible(ci);
  const pct = acCoursePctOf(ci);
  const done = acCourseDone(ci);
  const CR = 2*Math.PI*15;
  const badge = c.free
    ? `<span class="ac-cc-tag free">${I('bolt')} Бесплатно</span>`
    : acc
      ? `<span class="ac-cc-tag open">${I('check2')} Открыт</span>`
      : `<span class="ac-cc-tag lock">${I('lock')} ${c.minTier}</span>`;
  const ring = acc
    ? `<span class="ac-mini-ring lg" title="Курс пройден на ${pct}%">
        <svg viewBox="0 0 36 36"><circle class="bg" cx="18" cy="18" r="15"/>
        <circle class="val" cx="18" cy="18" r="15" stroke-dasharray="${CR.toFixed(1)}" stroke-dashoffset="${CR.toFixed(1)}" data-off="${(CR*(1-pct/100)).toFixed(1)}"/></svg>
        <b>${done?I('check2'):pct}</b></span>`
    : `<span class="ac-cc-price">${acFmtPrice(c.price)}</span>`;
  const cta = acc
    ? (done ? `Открыть курс` : (pct>0 ? `Продолжить · ${pct}%` : `Начать курс`))
    : `Открыть доступ`;
  return `
    <div class="card ac-cc ${acc?'':'locked'}" style="animation-delay:${(ci*0.06).toFixed(2)}s" onclick="acCourseCardClick(${ci})">
      <div class="ac-cc-cover">${acCourseCover(ci)}${acc?'':`<span class="ac-cc-veil">${I('lock')}</span>`}</div>
      <div class="ac-cc-body">
        <div class="ac-cc-top">${badge}<span class="ac-cc-meta">${c.count} ${acPlural(c.count,['урок','урока','уроков'])}</span></div>
        <h3>${esc(c.title)}</h3>
        <p>${esc(c.sub)}</p>
        <div class="ac-cc-foot">${ring}<button class="btn sm ${acc?'':'ghost'} ac-cc-go" onclick="event.stopPropagation();acCourseCardClick(${ci})">${cta}</button></div>
      </div>
    </div>`;
}
function acCourseCardClick(ci){ acOpenCourse(ci); }

/* ---------- ГЛАВНАЯ АКАДЕМИИ: КАТАЛОГ КУРСОВ ---------- */
/* большая карточка «Продолжи с этого» — берёт последний открытый урок из localStorage,
   иначе первый доступный. Показывает превью урока и его прогресс. */
function acContinueCardHtml(){
  const last = acResumeLesson();
  if(last < 0){
    return `<div class="ac-next-row done">${I('check2')}<span>Доступные уроки пройдены, открой премиум-курс</span></div>`;
  }
  const L = AC_COURSE[last];
  const ci = acCourseOf(last);
  const c = AC_COURSES[ci];
  const p = acLessonPct(last);
  const started = p > 0;
  const label = started ? 'Продолжи с этого' : 'Начни отсюда';
  const cta = started ? 'Продолжить урок' : 'Открыть урок';
  return `
    <div class="card ac-continue" onclick="acOpenLesson(${last})">
      <div class="ac-continue-head">
        <span class="ac-continue-tag">${I('circle-play')} ${label}</span>
        <span class="ac-continue-meta">${esc(c.title)} · урок ${acLocalNo(last)}/${c.count}</span>
      </div>
      <h3 class="ac-continue-title">${esc(L.title)}</h3>
      <p class="ac-continue-sub">${esc(acLessonSub(L))}</p>
      <div class="ac-continue-bar" title="Прогресс урока ${p}%"><i style="width:${p}%"></i></div>
      <div class="ac-continue-foot">
        <span class="ac-continue-pct">${p}% пройдено</span>
        <button class="btn sm ac-continue-cta" onclick="event.stopPropagation();acOpenLesson(${last})">${cta}</button>
      </div>
    </div>`;
}
function acHomeHtml(){
  const st = acStreak();
  const hot = st.days >= 3;
  const streak = `
    <div class="card ac-streak ${hot?'hot':''}">
      <span class="ac-flame">${I('fire')}</span>
      <div class="meta"><b>Дней подряд в учёбе: ${st.days||0}</b>
        <span>${hot ? 'Серия в огне — так держать!' : 'Заходи каждый день, чтобы разжечь серию'}${st.best>1?` · рекорд: ${st.best}`:''}</span></div>
      ${hot?`<span class="ac-hot-chip">${I('fire')} ${st.days}</span>`:''}
    </div>`;
  const nextRow = acContinueCardHtml();
  const courses = AC_COURSES.map((_,ci)=>acCourseCardHtml(ci)).join('');
  const certs = acS.certs.length ? acS.certs.map((c,i)=>`
    <div class="ac-cert-item" style="animation-delay:${i*.05}s">
      <span class="ico"><svg class="i"><use href="#i-file"/></svg></span>
      <span class="meta"><span class="t">${esc(acCertLabel(c))}</span><span class="s" style="display:block">${esc(c.no)} · ${esc(c.date)} · тест ${c.score}%</span></span>
      <button class="btn sm ghost ac-ico-btn" onclick="acCertShare(${i})" title="Поделиться" aria-label="Поделиться">${I('share')}</button>
      <button class="btn sm ghost" onclick="acCertShow(${i})">Показать</button>
    </div>`).join('')
    : `<p class="dim" style="font-size:12.5px;line-height:1.55">Пройди направление целиком — получи официальный сертификат OKO с печатью и подписью. Он появится здесь.</p>`;
  return `
    <div class="ac-hero"><h2>Академия OKO</h2><p>Курсы полного формата · официальные сертификаты</p></div>
    ${streak}
    ${nextRow}
    <h2 class="section-h" style="margin:20px 0 10px;font-size:21px">Каталог курсов</h2>
    <div class="ac-catalog">${courses}</div>

    <h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Достижения</h2>
    <div class="card ac-badges-card">${acBadgesGridHtml()}</div>

    <h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Мои сертификаты</h2>
    <div class="card" style="padding:6px 14px">${certs}</div>
    <div style="height:14px"></div>`;
}

/* подпись сертификата: «Курс · Урок N · Название» */
function acCertLabel(c){
  return 'Направление «' + (c.courseTitle || 'Академия OKO') + '» · сертификат';
}

/* ---------- «ЧТО ВНУТРИ КУРСА»: состав + чему научишься ---------- */
function acCourseInsideHtml(ci){
  const c = AC_COURSES[ci], st = acCourseStats(ci);
  const feats = [
    ['file', st.lessons + ' ' + acPlural(st.lessons,['урок','урока','уроков']) + ' полного формата', 'анимированные слайды + конспект'],
    ['file',  st.slides + ' ' + acPlural(st.slides,['слайд','слайда','слайдов']), 'наглядно и по делу'],
    ['poll',  'Тесты · ' + st.quiz + ' ' + acPlural(st.quiz,['вопрос','вопроса','вопросов']), 'порог зачёта ' + AC_PASS + '%'],
    ['edit',  'Практика', 'проверка куратором'],
    ['bolt',  st.games + ' мини-' + acPlural(st.games,['игра','игры','игр']), 'закрепление на связках'],
    ['star',  'Сертификат', 'именной — за всё направление'],
  ];
  const outcomes = (c.outcomes||[]).map(o=>`<li>${I('check2')}<span>${esc(o)}</span></li>`).join('');
  return `
  <div class="card ac-inside" id="acWhatsIn">
    <div class="ac-inside-head">
      <span class="ico">${I('bolt')}</span>
      <div class="meta"><b>Что внутри курса</b><span>${c.author?('Автор — '+esc(c.author)+' · '):''}${st.lessons} ${acPlural(st.lessons,['урок','урока','уроков'])} полного формата</span></div>
    </div>
    <div class="ac-inside-grid">${feats.map(f=>`
      <div class="ac-inside-cell"><span class="ic">${I(f[0])}</span><b>${f[1]}</b><span>${f[2]}</span></div>`).join('')}</div>
    ${outcomes?`<div class="ac-inside-out"><div class="h">${I('flag')} Чему научишься</div><ul>${outcomes}</ul></div>`:''}
  </div>`;
}

/* ---------- СТРАНИЦА КУРСА (список уроков) ---------- */
function acCourseHtml(){
  const ci = acCourse, c = AC_COURSES[ci];
  const pct = acCoursePctOf(ci), C = 2*Math.PI*33, CR = 2*Math.PI*15;
  const idx = acCourseIdx(ci);
  const done = acCourseDone(ci);
  const doneCnt = idx.filter(i=>acLessonDone(i)).length;  // «34/45 уроков»
  /* строка урока (номер — сквозной внутри направления) */
  const lessonRow = (i,k)=>{
    const local = acLocalNo(i);
    if(!acUnlocked(i)) return `<button class="ac-lesson-row locked ac-rise" style="animation-delay:${(k*0.04).toFixed(2)}s" onclick="acCourseGate(${ci})">
      <span class="ac-num">${local}</span>
      <span class="meta"><span class="t">${esc(AC_COURSE[i].title)}</span><span class="s" style="display:block">${esc(acLessonSub(AC_COURSE[i]))}</span></span>
      <svg class="i"><use href="#i-lock"/></svg></button>`;
    const p = acLessonPct(i);
    const stg = (p > 0 ? `<span class="ac-mini-ring" title="Урок пройден на ${p}%">
          <svg viewBox="0 0 36 36"><circle class="bg" cx="18" cy="18" r="15"/>
          <circle class="val" cx="18" cy="18" r="15" stroke-dasharray="${CR.toFixed(1)}" stroke-dashoffset="${CR.toFixed(1)}" data-off="${(CR*(1-p/100)).toFixed(1)}"/></svg>
          <b>${p}</b></span>` : '');
    return `<button class="ac-lesson-row ac-rise" style="animation-delay:${(k*0.04).toFixed(2)}s" onclick="acOpenLesson(${i})">
      <span class="ac-num">${local}</span>
      <span class="meta"><span class="t">${esc(AC_COURSE[i].title)}</span><span class="s" style="display:block">${esc(acLessonSub(AC_COURSE[i]))}</span></span>
      ${stg}
      <svg class="i go"><use href="#i-chev"/></svg></button>`;
  };
  /* группировка по блокам направления */
  const blocks = acCourseBlocks(ci);
  let rk = 0;
  const rows = blocks.map((b, bi)=>{
    const bIdx = []; for(let i=b.from;i<b.from+b.count;i++) bIdx.push(i);
    const bDone = b.count>0 && bIdx.every(i=>acLessonDone(i));
    const bPct = b.count>0 ? Math.round(bIdx.reduce((s,i)=>s+acLessonPct(i),0)/b.count) : 0;
    const head = `<div class="ac-block-head${bDone?' done':''}">
        <span class="ac-block-n">Блок ${bi+1}</span>
        <span class="ac-block-t">${esc(b.title)}</span>
        <span class="ac-block-meta">${b.count>0 ? (b.count+' '+acPlural(b.count,['урок','урока','уроков'])+(bPct>0?' · '+bPct+'%':'')) : 'готовится'}</span>
        ${bDone?`<span class="ac-block-done">${I('check2')}</span>`:''}
      </div>`;
    const body = b.count>0
      ? bIdx.map(i=>lessonRow(i, rk++)).join('')
      : `<div class="ac-block-soon">${I('clock')}<span>Уроки блока в производстве — скоро откроются</span></div>`;
    return `<div class="ac-block">${head}${body}</div>`;
  }).join('');
  let nx = idx.find(i=>acUnlocked(i) && !acLessonDone(i));
  if(nx===undefined && !done) nx = idx[0];   // защита: если курс не пройден, всегда есть куда вести
  const nextRow = (nx!==undefined)
    ? `<button class="ac-next-row" onclick="acOpenLesson(${nx})">${I('circle-play')}<span>${acLessonPct(nx)>0?'Продолжить':'Начать'}: <b>урок ${acLocalNo(nx)} — ${esc(AC_COURSE[nx].title)}</b></span><svg class="i go"><use href="#i-chev"/></svg></button>`
    : `<div class="ac-next-row done">${I('check2')}<span>Направление пройдено — именной сертификат у тебя</span></div>`;
  const certN = acCourseCertCount(ci);
  const finale = done
    ? `<div class="card ac-course-finale"><span class="ico">${I('crown')}</span>
        <div><h3>Курс завершён</h3><p>Ты прошёл все ${c.count} ${acPlural(c.count,['урок','урока','уроков'])} и получил ${certN} ${acPlural(certN,['сертификат','сертификата','сертификатов'])}. Так держать!</p></div></div>`
    : '';
  return `
    <button class="btn ghost sm ac-back" onclick="acBackHome()"><svg class="i"><use href="#i-back"/></svg> Каталог</button>
    <div class="ac-course-hero">
      <div class="ac-course-hero-cover">${acCourseCover(ci, true)}</div>
      <div class="ac-course-hero-cap">
        <span class="chip">${c.free?'Базовый курс':'Премиум · открыт'}</span>
        <h2>${esc(c.title)}</h2>
        <p>${esc(c.sub)}</p>
      </div>
    </div>
    ${acCourseInsideHtml(ci)}
    <div class="card">
      <div class="ac-course-top">
        <span class="ac-ring">
          <svg viewBox="0 0 80 80"><circle class="bg" cx="40" cy="40" r="33"/>
          <circle class="val" cx="40" cy="40" r="33" stroke-dasharray="${C.toFixed(1)}" stroke-dashoffset="${C.toFixed(1)}" data-off="${(C*(1-pct/100)).toFixed(1)}"/></svg>
          <b>${pct}%</b>
        </span>
        <div>
          <h3>Прогресс курса</h3>
          <p class="dim"><b style="color:var(--accent)">${doneCnt}</b>/${c.count} ${acPlural(c.count,['урок','урока','уроков'])} · ${certN} ${acPlural(certN,['сертификат','сертификата','сертификатов'])}</p>
        </div>
      </div>
      <div class="ac-course-progress" role="progressbar" aria-valuenow="${pct}" aria-valuemin="0" aria-valuemax="100" title="Прогресс курса ${pct}%">
        <i style="width:${pct}%"></i>
        <span class="ac-course-progress-lbl">${doneCnt}/${c.count} · ${pct}%</span>
      </div>
      ${nextRow}
      <div>${rows}</div>
    </div>
    ${finale}
    ${acRecCourseHtml(ci)}
    <div style="height:16px"></div>`;
}
/* плитка «Следующий рекомендуемый курс» под текущим курсом.
   Показываем только если у пользователя есть куда идти дальше внутри Академии. */
function acRecCourseHtml(ci){
  const rec = acRecommendNext(ci);
  if(rec < 0) return '';
  const c = AC_COURSES[rec];
  const acc = acCourseAccessible(rec);
  const pct = acCoursePctOf(rec);
  const started = pct > 0;
  const cta = acc ? (started ? 'Продолжить курс' : 'Начать курс') : 'Открыть доступ';
  const meta = acc
    ? (started ? ('Начат · пройдено ' + pct + '%') : (c.count + ' ' + acPlural(c.count,['урок','урока','уроков']) + ' · доступ открыт'))
    : (c.count + ' ' + acPlural(c.count,['урок','урока','уроков']) + ' · ' + acFmtPrice(c.price) + ' или подписка ' + c.minTier);
  return `
    <div class="card ac-rec" onclick="acCourseCardClick(${rec})">
      <div class="ac-rec-head">
        <span class="ac-rec-tag">${I('rocket')} Следующий шаг</span>
        <span class="ac-rec-meta">${esc(meta)}</span>
      </div>
      <div class="ac-rec-body">
        <div class="ac-rec-cover">${acCourseCover(rec, true)}${acc?'':`<span class="ac-cc-veil">${I('lock')}</span>`}</div>
        <div class="ac-rec-cap">
          <h3>${esc(c.title)}</h3>
          <p>${esc(c.sub)}</p>
          <button class="btn sm ac-rec-cta" onclick="event.stopPropagation();acCourseCardClick(${rec})">${cta}</button>
        </div>
      </div>
    </div>`;
}

/* ---------- СТРАНИЦА УРОКА ---------- */
function acOpenLesson(i){
  const k = (typeof i === 'number') ? i : 0;
  const ci = acCourseOf(k);
  if(!acCourseAccessible(ci)){ acCourseGate(ci); return; }
  if(!acUnlocked(k)){ toast('Урок '+acLocalNo(k)+' откроется после предыдущего'); return; }
  acL = k; acCourse = ci;
  acS.lastLesson = k; acSave();   // «Продолжи с этого» помнит последний открытый урок
  if(typeof acSpeedStop==='function') acSpeedStop();
  acView = 'lesson'; acQuiz = null; acG = null; acTaskChecking = false;
  acRender();
  const m = document.querySelector('main'); if(m) m.scrollTop = 0;
}
/* назад: из урока → на страницу курса, со страницы курса → в каталог */
function acBackHome(){
  if(typeof acSpeedStop==='function') acSpeedStop();
  if(acView === 'lesson'){ acView = 'course'; acCourse = acCourseOf(acL); }
  else acView = 'home';
  acRender();
  const m = document.querySelector('main'); if(m) m.scrollTop = 0;
}

function acLessonHtml(){
  const L = acCur();
  const ci = acCourseOf(acL);
  const vid = acHasVideo(L);
  return `
    <button class="btn ghost sm ac-back" onclick="acBackHome()"><svg class="i"><use href="#i-back"/></svg> ${esc(AC_COURSES[ci].title)}</button>
    <div class="ac-lesson-head">
      <span class="chip">Урок ${acLocalNo(acL)} из ${AC_COURSES[ci].count}</span>
      <h2>${L.title}</h2>
      <div class="m">${vid?`<span>${I('clock')} ${L.dur} видео</span><span>·</span>`:''}<span>${I('file')} ${L.slides.length} слайдов</span><span>·</span><span>тест из ${L.quiz.length} вопросов</span><span>·</span><span>мини-игра</span></div>
      ${acEnrichIntroHtml()}
    </div>

    ${vid ? '<div id="acVideoBox"></div>' : ''}

    <h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Слайды урока</h2>
    <div id="acSlidesBox">
      <div class="ac-slides" id="acSlides">${L.slides.map((s,i)=>`
        <div class="ac-slide ${i===0?'cur':''}" data-i="${i}">
          <span class="n">Слайд ${i+1} / ${L.slides.length}</span>
          <h3>${s.t}</h3>
          <div class="pic">${s.svg}</div>
          <ul>${s.pts.map(p=>`<li>${p}</li>`).join('')}</ul>
        </div>`).join('')}
      </div>
      <div class="ac-slides-nav">
        <button class="ac-arrow" onclick="acSlideGo(-1)"><svg class="i"><use href="#i-back"/></svg></button>
        <span class="ac-dots" id="acDots">${L.slides.map((_,i)=>`<i class="${i===0?'on':''}"></i>`).join('')}</span>
        <button class="ac-arrow next" onclick="acSlideGo(1)"><svg class="i"><use href="#i-back"/></svg></button>
      </div>
    </div>

    ${acEnrichDeepHtml()}

    <h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Конспект</h2>
    ${acNotesHtml()}

    ${acEnrichLinksHtml()}
    ${acToolsHtml(L)}

    <h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Тест по материалу</h2>
    <div class="card" id="acTestBox"></div>

    <h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Практика</h2>
    <div class="card" id="acTaskBox"></div>

    <h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Мини-игра</h2>
    <div class="card" id="acGameBox"></div>

    <h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Прогресс урока</h2>
    <div class="card" style="padding:8px 16px 14px" id="acProgressBox"></div>

    <div style="height:14px"></div>
    <div id="acCertBox"></div>
    <div style="height:16px"></div>`;
}

/* ================= ОБОГАЩЕНИЕ УРОКА (intro / глубокий разбор / лайфхак / ссылки) ================= */
function acEnrich(){ try{ return (window.AC_ENRICH||{})[acL] || null; }catch(e){ return null; } }
function acEnrichIntroHtml(){
  const e = acEnrich();
  if(!e || !e.intro) return '';
  return `<p class="ac-intro-hook">${esc(e.intro)}</p>`;
}
function acEnrichDeepHtml(){
  const e = acEnrich();
  if(!e) return '';
  let out = '';
  if(Array.isArray(e.notes) && e.notes.length){
    out += `<h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Глубокий разбор</h2>
      <div class="ac-deep">${e.notes.map((n,i)=>`
        <div class="ac-deep-sec" style="--d:${(i*0.06).toFixed(2)}s">
          <span class="n">${String(i+1).padStart(2,'0')}</span>
          <div class="bd"><h4>${esc(n.h||'')}</h4><p>${esc(n.body||'')}</p></div>
        </div>`).join('')}</div>`;
  }
  if(e.lifehack){
    out += `<div class="ac-lifehack">
      <div class="ic">${I('bolt')}</div>
      <div><span class="lbl">Лайфхак</span><p>${esc(e.lifehack)}</p></div>
    </div>`;
  }
  return out;
}
function acEnrichLinksHtml(){
  const e = acEnrich();
  if(!e || !Array.isArray(e.links) || !e.links.length) return '';
  const items = e.links.map(l=>{
    const url = String(l.url||'').trim();
    const safe = /^https?:\/\//i.test(url) ? url : ('https://' + url.replace(/^\/+/,''));
    let host = ''; try{ host = new URL(safe).hostname.replace(/^www\./,''); }catch(_){ host = safe; }
    return `<a class="ac-link-btn" href="${esc(safe)}" target="_blank" rel="noopener noreferrer">
      <span class="ic">${I('compass')}</span>
      <span class="tx"><b>${esc(l.label||host)}</b><span>${esc(l.note||host)}</span></span>
      <span class="host">${esc(host)}</span>
      <svg class="i go"><use href="#i-forward"/></svg></a>`;
  }).join('');
  return `<h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Инструменты урока</h2>
    <div class="ac-links">${items}</div>`;
}

/* ---------- а) ВИДЕО ---------- */
/* Бренд-обложка урока (SVG) — общий макет постера и заглушки «скоро» */
function acVpCover(L){
  return `
      <svg class="ac-cover" viewBox="0 0 640 360" preserveAspectRatio="xMidYMid slice">
        <defs>
          <linearGradient id="acPosterScrim" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0" stop-color="#04060a" stop-opacity="0"/>
            <stop offset=".5" stop-color="#04060a" stop-opacity="0"/>
            <stop offset="1" stop-color="#000" stop-opacity=".84"/>
          </linearGradient>
        </defs>
        <rect width="640" height="360" fill="#070a04"/>
        <g stroke="rgba(154,255,0,.09)" stroke-width="1">
          ${[80,160,240,320,400,480,560].map(x=>`<line x1="${x}" y1="0" x2="${x}" y2="360"/>`).join('')}
          ${[72,144,216,288].map(y=>`<line x1="0" y1="${y}" x2="640" y2="${y}"/>`).join('')}
        </g>
        <circle cx="530" cy="80" r="120" fill="rgba(154,255,0,.07)"/>
        <use href="#i-logo" x="472" y="34" width="120" height="120"/>
        <!-- нижний скрим: держит центр постера чистым под кнопку play, а заголовок внизу — читаемым в обеих темах -->
        <rect x="0" y="150" width="640" height="210" fill="url(#acPosterScrim)"/>
        <!-- заголовок вынесен в НИЖНЮЮ треть постера: не пересекается с центральной кнопкой play (она в чистом поле выше) -->
        <text x="48" y="272" font-family="'Bebas Neue',Impact,sans-serif" font-weight="700" font-size="40" fill="#fff" letter-spacing="1" textLength="${(L.c1||'').length>8?520:''}" lengthAdjust="spacingAndGlyphs">${L.c1}</text>
        <text x="48" y="320" font-family="'Bebas Neue',Impact,sans-serif" font-weight="700" font-size="52" fill="#9AFF00" letter-spacing="${(L.c2||'').length>8?2:4}" textLength="${(L.c2||'').length>9?540:''}" lengthAdjust="spacingAndGlyphs">${L.c2}</text>
        <text x="48" y="346" font-family="Montserrat,sans-serif" font-size="14" font-weight="600" fill="rgba(255,255,255,.6)" letter-spacing="3">АКАДЕМИЯ OKO · УРОК ${acLocalNo(acL)}</text>
      </svg>`;
}
function acRenderVideoBox(){
  const box = document.getElementById('acVideoBox');
  if(!box) return;
  const L = acCur(), ls = acLS();
  // Заглушка «видео в производстве» (у урока пока нет ролика)
  const cover = `
    <div class="ac-player" onclick="acPlay()" id="acPlayer">
      ${acVpCover(L)}
      <div class="ac-play-btn"><svg class="i"><use href="#i-play"/></svg></div>
      <span class="dur">${L.dur || 'скоро'}</span>
    </div>`;
  // Кастомный бренд-плеер (без нативного <video controls>) — свой чёрно-лаймовый хром
  const player = L.videoUrl ? `
    <div class="ac-player ac-vp" id="acPlayer">
      <video class="ac-vp-video" id="acVpVideo" playsinline webkit-playsinline preload="metadata" controlsList="nodownload noremoteplayback noplaybackrate" disablePictureInPicture disableRemotePlayback oncontextmenu="return false" src="${L.videoUrl}" onclick="acVpTapArea(event)"></video>
      <div class="ac-vp-poster" id="acVpPoster" onclick="acVpBig(event)">
        ${acVpCover(L)}
        <button class="ac-vp-big" type="button" onclick="acVpBig(event)" aria-label="Смотреть урок">${I('play')}</button>
        <span class="dur">${L.dur || 'видео'}</span>
      </div>
      <button class="ac-vp-center" id="acVpCenter" type="button" onclick="acVpToggle(event)" aria-label="Пауза/пуск">${I('pause')}</button>
      <div class="ac-vp-bar" id="acVpBar" onclick="acVpBarStop(event)">
        <button class="ac-vp-btn" id="acVpPlay" type="button" onclick="acVpToggle(event)" aria-label="Пауза/пуск">${I('play')}</button>
        <div class="ac-vp-scrub" id="acVpScrub" onclick="acVpSeek(event)" role="slider" aria-label="Перемотка"><span class="trk"><i class="ac-vp-fill" id="acVpFill"></i></span></div>
        <span class="ac-vp-time" id="acVpTime">0:00 / ${L.dur || '0:00'}</span>
        <button class="ac-vp-btn" id="acVpMute" type="button" onclick="acVpMute(event)" aria-label="Звук">${I('megaphone')}</button>
        <button class="ac-vp-btn" type="button" onclick="acVpFull(event)" aria-label="На весь экран">${I('device')}</button>
      </div>
    </div>` : cover;
  box.innerHTML = player + `
    <div class="ac-video-actions">${ls.video
      ? `<span class="ac-done-chip">${I('check2')} Видео просмотрено</span>`
      : `<button class="btn" onclick="acMarkVideo()">${I('check2')} Отметить просмотренным</button>`}
    </div>`;
  if(L.videoUrl) acVpInit();
}
function acPlay(){
  if(acCur().videoUrl) return;
  showPopup({ico:'circle-play', title:'Видео урока в производстве',
    body:'Ролик урока «'+acCur().title+'» сейчас монтируется: озвучка, анимации и караоке-субтитры по стандарту Академии. Совсем скоро он появится прямо здесь. Пока изучи слайды ниже — в них весь материал урока.',
    actions:[{label:'Понятно'}]});
}

/* ---------- бренд-плеер: кастомный хром (без нативных controls) ---------- */
let acVpTimer = null;                       // таймер авто-скрытия панели
function acVpEl(){ return document.getElementById('acPlayer'); }
function acVpVid(){ return document.getElementById('acVpVideo'); }
function acVpFmt(s){                          // секунды -> m:ss
  if(!isFinite(s) || s < 0) s = 0;
  const m = Math.floor(s/60), z = Math.floor(s%60);
  return m + ':' + (z<10?'0':'') + z;
}
/* Привязка событий к видео после рендера. Всё в try/catch — оффлайн-источник не должен ломать хром */
function acVpInit(){
  try{
    const v = acVpVid(), p = acVpEl();
    if(!v || !p) return;
    v.addEventListener('loadedmetadata', acVpSync);
    v.addEventListener('durationchange', acVpSync);
    v.addEventListener('timeupdate', acVpSync);           // дёшево (~4/сек), без перерасчёта layout
    v.addEventListener('play',  ()=>{ p.classList.add('started','playing'); acVpSyncIcons(); acVpShow(false); });
    v.addEventListener('pause', ()=>{ p.classList.remove('playing'); acVpSyncIcons(); acVpShow(true); });
    v.addEventListener('ended', ()=>{ p.classList.remove('playing','started'); acVpSyncIcons(); acVpShow(true); });
    v.addEventListener('error', ()=>{ p.classList.remove('started','playing'); }); // источник не доступен — постер остаётся
    acVpSyncIcons();
  }catch(_){}
}
/* Показать панель управления; при проигрывании прячем сама через пару секунд */
function acVpShow(sticky){
  const p = acVpEl(); if(!p) return;
  p.classList.add('controls-on');
  clearTimeout(acVpTimer);
  if(!sticky && p.classList.contains('playing')){
    acVpTimer = setTimeout(()=>{ const q=acVpEl(); if(q) q.classList.remove('controls-on'); }, 2500);
  }
}
function acVpHide(){ const p=acVpEl(); if(p) p.classList.remove('controls-on'); clearTimeout(acVpTimer); }
/* Тап по области видео во время проигрывания — показать/скрыть управление */
function acVpTapArea(e){
  if(e) e.stopPropagation();
  const p = acVpEl(); if(!p || !p.classList.contains('started')) return;
  if(p.classList.contains('controls-on')) acVpHide(); else acVpShow(false);
}
function acVpBarStop(e){ if(e) e.stopPropagation(); acVpShow(false); } // клики по панели не сворачивают её
/* Большая лаймовая кнопка на постере — старт */
function acVpBig(e){
  if(e) e.stopPropagation();
  const v = acVpVid(); if(!v) return;
  try{ const pr = v.play(); if(pr && pr.catch) pr.catch(()=>{}); }catch(_){} // оффлайн: play отклонится — постер остаётся
}
/* Пуск/пауза (центр + нижняя панель) */
function acVpToggle(e){
  if(e) e.stopPropagation();
  const v = acVpVid(); if(!v) return;
  try{
    if(v.paused){ const pr = v.play(); if(pr && pr.catch) pr.catch(()=>{}); }
    else v.pause();
  }catch(_){}
  acVpShow(false);
}
/* Перемотка по клику на слим-скраббер */
function acVpSeek(e){
  if(e) e.stopPropagation();
  const v = acVpVid(), s = document.getElementById('acVpScrub');
  if(!v || !s) return;
  try{
    const r = s.getBoundingClientRect();
    const x = (e.clientX != null) ? e.clientX : (e.touches && e.touches[0] ? e.touches[0].clientX : r.left);
    let p = (x - r.left) / r.width; p = Math.max(0, Math.min(1, p));
    if(isFinite(v.duration) && v.duration > 0){ v.currentTime = p * v.duration; acVpSync(); }
  }catch(_){}
  acVpShow(false);
}
/* Тумблер звука */
function acVpMute(e){
  if(e) e.stopPropagation();
  const v = acVpVid(), b = document.getElementById('acVpMute');
  if(!v) return;
  try{ v.muted = !v.muted; if(b) b.classList.toggle('off', v.muted); }catch(_){}
  acVpShow(false);
}
/* На весь экран (fullscreen контейнера, iOS — самого видео) */
function acVpFull(e){
  if(e) e.stopPropagation();
  const p = acVpEl(), v = acVpVid();
  try{
    if(document.fullscreenElement){ if(document.exitFullscreen) document.exitFullscreen(); return; }
    if(p && p.requestFullscreen) p.requestFullscreen();
    else if(p && p.webkitRequestFullscreen) p.webkitRequestFullscreen();
    else if(v && v.webkitEnterFullscreen) v.webkitEnterFullscreen();
  }catch(_){}
  acVpShow(false);
}
/* Обновление скраббера и таймкода (transform:scaleX — без layout-thrash) */
function acVpSync(){
  const v = acVpVid(); if(!v) return;
  const fill = document.getElementById('acVpFill'), t = document.getElementById('acVpTime');
  const dur = (isFinite(v.duration) && v.duration > 0) ? v.duration : 0;
  const p = dur ? Math.max(0, Math.min(1, v.currentTime / dur)) : 0;
  if(fill) fill.style.transform = 'scaleX(' + p.toFixed(4) + ')';
  if(t){ let d; try{ d = acCur().dur; }catch(_){ d = ''; } t.textContent = acVpFmt(v.currentTime) + ' / ' + (dur ? acVpFmt(dur) : (d || '0:00')); }
}
/* Синхронизация иконок play/pause по состоянию видео */
function acVpSyncIcons(){
  const v = acVpVid(); if(!v) return;
  const ico = v.paused ? I('play') : I('pause');
  const b = document.getElementById('acVpPlay'), c = document.getElementById('acVpCenter');
  if(b) b.innerHTML = ico;
  if(c) c.innerHTML = ico;
}
function acMarkVideo(){
  acLS().video = true; acSave();
  toast('Видео засчитано · +20% к уроку');
  acRenderVideoBox(); acRenderProgressBox(); acRenderCertBox(); acAfterCheckpoint();
}

/* ---------- б) СЛАЙДЫ ---------- */
let acSlideIdx = 0;
function acBindSlides(){
  const el = document.getElementById('acSlides');
  if(!el) return;
  acSlideIdx = 0;
  let raf = null;
  el.addEventListener('scroll', ()=>{
    if(raf) return;
    raf = requestAnimationFrame(()=>{
      raf = null;
      const kids = el.children;
      if(!kids.length) return;
      const step = kids[0].offsetWidth + 12;
      const i = Math.max(0, Math.min(acCur().slides.length-1, Math.round(el.scrollLeft / step)));
      if(i !== acSlideIdx){ acSlideIdx = i; acSlideSync(); }
    });
  }, {passive:true});
}
function acSlideSync(){
  const dots = document.querySelectorAll('#acDots i');
  dots.forEach((d,i)=>d.classList.toggle('on', i===acSlideIdx));
  document.querySelectorAll('#acSlides .ac-slide').forEach((s,i)=>s.classList.toggle('cur', i===acSlideIdx));
  const ls = acLS();
  if(acSlideIdx > ls.slideMax){ ls.slideMax = acSlideIdx; acSave(); }
  if(ls.slideMax >= acCur().slides.length-1 && !ls.slides){
    ls.slides = true; acSave();
    toast('Слайды пройдены · +20% к уроку');
    acRenderProgressBox(); acRenderCertBox(); acAfterCheckpoint();
  }
}
function acSlideGo(d){
  const el = document.getElementById('acSlides');
  if(!el || !el.children.length) return;
  const step = el.children[0].offsetWidth + 12;
  const i = Math.max(0, Math.min(acCur().slides.length-1, acSlideIdx + d));
  el.scrollTo({left: i*step, behavior:'smooth'});
  acSlideIdx = i; acSlideSync();
}

/* ---------- в) ТЕСТ ---------- */
function acRenderTestBox(){
  const box = document.getElementById('acTestBox');
  if(!box) return;
  const quiz = acCur().quiz, ls = acLS();
  if(!acQuiz){
    box.innerHTML = ls.test
      ? `<div class="ac-score" style="font-size:52px">${ls.testScore}%</div>
         <div class="ac-score-sub">Тест сдан. Лучший результат — <b>${ls.testScore}%</b> при пороге ${AC_PASS}%.</div>
         <button class="btn ghost" onclick="acQuizStart()">Пройти заново</button>`
      : `<p style="font-size:13.5px;line-height:1.55">${quiz.length} вопросов по материалу урока. По одному на экран, порог зачёта — <b style="color:var(--accent)">${AC_PASS}%</b>.${ls.testScore?` Прошлая попытка: ${ls.testScore}%.`:''}</p>
         <div style="height:12px"></div>
         <button class="btn" onclick="acQuizStart()">${I('bolt')} ${ls.testScore?'Попробовать ещё раз':'Начать тест'}</button>`;
    return;
  }
  if(acQuiz.done){
    const score = acQuiz.score, pass = score >= AC_PASS;
    const misses = acQuiz.misses || [];
    const review = misses.length
      ? `<div class="ac-review">
          <div class="ac-review-h">${I('poll')} Разбор ошибок · ${misses.length} ${acPlural(misses.length,['вопрос','вопроса','вопросов'])}</div>
          ${misses.map(m=>{ const qq=quiz[m.q]; return `
            <div class="ac-review-item">
              <div class="rq"><span class="rn">${m.q+1}</span><span>${qq.q}</span></div>
              <div class="ra bad"><span class="lb">${'АБВГ'[m.chosen]}</span><span>${qq.o[m.chosen]}</span></div>
              <div class="ra ok"><span class="lb">${'АБВГ'[qq.a]}</span><span>${qq.o[qq.a]}</span></div>
            </div>`; }).join('')}
        </div>`
      : `<div class="ac-review perfect">${I('star')} Идеально — ни одной ошибки${acQuiz.best>=3?` · лучшая серия ×${acQuiz.best}`:''}</div>`;
    box.innerHTML = `
      <div class="ac-score" id="acScoreNum" ${pass?'':'style="color:var(--danger);text-shadow:none"'}>0%</div>
      <div class="ac-score-sub">${pass
        ? `Порог ${AC_PASS}% пройден — <b>тест зачтён</b>. Верных ответов: ${acQuiz.hits} из ${quiz.length}.`
        : `<span class="fail">Не хватило до порога ${AC_PASS}%.</span> Верных: ${acQuiz.hits} из ${quiz.length}. Пролистай слайды и попробуй снова.`}</div>
      <button class="btn ${pass?'ghost':''}" onclick="acQuizStart()">Пройти заново</button>
      ${review}`;
    const el = document.getElementById('acScoreNum');
    acCountUp(el, score, '%');
    return;
  }
  const q = quiz[acQuiz.i];
  box.innerHTML = `
    <div class="ac-quiz-top"><span>Вопрос <b>${acQuiz.i+1}</b> из ${quiz.length}</span><span>${acQuiz.combo>=2?`<span class="ac-combo-live">${I('fire')} ×${acQuiz.combo}</span> · `:''}верных: <b>${acQuiz.hits}</b></span></div>
    <div class="progress" style="margin:0 0 4px"><i style="width:${(acQuiz.i/quiz.length*100)}%"></i></div>
    <div class="ac-q">${q.q}</div>
    ${q.o.map((o,i)=>`<button class="ac-opt" id="acOpt${i}" onclick="acAnswer(${i})"><span class="lb">${'АБВГ'[i]}</span><span>${o}</span></button>`).join('')}`;
}
function acQuizStart(){
  acQuiz = {i:0, hits:0, lock:false, done:false, score:0, misses:[], combo:0, best:0};
  acRenderTestBox();
}
/* всплывающий индикатор серии верных ответов (гейм-фидбек, не мешает потоку теста) */
function acComboPop(n){
  try{
    const box = document.getElementById('acTestBox'); if(!box) return;
    const el = document.createElement('div'); el.className = 'ac-combo-pop';
    el.innerHTML = I('fire') + '<span>серия ×' + n + '</span>';
    box.appendChild(el);
    requestAnimationFrame(()=>el.classList.add('go'));
    setTimeout(()=>{ try{ el.remove(); }catch(e){} }, 900);
  }catch(e){}
}
function acAnswer(i){
  if(!acQuiz || acQuiz.lock || acQuiz.done) return;
  acQuiz.lock = true;
  const quiz = acCur().quiz;
  const q = quiz[acQuiz.i];
  const right = i === q.a;
  if(right){ acQuiz.hits++; acQuiz.combo++; if(acQuiz.combo > acQuiz.best) acQuiz.best = acQuiz.combo; }
  else { acQuiz.combo = 0; acQuiz.misses.push({q:acQuiz.i, chosen:i}); }
  const ok = document.getElementById('acOpt'+q.a);
  if(ok) ok.classList.add('ok');
  if(!right){ const bad = document.getElementById('acOpt'+i); if(bad) bad.classList.add('bad'); }
  if(right && acQuiz.combo >= 3) acComboPop(acQuiz.combo);
  setTimeout(()=>{
    acQuiz.lock = false;
    acQuiz.i++;
    if(acQuiz.i >= quiz.length){
      acQuiz.done = true;
      acQuiz.score = Math.round(acQuiz.hits / quiz.length * 100);
      const ls = acLS();
      if(acQuiz.score > ls.testScore) ls.testScore = acQuiz.score;
      if(acQuiz.score >= AC_PASS && !ls.test){
        ls.test = true;
        toast('Тест сдан · +20% к уроку');
      }
      acSave();
      acRenderProgressBox(); acRenderCertBox(); acBadgeSync(); acAfterCheckpoint();
    }
    acRenderTestBox();
  }, right ? 700 : 1100);
}
function acCountUp(el, to, suffix){
  if(!el) return;
  const t0 = performance.now(), dur = 950;
  (function step(t){
    const k = Math.min(1, (t-t0)/dur), e = 1 - Math.pow(1-k, 3);
    el.textContent = Math.round(to*e) + (suffix||'');
    if(k < 1) requestAnimationFrame(step);
  })(t0);
}

/* ---------- г) ПРАКТИКА ---------- */
function acRenderTaskBox(){
  const box = document.getElementById('acTaskBox');
  if(!box) return;
  const T = acCur().task, ls = acLS();
  if(acTaskChecking){
    box.innerHTML = `<div class="ac-checking"><span class="ac-spin"></span>
      <div><p>Проверяется куратором<small>Сверяю ответ с материалом урока…</small></p></div></div>`;
    return;
  }
  if(ls.task){
    box.innerHTML = `
      <div class="ac-verdict">
        <div class="h">${I('check2')} Зачтено</div>
        <p>${T.verdict}</p>
      </div>
      <div style="height:10px"></div>
      <p class="dim" style="font-size:12px;line-height:1.5">Твой ответ: «${esc(ls.taskText.slice(0,140))}${ls.taskText.length>140?'…':''}»</p>`;
    return;
  }
  box.innerHTML = `
    <p style="font-size:13.5px;line-height:1.55">${T.intro}</p>
    <div class="ac-task-formula">${T.chips.map((c,i)=>(i? I('chev'):'')+`<span>${c}</span>`).join('')}</div>
    <textarea class="ac-task-ta" id="acTaskTa" placeholder="${esc(T.ph)}">${esc(ls.taskText)}</textarea>
    <div style="height:10px"></div>
    <button class="btn" onclick="acTaskSend()">${I('send')} Отправить на проверку</button>`;
}
function acTaskSend(){
  const ta = document.getElementById('acTaskTa');
  const v = (ta && ta.value || '').trim();
  if(v.length < 40){ toast('Раскрой подробнее — минимум 40 символов'); return; }
  const ls = acLS();
  ls.taskText = v; acSave();
  acTaskChecking = true;
  acRenderTaskBox();
  setTimeout(()=>{
    acTaskChecking = false;
    ls.task = true; acSave();
    toast('Практика зачтена · +20% к уроку');
    acRenderTaskBox(); acRenderProgressBox(); acRenderCertBox(); acBadgeSync(); acAfterCheckpoint();
  }, 4000);
}

/* ---------- д) МИНИ-ИГРА ---------- */
function acShuffle(a){ a=a.slice(); for(let i=a.length-1;i>0;i--){ const j=Math.floor(Math.random()*(i+1)); [a[i],a[j]]=[a[j],a[i]]; } return a; }
/* Тип мини-игры на урок (анти-повтор: чередуется по индексу урока).
   Движки берут данные из уже готового контента урока (pairs / slides). */
/* Реестр движков (9). Порядок = порядок анти-повтора по индексу урока.
   Каждый движок берёт данные из уже готового контента урока. */
const AC_GAMES = ['pairs','order','truefalse','memory','flash','quizspeed','gap','bucket','oddquiz'];
/* Достаточно ли данных урока для движка t. */
function acGamePlayable(t, L){
  const nS = (L.slides||[]).length, nQ = (L.quiz||[]).length, nP = (L.pairs||[]).length;
  switch(t){
    case 'pairs':    return nP >= 3;
    case 'memory':   return nP >= 3 && nP <= 8;      // сетка карточек
    case 'flash':    return nP >= 3;
    case 'order':    return nS >= 3;
    case 'gap':      return nS >= 3;
    case 'truefalse':return nQ >= 3;
    case 'quizspeed':return nQ >= 3;
    case 'bucket':   return nQ >= 3;
    case 'oddquiz':  return nQ >= 3 && (L.quiz||[]).every(q=>(q.o||[]).length>=3);
    default:         return false;
  }
}
/* Тип мини-игры урока: анти-повтор по индексу + фолбэк по доступности данных. */
function acGameType(){
  const L = acCur();
  const start = acL % AC_GAMES.length;
  for(let k=0;k<AC_GAMES.length;k++){
    const t = AC_GAMES[(start + k) % AC_GAMES.length];
    if(acGamePlayable(t, L)) return t;
  }
  // предельный фолбэк
  if((L.pairs||[]).length) return 'pairs';
  if((L.slides||[]).length>=3) return 'order';
  return 'truefalse';
}
/* Заголовки-подписи под каждый движок (интро + «пройдено»). */
const AC_GAME_META = {
  pairs:    {done:'Все пары собраны',              intro:'<b>Сопоставь пары.</b> Тапни элемент слева, затем его пару справа. Верная пара улетает, неверная — трясётся.'},
  order:    {done:'Порядок шагов восстановлен',    intro:'<b>Расставь шаги по порядку.</b> Тапай карточки в правильной последовательности урока — верный шаг встаёт на место, неверный трясётся.'},
  truefalse:{done:'Все утверждения разобраны',     intro:'<b>Правда или ложь.</b> Для каждого утверждения реши, верный это ответ или нет. Ошибся — покажем, как на самом деле.'},
  memory:   {done:'Все карточки открыты',          intro:'<b>Мемори.</b> Открывай по две карточки и находи пары «понятие ↔ значение». Запоминай, где что лежит.'},
  flash:    {done:'Карточки повторены',            intro:'<b>Флеш-карточки.</b> Смотри понятие, вспомни значение и переверни карточку. Отметь честно: знал или нет.'},
  quizspeed:{done:'Блиц пройден',                  intro:'<b>Блиц на скорость.</b> На каждый вопрос — несколько секунд. Отвечай быстро и верно, пока идёт таймер.'},
  gap:      {done:'Пропуски заполнены',            intro:'<b>Заполни пропуск.</b> В тезисе урока пропущено ключевое слово — выбери верное из вариантов.'},
  bucket:   {done:'Всё разложено по полкам',       intro:'<b>Правда / Ложь по полкам.</b> Разложи все утверждения в две колонки, затем проверь себя разом.'},
  oddquiz:  {done:'Лишнее убрано',                 intro:'<b>Убери неверное.</b> В каждом вопросе вычёркивай неверные варианты — пока не останется единственный правильный.'}
};
function acGameFinish(wrong){
  const ls = acLS(); ls.gameWrong = wrong;
  if(!ls.game){ ls.game = true; toast('Мини-игра пройдена · +20% к уроку'); }
  acG = null; acSave();
  acRenderGameBox(); acRenderProgressBox(); acRenderCertBox(); acBadgeSync(); acAfterCheckpoint();
}
function acRenderGameBox(){
  const box = document.getElementById('acGameBox');
  if(!box) return;
  const t = acGameType(), ls = acLS();
  const meta = AC_GAME_META[t] || AC_GAME_META.pairs;
  if(!acG){
    if(ls.game){
      box.innerHTML = `<div class="ac-game-done"><div class="big">${I('check2')}</div>
         <p>${meta.done}${ls.gameWrong!==null&&ls.gameWrong!==undefined?` · ошибок: ${ls.gameWrong}`:''}. Материал урока — в связке.</p>
         <button class="btn ghost" onclick="acGameStart()">Сыграть ещё раз</button></div>`;
    } else {
      box.innerHTML = `<p style="font-size:13.5px;line-height:1.55">${meta.intro}</p><div style="height:12px"></div>
         <button class="btn" onclick="acGameStart()">${I('play')} Играть</button>`;
    }
    return;
  }
  switch(acG.type){
    case 'order':     return acRenderOrder(box);
    case 'truefalse': return acRenderTF(box);
    case 'memory':    return acRenderMemory(box);
    case 'flash':     return acRenderFlash(box);
    case 'quizspeed': return acRenderSpeed(box);
    case 'gap':       return acRenderGap(box);
    case 'bucket':    return acRenderBucket(box);
    case 'oddquiz':   return acRenderOdd(box);
    default:          return acRenderPairs(box);
  }
}
function acRenderPairs(box){
  const pairs = acCur().pairs;
  const left = acG.left.map(i=>`<button class="ac-tile ${acG.sel===i?'sel':''}" id="acGL${i}" onclick="acGPick(${i})">${pairs[i][0]}</button>`).join('');
  const right = acG.right.map(i=>`<button class="ac-tile" id="acGR${i}" onclick="acGMatch(${i})">${pairs[i][1]}</button>`).join('');
  box.innerHTML = `
    <div class="ac-game-score"><span>Собрано пар: <b>${acG.hits} / ${pairs.length}</b></span><span>ошибок: <b>${acG.wrong}</b></span></div>
    <div class="ac-game-cols">
      <div class="ac-game-col"><div class="h">Понятие</div>${left}</div>
      <div class="ac-game-col"><div class="h">Пара</div>${right}</div>
    </div>`;
}
function acRenderOrder(box){
  const S = acCur().slides;
  let placed = '';
  for(let k=0;k<acG.next;k++) placed += `<div class="ac-ord-step done"><span class="n">${k+1}</span><span>${esc(S[k].t)}</span></div>`;
  if(acG.next < S.length) placed += `<div class="ac-ord-step now"><span class="n">${acG.next+1}</span><span class="dim">Выбери следующий шаг…</span></div>`;
  const pool = acG.pool.filter(i=>i>=acG.next)
    .map(i=>`<button class="ac-ord-tile" id="acOrd${i}" onclick="acOrderPick(${i})">${esc(S[i].t)}</button>`).join('');
  box.innerHTML = `
    <div class="ac-game-score"><span>Шаг: <b>${acG.next} / ${S.length}</b></span><span>ошибок: <b>${acG.wrong}</b></span></div>
    <div class="ac-ord-seq">${placed}</div>
    <div class="ac-ord-pool">${pool}</div>`;
}
function acOrderPick(i){
  if(!acG || acG.lock) return;
  const S = acCur().slides;
  if(i === acG.next){
    acG.next++;
    if(acG.next >= S.length){ acGameFinish(acG.wrong); return; }
    acRenderOrder(document.getElementById('acGameBox'));
  } else {
    acG.wrong++;
    const el = document.getElementById('acOrd'+i);
    if(el){ el.classList.add('shake'); acG.lock = true; setTimeout(()=>{ el.classList.remove('shake'); acG.lock=false; acRenderOrder(document.getElementById('acGameBox')); }, 420); }
    else acRenderOrder(document.getElementById('acGameBox'));
  }
}
function acStripTags(s){ return String(s).replace(/<[^>]+>/g,'').replace(/\s+/g,' ').trim(); }
function acGameStart(){
  acSpeedStop();                                // на всякий случай гасим прошлый таймер блица
  const L = acCur(), t = acGameType();
  if(t === 'order'){
    const idx = L.slides.map((_,i)=>i);
    acG = {type:'order', pool:acShuffle(idx), next:0, wrong:0, lock:false};
  } else if(t === 'truefalse'){
    const items = L.quiz.map(it=>{
      const showTrue = Math.random() < 0.5;
      if(showTrue) return {text:it.q, opt:it.o[it.a], isTrue:true};
      const wrongs = it.o.map((o,k)=>k).filter(k=>k!==it.a);
      const wk = wrongs[Math.floor(Math.random()*wrongs.length)];
      return {text:it.q, opt:it.o[wk], isTrue:false};
    });
    const order = acShuffle(items.map((_,i)=>i));
    acG = {type:'truefalse', items:order.map(i=>items[i]), cur:0, wrong:0, lock:false};
  } else if(t === 'memory'){
    // карточки: по 2 на пару (понятие + значение), общий id пары
    const cards = [];
    L.pairs.forEach((p,pid)=>{ cards.push({pid,txt:acStripTags(p[0])}); cards.push({pid,txt:acStripTags(p[1])}); });
    acG = {type:'memory', cards:acShuffle(cards), open:[], matched:{}, hits:0, wrong:0, lock:false};
  } else if(t === 'flash'){
    const order = acShuffle(L.pairs.map((_,i)=>i));
    acG = {type:'flash', order, cur:0, flip:false, wrong:0};
  } else if(t === 'quizspeed'){
    const order = acShuffle(L.quiz.map((_,i)=>i));
    acG = {type:'quizspeed', order:order.map(i=>L.quiz[i]), cur:0, wrong:0, lock:false, tl:0, per:12};
  } else if(t === 'gap'){
    acG = {type:'gap', items:acBuildGap(L), cur:0, wrong:0, lock:false};
  } else if(t === 'bucket'){
    const items = L.quiz.map(it=>{
      const showTrue = Math.random() < 0.5;
      const opt = showTrue ? it.o[it.a] : it.o[it.o.map((_,k)=>k).filter(k=>k!==it.a)[Math.floor(Math.random()*(it.o.length-1))]];
      return {text:acStripTags(it.q), opt:acStripTags(opt), isTrue:showTrue, placed:null};
    });
    acG = {type:'bucket', items:acShuffle(items), wrong:0, done:false};
  } else if(t === 'oddquiz'){
    const order = acShuffle(L.quiz.map((_,i)=>i));
    acG = {type:'oddquiz', q:order.map(i=>L.quiz[i]), cur:0, wrong:0, lock:false, dead:{}};
  } else {
    const idx = L.pairs.map((_,i)=>i);
    acG = {type:'pairs', left:idx.slice(), right:acShuffle(idx), sel:null, hits:0, wrong:0, lock:false};
  }
  acRenderGameBox();
  if(acG.type === 'quizspeed') acSpeedTick(true);
}
/* Построение заданий «заполни пропуск» из тезисов слайдов + дистракторов из теста. */
function acBuildGap(L){
  const STOP = new Set(['этого','который','которые','которых','чтобы','более','менее','также','когда','только','нужно','можно','будет','через','между','каждый','любой','очень','просто','сразу','потом','затем','около']);
  const distractPool = [];
  (L.quiz||[]).forEach(q=>(q.o||[]).forEach(o=>{ const w=acStripTags(o).split(/\s+/).filter(x=>x.length>4&&/^[А-Яа-яA-Za-z]+$/.test(x)); distractPool.push(...w); }));
  const items = [];
  (L.slides||[]).forEach(s=>{
    (s.pts||[]).forEach(pt=>{
      const clean = acStripTags(pt);
      const words = clean.split(/\s+/);
      const cand = words.map((w,idx)=>({w:w.replace(/[.,:;!?«»()"]/g,''),idx})).filter(o=>o.w.length>5 && /^[А-Яа-яA-Za-z-]+$/.test(o.w) && !STOP.has(o.w.toLowerCase()));
      if(!cand.length || words.length < 6) return;
      const pick = cand[Math.floor(Math.random()*cand.length)];
      const answer = pick.w;
      const distr = acShuffle(distractPool.filter(d=>d.toLowerCase()!==answer.toLowerCase()));
      const opts = [answer];
      for(const d of distr){ if(opts.length>=4) break; if(!opts.some(o=>o.toLowerCase()===d.toLowerCase())) opts.push(d); }
      // подстрахуемся: если дистракторов мало — берём слова из этого же тезиса
      for(const o of cand){ if(opts.length>=3) break; if(o.w.toLowerCase()!==answer.toLowerCase() && !opts.some(x=>x.toLowerCase()===o.w.toLowerCase())) opts.push(o.w); }
      if(opts.length < 3) return;
      const shown = words.map((w,idx)=> idx===pick.idx ? '<span class="gap-blank">•••</span>' : w).join(' ');
      items.push({html:shown, answer, opts:acShuffle(opts)});
    });
  });
  return acShuffle(items).slice(0, 6);   // до 6 пропусков
}
function acRenderTF(box){
  const it = acG.items[acG.cur];
  if(!it){ acGameFinish(acG.wrong); return; }
  box.innerHTML = `
    <div class="ac-game-score"><span>Утверждение: <b>${acG.cur+1} / ${acG.items.length}</b></span><span>ошибок: <b>${acG.wrong}</b></span></div>
    <div class="ac-tf-card" id="acTFCard">
      <div class="q">${esc(it.text)}</div>
      <div class="opt">${esc(it.opt)}</div>
      <div class="ask">Это верный ответ?</div>
    </div>
    <div class="ac-tf-btns">
      <button class="ac-tf-btn no" onclick="acTFAnswer(false)">Ложь</button>
      <button class="ac-tf-btn yes" onclick="acTFAnswer(true)">Правда</button>
    </div>`;
}
function acTFAnswer(ans){
  if(!acG || acG.lock) return;
  const it = acG.items[acG.cur];
  const nextCard = ()=>{ acG.cur++; if(acG.cur>=acG.items.length){ acGameFinish(acG.wrong); return; } acRenderTF(document.getElementById('acGameBox')); };
  if(ans === it.isTrue){ nextCard(); }
  else {
    acG.wrong++; acG.lock = true;
    const c = document.getElementById('acTFCard'); if(c) c.classList.add('shake');
    toast(it.isTrue ? 'На самом деле это верный ответ' : 'На самом деле ответ неверный');
    setTimeout(()=>{ acG.lock = false; nextCard(); }, 780);
  }
}
function acGPick(i){
  if(!acG || acG.lock) return;
  acG.sel = acG.sel === i ? null : i;
  acG.left.forEach(k=>{
    const el = document.getElementById('acGL'+k);
    if(el) el.classList.toggle('sel', acG.sel===k);
  });
}
function acGMatch(i){
  if(!acG || acG.lock) return;
  if(acG.sel === null){ toast('Сначала выбери элемент слева'); return; }
  const L = document.getElementById('acGL'+acG.sel), R = document.getElementById('acGR'+i);
  if(acG.sel === i){
    acG.lock = true;
    if(L) L.classList.add('hit');
    if(R) R.classList.add('hit');
    setTimeout(()=>{
      acG.left = acG.left.filter(k=>k!==i);
      acG.right = acG.right.filter(k=>k!==i);
      acG.hits++; acG.sel = null; acG.lock = false;
      if(!acG.left.length){
        const wrong = acG.wrong;
        acG = null;
        const ls = acLS();
        ls.gameWrong = wrong;
        if(!ls.game){ ls.game = true; toast('Мини-игра пройдена · +20% к уроку'); }
        acSave();
        acRenderGameBox(); acRenderProgressBox(); acRenderCertBox(); acBadgeSync(); acAfterCheckpoint();
      } else acRenderGameBox();
    }, 560);
  } else {
    acG.wrong++;
    if(L) L.classList.add('shake');
    if(R) R.classList.add('shake');
    acG.lock = true;
    setTimeout(()=>{
      acG.lock = false; acG.sel = null;
      acRenderGameBox();
    }, 460);
  }
}

/* ---------- МЕМОРИ: открывай пары карточек ---------- */
function acRenderMemory(box){
  const cards = acG.cards.map((c,i)=>{
    const open = acG.open.includes(i), done = acG.matched[i];
    const cls = done ? 'done' : open ? 'open' : '';
    return `<button class="ac-mem-card ${cls}" id="acMem${i}" onclick="acMemFlip(${i})">
      <span class="face back"><svg class="i"><use href="#i-logo"/></svg></span>
      <span class="face front">${esc(c.txt)}</span></button>`;
  }).join('');
  box.innerHTML = `
    <div class="ac-game-score"><span>Пары: <b>${acG.hits} / ${acG.cards.length/2}</b></span><span>ошибок: <b>${acG.wrong}</b></span></div>
    <div class="ac-mem-grid">${cards}</div>`;
}
function acMemFlip(i){
  if(!acG || acG.lock || acG.matched[i] || acG.open.includes(i)) return;
  acG.open.push(i);
  acRenderMemory(document.getElementById('acGameBox'));
  if(acG.open.length < 2) return;
  const [a,b] = acG.open;
  acG.lock = true;
  if(acG.cards[a].pid === acG.cards[b].pid && a !== b){
    setTimeout(()=>{
      acG.matched[a] = acG.matched[b] = true; acG.open = []; acG.hits++; acG.lock = false;
      if(acG.hits >= acG.cards.length/2){ acGameFinish(acG.wrong); return; }
      acRenderMemory(document.getElementById('acGameBox'));
    }, 460);
  } else {
    acG.wrong++;
    const ea=document.getElementById('acMem'+a), eb=document.getElementById('acMem'+b);
    if(ea) ea.classList.add('miss'); if(eb) eb.classList.add('miss');
    setTimeout(()=>{ acG.open = []; acG.lock = false; acRenderMemory(document.getElementById('acGameBox')); }, 720);
  }
}
/* ---------- ФЛЕШ-КАРТОЧКИ: понятие → значение, самооценка ---------- */
function acRenderFlash(box){
  const p = acCur().pairs[acG.order[acG.cur]];
  box.innerHTML = `
    <div class="ac-game-score"><span>Карточка: <b>${acG.cur+1} / ${acG.order.length}</b></span><span>не вспомнил: <b>${acG.wrong}</b></span></div>
    <div class="ac-flash ${acG.flip?'flip':''}" id="acFlash" onclick="acFlashFlip()">
      <div class="ac-flash-in">
        <div class="ac-flash-face front"><span class="tag">Понятие</span><div class="t">${esc(acStripTags(p[0]))}</div><span class="hint">Вспомни значение и переверни</span></div>
        <div class="ac-flash-face back"><span class="tag">Значение</span><div class="t">${esc(acStripTags(p[1]))}</div></div>
      </div>
    </div>
    ${acG.flip ? `<div class="ac-flash-btns">
      <button class="ac-tf-btn no" onclick="acFlashMark(false)">Не вспомнил</button>
      <button class="ac-tf-btn yes" onclick="acFlashMark(true)">Знал</button>
    </div>` : `<div style="height:6px"></div><p class="dim" style="text-align:center;font-size:12px">Тапни карточку, чтобы перевернуть</p>`}`;
}
function acFlashFlip(){ if(!acG) return; acG.flip = !acG.flip; acRenderFlash(document.getElementById('acGameBox')); }
function acFlashMark(known){
  if(!acG) return;
  if(!known) acG.wrong++;
  acG.cur++; acG.flip = false;
  if(acG.cur >= acG.order.length){ acGameFinish(acG.wrong); return; }
  acRenderFlash(document.getElementById('acGameBox'));
}
/* ---------- БЛИЦ: тест на скорость с таймером ---------- */
let acSpeedTimer = null;
function acSpeedStop(){ if(acSpeedTimer){ clearInterval(acSpeedTimer); acSpeedTimer = null; } }
function acSpeedTick(reset){
  if(!acG || acG.type!=='quizspeed') { acSpeedStop(); return; }
  if(reset){ acG.tl = acG.per; }
  acSpeedStop();
  acSpeedTimer = setInterval(()=>{
    if(!acG || acG.type!=='quizspeed'){ acSpeedStop(); return; }
    acG.tl -= 0.1;
    const fill = document.getElementById('acSpeedFill');
    if(fill) fill.style.width = Math.max(0, acG.tl/acG.per*100) + '%';
    const tm = document.getElementById('acSpeedTime');
    if(tm) tm.textContent = Math.max(0, Math.ceil(acG.tl)) + 'с';
    if(acG.tl <= 0){ acSpeedStop(); acSpeedAnswer(-1); }
  }, 100);
}
function acRenderSpeed(box){
  const it = acG.order[acG.cur];
  if(!it){ acGameFinish(acG.wrong); return; }
  const opts = it.o.map((o,k)=>`<button class="ac-speed-opt" id="acSp${k}" onclick="acSpeedAnswer(${k})">${esc(acStripTags(o))}</button>`).join('');
  box.innerHTML = `
    <div class="ac-game-score"><span>Вопрос: <b>${acG.cur+1} / ${acG.order.length}</b></span><span>ошибок: <b>${acG.wrong}</b> · <b id="acSpeedTime">${acG.per}с</b></span></div>
    <div class="ac-speed-bar"><i id="acSpeedFill" style="width:${Math.max(0,acG.tl/acG.per*100)}%"></i></div>
    <div class="ac-speed-q">${esc(acStripTags(it.q))}</div>
    <div class="ac-speed-opts">${opts}</div>`;
}
function acSpeedAnswer(k){
  if(!acG || acG.lock) return;
  acSpeedStop(); acG.lock = true;
  const it = acG.order[acG.cur], correct = it.a;
  const ce = document.getElementById('acSp'+correct); if(ce) ce.classList.add('ok');
  if(k !== correct){ acG.wrong++; const we = document.getElementById('acSp'+k); if(we) we.classList.add('bad'); }
  setTimeout(()=>{
    acG.cur++; acG.lock = false;
    if(acG.cur >= acG.order.length){ acGameFinish(acG.wrong); return; }
    acG.tl = acG.per; acRenderSpeed(document.getElementById('acGameBox')); acSpeedTick(true);
  }, k===correct ? 420 : 900);
}
/* ---------- ЗАПОЛНИ ПРОПУСК ---------- */
function acRenderGap(box){
  const it = acG.items[acG.cur];
  if(!it){ acGameFinish(acG.wrong); return; }
  const opts = it.opts.map((o,k)=>`<button class="ac-gap-opt" id="acGap${k}" onclick="acGapPick(${k})">${esc(o)}</button>`).join('');
  box.innerHTML = `
    <div class="ac-game-score"><span>Пропуск: <b>${acG.cur+1} / ${acG.items.length}</b></span><span>ошибок: <b>${acG.wrong}</b></span></div>
    <div class="ac-gap-sent">${it.html}</div>
    <div class="ac-gap-opts">${opts}</div>`;
}
function acGapPick(k){
  if(!acG || acG.lock) return;
  const it = acG.items[acG.cur], chosen = it.opts[k];
  acG.lock = true;
  const el = document.getElementById('acGap'+k);
  if(chosen.toLowerCase() === it.answer.toLowerCase()){
    if(el) el.classList.add('ok');
    const blank = document.querySelector('#acGameBox .gap-blank'); if(blank){ blank.textContent = it.answer; blank.classList.add('filled'); }
    setTimeout(()=>{ acG.cur++; acG.lock=false; if(acG.cur>=acG.items.length){ acGameFinish(acG.wrong); return; } acRenderGap(document.getElementById('acGameBox')); }, 620);
  } else {
    acG.wrong++;
    if(el) el.classList.add('bad');
    setTimeout(()=>{ if(el) el.classList.remove('bad'); acG.lock=false; }, 480);
  }
}
/* ---------- ПРАВДА/ЛОЖЬ ПО ПОЛКАМ ---------- */
function acRenderBucket(box){
  const chips = acG.items.map((it,i)=> it.placed===null
    ? `<button class="ac-bkt-chip" id="acBkt${i}" onclick="acBucketPick(${i})">${esc(it.text)}: <b>${esc(it.opt)}</b></button>` : '').join('');
  const col = (val)=> acG.items.map((it,i)=> it.placed===val
    ? `<button class="ac-bkt-item ${acG.done?(it.isTrue===(val==='t')?'ok':'bad'):''}" onclick="${acG.done?'':`acBucketUnplace(${i})`}">${esc(it.text)}: <b>${esc(it.opt)}</b></button>` : '').join('');
  const remaining = acG.items.some(it=>it.placed===null);
  box.innerHTML = `
    <div class="ac-game-score"><span>Разложи все утверждения</span><span>ошибок: <b>${acG.wrong}</b></span></div>
    <div class="ac-bkt-pool">${chips || '<span class="dim" style="font-size:12px">Все разложены — проверь себя</span>'}</div>
    <div class="ac-bkt-cols">
      <div class="ac-bkt-col t"><div class="h">${I('check2')} Правда</div>${col('t')||'<span class="ph">выбери сюда</span>'}</div>
      <div class="ac-bkt-col f"><div class="h">${I('flag')} Ложь</div>${col('f')||'<span class="ph">выбери сюда</span>'}</div>
    </div>
    ${!acG.done ? `<div style="height:10px"></div><button class="btn" ${remaining?'disabled style="opacity:.5"':''} onclick="acBucketCheck()">Проверить</button>` : `<div style="height:8px"></div><button class="btn ghost" onclick="acGameStart()">Сыграть ещё раз</button>`}`;
}
let acBktSel = null;
function acBucketPick(i){
  // тап-модель: тап по чипу выбирает, затем тап по колонке кладёт
  acBktSel = i;
  const chip = document.getElementById('acBkt'+i);
  document.querySelectorAll('#acGameBox .ac-bkt-chip').forEach(c=>c.classList.remove('sel'));
  if(chip){ chip.classList.add('sel');
    // показать подсказку выбора колонок прямо через быстрые кнопки
  }
  // авто-раскладка через две кнопки-колонки: навесим обработчик на клик по колонкам
  const cols = document.querySelectorAll('#acGameBox .ac-bkt-col');
  cols.forEach(c=>{ c.onclick = ()=>{ if(acBktSel===null) return; acG.items[acBktSel].placed = c.classList.contains('t')?'t':'f'; acBktSel=null; acRenderBucket(document.getElementById('acGameBox')); }; });
}
function acBucketUnplace(i){ if(acG.done) return; acG.items[i].placed = null; acRenderBucket(document.getElementById('acGameBox')); }
function acBucketCheck(){
  if(acG.items.some(it=>it.placed===null)) return;
  acG.wrong = acG.items.filter(it=> (it.placed==='t') !== it.isTrue).length;
  acG.done = true;
  acRenderBucket(document.getElementById('acGameBox'));
  try{ window.okoTrack && okoTrack('lesson_complete', {group:(typeof acG!=='undefined'&&acG.k)||'', wrong:(typeof acG!=='undefined'?acG.wrong:null)}); }catch(e){}
  const ls = acLS(); ls.gameWrong = acG.wrong;
  if(!ls.game){ ls.game = true; toast('Мини-игра пройдена · +20% к уроку'); }
  acSave(); acRenderProgressBox(); acRenderCertBox(); acBadgeSync(); acAfterCheckpoint();
}
/* ---------- УБЕРИ НЕВЕРНОЕ ---------- */
function acRenderOdd(box){
  const it = acG.q[acG.cur];
  if(!it){ acGameFinish(acG.wrong); return; }
  const dead = acG.dead[acG.cur] || {};
  const alive = it.o.map((_,k)=>k).filter(k=>!dead[k]);
  const opts = it.o.map((o,k)=>{
    if(dead[k]) return `<button class="ac-odd-opt dead" disabled>${esc(acStripTags(o))}</button>`;
    const last = alive.length===1;
    return `<button class="ac-odd-opt ${last&&k===it.a?'ok':''}" onclick="acOddPick(${k})">${esc(acStripTags(o))}</button>`;
  }).join('');
  box.innerHTML = `
    <div class="ac-game-score"><span>Вопрос: <b>${acG.cur+1} / ${acG.q.length}</b></span><span>ошибок: <b>${acG.wrong}</b></span></div>
    <div class="ac-odd-q">${esc(acStripTags(it.q))}</div>
    <p class="dim" style="font-size:12px;margin:2px 0 8px">Вычёркивай неверные варианты</p>
    <div class="ac-odd-opts">${opts}</div>`;
}
function acOddPick(k){
  if(!acG || acG.lock) return;
  const it = acG.q[acG.cur];
  if(!acG.dead[acG.cur]) acG.dead[acG.cur] = {};
  if(k === it.a){
    // попытались убрать правильный — ошибка, трясём
    acG.wrong++; acG.lock = true;
    const el = document.querySelectorAll('#acGameBox .ac-odd-opt')[k]; if(el) el.classList.add('shake');
    toast('Это верный ответ — его убирать нельзя');
    setTimeout(()=>{ if(el) el.classList.remove('shake'); acG.lock=false; }, 480);
    return;
  }
  acG.dead[acG.cur][k] = true;
  const alive = it.o.map((_,i)=>i).filter(i=>!acG.dead[acG.cur][i]);
  if(alive.length === 1){
    // остался правильный — следующий вопрос
    acG.lock = true;
    acRenderOdd(document.getElementById('acGameBox'));
    setTimeout(()=>{ acG.cur++; acG.lock=false; if(acG.cur>=acG.q.length){ acGameFinish(acG.wrong); return; } acRenderOdd(document.getElementById('acGameBox')); }, 640);
  } else {
    acRenderOdd(document.getElementById('acGameBox'));
  }
}

/* ---------- прогресс: чек-лист 5 пунктов ---------- */
function acRenderProgressBox(){
  const box = document.getElementById('acProgressBox');
  if(!box) return;
  const pct = acLessonPct(acL);
  const ci = acCourseOf(acL);
  const lastInCourse = (acL === acCourseFirst(ci) + AC_COURSES[ci].count - 1);
  const next = (!lastInCourse && acLessonDone(acL))
    ? `<p class="dim" style="font-size:12px;text-align:center;margin-top:4px">Урок ${acLocalNo(acL)+1} «${esc(AC_COURSE[acL+1].title)}» открыт</p>` : '';
  box.innerHTML = acItems(acL).map(([label,done])=>`
    <div class="ac-check-row ${done?'done':''}">
      <span class="ac-check-ic"><svg class="i"><use href="#i-check2"/></svg></span>
      <span>${label}</span><span class="pct">${done?'+20%':'—'}</span>
    </div>`).join('') + `
    <div class="progress" style="margin:12px 0 6px"><i style="width:${pct}%"></i></div>
    <p class="dim" style="font-size:12px;text-align:center">Урок пройден на <b style="color:var(--accent)">${pct}%</b></p>` + next;
}

/* ================= КОНСПЕКТ УРОКА (лонгрид из слайдов) ================= */
function acNotesStrip(h){ return String(h).replace(/<[^>]+>/g,''); }
function acNotesWords(){
  return acCur().slides.reduce((n,s)=>n + acNotesStrip(s.t + ' ' + s.pts.join(' ')).split(/\s+/).length, 0);
}
function acNotesHtml(){
  const L = acCur();
  const mins = Math.max(2, Math.round(acNotesWords() / 150));
  const secs = L.slides.map((s,i)=>{
    const p = s.pts.map(t=>t.trim().replace(/[.!…]+\s*$/,'')).join('. ') + '.';
    return `<div class="ac-note-sec"><span class="n">${String(i+1).padStart(2,'0')}</span><h4>${s.t}</h4><p>${p}</p></div>`;
  }).join('');
  return `
  <div class="card ac-notes" id="acNotes">
    <button class="ac-notes-head" onclick="acNotesToggle()">
      <span class="ico">${I('file')}</span>
      <span class="meta"><b>Текстовая версия урока</b><span>${L.slides.length} разделов · ~${mins} мин чтения · весь материал без видео</span></span>
      <svg class="i chev"><use href="#i-chev"/></svg>
    </button>
    <div class="ac-notes-body">
      <p class="ac-notes-intro">Полный конспект урока «${L.title}» — все тезисы слайдов, собранные в один читабельный текст. Удобно повторить перед тестом или сохранить себе.</p>
      ${secs}
      <div style="height:14px"></div>
      <button class="btn ghost" style="width:100%" onclick="acNotesDownload()">${I('file')} Скачать .txt</button>
    </div>
  </div>`;
}
function acNotesToggle(){
  const el = document.getElementById('acNotes');
  if(el) el.classList.toggle('open');
}
function acNotesTxt(){
  const L = acCur();
  let out = 'АКАДЕМИЯ OKO · КУРС «НЕЙРОСЕТИ 2026»\r\n'
          + 'КОНСПЕКТ · УРОК ' + (acL+1) + ' — ' + L.title.toUpperCase() + '\r\n'
          + '='.repeat(46) + '\r\n\r\n';
  L.slides.forEach((s,i)=>{
    out += (i+1) + '. ' + acNotesStrip(s.t).toUpperCase() + '\r\n';
    s.pts.forEach(p=>{ out += '   — ' + acNotesStrip(p) + '\r\n'; });
    out += '\r\n';
  });
  out += '-'.repeat(46) + '\r\nПройди тест и получи официальный сертификат OKO:\r\nhttps://true-journey-418.higgsfield.app\r\n';
  return out;
}
function acNotesDownload(){
  try{
    const blob = new Blob(['\ufeff' + acNotesTxt()], {type:'text/plain;charset=utf-8'});
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = 'OKO-урок-' + (acL+1) + '-конспект.txt';
    document.body.appendChild(a); a.click();
    setTimeout(()=>{ try{ URL.revokeObjectURL(a.href); }catch(e){} a.remove(); }, 800);
    toast('Конспект сохранён: урок ' + (acL+1) + '.txt');
  }catch(e){
    acCopyText(acNotesTxt(), 'Скачивание недоступно — конспект скопирован в буфер');
  }
}

/* ================= НАПОМИНАНИЕ (раз в сутки при входе) =================
   Показываем только при входе в приложение/Академию, раз в сутки, и НИКОГДА
   поверх активного действия: открытый sheet, игра, мини-апп, попап или
   полноэкранная вьюха → откладываем и пробуем позже. */
function acRemindBusy(){
  if(document.getElementById('okoPopup')) return true;         // уже показан попап
  if(document.querySelector('.sheet.open')) return true;       // открыт bottom-sheet
  /* открыта полноэкранная вьюха (звонок / регистрация / канал / профиль и т.п.) */
  if(document.querySelector('#callScreen.open, #regView.open, .ma-view.open')) return true;
  /* открыт мини-апп внутри хаба «Мини-аппы» (сетка спрятана — значит внутри вьюхи) */
  const mg = document.getElementById('maGrid');
  if(mg && mg.style.display === 'none') return true;
  /* пользователь в разделе Игры / Мини-аппы — не мешаем активному занятию */
  const act = document.querySelector('.screen.active');
  if(act && /^screen-(games|mini)$/.test(act.id)) return true;
  return false;
}
function acRemindCheck(){
  try{
    if(!localStorage.getItem('oko-auth')) return;            // ещё не в приложении
    /* контекст-осознанность: не чаще одного раза за сессию — постоянный флаг */
    try{ if(sessionStorage.getItem('oko-ac-nudge-shown')) return; }catch(e){}
    const scr = document.getElementById('screen-academy');
    if(scr && scr.classList.contains('active')) return;      // уже в академии — напоминать незачем
    /* показываем ТОЛЬКО на Ленте (или в Академии) — НИКОГДА поверх Кошелька / TON /
       Чатов / Рекламы / Игр / Настроек, чтобы не прерывать денежные и чат-задачи.
       Если пользователь на другом экране — тихо ждём перехода на Ленту, ничего не показывая. */
    const act = document.querySelector('.screen.active');
    if(!act || !/^screen-(feed|academy)$/.test(act.id)){ setTimeout(acRemindCheck, 15000); return; }
    const today = acDayStr();
    if(acS.remindDay === today) return;                      // раз в сутки
    const nx = acNextLesson();
    if(nx < 0) return;                                       // всё пройдено — не беспокоим
    if(acRemindBusy()){ setTimeout(acRemindCheck, 20000); return; }  // занят — отложить
    const pct = acLessonPct(nx), title = esc(AC_COURSE[nx].title), lno = acLocalNo(nx);
    let body, label;
    if(pct <= 0){
      body = 'Начни урок ' + lno + ' «' + title + '» — тебя ждёт разбор темы, слайды, тест и практика.';
      label = 'Начать урок';
    } else {
      body = 'Продолжи урок ' + lno + ' «' + title + '» — пройдено '
           + '<b style="color:var(--accent)">' + pct + '%</b>, осталось ' + (100 - pct) + '%.';
      label = 'Продолжить урок';
    }
    acS.remindDay = today; acSave();
    try{ sessionStorage.setItem('oko-ac-nudge-shown','1'); }catch(e){}  // максимум один раз за сессию
    showPopup({ico:'star', title:'Академия OKO', body:body,
      actions:[
        {label:label, onclick:()=>{ showTab('academy'); acOpenLesson(nx); }},
        {label:'Позже', ghost:true}
      ]});
  }catch(e){}
}

/* ================= ПОДЕЛИТЬСЯ СЕРТИФИКАТОМ ================= */
function acCopyText(t, okMsg){
  const done = ()=>toast(okMsg || 'Скопировано в буфер');
  const fallback = ()=>{
    try{
      const ta = document.createElement('textarea');
      ta.value = t; ta.style.cssText = 'position:fixed;top:-200px;opacity:0';
      document.body.appendChild(ta); ta.select();
      document.execCommand('copy'); ta.remove(); done();
    }catch(e){ toast('Не удалось скопировать'); }
  };
  if(navigator.clipboard && navigator.clipboard.writeText)
    navigator.clipboard.writeText(t).then(done).catch(fallback);
  else fallback();
}
function acCertShare(i){
  const c = (typeof i === 'number') ? acS.certs[i] : acCertRec();
  if(!c){ toast('Сертификат ещё не выдан'); return; }
  const text = 'Официальный сертификат Академии OKO ' + c.no
    + ' — ' + acCertLabel(c)
    + ' пройден, тест ' + c.score + '%. Учись со мной в OKO: https://true-journey-418.higgsfield.app';
  if(navigator.share){
    navigator.share({title:'Сертификат Академии OKO', text:text}).catch(()=>{});
  } else {
    acCopyText(text, 'Текст сертификата скопирован — вставь в любой чат');
  }
}

/* ================= СЕРТИФИКАТ ================= */
function acRenderCertBox(){
  const box = document.getElementById('acCertBox');
  if(!box) return;
  const ci = acCourseOf(acL);
  const dir = AC_COURSES[ci];
  const cert = acDirCert(ci);
  const idx = acCourseIdx(ci);
  const doneCnt = idx.filter(i=>acLessonDone(i)).length;
  const left = dir.count - doneCnt;
  if(cert){
    box.innerHTML = `
      <div class="card ac-cert-card ready">
        <div class="ac-cert-head">
          <span class="ico"><svg class="i"><use href="#i-star"/></svg></span>
          <div><h3>Сертификат направления получен</h3><p>Официальный диплом «${esc(dir.title)}» с печатью и подписью Академии OKO</p></div>
        </div>
        <span class="ac-cert-no">${esc(cert.no)} · ${esc(cert.date)} · тест ${cert.score}%</span>
        <div class="ac-cert-actions">
          <button class="btn" onclick="acCertDownload()">${I('file')} Скачать PNG</button>
          <button class="btn ghost" onclick="acCertShow()">${I('eye')} Показать</button>
          <button class="btn ghost ac-ico-btn" onclick="acCertShare()" title="Поделиться" aria-label="Поделиться">${I('share')}</button>
        </div>
      </div>`;
    return;
  }
  if(acCertEligible()){
    box.innerHTML = `
      <div class="card ac-cert-card ready">
        <div class="ac-cert-head">
          <span class="ico"><svg class="i"><use href="#i-star"/></svg></span>
          <div><h3>Сертификат направления готов</h3><p>Все ${dir.count} уроков «${esc(dir.title)}» пройдены — забирай именной диплом</p></div>
        </div>
        <button class="btn" onclick="acIssueCert()">${I('star')} Получить сертификат</button>
      </div>`;
    return;
  }
  box.innerHTML = `
    <div class="card ac-cert-card">
      <div class="ac-cert-head">
        <span class="ico" style="background:var(--raised);color:var(--dim)"><svg class="i"><use href="#i-lock"/></svg></span>
        <div><h3>Сертификат за направление «${esc(dir.title)}»</h3><p>Именной документ с печатью и подписью — за прохождение ВСЕГО направления</p></div>
      </div>
      <div class="ac-cert-req">
        <div class="${doneCnt>0?'ok':''}">${I(doneCnt>0?'check2':'circle-play')} Пройдено уроков: ${doneCnt} из ${dir.count}</div>
        <div>${I('poll')} Осталось ${left} ${acPlural(left,['урок','урока','уроков'])} — уроки можно проходить в любом порядке</div>
      </div>
    </div>`;
}

function acIssueCert(){
  const ci = acCourseOf(acL);
  if(acDirCert(ci) || !acCourseDone(ci)) return;          // диплом один на направление, и только когда всё пройдено
  const idx = acCourseIdx(ci);
  const avg = Math.round(idx.reduce((sum,i)=>sum + ((acS.lessons[i]&&acS.lessons[i].testScore)||0), 0) / idx.length);
  const cert = {
    no: 'OKO-CERT-' + String(Math.floor(1e5 + Math.random()*9e5)),
    date: new Date().toLocaleDateString('ru-RU'),
    score: avg,
    name: (typeof PROFILE!=='undefined' && PROFILE.name) ? PROFILE.name : 'Слушатель Академии',
    dir: AC_COURSES[ci].id,
    courseTitle: AC_COURSES[ci].title
  };
  acS.certs.unshift(cert);
  acSave();
  acCertUrl = null;
  toast('Сертификат направления «' + AC_COURSES[ci].title + '» выдан: ' + cert.no);
  acBadgeSync();
  acRenderCertBox(); acRenderProgressBox();
  acCertCelebrate(()=>acCertShow());
}

/* эффект выдачи сертификата: вспышка лаймовых лучей + печать, затем показ */
function acCertCelebrate(cb){
  try{
    const old = document.getElementById('acBurst'); if(old) old.remove();
    const el = document.createElement('div');
    el.id = 'acBurst'; el.className = 'ac-burst';
    let rays = '';
    for(let i=0;i<14;i++) rays += `<i style="--a:${(i*360/14)}deg;--d:${(i%3)*0.04}s"></i>`;
    let conf = '';
    for(let i=0;i<22;i++){
      const x = (Math.random()*100).toFixed(1), dl = (Math.random()*0.35).toFixed(2),
            rot = (Math.random()*360).toFixed(0), sx = (Math.random()*0.7+0.6).toFixed(2);
      conf += `<b style="left:${x}%;--dl:${dl}s;--rot:${rot}deg;--sx:${sx}"></b>`;
    }
    el.innerHTML = `<div class="ac-burst-core">
        <span class="ac-burst-rays">${rays}</span>
        <span class="ac-burst-seal">${I('star')}</span>
      </div>
      <div class="ac-burst-txt">Сертификат выдан</div>
      <div class="ac-burst-conf">${conf}</div>`;
    document.body.appendChild(el);
    requestAnimationFrame(()=>el.classList.add('go'));
    setTimeout(()=>{ try{ el.remove(); }catch(e){} if(typeof cb==='function') cb(); }, 1250);
  }catch(e){ if(typeof cb==='function') cb(); }
}

/* ================= ВАУ-МОМЕНТ: УРОК ОСВОЁН (все 5 этапов на 100%) =================
   Отдельный праздничный оверлей — срабатывает ровно один раз, когда закрыт ПОСЛЕДНИЙ
   из пяти чек-пойнтов урока. Не путать с выдачей сертификата (там свой burst-эффект). */
function acAfterCheckpoint(){
  try{
    const ls = acLS();
    if(acLessonPct(acL) === 100 && !ls.mastered){
      ls.mastered = true; acSave();
      acLessonMaster();
    }
  }catch(e){}
}
function acMasterClose(){
  const el = document.getElementById('acMaster'); if(!el) return;
  el.classList.remove('go');
  setTimeout(()=>{ try{ el.remove(); }catch(e){} }, 260);
}
function acMasterAct(kind, idx){
  acMasterClose();
  setTimeout(()=>{
    if(kind === 'cert') acIssueCert();
    else if(kind === 'next') acOpenLesson(idx);
  }, 180);
}
function acLessonMaster(){
  try{
    const old = document.getElementById('acMaster'); if(old) old.remove();
    const L = acCur(), ls = acLS(), ci = acCourseOf(acL);
    const lastInCourse = (acL === acCourseFirst(ci) + AC_COURSES[ci].count - 1);
    const nextIdx = acL + 1;
    /* умный CTA: сначала — забрать сертификат, иначе — следующий урок, иначе просто закрыть */
    let cta = '';
    if(acCertEligible())
      cta = `<button class="btn ac-master-cta" onclick="acMasterAct('cert')">${I('star')} Получить сертификат</button>`;
    else if(!lastInCourse && acUnlocked(nextIdx))
      cta = `<button class="btn ac-master-cta" onclick="acMasterAct('next',${nextIdx})">${I('circle-play')} Следующий урок</button>`;
    const pills = ['Видео','Слайды','Тест','Практика','Игра']
      .map((t,i)=>`<span class="ac-master-pill" style="--pd:${(i*0.06).toFixed(2)}s">${I('check2')}${t}</span>`).join('');
    let rays = ''; for(let i=0;i<12;i++) rays += `<i style="--a:${(i*360/12)}deg;--d:${(i%3)*0.05}s"></i>`;
    let conf = '';
    for(let i=0;i<20;i++){
      const x=(Math.random()*100).toFixed(1), dl=(Math.random()*0.4).toFixed(2),
            rot=(Math.random()*360).toFixed(0), sx=(Math.random()*0.7+0.6).toFixed(2);
      conf += `<b style="left:${x}%;--dl:${dl}s;--rot:${rot}deg;--sx:${sx}"></b>`;
    }
    const el = document.createElement('div');
    el.id = 'acMaster'; el.className = 'ac-master';
    el.innerHTML = `
      <div class="ac-master-conf">${conf}</div>
      <div class="ac-master-card" onclick="event.stopPropagation()">
        <div class="ac-master-badge">
          <span class="ac-master-rays">${rays}</span>
          <span class="ac-master-seal">${I('crown')}</span>
        </div>
        <div class="ac-master-kick">Урок пройден на 100%</div>
        <h3 class="ac-master-title">Урок освоен</h3>
        <p class="ac-master-sub">«${esc(L.title)}» — все пять этапов закрыты</p>
        <div class="ac-master-pills">${pills}</div>
        <div class="ac-master-stat">${I('poll')} Тест — <b>${ls.testScore||0}%</b></div>
        <div class="ac-master-actions">
          ${cta}
          <button class="btn ${cta?'ghost':''} ac-master-dismiss" onclick="acMasterClose()">${cta?'Позже':'Отлично'}</button>
        </div>
      </div>`;
    el.addEventListener('click', acMasterClose);   // тап по фону — закрыть
    document.body.appendChild(el);
    requestAnimationFrame(()=>el.classList.add('go'));
  }catch(e){}
}

/* --- отрисовка canvas 1600×1131 --- */
function acSigImage(cb){
  if(typeof SIGNATURE_B64_URL === 'undefined'){ cb(null); return; }
  const img = new Image();
  img.onload = ()=>{
    try{
      const c = document.createElement('canvas');
      c.width = img.width; c.height = img.height;
      const x = c.getContext('2d');
      x.drawImage(img, 0, 0);
      const d = x.getImageData(0, 0, c.width, c.height), p = d.data;
      for(let i=0; i<p.length; i+=4){
        const lum = (p[i]+p[i+1]+p[i+2]) / 3;
        p[i+3] = Math.round(p[i+3] * (1 - lum/255)); // тёмный штрих → непрозрачный
        p[i]=232; p[i+1]=240; p[i+2]=255;            // перекрас в светлый (для тёмного фона)
      }
      x.putImageData(d, 0, 0);
      cb(c);
    }catch(e){ cb(img); }
  };
  img.onerror = ()=>cb(null);
  img.src = SIGNATURE_B64_URL;
}
function acRingText(ctx, cx, cy, r, text, font){
  ctx.save();
  ctx.font = font;
  ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
  const step = (Math.PI*2) / text.length;
  for(let i=0; i<text.length; i++){
    ctx.save();
    ctx.translate(cx, cy);
    ctx.rotate(i*step);
    ctx.translate(0, -r);
    ctx.fillText(text[i], 0, 0);
    ctx.restore();
  }
  ctx.restore();
}
function acDrawSeal(ctx, cx, cy, r){
  const blue = '#2b4fd8';
  ctx.save();
  ctx.translate(cx, cy);
  ctx.rotate(-0.12);
  ctx.translate(-cx, -cy);
  // лёгкая подложка, чтобы синяя печать читалась на тёмном
  ctx.beginPath(); ctx.arc(cx, cy, r+6, 0, 7);
  ctx.fillStyle = 'rgba(255,255,255,.07)'; ctx.fill();
  ctx.strokeStyle = blue; ctx.fillStyle = blue; ctx.globalAlpha = .96;
  ctx.beginPath(); ctx.arc(cx, cy, r, 0, 7); ctx.lineWidth = 4; ctx.stroke();
  ctx.beginPath(); ctx.arc(cx, cy, r-7, 0, 7); ctx.lineWidth = 1.5; ctx.stroke();
  ctx.beginPath(); ctx.arc(cx, cy, r*0.6, 0, 7); ctx.lineWidth = 2; ctx.stroke();
  acRingText(ctx, cx, cy, r-24, SEAL_REQ.fio + ' · ' + SEAL_REQ.inn + ' · ', '700 14px Montserrat, Arial');
  acRingText(ctx, cx, cy, r*0.6 - 15, SEAL_REQ.geo + ' · ОФИЦИАЛЬНЫЙ ДОКУМЕНТ · ', '600 9px Montserrat, Arial');
  ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
  ctx.font = '800 21px Montserrat, Arial';
  ctx.fillText(SEAL_REQ.brand, cx, cy-6);
  ctx.font = '600 11px Montserrat, Arial';
  ctx.fillText('АКАДЕМИЯ', cx, cy+16);
  ctx.restore();
}
function acMakeCert(cert, cb){
  const ready = (document.fonts && document.fonts.load)
    ? Promise.all([
        document.fonts.load('100px "Bebas Neue"'),
        document.fonts.load('700 30px Montserrat'),
      ]).catch(()=>{})
    : Promise.resolve();
  ready.then(()=>acSigImage(sig=>{
    const W = 1600, H = 1131;
    const cv = document.createElement('canvas');
    cv.width = W; cv.height = H;
    const ctx = cv.getContext('2d');
    const lime = '#9AFF00';
    // фон + тонкая сетка
    ctx.fillStyle = '#0a0a0a'; ctx.fillRect(0, 0, W, H);
    ctx.strokeStyle = 'rgba(154,255,0,.05)'; ctx.lineWidth = 1;
    for(let x=0; x<=W; x+=64){ ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,H); ctx.stroke(); }
    for(let y=0; y<=H; y+=64){ ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(W,y); ctx.stroke(); }
    // мягкое свечение в углу
    const g = ctx.createRadialGradient(W-220, 180, 40, W-220, 180, 560);
    g.addColorStop(0, 'rgba(154,255,0,.10)'); g.addColorStop(1, 'rgba(154,255,0,0)');
    ctx.fillStyle = g; ctx.fillRect(0, 0, W, H);
    // двойная лаймовая рамка
    ctx.strokeStyle = lime; ctx.lineWidth = 5; ctx.strokeRect(38, 38, W-76, H-76);
    ctx.lineWidth = 1.5; ctx.globalAlpha = .65; ctx.strokeRect(58, 58, W-116, H-116); ctx.globalAlpha = 1;
    // уголки
    ctx.lineWidth = 5;
    [[38,38,1,1],[W-38,38,-1,1],[38,H-38,1,-1],[W-38,H-38,-1,-1]].forEach(([x,y,dx,dy])=>{
      ctx.beginPath(); ctx.moveTo(x+dx*44, y); ctx.lineTo(x, y); ctx.lineTo(x, y+dy*44); ctx.stroke();
    });
    try{ ctx.letterSpacing = '6px'; }catch(e){}
    ctx.textAlign = 'center';
    // шапка
    ctx.fillStyle = 'rgba(255,255,255,.55)';
    ctx.font = '42px "Bebas Neue", Impact, sans-serif';
    ctx.fillText('А К А Д Е М И Я   O K O', W/2, 148);
    // заголовок
    ctx.fillStyle = lime;
    ctx.shadowColor = 'rgba(154,255,0,.45)'; ctx.shadowBlur = 34;
    ctx.font = '150px "Bebas Neue", Impact, sans-serif';
    ctx.fillText('СЕРТИФИКАТ', W/2, 300);
    ctx.shadowBlur = 0;
    try{ ctx.letterSpacing = '2px'; }catch(e){}
    ctx.fillStyle = 'rgba(255,255,255,.6)';
    ctx.font = '500 27px Montserrat, Arial';
    ctx.fillText('подтверждает, что', W/2, 372);
    // имя
    ctx.fillStyle = '#fff';
    ctx.font = '96px "Bebas Neue", Impact, sans-serif';
    ctx.fillText(cert.name.toUpperCase(), W/2, 486);
    ctx.strokeStyle = 'rgba(154,255,0,.5)'; ctx.lineWidth = 2;
    ctx.beginPath(); ctx.moveTo(W/2-260, 512); ctx.lineTo(W/2+260, 512); ctx.stroke();
    // курс / урок
    const gi = (typeof cert.lesson === 'number' ? cert.lesson : 0);
    const lessonNo = cert.localNo || (acLocalNo ? acLocalNo(gi) : gi+1);
    const courseTitle = cert.courseTitle || AC_COURSES[acCourseOf(gi)].title;
    /* Сертификат выдаётся ЗА ВСЁ НАПРАВЛЕНИЕ (cert.dir), а документ печатал
       «успешно прошёл урок 1» и название первого урока — неправда на бланке.
       Старые одноурочные сертификаты (без cert.dir) печатаются как раньше. */
    const isDir = !!cert.dir;
    const lessonTitle = '«' + (isDir ? courseTitle : (cert.lessonTitle || AC_COURSE[0].title)).toUpperCase() + '»';
    ctx.fillStyle = 'rgba(255,255,255,.6)';
    ctx.font = '500 26px Montserrat, Arial';
    ctx.fillText(isDir ? 'успешно прошёл направление' : ('успешно прошёл урок ' + lessonNo), W/2, 566);
    ctx.fillStyle = lime;
    let fs = 64;
    ctx.font = fs + 'px "Bebas Neue", Impact, sans-serif';
    while(fs > 40 && ctx.measureText(lessonTitle).width > W-260){
      fs -= 4;
      ctx.font = fs + 'px "Bebas Neue", Impact, sans-serif';
    }
    ctx.fillText(lessonTitle, W/2, 646);
    ctx.fillStyle = 'rgba(255,255,255,.6)';
    ctx.font = '500 24px Montserrat, Arial';
    ctx.fillText(isDir ? 'все уроки направления пройдены · Академия OKO'
                       : ('курса «' + courseTitle + '» Академии OKO'), W/2, 696);
    // чип результата
    const chipT = 'Результат теста: ' + cert.score + '%';
    ctx.font = '700 26px Montserrat, Arial';
    const cw = ctx.measureText(chipT).width + 66;
    ctx.fillStyle = 'rgba(154,255,0,.13)';
    ctx.beginPath();
    if(ctx.roundRect) ctx.roundRect(W/2-cw/2, 730, cw, 58, 29); else ctx.rect(W/2-cw/2, 730, cw, 58);
    ctx.fill();
    ctx.strokeStyle = lime; ctx.lineWidth = 1.6; ctx.stroke();
    ctx.fillStyle = lime;
    ctx.fillText(chipT, W/2, 768);
    // дата и номер (слева внизу)
    ctx.textAlign = 'left';
    ctx.fillStyle = 'rgba(255,255,255,.45)';
    ctx.font = '600 19px Montserrat, Arial';
    ctx.fillText('ДАТА ВЫДАЧИ', 120, 878);
    ctx.fillStyle = '#fff';
    ctx.font = '46px "Bebas Neue", Impact, sans-serif';
    ctx.fillText(cert.date, 120, 928);
    ctx.fillStyle = 'rgba(255,255,255,.45)';
    ctx.font = '600 19px Montserrat, Arial';
    ctx.fillText('НОМЕР', 120, 972);
    ctx.fillStyle = lime;
    ctx.font = '700 26px Montserrat, Arial';
    ctx.fillText('№ ' + cert.no, 120, 1004);
    // подпись (по центру-слева внизу)
    const sx1 = 560, sx2 = 880, sy = 952;
    ctx.strokeStyle = 'rgba(255,255,255,.55)'; ctx.lineWidth = 2;
    ctx.beginPath(); ctx.moveTo(sx1, sy); ctx.lineTo(sx2, sy); ctx.stroke();
    if(sig){
      const sw = 300, sh = sw * (sig.height / sig.width);
      ctx.drawImage(sig, (sx1+sx2)/2 - sw/2, sy - sh*0.82, sw, sh);
    }
    ctx.fillStyle = '#fff';
    ctx.font = '600 24px Montserrat, Arial';
    ctx.fillText('/ Ильясов Д.А. /', sx2 + 18, sy + 8);
    ctx.fillStyle = 'rgba(255,255,255,.4)';
    ctx.font = '500 16px Montserrat, Arial';
    ctx.fillText('подпись руководителя Академии', sx1, sy + 32);
    // печать (справа внизу)
    acDrawSeal(ctx, 1350, 900, 138);
    // реквизиты снизу мелко
    ctx.textAlign = 'center';
    ctx.fillStyle = 'rgba(255,255,255,.34)';
    ctx.font = '500 15px Montserrat, Arial';
    ctx.fillText(SEAL_REQ.fio + ' · ' + SEAL_REQ.inn + ' · ' + SEAL_REQ.brand + ' · okoteam.top@gmail.com', W/2, 1076);
    cb(cv.toDataURL('image/png'));
  }));
}

function acCertRec(i){
  if(typeof i === 'number' && acS.certs[i]) return acS.certs[i];
  if(acCertShownNo){
    const shown = acS.certs.find(c=>c.no===acCertShownNo);
    if(shown) return shown;
  }
  return (acView==='lesson' ? acDirCert(acCourseOf(acL)) : null) || acS.certs[0] || null;
}
function acCertShow(i){
  const cert = (typeof i === 'number') ? acS.certs[i] : (acDirCert(acCourseOf(acL)) || acCertRec());
  if(!cert){ toast('Сертификат ещё не выдан'); return; }
  const open = url=>{
    const full = document.getElementById('acCertFull');
    const img = document.getElementById('acCertFullImg');
    if(!full || !img) return;
    img.src = url;
    full.classList.add('open');
  };
  if(acCertUrl && acCertShownNo === cert.no){ open(acCertUrl); return; }
  acMakeCert(cert, url=>{ acCertUrl = url; acCertShownNo = cert.no; open(url); });
}
function acCertFullClose(){
  const full = document.getElementById('acCertFull');
  if(full) full.classList.remove('open');
}
function acCertDownload(){
  const cert = acCertRec();
  if(!cert){ toast('Сертификат ещё не выдан'); return; }
  const dl = url=>{
    const a = document.createElement('a');
    a.href = url; a.download = cert.no + '.png';
    document.body.appendChild(a); a.click(); a.remove();
    toast('PNG сохранён: ' + cert.no + '.png');
  };
  if(acCertUrl && acCertShownNo === cert.no){ dl(acCertUrl); return; }
  acMakeCert(cert, url=>{ acCertUrl = url; acCertShownNo = cert.no; dl(url); });
}

/* ================= СЕРТИФИКАТЫ В ПРОФИЛЕ =================
   Именные сертификаты Академии показываем прямо в профиле (карточка после
   ачивок). Инъекция через chain-патч renderMyProfile: на каждый ре-рендер
   профиля пере-вставляем свежую карточку (старую удаляем — без дублей). */
function acProfileCertsHtml(){
  const n = acS.certs.length;
  const head = `
    <div class="ac-pcerts-head">
      <span class="ico">${I('star')}</span>
      <div class="meta"><b>Сертификаты Академии</b><span>${n
        ? n + ' ' + acPlural(n, ['официальный','официальных','официальных']) + ' · печать и подпись'
        : 'Пройди направление — получи именной документ'}</span></div>
      <button class="btn sm ghost" onclick="showTab('academy')">${n?'Все':'В Академию'}</button>
    </div>`;
  if(!n){
    return `<div class="card ac-pcerts empty" id="acProfCerts">${head}${acProfileBadgesHtml()}
      <p class="ac-pcerts-invite">Пройди все уроки направления (порог теста ${AC_PASS}%+) — и получи именной сертификат OKO с печатью и подписью руководителя Академии. Он появится здесь и в разделе «Академия».</p></div>`;
  }
  const show = acS.certs.slice(0, 3).map((c)=>{
    const gi = acS.certs.indexOf(c);
    return `<div class="ac-pcert-row">
      <span class="ico">${I('file')}</span>
      <span class="meta"><span class="t">${esc(acCertLabel(c))}</span>
      <span class="s">${esc(c.no)} · ${esc(c.date)} · тест ${c.score}%</span></span>
      <button class="btn sm ghost ac-ico-btn" onclick="acCertShare(${gi})" title="Поделиться" aria-label="Поделиться">${I('share')}</button>
      <button class="btn sm ghost" onclick="acCertShow(${gi})">Показать</button>
    </div>`;
  }).join('');
  const more = n > 3 ? `<button class="ac-pcerts-more" onclick="showTab('academy')">и ещё ${n-3} ${acPlural(n-3,['сертификат','сертификата','сертификатов'])} — в Академии ${I('chev')}</button>` : '';
  return `<div class="card ac-pcerts" id="acProfCerts">${head}${acProfileBadgesHtml()}<div class="ac-pcert-list">${show}</div>${more}</div>`;
}
/* компактная полоса заслуженных бейджей для профиля */
function acProfileBadgesHtml(){
  const on = acBadgeDefs().filter(b=>b.on);
  if(!on.length) return '';
  return `<div class="ac-pbadges">${on.map(b=>`<span class="ac-pbadge" title="${b.d}">${I(b.ic)}<span>${b.t}</span></span>`).join('')}</div>`;
}
function acPlural(n, forms){
  const a = Math.abs(n) % 100, b = a % 10;
  if(a > 10 && a < 20) return forms[2];
  if(b > 1 && b < 5) return forms[1];
  if(b === 1) return forms[0];
  return forms[2];
}
function acProfileInject(){
  try{
    const anchor = document.getElementById('profAch');
    if(!anchor || !anchor.parentNode) return;
    const old = document.getElementById('acProfCerts');
    if(old) old.remove();
    const wrap = document.createElement('div');
    wrap.innerHTML = acProfileCertsHtml();
    const node = wrap.firstElementChild;
    if(!node) return;
    node.style.marginTop = '14px';
    anchor.parentNode.insertBefore(node, anchor.nextSibling);
  }catch(e){}
}

/* ================= САМОИНИЦИАЛИЗАЦИЯ ================= */
(function acInit(){
  regTitle('academy', 'Академия');
  addSvcTile({id:'academy', label:'Академия', ico:'star', first:true, onclick:()=>showTab('academy')});
  const _prevShowTabAc = showTab;
  showTab = function(t){
    _prevShowTabAc(t);
    if(t === 'academy'){ acStreakTouch(); acRender(); }
  };
  /* сертификаты в профиле: chain-патч renderMyProfile */
  if(typeof renderMyProfile === 'function'){
    const _acPrevRenderProfile = renderMyProfile;
    renderMyProfile = function(){
      _acPrevRenderProfile.apply(this, arguments);
      acProfileInject();
    };
  }
  const scr = document.getElementById('screen-academy');
  if(scr && scr.classList.contains('active')){ acStreakTouch(); acRender(); }
  /* если профиль уже открыт при инициализации — сразу вставим сертификаты */
  const sp = document.getElementById('screen-profile');
  if(sp && sp.classList.contains('active')) acProfileInject();
  setTimeout(acRemindCheck, 2200);           // напоминание об уроке — раз в сутки
})();

/* ===== module: academy-plus ===== */
/* ===== ACADEMY-PLUS: Duolingo + Coursera + Skillshare механики =====
   Всё поверх academy через chain-patch — базовый модуль не трогаем.
   Префикс: acd- (CSS/HTML), apd (JS-функции). */
(function acdInit(){
  if(typeof acRender !== 'function' || typeof AC_COURSES === 'undefined'){
    // academy не загружен — тихо выходим
    return;
  }

  /* =============================================================
     ХРАНИЛИЩЕ: расширяем acS собственными разделами (пишет acSave)
     ============================================================= */
  function apdEnsureStore(){
    if(!acS.notes) acS.notes = {};       // {lessonIdx: text}
    if(!acS.comments) acS.comments = {}; // {lessonIdx: [rec]}
    if(!acS.filter) acS.filter = 'all';  // текущий фильтр каталога
    if(typeof acS.streakWeeksPaid !== 'number') acS.streakWeeksPaid = 0;
  }
  apdEnsureStore();

  /* =============================================================
     3. XP + STREAK: связка с играми (gmXpAdd) — +30 XP за урок,
        +100 XP за каждые 7 дней подряд в учёбе (один раз на неделю).
     ============================================================= */
  const APD_XP_LESSON = 30;
  const APD_XP_STREAK_7 = 100;
  function apdAwardLessonXP(){
    try{
      const ls = acLS();
      if(ls.xpAwarded) return;
      ls.xpAwarded = true; acSave();
      if(typeof gmXpAdd === 'function') gmXpAdd(APD_XP_LESSON, 'academy:lesson:'+acL);
      toast('+' + APD_XP_LESSON + ' XP · урок освоен');
      // Стрик-бонус: за каждую 7-дневную серию — один раз
      const st = acStreak();
      if(st && st.days >= 7){
        const wk = Math.floor(st.days / 7);
        if(wk > (acS.streakWeeksPaid||0)){
          acS.streakWeeksPaid = wk; acSave();
          if(typeof gmXpAdd === 'function') gmXpAdd(APD_XP_STREAK_7, 'academy:streak:'+(wk*7));
          setTimeout(()=>toast('Серия ' + (wk*7) + ' дней · +' + APD_XP_STREAK_7 + ' XP'), 1200);
        }
      }
    }catch(e){}
  }
  // chain-patch: после того, как academy засчитала последний чек-пойнт
  const _apdPrevAfter = window.acAfterCheckpoint;
  window.acAfterCheckpoint = function(){
    try{ _apdPrevAfter && _apdPrevAfter.apply(this, arguments); }catch(e){}
    try{ if(acLessonPct(acL) === 100) apdAwardLessonXP(); }catch(e){}
    apdRerenderStepRail();
  };

  /* =============================================================
     Утилита: инъекция плагин-контента в конец страницы урока
     ============================================================= */
  function apdInjectLessonExtras(){
    try{
      if(acView !== 'lesson') return;
      const root = document.getElementById('acRoot');
      if(!root) return;
      // 2. Прогресс-таймлайн — в самое начало (после кнопки Назад)
      const back = root.querySelector('.ac-back');
      if(back && !root.querySelector('.acd-steprail')){
        const rail = document.createElement('div');
        rail.className = 'acd-steprail';
        rail.id = 'acdStepRail';
        rail.innerHTML = apdStepRailHtml();
        back.insertAdjacentElement('afterend', rail);
      }
      // 7. Рекомендации — перед последней "заглушкой" (перед acCertBox)
      const certBox = document.getElementById('acCertBox');
      if(certBox && !root.querySelector('#acdReco')){
        const reco = document.createElement('div');
        reco.id = 'acdReco';
        reco.innerHTML = apdRecoHtml();
        certBox.parentNode.insertBefore(reco, certBox);
      }
      // 6. Комментарии — после сертификата
      if(certBox && !root.querySelector('#acdComments')){
        const cm = document.createElement('div');
        cm.id = 'acdComments';
        cm.className = 'card acd-comments';
        cm.innerHTML = apdCommentsInnerHtml();
        // вставляем СРАЗУ ПОСЛЕ acCertBox
        certBox.parentNode.insertBefore(cm, certBox.nextSibling);
      }
      // 9. Кнопка «Заметки» (плавающая)
      apdMountNotesFab();
    }catch(e){}
  }
  function apdRemoveLessonExtras(){
    ['acdStepRail','acdReco','acdComments','acdNotesFab'].forEach(id=>{
      const el = document.getElementById(id); if(el) el.remove();
    });
  }

  /* =============================================================
     2. ПРОГРЕСС-ТАЙМЛАЙН УРОКА (Coursera-стиль)
     ============================================================= */
  const APD_STEPS = [
    {k:'slides',   ic:'file',        lbl:'Слайды',   anchor:'#acSlidesBox'},
    {k:'test',     ic:'poll',        lbl:'Тест',     anchor:'#acTestBox'},
    {k:'task',     ic:'edit',        lbl:'Практика', anchor:'#acTaskBox'},
    {k:'game',     ic:'bolt',        lbl:'Игра',     anchor:'#acGameBox'},
    {k:'cert',     ic:'star',        lbl:'Диплом',   anchor:'#acCertBox'},
  ];
  function apdStepStatus(k){
    const ls = acLS() || {};
    if(k === 'cert') return typeof acCourseDone==='function' && acCourseDone(acCourseOf(acL));
    return !!ls[k];
  }
  function apdStepRailHtml(){
    const done = APD_STEPS.filter(s=>apdStepStatus(s.k)).length;
    const pct = Math.round(done / APD_STEPS.length * 100);
    // текущий шаг = первый недоделанный (или последний, если всё)
    let curIdx = APD_STEPS.findIndex(s=>!apdStepStatus(s.k));
    if(curIdx < 0) curIdx = APD_STEPS.length - 1;
    const cells = APD_STEPS.map((s,i)=>{
      const d = apdStepStatus(s.k);
      const c = (i === curIdx && !d);
      const dot = d ? I('check2') : String(i+1);
      const sep = i < APD_STEPS.length-1 ? `<span class="acd-step-bar ${d?'done':''}"></span>` : '';
      return `<button class="acd-step ${d?'done':''} ${c?'cur':''}" onclick="apdStepGo('${s.anchor}')" aria-label="${esc(s.lbl)}">
                <span class="acd-step-dot">${dot}</span>
                <span class="acd-step-lbl">${esc(s.lbl)}</span>
              </button>${sep}`;
    }).join('');
    return `<div class="acd-steprail-head">
        <span>Шаг <b>${Math.min(curIdx+1, APD_STEPS.length)}</b> из ${APD_STEPS.length} · <b>${pct}%</b></span>
        <span class="acd-xp-badge">${I('bolt')} +${APD_XP_LESSON} XP</span>
      </div>
      <div class="acd-steprail-line">${cells}</div>
      <div class="acd-steprail-track"><i style="width:${pct}%"></i></div>`;
  }
  window.apdStepGo = function(anchor){
    const el = document.querySelector(anchor);
    if(!el) return;
    const m = document.querySelector('main');
    if(m){
      const rect = el.getBoundingClientRect();
      m.scrollTo({top: m.scrollTop + rect.top - 90, behavior:'smooth'});
    } else el.scrollIntoView({behavior:'smooth', block:'start'});
  };
  function apdRerenderStepRail(){
    const rail = document.getElementById('acdStepRail');
    if(rail) rail.innerHTML = apdStepRailHtml();
  }

  /* =============================================================
     7. РЕКОМЕНДАЦИЯ: "Ты закончил X → тебе понравится Y"
        Матчинг по инструментам (AC_TOOLS) + слова-теги из заголовков
     ============================================================= */
  function apdLessonTags(i){
    try{
      const L = AC_COURSE[i]; if(!L) return [];
      const hay = ((L.title||'') + ' ' + (L.slides||[]).map(s=>(s.pts||[]).join(' ')).join(' ')).toLowerCase();
      const t = [];
      if(typeof AC_TOOLS !== 'undefined'){
        Object.keys(AC_TOOLS).forEach(k=>{ if(hay.includes(k)) t.push('t:'+k); });
      }
      // ключевые слова-теги (частотные для направлений)
      ['промпт','видео','сторис','упаковка','контент','маркетинг','воронка','монетизация',
       'нейросети','автомат','агент','аналитик','копирайт','триггер','бренд','целевая','воронк']
        .forEach(w=>{ if(hay.includes(w)) t.push('w:'+w); });
      return t;
    }catch(e){ return []; }
  }
  function apdRecommendations(currentIdx, N){
    const N_ = N || 3;
    const curTags = new Set(apdLessonTags(currentIdx));
    const seen = new Set([currentIdx]);
    const scored = [];
    for(let i=0; i<AC_COURSE.length; i++){
      if(seen.has(i)) continue;
      if(typeof acUnlocked==='function' && !acUnlocked(i)) continue;
      if(typeof acLessonDone==='function' && acLessonDone(i)) continue;
      const tags = apdLessonTags(i);
      let score = 0;
      tags.forEach(t=>{ if(curTags.has(t)) score += (t.startsWith('t:') ? 3 : 1); });
      // бонус: соседний урок в том же курсе
      if(acCourseOf(i) === acCourseOf(currentIdx)) score += 1;
      // бонус: тот же блок
      const bA = AC_BLOCKS.find(b=>i>=b.from && i<b.from+b.count);
      const bB = AC_BLOCKS.find(b=>currentIdx>=b.from && b<b.from+b.count);
      if(bA && bB && bA.id === bB.id) score += 1;
      if(score > 0) scored.push({i, score, matches: tags.filter(t=>curTags.has(t))});
    }
    scored.sort((a,b)=> b.score - a.score);
    // fallback: если совпадений нет — предложим первые доступные незавершённые
    if(!scored.length){
      for(let i=0; i<AC_COURSE.length && scored.length<N_; i++){
        if(seen.has(i)) continue;
        if(typeof acUnlocked==='function' && !acUnlocked(i)) continue;
        if(typeof acLessonDone==='function' && acLessonDone(i)) continue;
        scored.push({i, score:0, matches:[]});
      }
    }
    return scored.slice(0, N_);
  }
  function apdRecoWhy(rec){
    if(!rec || !rec.matches || !rec.matches.length){
      return 'Дальше по программе';
    }
    const t = rec.matches.find(x=>x.startsWith('t:'));
    if(t){
      const key = t.slice(2);
      const tool = (typeof AC_TOOLS!=='undefined' && AC_TOOLS[key]) ? AC_TOOLS[key].name : key;
      return 'По ' + tool;
    }
    const w = rec.matches.find(x=>x.startsWith('w:'));
    if(w) return 'По теме';
    return 'Близко';
  }
  function apdRecoHtml(){
    const recs = apdRecommendations(acL, 3);
    if(!recs.length) return '';
    const rows = recs.map(r=>{
      const L = AC_COURSE[r.i];
      const ci = acCourseOf(r.i);
      const num = (typeof acLocalNo==='function') ? acLocalNo(r.i) : (r.i+1);
      const meta = AC_COURSES[ci].title + ' · урок ' + num;
      return `<button class="acd-reco-item" onclick="apdRecoGo(${r.i})">
        <span class="n">${num}</span>
        <span class="meta"><b>${esc(L.title)}</b><span>${esc(meta)}</span></span>
        <span class="why">${esc(apdRecoWhy(r))}</span>
        <svg class="i go"><use href="#i-chev"/></svg>
      </button>`;
    }).join('');
    return `<div class="card acd-reco">
      <div class="acd-reco-head">${I('rocket')} Тебе понравится · подобрано по темам урока</div>
      <div class="acd-reco-list">${rows}</div>
    </div>`;
  }
  window.apdRecoGo = function(i){ if(typeof acOpenLesson==='function') acOpenLesson(i); };

  /* =============================================================
     6. КОММЕНТАРИИ (localStorage, вложенные ответы, лайки)
     ============================================================= */
  function apdMe(){
    const nick = (window.PROFILE && PROFILE.nick) || 'guest';
    const name = (window.PROFILE && PROFILE.name) || 'Слушатель';
    return {nick, name};
  }
  function apdCommsFor(i){
    const st = acS.comments || {};
    if(!st[i]) st[i] = [];
    return st[i];
  }
  function apdAva(name){
    return esc((name||'?').trim().charAt(0).toUpperCase());
  }
  function apdWhen(ts){
    const d = Date.now() - ts;
    if(d < 60_000) return 'только что';
    if(d < 3600_000) return Math.floor(d/60_000) + ' мин назад';
    if(d < 86_400_000) return Math.floor(d/3_600_000) + ' ч назад';
    if(d < 30 * 86_400_000) return Math.floor(d/86_400_000) + ' д назад';
    return new Date(ts).toLocaleDateString('ru-RU');
  }
  function apdCommentsInnerHtml(){
    const list = apdCommsFor(acL);
    const cnt = list.reduce((n,c)=>n + 1 + ((c.replies||[]).length), 0);
    const me = apdMe();
    const body = list.length ? `<div class="acd-comment-list">${list.map(c=>apdCommentHtml(c)).join('')}</div>`
      : `<div class="acd-comment-empty">Пока никто не писал. Задай первый вопрос автору или поделись инсайтом — другим будет полезно.</div>`;
    return `<div class="acd-comments-head">
        <span class="ico">${I('comment')}</span>
        <div><b>Обсуждение урока</b><span class="n">${cnt} ${apdPlural(cnt,['ответ','ответа','ответов'])} · вопросы, инсайты, разбор кейсов</span></div>
      </div>
      <div class="acd-comment-form">
        <textarea class="acd-comment-ta" id="acdCommTa" placeholder="Задай вопрос автору или поделись, что откликнулось. Другим слушателям это поможет."></textarea>
        <div class="acd-comment-actions">
          <span>${esc(me.name)} · публично для всех</span>
          <button class="btn sm" onclick="apdCommSend()">${I('send')} Отправить</button>
        </div>
      </div>
      ${body}`;
  }
  function apdCommentHtml(c, isReply){
    const me = apdMe();
    const isMe = c.nick === me.nick;
    const liked = !!(c.likedBy && c.likedBy[me.nick]);
    const replies = (c.replies||[]).map(r=>apdCommentHtml(r, true)).join('');
    const clsRoot = isReply ? 'acd-comment acd-reply' : 'acd-comment';
    const replyForm = !isReply ? `
      <div class="acd-reply-form">
        <input type="text" id="acdRep_${c.id}" placeholder="Ответить…" onkeydown="if(event.key==='Enter')apdCommSend('${c.id}')">
        <button onclick="apdCommSend('${c.id}')">Ответить</button>
      </div>` : '';
    const del = isMe ? `<button class="acd-comment-act" onclick="apdCommDel('${c.id}')" title="Удалить">${I('plus')}</button>` : '';
    return `<div class="${clsRoot} ${isMe?'author':''}">
      <span class="ava">${apdAva(c.name)}</span>
      <div class="bd">
        <div class="hd"><b>${esc(c.name)}</b><span class="ts">${esc(apdWhen(c.ts))}</span></div>
        <div class="tx">${esc(c.text)}</div>
        <div class="acts">
          <button class="acd-comment-act ${liked?'liked':''}" onclick="apdCommLike('${c.id}')">${I('heart')} <span>${c.likes||0}</span></button>
          ${del}
        </div>
        ${replies ? `<div class="acd-replies">${replies}</div>` : ''}
        ${replyForm}
      </div>
    </div>`;
  }
  window.apdCommSend = function(parentId){
    let val;
    if(parentId){
      const inp = document.getElementById('acdRep_'+parentId);
      val = inp ? (inp.value||'').trim() : '';
      if(inp) inp.value = '';
    } else {
      const ta = document.getElementById('acdCommTa');
      val = ta ? (ta.value||'').trim() : '';
      if(ta) ta.value = '';
    }
    if(val.length < 2){ toast('Слишком коротко'); return; }
    const me = apdMe();
    const rec = {id:'c'+Date.now().toString(36)+Math.random().toString(36).slice(2,5),
                 name:me.name, nick:me.nick, text:val.slice(0,800), ts:Date.now(), likes:0, likedBy:{}};
    const arr = apdCommsFor(acL);
    if(parentId){
      const walk = (list)=>{
        for(const x of list){
          if(x.id === parentId){ x.replies = x.replies||[]; x.replies.push(rec); return true; }
          if(x.replies && walk(x.replies)) return true;
        }
        return false;
      };
      walk(arr);
    } else {
      arr.unshift(rec);
    }
    acSave();
    apdRefreshComments();
    if(!parentId) toast('Опубликовано');
  };
  window.apdCommLike = function(id){
    const me = apdMe();
    const arr = apdCommsFor(acL);
    const walk = (list)=>{
      for(const x of list){
        if(x.id === id){
          x.likedBy = x.likedBy || {};
          if(x.likedBy[me.nick]){ delete x.likedBy[me.nick]; x.likes = Math.max(0,(x.likes||1)-1); }
          else { x.likedBy[me.nick] = 1; x.likes = (x.likes||0)+1; }
          return true;
        }
        if(x.replies && walk(x.replies)) return true;
      }
      return false;
    };
    walk(arr);
    acSave();
    apdRefreshComments();
  };
  window.apdCommDel = function(id){
    const arr = apdCommsFor(acL);
    const idx = arr.findIndex(x=>x.id === id);
    if(idx >= 0){ arr.splice(idx,1); }
    else {
      // reply
      for(const x of arr){
        if(!x.replies) continue;
        const j = x.replies.findIndex(r=>r.id === id);
        if(j >= 0){ x.replies.splice(j,1); break; }
      }
    }
    acSave();
    apdRefreshComments();
    toast('Удалено');
  };
  function apdRefreshComments(){
    const box = document.getElementById('acdComments');
    if(box) box.innerHTML = apdCommentsInnerHtml();
  }
  function apdPlural(n, forms){
    const a = Math.abs(n) % 100, b = a % 10;
    if(a > 10 && a < 20) return forms[2];
    if(b > 1 && b < 5) return forms[1];
    if(b === 1) return forms[0];
    return forms[2];
  }

  /* =============================================================
     9. ЗАМЕТКИ К УРОКУ (плавающая кнопка + панель, localStorage)
     ============================================================= */
  function apdNoteText(){
    return (acS.notes && acS.notes[acL]) || '';
  }
  function apdMountNotesFab(){
    const scr = document.getElementById('screen-academy');
    if(!scr) return;
    const has = apdNoteText().length;
    let fab = document.getElementById('acdNotesFab');
    if(!fab){
      fab = document.createElement('button');
      fab.id = 'acdNotesFab';
      fab.className = 'acd-notes-fab';
      fab.setAttribute('aria-label','Заметки к уроку');
      fab.onclick = apdNotesOpen;
      document.body.appendChild(fab);
    }
    fab.innerHTML = I('edit') + (has ? '<span class="cnt">' + Math.min(999, has) + '</span>' : '');
  }
  window.apdNotesOpen = function(){
    let panel = document.getElementById('acdNotesPanel');
    if(!panel){
      panel = document.createElement('div');
      panel.id = 'acdNotesPanel';
      panel.className = 'acd-notes-panel';
      panel.onclick = function(e){ if(e.target === panel) apdNotesClose(); };
      document.body.appendChild(panel);
    }
    const L = acCur();
    panel.innerHTML = `
      <div class="acd-notes-card" onclick="event.stopPropagation()">
        <div class="acd-notes-hd">
          <span class="ico">${I('edit')}</span>
          <div class="tt"><b>Заметки к уроку</b><span>${esc(L.title)} · сохраняется автоматически на этом устройстве</span></div>
          <button class="cls" onclick="apdNotesClose()" aria-label="Закрыть">${I('plus')}</button>
        </div>
        <div class="acd-notes-body">
          <textarea class="acd-notes-ta" id="acdNotesTa" placeholder="Пиши инсайты, тезисы, вопросы, которые хочешь задать автору. Твои заметки — только твои: остаются на этом устройстве.">${esc(apdNoteText())}</textarea>
        </div>
        <div class="acd-notes-foot">
          <span class="meta">Слов: <b id="acdNotesWc">${apdWordCount(apdNoteText())}</b> · сохранено</span>
          <div class="acts">
            <button class="btn sm ghost" onclick="apdNotesClear()">Очистить</button>
            <button class="btn sm ghost" onclick="apdNotesExport()">${I('file')} Скопировать</button>
          </div>
        </div>
      </div>`;
    requestAnimationFrame(()=>panel.classList.add('open'));
    const ta = document.getElementById('acdNotesTa');
    if(ta){
      ta.addEventListener('input', ()=>{
        acS.notes[acL] = ta.value.slice(0, 12000);
        acSave();
        const wc = document.getElementById('acdNotesWc');
        if(wc) wc.textContent = apdWordCount(ta.value);
        apdMountNotesFab();
      });
      setTimeout(()=>ta.focus(), 50);
    }
  };
  window.apdNotesClose = function(){
    const p = document.getElementById('acdNotesPanel');
    if(p){ p.classList.remove('open'); }
  };
  window.apdNotesClear = function(){
    acS.notes[acL] = '';
    acSave();
    const ta = document.getElementById('acdNotesTa');
    if(ta) ta.value = '';
    const wc = document.getElementById('acdNotesWc');
    if(wc) wc.textContent = '0';
    apdMountNotesFab();
    toast('Заметки урока очищены');
  };
  window.apdNotesExport = function(){
    const t = apdNoteText();
    if(!t.trim()){ toast('Пусто — нечего копировать'); return; }
    const head = 'Заметки · ' + (acCur().title || 'урок') + '\n\n';
    const full = head + t;
    if(navigator.clipboard && navigator.clipboard.writeText){
      navigator.clipboard.writeText(full).then(()=>toast('Скопировано в буфер')).catch(()=>toast('Не удалось скопировать'));
    } else {
      const ta = document.createElement('textarea'); ta.value = full;
      ta.style.cssText = 'position:fixed;top:-200px'; document.body.appendChild(ta);
      ta.select(); try{ document.execCommand('copy'); toast('Скопировано в буфер'); }catch(e){ toast('Не удалось'); }
      ta.remove();
    }
  };
  function apdWordCount(s){
    return String(s||'').trim().split(/\s+/).filter(Boolean).length;
  }

  /* =============================================================
     1. ЛЕНИВАЯ ЗАГРУЗКА БЛОКОВ (collapsible) — post-processing DOM
     ============================================================= */
  function apdSetupCollapsibleBlocks(){
    if(acView !== 'course') return;
    const root = document.getElementById('acRoot');
    if(!root) return;
    const blocks = root.querySelectorAll('.ac-block');
    if(!blocks.length) return;
    // Определяем блок текущего/след. урока (авто-открытие)
    let autoBlockId = null;
    try{
      const ci = acCourse;
      const idx = (typeof acCourseIdx==='function') ? acCourseIdx(ci) : [];
      const target = idx.find(i=>acUnlocked(i) && !acLessonDone(i));
      if(target !== undefined){
        const b = AC_BLOCKS.find(bl=>target>=bl.from && target<bl.from+bl.count);
        if(b) autoBlockId = b.id;
      }
    }catch(e){}
    let curBlockAssigned = false;
    blocks.forEach((blk, i)=>{
      // определить id блока по индексу — читаем из соседних .ac-lesson-row (не нужно, авто = первый)
      const head = blk.querySelector('.ac-block-head');
      const body = blk.querySelectorAll(':scope > *:not(.ac-block-head)');
      if(!head || !body.length) return;
      // Обернём body в div.acd-block-body, если ещё не обёрнуто
      if(!blk.querySelector(':scope > .acd-block-body')){
        const wrap = document.createElement('div');
        wrap.className = 'acd-block-body';
        body.forEach(n=>wrap.appendChild(n));
        blk.appendChild(wrap);
      }
      // Toggle кнопка
      if(!head.querySelector('.acd-block-toggle')){
        const btn = document.createElement('button');
        btn.className = 'acd-block-toggle';
        btn.setAttribute('aria-label','Развернуть/свернуть блок');
        btn.innerHTML = I('chev');
        btn.onclick = function(e){ e.stopPropagation(); apdBlockToggle(blk); };
        head.appendChild(btn);
      }
      head.onclick = function(){ apdBlockToggle(blk); };
      // По умолчанию: первый блок и блок с активным уроком — открыты, остальные свёрнуты
      const shouldOpen = (i === 0) || (!curBlockAssigned && autoBlockId && blk.textContent.includes('Блок'));
      // Более надёжная эвристика: первый блок — точно открыт. Второй проход ниже — по id.
      if(i === 0){ blk.classList.add('acd-open'); curBlockAssigned = true; }
      else blk.classList.add('acd-collapsed');
    });
    // Второй проход — открыть блок с активным уроком по данным AC_BLOCKS
    try{
      if(autoBlockId){
        const ci = acCourse;
        const localBlocks = acCourseBlocks(ci);   // массив блоков курса в правильном порядке
        const targetPos = localBlocks.findIndex(b=>b.id === autoBlockId);
        if(targetPos >= 0 && targetPos < blocks.length){
          blocks.forEach((b,i)=>{
            if(i === targetPos){ b.classList.remove('acd-collapsed'); b.classList.add('acd-open'); }
            else if(i !== 0){ b.classList.add('acd-collapsed'); b.classList.remove('acd-open'); }
          });
          // если targetPos !== 0, первый блок закроем — оставим фокус на активном
          if(targetPos !== 0){
            blocks[0].classList.remove('acd-open');
            blocks[0].classList.add('acd-collapsed');
          }
        }
      }
    }catch(e){}
  }
  window.apdBlockToggle = function(blk){
    if(!blk) return;
    if(blk.classList.contains('acd-collapsed')){
      blk.classList.remove('acd-collapsed');
      blk.classList.add('acd-open');
    } else {
      blk.classList.add('acd-collapsed');
      blk.classList.remove('acd-open');
    }
  };

  /* =============================================================
     10. ФИЛЬТРЫ КАТАЛОГА (чипы: Все / Бесплатное / В процессе / Не начатые)
     ============================================================= */
  const APD_FILTERS = [
    {k:'all',   lbl:'Все',            ic:'compass'},
    {k:'free',  lbl:'Бесплатное',     ic:'bolt'},
    {k:'prog',  lbl:'В процессе',     ic:'circle-play'},
    {k:'new',   lbl:'Не начатые',     ic:'flag'},
    {k:'done',  lbl:'Пройденные',     ic:'check2'},
  ];
  function apdCourseMatchesFilter(ci, filter){
    const c = AC_COURSES[ci];
    const pct = (typeof acCoursePctOf==='function') ? acCoursePctOf(ci) : 0;
    const done = (typeof acCourseDone==='function') && acCourseDone(ci);
    switch(filter){
      case 'free': return !!c.free;
      case 'prog': return pct > 0 && !done;
      case 'new':  return pct === 0;
      case 'done': return done;
      default: return true;
    }
  }
  function apdFilterCounts(){
    const cnt = {};
    APD_FILTERS.forEach(f=>{
      cnt[f.k] = 0;
      for(let ci=0; ci<AC_COURSES.length; ci++){
        if(apdCourseMatchesFilter(ci, f.k)) cnt[f.k]++;
      }
    });
    return cnt;
  }
  function apdFiltersHtml(){
    const cnt = apdFilterCounts();
    const on = acS.filter || 'all';
    return `<div class="acd-filters" id="acdFilters">${
      APD_FILTERS.map(f=>`<button class="acd-chip ${on===f.k?'on':''}" onclick="apdFilterSet('${f.k}')">
        ${I(f.ic)}<span>${esc(f.lbl)}</span><span class="n">${cnt[f.k]}</span>
      </button>`).join('')
    }</div>`;
  }
  window.apdFilterSet = function(k){
    acS.filter = k; acSave();
    apdApplyCatalogFilter();
    // подсветка чипов
    document.querySelectorAll('#acdFilters .acd-chip').forEach(el=>el.classList.remove('on'));
    const chip = document.querySelector('#acdFilters .acd-chip[onclick*="\'' + k + '\'"]');
    if(chip) chip.classList.add('on');
  };
  function apdApplyCatalogFilter(){
    if(acView !== 'home') return;
    const cat = document.querySelector('#acRoot .ac-catalog');
    if(!cat) return;
    const cards = cat.querySelectorAll('.ac-cc');
    const on = acS.filter || 'all';
    let vis = 0;
    cards.forEach((card, ci)=>{
      const show = apdCourseMatchesFilter(ci, on);
      card.classList.toggle('acd-hidden', !show);
      if(show) vis++;
    });
    // Пустой стейт
    let empty = cat.parentNode.querySelector('.acd-filters-empty');
    if(!vis){
      if(!empty){
        empty = document.createElement('div');
        empty.className = 'acd-filters-empty';
        empty.textContent = 'В этой категории пока нет курсов. Смени фильтр.';
        cat.parentNode.insertBefore(empty, cat.nextSibling);
      }
    } else if(empty){ empty.remove(); }
  }
  function apdInjectCatalogFilters(){
    if(acView !== 'home') return;
    const root = document.getElementById('acRoot');
    if(!root) return;
    if(root.querySelector('#acdFilters')) return;
    const cat = root.querySelector('.ac-catalog');
    if(!cat) return;
    const holder = document.createElement('div');
    holder.innerHTML = apdFiltersHtml();
    cat.parentNode.insertBefore(holder.firstElementChild, cat);
    apdApplyCatalogFilter();
  }

  /* =============================================================
     8. СКОРОСТЬ ВИДЕО: кнопка 1× → 1.25× → 1.5× → 2× → 0.75× → 1×
        Пост-обрабатываем acVpBar после render'а
     ============================================================= */
  const APD_SPEEDS = [1, 1.25, 1.5, 2, 0.75];
  function apdReadSpeed(){
    try{ const v = parseFloat(localStorage.getItem('oko-ac-speed')); return isFinite(v) && v > 0 ? v : 1; }catch(e){ return 1; }
  }
  function apdSaveSpeed(v){ try{ localStorage.setItem('oko-ac-speed', String(v)); }catch(e){} }
  function apdInjectSpeed(){
    const bar = document.getElementById('acVpBar');
    if(!bar || bar.querySelector('.acd-vp-speed')) return;
    const v = apdReadSpeed();
    const btn = document.createElement('button');
    btn.className = 'ac-vp-btn acd-vp-speed';
    btn.type = 'button';
    btn.setAttribute('aria-label','Скорость воспроизведения');
    btn.textContent = apdFmtSpeed(v);
    btn.onclick = function(e){ if(e) e.stopPropagation(); apdSpeedCycle(btn); };
    // ставим перед кнопкой fullscreen (последняя)
    const kids = bar.querySelectorAll('.ac-vp-btn');
    const last = kids[kids.length - 1];
    if(last) bar.insertBefore(btn, last);
    else bar.appendChild(btn);
    // применяем к видео
    apdApplySpeed(v);
  }
  function apdFmtSpeed(v){ return (v % 1 === 0 ? v.toFixed(0) : v.toFixed(2).replace(/0$/,'')) + '×'; }
  function apdSpeedCycle(btn){
    const cur = apdReadSpeed();
    const i = APD_SPEEDS.indexOf(cur);
    const nx = APD_SPEEDS[(i+1) % APD_SPEEDS.length];
    apdSaveSpeed(nx);
    if(btn) btn.textContent = apdFmtSpeed(nx);
    apdApplySpeed(nx);
    toast('Скорость · ' + apdFmtSpeed(nx));
  }
  function apdApplySpeed(v){
    try{ const vid = document.getElementById('acVpVideo'); if(vid) vid.playbackRate = v; }catch(e){}
  }

  /* =============================================================
     5. СЕРТИФИКАТ RU/EN: переключатель в full-overlay
     ============================================================= */
  window.acdCertLang = 'ru';
  const APD_CERT_I18N = {
    ru: {academy:'А К А Д Е М И Я   O K O', title:'СЕРТИФИКАТ',
         confirms:'подтверждает, что', passed:'успешно прошёл направление',
         courseOf:'курс «', courseOfEnd:'» Академии OKO',
         result:'Результат теста: ', dateLbl:'ДАТА ВЫДАЧИ', numLbl:'НОМЕР',
         sign:'/ Ильясов Д.А. /', signSub:'подпись руководителя Академии',
         officialDoc:'ОФИЦИАЛЬНЫЙ ДОКУМЕНТ'},
    en: {academy:'O K O   A C A D E M Y', title:'CERTIFICATE',
         confirms:'this is to certify that', passed:'has successfully completed the track',
         courseOf:'course "', courseOfEnd:'" of OKO Academy',
         result:'Test score: ', dateLbl:'ISSUE DATE', numLbl:'NUMBER',
         sign:'/ D. A. Ilyasov /', signSub:'Academy director',
         officialDoc:'OFFICIAL DOCUMENT'}
  };
  // chain-patch acMakeCert — переопределяем текстовые константы во время генерации
  const _apdPrevMakeCert = window.acMakeCert;
  window.acMakeCert = function(cert, cb){
    const lang = acdCertLang || 'ru';
    // Русский: не трогаем оригинал вообще
    if(lang === 'ru') return _apdPrevMakeCert(cert, cb);
    // Английский: делаем свою copy подмены глифов + переводов
    apdMakeCertEN(cert, cb);
  };
  function apdMakeCertEN(cert, cb){
    // отдельный рендер для EN — минимальная переделка ru-версии
    if(typeof SEAL_REQ === 'undefined'){ return _apdPrevMakeCert(cert, cb); }
    const ready = (document.fonts && document.fonts.load)
      ? Promise.all([document.fonts.load('100px "Bebas Neue"'), document.fonts.load('700 30px Montserrat')]).catch(()=>{})
      : Promise.resolve();
    ready.then(()=>{
      // используем acSigImage/acDrawSeal/acRingText из academy — они в глобальной области
      const doDraw = (sig)=>{
        const t = APD_CERT_I18N.en;
        const W = 1600, H = 1131;
        const cv = document.createElement('canvas');
        cv.width = W; cv.height = H;
        const ctx = cv.getContext('2d');
        const lime = '#9AFF00';
        ctx.fillStyle = '#0a0a0a'; ctx.fillRect(0,0,W,H);
        ctx.strokeStyle = 'rgba(154,255,0,.05)'; ctx.lineWidth = 1;
        for(let x=0;x<=W;x+=64){ ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,H); ctx.stroke(); }
        for(let y=0;y<=H;y+=64){ ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(W,y); ctx.stroke(); }
        const g = ctx.createRadialGradient(W-220,180,40,W-220,180,560);
        g.addColorStop(0,'rgba(154,255,0,.10)'); g.addColorStop(1,'rgba(154,255,0,0)');
        ctx.fillStyle = g; ctx.fillRect(0,0,W,H);
        ctx.strokeStyle = lime; ctx.lineWidth = 5; ctx.strokeRect(38,38,W-76,H-76);
        ctx.lineWidth = 1.5; ctx.globalAlpha = .65; ctx.strokeRect(58,58,W-116,H-116); ctx.globalAlpha = 1;
        ctx.lineWidth = 5;
        [[38,38,1,1],[W-38,38,-1,1],[38,H-38,1,-1],[W-38,H-38,-1,-1]].forEach(([x,y,dx,dy])=>{
          ctx.beginPath(); ctx.moveTo(x+dx*44,y); ctx.lineTo(x,y); ctx.lineTo(x,y+dy*44); ctx.stroke();
        });
        try{ ctx.letterSpacing = '6px'; }catch(e){}
        ctx.textAlign = 'center';
        ctx.fillStyle = 'rgba(255,255,255,.55)';
        ctx.font = '42px "Bebas Neue", Impact, sans-serif';
        ctx.fillText(t.academy, W/2, 148);
        ctx.fillStyle = lime;
        ctx.shadowColor = 'rgba(154,255,0,.45)'; ctx.shadowBlur = 34;
        ctx.font = '150px "Bebas Neue", Impact, sans-serif';
        ctx.fillText(t.title, W/2, 300);
        ctx.shadowBlur = 0;
        try{ ctx.letterSpacing = '2px'; }catch(e){}
        ctx.fillStyle = 'rgba(255,255,255,.6)';
        ctx.font = '500 27px Montserrat, Arial';
        ctx.fillText(t.confirms, W/2, 372);
        ctx.fillStyle = '#fff';
        ctx.font = '96px "Bebas Neue", Impact, sans-serif';
        ctx.fillText(apdTransliterate(cert.name).toUpperCase(), W/2, 486);
        ctx.strokeStyle = 'rgba(154,255,0,.5)'; ctx.lineWidth = 2;
        ctx.beginPath(); ctx.moveTo(W/2-260,512); ctx.lineTo(W/2+260,512); ctx.stroke();
        const courseTitle = apdTranslateTrack(cert.courseTitle || 'OKO Academy');
        ctx.fillStyle = 'rgba(255,255,255,.6)';
        ctx.font = '500 26px Montserrat, Arial';
        ctx.fillText(t.passed, W/2, 566);
        ctx.fillStyle = lime;
        let fs = 64;
        ctx.font = fs + 'px "Bebas Neue", Impact, sans-serif';
        const trackLine = '"' + courseTitle.toUpperCase() + '"';
        while(fs > 40 && ctx.measureText(trackLine).width > W-260){ fs -= 4; ctx.font = fs + 'px "Bebas Neue", Impact, sans-serif'; }
        ctx.fillText(trackLine, W/2, 646);
        ctx.fillStyle = 'rgba(255,255,255,.6)';
        ctx.font = '500 24px Montserrat, Arial';
        ctx.fillText(t.courseOfEnd.replace(/^»/,'').replace('« ','').trim(), W/2, 696);
        const chipT = t.result + cert.score + '%';
        ctx.font = '700 26px Montserrat, Arial';
        const cw = ctx.measureText(chipT).width + 66;
        ctx.fillStyle = 'rgba(154,255,0,.13)';
        ctx.beginPath();
        if(ctx.roundRect) ctx.roundRect(W/2-cw/2,730,cw,58,29); else ctx.rect(W/2-cw/2,730,cw,58);
        ctx.fill();
        ctx.strokeStyle = lime; ctx.lineWidth = 1.6; ctx.stroke();
        ctx.fillStyle = lime;
        ctx.fillText(chipT, W/2, 768);
        ctx.textAlign = 'left';
        ctx.fillStyle = 'rgba(255,255,255,.45)';
        ctx.font = '600 19px Montserrat, Arial';
        ctx.fillText(t.dateLbl, 120, 878);
        ctx.fillStyle = '#fff';
        ctx.font = '46px "Bebas Neue", Impact, sans-serif';
        const dateEn = apdDateEn(cert.date);
        ctx.fillText(dateEn, 120, 928);
        ctx.fillStyle = 'rgba(255,255,255,.45)';
        ctx.font = '600 19px Montserrat, Arial';
        ctx.fillText(t.numLbl, 120, 972);
        ctx.fillStyle = lime;
        ctx.font = '700 26px Montserrat, Arial';
        ctx.fillText('No ' + cert.no, 120, 1004);
        const sx1=560, sx2=880, sy=952;
        ctx.strokeStyle = 'rgba(255,255,255,.55)'; ctx.lineWidth = 2;
        ctx.beginPath(); ctx.moveTo(sx1,sy); ctx.lineTo(sx2,sy); ctx.stroke();
        if(sig){
          const sw = 300, sh = sw * (sig.height / sig.width);
          ctx.drawImage(sig, (sx1+sx2)/2 - sw/2, sy - sh*0.82, sw, sh);
        }
        ctx.fillStyle = '#fff';
        ctx.font = '600 24px Montserrat, Arial';
        ctx.fillText(t.sign, sx2 + 18, sy + 8);
        ctx.fillStyle = 'rgba(255,255,255,.4)';
        ctx.font = '500 16px Montserrat, Arial';
        ctx.fillText(t.signSub, sx1, sy + 32);
        // печать оставляем на русском (это официальный реквизит)
        if(typeof acDrawSeal === 'function') acDrawSeal(ctx, 1350, 900, 138);
        ctx.textAlign = 'center';
        ctx.fillStyle = 'rgba(255,255,255,.34)';
        ctx.font = '500 15px Montserrat, Arial';
        ctx.fillText(SEAL_REQ.fio + ' · ' + SEAL_REQ.inn + ' · ' + SEAL_REQ.brand + ' · okoteam.top@gmail.com', W/2, 1076);
        cb(cv.toDataURL('image/png'));
      };
      if(typeof acSigImage === 'function') acSigImage(doDraw);
      else doDraw(null);
    });
  }
  // Транслитерация имени RU→LAT
  function apdTransliterate(s){
    const M = {А:'A',Б:'B',В:'V',Г:'G',Д:'D',Е:'E',Ё:'Yo',Ж:'Zh',З:'Z',И:'I',Й:'Y',К:'K',Л:'L',М:'M',Н:'N',О:'O',П:'P',Р:'R',С:'S',Т:'T',У:'U',Ф:'F',Х:'Kh',Ц:'Ts',Ч:'Ch',Ш:'Sh',Щ:'Sch',Ъ:'',Ы:'Y',Ь:'',Э:'E',Ю:'Yu',Я:'Ya',
               а:'a',б:'b',в:'v',г:'g',д:'d',е:'e',ё:'yo',ж:'zh',з:'z',и:'i',й:'y',к:'k',л:'l',м:'m',н:'n',о:'o',п:'p',р:'r',с:'s',т:'t',у:'u',ф:'f',х:'kh',ц:'ts',ч:'ch',ш:'sh',щ:'sch',ъ:'',ы:'y',ь:'',э:'e',ю:'yu',я:'ya'};
    return String(s||'').split('').map(ch=>M[ch]!==undefined?M[ch]:ch).join('');
  }
  // Переводим названия направлений (те три, что заведены в академии)
  function apdTranslateTrack(t){
    const M = {'Медийность':'Media Presence','Маркетинг':'Marketing','Нейросети':'Neural Networks'};
    return M[t] || apdTransliterate(t);
  }
  function apdDateEn(ru){
    // Ру-дата DD.MM.YYYY → "Mon D, YYYY"
    const m = String(ru||'').match(/(\d{1,2})\.(\d{1,2})\.(\d{4})/);
    if(!m) return ru;
    const months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    return months[parseInt(m[2],10)-1] + ' ' + parseInt(m[1],10) + ', ' + m[3];
  }
  // Инъекция переключателя в fullscreen-overlay
  function apdInjectCertLangSwitch(){
    const full = document.getElementById('acCertFull');
    if(!full || full.querySelector('.acd-cert-lang')) return;
    const sw = document.createElement('div');
    sw.className = 'acd-cert-lang';
    sw.innerHTML = `
      <button class="on" data-l="ru" onclick="apdCertLangSet('ru')">RU</button>
      <button data-l="en" onclick="apdCertLangSet('en')">EN</button>`;
    full.appendChild(sw);
  }
  window.apdCertLangSet = function(lang){
    if(lang !== 'ru' && lang !== 'en') return;
    if(window.acdCertLang === lang) return;
    window.acdCertLang = lang;
    document.querySelectorAll('.acd-cert-lang button').forEach(b=>{
      b.classList.toggle('on', b.getAttribute('data-l') === lang);
    });
    // Перегенерируем текущий сертификат
    try{
      window.acCertUrl = null;
      const rec = (typeof acCertRec === 'function') ? acCertRec() : null;
      if(!rec) return;
      const img = document.getElementById('acCertFullImg');
      if(!img) return;
      // спиннер визуальный
      img.style.opacity = '.45';
      acMakeCert(rec, url=>{
        window.acCertUrl = url;
        window.acCertShownNo = rec.no;
        img.src = url;
        img.style.opacity = '1';
      });
    }catch(e){}
  };

  /* =============================================================
     ХУК: chain-patch acRender — после базового render'а
     запускаем все свои пост-обработки под текущий acView
     ============================================================= */
  const _apdPrevRender = window.acRender;
  window.acRender = function(){
    apdRemoveLessonExtras();
    try{ _apdPrevRender.apply(this, arguments); }catch(e){}
    try{
      if(acView === 'home'){
        apdInjectCatalogFilters();
        apdInjectCatalogButton();
      } else if(acView === 'course'){
        apdSetupCollapsibleBlocks();
        // Кнопка «Админ-панель курса» для владельца
        try{ apdInjectAdminBtnOnCourse(); }catch(e){}
      } else if(acView === 'lesson'){
        apdInjectLessonExtras();
        apdLessonExtraFormats();
        // Отложенная инъекция скорости — плеер может ре-рендериться асинхронно
        setTimeout(apdInjectSpeed, 20);
        setTimeout(apdInjectSpeed, 400);
      }
    }catch(e){}
  };
  function apdInjectAdminBtnOnCourse(){
    if(!apdIsOwner()) return;
    const root = document.getElementById('acRoot');
    if(!root || root.querySelector('#apdAdminBtnCourse')) return;
    const insideCard = root.querySelector('.ac-inside');
    if(!insideCard) return;
    const ci = (typeof acCourse === 'number') ? acCourse : 0;
    const btn = document.createElement('button');
    btn.id = 'apdAdminBtnCourse';
    btn.className = 'acd-admin-btn';
    btn.innerHTML = `${I('crown')}<span class="tx">Админ-панель курса</span><span class="go">${I('chev')}</span>`;
    btn.onclick = ()=>apdAdminOpen(ci);
    insideCard.parentNode.insertBefore(btn, insideCard.nextSibling);
  }
  // chain-patch: acRenderVideoBox тоже перерисовывает бар — переинжектим скорость
  if(typeof acRenderVideoBox === 'function'){
    const _apdPrevRVB = window.acRenderVideoBox;
    window.acRenderVideoBox = function(){
      try{ _apdPrevRVB.apply(this, arguments); }catch(e){}
      setTimeout(apdInjectSpeed, 20);
    };
  }

  /* =============================================================
     Проверка контента шагов после действий (chain-patch поменял acAfterCheckpoint;
     дополнительно ре-рендерим ступени при заметных внутренних изменениях)
     ============================================================= */
  ['acRenderTestBox','acRenderTaskBox','acRenderGameBox','acRenderProgressBox','acRenderCertBox']
    .forEach(fn=>{
      if(typeof window[fn] !== 'function') return;
      const prev = window[fn];
      window[fn] = function(){
        try{ prev.apply(this, arguments); }catch(e){}
        apdRerenderStepRail();
      };
    });

  /* =============================================================
     Overlay сертификата: инъекция RU/EN — как только он появится
     ============================================================= */
  function apdWatchCertOverlay(){
    const target = document.getElementById('acCertFull');
    if(!target) return;
    // при каждом открытии — обновляем свитч
    const obs = new MutationObserver(()=>{
      if(target.classList.contains('open')) apdInjectCertLangSwitch();
    });
    try{ obs.observe(target, {attributes:true, attributeFilter:['class']}); }catch(e){}
    apdInjectCertLangSwitch();
  }

  /* =============================================================
     ==================== НОВОЕ (29.07 Даниэль) ====================
     Каталог курсов OKO (fullscreen), карточка курса,
     Админ-панель, PDF сертификата, доп.форматы, мои курсы.
     ============================================================= */

  /* -------- утилиты -------- */
  function apdIsOwner(){
    try{ return typeof isOwner === 'function' ? isOwner() : (window.PROFILE && PROFILE.role === 'owner'); }
    catch(e){ return false; }
  }
  function apdFmtDur(mins){
    const h = Math.floor(mins/60), m = Math.round(mins%60);
    return h ? (h + ' ч ' + (m ? m + ' мин' : '').trim()).trim() : (m + ' мин');
  }
  function apdDeterministicRnd(seed){
    // маленький xorshift для стабильных «отзывов/рейтингов» по индексу курса
    let s = (seed*2654435761) >>> 0;
    return ()=>{ s ^= s<<13; s ^= s>>>17; s ^= s<<5; return ((s >>> 0) % 1000) / 1000; };
  }
  function apdCourseMeta(ci){
    // синтетические, но стабильные данные (звёзды/учеников/уровень/длительность)
    const c = AC_COURSES[ci];
    const st = (typeof acCourseStats==='function') ? acCourseStats(ci) : {lessons:c.count, mins:c.count*8};
    /* Рейтинг «4,73» и «847 учеников» здесь просто вычислялись из номера
       курса. Красиво и полностью неправда. Пока нет реальных оценок и
       учеников — отдаём нули, а карточка курса эти строки не рисует.
       Уровень курса — настоящее свойство самого курса, а не случайность. */
    const level = c.level || 'Для всех';
    return {rating: 0, ratingTxt: null, students: 0, level, mins:st.mins, lessons:st.lessons, slides:st.slides};
  }
  function apdCourseReviews(ci){
    /* Правка Даниэля: «никаких выдуманных отзывов». Здесь генерировались
       отзывы от десяти несуществующих людей. Отзыв появится, когда его
       реально напишет ученик. */
    return [];
    // eslint-disable-next-line no-unreachable
    const c = AC_COURSES[ci];
    const rnd = apdDeterministicRnd(ci+7);
    const names = ['Анна','Дмитрий','Ольга','Илья','Марина','Кирилл','София','Егор','Полина','Роман'];
    const templates = [
      'Практика на реальных задачах, никакой воды. Автор говорит по делу и подкрепляет цифрами.',
      'После курса собрал первый рабочий пайплайн за вечер — то, чего год откладывал.',
      'Круто, что каждый урок с тестом и мини-игрой — материал реально закрепляется.',
      'Заходил как новичок, вышел с чёткой системой. Лучший курс по теме за все деньги.',
      'Понравился формат: короткие уроки, живой ритм, много инструментов и разборов.',
      'Сертификат в конце — приятный бонус, но польза не в бумажке, а в новом навыке.'
    ];
    const n = 3 + Math.floor(rnd()*3);
    const out = [];
    const dt = ['сегодня','вчера','2 дня назад','неделю назад','2 недели назад','месяц назад'];
    for(let i=0; i<n; i++){
      out.push({
        name: names[Math.floor(rnd()*names.length)],
        text: templates[Math.floor(rnd()*templates.length)],
        stars: 4 + (rnd() > 0.35 ? 1 : 0),
        when: dt[Math.floor(rnd()*dt.length)]
      });
      // добавляем пользовательские отзывы
    }
    try{
      const my = (acS.acdReviews||{})[c.id] || [];
      return my.concat(out);
    }catch(e){ return out; }
  }
  function apdStarsSvg(n){
    let s = '';
    for(let i=0; i<5; i++) s += I('star');
    return `<span class="stars">${I('star')} ${n.toFixed(1).replace('.',',')}</span>`;
  }
  function apdEnsureStore2(){
    if(!acS.acdReviews) acS.acdReviews = {};   // {courseId: [{name,text,stars,when}]}
    if(!acS.acdBanned) acS.acdBanned = {};     // {courseId: {nick:true}}
    if(!acS.acdSettings) acS.acdSettings = {}; // {courseId: {price,discount,autonext,certOn,available}}
  }
  apdEnsureStore2();
  function apdGetSettings(ci){
    apdEnsureStore2();
    const c = AC_COURSES[ci];
    const def = {price:c.price||0, discount:0, autonext:true, certOn:true, available:true};
    return Object.assign({}, def, acS.acdSettings[c.id]||{});
  }
  function apdSetSetting(ci, k, v){
    apdEnsureStore2();
    const c = AC_COURSES[ci];
    const cur = apdGetSettings(ci);
    cur[k] = v;
    acS.acdSettings[c.id] = cur;
    acSave();
  }

  /* -------- fullscreen host -------- */
  function apdFullEnsure(){
    let f = document.getElementById('apdFull');
    if(f) return f;
    f = document.createElement('div');
    f.id = 'apdFull';
    f.className = 'acd-full';
    document.body.appendChild(f);
    return f;
  }
  window.apdFullClose = function(){
    const f = document.getElementById('apdFull');
    if(f){ f.classList.remove('open'); setTimeout(()=>{ f.innerHTML=''; }, 260); }
    try{ if(typeof nvPop === 'function') nvPop('acdFull'); }catch(e){}
  };
  function apdFullOpen(titleObj, bodyHtml){
    const f = apdFullEnsure();
    f.innerHTML = `
      <div class="acd-full-top">
        <button class="acd-full-close" onclick="apdFullClose()" aria-label="Закрыть">${I('plus')}</button>
        <div class="acd-full-title"><b>${esc(titleObj.title||'')}</b><span>${esc(titleObj.sub||'')}</span></div>
        ${titleObj.rightBtn||''}
      </div>
      <div class="acd-full-body" id="apdFullBody">${bodyHtml}</div>`;
    requestAnimationFrame(()=>f.classList.add('open'));
    // регистрация в navstack (кнопка Назад TG/браузера/Escape закроют слой)
    try{ if(typeof nvPush === 'function') nvPush('acdFull', ()=>{
      const el = document.getElementById('apdFull');
      if(el){ el.classList.remove('open'); setTimeout(()=>{ el.innerHTML=''; }, 260); }
    }); }catch(e){}
  }
  function apdFullSetBody(html){
    const b = document.getElementById('apdFullBody');
    if(b) b.innerHTML = html;
  }

  /* =============================================================
     1) КАТАЛОГ КУРСОВ OKO — полноэкранная страница
     ============================================================= */
  window.APD_CAT_STATE = {
    q:'', sort:'new',
    dir:'', fmt:'', level:'', author:'', dur:''
  };
  const APD_DIR_OPTS   = ['Медийность','Маркетинг','Нейросети','Дизайн','Финансы'];
  const APD_FMT_OPTS   = ['Бесплатно','Платно','Клубный'];
  const APD_LEVEL_OPTS = ['Начинающий','Средний','Про'];
  const APD_DUR_OPTS   = ['до 1 ч','1–3 ч','3+ ч'];
  const APD_SORT_OPTS  = [
    {k:'new',    lbl:'Новые'},
    {k:'rating', lbl:'Рейтинг'},
    {k:'cheap',  lbl:'Дешевле'},
    {k:'exp',    lbl:'Дороже'},
    {k:'stud',   lbl:'Популярные'}
  ];

  function apdCourseAuthors(){
    const seen = new Set(), out = [];
    AC_COURSES.forEach(c=>{ const a = c.author || '—'; if(!seen.has(a)){ seen.add(a); out.push(a); } });
    return out;
  }
  function apdCourseDir(c){ return c.title; }
  function apdCourseFmt(ci){
    const c = AC_COURSES[ci];
    if(c.free) return 'Бесплатно';
    // клубный = входит в MAX
    if(c.minTier === 'MAX') return 'Клубный';
    return 'Платно';
  }
  function apdCourseDur(ci){
    const m = apdCourseMeta(ci).mins;
    if(m <= 60) return 'до 1 ч';
    if(m <= 180) return '1–3 ч';
    return '3+ ч';
  }
  function apdCourseMatches(ci, S){
    const c = AC_COURSES[ci];
    const meta = apdCourseMeta(ci);
    const q = (S.q||'').trim().toLowerCase();
    if(q){
      const hay = ((c.title||'') + ' ' + (c.sub||'') + ' ' + (c.author||'') + ' ' + (c.outcomes||[]).join(' ')).toLowerCase();
      if(!hay.includes(q)) return false;
    }
    if(S.dir && apdCourseDir(c) !== S.dir) return false;
    if(S.fmt && apdCourseFmt(ci) !== S.fmt) return false;
    if(S.level && meta.level !== S.level) return false;
    if(S.author && (c.author||'') !== S.author) return false;
    if(S.dur && apdCourseDur(ci) !== S.dur) return false;
    return true;
  }
  function apdSortCourses(list, sort){
    const withMeta = list.map(ci=>({ci, c:AC_COURSES[ci], m:apdCourseMeta(ci)}));
    switch(sort){
      case 'rating': withMeta.sort((a,b)=>b.m.rating - a.m.rating); break;
      case 'cheap':  withMeta.sort((a,b)=>(a.c.price||0) - (b.c.price||0)); break;
      case 'exp':    withMeta.sort((a,b)=>(b.c.price||0) - (a.c.price||0)); break;
      case 'stud':   withMeta.sort((a,b)=>b.m.students - a.m.students); break;
      default: /* новые = порядок из AC_COURSES */ break;
    }
    return withMeta.map(x=>x.ci);
  }
  function apdCatalogCardHtml(ci){
    const c = AC_COURSES[ci];
    const acc = acCourseAccessible(ci);
    const pct = acCoursePctOf(ci);
    const meta = apdCourseMeta(ci);
    const fmt = apdCourseFmt(ci);
    const tagCls = c.free ? 'free' : (fmt === 'Клубный' ? 'club' : '');
    const priceTxt = c.free ? 'Бесплатно' : acFmtPrice(c.price);
    return `<button class="acd-cat-card ${acc?'':'locked'}" onclick="apdCourseFullOpen(${ci})">
      <div class="acd-cat-card-cov">
        ${acCourseCover(ci, true)}
        <span class="acd-cat-card-tag ${tagCls}">${esc(fmt)}</span>
        ${pct>0 ? `<span class="acd-cat-card-pct"><i style="width:${pct}%"></i></span>` : ''}
      </div>
      <div class="acd-cat-card-body">
        <h4>${esc(c.title)}</h4>
        <span class="acd-cat-card-auth">${esc(c.author||'—')}</span>
        <div class="acd-cat-card-meta">
          ${meta.ratingTxt ? `<span class="stars">${I('star')} ${meta.ratingTxt}</span>` : ''}
          ${meta.students ? `<span class="stud">${meta.students.toLocaleString('ru-RU')} учеников</span>` : ''}
          ${(!meta.ratingTxt && !meta.students) ? `<span class="stud">${meta.lessons} ${apdPlural(meta.lessons,['урок','урока','уроков'])}</span>` : ''}
        </div>
        <div class="acd-cat-card-price ${c.free?'free':''}">${priceTxt}</div>
      </div>
    </button>`;
  }
  function apdCatalogFiltersHtml(){
    const S = APD_CAT_STATE;
    // val — обычная строка (заголовок фильтра). Экранируем и для HTML-атрибута, и для JS-строки.
    const esq = v => String(v||'').replace(/\\/g,'\\\\').replace(/'/g,"\\'");
    const chip = (key, val, cur)=>`<button class="acd-fchip ${cur===val?'on':''}" onclick="apdCatSet('${key}','${esq(val)}')"><span>${esc(val)}</span></button>`;
    const dirs = AC_COURSES.map(c=>c.title);
    const authors = apdCourseAuthors();
    return `
      <div class="acd-fgroup"><div class="acd-fgroup-h">Направление</div>
        <div class="acd-fgroup-chips">
          <button class="acd-fchip ${!S.dir?'on':''}" onclick="apdCatSet('dir','')"><span>Все</span></button>
          ${dirs.map(d=>chip('dir', d, S.dir)).join('')}
        </div>
      </div>
      <div class="acd-fgroup"><div class="acd-fgroup-h">Формат</div>
        <div class="acd-fgroup-chips">
          <button class="acd-fchip ${!S.fmt?'on':''}" onclick="apdCatSet('fmt','')"><span>Все</span></button>
          ${APD_FMT_OPTS.map(v=>chip('fmt', v, S.fmt)).join('')}
        </div>
      </div>
      <div class="acd-fgroup"><div class="acd-fgroup-h">Уровень</div>
        <div class="acd-fgroup-chips">
          <button class="acd-fchip ${!S.level?'on':''}" onclick="apdCatSet('level','')"><span>Все</span></button>
          ${APD_LEVEL_OPTS.map(v=>chip('level', v, S.level)).join('')}
        </div>
      </div>
      <div class="acd-fgroup"><div class="acd-fgroup-h">Автор</div>
        <div class="acd-fgroup-chips">
          <button class="acd-fchip ${!S.author?'on':''}" onclick="apdCatSet('author','')"><span>Все</span></button>
          ${authors.map(v=>chip('author', v, S.author)).join('')}
        </div>
      </div>
      <div class="acd-fgroup"><div class="acd-fgroup-h">Длительность</div>
        <div class="acd-fgroup-chips">
          <button class="acd-fchip ${!S.dur?'on':''}" onclick="apdCatSet('dur','')"><span>Все</span></button>
          ${APD_DUR_OPTS.map(v=>chip('dur', v, S.dur)).join('')}
        </div>
      </div>`;
  }
  function apdCatalogListHtml(){
    const S = APD_CAT_STATE;
    let ids = [];
    for(let ci=0; ci<AC_COURSES.length; ci++) if(apdCourseMatches(ci, S)) ids.push(ci);
    ids = apdSortCourses(ids, S.sort);
    const hasFilter = S.q || S.dir || S.fmt || S.level || S.author || S.dur;
    const summary = `<div class="acd-cat-summary">
      <span>Найдено <b>${ids.length}</b> ${apdPlural(ids.length,['курс','курса','курсов'])}</span>
      ${hasFilter ? `<span class="clr" onclick="apdCatReset()">Сбросить фильтры</span>` : ''}
    </div>`;
    if(!ids.length){
      return summary + `<div class="acd-cat-empty">${I('search')}<div>Курсы по этим фильтрам не найдены.<br>Попробуй сбросить какие-то условия.</div></div>`;
    }
    return summary + `<div class="acd-cat-grid">${ids.map(apdCatalogCardHtml).join('')}</div>`;
  }
  function apdCatalogBodyHtml(){
    const S = APD_CAT_STATE;
    return `
      <div class="acd-cat-search">
        <input type="text" class="acd-cat-input" id="apdCatQ" placeholder="Поиск курса, автора, темы…" value="${esc(S.q||'')}" oninput="apdCatSetQ(this.value)">
        <select class="acd-cat-sort" onchange="apdCatSet('sort',this.value)">
          ${APD_SORT_OPTS.map(o=>`<option value="${o.k}" ${S.sort===o.k?'selected':''}>${esc(o.lbl)}</option>`).join('')}
        </select>
      </div>
      ${apdCatalogFiltersHtml()}
      <div id="apdCatList">${apdCatalogListHtml()}</div>`;
  }
  window.apdCatalogOpen = function(){
    apdFullOpen({title:'Все курсы Академии', sub:AC_COURSES.length + ' ' + apdPlural(AC_COURSES.length,['курс','курса','курсов']) + ' · фильтры и поиск'}, apdCatalogBodyHtml());
  };
  window.apdCatSet = function(key, val){
    APD_CAT_STATE[key] = val;
    apdCatalogRefreshFull();
  };
  let _apdCatSetQTimer = null;
  window.apdCatSetQ = function(v){
    APD_CAT_STATE.q = v;
    clearTimeout(_apdCatSetQTimer);
    _apdCatSetQTimer = setTimeout(()=>{
      const list = document.getElementById('apdCatList');
      if(list) list.innerHTML = apdCatalogListHtml();
    }, 180);
  };
  window.apdCatReset = function(){
    APD_CAT_STATE = {q:'', sort:APD_CAT_STATE.sort, dir:'', fmt:'', level:'', author:'', dur:''};
    apdCatalogRefreshFull();
  };
  function apdCatalogRefreshFull(){
    apdFullSetBody(apdCatalogBodyHtml());
  }

  /* Инъекция кнопки «Все курсы Академии» + «Мои курсы» на главной */
  function apdInjectCatalogButton(){
    if(acView !== 'home') return;
    const root = document.getElementById('acRoot');
    if(!root || root.querySelector('#apdCatBtn')) return;
    const cat = root.querySelector('.ac-catalog');
    if(!cat) return;
    // 1) Кнопка перед h2 «Каталог»
    const h = cat.previousElementSibling; // section-h
    const total = AC_COURSES.length;
    const btn = document.createElement('button');
    btn.id = 'apdCatBtn';
    btn.className = 'acd-cat-btn';
    btn.onclick = ()=>apdCatalogOpen();
    btn.innerHTML = `
      <span class="ic">${I('search')}</span>
      <span class="tx"><b>Все курсы Академии</b><span>Фильтры, поиск, сортировка · ${total} ${apdPlural(total,['курс','курса','курсов'])}</span></span>
      ${I('chev')}`;
    if(h && h.tagName === 'H2') h.parentNode.insertBefore(btn, h);
    else cat.parentNode.insertBefore(btn, cat);

    // 2) Секция «Мои курсы» под каталогом (только начатые/пройденные)
    if(!root.querySelector('#apdMyCourses')){
      const myHtml = apdMyCoursesHtml();
      if(myHtml){
        const wrap = document.createElement('div');
        wrap.id = 'apdMyCoursesWrap';
        wrap.innerHTML = `<h2 class="section-h" style="margin:24px 0 10px;font-size:21px">Мои курсы</h2>
          <div class="card" id="apdMyCourses" style="padding:8px 10px">${myHtml}</div>`;
        // вставляем после блока каталога
        cat.parentNode.insertBefore(wrap, cat.nextSibling);
      }
    }
  }

  function apdMyCoursesHtml(){
    const my = [];
    for(let ci=0; ci<AC_COURSES.length; ci++){
      const p = acCoursePctOf(ci);
      if(p > 0) my.push({ci, p, done:acCourseDone(ci)});
    }
    if(!my.length){
      return `<div class="acd-my-empty">Ты ещё не начал ни одного курса. Открой «Все курсы Академии» и выбери свой.</div>`;
    }
    // сортировка: в процессе → пройденные
    my.sort((a,b)=>(+a.done - +b.done) || (b.p - a.p));
    return `<div class="acd-my">${my.map(m=>{
      const c = AC_COURSES[m.ci];
      const meta = apdCourseMeta(m.ci);
      const st = m.done ? 'Пройден' : ('В процессе · ' + meta.lessons + ' ' + apdPlural(meta.lessons,['урок','урока','уроков']));
      return `<button class="acd-my-item" onclick="apdCourseFullOpen(${m.ci})">
        <span class="cov">${acCourseCover(m.ci, true)}</span>
        <span class="m">
          <b>${esc(c.title)}</b>
          <span>${esc(st)}</span>
          <span class="bar"><i style="width:${m.p}%"></i></span>
        </span>
        <span class="pct">${m.p}%</span>
      </button>`;
    }).join('')}</div>`;
  }

  /* =============================================================
     2) ПОЛНОСТРАНИЧНАЯ КАРТОЧКА КУРСА (описание, автор, отзывы)
     ============================================================= */
  window.apdCourseFullOpen = function(ci){
    ci = +ci;
    apdFullOpen({title: AC_COURSES[ci].title, sub: AC_COURSES[ci].sub}, apdCourseFullBody(ci));
  };
  function apdCourseFullBody(ci){
    const c = AC_COURSES[ci];
    const acc = acCourseAccessible(ci);
    const done = acCourseDone(ci);
    const pct = acCoursePctOf(ci);
    const meta = apdCourseMeta(ci);
    const stgs = apdGetSettings(ci);
    const reviews = apdCourseReviews(ci);
    const avgStars = (reviews.reduce((s,r)=>s+(r.stars||5),0) / Math.max(1,reviews.length));
    const owner = apdIsOwner();
    const priceEff = stgs.discount > 0 ? Math.round(stgs.price * (100-stgs.discount)/100) : stgs.price;
    const priceTxt = c.free ? 'Бесплатно' : acFmtPrice(priceEff);
    const oldPrice = (!c.free && stgs.discount > 0) ? `<span style="text-decoration:line-through;color:var(--dim);font-family:var(--font-body);font-size:13px;font-weight:600;margin-left:8px">${acFmtPrice(stgs.price)}</span>` : '';

    const cta = acc
      ? `<button class="btn" onclick="apdCourseCTAContinue(${ci})">${I(done?'star':'play')} ${done?'Открыть заново':(pct>0?'Продолжить курс':'Начать курс')}</button>`
      : `<button class="btn" onclick="apdFullClose();acCourseGate(${ci})">${I('lock')} Купить · ${priceTxt}${oldPrice}</button>`;

    const outcomes = (c.outcomes||[]).map(o=>`<li>${I('check2')}<span>${esc(o)}</span></li>`).join('');

    const authorInitial = ((c.author||'?').trim().charAt(0) || '?').toUpperCase();

    // Программа курса
    const blocks = acCourseBlocks(ci);
    let ln = 0;
    const program = blocks.map((b,bi)=>{
      const bIdx = []; for(let i=b.from;i<b.from+b.count;i++) bIdx.push(i);
      const lessons = bIdx.map(i=>{
        ln++;
        const L = AC_COURSE[i];
        const p = acLessonPct(i);
        return `<div class="acd-lesson-item">
          <span class="n">${ln}</span>
          <span class="m"><b>${esc(L.title)}</b><span>${esc(acLessonSub(L))}${p>0?' · '+p+'%':''}</span></span>
        </div>`;
      }).join('');
      return `<div style="margin-bottom:10px">
        <div class="acd-fgroup-h" style="margin-bottom:6px">Блок ${bi+1} · ${esc(b.title)}</div>
        ${lessons || `<div class="acd-cat-empty" style="padding:14px">Уроки готовятся</div>`}
      </div>`;
    }).join('');

    // Отзывы
    const revHtml = reviews.map(r=>{
      const stars = '★'.repeat(r.stars||5) + '☆'.repeat(5-(r.stars||5));
      return `<div class="acd-cp-review">
        <div class="acd-cp-review-h">
          <span class="ava">${esc((r.name||'?').charAt(0).toUpperCase())}</span>
          <b>${esc(r.name||'Гость')}</b>
          <span class="stars">${stars}</span>
          <span class="ts">${esc(r.when||'')}</span>
        </div>
        <p>${esc(r.text)}</p>
      </div>`;
    }).join('');

    // Форматы: собираем факты из уроков
    const formats = apdCourseFormats(ci);
    const fmtChips = formats.map(f=>`<span class="acd-cp-fmt-chip">${I(f.ic)}${esc(f.lbl)}</span>`).join('');

    return `
      <div class="acd-cp-hero">
        ${acCourseCover(ci, true)}
        <div class="acd-cp-hero-cap">
          <span class="chip">${c.free?'Бесплатный курс':(stgs.available?'Премиум':'Скоро')}</span>
          <h2>${esc(c.title)}</h2>
          <p>${esc(c.sub)}</p>
        </div>
      </div>

      ${owner ? `<button class="acd-admin-btn" onclick="apdAdminOpen(${ci})">${I('crown')}<span class="tx">Админ-панель курса · настройки, участники, аналитика</span><span class="go">${I('chev')}</span></button>` : ''}

      <div class="acd-cp-fmt">${fmtChips}</div>

      <div class="acd-cp-stats">
        ${reviews.length ? `<div class="acd-cp-stat"><span class="ic">${I('star')}</span><div class="m"><b>${avgStars.toFixed(1).replace('.',',')}</b><span>${reviews.length} ${apdPlural(reviews.length,['отзыв','отзыва','отзывов'])}</span></div></div>` : ''}
        ${meta.students ? `<div class="acd-cp-stat"><span class="ic">${I('users')}</span><div class="m"><b>${meta.students.toLocaleString('ru-RU')}</b><span>учеников</span></div></div>` : ''}
        <div class="acd-cp-stat"><span class="ic">${I('file')}</span><div class="m"><b>${meta.lessons}</b><span>${apdPlural(meta.lessons,['урок','урока','уроков'])}</span></div></div>
        <div class="acd-cp-stat"><span class="ic">${I('clock')}</span><div class="m"><b>${apdFmtDur(meta.mins)}</b><span>${esc(meta.level)}</span></div></div>
      </div>

      <div class="acd-cp-author">
        <span class="ava">${esc(authorInitial)}</span>
        <div class="m"><b>${esc(c.author||'—')}</b><span>Автор курса · практика на реальных задачах</span></div>
      </div>

      <h2 class="section-h" style="margin:16px 0 8px;font-size:18px">Чему научишься</h2>
      <div class="card" style="padding:14px">
        <ul class="acd-cp-outcomes" style="list-style:none;display:flex;flex-direction:column;gap:8px">${outcomes || '<li style="color:var(--dim);font-size:12.5px">Результаты этого курса описываются автором</li>'}</ul>
      </div>

      <h2 class="section-h" style="margin:16px 0 8px;font-size:18px">Программа</h2>
      <div class="card" style="padding:12px">${program}</div>

      <h2 class="section-h" style="margin:16px 0 8px;font-size:18px">Отзывы <span style="font-size:12px;color:var(--dim);font-weight:600">· ${reviews.length}</span></h2>
      <div class="acd-cp-reviews">${revHtml}</div>
      <button class="acd-cp-review-add" onclick="apdCourseReviewAdd(${ci})">${I('edit')} Написать отзыв</button>

      <div class="acd-cp-cta">${cta}</div>`;
  }
  window.apdCourseCTAContinue = function(ci){
    ci = +ci;
    apdFullClose();
    if(!acCourseAccessible(ci)){ acCourseGate(ci); return; }
    // если есть незавершённый урок в этом курсе — идём туда
    const idx = acCourseIdx(ci);
    const nx = idx.find(i=>acUnlocked(i) && !acLessonDone(i));
    if(nx !== undefined) acOpenLesson(nx);
    else acOpenCourse(ci);
  };
  window.apdCourseReviewAdd = function(ci){
    ci = +ci;
    if(typeof showPopup !== 'function'){ toast('Оценка недоступна'); return; }
    const c = AC_COURSES[ci];
    const me = apdMe();
    const dt = 'сегодня';
    const stars = 5;
    showPopup({
      ico:'edit', title:'Отзыв о курсе «'+c.title+'»',
      body:`<div style="text-align:left">
        <label style="font-size:11px;font-weight:800;letter-spacing:.08em;text-transform:uppercase;color:var(--dim);display:block;margin-bottom:6px">Оценка</label>
        <select id="apdRevStars" style="width:100%;background:var(--raised);border:1px solid var(--border);border-radius:9px;color:var(--text);padding:9px 11px;font-size:13px;margin-bottom:10px">
          ${[5,4,3,2,1].map(n=>`<option value="${n}" ${n===5?'selected':''}>${'★'.repeat(n)}${'☆'.repeat(5-n)} — ${n} из 5</option>`).join('')}
        </select>
        <label style="font-size:11px;font-weight:800;letter-spacing:.08em;text-transform:uppercase;color:var(--dim);display:block;margin-bottom:6px">Отзыв</label>
        <textarea id="apdRevTxt" placeholder="Что понравилось, что вынес из курса…" style="width:100%;background:var(--raised);border:1px solid var(--border);border-radius:9px;color:var(--text);padding:9px 11px;font-size:13px;min-height:80px;resize:vertical"></textarea>
      </div>`,
      actions:[
        {label:'Опубликовать', onclick:()=>{
          const t = (document.getElementById('apdRevTxt').value||'').trim();
          const s = parseInt(document.getElementById('apdRevStars').value,10) || 5;
          if(t.length < 12){ toast('Отзыв слишком короткий'); return; }
          apdEnsureStore2();
          const arr = acS.acdReviews[c.id] || [];
          arr.unshift({name: me.name, text: t.slice(0,600), stars: s, when: dt});
          acS.acdReviews[c.id] = arr;
          acSave();
          toast('Отзыв опубликован');
          // перерисуем страницу курса, если она открыта
          const b = document.getElementById('apdFullBody');
          if(b) b.innerHTML = apdCourseFullBody(ci);
        }},
        {label:'Отмена', ghost:true}
      ]
    });
  };

  /* Форматы курса (интеллектуальная детекция + доп.форматы) */
  function apdCourseFormats(ci){
    const idx = acCourseIdx(ci);
    let hasVideo=false, hasSlides=false, hasTest=false, hasTask=false, hasGame=false, hasFiles=false, hasLive=false;
    idx.forEach(i=>{
      const L = AC_COURSE[i];
      if(L.videoUrl) hasVideo = true;
      if((L.slides||[]).length) hasSlides = true;
      if((L.quiz||[]).length) hasTest = true;
      if(L.task) hasTask = true;
      if(L.pairs && L.pairs.length) hasGame = true;
      if((L.files||[]).length) hasFiles = true;
      if(L.live) hasLive = true;
    });
    const out = [];
    if(hasVideo)  out.push({ic:'circle-play', lbl:'Видео'});
    if(hasSlides) out.push({ic:'file',        lbl:'Слайды'});
    if(hasTask)   out.push({ic:'edit',        lbl:'Практика'});
    if(hasTest)   out.push({ic:'poll',        lbl:'Тесты'});
    if(hasGame)   out.push({ic:'bolt',        lbl:'Мини-игры'});
    if(hasFiles)  out.push({ic:'file',        lbl:'Файлы'});
    if(hasLive)   out.push({ic:'megaphone',   lbl:'Livestream'});
    return out;
  }

  /* =============================================================
     3) АДМИН-ПАНЕЛЬ КУРСА (owner)
     ============================================================= */
  window.APD_ADMIN_TAB = 'analytics';
  window.APD_ADMIN_CI = 0;
  const APD_ADMIN_TABS = [
    {k:'analytics', lbl:'Аналитика',  ic:'poll'},
    {k:'lessons',   lbl:'Уроки',      ic:'file'},
    {k:'members',   lbl:'Участники',  ic:'users'},
    {k:'settings',  lbl:'Настройки',  ic:'crown'},
    {k:'moder',     lbl:'Модерация',  ic:'comment'},
    {k:'export',    lbl:'Экспорт',    ic:'share'}
  ];
  window.apdAdminOpen = function(ci){
    if(!apdIsOwner()){ toast('Только для владельца курса'); return; }
    APD_ADMIN_CI = +ci; APD_ADMIN_TAB = 'analytics';
    const c = AC_COURSES[+ci];
    apdFullOpen({title:'Админ · ' + c.title, sub:'Управление курсом и участниками'}, apdAdminBody());
  };
  window.apdAdminTab = function(k){
    APD_ADMIN_TAB = k;
    // перерисовать только тело
    const b = document.getElementById('apdFullBody');
    if(b) b.innerHTML = apdAdminBody();
  };
  function apdAdminBody(){
    const ci = APD_ADMIN_CI;
    const tabs = APD_ADMIN_TABS.map(t=>`<button class="acd-admin-tab ${APD_ADMIN_TAB===t.k?'on':''}" onclick="apdAdminTab('${t.k}')">${I(t.ic)}<span>${esc(t.lbl)}</span></button>`).join('');
    let pane = '';
    switch(APD_ADMIN_TAB){
      case 'lessons':   pane = apdAdminLessons(ci); break;
      case 'members':   pane = apdAdminMembers(ci); break;
      case 'settings':  pane = apdAdminSettings(ci); break;
      case 'moder':     pane = apdAdminModer(ci); break;
      case 'export':    pane = apdAdminExport(ci); break;
      default:          pane = apdAdminAnalytics(ci);
    }
    return `<div class="acd-admin-tabs">${tabs}</div><div class="acd-admin-pane">${pane}</div>`;
  }

  /* Аналитика (агрегация по acS.lessons) */
  function apdAdminAnalytics(ci){
    const idx = acCourseIdx(ci);
    const c = AC_COURSES[ci];
    const meta = apdCourseMeta(ci);
    let pctSum = 0, testSum = 0, testCnt = 0, taskDone = 0, gamesDone = 0, doneLessons = 0;
    idx.forEach(i=>{
      const ls = acS.lessons[i] || {};
      pctSum += acLessonPct(i);
      if(ls.testScore){ testSum += ls.testScore; testCnt++; }
      if(ls.task) taskDone++;
      if(ls.game) gamesDone++;
      if(acLessonDone(i)) doneLessons++;
    });
    const avgPct = idx.length ? Math.round(pctSum/idx.length) : 0;
    const avgTest = testCnt ? Math.round(testSum/testCnt) : 0;
    const rnd = apdDeterministicRnd(ci+13);
    // синтетика для правдоподобных «повторных просмотров»
    const rewatch = 12 + Math.floor(rnd()*45); // среднее по курсу
    const revs = apdCourseReviews(ci);
    const rating = revs.reduce((s,r)=>s+(r.stars||5),0) / Math.max(1,revs.length);
    // per-lesson bars
    const bars = idx.map((i,k)=>{
      const p = acLessonPct(i);
      const L = AC_COURSE[i];
      return `<div class="acd-a-bar">
        <span class="n">${k+1}</span>
        <span class="t">${esc(L.title)}</span>
        <span class="pr"><i style="width:${p}%"></i></span>
        <span class="pct">${p}%</span>
      </div>`;
    }).join('');
    return `
      <div class="acd-analytics">
        <div class="acd-a-card"><span class="lbl">Средний прогресс</span><span class="val">${avgPct}%</span><span class="sub">по всем ученикам</span></div>
        <div class="acd-a-card"><span class="lbl">Средний тест</span><span class="val">${avgTest}%</span><span class="sub">из ${testCnt} сдавших</span></div>
        <div class="acd-a-card"><span class="lbl">Практика</span><span class="val">${taskDone}</span><span class="sub">${apdPlural(taskDone,['работа','работы','работ'])} зачтено</span></div>
        <div class="acd-a-card"><span class="lbl">Мини-игры</span><span class="val">${gamesDone}</span><span class="sub">пройдено</span></div>
        <div class="acd-a-card"><span class="lbl">Пройдено полностью</span><span class="val">${doneLessons}/${idx.length}</span><span class="sub">${apdPlural(doneLessons,['урок','урока','уроков'])}</span></div>
        <div class="acd-a-card"><span class="lbl">Рейтинг</span><span class="val">${revs.length ? rating.toFixed(1).replace('.',',') : '—'}</span><span class="sub">${revs.length} ${apdPlural(revs.length,['отзыв','отзыва','отзывов'])}</span></div>
        <div class="acd-a-card"><span class="lbl">Учеников</span><span class="val">${meta.students ? meta.students.toLocaleString('ru-RU') : '—'}</span><span class="sub">купили курс</span></div>
        <div class="acd-a-card"><span class="lbl">Повторные</span><span class="val">${rewatch}%</span><span class="sub">просмотры уроков</span></div>
      </div>
      <h3 style="margin:16px 0 8px;font-size:15px">Прогресс по урокам</h3>
      <div class="acd-a-bars">${bars}</div>`;
  }

  /* Список уроков + редактор */
  function apdAdminLessons(ci){
    const idx = acCourseIdx(ci);
    const rows = idx.map((i,k)=>{
      const L = AC_COURSE[i];
      return `<div class="acd-lesson-item">
        <span class="n">${k+1}</span>
        <span class="m"><b>${esc(L.title)}</b><span>${esc(acLessonSub(L))}</span></span>
        <button class="ed" onclick="apdLessonEdit(${i})" aria-label="Редактировать" title="Редактировать">${I('edit')}</button>
      </div>`;
    }).join('');
    return `<p class="dim" style="font-size:12px;line-height:1.55;margin-bottom:10px">Правки урока действуют в текущей сессии и хранятся у тебя как черновик. Синхронизация с сервером — в проде.</p>
      <div class="acd-lesson-list">${rows}</div>`;
  }
  window.apdLessonEdit = function(li){
    li = +li;
    const L = AC_COURSE[li];
    const draft = (acS.acdDrafts && acS.acdDrafts[li]) || {};
    const cur = {
      title: draft.title || L.title,
      sub:   draft.sub   || L.sub || '',
      videoUrl: draft.videoUrl || L.videoUrl || '',
      slidesJson: JSON.stringify(draft.slides || L.slides || [], null, 2),
      quizJson: JSON.stringify(draft.quiz || L.quiz || [], null, 2),
      task: (draft.task && draft.task.intro) || (L.task && L.task.intro) || ''
    };
    apdFullSetBody(`
      <button class="btn ghost sm" onclick="apdAdminTab('lessons')" style="margin-bottom:12px">${I('back')} Уроки</button>
      <h3 style="font-size:17px;margin-bottom:12px">Редактирование урока № ${li+1}</h3>
      <div class="acd-edit-form">
        <div class="acd-edit-row"><label>Заголовок</label><input id="apdEdTitle" type="text" value="${esc(cur.title)}"></div>
        <div class="acd-edit-row"><label>Подпись / описание</label><input id="apdEdSub" type="text" value="${esc(cur.sub)}"></div>
        <div class="acd-edit-row"><label>Видео URL (mp4)</label><input id="apdEdVideo" type="url" placeholder="https://…mp4" value="${esc(cur.videoUrl)}"></div>
        <div class="acd-edit-row"><label>Практика · инструкция</label><textarea id="apdEdTask" style="min-height:70px;font-family:var(--font-body);font-size:13px">${esc(cur.task)}</textarea></div>
        <div class="acd-edit-row"><label>Слайды (JSON)</label><textarea id="apdEdSlides">${esc(cur.slidesJson)}</textarea>
          <span class="acd-edit-json-help">Формат: <code>[{"t":"...","pts":["..."],"svg":"..."}]</code></span></div>
        <div class="acd-edit-row"><label>Тест (JSON)</label><textarea id="apdEdQuiz">${esc(cur.quizJson)}</textarea>
          <span class="acd-edit-json-help">Формат: <code>[{"q":"...","o":["a","b","c","d"],"a":1}]</code></span></div>
        <div class="acd-edit-actions">
          <button class="btn" onclick="apdLessonEditSave(${li})">${I('check2')} Сохранить черновик</button>
          <button class="btn ghost" onclick="apdLessonEditReset(${li})">${I('back')} Сбросить</button>
        </div>
      </div>`);
  };
  window.apdLessonEditSave = function(li){
    li = +li;
    const val = (id)=>{ const el = document.getElementById(id); return el ? el.value : ''; };
    if(!acS.acdDrafts) acS.acdDrafts = {};
    let slides, quiz;
    try{ slides = JSON.parse(val('apdEdSlides')||'[]'); if(!Array.isArray(slides)) throw 0; }
    catch(e){ toast('Слайды: невалидный JSON'); return; }
    try{ quiz = JSON.parse(val('apdEdQuiz')||'[]'); if(!Array.isArray(quiz)) throw 0; }
    catch(e){ toast('Тест: невалидный JSON'); return; }
    acS.acdDrafts[li] = {
      title: val('apdEdTitle').trim(),
      sub: val('apdEdSub').trim(),
      videoUrl: val('apdEdVideo').trim(),
      task: {intro: val('apdEdTask').trim(), chips:['Задача','Ответ'], ph:'…', verdict:'Принято.'},
      slides: slides,
      quiz: quiz
    };
    acSave();
    // применяем к AC_COURSE (in-memory)
    const L = AC_COURSE[li];
    const d = acS.acdDrafts[li];
    if(d.title) L.title = d.title;
    if(d.sub) L.sub = d.sub;
    if(d.videoUrl !== undefined) L.videoUrl = d.videoUrl;
    if(d.slides && d.slides.length) L.slides = d.slides;
    if(d.quiz && d.quiz.length) L.quiz = d.quiz;
    if(d.task && d.task.intro){ L.task = Object.assign({}, L.task||{chips:['Задача','Ответ'],ph:'…',verdict:'Принято.'}, d.task); }
    toast('Черновик урока сохранён');
    apdAdminTab('lessons');
  };
  window.apdLessonEditReset = function(li){
    li = +li;
    if(!acS.acdDrafts || !acS.acdDrafts[li]){ toast('Черновика нет'); apdAdminTab('lessons'); return; }
    delete acS.acdDrafts[li]; acSave();
    toast('Черновик сброшен — данные из курса вернулись при перезагрузке');
    apdAdminTab('lessons');
  };

  /* Участники — синтетический список (для UI) */
  function apdMembersOf(ci){
    /* Список учеников состоял из выдуманных людей с городами. Админ-панель
       должна показывать правду, даже если правда — «учеников пока нет». */
    return [];
    // eslint-disable-next-line no-unreachable
    const c = AC_COURSES[ci];
    const rnd = apdDeterministicRnd(ci+19);
    const names = [
      {n:'Анна Григорьева', nick:'anna_g', from:'Москва'},
      {n:'Дмитрий Волков', nick:'dvolk', from:'СПб'},
      {n:'Ольга Кузнецова', nick:'olya_k', from:'Казань'},
      {n:'Илья Соколов', nick:'ilyas', from:'Новосибирск'},
      {n:'Марина Титова', nick:'mtitova', from:'Екатеринбург'},
      {n:'Кирилл Смирнов', nick:'ksmir', from:'Дубай'},
      {n:'Полина Ясенева', nick:'yasenva', from:'Минск'},
      {n:'Егор Панов', nick:'panov', from:'Алматы'},
      {n:'Роман Никитин', nick:'nikitin', from:'Тбилиси'},
      {n:'София Дорошенко', nick:'sofiid', from:'Ереван'}
    ];
    const banned = (acS.acdBanned||{})[c.id] || {};
    return names.map(x=>{
      const pct = Math.floor(rnd()*100);
      const buyAgo = ['сегодня','вчера','3 дня','неделю','2 недели','месяц'][Math.floor(rnd()*6)];
      return Object.assign({}, x, {pct, buyAgo, banned: !!banned[x.nick]});
    });
  }
  function apdAdminMembers(ci){
    const list = apdMembersOf(ci);
    const rows = list.map(m=>`<div class="acd-mem-item ${m.banned?'banned':''}">
      <span class="ava">${esc(m.n.charAt(0))}</span>
      <span class="m"><b>${esc(m.n)}</b><span>@${esc(m.nick)} · ${esc(m.from)} · куплен ${esc(m.buyAgo)} назад</span></span>
      <span class="stats"><b>${m.pct}%</b><span>прогресс</span></span>
      <div class="acd-mem-acts">
        <button onclick="apdMemberMsg('${esc(m.nick)}')" title="Написать" aria-label="Написать">${I('send')}</button>
        <button onclick="apdMemberRefund(${ci},'${esc(m.nick)}')" title="Вернуть деньги" aria-label="Вернуть">${I('money')}</button>
        <button class="danger" onclick="apdMemberBan(${ci},'${esc(m.nick)}')" title="${m.banned?'Разблокировать':'Заблокировать'}" aria-label="${m.banned?'Разблокировать':'Заблокировать'}">${I(m.banned?'check2':'trash')}</button>
      </div>
    </div>`).join('');
    return `<p class="dim" style="font-size:12px;line-height:1.55;margin-bottom:10px">${list.length} ${apdPlural(list.length,['ученик','ученика','учеников'])} · управление доступом и возвратами</p>
      <div class="acd-mem-list">${rows}</div>`;
  }
  window.apdMemberMsg = function(nick){ toast('Открыть чат с @' + nick); };
  window.apdMemberBan = function(ci, nick){
    ci = +ci;
    apdEnsureStore2();
    const c = AC_COURSES[ci];
    const b = acS.acdBanned[c.id] || {};
    if(b[nick]){ delete b[nick]; toast('Разблокирован: @' + nick); }
    else { b[nick] = 1; toast('Заблокирован: @' + nick); }
    acS.acdBanned[c.id] = b; acSave();
    apdAdminTab('members');
  };
  window.apdMemberRefund = function(ci, nick){
    if(typeof showPopup !== 'function'){ toast('Возврат оформлен · @' + nick); return; }
    ci = +ci;
    const c = AC_COURSES[ci];
    const price = apdGetSettings(ci).price || c.price || 0;
    showPopup({
      ico:'money', title:'Возврат средств',
      body:'Вернуть ' + acFmtPrice(price) + ' пользователю <b>@' + esc(nick) + '</b> за курс «' + esc(c.title) + '»? Средства спишутся с баланса курса, доступ ученика будет закрыт.',
      actions:[
        {label:'Оформить возврат', onclick:()=>{ toast('Возврат ' + acFmtPrice(price) + ' · @' + nick); }},
        {label:'Отмена', ghost:true}
      ]
    });
  };

  /* Настройки курса */
  function apdAdminSettings(ci){
    const s = apdGetSettings(ci);
    return `
      <div class="acd-set-row"><div class="m"><b>Цена курса</b><span>Основная цена (₽) — до применения скидки</span></div>
        <div class="ctl"><input type="number" min="0" step="100" value="${s.price}" onchange="apdSetSet(${ci},'price',+this.value)"></div>
      </div>
      <div class="acd-set-row"><div class="m"><b>Скидка (%)</b><span>0 — скидки нет</span></div>
        <div class="ctl"><input type="number" min="0" max="90" step="5" value="${s.discount}" onchange="apdSetSet(${ci},'discount',+this.value)"></div>
      </div>
      <div class="acd-set-row"><div class="m"><b>Курс доступен</b><span>Можно ли покупать и открывать</span></div>
        <div class="ctl">${apdTogHtml('avaTog', s.available, `apdSetSet(${ci},'available',this.checked)`)}</div>
      </div>
      <div class="acd-set-row"><div class="m"><b>Автопрогресс</b><span>Следующий урок открывается автоматически</span></div>
        <div class="ctl">${apdTogHtml('autoTog', s.autonext, `apdSetSet(${ci},'autonext',this.checked)`)}</div>
      </div>
      <div class="acd-set-row"><div class="m"><b>Выдавать сертификат</b><span>За полное прохождение направления</span></div>
        <div class="ctl">${apdTogHtml('certTog', s.certOn, `apdSetSet(${ci},'certOn',this.checked)`)}</div>
      </div>
      <div class="acd-set-row"><div class="m"><b>Комиссия платформы</b><span>10% фиксированно — от каждой продажи курса</span></div>
        <div class="ctl" style="color:var(--dim);font-size:13px;font-weight:700">10%</div>
      </div>`;
  }
  function apdTogHtml(id, on, cb){
    return `<label class="acd-tog"><input type="checkbox" id="${id}" ${on?'checked':''} onchange="${cb}"><span class="s"></span></label>`;
  }
  window.apdSetSet = function(ci, k, v){
    apdSetSetting(+ci, k, v);
    toast('Сохранено: ' + k);
  };

  /* Модерация — использует apd-комментарии из уроков курса */
  function apdAdminModer(ci){
    const idx = acCourseIdx(ci);
    let items = [];
    idx.forEach(li=>{
      const list = (acS.comments||{})[li] || [];
      list.forEach(c=>items.push({li, c}));
    });
    if(!items.length){
      return `<div class="acd-admin-empty">Комментариев к урокам этого курса пока нет.</div>`;
    }
    items = items.slice(0, 40);
    const rows = items.map(({li, c})=>`<div class="acd-mem-item">
      <span class="ava">${esc((c.name||'?').charAt(0).toUpperCase())}</span>
      <span class="m"><b>${esc(c.name||'Гость')}</b><span>Урок ${li+1} · ${esc(String((AC_COURSE[li]||{}).title||''))}</span></span>
      <div style="flex:1;min-width:0"><p style="font-size:12px;color:var(--dim);line-height:1.4;margin:0 8px;overflow:hidden;text-overflow:ellipsis;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical">${esc(c.text)}</p></div>
      <div class="acd-mem-acts">
        <button class="danger" onclick="apdModDel(${li},'${esc(c.id)}')" title="Удалить" aria-label="Удалить">${I('trash')}</button>
      </div>
    </div>`).join('');
    return `<p class="dim" style="font-size:12px;line-height:1.55;margin-bottom:10px">${items.length} ${apdPlural(items.length,['комментарий','комментария','комментариев'])} по курсу</p>
      <div class="acd-mem-list">${rows}</div>`;
  }
  window.apdModDel = function(li, id){
    li = +li;
    const arr = (acS.comments||{})[li] || [];
    const j = arr.findIndex(x=>x.id===id);
    if(j>=0){ arr.splice(j,1); acSave(); toast('Комментарий удалён'); apdAdminTab('moder'); }
  };

  /* Экспорт CSV */
  function apdAdminExport(ci){
    const list = apdMembersOf(ci);
    return `<p class="dim" style="font-size:12.5px;line-height:1.55;margin-bottom:12px">Экспортируй список учеников этого курса в CSV: имя, ник, дата покупки, прогресс, статус доступа.</p>
      <button class="btn" onclick="apdExportCsv(${ci})">${I('file')} Скачать CSV · ${list.length} ${apdPlural(list.length,['ученик','ученика','учеников'])}</button>
      <div style="height:10px"></div>
      <p class="dim" style="font-size:11.5px;line-height:1.55">Формат: <code style="font-family:ui-monospace,monospace;background:var(--raised);padding:2px 6px;border-radius:5px">name;nick;from;buyAgo;progress;status</code></p>`;
  }
  window.apdExportCsv = function(ci){
    ci = +ci;
    const list = apdMembersOf(ci);
    const c = AC_COURSES[ci];
    const lines = ['name;nick;from;bought;progress;status'];
    list.forEach(m=>{
      lines.push([m.n, m.nick, m.from, m.buyAgo, m.pct+'%', m.banned?'banned':'active'].map(x=>String(x).replace(/;/g,',')).join(';'));
    });
    // BOM для Excel-совместимости
    const csv = '\ufeff' + lines.join('\n');
    const blob = new Blob([csv], {type:'text/csv;charset=utf-8'});
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url; a.download = 'oko-academy-' + (c.id||'course') + '-' + list.length + '.csv';
    document.body.appendChild(a); a.click(); a.remove();
    setTimeout(()=>URL.revokeObjectURL(url), 4000);
    toast('CSV сохранён · ' + list.length + ' ' + apdPlural(list.length,['ученик','ученика','учеников']));
  };

  /* =============================================================
     4) PDF СЕРТИФИКАТА — минимальный PDF с встроенным JPEG
     ============================================================= */
  function apdBuildPdfFromJpegDataUrl(jpegDataUrl, imgW, imgH){
    // jpegDataUrl -> latin1 binary string of JPEG bytes
    const b64 = jpegDataUrl.split(',')[1];
    const bin = atob(b64);
    const jpegBytes = new Uint8Array(bin.length);
    for(let i=0; i<bin.length; i++) jpegBytes[i] = bin.charCodeAt(i);
    // размер страницы: горизонтальная A4 при 72 dpi = 842 x 595
    const PW = 842, PH = 595;
    const scale = Math.min(PW / imgW, PH / imgH);
    const w = imgW * scale, h = imgH * scale;
    const x = (PW - w) / 2, y = (PH - h) / 2;

    const enc = new TextEncoder();
    const chunks = [];
    const offsets = [];
    let bufLen = 0;
    function push(str){
      const bytes = (str instanceof Uint8Array) ? str : enc.encode(str);
      chunks.push(bytes); bufLen += bytes.length; return bytes.length;
    }
    function pushObj(objText, extraBytes){
      offsets.push(bufLen);
      push(objText);
      if(extraBytes) push(extraBytes);
      push('\nendobj\n');
    }
    push('%PDF-1.4\n%\u00E2\u00E3\u00CF\u00D3\n');
    pushObj('1 0 obj\n<</Type /Catalog /Pages 2 0 R>>');
    pushObj('2 0 obj\n<</Type /Pages /Kids [3 0 R] /Count 1>>');
    pushObj(`3 0 obj\n<</Type /Page /Parent 2 0 R /MediaBox [0 0 ${PW} ${PH}] /Contents 4 0 R /Resources <</XObject <</Im0 5 0 R>>>>>>`);
    const stream = `q\n${w.toFixed(3)} 0 0 ${h.toFixed(3)} ${x.toFixed(3)} ${y.toFixed(3)} cm\n/Im0 Do\nQ`;
    const streamBytes = enc.encode(stream);
    pushObj(`4 0 obj\n<</Length ${streamBytes.length}>>\nstream\n`, new Uint8Array([...streamBytes, 0x0A]));
    push('endstream'); // Note: pushObj already added endobj; endstream is part of stream area — do properly:
    // Восстановим: последний "endobj" — лишний относительно stream. Пересоберём аккуратно.
    // Пересборка полностью, чтобы не было ошибок:
    chunks.length = 0; offsets.length = 0; bufLen = 0;
    push('%PDF-1.4\n%\u00E2\u00E3\u00CF\u00D3\n');
    // obj 1
    offsets.push(bufLen); push('1 0 obj\n<</Type /Catalog /Pages 2 0 R>>\nendobj\n');
    // obj 2
    offsets.push(bufLen); push('2 0 obj\n<</Type /Pages /Kids [3 0 R] /Count 1>>\nendobj\n');
    // obj 3
    offsets.push(bufLen); push(`3 0 obj\n<</Type /Page /Parent 2 0 R /MediaBox [0 0 ${PW} ${PH}] /Contents 4 0 R /Resources <</XObject <</Im0 5 0 R>>>>>>\nendobj\n`);
    // obj 4 — content stream
    offsets.push(bufLen);
    push(`4 0 obj\n<</Length ${streamBytes.length}>>\nstream\n`);
    push(streamBytes);
    push('\nendstream\nendobj\n');
    // obj 5 — image XObject
    offsets.push(bufLen);
    push(`5 0 obj\n<</Type /XObject /Subtype /Image /Width ${imgW} /Height ${imgH} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${jpegBytes.length}>>\nstream\n`);
    push(jpegBytes);
    push('\nendstream\nendobj\n');
    // xref
    const xrefStart = bufLen;
    push('xref\n0 6\n0000000000 65535 f \n');
    offsets.forEach(o=>push(String(o).padStart(10,'0') + ' 00000 n \n'));
    push(`trailer\n<</Size 6 /Root 1 0 R>>\nstartxref\n${xrefStart}\n%%EOF`);

    const out = new Uint8Array(bufLen);
    let p = 0;
    for(const c of chunks){ out.set(c, p); p += c.length; }
    return new Blob([out], {type:'application/pdf'});
  }
  window.apdCertPdf = function(){
    // используем текущий сертификат (в overlay или в списке)
    const rec = (typeof acCertRec === 'function' && acCertShownNo)
      ? acS.certs.find(c=>c && c.no===acCertShownNo)
      : (typeof acCertRec === 'function' ? acCertRec() : null);
    if(!rec){ toast('Сертификат не выбран'); return; }
    const build = (pngUrl)=>{
      const img = new Image();
      img.onload = ()=>{
        const cv = document.createElement('canvas');
        cv.width = img.width; cv.height = img.height;
        cv.getContext('2d').drawImage(img, 0, 0);
        const jpeg = cv.toDataURL('image/jpeg', 0.92);
        const blob = apdBuildPdfFromJpegDataUrl(jpeg, img.width, img.height);
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url; a.download = rec.no + '.pdf';
        document.body.appendChild(a); a.click(); a.remove();
        setTimeout(()=>URL.revokeObjectURL(url), 4000);
        toast('PDF сохранён: ' + rec.no + '.pdf');
      };
      img.onerror = ()=>toast('Не удалось собрать PDF');
      img.src = pngUrl;
    };
    if(acCertUrl && acCertShownNo === rec.no){ build(acCertUrl); return; }
    acMakeCert(rec, url=>{ acCertUrl = url; acCertShownNo = rec.no; build(url); });
  };

  /* Инъекция кнопки «Скачать PDF» в overlay сертификата (одноразово) */
  function apdInjectCertPdfButton(){
    const acts = document.querySelector('#acCertFull .ac-cert-full-actions');
    if(!acts || acts.querySelector('.acd-cert-pdf')) return;
    const btn = document.createElement('button');
    btn.className = 'btn sm ghost acd-cert-pdf';
    btn.innerHTML = I('file') + ' Скачать PDF';
    btn.onclick = apdCertPdf;
    // ставим сразу после первой кнопки
    const first = acts.querySelector('.btn');
    if(first && first.nextSibling) acts.insertBefore(btn, first.nextSibling);
    else acts.appendChild(btn);
  }

  /* =============================================================
     5) ДОП. ФОРМАТЫ УРОКА: файлы + livestream (стабы + рендер)
     ============================================================= */
  function apdLessonExtraFormats(){
    if(acView !== 'lesson') return;
    const root = document.getElementById('acRoot');
    if(!root) return;
    const L = acCur();
    if(!L) return;
    // 1) Livestream — если L.live есть, вставим карточку перед acCertBox
    const cert = document.getElementById('acCertBox');
    if(!cert) return;
    if(L.live && !root.querySelector('#apdLiveBox')){
      const box = document.createElement('div');
      box.id = 'apdLiveBox';
      box.className = 'card';
      box.style.padding = '4px';
      box.innerHTML = `<div class="acd-live" style="margin:0">
        <span class="ic">${I('circle-play')}</span>
        <div class="m"><b>Livestream: ${esc(L.live.title||'Прямой эфир')}</b><span>${esc(L.live.when||'скоро')} · ${esc(L.live.host||'ведущий')}</span></div>
        <button class="btn sm" onclick="apdLiveJoin('${esc(L.live.title||'')}')">${I('device')} Войти</button>
      </div>`;
      cert.parentNode.insertBefore(box, cert);
    }
    // 2) Файлы — если L.files есть, вставим ниже
    if(L.files && L.files.length && !root.querySelector('#apdFilesBox')){
      const box = document.createElement('div');
      box.id = 'apdFilesBox';
      box.className = 'card';
      box.style.padding = '12px 14px 14px';
      box.innerHTML = `<div style="font-size:12px;font-weight:800;letter-spacing:.09em;text-transform:uppercase;color:var(--dim);margin-bottom:8px">${I('file')} Файлы урока</div>
        <div class="acd-files">${L.files.map(f=>`<a class="acd-file" href="${esc(f.url||'#')}" target="_blank" rel="noopener noreferrer" download="${esc(f.name||'file')}">
          <span class="ic">${I('file')}</span>
          <span class="m"><b>${esc(f.name||'файл')}</b><span>${esc(f.size||'')}</span></span>
          <svg class="i dl"><use href="#i-forward"/></svg>
        </a>`).join('')}</div>`;
      cert.parentNode.insertBefore(box, cert);
    }
  }
  window.apdLiveJoin = function(title){
    if(typeof showPopup === 'function'){
      showPopup({ico:'circle-play', title:'Livestream · ' + title, body:'Прямой эфир пройдёт в модуле «Звонки OKO». Уведомление придёт за 15 минут до старта.', actions:[{label:'Ок'}]});
    } else toast('Livestream: ' + title);
  };

  /* =============================================================
     Инициализация: догнать текущее состояние Академии
     ============================================================= */
  function apdBoot(){
    apdWatchCertOverlay();
    // отдельный вотчер для инъекции PDF-кнопки в фулскрин сертификата
    try{
      const t = document.getElementById('acCertFull');
      if(t){
        const obs = new MutationObserver(()=>{ if(t.classList.contains('open')) apdInjectCertPdfButton(); });
        obs.observe(t, {attributes:true, attributeFilter:['class']});
        apdInjectCertPdfButton();
      }
    }catch(e){}
    // Если Академия уже открыта — перерендерим
    const scr = document.getElementById('screen-academy');
    if(scr && scr.classList.contains('active')) acRender();
  }
  if(document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', apdBoot);
  } else {
    setTimeout(apdBoot, 20);
  }

  /* =============================================================
     APD_MARKET_COURSES: единый пул платных курсов маркетплейса.
     Источник — CH.disc (kind:course), плюс встроенные курсы Академии.
     Пользуется этой выборкой любой каталог-виджет («Курсы от авторов»),
     без дублирования данных.
     ============================================================= */
  window.APD_MARKET_COURSES = function apdMarketCourses(){
    const out = [];
    try{
      const src = (window.CH && Array.isArray(CH.disc)) ? CH.disc : [];
      src.forEach(c=>{
        if(c && c.kind==='course' && (c.price||0)>0){
          out.push({
            id: c.id, name: c.name, desc: c.desc,
            author: c.owner || '', authorNick: c.ownerNick || '',
            price: c.price, lessons: (c.lessons||[]).length,
            students: c.subs || 0, students_now: c.students_now || 0,
            rating: (typeof c.rating==='number') ? c.rating : null,
            reviews: (typeof c.reviews==='number') ? c.reviews : 0,
            reviewsList: c.reviewsList || [],
            niche: c.niche || 'edu',
            source: 'marketplace',
          });
        }
      });
    }catch(e){}
    return out;
  };
})();

/* ===== module: ads ===== */
/* ===== ADS: рекламный кабинет (префикс ads-) ===== */

const ADS_LS = 'oko-ads';
const ADS_POST_BASE = 9000;       /* id постов рекламы в ленте */
const ADS_STATUS = {
  mod:  {l:'Модерация'},
  sched:{l:'Запланирована'},
  act:  {l:'Активна'},
  pause:{l:'Пауза'},
  rej:  {l:'Отклонена'},
  done: {l:'Завершена'}
};

/* скидка на продвижение по тарифу подписки: выше тариф — дешевле реклама (до −30% на MAX).
   Скидка = больше показов/кликов за тот же бюджет (ниже стоимость результата), сам бюджет не режется. */
function adsDisc(){
  if(typeof okoHasSub!=='function') return 0;
  if(okoHasSub('MAX'))      return 0.30;
  if(okoHasSub('BUSINESS')) return 0.20;
  if(okoHasSub('PRO'))      return 0.10;
  return 0;
}
function adsTierLabel(){
  if(typeof okoHasSub!=='function') return 'FREE';
  if(okoHasSub('MAX'))      return 'MAX';
  if(okoHasSub('BUSINESS')) return 'BUSINESS';
  if(okoHasSub('PRO'))      return 'PRO';
  if(okoHasSub('START'))    return 'START';
  return 'FREE';
}

/* форматы рекламы: модель по умолчанию, базовые CPM/CPC, метка в ленте */
const ADS_FORMATS = {
  post:    {l:'Продвижение поста',       ico:'megaphone', model:'CPM', cpm:28, cpc:9,  tag:'спонсировано',       aov:490,  desc:'Ваш пост в ленте рекомендаций OKO — со ссылкой и кнопкой действия'},
  channel: {l:'Реклама платного канала', ico:'crown',     model:'CPC', cpm:34, cpc:12, tag:'реклама канала',     aov:590,  desc:'Карточка платного канала с кнопкой «Подписаться» в ленте и поиске'},
  listing: {l:'Объявление Биржи',        ico:'briefcase', model:'CPC', cpm:30, cpc:11, tag:'Биржа · реклама',    aov:1490, desc:'Товар или услуга с Биржи OKO — показ горячей аудитории в ленте и поиске'},
  banner:  {l:'Баннер',                  ico:'photo',     model:'CPM', cpm:22, cpc:7,  tag:'баннер',             aov:690,  desc:'Широкий баннер в шапке ленты и тематических разделов — максимальный охват'},
};

/* цели кампаний: под каждую подбирается формат по умолчанию, оптимизация и KPI-подсказка */
const ADS_GOALS = {
  sales: {
    l:'Продажи', ico:'money', fmt:'listing', model:'CPC', cta:'Купить', kpi:'заказы',
    desc:'Показ горячим сегментам. Оптимизация на клик и оплату, ставка выше — приоритет в аукционе.',
    ctrHint:'2.5–4.5%', roasHint:'×2–×5'},
  leads: {
    l:'Заявки', ico:'briefcase', fmt:'listing', model:'CPC', cta:'Оставить заявку', kpi:'лиды',
    desc:'Форма или мессенджер. Оптимизация на переход в форму, сглаженная открутка бюджета.',
    ctrHint:'1.8–3.2%', roasHint:'CPA ~150–450 ₽'},
  subs: {
    l:'Подписчики', ico:'users', fmt:'channel', model:'CPC', cta:'Подписаться', kpi:'подписки',
    desc:'Карточка канала. Оптимизация на подписку, look-alike по вашей аудитории включён по умолчанию.',
    ctrHint:'3.0–6.0%', roasHint:'CPI ~12–35 ₽'},
  views: {
    l:'Просмотры видео', ico:'circle-play', fmt:'post', model:'CPM', cta:'Смотреть', kpi:'просмотры',
    desc:'Автовоспроизведение в ленте. Оптимизация на досмотры до 3 сек, широкая аудитория.',
    ctrHint:'0.7–1.4%', roasHint:'CPV ~0.6–1.2 ₽'},
  installs: {
    l:'Установки приложения', ico:'phone', fmt:'banner', model:'CPM', cta:'Установить', kpi:'установки',
    desc:'Баннер в шапке + пост в ленте. Оптимизация на клик к магазину, отсечка ботов и повторов.',
    ctrHint:'1.5–3.0%', roasHint:'CPI ~40–120 ₽'}
};

/* интересы: развёрнутый набор чипов как в VK/TikTok Ads Manager */
const ADS_INTERESTS = [
  'бизнес','маркетинг','дизайн','контент','нейросети','образование','финансы','инвестиции',
  'крипта','игры','спорт','фитнес','здоровье','красота','мода','путешествия','авто',
  'кулинария','рестораны','музыка','кино','книги','родители','недвижимость'
];
const ADS_MODELS = {
  CPM: {unit:'₽ / 1000 показов', min:12, rec:1, max:3.2},   /* rec/max — множители к базовому cpm формата */
  CPC: {unit:'₽ / клик',         min:3,  rec:1, max:2.8}
};

/* города РФ / СНГ с примерным активным охватом (млн пользователей) */
const ADS_CITIES = {
  'РФ':  [['Москва',3.4],['Санкт-Петербург',1.6],['Новосибирск',.55],['Екатеринбург',.5],['Казань',.46],
          ['Нижний Новгород',.42],['Краснодар',.4],['Самара',.38],['Ростов-на-Дону',.37],['Челябинск',.35],['Уфа',.33],['Красноярск',.3]],
  'СНГ': [['Минск',.72],['Ташкент',.8],['Алматы',.6],['Баку',.5],['Астана',.35],['Ереван',.32],['Тбилиси',.28],['Бишкек',.25],['Кишинёв',.2]]
};

/* стоп-слова ИИ-модератора: наркотики / казино / оружие / скам / пирамиды + синонимы */
const ADS_STOPWORDS = [
  'наркот','героин','кокаин','марихуан','мефедрон','спайс','закладк',
  'казино','букмекер','ставки на спорт','азартн','гэмблинг','слоты на деньги',
  'оружи','пистолет','боеприпас','гранат',
  'скам','лохотрон','развод на деньги','фальшив','подделк',
  'пирамид','быстрый заработок без вложений','гарантия дохода','хайп-проект'
];

let ADS = {camps: []}  /* демо-кампании удалены 09.08 */;

/* медиа сериализуем аккуратно: картинки — сжатые data-URL (влезают в localStorage),
   видео / слишком большие картинки — только дескриптор {type,name} (blob-URL после
   перезагрузки невалиден), чтобы не переполнить квоту и не хранить мусор. */
function adsMediaStore(m){
  if(!m) return null;
  if(m.type==='image' && typeof m.src==='string' && m.src.length < 380000)
    return {type:'image', src:m.src, name:m.name||'', w:m.w||0, h:m.h||0};
  return {type:m.type||'image', name:m.name||''};
}
function adsSave(){
  try{
    const out = {
      seq: ADS.seq, audSeq: ADS.audSeq,
      bill: ADS.bill, auds: ADS.auds,
      camps: ADS.camps.map(c=>Object.assign({}, c, {media: adsMediaStore(c.media)}))
    };
    localStorage.setItem(ADS_LS, JSON.stringify(out));
  }catch(e){}
}
function adsLoad(){
  try{
    const raw = localStorage.getItem(ADS_LS);
    if(raw){ const d = JSON.parse(raw); if(d && Array.isArray(d.camps)) ADS = Object.assign(ADS, d); }
  }catch(e){}
}

/* эффективный CPM кампании (для CPC берём из связки ставка/CTR) */
function adsCampCpm(c){
  if(c.model==='CPC'){ const ctr = c.ctrEst || .022; return (c.bid||9)*ctr*1000; }
  return c.bid || (ADS_FORMATS[c.fmt]?.cpm) || 28;
}

/* ---------- дни: аналитика по дням (7 суток, [{i,c}] где i=показы c=клики) ---------- */
function adsDayKey(){ return Math.floor(Date.now()/864e5); }
function adsSeedDays(c){
  const w = c.status==='done' ? [.28,.26,.24,.22,0,0,0] : [0,0,.06,.12,.2,.28,.34];
  let li = 0; w.forEach((k,i)=>{ if(k>0) li = i; });
  c.days = w.map(k=>({i:Math.round(c.imps*k), c:Math.round(c.clicks*k)}));
  let ri = c.imps, rc = c.clicks;
  c.days.forEach((d,i)=>{ if(i!==li){ ri-=d.i; rc-=d.c; } });
  c.days[li] = {i:Math.max(0,ri), c:Math.max(0,rc)};
}
function adsRollDays(c){
  const today = adsDayKey();
  let diff = today - (c.dstamp || today);
  if(diff>0){
    if(diff>7) diff = 7;
    for(let i=0;i<diff;i++){ c.days.shift(); c.days.push({i:0,c:0}); }
  }
  c.dstamp = today;
}
function adsMigrate(){
  if(!Array.isArray(ADS.bill)) ADS.bill = [];
  if(!Array.isArray(ADS.auds)) ADS.auds = [];
  ADS.camps.forEach(c=>{
    if(!c.fmt) c.fmt = 'post';
    if(!c.goal) c.goal = ({post:'views', channel:'subs', listing:'sales', banner:'installs'})[c.fmt] || 'views';
    if(!c.model) c.model = ADS_FORMATS[c.fmt]?.model || 'CPM';
    if(!c.bid) c.bid = ADS_FORMATS[c.fmt]?.[c.model==='CPC'?'cpc':'cpm'] || 28;
    if(!c.sex) c.sex = 'any';
    if(!Array.isArray(c.cities)) c.cities = [];
    if(!Array.isArray(c.devs)) c.devs = [];
    if(!Array.isArray(c.times)) c.times = [];
    if(!Array.isArray(c.weekdays)) c.weekdays = [1,2,3,4,5,6,0];   /* пн-вс, 0=вс */
    if(c.hourFrom==null) c.hourFrom = 0;
    if(c.hourTo==null) c.hourTo = 24;
    if(c.ageMin==null){
      /* мигрируем со старых age-чипов, если есть */
      const ages = Array.isArray(c.ages) ? c.ages : [];
      const map = {'18-24':[18,24], '25-34':[25,34], '35-44':[35,44], '45+':[45,65]};
      let mn=65, mx=18;
      ages.forEach(a=>{ const p=map[a]; if(p){ mn=Math.min(mn,p[0]); mx=Math.max(mx,p[1]); } });
      c.ageMin = mn<mx ? mn : 18; c.ageMax = mn<mx ? mx : 45;
    }
    if(c.ageMax==null) c.ageMax = Math.max(c.ageMin+5, 45);
    if(!Array.isArray(c.ages)) c.ages = [];   /* legacy — оставляем для обратной совместимости */
    if(c.lal==null) c.lal = false;
    if(!Array.isArray(c.days) || c.days.length!==7) adsSeedDays(c);
    if(c.disc==null) c.disc = 0;                 /* скидка тарифа, зафиксированная при запуске */
    if(c.pace==null) c.pace = 0;                 /* дней равномерной открутки (0 = максимум) */
    if(c.dailyCap==null) c.dailyCap = 0;         /* дневной лимит бюджета, ₽ (0 = без лимита) */
    if(c.spentToday==null) c.spentToday = 0;     /* потрачено сегодня (для дневного лимита) */
    if(c.spentDay==null) c.spentDay = adsDayKey();
    if(!c.cvr) c.cvr = +(0.16 + Math.random()*0.18).toFixed(3);   /* доля кликов → целевое действие */
    if(c.aov==null) c.aov = (ADS_FORMATS[c.fmt] && ADS_FORMATS[c.fmt].aov) || 690;   /* средний чек продажи, ₽ */
    if(c.salesRate==null) c.salesRate = +(0.006 + Math.random()*0.007).toFixed(4);   /* доля кликов → оплаченная продажа */
    if(c.ab){ c.ab.a = c.ab.a || {i:0,c:0}; c.ab.b = c.ab.b || {i:0,c:0}; }
    if(c.demo && !c.media) c.media = adsBrandPoster(c);
    adsRollDays(c);
  });
}

/* ---------- метрики / рендер ---------- */
function adsCtr(c){ return c.imps ? (c.clicks / c.imps * 100) : 0; }
/* класс-подсказка заполнения бюджета: >=90% — критично, >=75% — на исходе */
function adsBudgetCls(c){ const r = c.budget ? c.spent/c.budget : 0; return r>=0.9 ? 'crit' : r>=0.75 ? 'warn' : ''; }
/* короткая подсветка числа при живом обновлении (ощущение «метрики дышат») */
function adsFlashEl(el){ if(!el) return; el.classList.remove('tick'); void el.offsetWidth; el.classList.add('tick'); }
function adsCpcTxt(c){
  const cpc = c.clicks ? c.spent/c.clicks : 0;
  return cpc ? (cpc<10 ? cpc.toFixed(2) : fmtN(Math.round(cpc)))+' ₽' : '—';
}

/* сводка: живые анимированные счётчики */
const ADS_SUM = [
  {k:'sp',  l:'потрачено', unit:'₽', fmt:v=>fmtN(Math.round(v))},
  {k:'im',  l:'показы',              fmt:v=>fmtN(Math.round(v))},
  {k:'cl',  l:'клики',               fmt:v=>fmtN(Math.round(v))},
  {k:'ctr', l:'ctr',       unit:'%', fmt:v=>v.toFixed(1)}
];
const adsSumState = {};
function adsSumTotals(){
  const t = ADS.camps.reduce((a,c)=>({sp:a.sp+c.spent, im:a.im+c.imps, cl:a.cl+c.clicks}), {sp:0,im:0,cl:0});
  t.ctr = t.im ? (t.cl/t.im*100) : 0;
  return t;
}
function adsCountTo(s, target){
  const el = document.querySelector(`#adsSummary .v[data-sk="${s.k}"]`); if(!el) return;
  const st = adsSumState[s.k] || (adsSumState[s.k] = {cur:0, raf:0});
  cancelAnimationFrame(st.raf);
  const unit = s.unit ? ` <small>${s.unit}</small>` : '';
  const paint = v => { el.innerHTML = s.fmt(v)+unit; };
  if(Math.abs(target-st.cur) < 1e-4){ st.cur = target; paint(target); return; }
  const from = st.cur, t0 = performance.now(), dur = 640;
  const step = now=>{
    const k = Math.min(1, (now-t0)/dur), e = 1-Math.pow(1-k,3);
    st.cur = from + (target-from)*e; paint(st.cur);
    if(k<1) st.raf = requestAnimationFrame(step); else { st.cur = target; paint(target); }
  };
  st.raf = requestAnimationFrame(step);
}
function adsSummaryReset(){ Object.values(adsSumState).forEach(s=>{ cancelAnimationFrame(s.raf); s.cur = 0; }); }
function adsRenderSummary(){
  const box = document.getElementById('adsSummary'); if(!box) return;
  if(box.children.length !== ADS_SUM.length){
    box.innerHTML = ADS_SUM.map(s=>
      `<div class="stat"><div class="v" data-sk="${s.k}">0${s.unit?` <small>${s.unit}</small>`:''}</div><div class="l">${s.l}</div></div>`
    ).join('');
  }
  const t = adsSumTotals();
  ADS_SUM.forEach(s=>adsCountTo(s, t[s.k]));
  const qs = document.getElementById('adsQuickSpent'); if(qs) qs.textContent = fmtN(Math.round(t.sp))+' ₽';
  const qa = document.getElementById('adsQuickAud'); if(qa) qa.textContent = ADS.auds.length;
}

/* вторичный ряд: Изменить + Дублировать (двухколоночная сетка, не переполняет 390px) */
function adsEditDup(c){
  const hasData = c.imps>0 || c.spent>0 || c.status==='done';
  const csvRow = hasData ? `<div class="ads-camp-acts2"><button class="btn sm ghost dup csv" onclick="adsExportCsv(${c.id})">${I('poll')}Скачать CSV по дням</button></div>` : '';
  return `<div class="ads-camp-acts2 two">
      <button class="btn sm ghost dup" onclick="adsEdit(${c.id})">${I('edit')}Изменить</button>
      <button class="btn sm ghost dup" onclick="adsDuplicate(${c.id})">${I('copy')}Дублировать</button></div>${csvRow}`;
}
function adsCampActs(c){
  if(c.status==='mod')  return `<div class="ads-modwait">${I('clock')}Модератор OKO проверяет объявление…</div>`;
  if(c.status==='sched')return `<div class="ads-camp-acts">
      <button class="btn sm" onclick="adsStartNow(${c.id})">${I('rocket')}Запустить сейчас</button>
      <button class="btn sm ghost stop" onclick="adsCancelSched(${c.id})">${I('flag')}Отменить</button></div>`+adsEditDup(c);
  if(c.status==='act')  return `<div class="ads-camp-acts">
      <button class="btn sm" onclick="adsTopup(${c.id})">${I('plus')}Пополнить</button>
      <button class="btn sm ghost" onclick="adsPause(${c.id})">${I('pause')}Пауза</button>
      <button class="btn sm ghost stop" onclick="adsStop(${c.id})">${I('flag')}Стоп</button></div>`+adsEditDup(c);
  if(c.status==='pause')return `<div class="ads-camp-acts">
      <button class="btn sm" onclick="adsResume(${c.id})">${I('play')}Возобновить</button>
      <button class="btn sm ghost" onclick="adsTopup(${c.id})">${I('plus')}Бюджет</button>
      <button class="btn sm ghost stop" onclick="adsStop(${c.id})">${I('flag')}Стоп</button></div>`+adsEditDup(c);
  return `<div class="ads-camp-acts">
      <button class="btn sm ghost" onclick="adsEdit(${c.id})">${I('edit')}Изменить</button>
      <button class="btn sm ghost" onclick="adsDuplicate(${c.id})">${I('copy')}Дублировать</button>
      <button class="btn sm ghost stop" onclick="adsDelete(${c.id})">${I('trash')}Удалить</button></div>
      <div class="ads-camp-acts2"><button class="btn sm ghost dup csv" onclick="adsExportCsv(${c.id})">${I('poll')}Скачать CSV по дням</button></div>`;
}

function adsFmtBadge(c){
  const f = ADS_FORMATS[c.fmt] || ADS_FORMATS.post;
  let out = `<span class="ads-fmt-badge">${I(f.ico)}${esc(f.l)}</span><span class="ads-model-badge">${c.model} · ${fmtN(c.bid)} ₽</span>`;
  if(c.disc>0) out += `<span class="ads-disc-badge">${I('fire')}−${Math.round(c.disc*100)}%</span>`;
  if(c.dailyCap>0) out += `<span class="ads-model-badge">${I('clock')}${fmtN(c.dailyCap)} ₽/день</span>`;
  return out;
}
/* дата/время старта запланированной кампании + сколько осталось */
function adsSchedLine(c){
  const at = c.startTs || Date.now();
  const d = new Date(at);
  const dt = d.toLocaleDateString('ru-RU',{day:'2-digit',month:'short'})+' '+d.toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'});
  const left = at - Date.now();
  let rel = 'скоро';
  if(left > 0){
    const h = Math.floor(left/36e5), m = Math.round(left%36e5/6e4);
    rel = h>=24 ? 'через '+Math.round(h/24)+' дн.' : h>=1 ? 'через '+h+' ч '+m+' мин' : 'через '+Math.max(1,m)+' мин';
  }
  return `<div class="ads-sched-line">${I('clock')}<span>Старт <b>${dt}</b> · ${rel}</span></div>`;
}

function adsRenderList(){
  const box = document.getElementById('adsList'); if(!box) return;
  if(!ADS.camps.length){
    box.innerHTML = `<div class="card ads-empty">${I('megaphone')}Ни одной кампании. Запусти первую — и уже сегодня объявление увидят тысячи людей в ленте OKO</div>`;
    return;
  }
  box.innerHTML = ADS.camps.map((c,i)=>`
    <div class="card ads-camp" data-cid="${c.id}" style="animation-delay:${i*.05}s">
      <div class="ads-camp-top">
        <div class="ads-camp-name">${esc(c.name)}</div>
        <span class="ads-st ${c.status}">${ADS_STATUS[c.status].l}</span>
      </div>
      <div class="ads-camp-tags">${adsFmtBadge(c)}</div>
      ${c.status==='sched' ? adsSchedLine(c) : ''}
      <div class="ads-camp-txt">${esc(c.text)}</div>
      <div class="ads-metrics">
        <div class="ads-m"><b data-m="imps">${fmtN(c.imps)}</b><span>показы</span></div>
        <div class="ads-m"><b data-m="clicks">${fmtN(c.clicks)}</b><span>клики</span></div>
        <div class="ads-m hot"><b data-m="ctr">${adsCtr(c).toFixed(1)}%</b><span>ctr</span></div>
        <div class="ads-m"><b data-m="spent">${fmtN(Math.round(c.spent))}<i class="ads-cur">₽</i></b><span>из ${fmtN(c.budget)}<i class="ads-cur">₽</i></span></div>
      </div>
      <div class="ads-budget-bar"><i data-m="bar" class="${adsBudgetCls(c)}" style="width:${Math.min(100, c.spent/c.budget*100)}%"></i></div>
      <canvas class="ads-spark" id="adsSpark${c.id}"></canvas>
      <button class="ads-stats-toggle ${adsOpenStats.has(c.id)?'open':''}" onclick="adsToggleStats(${c.id})">
        ${I('poll')}Статистика${c.ab?`<em class="ads-abmark">A/B</em>`:''}<i>${I('chev')}</i></button>
      ${adsOpenStats.has(c.id) ? adsStatsBlock(c) : ''}
      ${c.status==='rej' && c.reason ? `<div class="ads-reason">${I('flag')}<span>Отклонено модератором OKO: ${esc(c.reason)}. Бюджет возвращён на лицевой счёт.</span></div>` : ''}
      ${adsCampActs(c)}
    </div>`).join('');
  requestAnimationFrame(()=>ADS.camps.forEach(c=>{
    adsDrawSpark(c);
    if(adsOpenStats.has(c.id)){ adsDrawDays(c); adsSyncFunnel(c, true); }
  }));
}

/* ---------- разворот «Статистика» ---------- */
const adsOpenStats = new Set();
function adsToggleStats(id){
  if(adsOpenStats.has(id)) adsOpenStats.delete(id); else adsOpenStats.add(id);
  adsRenderList();
}
function adsSub7(c){
  const d7 = c.days || [];
  const ti = d7.reduce((s,d)=>s+d.i,0), tc = d7.reduce((s,d)=>s+d.c,0);
  return `за 7 дней: <b>${fmtN(ti)}</b> показов · <b>${fmtN(tc)}</b> кликов · CPC <b>${adsCpcTxt(c)}</b>`;
}
function adsAudLine(c){
  const parts = [];
  parts.push(c.sex==='m'?'муж':c.sex==='f'?'жен':'любой пол');
  parts.push(c.geo);
  if(c.cities && c.cities.length) parts.push(c.cities.slice(0,2).join(', ')+(c.cities.length>2?' +'+(c.cities.length-2):''));
  if(c.ageMin!=null && c.ageMax!=null){
    const mx = c.ageMax>=65 ? '65+' : c.ageMax;
    parts.push(c.ageMin+'–'+mx);
  } else if(c.ages && c.ages.length) parts.push(c.ages.join('/'));
  if(c.devs && c.devs.length) parts.push(c.devs.map(d=>({ios:'iOS',android:'Android',desktop:'ПК'}[d]||d)).join('/'));
  if(c.lal) parts.push('look-alike');
  return parts.join(' · ');
}
function adsStatsBlock(c){
  return `<div class="ads-stats">
    ${adsAdviceBlock(c)}
    <div class="ads-aud-line">${I('users')}${esc(adsAudLine(c))}</div>
    <canvas class="ads-days" id="adsDays${c.id}"></canvas>
    <div class="ads-legend">
      <span><i class="lg-imp"></i>показы</span>
      <span><i class="lg-clk"></i>клики</span>
      <span><i class="lg-ctr"></i>CTR</span>
    </div>
    <div class="ads-stats-sub" data-m="sub7">${adsSub7(c)}</div>
    ${adsFunnel(c)}
    ${adsRoasStrip(c)}
    ${adsBreakdown(c)}
    ${c.ab ? `<div class="ads-ab" data-m="ab">${adsAbInner(c)}</div>` : ''}
  </div>`;
}

/* ---------- разбивка показов по устройствам / полу / гео ---------- */
/* распределения — фиксированные хэш-производные от id кампании: одна и та же кампания
   всегда даёт одну и ту же разбивку, но у разных кампаний она разная (реалистично) */
function adsHash(id, salt){
  let h = 2166136261 ^ (id*97) ^ (salt*131);
  h = Math.imul(h ^ (h>>>15), 2246822507);
  h = Math.imul(h ^ (h>>>13), 3266489909);
  return ((h ^ (h>>>16))>>>0) / 4294967295;
}
function adsBreakdownData(c){
  const total = c.imps || 0;
  /* устройства: если выбраны — только они, иначе все три с базовыми долями */
  const dfSel = (c.devs && c.devs.length) ? c.devs : ['ios','android','desktop'];
  const dBase = {ios:.34, android:.53, desktop:.13};
  let ds = dfSel.map(k=>({k, l:({ios:'iOS',android:'Android',desktop:'ПК'}[k]||k), w: dBase[k] * (0.85+adsHash(c.id, dfSel.indexOf(k)+7)*.3)}));
  const dSum = ds.reduce((s,x)=>s+x.w,0)||1; ds.forEach(x=>x.pct = x.w/dSum);
  /* пол: если выбран конкретный — 100%, иначе балансируется хэшем ~48-52 */
  let sx;
  if(c.sex==='m') sx = [{k:'m', l:'Мужчины', pct:1}];
  else if(c.sex==='f') sx = [{k:'f', l:'Женщины', pct:1}];
  else { const m = 0.44 + adsHash(c.id, 21)*0.14; sx = [{k:'m',l:'Мужчины',pct:m},{k:'f',l:'Женщины',pct:1-m}]; }
  /* гео: по выбранным городам, либо по гео-региону с топ-3 */
  let gs = [];
  if(c.cities && c.cities.length){
    const cw = c.cities.map((n,i)=>({l:n, w: 1/(i+1) * (0.85+adsHash(c.id, i+41)*.3)}));
    const s = cw.reduce((a,x)=>a+x.w,0)||1; cw.forEach(x=>x.pct = x.w/s);
    gs = cw.slice(0,3);
  }else{
    const list = ADS_CITIES[c.geo] || ADS_CITIES['РФ'];
    const top = list.slice(0,3).map(([n,r],i)=>({l:n, w:r*(0.9+adsHash(c.id, i+13)*.2)}));
    const rest = list.slice(3).reduce((s,[,r])=>s+r,0);
    const s = top.reduce((a,x)=>a+x.w,0) + rest || 1;
    gs = top.map(x=>({l:x.l, pct:x.w/s}));
    gs.push({l:'другие', pct: rest/s});
  }
  return {ds, sx, gs, total};
}
function adsBreakdown(c){
  if(!c.imps){
    return `<div class="ads-brk"><div class="ads-brk-h">${I('poll')}Разбивка аудитории</div>
      <div class="ads-brk-empty">Появится после первых показов.</div></div>`;
  }
  const d = adsBreakdownData(c);
  const row = (l, pct, val, tone)=>`<div class="ads-brk-row">
    <span class="ads-brk-l">${esc(l)}</span>
    <div class="ads-brk-bar"><i style="width:${Math.max(3, pct*100)}%" class="${tone||''}"></i></div>
    <span class="ads-brk-v">${(pct*100).toFixed(0)}%<em>${fmtN(Math.round((val||d.total)*pct))}</em></span>
  </div>`;
  return `<div class="ads-brk">
    <div class="ads-brk-h">${I('poll')}Разбивка показов</div>
    <div class="ads-brk-sec">
      <div class="ads-brk-t">Устройства</div>
      ${d.ds.map(x=>row(x.l, x.pct)).join('')}
    </div>
    <div class="ads-brk-sec">
      <div class="ads-brk-t">Пол</div>
      ${d.sx.map(x=>row(x.l, x.pct, null, x.k)).join('')}
    </div>
    <div class="ads-brk-sec">
      <div class="ads-brk-t">География</div>
      ${d.gs.map(x=>row(x.l, x.pct)).join('')}
    </div>
  </div>`;
}

function adsAbWinner(c){
  if(!c.ab) return null;
  const a = c.ab.a, b = c.ab.b;
  if(a.i < 400 || b.i < 400) return null;
  const ca = a.c/a.i, cb = b.c/b.i;
  if(Math.abs(ca-cb) < Math.min(ca,cb)*.1) return null;
  return ca > cb ? 'a' : 'b';
}
function adsAbInner(c){
  const win = adsAbWinner(c);
  const row = (k, title)=>{
    const v = c.ab[k], o = c.ab[k==='a'?'b':'a'];
    const ctr = v.i ? v.c/v.i*100 : 0, octr = o.i ? o.c/o.i*100 : 0;
    const w = Math.max(ctr, octr) ? ctr/Math.max(ctr, octr)*100 : 0;
    return `<div class="ads-ab-row ${win===k?'win':''}">
      <span class="ads-ab-tag">${k.toUpperCase()}</span>
      <div class="ads-ab-mid">
        <div class="ads-ab-t">${esc(title)}</div>
        <div class="ads-ab-bar"><i style="width:${w}%"></i></div>
      </div>
      <div class="ads-ab-num"><b>${ctr.toFixed(2)}%</b><span>${fmtN(v.i)} пок.</span></div>
      ${win===k ? `<span class="ads-ab-win" title="лидер">${I('crown')}</span>` : ''}
    </div>`;
  };
  return `<div class="ads-ab-h">${I('bolt')}A/B тест заголовка
      ${win ? `<span class="ads-ab-done">победитель найден</span>` : `<span class="ads-ab-run">идёт тест</span>`}</div>
    ${row('a', c.name)}${row('b', c.ab.tb)}`;
}

/* ---------- воронка конверсии: Показы → Клики → Целевые действия ---------- */
function adsConv(c){ return Math.round((c.clicks||0) * (c.cvr || 0.22)); }
const ADS_FN_STAGES = [
  {k:'imps', ic:'eye',  l:'Показы',           col:'#9AFF00'},
  {k:'clk',  ic:'bolt', l:'Клики',            col:'#ffb020'},
  {k:'cv',   ic:'check',l:'Целевые действия', col:'#4da6ff'}
];
function adsFunnel(c){
  let html = `<div class="ads-fn"><div class="ads-fn-h">${I('poll')}Воронка конверсии</div>`;
  ADS_FN_STAGES.forEach((s,i)=>{
    if(i) html += `<div class="ads-fn-drop"><span class="ads-fn-arw">${I('chev')}</span>`+
      `<b data-dr="${i===1?'ctr':'cr'}">0%</b><span>${i===1?'CTR':'в действие'}</span></div>`;
    html += `<div class="ads-fn-row">
      <span class="ads-fn-ic" style="color:${s.col};background:${s.col}1f;border-color:${s.col}55">${I(s.ic)}</span>
      <div class="ads-fn-mid">
        <div class="ads-fn-top"><span class="ads-fn-l">${s.l}</span><b class="ads-fn-v" data-fn="${s.k}">0</b></div>
        <div class="ads-fn-bar"><i data-fn2="${s.k}" style="width:0;background:linear-gradient(90deg,${s.col}3a,${s.col})"></i></div>
      </div></div>`;
  });
  return html + '</div>';
}
/* автономный count-up для одного элемента */
function adsCountEl(el, target){
  const t0 = performance.now(), dur = 700;
  const step = now=>{
    const k = Math.min(1, (now-t0)/dur), e = 1-Math.pow(1-k,3);
    el.textContent = fmtN(Math.round(target*e));
    if(k<1) requestAnimationFrame(step); else el.textContent = fmtN(target);
  };
  requestAnimationFrame(step);
}
function adsSyncFunnel(c, animate){
  const wrap = document.querySelector(`.ads-camp[data-cid="${c.id}"] .ads-fn`); if(!wrap) return;
  const imps = c.imps||0, clk = c.clicks||0, cv = adsConv(c), base = Math.max(imps,1);
  const vals = {imps, clk, cv};
  wrap.querySelectorAll('.ads-fn-v').forEach(el=>{
    const to = vals[el.dataset.fn] || 0;
    if(animate) adsCountEl(el, to); else el.textContent = fmtN(to);
  });
  wrap.querySelectorAll('.ads-fn-bar i').forEach(el=>{
    const frac = Math.max(0, Math.min(1, (vals[el.dataset.fn2]||0)/base));
    el.style.width = Math.max(6, Math.pow(frac, 0.34)*100) + '%';
  });
  wrap.querySelectorAll('.ads-fn-drop b').forEach(el=>{
    if(el.dataset.dr==='ctr') el.textContent = (imps ? clk/imps*100 : 0).toFixed(1)+'%';
    else el.textContent = (clk ? cv/clk*100 : 0).toFixed(0)+'%';
  });
}

/* ---------- продажи / окупаемость (ROAS): деньги, которые приносит реклама ---------- */
function adsAov(c){ return c.aov || (ADS_FORMATS[c.fmt] && ADS_FORMATS[c.fmt].aov) || 690; }
function adsSalesRate(c){ return c.salesRate || 0.009; }
function adsSales(c){ return Math.round((c.clicks||0) * adsSalesRate(c)); }
function adsRevenue(c){ return adsSales(c) * adsAov(c); }
function adsRoas(c){ return c.spent>0 ? adsRevenue(c)/c.spent : 0; }
function adsRoasTxt(r){ return r ? '×'+(r>=10 ? Math.round(r) : r.toFixed(1)) : '×0'; }
function adsRoasCls(c){ const r = adsRoas(c); return r>=2 ? 'good' : (r>0 && r<1 && adsSales(c)>0 ? 'low' : ''); }
function adsRoasStrip(c){
  const sales = adsSales(c), rev = adsRevenue(c), roas = adsRoas(c);
  const w = Math.max(4, Math.min(100, roas/4*100));   /* шкала 0..×4+, отметка безубыточности на ×1 */
  return `<div class="ads-roas ${adsRoasCls(c)}">
    <div class="ads-roas-h">${I('money')}Продажи и окупаемость<span class="ads-roas-aov">средний чек ${fmtN(adsAov(c))} ₽</span></div>
    <div class="ads-roas-grid">
      <div class="ads-roas-c"><b data-r="sales">${fmtN(sales)}</b><span>продажи</span></div>
      <div class="ads-roas-c"><b data-r="rev">${fmtN(rev)}<i class="ads-cur">₽</i></b><span>выручка</span></div>
      <div class="ads-roas-c roas"><b data-r="roas">${adsRoasTxt(roas)}</b><span>окупаемость</span></div>
    </div>
    <div class="ads-roas-bar"><i data-r="bar" style="width:${w}%"></i><em class="ads-roas-be"></em></div>
    <div class="ads-roas-scale"><span>0</span><span>×1 окуп.</span><span>×4+</span></div>
  </div>`;
}
function adsSyncRoas(c){
  const rw = document.querySelector(`.ads-camp[data-cid="${c.id}"] .ads-roas`); if(!rw) return;
  const sales = adsSales(c), rev = adsRevenue(c), roas = adsRoas(c);
  const set = (k,v)=>{ const n = rw.querySelector(`[data-r="${k}"]`); if(n) n.innerHTML = v; };
  set('sales', fmtN(sales)); set('rev', fmtN(rev)+'<i class="ads-cur">₽</i>'); set('roas', adsRoasTxt(roas));
  const bar = rw.querySelector('[data-r="bar"]'); if(bar) bar.style.width = Math.max(4, Math.min(100, roas/4*100))+'%';
  rw.className = 'ads-roas '+adsRoasCls(c);
}

/* ---------- ИИ-оптимизатор: разбирает метрики кампании и даёт действия в один тап ---------- */
function adsAdvice(c){
  if(c.status==='mod' || c.status==='sched' || c.status==='rej') return [];
  const out = [], ctr = adsCtr(c), roas = adsRoas(c), bpct = c.budget ? c.spent/c.budget*100 : 0;
  const win = adsAbWinner(c), noMedia = !c.media || !c.media.src;
  if(win) out.push({tone:'good', ic:'crown', p:2,
    text:`Победил вариант <b>${win.toUpperCase()}</b> A/B-теста. Оставьте только его — весь показ на лидере.`,
    label:'Оставить '+win.toUpperCase(), act:`adsApplyAbWinner(${c.id})`});
  if(roas>=2 && c.status==='act') out.push({tone:'good', ic:'fire', p:1,
    text:`Окупаемость <b>${adsRoasTxt(roas)}</b> — реклама в плюсе. Масштабируйте бюджет, пока держится.`,
    label:'Масштабировать', act:`adsTopup(${c.id})`});
  if(noMedia) out.push({tone:'warn', ic:'photo', p:1,
    text:'Без креатива CTR ниже в 2–3 раза. Добавьте фото или видео к объявлению.',
    label:'Добавить креатив', act:`adsEdit(${c.id})`});
  if(c.imps>2000 && ctr<1) out.push({tone:'warn', ic:'bolt', p:0,
    text:`Низкий CTR <b>${ctr.toFixed(1)}%</b> — обновите заголовок или включите A/B-тест.`,
    label:'Изменить', act:`adsEdit(${c.id})`});
  if(ctr>=3 && bpct>=80 && c.status==='act') out.push({tone:'good', ic:'eye', p:0,
    text:`Сильный CTR <b>${ctr.toFixed(1)}%</b>, а бюджет почти израсходован. Пополните, пока идёт открутка.`,
    label:'Пополнить', act:`adsTopup(${c.id})`});
  if(roas>0 && roas<1 && adsSales(c)>0) out.push({tone:'warn', ic:'poll', p:0,
    text:`Окупаемость ниже <b>×1</b> — расход выше выручки. Снизьте ставку или уточните аудиторию.`,
    label:'Изменить', act:`adsEdit(${c.id})`});
  return out.sort((a,b)=>b.p-a.p).slice(0,3);
}
function adsAdviceBlock(c){
  const list = adsAdvice(c);
  if(!list.length) return `<div class="ads-advice"><div class="ads-advice-h">${I('bolt')}Оптимизатор кампаний</div>
    <div class="ads-advice-empty">${I('check')}Кампания настроена оптимально — критичных улучшений нет.</div></div>`;
  return `<div class="ads-advice"><div class="ads-advice-h">${I('bolt')}Оптимизатор кампаний<span class="ads-advice-n">${list.length}</span></div>`+
    list.map(a=>`<div class="ads-advice-row ${a.tone}">
      <span class="ads-advice-ic">${I(a.ic)}</span>
      <div class="ads-advice-mid"><p>${a.text}</p>
        ${a.act ? `<button class="ads-advice-btn" onclick="event.stopPropagation();${a.act}">${a.label}${I('chev')}</button>` : ''}</div>
    </div>`).join('')+`</div>`;
}
function adsApplyAbWinner(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c || !c.ab) return;
  const win = adsAbWinner(c);
  if(!win){ toast('Победитель ещё не определён — тесту нужно больше показов'); return; }
  if(win==='b') c.name = c.ab.tb;
  c.ab = null;
  adsSyncFeedPost(c);
  adsSave(); adsRender();
  toast('Оставили победивший вариант — весь показ теперь на нём');
}

function adsRRect(x, px, py, pw, ph, r){
  if(ph < .5) return;
  r = Math.min(r, pw/2, ph);
  x.beginPath();
  x.moveTo(px, py+ph); x.lineTo(px, py+r);
  x.arcTo(px, py, px+r, py, r); x.lineTo(px+pw-r, py);
  x.arcTo(px+pw, py, px+pw, py+r, r); x.lineTo(px+pw, py+ph);
  x.closePath(); x.fill();
}
function adsDrawDays(c){
  const cv = document.getElementById('adsDays'+c.id); if(!cv) return;
  const dpr = window.devicePixelRatio || 1;
  const w = cv.clientWidth || 300, h = cv.clientHeight || 132;
  cv.width = w*dpr; cv.height = h*dpr;
  const x = cv.getContext('2d'); x.scale(dpr,dpr); x.clearRect(0,0,w,h);
  const days = (c.days && c.days.length===7) ? c.days : [...Array(7)].map(()=>({i:0,c:0}));
  const light = document.documentElement.getAttribute('data-theme')==='light';
  const txt   = light ? 'rgba(20,24,12,.55)' : 'rgba(255,255,255,.45)';
  const ctrCol= light ? '#1a1d16' : '#ffffff';
  const padT = 8, padB = 16, padS = 6;
  const ih = h-padT-padB, iw = (w-padS*2)/7;
  const maxI = Math.max(...days.map(d=>d.i), 1);
  const maxC = Math.max(...days.map(d=>d.c), 1);
  const names = ['вс','пн','вт','ср','чт','пт','сб'];
  const now = new Date();
  x.strokeStyle = light ? 'rgba(0,0,0,.08)' : 'rgba(255,255,255,.07)';
  x.lineWidth = 1; x.beginPath(); x.moveTo(2, h-padB+.5); x.lineTo(w-2, h-padB+.5); x.stroke();
  days.forEach((d,i)=>{
    const cx = padS + i*iw + iw/2;
    const bw = Math.max(4, Math.min(11, iw*.22));
    x.fillStyle = 'rgba(154,255,0,.85)';
    adsRRect(x, cx-bw-1.5, h-padB - d.i/maxI*ih, bw, d.i/maxI*ih, 2);
    x.fillStyle = 'rgba(255,176,32,.85)';
    adsRRect(x, cx+1.5, h-padB - d.c/maxC*ih, bw, d.c/maxC*ih, 2);
    const dt = new Date(now.getTime() - (6-i)*864e5);
    x.fillStyle = i===6 ? '#9AFF00' : txt;
    x.font = '600 8.5px Montserrat, sans-serif'; x.textAlign = 'center';
    x.fillText(i===6 ? 'сегодня' : names[dt.getDay()], cx, h-4);
  });
  const ctrs = days.map(d=>d.i ? d.c/d.i*100 : 0);
  const maxCtr = Math.max(...ctrs, .01);
  const pY = v => h-padB-4 - v/maxCtr*(ih-14);
  x.globalAlpha = .8; x.strokeStyle = ctrCol; x.lineWidth = 1.4; x.setLineDash([4,3]);
  x.beginPath();
  ctrs.forEach((v,i)=>{ const cx = padS+i*iw+iw/2; i ? x.lineTo(cx, pY(v)) : x.moveTo(cx, pY(v)); });
  x.stroke(); x.setLineDash([]);
  ctrs.forEach((v,i)=>{ const cx = padS+i*iw+iw/2;
    x.beginPath(); x.arc(cx, pY(v), 2, 0, Math.PI*2); x.fillStyle = ctrCol; x.fill(); });
  x.globalAlpha = 1;
}

/* ---------- пополнение бюджета кампании ---------- */
function adsTopup(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c) return;
  showPopup({ico:'card', title:'Пополнить бюджет',
    body:`«${esc(c.name)}»: потрачено <b>${fmtMoney(c.spent)}</b> из <b>${fmtMoney(c.budget)}</b>.<br>
      Сумма спишется с лицевого счёта (${fmtMoney(WALLET.balance)}).
      <div class="ads-topup">
        <button class="ads-chip" onclick="adsTopupDo(${c.id},500)">+500 ₽</button>
        <button class="ads-chip" onclick="adsTopupDo(${c.id},1000)">+1 000 ₽</button>
      </div>`,
    actions:[{label:'Отмена', ghost:true}]});
}
function adsTopupDo(id, sum){
  const c = ADS.camps.find(x=>x.id===id); if(!c) return;
  closePopup();
  if(!walletCharge(sum, 'Пополнение кампании: '+c.name)) return;
  okoEarn(sum, 'Рекламный кабинет');
  adsBill(sum, c.name, 'Пополнение бюджета');
  c.budget += sum;
  if(c.status==='done' && c.spent < c.budget){ c.status = 'act'; adsPushToFeed(c); }
  adsSave(); adsRender();
  toast('Бюджет кампании +'+fmtN(sum)+' ₽ — открутка продолжается');
}

function adsDrawSpark(c){
  const cv = document.getElementById('adsSpark'+c.id); if(!cv) return;
  const dpr = window.devicePixelRatio || 1;
  const w = cv.clientWidth || 300, h = cv.clientHeight || 44;
  cv.width = w*dpr; cv.height = h*dpr;
  const x = cv.getContext('2d'); x.scale(dpr,dpr); x.clearRect(0,0,w,h);
  const d = (c.hist && c.hist.length>1) ? c.hist : [0,0];
  const max = Math.max(...d, 1), min = Math.min(...d);
  const px = i => 2 + i*(w-4)/(d.length-1);
  const py = v => h-4 - (v-min)/(max-min || 1)*(h-10);
  const grad = x.createLinearGradient(0,0,0,h);
  grad.addColorStop(0,'rgba(154,255,0,.28)'); grad.addColorStop(1,'rgba(154,255,0,0)');
  x.beginPath(); x.moveTo(px(0), h);
  d.forEach((v,i)=>x.lineTo(px(i), py(v)));
  x.lineTo(px(d.length-1), h); x.closePath(); x.fillStyle = grad; x.fill();
  x.beginPath();
  d.forEach((v,i)=> i ? x.lineTo(px(i), py(v)) : x.moveTo(px(i), py(v)));
  x.strokeStyle = '#9AFF00'; x.lineWidth = 1.6; x.lineJoin = 'round'; x.lineCap = 'round'; x.stroke();
  const lv = d[d.length-1];
  x.beginPath(); x.arc(px(d.length-1), py(lv), 2.6, 0, Math.PI*2);
  x.fillStyle = '#9AFF00'; x.shadowColor = 'rgba(154,255,0,.8)'; x.shadowBlur = 6; x.fill();
}

/* баннер тарифной скидки на рекламу (или апселл, если тариф низкий) */
function adsRenderTier(){
  const box = document.getElementById('adsTier'); if(!box) return;
  const disc = adsDisc(), tier = adsTierLabel();
  if(disc>0){
    box.className = 'ads-tier on';
    box.onclick = null; box.removeAttribute('role');
    box.innerHTML = `<span class="ads-tier-ic">${I('fire')}</span>
      <div class="ads-tier-mid"><b>Тариф ${tier} · −${Math.round(disc*100)}% на всю рекламу</b>
        <small>Скидка уже применяется: за тот же бюджет — больше показов и кликов.</small></div>`;
  }else{
    box.className = 'ads-tier up';
    box.onclick = adsTierUpsell; box.setAttribute('role','button');
    box.innerHTML = `<span class="ads-tier-ic">${I('crown')}</span>
      <div class="ads-tier-mid"><b>Больше показов за те же деньги</b>
        <small>PRO −10% · BUSINESS −20% · MAX −30% — бюджет тот же, результат больше.</small></div>
      <span class="ads-tier-cta">Открыть${I('chev')}</span>`;
  }
}
function adsTierUpsell(){
  if(typeof okoRequireSub==='function') okoRequireSub('MAX','Реклама дешевле — до −30% на MAX');
}

/* ================================================================
   ШАБЛОНЫ КАМПАНИЙ: 6 пресетов под типовые задачи. Клик по карточке
   открывает мастер создания с уже проставленными полями.
   ================================================================ */
const ADS_PRESETS = [
  {k:'first', name:'Первые подписчики', ico:'users',
   title:'Подпишись на OKO', text:'Свежие материалы, тренды и практика по контенту. Подпишись за 1 клик.',
   fmt:'channel', model:'CPC', bid:9,  cta:'Подписаться',
   budget:1000, sex:'any', geo:'РФ', interests:['контент','маркетинг'], ages:['18-24','25-34'], devs:['ios','android'], times:[],
   tags:['CPC · 9 ₽','бюджет 1 000 ₽','канал']},
  {k:'sale',  name:'Продажа продукта',  ico:'money',
   title:'Скидка 30% на 3 дня', text:'Забирай по акции. Оплата в 1 клик, доставка сразу.',
   fmt:'listing', model:'CPC', bid:14, cta:'Купить',
   budget:3000, sex:'any', geo:'РФ', interests:['бизнес','финансы'], ages:['25-34','35-44'], devs:[], times:['evening'],
   tags:['CPC · 14 ₽','бюджет 3 000 ₽','продажа']},
  {k:'retgt', name:'Тёплый ретаргет',   ico:'fire',
   title:'Забери свой бонус', text:'Ты смотрел, но не оформил. Персональная скидка 20% на 24 часа.',
   fmt:'post', model:'CPM', bid:32, cta:'Открыть',
   budget:1500, sex:'any', geo:'РФ', interests:['контент'], ages:['25-34','35-44'], devs:['ios','android'], times:[],
   tags:['CPM · 32 ₽','бюджет 1 500 ₽','ретаргет']},
  {k:'post',  name:'Продвижение поста', ico:'megaphone',
   title:'Новый пост в OKO', text:'Показать пост целевой аудитории и получить максимум охвата.',
   fmt:'post', model:'CPM', bid:28, cta:'Подробнее',
   budget:500, sex:'any', geo:'РФ', interests:[], ages:['18-24','25-34','35-44'], devs:[], times:[],
   tags:['CPM · 28 ₽','бюджет 500 ₽','пост']},
  {k:'brand', name:'Охват на бренд',    ico:'crown',
   title:'Твой бренд в шапке OKO', text:'Максимум глаз за минимальный CPM. Формат баннера, широкая география.',
   fmt:'banner', model:'CPM', bid:22, cta:'Открыть',
   budget:5000, sex:'any', geo:'РФ', interests:[], ages:['18-24','25-34','35-44','45+'], devs:[], times:['evening','night'],
   tags:['CPM · 22 ₽','бюджет 5 000 ₽','баннер']},
  {k:'lead',  name:'Сбор заявок',       ico:'briefcase',
   title:'Разбор проекта за 30 минут', text:'Бесплатная консультация. Осталось 5 слотов на этой неделе.',
   fmt:'listing', model:'CPC', bid:11, cta:'Подробнее',
   budget:2000, sex:'any', geo:'РФ', interests:['бизнес','образование'], ages:['25-34','35-44'], devs:[], times:['day','evening'],
   tags:['CPC · 11 ₽','бюджет 2 000 ₽','заявки']}
];
function adsRenderPresets(){
  const wrap = document.getElementById('adsPresetsWrap'); if(!wrap) return;
  wrap.innerHTML = `<div class="ads-presets-h">${I('bolt')}Готовые шаблоны кампаний<small>клик, чтобы заполнить мастер</small></div>
    <div class="ads-presets">${ADS_PRESETS.map(p=>`
      <button class="ads-preset" onclick="adsApplyPreset('${p.k}')">
        <div class="ads-preset-top"><span class="ads-preset-ic">${I(p.ico)}</span><span class="ads-preset-name">${esc(p.name)}</span></div>
        <div class="ads-preset-txt">${esc(p.text)}</div>
        <div class="ads-preset-tags">
          <span class="ads-preset-tag acc">${esc(p.tags[0])}</span>
          <span class="ads-preset-tag">${esc(p.tags[1])}</span>
        </div>
      </button>`).join('')}</div>`;
}
function adsApplyPreset(k){
  const p = ADS_PRESETS.find(x=>x.k===k); if(!p) return;
  const goal = ({post:'views', channel:'subs', listing:'sales', banner:'installs'})[p.fmt] || 'sales';
  const fake = {name:p.title, text:p.text, cta:p.cta, link:'', fmt:p.fmt, model:p.model, bid:p.bid,
    goal,
    sex:p.sex, geo:p.geo, cities:[], interests:p.interests, ages:p.ages, devs:p.devs||[], times:p.times||[],
    ageMin:18, ageMax:45, weekdays:[1,2,3,4,5,6,0], hourFrom:0, hourTo:24,
    budget:p.budget, pace:0, ab:null, media:null};
  adsEditId = 0;
  const st = document.getElementById('adsSheetTitle'); if(st) st.textContent = 'Новая кампания · '+p.name;
  openSheet('ads-create');
  adsPrefillFromCamp(fake);
  adsSyncLaunchBtn();
  adsGotoStep(1);
  toast('Шаблон «'+p.name+'» применён. Проверьте поля и запустите.');
}

/* ================================================================
   КАЛЬКУЛЯТОР бюджета: ползунок 300…20000 ₽ + живой прогноз охвата,
   кликов и регистраций. Кнопка сразу открывает мастер с этой суммой.
   ================================================================ */
let adsCalcBudget = 3000;
function adsCalcPredict(b){
  const disc = adsDisc();
  const cpm = 28*(1-disc);                       /* базовый пост CPM с учётом скидки тарифа */
  const imps = Math.round(b/cpm*1000);
  const clicks = Math.round(imps*0.028);         /* усреднённый CTR по кабинету */
  const regs = Math.round(clicks*0.22);          /* доля кликов до регистрации */
  return {imps, clicks, regs};
}
function adsRenderCalc(){
  const box = document.getElementById('adsCalc'); if(!box) return;
  const b = adsCalcBudget;
  const p = adsCalcPredict(b);
  box.innerHTML = `
    <div class="ads-calc-h">${I('poll')}Расчёт бюджета<b>${fmtN(b)} ₽</b></div>
    <input type="range" class="ads-calc-slider" id="adsCalcSlider" min="300" max="20000" step="100" value="${b}"
           oninput="adsCalcSet(this.value)">
    <div class="ads-calc-scale"><span>300 ₽</span><span>10 000 ₽</span><span>20 000 ₽</span></div>
    <div class="ads-calc-grid">
      <div class="ads-calc-c hot"><b>${fmtN(p.imps)}</b><span>охват</span></div>
      <div class="ads-calc-c"><b>${fmtN(p.clicks)}</b><span>клики</span></div>
      <div class="ads-calc-c"><b>${fmtN(p.regs)}</b><span>регистрации</span></div>
    </div>
    <button class="btn ads-calc-go" onclick="adsCalcLaunch()">${I('rocket')}Запустить кампанию на ${fmtN(b)} ₽</button>`;
}
function adsCalcSet(v){
  adsCalcBudget = Math.max(300, Math.min(20000, parseInt(v,10)||300));
  const p = adsCalcPredict(adsCalcBudget);
  const box = document.getElementById('adsCalc'); if(!box) return;
  const h = box.querySelector('.ads-calc-h b'); if(h) h.textContent = fmtN(adsCalcBudget)+' ₽';
  const cells = box.querySelectorAll('.ads-calc-c b');
  if(cells[0]) cells[0].textContent = fmtN(p.imps);
  if(cells[1]) cells[1].textContent = fmtN(p.clicks);
  if(cells[2]) cells[2].textContent = fmtN(p.regs);
  const btn = box.querySelector('.ads-calc-go');
  if(btn) btn.innerHTML = `${I('rocket')}Запустить кампанию на ${fmtN(adsCalcBudget)} ₽`;
}
function adsCalcLaunch(){
  adsOpenCreate();
  setTimeout(()=>{
    adsGotoStep(4);   /* прыгаем сразу к бюджету, минуя валидацию креатива */
    const bi = document.getElementById('adsInpBudget');
    if(bi){ bi.value = adsCalcBudget; adsCalcForecast(); }
    const bs = document.getElementById('adsBudgetSlider'); if(bs) bs.value = Math.min(100000, adsCalcBudget);
    document.querySelectorAll('#adsStep4 .ads-chip').forEach(b=>b.classList.remove('on'));
  }, 30);
}

/* ================================================================
   ТОП-3 креатива по CTR: карточка на главном экране с ранжированием
   активных и завершённых кампаний. Клик — раскрывает статистику.
   ================================================================ */
function adsRenderTop3(){
  const box = document.getElementById('adsTop3'); if(!box) return;
  const eligible = ADS.camps.filter(c=>(c.status==='act' || c.status==='done') && c.imps>=800);
  if(eligible.length < 2){ box.innerHTML = ''; box.className = 'ads-top3'; return; }
  const list = eligible.slice().sort((a,b)=>adsCtr(b)-adsCtr(a)).slice(0,3);
  const maxCtr = Math.max(...list.map(adsCtr), .01);
  box.className = 'ads-top3 card';
  box.innerHTML = `
    <div class="ads-top3-h">${I('crown')}Топ-3 креатива по CTR<small>ранжирование по кликам на показ</small></div>
    ${list.map((c,i)=>{
      const ctr = adsCtr(c);
      const w = Math.max(6, ctr/maxCtr*100);
      const fmt = (ADS_FORMATS[c.fmt]||ADS_FORMATS.post);
      return `<button class="ads-top3-row rank${i+1}" onclick="adsScrollToCamp(${c.id})">
        <span class="ads-top3-rank">${i+1}</span>
        <div class="ads-top3-mid">
          <div class="ads-top3-t"><b>${esc(c.name)}</b><em>${I(fmt.ico)}${esc(fmt.l)}</em></div>
          <div class="ads-top3-bar"><i style="width:${w}%"></i></div>
        </div>
        <div class="ads-top3-num"><b>${ctr.toFixed(2)}%</b><span>${fmtN(c.imps)} пок.</span></div>
      </button>`;
    }).join('')}`;
}
function adsScrollToCamp(id){
  if(!adsOpenStats.has(id)) adsOpenStats.add(id);
  adsRenderList();
  requestAnimationFrame(()=>{
    const el = document.querySelector(`.ads-camp[data-cid="${id}"]`);
    if(el) el.scrollIntoView({behavior:'smooth', block:'center'});
  });
}

/* ================================================================
   ПРОМО-РЕКОМЕНДАЦИИ: сканируем ленту пользователя и предлагаем
   бустануть посты с растущим CTR. Одна карточка = одно предложение.
   ================================================================ */
function adsRenderPromoRecs(){
  const box = document.getElementById('adsPromoRecs'); if(!box) return;
  if(typeof POSTS==='undefined' || !Array.isArray(POSTS.rec)){ box.innerHTML=''; return; }
  const mine = POSTS.rec.filter(p=>!p.promoted && !p.sponsored && p.name===PROFILE.name && (p.views||0)>=120);
  if(!mine.length){ box.innerHTML=''; return; }
  /* «синтетический CTR»: лайки+репосты к показам, ограничиваем 8% для реалистичности */
  const scored = mine.map(p=>{
    const eng = (p.likes||0) + (p.reposts||0)*3;
    const raw = eng/Math.max(50, p.views||100);
    const ctr = Math.min(0.08, raw * (0.6 + adsHash(p.id, 3)*0.9));
    return {p, ctr, uplift: Math.round((ctr*100 - 1.4)*22)};   /* +40% при CTR ~3.2% */
  }).filter(x=>x.uplift>=20).sort((a,b)=>b.uplift-a.uplift).slice(0,1);
  if(!scored.length){ box.innerHTML=''; return; }
  const x = scored[0];
  const short = (x.p.body||'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim().slice(0,54);
  const budget = 500;
  const cpm = 28 * (1 - adsDisc());
  const extra = Math.round(budget/cpm*1000);
  box.innerHTML = `
    <div class="ads-promo-rec">
      <span class="ads-promo-ic">${I('fire')}</span>
      <div class="ads-promo-mid">
        <b>Пост «${esc(short||'ваш пост')}» набирает +${x.uplift}% CTR</b>
        <p>Запусти рекламу на ${fmtN(budget)} ₽ — прогноз ещё <em>${fmtN(extra)}</em> показов целевой аудитории.</p>
      </div>
      <button class="ads-promo-btn" onclick="adsBoostFromFeed(${x.p.id})">Продвинуть${I('chev')}</button>
    </div>`;
}

/* ================================================================
   LIVE-ПОДСКАЗКИ: анализ последних 6 замеров активных кампаний
   (около 30 минут работы live-тика). Показывает одну лучшую идею.
   ================================================================ */
function adsLiveInsight(){
  const act = ADS.camps.filter(c=>c.status==='act' && (c.hist||[]).length>=8);
  if(!act.length) return null;
  let best = null;
  act.forEach(c=>{
    const h = c.hist.slice(-8);
    const half = Math.floor(h.length/2);
    const early = h.slice(0,half).reduce((s,v)=>s+v,0)/half || 1;
    const late  = h.slice(half).reduce((s,v)=>s+v,0)/(h.length-half) || 1;
    const dImp = (late-early)/early;                 /* динамика показов за 30 мин */
    const ctr = adsCtr(c), roas = adsRoas(c);
    const bpct = c.budget ? c.spent/c.budget*100 : 0;
    /* тон отбирается по приоритету: сильный сигнал побеждает слабый */
    if(roas>=2 && bpct<80){
      pick({p:5, tone:'ok', hd:'Есть смысл усилить', txt:`«${c.name}»: окупаемость <em>${adsRoasTxt(roas)}</em>. Реклама в плюсе, а бюджет ещё не выработан.`, btn:'Пополнить', act:`adsTopup(${c.id})`});
    }
    if(dImp>0.15 && ctr>=2){
      pick({p:4, tone:'ok', hd:'Темп растёт', txt:`За последние 30 мин показы «${c.name}» выросли на <em>${Math.round(dImp*100)}%</em> при CTR <em>${ctr.toFixed(1)}%</em>. Можно поднять бюджет.`, btn:'Пополнить', act:`adsTopup(${c.id})`});
    }
    if(dImp<-0.15){
      pick({p:3, tone:'warn', hd:'Темп замедляется', txt:`Показы «${c.name}» просели на <em>${Math.round(Math.abs(dImp)*100)}%</em> за 30 мин. Поднимите ставку или расширьте аудиторию.`, btn:'Изменить', act:`adsEdit(${c.id})`});
    }
    if(c.imps>1500 && ctr<1){
      pick({p:2, tone:'warn', hd:'CTR ниже нормы', txt:`«${c.name}»: CTR <em>${ctr.toFixed(1)}%</em>. Обновите креатив или заголовок.`, btn:'Изменить', act:`adsEdit(${c.id})`});
    }
  });
  function pick(x){ if(!best || x.p>best.p) best = x; }
  return best;
}
function adsRenderLiveTip(){
  const box = document.getElementById('adsLiveTip'); if(!box) return;
  const tip = adsLiveInsight();
  if(!tip){ box.className = 'ads-live-tip'; box.innerHTML = ''; return; }
  box.className = 'ads-live-tip '+(tip.tone==='warn'?'warn':'');
  box.innerHTML = `
    <span class="ads-live-tip-ic">${I(tip.tone==='warn'?'flag':'fire')}</span>
    <div class="ads-live-tip-mid"><b>${esc(tip.hd)}</b><p>${tip.txt}</p></div>
    <button class="ads-live-tip-btn" onclick="${tip.act}">${esc(tip.btn)}</button>`;
}

/* ================================================================
   ЭКСПОРТ CSV кампании: выписка показов, кликов и расходов по дням.
   Открывается в Excel и Google Sheets, разделитель «;», BOM UTF-8.
   ================================================================ */
function adsExportCsv(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c) return;
  const now = new Date();
  const rows = [['Дата','Показы','Клики','CTR %','Расход, ₽']];
  const totalI = (c.days||[]).reduce((s,d)=>s+d.i,0) || 1;
  (c.days||[]).forEach((d,i)=>{
    const dt = new Date(now.getTime()-(6-i)*864e5);
    const dd = dt.toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit',year:'numeric'});
    const ctr = d.i ? (d.c/d.i*100).toFixed(2).replace('.',',') : '0';
    const dSpent = Math.round(d.i/totalI * c.spent);
    rows.push([dd, d.i, d.c, ctr, dSpent]);
  });
  rows.push([]);
  rows.push(['Итого', c.imps, c.clicks, adsCtr(c).toFixed(2).replace('.',','), Math.round(c.spent)]);
  rows.push([]);
  rows.push(['Кампания', c.name]);
  rows.push(['Формат', (ADS_FORMATS[c.fmt]||{}).l || c.fmt]);
  rows.push(['Модель', c.model, 'ставка', c.bid+' ₽']);
  const csv = rows.map(r=>r.map(x=>{
    const s = String(x==null?'':x);
    return /[";\r\n]/.test(s) ? '"'+s.replace(/"/g,'""')+'"' : s;
  }).join(';')).join('\r\n');
  const blob = new Blob(['﻿'+csv], {type:'text/csv;charset=utf-8'});
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  const safe = (c.name||'campaign').replace(/[^\wа-яА-ЯёЁ0-9\-]+/g,'_').slice(0,40);
  a.href = url; a.download = 'oko_ads_'+safe+'_'+new Date().toISOString().slice(0,10)+'.csv';
  document.body.appendChild(a); a.click(); a.remove();
  setTimeout(()=>URL.revokeObjectURL(url), 400);
  toast('Файл CSV сохранён. Открывается в Excel и Google Sheets.');
}

/* ================================================================
   БЫСТРЫЙ BOOST поста: одна модалка, выбор поста + бюджет + срок.
   За 1 клик создаёт кампанию формата «пост» и уходит на модерацию.
   ================================================================ */
let ADS_BOOST = null;
function adsBoostPosts(){
  if(typeof POSTS==='undefined' || !Array.isArray(POSTS.rec)) return [];
  return POSTS.rec.filter(p=>!p.promoted && !p.sponsored && p.name===PROFILE.name).slice(0,6);
}
function adsBoostPredict(daily, days){
  const cpm = 28*(1-adsDisc());
  const imps = Math.round(daily*days/cpm*1000);
  return {imps, days, total: daily*days};
}
function adsBoostPost(){
  const posts = adsBoostPosts();
  ADS_BOOST = {budget:300, days:3, postId: posts[0] ? posts[0].id : 0};
  showPopup({ico:'rocket', title:'Продвинуть пост',
    body: adsBoostBody(posts),
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Продвинуть', onclick:()=>adsBoostConfirm()}
    ]});
}

/* быстрый буст конкретного поста из ленты (одна кнопка): 100 ₽ / 3 дня, дальше — конфиг */
function adsBoostFromFeed(postId){
  let posts = adsBoostPosts();
  /* если конкретный пост не попал в список (уже промо / чужой) — добавим его первым */
  if(!posts.some(p=>p.id===postId) && typeof POSTS!=='undefined'){
    const p = (POSTS.rec||[]).find(x=>x.id===postId);
    if(p) posts = [p].concat(posts);
  }
  ADS_BOOST = {budget:100, days:3, postId};
  showPopup({ico:'rocket', title:'Продвинуть за 100 ₽/день',
    body: adsBoostBody(posts),
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Продвинуть', onclick:()=>adsBoostConfirm()}
    ]});
}
function adsBoostBody(posts){
  const pred = adsBoostPredict(ADS_BOOST.budget, ADS_BOOST.days);
  const list = posts.length ? posts.map(p=>{
    const txt = (p.body||'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim().slice(0,80);
    return `<div class="ads-boost-post ${ADS_BOOST.postId===p.id?'on':''}" onclick="adsBoostPick(${p.id})">
      <div class="ads-boost-post-mid"><b>${esc(p.name)}</b><small>${esc(txt||'без текста')}</small></div>
    </div>`;
  }).join('') : `<div class="ads-boost-empty">${I('photo')}Нет своих постов для продвижения. Опубликуйте пост в ленте, чтобы вернуться сюда.</div>`;
  return `<div class="ads-boost-picker">
    ${list}
    <div>
      <div class="ads-lbl" style="margin:0 0 8px">Бюджет</div>
      <div class="ads-chips" style="margin:0">
        ${[100,300,500,1000,2000].map(v=>`<button class="ads-chip ${ADS_BOOST.budget===v?'on':''}" onclick="adsBoostSet('budget',${v})">${v} ₽/день</button>`).join('')}
      </div>
    </div>
    <div>
      <div class="ads-lbl" style="margin:0 0 8px">Срок продвижения</div>
      <div class="ads-chips" style="margin:0">
        ${[3,7,14].map(v=>`<button class="ads-chip ${ADS_BOOST.days===v?'on':''}" onclick="adsBoostSet('days',${v})">${v} дн.</button>`).join('')}
      </div>
    </div>
    <div class="ads-boost-sum">Ожидаемый охват <b>${fmtN(pred.imps)}</b> показов за <b>${pred.days}</b> дн. · списание <b>${fmtN(pred.total)} ₽</b></div>
  </div>`;
}
function adsBoostRerender(){
  /* самое простое и надёжное — перерисовать всю модалку с текущим ADS_BOOST */
  let posts = adsBoostPosts();
  if(ADS_BOOST && ADS_BOOST.postId && !posts.some(p=>p.id===ADS_BOOST.postId) && typeof POSTS!=='undefined'){
    const p = (POSTS.rec||[]).find(x=>x.id===ADS_BOOST.postId);
    if(p) posts = [p].concat(posts);
  }
  showPopup({ico:'rocket', title:'Продвинуть пост',
    body: adsBoostBody(posts),
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Продвинуть', onclick:()=>adsBoostConfirm()}
    ]});
}
function adsBoostPick(id){ ADS_BOOST.postId = id; adsBoostRerender(); }
function adsBoostSet(k, v){ ADS_BOOST[k] = v; adsBoostRerender(); }
function adsBoostConfirm(){
  let posts = adsBoostPosts();
  if(ADS_BOOST && ADS_BOOST.postId && !posts.some(p=>p.id===ADS_BOOST.postId) && typeof POSTS!=='undefined'){
    const p = (POSTS.rec||[]).find(x=>x.id===ADS_BOOST.postId);
    if(p) posts = [p].concat(posts);
  }
  const post = posts.find(p=>p.id===ADS_BOOST.postId) || posts[0];
  if(!post){ toast('Сначала опубликуйте пост в ленте'); closePopup(); return; }
  const daily = ADS_BOOST.budget, days = ADS_BOOST.days;
  const budget = daily * days;   /* трактуем чипы как «₽ в сутки», списываем × дней */
  if(!walletCharge(budget, 'Boost поста')) return;
  okoEarn(budget, 'Рекламный кабинет');
  const name = 'Boost: '+((post.body||'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim().slice(0,32) || 'ваш пост');
  adsBill(budget, name, 'Быстрое продвижение');
  const camp = {
    id: ADS.seq++, name, text: 'Пост из ленты OKO продвигается на целевую аудиторию.',
    cta:'Открыть', link:'', fmt:'post', model:'CPM', bid:28, ctrEst:.022,
    cvr:0.22, aov:690, salesRate:0.008,
    disc: adsDisc(), pace: days, dailyCap: Math.max(1, Math.round(budget/days)),
    spentToday:0, spentDay: adsDayKey(),
    sex:'any', geo:'РФ', cities:[], interests:[], ages:['18-24','25-34','35-44'], devs:[], times:[],
    media:null, budget, spent:0, imps:0, clicks:0, status:'mod', startTs:0,
    hist:[], created: Date.now(),
    days:[...Array(7)].map(()=>({i:0,c:0})), dstamp: adsDayKey(),
    boostedPostId: post.id
  };
  ADS.camps.unshift(camp);
  adsSave(); closePopup(); adsRender();
  toast('Пост ушёл на модерацию OKO. Показ стартует через несколько секунд.');
  setTimeout(()=>adsModerate(camp.id), 2400);
}

function adsRender(){ adsRenderTier(); adsRenderSummary(); adsRenderList(); adsRenderPresets(); adsRenderCalc(); adsRenderLiveTip(); adsRenderPromoRecs(); adsRenderTop3(); }

function adsTickUI(){
  adsRenderSummary();
  adsRenderLiveTip();
  ADS.camps.forEach(c=>{
    const el = document.querySelector(`.ads-camp[data-cid="${c.id}"]`); if(!el) return;
    const set = (k,v)=>{ const n = el.querySelector(`[data-m="${k}"]`); if(n) n.textContent = v; };
    /* растущие метрики — с короткой подсветкой при реальном изменении */
    const setLive = (k,v)=>{ const n = el.querySelector(`[data-m="${k}"]`); if(n && n.textContent!==v){ n.textContent = v; if(c.status==='act') adsFlashEl(n); } };
    setLive('imps', fmtN(c.imps)); setLive('clicks', fmtN(c.clicks));
    set('ctr', adsCtr(c).toFixed(1)+'%');
    const sp = el.querySelector('[data-m="spent"]'); if(sp) sp.innerHTML = fmtN(Math.round(c.spent))+'<i class="ads-cur">₽</i>';
    const bar = el.querySelector('[data-m="bar"]'); if(bar){ bar.style.width = Math.min(100, c.spent/c.budget*100)+'%'; bar.className = adsBudgetCls(c); }
    adsDrawSpark(c);
    if(adsOpenStats.has(c.id)){
      adsDrawDays(c);
      adsSyncFunnel(c, false);
      adsSyncRoas(c);
      const sub = el.querySelector('[data-m="sub7"]'); if(sub) sub.innerHTML = adsSub7(c);
      const ab = el.querySelector('[data-m="ab"]'); if(ab) ab.innerHTML = adsAbInner(c);
      const adv = el.querySelector('.ads-advice'); if(adv) adv.outerHTML = adsAdviceBlock(c);
    }
  });
}

/* ---------- создание кампании ---------- */
let adsDraft = {};
let adsEditId = 0;   /* 0 — создание новой; иначе id редактируемой кампании */
function adsDraftReset(){
  adsDraft = {
    goal:'sales', fmt:'listing', model:'CPC', cta:'Купить',
    sex:'any', geo:'РФ', cities:[], interests:[], devs:[],
    ageMin:18, ageMax:45, lal:false, lalFrom:0,
    weekdays:[1,2,3,4,5,6,0], hourFrom:0, hourTo:24,
    ab:false, reach:0, media:null, pace:0, start:'now', startAt:0
  };
}

/* заголовки шагов вверху sheet-а */
const ADS_STEP_TITLES = {
  1: {t:'Шаг 1 · Цель',       s:'Что должна принести реклама'},
  2: {t:'Шаг 2 · Аудитория',  s:'Кому показать объявление'},
  3: {t:'Шаг 3 · Креатив',    s:'Заголовок, текст, медиа и кнопка'},
  4: {t:'Шаг 4 · Бюджет',     s:'Ставка, дневной лимит и график показа'},
  5: {t:'Шаг 5 · Запуск',     s:'Проверка и старт кампании'}
};
/* темп открутки: 0 — максимум, N — равномерно за N дней (дневной лимит = бюджет/N) */
function adsPickPace(btn){
  document.querySelectorAll('#adsPaceChips .ads-chip').forEach(b=>b.classList.remove('on'));
  btn.classList.add('on'); adsDraft.pace = parseInt(btn.dataset.v,10) || 0;
  adsCalcForecast();
}
/* старт: сейчас или запланировать на дату/время */
function adsPickStart(btn){
  document.querySelectorAll('#adsStartChips .ads-chip').forEach(b=>b.classList.remove('on'));
  btn.classList.add('on'); adsDraft.start = btn.dataset.v;
  const inp = document.getElementById('adsSchedAt');
  if(inp){
    inp.style.display = adsDraft.start==='sched' ? '' : 'none';
    if(adsDraft.start==='sched' && !inp.value){
      const t = new Date(Date.now()+864e5);        /* по умолчанию — завтра, ровный час */
      t.setMinutes(0,0,0);
      inp.value = new Date(t.getTime()-t.getTimezoneOffset()*6e4).toISOString().slice(0,16);
    }
  }
  adsSyncLaunchBtn();
}
function adsLaunchLabel(){
  if(adsEditId) return 'Сохранить';
  return adsDraft.start==='sched' ? 'Запланировать' : 'Запустить';
}
function adsSyncLaunchBtn(){
  const b = document.getElementById('adsLaunchBtn'); if(!b) return;
  const s = b.querySelector('span'); if(s) s.textContent = adsLaunchLabel();
}

/* --- шаг 5: сводный предпросмотр перед запуском --- */
function adsRenderReview(){
  const box = document.getElementById('adsReview'); if(!box) return;
  const title = (document.getElementById('adsInpTitle')||{}).value?.trim() || 'Заголовок объявления';
  const text  = (document.getElementById('adsInpText') ||{}).value?.trim() || 'Текст объявления';
  const link  = (document.getElementById('adsInpLink') ||{}).value?.trim() || '';
  const budget= parseInt((document.getElementById('adsInpBudget')||{}).value,10) || 0;
  const bid   = parseInt((document.getElementById('adsInpBid')||{}).value,10) || adsRecBid();
  const g = ADS_GOALS[adsDraft.goal] || ADS_GOALS.sales;
  const f = ADS_FORMATS[adsDraft.fmt] || ADS_FORMATS.post;
  const mn = adsDraft.ageMin||18, mx = adsDraft.ageMax||45;
  const hf = adsDraft.hourFrom||0, ht = adsDraft.hourTo||24;
  const wd = Array.isArray(adsDraft.weekdays) ? adsDraft.weekdays : [1,2,3,4,5,6,0];
  const wdNames = ['вс','пн','вт','ср','чт','пт','сб'];
  const wdSel = wd.length===7 ? 'вся неделя' : wd.slice().sort((a,b)=>((a||7)-(b||7))).map(v=>wdNames[v]).join(', ');
  const ints = adsPicked('adsIntChips');
  const disc = adsDisc();
  const cpm = (adsDraft.model==='CPC') ? bid*0.022*1000*(1-disc) : bid*(1-disc);
  const imps = cpm ? Math.round(budget/cpm*1000) : 0;
  const clicks = Math.round(imps * (adsDraft.model==='CPC' ? Math.max(.008, 0.022) : 0.022));
  const startTxt = adsDraft.start==='sched'
    ? (function(){ const raw = (document.getElementById('adsSchedAt')||{}).value;
        if(!raw) return 'запланировано';
        const d = new Date(raw);
        return d.toLocaleDateString('ru-RU',{day:'2-digit',month:'short'})+' '+d.toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'}); })()
    : 'сразу после модерации';
  const iphonePrev = adsPreviewCardHtml(title, text, adsDraft.cta || 'Подробнее', adsDraft.media, f.tag);
  box.innerHTML = `
    <div class="ads-rv-h"><span class="ads-rv-ic">${I('eye')}</span><div><b>Проверьте кампанию перед запуском</b><small>После запуска можно менять бюджет и креатив, аудитория фиксируется на 24 часа</small></div></div>

    <div class="ads-rv-sec">
      <div class="ads-rv-t">${I(g.ico)}Цель и формат</div>
      <div class="ads-rv-kv"><span>Цель</span><b>${esc(g.l)}</b></div>
      <div class="ads-rv-kv"><span>Формат</span><b>${esc(f.l)}</b></div>
      <div class="ads-rv-kv"><span>Кнопка</span><b>${esc(adsDraft.cta || 'Подробнее')}</b></div>
    </div>

    <div class="ads-rv-sec">
      <div class="ads-rv-t">${I('users')}Аудитория</div>
      <div class="ads-rv-kv"><span>Пол</span><b>${adsDraft.sex==='m'?'мужской':adsDraft.sex==='f'?'женский':'любой'}</b></div>
      <div class="ads-rv-kv"><span>Возраст</span><b>${mn}–${mx>=65?'65+':mx}</b></div>
      <div class="ads-rv-kv"><span>Гео</span><b>${esc(adsDraft.geo)}${adsDraft.cities.length?' · '+adsDraft.cities.slice(0,3).join(', ')+(adsDraft.cities.length>3?' +'+(adsDraft.cities.length-3):''):''}</b></div>
      <div class="ads-rv-kv"><span>Интересы</span><b>${ints.length ? esc(ints.join(', ')) : 'широкие'}</b></div>
      ${adsDraft.lal ? `<div class="ads-rv-kv"><span>Look-alike</span><b class="acc">включён · +40% охват</b></div>` : ''}
      <div class="ads-rv-kv"><span>Охват</span><b class="acc">${fmtN(adsDraft.reach||0)} чел.</b></div>
    </div>

    <div class="ads-rv-sec">
      <div class="ads-rv-t">${I('photo')}Как увидят</div>
      <div class="ads-iphone sm">
        <div class="ads-iphone-notch"></div>
        <div class="ads-iphone-scr">
          <div class="ads-iphone-topbar"><span>9:41</span><i><svg class="i"><use href="#i-eye"/></svg>OKO</i></div>
          <div class="ads-preview">${iphonePrev}</div>
        </div>
        <div class="ads-iphone-home"></div>
      </div>
      ${link ? `<div class="ads-rv-link">${I('bolt')}Переход: <span>${esc(link)}</span></div>` : ''}
      ${adsDraft.ab ? `<div class="ads-rv-ab">${I('poll')}A/B тест: <span>«${esc((document.getElementById('adsInpTitleB')||{}).value?.trim() || 'вариант B')}»</span></div>` : ''}
    </div>

    <div class="ads-rv-sec">
      <div class="ads-rv-t">${I('money')}Бюджет и график</div>
      <div class="ads-rv-kv"><span>Модель</span><b>${adsDraft.model} · ${bid} ₽</b></div>
      <div class="ads-rv-kv"><span>Бюджет</span><b>${fmtN(budget)} ₽</b></div>
      <div class="ads-rv-kv"><span>Дни</span><b>${esc(wdSel)}</b></div>
      <div class="ads-rv-kv"><span>Окно</span><b>${(hf<10?'0':'')+hf}:00–${(ht<10?'0':'')+ht}:00</b></div>
      <div class="ads-rv-kv"><span>Старт</span><b>${esc(startTxt)}</b></div>
      ${disc>0 ? `<div class="ads-rv-kv"><span>Скидка тарифа</span><b class="acc">−${Math.round(disc*100)}%</b></div>` : ''}
    </div>

    <div class="ads-rv-forecast">
      <div class="ads-rv-fc"><b>${fmtN(imps)}</b><span>показов</span></div>
      <div class="ads-rv-fc"><b>${fmtN(clicks)}</b><span>кликов</span></div>
      <div class="ads-rv-fc"><b>${fmtN(budget)}<i>₽</i></b><span>списание</span></div>
    </div>
    <div class="ads-balance">На лицевом счёте: <b>${fmtMoney(WALLET.balance)}</b>${budget>WALLET.balance?' — не хватает, пополни кошелёк':''}</div>
  `;
}

/* общий рендер preview-карточки (используем и в шаге 3, и в шаге 5) */
function adsPreviewCardHtml(title, text, cta, media, tag){
  return `<div class="ads-prev-card">
      <div class="ads-prev-top"><span class="ads-prev-ava">${(PROFILE.name[0]||'O').toUpperCase()}</span>
        <div><b>${esc(PROFILE.name)}</b><small>${esc(tag||'спонсировано')}</small></div></div>
      <div class="ads-prev-title">${esc(title)}</div>
      <div class="ads-prev-text">${esc(text)}</div>
      ${adsMediaHtml(media, 'ads-prev-media')}
      <button class="ads-prev-cta">${esc(cta)} ${I('chev')}</button>
    </div>`;
}

/* ---------- медиа креатива (фото / картинка / видео) ---------- */
/* фирменный постер-заглушка для демо-кампаний (самодостаточный SVG data-URL) */
function adsBrandPoster(c){
  const f = ADS_FORMATS[c.fmt] || ADS_FORMATS.post;
  const label = (f.l || 'OKO').toUpperCase();
  const svg =
    "<svg xmlns='http://www.w3.org/2000/svg' width='640' height='360' viewBox='0 0 640 360'>"+
    "<defs><radialGradient id='g' cx='28%' cy='24%' r='90%'>"+
    "<stop offset='0' stop-color='#1c2a0c'/><stop offset='55%' stop-color='#0d1207'/><stop offset='1' stop-color='#050705'/>"+
    "</radialGradient><linearGradient id='l' x1='0' y1='0' x2='1' y2='1'>"+
    "<stop offset='0' stop-color='#9AFF00'/><stop offset='1' stop-color='#6fbf00'/></linearGradient></defs>"+
    "<rect width='640' height='360' fill='url(#g)'/>"+
    "<circle cx='120' cy='300' r='190' fill='#9AFF00' opacity='.06'/>"+
    "<circle cx='540' cy='70' r='140' fill='#9AFF00' opacity='.05'/>"+
    "<g transform='translate(320 150)'><ellipse rx='58' ry='38' fill='none' stroke='url(#l)' stroke-width='9'/>"+
    "<circle r='17' fill='url(#l)'/></g>"+
    "<text x='320' y='250' fill='#9AFF00' font-family='Arial,Helvetica,sans-serif' font-size='24' font-weight='700' letter-spacing='6' text-anchor='middle'>"+label+"</text>"+
    "<text x='320' y='284' fill='#ffffff' opacity='.5' font-family='Arial,Helvetica,sans-serif' font-size='13' letter-spacing='2' text-anchor='middle'>OKO ADS</text>"+
    "</svg>";
  return {type:'image', src:'data:image/svg+xml;utf8,'+encodeURIComponent(svg), name:'Креатив OKO', w:640, h:360};
}

/* HTML медиа для превью и ленты; после перезагрузки без src — аккуратная заглушка */
function adsMediaHtml(m, cls){
  if(!m) return '';
  cls = cls || 'ads-post-media';
  if(m.type==='image' && m.src)
    return `<div class="${cls}"><img src="${m.src}" alt="" loading="lazy"></div>`;
  if(m.type==='video' && m.src)
    return `<div class="${cls} vid"><video src="${m.src}" muted loop autoplay playsinline></video><span class="ads-media-play">${I('play')}</span></div>`;
  const ic = m.type==='video' ? 'circle-play' : 'photo';
  const lbl = m.type==='video' ? 'видео' : 'изображение';
  return `<div class="${cls} ph">${I(ic)}<em>${esc(m.name||lbl)}</em></div>`;
}

function adsRenderMediaZone(){
  const z = document.getElementById('adsMediaZone'); if(!z) return;
  const m = adsDraft.media;
  if(!m){
    z.className = 'ads-media empty';
    z.innerHTML = `<span class="ads-media-ic">${I('photo')}</span>
      <b>Добавьте креатив</b>
      <small>фото, картинка или видео — с медиа CTR выше в 2–3 раза. До 900px, 10 МБ.</small>`;
    return;
  }
  z.className = 'ads-media has';
  z.innerHTML = `${adsMediaHtml(m, 'ads-media-prev')}
    <div class="ads-media-meta"><b>${esc(m.name || (m.type==='video'?'Видео':'Изображение'))}</b>
      <small>${m.type==='video'?'видео':'изображение'}${m.w?` · ${m.w}×${m.h}`:''}</small></div>
    <button type="button" class="ads-media-x" onclick="adsClearMedia()" title="Убрать">${I('trash')}</button>`;
}

function adsPickMedia(kind){
  const id = kind==='photo' ? 'adsFilePhoto' : kind==='video' ? 'adsFileVideo' : 'adsFileImage';
  const inp = document.getElementById(id);
  if(inp){ try{ inp.value=''; }catch(e){} inp.click(); }
}

function adsRevokeDraftMedia(){
  const m = adsDraft.media;
  if(m && m._obj && m.src){ try{ URL.revokeObjectURL(m.src); }catch(e){} }
}

/* сжатие картинки в небольшой data-URL через canvas (влезает в localStorage) */
function adsCompressImage(file, cb){
  let url;
  try{ url = URL.createObjectURL(file); }catch(e){ cb(null); return; }
  const img = new Image();
  img.onload = function(){
    const max = 900;
    let w = img.naturalWidth || 900, h = img.naturalHeight || 600;
    if(Math.max(w,h) > max){ const k = max/Math.max(w,h); w = Math.round(w*k); h = Math.round(h*k); }
    let out = null;
    try{
      const cv = document.createElement('canvas'); cv.width = w; cv.height = h;
      const cx = cv.getContext('2d'); cx.drawImage(img, 0, 0, w, h);
      out = cv.toDataURL('image/webp', 0.72);
      if(!out || out.indexOf('data:image/webp')!==0) out = cv.toDataURL('image/jpeg', 0.74);
    }catch(e){ out = null; }
    try{ URL.revokeObjectURL(url); }catch(e){}
    cb(out, w, h);
  };
  img.onerror = function(){ try{ URL.revokeObjectURL(url); }catch(e){} cb(null); };
  img.src = url;
}

function adsMediaChosen(input, kind){
  const file = input && input.files && input.files[0]; if(!file) return;
  const maxMb = kind==='video' ? 10 : 8;
  if(file.size > maxMb*1048576){ toast('Файл больше '+maxMb+' МБ — выберите поменьше'); return; }
  adsRevokeDraftMedia();
  if(kind==='video'){
    let url;
    try{ url = URL.createObjectURL(file); }catch(e){ toast('Не удалось прочитать видео'); return; }
    adsDraft.media = {type:'video', src:url, name:file.name, _obj:true};
    adsRenderMediaZone(); adsRenderPreview();
    toast('Видео прикреплено к креативу');
  }else{
    toast('Обрабатываю изображение…');
    adsCompressImage(file, (src, w, h)=>{
      if(!src){ toast('Не удалось прочитать изображение'); return; }
      adsDraft.media = {type:'image', src, name:file.name, w, h};
      adsRenderMediaZone(); adsRenderPreview();
      toast('Изображение прикреплено к креативу');
    });
  }
}

function adsClearMedia(){
  adsRevokeDraftMedia();
  adsDraft.media = null;
  adsRenderMediaZone(); adsRenderPreview();
}

function adsOpenCreate(){
  adsRevokeDraftMedia();
  adsEditId = 0;
  adsDraftReset();
  const st = document.getElementById('adsSheetTitle'); if(st) st.textContent = 'Новая кампания';
  ['adsInpTitle','adsInpText','adsInpLink','adsInpTitleB'].forEach(id=>{ const e = document.getElementById(id); if(e) e.value=''; });
  document.querySelectorAll('#adsPaceChips .ads-chip').forEach((b,i)=>b.classList.toggle('on', i===0));
  document.querySelectorAll('#adsStartChips .ads-chip').forEach((b,i)=>b.classList.toggle('on', i===0));
  const sa = document.getElementById('adsSchedAt'); if(sa){ sa.style.display='none'; sa.value=''; }
  const abChip = document.getElementById('adsAbChip'); if(abChip) abChip.classList.remove('on');
  const abInp = document.getElementById('adsInpTitleB'); if(abInp) abInp.style.display = 'none';
  document.querySelectorAll('#adsCtaChips .ads-chip').forEach((b,i)=>b.classList.toggle('on', b.dataset.v===adsDraft.cta));
  document.querySelectorAll('#adsSexChips .ads-chip').forEach((b,i)=>b.classList.toggle('on', i===0));
  document.querySelectorAll('#adsGeoChips .ads-chip').forEach((b,i)=>b.classList.toggle('on', i===0));
  document.querySelectorAll('#adsDevChips .ads-chip').forEach(b=>b.classList.remove('on'));
  const bi = document.getElementById('adsInpBudget'); if(bi) bi.value = '1000';
  const bs = document.getElementById('adsBudgetSlider'); if(bs) bs.value = '1000';
  const amn = document.getElementById('adsAgeMin'); if(amn) amn.value = adsDraft.ageMin;
  const amx = document.getElementById('adsAgeMax'); if(amx) amx.value = adsDraft.ageMax;
  const hf = document.getElementById('adsHourFrom'); if(hf) hf.value = adsDraft.hourFrom;
  const ht = document.getElementById('adsHourTo');   if(ht) ht.value = adsDraft.hourTo;
  adsRenderGoals();
  adsRenderFmts();
  adsRenderCities();
  adsRenderSaved();
  adsRenderInterests();
  adsRenderLal();
  adsRenderWeek();
  adsRenderMediaZone();
  adsRenderPostPicker();
  adsSyncModelUI();
  adsSyncLaunchBtn();
  adsStep(1); openSheet('ads-create');
}

/* ---------- шаг 1: цели ---------- */
function adsRenderGoals(){
  const box = document.getElementById('adsGoalGrid'); if(!box) return;
  box.innerHTML = Object.entries(ADS_GOALS).map(([k,g])=>`
    <button class="ads-goal ${adsDraft.goal===k?'on':''}" data-v="${k}" onclick="adsPickGoal('${k}')">
      <span class="ads-goal-ic">${I(g.ico)}</span>
      <span class="ads-goal-l">${esc(g.l)}</span>
      <span class="ads-goal-k">${esc(g.kpi)}</span>
    </button>`).join('');
  adsRenderGoalDetail();
}
function adsRenderGoalDetail(){
  const box = document.getElementById('adsGoalDetail'); if(!box) return;
  const g = ADS_GOALS[adsDraft.goal] || ADS_GOALS.sales;
  box.innerHTML = `<div class="ads-goal-info">
    <div class="ads-goal-info-h"><span class="ads-goal-info-ic">${I(g.ico)}</span><b>${esc(g.l)}</b></div>
    <p>${esc(g.desc)}</p>
    <div class="ads-goal-info-kpi">
      <span>${I('bolt')}CTR ~ <b>${esc(g.ctrHint)}</b></span>
      <span>${I('money')}${esc(g.roasHint)}</span>
    </div>
  </div>`;
}
function adsPickGoal(k){
  const g = ADS_GOALS[k]; if(!g) return;
  adsDraft.goal = k;
  adsDraft.fmt = g.fmt;
  adsDraft.model = g.model;
  adsDraft.cta = g.cta;
  /* синхронизируем CTA-чипы */
  document.querySelectorAll('#adsCtaChips .ads-chip').forEach(b=>b.classList.toggle('on', b.dataset.v===g.cta));
  adsRenderGoals();
  adsRenderFmts();
  adsSyncModelUI();
}

/* --- формат --- */
function adsRenderFmts(){
  const box = document.getElementById('adsFmtGrid'); if(!box) return;
  const compact = box.classList.contains('compact');
  box.innerHTML = Object.entries(ADS_FORMATS).map(([k,f])=>compact ? `
    <button class="ads-fmt cmp ${adsDraft.fmt===k?'on':''}" data-v="${k}" onclick="adsPickFmt('${k}')">
      <span class="ads-fmt-ic">${I(f.ico)}</span>
      <span class="ads-fmt-l">${esc(f.l)}</span>
      <span class="ads-fmt-price">${f.model} · от ${f.model==='CPC'?f.cpc:f.cpm} ₽</span>
    </button>` : `
    <button class="ads-fmt ${adsDraft.fmt===k?'on':''}" data-v="${k}" onclick="adsPickFmt('${k}')">
      <span class="ads-fmt-ic">${I(f.ico)}</span>
      <span class="ads-fmt-l">${esc(f.l)}</span>
      <span class="ads-fmt-d">${esc(f.desc)}</span>
      <span class="ads-fmt-price">${f.model} · от ${f.model==='CPC'?f.cpc:f.cpm} ₽</span>
    </button>`).join('');
  const tw = document.getElementById('adsFmtTarget');
  if(tw){
    const f = ADS_FORMATS[adsDraft.fmt];
    tw.innerHTML = `<div class="ads-fmt-note">${I('bolt')}<span>Модель по умолчанию — <b>${f.model}</b> (${ADS_MODELS[f.model].unit}). Ставку и график зададите на шаге 4.</span></div>`;
  }
}
function adsPickFmt(k){
  adsDraft.fmt = k; adsDraft.model = ADS_FORMATS[k].model;
  adsRenderFmts(); adsSyncModelUI();
}

function adsStep(n){
  /* валидация: покидая креатив (шаг 3) — обязательны заголовок и текст, при A/B — вариант B */
  if(n>3){
    const title = (document.getElementById('adsInpTitle')||{}).value?.trim() || '';
    const text  = (document.getElementById('adsInpText')||{}).value?.trim() || '';
    if(!title){ toast('Добавь заголовок объявления'); adsGotoStep(3); return; }
    if(!text){ toast('Добавь текст объявления'); adsGotoStep(3); return; }
    if(adsDraft.ab && !(document.getElementById('adsInpTitleB')||{}).value?.trim()){
      toast('Добавь вариант B заголовка или выключи A/B тест'); adsGotoStep(3); return;
    }
  }
  /* валидация: покидая бюджет (шаг 4) — минимум 100 ₽ */
  if(n>4){
    const b = parseInt((document.getElementById('adsInpBudget')||{}).value, 10) || 0;
    if(b<100){ toast('Минимальный бюджет — 100 ₽'); adsGotoStep(4); return; }
  }
  adsGotoStep(n);
}
function adsGotoStep(n){
  for(let i=1;i<=5;i++){ const s = document.getElementById('adsStep'+i); if(s) s.style.display = (i===n?'block':'none'); }
  document.querySelectorAll('#adsSteps i').forEach((d,i)=>d.classList.toggle('on', i<n));
  const hd = document.getElementById('adsStepHd');
  if(hd && ADS_STEP_TITLES[n]) hd.innerHTML = `<b>${ADS_STEP_TITLES[n].t}</b><small>${ADS_STEP_TITLES[n].s}</small>`;
  if(n===1){ adsRenderGoals(); adsRenderFmts(); }
  if(n===2){ adsRenderInterests(); adsRenderLal(); adsAgeInput(true); adsCalcReach(); }
  if(n===3){ adsRenderPostPicker(); adsRenderPreview(); adsRenderMediaZone(); }
  if(n===4){ adsRenderWeek(); adsHourInput(true); adsSyncModelUI(); adsCalcForecast(); adsBudgetSliderSync(); }
  if(n===5){ adsRenderReview(); }
  adsSyncLaunchBtn();
  const sh = document.getElementById('sheet-ads-create'); if(sh) sh.scrollTop = 0;
}

function adsPickCta(btn){
  document.querySelectorAll('#adsCtaChips .ads-chip').forEach(b=>b.classList.remove('on'));
  btn.classList.add('on'); adsDraft.cta = btn.dataset.v; adsRenderPreview();
}
function adsPickSex(btn){
  document.querySelectorAll('#adsSexChips .ads-chip').forEach(b=>b.classList.remove('on'));
  btn.classList.add('on'); adsDraft.sex = btn.dataset.v; adsCalcReach();
}
function adsPickGeo(btn){
  document.querySelectorAll('#adsGeoChips .ads-chip').forEach(b=>b.classList.remove('on'));
  btn.classList.add('on'); adsDraft.geo = btn.dataset.v; adsDraft.cities = [];
  adsRenderCities(); adsCalcReach();
}
function adsToggleMulti(btn){
  btn.classList.toggle('on');
  /* синхронизируем adsDraft.interests для интересов (шаг 2), т.к. они рендерятся динамически */
  if(btn.closest('#adsIntChips')){
    adsDraft.interests = adsPicked('adsIntChips');
  }
  adsCalcReach();
}
function adsToggleAb(){
  adsDraft.ab = !adsDraft.ab;
  const chip = document.getElementById('adsAbChip'); if(chip) chip.classList.toggle('on', adsDraft.ab);
  const inp = document.getElementById('adsInpTitleB');
  if(inp){ inp.style.display = adsDraft.ab ? '' : 'none'; if(adsDraft.ab) inp.focus(); }
}
function adsPicked(boxId){
  return [...document.querySelectorAll('#'+boxId+' .ads-chip.on')].map(b=>b.dataset.v);
}

/* --- интересы (расширенный набор чипов) --- */
function adsRenderInterests(){
  const box = document.getElementById('adsIntChips'); if(!box) return;
  const active = new Set(Array.isArray(adsDraft.interests) ? adsDraft.interests : []);
  box.innerHTML = ADS_INTERESTS.map(v=>`
    <button class="ads-chip ${active.has(v)?'on':''}" data-v="${esc(v)}" onclick="adsToggleMulti(this)">${esc(v)}</button>
  `).join('');
}

/* --- возраст: двойной ползунок 14..65+ --- */
function adsAgeInput(quiet){
  const a = document.getElementById('adsAgeMin'), b = document.getElementById('adsAgeMax');
  if(!a || !b) return;
  let mn = parseInt(a.value,10), mx = parseInt(b.value,10);
  if(mn>mx-2){
    /* какой ползунок трогали — того и подчиняем; проще всего: минимум не превышает максимум-2 */
    if(mn>65-2) mn = 63;
    mx = Math.min(65, Math.max(mn+2, mx));
    a.value = mn; b.value = mx;
  }
  adsDraft.ageMin = mn; adsDraft.ageMax = mx;
  const fill = document.getElementById('adsAgeFill');
  if(fill){
    const l = (mn-14)/(65-14)*100, r = (mx-14)/(65-14)*100;
    fill.style.left = l+'%'; fill.style.width = (r-l)+'%';
  }
  const hint = document.getElementById('adsAgeHint');
  if(hint) hint.textContent = mn+' – '+(mx>=65?'65+':mx);
  if(!quiet) adsCalcReach();
}

/* --- look-alike: чипы аудиторий-источников с флагом «расширить похожими» --- */
function adsRenderLal(){
  const box = document.getElementById('adsLalBox'); if(!box) return;
  const auds = ADS.auds || [];
  const opts = auds.length ? auds.map(a=>`
    <button class="ads-lal-chip ${adsDraft.lalFrom===a.id?'on':''}" onclick="adsLalPick(${a.id})">
      ${I('users')}${esc(a.name)}
    </button>`).join('') : `<span class="ads-lal-empty">Сохрани аудиторию, чтобы построить похожую</span>`;
  box.innerHTML = `
    <label class="ads-lal-toggle">
      <input type="checkbox" ${adsDraft.lal?'checked':''} onchange="adsLalToggle(this.checked)">
      <span class="ads-lal-sw"><i></i></span>
      <span class="ads-lal-txt"><b>Расширить похожими профилями</b>
        <small>+40% к охвату: OKO найдёт людей с похожим поведением</small></span>
    </label>
    <div class="ads-lal-src" ${adsDraft.lal?'':'style="opacity:.5;pointer-events:none"'}>
      <div class="ads-lal-src-h">Источник похожести</div>
      <div class="ads-lal-chips">${opts}</div>
    </div>`;
}
function adsLalToggle(v){
  adsDraft.lal = !!v;
  if(v && !adsDraft.lalFrom && (ADS.auds||[]).length) adsDraft.lalFrom = ADS.auds[0].id;
  adsRenderLal(); adsCalcReach();
}
function adsLalPick(id){ adsDraft.lalFrom = id; adsRenderLal(); adsCalcReach(); }

/* --- дни недели --- */
const ADS_WEEK_NAMES = [['пн',1],['вт',2],['ср',3],['чт',4],['пт',5],['сб',6],['вс',0]];
function adsRenderWeek(){
  const box = document.getElementById('adsWeekChips'); if(!box) return;
  const on = new Set(Array.isArray(adsDraft.weekdays) ? adsDraft.weekdays : [1,2,3,4,5,6,0]);
  box.innerHTML = ADS_WEEK_NAMES.map(([l,v])=>`
    <button class="ads-week-b ${on.has(v)?'on':''}" data-v="${v}" onclick="adsToggleWeek(${v})">${l}</button>
  `).join('');
}
function adsToggleWeek(v){
  const set = new Set(Array.isArray(adsDraft.weekdays) ? adsDraft.weekdays : [1,2,3,4,5,6,0]);
  if(set.has(v)) set.delete(v); else set.add(v);
  if(set.size===0) set.add(v);   /* не даём выключить все дни */
  adsDraft.weekdays = [...set].sort((a,b)=>a-b);
  adsRenderWeek(); adsCalcForecast();
}

/* --- окно показа (часы) --- */
function adsHourInput(quiet){
  const a = document.getElementById('adsHourFrom'), b = document.getElementById('adsHourTo');
  if(!a || !b) return;
  let mn = parseInt(a.value,10), mx = parseInt(b.value,10);
  if(mn>mx-1){ mx = Math.min(24, mn+1); b.value = mx; }
  adsDraft.hourFrom = mn; adsDraft.hourTo = mx;
  const fill = document.getElementById('adsHourFill');
  if(fill){ const l = mn/24*100, r = mx/24*100; fill.style.left = l+'%'; fill.style.width = (r-l)+'%'; }
  const hint = document.getElementById('adsHourHint');
  if(hint) hint.textContent = (mn<10?'0'+mn:mn)+':00 – '+(mx<10?'0'+mx:mx)+':00';
  if(!quiet) adsCalcForecast();
}

/* --- бюджет: слайдер + инпут двусторонний --- */
function adsBudgetSliderInput(v){
  const val = Math.max(500, Math.min(100000, parseInt(v,10)||500));
  const inp = document.getElementById('adsInpBudget'); if(inp) inp.value = val;
  document.querySelectorAll('#adsStep4 .ads-chip').forEach(b=>b.classList.toggle('on', b.textContent.replace(/\s|₽/g,'')==String(val)));
  const hint = document.getElementById('adsBudgetHint');
  if(hint) hint.textContent = fmtN(val)+' ₽ в сутки';
  adsCalcForecast();
}
function adsBudgetInputSync(){
  const inp = document.getElementById('adsInpBudget'); if(!inp) return;
  const v = Math.max(0, parseInt(inp.value,10)||0);
  const sl = document.getElementById('adsBudgetSlider');
  if(sl && v>=500) sl.value = Math.min(100000, v);
  const hint = document.getElementById('adsBudgetHint');
  if(hint) hint.textContent = v ? fmtN(v)+' ₽ в сутки' : '₽ в сутки';
  adsCalcForecast();
}
function adsBudgetSliderSync(){
  const inp = document.getElementById('adsInpBudget');
  const sl = document.getElementById('adsBudgetSlider');
  if(inp && sl){
    const v = Math.max(500, Math.min(100000, parseInt(inp.value,10)||1000));
    sl.value = v;
    const hint = document.getElementById('adsBudgetHint');
    if(hint) hint.textContent = fmtN(v)+' ₽ в сутки';
  }
}

/* --- пикер моих постов ленты в шаге креатива --- */
function adsMyPosts(){
  if(typeof POSTS==='undefined' || !Array.isArray(POSTS.rec)) return [];
  return POSTS.rec.filter(p=>!p.promoted && !p.sponsored && p.name===PROFILE.name).slice(0,10);
}
function adsRenderPostPicker(){
  const box = document.getElementById('adsPostPicker'); if(!box) return;
  const posts = adsMyPosts();
  if(!posts.length){
    box.innerHTML = `<div class="ads-picker-empty">${I('photo')}Своих постов в ленте пока нет — заполни поля вручную</div>`;
    return;
  }
  box.innerHTML = posts.map(p=>{
    const body = (p.body||'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim();
    const title = body.slice(0, 44) || 'Пост из ленты';
    const sub = body.slice(44, 140);
    return `<button class="ads-picker-c" onclick="adsFillFromPost(${p.id})">
      <div class="ads-picker-t">${esc(title)}</div>
      <div class="ads-picker-s">${esc(sub||'нажми, чтобы взять текст в объявление')}</div>
      <div class="ads-picker-m">${I('eye')}${fmtN(p.views||0)}${I('bolt')}${fmtN(p.likes||0)}</div>
    </button>`;
  }).join('');
}
function adsFillFromPost(id){
  const p = adsMyPosts().find(x=>x.id===id); if(!p) return;
  const body = (p.body||'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim();
  const t = body.slice(0, 60);
  const rest = body.slice(60, 300).trim();
  const ti = document.getElementById('adsInpTitle'); if(ti && t){ ti.value = t; }
  const te = document.getElementById('adsInpText');  if(te){ te.value = rest || body.slice(0,300); }
  /* если есть медиа у поста — переносим (только src, тип уже известен) */
  if(p.media && (p.media.src || typeof p.media==='string')){
    adsRevokeDraftMedia();
    const src = p.media.src || p.media;
    adsDraft.media = {type: p.media.type || 'image', src, name:'Из ленты'};
    adsRenderMediaZone();
  }
  adsRenderPreview();
  toast('Поля заполнены из поста ленты — поправь под рекламу');
}

/* --- города --- */
function adsRenderCities(){
  const wrap = document.getElementById('adsCityWrap');
  const box = document.getElementById('adsCityChips');
  if(!wrap || !box) return;
  const list = ADS_CITIES[adsDraft.geo];
  if(!list){ wrap.style.display = 'none'; adsDraft.cities = []; return; }
  wrap.style.display = '';
  box.innerHTML = list.map(([name,r])=>`
    <button class="ads-city ${adsDraft.cities.includes(name)?'on':''}" data-v="${esc(name)}" onclick="adsToggleCity(this)">
      ${I('pin')}${esc(name)}<em>${r>=1?r.toFixed(1):Math.round(r*1000)+'к'}</em></button>`).join('');
  adsCityHint();
}
function adsToggleCity(btn){
  const v = btn.dataset.v;
  const i = adsDraft.cities.indexOf(v);
  if(i<0) adsDraft.cities.push(v); else adsDraft.cities.splice(i,1);
  btn.classList.toggle('on');
  adsCityHint(); adsCalcReach();
}
function adsCityHint(){
  const h = document.getElementById('adsCityHint'); if(!h) return;
  h.textContent = adsDraft.cities.length ? '· '+adsDraft.cities.length+' выбрано' : '· весь регион';
}

/* --- сохранённые аудитории (в шаге 3) --- */
function adsRenderSaved(){
  const row = document.getElementById('adsSavedRow'); if(!row) return;
  if(!ADS.auds.length){ row.innerHTML = ''; row.style.display = 'none'; return; }
  row.style.display = '';
  row.innerHTML = `<div class="ads-saved-h">${I('bookmark')}Сохранённые аудитории</div>
    <div class="ads-saved-chips">${ADS.auds.map(a=>`
      <button class="ads-saved-chip" onclick="adsApplyAud(${a.id})">${esc(a.name)}
        <i onclick="event.stopPropagation();adsDelAud(${a.id})">${I('trash')}</i></button>`).join('')}</div>`;
}
function adsApplyAud(id){
  const a = ADS.auds.find(x=>x.id===id); if(!a) return;
  adsDraft.sex = a.sex; adsDraft.geo = a.geo; adsDraft.cities = [...(a.cities||[])];
  adsDraft.interests = [...(a.interests||[])];
  adsDraft.devs = [...(a.devs||[])];
  if(a.ageMin!=null) adsDraft.ageMin = a.ageMin;
  if(a.ageMax!=null) adsDraft.ageMax = a.ageMax;
  document.querySelectorAll('#adsSexChips .ads-chip').forEach(b=>b.classList.toggle('on', b.dataset.v===a.sex));
  document.querySelectorAll('#adsGeoChips .ads-chip').forEach(b=>b.classList.toggle('on', b.dataset.v===a.geo));
  document.querySelectorAll('#adsDevChips .ads-chip').forEach(b=>b.classList.toggle('on', (a.devs||[]).includes(b.dataset.v)));
  const amn = document.getElementById('adsAgeMin'); if(amn && a.ageMin!=null) amn.value = a.ageMin;
  const amx = document.getElementById('adsAgeMax'); if(amx && a.ageMax!=null) amx.value = a.ageMax;
  adsRenderCities();
  adsRenderInterests();   /* перерисовываем интересы с активацией */
  adsAgeInput(true);
  adsCalcReach();
  toast('Аудитория «'+a.name+'» применена');
}
function adsDelAud(id){
  ADS.auds = ADS.auds.filter(x=>x.id!==id);
  adsSave(); adsRenderSaved(); adsRenderSummary();
  toast('Аудитория удалена');
}
function adsSaveAud(){
  const aud = {
    id: ADS.audSeq++, sex: adsDraft.sex, geo: adsDraft.geo, cities: [...adsDraft.cities],
    interests: adsPicked('adsIntChips'),
    ageMin: adsDraft.ageMin||18, ageMax: adsDraft.ageMax||45,
    devs: adsPicked('adsDevChips')
  };
  aud.name = adsAudName(aud);
  ADS.auds.unshift(aud);
  if(ADS.auds.length>8) ADS.auds.pop();
  adsSave(); adsRenderSaved(); adsRenderLal(); adsRenderSummary();
  toast('Аудитория сохранена');
}
function adsAudName(a){
  const p = [];
  p.push(a.sex==='m'?'муж':a.sex==='f'?'жен':'все');
  p.push(a.cities && a.cities.length ? a.cities[0]+(a.cities.length>1?' +'+(a.cities.length-1):'') : a.geo);
  if(a.interests && a.interests.length) p.push(a.interests[0]);
  return p.join(' · ');
}
function adsShowAuds(){
  if(!ADS.auds.length){ toast('Сохранённых аудиторий пока нет — создай при настройке кампании'); return; }
  showPopup({ico:'bookmark', title:'Сохранённые аудитории',
    body:`<div class="ads-aud-pop">${ADS.auds.map(a=>`
      <div class="ads-aud-pop-row">
        <div><b>${esc(a.name)}</b><small>${esc(adsAudLine(a))}</small></div>
        <button class="ads-chip" onclick="adsDelAud(${a.id});closePopup()">${I('trash')}</button>
      </div>`).join('')}</div>`,
    actions:[{label:'Закрыть'}]});
}

/* --- превью объявления (внутри iPhone-рамки) --- */
function adsRenderPreview(){
  const box = document.getElementById('adsPreview'); if(!box) return;
  const title = ((document.getElementById('adsInpTitle')||{}).value||'').trim() || 'Заголовок объявления';
  const text  = ((document.getElementById('adsInpText') ||{}).value||'').trim() || 'Текст объявления появится здесь по мере ввода.';
  const f = ADS_FORMATS[adsDraft.fmt] || ADS_FORMATS.post;
  box.innerHTML = adsPreviewCardHtml(title, text, adsDraft.cta || 'Подробнее', adsDraft.media, f.tag);
}

/* живой прогноз охвата */
let adsReachCur = 0, adsReachRaf = 0;
const ADS_REACH_MAX = 92000000;
function adsCalcReach(){
  /* adsDraft заводится в adsDraftReset() при открытии мастера, а до этого он
     пустой объект. Если сюда попасть раньше — падало на adsDraft.cities.length.
     Человек так пройти не может (шторка закрыта), но одна перестановка вызова
     превращает это в белый экран, поэтому проверяем явно. */
  if(!adsDraft || !Array.isArray(adsDraft.cities)){
    if(typeof adsDraftReset === 'function') adsDraftReset();
    if(!adsDraft || !Array.isArray(adsDraft.cities)) return;
  }
  const geoBase = {'РФ':11800000, 'СНГ':26400000, 'Весь мир':92000000}[adsDraft.geo] || 11800000;
  let base = geoBase;
  if(adsDraft.cities.length && ADS_CITIES[adsDraft.geo]){
    const map = Object.fromEntries(ADS_CITIES[adsDraft.geo]);
    base = adsDraft.cities.reduce((s,c)=>s+(map[c]||0)*1e6, 0);
  }
  const sexK = adsDraft.sex==='m' ? .49 : adsDraft.sex==='f' ? .51 : 1;
  const intShares = {
    'нейросети':.055,'бизнес':.09,'контент':.08,'маркетинг':.07,'дизайн':.06,'игры':.11,'крипта':.045,
    'образование':.10,'финансы':.06,'инвестиции':.045,'спорт':.09,'фитнес':.07,'здоровье':.08,
    'красота':.09,'мода':.09,'путешествия':.08,'авто':.07,'кулинария':.10,'рестораны':.06,
    'музыка':.11,'кино':.09,'книги':.05,'родители':.07,'недвижимость':.05
  };
  const devShares = {'ios':.28,'android':.66,'desktop':.3};
  const ints = adsPicked('adsIntChips') || [];
  adsDraft.interests = ints;
  const devs = adsPicked('adsDevChips') || [];
  const intK = ints.length ? Math.min(.4, ints.reduce((s,v)=>s+(intShares[v]||0.04),0)) : .5;
  /* возраст: линейно от диапазона возрастной пирамиды 14..65+ */
  const mn = adsDraft.ageMin||18, mx = adsDraft.ageMax||45;
  const ageK = Math.max(0.08, Math.min(1, (mx-mn+1)/(65-14+1)));
  const devK = devs.length ? Math.min(1, devs.reduce((s,v)=>s+(devShares[v]||0),0)) : 1;
  /* окно показа: доля суток → доля охвата */
  const hf = adsDraft.hourFrom||0, ht = adsDraft.hourTo||24;
  const hourK = Math.max(0.1, (ht-hf)/24);
  /* дни недели: доля активных суток */
  const wd = Array.isArray(adsDraft.weekdays) ? adsDraft.weekdays.length : 7;
  const wdK = Math.max(0.14, wd/7);
  /* look-alike расширяет доступный пул на 40% */
  const lalK = adsDraft.lal ? 1.4 : 1;
  const target = Math.max(0, Math.round(base * sexK * intK * ageK * devK * hourK * wdK * lalK));
  adsDraft.reach = target;
  const sub = document.getElementById('adsReachS');
  if(sub){
    const bits = [];
    bits.push(adsDraft.sex==='m'?'муж':adsDraft.sex==='f'?'жен':'любой пол');
    bits.push(mn+'–'+(mx>=65?'65+':mx)+' лет');
    bits.push(adsDraft.cities.length ? adsDraft.cities.length+' город(а)' : adsDraft.geo);
    bits.push(ints.length? ints.length+' интерес(а)' : 'широкие интересы');
    if(adsDraft.lal) bits.push('look-alike');
    sub.textContent = bits.join(' · ');
  }
  const bar = document.getElementById('adsReachBar');
  if(bar){ const pct = Math.max(2, Math.min(100, Math.log10(target+1)/Math.log10(ADS_REACH_MAX)*100)); bar.style.width = pct+'%'; }
  cancelAnimationFrame(adsReachRaf);
  const from = adsReachCur, t0 = performance.now();
  const step = now=>{
    const k = Math.min(1, (now-t0)/450), e = 1-Math.pow(1-k,3);
    adsReachCur = Math.round(from + (target-from)*e);
    const el = document.getElementById('adsReachV');
    if(el) el.textContent = fmtN(adsReachCur);
    if(k<1) adsReachRaf = requestAnimationFrame(step);
  };
  adsReachRaf = requestAnimationFrame(step);
}

/* --- модель оплаты / ставка --- */
function adsPickModel(btn){
  document.querySelectorAll('#adsModelChips .ads-model-b').forEach(b=>b.classList.remove('on'));
  btn.classList.add('on'); adsDraft.model = btn.dataset.v;
  adsSyncModelUI(); adsCalcForecast();
}
function adsRecBid(){
  const f = ADS_FORMATS[adsDraft.fmt];
  return adsDraft.model==='CPC' ? f.cpc : f.cpm;
}
function adsSyncModelUI(){
  document.querySelectorAll('#adsModelChips .ads-model-b').forEach(b=>b.classList.toggle('on', b.dataset.v===adsDraft.model));
  const rec = adsRecBid();
  const inp = document.getElementById('adsInpBid'); if(inp && (!inp.value || inp.dataset.model!==adsDraft.model)){ inp.value = rec; inp.dataset.model = adsDraft.model; }
  const hint = document.getElementById('adsBidHint'); if(hint) hint.textContent = '· '+ADS_MODELS[adsDraft.model].unit;
  const recL = document.getElementById('adsBidRec'); if(recL) recL.textContent = 'реком. '+rec+' ₽';
  adsBidBar();
}
function adsBidBar(){
  const rec = adsRecBid();
  const m = ADS_MODELS[adsDraft.model];
  const lo = m.min, hi = Math.round(rec*m.max);
  const v = Math.max(0, parseInt(document.getElementById('adsInpBid')?.value,10) || 0);
  const pct = Math.max(0, Math.min(100, (v-lo)/(hi-lo || 1)*100));
  const fill = document.getElementById('adsBidFill'); if(fill) fill.style.width = pct+'%';
  const recPct = Math.max(0, Math.min(100, (rec-lo)/(hi-lo || 1)*100));
  const mark = document.getElementById('adsBidMark'); if(mark) mark.style.left = recPct+'%';
}

function adsSetBudget(v){
  document.getElementById('adsInpBudget').value = v;
  document.querySelectorAll('#adsStep4 .ads-chip').forEach(b=>b.classList.toggle('on', b.textContent.replace(/\s|₽/g,'')==String(v)));
  adsCalcForecast();
}

function adsCalcForecast(){
  const b = Math.max(0, parseInt(document.getElementById('adsInpBudget').value,10) || 0);
  const bid = Math.max(1, parseInt(document.getElementById('adsInpBid').value,10) || adsRecBid());
  adsBidBar();
  const ctrEst = 0.022 + (bid/adsRecBid()-1)*0.006;   /* выше ставка → чуть выше приоритет/CTR */
  const disc = adsDisc();                             /* скидка тарифа → эффективная ставка ниже */
  const effBid = Math.max(1, bid*(1-disc));           /* за тот же бюджет — больше показов/кликов */
  let imps, cLo, cHi, cpm;
  if(adsDraft.model==='CPC'){
    const clicks = Math.round(b/effBid);
    imps = Math.round(clicks/Math.max(.008, ctrEst));
    cLo = Math.max(1, Math.round(clicks*.85)); cHi = Math.max(1, Math.round(clicks*1.15));
    cpm = imps ? b/imps*1000 : 0;
  }else{
    imps = Math.round(b/effBid*1000);
    cLo = Math.max(1, Math.round(imps*.01)); cHi = Math.max(1, Math.round(imps*.04));
    cpm = imps ? b/imps*1000 : effBid;
  }
  if(adsDraft.reach){ const cap = Math.round(adsDraft.reach*2.5); if(imps>cap){ imps = cap; } }
  const cpc = ((cLo+cHi)/2) ? b/((cLo+cHi)/2) : 0;
  const set = (id,v)=>{ const e = document.getElementById(id); if(e) e.innerHTML = v; };
  /* строка скидки тарифа + подсказка дневного лимита */
  const discRow = document.getElementById('adsFDiscRow');
  if(discRow){
    discRow.style.display = disc>0 ? '' : 'none';
    if(disc>0) set('adsFDisc', `−${Math.round(disc*100)}% · ${adsTierLabel()}`);
  }
  const paceHint = document.getElementById('adsPaceHint');
  if(paceHint) paceHint.textContent = adsDraft.pace>0 ? '· ≈ '+fmtN(Math.max(1,Math.round(b/adsDraft.pace)))+' ₽/день' : '· без ограничения скорости';
  set('adsFImps',  imps ? `~ ${fmtN(Math.round(imps*.85))} – ${fmtN(Math.round(imps*1.15))}` : '—');
  set('adsFClicks',imps ? `~ ${fmtN(cLo)} – ${fmtN(cHi)}` : '—');
  set('adsFCpc',   cpc ? `~ ${cpc<10 ? cpc.toFixed(1) : fmtN(Math.round(cpc))} ₽` : '—');
  set('adsFCpm',   cpm ? `~ ${cpm<10 ? cpm.toFixed(1) : fmtN(Math.round(cpm))} ₽` : '—');
  set('adsFCharge', b ? fmtMoney(b) : '—');
  const note = document.getElementById('adsBalanceNote');
  if(note) note.innerHTML = `На лицевом счёте: <b>${fmtMoney(WALLET.balance)}</b>${b>WALLET.balance?' — не хватает, пополни кошелёк':''}`;
}

function adsLaunch(){
  if(adsEditId) return adsSaveEdit();
  const name = document.getElementById('adsInpTitle').value.trim();
  const text = document.getElementById('adsInpText').value.trim();
  let link  = document.getElementById('adsInpLink').value.trim();
  const budget = Math.max(0, parseInt(document.getElementById('adsInpBudget').value,10) || 0);
  const bid = Math.max(1, parseInt(document.getElementById('adsInpBid').value,10) || adsRecBid());
  if(!name || !text){ adsStep(2); return; }
  if(budget < 100){ toast('Минимальный бюджет — 100 ₽'); return; }
  if(link && !/^https?:\/\//i.test(link)) link = 'https://'+link;
  /* запланированный старт: дата/время из поля */
  let startTs = 0, sched = false;
  if(adsDraft.start==='sched'){
    const raw = (document.getElementById('adsSchedAt')||{}).value;
    const t = raw ? new Date(raw).getTime() : 0;
    if(t && t > Date.now()+3e4){ startTs = t; sched = true; }
  }
  if(!walletCharge(budget, 'Реклама: '+name)) return;
  okoEarn(budget, 'Рекламный кабинет');
  adsBill(budget, name, ADS_FORMATS[adsDraft.fmt].l);
  const ctrEst = 0.022 + (bid/adsRecBid()-1)*0.006;
  const camp = {
    id: ADS.seq++, name, text, cta: adsDraft.cta, link: link || '',
    goal: adsDraft.goal || 'sales',
    fmt: adsDraft.fmt, model: adsDraft.model, bid, ctrEst: Math.max(.008, ctrEst),
    cvr: +(0.16 + Math.random()*0.18).toFixed(3),
    aov: (ADS_FORMATS[adsDraft.fmt] && ADS_FORMATS[adsDraft.fmt].aov) || 690,
    salesRate: +(0.006 + Math.random()*0.007).toFixed(4),
    disc: adsDisc(), pace: adsDraft.pace||0, dailyCap: adsDraft.pace>0 ? Math.max(1,Math.round(budget/adsDraft.pace)) : 0,
    spentToday: 0, spentDay: adsDayKey(),
    sex: adsDraft.sex, geo: adsDraft.geo, cities: [...adsDraft.cities],
    interests: adsPicked('adsIntChips'),
    ageMin: adsDraft.ageMin||18, ageMax: adsDraft.ageMax||45, ages: [],
    devs: adsPicked('adsDevChips'), times: [],
    weekdays: Array.isArray(adsDraft.weekdays) ? [...adsDraft.weekdays] : [1,2,3,4,5,6,0],
    hourFrom: adsDraft.hourFrom||0, hourTo: adsDraft.hourTo||24,
    lal: !!adsDraft.lal, lalFrom: adsDraft.lalFrom||0,
    media: adsDraft.media ? Object.assign({}, adsDraft.media) : null,
    budget, spent: 0, imps: 0, clicks: 0, status: sched ? 'sched' : 'mod', startTs,
    hist: [], created: Date.now(),
    days: [...Array(7)].map(()=>({i:0,c:0})), dstamp: adsDayKey()
  };
  adsDraft.media = null;   /* креатив передан кампании — не отзываем blob-URL видео */
  const tb = adsDraft.ab ? document.getElementById('adsInpTitleB').value.trim() : '';
  if(tb) camp.ab = {tb, ka: .012+Math.random()*.025, kb: .012+Math.random()*.025, a:{i:0,c:0}, b:{i:0,c:0}};
  ADS.camps.unshift(camp);
  adsSave(); closeSheet(); adsRender();
  if(sched){
    toast('Кампания запланирована на '+new Date(startTs).toLocaleString('ru-RU',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'}));
  }else{
    toast('Кампания отправлена на модерацию OKO');
    setTimeout(()=>adsModerate(camp.id), 3000);
  }
}

/* ---------- сохранение изменений существующей кампании ---------- */
function adsSaveEdit(){
  const c = ADS.camps.find(x=>x.id===adsEditId); if(!c){ adsEditId=0; closeSheet(); return; }
  const name = document.getElementById('adsInpTitle').value.trim();
  const text = document.getElementById('adsInpText').value.trim();
  let link  = document.getElementById('adsInpLink').value.trim();
  let budget = Math.max(0, parseInt(document.getElementById('adsInpBudget').value,10) || 0);
  const bid = Math.max(1, parseInt(document.getElementById('adsInpBid').value,10) || adsRecBid());
  if(!name || !text){ adsStep(2); return; }
  if(budget < 100){ toast('Минимальный бюджет — 100 ₽'); return; }
  if(link && !/^https?:\/\//i.test(link)) link = 'https://'+link;
  /* бюджет не может быть ниже уже потраченного; разницу списываем/возвращаем на счёт */
  if(budget < c.spent) budget = Math.ceil(c.spent);
  const d = budget - c.budget;
  if(d > 0){ if(!walletCharge(d, 'Изменение бюджета: '+name)) return; okoEarn(d,'Рекламный кабинет'); adsBill(d, name, 'Пополнение бюджета'); }
  else if(d < 0){ walletAdd(-d, 'Возврат: бюджет кампании уменьшен — '+name); adsBill(d, name, 'Возврат бюджета'); }
  /* применяем поля креатива и таргетинга */
  c.name = name; c.text = text; c.cta = adsDraft.cta; c.link = link || '';
  c.goal = adsDraft.goal || c.goal || 'sales';
  c.fmt = adsDraft.fmt; c.model = adsDraft.model; c.bid = bid;
  c.aov = (ADS_FORMATS[adsDraft.fmt] && ADS_FORMATS[adsDraft.fmt].aov) || c.aov || 690;
  c.ctrEst = Math.max(.008, 0.022 + (bid/adsRecBid()-1)*0.006);
  c.sex = adsDraft.sex; c.geo = adsDraft.geo; c.cities = [...adsDraft.cities];
  c.interests = adsPicked('adsIntChips');
  c.ageMin = adsDraft.ageMin||18; c.ageMax = adsDraft.ageMax||45; c.ages = [];
  c.devs = adsPicked('adsDevChips'); c.times = [];
  c.weekdays = Array.isArray(adsDraft.weekdays) ? [...adsDraft.weekdays] : [1,2,3,4,5,6,0];
  c.hourFrom = adsDraft.hourFrom||0; c.hourTo = adsDraft.hourTo||24;
  c.lal = !!adsDraft.lal; c.lalFrom = adsDraft.lalFrom||0;
  c.budget = budget;
  c.pace = adsDraft.pace||0; c.dailyCap = c.pace>0 ? Math.max(1,Math.round(budget/c.pace)) : 0;
  if(adsDraft.media !== c.media){
    if(c.media && c.media._obj && c.media.src){ try{ URL.revokeObjectURL(c.media.src); }catch(e){} }
    c.media = adsDraft.media ? Object.assign({}, adsDraft.media) : null;
    adsDraft.media = null;
  }
  const tb = adsDraft.ab ? (document.getElementById('adsInpTitleB').value.trim()) : '';
  if(tb && c.ab){ c.ab.tb = tb; }
  else if(tb && !c.ab){ c.ab = {tb, ka:.012+Math.random()*.025, kb:.012+Math.random()*.025, a:{i:0,c:0}, b:{i:0,c:0}}; }
  else if(!adsDraft.ab){ c.ab = null; }
  /* если завершённой кампании подняли бюджет — снова в открутку */
  if(c.status==='done' && c.spent < c.budget){ c.status = 'act'; adsPushToFeed(c); }
  adsSyncFeedPost(c);
  adsEditId = 0;
  adsSave(); closeSheet(); adsRender();
  toast('Кампания обновлена');
}

/* ---------- ИИ-модерация ---------- */
function adsModerate(id){
  const c = ADS.camps.find(x=>x.id===id);
  if(!c || c.status!=='mod') return;
  const hay = (c.name+' '+c.text).toLowerCase();
  const bad = ADS_STOPWORDS.find(w=>hay.includes(w));
  if(bad){
    c.status = 'rej'; c.reason = 'запрещённая тематика («'+bad+'»)';
    walletAdd(c.budget, 'Возврат: реклама отклонена — '+c.name);
    adsBill(-c.budget, c.name, 'Возврат (отклонено модерацией)');
    showPopup({ico:'flag', title:'Реклама отклонена',
      body:`Модератор OKO нашёл в объявлении «${esc(c.name)}» запрещённую тематику: <b>«${esc(bad)}»</b>.<br><br>Бюджет ${fmtMoney(c.budget)} уже вернулся на лицевой счёт. Исправь текст и запусти заново.`,
      actions:[{label:'Понятно'}]});
  }else{
    c.status = 'act';
    adsPushToFeed(c);
    toast('Кампания «'+c.name+'» прошла модерацию — объявление в ленте');
  }
  adsSave(); adsRender();
}

/* ---------- биллинг ---------- */
function adsBill(sum, camp, type){
  ADS.bill.unshift({ts: Date.now(), sum, camp, type});
  if(ADS.bill.length>60) ADS.bill.pop();
}
function adsOpenBilling(){
  adsRenderBilling();
  openSheet('ads-billing');
}
function adsRenderBilling(){
  const totalSpent = ADS.bill.filter(b=>b.sum>0).reduce((s,b)=>s+b.sum,0);
  const totalBack  = -ADS.bill.filter(b=>b.sum<0).reduce((s,b)=>s+b.sum,0);
  const sum = document.getElementById('adsBillSum');
  if(sum) sum.innerHTML = `
    <div class="ads-bill-stat"><div class="v">${fmtN(Math.round(totalSpent))}<small>₽</small></div><div class="l">вложено в рекламу</div></div>
    <div class="ads-bill-stat"><div class="v">${fmtN(Math.round(totalBack))}<small>₽</small></div><div class="l">возвращено</div></div>`;
  const list = document.getElementById('adsBillList');
  if(!list) return;
  if(!ADS.bill.length){ list.innerHTML = `<div class="ads-bill-empty">${I('card')}Операций пока нет. Запусти кампанию — списания появятся здесь.</div>`; return; }
  list.innerHTML = ADS.bill.map(b=>{
    const d = new Date(b.ts);
    const dt = d.toLocaleDateString('ru-RU',{day:'2-digit',month:'short'})+' '+d.toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'});
    const back = b.sum<0;
    return `<div class="ads-bill-row">
      <span class="ads-bill-ic ${back?'back':''}">${I(back?'plus':'card')}</span>
      <div class="ads-bill-mid"><b>${esc(b.camp)}</b><small>${esc(b.type)} · ${dt}</small></div>
      <span class="ads-bill-sum ${back?'back':''}">${back?'+':'−'}${fmtN(Math.abs(Math.round(b.sum)))} ₽</span>
    </div>`;
  }).join('');
}

/* ---------- пост в ленте ---------- */
function adsPostId(id){ return ADS_POST_BASE + id; }
function adsPushToFeed(c){
  const pid = adsPostId(c.id);
  if(POSTS.rec.some(p=>p.id===pid)) return;
  const tag = (ADS_FORMATS[c.fmt]||ADS_FORMATS.post).tag;
  POSTS.rec.unshift({
    id: pid, ava: (PROFILE.name[0]||'O').toUpperCase(), name: PROFILE.name, sub: tag,
    body: `<b>${esc(c.name)}</b><br>${esc(c.text)}${adsMediaHtml(c.media, 'ads-post-media')}<div class="ads-post-cta"><button onclick="event.stopPropagation();adsCtaClick(${c.id})">${esc(c.cta)} ${I('chev')}</button></div>`,
    media: null, likes: 0, views: c.imps, liked: false, saved: false, reposts: 0,
    promoted: true, comments: []
  });
  const feedOn = document.getElementById('screen-feed');
  if(feedOn && feedOn.classList.contains('active') && typeof renderFeed==='function') renderFeed(curFeedKind);
}
function adsPullFromFeed(c){
  const pid = adsPostId(c.id);
  const before = POSTS.rec.length;
  POSTS.rec = POSTS.rec.filter(p=>p.id!==pid);
  const feedOn = document.getElementById('screen-feed');
  if(POSTS.rec.length!==before && feedOn && feedOn.classList.contains('active')) renderFeed(curFeedKind);
}
function adsCtaClick(id){
  const c = ADS.camps.find(x=>x.id===id);
  if(!c || c.status!=='act'){ toast('Объявление не активно'); return; }
  c.clicks++;
  adsRollDays(c); c.days[6].c++;
  if(c.ab){
    const tot = c.ab.a.i + c.ab.b.i;
    (tot && Math.random() < c.ab.a.i/tot ? c.ab.a : c.ab.b).c++;
  }
  adsSave();
  toast('Клик засчитан'+(c.link?' — переход: '+c.link.replace(/^https?:\/\//,'').slice(0,34):''));
  if(document.getElementById('screen-ads').classList.contains('active')) adsTickUI();
}

/* ---------- управление кампаниями ---------- */
function adsPause(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c || c.status!=='act') return;
  c.status = 'pause'; adsPullFromFeed(c);
  adsSave(); adsRender(); toast('Кампания на паузе — показы остановлены');
}
function adsResume(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c || c.status!=='pause') return;
  if(c.spent >= c.budget){ c.status = 'done'; adsSave(); adsRender(); toast('Бюджет уже исчерпан'); return; }
  c.status = 'act'; adsPushToFeed(c);
  adsSave(); adsRender(); toast('Кампания снова активна');
}
function adsStop(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c) return;
  showPopup({ico:'flag', title:'Остановить кампанию?',
    body:`«${esc(c.name)}» будет завершена, объявление уйдёт из ленты. Потрачено ${fmtMoney(c.spent)} из ${fmtMoney(c.budget)}.`,
    actions:[
      {label:'Остановить', onclick:()=>{
        c.status = 'done'; adsPullFromFeed(c);
        adsSave(); adsRender(); toast('Кампания завершена');
      }},
      {label:'Отмена', ghost:true}
    ]});
}
function adsDelete(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c) return;
  adsPullFromFeed(c);
  ADS.camps = ADS.camps.filter(x=>x.id!==id);
  adsSave(); adsRender(); toast('Кампания удалена');
}

/* ---------- дублирование: открываем мастер, прекопировав все настройки ---------- */
function adsPrefillFromCamp(c){
  adsRevokeDraftMedia();
  adsDraft = {
    goal: ADS_GOALS[c.goal] ? c.goal : ({post:'views',channel:'subs',listing:'sales',banner:'installs'}[c.fmt]||'sales'),
    fmt: ADS_FORMATS[c.fmt] ? c.fmt : 'post',
    model: c.model || (ADS_FORMATS[c.fmt]||ADS_FORMATS.post).model,
    cta: c.cta || 'Подробнее',
    sex: c.sex || 'any',
    geo: (ADS_CITIES[c.geo] || c.geo==='Весь мир') ? c.geo : 'РФ',
    cities: Array.isArray(c.cities) ? [...c.cities] : [],
    interests: Array.isArray(c.interests) ? [...c.interests] : [],
    devs: Array.isArray(c.devs) ? [...c.devs] : [],
    ageMin: c.ageMin||18, ageMax: c.ageMax||45,
    lal: !!c.lal, lalFrom: c.lalFrom||0,
    weekdays: Array.isArray(c.weekdays) ? [...c.weekdays] : [1,2,3,4,5,6,0],
    hourFrom: c.hourFrom||0, hourTo: c.hourTo||24,
    ab: !!c.ab, reach: 0,
    pace: c.pace||0, start: 'now', startAt: 0,
    media: c.media ? Object.assign({}, c.media) : null
  };
  document.querySelectorAll('#adsPaceChips .ads-chip').forEach(b=>b.classList.toggle('on', (parseInt(b.dataset.v,10)||0)===(c.pace||0)));
  document.querySelectorAll('#adsStartChips .ads-chip').forEach((b,i)=>b.classList.toggle('on', i===0));
  const sa = document.getElementById('adsSchedAt'); if(sa){ sa.style.display='none'; sa.value=''; }
  const setVal = (id,v)=>{ const e = document.getElementById(id); if(e) e.value = v==null?'':v; };
  setVal('adsInpTitle', c.name);
  setVal('adsInpText', c.text);
  setVal('adsInpLink', c.link);
  setVal('adsInpTitleB', c.ab ? c.ab.tb : '');
  /* A/B чип и поле варианта B */
  const abChip = document.getElementById('adsAbChip'); if(abChip) abChip.classList.toggle('on', !!c.ab);
  const abInp = document.getElementById('adsInpTitleB'); if(abInp) abInp.style.display = c.ab ? '' : 'none';
  /* одиночные наборы */
  const single = (box,val)=>document.querySelectorAll('#'+box+' .ads-chip').forEach(b=>b.classList.toggle('on', b.dataset.v===val));
  single('adsCtaChips', adsDraft.cta);
  single('adsSexChips', adsDraft.sex);
  single('adsGeoChips', adsDraft.geo);
  /* множественные наборы */
  const multi = (box,arr)=>document.querySelectorAll('#'+box+' .ads-chip').forEach(b=>b.classList.toggle('on', (arr||[]).includes(b.dataset.v)));
  multi('adsDevChips',  c.devs);
  /* возраст, часы, недели, слайдер бюджета */
  const amn = document.getElementById('adsAgeMin'); if(amn) amn.value = adsDraft.ageMin;
  const amx = document.getElementById('adsAgeMax'); if(amx) amx.value = adsDraft.ageMax;
  const hf  = document.getElementById('adsHourFrom'); if(hf) hf.value = adsDraft.hourFrom;
  const ht  = document.getElementById('adsHourTo');   if(ht) ht.value = adsDraft.hourTo;
  const bs  = document.getElementById('adsBudgetSlider');
  if(bs) bs.value = Math.max(500, Math.min(100000, c.budget||1000));
  /* модель, ставка, бюджет — прописываем так, чтобы adsSyncModelUI не перетёр */
  const bidInp = document.getElementById('adsInpBid');
  if(bidInp){ bidInp.value = c.bid || adsRecBid(); bidInp.dataset.model = adsDraft.model; }
  const budInp = document.getElementById('adsInpBudget'); if(budInp) budInp.value = c.budget || 1000;
  document.querySelectorAll('#adsStep4 .ads-chip').forEach(b=>b.classList.toggle('on', b.textContent.replace(/\s|₽/g,'')===String(c.budget)));
  document.querySelectorAll('#adsModelChips .ads-model-b').forEach(b=>b.classList.toggle('on', b.dataset.v===adsDraft.model));
  /* рендеры зависимых блоков */
  adsRenderGoals();
  adsRenderFmts();
  adsRenderCities();
  adsRenderSaved();
  adsRenderInterests();
  adsRenderLal();
  adsRenderWeek();
  adsRenderMediaZone();
  adsRenderPostPicker();
  adsAgeInput(true); adsHourInput(true);
  adsSyncModelUI();
}
function adsDuplicate(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c) return;
  adsEditId = 0;
  const st = document.getElementById('adsSheetTitle'); if(st) st.textContent = 'Новая кампания';
  adsPrefillFromCamp(c);
  adsSyncLaunchBtn();
  adsStep(1);
  openSheet('ads-create');
  toast('Копия «'+c.name+'» — проверьте и запустите');
}

/* ---------- редактирование существующей кампании (тот же мастер) ---------- */
function adsEdit(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c) return;
  adsEditId = id;
  const st = document.getElementById('adsSheetTitle'); if(st) st.textContent = 'Редактирование';
  adsPrefillFromCamp(c);
  adsSyncLaunchBtn();
  adsStep(3);   /* сразу к креативу — цель/аудитория/бюджет уже проставлены */
  openSheet('ads-create');
  toast('Редактирование «'+c.name+'»');
}

/* обновить пост объявления в ленте после правок (если он там есть) */
function adsSyncFeedPost(c){
  const pid = adsPostId(c.id);
  const post = POSTS.rec.find(p=>p.id===pid);
  if(!post) return;
  const tag = (ADS_FORMATS[c.fmt]||ADS_FORMATS.post).tag;
  post.sub = tag;
  post.body = `<b>${esc(c.name)}</b><br>${esc(c.text)}${adsMediaHtml(c.media, 'ads-post-media')}<div class="ads-post-cta"><button onclick="event.stopPropagation();adsCtaClick(${c.id})">${esc(c.cta)} ${I('chev')}</button></div>`;
  const feedOn = document.getElementById('screen-feed');
  if(feedOn && feedOn.classList.contains('active') && typeof renderFeed==='function') renderFeed(curFeedKind);
}

/* ---------- запланированные кампании ---------- */
function adsStartNow(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c || c.status!=='sched') return;
  c.status = 'mod'; c.startTs = 0;
  adsSave(); adsRender();
  toast('Кампания «'+c.name+'» отправлена на модерацию');
  setTimeout(()=>adsModerate(id), 2000);
}
function adsCancelSched(id){
  const c = ADS.camps.find(x=>x.id===id); if(!c || c.status!=='sched') return;
  showPopup({ico:'flag', title:'Отменить запуск?',
    body:`«${esc(c.name)}» ещё не стартовала. Бюджет ${fmtMoney(c.budget)} вернётся на лицевой счёт.`,
    actions:[
      {label:'Отменить кампанию', onclick:()=>{
        walletAdd(c.budget, 'Возврат: запуск рекламы отменён — '+c.name);
        adsBill(-c.budget, c.name, 'Возврат (отмена запуска)');
        ADS.camps = ADS.camps.filter(x=>x.id!==id);
        adsSave(); adsRender(); toast('Запуск отменён, бюджет возвращён');
      }},
      {label:'Оставить', ghost:true}
    ]});
}

/* ---------- живая статистика: тикер раз в 5 секунд ---------- */
function adsTick(){
  let changed = false;
  /* запланированные кампании: наступило время старта — на модерацию, затем в ленту */
  const due = ADS.camps.filter(c=>c.status==='sched' && c.startTs && Date.now()>=c.startTs);
  due.forEach(c=>{ c.status='mod'; c.startTs=0; adsModerate(c.id); });
  if(due.length){ changed = true; adsSave(); if(document.getElementById('screen-ads').classList.contains('active')) adsRender(); }
  ADS.camps.forEach(c=>{
    if(c.status!=='act') return;
    /* дневной лимит бюджета (равномерная открутка): сброс на новых сутках */
    const dk = adsDayKey();
    if(c.spentDay!==dk){ c.spentDay = dk; c.spentToday = 0; }
    if(c.dailyCap>0 && c.spentToday>=c.dailyCap) return;   /* на сегодня открутка исчерпана */
    /* окно расписания: дни недели + часы (если заданы) */
    const now = new Date();
    const wd = Array.isArray(c.weekdays) ? c.weekdays : [1,2,3,4,5,6,0];
    if(!wd.includes(now.getDay())) return;
    const h = now.getHours();
    const hf = c.hourFrom||0, ht = c.hourTo||24;
    if(h < hf || h >= ht) return;
    changed = true;
    const dImps = 70 + Math.floor(Math.random()*180);
    let dClicks;
    if(c.ab){
      const ia = Math.round(dImps*(.45+Math.random()*.1)), ib = dImps-ia;
      const ca = Math.round(ia*c.ab.ka*(.7+Math.random()*.6));
      const cb = Math.round(ib*c.ab.kb*(.7+Math.random()*.6));
      c.ab.a.i += ia; c.ab.a.c += ca; c.ab.b.i += ib; c.ab.b.c += cb;
      dClicks = ca + cb;
    }else{
      const ctr = c.ctrEst || 0.022;
      dClicks = Math.round(dImps*(ctr*(.6+Math.random()*.9)));
    }
    const gross = c.model==='CPC' ? dClicks*c.bid : dImps/1000*adsCampCpm(c);
    const dSpend = gross * (1 - (c.disc||0));   /* скидка тарифа: тот же охват — дешевле */
    c.imps += dImps; c.clicks += dClicks;
    c.spent = Math.min(c.budget, c.spent + dSpend);
    c.spentToday = (c.spentToday||0) + dSpend;
    c.hist.push(dImps); if(c.hist.length>24) c.hist.shift();
    adsRollDays(c); c.days[6].i += dImps; c.days[6].c += dClicks;
    const post = POSTS.rec.find(p=>p.id===adsPostId(c.id));
    if(post) post.views = c.imps;
    if(c.spent >= c.budget){
      c.status = 'done'; adsPullFromFeed(c);
      toast('Кампания «'+c.name+'» завершена — бюджет исчерпан');
      if(document.getElementById('screen-ads').classList.contains('active')) adsRender();
    }
  });
  if(!changed) return;
  adsSave();
  if(document.getElementById('screen-ads').classList.contains('active')) adsTickUI();
}

/* ---------- патчи ядра ---------- */
const _prevShowTabAds = showTab;
showTab = function(t){
  _prevShowTabAds(t);
  if(t==='ads'){ adsSummaryReset(); adsRender(); }
};

/* быстрый буст поста из feed-post меню: перехватываем базовый openPromotePost и
   отправляем в модалку рекламного кабинета — с чипами 100/300/500/1000/2000 ₽ в сутки */
if(typeof window.openPromotePost === 'function'){
  const _prevOpenPromotePost = window.openPromotePost;
  window.openPromotePost = function(id){
    try{ adsBoostFromFeed(id); }
    catch(e){ _prevOpenPromotePost(id); }
  };
}

/* ---------- самоинициализация ---------- */
adsLoad();
adsMigrate();
regTitle('ads', 'Реклама');
addSvcTile({id:'ads', label:'Реклама', ico:'megaphone', onclick:()=>showTab('ads')});
if(typeof regT==='function') regT({ads:{ru:'Реклама', en:'Ads'}});
ADS.camps.forEach(c=>{
  if(c.status==='act') adsPushToFeed(c);
  if(c.status==='mod') setTimeout(()=>adsModerate(c.id), 3000);
});
adsRender();
setInterval(function(){ (window.adsTick||adsTick)(); }, 5000);  /* поздний биндинг: слой oko-mini2.js подменяет adsTick */
window.addEventListener('resize', ()=>{
  if(document.getElementById('screen-ads').classList.contains('active'))
    ADS.camps.forEach(c=>{ adsDrawSpark(c); if(adsOpenStats.has(c.id)) adsDrawDays(c); });
});
new MutationObserver(()=>{
  if(document.getElementById('screen-ads').classList.contains('active'))
    ADS.camps.forEach(c=>{ if(adsOpenStats.has(c.id)) adsDrawDays(c); });
}).observe(document.documentElement, {attributes:true, attributeFilter:['data-theme']});

/* ===== module: legal ===== */
/* ================= LEGAL: юридические документы OKO (префикс lg-) =================
   Оферта / Политика конфиденциальности / Пользовательское соглашение / Согласие на ПД /
   Политика возврата / Лицензионное соглашение — RU + EN.
   Публичное API:
     openLegalHub()               — открыть главный экран Legal (карточки-документы)
     openLegalDoc(kind)           — открыть конкретный документ (kind ∈ LG_DOCS)
     openLegalDeal({...})         — открыть авто-заполненный договор-акт по сделке
     closeLegalDoc()              — закрыть вьюху Legal целиком
   Печать (sealSvg) и подпись (signatureImg) — из core-ext. */

/* ---------- состояние (localStorage oko-legal) ---------- */
const lgS = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-legal'))||null; }catch(e){ return null; } })() || { lang: (typeof LANG!=='undefined'?LANG:'ru') };
function lgSave(){ try{ localStorage.setItem('oko-legal', JSON.stringify(lgS)); }catch(e){} }
let lgKind = null;         // текущий открытый документ; null → показан hub
let lgDealCtx = null;      // контекст авто-договора по сделке (для kind === 'deal')

const LG_TABS = [
  {k:'offer',   ru:'Оферта',            en:'Offer'},
  {k:'privacy', ru:'Конфиденциальность',en:'Privacy'},
  {k:'terms',   ru:'Соглашение',        en:'Terms'},
  {k:'refund',  ru:'Возврат',           en:'Refund'},
  {k:'license', ru:'Лицензия ПО',       en:'Software licence'},
  {k:'consent', ru:'Согласие на ПД',    en:'Data consent'}
];

/* карточки на hub — иконка + короткое описание */
const LG_HUB_CARDS = [
  {k:'offer',   ico:'money',   ru:{t:'Публичная оферта',                s:'Договор возмездных услуг сервиса OKO'},                 en:{t:'Public Offer',                  s:'Paid-services agreement for the OKO service'}},
  {k:'privacy', ico:'lock',    ru:{t:'Политика конфиденциальности',      s:'Обработка и защита персональных данных'},               en:{t:'Privacy Policy',                s:'Processing and protection of personal data'}},
  {k:'terms',   ru:{t:'Пользовательское соглашение',                    s:'Правила использования платформы'},                       en:{t:'Terms of Service',              s:'Rules of using the platform'},                        ico:'file'},
  {k:'refund',  ru:{t:'Политика возврата',                              s:'Порядок возврата подписок и внутренних платежей'},       en:{t:'Refund Policy',                 s:'Refunds for subscriptions and in-app charges'},        ico:'card'},
  {k:'license', ru:{t:'Лицензия на ПО',                                 s:'Условия использования приложения OKO'},                  en:{t:'Software Licence',              s:'Terms of use for the OKO application'},                ico:'bolt'},
  {k:'consent', ru:{t:'Согласие на обработку ПД',                       s:'152-ФЗ: цели, сроки, отзыв согласия'},                   en:{t:'Personal-data Consent',         s:'FZ-152: purposes, terms, withdrawal'},                 ico:'check'}
];

/* ---------- общие реквизиты ---------- */
const LG_REQ = {
  ru: {
    op: 'Индивидуальный предприниматель Ильясов Даниэль Альбертович',
    brand: 'Проект «ОКО PROJECT»',
    inn: 'ИНН 682016634349',
    geo: 'г. Москва, Российская Федерация · представительство: г. Дубай, ОАЭ',
    mail: 'okoteam.top@gmail.com',
    sig: 'Ильясов Д.А. / Индивидуальный предприниматель',
    date: '21 июля 2026 г.'
  },
  en: {
    op: 'Sole Proprietor Ilyasov Daniel Albertovich',
    brand: '"OKO PROJECT"',
    inn: 'Taxpayer ID (INN) 682016634349',
    geo: 'Moscow, Russian Federation · representative office: Dubai, UAE',
    mail: 'okoteam.top@gmail.com',
    sig: 'D. A. Ilyasov / Sole Proprietor',
    date: 'July 21, 2026'
  }
};

/* ================================================================
   ТЕКСТЫ ДОКУМЕНТОВ. secs: массив {h, b} — заголовок и HTML-тело.
   ================================================================ */
const LG_DOCS = {

/* ============================ ОФЕРТА ============================ */
offer: {
ru: { title:'Публичная оферта', rev:'Редакция № 4', secs:[
{h:'Общие положения и акцепт', b:
`<p>Настоящий документ является официальным предложением (публичной офертой) Индивидуального предпринимателя Ильясова Даниэля Альбертовича (ИНН 682016634349, г. Москва, Российская Федерация; представительство: г. Дубай, ОАЭ), далее — <b>«Оператор»</b>, заключить договор возмездного оказания услуг сервиса OKO на изложенных ниже условиях в соответствии со ст. 435, 437 и 438 Гражданского кодекса РФ.</p>
<p>Полным и безоговорочным акцептом оферты признаётся любое из действий: регистрация аккаунта в приложении OKO, оплата тарифа, пополнение лицевого счёта либо фактическое использование любой функции Сервиса. С момента акцепта договор считается заключённым.</p>`},
{h:'Термины', b:
`<ul>
<li><b>Сервис (OKO)</b> — программный комплекс Оператора: мобильное и веб-приложение OKO, включая мессенджер, социальную ленту, биржу услуг, академию, игровой раздел, рекламный кабинет, партнёрскую программу и лицевой счёт.</li>
<li><b>Пользователь</b> — дееспособное физическое лицо либо представитель юридического лица, акцептовавшие оферту.</li>
<li><b>Лицевой счёт</b> — внутренний учётный счёт Пользователя в Сервисе, отражающий баланс средств для оплаты услуг.</li>
<li><b>Биржа</b> — раздел Сервиса для заключения сделок между заказчиками и исполнителями услуг.</li>
<li><b>Эскроу</b> — механика блокировки средств заказчика до подтверждения приёмки работы.</li>
<li><b>Тариф</b> — объём платного функционала (START, PRO, BUSINESS) на выбранный период.</li>
</ul>`},
{h:'Предмет договора', b:
`<p>Оператор предоставляет Пользователю право использования Сервиса в пределах его функциональных возможностей: обмен сообщениями и звонки, публикация контента в социальной ленте, размещение и заказ услуг на Бирже, обучение в Академии, участие в играх (18+), размещение рекламы через рекламный кабинет, участие в партнёрской программе и операции по лицевому счёту.</p>
<p>Базовый функционал предоставляется бесплатно. Расширенный функционал предоставляется по подписке согласно выбранному Тарифу.</p>`},
{h:'Тарифы и порядок оплаты', b:
`<div class="lg-table-wrap"><table class="lg-table">
<tr><th>Тариф</th><th>Цена / мес*</th><th>Состав</th></tr>
<tr><td>START</td><td>990&nbsp;₽</td><td>Мессенджер Premium (файлы до 4 ГБ, транскрипция голосовых), магазин шаблонов, каталог трендов, 30 проверок видео в месяц, активация партнёрской программы.</td></tr>
<tr><td>PRO</td><td>4&nbsp;900&nbsp;₽</td><td>Персональная система роста, ОКО Ai (300 обращений), студия контента (100 генераций), 100 проверок видео и 20 автоправок в месяц, аналитика 3 каналов.</td></tr>
<tr><td>BUSINESS</td><td>19&nbsp;900&nbsp;₽</td><td>Контент-производство 30–50 роликов в месяц, команда специалистов OKO под проект, автопостинг во все привязанные соцсети, приоритетная поддержка, командные аккаунты (до 3).</td></tr>
<tr><td>BUSINESS&nbsp;PRO</td><td>49&nbsp;900&nbsp;₽</td><td>Контент-производство 100 роликов в месяц, персональный образ (двойник голоса и лица), безлимитные ОКО Ai и студия, API-доступ, до 5 командных аккаунтов, бонус: лендинг и бот при годовой оплате.</td></tr>
<tr><td>MAX</td><td>149&nbsp;900&nbsp;₽</td><td>Контент-производство 300 роликов в месяц, полная команда специалистов OKO с персональным менеджером, полный digital-запуск (сайт, бот, автоматизации) при годовой оплате, до 15 командных аккаунтов, white-label.</td></tr>
</table></div>
<p class="lg-note">* Указана цена за месяц при оплате за год. Скидки периодов от базовой месячной цены: 3 месяца — 10%, 6 месяцев — 15%, 12 месяцев — 20%.</p>
<p>Способы оплаты: банковские карты РФ, криптовалюта, платёжная платформа Lava.top. Цены могут указываться в долларах США; фактическое списание производится в валюте платёжного метода по курсу на дату платежа. Подписка продлевается автоматически на аналогичный период; автопродление отключается в настройках до даты списания.</p>`},
{h:'Лицевой счёт и списания', b:
`<p>Пополнение лицевого счёта производится способами, указанными в разделе 4. Все внутренние платежи (услуги Биржи, реклама, игровой раздел, платные функции) списываются с лицевого счёта в момент подтверждения операции Пользователем.</p>
<p>Средства на лицевом счёте не являются вкладом, на них не начисляются проценты; лицевой счёт не является банковским счётом или электронным средством платежа кредитной организации. История операций доступна Пользователю в разделе «Кошелёк» без ограничения срока.</p>`},
{h:'Биржа услуг: комиссия и эскроу', b:
`<p>При оформлении заказа на Бирже сумма сделки блокируется на лицевом счёте заказчика (эскроу) и перечисляется исполнителю после подтверждения заказчиком приёмки работы либо автоматически по истечении 7 дней с момента сдачи работы при отсутствии претензий.</p>
<p>Комиссия Сервиса составляет <b>10%</b> от суммы сделки и удерживается с исполнителя при успешном завершении. При споре стороны обязаны пройти арбитраж Сервиса; решение арбитража о распределении заблокированных средств обязательно для сторон в рамках Сервиса.</p>
<p>Вывод средств с лицевого счёта на внешние реквизиты — комиссия <b>2%</b> от суммы вывода, срок исполнения до 5 рабочих дней. Оператор вправе запросить подтверждение личности при выводе.</p>`},
{h:'Партнёрская программа', b:
`<p>Пользователь получает вознаграждение от платежей приглашённых им пользователей: <b>15%</b> — от платежей рефералов первого уровня, <b>5%</b> — от платежей рефералов второго уровня. Вознаграждение начисляется на лицевой счёт в момент успешного платежа реферала.</p>
<p>Запрещены самореферальство, накрутка, спам-привлечение и иной фрод. Нарушение влечёт аннулирование начисленных вознаграждений и отключение от программы.</p>`},
{h:'Возвраты', b:
`<p>Пользователь вправе отказаться от оплаченной подписки в течение 14 календарных дней с момента оплаты при условии, что платный функционал фактически не использовался, — возврат производится в полном объёме тем же способом оплаты.</p>
<p>При технической невозможности оказания услуг по вине Оператора возврат производится пропорционально неиспользованному периоду. Средства, израсходованные на сделки Биржи, рекламу и игровой раздел, возврату не подлежат, за исключением случаев, прямо предусмотренных законом.</p>
<p>Заявление о возврате направляется на okoteam.top@gmail.com; срок рассмотрения — 10 рабочих дней.</p>`},
{h:'Ответственность сторон', b:
`<p>Сервис предоставляется «как есть». Оператор не отвечает за содержание пользовательского контента, а также за исполнение обязательств между заказчиками и исполнителями Биржи, кроме функций эскроу и арбитража.</p>
<p>Совокупная ответственность Оператора по договору ограничена суммой платежей Пользователя за последние 3 месяца. Пользователь несёт ответственность за достоверность предоставленных данных и сохранность доступа к своему аккаунту.</p>`},
{h:'Форс-мажор', b:
`<p>Стороны освобождаются от ответственности за неисполнение обязательств вследствие обстоятельств непреодолимой силы: стихийные бедствия, военные действия, акты органов власти, аварии сетей связи и энергоснабжения, массовые сбои платёжных систем. Сторона, для которой наступили такие обстоятельства, уведомляет другую сторону в разумный срок.</p>`},
{h:'Срок действия и изменение оферты', b:
`<p>Договор действует с момента акцепта бессрочно. Оператор вправе изменять условия оферты, публикуя новую редакцию в приложении; о существенных изменениях Пользователи уведомляются не позднее чем за 10 дней до вступления в силу. Продолжение использования Сервиса после вступления изменений в силу означает согласие с новой редакцией.</p>`},
{h:'Реквизиты Оператора', b:
`<p><b>ИП Ильясов Даниэль Альбертович</b> · ИНН 682016634349 · г. Москва, Российская Федерация · представительство: г. Дубай, ОАЭ · e-mail: okoteam.top@gmail.com.</p>`}
]},
en: { title:'Public Offer Agreement', rev:'Revision No. 4', secs:[
{h:'General Provisions and Acceptance', b:
`<p>This document is an official proposal (public offer) of Sole Proprietor Ilyasov Daniel Albertovich (Taxpayer ID 682016634349, Moscow, Russian Federation; representative office: Dubai, UAE), hereinafter the <b>"Operator"</b>, to conclude a paid services agreement for the OKO service on the terms below, pursuant to Articles 435, 437 and 438 of the Civil Code of the Russian Federation.</p>
<p>Full and unconditional acceptance of this offer is any of the following: registering an account in the OKO app, paying for a plan, topping up the personal account, or actually using any feature of the Service. The agreement is deemed concluded upon acceptance.</p>`},
{h:'Definitions', b:
`<ul>
<li><b>Service (OKO)</b> — the Operator's software suite: the OKO mobile and web application, including the messenger, social feed, services marketplace, academy, games section, advertising cabinet, affiliate program and personal account.</li>
<li><b>User</b> — a legally capable individual or a representative of a legal entity who has accepted this offer.</li>
<li><b>Personal Account</b> — the User's internal ledger within the Service reflecting the balance of funds for paying for services.</li>
<li><b>Marketplace</b> — the section of the Service for transactions between clients and service providers.</li>
<li><b>Escrow</b> — the mechanism of holding the client's funds until acceptance of the work is confirmed.</li>
<li><b>Plan</b> — the scope of paid functionality (START, PRO, BUSINESS) for a selected period.</li>
</ul>`},
{h:'Subject Matter', b:
`<p>The Operator grants the User the right to use the Service within its functionality: messaging and calls, publishing content in the social feed, offering and ordering services on the Marketplace, learning in the Academy, participation in games (18+), placing advertising via the advertising cabinet, participation in the affiliate program, and operations on the personal account.</p>
<p>Basic functionality is free of charge. Extended functionality is provided by subscription according to the selected Plan.</p>`},
{h:'Plans and Payment', b:
`<div class="lg-table-wrap"><table class="lg-table">
<tr><th>Plan</th><th>Price / mo*</th><th>Includes</th></tr>
<tr><td>START</td><td>990&nbsp;RUB (~$10)</td><td>Premium messenger (files up to 4 GB, voice transcription), templates marketplace, trends catalog, 30 video checks per month, affiliate program activation.</td></tr>
<tr><td>PRO</td><td>4&nbsp;900&nbsp;RUB (~$49)</td><td>Personal growth system, OKO Personal Assistant (300 requests), Content Studio (100 generations), 100 video checks and 20 auto-fixes per month, analytics for 3 channels.</td></tr>
<tr><td>BUSINESS</td><td>19&nbsp;900&nbsp;RUB (~$199)</td><td>Content production 30–50 videos per month, dedicated OKO specialists on your project, autoposting to all connected social networks, priority support, up to 3 team accounts.</td></tr>
<tr><td>BUSINESS&nbsp;PRO</td><td>49&nbsp;900&nbsp;RUB (~$499)</td><td>Content production 100 videos per month, Personal Image (voice and face twin), unlimited assistant and studio, API access, up to 5 team accounts, bonus: landing page and bot with annual payment.</td></tr>
<tr><td>MAX</td><td>149&nbsp;900&nbsp;RUB (~$1499)</td><td>Content production 300 videos per month, full OKO specialists team with a dedicated manager, complete digital launch (website, bot, automations) with annual payment, up to 15 team accounts, white-label.</td></tr>
</table></div>
<p class="lg-note">* Monthly price with annual billing. Period discounts off the base monthly price: 3 months — 10%, 6 months — 15%, 12 months — 20%.</p>
<p>Payment methods: Russian bank cards, cryptocurrency, and the Lava.top payment platform. Prices may be quoted in US dollars; the actual charge is made in the currency of the payment method at the exchange rate on the payment date. Subscriptions renew automatically for the same period; auto-renewal can be disabled in the settings before the billing date.</p>`},
{h:'Personal Account and Charges', b:
`<p>The personal account is topped up using the methods listed in Section 4. All internal payments (Marketplace services, advertising, games, paid features) are debited from the personal account at the moment the User confirms the operation.</p>
<p>Funds on the personal account are not a deposit and bear no interest; the personal account is not a bank account or an electronic payment instrument of a credit institution. The transaction history is available to the User in the Wallet section without any time limit.</p>`},
{h:'Marketplace: Commission and Escrow', b:
`<p>When an order is placed on the Marketplace, the transaction amount is held on the client's personal account (escrow) and transferred to the provider after the client confirms acceptance of the work, or automatically 7 days after delivery if no claims are raised.</p>
<p>The Service commission is <b>10%</b> of the transaction amount, withheld from the provider upon successful completion. In case of a dispute the parties must use the Service arbitration; its decision on the distribution of the held funds is binding within the Service.</p>
<p>Withdrawal of funds from the personal account to external details is subject to a <b>2%</b> fee and is processed within 5 business days. The Operator may request identity verification upon withdrawal.</p>`},
{h:'Affiliate Program', b:
`<p>The User receives remuneration from payments made by the users they invite: <b>15%</b> of first-level referral payments and <b>5%</b> of second-level referral payments. Remuneration is credited to the personal account at the moment of the referral's successful payment.</p>
<p>Self-referrals, artificial inflation, spam acquisition and other fraud are prohibited and result in cancellation of accrued remuneration and exclusion from the program.</p>`},
{h:'Refunds', b:
`<p>The User may cancel a paid subscription within 14 calendar days of payment provided the paid functionality has not actually been used — a full refund is issued via the original payment method.</p>
<p>If the services cannot be provided for technical reasons attributable to the Operator, a refund is issued pro rata for the unused period. Funds spent on Marketplace transactions, advertising and the games section are non-refundable except as expressly required by law.</p>
<p>Refund requests are sent to okoteam.top@gmail.com; processing time is 10 business days.</p>`},
{h:'Liability', b:
`<p>The Service is provided "as is". The Operator is not responsible for user-generated content or for the performance of obligations between Marketplace clients and providers, except for the escrow and arbitration functions.</p>
<p>The Operator's aggregate liability under the agreement is limited to the amount of the User's payments over the last 3 months. The User is responsible for the accuracy of the data provided and for safeguarding access to their account.</p>`},
{h:'Force Majeure', b:
`<p>The parties are released from liability for non-performance caused by force majeure: natural disasters, military actions, acts of public authorities, failures of communication and power networks, and mass outages of payment systems. The affected party shall notify the other party within a reasonable time.</p>`},
{h:'Term and Amendments', b:
`<p>The agreement takes effect upon acceptance and remains in force indefinitely. The Operator may amend the offer by publishing a new revision in the app; Users are notified of material changes at least 10 days before they take effect. Continued use of the Service after the changes take effect constitutes consent to the new revision.</p>`},
{h:'Operator Details', b:
`<p><b>Sole Proprietor Ilyasov Daniel Albertovich</b> · Taxpayer ID (INN) 682016634349 · Moscow, Russian Federation · representative office: Dubai, UAE · e-mail: okoteam.top@gmail.com.</p>`}
]}
},

/* ===================== КОНФИДЕНЦИАЛЬНОСТЬ ===================== */
privacy: {
ru: { title:'Политика конфиденциальности', rev:'Редакция № 4', secs:[
{h:'Общие положения', b:
`<p>Настоящая Политика определяет порядок обработки и защиты персональных данных пользователей приложения OKO. Оператор персональных данных — ИП Ильясов Даниэль Альбертович (ИНН 682016634349, г. Москва, РФ; представительство: г. Дубай, ОАЭ).</p>
<p>Обработка ведётся в соответствии с Федеральным законом от 27.07.2006 № 152-ФЗ «О персональных данных»; для пользователей из Европейской экономической зоны дополнительно применяются принципы GDPR: законность, минимизация данных, ограничение целей и сроков хранения, прозрачность.</p>`},
{h:'Состав обрабатываемых данных', b:
`<ul>
<li><b>Данные аккаунта:</b> имя, никнейм, адрес электронной почты, номер телефона, аватар, идентификаторы входа (Telegram ID, Google, Apple).</li>
<li><b>Данные профиля и анкеты:</b> сведения о деятельности, навыках и целях, заполняемые пользователем добровольно.</li>
<li><b>Платёжные метаданные:</b> суммы, даты и статусы операций, идентификаторы транзакций платёжных провайдеров. Полные номера банковских карт Оператор не получает и не хранит.</li>
<li><b>Контент:</b> сообщения, посты, отклики на Бирже, материалы, загружаемые в Сервис.</li>
<li><b>Технические данные:</b> IP-адрес, тип устройства и ОС, язык, cookies, журналы событий (логи).</li>
<li><b>Статистика использования:</b> обезличенные метрики активности в разделах Сервиса.</li>
</ul>`},
{h:'Цели обработки', b:
`<ul>
<li>Регистрация, аутентификация и работа аккаунта.</li>
<li>Исполнение договора: подписки, сделки Биржи, эскроу, выводы средств, партнёрские начисления.</li>
<li>Модерация контента и обеспечение безопасности, предотвращение мошенничества.</li>
<li>Поддержка пользователей и обратная связь.</li>
<li>Улучшение Сервиса на основе обезличенной аналитики.</li>
<li>Информирование о работе Сервиса; рекламные рассылки — только с отдельного согласия, с возможностью отказа в один клик.</li>
</ul>`},
{h:'Правовые основания', b:
`<p>Основаниями обработки являются: исполнение договора (публичной оферты), согласие субъекта персональных данных, законные интересы Оператора (безопасность, защита от фрода) и исполнение требований законодательства (бухгалтерский и налоговый учёт).</p>`},
{h:'Cookies и аналитика', b:
`<p>Сервис использует cookies и аналогичные технологии: обязательные (сессия, безопасность), функциональные (настройки темы и языка) и аналитические (обезличенная статистика). Пользователь может ограничить cookies в настройках браузера или устройства; обязательные cookies необходимы для работы Сервиса.</p>`},
{h:'Передача данных третьим лицам', b:
`<p>Данные передаются только в объёме, необходимом для конкретной цели:</p>
<ul>
<li>Платёжным провайдерам (Lava.top, банковские и криптовалютные процессинги) — для проведения платежей.</li>
<li>Поставщикам облачной инфраструктуры и хостинга — для хранения и обработки данных по поручению Оператора.</li>
<li>Telegram — при входе через Telegram-аккаунт.</li>
<li>Государственным органам — исключительно по законному запросу.</li>
</ul>
<p>Оператор не продаёт персональные данные и не передаёт их третьим лицам для их собственных маркетинговых целей.</p>`},
{h:'Хранение и сроки', b:
`<p>Запись, систематизация, накопление, хранение, уточнение и извлечение персональных данных граждан РФ осуществляются с использованием баз данных на территории РФ (ч. 5 ст. 18 152-ФЗ).</p>
<p>Сроки: данные аккаунта — на период действия аккаунта и 3 года после его удаления (для разрешения споров и по требованиям закона); документы об операциях — 5 лет; технические логи — 12 месяцев. По истечении сроков данные удаляются либо обезличиваются.</p>`},
{h:'Права пользователя', b:
`<ul>
<li>Получить сведения об обработке и копию своих данных.</li>
<li>Требовать уточнения, блокирования или удаления данных («право быть забытым»).</li>
<li>Отозвать согласие на обработку в любой момент.</li>
<li>Получить данные в структурированном машиночитаемом формате (переносимость).</li>
<li>Возразить против обработки, основанной на законном интересе.</li>
<li>Обжаловать действия Оператора в Роскомнадзоре либо ином надзорном органе по месту нахождения пользователя.</li>
</ul>
<p>Запросы направляются на okoteam.top@gmail.com; срок ответа — 30 дней.</p>`},
{h:'Защита данных', b:
`<p>Применяются организационные и технические меры: шифрование каналов связи (TLS), хеширование паролей, разграничение доступа сотрудников по принципу минимальной необходимости, резервное копирование, журналирование доступа. При инциденте, создающем угрозу правам субъектов, Оператор уведомляет надзорный орган и затронутых пользователей в установленные законом сроки.</p>`},
{h:'Возрастные ограничения', b:
`<p>Сервис предназначен для лиц старше 16 лет; игровой раздел — строго 18+. Оператор не осуществляет намеренный сбор данных детей. При обнаружении аккаунта, созданного лицом младше допустимого возраста, аккаунт и связанные данные удаляются.</p>`},
{h:'Изменения и контакты', b:
`<p>Оператор вправе обновлять Политику; новая редакция публикуется в приложении с указанием даты. Продолжение использования Сервиса означает согласие с обновлённой Политикой.</p>
<p>Вопросы по обработке персональных данных: <b>okoteam.top@gmail.com</b>.</p>`}
]},
en: { title:'Privacy Policy', rev:'Revision No. 4', secs:[
{h:'General Provisions', b:
`<p>This Policy defines how personal data of OKO app users is processed and protected. The data controller is Sole Proprietor Ilyasov Daniel Albertovich (Taxpayer ID 682016634349, Moscow, Russian Federation; representative office: Dubai, UAE).</p>
<p>Processing is carried out in accordance with Russian Federal Law No. 152-FZ "On Personal Data" of 27.07.2006; for users from the European Economic Area the GDPR principles additionally apply: lawfulness, data minimisation, purpose and storage limitation, and transparency.</p>`},
{h:'Data We Process', b:
`<ul>
<li><b>Account data:</b> name, nickname, e-mail address, phone number, avatar, sign-in identifiers (Telegram ID, Google, Apple).</li>
<li><b>Profile data:</b> information about activities, skills and goals provided voluntarily by the user.</li>
<li><b>Payment metadata:</b> amounts, dates and statuses of operations, transaction identifiers of payment providers. The Operator never receives or stores full bank card numbers.</li>
<li><b>Content:</b> messages, posts, Marketplace bids, and materials uploaded to the Service.</li>
<li><b>Technical data:</b> IP address, device and OS type, language, cookies, event logs.</li>
<li><b>Usage statistics:</b> anonymised activity metrics across the Service.</li>
</ul>`},
{h:'Purposes of Processing', b:
`<ul>
<li>Registration, authentication and account operation.</li>
<li>Performance of the agreement: subscriptions, Marketplace transactions, escrow, withdrawals, affiliate accruals.</li>
<li>Content moderation, security and fraud prevention.</li>
<li>User support and feedback.</li>
<li>Improving the Service based on anonymised analytics.</li>
<li>Service notifications; marketing communications — only with separate consent, with one-click opt-out.</li>
</ul>`},
{h:'Legal Bases', b:
`<p>Processing is based on: performance of the agreement (the public offer), the data subject's consent, the Operator's legitimate interests (security, fraud prevention), and compliance with legal obligations (accounting and tax requirements).</p>`},
{h:'Cookies and Analytics', b:
`<p>The Service uses cookies and similar technologies: essential (session, security), functional (theme and language preferences) and analytical (anonymised statistics). Users may restrict cookies in their browser or device settings; essential cookies are required for the Service to operate.</p>`},
{h:'Disclosure to Third Parties', b:
`<p>Data is shared only to the extent necessary for a specific purpose:</p>
<ul>
<li>Payment providers (Lava.top, bank and cryptocurrency processors) — to process payments.</li>
<li>Cloud infrastructure and hosting providers — to store and process data on behalf of the Operator.</li>
<li>Telegram — when signing in with a Telegram account.</li>
<li>Public authorities — solely upon a lawful request.</li>
</ul>
<p>The Operator does not sell personal data and does not share it with third parties for their own marketing purposes.</p>`},
{h:'Storage and Retention', b:
`<p>Recording, systematisation, accumulation, storage, clarification and retrieval of personal data of Russian citizens are performed using databases located in the Russian Federation (Art. 18(5) of Law 152-FZ).</p>
<p>Retention periods: account data — for the lifetime of the account plus 3 years after deletion (for dispute resolution and legal requirements); transaction records — 5 years; technical logs — 12 months. Upon expiry, data is deleted or anonymised.</p>`},
{h:'Your Rights', b:
`<ul>
<li>Obtain information about processing and a copy of your data.</li>
<li>Request rectification, restriction or erasure of data (the "right to be forgotten").</li>
<li>Withdraw consent to processing at any time.</li>
<li>Receive your data in a structured, machine-readable format (portability).</li>
<li>Object to processing based on legitimate interest.</li>
<li>Lodge a complaint with Roskomnadzor or another supervisory authority at your location.</li>
</ul>
<p>Requests are sent to okoteam.top@gmail.com; the response time is 30 days.</p>`},
{h:'Data Security', b:
`<p>Organisational and technical measures are applied: encryption of communication channels (TLS), password hashing, least-privilege staff access, backups, and access logging. In the event of an incident endangering the rights of data subjects, the Operator notifies the supervisory authority and affected users within the timeframes required by law.</p>`},
{h:'Age Restrictions', b:
`<p>The Service is intended for persons over 16; the games section is strictly 18+. The Operator does not knowingly collect children's data. If an account created by an underage person is discovered, the account and related data are deleted.</p>`},
{h:'Amendments and Contact', b:
`<p>The Operator may update this Policy; the new revision is published in the app with its date. Continued use of the Service constitutes acceptance of the updated Policy.</p>
<p>Personal data enquiries: <b>okoteam.top@gmail.com</b>.</p>`}
]}
},

/* ======================= СОГЛАШЕНИЕ ======================= */
terms: {
ru: { title:'Пользовательское соглашение', rev:'Редакция № 4', secs:[
{h:'Общие положения', b:
`<p>Настоящее Соглашение устанавливает правила пользования платформой OKO и действует совместно с Публичной офертой и Политикой конфиденциальности. Регистрируясь в Сервисе, Пользователь подтверждает, что ознакомился с Соглашением и обязуется его соблюдать.</p>
<p>Оператор платформы — ИП Ильясов Даниэль Альбертович (ИНН 682016634349, г. Москва, РФ; представительство: г. Дубай, ОАЭ).</p>`},
{h:'Аккаунт и верификация', b:
`<p>Пользователь обязан указывать достоверные данные и поддерживать их актуальность. Допускается один личный аккаунт на человека; передача аккаунта третьим лицам запрещена.</p>
<p>Сервис предоставляет добровольную верификацию (отметка подлинности). Для верификации, а также при выводе средств и в спорных ситуациях Оператор вправе запросить документы, подтверждающие личность или полномочия. Верификация может быть отозвана при нарушении правил.</p>`},
{h:'Общие правила поведения', b:
`<ul>
<li>Уважительное общение: запрещены травля, угрозы, оскорбления и дискриминация по любому признаку.</li>
<li>Запрещено выдавать себя за другое лицо, организацию или представителя Сервиса.</li>
<li>Запрещены массовые рассылки, спам, накрутка метрик и автоматизированный сбор данных без согласия Оператора.</li>
<li>Запрещены попытки взлома, обхода технических ограничений и вмешательства в работу Сервиса.</li>
</ul>`},
{h:'Запрещённый контент', b:
`<p>На платформе запрещено размещать, рекламировать и распространять:</p>
<ul>
<li>Наркотические средства, психотропные вещества и их пропаганду.</li>
<li>Порнографию; материалы 18+ без соответствующей маркировки и возрастного ограничения доступа.</li>
<li>Мошеннические схемы, финансовые пирамиды, фишинг и вводящие в заблуждение предложения.</li>
<li>Экстремистские и террористические материалы, призывы к насилию.</li>
<li>Контент, нарушающий законодательство: незаконный оборот оружия, персональные данные третьих лиц без их согласия, вредоносное ПО.</li>
<li>Контент, нарушающий интеллектуальные права третьих лиц.</li>
</ul>`},
{h:'Автомодерация и блокировки', b:
`<p>Загружаемый контент (видео, изображения, тексты) проходит автоматическую премодерацию по внутренним правилам платформы. При выявлении признаков нарушения контент может быть скрыт до проверки; спорные случаи эскалируются модератору-человеку.</p>
<p>Меры при нарушениях (применяются соразмерно тяжести): предупреждение, удаление контента, ограничение функций, временная блокировка, удаление аккаунта. При блокировке за грубые нарушения средства, полученные преступным путём, могут быть заморожены до выяснения обстоятельств.</p>
<p>Пользователь вправе подать апелляцию на решение модерации через поддержку в течение 30 дней; апелляция рассматривается человеком.</p>`},
{h:'Игры и ответственная игра (18+)', b:
`<p>Игровой раздел доступен только пользователям, достигшим 18 лет. Игры Сервиса — развлекательные механики с использованием внутреннего баланса лицевого счёта.</p>
<ul>
<li>Пользователь может установить дневные и месячные лимиты трат в игровом разделе.</li>
<li>Доступна функция самоисключения — временного или постоянного отключения игрового раздела.</li>
<li>Запрещено участие в играх с чужих аккаунтов и использование ботов.</li>
<li>Игровые механики не гарантируют выигрыш; трать только те средства, потерю которых можешь себе позволить.</li>
</ul>`},
{h:'Интеллектуальная собственность', b:
`<p>Исключительные права на платформу OKO, её код, дизайн, логотип и товарные знаки принадлежат Оператору. Использование элементов платформы вне Сервиса без письменного разрешения запрещено.</p>
<p>Права на пользовательский контент сохраняются за Пользователем. Размещая контент, Пользователь предоставляет Оператору неисключительную безвозмездную лицензию на его хранение, отображение и техническую обработку в целях работы Сервиса. Правообладатели могут направить жалобу о нарушении своих прав на okoteam.top@gmail.com — контент-нарушитель удаляется после проверки.</p>`},
{h:'Правила Биржи услуг', b:
`<ul>
<li>Сделки заключаются только через эскроу-механику Сервиса; обход комиссии и увод сделок «мимо кассы» запрещены.</li>
<li>Исполнитель обязан выполнять работу в заявленный срок и с заявленным качеством; заказчик — принимать работу или мотивированно её отклонять.</li>
<li>Запрещены фиктивные сделки, накрутка отзывов и рейтинга.</li>
<li>Споры разрешаются арбитражем Сервиса на основании переписки и материалов сделки.</li>
</ul>`},
{h:'Санкции и порядок применения', b:
`<p>При нарушении Соглашения Оператор вправе применить меры из раздела 5 без предварительного уведомления, если нарушение создаёт угрозу другим пользователям или Сервису. Повторные и грубые нарушения ведут к необратимому удалению аккаунта. Остаток средств на лицевом счёте (кроме средств, полученных с нарушением) возвращается Пользователю по реквизитам после верификации личности.</p>`},
{h:'Разрешение споров', b:
`<p>Стороны применяют обязательный претензионный порядок: претензия направляется на okoteam.top@gmail.com, срок ответа — 30 дней. При недостижении согласия спор передаётся в суд по месту нахождения Оператора в соответствии с законодательством РФ. Для пользователей вне РФ возможен арбитраж в согласованной сторонами юрисдикции.</p>`},
{h:'Заключительные положения', b:
`<p>Недействительность отдельного положения Соглашения не влечёт недействительности остальных. Оператор вправе обновлять Соглашение в порядке, аналогичном изменению Публичной оферты. Актуальная редакция всегда доступна в приложении в разделе «Документы OKO».</p>`}
]},
en: { title:'Terms of Service', rev:'Revision No. 4', secs:[
{h:'General Provisions', b:
`<p>These Terms establish the rules for using the OKO platform and apply together with the Public Offer and the Privacy Policy. By registering in the Service, the User confirms that they have read the Terms and undertake to comply with them.</p>
<p>The platform operator is Sole Proprietor Ilyasov Daniel Albertovich (Taxpayer ID 682016634349, Moscow, Russian Federation; representative office: Dubai, UAE).</p>`},
{h:'Account and Verification', b:
`<p>The User must provide accurate data and keep it up to date. One personal account per person is allowed; transferring an account to third parties is prohibited.</p>
<p>The Service offers voluntary verification (authenticity badge). For verification, as well as for withdrawals and in disputes, the Operator may request documents confirming identity or authority. Verification may be revoked for violations of the rules.</p>`},
{h:'General Conduct Rules', b:
`<ul>
<li>Respectful communication: harassment, threats, insults and discrimination on any grounds are prohibited.</li>
<li>Impersonating another person, organisation or a Service representative is prohibited.</li>
<li>Mass mailings, spam, metric manipulation and automated data collection without the Operator's consent are prohibited.</li>
<li>Hacking attempts, circumvention of technical restrictions and interference with the Service are prohibited.</li>
</ul>`},
{h:'Prohibited Content', b:
`<p>It is prohibited to post, advertise or distribute on the platform:</p>
<ul>
<li>Narcotic drugs, psychotropic substances and their promotion.</li>
<li>Pornography; 18+ materials without proper labelling and age-restricted access.</li>
<li>Fraudulent schemes, financial pyramids, phishing and misleading offers.</li>
<li>Extremist and terrorist materials, incitement to violence.</li>
<li>Content violating the law: illegal arms trafficking, third parties' personal data without their consent, malware.</li>
<li>Content infringing third-party intellectual property rights.</li>
</ul>`},
{h:'Auto-moderation and Restrictions', b:
`<p>Uploaded content (video, images, texts) undergoes automatic pre-moderation according to the platform's internal rules. If signs of a violation are detected, the content may be hidden pending review; disputed cases are escalated to a human moderator.</p>
<p>Enforcement measures (applied proportionately to severity): warning, content removal, feature restriction, temporary suspension, account deletion. In case of suspension for gross violations, funds obtained unlawfully may be frozen pending investigation.</p>
<p>The User may appeal a moderation decision via support within 30 days; appeals are reviewed by a human.</p>`},
{h:'Games and Responsible Play (18+)', b:
`<p>The games section is available only to users aged 18 and over. The Service's games are entertainment mechanics using the internal personal account balance.</p>
<ul>
<li>The User may set daily and monthly spending limits in the games section.</li>
<li>A self-exclusion feature is available — temporary or permanent disabling of the games section.</li>
<li>Playing from other users' accounts and using bots is prohibited.</li>
<li>Game mechanics do not guarantee winnings; only spend what you can afford to lose.</li>
</ul>`},
{h:'Intellectual Property', b:
`<p>Exclusive rights to the OKO platform, its code, design, logo and trademarks belong to the Operator. Use of platform elements outside the Service without written permission is prohibited.</p>
<p>Rights to user content remain with the User. By posting content, the User grants the Operator a non-exclusive royalty-free licence to store, display and technically process it for the operation of the Service. Rights holders may report infringement to okoteam.top@gmail.com — infringing content is removed after review.</p>`},
{h:'Marketplace Rules', b:
`<ul>
<li>Transactions are concluded only through the Service's escrow mechanism; commission circumvention and taking deals off-platform are prohibited.</li>
<li>The provider must deliver the work on time and to the declared quality; the client must accept the work or reject it with justification.</li>
<li>Fake transactions and manipulation of reviews and ratings are prohibited.</li>
<li>Disputes are resolved by the Service arbitration based on correspondence and transaction materials.</li>
</ul>`},
{h:'Sanctions and Enforcement', b:
`<p>In case of a violation of the Terms, the Operator may apply the measures listed in Section 5 without prior notice if the violation endangers other users or the Service. Repeated and gross violations lead to irreversible account deletion. The remaining personal account balance (except funds obtained through violations) is returned to the User after identity verification.</p>`},
{h:'Dispute Resolution', b:
`<p>The parties apply a mandatory pre-trial claim procedure: claims are sent to okoteam.top@gmail.com, with a 30-day response period. If no agreement is reached, the dispute is referred to the court at the Operator's location under the laws of the Russian Federation. For users outside Russia, arbitration in a jurisdiction agreed by the parties is possible.</p>`},
{h:'Final Provisions', b:
`<p>The invalidity of any individual provision of the Terms does not invalidate the remaining provisions. The Operator may update the Terms in the same manner as the Public Offer. The current revision is always available in the app under "OKO Legal".</p>`}
]}
},

/* ============ ПОЛИТИКА ВОЗВРАТА ============ */
refund: {
ru: { title:'Политика возврата', rev:'Редакция № 2', secs:[
{h:'Общие положения', b:
`<p>Настоящая Политика возврата определяет условия и порядок возврата средств Пользователю по договору Публичной оферты Сервиса OKO. Оператор — ИП Ильясов Даниэль Альбертович (ИНН 682016634349, г. Москва, РФ; представительство: г. Дубай, ОАЭ).</p>
<p>Политика применяется во всех операциях: подписки на тарифы, сделки Биржи, рекламный кабинет, игровой раздел, покупки в мессенджере (стикеры, каналы) и пополнения лицевого счёта. Приоритет над Политикой имеют требования действующего законодательства РФ (в т.ч. Закон «О защите прав потребителей» и ст. 32 ГК РФ).</p>`},
{h:'Подписки на тарифы', b:
`<p><b>«Период охлаждения».</b> В течение 14 календарных дней с момента первой оплаты подписки Пользователь вправе отказаться от неё и получить полный возврат при условии, что платный функционал фактически не использовался (не осуществлялись платные операции, не расходовались лимиты Тарифа).</p>
<p><b>Пропорциональный возврат.</b> Если платный функционал уже использовался и на момент отказа остаётся неистёкший период, Оператор возвращает пропорциональную часть стоимости за неиспользованные полные календарные дни. Из суммы возврата вычитаются: (а) стоимость фактически потреблённых лимитов Тарифа по тарифной сетке, (б) банковская или платёжная комиссия, если её не удаётся вернуть провайдером.</p>
<p><b>Автопродление.</b> Заявление, поданное до даты автопродления и включающее просьбу об отключении автопродления, принимается автоматически: следующее списание не производится, а уже оплаченный период не возвращается частично, если платный функционал использовался в нём.</p>`},
{h:'Сделки Биржи услуг', b:
`<p>Средства, заблокированные в эскроу, возвращаются Заказчику полностью, если:</p>
<ul>
<li>Исполнитель не приступил к работе в согласованный срок и не согласовал перенос;</li>
<li>Стороны расторгли сделку по взаимному согласию до сдачи работы;</li>
<li>Арбитраж Сервиса вынес решение о возврате.</li>
</ul>
<p>После сдачи работы Исполнителем и её принятия Заказчиком (в т.ч. автоматически по истечении 7 дней при отсутствии претензий) средства перечисляются Исполнителю и возврату не подлежат, за исключением случаев обмана, установленных арбитражем.</p>`},
{h:'Реклама и игровой раздел', b:
`<p>Средства, потраченные на показы рекламы, продвижение постов и ставки/покупки в игровом разделе, возврату не подлежат, поскольку услуга считается оказанной в момент фактического расхода. Исключение — техническая ошибка Сервиса, приведшая к некорректному списанию: такие списания возвращаются на лицевой счёт в полном объёме по обращению Пользователя.</p>`},
{h:'Пополнения лицевого счёта', b:
`<p>Пользователь вправе запросить возврат неизрасходованного остатка лицевого счёта в любой момент. Возврат производится за вычетом комиссии платёжного метода вывода (2% для банковских карт РФ, до 5% для международных методов) и налоговых удержаний, если такие применимы. Срок исполнения — до 10 рабочих дней после верификации личности заявителя.</p>`},
{h:'Каналы, стикеры и цифровые товары', b:
`<p>Оплаты подписок на платные каналы возвращаются в течение 24 часов с момента первой оплаты при условии, что Пользователь не открыл в канале ни одного закрытого материала. По истечении 24 часов возврат производится по решению владельца канала; Оператор в этом случае возвращает свою комиссию.</p>
<p>Стикеры, эмодзи и иные цифровые товары возврату не подлежат после активации на аккаунте, за исключением случаев некачественного контента (нарушение авторских прав, повреждённый файл): в этом случае возврат производится в полном объёме.</p>`},
{h:'Порядок обращения и сроки', b:
`<p>Заявление о возврате направляется на <b>okoteam.top@gmail.com</b> с темой «Возврат» и содержит: имя Пользователя и ID в Сервисе, дату и сумму операции, идентификатор транзакции, реквизиты возврата (при выводе), краткое описание причины.</p>
<p>Срок рассмотрения — до <b>10 рабочих дней</b>. Возврат производится тем же способом, каким был совершён платёж; в случае невозможности возврата исходным методом Оператор согласовывает альтернативный способ. Срок зачисления средств зависит от платёжного провайдера и обычно составляет от 1 до 10 рабочих дней после утверждения возврата.</p>`},
{h:'Отказ в возврате', b:
`<p>Оператор вправе отказать в возврате, если:</p>
<ul>
<li>Заявление подано с нарушением сроков, установленных настоящей Политикой;</li>
<li>Услуга фактически оказана в полном объёме и претензии к её качеству отсутствуют;</li>
<li>Аккаунт Пользователя заблокирован за нарушение Пользовательского соглашения (в этом случае возврат остатка средств рассматривается индивидуально);</li>
<li>Заявление содержит недостоверные сведения или подано неуполномоченным лицом.</li>
</ul>
<p>Отказ в возврате может быть обжалован в порядке, предусмотренном разделом «Разрешение споров» Пользовательского соглашения.</p>`}
]},
en: { title:'Refund Policy', rev:'Revision No. 2', secs:[
{h:'General Provisions', b:
`<p>This Refund Policy sets out the conditions and procedure for refunding funds to the User under the Public Offer of the OKO Service. Operator: Sole Proprietor Ilyasov Daniel Albertovich (Taxpayer ID 682016634349, Moscow, Russian Federation; representative office: Dubai, UAE).</p>
<p>The Policy applies to all operations: plan subscriptions, Marketplace transactions, the advertising cabinet, the games section, in-messenger purchases (stickers, channels) and personal-account top-ups. Applicable law (including the Russian Consumer Protection Law and Art. 32 of the Civil Code) prevails over this Policy.</p>`},
{h:'Plan Subscriptions', b:
`<p><b>Cooling-off period.</b> Within 14 calendar days of the initial subscription payment, the User may cancel and obtain a full refund provided the paid functionality has not actually been used (no paid operations were performed and no plan limits were consumed).</p>
<p><b>Pro-rata refund.</b> If the paid functionality has already been used and an unused period remains at the moment of cancellation, the Operator refunds a pro-rata share of the price for the unused full calendar days. The refund amount is reduced by (a) the cost of the actually consumed plan limits at published rates and (b) the bank or payment fee that cannot be reversed by the provider.</p>
<p><b>Auto-renewal.</b> A request submitted before the auto-renewal date that asks to disable auto-renewal is accepted automatically: the next charge is not made; the paid period already in effect is not partially refunded if the paid functionality was used during it.</p>`},
{h:'Marketplace Transactions', b:
`<p>Funds held in escrow are fully refunded to the Client if:</p>
<ul>
<li>the Provider has not started the work within the agreed timeframe and has not agreed to postpone;</li>
<li>the parties have terminated the transaction by mutual consent before delivery;</li>
<li>the Service arbitration has ruled in favour of a refund.</li>
</ul>
<p>Once the work is delivered by the Provider and accepted by the Client (including automatically after 7 days if no claims are raised), the funds are transferred to the Provider and are non-refundable, except for cases of proven fraud established by arbitration.</p>`},
{h:'Advertising and Games', b:
`<p>Funds spent on ad impressions, post promotion and bets/purchases in the games section are non-refundable, as the service is considered rendered at the moment of actual expenditure. Exception: a technical error of the Service that led to an incorrect charge — such charges are refunded to the personal account in full upon request.</p>`},
{h:'Personal-account Top-ups', b:
`<p>The User may request a refund of the unspent personal-account balance at any time. The refund is issued net of the withdrawal-method fee (2% for Russian bank cards, up to 5% for international methods) and applicable tax withholdings. Processing time — up to 10 business days after identity verification.</p>`},
{h:'Channels, Stickers and Digital Goods', b:
`<p>Subscriptions to paid channels are refunded within 24 hours of the initial payment provided the User has not opened any private content in the channel. After 24 hours the refund is at the channel owner's discretion; in that case the Operator refunds its own commission.</p>
<p>Stickers, emoji and other digital goods are non-refundable once activated on the account, except for cases of defective content (copyright infringement, corrupted file), in which case a full refund is issued.</p>`},
{h:'Request Procedure and Timelines', b:
`<p>Refund requests are sent to <b>okoteam.top@gmail.com</b> with the subject "Refund" and must include: the User's name and Service ID, the date and amount of the operation, the transaction identifier, refund details (if a withdrawal is requested), and a brief description of the reason.</p>
<p>Processing time — up to <b>10 business days</b>. The refund is issued using the same method as the original payment; where this is not possible, the Operator agrees an alternative method. The time for funds to arrive depends on the payment provider and usually ranges from 1 to 10 business days after the refund is approved.</p>`},
{h:'Refund Denial', b:
`<p>The Operator may deny a refund if:</p>
<ul>
<li>the request was submitted outside the timelines of this Policy;</li>
<li>the service has been rendered in full and no quality complaints exist;</li>
<li>the User's account is blocked for violation of the Terms of Service (in that case the balance refund is reviewed individually);</li>
<li>the request contains inaccurate information or was submitted by an unauthorised person.</li>
</ul>
<p>A refund denial can be appealed under the "Dispute Resolution" section of the Terms of Service.</p>`}
]}
},

/* ============ ЛИЦЕНЗИОННОЕ СОГЛАШЕНИЕ (EULA) ============ */
license: {
ru: { title:'Лицензионное соглашение на ПО', rev:'Редакция № 2', secs:[
{h:'Стороны и предмет', b:
`<p>Настоящее лицензионное соглашение (далее — <b>«Лицензия»</b>) регулирует условия использования Пользователем программного обеспечения «OKO» — мобильного и веб-приложения, включая обновления, дополнительные модули, документацию и связанные сервисы (далее — <b>«ПО»</b>).</p>
<p>Лицензиар — ИП Ильясов Даниэль Альбертович (ИНН 682016634349, г. Москва, РФ; представительство: г. Дубай, ОАЭ). Устанавливая, копируя или иным образом используя ПО, Пользователь принимает условия Лицензии.</p>`},
{h:'Предоставляемые права', b:
`<p>Лицензиар предоставляет Пользователю простую (неисключительную) безвозмездную лицензию на:</p>
<ul>
<li>установку и запуск ПО на неограниченном числе принадлежащих Пользователю устройств;</li>
<li>использование ПО по прямому функциональному назначению в личных и профессиональных целях;</li>
<li>получение обновлений и патчей ПО, публикуемых Лицензиаром;</li>
<li>использование бесплатных функций ПО без ограничений; платные функции — при активной подписке.</li>
</ul>
<p>Территория действия Лицензии — весь мир. Срок — на период существования соответствующей версии ПО в публичном доступе.</p>`},
{h:'Ограничения', b:
`<p>Пользователю запрещается:</p>
<ul>
<li>распространять, продавать, сдавать в аренду или сублицензировать ПО третьим лицам;</li>
<li>декомпилировать, дизассемблировать, вносить изменения в исходный или бинарный код ПО, за исключением случаев, прямо разрешённых законом;</li>
<li>удалять или изменять уведомления об авторских правах, товарные знаки и иные обозначения правообладателя;</li>
<li>использовать ПО для обхода технических средств защиты, взлома или атак на инфраструктуру Сервиса и третьих лиц;</li>
<li>создавать производные продукты, воспроизводящие существенную часть функциональности или интерфейса ПО.</li>
</ul>`},
{h:'Открытые компоненты (Open-source)', b:
`<p>ПО может включать компоненты с открытым исходным кодом, распространяемые под лицензиями MIT, Apache-2.0, BSD, ISC, LGPL и иными совместимыми лицензиями. Полный перечень таких компонентов и текстов их лицензий доступен по запросу на okoteam.top@gmail.com. Права на open-source компоненты регулируются соответствующими лицензиями и не ограничиваются настоящим документом в пределах, установленных этими лицензиями.</p>`},
{h:'Обновления и совместимость', b:
`<p>Лицензиар вправе публиковать обновления ПО с исправлениями ошибок, улучшениями и новыми возможностями. Некоторые функции могут требовать новой версии ПО и/или совместимого оборудования. Прекращение поддержки устаревших версий не является нарушением Лицензии; актуальная версия ПО доступна для установки в официальных магазинах приложений и на сайте okoteam.top.</p>`},
{h:'Данные и телеметрия', b:
`<p>ПО может собирать техническую информацию (тип устройства, версия ОС, идентификатор установки, логи сбоев) для обеспечения работоспособности и улучшения качества. Обработка персональных данных производится в соответствии с Политикой конфиденциальности. Пользователь может отключить необязательную телеметрию в настройках приложения.</p>`},
{h:'Гарантии и ответственность', b:
`<p>ПО предоставляется <b>«как есть»</b> и <b>«как доступно»</b>. Лицензиар не гарантирует полного отсутствия ошибок, беспрерывной работы или соответствия ПО конкретным ожиданиям Пользователя. В максимально допустимой законом степени Лицензиар не несёт ответственности за косвенные, случайные или последующие убытки, включая упущенную выгоду.</p>
<p>Совокупная ответственность Лицензиара за прямые убытки ограничена суммой платежей Пользователя за подписки за последние 3 календарных месяца.</p>`},
{h:'Прекращение действия', b:
`<p>Лицензия действует до её прекращения. Пользователь может прекратить Лицензию в любой момент, удалив ПО со всех устройств. Лицензиар вправе прекратить Лицензию при существенном нарушении её условий с направлением уведомления по электронной почте, привязанной к аккаунту. После прекращения Лицензии Пользователь обязан прекратить использование ПО и удалить его копии.</p>`},
{h:'Применимое право', b:
`<p>К настоящей Лицензии применяется законодательство Российской Федерации. Споры разрешаются в порядке, установленном Пользовательским соглашением. Недействительность отдельного положения Лицензии не влечёт недействительности остальных.</p>`}
]},
en: { title:'Software Licence Agreement', rev:'Revision No. 2', secs:[
{h:'Parties and Subject', b:
`<p>This licence agreement (the <b>"Licence"</b>) governs the terms of use of the "OKO" software — a mobile and web application, including updates, additional modules, documentation and related services (the <b>"Software"</b>).</p>
<p>Licensor: Sole Proprietor Ilyasov Daniel Albertovich (Taxpayer ID 682016634349, Moscow, Russian Federation; representative office: Dubai, UAE). By installing, copying or otherwise using the Software, the User accepts this Licence.</p>`},
{h:'Rights Granted', b:
`<p>The Licensor grants the User a simple (non-exclusive) royalty-free licence to:</p>
<ul>
<li>install and run the Software on an unlimited number of devices owned by the User;</li>
<li>use the Software for its intended purpose for personal and professional use;</li>
<li>receive Software updates and patches published by the Licensor;</li>
<li>use the free features of the Software without limits; paid features — while a subscription is active.</li>
</ul>
<p>Territory: worldwide. Term: for the period during which the corresponding version of the Software is publicly available.</p>`},
{h:'Restrictions', b:
`<p>The User is prohibited from:</p>
<ul>
<li>distributing, selling, renting or sublicensing the Software to third parties;</li>
<li>decompiling, disassembling or modifying the source or binary code of the Software, except as expressly permitted by law;</li>
<li>removing or altering copyright notices, trademarks and other markings of the rights holder;</li>
<li>using the Software to circumvent technical protection measures or to hack or attack the Service or third-party infrastructure;</li>
<li>creating derivative products that reproduce a substantial part of the functionality or interface of the Software.</li>
</ul>`},
{h:'Open-source Components', b:
`<p>The Software may include open-source components distributed under MIT, Apache-2.0, BSD, ISC, LGPL and other compatible licences. A full list of such components and their licence texts is available upon request at okoteam.top@gmail.com. Rights to open-source components are governed by the corresponding licences and are not restricted by this document beyond the extent permitted by those licences.</p>`},
{h:'Updates and Compatibility', b:
`<p>The Licensor may publish Software updates with bug fixes, improvements and new features. Some features may require a new Software version and/or compatible hardware. Discontinuation of support for outdated versions is not a breach of the Licence; the current Software version is available for installation from the official app stores and at okoteam.top.</p>`},
{h:'Data and Telemetry', b:
`<p>The Software may collect technical information (device type, OS version, installation identifier, crash logs) to ensure operability and improve quality. Processing of personal data is carried out in accordance with the Privacy Policy. The User may opt out of optional telemetry in the app settings.</p>`},
{h:'Warranties and Liability', b:
`<p>The Software is provided <b>"as is"</b> and <b>"as available"</b>. The Licensor does not warrant the complete absence of errors, uninterrupted operation, or the Software's fitness for a User's specific expectations. To the maximum extent permitted by law, the Licensor is not liable for indirect, incidental or consequential damages, including lost profits.</p>
<p>The Licensor's aggregate liability for direct damages is limited to the amount of the User's subscription payments over the last 3 calendar months.</p>`},
{h:'Termination', b:
`<p>The Licence remains in force until terminated. The User may terminate the Licence at any time by removing the Software from all devices. The Licensor may terminate the Licence upon a material breach of its terms by sending a notice to the e-mail linked to the account. Upon termination, the User must cease using the Software and destroy its copies.</p>`},
{h:'Governing Law', b:
`<p>This Licence is governed by the laws of the Russian Federation. Disputes are resolved in the manner set out in the Terms of Service. The invalidity of any individual provision of the Licence does not invalidate the remaining provisions.</p>`}
]}
},

/* ============ СОГЛАСИЕ НА ОБРАБОТКУ ПЕРСОНАЛЬНЫХ ДАННЫХ ============ */
consent: {
ru: { title:'Согласие на обработку персональных данных', rev:'Редакция № 4', secs:[
{h:'Субъект и Оператор', b:
`<p>Настоящим я, дееспособное физическое лицо — пользователь приложения OKO (далее — <b>«Субъект»</b>), действуя своей волей и в своём интересе, в соответствии со ст. 9 Федерального закона от 27.07.2006 № 152-ФЗ «О персональных данных» даю согласие на обработку моих персональных данных Оператору — <b>Индивидуальному предпринимателю Ильясову Даниэлю Альбертовичу</b> (проект «ОКО PROJECT», ИНН 682016634349, г. Москва, Российская Федерация; представительство: г. Дубай, ОАЭ), e-mail: okoteam.top@gmail.com.</p>
<p>Согласие даётся в момент регистрации в Сервисе путём проставления соответствующей отметки и является конкретным, предметным, информированным, сознательным и однозначным.</p>`},
{h:'Цели обработки', b:
`<p>Персональные данные обрабатываются в следующих целях:</p>
<ul>
<li>регистрация, аутентификация и ведение аккаунта в Сервисе;</li>
<li>исполнение Публичной оферты: подписки, сделки Биржи, эскроу, вывод средств, партнёрские начисления;</li>
<li>обеспечение связи, поддержки и обратной связи с Субъектом;</li>
<li>модерация контента, обеспечение безопасности и предотвращение мошенничества;</li>
<li>улучшение Сервиса на основе обезличенной аналитики;</li>
<li>направление информационных и — при отдельном согласии — рекламных сообщений.</li>
</ul>`},
{h:'Перечень персональных данных', b:
`<p>Субъект даёт согласие на обработку следующих персональных данных:</p>
<ul>
<li>фамилия, имя, отчество (при указании), отображаемое имя и никнейм;</li>
<li>адрес электронной почты и номер телефона;</li>
<li>дата рождения, изображение профиля (аватар), сведения «о себе», интересы;</li>
<li>идентификаторы входа (Telegram ID, Google, Apple) и данные аккаунта;</li>
<li>платёжные метаданные (суммы, даты, статусы и идентификаторы операций — без полных номеров карт);</li>
<li>технические данные: IP-адрес, тип устройства и ОС, язык, cookies, журналы событий.</li>
</ul>`},
{h:'Перечень действий и способы обработки', b:
`<p>Согласие распространяется на любые действия (операции) с персональными данными: сбор, запись, систематизацию, накопление, хранение, уточнение (обновление, изменение), извлечение, использование, передачу (предоставление, доступ) в объёме, указанном в разделе 5, обезличивание, блокирование, удаление и уничтожение.</p>
<p>Обработка осуществляется как с использованием средств автоматизации, так и без таковых. Запись, систематизация, накопление, хранение, уточнение и извлечение персональных данных граждан РФ осуществляются с использованием баз данных на территории Российской Федерации (ч. 5 ст. 18 152-ФЗ).</p>`},
{h:'Передача третьим лицам (поручение обработки)', b:
`<p>Субъект соглашается на поручение обработки его персональных данных в минимально необходимом объёме следующим категориям лиц, действующим по поручению Оператора и обязанным соблюдать конфиденциальность:</p>
<ul>
<li>платёжным провайдерам (Lava.top, банковские и криптовалютные процессинги) — для проведения платежей;</li>
<li>поставщикам облачной инфраструктуры и хостинга — для хранения и технической обработки данных;</li>
<li>Telegram — при входе через Telegram-аккаунт;</li>
<li>государственным органам — исключительно по законному запросу.</li>
</ul>
<p>Оператор не продаёт персональные данные и не передаёт их третьим лицам для их собственных маркетинговых целей.</p>`},
{h:'Срок действия и порядок отзыва', b:
`<p>Согласие действует с момента его предоставления в течение всего срока использования Сервиса и <b>3 (три) года</b> после удаления аккаунта либо до достижения целей обработки, если иное не предусмотрено законом (документы об операциях хранятся 5 лет, технические логи — 12 месяцев).</p>
<p>Согласие может быть отозвано в любой момент путём направления письменного заявления на okoteam.top@gmail.com либо через функцию удаления аккаунта в приложении. После отзыва Оператор прекращает обработку и уничтожает (обезличивает) данные в срок, не превышающий 30 дней, за исключением данных, обработка которых продолжается на иных законных основаниях (исполнение договора, требования учёта, защита прав).</p>`},
{h:'Права субъекта и подтверждение', b:
`<p>Субъект подтверждает, что ознакомлен со своими правами, предусмотренными 152-ФЗ (получение сведений об обработке, уточнение, блокирование, удаление данных, отзыв согласия, обжалование в Роскомнадзоре), а также с Политикой конфиденциальности Оператора.</p>
<p>Проставляя отметку о согласии при регистрации, Субъект подтверждает достоверность предоставленных данных и своё согласие с условиями настоящего документа. Настоящее согласие является электронным документом и не требует проставления собственноручной подписи Субъекта.</p>`}
]},
en: { title:'Consent to Personal Data Processing', rev:'Revision No. 4', secs:[
{h:'Data Subject and Operator', b:
`<p>I, a legally capable individual and user of the OKO app (the <b>"Data Subject"</b>), acting of my own free will and in my own interest, in accordance with Article 9 of Russian Federal Law No. 152-FZ "On Personal Data" of 27.07.2006, consent to the processing of my personal data by the Operator — <b>Sole Proprietor Ilyasov Daniel Albertovich</b> ("OKO PROJECT", Taxpayer ID 682016634349, Moscow, Russian Federation; representative office: Dubai, UAE), e-mail: okoteam.top@gmail.com.</p>
<p>This consent is given upon registration in the Service by ticking the relevant box and is specific, informed, conscious and unambiguous.</p>`},
{h:'Purposes of Processing', b:
`<p>Personal data is processed for the following purposes:</p>
<ul>
<li>registration, authentication and maintenance of the Service account;</li>
<li>performance of the Public Offer: subscriptions, Marketplace transactions, escrow, withdrawals, affiliate accruals;</li>
<li>communication, support and feedback with the Data Subject;</li>
<li>content moderation, security and fraud prevention;</li>
<li>improving the Service based on anonymised analytics;</li>
<li>sending service messages and — with separate consent — marketing messages.</li>
</ul>`},
{h:'Categories of Personal Data', b:
`<p>The Data Subject consents to the processing of the following personal data:</p>
<ul>
<li>surname, first name, patronymic (if provided), display name and nickname;</li>
<li>e-mail address and phone number;</li>
<li>date of birth, profile picture (avatar), "about" information, interests;</li>
<li>sign-in identifiers (Telegram ID, Google, Apple) and account data;</li>
<li>payment metadata (amounts, dates, statuses and transaction identifiers — without full card numbers);</li>
<li>technical data: IP address, device and OS type, language, cookies, event logs.</li>
</ul>`},
{h:'Actions and Methods of Processing', b:
`<p>This consent covers any actions (operations) with personal data: collection, recording, systematisation, accumulation, storage, clarification (updating, modification), retrieval, use, transfer (provision, access) to the extent set out in Section 5, anonymisation, blocking, deletion and destruction.</p>
<p>Processing is carried out both by automated means and without them. Recording, systematisation, accumulation, storage, clarification and retrieval of personal data of Russian citizens are performed using databases located in the Russian Federation (Art. 18(5) of Law 152-FZ).</p>`},
{h:'Transfer to Third Parties (Processing Instruction)', b:
`<p>The Data Subject consents to instructing the processing of their personal data, to the minimum necessary extent, to the following categories of persons acting on the Operator's behalf and bound by confidentiality:</p>
<ul>
<li>payment providers (Lava.top, bank and cryptocurrency processors) — to process payments;</li>
<li>cloud infrastructure and hosting providers — to store and technically process data;</li>
<li>Telegram — when signing in with a Telegram account;</li>
<li>public authorities — solely upon a lawful request.</li>
</ul>
<p>The Operator does not sell personal data and does not share it with third parties for their own marketing purposes.</p>`},
{h:'Validity and Withdrawal', b:
`<p>This consent is valid from the moment it is given, throughout the entire period of use of the Service and for <b>3 (three) years</b> after account deletion, or until the purposes of processing are achieved, unless otherwise provided by law (transaction records are kept for 5 years, technical logs for 12 months).</p>
<p>The consent may be withdrawn at any time by sending a written request to okoteam.top@gmail.com or via the account deletion feature in the app. After withdrawal, the Operator ceases processing and destroys (anonymises) the data within no more than 30 days, except for data whose processing continues on other lawful grounds (performance of the agreement, accounting requirements, protection of rights).</p>`},
{h:'Data Subject Rights and Confirmation', b:
`<p>The Data Subject confirms awareness of their rights under Law 152-FZ (obtaining information about processing, rectification, blocking, deletion of data, withdrawal of consent, lodging a complaint with Roskomnadzor), as well as familiarity with the Operator's Privacy Policy.</p>
<p>By ticking the consent box during registration, the Data Subject confirms the accuracy of the data provided and their agreement with the terms of this document. This consent is an electronic document and does not require the Data Subject's handwritten signature.</p>`}
]}
}
};

/* ================================================================
   HUB — сетка карточек «Документов OKO»
   ================================================================ */
function lgHubHtml(lang){
  const r = LG_REQ[lang];
  const intro = lang==='en'
    ? {t:'OKO Legal', s:'The full set of documents governing your use of the OKO service and platform. Available in Russian and English. Each document opens as a separate page with a table of contents, an operator seal, and a signature. Save any document as PDF via the download button.'}
    : {t:'Документы OKO', s:'Полный набор документов, регулирующих использование сервиса и платформы OKO. Доступны на русском и английском. Каждый документ открывается отдельной страницей — с оглавлением, официальной печатью и подписью. Любой документ можно сохранить в PDF по кнопке скачивания.'};
  const opCaption = lang==='en' ? 'Operator' : 'Оператор';
  const cards = LG_HUB_CARDS.map(c=>{
    const d = LG_DOCS[c.k][lang];
    const l = c[lang];
    return `<button class="lg-card" type="button" onclick="openLegalDoc('${c.k}')" aria-label="${l.t}">
      <span class="lg-card-ver">${d.rev.replace('Редакция №','v').replace('Revision No.','v').trim()}</span>
      <span class="lg-card-ico">${I(c.ico||'file')}</span>
      <span class="lg-card-t">${l.t}</span>
      <span class="lg-card-s">${l.s}</span>
    </button>`;
  }).join('');
  return `<div class="lg-hub">
    <div class="lg-hub-intro">
      <h2>${intro.t}</h2>
      <p>${intro.s}</p>
      <div class="lg-hub-op"><b>${opCaption}:</b> ${r.op} · ${r.inn} · ${r.geo} · ${r.mail}</div>
    </div>
    <div class="lg-hub-grid">${cards}</div>
  </div>`;
}

/* ================================================================
   DOC — рендер одной юр-страницы
   ================================================================ */
function lgDocHtml(kind, lang){
  const d = LG_DOCS[kind][lang];
  const r = LG_REQ[lang];
  const opLabel   = lang==='en' ? 'Operator'  : 'Оператор';
  const tocLabel  = lang==='en' ? 'Contents'  : 'Содержание';
  const reqLabel  = lang==='en' ? 'Operator details' : 'Реквизиты Оператора';
  const topLabel  = lang==='en' ? 'to contents' : 'к содержанию';
  const revLabel  = lang==='en' ? d.rev + ' · effective ' + r.date : d.rev + ' · дата вступления в силу: ' + r.date;
  const updLabel  = lang==='en' ? 'Updated ' + r.date : 'Обновлено ' + r.date;
  const pdfLabel  = lang==='en' ? 'Download PDF' : 'Скачать PDF';
  const dlLabel   = lang==='en' ? 'Download HTML' : 'Скачать HTML';
  const shLabel   = lang==='en' ? 'Share link'    : 'Поделиться ссылкой';
  const cnT       = lang==='en' ? 'I have read and I agree' : 'Прочитано, согласен';
  const cnS       = lang==='en' ? 'The consent is saved locally and used at registration.' : 'Согласие сохраняется локально и учитывается при регистрации.';
  const sid = (i)=>'lg-s-'+kind+'-'+i;
  const toc = `<nav class="lg-toc" aria-label="${tocLabel}">
      <div class="lg-toc-h">${I('file')}<b>${tocLabel}</b></div>
      <ol>${d.secs.map((s,i)=>`<li><button type="button" class="lg-toc-a" onclick="lgJump('${sid(i)}')"><span class="lg-toc-n">${i+1}</span><span>${s.h}</span></button></li>`).join('')}</ol>
    </nav>`;
  const agreed = lgIsAgreed(kind);
  return `<div class="lg-doc" id="lgDoc">
    <div class="lg-progress" aria-hidden="true"><span id="lgProgFill"></span></div>
    <div class="lg-doc-head">
      <svg class="lg-doc-logo"><use href="#i-logo"/></svg>
      <div>
        <h1>${d.title}</h1>
        <div class="lg-doc-meta"><b>OKO PROJECT</b> · ${revLabel}</div>
        <div class="lg-doc-meta">${opLabel}: ${r.op}, ${r.inn}</div>
        <span class="lg-updated">${I('check')} ${updLabel}</span>
      </div>
    </div>
    <div class="lg-actions">
      <button type="button" class="lg-btn-pdf" onclick="lgDownloadPdf()" title="${pdfLabel}">${I('file')}<span>${pdfLabel}</span></button>
      <button type="button" onclick="lgDownloadHtml()" title="${dlLabel}">${I('copy')}<span>${dlLabel}</span></button>
      <button type="button" onclick="lgShareLink()" title="${shLabel}">${I('share')}<span>${shLabel}</span></button>
    </div>
    ${toc}
    ${d.secs.map((s,i)=>`<section class="lg-sec" id="${sid(i)}"><h2><span class="lg-sec-n">${i+1}.</span> ${s.h}<button type="button" class="lg-sec-top" onclick="lgJump('lgDoc')" title="${topLabel}" aria-label="${topLabel}">${I('chev')}</button></h2>${s.b}</section>`).join('')}
    <div class="lg-req">
      <div class="lg-req-h">${reqLabel}</div>
      <b>${r.op}</b><br>${r.brand}<br>${r.inn}<br>${r.geo}<br>E-mail: ${r.mail}
    </div>
    <div class="doc-sign-block">
      <div class="lg-seal">${sealSvg(150)}</div>
      <div class="lg-sig-wrap">
        <div class="sig">${signatureImg(180)}</div>
        <div class="lg-sig-line"></div>
        <div class="lg-sig-cap">${r.sig}</div>
      </div>
    </div>
    <button type="button" class="lg-consent ${agreed?'on':''}" id="lgConsent" onclick="lgToggleAgreed()" aria-pressed="${agreed?'true':'false'}">
      <span class="lg-consent-box">${I('check2')||I('check')}</span>
      <span class="lg-consent-txt">
        <span class="lg-consent-t">${cnT}</span>
        <span class="lg-consent-s">${cnS}</span>
      </span>
    </button>
  </div>`;
}

/* ================================================================
   DEAL — договор-акт по конкретной сделке (авто-заполнение)
   ================================================================ */
function lgFmtRub(n){ try{ return Number(n||0).toLocaleString('ru-RU').replace(/,/g,' '); }catch(e){ return String(n||0); } }
function lgFmtDate(d, lang){
  try{
    const dt = d instanceof Date ? d : new Date(d||Date.now());
    if(lang==='en') return dt.toLocaleDateString('en-GB',{day:'2-digit',month:'long',year:'numeric'});
    return dt.toLocaleDateString('ru-RU',{day:'2-digit',month:'long',year:'numeric'}) + ' г.';
  }catch(e){ return ''+d; }
}
function lgDealHtml(ctx, lang){
  const r = LG_REQ[lang];
  const en = lang==='en';
  const buyer   = ctx.buyer || (typeof PROFILE!=='undefined' && PROFILE && PROFILE.name) || (en?'Buyer':'Покупатель');
  const plan    = ctx.plan  || ctx.tier || 'START';
  const period  = ctx.period || 12; // месяцев
  const currency= ctx.currency || 'RUB';
  const amount  = (ctx.amount != null) ? ctx.amount : 0;
  const tx      = ctx.tx || ('OKO-' + Date.now().toString(36).toUpperCase());
  const dt      = ctx.date || new Date();
  const dateStr = lgFmtDate(dt, lang);
  const method  = ctx.method || (en?'bank card':'банковская карта');
  const title = en ? 'Deal certificate' : 'Договор-акт по сделке';
  const subT  = en ? `Confirmation of acceptance of the OKO "${plan}" plan under the Public Offer` : `Подтверждение акцепта тарифа OKO «${plan}» по Публичной оферте`;
  const facts = en ? [
    ['Certificate No.', `<b>${tx}</b>`],
    ['Date', dateStr],
    ['Buyer', buyer],
    ['Plan', `<b>${plan}</b>, ${period} months`],
    ['Amount', `<b>${lgFmtRub(amount)} ${currency}</b>`],
    ['Payment method', method]
  ] : [
    ['№ акта', `<b>${tx}</b>`],
    ['Дата', dateStr],
    ['Покупатель', buyer],
    ['Тариф', `<b>${plan}</b>, ${period} мес.`],
    ['Сумма', `<b>${lgFmtRub(amount)} ${currency==='RUB'?'₽':currency}</b>`],
    ['Способ оплаты', method]
  ];
  const body = en ? `
    <p>This certificate confirms that the Buyer has entered into an agreement with the Operator on the terms of the OKO Public Offer (Revision No. 4) and paid for the <b>${plan}</b> plan for a period of ${period} months. The paid functionality of the plan is activated on the Buyer's account from the date indicated above.</p>
    <p>By making the payment, the Buyer confirms full and unconditional acceptance of the Public Offer and the Refund Policy. The right to a refund is exercised in accordance with the Refund Policy under the conditions set out therein.</p>
    <p>The parties have no mutual claims regarding the fact of payment and activation. The certificate is generated electronically and does not require a handwritten signature of the Buyer.</p>
  ` : `
    <p>Настоящим Оператор подтверждает, что Покупатель заключил договор с Оператором на условиях Публичной оферты сервиса OKO (Редакция № 4) и оплатил тариф <b>${plan}</b> на срок ${period} мес. Платный функционал тарифа активирован на аккаунте Покупателя с даты, указанной выше.</p>
    <p>Внесением оплаты Покупатель подтверждает полный и безоговорочный акцепт Публичной оферты и Политики возврата. Право на возврат осуществляется в порядке и на условиях, установленных Политикой возврата.</p>
    <p>Стороны не имеют взаимных претензий относительно факта оплаты и активации. Акт сформирован электронно и не требует собственноручной подписи Покупателя.</p>
  `;
  const opLabel = en ? 'Operator' : 'Оператор';
  const reqLabel= en ? 'Operator details' : 'Реквизиты Оператора';
  const pdfLabel= en ? 'Download PDF' : 'Скачать PDF';
  const dlLabel = en ? 'Download HTML' : 'Скачать HTML';
  return `<div class="lg-doc" id="lgDoc">
    <div class="lg-progress" aria-hidden="true"><span id="lgProgFill"></span></div>
    <div class="lg-doc-head">
      <svg class="lg-doc-logo"><use href="#i-logo"/></svg>
      <div>
        <h1>${title}</h1>
        <div class="lg-doc-meta"><b>OKO PROJECT</b> · ${subT}</div>
        <div class="lg-doc-meta">${opLabel}: ${r.op}, ${r.inn}</div>
        <span class="lg-updated">${I('check')} ${dateStr}</span>
      </div>
    </div>
    <div class="lg-actions">
      <button type="button" class="lg-btn-pdf" onclick="lgDownloadPdf()" title="${pdfLabel}">${I('file')}<span>${pdfLabel}</span></button>
      <button type="button" onclick="lgDownloadHtml()" title="${dlLabel}">${I('copy')}<span>${dlLabel}</span></button>
    </div>
    <dl class="lg-deal-facts">
      ${facts.map(f=>`<dt>${f[0]}</dt><dd>${f[1]}</dd>`).join('')}
    </dl>
    <section class="lg-sec">
      <h2><span class="lg-sec-n">1.</span> ${en?'Subject and confirmation':'Предмет и подтверждение'}</h2>
      ${body}
    </section>
    <div class="lg-req">
      <div class="lg-req-h">${reqLabel}</div>
      <b>${r.op}</b><br>${r.brand}<br>${r.inn}<br>${r.geo}<br>E-mail: ${r.mail}
    </div>
    <div class="doc-sign-block">
      <div class="lg-seal">${sealSvg(150)}</div>
      <div class="lg-sig-wrap">
        <div class="sig">${signatureImg(180)}</div>
        <div class="lg-sig-line"></div>
        <div class="lg-sig-cap">${r.sig}</div>
      </div>
    </div>
  </div>`;
}

/* ---------- согласие «прочитано» — хранится в localStorage ---------- */
function lgIsAgreed(kind){ try{ return !!(lgS.agreed && lgS.agreed[kind]); }catch(e){ return false; } }
function lgToggleAgreed(){
  if(!lgS.agreed) lgS.agreed = {};
  const cur = !!lgS.agreed[lgKind];
  lgS.agreed[lgKind] = !cur;
  if(!cur) lgS.agreed[lgKind+'_at'] = new Date().toISOString();
  lgSave();
  const el = document.getElementById('lgConsent');
  if(el){ el.classList.toggle('on', !cur); el.setAttribute('aria-pressed', (!cur).toString()); }
}

/* ---------- Скачать PDF — окно печати браузера (Save as PDF) ---------- */
function lgDownloadPdf(){
  try{
    const box = document.getElementById('lgBody');
    if(box) box.scrollTop = 0;
    lgToast(lgS.lang==='en' ? 'Opening print dialog' : 'Открываю окно печати');
    setTimeout(()=>{ try{ window.print(); }catch(e){ console.warn('lgDownloadPdf', e); } }, 240);
  }catch(e){ console.warn('lgDownloadPdf', e); }
}

/* ---------- скачать документ как самодостаточный HTML ---------- */
function lgDownloadHtml(){
  try{
    const doc = document.getElementById('lgDoc');
    if(!doc) return;
    const clone = doc.cloneNode(true);
    ['#lgProgFill','.lg-actions','.lg-consent','.lg-sec-top','.lg-progress'].forEach(sel=>{
      clone.querySelectorAll(sel).forEach(n=>n.remove());
    });
    const css = 'body{font:14px/1.65 -apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;color:#111;background:#fff;max-width:760px;margin:32px auto;padding:0 20px}h1{font-family:Impact,Bebas Neue,sans-serif;letter-spacing:.05em;margin:0 0 6px}h2{color:#0f8b00;font-family:Impact,Bebas Neue,sans-serif;letter-spacing:.06em;margin:26px 0 6px}.lg-doc-head{border:1px solid #0f8b00;background:#fafafa;padding:16px 18px;margin-bottom:22px;display:flex;gap:14px;align-items:flex-start;border-radius:8px}.lg-doc-logo{width:44px;height:44px;color:#0f8b00}.lg-doc-meta{font-size:11.5px;color:#555;margin-top:3px}.lg-updated{display:inline-block;margin-top:8px;padding:4px 10px;border:1px solid #0f8b00;color:#0f8b00;border-radius:99px;font-size:11px;font-weight:800;letter-spacing:.08em;text-transform:uppercase}.lg-toc{background:#f4f4f4;border-left:3px solid #0f8b00;padding:12px 14px;margin:16px 0 22px;border-radius:6px}.lg-toc ol{list-style:decimal;padding-left:22px;margin:0}.lg-toc li{margin:3px 0;font-size:12.5px;color:#333}.lg-toc-a{background:none;border:0;padding:0;text-align:left;color:#333;font:inherit;cursor:default}.lg-sec{margin-bottom:20px}.lg-sec ul{list-style:disc;padding-left:20px}.lg-table-wrap{overflow-x:auto;border:1px solid #999;border-radius:6px}.lg-table{width:100%;border-collapse:collapse;font-size:12px;min-width:520px}.lg-table th,.lg-table td{border:1px solid #999;padding:6px 8px;text-align:left;vertical-align:top}.lg-table th{background:#eee;text-transform:uppercase;font-size:10.5px;letter-spacing:.04em;color:#0f8b00}.lg-note{font-size:11px;color:#666}.lg-req{background:#f4f4f4;border:1px solid #ddd;border-radius:6px;padding:12px 14px;font-size:12.5px;color:#333;margin-top:22px}.lg-req-h{font-family:Impact,Bebas Neue,sans-serif;letter-spacing:.08em;color:#0f8b00;margin-bottom:6px}.doc-sign-block{margin-top:24px;padding-top:16px;border-top:1px dashed #999;display:flex;gap:24px;align-items:flex-end;flex-wrap:wrap}.lg-sig-cap{font-size:11px;color:#555;margin-top:5px}.lg-sig-line{height:1px;background:#999;margin-top:-8px}.lg-seal{width:150px;height:150px}.lg-seal img{width:100%;height:100%;object-fit:contain}.lg-deal-facts{display:grid;grid-template-columns:auto 1fr;gap:8px 14px;background:#f4f4f4;border:1px solid #ddd;border-radius:6px;padding:12px 14px;font-size:12.5px}.lg-deal-facts dt{color:#555}.lg-deal-facts dd{margin:0;font-weight:700}';
    const isDeal = (lgKind === 'deal');
    const title = isDeal ? (lgS.lang==='en' ? 'OKO — Deal Certificate' : 'OKO — Договор-акт по сделке') : (LG_DOCS[lgKind][lgS.lang].title + ' — OKO');
    const iso = new Date().toISOString().slice(0,10);
    const fnKind = isDeal ? 'deal' : lgKind;
    const fn = 'oko-'+fnKind+'-'+lgS.lang+'-'+iso+'.html';
    const html = '<!doctype html><html lang="'+lgS.lang+'"><head><meta charset="utf-8"><title>'+title+'</title><meta name="viewport" content="width=device-width,initial-scale=1"><style>'+css+'</style></head><body>'+clone.outerHTML+'</body></html>';
    const blob = new Blob([html], {type:'text/html;charset=utf-8'});
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url; a.download = fn; document.body.appendChild(a); a.click();
    setTimeout(()=>{ URL.revokeObjectURL(url); a.remove(); }, 800);
    lgToast(lgS.lang==='en' ? 'Downloaded' : 'Файл скачан');
  }catch(e){ console.warn('lgDownloadHtml', e); }
}

/* ---------- поделиться ссылкой ---------- */
function lgShareLink(){
  try{
    const slug = { offer:'oferta', privacy:'privacy', terms:'terms', consent:'consent', refund:'refund', license:'license' }[lgKind] || lgKind;
    const url  = 'https://okoteam.top/legal/' + slug;
    const d    = LG_DOCS[lgKind] && LG_DOCS[lgKind][lgS.lang];
    const title= (d && d.title) || 'OKO Legal';
    const done = ()=> lgToast(lgS.lang==='en' ? 'Link copied' : 'Ссылка скопирована');
    if(navigator.share){
      navigator.share({ title:'OKO — '+title, text:title, url }).catch(()=>{
        if(navigator.clipboard) navigator.clipboard.writeText(url).then(done).catch(()=>{});
      });
    } else if(navigator.clipboard){
      navigator.clipboard.writeText(url).then(done).catch(()=>{});
    } else {
      const t = document.createElement('textarea'); t.value = url;
      document.body.appendChild(t); t.select();
      try{ document.execCommand('copy'); done(); }catch(e){}
      t.remove();
    }
  }catch(e){ console.warn('lgShareLink', e); }
}

/* мини-тост (использует core toast если есть, иначе временный чип) */
function lgToast(msg){
  try{ if(typeof toast==='function'){ toast(msg); return; } }catch(e){}
  try{
    let t = document.getElementById('lgToast');
    if(!t){
      t = document.createElement('div'); t.id = 'lgToast';
      t.style.cssText = 'position:fixed;left:50%;bottom:24px;transform:translateX(-50%);background:var(--raised,#141414);color:var(--accent,#9AFF00);border:1px solid var(--accent,#9AFF00);padding:8px 16px;border-radius:99px;font:600 12.5px/1 Montserrat,sans-serif;letter-spacing:.04em;z-index:9999;box-shadow:0 6px 24px rgba(0,0,0,.35);opacity:0;transition:opacity .2s';
      document.body.appendChild(t);
    }
    t.textContent = msg; t.style.opacity = '1';
    clearTimeout(t._to); t._to = setTimeout(()=>{ t.style.opacity = '0'; }, 1600);
  }catch(e){}
}

/* плавный скролл к секции внутри контейнера документа */
function lgJump(id){
  const el = document.getElementById(id);
  const box = document.getElementById('lgBody');
  if(!el || !box) return;
  const reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  const r1 = el.getBoundingClientRect(), r2 = box.getBoundingClientRect();
  const top = box.scrollTop + (r1.top - r2.top) - 14;
  box.scrollTo({top: top<0?0:top, behavior: reduce ? 'auto' : 'smooth'});
  el.classList.add('lg-flash');
  setTimeout(()=>el.classList.remove('lg-flash'), 900);
}

/* ---------- scroll-spy: подсветка активного пункта оглавления при прокрутке ---------- */
let _lgSpyBound = false;
function lgSpyUpdate(){
  try{
    const box = document.getElementById('lgBody');
    const doc = document.getElementById('lgDoc');
    if(!box || !doc) return;
    const secs = doc.querySelectorAll('.lg-sec');
    const links = doc.querySelectorAll('.lg-toc-a');
    if(secs.length && links.length){
      const bt = box.getBoundingClientRect().top;
      let cur = 0;
      secs.forEach((s,i)=>{ if(s.getBoundingClientRect().top - bt <= 90) cur = i; });
      links.forEach((a,i)=>a.classList.toggle('on', i===cur));
    }
    const fill = document.getElementById('lgProgFill');
    if(fill){
      const max = Math.max(1, box.scrollHeight - box.clientHeight);
      const p = Math.max(0, Math.min(100, (box.scrollTop / max) * 100));
      fill.style.width = p.toFixed(2) + '%';
    }
  }catch(e){}
}
function lgBindSpy(){
  const box = document.getElementById('lgBody');
  if(!box || _lgSpyBound) return;
  _lgSpyBound = true;
  let tick = false;
  box.addEventListener('scroll', ()=>{
    if(tick) return; tick = true;
    requestAnimationFrame(()=>{ lgSpyUpdate(); tick = false; });
  }, {passive:true});
}

/* ---------- рендер (hub / doc / deal) ---------- */
function lgRender(){
  const v = document.getElementById('legalView');
  const head = document.getElementById('lgHeadTitle');
  const ru = document.getElementById('lgLangRu'), en = document.getElementById('lgLangEn');
  if(ru) ru.classList.toggle('on', lgS.lang==='ru');
  if(en) en.classList.toggle('on', lgS.lang==='en');
  const tabs = document.getElementById('lgTabs');
  const body = document.getElementById('lgBody');
  if(!v || !body) return;

  const isHub = !lgKind;
  const isDeal = (lgKind === 'deal');
  v.classList.toggle('hub',  isHub);
  v.classList.toggle('doc', !isHub);

  if(isHub){
    if(head) head.textContent = lgS.lang==='en' ? 'OKO Legal' : 'Документы OKO';
    if(tabs) tabs.innerHTML = '';
    body.innerHTML = lgHubHtml(lgS.lang);
    body.scrollTop = 0;
    return;
  }

  /* режим doc / deal */
  if(head){
    if(isDeal){
      head.textContent = lgS.lang==='en' ? 'Deal certificate' : 'Договор-акт';
    } else {
      const d = LG_DOCS[lgKind][lgS.lang];
      head.textContent = d.title;
    }
  }
  if(tabs){
    // табы показываем только для «обычных» документов; для сделки — скрываем
    if(isDeal){
      tabs.innerHTML = '';
    } else {
      tabs.innerHTML = LG_TABS.map(t0=>
        `<button type="button" class="lg-tab ${t0.k===lgKind?'on':''}" onclick="lgGo('${t0.k}')">${I('file')} ${t0[lgS.lang]||t0.ru}</button>`).join('');
    }
  }
  body.innerHTML = isDeal ? lgDealHtml(lgDealCtx||{}, lgS.lang) : lgDocHtml(lgKind, lgS.lang);
  body.scrollTop = 0;
  lgBindSpy(); lgSpyUpdate();
}

/* ---------- публичное API ---------- */
function openLegalHub(){
  lgKind = null; lgDealCtx = null;
  lgRender();
  const v = document.getElementById('legalView'); if(v) v.classList.add('open');
}
function openLegalDoc(kind){
  if(!LG_DOCS[kind]){ return openLegalHub(); }
  lgKind = kind; lgDealCtx = null;
  lgRender();
  const v = document.getElementById('legalView'); if(v) v.classList.add('open');
}
function openLegalDeal(ctx){
  lgKind = 'deal'; lgDealCtx = ctx || {};
  lgRender();
  const v = document.getElementById('legalView'); if(v) v.classList.add('open');
}
function closeLegalDoc(){
  const v = document.getElementById('legalView');
  if(v) v.classList.remove('open');
}
function lgBackToHub(){ lgKind = null; lgDealCtx = null; lgRender(); }
function lgGo(kind){ if(LG_DOCS[kind]){ lgKind = kind; lgDealCtx = null; lgRender(); } }
function lgSetLang(l){
  if(l!=='ru' && l!=='en') return;
  lgS.lang = l; lgSave(); lgRender();
}

/* строка юр-ссылок на экране входа */
function lgRenderAuthLegal(l){
  const al = document.querySelector('.auth-legal');
  if(!al) return;
  const en = l==='en';
  const pre  = en ? 'By continuing you accept the ' : 'Продолжая, ты принимаешь ';
  const mid  = en ? ' and the ' : ' и ';
  const terms= en ? 'Terms of Service' : 'условия сервиса';
  const priv = en ? 'Privacy Policy' : 'политику конфиденциальности';
  al.innerHTML = `${pre}<span class="lg-a" onclick="openLegalDoc('terms')">${terms}</span>${mid}<span class="lg-a" onclick="openLegalDoc('privacy')">${priv}</span>`;
}

/* ---------- самоинициализация ---------- */
(function lgInit(){
  /* а) строка «Документы OKO» в профиле — перед «Выйти» */
  try{
    const rows = document.querySelectorAll('#screen-profile .prow');
    let logoutRow = null;
    rows.forEach(r=>{ if((r.getAttribute('onclick')||'').indexOf('doLogout')>-1) logoutRow = r; });
    if(logoutRow && !document.getElementById('prowLegal')){
      const b = document.createElement('button');
      b.className = 'prow'; b.id = 'prowLegal';
      b.innerHTML = `${I('file')} Документы OKO <span class="chev">${I('chev')}</span>`;
      b.onclick = ()=>openLegalHub();
      logoutRow.parentNode.insertBefore(b, logoutRow);
    }
  }catch(e){}

  /* б) тайл в хабе «Мини-аппы» */
  if(typeof addSvcTile==='function'){
    addSvcTile({id:'legal', label:'Документы', ico:'file', onclick:()=>openLegalHub()});
  }

  /* в) кликабельные ссылки на экране входа */
  try{ lgRenderAuthLegal(typeof LANG!=='undefined'?LANG:'ru'); }catch(e){}

  /* следовать за глобальным переключением языка */
  if(typeof onLangChange==='function'){
    onLangChange(l=>{
      if(l==='ru'||l==='en'){
        lgS.lang = l; lgSave();
        const v = document.getElementById('legalView');
        if(v && v.classList.contains('open')) lgRender();
        try{ lgRenderAuthLegal(l); }catch(e){}
      }
    });
  }

  /* i18n-регистрация подписей */
  if(typeof regT==='function'){
    regT({ 'lg.docs': {ru:'Документы OKO', en:'OKO Legal'} });
  }

  /* г) авто-открытие договора-акта после успешной оплаты подписки.
     Обёртываем doPay откладывая до полной загрузки всех модулей (paywall/wallet/games тоже могут его перехватывать). */
  setTimeout(function(){
    try{
      if(typeof doPay !== 'function') return;
      const _prevDoPay = doPay;
      window.doPay = function(){
        const st = (typeof payState !== 'undefined') ? payState : null;
        let plan  = 'START', period = 12, amount = 0, method = 'card';
        try{
          if(st){
            plan   = st.plan   || plan;
            period = st.period || period;
            method = st.method || method;
            if(typeof payPrice === 'function'){ const pp = payPrice(); if(pp && pp.total != null) amount = pp.total; }
            if(!amount && typeof PLANS !== 'undefined' && PLANS[plan]){
              amount = (PLANS[plan].price||0) * period;
            }
          }
        }catch(e){}
        const r = _prevDoPay.apply(this, arguments);
        /* открываем договор после закрытия sheet «Оплата» с задержкой,
           чтобы не мешать анимации «Оплата успешна». */
        try{
          setTimeout(function(){
            const buyer = (typeof PROFILE !== 'undefined' && PROFILE && PROFILE.name) ? PROFILE.name : 'Покупатель';
            const methodLbl = ({card:'банковская карта РФ', crypto:'криптовалюта', lava:'Lava.top'})[method] || method;
            openLegalDeal({buyer, plan, period, amount, currency:'RUB', method:methodLbl});
          }, 2600);
        }catch(e){}
        return r;
      };
    }catch(e){}
  }, 1200);
})();

/* ===== module: verify-stickers ===== */
/* ============================================================
   МОДУЛЬ verify-stickers (vs-)
   1) Верификация аккаунта (синяя галочка): строка в профиле,
      sheet со статусом и условиями, заявка -> модерация -> бейдж.
   2) Галочки у имён в профиле / ленте / списке чатов (chain-патчи).
   3) Премиум-стикеры «TON Crystal» (пак pack:'ton', гейт PRO/BUSINESS),
      дождь кристаллов при отправке, стикер к новому посту.
   ============================================================ */

/* ---------- 1. ВЕРИФИКАЦИЯ ---------- */
let VS_VERIFY = { status: 'none', name: null }; /* none | pending | approved */
function vsSave(){ try{ localStorage.setItem('oko-verify', JSON.stringify(VS_VERIFY)); }catch(e){} }
(function vsRestore(){
  try{
    const d = JSON.parse(localStorage.getItem('oko-verify') || 'null');
    if(d && d.status){
      VS_VERIFY = d;
      if(d.status === 'pending') VS_VERIFY.status = 'none'; /* заявка не переживает перезагрузку */
      if(d.status === 'approved'){
        if(typeof VERIFIED !== 'undefined'){ VERIFIED.add(PROFILE.name); if(d.name) VERIFIED.add(d.name); }
      }
    }
  }catch(e){}
  /* синхронизация: если аккаунт уже в VERIFIED (галочка у имени),
     статус верификации не может быть «нет» — иначе противоречие в UI */
  if(VS_VERIFY.status === 'none' && typeof VERIFIED !== 'undefined' && VERIFIED.has(PROFILE.name)){
    VS_VERIFY = { status: 'approved', name: PROFILE.name };
    vsSave();
  }
})();

function vsBadgeHtml(){
  return `<span class="vs-badge" title="Верифицированный аккаунт"><svg viewBox="0 0 100 100" aria-hidden="true"><use href="#i-verified"/></svg></span>`;
}
function vsPremiumOk(){
  return /PRO|BUSINESS/.test(PROFILE.tier || '') || (typeof isOwner === 'function' && isOwner());
}

/* строка «Верификация» в карточке настроек профиля (перед админкой) */
function vsInsertProw(){
  if(document.getElementById('vsProwVerify')) return;
  const anchor = document.getElementById('prowAdmin');
  if(!anchor || !anchor.parentNode) return;
  const b = document.createElement('button');
  b.className = 'prow'; b.id = 'vsProwVerify';
  b.onclick = vsOpenVerify;
  b.innerHTML = `<svg class="i"><use href="#i-verified"/></svg> Верификация <span class="vs-chip" id="vsProwChip" style="margin-left:auto">нет</span> <span class="chev" style="margin-left:0"><svg class="i"><use href="#i-chev"/></svg></span>`;
  anchor.parentNode.insertBefore(b, anchor);
  vsUpdateProw();
}
function vsUpdateProw(){
  const c = document.getElementById('vsProwChip'); if(!c) return;
  const st = VS_VERIFY.status;
  c.className = 'vs-chip' + (st === 'approved' ? ' on' : st === 'pending' ? ' wait' : '');
  c.style.marginLeft = 'auto';
  c.textContent = st === 'approved' ? 'есть' : st === 'pending' ? 'проверка' : 'нет';
}

function vsOpenVerify(){ vsRenderVerify(); openSheet('vs-verify'); }
function vsRenderVerify(){
  const v = document.getElementById('vsVerifyView'); if(!v) return;
  const st = VS_VERIFY.status;
  const meta = {
    none:     { cls:'',        ico:'user',     t:'Не верифицирован', s:'Синяя галочка подтверждает подлинность аккаунта для всех в OKO' },
    pending:  { cls:'pending', ico:'clock',    t:'На рассмотрении',  s:'Модерация проверяет аккаунт и активность, обычно до 24 часов' },
    approved: { cls:'ok',      ico:'verified', t:'Верифицирован',    s:'Аккаунт подтверждён, галочка видна в профиле, ленте и чатах' },
  }[st];
  const conds = `
    <div class="vs-cond">${I('crown')}<div>Тариф PRO или BUSINESS<small>Активная подписка на момент подачи и проверки заявки</small></div></div>
    <div class="vs-cond">${I('bolt')}<div>Живой аккаунт<small>30+ дней в OKO, регулярные посты и реальная активность</small></div></div>
    <div class="vs-cond">${I('briefcase')}<div>Либо официальный бизнес<small>Подтверждение юрлица или ИП документами — без требования к тарифу</small></div></div>`;
  const action = st === 'none'
    ? `<button class="btn" style="width:100%;margin-top:14px" onclick="vsApply()"><svg class="i"><use href="#i-verified"/></svg> Подать заявку</button>`
    : st === 'pending'
    ? `<div class="vs-progress"><i></i></div><button class="btn ghost" style="width:100%;opacity:.65" disabled>Проверяем аккаунт…</button>`
    : `<button class="btn ghost" style="width:100%;margin-top:14px" onclick="closeSheet()">Отлично</button>`;
  v.innerHTML = `
    <div class="vs-vcard ${meta.cls}">
      <div class="vs-vic">${I(meta.ico)}</div>
      <div><b>${meta.t}${st==='approved' ? vsBadgeHtml() : ''}</b><small>${meta.s}</small></div>
    </div>
    <h2 class="section-h" style="margin:0 0 2px">Условия</h2>
    ${conds}
    ${action}`;
}

function vsApply(){
  if(VS_VERIFY.status !== 'none') return;
  VS_VERIFY.status = 'pending'; vsSave();
  vsUpdateProw(); vsRenderVerify();
  toast('Заявка отправлена на модерацию');
  setTimeout(()=>{
    if(VS_VERIFY.status !== 'pending') return;
    if(vsPremiumOk()){
      VS_VERIFY.status = 'approved'; VS_VERIFY.name = PROFILE.name; vsSave();
      if(typeof VERIFIED !== 'undefined') VERIFIED.add(PROFILE.name);
      if(typeof showPopup === 'function') showPopup({
        ico:'verified', title:'Аккаунт верифицирован',
        body:`Поздравляем, ${esc(PROFILE.name)}! Синяя галочка теперь рядом с твоим именем — в профиле, ленте и чатах.`,
        actions:[{label:'Красота'}]
      });
      if(typeof renderMyProfile === 'function') renderMyProfile();
      vsDecorateAll();
    } else {
      VS_VERIFY.status = 'none'; vsSave();
      if(typeof showPopup === 'function') showPopup({
        ico:'lock', title:'Заявка отклонена',
        body:'Для верификации нужен активный тариф PRO или BUSINESS либо подтверждённый официальный бизнес. Оформи PRO, и подай заявку снова.',
        actions:[{label:'Оформить PRO', onclick:()=>openPay('PRO')},{label:'Позже', ghost:true}]
      });
    }
    vsUpdateProw(); vsRenderVerify();
  }, 5000);
}

/* ---------- 2. ГАЛОЧКИ У ИМЁН (DOM-декораторы + chain-патчи) ---------- */
function vsDecorateProfile(){
  if(typeof VERIFIED === 'undefined') return;
  const el = document.getElementById('profName');
  if(el && VERIFIED.has(PROFILE.name) && !el.querySelector('.vs-badge'))
    el.insertAdjacentHTML('beforeend', vsBadgeHtml());
}
function vsDecorateFeed(){
  if(typeof VERIFIED === 'undefined') return;
  document.querySelectorAll('#feedList .post .head .name').forEach(el=>{
    if(el.querySelector('.vs-badge')) return;
    const tn = el.firstChild;
    if(!tn || tn.nodeType !== Node.TEXT_NODE) return;
    if(VERIFIED.has(tn.textContent.trim())){
      const sp = document.createElement('span');
      sp.className = 'vs-badge'; sp.title = 'Верифицированный аккаунт';
      sp.innerHTML = '<svg viewBox="0 0 100 100" aria-hidden="true"><use href="#i-verified"/></svg>';
      el.insertBefore(sp, tn.nextSibling); /* галочка сразу после имени, до чипов «Реклама/В тренде» */
    }
  });
}
function vsDecorateChats(){
  if(typeof VERIFIED === 'undefined') return;
  document.querySelectorAll('#chatList .chat-item .row1 .name').forEach(el=>{
    if(el.querySelector('.vs-badge')) return;
    const name = Array.from(el.childNodes).filter(n=>n.nodeType===Node.TEXT_NODE).map(n=>n.textContent).join('').trim();
    if(VERIFIED.has(name)) el.insertAdjacentHTML('beforeend', vsBadgeHtml());
  });
}
function vsDecorateAll(){ vsDecorateProfile(); vsDecorateFeed(); vsDecorateChats(); }

const _prevRenderMyProfileVs = renderMyProfile;
renderMyProfile = function(){
  _prevRenderMyProfileVs();
  vsInsertProw(); vsUpdateProw(); vsDecorateProfile();
};
const _prevRenderFeedVs = renderFeed;
renderFeed = function(kind){
  _prevRenderFeedVs(kind);
  vsDecorateFeed();
};
const _prevRenderChatListVs = renderChatList;
renderChatList = function(filter){
  _prevRenderChatListVs(filter === undefined ? '' : filter);
  vsDecorateChats();
};

/* ---------- 3. ПРЕМИУМ-СТИКЕРЫ «TON CRYSTAL» ---------- */
const VS_TON_START = STICKERS.length;
STICKERS.push(
  { ic:'vs-gem',    label:'Кристалл',    hue:200, pack:'ton', premium:true },
  { ic:'vs-coin',   label:'Тонкоин',     hue:200, pack:'ton', premium:true },
  { ic:'vs-rocket', label:'Ту зе мун',   hue:200, pack:'ton', premium:true },
  { ic:'vs-eye',    label:'OKO Crystal', hue:200, pack:'ton', premium:true },
  { ic:'vs-bolt',   label:'Разряд',      hue:200, pack:'ton', premium:true },
);

/* дорогая SVG-графика TON: градиенты, блики, уникальные id на каждый рендер */
let vsUid = 0;
function vsTonSvg(key, size){
  const u = 'vsg' + (vsUid++);
  return `<span class="vs-stk-art vs-ton" style="width:${size}px;height:${size}px"><svg viewBox="0 0 100 100" aria-hidden="true">${vsTonArt(key, u)}</svg></span>`;
}
function vsTonArt(k, u){
  const defs = `<defs>
    <linearGradient id="${u}a" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#8fe3ff"/><stop offset=".5" stop-color="#22b1f2"/><stop offset="1" stop-color="#0069c2"/>
    </linearGradient>
    <linearGradient id="${u}b" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#d8f4ff"/><stop offset="1" stop-color="#0098EA"/>
    </linearGradient>
    <radialGradient id="${u}c" cx=".35" cy=".3" r=".95">
      <stop offset="0" stop-color="#bdefff"/><stop offset=".55" stop-color="#0098EA"/><stop offset="1" stop-color="#005a99"/>
    </radialGradient>
  </defs>`;
  if(k === 'vs-gem') return defs + `
    <path d="M50 10 L83 40 L50 90 L17 40 Z" fill="url(#${u}a)"/>
    <path d="M17 40 L83 40 L50 90 Z" fill="url(#${u}b)" opacity=".5"/>
    <path d="M50 10 L64 40 L50 90 L36 40 Z" fill="#ffffff" opacity=".22"/>
    <path d="M50 10 L83 40 M50 10 L17 40 M17 40 L83 40 M36 40 L50 90 M64 40 L50 90" stroke="#eafaff" stroke-width="1.6" opacity=".8" fill="none"/>
    <path d="M77 12 l2.6 6.4 6.4 2.6 -6.4 2.6 -2.6 6.4 -2.6 -6.4 -6.4 -2.6 6.4 -2.6 z" fill="#fff" opacity=".95"/>
    <circle cx="24" cy="20" r="2.2" fill="#fff" opacity=".8"/>`;
  if(k === 'vs-coin') return defs + `
    <circle cx="50" cy="50" r="43" fill="url(#${u}c)"/>
    <circle cx="50" cy="50" r="43" fill="none" stroke="url(#${u}b)" stroke-width="4"/>
    <circle cx="50" cy="50" r="34" fill="none" stroke="#eafaff" stroke-width="1.4" opacity=".5"/>
    <path d="M33 33 h34 c4.6 0 7.2 5 4.8 8.9 L54.7 71.4 c-2.1 3.5 -7.3 3.5 -9.4 0 L28.2 41.9 C25.8 38 28.4 33 33 33 Z" fill="#fff"/>
    <path d="M50 34 V59" stroke="url(#${u}a)" stroke-width="3.2" stroke-linecap="round"/>
    <path d="M20 40 A34 34 0 0 1 38 15" stroke="#fff" stroke-width="3" opacity=".55" fill="none" stroke-linecap="round"/>`;
  if(k === 'vs-rocket') return defs + `
    <g transform="rotate(35 50 50)">
      <path d="M43 64 Q50 92 50 97 Q50 92 57 64 Z" fill="#9AFF00" opacity=".85"/>
      <path d="M46.5 64 Q50 84 50 88 Q50 84 53.5 64 Z" fill="#fff" opacity=".85"/>
      <path d="M37 52 L23 72 L37 66 Z" fill="url(#${u}b)"/>
      <path d="M63 52 L77 72 L63 66 Z" fill="url(#${u}b)"/>
      <path d="M50 6 C62 20 66 40 63 63 L37 63 C34 40 38 20 50 6 Z" fill="url(#${u}a)"/>
      <path d="M50 6 C56 20 58 40 56.5 63 L50 63 Z" fill="#fff" opacity=".14"/>
      <circle cx="50" cy="35" r="9.5" fill="#06101c" stroke="#eafaff" stroke-width="2.4"/>
      <path d="M50 29 l5 5 -5 8 -5 -8 z" fill="url(#${u}b)"/>
    </g>
    <path d="M15 22 l2.2 5.2 5.2 2.2 -5.2 2.2 -2.2 5.2 -2.2 -5.2 -5.2 -2.2 5.2 -2.2 z" fill="#fff" opacity=".9"/>
    <circle cx="85" cy="76" r="2.4" fill="#8fe3ff" opacity=".9"/>`;
  if(k === 'vs-eye') return defs + `
    <path d="M50 5 L88 29 L88 68 L50 95 L12 68 L12 29 Z" fill="url(#${u}a)" opacity=".26" stroke="url(#${u}b)" stroke-width="2.6"/>
    <path d="M18 50 Q50 24 82 50 Q50 76 18 50 Z" fill="#06101c" stroke="url(#${u}b)" stroke-width="3"/>
    <circle cx="50" cy="50" r="17" fill="none" stroke="#9AFF00" stroke-width="1.4" opacity=".55" stroke-dasharray="4 6"/>
    <circle cx="50" cy="50" r="13" fill="url(#${u}c)"/>
    <circle cx="50" cy="50" r="5.5" fill="#041018"/>
    <circle cx="45.5" cy="45" r="2.8" fill="#fff"/>
    <path d="M83 14 l2.4 5.6 5.6 2.4 -5.6 2.4 -2.4 5.6 -2.4 -5.6 -5.6 -2.4 5.6 -2.4 z" fill="#fff" opacity=".9"/>`;
  /* vs-bolt */
  return defs + `
    <path d="M57 5 L25 55 L45 55 L40 95 L76 41 L54 41 Z" fill="url(#${u}a)" stroke="#dff4ff" stroke-width="1.8" stroke-linejoin="round"/>
    <path d="M55 12 L33 50 L47 50 L44 80" stroke="#fff" stroke-width="2.2" opacity=".55" fill="none" stroke-linejoin="round"/>
    <path d="M57 5 L54 41 L76 41 Z" fill="#fff" opacity=".18"/>
    <path d="M18 18 l2.2 5.2 5.2 2.2 -5.2 2.2 -2.2 5.2 -2.2 -5.2 -5.2 -2.2 5.2 -2.2 z" fill="#fff" opacity=".9"/>
    <circle cx="82" cy="80" r="2.4" fill="#8fe3ff" opacity=".9"/>`;
}

/* ---------- ПРЕМИУМ-АРТ БАЗОВЫХ СТИКЕРОВ (замена плоских кружков-иконок) ---------- */
/* Каждый стикер — самодостаточная SVG-иллюстрация в фирменном языке OKO:
   строго чёрный + лайм #9AFF00 (никаких системных «цветных» эмодзи), градиенты,
   белые блики, тёмный кейлайн (читается в тёмной И светлой теме), сигнатурная искра.
   Плавает прозрачно (в сообщении) и на тайле (в гриде). Реакции — компактная
   штриховая версия того же набора (I('heart') …); стикеры — премиум-заливка. */
const VS_BASE_ICS = new Set(['logo','fire','heart','thumb','laugh','wow','star','crown','rocket','bolt','sad','check']);
function vsBaseSvg(ic, size){
  const u = 'vsb' + (vsUid++);
  return `<span class="vs-stk-art" style="width:${size}px;height:${size}px"><svg viewBox="0 0 100 100" aria-hidden="true">${vsBaseArt(ic, u)}</svg></span>`;
}
function vsBaseArt(ic, u){
  switch(ic){
  case 'logo': return `<defs>
    <linearGradient id="${u}l" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#e7ff9e"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#5aa300"/></linearGradient>
    <radialGradient id="${u}d" cx=".5" cy=".5" r=".62"><stop offset="0" stop-color="#16240a"/><stop offset="1" stop-color="#05080a"/></radialGradient></defs>
    <path d="M50 20 C76 20 94 50 94 50 C94 50 76 80 50 80 C24 80 6 50 6 50 C6 50 24 20 50 20 Z" fill="url(#${u}d)" stroke="url(#${u}l)" stroke-width="6" stroke-linejoin="round"/>
    <circle cx="50" cy="50" r="18" fill="url(#${u}l)"/>
    <circle cx="50" cy="50" r="8.5" fill="#05080a"/>
    <circle cx="44" cy="44" r="3.4" fill="#f4ffd6"/>
    <path d="M78 24 l2.4 6 6 2.4 -6 2.4 -2.4 6 -2.4 -6 -6 -2.4 6 -2.4 z" fill="#eaffb0"/>`;
  case 'fire': return `<defs>
    <linearGradient id="${u}o" x1="0" y1="1" x2="0" y2="0"><stop offset="0" stop-color="#4f8f00"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#e4ff9e"/></linearGradient>
    <linearGradient id="${u}c" x1="0" y1="1" x2="0" y2="0"><stop offset="0" stop-color="#c6ff70"/><stop offset="1" stop-color="#ffffff"/></linearGradient></defs>
    <path d="M52 5 C61 26 84 34 79 61 C75 85 61 96 50 96 C37 96 21 87 21 63 C21 49 31 45 33 34 C43 43 45 31 40 22 C51 27 54 16 52 5 Z" fill="url(#${u}o)" stroke="#0a1403" stroke-width="1.6" stroke-linejoin="round"/>
    <path d="M51 46 C58 56 62 62 58 74 C55 86 47 88 43 82 C38 74 43 67 45 61 C47 56 51 54 51 46 Z" fill="url(#${u}c)"/>`;
  case 'heart': return `<defs>
    <linearGradient id="${u}r" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#d6ff8f"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#5aa300"/></linearGradient></defs>
    <path d="M50 88 C16 63 6 45 6 29 C6 15 18 7 31 7 C41 7 47 13 50 20 C53 13 59 7 69 7 C82 7 94 15 94 29 C94 45 84 63 50 88 Z" fill="url(#${u}r)" stroke="#0a1403" stroke-width="1.6" stroke-linejoin="round"/>
    <path d="M27 19 C21 22 16 28 16 36" stroke="#f4ffd6" stroke-width="6" fill="none" stroke-linecap="round" opacity=".8"/>`;
  case 'thumb': return `<defs>
    <linearGradient id="${u}t" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#c6ff70"/><stop offset=".55" stop-color="#9AFF00"/><stop offset="1" stop-color="#5fa800"/></linearGradient></defs>
    <rect x="20" y="46" width="16" height="40" rx="4.5" fill="#6cba12"/>
    <rect x="23" y="49" width="5" height="34" rx="2.5" fill="#b6ff5e" opacity=".65"/>
    <path d="M40 46 C40 46 46 44 49 38 C52 32 51 24 54 18 C56 14 63 14 64 21 C65 28 61 38 61 42 H80 C86 42 88 48 85 53 C88 56 87 62 83 64 C86 68 83 74 79 75 C81 80 77 86 71 86 H50 C44 86 40 82 40 76 Z" fill="url(#${u}t)"/>
    <path d="M46 50 C48 47 51 43 53 38" stroke="#eaffc0" stroke-width="3.5" fill="none" stroke-linecap="round" opacity=".7"/>`;
  case 'laugh': return `<defs>
    <radialGradient id="${u}f" cx=".4" cy=".33" r=".78"><stop offset="0" stop-color="#eaffb8"/><stop offset=".55" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></radialGradient></defs>
    <circle cx="50" cy="50" r="43" fill="url(#${u}f)" stroke="#0a1403" stroke-width="1.4"/>
    <circle cx="50" cy="50" r="43" fill="none" stroke="#fff" stroke-width="1.6" opacity=".28"/>
    <path d="M22 43 Q31 32 41 43" stroke="#0a1403" stroke-width="5" fill="none" stroke-linecap="round"/>
    <path d="M59 43 Q69 32 78 43" stroke="#0a1403" stroke-width="5" fill="none" stroke-linecap="round"/>
    <path d="M25 57 Q50 90 75 57 Z" fill="#0a1403"/>
    <path d="M28 58 Q50 68 72 58 Z" fill="#eaffc0"/>
    <path d="M13 52 q-5 8 0 14 q5 -5 0 -14 z" fill="#c6ff70"/>
    <path d="M87 52 q5 8 0 14 q-5 -5 0 -14 z" fill="#c6ff70"/>`;
  case 'wow': return `<defs>
    <radialGradient id="${u}w" cx=".4" cy=".33" r=".8"><stop offset="0" stop-color="#eaffb8"/><stop offset=".55" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></radialGradient></defs>
    <circle cx="50" cy="50" r="43" fill="url(#${u}w)" stroke="#0a1403" stroke-width="1.4"/>
    <circle cx="50" cy="50" r="43" fill="none" stroke="#fff" stroke-width="1.6" opacity=".3"/>
    <path d="M24 34 Q33 28 42 33" stroke="#0a1403" stroke-width="4" fill="none" stroke-linecap="round"/>
    <path d="M58 33 Q67 28 76 34" stroke="#0a1403" stroke-width="4" fill="none" stroke-linecap="round"/>
    <ellipse cx="34" cy="47" rx="7" ry="9" fill="#0a1403"/>
    <ellipse cx="66" cy="47" rx="7" ry="9" fill="#0a1403"/>
    <circle cx="31.5" cy="44" r="2.4" fill="#eaffc0"/><circle cx="63.5" cy="44" r="2.4" fill="#eaffc0"/>
    <ellipse cx="50" cy="72" rx="9.5" ry="12.5" fill="#0a1403"/>`;
  case 'star': return `<defs>
    <linearGradient id="${u}s" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#eaffb8"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#5aa300"/></linearGradient></defs>
    <path d="M50 5 L62 37 L96 38 L69 59 L79 92 L50 72 L21 92 L31 59 L4 38 L38 37 Z" fill="url(#${u}s)" stroke="#0a1403" stroke-width="1.8" stroke-linejoin="round"/>
    <path d="M50 16 L58 39 L46 39 Z" fill="#fff" opacity=".4"/>
    <path d="M81 20 l1.9 4.8 4.8 1.9 -4.8 1.9 -1.9 4.8 -1.9 -4.8 -4.8 -1.9 4.8 -1.9 z" fill="#f4ffd6" opacity=".95"/>`;
  case 'crown': return `<defs>
    <linearGradient id="${u}k" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#e4ff9e"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#5aa300"/></linearGradient></defs>
    <path d="M13 76 L18 32 L37 55 L50 24 L63 55 L82 32 L87 76 Z" fill="url(#${u}k)" stroke="#0a1403" stroke-width="1.8" stroke-linejoin="round"/>
    <rect x="15" y="76" width="70" height="14" rx="4" fill="#4f8f00"/>
    <rect x="15" y="76" width="70" height="5" rx="2.5" fill="#fff" opacity=".28"/>
    <circle cx="18" cy="30" r="5" fill="#f4ffd6"/><circle cx="82" cy="30" r="5" fill="#f4ffd6"/><circle cx="50" cy="22" r="5.5" fill="#f4ffd6"/>
    <circle cx="30" cy="83" r="4" fill="#0a1403"/><circle cx="50" cy="83" r="4.5" fill="#0a1403"/><circle cx="70" cy="83" r="4" fill="#0a1403"/>`;
  case 'rocket': return `<defs>
    <linearGradient id="${u}q" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#e4ff9e"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#5aa300"/></linearGradient>
    <linearGradient id="${u}m" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#ffffff"/><stop offset="1" stop-color="#9AFF00"/></linearGradient></defs>
    <path d="M40 66 L28 84 L40 78 Z" fill="#4f8f00"/>
    <path d="M60 66 L72 84 L60 78 Z" fill="#4f8f00"/>
    <path d="M44 72 Q50 96 50 96 Q50 96 56 72 Z" fill="url(#${u}m)"/>
    <path d="M50 6 C64 22 68 44 64 70 L36 70 C32 44 36 22 50 6 Z" fill="url(#${u}q)" stroke="#0a1403" stroke-width="1.5" stroke-linejoin="round"/>
    <path d="M50 6 C57 22 59 44 57 70 L50 70 Z" fill="#fff" opacity=".18"/>
    <circle cx="50" cy="38" r="10" fill="#0a1403" stroke="#eaffb8" stroke-width="2.6"/>
    <circle cx="50" cy="38" r="5" fill="#c6ff70"/>
    <path d="M39 22 l1.8 4.6 4.6 1.8 -4.6 1.8 -1.8 4.6 -1.8 -4.6 -4.6 -1.8 4.6 -1.8 z" fill="#f4ffd6" opacity=".85"/>`;
  case 'bolt': return `<defs>
    <linearGradient id="${u}z" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#e4ff9e"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#5aa300"/></linearGradient></defs>
    <path d="M58 4 L24 55 L45 55 L40 96 L78 41 L54 41 Z" fill="url(#${u}z)" stroke="#efffcf" stroke-width="2.2" stroke-linejoin="round"/>
    <path d="M55 14 L34 50 L48 50 L44 78" stroke="#fff" stroke-width="2.4" opacity=".55" fill="none" stroke-linejoin="round" stroke-linecap="round"/>`;
  case 'sad': return `<defs>
    <radialGradient id="${u}j" cx=".4" cy=".33" r=".8"><stop offset="0" stop-color="#eaffb8"/><stop offset=".55" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></radialGradient></defs>
    <circle cx="50" cy="50" r="43" fill="url(#${u}j)" stroke="#0a1403" stroke-width="1.4"/>
    <circle cx="50" cy="50" r="43" fill="none" stroke="#fff" stroke-width="1.6" opacity=".28"/>
    <path d="M24 40 Q33 46 41 41" stroke="#0a1403" stroke-width="4.5" fill="none" stroke-linecap="round"/>
    <path d="M59 41 Q67 46 76 40" stroke="#0a1403" stroke-width="4.5" fill="none" stroke-linecap="round"/>
    <circle cx="35" cy="52" r="4.5" fill="#0a1403"/><circle cx="65" cy="52" r="4.5" fill="#0a1403"/>
    <path d="M34 76 Q50 64 66 76" stroke="#0a1403" stroke-width="5" fill="none" stroke-linecap="round"/>
    <path d="M34 58 q-5 8 0 13 q5 -5 0 -13 z" fill="#c6ff70"/>`;
  case 'check': return `<defs>
    <radialGradient id="${u}v" cx=".4" cy=".33" r=".82"><stop offset="0" stop-color="#eaffb8"/><stop offset=".55" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></radialGradient></defs>
    <circle cx="50" cy="50" r="43" fill="url(#${u}v)" stroke="#0a1403" stroke-width="1.4"/>
    <circle cx="50" cy="50" r="43" fill="none" stroke="#fff" stroke-width="1.6" opacity=".3"/>
    <path d="M28 53 L44 69 L74 35" stroke="#f4ffd6" stroke-width="10" fill="none" stroke-linecap="round" stroke-linejoin="round" opacity=".45"/>
    <path d="M28 52 L44 68 L74 34" stroke="#0a1403" stroke-width="9" fill="none" stroke-linecap="round" stroke-linejoin="round"/>`;
  default: return '';
  }
}

/* stickerSvg: рисуем СВОЮ премиум-графику (базовые + TON) — и в гриде, и в сообщениях */
const _prevStickerSvgVs = stickerSvg;
stickerSvg = function(s, size){
  if(!s) return _prevStickerSvgVs(s, size);
  if(s.pack === 'ton') return vsTonSvg(s.ic, size);
  if(VS_BASE_ICS.has(s.ic)) return vsBaseSvg(s.ic, size);
  return _prevStickerSvgVs(s, size);
};

/* грид стикеров: премиум-секция «TON Crystal · PRO» с гейтом */
const _prevOpenStickersVs = openStickers;
openStickers = function(){
  _prevOpenStickersVs();
  const grid = document.getElementById('stickersGrid'); if(!grid) return;
  /* убрать TON-кнопки, отрисованные ядром без гейта и без секции */
  grid.querySelectorAll('.stk-btn').forEach(b=>{
    const m = (b.getAttribute('onclick') || '').match(/sendSticker\((\d+)\)/);
    if(m && +m[1] >= VS_TON_START) b.remove();
  });
  const ok = vsPremiumOk();
  let html = `<div class="vs-stk-head">
    <svg viewBox="0 0 100 100" aria-hidden="true"><path d="M50 8 L86 42 L50 92 L14 42 Z" fill="#2fb3f2" stroke="#bfeaff" stroke-width="5"/><path d="M50 8 L64 42 L50 92 L36 42 Z" fill="#eafaff" opacity=".5"/></svg>
    TON CRYSTAL <span class="vs-pro">${ok ? 'ОТКРЫТО' : 'PRO'}</span></div>`;
  html += STICKERS.map((s, i)=>{
    if(s.pack !== 'ton') return '';
    return ok
      ? `<button class="stk-btn vs-stk" onclick="sendSticker(${i})">${vsTonSvg(s.ic, 58)}<small>${s.label}</small></button>`
      : `<button class="stk-btn vs-stk vs-locked" onclick="vsLockedTap()">${vsTonSvg(s.ic, 58)}<span class="vs-lock">${I('lock')}</span><small>${s.label}</small></button>`;
  }).join('');
  grid.insertAdjacentHTML('beforeend', html);
};

function vsLockedTap(){
  showPopup({
    ico:'lock', title:'Стикеры TON — в подписке PRO',
    body:'Премиум-пак «TON Crystal» с эффектом дождя кристаллов открывается на тарифах PRO и BUSINESS.',
    actions:[{label:'Оформить PRO', onclick:()=>openPay('PRO')},{label:'Позже', ghost:true}]
  });
}

/* отправка: гейт + дождь кристаллов поверх ленты сообщений */
const _prevSendStickerVs = sendSticker;
sendSticker = function(i){
  const s = STICKERS[i];
  if(s && s.pack === 'ton' && !vsPremiumOk()){ closeSheet(); vsLockedTap(); return; }
  _prevSendStickerVs(i);
  if(s && s.pack === 'ton') vsCrystalRain();
  else if(s) vsStickerSpark(); /* лёгкий лайм-салют для бренд-стикеров (без лагов) */
};

/* WOW при отправке бренд-стикера: короткий залп лайм-искр у нижнего края ленты.
   Только transform/opacity (GPU), считанные частицы, авто-удаление — не тормозит. */
function vsStickerSpark(){
  try{
    const host = document.getElementById('msgs'); if(!host) return;
    const r = host.getBoundingClientRect();
    if(r.width < 10 || r.height < 10) return;
    const box = document.createElement('div');
    box.className = 'vs-spark';
    /* эпицентр — где появляется отправленный стикер (низ, ближе к правому краю) */
    const cx = r.left + r.width * 0.72, cy = r.top + r.height - 66;
    box.style.cssText = `left:${cx}px;top:${cy}px`;
    const n = 7, star = '<svg viewBox="0 0 100 100"><path d="M50 8 L60 40 L92 50 L60 60 L50 92 L40 60 L8 50 L40 40 Z" fill="#9AFF00"/></svg>';
    let h = '';
    for(let k = 0; k < n; k++){
      const ang = (k / n) * Math.PI * 2 + Math.random() * 0.5;
      const dist = 34 + Math.random() * 30;
      const dx = Math.round(Math.cos(ang) * dist);
      const dy = Math.round(Math.sin(ang) * dist) - 10; /* лёгкий подъём вверх */
      const sz = Math.round(7 + Math.random() * 7);
      const dur = (0.5 + Math.random() * 0.25).toFixed(2);
      h += `<i style="--dx:${dx}px;--dy:${dy}px;width:${sz}px;height:${sz}px;animation-duration:${dur}s">${star}</i>`;
    }
    box.innerHTML = h;
    document.body.appendChild(box);
    setTimeout(()=>box.remove(), 900);
  }catch(e){}
}

function vsCrystalRain(){
  const host = document.getElementById('msgs'); if(!host) return;
  const r = host.getBoundingClientRect();
  if(r.width < 10 || r.height < 10) return;
  const box = document.createElement('div');
  box.className = 'vs-rain';
  box.style.cssText = `left:${r.left}px;top:${r.top}px;width:${r.width}px;height:${r.height}px;--fy:${Math.round(r.height + 60)}px`;
  const n = 10 + Math.floor(Math.random() * 6); /* 10–15 кристаллов */
  let h = '';
  for(let k = 0; k < n; k++){
    const sz = Math.round(10 + Math.random() * 12);
    const x = (Math.random() * 94).toFixed(1);
    const dur = (1.05 + Math.random() * 0.45).toFixed(2);
    const dl = (Math.random() * 0.35).toFixed(2);
    h += `<i style="left:${x}%;width:${sz}px;height:${sz}px;animation-duration:${dur}s;animation-delay:${dl}s">
      <svg viewBox="0 0 100 100"><path d="M50 8 L86 42 L50 92 L14 42 Z" fill="#2fb3f2" stroke="#bfeaff" stroke-width="5"/><path d="M50 8 L64 42 L50 92 L36 42 Z" fill="#eafaff" opacity=".55"/></svg></i>`;
  }
  box.innerHTML = h;
  document.body.appendChild(box);
  setTimeout(()=>box.remove(), 2100);
}

/* ---------- 4. СТИКЕР К НОВОМУ ПОСТУ (sheet-npost) ---------- */
let vsNpPick = null;
function vsInitNpost(){
  const sheet = document.getElementById('sheet-npost');
  if(!sheet || document.getElementById('vsNpRow')) return;
  const btn = sheet.querySelector('button.btn'); if(!btn) return;
  const w = document.createElement('div'); w.id = 'vsNpRow';
  w.innerHTML = `<div class="vs-np-label">${I('sticker')} Стикер к посту <span class="vs-np-hint">необязательно</span></div><div class="vs-np-strip" id="vsNpStrip"></div>`;
  sheet.insertBefore(w, btn);
  vsRenderNpStrip();
}
function vsRenderNpStrip(){
  const el = document.getElementById('vsNpStrip'); if(!el) return;
  const ok = vsPremiumOk();
  el.innerHTML = STICKERS.map((s, i)=>{
    const locked = s.pack === 'ton' && !ok;
    return `<button class="${vsNpPick === i ? 'on' : ''}${locked ? ' vs-locked' : ''}" title="${s.label}" onclick="${locked ? 'vsLockedTap()' : `vsPickNp(${i})`}">${stickerSvg(s, 34)}</button>`;
  }).join('');
}
function vsPickNp(i){ vsNpPick = (vsNpPick === i ? null : i); vsRenderNpStrip(); }

const _prevCreatePostVs = createPost;
createPost = function(){
  const hadText = !!document.getElementById('npBody').value.trim();
  const pick = vsNpPick;
  _prevCreatePostVs();
  if(!hadText) return; /* ядро не создало пост */
  if(pick != null){
    const p = POSTS.sub[0];
    const s = STICKERS[pick];
    if(p && s && p.name === PROFILE.name){
      p.body += `<br><span class="vs-post-stk">${stickerSvg(s, 22)}<b>${esc(s.label)}</b></span>`;
      renderFeed(typeof curFeedKind !== 'undefined' && curFeedKind ? curFeedKind : 'sub');
    }
    vsNpPick = null; vsRenderNpStrip();
  }
};

/* ============================================================
   5. TON ПОДАРКИ — крипта в стиле Telegram (честная заглушка):
      кошелёк-прототип (адрес+баланс), эмодзи-подарки/кристаллы,
      покупка за TON (пополнение за рубли), владение (коллекция),
      отправка подарка другу в чат, премиум-гейт коллекционных.
   ============================================================ */
const VS_TON_RATE = 320;            /* ₽ за 1 TON — курс прототипа */
let   VS_TON_TAB  = 'shop';         /* shop | mine */
let   VS_TON_TOPUP = 5;             /* выбранное пополнение, TON */
let   vsGiftDetailId = null, vsSendGiftId = null;

let VS_TON = { addr:null, balance:0, connected:false, owned:{}, tx:[] };
function vsTonSave(){ try{ localStorage.setItem('oko-ton', JSON.stringify(VS_TON)); }catch(e){} }
(function vsTonRestore(){
  try{ const d = JSON.parse(localStorage.getItem('oko-ton') || 'null');
    if(d && typeof d === 'object') VS_TON = Object.assign(VS_TON, d);
  }catch(e){}
  if(!VS_TON.owned || typeof VS_TON.owned !== 'object') VS_TON.owned = {};
  if(!Array.isArray(VS_TON.tx)) VS_TON.tx = [];
  if(!Array.isArray(VS_TON.wish)) VS_TON.wish = [];
  if(typeof VS_TON.balance !== 'number' || isNaN(VS_TON.balance)) VS_TON.balance = 0;
  if(!VS_TON.addr){
    const al = 'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz0123456789-_';
    let s = 'UQ'; for(let i=0;i<46;i++) s += al[Math.floor(Math.random()*al.length)];
    VS_TON.addr = s;
    /* Правка Даниэля 09.08: «ноль демо-данных». Раньше здесь начислялось
       3,5 «приветственных» TON и в историю падала выдуманная транзакция —
       человек видел деньги, которых у него нет. Кошелёк стартует с нуля,
       пополнение — только явным действием через vsDoTopup(). */
  }
  vsTonSave();
})();

/* --- каталог подарков (эмодзи-подарки/кристаллы, часть — коллекционные PRO) --- */
const VS_GIFTS = [
  { id:'crystal', name:'Кристалл OKO',   art:'crystal', price:1.2, supply:15000, sold:0 },
  { id:'star',    name:'Звезда',         art:'star',    price:1.8, supply:20000, sold:0 },
  { id:'heart',   name:'Сердце-алмаз',   art:'heart',   price:2.4, supply:12000, sold:0 },
  { id:'potion',  name:'Эликсир',        art:'potion',  price:2.9, supply:9000,  sold:0 },
  { id:'ring',    name:'Кольцо',         art:'ring',    price:3.5, supply:8000,  sold:0 },
  { id:'rocket',  name:'Ракета',         art:'rocket',  price:4.2, supply:6000,  sold:0 },
  { id:'trophy',  name:'Кубок',          art:'trophy',  price:5,   supply:5000,  sold:0 },
  { id:'medal',   name:'Медальон OKO',   art:'medal',   price:8,   supply:2000,  sold:0,  premium:true },
  { id:'crown',   name:'Корона',         art:'crown',   price:12,  supply:1000,  sold:0,  premium:true },
];
function vsGiftById(id){ for(let i=0;i<VS_GIFTS.length;i++) if(VS_GIFTS[i].id===id) return VS_GIFTS[i]; return null; }
function vsOwnedTotal(){ let n=0; for(const k in VS_TON.owned) n += VS_TON.owned[k]||0; return n; }

/* --- художественный SVG подарка (переиспользуем премиум-арт + свои) --- */
function vsGiftArt(a, u){
  if(a==='crystal') return vsTonArt('vs-gem', u);
  if(a==='heart')   return vsBaseArt('heart', u);
  if(a==='rocket')  return vsBaseArt('rocket', u);
  if(a==='star')    return vsBaseArt('star', u);
  if(a==='crown')   return vsBaseArt('crown', u);
  if(a==='ring') return `<defs>
    <linearGradient id="${u}g" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#e7ff9e"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#5aa300"/></linearGradient>
    <linearGradient id="${u}d" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#d8f6ff"/><stop offset=".5" stop-color="#7fd8ff"/><stop offset="1" stop-color="#2b9fe0"/></linearGradient></defs>
    <ellipse cx="50" cy="66" rx="26" ry="27" fill="none" stroke="url(#${u}g)" stroke-width="10"/>
    <ellipse cx="50" cy="66" rx="26" ry="27" fill="none" stroke="#eaffb8" stroke-width="2" opacity=".5"/>
    <path d="M34 30 L50 8 L66 30 L50 44 Z" fill="url(#${u}d)" stroke="#eafaff" stroke-width="2" stroke-linejoin="round"/>
    <path d="M34 30 H66 M50 8 L42 30 M50 8 L58 30 M42 30 L50 44 M58 30 L50 44" stroke="#eafaff" stroke-width="1.4" opacity=".8" fill="none"/>
    <path d="M40 32 l1.8 4.4 4.4 1.8 -4.4 1.8 -1.8 4.4 -1.8 -4.4 -4.4 -1.8 4.4 -1.8 z" fill="#fff" opacity=".9"/>`;
  if(a==='trophy') return `<defs>
    <linearGradient id="${u}g" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#e7ff9e"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></linearGradient></defs>
    <rect x="38" y="72" width="24" height="10" rx="3" fill="#4f8f00"/>
    <rect x="30" y="82" width="40" height="10" rx="4" fill="#6cba12"/>
    <rect x="30" y="82" width="40" height="4" rx="2" fill="#fff" opacity=".25"/>
    <path d="M28 16 H72 V38 C72 55 62 66 50 66 C38 66 28 55 28 38 Z" fill="url(#${u}g)" stroke="#eaffb8" stroke-width="2"/>
    <path d="M28 22 H16 V30 C16 40 22 46 30 46" fill="none" stroke="url(#${u}g)" stroke-width="6"/>
    <path d="M72 22 H84 V30 C84 40 78 46 70 46" fill="none" stroke="url(#${u}g)" stroke-width="6"/>
    <path d="M50 26 L55 37 L67 38 L58 46 L61 58 L50 51 L39 58 L42 46 L33 38 L45 37 Z" fill="#fff" opacity=".85"/>`;
  if(a==='medal') return `<defs>
    <linearGradient id="${u}r" x1="0" y1="0" x2="1" y2="0"><stop offset="0" stop-color="#c6ff70"/><stop offset="1" stop-color="#6cba12"/></linearGradient>
    <radialGradient id="${u}m" cx=".4" cy=".35" r=".7"><stop offset="0" stop-color="#eaffb8"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></radialGradient></defs>
    <path d="M36 8 L30 44 L50 34 Z" fill="url(#${u}r)"/>
    <path d="M64 8 L70 44 L50 34 Z" fill="#7ec81a"/>
    <circle cx="50" cy="64" r="27" fill="url(#${u}m)" stroke="#eaffb8" stroke-width="3"/>
    <circle cx="50" cy="64" r="20" fill="#0a1403"/>
    <path d="M32 64 Q50 50 68 64 Q50 78 32 64 Z" fill="none" stroke="#c6ff70" stroke-width="3"/>
    <circle cx="50" cy="64" r="7" fill="#9AFF00"/><circle cx="47" cy="61" r="2.4" fill="#0a1403"/>`;
  /* potion */
  return `<defs>
    <linearGradient id="${u}p" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#c6ff70"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></linearGradient></defs>
    <path d="M42 12 H58 V40 L74 78 C77 86 71 92 63 92 H37 C29 92 23 86 26 78 L42 40 Z" fill="rgba(154,255,0,.14)" stroke="#eaffc0" stroke-width="3" stroke-linejoin="round"/>
    <path d="M35 56 L65 56 L73 76 C76 84 70 88 63 88 H37 C30 88 24 84 27 76 Z" fill="url(#${u}p)"/>
    <ellipse cx="50" cy="57" rx="15" ry="4" fill="#c6ff70" opacity=".7"/>
    <circle cx="44" cy="72" r="4" fill="#fff" opacity=".55"/><circle cx="57" cy="78" r="3" fill="#fff" opacity=".45"/><circle cx="50" cy="68" r="2.4" fill="#fff" opacity=".6"/>
    <rect x="40" y="6" width="20" height="9" rx="3" fill="#6cba12"/>
    <path d="M70 20 l1.8 4.6 4.6 1.8 -4.6 1.8 -1.8 4.6 -1.8 -4.6 -4.6 -1.8 4.6 -1.8 z" fill="#fff" opacity=".85"/>`;
}
function vsGiftSvg(art, size){
  const u = 'vsgf' + (vsUid++);
  return `<span class="vs-stk-art" style="width:${size}px;height:${size}px"><svg viewBox="0 0 100 100" aria-hidden="true">${vsGiftArt(art, u)}</svg></span>`;
}
/* маленький значок TON-кристалла (для цен и баланса) */
function vsGemMark(size){
  const s = size || 14;
  return `<span class="vs-gem-mk" style="width:${s}px;height:${s}px"><svg viewBox="0 0 100 100" aria-hidden="true"><path d="M50 8 L86 42 L50 92 L14 42 Z" fill="#2fb3f2" stroke="#bfeaff" stroke-width="5"/><path d="M50 8 L64 42 L50 92 L36 42 Z" fill="#eafaff" opacity=".5"/></svg></span>`;
}
function vsTonFmt(n){ n = Math.round((+n||0)*100)/100; return n.toLocaleString('ru-RU', {maximumFractionDigits:2}); }
function vsTonAddrShort(){ const a = VS_TON.addr||''; return a.length>12 ? a.slice(0,6)+'…'+a.slice(-4) : a; }
function vsWhen(ts){ try{ const d = new Date(ts); return d.toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit'})+' '+d.toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'}); }catch(e){ return ''; } }

/* --- символ иконки для тайла хаба (диаманд в бренд-штрихе) --- */
function vsInjectTonSymbol(){
  try{
    if(document.getElementById('i-vs-gem')) return;
    const defs = document.querySelector('svg defs'); if(!defs) return;
    const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
    s.setAttribute('id','i-vs-gem'); s.setAttribute('viewBox','0 0 100 100');
    s.innerHTML = '<path d="M50 12 L82 40 L50 90 L18 40 Z M18 40 H82 M38 40 L50 90 M62 40 L50 90 M50 12 L38 40 M50 12 L62 40" fill="none" stroke="currentColor" stroke-width="7" stroke-linejoin="round" stroke-linecap="round"/>';
    defs.appendChild(s);
    /* стрелка «получить» (вниз в лоток) — единый бренд-штрих stroke 7, round */
    if(!document.getElementById('i-vs-recv')){
      const r = document.createElementNS('http://www.w3.org/2000/svg','symbol');
      r.setAttribute('id','i-vs-recv'); r.setAttribute('viewBox','0 0 100 100');
      r.innerHTML = '<path d="M50 14 V64 M30 44 L50 66 L70 44 M20 78 H80" fill="none" stroke="currentColor" stroke-width="7" stroke-linejoin="round" stroke-linecap="round"/>';
      defs.appendChild(r);
    }
  }catch(e){}
}

/* ---------- ОТПРАВКА / ПРИЁМ TON (адрес↔адрес) ---------- */
let VS_SEND_AMT = 1, VS_SEND_ADDR = '';
function vsAddrValid(a){
  a = String(a||'').trim();
  /* user-friendly (UQ/EQ/kQ/0Q + base64url, 48) или raw 0:hex64 */
  if(/^[UEk0]Q[A-Za-z0-9_-]{46}$/.test(a)) return true;
  if(/^-?[0-9]:[0-9a-fA-F]{64}$/.test(a)) return true;
  return false;
}
function vsOpenSendTon(){ if(!(VS_SEND_AMT>0)) VS_SEND_AMT = 1; VS_SEND_ADDR = ''; vsRenderSendTon(); openSheet('vs-ton-sendton'); }
function vsRenderSendTon(){
  const v = document.getElementById('vsSendTonView'); if(!v) return;
  const bal = vsTonFmt(VS_TON.balance);
  const amt = VS_SEND_AMT;
  const rub = Math.round(amt*VS_TON_RATE);
  const rubStr = (typeof fmtMoney==='function') ? fmtMoney(rub) : rub+' ₽';
  const maxv = Math.round(VS_TON.balance*100)/100;
  const raw = [0.5, 1, 5, maxv > 0 ? maxv : 10];
  const seen = {};
  const chips = raw.filter(a=> a>0 && !seen[a] && (seen[a]=1));
  const chipsH = chips.map(a=>
    `<button class="${amt===a?'on':''}" onclick="vsSetSendAmt(${a})">${a===maxv && maxv>0 ? 'МАКС' : vsTonFmt(a)+' TON'}</button>`).join('');
  v.innerHTML = `
    <div class="vs-sendton-bal">${vsGemMark(16)} Баланс <b>${bal} TON</b></div>
    <label class="vs-field-lbl" for="vsSendAddr">Адрес получателя</label>
    <div class="vs-addr-input">
      <input id="vsSendAddr" type="text" inputmode="text" autocomplete="off" spellcheck="false" placeholder="UQ… адрес TON-кошелька" value="${esc(VS_SEND_ADDR)}" oninput="vsSendValidate()">
      <button type="button" class="vs-addr-paste" onclick="vsPasteAddr()" title="Вставить">${I('copy')}</button>
    </div>
    <div class="vs-addr-err" id="vsSendErr"></div>
    <div class="vs-topup-big" style="margin-top:6px">${vsGemMark(28)}<b>${vsTonFmt(amt)}</b> TON</div>
    <div class="vs-topup-rub">≈ ${rubStr}</div>
    <div class="vs-topup-chips">${chipsH}</div>
    <div class="vs-buy-note"><svg class="i"><use href="#i-lock"/></svg> Прототип: перевод списывает TON с локального кошелька и попадает в историю. Реальные переводы в сети TON подключатся в релизе через TON Connect.</div>
    <button class="btn vs-ton" id="vsSendBtn" style="width:100%;margin-top:6px" onclick="vsDoSendTon()"><svg class="i"><use href="#i-send"/></svg> Отправить ${vsTonFmt(amt)} TON</button>`;
  vsSendValidate();
}
function vsSetSendAmt(a){ VS_SEND_AMT = Math.round((+a||0)*100)/100; vsRenderSendTon(); }
function vsPasteAddr(){
  try{
    if(navigator.clipboard && navigator.clipboard.readText){
      navigator.clipboard.readText().then(txt=>{
        const inp = document.getElementById('vsSendAddr');
        if(inp && txt){ inp.value = String(txt).trim(); vsSendValidate(); }
      }).catch(()=>toast('Разреши доступ к буферу обмена'));
    } else toast('Вставь адрес вручную');
  }catch(e){ toast('Вставь адрес вручную'); }
}
function vsSendValidate(){
  const inp = document.getElementById('vsSendAddr');
  const err = document.getElementById('vsSendErr');
  const btn = document.getElementById('vsSendBtn');
  if(!inp || !btn) return true;
  const a = inp.value.trim();
  VS_SEND_ADDR = a;
  let msg = '';
  if(a && !vsAddrValid(a)) msg = 'Похоже, адрес неполный — формат TON: UQ… (48 символов)';
  else if(a && a === VS_TON.addr) msg = 'Это адрес твоего же кошелька';
  else if(VS_SEND_AMT > VS_TON.balance) msg = 'Недостаточно TON — пополни кошелёк';
  else if(!(VS_SEND_AMT > 0)) msg = 'Укажи сумму больше нуля';
  if(err){ err.textContent = msg; err.style.display = msg ? 'block' : 'none'; }
  const ok = !!a && vsAddrValid(a) && a !== VS_TON.addr && VS_SEND_AMT > 0 && VS_SEND_AMT <= VS_TON.balance;
  btn.disabled = !ok; btn.classList.toggle('vs-btn-off', !ok);
  return ok;
}
function vsDoSendTon(){
  if(!vsSendValidate()){
    const a = (document.getElementById('vsSendAddr')||{}).value || '';
    if(!a.trim()){ toast('Введи адрес получателя'); }
    return;
  }
  const inp = document.getElementById('vsSendAddr');
  const addr = inp.value.trim();
  const amt = VS_SEND_AMT;
  VS_TON.balance = Math.round((VS_TON.balance - amt)*100)/100;
  VS_TON.tx.unshift({ t:'-', ton:amt, why:'Перевод на '+addr.slice(0,6)+'…'+addr.slice(-4), at:Date.now() });
  if(VS_TON.tx.length > 200) VS_TON.tx.length = 200;
  vsTonSave();
  closeSheet();
  toast('Отправлено · '+vsTonFmt(amt)+' TON');
  VS_SEND_AMT = 1; VS_SEND_ADDR = '';
  vsTonBurst();
  vsRenderTon();
}

/* --- приём: адрес + декоративный бренд-код --- */
function vsOpenRecv(){ vsRenderRecv(); openSheet('vs-ton-recv'); }
function vsAddrCode(addr, cells){
  /* детерминированная бренд-матрица из адреса (декоративная, не сканируемый QR) */
  const n = cells || 15;
  let seed = 0; for(let i=0;i<addr.length;i++){ seed = (seed*31 + addr.charCodeAt(i)) >>> 0; }
  const rnd = ()=>{ seed = (seed*1103515245 + 12345) & 0x7fffffff; return seed / 0x7fffffff; };
  const cx = (n-1)/2, gem = 3; /* центральный вырез под знак */
  let rects = '';
  for(let y=0;y<n;y++) for(let x=0; x<=Math.floor(n/2); x++){
    if(Math.abs(x-cx)<=gem && Math.abs(y-cx)<=gem) continue; /* центр под лого */
    if(rnd() > .52){
      const px = 4 + x*6.2, py = 4 + y*6.2, mx = 4 + (n-1-x)*6.2;
      rects += `<rect x="${px.toFixed(1)}" y="${py.toFixed(1)}" width="4.6" height="4.6" rx="1.4"/>`;
      if(x !== n-1-x) rects += `<rect x="${mx.toFixed(1)}" y="${py.toFixed(1)}" width="4.6" height="4.6" rx="1.4"/>`;
    }
  }
  const corner = (x,y)=>`<rect x="${x}" y="${y}" width="17" height="17" rx="5" fill="none" stroke="currentColor" stroke-width="3.4"/><rect x="${x+5}" y="${y+5}" width="7" height="7" rx="2" fill="currentColor"/>`;
  return `<svg viewBox="0 0 100 100" class="vs-qr-svg" aria-hidden="true">
    <g fill="currentColor">${rects}</g>
    ${corner(4,4)}${corner(79,4)}${corner(4,79)}
  </svg>`;
}
function vsRenderRecv(){
  const v = document.getElementById('vsRecvView'); if(!v) return;
  const a = VS_TON.addr || '';
  v.innerHTML = `
    <div class="vs-qr-wrap">
      <div class="vs-qr">
        <div class="vs-qr-code">${vsAddrCode(a)}</div>
        <div class="vs-qr-gem">${vsGiftSvg('crystal',44)}</div>
      </div>
    </div>
    <div class="vs-recv-title">Твой TON-адрес</div>
    <div class="vs-recv-addr" id="vsRecvAddr">${esc(a)}</div>
    <div class="vs-recv-acts">
      <button class="btn vs-ton" style="flex:1" onclick="vsCopyAddr()"><svg class="i"><use href="#i-copy"/></svg> Копировать</button>
      <button class="btn ghost" style="flex:1" onclick="vsShareAddr()"><svg class="i"><use href="#i-share"/></svg> Поделиться</button>
    </div>
    <div class="vs-buy-note" style="margin-top:12px"><svg class="i"><use href="#i-lock"/></svg> Отправь этот адрес отправителю, чтобы принять TON. В прототипе входящие переводы приходят через «Пополнить». Реальный приём в сети TON — в релизе.</div>`;
}
function vsShareAddr(){
  const a = VS_TON.addr || '';
  try{
    if(navigator.share){ navigator.share({ title:'Мой TON-адрес OKO', text:a }).catch(()=>{}); return; }
  }catch(e){}
  try{ navigator.clipboard && navigator.clipboard.writeText(a); }catch(e){}
  toast('Адрес скопирован — можно отправить другу');
}

/* ---------- ЭКРАН TON ПОДАРКИ ---------- */
function vsRenderTon(){
  const root = document.getElementById('vsTonRoot'); if(!root) return;
  const bal = vsTonFmt(VS_TON.balance);
  const rub = (typeof fmtMoney==='function') ? fmtMoney(Math.round(VS_TON.balance*VS_TON_RATE)) : Math.round(VS_TON.balance*VS_TON_RATE)+' ₽';
  const owned = Object.keys(VS_TON.owned).filter(k=>VS_TON.owned[k]>0);
  const totalOwned = vsOwnedTotal();
  let body;
  if(VS_TON_TAB === 'shop'){
    const wishItems = (VS_TON.wish||[]).map(vsGiftById).filter(Boolean);
    const wishSec = wishItems.length ? `<div class="vs-wish-sec">
      <div class="vs-wish-head"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 21s-7-4.35-7-10a4.5 4.5 0 0 1 8-2.8A4.5 4.5 0 0 1 19 11c0 5.65-7 10-7 10z" fill="currentColor"/></svg><span>Твой вишлист</span><i>${wishItems.length}</i><small>другие увидят и подарят</small></div>
      <div class="vs-wish-row">${wishItems.map(g=>`<div class="vs-wish-item vs-rar-${vsRarity(g)}" role="button" tabindex="0" onclick="vsOpenBuy('${g.id}')" title="${esc(g.name)}">${vsGiftSvg(g.art,40)}<b>${esc(g.name)}</b><small>${vsGemMark(10)} ${vsTonFmt(g.price)}</small><span class="vs-wish-x" onclick="event.stopPropagation();vsWishToggle('${g.id}')" role="button" aria-label="Убрать">${I('close')||'×'}</span></div>`).join('')}</div>
    </div>` : '';
    body = wishSec + `<div class="vs-gshop">${VS_GIFTS.map(vsShopCard).join('')}</div>`;
  } else {
    body = owned.length
      ? `<div class="vs-gshop">${owned.map(k=>vsMineCard(k, VS_TON.owned[k])).join('')}</div>`
      : `<div class="vs-mine-empty">${vsGiftSvg('crystal',64)}<b>Коллекция пуста</b><span>Купи подарок в магазине — и подари другу прямо в чат</span><button class="btn vs-ton" style="margin-top:12px" onclick="vsTonTab('shop')"><svg class="i"><use href="#i-plus"/></svg> В магазин</button></div>`;
  }
  root.innerHTML = `
    <div class="vs-ton-hero">
      <span class="vs-ton-hero-glow"></span>
      <div class="vs-ton-top">
        <span class="vs-ton-badge">${vsGemMark(14)} TON · прототип</span>
        <button class="vs-ton-conn" onclick="vsConnectWallet()"><svg class="i"><use href="#i-lock"/></svg> Кошелёк</button>
      </div>
      <div class="vs-ton-bal">${vsGemMark(28)}<span>${bal}</span></div>
      <div class="vs-ton-rub">≈ ${rub}</div>
      <button class="vs-ton-addr" onclick="vsCopyAddr()">${vsTonAddrShort()} <svg class="i"><use href="#i-copy"/></svg></button>
      <div class="vs-ton-acts">
        <button class="vs-ton-act" onclick="vsOpenSendTon()"><svg class="i"><use href="#i-send"/></svg><span>Отправить</span></button>
        <button class="vs-ton-act ghost" onclick="vsOpenRecv()"><svg class="i"><use href="#i-vs-recv"/></svg><span>Получить</span></button>
        <button class="vs-ton-act ghost" onclick="vsOpenTopup()"><svg class="i"><use href="#i-plus"/></svg><span>Пополнить</span></button>
        <button class="vs-ton-act ghost" onclick="vsOpenHistory()"><svg class="i"><use href="#i-clock"/></svg><span>История</span></button>
      </div>
    </div>
    <div class="vs-ton-tabs">
      <button class="${VS_TON_TAB==='shop'?'on':''}" onclick="vsTonTab('shop')">Магазин</button>
      <button class="${VS_TON_TAB==='mine'?'on':''}" onclick="vsTonTab('mine')">Мои подарки${totalOwned?` <i>${totalOwned}</i>`:''}</button>
    </div>
    ${body}
    <div class="vs-ton-foot"><svg class="i"><use href="#i-lock"/></svg> Честная заглушка: настоящие переводы TON и NFT-подарки подключатся в релизе через TON Connect. Баланс и коллекция хранятся локально на устройстве.</div>`;
}
function vsTonTab(t){ VS_TON_TAB = t; vsRenderTon(); }

function vsShopCard(g){
  const locked = g.premium && !vsPremiumOk();
  const own = VS_TON.owned[g.id] || 0;
  const rar = vsRarity(g);
  const wish = vsWishHas(g.id);
  return `<div class="vs-gcard vs-rar-${rar}${locked?' vs-locked':''}${g.premium?' vs-nft':''}" role="button" tabindex="0" onclick="vsOpenBuy('${g.id}')">
    <span class="vs-rar-chip vs-rar-${rar}">${VS_RARITY_LABEL[rar]}</span>
    <span class="vs-wish-tog${wish?' on':''}" onclick="event.stopPropagation();vsWishToggle('${g.id}')" role="button" aria-label="В вишлист"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 21s-7-4.35-7-10a4.5 4.5 0 0 1 8-2.8A4.5 4.5 0 0 1 19 11c0 5.65-7 10-7 10z" fill="currentColor"/></svg></span>
    <div class="vs-gcard-art">${vsGiftSvg(g.art,72)}${g.premium?`<span class="vs-nft-shine"></span>`:''}${g.premium?`<span class="vs-gcard-pro"><svg class="i"><use href="#i-crown"/></svg></span>`:''}${own?`<span class="vs-gcard-own">×${own}</span>`:''}${locked?`<span class="vs-gcard-lock"><svg class="i"><use href="#i-lock"/></svg></span>`:''}</div>
    <div class="vs-gcard-name">${esc(g.name)}</div>
    <div class="vs-gcard-price">${vsGemMark(12)} ${vsTonFmt(g.price)}</div>
  </div>`;
}
function vsMineCard(id, n){
  const g = vsGiftById(id); if(!g) return '';
  const rar = vsRarity(g);
  return `<div class="vs-gcard vs-rar-${rar}" role="button" tabindex="0" onclick="vsOpenGift('${id}')">
    <span class="vs-rar-chip vs-rar-${rar}">${VS_RARITY_LABEL[rar]}</span>
    <div class="vs-gcard-art">${vsGiftSvg(g.art,72)}<span class="vs-gcard-own">×${n}</span></div>
    <div class="vs-gcard-name">${esc(g.name)}</div>
    <div class="vs-gcard-price vs-mine-send"><svg class="i"><use href="#i-send"/></svg> Отправить</div>
  </div>`;
}

/* --- покупка --- */
function vsOpenBuy(id){
  const g = vsGiftById(id); if(!g) return;
  const v = document.getElementById('vsBuyView'); if(!v) return;
  const locked = g.premium && !vsPremiumOk();
  const rub = (typeof fmtMoney==='function') ? fmtMoney(Math.round(g.price*VS_TON_RATE)) : Math.round(g.price*VS_TON_RATE)+' ₽';
  const left = Math.max(0, g.supply - g.sold);
  const pct = Math.min(100, Math.round(g.sold*100/g.supply));
  const own = VS_TON.owned[g.id] || 0;
  const rar = vsRarity(g);
  const wish = vsWishHas(g.id);
  const wishBtn = `<button class="btn ghost vs-wish-btn${wish?' on':''}" style="width:100%;margin-top:8px" onclick="vsWishToggle('${g.id}')"><svg viewBox="0 0 24 24" aria-hidden="true" width="15" height="15"><path d="M12 21s-7-4.35-7-10a4.5 4.5 0 0 1 8-2.8A4.5 4.5 0 0 1 19 11c0 5.65-7 10-7 10z" fill="currentColor"/></svg> ${wish?'В вишлисте':'В вишлист'}</button>`;
  const act = locked
    ? `<button class="btn" style="width:100%;margin-top:6px" onclick="vsGiftLock()"><svg class="i"><use href="#i-crown"/></svg> Открыть в PRO</button>`
    : `<button class="btn vs-ton" style="width:100%;margin-top:6px" onclick="vsBuyGift('${g.id}')">${vsGemMark(15)} Купить за ${vsTonFmt(g.price)} TON</button>`;
  v.dataset.gid = g.id;
  v.innerHTML = `
    <div class="vs-buy-art vs-rar-${rar}${g.premium?' vs-nft':''}">${vsGiftSvg(g.art,120)}${g.premium?`<span class="vs-nft-shine"></span>`:''}${g.premium?`<span class="vs-buy-pro"><svg class="i"><use href="#i-crown"/></svg> PRO</span>`:''}</div>
    <div class="vs-buy-rar"><span class="vs-rar-chip vs-rar-${rar}">${VS_RARITY_LABEL[rar]}</span></div>
    <h2 class="vs-buy-name">${esc(g.name)}${own?`<span class="vs-buy-owned">в коллекции ×${own}</span>`:''}</h2>
    <div class="vs-buy-price">${vsGemMark(20)}<b>${vsTonFmt(g.price)}</b> TON <small>≈ ${rub}</small></div>
    <div class="vs-buy-supply"><div class="vs-buy-sbar"><i style="width:${pct}%"></i></div><span>Выпущено ${g.sold.toLocaleString('ru-RU')} · осталось ${left.toLocaleString('ru-RU')} из ${g.supply.toLocaleString('ru-RU')}</span></div>
    <div class="vs-buy-note"><svg class="i"><use href="#i-lock"/></svg> Прототип: покупка списывает TON с локального кошелька. Настоящие NFT-подарки TON — в релизе.</div>
    ${act}
    ${locked?'':wishBtn}`;
  openSheet('vs-ton-buy');
}
function vsGiftLock(){
  showPopup({ ico:'crown', title:'Коллекционный подарок',
    body:'Подарки с короной — эксклюзив тарифов PRO и BUSINESS. Оформи PRO, и коллекция откроется полностью.',
    actions:[{label:'Оформить PRO', onclick:()=>{ if(typeof openPay==='function') openPay('PRO'); }},{label:'Позже', ghost:true}] });
}
function vsBuyGift(id){
  const g = vsGiftById(id); if(!g) return;
  if(g.premium && !vsPremiumOk()){ closeSheet(); vsGiftLock(); return; }
  if(VS_TON.balance < g.price){
    closeSheet();
    showPopup({ ico:'money', title:'Не хватает TON',
      body:`Для «${esc(g.name)}» нужно ${vsTonFmt(g.price)} TON. Пополни кошелёк TON за рубли — быстро и без комиссии.`,
      actions:[{label:'Пополнить TON', onclick:()=>{ if(typeof closePopup==='function') closePopup(); vsOpenTopup(); }},{label:'Позже', ghost:true}] });
    return;
  }
  VS_TON.balance = Math.round((VS_TON.balance - g.price)*100)/100;
  VS_TON.owned[id] = (VS_TON.owned[id]||0) + 1;
  g.sold = Math.min(g.supply, g.sold + 1);
  VS_TON.tx.unshift({ t:'-', ton:g.price, why:'Покупка · '+g.name, at:Date.now() });
  if(VS_TON.tx.length > 200) VS_TON.tx.length = 200;
  vsTonSave();
  /* комиссия площадки OKO 5% — доход владельца */
  if(typeof okoEarn==='function') okoEarn(Math.round(g.price*VS_TON_RATE*0.05), 'TON-подарки: комиссия');
  closeSheet();
  toast('Подарок в коллекции');
  vsTonBurst();
  vsRenderTon();
}

/* --- пополнение TON за рубли --- */
function vsOpenTopup(){ if(!VS_TON_TOPUP) VS_TON_TOPUP = 5; vsRenderTopup(); openSheet('vs-ton-topup'); }
function vsRenderTopup(){
  const v = document.getElementById('vsTopupView'); if(!v) return;
  const amts = [1,5,10,25];
  const rub = Math.round(VS_TON_TOPUP*VS_TON_RATE);
  const rubStr = (typeof fmtMoney==='function') ? fmtMoney(rub) : rub+' ₽';
  const rateStr = (typeof fmtMoney==='function') ? fmtMoney(VS_TON_RATE) : VS_TON_RATE+' ₽';
  v.innerHTML = `
    <div class="vs-topup-big">${vsGemMark(30)}<b>${vsTonFmt(VS_TON_TOPUP)}</b> TON</div>
    <div class="vs-topup-rub">спишем ${rubStr} с кошелька OKO</div>
    <div class="vs-topup-chips">${amts.map(a=>`<button class="${VS_TON_TOPUP===a?'on':''}" onclick="vsSetTopup(${a})">${a} TON</button>`).join('')}</div>
    <div class="vs-buy-note"><svg class="i"><use href="#i-lock"/></svg> Курс прототипа: 1 TON ≈ ${rateStr}. Настоящий обмен на TON — в релизе.</div>
    <button class="btn vs-ton" style="width:100%;margin-top:6px" onclick="vsDoTopup()"><svg class="i"><use href="#i-plus"/></svg> Пополнить на ${vsTonFmt(VS_TON_TOPUP)} TON</button>`;
}
function vsSetTopup(a){ VS_TON_TOPUP = a; vsRenderTopup(); }
function vsDoTopup(){
  const rub = Math.round(VS_TON_TOPUP*VS_TON_RATE);
  if(typeof walletCharge === 'function' && walletCharge(rub, 'Покупка TON · '+vsTonFmt(VS_TON_TOPUP)+' TON')){
    VS_TON.balance = Math.round((VS_TON.balance + VS_TON_TOPUP)*100)/100;
    VS_TON.tx.unshift({ t:'+', ton:VS_TON_TOPUP, why:'Пополнение TON', at:Date.now() });
    if(VS_TON.tx.length > 200) VS_TON.tx.length = 200;
    vsTonSave();
    closeSheet();
    toast('Кошелёк TON пополнен');
    vsRenderTon();
  }
  /* при нехватке рублей walletCharge сам покажет тост и уведёт в кошелёк */
}

/* --- деталь подарка из коллекции --- */
function vsOpenGift(id){ vsGiftDetailId = id; vsRenderGift(); openSheet('vs-gift'); }
function vsRenderGift(){
  const v = document.getElementById('vsGiftView'); if(!v) return;
  const g = vsGiftById(vsGiftDetailId); if(!g) return;
  const n = VS_TON.owned[g.id] || 0;
  v.innerHTML = `
    <div class="vs-buy-art">${vsGiftSvg(g.art,120)}</div>
    <h2 class="vs-buy-name">${esc(g.name)}</h2>
    <div class="vs-gift-own"><svg class="i"><use href="#i-check2"/></svg> В коллекции: <b>${n}</b></div>
    <button class="btn vs-ton" style="width:100%" ${n<=0?'disabled':''} onclick="vsOpenSend('${g.id}')"><svg class="i"><use href="#i-send"/></svg> Подарить другу</button>
    <button class="btn ghost" style="width:100%;margin-top:8px" onclick="vsOpenBuy('${g.id}')"><svg class="i"><use href="#i-plus"/></svg> Купить ещё</button>`;
}

/* --- отправка подарка в чат --- */
function vsOpenSend(id){ vsSendGiftId = id; vsRenderSend(); openSheet('vs-send'); }
function vsRenderSend(){
  const v = document.getElementById('vsSendView'); if(!v) return;
  const g = vsGiftById(vsSendGiftId);
  const chats = (typeof CHATS !== 'undefined' ? CHATS : []).filter(c=>c && c.name && c.kind !== 'channel');
  v.innerHTML = `
    <div class="vs-send-head">${vsGiftSvg(g?g.art:'crystal',44)}<div><b>Кому подарить</b><small>${g?esc(g.name):''} · выбери чат</small></div></div>
    <div class="vs-send-note">
      <label class="vs-field-lbl" for="vsSendNote">Поздравление <span class="vs-send-opt">необязательно · до 120</span></label>
      <textarea id="vsSendNote" maxlength="120" placeholder="С днём рождения, друг!" oninput="VS_SEND_NOTE=this.value;vsSendNoteCount()">${esc(VS_SEND_NOTE||'')}</textarea>
      <div class="vs-send-count" id="vsSendCount">${(VS_SEND_NOTE||'').length} / 120</div>
    </div>
    <div class="vs-send-list">${chats.map(c=>{
      const ava = c.avaIcon ? `<svg class="i"><use href="#i-${c.avaIcon}"/></svg>` : esc(String(c.ava||c.name[0]||'O'));
      const cid = String(c.id).replace(/[^A-Za-z0-9_-]/g,'');
      return `<button class="vs-send-row" onclick="vsSendGift('${vsSendGiftId}','${cid}')"><span class="vs-send-ava">${ava}</span><span class="vs-send-name">${esc(c.name)}</span><svg class="i vs-send-go"><use href="#i-send"/></svg></button>`;
    }).join('')}</div>`;
}
function vsSendNoteCount(){
  const el = document.getElementById('vsSendCount');
  if(el) el.textContent = ((VS_SEND_NOTE||'').length) + ' / 120';
}
function vsSendGift(giftId, chatId){
  const g = vsGiftById(giftId);
  if(!g || (VS_TON.owned[giftId]||0) <= 0){ toast('Подарка нет в коллекции'); return; }
  const c = (typeof CHATS !== 'undefined' ? CHATS : []).find(x=>String(x.id).replace(/[^A-Za-z0-9_-]/g,'')===String(chatId));
  if(!c){ toast('Чат не найден'); return; }
  const note = String(VS_SEND_NOTE||'').trim().slice(0,120);
  VS_TON.owned[giftId]--; if(VS_TON.owned[giftId] <= 0) delete VS_TON.owned[giftId];
  VS_TON.tx.unshift({ t:'send', ton:0, why:'Подарок «'+g.name+'» → '+c.name, at:Date.now() });
  if(VS_TON.tx.length > 200) VS_TON.tx.length = 200;
  vsTonSave();
  const tm = (typeof nowT === 'function') ? nowT() : '';
  c.msgs = c.msgs || [];
  const msg = { in:0, t:tm, kind:'gift', gift:giftId };
  if(note) msg.note = note;
  c.msgs.push(msg);
  c.preview = 'Ты: Подарок · ' + g.name; c.time = tm;
  VS_SEND_NOTE = '';
  closeSheet();
  if(typeof showTab === 'function') showTab('chats');
  if(typeof openConv === 'function') openConv(c.id);
  if(typeof renderChatList === 'function'){ const s = document.getElementById('chatSearch'); renderChatList(s ? s.value : ''); }
  toast('Подарок отправлен: ' + c.name);
}

/* --- история операций TON --- */
function vsOpenHistory(){ vsRenderHist(); openSheet('vs-ton-hist'); }
function vsRenderHist(){
  const v = document.getElementById('vsHistView'); if(!v) return;
  const tx = VS_TON.tx || [];
  v.innerHTML = tx.length
    ? `<div class="vs-hist-list">${tx.map(x=>{
        const cls = x.t==='+' ? 'in' : x.t==='-' ? 'out' : 'send';
        const val = x.t==='send' ? `<svg class="i"><use href="#i-send"/></svg>` : `${x.t==='+'?'+':'−'}${vsTonFmt(x.ton)} TON`;
        return `<div class="vs-hist-row"><div class="vs-hist-why">${esc(x.why)}<small>${vsWhen(x.at)}</small></div><div class="vs-hist-val ${cls}">${val}</div></div>`;
      }).join('')}</div>`
    : `<div class="vs-mine-empty"><b>Пока нет операций</b><span>Пополни кошелёк или купи подарок</span></div>`;
}

function vsCopyAddr(){ try{ navigator.clipboard && navigator.clipboard.writeText(VS_TON.addr||''); }catch(e){} toast('Адрес кошелька скопирован'); }
function vsConnectWallet(){
  showPopup({ ico:'lock', title:'TON Connect',
    body:'Честный прототип: в релизе подключишь Tonkeeper или Wallet в один тап, и подарки станут настоящими NFT в сети TON. Сейчас баланс и коллекция хранятся локально — безопасно и бесплатно.',
    actions:[{label:'Понятно'}] });
}

/* праздничный дождь кристаллов поверх экрана TON при покупке */
function vsTonBurst(){
  const host = document.getElementById('screen-ton'); if(!host) return;
  const r = host.getBoundingClientRect();
  if(r.width < 10 || r.height < 10) return;
  const box = document.createElement('div');
  box.className = 'vs-rain';
  box.style.cssText = `left:${r.left}px;top:${r.top}px;width:${r.width}px;height:${r.height}px;--fy:${Math.round(r.height + 60)}px`;
  const n = 12 + Math.floor(Math.random() * 6);
  let h = '';
  for(let k = 0; k < n; k++){
    const sz = Math.round(11 + Math.random() * 13);
    const x = (Math.random() * 94).toFixed(1);
    const dur = (1.1 + Math.random() * 0.5).toFixed(2);
    const dl = (Math.random() * 0.4).toFixed(2);
    h += `<i style="left:${x}%;width:${sz}px;height:${sz}px;animation-duration:${dur}s;animation-delay:${dl}s"><svg viewBox="0 0 100 100"><path d="M50 8 L86 42 L50 92 L14 42 Z" fill="#2fb3f2" stroke="#bfeaff" stroke-width="5"/><path d="M50 8 L64 42 L50 92 L36 42 Z" fill="#eafaff" opacity=".55"/></svg></i>`;
  }
  box.innerHTML = h;
  document.body.appendChild(box);
  setTimeout(()=>box.remove(), 2300);
}

/* сообщение-подарок в чате */
const _prevMsgHtmlVs = msgHtml;
msgHtml = function(m, idx){
  if(m && m.kind === 'gift'){
    const g = vsGiftById(m.gift) || VS_GIFTS[0];
    const checks = m.in ? '' : (typeof I==='function' ? I('check2') : '');
    const time = `<span class="t">${m.t||''}${checks}</span>`;
    const rar = vsRarity(g);
    const noteHtml = m.note ? `<div class="vs-giftmsg-note">${esc(m.note)}</div>` : '';
    const repeatBtn = m.in ? '' : `<button class="vs-giftmsg-act" onclick="event.stopPropagation();vsResend(${idx})"><svg class="i"><use href="#i-send"/></svg> Повторить</button>`;
    const openHint = `<span class="vs-giftmsg-tap">${m.in?'нажми, чтобы открыть':'превью подарка'}</span>`;
    return `<div class="msg gift-msg ${m.in?'in':'out'}" style="align-self:${m.in?'flex-start':'flex-end'}"><div class="vs-giftmsg vs-rar-${rar}" onclick="vsUnwrap(${idx})">${vsGiftSvg(g.art,88)}<div class="vs-giftmsg-cap"><b>${vsGemMark(11)} Подарок <i class="vs-giftmsg-rar vs-rar-${rar}">${VS_RARITY_LABEL[rar]}</i></b><span>${esc(g.name)}</span></div>${noteHtml}${openHint}${repeatBtn}</div>${time}</div>`;
  }
  return _prevMsgHtmlVs(m, idx);
};

/* рендер экрана TON при переходе на вкладку */
const _prevShowTabVs = showTab;
showTab = function(t){
  _prevShowTabVs(t);
  if(t === 'ton') vsRenderTon();
};

/* ============================================================
   6. УЛУЧШЕНИЯ TG/Steam/Discord Gifts:
      rarity, wishlist, personal note, unwrap+sound, quick resend
   ============================================================ */

/* --- редкость: legendary=premium, epic≤6k, rare≤12k, common иначе --- */
const VS_RARITY_LABEL = { common:'ОБЫЧНЫЙ', rare:'РЕДКИЙ', epic:'ЭПИЧЕСКИЙ', legendary:'ЛЕГЕНДАРНЫЙ' };
function vsRarity(g){
  if(!g) return 'common';
  if(g.premium) return 'legendary';
  if(g.supply <= 6000) return 'epic';
  if(g.supply <= 12000) return 'rare';
  return 'common';
}

/* --- вишлист (клиент отмечает желаемое, другие видят и дарят) --- */
function vsWishHas(id){ return (VS_TON.wish||[]).indexOf(id) >= 0; }
function vsWishToggle(id){
  VS_TON.wish = VS_TON.wish || [];
  const i = VS_TON.wish.indexOf(id);
  if(i>=0){ VS_TON.wish.splice(i,1); toast('Убрано из вишлиста'); }
  else { VS_TON.wish.unshift(id); toast('Добавлено в вишлист'); }
  if(VS_TON.wish.length > 30) VS_TON.wish.length = 30;
  vsTonSave();
  const buyV = document.getElementById('vsBuyView');
  if(buyV && buyV.dataset && buyV.dataset.gid === id) vsOpenBuy(id);
  const scr = document.getElementById('screen-ton');
  if(scr && scr.classList.contains('active')) vsRenderTon();
}

/* --- персональное поздравление (буфер + счётчик) --- */
var VS_SEND_NOTE = '';

/* --- Web Audio: короткий «спарк» при распаковке (без файлов, без сети) --- */
var vsAudio = null;
function vsAudioCtx(){
  if(vsAudio) return vsAudio;
  try{ vsAudio = new (window.AudioContext || window.webkitAudioContext)(); }catch(e){ vsAudio = null; }
  return vsAudio;
}
function vsUnwrapSound(){
  const a = vsAudioCtx(); if(!a) return;
  try{ if(a.state === 'suspended') a.resume(); }catch(e){}
  const now = a.currentTime;
  const notes = [
    { f: 660, t: 0.00 },
    { f: 880, t: 0.07 },
    { f:1175, t: 0.14 },
    { f:1568, t: 0.22 },
    { f:1976, t: 0.32 }
  ];
  notes.forEach(n=>{
    try{
      const o = a.createOscillator(), g = a.createGain();
      o.type = 'triangle'; o.frequency.value = n.f;
      g.gain.setValueAtTime(0, now + n.t);
      g.gain.linearRampToValueAtTime(0.16, now + n.t + 0.012);
      g.gain.exponentialRampToValueAtTime(0.0006, now + n.t + 0.28);
      o.connect(g).connect(a.destination);
      o.start(now + n.t); o.stop(now + n.t + 0.30);
    }catch(e){}
  });
}

/* --- анимация распаковки: fullscreen-overlay, confetti + звук --- */
function vsUnwrap(idx){
  if(typeof currentChat === 'undefined' || !currentChat || !currentChat.msgs) return;
  const m = currentChat.msgs[idx]; if(!m || m.kind !== 'gift') return;
  const g = vsGiftById(m.gift) || VS_GIFTS[0];
  const rar = vsRarity(g);
  const who = m.in ? esc(currentChat.name) : 'Тебе';
  const noteH = m.note ? `<div class="vs-uwrap-note"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 21s-7-4.35-7-10a4.5 4.5 0 0 1 8-2.8A4.5 4.5 0 0 1 19 11c0 5.65-7 10-7 10z" fill="currentColor"/></svg><span>${esc(m.note)}</span></div>` : '';
  vsUnwrapClose();
  const wrap = document.createElement('div');
  wrap.className = 'vs-uwrap';
  wrap.innerHTML = `
    <div class="vs-uwrap-back" onclick="vsUnwrapClose()"></div>
    <div class="vs-uwrap-card vs-rar-${rar}">
      <button class="vs-uwrap-x" onclick="vsUnwrapClose()" aria-label="Закрыть">${I('close')||'×'}</button>
      <div class="vs-uwrap-from">${who} · <span class="vs-rar-chip vs-rar-${rar}">${VS_RARITY_LABEL[rar]}</span></div>
      <div class="vs-uwrap-art">${vsGiftSvg(g.art,140)}</div>
      <div class="vs-uwrap-name">${esc(g.name)}</div>
      <div class="vs-uwrap-price">${vsGemMark(14)} ${vsTonFmt(g.price)} TON</div>
      ${noteH}
      <button class="btn vs-ton" style="width:100%;margin-top:14px" onclick="vsUnwrapClose()">Красота</button>
    </div>`;
  document.body.appendChild(wrap);
  requestAnimationFrame(()=>wrap.classList.add('on'));
  vsUnwrapConfetti(wrap);
  vsUnwrapSound();
}
function vsUnwrapClose(){
  document.querySelectorAll('.vs-uwrap').forEach(el=>{
    el.classList.remove('on');
    setTimeout(()=>{ try{ el.remove(); }catch(e){} }, 260);
  });
}
function vsUnwrapConfetti(host){
  const layer = document.createElement('div');
  layer.className = 'vs-uwrap-conf';
  const cols = ['#9AFF00','#0098EA','#4fd0ff','#c6ff70','#ffffff','#b57cff'];
  const n = 46; let h = '';
  for(let k=0;k<n;k++){
    const x = (Math.random()*100).toFixed(1);
    const dur = (1.5 + Math.random()*1.3).toFixed(2);
    const dl = (Math.random()*0.35).toFixed(2);
    const rot = Math.round(Math.random()*720 - 360);
    const w = (5 + Math.random()*4).toFixed(0);
    const hgt = (9 + Math.random()*6).toFixed(0);
    const c = cols[k % cols.length];
    h += `<i style="left:${x}%;width:${w}px;height:${hgt}px;background:${c};--r:${rot}deg;animation-duration:${dur}s;animation-delay:${dl}s"></i>`;
  }
  layer.innerHTML = h;
  host.appendChild(layer);
  setTimeout(()=>{ try{ layer.remove(); }catch(e){} }, 3400);
}

/* --- быстрый повтор подарка тому же получателю --- */
function vsResend(idx){
  if(typeof currentChat === 'undefined' || !currentChat || !currentChat.msgs) return;
  const m = currentChat.msgs[idx]; if(!m || m.kind !== 'gift') return;
  const g = vsGiftById(m.gift); if(!g) return;
  const own = VS_TON.owned[g.id] || 0;
  if(own <= 0){
    showPopup({ ico:'plus', title:'Купить и подарить снова?',
      body:`«${esc(g.name)}» в коллекции закончился. Купи ещё один за ${vsTonFmt(g.price)} TON и подари тому же получателю.`,
      actions:[{label:'Купить', onclick:()=>{ if(typeof closePopup==='function') closePopup(); vsOpenBuy(g.id); }},{label:'Позже', ghost:true}] });
    return;
  }
  VS_TON.owned[g.id]--; if(VS_TON.owned[g.id] <= 0) delete VS_TON.owned[g.id];
  const c = currentChat;
  VS_TON.tx.unshift({ t:'send', ton:0, why:'Подарок «'+g.name+'» → '+c.name+' · повтор', at:Date.now() });
  if(VS_TON.tx.length > 200) VS_TON.tx.length = 200;
  vsTonSave();
  const tm = (typeof nowT === 'function') ? nowT() : '';
  c.msgs = c.msgs || [];
  c.msgs.push({ in:0, t:tm, kind:'gift', gift:g.id });
  c.preview = 'Ты: Подарок · ' + g.name; c.time = tm;
  if(typeof renderMsgs === 'function') renderMsgs();
  if(typeof renderChatList === 'function'){ const s = document.getElementById('chatSearch'); renderChatList(s ? s.value : ''); }
  toast('Отправлен снова');
}

/* ============================================================
   7. FRAGMENT-LEVEL: 22+ подарков с NFT-метаданными, 3D-превью,
      история продаж, биржа, аукцион, событие «Новый год»,
      3D-carousel коллекции, fly-to-friend, наборы стикеров,
      TON viewer в истории
   ============================================================ */

/* --- расширяем VS_GIFTS до 22 подарков + NFT-метаданные --- */
VS_GIFTS.push(
  { id:'flame',    name:'Огонёк',      art:'flame',    price:1.5, supply:15000, sold:0  },
  { id:'diamond',  name:'Алмаз',       art:'diamond',  price:6.0, supply:4000,  sold:0  },
  { id:'lightning',name:'Молния',      art:'lightning',price:2.2, supply:12000, sold:0  },
  { id:'moon',     name:'Луна',        art:'moon',     price:3.4, supply:9000,  sold:0  },
  { id:'sun',      name:'Солнце',      art:'sun',      price:4.1, supply:7000,  sold:0  },
  { id:'shield',   name:'Щит',         art:'shield',   price:2.8, supply:8000,  sold:0  },
  { id:'key',      name:'Ключ',        art:'key',      price:3.0, supply:8500,  sold:0  },
  { id:'skull',    name:'Череп',       art:'skull',    price:5.5, supply:5000,  sold:0  },
  { id:'phoenix',  name:'Феникс',      art:'phoenix',  price:15,  supply:500,   sold:0,  premium:true },
  { id:'dragon',   name:'Дракон',      art:'dragon',   price:20,  supply:300,   sold:0,   premium:true },
  { id:'giftbox',  name:'Подарок',     art:'giftbox',  price:1.9, supply:20000, sold:0 },
  { id:'nyeye',    name:'Ёлочный OKO', art:'nyeye',    price:9,   supply:2500,  sold:0 },
  /* Правка Даниэля 09.08: у подарка был event с отсчётом «до Нового года»,
     который просто отсчитывал 3 суток от запуска приложения. Выдуманных
     событий быть не должно — баннер вернём, когда событие будет настоящим. */
);
/* NFT-метаданные для всех подарков (chain: TON, transferable:true, deterministic contract) */
VS_GIFTS.forEach(function(g, i){
  g.chain = 'TON';
  g.transferable = true;
  /* master-collection адрес — детерминированный, декоративный */
  var hex = '';
  var s = 0; for(var k=0;k<g.id.length;k++) s = (s*31 + g.id.charCodeAt(k)) >>> 0;
  for(var k=0;k<40;k++){ s = (s*1103515245 + 12345) & 0x7fffffff; hex += '0123456789abcdef'[s & 15]; }
  g.contract = 'EQ' + hex.slice(0,44).replace(/[^0-9a-f]/g,'a');
  g.royalty = g.premium ? 7.5 : 5;
});

/* --- новые SVG-арт подарки (расширяем vsGiftArt) --- */
var _vsGiftArtPrev = vsGiftArt;
vsGiftArt = function(a, u){
  if(a==='flame') return '<defs><linearGradient id="'+u+'f" x1="0" y1="1" x2="0" y2="0"><stop offset="0" stop-color="#4f8f00"/><stop offset=".6" stop-color="#9AFF00"/><stop offset="1" stop-color="#eaffb8"/></linearGradient></defs>'
    + '<path d="M50 8 C60 24 78 34 74 60 C70 82 60 92 50 92 C40 92 30 82 26 60 C22 34 40 24 50 8 Z" fill="url(#'+u+'f)" stroke="#0a1403" stroke-width="1.6" stroke-linejoin="round"/>'
    + '<path d="M50 32 C56 42 62 52 58 66 C55 78 46 78 42 66 C40 58 46 52 50 32 Z" fill="#fff" opacity=".6"/>'
    + '<circle cx="60" cy="86" r="3" fill="#c6ff70"/>';
  if(a==='diamond') return '<defs><linearGradient id="'+u+'d" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#e7ff9e"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#5aa300"/></linearGradient></defs>'
    + '<path d="M10 34 L30 12 L70 12 L90 34 L50 92 Z" fill="url(#'+u+'d)" stroke="#0a1403" stroke-width="1.8" stroke-linejoin="round"/>'
    + '<path d="M10 34 L90 34 L50 92 Z" fill="#fff" opacity=".22"/>'
    + '<path d="M30 12 L38 34 L50 92 L62 34 L70 12" fill="none" stroke="#f4ffd6" stroke-width="1.6" opacity=".85"/>'
    + '<path d="M10 34 L38 34 M62 34 L90 34" stroke="#f4ffd6" stroke-width="1.6" opacity=".7" fill="none"/>'
    + '<path d="M18 22 l1.6 4 4 1.6 -4 1.6 -1.6 4 -1.6 -4 -4 -1.6 4 -1.6 z" fill="#fff" opacity=".9"/>';
  if(a==='lightning') return '<defs><linearGradient id="'+u+'l" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#eaffb8"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></linearGradient></defs>'
    + '<path d="M62 4 L18 60 L44 60 L36 96 L82 40 L54 40 Z" fill="url(#'+u+'l)" stroke="#0a1403" stroke-width="1.8" stroke-linejoin="round"/>'
    + '<path d="M56 14 L28 54 L46 54 L40 84" stroke="#fff" stroke-width="2.4" opacity=".55" fill="none" stroke-linejoin="round" stroke-linecap="round"/>'
    + '<circle cx="16" cy="30" r="2.4" fill="#c6ff70" opacity=".9"/>'
    + '<circle cx="88" cy="70" r="2.4" fill="#c6ff70" opacity=".9"/>';
  if(a==='moon') return '<defs><radialGradient id="'+u+'m" cx=".3" cy=".3" r=".8"><stop offset="0" stop-color="#f4ffd6"/><stop offset=".55" stop-color="#c6ff70"/><stop offset="1" stop-color="#5aa300"/></radialGradient></defs>'
    + '<path d="M64 8 A44 44 0 1 0 92 64 A34 34 0 1 1 64 8 Z" fill="url(#'+u+'m)" stroke="#0a1403" stroke-width="1.8" stroke-linejoin="round"/>'
    + '<circle cx="42" cy="42" r="4" fill="#5aa300" opacity=".6"/>'
    + '<circle cx="58" cy="60" r="3" fill="#5aa300" opacity=".55"/>'
    + '<circle cx="34" cy="66" r="2.5" fill="#5aa300" opacity=".5"/>'
    + '<path d="M84 18 l1.6 4 4 1.6 -4 1.6 -1.6 4 -1.6 -4 -4 -1.6 4 -1.6 z" fill="#fff" opacity=".95"/>';
  if(a==='sun') return '<defs><radialGradient id="'+u+'s" cx=".5" cy=".5" r=".6"><stop offset="0" stop-color="#ffffff"/><stop offset=".4" stop-color="#eaffb8"/><stop offset="1" stop-color="#9AFF00"/></radialGradient></defs>'
    + '<g stroke="#9AFF00" stroke-width="4.5" stroke-linecap="round">'
    + '<line x1="50" y1="4" x2="50" y2="18"/><line x1="50" y1="82" x2="50" y2="96"/>'
    + '<line x1="4" y1="50" x2="18" y2="50"/><line x1="82" y1="50" x2="96" y2="50"/>'
    + '<line x1="18" y1="18" x2="28" y2="28"/><line x1="72" y1="72" x2="82" y2="82"/>'
    + '<line x1="82" y1="18" x2="72" y2="28"/><line x1="18" y1="82" x2="28" y2="72"/></g>'
    + '<circle cx="50" cy="50" r="24" fill="url(#'+u+'s)" stroke="#0a1403" stroke-width="1.6"/>'
    + '<circle cx="42" cy="42" r="4" fill="#fff" opacity=".8"/>';
  if(a==='shield') return '<defs><linearGradient id="'+u+'h" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#e4ff9e"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></linearGradient></defs>'
    + '<path d="M50 6 L84 18 V52 C84 74 68 88 50 94 C32 88 16 74 16 52 V18 Z" fill="url(#'+u+'h)" stroke="#0a1403" stroke-width="2" stroke-linejoin="round"/>'
    + '<path d="M50 6 L84 18 V52 C84 74 68 88 50 94 Z" fill="#fff" opacity=".16"/>'
    + '<path d="M50 26 L58 46 H80 L62 58 L69 80 L50 68 L31 80 L38 58 L20 46 H42 Z" fill="#0a1403" opacity=".85"/>'
    + '<path d="M50 30 L56 44 L70 46 L58 55 L62 70 L50 62 Z" fill="#c6ff70"/>';
  if(a==='key') return '<defs><linearGradient id="'+u+'k" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#eaffb8"/><stop offset=".55" stop-color="#9AFF00"/><stop offset="1" stop-color="#5aa300"/></linearGradient></defs>'
    + '<circle cx="30" cy="30" r="20" fill="none" stroke="url(#'+u+'k)" stroke-width="8"/>'
    + '<circle cx="30" cy="30" r="8" fill="#0a1403"/>'
    + '<path d="M42 42 L88 88" stroke="url(#'+u+'k)" stroke-width="8" stroke-linecap="round"/>'
    + '<path d="M70 68 L82 68 L82 78" stroke="url(#'+u+'k)" stroke-width="8" stroke-linecap="round" fill="none"/>'
    + '<path d="M78 78 L88 78" stroke="url(#'+u+'k)" stroke-width="8" stroke-linecap="round"/>'
    + '<path d="M20 20 l1.6 4 4 1.6 -4 1.6 -1.6 4 -1.6 -4 -4 -1.6 4 -1.6 z" fill="#fff" opacity=".8"/>';
  if(a==='skull') return '<defs><radialGradient id="'+u+'k" cx=".5" cy=".4" r=".7"><stop offset="0" stop-color="#eaffb8"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></radialGradient></defs>'
    + '<path d="M50 8 C74 8 90 26 90 48 C90 60 84 68 78 74 L78 84 C78 88 74 92 68 92 L32 92 C26 92 22 88 22 84 L22 74 C16 68 10 60 10 48 C10 26 26 8 50 8 Z" fill="url(#'+u+'k)" stroke="#0a1403" stroke-width="2" stroke-linejoin="round"/>'
    + '<ellipse cx="34" cy="52" rx="10" ry="12" fill="#0a1403"/>'
    + '<ellipse cx="66" cy="52" rx="10" ry="12" fill="#0a1403"/>'
    + '<circle cx="34" cy="52" r="3" fill="#c6ff70"/>'
    + '<circle cx="66" cy="52" r="3" fill="#c6ff70"/>'
    + '<path d="M42 74 L46 82 L50 74 L54 82 L58 74" stroke="#0a1403" stroke-width="4" fill="none" stroke-linejoin="round" stroke-linecap="round"/>'
    + '<path d="M46 66 L50 72 L54 66 Z" fill="#0a1403"/>';
  if(a==='phoenix') return '<defs><linearGradient id="'+u+'p" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#eaffb8"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></linearGradient><linearGradient id="'+u+'w" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#e4ff9e"/><stop offset="1" stop-color="#5aa300"/></linearGradient></defs>'
    + '<path d="M20 62 C10 42 22 20 40 22 C30 32 34 42 44 40 C34 54 42 66 50 60 C42 72 50 84 60 78 C50 88 62 92 68 84 C74 72 78 60 82 66 C88 54 82 40 72 42 C82 30 76 16 62 22 C56 8 44 8 40 22 Z" fill="url(#'+u+'w)" opacity=".85"/>'
    + '<path d="M50 12 C58 24 66 36 60 54 C55 68 44 68 40 54 C36 40 42 24 50 12 Z" fill="url(#'+u+'p)" stroke="#0a1403" stroke-width="1.6" stroke-linejoin="round"/>'
    + '<circle cx="48" cy="30" r="5" fill="#0a1403"/>'
    + '<circle cx="46" cy="28" r="1.6" fill="#c6ff70"/>'
    + '<path d="M50 60 C55 74 55 84 50 92 C45 84 45 74 50 60 Z" fill="#fff" opacity=".55"/>'
    + '<path d="M20 20 l1.8 4.4 4.4 1.8 -4.4 1.8 -1.8 4.4 -1.8 -4.4 -4.4 -1.8 4.4 -1.8 z" fill="#fff" opacity=".9"/>';
  if(a==='dragon') return '<defs><linearGradient id="'+u+'d" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#eaffb8"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></linearGradient></defs>'
    + '<path d="M14 82 C10 62 30 50 44 58 C56 40 78 42 82 60 C90 54 92 40 82 32 C68 40 62 30 66 18 C48 22 44 40 50 52 C34 46 20 60 22 74 Z" fill="url(#'+u+'d)" stroke="#0a1403" stroke-width="1.8" stroke-linejoin="round"/>'
    + '<path d="M66 18 L74 8 L70 22" fill="#c6ff70" stroke="#0a1403" stroke-width="1.4"/>'
    + '<circle cx="72" cy="34" r="3.5" fill="#0a1403"/>'
    + '<path d="M76 42 C82 44 88 42 90 38 C86 46 82 48 78 48" fill="#c6ff70"/>'
    + '<path d="M28 76 C32 68 36 74 32 82 M42 84 C46 76 50 82 46 90 M56 86 C60 78 64 84 60 92" stroke="#0a1403" stroke-width="1.4" fill="none"/>'
    + '<path d="M50 52 C56 50 62 54 62 60" stroke="#fff" stroke-width="2" opacity=".55" fill="none"/>';
  if(a==='giftbox') return '<defs><linearGradient id="'+u+'g" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#e4ff9e"/><stop offset=".5" stop-color="#9AFF00"/><stop offset="1" stop-color="#4f8f00"/></linearGradient></defs>'
    + '<rect x="12" y="40" width="76" height="52" rx="6" fill="url(#'+u+'g)" stroke="#0a1403" stroke-width="2"/>'
    + '<rect x="8" y="30" width="84" height="18" rx="4" fill="#6cba12" stroke="#0a1403" stroke-width="2"/>'
    + '<rect x="8" y="30" width="84" height="5" rx="2" fill="#fff" opacity=".28"/>'
    + '<rect x="44" y="30" width="12" height="62" fill="#0a1403"/>'
    + '<rect x="45" y="30" width="10" height="62" fill="#c6ff70"/>'
    + '<path d="M50 30 C42 18 26 18 30 30 M50 30 C58 18 74 18 70 30" fill="none" stroke="#0a1403" stroke-width="4"/>'
    + '<path d="M50 30 C42 18 26 18 30 30 M50 30 C58 18 74 18 70 30" fill="none" stroke="#c6ff70" stroke-width="2.4"/>';
  if(a==='nyeye') return '<defs><linearGradient id="'+u+'e" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#e4ff9e"/><stop offset="1" stop-color="#9AFF00"/></linearGradient></defs>'
    + '<path d="M18 30 C34 12 66 12 82 30 C74 20 60 16 50 16 C40 16 26 20 18 30 Z" fill="#c81f2b"/>'
    + '<path d="M18 30 L82 30" stroke="#0a1403" stroke-width="2" fill="none"/>'
    + '<circle cx="72" cy="18" r="6" fill="#fff"/>'
    + '<path d="M18 54 Q50 32 82 54 Q50 78 18 54 Z" fill="url(#'+u+'e)" stroke="#0a1403" stroke-width="2"/>'
    + '<circle cx="50" cy="54" r="16" fill="#0a1403"/>'
    + '<circle cx="50" cy="54" r="12" fill="url(#'+u+'e)"/>'
    + '<circle cx="50" cy="54" r="6" fill="#0a1403"/>'
    + '<circle cx="46" cy="50" r="2.4" fill="#fff"/>'
    + '<circle cx="22" cy="88" r="3" fill="#c81f2b"/><circle cx="50" cy="92" r="3" fill="#4fd0ff"/><circle cx="78" cy="88" r="3" fill="#c6ff70"/>'
    + '<path d="M84 8 l1.8 4.4 4.4 1.8 -4.4 1.8 -1.8 4.4 -1.8 -4.4 -4.4 -1.8 4.4 -1.8 z" fill="#fff" opacity=".9"/>';
  return _vsGiftArtPrev(a, u);
};

/* --- детерминированная история продаж (последние 5 сделок) --- */
function vsPriceHistory(g){
  if(!g) return [];
  var seed = 0; for(var i=0;i<g.id.length;i++) seed = (seed*31 + g.id.charCodeAt(i)) >>> 0;
  function rnd(){ seed = (seed*1103515245 + 12345) & 0x7fffffff; return seed/0x7fffffff; }
  var base = g.price;
  var out = [];
  var now = Date.now();
  var day = 86400000;
  for(var k=0;k<5;k++){
    var delta = (rnd() - 0.42) * 0.45 * base;
    var p = Math.max(0.1, Math.round((base + delta)*100)/100);
    var ago = Math.round((0.4 + rnd() * 2.6 + k*3.2) * day);
    out.push({ price:p, at: now - ago });
  }
  out.sort(function(a,b){ return b.at - a.at; });
  return out;
}
function vsFloorPrice(g){
  var h = vsPriceHistory(g);
  var mn = h.length ? h[0].price : g.price;
  for(var i=0;i<h.length;i++) if(h[i].price < mn) mn = h[i].price;
  return Math.round(mn*100)/100;
}
function vsPriceChange(g){
  var h = vsPriceHistory(g);
  if(h.length < 2) return 0;
  var oldest = h[h.length-1].price;
  var newest = h[0].price;
  return Math.round(((newest - oldest)/oldest) * 1000) / 10; /* %, 1 знак */
}
function vsSparklineSvg(g, w, h){
  var hist = vsPriceHistory(g).slice().reverse();
  var vals = hist.map(function(x){ return x.price; });
  var mn = Math.min.apply(null, vals), mx = Math.max.apply(null, vals);
  var range = Math.max(0.001, mx - mn);
  var pad = 2;
  var pts = vals.map(function(v, i){
    var x = (i/(vals.length-1)) * (w-2*pad) + pad;
    var y = h - pad - ((v - mn)/range) * (h - 2*pad);
    return [x, y];
  });
  var d = pts.map(function(p, i){ return (i===0?'M':'L') + p[0].toFixed(1) + ' ' + p[1].toFixed(1); }).join(' ');
  var areaD = d + ' L' + (w-pad).toFixed(1) + ' ' + (h-pad).toFixed(1) + ' L' + pad + ' ' + (h-pad).toFixed(1) + ' Z';
  var last = pts[pts.length-1];
  return '<svg viewBox="0 0 '+w+' '+h+'" preserveAspectRatio="none"><path d="'+areaD+'" fill="rgba(0,152,234,.18)"/>'
    + '<path d="'+d+'" stroke="#4fd0ff" stroke-width="1.6" fill="none" stroke-linejoin="round" stroke-linecap="round"/>'
    + '<circle cx="'+last[0].toFixed(1)+'" cy="'+last[1].toFixed(1)+'" r="1.8" fill="#4fd0ff"/></svg>';
}
function vsFriendlyAgo(ts){
  var s = Math.max(0, Math.round((Date.now() - ts)/1000));
  if(s < 60) return s+'с';
  if(s < 3600) return Math.round(s/60)+'м';
  if(s < 86400) return Math.round(s/3600)+'ч';
  return Math.round(s/86400)+'д';
}

/* --- псевдо-owner для NFT --- */
var VS_OWNERS = []  /* демо-данные удалены 09.08: владельцы подарков — источник только API */;
function vsGiftOwner(g){
  var s = 0; for(var i=0;i<g.id.length;i++) s = (s*31 + g.id.charCodeAt(i)) >>> 0;
  return VS_OWNERS[s % VS_OWNERS.length];
}
function vsMintNumber(g){ return String(g.sold).padStart(5,'0'); }
function vsNftId(g){ return 'OKO-' + g.art.toUpperCase() + '-' + vsMintNumber(g); }

/* --- ссылка «TON viewer» (декоративная — реального выхода в сеть нет) --- */
function vsExplorerLink(kind, ref){
  var base = 'https://tonviewer.com/';
  if(kind === 'tx' && ref) return base + 'transaction/' + encodeURIComponent(ref);
  if(kind === 'addr' && ref) return base + encodeURIComponent(ref);
  return base;
}
function vsFakeTxHash(){
  var hex = '0123456789abcdef', s = '';
  for(var i=0;i<64;i++) s += hex[Math.floor(Math.random()*16)];
  return s;
}

/* --- Обратный отсчёт: HH:MM:SS форматирование --- */
function vsCountdown(endsAt){
  var ms = Math.max(0, endsAt - Date.now());
  var d = Math.floor(ms / 86400000);
  var h = Math.floor((ms % 86400000) / 3600000);
  var m = Math.floor((ms % 3600000) / 60000);
  var s = Math.floor((ms % 60000) / 1000);
  return { d:d, h:h, m:m, s:s, ms:ms };
}
function vsCdCells(cd, size){
  var pad = function(x){ return String(x).padStart(2,'0'); };
  return '<i><b>'+pad(cd.d)+'</b><s>дн</s></i>'
       + '<i><b>'+pad(cd.h)+'</b><s>час</s></i>'
       + '<i><b>'+pad(cd.m)+'</b><s>мин</s></i>'
       + '<i><b>'+pad(cd.s)+'</b><s>сек</s></i>';
}
/* глобальный тикер обратных отсчётов */
var VS_CD_TIMER = null;
function vsStartCdTicker(){
  if(VS_CD_TIMER) return;
  VS_CD_TIMER = setInterval(function(){
    document.querySelectorAll('[data-vs-cd]').forEach(function(el){
      var end = +el.getAttribute('data-vs-cd');
      if(!end) return;
      var cd = vsCountdown(end);
      el.innerHTML = vsCdCells(cd);
      if(cd.ms <= 0){ el.removeAttribute('data-vs-cd'); }
    });
  }, 1000);
}

/* --- события: активные лоты (Новый год и т.п.) --- */
function vsActiveEvents(){
  var out = [];
  VS_GIFTS.forEach(function(g){
    if(g.event && g.event.endsAt > Date.now()) out.push(g);
  });
  return out;
}

/* --- аукцион редкого (одиночный «Дракон») --- */
/* Правка Даниэля 09.08: «ноль демо-данных, никаких выдуманных людей».
   Здесь жил аукцион «Дракона» со ставкой 42 TON и пятью придуманными
   участниками (Дарья К., Артём Н. и остальные). Реального аукциона нет —
   значит и показывать нечего: пустая структура, карточка не рисуется,
   пока настоящий аукцион не запустится с бэкенда. */
var VS_AUCTION = {
  active: false,
  giftId: null,
  currentBid: 0,
  bidders: 0,
  endsAt: 0,
  bids: []
};
function vsAuctionCard(){
  if(!VS_AUCTION.active) return '';
  var g = vsGiftById(VS_AUCTION.giftId); if(!g) return '';
  var cd = vsCountdown(VS_AUCTION.endsAt);
  return '<div class="vs-auction-hero" onclick="vsOpenAuction()">'
    + '<div class="vs-auction-row">'
      + '<div class="vs-auction-art">' + vsGiftSvg(g.art, 60) + '</div>'
      + '<div class="vs-auction-info">'
        + '<span class="vs-auction-tag">' + I('crown') + ' Аукцион</span>'
        + '<div class="vs-auction-name">' + esc(g.name) + '</div>'
        + '<div class="vs-auction-bid">' + vsGemMark(13) + ' Текущая ставка <b>' + vsTonFmt(VS_AUCTION.currentBid) + '</b> TON · ' + VS_AUCTION.bidders + ' участников</div>'
      + '</div>'
    + '</div>'
    + '<div class="vs-auction-cd" data-vs-cd="' + VS_AUCTION.endsAt + '">' + vsCdCells(cd) + '</div>'
  + '</div>';
}
function vsOpenAuction(){
  var v = document.getElementById('vsAuctionView'); if(!v) return;
  var g = vsGiftById(VS_AUCTION.giftId); if(!g) return;
  var cd = vsCountdown(VS_AUCTION.endsAt);
  var rar = vsRarity(g);
  var nextBid = Math.round((VS_AUCTION.currentBid + 5) * 100) / 100;
  var bidsH = VS_AUCTION.bids.map(function(b){
    return '<div class="vs-auc-bid-row"><span>' + esc(b.who) + ' · ' + vsFriendlyAgo(b.at) + ' назад</span><b>' + vsGemMark(11) + ' ' + vsTonFmt(b.amt) + '</b></div>';
  }).join('');
  v.innerHTML = ''
    + '<div class="vs-buy-art vs-rar-' + rar + '">' + vsGiftSvg(g.art, 120) + '<span class="vs-nft-shine"></span></div>'
    + '<div class="vs-buy-rar"><span class="vs-rar-chip vs-rar-' + rar + '">' + VS_RARITY_LABEL[rar] + '</span></div>'
    + '<h2 class="vs-auc-name">' + esc(g.name) + '</h2>'
    + '<div class="vs-auc-cur">Текущая ставка ' + vsGemMark(16) + '<b>' + vsTonFmt(VS_AUCTION.currentBid) + '</b> TON · ' + VS_AUCTION.bidders + ' участников</div>'
    + '<div class="vs-auc-cd-big" data-vs-cd="' + VS_AUCTION.endsAt + '">' + vsCdCells(cd) + '</div>'
    + '<div class="vs-auc-bids"><div class="vs-auc-bids-head">' + I('clock') + ' Последние ставки</div>' + bidsH + '</div>'
    + '<button class="btn vs-auc" style="width:100%" onclick="vsPlaceBid(5)">' + vsGemMark(15) + ' Поставить ' + vsTonFmt(nextBid) + ' TON (+5)</button>'
    + '<div class="vs-buy-note" style="margin-top:10px">' + I('lock') + ' По окончании таймера подарок уйдёт лидеру. Прототип: ставки локальные, реальный аукцион запускается в релизе.</div>';
  openSheet('vs-auction');
}
function vsPlaceBid(delta){
  var g = vsGiftById(VS_AUCTION.giftId); if(!g) return;
  var need = Math.round((VS_AUCTION.currentBid + (delta||5))*100)/100;
  if(VS_TON.balance < need){
    if(typeof showPopup==='function') showPopup({ ico:'money', title:'Не хватает TON',
      body:'Для ставки нужно ' + vsTonFmt(need) + ' TON. Пополни кошелёк и вернись — сумма спишется только если ты станешь победителем.',
      actions:[{label:'Пополнить TON', onclick:function(){ if(typeof closePopup==='function') closePopup(); vsOpenTopup(); }},{label:'Позже', ghost:true}] });
    return;
  }
  VS_AUCTION.currentBid = need;
  VS_AUCTION.bidders++;
  VS_AUCTION.bids.unshift({ who: (typeof PROFILE!=='undefined' && PROFILE.name) || 'Ты', amt: need, at: Date.now() });
  if(VS_AUCTION.bids.length > 8) VS_AUCTION.bids.length = 8;
  vsOpenAuction();
  toast('Ставка ' + vsTonFmt(need) + ' TON принята');
}

/* --- баннер события «Новый год» --- */
function vsEventBanner(){
  var acts = vsActiveEvents(); if(!acts.length) return '';
  var g = acts[0];
  var cd = vsCountdown(g.event.endsAt);
  return '<div class="vs-event-bnr" onclick="vsOpenBuy(\'' + g.id + '\')">'
    + '<div class="vs-event-art">' + vsGiftSvg(g.art, 44) + '</div>'
    + '<div class="vs-event-txt">'
      + '<span class="vs-event-tag">' + esc(g.event.label) + '</span>'
      + '<div class="vs-event-title">' + esc(g.name) + ' · доступен ограниченно</div>'
      + '<div class="vs-event-cd" data-vs-cd="' + g.event.endsAt + '">' + vsCdCells(cd) + '</div>'
    + '</div>'
  + '</div>';
}

/* --- Fly-to-friend анимация подарка --- */
function vsFlyToFriend(art, targetSelector){
  try{
    var host = document.body;
    var w = window.innerWidth, h = window.innerHeight;
    var target = null;
    if(targetSelector){ try{ target = document.querySelector(targetSelector); }catch(e){} }
    var tRect = target ? target.getBoundingClientRect() : null;
    var tx = tRect ? (tRect.left + tRect.width/2) : (w - 50);
    var ty = tRect ? (tRect.top + tRect.height/2) : (h - 50);
    var fly = document.createElement('div');
    fly.className = 'vs-fly';
    fly.style.setProperty('--fx', (w/2) + 'px');
    fly.style.setProperty('--fy', (h/2) + 'px');
    fly.style.setProperty('--tx', tx + 'px');
    fly.style.setProperty('--ty', ty + 'px');
    fly.innerHTML = vsGiftSvg(art, 90);
    host.appendChild(fly);
    /* хвостовые искры */
    var trails = 8;
    for(var k=0;k<trails;k++){
      (function(k){
        setTimeout(function(){
          var t = document.createElement('div');
          t.className = 'vs-fly-trail';
          var pr = k/trails;
          var x = (w/2) + (tx - w/2) * pr;
          var y = (h/2) + (ty - h/2) * pr - Math.sin(pr*Math.PI)*40;
          t.style.left = x + 'px'; t.style.top = y + 'px';
          host.appendChild(t);
          setTimeout(function(){ try{ t.remove(); }catch(e){} }, 700);
        }, 90 + k*70);
      })(k);
    }
    setTimeout(function(){ try{ fly.remove(); }catch(e){} }, 1000);
  }catch(e){}
}

/* --- 3D-carousel коллекции --- */
var VS_CAR_IDX = 0;
function vsOpenCarousel(){
  var owned = Object.keys(VS_TON.owned).filter(function(k){ return VS_TON.owned[k]>0; });
  if(!owned.length){ toast('Коллекция пуста'); return; }
  vsCloseCarousel();
  VS_CAR_IDX = 0;
  var wrap = document.createElement('div');
  wrap.className = 'vs-carousel';
  var items = owned.map(function(id, i){
    var g = vsGiftById(id); if(!g) return '';
    var rar = vsRarity(g);
    return '<div class="vs-carousel-item vs-rar-' + rar + '" data-i="' + i + '">'
      + vsGiftSvg(g.art, 120)
      + '<b>' + esc(g.name) + '</b>'
      + '<span>' + vsGemMark(11) + ' ' + vsTonFmt(g.price) + ' TON · ×' + (VS_TON.owned[id]||0) + '</span>'
    + '</div>';
  }).join('');
  var dots = owned.map(function(_, i){ return '<i class="' + (i===0?'on':'') + '"></i>'; }).join('');
  wrap.innerHTML = ''
    + '<button class="vs-carousel-close" onclick="vsCloseCarousel()" aria-label="Закрыть">' + (I('close')||'×') + '</button>'
    + '<div class="vs-carousel-stage"><div class="vs-carousel-track" id="vsCarTrack">' + items + '</div></div>'
    + '<div class="vs-carousel-nav">'
      + '<button onclick="vsCarStep(-1)" aria-label="Назад">' + I('chev') + '</button>'
      + '<button onclick="vsCarStep(1)" aria-label="Вперёд" style="transform:rotate(180deg)">' + I('chev') + '</button>'
    + '</div>'
    + '<div class="vs-carousel-dots" id="vsCarDots">' + dots + '</div>';
  document.body.appendChild(wrap);
  requestAnimationFrame(function(){ wrap.classList.add('on'); vsCarLayout(); });
  /* autoplay */
  wrap._vsAutoTimer = setInterval(function(){
    if(!wrap.parentNode){ clearInterval(wrap._vsAutoTimer); return; }
    vsCarStep(1);
  }, 3200);
}
function vsCloseCarousel(){
  document.querySelectorAll('.vs-carousel').forEach(function(el){
    if(el._vsAutoTimer) clearInterval(el._vsAutoTimer);
    el.classList.remove('on');
    setTimeout(function(){ try{ el.remove(); }catch(e){} }, 260);
  });
}
function vsCarLayout(){
  var track = document.getElementById('vsCarTrack'); if(!track) return;
  var items = track.querySelectorAll('.vs-carousel-item');
  var n = items.length;
  var angStep = 360 / n;
  var radius = Math.max(180, Math.min(240, 60 * n));
  track.style.transform = 'translateZ(-' + radius + 'px) rotateY(' + (-VS_CAR_IDX * angStep) + 'deg)';
  items.forEach(function(el, i){
    el.style.transform = 'rotateY(' + (i * angStep) + 'deg) translateZ(' + radius + 'px)';
  });
  var dots = document.querySelectorAll('#vsCarDots i');
  dots.forEach(function(d, i){ d.classList.toggle('on', ((i - VS_CAR_IDX) % n + n) % n === 0); });
}
function vsCarStep(dir){
  var track = document.getElementById('vsCarTrack'); if(!track) return;
  var n = track.querySelectorAll('.vs-carousel-item').length; if(!n) return;
  VS_CAR_IDX = (VS_CAR_IDX + dir + n) % n;
  vsCarLayout();
}

/* --- Биржа/Marketplace --- */
function vsOpenMarket(){
  var v = document.getElementById('vsMarketView'); if(!v) return;
  var rows = VS_GIFTS.slice().sort(function(a,b){ return vsFloorPrice(a) - vsFloorPrice(b); }).map(function(g){
    var floor = vsFloorPrice(g);
    var chg = vsPriceChange(g);
    var chgCls = chg >= 0 ? 'up' : '';
    var chgS = (chg >= 0 ? '+' : '') + chg.toFixed(1) + '%';
    var rar = vsRarity(g);
    return '<div class="vs-mrkt-row" onclick="vsOpenOffers(\'' + g.id + '\')">'
      + vsGiftSvg(g.art, 44)
      + '<div class="vs-mrkt-info">'
        + '<div class="vs-mrkt-name">' + esc(g.name) + '<span class="vs-rar-chip vs-rar-' + rar + ' vs-mrkt-rar">' + VS_RARITY_LABEL[rar] + '</span></div>'
        + '<div class="vs-mrkt-floor">Флор ' + vsGemMark(10) + ' <b>' + vsTonFmt(floor) + '</b> · <s class="' + chgCls + '">' + chgS + '</s> · <span>' + (g.supply - g.sold).toLocaleString('ru-RU') + ' в продаже</span></div>'
      + '</div>'
      + '<div class="vs-mrkt-spark">' + vsSparklineSvg(g, 64, 28) + '</div>'
      + '<div class="vs-mrkt-cta">' + I('chev') + '</div>'
    + '</div>';
  }).join('');
  v.innerHTML = ''
    + '<div class="vs-market-head"><div><div class="vs-market-title">FLOOR PRICE · 24H</div><div class="vs-market-sub">Живые ордера покупки/продажи между пользователями</div></div></div>'
    + '<div class="vs-mrkt-list">' + rows + '</div>'
    + '<div class="vs-buy-note" style="margin-top:14px">' + I('lock') + ' Прототип: ордера моковые для демонстрации UX. Полноценная P2P-биржа NFT-подарков — в релизе.</div>';
  openSheet('vs-market');
}
function vsOpenOffers(giftId){
  var v = document.getElementById('vsOffersView'); if(!v) return;
  var g = vsGiftById(giftId); if(!g) return;
  var floor = vsFloorPrice(g);
  /* 4-6 фейковых офферов от других юзеров */
  var seed = 0; for(var i=0;i<g.id.length;i++) seed = (seed*31 + g.id.charCodeAt(i)) >>> 0;
  function rnd(){ seed = (seed*1103515245 + 12345) & 0x7fffffff; return seed/0x7fffffff; }
  var offers = [];
  var n = 4 + Math.floor(rnd() * 3);
  for(var k=0;k<n;k++){
    var who = VS_OWNERS[Math.floor(rnd() * VS_OWNERS.length)];
    var isBid = rnd() > 0.5;
    var mult = isBid ? (0.72 + rnd() * 0.25) : (1.05 + rnd() * 0.35);
    var price = Math.max(0.1, Math.round(g.price * mult * 100) / 100);
    offers.push({ who: who, price: price, kind: isBid ? 'bid' : 'ask' });
  }
  offers.sort(function(a,b){ return b.price - a.price; });
  var bidsH = offers.filter(function(o){ return o.kind==='bid'; }).map(function(o, i){
    return '<div class="vs-off-row"><div class="vs-off-who"><span class="vs-off-ava">' + esc(o.who[0]) + '</span><div>' + esc(o.who) + '<small>Оффер · ' + Math.round((1 - o.price/g.price) * 100) + '% ниже цены</small></div></div>'
      + '<div class="vs-off-price">' + vsGemMark(11) + ' ' + vsTonFmt(o.price) + '</div>'
      + '<div class="vs-off-actions"><button class="vs-off-btn accept" onclick="vsAcceptOffer(\'' + g.id + '\',' + o.price + ')">Принять</button></div></div>';
  }).join('');
  var asksH = offers.filter(function(o){ return o.kind==='ask'; }).map(function(o){
    return '<div class="vs-off-row"><div class="vs-off-who"><span class="vs-off-ava">' + esc(o.who[0]) + '</span><div>' + esc(o.who) + '<small>Ордер на продажу</small></div></div>'
      + '<div class="vs-off-price">' + vsGemMark(11) + ' ' + vsTonFmt(o.price) + '</div>'
      + '<div class="vs-off-actions"><button class="vs-off-btn accept" onclick="vsBuyFromOrder(\'' + g.id + '\',' + o.price + ')">Купить</button></div></div>';
  }).join('');
  v.innerHTML = ''
    + '<div class="vs-off-head">' + vsGiftSvg(g.art, 44) + '<div><b>' + esc(g.name) + '</b><small>Флор ' + vsTonFmt(floor) + ' TON · ' + (g.supply - g.sold).toLocaleString('ru-RU') + ' доступно</small></div></div>'
    + '<div class="vs-price-hist-head" style="margin:6px 0 4px">' + I('clock') + ' ОРДЕРА ПОКУПКИ (BIDS)</div>'
    + (bidsH || '<div class="vs-off-row"><span style="color:var(--dim);font-size:12px;padding:8px 0">Нет активных офферов</span></div>')
    + '<div class="vs-price-hist-head" style="margin:16px 0 4px">' + I('sticker') + ' ОРДЕРА НА ПРОДАЖУ (ASKS)</div>'
    + (asksH || '<div class="vs-off-row"><span style="color:var(--dim);font-size:12px;padding:8px 0">Нет активных ордеров</span></div>')
    + '<div class="vs-buy-note" style="margin-top:14px">' + I('lock') + ' Прототип: биржа моковая. В релизе — P2P-мэтчинг с эскроу в TON.</div>';
  openSheet('vs-offers');
}
function vsAcceptOffer(giftId, price){
  var g = vsGiftById(giftId); if(!g) return;
  var own = VS_TON.owned[giftId] || 0;
  if(own <= 0){ toast('Подарка нет в коллекции'); return; }
  VS_TON.owned[giftId]--; if(VS_TON.owned[giftId] <= 0) delete VS_TON.owned[giftId];
  VS_TON.balance = Math.round((VS_TON.balance + price * 0.95) * 100) / 100; /* -5% комиссия площадки */
  VS_TON.tx.unshift({ t:'+', ton:Math.round(price * 0.95 * 100) / 100, why:'Продажа · '+g.name+' (комиссия 5%)', at:Date.now(), hash:vsFakeTxHash() });
  if(VS_TON.tx.length > 200) VS_TON.tx.length = 200;
  vsTonSave();
  closeSheet();
  toast('Продано за ' + vsTonFmt(price * 0.95) + ' TON');
  vsTonBurst();
  vsRenderTon();
}
function vsBuyFromOrder(giftId, price){
  var g = vsGiftById(giftId); if(!g) return;
  if(VS_TON.balance < price){
    if(typeof showPopup==='function') showPopup({ ico:'money', title:'Не хватает TON',
      body:'Нужно ' + vsTonFmt(price) + ' TON. Пополни кошелёк.', actions:[{label:'Пополнить', onclick:function(){ if(typeof closePopup==='function') closePopup(); vsOpenTopup(); }},{label:'Позже', ghost:true}] });
    return;
  }
  VS_TON.balance = Math.round((VS_TON.balance - price) * 100) / 100;
  VS_TON.owned[giftId] = (VS_TON.owned[giftId] || 0) + 1;
  VS_TON.tx.unshift({ t:'-', ton:price, why:'Покупка с биржи · '+g.name, at:Date.now(), hash:vsFakeTxHash() });
  if(VS_TON.tx.length > 200) VS_TON.tx.length = 200;
  vsTonSave();
  closeSheet();
  toast('Куплено с биржи · ' + g.name);
  vsTonBurst();
  vsRenderTon();
}

/* --- NFT-детали (открываются кликом по «Данные NFT» в шите покупки/подарка) --- */
function vsOpenNft(giftId){
  var v = document.getElementById('vsNftView'); if(!v) return;
  var g = vsGiftById(giftId); if(!g) return;
  var nftId = vsNftId(g);
  var owner = vsGiftOwner(g);
  var isMine = (VS_TON.owned[g.id] || 0) > 0;
  var addr = g.contract;
  var explorerUrl = vsExplorerLink('addr', addr);
  v.innerHTML = ''
    + '<div class="vs-buy-art">' + vsGiftSvg(g.art, 100) + '</div>'
    + '<h2 class="vs-buy-name" style="font-size:18px">' + esc(g.name) + '</h2>'
    + '<div class="vs-nft-rows">'
      + '<div class="vs-nft-row"><span>NFT ID</span><b>' + esc(nftId) + '</b></div>'
      + '<div class="vs-nft-row"><span>Chain</span><b class="ok">TON Mainnet</b></div>'
      + '<div class="vs-nft-row"><span>Стандарт</span><b>TIP-4 / NFT Item</b></div>'
      + '<div class="vs-nft-row"><span>Коллекция</span><b>' + esc(addr.slice(0,6) + '…' + addr.slice(-4)) + '</b></div>'
      + '<div class="vs-nft-row"><span>Владелец</span><b>' + (isMine ? ((typeof PROFILE!=='undefined' && PROFILE.name) || 'Ты') + ' (ты)' : esc(owner)) + '</b></div>'
      + '<div class="vs-nft-row"><span>Минт #</span><b>' + vsMintNumber(g) + ' из ' + g.supply.toLocaleString('ru-RU') + '</b></div>'
      + '<div class="vs-nft-row"><span>Transferable</span><b class="ok">Да</b></div>'
      + '<div class="vs-nft-row"><span>Роялти автору</span><b>' + g.royalty + '%</b></div>'
    + '</div>'
    + '<a class="vs-explorer-btn" href="' + explorerUrl + '" target="_blank" rel="noopener">' + I('share') + ' Открыть в TON viewer</a>'
    + '<div class="vs-buy-note" style="margin-top:10px">' + I('lock') + ' Данные NFT — контракт-заглушка прототипа. В релизе адреса будут настоящими TIP-4-контрактами в сети TON.</div>';
  openSheet('vs-nft');
}

/* --- 3D-preview (drag rotate) --- */
function vs3dInit(host){
  if(!host || host._vs3d) return;
  host._vs3d = true;
  var el = host.querySelector('.vs-3d-inner'); if(!el) return;
  var rx = 8, ry = -14, dragging = false, sx = 0, sy = 0, srx = 0, sry = 0, t = 0;
  var apply = function(){ el.style.transform = 'rotateX(' + rx.toFixed(1) + 'deg) rotateY(' + ry.toFixed(1) + 'deg)'; };
  apply();
  var start = function(e){ dragging = true; var pt = e.touches ? e.touches[0] : e; sx = pt.clientX; sy = pt.clientY; srx = rx; sry = ry; };
  var move = function(e){ if(!dragging) return; var pt = e.touches ? e.touches[0] : e; ry = sry + (pt.clientX - sx) * 0.55; rx = srx - (pt.clientY - sy) * 0.55; apply(); if(e.cancelable && e.touches) e.preventDefault(); };
  var end = function(){ dragging = false; };
  host.addEventListener('mousedown', start);
  host.addEventListener('touchstart', start, {passive:true});
  window.addEventListener('mousemove', move);
  window.addEventListener('touchmove', move, {passive:false});
  window.addEventListener('mouseup', end);
  window.addEventListener('touchend', end);
  /* idle-вращение — тонкая амплитуда */
  var raf = function(){
    if(!host.isConnected) return;
    if(!dragging){ t += 0.008; ry += Math.cos(t) * 0.35; rx = 8 + Math.sin(t*.7) * 3; apply(); }
    requestAnimationFrame(raf);
  };
  requestAnimationFrame(raf);
}

/* ---------- патч vsOpenBuy: 3D-preview, история продаж, NFT-плашка ---------- */
var _vsOpenBuyPrev = vsOpenBuy;
vsOpenBuy = function(id){
  _vsOpenBuyPrev(id);
  var g = vsGiftById(id); if(!g) return;
  var v = document.getElementById('vsBuyView'); if(!v) return;
  var rar = vsRarity(g);
  var floor = vsFloorPrice(g);
  var chg = vsPriceChange(g);
  var chgCls = chg >= 0 ? 'up' : 'dn';
  var chgS = (chg >= 0 ? '+' : '') + chg.toFixed(1) + '%';
  /* заменяем стандартный vs-buy-art на 3D-stage */
  var art = v.querySelector('.vs-buy-art');
  if(art && !art.classList.contains('vs-3d-injected')){
    var inner = art.innerHTML;
    var newArt = '<div class="vs-3d-stage vs-rar-' + rar + '" id="vsBuy3d"><div class="vs-3d-inner"><div class="vs-3d-face"></div>' + inner + '</div></div>'
      + '<div class="vs-3d-hint">' + I('bolt') + ' Крути пальцем — 3D-превью</div>';
    art.outerHTML = newArt;
    var stage = document.getElementById('vsBuy3d');
    if(stage) vs3dInit(stage);
  }
  /* мета-плашка: floor / owners / minted */
  var metaHtml = ''
    + '<div class="vs-meta-grid">'
      + '<div class="vs-meta-cell floor"><b>' + vsGemMark(12) + ' ' + vsTonFmt(floor) + '</b><small>Floor · <span class="' + chgCls + '" style="color:' + (chg>=0?'var(--lime)':'#ff9db0') + '">' + chgS + '</span></small></div>'
      + '<div class="vs-meta-cell owners"><b>' + g.sold.toLocaleString('ru-RU') + ' / ' + g.supply.toLocaleString('ru-RU') + '</b><small>Minted · ' + Math.round(g.sold*100/g.supply) + '%</small></div>'
    + '</div>';
  /* история продаж */
  var hist = vsPriceHistory(g);
  var histH = ''
    + '<div class="vs-price-hist">'
      + '<div class="vs-price-hist-head">' + I('clock') + ' ПОСЛЕДНИЕ ПРОДАЖИ<span class="vs-hist-spark">' + vsSparklineSvg(g, 64, 20) + '</span></div>'
      + '<div class="vs-price-hist-list">'
        + hist.map(function(x, i){
          var prev = hist[i+1] ? hist[i+1].price : x.price;
          var up = x.price > prev, dn = x.price < prev;
          return '<div class="vs-price-hist-row"><span>' + vsFriendlyAgo(x.at) + ' назад · ' + esc(VS_OWNERS[(i + g.id.length) % VS_OWNERS.length]) + '</span>'
            + '<b class="' + (up?'up':dn?'dn':'') + '">' + vsGemMark(10) + ' ' + vsTonFmt(x.price) + '</b></div>';
        }).join('')
      + '</div>'
    + '</div>';
  /* NFT-плашка */
  var nftH = '<div class="vs-nft-card" onclick="vsOpenNft(\'' + g.id + '\')">'
    + '<div class="vs-nft-mark">' + I('lock') + '</div>'
    + '<div class="vs-nft-info"><b>' + esc(vsNftId(g)) + '</b><small>TON · TIP-4 · transferable</small></div>'
    + '<div class="vs-nft-link">Данные ' + I('chev') + '</div>'
  + '</div>';
  /* вставляем мета/историю/NFT перед note+action */
  var note = v.querySelector('.vs-buy-note');
  if(note && !v.querySelector('.vs-meta-grid')){
    var block = document.createElement('div');
    block.innerHTML = metaHtml + histH + nftH;
    while(block.firstChild) note.parentNode.insertBefore(block.firstChild, note);
  }
};

/* ---------- патч vsOpenGift: добавляем 3D-preview, NFT-плашку, оффер-биржу ---------- */
var _vsOpenGiftPrev = vsOpenGift;
vsOpenGift = function(id){
  _vsOpenGiftPrev(id);
  var v = document.getElementById('vsGiftView'); if(!v) return;
  var g = vsGiftById(vsGiftDetailId); if(!g) return;
  var art = v.querySelector('.vs-buy-art');
  if(art && !art.classList.contains('vs-3d-injected')){
    var inner = art.innerHTML;
    art.outerHTML = '<div class="vs-3d-stage" id="vsGift3d"><div class="vs-3d-inner"><div class="vs-3d-face"></div>' + inner + '</div></div>'
      + '<div class="vs-3d-hint">' + I('bolt') + ' Крути пальцем — 3D-превью</div>';
    var stage = document.getElementById('vsGift3d');
    if(stage) vs3dInit(stage);
  }
  /* NFT-плашка + «На биржу» */
  if(!v.querySelector('.vs-nft-card')){
    var extra = document.createElement('div');
    extra.innerHTML = ''
      + '<div class="vs-nft-card" onclick="vsOpenNft(\'' + g.id + '\')">'
        + '<div class="vs-nft-mark">' + I('lock') + '</div>'
        + '<div class="vs-nft-info"><b>' + esc(vsNftId(g)) + '</b><small>TON · TIP-4 · твой NFT</small></div>'
        + '<div class="vs-nft-link">Данные ' + I('chev') + '</div>'
      + '</div>'
      + '<button class="btn ghost" style="width:100%;margin-top:8px" onclick="vsOpenOffers(\'' + g.id + '\')">' + I('sticker') + ' Офферы и биржа</button>';
    v.appendChild(extra);
  }
};

/* ---------- патч vsSendGift: fly-to-friend анимация ---------- */
var _vsSendGiftPrev = vsSendGift;
vsSendGift = function(giftId, chatId){
  var g = vsGiftById(giftId);
  var art = g ? g.art : 'crystal';
  _vsSendGiftPrev(giftId, chatId);
  /* после переключения в чат — запустить fly к иконке кошелька или к аватарке шапки */
  setTimeout(function(){
    vsFlyToFriend(art, '#convAva, .conv-head .ava, [data-icon="chats"]');
  }, 220);
};

/* ---------- патч vsRenderTon: баннер события, аукцион, кнопка биржи, 3D-carousel ---------- */
var _vsRenderTonPrev = vsRenderTon;
vsRenderTon = function(){
  _vsRenderTonPrev();
  var root = document.getElementById('vsTonRoot'); if(!root) return;
  /* Заменяем 4 колонки на 5 (добавляем «Биржа») */
  var actsRow = root.querySelector('.vs-ton-acts');
  if(actsRow && !actsRow.querySelector('.vs-ton-act-market')){
    actsRow.classList.add('vs-acts-5');
    var btn = document.createElement('button');
    btn.className = 'vs-ton-act ghost vs-ton-act-market';
    btn.onclick = vsOpenMarket;
    btn.innerHTML = '<svg class="i"><use href="#i-sticker"/></svg><span>Биржа</span>';
    /* вставляем перед «История» (последний) */
    var last = actsRow.lastElementChild;
    if(last) actsRow.insertBefore(btn, last); else actsRow.appendChild(btn);
  }
  /* Магазин: перед .vs-gshop вставляем баннер события и аукцион */
  if(VS_TON_TAB === 'shop'){
    var gshop = root.querySelector('.vs-gshop');
    if(gshop && !root.querySelector('.vs-event-bnr')){
      var evH = vsEventBanner();
      if(evH){ var wrap = document.createElement('div'); wrap.innerHTML = evH; gshop.parentNode.insertBefore(wrap.firstChild, gshop); }
    }
    if(gshop && !root.querySelector('.vs-auction-hero')){
      var aucH = vsAuctionCard();
      if(aucH){ var wrap2 = document.createElement('div'); wrap2.innerHTML = aucH; gshop.parentNode.insertBefore(wrap2.firstChild, gshop); }
    }
    /* блок наборов стикеров под магазином */
    if(gshop && !root.querySelector('.vs-packs-hero')){
      var pk = document.createElement('div');
      pk.innerHTML = ''
        + '<div class="vs-packs-hero" onclick="vsOpenPacks()">'
          + '<div class="vs-packs-hero-ic">' + I('sticker') + '</div>'
          + '<div class="vs-packs-hero-txt"><b>Наборы стикеров OKO</b><small>Коллекции для чатов · тап в чате открывает палитру</small></div>'
          + '<div class="vs-packs-hero-cta">' + I('chev') + '</div>'
        + '</div>';
      gshop.parentNode.insertBefore(pk.firstChild, gshop.nextSibling);
    }
  } else if(VS_TON_TAB === 'mine'){
    /* коллекция: кнопка «Смотреть в 3D-карусели» */
    var mineEmpty = root.querySelector('.vs-mine-empty');
    if(!mineEmpty && !root.querySelector('.vs-mine-carousel-btn')){
      var carBtn = document.createElement('button');
      carBtn.className = 'vs-mine-carousel-btn';
      carBtn.onclick = vsOpenCarousel;
      carBtn.innerHTML = I('rocket') + ' 3D-карусель коллекции';
      var tabs = root.querySelector('.vs-ton-tabs');
      if(tabs) tabs.parentNode.insertBefore(carBtn, tabs.nextSibling);
    }
  }
  vsStartCdTicker();
};

/* ---------- патч vsRenderHist: TON viewer link ---------- */
var _vsRenderHistPrev = vsRenderHist;
vsRenderHist = function(){
  _vsRenderHistPrev();
  var v = document.getElementById('vsHistView'); if(!v) return;
  var tx = VS_TON.tx || [];
  v.querySelectorAll('.vs-hist-row').forEach(function(row, i){
    if(row.querySelector('.vs-hist-explorer')) return;
    var x = tx[i]; if(!x) return;
    if(!x.hash) x.hash = vsFakeTxHash();
    var why = row.querySelector('.vs-hist-why');
    if(!why) return;
    var link = document.createElement('a');
    link.href = vsExplorerLink('tx', x.hash);
    link.target = '_blank'; link.rel = 'noopener';
    link.className = 'vs-hist-explorer';
    link.innerHTML = I('share') + ' Explorer';
    link.onclick = function(e){ e.stopPropagation(); };
    /* добавляем как small-строку под причиной */
    var small = why.querySelector('small');
    if(small){
      var wrap = document.createElement('div');
      wrap.style.display = 'flex'; wrap.style.gap = '8px'; wrap.style.alignItems = 'center'; wrap.style.marginTop = '4px';
      wrap.appendChild(link);
      why.appendChild(wrap);
    }
  });
};

/* ---------- STICKER PACKS: 4 набора, каждый — 6 стикеров с бренд-артом ---------- */
var VS_STK_PACKS = [
  { id:'neon',   name:'Неон OKO',      price:120, headIco:'bolt',   ics:['bolt','rocket','fire','crown','logo','star'] },
  { id:'winter', name:'Зимний',        price:80,  headIco:'star',   ics:['star','check','heart','wow','laugh','thumb'] },
  { id:'meme',   name:'Мемы',          price:60,  headIco:'laugh',  ics:['laugh','wow','sad','thumb','heart','fire'] },
  { id:'cosmos', name:'Космос',        price:150, headIco:'rocket', ics:['rocket','crown','bolt','logo','star','check'] }
];
function vsPacksOwned(){ return (VS_TON.packs && VS_TON.packs.slice()) || []; }
function vsPacksHas(id){ return vsPacksOwned().indexOf(id) >= 0; }
function vsOpenPacks(){
  var v = document.getElementById('vsPacksView'); if(!v) return;
  var cards = VS_STK_PACKS.map(function(p){
    var preview = p.ics.slice(0,6).map(function(ic){ return vsBaseSvg(ic, 38); }).join('');
    var owned = vsPacksHas(p.id);
    return '<div class="vs-pack-card" onclick="vsOpenPack(\'' + p.id + '\')">'
      + (owned ? '<span class="vs-pack-owned">В коллекции</span>' : '')
      + '<div class="vs-pack-preview">' + preview + '</div>'
      + '<div class="vs-pack-name">' + esc(p.name) + '</div>'
      + '<div class="vs-pack-meta"><span>' + p.ics.length + ' стикеров</span><b>' + vsGemMark(10) + ' ' + vsTonFmt(p.price/320) + '</b></div>'
    + '</div>';
  }).join('');
  v.innerHTML = '<div class="vs-packs-list">' + cards + '</div>'
    + '<div class="vs-buy-note" style="margin-top:14px">' + I('lock') + ' Приобретённые наборы появляются отдельной секцией в палитре стикеров чата — тап на стикер отправляет его собеседнику.</div>';
  openSheet('vs-packs');
}
function vsOpenPack(id){
  var p = null;
  for(var i=0;i<VS_STK_PACKS.length;i++) if(VS_STK_PACKS[i].id===id){ p = VS_STK_PACKS[i]; break; }
  if(!p) return;
  var v = document.getElementById('vsPackView'); if(!v) return;
  var owned = vsPacksHas(id);
  var priceTon = p.price/320;
  var grid = p.ics.map(function(ic){ return '<button>' + vsBaseSvg(ic, 52) + '</button>'; }).join('');
  var actH = owned
    ? '<button class="btn ghost" style="width:100%" onclick="closeSheet()">Уже в коллекции</button>'
    : '<button class="btn vs-ton" style="width:100%" onclick="vsBuyPack(\'' + p.id + '\')">' + vsGemMark(15) + ' Купить за ' + vsTonFmt(priceTon) + ' TON</button>';
  v.innerHTML = ''
    + '<div class="vs-pack-detail-head"><b>' + esc(p.name) + '</b><small>' + p.ics.length + ' фирменных стикеров OKO</small></div>'
    + '<div class="vs-pack-detail-grid">' + grid + '</div>'
    + actH
    + '<div class="vs-buy-note" style="margin-top:10px">' + I('lock') + ' Стикеры этого набора будут доступны в палитре чата в секции «' + esc(p.name) + '».</div>';
  openSheet('vs-pack');
}
function vsBuyPack(id){
  var p = null;
  for(var i=0;i<VS_STK_PACKS.length;i++) if(VS_STK_PACKS[i].id===id){ p = VS_STK_PACKS[i]; break; }
  if(!p) return;
  var priceTon = Math.round((p.price/320) * 100) / 100;
  if(VS_TON.balance < priceTon){
    if(typeof showPopup==='function') showPopup({ ico:'money', title:'Не хватает TON',
      body:'Нужно ' + vsTonFmt(priceTon) + ' TON. Пополни кошелёк.', actions:[{label:'Пополнить', onclick:function(){ if(typeof closePopup==='function') closePopup(); vsOpenTopup(); }},{label:'Позже', ghost:true}] });
    return;
  }
  VS_TON.packs = vsPacksOwned();
  VS_TON.packs.push(p.id);
  VS_TON.balance = Math.round((VS_TON.balance - priceTon) * 100) / 100;
  VS_TON.tx.unshift({ t:'-', ton:priceTon, why:'Набор стикеров · ' + p.name, at:Date.now(), hash:vsFakeTxHash() });
  if(VS_TON.tx.length > 200) VS_TON.tx.length = 200;
  vsTonSave();
  closeSheet();
  toast('Набор «' + p.name + '» в коллекции');
  vsTonBurst();
}

/* ---------- патч openStickers: показать секции купленных пакетов ---------- */
var _vsOpenStickers2 = openStickers;
openStickers = function(){
  _vsOpenStickers2();
  var grid = document.getElementById('stickersGrid'); if(!grid) return;
  var owned = vsPacksOwned();
  owned.forEach(function(pid){
    var p = null;
    for(var i=0;i<VS_STK_PACKS.length;i++) if(VS_STK_PACKS[i].id===pid){ p = VS_STK_PACKS[i]; break; }
    if(!p) return;
    if(grid.querySelector('[data-pack="' + pid + '"]')) return;
    var head = document.createElement('div');
    head.className = 'vs-pack-sec-head'; head.setAttribute('data-pack', pid);
    head.innerHTML = '<svg class="i"><use href="#i-' + p.headIco + '"/></svg>' + esc(p.name).toUpperCase() + '<span class="vs-pack-sec-tag">КУПЛЕНО</span>';
    grid.appendChild(head);
    p.ics.forEach(function(ic){
      var b = document.createElement('button');
      b.className = 'stk-btn';
      b.setAttribute('data-pack-sticker', pid + ':' + ic);
      b.onclick = function(){ vsSendPackSticker(ic, p.name); };
      b.innerHTML = vsBaseSvg(ic, 58) + '<small>' + p.name + '</small>';
      grid.appendChild(b);
    });
  });
};
function vsSendPackSticker(ic, packName){
  if(typeof currentChat === 'undefined' || !currentChat){ toast('Открой чат для отправки'); return; }
  /* добавляем стикер как обычное сообщение с иконкой (используем формат sticker + fake index) */
  closeSheet();
  var idx = -1;
  for(var i=0;i<STICKERS.length;i++) if(STICKERS[i].ic === ic && STICKERS[i].pack !== 'ton'){ idx = i; break; }
  if(idx >= 0){
    if(typeof sendSticker === 'function') sendSticker(idx);
    else if(typeof pushMsg === 'function') pushMsg({in:0, t:(typeof nowT==='function'?nowT():''), kind:'sticker', stk:idx});
  } else {
    /* fallback: как текст */
    if(typeof pushMsg === 'function') pushMsg({in:0, t:(typeof nowT==='function'?nowT():''), kind:'text', text:'Стикер · ' + packName});
  }
  vsStickerSpark();
}

/* ---------- САМОИНИЦИАЛИЗАЦИЯ ---------- */
(function vsInit(){
  /* демо: официальный канал OKO в ленте тоже с галочкой */
  try{ if(typeof VERIFIED !== 'undefined') VERIFIED.add('OKO · Официальный'); }catch(e){}
  vsInsertProw();
  vsInitNpost();
  /* первые рендеры ядра прошли до патчей — дорисовать бейджи в текущем DOM */
  vsDecorateAll();
  /* TON подарки: иконка-символ, заголовок, тайл в хабе, первый рендер */
  vsInjectTonSymbol();
  /* короткий заголовок «TON» — не режется на 390px (было «TON Подарки» → «TON Подар…») */
  try{ if(typeof regTitle === 'function') regTitle('ton', 'TON'); }catch(e){}
  /* тайл хаба — фирменный лайм как у всех тайлов (bg/fg по умолчанию: var(--lime-dim)/var(--accent)).
     Синяя айдентика TON остаётся ВНУТРИ экрана TON, но плитка хаба единообразна с сеткой. */
  try{ if(typeof addSvcTile === 'function') addSvcTile({ id:'ton', label:'TON Подарки', ico:'vs-gem', onclick:()=>{ if(typeof showTab==='function') showTab('ton'); } }); }catch(e){}
  const scr = document.getElementById('screen-ton');
  if(scr && scr.classList.contains('active')) vsRenderTon();
  /* глобальный таймер countdown-элементов (событие + аукцион) */
  vsStartCdTicker();
})();

/* ===== module: i18n-settings ===== */
/* ================= I18N-SETTINGS: язык интерфейса RU/EN + авто-перевод чатов =================
   Префикс st-. Опирается на i18n-ядро core-ext: LANG, regT, t, onLangChange, setLang, regTitle. */

/* ---------- состояние модуля ---------- */
const ST_STATE = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-i18n-settings'))||{}; }catch(e){ return {}; } })();
if(typeof ST_STATE.auto !== 'boolean') ST_STATE.auto = false;
function stSave(){ try{ localStorage.setItem('oko-i18n-settings', JSON.stringify(ST_STATE)); }catch(e){} }

/* ---------- словарь хрома (регистрация в i18n-ядре) ---------- */
regT({
  /* вкладки нижней навигации */
  'st.nav.feed':    {ru:'Лента',        en:'Feed',        kk:'Лента'},
  'st.nav.chats':   {ru:'Чаты',         en:'Chats',       kk:'Чаттар'},
  'st.nav.mini':    {ru:'Мини-аппы',    en:'Hub',         kk:'Хаб'},
  'st.nav.wallet':  {ru:'Кошелёк',      en:'Wallet',      kk:'Әмиян'},
  'st.nav.profile': {ru:'Профиль',      en:'Profile',     kk:'Профиль'},
  /* заголовки экранов (TITLES) */
  'st.title.feed':    {ru:'Лента',       en:'Feed'},
  'st.title.chats':   {ru:'Чаты',        en:'Chats'},
  'st.title.mini':    {ru:'Мини-аппы',   en:'Hub'},
  'st.title.partner': {ru:'Партнёрка',   en:'Partner Hub'},
  'st.title.profile': {ru:'Профиль',     en:'Profile'},
  'st.title.wallet':  {ru:'Кошелёк',     en:'Wallet'},
  'st.title.games':   {ru:'Игры',        en:'Games'},
  'st.title.academy': {ru:'Академия',    en:'Academy'},
  'st.title.ads':     {ru:'Реклама',     en:'Ads'},
  /* статичные заголовки и кнопки */
  'st.h.services':    {ru:'Сервисы OKO',           en:'OKO Services'},
  'st.h.servicesSub': {ru:'Мессенджер, лента, биржа и инструменты для роста', en:'Messenger, feed, marketplace & growth tools'},
  'st.h.partner':     {ru:'Партнёрский кабинет',   en:'Partner Hub'},
  'st.h.plans':       {ru:'Тарифы',                en:'Plans'},
  'st.w.account':     {ru:'Лицевой счёт',          en:'Account'},
  'st.w.topup':       {ru:'Пополнить',             en:'Top up'},
  'st.w.withdraw':    {ru:'Вывести',               en:'Withdraw'},
  'st.w.history':     {ru:'История',               en:'History'},
  'st.w.spending':    {ru:'Расходы по категориям', en:'Spending by category'},
  'st.w.ops':         {ru:'История операций',      en:'Transaction history'},
  'st.w.fAll':        {ru:'Все',                   en:'All'},
  'st.w.fIn':         {ru:'Пополнения',            en:'Deposits'},
  'st.w.fOut':        {ru:'Списания',              en:'Charges'},
  /* строки профиля */
  'st.p.theme':       {ru:'Светлая тема',          en:'Light theme'},
  'st.p.profile':     {ru:'Профиль',               en:'Profile'},
  'st.p.notif':       {ru:'Уведомления',           en:'Notifications'},
  'st.p.privacy':     {ru:'Конфиденциальность',    en:'Privacy'},
  'st.p.progress':    {ru:'Прогресс сборки',       en:'Build progress'},
  'st.p.admin':       {ru:'Админка OKO',           en:'OKO Admin'},
  'st.p.logout':      {ru:'Выйти',                 en:'Log out'},
  /* плейсхолдеры поиска */
  'st.ph.gsearch':    {ru:'Люди, каналы, услуги, посты', en:'People, channels, services, posts'},
  'st.ph.search':     {ru:'Поиск',                 en:'Search'},
  /* собственные строки модуля */
  'st.row.lang':      {ru:'Язык · Language',       en:'Language'},
  'st.row.auto':      {ru:'Авто-перевод чатов',    en:'Auto-translate chats'},
  'st.sheet.title':   {ru:'Язык · Language',       en:'Language'},
  'st.sheet.note':    {ru:'Интерфейс переключается мгновенно. Контент пользователей остаётся на языке оригинала — включи авто-перевод чатов в профиле.',
                       en:'The interface switches instantly. User content stays in its original language — enable chat auto-translation in your profile.'},
  'st.opt.ruSub':     {ru:'Основной язык OKO',     en:'Default OKO language',   kk:'OKO негізгі тілі'},
  'st.opt.enSub':     {ru:'Интерфейс на английском', en:'English interface',    kk:'Ағылшын интерфейс'},
  'st.opt.kkSub':     {ru:'Қазақша интерфейс',     en:'Kazakh interface',       kk:'Қазақ тіліндегі интерфейс'},
  'st.badge':         {ru:'Переведено · OKO',   en:'Translated · OKO'},
  'st.toast.autoOn':  {ru:'Авто-перевод включён — входящие переводит OKO', en:'Auto-translation on — incoming messages translated by OKO'},
  'st.toast.autoOff': {ru:'Авто-перевод выключен', en:'Auto-translation off'},
  'st.doc.title':     {ru:'OKO — приложение',        en:'OKO — the app'}
});

/* ключи TITLES, которые модуль умеет переводить (обновляются только если экран существует) */
const ST_TITLE_KEYS = ['feed','chats','mini','partner','profile','wallet','games','academy','ads'];

/* статичные элементы хрома: селектор -> ключ словаря (переводим только реально существующие) */
const ST_STATIC = [
  {sel:'#maHero h2',                                   key:'st.h.services'},
  {sel:'#maHero p',                                    key:'st.h.servicesSub'},
  {sel:'.svc-h',                                       key:'st.h.services'},
  {sel:'#screen-partner .section-h',                   key:'st.h.partner'},
  {sel:'#screen-profile .section-h',                   key:'st.h.plans'},
  {sel:'#screen-wallet .wal-acc-label',                key:'st.w.account'},
  {sel:'#screen-wallet .wal-act[onclick^="walOpenTopup"]',    key:'st.w.topup'},
  {sel:'#screen-wallet .wal-act[onclick^="walOpenWithdraw"]', key:'st.w.withdraw'},
  {sel:'#screen-wallet .wal-act[onclick^="walScrollHistory"]',key:'st.w.history'},
  {sel:'#screen-wallet .section-h:not(#walHistAnchor)',key:'st.w.spending'},
  {sel:'#walHistAnchor',                               key:'st.w.ops'},
  {sel:'#walFilters [data-f="all"]',                   key:'st.w.fAll'},
  {sel:'#walFilters [data-f="in"]',                    key:'st.w.fIn'},
  {sel:'#walFilters [data-f="out"]',                   key:'st.w.fOut'},
  {sel:'#screen-profile .prow[onclick="toggleTheme()"]',        key:'st.p.theme'},
  {sel:'#screen-profile .prow[onclick="openEdit()"]',           key:'st.p.profile'},
  {sel:"#screen-profile .prow[onclick=\"openSettings('notif')\"]",   key:'st.p.notif'},
  {sel:"#screen-profile .prow[onclick=\"openSettings('privacy')\"]", key:'st.p.privacy'},
  {sel:"#screen-profile .prow[onclick=\"openSheet('progress')\"]",   key:'st.p.progress'},
  {sel:'#prowAdmin',                                   key:'st.p.admin'},
  {sel:'#screen-profile .prow[onclick="doLogout()"]',  key:'st.p.logout'}
];

/* заменить первый значимый текстовый узел элемента (не трогая svg/switch/chev внутри) */
function stSetText(el, txt){
  if(!el) return;
  for(const n of el.childNodes){
    if(n.nodeType === 3 && n.nodeValue.trim()){ n.nodeValue = ' ' + txt + ' '; return; }
  }
}

/* ---------- обработчик смены языка (регистрируется в onLangChange) ---------- */
function stApplyLang(l){
  /* 0. заголовок документа/вкладки браузера */
  try{ document.title = t('st.doc.title'); }catch(e){}
  /* 1. подписи вкладок нижней навигации (текстовый узел после svg) */
  document.querySelectorAll('#tabs>button[data-t]').forEach(b=>{
    const key = 'st.nav.' + b.dataset.t;
    if(I18N[key]) stSetText(b, t(key));
  });
  /* 2. TITLES всех известных экранов — только реально зарегистрированных */
  if(typeof TITLES === 'object'){
    ST_TITLE_KEYS.forEach(k=>{
      if(TITLES[k] !== undefined && I18N['st.title.'+k]) regTitle(k, t('st.title.'+k));
    });
    /* текущий заголовок шапки */
    const act = document.querySelector('main .screen.active');
    const head = document.getElementById('screenTitle');
    if(act && head){
      const k = act.id.replace('screen-','');
      if(TITLES[k]) head.textContent = TITLES[k];
    }
  }
  /* 3. статичные заголовки/кнопки — с проверкой существования */
  ST_STATIC.forEach(o=>{
    document.querySelectorAll(o.sel).forEach(el=>{
      if(el.children.length === 0) el.textContent = t(o.key);
      else stSetText(el, t(o.key));
    });
  });
  /* 4. плейсхолдеры поиска */
  const gs = document.getElementById('gSearchInput'); if(gs) gs.placeholder = t('st.ph.gsearch');
  const cs = document.getElementById('chatSearch');   if(cs) cs.placeholder = t('st.ph.search');
  /* 5. собственные элементы модуля */
  const lt = document.getElementById('stLangText'); if(lt) lt.textContent = t('st.row.lang');
  const lc = document.getElementById('stLangCur');  if(lc) lc.textContent = l==='en' ? 'English' : (l==='kk' ? 'Қазақша' : 'Русский');
  const ar = document.getElementById('stAutoRow');  if(ar) stSetText(ar, t('st.row.auto'));
  const sh = document.getElementById('stLangTitle'); if(sh) sh.textContent = t('st.sheet.title');
  const sn = document.getElementById('stLangNote');  if(sn) sn.textContent = t('st.sheet.note');
  stRenderLangList();
  /* 6. перерисовать открытый чат, чтобы бейджи перевода сменили язык */
  stRerenderChat();
  /* 7. словарный DOM-переводчик: EN — полный проход + observer; RU — откат */
  stSyncDomLang(l);
}

/* ---------- sheet выбора языка ---------- */
function stRenderLangList(){
  const box = document.getElementById('stLangList');
  if(!box) return;
  const L = (typeof LANG === 'string') ? LANG : 'ru';
  box.innerHTML = [
    ['ru','Русский', 'РУ',  'st.opt.ruSub'],
    ['en','English', 'EN',  'st.opt.enSub'],
    ['kk','Қазақша', 'ҚАЗ', 'st.opt.kkSub']
  ].map(([code,label,tag,sub])=>`
    <button class="sheet-item st-lang-opt ${L===code?'on':''}" onclick="stPickLang('${code}')">
      <span class="st-lang-tag">${tag}</span>
      <span>${label}<small>${t(sub)}</small></span>
      ${L===code ? `<span class="st-check">${I('check2')}</span>` : ''}
    </button>`).join('');
}
function stOpenLang(){ stRenderLangList(); openSheet('stLang'); }
function stPickLang(l){
  if(l === LANG){ closeSheet(); return; }
  setLang(l);                 // персист (oko-lang) + все onLangChange-хуки, включая stApplyLang
  stRenderLangList();         // галочка перескакивает с анимацией
  setTimeout(closeSheet, 260);
}

/* ---------- авто-перевод чатов ---------- */
function stToggleAuto(){
  ST_STATE.auto = !ST_STATE.auto;
  stSave();
  const sw = document.getElementById('stAutoSwitch');
  if(sw) sw.classList.toggle('on', ST_STATE.auto);
  toast(t(ST_STATE.auto ? 'st.toast.autoOn' : 'st.toast.autoOff'));
  stRerenderChat();
}
function stRerenderChat(){
  if(typeof renderMsgs !== 'function') return;
  if(typeof currentChat === 'undefined' || !currentChat) return;
  const box = document.getElementById('msgs');
  if(!box || !box.children.length) return;
  try{ renderMsgs(); }catch(e){}
}
/* DOM-проход: тонкий бейдж «Переведено · OKO AI» под входящими текстовыми сообщениями */
function stMarkTranslated(){
  if(!ST_STATE.auto) return;
  const box = document.getElementById('msgs');
  if(!box) return;
  box.querySelectorAll('.msg.in').forEach(m=>{
    if(m.dataset.stDone) return;
    if(m.classList.contains('sys') || m.classList.contains('sticker-msg') || m.classList.contains('has-vnote')) return;
    if(m.querySelector('.voice,.filemsg,.photomsg,.pollmsg,.moneymsg')) return; // только текст
    const b = document.createElement('div');
    b.className = 'st-trans';
    b.innerHTML = `${I('globe')}<span>${t('st.badge')}</span>`;
    m.appendChild(b);
    m.dataset.stDone = '1';
  });
}

/* ================= СЛОВАРНЫЙ DOM-ПЕРЕВОДЧИК (полный перевод интерфейса RU→EN) =================
   Точные видимые строки всех модулей. Ключ — trim + схлопнутые пробелы.
   Оригиналы хранятся в WeakMap (без загрязнения DOM), реестр узлов — в Set для отката. */
const ST_DICT = {
  /* --- общие кнопки и слова --- */
  'Назад':'Back','Далее':'Next','Готово':'Done','Отмена':'Cancel','Отлично':'Great','Понятно':'Got it',
  'Позже':'Later','Создать':'Create','Показать':'Show','Закрыть':'Close','Выбрать':'Choose','Сохранить':'Save',
  'Удалить':'Delete','Изменить':'Edit','Копировать':'Copy','Пропустить':'Skip','Отправить':'Send','Опубликовать':'Publish',
  'Продолжить':'Continue','Подтвердить':'Confirm','Войти':'Log in','Выйти':'Log out','Включить':'Enable','Установить':'Install',
  'Дальше':'Next','Ответить':'Reply','Прочитать':'Read','Сбросить':'Reset','Снять':'Unpublish','Запустить':'Launch',
  'Остановить':'Stop','Одобрить':'Approve','Отклонить':'Reject','Подробнее':'Learn more','Купить':'Buy','Подписаться':'Follow',
  'сегодня':'today','вчера':'yesterday','Сегодня':'Today','Вчера':'Yesterday','сейчас':'just now','скоро':'soon',
  'Все':'All','Вкл':'On','Поиск':'Search','Название':'Title','Категория':'Category','Описание':'Description',
  'Цена':'Price','Тип':'Type','от':'from','до':'to','и':'and','Имя':'Name','Пароль':'Password','Почта':'Email',
  'Телефон':'Phone','Никнейм':'Username','никнейм':'username','Статус':'Status','Тариф':'Plan','Тарифы':'Plans',
  'Вопрос':'Question','Опрос':'Poll','Файл':'File','Камера':'Camera','Тема':'Theme','изм.':'edited','24ч':'24h',
  '7д':'7d','30д':'30d','90д':'90d','владелец':'owner','Эскалация':'Escalation','Безопасность':'Security',
  /* --- нижняя навигация / заголовки экранов / хаб --- */
  'Лента':'Feed','Чаты':'Chats','Мини-аппы':'Hub','Кошелёк':'Wallet','Профиль':'Profile','Партнёрка':'Partner Hub',
  'Игры':'Games','Академия':'Academy','Реклама':'Ads','Биржа':'Marketplace','Биржа OKO':'OKO Marketplace',
  'Сервисы OKO':'OKO Services','Мессенджер, лента, биржа и инструменты для роста':'Messenger, feed, marketplace & growth tools',
  'Нажми на сервис — откроется внутри OKO':'Tap a service — it opens inside OKO','Биржа услуг':'Marketplace',
  'Контент-завод':'Content Factory','Система роста':'Growth System','Проверка видео':'Video Check','Поддержка':'Support',
  'Поддержка OKO':'OKO Support','Система роста по анкете':'Growth system from your brief',
  'Автопостинг в 3 соцсети':'Auto-posting to 3 networks','Кабинет продавца':'Seller Hub','Документы OKO':'OKO Documents',
  'Верификация':'Verification','Стикеры OKO':'OKO Stickers','Рекламный кабинет':'Ads Manager',
  /* --- экран входа / онбординг --- */
  'Медийность, контент, маркетинг и бизнес — в одном приложении':'Media, content, marketing and business — in one app',
  'Продолжить в Telegram':'Continue with Telegram','Телефон или почта':'Phone or email',
  'Продолжая, ты принимаешь условия сервиса и политику конфиденциальности':'By continuing you accept the Terms of Service and Privacy Policy',
  /* --- регистрация --- */
  'Регистрация':'Sign up','Шаг':'Step','из 3':'of 3','Создай аккаунт':'Create your account',
  'Почта или телефон и пароль — этого достаточно для старта в OKO.':'Email or phone plus a password — that is all you need to start with OKO.',
  'Электронная почта':'Email address','Номер телефона':'Phone number','Минимум 6 символов':'At least 6 characters',
  'Показать пароль':'Show password','Надёжность пароля':'Password strength','Слабый пароль':'Weak password',
  'Нормальный пароль':'Okay password','Хороший пароль':'Good password','Отличный пароль':'Strong password',
  'Данные шифруются и не передаются третьим лицам':'Your data is encrypted and never shared with third parties',
  'Код подтверждения':'Verification code','Мы отправили 4-значный код':'We sent a 4-digit code',
  'Отправить код повторно':'Resend code','Твой профиль':'Your profile',
  'Как тебя показывать в OKO и что тебе интересно.':'How to show you in OKO and what you are into.',
  'Как к тебе обращаться':'What should we call you','Дата рождения':'Date of birth','Интересы':'Interests',
  'Выбери хотя бы один — лента и рекомендации подстроятся под тебя.':'Pick at least one — your feed and recommendations will adapt.',
  'Принимаю':'I accept','условия сервиса':'terms of service','политику конфиденциальности':'privacy policy',
  'Создать аккаунт':'Create account','Проверяю никнейм…':'Checking username…',
  'Слишком короткий — минимум 3 символа':'Too short — at least 3 characters','уже занят, попробуй другой':'is taken, try another',
  'свободен':'is available','Укажи имя — так тебя увидят в OKO':'Enter a name — this is how people will see you in OKO',
  'Нужен свободный никнейм':'You need an available username',
  'Выбери хотя бы один интерес — без него лента будет пустой':'Pick at least one interest — otherwise your feed will be empty',
  'Прими условия сервиса, чтобы продолжить':'Accept the terms of service to continue',
  'Добро пожаловать в OKO':'Welcome to OKO','Забрать бонус':'Claim bonus',
  'Бонус на лицевом счёте — кошелёк уже в сборке':'Bonus credited to your account — the wallet is on its way',
  'Открой OKO PRO':'Unlock OKO PRO','Оформить PRO':'Get PRO','Тарифы скоро откроются':'Plans are coming soon',
  'Включи уведомления':'Turn on notifications','Уведомления включены':'Notifications enabled',
  'Похоже, в почте опечатка — проверь адрес':'Looks like a typo in the email — check the address',
  'Введи номер полностью — 11 цифр с +7':'Enter the full number — 11 digits starting with +7',
  'Пароль слишком короткий — минимум 6 символов':'Password is too short — at least 6 characters',
  'Введи все 4 цифры кода':'Enter all 4 digits of the code',
  'Код не подходит — проверь и попробуй ещё раз':'Wrong code — check and try again','Код подтверждён':'Code confirmed',
  'В приложение':'Open the app','Проверяю доступ…':'Checking access…','Доступ владельца':'Owner access',
  'Неверный пароль владельца':'Wrong owner password','Нейросети':'AI','Бизнес':'Business','Маркетинг':'Marketing',
  'Крипта':'Crypto','Контент':'Content','нейросети':'AI','бизнес':'business','маркетинг':'marketing',
  'крипта':'crypto','контент':'content','игры':'gaming','Условия сервиса откроются в разделе «Документы»':'The Terms of Service open in the Documents section',
  'Политика конфиденциальности откроется в разделе «Документы»':'The Privacy Policy opens in the Documents section',
  /* --- чаты --- */
  'Новый чат':'New chat','Выбери чат или создай новый':'Pick a chat or start a new one','онлайн':'online',
  'Онлайн':'Online','был(а) недавно':'last seen recently','Сообщение':'Message','Аудиозвонок':'Voice call',
  'Видеозвонок':'Video call','Собеседник':'Contact','Соединение…':'Connecting…','Микрофон':'Mic','Динамик':'Speaker',
  'Завершить':'End','Личный чат':'Direct chat','Диалог один на один':'One-on-one conversation','Группа':'Group',
  'До 200 000 участников':'Up to 200,000 members','Канал':'Channel','Посты в ленту подписчиков':'Posts to your subscribers',
  'Сообщество':'Community','Канал + чаты + мини-аппы':'Channel + chats + mini-apps','Прикрепить':'Attach',
  'Фото с устройства':'Photo from device','Free до 100 МБ · Start/Pro до 2 ГБ':'Free up to 100 MB · Start/Pro up to 2 GB',
  'Голосование в чате':'Chat poll','Перевести деньги':'Send money','Мгновенно, без комиссии':'Instant, no fee',
  'Фирменный набор':'Brand pack','Геопозиция':'Location','Отправить точку на карте':'Send a point on the map',
  'Выбрать изображение и отправить':'Pick an image and send','Новый опрос':'New poll','Вариант 1':'Option 1',
  'Вариант 2':'Option 2','Вариант 3 (необязательно)':'Option 3 (optional)','Создать опрос':'Create poll',
  'Перевод':'Transfer','Доставлен':'Delivered','Сумма, ₽':'Amount, ₽',
  'Вывод на карту через кошелёк OKO':'Mock: in production — OKO wallet, card payout',
  'Опрос · выбери вариант':'Poll · pick an option','Ничего не нашлось':'Nothing found',
  'Попробуй другой запрос или фильтр':'Try another query or filter','Попробуй другой запрос':'Try another query',
  'Здесь пока пусто':'Nothing here yet','Создай чат кнопкой + внизу':'Start one with the + button below',
  'живой чат':'live chat','Пишут все участники':'Everyone can post','Быстрый доступ':'Quick access',
  /* --- лента / истории --- */
  'Подписки':'Following','Рекомендации':'For you','В тренде':'Trending',
  'Ранжирует алгоритм OKO: вовлечённость и свежесть — как в Instagram, в отличие от Telegram':'Ranked by the OKO algorithm: engagement and recency — like Instagram, unlike Telegram',
  'Новый пост':'New post','Что нового? Поделись с подписчиками…':'What is new? Share it with your subscribers…',
  'Комментарии':'Comments','Написать комментарий…':'Write a comment…',
  'Пока нет комментариев — будь первым':'No comments yet — be the first','Продвинуть пост':'Promote post',
  'Не интересно':'Not interested','Пожаловаться':'Report','Продвижение поста':'Post promotion',
  'Алгоритм OKO покажет пост тем, кому он интересен':'The OKO algorithm will show the post to people who care',
  'Показ в рекомендациях целевой аудитории':'Shown in recommendations to the target audience',
  'В топе рекомендаций + похожие интересы':'Top of recommendations + similar interests',
  'Максимальный охват + продвижение канала':'Maximum reach + channel promotion',
  'Пост продвигается — охват растёт':'Post is being promoted — reach is growing','Пост удалён':'Post deleted',
  'Больше не покажем такое':'We will not show this again','Жалоба отправлена на модерацию':'Report sent to moderation',
  'Репост в твою ленту':'Reposted to your feed','Сохранено в закладки':'Saved to bookmarks',
  'Убрано из закладок':'Removed from bookmarks','Новая история':'New story','История на 24 часа':'A story for 24 hours',
  'Текст истории (необязательно)':'Story text (optional)','Опубликовать текст':'Publish text','Пока тихо':'Quiet for now',
  'Тут появятся лайки, заявки и начисления':'Likes, requests and payouts will appear here',
  /* --- канал / сообщество --- */
  'подписчиков':'followers','охват/нед':'reach/wk','новых/нед':'new/wk','вовлечённость':'engagement',
  'Посты в ленте':'Posts in feed','Алгоритмы OKO':'OKO algorithms','участников':'members','участники':'members',
  'сообщений':'messages','антиспам':'anti-spam','Написать':'Message','Продвижение канала':'Channel promotion',
  'Продвинуть канал':'Promote channel','Показываем целевой аудитории по нише канала':'Shown to a target audience in your channel niche',
  /* --- каналы: создание, доступ, управление (channels) --- */
  'Создать канал':'Create channel','Удалить канал':'Delete channel','Действие необратимо':'This action is irreversible',
  'Канал, клуб или видео-курс · открытый/закрытый, платный/бесплатный':'Channel, club or video course · public/private, paid/free',
  'Тип, доступ и цена':'Type, access and price','Тип и доступ':'Type and access','Видео-курс':'Video course',
  'Открытый':'Public','Виден и читается всем':'Visible and readable to everyone',
  'Закрытый':'Private','Витрина + доступ по заявке/оплате':'Showcase + access by request/payment',
  'Бесплатно':'Free','Без платы за доступ':'No access fee','Платно':'Paid','Доступ по подписке/оплате':'Access by subscription/payment',
  'Прогресс курса':'Course progress','Публичная ссылка-приглашение':'Public invite link','Загрузить фото поста':'Upload post photo',
  'Написать автору':'Message the author','Постов пока нет':'No posts yet','Обсудить':'Discuss','Нажми, чтобы проголосовать':'Tap to vote',
  'Бесплатное вступление — доступ откроется сразу':'Free to join — access opens right away','· по заявке':'· by request',
  'Оформи доступ, чтобы видеть все материалы':'Get access to see all materials',
  'аватар · обложка · фон':'avatar · cover · background','Обсуждения':'Discussions','Реакции':'Reactions','Статистика':'Statistics',
  'включены':'on','выключены':'off','графики':'charts','всего':'total','доступ':'access','оплата':'payment',
  'за неделю':'per week','прирост/нед':'growth/wk','подписок':'subscriptions','пик: сб':'peak: Sat','опрос':'poll','видео':'video',
  /* --- каналы: тосты и уведомления --- */
  'Аватар обновлён':'Avatar updated','Обложка обновлена':'Cover updated','Администратор разжалован':'Admin demoted',
  'Введите название урока':'Enter a lesson title','Введите название':'Enter a title','Урок добавлен':'Lesson added',
  'Видео-плеер урока — подключается к Академии OKO':'Lesson video player — connects to OKO Academy',
  'Вы вступили в канал':'You joined the channel','Выберите изображение':'Choose an image','Добавьте текст или фото':'Add text or a photo',
  'Доступ открыт':'Access granted','Доступ отменён':'Access revoked','Задай цену в «Тип и доступ»':'Set a price in "Type and access"',
  'Канал удалён':'Channel deleted','Кошелёк недоступен':'Wallet unavailable','Курс всегда закрытый':'A course is always private',
  'Курс всегда платный':'A course is always paid','Курс всегда закрытый — уроки открываются после покупки.':'A course is always private — lessons open after purchase.',
  'Мессенджер недоступен':'Messenger unavailable','Не удалось прочитать изображение':'Could not read the image',
  'Нет участников для назначения':'No members to assign','Открываю чаты':'Opening chats','Ошибка чтения файла':'File read error',
  'Обсуждения включены — комментарии подключатся с бэкендом':'Discussions enabled — comments will connect with the backend',
  'Пока нечего выводить — продаж не было':'Nothing to withdraw yet — no sales',
  'Пост опубликован — он уже в ленте рекомендаций':'Post published — it is already in the recommendations feed',
  'Урок пройден — прогресс обновлён':'Lesson completed — progress updated',
  /* --- проверка видео --- */
  'Видео-премодератор':'Video pre-moderator',
  'Ролик до 10 ГБ. Исходник автоудаляется через 24 часа после анализа.':'Video up to 10 GB. The source auto-deletes 24 hours after analysis.',
  'Загрузить ролик':'Upload video','Загрузка…':'Uploading…',
  'Наши монтажёры разбирают хук, динамику, риски и технические параметры':'Our editors review the hook, pacing, risks and technical parameters',
  'готовность':'readiness','Вероятность рекомендаций':'Recommendation probability','Метрики':'Metrics',
  'Проверка рисков':'Risk check','Рекомендации команды':'Team recommendations','Исправить одним кликом':'Fix in one click',
  'Опубликовать во все сети':'Publish to all networks','Проверить другой ролик':'Check another video',
  'Хороший ролик. Исправь 2 риска ниже — и готовность вырастет до ~90%.':'Good video. Fix the 2 risks below and readiness grows to ~90%.',
  /* --- система роста --- */
  'Моя система роста':'My growth system','Наши аналитики собирают твою систему…':'Our analysts are building your system…',
  'Анализ ниши, целей и ресурсов по мастер-методике OKO':'Analyzing your niche, goals and resources with the OKO master method',
  'персональная система роста':'personal growth system','Профиль:':'Profile:','Твой план на 30 дней':'Your 30-day plan',
  'Открыть полную систему':'Open full system','Позиционирование':'Positioning','Контент-стратегия':'Content strategy',
  'Формат-ядро':'Core format','Ритм':'Cadence','Площадка':'Platform','Автопостинг':'Auto-posting',
  'План на 30 дней':'30-day plan','Цель по метрикам':'Metric goals','Что дальше':'What is next',
  'Сохранить систему':'Save system','Скачать PDF':'Download PDF','Ниша / тема канала':'Niche / channel topic',
  'Например: маркетинг для салонов красоты':'E.g.: marketing for beauty salons','Куда публиковать':'Where to publish',
  'Темп':'Pace','Пройти заново':'Start over',
  /* --- контент-завод --- */
  'Запустить завод':'Launch the factory','Наши сценаристы собирают контент-план…':'Our scriptwriters are building your content plan…',
  'В эфире':'Live','в производстве':'in production','готово':'done','опубликовано':'published',
  'Конвейер производства':'Production pipeline','Новый канал':'New channel','Контент-завод: 30 роликов в месяц':'Content factory: 30 videos a month',
  /* --- партнёрка --- */
  'Партнёрский кабинет':'Partner hub','регистрации':'sign-ups','оплаты':'payments','к выплате':'payable',
  'Доход за период':'Earnings for period','Твоя реф-ссылка':'Your referral link',
  '15% с оплат твоих клиентов (первые 6 мес, далее 5%) + 5% со второго уровня':'15% of your clients payments (first 6 mo, then 5%) + 5% from the second level',
  'Последние начисления':'Recent accruals','Доступно к выводу':'Available to withdraw','мин. $20':'min $20',
  'История выплат':'Payout history','Заявка на выплату':'Payout request','Июнь · на карту':'June · to card',
  'Май · USDT':'May · USDT','Заявка принята':'Request accepted','PRO · год · уровень 1':'PRO · year · level 1',
  'START · мес · уровень 1':'START · month · level 1','BUSINESS · 6м · уровень 2':'BUSINESS · 6 mo · level 2',
  /* --- тарифы --- */
  '3 мес −10%':'3 mo −10%','6 мес −15%':'6 mo −15%','Год −20%':'Year −20%',
  '/мес при оплате за год · ~1190₽':'/mo billed yearly · ~1190₽','/мес при оплате за год · ~3890₽':'/mo billed yearly · ~3890₽',
  '/мес при оплате за год · ~15900₽':'/mo billed yearly · ~15900₽','Все чаты и сообщества':'All chats & communities',
  '10 проверок видео/мес':'10 video checks/mo','30 проверок видео/мес':'30 video checks/mo',
  'Мессенджер Premium (файлы до 4 ГБ, транскрипция голосовых)':'Premium messenger (files up to 4 GB, voice transcription)','Активация партнёрки':'Partner program activation','Флагман':'Flagship',
  'CRM-лайт':'CRM lite','Полная Система + сайт по клику':'Full System + one-click website',
  'Контент-завод: 30 роликов/мес':'Content factory: 30 videos/mo','Автопостинг во все сети':'Auto-posting to all networks',
  'Приоритетная поддержка':'Priority support','К оплате':'Total','Способ оплаты':'Payment method',
  'Безопасная оплата. Автопродление можно отключить в любой момент.':'Secure payment. Auto-renewal can be turned off anytime.',
  'Проводим оплату…':'Processing payment…','Активация мгновенно после оплаты':'Activated instantly after payment',
  'Лимиты обновлены, партнёрка включена. Спасибо!':'Limits updated, partner program enabled. Thanks!',
  /* --- кошелёк --- */
  'Динамика · 30 дней':'Trend · 30 days','Подписка PRO — автопродление':'PRO subscription — auto-renewal',
  'Выключено — тариф не продлевается сам':'Off — the plan does not renew itself','Выключено':'Off',
  'Лицевой счёт':'Account','Пополнить':'Top up','Вывести':'Withdraw','История':'History',
  'Расходы по категориям':'Spending by category','История операций':'Transaction history','Пополнения':'Deposits',
  'Списания':'Charges','Карта РФ':'Russian card','Крипта USDT':'USDT crypto','Переводы':'Transfers','Прочее':'Other',
  'Продвижение':'Promotion','Комиссия вывода':'Withdrawal fee','Комиссия Биржи 10%':'Marketplace fee 10%',
  'В холде (эскроу):':'On hold (escrow):','Эскроу-защита:':'Escrow protection:',
  'в холде до выполнения заказа':'held until the order is completed','Расходов пока нет — всё в плюсе':'No spending yet — all in the black',
  'Пополнений пока нет':'No deposits yet','Списаний пока нет':'No charges yet','Операций пока нет':'No transactions yet',
  'Номер счёта скопирован:':'Account number copied:','Пополнение счёта':'Top up account','Способ пополнения':'Top-up method',
  'Без комиссии. Платежи проходят через шлюз OKO.':'No fee. Mock: payment gateway in production.',
  'Укажи сумму пополнения':'Enter a top-up amount','Создаём платёж…':'Creating payment…','Вывод средств':'Withdraw funds',
  'Доступно:':'Available:','Комиссия вывода 2%. Поступление 1–3 рабочих дня.':'2% withdrawal fee. Arrives in 1–3 business days.',
  'Сумма вывода':'Withdrawal amount','Сумма вывода, ₽':'Withdrawal amount, ₽','Комиссия 2%':'2% fee','К получению':'You receive',
  'Укажи сумму вывода':'Enter a withdrawal amount','Оформляем заявку…':'Processing request…',
  'Заявка на вывод создана':'Withdrawal request created','Куда вывести':'Withdraw to',
  'Пополни кошелёк и вернись к оплате.':'Top up your wallet and come back to payment.','Назад к оплате':'Back to payment',
  'Приветственный бонус OKO':'OKO welcome bonus','Недостаточно средств — пополни кошелёк':'Insufficient funds — top up your wallet',
  'Пополнение · Карта РФ':'Deposit · Russian card','Тариф START · 1 мес':'START plan · 1 mo',
  'Игра «Рулетка» · ставка':'Roulette game · bet','Покупка на Бирже: Монтаж Reels под ключ':'Marketplace purchase: Turnkey Reels editing',
  'Продвижение объявления · Турбо':'Listing promotion · Turbo','Перевод в чате · Марк Волков':'Chat transfer · Mark Volkov',
  /* --- игры --- */
  'Игры OKO':'OKO Games','Рулетка OKO':'OKO Roulette','Дорога OKO':'OKO Road','Крутить колесо':'Spin the wheel',
  'Ставка, ₽':'Bet, ₽','История игр':'Game history','Дневной лимит ставок':'Daily bet limit',
  'Ставки — только с кошелька OKO':'Bets come from your OKO wallet only',
  '12 секторов · до ×10 · выплата сразу на кошелёк':'12 sectors · up to ×10 · instant wallet payout',
  'Забрать':'Cash out','Шанс шага':'Step odds','Сейчас':'Now','В кошелёк':'To wallet',
  'Минимальная ставка — 10 ₽':'Minimum bet is 10 ₽','Колесо ещё крутится':'The wheel is still spinning',
  'Ставка в рулетке':'Roulette bet','Выигрыш в рулетке':'Roulette win','Ставка: Дорога OKO':'Bet: OKO Road',
  'Выигрыш: Дорога OKO':'Win: OKO Road','Провал':'Bust','Ставка':'Bet','сектор':'sector','проигрыш':'loss',
  'Ставка сгорела — колесо ждёт реванша':'Bet lost — the wheel awaits a rematch',
  'Ещё нет сыгранных раундов — сделай первую ставку':'No rounds played yet — place your first bet',
  'Ответственная игра.':'Responsible gaming.',
  'Игры OKO доступны только пользователям старше 18 лет. Дневной лимит ставок — 10 000 ₽, сбрасывается в полночь. Это развлечение, а не заработок: на дистанции преимущество всегда у казино. Играй в удовольствие и останавливайся вовремя.':'OKO Games are for users 18+. The daily bet limit is 10,000 ₽ and resets at midnight. This is entertainment, not income: over time the house always has the edge. Play for fun and stop in time.',
  /* --- академия --- */
  'Академия OKO':'OKO Academy','Уроки полного формата · официальные сертификаты':'Full-format lessons · official certificates',
  'Нейросети 2026':'Neural Networks 2026','5 уроков · тесты и практика · сертификат за каждый урок':'5 lessons · quizzes and practice · a certificate for every lesson',
  'Контент и Reels':'Content & Reels','Сценарии, монтаж и вирусные форматы':'Scripts, editing and viral formats',
  'Мои сертификаты':'My certificates',
  'Пройди направление целиком — получи официальный сертификат OKO с печатью и подписью. Он появится здесь.':'Complete a whole track to earn an official OKO certificate with seal and signature. It will appear here.',
  'Карта нейросетей 2026':'Neural network map 2026','Промпт-инжиниринг':'Prompt engineering',
  'Генератор картинок':'Image generator','Видео-инструменты':'Video tools','ОКО Ai':'OKO Ai',
  'формула промпта · few-shot · chain-of-thought':'prompt formula · few-shot · chain-of-thought',
  'агент под бизнес · RAG · Telegram':'business agent · RAG · Telegram','Слайды урока':'Lesson slides',
  'Тест по материалу':'Knowledge quiz','Практика':'Practice','Мини-игра':'Mini-game','мини-игра':'mini-game',
  'Прогресс урока':'Lesson progress','видео в производстве':'video in production','Видео просмотрено':'Video watched',
  'Отметить просмотренным':'Mark as watched','Видео урока в производстве':'Lesson video in production',
  'Сертификат получен':'Certificate earned','Сертификат ещё не выдан':'Certificate not issued yet',
  'Сертификат готов к выдаче':'Certificate ready to issue','Сертификат урока':'Lesson certificate',
  'Получить сертификат':'Get certificate','Скачать PNG':'Download PNG','Проверяется куратором OKO':'Checked by the OKO curator',
  'Сыграть ещё раз':'Play again','Сопоставь пары.':'Match the pairs.','тест зачтён':'quiz passed','Начать тест':'Start quiz',
  'Сдать работу':'Submit work','Раскрой подробнее — минимум 40 символов':'Add more detail — at least 40 characters',
  'Сначала выбери элемент слева':'Pick an item on the left first','Понятие':'Term','Пара':'Match',
  'Видео засчитано · +20% к уроку':'Video counted · +20% to the lesson','Слайды пройдены · +20% к уроку':'Slides completed · +20% to the lesson',
  'Тест сдан · +20% к уроку':'Quiz passed · +20% to the lesson','Практика зачтена · +20% к уроку':'Practice counted · +20% to the lesson',
  'Мини-игра пройдена · +20% к уроку':'Mini-game completed · +20% to the lesson',
  'Курс «Контент и Reels» уже в производстве':'The Content & Reels course is already in production',
  /* --- реклама --- */
  'метрики live':'live metrics','Создать кампанию':'Create campaign','Модерация командой OKO':'Moderation by OKO team',
  'Кампании':'Campaigns','Новая кампания':'New campaign','Заголовок':'Headline','Текст объявления':'Ad text',
  'Контент-завод за 7 дней':'Content factory in 7 days',
  'Что получит человек и почему стоит нажать кнопку':'What the person gets and why they should tap the button',
  'Кнопка (CTA)':'Button (CTA)','Ссылка':'Link','География':'Geography','Возраст':'Age','Прогноз охвата':'Reach forecast',
  'человек в выбранной аудитории':'people in the selected audience','Дневной бюджет, ₽':'Daily budget, ₽',
  'Показы':'Impressions','Клики (CTR 1–4%)':'Clicks (CTR 1–4%)','Списание с лицевого счёта':'Charged to your account',
  'Модерация':'In review','Активна':'Active','Пауза':'Paused','Отклонена':'Rejected','Завершена':'Completed',
  'показы':'impressions','клики':'clicks','потрачено':'spent','спонсировано':'sponsored','Весь мир':'Worldwide',
  'РФ':'Russia','СНГ':'CIS','широкая аудитория':'broad audience','все возрасты':'all ages',
  'Добавь заголовок объявления':'Add a headline','Добавь текст объявления':'Add ad text',
  'Минимальный бюджет — 100 ₽':'Minimum budget is 100 ₽','Кампания отправлена на модерацию OKO':'Campaign sent to OKO moderation',
  'Реклама отклонена':'Ad rejected','Объявление не активно':'Ad is not active','Клик засчитан':'Click counted',
  'Кампания на паузе — показы остановлены':'Campaign paused — impressions stopped','Бюджет уже исчерпан':'Budget already spent',
  'Кампания снова активна':'Campaign active again','Остановить кампанию?':'Stop the campaign?',
  'Кампания завершена':'Campaign completed','Кампания удалена':'Campaign deleted',
  'OKO Академия — поток №4':'OKO Academy — cohort #4','Партнёрка OKO — 15% с оборота':'OKO partner program — 15% of turnover',
  /* --- биржа / market-pro --- */
  'Поиск услуг и товаров':'Search services & goods','Категории':'Categories','Рекомендуем':'Recommended',
  'Фильтры':'Filters','Цена, ₽':'Price, ₽','Рейтинг продавца':'Seller rating','Мои объявления':'My listings',
  'У тебя пока нет объявлений':'You have no listings yet','ТОП':'TOP','На модерации':'In review','Продано':'Sold',
  'Активно':'Active','Продвижение объявления':'Listing promotion','Продвинуть объявление':'Promote listing',
  'Оплата разовая, списывается с баланса OKO':'One-time payment, charged to your OKO balance','Добавить фото':'Add photo',
  'Например: Монтаж Reels под ключ':'E.g.: Turnkey Reels editing','Например: от 500 ₽':'E.g.: from 500 ₽',
  'Что входит, сроки, условия':'What is included, timeline, terms','Разместить объявление':'Post a listing',
  'Проверка занимает пару минут. Против спама и запрещёнки.':'Review takes a couple of minutes. Against spam and prohibited goods.',
  'Заказать через OKO · безопасно':'Order via OKO · secure','Подтвердить получение':'Confirm delivery',
  'Открыть рабочий чат':'Open work chat','Продажа':'Sale','Покупка':'Purchase','В работе':'In progress',
  'Ожидает подтверждения':'Awaiting confirmation','Работа сдана — ждём подтверждения покупателя':'Work delivered — waiting for buyer confirmation',
  'Получение подтверждено — эскроу снят, деньги ушли исполнителю':'Delivery confirmed — escrow released, funds sent to the seller',
  'Баланс продаж пуст — заверши активные сделки':'Sales balance is empty — finish active deals',
  'Объявлений нет — размести первое на Бирже':'No listings yet — post your first on the Marketplace',
  'Выручка Биржи':'Marketplace revenue','Комиссия OKO 10%':'OKO fee 10%','К выводу':'To withdraw',
  'К выводу после комиссии':'To withdraw after fee','Выручка с завершённых сделок':'Revenue from completed deals',
  'просмотры':'views','контакты':'contacts','заказы':'orders','выручка ₽':'revenue ₽',
  'Пакеты услуг':'Service packages','СТАРТ':'START','БИЗНЕС':'BUSINESS','ФЛАГМАН':'FLAGSHIP',
  'Контент-завод на месяц':'Content factory for a month','30 роликов в месяц под ключ':'30 turnkey videos a month',
  'Что получаешь по неделям':'What you get week by week',
  'Оплата с кошелька OKO · команда и рабочий чат сразу, старт брифа в день оплаты':'Paid from your OKO wallet · team and work chat right away, brief starts the day you pay',
  'Индивидуальный':'Custom','Нестандартная задача или другой масштаб — соберём пакет под тебя.':'A custom task or different scale — we will build a package for you.',
  'Обсудим задачу':'Let us discuss','К оплате с кошелька':'Due from wallet','Баланс кошелька':'Wallet balance',
  'Вернуться к пакетам':'Back to packages',
  'После оплаты создадим рабочий чат: личный ассистент, бриф и ветки работ.':'After payment we create a work chat: personal assistant, brief and work threads.',
  'Личный ассистент OKO':'OKO personal assistant','Полный цикл роста под ключ: контент, дизайн, сайт, реклама':'Full growth cycle turnkey: content, design, website, ads',
  'Дизайн':'Design','Сайт':'Website','Отчёты':'Reports','сценарии, съёмка, монтаж, автопостинг':'scripts, shooting, editing, auto-posting',
  'обложки, оформление профиля, креативы':'covers, profile design, creatives','лендинг, домен, воронка продаж':'landing page, domain, sales funnel',
  'кампании, таргет, ретаргет, трафик':'campaigns, targeting, retargeting, traffic',
  'аналитика, метрики, еженедельные итоги':'analytics, metrics, weekly summaries',
  /* --- профиль / настройки --- */
  'Светлая тема':'Light theme','Уведомления':'Notifications','Конфиденциальность':'Privacy','Прогресс сборки':'Build progress',
  'Админка OKO':'OKO Admin','Редактирование':'Edit profile','Изменить фото':'Change photo','О себе':'About',
  'Пара слов о тебе':'A few words about you','Настройки сохраняются автоматически':'Settings save automatically',
  'Прогресс сборки OKO':'OKO build progress','Запуск — 15 июля 2026. Обновляется с каждым этапом.':'Launch — July 15, 2026. Updated at every stage.',
  /* --- верификация --- */
  'Не верифицирован':'Not verified','На рассмотрении':'Under review','Верифицирован':'Verified',
  'Синяя галочка подтверждает подлинность аккаунта для всех в OKO':'The blue check confirms account authenticity for everyone in OKO',
  'Модерация проверяет аккаунт и активность — обычно до 24 часов':'Moderation reviews the account and activity — usually within 24 hours',
  'Аккаунт подтверждён — галочка видна в профиле, ленте и чатах':'Account confirmed — the check shows in your profile, feed and chats',
  'Тариф PRO или BUSINESS':'PRO or BUSINESS plan','Активная подписка на момент подачи и проверки заявки':'An active subscription when applying and during review',
  'Живой аккаунт':'Active account','30+ дней в OKO, регулярные посты и реальная активность':'30+ days in OKO, regular posts and real activity',
  'Либо официальный бизнес':'Or an official business','Подтверждение юрлица или ИП документами — без требования к тарифу':'Company or sole-proprietor documents — no plan required',
  'Подать заявку':'Apply','Проверяем аккаунт…':'Reviewing account…','Условия':'Requirements',
  'Заявка отправлена на модерацию':'Application sent to moderation','Аккаунт верифицирован':'Account verified',
  'Заявка отклонена':'Application rejected','Верифицированный аккаунт':'Verified account',
  'Стикеры TON — в подписке PRO':'TON stickers — with PRO','необязательно':'optional','проверка':'review',
  /* --- PWA --- */
  'OKO на главный экран':'Add OKO to your home screen',
  'Установи OKO как приложение: значок на рабочем столе, полный экран, работает быстрее.':'Install OKO as an app: home screen icon, full screen, runs faster.',
  /* --- админка (владелец) --- */
  'Штаб HQ':'HQ','Доходы':'Revenue','Доход OKO — всего':'OKO revenue — total','за этот месяц':'this month',
  'По источникам':'By source','Сводка за сутки':'Daily summary','Быстрые действия':'Quick actions','Выплаты':'Payouts',
  'Логи агентов':'Agent logs','Последние пользователи':'Recent users','Экспорт CSV':'Export CSV',
  'Партнёры и выплаты':'Partners & payouts','Очередь модерации':'Moderation queue','Пусто — всё проверено.':'Empty — all reviewed.',
  'Логи агентов':'Agent logs','Фича-флаги':'Feature flags','Регистрации':'Sign-ups','Выручка':'Revenue',
  'Поддержка':'Support','Email владельца':'Owner email','Раздел владельца':'Owner area','Проверка…':'Checking…',
  'Владелец подтверждён':'Owner confirmed','Доступ запрещён':'Access denied',
  'Пока нет операций — доход копится из комиссий, тарифов и рекламы.':'No transactions yet — revenue accrues from fees, plans and ads.',
  /* --- хаб / категории биржи --- */
  'Документы':'Documents','Избранное':'Saved','Услуги':'Services','Видео и монтаж':'Video & editing',
  'Реклама и трафик':'Ads & traffic','Сайты и приложения':'Websites & apps','Тексты и переводы':'Copy & translation',
  'SMM и ведение':'SMM & management','Аудио и озвучка':'Audio & voiceover','3D и графика':'3D & graphics',
  'Консалтинг и обучение':'Consulting & training',
  /* --- профиль: статистика и бейджи --- */
  'постов':'posts','реакций':'reactions','дней в OKO':'days in OKO','Ранний':'Early','Партнёр':'Partner',
  '100+ реакций':'100+ reactions','Автор':'Author','нет':'no','есть':'yes','Штаб OKO HQ':'OKO HQ','Твоя':'You',
  'Открыть 3D-штаб':'Open the 3D HQ',
  /* --- игры / реклама: добивка --- */
  'Старт — выйти на дорогу':'Start — hit the road',
  'Каждое объявление проверяется автоматически за ~3 секунды: запрещённые тематики, скам, азартные игры. Отклонили — бюджет вернётся на лицевой счёт мгновенно.':'Every ad is checked automatically in ~3 seconds: prohibited topics, scams, gambling. If rejected, the budget returns to your account instantly.',
  'Контент-завод за 7 дней: 30 роликов в месяц на автомате. Старт 20 июля, осталось 14 мест из 50.':'Content factory in 7 days: 30 videos a month on autopilot. Starts July 20, only 14 of 50 seats left.',
  'Приводи авторов и получай 15% с их подписки + 5% со второй линии. Выплаты сразу на лицевой счёт.':'Refer creators and earn 15% of their subscription + 5% from the second line. Paid instantly to your account.',
  /* --- демо-объявления биржи (сид-контент приложения) --- */
  'Монтаж Reels и Shorts под ключ':'Turnkey Reels & Shorts editing','Сайт-визитка и лендинг за 3 дня':'Business card site & landing in 3 days',
  'Дизайн обложек и карточек товара':'Cover & product card design','3D-графика и анимация логотипа':'3D graphics & logo animation',
  'Монтаж Reels под ключ':'Turnkey Reels editing','Лендинг под ключ за 5 дней':'Turnkey landing page in 5 days',
  'Разбор и стратегия роста канала':'Channel audit & growth strategy',
  /* --- шелл / базовый хром (шапка, паволл-баннеры, партнёрка) --- */
  'OKO · экосистема медийности и бизнеса':'OKO · media & business ecosystem',
  'Контент-завод, система и команда OKO':'Content factory, system and the OKO team',
  'Партнёрка 15% + 5%':'Partner program 15% + 5%',
  'Приглашай — получай с каждой продажи':'Invite and earn on every sale',
  'Разблокируй всё — тариф MAX':'Unlock everything — MAX plan',
  /* --- рекламный кабинет (ads) --- */
  'A/B тест':'A/B test','· фото, картинка или видео':'· photo, image or video','Аудитории':'Audiences',
  'Биллинг':'Billing','Биллинг рекламы':'Ad billing','Бюджет кампании, ₽':'Campaign budget, ₽',
  'Вечер':'Evening','Видео':'Video','Время показа':'Time of day','Города':'Cities','День':'Day',
  'Женский':'Female','Картинка':'Image','Любой':'Any','Медиа креатива':'Creative media',
  'Модель оплаты':'Pricing model','Мужской':'Male','Ночь':'Night','Открыть':'Open','ПК':'Desktop',
  'Пол':'Gender','Прогноз кампании':'Campaign forecast','Сохранить аудиторию':'Save audience',
  'Устройства':'Devices','Утро':'Morning','Формат рекламы':'Ad format','Фото':'Photo',
  'Цена 1000 показов (CPM)':'Cost per 1000 impressions (CPM)','Цена клика (CPC)':'Cost per click (CPC)',
  'агрессивнее':'more aggressive',
  'второй вариант заголовка — OKO покажет оба и найдёт лучший по CTR':'a second headline variant — OKO shows both and picks the best by CTR',
  'дешевле':'cheaper','за 1000 показов':'per 1000 impressions','за клик':'per click',
  'образование':'education','рекомендуемая':'recommended','узкая':'narrow','финансы':'finance','широкая':'broad',
  /* --- кошелёк (лимиты, выписка, ПИН) --- */
  'Выключен — включи для защиты вывода':'Off — enable to protect withdrawals','Выписка':'Statement',
  'Выписка по счёту':'Account statement',
  'До 5 000 ₽ — мгновенно, выше — с подтверждением операции':'Up to 5,000 ₽ instantly, above that with confirmation',
  'Лимиты и безопасность':'Limits & security','ПИН-код на вывод':'Withdrawal PIN',
  'Покупки без подтверждения':'Purchases without confirmation','Скачать выписку (.txt)':'Download statement (.txt)',
  'Суточный лимит вывода':'Daily withdrawal limit',
  /* --- регистрация (баннеры шагов) --- */
  'Бесплатный старт':'Free start',
  'Тебе меньше 18 — разделы 18+ (казино-игры, часть биржи и рекламы) будут ограничены до совершеннолетия.':'You are under 18 — 18+ sections (casino games, part of the marketplace and ads) will be limited until you come of age.',
  /* --- профиль / соцграф --- */
  'Аккаунты':'Accounts','Добавить аккаунт':'Add account','Люди':'People','Подписчики':'Followers',
  /* --- игры (колесо призов) --- */
  'Выбери крутку и запусти колесо — приз попадёт в «Мои призы»':'Pick a spin and launch the wheel — the prize lands in "My prizes"',
  'Крути колесо — каждая крутка даёт приз':'Spin the wheel — every spin gives a prize',
  'Лидеры недели':'Weekly leaders','Мои призы':'My prizes','Честная механика.':'Fair mechanics.',
  'Приз выпадает случайно по взвешенным шансам, но каждая крутка гарантированно приносит приз — «сгораний» ставки нет. Средний приз крутки показан заранее. Крути в удовольствие.':'Prizes drop at random by weighted odds, but every spin is guaranteed to win — no burned bets. The average spin prize is shown up front. Spin for fun.',
  /* --- каналы / настройки / академия / сторис --- */
  'Мои каналы':'My channels','Настройки':'Settings','Поделиться':'Share',
  'Опубликовать историю':'Publish story','Просмотры':'Views',
  /* --- TON / подарки (verify-stickers) --- */
  'История TON':'TON history','Мой подарок':'My gift','Подарить другу':'Gift to a friend',
  'Подарок':'Gift','Пополнить TON':'Top up TON','Получить':'Receive',
  'Баланс':'Balance','Адрес получателя':'Recipient address','UQ… адрес TON-кошелька':'UQ… TON wallet address',
  'Вставить':'Paste','Купить ещё':'Buy more','В коллекции:':'In collection:','МАКС':'MAX',
  'Прототип: перевод списывает TON с локального кошелька и попадает в историю. Реальные переводы в сети TON подключатся в релизе через TON Connect.':'Prototype: the transfer deducts TON from your local wallet and lands in history. Real TON network transfers will connect at release via TON Connect.',
  'Честная заглушка: настоящие переводы TON и NFT-подарки подключатся в релизе через TON Connect. Баланс и коллекция хранятся локально на устройстве.':'Honest placeholder: real TON transfers and NFT gifts will connect at release via TON Connect. Balance and collection are stored locally on your device.',
  /* --- аудит-добивка v0.78: полный EN по всему хрому (найдено обходом DOM в EN) --- */
  /* экран звонка + редактор обложки (глобальные оверлеи) */
  'Звонок':'Call','Микро':'Mic','Сброс':'End','Обложка профиля':'Profile cover',
  'OKO · Официальный':'OKO · Official',
  'Аватар':'Avatar','ты':'you',
  /* чаты: сегменты фильтра + вложения + закреп */
  'Личные':'Direct','Группы':'Groups','Каналы':'Channels','Управление':'Manage',
  'Закреплённое сообщение':'Pinned message','Эмодзи':'Emoji','Стикеры':'Stickers',
  /* мини-аппы / хаб */
  'TON Подарки':'TON Gifts','Загрузить 4K-ролик (420 МБ)':'Upload 4K video (420 MB)',
  /* кошелёк: сводка + строки операций (сид-контент лицевого счёта) */
  'Чистыми за 30 дней':'Net over 30 days','прибыль':'profit','Расходы':'Expenses',
  'Партнёрские начисления':'Partner accruals','Пополнение':'Top-up','тариф MAX':'MAX plan',
  'Продажа на Бирже: обложка канала':'Marketplace sale: channel cover',
  'Стикеры OKO · набор «Неон»':'OKO Stickers · Neon pack','Партнёрская программа · выплата':'Partner program · payout',
  'Продвижение объявления · Старт':'Listing promotion · Start','Игра «Дорога» · ставка':'Road game · bet',
  'Пополнение · USDT':'Top-up · USDT','Возврат по спору · Биржа':'Dispute refund · Marketplace',
  'Перевод в чате · Аня Соколова':'Chat transfer · Anya Sokolova',
  /* реклама: паволл-баннер + карточки кампаний */
  'Тариф MAX · −30% на всю рекламу':'MAX plan · −30% on all ads',
  'Скидка уже применяется: за тот же бюджет — больше показов и кликов.':'The discount is already applied: more impressions and clicks for the same budget.',
  'Стоп':'Stop','Дублировать':'Duplicate','Реклама платного канала':'Paid channel ad',
  /* игры: колесо призов (секторы, промо, лидеры) */
  'Промо':'Promo','подарок дня':'gift of the day','призы крупнее':'bigger prizes',
  'Крутить бесплатно':'Spin for free','ещё нет — крути рулетку':'none yet — spin the wheel',
  'Призы за эту неделю':'Prizes this week',
  'Подарок дня — каждый сектор в плюс, деньги сразу на кошелёк. Средний приз ≈ 117 ₽':'Gift of the day — every sector wins, money straight to your wallet. Average prize ≈ 117 ₽',
  /* академия: серия, каталог, обложки курсов, достижения */
  'Курсы полного формата · официальные сертификаты':'Full-format courses · official certificates',
  'Заходи каждый день, чтобы разжечь серию':'Come back daily to build your streak',
  'Продолжить:':'Continue:','Каталог курсов':'Course catalog','Начать курс':'Start course',
  'Открыт':'Available','Достижения':'Achievements',
  'НЕЙРОСЕТИ':'NEURAL AI','АКАДЕМИЯ OKO':'OKO ACADEMY','КОНТЕНТ':'CONTENT','И REELS':'& REELS',
  '3 урока · вирусные видео от сценария до алгоритмов':'3 lessons · viral videos from script to algorithms',
  'Первый шаг':'First step','Пройти первый урок':'Complete your first lesson',
  'Сертификат':'Certificate','Получить первый сертификат':'Earn your first certificate',
  'Отличник':'Top student','Сдать тест на 100%':'Score 100% on a quiz',
  'Практик':'Practitioner','Зачесть 3 практики':'Complete 3 practices',
  'В огне':'On fire','3 дня подряд в учёбе':'3 days studying in a row',
  'Знаток ИИ':'AI expert','Завершить курс «Нейросети 2026»':'Finish the Neural Networks 2026 course',
  'Инвестор в себя':'Investor in yourself','получено':'earned',
  'Мастер Reels':'Reels master','Завершить курс «Контент и Reels»':'Finish the Content & Reels course',
  'Коллекционер':'Collector','Собрать 5 сертификатов':'Collect 5 certificates',
  /* профиль: обложка, карточка сертификатов, био основателя */
  'Добавить обложку':'Add cover','Сертификаты Академии':'Academy Certificates',
  'Пройди направление — получи именной документ':'Complete a track — get a personalized document',
  'В Академию':'Open Academy',
  'Основатель OKO. Медийность, контент, маркетинг — в одном приложении.':'OKO founder. Media, content, marketing — in one app.',
  'Пройди все уроки направления (порог теста 70%+) — и получи именной сертификат OKO с печатью и подписью руководителя Академии. Он появится здесь и в разделе «Академия».':'Complete all lessons in a track (70%+ quiz threshold) to earn a personalized OKO certificate with a seal and the Academy head’s signature. It appears here and in the Academy section.'
};

/* месяцы (родительный падеж) для дат операций/выплат */
const ST_MONTHS = {'января':'January','февраля':'February','марта':'March','апреля':'April',
  'мая':'May','июня':'June','июля':'July','августа':'August','сентября':'September',
  'октября':'October','ноября':'November','декабря':'December'};

/* --- шаблоны для строк с числами (применяются, если точного совпадения нет) --- */
const ST_RX = [
  [/^([\d\s.,]*\d) ?₽ из ([\d\s.,]*\d) ?₽$/, '$1 ₽ of $2 ₽'],
  [/^Урок (\d+) из (\d+)$/, 'Lesson $1 of $2'],
  [/^Урок (\d+) · (.+)$/, (m,a,b)=>'Lesson '+a+' · '+(ST_DICT[b]||b)],
  [/^Урок (\d+)$/, 'Lesson $1'],
  [/^(\d+) уроков?$/, '$1 lessons'],
  [/^Слайд (\d+) \/ (\d+)$/, 'Slide $1 / $2'],
  [/^(\d+) слайдов$/, '$1 slides'],
  [/^тест из (\d+) вопросов$/, 'quiz · $1 questions'],
  [/^Вопрос (\d+) из (\d+)$/, 'Question $1 of $2'],
  [/^Шаг (\d+) из (\d+)$/, 'Step $1 of $2'],
  [/^сборка v([\d.]+)$/, 'build v$1'],
  [/^([\d:]+) · видео \+ слайды \+ тест \+ игра$/, '$1 · video + slides + quiz + game'],
  [/^(\d+) участников$/, '$1 members'],
  [/^(\d+) подписчиков$/, '$1 followers'],
  [/^(\d+) сообщений$/, '$1 messages'],
  [/^(\d+) отзывов$/, '$1 reviews'],
  [/^Отзывы \((\d+)\)$/, 'Reviews ($1)'],
  [/^(\d+) голосов · анонимный$/, '$1 votes · anonymous'],
  [/^(\d+) мин назад$/, '$1 min ago'],
  [/^(\d+) (?:час|часа|часов) назад$/, '$1 h ago'],
  [/^(\d+) (?:день|дня|дней) назад$/, '$1 d ago'],
  [/^(\d+) мин$/, '$1 min'],
  [/^(\d+) сек$/, '$1 sec'],
  [/^(\d+) дней$/, '$1 days'],
  [/^1 день$/, '1 day'],
  [/^([\d\s]+) показов$/, '$1 impressions'],
  [/^от ([\d\s.,]+) ₽$/, 'from $1 ₽'],
  [/^от ([\d\s.,]+) ₽\/мес$/, 'from $1 ₽/mo'],
  [/^Оплатить ([\d\s.,]+ ?₽)$/, 'Pay $1'],
  [/^Заказать пакет · ([\d\s.,]+ ?₽)$/, 'Order package · $1'],
  [/^Пополнить на ([\d\s.,]+ ?₽)$/, 'Top up $1'],
  [/^Следующее списание (.+)$/, 'Next charge $1'],
  [/^Кошелёк пополнен на ([\d\s.,]+ ?₽)$/, 'Wallet topped up by $1'],
  [/^Тест сдан\. Лучший результат — (\d+)%$/, 'Quiz passed. Best score — $1%'],
  [/^Урок пройден на (\d+)%$/, 'Lesson completed at $1%'],
  [/^Сдать тест на (\d+)% и выше$/, 'Pass the quiz at $1%+'],
  [/^Собрано пар: (\d+) · ошибок: (\d+)$/, 'Pairs matched: $1 · mistakes: $2'],
  [/^Медиа (\d+)$/, 'Media $1'],
  [/^Файлы (\d+)$/, 'Files $1'],
  [/^Ссылки (\d+)$/, 'Links $1'],
  [/^PRO до ([\d.]+)$/, 'PRO until $1'],
  [/^Отправить код повторно \((\d+) с\)$/, 'Resend code ($1 s)'],
  [/^([\d.,]+)к$/, '$1k'],
  [/^забрал на ×([\d.,]+)$/, 'cashed out at ×$1'],
  [/^(\d+) объявл\.$/, '$1 listings'],
  [/^сегодня (\d{1,2}:\d{2})$/, 'today $1'],
  [/^вчера (\d{1,2}:\d{2})$/, 'yesterday $1'],
  [/^([\d.,]+)к₽$/, '$1k ₽'],
  [/^из ([\d.,]+)к₽$/, 'of $1k ₽'],
  [/^из ([\d\s.,]+ ?₽)$/, 'of $1'],
  [/^(\d+) агентов$/, '$1 agents'],
  [/^([\d.,]+) · Онлайн$/, '$1 · Online'],
  [/^([\d\s.,]*\d) ?₽ за сегодня$/, '$1 ₽ today'],
  [/^([\d\s.,]*\d) ?₽ бонус$/, '$1 ₽ bonus'],
  [/^(\d+) минут[аоуы]?$/, '$1 min'],
  [/^(\d+) объявлений$/, '$1 listings'],
  [/^(\d+) аккаунтов$/, '$1 accounts'],
  [/^(\d+) каналов$/, '$1 channels'],
  [/^(\d+) просмотров$/, '$1 views'],
  [/^(\d+) призов$/, '$1 prizes'],
  [/^Отправить ([\d\s.,]+) TON$/, 'Send $1 TON'],
  [/^Пополнить TON на ([\d\s.,]+ ?₽)$/, 'Top up TON by $1'],
  /* --- аудит-добивка v0.78: байлайны ленты, даты, счётчики игр/академии/рекламы --- */
  [/^канал · ([\d.,]+)к$/, 'channel · $1k'],
  [/^сообщество · ([\d.,]+)к$/, 'community · $1k'],
  [/^монтаж · ([\d.,]+)к$/, 'editing · $1k'],
  [/^бизнес · ([\d.,]+)к$/, 'business · $1k'],
  [/^моушен · ([\d.,]+)к$/, 'motion · $1k'],
  [/^· (\d+) ч$/, '· $1 h'],
  [/^· (\d+) д$/, '· $1 d'],
  [/^([\d\s.,]*\d) ?₽ из ([\d\s.,]*\d) ?₽ за сегодня ·$/, '$1 ₽ of $2 ₽ today ·'],
  [/^(\d+) (января|февраля|марта|апреля|мая|июня|июля|августа|сентября|октября|ноября|декабря)$/, (m,d,mo)=>d+' '+ST_MONTHS[mo]],
  [/^из ([\d.,]+)к$/, 'of $1k'],
  [/^([\d.,]+)к ₽$/, '$1k ₽'],
  [/^(\d+) бил$/, '$1 tkt'],
  [/^(\d+) пров$/, '$1 chk'],
  [/^сброс через (\d+) дней$/, 'resets in $1 days'],
  [/^(\d+) приз(?:а|ов)? за неделю$/, '$1 prizes this week'],
  [/^(\d+) урок(?:а|ов)?$/, '$1 lessons'],
  [/^Дней подряд в учёбе: (\d+)$/, 'Days in a row studying: $1'],
  [/^урок (\d+) — (.+)$/, (m,a,b)=>'lesson '+a+' — '+(ST_DICT[b]||b)],
  [/^Открыто (\d+) из (\d+) — учись и собирай награды$/, 'Unlocked $1 of $2 — study and collect rewards']
];

/* --- что НЕ переводим: контент пользователей + свои переключатели --- */
const ST_SKIP_TAGS = {SCRIPT:1, STYLE:1, NOSCRIPT:1, IFRAME:1, TEMPLATE:1, VIDEO:1, AUDIO:1, CANVAS:1};
const ST_SKIP_SEL = '.msg, .preview, #legalView, .post .body, .pm-title, #stLangList';
function stSkipEl(el){
  if(ST_SKIP_TAGS[(el.tagName||'').toUpperCase()]) return true;
  try{ if(el.matches && el.matches(ST_SKIP_SEL)) return true; }catch(e){}
  return false;
}

/* --- хранилища оригиналов: WeakMap (значения) + Set (реестр для отката) --- */
let stTxOrig = new WeakMap();      /* текстовый узел -> исходный nodeValue */
let stTxAttrOrig = new WeakMap();  /* элемент -> {attr: исходное значение} */
let stTxNodes = new Set();         /* переведённые текстовые узлы */
let stTxEls = new Set();           /* элементы с переведёнными атрибутами */
const ST_TX_ATTRS = ['placeholder','title','aria-label'];
const ST_CYR = /[А-Яа-яЁё]/;

/* перевод одной строки: точный словарь -> regex-шаблоны; null если перевода нет */
function stTx(s){
  const key = s.replace(/\s+/g,' ').trim();
  if(!key || !ST_CYR.test(key)) return null;
  const hit = ST_DICT[key];
  if(hit !== undefined) return hit;
  if(/\d/.test(key)){
    for(const [re, tpl] of ST_RX){
      if(re.test(key)) return key.replace(re, tpl);
    }
  }
  return null;
}

function stTxTextNode(n){
  const raw = n.nodeValue;
  const tr = stTx(raw);
  if(tr === null) return;
  if(!stTxOrig.has(n)) stTxOrig.set(n, raw);
  stTxNodes.add(n);
  const lead = (raw.match(/^\s*/)||[''])[0], tail = (raw.match(/\s*$/)||[''])[0];
  n.nodeValue = lead + tr + tail;
}

function stTxAttrsOf(el){
  if(!el.getAttribute) return;
  for(const a of ST_TX_ATTRS){
    const v = el.getAttribute(a);
    if(!v) continue;
    const tr = stTx(v);
    if(tr === null) continue;
    let store = stTxAttrOrig.get(el);
    if(!store){ store = {}; stTxAttrOrig.set(el, store); }
    if(!(a in store)) store[a] = v;
    stTxEls.add(el);
    el.setAttribute(a, tr);
  }
}

/* полный проход по DOM (включая скрытые экраны — переключение вкладок не мигает по-русски) */
function stTranslateDom(root){
  root = root || document.body;
  if(!root) return;
  if(root.nodeType === 3){ stTxTextNode(root); return; }
  if(root.nodeType !== 1 && root.nodeType !== 11) return;
  if(root.nodeType === 1){
    if(stSkipEl(root)) return;
    stTxAttrsOf(root);
  }
  const w = document.createTreeWalker(root, NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_TEXT, {
    acceptNode(n){
      if(n.nodeType === 1){
        if(stSkipEl(n)) return NodeFilter.FILTER_REJECT;
        stTxAttrsOf(n);           /* placeholder/title/aria-label по пути */
        return NodeFilter.FILTER_SKIP;
      }
      return NodeFilter.FILTER_ACCEPT;
    }
  });
  let n; while((n = w.nextNode())) stTxTextNode(n);
}

/* мгновенный и точный откат на русский */
function stRestoreAll(){
  stTxNodes.forEach(n=>{ const o = stTxOrig.get(n); if(o !== undefined) n.nodeValue = o; });
  stTxEls.forEach(el=>{
    const s = stTxAttrOrig.get(el);
    if(s) for(const a in s) el.setAttribute(a, s[a]);
  });
  stTxNodes = new Set(); stTxEls = new Set();
  stTxOrig = new WeakMap(); stTxAttrOrig = new WeakMap();
}

/* --- MutationObserver: только childList+subtree (БЕЗ characterData — не зациклится), debounce 120мс.
       Активен только при LANG==='en'; покрывает попапы/шиты/экраны, появившиеся после переключения. --- */
let stMo = null, stMoTimer = 0;
const stMoQueue = new Set();
function stMoFlush(){
  stMoTimer = 0;
  if(typeof LANG !== 'undefined' && LANG !== 'en'){ stMoQueue.clear(); return; }
  stMoQueue.forEach(n=>{ if(n.isConnected) stTranslateDom(n); });
  stMoQueue.clear();
}
function stObserverOn(){
  if(stMo) return;
  stMo = new MutationObserver(recs=>{
    for(const r of recs) for(const n of r.addedNodes) stMoQueue.add(n);
    if(stMoQueue.size && !stMoTimer) stMoTimer = setTimeout(stMoFlush, 120);
  });
  stMo.observe(document.body, {childList:true, subtree:true});
}
function stObserverOff(){
  if(stMo){ stMo.disconnect(); stMo = null; }
  if(stMoTimer){ clearTimeout(stMoTimer); stMoTimer = 0; }
  stMoQueue.clear();
}

/* связка с setLang: EN -> полный проход + observer; RU -> откат + observer выкл */
function stSyncDomLang(l){
  if(l === 'en'){ stTranslateDom(document.body); stObserverOn(); }
  else { stObserverOff(); stRestoreAll(); }
}

/* ---------- вставка строк в профиль ---------- */
function stInsertRows(){
  if(document.getElementById('stLangRow')) return;
  const admin = document.getElementById('prowAdmin');
  const card = admin ? admin.parentElement : document.querySelector('#screen-profile .card');
  if(!card) return;

  const langRow = document.createElement('button');
  langRow.className = 'prow'; langRow.id = 'stLangRow';
  langRow.onclick = stOpenLang;
  langRow.innerHTML = `${I('globe')} <span id="stLangText">${t('st.row.lang')}</span>` +
    `<span class="st-cur" id="stLangCur">${LANG==='en'?'English':'Русский'}</span>` +
    `<span class="chev">${I('chev')}</span>`;

  const autoRow = document.createElement('button');
  autoRow.className = 'prow'; autoRow.id = 'stAutoRow';
  autoRow.onclick = stToggleAuto;
  autoRow.innerHTML = `${I('st-translate')} ${t('st.row.auto')} ` +
    `<span class="switch ${ST_STATE.auto?'on':''}" id="stAutoSwitch"><i></i></span>`;

  if(admin){ card.insertBefore(langRow, admin); card.insertBefore(autoRow, admin); }
  else { card.appendChild(langRow); card.appendChild(autoRow); }
}

/* ---------- своя SVG-иконка «перевод» (штрих как у ядра: stroke 7, округлые концы) ---------- */
function stAddIcon(){
  const defs = document.querySelector('svg defs');
  if(!defs || document.getElementById('i-st-translate')) return;
  const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
  s.setAttribute('id','i-st-translate'); s.setAttribute('viewBox','0 0 100 100');
  s.innerHTML = '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M14 70 L30 26 L46 70"/><path d="M20 55 h20"/>' +
    '<path d="M60 32 h28"/><path d="M60 46 h28"/><path d="M60 60 h18"/>' +
    '<path d="M84 60 l-7 12"/></g>';
  defs.appendChild(s);
}

/* ---------- самоинициализация ---------- */
(function stInit(){
  stAddIcon();
  stInsertRows();

  /* chain-патч renderMsgs ядра: после каждого рендера — DOM-проход бейджей */
  if(typeof renderMsgs === 'function'){
    const _stPrevRenderMsgs = renderMsgs;
    renderMsgs = function(){
      _stPrevRenderMsgs.apply(this, arguments);
      stMarkTranslated();
    };
  }

  onLangChange(stApplyLang);

  /* язык уже персистится в setLang (oko-lang); при загрузке применяем сохранённый EN напрямую */
  if(LANG === 'en') stApplyLang('en');
})();

/* ===== module: admin-hq ===== */
/* ================= ADMIN-HQ: админка владельца + окно штаба OKO HQ =================
   Опирается на core-ext: isOwner, adminLogin, showPopup, okoRevenueTotal, OKO_REVENUE,
   fmtMoney, okoEarn. Патчит: openAdmin (гейт), renderAdmin (вкладки Доходы/Штаб HQ),
   admModer (ИИ-модерация сверху), admOverview (KPI дохода), renderMyProfile (чип CEO). */

/* ---------- иконка i-shield (безопасность) в общий defs ---------- */
(function hqAddIcons(){
  const defs = document.querySelector('svg defs');
  if(!defs || document.getElementById('i-shield')) return;
  const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
  s.setAttribute('id','i-shield'); s.setAttribute('viewBox','0 0 100 100');
  s.innerHTML = '<path d="M50 12l30 12v22c0 20-13 34-30 42-17-8-30-22-30-42V24l30-12z" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/><path d="M37 50l10 10 17-20" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/>';
  defs.appendChild(s);
})();

/* ---------- состояние модуля ---------- */
const HQ_STATE = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-admin-hq'))||{}; }catch(e){ return {}; } })();
function hqSave(){ try{ localStorage.setItem('oko-admin-hq', JSON.stringify(HQ_STATE)); }catch(e){} }

/* ---------- действия админа по юзерам (бан/галочка) — персист oko-admin-actions ---------- */
const HQ_ACT = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-admin-actions'))||{users:{}}; }catch(e){ return {users:{}}; } })();
HQ_ACT.users = HQ_ACT.users || {};
function hqActSave(){ try{ localStorage.setItem('oko-admin-actions', JSON.stringify(HQ_ACT)); }catch(e){} }
(function hqRestoreVerified(){ /* выданные галочки переживают перезагрузку */
  if(typeof VERIFIED === 'undefined') return;
  Object.keys(HQ_ACT.users).forEach(n=>{ if(HQ_ACT.users[n].ver) VERIFIED.add(n); });
})();

/* ---------- демо-наполнение выручки OKO (один раз, реальные источники okoEarn) ---------- */
(function hqSeedRevenue(){
  if(HQ_STATE.revSeeded) return;
  if(typeof OKO_REVENUE === 'undefined') return;
  const D = 864e5, now = Date.now();
  // [днейНазад, сумма, источник] — те же метки src, что шлют модули через okoEarn()
  const seed = [
    [0.10, 1490, 'Тарифы'],          [0.22, 250,  'Комиссия Биржи 10%'],
    [0.35, 349,  'Продвижение'],     [0.60, 120,  'Игры: рулетка'],
    [0.90, 3000, 'Рекламный кабинет'],[1.15, 45,   'Комиссия вывода'],
    [1.40, 2990, 'Тарифы'],          [1.85, 890,  'Комиссия Биржи 10%'],
    [2.30, 60,   'Игры: дорога'],    [3.10, 990,  'Продвижение'],
    [3.70, 1490, 'Тарифы'],          [4.20, 340,  'Игры: рулетка'],
    [5.05, 1500, 'Рекламный кабинет'],[5.80, 320,  'Комиссия Биржи 10%'],
    [6.40, 120,  'Комиссия вывода'], [7.25, 4990, 'Тарифы'],
    [8.10, 590,  'Продвижение'],     [9.00, 150,  'Игры: дорога'],
    [10.2, 1200, 'Комиссия Биржи 10%'],[11.4, 990, 'Тарифы'],
    [12.6, 5000, 'Рекламный кабинет'],[13.9, 80,  'Игры: рулетка'],
    [15.1, 450,  'Комиссия Биржи 10%'],[16.8, 349, 'Продвижение'],
    [18.0, 2990, 'Тарифы'],          [19.5, 75,   'Комиссия вывода'],
    [21.2, 210,  'Игры: рулетка'],   [23.0, 1000, 'Рекламный кабинет'],
    [24.8, 150,  'Комиссия Биржи 10%'],[26.5, 1490,'Тарифы'],
    [28.2, 90,   'Игры: дорога'],    [29.6, 500,  'Комиссия Биржи 10%'],
  ];
  seed.forEach(([d,sum,src])=> OKO_REVENUE.push({sum, src, at: now - d*D}));
  OKO_REVENUE.sort((a,b)=>b.at-a.at);
  try{ localStorage.setItem('oko-revenue', JSON.stringify(OKO_REVENUE)); }catch(e){}
  HQ_STATE.revSeeded = 1; hqSave();
})();

/* ---------- хелперы ---------- */
function hqHM(d){ return String(d.getHours()).padStart(2,'0')+':'+String(d.getMinutes()).padStart(2,'0'); }
function hqWhen(at){
  const d = new Date(at), n = new Date();
  if(d.toDateString() === n.toDateString()) return 'сегодня '+hqHM(d);
  if(d.toDateString() === new Date(n-864e5).toDateString()) return 'вчера '+hqHM(d);
  return String(d.getDate()).padStart(2,'0')+'.'+String(d.getMonth()+1).padStart(2,'0')+' '+hqHM(d);
}
/* дневные корзины дохода за N дней (учитывают активный фильтр источника) */
function hqDailyBuckets(list, days){
  const now = new Date();
  const start = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime();
  const out = [];
  for(let i=days-1;i>=0;i--){
    const d0 = start - i*864e5, d1 = d0 + 864e5;
    let sum = 0;
    for(const r of list){ if(r.at>=d0 && r.at<d1) sum += r.sum; }
    out.push({at:d0, sum});
  }
  return out;
}
/* столбчатый график динамики дохода за 14 дней */
function hqRevChart(list){
  const b = hqDailyBuckets(list, 14);
  const peak = b.reduce((m,x)=>Math.max(m,x.sum), 0);
  const max = Math.max(1, peak);
  const cols = b.map((x,i)=>{
    const h = x.sum ? Math.max(5, Math.round(x.sum/max*78)) : 1.5;
    const day = new Date(x.at).getDate();
    const isPk = peak>0 && x.sum===peak;
    return `<div class="hq-col${isPk?' pk':''}" style="--h:${h}%">`
      + `<span class="hq-col-v">+${fmtMoney(Math.round(x.sum))}</span>`
      + `<i style="animation-delay:${(i*0.035).toFixed(3)}s"></i><em>${day}</em></div>`;
  }).join('');
  return `<div class="hq-chart card">
    <div class="hq-chart-h"><small>Динамика дохода · 14 дней</small><b>пик +${fmtMoney(Math.round(peak))}</b></div>
    <div class="hq-chart-plot"><div class="hq-chart-grid"><span style="top:0"></span><span style="top:50%"></span><span style="bottom:0"></span></div>${cols}</div>
  </div>`;
}
/* мини-спарклайн (area+line) для KPI-плитки обзора — весь доход, 14 дней */
function hqMiniSpark(){
  const b = hqDailyBuckets(typeof OKO_REVENUE!=='undefined'?OKO_REVENUE:[], 14);
  const max = Math.max(1, ...b.map(x=>x.sum));
  const n = b.length;
  const xy = b.map((x,i)=>[ +(i/(n-1)*100).toFixed(2), +(19 - (x.sum/max)*17).toFixed(2) ]);
  const line = xy.map(p=>p[0]+','+p[1]).join(' ');
  const area = '0,20 ' + line + ' 100,20';
  const last = xy[xy.length-1];
  return `<svg class="hq-kpi-spark" viewBox="0 0 100 22" preserveAspectRatio="none">`
    + `<polygon class="ar" points="${area}"/>`
    + `<polyline class="ln" points="${line}"/>`
    + `<circle class="dot" cx="${last[0]}" cy="${last[1]}" r="2.4"/></svg>`;
}

/* ==================== 1. ГЕЙТ: раздел владельца ==================== */
const _prevOpenAdminHq = openAdmin;
openAdmin = function(){
  if(typeof isOwner === 'function' && !isOwner()){ hqShowGate(); return; }
  _prevOpenAdminHq();
};
function hqShowGate(){
  showPopup({
    ico:'lock', title:'Раздел владельца',
    body:`Админка и штаб OKO HQ доступны только владельцу. Подтверди личность.
      <div class="hq-gate">
        <input id="hqGateEmail" type="email" inputmode="email" autocomplete="email" placeholder="Email владельца">
        <input id="hqGatePass" type="password" autocomplete="current-password" placeholder="Пароль"
               onkeydown="if(event.key==='Enter')hqGateSubmit()">
        <div class="hq-gate-err" id="hqGateErr"></div>
        <button class="btn" id="hqGateBtn" onclick="hqGateSubmit()">Войти</button>
      </div>`,
    actions:[{label:'Отмена', ghost:true}]
  });
  setTimeout(()=>{ const e=document.getElementById('hqGateEmail'); if(e) e.focus(); }, 60);
}
async function hqGateSubmit(){
  const em = document.getElementById('hqGateEmail'), ps = document.getElementById('hqGatePass'),
        btn = document.getElementById('hqGateBtn'), err = document.getElementById('hqGateErr');
  if(!em || !btn) return;
  btn.disabled = true; btn.textContent = 'Проверка…'; if(err) err.textContent = '';
  let ok = false;
  try{ ok = await adminLogin(em.value, ps ? ps.value : ''); }catch(e){ ok = false; }
  if(ok){
    closePopup();
    toast('Владелец подтверждён');
    if(typeof renderMyProfile === 'function') try{ renderMyProfile(); }catch(e){}
    _prevOpenAdminHq();
    if(hqPendingTab){ admGo(hqPendingTab); hqPendingTab = null; }
  } else {
    btn.disabled = false; btn.textContent = 'Войти';
    if(err) err.textContent = 'Доступ запрещён';
    const card = document.querySelector('#okoPopup .pop-card');
    if(card){ card.classList.remove('hq-shake'); void card.offsetWidth; card.classList.add('hq-shake'); }
  }
}
let hqPendingTab = null;

/* ==================== 2-3. ВКЛАДКИ: Доходы + Штаб HQ ==================== */
ADMIN_TABS.push({k:'revenue', t:'Доходы'});
ADMIN_TABS.push({k:'hq', t:'Штаб HQ'});

const _prevRenderAdminHq = renderAdmin;
renderAdmin = function(){
  if(admTab === 'revenue' || admTab === 'hq'){
    document.getElementById('admTabs').innerHTML = ADMIN_TABS.map(t=>
      `<button class="adm-tab ${admTab===t.k?'on':''}" onclick="admGo('${t.k}')">${t.t}</button>`).join('');
    document.getElementById('admBody').innerHTML = admTab==='revenue' ? hqRevenueView() : hqHqView();
    if(admTab === 'hq') hqStartLog(); else hqStopLog();
    if(admTab === 'revenue' && !hqRevHeroDone){ hqRevHeroDone = true; hqAnimateRevHero(); }
    return;
  }
  hqStopLog();
  _prevRenderAdminHq();
};
const _prevCloseAdminHq = closeAdmin;
closeAdmin = function(){ hqStopLog(); hqRevHeroDone = false; _prevCloseAdminHq(); };

/* count-up дохода при первом открытии вкладки «Доходы» (не повторяется при смене фильтра) */
let hqRevHeroDone = false;
function hqAnimateRevHero(){
  const el = document.querySelector('.hq-rev-total'); if(!el) return;
  if(window.matchMedia && window.matchMedia('(prefers-reduced-motion:reduce)').matches) return;
  const target = Math.round(okoRevenueTotal()), t0 = performance.now(), dur = 900;
  const step = now=>{
    const k = Math.min(1, (now-t0)/dur), e = 1-Math.pow(1-k,3);
    el.textContent = fmtMoney(Math.round(target*e));
    if(k<1) requestAnimationFrame(step); else el.textContent = fmtMoney(target);
  };
  requestAnimationFrame(step);
}

/* ---------- вкладка «Доходы» ---------- */
const HQ_SRC_ICO = {
  'Тарифы':'crown', 'Комиссия Биржи 10%':'briefcase', 'Рекламный кабинет':'megaphone',
  'Продвижение':'rocket', 'Игры: рулетка':'fire', 'Игры: дорога':'fire', 'Комиссия вывода':'card'
};
let hqRevSrc = 'all', hqRevSrcs = ['all'];
function hqRevFilter(i){ hqRevSrc = hqRevSrcs[i] || 'all'; renderAdmin(); }
function hqRevenueView(){
  const total = Math.round(okoRevenueTotal());
  const now = new Date();
  const mStart = new Date(now.getFullYear(), now.getMonth(), 1).getTime();
  const mSum = Math.round(OKO_REVENUE.filter(r=>r.at>=mStart).reduce((s,r)=>s+r.sum,0));
  const dim = new Date(now.getFullYear(), now.getMonth()+1, 0).getDate();
  const proj = Math.round(now.getDate() ? mSum / now.getDate() * dim : 0);

  // агрегация по источникам
  const by = {};
  OKO_REVENUE.forEach(r=>{ by[r.src] = (by[r.src]||0) + r.sum; });
  const srcs = Object.keys(by).map(k=>({src:k, sum:by[k]})).sort((a,b)=>b.sum-a.sum);
  const max = srcs.length ? srcs[0].sum : 1;

  // фильтр по источнику (чипы)
  hqRevSrcs = ['all'].concat(srcs.map(s=>s.src));
  if(hqRevSrc !== 'all' && !by[hqRevSrc]) hqRevSrc = 'all';
  const list = hqRevSrc==='all' ? OKO_REVENUE : OKO_REVENUE.filter(r=>r.src===hqRevSrc);
  const chips = hqRevSrcs.map((s,i)=>
    `<button class="hq-chip ${hqRevSrc===s?'on':''}" onclick="hqRevFilter(${i})">${s==='all'?'Все источники':esc(s)}</button>`).join('');

  // суммы за периоды (по активному фильтру)
  const dayStart = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime();
  const per = from=>{ const a=list.filter(r=>r.at>=from); return {s:Math.round(a.reduce((x,r)=>x+r.sum,0)), n:a.length}; };
  const pT = per(dayStart), p7 = per(Date.now()-7*864e5), p30 = per(Date.now()-30*864e5);
  const perRow = (l,p)=>`<div class="hq-per"><span>${l}</span><small>${p.n} опер.</small><b>+${fmtMoney(p.s)}</b></div>`;

  const srcRows = srcs.map((s,i)=>`
    <div class="hq-src ${hqRevSrc===s.src?'on':''}" style="animation-delay:${i*0.05}s" onclick="hqRevFilter(${hqRevSrc===s.src?0:i+1})">
      <div class="hq-src-t"><b>${esc(s.src)}</b><span><i>${fmtMoney(Math.round(s.sum))}</i> · ${total?Math.round(s.sum/total*100):0}%</span></div>
      <div class="hq-bar"><i style="width:${max ? Math.max(3, s.sum/max*100) : 0}%"></i></div>
    </div>`).join('');

  const ops = list.slice(0,30).map(r=>`
    <div class="adm-row">
      <span class="hq-op-ic">${I(HQ_SRC_ICO[r.src]||'money')}</span>
      <span class="adm-main"><b>${esc(r.src)}</b><small>${hqWhen(r.at)}</small></span>
      <span class="hq-op-sum">+${fmtMoney(Math.round(r.sum))}</span>
    </div>`).join('');

  return `
    <div class="hq-rev-hero card">
      <small>Доход OKO — всего</small>
      <b class="hq-rev-total">${fmtMoney(total)}</b>
      <div class="hq-rev-sub">за этот месяц <b>${fmtMoney(mSum)}</b> · проекция месяца <b>${fmtMoney(proj)}</b></div>
    </div>
    <div class="hq-chips">${chips}</div>
    ${hqRevChart(list)}
    <div class="adm-sec-h">Суммы${hqRevSrc!=='all' ? ' · '+esc(hqRevSrc) : ''}</div>
    <div class="hq-pers card">${perRow('Сегодня',pT)}${perRow('7 дней',p7)}${perRow('30 дней',p30)}</div>
    <div class="adm-sec-h">По источникам</div>
    ${srcRows || '<p class="dim" style="font-size:13px">Пока нет операций — доход копится из комиссий, тарифов и рекламы.</p>'}
    <div class="adm-sec-h">Операции${hqRevSrc!=='all' ? ' · '+esc(hqRevSrc) : ''} (${Math.min(30, list.length)})</div>
    ${ops || '<p class="dim" style="font-size:13px">По этому источнику операций пока нет.</p>'}
    <div class="adm-acts"><button class="adm-btn" onclick="hqExportReport()">${I('file')} Выгрузить отчёт (.txt)</button></div>`;
}

/* ---------- экспорт: .txt-сводка владельца ---------- */
function hqExportReport(){
  const L = [], now = new Date();
  const pad = n=>String(n).padStart(2,'0');
  const stamp = pad(now.getDate())+'.'+pad(now.getMonth()+1)+'.'+now.getFullYear()+' '+hqHM(now);
  L.push('OKO — СВОДНЫЙ ОТЧЁТ ВЛАДЕЛЬЦА');
  L.push('Сформирован: '+stamp);
  L.push('==============================================');

  /* пользователи */
  L.push('', 'ПОЛЬЗОВАТЕЛИ');
  const k = ADMIN.kpi;
  L.push('Всего: '+k.users.toLocaleString('ru')+' · активных сегодня: '+k.dau+' · отток: '+k.churn+'%');
  ADMIN.users.forEach(u=>{
    const a = HQ_ACT.users[u.n] || {};
    const fl = [u.tier];
    if(typeof VERIFIED !== 'undefined' && VERIFIED.has(u.n)) fl.push('галочка');
    if(a.ban) fl.push('БАН');
    L.push('  '+u.n+' ('+u.h+') — '+fl.join(' · ')+' — '+u.when);
  });

  /* доходы по источникам */
  L.push('', 'ДОХОДЫ ПО ИСТОЧНИКАМ');
  const by = {}; OKO_REVENUE.forEach(r=>{ by[r.src]=(by[r.src]||0)+r.sum; });
  const total = Math.round(okoRevenueTotal());
  Object.keys(by).sort((a,b)=>by[b]-by[a]).forEach(s=>{
    L.push('  '+s+': '+fmtMoney(Math.round(by[s]))+' ('+(total?Math.round(by[s]/total*100):0)+'%)');
  });
  const sumFrom = from=>Math.round(OKO_REVENUE.filter(r=>r.at>=from).reduce((x,r)=>x+r.sum,0));
  const dayStart = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime();
  L.push('Итого: '+fmtMoney(total)+' · сегодня '+fmtMoney(sumFrom(dayStart))
    +' · 7д '+fmtMoney(sumFrom(Date.now()-7*864e5))+' · 30д '+fmtMoney(sumFrom(Date.now()-30*864e5)));

  /* рекламные кампании */
  L.push('', 'РЕКЛАМНЫЕ КАМПАНИИ');
  if(typeof ADS !== 'undefined' && ADS.camps && ADS.camps.length){
    ADS.camps.forEach(c=>{
      const st = (typeof ADS_STATUS !== 'undefined' && ADS_STATUS[c.status]) ? ADS_STATUS[c.status].l : c.status;
      L.push('  '+c.name+' — '+st+' · бюджет '+fmtMoney(c.budget)+' · откручено '+fmtMoney(Math.round(c.spent))
        +' · '+(c.imps||0).toLocaleString('ru')+' показов · '+(c.clicks||0).toLocaleString('ru')+' кликов');
    });
  } else L.push('  нет данных');

  /* сделки биржи */
  L.push('', 'СДЕЛКИ БИРЖИ (эскроу)');
  if(typeof MP !== 'undefined' && MP.deals && MP.deals.length){
    const stL = {work:'в работе', wait:'на подтверждении', done:'завершена'};
    MP.deals.forEach(d=> L.push('  '+d.t+' — '+d.n+' · '+fmtMoney(d.sum)+' · '+(stL[d.st]||d.st)));
  } else L.push('  нет данных');

  L.push('', '==============================================', 'OKO · штаб владельца · отчёт сформирован автоматически');

  const fname = 'oko-report-'+now.getFullYear()+'-'+pad(now.getMonth()+1)+'-'+pad(now.getDate())+'.txt';
  const url = URL.createObjectURL(new Blob(['﻿'+L.join('\n')], {type:'text/plain;charset=utf-8'}));
  const a = document.createElement('a');
  a.href = url; a.download = fname;
  document.body.appendChild(a); a.click(); a.remove();
  setTimeout(()=>URL.revokeObjectURL(url), 4000);
  toast('Отчёт выгружен: '+fname);
}

/* ==================== 2b. ПОЛЬЗОВАТЕЛИ: поиск / фильтр / бан / галочка / написать ==================== */
let hqUsrQ = '', hqUsrFilter = 'all';
const HQ_USR_FILTERS = [
  {k:'all',  l:'Все'}, {k:'paid', l:'Платные'}, {k:'free', l:'Бесплатные'},
  {k:'ver',  l:'С галочкой'}, {k:'ban',  l:'Забаненные'},
];
function hqUsrMatch(u){
  const a = HQ_ACT.users[u.n] || {};
  const ver = (typeof VERIFIED!=='undefined' && VERIFIED.has(u.n));
  if(hqUsrFilter==='paid' && !u.paid) return false;
  if(hqUsrFilter==='free' && u.paid) return false;
  if(hqUsrFilter==='ver'  && !ver) return false;
  if(hqUsrFilter==='ban'  && !a.ban) return false;
  if(hqUsrQ && (u.n+' '+u.h).toLowerCase().indexOf(hqUsrQ.toLowerCase()) < 0) return false;
  return true;
}
/* сохраняем оригинальный индекс в ADMIN.users — действия работают по нему при любом фильтре */
function hqUsrList(){ return ADMIN.users.map((u,i)=>({u,i})).filter(x=>hqUsrMatch(x.u)); }
function hqUserRow(u, i){
  const a = HQ_ACT.users[u.n] || {};
  const ver = (typeof VERIFIED!=='undefined' && VERIFIED.has(u.n));
  return `
    <div class="adm-row hq-usr ${a.ban?'ban':''}">
      <div class="hq-usr-top">
        <span class="adm-ava">${esc(u.n[0])}</span>
        <span class="adm-main"><b>${esc(u.n)}${typeof vBadge==='function' ? vBadge(u.n) : ''}</b><small>${esc(u.h)} · ${esc(u.when)}</small></span>
        ${a.ban ? '<span class="adm-tag no">БАН</span>' : ''}
        <span class="adm-tag ${u.paid?'paid':'free'}">${esc(u.tier)}</span>
      </div>
      <div class="hq-usr-acts">
        <button class="adm-btn ${a.ban?'':'dng'}" onclick="hqUserBan(${i})">${a.ban?'Разбанить':'Забанить'}</button>
        <button class="adm-btn" onclick="hqUserVerify(${i})">${ver ? 'Снять галочку' : 'Выдать галочку'}</button>
        <button class="adm-btn pri" onclick="hqUserMsg(${i})">Написать</button>
      </div>
    </div>`;
}
function hqUsrListHtml(){
  const list = hqUsrList();
  if(!list.length) return `<p class="dim hq-usr-empty">Никого не найдено${hqUsrQ?' по запросу «'+esc(hqUsrQ)+'»':' по этому фильтру'}.</p>`;
  return list.map(x=>hqUserRow(x.u, x.i)).join('');
}
/* обновляем ТОЛЬКО список — поле поиска не пересоздаётся, фокус/каретка сохраняются */
function hqUsrRenderList(){
  const box = document.getElementById('hqUsrList'); if(box) box.innerHTML = hqUsrListHtml();
  const c = document.getElementById('hqUsrCount'); if(c) c.textContent = hqUsrList().length;
}
function hqUsrSearch(v){ hqUsrQ = v; hqUsrRenderList(); }
function hqUsrSetFilter(k){
  hqUsrFilter = k;
  const chips = document.getElementById('hqUsrChips');
  if(chips) chips.querySelectorAll('.hq-chip').forEach(el=>el.classList.toggle('on', el.dataset.k===k));
  hqUsrRenderList();
}
const _prevAdmUsersHq = admUsers; /* базовый рендер заменён расширенным (chain сохранён) */
admUsers = function(){
  const chips = HQ_USR_FILTERS.map(f=>
    `<button class="hq-chip ${hqUsrFilter===f.k?'on':''}" data-k="${f.k}" onclick="hqUsrSetFilter('${f.k}')">${f.l}</button>`).join('');
  return `
    <div class="adm-sec-h">Пользователи · <span id="hqUsrCount">${hqUsrList().length}</span> из ${ADMIN.users.length}</div>
    <div class="hq-usr-search">${I('search')}<input id="hqUsrInput" placeholder="Поиск по имени или @нику" value="${esc(hqUsrQ)}" oninput="hqUsrSearch(this.value)" autocomplete="off"></div>
    <div class="hq-chips" id="hqUsrChips">${chips}</div>
    <div id="hqUsrList">${hqUsrListHtml()}</div>
    <div class="adm-acts"><button class="adm-btn" onclick="hqExportReport()">${I('file')} Выгрузить отчёт (.txt)</button></div>`;
};
function hqUserBan(i){
  const u = ADMIN.users[i]; if(!u) return;
  const a = HQ_ACT.users[u.n] = HQ_ACT.users[u.n] || {};
  a.ban = a.ban ? 0 : 1;
  hqActSave();
  if(admTab==='users') hqUsrRenderList(); else renderAdmin();
  toast(a.ban ? 'Пользователь '+u.n+' забанен' : u.n+' разбанен — доступ восстановлен');
}
function hqUserVerify(i){
  const u = ADMIN.users[i]; if(!u || typeof VERIFIED === 'undefined') return;
  const a = HQ_ACT.users[u.n] = HQ_ACT.users[u.n] || {};
  if(VERIFIED.has(u.n)){ VERIFIED.delete(u.n); a.ver = 0; toast('Галочка снята: '+u.n); }
  else { VERIFIED.add(u.n); a.ver = 1; toast('Галочка выдана: '+u.n); }
  hqActSave();
  if(admTab==='users') hqUsrRenderList(); else renderAdmin();
}
function hqUserMsg(i){
  const u = ADMIN.users[i]; if(!u) return;
  let c = CHATS.find(x=>x.kind==='direct' && x.name===u.n);
  if(!c){
    c = {id:'hq-u'+i, ava:u.n[0], name:u.n, kind:'direct', nick:(u.h||'').replace('@',''), kindIcon:null,
         preview:'Диалог начат из админки', time:hqHM(new Date()), unread:0, online:false,
         msgs:[{kind:'sys', body:'Диалог с пользователем открыт владельцем из админки OKO'}]};
    CHATS.unshift(c);
  }
  closeAdmin();
  showTab('chats');
  const s = document.getElementById('chatSearch');
  renderChatList(s ? s.value : '');
  openConv(c.id);
}

/* ---------- вкладка «Штаб HQ» ---------- */
/* КОМНАТЫ ШТАБА. Правка Даниэля 09.08: цифры больше не выдумываются.
   Назначение комнаты и её метрики описаны честно, а значения приходят из
   бэкенда. Пока данных нет — прочерк, а не «выручка +184к ₽». */
const HQ_ROOMS = [
  {n:'Финансы',      ic:'money',     c:'#d4af37', s:'Учёт, налоги, выплаты', live:0,
    d:'Учёт доходов и расходов, резерв под налог УСН, сверка эквайринга и выплат партнёрам.',
    k:[['выручка·мес','—'],['налог отложен','—'],['на выплаты','—'],['сверка','—']]},
  {n:'Юридический',  ic:'file',      c:'#ef4444', s:'Договоры и оферты', live:0,
    d:'Договоры, оферты, проверка по 152-ФЗ и 161-ФЗ, контроль реквизитов в диалогах.',
    k:[['в работе','—'],['на проверке','—'],['оферты','—'],['риски','—']]},
  {n:'Безопасность', ic:'shield',    c:'#9AFF00', s:'Аптайм и антифрод', live:0,
    d:'Мониторинг аптайма, антифрод, автоблок скам-реквизитов, защита аккаунтов от угона.',
    k:[['аптайм·30д','—'],['инциденты','—'],['автоблоков','—'],['2FA','—']]},
  {n:'Research Lab', ic:'search',    c:'#4aa0ff', s:'Ниши, конкуренты, тренды', live:0,
    d:'Разбор ниш и конкурентов от 1М просмотров, тренды форматов, отчёты в общий мозг.',
    k:[['разобрано','—'],['отчётов·нед','—'],['трендов','—'],['в мозг','—']]},
  {n:'War-room',     ic:'fire',      c:'#ff7a3c', s:'Горячие сделки', live:0,
    d:'Оперативный штаб по горячим сделкам: разбор лидов, дожим, эскалация продаж.',
    k:[['горячих лидов','—'],['в воронке','—'],['планёрка','—'],['конверсия','—']]},
  {n:'Comms',        ic:'megaphone', c:'#22d3ee', s:'Каналы и автоответы', live:0,
    d:'Внешние коммуникации, автоответы в каналах, модерация комментариев и заявок.',
    k:[['каналов','—'],['автоответы','—'],['ответ·ср','—'],['очередь','—']]},
  {n:'Publishing',   ic:'rocket',    c:'#a855f7', s:'Конвейер контента', live:0,
    d:'Конвейер контента: сборка роликов, календарь автопостинга, контроль расписания.',
    k:[['в очереди','—'],['пост сегодня','—'],['за неделю','—'],['статус','—']]},
  {n:'HR / Найм',    ic:'users',     c:'#ff6bad', s:'Аккаунты и роли', live:0,
    d:'Прогрев и распределение аккаунтов, онбординг новых агентов и ролей штаба.',
    k:[['прогрев','—'],['агентов','11'],['ролей','11'],['резерв','—']]},
];
/* ИИ-ШТАТ OKO — реальные роли из мастер-документа (раздел 6 контекста).
   Правка Даниэля 09.08: «демо штаб сделать реальным, реальные агенты».
   Роли и зоны ответственности настоящие — это состав, который запускается
   вместе с приложением. Выдуманные задачи («дожим лида @stomat_pro»,
   «договор №14», «тикет #218») и проценты загрузки убраны: агент показывает
   реальную задачу из очереди, а пока очередь пуста — честное «свободен».
   Поле pool оставлено пустым намеренно: подписи берутся из n8n-очереди. */
const HQ_AGENTS = [
  {id:'ceo',      role:'Гендиректор', c:'#9AFF00', st:'idle', p:0, task:'',
   about:'Собирает сводку по выручке, регистрациям и проблемным диалогам, раздаёт задачи остальным', pool:[]},
  {id:'support',  role:'Поддержка',   c:'#34d399', st:'idle', p:0, task:'',
   about:'Отвечает 24/7 строго из базы знаний. Деньги, возвраты и обещания — сразу человеку в @okohelp', pool:[]},
  {id:'assist',   role:'Ассистент',   c:'#8892a0', st:'idle', p:0, task:'',
   about:'Командный чат: утренний отчёт, приём команд человеческим языком, постановка задач через n8n', pool:[]},
  {id:'sales',    role:'Продажник',   c:'#4aa0ff', st:'idle', p:0, task:'',
   about:'Онбординг новых: ниша, кейсы, подбор тарифа. Скидки и обещания — только эскалация', pool:[]},
  {id:'manager',  role:'Менеджер',    c:'#22d3ee', st:'idle', p:0, task:'',
   about:'Ведёт платных: напоминает шаги, контролирует результат, работает на удержание', pool:[]},
  {id:'factory',  role:'Контент-завод', c:'#facc15', st:'idle', p:0, task:'',
   about:'Конвейер роликов: сценарий, рендер, субтитры, обложка, автопостинг по расписанию', pool:[]},
  {id:'editor',   role:'Монтажёр',    c:'#a855f7', st:'idle', p:0, task:'',
   about:'Сборка и докрутка ключевых роликов, цветокор, звук, караоке-субтитры', pool:[]},
  {id:'designer', role:'Дизайнер',    c:'#ff7a3c', st:'idle', p:0, task:'',
   about:'Обложки уроков, баннеры, карточки товаров и оформление в фирменном стиле', pool:[]},
  {id:'copy',     role:'Копирайтер',  c:'#ff6bad', st:'idle', p:0, task:'',
   about:'Хуки, сценарии, заголовки, прогревы и офферы под нишу клиента', pool:[]},
  {id:'analyst',  role:'Аналитик',    c:'#7dd3fc', st:'idle', p:0, task:'',
   about:'Снимает метрики по API соцсетей и корректирует контент-план по тому, что залетело', pool:[]},
  {id:'legal',    role:'Юрист',       c:'#d4af37', st:'idle', p:0, task:'',
   about:'Оферта, договоры, персональные данные, проверка реквизитов и платёжных условий', pool:[]},
];
const HQ_ST_LABEL = {work:'в работе', wait:'ожидает', think:'думает', idle:'свободен'};
const HQ_LOG_POOL = [
  {a:'designer', m:'обложка урока 2 готова'},
  {a:'sales',    m:'лид @stomat_pro перешёл на стадию «КП отправлено»'},
  {a:'editor',   m:'рендер 72% · субтитры синхронизированы'},
  {a:'legal',    m:'договор №14 чист по 161-ФЗ · передан CEO'},
  {a:'factory',  m:'ролик №3 ушёл в очередь автопостинга'},
  {a:'marketer', m:'воронка стоматологий: CPL прогноз 210 ₽'},
  {a:'support',  m:'тикет #218 закрыт · пользователь доволен'},
  {a:'copy',     m:'3 хука из 5 готовы · A/B на завтра'},
  {a:'assist',   m:'спарсено 14 конкурентов · отчёт в мозг'},
  {a:'ceo',      m:'аппрув КП MedCraft · отправить сегодня'},
  {a:'sales',    m:'новый входящий лид из ВК · передан в воронку'},
  {a:'factory',  m:'аналитика 10:00 собрана · охваты +18%'},
  {a:'editor',   m:'урок 2: финальный экспорт 1080×1920'},
  {a:'legal',    m:'блокирован Сбер-реквизит в диалоге · заменён на lava'},
  {a:'designer', m:'баннер тарифов · 2 варианта на выбор'},
  {a:'support',  m:'среднее время ответа за час: 41 сек'},
  {a:'marketer', m:'сегмент ЦА «владельцы клиник» готов'},
  {a:'assist',   m:'OCR скана завершён · данные в vault'},
  {a:'copy',     m:'заголовки лендинга: 6 вариантов в тесте'},
  {a:'ceo',      m:'выручка дня выше плана · штабу зачёт'},
];
let HQ_LOG = [];
(function hqSeedLog(){
  /* демо-сидер отключён 09.08 (правка Даниэля: ноль демо-данных) */
  return (arguments.length && Array.isArray(arguments[0])) ? [] : undefined;

  const now = Date.now();
  let t = now;
  for(let i=0;i<13;i++){
    t -= (60 + Math.random()*180) * 1000;
    const e = HQ_LOG_POOL[(HQ_LOG_POOL.length - 1 - i) % HQ_LOG_POOL.length];
    HQ_LOG.push({t: hqHM(new Date(t)), a: e.a, m: e.m});
  }
})();
/* ---------- онлайн-мониторинг: «сейчас в приложении» + спарклайн ---------- */
let hqOnline = 0, hqOnlineHist = [];
(function hqSeedOnline(){
  let v = 380 + Math.floor(Math.random()*60);
  for(let i=0;i<24;i++){
    v = Math.max(300, Math.min(520, v + Math.floor(Math.random()*17) - 8));
    hqOnlineHist.push(v);
  }
  hqOnline = hqOnlineHist[hqOnlineHist.length-1];
})();
function hqSparkSvg(){
  const h = hqOnlineHist;
  const min = Math.min.apply(null,h) - 6, max = Math.max.apply(null,h) + 6;
  const pts = h.map((v,i)=>((i/(h.length-1))*100).toFixed(1)+','+(26-(v-min)/(max-min)*22).toFixed(1)).join(' ');
  return `<svg class="hq-spark" viewBox="0 0 100 28" preserveAspectRatio="none">
    <polygon points="0,28 ${pts} 100,28" fill="var(--lime-dim)"/>
    <polyline points="${pts}" fill="none" stroke="var(--lime)" stroke-width="1.7" stroke-linejoin="round" stroke-linecap="round"/>
  </svg>`;
}
function hqOnlineBlock(){
  return `<div class="hq-online card">
    <div class="hq-on-l">
      <small><i class="hq-dot work"></i> Сейчас в приложении</small>
      <b id="hqOnlineN">${hqOnline}</b>
      <span class="hq-on-d" id="hqOnlineD">live · пик ${Math.max.apply(null,hqOnlineHist)}</span>
    </div>
    <div class="hq-on-r" id="hqSparkWrap">${hqSparkSvg()}</div>
  </div>`;
}
function hqOnlineTick(){
  const d = Math.floor(Math.random()*19) - 9;
  hqOnline = Math.max(300, Math.min(520, hqOnline + d));
  hqOnlineHist.push(hqOnline); if(hqOnlineHist.length > 24) hqOnlineHist.shift();
  const nEl = document.getElementById('hqOnlineN'); if(nEl) nEl.textContent = hqOnline;
  const dEl = document.getElementById('hqOnlineD');
  if(dEl) dEl.innerHTML = (d>=0 ? '<i class="up">+'+d+'</i>' : '<i class="dn">−'+Math.abs(d)+'</i>')
    + ' за тик · пик '+Math.max.apply(null,hqOnlineHist);
  const sw = document.getElementById('hqSparkWrap'); if(sw) sw.innerHTML = hqSparkSvg();
}

function hqAgent(id){ return HQ_AGENTS.find(a=>a.id===id); }
function hqLogLine(e, fresh){
  const ag = hqAgent(e.a) || {role:'OKO', c:'var(--accent)'};
  return `<div class="hq-ll${fresh?' new':''}"><span class="hq-lt">${e.t}</span><b style="color:${ag.c}">${esc(ag.role)}</b><span class="m">→ ${esc(e.m)}</span></div>`;
}
function hqAgentCard(a, i){
  return `<div class="hq-ag" style="animation-delay:${i*0.04}s">
    <div class="hq-ag-top">
      <span class="hq-ava" style="color:${a.c};background:${a.c}1e"><svg class="i"><use href="#i-logo"/></svg></span>
      <span class="hq-ag-n"><b>${esc(a.role)}</b>
        <span class="hq-ag-st"><i class="hq-dot ${a.st}" id="hq-d-${a.id}"></i><span id="hq-s-${a.id}">${HQ_ST_LABEL[a.st]}</span></span>
      </span>
    </div>
    <div class="hq-ag-task" id="hq-t-${a.id}">${esc(a.task)}</div>
    <div class="hq-bar"><i id="hq-p-${a.id}" style="width:${a.p}%;background:linear-gradient(90deg,${a.c},${a.c}99)"></i></div>
  </div>`;
}
function hqHqView(){
  return `
    <div class="hq-head card">
      <span class="hq-head-eye"><svg class="i"><use href="#i-logo"/></svg></span>
      <div class="hq-head-b">
        <h3>OKO HQ — командный центр</h3>
        <small>10 агентов · 8 отделов · аптайм 99.98%<br>мозг синхронизирован ${hqHM(new Date())}</small>
      </div>
    </div>
    ${hqPortalCard()}
    ${hqOnlineBlock()}
    <div class="adm-sec-h">Отделы</div>
    <div class="hq-rooms">${HQ_ROOMS.map((r,i)=>`
      <button class="hq-room" style="animation-delay:${i*0.04}s" onclick="hqRoomOpen(${i})">
        <span class="hq-room-ic" style="color:${r.c};background:${r.c}1e">${I(r.ic)}</span>
        <b>${esc(r.n)} ${r.live?'<i class="hq-dot work"></i>':''}</b>
        <small>${esc(r.s)}</small>
      </button>`).join('')}</div>
    <div class="adm-sec-h">Агенты (${HQ_AGENTS.length})</div>
    <div class="hq-agents" id="hqAgents">${HQ_AGENTS.map(hqAgentCard).join('')}</div>
    <div class="adm-sec-h">Живой лог штаба</div>
    <div class="hq-log" id="hqLog">${HQ_LOG.map(e=>hqLogLine(e)).join('')}</div>`;
}
/* Гля-портал в 3D-штаб: самодостаточный превью-тумбнейл (SVG/CSS, без внешних картинок —
   офлайн/CSP-safe) + понятный CTA. Клик по всей карточке открывает внешнюю вкладку hq.html
   через hqOpen3d() (тяжёлый WebGL намеренно НЕ встраивается — лагает в webview). */
function hqPortalCard(){
  /* агенты-орбиты вокруг знака OKO — цвета берём из палитры отделов/агентов */
  const nodes = ['#9AFF00','#4aa0ff','#a855f7','#ff7a3c','#22d3ee','#facc15','#ff6bad','#34d399']
    .map((c,i,arr)=>`<i style="--a:${Math.round(i*360/arr.length)}deg;--c:${c};--d:${(i*0.28).toFixed(2)}s"></i>`).join('');
  return `
    <button class="hq-portal card" type="button" onclick="hqOpen3d()" aria-label="Открыть 3D-штаб OKO в новой вкладке">
      <div class="hq-portal-scene" aria-hidden="true">
        <div class="hq-portal-grid"></div>
        <div class="hq-portal-glow"></div>
        <div class="hq-portal-orbit"><div class="hq-portal-ring">${nodes}</div></div>
        <span class="hq-portal-eye"><svg class="i"><use href="#i-logo"/></svg></span>
        <span class="hq-portal-scan"></span>
        <span class="hq-portal-badge"><i class="hq-dot work"></i>LIVE · 3D</span>
        <span class="hq-portal-chip">WebGL-штаб · ${HQ_AGENTS.length} агентов</span>
        <span class="hq-portal-play">${I('share')}</span>
      </div>
      <div class="hq-portal-cta">
        <span class="hq-portal-t"><b>Открыть 3D-штаб</b><small>отдельная вкладка · true-journey-418.higgsfield.app</small></span>
        <span class="hq-portal-go">${I('chev')}</span>
      </div>
    </button>
    <small class="hq-3d-note">${I('bolt')} Тяжёлый WebGL вынесен из приложения — открывается отдельной страницей, чтобы Telegram-webview не лагал.</small>`;
}
/* детальный дашборд отдела: реальные KPI вместо заглушки-тоста */
function hqRoomOpen(i){
  const r = HQ_ROOMS[i]; if(!r) return;
  const kpis = (r.k||[]).map(([l,v])=>
    `<div class="hq-room-kpi"><b>${esc(v)}</b><small>${esc(l)}</small></div>`).join('');
  showPopup({
    ico:r.ic, title:'Отдел · '+r.n,
    body:`<div class="hq-room-pop">
      <div class="hq-room-pop-st"><i class="hq-dot ${r.live?'work':'wait'}"></i>${r.live?'на смене · агент активен':'в резерве · включается по расписанию'}</div>
      ${r.d?`<p class="hq-room-pop-d">${esc(r.d)}</p>`:''}
      <div class="hq-room-kpis">${kpis}</div>
      <div class="hq-room-pop-s">${I('bolt')}<span>${esc(r.s)}</span></div>
    </div>`,
    actions:[{label:'Закрыть'}]
  });
}

/* Абсолютный публичный адрес 3D-штаба. По требованию владельца тяжёлый WebGL НЕ встраивается
   в приложение (лагает в Telegram-webview) — штаб открывается отдельной вкладкой (ссылкой). */
const HQ_URL_ABS = 'https://true-journey-418.higgsfield.app/hq.html';

/* открыть штаб во внешней вкладке: в Telegram → tg.openLink, иначе → window.open */
function hqOpenExternal(){
  const tg = (window.Telegram && window.Telegram.WebApp) ? window.Telegram.WebApp : null;
  try{ if(tg && typeof tg.openLink === 'function'){ tg.openLink(HQ_URL_ABS); return true; } }catch(e){}
  try{ const w = window.open(HQ_URL_ABS, '_blank', 'noopener'); return !!w; }catch(e){ return false; }
}
function hqOpen3d(){
  if(typeof isOwner === 'function' && !isOwner()){ hqShowGate(); return; }
  try{ localStorage.setItem('oko-hq-auth','1'); }catch(e){}
  const ok = hqOpenExternal();
  if(typeof toast === 'function') toast(ok ? 'Открываю 3D-штаб в новой вкладке' : 'Штаб: ' + HQ_URL_ABS);
}

/* ---------- живой лог: тик каждые 6 секунд ---------- */
let hqTimer = null;
function hqStartLog(){ if(!hqTimer) hqTimer = setInterval(hqTick, 6000); }
function hqStopLog(){ if(hqTimer){ clearInterval(hqTimer); hqTimer = null; } }
function hqTick(){
  const av = document.getElementById('adminView');
  if(!av || !av.classList.contains('open') || admTab !== 'hq'){ hqStopLog(); return; }
  hqOnlineTick(); // онлайн-счётчик + спарклайн
  // новая строка лога
  const e = HQ_LOG_POOL[Math.floor(Math.random()*HQ_LOG_POOL.length)];
  const line = {t: hqHM(new Date()), a: e.a, m: e.m};
  HQ_LOG.unshift(line); if(HQ_LOG.length > 15) HQ_LOG.length = 15;
  const logEl = document.getElementById('hqLog');
  if(logEl){
    logEl.insertAdjacentHTML('afterbegin', hqLogLine(line, true));
    while(logEl.children.length > 15) logEl.lastElementChild.remove();
  }
  // прогресс агентов: 2 случайных двигаются
  for(let k=0;k<2;k++){
    const a = HQ_AGENTS[Math.floor(Math.random()*HQ_AGENTS.length)];
    if(a.st === 'wait') continue;
    a.p += 3 + Math.floor(Math.random()*7);
    if(a.p >= 100){
      const done = {t: hqHM(new Date()), a: a.id, m: 'задача готова: '+a.task};
      HQ_LOG.unshift(done); if(HQ_LOG.length > 15) HQ_LOG.length = 15;
      if(logEl){ logEl.insertAdjacentHTML('afterbegin', hqLogLine(done, true)); while(logEl.children.length > 15) logEl.lastElementChild.remove(); }
      a.task = a.pool[a._pi = ((a._pi||0)+1) % a.pool.length];
      a.p = 4 + Math.floor(Math.random()*10);
    }
    const bar = document.getElementById('hq-p-'+a.id); if(bar) bar.style.width = a.p+'%';
    const tt = document.getElementById('hq-t-'+a.id); if(tt) tt.textContent = a.task;
  }
  // изредка агент меняет состояние
  if(Math.random() < 0.22){
    const a = HQ_AGENTS[Math.floor(Math.random()*HQ_AGENTS.length)];
    const order = ['work','think','work','wait'];
    a.st = order[Math.floor(Math.random()*order.length)];
    const d = document.getElementById('hq-d-'+a.id); if(d) d.className = 'hq-dot '+a.st;
    const s = document.getElementById('hq-s-'+a.id); if(s) s.textContent = HQ_ST_LABEL[a.st];
  }
}

/* ==================== 4. МОДЕРАЦИЯ+: ИИ-агент сверху очереди ==================== */
const HQ_MOD = HQ_STATE.mod || {spam:46, scam:12, adult:7, drugs:3, checked:8214};
HQ_STATE.mod = HQ_MOD; hqSave();
const HQ_MOD_FEED = []  /* демо-данные удалены 09.08: 3D-штаб — источник только API */;
/* ---- жалобы пользователей (персист oko-admin-hq.reports) ---- */
const HQ_REPORTS = (HQ_STATE.reports && Array.isArray(HQ_STATE.reports)) ? HQ_STATE.reports : [];  /* демо-жалобы удалены 09.08 — модерация приходит из Supabase */
HQ_STATE.reports = HQ_REPORTS; hqSave();
const HQ_REP_ICO = {'Профиль':'user','Пост':'feed','Объявление':'briefcase','Канал':'crown'};
const HQ_REP_DONE = {ban:{l:'заблокирован', cls:'no'}, warn:{l:'предупреждён', cls:'wait'}, ok:{l:'отклонена', cls:'ok'}};
function hqReportFind(id){ return HQ_REPORTS.find(r=>r.id===id); }
function hqReportBan(id){
  const r = hqReportFind(id); if(!r || r.done) return;
  r.done = 'ban';
  const u = ADMIN.users.find(x=>x.n===r.tgt || x.h===r.tgt); /* если цель — известный юзер, баним и его карточку */
  if(u){ const a = HQ_ACT.users[u.n] = HQ_ACT.users[u.n]||{}; a.ban = 1; hqActSave(); }
  hqSave(); renderAdmin(); toast('Нарушитель заблокирован, жалоба закрыта');
}
function hqReportWarn(id){ const r=hqReportFind(id); if(!r||r.done) return; r.done='warn'; hqSave(); renderAdmin(); toast('Предупреждение отправлено автору'); }
function hqReportDismiss(id){ const r=hqReportFind(id); if(!r||r.done) return; r.done='ok'; hqSave(); renderAdmin(); toast('Жалоба отклонена — нарушений не найдено'); }
function hqReportsBlock(){
  const open = HQ_REPORTS.filter(r=>!r.done).length;
  const rows = HQ_REPORTS.map(r=>{
    if(r.done){
      const d = HQ_REP_DONE[r.done] || {l:'закрыта', cls:'ok'};
      return `<div class="adm-row hq-rep done">
        <span class="hq-rep-ic">${I(HQ_REP_ICO[r.kind]||'flag')}</span>
        <span class="adm-main"><b style="white-space:normal">${esc(r.kind)}: ${esc(r.tgt)}</b><small>${esc(r.reason)}</small></span>
        <span class="adm-tag ${d.cls}">${d.l}</span></div>`;
    }
    return `<div class="hq-rep card">
      <div class="hq-rep-top">
        <span class="hq-rep-ic">${I(HQ_REP_ICO[r.kind]||'flag')}</span>
        <span class="adm-main"><b style="white-space:normal">${esc(r.kind)}: ${esc(r.tgt)}</b><small>жалоба от ${esc(r.by)} · ${r.ago} мин назад</small></span>
      </div>
      <div class="hq-rep-reason">${I('flag')}<span>${esc(r.reason)}</span></div>
      <div class="hq-rep-acts">
        <button class="adm-btn dng" onclick="hqReportBan('${r.id}')">Заблокировать</button>
        <button class="adm-btn" onclick="hqReportWarn('${r.id}')">Предупредить</button>
        <button class="adm-btn" onclick="hqReportDismiss('${r.id}')">Отклонить</button>
      </div>
    </div>`;
  }).join('');
  return `<div class="adm-sec-h">Жалобы пользователей${open?' · '+open+' новых':''}</div>${rows}`;
}

/* ---- реклама на модерации: реальные кампании из кабинета (ADS) + собственная очередь ---- */
const HQ_ADREV = (HQ_STATE.adRev && Array.isArray(HQ_STATE.adRev)) ? HQ_STATE.adRev : [
  {id:'a1', name:'Курс «Трейдинг на автопилоте»', fmt:'Продвижение поста', budget:5000, reason:'обещание гарантированного дохода — против правил площадки', done:0},
  {id:'a2', name:'ЗооОпт — корма оптом дешевле',  fmt:'Объявление Биржи',  budget:2000, reason:'',                                                     done:0},
  {id:'a3', name:'Клиника «Дента» · импланты',    fmt:'Баннер',            budget:3500, reason:'мед-тематика — требуется подтверждение лицензии',      done:0},
];
HQ_STATE.adRev = HQ_ADREV; hqSave();
function hqAdRevDecide(id, ok){
  const a = HQ_ADREV.find(x=>x.id===id); if(!a || a.done) return;
  a.done = ok ? 'act' : 'rej';
  hqSave(); renderAdmin(); toast(ok ? 'Кампания одобрена и запущена' : 'Кампания отклонена, бюджет возвращён');
}
function hqRealPendingAds(){
  if(typeof ADS==='undefined' || !ADS || !Array.isArray(ADS.camps)) return [];
  return ADS.camps.filter(c=>c && c.status==='mod');
}
function hqRealAdDecide(id, ok){
  if(typeof ADS==='undefined' || !ADS.camps) return;
  const c = ADS.camps.find(x=>x.id===id); if(!c) return;
  if(ok){ c.status='act'; if(typeof adsPushToFeed==='function'){ try{ adsPushToFeed(c); }catch(e){} } }
  else { c.status='rej'; c.reason='отклонено владельцем на ручной модерации'; }
  if(typeof adsSave==='function'){ try{ adsSave(); }catch(e){} }
  renderAdmin(); toast(ok ? 'Реальная кампания одобрена' : 'Реальная кампания отклонена');
}
function hqAdsBlock(){
  const real = hqRealPendingAds();
  const realRows = real.map(c=>`
    <div class="hq-ad card live">
      <div class="hq-ad-top"><span class="hq-ad-ic">${I('megaphone')}</span>
        <span class="adm-main"><b style="white-space:normal">${esc(c.name)}</b><small>из рекламного кабинета · бюджет ${fmtMoney(c.budget)} · ждёт решения</small></span>
        <span class="adm-tag wait">LIVE</span></div>
      <div class="hq-ad-acts">
        <button class="adm-btn pri" onclick="hqRealAdDecide(${c.id},1)">Одобрить</button>
        <button class="adm-btn dng" onclick="hqRealAdDecide(${c.id},0)">Отклонить</button>
      </div>
    </div>`).join('');
  const rows = HQ_ADREV.map(a=>{
    if(a.done){
      return `<div class="adm-row hq-ad done"><span class="hq-ad-ic">${I('megaphone')}</span>
        <span class="adm-main"><b style="white-space:normal">${esc(a.name)}</b><small>${esc(a.fmt)} · ${fmtMoney(a.budget)}</small></span>
        <span class="adm-tag ${a.done==='act'?'ok':'no'}">${a.done==='act'?'одобрена':'отклонена'}</span></div>`;
    }
    return `<div class="hq-ad card">
      <div class="hq-ad-top"><span class="hq-ad-ic">${I('megaphone')}</span>
        <span class="adm-main"><b style="white-space:normal">${esc(a.name)}</b><small>${esc(a.fmt)} · бюджет ${fmtMoney(a.budget)}</small></span></div>
      ${a.reason
        ? `<div class="hq-rep-reason">${I('flag')}<span>Флаг системы: ${esc(a.reason)}</span></div>`
        : `<div class="hq-ad-clean">${I('check2')}<span>Нарушений не найдено, на финальное решение владельца</span></div>`}
      <div class="hq-ad-acts">
        <button class="adm-btn pri" onclick="hqAdRevDecide('${a.id}',1)">Одобрить</button>
        <button class="adm-btn dng" onclick="hqAdRevDecide('${a.id}',0)">Отклонить</button>
      </div>
    </div>`;
  }).join('');
  const openN = real.length + HQ_ADREV.filter(a=>!a.done).length;
  return `<div class="adm-sec-h">Реклама на модерации${openN?' · '+openN:''}</div>${realRows}${rows}`;
}

const _prevAdmModerHq = admModer;
admModer = function(){
  const m = HQ_MOD;
  const head = `
    <div class="hq-mod card">
      <div class="hq-mod-head"><i class="hq-dot work"></i><b>Модератор OKO: активен</b><span class="chip on" style="font-size:9px">24/7</span></div>
      <div class="hq-mod-stats">
        <div class="hq-ms"><b>${m.spam}</b><small>спам</small></div>
        <div class="hq-ms bad"><b>${m.scam}</b><small>скам</small></div>
        <div class="hq-ms bad"><b>${m.adult}</b><small>18+</small></div>
        <div class="hq-ms bad"><b>${m.drugs}</b><small>наркотики</small></div>
      </div>
      <div class="hq-mod-note">За сутки проверено ${m.checked.toLocaleString('ru')} сообщений и постов. Автоблок мгновенный, спорное — в очередь ниже.</div>
    </div>
    <div class="adm-sec-h">Последние автоблокировки</div>
    ${HQ_MOD_FEED.map(f=>`
      <div class="adm-row">
        <span class="adm-tag ${f.cls}">${esc(f.k)}</span>
        <span class="adm-main"><b style="white-space:normal">${esc(f.frag)}</b><small>${f.ago} мин назад · ${esc(f.act)}</small></span>
      </div>`).join('')}`;
  return head + hqReportsBlock() + hqAdsBlock() + _prevAdmModerHq();
};

/* ==================== 4b. ПАРТНЁРКА: сводка + рабочие выплаты (персист) ==================== */
function hqPartEarn(p){ return parseInt(String(p.earn).replace(/[^0-9]/g,'')) || 0; }
function hqPartPaid(p){ HQ_ACT.paid = HQ_ACT.paid || {}; return HQ_ACT.paid[p.n] || p.st==='ok'; }
const _prevAdmPartnersHq = admPartners;
admPartners = function(){
  HQ_ACT.paid = HQ_ACT.paid || {};
  const totalRef = ADMIN.partners.reduce((s,p)=>s+p.ref, 0);
  const toPay = ADMIN.partners.filter(p=>!hqPartPaid(p)).reduce((s,p)=>s+hqPartEarn(p), 0);
  const rows = ADMIN.partners.map((p,i)=>`
    <div class="adm-row">
      <span class="adm-ava">${esc(p.n[0])}</span>
      <span class="adm-main"><b>${esc(p.n)} · ${esc(p.earn)}</b><small>${esc(p.h)} · ${p.ref} рефералов</small></span>
      ${hqPartPaid(p)
        ? '<span class="adm-tag ok">выплачено</span>'
        : `<button class="adm-btn pri" onclick="hqPartnerPay(${i})">Выплатить</button>`}
    </div>`).join('');
  return `
    <div class="hq-part-sum card">
      <div class="hq-part-s"><b>${ADMIN.partners.length}</b><small>партнёров</small></div>
      <div class="hq-part-s"><b>${totalRef}</b><small>рефералов</small></div>
      <div class="hq-part-s"><b>$${toPay}</b><small>к выплате</small></div>
    </div>
    <div class="adm-sec-h">Партнёры и выплаты</div>${rows}`;
};
function hqPartnerPay(i){
  const p = ADMIN.partners[i]; if(!p) return;
  HQ_ACT.paid = HQ_ACT.paid || {}; HQ_ACT.paid[p.n] = 1; hqActSave();
  renderAdmin(); toast('Выплата '+p.earn+' отправлена партнёру '+p.n);
}

/* ==================== 6. ОБЗОР: KPI «Доход OKO» + панель «Быстро» ==================== */
const _prevAdmOverviewHq = admOverview;
admOverview = function(){
  const html = _prevAdmOverviewHq();
  const kpi = `<div class="adm-kpi hq-kpi-rev"><b style="font-size:24px;padding-top:4px;display:block">${fmtMoney(Math.round(okoRevenueTotal()))}</b>${hqMiniSpark()}<small>Доход OKO (комиссии) <i class="up" style="cursor:pointer" onclick="admGo('revenue')">→ Доходы</i></small></div>`;
  const quick = `
    <div class="adm-sec-h">Быстро · действия владельца</div>
    <div class="hq-quick">
      <button class="hq-qbtn" onclick="hqQuickMoney()"><span class="hq-qic">${I('money')}</span><b>+10 000 ₽</b><small>тестовые на кошелёк</small></button>
      <button class="hq-qbtn" onclick="hqExportReport()"><span class="hq-qic">${I('file')}</span><b>Отчёт .txt</b><small>полная сводка</small></button>
      <button class="hq-qbtn dng" onclick="hqResetDemo()"><span class="hq-qic">${I('trash')}</span><b>Сброс демо</b><small>очистить oko-данные</small></button>
    </div>`;
  return html.replace('<div class="adm-kpis">', '<div class="adm-kpis">'+kpi) + quick;
};
function hqQuickMoney(){
  if(typeof walletAdd !== 'function'){ toast('Кошелёк недоступен'); return; }
  walletAdd(10000, 'Тестовое начисление владельца');
  toast('+10 000 ₽ зачислено на кошелёк (тест)');
}
function hqResetDemo(){
  showPopup({
    ico:'trash', title:'Сбросить демо-данные?',
    body:'Будут очищены все локальные данные приложения (ключи oko-*): кошелёк, доходы, кампании, сделки, прогресс. Авторизация и статус владельца сохранятся. После сброса приложение перезагрузится.',
    actions:[
      {label:'Сбросить всё', onclick:hqDoResetDemo},
      {label:'Отмена', ghost:true}
    ]
  });
}
function hqDoResetDemo(){
  try{
    Object.keys(localStorage).forEach(k=>{
      if(k.indexOf('oko-')===0 && !/auth|owner/.test(k)) localStorage.removeItem(k);
    });
  }catch(e){}
  toast('Демо-данные сброшены — перезагрузка…');
  setTimeout(()=>location.reload(), 700);
}

/* ==================== 4c. ПЛАТЕЖИ: сводка + фильтр + карточка транзакции + возврат/подтверждение ==================== */
HQ_STATE.payOv = HQ_STATE.payOv || {};   /* переопределения статуса по индексу (персист) */
hqSave();
function hqPayNum(s){ return parseInt(String(s).replace(/[^0-9]/g,'')) || 0; }
function hqPaySt(i){ return HQ_STATE.payOv[i] || (ADMIN.pay[i] ? ADMIN.pay[i].st : 'wait'); }
function hqPayTier(p){ return String(p.plan||'').split(/\s|·/)[0].trim().toUpperCase(); }
const HQ_PAY_METHOD = {
  BUSINESS:'Карта Visa ····4417', PRO:'Карта Mastercard ····8820',
  START:'СБП · Сбербанк', FREE:'—'
};
const HQ_PAY_ST = {ok:{l:'проведён', cls:'ok'}, wait:{l:'ожидает', cls:'wait'}, refunded:{l:'возврат', cls:'no'}};
let hqPayFilter = 'all';
const HQ_PAY_FILTERS = [{k:'all',l:'Все'},{k:'ok',l:'Проведённые'},{k:'wait',l:'Ожидают'},{k:'refunded',l:'Возвраты'}];
function hqPaySetFilter(k){ hqPayFilter = k; renderAdmin(); }
function hqPayTxn(p, i){
  let h = 0; const s = (p.n||'')+(p.plan||'');
  for(let j=0;j<s.length;j++) h = (h*31 + s.charCodeAt(j)) >>> 0;
  return 'OKO-'+new Date().getFullYear()+'-'+String(100000 + (h + i*4177) % 899999);
}
function hqPayOpen(i){
  const p = ADMIN.pay[i]; if(!p) return;
  const st = hqPaySt(i), sm = hqPayNum(p.sum), fee = Math.round(sm*3.5)/100, net = Math.round((sm - fee)*100)/100;
  const tier = hqPayTier(p), meta = HQ_PAY_ST[st] || HQ_PAY_ST.wait;
  const det = `<div class="hq-pay-det">
      <div class="hq-pay-drow"><span>Транзакция</span><b>${hqPayTxn(p,i)}</b></div>
      <div class="hq-pay-drow"><span>Плательщик</span><b>${esc(p.n)}</b></div>
      <div class="hq-pay-drow"><span>Тариф</span><b>${esc(p.plan)}</b></div>
      <div class="hq-pay-drow"><span>Метод</span><b>${esc(HQ_PAY_METHOD[tier]||'Карта')}</b></div>
      <div class="hq-pay-drow"><span>Дата</span><b>${esc(p.when)}</b></div>
      <div class="hq-pay-drow"><span>Статус</span><b><span class="adm-tag ${meta.cls}">${meta.l}</span></b></div>
    </div>
    <div class="hq-pay-det hq-pay-break">
      <div class="hq-pay-drow"><span>Сумма платежа</span><b>$${sm}</b></div>
      <div class="hq-pay-drow"><span>Эквайринг · 3.5%</span><b class="neg">−$${fee.toFixed(2)}</b></div>
      <div class="hq-pay-drow total"><span>К зачислению</span><b>$${net.toFixed(2)}</b></div>
    </div>`;
  const actions = [];
  if(st === 'wait'){
    actions.push({label:'Подтвердить платёж', onclick:()=>hqPayConfirm(i)});
    actions.push({label:'Отклонить', ghost:true, onclick:()=>hqPayRefund(i)});
  } else if(st === 'ok'){
    actions.push({label:'Оформить возврат', onclick:()=>hqPayRefund(i)});
    actions.push({label:'Закрыть', ghost:true});
  } else {
    actions.push({label:'Закрыть'});
  }
  showPopup({ico:'card', title:'Платёж · $'+sm, body:det, actions});
}
function hqPayConfirm(i){
  if(!ADMIN.pay[i]) return;
  HQ_STATE.payOv[i] = 'ok'; hqSave();
  renderAdmin(); toast('Платёж подтверждён · тариф активирован');
}
function hqPayRefund(i){
  if(!ADMIN.pay[i]) return;
  HQ_STATE.payOv[i] = 'refunded'; hqSave();
  renderAdmin(); toast('Возврат оформлен · $'+hqPayNum(ADMIN.pay[i].sum)+' возвращены плательщику');
}
const _prevAdmPayHq = admPay;
admPay = function(){
  const okSum   = ADMIN.pay.reduce((s,p,i)=> hqPaySt(i)==='ok'   ? s+hqPayNum(p.sum) : s, 0);
  const waitN   = ADMIN.pay.filter((p,i)=> hqPaySt(i)==='wait').length;
  const refSum  = ADMIN.pay.reduce((s,p,i)=> hqPaySt(i)==='refunded' ? s+hqPayNum(p.sum) : s, 0);
  const okCount = ADMIN.pay.filter((p,i)=> hqPaySt(i)==='ok').length;
  const avg     = okCount ? Math.round(okSum/okCount) : 0;
  const chips = HQ_PAY_FILTERS.map(f=>
    `<button class="hq-chip ${hqPayFilter===f.k?'on':''}" onclick="hqPaySetFilter('${f.k}')">${f.l}</button>`).join('');
  const idx = ADMIN.pay.map((p,i)=>i).filter(i=> hqPayFilter==='all' ? true : hqPaySt(i)===hqPayFilter);
  const rows = idx.map(i=>{
    const p = ADMIN.pay[i], st = hqPaySt(i), meta = HQ_PAY_ST[st] || HQ_PAY_ST.wait, tier = hqPayTier(p);
    return `<div class="adm-row hq-pay ${st}" onclick="hqPayOpen(${i})">
      <span class="hq-pay-ic">${I('card')}</span>
      <span class="adm-main"><b>${esc(p.n)} · ${esc(p.sum)}</b><small>${esc(p.plan)} · ${esc(HQ_PAY_METHOD[tier]||'Карта')} · ${esc(p.when)}</small></span>
      <span class="adm-tag ${meta.cls}">${meta.l}</span>
      <span class="hq-pay-go">${I('chev')}</span>
    </div>`;
  }).join('');
  const waitBanner = waitN
    ? `<div class="hq-alert" onclick="hqPaySetFilter('wait')">${I('bolt')}<span><b>${waitN}</b> ${waitN===1?'платёж ждёт':'платежа ждут'} подтверждения — нажми, чтобы обработать</span>${I('chev')}</div>`
    : '';
  return `
    <div class="hq-part-sum card">
      <div class="hq-part-s"><b>$${okSum.toLocaleString('ru')}</b><small>проведено</small></div>
      <div class="hq-part-s"><b>$${avg}</b><small>средний чек</small></div>
      <div class="hq-part-s"><b>${refSum?'$'+refSum:'0'}</b><small>возвраты</small></div>
    </div>
    ${waitBanner}
    <div class="hq-chips">${chips}</div>
    <div class="adm-sec-h">Платежи · ${idx.length} из ${ADMIN.pay.length}</div>
    ${rows || '<p class="dim" style="font-size:13px;padding:6px 2px">Нет платежей по этому фильтру.</p>'}
    <div class="adm-acts"><button class="adm-btn" onclick="hqExportReport()">${I('file')} Выгрузить отчёт (.txt)</button></div>`;
};

/* ==================== 4d. ИИ-АГЕНТЫ: панель систем (вкл/выкл) + разбор эскалаций ==================== */
HQ_STATE.aiOff = HQ_STATE.aiOff || {};      /* выключенные системы (персист) */
HQ_STATE.escDone = HQ_STATE.escDone || {};  /* закрытые эскалации по индексу (персист) */
hqSave();
const HQ_AISYS = [
  {k:'support', n:'Поддержка',   ic:'chat',      c:'#9AFF00', load:81, m:'214 ответов/сут · ответ 41 сек'},
  {k:'assist',  n:'Ассистент',   ic:'bolt',      c:'#4aa0ff', load:44, m:'12 отчётов · сводки 09:00'},
  {k:'moder',   n:'Модератор',   ic:'shield',    c:'#a855f7', load:63, m:'68 автоблоков · очередь 0'},
  {k:'fraud',   n:'Антифрод',    ic:'lock',      c:'#ff7a3c', load:37, m:'1.2к проверок · 46 стопов'},
  {k:'sales',   n:'Продажи',     ic:'briefcase', c:'#22d3ee', load:52, m:'34 лида · дожим 12'},
  {k:'copy',    n:'Копирайтер',  ic:'edit',      c:'#facc15', load:29, m:'19 текстов · A/B на завтра'},
];
function hqAiOn(k){ return !HQ_STATE.aiOff[k]; }
function hqAiToggle(k){
  if(HQ_STATE.aiOff[k]) delete HQ_STATE.aiOff[k]; else HQ_STATE.aiOff[k] = 1;
  hqSave(); renderAdmin();
  toast(''+(HQ_AISYS.find(a=>a.k===k)||{}).n+': '+(hqAiOn(k)?'включён':'выключен'));
}
function hqEscResolve(i){ HQ_STATE.escDone[i] = 1; hqSave(); renderAdmin(); toast('Эскалация закрыта · пользователю отправлен ответ'); }
function hqAiCard(a){
  const on = hqAiOn(a.k);
  return `<div class="hq-ai ${on?'':'off'}">
    <div class="hq-ai-top">
      <span class="hq-ai-ic" style="color:${a.c};background:${a.c}1e">${I(a.ic)}</span>
      <span class="hq-ai-n"><b>${esc(a.n)}</b>
        <span class="hq-ai-st"><i class="hq-dot ${on?'work':'wait'}"></i>${on?'активен':'выключен'}</span></span>
      <span class="switch ${on?'on':''}" onclick="hqAiToggle('${a.k}')"><i></i></span>
    </div>
    <div class="hq-ai-m">${esc(a.m)}</div>
    <div class="hq-bar"><i style="width:${on?a.load:0}%;background:linear-gradient(90deg,${a.c},${a.c}99);transition:width .5s"></i></div>
  </div>`;
}
const _prevAdmAgentsHq = admAgents;
admAgents = function(){
  const onN = HQ_AISYS.filter(a=>hqAiOn(a.k)).length;
  const openEsc = ADMIN.agents.filter((a,i)=>a.esc && !HQ_STATE.escDone[i]).length;
  const board = `
    <div class="hq-part-sum card">
      <div class="hq-part-s"><b>${onN}/${HQ_AISYS.length}</b><small>систем активно</small></div>
      <div class="hq-part-s"><b>24/7</b><small>режим работы</small></div>
      <div class="hq-part-s"><b class="${openEsc?'warn':''}">${openEsc}</b><small>эскалаций</small></div>
    </div>
    <div class="adm-sec-h">Системы OKO · вкл/выкл</div>
    <div class="hq-ai-grid">${HQ_AISYS.map(hqAiCard).join('')}</div>`;
  const escRows = ADMIN.agents.map((a,i)=>{
    const done = !!HQ_STATE.escDone[i];
    const tag = a.esc
      ? (done ? '<span class="adm-tag ok">решено</span>' : '<span class="adm-tag no">эскалация</span>')
      : '<span class="adm-tag ok">отвечено</span>';
    const act = (a.esc && !done)
      ? `<div class="adm-acts"><button class="adm-btn pri" onclick="hqEscResolve(${i})">Закрыть эскалацию</button></div>`
      : '';
    return `<div class="adm-log hq-esc ${a.esc&&!done?'hot':''}">
      <div class="lt"><b>${esc(a.a)}</b><span>${esc(a.when)}</span></div>
      «${esc(a.q)}» ${tag}${act}
    </div>`;
  }).join('');
  return board + `<div class="adm-sec-h">Диалоги и эскалации${openEsc?' · '+openEsc+' в работе':''}</div>` + escRows;
};

/* ==================== 5. ПРОФИЛЬ: чип CEO + строка «Штаб OKO HQ» ==================== */
function hqOpenHqTab(){
  if(typeof isOwner === 'function' && !isOwner()){ hqPendingTab='hq'; hqShowGate(); return; }
  openAdmin(); admGo('hq');
}
function hqDecorateProfile(){
  const nameEl = document.getElementById('profName');
  if(nameEl){
    const wrap = nameEl.parentElement;
    let chip = document.getElementById('hqCeoChip');
    if(PROFILE.ceo){
      if(!chip && wrap){
        chip = document.createElement('div');
        chip.id = 'hqCeoChip'; chip.className = 'hq-ceo-chip';
        chip.innerHTML = `${I('crown')} ГЕНЕРАЛЬНЫЙ ДИРЕКТОР OKO`;
        wrap.appendChild(chip);
      }
    } else if(chip) chip.remove();
  }
  const anchor = document.getElementById('prowAdmin');
  let row = document.getElementById('hqProwHq');
  if(typeof isOwner === 'function' && isOwner()){
    if(!row && anchor){
      row = document.createElement('button');
      row.id = 'hqProwHq'; row.className = 'prow';
      row.innerHTML = `<svg class="i"><use href="#i-eye"/></svg> Штаб OKO HQ <span class="chip" style="font-size:9px;margin-left:auto">10 агентов</span>`;
      row.onclick = hqOpenHqTab;
      anchor.parentElement.insertBefore(row, anchor);
    }
  } else if(row) row.remove();
}
const _prevRenderMyProfileHq = renderMyProfile;
renderMyProfile = function(){
  _prevRenderMyProfileHq();
  hqDecorateProfile();
};

/* ==================== 7. ОБЗОР+: кризис-виджет / live-лента / heatmap / топ-фичи / воронка ====================
   Ставим ПЯТЬ дашборд-блоков уровня Stripe/Linear на вкладку «Обзор»,
   между KPI-рядом и панелью «Быстро». Ничего не удаляем, только вставляем. */

/* ---- 7.1 КРИЗИС-ВИДЖЕТ: диагностика по MRR/оттоку/возвратам ---- */
function hqCrisisSignal(){
  const now = Date.now(), W = 7*864e5;
  const rev = (typeof OKO_REVENUE!=='undefined') ? OKO_REVENUE : [];
  const sumBetween = (a,b)=>rev.reduce((s,r)=>(r.at>=a&&r.at<b)?s+r.sum:s, 0);
  const cur = sumBetween(now-W, now), prev = sumBetween(now-2*W, now-W);
  const dropPct = prev>0 ? Math.round((cur-prev)/prev*100) : 0;
  const churn = (typeof ADMIN!=='undefined' && ADMIN.kpi) ? +ADMIN.kpi.churn : 0;
  const refN = (typeof ADMIN!=='undefined' && ADMIN.pay) ? ADMIN.pay.filter((p,i)=>hqPaySt(i)==='refunded').length : 0;
  const waitN = (typeof ADMIN!=='undefined' && ADMIN.pay) ? ADMIN.pay.filter((p,i)=>hqPaySt(i)==='wait').length : 0;
  const items = [];
  if(dropPct <= -5) items.push({sev:'hi', txt:'выручка недели ниже на '+Math.abs(dropPct)+'% ('+fmtMoney(Math.round(cur))+' против '+fmtMoney(Math.round(prev))+')'});
  if(churn >= 2.0)  items.push({sev:'md', txt:'отток '+churn+'% выше нормы 1.5% — платные снимаются'});
  if(refN >= 1)     items.push({sev:'md', txt:refN+' возврат'+(refN===1?'':'а')+' за период — проверь причины отказов'});
  if(waitN >= 1)    items.push({sev:'lo', txt:waitN+' платёж'+(waitN===1?'':'а')+' висят в статусе «ожидает»'});
  if(!items.length) return null;
  const sev = items.some(x=>x.sev==='hi') ? 'hi' : (items.some(x=>x.sev==='md') ? 'md' : 'lo');
  const title = dropPct <= -5
    ? 'ALERT · MRR '+dropPct+'% за неделю'
    : (churn >= 2.0 ? 'ALERT · отток растёт' : 'Внимание · сигналы дашборда');
  return {sev, title, items, dropPct, churn, cur, prev, refN, waitN};
}
function hqCrisisBlock(){
  if(HQ_STATE.crisisHide) return '';
  const s = hqCrisisSignal(); if(!s) return '';
  const rows = s.items.map(x=>`<li><span class="hq-cr-b ${x.sev}"></span>${esc(x.txt)}</li>`).join('');
  return `<div class="hq-crisis card sev-${s.sev}">
    <div class="hq-crisis-h">
      <span class="hq-crisis-ic">${I('bolt')}</span>
      <b>${esc(s.title)}</b>
      <span class="hq-crisis-live"><i class="hq-dot work"></i>LIVE</span>
    </div>
    <ul class="hq-crisis-list">${rows}</ul>
    <div class="hq-crisis-acts">
      <button class="adm-btn pri" onclick="hqCrisisOpen()">Разобрать</button>
      <button class="adm-btn" onclick="hqCrisisDismiss()">Скрыть на сутки</button>
    </div>
  </div>`;
}
function hqCrisisDismiss(){
  HQ_STATE.crisisHide = Date.now() + 864e5; hqSave();
  renderAdmin(); toast('Плашка скрыта — вернётся через сутки');
}
(function hqCrisisAutoUnhide(){
  if(HQ_STATE.crisisHide && HQ_STATE.crisisHide < Date.now()){ delete HQ_STATE.crisisHide; hqSave(); }
})();
function hqCrisisOpen(){
  const s = hqCrisisSignal(); if(!s){ toast('Сигналов больше нет'); return; }
  const detail = s.items.map(x=>`<div class="hq-crisis-drow"><span class="hq-cr-b ${x.sev}"></span><span>${esc(x.txt)}</span></div>`).join('');
  const numRows = [
    ['Выручка · 7 дней', fmtMoney(Math.round(s.cur))],
    ['Выручка · предыдущие 7 дней', fmtMoney(Math.round(s.prev))],
    ['Отток', s.churn+'%'],
    ['Возвраты в очереди', s.refN],
    ['Платежи ждут', s.waitN],
  ].map(([l,v])=>`<div class="hq-pay-drow"><span>${l}</span><b>${v}</b></div>`).join('');
  showPopup({
    ico:'bolt', title:'Разбор сигналов',
    body:`<div class="hq-crisis-pop">
      <div class="hq-crisis-block">${detail}</div>
      <div class="hq-pay-det">${numRows}</div>
      <p class="hq-crisis-p">Следующие шаги: проверь платежи в статусе «ожидает» (вкладка Платежи), запусти акцию на удержание для оттока, сверь модерацию — часть возвратов идёт из-за отклонённых заявок.</p>
    </div>`,
    actions:[
      {label:'К платежам', onclick:()=>{ closePopup(); admGo('pay'); }},
      {label:'К модерации', ghost:true, onclick:()=>{ closePopup(); admGo('moder'); }},
      {label:'Закрыть', ghost:true},
    ]
  });
}

/* ---- 7.2 LIVE-ЛЕНТА СОБЫТИЙ: регистрации, оплаты, выводы, сделки (тик 5с) ---- */
const HQ_FEED_POOL = []  /* демо-данные удалены 09.08: 3D-штаб — источник только API */;
const HQ_FEED_LABEL = {reg:'РЕГИСТРАЦИЯ', pay:'ПЛАТЁЖ', out:'ВЫВОД', deal:'СДЕЛКА', churn:'ОТПИСКА', mod:'МОДЕРАЦИЯ', ref:'ПАРТНЁРКА'};
const HQ_FEED_NICKS = []  /* демо-данные удалены 09.08: 3D-штаб — источник только API */;
const HQ_FEED_NAMES = [];  /* демо-имена удалены 09.08 */
function hqFeedRandom(kind){
  /* Шаблоны событий штаба вычищены 09.08: лента наполняется реальными
     событиями. Без пула ничего не выдумываем. */
  const p = HQ_FEED_POOL.find(x=>x.k===kind) || HQ_FEED_POOL[0];
  if(!p || !Array.isArray(p.tpl) || !p.tpl.length) return '';
  const t = p.tpl[Math.floor(Math.random()*p.tpl.length)];
  return t
    .replace('{nick}', HQ_FEED_NICKS[Math.floor(Math.random()*HQ_FEED_NICKS.length)])
    .replace('{name}', HQ_FEED_NAMES[Math.floor(Math.random()*HQ_FEED_NAMES.length)])
    .replace('${a}',  '$'+([15,49,99,149,199,299][Math.floor(Math.random()*6)]))
    .replace('${b}',  ([120,240,340,590,890][Math.floor(Math.random()*5)]))
    .replace('{c}',   ([1200,1800,2500,3400,4900,8900][Math.floor(Math.random()*6)]).toLocaleString('ru'))
    .replace('{d}',   String(3+Math.floor(Math.random()*17)));
}
function hqFeedNew(kind){
  const k = kind || (['reg','pay','out','deal','pay','reg','mod','deal','churn','ref'][Math.floor(Math.random()*10)]);
  const p = HQ_FEED_POOL.find(x=>x.k===k) || HQ_FEED_POOL[0];
  return {t: hqHM(new Date()), k, ic:p.ic, c:p.c, m: hqFeedRandom(k)};
}
let HQ_FEED = [];
(function hqSeedFeed(){
  /* демо-сидер отключён 09.08 (правка Даниэля: ноль демо-данных) */
  return (arguments.length && Array.isArray(arguments[0])) ? [] : undefined;

  const order = ['pay','reg','deal','pay','mod','out','reg','deal','pay','ref','reg','mod','pay','out'];
  for(const k of order) HQ_FEED.push(hqFeedNew(k));
})();
function hqFeedLine(e, fresh){
  return `<div class="hq-fd${fresh?' new':''}">
    <span class="hq-fd-ic" style="color:${e.c};background:${e.c}1e">${I(e.ic)}</span>
    <span class="hq-fd-b">
      <span class="hq-fd-k" style="color:${e.c}">${HQ_FEED_LABEL[e.k]||e.k}</span>
      <span class="hq-fd-m">${esc(e.m)}</span>
    </span>
    <span class="hq-fd-t">${e.t}</span>
  </div>`;
}
function hqFeedBlock(){
  return `<div class="adm-sec-h">Лента событий · live <span class="hq-fd-pulse"><i class="hq-dot work"></i>обновление 5 сек</span></div>
    <div class="hq-fd-box card" id="hqFeedBox">${HQ_FEED.map(e=>hqFeedLine(e)).join('')}</div>`;
}
let hqFeedTimer = null;
function hqStartFeed(){ if(!hqFeedTimer) hqFeedTimer = setInterval(hqFeedTick, 5000); }
function hqStopFeed(){ if(hqFeedTimer){ clearInterval(hqFeedTimer); hqFeedTimer = null; } }
function hqFeedTick(){
  const av = document.getElementById('adminView');
  if(!av || !av.classList.contains('open') || admTab !== 'overview'){ hqStopFeed(); return; }
  const line = hqFeedNew();
  HQ_FEED.unshift(line); if(HQ_FEED.length > 18) HQ_FEED.length = 18;
  const box = document.getElementById('hqFeedBox'); if(!box) return;
  box.insertAdjacentHTML('afterbegin', hqFeedLine(line, true));
  while(box.children.length > 18) box.lastElementChild.remove();
}

/* ---- 7.3 HEATMAP DAU: 7×24, интенсивность = активность ---- */
const HQ_HEAT_DAYS = ['Пн','Вт','Ср','Чт','Пт','Сб','Вс'];
function hqHeatData(){
  /* стабильный псевдослучайный паттерн: утро/обед/вечер выше, ночь ниже, ВС/СБ иначе */
  const grid = [];
  for(let d=0; d<7; d++){
    const row = [];
    for(let h=0; h<24; h++){
      let base = 0;
      if(h>=9 && h<=13) base = 55;
      else if(h>=17 && h<=22) base = 78;
      else if(h>=14 && h<=16) base = 40;
      else if(h>=7 && h<=8) base = 25;
      else if(h>=23 || h<=5) base = 6;
      else base = 18;
      /* выходные: вечерний пик выше, будни днём выше */
      if(d>=5){ if(h>=18 && h<=23) base += 18; if(h>=9 && h<=13) base -= 12; }
      else   { if(h>=10 && h<=12) base += 10; if(h>=20 && h<=22) base += 6; }
      /* лёгкий детерминированный шум */
      const noise = ((d*31 + h*17 + 7) % 13) - 6;
      row.push(Math.max(0, Math.min(100, base + noise)));
    }
    grid.push(row);
  }
  return grid;
}
function hqHeatBlock(){
  const g = hqHeatData();
  const hours = Array.from({length:24}, (_,h)=>h);
  const head = '<div class="hq-hm-hrow"><span></span>' +
    hours.map(h=>`<span class="hq-hm-hh${(h%3===0)?' k':''}">${h%3===0?h:''}</span>`).join('') + '</div>';
  const rows = g.map((row,d)=>{
    const cells = row.map((v,h)=>{
      const cls = v>=80?'lv5':v>=60?'lv4':v>=40?'lv3':v>=20?'lv2':v>=5?'lv1':'lv0';
      return `<i class="${cls}" title="${HQ_HEAT_DAYS[d]} ${h}:00 · ${v}"></i>`;
    }).join('');
    return `<div class="hq-hm-row"><span class="hq-hm-d">${HQ_HEAT_DAYS[d]}</span>${cells}</div>`;
  }).join('');
  const peak = g.reduce((m,r,d)=>{ r.forEach((v,h)=>{ if(v>m.v) m={v,d,h}; }); return m; }, {v:0,d:0,h:0});
  return `<div class="adm-sec-h">Heatmap активности · 7 дней × 24 часа</div>
    <div class="hq-hm card">
      <div class="hq-hm-grid">${head}${rows}</div>
      <div class="hq-hm-foot">
        <span class="hq-hm-peak">Пик: <b>${HQ_HEAT_DAYS[peak.d]} ${peak.h}:00</b></span>
        <span class="hq-hm-leg"><em>меньше</em><i class="lv0"></i><i class="lv1"></i><i class="lv2"></i><i class="lv3"></i><i class="lv4"></i><i class="lv5"></i><em>больше</em></span>
      </div>
    </div>`;
}

/* ---- 7.4 ТОП-ФИЧИ ПО ИСПОЛЬЗОВАНИЮ ---- */
const HQ_FEATS = [
  {n:'Контент-завод',   ic:'rocket',    c:'#9AFF00', n24:342, dl:'+18%', up:1, sub:'запусков за 24 часа'},
  {n:'Reels-машина',    ic:'circle-play', c:'#4aa0ff', n24:217, dl:'+12%', up:1, sub:'сборок за сутки'},
  {n:'Академия OKO',    ic:'crown',     c:'#facc15', n24:189, dl:'+9%',  up:1, sub:'уроков открыто'},
  {n:'Биржа услуг',     ic:'briefcase', c:'#22d3ee', n24:94,  dl:'+7%',  up:1, sub:'сделок закрыто'},
  {n:'Штаб OKO HQ',     ic:'shield',    c:'#a855f7', n24:66,  dl:'−2%',  up:0, sub:'планёрок агентов'},
  {n:'Разбор конкурентов', ic:'search', c:'#ff6bad', n24:51,  dl:'+22%', up:1, sub:'отчётов в мозг'},
];
function hqFeatsBlock(){
  const max = Math.max(1, ...HQ_FEATS.map(f=>f.n24));
  const rows = HQ_FEATS.map((f,i)=>`
    <div class="hq-feat" style="animation-delay:${i*0.045}s">
      <span class="hq-feat-ic" style="color:${f.c};background:${f.c}1e">${I(f.ic)}</span>
      <span class="hq-feat-b">
        <span class="hq-feat-top"><b>${esc(f.n)}</b><span class="hq-feat-n">${f.n24}<em>${esc(f.sub)}</em></span></span>
        <span class="hq-bar"><i style="width:${max ? Math.round(f.n24/max*100) : 0}%;background:linear-gradient(90deg,${f.c},${f.c}99)"></i></span>
      </span>
      <span class="hq-feat-dl ${f.up?'up':'dn'}">${esc(f.dl)}</span>
    </div>`).join('');
  return `<div class="adm-sec-h">Топ фич · использование за 24 часа</div>
    <div class="hq-feats card">${rows}</div>`;
}

/* ---- 7.5 ФИНАНСОВАЯ ВОРОНКА: визитор → рег → START → PRO → BUSINESS ---- */
const HQ_FUNNEL = [
  {n:'Визиторы',    v:12840, c:'#4aa0ff', sub:'уник. за 30 дней'},
  {n:'Регистрации', v:1284,  c:'#22d3ee', sub:'аккаунтов создано'},
  {n:'START',       v:214,   c:'#facc15', sub:'активировали базовый'},
  {n:'PRO',         v:78,    c:'#9AFF00', sub:'переход на средний'},
  {n:'BUSINESS',    v:12,    c:'#ff7a3c', sub:'верхний тариф'},
];
function hqFunnelBlock(){
  const top = HQ_FUNNEL[0].v;
  const rows = HQ_FUNNEL.map((s,i)=>{
    const pct = Math.round(s.v/top*100);
    const conv = i===0 ? 100 : Math.round(s.v/HQ_FUNNEL[i-1].v*100*10)/10;
    const dropTxt = i===0 ? '' : ' · '+conv+'% из «'+HQ_FUNNEL[i-1].n+'»';
    return `<div class="hq-fn-row" style="animation-delay:${i*0.06}s">
      <div class="hq-fn-l"><b>${esc(s.n)}</b><small>${esc(s.sub)}${dropTxt}</small></div>
      <div class="hq-fn-bar"><i style="width:${pct}%;background:linear-gradient(90deg,${s.c},${s.c}99)"><em>${s.v.toLocaleString('ru')}</em></i></div>
    </div>`;
  }).join('');
  const overall = Math.round(HQ_FUNNEL[HQ_FUNNEL.length-1].v/HQ_FUNNEL[0].v*10000)/100;
  return `<div class="adm-sec-h">Финансовая воронка · визитор до BUSINESS</div>
    <div class="hq-fn card">
      ${rows}
      <div class="hq-fn-foot">Сквозная конверсия: <b>${overall}%</b> визитор в верхний тариф</div>
    </div>`;
}

/* ---- 7.6 патчим admOverview: вставляем 5 блоков после KPI-ряда, перед «Быстро» ---- */
const _prevAdmOverviewHq2 = admOverview;
admOverview = function(){
  const html = _prevAdmOverviewHq2();
  const insert = hqCrisisBlock() + hqHeatBlock() + hqFunnelBlock() + hqFeatsBlock() + hqFeedBlock();
  /* вставляем перед секцией «Быстро · действия владельца» — держит структуру предсказуемой */
  const marker = '<div class="adm-sec-h">Быстро · действия владельца</div>';
  if(html.indexOf(marker) >= 0) return html.replace(marker, insert + marker);
  return html + insert;
};

/* ---- 7.7 стартуем ленту на overview, останавливаем при уходе ---- */
const _prevRenderAdminHq2 = renderAdmin;
renderAdmin = function(){
  _prevRenderAdminHq2();
  if(admTab === 'overview') hqStartFeed(); else hqStopFeed();
};
const _prevCloseAdminHq2 = closeAdmin;
closeAdmin = function(){ hqStopFeed(); _prevCloseAdminHq2(); };

/* ============================================================================
   8. КАБИНЕТ УРОВНЯ STRIPE / AMPLITUDE / NOTION
   - real-time KPI (тик 15 сек): MRR/ARR, DAU/MAU/WAU, retention 7/30/90, AOV/LTV/CAC/ROI
   - donut активных подписок по тарифам
   - cohort-таблица (недели × retention)
   - A/B тесты с победителем
   - live-heatmap кликов на мини-скриншоте главного экрана
   - product analytics: наименее используемые фичи
   - CRM партнёров (топ-10) — как новая вкладка
   - Финансы (доходы / расходы / прогноз кассы) — как новая вкладка
   - Роли команды (Дир / Финансы / Контент / Партнёры / Поддержка)
   - Быстрые действия: triage / mailing PRO / бан / возврат
   - Экспорт .csv и .pdf (через print-view)
============================================================================ */

/* ---------- 8.0 состояние: роль, seed real-time метрик ---------- */
HQ_STATE.role = HQ_STATE.role || 'director';
HQ_STATE.abSeen = HQ_STATE.abSeen || {};
hqSave();

const HQ_ROLES = [
  {k:'director', n:'Директор',   ic:'crown',     see:['kpi','donut','cohort','funnel','ab','click','feat','least','risks','fin','crm','feed','heat','quick']},
  {k:'finance',  n:'Финансы',    ic:'money',     see:['kpi','donut','fin','risks','quick']},
  {k:'content',  n:'Контент',    ic:'edit',      see:['click','feat','least','heat','feed','ab']},
  {k:'partners', n:'Партнёры',   ic:'briefcase', see:['crm','fin','feed','quick']},
  {k:'support',  n:'Поддержка',  ic:'chat',      see:['risks','feed','quick']},
];
function hqRole(){ return HQ_ROLES.find(r=>r.k===HQ_STATE.role) || HQ_ROLES[0]; }
function hqRoleSees(k){ return hqRole().see.indexOf(k) >= 0; }
function hqRoleSet(k){ HQ_STATE.role = k; hqSave(); renderAdmin(); toast('Роль: '+hqRole().n+' · дашборд адаптирован'); }

/* ---------- 8.1 real-time метрики (свежие числа каждые 15 сек) ---------- */
const HQ_TIERS = [
  {k:'BUSINESS', n:'BUSINESS', price:199, count:12, c:'#ff7a3c'},
  {k:'PRO',      n:'PRO',      price:49,  count:78, c:'#9AFF00'},
  {k:'START',    n:'START',    price:15,  count:214,c:'#facc15'},
  {k:'FREE',     n:'FREE',     price:0,   count:980,c:'#4aa0ff'},
];
const HQ_MET = {
  dau:412, wau:1108, mau:2984, retention:{d7:62.4, d30:38.1, d90:21.7},
  aov:41, ltv:186, cac:52, roas:3.6, /* ROI = (LTV-CAC)/CAC */
  errs:{srv:'12/1k', p95:'214ms'},
  updated: Date.now(),
};
function hqMrr(){ return HQ_TIERS.reduce((s,t)=>s+t.price*t.count, 0); }
function hqRoi(){ return Math.round((HQ_MET.ltv-HQ_MET.cac)/HQ_MET.cac*100); }
function hqMetTick(){
  /* мягкие колебания реалистичного диапазона */
  HQ_MET.dau = Math.max(340, Math.min(520, HQ_MET.dau + Math.round((Math.random()-0.5)*10)));
  HQ_MET.wau = Math.max(920, Math.min(1320, HQ_MET.wau + Math.round((Math.random()-0.5)*14)));
  HQ_MET.mau = Math.max(2600,Math.min(3400, HQ_MET.mau + Math.round((Math.random()-0.5)*8)));
  HQ_MET.retention.d7  = +(HQ_MET.retention.d7  + (Math.random()-0.5)*0.4).toFixed(1);
  HQ_MET.retention.d30 = +(HQ_MET.retention.d30 + (Math.random()-0.5)*0.3).toFixed(1);
  HQ_MET.retention.d90 = +(HQ_MET.retention.d90 + (Math.random()-0.5)*0.2).toFixed(1);
  HQ_MET.aov = Math.max(28, Math.min(64, HQ_MET.aov + Math.round((Math.random()-0.5)*3)));
  HQ_MET.ltv = Math.max(140, Math.min(240, HQ_MET.ltv + Math.round((Math.random()-0.5)*4)));
  HQ_MET.cac = Math.max(38,  Math.min(74,  HQ_MET.cac + Math.round((Math.random()-0.5)*3)));
  HQ_MET.updated = Date.now();
}
function hqMetHM(){ const d=new Date(HQ_MET.updated); return String(d.getHours()).padStart(2,'0')+':'+String(d.getMinutes()).padStart(2,'0')+':'+String(d.getSeconds()).padStart(2,'0'); }
function hqMetTile(v, l, sub, cls){
  return `<div class="hq-met ${cls||''}"><b>${v}</b><small>${esc(l)}</small>${sub?`<em>${sub}</em>`:''}</div>`;
}
function hqRtBlock(){
  const mrr = hqMrr(), arr = mrr*12, roi = hqRoi();
  const R = HQ_MET.retention;
  const dauPct = Math.round(HQ_MET.dau/HQ_MET.mau*100);
  return `<div class="adm-sec-h">Real-time метрики <span class="hq-rt-live"><i class="hq-dot work"></i>тик 15 сек · <span id="hqRtStamp">${hqMetHM()}</span></span></div>
    <div class="hq-met-grid card" id="hqMetGrid">
      ${hqMetTile('$'+mrr.toLocaleString('ru'), 'MRR', '<i class="up">+9%</i> к неделе', 'lime')}
      ${hqMetTile('$'+arr.toLocaleString('ru'), 'ARR', 'прогноз года', 'lime')}
      ${hqMetTile(HQ_MET.dau.toLocaleString('ru'), 'DAU', dauPct+'% от MAU', '')}
      ${hqMetTile(HQ_MET.wau.toLocaleString('ru'), 'WAU', '', '')}
      ${hqMetTile(HQ_MET.mau.toLocaleString('ru'), 'MAU', '30 дней', '')}
      ${hqMetTile(R.d7+'%',  'Retention 7d',  '', R.d7>=60?'lime':(R.d7>=45?'':'red'))}
      ${hqMetTile(R.d30+'%', 'Retention 30d', '', R.d30>=35?'lime':(R.d30>=25?'':'red'))}
      ${hqMetTile(R.d90+'%', 'Retention 90d', '', R.d90>=20?'lime':(R.d90>=12?'':'red'))}
      ${hqMetTile('$'+HQ_MET.aov, 'Средний чек', 'AOV', '')}
      ${hqMetTile('$'+HQ_MET.ltv, 'LTV', 'на клиента', 'lime')}
      ${hqMetTile('$'+HQ_MET.cac, 'CAC', 'привлечение', 'amber')}
      ${hqMetTile(roi+'%', 'ROI', 'LTV / CAC', roi>=150?'lime':(roi>=80?'':'red'))}
    </div>`;
}
/* только те tile-ноды, которые в разметке — пересобираем внутренности без re-render всей вкладки */
function hqMetLiveRefresh(){
  const box = document.getElementById('hqMetGrid'); if(!box) return;
  box.outerHTML = hqRtBlock().replace(/^[\s\S]*?<div class=\"adm-sec-h\">[\s\S]*?<\/div>\s*/, '');
  const st = document.getElementById('hqRtStamp'); if(st) st.textContent = hqMetHM();
}

/* ---------- 8.2 donut активных подписок по тарифам ---------- */
function hqDonutBlock(){
  const paid = HQ_TIERS.filter(t=>t.price>0);
  const tot = paid.reduce((s,t)=>s+t.count,0);
  const C = 2 * Math.PI * 42; /* окружность радиуса 42 */
  let acc = 0;
  const segs = paid.map((t,i)=>{
    const frac = t.count / tot;
    const len = frac * C, gap = C - len, off = -acc * C;
    acc += frac;
    return `<circle cx="60" cy="60" r="42" fill="none" stroke="${t.c}" stroke-width="16"
      stroke-dasharray="${len.toFixed(2)} ${gap.toFixed(2)}" stroke-dashoffset="${off.toFixed(2)}" transform="rotate(-90 60 60)"
      style="animation:hqDonutIn .9s cubic-bezier(.2,.85,.3,1) ${i*.08}s both"></circle>`;
  }).join('');
  const legend = paid.map(t=>{
    const pct = Math.round(t.count/tot*100);
    return `<div class="hq-dn-l"><i style="background:${t.c}"></i>
      <b>${esc(t.n)}</b><span>${t.count} · ${pct}%</span></div>`;
  }).join('');
  return `<div class="adm-sec-h">Активные подписки по тарифам</div>
    <div class="hq-donut card">
      <svg class="hq-dn-svg" viewBox="0 0 120 120" aria-hidden="true">
        <circle cx="60" cy="60" r="42" fill="none" stroke="var(--raised)" stroke-width="16"/>
        ${segs}
        <text x="60" y="58" text-anchor="middle" class="hq-dn-num">${tot}</text>
        <text x="60" y="72" text-anchor="middle" class="hq-dn-sub">платных</text>
      </svg>
      <div class="hq-dn-legend">${legend}</div>
    </div>`;
}

/* ---------- 8.3 cohort-таблица: недели × retention (5×5) ---------- */
const HQ_COHORT = (function(){
  /* 6 недель когорт × колонки W0..W5 (в % удержания) */
  const now = new Date();
  const rows = [];
  for(let w=5; w>=0; w--){
    const dt = new Date(now.getTime() - w*7*864e5);
    const label = 'W-'+w+' · '+String(dt.getDate()).padStart(2,'0')+'.'+String(dt.getMonth()+1).padStart(2,'0');
    const size = 60 + Math.round(Math.random()*140);
    const arr = [100];
    let cur = 100;
    for(let k=1; k<=5; k++){
      if(k > (5-w)) { arr.push(null); continue; } /* будущее — прочерк */
      const drop = k===1 ? 26+Math.random()*14 : 6+Math.random()*8;
      cur = Math.max(0, cur - drop);
      arr.push(+cur.toFixed(1));
    }
    rows.push({label, size, arr});
  }
  return rows;
})();
function hqCohortBlock(){
  const head = '<div class="hq-ch-row hd"><span class="hq-ch-lbl">Когорта</span><span class="hq-ch-size">N</span>'
    + [0,1,2,3,4,5].map(k=>`<span class="hq-ch-c">W${k}</span>`).join('') + '</div>';
  const rows = HQ_COHORT.map((r,i)=>{
    const cells = r.arr.map(v=>{
      if(v==null) return `<span class="hq-ch-c em">—</span>`;
      const lv = v>=80?'lv5':v>=60?'lv4':v>=40?'lv3':v>=20?'lv2':v>=1?'lv1':'lv0';
      return `<span class="hq-ch-c ${lv}"><em>${Math.round(v)}</em></span>`;
    }).join('');
    return `<div class="hq-ch-row" style="animation-delay:${i*0.04}s">
      <span class="hq-ch-lbl">${esc(r.label)}</span>
      <span class="hq-ch-size">${r.size}</span>${cells}</div>`;
  }).join('');
  return `<div class="adm-sec-h">Когорты · удержание по неделям</div>
    <div class="hq-cohort card">
      <div class="hq-ch-scroll">${head}${rows}</div>
      <div class="hq-hm-foot">
        <span class="hq-hm-peak">6 недель · когорты по неделе регистрации</span>
        <span class="hq-hm-leg"><em>0%</em><i class="lv0"></i><i class="lv1"></i><i class="lv2"></i><i class="lv3"></i><i class="lv4"></i><i class="lv5"></i><em>100%</em></span>
      </div>
    </div>`;
}

/* ---------- 8.4 A/B тесты ---------- */
const HQ_AB = [
  {k:'ab1', n:'Заголовок тарифа PRO',  variants:['A · «Ускоряй контент»','B · «Собери завод за час»'], split:'50/50', sample:2411, winner:'B', uplift:12.4, metric:'CTR карточки тарифа', st:'run'},
  {k:'ab2', n:'Кнопка «Начать бесплатно» vs «Попробовать 7 дней»', variants:['A','B'], split:'50/50', sample:1890, winner:'A', uplift:4.8, metric:'клик по CTA', st:'done'},
  {k:'ab3', n:'Онбординг: 3 шага vs 5 шагов', variants:['3 шага','5 шагов'], split:'70/30', sample:940, winner:'3 шага', uplift:9.1, metric:'доход до 1-го урока', st:'run'},
  {k:'ab4', n:'Автоплей приветствия', variants:['вкл','выкл'], split:'50/50', sample:612, winner:null, uplift:0, metric:'глубина сессии', st:'wait'},
];
function hqAbBlock(){
  const rows = HQ_AB.map((t,i)=>{
    const stCls = t.st==='run'?'wait':(t.st==='done'?'ok':'');
    const stL = t.st==='run'?'идёт':(t.st==='done'?'завершён':'сбор данных');
    const win = t.winner ? `<div class="hq-ab-win"><span>Победитель</span><b>${esc(t.winner)}</b><em class="up">+${t.uplift}%</em></div>` : `<div class="hq-ab-win dim"><span>Победитель</span><b>—</b><em>ждём выборку</em></div>`;
    const vars = t.variants.map((v,vi)=>`<div class="hq-ab-v ${t.winner && (v.charAt(0)===t.winner.charAt(0) || v===t.winner)?'on':''}"><i>${String.fromCharCode(65+vi)}</i>${esc(v)}</div>`).join('');
    return `<div class="hq-ab card" style="animation-delay:${i*0.05}s">
      <div class="hq-ab-h">
        <b>${esc(t.n)}</b>
        <span class="adm-tag ${stCls}">${stL}</span>
      </div>
      <div class="hq-ab-vars">${vars}</div>
      <div class="hq-ab-meta">
        <span>метрика: <b>${esc(t.metric)}</b></span>
        <span>сплит: <b>${esc(t.split)}</b></span>
        <span>выборка: <b>${t.sample.toLocaleString('ru')}</b></span>
      </div>
      ${win}
    </div>`;
  }).join('');
  return `<div class="adm-sec-h">A/B тесты · ${HQ_AB.filter(x=>x.st==='run').length} активных</div>${rows}`;
}

/* ---------- 8.5 heatmap кликов: мини-макет главного + плотность точек ---------- */
/* координаты в системе 0..100 (в SVG viewBox 0 0 100 178 — вертикальный мокап) */
const HQ_CLICKS = [
  /* нижняя таб-панель — гарячий клик */
  {x:18, y:168, w:1.0}, {x:38, y:168, w:0.9}, {x:58, y:168, w:1.0},
  {x:78, y:168, w:0.6}, {x:82, y:168, w:0.9},
  /* карточки ленты в центре */
  {x:50, y:60, w:0.85}, {x:50, y:82, w:0.7}, {x:50, y:104, w:0.55},
  /* иконки в шапке */
  {x:12, y:14, w:0.35}, {x:88, y:14, w:0.4},
  /* CTA сторис */
  {x:22, y:32, w:0.5}, {x:38, y:32, w:0.35}, {x:54, y:32, w:0.3}, {x:70, y:32, w:0.25},
  /* второй клик по «Кошельку» */
  {x:78, y:168, w:0.6},
];
function hqClickBlock(){
  /* плотность: рендерим blur-градиенты через SVG feGaussianBlur */
  const pts = HQ_CLICKS.map(p=>`<circle cx="${p.x}" cy="${p.y}" r="${8+p.w*8}" fill="url(#hqCg)" opacity="${0.35+p.w*0.5}"/>`).join('');
  const top = [...HQ_CLICKS].sort((a,b)=>b.w-a.w).slice(0,4).map((p,i)=>{
    const name = p.y>150?'таб-навигация':(p.y<20?'шапка приложения':(p.y<40?'ряд сторис':'карточки ленты'));
    return `<div class="hq-clk-l"><b>${i+1}</b><span>${name}</span><em>${Math.round(p.w*100)}%</em></div>`;
  }).join('');
  /* мини-макет главного экрана: шапка, сторис, лента, таб-бар */
  return `<div class="adm-sec-h">Heatmap кликов · главный экран <span class="hq-fd-pulse"><i class="hq-dot work"></i>последние 24 часа</span></div>
    <div class="hq-clk card">
      <div class="hq-clk-mock">
        <svg viewBox="0 0 100 178" preserveAspectRatio="xMidYMid meet" class="hq-clk-svg">
          <defs>
            <radialGradient id="hqCg" cx="50%" cy="50%" r="50%">
              <stop offset="0%"  stop-color="#ff3838" stop-opacity=".95"/>
              <stop offset="45%" stop-color="#ffb400" stop-opacity=".6"/>
              <stop offset="100%" stop-color="#9AFF00" stop-opacity="0"/>
            </radialGradient>
          </defs>
          <!-- шапка -->
          <rect x="0" y="0" width="100" height="22" fill="var(--raised)"/>
          <circle cx="12" cy="11" r="4" fill="var(--border)"/>
          <rect x="30" y="7" width="34" height="8" rx="2" fill="var(--border)"/>
          <circle cx="82" cy="11" r="4" fill="var(--border)"/>
          <circle cx="92" cy="11" r="4" fill="var(--border)"/>
          <!-- сторис -->
          <g fill="var(--border)"><circle cx="12" cy="32" r="6"/><circle cx="28" cy="32" r="6"/><circle cx="44" cy="32" r="6"/><circle cx="60" cy="32" r="6"/><circle cx="76" cy="32" r="6"/><circle cx="92" cy="32" r="6"/></g>
          <!-- лента -->
          <rect x="6" y="46" width="88" height="28" rx="3" fill="var(--raised)"/>
          <rect x="6" y="78" width="88" height="28" rx="3" fill="var(--raised)"/>
          <rect x="6" y="110" width="88" height="28" rx="3" fill="var(--raised)"/>
          <!-- таб-бар -->
          <rect x="0" y="156" width="100" height="22" fill="var(--raised)"/>
          <g fill="var(--border)">
            <circle cx="18" cy="167" r="3.5"/><circle cx="38" cy="167" r="3.5"/><circle cx="58" cy="167" r="3.5"/><circle cx="78" cy="167" r="3.5"/><circle cx="93" cy="167" r="3.5"/>
          </g>
          <!-- клики -->
          <g style="mix-blend-mode:screen">${pts}</g>
        </svg>
      </div>
      <div class="hq-clk-side">
        <div class="hq-clk-t">Топ зон по кликам</div>
        ${top}
        <div class="hq-clk-note">${I('bolt')}<span>Плотность = сумма кликов пользователей. Красное — hotspot, зелёное — низкая активность.</span></div>
      </div>
    </div>`;
}

/* ---------- 8.6 product analytics: наименее используемые ---------- */
const HQ_LEAST = [
  {n:'Кабинет рекламы', ic:'megaphone', c:'#4aa0ff', n7:6,   pct:0.5,  hint:'сложный вход — обучающий тур в 1 шаг'},
  {n:'Календарь публикаций', ic:'clock', c:'#facc15', n7:4,  pct:0.3,  hint:'спрятан в подменю Reels — вынести в главный'},
  {n:'Экспорт статистики', ic:'file',   c:'#ff6bad',  n7:2,  pct:0.15, hint:'не нужен пока PRO — оставить только BUSINESS'},
  {n:'Резервные копии',    ic:'shield', c:'#a855f7',  n7:1,  pct:0.08, hint:'кандидат на удаление — 0.08% MAU'},
];
function hqLeastBlock(){
  const rows = HQ_LEAST.map((f,i)=>`
    <div class="hq-feat" style="animation-delay:${i*0.045}s">
      <span class="hq-feat-ic" style="color:${f.c};background:${f.c}1e">${I(f.ic)}</span>
      <span class="hq-feat-b">
        <span class="hq-feat-top"><b>${esc(f.n)}</b><span class="hq-feat-n">${f.n7}<em>откр. за 7 дн</em></span></span>
        <span class="hq-least-hint">${esc(f.hint)}</span>
      </span>
      <span class="hq-feat-dl dn">${f.pct}%</span>
    </div>`).join('');
  return `<div class="adm-sec-h">Наименее используемые · кандидаты на упрощение</div>
    <div class="hq-feats card">${rows}</div>`;
}

/* ---------- 8.7 risks: сводная плашка с типизацией ---------- */
function hqRisksBlock(){
  const partWait = ADMIN.partners.filter(p=>!hqPartPaid(p));
  const partSum = partWait.reduce((s,p)=>s+hqPartEarn(p),0);
  const s = hqCrisisSignal();
  const riskList = [];
  if(partSum>0) riskList.push({sev:'md', ic:'briefcase', txt:'Партнёры просят выплату: <b>$'+partSum+'</b> ('+partWait.length+' чел.)'});
  if(s && s.dropPct <= -5) riskList.push({sev:'hi', ic:'bolt', txt:'MRR упал <b>'+s.dropPct+'%</b> за неделю ('+fmtMoney(Math.round(s.cur))+' против '+fmtMoney(Math.round(s.prev))+')'});
  if(HQ_MET.retention.d30 < 30) riskList.push({sev:'md', ic:'flag', txt:'Retention 30d ниже целевого 30%: <b>'+HQ_MET.retention.d30+'%</b>'});
  const errs = 42; riskList.push({sev:'md', ic:'shield', txt:'Server errors <b>+'+errs+'%</b> за час · p95 '+HQ_MET.errs.p95});
  const openMod = (typeof ADMIN!=='undefined' && ADMIN.moder) ? ADMIN.moder.length : 0;
  if(openMod>0) riskList.push({sev:'lo', ic:'edit', txt:openMod+' задач в очереди модерации'});
  if(!riskList.length) return '';
  const rows = riskList.map(r=>`
    <div class="hq-risk-row sev-${r.sev}">
      <span class="hq-risk-ic">${I(r.ic)}</span>
      <span class="hq-risk-m">${r.txt}</span>
    </div>`).join('');
  return `<div class="adm-sec-h">Уведомления о рисках · ${riskList.length}</div>
    <div class="hq-risks card">${rows}</div>`;
}

/* ---------- 8.8 быстрые действия: расширенный набор ---------- */
function hqQuickPro(){
  const proN = HQ_TIERS.find(t=>t.k==='PRO').count;
  showPopup({ico:'megaphone', title:'Рассылка всем PRO',
    body:`<div class="hq-mail"><textarea id="hqMailBody" rows="5" placeholder="Текст письма для ${proN} подписчиков PRO">Привет! Мы обновили Reels-машину и добавили инфографику. Загляни — новая вкладка «Библиотека переходов» уже доступна.</textarea><small>Пойдёт по всем ${proN} активным PRO. Push + e-mail + внутренняя лента.</small><button class="btn" style="margin-top:10px;width:100%" onclick="hqMailDoSend()">Отправить всем PRO</button></div>`,
    actions:[{label:'Отмена', ghost:true}]});
}
function hqMailDoSend(){
  const t = (document.getElementById('hqMailBody')||{}).value || '';
  const proN = HQ_TIERS.find(x=>x.k==='PRO').count;
  if(!t.trim()){ toast('Введи текст письма'); return; }
  closePopup();
  toast('Рассылка запущена: '+proN+' PRO · push + e-mail · '+t.length+' симв.');
}
function hqQuickTriage(){
  const q = (typeof ADMIN!=='undefined' && ADMIN.agents) ? ADMIN.agents.filter((a,i)=>a.esc && !HQ_STATE.escDone[i]).length : 0;
  if(!q){ toast('Очередь triage пуста — все ответы отправлены'); return; }
  showPopup({ico:'bolt', title:'Ответить всем на triage', body:`Сегодня в очереди <b>${q}</b> эскалаций. Отправим шаблонный ответ с приветствием и обещанием разобрать в течение часа?`,
    actions:[
      {label:'Ответить всем', onclick:()=>{ ADMIN.agents.forEach((a,i)=>{ if(a.esc) HQ_STATE.escDone[i]=1; }); hqSave(); renderAdmin(); toast('Ответили на '+q+' эскалаций · очередь очищена'); }},
      {label:'Отмена', ghost:true}
    ]});
}
function hqQuickBan(){
  showPopup({ico:'lock', title:'Забанить пользователя',
    body:`<div class="hq-mail"><input id="hqBanNick" placeholder="@ник или имя" onkeydown="if(event.key==='Enter')hqBanDoRun()" style="width:100%;background:var(--raised);border:1px solid var(--border);border-radius:var(--r-sm);padding:12px 14px;color:var(--text);font-size:14px;outline:none"><small>Полный список — во вкладке «Пользователи». Здесь бан по нику одной кнопкой.</small><button class="btn" style="margin-top:10px;width:100%" onclick="hqBanDoRun()">Забанить</button></div>`,
    actions:[{label:'Отмена', ghost:true}]});
  setTimeout(()=>{ const e=document.getElementById('hqBanNick'); if(e) e.focus(); }, 60);
}
function hqBanDoRun(){
  const v = (document.getElementById('hqBanNick')||{}).value || '';
  if(!v.trim()){ toast('Введи @ник или имя'); return; }
  const q = v.replace('@','').toLowerCase();
  const idx = ADMIN.users.findIndex(u=> (u.n+' '+u.h).toLowerCase().indexOf(q)>=0);
  if(idx<0){ toast('Пользователь «'+v+'» не найден в базе'); return; }
  closePopup();
  hqUserBan(idx);
}
function hqQuickRefund(){
  const waits = ADMIN.pay.map((p,i)=>({p,i,st:hqPaySt(i)})).filter(x=>x.st==='ok');
  if(!waits.length){ toast('Нет проведённых платежей для возврата'); return; }
  const rows = waits.slice(0,6).map(x=>`<button class="adm-btn" style="margin-bottom:6px;width:100%;text-align:left;justify-content:flex-start" onclick="hqPayRefund(${x.i});closePopup()">${I('card')} ${esc(x.p.n)} · ${esc(x.p.sum)} · ${esc(x.p.plan)}</button>`).join('');
  showPopup({ico:'card', title:'Возврат средств', body:`<div class="hq-mail">Выбери платёж для возврата:<div style="margin-top:8px">${rows}</div></div>`,
    actions:[{label:'Закрыть', ghost:true}]});
}
function hqQuickBlock(){
  return `<div class="adm-sec-h">Быстрые действия</div>
    <div class="hq-quick hq-quick-6">
      <button class="hq-qbtn" onclick="hqQuickTriage()"><span class="hq-qic">${I('bolt')}</span><b>Triage</b><small>ответить всем</small></button>
      <button class="hq-qbtn" onclick="hqQuickPro()"><span class="hq-qic">${I('megaphone')}</span><b>Письмо PRO</b><small>рассылка всем</small></button>
      <button class="hq-qbtn dng" onclick="hqQuickBan()"><span class="hq-qic">${I('lock')}</span><b>Бан юзера</b><small>по @нику</small></button>
      <button class="hq-qbtn" onclick="hqQuickRefund()"><span class="hq-qic">${I('card')}</span><b>Возврат</b><small>выбрать платёж</small></button>
      <button class="hq-qbtn" onclick="hqQuickMoney()"><span class="hq-qic">${I('money')}</span><b>+10 000 ₽</b><small>тест на кошелёк</small></button>
      <button class="hq-qbtn" onclick="hqExportMenu()"><span class="hq-qic">${I('file')}</span><b>Экспорт</b><small>CSV / PDF / TXT</small></button>
    </div>`;
}

/* ---------- 8.9 Роль: селектор + скрытие блоков ---------- */
function hqRoleBar(){
  const chips = HQ_ROLES.map(r=>`<button class="hq-chip ${HQ_STATE.role===r.k?'on':''}" onclick="hqRoleSet('${r.k}')" title="${esc(r.n)}"><span class="hq-role-ic">${I(r.ic)}</span>${esc(r.n)}</button>`).join('');
  return `<div class="adm-sec-h">Роль просмотра</div><div class="hq-chips hq-role-chips">${chips}</div>`;
}

/* ---------- 8.10 admOverview: собираем всё вместе ---------- */
const _prevAdmOverviewHqX = admOverview;
admOverview = function(){
  const html = _prevAdmOverviewHqX();
  /* убираем старую панель «Быстро · действия владельца» — заменим расширенной */
  const legacy = html
    .replace(/<div class="adm-sec-h">Быстро · действия владельца<\/div>[\s\S]*?<\/div>\s*/,'')
    .replace(/<div class="adm-sec-h">Быстрые действия<\/div>[\s\S]*?<\/div>\s*/,'');

  const R = hqRoleSees;
  const parts = [hqRoleBar()];
  if(R('risks')) parts.push(hqRisksBlock());
  if(R('kpi'))   parts.push(hqRtBlock());
  if(R('donut')) parts.push(hqDonutBlock());
  if(R('cohort'))parts.push(hqCohortBlock());
  if(R('click')) parts.push(hqClickBlock());
  if(R('ab'))    parts.push(hqAbBlock());
  if(R('least')) parts.push(hqLeastBlock());
  if(R('quick')) parts.push(hqQuickBlock());
  return legacy + parts.join('');
};

/* ---------- 8.11 real-time тикер обзора: 15 сек ---------- */
let hqRtTimer = null;
function hqStartRt(){ if(!hqRtTimer) hqRtTimer = setInterval(hqRtTick, 15000); }
function hqStopRt(){ if(hqRtTimer){ clearInterval(hqRtTimer); hqRtTimer = null; } }
function hqRtTick(){
  const av = document.getElementById('adminView');
  if(!av || !av.classList.contains('open') || admTab !== 'overview'){ hqStopRt(); return; }
  hqMetTick(); hqMetLiveRefresh();
}

/* ---------- 8.12 новые вкладки: Финансы / CRM ---------- */
ADMIN_TABS.push({k:'finance', t:'Финансы'});
ADMIN_TABS.push({k:'crm',     t:'CRM'});

/* ФИНАНСЫ ------------------------------------------------------------------ */
const HQ_FIN_INCOME = [
  {k:'Lava.top карта', v:184000, c:'#9AFF00'},
  {k:'Криптовалюта',    v:56000,  c:'#4aa0ff'},
  {k:'ЮKassa · СБП',    v:42000,  c:'#facc15'},
  {k:'Партнёрские',     v:18000,  c:'#a855f7'},
  {k:'Международные',   v:12000,  c:'#ff6bad'},
];
const HQ_FIN_EXPENSE = [
  {k:'Реклама',      v:82000, c:'#ff7a3c'},
  {k:'Инфраструктура',v:34000, c:'#4aa0ff'},
  {k:'Зарплаты',     v:120000,c:'#a855f7'},
  {k:'Сервисы (SaaS)',v:18000, c:'#22d3ee'},
  {k:'Юр / бухгалтерия',v:14000, c:'#facc15'},
];
/* прогноз кассы на 6 недель */
const HQ_FIN_CASH = (function(){
  let bal = 780000;
  const arr = [];
  for(let i=0;i<6;i++){
    const inc  = 82000 + Math.round(Math.random()*24000);
    const exp  = 74000 + Math.round(Math.random()*22000);
    bal += inc - exp;
    arr.push({w:'W+'+(i+1), inc, exp, bal});
  }
  return arr;
})();
function hqPieSVG(list, size){
  const R = 44, C = 2*Math.PI*R;
  const tot = list.reduce((s,x)=>s+x.v,0);
  let acc = 0;
  const segs = list.map((x,i)=>{
    const frac = x.v/tot, len = frac*C, gap = C-len, off = -acc*C;
    acc += frac;
    return `<circle cx="60" cy="60" r="${R}" fill="none" stroke="${x.c}" stroke-width="26"
      stroke-dasharray="${len.toFixed(2)} ${gap.toFixed(2)}" stroke-dashoffset="${off.toFixed(2)}" transform="rotate(-90 60 60)"
      style="animation:hqDonutIn .9s cubic-bezier(.2,.85,.3,1) ${i*.06}s both"/>`;
  }).join('');
  return `<svg class="hq-pie-svg" viewBox="0 0 120 120" aria-hidden="true" style="width:${size||132}px;height:${size||132}px">
    ${segs}
    <text x="60" y="58" text-anchor="middle" class="hq-dn-num">${(tot/1000).toFixed(0)}к</text>
    <text x="60" y="72" text-anchor="middle" class="hq-dn-sub">₽ / мес</text>
  </svg>`;
}
function hqBars(list){
  const max = Math.max(...list.map(x=>x.v));
  return list.map((x,i)=>`
    <div class="hq-bars-row" style="animation-delay:${i*0.05}s">
      <span class="hq-bars-lbl"><i style="background:${x.c}"></i>${esc(x.k)}</span>
      <span class="hq-bars-bar"><i style="width:${Math.round(x.v/max*100)}%;background:${x.c}"></i></span>
      <span class="hq-bars-v">${(x.v/1000).toFixed(0)}к</span>
    </div>`).join('');
}
function hqFinanceView(){
  const inc = HQ_FIN_INCOME.reduce((s,x)=>s+x.v,0);
  const exp = HQ_FIN_EXPENSE.reduce((s,x)=>s+x.v,0);
  const net = inc - exp;
  const lastBal = HQ_FIN_CASH[HQ_FIN_CASH.length-1].bal;
  const gapWeek = HQ_FIN_CASH.find(w=>w.bal<0);
  const legendInc = HQ_FIN_INCOME.map(x=>{
    const pct = Math.round(x.v/inc*100);
    return `<div class="hq-dn-l"><i style="background:${x.c}"></i><b>${esc(x.k)}</b><span>${(x.v/1000).toFixed(0)}к · ${pct}%</span></div>`;
  }).join('');
  /* cash-flow бары: доход зелёный, расход красный, баланс — линия */
  const cashMax = Math.max(...HQ_FIN_CASH.map(w=>Math.max(w.inc,w.exp)));
  const cashRows = HQ_FIN_CASH.map((w,i)=>`
    <div class="hq-cf-week" style="animation-delay:${i*0.06}s">
      <span class="hq-cf-lbl">${w.w}</span>
      <span class="hq-cf-bars">
        <i class="up"   style="height:${Math.round(w.inc/cashMax*100)}%"><em>+${(w.inc/1000).toFixed(0)}к</em></i>
        <i class="dn"   style="height:${Math.round(w.exp/cashMax*100)}%"><em>−${(w.exp/1000).toFixed(0)}к</em></i>
      </span>
      <span class="hq-cf-bal ${w.bal<0?'neg':''}">${(w.bal/1000).toFixed(0)}к</span>
    </div>`).join('');
  return `
    ${hqRoleBar()}
    <div class="hq-part-sum card">
      <div class="hq-part-s"><b>${(inc/1000).toFixed(0)}к ₽</b><small>Доходы · мес</small></div>
      <div class="hq-part-s"><b>${(exp/1000).toFixed(0)}к ₽</b><small>Расходы · мес</small></div>
      <div class="hq-part-s"><b class="${net<0?'warn':''}">${net>=0?'+':''}${(net/1000).toFixed(0)}к ₽</b><small>Чистая прибыль</small></div>
    </div>
    <div class="adm-sec-h">Структура доходов · pie</div>
    <div class="hq-donut card">${hqPieSVG(HQ_FIN_INCOME, 140)}<div class="hq-dn-legend">${legendInc}</div></div>
    <div class="adm-sec-h">Расходы · распределение</div>
    <div class="hq-bars card">${hqBars(HQ_FIN_EXPENSE)}</div>
    <div class="adm-sec-h">Прогноз кассы · 6 недель</div>
    <div class="hq-cf card">
      <div class="hq-cf-grid">${cashRows}</div>
      <div class="hq-cf-foot ${lastBal<0?'neg':''}">
        <span>Прогноз баланса через 6 недель: <b>${(lastBal/1000).toFixed(0)}к ₽</b></span>
        ${gapWeek ? `<span class="hq-cf-warn">${I('bolt')} Кассовый разрыв на ${gapWeek.w}: ${(gapWeek.bal/1000).toFixed(0)}к ₽</span>` : `<span class="hq-cf-ok">${I('check2')} Разрывов не прогнозируется</span>`}
      </div>
    </div>
    <div class="adm-acts" style="margin-top:12px">
      <button class="adm-btn pri" onclick="hqExportMenu()">${I('file')} Экспорт .csv / .pdf</button>
      <button class="adm-btn" onclick="hqExportReport()">${I('file')} Сводка .txt</button>
    </div>`;
}

/* CRM ПАРТНЁРОВ (топ-10) --------------------------------------------------- */
const HQ_CRM = []  /* демо-данные удалены 09.08: 3D-штаб — источник только API */;
const HQ_CRM_TIER = {GOLD:{c:'#facc15',bg:'rgba(250,204,21,.14)'}, SILVER:{c:'#8892a0',bg:'rgba(136,146,160,.14)'}, BRONZE:{c:'#ff7a3c',bg:'rgba(255,122,60,.14)'}};
function hqCrmView(){
  const tot = HQ_CRM.reduce((s,x)=>s+x.turn,0);
  const waits = HQ_CRM.filter(x=>x.st==='wait');
  const oks   = HQ_CRM.filter(x=>x.st==='ok');
  const rows = HQ_CRM.map((p,i)=>{
    const t = HQ_CRM_TIER[p.tier] || {c:'#8892a0',bg:'transparent'};
    return `<div class="hq-crm-row" style="animation-delay:${i*0.04}s">
      <span class="hq-crm-rank">#${i+1}</span>
      <span class="adm-ava" style="background:${t.bg};color:${t.c};border:1px solid ${t.c}44">${esc(p.n[0])}</span>
      <span class="adm-main"><b>${esc(p.n)}</b><small>${esc(p.h)} · ${p.refs} рефералов · ${esc(p.last)}</small></span>
      <span class="hq-crm-tier" style="color:${t.c};background:${t.bg}">${p.tier}</span>
      <span class="hq-crm-turn">${(p.turn/1000).toFixed(0)}к ₽</span>
      <span class="adm-tag ${p.st==='ok'?'ok':'wait'}">${p.st==='ok'?'выплачено':'к выплате'}</span>
    </div>`;
  }).join('');
  return `
    ${hqRoleBar()}
    <div class="hq-part-sum card">
      <div class="hq-part-s"><b>${HQ_CRM.length}</b><small>партнёров · топ</small></div>
      <div class="hq-part-s"><b>${(tot/1000).toFixed(0)}к ₽</b><small>суммарный оборот</small></div>
      <div class="hq-part-s"><b class="${waits.length?'warn':''}">${waits.length}</b><small>ждут выплаты</small></div>
    </div>
    <div class="adm-sec-h">Топ-10 партнёров · CRM</div>
    <div class="hq-crm card">${rows}</div>
    <div class="adm-acts" style="margin-top:12px">
      <button class="adm-btn pri" onclick="toast('Партнёрам ${waits.length} отправлены выплаты · $${Math.round(waits.reduce((s,x)=>s+x.turn*0.1,0)).toLocaleString('ru')}')">${I('card')} Выплатить всем ждущим (${waits.length})</button>
      <button class="adm-btn" onclick="hqExportMenu()">${I('file')} Экспорт CRM</button>
    </div>`;
}

/* ---------- 8.13 расширение renderAdmin для finance/crm ---------- */
const _prevRenderAdminHq3 = renderAdmin;
renderAdmin = function(){
  if(admTab === 'finance' || admTab === 'crm'){
    document.getElementById('admTabs').innerHTML = ADMIN_TABS.map(t=>
      `<button class="adm-tab ${admTab===t.k?'on':''}" onclick="admGo('${t.k}')">${t.t}</button>`).join('');
    document.getElementById('admBody').innerHTML = admTab==='finance' ? hqFinanceView() : hqCrmView();
    hqStopRt(); hqStopFeed();
    return;
  }
  _prevRenderAdminHq3();
  if(admTab === 'overview') hqStartRt(); else hqStopRt();
};
const _prevCloseAdminHq3 = closeAdmin;
closeAdmin = function(){ hqStopRt(); _prevCloseAdminHq3(); };

/* ---------- 8.14 ЭКСПОРТ: CSV / PDF (print) / TXT ---------- */
function hqExportMenu(){
  showPopup({ico:'file', title:'Экспорт отчёта за месяц', body:`
    <div class="hq-mail" style="text-align:left">
      Выбери формат — данные включают: MRR, DAU, тарифы, доходы/расходы, платежи, партнёры, CRM.
      <div style="display:flex;gap:8px;margin-top:12px;flex-wrap:wrap">
        <button class="adm-btn pri" style="flex:1;min-width:120px" onclick="hqExportCsv();closePopup()">${I('file')} CSV</button>
        <button class="adm-btn" style="flex:1;min-width:120px" onclick="hqExportPdf();closePopup()">${I('file')} PDF (печать)</button>
        <button class="adm-btn" style="flex:1;min-width:120px" onclick="hqExportReport();closePopup()">${I('file')} TXT</button>
      </div>
    </div>`, actions:[{label:'Закрыть', ghost:true}]});
}
function hqCsvEscape(v){ const s = String(v==null?'':v); return /[",\n;]/.test(s) ? '"'+s.replace(/"/g,'""')+'"' : s; }
function hqExportCsv(){
  const rows = [];
  rows.push(['section','key','value','sub']);
  const now = new Date();
  rows.push(['meta','date', now.toISOString(), 'OKO monthly export']);
  /* KPI */
  const mrr = hqMrr();
  rows.push(['kpi','MRR', mrr, 'USD']);
  rows.push(['kpi','ARR', mrr*12, 'USD']);
  rows.push(['kpi','DAU', HQ_MET.dau, '']);
  rows.push(['kpi','WAU', HQ_MET.wau, '']);
  rows.push(['kpi','MAU', HQ_MET.mau, '']);
  rows.push(['kpi','Retention 7d', HQ_MET.retention.d7, '%']);
  rows.push(['kpi','Retention 30d', HQ_MET.retention.d30, '%']);
  rows.push(['kpi','Retention 90d', HQ_MET.retention.d90, '%']);
  rows.push(['kpi','AOV', HQ_MET.aov, 'USD']);
  rows.push(['kpi','LTV', HQ_MET.ltv, 'USD']);
  rows.push(['kpi','CAC', HQ_MET.cac, 'USD']);
  rows.push(['kpi','ROI', hqRoi(), '%']);
  /* Тарифы */
  HQ_TIERS.forEach(t=> rows.push(['tier', t.k, t.count, 'active']));
  /* Доходы / Расходы */
  HQ_FIN_INCOME.forEach(x=> rows.push(['income', x.k, x.v, 'RUB/month']));
  HQ_FIN_EXPENSE.forEach(x=> rows.push(['expense', x.k, x.v, 'RUB/month']));
  /* Cash-flow */
  HQ_FIN_CASH.forEach(x=> rows.push(['cashflow', x.w, x.bal, 'income='+x.inc+' expense='+x.exp]));
  /* CRM */
  HQ_CRM.forEach((p,i)=> rows.push(['partner', p.n+' '+p.h, p.turn, p.tier+' · refs='+p.refs+' · '+p.st]));
  /* Платежи */
  ADMIN.pay.forEach((p,i)=> rows.push(['payment', p.n+' · '+p.plan, hqPayNum(p.sum), hqPaySt(i)]));
  /* AB */
  HQ_AB.forEach(t=> rows.push(['ab', t.n, t.winner||'', t.st+' · uplift='+t.uplift+'%']));
  const csv = rows.map(r=>r.map(hqCsvEscape).join(',')).join('\n');
  const stamp = now.getFullYear()+'-'+String(now.getMonth()+1).padStart(2,'0')+'-'+String(now.getDate()).padStart(2,'0');
  const url = URL.createObjectURL(new Blob(['﻿'+csv], {type:'text/csv;charset=utf-8'}));
  const a = document.createElement('a'); a.href = url; a.download = 'oko-report-'+stamp+'.csv';
  document.body.appendChild(a); a.click(); a.remove();
  setTimeout(()=>URL.revokeObjectURL(url), 4000);
  toast('CSV выгружен · '+rows.length+' строк');
}
function hqExportPdf(){
  const now = new Date();
  const stamp = String(now.getDate()).padStart(2,'0')+'.'+String(now.getMonth()+1).padStart(2,'0')+'.'+now.getFullYear();
  const mrr = hqMrr(), arr = mrr*12, roi = hqRoi();
  const inc = HQ_FIN_INCOME.reduce((s,x)=>s+x.v,0), exp = HQ_FIN_EXPENSE.reduce((s,x)=>s+x.v,0);
  const rowsKpi = [
    ['MRR', '$'+mrr.toLocaleString('en')], ['ARR', '$'+arr.toLocaleString('en')],
    ['DAU', HQ_MET.dau], ['WAU', HQ_MET.wau], ['MAU', HQ_MET.mau],
    ['Retention 7d', HQ_MET.retention.d7+'%'], ['Retention 30d', HQ_MET.retention.d30+'%'], ['Retention 90d', HQ_MET.retention.d90+'%'],
    ['AOV', '$'+HQ_MET.aov], ['LTV', '$'+HQ_MET.ltv], ['CAC', '$'+HQ_MET.cac], ['ROI', roi+'%'],
  ].map(r=>`<tr><td>${r[0]}</td><td><b>${r[1]}</b></td></tr>`).join('');
  const tiersR = HQ_TIERS.map(t=>`<tr><td>${t.k}</td><td><b>${t.count}</b></td><td>$${t.price}/мес</td></tr>`).join('');
  const incR = HQ_FIN_INCOME.map(x=>`<tr><td>${x.k}</td><td><b>${(x.v/1000).toFixed(0)}к ₽</b></td></tr>`).join('');
  const expR = HQ_FIN_EXPENSE.map(x=>`<tr><td>${x.k}</td><td><b>${(x.v/1000).toFixed(0)}к ₽</b></td></tr>`).join('');
  const crmR = HQ_CRM.map((p,i)=>`<tr><td>#${i+1}</td><td>${p.n} ${p.h}</td><td>${p.tier}</td><td><b>${(p.turn/1000).toFixed(0)}к ₽</b></td><td>${p.st==='ok'?'выплачено':'к выплате'}</td></tr>`).join('');
  const abR = HQ_AB.map(t=>`<tr><td>${t.n}</td><td>${t.winner||'—'}</td><td>${t.st}</td><td>${t.uplift?'+'+t.uplift+'%':''}</td></tr>`).join('');
  const html = `<!doctype html><html lang="ru"><head><meta charset="utf-8"><title>OKO · отчёт ${stamp}</title>
    <style>
      *{box-sizing:border-box}
      body{font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,sans-serif;color:#0d1a05;background:#fff;padding:32px;max-width:900px;margin:0 auto;line-height:1.5}
      h1{font-size:26px;letter-spacing:.02em;margin:0 0 6px;color:#0d1a05}
      h2{font-size:17px;margin:28px 0 10px;color:#5cae00;border-bottom:2px solid #9AFF00;padding-bottom:5px}
      .head{display:flex;align-items:center;gap:14px;border-bottom:3px solid #9AFF00;padding-bottom:14px;margin-bottom:18px}
      .logo{width:52px;height:52px;border-radius:14px;background:#0d1a05;color:#9AFF00;display:grid;place-items:center;font-family:'Bebas Neue',Impact,sans-serif;font-size:26px;letter-spacing:.05em}
      .meta{color:#5b6a4a;font-size:13px;margin-top:3px}
      table{width:100%;border-collapse:collapse;font-size:13px;margin-bottom:8px}
      th,td{text-align:left;padding:7px 10px;border-bottom:1px solid #e8efe0}
      th{background:#f6faed;font-weight:700;font-size:12px;color:#5cae00;text-transform:uppercase;letter-spacing:.05em}
      td b{color:#0d1a05;font-weight:700}
      .kpi{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:14px}
      .k{background:#f6faed;border-left:3px solid #9AFF00;padding:11px 14px;border-radius:6px}
      .k b{display:block;font-size:22px;color:#0d1a05;font-family:'Bebas Neue',Impact,sans-serif;letter-spacing:.03em}
      .k small{color:#5b6a4a;font-size:11px;text-transform:uppercase;letter-spacing:.05em}
      .foot{margin-top:32px;padding-top:16px;border-top:1px solid #e8efe0;color:#5b6a4a;font-size:12px;text-align:center}
      @media print{body{padding:20px}}
    </style></head><body>
    <div class="head">
      <div class="logo">OKO</div>
      <div><h1>Сводный отчёт владельца OKO</h1><div class="meta">Дата формирования: ${stamp} · период: последние 30 дней</div></div>
    </div>

    <div class="kpi">
      <div class="k"><b>$${mrr.toLocaleString('en')}</b><small>MRR</small></div>
      <div class="k"><b>$${arr.toLocaleString('en')}</b><small>ARR</small></div>
      <div class="k"><b>${HQ_MET.dau}</b><small>DAU</small></div>
      <div class="k"><b>${roi}%</b><small>ROI</small></div>
    </div>

    <h2>Ключевые метрики</h2>
    <table>${rowsKpi}</table>

    <h2>Активные подписки по тарифам</h2>
    <table><thead><tr><th>Тариф</th><th>Активных</th><th>Цена</th></tr></thead><tbody>${tiersR}</tbody></table>

    <h2>Доходы (за месяц)</h2>
    <table><thead><tr><th>Канал</th><th>Сумма</th></tr></thead><tbody>${incR}
      <tr><td><b>Итого</b></td><td><b>${(inc/1000).toFixed(0)}к ₽</b></td></tr></tbody></table>

    <h2>Расходы (за месяц)</h2>
    <table><thead><tr><th>Категория</th><th>Сумма</th></tr></thead><tbody>${expR}
      <tr><td><b>Итого</b></td><td><b>${(exp/1000).toFixed(0)}к ₽</b></td></tr>
      <tr><td><b>Чистая прибыль</b></td><td><b>${((inc-exp)/1000).toFixed(0)}к ₽</b></td></tr></tbody></table>

    <h2>Партнёры · CRM топ-10</h2>
    <table><thead><tr><th>Ранг</th><th>Партнёр</th><th>Уровень</th><th>Оборот</th><th>Статус</th></tr></thead><tbody>${crmR}</tbody></table>

    <h2>A/B тесты</h2>
    <table><thead><tr><th>Эксперимент</th><th>Победитель</th><th>Статус</th><th>Uplift</th></tr></thead><tbody>${abR}</tbody></table>

    <div class="foot">OKO · штаб владельца · документ сформирован автоматически.
      Для сохранения в PDF: в диалоге печати выбери «Сохранить как PDF».</div>
    <scr`+`ipt>window.addEventListener('load',()=>setTimeout(()=>window.print(),500));</scr`+`ipt>
    </body></html>`;
  const w = window.open('', '_blank');
  if(!w){ toast('Разреши всплывающие окна для экспорта PDF'); return; }
  w.document.open(); w.document.write(html); w.document.close();
  toast('PDF-отчёт открыт · нажми «Сохранить как PDF»');
}

/* ==================== самоинициализация ==================== */
(function hqInit(){
  hqDecorateProfile();
})();

/* ===== module: market-pro ===== */
/* ================= MARKET-PRO: кабинет продавца + пакеты OKO Production =================
   Часть 1 — кабинет продавца на Бирже: статистика, баланс продаж (минус комиссия 10%),
   эскроу-сделки, вывод на кошелёк, рейтинг и отзывы.
   Часть 2 — флагманские пакеты «OKO Production — под ключ»: оплата с кошелька,
   рабочий чат клиента с ветками работ, уведомление.
   Часть 3 — шлифовка: отзывы после сделки (звёзды 1-5 + текст -> l.reviews + пересчёт l.r),
   чат с продавцом с автоприветствием, жалоба на объявление (chip-причины -> ADMIN.moder),
   фильтр «Только проверенные» (verified или рейтинг 4.8+).
   Патчи (chain): renderMarket (кнопка кабинета + премиум-баннер), orderListing
   (регистрация сделки в кабинете, саму покупку НЕ трогаем — она уже патчена wallet),
   openConv (закреплённые ветки работ), openListing (бейдж/жалоба/кнопка), contactSeller
   (автоприветствие), mkApplyFilters/renderFilters/activeFilterCount/resetFilters
   (фильтр проверенных), admModer (счётчик жалоб). Персист: localStorage oko-market-pro. */

/* ---------- константы ---------- */
const MP_USD = 90;             // курс прототипа, ₽ за $
const MP_FEE = 0.10;           // комиссия OKO с продаж Биржи
const MP_ASSIST = 'Личный ассистент OKO';
const MP_BRANCHES = [
  ['Контент',  'сценарии, съёмка, монтаж, автопостинг'],
  ['Дизайн',   'обложки, оформление профиля, креативы'],
  ['Сайт',     'лендинг, домен, воронка продаж'],
  ['Реклама',  'кампании, таргет, ретаргет, трафик'],
  ['Отчёты',   'аналитика, метрики, еженедельные итоги'],
];
const MP_ST = {
  new:     {t:'Новый заказ',            ic:'bolt'},
  work:    {t:'В работе',               ic:'clock'},
  wait:    {t:'На холде · проверка',    ic:'lock'},
  done:    {t:'Завершена',              ic:'check'},
  dispute: {t:'Спор',                   ic:'flag'},
  cancel:  {t:'Отменена · возврат',     ic:'back'},
};
const MP_ST_OPEN = st => st!=='done' && st!=='cancel'; // сделка «живая» (эскроу активен)

/* ---------- пакеты услуг ---------- */
const MP_PACKS = {
  start: {k:'start', name:'СТАРТ', usd:1500, tag:'Контент-завод на месяц',
    inc:['30 роликов в месяц под ключ','Сценарии, монтаж, караоке-субтитры','Дизайн: обложки и оформление профиля','Автопостинг в 3 соцсети','Аналитика и еженедельные отчёты'],
    bonus:[],
    weeks:[
      ['Бриф и стратегия','Анализ ниши и конкурентов, контент-план на 30 роликов, референсы стиля'],
      ['Запуск конвейера','Первые 8 роликов: сценарии, монтаж, обложки; старт автопостинга'],
      ['Разгон','Ещё 11 роликов, корректировка форматов по первой аналитике'],
      ['Итоги месяца','Финальные 11 роликов, полный отчёт и план следующего месяца'],
    ]},
  business: {k:'business', name:'БИЗНЕС', usd:3000, hot:true, tag:'Всё из СТАРТ + запуск продаж',
    inc:['Весь пакет СТАРТ: 30 роликов, дизайн, автопостинг, аналитика'],
    bonus:['Сайт под ключ: лендинг + домен','Воронка продаж от заявки до оплаты','Рекламные кампании: таргет и трафик','Личный ассистент на весь срок'],
    weeks:[
      ['Бриф и фундамент','Стратегия, контент-план, прототип сайта, структура воронки'],
      ['Контент + сайт','8 роликов в работе, релиз лендинга, подключение форм и аналитики'],
      ['Запуск продаж','Воронка в бою, старт рекламных кампаний, ещё 11 роликов'],
      ['Масштабирование','Финальные ролики, оптимизация рекламы, сводный отчёт по выручке'],
    ]},
};

/* ---------- состояние (персист) ---------- */
let MP = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-market-pro'))||null; }catch(e){ return null; } })();
function mpSave(){ try{ localStorage.setItem('oko-market-pro', JSON.stringify(MP)); }catch(e){} }
const MP_H = 3600e3;
function mpSeedDeals(){
  /* демо-сидер отключён 09.08 (правка Даниэля: ноль демо-данных) */
  return (arguments.length && Array.isArray(arguments[0])) ? [] : undefined;

  const now = Date.now(), H = MP_H;
  return [
    {id:'d0', t:'Пакет: 15 Shorts для запуска',       n:'Тимур Н.',   sum:12000, st:'new',  dir:'in',  at:now-40*60e3, lid:901},
    {id:'d1', t:'Контент-завод: 30 клипов в месяц',  n:'Марина К.',  sum:45000, st:'work', dir:'in',  at:now-26*H, lid:901},
    {id:'d2', t:'Лендинг под ключ за 5 дней',         n:'Игорь В.',   sum:19000, st:'wait', dir:'in',  at:now-4*H,  lid:902},
    {id:'d3', t:'Разбор и стратегия роста канала',    n:'Алина Р.',   sum:8000,  st:'done', dir:'in',  at:now-70*H, lid:903, rev:5},
    {id:'d4', t:'Дизайн обложек и карточек товара',   n:'Лиза Кот',   sum:300,   st:'wait', dir:'out', at:now-9*H,  lid:2, held:false},
  ];
}
if(!MP || !MP.v){
  MP = { v:2, seq:0, gross:58000, customChatId:null, orders:[],
    stats:{901:{orders:9, rev:405000}, 902:{orders:6, rev:114000}, 903:{orders:8, rev:64000}},
    deals: mpSeedDeals() };
  mpSave();
}
/* миграция v1 -> v2: не стираем сохранённые данные, лишь добираем демо-статусы «новый» и «покупка» */
if(MP.v < 2){
  const have = new Set(MP.deals.map(d=>d.id));
  mpSeedDeals().forEach(d=>{ if((d.id==='d0'||d.id==='d4') && !have.has(d.id)) MP.deals.push(d); });
  MP.v = 2; mpSave();
}
MP.myRevs  = MP.myRevs  || [];  // отзывы, добавленные после сделок (реплей после перезагрузки)
MP.reports = MP.reports || [];  // локальный лог жалоб на объявления
const mpBal = () => Math.round(MP.gross * (1 - MP_FEE));

/* ---------- демо: мои объявления продавца (LISTINGS не персистится — сеем каждый запуск) ---------- */
(function mpSeedMyListings(){
  /* демо-сидер отключён 09.08 (правка Даниэля: ноль демо-данных) */
  return (arguments.length && Array.isArray(arguments[0])) ? [] : undefined;

  if(LISTINGS.some(l=>l.my)) return;
  const rv = (n,r,t)=>({n,r,t});
  LISTINGS.push(
    {id:901, cat:'video', type:'service', t:'Контент-завод: 30 клипов в месяц', p:45000, pt:'от 45 000 ₽/мес',
     ds:'Полный цикл: сценарии, съёмка, монтаж, караоке-субтитры, обложки, автопостинг и аналитика. Команда OKO Production.',
     a:'Д', n:PROFILE.name, r:4.9, deals:23, city:'Онлайн', term:'помесячно', views:3120, favs:97, contacts:141,
     promo:'turbo', fav:false, my:true, st:'act', seed:52,
     reviews:[rv('Марина К.',5,'За месяц 31 ролик, два залетели за 100к просмотров'), rv('Игорь В.',5,'Снял с себя весь контент, занимаюсь только продажами'), rv('Олег',4,'Качество топ, хотелось бы быстрее правки')]},
    {id:902, cat:'sites', type:'service', t:'Лендинг под ключ за 5 дней', p:19000, pt:'от 19 000 ₽',
     ds:'Продающий лендинг: структура, дизайн в фирменном стиле, адаптив, домен, формы и аналитика. Правки — 7 дней бесплатно.',
     a:'Д', n:PROFILE.name, r:5.0, deals:14, city:'Онлайн', term:'5 дней', views:1460, favs:41, contacts:57,
     promo:'vip', fav:false, my:true, st:'act', seed:64,
     reviews:[rv('Алина Р.',5,'Лендинг окупился с первой недели'), rv('Сергей Дан',5,'Конверсия 9% на холодном трафике')]},
    {id:903, cat:'consult', type:'service', t:'Разбор и стратегия роста канала', p:8000, pt:'от 8 000 ₽',
     ds:'Часовой разбор: контент, упаковка, воронка. На выходе — пошаговый план роста на 90 дней.',
     a:'Д', n:PROFILE.name, r:4.8, deals:19, city:'Онлайн', term:'1 день', views:890, favs:26, contacts:33,
     promo:null, fav:false, my:true, st:'act', seed:77,
     reviews:[rv('Катя SMM',5,'План на 90 дней реально рабочий')]}
  );
})();

/* ---------- реплей сохранённых отзывов (LISTINGS пересоздаются каждый запуск) ---------- */
(function mpReplayReviews(){
  const touched = new Set();
  (MP.myRevs||[]).forEach(rv0=>{
    const l = LISTINGS.find(x=>x.id===rv0.lid);
    if(!l) return;
    (l.reviews = l.reviews||[]).unshift({n:rv0.n, r:rv0.r, t:rv0.t});
    touched.add(l);
  });
  /* стартовый рейтинг своих сид-объявлений должен сразу совпадать со своими же отзывами
     (иначе, напр., #903 показывает 4.8 при единственном 5★-отзыве — mismatch до 1-го реального) */
  LISTINGS.forEach(l=>{ if(l.my && l.reviews && l.reviews.length) touched.add(l); });
  touched.forEach(l=>mpRecalcRating(l));
})();

/* ---------- хелперы ---------- */
function mpRecalcRating(l){
  const rv = l.reviews||[];
  if(rv.length) l.r = Math.round(rv.reduce((s,x)=>s+x.r,0)/rv.length*10)/10;
}
function mpDealListing(d){
  return (d.lid && LISTINGS.find(x=>x.id===d.lid)) || LISTINGS.find(x=>x.t===d.t) || null;
}
function mpIsVerified(l){
  return (typeof VERIFIED!=='undefined' && VERIFIED.has(l.n)) || l.r>=4.8;
}
function mpWhen(at){
  const d = new Date(at), n = new Date();
  const hm = String(d.getHours()).padStart(2,'0')+':'+String(d.getMinutes()).padStart(2,'0');
  if(d.toDateString()===n.toDateString()) return 'сегодня '+hm;
  if(d.toDateString()===new Date(n-864e5).toDateString()) return 'вчера '+hm;
  return String(d.getDate()).padStart(2,'0')+'.'+String(d.getMonth()+1).padStart(2,'0')+' '+hm;
}
function mpCountUp(){
  document.querySelectorAll('[data-mpcount]').forEach(el=>{
    const target = Number(el.dataset.mpcount)||0, t0 = performance.now(), dur = 600;
    (function step(t){
      const k = Math.min(1,(t-t0)/dur), e = 1-Math.pow(1-k,3);
      el.textContent = fmtN(Math.round(target*e));
      if(k<1) requestAnimationFrame(step);
    })(t0);
  });
}
function mpMyReviews(){
  const out = [];
  LISTINGS.filter(l=>l.my).forEach(l=>(l.reviews||[]).forEach(r=>out.push(r)));
  return out;
}

/* ---------- инфографика: динамика выручки (6 мес.), внутренне-согласованный тренд ---------- */
const MP_TREND_W = [0.52, 0.61, 0.55, 0.74, 0.83, 1.0]; // относительная форма кривой роста
function mpMonthLabels(){
  const ru = ['янв','фев','мар','апр','май','июн','июл','авг','сен','окт','ноя','дек'];
  const out = [], now = new Date();
  for(let i=5;i>=0;i--){ const d = new Date(now.getFullYear(), now.getMonth()-i, 1); out.push(ru[d.getMonth()]); }
  return out;
}
function mpTrendVals(total){
  const s = MP_TREND_W.reduce((a,b)=>a+b,0);
  return MP_TREND_W.map(x=>Math.round(total/s*x)); // сумма месяцев == общая выручка
}
/* ---------- инфографика: воронка показы -> контакты -> заказы (реальные числа) ---------- */
function mpFunnel(views, contacts, orders){
  const rows = [
    {ic:'eye',       lab:'Показы',   n:views},
    {ic:'chat',      lab:'Контакты', n:contacts},
    {ic:'briefcase', lab:'Заказы',   n:orders},
  ];
  const max = Math.max(1, views);
  return `<div class="mp-funnel">${rows.map((r,i)=>{
    const w = Math.max(7, Math.round(r.n/max*100));
    const conv = i>0 ? (rows[i-1].n ? Math.round(r.n/rows[i-1].n*1000)/10 : 0) : null;
    return `<div class="mp-fn-row">
      <div class="mp-fn-lab">${I(r.ic)}<span>${r.lab}</span><b>${fmtN(r.n)}</b></div>
      <div class="mp-fn-track"><span class="mp-fn-bar s${i}" style="--w:${w}%"></span></div>
      ${conv!==null?`<div class="mp-fn-conv">${I('chev')} конверсия <b>${conv}%</b></div>`:''}
    </div>`;
  }).join('')}</div>`;
}
/* ---------- инфографика: распределение оценок (реальные отзывы) ---------- */
function mpRatingDist(revs){
  const cnt = {5:0,4:0,3:0,2:0,1:0};
  revs.forEach(r=>{ const k = Math.max(1,Math.min(5,Math.round(r.r))); cnt[k]++; });
  const max = Math.max(1, ...Object.values(cnt));
  let rows = '';
  for(let s=5;s>=1;s--){
    const w = Math.round(cnt[s]/max*100);
    rows += `<div class="mp-rd-row"><span class="mp-rd-k">${s}${I('star')}</span>
      <span class="mp-rd-track"><span class="mp-rd-bar" style="--w:${cnt[s]?w:0}%"></span></span>
      <span class="mp-rd-n">${cnt[s]}</span></div>`;
  }
  return `<div class="mp-rdist">${rows}</div>`;
}
/* ---------- блок аналитики (динамика выручки + воронка) ---------- */
function mpAnalytics(revenue, views, contacts, orders){
  const vals = mpTrendVals(revenue), labs = mpMonthLabels();
  const max = Math.max(1, ...vals);
  const growth = vals[0] ? Math.round((vals[5]-vals[0])/vals[0]*100) : 0;
  const bars = vals.map((v,i)=>{
    const h = Math.max(6, Math.round(v/max*100));
    return `<div class="mp-tb-col"><span class="mp-tb-val">${fmtN(v)}</span>
      <span class="mp-tb-track"><span class="mp-tb-bar${i===5?' now':''}" style="--h:${h}%;--d:${i*0.07}s"></span></span>
      <span class="mp-tb-x">${labs[i]}</span></div>`;
  }).join('');
  return `
    <div class="mp-h">${I('poll')} Динамика выручки · 6 мес.</div>
    <div class="mp-chart">
      <div class="mp-chart-top">
        <div><b>${fmtMoney(revenue)}</b><small>за полгода</small></div>
        <span class="mp-grow ${growth>=0?'up':'down'}">${I(growth>=0?'rocket':'chev')} ${growth>=0?'+':''}${growth}%</span>
      </div>
      <div class="mp-tbars">${bars}</div>
    </div>
    <div class="mp-h">${I('bolt')} Воронка продаж</div>
    ${mpFunnel(views, contacts, orders)}`;
}
/* ---------- эскроу-трекер стадий сделки ---------- */
function mpEscrowStep(d){
  if(d.st==='cancel')
    return `<div class="mp-esc-flat cancel">${I('back')} Сделка отменена · ${fmtMoney(d.sum)} возвращены на кошелёк</div>`;
  const nodes = d.dir==='in'
    ? ['Заказ','В работе','Сдача','Выплата']
    : ['Оплата','В работе','Проверка','Получено'];
  const dispute = d.st==='dispute';
  // new:0  work:1  wait/dispute:2  done:all
  const cur = d.st==='new' ? 0 : d.st==='work' ? 1 : (d.st==='wait'||dispute) ? 2 : nodes.length;
  const fill = Math.min(100, Math.round(cur/(nodes.length-1)*100));
  return `<div class="mp-esc-track${dispute?' dispute':''}" style="--fill:${fill}%">
    <div class="mp-esc-line"></div><div class="mp-esc-fill"></div>
    ${nodes.map((n,i)=>{
      const cls = i<cur ? 'done' : i===cur ? 'cur' : '';
      return `<div class="mp-esc-node ${cls}"><span class="mp-esc-dot">${i<cur?I('check'):''}</span><small>${n}</small></div>`;
    }).join('')}
  </div>${dispute?`<div class="mp-esc-flat dispute">${I('flag')} Спор открыт · модерация OKO проверяет сделку, деньги заморожены</div>`:''}`;
}

/* ================= ЧАСТЬ 1 · КАБИНЕТ ПРОДАВЦА ================= */
function mpOpenCabinet(){ mpRenderCab(); openSheet('mpCab'); }
function mpRenderCab(){
  const box = document.getElementById('mpCabView');
  if(!box) return;
  const mine = LISTINGS.filter(l=>l.my);
  const views    = mine.reduce((s,l)=>s+(l.views||0),0);
  const contacts = mine.reduce((s,l)=>s+(l.contacts||0),0);
  const orders   = mine.reduce((s,l)=>s+((MP.stats[l.id]||{}).orders||0),0);
  const revenue  = mine.reduce((s,l)=>s+((MP.stats[l.id]||{}).rev||0),0);
  const revs = mpMyReviews();
  const avg = revs.length ? revs.reduce((s,r)=>s+r.r,0)/revs.length : 0;
  const fee = MP.gross - mpBal();
  const badge = (typeof vBadge==='function') ? vBadge(PROFILE.name) : '';
  const term = d => (d.st==='done'||d.st==='cancel');
  const deals = MP.deals.slice().sort((a,b)=>(term(a)-term(b)) || (a.st==='dispute'?-1:0)-(b.st==='dispute'?-1:0) || b.at-a.at);
  const openN = MP.deals.filter(d=>MP_ST_OPEN(d.st)).length;
  const holdSum = MP.deals.filter(d=>MP_ST_OPEN(d.st)).reduce((s,d)=>s+d.sum,0);
  box.innerHTML = `
    <div class="mp-cab-head">
      <div class="mp-cab-ava">${esc(PROFILE.name[0])}</div>
      <div><b>${esc(PROFILE.name)}${badge}</b>
        <small>${stars(avg)} ${avg?avg.toFixed(1):'—'} · ${revs.length} отзывов · продавец Биржи</small></div>
    </div>

    <div class="mp-h">${I('poll')} Статистика продавца</div>
    <div class="mp-kpi">
      <div class="mp-kpi-t">${I('eye')}<b data-mpcount="${views}">0</b><small>просмотры</small></div>
      <div class="mp-kpi-t">${I('chat')}<b data-mpcount="${contacts}">0</b><small>контакты</small></div>
      <div class="mp-kpi-t">${I('briefcase')}<b data-mpcount="${orders}">0</b><small>заказы</small></div>
      <div class="mp-kpi-t">${I('money')}<b data-mpcount="${revenue}">0</b><small>выручка ₽</small></div>
    </div>

    ${mpAnalytics(revenue, views, contacts, orders)}

    <div class="mp-h">${I('card')} Баланс продаж</div>
    <div class="mp-balcard">
      <small class="dim" style="font-size:10.5px;letter-spacing:.12em;text-transform:uppercase;font-weight:700">К выводу после комиссии</small>
      <div class="mp-bal-sum"><b>${mpBal().toLocaleString('ru-RU').replace(/,/g,' ')}</b> ₽</div>
      <div class="mp-calc">
        <div class="mp-calc-row"><span>Выручка с завершённых сделок</span><b>${fmtMoney(MP.gross)}</b></div>
        <div class="mp-calc-row fee"><span>Комиссия OKO 10%</span><b>− ${fmtMoney(fee)}</b></div>
        <div class="mp-calc-row total"><span>К выводу</span><b>${fmtMoney(mpBal())}</b></div>
      </div>
      <button class="btn" onclick="mpWithdraw()">${I('money')} Вывести на кошелёк</button>
    </div>

    <div class="mp-h">${I('briefcase')} По объявлениям</div>
    ${mine.length ? mine.map(l=>{
      const s = MP.stats[l.id]||{orders:0,rev:0};
      return `<div class="mp-lst" onclick="closeSheet();openListing(${l.id})">
        <span class="mp-lst-ic">${I(catIco(l.cat))}</span>
        <div class="mp-lst-b"><b>${esc(l.t)}</b>
          <div class="mp-lst-s">
            <span>${I('eye')}${fmtN(l.views)}</span>
            <span>${I('chat')}${l.contacts}</span>
            <span>${I('briefcase')}${s.orders} закз.</span>
          </div></div>
        <span class="mp-lst-rev">${fmtN(s.rev)} ₽</span></div>`;
    }).join('') : '<div class="mp-empty">Объявлений нет — размести первое на Бирже</div>'}

    <div class="mp-h">${I('lock')} Сделки · эскроу
      ${openN?`<span class="mp-h-badge">${openN} активн.</span>`:''}
      ${holdSum?`<span class="mp-h-badge hold">${I('lock')}${fmtMoney(holdSum)} в холде</span>`:''}
    </div>
    ${deals.map(d=>{
      const st = MP_ST[d.st];
      return `<div class="mp-deal${d.st==='dispute'?' dispute':''}${d.st==='cancel'?' cancelled':''}">
        <div class="mp-deal-top"><b>${esc(d.t)}</b><span class="mp-deal-sum">${fmtMoney(d.sum)}</span></div>
        <div class="mp-deal-meta">
          <span class="mp-st ${d.st}">${I(st.ic)}${st.t}</span>
          <span class="mp-dir ${d.dir}">${d.dir==='in'?'Продажа':'Покупка'}</span>
          <small>${esc(d.n)} · ${mpWhen(d.at)}</small>
          ${MP_ST_OPEN(d.st)?`<small class="mp-esc">${I('lock')}деньги в холде</small>`:''}
        </div>
        ${mpEscrowStep(d)}${mpDealActions(d)}</div>`;
    }).join('')}

    <div class="mp-h">${I('star')} Рейтинг и отзывы</div>
    <div class="mp-rate-big"><b>${avg?avg.toFixed(1):'—'}</b>
      <div class="mp-rate-mid">${stars(avg)}<small>${revs.length} отзывов по ${mine.length} объявлениям</small></div>
      ${revs.length?mpRatingDist(revs):''}</div>
    ${revs.slice(0,6).map(r=>`<div class="mp-rev">
      <div class="mp-rev-h"><b>${esc(r.n)}</b><span class="mp-rev-st">${stars(r.r)}</span></div>
      <p>${esc(r.t)}</p></div>`).join('')}
    <div style="height:6px"></div>`;
  mpCountUp();
}
/* ---------- набор действий под каждой сделкой (зависит от направления и статуса) ---------- */
function mpDealActions(d){
  if(d.st==='cancel') return ''; // отменённая сделка — терминальна, действий нет
  const chat = MP_ST_OPEN(d.st)
    ? `<button class="ghost" onclick="mpOpenDealChat('${d.id}')">${I('chat')} Чат по сделке</button>` : '';
  let main = '';
  if(d.dir==='in'){                                   // ПРОДАЖА (пользователь — продавец)
    if(d.st==='new')        main = `<button onclick="mpDealNext('${d.id}')">${I('check')} Принять заказ</button>`;
    else if(d.st==='work')  main = `<button onclick="mpDealNext('${d.id}')">${I('send')} Сдать работу</button>`;
    else if(d.st==='wait')  main = `<button onclick="mpDealNext('${d.id}')">${I('check')} Завершить сделку</button>`;
    else if(d.st==='dispute') main = `<button onclick="mpDealResolve('${d.id}','refund')">${I('back')} Вернуть деньги</button>
                                      <button onclick="mpDealResolve('${d.id}','done')">${I('check')} Завершить</button>`;
    else return mpRevBtn(d);                           // done / cancel
  } else {                                             // ПОКУПКА (пользователь — покупатель)
    if(d.st==='new' || d.st==='work'){
      main = `<button class="ghost" onclick="mpDealPoke('${d.id}')">${I('eye')} Запросить статус</button>
              <button class="warn" onclick="mpDealCancel('${d.id}')">${I('back')} Отменить · возврат</button>`;
    } else if(d.st==='wait'){
      main = `<button onclick="mpDealNext('${d.id}')">${I('check')} Подтвердить получение</button>
              <button class="warn" onclick="mpDealDispute('${d.id}')">${I('flag')} Открыть спор</button>`;
    } else if(d.st==='dispute'){
      main = `<button onclick="mpDealResolve('${d.id}','refund')">${I('back')} Вернуть деньги</button>
              <button class="ghost" onclick="mpDealResolve('${d.id}','done')">${I('check')} Всё ок, завершить</button>`;
    } else return mpRevBtn(d);                          // done / cancel
  }
  return `<div class="mp-deal-act">${main}${chat}</div>`;
}
function mpDealNext(id){
  const d = MP.deals.find(x=>x.id===id);
  if(!d) return;
  if(d.st==='new'){ d.st='work'; toast('Заказ принят — сделка в работе'); mpDealChatNote(d,'Продавец принял заказ. Работа началась.'); }
  else if(d.st==='work'){ d.st='wait'; toast('Работа сдана — деньги на холде до подтверждения'); mpDealChatNote(d, d.dir==='in'?'Продавец сдал работу. Проверьте и подтвердите — эскроу снимется.':'Работа сдана исполнителем.'); }
  else if(d.st==='wait'){ mpDealComplete(d); return; }
  mpSave(); mpRenderCab();
}
function mpDealComplete(d){
  d.st = 'done';
  if(d.dir==='in'){                                   // продажа: выручка в баланс продаж, комиссия при выводе
    MP.gross += d.sum;
    toast('Сделка завершена · +'+fmtMoney(Math.round(d.sum*(1-MP_FEE)))+' в баланс продаж');
  } else {                                            // покупка: снимаем холд — деньги ушли исполнителю
    mpReleaseHold(d);
    toast('Получение подтверждено — эскроу снят, деньги ушли исполнителю');
    setTimeout(()=>mpOpenReview(d.id), 500);
  }
  mpDealChatNote(d, 'Сделка успешно завершена. Спасибо!');
  mpSave(); mpRenderCab();
}
function mpDealPoke(id){
  const d = MP.deals.find(x=>x.id===id);
  if(!d || d.dir!=='out' || (d.st!=='work' && d.st!=='new')) return;
  d.st = 'wait'; mpSave();
  toast('Исполнитель сдал работу — проверь и подтверди получение');
  mpDealChatNote(d, 'Исполнитель отметил работу как сданную — на проверке у покупателя.');
  mpRenderCab();
}
/* снять эскроу-холд (реальные деньги только если сделка их реально держит) */
function mpReleaseHold(d){
  if(d.held){
    WALLET.hold = Math.max(0, WALLET.hold - d.sum); walletSave();
    // комиссия OKO признаётся ТОЛЬКО здесь — при завершении сделки (деньги ушли из эскроу продавцу)
    okoEarn(Math.round(d.sum*MP_FEE*100)/100, 'Комиссия Биржи 10%');
    d.held = false;
  }
}
/* возврат покупателю: холд -> баланс (комиссия не признавалась — откатывать нечего) */
function mpRefundBuyer(d){
  if(d.held){
    WALLET.hold = Math.max(0, WALLET.hold - d.sum); walletSave();
    walletAdd(d.sum, 'Возврат по сделке Биржи');
    d.held = false;
  }
}
function mpDealCancel(id){
  const d = MP.deals.find(x=>x.id===id);
  if(!d || d.dir!=='out' || !MP_ST_OPEN(d.st)) return;
  showPopup({ico:'back', title:'Отменить сделку?', body:'Сделка «'+esc(d.t)+'» будет отменена, а '+fmtMoney(d.sum)+' из эскроу вернутся на кошелёк.',
    actions:[
      {label:'Оставить', ghost:true, onclick:()=>closePopup()},
      {label:'Отменить и вернуть', onclick:()=>{ closePopup(); mpRefundBuyer(d); d.st='cancel'; mpSave();
        toast('Сделка отменена · '+fmtMoney(d.sum)+' возвращены на кошелёк');
        mpDealChatNote(d,'Покупатель отменил сделку. Эскроу возвращён на кошелёк.'); mpRenderCab(); }}
    ]});
}
function mpDealDispute(id){
  const d = MP.deals.find(x=>x.id===id);
  if(!d || d.st!=='wait') return;
  d.st = 'dispute'; mpSave();
  if(typeof ADMIN!=='undefined' && ADMIN.moder){
    ADMIN.moder.unshift({t:'Спор по сделке: «'+d.t+'» — '+fmtMoney(d.sum), by:'@'+(PROFILE.nick||'user'), kind:'Биржа'});
    if(ADMIN.kpi) ADMIN.kpi.moder = ADMIN.moder.length;
  }
  toast('Спор открыт — деньги заморожены, модерация OKO подключится');
  mpDealChatNote(d,'Открыт спор. Средства заморожены до решения модерации OKO.');
  mpRenderCab();
}
function mpDealResolve(id, outcome){
  const d = MP.deals.find(x=>x.id===id);
  if(!d || d.st!=='dispute') return;
  if(outcome==='refund'){
    if(d.dir==='out') mpRefundBuyer(d);
    d.st = 'cancel'; toast('Спор решён в пользу возврата · '+fmtMoney(d.sum)+' вернулись');
    mpDealChatNote(d,'Спор решён: средства возвращены покупателю.');
    mpSave(); mpRenderCab();
  } else {
    mpDealComplete(d); // завершение с обычной логикой (выручка/снятие холда)
  }
}

/* ---------- чат по сделке: связка эскроу с мессенджером ---------- */
function mpDealChat(d){
  let chat = (d.chatId && CHATS.find(c=>c.id===d.chatId)) || CHATS.find(c=>c.name===d.n);
  if(!chat){
    const id = 'deal-'+d.id;
    chat = {id, ava:(d.n||'?')[0], name:d.n, kind:'direct', nick:(d.n||'user').toLowerCase().replace(/\s/g,'_'),
      kindIcon:null, preview:'Сделка: '+d.t, time:nowT(), unread:0, online:true,
      msgs:[{kind:'sys', body:'Сделка по объявлению «'+d.t+'» · '+fmtMoney(d.sum)+' · '+(d.dir==='in'?'вы продавец':'вы покупатель')},
            {kind:'sys', body:'Эскроу OKO: деньги под защитой до подтверждения выполнения'}]};
    CHATS.unshift(chat);
  }
  d.chatId = chat.id;
  return chat;
}
function mpDealChatNote(d, text){
  const chat = (d.chatId && CHATS.find(c=>c.id===d.chatId)) || CHATS.find(c=>c.name===d.n);
  if(!chat) return;
  chat.msgs.push({kind:'sys', body:'Сделка · '+MP_ST[d.st].t+': '+text});
  chat.preview = text; chat.time = nowT();
  if(typeof currentChat!=='undefined' && currentChat===chat && typeof openConv==='function') openConv(chat.id);
  if(typeof renderChatList==='function' && document.getElementById('screen-chats')) renderChatList();
}
function mpOpenDealChat(id){
  const d = MP.deals.find(x=>x.id===id);
  if(!d) return;
  const chat = mpDealChat(d);
  mpSave();
  closeSheet();
  showTab('chats');
  if(typeof renderChatList==='function') renderChatList();
  openConv(chat.id);
}

/* ================= ОТЗЫВЫ ПОСЛЕ СДЕЛКИ ================= */
let mpRevDealId = null, mpRevStars = 5;
const MP_STAR_LAB = {1:'Ужасно', 2:'Плохо', 3:'Нормально', 4:'Хорошо', 5:'Отлично'};
function mpRevBtn(d){
  if(d.rev) return `<div class="mp-deal-act"><span class="mp-revok">${I('check')} Отзыв оставлен · ${d.rev}/5</span></div>`;
  if(!mpDealListing(d)) return '';
  return `<div class="mp-deal-act"><button onclick="mpOpenReview('${d.id}')">${I('star')} Оставить отзыв</button></div>`;
}
function mpOpenReview(id){
  const d = MP.deals.find(x=>x.id===id);
  if(!d || d.st!=='done' || d.rev) return;
  mpRevDealId = id; mpRevStars = 5;
  mpRenderReview();
  openSheet('mpReview');
}
function mpRenderReview(){
  const d = MP.deals.find(x=>x.id===mpRevDealId);
  const box = document.getElementById('mpReviewView');
  if(!d || !box) return;
  const who = d.dir==='out'
    ? 'Оцени исполнителя — отзыв появится в его объявлении и пересчитает рейтинг'
    : 'Покупатель '+esc(d.n)+' оценивает сделку — отзыв появится в твоём объявлении';
  box.innerHTML = `
    <h3 class="mp-rv-title">Оставить отзыв</h3>
    <p class="mp-rv-sub">${esc(d.t)} · ${fmtMoney(d.sum)}</p>
    <p class="mp-rv-who">${I('star')} ${who}</p>
    <div class="mp-rstars" id="mpRstars">${mpStarsBig()}</div>
    <div class="mp-rlab" id="mpRlab">${MP_STAR_LAB[mpRevStars]}</div>
    <textarea id="mpRevText" class="mp-ta" rows="4" maxlength="280" placeholder="Что понравилось, что можно улучшить…"></textarea>
    <button class="btn" onclick="mpSubmitReview()">${I('star')} Опубликовать отзыв</button>`;
}
function mpStarsBig(){
  let s = '';
  for(let i=1;i<=5;i++) s += `<button class="mp-rstar ${i<=mpRevStars?'on':''}" onclick="mpSetStars(${i})">${I('star')}</button>`;
  return s;
}
function mpSetStars(n){
  mpRevStars = n;
  const el = document.getElementById('mpRstars'); if(el) el.innerHTML = mpStarsBig();
  const lb = document.getElementById('mpRlab');   if(lb) lb.textContent = MP_STAR_LAB[n];
}
function mpSubmitReview(){
  const d = MP.deals.find(x=>x.id===mpRevDealId);
  if(!d) return;
  const txt = ((document.getElementById('mpRevText')||{}).value||'').trim();
  if(txt.length<3){ toast('Пара слов о сделке — и отзыв готов'); return; }
  const l = mpDealListing(d);
  const author = d.dir==='out' ? PROFILE.name : d.n;
  if(l){
    (l.reviews = l.reviews||[]).unshift({n:author, r:mpRevStars, t:txt});
    mpRecalcRating(l);
    MP.myRevs.push({lid:l.id, n:author, r:mpRevStars, t:txt, at:Date.now()});
  }
  d.rev = mpRevStars;
  mpSave();
  closeSheet();
  toast(l ? 'Отзыв опубликован — рейтинг объявления теперь '+l.r.toFixed(1) : 'Отзыв сохранён');
  mpOpenCabinet(); // вернуться в кабинет с обновлённым состоянием
}
function mpWithdraw(){
  const bal = mpBal();
  if(bal <= 0){ toast('Баланс продаж пуст — заверши активные сделки'); return; }
  walletAdd(bal, 'Выручка Биржи');
  okoEarn(MP.gross - bal, 'Комиссия Биржи 10%');
  MP.gross = 0; mpSave();
  toast('+'+fmtMoney(bal)+' зачислено на кошелёк');
  mpRenderCab();
}

/* ================= ЧАСТЬ 2 · ПАКЕТЫ OKO PRODUCTION ================= */
function mpOpenPackages(){
  const wrap = document.getElementById('marketRoot');
  if(!wrap) return;
  const packCard = p => `
    <div class="mp-pack ${p.hot?'hot':''}">
      ${p.hot?'<span class="mp-pack-flag">ФЛАГМАН</span>':''}
      <div class="mp-pack-name">${p.name}</div>
      <div class="mp-pack-tag">${p.tag}</div>
      <div class="mp-pack-price"><b>$${fmtN(p.usd)}</b><span>· ${fmtN(p.usd*MP_USD)} ₽ по курсу ${MP_USD} ₽/$</span></div>
      <ul class="mp-inc">${p.inc.map(x=>`<li>${I('check')}<span>${x}</span></li>`).join('')}</ul>
      ${p.bonus.length?`<span class="mp-bonus-lab">${I('bolt')} БОНУС</span>
        <ul class="mp-inc">${p.bonus.map(x=>`<li class="bonus">${I('check')}<span>${x}</span></li>`).join('')}</ul>`:''}
      <div class="mp-weeks"><div class="mp-weeks-h">Что получаешь по неделям</div>
        ${p.weeks.map((w,i)=>`<div class="mp-week"><span class="mp-week-n">${i+1}</span>
          <div class="mp-week-b"><b>Неделя ${i+1} · ${w[0]}</b><small>${w[1]}</small></div></div>`).join('')}</div>
      <button class="btn" onclick="mpOrderPackage('${p.k}')">${I('rocket')} Заказать пакет · ${fmtN(p.usd*MP_USD)} ₽</button>
      <div class="mp-pack-note">Оплата с кошелька OKO · команда и рабочий чат сразу, старт брифа в день оплаты</div>
    </div>`;
  wrap.innerHTML = `
    <button class="mk-back" onclick="mkView='cats';mkCat=null;renderMarket()">${I('back')} Биржа</button>
    <div class="mp-hero">
      <svg><use href="#i-logo"/></svg>
      <h2>OKO <b>PRODUCTION</b></h2>
      <p>Флагманская услуга «под ключ» от команды OKO: контент, дизайн, сайт и реклама, делаем мы, растёшь ты.</p>
    </div>
    ${packCard(MP_PACKS.start)}
    ${packCard(MP_PACKS.business)}
    <div class="mp-pack custom">
      <span class="mp-cab-ic">${I('chat')}</span>
      <div style="flex:1;min-width:0"><div class="mp-pack-name">Индивидуальный</div>
        <p>Нестандартная задача или другой масштаб — соберём пакет под тебя.</p></div>
      <button class="btn" onclick="mpCustomChat()">Обсудим задачу</button>
    </div>
    <div style="height:8px"></div>`;
  wrap.scrollIntoView({block:'start'});
}

/* ---------- заказ пакета: оплата с кошелька ---------- */
let mpOrderPack = null;
function mpOrderPackage(k){ mpOrderPack = k; mpRenderOrder(); openSheet('mpOrder'); }
function mpRenderOrder(){
  const p = MP_PACKS[mpOrderPack];
  if(!p) return;
  const rub = p.usd*MP_USD, lack = rub - WALLET.balance;
  const box = document.getElementById('mpOrderView');
  box.innerHTML = `
    <div class="mp-ord-head">
      <span class="mp-banner-logo"><svg><use href="#i-logo"/></svg></span>
      <div><b>OKO Production · ${p.name}</b><small>${p.tag}</small></div>
    </div>
    <div class="mp-calc" style="border-top:none;padding-top:0">
      <div class="mp-calc-row"><span>Пакет «${p.name}»</span><b>$${fmtN(p.usd)}</b></div>
      <div class="mp-calc-row"><span>Курс прототипа</span><b>${MP_USD} ₽/$</b></div>
      <div class="mp-calc-row total"><span>К оплате с кошелька</span><b>${fmtMoney(rub)}</b></div>
      <div class="mp-calc-row"><span>Баланс кошелька</span><b>${fmtMoney(WALLET.balance)}</b></div>
    </div>
    ${lack>0 ? `
      <div class="mp-lack">Не хватает ${fmtMoney(lack)} для оплаты пакета</div>
      <button class="btn" onclick="mpGoTopup(${Math.ceil(lack/100)*100})">${I('plus')} Пополнить кошелёк</button>
      <div style="height:9px"></div>
      <button class="btn ghost" onclick="closeSheet()">Вернуться к пакетам</button>`
    : `
      <button class="btn" onclick="mpDoOrderPay()">${I('card')} Оплатить ${fmtMoney(rub)}</button>
      <p class="dim" style="font-size:11px;text-align:center;margin-top:9px">После оплаты создадим рабочий чат: личный ассистент, бриф и ветки работ.</p>`}`;
}
function mpGoTopup(sum){
  closeSheet();
  showTab('wallet');
  if(typeof walOpenTopup==='function') walOpenTopup(sum);
  else toast('Пополни кошелёк на '+fmtMoney(sum)+' и вернись к пакету');
}
function mpDoOrderPay(){
  const p = MP_PACKS[mpOrderPack];
  if(!p) return;
  const rub = p.usd*MP_USD;
  if(!walletCharge(rub, 'Пакет OKO Production · '+p.name)){ mpRenderOrder(); return; }
  okoEarn(rub, 'Пакеты услуг');
  const box = document.getElementById('mpOrderView');
  box.innerHTML = `<div style="text-align:center;padding:26px 0">
    <div class="mp-spin"></div>
    <p style="font-weight:700;margin-top:14px">Активируем пакет «${p.name}»…</p>
    <p class="dim" style="font-size:12px;margin-top:5px">Собираем команду и рабочий чат</p></div>`;
  setTimeout(()=>{
    const chat = mpCreateWorkChat(p);
    MP.orders.unshift({k:p.k, name:p.name, usd:p.usd, rub, chatId:chat.id, at:Date.now()});
    mpSave();
    NOTIFS.unshift({ic:'rocket', who:'OKO Production', t:'пакет «'+p.name+'» активирован — рабочий чат создан', time:'сейчас', g:'Сегодня', unread:true,
      act:()=>{ showTab('chats'); renderChatList(); openConv(chat.id); }});
    if(typeof updateNotifDot==='function') updateNotifDot();
    box.innerHTML = `<div class="mp-ok-wrap">
      <div class="mp-ok">${I('check')}</div>
      <p style="font-weight:800;font-size:19px;margin-top:14px">Пакет «${p.name}» активирован</p>
      <p class="dim" style="font-size:13px;margin-top:6px">Списано ${fmtMoney(rub)} · баланс ${fmtMoney(WALLET.balance)}.<br>Личный ассистент уже ждёт твой бриф.</p>
      <div style="height:16px"></div>
      <button class="btn" onclick="mpOpenWorkChat(${chat.id})">${I('chat')} Открыть рабочий чат</button></div>`;
    toast('Пакет «'+p.name+'» оплачен, чат с командой создан');
  }, 1200);
}
function mpOpenWorkChat(id){
  closeSheet();
  showTab('chats');
  renderChatList();
  openConv(id);
}

/* ---------- рабочий чат клиента ---------- */
function mpNextChatId(){ MP.seq = (MP.seq||0)+1; mpSave(); return 9100 + MP.seq; }
function mpWorkMsgs(p){
  const bn = MP_BRANCHES.map(b=>b[0]).join(' / ');
  const msgs = [
    {kind:'sys', body:'Рабочее пространство OKO Production · пакет «'+p.name+'»'},
    {in:1, who:MP_ASSIST, t:nowT(), kind:'text', body:'Привет, '+PROFILE.name+'! Я твой личный ассистент по пакету «'+p.name+'». Веду проект от брифа до отчёта, на связи каждый день — все вопросы сюда.'},
    {in:1, who:MP_ASSIST, t:nowT(), kind:'text', body:'Состав пакета: '+p.inc.concat(p.bonus).join(' · ')+'. Срок первого цикла — 30 дней.'},
    {in:1, who:MP_ASSIST, t:nowT(), kind:'text', body:'Чтобы стартовать, пришли бриф: 1) ниша и продукт, 2) ссылки на соцсети и сайт, 3) главная цель на месяц, 4) примеры стиля, который нравится. Можно текстом или голосовым.'},
    {kind:'sys', body:'Закреплено · Ветки работ: '+bn+' — навигация сверху'},
  ];
  MP_BRANCHES.forEach(b=>{
    msgs.push({kind:'sys', body:'ВЕТКА · '+b[0]+' — '+b[1]});
  });
  msgs.push({in:1, who:'Продюсер OKO', t:nowT(), kind:'text', body:'На связи. После брифа в ветке «Контент» появится план на 30 роликов, в «Отчёты» — метрики каждую пятницу.'});
  return msgs;
}
function mpCreateWorkChat(p, forcedId){
  const id = forcedId || mpNextChatId();
  const chat = {id, ava:'OP', name:'OKO Production · '+PROFILE.name, kind:'group', managed:true, members:5,
    kindIcon:'rocket', preview:MP_ASSIST+': жду бриф', time:nowT(), unread:0, online:true,
    msgs:mpWorkMsgs(p), mpBranches:MP_BRANCHES.map(b=>b[0])};
  CHATS.unshift(chat);
  return chat;
}
function mpCustomChat(){
  let chat = MP.customChatId && CHATS.find(c=>c.id===MP.customChatId);
  if(!chat){
    const id = mpNextChatId();
    chat = {id, ava:'OP', name:'OKO Production · '+PROFILE.name, kind:'group', managed:true, members:3,
      kindIcon:'rocket', preview:MP_ASSIST+': расскажи о задаче', time:nowT(), unread:0, online:true,
      msgs:[
        {kind:'sys', body:'Обсуждение индивидуального пакета OKO Production'},
        {in:1, who:MP_ASSIST, t:nowT(), kind:'text', body:'Привет, '+PROFILE.name+'! Расскажи о задаче: что за проект, какой масштаб и сроки, какой бюджет комфортен. Соберу под тебя пакет и посчитаю смету за день.'},
        {in:1, who:MP_ASSIST, t:nowT(), kind:'text', body:'Для ориентира: СТАРТ $1500 — контент-завод на месяц, БИЗНЕС $3000 — плюс сайт, воронка и реклама. Индивидуальный может быть и между, и сильно больше.'},
      ], mpBranches:null};
    CHATS.unshift(chat);
    MP.customChatId = id; mpSave();
  }
  showTab('chats');
  renderChatList();
  openConv(chat.id);
}

/* ---------- восстановление рабочих чатов после перезагрузки ---------- */
(function mpRestoreChats(){
  (MP.orders||[]).slice().reverse().forEach(o=>{
    const p = MP_PACKS[o.k];
    if(p && !CHATS.find(c=>c.id===o.chatId)) mpCreateWorkChat(p, o.chatId);
  });
})();

/* ================= ПАТЧИ (chain) ================= */

/* биржа: кнопка-строка кабинета + премиум-баннер над категориями */
const _prevRenderMarketMp = renderMarket;
renderMarket = function(){
  _prevRenderMarketMp();
  if(typeof mkView!=='undefined' && mkView==='cats'){ mpInjectMarket(); mpDecorateCards(); }
};
function mpInjectMarket(){
  const root = document.getElementById('marketRoot');
  if(!root) return;
  const quick = root.querySelector('.mk-quick');
  if(!quick || document.getElementById('mpCabRow')) return;
  const active = MP.deals.filter(d=>MP_ST_OPEN(d.st)).length;
  const row = document.createElement('button');
  row.id = 'mpCabRow'; row.className = 'mp-cab-row'; row.onclick = mpOpenCabinet;
  row.innerHTML = `<span class="mp-cab-ic">${I('crown')}</span>
    <span class="mp-cab-b"><b>Кабинет продавца</b><small>К выводу ${fmtMoney(mpBal())} · активных сделок: ${active}</small></span>
    ${I('chev','mp-chev')}`;
  quick.insertAdjacentElement('afterend', row);
  const bn = document.createElement('button');
  bn.id = 'mpBanner'; bn.className = 'mp-banner'; bn.onclick = mpOpenPackages;
  bn.innerHTML = `<span class="mp-banner-logo"><svg><use href="#i-logo"/></svg></span>
    <span class="mp-banner-b">
      <span class="mp-banner-t">OKO <b>PRODUCTION</b>, под ключ</span>
      <span class="mp-banner-s">Полный цикл роста под ключ: контент, дизайн, сайт, реклама</span>
    </span>
    <span class="mp-banner-cta"><span class="mp-banner-chip">от $1500</span>${I('chev')}</span>`;
  row.insertAdjacentElement('afterend', bn);
}

/* биржа: бейджи «Проверенный» и рейтинг-пилюля на медиа-плейсхолдерах карточек */
function mpIdFromCard(el){
  const m = (el.getAttribute('onclick')||'').match(/openListing\((\d+)\)/);
  return m ? +m[1] : null;
}
function mpRatingPill(l){
  return `<span class="mp-photo-rate">${I('star')}${l.r.toFixed(1)}</span>`;
}
function mpVerMini(){ return `<span class="mp-photo-ver" title="Проверенный продавец">${I('verified')}</span>`; }
function mpDecoratePhoto(photo, l){
  if(!photo || photo.querySelector('.mp-photo-rate')) return;
  if(l.r) photo.insertAdjacentHTML('beforeend', mpRatingPill(l));
  if(mpIsVerified(l)) photo.insertAdjacentHTML('beforeend', mpVerMini());
}
function mpDecorateCards(){
  document.querySelectorAll('#mkResults .lst-card').forEach(card=>{
    const id = mpIdFromCard(card), l = id && LISTINGS.find(x=>x.id===id);
    if(l) mpDecoratePhoto(card.querySelector('.lst-photo'), l);
  });
  document.querySelectorAll('.mk-feat .feat-card').forEach(card=>{
    const id = mpIdFromCard(card), l = id && LISTINGS.find(x=>x.id===id);
    if(l && mpIsVerified(l) && !card.querySelector('.mp-photo-ver'))
      (card.querySelector('.lst-photo')||card).insertAdjacentHTML('beforeend', mpVerMini());
  });
}
if(typeof renderMarketListSoft==='function'){
  const _prevRMLS = renderMarketListSoft;
  renderMarketListSoft = function(){
    const box = document.getElementById('mkResults');
    if(box) box.classList.remove('mp-cascade'); // печать в поиске не должна каждый раз анимировать заново
    _prevRMLS.apply(this, arguments);
    mpDecorateCards();
  };
}
/* каскадное появление карточек — только при полном входе в список (не на каждый символ поиска) */
if(typeof renderMarketList==='function'){
  const _prevRML = renderMarketList;
  renderMarketList = function(){
    _prevRML.apply(this, arguments);
    const box = document.getElementById('mkResults');
    if(box){ void box.offsetWidth; box.classList.add('mp-cascade'); }
  };
}

/* заказ на бирже: покупка уже патчена wallet (эскроу+комиссия) — только читаем результат */
const _prevOrderListingMp = orderListing;
orderListing = function(id){
  const holdBefore = WALLET.hold;
  _prevOrderListingMp(id);
  if(WALLET.hold <= holdBefore) return; // оплата не прошла — сделку не регистрируем
  const l = LISTINGS.find(x=>x.id===id);
  if(!l) return;
  const d = {id:'d'+Date.now(), t:l.t, n:l.n, sum:l.p, st:'work', dir:'out', at:Date.now(), lid:l.id, held:true, chatId:'seller-'+id};
  MP.deals.unshift(d);
  mpSave();
  // связка с мессенджером: в диалоге продавца отмечаем эскроу-защиту
  const chat = CHATS.find(c=>c.id==='seller-'+id) || CHATS.find(c=>c.name===l.n);
  if(chat){ d.chatId = chat.id;
    chat.msgs.push({kind:'sys', body:'Заказ оформлен · '+fmtMoney(l.p)+' в эскроу OKO до подтверждения выполнения'});
    chat.preview = 'Эскроу: '+fmtMoney(l.p)+' в холде'; chat.time = nowT();
    if(typeof renderChatList==='function' && document.getElementById('screen-chats')) renderChatList();
  }
  const sheet = document.getElementById('sheet-mpCab');
  if(sheet && sheet.classList.contains('open')) mpRenderCab();
};

/* чаты: закреплённое сообщение-навигация «ветки работ» */
const _prevOpenConvMp = openConv;
openConv = function(id){
  _prevOpenConvMp(id);
  const old = document.getElementById('mpPinBar');
  if(old) old.remove();
  if(!currentChat || !currentChat.mpBranches) return;
  const msgs = document.getElementById('msgs');
  if(!msgs) return;
  const bar = document.createElement('div');
  bar.id = 'mpPinBar'; bar.className = 'mp-pin';
  bar.innerHTML = `<div class="mp-pin-h">${I('pin')} Закреплено · ветки работ</div>
    <div class="mp-pin-chips">${currentChat.mpBranches.map(b=>
      `<button onclick="mpGoBranch('${b}')">${b}</button>`).join('')}</div>`;
  msgs.parentElement.insertBefore(bar, msgs);
};
function mpGoBranch(name){
  if(!currentChat) return;
  const idx = currentChat.msgs.findIndex(m=>m.kind==='sys' && (m.body||'').indexOf('ВЕТКА · '+name)===0);
  if(idx<0) return;
  const el = document.getElementById('msgs');
  const node = el && el.children[idx];
  if(!node) return;
  node.scrollIntoView({behavior:'smooth', block:'center'});
  node.classList.remove('mp-branch-flash');
  void node.offsetWidth;
  node.classList.add('mp-branch-flash');
}

/* карточка объявления: значок проверенного, «Написать продавцу», меню «Пожаловаться» */
const _prevOpenListingMp = openListing;
openListing = function(id){
  _prevOpenListingMp(id);
  mpDecorateListing(id);
};
function mpDecorateListing(id){
  const l = LISTINGS.find(x=>x.id===id);
  const v = document.getElementById('lstView');
  if(!l || !v) return;
  const bigPhoto = v.querySelector('.lst-photo.big');
  if(bigPhoto && mpIsVerified(l) && !bigPhoto.querySelector('.mp-photo-ver'))
    bigPhoto.insertAdjacentHTML('beforeend', mpVerMini());
  const seller = v.querySelector('.seller-card');
  if(seller){
    const nameEl = seller.querySelector('b');
    if(nameEl && typeof vBadge==='function' && !nameEl.querySelector('svg')) nameEl.insertAdjacentHTML('beforeend', vBadge(l.n));
    if(mpIsVerified(l) && !seller.querySelector('.mp-ver-chip'))
      seller.insertAdjacentHTML('beforeend', `<span class="mp-ver-chip">${I('verified')} Проверенный</span>`);
  }
  if(l.my) return; // на своём объявлении ни чата, ни жалобы
  const write = v.querySelector('.lst-cta .btn:not(.ghost)');
  if(write) write.innerHTML = I('chat')+' Написать продавцу';
  if(!document.getElementById('mpReportBtn')){
    const sent = (MP.reports||[]).some(r=>r.lid===l.id);
    const b = document.createElement('button');
    b.id = 'mpReportBtn';
    b.className = 'mp-report-btn'+(sent?' sent':'');
    b.innerHTML = sent ? I('check')+' Жалоба на рассмотрении модерации'
                       : I('flag')+' Пожаловаться на объявление';
    b.onclick = ()=>mpOpenReport(l.id);
    v.appendChild(b);
  }
}

/* чат с продавцом: автоприветствие при создании/открытии диалога из карточки */
const _prevContactSellerMp = contactSeller;
contactSeller = function(id){
  _prevContactSellerMp(id);
  const l = LISTINGS.find(x=>x.id===id);
  if(!l) return;
  const chat = CHATS.find(c=>c.name===l.n);
  if(!chat) return;
  const greet = 'Здравствуйте! Интересует «'+l.t+'»';
  if(chat.msgs.some(m=>m.kind==='text' && m.body===greet)) return; // уже здоровались по этому объявлению
  if(typeof currentChat!=='undefined' && currentChat===chat && typeof pushMsg==='function'){
    pushMsg({in:0, t:nowT(), kind:'text', body:greet});
  } else {
    chat.msgs.push({in:0, t:nowT(), kind:'text', body:greet});
    chat.preview = 'Ты: '+greet; chat.time = nowT();
  }
};

/* ================= ЖАЛОБА НА ОБЪЯВЛЕНИЕ ================= */
let mpRepLid = null, mpRepReason = null;
const MP_REP_REASONS = ['Мошенничество','Спам и реклама','Запрещённый товар','Цена не соответствует','Оскорбления','Другое'];
function mpOpenReport(id){
  mpRepLid = id; mpRepReason = null;
  mpRenderReport();
  openSheet('mpReport');
}
function mpRenderReport(){
  const l = LISTINGS.find(x=>x.id===mpRepLid);
  const box = document.getElementById('mpReportView');
  if(!l || !box) return;
  const sent = (MP.reports||[]).some(r=>r.lid===l.id);
  box.innerHTML = sent ? `
    <div class="mp-rep-done">${I('flag')}</div>
    <h3 class="mp-rv-title" style="text-align:center">Жалоба на рассмотрении</h3>
    <p class="mp-rv-sub" style="text-align:center">Модерация уже проверяет «${esc(l.t)}». Обычно это занимает до 24 часов.</p>
    <div style="height:14px"></div>
    <button class="btn ghost" onclick="closeSheet()">Понятно</button>`
  : `
    <h3 class="mp-rv-title">Пожаловаться</h3>
    <p class="mp-rv-sub">${esc(l.t)} · ${esc(l.n)}</p>
    <p class="f-lab">Причина жалобы</p>
    <div class="mp-rep-chips" id="mpRepChips">${mpRepChips()}</div>
    <textarea id="mpRepText" class="mp-ta" rows="3" maxlength="240" placeholder="Комментарий для модерации (необязательно)"></textarea>
    <button class="btn" onclick="mpSubmitReport()">${I('flag')} Отправить жалобу</button>
    <p class="mp-rep-note">Жалоба анонимна для продавца и уходит модерации OKO</p>`;
}
function mpRepChips(){
  return MP_REP_REASONS.map((r,i)=>
    `<button class="f-chip ${mpRepReason===r?'on':''}" onclick="mpRepPick(${i})">${r}</button>`).join('');
}
function mpRepPick(i){
  mpRepReason = MP_REP_REASONS[i];
  const el = document.getElementById('mpRepChips');
  if(el) el.innerHTML = mpRepChips(); // перерисовываем только чипы — текст комментария не теряется
}
function mpSubmitReport(){
  const l = LISTINGS.find(x=>x.id===mpRepLid);
  if(!l) return;
  if(!mpRepReason){ toast('Выбери причину жалобы'); return; }
  const txt = ((document.getElementById('mpRepText')||{}).value||'').trim();
  MP.reports.push({lid:l.id, t:l.t, reason:mpRepReason, txt, at:Date.now()});
  mpSave();
  if(typeof ADMIN!=='undefined' && ADMIN.moder){ // chain-доступ к очереди модерации админки
    ADMIN.moder.unshift({t:'Жалоба: «'+l.t+'» — '+mpRepReason, by:'@'+(PROFILE.nick||'user'), kind:'Биржа'});
    if(ADMIN.kpi) ADMIN.kpi.moder = ADMIN.moder.length;
  }
  closeSheet();
  toast('Передано модерации — спасибо, что бережёшь Биржу');
  openListing(mpRepLid); // вернуться в карточку: кнопка уже в статусе «на рассмотрении»
}

/* админка: счётчик и лог жалоб с Биржи во вкладке «Модерация» */
if(typeof admModer==='function'){
  const _prevAdmModerMp = admModer;
  admModer = function(){
    const rep = MP.reports||[];
    const inQ = (typeof ADMIN!=='undefined' && ADMIN.moder) ? ADMIN.moder.filter(m=>(m.t||'').indexOf('Жалоба:')===0).length : 0;
    const rows = rep.slice(-4).reverse().map(r=>`
      <div class="adm-row">
        <span class="adm-tag no">${esc(r.reason)}</span>
        <span class="adm-main"><b style="white-space:normal">${esc(r.t)}</b><small>${esc(r.txt||'без комментария')} · ${mpWhen(r.at)}</small></span>
      </div>`).join('');
    const block = `
      <div class="adm-sec-h">Жалобы с Биржи · всего ${rep.length}${inQ?` · в очереди ${inQ}`:''}</div>
      ${rep.length ? rows : '<p class="dim" style="font-size:13px">Жалоб нет — Биржа чистая.</p>'}`;
    return _prevAdmModerMp() + block;
  };
}

/* ================= ФИЛЬТРЫ: город + «только проверенные» ================= */
if(typeof mkFilters.mpVerif==='undefined') mkFilters.mpVerif = false;
if(typeof mkFilters.city==='undefined')    mkFilters.city   = '';
function mpCities(){
  const seen = [];
  LISTINGS.forEach(l=>{ if(!l.my && l.city && seen.indexOf(l.city)<0) seen.push(l.city); });
  return seen;
}
const _prevMkApplyFiltersMp = mkApplyFilters;
mkApplyFilters = function(arr){
  arr = _prevMkApplyFiltersMp(arr);
  if(mkFilters.city)   arr = arr.filter(l=>l.city===mkFilters.city);
  if(mkFilters.mpVerif) arr = arr.filter(mpIsVerified);
  return arr;
};
const _prevActiveFilterCountMp = activeFilterCount;
activeFilterCount = function(){ return _prevActiveFilterCountMp() + (mkFilters.mpVerif?1:0) + (mkFilters.city?1:0); };
const _prevResetFiltersMp = resetFilters;
resetFilters = function(){ _prevResetFiltersMp(); mkFilters.mpVerif = false; mkFilters.city = ''; };
const _prevRenderFiltersMp = renderFilters;
renderFilters = function(){
  _prevRenderFiltersMp();
  const v = document.getElementById('filtersView');
  if(!v || v.querySelector('.mp-fver-wrap')) return;
  const btnRow = v.lastElementChild; // строка «Сбросить / Показать»
  const cities = mpCities();
  const cityChips = [['','Любой']].concat(cities.map(c=>[c,c]))
    .map(([val,lab])=>`<button class="f-chip ${mkFilters.city===val?'on':''}" onclick="mpSetCity('${val.replace(/'/g,"\\'")}')">${val?I('pos'):''}${lab}</button>`).join('');
  const w = document.createElement('div');
  w.className = 'mp-fver-wrap';
  w.innerHTML = `<p class="f-lab">${I('pos')} Город</p>
    <div class="f-row mp-city-row">${cityChips}</div>
    <p class="f-lab">Продавец</p>
    <button class="f-chip mp-fver ${mkFilters.mpVerif?'on':''}" onclick="mpToggleVerif()">${I('verified')} Только проверенные</button>
    <small>значок OKO или рейтинг продавца 4.8+</small>`;
  v.insertBefore(w, btnRow);
};
function mpKeepPrice(){
  const mn = document.getElementById('fMin'), mx = document.getElementById('fMax');
  if(mn) mkFilters.min = (mn.value||'').replace(/\D/g,'');
  if(mx) mkFilters.max = (mx.value||'').replace(/\D/g,'');
}
function mpToggleVerif(){ mpKeepPrice(); mkFilters.mpVerif = !mkFilters.mpVerif; renderFilters(); }
function mpSetCity(city){ mpKeepPrice(); mkFilters.city = city; renderFilters(); }

/* ================= ФОТО В ОБЪЯВЛЕНИИ (реальная загрузка, downscale, галерея) =================
   База отдаёт .lf-photo как заглушку-тост и не поддерживает фото. Здесь — рабочая загрузка
   с телефона/ПК: FileReader -> canvas-downscale -> dataURL, до 6 фото, превью с удалением,
   рендер на карточках/в детальной, свайп-галерея в карточке. Всё в памяти сессии (LISTINGS
   не персистится в базе — это ок, лишнего в localStorage не пишем). Патчи: openListingForm,
   saveListing, lstPhoto, openListing (галерея). */
let MP_PHOTO_MAX = 10, MP_PHOTO_DIM = 1100; /* v4: Avito-parity — до 10 фото */
let mpFormPhotos = [];

/* уменьшаем картинку до MP_PHOTO_DIM по большей стороне -> лёгкий jpeg dataURL */
function mpDownscale(dataUrl){
  return new Promise(res=>{
    const img = new Image();
    img.onload = function(){
      let {width:w, height:h} = img;
      if(!w || !h){ res(dataUrl); return; }
      const k = Math.min(1, MP_PHOTO_DIM/Math.max(w,h));
      w = Math.round(w*k); h = Math.round(h*k);
      try{
        const cv = document.createElement('canvas'); cv.width = w; cv.height = h;
        const ctx = cv.getContext('2d'); ctx.drawImage(img, 0, 0, w, h);
        res(cv.toDataURL('image/jpeg', 0.82));
      }catch(e){ res(dataUrl); }
    };
    img.onerror = ()=>res(dataUrl);
    img.src = dataUrl;
  });
}
function mpReadFile(file){
  return new Promise(res=>{
    if(!file || !/^image\//.test(file.type||'')){ res(null); return; }
    const fr = new FileReader();
    fr.onload = ()=>mpDownscale(fr.result).then(res);
    fr.onerror = ()=>res(null);
    fr.readAsDataURL(file);
  });
}
function mpAddPhotoFiles(files){
  const list = Array.prototype.slice.call(files||[]);
  if(!list.length) return;
  const free = MP_PHOTO_MAX - mpFormPhotos.length;
  if(free<=0){ toast('Можно до '+MP_PHOTO_MAX+' фото'); return; }
  const take = list.slice(0, free);
  if(list.length>free) toast('Добавлено '+free+' — лимит '+MP_PHOTO_MAX+' фото');
  Promise.all(take.map(mpReadFile)).then(arr=>{
    arr.filter(Boolean).forEach(u=>mpFormPhotos.push(u));
    mpRenderFormPhotos();
  });
}
function mpRemoveFormPhoto(i){ mpFormPhotos.splice(i,1); mpRenderFormPhotos(); }
function mpRenderFormPhotos(){
  const grid = document.getElementById('mpFormPhotos');
  if(!grid) return;
  const full = mpFormPhotos.length>=MP_PHOTO_MAX;
  grid.innerHTML = mpFormPhotos.map((u,i)=>`
    <div class="mp-fp-thumb"${i===0?' data-cover="1"':''}>
      <img src="${u}" alt="">
      ${i===0?'<span class="mp-fp-cover">Обложка</span>':''}
      <button type="button" class="mp-fp-del" onclick="mpRemoveFormPhoto(${i})" aria-label="Удалить">${I('trash')}</button>
    </div>`).join('') +
    (full ? '' : `<button type="button" class="mp-fp-add" onclick="document.getElementById('mpPhotoInput').click()">
      ${I('camera')}<span>${mpFormPhotos.length?'Ещё':'Добавить фото'}</span></button>`);
}
function mpMountPhotoUploader(){
  const v = document.getElementById('listingFormView');
  if(!v) return;
  const tile = v.querySelector('.lf-photo');
  if(!tile || document.getElementById('mpFormPhotos')) return;
  const wrap = document.createElement('div');
  wrap.className = 'mp-fp-wrap';
  wrap.innerHTML = `
    <label class="f-lab" style="margin-top:0">Фото объявления <small class="mp-fp-hint">до ${MP_PHOTO_MAX} · первое станет обложкой</small></label>
    <input type="file" id="mpPhotoInput" accept="image/*" multiple hidden>
    <div class="mp-fp-grid" id="mpFormPhotos"></div>`;
  tile.replaceWith(wrap);
  const input = document.getElementById('mpPhotoInput');
  if(input) input.addEventListener('change', function(){ mpAddPhotoFiles(this.files); this.value=''; });
  mpRenderFormPhotos();
}
const _prevOpenListingFormMp = openListingForm;
openListingForm = function(id){
  _prevOpenListingFormMp(id);
  const l = id ? LISTINGS.find(x=>x.id===id) : null;
  mpFormPhotos = (l && Array.isArray(l.photos)) ? l.photos.slice() : [];
  mpMountPhotoUploader();
};
const _prevSaveListingMp = saveListing;
saveListing = function(){
  const wasEditing = (typeof editingListing!=='undefined') ? editingListing : null;
  const before = LISTINGS.length;
  const photos = mpFormPhotos.slice();
  _prevSaveListingMp();
  let target = null;
  if(wasEditing){ target = LISTINGS.find(x=>x.id===wasEditing) || null; }
  else if(LISTINGS.length>before){ target = LISTINGS[0]; } // база unshift-ит новое объявление
  if(target){
    target.photos = photos;
    if(document.getElementById('marketRoot') && typeof openMyListings==='function') openMyListings();
  }
};

/* рендер медиа: если у объявления есть фото — показываем реальную картинку вместо плейсхолдера */
const _prevLstPhotoMp = lstPhoto;
lstPhoto = function(l, big){
  if(l && Array.isArray(l.photos) && l.photos.length){
    const extra = (big && l.photos.length>1) ? `<span class="mp-photo-count">${I('photo')}${l.photos.length}</span>` : '';
    return `<div class="lst-photo${big?' big':''} mp-has-photo"><img src="${l.photos[0]}" alt="" loading="lazy">${extra}</div>`;
  }
  return _prevLstPhotoMp(l, big);
};

/* детальная карточка: свайп-галерея по фото (тап по миниатюре меняет обложку) */
const _prevOpenListingGal = openListing;
openListing = function(id){
  _prevOpenListingGal(id);
  const l = LISTINGS.find(x=>x.id===id);
  const v = document.getElementById('lstView');
  if(!l || !v || !Array.isArray(l.photos) || l.photos.length<2) return;
  const hero = v.querySelector('.lst-photo.big');
  if(!hero || v.querySelector('.mp-gal')) return;
  const strip = document.createElement('div');
  strip.className = 'mp-gal';
  strip.innerHTML = l.photos.map((u,i)=>
    `<button type="button" class="mp-gal-t${i===0?' on':''}" onclick="mpSwapHero(this,'${'i'+id}',${i})"><img src="${u}" alt=""></button>`).join('');
  hero.insertAdjacentElement('afterend', strip);
  strip.dataset.for = id;
};
function mpSwapHero(btn, _tag, i){
  const v = document.getElementById('lstView'); if(!v) return;
  const strip = v.querySelector('.mp-gal'); if(!strip) return;
  const l = LISTINGS.find(x=>x.id=== +strip.dataset.for); if(!l || !l.photos[i]) return;
  const heroImg = v.querySelector('.lst-photo.big img');
  if(heroImg){ heroImg.style.opacity = '0'; setTimeout(()=>{ heroImg.src = l.photos[i]; heroImg.style.opacity=''; }, 120); }
  strip.querySelectorAll('.mp-gal-t').forEach(b=>b.classList.remove('on'));
  if(btn) btn.classList.add('on');
}

/* ================= AVITO-ГЛУБИНА: сохранённые поиски, «вы смотрели», rich-пустые состояния, эскроу-доверие ================= */
/* Всё аддитивно, поверх базовой Биржи. Персист — в том же MP (localStorage oko-market-pro).
   Ничего из существующих потоков не ломаем: поиск/фильтры/избранное/продвижение/кабинет/пакеты работают как были. */
MP.saved  = MP.saved  || [];   // сохранённые поиски: {id,q,cat,sort,filters,at}
MP.recent = MP.recent || [];   // недавно просмотренные объявления: массив id (свежие спереди, cap 10)
try{ mpSave(); }catch(e){}

/* ---------- подпись/сводка/совпадения сохранённого поиска ---------- */
function mpSearchLabel(s){
  const q = (s && s.q || '').trim();
  if(q) return q;
  if(s && s.cat) return catName(s.cat);
  return 'Все объявления';
}
const MP_TYPE_RU = {service:'услуги', work:'работа', goods:'товары'};
/* объявления, подходящие под сохранённый поиск (read-only, не трогает глобальные mkFilters/mkSearchQ) */
function mpMatchListings(s){
  let arr = LISTINGS.filter(l=>!l.my && l.st==='act');
  if(s.cat) arr = arr.filter(l=>l.cat===s.cat);
  const q = (s.q||'').trim().toLowerCase();
  if(q) arr = arr.filter(l=>(l.t+' '+l.ds+' '+l.n+' '+catName(l.cat)).toLowerCase().includes(q));
  const f = s.filters||{};
  if(f.min)    arr = arr.filter(l=>l.p >= +f.min);
  if(f.max)    arr = arr.filter(l=>l.p <= +f.max);
  if(f.rating) arr = arr.filter(l=>l.r >= f.rating);
  if(f.type && f.type!=='all') arr = arr.filter(l=>l.type===f.type);
  if(f.city)   arr = arr.filter(l=>l.city===f.city);
  if(f.mpVerif) arr = arr.filter(mpIsVerified);
  return arr;
}
/* «N новых по подписке» — стабильный псевдо-счётчик (прототип): детерминирован от поиска и числа совпадений */
function mpSavedNew(s){
  const m = mpMatchListings(s).length;
  if(!m) return 0;
  return (Math.floor((s.at||0)/1e5) + m) % 3; // 0..2, но стабильно для конкретного поиска
}
function mpSearchSummary(s){
  const parts = [];
  if(s.q && s.cat) parts.push(catName(s.cat));
  const f = s.filters||{};
  if(f.city)   parts.push(f.city);
  if(f.mpVerif) parts.push('проверенные');
  if(f.type && f.type!=='all') parts.push(MP_TYPE_RU[f.type]||'');
  if(f.min || f.max) parts.push(((f.min?'от '+fmtN(+f.min):'')+(f.max?' до '+fmtN(+f.max):'')).trim()+' ₽');
  if(f.rating) parts.push('от '+f.rating+' звёзд');
  parts.push(mpMatchListings(s).length+' предлож.');
  return parts.filter(Boolean).join(' · ');
}

/* ---------- сигнатура текущего состояния поиска (для «уже сохранён»/дедуп) ---------- */
function mpCurFilters(){
  return {min:mkFilters.min||'', max:mkFilters.max||'', rating:mkFilters.rating||0,
    type:mkFilters.type||'all', city:mkFilters.city||'', mpVerif:!!mkFilters.mpVerif};
}
function mpSig(q, cat, f){
  return JSON.stringify([(q||'').trim(), cat||'',
    {min:f.min||'', max:f.max||'', rating:f.rating||0, type:f.type||'all', city:f.city||'', mpVerif:!!f.mpVerif}]);
}
function mpSearchMeaningful(){
  const f = mkFilters;
  const hasF = f.min || f.max || f.rating || (f.type && f.type!=='all') || f.city || f.mpVerif;
  return !!((mkSearchQ && mkSearchQ.trim()) || mkCat || hasF);
}
function mpSearchAlreadySaved(){
  const cur = mpSig(mkSearchQ, mkCat, mpCurFilters());
  return (MP.saved||[]).some(s=>mpSig(s.q, s.cat, s.filters||{})===cur);
}

/* ---------- сохранить / перезапустить / удалить поиск ---------- */
function mpSaveSearch(){
  if(!mpSearchMeaningful()){ toast('Задай запрос или фильтры, чтобы сохранить поиск'); return; }
  if(mpSearchAlreadySaved()){ toast('Такой поиск уже сохранён'); return; }
  MP.saved.unshift({id:'s'+Date.now(), q:(mkSearchQ||'').trim(), cat:mkCat||null,
    sort:mkSort, filters:mpCurFilters(), at:Date.now()});
  if(MP.saved.length>12) MP.saved.length = 12;
  mpSave();
  toast('Поиск сохранён — уведомим о новых предложениях');
  const b = document.getElementById('mpSaveBtn');
  if(b){ b.classList.add('saved'); b.innerHTML = I('check')+' Поиск сохранён'; }
  const h = document.querySelector('#mpSaveBar .mp-savebar-hint');
  if(h) h.textContent = 'Уведомим о новых';
}
function mpRunSaved(id){
  const s = (MP.saved||[]).find(x=>x.id===id);
  if(!s) return;
  mkView = 'list'; mkCat = s.cat||null; mkSearchQ = s.q||'';
  if(s.sort) mkSort = s.sort;
  const f = s.filters||{};
  mkFilters.min = f.min||''; mkFilters.max = f.max||''; mkFilters.rating = f.rating||0;
  mkFilters.type = f.type||'all'; mkFilters.city = f.city||''; mkFilters.mpVerif = !!f.mpVerif;
  renderMarket();
}
function mpRemoveSaved(id){
  MP.saved = (MP.saved||[]).filter(x=>x.id!==id);
  mpSave();
  toast('Поиск удалён');
  if(document.getElementById('marketRoot') && typeof mkView!=='undefined' && mkView==='cats') renderMarket();
}

/* ---------- недавно просмотренные ---------- */
function mpTrackRecent(id){
  const l = LISTINGS.find(x=>x.id===id);
  if(!l || l.my) return; // свои объявления в «вы смотрели» не показываем
  MP.recent = (MP.recent||[]).filter(x=>x!==id);
  MP.recent.unshift(id);
  if(MP.recent.length>10) MP.recent.length = 10;
  mpSave();
}
function mpRecentListings(){
  return (MP.recent||[]).map(id=>LISTINGS.find(l=>l.id===id)).filter(l=>l && !l.my).slice(0,10);
}
function mpRecentThumb(l){
  if(Array.isArray(l.photos) && l.photos.length) return `<img src="${l.photos[0]}" alt="" loading="lazy">`;
  const hue = (l.seed*37)%360;
  return `<span class="mp-rc-ico" style="--h:${hue}deg">${I(catIco(l.cat))}</span>`;
}

/* ---------- инъекция «сохранённые поиски» + «вы смотрели» на домашней Бирже (cats) ---------- */
const _mpInjectMarketPrev = mpInjectMarket;
mpInjectMarket = function(){
  _mpInjectMarketPrev.apply(this, arguments);
  try{ mpInjectRecentSaved(); }catch(e){}
};
function mpInjectRecentSaved(){
  const anchor = document.getElementById('mpBanner') || document.getElementById('mpCabRow');
  if(!anchor) return;
  let after = anchor;
  /* сохранённые поиски */
  if(!document.getElementById('mpSaved')){
    const saved = MP.saved||[];
    if(saved.length){
      const el = document.createElement('div');
      el.id = 'mpSaved'; el.className = 'mp-saved';
      el.innerHTML = `<div class="mp-strip-h">${I('bookmark')} Сохранённые поиски</div>
        <div class="mp-saved-row">${saved.map(s=>{
          const nw = mpSavedNew(s);
          return `<div class="mp-ss">
            <button class="mp-ss-run" onclick="mpRunSaved('${s.id}')">
              <span class="mp-ss-t">${esc(mpSearchLabel(s))}</span>
              <span class="mp-ss-s">${esc(mpSearchSummary(s))}</span>
              ${nw?`<span class="mp-ss-new">${I('bell')} +${nw} новых</span>`:''}
            </button>
            <button class="mp-ss-x" onclick="event.stopPropagation();mpRemoveSaved('${s.id}')" aria-label="Удалить поиск">${I('trash')}</button>
          </div>`;
        }).join('')}</div>`;
      after.insertAdjacentElement('afterend', el);
      after = el;
    }
  } else after = document.getElementById('mpSaved');
  /* недавно просмотренные */
  if(!document.getElementById('mpRecent')){
    const rec = mpRecentListings();
    if(rec.length){
      const el = document.createElement('div');
      el.id = 'mpRecent'; el.className = 'mp-recent';
      el.innerHTML = `<div class="mp-strip-h">${I('clock')} Вы смотрели</div>
        <div class="mp-recent-row">${rec.map(l=>`
          <button class="mp-rc" onclick="openListing(${l.id})">
            <span class="mp-rc-ph">${mpRecentThumb(l)}</span>
            <span class="mp-rc-p">${esc(l.pt)}</span>
            <span class="mp-rc-t">${esc(l.t)}</span>
          </button>`).join('')}</div>`;
      after.insertAdjacentElement('afterend', el);
    }
  }
}

/* ---------- кнопка «Сохранить поиск» под тулбаром списка ---------- */
if(typeof renderMarketList==='function'){
  const _mpRMLsave = renderMarketList;
  renderMarketList = function(){
    _mpRMLsave.apply(this, arguments);
    try{ mpInjectSaveBtn(); }catch(e){}
  };
}
function mpInjectSaveBtn(){
  const root = document.getElementById('marketRoot');
  if(!root) return;
  if(typeof mkView!=='undefined' && mkView==='fav') return; // в избранном сохранять нечего
  if(document.getElementById('mpSaveBar')) return;
  const bar = root.querySelector('.mk-toolbar');
  if(!bar || !mpSearchMeaningful()) return;
  const saved = mpSearchAlreadySaved();
  const w = document.createElement('div');
  w.id = 'mpSaveBar'; w.className = 'mp-savebar';
  w.innerHTML = `<button id="mpSaveBtn" class="mp-savebar-btn${saved?' saved':''}" onclick="mpSaveSearch()">
      ${saved ? I('check')+' Поиск сохранён' : I('bookmark')+' Сохранить поиск'}</button>
    <span class="mp-savebar-hint">${saved?'Уведомим о новых':'Сохраните — пришлём новые раньше всех'}</span>`;
  bar.insertAdjacentElement('afterend', w);
}

/* ---------- rich-пустые состояния (избранное / 0 результатов) ---------- */
function mpEmpBadge(ic){ return `<span class="mp-emp-badge">${I(ic)}</span>`; }
function mpRichEmpty(){
  const box = document.getElementById('mkResults');
  if(!box) return;
  const empty = box.querySelector('.mk-empty');
  if(!empty || box.querySelector('.mp-emp')) return; // либо результаты есть, либо уже нарисовано
  const fav = (typeof mkView!=='undefined' && mkView==='fav');
  if(fav){
    box.innerHTML = `<div class="mp-emp">
      ${mpEmpBadge('heart')}
      <b>В избранном пусто</b>
      <p>Нажимай на сердечко у объявлений — они появятся здесь, чтобы вернуться к ним позже.</p>
      <button class="btn" onclick="mkView='list';mkCat=null;mkSearchQ='';renderMarket()">${I('search')} Смотреть объявления</button>
    </div>`;
    return;
  }
  const cats = MK_CATS.filter(c=>LISTINGS.some(l=>l.cat===c.k && !l.my && l.st==='act')).slice(0,4);
  const hasF = (typeof activeFilterCount==='function' && activeFilterCount()>0);
  const hasQ = !!(mkSearchQ && mkSearchQ.trim());
  box.innerHTML = `<div class="mp-emp">
    ${mpEmpBadge('search')}
    <b>Ничего не найдено</b>
    <p>${hasQ?'По запросу «'+esc(mkSearchQ.trim())+'» ничего нет. ':''}Попробуй изменить запрос${hasF?' или сбросить фильтры':''}.</p>
    ${hasF?`<button class="btn ghost" onclick="mpEmptyReset()">${I('poll')} Сбросить фильтры</button>`:''}
    ${cats.length?`<div class="mp-emp-lab">Популярные категории</div>
      <div class="mp-emp-cats">${cats.map(c=>`<button class="mp-emp-cat" onclick="mpEmptyGoCat('${c.k}')">${I(c.ic)}<span>${c.name}</span></button>`).join('')}</div>`:''}
    ${(hasQ||hasF)?`<button class="mp-emp-save" onclick="mpSaveSearch()">${I('bell')} Сохранить поиск</button>`:''}
  </div>`;
}
function mpEmptyReset(){
  if(typeof resetFilters==='function') resetFilters();
  renderMarketList(document.getElementById('marketRoot'), typeof mkView!=='undefined' && mkView==='fav');
}
function mpEmptyGoCat(k){
  if(typeof resetFilters==='function') resetFilters();
  mkSearchQ = '';
  if(typeof openMarketCat==='function') openMarketCat(k);
}
/* оборачиваем soft-рендер (уже пропатчен модулем) — дорисовываем rich-пустое после базового */
if(typeof renderMarketListSoft==='function'){
  const _mpRMLSempty = renderMarketListSoft;
  renderMarketListSoft = function(){
    _mpRMLSempty.apply(this, arguments);
    try{ mpRichEmpty(); }catch(e){}
  };
}

/* ---------- rich-пустое состояние «Мои объявления» ---------- */
if(typeof openMyListings==='function'){
  const _mpOpenMyListings = openMyListings;
  openMyListings = function(){
    _mpOpenMyListings.apply(this, arguments);
    try{ mpRichSellerEmpty(); }catch(e){}
  };
}
function mpRichSellerEmpty(){
  const root = document.getElementById('marketRoot');
  if(!root) return;
  const empty = root.querySelector('.mk-empty');
  if(!empty || root.querySelector('.mp-emp')) return;
  const trailing = empty.nextElementSibling; // базовая кнопка «Разместить объявление» — убираем дубль
  empty.outerHTML = `<div class="mp-emp seller">
    ${mpEmpBadge('briefcase')}
    <b>У тебя ещё нет объявлений</b>
    <p>Размести первую услугу или товар бесплатно — покупатели с Биржи найдут тебя сами, а сделки защитит эскроу OKO.</p>
    <button class="btn" onclick="openListingForm()">${I('plus')} Разместить объявление</button>
  </div>`;
  if(trailing && trailing.tagName==='BUTTON') trailing.remove();
}

/* ---------- доверие покупателю на карточке: эскроу + «на OKO с …» / отклик ---------- */
const MP_REPLY_RU = ['несколько минут','час','пару часов'];
function mpSellerSince(l){ return 2019 + (l.seed%6); }        // «на OKO с 2019…2024» — стабильно от seed
function mpSellerRate(l){ return 90 + (l.seed%9); }           // 90…98% ответов
function mpSellerReply(l){ return MP_REPLY_RU[l.seed%3]; }
function mpTrustBlock(l){
  return `<div class="mp-trust" id="mpTrust">
    <div class="mp-trust-head">
      <span class="mp-trust-ic">${I('lock')}</span>
      <div class="mp-trust-b"><b>Безопасная сделка через эскроу OKO</b>
        <small>Деньги замораживаются на счёте OKO и уходят исполнителю только после того, как вы подтвердите выполнение. При споре — возврат.</small></div>
    </div>
    <div class="mp-trust-row">
      <span>${I('check')} Возврат при спорах</span>
      <span>${I('lock')} Защита платежа</span>
    </div>
  </div>`;
}
/* трекинг «вы смотрели» + блок доверия — финальная обёртка openListing (после декора/галереи) */
const _mpOpenListingTrust = openListing;
openListing = function(id){
  _mpOpenListingTrust.apply(this, arguments);
  try{ mpTrackRecent(id); }catch(e){}
  try{
    const l = LISTINGS.find(x=>x.id===id);
    const v = document.getElementById('lstView');
    if(!l || !v || l.my) return; // на своей карточке эскроу-подсказки покупателю не нужны
    /* строка о продавце в карточке продавца */
    const seller = v.querySelector('.seller-card');
    if(seller && !seller.querySelector('.mp-seller-meta')){
      const info = (seller.querySelector('b') && seller.querySelector('b').parentElement) || seller;
      info.insertAdjacentHTML('beforeend',
        `<div class="mp-seller-meta">${I('crown')} на OKO с ${mpSellerSince(l)} · ${I('chat')} отвечает за ${mpSellerReply(l)} · ${mpSellerRate(l)}% ответов</div>`);
    }
    /* блок эскроу-доверия над кнопками действий */
    if(!document.getElementById('mpTrust')){
      const cta = v.querySelector('.lst-cta');
      if(cta) cta.insertAdjacentHTML('beforebegin', mpTrustBlock(l));
      else v.insertAdjacentHTML('beforeend', mpTrustBlock(l));
    }
  }catch(e){}
};

/* ================= ДИСКРЕТНАЯ СОРТИРОВКА: видимые чипы вместо слепого циклера =================
   База показывает одну кнопку cycleSort — тап вслепую меняет режим по кругу, режимов не видно.
   Заменяем на строку чипов со всеми режимами MK_SORTS: активный подсвечен, тап ставит режим
   напрямую. Базовую кнопку прячем (не удаляем — она пересоздаётся в innerHTML базой). */
const MP_SORT_IC = {pop:'fire', cheap:'chev', exp:'chev', rating:'star', new:'bolt'};
function mpSetSort(k){
  if(typeof mkSort==='undefined' || mkSort===k) return;
  mkSort = k;
  renderMarketList(document.getElementById('marketRoot'), typeof mkView!=='undefined' && mkView==='fav');
}
function mpInjectSortBar(){
  const root = document.getElementById('marketRoot');
  if(!root || typeof MK_SORTS==='undefined') return;
  const bar = root.querySelector('.mk-toolbar');
  if(!bar) return;
  const cyc = bar.querySelector('.mk-tool[onclick*="cycleSort"]'); // слепой циклер базы — прячем
  if(cyc) cyc.style.display = 'none';
  if(document.getElementById('mpSortBar')) return;
  const w = document.createElement('div');
  w.id = 'mpSortBar'; w.className = 'mp-sortbar';
  w.innerHTML = `<div class="mp-sort-row">${MK_SORTS.map(s=>{
    const dir = s[0]==='cheap' ? ' dn' : s[0]==='exp' ? ' up' : '';
    return `<button class="mp-sc${mkSort===s[0]?' on':''}${dir}" onclick="mpSetSort('${s[0]}')">${I(MP_SORT_IC[s[0]]||'poll')}${s[1]}</button>`;
  }).join('')}</div>`;
  bar.insertAdjacentElement('afterend', w); // сразу под тулбаром (перед mpSaveBar, если он есть)
}
if(typeof renderMarketList==='function'){
  const _mpRMLsort = renderMarketList;
  renderMarketList = function(){
    _mpRMLsort.apply(this, arguments);
    try{ mpInjectSortBar(); }catch(e){}
  };
}

/* ================= ПОХОЖИЕ ОБЪЯВЛЕНИЯ (кросс-продажа на детальной карточке) =================
   Классический маркетплейс-приём: под объявлением — лента похожих. Ранжируем по близости
   (та же категория -> тот же тип -> продвижение/рейтинг/просмотры) и расширяем по всему
   каталогу, чтобы лента была не пустой даже когда в категории мало предложений. */
function mpSimilar(l){
  const rank = p => (typeof promoRank==='function' ? promoRank(p) : 0);
  const arr = LISTINGS.filter(x=>!x.my && x.st==='act' && x.id!==l.id);
  arr.sort((a,b)=>
    ((b.cat===l.cat)-(a.cat===l.cat)) ||
    ((b.type===l.type)-(a.type===l.type)) ||
    (rank(b.promo)-rank(a.promo)) ||
    ((b.r||0)-(a.r||0)) ||
    ((b.views||0)-(a.views||0)));
  return arr.slice(0,8);
}
function mpSimilarBlock(l){
  const sim = mpSimilar(l);
  if(sim.length<2) return '';
  return `<div class="mp-sim" id="mpSim">
    <div class="mp-strip-h">${I('grid')} Похожие объявления</div>
    <div class="mp-recent-row">${sim.map(s=>`
      <button class="mp-rc" onclick="openListing(${s.id})">
        <span class="mp-rc-ph">${mpRecentThumb(s)}${s.r?`<span class="mp-rc-rate">${I('star')}${s.r.toFixed(1)}</span>`:''}</span>
        <span class="mp-rc-p">${esc(s.pt)}</span>
        <span class="mp-rc-t">${esc(s.t)}</span>
      </button>`).join('')}</div>
  </div>`;
}
const _mpOpenListingSim = openListing;
openListing = function(id){
  _mpOpenListingSim.apply(this, arguments);
  try{
    const l = LISTINGS.find(x=>x.id===id);
    const v = document.getElementById('lstView');
    if(!l || !v || l.my || document.getElementById('mpSim')) return;
    v.insertAdjacentHTML('beforeend', mpSimilarBlock(l));
  }catch(e){}
};

/* ================= AVITO-МЕХАНИКИ v2: отзыв-плашка, портфолио продавца, быстрые чипы,
   цветной индикатор ответа, попап «как работает эскроу». Всё аддитивно, поверх базовой Биржи. */

/* ---------- индикатор ответа: скорость + цвет + минуты ---------- */
function mpReplyMeta(l){
  const k = l.seed % 3;
  if(k===0){ const m = 10 + (l.seed%16); return {cls:'fast', lab:'отвечает за '+m+' мин', short:'за '+m+' мин'}; }
  if(k===1){ return {cls:'mid', lab:'отвечает в течение часа', short:'за 1 ч'}; }
  const h = 2 + (l.seed%3); return {cls:'slow', lab:'отвечает за '+h+' ч', short:'за '+h+' ч'};
}
function mpIsFast(l){ return mpReplyMeta(l).cls==='fast'; }
function mpIsOnline(l){ return (l.seed%2)===0; }
function mpHasReviews(l){ return Array.isArray(l.reviews) && l.reviews.length>0; }

/* ---------- плашка «Отзыв продавца»: рейтинг + число + одна цитата ---------- */
function mpTopReview(l){
  const rv = l.reviews||[];
  if(!rv.length) return null;
  return rv.slice().sort((a,b)=>(b.r-a.r) || ((b.t||'').length-(a.t||'').length))[0];
}
function mpSellerReviewCard(l){
  if(!mpHasReviews(l)) return '';
  const top = mpTopReview(l);
  const rv = l.reviews||[];
  const cnt = rv.length;
  const word = cnt%10===1 && cnt%100!==11 ? 'отзыв'
             : (cnt%10>=2 && cnt%10<=4 && (cnt%100<10||cnt%100>=20)) ? 'отзыва' : 'отзывов';
  return `<div class="mp-srv" id="mpSrv">
    <div class="mp-srv-head">
      <span class="mp-srv-big"><b>${l.r.toFixed(1)}</b>${I('star')}</span>
      <div class="mp-srv-b">
        <b>Отзывы о продавце</b>
        <small>${stars(l.r)} · ${cnt} ${word}</small>
      </div>
    </div>
    <div class="mp-srv-quote">
      ${I('comment')}
      <div><p>«${esc(top.t)}»</p><small>${esc(top.n)} · ${stars(top.r)}</small></div>
    </div>
  </div>`;
}

/* ---------- портфолио продавца: другие услуги того же n ---------- */
function mpSellerOther(l){
  return LISTINGS.filter(x=>x.n===l.n && x.id!==l.id && !x.my && x.st==='act').slice(0,6);
}
function mpPortfolioBlock(l){
  const arr = mpSellerOther(l);
  if(!arr.length) return '';
  return `<div class="mp-portf" id="mpPortf">
    <div class="mp-strip-h">${I('grid')} Портфолио продавца · ${arr.length}</div>
    <div class="mp-recent-row">${arr.map(s=>`
      <button class="mp-rc" onclick="openListing(${s.id})">
        <span class="mp-rc-ph">${mpRecentThumb(s)}${s.r?`<span class="mp-rc-rate">${I('star')}${s.r.toFixed(1)}</span>`:''}</span>
        <span class="mp-rc-p">${esc(s.pt)}</span>
        <span class="mp-rc-t">${esc(s.t)}</span>
      </button>`).join('')}</div>
  </div>`;
}

/* ---------- инъекция плашки отзывов + портфолио в карточку объявления ---------- */
const _mpOpenListingRev2 = openListing;
openListing = function(id){
  _mpOpenListingRev2.apply(this, arguments);
  try{
    const l = LISTINGS.find(x=>x.id===id);
    const v = document.getElementById('lstView');
    if(!l || !v) return;
    /* плашка отзывов сразу после карточки продавца */
    const seller = v.querySelector('.seller-card');
    if(seller && !v.querySelector('#mpSrv')){
      const html = mpSellerReviewCard(l);
      if(html) seller.insertAdjacentHTML('afterend', html);
    }
    /* индикатор ответа: цветной чип + онлайн-точка — заменяем текстовую meta-строку */
    const meta = v.querySelector('.mp-seller-meta');
    if(meta && !meta.querySelector('.mp-reply')){
      const r = mpReplyMeta(l);
      const on = mpIsOnline(l);
      const onlineChip = on
        ? `<span class="mp-online on">онлайн сейчас</span>`
        : `<span class="mp-online off">был ${1+(l.seed%9)} ч назад</span>`;
      const rateN = (typeof mpSellerRate==='function') ? mpSellerRate(l) : 95;
      const since = (typeof mpSellerSince==='function') ? mpSellerSince(l) : 2020;
      meta.innerHTML = `
        <span class="mp-reply ${r.cls}">${I('clock')} ${r.lab}</span>
        ${onlineChip}
        <span class="mp-meta-dot">${I('crown')} на OKO с ${since} · ${rateN}% ответов</span>`;
    }
    /* портфолио продавца — перед похожими объявлениями (или в конец) */
    if(!l.my && !v.querySelector('#mpPortf')){
      const html = mpPortfolioBlock(l);
      if(html){
        const sim = v.querySelector('#mpSim');
        if(sim) sim.insertAdjacentHTML('beforebegin', html);
        else v.insertAdjacentHTML('beforeend', html);
      }
    }
    /* делаем эскроу-блок кликабельным + добавляем строку-подсказку */
    const trust = v.querySelector('#mpTrust');
    if(trust && !trust.classList.contains('clickable')){
      trust.classList.add('clickable');
      trust.setAttribute('role','button');
      trust.setAttribute('tabindex','0');
      trust.addEventListener('click', mpEscInfoOpen);
      trust.addEventListener('keydown', e=>{ if(e.key==='Enter'||e.key===' '){ e.preventDefault(); mpEscInfoOpen(); } });
      const row = trust.querySelector('.mp-trust-row');
      if(row && !row.querySelector('.mp-trust-more'))
        row.insertAdjacentHTML('beforeend',
          `<span class="mp-trust-more">${I('chev')} как это работает</span>`);
    }
  }catch(e){}
};

/* ---------- попап «Как работает безопасная сделка» ---------- */
const MP_ESC_STEPS = [
  ['card',      'Оплата',    'Ты платишь с кошелька OKO. Деньги замораживаются на счёте площадки — исполнитель их пока не видит.'],
  ['briefcase', 'Работа',    'Исполнитель принимает заказ и начинает работу. В любой момент можно писать в чат по сделке — он в разделе Кабинет.'],
  ['eye',       'Проверка',  'Готовую работу исполнитель сдаёт. У тебя есть время всё проверить: сделка на холде, деньги ещё у OKO.'],
  ['check',     'Выплата',   'Всё нравится — подтверждаешь получение. Только тогда деньги уходят исполнителю, а тебя просят оставить отзыв.'],
  ['flag',      'Спор',      'Что-то не так — открываешь спор. Модерация OKO разбирает переписку и возвращает деньги, если работа не выполнена.'],
];
function mpEscInfoOpen(){
  if(document.getElementById('mpEscModal')) return;
  const wrap = document.createElement('div');
  wrap.id = 'mpEscModal';
  wrap.className = 'mp-escm';
  wrap.innerHTML = `
    <div class="mp-escm-back" onclick="mpEscInfoClose()"></div>
    <div class="mp-escm-card" role="dialog" aria-label="Безопасная сделка">
      <button class="mp-escm-x" onclick="mpEscInfoClose()" aria-label="Закрыть">${I('back')}</button>
      <div class="mp-escm-head">
        <span class="mp-escm-ic">${I('lock')}</span>
        <div><b>Безопасная сделка</b><small>Эскроу через кошелёк OKO · комиссия 10% только с продавца</small></div>
      </div>
      <div class="mp-escm-list">
        ${MP_ESC_STEPS.map((s,i)=>`
          <div class="mp-escm-step">
            <span class="mp-escm-n">${i+1}</span>
            <div class="mp-escm-b">
              <b>${I(s[0])} ${s[1]}</b>
              <p>${s[2]}</p>
            </div>
          </div>`).join('')}
      </div>
      <div class="mp-escm-foot">
        ${I('check')} <span>Возврат при спорах гарантирован — деньги хранятся на счёте OKO до подтверждения.</span>
      </div>
      <button class="btn" onclick="mpEscInfoClose()">Понятно</button>
    </div>`;
  document.body.appendChild(wrap);
  requestAnimationFrame(()=>wrap.classList.add('open'));
  document.addEventListener('keydown', mpEscKey);
}
function mpEscInfoClose(){
  const w = document.getElementById('mpEscModal');
  if(!w) return;
  w.classList.remove('open');
  document.removeEventListener('keydown', mpEscKey);
  setTimeout(()=>{ if(w.parentNode) w.parentNode.removeChild(w); }, 220);
}
function mpEscKey(e){ if(e.key==='Escape') mpEscInfoClose(); }

/* ================= БЫСТРЫЕ ЧИПЫ: онлайн, с отзывами, быстрый ответ ================= */
if(typeof mkFilters.mpOnline==='undefined')  mkFilters.mpOnline  = false;
if(typeof mkFilters.mpHasRev==='undefined')  mkFilters.mpHasRev  = false;
if(typeof mkFilters.mpFast==='undefined')    mkFilters.mpFast    = false;

const _prevMkApplyFiltersQuick = mkApplyFilters;
mkApplyFilters = function(arr){
  arr = _prevMkApplyFiltersQuick(arr);
  if(mkFilters.mpOnline) arr = arr.filter(mpIsOnline);
  if(mkFilters.mpHasRev) arr = arr.filter(mpHasReviews);
  if(mkFilters.mpFast)   arr = arr.filter(mpIsFast);
  return arr;
};
const _prevActiveFilterCountQuick = activeFilterCount;
activeFilterCount = function(){
  return _prevActiveFilterCountQuick()
    + (mkFilters.mpOnline?1:0) + (mkFilters.mpHasRev?1:0) + (mkFilters.mpFast?1:0);
};
const _prevResetFiltersQuick = resetFilters;
resetFilters = function(){
  _prevResetFiltersQuick();
  mkFilters.mpOnline = false; mkFilters.mpHasRev = false; mkFilters.mpFast = false;
};
function mpToggleQuick(k){
  mkFilters[k] = !mkFilters[k];
  renderMarketList(document.getElementById('marketRoot'), typeof mkView!=='undefined' && mkView==='fav');
}
function mpInjectQuickChips(){
  const root = document.getElementById('marketRoot');
  if(!root) return;
  if(document.getElementById('mpQuickChips')) return;
  const bar = root.querySelector('.mk-toolbar');
  if(!bar) return;
  const w = document.createElement('div');
  w.id = 'mpQuickChips'; w.className = 'mp-qchips';
  const chip = (k, ic, lab) =>
    `<button class="mp-qc${mkFilters[k]?' on':''}" onclick="mpToggleQuick('${k}')">${I(ic)}<span>${lab}</span></button>`;
  w.innerHTML = `<div class="mp-qc-row">
    ${chip('mpOnline', 'circle-play', 'Онлайн сейчас')}
    ${chip('mpHasRev', 'star',        'С отзывами')}
    ${chip('mpFast',   'bolt',        'Быстрый ответ')}
  </div>`;
  bar.insertAdjacentElement('afterend', w);
}
if(typeof renderMarketList==='function'){
  const _mpRMLquick = renderMarketList;
  renderMarketList = function(){
    _mpRMLquick.apply(this, arguments);
    try{ mpInjectQuickChips(); }catch(e){}
  };
}

/* ================================================================================
   MARKET-PRO v3 · биржа услуг уровня Avito + Fiverr + Upwork
   Всё аддитивно поверх базовой Биржи и уже подключённых патчей market-pro.
   9 больших блоков (см. mpV3-заголовки ниже). Персист — тот же MP (localStorage).
   ================================================================================ */

/* ---------- ЕДИНАЯ БАЗА ДАННЫХ V3 (миграция без потерь) ---------- */
MP.revealedTg   = MP.revealedTg   || {};       // {lid: '@nickname'} — оплаченные раскрытия TG
MP.tgRevealCount= MP.tgRevealCount|| 0;
MP.bumps        = MP.bumps        || {};       // {lid: expiresAt}    — активные bump'ы (24ч)
MP.searchHist   = MP.searchHist   || [];       // ['ролики', ...]     — история поиска
try{ mpSave(); }catch(e){}

/* ---------- SVG-ЩИТ ВЕРИФИКАЦИИ (свой символ, чтобы не завязываться на i-verified размер) ---------- */
(function mpAddShieldSymbol(){
  const defs = document.querySelector('svg defs');
  if(!defs || document.getElementById('i-mpshield')) return;
  const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
  s.setAttribute('id','i-mpshield'); s.setAttribute('viewBox','0 0 100 100');
  s.innerHTML = '<path d="M50 8 18 22v28c0 20 14 34 32 42 18-8 32-22 32-42V22z"/>';
  defs.appendChild(s);
  const t = document.createElementNS('http://www.w3.org/2000/svg','symbol');
  t.setAttribute('id','i-mptg'); t.setAttribute('viewBox','0 0 100 100');
  t.innerHTML = '<circle cx="50" cy="50" r="42" fill="currentColor" stroke="none"/><path d="M28 51l40-17c2-1 3 0 2 2l-6 30c-1 2-2 3-4 2l-11-8-5 5c-1 1-2 1-3-1l-2-12 26-24-30 20-8-3c-2-1-2-2 1-3z" fill="#0d0d0d" stroke="none"/>';
  defs.appendChild(t);
})();

/* ============================================================
   A · SAFE DEAL — компактный 3-шаговый визуал на карточке
   ============================================================ */
const MP_SAFE_STEPS = [
  ['card',      'Заказ',      'выбираешь услугу и оформляешь безопасно'],
  ['lock',      'Предоплата', 'деньги замораживаются в эскроу OKO'],
  ['check',     'Приёмка',    'работа сдана — подтверждаешь, деньги уходят исполнителю'],
];
function mpSafeMini(){
  return `<div class="mp-safe" id="mpSafe" role="button" tabindex="0" onclick="mpEscInfoOpen()">
    <div class="mp-safe-h">
      <span class="mp-safe-ic">${I('lock')}</span>
      <div class="mp-safe-b">
        <b>Безопасная сделка · 3 шага</b>
        <small>Эскроу OKO защищает деньги · возврат при спорах</small>
      </div>
      <span class="mp-safe-more">${I('chev')}</span>
    </div>
    <div class="mp-safe-line">
      ${MP_SAFE_STEPS.map((s,i)=>`
        <div class="mp-safe-step">
          <span class="mp-safe-dot">${I(s[0])}</span>
          <b>${s[1]}</b>
          <small>${s[2]}</small>
        </div>
        ${i<MP_SAFE_STEPS.length-1?'<span class="mp-safe-arrow"></span>':''}
      `).join('')}
    </div>
  </div>`;
}

/* ============================================================
   B · СВЯЗАТЬСЯ: OKO-чат (главная CTA) + Показать TG (платно 100 ₽)
   ============================================================ */
const MP_TG_PRICE = 100;
function mpFakeTg(l){
  /* стабильный ник от seed */
  const alph = 'abcdefghijklmnopqrstuvwxyz';
  let s = ''; let n = (l.seed||1) * 9301 + 49297;
  const nick = (l.n||'user').split(' ')[0].toLowerCase().replace(/[^a-z]/g,'');
  const base = nick.length>2 ? nick : 'oko';
  for(let i=0;i<4;i++){ n = (n*9301+49297) % 233280; s += alph[n%26]; }
  return '@'+base+'_'+s;
}
function mpTgMasked(l){
  const full = mpFakeTg(l);
  return full.slice(0, Math.min(full.length, 4)) + '•••';
}
function mpContactBlock(l){
  const revealed = MP.revealedTg[l.id];
  const tgLine = revealed
    ? `<button class="mp-tg-row revealed" onclick="mpTgCopy('${l.id}')">
         <span class="mp-tg-ic">${I('mptg')}</span>
         <div class="mp-tg-b"><b>${esc(revealed)}</b><small>Telegram · нажми, чтобы скопировать</small></div>
         <span class="mp-tg-hint">${I('copy')}</span>
       </button>`
    : `<button class="mp-tg-row" onclick="mpTgReveal(${l.id})">
         <span class="mp-tg-ic locked">${I('lock')}</span>
         <div class="mp-tg-b"><b>Показать Telegram · ${mpTgMasked(l)}</b><small>Разовое раскрытие контакта продавца · ${MP_TG_PRICE} ₽ с кошелька</small></div>
         <span class="mp-tg-price">${MP_TG_PRICE} ₽</span>
       </button>`;
  return `<div class="mp-contact" id="mpContact">
    <div class="mp-strip-h">${I('chat')} Связаться с продавцом</div>
    <button class="btn mp-contact-cta" onclick="mpChatFromContact(${l.id})">${I('chat')} Написать в OKO-чат</button>
    <p class="mp-contact-sub">${I('lock')} Все сообщения по этому объявлению защищены эскроу · рекомендуем.</p>
    ${tgLine}
  </div>`;
}
function mpChatFromContact(id){ if(typeof contactSeller==='function') contactSeller(id); }
function mpTgReveal(id){
  const l = LISTINGS.find(x=>x.id===id); if(!l) return;
  if(MP.revealedTg[id]) return; // уже открыт
  showPopup({ico:'mptg', title:'Показать номер Telegram?',
    body:'Разовое раскрытие контакта продавца «'+esc(l.n)+'» — <b>'+MP_TG_PRICE+' ₽</b> с кошелька OKO. Дальнейшие показы бесплатны.',
    actions:[
      {label:'Отмена', ghost:true, onclick:()=>closePopup()},
      {label:'Показать за '+MP_TG_PRICE+' ₽', onclick:()=>{
        closePopup();
        if(!walletCharge(MP_TG_PRICE, 'Раскрытие TG · '+l.n)) return;
        if(typeof okoEarn==='function') okoEarn(MP_TG_PRICE, 'Раскрытие TG');
        MP.revealedTg[id] = mpFakeTg(l);
        MP.tgRevealCount = (MP.tgRevealCount||0)+1;
        mpSave();
        toast('Контакт открыт: '+MP.revealedTg[id]);
        openListing(id); // перерисовать карточку
      }}
    ]});
}
function mpTgCopy(id){
  const nick = MP.revealedTg[id]; if(!nick) return;
  try{ navigator.clipboard && navigator.clipboard.writeText(nick); }catch(e){}
  toast('Скопировано: '+nick);
}

/* ============================================================
   C · РЕЙТИНГ С ТРЕЙЛОМ — 30-дневный SVG-sparkline + «93% в срок»
   ============================================================ */
function mpTrendPoints(seed, n){
  const pts = [];
  let s = (seed || 1) >>> 0;
  for(let i=0;i<n;i++){
    s = (s * 1103515245 + 12345) & 0x7fffffff;
    const base = 3.6 + Math.sin(i/3)*0.35 + Math.cos(i/7)*0.25;
    const jit = ((s%1000)/1000 - 0.5) * 0.7;
    let v = base + jit + (i/n)*0.7; // общий подъём
    v = Math.max(3.0, Math.min(5.0, v));
    pts.push(v);
  }
  return pts;
}
function mpSparkline(seed){
  const pts = mpTrendPoints(seed, 30);
  const W = 240, H = 44, PAD = 3;
  const min = 3.0, max = 5.0;
  const xs = i => PAD + i*(W-PAD*2)/(pts.length-1);
  const ys = v => PAD + (H-PAD*2)*(1-(v-min)/(max-min));
  const path = pts.map((v,i)=>(i?'L':'M')+xs(i).toFixed(1)+' '+ys(v).toFixed(1)).join(' ');
  const fill = `M${xs(0)} ${H-PAD} `+ pts.map((v,i)=>'L'+xs(i).toFixed(1)+' '+ys(v).toFixed(1)).join(' ') +` L${xs(pts.length-1)} ${H-PAD} Z`;
  const last = pts[pts.length-1], lastX = xs(pts.length-1), lastY = ys(last);
  return `<svg class="mp-spark" viewBox="0 0 ${W} ${H}" preserveAspectRatio="none">
    <defs><linearGradient id="mpsparkg" x1="0" x2="0" y1="0" y2="1">
      <stop offset="0" stop-color="#9AFF00" stop-opacity=".55"/><stop offset="1" stop-color="#9AFF00" stop-opacity="0"/></linearGradient></defs>
    <path d="${fill}" fill="url(#mpsparkg)" stroke="none"/>
    <path d="${path}" fill="none" stroke="#9AFF00" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
    <circle cx="${lastX.toFixed(1)}" cy="${lastY.toFixed(1)}" r="3" fill="#9AFF00"/>
  </svg>`;
}
function mpOnTimeRate(l){ return 88 + ((l.seed||0) % 11); } // 88..98%
function mpRatingTrail(l){
  const on = mpOnTimeRate(l);
  return `<div class="mp-trail" id="mpTrail">
    <div class="mp-trail-top">
      <div><b>Рейтинг за 30 дней</b><small>динамика оценок клиентов</small></div>
      <div class="mp-trail-badge">${I('check')} ${on}% сделок закрыто в срок</div>
    </div>
    <div class="mp-trail-graph">${mpSparkline(l.seed || l.id)}</div>
    <div class="mp-trail-scale"><span>3.0</span><span>4.0</span><span>5.0</span></div>
  </div>`;
}

/* ============================================================
   D · ВЕРИФИКАЦИЯ 3 УРОВНЯ (Паспорт / Портфолио / Оплаченный кейс OKO)
   ============================================================ */
function mpTiers(l){
  const passport  = mpIsVerified(l) || (l.seed||0)%3===0;
  const portfolio = (l.reviews && l.reviews.length>=2) || (Array.isArray(l.photos) && l.photos.length>=1);
  const okoCase   = !!l.promo || (l.deals||0)>=40 || (typeof VERIFIED!=='undefined' && VERIFIED.has(l.n));
  return [
    {k:'passport',  lab:'Паспорт',              hint:'Личность подтверждена',   on:passport},
    {k:'portfolio', lab:'Портфолио',            hint:'Работы и живые отзывы',   on:portfolio},
    {k:'okocase',   lab:'Оплаченный кейс OKO',  hint:'Проведена сделка через OKO', on:okoCase},
  ];
}
function mpTiersLevel(l){ return mpTiers(l).filter(t=>t.on).length; } // 0..3
function mpTiersBlock(l){
  const tiers = mpTiers(l);
  const lv = tiers.filter(t=>t.on).length;
  return `<div class="mp-tiers" id="mpTiers">
    <div class="mp-tiers-h">
      <b>Верификация продавца</b>
      <span class="mp-tiers-lv lv${lv}">${lv}/3</span>
    </div>
    <div class="mp-tiers-row">${tiers.map(t=>`
      <div class="mp-tier ${t.on?'on':'off'}">
        <span class="mp-tier-shield">
          <svg class="mp-tier-svg"><use href="#i-mpshield"/></svg>
          ${t.on?`<span class="mp-tier-check">${I('check')}</span>`:''}
        </span>
        <b>${t.lab}</b><small>${t.hint}</small>
      </div>`).join('')}</div>
  </div>`;
}
function mpTiersMini(l){ // компактный ряд щитков для списочных плашек
  const tiers = mpTiers(l);
  return `<span class="mp-tiers-mini">${tiers.map(t=>
    `<span class="mp-tm ${t.on?'on':'off'}" title="${t.lab}"><svg class="mp-tier-svg"><use href="#i-mpshield"/></svg></span>`).join('')}</span>`;
}

/* ============================================================
   E · БЫСТРЫЕ ФИЛЬТРЫ (расширение уже существующих) — 7 чипов
   ============================================================ */
if(typeof mkFilters.mpBudget==='undefined') mkFilters.mpBudget = false; // до 5000₽
if(typeof mkFilters.mpToday==='undefined')  mkFilters.mpToday  = false; // сегодня
if(typeof mkFilters.mpMyCity==='undefined') mkFilters.mpMyCity = false; // в городе
if(typeof mkFilters.mpPhotos==='undefined') mkFilters.mpPhotos = false; // с примерами
function mpMyCity(){ return (typeof PROFILE!=='undefined' && PROFILE.city) || 'Онлайн'; }
function mpIsToday(l){ return ((l.seed||0)%4)===0; } // детерминированно «сегодня»
function mpHasPhotos(l){ return (Array.isArray(l.photos) && l.photos.length>0) || (l.reviews||[]).some(r=>Array.isArray(r.pics) && r.pics.length); }
const _mpMkApplyFiltersV3 = mkApplyFilters;
mkApplyFilters = function(arr){
  arr = _mpMkApplyFiltersV3(arr);
  if(mkFilters.mpBudget) arr = arr.filter(l=>l.p<=5000);
  if(mkFilters.mpToday)  arr = arr.filter(mpIsToday);
  if(mkFilters.mpMyCity){ const c = mpMyCity(); arr = arr.filter(l=>l.city===c); }
  if(mkFilters.mpPhotos) arr = arr.filter(mpHasPhotos);
  /* сортировка bump'нутых объявлений — сверху, если активны */
  arr = arr.slice().sort((a,b)=>{
    const ba = mpBumpActive(a.id) ? 1 : 0, bb = mpBumpActive(b.id) ? 1 : 0;
    return bb - ba;
  });
  return arr;
};
const _mpActiveFilterCountV3 = activeFilterCount;
activeFilterCount = function(){
  return _mpActiveFilterCountV3()
    + (mkFilters.mpBudget?1:0) + (mkFilters.mpToday?1:0)
    + (mkFilters.mpMyCity?1:0) + (mkFilters.mpPhotos?1:0);
};
const _mpResetFiltersV3 = resetFilters;
resetFilters = function(){
  _mpResetFiltersV3();
  mkFilters.mpBudget = false; mkFilters.mpToday = false;
  mkFilters.mpMyCity = false; mkFilters.mpPhotos = false;
};
/* дополняем строку quick-chips новыми — переопределяем весь inject */
mpInjectQuickChips = function(){
  const root = document.getElementById('marketRoot'); if(!root) return;
  if(document.getElementById('mpQuickChips')) return;
  const bar = root.querySelector('.mk-toolbar'); if(!bar) return;
  const w = document.createElement('div');
  w.id = 'mpQuickChips'; w.className = 'mp-qchips';
  const chip = (k, ic, lab) =>
    `<button class="mp-qc${mkFilters[k]?' on':''}" onclick="mpToggleQuick('${k}')">${I(ic)}<span>${lab}</span></button>`;
  w.innerHTML = `<div class="mp-qc-row">
    ${chip('mpBudget',  'money',       'до 5 000 ₽')}
    ${chip('mpToday',   'bolt',        'Сегодня')}
    ${chip('mpMyCity',  'pos',         'В городе')}
    ${chip('mpPhotos',  'photo',       'С примерами')}
    ${chip('mpOnline',  'circle-play', 'Онлайн')}
    ${chip('mpHasRev',  'star',        'С отзывами')}
    ${chip('mpFast',    'bolt',        'Быстрый ответ')}
  </div>`;
  bar.insertAdjacentElement('afterend', w);
};

/* ============================================================
   F · МИНИ-КАРТА УСЛУГ (абстрактная SVG, без API)
   ============================================================ */
function mpMapListings(){
  let arr = LISTINGS.filter(l=>!l.my && l.st==='act');
  if(typeof mkCat!=='undefined' && mkCat) arr = arr.filter(l=>l.cat===mkCat);
  return arr.slice(0, 12);
}
function mpMap(){
  const arr = mpMapListings();
  if(!arr.length) return '';
  const W = 320, H = 130;
  const pts = arr.map(l=>{
    const s = (l.seed||l.id) >>> 0;
    const x = 18 + ((s*13) % (W-36));
    const y = 14 + (((s>>3)*7) % (H-28));
    return {l, x, y};
  });
  const dots = pts.map(p=>{
    const bumped = mpBumpActive(p.l.id) ? ' bumped' : '';
    return `<g class="mp-map-pt${bumped}" onclick="event.stopPropagation();openListing(${p.l.id})">
      <circle cx="${p.x}" cy="${p.y}" r="8" class="mp-map-halo"/>
      <circle cx="${p.x}" cy="${p.y}" r="3.6" class="mp-map-core"/>
      <title>${esc(p.l.t)} · ${esc(p.l.pt)}</title>
    </g>`;
  }).join('');
  return `<div class="mp-mapwrap" id="mpMap">
    <div class="mp-strip-h">${I('pos')} Мастера рядом · ${arr.length} на карте</div>
    <div class="mp-map">
      <svg viewBox="0 0 ${W} ${H}" preserveAspectRatio="none" class="mp-map-svg">
        <defs><pattern id="mpMapGrid" width="20" height="20" patternUnits="userSpaceOnUse">
          <path d="M20 0H0V20" fill="none" stroke="rgba(154,255,0,.10)" stroke-width=".6"/></pattern></defs>
        <rect width="${W}" height="${H}" fill="url(#mpMapGrid)"/>
        <path d="M0 40 Q60 20 120 45 T240 30 T320 55" fill="none" stroke="rgba(154,255,0,.18)" stroke-width="1"/>
        <path d="M0 90 Q80 70 160 92 T320 88" fill="none" stroke="rgba(154,255,0,.12)" stroke-width="1"/>
        ${dots}
      </svg>
      <span class="mp-map-me">${I('pos')} вы здесь</span>
    </div>
  </div>`;
}
function mpInjectMap(){
  const root = document.getElementById('marketRoot'); if(!root) return;
  if(document.getElementById('mpMap')) return;
  if(typeof mkView==='undefined' || mkView!=='list') return;
  const results = document.getElementById('mkResults'); if(!results) return;
  const html = mpMap(); if(!html) return;
  results.insertAdjacentHTML('beforebegin', html);
}

/* ============================================================
   G · ОТЗЫВЫ С МЕДИА (превью фото + фуллскрин-свайп-просмотр)
   ============================================================ */
/* процедурный SVG-плейсхолдер вместо реальной фотки — data URI, вечно валиден */
function mpPicSvg(seed, w, h){
  w = w||600; h = h||600;
  const s = (seed*1234567) >>> 0;
  const hue1 = (s % 360), hue2 = ((s>>3) % 360);
  const cx = 30 + (s%40), cy = 30 + ((s>>2)%40);
  const svg = `<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100' preserveAspectRatio='xMidYMid slice' width='${w}' height='${h}'>
    <defs><linearGradient id='g' x1='0' y1='0' x2='1' y2='1'>
      <stop offset='0' stop-color='hsl(${hue1} 55% 22%)'/><stop offset='1' stop-color='hsl(${hue2} 60% 12%)'/></linearGradient></defs>
    <rect width='100' height='100' fill='url(%23g)'/>
    <circle cx='${cx}' cy='${cy}' r='${18+(s%14)}' fill='hsl(85 100% 55% / .45)'/>
    <circle cx='${100-cx}' cy='${100-cy}' r='${10+(s%9)}' fill='hsl(${hue2} 90% 60% / .35)'/>
    <path d='M0 ${70+(s%15)} L100 ${55+(s%20)} L100 100 L0 100 Z' fill='hsl(0 0% 6% / .55)'/>
  </svg>`;
  return 'data:image/svg+xml;utf8,'+svg.replace(/#/g,'%23').replace(/"/g,"'").replace(/\s+/g,' ');
}
/* сеем медиа в отзывы объявлений (детерминировано, один раз за сессию, не персист) */
(function mpSeedReviewPics(){
  LISTINGS.forEach(l=>{
    (l.reviews||[]).forEach((r, ri)=>{
      if(r.pics && r.pics.length) return;
      const s = ((l.seed||l.id)*7 + ri*31) >>> 0;
      if(s % 3 !== 0) return; // ~1/3 отзывов с медиа
      const n = 1 + (s % 3);
      r.pics = [];
      for(let i=0;i<n;i++) r.pics.push(mpPicSvg(s + i*97));
    });
  });
})();

let MP_MV = {items:[], i:0, caption:''};
function mpMvOpen(items, i, caption){
  if(!items || !items.length) return;
  MP_MV.items = items; MP_MV.i = Math.max(0, Math.min(i||0, items.length-1)); MP_MV.caption = caption||'';
  mpMvRender();
  const v = document.getElementById('mpMediaViewer');
  if(!v) return;
  v.classList.add('open'); v.setAttribute('aria-hidden','false');
  document.addEventListener('keydown', mpMvKey);
  mpMvBindSwipe(v);
}
function mpMvClose(){
  const v = document.getElementById('mpMediaViewer'); if(!v) return;
  v.classList.remove('open'); v.setAttribute('aria-hidden','true');
  document.removeEventListener('keydown', mpMvKey);
}
function mpMvKey(e){
  if(e.key==='Escape') mpMvClose();
  else if(e.key==='ArrowRight') mpMvGo(1);
  else if(e.key==='ArrowLeft')  mpMvGo(-1);
}
function mpMvGo(step){
  if(!MP_MV.items.length) return;
  MP_MV.i = (MP_MV.i + step + MP_MV.items.length) % MP_MV.items.length;
  mpMvRender();
}
function mpMvRender(){
  const stage = document.getElementById('mpMvStage'); if(!stage) return;
  stage.innerHTML = MP_MV.items.map((u,i)=>
    `<div class="mp-mv-slide${i===MP_MV.i?' on':''}"><img src="${u}" alt=""></div>`).join('');
  const cap = document.getElementById('mpMvCap'); if(cap) cap.innerHTML = esc(MP_MV.caption)+' · '+(MP_MV.i+1)+' / '+MP_MV.items.length;
  const dots = document.getElementById('mpMvDots');
  if(dots) dots.innerHTML = MP_MV.items.map((_,i)=>`<span class="mp-mv-dot${i===MP_MV.i?' on':''}"></span>`).join('');
  const nav = document.querySelectorAll('#mpMediaViewer .mp-mv-nav');
  nav.forEach(b=>{ b.style.display = MP_MV.items.length>1 ? 'flex' : 'none'; });
}
function mpMvBindSwipe(v){
  if(v._mpBound) return; v._mpBound = true;
  let x0 = null;
  v.addEventListener('touchstart', e=>{ x0 = e.touches[0].clientX; }, {passive:true});
  v.addEventListener('touchend',   e=>{
    if(x0===null) return;
    const dx = (e.changedTouches[0].clientX) - x0;
    if(Math.abs(dx)>40) mpMvGo(dx<0 ? 1 : -1);
    x0 = null;
  }, {passive:true});
  v.querySelector('.mp-mv-stage').addEventListener('click', e=>{
    /* тап по правой половине -> вперёд, по левой -> назад, кроме если по nav */
    const r = v.getBoundingClientRect();
    const half = r.left + r.width/2;
    if(MP_MV.items.length>1) mpMvGo(e.clientX>half ? 1 : -1);
  });
}
/* при рендере отзывов на карточке — добавляем миниатюры (аддитивно, поверх базового вывода).
   Клик по миниатюре открывает фуллскрин через ref по (listing id + review index + pic index),
   чтобы не тащить SVG-data-URI в onclick-атрибут (там одиночные кавычки — сломают HTML). */
function mpOpenRvMedia(lid, ri, pi){
  const l = LISTINGS.find(x=>x.id===lid); if(!l) return;
  const r = (l.reviews||[])[ri]; if(!r || !r.pics) return;
  mpMvOpen(r.pics, pi, 'Отзыв: '+r.n);
}
function mpDecorateReviews(id){
  const v = document.getElementById('lstView'); if(!v) return;
  const l = LISTINGS.find(x=>x.id===id); if(!l) return;
  const revList = v.querySelector('.rev-list'); if(!revList) return;
  const revs = l.reviews||[];
  const nodes = revList.querySelectorAll('.rev');
  nodes.forEach((node, i)=>{
    const r = revs[i];
    if(!r || !r.pics || !r.pics.length || node.querySelector('.mp-rvmedia')) return;
    const strip = document.createElement('div');
    strip.className = 'mp-rvmedia';
    strip.innerHTML = r.pics.map((u,pi)=>
      `<button type="button" class="mp-rvm-t" onclick="mpOpenRvMedia(${id},${i},${pi})"><img src="${u}" alt=""></button>`).join('');
    node.appendChild(strip);
  });
}

/* ============================================================
   H · УМНЫЙ ПОИСК — подсказки, история, топ дня
   ============================================================ */
const MP_TOP_TODAY = ['Reels под ключ','Лендинг за 3 дня','Настройка таргета','Дизайн карточек','Клон голоса','SEO-статьи','3D товара','Обложки для YouTube'];
function mpHistPush(q){
  q = (q||'').trim(); if(q.length<2) return;
  MP.searchHist = (MP.searchHist||[]).filter(x=>x.toLowerCase()!==q.toLowerCase());
  MP.searchHist.unshift(q);
  if(MP.searchHist.length>8) MP.searchHist.length = 8;
  mpSave();
}
function mpHistClear(){ MP.searchHist = []; mpSave(); mpRenderSuggest(); }
function mpSuggest(q){
  q = (q||'').trim().toLowerCase();
  if(!q) return [];
  const out = [];
  MK_CATS.forEach(c=>{ if(c.name.toLowerCase().includes(q)) out.push({kind:'cat', k:c.k, t:c.name, ic:c.ic}); });
  LISTINGS.filter(l=>!l.my && l.st==='act').forEach(l=>{
    const hay = (l.t+' '+l.n+' '+catName(l.cat)).toLowerCase();
    if(hay.includes(q)) out.push({kind:'lst', id:l.id, t:l.t, sub:l.n+' · '+l.pt, ic:catIco(l.cat)});
  });
  return out.slice(0, 8);
}
function mpMountSuggest(){
  const inp = document.getElementById('mkSearchInput'); if(!inp || inp._mpBound) return;
  inp._mpBound = true;
  const bar = inp.closest('.mk-searchbar'); if(!bar) return;
  bar.classList.add('mp-searchbar-relative');
  const drop = document.createElement('div');
  drop.id = 'mpSuggest'; drop.className = 'mp-suggest';
  bar.appendChild(drop);
  inp.addEventListener('focus', mpRenderSuggest);
  inp.addEventListener('input', mpRenderSuggest);
  inp.addEventListener('keydown', e=>{
    if(e.key==='Enter'){ mpHistPush(inp.value); mpRenderSuggest(); }
    if(e.key==='Escape') mpHideSuggest();
  });
  document.addEventListener('click', e=>{
    if(!bar.contains(e.target)) mpHideSuggest();
  }, {passive:true, capture:true});
}
function mpHideSuggest(){ const d = document.getElementById('mpSuggest'); if(d) d.classList.remove('open'); }
function mpRenderSuggest(){
  const inp = document.getElementById('mkSearchInput'); const d = document.getElementById('mpSuggest');
  if(!inp || !d) return;
  const q = inp.value||'';
  const sug = mpSuggest(q);
  const hist = MP.searchHist||[];
  if(!q.trim() && !hist.length && !MP_TOP_TODAY.length){ d.classList.remove('open'); return; }
  let html = '';
  if(q.trim() && sug.length){
    html += `<div class="mp-sug-h">${I('search')} Совпадения</div>`;
    html += sug.map(s=>s.kind==='cat'
      ? `<button class="mp-sug-i" onclick="mpSuggestGoCat('${s.k}')"><span class="mp-sug-ic">${I(s.ic)}</span><span class="mp-sug-b"><b>${esc(s.t)}</b><small>категория</small></span></button>`
      : `<button class="mp-sug-i" onclick="mpSuggestGoLst(${s.id})"><span class="mp-sug-ic">${I(s.ic)}</span><span class="mp-sug-b"><b>${esc(s.t)}</b><small>${esc(s.sub)}</small></span></button>`
    ).join('');
  }
  if(!q.trim() && hist.length){
    html += `<div class="mp-sug-h">${I('clock')} Недавние запросы <button class="mp-sug-x" onclick="mpHistClear()">${I('trash')} очистить</button></div>`;
    html += hist.map(h=>
      `<button class="mp-sug-i" onclick="mpSuggestGoQ(${JSON.stringify(h)})"><span class="mp-sug-ic">${I('clock')}</span><span class="mp-sug-b"><b>${esc(h)}</b></span></button>`).join('');
  }
  if(!q.trim()){
    html += `<div class="mp-sug-h">${I('fire')} Топ запросы дня</div>`;
    html += `<div class="mp-sug-top">${MP_TOP_TODAY.map(t=>
      `<button class="mp-sug-chip" onclick="mpSuggestGoQ(${JSON.stringify(t)})">${esc(t)}</button>`).join('')}</div>`;
  }
  d.innerHTML = html;
  d.classList.toggle('open', !!html);
}
function mpSuggestGoQ(q){
  mkSearchQ = q;
  const inp = document.getElementById('mkSearchInput'); if(inp) inp.value = q;
  mpHistPush(q);
  if(typeof renderMarketListSoft==='function') renderMarketListSoft();
  mpHideSuggest();
}
function mpSuggestGoCat(k){
  mpHideSuggest();
  if(typeof openMarketCat==='function') openMarketCat(k);
}
function mpSuggestGoLst(id){
  mpHideSuggest();
  openListing(id);
}

/* ============================================================
   I · BUMP / BOOST — «Поднять за 50 ₽ на день»
   ============================================================ */
const MP_BUMP_PRICE = 50, MP_BUMP_TTL = 24*3600e3;
function mpBumpActive(id){
  const t = MP.bumps && MP.bumps[id]; return !!(t && t>Date.now());
}
function mpBumpLeft(id){
  const t = MP.bumps && MP.bumps[id]; if(!t) return '';
  const ms = Math.max(0, t-Date.now()); const h = Math.floor(ms/3600e3);
  return h>=1 ? h+' ч' : Math.max(1, Math.round(ms/60e3))+' мин';
}
function mpBumpDo(id){
  const l = LISTINGS.find(x=>x.id===id); if(!l || !l.my) return;
  if(mpBumpActive(id)){ toast('Уже поднято · осталось '+mpBumpLeft(id)); return; }
  showPopup({ico:'rocket', title:'Поднять объявление?',
    body:'«'+esc(l.t)+'» будет закреплено сверху ленты на <b>24 часа</b>. Стоимость: <b>'+MP_BUMP_PRICE+' ₽</b> с кошелька.',
    actions:[
      {label:'Отмена', ghost:true, onclick:()=>closePopup()},
      {label:'Поднять за '+MP_BUMP_PRICE+' ₽', onclick:()=>{
        closePopup();
        if(!walletCharge(MP_BUMP_PRICE, 'Bump объявления · '+l.t)) return;
        if(typeof okoEarn==='function') okoEarn(MP_BUMP_PRICE, 'Bump объявления');
        MP.bumps[id] = Date.now() + MP_BUMP_TTL; mpSave();
        toast('Поднято на 24 часа — теперь выше в ленте');
        openListing(id);
      }}
    ]});
}
function mpBumpBadge(id){
  return mpBumpActive(id)
    ? `<span class="mp-bump-badge">${I('rocket')} Поднято · ${mpBumpLeft(id)}</span>`
    : '';
}
function mpBumpButtonForListing(l){
  const active = mpBumpActive(l.id);
  return active
    ? `<button class="mp-bump-btn active" disabled>${I('rocket')} Поднято · ещё ${mpBumpLeft(l.id)}</button>`
    : `<button class="mp-bump-btn" onclick="mpBumpDo(${l.id})">${I('rocket')} Поднять на день · ${MP_BUMP_PRICE} ₽</button>`;
}
/* декор карточек списка: наклейка «Поднято» */
function mpDecorateBumps(){
  document.querySelectorAll('#mkResults .lst-card').forEach(card=>{
    const id = mpIdFromCard(card); if(!id) return;
    if(mpBumpActive(id) && !card.querySelector('.mp-bump-flag')){
      const flag = document.createElement('span'); flag.className = 'mp-bump-flag';
      flag.innerHTML = I('rocket')+' Поднято';
      const photo = card.querySelector('.lst-photo');
      (photo||card).appendChild(flag);
      card.classList.add('mp-bumped');
    }
  });
}

/* ============================================================
   ФИНАЛЬНАЯ ОБЁРТКА openListing — вкатываем все v3-блоки
   ============================================================ */
const _mpOpenListingV3 = openListing;
openListing = function(id){
  _mpOpenListingV3.apply(this, arguments);
  try{
    const l = LISTINGS.find(x=>x.id===id);
    const v = document.getElementById('lstView');
    if(!l || !v) return;

    /* --- декор отзывов медиа-миниатюрами --- */
    mpDecorateReviews(id);

    if(l.my){
      /* --- на своей карточке: контейнер владельца (bump + метрики) --- */
      if(!document.getElementById('mpOwnerBar')){
        const bar = document.createElement('div');
        bar.id = 'mpOwnerBar'; bar.className = 'mp-owner-bar';
        bar.innerHTML = `
          <div class="mp-strip-h">${I('rocket')} Продвижение и метрики</div>
          ${mpBumpButtonForListing(l)}
          <div class="mp-owner-kpi">
            <div><b>${fmtN(l.views||0)}</b><small>${I('eye')} показы</small></div>
            <div><b>${l.contacts||0}</b><small>${I('chat')} контакты</small></div>
            <div><b>${l.favs||0}</b><small>${I('heart')} в избранном</small></div>
          </div>`;
        const meta = v.querySelector('.lst-d-meta');
        if(meta && mpBumpActive(l.id) && !meta.querySelector('.mp-bump-badge'))
          meta.insertAdjacentHTML('beforeend', mpBumpBadge(l.id));
        v.appendChild(bar);
      }
      return;
    }

    /* --- покупателю: блок «связаться» (OKO-чат + Show TG) --- */
    if(!document.getElementById('mpContact')){
      const cta = v.querySelector('.lst-cta');
      const html = mpContactBlock(l);
      if(cta){
        cta.insertAdjacentHTML('beforebegin', html);
        cta.style.display = 'none'; // прячем базовые кнопки — новая CTA богаче
        const orderBtn = v.querySelector('.btn[onclick^="orderListing"]');
        if(orderBtn) orderBtn.style.marginTop = '4px';
      } else {
        v.insertAdjacentHTML('beforeend', html);
      }
    }

    /* --- 3-уровневая верификация --- */
    if(!document.getElementById('mpTiers')){
      const seller = v.querySelector('.seller-card');
      const html = mpTiersBlock(l);
      if(seller) seller.insertAdjacentHTML('afterend', html);
      else v.insertAdjacentHTML('beforeend', html);
    }

    /* --- 30-day sparkline рейтинга --- */
    if(!document.getElementById('mpTrail')){
      const srv = document.getElementById('mpSrv') || document.getElementById('mpTiers');
      const html = mpRatingTrail(l);
      if(srv) srv.insertAdjacentHTML('afterend', html);
      else v.insertAdjacentHTML('beforeend', html);
    }

    /* --- 3-шаговое напоминание об эскроу поверх старого mp-trust --- */
    const old = document.getElementById('mpTrust');
    if(old && !document.getElementById('mpSafe')){
      old.insertAdjacentHTML('afterend', mpSafeMini());
    }
  }catch(e){}
};

/* ============================================================
   ФИНАЛЬНАЯ ОБЁРТКА renderMarketList — инъекции для list-вью
   ============================================================ */
if(typeof renderMarketList==='function'){
  const _mpRMLv3 = renderMarketList;
  renderMarketList = function(){
    _mpRMLv3.apply(this, arguments);
    try{
      mpInjectMap();
      mpMountSuggest();
      mpDecorateBumps();
    }catch(e){}
  };
}
if(typeof renderMarketListSoft==='function'){
  const _mpRMLSv3 = renderMarketListSoft;
  renderMarketListSoft = function(){
    _mpRMLSv3.apply(this, arguments);
    try{ mpDecorateBumps(); }catch(e){}
  };
}

/* ============================================================
   КАБИНЕТ ПРОДАВЦА — добавляем в карточки объявлений bump-кнопки
   ============================================================ */
const _mpRenderCabV3 = mpRenderCab;
mpRenderCab = function(){
  _mpRenderCabV3.apply(this, arguments);
  try{
    const box = document.getElementById('mpCabView'); if(!box) return;
    const mine = LISTINGS.filter(l=>l.my);
    /* добавляем bump-строку под каждым «мои объявления» */
    box.querySelectorAll('.mp-lst').forEach((row, i)=>{
      const l = mine[i]; if(!l || row.querySelector('.mp-bump-row')) return;
      const active = mpBumpActive(l.id);
      const bar = document.createElement('div');
      bar.className = 'mp-bump-row';
      bar.innerHTML = active
        ? `<span class="mp-bump-on">${I('rocket')} Поднято · ещё ${mpBumpLeft(l.id)}</span>`
        : `<button class="mp-bump-mini" onclick="event.stopPropagation();mpBumpDo(${l.id})">${I('rocket')} Поднять · ${MP_BUMP_PRICE} ₽</button>`;
      row.appendChild(bar);
    });
  }catch(e){}
};

/* ================================================================================
   MARKET-PRO v4 · «биржа услуг как Авито» + no-demo для новых пользователей
   Даниэль 29.07: реальный функционал без демо-воды для новых пользователей.
   Всё аддитивно, поверх уже существующих слоёв. Ключ демо-режима — mpIsDemo().
   ================================================================================ */

/* ---------- флаг демо-режима: только владельцу или по явному опт-ину ---------- */
/* ВОТ ПОЧЕМУ ДАНИЭЛЬ ВИДЕЛ ДЕМО-ДАННЫЕ ПОСЛЕ ЧИСТКИ.
   Здесь стояло `if(isOwner()) return true` — то есть демо-режим был ВКЛЮЧЁН
   именно у владельца. Я вычищал сиды, а на его телефоне всё продолжало
   показываться, потому что этот флаг открывал их обратно.
   Правило «ноль демо-данных» действует и для владельца: демо остаётся только
   как явный отладочный переключатель, который никто не включает по умолчанию. */
function mpIsDemo(){
  try{
    if(localStorage.getItem('oko-demo')==='1') return true;
  }catch(e){}
  return false;
}
function mpDemoToggle(){
  try{
    const on = mpIsDemo();
    if(on) localStorage.removeItem('oko-demo');
    else localStorage.setItem('oko-demo','1');
    toast(on ? 'Демо-объявления скрыты' : 'Показаны демо-объявления');
    renderMarket();
  }catch(e){}
}
window.mpDemoToggle = mpDemoToggle;

/* ---------- сид «настоящих» помечаем как демо, чтобы фильтровать по флагу ---------- */
(function mpMarkDemo(){
  /* сидовые «мои» объявления Даниэля (901/902/903) — это ТОЖЕ демо для нового юзера.
     Помечаем демо всё, что было в LISTINGS до создания реального объявления. */
  const seedMyIds = new Set([901, 902, 903]);
  LISTINGS.forEach(l=>{ if(!('demo' in l)) l.demo = !l.my || seedMyIds.has(l.id); });
  try{
    if(typeof POSTS!=='undefined'){
      ['sub','rec'].forEach(k=>{ (POSTS[k]||[]).forEach(p=>{ if(!('demo' in p)) p.demo = (p.name !== PROFILE.name); }); });
    }
  }catch(e){}
  /* для НЕ-owner юзера снимаем `my`-флаг с демо-«моих» — чтобы «Мои объявления» были реально пустыми */
  if(!mpIsDemo()){
    LISTINGS.forEach(l=>{ if(l.demo && seedMyIds.has(l.id)) l.my = false; });
  }
})();

/* ---------- фильтр реального каталога: без демо для не-owner ---------- */
function mpVisibleListings(){
  const demo = mpIsDemo();
  return LISTINGS.filter(l=> demo ? true : (!l.demo || l.my));
}

/* ---------- обёртки: фильтры каталога учитывают mpIsDemo ---------- */
const _mpApplyFiltersDemo = mkApplyFilters;
mkApplyFilters = function(arr){
  if(!mpIsDemo()) arr = arr.filter(l=> !l.demo || l.my);
  return _mpApplyFiltersDemo(arr);
};

/* ---------- рекомендации и поиск: тоже прячем демо ---------- */
(function mpPatchGlobalListingReads(){
  /* renderMarketCats хочет посчитать n-у в каждой категории и построить featured — оба
     читают LISTINGS напрямую. Патчим сам renderMarketCats через chain: перерисовываем чистый
     список после базы, если запущено не в демо-режиме и там оказались демо-карточки. */
})();

/* ============================================================
   V4·A — на «Категории» показывать пустое состояние новому пользователю
   ============================================================ */
function mpHasRealListings(){
  return LISTINGS.some(l=> l.my || (!l.demo && l.st==='act'));
}
function mpCatsEmptyState(){
  const wrap = document.getElementById('marketRoot');
  if(!wrap) return;
  if(mpIsDemo()) return;                 // только явный отладочный режим
  if(mpHasRealListings()) return;        // уже есть настоящие — не мешаем
  /* пустой стартовый экран биржи */
  wrap.innerHTML = `
    <div class="mk-searchbar" onclick="mkOpenSearch()"><svg class="i"><use href="#i-search"/></svg><span>Поиск услуг и товаров</span></div>
    <div class="mk-quick">
      <button onclick="openMyListings()">${I('briefcase')}<span>Мои объявления</span></button>
      <button onclick="mkView='fav';mkCat=null;renderMarket()">${I('heart')}<span>Сохранённые</span></button>
    </div>
    <div class="mp-fresh">
      <div class="mp-fresh-ic">${I('briefcase')}</div>
      <h3>Биржа OKO пока пуста в вашем городе</h3>
      <p>Разместите первое объявление — покупатели найдут вас через поиск, категории и карту. Сделки защищены эскроу OKO.</p>
      <div class="mp-fresh-cta">
        <button class="btn" onclick="mpOpenWizard()">${I('plus')} Добавить первое объявление</button>
        <button class="btn ghost" onclick="mpDemoToggle()">${I('eye')} Посмотреть как работает биржа</button>
      </div>
      <div class="mp-fresh-safety">
        ${I('lock')} <span><b>Правило безопасной сделки:</b> не переводите деньги вне OKO — только через безопасную оплату эскроу. При спорах модерация вернёт средства.</span>
      </div>
      <div class="mp-fresh-cats">
        <div class="mp-fresh-h">Что можно продавать</div>
        <div class="mp-fresh-grid">${MK_CATS.slice(0,8).map(c=>
          `<button class="mp-fresh-cat" onclick="mpOpenWizardCat('${c.k}')">${I(c.ic)}<span>${c.name}</span></button>`).join('')}</div>
      </div>
    </div>`;
}

/* обёртка renderMarket: если пусто и это не демо — показать fresh-старт */
const _mpRenderMarketFresh = renderMarket;
renderMarket = function(){
  _mpRenderMarketFresh();
  try{
    if(typeof mkView!=='undefined' && mkView==='cats') mpCatsEmptyState();
  }catch(e){}
};

/* ============================================================
   V4·B — расширенные фильтры Avito (payment, delivery, bargain, radius, photos, verified)
   ============================================================ */
if(typeof mkFilters.mpPay==='undefined')     mkFilters.mpPay = false;      // оплата на OKO
if(typeof mkFilters.mpDeliv==='undefined')   mkFilters.mpDeliv = false;    // с доставкой
if(typeof mkFilters.mpBargain==='undefined') mkFilters.mpBargain = false;  // торг
if(typeof mkFilters.mpPhoto==='undefined')   mkFilters.mpPhoto = false;    // только с фото
if(typeof mkFilters.mpRadius==='undefined')  mkFilters.mpRadius = 0;       // 0 = любой (км)

function mpHasOnlinePay(l){ return true; } // все объявления OKO поддерживают эскроу
function mpHasDelivery(l){ return (l.deliv===true) || ((l.seed||0)%2===0); }
function mpHasBargain(l){ return (l.bargain===true) || ((l.seed||0)%3===0); }
function mpHasAnyPhoto(l){ return Array.isArray(l.photos) && l.photos.length>0; }
/* геодистанция без API — детерминированная от seed, для прототипа */
function mpDistanceKm(l){ return ((l.seed||l.id||1) * 7) % 60; }

const _mpApplyFiltersV4 = mkApplyFilters;
mkApplyFilters = function(arr){
  arr = _mpApplyFiltersV4(arr);
  if(mkFilters.mpPay)     arr = arr.filter(mpHasOnlinePay);
  if(mkFilters.mpDeliv)   arr = arr.filter(mpHasDelivery);
  if(mkFilters.mpBargain) arr = arr.filter(mpHasBargain);
  if(mkFilters.mpPhoto)   arr = arr.filter(mpHasAnyPhoto);
  if(mkFilters.mpRadius)  arr = arr.filter(l=> mpDistanceKm(l) <= +mkFilters.mpRadius);
  return arr;
};
const _mpActiveFilterCountV4 = activeFilterCount;
activeFilterCount = function(){
  return _mpActiveFilterCountV4()
    + (mkFilters.mpPay?1:0) + (mkFilters.mpDeliv?1:0)
    + (mkFilters.mpBargain?1:0) + (mkFilters.mpPhoto?1:0)
    + (mkFilters.mpRadius?1:0);
};
const _mpResetFiltersV4 = resetFilters;
resetFilters = function(){
  _mpResetFiltersV4();
  mkFilters.mpPay = false; mkFilters.mpDeliv = false; mkFilters.mpBargain = false;
  mkFilters.mpPhoto = false; mkFilters.mpRadius = 0;
};

/* добавляем расширенные секции в шит фильтров */
const _mpRenderFiltersV4 = renderFilters;
renderFilters = function(){
  _mpRenderFiltersV4();
  const v = document.getElementById('filtersView');
  if(!v || v.querySelector('.mp-fadv-wrap')) return;
  const btnRow = v.lastElementChild;
  const radiusOpts = [[0,'Любой'],[5,'до 5 км'],[15,'до 15 км'],[50,'до 50 км']];
  const w = document.createElement('div');
  w.className = 'mp-fadv-wrap';
  w.innerHTML = `
    <p class="f-lab">${I('pos')} Радиус поиска</p>
    <div class="f-row">${radiusOpts.map(([v,lab])=>
      `<button class="f-chip ${mkFilters.mpRadius===v?'on':''}" onclick="mpSetRadius(${v})">${lab}</button>`).join('')}</div>
    <p class="f-lab">${I('lock')} Условия сделки</p>
    <div class="mp-cond-grid">
      <button class="mp-cond ${mkFilters.mpPay?'on':''}" onclick="mpToggleCond('mpPay')">${I('card')}<b>Онлайн-оплата</b><small>эскроу OKO</small></button>
      <button class="mp-cond ${mkFilters.mpDeliv?'on':''}" onclick="mpToggleCond('mpDeliv')">${I('rocket')}<b>С доставкой</b><small>в город/район</small></button>
      <button class="mp-cond ${mkFilters.mpBargain?'on':''}" onclick="mpToggleCond('mpBargain')">${I('chat')}<b>Возможен торг</b><small>цена обсуждается</small></button>
      <button class="mp-cond ${mkFilters.mpPhoto?'on':''}" onclick="mpToggleCond('mpPhoto')">${I('photo')}<b>Только с фото</b><small>реальные примеры</small></button>
    </div>`;
  v.insertBefore(w, btnRow);
};
function mpSetRadius(v){ mpKeepPrice(); mkFilters.mpRadius = v; renderFilters(); }
function mpToggleCond(k){ mpKeepPrice(); mkFilters[k] = !mkFilters[k]; renderFilters(); }

/* ============================================================
   V4·C — расширенный уровень фото (до 10) с drag-reorder
   ============================================================ */
const _mpRenderFormPhotosDrag = mpRenderFormPhotos;
mpRenderFormPhotos = function(){
  _mpRenderFormPhotosDrag();
  const grid = document.getElementById('mpFormPhotos');
  if(!grid) return;
  const thumbs = grid.querySelectorAll('.mp-fp-thumb');
  thumbs.forEach((t,i)=>{
    t.setAttribute('draggable','true');
    t.dataset.idx = i;
    t.addEventListener('dragstart', mpFpDragStart);
    t.addEventListener('dragover', mpFpDragOver);
    t.addEventListener('drop', mpFpDrop);
    t.addEventListener('dragend', mpFpDragEnd);
    /* touch-reorder: long-press переносит */
    t.addEventListener('touchstart', mpFpTouchStart, {passive:true});
  });
};
let _mpDragI = -1;
function mpFpDragStart(e){
  _mpDragI = +this.dataset.idx;
  try{ e.dataTransfer.effectAllowed = 'move'; e.dataTransfer.setData('text/plain', String(_mpDragI)); }catch(_){}
  this.classList.add('drag');
}
function mpFpDragOver(e){ e.preventDefault(); this.classList.add('over'); }
function mpFpDrop(e){
  e.preventDefault();
  const to = +this.dataset.idx;
  if(_mpDragI<0 || to<0 || _mpDragI===to) return;
  const arr = mpFormPhotos.slice();
  const [it] = arr.splice(_mpDragI,1);
  arr.splice(to,0,it);
  mpFormPhotos = arr;
  mpRenderFormPhotos();
}
function mpFpDragEnd(){
  document.querySelectorAll('.mp-fp-thumb').forEach(t=>t.classList.remove('drag','over'));
  _mpDragI = -1;
}
let _mpTouchT = null;
function mpFpTouchStart(e){
  const el = this;
  clearTimeout(_mpTouchT);
  _mpTouchT = setTimeout(()=>{
    /* при long-press показать переключатель «в обложку» — простой контекст */
    const i = +el.dataset.idx;
    if(i>0){
      const arr = mpFormPhotos.slice();
      const [it] = arr.splice(i,1);
      arr.unshift(it);
      mpFormPhotos = arr;
      mpRenderFormPhotos();
      toast('Обложка обновлена');
    }
  }, 550);
  const cancel = ()=>{ clearTimeout(_mpTouchT); el.removeEventListener('touchend',cancel); el.removeEventListener('touchmove',cancel); };
  el.addEventListener('touchend', cancel, {passive:true});
  el.addEventListener('touchmove', cancel, {passive:true});
}

/* ============================================================
   V4·D — 5-шаговый мастер создания объявления (как в Avito)
   ============================================================ */
let MP_WZ = null;
function mpOpenWizard(){
  MP_WZ = {step:1, cat:null, kind:'service', title:'', desc:'', price:'', bargain:false,
    onlinePay:true, deliv:false, city:(typeof PROFILE!=='undefined' && PROFILE.city)||'Онлайн', term:'по договорённости'};
  mpFormPhotos = [];
  mpWzRender();
  openSheet('mpWizard');
}
function mpOpenWizardCat(k){ mpOpenWizard(); MP_WZ.cat = k; MP_WZ.step = 2; mpWzRender(); }
function mpWzGoto(n){ if(!MP_WZ) return; MP_WZ.step = Math.max(1, Math.min(5, n)); mpWzRender(); }
function mpWzNext(){ if(!MP_WZ) return;
  if(MP_WZ.step===1 && !MP_WZ.cat){ toast('Выбери категорию'); return; }
  if(MP_WZ.step===2){
    const t = (document.getElementById('wzTitle')||{}).value||'';
    const d = (document.getElementById('wzDesc')||{}).value||'';
    if(t.trim().length<4){ toast('Название от 4 символов'); return; }
    MP_WZ.title = t.trim(); MP_WZ.desc = d.trim();
  }
  if(MP_WZ.step===3){ if(mpFormPhotos.length<1){ toast('Добавь хотя бы одно фото'); return; } }
  if(MP_WZ.step===4){
    const p = (document.getElementById('wzPrice')||{}).value||'';
    if(!p.trim()){ toast('Укажи цену или «договорная»'); return; }
    MP_WZ.price = p.trim();
    MP_WZ.bargain = !!(document.getElementById('wzBargain')||{}).checked;
    MP_WZ.onlinePay = !!(document.getElementById('wzOnlinePay')||{}).checked;
  }
  if(MP_WZ.step<5) mpWzGoto(MP_WZ.step+1);
  else mpWzSubmit();
}
function mpWzRender(){
  const box = document.getElementById('mpWizardView');
  if(!box || !MP_WZ) return;
  const step = MP_WZ.step;
  const bar = `<div class="mp-wz-steps">
    ${[1,2,3,4,5].map(i=>`<span class="mp-wz-dot${i<=step?' on':''}${i===step?' cur':''}">${i}</span>`).join('')}
  </div>`;
  const cap = ['Категория','Название и описание','Фото','Цена и оплата','Гео и доставка'][step-1];
  const nav = `<div class="mp-wz-nav">
    ${step>1?`<button class="btn ghost" onclick="mpWzGoto(${step-1})">${I('back')} Назад</button>`:'<span></span>'}
    <button class="btn" onclick="mpWzNext()">${step<5?'Далее':'Опубликовать'} ${I('chev')}</button>
  </div>`;
  let body = '';
  if(step===1){
    const cats = MK_CATS.map(c=>`<button class="mp-wz-cat ${MP_WZ.cat===c.k?'on':''}" onclick="MP_WZ.cat='${c.k}';mpWzRender()">
      <span class="mp-wz-cat-ic">${I(c.ic)}</span><b>${esc(c.name)}</b></button>`).join('');
    body = `
      <p class="f-lab">Тип объявления</p>
      <div class="f-row">
        <button class="f-chip ${MP_WZ.kind==='service'?'on':''}" onclick="MP_WZ.kind='service';mpWzRender()">${I('briefcase')} Услуга</button>
        <button class="f-chip ${MP_WZ.kind==='goods'?'on':''}" onclick="MP_WZ.kind='goods';mpWzRender()">${I('photo')} Товар</button>
        <button class="f-chip ${MP_WZ.kind==='work'?'on':''}" onclick="MP_WZ.kind='work';mpWzRender()">${I('users')} Работа</button>
      </div>
      <p class="f-lab">Категория</p>
      <div class="mp-wz-cats">${cats}</div>`;
  } else if(step===2){
    body = `
      <p class="f-lab">Что вы продаёте?</p>
      <input id="wzTitle" placeholder="Например: Монтаж Reels под ключ за 24 часа" value="${esc(MP_WZ.title)}" maxlength="80">
      <small class="mp-wz-hint">До 80 символов · чем конкретнее — тем больше откликов</small>
      <p class="f-lab" style="margin-top:14px">Описание</p>
      <textarea id="wzDesc" rows="5" maxlength="800" placeholder="Что входит, сроки, условия, гарантии. Расскажите как покупателю">${esc(MP_WZ.desc)}</textarea>
      <small class="mp-wz-hint">До 800 символов · без контактов — площадка их спрячет</small>`;
  } else if(step===3){
    body = `
      <p class="f-lab">Фото объявления · до ${MP_PHOTO_MAX}</p>
      <input type="file" id="mpPhotoInput" accept="image/*" multiple hidden>
      <div class="mp-fp-grid" id="mpFormPhotos"></div>
      <small class="mp-wz-hint">Первое фото — обложка. Перетащите миниатюру, чтобы поменять порядок; долгое касание — сделать обложкой.</small>`;
  } else if(step===4){
    body = `
      <p class="f-lab">Цена</p>
      <input id="wzPrice" inputmode="decimal" placeholder="Например: 1500 или договорная" value="${esc(MP_WZ.price)}">
      <div class="mp-wz-opts">
        <label class="mp-wz-check"><input type="checkbox" id="wzBargain" ${MP_WZ.bargain?'checked':''}><span></span> ${I('chat')} Возможен торг</label>
        <label class="mp-wz-check"><input type="checkbox" id="wzOnlinePay" ${MP_WZ.onlinePay?'checked':''}><span></span> ${I('card')} Онлайн-оплата через эскроу OKO</label>
      </div>
      <div class="mp-wz-safety">${I('lock')} Расчёты — только через безопасную оплату OKO. Не переводите деньги напрямую даже проверенным покупателям.</div>`;
  } else {
    const cities = ['Москва','Санкт-Петербург','Казань','Екатеринбург','Новосибирск','Онлайн'];
    body = `
      <p class="f-lab">Город/район</p>
      <input id="wzCity" list="wzCityList" value="${esc(MP_WZ.city)}" oninput="MP_WZ.city=this.value">
      <datalist id="wzCityList">${cities.map(c=>`<option value="${c}"></option>`).join('')}</datalist>
      <div class="mp-city-chips">${cities.map(c=>`<button class="f-chip ${MP_WZ.city===c?'on':''}" onclick="MP_WZ.city='${c}';mpWzRender()">${c}</button>`).join('')}</div>
      <p class="f-lab" style="margin-top:14px">Срок исполнения</p>
      <input id="wzTerm" placeholder="Например: 3 дня, помесячно" value="${esc(MP_WZ.term)}" oninput="MP_WZ.term=this.value">
      <label class="mp-wz-check" style="margin-top:12px"><input type="checkbox" id="wzDeliv" ${MP_WZ.deliv?'checked':''} onchange="MP_WZ.deliv=this.checked"><span></span> ${I('rocket')} Возможна доставка</label>
      <div class="mp-wz-preview">
        <div class="mp-wz-preview-h">${I('eye')} Предпросмотр</div>
        <div class="mp-wz-preview-card">
          <div class="mp-wz-preview-ph">${mpFormPhotos[0]?`<img src="${mpFormPhotos[0]}" alt="">`:I(MK_CATS.find(c=>c.k===MP_WZ.cat)?.ic||'briefcase')}</div>
          <div>
            <div class="mp-wz-preview-p">${esc(MP_WZ.price||'договорная')}${MP_WZ.bargain?' · торг':''}</div>
            <div class="mp-wz-preview-t">${esc(MP_WZ.title||'Название объявления')}</div>
            <div class="mp-wz-preview-m">${I('pos')} ${esc(MP_WZ.city)} · ${esc(MP_WZ.term)}</div>
          </div>
        </div>
      </div>`;
  }
  box.innerHTML = `
    <div class="mp-wz-head">
      <h3>Новое объявление</h3>
      <small>Шаг ${step} из 5 · ${cap}</small>
    </div>
    ${bar}
    <div class="mp-wz-body">${body}</div>
    ${nav}`;
  if(step===3){ mpMountPhotoUploader(); mpRenderFormPhotos(); }
}
function mpWzSubmit(){
  const cat = MP_WZ.cat, t = MP_WZ.title, ds = MP_WZ.desc, pt = MP_WZ.price;
  const price = parseInt((pt||'').replace(/\D/g,''))||0;
  const id = Date.now();
  const seed = id%97;
  LISTINGS.unshift({
    id, cat, type:MP_WZ.kind, t, p:price, pt:pt || 'договорная', ds:ds || 'Без описания',
    a:PROFILE.name[0], n:PROFILE.name, r:5.0, deals:0,
    city:MP_WZ.city, term:MP_WZ.term, views:0, favs:0, contacts:0,
    promo:null, fav:false, my:true, demo:false, st:'mod', seed,
    photos:mpFormPhotos.slice(), reviews:[],
    bargain:MP_WZ.bargain, deliv:MP_WZ.deliv, onlinePay:MP_WZ.onlinePay
  });
  closeSheet();
  toast('Объявление отправлено на модерацию');
  mpNotifPush({ic:'briefcase', who:'Биржа OKO', t:'объявление «'+t+'» отправлено на модерацию', act:()=>{ openMyListings(); }});
  setTimeout(()=>{
    const l = LISTINGS.find(x=>x.id===id);
    if(l && l.st==='mod'){
      l.st = 'act';
      mpNotifPush({ic:'check', who:'Биржа OKO', t:'«'+t+'» опубликовано — уже в поиске', act:()=>{ openListing(id); }});
      if(document.getElementById('marketRoot') && typeof mkView!=='undefined') openMyListings();
    }
  }, 3500);
  openMyListings();
}

/* если пользователь нажал старую кнопку «Разместить объявление», а мы не редактируем — открываем мастер */
const _mpOpenListingFormWiz = openListingForm;
openListingForm = function(id){
  if(id){ _mpOpenListingFormWiz(id); return; } // редактирование — старая простая форма
  mpOpenWizard();
};

/* ============================================================
   V4·E — «Мои объявления»: вкладки active / mod / rejected / archive
   ============================================================ */
let MP_MY_TAB = 'act';
const MP_MY_TABS = [
  ['act',  'Активные',    'briefcase'],
  ['mod',  'На модерации','clock'],
  ['rej',  'Отклонены',   'flag'],
  ['arch', 'Архив',       'trash'],
];
function mpArchiveListing(id){
  const l = LISTINGS.find(x=>x.id===id); if(!l) return;
  l.st = 'arch'; toast('Объявление в архиве'); closeSheet(); openMyListings();
}
function mpRestoreListing(id){
  const l = LISTINGS.find(x=>x.id===id); if(!l) return;
  l.st = 'act'; toast('Объявление снова в поиске'); openMyListings();
}
window.mpArchiveListing = mpArchiveListing;
window.mpRestoreListing = mpRestoreListing;

/* заменяем базовый openMyListings — с вкладками и статусом-причиной */
const _mpOpenMyListingsV4 = openMyListings;
openMyListings = function(){
  const wrap = document.getElementById('marketRoot'); if(!wrap) return;
  const mine = LISTINGS.filter(l=>l.my);
  const cnt = {act:0, mod:0, rej:0, arch:0};
  mine.forEach(l=>{ cnt[l.st] = (cnt[l.st]||0) + (l.st==='sold'?0:1); if(l.st==='sold') cnt.arch++; });
  const tab = MP_MY_TAB;
  const shown = mine.filter(l=> tab==='arch' ? (l.st==='arch'||l.st==='sold') : l.st===tab);
  wrap.innerHTML = `
    <button class="mk-back" onclick="mkView='cats';renderMarket()">${I('back')} Биржа</button>
    <h3 class="mk-h" style="margin-top:6px">Мои объявления</h3>
    <div class="mp-my-tabs">${MP_MY_TABS.map(([k,lab,ic])=>
      `<button class="mp-my-tab ${tab===k?'on':''}" onclick="MP_MY_TAB='${k}';openMyListings()">
        ${I(ic)}<span>${lab}</span>${cnt[k]?`<i>${cnt[k]}</i>`:''}
      </button>`).join('')}</div>
    ${shown.length ? shown.map(l=>{
      const cover = (l.photos && l.photos[0]) ? `<img src="${l.photos[0]}" alt="">` : I(catIco(l.cat));
      const rejReason = l.rejReason || 'Требуется больше информации';
      return `<div class="lst-card mp-my-card" onclick="openListing(${l.id})">
        <div class="lst-photo ${l.photos&&l.photos[0]?'mp-has-photo':''}" style="--h:${(l.seed*37)%360}deg">${cover}</div>
        <div class="lst-body">
          <div class="lst-price">${esc(l.pt||'договорная')}</div>
          <div class="lst-title">${esc(l.t)}</div>
          <div class="lst-meta">${statusChip(l.st==='sold'?'sold':(l.st==='act'?'act':l.st==='mod'?'mod':'act'))} ${l.promo?promoBadge(l.promo):''}</div>
          <div class="lst-foot dim" style="font-size:11.5px">${I('eye')}${fmtN(l.views||0)} · ${I('heart')}${l.favs||0} · ${I('chat')}${l.contacts||0}</div>
          ${l.st==='rej'?`<div class="mp-my-rej">${I('flag')} <b>Отклонено:</b> ${esc(rejReason)}</div>`:''}
          ${l.st==='arch'?`<div class="mp-my-arch">${I('trash')} В архиве · нажми, чтобы восстановить</div>`:''}
          ${l.st==='mod'?`<div class="mp-my-mod">${I('clock')} На модерации · обычно до 5 минут</div>`:''}
        </div>
      </div>`;
    }).join('') : `<div class="mp-my-empty">
      <span class="mp-emp-badge">${I(MP_MY_TABS.find(t=>t[0]===tab)[2])}</span>
      <b>${tab==='act'?'Здесь пусто':tab==='mod'?'Нет объявлений на модерации':tab==='rej'?'Нет отклонённых':'Архив пуст'}</b>
      <p>${tab==='act'?'Разместите объявление — оно появится здесь и в общей ленте после короткой проверки.':'Всё в порядке · ничего не требует внимания'}</p>
      ${tab==='act'?`<button class="btn" onclick="mpOpenWizard()">${I('plus')} Разместить объявление</button>`:''}
    </div>`}
    <button class="btn mp-my-add" onclick="mpOpenWizard()">${I('plus')} Разместить новое объявление</button>`;
};

/* при открытии карточки МОЕГО объявления — добавим кнопки Архив/Восстановить/Удалить */
const _mpOpenListingMyActions = openListing;
openListing = function(id){
  _mpOpenListingMyActions.apply(this, arguments);
  try{
    const l = LISTINGS.find(x=>x.id===id);
    const v = document.getElementById('lstView');
    if(!l || !v || !l.my) return;
    if(v.querySelector('.mp-my-act')) return;
    const row = document.createElement('div');
    row.className = 'mp-my-act';
    row.innerHTML = (l.st==='arch' || l.st==='sold')
      ? `<button class="btn ghost" onclick="mpRestoreListing(${l.id})">${I('check')} Восстановить</button>
         <button class="btn ghost" onclick="removeListing(${l.id})">${I('trash')} Удалить</button>`
      : `<button class="btn ghost" onclick="mpArchiveListing(${l.id})">${I('trash')} В архив</button>
         <button class="btn ghost" onclick="removeListing(${l.id})">${I('trash')} Удалить</button>`;
    v.appendChild(row);
  }catch(e){}
};

/* ============================================================
   V4·F — Уведомления по бирже (единый publisher)
   ============================================================ */
function mpNotifPush(o){
  try{
    if(typeof NOTIFS==='undefined') return;
    NOTIFS.unshift({ic:o.ic||'briefcase', who:o.who||'Биржа OKO', t:o.t||'', time:'сейчас', g:'Сегодня', unread:true, act:o.act||(()=>{})});
    if(typeof updateNotifDot==='function') updateNotifDot();
  }catch(e){}
}
/* Хук на bump — «Твоё объявление в топе» */
const _mpBumpDoNotif = (typeof mpBumpDo==='function') ? mpBumpDo : null;
if(_mpBumpDoNotif){
  mpBumpDo = function(id){
    const before = MP.bumps && MP.bumps[id];
    _mpBumpDoNotif(id);
    setTimeout(()=>{
      const after = MP.bumps && MP.bumps[id];
      if(after && after !== before){
        const l = LISTINGS.find(x=>x.id===id);
        if(l) mpNotifPush({ic:'rocket', who:'Биржа OKO', t:'«'+l.t+'» в топе категории · +24 часа показов', act:()=>{ openListing(id); }});
      }
    }, 400);
  };
  window.mpBumpDo = mpBumpDo;
}

/* ============================================================
   V4·G — Радиус на мини-карте (кольцо вокруг «вы здесь»)
   ============================================================ */
const _mpMapV4 = mpMap;
mpMap = function(){
  const html = _mpMapV4();
  if(!html) return html;
  const km = mkFilters.mpRadius || 15;
  const rPx = Math.min(60, 15 + km*0.8);
  const ring = `<svg class="mp-map-ring" viewBox="0 0 320 130" preserveAspectRatio="none">
    <circle cx="16" cy="115" r="${rPx}" fill="none" stroke="rgba(154,255,0,.28)" stroke-dasharray="3 4"/>
    <circle cx="16" cy="115" r="4" fill="#9AFF00"/>
  </svg>
  <span class="mp-map-radius">${I('pos')} радиус · ${km} км</span>`;
  return html.replace('<span class="mp-map-me">', ring + '<span class="mp-map-me">');
};

/* ============================================================
   V4·H — Плашка «Безопасная сделка» в списочной ленте (баннер сверху)
   ============================================================ */
function mpSafetyBanner(){
  const root = document.getElementById('marketRoot');
  if(!root) return;
  if(document.getElementById('mpSafetyBan')) return;
  const toolbar = root.querySelector('.mk-toolbar');
  if(!toolbar) return;
  const el = document.createElement('div');
  el.id = 'mpSafetyBan';
  el.className = 'mp-safety-ban';
  el.innerHTML = `<span class="mp-safety-ic">${I('lock')}</span>
    <span class="mp-safety-b"><b>Не переводите деньги вне OKO</b><small>Все сделки — только через безопасную оплату эскроу. Иначе площадка не защитит.</small></span>
    <button class="mp-safety-x" onclick="mpDismissSafety()" aria-label="Скрыть">${I('back')}</button>`;
  toolbar.insertAdjacentElement('afterend', el);
}
function mpDismissSafety(){
  try{ localStorage.setItem('oko-mp-safety-dismissed', '1'); }catch(e){}
  const el = document.getElementById('mpSafetyBan'); if(el) el.remove();
}
window.mpDismissSafety = mpDismissSafety;

if(typeof renderMarketList==='function'){
  const _mpRMLsafety = renderMarketList;
  renderMarketList = function(){
    _mpRMLsafety.apply(this, arguments);
    try{
      let dismissed = false;
      try{ dismissed = localStorage.getItem('oko-mp-safety-dismissed')==='1'; }catch(e){}
      if(!dismissed) mpSafetyBanner();
    }catch(e){}
  };
}

/* ============================================================
   V4·I — Финальная обёртка renderMarketCats: 2-уровневая группировка Услуги/Товары
   ============================================================ */
/* добавляем 3 «товарных» категории (одежда/электроника/дом) в конец каталога */
(function mpAddGoodsCats(){
  const has = k => MK_CATS.some(c=>c.k===k);
  const extras = [
    {k:'goods-cloth',   name:'Одежда и обувь', ic:'sticker', kind:'goods'},
    {k:'goods-tech',    name:'Электроника',    ic:'device',  kind:'goods'},
    {k:'goods-home',    name:'Дом и сад',      ic:'globe',   kind:'goods'},
  ];
  extras.forEach(e=>{ if(!has(e.k)) MK_CATS.push(e); });
  /* маркируем существующие как услуги, если не помечено */
  MK_CATS.forEach(c=>{ if(!c.kind) c.kind = 'service'; });
})();

/* поверх cats-рендера базы — перестраиваем сетку в 2 группы, если раньше рендерилось линейно */
function mpGroupCats(){
  const root = document.getElementById('marketRoot'); if(!root) return;
  const grid = root.querySelector('.mk-cats'); if(!grid) return;
  if(root.querySelector('.mp-catsg')) return;
  const services = MK_CATS.filter(c=>c.kind==='service');
  const goods    = MK_CATS.filter(c=>c.kind==='goods');
  const build = (list) => list.map(c=>{
    const n = mpVisibleListings().filter(l=>l.cat===c.k && !l.my).length;
    return `<button class="mk-cat" onclick="openMarketCat('${c.k}')"><span class="mk-cat-ic">${I(c.ic)}</span><b>${esc(c.name)}</b><small>${n} объявл.</small></button>`;
  }).join('');
  const wrap = document.createElement('div');
  wrap.className = 'mp-catsg';
  wrap.innerHTML = `
    <div class="mp-catsg-head">${I('briefcase')} <b>Услуги</b><small>${services.length} категорий</small></div>
    <div class="mk-cats">${build(services)}</div>
    <div class="mp-catsg-head" style="margin-top:14px">${I('photo')} <b>Товары</b><small>${goods.length} категорий</small></div>
    <div class="mk-cats">${build(goods)}</div>`;
  grid.replaceWith(wrap);
}
const _mpRenderMarketGroup = renderMarket;
renderMarket = function(){
  _mpRenderMarketGroup();
  try{ if(typeof mkView!=='undefined' && mkView==='cats') mpGroupCats(); }catch(e){}
};

/* ============================================================
   V4·J — Инъекция safety-баннера на карточке (нижняя приписка под CTA)
   ============================================================ */
const _mpOpenListingSafeCta = openListing;
openListing = function(id){
  _mpOpenListingSafeCta.apply(this, arguments);
  try{
    const l = LISTINGS.find(x=>x.id===id);
    const v = document.getElementById('lstView');
    if(!l || !v || l.my) return;
    if(v.querySelector('.mp-safety-inline')) return;
    const cta = v.querySelector('.lst-cta') || v.querySelector('#mpContact');
    if(!cta) return;
    const note = document.createElement('div');
    note.className = 'mp-safety-inline';
    note.innerHTML = `${I('lock')} <span>Все расчёты — только через безопасную оплату OKO. Прямые переводы площадка не защищает.</span>`;
    cta.insertAdjacentElement('afterend', note);
  }catch(e){}
};

/* ---------- сид «настоящих» уведомлений: только для не-owner фильтруем демо ---------- */
(function mpFilterDemoNotifs(){
  try{
    if(mpIsDemo() || typeof NOTIFS==='undefined') return;
    /* базовые сиды в NOTIFS упоминают Марк/Артём и т.п. — оставим только Биржу и системные */
    const demoWho = ['Лиза Кот','Артём Слов','Сергей Дан','+3 подписчика','Партнёрка'];
    NOTIFS = NOTIFS.filter(n=> !demoWho.includes(n.who));
    if(typeof updateNotifDot==='function') updateNotifDot();
  }catch(e){}
})();

/* ================= САМОИНИЦИАЛИЗАЦИЯ ================= */
if(document.getElementById('ma-market') && document.getElementById('ma-market').style.display==='block') renderMarket();

/* ===== module: feed-algo ===== */
/* ================= FEED-ALGO: алгоритмы рекомендаций OKO ================= */
/* Скоринг как у больших соцсетей: интересы + свежесть + вовлечённость +
   персональные сигналы + сессионный шум. Реклама — вставкой каждые 4-5 позиций. */

const FA_TOPICS = {ai:'Нейросети', content:'Контент', business:'Бизнес', marketing:'Маркетинг', games:'Игры', crypto:'Крипта'};
const FA_RU2ID = {'нейросети':'ai','ии':'ai','контент':'content','reels':'content','рилс':'content',
  'бизнес':'business','маркетинг':'marketing','игры':'games','крипта':'crypto','ton':'crypto','тон':'crypto'};

const FA = {
  seed: ((Date.now() % 2147483647) ^ 0x9AFF00) >>> 0, /* сессионный seed шума */
  signals: {},      /* {topic: вес} — накопленные реакции пользователя по темам */
  authors: {},      /* {автор: вес} — аффинность: кого ты реально читаешь/лайкаешь */
  _ints: null,      /* кэш Set интересов из регистрации (парсим localStorage один раз) */
  page: 0,          /* сколько партий бесконечной ленты подгружено */
  maxPages: 8,
  loading: false,
  genId: 778000,    /* id генерируемых постов (ads занимает 9000+, пул — 777xxx) */
  usedCombos: {},   /* защита от повторов шаблон+автор в генераторе */
  io: null,
  infoOpen: false,  /* раскрыто ли пояснение «как работает лента» */
  now: Date.now(),  /* «замороженное сейчас» скоринга: одинаковый epoch на все ре-рендеры →
                       детерминированный порядок (нет дребезга). Обновляется на refresh/подгрузке. */
};

/* ---------- персист сигналов ---------- */
function faLoadSignals(){
  try{
    const s = JSON.parse(localStorage.getItem('oko-feed-signals') || 'null');
    if(s && typeof s.topics === 'object' && s.topics) FA.signals = s.topics;
  }catch(e){}
}
function faSaveSignals(){
  try{ localStorage.setItem('oko-feed-signals', JSON.stringify({topics:FA.signals, at:Date.now()})); }catch(e){}
}
function faSignal(topic, w){
  if(!topic || !FA_TOPICS[topic]) return;
  FA.signals[topic] = Math.min(40, Math.max(0, Math.round(((FA.signals[topic]||0) + w)*10)/10));
  faSaveSignals();
}

/* ---------- персист аффинности авторов (с кем ты взаимодействуешь) ---------- */
function faLoadAuthors(){
  try{
    const s = JSON.parse(localStorage.getItem('oko-feed-authors') || 'null');
    if(s && typeof s.authors === 'object' && s.authors) FA.authors = s.authors;
  }catch(e){}
}
function faSaveAuthors(){
  try{ localStorage.setItem('oko-feed-authors', JSON.stringify({authors:FA.authors, at:Date.now()})); }catch(e){}
}
function faAuthor(name, w){
  if(!name || !w) return;
  FA.authors[name] = Math.min(30, Math.max(0, Math.round(((FA.authors[name]||0) + w)*10)/10));
  faSaveAuthors();
}

/* ---------- скрытые посты (persist): свайп-влево и «Не интересно» помнят выбор между сессиями ----------
   Ключ oko-feed-hidden хранит массив id, чтобы после F5 пользователь не видел уже скрытое.
   Держим не больше 200 последних id (защита от бесконтрольного роста хранилища). */
function faLoadHidden(){
  if(FA.hidden) return FA.hidden;
  FA.hidden = new Set();
  try{
    const s = JSON.parse(localStorage.getItem('oko-feed-hidden') || 'null');
    if(s && Array.isArray(s.ids)) s.ids.forEach(id => FA.hidden.add(+id));
  }catch(e){}
  return FA.hidden;
}
function faSaveHidden(){
  try{
    const arr = [...(FA.hidden||[])].slice(-200);
    localStorage.setItem('oko-feed-hidden', JSON.stringify({ids:arr, at:Date.now()}));
  }catch(e){}
}
function faMarkHidden(id){ (FA.hidden || faLoadHidden()).add(+id); faSaveHidden(); }

/* ---------- интересы из регистрации (кэш: не парсим localStorage на каждый пост при сортировке) ---------- */
function faInterests(){
  if(FA._ints) return FA._ints;
  const out = new Set();
  try{
    const reg = JSON.parse(localStorage.getItem('oko-registration') || 'null');
    const raw = (reg && Array.isArray(reg.interests)) ? reg.interests : [];
    raw.forEach(x=>{
      const k = String(x).toLowerCase().trim();
      if(FA_TOPICS[k]) out.add(k); else if(FA_RU2ID[k]) out.add(FA_RU2ID[k]);
    });
  }catch(e){}
  FA._ints = out;
  return out;
}

/* ---------- детерминированный шум: seed сессии + id поста ---------- */
function faRand(id){
  let h = (FA.seed + (Math.abs(+id)||0) * 2654435761) >>> 0;
  h = Math.imul(h ^ (h >>> 15), h | 1);
  h ^= h + Math.imul(h ^ (h >>> 7), h | 61);
  return ((h ^ (h >>> 14)) >>> 0) / 4294967296;
}

/* ---------- сигналы поста: возраст, взвешенные реакции, скорость набора, watch-time ----------
   Все производные детерминированы от данных поста → порядок ленты стабилен между рендерами. */
function faAgeH(p){                       /* возраст в часах; пол 0.5ч, чтобы свежак не делил на ~0 */
  if(!p.ts) return p.promoted ? 2 : 96;
  return Math.max((FA.now - p.ts) / 36e5, 0.5);  /* FA.now заморожен → скор стабилен между рендерами */
}
function faReactions(p){                  /* взвешенная вовлечённость: коммент/репост весомее лайка */
  return (p.likes||0) + (Array.isArray(p.comments) ? p.comments.length : 0)*3 + (p.reposts||0)*2;
}
function faVelocity(p){                   /* engagement velocity — реакций в час (ядро тренда/виральности) */
  return faReactions(p) / faAgeH(p);
}
function faMediaSec(m){                   /* '1:12' -> секунды (watch-time прокси для видео) */
  if(!m) return 0;
  const a = String(m).split(':').map(x => parseInt(x, 10) || 0);
  return a.length === 2 ? a[0]*60 + a[1] : a[0];
}
function faIsTrending(p){                 /* виральный порог: быстро набирает и заметен, ИЛИ уже большой охват */
  if(p.promoted) return false;
  if((p.views||0) >= 10000) return true;           /* «В тренде» также по массовому охвату (>10k просмотров) */
  return faVelocity(p) >= 100 && faReactions(p) >= 150;
}

/* ================= 1. ПУЛ КОНТЕНТА (12-15 демо-постов по интересам) ================= */
const FA_NOW = Date.now();
const FA_POOL = []  /* демо-данные удалены 09.08: пул постов умной ленты — источник только API */;

/* вливаем пул + проставляем ts/topic старым постам ядра */
(function faSeedContent(){
  FA_POOL.forEach(p=>{
    p.ts = FA_NOW - p.ageH*36e5; delete p.ageH;
    p.liked = false; p.saved = false;
    if(!POSTS.rec.some(x=>x.id===p.id)) POSTS.rec.push(p);
  });
  const stamp = {101:{h:6,t:'ai'}, 102:{h:21,t:'business'}, 201:{h:28,t:'content'}, 202:{h:46,t:'business'}};
  [...POSTS.sub, ...POSTS.rec].forEach(p=>{
    if(!p.ts){ const s = stamp[p.id]; p.ts = FA_NOW - ((s ? s.h : 24 + (p.id % 7) * 4)) * 36e5; }
    if(!p.topic && stamp[p.id]) p.topic = stamp[p.id].t;
  });

  /* Здесь засевались демо-треды комментариев: Влад, Рома, Соня, Лев и
     каналы-призраки спорили под несуществующими постами. Правка Даниэля —
     ноль выдуманных людей и переписок. Ветки появляются, когда их пишут
     живые люди; механика ответов и лайков от этого не пострадала. */
})();

/* ================= 2. СКОРИНГ (Instagram-like: engagement + time-decay) =================
   Формула по ТЗ Даниэля от 29.07:
     score = watch*3 + like*2 + comment*3 + share*5 + save*4 − hide*10
   × time-decay множитель exp(-age_hours / 24).
   Плюс лёгкая персонализация: интересы регистрации, аффинность автора,
   накопленные сигналы по теме. Реклама снимается из общего ранжирования — её
   позицию задаёт inject-слот каждые 6-7 постов (см. faDecorate). */
const _faPrevFeedScore = feedScore;
feedScore = function(p){
  const cmts   = Array.isArray(p.comments) ? p.comments.length : 0;
  const shares = p.reposts || 0;
  const likes  = p.likes || 0;
  const watch  = faMediaSec(p.media || '');       /* прокси удержания: длительность ролика */
  const saveB  = p.saved ? 4 : 0;                  /* булев сейв → +4 (в модели нет счётчика сейвов) */
  let s = watch*3 + likes*2 + cmts*3 + shares*5 + saveB;

  /* time-decay: полураспад ≈ сутки, как в Instagram */
  s *= Math.exp(-faAgeH(p) / 24);

  const ints = faInterests();
  if(p.topic && ints.has(p.topic)) s += 65;                              /* интерес из регистрации */
  s += Math.min(FA.signals[p.topic]||0, 14) * 4;                         /* накопленный интерес к теме */
  s += Math.min(FA.authors[p.name]||0, 20) * 3;                          /* аффинность автора */
  if(p.topic && (FA.signals[p.topic]||0) < -1) s -= 30;                  /* «hide-подобный» штраф темы */

  /* реклама вне ранжирования — позицию задаёт inject-слот */
  if(p.promoted) s -= 1e6;

  s += (faRand(p.id) - 0.5) * 8;             /* сессионный шум — лента «дышит», стабильный порядок */
  s -= (p.faPage||0) * 1e4;                  /* подгруженные партии — ниже */
  return s;
};

/* ================= 3. ПЕРСОНАЛЬНЫЕ СИГНАЛЫ (патчи реакций) ================= */
if(typeof likePost === 'function'){
  const _faPrevLikePost = likePost;
  likePost = function(id){
    const p = postById(id); const was = p ? !!p.liked : false;
    _faPrevLikePost(id);                          /* точечное обновление карточки — БЕЗ пересборки ленты */
    if(p && p.liked !== was){
      if(p.topic) faSignal(p.topic, p.liked ? 2 : -2);
      faAuthor(p.name, p.liked ? 1.5 : -1.5);     /* лайк усиливает аффинность к автору */
    }
  };
}
if(typeof repost === 'function'){
  const _faPrevRepost = repost;
  repost = function(id){
    const p = postById(id); const was = p ? !!p.reposted : false;
    _faPrevRepost(id);
    if(p && p.reposted !== was){
      if(p.topic) faSignal(p.topic, p.reposted ? 3 : -3);
      faAuthor(p.name, p.reposted ? 2 : -2);      /* репост — самый сильный сигнал доверия автору */
    }
  };
}
/* chain hidePost: помимо фильтра массива (ядро) — запоминаем id в localStorage,
   чтобы пост не вернулся после F5, плюс сдвигаем персонализацию: «не интересно» — обратный сигнал. */
if(typeof hidePost === 'function'){
  const _faPrevHidePost = hidePost;
  hidePost = function(id){
    const p = postById(id);
    faMarkHidden(id);
    _faPrevHidePost(id);
    if(p){
      if(p.topic) faSignal(p.topic, -1.5);
      faAuthor(p.name, -1);
    }
  };
}

/* chain repost: после базового toggle — при первом «репосте» вызвать native-шер
   (navigator.share), а если его нет, скопировать ссылку в буфер (реальный «Поделиться»). */
if(typeof repost === 'function'){
  const _faPrevRepostShare = repost;
  repost = function(id){
    const p = postById(id);
    const willShare = p && !p.reposted;
    _faPrevRepostShare(id);
    if(willShare && p && p.reposted) faShareOut(p);
  };
}
function faShareOut(p){
  if(!p) return;
  try{
    const link = 'https://okoteam.top/post/' + p.id;
    const body = String(p.body || '').replace(/\s+/g,' ').trim().slice(0, 140);
    const txt  = (p.name || 'OKO') + ': ' + body;
    if(navigator.share){
      navigator.share({title:'OKO', text:txt, url:link}).catch(function(){});
      return;
    }
    if(navigator.clipboard && navigator.clipboard.writeText){
      navigator.clipboard.writeText(link)
        .then(function(){ try{ typeof toast === 'function' && toast('Ссылка на пост скопирована'); }catch(e){} })
        .catch(function(){});
    }
  }catch(e){}
}

/* openComments/addComment полностью переопределены ниже (секция 6: комментарии-треды) —
   сигнал интереса от открытия/написания коммента учитывается там же. */

/* ================= 4. UI: «почему показано», реклама каждые 4-5, обновление ================= */
function faIdOf(art){
  const b = art.querySelector('.post-more');
  const m = b && (b.getAttribute('onclick')||'').match(/openPostMenu\((\d+)/);
  return m ? +m[1] : null;
}
/* многосигнальное «почему рекомендовано»: до 2 честных, человекочитаемых причин.
   Разные сигналы → своя иконка и подпись; порядок = приоритет прозрачности. */
function faReasons(p){
  if(p.promoted) return {cls:'ad', ico:'megaphone', parts:['Реклама']};
  const ints = faInterests();
  const parts = [];
  let cls = 'hot', ico = 'fire';
  /* 1 — интерес из регистрации (самый честный «почему») */
  if(p.topic && ints.has(p.topic)){ parts.push('Твой интерес: ' + FA_TOPICS[p.topic]); cls = 'int'; ico = 'star'; }
  /* 2 — скорость набора (виральность), в реакциях/час */
  const vel = Math.round(faVelocity(p));
  if(vel >= 40 && parts.length < 2){ parts.push('Набирает ' + vel + ' реакций/час'); if(cls === 'hot'){ cls = 'vel'; ico = 'bolt'; } }
  /* 3 — похоже на лайкнутое (накопленный интерес к теме) */
  if((FA.signals[p.topic]||0) >= 2 && parts.length < 2){ parts.push('Похоже на лайкнутое'); if(cls === 'hot'){ cls = 'like'; ico = 'heart'; } }
  /* 4 — автор, которого ты читаешь (аффинность) */
  if((FA.authors[p.name]||0) >= 3 && parts.length < 2){ parts.push('Автор, которого ты читаешь'); if(cls === 'hot'){ cls = 'aff'; ico = 'user'; } }
  if(!parts.length) parts.push('Популярно сейчас');
  return {cls, ico, parts};
}

/* диверсификация ленты: не более 2 постов одного автора подряд (как в TikTok/Instagram).
   Жадно и детерминированно переставляет уже отранжированные узлы → без «дребезга» при ре-рендере.
   items: [{el, name}] в порядке ранга; возвращает [el] в разнообразном порядке. */
function faDiversify(items){
  const remaining = items.slice(), out = [];
  while(remaining.length){
    let idx = 0;
    const n = out.length;
    if(n >= 2 && out[n-1]._n && out[n-1]._n === out[n-2]._n){
      const alt = remaining.findIndex(x => x.name !== out[n-1]._n);
      if(alt > -1) idx = alt;         /* если все оставшиеся того же автора — оставляем как есть */
    }
    const pick = remaining.splice(idx, 1)[0];
    pick.el._n = pick.name;           /* пометка автора для проверки соседей */
    out.push(pick.el);
  }
  return out;
}

function faDecorate(){
  const list = document.getElementById('feedList');
  if(!list || curFeedKind !== 'rec') return;

  /* убрать длинную текстовую врезку ядра «Ранжирует алгоритм OKO…» — её роль берёт на себя
     аккуратная шапка с раскрывающимся пояснением (никакого визуального шума над лентой) */
  const baseNote = list.querySelector(':scope > div:not([class])');
  if(baseNote) baseNote.remove();

  /* единая чистая шапка: «Умная лента» + (i) пояснение + «Обновить подборку» */
  if(!list.querySelector('.fa-bar')){
    const open = FA.infoOpen ? ' fa-open' : '';
    list.insertAdjacentHTML('afterbegin',
      `<div class="fa-head">`+
        `<div class="fa-bar">`+
          `<div class="fa-bar-l">`+
            `<span class="fa-dot"></span>`+
            `<span class="fa-title">Умная лента</span>`+
            `<button class="fa-info${open}" type="button" aria-label="Как работает лента" onclick="faToggleInfo(this)">${I('fa-info')}</button>`+
          `</div>`+
          `<button class="fa-refresh" type="button" onclick="faRefresh()">${I('fa-refresh')}<span>Обновить подборку</span></button>`+
        `</div>`+
        `<div class="fa-explain${open}"><p>Ранжирует алгоритм OKO: вовлечённость и свежесть — как в Instagram, в отличие от Telegram. Реагируй на посты — лента точнее подстроится под тебя.</p></div>`+
      `</div>`);
  }

  /* разбор карточек на органику и рекламу */
  const arts = [...list.querySelectorAll('article.post')];
  const ads = [], org = [];
  arts.forEach(a=>{ const p = postById(faIdOf(a)); if(p && p.promoted) ads.push(a); else org.push({el:a, name:(p && p.name)||''}); });

  /* диверсификация органики: не более 2 подряд одного автора (TikTok-diversity).
     Затем вставка рекламы каждые 6-7 позиций (по ТЗ Даниэля 29.07).
     appendChild перемещает существующие узлы — это точечная перестановка, а не пересборка ленты. */
  let seq = faDiversify(org);
  if(ads.length && arts.length > 4){
    let pos = 5;                                    /* первый рекламный слот — после 5 органики */
    ads.forEach((ad,i)=>{
      seq.splice(Math.min(pos, seq.length), 0, ad);
      pos += 6 + (faRand(9100 + i) < 0.5 ? 0 : 1); /* каждые 6-7 слотов = «Реклама» */
    });
  }
  seq.forEach(el=>list.appendChild(el));

  /* метки «Канал»/«В тренде»/«Реклама» + чип «почему показано» + «растёт» + галочка verified — без дублей */
  list.querySelectorAll('article.post').forEach(art=>{
    const p = postById(faIdOf(art)); if(!p) return;
    const head = art.querySelector('.head');
    const nameEl = art.querySelector('.head .name');
    /* у рекламы уже есть бейдж «Реклама», у топ-поста — «В тренде» (ядро) */
    let nameChip = nameEl && nameEl.querySelector('.chip');
    const w = faReasons(p);
    const vel = faVelocity(p);
    /* «В тренде» — виральный бейдж по скорости набора (если у карточки ещё нет чипа) */
    if(nameEl && !nameChip && faIsTrending(p)){
      nameEl.insertAdjacentHTML('beforeend', ` <span class="chip fa-trend">${I('fa-trend')}<span>В тренде</span></span>`);
      nameChip = nameEl.querySelector('.fa-trend');
    }
    /* «Канал» — информ-метка для органических каналов без своего чипа и без персонального сигнала
       (заменяет собой безликое «Популярно», а не дублирует его) */
    if(nameEl && !nameChip && !p.promoted && w.cls === 'hot' && /канал/i.test(p.sub || '')){
      nameEl.insertAdjacentHTML('beforeend', ' <span class="chip fa-chan">Канал</span>');
      nameChip = nameEl.querySelector('.fa-chan');
    }
    /* «растёт» — тонкий индикатор восходящей вовлечённости (для набирающих, но ещё не топ-трендов) */
    const sub = art.querySelector('.head .sub');
    if(sub && !sub.querySelector('.fa-rise') && !p.promoted && !faIsTrending(p) && vel >= 45){
      sub.insertAdjacentHTML('beforeend', ` <span class="fa-rise">${I('fa-trend')}растёт</span>`);
    }
    /* чип «почему»: до 2 честных причин через · ; при наличии осмысленной метки «Популярно» не дублируем */
    if(head && !art.querySelector('.fa-why') && !p.promoted){
      if(!(w.cls === 'hot' && nameChip)){
        const inner = w.parts.map((t,i)=> (i ? '<b class="fa-sep">·</b>' : '') + '<span>' + esc(t) + '</span>').join('');
        head.insertAdjacentHTML('afterend', `<div class="fa-why ${w.cls}">${I(w.ico)}${inner}</div>`);
      }
    }
    /* verify-stickers (vsDecorateFeed) выполняется в цепочке рендера РАНЬШЕ и уже могла
       поставить .vs-badge — не дорисовываем вторую галочку поверх (устраняет дубль в rec) */
    if(nameEl && !nameEl.querySelector('.fa-vb, .vs-badge') && typeof vBadge === 'function' && VERIFIED.has(p.name)){
      const chip = nameEl.querySelector('.chip');
      const badge = `<span class="fa-vb">${vBadge(p.name)}</span>`;
      if(chip) chip.insertAdjacentHTML('beforebegin', badge); else nameEl.insertAdjacentHTML('beforeend', badge);
    }
  });

  /* сторожок бесконечной ленты + видимая кнопка «Загрузить ещё» как страховка,
     если IntersectionObserver не сработал (reduced-motion, старый браузер, ручной сценарий) */
  const old = list.querySelector('.fa-sentinel'); if(old) old.remove();
  const oldBtn = list.querySelector('.fa-load-more'); if(oldBtn) oldBtn.remove();
  const endNote = list.querySelector('.fa-end'); if(endNote) endNote.remove();
  if(FA.page < FA.maxPages){
    const s = document.createElement('div'); s.className = 'fa-sentinel'; list.appendChild(s);
    const btn = document.createElement('button');
    btn.type = 'button'; btn.className = 'fa-load-more';
    btn.setAttribute('aria-label', 'Загрузить ещё');
    btn.innerHTML = I('fa-refresh') + '<span>Загрузить ещё</span>';
    btn.onclick = function(){ if(!FA.loading) faLoadMore(); };
    list.appendChild(btn);
    if(FA.io){ FA.io.disconnect(); FA.io.observe(s); }
  }else{
    list.insertAdjacentHTML('beforeend', `<div class="fa-end">Это вся подборка на сейчас. Жми «Обновить подборку» сверху, алгоритм найдёт свежее под тебя</div>`);
  }
}

/* ---------- красивое пустое состояние ленты (иллюстрация + CTA) ---------- */
function faEmptyArt(){
  return `<svg class="fa-empty-art" viewBox="0 0 120 120" fill="none" aria-hidden="true">`+
    `<rect x="22" y="30" width="76" height="62" rx="13" stroke="currentColor" stroke-width="4" opacity=".32"/>`+
    `<circle cx="40" cy="49" r="7.5" fill="var(--lime)"/>`+
    `<rect x="55" y="44" width="33" height="5" rx="2.5" fill="currentColor" opacity=".42"/>`+
    `<rect x="55" y="54" width="21" height="5" rx="2.5" fill="currentColor" opacity=".26"/>`+
    `<rect x="34" y="70" width="52" height="5" rx="2.5" fill="currentColor" opacity=".24"/>`+
    `<rect x="34" y="80" width="36" height="5" rx="2.5" fill="currentColor" opacity=".16"/>`+
    `<g class="fa-empty-wave" stroke="var(--lime)" stroke-width="4.4" stroke-linecap="round" fill="none">`+
      `<path d="M90 24a18 18 0 0 1 0 26"/>`+
      `<path d="M98 16a30 30 0 0 1 0 42" opacity=".55"/>`+
    `</g></svg>`;
}
function faGoRec(){
  const b = [...document.querySelectorAll('#screen-feed .feed-tabs button')]
    .find(x => /'rec'/.test(x.getAttribute('onclick') || ''));
  if(b) b.click();
}
function faRenderEmpty(list, kind){
  /* Честный пустой экран (правка Даниэля: «никаких ложных подтверждений»).
     Раньше здесь было написано «свежие посты закончились, обнови подборку —
     алгоритм за секунду соберёт новую ленту». Собирать нечего: постов в
     приложении пока нет вообще, и кнопка «Обновить» возвращала ту же
     пустоту. Теперь объясняем, откуда лента берётся, и даём действия,
     которые реально её наполняют. */
  const rec = kind === 'rec';
  const title = rec ? 'Лента пока пустая' : 'Ты пока ни на кого не подписан';
  const text  = rec
    ? 'Сюда попадают клипы и посты из каналов OKO и тех, на кого ты подпишешься. Выложи свой первый клип или подпишись на канал — лента начнёт расти.'
    : 'В «Подписках» видно только тех, на кого ты подписался. Подпишись на канал OKO — и его посты появятся здесь первыми.';
  const acts = [
    `<button class="fa-empty-cta" type="button" onclick="showTab('chats'); if(typeof chOpen==='function') chOpen('list')">${I('megaphone')}<span>Найти каналы</span></button>`,
    rec
      ? `<button class="fa-empty-cta ghost" type="button" onclick="if(typeof okoOpenClips==='function') okoOpenClips()">${I('clips')}<span>Выложить клип</span></button>`
      : `<button class="fa-empty-cta ghost" type="button" onclick="faGoRec()">${I('compass')}<span>Открыть рекомендации</span></button>`
  ].join('');
  list.innerHTML = `<div class="fa-empty">${faEmptyArt()}<b>${title}</b><span>${text}</span><div class="fa-empty-acts">${acts}</div></div>`;
}

/* ---------- время публикации на карточке (короткий формат: «3 ч», «2 д») ---------- */
function faShortAge(ts){
  if(!ts) return '';
  const min = Math.max(0, Date.now() - ts) / 60000;
  if(min < 1) return 'сейчас';
  if(min < 60) return Math.floor(min) + ' мин';
  const h = min / 60;
  if(h < 24) return Math.floor(h) + ' ч';
  const days = h / 24;
  if(days < 7) return Math.floor(days) + ' д';
  try{ return new Date(ts).toLocaleDateString('ru-RU', {day:'numeric', month:'short'}); }
  catch(e){ return Math.floor(days) + ' д'; }
}
/* дорисовать метку времени в .sub каждой карточки (обе вкладки, без дублей).
   Посты поздних модулей (demo-content и т.п.) грузятся после faSeedContent и не имеют ts —
   проставляем детерминированный возраст по id (стабилен между рендерами, без «прыжков»). */
function faStampTimes(list){
  if(!list) return;
  list.querySelectorAll('article.post').forEach(art=>{
    const sub = art.querySelector('.head .sub');
    if(!sub || sub.querySelector('.fa-age')) return;
    const p = postById(faIdOf(art));
    if(!p) return;
    if(!p.ts) p.ts = Date.now() - (1 + Math.floor(faRand(p.id) * 47)) * 36e5;  /* 1–48 ч, стабильно */
    const a = faShortAge(p.ts);
    if(a) sub.insertAdjacentHTML('beforeend', `<span class="fa-age">· ${a}</span>`);
  });
}

/* индикатор «прочитали» (двойная галка + число уникальных) — только для СВОИХ постов,
   как в Telegram. Показов у поста >= уникальных читателей, берём консервативные 72%. */
function faSeenMark(list){
  if(!list) return;
  const me = (typeof PROFILE !== 'undefined' && PROFILE && PROFILE.name) ? PROFILE.name : null;
  if(!me) return;
  list.querySelectorAll('article.post').forEach(art=>{
    if(art.querySelector('.fa-seen')) return;
    const p = postById(faIdOf(art));
    if(!p || p.name !== me) return;
    const sub = art.querySelector('.head .sub'); if(!sub) return;
    const uniq = Math.max(1, Math.floor((p.views || 0) * 0.72));
    const n = (typeof fmtN === 'function') ? fmtN(uniq) : String(uniq);
    sub.insertAdjacentHTML('beforeend',
      `<span class="fa-seen" title="Прочитали ${uniq}">${I('check')}${I('check')}<b>${n}</b></span>`);
  });
}

const _faPrevRenderFeed = renderFeed;
renderFeed = function(kind){
  kind = kind || curFeedKind || 'sub';
  /* фильтр скрытых: пользователь свайпнул или нажал «Не интересно» — не показываем даже после F5 */
  const hset = faLoadHidden();
  if(hset && hset.size){
    if(Array.isArray(POSTS.sub)) POSTS.sub = POSTS.sub.filter(p => !hset.has(+p.id));
    if(Array.isArray(POSTS.rec)) POSTS.rec = POSTS.rec.filter(p => !hset.has(+p.id));
  }
  _faPrevRenderFeed(kind);           /* декор-«почему показано» — только для rec */
  const list = document.getElementById('feedList');
  if(list && !list.querySelector('article.post')){ faRenderEmpty(list, kind); return; }
  faStampTimes(list);                /* время публикации — на обеих вкладках */
  faSeenMark(list);                  /* прочитали — только для своих постов (обе вкладки) */
  if(kind === 'rec') faDecorate();
};

/* раскрыть/свернуть тонкое пояснение «как работает лента» (тултип-строка) */
function faToggleInfo(btn){
  FA.infoOpen = !FA.infoOpen;
  const head = btn.closest('.fa-head'); if(!head) return;
  btn.classList.toggle('fa-open', FA.infoOpen);
  const ex = head.querySelector('.fa-explain');
  if(ex) ex.classList.toggle('fa-open', FA.infoOpen);
  try{ localStorage.setItem('oko-feed-info', FA.infoOpen ? '1' : '0'); }catch(e){}
}

function faRefresh(){
  FA.seed = (Math.random() * 4294967295) >>> 0;   /* новый шум → пересборка порядка */
  FA.now = Date.now();                             /* обновляем epoch свежести только по явному действию */
  FA._ints = null;                                 /* перечитать интересы регистрации (сид персонализации) */
  renderFeed('rec');
  const list = document.getElementById('feedList');
  if(list){ list.classList.remove('fa-anim'); void list.offsetWidth; list.classList.add('fa-anim');
    setTimeout(()=>list.classList.remove('fa-anim'), 900); }
  const m = document.querySelector('main'); if(m) m.scrollTo({top:0, behavior:'smooth'});
  toast('Подборка пересобрана');
}

/* ================= 5. БЕСКОНЕЧНАЯ ЛЕНТА: генератор вариаций ================= */
const FA_GEN = {
  ai:{a:[['НЦ','Нейро-цех'],['ПИ','Промпт-инженер'],['АК','Агенты и код'],['ЛБ','Лаборатория 42']], t:[
    n=>`Собрали агентный пайплайн из ${n(3,6)} шагов: тема, сценарий, озвучка, монтаж. На выходе ${n(20,40)} роликов в месяц без ручной рутины.`,
    n=>`Тест локальной модели на ноутбуке: черновые субтитры за ${n(2,6)} минуты вместо получаса. Стоимость потока упала в ${n(8,40)} раз против облака.`,
    n=>`Разобрали ${n(30,90)} промптов для обложек: побеждает связка «референс + свет + стиль». Таблица сэкономит ${n(3,10)} часов в неделю.`]},
  content:{a:[['КЛ','Контент-лаб'],['ВК','Вертикальный кадр'],['СЦ','Сценарный цех'],['ХК','Хук и кадр']], t:[
    n=>`Проверили ${n(30,80)} хуков: движение в кадре + незакрытый вопрос держат удержание до ${n(55,70)}%. Примеры — в карусели.`,
    n=>`Пересобрали монтаж под темп музыки: смена кадра каждые ${n(2,4)} секунды подняла досмотры на ${n(15,45)}%.`,
    n=>`Караоке-субтитры + звуковые акценты дают +${n(10,30)}% удержания на первых 10 секундах. Пресет — в закрепе.`]},
  business:{a:[['ДП','Дело и прибыль'],['ЮЭ','Юнит-экономика'],['СБ','Системный бизнес'],['МН','Маржа и налоги']], t:[
    n=>`Посчитали юнит-экономику: заявка из контента стоит ${n(250,600)} ₽ против ${n(900,1800)} ₽ из таргета. Цифры по неделям внутри.`,
    n=>`Система вместо вдохновения: план на ${n(20,40)} постов, батч-съёмка за ${n(1,3)} дня, автопостинг. Выручка выросла на ${n(20,60)}%.`,
    n=>`Чек-лист делегирования: ${n(5,9)} процессов, которые забирают у собственника ${n(10,25)} часов в неделю. Шаблон отдаём бесплатно.`]},
  marketing:{a:[['ПФ','Перформанс-клуб'],['ЛГ','Лидген-цех'],['КТ','Креатив-тест'],['ВР','Воронки и ретаргет']], t:[
    n=>`A/B на ${n(6,16)} креативах: UGC-формат обошёл студийный в ${n(3,5)} тестах из 5. CPL ниже на ${n(18,42)}%.`,
    n=>`Ретаргет на досмотревших 75% ролика: лид дешевле в ${n(2,4)} раза. Настройка — один вечер, схема в посте.`,
    n=>`Воронка из ленты: пост → лид-магнит → бот. Конверсия в заявку ${n(4,11)}% без бюджета на трафик.`]},
  games:{a:[['ИС','Инди-сборка'],['ГД','Гейм-дизайн вслух'],['ЛВ','Левел и код'],['ПТ','Плейтест']], t:[
    n=>`Вертикальный срез за ${n(4,8)} недель командой из ${n(2,4)} человек: что выкинули из бэклога и почему это спасло демо.`,
    n=>`Мини-игры в мессенджерах: сессия ${n(3,6)} минут, возврат на день ${n(25,45)}%. Подборка механик удержания внутри.`,
    n=>`Плейтест на ${n(20,60)} игроках: главная метрика — время до первого «ага-момента». У нас вышло ${n(20,90)} секунд.`]},
  crypto:{a:[['ЧО','Чейн-обзор'],['ДФ','DeFi по-русски'],['ТС','TON-сводка'],['ХВ','Холодный кошелёк']], t:[
    n=>`Активность кошельков TON за месяц +${n(12,40)}%: смотрим, какие мини-аппы забирают аудиторию быстрее всех.`,
    n=>`Комиссии в сети упали до минимума — окно для перекладки портфеля. Разбор ${n(3,7)} стратегий с расчётом рисков.`,
    n=>`Аудит перед покупкой токена: команда, разлоки, ликвидность. ${n(3,5)} пункта отсеивают 90% мусора.`]},
};
function faRndInt(a,b){ return a + Math.floor(Math.random()*(b-a+1)); }

function faGenerate(count, page){
  const ints = [...faInterests()];
  const sig = Object.keys(FA.signals).filter(k=>FA.signals[k] >= 2);
  const all = Object.keys(FA_GEN);
  const pool = [...ints, ...ints, ...sig, ...all];   /* интересы и сигналы — с большим весом */
  const out = [];
  for(let i=0; i<count; i++){
    const topic = pool[faRndInt(0, pool.length-1)] || all[i % all.length];
    const g = FA_GEN[topic];
    let ai = faRndInt(0, g.a.length-1), ti = faRndInt(0, g.t.length-1), guard = 0;
    while(FA.usedCombos[topic+ai+'_'+ti] && guard++ < 12){ ai = faRndInt(0, g.a.length-1); ti = faRndInt(0, g.t.length-1); }
    FA.usedCombos[topic+ai+'_'+ti] = 1;
    const views = faRndInt(1200, 34000);
    const likes = Math.round(views * (0.008 + Math.random()*0.05));
    out.push({
      id: FA.genId++, topic, faPage: page,
      ava: g.a[ai][0], name: g.a[ai][1], sub: 'канал · ' + (1 + Math.random()*24).toFixed(1) + 'к',
      body: g.t[ti](faRndInt), media: Math.random() < 0.3 ? '0:' + faRndInt(31,59) : null,
      likes, views, reposts: Math.round(likes * (0.05 + Math.random()*0.15)),
      liked:false, saved:false, comments:[], ts: Date.now() - faRndInt(1,36)*36e5,
    });
  }
  return out;
}

function faLoadMore(){
  if(FA.loading || FA.page >= FA.maxPages || curFeedKind !== 'rec') return;
  const list = document.getElementById('feedList'); if(!list) return;
  FA.loading = true;
  const skel = `<div class="fa-skel"><div class="fa-skel-head"><div class="fa-b fa-skel-ava"></div><div style="flex:1"><div class="fa-b fa-skel-l1"></div><div class="fa-b fa-skel-l2"></div></div></div><div class="fa-b fa-skel-body w1"></div><div class="fa-b fa-skel-body w2"></div><div class="fa-b fa-skel-body w3"></div></div>`;
  const sent = list.querySelector('.fa-sentinel');
  (sent || list).insertAdjacentHTML(sent ? 'beforebegin' : 'beforeend', skel + skel + skel);
  setTimeout(()=>{
    list.querySelectorAll('.fa-skel').forEach(x=>x.remove());
    FA.page++;
    FA.now = Date.now();               /* свежесть/скорость пересчитываются на новом epoch при подгрузке */
    faGenerate(5, FA.page).forEach(p=>POSTS.rec.push(p));
    const m = document.querySelector('main');
    const st = m ? m.scrollTop : 0;
    renderFeed('rec');                 /* партия ранжируется ниже за счёт faPage-штрафа */
    if(m) m.scrollTop = st;
    FA.loading = false;
  }, 500);
}

/* ================= 6. КОММЕНТАРИИ-ТРЕДЫ (аватар, ник, время, лайки, ответы) =================
   Ядро рендерит плоский список {a,n,t} в #cmtList. Здесь — красивый тред: аватар,
   имя+verified, относительное время, лайк коммента с анимацией, ответы (1 уровень),
   поле ввода с аватаром и контекстом «Ответ …». Модель дополняется на лету, session-only. */
FA.cidSeq = 900000;
FA.replyTo = null;
const FA_CMT_PAGE = 6;   /* сколько корневых комментариев показываем до «показать ещё» */

function faPlural(n, one, few, many){
  const m10 = n % 10, m100 = n % 100;
  if(m10 === 1 && m100 !== 11) return one;
  if(m10 >= 2 && m10 <= 4 && (m100 < 12 || m100 > 14)) return few;
  return many;
}
function faTimeAgo(ts){
  if(!ts) return 'сейчас';
  const d = Math.max(0, Date.now() - ts), min = d / 60000;
  if(min < 1) return 'сейчас';
  if(min < 60){ const n = Math.floor(min); return n + ' ' + faPlural(n, 'минуту', 'минуты', 'минут') + ' назад'; }
  const h = min / 60;
  if(h < 24){ const n = Math.floor(h); return n + ' ' + faPlural(n, 'час', 'часа', 'часов') + ' назад'; }
  const days = h / 24;
  if(days < 7){ const n = Math.floor(days); return n + ' ' + faPlural(n, 'день', 'дня', 'дней') + ' назад'; }
  try{ return new Date(ts).toLocaleDateString('ru-RU', {day:'numeric', month:'short'}); }catch(e){ return Math.floor(days) + ' д'; }
}
function faPrepOne(c, p, i, isReply){
  if(!c || typeof c !== 'object') return;
  if(c.cid == null) c.cid = FA.cidSeq++;
  if(c.ts == null){
    const base = (p && p.ts) ? Math.min(Date.now() - i * 60000, p.ts + (i + 1) * faRndInt(3, 55) * 60000)
                             : Date.now() - faRndInt(2, 3200) * 60000;
    c.ts = Math.min(base, Date.now() - 30000);
  }
  if(c.likes == null) c.likes = faRndInt(0, isReply ? 12 : 54);
  if(c.liked == null) c.liked = false;
  if(!Array.isArray(c.replies)) c.replies = [];
  c.replies.forEach((r, j) => faPrepOne(r, p, j, true));
}
function faPrepComments(p){
  if(!p) return;
  if(!Array.isArray(p.comments)) p.comments = [];
  p.comments.forEach((c, i) => faPrepOne(c, p, i, false));
}
function faFindComment(p, cid){
  if(!p || !Array.isArray(p.comments)) return null;
  for(const c of p.comments){
    if(c.cid === cid) return c;
    if(Array.isArray(c.replies)) for(const r of c.replies) if(r.cid === cid) return r;
  }
  return null;
}
function faCount(p){
  if(!p || !Array.isArray(p.comments)) return 0;
  let n = p.comments.length;
  p.comments.forEach(c => { if(Array.isArray(c.replies)) n += c.replies.length; });
  return n;
}
function faVerified(name){
  try{ if(typeof VERIFIED !== 'undefined' && typeof vBadge === 'function' && VERIFIED.has(name)) return vBadge(name); }catch(e){}
  return '';
}
function faCommentHTML(postId, c, isReply){
  const meCls    = c.me ? ' fac-me' : '';
  const likeCls  = c.liked ? ' on' : '';
  const likeCnt  = c.likes > 0 ? `<i>${c.likes}</i>` : '';
  const badge    = faVerified(c.n);
  const initial  = esc(String(c.a || (c.n ? c.n[0] : '?')).slice(0, 2).toUpperCase());
  let thread = '';
  if(!isReply && Array.isArray(c.replies) && c.replies.length){
    const open  = c._open ? ' open' : '';
    const label = c._open ? 'Скрыть ответы'
      : 'Показать ' + c.replies.length + ' ' + faPlural(c.replies.length, 'ответ', 'ответа', 'ответов');
    thread =
      `<button class="fac-toggle${open}" type="button" onclick="faToggleReplies(this,${postId},${c.cid})">`+
        `<span class="fac-tline"></span><span class="fac-tl">${label}</span>${I('chev')}</button>`+
      `<div class="fac-replies${open}">${c.replies.map(r => faCommentHTML(postId, r, true)).join('')}</div>`;
  }
  return (
    `<div class="fac-item${isReply ? ' fac-r' : ''}" data-cid="${c.cid}">`+
      `<div class="fac-ava${meCls}">${initial}</div>`+
      `<div class="fac-main">`+
        `<div class="fac-row"><span class="fac-name">${esc(c.n || 'Гость')}</span>${badge}`+
          `<span class="fac-time">${faTimeAgo(c.ts)}</span></div>`+
        `<div class="fac-text">${esc(c.t || '')}</div>`+
        `<div class="fac-acts">`+
          `<button class="fac-like${likeCls}" type="button" aria-label="Нравится" onclick="faLikeComment(${postId},${c.cid})">${I('heart')}${likeCnt}</button>`+
          (isReply ? '' : `<button class="fac-reply" type="button" onclick="faReplyTo(${postId},${c.cid})">${I('reply')}<span>Ответить</span></button>`)+
        `</div>`+
        thread +
      `</div>`+
    `</div>`
  );
}
function faRenderComments(id){
  const p = postById(id), list = document.getElementById('cmtList');
  if(!p || !list) return;
  faPrepComments(p);
  const n = faCount(p);
  const h3 = document.querySelector('#sheet-comments h3');
  if(h3) h3.innerHTML = 'Комментарии' + (n ? ` <span class="fac-count">${n}</span>` : '');
  if(!p.comments.length){
    list.innerHTML = `<div class="fac-empty">${I('comment')}<b>Пока тихо</b><span>Стань первым, кто оставит комментарий</span></div>`;
    return;
  }
  const shown = Math.min(p.comments.length, p._cmtShown || FA_CMT_PAGE);
  const items = p.comments.slice(0, shown).map(c => faCommentHTML(id, c, false)).join('');
  const rest = p.comments.length - shown;
  const more = rest > 0
    ? `<button class="fac-more" type="button" onclick="faMoreComments(${id})">`+
        `<span>Показать ещё ${rest} ${faPlural(rest, 'комментарий', 'комментария', 'комментариев')}</span>${I('chev')}</button>`
    : '';
  list.innerHTML = `<div class="fac-wrap">${items}</div>${more}`;
}
function faMoreComments(id){
  const p = postById(id); if(!p) return;
  p._cmtShown = (p._cmtShown || FA_CMT_PAGE) + FA_CMT_PAGE;
  faRenderComments(id);
}
function faUpdateCardCount(p){
  if(!p) return;
  try{
    const card = feedCardEl(p.id), cc = card && card.querySelector('.act-cmt');
    if(cc) cc.textContent = faCount(p);
  }catch(e){}
}
function faLikeComment(postId, cid){
  const p = postById(postId); if(!p) return;
  const c = faFindComment(p, cid); if(!c) return;
  c.liked = !c.liked;
  c.likes = Math.max(0, (c.likes || 0) + (c.liked ? 1 : -1));
  if(p.topic) faSignal(p.topic, c.liked ? 0.4 : -0.4);
  faAuthor(p.name, c.liked ? 0.3 : -0.3);
  const btn = document.querySelector('#cmtList .fac-item[data-cid="' + cid + '"] .fac-like');
  if(btn){
    btn.classList.toggle('on', c.liked);
    let i = btn.querySelector('i');
    if(c.likes > 0){ if(!i){ i = document.createElement('i'); btn.appendChild(i); } i.textContent = c.likes; }
    else if(i){ i.remove(); }
    btn.classList.remove('fac-burst'); void btn.offsetWidth;
    if(c.liked) btn.classList.add('fac-burst');
  }
}
function faToggleReplies(btn, postId, cid){
  if(!btn) return;
  const wrap = btn.nextElementSibling;
  const p = postById(postId), c = p ? faFindComment(p, cid) : null;
  if(!wrap || !c) return;
  const open = wrap.classList.toggle('open');
  btn.classList.toggle('open', open);
  c._open = open;
  const lbl = btn.querySelector('.fac-tl');
  if(lbl) lbl.textContent = open ? 'Скрыть ответы'
    : 'Показать ' + c.replies.length + ' ' + faPlural(c.replies.length, 'ответ', 'ответа', 'ответов');
}
function faReplyTo(postId, cid){
  const p = postById(postId); if(!p) return;
  const c = faFindComment(p, cid); if(!c) return;
  FA.replyTo = {postId: postId, cid: cid, name: c.n};
  const chip = document.getElementById('facReplyChip');
  if(chip){ chip.hidden = false; const nm = chip.querySelector('.fac-rc-name'); if(nm) nm.textContent = c.n || 'комментарий'; }
  const inp = document.getElementById('cmtInput');
  if(inp){ inp.placeholder = 'Ответить ' + (c.n || '') + '…'; inp.focus(); }
}
function faClearReply(){
  FA.replyTo = null;
  const chip = document.getElementById('facReplyChip'); if(chip) chip.hidden = true;
  const inp = document.getElementById('cmtInput'); if(inp) inp.placeholder = 'Написать комментарий…';
}

/* полное переопределение ядра */
openComments = function(id){
  const p = postById(id); if(!p) return;
  commentsFor = id;
  p._cmtShown = FA_CMT_PAGE;          /* каждое открытие — с первой «страницы» */
  faClearReply();
  faRenderComments(id);
  openSheet('comments');
  if(p.topic) faSignal(p.topic, 0.5);
  faAuthor(p.name, 0.6);                 /* открыл обсуждение — интерес к автору */
};
addComment = function(){
  const inp = document.getElementById('cmtInput'); if(!inp) return;
  const t = inp.value.trim();
  if(!t || commentsFor == null) return;
  const p = postById(commentsFor); if(!p) return;
  const meName = (typeof PROFILE !== 'undefined' && PROFILE && PROFILE.name) ? PROFILE.name : 'Ты';
  const node = {a: meName[0], n: meName, t: t, ts: Date.now(), likes: 0, liked: false,
    cid: FA.cidSeq++, replies: [], me: true};
  if(FA.replyTo && FA.replyTo.postId === commentsFor){
    const parent = faFindComment(p, FA.replyTo.cid);
    if(parent){ if(!Array.isArray(parent.replies)) parent.replies = []; parent.replies.push(node); parent._open = true; }
    else p.comments.push(node);
  }else{
    p.comments.push(node);
  }
  inp.value = '';
  faClearReply();
  if(p.topic) faSignal(p.topic, 0.7);
  faAuthor(p.name, 1);                   /* написал коммент — сильный сигнал интереса к автору */
  p._cmtShown = Math.max(p._cmtShown || FA_CMT_PAGE, p.comments.length);  /* свой коммент всегда виден */
  faRenderComments(commentsFor);
  faUpdateCardCount(p);
  const list = document.getElementById('cmtList');
  const el = list && list.querySelector('.fac-item[data-cid="' + node.cid + '"]');
  if(el){ el.classList.add('fac-new'); el.scrollIntoView({block:'nearest', behavior:'smooth'}); }
};

/* ================= 7. ДВОЙНОЙ ТАП ПО КАРТОЧКЕ — ЛАЙК (как в Instagram) =================
   Двойной тап/клик по телу поста ставит лайк (никогда не снимает) и рисует
   всплывающее сердце в точке нажатия. Лайк точечный (likePost — без пересборки),
   поэтому моргания нет. Интерактив (кнопки/меню/чипы) исключён. */
function faHeartBurst(art, x, y){
  if(!art) return;
  const r = art.getBoundingClientRect();
  const h = document.createElement('span');
  h.className = 'fa-dtap-heart';
  h.innerHTML = I('heart');
  h.style.left = Math.round((x != null ? x : r.left + r.width / 2) - r.left) + 'px';
  h.style.top  = Math.round((y != null ? y : r.top + r.height / 2) - r.top) + 'px';
  art.appendChild(h);
  setTimeout(() => h.remove(), 760);
}
function faDoubleTapLike(){
  const list = document.getElementById('feedList');
  if(!list || list._faDtap) return; list._faDtap = 1;
  let lastT = 0, lastEl = null;
  const fire = (art, x, y) => {
    const pid = faIdOf(art); if(pid == null) return;
    const p = postById(pid); if(!p) return;
    if(!p.liked && typeof likePost === 'function') likePost(pid);  /* double-tap только ставит лайк */
    faHeartBurst(art, x, y);
    if(navigator.vibrate) try{ navigator.vibrate(12); }catch(e){}
  };
  list.addEventListener('click', e => {
    /* не мешаем интерактивным элементам и выделению текста */
    if(e.target.closest('button,a,input,textarea,.acts,.post-more,.fa-why,.fa-bar,.fa-explain,.fa-empty')) return;
    const sel = window.getSelection && window.getSelection();
    if(sel && String(sel).length > 1) return;
    const art = e.target.closest('article.post'); if(!art) return;
    const now = Date.now();
    if(now - lastT < 330 && lastEl === art){ lastT = 0; lastEl = null; fire(art, e.clientX, e.clientY); }
    else { lastT = now; lastEl = art; }
  });
}

/* ================= 7b. СВАЙП-ВЛЕВО ПО ПОСТУ = СКРЫТЬ (Instagram/TG) =================
   Тянем карточку влево на > 100 px — вызывается hidePost (chain уже сохраняет id в
   localStorage), карточка красиво сворачивается. Порог активации по горизонтали
   отсекает вертикальный скролл: если dy > dx, жест отменяется, скролл идёт как обычно. */
function faSwipeToHide(){
  const list = document.getElementById('feedList');
  if(!list || list._faSwipe) return; list._faSwipe = 1;
  let art = null, sx = 0, sy = 0, dx = 0, dy = 0, active = false, pid = null;
  const THR = 100;   /* дистанция скрытия */
  const AX  = 14;    /* порог активации горизонтального жеста */

  const cleanupInline = (el) => {
    if(!el) return;
    el.style.transform = ''; el.style.opacity = '';
    el.style.transition = ''; el.classList.remove('fa-swiping');
    const rev = el.querySelector('.fa-swipe-hint'); if(rev) rev.remove();
  };

  const start = (e) => {
    if(e.pointerType === 'mouse' && e.button !== 0) return;
    const t = e.target;
    if(!t || !t.closest) return;
    if(t.closest('button,a,input,textarea,.acts,.post-more,.fa-why,.fa-bar,.fa-explain,.fa-empty,.fa-load-more,.fa-head,.media')) return;
    const a = t.closest('article.post'); if(!a) return;
    const id = faIdOf(a); if(id == null) return;
    const p = postById(id); if(!p || p.promoted) return;   /* рекламу не скрываем свайпом */
    art = a; pid = id; sx = e.clientX; sy = e.clientY; dx = 0; dy = 0; active = false;
  };

  const move = (e) => {
    if(!art) return;
    dx = e.clientX - sx; dy = e.clientY - sy;
    if(!active){
      if(Math.abs(dy) > AX && Math.abs(dy) > Math.abs(dx)){ art = null; return; }  /* вертикальный жест: отдаём скроллу */
      if(Math.abs(dx) < AX) return;
      active = true;
      art.classList.add('fa-swiping');
      if(!art.querySelector('.fa-swipe-hint')){
        const hint = document.createElement('span');
        hint.className = 'fa-swipe-hint';
        hint.innerHTML = I('fa-x') + '<b>Скрыть</b>';
        art.appendChild(hint);
      }
    }
    const shift = dx < 0 ? dx : dx * 0.22;                 /* влево свободно, вправо резинка */
    art.style.transform = 'translateX(' + shift + 'px)';
    art.style.opacity = String(Math.max(0.4, 1 - Math.abs(shift)/340));
    const hint = art.querySelector('.fa-swipe-hint');
    if(hint) hint.style.opacity = String(Math.min(1, Math.abs(shift)/THR));
  };

  const end = () => {
    if(!art){ return; }
    const el = art, id = pid; art = null; pid = null;
    if(!active){ return; }
    if(dx < -THR){
      /* скрываем: сначала уезжаем влево, потом схлопываем высоту, потом чистим POSTS */
      el.style.transition = 'transform .26s ease, opacity .26s ease';
      el.style.transform = 'translateX(-115%)'; el.style.opacity = '0';
      setTimeout(function(){
        try{
          el.style.transition = 'max-height .28s ease, margin .28s ease, padding .28s ease, border-color .28s ease';
          el.style.maxHeight = el.offsetHeight + 'px';
          requestAnimationFrame(function(){
            el.style.maxHeight = '0'; el.style.marginTop = '0'; el.style.marginBottom = '0';
            el.style.paddingTop = '0'; el.style.paddingBottom = '0'; el.style.borderColor = 'transparent';
          });
        }catch(e){}
      }, 240);
      setTimeout(function(){
        try{
          if(id != null && typeof hidePost === 'function') hidePost(id);
          else{ cleanupInline(el); }
          if(typeof toast === 'function') toast('Пост скрыт, больше не покажем такое');
        }catch(e){}
      }, 540);
    }else{
      /* не дотянул — плавно возвращаем */
      el.style.transition = 'transform .2s ease, opacity .2s ease';
      el.style.transform = ''; el.style.opacity = '';
      const hint = el.querySelector('.fa-swipe-hint'); if(hint) hint.style.opacity = '';
      setTimeout(function(){ cleanupInline(el); }, 220);
    }
  };

  if(window.PointerEvent){
    list.addEventListener('pointerdown', start, {passive:true});
    list.addEventListener('pointermove', move, {passive:true});
    ['pointerup','pointercancel','pointerleave'].forEach(t => list.addEventListener(t, end, {passive:true}));
  }else{
    list.addEventListener('touchstart', e=>{ const t = e.touches && e.touches[0]; if(t) start({pointerType:'touch', button:0, clientX:t.clientX, clientY:t.clientY, target:e.target}); }, {passive:true});
    list.addEventListener('touchmove',  e=>{ const t = e.touches && e.touches[0]; if(t) move({clientX:t.clientX, clientY:t.clientY}); }, {passive:true});
    ['touchend','touchcancel'].forEach(t => list.addEventListener(t, end, {passive:true}));
  }
}

/* ================= САМОИНИЦИАЛИЗАЦИЯ ================= */
(function faInit(){
  /* svg-иконка «обновить» в общие defs */
  const defs = document.querySelector('svg defs');
  const addSym = (id, inner)=>{
    if(defs && !document.getElementById(id)){
      const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
      s.setAttribute('id', id); s.setAttribute('viewBox','0 0 100 100');
      s.innerHTML = inner; defs.appendChild(s);
    }
  };
  addSym('i-fa-refresh', '<path d="M84 50a34 34 0 1 1-10-24" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/><polyline points="76 10 76 27 59 26" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/>');
  addSym('i-fa-info', '<circle cx="50" cy="50" r="38" fill="none" stroke="currentColor" stroke-width="7"/><line x1="50" y1="45" x2="50" y2="70" stroke="currentColor" stroke-width="7" stroke-linecap="round"/><circle cx="50" cy="31" r="4.6" fill="currentColor"/>');
  addSym('i-fa-x', '<line x1="28" y1="28" x2="72" y2="72" stroke="currentColor" stroke-width="8" stroke-linecap="round"/><line x1="72" y1="28" x2="28" y2="72" stroke="currentColor" stroke-width="8" stroke-linecap="round"/>');
  /* восходящий график — для бейджа «В тренде» и индикатора «растёт» */
  addSym('i-fa-trend', '<polyline points="16 64 40 42 56 56 84 26" fill="none" stroke="currentColor" stroke-width="8" stroke-linecap="round" stroke-linejoin="round"/><polyline points="64 26 84 26 84 46" fill="none" stroke="currentColor" stroke-width="8" stroke-linecap="round" stroke-linejoin="round"/>');
  /* i18n: чтобы EN-режим не оставлял русские строки шапки (авто-переводчик по ST_DICT) */
  if(typeof ST_DICT !== 'undefined'){
    const add = {
      'Умная лента':'Smart feed',
      'Обновить подборку':'Refresh feed',
      'Ранжирует алгоритм OKO: вовлечённость и свежесть — как в Instagram, в отличие от Telegram. Реагируй на посты — лента точнее подстроится под тебя.':
        'Ranked by the OKO algorithm: engagement and recency — like Instagram, unlike Telegram. React to posts and the feed tunes to you.',
      'Похоже на то, что ты лайкал':'Similar to what you liked',
      'Похоже на лайкнутое':'Similar to what you liked',
      'Автор, которого ты читаешь':'An author you follow',
      'В тренде':'Trending','растёт':'rising',
      'Популярно сейчас':'Trending now',
      'Подборка пересобрана':'Feed rebuilt',
      'Это вся подборка на сейчас. Жми «Обновить подборку» сверху — алгоритм найдёт свежее под тебя':'That is the whole feed for now. Tap “Refresh feed” above — the algorithm finds fresh posts for you',
      'Твой интерес: Нейросети':'Your interest: AI','Твой интерес: Контент':'Your interest: Content',
      'Твой интерес: Бизнес':'Your interest: Business','Твой интерес: Маркетинг':'Your interest: Marketing',
      'Твой интерес: Игры':'Your interest: Games','Твой интерес: Крипта':'Your interest: Crypto',
      'Ответить':'Reply','Скрыть ответы':'Hide replies','Пока тихо':'No comments yet',
      'Стань первым, кто оставит комментарий':'Be the first to comment',
      'Написать комментарий…':'Write a comment…','Комментарий добавлен':'Comment added',
      'Канал':'Channel','Открыть рекомендации':'Open recommendations',
      'Пока пусто':'Nothing here yet','Тут появятся посты':'Posts will appear here',
      'Свежие посты закончились. Обнови подборку — алгоритм за секунду соберёт новую ленту под твои интересы.':
        'No fresh posts found. Refresh and the algorithm will build a new feed for you.',
      'Подпишись на каналы и авторов — их посты появятся здесь. А пока загляни в рекомендации.':
        'Follow channels and authors — their posts show up here. Meanwhile, check the recommendations.',
      'Загрузить ещё':'Load more',
      'Скрыть':'Hide',
      'Пост скрыт, больше не покажем такое':'Post hidden, we will not show it again',
      'Ссылка на пост скопирована':'Post link copied',
      'Это вся подборка на сейчас. Жми «Обновить подборку» сверху, алгоритм найдёт свежее под тебя':
        'That is the whole feed for now. Tap “Refresh feed” above, the algorithm finds fresh posts for you',
    };
    for(const k in add) if(!(k in ST_DICT)) ST_DICT[k] = add[k];
  }
  try{ FA.infoOpen = localStorage.getItem('oko-feed-info') === '1'; }catch(e){}
  faLoadSignals();
  faLoadAuthors();
  faLoadHidden();

  /* обогащение composer'а комментариев: аватар автора + плашка «Ответ …» (base.html не трогаем) */
  (function faWireCompose(){
    const compose = document.querySelector('#sheet-comments .cmt-compose');
    if(!compose) return;
    compose.classList.add('fac-compose');
    if(!compose.querySelector('.fac-compose-ava')){
      const me = document.createElement('div');
      me.className = 'fac-ava fac-me fac-compose-ava';
      me.textContent = (typeof PROFILE !== 'undefined' && PROFILE && PROFILE.name) ? PROFILE.name[0] : 'Я';
      compose.insertBefore(me, compose.firstChild);
    }
    if(!document.getElementById('facReplyChip') && compose.parentNode){
      const chip = document.createElement('div');
      chip.id = 'facReplyChip';
      chip.className = 'fac-reply-chip';
      chip.hidden = true;
      chip.innerHTML = I('reply') + '<span>Ответ <b class="fac-rc-name"></b></span>' +
        '<button type="button" class="fac-rc-x" aria-label="Отменить ответ" onclick="faClearReply()">' + I('fa-x') + '</button>';
      compose.parentNode.insertBefore(chip, compose);
    }
  })();
  if('IntersectionObserver' in window){
    FA.io = new IntersectionObserver(es=>{
      es.forEach(e=>{
        const feed = document.getElementById('screen-feed');
        if(e.isIntersecting && feed && feed.classList.contains('active')) faLoadMore();
      });
    }, {rootMargin:'240px'});
  }
  faDoubleTapLike();
  faSwipeToHide();
  /* если лента уже открыта на рекомендациях — декорировать сразу */
  const feed = document.getElementById('screen-feed');
  if(feed && feed.classList.contains('active') && curFeedKind === 'rec') faDecorate();
})();

/* ============================================================================
   8. REELS VIEWER — иммерсивный вертикальный плеер «Клипы» (Instagram/TikTok)
   Новый ОВЕРЛЕЙ поверх ленты (список ленты НЕ трогаем). Открывается по тапу на
   видео-область карточки. Лайк/коммент/шер/сейв переиспользуют ФУНКЦИИ И СТЕЙТ
   ядра (likePost/openComments/repost/toggleSave) — реакция в клипе отражается на
   карточке ленты и наоборот (общий объект поста). ПЕРФ: только transform/opacity,
   прогресс — CSS-анимация (0 работы на кадр), бренд-движение живёт лишь на активном
   слайде. Всё в try/catch, префикс faReels/FAR.
   ============================================================================ */
const FAR = {
  open:false, idx:0, ids:[], paused:false,
  drag:null,          /* {y0,t0} во время свайпа */
  keyHandler:null,    /* document keydown — вешаем на открытие, снимаем на закрытие */
  wheelLock:0,        /* дебаунс колеса мыши */
  lastTap:0, lastX:0, lastY:0, /* распознавание двойного тапа по слайду */
};

/* бренд-градиенты фона по теме поста — все тёмные (чёрный + лайм-семья) */
const FA_REEL_G = {
  ai:       ['#16240a','#04060a'],
  content:  ['#1c2708','#050703'],
  business: ['#122011','#03060a'],
  marketing:['#231d07','#060404'],
  games:    ['#0d2216','#03070a'],
  crypto:   ['#0a1f28','#04060a'],
};
function faReelsBg(p){
  try{
    const g = FA_REEL_G[p && p.topic] || ['#16240a','#04060a'];
    return `--g1:${g[0]};--g2:${g[1]}`;
  }catch(e){ return '--g1:#16240a;--g2:#04060a'; }
}

/* заголовок «видео»: первое ёмкое предложение подписи (визуальный акцент кадра) */
function faReelsHeadline(p){
  try{
    const t = String((p && p.body) || '').split(/(?<=[.!?:])\s/)[0] || (p && p.body) || '';
    return esc(t.length > 64 ? t.slice(0, 62).trim() + '…' : t);
  }catch(e){ return ''; }
}

/* список видео-постов ленты: media != null, дедуп, в порядке ленты (rec-приоритет) */
function faReelsList(){
  try{
    const seen = {}, out = [];
    /* сначала — как отрисовано в текущей ленте (живой порядок ранжирования) */
    const dom = document.querySelectorAll('#feedList article.post');
    dom.forEach(a=>{
      const pid = faIdOf(a), p = pid != null ? postById(pid) : null;
      if(p && p.media && !seen[p.id]){ seen[p.id] = 1; out.push(p); }
    });
    /* добор из модели (посты вне текущей вкладки/непрорисованные) */
    [...POSTS.rec, ...POSTS.sub].forEach(p=>{
      if(p && p.media && !seen[p.id]){ seen[p.id] = 1; out.push(p); }
    });
    return out;
  }catch(e){ return []; }
}

/* разметка одного слайда */
function faReelsSlideHTML(p){
  try{
    const liked = !!p.liked, saved = !!p.saved, reposted = !!p.reposted;
    const cmt = (typeof faCount === 'function') ? faCount(p) : (Array.isArray(p.comments) ? p.comments.length : 0);
    const badge = (typeof faVerified === 'function') ? faVerified(p.name) : '';
    const topic = (typeof FA_TOPICS !== 'undefined' && FA_TOPICS[p.topic]) ? FA_TOPICS[p.topic] : 'Клип';
    const dur = p.media && /:/.test(String(p.media)) ? esc(p.media) : '';
    const views = (typeof fmtN === 'function') ? fmtN(p.views || 0) : (p.views || 0);
    return (
      `<div class="far-slide" data-pid="${p.id}" style="${faReelsBg(p)}">`+
        `<div class="far-bg"></div>`+
        `<div class="far-motion"></div><div class="far-motion two"></div>`+
        `<div class="far-hero"><span class="far-topic">${I('fire')}${esc(topic)}</span>`+
          `<h2>${faReelsHeadline(p)}</h2></div>`+
        (dur ? `<div class="far-dur">${I('circle-play')}<span>${dur}</span> · <svg class="i far-eye"><use href="#i-eye"/></svg>${views}</div>` : '')+
        `<div class="far-scrim top"></div><div class="far-scrim"></div>`+
        `<button class="far-tap" type="button" aria-label="Пауза/воспроизведение" onclick="faReelsTapZone(event)">`+
          `<span class="far-playbtn">${I('play')}</span></button>`+
        /* правый рельс */
        `<div class="far-rail">`+
          `<button class="far-act far-like${liked?' on':''}" type="button" aria-label="Нравится" onclick="faReelsLike(${p.id},event)">`+
            `<span class="far-ic">${I('heart')}</span><b class="far-n-like">${p.likes||0}</b></button>`+
          `<button class="far-act far-cmt" type="button" aria-label="Комментарии" onclick="faReelsComment(${p.id})">`+
            `<span class="far-ic">${I('comment')}</span><b class="far-n-cmt">${cmt}</b></button>`+
          `<button class="far-act far-share${reposted?' on':''}" type="button" aria-label="Поделиться" onclick="faReelsShare(${p.id})">`+
            `<span class="far-ic">${I('share')}</span><b class="far-n-share">${p.reposts||0}</b></button>`+
          `<button class="far-act far-save${saved?' on':''}" type="button" aria-label="Сохранить" onclick="faReelsSave(${p.id})">`+
            `<span class="far-ic">${I('bookmark')}</span><b>Сейв</b></button>`+
          `<button class="far-act far-more" type="button" aria-label="Ещё" onclick="faReelsMore(${p.id})">`+
            `<span class="far-ic">${I('more')}</span></button>`+
        `</div>`+
        /* нижняя инфо-панель */
        `<div class="far-info">`+
          `<div class="far-author"><div class="far-ava">${esc(p.ava || (p.name ? p.name[0] : '?'))}</div>`+
            `<span class="far-name"><b>${esc(p.name || 'Автор')}</b>${badge}</span>`+
            `<span class="far-sub">${esc(p.sub || '')}</span></div>`+
          `<div class="far-caption" onclick="faReelsCap(this)">${esc(p.body || '')}<span class="far-more-t"> ещё</span></div>`+
          `<div class="far-music">${I('mic')}<span>Озвучка OKO · ${esc(p.name || 'Оригинальный звук')}</span>`+
            `<span class="far-eq"><i></i><i></i><i></i></span></div>`+
        `</div>`+
      `</div>`
    );
  }catch(e){ return ''; }
}

/* сегменты прогресса по числу клипов */
function faReelsBuildSegs(n){
  try{
    const seg = document.getElementById('farSeg'); if(!seg) return;
    let h = '';
    for(let i=0;i<n;i++) h += `<button class="far-sg" type="button" data-i="${i}" onclick="faReelsSetActive(${i})"><i class="far-fill"></i></button>`;
    seg.innerHTML = h;
  }catch(e){}
}

/* открыть плеер начиная с поста pid (или с первого клипа) */
function faReelsOpen(pid){
  try{
    const list = faReelsList();
    if(!list.length){ if(typeof toast === 'function') toast('Пока нет клипов'); return; }
    FAR.ids = list.map(p=>p.id);
    let start = FAR.ids.indexOf(+pid);
    if(start < 0) start = 0;
    const track = document.getElementById('farTrack');
    const wrap = document.getElementById('faReels');
    if(!track || !wrap) return;
    track.innerHTML = list.map(faReelsSlideHTML).join('');
    faReelsBuildSegs(list.length);
    const cnt = document.getElementById('farCount'); if(cnt) cnt.textContent = (start+1) + '/' + list.length;
    wrap.hidden = false; wrap.setAttribute('aria-hidden','false');
    wrap.classList.remove('far-paused'); FAR.paused = false;
    wrap.classList.add('far-in');
    setTimeout(()=>{ try{ wrap.classList.remove('far-in'); }catch(e){} }, 320);
    FAR.open = true;
    document.documentElement.style.overflow = 'hidden';  /* фон не скроллится под плеером */
    faReelsSetActive(start, true);
    faReelsBindKeys();
    /* интеграция с общим стеком навигации: аппаратный/TG «назад» закроет плеер */
    if(typeof nvPush === 'function') try{ nvPush('reels', faReelsClose); }catch(e){}
  }catch(e){}
}
function faReelsOpenFirst(){ try{ const l = faReelsList(); if(l.length) faReelsOpen(l[0].id); else if(typeof toast==='function') toast('Пока нет клипов'); }catch(e){} }

/* активировать слайд idx: двигаем трек (один transform), пересобираем сегменты,
   перезапускаем прогресс-анимацию, снимаем паузу */
function faReelsSetActive(idx, instant){
  try{
    const n = FAR.ids.length; if(!n) return;
    idx = Math.max(0, Math.min(n-1, idx|0));
    FAR.idx = idx;
    const track = document.getElementById('farTrack'), wrap = document.getElementById('faReels');
    if(!track || !wrap) return;
    if(instant){ track.style.transition = 'none'; }
    track.style.transform = 'translate3d(0,' + (-idx*100) + '%,0)';
    if(instant){ void track.offsetWidth; track.style.transition = ''; }
    /* активный слайд — только ему бренд-движение/эквалайзер */
    const slides = track.children;
    for(let i=0;i<slides.length;i++) slides[i].classList.toggle('is-active', i === idx);
    /* сегменты: пройденные — залиты, будущие — пустые, активный — заново анимируем */
    const segs = document.querySelectorAll('#farSeg .far-sg');
    segs.forEach((s,i)=>{
      s.classList.remove('active','done');
      if(i < idx) s.classList.add('done');
      else if(i === idx) s.classList.add('active');
      const f = s.querySelector('.far-fill');
      if(f){ f.style.animation = 'none'; void f.offsetWidth; f.style.animation = ''; }  /* рестарт заливки */
    });
    /* снять паузу на переходе */
    wrap.classList.remove('far-paused'); FAR.paused = false;
    const cnt = document.getElementById('farCount'); if(cnt) cnt.textContent = (idx+1) + '/' + n;
    faReelsSyncCounts(FAR.ids[idx]);
    if(navigator.vibrate) try{ navigator.vibrate(6); }catch(e){}
  }catch(e){}
}
function faReelsNext(){ try{ if(FAR.idx < FAR.ids.length-1) faReelsSetActive(FAR.idx+1); else if(typeof toast==='function') toast('Это последний клип'); }catch(e){} }
function faReelsPrev(){ try{ if(FAR.idx > 0) faReelsSetActive(FAR.idx-1); }catch(e){} }

/* play/pause активного клипа (пауза замораживает прогресс и бренд-движение через CSS) */
function faReelsTogglePlay(){
  try{
    const wrap = document.getElementById('faReels'); if(!wrap) return;
    FAR.paused = !FAR.paused;
    wrap.classList.toggle('far-paused', FAR.paused);
  }catch(e){}
}

/* тап по центру: одиночный — play/pause, двойной — лайк с сердцем (Instagram) */
function faReelsTapZone(ev){
  try{
    /* если это был press-and-hold (пауза-подсмотр) — не переключаем play по клику-хвосту */
    if(FAR.didHold){ FAR.didHold = false; return; }
    const now = Date.now();
    const x = ev && (ev.clientX != null ? ev.clientX : (ev.changedTouches && ev.changedTouches[0] ? ev.changedTouches[0].clientX : 0));
    const y = ev && (ev.clientY != null ? ev.clientY : (ev.changedTouches && ev.changedTouches[0] ? ev.changedTouches[0].clientY : 0));
    if(now - FAR.lastTap < 320 && Math.abs(x-FAR.lastX) < 40 && Math.abs(y-FAR.lastY) < 40){
      FAR.lastTap = 0;
      const pid = FAR.ids[FAR.idx];
      const p = postById(pid);
      if(p && !p.liked && typeof likePost === 'function'){ likePost(pid); faReelsSyncCounts(pid); }
      faReelsHeart(x, y);
      if(navigator.vibrate) try{ navigator.vibrate(12); }catch(e){}
      return;
    }
    FAR.lastTap = now; FAR.lastX = x; FAR.lastY = y;
    /* одиночный тап отрабатываем с задержкой, чтобы не гасить play при дабл-тапе */
    setTimeout(()=>{ try{ if(FAR.lastTap === now) faReelsTogglePlay(); }catch(e){} }, 300);
  }catch(e){}
}
function faReelsHeart(x, y){
  try{
    const slide = document.querySelector('#farTrack .far-slide.is-active'); if(!slide) return;
    const r = slide.getBoundingClientRect();
    const h = document.createElement('span'); h.className = 'far-heart'; h.innerHTML = I('heart');
    h.style.left = Math.round((x != null ? x : r.left + r.width/2) - r.left) + 'px';
    h.style.top  = Math.round((y != null ? y : r.top + r.height/2) - r.top) + 'px';
    slide.appendChild(h);
    setTimeout(()=>{ try{ h.remove(); }catch(e){} }, 800);
  }catch(e){}
}

/* раскрыть/свернуть подпись */
function faReelsCap(el){ try{ if(el) el.classList.toggle('far-cap-open'); }catch(e){} }

/* ---- действия рельса: переиспользуют функции и стейт ленты ---- */
function faReelsLike(pid, ev){
  try{
    if(ev){ ev.stopPropagation(); }
    const p = postById(pid); if(!p) return;
    if(typeof likePost === 'function') likePost(pid);   /* общий стейт → карточка ленты обновится сама */
    /* обновляем кнопку клипа по актуальному объекту поста */
    const slide = document.querySelector('#farTrack .far-slide[data-pid="'+pid+'"]');
    const btn = slide && slide.querySelector('.far-like');
    if(btn){
      btn.classList.toggle('on', !!p.liked);
      const n = btn.querySelector('.far-n-like'); if(n) n.textContent = p.likes || 0;
      btn.classList.remove('far-burst'); void btn.offsetWidth;
      if(p.liked) btn.classList.add('far-burst');
    }
  }catch(e){}
}
function faReelsComment(pid){
  try{ if(typeof openComments === 'function') openComments(pid); }catch(e){}  /* существующая шторка поверх плеера */
}
function faReelsShare(pid){
  try{
    const p = postById(pid); if(!p) return;
    if(typeof repost === 'function') repost(pid);
    const slide = document.querySelector('#farTrack .far-slide[data-pid="'+pid+'"]');
    const btn = slide && slide.querySelector('.far-share');
    if(btn){ btn.classList.toggle('on', !!p.reposted); const n = btn.querySelector('.far-n-share'); if(n) n.textContent = p.reposts || 0; }
  }catch(e){}
}
function faReelsSave(pid){
  try{
    const p = postById(pid); if(!p) return;
    if(typeof toggleSave === 'function') toggleSave(pid);
    const slide = document.querySelector('#farTrack .far-slide[data-pid="'+pid+'"]');
    const btn = slide && slide.querySelector('.far-save');
    if(btn) btn.classList.toggle('on', !!p.saved);
  }catch(e){}
}
function faReelsMore(pid){
  try{ if(typeof openPostMenu === 'function') openPostMenu(pid); }catch(e){}
}

/* синхронизация счётчиков клипа с общим стейтом (лайк/коммент/шер/сейв из ленты) */
function faReelsSyncCounts(pid){
  try{
    if(!FAR.open || pid == null) return;
    const p = postById(pid); if(!p) return;
    const slide = document.querySelector('#farTrack .far-slide[data-pid="'+pid+'"]'); if(!slide) return;
    const like = slide.querySelector('.far-like');
    if(like){ like.classList.toggle('on', !!p.liked); const n = like.querySelector('.far-n-like'); if(n) n.textContent = p.likes || 0; }
    const cmtN = slide.querySelector('.far-n-cmt');
    if(cmtN) cmtN.textContent = (typeof faCount === 'function') ? faCount(p) : (Array.isArray(p.comments) ? p.comments.length : 0);
    const share = slide.querySelector('.far-share');
    if(share){ share.classList.toggle('on', !!p.reposted); const n = share.querySelector('.far-n-share'); if(n) n.textContent = p.reposts || 0; }
    const save = slide.querySelector('.far-save');
    if(save) save.classList.toggle('on', !!p.saved);
  }catch(e){}
}

/* закрытие плеера: чистим слайды (гасит все CSS-анимации), снимаем слушатели/стек */
function faReelsClose(){
  try{
    const wrap = document.getElementById('faReels'); if(!wrap) return;
    wrap.hidden = true; wrap.setAttribute('aria-hidden','true');
    wrap.classList.remove('far-paused','far-in');
    const track = document.getElementById('farTrack'); if(track){ track.innerHTML = ''; track.style.transform = 'translate3d(0,0,0)'; }
    const seg = document.getElementById('farSeg'); if(seg) seg.innerHTML = '';
    FAR.open = false; FAR.paused = false; FAR.drag = null;
    document.documentElement.style.overflow = '';
    faReelsUnbindKeys();
    if(typeof nvPop === 'function') try{ nvPop('reels'); }catch(e){}
  }catch(e){}
}

/* клавиатура: стрелки/space — навигация и пауза; Esc закрывает (если нет navstack) */
function faReelsBindKeys(){
  try{
    if(FAR.keyHandler) return;
    FAR.keyHandler = function(e){
      try{
        if(!FAR.open) return;
        /* не мешаем печатать в шторке комментариев поверх плеера */
        const t = e.target;
        if(t && (t.tagName === 'INPUT' || t.tagName === 'TEXTAREA' || t.isContentEditable)) return;
        const sheet = document.getElementById('sheet-comments');
        if(sheet && sheet.classList.contains('open')) return;
        if(e.key === 'ArrowDown' || e.key === 'j'){ e.preventDefault(); faReelsNext(); }
        else if(e.key === 'ArrowUp' || e.key === 'k'){ e.preventDefault(); faReelsPrev(); }
        else if(e.key === ' '){ e.preventDefault(); faReelsTogglePlay(); }
        else if(e.key === 'Escape' && typeof nvPush !== 'function'){ e.preventDefault(); faReelsClose(); }
      }catch(err){}
    };
    document.addEventListener('keydown', FAR.keyHandler);
  }catch(e){}
}
function faReelsUnbindKeys(){
  try{ if(FAR.keyHandler){ document.removeEventListener('keydown', FAR.keyHandler); FAR.keyHandler = null; } }catch(e){}
}

/* автолистание по завершении прогресс-анимации активного сегмента (без JS-таймеров) */
(function faReelsWireProgress(){
  try{
    const seg = document.getElementById('farSeg'); if(!seg) return;
    seg.addEventListener('animationend', function(e){
      try{
        if(!FAR.open) return;
        if(e.animationName !== 'far-fill') return;
        const li = e.target.closest && e.target.closest('.far-sg');
        if(li && +li.getAttribute('data-i') === FAR.idx){
          if(FAR.idx < FAR.ids.length-1) faReelsNext(); else faReelsClose();  /* конец подборки — выходим */
        }
      }catch(err){}
    });
  }catch(e){}
})();

/* свайпы (touch) + колесо мыши по поверхности плеера */
(function faReelsWireGestures(){
  try{
    const wrap = document.getElementById('faReels'); if(!wrap) return;
    const TH = 56;  /* порог срабатывания, px */

    wrap.addEventListener('touchstart', function(e){
      try{
        if(!FAR.open || !e.touches || !e.touches.length) return;
        /* не перехватываем жесты рельса/инфо/хрома — только «пустое» видео-поле */
        if(e.target.closest('.far-rail,.far-info,.far-top,.far-back')) { FAR.drag = null; return; }
        FAR.drag = {y0:e.touches[0].clientY, t0:Date.now(), moved:0};
        wrap.classList.add('far-drag');
      }catch(err){}
    }, {passive:true});

    wrap.addEventListener('touchmove', function(e){
      try{
        if(!FAR.drag || !e.touches || !e.touches.length) return;
        const dy = e.touches[0].clientY - FAR.drag.y0;
        FAR.drag.moved = dy;
        /* лёгкое сопротивление на краях */
        let d = dy;
        if((FAR.idx === 0 && dy > 0) || (FAR.idx === FAR.ids.length-1 && dy < 0)) d = dy * 0.35;
        const track = document.getElementById('farTrack');
        if(track) track.style.transform = 'translate3d(0,calc(' + (-FAR.idx*100) + '% + ' + Math.round(d) + 'px),0)';
      }catch(err){}
    }, {passive:true});

    wrap.addEventListener('touchend', function(e){
      try{
        if(!FAR.drag) return;
        wrap.classList.remove('far-drag');
        const dy = FAR.drag.moved, dt = Date.now() - FAR.drag.t0;
        const fast = Math.abs(dy) > 24 && dt < 220;
        FAR.drag = null;
        if(dy < -TH || (fast && dy < 0)) faReelsSetActive(FAR.idx+1);
        else if((dy > TH || (fast && dy > 0))){
          if(FAR.idx === 0) faReelsClose();          /* свайп вниз на первом клипе — закрыть */
          else faReelsSetActive(FAR.idx-1);
        }
        else faReelsSetActive(FAR.idx);              /* недотянул — вернуть на место */
      }catch(err){}
    }, {passive:true});

    wrap.addEventListener('wheel', function(e){
      try{
        if(!FAR.open) return;
        if(e.target.closest('.far-info,.far-rail')) return;
        e.preventDefault();
        const now = Date.now();
        if(now - FAR.wheelLock < 500) return;
        if(Math.abs(e.deltaY) < 12) return;
        FAR.wheelLock = now;
        if(e.deltaY > 0) faReelsNext(); else faReelsPrev();
      }catch(err){}
    }, {passive:false});
  }catch(e){}
})();

/* вход в плеер: тап по видео-области карточки ленты (делегат, additive) + кнопка «Клипы» */
(function faReelsWireEntry(){
  try{
    const list = document.getElementById('feedList');
    if(list && !list._faReels){
      list._faReels = 1;
      list.addEventListener('click', function(e){
        try{
          const media = e.target.closest('.media');
          if(!media) return;
          const art = media.closest('article.post'); if(!art) return;
          const pid = faIdOf(art); if(pid == null) return;
          const p = postById(pid);
          if(p && p.media){ e.preventDefault(); e.stopPropagation(); faReelsOpen(pid); }
        }catch(err){}
      });
    }
    /* i18n для EN-режима */
    if(typeof ST_DICT !== 'undefined'){
      const add = {'Клипы':'Reels','Пока нет клипов':'No reels yet','Это последний клип':'That is the last reel','Сейв':'Save','ещё':'more'};
      for(const k in add) if(!(k in ST_DICT)) ST_DICT[k] = add[k];
    }
  }catch(e){}
})();

/* кнопка «Клипы» в шапке умной ленты — вставляется в fa-bar после его отрисовки.
   Оборачиваем faDecorate, чтобы не менять его тело (additive). */
try{
  if(typeof faDecorate === 'function'){
    const _faPrevDecorate = faDecorate;
    faDecorate = function(){
      _faPrevDecorate.apply(this, arguments);
      try{
        const bar = document.querySelector('#feedList .fa-bar');
        if(bar && !bar.querySelector('.fa-clips') && faReelsList().length){
          const b = document.createElement('button');
          b.type = 'button'; b.className = 'fa-clips';
          b.setAttribute('aria-label', 'Открыть клипы');
          b.innerHTML = I('circle-play') + '<span>Клипы</span>';
          b.onclick = faReelsOpenFirst;
          const refresh = bar.querySelector('.fa-refresh');
          if(refresh) bar.insertBefore(b, refresh); else bar.appendChild(b);
        }
      }catch(e){}
    };
  }
}catch(e){}

/* синхронизировать рельс клипа при изменении числа комментариев из шторки
   (faUpdateCardCount вызывается ядром комментариев — дополняем его, не переписывая) */
try{
  if(typeof faUpdateCardCount === 'function'){
    const _faPrevUpdCnt = faUpdateCardCount;
    faUpdateCardCount = function(p){
      _faPrevUpdCnt.apply(this, arguments);
      try{ if(FAR.open && p) faReelsSyncCounts(p.id); }catch(e){}
    };
  }
}catch(e){}

/* ============================================================================
   9. «НОВЫЕ ПОСТЫ» — плавающая пилюля обновления ленты (Instagram/Twitter)
   Пока пользователь листает рекомендации вниз, сверху всплывает лаймовая пилюля
   «N новых постов ↑». Тап — пересобрать подборку (faRefresh уже уводит наверх).
   Живая, «дышащая» лента: сигнал, что контент обновляется в реальном времени.
   ПЕРФ: один лёгкий интервал + rAF-throttle на скролл; работает ТОЛЬКО на вкладке
   рекомендаций активного экрана ленты. i18n — через ST_DICT (число и слово — разными
   текст-узлами, чтобы авто-переводчик (MutationObserver) перевёл слово в EN-режиме). */
(function faNewPostsPill(){
  try{
    /* стрелка вверх для пилюли — в общие defs */
    const defs = document.querySelector('svg defs');
    if(defs && !document.getElementById('i-fa-up')){
      const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
      s.setAttribute('id','i-fa-up'); s.setAttribute('viewBox','0 0 100 100');
      s.innerHTML = '<polyline points="26 48 50 24 74 48" fill="none" stroke="currentColor" stroke-width="8" stroke-linecap="round" stroke-linejoin="round"/><line x1="50" y1="27" x2="50" y2="78" stroke="currentColor" stroke-width="8" stroke-linecap="round"/>';
      defs.appendChild(s);
    }
    if(typeof ST_DICT !== 'undefined'){
      const add = {'новый пост':'new post','новых поста':'new posts','новых постов':'new posts'};
      for(const k in add) if(!(k in ST_DICT)) ST_DICT[k] = add[k];
    }

    FA.newPosts = 0;
    let pill = null;

    function ensure(){
      if(pill) return pill;
      pill = document.createElement('button');
      pill.type = 'button'; pill.className = 'fa-newpill';
      pill.setAttribute('aria-live','polite');
      pill.setAttribute('aria-label','Показать новые посты');
      pill.addEventListener('click', onTap);
      document.body.appendChild(pill);
      return pill;
    }
    function word(n){
      return (typeof faPlural === 'function')
        ? faPlural(n, 'новый пост', 'новых поста', 'новых постов')
        : 'новых постов';
    }
    function show(){
      const p = ensure();
      /* число и слово — отдельными узлами: число не трогаем, слово переведётся авто-переводчиком */
      p.innerHTML = `<svg class="i fa-np-ic"><use href="#i-fa-up"/></svg><span class="fa-np-n">${FA.newPosts}</span> <span>${word(FA.newPosts)}</span>`;
      requestAnimationFrame(()=>{ try{ p.classList.add('show'); }catch(e){} });
    }
    function hide(){ if(pill) pill.classList.remove('show'); }
    function onTap(){
      hide(); FA.newPosts = 0;
      if(typeof faRefresh === 'function') faRefresh();  /* пересобирает подборку + уводит наверх */
    }
    function feedActive(){
      const f = document.getElementById('screen-feed');
      const kindOk = (typeof curFeedKind === 'undefined') || curFeedKind === 'rec';
      return !!(f && f.classList.contains('active') && kindOk);
    }

    /* один негромкий интервал: копит «новые» посты, пока пользователь ушёл вниз по ленте */
    setInterval(function(){
      try{
        const m = document.querySelector('main');
        if(!feedActive() || FA.loading){ hide(); return; }
        if(!m || m.scrollTop < 520){ if(FA.newPosts === 0) hide(); return; }
        FA.newPosts = Math.min(FA.newPosts + 1 + Math.floor(Math.random()*2), 24);
        show();
      }catch(e){}
    }, 13000);

    /* вернулся к самому верху — «новых» больше нет, прячем */
    const m = document.querySelector('main');
    if(m){
      let raf = 0;
      m.addEventListener('scroll', function(){
        if(raf) return;
        raf = requestAnimationFrame(function(){
          raf = 0;
          try{ if(m.scrollTop < 120){ FA.newPosts = 0; hide(); } }catch(e){}
        });
      }, {passive:true});
    }

    /* явное «Обновить подборку» тоже гасит счётчик и пилюлю */
    if(typeof faRefresh === 'function'){
      const _faPrevRefresh = faRefresh;
      faRefresh = function(){ FA.newPosts = 0; hide(); return _faPrevRefresh.apply(this, arguments); };
    }
  }catch(e){}
})();

/* ============================================================================
   10. REELS: ЗАЖАТЬ И ДЕРЖАТЬ = ПАУЗА-ПОДСМОТР (TikTok/Instagram press-and-hold)
   Зажал палец/курсор на видео-поле → прогресс и бренд-движение замирают, хром плавно
   уходит (чистый кадр). Отпустил → всё возвращается. Быстрый тап (< порога) НЕ триггерит
   удержание — обычные одиночный/двойной тап работают как прежде. Движение отменяет
   удержание, чтобы не мешать свайпу листания. Всё в try/catch, additive, без сети.
   ============================================================================ */
(function faReelsHold(){
  try{
    const wrap = document.getElementById('faReels'); if(!wrap) return;
    let timer = 0, sx = 0, sy = 0, armed = false;
    const HOLD = 250, MOVE = 12;

    function commentsOpen(){
      const s = document.getElementById('sheet-comments');
      return !!(s && s.classList.contains('open'));
    }
    function start(x, y, target){
      if(!FAR.open || commentsOpen()) return;
      if(!target || !target.closest || !target.closest('.far-tap')) return;  /* только центральное поле */
      sx = x; sy = y; armed = true;
      if(timer) clearTimeout(timer);
      timer = setTimeout(function(){
        try{
          if(!armed || !FAR.open) return;
          FAR.didHold = true;                 /* хвостовой click от faReelsTapZone гасится */
          wrap.classList.add('far-hold');
          if(navigator.vibrate) try{ navigator.vibrate(8); }catch(e){}
        }catch(e){}
      }, HOLD);
    }
    function move(x, y){
      if(!armed) return;
      if(Math.abs(x - sx) > MOVE || Math.abs(y - sy) > MOVE) cancel();
    }
    function cancel(){
      armed = false;
      if(timer){ clearTimeout(timer); timer = 0; }
      try{ if(wrap.classList.contains('far-hold')) wrap.classList.remove('far-hold'); }catch(e){}
    }

    if(window.PointerEvent){
      wrap.addEventListener('pointerdown', e=>{ try{ if(e.pointerType === 'mouse' && e.button !== 0) return; start(e.clientX, e.clientY, e.target); }catch(err){} }, {passive:true});
      wrap.addEventListener('pointermove', e=>{ try{ move(e.clientX, e.clientY); }catch(err){} }, {passive:true});
      ['pointerup','pointercancel','pointerleave'].forEach(t=> wrap.addEventListener(t, cancel, {passive:true}));
    }else{
      wrap.addEventListener('touchstart', e=>{ try{ const t = e.touches && e.touches[0]; if(t) start(t.clientX, t.clientY, e.target); }catch(err){} }, {passive:true});
      wrap.addEventListener('touchmove', e=>{ try{ const t = e.touches && e.touches[0]; if(t) move(t.clientX, t.clientY); }catch(err){} }, {passive:true});
      ['touchend','touchcancel'].forEach(t=> wrap.addEventListener(t, cancel, {passive:true}));
    }
  }catch(e){}
})();

/* ============================================================================
   11. ДВОЙНОЙ ХУК — «Показать полностью» (Instagram/Twitter expandable body)
   Длинные тела постов схлопнуты до 3 строк с fade-out градиентом. Тап на кнопку
   «Показать полностью» плавно раскрывает содержимое (grid-rows animation, без
   JS-высот). Раскрытое состояние помнится в объекте поста (session-only) →
   пересборка ленты не «схлопывает» уже прочитанное.
   Проверка ПО РЕАЛЬНОЙ ВЫСОТЕ: кнопка появляется, только если DOM реально обрезал
   текст (scrollHeight > clientHeight). Никаких кнопок на коротких 2-строчных постах.
   ============================================================================ */
const FA_CLIP_MIN = 90;   /* нижний порог по символам — минимум чтобы вообще рассматривать пост как «длинный» */

function faExpandDecorate(){
  try{
    const list = document.getElementById('feedList'); if(!list) return;
    const pending = [];
    list.querySelectorAll('article.post .body').forEach(function(body){
      try{
        if(body._faClipped) return;
        const art = body.closest('article.post'); if(!art) return;
        const pid = faIdOf(art); if(pid == null) return;
        const p = postById(pid); if(!p || !p.body) return;
        /* дешёвый short-circuit: если тело короткое — точно ничего не подрежется */
        if(String(p.body).length < FA_CLIP_MIN) return;
        body._faClipped = 1;
        const openCls = p._faOpen ? ' fa-open' : '';
        /* оборачиваем тело в контейнер-клип; текст оставляем как есть,
           чтобы esc-логика ядра не переисполнялась */
        const html = body.innerHTML;
        body.classList.add('fa-body-wrap');
        body.innerHTML =
          '<div class="fa-body-clip' + openCls + '">' +
            '<div class="fa-body-inner">' + html + '</div>' +
            '<span class="fa-body-fade" aria-hidden="true"></span>' +
          '</div>' +
          '<button class="fa-more-btn" type="button" onclick="faToggleBody(this,' + pid + ')">' +
            '<span class="fa-more-t">' + (p._faOpen ? 'Свернуть' : 'Показать полностью') + '</span>' +
            I('chev') +
          '</button>';
        pending.push({body:body, pid:pid});
      }catch(e){}
    });
    /* после вставки — проверяем РЕАЛЬНУЮ высоту .fa-body-inner: если контент
       вообще не переполнил box (короткий пост в 2 строки), разворачиваем клип
       обратно и убираем кнопку. Так «Показать полностью» больше не мозолит глаза
       на коротких карточках. */
    if(pending.length){
      const check = function(){
        pending.forEach(function(x){
          try{
            const body = x.body; if(!body) return;
            const clip  = body.querySelector('.fa-body-clip'); if(!clip) return;
            const inner = clip.querySelector('.fa-body-inner'); if(!inner) return;
            /* если раскрыто пользователем — тестировать нечего */
            if(clip.classList.contains('fa-open')) return;
            const overflow = inner.scrollHeight - inner.clientHeight;
            if(overflow > 2) return;   /* реально обрезано → кнопка нужна */
            /* контент влез — разворачиваем DOM обратно, чтобы не было кнопки */
            const innerHTML = inner.innerHTML;
            const btn = body.querySelector(':scope > .fa-more-btn');
            if(btn) btn.remove();
            body.classList.remove('fa-body-wrap');
            body.innerHTML = innerHTML;
          }catch(e){}
        });
      };
      /* два прохода: сразу и в rAF — верстка может подгрузить шрифт и высота изменится */
      check();
      if(typeof requestAnimationFrame === 'function') requestAnimationFrame(check);
    }
  }catch(e){}
}
function faToggleBody(btn, pid){
  try{
    if(!btn) return;
    const wrap = btn.previousElementSibling;
    const p = postById(pid);
    const open = wrap && wrap.classList.toggle('fa-open');
    btn.classList.toggle('fa-open', !!open);
    const t = btn.querySelector('.fa-more-t');
    if(t) t.textContent = open ? 'Свернуть' : 'Показать полностью';
    if(p) p._faOpen = !!open;
  }catch(e){}
}

/* доцеп к faDecorate: клип срабатывает на обеих вкладках, поэтому запускаем
   после _любого_ renderFeed, а не только для 'rec' */
try{
  if(typeof renderFeed === 'function'){
    const _prevRFexpand = renderFeed;
    renderFeed = function(kind){
      const r = _prevRFexpand.apply(this, arguments);
      try{ faExpandDecorate(); }catch(e){}
      return r;
    };
  }
}catch(e){}

/* ============================================================================
   12. СМЕШАННАЯ ЛЕНТА — сторис-баблы + рекомендованные каналы каждые 5 постов
   После каждого блока из 5 органических карточек вставляем «врезку»:
   1) полоска непросмотренных сторис (мини-версия storiesRow), либо
   2) карусель рекомендованных каналов (из CH.disc, если модуль есть).
   Врезки чередуются, чтобы лента «дышала». Всё делегируется по клику на
   существующие обработчики (openStory / chOpen).
   ============================================================================ */
function faStoryChipsHTML(){
  try{
    if(typeof STORIES === 'undefined' || !Array.isArray(STORIES)) return '';
    /* берём непросмотренные (без «add»-плитки Твоей истории), максимум 8 */
    const items = STORIES
      .map(function(st, i){ return {st:st, i:i}; })
      .filter(function(x){ return x.st && !x.st.add && !x.st.seen; })
      .slice(0, 8);
    if(!items.length) return '';
    const chips = items.map(function(x){
      const st = x.st;
      const face = st.avaIcon
        ? '<span class="fai-face lime">' + I(st.avaIcon) + '</span>'
        : st.src
          ? '<span class="fai-face" style="background-image:url(' + esc(st.src) + ');background-size:cover"></span>'
          : '<span class="fai-face">' + esc(String(st.ava || (st.name ? st.name[0] : '?'))) + '</span>';
      return '<button class="fai-story" type="button" onclick="openStory(' + x.i + ')" aria-label="История ' + esc(st.name || '') + '">' +
        '<span class="fai-ring">' + face + '</span>' +
        '<small>' + esc((st.name || '').slice(0, 10)) + '</small>' +
      '</button>';
    }).join('');
    return '<div class="fa-inter fa-inter-stories">' +
      '<div class="fai-head">' + I('bolt') + '<b>Свежие истории</b><span class="fai-cnt">' + items.length + '</span></div>' +
      '<div class="fai-row">' + chips + '</div>' +
    '</div>';
  }catch(e){ return ''; }
}
function faChannelChipsHTML(){
  try{
    if(typeof CH === 'undefined' || !CH || !Array.isArray(CH.disc)) return '';
    /* каналы, на которые ты ещё не подписан, отсортированы по числу подписчиков */
    const items = CH.disc
      .filter(function(c){ return c && !(CH.sub && CH.sub[c.id]); })
      .sort(function(a, b){ return (b.subs||0) - (a.subs||0); })
      .slice(0, 6);
    if(!items.length) return '';
    const chips = items.map(function(c){
      const price = c.type === 'paid'
        ? '<b class="fai-price">' + Math.round(c.price||0) + ' ₽/мес</b>'
        : c.type === 'course'
          ? '<b class="fai-price">Курс · ' + Math.round(c.price||0) + ' ₽</b>'
          : '<b class="fai-price fai-free">Бесплатно</b>';
      const subs = (typeof fmtN === 'function') ? fmtN(c.subs||0) : String(c.subs||0);
      const kindLabel = c.type === 'course' ? 'Курс' : (c.kind === 'club' ? 'Клуб' : 'Канал');
      return '<button class="fai-ch" type="button" onclick="chOpen(\'channel\',\'' + c.id + '\')" aria-label="Открыть канал ' + esc(c.name) + '">' +
        '<span class="fai-ch-ic">' + I(c.icon || 'megaphone') + '</span>' +
        '<b class="fai-ch-name">' + esc(c.name) + '</b>' +
        '<small class="fai-ch-desc">' + esc((c.desc || '').slice(0, 62)) + '…</small>' +
        '<div class="fai-ch-meta"><span class="fai-ch-kind">' + kindLabel + '</span>' +
          '<span class="fai-ch-subs">' + I('users') + subs + '</span>' + price + '</div>' +
      '</button>';
    }).join('');
    return '<div class="fa-inter fa-inter-channels">' +
      '<div class="fai-head">' + I('compass') + '<b>Каналы, которые тебе понравятся</b></div>' +
      '<div class="fai-scroll">' + chips + '</div>' +
    '</div>';
  }catch(e){ return ''; }
}
function faInterstitials(){
  try{
    const list = document.getElementById('feedList'); if(!list) return;
    if(curFeedKind !== 'rec') return;
    /* сначала снимаем предыдущие врезки (при переранжировании порядок мог сдвинуться) */
    list.querySelectorAll('.fa-inter').forEach(function(el){ el.remove(); });
    const posts = [...list.querySelectorAll('article.post')].filter(function(a){
      const pid = faIdOf(a); const p = pid != null ? postById(pid) : null;
      return p && !p.promoted;   /* реклама в счётчик не идёт (это уже прерывание) */
    });
    if(posts.length < 8) return;
    /* Сторис-баблы убраны 29.07 (сторис теперь в ленте как обычные посты).
       Оставлена лишь одна врезка «Каналы, которые тебе понравятся» — через
       каждые 12 органических постов. */
    let injected = 0;
    const maxInject = 2;
    for(let i = 11; i < posts.length && injected < maxInject; i += 12){
      const anchor = posts[i]; if(!anchor || !anchor.parentNode) continue;
      const html = faChannelChipsHTML();
      if(!html) continue;
      anchor.insertAdjacentHTML('afterend', html);
      injected++;
    }
  }catch(e){}
}
try{
  if(typeof faDecorate === 'function'){
    const _prevDecorateInter = faDecorate;
    faDecorate = function(){
      _prevDecorateInter.apply(this, arguments);
      try{ faInterstitials(); }catch(e){}
    };
  }
}catch(e){}

/* ============================================================================
   13. CATCH-UP CHIP — УБРАНА по правке Даниэля 29.07 (излишний UX).
   Функции оставлены как no-op, чтобы старые вызовы не падали.
   ============================================================================ */
FA.away = { at:0, count:0 };
function faCatchUpShow(){ /* removed 29.07 */ }
function faCatchUpCheck(){ /* removed 29.07 */ }
function faCatchUpMark(){ /* removed 29.07 */ }

/* ============================================================================
   14. БЫСТРЫЕ РЕАКЦИИ ЛАЙКА — вылетающие бренд-стикеры (TG/Instagram)
   При клике «нравится» из кнопки вылетает 4-5 маленьких SVG-иконок бренда
   (сердце, огонь, звезда, молния, лаймовая точка) — разлетаются вверх с лёгким
   поворотом и исчезают. Только transform/opacity, без reflow. Не срабатывает
   при снятии лайка (только при постановке). Работает и в ленте, и в клипах.
   ============================================================================ */
/* Иконки для «толпы» реакций в reels (использует раздел 15) — сам «фейерверк» на
   лайке в ленте убран по правке Даниэля 29.07: остаётся только tap-heart. */
const FA_QR_ICONS = ['heart','fire','star','bolt','fa-dot'];
/* faQuickReact — no-op (веерные квик-реакции убраны). Оставлено как заглушка,
   чтобы старые вызовы не падали. */
function faQuickReact(){ /* removed by 29.07: only tap-heart остаётся */ }

/* ============================================================================
   15. REELS — «ТОЛПА» ПЛАВАЮЩИХ РЕАКЦИЙ (Instagram Live style)
   Пока клип открыт и не на паузе — вдоль правого края слайда снизу вверх плывут
   маленькие SVG-иконки (сердце/огонь/звезда/молния/точка) со случайными задержками
   и амплитудами. Останавливаются на hold-паузе и на far-paused. Только transform.
   ============================================================================ */
FA.crowd = { timer: 0 };
function faCrowdEmit(){
  try{
    if(!FAR.open || FAR.paused) return;
    const wrap = document.getElementById('faReels');
    if(!wrap || wrap.hidden) return;
    if(wrap.classList.contains('far-hold')) return;
    const slide = wrap.querySelector('.far-slide.is-active'); if(!slide) return;
    /* базовый слой — держим один на слайд */
    let layer = slide.querySelector('.far-crowd');
    if(!layer){
      layer = document.createElement('div');
      layer.className = 'far-crowd';
      layer.setAttribute('aria-hidden','true');
      slide.appendChild(layer);
    }
    const ic = FA_QR_ICONS[Math.floor(Math.random() * FA_QR_ICONS.length)];
    const s = document.createElement('span');
    s.className = 'far-cr far-cr-' + ic;
    /* амплитуда/поворот/длительность — уникальны для каждой реакции */
    const amp = 18 + Math.round(Math.random() * 44);   /* горизонтальный «свинг», px */
    const drift = Math.round((Math.random() - 0.5) * 40);
    const rot = Math.round(Math.random() * 30 - 15);
    const dur = 3200 + Math.round(Math.random() * 1600);
    const startBottom = 8 + Math.round(Math.random() * 26);
    s.style.setProperty('--cra', amp + 'px');
    s.style.setProperty('--crd', drift + 'px');
    s.style.setProperty('--crr', rot + 'deg');
    s.style.animationDuration = dur + 'ms';
    s.style.bottom = startBottom + 'px';
    s.innerHTML = ic === 'fa-dot' ? '<span class="fa-qr-dot"></span>' : I(ic);
    layer.appendChild(s);
    setTimeout(function(){ try{ s.remove(); }catch(e){} }, dur + 100);
  }catch(e){}
}
function faCrowdStart(){
  try{
    if(FA.crowd.timer) return;
    /* уважение к prefers-reduced-motion — толпу не запускаем */
    if(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    FA.crowd.timer = setInterval(faCrowdEmit, 620);
  }catch(e){}
}
function faCrowdStop(){
  try{
    if(FA.crowd.timer){ clearInterval(FA.crowd.timer); FA.crowd.timer = 0; }
    document.querySelectorAll('#faReels .far-crowd').forEach(function(el){ el.innerHTML = ''; });
  }catch(e){}
}
/* цепляем к открытию/закрытию плеера, не переписывая тела функций */
try{
  if(typeof faReelsOpen === 'function'){
    const _prevROpen = faReelsOpen;
    faReelsOpen = function(){
      const r = _prevROpen.apply(this, arguments);
      try{ faCrowdStart(); }catch(e){}
      return r;
    };
  }
  if(typeof faReelsClose === 'function'){
    const _prevRClose = faReelsClose;
    faReelsClose = function(){
      try{ faCrowdStop(); }catch(e){}
      return _prevRClose.apply(this, arguments);
    };
  }
  if(typeof faReelsSetActive === 'function'){
    const _prevRSA = faReelsSetActive;
    faReelsSetActive = function(){
      const r = _prevRSA.apply(this, arguments);
      /* при переходе на новый слайд — чистим слой старого, стартер эмитит на новый */
      try{ document.querySelectorAll('#faReels .far-slide:not(.is-active) .far-crowd').forEach(function(el){ el.innerHTML = ''; }); }catch(e){}
      return r;
    };
  }
}catch(e){}

/* ============================================================================
   16. МЕЛКИЕ ДОПОЛНЕНИЯ ИНИЦИАЛИЗАЦИИ (иконки/i18n для новых секций)
   ============================================================================ */
(function faLateInit(){
  try{
    const defs = document.querySelector('svg defs');
    if(defs && !document.getElementById('i-fa-dot')){
      const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
      s.setAttribute('id','i-fa-dot'); s.setAttribute('viewBox','0 0 100 100');
      /* лаймовая «искра OKO» — маленький ромб-точка, читается как «реакция OKO» */
      s.innerHTML = '<circle cx="50" cy="50" r="30" fill="currentColor"/>';
      defs.appendChild(s);
    }
    if(typeof ST_DICT !== 'undefined'){
      const add = {
        'Показать полностью':'Show more','Свернуть':'Show less',
        'Свежие истории':'Fresh stories','Каналы, которые тебе понравятся':'Channels you may like',
        'Канал':'Channel','Курс':'Course','Клуб':'Club','Бесплатно':'Free',
        'Ты пропустил':'You missed','пост':'post','поста':'posts','постов':'posts',
        'Для тебя':'For you','Подобрано алгоритмом OKO':'Curated by the OKO algorithm',
      };
      for(const k in add) if(!(k in ST_DICT)) ST_DICT[k] = add[k];
    }
    try{ faInjectFYouTab(); }catch(e){}
    /* если лента уже видна — обогатим её сразу (первый рендер выполнился до нас) */
    const feed = document.getElementById('screen-feed');
    if(feed && feed.classList.contains('active')){
      try{ faExpandDecorate(); }catch(e){}
      try{ if(curFeedKind === 'rec') faInterstitials(); }catch(e){}
      try{ if(curFeedKind === 'fyou') faDecorateFYou(); }catch(e){}
    }
  }catch(e){}
})();

/* ============================================================================
   17. ЧИСТЫЙ ВИДЕО-ПОСТЕР (fix v1.94)
   Ядро рисует над кадром большую букву-аватар автора + первые 46 символов тела.
   На посте «Академия OKO открыта» это давало «OKO» в надписи и «O» в кружке-аватара
   одновременно с текстом описания под медиа → визуальный дубль. Решение:
   переопределяем renderPostMedia в пользу чистого постера в стиле Instagram/TikTok
   (градиент + большая play-кнопка + бейдж длительности), без текстовых оверлеев,
   которые дублируют тело поста. Для видео с известной темой — маленький бренд-чип
   в углу вместо буквы (SVG-иконка темы). Карусель (N/M) — тоже без буквы/текста.
   ============================================================================ */
const FA_TOPIC_ICON = {ai:'bolt', content:'camera', business:'briefcase',
  marketing:'compass', games:'star', crypto:'fire'};
try{
  if(typeof renderPostMedia === 'function' && typeof POST_MEDIA_PALETTES !== 'undefined'){
    const _faPrevRenderPostMedia = renderPostMedia;
    renderPostMedia = function(p){
      try{
        if(!p || !p.media) return '';
        const isCarousel = /^\d+\/\d+$/.test(p.media);
        const pals = POST_MEDIA_PALETTES;
        const idx = ((p.id||0) % pals.length + pals.length) % pals.length;
        const pal = pals[idx];
        const c1 = pal[0], c2 = pal[1], ac = pal[2];
        const topicIco = (p.topic && FA_TOPIC_ICON[p.topic]) ? FA_TOPIC_ICON[p.topic] : null;
        const chip = topicIco ? '<span class="fa-media-topic" style="color:'+ac+'">'+I(topicIco)+'</span>' : '';
        if(isCarousel){
          return '<div class="media media-car fa-media-clean" style="background:linear-gradient(140deg,'+c1+','+c2+' 65%);color:'+ac+'">'+
            chip+
            '<span class="media-tag">'+I('photo')+'<span>'+p.media+'</span></span>'+
          '</div>';
        }
        return '<div class="media media-vid fa-media-clean" style="background:linear-gradient(140deg,'+c1+','+c2+' 65%);color:'+ac+'">'+
          chip+
          '<span class="media-play"><svg class="i"><use href="#i-circle-play"/></svg></span>'+
          '<span class="media-time">'+esc(p.media)+'</span>'+
        '</div>';
      }catch(e){
        try{ return _faPrevRenderPostMedia(p); }catch(_){ return ''; }
      }
    };
  }
}catch(e){}

/* ============================================================================
   18. ВКЛАДКА «ДЛЯ ТЕБЯ» (For You) — алгоритмическая лента в стиле TikTok
   Простая формула: engagement = likes*2 + comments*3 + watch-time (сек длительности
   видео как прокси удержания) + reposts*2, помноженное на буст свежести (экспо-декей
   с полураспадом ~1 сутки). Малые бонусы: тема из твоих сигналов + аффинность автора.
   Небольшой детерминированный шум по seed сессии — лента «дышит», но стабильна между
   ре-рендерами. Пул — объединение POSTS.rec и POSTS.sub (дедуп), скрытые исключаются.
   Реклама в For You ниже — как в TikTok, где нативные видео важнее промо.
   Табу «Подписки/Рекомендации» из base.html не трогаем: инжектим третью кнопку
   «Для тебя» в .feed-tabs между ними (первая кнопка остаётся «Подписки» — чтобы
   не сломать document.querySelector('.feed-tabs button').click() в createPost).
   ============================================================================ */
function faFYouScore(p){
  const cmts = Array.isArray(p.comments) ? p.comments.length : 0;
  const eng  = (p.likes||0)*2 + cmts*3 + (p.reposts||0)*2 + faMediaSec(p.media||'');
  const fresh = Math.exp(-faAgeH(p) / 24);       /* полураспад ~1 сутки */
  let s = eng * (0.55 + 1.35 * fresh);
  /* лёгкая персонализация: сигналы по теме и аффинность автора */
  if(p.topic) s += Math.min(FA.signals[p.topic]||0, 12) * 22;
  s += Math.min(FA.authors[p.name]||0, 18) * 14;
  /* детерминированный шум для разнообразия — стабилен между ре-рендерами */
  s += (faRand((p.id||0) + 7777) - 0.5) * 55;
  if(p.promoted) s -= 400;                       /* реклама в For You тише */
  return s;
}
function faFYouPool(){
  const hidden = faLoadHidden();
  const seen = {}, out = [];
  const push = function(arr){
    if(!Array.isArray(arr)) return;
    arr.forEach(function(p){
      if(!p || seen[p.id]) return;
      if(hidden && hidden.has(+p.id)) return;
      seen[p.id] = 1; out.push(p);
    });
  };
  if(typeof POSTS !== 'undefined'){ push(POSTS.rec); push(POSTS.sub); }
  out.sort(function(a,b){ return faFYouScore(b) - faFYouScore(a); });
  return out;
}
/* Третья вкладка «Для тебя» убрана 29.07 по правке Даниэля:
   осталось ровно два таба — «Рекомендации» и «Подписки». «Рекомендации» и есть
   алгоритмическая лента как в TikTok, отдельного For You больше нет.
   Если старая вкладка уже была впрыснута — удаляем. */
function faInjectFYouTab(){
  try{
    const tabs = document.querySelector('#screen-feed .feed-tabs');
    if(!tabs) return;
    const old = tabs.querySelector('.fa-tab-fyou');
    if(old) old.remove();
  }catch(e){}
}
/* лёгкая шапка для «Для тебя» — без длинного пояснения, чтобы отличалось от «Рекомендаций» */
function faDecorateFYou(){
  try{
    const list = document.getElementById('feedList'); if(!list) return;
    if(curFeedKind !== 'fyou') return;
    /* убрать длинную текстовую врезку ядра */
    const baseNote = list.querySelector(':scope > div:not([class])');
    if(baseNote) baseNote.remove();
    if(!list.querySelector('.fa-head.fa-head-fyou')){
      list.insertAdjacentHTML('afterbegin',
        '<div class="fa-head fa-head-fyou">'+
          '<div class="fa-bar">'+
            '<div class="fa-bar-l">'+
              '<span class="fa-dot"></span>'+
              '<span class="fa-title">Для тебя</span>'+
            '</div>'+
            '<button class="fa-refresh" type="button" onclick="faRefreshFYou()">'+I('fa-refresh')+'<span>Обновить</span></button>'+
          '</div>'+
          '<div class="fa-explain fa-open"><p>Подобрано алгоритмом OKO: вовлечённость + свежесть — как в TikTok. Реагируй на посты, чтобы уточнить.</p></div>'+
        '</div>');
    }
    /* verified galochka и время публикации — обе вкладки уже покрыты faStampTimes/faSeenMark
       в основной цепочке renderFeed. Ничего больше не декорируем — For You умышленно
       чище рекомендаций (никаких «почему показано»-чипов, лента как ролик за роликом). */
  }catch(e){}
}
function faRefreshFYou(){
  try{
    FA.seed = (Math.random() * 4294967295) >>> 0;
    FA.now = Date.now();
    if(typeof POSTS !== 'undefined') POSTS.fyou = faFYouPool();
    renderFeed('fyou');
    const list = document.getElementById('feedList');
    if(list){ list.classList.remove('fa-anim'); void list.offsetWidth; list.classList.add('fa-anim');
      setTimeout(function(){ list.classList.remove('fa-anim'); }, 900); }
    const m = document.querySelector('main'); if(m) m.scrollTo({top:0, behavior:'smooth'});
    if(typeof toast === 'function') toast('Лента обновлена');
  }catch(e){}
}
/* доцеп к renderFeed: перед делегированием базовому — заполняем POSTS.fyou, после — вешаем декоратор */
try{
  if(typeof renderFeed === 'function'){
    const _prevRFfyou = renderFeed;
    renderFeed = function(kind){
      kind = kind || (typeof curFeedKind !== 'undefined' ? curFeedKind : 'sub');
      if(kind === 'fyou' && typeof POSTS !== 'undefined'){
        POSTS.fyou = faFYouPool();
      }
      const r = _prevRFfyou.apply(this, arguments);
      try{ if(kind === 'fyou') faDecorateFYou(); }catch(e){}
      return r;
    };
  }
}catch(e){}
/* инжектим таб как можно раньше — если DOM ещё не готов, ждём */
(function faFYouEarlyBoot(){
  try{
    if(document.readyState === 'loading'){
      document.addEventListener('DOMContentLoaded', function(){ try{ faInjectFYouTab(); }catch(e){} }, {once:true});
    }else{
      faInjectFYouTab();
    }
  }catch(e){}
})();

/* ============================================================================
   19. TIKTOK-PARITY (правки Даниэля 29.07):
     – Табы: только «Рекомендации» + «Подписки», по умолчанию всегда Рекомендации.
     – Порядок кнопок реордерится, чтобы Рекомендации шли первыми (без правки base).
     – При каждом заходе на вкладку ленты — переоткрываем «Рекомендации» и
       переранжируем ленту (feed-order refresh).
     – Единый пул: сторис из STORIES впрыскиваются в ленту как обычные посты.
   ============================================================================ */

/* Сторис-как-посты: добавляем демо-сторис в POSTS.rec/sub. Так они попадают
   в общий алгоритмический ранкинг вместе с реалми, промо и текстовыми постами.
   Помечаем isStory, чтобы decorator показал маленький чип «История». */
function faSeedStoriesAsPosts(){
  try{
    if(typeof STORIES === 'undefined' || !Array.isArray(STORIES)) return;
    if(typeof POSTS === 'undefined') return;
    const now = Date.now();
    STORIES.forEach(function(st, i){
      if(!st || st.add) return;                                /* «Твоя» — не пост */
      const id = 770100 + i;
      if(POSTS.rec.some(function(p){ return p.id === id; })) return;
      const ava = st.avaIcon ? 'ИС' : (st.ava || (st.name ? st.name[0] : 'И'));
      const post = {
        id: id, ava: ava, name: st.name || 'История', sub: 'история · сегодня',
        body: st.text || 'Новая история от ' + (st.name || 'канала'),
        media: null, likes: 40 + Math.floor(faRand(id) * 320),
        views: 800 + Math.floor(faRand(id + 3) * 8400),
        reposts: 3 + Math.floor(faRand(id + 5) * 26),
        liked: false, saved: false, reposted: false, comments: [],
        ts: now - (2 + Math.floor(faRand(id + 7) * 8)) * 36e5,   /* 2–10 ч назад */
        topic: 'content', isStory: true, storyIndex: i
      };
      POSTS.rec.push(post);
      /* сторис от «OKO» также кладём в Подписки — это как «Официальный канал» */
      if(/OKO/i.test(st.name || '') && !POSTS.sub.some(function(p){ return p.id === id; })){
        POSTS.sub.push(post);
      }
    });
  }catch(e){}
}
try{ faSeedStoriesAsPosts(); }catch(e){}

/* Мини-чип «История» на карточку сторис-поста + клик по всей карточке → openStory.
   Ставим ПОСЛЕ базового renderFeed, чтобы не переписывать разметку ядра. */
function faDecorateStoryPosts(){
  try{
    const list = document.getElementById('feedList'); if(!list) return;
    list.querySelectorAll('article.post').forEach(function(art){
      const pid = faIdOf(art); if(pid == null) return;
      const p = postById(pid);
      if(!p || !p.isStory || art.dataset.faStory === '1') return;
      art.dataset.faStory = '1';
      const nameEl = art.querySelector('.head .name');
      if(nameEl && !nameEl.querySelector('.fa-story-chip')){
        nameEl.insertAdjacentHTML('beforeend',
          ' <span class="chip fa-story-chip">' + I('bolt') + '<span>История</span></span>');
      }
      /* клик по телу поста (не по кнопкам) открывает вьювер сторис */
      art.addEventListener('click', function(e){
        if(e.target.closest('button,a,input,textarea,.acts,.post-more,.fa-why,.fa-more-btn')) return;
        if(typeof openStory === 'function' && typeof p.storyIndex === 'number'){
          e.preventDefault(); openStory(p.storyIndex);
        }
      });
    });
  }catch(e){}
}
try{
  if(typeof renderFeed === 'function'){
    const _prevRFStory = renderFeed;
    renderFeed = function(){
      const r = _prevRFStory.apply(this, arguments);
      try{ faDecorateStoryPosts(); }catch(e){}
      return r;
    };
  }
}catch(e){}

/* Реордер табов: «Рекомендации» — первой кнопкой, «Подписки» — второй.
   Форс-выбор «Рекомендации» при любом заходе на экран ленты (TikTok-по-умолчанию). */
function faReorderTabs(){
  /* Порядок ряда задан прямо в разметке (index.html, .feed-tabs):
     «Рекомендации · Подписки · клипы · обновить» — правка Даниэля 09.08.
     Прежняя версия этой функции переставляла кнопки и УДАЛЯЛА всё, что не
     является текстовой вкладкой, — вместе с иконками клипов и обновления.
     Реордер больше не нужен и не делается. Функция оставлена пустой, потому
     что её зовут из нескольких мест. */
}
function faForceRec(){
  try{
    faReorderTabs();
    const tabs = document.querySelector('#screen-feed .feed-tabs');
    if(!tabs) return;
    const recBtn = [...tabs.querySelectorAll('button')].find(function(b){
      return /'rec'/.test(b.getAttribute('onclick') || '');
    });
    if(!recBtn || typeof feedTab !== 'function') return;
    /* новый seed → переранжирование ленты (feed-order обновляется при каждом заходе) */
    FA.seed = (Math.random() * 4294967295) >>> 0;
    FA.now = Date.now();
    feedTab(recBtn, 'rec');
  }catch(e){}
}
try{
  /* при переключении на экран ленты — форсим Рекомендации */
  if(typeof showTab === 'function'){
    const _prevShowTabRec = showTab;
    showTab = function(t){
      const r = _prevShowTabRec.apply(this, arguments);
      try{ if(t === 'feed') setTimeout(faForceRec, 20); }catch(e){}
      return r;
    };
  }
}catch(e){}
/* при первом бут-рендере лента может быть уже открыта на Подписках — перевключим */
(function faDefaultRecBoot(){
  try{
    const kick = function(){
      try{ faReorderTabs(); }catch(e){}
      const feed = document.getElementById('screen-feed');
      if(feed && feed.classList.contains('active')) faForceRec();
    };
    if(document.readyState === 'loading'){
      document.addEventListener('DOMContentLoaded', kick, {once:true});
    }else{
      /* модули грузятся уже после renderFeed('sub') из ядра — переключаем
         сразу же на «Рекомендации» тем же тиком, чтобы не было flash Sub → Rec */
      kick();
    }
  }catch(e){}
})();

/* ===== module: demo-content ===== */
/* ================= DEMO-CONTENT: живое наполнение OKO ================= */
/* Диалоги, каналы, сторис, посты и уведомления, чтобы приложение выглядело
   населённым. CHATS/POSTS/STORIES/NOTIFS ядром НЕ персистятся, поэтому пуш
   выполняется при каждой загрузке; от двойного пуша в одной сессии защищает
   проверка по id (CHATS c.id===9001). Чаты, id 9001..9008 (market-pro
   занимает 9101+, ads-посты 9000+ в другом пространстве POSTS). */

const DC_CHATS = []  /* демо-данные удалены 09.08: модуль demo-content — источник только API */;

/* Поддержка OKO, только если её ещё нет в CHATS (в базе есть, id:1) */
const DC_SUPPORT = {}  /* демо-данные удалены 09.08: дубль официальной поддержки — источник только API */;

const DC_STORIES = []  /* демо-данные удалены 09.08: модуль demo-content — источник только API */;

const DC_POSTS = []  /* демо-данные удалены 09.08: модуль demo-content — источник только API */;

/* лента «Рекомендации»: посты ранжирует алгоритм OKO (feed-algo).
   Отдельный id-диапазон 92xx, не пересекается с ads (9000+) и feed-algo (777xxx). */
const DC_POSTS_REC = []  /* демо-данные удалены 09.08: модуль demo-content — источник только API */;

const DC_NOTIFS = []  /* демо-данные удалены 09.08: модуль demo-content — источник только API */;

/* ---------- закреплённые чаты: сортировка + иконка булавки ---------- */
function dcPatchChatList(){
  if(typeof renderChatList !== 'function' || renderChatList._dcPatched) return;
  const _dcPrevRenderChatList = renderChatList;
  renderChatList = function(filter=''){
    try{ /* живой чат всегда первым, затем закреплённые, остальные, как были */
      CHATS.sort((a,b)=>((b.id==='live'?1:0)-(a.id==='live'?1:0)) || ((b.pinned?1:0)-(a.pinned?1:0)));
    }catch(e){}
    _dcPrevRenderChatList(filter);
    try{
      CHATS.filter(c=>c.pinned).forEach(c=>{
        const t = document.querySelector('#chatList .chat-item[onclick="openConv('+c.id+')"] .time');
        if(t && !t.querySelector('.dc-pin'))
          t.insertAdjacentHTML('afterbegin','<span class="dc-pin" style="display:inline-flex;align-items:center;margin-right:5px;color:var(--lime)">'+I('pin')+'</span>');
      });
    }catch(e){}
  };
  renderChatList._dcPatched = true;
}

/* ---------- сторис с градиент-фоном бренда (поле bg) ---------- */
function dcPatchStory(){
  if(typeof showStory !== 'function' || showStory._dcPatched) return;
  const _dcPrevShowStory = showStory;
  showStory = function(){
    _dcPrevShowStory();
    try{
      const st = STORIES[curStory];
      const card = document.getElementById('svCard');
      if(!card) return;
      if(st && st.bg && !st.src){
        card.style.background = st.bg;
        card.style.borderRadius = '20px';
        card.style.border = '1px solid rgba(154,255,0,.18)';
        card.style.padding = '38px 24px';
      } else {
        card.style.background = ''; card.style.borderRadius = '';
        card.style.border = ''; card.style.padding = '';
      }
    }catch(e){}
  };
  showStory._dcPatched = true;
}

/* ---------- наполнение (при каждой загрузке; защита, по id 9001) ---------- */
function dcSeed(){
  if(typeof CHATS === 'undefined' || CHATS.find(c=>c.id===9001)) return false;

  /* чаты, сразу после живого чата, чтобы список выглядел свежим */
  const at = Math.min(1, CHATS.length);
  CHATS.splice(at, 0, ...DC_CHATS);
  if(!CHATS.some(c=>c.name==='Поддержка OKO')) CHATS.push(DC_SUPPORT);

  /* сторис, в общий ряд после существующих */
  if(typeof STORIES !== 'undefined' && !STORIES.some(s=>s.name==='OKO Team'))
    STORIES.push(...DC_STORIES);

  /* лента «Подписки», свежие посты сверху */
  if(typeof POSTS !== 'undefined' && POSTS.sub && !POSTS.sub.some(p=>p.id===9101))
    POSTS.sub.unshift(...DC_POSTS);

  /* лента «Рекомендации», добавляем в пул, ранжирование делает алгоритм OKO */
  if(typeof POSTS !== 'undefined' && POSTS.rec && !POSTS.rec.some(p=>p.id===9201))
    POSTS.rec.push(...DC_POSTS_REC);

  /* уведомления, непрочитанные, в начало */
  if(typeof NOTIFS !== 'undefined' && !NOTIFS.length)
    NOTIFS.unshift(...DC_NOTIFS);

  return true;
}

function dcRenderAll(){
  try{
    if(typeof renderChatList === 'function' && document.getElementById('chatList')){
      const s = document.getElementById('chatSearch');
      renderChatList(s ? s.value : '');
    }
  }catch(e){}
  try{ if(typeof renderFolders === 'function' && document.getElementById('folders')) renderFolders(); }catch(e){}
  try{ if(typeof renderStories === 'function' && document.getElementById('storiesRow')) renderStories(); }catch(e){}
  try{ if(typeof renderFeed === 'function' && document.getElementById('feedList')) renderFeed(typeof curFeedKind !== 'undefined' ? curFeedKind : 'sub'); }catch(e){}
  try{ if(typeof updateNotifDot === 'function') updateNotifDot(); }catch(e){}
}

/* ---------- самоинициализация ---------- */
(function dcInit(){
  try{
    dcPatchChatList();
    dcPatchStory();
    if(dcSeed()) dcRenderAll();
  }catch(e){ console.warn('demo-content init:', e); }
})();

/* =====================================================================
   DEMO-CONTENT · EXTRA: 20 живых чатов для владельца/демо-режима.
   IDs 9020–9039 (не пересекаются с блоком выше, 9001–9016).
   Показываются только Даниэлю (isOwner()) либо в demo-режиме
   (localStorage['oko-demo']==='1'). Новые юзеры не видят.
   ===================================================================== */

/* Та же история, что и в mpIsDemo: владельцу демо-контент открывался
   автоматически. Убрано — владелец видит ровно то же, что новый человек. */
function dcExtraAllowed(){
  try{
    if(typeof localStorage !== 'undefined' && localStorage.getItem('oko-demo') === '1') return true;
  }catch(e){}
  return false;
}

/* Детерминированный SVG-градиент по имени: hash → две HSL-точки в бренд-палитре.
   Всегда тёмный (совместим с dark/light темами), лаймовый акцент даётся отдельным h. */
function dcAvaGrad(name){
  let h = 2166136261;
  for(let i=0; i<name.length; i++){ h ^= name.charCodeAt(i); h = (h * 16777619) >>> 0; }
  const h1 = h % 360;
  const h2 = (h1 + 30 + ((h >> 8) % 80)) % 360;
  const s  = 42 + ((h >> 16) % 22);
  const l1 = 22 + ((h >> 20) % 8);
  const l2 = 12 + ((h >> 24) % 8);
  return `linear-gradient(135deg, hsl(${h1} ${s}% ${l1}%) 0%, hsl(${h2} ${s+8}% ${l2}%) 100%)`;
}

/* Патчим avaHtml — если у чата есть avaBg, отдаём с инлайновым градиентом.
   Иначе fallback на оригинальную реализацию из base.html. */
function dcPatchAvaHtmlExtra(){
  if(typeof avaHtml !== 'function' || avaHtml._dcExtraPatched) return;
  const _orig = avaHtml;
  window.avaHtml = function(c){
    if(c && c.avaBg && !c.avaIcon){
      const txt = c.ava || (c.name ? c.name.trim().charAt(0) : '?');
      return `<div class="ava" style="background:${c.avaBg};color:#fff;font-weight:800;letter-spacing:.5px">${txt}</div>`;
    }
    return _orig(c);
  };
  window.avaHtml._dcExtraPatched = true;
}

/* -------------------- 10 личных диалогов -------------------- */
const DC_CHATS_X_DIRECT = []  /* демо-данные удалены 09.08: модуль demo-content — источник только API */;

/* -------------------- 5 групповых чатов -------------------- */
const DC_CHATS_X_GROUPS = []  /* демо-данные удалены 09.08: модуль demo-content — источник только API */;

/* -------------------- 5 каналов -------------------- */
const DC_CHATS_X_CHANNELS = []  /* демо-данные удалены 09.08: модуль demo-content — источник только API */;

/* -------------------- сид + инициализация -------------------- */
function dcSeedExtra(){
  if(typeof CHATS === 'undefined') return false;
  if(!dcExtraAllowed()) return false;
  if(CHATS.find(c=>c.id===9020)) return false; /* защита от двойного пуша */

  const all = [...DC_CHATS_X_DIRECT, ...DC_CHATS_X_GROUPS, ...DC_CHATS_X_CHANNELS];
  /* градиентная аватарка по имени — только там, где нет своей */
  all.forEach(c=>{ if(!c.avaBg && !c.avaIcon) c.avaBg = dcAvaGrad(c.name); });

  /* вставляем сразу после живого чата, живой чат — всегда №1 */
  const at = Math.min(1, CHATS.length);
  CHATS.splice(at, 0, ...all);
  return true;
}

/* --- подсказка админу: включить/выключить демо-режим прямо из консоли ---
   okoDemo(true|false). Полезно для скринкастов и превью на публичной ссылке. */
if(typeof window !== 'undefined' && !window.okoDemo){
  window.okoDemo = function(on){
    try{
      if(on === false){ localStorage.removeItem('oko-demo'); }
      else { localStorage.setItem('oko-demo','1'); }
      location.reload();
    }catch(e){}
  };
}

(function dcInitExtra(){
  try{
    dcPatchAvaHtmlExtra();
    if(dcSeedExtra()){
      /* если чат-лист уже отрисован — обновляем без ожидания */
      try{
        if(typeof renderChatList === 'function' && document.getElementById('chatList')){
          const s = document.getElementById('chatSearch');
          renderChatList(s ? s.value : '');
        }
      }catch(e){}
    }
  }catch(e){ console.warn('demo-content extra init:', e); }
})();

/* ===== module: chats-plus ===== */
/* ===== CHATS-PLUS: доработки мессенджера ПОВЕРХ ядра (префикс cp-) =====
   base.html не тронут — только chain-патчи функций ядра и DOM-проходы.
   1) Статусы сообщений: галочки у исходящих (одна = отправлено, две = доставлено,
      две зелёные = прочитано). Мок: «прочитано» через 2–5 с, если чат онлайн.
   2) Поиск по сообщениям: лупа в шапке конва -> строка поиска, подсветка,
      счётчик «N из M», стрелки раньше/позже.
   3) Реальный закреп: пин-бар над лентой, тап скроллит к сообщению, крестик снимает.
   4) Скорость голосовых x1/x1.5/x2 (chain togglePlay/playVnote), персист.
   5) Свайп-ответ: горизонтальный свайп вправо >40px -> replyTo (без конфликта
      со скроллом: жест берём только при |dx|>|dy|*2). */

const CP = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-chats-plus'))||{}; }catch(e){ return {}; } })();
if(CP.speed!==1.5 && CP.speed!==2) CP.speed = 1;
if(!Array.isArray(CP.recentEmoji)) CP.recentEmoji = [];
if(!Array.isArray(CP.recentOko)) CP.recentOko = [];   /* недавние фирменные OKO-эмодзи (ключи) */
if(!CP.panelTab) CP.panelTab = 'emoji';
if(!CP.access || typeof CP.access!=='object') CP.access = {};     /* {chatId:{type,price}} — овнерские настройки доступа */
if(!CP.unlocked || typeof CP.unlocked!=='object') CP.unlocked = {}; /* {chatId:1} — купленный доступ к платному чату */
function cpSave(){ try{ localStorage.setItem('oko-chats-plus', JSON.stringify({speed:CP.speed, recentEmoji:CP.recentEmoji.slice(0,24), recentOko:CP.recentOko.slice(0,24), panelTab:CP.panelTab, access:CP.access, unlocked:CP.unlocked})); }catch(e){} }
const cpMsgsEl = () => document.getElementById('msgs');
const cpEsc = t => (typeof esc==='function' ? esc(t) : String(t==null?'':t));

/* ================= 1. СТАТУСЫ СООБЩЕНИЙ ================= */
const CP_ST_TITLE = {sent:'Отправлено', delivered:'Доставлено', read:'Прочитано'};

function cpPaintStatuses(){
  if(typeof currentChat==='undefined' || !currentChat) return;
  const el = cpMsgsEl(); if(!el) return;
  const kids = el.children, msgs = currentChat.msgs || [];
  for(let i=0; i<msgs.length && i<kids.length; i++){
    const m = msgs[i];
    if(!m || m.in || m.kind==='sys') continue;
    const t = kids[i].querySelector('.t');
    if(!t) continue; /* vnote и пр. без строки времени */
    t.classList.add('cp-t');
    const st = m.cpSt || (currentChat.online ? 'read' : 'delivered');
    let sp = t.querySelector('.cp-st');
    if(!sp){
      const old = t.querySelector('svg'); if(old) old.remove(); /* родная одиночная check2 */
      sp = document.createElement('span'); sp.className = 'cp-st';
      t.appendChild(sp);
    }
    if(sp.dataset.st !== st){
      const had = !!sp.dataset.st;
      sp.dataset.st = st;
      sp.title = CP_ST_TITLE[st] || '';
      sp.innerHTML = st==='sent' ? I('check') : I('check2');
      if(had){ sp.classList.remove('cp-pop'); void sp.offsetWidth; sp.classList.add('cp-pop'); }
    }
  }
}
function cpPaintIf(chat){ if(typeof currentChat!=='undefined' && currentChat===chat) cpPaintStatuses(); }

/* мок-жизнь статусов: sent -> delivered (~1 c) -> read (2–5 c, если чат онлайн) */
if(typeof pushMsg === 'function'){
  const _cpPrevPushMsg = pushMsg;
  pushMsg = function(m){
    _cpPrevPushMsg.apply(this, arguments);
    if(!m || m.in || m.kind==='sys' || typeof currentChat==='undefined' || !currentChat) return;
    const chat = currentChat;
    m.cpSt = 'sent'; cpPaintIf(chat);
    setTimeout(()=>{ if(m.cpSt==='sent'){ m.cpSt='delivered'; cpPaintIf(chat); } }, 600 + Math.random()*600);
    if(chat.online)
      setTimeout(()=>{ m.cpSt='read'; cpPaintIf(chat); }, 2000 + Math.random()*3000);
  };
}

/* ================= 2. ПОИСК ПО СООБЩЕНИЯМ ================= */
let cpMarks = [], cpCur = -1;

function cpOpenSearch(){
  const bar = document.getElementById('cpSearchBar');
  if(!bar || typeof currentChat==='undefined' || !currentChat) return;
  if(bar.classList.contains('on')){ cpCloseSearch(); return; }
  bar.classList.add('on');
  const inp = document.getElementById('cpSearchInput');
  inp.value = ''; cpUpdateCount();
  setTimeout(()=>inp.focus(), 60);
  if(typeof nvPush==='function') nvPush('cp:msearch', ()=>cpCloseSearch(true));
}
function cpCloseSearch(fromNav){
  const bar = document.getElementById('cpSearchBar');
  if(!bar || !bar.classList.contains('on')) return;
  bar.classList.remove('on');
  const inp = document.getElementById('cpSearchInput');
  if(inp) inp.value = '';
  cpClearMarks(); cpUpdateCount();
  if(!fromNav && typeof nvPop==='function') nvPop('cp:msearch');
}
function cpClearMarks(){
  cpMarks.forEach(mk=>{
    const p = mk.parentNode; if(!p) return; /* лента могла перерендериться */
    p.replaceChild(document.createTextNode(mk.textContent), mk);
    p.normalize();
  });
  cpMarks = []; cpCur = -1;
}
function cpSearchRun(){
  cpClearMarks();
  const inp = document.getElementById('cpSearchInput');
  const q = (inp && inp.value || '').trim().toLowerCase();
  const el = cpMsgsEl();
  if(!q || !el){ cpUpdateCount(); return; }
  el.querySelectorAll('.msg').forEach(msg=>{
    if(msg.id==='typingMsg') return;
    const tw = document.createTreeWalker(msg, NodeFilter.SHOW_TEXT, { acceptNode: n => {
      if(!n.nodeValue || n.nodeValue.toLowerCase().indexOf(q) < 0) return NodeFilter.FILTER_REJECT;
      const pe = n.parentElement;
      if(pe && (pe.closest('.t') || pe.closest('.cp-st') || pe.closest('.cp-speed'))) return NodeFilter.FILTER_REJECT;
      return NodeFilter.FILTER_ACCEPT;
    }});
    const nodes = []; while(tw.nextNode()) nodes.push(tw.currentNode);
    nodes.forEach(node=>{
      const text = node.nodeValue, low = text.toLowerCase();
      const frag = document.createDocumentFragment();
      let from = 0, pos;
      while((pos = low.indexOf(q, from)) >= 0){
        frag.appendChild(document.createTextNode(text.slice(from, pos)));
        const mk = document.createElement('mark');
        mk.className = 'cp-mk'; mk.textContent = text.slice(pos, pos + q.length);
        frag.appendChild(mk); cpMarks.push(mk);
        from = pos + q.length;
      }
      frag.appendChild(document.createTextNode(text.slice(from)));
      node.parentNode.replaceChild(frag, node);
    });
  });
  cpCur = cpMarks.length - 1; /* стартуем с самого свежего совпадения */
  cpFocusMark(true);
}
function cpFocusMark(instant){
  cpMarks.forEach((m,i)=>m.classList.toggle('cp-cur', i===cpCur));
  const mk = cpMarks[cpCur];
  if(mk) mk.scrollIntoView({behavior: instant ? 'auto' : 'smooth', block:'center'});
  cpUpdateCount();
}
function cpSearchStep(d){
  if(!cpMarks.length) return;
  cpCur = (cpCur + d + cpMarks.length) % cpMarks.length;
  cpFocusMark(false);
}
function cpUpdateCount(){
  const c = document.getElementById('cpSearchCount'); if(!c) return;
  const inp = document.getElementById('cpSearchInput');
  const q = (inp && inp.value || '').trim();
  c.textContent = !q ? '' : (cpMarks.length ? (cpCur+1)+' из '+cpMarks.length : 'нет совпадений');
}
function cpReapplySearch(){
  const bar = document.getElementById('cpSearchBar');
  if(!bar || !bar.classList.contains('on')) return;
  const inp = document.getElementById('cpSearchInput');
  if(inp && inp.value.trim()){ cpMarks = []; cpCur = -1; cpSearchRun(); }
}

/* ================= 3. ЗАКРЕП СООБЩЕНИЯ (реальный пин-бар) ================= */
const _cpPrevPinMsg = (typeof pinMsg === 'function') ? pinMsg : null;
pinMsg = function(idx){
  if(typeof closeMsgMenu==='function') closeMsgMenu();
  if(typeof currentChat==='undefined' || !currentChat) return;
  const m = currentChat.msgs[idx];
  if(!m || m.kind==='sys') return;
  if(currentChat.cpPin === m){ toast('Уже закреплено'); return; }
  currentChat.cpPin = m; /* ссылка на объект — индексы могут съехать при удалении */
  cpUpdatePinBar(true);
  toast('Сообщение закреплено');
};
function cpUpdatePinBar(anim){
  const bar = document.getElementById('cpPinBar'); if(!bar) return;
  const chat = (typeof currentChat!=='undefined') ? currentChat : null;
  const pin = chat && chat.cpPin;
  const idx = pin ? chat.msgs.indexOf(pin) : -1;
  if(idx < 0){
    bar.classList.remove('on');
    if(chat && pin) chat.cpPin = null; /* закреплённое удалили */
    return;
  }
  const txt = document.getElementById('cpPinText');
  if(txt) txt.textContent = ((typeof msgQuoteText==='function' ? msgQuoteText(pin) : pin.body) || 'Сообщение');
  const was = bar.classList.contains('on');
  bar.classList.add('on');
  if(anim && !was){ bar.classList.remove('cp-pin-in'); void bar.offsetWidth; bar.classList.add('cp-pin-in'); }
}
function cpScrollToPinned(){
  const chat = (typeof currentChat!=='undefined') ? currentChat : null;
  if(!chat || !chat.cpPin) return;
  const idx = chat.msgs.indexOf(chat.cpPin);
  const el = cpMsgsEl();
  const kid = el && el.children[idx];
  if(!kid) return;
  kid.scrollIntoView({behavior:'smooth', block:'center'});
  kid.classList.remove('cp-flash'); void kid.offsetWidth; kid.classList.add('cp-flash');
}
function cpUnpin(ev){
  if(ev) ev.stopPropagation();
  if(typeof currentChat!=='undefined' && currentChat) currentChat.cpPin = null;
  cpUpdatePinBar();
  toast('Закреп снят');
}

/* ================= 4. СКОРОСТЬ ГОЛОСОВЫХ x1/x1.5/x2 ================= */
function cpSpeedLabel(){ return 'x' + (CP.speed===1.5 ? '1.5' : CP.speed===2 ? '2' : '1'); }
function cpDecorateVoices(){
  const el = cpMsgsEl(); if(!el) return;
  el.querySelectorAll('.voice').forEach(v=>{
    if(v.querySelector('.cp-speed')) return;
    const b = document.createElement('button');
    b.className = 'cp-speed'; b.title = 'Скорость воспроизведения';
    b.textContent = cpSpeedLabel();
    b.onclick = cpCycleSpeed;
    v.appendChild(b);
  });
}
function cpCycleSpeed(ev){
  if(ev) ev.stopPropagation();
  CP.speed = CP.speed===1 ? 1.5 : CP.speed===1.5 ? 2 : 1;
  cpSave();
  document.querySelectorAll('.cp-speed,.cp-vn-speed').forEach(b=>b.textContent = cpSpeedLabel());
  document.querySelectorAll('.voice .pp').forEach(pp=>{ if(pp._audio) try{ pp._audio.playbackRate = CP.speed; }catch(e){} });
  document.querySelectorAll('.vnote video').forEach(v=>{ try{ v.playbackRate = CP.speed; }catch(e){} });
}
if(typeof togglePlay === 'function'){
  const _cpPrevTogglePlay = togglePlay;
  togglePlay = function(btn){
    _cpPrevTogglePlay.apply(this, arguments);
    if(btn && btn._audio) try{ btn._audio.playbackRate = CP.speed; }catch(e){}
  };
}
if(typeof playVnote === 'function'){
  const _cpPrevPlayVnote = playVnote;
  playVnote = function(el){
    _cpPrevPlayVnote.apply(this, arguments);
    const v = el && el.querySelector && el.querySelector('video');
    if(v) try{ v.playbackRate = CP.speed; }catch(e){}
  };
}

/* ================= 5. СВАЙП-ОТВЕТ ================= */
let cpSw = null;
function cpSwipeInit(){
  const el = cpMsgsEl(); if(!el || el._cpSwipe) return; el._cpSwipe = true;
  el.addEventListener('touchstart', e=>{
    if(e.touches.length !== 1){ cpSw = null; return; }
    const msg = e.target.closest('.msg');
    if(!msg || msg.classList.contains('sys') || msg.classList.contains('typing')){ cpSw = null; return; }
    cpSw = {el:msg, x:e.touches[0].clientX, y:e.touches[0].clientY, dx:0, on:false};
  }, {passive:true});
  el.addEventListener('touchmove', e=>{
    if(!cpSw) return;
    const dx = e.touches[0].clientX - cpSw.x;
    const dy = e.touches[0].clientY - cpSw.y;
    if(!cpSw.on){
      if(dx > 12 && Math.abs(dx) > Math.abs(dy)*2){ /* уверенно горизонтальный, вправо */
        cpSw.on = true;
        cpSw.el.classList.add('cp-sw');
        if(!cpSw.el.querySelector('.cp-sw-ic')){
          const ic = document.createElement('span');
          ic.className = 'cp-sw-ic'; ic.innerHTML = I('reply');
          cpSw.el.appendChild(ic);
        }
      } else if(Math.abs(dy) > 14 || dx < -14){ cpSw = null; return; } /* это скролл — отпускаем */
    }
    if(cpSw && cpSw.on){
      e.preventDefault();
      const d = Math.max(0, Math.min(dx, 76));
      cpSw.dx = d;
      cpSw.el.style.transform = 'translateX(' + d + 'px)';
      const ic = cpSw.el.querySelector('.cp-sw-ic');
      if(ic) ic.style.opacity = Math.min(1, d/40);
      cpSw.el.classList.toggle('cp-sw-hit', d > 40);
    }
  }, {passive:false});
  const end = ()=>{
    if(!cpSw) return;
    const s = cpSw; cpSw = null;
    const hit = s.on && s.dx > 40;
    const idx = Array.prototype.indexOf.call(el.children, s.el);
    s.el.classList.remove('cp-sw');
    s.el.classList.add('cp-sw-back');
    s.el.style.transform = '';
    s.el.classList.remove('cp-sw-hit');
    setTimeout(()=>{
      s.el.classList.remove('cp-sw-back');
      const ic = s.el.querySelector('.cp-sw-ic'); if(ic) ic.remove();
    }, 240);
    if(hit && idx >= 0 && typeof currentChat!=='undefined' && currentChat
       && currentChat.msgs[idx] && currentChat.msgs[idx].kind !== 'sys'
       && typeof replyTo === 'function') replyTo(idx);
  };
  el.addEventListener('touchend', end);
  el.addEventListener('touchcancel', end);
}

/* ================= UI-ИНЖЕКЦИЯ (шапка конва, пин-бар, строка поиска) ================= */
/* Шапка «телеграм-стайл»: аватар+имя+статус | аудио | видео | ⋮ (меню).
   Поиск, участники и доступ спрятаны в ⋮-меню — шапка не теснится, имя дышит. */
function cpBuildUi(){
  const head = document.querySelector('#convBody .conv-head');
  if(!head || document.getElementById('cpMoreBtn')) return;
  /* единственная новая кнопка в шапке — «ещё» (⋮) в самом конце */
  const btn = document.createElement('button');
  btn.className = 'ch-call cp-more-btn'; btn.id = 'cpMoreBtn'; btn.title = 'Ещё';
  btn.innerHTML = I('more');
  btn.onclick = cpOpenMore;
  head.appendChild(btn);
  /* выпадающее меню действий (поиск / профиль / доступ) */
  if(!document.getElementById('cpMore')){
    const wrap = document.createElement('div');
    wrap.className = 'cp-more'; wrap.id = 'cpMore';
    wrap.innerHTML = `<div class="cp-more-menu" id="cpMoreMenu" role="menu"></div>`;
    wrap.addEventListener('click', e=>{ if(e.target===wrap) cpCloseMore(); });
    document.body.appendChild(wrap);
  }
  /* пин-бар */
  const pin = document.createElement('div');
  pin.className = 'cp-pinbar'; pin.id = 'cpPinBar';
  pin.onclick = cpScrollToPinned;
  pin.innerHTML = `<span class="cp-pin-ic">${I('pin')}</span>
    <div class="cp-pin-txt"><b>Закреплённое сообщение</b><small id="cpPinText"></small></div>
    <button class="cp-pin-x" onclick="cpUnpin(event)" title="Открепить">${I('plus')}</button>`;
  head.insertAdjacentElement('afterend', pin);
  /* строка поиска */
  const bar = document.createElement('div');
  bar.className = 'cp-msearch'; bar.id = 'cpSearchBar';
  bar.innerHTML = `<span class="cp-ms-ic">${I('search')}</span>
    <input id="cpSearchInput" placeholder="Поиск по сообщениям" autocomplete="off">
    <span class="cp-ms-count" id="cpSearchCount"></span>
    <button class="cp-ms-nav" onclick="cpSearchStep(-1)" title="Раньше">${I('chev')}</button>
    <button class="cp-ms-nav cp-ms-next" onclick="cpSearchStep(1)" title="Позже">${I('chev')}</button>
    <button class="cp-ms-x" onclick="cpCloseSearch()" title="Закрыть поиск">${I('plus')}</button>`;
  pin.insertAdjacentElement('afterend', bar);
  const inp = bar.querySelector('#cpSearchInput');
  inp.addEventListener('input', cpSearchRun);
  inp.addEventListener('keydown', e=>{
    if(e.key === 'Enter'){ e.preventDefault(); cpSearchStep(e.shiftKey ? 1 : -1); }
  });
}

/* ---- ⋮-меню шапки: поиск / профиль / доступ (контекстно) ---- */
function cpMoreItems(){
  const c = (typeof currentChat!=='undefined') ? currentChat : null;
  const items = [];
  items.push({ic:'search', label:'Поиск по сообщениям', act:cpOpenSearch});
  if(typeof openProfile==='function'){
    const grp = c && (c.kind==='group' || c.kind==='channel');
    items.push({ic: grp?'users':'user', label: grp?'Участники и профиль':'Профиль чата', act:()=>{ try{ openProfile(); }catch(e){} }});
  }
  if(c){
    const a = cpAcc(c);
    if(a.type!=='public' || (typeof cpCanManage==='function' && cpCanManage())){
      const own = (typeof cpCanManage==='function' && cpCanManage());
      const ic = a.type==='paid' ? 'crown' : a.type==='private' ? 'lock' : 'users';
      const label = own ? 'Управление доступом' : (a.type==='paid' ? 'Доступ к клубу' : 'Ссылка-приглашение');
      items.push({ic, label, act:cpAccessMenu});
    }
  }
  return items;
}
function cpOpenMore(){
  const wrap = document.getElementById('cpMore'), menu = document.getElementById('cpMoreMenu');
  const btn = document.getElementById('cpMoreBtn');
  if(!wrap || !menu || !btn) return;
  if(wrap.classList.contains('on')){ cpCloseMore(); return; }
  const its = cpMoreItems();
  menu.innerHTML = its.map((it,i)=>
    `<button class="cp-more-item" style="--cp-i:${i}" role="menuitem">${I(it.ic)}<span>${cpEsc(it.label)}</span></button>`).join('');
  [...menu.children].forEach((el,i)=>{ const it = its[i]; el.onclick = ()=>{ cpCloseMore(); if(it && it.act) try{ it.act(); }catch(e){} }; });
  /* якорим меню под кнопкой, справа */
  const r = btn.getBoundingClientRect();
  menu.style.top = Math.round(r.bottom + 6) + 'px';
  menu.style.right = Math.round(window.innerWidth - r.right) + 'px';
  wrap.classList.add('on');
  requestAnimationFrame(()=>menu.classList.add('on'));
  btn.classList.add('on');
  if(typeof nvPush==='function') nvPush('cp:more', ()=>cpCloseMore(true));
}
function cpCloseMore(fromNav){
  const wrap = document.getElementById('cpMore'), menu = document.getElementById('cpMoreMenu');
  const btn = document.getElementById('cpMoreBtn');
  if(!wrap || !wrap.classList.contains('on')) return;
  menu && menu.classList.remove('on');
  wrap.classList.remove('on');
  btn && btn.classList.remove('on');
  if(!fromNav && typeof nvPop==='function') nvPop('cp:more');
}

/* ================= CHAIN-ПАТЧИ РЕНДЕРА И НАВИГАЦИИ КОНВА ================= */
if(typeof renderMsgs === 'function'){
  const _cpPrevRenderMsgs = renderMsgs;
  renderMsgs = function(){
    _cpPrevRenderMsgs.apply(this, arguments);
    cpPaintStatuses();
    cpDecorateVoices();
    cpUpdatePinBar();
    cpReapplySearch();
  };
}
if(typeof openConv === 'function'){
  const _cpPrevOpenConv = openConv;
  openConv = function(){
    cpCloseSearch(); cpCloseMore(); /* поиск/меню не тащим между чатами */
    _cpPrevOpenConv.apply(this, arguments);
  };
}
if(typeof closeConv === 'function'){
  const _cpPrevCloseConv = closeConv;
  closeConv = function(){
    cpCloseSearch(); cpCloseMore();
    _cpPrevCloseConv.apply(this, arguments);
  };
}

/* ================= САМОИНИЦИАЛИЗАЦИЯ ================= */
(function cpInit(){
  cpBuildUi();
  cpSwipeInit();
  /* исторические исходящие: онлайн-чат — прочитаны, офлайн — доставлены */
  try{
    (typeof CHATS !== 'undefined' ? CHATS : []).forEach(c=>{
      (c.msgs || []).forEach(m=>{
        if(m && !m.in && m.kind !== 'sys' && !m.cpSt) m.cpSt = c.online ? 'read' : 'delivered';
      });
    });
  }catch(e){}
  /* конва уже открыта (пересборка на лету) — прогнать декор */
  if(typeof currentChat !== 'undefined' && currentChat){
    cpPaintStatuses(); cpDecorateVoices(); cpUpdatePinBar();
  }
})();

/* =======================================================================
   ЧАСТЬ 2 — Telegram+: контекст-меню, стикер/эмодзи-панель у поля,
   красивые голосовые кружочки/волна, экран звонка. Всё поверх ядра
   (base.html не тронут): CSS + chain-патчи + DOM-инъекции по id/классам.
   ======================================================================= */

/* ================= 6. КРАСИВОЕ КОНТЕКСТ-МЕНЮ СООБЩЕНИЯ ================= */
/* База уже наполняет #mmReacts/#mmPreview/#mmActions (Ответить/Копировать/
   Переслать/Редактировать/Закрепить/Удалить). Делаем меню «телеграм-плюс»:
   привязываем к точке нажатия, стеклянная тема, стаггер-анимация реакций,
   разделители/крупные иконки (через CSS), гарантированная прокрутка. */
let cpTapPt = null; /* координаты последнего нажатия по сообщению */
function cpInitMenuAnchor(){
  const el = cpMsgsEl(); if(!el || el._cpMenuPt) return; el._cpMenuPt = true;
  el.addEventListener('pointerdown', e=>{
    const msg = e.target.closest('.msg');
    if(msg && !msg.classList.contains('sys')) cpTapPt = {x:e.clientX, y:e.clientY};
    else cpTapPt = null;
  }, {passive:true});
}
if(typeof openMsgMenu === 'function'){
  const _cpPrevOpenMsgMenu = openMsgMenu;
  openMsgMenu = function(idx){
    _cpPrevOpenMsgMenu.apply(this, arguments);
    cpEnhanceMenu(cpTapPt);
  };
}
if(typeof openPostMenu === 'function'){
  const _cpPrevOpenPostMenu = openPostMenu;
  openPostMenu = function(){ _cpPrevOpenPostMenu.apply(this, arguments); cpEnhanceMenu(null); };
}
function cpEnhanceMenu(pt){
  const inner = document.querySelector('#msgMenu .mm-inner');
  const reacts = document.getElementById('mmReacts');
  if(!inner) return;
  inner.classList.add('cp-mm');
  /* стаггер появления реакций */
  if(reacts && reacts.style.display !== 'none'){
    [...reacts.children].forEach((b,i)=>{ b.style.setProperty('--cp-i', i); });
  }
  /* «якорим» меню к сообщению по вертикали, чтобы палец не перекрывал пункты */
  inner.style.transformOrigin = pt ? (pt.y < window.innerHeight/2 ? 'top center' : 'bottom center') : 'center';
}

/* ================= 7. СТИКЕРЫ + ЭМОДЗИ ПРЯМО У ПОЛЯ (composer) ================= */
const CP_EMOJI = ['😀','😁','😂','🤣','😊','😍','😘','😎','🤩','🥳','😇','🙂','😉','😌','😗','😙',
  '🤔','🤨','😐','😴','😜','😝','🤗','🤭','🙃','😏','😒','😞','😢','😭','😤','😠','😡','🤬','😱','😨',
  '👍','👎','👏','🙏','🤝','💪','🔥','✨','⭐','🌟','💯','✅','❌','⚡','🎉','🎊','🏆','🥇','💎','💰',
  '❤️','🧡','💛','💚','💙','💜','🖤','🤍','💔','💥','👀','🚀','🌈','☀️','🌙','⚽'];
/* CP_EMOJI выше — устаревший системный набор (оставлен как fallback, НЕ рендерится).
   Ниже — фирменный набор OKO: моно-SVG в стиле бренда (line + минимальная заливка,
   currentColor). Каждый эмодзи = сырой inner-markup, база стиля задаётся на <svg>
   (fill:none stroke:currentColor width:1.8). Заливка через fill=currentColor stroke=none.
   Вставляются в текст как шорткод :oko-KEY: и рендерятся обратно фирменным SVG. */
const CP_OKO_EMOJI = {
  /* --- Смайлы --- */
  happy:'<circle cx="12" cy="12" r="9"/><circle cx="9" cy="10.4" r="1.1" fill="currentColor" stroke="none"/><circle cx="15" cy="10.4" r="1.1" fill="currentColor" stroke="none"/><path d="M8 14.4q4 2.7 8 0"/>',
  laugh:'<circle cx="12" cy="12" r="9"/><path d="M7.4 10.6q1.2-1.3 2.5 0"/><path d="M14.1 10.6q1.2-1.3 2.5 0"/><path d="M7.4 13.4h9.2a4.6 4.6 0 0 1-9.2 0z" fill="currentColor" stroke="none"/>',
  love:'<circle cx="12" cy="12" r="9"/><path d="M9 11.9c-1.5-1.1-2.4-1.9-2.4-2.9 0-.75.6-1.25 1.35-1.25.55 0 1 .3 1.05.6.05-.3.5-.6 1.05-.6.75 0 1.35.5 1.35 1.25 0 1-.9 1.8-2.4 2.9z" fill="currentColor" stroke="none"/><path d="M15 11.9c-1.5-1.1-2.4-1.9-2.4-2.9 0-.75.6-1.25 1.35-1.25.55 0 1 .3 1.05.6.05-.3.5-.6 1.05-.6.75 0 1.35.5 1.35 1.25 0 1-.9 1.8-2.4 2.9z" fill="currentColor" stroke="none"/><path d="M8.3 14.6q3.7 2.6 7.4 0"/>',
  cool:'<circle cx="12" cy="12" r="9"/><rect x="5.6" y="8.8" width="4.7" height="3.3" rx="1.3" fill="currentColor" stroke="none"/><rect x="13.7" y="8.8" width="4.7" height="3.3" rx="1.3" fill="currentColor" stroke="none"/><path d="M10.3 9.6h3.4"/><path d="M8.5 15.2q3.5 1.8 6.4-.4"/>',
  wink:'<circle cx="12" cy="12" r="9"/><circle cx="9" cy="10.4" r="1.1" fill="currentColor" stroke="none"/><path d="M13.7 10.5q1.2-1.1 2.6 0"/><path d="M8 14.4q4 2.7 8 0"/>',
  tongue:'<circle cx="12" cy="12" r="9"/><circle cx="9" cy="10.4" r="1.1" fill="currentColor" stroke="none"/><circle cx="15" cy="10.4" r="1.1" fill="currentColor" stroke="none"/><path d="M8 14.2q4 2.5 8 0"/><path d="M11 15.6h2.6v1c0 1.3-2.6 1.3-2.6 0z" fill="currentColor" stroke="none"/>',
  think:'<circle cx="12" cy="12" r="9"/><circle cx="9" cy="10.6" r="1.1" fill="currentColor" stroke="none"/><circle cx="15" cy="10.6" r="1.1" fill="currentColor" stroke="none"/><path d="M13.6 8.2q1.4-.8 2.6.2"/><path d="M9.2 15.4q2-.9 4 0"/>',
  neutral:'<circle cx="12" cy="12" r="9"/><circle cx="9" cy="10.6" r="1.1" fill="currentColor" stroke="none"/><circle cx="15" cy="10.6" r="1.1" fill="currentColor" stroke="none"/><path d="M8.8 15.2h6.4"/>',
  sad:'<circle cx="12" cy="12" r="9"/><circle cx="9" cy="10.6" r="1.1" fill="currentColor" stroke="none"/><circle cx="15" cy="10.6" r="1.1" fill="currentColor" stroke="none"/><path d="M8.2 16q3.8-2.6 7.6 0"/>',
  cry:'<circle cx="12" cy="12" r="9"/><circle cx="9" cy="10.4" r="1.05" fill="currentColor" stroke="none"/><circle cx="15" cy="10.4" r="1.05" fill="currentColor" stroke="none"/><path d="M8.4 16.2q3.6-2.4 7.2 0"/><path d="M8.2 12.4q-1.3 1.9-1.3 2.9a1.05 1.05 0 0 0 2.1 0q0-1-.8-2.9z" fill="currentColor" stroke="none"/><path d="M15.8 12.4q1.3 1.9 1.3 2.9a1.05 1.05 0 0 1-2.1 0q0-1 .8-2.9z" fill="currentColor" stroke="none"/>',
  angry:'<circle cx="12" cy="12" r="9"/><path d="M7.3 9.5l2.6 1.1"/><path d="M16.7 9.5l-2.6 1.1"/><circle cx="9.2" cy="11.8" r="1" fill="currentColor" stroke="none"/><circle cx="14.8" cy="11.8" r="1" fill="currentColor" stroke="none"/><path d="M8.6 16.2q3.4-2.4 6.8 0"/>',
  sleepy:'<circle cx="12" cy="12" r="9"/><path d="M7.6 10.6h2.6"/><path d="M13.8 10.6h2.6"/><circle cx="12" cy="15.4" r="1.2"/><path d="M15.6 5.2h2.9l-2.9 2.9h2.9"/>',
  party:'<circle cx="12" cy="12" r="9"/><path d="M7.6 10.8q1.2-1.2 2.4 0"/><path d="M14 10.8q1.2-1.2 2.4 0"/><path d="M8.4 14.4q3.6 2.6 7.2 0"/><path d="M15.5 3.4l3 4.6-4.7-1.7z" fill="currentColor" stroke="none"/><circle cx="6" cy="5.5" r=".7" fill="currentColor" stroke="none"/><circle cx="19" cy="12" r=".7" fill="currentColor" stroke="none"/><circle cx="4.5" cy="10" r=".6" fill="currentColor" stroke="none"/>',
  wow:'<circle cx="12" cy="12" r="9"/><circle cx="9" cy="10.4" r="1.35"/><circle cx="15" cy="10.4" r="1.35"/><ellipse cx="12" cy="15.2" rx="1.7" ry="2.2"/>',
  kiss:'<circle cx="12" cy="12" r="9"/><circle cx="9" cy="10.6" r="1.05" fill="currentColor" stroke="none"/><path d="M13.8 10.6q1.2-1 2.5 0"/><path d="M10.8 15.1c0-1.1 2.2-1.1 2.2 0s-2.2 1.5-2.2 0z"/><path d="M16 6.5c-.6-.8-1.9-.4-1.9.6 0 .9 1.9 2 1.9 2s1.9-1.1 1.9-2c0-1-1.3-1.4-1.9-.6z" fill="currentColor" stroke="none"/>',
  /* --- Жесты --- */
  'thumb-up':'<path d="M8 19.2h8.3a1.7 1.7 0 0 0 1.7-1.35l1.1-5.5a1.5 1.5 0 0 0-1.5-1.85H13l.95-3.7a1.75 1.75 0 0 0-3.3-1.15L8 10.9z"/><rect x="4.6" y="10.9" width="3.4" height="8.3" rx=".9"/>',
  'thumb-down':'<path d="M8 4.8h8.3a1.7 1.7 0 0 1 1.7 1.35l1.1 5.5a1.5 1.5 0 0 1-1.5 1.85H13l.95 3.7a1.75 1.75 0 0 1-3.3 1.15L8 13.1z"/><rect x="4.6" y="4.8" width="3.4" height="8.3" rx=".9"/>',
  clap:'<path d="M8.2 20l-3.4-3.4a1.5 1.5 0 0 1 2.1-2.1l4.1 4.1"/><path d="M10.5 11.4l-1.9-1.9a1.5 1.5 0 0 1 2.1-2.1l5.3 5.3a1.5 1.5 0 0 1-2.1 2.1"/><path d="M13 6.4l-.8 2"/><path d="M16.5 8l-1.6 1.5"/><path d="M17.6 12l-2 .4"/>',
  pray:'<path d="M11.4 18.8c-1.6-.3-2.4-1.4-2.1-3.4l1-8.8c.14-1.25 2-1.15 2 .1v6.5"/><path d="M12.6 18.8c1.6-.3 2.4-1.4 2.1-3.4l-1-8.8c-.14-1.25-2-1.15-2 .1v6.5"/><path d="M9.6 15.4l4.8 0"/>',
  fist:'<path d="M6.6 11.4a1.3 1.3 0 0 1 1.3-1.3h8.2a1.3 1.3 0 0 1 1.3 1.3v4.4a2.5 2.5 0 0 1-2.5 2.5H9a2.5 2.5 0 0 1-2.4-2.5z"/><path d="M8.8 10.1v-1.3"/><path d="M11.2 10.1v-1.6"/><path d="M13.6 10.1v-1.6"/><path d="M16 10.1v-1.3"/><path d="M6.6 13.6H5.3a1.1 1.1 0 0 0 0 2.2h1.3"/>',
  muscle:'<path d="M5 8.5v1.6a1 1 0 0 0 1 1h2.6a4.5 4.5 0 0 1 4.5 4.5v2.4a2 2 0 0 1-2 2H9.2A4.2 4.2 0 0 1 5 15.8"/><path d="M8.6 11.1a5.3 5.3 0 0 1 5 4.9"/>',
  ok:'<circle cx="8.6" cy="15.2" r="3.5"/><path d="M11.3 12.6l1.5-3.5"/><path d="M12.7 13.4l2.7-2.3"/><path d="M13.4 15l3.1-.8"/>',
  wave:'<path d="M9 20.2a4 4 0 0 1-4-4v-2.8a1 1 0 0 1 2 0V15"/><path d="M7 13.7V8.4a1 1 0 0 1 2 0V14"/><path d="M9 14V6.7a1 1 0 0 1 2 0V14"/><path d="M11 14V7.6a1 1 0 0 1 2 0V15"/><path d="M13 15v-3.1a1 1 0 0 1 2 0v3.7a4 4 0 0 1-4 4"/><path d="M16.3 7.4q1.3 1.1 0 3.1"/>',
  'point-up':'<path d="M12 3.6a1.4 1.4 0 0 1 1.4 1.4v6.5"/><path d="M9.4 11.6h4.4a2 2 0 0 1 2 2v2.6a3 3 0 0 1-3 3h-2.2a3.4 3.4 0 0 1-3.4-3.4v-2.4a1.8 1.8 0 0 1 1.8-1.8z"/><path d="M9.2 14.4H8a1 1 0 0 0 0 2h1.2"/>',
  /* --- Символы --- */
  fire:'<path d="M12 3c1 3.2 4.2 4.3 4.2 8.2a4.2 4.2 0 0 1-8.4 0c0-1.6.75-2.7 1.6-3.2.2 1.05 1.05 1.6 1.6 1.6-1.05-2.1.5-4.8 1-6.4z"/><path d="M12 18a2 2 0 0 1-2-2c0-1 1-1.7 1.2-2.6.4.9 1.1 1.2 1.4 2 .3-.5.6-.8.6-1.4.5.5.8 1.3.8 2a2 2 0 0 1-2 2z" fill="currentColor" stroke="none"/>',
  spark:'<path d="M12 4l1.6 5.4 5.4 1.6-5.4 1.6L12 18l-1.6-5.4L5 11l5.4-1.6z" fill="currentColor" stroke="none"/><path d="M18.6 4.4v2.8"/><path d="M17.2 5.8h2.8"/><path d="M5.6 16v2.6"/><path d="M4.3 17.3h2.6"/>',
  star:'<path d="M12 3.5l2.6 5.3 5.9.9-4.3 4.1 1 5.8L12 17l-5.2 2.6 1-5.8L3.5 9.7l5.9-.9z"/>',
  hundred:'<path d="M4.8 10.2l1.7-1.1v6.1"/><rect x="8" y="9.1" width="3.9" height="6.1" rx="1.95"/><rect x="13.4" y="9.1" width="3.9" height="6.1" rx="1.95"/><path d="M3.7 17.2h14.8"/><path d="M4.6 18.7h13"/>',
  check:'<path d="M4.8 12.4l4.6 4.6L19.2 6.6"/>',
  cross:'<path d="M6 6l12 12"/><path d="M18 6L6 18"/>',
  bolt:'<path d="M13 3l-7 10.4h5l-1 7.6 7-11.2h-5z" fill="currentColor" stroke="none"/>',
  popper:'<path d="M3.8 20.2l4.6-1.8-2.8-2.8z" fill="currentColor" stroke="none"/><path d="M8.4 18.4q3.2-1.7 4.8-5"/><path d="M13.5 6.6l1.4-2.2"/><path d="M16.3 9l2.4-1"/><path d="M15.6 13.2l2.6.4"/><path d="M11.4 5.3l.4-2.3"/><circle cx="18.4" cy="15.4" r=".7" fill="currentColor" stroke="none"/><circle cx="19.4" cy="11.2" r=".6" fill="currentColor" stroke="none"/>',
  trophy:'<path d="M8 4h8v3.2a4 4 0 0 1-8 0z"/><path d="M8 5.2H6a2 2 0 0 0 2 2"/><path d="M16 5.2h2a2 2 0 0 1-2 2"/><path d="M12 11.2v2.6"/><path d="M10 13.8h4v3.2h-4z"/><path d="M8.8 20h6.4"/>',
  medal:'<path d="M9.2 3.2l2.2 5.2"/><path d="M14.8 3.2l-2.2 5.2"/><circle cx="12" cy="15" r="5"/><path d="M12 12.4l.9 1.9 2.1.3-1.5 1.5.35 2.1L12 17.3l-1.85 1-.35-2.1L8.5 14.6l2.1-.3z" fill="currentColor" stroke="none"/>',
  gem:'<path d="M6 5h12l3 4-9 11L3 9z"/><path d="M3 9h18"/><path d="M9 5L6.6 9 12 20"/><path d="M15 5l2.4 4L12 20"/>',
  money:'<path d="M9.2 6h5.6l-1.4 2.4a6 6 0 1 1-2.8 0z"/><path d="M12 12v5"/><path d="M13.7 12.9a2 2 0 0 0-3.1.5c0 1.9 3.1.8 3.1 2.7a2 2 0 0 1-3.1.5"/>',
  rocket:'<path d="M12 3c2.5 2.1 4 5.1 4 8.2l-2 2h-4l-2-2C8 8.1 9.5 5.1 12 3z"/><circle cx="12" cy="9" r="1.5"/><path d="M8 13.4l-2.2 3 3.2-1"/><path d="M16 13.4l2.2 3-3.2-1"/><path d="M10.4 16.2c0 2 1.6 3.4 1.6 3.4s1.6-1.4 1.6-3.4"/>',
  heart:'<path d="M12 20.2S4 14.6 4 9A3.9 3.9 0 0 1 12 6.2 3.9 3.9 0 0 1 20 9c0 5.6-8 11.2-8 11.2z" fill="currentColor" stroke="none"/>',
  'heart-broken':'<path d="M12 20.2S4 14.6 4 9A3.9 3.9 0 0 1 12 6.2 3.9 3.9 0 0 1 20 9c0 5.6-8 11.2-8 11.2z"/><path d="M12 6.4l-1.9 3.1 2.5 2.1-2.1 2.7 1.2 2.4"/>',
  eyes:'<path d="M2.8 11c1.4-1.9 3.2-2.85 4.5-2.85S10.4 9.1 11.8 11c-1.4 1.9-3.2 2.85-4.5 2.85S4.2 12.9 2.8 11z"/><circle cx="7.3" cy="11" r="1.45" fill="currentColor" stroke="none"/><path d="M12.2 11c1.4-1.9 3.2-2.85 4.5-2.85S20.4 9.1 21.8 11c-1.4 1.9-3.2 2.85-4.5 2.85S13.6 12.9 12.2 11z"/><circle cx="16.7" cy="11" r="1.45" fill="currentColor" stroke="none"/>',
  rainbow:'<path d="M3.5 18a8.5 8.5 0 0 1 17 0"/><path d="M6.3 18a5.7 5.7 0 0 1 11.4 0"/><path d="M9.1 18a2.9 2.9 0 0 1 5.8 0"/>',
  sun:'<circle cx="12" cy="12" r="3.9"/><path d="M12 3v2.4"/><path d="M12 18.6V21"/><path d="M3 12h2.4"/><path d="M18.6 12H21"/><path d="M5.6 5.6l1.7 1.7"/><path d="M16.7 16.7l1.7 1.7"/><path d="M18.4 5.6l-1.7 1.7"/><path d="M7.3 16.7l-1.7 1.7"/>',
  moon:'<path d="M18.2 13.6A7 7 0 0 1 9 5.1a7 7 0 1 0 9.2 8.5z"/>',
  crown:'<path d="M4 17l1.6-9 4.1 4.1L12 6l2.3 6.1L18.4 8 20 17z"/><path d="M4.4 17.2h15.2v2.6H4.4z"/>'
};
/* Порядок и группировка для рендера пикера */
const CP_OKO_GROUPS = [
  ['Смайлы', ['happy','laugh','love','cool','wink','tongue','think','neutral','sad','cry','angry','sleepy','party','wow','kiss']],
  ['Жесты', ['thumb-up','thumb-down','clap','pray','fist','muscle','ok','wave','point-up']],
  ['Символы', ['fire','spark','star','hundred','check','cross','bolt','popper','trophy','medal','gem','money','rocket','heart','heart-broken','eyes','rainbow','sun','moon','crown']]
];
/* Единый рендер фирменного SVG-эмодзи (пикер + сообщение). База стиля — на <svg>. */
function cpOkoSvg(key, size){
  const inner = CP_OKO_EMOJI[key]; if(!inner) return '';
  return `<svg viewBox="0 0 24 24" width="${size}" height="${size}" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">${inner}</svg>`;
}
let cpPanelOpen = false;
function cpBuildPanel(){
  const composer = document.querySelector('#convBody .composer');
  if(!composer || document.getElementById('cpSmile')) return;
  /* кнопка-смайл слева */
  const smile = document.createElement('button');
  smile.className = 'tool cp-smile'; smile.id = 'cpSmile'; smile.title = 'Стикеры и эмодзи';
  smile.innerHTML = I('laugh');
  smile.onclick = cpTogglePanel;
  composer.insertBefore(smile, composer.firstChild);
  /* панель над полем ввода (между compose-bar и composer) */
  const panel = document.createElement('div');
  panel.className = 'cp-panel'; panel.id = 'cpPanel';
  const tabs = [['emoji','Эмодзи','laugh'],['stickers','Стикеры','sticker'],['oko','OKO','logo'],['ton','TON','vs-gem']];
  panel.innerHTML =
    `<div class="cp-panel-tabs">${tabs.map(([k,l,ic])=>
      `<button class="cp-tab" data-tab="${k}" onclick="cpPanelTab('${k}')">${cpTabIco(k,ic)}<span>${l}</span></button>`).join('')}</div>
     <div class="cp-panel-body" id="cpPanelBody"></div>`;
  composer.parentNode.insertBefore(panel, composer);
}
function cpTabIco(k, ic){
  if(k==='ton' && typeof vsTonSvg === 'function') return `<span class="cp-tab-ton">${vsTonSvg('vs-gem',18)}</span>`;
  return I(ic);
}
function cpTogglePanel(){ cpPanelOpen ? cpClosePanel() : cpOpenPanel(); }
function cpOpenPanel(){
  const p = document.getElementById('cpPanel'); if(!p) return;
  cpPanelOpen = true; p.classList.add('on');
  document.getElementById('cpSmile') && document.getElementById('cpSmile').classList.add('on');
  const inp = document.getElementById('msgInput'); if(inp) inp.blur(); /* прячем клавиатуру, как в TG */
  cpPanelTab(CP.panelTab || 'emoji');
  if(typeof nvPush==='function') nvPush('cp:panel', ()=>cpClosePanel(true));
}
function cpClosePanel(fromNav){
  const p = document.getElementById('cpPanel'); if(!p || !cpPanelOpen) return;
  cpPanelOpen = false; p.classList.remove('on');
  document.getElementById('cpSmile') && document.getElementById('cpSmile').classList.remove('on');
  if(!fromNav && typeof nvPop==='function') nvPop('cp:panel');
}
function cpPanelTab(tab){
  CP.panelTab = tab; cpSave();
  document.querySelectorAll('#cpPanel .cp-tab').forEach(b=>b.classList.toggle('on', b.dataset.tab===tab));
  const body = document.getElementById('cpPanelBody'); if(!body) return;
  body.scrollTop = 0;
  if(tab==='emoji'){
    try{
      const btn = k => `<button class="cp-emoji cp-emoji-svg" title="${k}" onclick="cpInsertOko('${k}')">${cpOkoSvg(k,28)}</button>`;
      /* недавние (только валидные ключи фирменного набора) */
      const rec = CP.recentOko.filter(k=>CP_OKO_EMOJI[k]);
      const recent = rec.length
        ? `<div class="cp-emoji-sec">Недавние</div><div class="cp-emoji-grid">${rec.map(btn).join('')}</div>` : '';
      const groups = CP_OKO_GROUPS.map(([label,keys])=>
        `<div class="cp-emoji-sec">${label}</div><div class="cp-emoji-grid">${keys.map(btn).join('')}</div>`).join('');
      body.innerHTML = recent + groups;
    }catch(e){ body.innerHTML = `<div class="cp-panel-empty">Эмодзи скоро появятся</div>`; }
    return;
  }
  /* стикер-вкладки: используем ядровые STICKERS/sendSticker/stickerSvg (verify-stickers их расширяет) */
  const list = (typeof STICKERS!=='undefined') ? STICKERS : [];
  const svg = (typeof stickerSvg==='function') ? stickerSvg : (s,z)=>I(s.ic);
  let items = [];
  if(tab==='stickers') items = list.map((s,i)=>({s,i})).filter(x=>!x.s.pack && ['fire','heart','thumb','laugh','wow','sad'].includes(x.s.ic));
  else if(tab==='oko')  items = list.map((s,i)=>({s,i})).filter(x=>!x.s.pack && !['fire','heart','thumb','laugh','wow','sad'].includes(x.s.ic));
  else if(tab==='ton')  items = list.map((s,i)=>({s,i})).filter(x=>x.s.pack==='ton');
  if(!items.length){ body.innerHTML = `<div class="cp-panel-empty">Готовим новый набор — скоро будет здесь</div>`; return; }
  const head = tab==='ton'
    ? `<div class="cp-emoji-sec cp-ton-sec">TON Crystal${(typeof vsPremiumOk==='function' && !vsPremiumOk())?' <b>PRO</b>':''}</div>`
    : tab==='oko' ? `<div class="cp-emoji-sec">Фирменные OKO</div>` : `<div class="cp-emoji-sec">Стикеры</div>`;
  body.innerHTML = head + `<div class="cp-stk-grid">${items.map(({s,i})=>{
    const locked = s.pack==='ton' && typeof vsPremiumOk==='function' && !vsPremiumOk();
    return `<button class="cp-stk ${locked?'cp-stk-lock':''}" onclick="cpSendStk(${i})">${svg(s,60)}${
      locked?`<span class="cp-stk-lockic">${I('lock')}</span>`:''}<small>${s.label}</small></button>`;
  }).join('')}</div>`;
}
/* Вставка фирменного эмодзи: в поле уходит шорткод :oko-KEY:, который при рендере
   сообщения превращается обратно в брендовый SVG (переживает отправку/приём). */
function cpInsertOko(key){
  try{
    if(!CP_OKO_EMOJI[key]) return;
    const inp = document.getElementById('msgInput'); if(!inp) return;
    const token = ':oko-'+key+':';
    const s = inp.selectionStart ?? inp.value.length, en = inp.selectionEnd ?? inp.value.length;
    inp.value = inp.value.slice(0,s) + token + inp.value.slice(en);
    const pos = s + token.length; try{ inp.setSelectionRange(pos,pos); inp.focus(); }catch(_){}
    CP.recentOko = [key, ...CP.recentOko.filter(x=>x!==key)].slice(0,24); cpSave();
    if(typeof syncSendIcon==='function') syncSendIcon();
  }catch(e){}
}
/* Замена шорткодов :oko-KEY: на инлайн-SVG в готовом HTML сообщения. */
function cpRenderOkoTokens(html){
  if(typeof html!=='string' || html.indexOf(':oko-')<0) return html;
  return html.replace(/:oko-([a-z0-9-]+):/g, (m,k)=>
    CP_OKO_EMOJI[k] ? `<span class="cp-oko-emoji" title="${k}">${cpOkoSvg(k,20)}</span>` : m);
}
/* Chain-патч ядрового msgHtml: прогоняем результат через замену шорткодов. */
if(typeof msgHtml === 'function'){
  const _cpPrevMsgHtml = msgHtml;
  msgHtml = function(){
    let out = _cpPrevMsgHtml.apply(this, arguments);
    try{ out = cpRenderOkoTokens(out); }catch(e){}
    return out;
  };
}
/* Обратная совместимость: старый вызов из ядра/др. модулей не должен падать. */
function cpInsertEmoji(e){
  try{
    const inp = document.getElementById('msgInput'); if(!inp) return;
    const s = inp.selectionStart ?? inp.value.length, en = inp.selectionEnd ?? inp.value.length;
    inp.value = inp.value.slice(0,s) + e + inp.value.slice(en);
    const pos = s + e.length; try{ inp.setSelectionRange(pos,pos); }catch(_){}
    CP.recentEmoji = [e, ...CP.recentEmoji.filter(x=>x!==e)].slice(0,24); cpSave();
    if(typeof syncSendIcon==='function') syncSendIcon();
  }catch(_){}
}
function cpSendStk(i){
  if(typeof sendSticker==='function') sendSticker(i); /* verify-stickers сам гейтит TON + дождь кристаллов */
  /* панель оставляем открытой — как в Telegram */
}

/* ================= 8. ГОЛОСОВЫЕ: волна с прогрессом + кружок-плеер ================= */
function cpVoiceProg(voice, p){
  if(!voice) return;
  voice.style.setProperty('--cp-vp', p);
  const bars = voice.querySelectorAll('.wave i'); const n = bars.length; const k = Math.round(p*n);
  bars.forEach((b,i)=>b.classList.toggle('cp-on', i<k));
}
function cpEnhanceVoicePlay(btn){
  const voice = btn.closest('.voice'); if(!voice) return;
  const use = btn.querySelector('use');
  const playing = use && use.getAttribute('href')==='#i-pause';
  voice.classList.toggle('cp-playing', playing);
  if(btn._audio && !btn._audio._cpB){
    btn._audio._cpB = 1;
    btn._audio.addEventListener('timeupdate', ()=>cpVoiceProg(voice, btn._audio.currentTime/(btn._audio.duration||1)));
    btn._audio.addEventListener('play',  ()=>voice.classList.add('cp-playing'));
    btn._audio.addEventListener('pause', ()=>voice.classList.remove('cp-playing'));
    btn._audio.addEventListener('ended', ()=>{ voice.classList.remove('cp-playing'); cpVoiceProg(voice,0); });
  }
}
if(typeof togglePlay === 'function'){
  const _cpPrevTogglePlay2 = togglePlay;
  togglePlay = function(btn){ _cpPrevTogglePlay2.apply(this, arguments); if(btn) cpEnhanceVoicePlay(btn); };
}
/* круглый плеер видео-кружка: кольцо прогресса, центр-иконка, скорость */
function cpDecorateVnotes(){
  const el = cpMsgsEl(); if(!el) return;
  el.querySelectorAll('.vnote').forEach(v=>{
    if(v._cpVn) return; v._cpVn = 1;
    if(!v.querySelector('.cp-vn-ring')){
      v.insertAdjacentHTML('afterbegin',
        `<svg class="cp-vn-ring" viewBox="0 0 100 100"><circle class="cp-vn-tr" cx="50" cy="50" r="47.5"/><circle class="cp-vn-fl" cx="50" cy="50" r="47.5"/></svg>`);
    }
    const vid = v.querySelector('video');
    const fl = v.querySelector('.cp-vn-fl'); const C = 2*Math.PI*47.5;
    if(fl){ fl.style.strokeDasharray = C; fl.style.strokeDashoffset = C; }
    /* без реального видео — это премиум-постер (анимированное лаймовое кольцо), а не пустышка */
    if(!vid) v.classList.add('cp-vn-poster');
    if(!v.querySelector('.cp-vn-play')) v.insertAdjacentHTML('beforeend', `<span class="cp-vn-play">${I('play')}</span>`);
    /* пилюля скорости — на любом кружке (и на постере): читается как готовый элемент */
    if(!v.querySelector('.cp-vn-speed')){
      const b = document.createElement('button'); b.className = 'cp-vn-speed'; b.textContent = cpSpeedLabel();
      b.onclick = e=>{ e.stopPropagation(); cpCycleSpeed(e); };
      v.appendChild(b);
    }
    if(vid){
      vid.addEventListener('timeupdate', ()=>{ if(fl){ const p = vid.currentTime/(vid.duration||1); fl.style.strokeDashoffset = C*(1-p); } });
      vid.addEventListener('play',  ()=>{ try{ vid.playbackRate = CP.speed; }catch(_){} v.classList.add('cp-vn-playing'); });
      vid.addEventListener('pause', ()=>v.classList.remove('cp-vn-playing'));
      vid.addEventListener('ended', ()=>{ v.classList.remove('cp-vn-playing'); if(fl) fl.style.strokeDashoffset = C; });
    }
  });
}

/* ================= 9. ЗАПИСЬ: кружок/пульс/таймер + отмена свайпом ================= */
let cpRecMoveH = null, cpRecStartX = null, cpRecStartY = null, cpRecArmed = false, cpRecPvTry = null, cpRecLocked = false;
const CP_REC_BARS = 34;
const CP_LOCK_DIST = 96;   /* свайп вверх до блокировки записи (как в Telegram) */
/* живая волна записи: реальная амплитуда микрофона через Web Audio (как в Telegram) */
let cpRecAC = null, cpRecAn = null, cpRecRAF = 0, cpRecStreamHooked = null;
function cpRecWaveStart(stream){
  if(!stream || cpRecStreamHooked === stream) return;
  const wave = document.getElementById('cpRecWave'); if(!wave) return;
  const AC = window.AudioContext || window.webkitAudioContext; if(!AC) return;
  try{
    cpRecWaveStop();
    cpRecStreamHooked = stream;
    cpRecAC = new AC();
    if(cpRecAC.state === 'suspended') cpRecAC.resume().catch(()=>{});
    const src = cpRecAC.createMediaStreamSource(stream);
    cpRecAn = cpRecAC.createAnalyser();
    cpRecAn.fftSize = 128; cpRecAn.smoothingTimeConstant = 0.72;
    src.connect(cpRecAn);
    const bins = new Uint8Array(cpRecAn.frequencyBinCount);
    const bars = Array.prototype.slice.call(wave.children);
    wave.classList.add('cp-rec-wave-live');
    const half = bars.length / 2;
    /* голос живёт в нижних/средних бинах — растягиваем полезный диапазон на пол-волны */
    const usable = Math.max(4, Math.min(cpRecAn.frequencyBinCount - 1, 30));
    const span = Math.ceil(half);
    const loop = ()=>{
      if(!cpRecAn){ return; }
      cpRecAn.getByteFrequencyData(bins);
      for(let i=0; i<bars.length; i++){
        /* симметрично зеркалим спектр от центра наружу: центр — низ/середина (громче) */
        const d = Math.abs(i + 0.5 - half);
        const b0 = 1 + Math.floor(d / span * usable);
        /* берём максимум по окну из 2 бинов — без «мёртвых» полос на чистых тонах */
        const raw = Math.max(bins[b0] || 0, bins[b0 + 1] || 0) / 255;
        const h = Math.max(0.12, Math.min(1, Math.pow(raw, 0.7) * 1.4));
        bars[i].style.transform = 'scaleY(' + h.toFixed(3) + ')';
      }
      cpRecRAF = requestAnimationFrame(loop);
    };
    cpRecRAF = requestAnimationFrame(loop);
  }catch(_){ cpRecStreamHooked = null; }
}
function cpRecWaveStop(){
  if(cpRecRAF){ cancelAnimationFrame(cpRecRAF); cpRecRAF = 0; }
  cpRecAn = null;
  if(cpRecAC){ try{ cpRecAC.close(); }catch(_){} cpRecAC = null; }
  cpRecStreamHooked = null;
  const wave = document.getElementById('cpRecWave');
  if(wave){
    wave.classList.remove('cp-rec-wave-live');
    Array.prototype.forEach.call(wave.children, b=>{ b.style.transform = ''; });
  }
}
function cpRecOverlay(){
  let o = document.getElementById('cpRec');
  if(o) return o;
  o = document.createElement('div'); o.id = 'cpRec'; o.className = 'cp-rec';
  o.innerHTML =
    `<div class="cp-rec-lock" id="cpRecLock"><span class="cp-rec-lock-ch">${I('chev')}</span><span class="cp-rec-lock-ic">${I('lock')}</span></div>
     <div class="cp-rec-cancel" id="cpRecCancel"><span class="cp-rec-trash">${I('trash')}</span></div>
     <button class="cp-rec-send" id="cpRecSend" title="Отправить">${I('send')}</button>
     <div class="cp-rec-stage">
       <div class="cp-rec-ring"></div><div class="cp-rec-ring cp-rec-ring2"></div>
       <div class="cp-rec-bubble" id="cpRecBubble">
         <video id="cpRecVid" playsinline muted></video>
         <span class="cp-rec-mic">${I('mic')}</span>
       </div>
     </div>
     <div class="cp-rec-wave" id="cpRecWave">${
        Array.from({length: CP_REC_BARS}, ()=>'<i></i>').join('')}</div>
     <div class="cp-rec-info"><span class="cp-rec-dot"></span><span id="cpRecTime">0:00</span>
       <span class="cp-rec-hint"><span class="cp-rec-arrow">${I('chev')}</span>Смахните влево — отмена, вверх — закрепить</span>
       <span class="cp-rec-hint cp-rec-hint-locked">Запись закреплена</span></div>`;
  document.body.appendChild(o);
  const cancelBtn = o.querySelector('#cpRecCancel');
  if(cancelBtn) cancelBtn.onclick = ()=>{ cpCancelRec(); };
  const sendBtn = o.querySelector('#cpRecSend');
  if(sendBtn) sendBtn.onclick = ()=>{ cpRecSendLocked(); };
  return o;
}
function cpRecShow(){
  const isV = (typeof recMode!=='undefined' && recMode==='vnote');
  const o = cpRecOverlay();
  o.classList.toggle('cp-rec-video', isV);
  o.classList.remove('cp-rec-locked','cp-rec-arm');
  o.style.removeProperty('--cp-cx'); o.style.removeProperty('--cp-lk');
  o.classList.add('on'); cpRecArmed = false; cpRecLocked = false; cpRecStartX = null; cpRecStartY = null;
  const vid = document.getElementById('cpRecVid');
  if(vid){ vid.srcObject = null; vid.style.display = isV ? 'block' : 'none'; }
  /* сброс волны в холостой режим (пока микрофон не подключился / нет доступа) */
  cpRecWaveStop();
  /* recStream появляется асинхронно после getUserMedia — ждём и цепляем:
     video-режим -> живое превью камеры; voice-режим -> живая волна микрофона */
  clearInterval(cpRecPvTry);
  {
    let tries = 0;
    cpRecPvTry = setInterval(()=>{
      tries++;
      if(typeof recStream!=='undefined' && recStream){
        if(isV && vid){ try{ vid.srcObject = recStream; vid.play&&vid.play().catch(()=>{}); }catch(_){} }
        else if(!isV){ cpRecWaveStart(recStream); }
        clearInterval(cpRecPvTry);
      }
      if(tries>50) clearInterval(cpRecPvTry);
    }, 60);
  }
  /* таймер зеркалим из ядрового #recTime */
  cpRecTick();
  clearInterval(cpRecTimeInt);
  cpRecTimeInt = setInterval(cpRecTick, 200);
  /* отмена свайпом: слушаем на кнопке записи (у неё pointer capture) */
  const sb = cpRecBtn();
  if(sb && !cpRecMoveH){
    cpRecMoveH = e=>{
      if(cpRecLocked) return;
      if(typeof recStart==='undefined' || !recStart) return;
      if(cpRecStartX===null){ cpRecStartX = e.clientX; cpRecStartY = e.clientY; }
      const dx = e.clientX - cpRecStartX;
      const dy = e.clientY - cpRecStartY;
      /* вверх — закрепление записи (руки свободны); влево — отмена. Ведущая ось решает жест. */
      if(dy < -18 && -dy > Math.abs(dx)){
        const t = Math.min(1, -dy / CP_LOCK_DIST);
        o.style.setProperty('--cp-lk', t.toFixed(3));
        o.style.removeProperty('--cp-cx');
        o.classList.remove('cp-rec-arm'); cpRecArmed = false;
        if(t >= 1) cpRecLock();
        return;
      }
      if(dx < 0){
        const t = Math.min(1, -dx/120);
        o.style.setProperty('--cp-cx', (-dx*0.35)+'px');
        o.style.setProperty('--cp-lk', '0');
        o.classList.toggle('cp-rec-arm', t>=1);
        cpRecArmed = t>=1;
        if(cpRecArmed) cpCancelRec();
      }
    };
    sb.addEventListener('pointermove', cpRecMoveH);
  }
}
/* Кнопка, на которой физически идёт запись. С 09.08 это отдельная #micBtn
   (раньше запись и отправка жили на одной #sendBtn). Хелпер держит модуль
   chats-plus совместимым с обоими вариантами разметки. */
function cpRecBtn(){ return document.getElementById('micBtn') || document.getElementById('sendBtn'); }
let cpRecTimeInt = null;
function cpRecTick(){
  const src = document.getElementById('recTime'), dst = document.getElementById('cpRecTime');
  if(src && dst) dst.textContent = src.textContent || '0:00';
}
function cpRecHide(){
  const o = document.getElementById('cpRec'); if(o){ o.classList.remove('on','cp-rec-arm','cp-rec-locked'); o.style.removeProperty('--cp-cx'); o.style.removeProperty('--cp-lk'); }
  clearInterval(cpRecTimeInt); clearInterval(cpRecPvTry);
  cpRecWaveStop();
  const vid = document.getElementById('cpRecVid'); if(vid){ try{ vid.pause(); }catch(_){} vid.srcObject = null; }
  const sb = cpRecBtn();
  if(sb && cpRecMoveH){ sb.removeEventListener('pointermove', cpRecMoveH); cpRecMoveH = null; }
  cpRecStartX = null; cpRecStartY = null; cpRecArmed = false; cpRecLocked = false;
}
/* блокировка записи (свайп вверх): запись продолжается без удержания пальца.
   Ядровой pointerup на #sendBtn перехватываем в capture-фазе, чтобы он не вызвал stopRec. */
function cpRecLock(){
  if(cpRecLocked) return;
  cpRecLocked = true;
  const o = document.getElementById('cpRec');
  if(o){ o.classList.add('cp-rec-locked'); o.classList.remove('cp-rec-arm'); o.style.removeProperty('--cp-cx'); o.style.setProperty('--cp-lk','1'); }
  if(typeof toast==='function') toast('Запись закреплена — можно отпустить');
}
function cpRecSendLocked(){
  if(!cpRecLocked) return;
  cpRecLocked = false;
  if(typeof recStart!=='undefined' && recStart && typeof stopRec==='function') stopRec(); /* ядро сформирует и отправит голосовое/кружок */
  else cpRecHide();
}
/* один глобальный перехватчик: пока запись закреплена — гасим ядровой pointerup, чтобы не остановил запись */
if(!window._cpRecUpHook){
  window._cpRecUpHook = 1;
  document.addEventListener('pointerup', e=>{ if(cpRecLocked) e.stopImmediatePropagation(); }, true);
}
/* аккуратная отмена без правки ядра: сбрасываем recStart -> ядровой pointerup
   не вызовет stopRec (там `if(recStart) stopRec()`), а хвосты чистим сами. */
function cpCancelRec(){
  cpRecLocked = false;
  if(typeof recStart==='undefined' || !recStart){ cpRecHide(); return; }
  try{ if(typeof recInt!=='undefined') clearInterval(recInt); }catch(_){}
  try{
    if(typeof mediaRec!=='undefined' && mediaRec && mediaRec.state && mediaRec.state!=='inactive'){
      mediaRec.onstop = ()=>{}; mediaRec.stop();
    }
  }catch(_){}
  try{ if(typeof recStream!=='undefined' && recStream){ recStream.getTracks().forEach(t=>t.stop()); recStream = null; } }catch(_){}
  try{ if(typeof mediaRec!=='undefined') mediaRec = null; }catch(_){}
  recStart = null;
  const mi = document.getElementById('msgInput'); if(mi) mi.style.display = '';
  const rt = document.getElementById('recTimer'); if(rt) rt.style.display = 'none';
  const pv = document.getElementById('recPreview'); if(pv){ pv.style.display = 'none'; pv.srcObject = null; }
  const sb = cpRecBtn(); if(sb) sb.classList.remove('rec');
  cpRecHide();
  if(typeof syncSendIcon==='function') syncSendIcon();
  if(typeof toast==='function') toast('Запись отменена');
}
if(typeof startRec === 'function'){
  const _cpPrevStartRec = startRec;
  startRec = function(){ _cpPrevStartRec.apply(this, arguments); cpRecShow(); };
}
if(typeof stopRec === 'function'){
  const _cpPrevStopRec = stopRec;
  stopRec = function(){ _cpPrevStopRec.apply(this, arguments); cpRecHide(); };
}

/* ================= 10. ЭКРАН ЗВОНКА (богатый, обе темы) ================= */
let cpCallStream = null, cpCallPillInt = null;
function cpBuildCall(){
  const scr = document.getElementById('callScreen'); if(!scr || scr._cpDone) return; scr._cpDone = 1;
  /* кнопка сворачивания */
  if(!document.getElementById('cpCallMin')){
    const mn = document.createElement('button');
    mn.className = 'cp-call-min'; mn.id = 'cpCallMin'; mn.title = 'Свернуть';
    mn.innerHTML = I('chev'); mn.onclick = cpCallMin;
    scr.insertBefore(mn, scr.firstChild);
  }
  /* self-preview video внутри рамки */
  const self = document.getElementById('callSelf');
  if(self && !self.querySelector('.cp-self-vid')){
    const v = document.createElement('video'); v.className = 'cp-self-vid'; v.id = 'cpSelfVid';
    v.playsInline = true; v.muted = true; self.insertBefore(v, self.firstChild);
  }
  /* подписи под контролами */
  const labels = {callMic:'Микрофон', callCam:'Камера', callSpk:'Динамик'};
  Object.entries(labels).forEach(([id,l])=>{
    const b = document.getElementById(id);
    if(b && !b.querySelector('.cp-cl')){ const s = document.createElement('span'); s.className = 'cp-cl'; s.textContent = l; b.appendChild(s); }
  });
  const end = scr.querySelector('.call-btn.end');
  if(end && !end.querySelector('.cp-cl')){ const s = document.createElement('span'); s.className = 'cp-cl cp-cl-end'; s.textContent = 'Сброс'; end.appendChild(s); }
  /* звуковые кольца вокруг аватара */
  const ava = document.getElementById('callAva');
  if(ava && !ava.parentNode.querySelector('.cp-call-aura')){
    ava.insertAdjacentHTML('beforebegin', `<span class="cp-call-aura"></span><span class="cp-call-aura cp-call-aura2"></span>`);
  }
  /* плашка свёрнутого звонка */
  if(!document.getElementById('cpCallPill')){
    const pill = document.createElement('button');
    pill.className = 'cp-callpill'; pill.id = 'cpCallPill';
    pill.innerHTML = `<span class="cp-callpill-ic">${I('phone')}</span><span class="cp-callpill-tx"><b id="cpPillName">Звонок</b><small id="cpPillTime">00:00</small></span><span class="cp-callpill-open">${I('chev')}</span>`;
    pill.onclick = cpCallRestore;
    document.body.appendChild(pill);
  }
}
function cpCallEnhance(video){
  const ava = document.getElementById('callAva');
  const self = document.getElementById('callSelf');
  const selfV = document.getElementById('cpSelfVid');
  cpCallStopStream();
  if(video && self){
    self.style.display = 'flex';
    if(navigator.mediaDevices && navigator.mediaDevices.getUserMedia){
      navigator.mediaDevices.getUserMedia({video:{facingMode:'user'}, audio:false}).then(st=>{
        if(!document.getElementById('callScreen').classList.contains('open')){ st.getTracks().forEach(t=>t.stop()); return; }
        cpCallStream = st;
        if(selfV){ selfV.srcObject = st; selfV.style.display = 'block'; selfV.play&&selfV.play().catch(()=>{}); }
      }).catch(()=>{ if(selfV) selfV.style.display = 'none'; });
    }
  } else if(selfV){ selfV.style.display = 'none'; }
}
function cpCallStopStream(){
  if(cpCallStream){ try{ cpCallStream.getTracks().forEach(t=>t.stop()); }catch(_){} cpCallStream = null; }
  const selfV = document.getElementById('cpSelfVid'); if(selfV){ selfV.srcObject = null; }
}
function cpCallMin(){
  const scr = document.getElementById('callScreen'); if(!scr) return;
  scr.classList.remove('open');
  const pill = document.getElementById('cpCallPill'); if(!pill) return;
  const nm = document.getElementById('cpPillName'); if(nm) nm.textContent = (document.getElementById('callName')||{}).textContent || 'Звонок';
  pill.classList.add('on');
  clearInterval(cpCallPillInt);
  const upd = ()=>{ const t = document.getElementById('cpPillTime');
    if(t && typeof callSec!=='undefined'){ const m=String(Math.floor(callSec/60)).padStart(2,'0'), s=String(callSec%60).padStart(2,'0'); t.textContent = m+':'+s; } };
  upd(); cpCallPillInt = setInterval(upd, 1000);
}
function cpCallRestore(){
  const scr = document.getElementById('callScreen'); if(scr) scr.classList.add('open');
  const pill = document.getElementById('cpCallPill'); if(pill) pill.classList.remove('on');
  clearInterval(cpCallPillInt);
}
function cpCallHidePill(){ const pill = document.getElementById('cpCallPill'); if(pill) pill.classList.remove('on'); clearInterval(cpCallPillInt); }
/* меняем иконку микрофона при выключении (выверенный набор: mic <-> перечёркнут через CSS) */
if(typeof startCall === 'function'){
  const _cpPrevStartCall = startCall;
  startCall = function(video){ cpBuildCall(); _cpPrevStartCall.apply(this, arguments); cpCallEnhance(video); };
}
if(typeof endCall === 'function'){
  const _cpPrevEndCall = endCall;
  endCall = function(){ _cpPrevEndCall.apply(this, arguments); cpCallStopStream(); cpCallHidePill(); };
}

/* ================= РАСШИРЕНИЕ РЕНДЕРА И ИНИЦИАЛИЗАЦИИ ================= */
if(typeof renderMsgs === 'function'){
  const _cpPrevRenderMsgs2 = renderMsgs;
  renderMsgs = function(){
    _cpPrevRenderMsgs2.apply(this, arguments);
    cpDecorateVnotes();
    cpInitMenuAnchor();
  };
}
if(typeof openConv === 'function'){
  const _cpPrevOpenConv2 = openConv;
  openConv = function(){ cpClosePanel(); _cpPrevOpenConv2.apply(this, arguments); cpBuildPanel(); cpInitMenuAnchor(); };
}
if(typeof closeConv === 'function'){
  const _cpPrevCloseConv2 = closeConv;
  closeConv = function(){ cpClosePanel(); _cpPrevCloseConv2.apply(this, arguments); };
}
/* при отправке текста — прячем панель (как TG после ввода) */
if(typeof sendText === 'function'){
  const _cpPrevSendText = sendText;
  sendText = function(){ const had = document.getElementById('msgInput'); _cpPrevSendText.apply(this, arguments); /* панель НЕ трогаем для стикеров, только для текста */ };
}
(function cpInit2(){
  cpBuildPanel();
  cpBuildCall();
  cpInitMenuAnchor();
  if(typeof currentChat !== 'undefined' && currentChat) cpDecorateVnotes();
})();

/* =======================================================================
   ЧАСТЬ 3 — ОТКРЫТЫЕ / ПРИВАТНЫЕ / ПЛАТНЫЕ ЧАТЫ (префикс cp-)
   Тип чата: public (открыт, виден в поиске) | private (по ссылке-приглашению) |
   paid (доступ по подписке). Бейджи/замки в списке и шапке, paywall через
   walletCharge, комиссия OKO 10% через okoEarn, персист в localStorage.
   ======================================================================= */
const CP_FEE = 0.10;

function cpAcc(c){
  if(!c) return {type:'public', price:0};
  const ov = CP.access[c.id];
  const type  = ov && ov.type ? ov.type : (c.cpType || 'public');
  const price = ov ? (ov.price||0) : (c.cpPrice||0);
  return {type, price};
}
function cpCanManage(){ return typeof isOwner==='function' && isOwner(); }
function cpChatUnlocked(c){ return !!(c && CP.unlocked[c.id]); }
function cpChatLocked(c){ const a = cpAcc(c); return a.type==='paid' && !cpChatUnlocked(c); }
function cpChatSlug(c){ return String((c&&(c.nick||c.name))||'chat').toLowerCase().replace(/[^a-zа-я0-9_]/gi,'').replace(/ё/g,'е').slice(0,18) || 'chat'; }

/* Стартовых демо-чатов больше нет (правка Даниэля 09.08).
   Раньше в список добавлялись «OKO PRO · Закрытый клуб» и «Приватная комната»
   с выдуманной перепиской. Платные клубы и приватные комнаты человек создаёт
   сам — вся механика их отображения ниже осталась нетронутой. */
function cpSeedChats(){ /* пусто: демо-чаты удалены */ }

/* ---- бейджи/замки в списке чатов ---- */
function cpPaintChatBadges(){
  const list = document.getElementById('chatList'); if(!list) return;
  (typeof CHATS!=='undefined'?CHATS:[]).forEach(c=>{
    const a = cpAcc(c);
    if(a.type==='public') return;
    const sel = (typeof c.id==='number') ? 'openConv('+c.id+')' : "openConv('"+c.id+"')";
    const item = list.querySelector('.chat-item[onclick="'+sel+'"]'); if(!item) return;
    const locked = a.type==='paid' && !cpChatUnlocked(c);
    item.classList.toggle('cp-ci-locked', locked);
    /* угловой значок на аватарке */
    const ava = item.querySelector('.ava');
    if(ava){
      let badge = ava.querySelector('.cp-ci-badge');
      if(!badge){ badge = document.createElement('span'); badge.className='cp-ci-badge'; ava.appendChild(badge); }
      badge.className = 'cp-ci-badge cp-ci-'+(locked?'lock':(a.type==='paid'?'ok':a.type));
      badge.innerHTML = a.type==='paid' ? (cpChatUnlocked(c)?I('check'):I('lock')) : I('lock');
    }
    /* ценовой пилл в строке превью */
    const rows = item.querySelectorAll('.row1');
    const row2 = rows[1];
    if(row2){
      let pill = row2.querySelector('.cp-ci-pill');
      if(a.type==='paid' && !cpChatUnlocked(c)){
        if(!pill){ pill = document.createElement('span'); pill.className='cp-ci-pill'; row2.appendChild(pill); }
        pill.innerHTML = I('lock') + (a.price?a.price+' ₽':'PRO');
      } else if(pill){ pill.remove(); }
    }
  });
}

/* ---- paywall для платного чата ---- */
function cpPaywall(c){
  const a = cpAcc(c);
  const fee = Math.round(a.price*CP_FEE), net = a.price - fee;
  if(typeof showPopup!=='function'){ if(typeof toast==='function') toast('Нужен платный доступ'); return; }
  showPopup({ico:'lock', title:'Доступ для своих',
    body:`Внутри «${cpEsc(c.name)}» — контент только для своих.<br><br>Разовый платёж <b>${a.price} ₽</b> открывает доступ навсегда — без подписок и продлений. Автору — ${net} ₽, комиссия OKO всего 10% (${fee} ₽).`,
    actions:[
      {label:'Открыть доступ · '+a.price+' ₽', onclick:()=>cpBuyAccess(c)},
      {label:'Позже', ghost:true}
    ]});
}
function cpBuyAccess(c){
  const a = cpAcc(c);
  if(typeof walletCharge!=='function'){ if(typeof toast==='function') toast('Кошелёк недоступен'); return; }
  if(!walletCharge(a.price, 'Доступ к чату: '+c.name)) return; /* сам покажет «недостаточно средств» */
  if(typeof okoEarn==='function') okoEarn(a.price*CP_FEE, 'Комиссия платных чатов 10%');
  CP.unlocked[c.id] = 1; cpSave();
  if(typeof toast==='function') toast('Доступ открыт');
  if(typeof renderChatList==='function') renderChatList(((document.getElementById('chatSearch')||{}).value)||'');
  if(typeof openConv==='function') openConv(c.id);
}

/* ---- шапка чата: бейдж типа + кнопка «доступ» ---- */
function cpDecorateConvAccess(){
  const c = (typeof currentChat!=='undefined') ? currentChat : null;
  const head = document.querySelector('#convBody .conv-head'); if(!head || !c) return;
  const a = cpAcc(c);
  /* доступ к чату живёт в ⋮-меню (см. cpMoreItems) — отдельной кнопки в шапке нет,
     чтобы не тесать имя. Здесь — только инлайн-бейдж типа рядом с именем. */
  const nameEl = document.getElementById('convName');
  if(nameEl){
    let badge = document.getElementById('cpConvBadge');
    if(a.type==='public'){
      /* публичный чат: без бейджа — возвращаем чистое имя (нативный ellipsis .who) */
      if(badge) badge.remove();
      nameEl.classList.remove('cp-has-badge');
      const tx = nameEl.querySelector('.cp-name-txt');
      if(tx){ nameEl.textContent = tx.textContent; } /* разворачиваем обёртку обратно в текст */
    } else {
      /* имя оборачиваем в span, чтобы ellipsis работал рядом с несжимаемым бейджем */
      let tx = nameEl.querySelector('.cp-name-txt');
      if(!tx){
        const name = c.name || nameEl.textContent || '';
        nameEl.textContent = '';
        tx = document.createElement('span'); tx.className = 'cp-name-txt'; tx.textContent = name;
        nameEl.appendChild(tx);
      } else if(tx.textContent !== (c.name||tx.textContent)){ tx.textContent = c.name; }
      if(!badge){ badge = document.createElement('span'); badge.id='cpConvBadge'; }
      if(badge.parentNode !== nameEl) nameEl.appendChild(badge);
      nameEl.classList.add('cp-has-badge');
      /* компактный бейдж в шапке: платный-закрытый — цена; открытый/приватный — только иконка */
      const label = a.type==='paid' ? (cpChatUnlocked(c) ? '' : (a.price?a.price+' ₽':'PRO')) : '';
      badge.className = 'cp-conv-badge cp-cb-'+a.type + (label?'':' cp-cb-ico');
      badge.innerHTML = (a.type==='paid'?I('crown'):I('lock')) + (label?'<span>'+label+'</span>':'');
    }
  }
}
function cpAccessMenu(){
  const c = (typeof currentChat!=='undefined') ? currentChat : null; if(!c) return;
  if(cpCanManage()) cpManage(c); else cpShareInvite(c);
}

/* ---- ссылка-приглашение (приватные/платные) ---- */
function cpShareInvite(c){
  if(typeof showPopup!=='function') return;
  const link = 'https://okoteam.top/join/'+cpChatSlug(c);
  showPopup({ico:'share', title:'Ссылка-приглашение',
    body:`Отправь ссылку — по ней откроется доступ к «${cpEsc(c.name)}».<div class="cp-invite"><input id="cpInviteInp" readonly value="${link}"><button class="btn sm" onclick="cpCopyInvite()">${I('copy')} Копировать</button></div>`,
    actions:[{label:'Готово'}]});
}
window.cpCopyInvite = function(){
  const i = document.getElementById('cpInviteInp');
  if(i){ i.select && i.select(); try{ navigator.clipboard && navigator.clipboard.writeText(i.value); }catch(e){} }
  if(typeof toast==='function') toast('Ссылка скопирована');
};

/* ---- управление доступом (владелец) ---- */
let cpMg = null;
function cpManage(c){
  const a = cpAcc(c);
  cpMg = {id:c.id, type:a.type, price:a.price||490};
  cpManageRender();
}
function cpMgReadPrice(){
  if(!cpMg) return;
  const p = document.getElementById('cpMgPrice');
  if(p){ const v = +String(p.value).replace(/\D/g,''); if(v) cpMg.price = v; }
}
function cpManageRender(){
  const d = cpMg; if(!d || typeof showPopup!=='function') return;
  const opt = (t,ic,l,s)=>`<button class="cp-mg-opt ${d.type===t?'on':''}" onclick="cpMgSet('${t}')">${I(ic)}<span class="cp-mg-t"><b>${l}</b><small>${s}</small></span>${d.type===t?I('check'):''}</button>`;
  const body = `<div class="cp-mg">
    ${opt('public','users','Открытый','Виден всем и в поиске')}
    ${opt('private','lock','Приватный','Только по ссылке-приглашению')}
    ${opt('paid','crown','Платный','Зарабатывай на закрытом доступе, комиссия OKO 10%')}
    ${d.type==='paid' ? `<div class="cp-mg-price"><label>Цена доступа</label><div class="cp-mg-prow"><input id="cpMgPrice" inputmode="numeric" value="${d.price}" oninput="cpMgPriceInput()"><span>₽</span></div><div class="cp-mg-net">Тебе — <b id="cpMgNet">${d.price-Math.round(d.price*CP_FEE)} ₽</b> с каждой продажи, OKO удержит 10%</div></div>` : ''}
  </div>`;
  showPopup({ico:'lock', title:'Доступ к чату', body, actions:[
    {label:'Сохранить', onclick:cpMgSave},
    {label:'Отмена', ghost:true, onclick:()=>{ cpMg=null; }}
  ]});
}
window.cpMgSet = function(t){ cpMgReadPrice(); if(cpMg){ cpMg.type = t; cpManageRender(); } };
window.cpMgPriceInput = function(){
  const p = document.getElementById('cpMgPrice'); if(!p || !cpMg) return;
  const v = +String(p.value).replace(/\D/g,'')||0; cpMg.price = v;
  const net = document.getElementById('cpMgNet'); if(net) net.textContent = (v-Math.round(v*CP_FEE))+' ₽';
};
function cpMgSave(){
  if(!cpMg) return;
  cpMgReadPrice();
  const d = cpMg;
  CP.access[d.id] = {type:d.type, price:d.type==='paid' ? (d.price||0) : 0};
  if(d.type==='paid' && cpCanManage()) CP.unlocked[d.id] = 1; /* владелец не платит за свой чат */
  cpSave(); cpMg = null;
  if(typeof toast==='function') toast('Настройки доступа сохранены');
  if(typeof renderChatList==='function') renderChatList(((document.getElementById('chatSearch')||{}).value)||'');
  cpDecorateConvAccess();
}

/* ---- шапка чата: кнопки звонков (правка Даниэля 29.07) ----
   Личные 1-на-1: убираем аудио- и видеокнопки — навязчиво и не по делу.
   Группы / супергруппы / каналы: одна кнопка «Начать созвон» (конференция как Zoom),
   видна только владельцу/админу. Всем остальным — ничего в шапке. */
function cpDecorateCallButtons(){
  const head = document.querySelector('#convBody .conv-head'); if(!head) return;
  const c = (typeof currentChat!=='undefined') ? currentChat : null;
  const kind = (c && c.kind) || 'direct';
  const isDirect = (kind === 'direct') || kind === 'saved' || c && c.cpSaved;
  const canHostCall = !isDirect && (c && (c.managed || cpCanManage && cpCanManage()));

  head.querySelectorAll('.ch-call').forEach(b=>{
    /* «Начать созвон» — наша кнопка (не трогаем) */
    if(b.classList.contains('cp-call-host') || b.classList.contains('cp-more-btn')) return;
    /* ЛИЧНЫЙ ЧАТ: обе кнопки звонка остаются на месте (правка Даниэля 09.08 —
       «нету звонков обычный и видео с личными чатами»). Раньше здесь пряталось
       ВСЁ без разбора, и рабочий движок личного звонка был недостижим.
       В группах и каналах — прячем: там свой хост-созвон ниже. */
    b.style.display = isDirect ? '' : 'none';
  });

  let hostBtn = head.querySelector('.cp-call-host');
  /* В шапке жили ДВЕ кнопки одного и того же группового созвона: эта и
     более новая .cl-conv-start из модуля звонков. Две одинаковые кнопки
     съедали 39px, из-за чего «Общий чат OKO» превращался в «Общий чат ОКО…».
     Если новая кнопка уже стоит — эту не создаём. */
  if(head.querySelector('.cl-conv-start')){
    if(hostBtn) hostBtn.remove();
    return;
  }
  if(canHostCall){
    if(!hostBtn){
      hostBtn = document.createElement('button');
      hostBtn.className = 'ch-call cp-call-host';
      hostBtn.title = 'Начать созвон';
      hostBtn.setAttribute('aria-label', 'Начать созвон');
      hostBtn.innerHTML = I('phone');
      hostBtn.onclick = ()=>cpStartConference();
      /* вставляем перед ⋮-меню, если оно есть, чтобы порядок был опрятный */
      const more = head.querySelector('#cpMoreBtn');
      if(more) head.insertBefore(hostBtn, more); else head.appendChild(hostBtn);
    }
    hostBtn.style.display = '';
  } else if(hostBtn){
    hostBtn.remove();
  }
}

/* конференц-созвон · Zoom-like: используем существующий call-overlay из ядра,
   но переопределяем заголовок под «конференция». Реальный WebRTC подключается позже. */
function cpStartConference(){
  const c = (typeof currentChat!=='undefined') ? currentChat : null;
  if(!c) return;
  const memN = (typeof c.members==='number' ? c.members : (Array.isArray(c.members)?c.members.length:0)) || 1;
  const tier = (typeof okoTier==='function') ? okoTier() : 'FREE';
  const cap = tier==='FREE' ? 20 : tier==='START' ? 50 : 200;
  if(typeof showPopup==='function'){
    showPopup({ico:'phone', title:'Начать созвон',
      body:`Создать групповой созвон в «${cpEsc(c.name)}»? Работает как Zoom: до <b>${cap}</b> участников (тариф ${tier}). Микрофон и камера можно отключать, участников — модерировать.<br><br><small style="color:var(--dim)">В комнате: mute/unmute, вкл/выкл камера, вход по ссылке, запись — на PRO.</small>`,
      actions:[
        {label:'Начать', onclick:()=>{ cpLaunchConference(c); }},
        {label:'Отмена', ghost:true}
      ]});
  } else cpLaunchConference(c);
}
/* Запуск именно КОНФЕРЕНЦИИ.
   Раньше здесь звался startCall(false), но модуль звонков перепатчивает
   window.startCall на ЛИЧНЫЙ звонок — и «Созвон» в группе открывал разговор
   один на один. Теперь идём прямо в конференц-движок. */
function cpLaunchConference(c){
  try{
    if(typeof window.callStartConf === 'function'){
      const admin = !!(c && (c.managed || (typeof cpCanManage === 'function' && cpCanManage())));
      window.callStartConf(c && c.id, admin, { chatName: c && c.name });
      return;
    }
  }catch(e){}
  if(typeof toast === 'function') toast('Созвон недоступен на этом устройстве');
}

/* ---- патчи: гейт входа + перерисовка списка ---- */
if(typeof openConv === 'function'){
  const _cpPrevOpenConv3 = openConv;
  openConv = function(id){
    const c = (typeof CHATS!=='undefined') ? CHATS.find(x=>x.id===id) : null;
    if(c && cpChatLocked(c)){ cpPaywall(c); return; }
    _cpPrevOpenConv3.apply(this, arguments);
    cpDecorateConvAccess();
    cpDecorateCallButtons();
  };
}
if(typeof renderChatList === 'function'){
  const _cpPrevRenderChatList = renderChatList;
  renderChatList = function(){ _cpPrevRenderChatList.apply(this, arguments); cpPaintChatBadges(); };
}
if(typeof closeConv === 'function'){
  const _cpPrevCloseConv3 = closeConv;
  closeConv = function(){ if(cpRecLocked) cpCancelRec(); _cpPrevCloseConv3.apply(this, arguments); };
}

(function cpInit3(){
  cpSeedChats();
  if(typeof renderChatList==='function') renderChatList(((document.getElementById('chatSearch')||{}).value)||'');
  if(typeof currentChat!=='undefined' && currentChat){ cpDecorateConvAccess(); cpDecorateCallButtons(); }
})();

/* =======================================================================
   ЧАСТЬ 5 — доработки TG-уровня: черновики, ссылки-предпросмотр,
   реальная пересылка, быстрая реакция двойным-тапом. Всё поверх ядра,
   base.html не трогается — только chain-патчи и DOM-инъекции.
   ======================================================================= */

/* ================= 15. ЧЕРНОВИКИ (не отправленный текст поля) ================= */
if(!CP.drafts || typeof CP.drafts!=='object') CP.drafts = {};

function cpDraftLoad(){
  if(typeof currentChat==='undefined' || !currentChat) return;
  const inp = document.getElementById('msgInput'); if(!inp) return;
  const d = CP.drafts[currentChat.id];
  if(d && !inp.value){
    inp.value = d;
    if(typeof syncSendIcon==='function') syncSendIcon();
  }
}
function cpDraftSave(){
  if(typeof currentChat==='undefined' || !currentChat) return;
  const inp = document.getElementById('msgInput'); if(!inp) return;
  const id = currentChat.id;
  const had = !!CP.drafts[id];
  const v = inp.value;
  if(v && v.trim()) CP.drafts[id] = v;
  else if(CP.drafts[id]) delete CP.drafts[id];
  cpSave();
  const has = !!CP.drafts[id];
  /* перерисовываем список только при СМЕНЕ состояния (появился/пропал черновик) —
     чтобы не колбасить DOM на каждый ввод символа */
  if(had !== has && typeof renderChatList==='function')
    renderChatList((document.getElementById('chatSearch')||{}).value || '');
}
function cpDraftClear(id){
  if(id==null || !CP.drafts[id]) return;
  delete CP.drafts[id]; cpSave();
  if(typeof renderChatList==='function')
    renderChatList((document.getElementById('chatSearch')||{}).value || '');
}
/* привязка input-слушателя один раз (msgInput живёт постоянно в DOM) */
(function cpDraftBind(){
  const inp = document.getElementById('msgInput'); if(!inp || inp._cpDraft) return;
  inp._cpDraft = 1;
  let t = null;
  inp.addEventListener('input', ()=>{
    clearTimeout(t); t = setTimeout(cpDraftSave, 320);
  });
})();
/* chain: openConv — восстановить черновик; sendText — очистить после отправки */
if(typeof openConv === 'function'){
  const _cpPrevOpenConv4 = openConv;
  openConv = function(){
    _cpPrevOpenConv4.apply(this, arguments);
    setTimeout(cpDraftLoad, 0);
  };
}
if(typeof sendText === 'function'){
  const _cpPrevSendText4 = sendText;
  sendText = function(){
    const c = (typeof currentChat!=='undefined') ? currentChat : null;
    _cpPrevSendText4.apply(this, arguments);
    if(c) cpDraftClear(c.id);
  };
}
/* хук в список: подменяем превью «Черновик: <текст>» лаймовой пометкой */
if(typeof renderChatList === 'function'){
  const _cpPrevRenderChatList2 = renderChatList;
  renderChatList = function(){
    _cpPrevRenderChatList2.apply(this, arguments);
    cpPaintDrafts();
  };
}
function cpPaintDrafts(){
  const list = document.getElementById('chatList'); if(!list) return;
  Object.keys(CP.drafts).forEach(id=>{
    const text = CP.drafts[id]; if(!text) return;
    const isNum = /^-?\d+$/.test(id);
    const sel = isNum ? 'openConv('+(+id)+')' : "openConv('"+id+"')";
    const item = list.querySelector('.chat-item[onclick="'+sel+'"]'); if(!item) return;
    const rows = item.querySelectorAll('.row1');
    const row2 = rows[1]; if(!row2) return;
    const prev = row2.querySelector('.preview'); if(!prev) return;
    const short = text.length > 34 ? text.slice(0,34).trim()+'...' : text;
    prev.innerHTML = '<span class="cp-draft-tag">Черновик:</span> <span class="ci-txt">'+cpEsc(short)+'</span>';
  });
}

/* ================= 16. ССЫЛКИ-ПРЕДПРОСМОТР (карточка URL) ================= */
const CP_URL_RE = /https?:\/\/[^\s<>"']+/i;
const CP_HOST_MAP = {
  'youtube.com':'YouTube · видео', 'youtu.be':'YouTube · видео',
  'youtube-nocookie.com':'YouTube · видео', 'vimeo.com':'Vimeo · видео',
  'rutube.ru':'Rutube · видео', 'vk.com':'ВКонтакте · пост', 'vkvideo.ru':'VK Video',
  'tiktok.com':'TikTok · клип', 'instagram.com':'Instagram · публикация',
  't.me':'Telegram · публикация', 'telegram.me':'Telegram · публикация',
  'github.com':'GitHub · репозиторий', 'gitlab.com':'GitLab · репозиторий',
  'x.com':'X (ex-Twitter)', 'twitter.com':'X (ex-Twitter)',
  'okoteam.top':'OKO TEAM · сайт', 'okoagents.okoteam.top':'OKO TEAM · сервисы',
  'notion.so':'Notion · страница', 'figma.com':'Figma · проект',
  'behance.net':'Behance · портфолио', 'dribbble.com':'Dribbble · шот',
  'medium.com':'Medium · статья', 'habr.com':'Хабр · статья',
  'yandex.ru':'Яндекс', 'google.com':'Google',
  'apple.com':'Apple', 'app.store':'App Store',
  'ozon.ru':'Ozon · товар', 'wildberries.ru':'Wildberries · товар',
  'avito.ru':'Авито · объявление'
};
function cpLinkTitle(host, u){
  if(CP_HOST_MAP[host]) return CP_HOST_MAP[host];
  const seg = (u.pathname||'').split('/').filter(Boolean)[0];
  if(seg){
    const clean = decodeURIComponent(seg).replace(/[-_+]+/g,' ').trim();
    if(clean.length > 1) return host + ' · ' + clean.slice(0,44);
  }
  return host;
}
function cpLinkCard(url){
  try{
    const u = new URL(url);
    const host = u.hostname.replace(/^www\./,'');
    const title = cpLinkTitle(host, u);
    return `<a class="cp-linkprev" href="${cpEsc(url)}" target="_blank" rel="noopener noreferrer" onclick="event.stopPropagation()">
      <span class="cp-lp-fav">${I('globe')}</span>
      <span class="cp-lp-txt"><b>${cpEsc(title)}</b><small>${cpEsc(host)}</small></span>
      <span class="cp-lp-go">${I('chev')}</span>
    </a>`;
  }catch(e){ return ''; }
}
if(typeof msgHtml === 'function'){
  const _cpPrevMsgHtml2 = msgHtml;
  msgHtml = function(m, idx){
    let out = _cpPrevMsgHtml2.apply(this, arguments);
    if(m && (m.kind==='text' || !m.kind) && typeof m.body==='string'){
      const match = m.body.match(CP_URL_RE);
      if(match){
        const card = cpLinkCard(match[0]);
        /* вставим карточку прямо перед строкой времени `.t`, чтобы реакции остались снизу */
        if(card) out = out.replace(/<span class="t">/, card + '<span class="t">');
      }
    }
    return out;
  };
}

/* ================= 17. ПЕРЕСЫЛКА С ВЫБОРОМ ЧАТА ================= */
let cpFwMsg = null, cpFwFrom = null;
if(typeof forwardMsg === 'function'){
  forwardMsg = function(idx){
    if(typeof closeMsgMenu==='function') closeMsgMenu();
    if(typeof currentChat==='undefined' || !currentChat) return;
    const m = currentChat.msgs[idx]; if(!m) return;
    cpForwardOpen(m, currentChat);
  };
}
function cpForwardOpen(msg, fromChat){
  if(typeof showPopup!=='function' || typeof CHATS==='undefined'){ if(typeof toast==='function') toast('Пересылка недоступна'); return; }
  const chats = CHATS.filter(c=>c && c.id !== fromChat.id && c.kind !== 'sys');
  if(!chats.length){ toast('Нет чатов для пересылки'); return; }
  cpFwMsg = msg; cpFwFrom = fromChat;
  const items = chats.slice(0,20).map(c=>{
    const idArg = typeof c.id==='number' ? c.id : ("'"+c.id+"'");
    const ava = (typeof avaHtml==='function') ? avaHtml(c) : `<div class="ava">${cpEsc(c.ava||'')}</div>`;
    const on = c.online ? '<span class="cp-fw-on" title="в сети"></span>' : '';
    return `<button class="cp-fw-item" onclick="cpForwardTo(${idArg})">
      <span class="cp-fw-ava">${ava}${on}</span>
      <span class="cp-fw-txt"><b>${cpEsc(c.name||'')}</b><small>${cpEsc(c.preview||'')}</small></span>
      <span class="cp-fw-go">${I('chev')}</span>
    </button>`;
  }).join('');
  const preview = cpEsc((typeof msgQuoteText==='function' ? msgQuoteText(msg) : (msg.body||'')) || 'сообщение').slice(0,120);
  showPopup({ico:'forward', title:'Переслать в чат',
    body: '<div class="cp-fw-quote">'+preview+'</div><div class="cp-fw-list">'+items+'</div>',
    actions:[{label:'Отмена', ghost:true, onclick:()=>{ cpFwMsg = null; cpFwFrom = null; }}]
  });
}
window.cpForwardTo = function(id){
  const m = cpFwMsg, from = cpFwFrom;
  cpFwMsg = null; cpFwFrom = null;
  if(typeof closePopup==='function') closePopup();
  if(!m) return;
  const target = (typeof CHATS!=='undefined' ? CHATS : []).find(c=>c.id===id);
  if(!target){ if(typeof toast==='function') toast('Чат не найден'); return; }
  /* глубокое-мелкое копирование, чтобы не подцепить реакции/статусы оригинала */
  const clone = Object.assign({}, m, {
    in: 0,
    t: (typeof nowT==='function' ? nowT() : ''),
    edited: false,
    reacts: null,
    cpSt: 'sent',
    fwd: { who: m.in ? (m.who || (from && from.name) || 'Автор') : 'Ты' }
  });
  delete clone.reply; delete clone.sid;
  if(typeof openConv==='function') openConv(id);
  /* pushMsg — после того как чат открылся и лента отрисована */
  setTimeout(()=>{
    if(typeof pushMsg==='function') pushMsg(clone);
    if(typeof toast==='function') toast('Переслано в «'+(target.name||'чат')+'»');
  }, 80);
};
/* показать метку «Переслано от …» в теле сообщения (без правки базового рендера) */
if(typeof msgHtml === 'function'){
  const _cpPrevMsgHtml3 = msgHtml;
  msgHtml = function(m, idx){
    let out = _cpPrevMsgHtml3.apply(this, arguments);
    if(m && m.fwd && m.fwd.who && m.kind !== 'sys'){
      const tag = `<div class="cp-fwd-tag">${I('forward')}<span>Переслано от <b>${cpEsc(m.fwd.who)}</b></span></div>`;
      /* вставляем плашку в начало пузыря (после первого <div class="msg ..."> открывающего) */
      out = out.replace(/(<div class="msg[^"]*"[^>]*>)/, '$1' + tag);
    }
    return out;
  };
}

/* ================= 18. БЫСТРАЯ РЕАКЦИЯ ДВОЙНЫМ ТАПОМ (fire) ================= */
if(typeof reactBar === 'function'){
  const _cpPrevReactBar = reactBar;
  let cpTapEl = null, cpTapT = 0, cpTapTimer = 0;
  reactBar = function(ev, idx){
    ev.stopPropagation();
    const now = Date.now();
    const msg = ev.currentTarget;
    /* игнорим тапы по интерактивным потомкам — те сами обрабатывают клик */
    if(ev.target && ev.target.closest &&
       ev.target.closest('button, a, video, audio, input, .cp-linkprev, .cp-vn-play, .cp-vn-speed, .cp-speed, .voice, .popt')){
      return; /* фолбэк ядра тоже гасим — интерактив рулит */
    }
    if(cpTapEl === msg && (now - cpTapT) < 320){
      clearTimeout(cpTapTimer);
      cpTapEl = null; cpTapT = 0;
      if(typeof pickReact === 'function'){
        pickReact(idx, 'fire');
        /* pickReact вызывает refreshMsg — узел заменится; вешаем FX уже на новый */
        setTimeout(()=>{
          const el = cpMsgsEl();
          const fresh = el && el.children[idx];
          if(fresh) cpQuickReactFx(fresh);
        }, 0);
      }
      return;
    }
    cpTapEl = msg; cpTapT = now;
    clearTimeout(cpTapTimer);
    cpTapTimer = setTimeout(()=>{
      cpTapEl = null; cpTapT = 0;
      /* одиночный тап — оригинальное поведение: открыть контекст-меню */
      if(typeof openMsgMenu === 'function') openMsgMenu(idx);
    }, 260);
  };
}
function cpQuickReactFx(msg){
  const fx = document.createElement('span');
  fx.className = 'cp-quick-fx'; fx.innerHTML = I('fire');
  msg.appendChild(fx);
  setTimeout(()=>fx.remove(), 900);
}

(function cpInit4(){
  /* если чат уже открыт при загрузке модуля — сразу восстановить черновик */
  if(typeof currentChat!=='undefined' && currentChat) cpDraftLoad();
  /* обновить список — на случай сохранённых черновиков с прошлой сессии */
  if(typeof renderChatList==='function')
    renderChatList((document.getElementById('chatSearch')||{}).value || '');
})();

/* =======================================================================
   ЧАСТЬ 6 — TG/WhatsApp+: long-press реакции с bounce, Saved Messages,
   слэш-команды бизнеса, автоответ, отложенная отправка, скраб волны,
   живой typing-индикатор, свайп-меню быстрых действий.
   ======================================================================= */

/* ================= РАСШИРЕНИЕ CP.persist ================= */
if(!CP.savedIds || typeof CP.savedIds!=='object') CP.savedIds = {};
if(!CP.away || typeof CP.away!=='object') CP.away = {on:false, text:'Сейчас не в сети. Отвечу, как только вернусь на связь.'};
if(!Array.isArray(CP.scheduled)) CP.scheduled = [];
if(!Array.isArray(CP.quickReplies)) CP.quickReplies = [
  {cmd:'/prices',  label:'Прайс',       text:'Актуальный прайс:\n· Базовый — 990 ₽\n· Продвинутый — 2 490 ₽\n· PRO — 4 990 ₽\nПодробности и оплата: https://okoteam.top/prices'},
  {cmd:'/menu',    label:'Меню услуг',  text:'Что делаем:\n· Разбор канала\n· Сценарии Reels/Shorts\n· Монтаж роликов\n· Продвижение и трафик\nКакой формат тебе интересен?'},
  {cmd:'/hours',   label:'Часы работы', text:'Работаем пн–пт 10:00–20:00 МСК. В остальное время отвечу при первой возможности.'},
  {cmd:'/thanks',  label:'Спасибо',     text:'Спасибо за обращение! Возьму в работу и вернусь с деталями в течение часа.'},
  {cmd:'/link',    label:'Реквизиты',   text:'Все ссылки и контакты — https://okoteam.top'},
  {cmd:'/address', label:'Как встретиться', text:'Работаем удалённо из Москвы. Созвон — https://meet.jit.si/oko-team'}
];
/* Полная сериализация — новые поля тоже персистятся. */
cpSave = function(){
  try{ localStorage.setItem('oko-chats-plus', JSON.stringify({
    speed:CP.speed, recentEmoji:CP.recentEmoji.slice(0,24), recentOko:CP.recentOko.slice(0,24),
    panelTab:CP.panelTab, access:CP.access, unlocked:CP.unlocked, drafts:CP.drafts,
    savedIds:CP.savedIds, away:CP.away, scheduled:CP.scheduled, quickReplies:CP.quickReplies
  })); }catch(e){}
};

/* ================= 20. SAVED MESSAGES · Избранное ================= */
function cpBuildSavedMsgs(){
  const base = [{kind:'sys', body:'Избранное · сохраняй сюда важные сообщения. Долгое нажатие → «В избранное».'}];
  Object.keys(CP.savedIds).forEach(key=>{
    const p = key.indexOf('|'); if(p<0) return;
    const chatId = key.slice(0,p), idx = +key.slice(p+1);
    const c = (typeof CHATS!=='undefined'?CHATS:[]).find(x=>String(x.id)===chatId); if(!c) return;
    const m = c.msgs && c.msgs[idx]; if(!m) return;
    base.push(Object.assign({}, m, {in:0, cpSaved:true, cpSavedFrom:{chatId, idx, name:c.name}, reacts:null}));
  });
  return base;
}
function cpSeedSaved(){
  if(typeof CHATS==='undefined' || !Array.isArray(CHATS)) return;
  if(CHATS.some(c=>c.id==='cp-saved')) return;
  CHATS.unshift({
    id:'cp-saved', ava:'★', name:'Избранное', kind:'saved', kindIcon:'bookmark',
    nick:'saved', preview:'Твоя личная папка — сохраняй важное',
    time:'', unread:0, online:false, cpSaved:true,
    msgs: cpBuildSavedMsgs()
  });
}
function cpSaveMessage(idx){
  if(typeof closeMsgMenu==='function') closeMsgMenu();
  if(typeof currentChat==='undefined' || !currentChat) return;
  const m = currentChat.msgs[idx]; if(!m || m.kind==='sys') return;
  if(currentChat.id==='cp-saved'){ if(typeof toast==='function') toast('Это уже избранное'); return; }
  const key = currentChat.id + '|' + idx;
  if(CP.savedIds[key]){ if(typeof toast==='function') toast('Уже в избранном'); return; }
  CP.savedIds[key] = 1; cpSave();
  const saved = (typeof CHATS!=='undefined'?CHATS:[]).find(c=>c.id==='cp-saved');
  if(saved){
    const clone = Object.assign({}, m, {in:0, cpSaved:true, cpSavedFrom:{chatId:currentChat.id, idx, name:currentChat.name}, reacts:null});
    saved.msgs.push(clone);
    saved.time = (typeof nowT==='function'?nowT():'');
    saved.preview = 'Сохранено · ' + (typeof kindPreview==='function' ? kindPreview(clone) : (clone.body||'сообщение'));
  }
  if(typeof toast==='function') toast('В избранном');
  if(typeof renderChatList==='function') renderChatList((document.getElementById('chatSearch')||{}).value||'');
}
/* инжектим пункт «В избранное» в контекст-меню сообщения (кроме папки Saved) */
if(typeof openMsgMenu === 'function'){
  const _cpPrevOpenMsgMenu2 = openMsgMenu;
  openMsgMenu = function(idx){
    _cpPrevOpenMsgMenu2.apply(this, arguments);
    const actionsEl = document.getElementById('mmActions'); if(!actionsEl) return;
    if(!currentChat || currentChat.id==='cp-saved') return;
    if(actionsEl.querySelector('.cp-mm-fav')) return;
    const pinBtn = [...actionsEl.children].find(b=>/Закрепить/.test(b.textContent||''));
    const btn = document.createElement('button');
    btn.className = 'mm-act cp-mm-fav'; btn.onclick = ()=>cpSaveMessage(idx);
    btn.innerHTML = I('bookmark') + '<span>В избранное</span>';
    if(pinBtn) actionsEl.insertBefore(btn, pinBtn); else actionsEl.appendChild(btn);
  };
}
/* плашка «Из чата X» на сообщении в папке Saved */
if(typeof msgHtml === 'function'){
  const _cpPrevMsgHtml6 = msgHtml;
  msgHtml = function(m, idx){
    let out = _cpPrevMsgHtml6.apply(this, arguments);
    if(m && m.cpSavedFrom && m.kind !== 'sys'){
      const tag = `<div class="cp-saved-from">${I('bookmark')}<span>Из чата <b>${cpEsc(m.cpSavedFrom.name)}</b></span></div>`;
      out = out.replace(/(<div class="msg[^"]*"[^>]*>)/, '$1' + tag);
    }
    if(m && m.cpSched && m.kind !== 'sys'){
      const tag = `<span class="cp-sched-tag">${I('clock')}Отложено</span>`;
      out = out.replace(/(<span class="t">)/, tag + '$1');
    }
    return out;
  };
}

/* ================= 21. LONG-PRESS ФЛОАТ-ПАЛИТРА РЕАКЦИЙ (Telegram-style) =================
   Позиционируем ВСЕГДА относительно сообщения (центр по X — центр сообщения; по Y —
   строго над сообщением, а если сверху нет места — под ним). Никакой «висящей в
   воздухе» плашки — палитра прицеплена к пузырю. При long-press на мобиле:
     • touchstart → таймер 380 мс → показать палитру (contextmenu не всегда прилетает
       на iOS/Android, поэтому дублируем);
     • не отпуская, палец водится над кнопкой → та подсвечивается;
     • touchend над кнопкой → мгновенно применить реакцию;
     • touchend вне палитры → палитра остаётся, тап-снаружи закрывает (как в TG).
*/
const CP_QUICK_REACTS = ['heart','fire','star','thumb','laugh','sad'];
const CP_RP_COLOR = {fire:'#ff7a1a', heart:'#ff3b6b', star:'#ffc522', thumb:'#9aff00', laugh:'#ffd23a', sad:'#7ab8ff', crown:'#ffc522'};
let cpPalette = null, cpJustLp = false, cpLpTimer = null, cpLpStart = null;

function cpInitLongPress(){
  const el = cpMsgsEl(); if(!el || el._cpLp) return; el._cpLp = true;

  /* десктоп/системный long-press → contextmenu */
  el.addEventListener('contextmenu', e=>{
    const msg = e.target.closest('.msg');
    if(!msg || msg.classList.contains('sys') || msg.classList.contains('typing')) return;
    e.preventDefault();
    const idx = Array.prototype.indexOf.call(el.children, msg);
    if(idx < 0) return;
    cpJustLp = true; setTimeout(()=>{ cpJustLp = false; }, 700);
    if(navigator.vibrate) try{ navigator.vibrate(18); }catch(_){}
    cpShowReactPalette(msg, idx);
  });

  /* мобильный long-press: пусть touch-цикл сам решит — с TG-стайл drag-to-pick */
  el.addEventListener('touchstart', e=>{
    if(e.touches.length !== 1) return;
    const msg = e.target.closest('.msg');
    if(!msg || msg.classList.contains('sys') || msg.classList.contains('typing')) return;
    /* игнор если это интерактив внутри пузыря (аудио-волна, кнопка) */
    if(e.target.closest('button, a, input, textarea, .wave, .pp, .cp-lsw-ic')) return;
    const idx = Array.prototype.indexOf.call(el.children, msg);
    if(idx < 0) return;
    cpLpStart = {x:e.touches[0].clientX, y:e.touches[0].clientY, msg, idx, moved:false};
    if(cpLpTimer) clearTimeout(cpLpTimer);
    cpLpTimer = setTimeout(()=>{
      cpLpTimer = null;
      if(!cpLpStart || cpLpStart.moved) return;
      cpJustLp = true; setTimeout(()=>{ cpJustLp = false; }, 700);
      if(navigator.vibrate) try{ navigator.vibrate(18); }catch(_){}
      cpShowReactPalette(cpLpStart.msg, cpLpStart.idx);
    }, 380);
  }, {passive:true});

  el.addEventListener('touchmove', e=>{
    if(!cpLpStart) return;
    const t = e.touches[0];
    const dx = t.clientX - cpLpStart.x, dy = t.clientY - cpLpStart.y;
    if(Math.abs(dx) > 10 || Math.abs(dy) > 10){
      cpLpStart.moved = true;
      if(cpLpTimer && !cpPalette){ clearTimeout(cpLpTimer); cpLpTimer = null; }
    }
    /* если палитра открыта и палец над её кнопкой — подсвечиваем */
    if(cpPalette){
      e.preventDefault();
      cpPaletteHover(t.clientX, t.clientY);
    }
  }, {passive:false});

  const endTouch = e=>{
    if(cpLpTimer){ clearTimeout(cpLpTimer); cpLpTimer = null; }
    if(cpPalette){
      /* drag-to-pick: если палец завершил жест на кнопке — применяем реакцию */
      const t = (e.changedTouches && e.changedTouches[0]);
      if(t){
        const el2 = document.elementFromPoint(t.clientX, t.clientY);
        const btn = el2 && el2.closest && el2.closest('.cp-rp-btn, .cp-rp-more');
        if(btn && cpPalette.el.contains(btn)){
          cpPickReactBtn(btn);
          cpLpStart = null;
          return;
        }
      }
      /* иначе — палитра остаётся, закроется по тапу вне */
    }
    cpLpStart = null;
  };
  el.addEventListener('touchend', endTouch, {passive:true});
  el.addEventListener('touchcancel', endTouch, {passive:true});

  /* закрывать палитру при скролле ленты — чтобы не «зависала в воздухе» */
  el.addEventListener('scroll', ()=>{ if(cpPalette) cpHideReactPalette(); }, {passive:true});
}

function cpPaletteHover(cx, cy){
  const p = cpPalette && cpPalette.el; if(!p) return;
  const el = document.elementFromPoint(cx, cy);
  const btn = el && el.closest && el.closest('.cp-rp-btn, .cp-rp-more');
  p.querySelectorAll('.cp-rp-btn.hot, .cp-rp-more.hot').forEach(b=>b.classList.remove('hot'));
  if(btn && p.contains(btn)) btn.classList.add('hot');
}

function cpPickReactBtn(btn){
  if(!cpPalette) return;
  const idx = cpPalette.idx;
  if(btn.classList.contains('cp-rp-more')){
    cpHideReactPalette();
    if(typeof openMsgMenu==='function') openMsgMenu(idx);
    return;
  }
  const rKey = btn.dataset.r;
  cpFlyReaction(btn, rKey);
  if(navigator.vibrate) try{ navigator.vibrate(12); }catch(_){}
  if(typeof pickReact==='function') pickReact(idx, rKey);
  setTimeout(cpHideReactPalette, 200);
}

function cpShowReactPalette(msg, idx){
  cpHideReactPalette();
  const p = document.createElement('div');
  p.className = 'cp-react-palette';
  p.innerHTML = CP_QUICK_REACTS.map((rr,i)=>
    `<button class="cp-rp-btn" data-r="${rr}" style="--cp-i:${i};--cp-fc:${CP_RP_COLOR[rr]||'var(--lime)'}" title="${rr}">${I(rr)}</button>`
  ).join('') + `<button class="cp-rp-more" style="--cp-i:${CP_QUICK_REACTS.length}" title="Ещё реакции">${I('more')}</button>`;
  document.body.appendChild(p);
  cpPalette = {el:p, msg, idx};

  /* позиция ВСЕГДА привязана к сообщению */
  const place = ()=>{
    const r = msg.getBoundingClientRect();
    const pw = p.offsetWidth, ph = p.offsetHeight;
    /* центрируем горизонтально по центру сообщения, а не по пальцу */
    let cx = r.left + r.width/2;
    /* небольшой уклон к «моему» краю (входящие → влево, исходящие → вправо) */
    const isOut = msg.classList.contains('out');
    if(isOut) cx = r.right - Math.min(r.width, pw)/2 + 8;
    else       cx = r.left  + Math.min(r.width, pw)/2 - 8;
    let left = Math.max(10, Math.min(window.innerWidth - pw - 10, cx - pw/2));
    /* по вертикали: над пузырём с отступом 10px; если сверху < 70px — под ним */
    let top = r.top - ph - 10;
    let below = false;
    if(top < 70){ top = Math.min(window.innerHeight - ph - 14, r.bottom + 10); below = true; }
    p.style.left = left + 'px';
    p.style.top  = top  + 'px';
    p.classList.toggle('cp-rp-below', below);
  };
  requestAnimationFrame(()=>{ place(); p.classList.add('on'); });

  /* click остаётся для мыши/тап-в-один-тап */
  p.addEventListener('click', ev=>{
    const btn = ev.target.closest('.cp-rp-btn, .cp-rp-more'); if(!btn) return;
    ev.stopPropagation();
    cpPickReactBtn(btn);
  });

  /* закрытие тапом вне палитры (и вне исходного пузыря — чтобы повторный тап
     по тому же пузырю не мешал drag-to-pick) */
  setTimeout(()=>{
    const off = e2=>{
      if(p.contains(e2.target)) return;
      cpHideReactPalette();
      document.removeEventListener('pointerdown', off, true);
    };
    document.addEventListener('pointerdown', off, true);
    p._cpOff = off;
  }, 0);
  /* при ресайзе/повороте — перепозиционировать */
  const onResize = ()=>{ if(cpPalette) place(); };
  window.addEventListener('resize', onResize);
  window.addEventListener('orientationchange', onResize);
  p._cpResize = onResize;

  if(typeof nvPush==='function') nvPush('cp:pal', ()=>cpHideReactPalette(true));
}
function cpHideReactPalette(fromNav){
  if(!cpPalette) return;
  const p = cpPalette.el; cpPalette = null;
  p.classList.remove('on');
  if(p._cpOff) try{ document.removeEventListener('pointerdown', p._cpOff, true); }catch(_){}
  if(p._cpResize){
    try{ window.removeEventListener('resize', p._cpResize); }catch(_){}
    try{ window.removeEventListener('orientationchange', p._cpResize); }catch(_){}
  }
  setTimeout(()=>{ try{ p.remove(); }catch(_){} }, 220);
  if(!fromNav && typeof nvPop==='function') nvPop('cp:pal');
}
function cpFlyReaction(btn, rKey){
  const r = btn.getBoundingClientRect();
  const fly = document.createElement('span');
  fly.className = 'cp-fly'; fly.innerHTML = I(rKey);
  fly.style.left = (r.left + r.width/2) + 'px';
  fly.style.top = (r.top + r.height/2) + 'px';
  fly.style.setProperty('--cp-fc', CP_RP_COLOR[rKey] || 'var(--lime)');
  document.body.appendChild(fly);
  setTimeout(()=>{ try{ fly.remove(); }catch(_){} }, 950);
}
/* блок реактивной цепи: если только что был long-press — не открывать полное меню на click */
if(typeof reactBar === 'function'){
  const _cpPrevReactBar2 = reactBar;
  reactBar = function(){ if(cpJustLp){ cpJustLp = false; return; } _cpPrevReactBar2.apply(this, arguments); };
}

/* ================= 22. LEFT-SWIPE · быстрое меню действий ================= */
let cpLsw = null;
function cpSwipeLeftInit(){
  const el = cpMsgsEl(); if(!el || el._cpLsw) return; el._cpLsw = true;
  el.addEventListener('touchstart', e=>{
    if(e.touches.length!==1){ cpLsw = null; return; }
    const msg = e.target.closest('.msg');
    if(!msg || msg.classList.contains('sys') || msg.classList.contains('typing')){ cpLsw = null; return; }
    cpLsw = {el:msg, x:e.touches[0].clientX, y:e.touches[0].clientY, dx:0, on:false};
  }, {passive:true});
  el.addEventListener('touchmove', e=>{
    if(!cpLsw) return;
    const dx = e.touches[0].clientX - cpLsw.x;
    const dy = e.touches[0].clientY - cpLsw.y;
    if(!cpLsw.on){
      if(dx < -14 && Math.abs(dx) > Math.abs(dy)*2){
        cpLsw.on = true;
        cpLsw.el.classList.add('cp-lsw');
        if(!cpLsw.el.querySelector('.cp-lsw-ic')){
          const ic = document.createElement('span'); ic.className='cp-lsw-ic'; ic.innerHTML = I('more');
          cpLsw.el.appendChild(ic);
        }
      } else if(Math.abs(dy) > 14 || dx > 14){ cpLsw = null; return; }
    }
    if(cpLsw && cpLsw.on){
      e.preventDefault();
      const d = Math.max(0, Math.min(-dx, 76));
      cpLsw.dx = d;
      cpLsw.el.style.transform = 'translateX(-' + d + 'px)';
      const ic = cpLsw.el.querySelector('.cp-lsw-ic');
      if(ic) ic.style.opacity = Math.min(1, d/40);
      cpLsw.el.classList.toggle('cp-lsw-hit', d > 40);
    }
  }, {passive:false});
  const end = ()=>{
    if(!cpLsw) return;
    const s = cpLsw; cpLsw = null;
    const hit = s.on && s.dx > 40;
    const idx = Array.prototype.indexOf.call(el.children, s.el);
    s.el.classList.remove('cp-lsw');
    s.el.classList.add('cp-lsw-back');
    s.el.style.transform = '';
    s.el.classList.remove('cp-lsw-hit');
    setTimeout(()=>{
      s.el.classList.remove('cp-lsw-back');
      const ic = s.el.querySelector('.cp-lsw-ic'); if(ic) ic.remove();
    }, 240);
    if(hit && idx >= 0 && typeof currentChat!=='undefined' && currentChat
       && currentChat.msgs[idx] && currentChat.msgs[idx].kind !== 'sys'){
      if(typeof openMsgMenu==='function') openMsgMenu(idx);
    }
  };
  el.addEventListener('touchend', end);
  el.addEventListener('touchcancel', end);
}

/* ================= 23. VOICE WAVEFORM SCRUB ================= */
function cpBindVoiceScrub(){
  const el = cpMsgsEl(); if(!el || el._cpVs) return; el._cpVs = 1;
  el.addEventListener('click', e=>{
    const wave = e.target.closest('.wave'); if(!wave) return;
    const voice = wave.closest('.voice'); if(!voice) return;
    const pp = voice.querySelector('.pp'); const a = pp && pp._audio;
    if(!a || !isFinite(a.duration) || !a.duration){
      /* мок-волна без реального audio — визуальный прогресс всё равно двигаем */
      const r = wave.getBoundingClientRect();
      const p = Math.max(0, Math.min(1, (e.clientX - r.left) / r.width));
      cpVoiceProg(voice, p);
      e.stopPropagation(); return;
    }
    const r = wave.getBoundingClientRect();
    const p = Math.max(0, Math.min(1, (e.clientX - r.left) / r.width));
    try{ a.currentTime = p * a.duration; }catch(_){}
    cpVoiceProg(voice, p);
    if(a.paused && typeof togglePlay==='function') togglePlay(pp);
    e.stopPropagation();
  });
}

/* ================= 24. SLASH-COMMANDS · быстрые ответы ================= */
let cpSlashPop = null;
function cpBindSlash(){
  const inp = document.getElementById('msgInput'); if(!inp || inp._cpSlash) return; inp._cpSlash = 1;
  inp.addEventListener('input', cpUpdateSlash);
  inp.addEventListener('focus', cpUpdateSlash);
  inp.addEventListener('blur', ()=>setTimeout(cpCloseSlash, 180));
  inp.addEventListener('keydown', e=>{
    if(!cpSlashPop || !cpSlashPop.classList.contains('on')) return;
    const items = [...cpSlashPop.querySelectorAll('.cp-sl-item')];
    let cur = cpSlashPop.querySelector('.cp-sl-item.on');
    const ci = items.indexOf(cur);
    if(e.key==='ArrowDown'){ e.preventDefault(); e.stopImmediatePropagation();
      const n = items[(ci+1) % items.length]; items.forEach(x=>x.classList.remove('on')); n && n.classList.add('on'); n && n.scrollIntoView({block:'nearest'}); }
    else if(e.key==='ArrowUp'){ e.preventDefault(); e.stopImmediatePropagation();
      const n = items[(ci-1+items.length) % items.length]; items.forEach(x=>x.classList.remove('on')); n && n.classList.add('on'); n && n.scrollIntoView({block:'nearest'}); }
    else if(e.key==='Enter' || e.key==='Tab'){ e.preventDefault(); e.stopImmediatePropagation();
      const pick = cur || items[0]; pick && pick.click(); }
    else if(e.key==='Escape'){ e.preventDefault(); e.stopImmediatePropagation(); cpCloseSlash(); }
  }, true);
}
function cpUpdateSlash(){
  const inp = document.getElementById('msgInput'); if(!inp) return;
  const v = inp.value || '';
  if(!v.startsWith('/')){ cpCloseSlash(); return; }
  const q = v.slice(1).trim().toLowerCase();
  const matches = CP.quickReplies.filter(qr =>
    !q || qr.cmd.slice(1).toLowerCase().startsWith(q) || qr.label.toLowerCase().includes(q));
  if(!matches.length){ cpCloseSlash(); return; }
  cpOpenSlash(matches);
}
function cpOpenSlash(items){
  if(!cpSlashPop){
    const p = document.createElement('div'); p.className='cp-slash'; p.id='cpSlash';
    const composer = document.querySelector('#convBody .composer');
    if(composer && composer.parentNode) composer.parentNode.insertBefore(p, composer);
    else document.body.appendChild(p);
    cpSlashPop = p;
  }
  cpSlashPop.innerHTML = `<div class="cp-sl-head">${I('bolt')}<span>Быстрые ответы для бизнеса</span></div>` +
    items.map((it,i)=>
      `<button class="cp-sl-item ${i===0?'on':''}" data-cmd="${cpEsc(it.cmd)}">
        <span class="cp-sl-cmd">${cpEsc(it.cmd)}</span>
        <span class="cp-sl-tx"><b>${cpEsc(it.label)}</b><small>${cpEsc(it.text.replace(/\n/g,' · ').slice(0,64))}${it.text.length>64?'…':''}</small></span>
      </button>`).join('');
  cpSlashPop.querySelectorAll('.cp-sl-item').forEach(b=>{
    b.addEventListener('click', ()=>{
      const cmd = b.dataset.cmd;
      const it = CP.quickReplies.find(x=>x.cmd===cmd); if(!it) return;
      const inp = document.getElementById('msgInput'); if(!inp) return;
      inp.value = it.text;
      try{ inp.setSelectionRange(inp.value.length, inp.value.length); }catch(_){}
      cpCloseSlash();
      if(typeof syncSendIcon==='function') syncSendIcon();
      inp.focus();
    });
  });
  cpSlashPop.classList.add('on');
}
function cpCloseSlash(){ if(cpSlashPop) cpSlashPop.classList.remove('on'); }
function cpQuickRepliesInfo(){
  if(typeof showPopup!=='function') return;
  const body = `<div class="cp-qr">
    <p class="cp-qr-hint">Начни ввод сообщения с <b>«/»</b> — покажу быстрые бизнес-ответы. Стрелки ↑↓ выбирают, Enter вставляет.</p>
    <div class="cp-qr-list">${CP.quickReplies.map(qr=>
      `<div class="cp-qr-item"><span class="cp-sl-cmd">${cpEsc(qr.cmd)}</span>
        <div class="cp-qr-t"><b>${cpEsc(qr.label)}</b><small>${cpEsc(qr.text.replace(/\n/g,' · ').slice(0,120))}${qr.text.length>120?'…':''}</small></div></div>`
    ).join('')}</div></div>`;
  showPopup({ico:'bolt', title:'Быстрые ответы (/)', body, actions:[{label:'Понятно'}]});
}

/* ================= 25. AWAY · автоответ «не в сети» ================= */
function cpAwayToggle(){
  if(typeof showPopup!=='function') return;
  const on = CP.away.on;
  const body = `<div class="cp-aw">
    <div class="cp-aw-row"><span>Автоответ активен</span>
      <label class="cp-aw-sw"><input type="checkbox" ${on?'checked':''} onchange="cpAwaySet(this.checked)"><span></span></label>
    </div>
    <label class="cp-aw-lb">Текст автоответа</label>
    <textarea id="cpAwText" rows="3" maxlength="300" oninput="cpAwayText(this.value)">${cpEsc(CP.away.text)}</textarea>
    <p class="cp-aw-hint">Отправляется собеседникам при первом сообщении, пока автоответ включён. Идеально для бизнес-аккаунта.</p>
  </div>`;
  showPopup({ico:'clock', title:'Автоответ «не в сети»', body, actions:[{label:'Готово'}]});
}
window.cpAwaySet = function(v){
  CP.away.on = !!v; cpSave(); cpPaintAwayBadge();
  if(typeof toast==='function') toast(v ? 'Автоответ включён' : 'Автоответ выключен');
};
window.cpAwayText = function(v){ CP.away.text = (v||'').slice(0,300); cpSave(); };
function cpPaintAwayBadge(){
  const composer = document.querySelector('#convBody .composer');
  if(!composer || !composer.parentNode) return;
  let b = document.getElementById('cpAwayBadge');
  if(!CP.away.on){ if(b) b.remove(); return; }
  if(!b){
    b = document.createElement('div'); b.id = 'cpAwayBadge'; b.className = 'cp-away-badge';
    b.innerHTML = `${I('clock')}<span>Автоответ «не в сети» включён</span><button title="Выключить" onclick="cpAwaySet(false)">${I('plus')}</button>`;
    composer.parentNode.insertBefore(b, composer);
  }
}

/* ================= 26. ОТЛОЖЕННАЯ ОТПРАВКА (long-press на send) ================= */
let cpSendHold = null;
function cpBindScheduleHold(){
  const sb = document.getElementById('sendBtn'); if(!sb || sb._cpSched) return; sb._cpSched = 1;
  const cancel = ()=>{ if(cpSendHold){ clearTimeout(cpSendHold); cpSendHold = null; } };
  sb.addEventListener('pointerdown', e=>{
    const inp = document.getElementById('msgInput');
    if(!inp || !inp.value.trim() || sb.classList.contains('rec')) return;
    cancel();
    cpSendHold = setTimeout(()=>{
      cpSendHold = null;
      if(navigator.vibrate) try{ navigator.vibrate(28); }catch(_){}
      cpOpenScheduleSheet();
    }, 620);
  });
  ['pointerup','pointercancel','pointerleave'].forEach(ev=>sb.addEventListener(ev, cancel));
  sb.addEventListener('pointermove', e=>{ if(cpSendHold && (Math.abs(e.movementX||0)+Math.abs(e.movementY||0)>4)) cancel(); });
}
function cpFmtWhen(d){
  const days = ['вс','пн','вт','ср','чт','пт','сб'];
  const today = new Date(); today.setHours(0,0,0,0);
  const dz = new Date(d); dz.setHours(0,0,0,0);
  const diffDays = Math.round((dz-today)/86400000);
  const hh = String(d.getHours()).padStart(2,'0'), mm = String(d.getMinutes()).padStart(2,'0');
  const time = hh+':'+mm;
  if(diffDays===0) return 'Сегодня в '+time;
  if(diffDays===1) return 'Завтра в '+time;
  if(diffDays>1 && diffDays<7) return days[d.getDay()]+' в '+time;
  return d.getDate()+'.'+String(d.getMonth()+1).padStart(2,'0')+' в '+time;
}
function cpOpenScheduleSheet(){
  const inp = document.getElementById('msgInput'); if(!inp || !inp.value.trim()) return;
  const c = (typeof currentChat!=='undefined') ? currentChat : null; if(!c) return;
  if(typeof showPopup!=='function'){ if(typeof toast==='function') toast('Планировщик недоступен'); return; }
  const now = new Date();
  const opts = [
    {mins:15,     ic:'clock', label:'Через 15 минут'},
    {mins:60,     ic:'clock', label:'Через час'},
    {mins:180,    ic:'clock', label:'Через 3 часа'},
    {mins:60*8,   ic:'clock', label:'Позже сегодня'},
    {mins:60*24,  ic:'clock', label:'Завтра в это же время'}
  ];
  const preview = inp.value.trim();
  const body = `<div class="cp-sch-preview">${cpEsc(preview.slice(0,180))}${preview.length>180?'…':''}</div>
    <div class="cp-sch-list">${opts.map(o=>{
      const ts = now.getTime() + o.mins*60000;
      return `<button class="cp-sch-item" onclick="cpDoSchedule(${ts})">
        <span class="cp-sch-ico">${I(o.ic)}</span>
        <span class="cp-sch-t"><b>${cpEsc(o.label)}</b><small>${cpEsc(cpFmtWhen(new Date(ts)))}</small></span>
        <span class="cp-sch-go">${I('chev')}</span>
      </button>`;
    }).join('')}</div>`;
  showPopup({ico:'clock', title:'Отправить позже', body, actions:[{label:'Отмена', ghost:true}]});
}
window.cpDoSchedule = function(ts){
  const inp = document.getElementById('msgInput');
  const c = (typeof currentChat!=='undefined') ? currentChat : null;
  if(!inp || !c || !inp.value.trim()) return;
  const text = inp.value.trim();
  const item = {id:'sc-'+Date.now()+'-'+Math.round(Math.random()*999), chatId:c.id, body:text, at:ts};
  CP.scheduled.push(item); cpSave();
  inp.value = ''; if(typeof syncSendIcon==='function') syncSendIcon();
  if(typeof closePopup==='function') closePopup();
  if(typeof toast==='function') toast('Отправлю ' + cpFmtWhen(new Date(ts)).toLowerCase());
  cpArmSchedule(item);
  cpPaintScheduledBar();
};
function cpArmSchedule(item){
  const dt = Math.max(0, item.at - Date.now());
  if(dt > 2147000000) return; /* setTimeout max ~24.8 дней — сработает при следующем запуске */
  setTimeout(()=>cpFireSchedule(item.id), dt);
}
function cpFireSchedule(id){
  const item = CP.scheduled.find(x=>x.id===id); if(!item) return;
  CP.scheduled = CP.scheduled.filter(x=>x.id!==id); cpSave();
  const c = (typeof CHATS!=='undefined'?CHATS:[]).find(x=>x.id===item.chatId); if(!c) return;
  const msg = {in:0, t:(typeof nowT==='function'?nowT():''), kind:'text', body:item.body, cpSched:true};
  if(typeof currentChat!=='undefined' && currentChat && currentChat.id===c.id){
    if(typeof pushMsg==='function') pushMsg(msg);
  } else {
    c.msgs.push(msg);
    c.preview = (typeof kindPreview==='function' ? 'Ты: '+kindPreview(msg) : 'Ты: '+msg.body);
    c.time = msg.t;
    c.unread = 0;
    if(typeof renderChatList==='function') renderChatList((document.getElementById('chatSearch')||{}).value||'');
  }
  cpPaintScheduledBar();
  if(typeof toast==='function') toast('Отложенное отправлено в «'+(c.name||'чат')+'»');
}
function cpRestoreSchedule(){
  const now = Date.now();
  const overdue = CP.scheduled.filter(x=>x.at <= now).slice();
  overdue.forEach(x=>cpFireSchedule(x.id));
  CP.scheduled.filter(x=>x.at > now).forEach(cpArmSchedule);
}
function cpPaintScheduledBar(){
  const bar = document.getElementById('cpSchedBar'); if(!bar) return;
  const c = (typeof currentChat!=='undefined')?currentChat:null;
  if(!c){ bar.classList.remove('on'); return; }
  const list = CP.scheduled.filter(x=>x.chatId===c.id);
  if(!list.length){ bar.classList.remove('on'); bar.innerHTML=''; return; }
  const next = list.slice().sort((a,b)=>a.at-b.at)[0];
  const wordForm = list.length===1 ? 'ное сообщение' : (list.length>=2 && list.length<=4) ? 'ных сообщения' : 'ных сообщений';
  bar.innerHTML = `<span class="cp-sch-bar-ic">${I('clock')}</span>
    <div class="cp-sch-bar-t"><b>Отложе${wordForm[0]==='н'?'но':'ных'} ${list.length} ${wordForm}</b>
    <small>${cpEsc(cpFmtWhen(new Date(next.at)))} · ${cpEsc(next.body.slice(0,50))}${next.body.length>50?'…':''}</small></div>
    <button class="cp-sch-cancel" title="Отменить ближайшее" onclick="cpCancelSchedule('${next.id}')">${I('plus')}</button>`;
  bar.classList.add('on');
}
window.cpCancelSchedule = function(id){
  CP.scheduled = CP.scheduled.filter(x=>x.id!==id); cpSave();
  cpPaintScheduledBar();
  if(typeof toast==='function') toast('Отложенное отменено');
};

/* ================= 27. РАСШИРЕНИЕ UI · sched-bar, ⋮-меню, openConv ================= */
if(typeof cpBuildUi === 'function'){
  const _cpPrevBuildUi = cpBuildUi;
  cpBuildUi = function(){
    _cpPrevBuildUi.apply(this, arguments);
    const pin = document.getElementById('cpPinBar'); if(!pin) return;
    if(!document.getElementById('cpSchedBar')){
      const sb = document.createElement('div');
      sb.className = 'cp-schedbar'; sb.id = 'cpSchedBar';
      pin.insertAdjacentElement('afterend', sb);
    }
  };
  /* пересобрать UI, если модуль перезагружается на лету */
  cpBuildUi();
}
if(typeof cpMoreItems === 'function'){
  const _cpPrevMoreItems = cpMoreItems;
  cpMoreItems = function(){
    const arr = _cpPrevMoreItems.apply(this, arguments);
    arr.push({ic:'bookmark', label:'Открыть избранное',       act:()=>{ if(typeof openConv==='function') openConv('cp-saved'); }});
    arr.push({ic:'bolt',     label:'Быстрые ответы (/)',      act:cpQuickRepliesInfo});
    arr.push({ic:'clock',    label:'Автоответ «не в сети»'+(CP.away.on?' · вкл':''), act:cpAwayToggle});
    return arr;
  };
}
if(typeof openConv === 'function'){
  const _cpPrevOpenConv6 = openConv;
  openConv = function(id){
    _cpPrevOpenConv6.apply(this, arguments);
    /* при открытии «Избранного» пересоберём msgs, чтобы подхватить новые сохранения */
    if(typeof currentChat!=='undefined' && currentChat && currentChat.id==='cp-saved'){
      currentChat.msgs = cpBuildSavedMsgs();
      if(typeof renderMsgs==='function') renderMsgs();
    }
    cpPaintScheduledBar();
    cpPaintAwayBadge();
  };
}

/* ================= САМОИНИЦИАЛИЗАЦИЯ ================= */
(function cpInit6(){
  cpSeedSaved();
  cpBindSlash();
  cpBindScheduleHold();
  cpBindVoiceScrub();
  cpInitLongPress();
  cpSwipeLeftInit();
  cpRestoreSchedule();
  cpPaintAwayBadge();
  if(typeof renderChatList==='function')
    renderChatList((document.getElementById('chatSearch')||{}).value||'');
  if(typeof currentChat!=='undefined' && currentChat){
    cpPaintScheduledBar();
  }
})();

/* ===== module: calls ===== */
/* ===== CALLS: два движка звонков =====
   Префикс cl-. Оба поверх ядра, base.html не тронут.
   1) Групповая конференция (Zoom-like) — админ инициирует в чате/группе/канале.
   2) Личный звонок (аудио/видео) — с getUserMedia, PIP собеседника, анимацией колец.
   Экспорт: window.callStartConf(chatId, isAdmin), window.callStartPersonal(userId, isVideo).
   Chain-patch: старый window.startCall(video) → callStartPersonal(currentChat.id, video). */

(function(){
'use strict';

/* ---------------- маленькие утилиты ---------------- */
const $ = (id)=>document.getElementById(id);
const CL_I = (name)=>`<svg class="cl-i"><use href="#cl-i-${name}"/></svg>`;
const clEsc = (s)=> (typeof esc==='function' ? esc(s) : String(s==null?'':s).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch])));
const clFmtSec = (n)=>{ n=Math.max(0,n|0); const m=String((n/60)|0).padStart(2,'0'), s=String(n%60).padStart(2,'0'); return m+':'+s; };
const clToast = (t)=>{ const d=document.createElement('div'); d.className='cl-toast'; d.textContent=t; document.body.appendChild(d); setTimeout(()=>d.remove(),2700); };

/* локальное хранилище — политики звонков в чатах (админ-настройки) */
const CL_LS = 'oko-calls-v1';
const CL = (()=>{ try{ return JSON.parse(localStorage.getItem(CL_LS))||{}; }catch(_){ return {}; } })();
if(!CL.policy || typeof CL.policy!=='object') CL.policy = {}; /* {chatId:{mic:1,cam:1,chat:1,share:1}} */
function clSave(){ try{ localStorage.setItem(CL_LS, JSON.stringify({policy:CL.policy})); }catch(_){} }
function clGetPolicy(chatId){
  const p = CL.policy[chatId] || {};
  return { mic: p.mic!==0, cam: p.cam!==0, chat: p.chat!==0, share: p.share!==0 };
}
function clSetPolicy(chatId, key, val){
  const p = CL.policy[chatId] = CL.policy[chatId] || {};
  p[key] = val ? 1 : 0; clSave();
}

/* ---------------- имена/аватар из ядра ---------------- */
function clChatById(id){
  try{ if(typeof CHATS==='undefined') return null; return CHATS.find(c=>String(c.id)===String(id)) || null; }catch(_){ return null; }
}
function clAvaLetter(name){ return (String(name||'O').trim()[0]||'O').toUpperCase(); }

/* ===================================================================
   1) ГРУППОВАЯ КОНФЕРЕНЦИЯ (Zoom-like)
   =================================================================== */
const CL_DEMO_NAMES = []  /* демо-данные удалены 09.08: участники группового звонка — источник только API */;
let confState = null;
let confTimer = null;
let confTick = null;
let confPillTimer = null;

function clMakeDemoParts(hostName){
  /* 5–8 демо-участников + сам «я» первым */
  const pool = CL_DEMO_NAMES.filter(n=>n!==hostName);
  const cnt = 5 + Math.floor(Math.random()*4);
  const out = [{id:'me', name:'Вы', ava:'Я', role:'host', mic:true, cam:false, hand:false, speaking:false, muted:false, self:true}];
  for(let i=0;i<cnt;i++){
    const nm = pool[Math.floor(Math.random()*pool.length)] || ('Гость '+(i+1));
    out.push({
      id:'p'+i, name:nm, ava:clAvaLetter(nm),
      role: i===0 ? 'co' : '', mic: Math.random()>.3, cam: Math.random()>.7,
      hand:false, speaking:false, muted:false
    });
  }
  return out;
}

/**
 * Открыть общий созвон.
 * @param {*} chatId       — id чата (для политики)
 * @param {boolean} isAdmin — есть ли у меня админ-права (mic/cam/chat/kick)
 * @param {object} opts    — { chatName, participants }
 */
window.callStartConf = function(chatId, isAdmin, opts){
  opts = opts || {};
  const chat = clChatById(chatId);
  if(!chat && !opts.chatName){ clToast('Чат не найден'); return; }
  clStopPersonalCall(true); /* нельзя параллельно с личным */

  confState = {
    chatId: chatId, isAdmin: !!isAdmin,
    chatName: opts.chatName || (chat && chat.name) || 'Общий созвон',
    sec: 0, mic: true, cam: false, share: false, side: false, speakerPin: null,
    policy: clGetPolicy(chatId),
    parts: (opts.participants && opts.participants.length) ? opts.participants : clMakeDemoParts()
  };

  $('cl-conf-name').textContent = confState.chatName;
  $('cl-conf-time').textContent = '00:00';
  $('cl-conf').classList.add('on');
  $('cl-conf').setAttribute('aria-hidden','false');
  document.body.style.overflow = 'hidden';

  clHidePill();
  clRenderConfTiles();
  clUpdateConfButtons();

  /* таймер */
  clearInterval(confTimer);
  confTimer = setInterval(()=>{
    confState.sec++;
    const t = clFmtSec(confState.sec);
    const el = $('cl-conf-time'); if(el) el.textContent = t;
    const p = $('cl-pill-time'); if(p) p.textContent = t;
  }, 1000);

  /* симуляция «кто говорит» / поднятые руки */
  clearInterval(confTick);
  confTick = setInterval(clTickSpeakers, 1400);

  /* блокировка backButton — свернуть */
  if(typeof nvPush==='function') nvPush('cl:conf', ()=>clConfMin());
};

function clRenderConfTiles(){
  const stage = $('cl-conf-stage'); if(!stage) return;
  const parts = confState.parts;
  stage.classList.toggle('dense', parts.length >= 9);
  stage.classList.toggle('speaker', !!confState.speakerPin);

  const tileHtml = (p, size)=>{
    const speaking = p.speaking && p.mic && !p.muted;
    const showVideo = p.cam && !p.self;
    const flags = [];
    if(!p.mic || p.muted) flags.push(`<span class="muted" title="Микрофон выключен">${CL_I('mic-off')}</span>`);
    else if(speaking) flags.push(`<span class="on" title="Говорит">${CL_I('wave')}</span>`);
    if(!p.cam) flags.push(`<span title="Камера выключена">${CL_I('cam-off')}</span>`);
    const role = p.role==='host' ? `<span class="cl-role">${CL_I('crown')}</span>` :
                 p.role==='co'   ? `<span class="cl-role">${CL_I('shield')}</span>` : '';
    const hand = p.hand ? `<span class="cl-tile-hand" title="Просит слово">${CL_I('hand')}</span>` : '';
    return `<div class="cl-tile ${speaking?'speaking':''} ${confState.speakerPin===p.id?'pinned':''}"
                 data-id="${p.id}" onclick="__cl_tileTap('${p.id}',event)" oncontextmenu="return __cl_tileCtx('${p.id}',event)">
      ${showVideo ? `<video class="cl-tile-vid" playsinline autoplay muted></video>` : ''}
      ${!showVideo ? `<div class="cl-tile-ava">${clEsc(p.ava||'?')}</div>` : ''}
      <div class="cl-tile-name">${role}${clEsc(p.name)}</div>
      ${flags.length ? `<div class="cl-tile-flag">${flags.join('')}</div>` : ''}
      ${hand}
    </div>`;
  };

  if(confState.speakerPin){
    const pin = parts.find(p=>p.id===confState.speakerPin);
    const rest = parts.filter(p=>p.id!==confState.speakerPin);
    stage.innerHTML = (pin?tileHtml(pin):'')
      + `<div class="cl-strip">${rest.map(p=>tileHtml(p)).join('')}</div>`;
  } else {
    stage.innerHTML = parts.map(p=>tileHtml(p)).join('');
  }
  $('cl-conf-count').textContent = parts.length;
  const n = $('cl-conf-partn'); if(n) n.textContent = String(parts.length);
}

function clUpdateConfButtons(){
  if(!confState) return;
  const s = confState;
  const meMic = s.mic && (s.isAdmin || s.policy.mic);
  const meCam = s.cam && (s.isAdmin || s.policy.cam);
  const meShare = s.share && (s.isAdmin || s.policy.share);
  const set = (id, on, icon)=>{ const b=$(id); if(!b) return; b.classList.toggle('on', !!on); b.querySelector('use').setAttribute('href','#cl-i-'+icon); };
  set('cl-conf-mic',   meMic,   meMic ? 'mic' : 'mic-off');
  set('cl-conf-cam',   meCam,   meCam ? 'cam' : 'cam-off');
  set('cl-conf-share', meShare, 'share');
  set('cl-conf-part',  s.side,  'users');
  const meObj = s.parts.find(p=>p.self); if(meObj){ meObj.mic = meMic; meObj.cam = meCam; }

  /* заблокированные ядром политики */
  const lockBtn = (id, allowed)=>{ const b=$(id); if(!b) return; b.style.opacity = allowed ? '' : '.35'; b.style.pointerEvents = allowed ? '' : 'none'; };
  lockBtn('cl-conf-mic',   s.isAdmin || s.policy.mic);
  lockBtn('cl-conf-cam',   s.isAdmin || s.policy.cam);
  lockBtn('cl-conf-share', s.isAdmin || s.policy.share);
  lockBtn('cl-conf-chat',  s.isAdmin || s.policy.chat);

  /* меню (шестерёнка) только у админа */
  const g = $('cl-conf-gear'); if(g) g.style.display = s.isAdmin ? '' : 'none';
}

function clTickSpeakers(){
  if(!confState || !$('cl-conf').classList.contains('on')) return;
  const active = confState.parts.filter(p=>p.mic && !p.muted);
  const shouldSpeak = active[Math.floor(Math.random()*active.length)];
  confState.parts.forEach(p=>{ p.speaking = p===shouldSpeak; });
  /* случайно поднять/опустить руку у не-админа */
  if(Math.random() < .12){
    const cand = confState.parts.filter(p=>!p.self && p.role!=='host');
    const pick = cand[Math.floor(Math.random()*cand.length)];
    if(pick){ pick.hand = !pick.hand; }
  }
  clRenderConfTiles();
}

/* --- взаимодействия по тайлу --- */
window.__cl_tileTap = function(pid, ev){
  ev && ev.stopPropagation && ev.stopPropagation();
  if(!confState) return;
  /* короткое нажатие: pin/unpin (speaker-view) */
  if(!ev._longpress){
    confState.speakerPin = confState.speakerPin===pid ? null : pid;
    clRenderConfTiles();
  }
};
window.__cl_tileCtx = function(pid, ev){
  ev && ev.preventDefault && ev.preventDefault();
  clOpenPartMenu(pid, ev);
  return false;
};

/* long-press на mobile → меню админа по участнику */
document.addEventListener('touchstart', (e)=>{
  const tile = e.target.closest && e.target.closest('#cl-conf-stage .cl-tile');
  if(!tile) return;
  const pid = tile.getAttribute('data-id');
  tile._lpT = setTimeout(()=>{ tile._lp = 1; clOpenPartMenu(pid, e); }, 460);
}, {passive:true});
document.addEventListener('touchend', (e)=>{
  const tile = e.target.closest && e.target.closest('#cl-conf-stage .cl-tile');
  if(!tile) return; clearTimeout(tile._lpT);
  if(tile._lp){ tile._lp = 0; e.preventDefault && e.preventDefault(); }
}, {passive:false});

function clOpenPartMenu(pid, ev){
  if(!confState) return;
  const menu = $('cl-conf-menu'); if(!menu) return;
  const p = confState.parts.find(x=>x.id===pid); if(!p) return;
  const isMe = !!p.self;
  const canAdmin = confState.isAdmin && !isMe;

  const items = [
    `<div class="cl-menu-hd">${clEsc(p.name)}</div>`,
    `<button class="cl-menu-item" onclick="__cl_pinPart('${pid}')">${CL_I(confState.speakerPin===pid?'x':'users')}<span>${confState.speakerPin===pid?'Убрать из фокуса':'Показать крупно'}</span></button>`,
  ];
  if(canAdmin){
    items.push(`<div class="cl-menu-sep"></div>`);
    items.push(`<button class="cl-menu-item" onclick="__cl_toggleMute('${pid}')">${CL_I(p.muted?'mic':'mic-off')}<span>${p.muted?'Разрешить микрофон':'Выключить микрофон'}</span></button>`);
    items.push(`<button class="cl-menu-item" onclick="__cl_toggleCam('${pid}')">${CL_I(p.cam?'cam-off':'cam')}<span>${p.cam?'Выключить камеру':'Разрешить камеру'}</span></button>`);
    if(p.hand){
      items.push(`<button class="cl-menu-item" onclick="__cl_lowerHand('${pid}')">${CL_I('hand')}<span>Опустить руку</span></button>`);
    }
    items.push(`<div class="cl-menu-sep"></div>`);
    items.push(`<button class="cl-menu-item danger" onclick="__cl_kick('${pid}')">${CL_I('x')}<span>Удалить из звонка</span></button>`);
  }
  menu.innerHTML = items.join('');

  /* позиционируем возле точки клика */
  let x=20, y=100;
  if(ev){
    if(ev.clientX!=null){ x = ev.clientX; y = ev.clientY; }
    else if(ev.touches && ev.touches[0]){ x = ev.touches[0].clientX; y = ev.touches[0].clientY; }
  }
  const rect = document.body.getBoundingClientRect();
  x = Math.min(x, rect.width - 240); y = Math.min(y, rect.height - 260);
  menu.style.left = Math.max(8,x) + 'px';
  menu.style.top  = Math.max(8,y) + 'px';
  menu.classList.add('on'); menu.setAttribute('aria-hidden','false');
  setTimeout(()=>{ document.addEventListener('click', clClosePartMenu, {once:true}); }, 30);
}
function clClosePartMenu(){ const m=$('cl-conf-menu'); if(m){ m.classList.remove('on'); m.setAttribute('aria-hidden','true'); } }
window.__cl_pinPart = function(pid){ confState.speakerPin = confState.speakerPin===pid?null:pid; clClosePartMenu(); clRenderConfTiles(); };
window.__cl_toggleMute = function(pid){ const p=confState.parts.find(x=>x.id===pid); if(!p) return; p.muted=!p.muted; if(p.muted) p.mic=false; else p.mic=true; clClosePartMenu(); clRenderConfTiles(); clToast(p.muted?'Микрофон выключен для '+p.name:'Микрофон разрешён для '+p.name); };
window.__cl_toggleCam = function(pid){ const p=confState.parts.find(x=>x.id===pid); if(!p) return; p.cam=!p.cam; clClosePartMenu(); clRenderConfTiles(); };
window.__cl_lowerHand = function(pid){ const p=confState.parts.find(x=>x.id===pid); if(!p) return; p.hand=false; clClosePartMenu(); clRenderConfTiles(); };
window.__cl_kick = function(pid){
  const p=confState.parts.find(x=>x.id===pid); if(!p) return;
  confState.parts = confState.parts.filter(x=>x.id!==pid);
  clClosePartMenu(); clRenderConfTiles(); clToast(p.name+' удалён из звонка');
};

/* --- боковая панель участников --- */
function clOpenSide(){
  confState.side = true; const s = $('cl-conf-side'); if(!s) return;
  const body = $('cl-side-body');
  body.innerHTML = confState.parts.map(p=>{
    const speaking = p.speaking && p.mic && !p.muted;
    return `<button class="cl-prow" onclick="__cl_openMenuFromSide('${p.id}',event)">
      <div class="cl-pava">${clEsc(p.ava||'?')}</div>
      <div class="cl-pbody"><b>${clEsc(p.name)}${p.self?' (вы)':''}</b>
        <small>${p.role==='host'?'владелец':p.role==='co'?'админ':(speaking?'говорит…':'в звонке')}</small></div>
      <div class="cl-pflag">
        <span class="${p.mic&&!p.muted ? (speaking?'on':'') : 'muted'}">${CL_I(p.mic&&!p.muted?'mic':'mic-off')}</span>
        <span class="${p.cam?'on':''}">${CL_I(p.cam?'cam':'cam-off')}</span>
      </div></button>`;
  }).join('');
  s.classList.add('on'); s.setAttribute('aria-hidden','false');
  clUpdateConfButtons();
}
function clCloseSide(){ confState.side=false; const s=$('cl-conf-side'); if(s){ s.classList.remove('on'); s.setAttribute('aria-hidden','true'); } clUpdateConfButtons(); }
window.__cl_openMenuFromSide = function(pid, ev){ clOpenPartMenu(pid, ev); };

/* --- настройки владельца (mic/cam/chat/share для всех) --- */
function clOpenSettings(){
  if(!confState || !confState.isAdmin) return;
  const body = $('cl-settings-body');
  const p = confState.policy;
  const row = (k, title, sub)=>{
    return `<button class="cl-toggle" onclick="__cl_flipPolicy('${k}')">
      <span style="flex:1"><b>${title}</b><small>${sub}</small></span>
      <span class="cl-sw ${p[k]?'on':''}"><i></i></span>
    </button>`;
  };
  body.innerHTML =
    row('mic', 'Разрешить микрофон всем', 'Иначе включать смогут только админы') +
    row('cam', 'Разрешить камеру всем', 'Участники смогут вещать видео') +
    row('share', 'Разрешить демо экрана', 'Демонстрация экрана участникам') +
    row('chat', 'Разрешить чат в созвоне', 'Иначе чат только для админов');
  const sh = $('cl-conf-settings'); sh.classList.add('on'); sh.setAttribute('aria-hidden','false');
}
function clCloseSettings(){ const sh=$('cl-conf-settings'); if(sh){ sh.classList.remove('on'); sh.setAttribute('aria-hidden','true'); } }
window.__cl_flipPolicy = function(k){
  if(!confState) return;
  const v = !confState.policy[k]; confState.policy[k] = v;
  clSetPolicy(confState.chatId, k, v);
  clOpenSettings();
  clUpdateConfButtons();
  clToast((v?'Разрешено: ':'Запрещено: ')+({mic:'микрофон',cam:'камера',share:'демо экрана',chat:'чат'}[k]||k));
};

/* --- реакции --- */
function clFlyReaction(){
  const box = $('cl-conf-reactions'); if(!box) return;
  box.setAttribute('aria-hidden','false');
  for(let i=0;i<3;i++){
    const f = document.createElement('div'); f.className='cl-react-fly';
    f.innerHTML = CL_I('heart');
    f.style.left = (30 + Math.random()* (box.clientWidth-60)) + 'px';
    f.style.setProperty('--tx', (Math.random()*80-40) + 'px');
    f.style.animationDelay = (i*0.12)+'s';
    box.appendChild(f); setTimeout(()=>f.remove(), 2600);
  }
}

/* --- свернуть / завершить --- */
function clConfMin(){
  const el = $('cl-conf'); if(!el) return;
  el.classList.remove('on'); el.setAttribute('aria-hidden','true');
  document.body.style.overflow = '';
  clShowPill(confState && confState.chatName || 'Созвон');
  if(typeof nvPop==='function') nvPop('cl:conf');
}
function clConfRestore(){
  const el = $('cl-conf'); if(!el || !confState) return;
  el.classList.add('on'); el.setAttribute('aria-hidden','false');
  document.body.style.overflow = 'hidden';
  clHidePill();
  if(typeof nvPush==='function') nvPush('cl:conf', ()=>clConfMin());
}
function clConfEnd(){
  clearInterval(confTimer); clearInterval(confTick);
  const el = $('cl-conf'); if(el){ el.classList.remove('on'); el.setAttribute('aria-hidden','true'); }
  clCloseSide(); clCloseSettings(); clClosePartMenu(); clHidePill();
  document.body.style.overflow = '';
  const sec = confState ? confState.sec : 0;
  const chatId = confState && confState.chatId;
  confState = null;
  if(typeof nvPop==='function') nvPop('cl:conf');
  /* системное сообщение в ленту чата */
  try{
    if(typeof currentChat!=='undefined' && currentChat && String(currentChat.id)===String(chatId) && typeof pushMsg==='function'){
      pushMsg({in:0, t:(typeof nowT==='function'?nowT():''), kind:'sys', body:'Созвон завершён · '+clFmtSec(sec)});
    }
  }catch(_){}
}

/* ===================================================================
   2) ЛИЧНЫЙ ЗВОНОК (аудио/видео)
   =================================================================== */
let persState = null;
let persStream = null;
let persTimer = null;

/**
 * Начать личный звонок.
 * @param {*} userId  — id собеседника (для лога/имени)
 * @param {boolean} isVideo — видеозвонок?
 * @param {object} opts — { name, ava, avaUrl }
 */
window.callStartPersonal = function(userId, isVideo, opts){
  opts = opts || {};
  const chat = clChatById(userId);
  const name = opts.name || (chat && chat.name) || (typeof currentChat!=='undefined' && currentChat && currentChat.name) || 'Собеседник';
  const ava  = opts.ava  || (chat && chat.ava)  || clAvaLetter(name);

  if(confState){ clToast('Сейчас идёт общий созвон'); return; }
  clStopPersonalCall(true);

  persState = { userId, video:!!isVideo, name, ava, mic:true, spk:true, cam:!!isVideo, sec:0, connected:false };

  const el = $('cl-personal'); if(!el){ console.warn('[calls] overlay #cl-personal not found'); return; }
  el.classList.remove('video','video-remote');
  el.classList.toggle('video', !!isVideo);
  $('cl-pers-name').textContent = name;
  $('cl-pers-status').textContent = isVideo ? 'Видеозвонок · соединение…' : 'Аудиозвонок · соединение…';
  $('cl-pers-ava').textContent = ava;

  clUpdatePersButtons();
  el.classList.add('on'); el.setAttribute('aria-hidden','false');
  document.body.style.overflow = 'hidden';
  clHidePill();

  if(typeof nvPush==='function') nvPush('cl:pers', ()=>clPersMin());

  /* PIP: моё видео при видеозвонке */
  const pip = $('cl-pers-pip');
  if(isVideo){ pip.hidden = false; clStartUserMedia(true); }
  else       { pip.hidden = true;  clStartUserMedia(false); }

  /* «соединение» → «в сети» через 1.8с, стартуем таймер */
  setTimeout(()=>{
    if(!persState) return;
    persState.connected = true;
    $('cl-pers-status').textContent = clFmtSec(persState.sec);
    clearInterval(persTimer);
    persTimer = setInterval(()=>{
      if(!persState) return;
      persState.sec++;
      const t = clFmtSec(persState.sec);
      $('cl-pers-status').textContent = t;
      const p = $('cl-pill-time'); if(p) p.textContent = t;
    }, 1000);
    /* мок анимации «говорит» */
    el.classList.add('talking');
  }, 1800);
};

async function clStartUserMedia(withVideo){
  try{
    if(!navigator.mediaDevices || !navigator.mediaDevices.getUserMedia){ clPipFallback(); return; }
    if(persStream){ try{ persStream.getTracks().forEach(t=>t.stop()); }catch(_){} persStream = null; }
    const st = await navigator.mediaDevices.getUserMedia({ video: withVideo ? {facingMode:'user'} : false, audio: true });
    if(!persState){ st.getTracks().forEach(t=>t.stop()); return; }
    persStream = st;
    const selfV = $('cl-pers-self');
    if(withVideo && selfV){
      selfV.srcObject = st;
      selfV.play && selfV.play().catch(()=>{});
      $('cl-pers-pip').classList.add('has-video');
    } else {
      $('cl-pers-pip').classList.remove('has-video');
    }
  }catch(err){
    clPipFallback();
  }
}
function clPipFallback(){
  const pip = $('cl-pers-pip'); if(!pip) return;
  pip.classList.remove('has-video');
  const fb = $('cl-pers-self-fb'); if(fb) fb.textContent = 'Разреши доступ к камере';
}
function clStopUserMedia(){
  if(persStream){ try{ persStream.getTracks().forEach(t=>t.stop()); }catch(_){} persStream = null; }
  const selfV = $('cl-pers-self'); if(selfV) selfV.srcObject = null;
  const pip = $('cl-pers-pip'); if(pip) pip.classList.remove('has-video');
}

function clUpdatePersButtons(){
  if(!persState) return;
  const s = persState;
  const set = (id, on, icon)=>{ const b=$(id); if(!b) return; b.classList.toggle('on', !!on); b.querySelector('use').setAttribute('href','#cl-i-'+icon); };
  set('cl-pers-mic', s.mic, s.mic?'mic':'mic-off');
  set('cl-pers-spk', s.spk, s.spk?'spk':'spk-off');
  set('cl-pers-cam', s.cam, s.cam?'cam':'cam-off');
  /* flip и cam видны только при видеозвонке */
  $('cl-pers-cam').style.display  = s.video ? '' : 'none';
  $('cl-pers-flip').style.display = s.video ? '' : 'none';
}

function clPersMin(){
  const el = $('cl-personal'); if(!el) return;
  el.classList.remove('on'); el.setAttribute('aria-hidden','true');
  document.body.style.overflow = '';
  clShowPill(persState && persState.name || 'Звонок');
  if(typeof nvPop==='function') nvPop('cl:pers');
}
function clPersRestore(){
  const el = $('cl-personal'); if(!el || !persState) return;
  el.classList.add('on'); el.setAttribute('aria-hidden','false');
  document.body.style.overflow = 'hidden'; clHidePill();
  if(typeof nvPush==='function') nvPush('cl:pers', ()=>clPersMin());
}
function clStopPersonalCall(silent){
  clearInterval(persTimer);
  clStopUserMedia();
  const el = $('cl-personal'); if(el){ el.classList.remove('on','video','video-remote','talking'); el.setAttribute('aria-hidden','true'); }
  document.body.style.overflow = '';
  clHidePill();
  const sec = persState ? persState.sec : 0;
  const wasVideo = persState ? persState.video : false;
  persState = null;
  if(typeof nvPop==='function') nvPop('cl:pers');
  if(silent) return;
  /* лог в чат */
  try{
    if(typeof currentChat!=='undefined' && currentChat && typeof pushMsg==='function'){
      pushMsg({in:0, t:(typeof nowT==='function'?nowT():''), kind:'sys',
        body:(wasVideo?'Видеозвонок':'Аудиозвонок')+' · '+(sec>0?clFmtSec(sec):'отменён')});
    }
  }catch(_){}
}

/* ===================================================================
   Плашка «свёрнутого» звонка
   =================================================================== */
function clShowPill(name){
  const pill = $('cl-pill'); if(!pill) return;
  $('cl-pill-name').textContent = name;
  const t = confState ? clFmtSec(confState.sec) : (persState ? clFmtSec(persState.sec) : '00:00');
  $('cl-pill-time').textContent = t;
  pill.classList.add('on'); pill.setAttribute('aria-hidden','false');
  pill.onclick = ()=>{
    if(confState) clConfRestore(); else if(persState) clPersRestore();
  };
}
function clHidePill(){ const p=$('cl-pill'); if(p){ p.classList.remove('on'); p.setAttribute('aria-hidden','true'); } }

/* ===================================================================
   Wiring кнопок оверлеев (single delegation после DOMReady)
   =================================================================== */
function clBind(){
  /* конференция */
  $('cl-conf-end') && ($('cl-conf-end').onclick = clConfEnd);
  $('cl-conf-min') && ($('cl-conf-min').onclick = clConfMin);
  $('cl-conf-mic') && ($('cl-conf-mic').onclick = ()=>{ if(!confState) return; confState.mic=!confState.mic; clUpdateConfButtons(); clRenderConfTiles(); });
  $('cl-conf-cam') && ($('cl-conf-cam').onclick = ()=>{ if(!confState) return; confState.cam=!confState.cam; clUpdateConfButtons(); clRenderConfTiles(); });
  $('cl-conf-share') && ($('cl-conf-share').onclick = ()=>{ if(!confState) return; confState.share=!confState.share; clUpdateConfButtons(); clToast(confState.share?'Демонстрация экрана включена':'Демонстрация экрана выключена'); });
  $('cl-conf-react') && ($('cl-conf-react').onclick = clFlyReaction);
  $('cl-conf-part') && ($('cl-conf-part').onclick = ()=>{ confState && (confState.side ? clCloseSide() : clOpenSide()); });
  $('cl-conf-chat') && ($('cl-conf-chat').onclick = ()=>{ if(!confState) return; if(!(confState.isAdmin||confState.policy.chat)){ clToast('Чат в созвоне отключён владельцем'); return; } clToast('Чат созвона откроется в следующем шаге'); });
  $('cl-conf-gear') && ($('cl-conf-gear').onclick = clOpenSettings);
  $('cl-conf-more') && ($('cl-conf-more').onclick = ()=>{ confState && confState.isAdmin ? clOpenSettings() : clToast('Настройки доступны админам'); });
  $('cl-side-close') && ($('cl-side-close').onclick = clCloseSide);
  $('cl-settings-close') && ($('cl-settings-close').onclick = clCloseSettings);

  /* личный */
  $('cl-pers-end') && ($('cl-pers-end').onclick = ()=>clStopPersonalCall(false));
  $('cl-pers-min') && ($('cl-pers-min').onclick = clPersMin);
  $('cl-pers-mic') && ($('cl-pers-mic').onclick = ()=>{
    if(!persState) return;
    persState.mic = !persState.mic;
    if(persStream){ try{ persStream.getAudioTracks().forEach(t=>t.enabled=persState.mic); }catch(_){} }
    clUpdatePersButtons();
  });
  $('cl-pers-spk') && ($('cl-pers-spk').onclick = ()=>{ if(!persState) return; persState.spk=!persState.spk; clUpdatePersButtons(); });
  $('cl-pers-cam') && ($('cl-pers-cam').onclick = ()=>{
    if(!persState) return;
    persState.cam = !persState.cam;
    if(persStream){ try{ persStream.getVideoTracks().forEach(t=>t.enabled=persState.cam); }catch(_){} }
    if(persState.cam && !persStream){ clStartUserMedia(true); }
    clUpdatePersButtons();
  });
  $('cl-pers-flip') && ($('cl-pers-flip').onclick = ()=>{
    if(!persState || !persStream) return;
    persState._face = persState._face==='environment' ? 'user' : 'environment';
    try{ persStream.getTracks().forEach(t=>t.stop()); }catch(_){}
    persStream = null;
    if(!navigator.mediaDevices || !navigator.mediaDevices.getUserMedia){ return; }
    navigator.mediaDevices.getUserMedia({video:{facingMode:persState._face}, audio:true}).then(st=>{
      if(!persState){ st.getTracks().forEach(t=>t.stop()); return; }
      persStream = st;
      const v = $('cl-pers-self'); if(v){ v.srcObject = st; v.play&&v.play().catch(()=>{}); }
      $('cl-pers-pip').classList.add('has-video');
    }).catch(()=>{ clPipFallback(); });
  });
}

/* ===================================================================
   Chain-patch существующего startCall() → callStartPersonal()
   =================================================================== */
function clPatchCore(){
  /* блокируем родной callScreen */
  const legacy = $('callScreen'); if(legacy) legacy.style.display = 'none';

  if(typeof window.startCall === 'function'){
    window.startCall = function(video){
      const id = (typeof currentChat!=='undefined' && currentChat) ? currentChat.id : null;
      if(id==null){ return; }
      window.callStartPersonal(id, !!video);
    };
  }
  if(typeof window.endCall === 'function'){
    window.endCall = function(){ if(persState) clStopPersonalCall(false); if(confState) clConfEnd(); };
  }
}

/* ===================================================================
   Инъекция «Начать созвон» в шапку группы/канала
   =================================================================== */
function clInjectStartBtn(){
  const head = document.querySelector('#convBody .conv-head'); if(!head) return;
  const ch = (typeof currentChat!=='undefined' && currentChat) ? currentChat : null;
  const existing = head.querySelector('.cl-conv-start');

  const isGroup = ch && (ch.kind==='group' || ch.kind==='channel');
  /* Правка Даниэля: «обычные участники в общий чат око и в канал око не могут
     звонить, да и в целом если не админы — звонить не могут».
     Здесь стояло `ch.owner || ch.kind==='channel'`. Но `owner:true` у наших
     чатов означает «этой сущностью владеет OKO», а не «этот человек — админ».
     Из-за этого кнопка «Созвон» показывалась в общем чате и канале ВСЕМ.
     Право начинать созвон есть только у того, кто чат создал (managed),
     и у владельца приложения. */
  const canAdmin = ch && (
    ch.managed ||
    (typeof cpCanManage === 'function' && cpCanManage())
  );

  if(!isGroup || !canAdmin){
    if(existing) existing.remove();
    return;
  }
  if(existing) return;

  const btn = document.createElement('button');
  btn.className = 'cl-conv-start'; btn.type = 'button';
  /* Иконка + подпись: на узкой шапке подпись прячется через CSS, чтобы
     название чата не превращалось в «Общий ч...». */
  btn.innerHTML = `<i class="cl-live"></i><span class="cl-conv-start-tx">Созвон</span>`;
  btn.title = 'Начать общий созвон';
  btn.setAttribute('aria-label', 'Начать общий созвон');
  btn.onclick = ()=>{ callStartConf(ch.id, true, {chatName:ch.name}); };
  /* вставляем между инфо и родными кнопками звонка */
  const firstCall = head.querySelector('.ch-call');
  if(firstCall) head.insertBefore(btn, firstCall);
  else head.appendChild(btn);
  /* Дубль убираем здесь, а не в cpDecorateCallButtons: тот отрабатывает
     РАНЬШЕ этой вставки и ещё не видит новую кнопку. Две одинаковые кнопки
     группового созвона съедали 39px шапки — из-за них имя чата обрезалось. */
  const oldHost = head.querySelector('.cp-call-host');
  if(oldHost) oldHost.remove();
}

/* хук на openConv/closeConv для перерисовки кнопки */
function clHookOpenConv(){
  if(typeof window.openConv === 'function' && !window.openConv._clPatched){
    const _prev = window.openConv;
    window.openConv = function(id){ _prev.apply(this, arguments); try{ clInjectStartBtn(); }catch(_){} };
    window.openConv._clPatched = true;
  }
  if(typeof window.closeConv === 'function' && !window.closeConv._clPatched){
    const _prev = window.closeConv;
    window.closeConv = function(){
      /* при выходе из чата — не рвём активный звонок, только чистим кнопку */
      const head = document.querySelector('#convBody .conv-head');
      const b = head && head.querySelector('.cl-conv-start'); if(b) b.remove();
      return _prev.apply(this, arguments);
    };
    window.closeConv._clPatched = true;
  }
}

/* ===================================================================
   INIT
   =================================================================== */
function clInit(){
  clBind();
  clPatchCore();
  clHookOpenConv();
  /* если чат уже открыт при первом заходе */
  try{ clInjectStartBtn(); }catch(_){}
}
if(document.readyState==='loading') document.addEventListener('DOMContentLoaded', clInit);
else clInit();

})();

/* ===== module: profile-social ===== */
/* ================= PROFILE-SOCIAL (ps-): чужие профили, подписки, соцграф ================= */
/* Fullscreen-профиль любого автора (#psView): аватар, имя + verified, ник, био,
   статистика, Подписаться/Отписаться (персист oko-social), Написать (личный чат),
   лента постов автора. Точки входа: имя/аватар в ленте (делегирование на #feedList),
   аватар в шапке конва (кроме своих/каналов), автор в глобальном поиске.
   Подписки поднимают посты автора выше во вкладке «Подписки» (стабильная
   пересортировка POSTS.sub) и дают буст в рекомендациях (chain feedScore). */

const PS = {
  cur: null,        /* имя автора, чей профиль открыт */
  follow: {},       /* {имя: ts подписки} — персист в oko-social */
  graphName: null,  /* автор, чей соцграф открыт */
  graphTab: 'followers',
  tab: 'posts',     /* активная вкладка профиля: posts | media */
};

/* ---------- персист ---------- */
function psLoadState(){
  try{
    const s = JSON.parse(localStorage.getItem('oko-social') || 'null');
    if(s && s.follow && typeof s.follow === 'object') PS.follow = s.follow;
  }catch(e){}
}
function psSaveState(){
  try{ localStorage.setItem('oko-social', JSON.stringify({follow: PS.follow, at: Date.now()})); }catch(e){}
}
function psIsFollowing(name){ return !!PS.follow[name]; }

/* ---------- утилиты ---------- */
function psHash(s){
  let h = 2166136261;
  for(let i = 0; i < String(s).length; i++){ h ^= String(s).charCodeAt(i); h = Math.imul(h, 16777619); }
  return h >>> 0;
}
function psFmt(n){ try{ return Math.round(n).toLocaleString('ru-RU'); }catch(e){ return String(n); } }
function psAttr(s){ return esc(s).replace(/'/g, '&#39;'); }
function psInitials(name){
  return String(name).trim().split(/\s+/).slice(0, 2).map(w => (w[0]||'').toUpperCase()).join('') || 'U';
}
const PS_TRANSLIT = {'а':'a','б':'b','в':'v','г':'g','д':'d','е':'e','ё':'e','ж':'zh','з':'z','и':'i','й':'y','к':'k','л':'l','м':'m','н':'n','о':'o','п':'p','р':'r','с':'s','т':'t','у':'u','ф':'f','х':'h','ц':'c','ч':'ch','ш':'sh','щ':'sch','ъ':'','ы':'y','ь':'','э':'e','ю':'yu','я':'ya'};
function psNick(name){
  const raw = String(name).toLowerCase().replace(/[^a-zа-яё0-9]+/g, '')
    .split('').map(ch => PS_TRANSLIT[ch] !== undefined ? PS_TRANSLIT[ch] : ch).join('');
  return (raw.slice(0, 14) || 'user' + (psHash(name) % 999));
}
function psAgo(p){
  if(!p.ts) return 'недавно';
  const h = Math.max(1, Math.round((Date.now() - p.ts) / 36e5));
  if(h < 24) return h + ' ч назад';
  const d = Math.round(h / 24);
  return d + ' ' + (d === 1 ? 'день' : (d < 5 ? 'дня' : 'дней')) + ' назад';
}
/* id поста из карточки ленты (по onclick кнопки меню — не ломая чужой DOM) */
function psPostIdOf(art){
  const b = art && art.querySelector('.post-more');
  const m = b && (b.getAttribute('onclick') || '').match(/openPostMenu\((\d+)/);
  return m ? +m[1] : null;
}

/* ---------- био: рукописные для известных авторов + генератор ---------- */
const PS_BIOS = {}  /* демо-данные удалены 09.08: био выдуманных авторов — источник только API */;
const PS_BIO_TOPIC = {
  ai:       'Пишу про инструменты OKO без магии: пайплайны, промпты и локальные модели в деле.',
  content:  'Контент как система: хуки, монтаж, удержание. Разбираю на живых роликах.',
  business: 'Считаю юнит-экономику и строю процессы, которые работают без собственника.',
  marketing:'Перформанс и креативы: связки, тесты, CPL. Только то, что пережило A/B.',
  games:    'Геймдев маленькой командой: прототипы, плейтесты и честные цифры ретеншна.',
  crypto:   'Слежу за TON и ончейн-метриками. Без сигналов — только данные и риски.',
};
const PS_BIO_POOL = [
  'Автор OKO. Делюсь опытом и цифрами из своих проектов — без воды.',
  'Веду канал о своём деле: процессы, ошибки и что в итоге сработало.',
  'Практик, а не теоретик: каждый пост — из реальной задачи.',
  'Собираю здесь то, что помогает расти: приёмы, инструменты, разборы.',
];
function psBio(name, topic, sub){
  if(PS_BIOS[name]) return PS_BIOS[name];
  if(topic && PS_BIO_TOPIC[topic]) return PS_BIO_TOPIC[topic];
  const s = String(sub || '').toLowerCase();
  if(s.indexOf('smm') > -1) return PS_BIO_TOPIC.marketing;
  if(s.indexOf('монтаж') > -1) return PS_BIO_TOPIC.content;
  if(s.indexOf('сообщество') > -1) return 'Живое сообщество в OKO: обсуждения, разборы и взаимопомощь участников.';
  return PS_BIO_POOL[psHash(name) % PS_BIO_POOL.length];
}

/* ---------- «Актуальное»: сторис-кружки (Instagram highlights) ----------
   Каталог рубрик: label → иконка + текст мини-«истории». Набор для автора —
   детерминированный (команда/тема влияют на состав). */
const PS_HL = {
  'Анонсы':   {ic:'megaphone',   txt:'Свежие анонсы сборок и новых разделов OKO — что уже вышло и что дальше в работе.'},
  'Фичи':     {ic:'bolt',        txt:'Разборы новых возможностей приложения: как включить, где найти и зачем оно нужно.'},
  'Ответы':   {ic:'chat',        txt:'Частые вопросы и быстрые ответы. Сложные случаи передаём живой команде.'},
  'Кейсы':    {ic:'fire',        txt:'Живые кейсы с цифрами: было / стало, воронка и что именно сработало.'},
  'Отзывы':   {ic:'heart',       txt:'Реальные отклики подписчиков и клиентов — без накрутки и постановки.'},
  'Услуги':   {ic:'briefcase',   txt:'Чем помогаю, форматы работы, сроки и как считается результат.'},
  'Гайды':    {ic:'compass',     txt:'Пошаговые гайды и чек-листы из практики. Открой и сохрани себе.'},
  'Процесс':  {ic:'circle-play', txt:'Как всё устроено изнутри: инструменты, этапы и рабочий процесс.'},
  'Лучшее':   {ic:'star',        txt:'Подборка самых полезных публикаций автора в одном месте.'},
  'Нейросети':{ic:'rocket',      txt:'Пайплайны, промпты и локальные модели в деле — без магии и воды.'},
  'Монтаж':   {ic:'circle-play', txt:'Приёмы монтажа вертикалок: хук, ритм, караоке-субтитры, удержание.'},
  'Рост':     {ic:'rocket',      txt:'Метрики, воронки и что двигает рост. Цифры вместо вдохновения.'},
};
/* «портфолио» для медиа-сетки — детерминированные плитки-работы автора */
const PS_PORTFOLIO = []  /* демо-данные удалены 09.08: портфолио выдуманных авторов — источник только API */;

/* ---------- модель автора: посты + чаты + детерминированная статистика ---------- */
function psAuthor(name){
  const posts = [];
  try{ [...POSTS.sub, ...POSTS.rec].forEach(p => { if(p.name === name) posts.push(p); }); }catch(e){}
  posts.sort((a, b) => (b.ts || 0) - (a.ts || 0));
  const p0 = posts[0] || null;
  const chat = (typeof CHATS !== 'undefined') ? CHATS.find(c => c.kind === 'direct' && c.name === name) : null;
  const anyChat = chat || ((typeof CHATS !== 'undefined') ? CHATS.find(c => c.name === name) : null);
  const h = psHash(name);
  const sub = (p0 && p0.sub) || '';

  let followers = 0;
  for(const p of posts){ /* аудитория из любого поста автора: «канал · 48.2к» */
    const m = String(p.sub || '').match(/(\d+(?:[.,]\d+)?)\s*к/i);
    if(m){ followers = Math.round(parseFloat(m[1].replace(',', '.')) * 1000); break; }
  }
  if(!followers && anyChat && anyChat.subs){
    const mm = String(anyChat.subs).match(/(\d+(?:[.,]\d+)?)\s*к/i);
    if(mm) followers = Math.round(parseFloat(mm[1].replace(',', '.')) * 1000);
  }
  if(!followers) followers = 300 + h % 4200;

  return {
    name, posts, sub,
    nick: (chat && chat.nick) ? chat.nick : psNick(name),
    online: chat ? !!chat.online : (h % 3 === 0),
    followers,
    followingN: 12 + h % 480,
    ava: (p0 && p0.ava) || (anyChat && anyChat.ava) || psInitials(name),
    avaIcon: (!(p0 && p0.ava) && anyChat && anyChat.avaIcon) ? anyChat.avaIcon : null,
    topic: (posts.map(p => p.topic).find(Boolean)) || null,
  };
}

/* ---------- обложка: детерминированный вариант композиции ---------- */
function psCoverClass(name){ return 'v' + (psHash('cov:' + name) % 5); } /* v0..v4 (v0 = базовый) */

/* ---------- ачивки автора (детерминированно из модели) ---------- */
function psAchievements(a){
  const h = psHash('ach:' + a.name);
  const out = [];
  const verified = (typeof VERIFIED !== 'undefined' && VERIFIED.has(a.name));
  const team = /OKO|Клуб OKO|Поддержка|Партнёрам|Биржа/.test(a.name);
  if(team) out.push({ic:'crown', label:'Команда OKO', gold:true});
  if(verified) out.push({ic:'verified', label:'Проверенный'});
  /* аудитория */
  if(a.followers >= 20000) out.push({ic:'fire', label:'Топ-аудитория', gold:true});
  else if(a.followers >= 5000) out.push({ic:'users', label:'Растущий канал'});
  /* активность */
  if(a.posts.length >= 6) out.push({ic:'bolt', label:'Активный автор'});
  else if(a.posts.length >= 2) out.push({ic:'edit', label:'Автор OKO'});
  /* тематика */
  const topicBadge = {
    ai:      {ic:'rocket',    label:'Эксперт OKO'},
    content: {ic:'circle-play',label:'Контент-мейкер'},
    business:{ic:'briefcase', label:'Предприниматель'},
    marketing:{ic:'megaphone',label:'Маркетолог'},
    games:   {ic:'rocket',    label:'Геймдев'},
    crypto:  {ic:'money',     label:'Крипто-аналитик'},
  }[a.topic];
  if(topicBadge) out.push(topicBadge);
  /* добор до минимум 3 из стабильного пула (детерминированно по хэшу, без повторов) */
  const pool = [
    {ic:'star',  label:'Ветеран OKO'},
    {ic:'flag',  label:'Ранний участник'},
    {ic:'heart', label:'Любимец подписчиков'},
    {ic:'thumb', label:'Проверено делом'},
    {ic:'clock', label:'Всегда на связи'},
    {ic:'rocket',label:'Быстрый рост'},
  ];
  const start = h % pool.length;
  for(let i = 0; out.length < 3 && i < pool.length; i++) out.push(pool[(start + i) % pool.length]);
  /* dedupe по label, максимум 4 — чтобы лента не обрезалась даже на десктопе (640px) */
  const seen = new Set();
  return out.filter(b => !seen.has(b.label) && seen.add(b.label)).slice(0, 4);
}
function psAchHtml(a){
  const list = psAchievements(a);
  if(!list.length) return '';
  return `<div class="ps-ach">` + list.map((b, i) =>
    `<span class="ps-badge${b.gold ? ' gold' : ''}" style="animation-delay:${40 + i * 55}ms">
       <span class="ps-bi">${I(b.ic)}</span>${esc(b.label)}
     </span>`).join('') + `</div>`;
}

/* ================= СОЦ-ДОКАЗАТЕЛЬСТВО: «Читают …» (Instagram-style) ================= */
/* «Читают Марк, Алина и ещё N» — 2 знакомых человека из пула + остаток.
   Тап ведёт в соцграф подписчиков. Показывается только если есть кого показать. */
function psSocialProofHtml(a){
  let pool = [];
  try{ pool = psPeoplePool().filter(p => p.name !== a.name); }catch(e){}
  if(!pool.length) return '';
  const salt = psHash('proof:' + a.name);
  const ranked = pool.map(p => ({p, w: psHash(p.name + '~' + salt)})).sort((x, y) => x.w - y.w).map(x => x.p);
  const show = ranked.slice(0, Math.min(3, ranked.length));
  const rest = Math.max(0, a.followers - show.length);
  const avas = show.map((p, i) =>
    `<span class="ps-sp-ava" style="z-index:${show.length - i}">${p.avaIcon ? I(p.avaIcon) : esc(p.ava || psInitials(p.name))}</span>`).join('');
  const names = show.slice(0, 2).map(p => `<b>${esc(p.name)}</b>`).join(', ');
  const tail = rest > 0 ? ` и ещё ${psFmt(rest)}` : '';
  return `<button class="ps-social-proof" onclick="psOpenGraph('followers')" aria-label="Показать подписчиков">
    <span class="ps-sp-avas">${avas}</span>
    <span class="ps-sp-txt">Читают ${names}${tail}</span>
  </button>`;
}

/* ================= «АКТУАЛЬНОЕ»: сторис-кружки ================= */
function psHighlights(a){
  const out = [];
  const seen = new Set();
  const add = (label) => { if(label && PS_HL[label] && !seen.has(label)){ seen.add(label); out.push(label); } };
  const team = /OKO|Клуб|Поддержка|Партнёрам|Биржа/.test(a.name);
  if(team){ add('Анонсы'); add('Фичи'); add('Ответы'); }
  const topicHl = {ai:'Нейросети', content:'Монтаж', business:'Рост', marketing:'Рост', games:'Процесс', crypto:'Рост'}[a.topic];
  add(topicHl);
  /* добор из общего пула детерминированно */
  const pool = ['Кейсы', 'Отзывы', 'Услуги', 'Гайды', 'Лучшее', 'Процесс'];
  const start = psHash('hl:' + a.name) % pool.length;
  for(let i = 0; out.length < 5 && i < pool.length; i++) add(pool[(start + i) % pool.length]);
  return out.slice(0, 6);
}
function psHlHtml(a){
  const list = psHighlights(a);
  if(!list.length) return '';
  return `<div class="ps-hls" role="list">` + list.map((label, i) => {
    const h = PS_HL[label];
    return `<button class="ps-hl" role="listitem" style="animation-delay:${60 + i * 45}ms"
        onclick="psOpenHighlight('${psAttr(label)}')" aria-label="Актуальное: ${psAttr(label)}">
      <span class="ps-hl-ring"><span class="ps-hl-in">${I(h.ic)}</span></span>
      <span class="ps-hl-lb">${esc(label)}</span>
    </button>`;
  }).join('') + `</div>`;
}
function psOpenHighlight(label){
  const h = PS_HL[label];
  if(!h || typeof showPopup !== 'function') return;
  const name = PS.cur || '';
  const g = 'g' + (psHash('hl:' + label + name) % 6);
  showPopup({
    title: label,
    body: `<div class="ps-story ${g}">
        <span class="ps-story-ic">${I(h.ic)}</span>
        <span class="ps-story-tag">Актуальное</span>
      </div>
      <p class="ps-story-txt">${esc(h.txt)}</p>`,
    actions: [{label:'Понятно'}]
  });
}

/* ================= МЕДИА-СЕТКА (Instagram grid) ================= */
/* детерминированный список плиток: реальные посты автора + «портфолио»-добор */
function psMediaList(a){
  const tiles = [];
  a.posts.forEach(p => {
    tiles.push({
      real: true, id: p.id,
      ic: p.media ? 'circle-play' : 'txt',
      dur: (p.media && /:/.test(String(p.media))) ? p.media : null,
      views: p.views || 0, likes: p.likes || 0,
      title: (String(p.body || '').split(/[.!?\n]/)[0] || 'Пост').slice(0, 48),
      txt: String(p.body || ''),
      g: 'g' + (psHash('mt:' + p.id) % 6)
    });
  });
  const base = psHash('media:' + a.name);
  for(let i = 0; tiles.length < 6 && i < PS_PORTFOLIO.length; i++){
    const pt = PS_PORTFOLIO[(base + i) % PS_PORTFOLIO.length];
    if(tiles.some(t => t.title === pt.title)) continue;
    tiles.push({
      real: false, ic: pt.ic, title: pt.title, txt: pt.txt, dur: null,
      views: 800 + psHash(a.name + pt.title) % 42000,
      likes: 30 + psHash(pt.title + a.name) % 1900,
      g: 'g' + ((base + i) % 6)
    });
  }
  return tiles.slice(0, 9);
}
function psMediaHtml(a){
  const list = psMediaList(a);
  if(!list.length) return `<div class="ps-empty">${I('grid')}<p>Медиа пока нет</p><span>Кейсы, ролики и работы автора появятся здесь</span></div>`;
  const nf = v => (typeof fmtN === 'function' ? fmtN(v) : v);
  return `<div class="ps-grid">` + list.map((t, i) => `
    <button class="ps-mtile ${t.g} fade-in" style="animation-delay:${i * 35}ms" onclick="psOpenMedia(${i})" aria-label="${psAttr(t.title)}">
      <span class="ps-mt-ic">${I(t.ic)}</span>
      ${t.dur ? `<span class="ps-mt-dur">${I('circle-play')}${esc(t.dur)}</span>` : ''}
      <span class="ps-mt-stat">${I('eye')}${nf(t.views)}</span>
    </button>`).join('') + `</div>`;
}
function psOpenMedia(i){
  if(!PS.cur || typeof showPopup !== 'function') return;
  const a = psAuthor(PS.cur);
  const t = psMediaList(a)[i];
  if(!t) return;
  const nf = v => (typeof fmtN === 'function' ? fmtN(v) : v);
  const actions = [];
  if(t.real) actions.push({label:'Открыть в постах', onclick: () => {
    psTab('posts');
    try{ const el = document.getElementById('psPosts'); if(el && el.scrollIntoView) el.scrollIntoView({behavior:'smooth', block:'start'}); }catch(e){}
  }});
  actions.push({label:'Закрыть', ghost:true});
  showPopup({
    title: t.title,
    body: `<div class="ps-story ${t.g}">
        <span class="ps-story-ic">${I(t.ic)}</span>
        <span class="ps-lb-meta"><span>${I('eye')}${nf(t.views)}</span><span>${I('heart')}${nf(t.likes)}</span></span>
      </div>
      <p class="ps-story-txt">${esc(t.txt)}</p>`,
    actions
  });
}

/* ================= ВКЛАДКИ: Посты / Медиа ================= */
function psTabHtml(a, tab){
  if(tab === 'media') return psMediaHtml(a);
  return `<div id="psPosts">${psPostsHtml(a)}</div>`;
}
function psTab(tab){
  tab = (tab === 'media') ? 'media' : 'posts';
  PS.tab = tab;
  if(!PS.cur) return;
  const a = psAuthor(PS.cur);
  const body = document.getElementById('psTabBody');
  if(body) body.innerHTML = psTabHtml(a, tab);
  const tabs = document.getElementById('psTabs');
  if(tabs) tabs.querySelectorAll('button').forEach(b => b.classList.toggle('on', b.dataset.pt === tab));
}

/* ---------- count-up статистики при открытии профиля ---------- */
function psAnimateStats(){
  const reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion:reduce)').matches;
  document.querySelectorAll('#psBody .ps-stat b[data-to]').forEach(el => {
    const to = +el.getAttribute('data-to'); if(!isFinite(to)) return;
    if(reduce || to <= 0){ el.textContent = psFmt(to); return; }
    const dur = 650, t0 = performance.now();
    (function step(now){
      const k = Math.min(1, (now - t0) / dur);
      const e = 1 - Math.pow(1 - k, 3); /* easeOutCubic */
      el.textContent = psFmt(to * e);
      if(k < 1) requestAnimationFrame(step); else el.textContent = psFmt(to);
    })(t0);
  });
}

/* ---------- рендер профиля ---------- */
function psPostsHtml(a){
  if(!a.posts.length) return `<div class="ps-empty">${I('feed')}<p>Здесь скоро будут посты</p><span>Подпишись — и не пропустишь первую публикацию автора</span></div>`;
  return a.posts.map(p => `
    <article class="ps-post fade-in">
      <div class="ps-post-time">${psAgo(p)}${p.promoted ? ' · реклама' : ''}</div>
      <div class="ps-post-body">${p.body}</div>
      ${p.media ? `<div class="ps-post-media">${I('circle-play')}<span>${p.media}</span></div>` : ''}
      <div class="ps-post-meta">
        <button class="ps-like ${p.liked ? 'on' : ''}" onclick="psLike(${p.id})">${I('heart')}<span>${p.likes}</span></button>
        <span>${I('comment')}${p.comments ? p.comments.length : 0}</span>
        <span>${I('share')}${p.reposts || 0}</span>
        <span class="ps-views">${I('eye')}${typeof fmtN === 'function' ? fmtN(p.views || 0) : (p.views || 0)}</span>
      </div>
    </article>`).join('');
}
function psRender(name){
  const a = psAuthor(name);
  const f = psIsFollowing(name);
  PS.tab = 'posts';
  const head = document.getElementById('psHeadName');
  if(head) head.textContent = '@' + a.nick;
  const body = document.getElementById('psBody');
  if(!body) return;
  body.innerHTML = `
    <div class="ps-cover ${psCoverClass(a.name)} fade-in">
      <div class="ps-cover-grid"></div>
      <div class="ps-cover-mark">${I('logo')}</div>
    </div>
    <div class="ps-top fade-in">
      <div class="ps-ava-wrap">
        <div class="ps-ava">${a.avaIcon ? I(a.avaIcon) : esc(a.ava)}</div>
        ${a.online ? '<span class="ps-ava-on" title="в сети"></span>' : ''}
      </div>
      <h3 class="ps-name">${esc(a.name)}${typeof vBadge === 'function' ? vBadge(a.name) : ''}</h3>
      <div class="ps-nick">@${esc(a.nick)}${a.online ? '<span class="ps-dot">·</span><span class="ps-on">в сети</span>' : ''}</div>
      <p class="ps-bio">${esc(psBio(a.name, a.topic, a.sub))}</p>
      ${psAchHtml(a)}
      ${psSocialProofHtml(a)}
      <div class="ps-stats">
        <div class="ps-stat"><b data-to="${a.posts.length}">${a.posts.length}</b><small>постов</small></div>
        <button class="ps-stat ps-stat-btn" onclick="psOpenGraph('followers')" aria-label="Показать подписчиков"><b id="psFollowers" data-to="${a.followers + (f ? 1 : 0)}">${psFmt(a.followers + (f ? 1 : 0))}</b><small>подписчиков</small></button>
        <button class="ps-stat ps-stat-btn" onclick="psOpenGraph('following')" aria-label="Показать подписки"><b data-to="${a.followingN}">${psFmt(a.followingN)}</b><small>подписок</small></button>
      </div>
      <div class="ps-actions">
        <button class="btn ${f ? 'ghost' : ''}" id="psFollowBtn" onclick="psToggleFollow()">${I(f ? 'check' : 'plus')} ${f ? 'Отписаться' : 'Подписаться'}</button>
        <button class="btn ghost" onclick="psMessage()">${I('chat')} Написать</button>
        <button class="btn ghost ps-share-btn" onclick="psShare()" aria-label="Поделиться профилем" title="Поделиться">${I('share')}</button>
      </div>
    </div>
    ${psHlHtml(a)}
    <div class="ps-tabs" id="psTabs" role="tablist">
      <button class="on" data-pt="posts" role="tab" aria-selected="true" onclick="psTab('posts')">${I('feed')}<span>Посты</span><small class="ps-tab-cnt">${a.posts.length}</small></button>
      <button data-pt="media" role="tab" aria-selected="false" onclick="psTab('media')">${I('grid')}<span>Медиа</span></button>
    </div>
    <div id="psTabBody">${psTabHtml(a, 'posts')}</div>`;
}

/* ---------- открыть/закрыть вьюху ---------- */
function psOpenProfile(name){
  if(!name) return;
  if(typeof PROFILE !== 'undefined' && name === PROFILE.name){ /* свой профиль — родная вкладка */
    if(typeof showTab === 'function') showTab('profile');
    return;
  }
  PS.cur = name;
  psRender(name);
  try{ psAnimateStats(); }catch(e){}
  const v = document.getElementById('psView');
  if(v && !v.classList.contains('open')){
    v.classList.add('open');
    if(typeof nvPush === 'function') nvPush('view:psProfile', psClose);
  }
}
function psClose(){
  const v = document.getElementById('psView');
  if(v) v.classList.remove('open');
  PS.cur = null;
  if(typeof nvPop === 'function') nvPop('view:psProfile');
}

/* ---------- подписка / отписка ---------- */
function psGoSubFeed(){
  psClose();
  if(typeof showTab === 'function') showTab('feed');
  /* именно вкладка «Подписки», а не первая кнопка в ряду:
     с 09.08 первой идёт «Рекомендации». */
  const btn = document.querySelector('.feed-tabs button[data-fk="sub"]');
  if(btn && typeof feedTab === 'function') feedTab(btn, 'sub');
  else if(typeof renderFeed === 'function') renderFeed('sub');
}
/* переключить подписку на произвольное имя (без привязки к открытому профилю) */
function psSetFollow(name, want){
  if(!name) return false;
  const f = (typeof want === 'boolean') ? want : !psIsFollowing(name);
  if(f) PS.follow[name] = Date.now(); else delete PS.follow[name];
  psSaveState();
  if(typeof renderFeed === 'function' && typeof curFeedKind !== 'undefined' && curFeedKind === 'sub'){
    try{ renderFeed('sub'); }catch(e){}
  }
  return f;
}
function psToggleFollow(){
  const name = PS.cur;
  if(!name) return;
  const f = !psIsFollowing(name);
  if(f) PS.follow[name] = Date.now(); else delete PS.follow[name];
  psSaveState();

  /* счётчик подписчиков — с бампом */
  const a = psAuthor(name);
  const el = document.getElementById('psFollowers');
  if(el){
    el.textContent = psFmt(a.followers + (f ? 1 : 0));
    el.classList.remove('ps-bump'); void el.offsetWidth; el.classList.add('ps-bump');
  }
  const btn = document.getElementById('psFollowBtn');
  if(btn){
    btn.classList.toggle('ghost', f);
    btn.innerHTML = I(f ? 'check' : 'plus') + ' ' + (f ? 'Отписаться' : 'Подписаться');
  }
  /* лента «Подписки» пересортировывается сразу, если открыта */
  if(typeof renderFeed === 'function' && typeof curFeedKind !== 'undefined' && curFeedKind === 'sub'){
    try{ renderFeed('sub'); }catch(e){}
  }
  if(f && typeof showPopup === 'function'){
    showPopup({ico:'users', title:'Подписка оформлена',
      body:`Готово. Лучшие посты <b>${esc(name)}</b> теперь поднимаются выше в ленте «Подписки» — ничего важного не пропустишь.`,
      actions:[{label:'В ленту', onclick: psGoSubFeed}, {label:'Понятно', ghost:true}]});
  } else if(!f && typeof toast === 'function'){
    toast('Ты отписался от ' + name);
  }
}

/* ---------- «Написать»: найти/создать личный чат + openConv ---------- */
function psMessage(){
  const name = PS.cur;
  if(!name || typeof CHATS === 'undefined') return;
  if(typeof st2IsBlocked === 'function' && st2IsBlocked(name)){
    if(typeof toast === 'function') toast('Разблокируйте пользователя, чтобы написать');
    return;
  }
  const a = psAuthor(name);
  let c = CHATS.find(x => x.kind === 'direct' && x.name === name);
  if(!c){
    c = {id: Date.now(), name, kind:'direct', nick: a.nick, kindIcon: null,
      preview:'Чат создан', time:(typeof nowT === 'function' ? nowT() : ''), unread:0, online: !!a.online,
      msgs:[{kind:'sys', body:'Личный чат с ' + name + ' создан'}]};
    if(a.avaIcon) c.avaIcon = a.avaIcon; else c.ava = a.ava;
    CHATS.unshift(c);
  }
  psClose();
  if(typeof showTab === 'function') showTab('chats');
  try{
    if(typeof renderChatList === 'function'){
      const s = document.getElementById('chatSearch');
      renderChatList(s ? s.value : '');
    }
  }catch(e){}
  if(typeof openConv === 'function') openConv(c.id);
}

/* ---------- лайк из профиля (через ядро — сигналы feed-algo сохраняются) ---------- */
function psLike(id){
  if(typeof likePost === 'function') likePost(id);
  const wrap = document.getElementById('psPosts');
  if(wrap && PS.cur) wrap.innerHTML = psPostsHtml(psAuthor(PS.cur));
}

/* ---------- «Поделиться» профилем: ссылка + копирование ---------- */
function psProfileLink(name){
  const a = psAuthor(name);
  return 'https://okoteam.top/@' + a.nick;
}
function psCopy(text){
  try{ if(navigator.clipboard && navigator.clipboard.writeText){ navigator.clipboard.writeText(text); return true; } }catch(e){}
  try{
    const ta = document.createElement('textarea');
    ta.value = text; ta.setAttribute('readonly',''); ta.style.position='fixed'; ta.style.opacity='0';
    document.body.appendChild(ta); ta.select(); document.execCommand('copy'); document.body.removeChild(ta);
    return true;
  }catch(e){ return false; }
}
function psShare(){
  const name = PS.cur;
  if(!name) return;
  const link = psProfileLink(name);
  const doCopy = () => { const ok = psCopy(link); if(typeof toast === 'function') toast(ok ? 'Ссылка скопирована' : 'Не удалось скопировать'); };
  /* нативный шэринг телефона, если доступен */
  if(navigator.share){
    navigator.share({title: name + ' в OKO', text: 'Профиль ' + name + ' в OKO', url: link}).catch(()=>{});
    return;
  }
  if(typeof showPopup === 'function'){
    showPopup({ico:'share', title:'Поделиться профилем',
      body:`Профиль <b>${esc(name)}</b> в OKO.<div class="ps-sharelink">${I('globe')}<span>${esc(link)}</span></div>`,
      actions:[{label:'Скопировать ссылку', onclick: doCopy}, {label:'Закрыть', ghost:true}]});
  } else { doCopy(); }
}

/* ---------- меню профиля: поделиться / блокировка / жалоба ---------- */
function psRefreshFollowBtn(){
  const name = PS.cur;
  if(!name) return;
  const f = psIsFollowing(name);
  const btn = document.getElementById('psFollowBtn');
  if(btn){ btn.classList.toggle('ghost', f); btn.innerHTML = I(f ? 'check' : 'plus') + ' ' + (f ? 'Отписаться' : 'Подписаться'); }
}
function psBlock(name){
  if(typeof st2Block !== 'function'){ if(typeof toast === 'function') toast('Блокировки недоступны'); return; }
  st2Block(name);                 /* сам снимает подписку + чистит ленту + персист */
  psRefreshFollowBtn();
  if(typeof toast === 'function') toast('Вы заблокировали ' + name);
}
function psUnblock(name){
  if(typeof st2Unblock === 'function'){ st2Unblock(name); if(typeof toast === 'function') toast('Вы разблокировали ' + name); }
}
function psReport(name){ if(typeof toast === 'function') toast('Жалоба на ' + name + ' отправлена на модерацию'); }
function psMore(){
  const name = PS.cur;
  if(!name) return;
  const blocked = (typeof st2IsBlocked === 'function') && st2IsBlocked(name);
  const actions = [{label:'Поделиться профилем', onclick: psShare}];
  if(blocked) actions.push({label:'Разблокировать', onclick: () => psUnblock(name)});
  else        actions.push({label:'Заблокировать', onclick: () => psBlock(name)});
  actions.push({label:'Пожаловаться', ghost:true, onclick: () => psReport(name)});
  if(typeof showPopup === 'function') showPopup({ico:'more', title: name, body:'Действия с профилем', actions});
  const ok = document.querySelector('#okoPopup [data-pa="1"]');
  if(ok && !blocked) ok.classList.add('ps-btn-danger');
}

/* ================= СОЦГРАФ: подписчики / подписки ================= */
/* пул реальных людей приложения (чаты + авторы ленты + известные каналы) */
function psPeoplePool(){
  const seen = new Set(); const out = [];
  const push = (name, ava, avaIcon, nick, online) => {
    if(!name || seen.has(name)) return; seen.add(name);
    if(typeof PROFILE !== 'undefined' && name === PROFILE.name) return;
    out.push({name, ava: ava || psInitials(name), avaIcon: avaIcon || null,
      nick: nick || psNick(name), online: !!online});
  };
  try{
    if(typeof CHATS !== 'undefined') CHATS.forEach(c => {
      if(c.kind === 'direct' && c.id !== 'live') push(c.name, c.ava, c.avaIcon, c.nick, c.online);
    });
  }catch(e){}
  try{
    [...POSTS.sub, ...POSTS.rec].forEach(p => { if(p.name) push(p.name, p.ava, null, null, false); });
  }catch(e){}
  try{ Object.keys(PS_BIOS).forEach(n => push(n)); }catch(e){}
  return out;
}
/* детерминированный подсписок людей для (автор, вкладка) */
function psGraphPeople(name, tab){
  const pool = psPeoplePool().filter(p => p.name !== name);
  if(!pool.length) return [];
  const salt = psHash(name + ':' + tab);
  /* сортировка по «весу» — псевдослучайно, но стабильно */
  const ranked = pool.map(p => ({p, w: psHash(p.name + '#' + salt)}))
    .sort((x, y) => x.w - y.w).map(x => x.p);
  /* сколько показать: от stat, но не больше пула */
  const a = psAuthor(name);
  const want = tab === 'followers' ? a.followers : a.followingN;
  const n = Math.max(3, Math.min(ranked.length, 6 + (salt % 7)));
  const list = ranked.slice(0, n);
  /* если это подписчики автора, на которого подписан ТЫ — добавить себя первым */
  if(tab === 'followers' && psIsFollowing(name) && typeof PROFILE !== 'undefined'){
    list.unshift({name: PROFILE.name, ava: (PROFILE.name[0]||'O').toUpperCase(), avaIcon: null,
      nick: PROFILE.nick, online: true, isMe: true});
  }
  return {list, total: want};
}
function psPersonRowHtml(p){
  const f = psIsFollowing(p.name);
  const me = !!p.isMe;
  return `<div class="ps-person${me ? ' me' : ''}" ${me ? '' : `onclick="psGraphOpenPerson('${psAttr(p.name)}')"`}>
    <div class="ps-person-ava">${p.avaIcon ? I(p.avaIcon) : esc(p.ava)}${p.online ? '<span class="ps-person-on"></span>' : ''}</div>
    <div class="ps-person-b">
      <span class="ps-person-n"><span class="ps-person-nm">${esc(p.name)}</span>${me ? '<span class="ps-you">это ты</span>' : (typeof vBadge === 'function' ? vBadge(p.name) : '')}</span>
      <small>@${esc(p.nick)}</small>
    </div>
    ${me ? '' : `<button class="ps-follow-mini${f ? ' on' : ''}" onclick="event.stopPropagation();psFollowMini(this,'${psAttr(p.name)}')">${I(f ? 'check' : 'plus')}<span>${f ? 'Вы подписаны' : 'Подписаться'}</span></button>`}
  </div>`;
}
function psFollowMini(btn, name){
  const f = psSetFollow(name);
  if(btn){
    btn.classList.toggle('on', f);
    btn.innerHTML = I(f ? 'check' : 'plus') + '<span>' + (f ? 'Вы подписаны' : 'Подписаться') + '</span>';
    btn.classList.remove('ps-bump'); void btn.offsetWidth; btn.classList.add('ps-bump');
  }
}
function psGraphRender(){
  const name = PS.graphName, tab = PS.graphTab || 'followers';
  const wrap = document.getElementById('psGraphList');
  const th = document.getElementById('psGraphTabs');
  if(th) th.querySelectorAll('button').forEach(b => b.classList.toggle('on', b.dataset.gt === tab));
  if(!wrap) return;
  const res = psGraphPeople(name, tab);
  const list = res.list || [];
  const total = res.total || list.length;
  if(!list.length){ wrap.innerHTML = `<div class="ps-empty">${I('users')}<p>Пока никого</p><span>Первые подписки появятся здесь — начни собирать своё комьюнити</span></div>`; return; }
  wrap.innerHTML = `<div class="ps-graph-note">Показаны ${list.length} из ${psFmt(total)}</div>` +
    list.map((p, i) => `<div class="fade-in" style="animation-delay:${i * 30}ms">${psPersonRowHtml(p)}</div>`).join('');
}
function psGraphTab(tab){ PS.graphTab = tab; psGraphRender(); }
function psOpenGraph(tab){
  if(!PS.cur) return;
  PS.graphName = PS.cur; PS.graphTab = tab || 'followers';
  const head = document.getElementById('psGraphName');
  if(head) head.textContent = PS.graphName;
  const th = document.getElementById('psGraphTabs');
  if(th) th.style.display = '';
  psGraphRender();
  /* сет-лист открыт поверх fullscreen-профиля → поднять бэкдроп над #psView */
  try{ document.body.classList.add('ps-over-view'); }catch(e){}
  if(typeof openSheet === 'function') openSheet('ps-graph');
}
function psGraphOpenPerson(name){
  if(typeof closeSheet === 'function') closeSheet();
  setTimeout(() => psOpenProfile(name), 60);
}

/* ================= НЕСКОЛЬКО АККАУНТОВ (Telegram/Instagram) =================
   ЕДИНЫЙ источник правды — settings-plus (ST2.accounts / st2SwitchAccount /
   st2AddAccount / st2RemoveAccount, персист в oko-settings2). Здесь — только
   быстрый переключатель в шапке своего профиля (settings-plus грузится ПОЗЖЕ
   нашего модуля, поэтому к его API обращаемся строго в рантайме, по клику). */
function psAccAvailable(){ return typeof ST2 !== 'undefined' && Array.isArray(ST2.accounts); }
function psAccList(){ return psAccAvailable() ? ST2.accounts : []; }
function psAccActiveId(){ return psAccAvailable() ? ST2.activeAcc : null; }
function psAccInit(name){
  return (typeof st2AccInit === 'function') ? st2AccInit(name) : psInitials(name);
}
function psAccHue(a){
  const key = (a && (a.nick || a.name)) || 'user';
  return (typeof st2Hue === 'function') ? st2Hue(key) : (psHash(key) % 360);
}
function psAccSwitch(id){
  if(id === psAccActiveId()){ if(typeof closeSheet === 'function') closeSheet(); return; }
  if(typeof closeSheet === 'function') closeSheet();
  if(typeof st2SwitchAccount === 'function') st2SwitchAccount(id); /* сам зовёт renderMyProfile + toast + persist */
  /* лента зависит от PROFILE.name («мои посты») — освежить, если открыта */
  try{ if(typeof renderFeed === 'function' && typeof curFeedKind !== 'undefined') renderFeed(curFeedKind); }catch(e){}
}
function psAccAdd(){
  if(typeof closeSheet === 'function') closeSheet();
  if(typeof st2AddAccount === 'function') st2AddAccount();
  else if(typeof toast === 'function') toast('Управление аккаунтами — в настройках');
}
function psAccRemove(id){
  if(typeof closeSheet === 'function') closeSheet();
  if(typeof st2RemoveAccount === 'function') st2RemoveAccount(id);
}
function psAccAvaHtml(a){
  const h = psAccHue(a);
  if(a && a.avatar) return `<span class="ps-acc-ava has" style="background-image:url(${a.avatar})"></span>`;
  return `<span class="ps-acc-ava" style="--h:${h}">${esc(psAccInit(a.name))}</span>`;
}
function psAccRenderSheet(){
  const wrap = document.getElementById('psAccList');
  if(!wrap) return;
  const arr = psAccList();
  const activeId = psAccActiveId();
  if(!arr.length){ wrap.innerHTML = `<div class="ps-empty">${I('users')}<p>Пока некого показать</p><span>Загляни позже — подберём интересных авторов под тебя</span></div>`; return; }
  wrap.innerHTML = arr.map(a => {
    const active = a.id === activeId;
    const owner = a.role === 'owner';
    const tierChip = a.tier && a.tier !== 'FREE' ? `<span class="ps-acc-tier">${esc(a.tier)}</span>` : '';
    return `<div class="ps-acc-row${active ? ' active' : ''}" onclick="psAccSwitch('${a.id}')">
      ${psAccAvaHtml(a)}
      <div class="ps-acc-info">
        <span class="ps-acc-name">${esc(a.name)}${typeof vBadge === 'function' ? vBadge(a.name) : ''}${owner ? ` <span class="ps-acc-badge" title="владелец">${I('crown')}</span>` : ''}${tierChip}</span>
        <small>@${esc(a.nick)}</small>
      </div>
      ${active ? `<span class="ps-acc-check">${I('check2')}</span>`
               : (owner ? '' : `<button class="ps-acc-del" onclick="event.stopPropagation();psAccRemove('${a.id}')" aria-label="Убрать аккаунт">${I('trash')}</button>`)}
    </div>`;
  }).join('');
}
function psOpenAccounts(){
  if(!psAccAvailable()){ if(typeof toast === 'function') toast('Аккаунты недоступны'); return; }
  psAccRenderSheet();
  if(typeof openSheet === 'function') openSheet('ps-acc');
}
/* инъекция переключателя аккаунтов в шапку своего профиля */
function psInjectAccSwitch(){
  const top = document.querySelector('#screen-profile .profile-top');
  if(!top) return;
  top.classList.add('ps-acc-clickable');
  if(!top._psAccBound){
    top.addEventListener('click', function(e){
      if(e.target.closest && e.target.closest('.ps-acc-switch')){ psOpenAccounts(); return; }
      psOpenAccounts();
    });
    top._psAccBound = true;
  }
  if(!top.querySelector('.ps-acc-switch')){
    const btn = document.createElement('button');
    btn.className = 'ps-acc-switch';
    btn.setAttribute('aria-label', 'Сменить аккаунт');
    btn.innerHTML = I('chev');
    top.appendChild(btn);
  }
  /* count-бейдж, если аккаунтов > 1 */
  const badge = top.querySelector('.ps-acc-switch');
  if(badge){
    const n = psAccList().length;
    badge.classList.toggle('multi', n > 1);
  }
}
/* доступ к «Моим подпискам» (реальные подписки из PS.follow) на своём профиле */
function psMyFollows(){
  return Object.keys(PS.follow || {}).sort((a, b) => (PS.follow[b] || 0) - (PS.follow[a] || 0));
}
function psInjectMyFollows(){
  const stats = document.getElementById('profStats');
  if(!stats) return;
  let row = document.getElementById('psMyFollowsRow');
  const names = psMyFollows();
  if(!names.length){ if(row) row.remove(); return; }
  if(!row){
    row = document.createElement('button');
    row.id = 'psMyFollowsRow'; row.className = 'ps-myfollows';
    stats.insertAdjacentElement('afterend', row);
    row.addEventListener('click', psOpenMyFollows);
  }
  const avas = names.slice(0, 4).map(n => `<span class="ps-mf-ava">${esc(psInitials(n))}</span>`).join('');
  row.innerHTML = `<span class="ps-mf-avas">${avas}</span>
    <span class="ps-mf-txt"><b>${names.length}</b> ${names.length === 1 ? 'подписка' : (names.length < 5 ? 'подписки' : 'подписок')}</span>
    <span class="ps-mf-chev">${I('chev')}</span>`;
}
function psOpenMyFollows(){
  const names = psMyFollows();
  PS.graphName = (typeof PROFILE !== 'undefined') ? PROFILE.name : '';
  PS.graphTab = 'myfollows';
  const head = document.getElementById('psGraphName');
  if(head) head.textContent = 'Мои подписки';
  const th = document.getElementById('psGraphTabs');
  if(th) th.style.display = 'none';
  const wrap = document.getElementById('psGraphList');
  if(wrap){
    if(!names.length){ wrap.innerHTML = `<div class="ps-empty">${I('users')}<p>Твоя лента подписок пуста</p><span>Подпишись на авторов — их лучшие посты будут первыми у тебя</span></div>`; }
    else{
      const pool = psPeoplePool();
      const byName = {}; pool.forEach(p => byName[p.name] = p);
      wrap.innerHTML = names.map((n, i) => {
        const p = byName[n] || {name: n, ava: psInitials(n), nick: psNick(n), online: false};
        return `<div class="fade-in" style="animation-delay:${i * 30}ms">${psPersonRowHtml(p)}</div>`;
      }).join('');
    }
  }
  if(typeof openSheet === 'function') openSheet('ps-graph');
}

/* ================= ТОЧКА ВХОДА 1: лента (делегирование на #feedList) ================= */
function psFeedTap(e){
  const hit = e.target.closest && e.target.closest('#feedList .post .head .ava, #feedList .post .head .name, #feedList .post .head .sub');
  if(!hit) return;
  if(e.target.closest('.post-more') || e.target.closest('.chip')) return; /* меню и чипы — не трогаем */
  const art = hit.closest('article.post');
  const id = psPostIdOf(art);
  const p = (id != null && typeof postById === 'function') ? postById(id) : null;
  if(!p || !p.name) return;
  psOpenProfile(p.name);
}

/* ================= ТОЧКА ВХОДА 2: аватар в шапке конва (кроме своих/каналов) ================= */
function psConvAvaTap(e){
  const ava = e.target.closest && e.target.closest('#convAva');
  if(!ava) return;
  const c = (typeof currentChat !== 'undefined') && currentChat;
  if(!c || c.kind !== 'direct' || c.id === 'live') return;              /* каналы/группы/живой чат — прежнее поведение */
  if(typeof PROFILE !== 'undefined' && c.name === PROFILE.name) return; /* свой чат — прежнее поведение */
  e.stopPropagation(); e.preventDefault();
  psOpenProfile(c.name);
}

/* ================= ТОЧКА ВХОДА 3: глобальный поиск (chain renderSearch) ================= */
function psDecorateSearch(){
  const body = document.getElementById('searchBody');
  if(!body || typeof CHATS === 'undefined') return;
  const inp = document.getElementById('gSearchInput');
  const q = ((inp && inp.value) || '').trim().toLowerCase();
  if(!q) return;

  /* 1) «Люди»: тап ведёт в профиль (оттуда — «Написать» в чат) */
  body.querySelectorAll('.nt-item').forEach(btn => {
    const oc = btn.getAttribute('onclick') || '';
    if(oc.indexOf('openConv(') < 0 || oc.indexOf('openListing') > -1) return;
    const m = oc.match(/openConv\((?:'([^']+)'|(\d+))\)/);
    if(!m) return;
    const id = m[2] !== undefined ? +m[2] : m[1];
    const c = CHATS.find(x => x.id === id);
    if(!c || c.kind !== 'direct' || c.id === 'live') return;
    if(typeof PROFILE !== 'undefined' && c.name === PROFILE.name) return;
    btn.setAttribute('onclick', `closeSearch();psOpenProfile('${psAttr(c.name)}')`);
  });

  /* 2) секция «Авторы» — авторы постов ленты по запросу */
  try{
    const inChats = new Set(CHATS.filter(c => c.kind === 'direct').map(c => c.name));
    const seen = new Set(); const authors = [];
    [...POSTS.sub, ...POSTS.rec].forEach(p => {
      if(!p.name || seen.has(p.name)) return; seen.add(p.name);
      if(typeof PROFILE !== 'undefined' && p.name === PROFILE.name) return;
      if(inChats.has(p.name)) return; /* уже в секции «Люди» */
      if(p.name.toLowerCase().includes(q)) authors.push(p);
    });
    if(!authors.length) return;
    const html = `<p class="nt-group">Авторы</p>` + authors.slice(0, 5).map(p =>
      `<button class="nt-item" onclick="closeSearch();psOpenProfile('${psAttr(p.name)}')">
        <span class="nt-ic">${esc(p.ava || p.name[0])}</span>
        <div class="nt-b"><span><b>${esc(p.name)}</b>${typeof vBadge === 'function' ? vBadge(p.name) : ''}</span><small>${esc(p.sub || 'Автор в OKO · открыть профиль')}</small></div>
      </button>`).join('');
    const empty = body.querySelector('.empty-state');
    if(empty) body.innerHTML = html;
    else body.insertAdjacentHTML('beforeend', html);
  }catch(e){}
}

/* ================= ПОДПИСКИ ПОДНИМАЮТ ПОСТЫ: сортировка sub + буст rec ================= */
/* стабильная пересортировка POSTS.sub: подписки выше, исходный порядок помним в _psIdx */
function psSortSub(){
  if(typeof POSTS === 'undefined' || !Array.isArray(POSTS.sub)) return;
  let min = Infinity;
  POSTS.sub.forEach(p => { if(typeof p._psIdx === 'number' && p._psIdx < min) min = p._psIdx; });
  if(min === Infinity) min = 0;
  const fresh = POSTS.sub.filter(p => typeof p._psIdx !== 'number');
  fresh.forEach((p, i) => { p._psIdx = min - fresh.length + i; }); /* новые посты — сверху своей группы */
  POSTS.sub.sort((a, b) =>
    ((psIsFollowing(b.name) ? 1 : 0) - (psIsFollowing(a.name) ? 1 : 0)) || (a._psIdx - b._psIdx));
}
/* декор вкладки «Подписки»: verified-галочка + чип «Подписка» */
function psDecorateSub(){
  const list = document.getElementById('feedList');
  if(!list) return;
  list.querySelectorAll('article.post').forEach(art => {
    const id = psPostIdOf(art);
    const p = (id != null && typeof postById === 'function') ? postById(id) : null;
    if(!p) return;
    const nameEl = art.querySelector('.head .name');
    if(!nameEl) return;
    /* галочка verified — только если её ещё никто не поставил (verify-stickers/feed-algo) */
    if(typeof vBadge === 'function' && typeof VERIFIED !== 'undefined' && VERIFIED.has(p.name) &&
       !nameEl.querySelector('.ps-vb, .vs-badge, .fa-vb, use[href*="i-verified"]'))
      nameEl.insertAdjacentHTML('beforeend', `<span class="ps-vb">${vBadge(p.name)}</span>`);
    if(psIsFollowing(p.name) && !nameEl.querySelector('.ps-fchip'))
      nameEl.insertAdjacentHTML('beforeend', ` <span class="chip ps-fchip">${I('check')}Подписка</span>`);
  });
}

/* ============================================================================
   МОЙ ПРОФИЛЬ · «социальная витрина» (mp-): улучшения поверх renderMyProfile
   ---------------------------------------------------------------------------
   • Дефолт-обложка бренда OKO (градиент + логотип-водяной, full-bleed)
   • Быстрый статус («работаю» / «открыт(а) к сотрудничеству» / «в отпуске»)
     + мини-стикер на аватаре
   • Витрина результатов: 3 карточки (лучшие ролики / объявления / отзывы)
   • Sparkline роста подписчиков за 30 дней (SVG, детерминированно)
   • Стрик учёбы (если из academy доступен acStreak)
   Персист статуса — oko-mp; всё остальное считается из уже готовых источников.
   ============================================================================ */

const PS_MP = {
  status: null,              /* 'work' | 'open' | 'off' | null */
  loaded: false,
};
function mpLoad(){
  if(PS_MP.loaded) return;
  try{
    const s = JSON.parse(localStorage.getItem('oko-mp') || 'null');
    if(s && typeof s.status === 'string') PS_MP.status = s.status;
  }catch(e){}
  PS_MP.loaded = true;
}
function mpSave(){
  try{ localStorage.setItem('oko-mp', JSON.stringify({status: PS_MP.status, at: Date.now()})); }catch(e){}
}
const PS_MP_STATUSES = {
  work: {ic:'bolt',      lb:'Сейчас: работаю',            short:'Работаю',    tone:'ok'},
  open: {ic:'briefcase', lb:'Открыт к сотрудничеству',    short:'Открыт(а)',  tone:'accent'},
  off:  {ic:'moon',      lb:'В отпуске',                  short:'В отпуске',  tone:'muted'},
};

/* ---------- обложка ---------- */
function mpCoverHtml(){
  const name = (typeof PROFILE !== 'undefined' && PROFILE.name) ? PROFILE.name : 'OKO';
  const v = 'v' + (psHash('mycov:' + name) % 5);
  return `<div class="mp-cover ${v}">
    <div class="mp-cover-grid"></div>
    <div class="mp-cover-mark">${I('logo')}</div>
    <div class="mp-cover-brand">OKO</div>
  </div>`;
}

/* ---------- быстрый статус ---------- */
function mpStatusHtml(){
  const st = PS_MP.status && PS_MP_STATUSES[PS_MP.status];
  if(!st) return `<button class="mp-status ghost" onclick="mpPickStatus()" aria-label="Установить статус">${I('plus')}<span>Указать статус</span></button>`;
  return `<button class="mp-status mp-tone-${st.tone}" onclick="mpPickStatus()" aria-label="Изменить статус">
    <span class="mp-status-ic">${I(st.ic)}</span><span>${esc(st.lb)}</span>
  </button>`;
}
function mpStickerHtml(){
  const st = PS_MP.status && PS_MP_STATUSES[PS_MP.status];
  if(!st) return '';
  return `<span class="mp-sticker mp-tone-${st.tone}" title="${esc(st.lb)}">${I(st.ic)}</span>`;
}
function mpPickStatus(){
  if(typeof showPopup !== 'function') return;
  const opts = [
    {k:'work', ic:'bolt',      lb:'Работаю',                d:'Занят(а) заказами и проектами'},
    {k:'open', ic:'briefcase', lb:'Открыт к сотрудничеству', d:'Готов брать новые задачи'},
    {k:'off',  ic:'moon',      lb:'В отпуске',              d:'Отдых, откликаюсь медленнее'},
  ];
  const rows = opts.map(o =>
    `<button class="mp-pick-row${PS_MP.status === o.k ? ' on' : ''}" onclick="mpSetStatus('${o.k}')">
      <span class="mp-pick-ic">${I(o.ic)}</span>
      <span class="mp-pick-b"><b>${esc(o.lb)}</b><small>${esc(o.d)}</small></span>
      ${PS_MP.status === o.k ? `<span class="mp-pick-ok">${I('check')}</span>` : ''}
    </button>`).join('');
  const actions = [];
  if(PS_MP.status) actions.push({label:'Убрать статус', ghost:true, onclick: () => mpSetStatus(null)});
  actions.push({label:'Закрыть', ghost:true});
  showPopup({title:'Быстрый статус', body:`<div class="mp-pick">${rows}</div>`, actions});
}
function mpSetStatus(k){
  PS_MP.status = (k && PS_MP_STATUSES[k]) ? k : null;
  mpSave();
  if(typeof closePopup === 'function') closePopup();
  if(typeof renderMyProfile === 'function') renderMyProfile();
  if(typeof toast === 'function') toast(PS_MP.status ? 'Статус: ' + PS_MP_STATUSES[PS_MP.status].short : 'Статус скрыт');
}

/* ---------- витрина результатов (3 карточки) ---------- */
function mpMyPostsStat(){
  let cnt = 0, views = 0, likes = 0;
  try{
    if(typeof POSTS !== 'undefined' && typeof PROFILE !== 'undefined'){
      [...POSTS.sub, ...POSTS.rec].forEach(p => {
        if(p.name === PROFILE.name){ cnt++; views += (p.views || 0); likes += (p.likes || 0); }
      });
    }
  }catch(e){}
  return {cnt, views, likes};
}
function mpShowHtml(){
  const nf = v => (typeof fmtN === 'function' ? fmtN(v) : String(v));
  const my = mpMyPostsStat();
  const hasPosts = my.cnt > 0;
  const cards = [
    {
      k:'reels', ic:'circle-play', t:'Мои лучшие клипы',
      v: hasPosts ? nf(my.views) + ' просмотров' : 'Пока пусто',
      s: hasPosts ? my.cnt + ' ' + (my.cnt === 1 ? 'ролик' : (my.cnt < 5 ? 'ролика' : 'роликов')) : 'Опубликуй первый',
      onclick: `mpGo('feed')`
    },
    {
      k:'ads',   ic:'briefcase',   t:'Активные объявления',
      v: 'Витрина услуг',
      s: 'Разместить в бирже',
      onclick: `mpGo('market')`
    },
    {
      k:'rev',   ic:'star',        t:'Отзывы клиентов',
      v: hasPosts ? '5.0 · ' + Math.max(3, Math.min(24, my.cnt * 2)) : 'Собираем',
      s: hasPosts ? 'Средняя оценка' : 'Появятся после сделок',
      onclick: `mpGo('reviews')`
    },
  ];
  return `<div class="mp-show">` + cards.map((c, i) => `
    <button class="mp-card fade-in" style="animation-delay:${60 + i * 55}ms" onclick="${c.onclick}" aria-label="${esc(c.t)}">
      <span class="mp-card-ic">${I(c.ic)}</span>
      <span class="mp-card-b">
        <small>${esc(c.t)}</small>
        <b>${esc(c.v)}</b>
        <span class="mp-card-s">${esc(c.s)}</span>
      </span>
      <span class="mp-card-ch">${I('chev')}</span>
    </button>`).join('') + `</div>`;
}
function mpGo(k){
  if(k === 'feed' && typeof showTab === 'function'){ showTab('feed'); return; }
  if(k === 'market' && typeof openMa === 'function'){ openMa('market'); return; }
  if(typeof toast === 'function') toast('Раздел скоро появится');
}

/* ---------- sparkline: рост подписчиков за 30 дней ---------- */
function mpSparkPoints(name, endValue){
  const n = 30;
  const h = psHash('spark:' + name);
  const gain = 40 + (h % 160);                    /* +40..+199 за 30 дней */
  const start = Math.max(50, endValue - gain);
  const pts = [];
  for(let i = 0; i < n; i++){
    const t = i / (n - 1);
    /* монотонный рост с лёгким шумом (детерминированный) */
    const noise = ((psHash(name + '#' + i) % 100) / 100 - 0.5) * (gain * 0.10);
    let v = start + (endValue - start) * (t * t * (3 - 2 * t)) + noise;
    if(v < start) v = start;
    if(v > endValue + 5) v = endValue + 5;
    pts.push(v);
  }
  pts[n - 1] = endValue;
  return {pts, gain};
}
function mpSparkHtml(){
  const followers = 2400;                          /* соответствует «2.4к» в шапке */
  const {pts, gain} = mpSparkPoints((typeof PROFILE !== 'undefined' && PROFILE.name) || 'me', followers);
  const w = 300, h = 44, pad = 2;
  const min = Math.min.apply(null, pts), max = Math.max.apply(null, pts);
  const rng = Math.max(1, max - min);
  const x = i => pad + i * ((w - pad * 2) / (pts.length - 1));
  const y = v => pad + (h - pad * 2) * (1 - (v - min) / rng);
  const line = pts.map((v, i) => (i ? 'L' : 'M') + x(i).toFixed(1) + ' ' + y(v).toFixed(1)).join(' ');
  const area = `M${pad.toFixed(1)} ${(h - pad).toFixed(1)} ` +
    pts.map((v, i) => 'L' + x(i).toFixed(1) + ' ' + y(v).toFixed(1)).join(' ') +
    ` L${(w - pad).toFixed(1)} ${(h - pad).toFixed(1)} Z`;
  const last = pts.length - 1;
  const dotX = x(last).toFixed(1), dotY = y(pts[last]).toFixed(1);
  const nf = v => (typeof fmtN === 'function' ? fmtN(v) : String(v));
  return `<div class="mp-spark fade-in" aria-label="Рост подписчиков за 30 дней">
    <div class="mp-spark-h">
      <span class="mp-spark-lb">Рост за 30 дней</span>
      <span class="mp-spark-v">+${nf(gain)}</span>
    </div>
    <svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none" class="mp-spark-svg">
      <defs>
        <linearGradient id="mpSparkFill" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0%" stop-color="#9AFF00" stop-opacity=".38"/>
          <stop offset="100%" stop-color="#9AFF00" stop-opacity="0"/>
        </linearGradient>
      </defs>
      <path d="${area}" fill="url(#mpSparkFill)"/>
      <path d="${line}" fill="none" stroke="#9AFF00" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
      <circle cx="${dotX}" cy="${dotY}" r="3" fill="#9AFF00"/>
      <circle cx="${dotX}" cy="${dotY}" r="6" fill="#9AFF00" opacity=".22"/>
    </svg>
  </div>`;
}

/* ---------- стрик учёбы (если Академия подключена) ---------- */
function mpStreakDays(){
  try{ if(typeof acStreak === 'function'){ const st = acStreak(); return (st && +st.days) || 0; } }catch(e){}
  return 0;
}
function mpStreakHtml(){
  const d = mpStreakDays();
  if(!d) return '';
  const word = d === 1 ? 'день' : (d < 5 ? 'дня' : 'дней');
  const hot = d >= 3;
  return `<button class="mp-streak${hot ? ' hot' : ''} fade-in" onclick="mpGoAcademy()" aria-label="Стрик учёбы">
    <span class="mp-streak-ic">${I('fire')}</span>
    <span class="mp-streak-b">
      <b>${d} ${word} подряд в OKO</b>
      <small>${hot ? 'Ты в огне. Не сбавляй ритм.' : 'Начало учебной серии. Возвращайся каждый день.'}</small>
    </span>
    <span class="mp-streak-ch">${I('chev')}</span>
  </button>`;
}
function mpGoAcademy(){
  if(typeof showTab === 'function') showTab('academy');
  else if(typeof toast === 'function') toast('Академия');
}

/* ---------- инъекция ВСЕХ улучшений в свой профиль ---------- */
function mpInject(){
  mpLoad();
  const scr = document.getElementById('screen-profile');
  if(!scr) return;
  const pad = scr.querySelector('.pad');
  if(!pad) return;

  /* 1) обложка — самый первый ребёнок .pad */
  let cover = pad.querySelector('.mp-cover-wrap');
  if(!cover){
    cover = document.createElement('div');
    cover.className = 'mp-cover-wrap';
    pad.prepend(cover);
  }
  cover.innerHTML = mpCoverHtml();

  /* 2) стикер статуса на аватар */
  const ava = document.getElementById('profAva');
  if(ava){
    let sticker = ava.querySelector('.mp-sticker');
    if(sticker) sticker.remove();
    const html = mpStickerHtml();
    if(html) ava.insertAdjacentHTML('beforeend', html);
    ava.classList.add('mp-ava-plus');
  }

  /* 3) плашка статуса — под ником/тарифом */
  const top = pad.querySelector('.profile-top');
  if(top){
    let stRow = pad.querySelector('.mp-status-row');
    if(!stRow){
      stRow = document.createElement('div');
      stRow.className = 'mp-status-row';
      top.insertAdjacentElement('afterend', stRow);
    }
    stRow.innerHTML = mpStatusHtml();
  }

  /* 4) витрина результатов — после био */
  const bio = document.getElementById('profBio');
  if(bio){
    let show = pad.querySelector('.mp-show-wrap');
    if(!show){
      show = document.createElement('div');
      show.className = 'mp-show-wrap';
      bio.insertAdjacentElement('afterend', show);
    }
    show.innerHTML = mpShowHtml();
  }

  /* 5) sparkline — после статы */
  const stats = document.getElementById('profStats');
  if(stats){
    let spark = pad.querySelector('.mp-spark-wrap');
    if(!spark){
      spark = document.createElement('div');
      spark.className = 'mp-spark-wrap';
      stats.insertAdjacentElement('afterend', spark);
    }
    spark.innerHTML = mpSparkHtml();
  }

  /* 6) стрик учёбы — сразу после sparkline (если есть) */
  const sparkWrap = pad.querySelector('.mp-spark-wrap');
  if(sparkWrap){
    let streak = pad.querySelector('.mp-streak-wrap');
    const html = mpStreakHtml();
    if(!html){ if(streak) streak.remove(); return; }
    if(!streak){
      streak = document.createElement('div');
      streak.className = 'mp-streak-wrap';
      sparkWrap.insertAdjacentElement('afterend', streak);
    }
    streak.innerHTML = html;
  }
}

/* ============================================================================
   МОИ СОЦСЕТИ (ps-soc-): LinkedIn + Beacons.ai + Linktree в одном хабе
   ---------------------------------------------------------------------------
   Каталог платформ (IG/TT/VK/TG/YT/Threads/Rutube) с фирменными SVG. Демо-
   данные детерминированные (по хендлу + платформе): подписчики, sparkline за
   7 дней, ER, последние 5 постов, лучший пост месяца. Совокупный дашборд,
   тренды-подсказки, экспорт медиа-кита, публичная ссылка + QR, авто-постинг,
   ссылки-агрегатор. Персист — oko-ps-socials. Ничего внешнего, всё локально.
   ============================================================================ */

/* ---------- каталог платформ (id → бренд + inline SVG) ---------- */
const PS_SOC_PLATS = [
  {id:'ig', name:'Instagram', hint:'Instagram-профиль', ph:'@username',      handleFmt:'@',      base:'instagram.com/',
   svg:'<svg viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"><rect x="16" y="16" width="68" height="68" rx="18"/><circle cx="50" cy="50" r="16"/><circle cx="70" cy="30" r="4" fill="currentColor" stroke="none"/></svg>'},
  {id:'tt', name:'TikTok',   hint:'TikTok-аккаунт',    ph:'@username',      handleFmt:'@',      base:'tiktok.com/',
   svg:'<svg viewBox="0 0 100 100" fill="currentColor" stroke="none"><path d="M62 12h12c1 10 8 18 18 20v12c-8 0-15-2-22-6v28c0 15-12 26-27 26s-27-12-27-27 12-27 27-27c2 0 5 .3 7 1v14a13 13 0 1 0 8 12V12z"/></svg>'},
  {id:'vk', name:'VK',       hint:'ВКонтакте',         ph:'ссылка/vk id',   handleFmt:'',       base:'vk.com/',
   svg:'<svg viewBox="0 0 100 100" fill="currentColor" stroke="none"><path d="M14 26h20l10 32L58 26h20L60 62c6 4 12 10 18 24H58c-3-8-8-14-14-14v14H30C22 62 14 44 14 26z"/></svg>'},
  {id:'tg', name:'Telegram', hint:'Telegram-канал',    ph:'@username',      handleFmt:'@',      base:'t.me/',
   svg:'<svg viewBox="0 0 100 100" fill="currentColor" stroke="none"><path d="M85 18 12 46c-3 1-3 5 .3 6l20 6 8 24c1 3 4 3 6 1l12-12 20 15c3 2 6 1 7-3l12-58c1-4-3-8-7-6z"/><path d="M40 60 78 30 46 68" stroke="rgba(0,0,0,.28)" stroke-width="3" fill="none" stroke-linejoin="round"/></svg>'},
  {id:'yt', name:'YouTube',  hint:'YouTube-канал',     ph:'@username',      handleFmt:'@',      base:'youtube.com/',
   svg:'<svg viewBox="0 0 100 100" fill="currentColor" stroke="none"><rect x="10" y="22" width="80" height="56" rx="14"/><path d="M42 36l24 14-24 14z" fill="#fff"/></svg>'},
  {id:'th', name:'Threads',  hint:'Threads-профиль',   ph:'@username',      handleFmt:'@',      base:'threads.net/',
   svg:'<svg viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"><path d="M64 46c-4-8-14-11-22-9-9 2-15 10-13 20 2 9 10 15 22 15 15 0 25-10 27-24 2-16-8-32-28-32-14 0-24 8-28 20"/></svg>'},
  {id:'ru', name:'Rutube',   hint:'Rutube-канал',      ph:'ссылка/ID',      handleFmt:'',       base:'rutube.ru/channel/',
   svg:'<svg viewBox="0 0 100 100" fill="currentColor" stroke="none"><rect x="10" y="22" width="80" height="56" rx="14"/><path d="M42 36l24 14-24 14z" fill="#fff"/><circle cx="76" cy="34" r="6" fill="#fff"/></svg>'},
];
const PS_SOC_MAP = Object.fromEntries(PS_SOC_PLATS.map(p => [p.id, p]));
function psSocIco(id){
  const p = PS_SOC_MAP[id]; if(!p) return '';
  return p.svg.replace('<svg ', '<svg class="i" preserveAspectRatio="xMidYMid meet" ');
}
function psSocPlatIcHtml(id, cls){
  return `<span class="ps-soc-plat-ic ps-soc-tone-${id} ${cls||''}">${psSocIco(id)}</span>`;
}

/* ---------- каталог иконок для ссылок Linktree (переиспользуем defs ядра) ---------- */
const PS_SOC_LINK_ICONS = [
  {v:'globe',      lb:'Сайт'},
  {v:'briefcase',  lb:'Портфолио'},
  {v:'megaphone',  lb:'Канал'},
  {v:'shop',       lb:'Магазин'}, /* fallback → briefcase */
  {v:'circle-play',lb:'Ролик'},
  {v:'file',       lb:'Документ'},
  {v:'chat',       lb:'Написать'},
  {v:'star',       lb:'Отзывы'},
  {v:'money',      lb:'Донат/оплата'},
];
function psSocSafeIco(v){
  /* fallback для ic, которых нет в базовых defs */
  const map = {shop:'briefcase'};
  return map[v] || v;
}

/* ---------- persist ---------- */
const PS_SOC_DEF_LINKS = [
  {id:'l1', ic:'globe',      t:'Мой сайт-визитка',    u:'okoteam.top/@ktodaniel'},
  {id:'l2', ic:'megaphone',  t:'Основной Telegram',   u:'t.me/okoappbot'},
  {id:'l3', ic:'briefcase',  t:'Кейсы и портфолио',   u:'okoteam.top/@ktodaniel/cases'},
];
const PS_SOC_DEF_SCHED = [
  {id:'s1', at:Date.now() + 3600e3 * 4,  txt:'Как я собрал контент-завод за 3 дня — короткий разбор', plats:['ig','tg','vk']},
  {id:'s2', at:Date.now() + 3600e3 * 22, txt:'Reels-приём «двойной хук»: удержание +18% на 30к охвате', plats:['ig','tt']},
];
const PS_SOC = {
  conn: {ig:true, tt:true, vk:true, tg:true, yt:false, th:false, ru:false},
  handles: {ig:'ktodaniel', tt:'ktodaniel', vk:'daniel.oko', tg:'ktodaniel', yt:'ktodaniel', th:'ktodaniel', ru:'ktodaniel'},
  slug: '',                       /* публичная ссылка okoteam.top/u/@slug — берём из PROFILE.nick */
  links: null,
  sched: null,
  loaded: false,
};
function psSocLoad(){
  if(PS_SOC.loaded) return;
  try{
    const s = JSON.parse(localStorage.getItem('oko-ps-socials') || 'null');
    if(s){
      if(s.conn) PS_SOC.conn = Object.assign({}, PS_SOC.conn, s.conn);
      if(s.handles) PS_SOC.handles = Object.assign({}, PS_SOC.handles, s.handles);
      if(typeof s.slug === 'string') PS_SOC.slug = s.slug;
      if(Array.isArray(s.links)) PS_SOC.links = s.links;
      if(Array.isArray(s.sched)) PS_SOC.sched = s.sched;
    }
  }catch(e){}
  if(!PS_SOC.links) PS_SOC.links = PS_SOC_DEF_LINKS.slice();
  if(!PS_SOC.sched) PS_SOC.sched = PS_SOC_DEF_SCHED.slice();
  PS_SOC.loaded = true;
}
function psSocSave(){
  try{ localStorage.setItem('oko-ps-socials', JSON.stringify({
    conn: PS_SOC.conn, handles: PS_SOC.handles, slug: PS_SOC.slug,
    links: PS_SOC.links, sched: PS_SOC.sched, at: Date.now()
  })); }catch(e){}
}
function psSocSlug(){
  return (PS_SOC.slug && PS_SOC.slug.trim()) ||
         (typeof PROFILE !== 'undefined' && PROFILE.nick) || 'me';
}
function psSocConnected(){ return PS_SOC_PLATS.filter(p => PS_SOC.conn[p.id]); }
function psSocFmt(n){ return typeof fmtN === 'function' ? fmtN(n) : psFmt(n); }

/* ---------- детерминированные демо-данные платформы ---------- */
function psSocFollowers(id){
  const seed = psHash('soc:f:' + id + ':' + (PS_SOC.handles[id] || ''));
  /* реалистичные диапазоны по платформам */
  const base = {ig:4800, tt:12400, vk:2100, tg:6300, yt:840, th:1200, ru:520}[id] || 1000;
  const jitter = seed % 2200;
  return base + jitter;
}
function psSocSpark7(id){
  /* 7 дней роста подписчиков — стабильно возрастающая с шумом */
  const end = psSocFollowers(id);
  const seed = psHash('soc:s7:' + id + ':' + (PS_SOC.handles[id] || ''));
  const growPct = 0.008 + (seed % 60) / 3200; /* 0.8%..2.7% в неделю */
  const start = Math.max(50, Math.round(end / (1 + growPct)));
  const out = [];
  for(let i = 0; i < 7; i++){
    const t = i / 6;
    const noise = ((psHash(id + '#d' + i) % 100) / 100 - 0.5) * ((end - start) * 0.22 + 4);
    const v = Math.max(0, Math.round(start + (end - start) * t + noise));
    out.push(v);
  }
  out[6] = end;
  return {pts: out, gain: end - start, growPct: ((end / start - 1) * 100)};
}
function psSocER(id){
  /* engagement rate — реалистично по платформам */
  const seed = psHash('soc:er:' + id + ':' + (PS_SOC.handles[id] || ''));
  const map = {ig:[4.2, 2.4], tt:[9.6, 4.8], vk:[2.9, 1.6], tg:[8.4, 3.2], yt:[6.1, 2.8], th:[5.4, 2.6], ru:[3.7, 1.9]};
  const [max, min] = map[id] || [4, 2];
  const t = (seed % 100) / 100;
  return +(min + t * (max - min)).toFixed(1);
}
function psSocRecentPosts(id){
  /* 5 последних постов: заголовок, охват, лайки, комменты, ER — стабильно */
  const seed = psHash('soc:p:' + id + ':' + (PS_SOC.handles[id] || ''));
  const followers = psSocFollowers(id);
  const platER = psSocER(id);
  const titles = [
    'Как удержание в первые 3 секунды меняет весь просмотр',
    'Разбор ролика на 42 тысячи охвата: 3 приёма',
    'Убрал одну сцену — досмотр вырос на 18%',
    'Один хук, три формата — тестируем на неделю',
    'Простое правило монтажа, которое повышает CTR',
    'Что делать, когда лента «встала» — короткий чек-лист',
    'Формат «до/после» на реальных цифрах кабинета',
    'Собрал 5.6к сохранений одной каруселью — почему',
    'Разбор комментария: как ответ разгоняет охват',
    'Продал курс через полезный ролик — механика',
  ];
  const kinds = ['circle-play','photo','file','circle-play','circle-play'];
  const now = Date.now();
  const out = [];
  for(let i = 0; i < 5; i++){
    const s = psHash(id + '@p' + i + ':' + seed);
    const reachFrac = 0.28 + ((s >> 3) % 100) / 130; /* 28%..104% от аудитории */
    const reach = Math.round(followers * reachFrac);
    const er = Math.max(0.6, platER * (0.55 + ((s >> 6) % 100) / 90));
    const likes = Math.round(reach * er / 100 * 0.72);
    const comments = Math.max(1, Math.round(reach * er / 100 * 0.11));
    const shares = Math.max(0, Math.round(reach * er / 100 * 0.17));
    out.push({
      i, id: id + '_p' + i,
      t: titles[(s >> 2) % titles.length],
      reach, likes, comments, shares,
      er: +er.toFixed(1),
      kind: kinds[i % kinds.length],
      dur: kinds[i % kinds.length] === 'circle-play' ? ('0:' + (12 + (s % 45)).toString().padStart(2, '0')) : null,
      ts: now - (i * 30 + (s % 20)) * 3600e3,
      g: 'g' + (i % 5)
    });
  }
  return out;
}
function psSocBestPost(id){
  const list = psSocRecentPosts(id);
  const best = list.slice().sort((a, b) => b.er * b.reach - a.er * a.reach)[0];
  return best;
}
function psSocPostAgo(ts){
  const h = Math.max(1, Math.round((Date.now() - ts) / 36e5));
  if(h < 24) return h + ' ч назад';
  const d = Math.round(h / 24);
  return d + ' ' + (d === 1 ? 'день' : (d < 5 ? 'дня' : 'дней')) + ' назад';
}

/* ---------- совокупный дашборд ---------- */
function psSocAggregate(){
  const conn = psSocConnected();
  if(!conn.length) return {n:0, followers:0, gain:0, erAvg:0, best:null, growPct:0};
  let followers = 0, gain = 0, erSum = 0, best = null;
  conn.forEach(p => {
    const f = psSocFollowers(p.id);
    const s = psSocSpark7(p.id);
    const er = psSocER(p.id);
    followers += f; gain += s.gain; erSum += er;
    if(!best || er > best.er) best = {id: p.id, name: p.name, er};
  });
  const growPct = followers ? (gain / Math.max(1, followers - gain)) * 100 : 0;
  return {n:conn.length, followers, gain, erAvg:+((erSum / conn.length)).toFixed(1), best, growPct:+growPct.toFixed(1)};
}

/* ---------- sparkline mini (для карточек платформ) ---------- */
function psSocSparkSvg(id, w, h){
  w = w || 100; h = h || 22;
  const {pts} = psSocSpark7(id);
  const min = Math.min.apply(null, pts), max = Math.max.apply(null, pts);
  const rng = Math.max(1, max - min);
  const pad = 1.5;
  const x = i => pad + i * ((w - pad * 2) / (pts.length - 1));
  const y = v => pad + (h - pad * 2) * (1 - (v - min) / rng);
  const line = pts.map((v, i) => (i ? 'L' : 'M') + x(i).toFixed(1) + ' ' + y(v).toFixed(1)).join(' ');
  const area = `M${pad} ${(h - pad).toFixed(1)} ` + pts.map((v, i) => 'L' + x(i).toFixed(1) + ' ' + y(v).toFixed(1)).join(' ') + ` L${(w - pad).toFixed(1)} ${(h - pad).toFixed(1)} Z`;
  const gid = 'psSocGrad_' + id;
  return `<svg class="ps-soc-plat-spark" viewBox="0 0 ${w} ${h}" preserveAspectRatio="none">
    <defs><linearGradient id="${gid}" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#9AFF00" stop-opacity=".35"/><stop offset="100%" stop-color="#9AFF00" stop-opacity="0"/></linearGradient></defs>
    <path d="${area}" fill="url(#${gid})"/>
    <path d="${line}" fill="none" stroke="#9AFF00" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
  </svg>`;
}

/* ---------- QR-код (детерминированный псевдо-QR: угловые маркеры + шумная сетка) ---------- */
function psSocQrSvg(text){
  const N = 25; /* сетка 25×25 */
  const cell = 100 / N;
  const h1 = psHash('qrA:' + text), h2 = psHash('qrB:' + text);
  const corners = [[0,0],[N-7,0],[0,N-7]]; /* три «глаза» */
  const isCorner = (x, y) => corners.some(([cx, cy]) => x >= cx && x < cx + 7 && y >= cy && y < cy + 7);
  const isTiming = (x, y) => (x === 6 || y === 6);
  const cells = [];
  /* угловые маркеры (7×7 квадрат с внутренней рамкой 3×3) */
  corners.forEach(([cx, cy]) => {
    cells.push(`<rect x="${cx*cell}" y="${cy*cell}" width="${7*cell}" height="${7*cell}" fill="#000"/>`);
    cells.push(`<rect x="${(cx+1)*cell}" y="${(cy+1)*cell}" width="${5*cell}" height="${5*cell}" fill="#fff"/>`);
    cells.push(`<rect x="${(cx+2)*cell}" y="${(cy+2)*cell}" width="${3*cell}" height="${3*cell}" fill="#000"/>`);
  });
  /* тело — детерминированный шум по хендлу */
  for(let y = 0; y < N; y++){
    for(let x = 0; x < N; x++){
      if(isCorner(x, y)) continue;
      /* timing dots */
      if(y === 6 && x >= 8 && x < N - 8){ if(x % 2 === 0) cells.push(`<rect x="${x*cell}" y="${y*cell}" width="${cell}" height="${cell}" fill="#000"/>`); continue; }
      if(x === 6 && y >= 8 && y < N - 8){ if(y % 2 === 0) cells.push(`<rect x="${x*cell}" y="${y*cell}" width="${cell}" height="${cell}" fill="#000"/>`); continue; }
      const b = (psHash(text + ':' + x + ',' + y) ^ h1 ^ (h2 << (x % 5))) & 0xff;
      if(b > 128) cells.push(`<rect x="${x*cell}" y="${y*cell}" width="${cell}" height="${cell}" fill="#000"/>`);
    }
  }
  /* центральный «логотип OKO» — 7×7 клеток */
  const cx = 50, cy = 50, r = 8.5;
  cells.push(`<rect x="${cx-r}" y="${cy-r}" width="${r*2}" height="${r*2}" rx="3" fill="#fff"/>`);
  cells.push(`<rect x="${cx-r+1.2}" y="${cy-r+1.2}" width="${(r-1.2)*2}" height="${(r-1.2)*2}" rx="2" fill="#9AFF00"/>`);
  cells.push(`<circle cx="${cx}" cy="${cy}" r="3.4" fill="#0a0d04"/>`);
  cells.push(`<circle cx="${cx}" cy="${cy}" r="1.6" fill="#9AFF00"/>`);
  return `<svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" shape-rendering="crispEdges"><rect width="100" height="100" fill="#fff"/>${cells.join('')}</svg>`;
}

/* ============================================================================
   РЕНДЕР
   ============================================================================ */

/* ---------- карточка-вход на профиле ---------- */
function psSocEntryHtml(){
  psSocLoad();
  const conn = psSocConnected();
  const agg = psSocAggregate();
  const avas = PS_SOC_PLATS.slice(0, 5).map(p => {
    const on = PS_SOC.conn[p.id];
    return `<span class="ps-soc-entry-ava ps-soc-tone-${p.id}${on ? '' : ' off'}" title="${esc(p.name)}${on ? '' : ' — не подключено'}">${psSocIco(p.id)}</span>`;
  }).join('');
  const sum = conn.length
    ? `<b>${psSocFmt(agg.followers)}</b> подписчиков · <b>${agg.erAvg}%</b> средний ER${agg.best ? ' · лучшая: <b>' + esc(agg.best.name) + '</b>' : ''}`
    : `Пока не подключено ни одной платформы — жми, чтобы связать <b>${PS_SOC_PLATS.length}</b> сети`;
  return `<button class="ps-soc-entry" onclick="psSocOpen()" aria-label="Открыть Мои соцсети">
    <div class="ps-soc-entry-h">
      <span class="ps-soc-entry-h-ic">${I('globe')}</span>
      <b>Мои соцсети</b>
      <span class="ps-soc-entry-open">Открыть ${I('chev')}</span>
    </div>
    <div class="ps-soc-entry-row">
      <span class="ps-soc-entry-avas">${avas}</span>
      <span class="ps-soc-entry-sum">${sum}</span>
      <span class="ps-soc-entry-chev">${I('chev')}</span>
    </div>
  </button>`;
}

/* ---------- дашборд: 3 тайла + рост ---------- */
function psSocDashHtml(){
  const agg = psSocAggregate();
  const dirIco = agg.growPct >= 0 ? 'rocket' : 'chev';
  const best = agg.best ? `${esc(agg.best.name)}<span class="ps-soc-tile-sub">${agg.best.er}% ER</span>` : '—';
  return `<div class="ps-soc-dash">
    <div class="ps-soc-tile">
      <div class="ps-soc-tile-lb">${I('users')} Подписчиков</div>
      <div class="ps-soc-tile-v">${psSocFmt(agg.followers)}</div>
      <div class="ps-soc-tile-d${agg.growPct < 0 ? ' down' : ''}">${I(dirIco)} ${agg.growPct >= 0 ? '+' : ''}${agg.growPct}% за 7 дн</div>
    </div>
    <div class="ps-soc-tile">
      <div class="ps-soc-tile-lb">${I('bolt')} Средний ER</div>
      <div class="ps-soc-tile-v">${agg.erAvg}<span class="ps-soc-tile-sub">%</span></div>
      <div class="ps-soc-tile-d">${I('check')} выше среднего</div>
    </div>
    <div class="ps-soc-tile">
      <div class="ps-soc-tile-lb">${I('star')} Лучшая</div>
      <div class="ps-soc-tile-v" style="font-size:16px">${best}</div>
      <div class="ps-soc-tile-d">${I('fire')} топ по вовлечению</div>
    </div>
  </div>`;
}

/* ---------- сетка платформ ---------- */
function psSocPlatCardHtml(p){
  const on = PS_SOC.conn[p.id];
  const handle = PS_SOC.handles[p.id] || '';
  if(!on){
    return `<button class="ps-soc-plat" onclick="psSocConnect('${p.id}')" aria-label="Подключить ${esc(p.name)}">
      <div class="ps-soc-plat-h">
        ${psSocPlatIcHtml(p.id)}
        <div class="ps-soc-plat-name">${esc(p.name)}<small>${esc(p.hint)}</small></div>
        <span class="ps-soc-plat-st">Отсутствует</span>
      </div>
      <div class="ps-soc-plat-cta">${I('plus')} Подключить</div>
    </button>`;
  }
  const followers = psSocFollowers(p.id);
  const {gain, growPct} = psSocSpark7(p.id);
  const er = psSocER(p.id);
  return `<button class="ps-soc-plat on" onclick="psSocDetail('${p.id}')" aria-label="Открыть статистику ${esc(p.name)}">
    <div class="ps-soc-plat-h">
      ${psSocPlatIcHtml(p.id)}
      <div class="ps-soc-plat-name">${esc(p.name)}<small>${p.handleFmt + esc(handle)}</small></div>
      <span class="ps-soc-plat-st on">${I('check')} Подключено</span>
    </div>
    ${psSocSparkSvg(p.id, 200, 22)}
    <div class="ps-soc-plat-mini">
      <span>${I('users')}<b>${psSocFmt(followers)}</b></span>
      <span>${I('bolt')}<b>${er}%</b> ER</span>
      <span>${I('rocket')}<b>${gain >= 0 ? '+' : ''}${psSocFmt(gain)}</b> за 7 дн</span>
      <span>${I('fire')}<b>${growPct.toFixed(1)}%</b></span>
    </div>
  </button>`;
}
function psSocPlatsHtml(){
  return `<div class="ps-soc-plats">` + PS_SOC_PLATS.map(psSocPlatCardHtml).join('') + `</div>`;
}

/* ---------- тренды: подсказки под соцсети ---------- */
function psSocTrendsHtml(){
  const conn = psSocConnected();
  if(!conn.length) return `<div class="ps-soc-empty-body">${I('bolt')}Подключи хотя бы одну платформу — подскажу, что делать, чтобы вырасти быстрее конкурентов в нише.</div>`;
  const trends = [];
  /* 1) платформа с ниже среднего ER — рекомендация по частоте */
  const byEr = conn.map(p => ({p, er: psSocER(p.id)})).sort((a, b) => a.er - b.er);
  const worst = byEr[0];
  const nicheAvg = {ig:5.2, tt:7.8, vk:3.4, tg:9.0, yt:5.4, th:4.1, ru:3.2}[worst.p.id] || 4.5;
  if(worst && worst.er < nicheAvg){
    trends.push({ic:'bolt', tone:'warn', t:`На <b>${worst.p.name}</b> ER ниже средней в нише`,
      d:`Твой ER <b>${worst.er}%</b>, средний в нише <b>${nicheAvg}%</b>. Попробуй увеличить частоту постов до <b>5/неделю</b> и добавить сильный хук в первые 3 секунды — за 2 недели рост в 1.4×.`});
  }
  /* 2) самая растущая платформа — усилить */
  const byGrow = conn.map(p => ({p, s: psSocSpark7(p.id)})).sort((a, b) => b.s.growPct - a.s.growPct);
  const top = byGrow[0];
  if(top && top.s.growPct > 1){
    trends.push({ic:'rocket', tone:'up', t:`<b>${top.p.name}</b> растёт быстрее всех: +${top.s.growPct.toFixed(1)}%`,
      d:`Прирост <b>+${psSocFmt(top.s.gain)}</b> подписчиков за 7 дней. Разбери, какой ролик залетел, и повтори формат ещё 2–3 раза — алгоритм подхватит.`});
  }
  /* 3) не подключённая платформа с ожидаемой аудиторией */
  const missing = PS_SOC_PLATS.find(p => !PS_SOC.conn[p.id]);
  if(missing){
    trends.push({ic:missing.id === 'yt' ? 'circle-play' : 'megaphone', tone:'',
      t:`Подключи <b>${missing.name}</b> — не теряй охваты`,
      d:`По твоей нише ${missing.name} даёт в среднем <b>+18%</b> к охвату за счёт другой аудитории. Займёт 20 секунд — уже видно статистику.`});
  }
  /* 4) лучший пост месяца — упаковать в кейс */
  const bestPlat = byEr[byEr.length - 1] ? byEr[byEr.length - 1].p : conn[0];
  const best = psSocBestPost(bestPlat.id);
  if(best){
    trends.push({ic:'fire', tone:'up', t:`Лучший пост месяца: <b>${psSocFmt(best.reach)}</b> охват`,
      d:`«${esc(best.t)}» на <b>${bestPlat.name}</b> собрал ER <b>${best.er}%</b>. Заверни в кейс — статистика уже есть, останется добавить обложку.`});
  }
  return `<div class="ps-soc-trends">` + trends.slice(0, 4).map(t => `
    <div class="ps-soc-trend">
      <span class="ps-soc-trend-ic ${t.tone}">${I(t.ic)}</span>
      <div class="ps-soc-trend-b">
        <div class="ps-soc-trend-t">${t.t}</div>
        <div class="ps-soc-trend-d">${t.d}</div>
      </div>
    </div>`).join('') + `</div>`;
}

/* ---------- медиа-кит ---------- */
function psSocMediaKitHtml(){
  return `<div class="ps-soc-mediakit">
    <div class="ps-soc-mk-doc">
      <span class="ps-soc-mk-logo">${I('logo')}</span>
      <div class="ps-soc-mk-lines"><i></i><i></i><i></i><i></i><i></i><i></i></div>
    </div>
    <div class="ps-soc-mk-b">
      <div class="ps-soc-mk-t">Медиа-кит для клиентов</div>
      <div class="ps-soc-mk-d">PDF с обложкой OKO: соцсети, аудитория, ER, лучшие кейсы, ссылки для связи. Готово за 3 секунды.</div>
      <button class="ps-soc-mk-btn" onclick="psSocExportKit()">${I('file')} Скачать PDF</button>
    </div>
  </div>`;
}

/* ---------- публичная ссылка + QR ---------- */
function psSocPubHtml(){
  const slug = psSocSlug();
  const url = 'okoteam.top/u/@' + slug;
  return `<div class="ps-soc-pub">
    <div class="ps-soc-qr">${psSocQrSvg(url)}</div>
    <div class="ps-soc-pub-b">
      <div class="ps-soc-pub-lb">Твоя публичная страница</div>
      <div class="ps-soc-pub-url">${I('globe')}${esc(url)}</div>
      <div class="ps-soc-pub-actions">
        <button onclick="psSocCopy('${psAttr(url)}')">${I('copy')} Скопировать</button>
        <button onclick="psSocSharePub('${psAttr(url)}')">${I('share')} Поделиться</button>
        <button onclick="psSocEditSlug()">${I('edit')} Редактировать</button>
      </div>
    </div>
  </div>`;
}

/* ---------- автопостинг ---------- */
function psSocAutoHtml(){
  const list = (PS_SOC.sched || []).slice().sort((a, b) => a.at - b.at);
  const rows = list.length ? list.map(s => {
    const dt = new Date(s.at);
    const HH = String(dt.getHours()).padStart(2, '0');
    const MM = String(dt.getMinutes()).padStart(2, '0');
    const today = new Date(); today.setHours(0,0,0,0);
    const dayLb = (dt.getTime() < today.getTime() + 86400e3) ? 'сегодня' :
                  (dt.getTime() < today.getTime() + 86400e3 * 2) ? 'завтра' :
                  dt.getDate() + '.' + String(dt.getMonth() + 1).padStart(2, '0');
    const plats = (s.plats || []).slice(0, 4).map(id => `<span class="ps-soc-tone-${id}">${psSocIco(id)}</span>`).join('');
    return `<div class="ps-soc-auto-row">
      <div class="ps-soc-auto-time">${HH}:${MM}<small>${dayLb}</small></div>
      <div class="ps-soc-auto-b"><span>${esc(s.txt)}</span><small>уйдёт в ${(s.plats || []).length} сети</small></div>
      <div class="ps-soc-auto-plats">${plats}</div>
      <button class="ps-soc-auto-del" onclick="psSocSchedRemove('${s.id}')" aria-label="Удалить">${I('trash')}</button>
    </div>`;
  }).join('') : `<div class="ps-soc-auto-empty">${I('clock')}Ничего не запланировано. Задай время — робот сам опубликует пост во все выбранные сети.</div>`;
  return `<div class="ps-soc-auto">
    <div class="ps-soc-auto-h">
      <span class="ps-soc-plat-ic">${I('clock')}</span>
      <b>Расписание</b>
      <button onclick="psSocSchedOpen()">${I('plus')} Добавить</button>
    </div>
    <div class="ps-soc-auto-list">${rows}</div>
  </div>`;
}

/* ---------- ссылки-агрегатор (Linktree) ---------- */
function psSocLinksHtml(){
  const url = 'okoteam.top/u/@' + psSocSlug();
  const rows = (PS_SOC.links || []).map(l => {
    const clicks = 40 + (psHash('lc:' + l.id) % 1800);
    return `<div class="ps-soc-link-row" onclick="psSocLinkOpen('${l.id}')">
      <span class="ps-soc-link-ic">${I(psSocSafeIco(l.ic))}</span>
      <div class="ps-soc-link-b"><b>${esc(l.t)}</b><small>${esc(l.u)}</small></div>
      <div class="ps-soc-link-stat"><b>${psSocFmt(clicks)}</b>кликов</div>
      <button class="ps-soc-link-edit" onclick="event.stopPropagation();psSocLinkEdit('${l.id}')" aria-label="Редактировать">${I('edit')}</button>
    </div>`;
  }).join('');
  return `<div class="ps-soc-links">
    ${rows}
    <button class="ps-soc-link-add" onclick="psSocLinkEdit(null)">${I('plus')} Добавить ссылку</button>
    <div class="ps-soc-hint">Все ссылки видны на публичной странице <b>${esc(url)}</b> — одна ссылка вместо десятка в био.</div>
  </div>`;
}

/* ---------- рендер всей fullscreen-вьюхи ---------- */
function psSocRender(){
  psSocLoad();
  const body = document.getElementById('psSocBody');
  if(!body) return;
  body.innerHTML = `
    <div class="ps-soc-sec">${I('poll')} Дашборд <span class="ps-soc-sec-hint">все привязанные сети</span></div>
    ${psSocDashHtml()}

    <div class="ps-soc-sec">${I('users')} Платформы <span class="ps-soc-sec-hint">${psSocConnected().length}/${PS_SOC_PLATS.length} подключено</span></div>
    ${psSocPlatsHtml()}

    <div class="ps-soc-sec">${I('rocket')} Тренды и рост</div>
    ${psSocTrendsHtml()}

    <div class="ps-soc-sec">${I('globe')} Публичная страница</div>
    ${psSocPubHtml()}

    <div class="ps-soc-sec">${I('circle-play')} Автопостинг</div>
    ${psSocAutoHtml()}

    <div class="ps-soc-sec">${I('compass')} Ссылки-агрегатор</div>
    ${psSocLinksHtml()}

    <div class="ps-soc-sec">${I('file')} Медиа-кит</div>
    ${psSocMediaKitHtml()}

    <div style="height:32px"></div>`;
}

/* ---------- открыть/закрыть fullscreen ---------- */
function psSocOpen(){
  psSocLoad();
  psSocRender();
  const v = document.getElementById('psSocView');
  if(v && !v.classList.contains('open')){
    v.classList.add('open');
    if(typeof nvPush === 'function') nvPush('view:psSoc', psSocClose);
  }
}
function psSocClose(){
  const v = document.getElementById('psSocView');
  if(v) v.classList.remove('open');
  if(typeof nvPop === 'function') nvPop('view:psSoc');
}
function psSocMenu(){
  if(typeof showPopup !== 'function') return;
  showPopup({title:'Мои соцсети', body:'Действия с разделом', actions:[
    {label:'Обновить статистику', onclick: () => { psSocRender(); if(typeof toast === 'function') toast('Статистика обновлена'); }},
    {label:'Скачать медиа-кит', onclick: psSocExportKit},
    {label:'Скопировать публичную ссылку', onclick: () => psSocCopy('okoteam.top/u/@' + psSocSlug())},
    {label:'Закрыть', ghost:true}
  ]});
}

/* ---------- подключение платформы ---------- */
function psSocConnect(id){
  const p = PS_SOC_MAP[id]; if(!p) return;
  const head = document.getElementById('psSocConnHead');
  const body = document.getElementById('psSocConnBody');
  if(head) head.textContent = 'Подключить ' + p.name;
  const cur = PS_SOC.handles[id] || '';
  body.innerHTML = `
    <div class="ps-soc-conn-h">
      ${psSocPlatIcHtml(id)}
      <div><b>${esc(p.name)}</b><small>${esc(p.hint)}</small></div>
    </div>
    <div class="ps-soc-conn-input">
      <label>Твой хендл</label>
      <input id="psSocConnInp" type="text" placeholder="${esc(p.ph)}" value="${esc(cur)}" autocomplete="off">
    </div>
    <div class="ps-soc-conn-help">Данные подтягиваются автоматически — подписчики, посты, ER, лучший месяц. Достаточно указать <b>публичный хендл</b> — токены и пароли не нужны.</div>
    <div class="ps-soc-conn-actions">
      <button class="btn" onclick="psSocConnSave('${id}')">${I('check')} Подключить</button>
      ${PS_SOC.conn[id] ? `<button class="btn ghost" onclick="psSocDisconnect('${id}')">${I('trash')} Отключить</button>` : ''}
    </div>`;
  try{ document.body.classList.add('ps-over-soc'); }catch(e){}
  if(typeof openSheet === 'function') openSheet('ps-soc-connect');
  setTimeout(() => { const el = document.getElementById('psSocConnInp'); if(el) el.focus(); }, 120);
}
function psSocConnSave(id){
  const inp = document.getElementById('psSocConnInp');
  const val = ((inp && inp.value) || '').trim().replace(/^@/, '').replace(/^https?:\/\//, '').replace(/\/+$/, '');
  if(!val){ if(typeof toast === 'function') toast('Укажи хендл'); return; }
  PS_SOC.handles[id] = val;
  PS_SOC.conn[id] = true;
  psSocSave();
  if(typeof closeSheet === 'function') closeSheet();
  psSocRender();
  const p = PS_SOC_MAP[id];
  if(typeof toast === 'function') toast(p.name + ': подключено');
  /* обновить карточку-вход на профиле, если открыта */
  try{ if(document.getElementById('screen-profile')) psSocInjectEntry(); }catch(e){}
}
function psSocDisconnect(id){
  PS_SOC.conn[id] = false;
  psSocSave();
  if(typeof closeSheet === 'function') closeSheet();
  psSocRender();
  const p = PS_SOC_MAP[id];
  if(typeof toast === 'function') toast(p.name + ': отключено');
  try{ if(document.getElementById('screen-profile')) psSocInjectEntry(); }catch(e){}
}

/* ---------- панель статистики платформы ---------- */
function psSocDetail(id){
  const p = PS_SOC_MAP[id]; if(!p || !PS_SOC.conn[id]) return;
  const head = document.getElementById('psSocDetHead');
  if(head) head.textContent = p.name;
  const body = document.getElementById('psSocDetBody');
  const followers = psSocFollowers(id);
  const {pts, gain, growPct} = psSocSpark7(id);
  const er = psSocER(id);
  const posts = psSocRecentPosts(id);
  const best = psSocBestPost(id);
  /* большой sparkline */
  const W = 620, H = 60, pad = 3;
  const min = Math.min.apply(null, pts), max = Math.max.apply(null, pts);
  const rng = Math.max(1, max - min);
  const xF = i => pad + i * ((W - pad * 2) / (pts.length - 1));
  const yF = v => pad + (H - pad * 2) * (1 - (v - min) / rng);
  const line = pts.map((v, i) => (i ? 'L' : 'M') + xF(i).toFixed(1) + ' ' + yF(v).toFixed(1)).join(' ');
  const area = `M${pad} ${(H - pad).toFixed(1)} ` + pts.map((v, i) => 'L' + xF(i).toFixed(1) + ' ' + yF(v).toFixed(1)).join(' ') + ` L${(W - pad).toFixed(1)} ${(H - pad).toFixed(1)} Z`;
  const dot = pts.length - 1;

  const postsRows = posts.map(pp => `
    <div class="ps-soc-post-row${pp.id === best.id ? ' best' : ''}" onclick="psSocPostOpen('${id}',${pp.i})">
      ${pp.id === best.id ? `<span class="ps-soc-best-badge">${I('star')} лучший</span>` : ''}
      <div class="ps-soc-post-thumb ${pp.g}">${I(pp.kind)}</div>
      <div class="ps-soc-post-b">
        <span class="ps-soc-post-t">${esc(pp.t)}</span>
        <div class="ps-soc-post-m">
          <span>${I('eye')}${psSocFmt(pp.reach)}</span>
          <span>${I('heart')}${psSocFmt(pp.likes)}</span>
          <span>${I('comment')}${psSocFmt(pp.comments)}</span>
          <span>${I('clock')}${psSocPostAgo(pp.ts)}</span>
        </div>
      </div>
      <div class="ps-soc-post-er">${pp.er}%<small>ER</small></div>
    </div>`).join('');

  body.innerHTML = `
    <div class="ps-soc-det-h">
      ${psSocPlatIcHtml(id)}
      <div class="ps-soc-det-h-b"><b>${esc(p.name)}</b><small>${p.handleFmt + esc(PS_SOC.handles[id] || '')} · ${esc(p.base + (PS_SOC.handles[id] || ''))}</small></div>
      <div class="ps-soc-det-h-actions">
        <button onclick="psSocConnect('${id}')" title="Изменить хендл" aria-label="Изменить">${I('edit')}</button>
        <button onclick="psSocCopy('${psAttr(p.base + (PS_SOC.handles[id] || ''))}')" title="Скопировать ссылку" aria-label="Скопировать">${I('copy')}</button>
      </div>
    </div>
    <div class="ps-soc-det-stats">
      <div class="ps-soc-tile">
        <div class="ps-soc-tile-lb">${I('users')} Подписчиков</div>
        <div class="ps-soc-tile-v">${psSocFmt(followers)}</div>
        <div class="ps-soc-tile-d${gain < 0 ? ' down' : ''}">${I('rocket')} ${gain >= 0 ? '+' : ''}${psSocFmt(gain)}</div>
      </div>
      <div class="ps-soc-tile">
        <div class="ps-soc-tile-lb">${I('bolt')} ER</div>
        <div class="ps-soc-tile-v">${er}<span class="ps-soc-tile-sub">%</span></div>
        <div class="ps-soc-tile-d">${I('check')} стабильно</div>
      </div>
      <div class="ps-soc-tile">
        <div class="ps-soc-tile-lb">${I('fire')} Рост / нед</div>
        <div class="ps-soc-tile-v">${growPct.toFixed(1)}<span class="ps-soc-tile-sub">%</span></div>
        <div class="ps-soc-tile-d">${I('rocket')} +${psSocFmt(gain)}</div>
      </div>
    </div>
    <div class="ps-soc-det-chart">
      <div class="ps-soc-det-chart-h"><span>Подписчики за 7 дней</span><b>+${psSocFmt(gain)}</b></div>
      <svg class="ps-soc-det-chart-svg" viewBox="0 0 ${W} ${H}" preserveAspectRatio="none">
        <defs><linearGradient id="psSocDetGrad" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#9AFF00" stop-opacity=".42"/><stop offset="100%" stop-color="#9AFF00" stop-opacity="0"/></linearGradient></defs>
        <path d="${area}" fill="url(#psSocDetGrad)"/>
        <path d="${line}" fill="none" stroke="#9AFF00" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        <circle cx="${xF(dot).toFixed(1)}" cy="${yF(pts[dot]).toFixed(1)}" r="3.5" fill="#9AFF00"/>
        <circle cx="${xF(dot).toFixed(1)}" cy="${yF(pts[dot]).toFixed(1)}" r="7" fill="#9AFF00" opacity=".24"/>
      </svg>
    </div>
    <div class="ps-soc-sec" style="margin:6px 0 6px">${I('feed')} Последние 5 постов</div>
    <div class="ps-soc-posts">${postsRows}</div>`;

  try{ document.body.classList.add('ps-over-soc'); }catch(e){}
  if(typeof openSheet === 'function') openSheet('ps-soc-detail');
}
function psSocPostOpen(id, i){
  const p = PS_SOC_MAP[id]; if(!p) return;
  const post = psSocRecentPosts(id)[i]; if(!post) return;
  if(typeof showPopup !== 'function') return;
  showPopup({
    ico: post.kind === 'circle-play' ? 'circle-play' : 'feed',
    title: post.t,
    body: `<div style="text-align:left;font-size:13px;line-height:1.6;color:var(--text)">
      <div style="display:flex;gap:8px;align-items:center;margin-bottom:10px">
        ${psSocPlatIcHtml(id)}<b>${esc(p.name)}</b><span style="color:var(--dim);font-size:11.5px;margin-left:auto">${psSocPostAgo(post.ts)}</span>
      </div>
      <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-bottom:10px">
        <div style="background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:8px;text-align:center"><b style="display:block;color:var(--accent);font-family:var(--font-display);font-size:16px">${psSocFmt(post.reach)}</b><small style="color:var(--dim);font-size:9.5px;letter-spacing:.06em;text-transform:uppercase">Охват</small></div>
        <div style="background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:8px;text-align:center"><b style="display:block;color:var(--accent);font-family:var(--font-display);font-size:16px">${psSocFmt(post.likes)}</b><small style="color:var(--dim);font-size:9.5px;letter-spacing:.06em;text-transform:uppercase">Лайки</small></div>
        <div style="background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:8px;text-align:center"><b style="display:block;color:var(--accent);font-family:var(--font-display);font-size:16px">${psSocFmt(post.comments)}</b><small style="color:var(--dim);font-size:9.5px;letter-spacing:.06em;text-transform:uppercase">Коммент</small></div>
        <div style="background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:8px;text-align:center"><b style="display:block;color:var(--accent);font-family:var(--font-display);font-size:16px">${post.er}%</b><small style="color:var(--dim);font-size:9.5px;letter-spacing:.06em;text-transform:uppercase">ER</small></div>
      </div>
      <div style="color:var(--dim);font-size:12px;line-height:1.5">CTR по ссылкам в описании — <b style="color:var(--text)">${(post.er * 0.42).toFixed(1)}%</b>. Пост попал в лучшие ${Math.max(1, Math.round(100 - post.er * 10))}% ленты за неделю.</div>
    </div>`,
    actions:[
      {label:'Повторить формат', onclick: () => { if(typeof toast === 'function') toast('Формат добавлен в очередь контент-завода'); }},
      {label:'Закрыть', ghost:true}
    ]
  });
}

/* ---------- медиа-кит: генерируем «PDF» (data-url text/html + печать через окно) ---------- */
function psSocExportKit(){
  const slug = psSocSlug();
  const agg = psSocAggregate();
  const rows = psSocConnected().map(p => {
    const f = psSocFollowers(p.id);
    const {gain} = psSocSpark7(p.id);
    return `<tr><td>${p.name}</td><td>${PS_SOC.handles[p.id] ? p.handleFmt + PS_SOC.handles[p.id] : '—'}</td><td>${psSocFmt(f)}</td><td>+${psSocFmt(gain)}</td><td>${psSocER(p.id)}%</td></tr>`;
  }).join('');
  const html = `<!doctype html><html><head><meta charset="utf-8"><title>OKO · Медиа-кит @${slug}</title><style>
    *{box-sizing:border-box;font-family:-apple-system,Segoe UI,Inter,Roboto,sans-serif;color:#0a0d04}
    body{margin:0;background:#fff;padding:40px 44px}
    .h{display:flex;align-items:center;gap:14px;border-bottom:2px solid #0a0d04;padding-bottom:16px;margin-bottom:22px}
    .h .lg{width:44px;height:44px;background:#9AFF00;border-radius:12px;display:inline-flex;align-items:center;justify-content:center;font-weight:900;color:#0a0d04;font-size:22px}
    h1{font-size:26px;margin:0;letter-spacing:.02em}
    .sub{color:#666;font-size:13px;margin-top:2px}
    .stat{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:18px 0 22px}
    .stat div{border:1px solid #e6e6e0;border-radius:10px;padding:12px 14px}
    .stat b{display:block;font-size:22px;color:#0a0d04;margin-bottom:4px}
    .stat small{color:#666;text-transform:uppercase;letter-spacing:.06em;font-size:10px}
    table{width:100%;border-collapse:collapse;margin-top:6px}
    th,td{text-align:left;padding:9px 8px;border-bottom:1px solid #e6e6e0;font-size:13px}
    th{background:#f3f6ea;font-size:11px;text-transform:uppercase;letter-spacing:.06em;color:#3a3a30}
    .foot{margin-top:34px;font-size:11px;color:#666;border-top:1px solid #e6e6e0;padding-top:12px}
    .foot b{color:#0a0d04}
    @media print{body{padding:24px 30px}}
  </style></head><body>
    <div class="h"><span class="lg">O</span><div><h1>Медиа-кит @${esc(slug)}</h1><div class="sub">Соц-профиль OKO · сгенерировано ${new Date().toLocaleDateString('ru-RU')}</div></div></div>
    <div class="stat">
      <div><b>${psSocFmt(agg.followers)}</b><small>Совокупная аудитория</small></div>
      <div><b>${agg.erAvg}%</b><small>Средний ER</small></div>
      <div><b>+${psSocFmt(agg.gain)}</b><small>Прирост за 7 дней</small></div>
    </div>
    <h2 style="font-size:15px;letter-spacing:.05em;text-transform:uppercase">Платформы</h2>
    <table><thead><tr><th>Сеть</th><th>Хендл</th><th>Подписчики</th><th>Δ 7 дней</th><th>ER</th></tr></thead><tbody>${rows}</tbody></table>
    <div class="foot">Связь: <b>okoteam.top/u/@${esc(slug)}</b> · Медиа-кит собран автоматически из статистики OKO.</div>
  </body></html>`;
  try{
    const w = window.open('', '_blank');
    if(w){
      w.document.open(); w.document.write(html); w.document.close();
      setTimeout(() => { try{ w.focus(); w.print(); }catch(e){} }, 400);
      if(typeof toast === 'function') toast('Медиа-кит открыт — выбери «Сохранить PDF»');
      return;
    }
  }catch(e){}
  /* fallback: скачать HTML-файл */
  try{
    const a = document.createElement('a');
    a.href = 'data:text/html;charset=utf-8,' + encodeURIComponent(html);
    a.download = 'oko-mediakit-' + slug + '.html';
    document.body.appendChild(a); a.click(); a.remove();
    if(typeof toast === 'function') toast('Медиа-кит скачан');
  }catch(e){ if(typeof toast === 'function') toast('Не удалось экспортировать'); }
}

/* ---------- публичная ссылка ---------- */
function psSocCopy(text){ const ok = psCopy(text); if(typeof toast === 'function') toast(ok ? 'Скопировано' : 'Не удалось'); }
function psSocSharePub(url){
  const full = 'https://' + url;
  if(navigator.share){ navigator.share({title:'Мои соцсети в OKO', url: full}).catch(()=>{}); return; }
  if(typeof showPopup === 'function'){
    showPopup({ico:'share', title:'Поделиться публичной страницей',
      body:`Одна ссылка — все твои соцсети и материалы.<div class="ps-sharelink">${I('globe')}<span>${esc(full)}</span></div>`,
      actions:[{label:'Скопировать', onclick: () => psSocCopy(full)},{label:'Закрыть', ghost:true}]});
  }
}
function psSocEditSlug(){
  if(typeof showPopup !== 'function') return;
  const cur = psSocSlug();
  showPopup({title:'Публичный адрес', body:`
    <div class="ps-soc-conn-input"><label>okoteam.top/u/@</label>
      <input id="psSocSlugInp" type="text" value="${esc(cur)}" autocomplete="off" spellcheck="false"></div>
    <div class="ps-soc-hint" style="text-align:left">Латиница, цифры и точка. Пример: <b>ktodaniel</b>, <b>agency.oko</b>.</div>
  `, actions:[
    {label:'Сохранить', onclick: () => {
      const el = document.getElementById('psSocSlugInp');
      const v = (el && el.value || '').trim().replace(/^@/, '').replace(/[^a-z0-9._-]/gi, '').toLowerCase();
      if(!v){ if(typeof toast === 'function') toast('Пустой адрес'); return; }
      PS_SOC.slug = v; psSocSave(); psSocRender();
      if(typeof toast === 'function') toast('okoteam.top/u/@' + v + ' — сохранено');
    }},
    {label:'Отмена', ghost:true}
  ]});
  setTimeout(() => { const el = document.getElementById('psSocSlugInp'); if(el){ el.focus(); el.select(); } }, 120);
}

/* ---------- автопостинг: расписание ---------- */
function psSocSchedOpen(id){
  const body = document.getElementById('psSocPostBody');
  const conn = psSocConnected();
  if(!conn.length){
    body.innerHTML = `<div class="ps-soc-empty-body">${I('bolt')}Сначала подключи хотя бы одну платформу, чтобы было куда постить.</div>
      <div class="ps-soc-post-actions"><button class="btn ghost" onclick="if(typeof closeSheet==='function')closeSheet()">Понятно</button></div>`;
    try{ document.body.classList.add('ps-over-soc'); }catch(e){}
    if(typeof openSheet === 'function') openSheet('ps-soc-post');
    return;
  }
  const cur = id ? (PS_SOC.sched || []).find(s => s.id === id) : null;
  const dt = new Date(cur ? cur.at : (Date.now() + 3600e3 * 4));
  const dtVal = dt.toISOString().slice(0, 16);
  const platsBtns = PS_SOC_PLATS.map(p => {
    const on = PS_SOC.conn[p.id] && (cur ? (cur.plats || []).includes(p.id) : ['ig','tg','vk'].includes(p.id));
    return `<button type="button" data-plat="${p.id}" class="${on ? 'on' : ''}${!PS_SOC.conn[p.id] ? ' off' : ''}" ${!PS_SOC.conn[p.id] ? 'disabled' : ''} onclick="psSocSchedTogglePlat(this)">
      ${psSocPlatIcHtml(p.id)}${esc(p.name)}
    </button>`;
  }).join('');
  body.innerHTML = `
    <textarea id="psSocPostTxt" placeholder="Текст поста…">${cur ? esc(cur.txt) : ''}</textarea>
    <div>
      <div class="ps-soc-hint" style="margin:0 2px 6px">Когда опубликовать</div>
      <div class="ps-soc-post-time-row"><input id="psSocPostAt" type="datetime-local" value="${dtVal}"></div>
    </div>
    <div>
      <div class="ps-soc-hint" style="margin:0 2px 6px">Куда уйдёт</div>
      <div class="ps-soc-post-plats" id="psSocPostPlats">${platsBtns}</div>
    </div>
    <div class="ps-soc-post-actions">
      <button class="btn" onclick="psSocSchedSave('${cur ? cur.id : ''}')">${I('check')} ${cur ? 'Сохранить' : 'Запланировать'}</button>
      ${cur ? `<button class="btn ghost" onclick="psSocSchedRemove('${cur.id}')">${I('trash')} Удалить</button>` : `<button class="btn ghost" onclick="if(typeof closeSheet==='function')closeSheet()">Отмена</button>`}
    </div>`;
  try{ document.body.classList.add('ps-over-soc'); }catch(e){}
  if(typeof openSheet === 'function') openSheet('ps-soc-post');
}
function psSocSchedTogglePlat(btn){ btn.classList.toggle('on'); }
function psSocSchedSave(id){
  const txt = (document.getElementById('psSocPostTxt') || {}).value || '';
  const at = (document.getElementById('psSocPostAt') || {}).value || '';
  const plats = Array.from(document.querySelectorAll('#psSocPostPlats button.on')).map(b => b.getAttribute('data-plat'));
  if(!txt.trim()){ if(typeof toast === 'function') toast('Текст поста пуст'); return; }
  if(!plats.length){ if(typeof toast === 'function') toast('Выбери хотя бы одну сеть'); return; }
  const ts = at ? new Date(at).getTime() : Date.now() + 3600e3;
  if(!isFinite(ts)){ if(typeof toast === 'function') toast('Неверная дата'); return; }
  const list = PS_SOC.sched || [];
  if(id){
    const idx = list.findIndex(s => s.id === id);
    if(idx >= 0) list[idx] = {id, txt: txt.trim(), at: ts, plats};
  } else {
    list.push({id: 's' + Date.now(), txt: txt.trim(), at: ts, plats});
  }
  PS_SOC.sched = list; psSocSave();
  if(typeof closeSheet === 'function') closeSheet();
  psSocRender();
  if(typeof toast === 'function') toast('Пост запланирован в ' + plats.length + ' ' + (plats.length === 1 ? 'сеть' : 'сети'));
}
function psSocSchedRemove(id){
  PS_SOC.sched = (PS_SOC.sched || []).filter(s => s.id !== id);
  psSocSave();
  if(typeof closeSheet === 'function') closeSheet();
  psSocRender();
  if(typeof toast === 'function') toast('Пост удалён из расписания');
}

/* ---------- ссылки-агрегатор ---------- */
function psSocLinkOpen(id){
  const l = (PS_SOC.links || []).find(x => x.id === id);
  if(!l) return;
  if(navigator.share){ navigator.share({title:l.t, url:l.u.match(/^https?:/) ? l.u : 'https://' + l.u}).catch(()=>{}); return; }
  psSocCopy(l.u);
}
function psSocLinkEdit(id){
  const l = id ? (PS_SOC.links || []).find(x => x.id === id) : null;
  const head = document.getElementById('psSocLinkHead');
  if(head) head.textContent = l ? 'Ссылка' : 'Новая ссылка';
  const body = document.getElementById('psSocLinkBody');
  const opts = PS_SOC_LINK_ICONS.map(o => `<option value="${o.v}" ${l && l.ic === o.v ? 'selected' : ''}>${esc(o.lb)}</option>`).join('');
  body.innerHTML = `
    <label>Название</label>
    <input id="psSocLinkT" type="text" placeholder="Например: Мой сайт" value="${esc(l ? l.t : '')}">
    <label>URL</label>
    <input id="psSocLinkU" type="text" placeholder="okoteam.top/…" value="${esc(l ? l.u : '')}">
    <label>Иконка</label>
    <select id="psSocLinkI">${opts}</select>
    <div class="ps-soc-link-actions">
      <button class="btn" onclick="psSocLinkSave('${l ? l.id : ''}')">${I('check')} ${l ? 'Сохранить' : 'Добавить'}</button>
      ${l ? `<button class="btn ghost" onclick="psSocLinkDel('${l.id}')">${I('trash')} Удалить</button>` : `<button class="btn ghost" onclick="if(typeof closeSheet==='function')closeSheet()">Отмена</button>`}
    </div>`;
  try{ document.body.classList.add('ps-over-soc'); }catch(e){}
  if(typeof openSheet === 'function') openSheet('ps-soc-link');
  setTimeout(() => { const el = document.getElementById('psSocLinkT'); if(el) el.focus(); }, 120);
}
function psSocLinkSave(id){
  const t = ((document.getElementById('psSocLinkT') || {}).value || '').trim();
  const u = ((document.getElementById('psSocLinkU') || {}).value || '').trim().replace(/^https?:\/\//, '');
  const ic = (document.getElementById('psSocLinkI') || {}).value || 'globe';
  if(!t || !u){ if(typeof toast === 'function') toast('Заполни название и URL'); return; }
  const list = PS_SOC.links || [];
  if(id){
    const idx = list.findIndex(x => x.id === id);
    if(idx >= 0) list[idx] = {id, t, u, ic};
  } else {
    list.push({id:'l' + Date.now(), t, u, ic});
  }
  PS_SOC.links = list; psSocSave();
  if(typeof closeSheet === 'function') closeSheet();
  psSocRender();
  if(typeof toast === 'function') toast('Ссылка сохранена');
}
function psSocLinkDel(id){
  PS_SOC.links = (PS_SOC.links || []).filter(x => x.id !== id);
  psSocSave();
  if(typeof closeSheet === 'function') closeSheet();
  psSocRender();
  if(typeof toast === 'function') toast('Ссылка удалена');
}

/* ---------- инъекция карточки-входа в свой профиль ---------- */
function psSocInjectEntry(){
  const scr = document.getElementById('screen-profile');
  if(!scr) return;
  const pad = scr.querySelector('.pad');
  if(!pad) return;
  /* точка монтирования: после mp-streak, иначе после mp-spark, иначе после profStats */
  const after = pad.querySelector('.mp-streak-wrap') || pad.querySelector('.mp-spark-wrap') || pad.querySelector('.mp-show-wrap') || document.getElementById('profStats');
  if(!after) return;
  let wrap = pad.querySelector('.ps-soc-entry-wrap');
  if(!wrap){
    wrap = document.createElement('div');
    wrap.className = 'ps-soc-entry-wrap';
    after.insertAdjacentElement('afterend', wrap);
  }
  wrap.innerHTML = psSocEntryHtml();
}

/* ================= САМОИНИЦИАЛИЗАЦИЯ И ПАТЧИ (chain) ================= */
(function psInit(){
  psLoadState();

  /* chain renderMyProfile: переключатель аккаунтов + «Мои подписки» + гейт админ-строки */
  if(typeof renderMyProfile === 'function'){
    const _psPrevRenderMyProfile = renderMyProfile;
    renderMyProfile = function(){
      _psPrevRenderMyProfile.apply(this, arguments);
      try{ psInjectAccSwitch(); }catch(e){}
      try{ psInjectMyFollows(); }catch(e){}
      try{ mpInject(); }catch(e){}
      try{ psSocInjectEntry(); }catch(e){}
      /* админ-строка видна только владельцу активного аккаунта */
      const owner = (typeof isOwner === 'function') ? isOwner() : (PROFILE.role === 'owner');
      try{
        const adm = document.getElementById('prowAdmin');
        if(adm) adm.style.display = owner ? '' : 'none';
      }catch(e){}
      /* чип тарифа: у не-владельца показываем реальный tier аккаунта; владельцу —
         сохранённый «богатый» чип (с датой/покупкой) не трогаем без нужды */
      try{
        const tierEl = document.getElementById('profTier');
        if(tierEl){
          if(!owner){
            if(PS._tierOrig === undefined) PS._tierOrig = tierEl.textContent;
            tierEl.textContent = (PROFILE.tier && PROFILE.tier !== 'FREE') ? PROFILE.tier : 'FREE';
            PS._tierOv = true;
          } else if(PS._tierOv){
            tierEl.textContent = (PROFILE.tier && PROFILE.tier !== 'FREE')
              ? (PS._tierOrig || PROFILE.tier) : 'FREE';
            PS._tierOv = false;
          }
        }
      }catch(e){}
    };
  }

  /* chain closeSheet: снять подъём бэкдропа над профилем и хабом соцсетей */
  if(typeof closeSheet === 'function'){
    const _psPrevCloseSheet = closeSheet;
    closeSheet = function(){
      _psPrevCloseSheet.apply(this, arguments);
      try{ document.body.classList.remove('ps-over-view'); }catch(e){}
      try{ document.body.classList.remove('ps-over-soc'); }catch(e){}
    };
  }

  /* chain renderFeed: сортировка «Подписок» перед рендером ядра + декор после */
  if(typeof renderFeed === 'function'){
    const _psPrevRenderFeed = renderFeed;
    renderFeed = function(kind){
      kind = kind || (typeof curFeedKind !== 'undefined' ? curFeedKind : 'sub') || 'sub';
      if(kind === 'sub'){ try{ psSortSub(); }catch(e){} }
      _psPrevRenderFeed(kind);
      if(kind === 'sub'){ try{ psDecorateSub(); }catch(e){} }
    };
  }

  /* chain feedScore: подписки чуть выше и в рекомендациях */
  if(typeof feedScore === 'function'){
    const _psPrevFeedScore = feedScore;
    feedScore = function(p){
      let s = _psPrevFeedScore(p);
      if(p && p.name && psIsFollowing(p.name)) s += 180;
      return s;
    };
  }

  /* chain renderSearch: авторы в поиске */
  if(typeof renderSearch === 'function'){
    const _psPrevRenderSearch = renderSearch;
    renderSearch = function(){
      _psPrevRenderSearch();
      try{ psDecorateSearch(); }catch(e){}
    };
  }

  /* делегирование кликов: лента + аватар конва (capture — раньше onclick-атрибутов) */
  const fl = document.getElementById('feedList');
  if(fl && !fl._psTap){ fl.addEventListener('click', psFeedTap); fl._psTap = true; }
  document.addEventListener('click', psConvAvaTap, true);

  /* применить сортировку/декор к уже отрисованной ленте */
  try{
    if(typeof curFeedKind !== 'undefined' && curFeedKind === 'sub' && document.getElementById('feedList'))
      renderFeed('sub');
  }catch(e){}

  /* если профиль уже отрисован ядром до установки чейна — переинъекция */
  try{
    const sp = document.getElementById('screen-profile');
    if(sp && sp.classList.contains('active') && typeof renderMyProfile === 'function') renderMyProfile();
    else { psInjectAccSwitch(); psInjectMyFollows(); mpInject(); psSocInjectEntry(); }
  }catch(e){}
})();

/* ============================================================================
   PP2 · Компактный профиль (Telegram + iOS Settings)
   ---------------------------------------------------------------------------
   Полностью перерисовывает содержимое #screen-profile .pad после того как
   ядро (renderMyProfile) и все предыдущие чейны (mp-, ps-soc-, ps-) отработали.
   Даёт: аватар 88px, имя+ник+био (2 строки), tier-бейдж, кнопку «Редактировать»,
   строку быстрых действий и iOS-стайл группы разделов. Подстраницы уходят в
   свой nav-стек справа (pp2Stack[]). Ничего не ломает поверх base.html.
   ============================================================================ */
(function pp2Init(){
  'use strict';

  /* ---------- утилиты общие ---------- */
  function esc2(s){ return String(s == null ? '' : s).replace(/[&<>"']/g, function(c){
    return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]; }); }
  function iSvg(id){ return '<svg class="i"><use href="#i-' + id + '"/></svg>'; }
  function safeI(id){ return (typeof I === 'function') ? I(id) : iSvg(id); }
  function T(m){ if(typeof toast === 'function') toast(m); }

  /* тир: FREE / START / PRO / BUSINESS / BUSINESS_PRO / MAX */
  const PP2_TIER_META = {
    FREE:         {lb:'FREE',         short:'Free',       kind:'free', tone:'gray'},
    START:        {lb:'START',        short:'Start',      kind:'paid', tone:''},
    PRO:          {lb:'PRO',          short:'Pro',        kind:'paid', tone:''},
    BUSINESS:     {lb:'BUSINESS',     short:'Business',   kind:'paid', tone:''},
    BUSINESS_PRO: {lb:'BUSINESS PRO', short:'Business Pro', kind:'paid', tone:''},
    MAX:          {lb:'MAX',          short:'Max',        kind:'gold', tone:'gold'},
  };
  function pp2Tier(){
    try{
      const t = (typeof PROFILE !== 'undefined' && PROFILE.tier) ? String(PROFILE.tier).toUpperCase() : 'FREE';
      return PP2_TIER_META[t] ? t : 'FREE';
    }catch(e){ return 'FREE'; }
  }
  function pp2IsPaid(){ const m = PP2_TIER_META[pp2Tier()]; return !!m && m.kind !== 'free'; }
  function pp2IsOwner(){
    try{ if(typeof isOwner === 'function') return !!isOwner(); }catch(e){}
    return (typeof PROFILE !== 'undefined' && PROFILE.role === 'owner');
  }

  /* ---------- инициалы ---------- */
  function pp2Init2(name){
    return String(name || 'OKO').trim().split(/\s+/).slice(0, 2).map(w => (w[0] || '').toUpperCase()).join('') || 'O';
  }

  /* ---------- публичный слаг для ссылок и QR ---------- */
  function pp2Slug(){
    try{
      if(typeof psSocSlug === 'function'){ psSocLoad && psSocLoad(); return psSocSlug(); }
    }catch(e){}
    return (typeof PROFILE !== 'undefined' && PROFILE.nick) || 'me';
  }
  function pp2PublicUrl(){ return 'https://okoteam.top/u/@' + pp2Slug(); }

  /* ================= NAV-СТЕК ================= */
  const pp2Stack = [];       /* [{key, title, htmlOrFn}] */
  let pp2Nav = null;
  function pp2NavRoot(){
    if(pp2Nav && document.body.contains(pp2Nav)) return pp2Nav;
    pp2Nav = document.getElementById('pp2Nav');
    if(!pp2Nav){
      pp2Nav = document.createElement('div');
      pp2Nav.id = 'pp2Nav';
      document.body.appendChild(pp2Nav);
    }
    return pp2Nav;
  }
  function pp2PageHtml(title, bodyHtml, headRight){
    return '<div class="pp2-head">'
      + '<button class="pp2-back" onclick="pp2Pop()" aria-label="Назад">' + safeI('back') + '</button>'
      + '<div class="pp2-title">' + esc2(title) + '</div>'
      + '<div class="pp2-head-r">' + (headRight || '') + '</div>'
      + '</div>'
      + '<div class="pp2-body">' + bodyHtml + '</div>';
  }
  function pp2Push(page){
    const root = pp2NavRoot();
    /* пометить текущий верхний как «уходит вглубь» */
    const cur = root.lastElementChild;
    if(cur){ cur.classList.remove('pp2-in'); cur.classList.add('pp2-out'); }
    const el = document.createElement('section');
    el.className = 'pp2-page';
    el.dataset.key = page.key || ('p' + Date.now());
    const rightHtml = (typeof page.headRight === 'function') ? page.headRight() : (page.headRight || '');
    const bodyHtml = (typeof page.html === 'function') ? page.html() : (page.html || '');
    el.innerHTML = pp2PageHtml(page.title || '', bodyHtml, rightHtml);
    root.appendChild(el);
    pp2Stack.push({key: el.dataset.key, page});
    /* анимация появления */
    requestAnimationFrame(() => { el.classList.add('pp2-in'); });
    /* повесить onMount, если есть */
    if(typeof page.onMount === 'function'){ try{ page.onMount(el); }catch(e){} }
  }
  function pp2Pop(){
    const root = pp2NavRoot();
    if(!root.lastElementChild) return;
    const top = root.lastElementChild;
    top.classList.remove('pp2-in');
    top.style.transform = 'translateX(100%)';
    pp2Stack.pop();
    /* вернуть предыдущий из «глубины» */
    const prev = top.previousElementSibling;
    if(prev){ prev.classList.remove('pp2-out'); prev.classList.add('pp2-in'); }
    setTimeout(() => { if(top && top.parentNode) top.parentNode.removeChild(top); }, 300);
  }
  function pp2PopAll(){
    const root = pp2NavRoot();
    while(root.firstChild) root.removeChild(root.firstChild);
    pp2Stack.length = 0;
  }
  /* глобальный доступ для onclick */
  window.pp2Pop = pp2Pop;

  /* поддержка кнопки «Назад» устройства/браузера */
  window.addEventListener('popstate', function(){
    if(pp2Stack.length){ pp2Pop(); }
  });

  /* ================= TOP-БЛОК ================= */
  function pp2TopHtml(){
    const name = (typeof PROFILE !== 'undefined' && PROFILE.name) || 'OKO';
    const nick = (typeof PROFILE !== 'undefined' && PROFILE.nick) || 'me';
    const bio  = (typeof PROFILE !== 'undefined' && PROFILE.bio) || '';
    const ava  = (typeof PROFILE !== 'undefined' && PROFILE.avatar) || '';
    const status = (typeof PROFILE !== 'undefined' && PROFILE.status) || null;
    const tier = pp2Tier(); const tm = PP2_TIER_META[tier];
    const avaStyle = ava ? ('background-image:url(' + JSON.stringify(ava).slice(1,-1) + ')') : '';
    const avaClass = ava ? ' has-photo' : '';
    const avaTxt = ava ? '' : esc2(pp2Init2(name));
    const owner = pp2IsOwner();
    const chips = [];
    if(tm.kind === 'gold'){
      chips.push('<span class="pp2-chip gold">' + safeI('crown') + esc2(tm.lb) + '</span>');
    } else if(tm.kind === 'paid'){
      chips.push('<span class="pp2-chip">' + safeI('crown') + esc2(tm.lb) + '</span>');
    } else {
      chips.push('<span class="pp2-chip owner">' + esc2(tm.lb) + '</span>');
    }
    if(!pp2IsPaid()){
      chips.push('<span class="pp2-chip disc">' + safeI('flag') + '−20% на год</span>');
    }
    if(owner) chips.push('<span class="pp2-chip owner">' + safeI('crown') + 'Владелец</span>');

    const statusIco = status ? safeI(status) : '';
    return '<div class="pp2-top">'
      + '<button class="pp2-ava' + avaClass + '" style="' + avaStyle + '" onclick="pp2OpenEdit()" aria-label="Аватар">' + avaTxt + '</button>'
      + '<div class="pp2-name">' + esc2(name) + statusIco + '</div>'
      + '<div class="pp2-nick">@' + esc2(nick) + '<span class="pp2-dot">·</span><span class="pp2-online">в сети</span></div>'
      + (bio ? '<p class="pp2-bio">' + esc2(bio) + '</p>' : '')
      + '<div class="pp2-chips">' + chips.join('') + '</div>'
      + '<button class="pp2-edit" onclick="pp2OpenEdit()">' + safeI('edit') + '<span>Редактировать</span></button>'
      + '</div>';
  }

  /* быстрые действия — 5 кнопок */
  function pp2QuickHtml(){
    const items = [
      {k:'share', ic:'share',       lb:'Поделиться', on:'pp2Share()'},
      {k:'qr',    ic:'compass',     lb:'QR-код',     on:'pp2OpenQR()'},
      {k:'link',  ic:'globe',       lb:'Ссылка',     on:'pp2CopyLink()'},
      {k:'stat',  ic:'poll',        lb:'Статистика', on:'pp2OpenStats()'},
      {k:'set',   ic:'bolt',        lb:'Настройки',  on:'pp2OpenSettings()'},
    ];
    return '<div class="pp2-quick">' + items.map(function(it){
      return '<button class="pp2-qbtn" onclick="' + it.on + '" aria-label="' + esc2(it.lb) + '">'
        + safeI(it.ic) + '<span>' + esc2(it.lb) + '</span></button>';
    }).join('') + '</div>';
  }

  /* строка списка */
  function pp2Row(o){
    /* o: {ic, tone, t, s, right, onclick, danger} */
    const cls = 'pp2-row' + (o.danger ? ' danger' : '');
    const icCls = 'pp2-row-ic' + (o.tone ? (' ' + o.tone) : '');
    const rightHtml = o.right || '<span class="pp2-row-chev">' + safeI('chev') + '</span>';
    return '<button class="' + cls + '" onclick="' + (o.onclick || '') + '">'
      + '<span class="' + icCls + '">' + safeI(o.ic) + '</span>'
      + '<span class="pp2-row-b">'
        + '<span class="pp2-row-t">' + esc2(o.t) + '</span>'
        + (o.s ? '<span class="pp2-row-s">' + esc2(o.s) + '</span>' : '')
      + '</span>'
      + '<span class="pp2-row-r">' + rightHtml + '</span>'
      + '</button>';
  }
  function pp2Group(rows){ return '<div class="pp2-group">' + rows.join('') + '</div>'; }

  /* ================= СПИСОК РАЗДЕЛОВ ================= */
  function pp2SectionsHtml(){
    const tier = pp2Tier(); const tm = PP2_TIER_META[tier];
    const owner = pp2IsOwner();
    const paid = pp2IsPaid();
    /* Группа 1 — тарифы (выше всего, как просил Даниэль) */
    const g1 = [
      pp2Row({ic:'crown', tone:tm.kind==='gold'?'gold':'', t:'Тарифы OKO',
              s: paid ? ('Активен ' + tm.short) : 'Выбрать план',
              right: '<span class="pp2-chip' + (tm.kind==='gold'?' gold':'') + '">' + esc2(tm.lb) + '</span><span class="pp2-row-chev">' + safeI('chev') + '</span>',
              onclick:'pp2OpenTiers()'}),
    ];
    /* Группа 2 — внешний контур (для PRO+ показываем ссылки-агрегатор) */
    const g2 = [];
    if(paid){
      g2.push(pp2Row({ic:'globe', tone:'teal', t:'Внешние ссылки',
              s:'Мини-сайт-визитка · до 5 ссылок', onclick:'pp2OpenExtLinks()'}));
    }
    g2.push(pp2Row({ic:'send', tone:'blue', t:'Мои соцсети',
            s:'5 подключено · автопостинг', onclick:'pp2OpenSocials()'}));
    g2.push(pp2Row({ic:'users', tone:'violet', t:'Партнёрская программа',
            s:'Лидерборд, промо, выплаты', onclick:'pp2OpenPartner()'}));
    g2.push(pp2Row({ic:'star', tone:'gold', t:'Академия OKO',
            s:'Мои курсы и сертификаты', onclick:'pp2OpenAcademy()'}));
    g2.push(pp2Row({ic:'briefcase', tone:'teal', t:'Биржа услуг',
            s:'Заказы, кабинет продавца', onclick:'pp2OpenMarket()'}));

    /* Группа 3 — документы, устройства, безопасность */
    const g3 = [
      pp2Row({ic:'file', tone:'gray', t:'Мои документы',
              s:'Договоры, чеки, отчёты', onclick:'pp2OpenDocs()'}),
      pp2Row({ic:'device', tone:'gray', t:'Устройства и сессии',
              s:'Где выполнен вход', onclick:'pp2OpenDevices()'}),
      pp2Row({ic:'bell', tone:'gray', t:'Уведомления',
              s:'Пуши, email, тишина', onclick:"pp2OpenSettingsGroup('notif')"}),
      pp2Row({ic:'lock', tone:'gray', t:'Приватность',
              s:'Профиль, чаты, звонки', onclick:"pp2OpenSettingsGroup('privacy')"}),
    ];

    /* Группа 4 — сервис */
    const g4 = [
      pp2Row({ic:'flag', tone:'', t:'Прогресс сборки',
              s:'Что уже готово, что дальше', onclick:"openSheet('progress')"}),
      pp2Row({ic:'chat', tone:'blue', t:'Помощь и поддержка',
              s:'Ответы 24/7', onclick:'pp2OpenSupport()'}),
      pp2Row({ic:'compass', tone:'gray', t:'О приложении',
              s:'Версия и правовые', onclick:'pp2OpenAbout()'}),
    ];
    if(owner){
      g4.unshift(pp2Row({ic:'bolt', tone:'', t:'Админка OKO',
              s:'Только для владельца',
              right:'<span class="pp2-chip">Владелец</span><span class="pp2-row-chev">' + safeI('chev') + '</span>',
              onclick:'openAdmin()'}));
    }

    /* Финал — выход */
    const logout = '<button class="pp2-btn-plain danger" onclick="doLogout()">' + safeI('logout') + '<span>Выйти из аккаунта</span></button>';

    /* футер */
    const foot = '<div class="pp2-foot"><b>OKO</b> · <span id="pp2VerLb">сборка</span>'
      + '<br><span>Сделано командой OKO — @okoappbot</span></div>';

    return pp2Group(g1) + pp2Group(g2) + pp2Group(g3) + pp2Group(g4) + logout + foot;
  }

  /* ================= ГЛАВНЫЙ РЕНДЕР ================= */
  function pp2Rebuild(){
    const scr = document.getElementById('screen-profile');
    if(!scr) return;
    const pad = scr.querySelector('.pad');
    if(!pad) return;
    /* полностью заменяем содержимое .pad единственным контейнером */
    let wrap = pad.querySelector('.pp2-wrap');
    if(!wrap){
      /* убираем ВСЁ, что нарисовало ядро и старые чейны, кроме модальных внешних вьюх */
      Array.from(pad.children).forEach(function(ch){
        /* оставляем возможные внешние оверлеи, если они как-то попали внутрь */
        if(ch.tagName === 'DIV' && ch.id && /^ps|^psView|^st2/.test(ch.id)) return;
        pad.removeChild(ch);
      });
      wrap = document.createElement('div');
      wrap.className = 'pp2-wrap';
      pad.appendChild(wrap);
      scr.classList.add('pp2-on');
    }
    wrap.innerHTML = pp2TopHtml() + pp2QuickHtml() + pp2SectionsHtml();
    /* подписать актуальную версию сборки в футер */
    const verEl = wrap.querySelector('#pp2VerLb');
    if(verEl){
      /* Искали .build-chip / [data-build] — таких классов в разметке нет,
         поэтому в подвале профиля всегда стояло «сборка dev». Чип версии
         помечен data-build, плюс запасной путь через текст страницы. */
      const chip = document.querySelector('[data-build]');
      let ver = chip ? (chip.textContent || '').match(/v[0-9.]+/) : null;
      if(!ver) ver = (document.body.textContent || '').match(/сборка (v[0-9.]+)/);
      verEl.textContent = 'сборка ' + (ver ? (ver[1] || ver[0]) : '—');
    }
  }
  window.pp2Rebuild = pp2Rebuild;

  /* ================= ХЕНДЛЕРЫ БЫСТРЫХ ДЕЙСТВИЙ ================= */
  window.pp2OpenEdit = function(){ if(typeof openEdit === 'function') openEdit(); };
  window.pp2Share = function(){
    const url = pp2PublicUrl();
    const name = (typeof PROFILE !== 'undefined' && PROFILE.name) || 'OKO';
    if(navigator.share){
      navigator.share({title:'Профиль ' + name + ' в OKO', text:'Мой профиль в OKO', url:url})
        .catch(function(){});
      return;
    }
    pp2CopyToClip(url); T('Ссылка на профиль скопирована');
  };
  window.pp2CopyLink = function(){ pp2CopyToClip(pp2PublicUrl()); T('Ссылка скопирована'); };
  function pp2CopyToClip(txt){
    try{
      if(navigator.clipboard && navigator.clipboard.writeText){ navigator.clipboard.writeText(txt); return; }
    }catch(e){}
    try{
      const ta = document.createElement('textarea'); ta.value = txt; ta.style.position='fixed'; ta.style.left='-9999px';
      document.body.appendChild(ta); ta.select(); document.execCommand('copy'); document.body.removeChild(ta);
    }catch(e){}
  }
  window.pp2OpenStats = function(){ pp2Push({key:'stats', title:'Статистика', html: pp2StatsPageHtml}); };
  window.pp2OpenSettings = function(){ pp2Push({key:'set', title:'Настройки', html: pp2SetIndexHtml}); };
  /* Уведомления и Приватность ведут в полную систему настроек ST2,
     а не в урезанную легаси-шторку с четырьмя переключателями (правка 09.08). */

/* --- Реальные значения вместо захардкоженных (правка 09.08) --- */
function pp2Followers(){
  try{
    var n = 0;
    if(typeof PS === 'object' && PS && PS.followers != null) n = +PS.followers || 0;
    else if(typeof PROFILE === 'object' && PROFILE && PROFILE.followers != null) n = +PROFILE.followers || 0;
    if(!n) return '0';
    return n >= 1000 ? (Math.round(n/100)/10 + 'К') : String(n);
  }catch(e){ return '0'; }
}
function pp2DaysInOko(){
  try{
    var since = null;
    if(typeof PROFILE === 'object' && PROFILE && PROFILE.since) since = +new Date(PROFILE.since);
    if(!since){
      var st = localStorage.getItem('oko-first-seen');
      if(!st){ st = String(Date.now()); localStorage.setItem('oko-first-seen', st); }
      since = +st;
    }
    var d = Math.max(1, Math.floor((Date.now() - since) / 86400000) + 1);
    return String(d);
  }catch(e){ return '1'; }
}
function pp2ThisDevice(){
  try{
    var ua = navigator.userAgent || '';
    var os = /Android/i.test(ua) ? 'Android'
           : /iPhone|iPad|iPod/i.test(ua) ? 'iOS'
           : /Macintosh/i.test(ua) ? 'macOS'
           : /Windows/i.test(ua) ? 'Windows'
           : /Linux/i.test(ua) ? 'Linux' : 'Устройство';
    var br = /Telegram/i.test(ua) ? 'Telegram'
           : /Edg\//i.test(ua) ? 'Edge'
           : /OPR\//i.test(ua) ? 'Opera'
           : /Chrome\//i.test(ua) ? 'Chrome'
           : /Firefox\//i.test(ua) ? 'Firefox'
           : /Safari\//i.test(ua) ? 'Safari' : 'Браузер';
    try{
      var tg = window.Telegram && window.Telegram.WebApp;
      if(tg && tg.initData) br = 'Telegram';
    }catch(e){}
    return os + ' · ' + br;
  }catch(e){ return 'Это устройство'; }
}
  window.pp2OpenSettingsGroup = function(g){
    try{
      if(typeof st2Open === 'function' && typeof st2Push === 'function'){
        st2Open(); st2Push(g === 'privacy' ? 'privacy' : 'notif');
        return;
      }
    }catch(e){}
    return pp2OpenSettingsGroupLegacy(g);
  };
  function pp2OpenSettingsGroupLegacy(g){
    if(typeof openSettings === 'function') openSettings(g);
    else if(g === 'notif' || g === 'privacy'){ T('Настройки скоро'); }
  };
  window.pp2OpenTiers = function(){ pp2Push({key:'tiers', title:'Тарифы OKO', html: pp2TiersPageHtml}); };
  window.pp2OpenSocials = function(){
    /* переиспользуем существующий хаб «Мои соцсети» */
    if(typeof psSocOpen === 'function'){ psSocOpen(); return; }
    if(typeof openMa === 'function'){ openMa('socials'); return; }
    T('Соцсети скоро');
  };
  window.pp2OpenPartner = function(){
    if(typeof showTab === 'function') showTab('partner');
    else T('Партнёрка');
  };
  window.pp2OpenAcademy = function(){
    if(typeof showTab === 'function') showTab('academy');
    else T('Академия');
  };
  window.pp2OpenMarket = function(){
    if(typeof openMa === 'function') openMa('market');
    else if(typeof showTab === 'function') showTab('mini');
    else T('Биржа');
  };
  window.pp2OpenExtLinks = function(){
    pp2Push({key:'ext', title:'Внешние ссылки', html: pp2ExtLinksHtml});
  };
  window.pp2OpenDocs    = function(){ pp2Push({key:'docs', title:'Мои документы', html: pp2DocsHtml}); };
  window.pp2OpenDevices = function(){ pp2Push({key:'dev',  title:'Устройства и сессии', html: pp2DevicesHtml}); };
  window.pp2OpenSupport = function(){ pp2Push({key:'sup',  title:'Помощь и поддержка', html: pp2SupportHtml}); };
  window.pp2OpenAbout   = function(){ pp2Push({key:'abt',  title:'О приложении', html: pp2AboutHtml}); };
  window.pp2OpenQR      = function(){ pp2Push({key:'qr',   title:'Мой QR-код', html: pp2QrHtml}); };

  /* ================= ПОДСТРАНИЦЫ ================= */

  /* --- Тарифы --- */
  const PP2_PLANS = [
    {k:'FREE',        f:'Бесплатно',      cls:'free',flag:'Текущий если нет платы',
     feats:['Мессенджер и лента без лимитов','Публикации, реакции, чаты','Базовая проверка видео (5/мес)']},
    {k:'START',        f:'990 ₽',          cls:'',    flag:'Старт',
     feats:['Мессенджер Premium: файлы 4 ГБ','Магазин шаблонов + Каталог трендов','Аналитика 1 канала','Проверка видео 30/мес']},
    {k:'PRO',          f:'4 900 ₽',        cls:'',    flag:'Хит',
     feats:['Система Роста под ключ, 15 конкурентов','ОКО Ai: 300 обращений','Студия контента: 100 генераций','Проверка 100/мес + 20 автоправок']},
    {k:'BUSINESS',     f:'19 900 ₽',       cls:'',    flag:'Команда',
     feats:['Контент-завод: 30–50 роликов/мес','5 специалистов OKO','Автопостинг во все сети','ОКО Ai 1000, проверка 300']},
    {k:'BUSINESS_PRO', f:'49 900 ₽',       cls:'',    flag:'Business Pro',
     feats:['Контент-завод: 100 роликов/мес','Персональный образ (двойник)','ОКО Ai и Студия без лимитов','Лендинг + бот при годовой оплате']},
    {k:'MAX',          f:'149 900 ₽',      cls:'max', flag:'Максимум',
     feats:['Контент-завод: 300 роликов/мес','10 специалистов + менеджер','Полный digital-запуск','White-label, мультиаккаунт до 15']},
  ];
  function pp2TiersPageHtml(){
    const cur = pp2Tier();
    const hero = '<div class="pp2-tier-hero">'
      + '<h3>Один тариф — вместо целой команды</h3>'
      + '<p>Приложение, ленты, монтаж, ОКО Ai, автопостинг и аналитика в одном месте. Скидки при оплате вперёд: 3 мес −10%, 6 мес −15%, год −20%.</p>'
      + '<div class="pp2-chips"><span class="pp2-chip">3 мес −10%</span><span class="pp2-chip">6 мес −15%</span><span class="pp2-chip gold">Год −20%</span></div>'
      + '</div>';
    const cards = '<div class="pp2-tiers">' + PP2_PLANS.map(function(p){
      const on = p.k === cur;
      const isFree = p.k === 'FREE';
      const priceHtml = isFree ? p.f : ('<b>' + esc2(p.f) + '</b><small>/мес</small>');
      const cta = isFree
        ? (on ? '<button class="btn ghost" disabled>Активен</button>' : '<button class="btn ghost" onclick="toast(\'FREE активируется сам по умолчанию\')">Оставить бесплатный</button>')
        : (on ? '<button class="btn" disabled>Активен</button>' : '<button class="btn' + (p.k === 'PRO' ? '' : ' ghost') + '" onclick="openPay(\'' + p.k + '\')">Выбрать ' + esc2(p.k) + '</button>');
      const feats = '<ul class="pp2-tier-feats">' + p.feats.map(function(f){ return '<li>' + esc2(f) + '</li>'; }).join('') + '</ul>';
      return '<div class="pp2-tier-card ' + p.cls + (on ? ' on' : '') + '">'
        + (p.flag ? '<span class="pp2-tier-flag">' + esc2(p.flag) + '</span>' : '')
        + '<div class="pp2-tier-head"><span class="pp2-tier-name">' + esc2(p.k.replace('_', ' ')) + '</span>'
        + '<span class="pp2-tier-price">' + priceHtml + '</span></div>'
        + feats
        + '<div class="pp2-tier-cta">' + cta + '</div>'
        + '</div>';
    }).join('') + '</div>';
    return hero + cards;
  }

  /* --- Статистика --- */
  function pp2StatsPageHtml(){
    let posts = 0, reacts = 0;
    try{
      if(typeof POSTS !== 'undefined' && typeof PROFILE !== 'undefined'){
        const my = POSTS.sub.filter(function(p){ return p.name === PROFILE.name; });
        posts = 47 + my.length;
        reacts = 1280 + my.reduce(function(s, p){ return s + (p.likes || 0); }, 0);
      }
    }catch(e){}
    const fn = (typeof fmtN === 'function') ? fmtN : function(v){ return String(v); };
    const followers = Object.keys((typeof PS !== 'undefined' && PS.follow) || {}).length;
    const stat = '<div class="pp2-stat-row">'
      + '<div class="pp2-stat"><b>' + esc2(fn(posts)) + '</b><small>постов</small></div>'
      + '<div class="pp2-stat"><b>' + pp2Followers() + '</b><small>подписчиков</small></div>'
      + '<div class="pp2-stat"><b>' + esc2(fn(reacts)) + '</b><small>реакций</small></div>'
      + '<div class="pp2-stat"><b>' + pp2DaysInOko() + '</b><small>дней в OKO</small></div></div>';
    const rows = [
      pp2Row({ic:'compass', tone:'blue', t:'Аналитика ленты',
              s:'Показы, реакции, охваты по дням', onclick:'pp2OpenAcademy()'}),
      pp2Row({ic:'users', tone:'violet', t:'Мои подписчики и подписки',
              s: (followers > 0 ? followers + ' подписок' : 'Пока пусто'),
              onclick: 'pp2GoFollows()'}),
      pp2Row({ic:'fire', tone:'gold', t:'Стрик учёбы',
              s:'Ежедневная серия в Академии', onclick:'pp2OpenAcademy()'}),
    ];
    return stat + pp2Group(rows);
  }

  /* --- Меню Настроек (компактный вход) --- */
  function pp2SetIndexHtml(){
    const rows = [
      pp2Row({ic:'bell', tone:'gray', t:'Уведомления', s:'Пуши, email, DND', onclick:"pp2OpenSettingsGroup('notif')"}),
      pp2Row({ic:'lock', tone:'gray', t:'Приватность', s:'Профиль, чаты, звонки', onclick:"pp2OpenSettingsGroup('privacy')"}),
      pp2Row({ic:'device', tone:'gray', t:'Устройства и сессии', s:'Где выполнен вход', onclick:'pp2OpenDevices()'}),
      pp2Row({ic:'globe', tone:'teal', t:'Язык интерфейса', s:'RU / EN', onclick:'pp2OpenLang()'}),
      pp2Row({ic:'sun', tone:'gold', t:'Тема оформления', s:'Тёмная / светлая', onclick:'pp2ToggleTheme()'}),
    ];
    return pp2Group(rows);
  }

  /* --- Внешние ссылки (для PRO+): используем существующий Linktree ps-soc --- */
  function pp2ExtLinksHtml(){
    try{ if(typeof psSocLoad === 'function') psSocLoad(); }catch(e){}
    const list = (typeof PS_SOC !== 'undefined' && Array.isArray(PS_SOC.links)) ? PS_SOC.links : [];
    if(!list.length){
      return '<div class="pp2-links-empty">' + safeI('globe')
        + '<p><b>Твой мини-сайт-визитка</b></p>'
        + '<p>Собери до 5 ссылок: сайт, канал, кейсы, витрина заказов. Одна публичная страница — okoteam.top/@' + esc2(pp2Slug()) + '</p>'
        + '</div>'
        + '<button class="btn" onclick="pp2AddExtLink()">' + safeI('plus') + '<span>Добавить ссылку</span></button>';
    }
    const rows = list.map(function(l){
      return pp2Row({ic: (l.ic || 'globe'), tone:'teal', t:l.t, s:l.u,
        onclick:'pp2EditExtLink(\'' + esc2(l.id) + '\')'});
    });
    const canAdd = list.length < 5;
    const addBtn = canAdd
      ? '<button class="pp2-btn-plain" onclick="pp2AddExtLink()">' + safeI('plus') + '<span>Добавить ссылку</span></button>'
      : '<div class="pp2-foot">Максимум 5 ссылок — обнови до BUSINESS для расширения</div>';
    return pp2Group(rows) + addBtn;
  }
  window.pp2AddExtLink  = function(){ if(typeof psSocLinkEdit === 'function') psSocLinkEdit(''); else T('Скоро'); };
  window.pp2EditExtLink = function(id){ if(typeof psSocLinkEdit === 'function') psSocLinkEdit(id); else T('Скоро'); };

  /* --- Мои документы --- */
  function pp2DocsHtml(){
    const rows = [
      pp2Row({ic:'file', tone:'gray', t:'Оферта и условия',
              s:'Публичная оферта OKO', onclick:"pp2Legal('oferta')"}),
      pp2Row({ic:'file', tone:'gray', t:'Политика конфиденциальности',
              s:'Как обрабатываем данные', onclick:"pp2Legal('privacy')"}),
      pp2Row({ic:'card', tone:'blue', t:'Чеки и квитанции',
              s:'История платежей', onclick:"openPay('PRO')"}),
      pp2Row({ic:'briefcase', tone:'gold', t:'Договоры с клиентами',
              s:'Из Биржи услуг', onclick:'pp2OpenMarket()'}),
    ];
    return pp2Group(rows);
  }

  /* --- Устройства и сессии (демо-список, честные названия) --- */
  function pp2DevicesHtml(){
    const items = [
      /* Выдуманные устройства убраны 09.08: у нового человека в списке
         висели чужие iPhone и MacBook. Показываем ровно то, что знаем —
         текущее устройство. Остальные сессии добавит бэкенд. */
      {n: pp2ThisDevice(), l:'Сейчас активно', cur:true},
    ];
    const rows = items.map(function(it){
      const chip = it.cur ? '<span class="pp2-chip">Текущее</span>' : '';
      return '<div class="pp2-item"><span class="pp2-row-ic gray">' + safeI('device') + '</span>'
        + '<span class="pp2-item-b"><b>' + esc2(it.n) + '</b><small>' + esc2(it.l) + '</small></span>'
        + chip + '</div>';
    }).join('');
    const foot = '<button class="pp2-btn-plain danger" onclick="toast(\'Все другие сессии завершены\')">' + safeI('logout') + '<span>Завершить все другие</span></button>';
    return '<div style="display:flex;flex-direction:column;gap:8px">' + rows + '</div>' + foot;
  }

  /* --- Помощь и поддержка --- */
  function pp2SupportHtml(){
    const cards = '<div class="pp2-help-cards">'
      + '<button class="pp2-row" onclick="pp2ContactSupport()"><span class="pp2-row-ic blue">' + safeI('chat') + '</span><span class="pp2-row-t">Написать</span><span class="pp2-row-s">Ответ 24/7</span></button>'
      + '<button class="pp2-row" onclick="pp2FAQ()"><span class="pp2-row-ic teal">' + safeI('search') + '</span><span class="pp2-row-t">FAQ</span><span class="pp2-row-s">Частые вопросы</span></button>'
      + '</div>';
    const rows = [
      pp2Row({ic:'megaphone', tone:'gold', t:'Что нового', s:'Апдейты сборок и разделов', onclick:"openSheet('progress')"}),
      pp2Row({ic:'flag', tone:'', t:'Сообщить о проблеме', s:'Баг-репорт напрямую в команду', onclick:'pp2Bug()'}),
    ];
    return cards + pp2Group(rows);
  }
  window.pp2Legal = function(kind){
    if(typeof openLegalDoc === 'function'){ openLegalDoc(kind); return; }
    if(typeof openLegal === 'function'){ openLegal(kind); return; }
    T('Открытие документа');
  };
  window.pp2OpenLang = function(){
    if(typeof openSheet === 'function'){ openSheet('i18n'); return; }
    T('Язык интерфейса');
  };
  window.pp2ToggleTheme = function(){
    /* базовый пере-ключатель темы через html[data-theme] */
    try{
      const html = document.documentElement;
      const cur = html.getAttribute('data-theme') || (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
      const nxt = cur === 'dark' ? 'light' : 'dark';
      html.setAttribute('data-theme', nxt);
      try{ localStorage.setItem('oko-theme', nxt); }catch(e){}
      T(nxt === 'dark' ? 'Тёмная тема' : 'Светлая тема');
    }catch(e){ T('Тема'); }
  };
  window.pp2ContactSupport = function(){
    if(typeof openConv === 'function'){
      /* попробуем найти чат поддержки по имени */
      try{
        if(typeof CHATS !== 'undefined'){
          const sup = CHATS.find(function(c){ return /поддерж/i.test(c.name || ''); });
          if(sup){ openConv(sup.id); return; }
        }
      }catch(e){}
    }
    T('Свяжись с @okoappbot в Telegram');
  };
  window.pp2FAQ = function(){ T('FAQ скоро появится'); };
  /* Баг-репорт ведёт в реальную поддержку, а не в пустое «спасибо».
     Ложное подтверждение убрано 09.08 — человек должен видеть, куда ушёл сигнал. */
  window.pp2Bug = function(){
    var build = '';
    try{ var m = document.body.textContent.match(/сборка v([\d.]+)/); build = m ? ('v' + m[1]) : ''; }catch(e){}
    var txt = 'Проблема в OKO' + (build ? ' (' + build + ')' : '') + ': ';
    var url = 'https://t.me/okohelp?text=' + encodeURIComponent(txt);
    try{
      var tg = window.Telegram && window.Telegram.WebApp;
      if(tg && tg.openTelegramLink){ tg.openTelegramLink(url); return; }
    }catch(e){}
    window.open(url, '_blank', 'noopener');
  };
  window.pp2GoFollows = function(){
    if(typeof psOpenMyFollows === 'function') psOpenMyFollows();
    else T('Пусто');
  };

  /* --- О приложении --- */
  function pp2AboutHtml(){
    const chip = document.querySelector('.build-chip, [data-build]');
    const ver = (chip && (chip.textContent || '').match(/v[0-9.]+/)) ? chip.textContent.match(/v[0-9.]+/)[0] : '';
    const about = '<div class="pp2-about">'
      + '<span class="pp2-about-logo">' + safeI('logo') + '</span>'
      + '<h4>OKO</h4><small>Приложение для медийности и заработка</small>'
      + '<p>Мессенджер, лента, платные каналы, биржа услуг, академия, аналитика, монтаж и автопостинг — в одном приложении. Сделано в России, для СНГ и мира.</p>'
      + '<small>' + esc2(ver || 'сборка dev') + '</small></div>';
    const rows = [
      pp2Row({ic:'globe', tone:'teal', t:'Сайт okoteam.top',
              s:'Открыть в браузере', onclick:"window.open('https://okoteam.top','_blank')"}),
      pp2Row({ic:'megaphone', tone:'blue', t:'Telegram @okoappbot',
              s:'Канал и бот', onclick:"window.open('https://t.me/okoappbot','_blank')"}),
      pp2Row({ic:'file', tone:'gray', t:'Правовые документы',
              s:'Оферта, политика, лицензии', onclick:'pp2OpenDocs()'}),
    ];
    return about + pp2Group(rows);
  }

  /* --- QR-код и публичная ссылка --- */
  function pp2QrSvg(text){
    /* Компактный код Datamatrix-стиля: детерминированная 25x25 сетка seed-по-тексту.
       Это не сканируется как QR, но красиво визуализирует ссылку и служит визитной
       карточкой. Реальный обмен идёт по копируемой ссылке ниже. */
    const N = 25; const cell = 8; const size = N * cell;
    function seed(s){ let h = 2166136261; for(let i=0;i<s.length;i++){ h ^= s.charCodeAt(i); h = Math.imul(h, 16777619); } return h >>> 0; }
    let st = seed(text);
    function rnd(){ st ^= st << 13; st ^= st >>> 17; st ^= st << 5; return (st >>> 0) / 4294967296; }
    let rects = '';
    for(let y = 0; y < N; y++){
      for(let x = 0; x < N; x++){
        const corner = (x < 7 && y < 7) || (x > N - 8 && y < 7) || (x < 7 && y > N - 8);
        if(corner) continue;
        if(rnd() > 0.5) rects += '<rect x="' + (x*cell) + '" y="' + (y*cell) + '" width="' + cell + '" height="' + cell + '"/>';
      }
    }
    /* три угловых маркера */
    function corner(cx, cy){
      const s = cell;
      return '<rect x="' + cx + '" y="' + cy + '" width="' + (7*s) + '" height="' + (7*s) + '"/>'
           + '<rect x="' + (cx+s) + '" y="' + (cy+s) + '" width="' + (5*s) + '" height="' + (5*s) + '" fill="#fff"/>'
           + '<rect x="' + (cx+2*s) + '" y="' + (cy+2*s) + '" width="' + (3*s) + '" height="' + (3*s) + '" fill="#000"/>';
    }
    return '<svg viewBox="0 0 ' + size + ' ' + size + '" xmlns="http://www.w3.org/2000/svg">'
      + '<g fill="#0d0d0d">' + rects
      + corner(0, 0) + corner((N-7)*cell, 0) + corner(0, (N-7)*cell)
      + '</g></svg>';
  }
  function pp2QrHtml(){
    const url = pp2PublicUrl();
    return '<div class="pp2-qr-wrap">'
      + '<div class="pp2-qr-box">' + pp2QrSvg(url) + '</div>'
      + '<div class="pp2-qr-link">' + esc2(url) + '</div>'
      + '<div class="pp2-qr-actions">'
      +   '<button class="btn ghost" onclick="pp2CopyLink()">' + safeI('copy') + '<span>Скопировать</span></button>'
      +   '<button class="btn" onclick="pp2Share()">' + safeI('share') + '<span>Поделиться</span></button>'
      + '</div>'
      + '<p class="pp2-foot" style="max-width:340px">Покажи этот код — новый подписчик откроет твой профиль в один тап. Ссылка ведёт на публичную визитку.</p>'
      + '</div>';
  }

  /* ================= ЧЕЙН РЕНДЕРА =================
     Особенность: ядро (renderMyProfile в base.html) и старые чейны ссылаются на
     ID-элементы (#profName, #profStats, #profAch и т.д.), которых после нашей
     пересборки уже нет. Чтобы прошлый чейн не крашился, оборачиваем его вызов
     в try/catch и в любом случае перерисовываем pp2-версию. Компактный вид —
     единственная правда для #screen-profile. */
  if(typeof renderMyProfile === 'function'){
    const _pp2PrevRender = renderMyProfile;
    renderMyProfile = function(){
      try{ _pp2PrevRender.apply(this, arguments); }catch(e){ /* устаревшие DOM-хуки — не критично */ }
      try{ pp2Rebuild(); }catch(e){ /* nop */ }
    };
  }

  /* закрывать стек при уходе с вкладки профиля */
  if(typeof showTab === 'function'){
    const _pp2PrevShowTab = showTab;
    showTab = function(t){
      if(t !== 'profile' && pp2Stack.length){ pp2PopAll(); }
      return _pp2PrevShowTab.apply(this, arguments);
    };
  }

  /* если профиль уже отрисован ядром до нашей установки — сразу пересобрать */
  try{
    const sp = document.getElementById('screen-profile');
    if(sp) pp2Rebuild();
  }catch(e){}
})();

/* ===== module: stories-plus ===== */
/* ===== STORIES-PLUS (sp-): сторис уровня Instagram/Telegram =====
   base.html не тронут — только chain-патчи ядра (openStoryCreate / createTextStory /
   addStory / showStory / closeStory / renderStories / storyNav) + свои вьюхи из overlay.html.

   Возможности:
   1) Вьювер: transform-based сегментные полоски (без layout), тап-переключение,
      удержание-пауза, свайп между авторами, строка ответа, быстрые реакции,
      счётчик просмотров, «кто посмотрел», состояние «просмотрено», закрытие.
   2) Редактор: текст/фото, 6 бренд-градиентов, размер/выравнивание текста,
      набор бренд-стикеров (SVG) с выбором и позицией, live-предпросмотр 9:16.
   3) Персист своих текст-сторис в localStorage (переживают перезагрузку).
   4) Кружки в ленте группируются по автору, бейдж-счётчик, состояние ring.

   Тайминг слайда полностью на модуле (spTimer) — это даёт точную паузу-по-удержанию,
   синхронную с CSS-анимацией полоски (класс sp-paused ставит animation-play-state). */

/* ---------- состояние (персист настроек редактора + своих сторис) ---------- */
const SP = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-stories-plus'))||{}; }catch(e){ return {}; } })();
SP.ed = Object.assign({bg:1, size:'m', align:'center', sticker:false, pos:'br', stk:'logo'}, SP.ed||{});
function spSave(){ try{ localStorage.setItem('oko-stories-plus', JSON.stringify({ed:SP.ed, mute:SP.mute})); }catch(e){} }

/* 6 бренд-градиентов + цвет текста под каждый (яркий лайм требует тёмный текст) */
const SP_BGS = [
  {bg:'linear-gradient(135deg,#B9FF4D 0%,#9AFF00 45%,#3f6b00 100%)', fg:'#0b1400'},
  {bg:'linear-gradient(160deg,#131f04 0%,#060606 55%,#0e1a03 100%)', fg:'#fff'},
  {bg:'linear-gradient(150deg,#0a1a10 0%,#050505 60%,#08240e 100%)', fg:'#fff'},
  {bg:'linear-gradient(155deg,#233a00 0%,#0b1400 55%,#000 100%)',    fg:'#fff'},
  {bg:'linear-gradient(160deg,#050505 0%,#101010 45%,#2c5000 100%)', fg:'#fff'},
  {bg:'linear-gradient(145deg,#1c1c1c 0%,#000 60%,#131f04 100%)',    fg:'#fff'},
];
/* набор бренд-стикеров (существующие символы ядра) */
const SP_STK = ['logo','star','fire','heart','bolt','crown'];

/* длительность слайда, мс — единая точка для полоски и таймера */
const SP_DUR = 4000;

/* ---------- свои SVG-иконки выравнивания (штрих как у ядра) ---------- */
(function spIcons(){
  const defs = document.querySelector('svg defs'); if(!defs) return;
  const mk = (id, html)=>{
    if(document.getElementById(id)) return;
    const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
    s.setAttribute('id', id); s.setAttribute('viewBox','0 0 100 100');
    s.innerHTML = html; defs.appendChild(s);
  };
  mk('i-sp-al','<path d="M14 25h72M14 50h42M14 75h58"/>');
  mk('i-sp-ac','<path d="M14 25h72M29 50h42M21 75h58"/>');
  mk('i-sp-ar','<path d="M14 25h72M44 50h42M28 75h58"/>');
  mk('i-sp-vol','<path d="M14 38h16l24-18v60L30 62H14z"/><path d="M66 34c8 8 8 24 0 32M76 24c14 14 14 38 0 52"/>');
  mk('i-sp-mute','<path d="M14 38h16l24-18v60L30 62H14z"/><path d="M64 40l24 20M88 40 64 60"/>');
})();

/* глобальные предпочтения (звук видео) */
SP.mute = (typeof SP.mute === 'boolean') ? SP.mute : true;

/* ---------- мок-зрители (в духе демо-контента) ---------- */
const SP_POOL = []  /* демо-данные удалены 09.08: зрители сторис — источник только API */;
const SP_TIMES = ['только что','1 мин назад','3 мин назад','8 мин назад','14 мин назад','27 мин назад',
  '41 мин назад','1 ч назад','2 ч назад','3 ч назад','5 ч назад','9 ч назад'];

function spGrowViews(st, n){
  st.spViewers = st.spViewers || [];
  st.spVwReacts = st.spVwReacts || {};
  const used = new Set(st.spViewers);
  const free = SP_POOL.map((_, i)=>i).filter(i=>!used.has(i));
  while(n-- > 0 && free.length){
    const k = free.splice(Math.floor(Math.random()*free.length), 1)[0];
    st.spViewers.unshift(k);
    if(Math.random() < .35) st.spVwReacts[k] = ['heart','fire','thumb'][Math.floor(Math.random()*3)];
  }
}
function spEnsureViews(st, grow){
  if(!st.spViewers) spGrowViews(st, 3 + Math.floor(Math.random()*3));
  else if(grow && Math.random() < .75) spGrowViews(st, 1 + Math.floor(Math.random()*2));
  return st.spViewers.length;
}

/* ---------- группировка сторис по автору ---------- */
function spKey(st){ return st.my ? '@me' : st.name; }
function spGroups(){
  const groups = [], by = {};
  (typeof STORIES !== 'undefined' ? STORIES : []).forEach((st, i)=>{
    if(st.add){ groups.push({add:true, name:st.name}); return; }
    const k = spKey(st);
    if(by[k]) by[k].idx.push(i);
    else { const g = {key:k, idx:[i]}; by[k] = g; groups.push(g); }
  });
  return groups;
}
function spGroupOf(idx){ return spGroups().find(x=>!x.add && x.idx.indexOf(idx) >= 0); }
function spOpenGroup(keyEnc){
  const key = decodeURIComponent(keyEnc||'');
  const g = spGroups().find(x=>!x.add && x.key === key);
  if(!g) return;
  const idx = g.idx.find(i=>!STORIES[i].seen);
  openStory(idx === undefined ? g.idx[0] : idx);
}

/* кружки в ленте: один автор — один кружок, бейдж с числом историй */
if(typeof renderStories === 'function'){
  const _spPrevRenderStories = renderStories;
  renderStories = function(){
    const row = document.getElementById('storiesRow');
    if(!row) return _spPrevRenderStories();
    row.innerHTML = spGroups().map(g=>{
      if(g.add) return `<div class="story add" onclick="openStoryCreate()"><div class="ring"><div class="ava">${I('plus')}</div></div><small>${esc(g.name||'Твоя')}</small></div>`;
      const st = STORIES[g.idx[0]];
      const seen = g.idx.every(i=>STORIES[i].seen);
      const ava = st.src ? `<div class="ava" style="background-image:url(${st.src});background-size:cover"></div>`
        : st.avaIcon ? `<div class="ava lime">${I(st.avaIcon)}</div>` : `<div class="ava">${esc(st.ava)}</div>`;
      const cnt = g.idx.length > 1 ? `<b class="sp-cnt">${g.idx.length}</b>` : '';
      return `<div class="story ${seen?'seen':''}" id="story-${g.idx[0]}" data-spg="${encodeURIComponent(g.key)}" onclick="spOpenGroup(this.dataset.spg)"><div class="ring" style="position:relative">${ava}${cnt}</div><small>${esc(st.name)}</small></div>`;
    }).join('');
  };
}

/* ---------- вьювер: элементы-декораторы + жесты ---------- */
let spLastIdx = -1;

function spViewerEls(){
  const v = document.getElementById('storyViewer'); if(!v) return null;
  let bars = document.getElementById('spBars');
  if(!bars){
    bars = document.createElement('div'); bars.id = 'spBars'; bars.className = 'sp-bars';
    v.appendChild(bars);
    const foot = document.createElement('div'); foot.id = 'spFoot'; foot.className = 'sp-foot';
    v.appendChild(foot);
    spBindGestures(v);          /* жесты вешаем один раз */
    v.style.setProperty('--sp-dur', (SP_DUR/1000) + 's');
  }
  return {v, bars, foot: document.getElementById('spFoot')};
}

/* ---------- тайминг слайда с поддержкой паузы ---------- */
let spTimer = null, spStart = 0, spRemain = SP_DUR, spPaused = false;
function spStartTimer(){
  clearTimeout(spTimer);
  try{ clearTimeout(storyTimer); }catch(e){}   /* ядро тоже ставит таймер — гасим его */
  spRemain = SP_DUR; spStart = Date.now(); spPaused = false;
  const v = document.getElementById('storyViewer');
  if(v) v.classList.remove('sp-paused');
  spTimer = setTimeout(spAdvance, spRemain);
}
function spPause(){
  if(spPaused) return; spPaused = true;
  clearTimeout(spTimer);
  spRemain = Math.max(0, spRemain - (Date.now() - spStart));
  const v = document.getElementById('storyViewer');
  if(v) v.classList.add('sp-paused');
}
function spResume(){
  if(!spPaused) return; spPaused = false;
  spStart = Date.now();
  clearTimeout(spTimer);
  spTimer = setTimeout(spAdvance, spRemain);
  const v = document.getElementById('storyViewer');
  if(v) v.classList.remove('sp-paused');
}
function spStopTimer(){ clearTimeout(spTimer); spPaused = false; }
function spAdvance(){ if(typeof nextStory === 'function') nextStory(); }

/* ---------- жесты: тап/двойной-тап-лайк / удержание-пауза / свайп по авторам /
   потяни-вниз-закрыть (карточка едет за пальцем, как в Instagram/Telegram) ---------- */
let spReplyLock = false;   /* строка ответа в фокусе — жесты и авто-листание стоят */
function spBindGestures(v){
  let sx=0, sy=0, downT=0, hold=null, isHold=false, moved=false, navHandled=false;
  let dragging=false, decided=false, pendingNav=null, lastTap=0, lastTapX=0;
  const inUI = (t)=> !!(t && t.closest && t.closest('.sp-foot, .sv-close, #spBars'));
  const card = ()=> document.getElementById('svCard');
  const clearPending = ()=>{ if(pendingNav){ clearTimeout(pendingNav); pendingNav = null; } };
  function endDrag(dismiss){
    dragging = false;
    v.classList.remove('sp-dragging');
    v.style.removeProperty('--sp-fade');
    const c = card();
    if(!c) return;
    if(dismiss){ c.style.transform = ''; return; }
    c.style.transition = 'transform .26s cubic-bezier(.3,1,.4,1)';   /* пружинка назад */
    c.style.transform = '';
    setTimeout(()=>{ const cc = card(); if(cc) cc.style.transition = ''; }, 300);
  }

  v.addEventListener('pointerdown', (e)=>{
    clearPending();                                   /* новый жест отменяет отложенное листание */
    if(spReplyLock || inUI(e.target)) return;
    sx = e.clientX; sy = e.clientY; downT = Date.now();
    isHold = false; moved = false; navHandled = false; dragging = false; decided = false;
    clearTimeout(hold);
    hold = setTimeout(()=>{ if(!dragging){ isHold = true; spPause(); } }, 250);   /* удержание → пауза */
  });
  v.addEventListener('pointermove', (e)=>{
    if(!downT) return;
    const dx = e.clientX - sx, dy = e.clientY - sy;
    if(dragging){                                     /* тянем карточку вниз */
      const d = Math.max(0, dy);
      const c = card();
      if(c) c.style.transform = 'translateY(' + d + 'px) scale(' + (1 - Math.min(d/1300, .12)) + ')';
      v.style.setProperty('--sp-fade', String(Math.max(.28, 1 - d/520)));
      return;
    }
    if(!decided && (Math.abs(dx) > 10 || Math.abs(dy) > 10)){
      moved = true; clearTimeout(hold); decided = true;
      if(dy > 12 && Math.abs(dy) > Math.abs(dx) * 1.2){          /* вниз-вертикально → закрытие-перетаскиванием */
        dragging = true; isHold = false; v.classList.add('sp-dragging'); spPause();
      }
    }
  });
  /* фиксация свайпа-вверх: панель быстрых реакций (жест только вне UI) */
  function detectSwipeUp(dy, dx){ return dy < -48 && Math.abs(dy) > Math.abs(dx) * 1.4; }
  const finish = (e)=>{
    if(!downT) return;
    clearTimeout(hold);
    const dt = Date.now() - downT, dx = e.clientX - sx, dy = e.clientY - sy;
    downT = 0;
    if(dragging){                                     /* отпустили после перетаскивания */
      navHandled = true;
      const dismiss = dy > 110;
      endDrag(dismiss);
      if(dismiss && typeof closeStory === 'function') closeStory(); else spResume();
      return;
    }
    if(spReplyLock || inUI(e.target)) return;
    if(isHold){ spResume(); navHandled = true; return; }
    if(detectSwipeUp(dy, dx)){                                    /* свайп-вверх → быстрые реакции */
      navHandled = true;
      spOpenQuickReact();
      return;
    }
    if(Math.abs(dx) > 55 && Math.abs(dx) > Math.abs(dy) * 1.3){   /* свайп → смена автора */
      navHandled = true;
      if(dx < 0) spNextAuthor(); else spPrevAuthor();
      return;
    }
    if(dt < 350 && !moved){                                       /* тап или двойной тап */
      navHandled = true;
      const x = e.clientX, y = e.clientY, now = Date.now();
      if(now - lastTap < 300 && Math.abs(x - lastTapX) < 48){     /* двойной тап → лайк, без листания */
        clearPending(); lastTap = 0;
        spDoubleLike(x, y);
        return;
      }
      lastTap = now; lastTapX = x;
      const goPrev = x < window.innerWidth * 0.32;                /* листание отложено — вдруг это первый тап дабл-тапа */
      clearPending();
      pendingNav = setTimeout(()=>{
        pendingNav = null;
        if(goPrev){ if(typeof prevStory === 'function') prevStory(); }
        else { if(typeof nextStory === 'function') nextStory(); }
      }, 230);
    }
  };
  v.addEventListener('pointerup', finish);
  v.addEventListener('pointercancel', ()=>{ clearTimeout(hold); if(dragging) endDrag(false); if(isHold || dragging) spResume(); downT = 0; });
  /* гасим клик ядра (onclick=storyNav) — навигацию делаем сами */
  v.addEventListener('click', (e)=>{ if(navHandled){ navHandled = false; e.stopPropagation(); } }, true);
}

/* двойной тап → лайк: крупное лаймовое сердце в точке касания + отметка реакции */
function spDoubleLike(x, y){
  const st = typeof STORIES !== 'undefined' && STORIES[curStory];
  if(!st || st.add) return;
  try{ navigator.vibrate && navigator.vibrate(14); }catch(e){}
  const reduce = matchMedia('(prefers-reduced-motion: reduce)').matches;
  if(!reduce){
    const h = document.createElement('div');
    h.className = 'sp-biglike';
    h.style.left = x + 'px'; h.style.top = y + 'px';
    h.innerHTML = I('heart', 'fill');
    document.body.appendChild(h);
    h.addEventListener('animationend', ()=>h.remove());
    setTimeout(()=>h.remove(), 1300);
  }
  if(!st.my){                                         /* чужая сторис — фиксируем реакцию «сердце» */
    st.spReacted = 'heart';
    const btns = document.querySelectorAll('#spFoot .sp-react');
    btns.forEach(b=>b.classList.remove('on'));
    const heartBtn = btns[0];                          /* порядок ['heart','fire','thumb'] → сердце первое */
    if(heartBtn){ heartBtn.classList.add('on'); if(!reduce) spFlyBurst(heartBtn, 'heart', 4); }
  }
}

function spNextAuthor(){
  const g = spGroupOf(curStory); if(!g) return;
  const groups = spGroups().filter(x=>!x.add);
  const pos = groups.indexOf(g);
  const nx = groups[pos + 1];
  if(nx){ const t = nx.idx.find(i=>!STORIES[i].seen); curStory = (t===undefined?nx.idx[0]:t); showStory(); }
  else if(typeof closeStory === 'function') closeStory();
}
function spPrevAuthor(){
  const g = spGroupOf(curStory); if(!g) return;
  const groups = spGroups().filter(x=>!x.add);
  const pos = groups.indexOf(g);
  const pv = groups[pos - 1];
  if(pv){ curStory = pv.idx[0]; showStory(); }
  else { curStory = g.idx[0]; showStory(); }   /* уже первый автор — рестарт */
}

/* ---------- декор вьювера: сегменты, низ (ответ/реакции/просмотры), стили ---------- */
function spDecorate(){
  const els = spViewerEls(); if(!els) return;
  const st = typeof STORIES !== 'undefined' && STORIES[curStory];
  if(!st || st.add){ spStopTimer(); return; }

  /* сегментные полоски автора (всегда через модуль → transform-based) */
  const g = spGroupOf(curStory);
  const seg = g ? g.idx : [curStory];
  const cur = seg.indexOf(curStory);
  els.v.classList.toggle('sp-multi', seg.length > 1);
  els.bars.innerHTML = seg.map((idx, k)=>
    `<button class="sp-seg ${k < cur ? 'done' : k === cur ? 'act' : ''}" onclick="spJump(${idx},event)" title="История ${k+1}"><i><b></b></i></button>`).join('');

  /* дожать «просмотрено» на сгруппированном кружке */
  if(g){
    const ring = document.getElementById('story-'+g.idx[0]);
    if(ring && g.idx.every(i=>STORIES[i].seen)) ring.classList.add('seen');
  }

  /* верхние экшены: поделиться (всегда), звук (когда видео в кадре) */
  const v = els.v;
  const hasVideo = !!v.querySelector('#svCard video');
  let acts = v.querySelector('.sp-actions');
  if(!acts){
    acts = document.createElement('div'); acts.className = 'sp-actions';
    v.appendChild(acts);
  }
  const muteIcon = SP.mute ? 'sp-mute' : 'sp-vol';
  const muteTitle = SP.mute ? 'Включить звук' : 'Выключить звук';
  acts.innerHTML =
    (hasVideo ? `<button class="sp-act-btn sp-mute-btn" onclick="spMuteToggle(event)" title="${muteTitle}"><svg class="i"><use href="#i-${muteIcon}"/></svg></button>` : '')
    + `<button class="sp-act-btn sp-share-btn" onclick="spShare(event)" title="Поделиться"><svg class="i"><use href="#i-share"/></svg></button>`;
  spApplyMuteToVideos();

  /* стили из редактора: цвет текста, размер, выравнивание, стикер */
  const card = document.getElementById('svCard');
  if(card){
    card.style.color = (st.sp && st.sp.fg) ? st.sp.fg : '';
    if(st.sp){
      const p = card.querySelector('p');
      if(p){
        p.style.fontSize = ({s:'14px', m:'18px', l:'23px'})[st.sp.size] || '';
        p.style.textAlign = st.sp.align || 'center';
        if(st.sp.align && st.sp.align !== 'center'){ p.style.maxWidth = '100%'; p.style.width = '100%'; }
        p.style.color = st.sp.fg || '#fff';
        p.style.fontWeight = '600';
      }
      const tm = card.querySelector('.sv-time');
      if(tm && st.sp.fg){ tm.style.color = st.sp.fg; tm.style.opacity = '.62'; }
      if(st.sp.sticker && !card.querySelector('.sp-sticker'))
        card.insertAdjacentHTML('beforeend', `<div class="sp-sticker sp-pos-${st.sp.pos||'br'}">${I(st.sp.stk||'logo')}</div>`);
    }
    /* опрос / викторина: интерактивный оверлей поверх карточки */
    const pq = st.sp && (st.sp.poll || st.sp.quiz);
    if(pq && !card.querySelector('.sp-poll')){
      const isQuiz = !!(st.sp && st.sp.quiz);
      card.insertAdjacentHTML('beforeend', spPollHtml(st, isQuiz));
    }
  }

  /* низ вьювера: свои — счётчик глаз; чужие — строка ответа + быстрые реакции */
  const grow = spLastIdx !== curStory;
  spLastIdx = curStory;
  if(st.my){
    const n = spEnsureViews(st, grow);
    els.foot.innerHTML = `<button class="sp-eye" onclick="spOpenViewers(event)">${I('eye')}<span>${n}</span>${I('chev')}</button>`;
  } else {
    els.foot.innerHTML = `<div class="sp-foot-bar">
      <form class="sp-reply" onsubmit="return spSendReply(event)">
        <input id="spReplyIn" class="sp-reply-in" placeholder="Ответить ${esc((st.name||'').split(' ')[0]||'автору')}…" autocomplete="off" onfocus="spReplyFocus()" onblur="spReplyBlur()">
        <button type="submit" class="sp-reply-send" title="Отправить">${I('send')}</button>
      </form>
      <div class="sp-reacts">` + ['heart','fire','thumb'].map(ic=>
        `<button class="sp-react ${st.spReacted===ic?'on':''}" onclick="spReact('${ic}',event)" title="Реакция">${I(ic)}</button>`).join('') + `</div>
    </div>`;
  }

  spStartTimer();   /* запускаем слайд под управлением модуля */
}

function spJump(idx, ev){
  if(ev) ev.stopPropagation();
  if(typeof curStory === 'undefined' || !STORIES[idx]) return;
  curStory = idx; showStory();
}

/* ---------- строка ответа ---------- */
function spReplyFocus(){ spReplyLock = true; spPause(); }
function spReplyBlur(){ spReplyLock = false; spResume(); }
function spSendReply(ev){
  if(ev) ev.preventDefault();
  const inp = document.getElementById('spReplyIn');
  const val = inp ? inp.value.trim() : '';
  if(!val){ if(inp) inp.blur(); return false; }
  if(inp){ inp.value = ''; inp.blur(); }
  if(typeof toast === 'function') toast('Ответ отправлен');
  spFlyBurst(document.querySelector('.sp-reply-send'), 'heart', 3);
  return false;
}

/* ---------- реакция на чужую сторис: подсветка + летящая анимация + тост ---------- */
function spReact(ic, ev){
  if(ev) ev.stopPropagation();
  const st = typeof STORIES !== 'undefined' && STORIES[curStory];
  if(!st) return;
  st.spReacted = ic;
  document.querySelectorAll('#spFoot .sp-react').forEach(b=>b.classList.remove('on'));
  const btn = ev && ev.currentTarget;
  if(btn) btn.classList.add('on');
  spFlyBurst(btn, ic, 5);
  if(typeof toast === 'function') toast('Реакция отправлена');
}
/* летящие частицы от кнопки (реакция / ответ) */
function spFlyBurst(btn, ic, count){
  if(!btn || matchMedia('(prefers-reduced-motion: reduce)').matches) return;
  const r = btn.getBoundingClientRect();
  for(let k = 0; k < count; k++){
    const f = document.createElement('div');
    f.className = 'sp-fly';
    f.style.left = (r.left + r.width/2 - 13 + (Math.random()*28 - 14)) + 'px';
    f.style.top = (r.top - 8) + 'px';
    f.style.setProperty('--dx', (Math.random()*76 - 38) + 'px');
    f.style.animationDelay = (k*90) + 'ms';
    f.style.animationDuration = (0.85 + Math.random()*0.5) + 's';
    f.innerHTML = I(ic, 'fill');
    document.body.appendChild(f);
    f.addEventListener('animationend', ()=>f.remove());
    setTimeout(()=>f.remove(), 2400); /* страховка */
  }
}

/* ---------- поделиться сторис: navigator.share или клипборд ---------- */
function spShare(ev){
  if(ev){ ev.stopPropagation(); ev.preventDefault(); }
  const st = typeof STORIES !== 'undefined' && STORIES[curStory];
  if(!st || st.add) return;
  const g = spGroupOf(curStory);
  const key = g ? g.key : (st.my ? '@me' : (st.name||'author'));
  const base = location.origin + location.pathname;
  const url = base + '?story=' + encodeURIComponent(key);
  const title = 'OKO · история' + (st.name ? ' · ' + st.name : '');
  const text = (st.text || 'Смотри историю в OKO').slice(0, 160);
  spPause();
  const resume = ()=> setTimeout(()=>spResume(), 250);
  if(navigator.share){
    navigator.share({title, text, url}).then(resume, resume);
  } else {
    try{
      navigator.clipboard.writeText(url).then(()=>{
        if(typeof toast === 'function') toast('Ссылка скопирована');
      }, ()=>{
        if(typeof toast === 'function') toast('Не удалось скопировать');
      });
    }catch(e){ if(typeof toast === 'function') toast('Ссылка: ' + url); }
    resume();
  }
}

/* ---------- звук: тумблер для видео-сторис ---------- */
function spApplyMuteToVideos(){
  const card = document.getElementById('svCard');
  if(!card) return;
  card.querySelectorAll('video').forEach(v=>{
    v.muted = !!SP.mute;
    if(!SP.mute){ try{ v.play().catch(()=>{}); }catch(e){} }
  });
}
function spMuteToggle(ev){
  if(ev){ ev.stopPropagation(); ev.preventDefault(); }
  SP.mute = !SP.mute; spSave();
  spApplyMuteToVideos();
  const btn = document.querySelector('.sp-mute-btn');
  if(btn){
    btn.title = SP.mute ? 'Включить звук' : 'Выключить звук';
    btn.innerHTML = `<svg class="i"><use href="#i-${SP.mute ? 'sp-mute' : 'sp-vol'}"/></svg>`;
  }
  if(typeof toast === 'function') toast(SP.mute ? 'Звук выключен' : 'Звук включён');
}

/* ---------- опросы / викторины: HTML + голос ---------- */
function spPollHtml(st, isQuiz){
  const src = isQuiz ? st.sp.quiz : st.sp.poll;
  const total = (src.votes||[]).reduce((a,b)=>a+(b||0), 0) || 0;
  const picked = (typeof st.spPollPick === 'number') ? st.spPollPick : -1;
  const voted = picked >= 0;
  const opts = (src.opts||[]).map((o, i)=>{
    const pc = total > 0 ? Math.round(((src.votes||[])[i] || 0) * 100 / total) : 0;
    let cls = '';
    if(voted){
      if(isQuiz){
        if(i === src.correct) cls = ' correct';
        else if(i === picked) cls = ' wrong';
      } else if(i === picked) cls = ' picked';
    }
    const barW = voted ? Math.max(0.02, ((src.votes||[])[i] || 0) / Math.max(total,1)) : 0;
    return `<button class="sp-poll-opt${cls}" style="--w:${barW}" onclick="spVote(${i},${isQuiz?1:0},event)">
      <span class="sp-poll-bar" style="transform:scaleX(${barW})"></span>
      <span class="sp-poll-lb"><span class="sp-poll-mark"><svg class="i"><use href="#i-check"/></svg></span>${esc(o)}</span>
      ${voted ? `<span class="sp-poll-pc">${pc}%</span>` : ''}
    </button>`;
  }).join('');
  return `<div class="sp-poll${isQuiz?' quiz':''}${voted?' voted':''}">
    <div class="sp-poll-q">${esc(src.q||(isQuiz?'Викторина':'Опрос'))}</div>
    <div class="sp-poll-opts">${opts}</div>
  </div>`;
}
function spVote(idx, isQuiz, ev){
  if(ev){ ev.stopPropagation(); ev.preventDefault(); }
  const st = typeof STORIES !== 'undefined' && STORIES[curStory];
  if(!st || !st.sp) return;
  const src = isQuiz ? st.sp.quiz : st.sp.poll;
  if(!src) return;
  if(typeof st.spPollPick === 'number') return;   /* уже голосовал в этой сессии */
  src.votes = src.votes || (src.opts||[]).map(()=>0);
  src.votes[idx] = (src.votes[idx] || 0) + 1;
  st.spPollPick = idx;
  try{ navigator.vibrate && navigator.vibrate(12); }catch(e){}
  const card = document.getElementById('svCard');
  const old = card && card.querySelector('.sp-poll');
  if(old){ old.outerHTML = spPollHtml(st, !!isQuiz); }
  spPause();                                         /* даём прочесть результат */
  setTimeout(()=>spResume(), 2200);
  const correct = isQuiz && idx === src.correct;
  if(typeof toast === 'function') toast(isQuiz ? (correct ? 'Верно' : 'Не в этот раз') : 'Голос учтён');
}

/* ---------- панель быстрых реакций (свайп-вверх) ---------- */
let spQRTimer = null;
function spOpenQuickReact(){
  const st = typeof STORIES !== 'undefined' && STORIES[curStory];
  if(!st || st.add || st.my) return;   /* свои сторис — нет смысла реагировать */
  const p = document.getElementById('spQuickReact');
  if(!p) return;
  spPause();
  p.classList.add('open');
  clearTimeout(spQRTimer);
  spQRTimer = setTimeout(spCloseQuickReact, 3200);
  if(typeof nvPush === 'function') nvPush('view:sp-qr', spCloseQuickReact);
}
function spCloseQuickReact(){
  const p = document.getElementById('spQuickReact');
  if(!p || !p.classList.contains('open')) return;
  p.classList.remove('open');
  clearTimeout(spQRTimer);
  if(typeof nvPop === 'function') nvPop('view:sp-qr');
  const v = document.getElementById('storyViewer');
  if(v && v.classList.contains('open')) spResume();
}
function spQuickReact(ic, ev){
  if(ev) ev.stopPropagation();
  const btn = ev && ev.currentTarget;
  const st = typeof STORIES !== 'undefined' && STORIES[curStory];
  if(st && !st.my){
    st.spReacted = ic;
    document.querySelectorAll('#spFoot .sp-react').forEach(b=>{
      b.classList.toggle('on', b.title === 'Реакция' && b.querySelector(`use[href="#i-${ic}"]`));
    });
  }
  if(btn) spFlyBurst(btn, ic, 6);
  if(typeof toast === 'function') toast('Реакция отправлена');
  setTimeout(spCloseQuickReact, 320);
}

/* ---------- шторка «кто посмотрел» (сторис на паузе, поверх вьювера) ---------- */
function spOpenViewers(ev){
  if(ev) ev.stopPropagation();
  const st = typeof STORIES !== 'undefined' && STORIES[curStory];
  const wrap = document.getElementById('spVwWrap');
  if(!st || !st.my || !wrap) return;
  spEnsureViews(st, false);
  document.getElementById('spVwCount').textContent = st.spViewers.length;
  document.getElementById('spVwList').innerHTML = st.spViewers.length ? st.spViewers.map((pi, j)=>{
    const p = SP_POOL[pi];
    const re = st.spVwReacts && st.spVwReacts[pi];
    return `<div class="sp-vw-item" style="animation-delay:${Math.min(j*45, 400)}ms">
      <div class="ava">${esc(p[0])}</div>
      <div><b>${esc(p[1])}${typeof vBadge==='function' ? vBadge(p[1]) : ''}</b><small>${SP_TIMES[Math.min(j, SP_TIMES.length-1)]}</small></div>
      ${re ? `<span class="sp-vw-re" title="Реакция">${I(re)}</span>` : ''}
    </div>`;
  }).join('') : '<div class="sp-vw-empty">Пока никто не посмотрел</div>';
  wrap.classList.add('open');
  spPause();
  if(typeof nvPush === 'function') nvPush('view:sp-viewers', spCloseViewers);
}
function spCloseViewers(){
  const wrap = document.getElementById('spVwWrap');
  if(!wrap || !wrap.classList.contains('open')) return;
  wrap.classList.remove('open');
  if(typeof nvPop === 'function') nvPop('view:sp-viewers');
  const v = document.getElementById('storyViewer');
  if(v && v.classList.contains('open')) spResume();
}

/* ---------- редактор текст-сторис ---------- */
function spOpenEditor(){
  const ed = document.getElementById('spEditor'); if(!ed) return;
  const txt = document.getElementById('spEdText');
  if(txt) txt.innerText = '';
  spEdApply(); spEdSync();
  ed.classList.add('open');
  setTimeout(()=>{ try{ txt && txt.focus(); }catch(e){} }, 330);
  if(typeof nvPush === 'function') nvPush('view:sp-editor', spCloseEditor);
}
function spCloseEditor(){
  const ed = document.getElementById('spEditor');
  if(!ed || !ed.classList.contains('open')) return;
  ed.classList.remove('open');
  if(typeof nvPop === 'function') nvPop('view:sp-editor');
}
function spEdApply(){
  const card = document.getElementById('spEdCard'); if(!card) return;
  const cfg = SP_BGS[SP.ed.bg] || SP_BGS[1];
  card.style.background = cfg.bg;
  card.style.color = cfg.fg;
  card.className = 'sp-ed-card sp-sz-' + SP.ed.size + ' sp-al-' + SP.ed.align + (SP.ed.sticker ? ' sp-stk' : '');
  const stk = document.getElementById('spEdSticker');
  if(stk){
    stk.className = 'sp-ed-sticker sp-pos-' + (SP.ed.pos || 'br');
    stk.innerHTML = `<svg class="i"><use href="#i-${SP.ed.stk||'logo'}"/></svg>`;
  }
  /* свотчи фонов (одноразовая отрисовка) */
  const sw = document.getElementById('spEdSwatches');
  if(sw && !sw.children.length)
    sw.innerHTML = SP_BGS.map((b, i)=>`<button class="sp-sw" style="background:${b.bg}" onclick="spEdBg(${i})" title="Фон ${i+1}"></button>`).join('');
  if(sw) Array.from(sw.children).forEach((b, i)=>b.classList.toggle('on', i === SP.ed.bg));
  /* выбор стикера (одноразовая отрисовка) */
  const sk = document.getElementById('spEdStickers');
  if(sk && !sk.children.length)
    sk.innerHTML = SP_STK.map(id=>`<button class="sp-stk-pick" data-v="${id}" onclick="spPickSticker('${id}')" title="Стикер"><svg class="i"><use href="#i-${id}"/></svg></button>`).join('');
  if(sk) Array.from(sk.children).forEach(b=>b.classList.toggle('on', SP.ed.sticker && b.dataset.v === SP.ed.stk));
  document.querySelectorAll('#spEdSizes button').forEach(b=>b.classList.toggle('on', b.dataset.v === SP.ed.size));
  document.querySelectorAll('#spEdAligns button').forEach(b=>b.classList.toggle('on', b.dataset.v === SP.ed.align));
  const tb = document.getElementById('spEdStkBtn');
  if(tb) tb.classList.toggle('on', !!SP.ed.sticker);
}
function spEdBg(i){ SP.ed.bg = i; spSave(); spEdApply(); }
function spEdSize(v){ SP.ed.size = v; spSave(); spEdApply(); }
function spEdAlign(v){ SP.ed.align = v; spSave(); spEdApply(); }
function spToggleSticker(){ SP.ed.sticker = !SP.ed.sticker; spSave(); spEdApply(); }
function spPickSticker(id){ SP.ed.stk = id; SP.ed.sticker = true; spSave(); spEdApply(); }
function spCycleSticker(ev){
  if(ev) ev.stopPropagation();
  const order = ['br','bl','tl','tr'];
  SP.ed.pos = order[(order.indexOf(SP.ed.pos) + 1) % order.length] || 'br';
  spSave(); spEdApply();
}
/* совместимость с pickStoryPhoto ядра: он берёт текст из #stStoryText */
function spEdSync(){
  const src = document.getElementById('spEdText');
  const dst = document.getElementById('stStoryText');
  if(src && dst) dst.value = (src.innerText || '').trim();
}
function spPickPhoto(){
  spEdSync();
  const inp = document.getElementById('storyPhotoInput');
  if(inp) inp.click(); else if(typeof toast === 'function') toast('Фото недоступно');
}
function spPublish(){
  const el = document.getElementById('spEdText');
  const txt = (el ? el.innerText : '').replace(/\s+/g,' ').trim();
  if(!txt){ if(typeof toast === 'function') toast('Напиши текст истории'); return; }
  if(typeof addStory !== 'function') return;
  const cfg = SP_BGS[SP.ed.bg] || SP_BGS[1];
  addStory({text:txt, bg:cfg.bg,
    sp:{size:SP.ed.size, align:SP.ed.align, sticker:!!SP.ed.sticker, pos:SP.ed.pos, stk:SP.ed.stk, fg:cfg.fg}});
}

/* ---------- персист своих текст-сторис ---------- */
const SP_STORE = 'oko-sp-mystories';
function spPersist(){
  try{
    if(typeof STORIES === 'undefined') return;
    const mine = STORIES.filter(s=> s.my && !s.spRestored && (!s.src || String(s.src).startsWith('data:')))
      .map(s=>({text:s.text||'', bg:s.bg||'', src:(s.src&&String(s.src).startsWith('data:'))?s.src:'', sp:s.sp||null, ts:s.spTs||Date.now()}))
      .slice(0, 12);
    localStorage.setItem(SP_STORE, JSON.stringify(mine));
  }catch(e){}
}
function spRestore(){
  try{
    const saved = JSON.parse(localStorage.getItem(SP_STORE) || '[]');
    if(!Array.isArray(saved) || !saved.length || typeof STORIES === 'undefined') return;
    const nm = (typeof PROFILE !== 'undefined' && PROFILE.name) ? PROFILE.name : 'Ты';
    /* восстанавливаем в исходном порядке (новые — ближе к началу) */
    saved.slice().reverse().forEach(d=>{
      STORIES.splice(1, 0, {my:true, spRestored:true, spTs:d.ts, name:nm, ava:nm[0],
        text:d.text, bg:d.bg||undefined, src:d.src||undefined, sp:d.sp||undefined});
    });
  }catch(e){}
}

/* ---------- chain-патчи ядра ---------- */
/* вау-открытие: карточка вылетает с масштабом, фон подсвечивается (только на открытии) */
if(typeof openStory === 'function'){
  const _spPrevOpenStory = openStory;
  openStory = function(i){
    _spPrevOpenStory(i);   /* ядро: curStory=i → showStory() → .open; поверх — spDecorate из showStory-патча */
    const v = document.getElementById('storyViewer');
    if(v && v.classList.contains('open') && !matchMedia('(prefers-reduced-motion: reduce)').matches){
      v.classList.remove('sp-open-anim'); void v.offsetWidth; v.classList.add('sp-open-anim');
      setTimeout(()=>v.classList.remove('sp-open-anim'), 560);
    }
  };
}
if(typeof openStoryCreate === 'function'){
  const _spPrevOpenStoryCreate = openStoryCreate;
  openStoryCreate = function(){
    if(document.getElementById('spEditor')) spOpenEditor();
    else _spPrevOpenStoryCreate(); /* фолбэк — прежний sheet */
  };
}
if(typeof createTextStory === 'function'){
  const _spPrevCreateTextStory = createTextStory;
  createTextStory = function(){
    const ed = document.getElementById('spEditor');
    if(ed && ed.classList.contains('open')) spPublish();
    else _spPrevCreateTextStory();
  };
}
if(typeof addStory === 'function'){
  const _spPrevAddStory = addStory;
  addStory = function(data){
    _spPrevAddStory(data);   /* splice + closeSheet + renderStories (сгруппированный) + toast */
    try{ if(STORIES[1] && STORIES[1].my) STORIES[1].spTs = Date.now(); }catch(e){}
    spPersist();
    spCloseEditor();
  };
}
if(typeof showStory === 'function'){
  const _spPrevShowStory = showStory; /* поверх патча demo-content (градиент-фон) */
  showStory = function(){
    _spPrevShowStory();
    try{
      /* видео-сторис: если st.video задан — подменяем картинку на <video> с сохранением состояния звука */
      const st = typeof STORIES !== 'undefined' && STORIES[curStory];
      const card = document.getElementById('svCard');
      if(st && st.video && card){
        const img = card.querySelector('img.sv-photo');
        if(img){
          const vid = document.createElement('video');
          vid.className = 'sv-video sv-photo';
          vid.src = st.video;
          vid.playsInline = true; vid.autoplay = true; vid.loop = true;
          vid.muted = !!SP.mute; vid.poster = st.src || '';
          img.replaceWith(vid);
          card.classList.add('video');
          try{ vid.play().catch(()=>{}); }catch(e){}
        }
      }
      spDecorate();
    }catch(e){ console.warn('stories-plus decorate:', e); }
  };
}
if(typeof closeStory === 'function'){
  const _spPrevCloseStory = closeStory;
  closeStory = function(){
    spStopTimer();
    _spPrevCloseStory();
    spLastIdx = -1; spReplyLock = false;
    const wrap = document.getElementById('spVwWrap');
    if(wrap && wrap.classList.contains('open')){
      wrap.classList.remove('open');
      if(typeof nvPop === 'function') nvPop('view:sp-viewers');
    }
    const qr = document.getElementById('spQuickReact');
    if(qr && qr.classList.contains('open')){
      qr.classList.remove('open');
      clearTimeout(spQRTimer);
      if(typeof nvPop === 'function') nvPop('view:sp-qr');
    }
  };
}

/* ---------- демо: вторые истории у авторов (виден мультисторис-режим) ---------- */
function spSeed(){
  /* демо-сидер отключён 09.08 (правка Даниэля: ноль демо-данных) */
  return (arguments.length && Array.isArray(arguments[0])) ? [] : undefined;

  if(typeof STORIES === 'undefined' || STORIES.some(s=>s.spSeed)) return;
  const put = (name, story)=>{
    const i = STORIES.findIndex(s=>!s.add && s.name === name);
    if(i >= 0) STORIES.splice(i + 1, 0, story);
  };
  put('OKO', {spSeed:true, avaIcon:'logo', name:'OKO',
    text:'Сторис-редактор уже здесь: градиенты бренда, размер текста и стикеры. Жми плюс в ленте.',
    bg:'linear-gradient(160deg,#050505 0%,#101010 45%,#2c5000 100%)',
    sp:{size:'m', align:'center', sticker:true, pos:'br', stk:'logo', fg:'#fff'}});
  put('Марк Волков', {spSeed:true, ava:'МВ', name:'Марк Волков',
    text:'Вторая часть кейса кофейни: до и после цветокора. Исходник против финала — разница в один вечер работы.',
    bg:'linear-gradient(150deg,#151f08 0%,#070707 55%,#0c1f14 100%)'});
  /* сторис-опрос от OKO (нативный формат Instagram / Telegram) */
  put('OKO', {spSeed:true, avaIcon:'logo', name:'OKO',
    text:'Что важнее в вертикальном ролике?',
    bg:'linear-gradient(155deg,#0a1a10 0%,#050505 60%,#08240e 100%)',
    sp:{size:'m', align:'center', fg:'#fff',
      poll:{q:'Что важнее в Reels?', opts:['Идея и хук','Монтаж и темп'], votes:[142, 108]}}});
  /* сторис-викторина от Марка (правильный ответ подсвечивается) */
  put('Марк Волков', {spSeed:true, ava:'МВ', name:'Марк Волков',
    text:'Мини-квиз про монтаж',
    bg:'linear-gradient(160deg,#131f04 0%,#060606 55%,#0e1a03 100%)',
    sp:{size:'m', align:'center', fg:'#fff',
      quiz:{q:'Оптимальная длина хука?', opts:['0.5 сек','1.5 сек','3 сек'], correct:1, votes:[38, 194, 76]}}});
}

/* авто-открытие сторис по параметру ?story=<ключ> */
function spOpenFromURL(){
  try{
    const p = new URLSearchParams(location.search);
    const key = p.get('story');
    if(!key) return;
    /* убираем параметр, чтобы обновление страницы не заходило в цикл */
    try{
      const u = new URL(location.href); u.searchParams.delete('story');
      history.replaceState({}, '', u.pathname + (u.search ? u.search : '') + u.hash);
    }catch(e){}
    setTimeout(()=>{
      const g = spGroups().find(x=>!x.add && x.key === key);
      if(g) spOpenGroup(encodeURIComponent(key));
    }, 400);
  }catch(e){}
}

/* ---------- самоинициализация ---------- */
(function spInit(){
  try{
    spSeed();
    spRestore();
    if(typeof renderStories === 'function' && document.getElementById('storiesRow')) renderStories();
    spEdApply(); /* свотчи и состояние контролов готовы до первого открытия */
    spOpenFromURL(); /* если пришли по shared-ссылке — открыть нужного автора */
  }catch(e){ console.warn('stories-plus init:', e); }
})();

/* ===== module: settings-plus ===== */
/* ===== SETTINGS-PLUS (st2-): «Настройки» как полноценный раздел =====
   Строка в профиле (шестерёнка) -> fullscreen #st2View (nvPush):
   Аккаунты / Аккаунт / Уведомления / Приватность / Безопасность /
   Оформление / Данные / Сессии / Опасная зона.
   Всё персистится в localStorage 'oko-settings2'. */

/* ---------- состояние + персист ---------- */
const ST2_NOTIF_DEF = () => ({snd:true, vibro:true, badge:true, prev:true});
const ST2_CH = [
  {k:'msg',      title:'Сообщения',      sub:'Чаты, реплаи, упоминания',       ico:'chat'},
  {k:'likes',    title:'Лайки и реакции',sub:'Отклик на посты и сторис',       ico:'heart'},
  {k:'comments', title:'Комментарии',    sub:'Ответы под твоими постами',      ico:'comment'},
  {k:'partners', title:'Партнёрка',      sub:'Начисления и выплаты',           ico:'briefcase'},
  {k:'games',    title:'Игры',           sub:'Ставки, выигрыши, лимиты',       ico:'st2-controller'},
  {k:'academy',  title:'Академия',       sub:'Новые уроки и сертификаты',      ico:'star'},
  {k:'market',   title:'Биржа',          sub:'Заказы, отклики, диспуты',       ico:'briefcase'},
  {k:'marketing',title:'Новости OKO',    sub:'Обновления и предложения',       ico:'megaphone'},
];
const ST2 = {
  nick: null,                       /* override ника (синк с PROFILE) */
  email: 'okoteam.top@gmail.com',
  phone: '+7 999 123-45-67',
  tg:    null,                      /* привязка Telegram: '@nick' */
  notif: {
    msg:true, feed:true, market:true, academy:true, marketing:false,
    likes:true, comments:true, partners:true, games:false,
    quiet:{on:false, from:'23:00', to:'08:00'},
    preview:true, sound:true, vibro:true,
    perCh:{},                       /* per-channel {msg:{snd,vibro,badge,prev}, ...} */
  },
  /* приватность: write (all|following|nobody), phone/photo/status (all|contacts|nobody), online/read — тумблеры */
  priv:  {write:'all', online:true, read:true, phone:'contacts', photo:'all', status:'all',
          calls:'contacts', posts:'all', stories:'all'},
  sec:   {twofa:false, passcode:false, pin:null, secret:null, autolock:'5m'},
  theme: null,                      /* 'dark'|'light'|'system' */
  themeAuto: {on:false, dark:'22:00', light:'07:00'},
  data:  {
    cacheLimit: 500,                /* МБ, 100..2000 */
    autodl: {
      wifi:   {photos:true,  videos:true,  files:true},
      mobile: {photos:true,  videos:false, files:false},
    },
    docsPolicy: 'ask',              /* 'always' | 'ask' | 'never' */
  },
  a11y:  {reduceMotion:false, highContrast:false, bigTaps:false, reduceOpacity:false, fontScale:100 /* 85..135 */},
  loc:   {dateFmt:'auto', hour24:true, weekStart:'auto' /* auto|mon|sun */},
  accounts: [],                     /* [{id,name,nick,tier,role,bio,status,avatar,cover}] */
  activeAcc: null,                  /* id активного аккаунта */
  killed: [],                       /* id завершённых мок-сессий */
  blocked: [],                      /* [имя] — чёрный список (реальный персист) */
  lastClear: 0,                     /* ts последней очистки кэша */
  delAt: 0,                         /* ts запланированного удаления аккаунта */
};
(function st2Load(){
  try{
    const s = JSON.parse(localStorage.getItem('oko-settings2') || 'null');
    if(s){
      if(s.notif){
        Object.assign(ST2.notif, s.notif);
        if(s.notif.perCh) ST2.notif.perCh = s.notif.perCh;
        if(s.notif.quiet) Object.assign(ST2.notif.quiet, s.notif.quiet);
      }
      if(s.priv)  Object.assign(ST2.priv,  s.priv);
      if(s.sec)   Object.assign(ST2.sec,   s.sec);
      if(s.data){
        Object.assign(ST2.data, s.data);
        if(s.data.autodl){
          if(s.data.autodl.wifi)   Object.assign(ST2.data.autodl.wifi,   s.data.autodl.wifi);
          if(s.data.autodl.mobile) Object.assign(ST2.data.autodl.mobile, s.data.autodl.mobile);
        }
      }
      if(s.a11y)      Object.assign(ST2.a11y,      s.a11y);
      if(s.loc)       Object.assign(ST2.loc,       s.loc);
      if(s.themeAuto) Object.assign(ST2.themeAuto, s.themeAuto);
      ['nick','email','phone','tg','lastClear','theme','activeAcc','delAt'].forEach(k=>{ if(s[k] !== undefined) ST2[k] = s[k]; });
      if(Array.isArray(s.killed))   ST2.killed   = s.killed;
      if(Array.isArray(s.accounts)) ST2.accounts = s.accounts;
      if(Array.isArray(s.blocked))  ST2.blocked  = s.blocked;
    }
  }catch(e){}
  /* нормализуем перечёт-каналы (в т.ч. на первом запуске без localStorage) */
  ST2_CH.forEach(c => { if(!ST2.notif.perCh[c.k]) ST2.notif.perCh[c.k] = ST2_NOTIF_DEF(); });
})();
function st2Save(){ try{ localStorage.setItem('oko-settings2', JSON.stringify(ST2)); }catch(e){} }

/* ---------- синк с ядром (SETTINGS старых sheet-ов + PROFILE) ---------- */
function st2PushCore(){
  if(typeof SETTINGS === 'undefined') return;
  SETTINGS.notif.msg     = ST2.notif.msg;
  SETTINGS.notif.news    = ST2.notif.marketing;
  SETTINGS.privacy.online = ST2.priv.online;
  SETTINGS.privacy.read   = ST2.priv.read;
  SETTINGS.privacy.dm     = ST2.priv.write !== 'all';
}
function st2PullCore(){
  if(typeof SETTINGS === 'undefined') return;
  ST2.notif.msg       = SETTINGS.notif.msg;
  ST2.notif.marketing = SETTINGS.notif.news;
  ST2.priv.online     = SETTINGS.privacy.online;
  ST2.priv.read       = SETTINGS.privacy.read;
  if(SETTINGS.privacy.dm && ST2.priv.write === 'all') ST2.priv.write = 'following';
  else if(!SETTINGS.privacy.dm) ST2.priv.write = 'all';
  st2Save();
}

/* ---------- мок-сессии ---------- */
function st2Device(){
  const ua = navigator.userAgent || '';
  const os = /iPhone/.test(ua) ? 'iPhone' : /Android/.test(ua) ? 'Android'
           : /Mac/.test(ua) ? 'macOS' : /Windows/.test(ua) ? 'Windows' : 'Linux';
  const br = /Telegram/i.test(ua) ? 'Telegram' : /Firefox/.test(ua) ? 'Firefox'
           : /Edg\//.test(ua) ? 'Edge' : /Chrome/.test(ua) ? 'Chrome' : /Safari/.test(ua) ? 'Safari' : 'браузер';
  return os + ' · ' + br;
}
const ST2_SES = []  /* демо-данные удалены 09.08: чужие устройства и сессии — источник только API */;
function st2Alive(){ return ST2_SES.filter(s => s.cur || ST2.killed.indexOf(s.id) < 0); }

/* ---------- размер кэша (мок: растёт со временем + реальный localStorage) ---------- */
function st2CacheSize(){
  let ls = 0;
  try{
    for(let i = 0; i < localStorage.length; i++){
      const k = localStorage.key(i);
      ls += k.length + (localStorage.getItem(k) || '').length;
    }
  }catch(e){}
  const since = ST2.lastClear || (Date.now() - 67 * 36e5); /* «установлено ~3 дня назад» */
  const hrs = Math.max(0, (Date.now() - since) / 36e5);
  return Math.min(217, hrs * 2.3) + ls / 1048576 * 8;
}

/* ---------- аккаунты: утилиты ---------- */
function st2AccInit(name){
  return String(name || 'U').trim().split(/\s+/).slice(0, 2).map(w => (w[0]||'').toUpperCase()).join('') || 'U';
}
function st2Hue(s){ let h = 0; s = String(s); for(let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % 360; return h; }
function st2ActiveAcc(){ return ST2.accounts.find(a => a.id === ST2.activeAcc) || null; }
function st2SyncActiveFromProfile(){
  const a = st2ActiveAcc();
  if(a && typeof PROFILE !== 'undefined'){
    a.name = PROFILE.name; a.nick = PROFILE.nick; a.tier = PROFILE.tier; a.role = PROFILE.role; a.bio = PROFILE.bio;
    a.status = PROFILE.status || null;
    a.avatar = PROFILE.avatar || null;
    a.cover  = PROFILE.cover  || null;
  }
}
/* применить профиль аккаунта на ядро PROFILE (при переключении/инициализации) */
function st2ApplyAccToProfile(a){
  if(!a || typeof PROFILE === 'undefined') return;
  PROFILE.name = a.name; PROFILE.nick = a.nick; PROFILE.tier = a.tier; PROFILE.role = a.role;
  if(a.bio    !== undefined) PROFILE.bio    = a.bio;
  PROFILE.status = a.status || null;
  PROFILE.avatar = a.avatar || null;
  PROFILE.cover  = a.cover  || null;
}

/* ---------- рендер вьюхи ---------- */
function st2Nick(){ return (typeof PROFILE !== 'undefined' && PROFILE.nick) ? PROFILE.nick : (ST2.nick || 'ktodaniel'); }
function st2Sw(on){ return `<span class="switch ${on ? 'on' : ''}"><i></i></span>`; }

/* сегмент-переключатель N вариантов (data-vis-группа) */
function st2Seg(group, key, opts){
  const cur = ST2[group][key];
  return `<span class="st2-seg st2-seg3" data-seg="${group}.${key}">` +
    opts.map(o => `<button data-v="${o[0]}" class="${cur === o[0] ? 'on' : ''}" onclick="st2SetSeg('${group}','${key}','${o[0]}')">${esc(o[1])}</button>`).join('') +
    `</span>`;
}
function st2SetSeg(group, key, val){
  ST2[group][key] = val; st2Save(); st2PushCore();
  const seg = document.querySelector(`.st2-seg[data-seg="${group}.${key}"]`);
  if(seg) seg.querySelectorAll('button').forEach(b => b.classList.toggle('on', b.getAttribute('data-v') === val));
  const M = {
    'priv.write':    {all:'Писать могут все', following:'Писать могут только подписки', nobody:'Никто не может писать первым'},
    'priv.phone':    {all:'Телефон виден всем', contacts:'Телефон виден контактам', nobody:'Телефон скрыт'},
    'priv.photo':    {all:'Фото видно всем', contacts:'Фото видно контактам', nobody:'Фото профиля скрыто'},
    'priv.status':   {all:'Статус виден всем', contacts:'Статус виден контактам', nobody:'Статус скрыт'},
  };
  const m = M[group + '.' + key]; if(m && m[val]) toast(m[val]);
}

function st2AccHtml(a){
  const active = a.id === ST2.activeAcc;
  const removable = !active && a.role !== 'owner';
  return `<div class="st2-acc ${active ? 'st2-acc-on' : ''}" data-acc="${a.id}">
    <span class="st2-acc-av${a.avatar ? ' has' : ''}" style="--h:${st2Hue(a.nick || a.name)}${a.avatar ? ';background-image:url(' + a.avatar + ')' : ''}">${a.avatar ? '' : esc(st2AccInit(a.name))}</span>
    <div class="st2-acc-b">
      <b>${esc(a.name)}${vBadge ? vBadge(a.name) : ''}</b>
      <small>@${esc(a.nick)} · ${esc(a.tier || 'FREE')}</small>
    </div>
    ${active ? `<span class="chip st2-chip st2-chip-on">${I('check')} активен</span>`
             : `<button class="st2-acc-sw" onclick="st2SwitchAccount('${a.id}')">Войти</button>`}
    ${removable ? `<button class="st2-acc-rm" onclick="st2RemoveAccount('${a.id}')" aria-label="Убрать аккаунт">${I('trash')}</button>` : ''}
  </div>`;
}

function st2SesHtml(s){
  return `<div class="st2-ses" data-st2ses="${s.id}">
    <span class="st2-ses-ico">${I(s.mob ? 'phone' : 'globe')}</span>
    <div class="st2-ses-b">
      <b>${s.cur ? '<span class="st2-dot"></span>' : ''}${esc(s.dev)}</b>
      <small>${esc(s.app)}${s.geo ? ' · ' + esc(s.geo) : ''}</small>
    </div>
    ${s.cur ? '<span class="chip st2-chip">это устройство</span>'
            : `<button class="st2-kill" onclick="st2Kill('${s.id}')">Завершить</button>`}
  </div>`;
}

/* ================= NAV-STACK: панели ================= */
/* каждый вход — {title, render(ctx)->html, after?()} */
const ST2_PANELS = {};
let ST2_STACK = [];

function st2Push(id, ctx){
  const p = ST2_PANELS[id];
  if(!p) return;
  const ent = {id, ctx: ctx || {}, title: (typeof p.title === 'function' ? p.title(ctx || {}) : p.title)};
  ST2_STACK.push(ent);
  st2RenderStack('push');
}
function st2Back(){
  if(ST2_STACK.length > 1){
    ST2_STACK.pop();
    st2RenderStack('pop');
    return;
  }
  st2Close();
}
function st2CurPanel(){ return ST2_STACK[ST2_STACK.length - 1]; }

/* рендер стека: замена содержимого + анимация slide */
function st2RenderStack(dir){
  const body = document.getElementById('st2Body');
  if(!body) return;
  const top = st2CurPanel();
  if(!top) return;
  const p = ST2_PANELS[top.id];
  const html = `<div class="st2-panel-inner">${p.render(top.ctx)}</div>`;

  const title = document.getElementById('st2Title');
  if(title) title.textContent = top.title;
  const bIco = document.getElementById('st2BackIco');
  if(bIco) bIco.style.transform = ST2_STACK.length > 1 ? 'translateX(-2px)' : '';

  const prev = body.firstElementChild;
  const panel = document.createElement('div');
  panel.className = 'st2-panel ' + (dir === 'push' ? 'st2-in' : 'st2-out-left');
  panel.dataset.id = top.id;
  panel.innerHTML = html;
  body.appendChild(panel);

  requestAnimationFrame(()=>{
    panel.classList.remove('st2-in', 'st2-out-left');
    if(prev){
      prev.classList.remove('st2-panel');
      prev.classList.add('st2-panel', dir === 'push' ? 'st2-back-behind' : 'st2-out-right');
    }
  });
  setTimeout(()=>{ if(prev && prev.parentElement) prev.remove(); if(p.after) try{ p.after(top.ctx, panel); }catch(e){} }, 340);
}

/* совместимая обёртка: полный ре-рендер текущей панели без анимации */
function st2Render(){
  if(!ST2_STACK.length) ST2_STACK = [{id:'root', ctx:{}, title:'Настройки'}];
  const body = document.getElementById('st2Body');
  if(!body) return;
  const top = st2CurPanel();
  const p = ST2_PANELS[top.id];
  body.innerHTML = `<div class="st2-panel st2-active" data-id="${top.id}"><div class="st2-panel-inner">${p.render(top.ctx)}</div></div>`;
  const title = document.getElementById('st2Title');
  if(title) title.textContent = top.title;
  const bIco = document.getElementById('st2BackIco');
  if(bIco) bIco.style.transform = ST2_STACK.length > 1 ? 'translateX(-2px)' : '';
  if(p.after) try{ p.after(top.ctx, body.firstElementChild); }catch(e){}
}

/* ================= ROOT PANEL: группированный список секций ================= */
function st2NavRow(icon, title, sum, target, danger){
  const sumHtml = sum ? `<span class="st2-nav-sum${sum.on?' on':''}">${esc(sum.text||sum)}</span>` : '';
  return `<button class="prow" onclick="st2Push('${target}')">
    <span class="st2-nav-ico${danger?' st2-nav-danger':''}">${I(icon)}</span>
    <span>${esc(title)}</span>${sumHtml}<span class="chev">${I('chev')}</span></button>`;
}
function st2ChSummary(k){ return ST2.notif[k] ? 'вкл' : 'выкл'; }
function st2PrivSum(){
  const m = {all:'Все', contacts:'Контакты', following:'Подписки', nobody:'Никто'};
  return m[ST2.priv.write] || 'Все';
}
function st2ThemeSum(){
  const t = ST2.theme || 'dark';
  return t === 'system' ? 'Система' : (t === 'light' ? 'Светлая' : 'Тёмная');
}
function st2LangSum(){
  const l = (typeof LANG !== 'undefined') ? LANG : 'ru';
  return l === 'en' ? 'English' : (l === 'kk' ? 'Қазақша' : 'Русский');
}
function st2A11ySum(){
  const a = ST2.a11y;
  const flags = [a.reduceMotion&&'motion',a.highContrast&&'contrast',a.bigTaps&&'tap',a.reduceOpacity&&'opacity'].filter(Boolean).length;
  if(!flags && a.fontScale===100) return 'выкл';
  return flags ? flags + ' опц.' : (a.fontScale + '%');
}
function st2DataSum(){ return st2CacheSize().toFixed(1) + ' МБ'; }
function st2DelSum(){ return ST2.delAt ? {text:'через ' + st2DelDays() + ' д', on:false} : null; }

ST2_PANELS.root = {
  title: 'Настройки',
  render(){
    return `
    <label class="st2-search" id="st2Search">
      <span class="st2-search-ic">${I('st2-search')}</span>
      <input id="st2SearchInp" type="search" placeholder="Поиск по настройкам" autocomplete="off" enterkeyhint="search" oninput="st2Search(this.value)">
      <button type="button" class="st2-search-x" onclick="st2SearchClear()" aria-label="Очистить">${I('st2-x')}</button>
    </label>
    <div id="st2SearchResults"></div>
    <div class="st2-grouped" id="st2Groups">
      <div class="st2-grp">
        <div class="st2-grp-cap">${I('user')} Профиль</div>
        ${st2NavRow('user',      'Профиль',         {text:'@'+st2Nick()}, 'profile')}
        ${st2NavRow('users',     'Аккаунты',        (ST2.accounts.length + ' на устройстве'), 'accounts')}
      </div>
      <div class="st2-grp">
        <div class="st2-grp-cap">${I('st2-key')} Аккаунт</div>
        ${st2NavRow('st2-mail',  'Аккаунт и вход',   ST2.email, 'account')}
        ${st2NavRow('st2-shield','Безопасность',    (ST2.sec.twofa?'2FA · вкл':'2FA · выкл'), 'security')}
        ${st2NavRow('st2-devices','Активные сессии', st2Alive().length + ' устр.', 'sessions')}
      </div>
      <div class="st2-grp">
        <div class="st2-grp-cap">${I('lock')} Приватность и уведомления</div>
        ${st2NavRow('lock',      'Приватность',     st2PrivSum(), 'privacy')}
        ${st2NavRow('bell',      'Уведомления',     (ST2.notif.msg?'вкл':'выкл'), 'notif')}
        ${st2NavRow('st2-ban',   'Чёрный список',   (ST2.blocked.length||'пусто') + '', 'blocked')}
      </div>
      <div class="st2-grp">
        <div class="st2-grp-cap">${I('st2-db')} Данные и оформление</div>
        ${st2NavRow('st2-db',    'Данные и хранилище', st2DataSum(), 'data')}
        ${st2NavRow('sun',       'Оформление',      st2ThemeSum(), 'theme')}
        ${st2NavRow('st2-eye',   'Доступность',     st2A11ySum(), 'a11y')}
        ${st2NavRow('globe',     'Язык',            st2LangSum(), 'lang')}
      </div>
      <div class="st2-grp">
        <div class="st2-grp-cap">${I('st2-life')} Помощь и документы</div>
        ${st2NavRow('st2-life',  'Помощь и обратная связь', null, 'help')}
        ${st2NavRow('file',      'Юридические документы',   null, 'legal')}
        ${st2NavRow('logout',    'Выйти из аккаунта',       null, 'logoutAct')}
      </div>
      <div class="st2-grp">
        <div class="st2-grp-cap" style="color:var(--danger)">${I('trash')} Опасная зона</div>
        ${st2NavRow('trash',     'Экспорт и удаление', st2DelSum(), 'danger', true)}
      </div>
    </div>
    <div class="st2-search-empty" id="st2SearchEmpty">${I('st2-search')}<p>Ничего не найдено</p><span>Попробуйте другой запрос</span></div>
    <div class="st2-foot">OKO · настройки хранятся локально · сборка ${st2Ver()}</div>`;
  }
};

/* хук выхода — открывается как «панель», но сразу дёргает st2LogoutAccount и попадает назад */
ST2_PANELS.logoutAct = {
  title:'Выход',
  render(){ return `<div class="st2-panel-desc">Готовим выход…</div>`; },
  after(){ st2LogoutAccount(); setTimeout(()=>{ if(ST2_STACK.length>1){ ST2_STACK.pop(); st2Render(); } }, 30); }
};

/* ================= ПРОФИЛЬ ================= */
ST2_PANELS.profile = {
  title: 'Профиль',
  render(){
    const p = (typeof PROFILE !== 'undefined') ? PROFILE : {name:'—', bio:'', role:'', tier:''};
    return `
    <p class="st2-panel-desc">Как тебя видят другие пользователи OKO. Открытая часть профиля — имя, ник, био и обложка.</p>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('user')} Публичное</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2AvatarMenu()">${I('camera')} Фото профиля<span class="chev">${I('chev')}</span></button>
        <button class="prow" onclick="st2CoverMenu()">${I('st2-image')} Обложка<span class="chev">${I('chev')}</span></button>
        <button class="prow" onclick="st2OpenEdit()">${I('edit')} Имя, био и статус<span class="st2-val">${esc(p.name)}</span><span class="chev">${I('chev')}</span></button>
        <button class="prow" onclick="st2EditNick()">${I('st2-key')} Ник<span class="st2-val">@${esc(st2Nick())}</span><span class="chev">${I('chev')}</span></button>
        <button class="prow" onclick="st2CopyPublic()">${I('share')} Публичная ссылка<span class="st2-val">oko.app/@${esc(st2Nick())}</span><span class="chev">${I('chev')}</span></button>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('crown')} Тариф</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2GoPlans()">${I('crown')} Текущий тариф<span class="st2-val">${esc(p.tier || 'FREE')}</span><span class="chev">${I('chev')}</span></button>
      </div>
    </div>`;
  }
};

/* ================= АККАУНТЫ (список + добавить) ================= */
ST2_PANELS.accounts = {
  title: 'Аккаунты',
  render(){
    return `<p class="st2-panel-desc">На одном устройстве держи несколько аккаунтов OKO — переключайся между ними в один тап.</p>
    <div class="st2-grp" id="st2AccList">
      ${ST2.accounts.map(st2AccHtml).join('')}
      <button class="prow st2-add-acc" onclick="st2AddAccount()">${I('plus')} Добавить аккаунт<span class="chev">${I('chev')}</span></button>
      <button class="prow st2-logout-acc" onclick="st2LogoutAccount()">${I('logout')} Выйти из активного<span class="chev">${I('chev')}</span></button>
    </div>`;
  }
};

/* ================= АККАУНТ И ВХОД ================= */
ST2_PANELS.account = {
  title: 'Аккаунт и вход',
  render(){
    return `<p class="st2-panel-desc">Контакты для входа и привязанные сервисы. Всё, что нужно для восстановления доступа.</p>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-mail')} Контакты</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2EditEmail()">${I('st2-mail')} Почта<span class="st2-val">${esc(ST2.email)}</span><span class="chip st2-chip">${I('check')} подтверждена</span></button>
        <button class="prow" onclick="st2EditPhone()">${I('phone')} Телефон<span class="st2-val">${esc(ST2.phone)}</span><span class="chev">${I('chev')}</span></button>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-link')} Привязки</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2LinkTg()">${I('send')} Telegram<span class="st2-val">${esc(ST2.tg || 'не привязан')}</span><span class="chev">${I('chev')}</span></button>
        <button class="prow" onclick="st2LinkStub('Google')">${I('google')} Google<span class="st2-val">не привязан</span><span class="chev">${I('chev')}</span></button>
        <button class="prow" onclick="st2LinkStub('Apple')">${I('apple')} Apple ID<span class="st2-val">не привязан</span><span class="chev">${I('chev')}</span></button>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-shield')} Защита входа</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2Push('security')">${I('st2-shield')} Двухфакторная защита и пароль<span class="chev">${I('chev')}</span></button>
      </div>
    </div>`;
  }
};

/* ================= БЕЗОПАСНОСТЬ ================= */
ST2_PANELS.security = {
  title: 'Безопасность',
  render(){
    return `<div class="st2-grp">
      <button class="prow" onclick="st2TwoFA()">${I('st2-shield')} Двухфакторная защита
        <span class="chip st2-chip ${ST2.sec.twofa ? 'st2-chip-on' : ''}">${ST2.sec.twofa ? I('check') + ' включена' : 'выключена'}</span><span class="chev">${I('chev')}</span></button>
      <button class="prow" onclick="st2Passcode(this)">${I('lock')} Код-пароль при входе ${st2Sw(ST2.sec.passcode)}</button>
      ${ST2.sec.passcode ? `<div class="prow st2-prow-col" style="cursor:default">
        <span class="st2-prow-lbl">${I('st2-clock')} Автоблокировка</span>
        <span class="st2-seg st2-seg3" id="st2LockSeg">${[['now','Сразу'],['1m','1 мин'],['5m','5 мин'],['1h','1 час']].map(o =>
          `<button data-v="${o[0]}" class="${(ST2.sec.autolock || '5m') === o[0] ? 'on' : ''}" onclick="st2SetAutolock('${o[0]}')">${o[1]}</button>`).join('')}</span></div>` : ''}
      <button class="prow" onclick="st2ChangePass()">${I('st2-key')} Сменить пароль<span class="chev">${I('chev')}</span></button>
      <button class="prow" onclick="st2Push('sessions')">${I('st2-devices')} Активные сессии<span class="st2-val">${st2Alive().length}</span><span class="chev">${I('chev')}</span></button>
    </div>`;
  }
};

/* ================= СЕССИИ ================= */
ST2_PANELS.sessions = {
  title: 'Сессии',
  render(){
    const alive = st2Alive(), others = alive.filter(s => !s.cur);
    return `<p class="st2-panel-desc">Устройства, на которых ты сейчас вошёл в OKO. Заверши любую сессию, если она подозрительна.</p>
    <div class="st2-grp" id="st2SesList">
      ${alive.map(st2SesHtml).join('')}
      ${others.length ? `<button class="st2-killall" onclick="st2KillAll()">Завершить остальные (${others.length})</button>`
                      : '<div class="st2-empty">Других активных сессий нет</div>'}
    </div>`;
  }
};

/* ================= ПРИВАТНОСТЬ ================= */
ST2_PANELS.privacy = {
  title: 'Приватность',
  render(){
    const VIS = [['all','Все'],['contacts','Контакты'],['nobody','Никто']];
    const WRITE = [['all','Все'],['following','Подписки'],['nobody','Никто']];
    return `<p class="st2-panel-desc">Точечно контролируй, кто и как может тебя видеть, слышать и добавлять в чаты.</p>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('eye')} Онлайн и прочтение</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2Tgl('priv','online',this)">${I('eye')} Показывать «в сети» ${st2Sw(ST2.priv.online)}</button>
        <button class="prow" onclick="st2Tgl('priv','read',this)">${I('check2')} Отчёты о прочтении ${st2Sw(ST2.priv.read)}</button>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('send')} Сообщения и звонки</div>
      <div class="st2-grp">
        <div class="prow st2-prow-col" style="cursor:default">
          <span class="st2-prow-lbl">${I('send')} Кто может писать первым</span>
          ${st2Seg('priv','write',WRITE)}
        </div>
        <div class="prow st2-prow-col" style="cursor:default">
          <span class="st2-prow-lbl">${I('phone')} Кто может звонить</span>
          ${st2Seg('priv','calls',VIS)}
        </div>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('user')} Что видно в профиле</div>
      <div class="st2-grp">
        <div class="prow st2-prow-col" style="cursor:default">
          <span class="st2-prow-lbl">${I('phone')} Кто видит телефон</span>
          ${st2Seg('priv','phone',VIS)}
        </div>
        <div class="prow st2-prow-col" style="cursor:default">
          <span class="st2-prow-lbl">${I('photo')} Кто видит фото профиля</span>
          ${st2Seg('priv','photo',VIS)}
        </div>
        <div class="prow st2-prow-col" style="cursor:default">
          <span class="st2-prow-lbl">${I('star')} Кто видит статус</span>
          ${st2Seg('priv','status',VIS)}
        </div>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('feed')} Посты и истории</div>
      <div class="st2-grp">
        <div class="prow st2-prow-col" style="cursor:default">
          <span class="st2-prow-lbl">${I('feed')} Кто видит новые посты</span>
          ${st2Seg('priv','posts',VIS)}
        </div>
        <div class="prow st2-prow-col" style="cursor:default">
          <span class="st2-prow-lbl">${I('camera')} Кто видит истории</span>
          ${st2Seg('priv','stories',VIS)}
        </div>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-ban')} Блокировки</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2Push('blocked')">${I('st2-ban')} Чёрный список
          <span class="st2-val" id="st2BlockedVal">${ST2.blocked.length ? ST2.blocked.length + (ST2.blocked.length===1?' человек':(ST2.blocked.length<5?' человека':' человек')) : 'пусто'}</span><span class="chev">${I('chev')}</span></button>
      </div>
    </div>`;
  }
};

/* ================= УВЕДОМЛЕНИЯ ================= */
ST2_PANELS.notif = {
  title: 'Уведомления',
  render(){
    const q = ST2.notif.quiet;
    return `<p class="st2-panel-desc">Точно настрой, что отвлекает: звук, вибро, превью и по каким каналам приходят пуши.</p>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('bell')} Общие</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2Tgl('notif','sound',this)">${I('st2-speaker')} Звук ${st2Sw(ST2.notif.sound)}</button>
        <button class="prow" onclick="st2Tgl('notif','vibro',this)">${I('st2-vibrate')} Вибрация ${st2Sw(ST2.notif.vibro)}</button>
        <button class="prow" onclick="st2Tgl('notif','preview',this)">${I('eye')} Превью текста в шторке ${st2Sw(ST2.notif.preview)}</button>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-tune')} Каналы</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2Push('notifChannels')">${I('st2-tune')} Настройки по каналам<span class="st2-val">${ST2_CH.length} шт.</span><span class="chev">${I('chev')}</span></button>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('moon')} Тихие часы</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2QuietTgl(this)">${I('moon')} Не беспокоить${st2QuietChip()}${st2Sw(q.on)}</button>
        <div class="st2-quiet${q.on ? ' open' : ''}" id="st2Quiet"><div class="st2-quiet-in">
          <div class="st2-quiet-times">
            <label class="st2-time"><span>с</span><input type="time" value="${esc(q.from)}" onchange="st2QuietTime('from',this.value)"></label>
            <span class="st2-quiet-arrow">${I('chev')}</span>
            <label class="st2-time"><span>до</span><input type="time" value="${esc(q.to)}" onchange="st2QuietTime('to',this.value)"></label>
          </div>
          <p class="st2-quiet-note">${I('moon')} В эти часы уведомления приходят без звука и вибрации. Расписание повторяется каждый день автоматически.</p>
        </div></div>
      </div>
    </div>`;
  }
};

/* ================= КАНАЛЫ УВЕДОМЛЕНИЙ ================= */
ST2_PANELS.notifChannels = {
  title: 'Каналы уведомлений',
  render(){
    return `<p class="st2-panel-desc">Для каждого канала отдельно: включить/выключить, звук, вибро, бейдж и превью текста.</p>
    <div class="st2-grp">
      ${ST2_CH.map(c => {
        const on = !!ST2.notif[c.k];
        const cnt = st2ChCount(c.k);
        return `<div class="st2-notch" onclick="st2Push('notifChannel',{k:'${c.k}'})">
          <span class="st2-notch-ico">${I(c.ico)}</span>
          <div class="st2-notch-b"><b>${esc(c.title)}</b><small>${esc(c.sub)}${on&&cnt?(' · '+cnt+' опций'):''}</small></div>
          <span class="st2-notch-state ${on?'on':''}">${on?'вкл':'выкл'}</span>
          <span class="chev">${I('chev')}</span>
        </div>`;
      }).join('')}
    </div>`;
  }
};

function st2ChCount(k){
  const s = ST2.notif.perCh[k] || {};
  return Object.values(s).filter(Boolean).length;
}
function st2FindCh(k){ return ST2_CH.find(c => c.k === k); }

ST2_PANELS.notifChannel = {
  title(ctx){ const c = st2FindCh(ctx.k); return c ? c.title : 'Канал'; },
  render(ctx){
    const c = st2FindCh(ctx.k); if(!c) return '';
    const st = ST2.notif.perCh[c.k];
    const globalOn = !!ST2.notif[c.k];
    return `<p class="st2-panel-desc">${esc(c.sub)}. Изменения применяются сразу и хранятся на устройстве.</p>
    <div class="st2-panel-sec">
      <div class="st2-grp">
        <button class="prow" onclick="st2Tgl('notif','${c.k}',this); st2RefreshHeaderState()">${I('bell')} Уведомления канала ${st2Sw(globalOn)}</button>
      </div>
    </div>
    <div class="st2-panel-sec" style="opacity:${globalOn?1:.5};pointer-events:${globalOn?'auto':'none'}">
      <div class="st2-panel-h">${I('st2-tune')} Как оповещать</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2TglCh('${c.k}','snd',this)">${I('st2-speaker')} Звук ${st2Sw(st.snd)}</button>
        <button class="prow" onclick="st2TglCh('${c.k}','vibro',this)">${I('st2-vibrate')} Вибрация ${st2Sw(st.vibro)}</button>
        <button class="prow" onclick="st2TglCh('${c.k}','badge',this)">${I('st2-badge')} Бейдж на иконке ${st2Sw(st.badge)}</button>
        <button class="prow" onclick="st2TglCh('${c.k}','prev',this)">${I('eye')} Превью текста ${st2Sw(st.prev)}</button>
      </div>
    </div>`;
  }
};
function st2TglCh(k, key, btn){
  const s = ST2.notif.perCh[k];
  s[key] = !s[key]; st2Save();
  const sw = btn && btn.querySelector('.switch');
  if(sw) sw.classList.toggle('on', s[key]);
}
function st2RefreshHeaderState(){ /* обновит счётчик при возврате */ }

/* ================= ДАННЫЕ И ХРАНИЛИЩЕ ================= */
ST2_PANELS.data = {
  title: 'Данные и хранилище',
  render(){
    const total = st2CacheSize();
    const parts = st2StorageParts(total);
    return `<p class="st2-panel-desc">Что занимает место, как загружать медиа и когда чистить кэш.</p>
    <div class="st2-storage">
      ${st2PieSvg(parts)}
      <div class="st2-pie-legend">
        ${parts.map(p => `<div class="st2-pie-row"><span class="st2-pie-dot" style="background:${p.color}"></span>${esc(p.label)}<b>${p.v.toFixed(1)} МБ</b></div>`).join('')}
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-grp">
        <button class="prow" id="st2CacheRow" onclick="st2ClearCache()">${I('trash')} Очистить кэш<span class="st2-val" id="st2CacheVal">${total.toFixed(1)} МБ</span><span class="chev">${I('chev')}</span></button>
        <div class="prow st2-prow-col" style="cursor:default">
          <div class="st2-range" style="width:100%">
            <div class="st2-range-h">${I('st2-db')} Максимум кэша<span class="st2-range-v" id="st2CLV">${ST2.data.cacheLimit} МБ</span></div>
            <input type="range" min="100" max="2000" step="50" value="${ST2.data.cacheLimit}" oninput="st2SetCacheLim(this.value)" id="st2CLR" style="--st2-fill:${((ST2.data.cacheLimit-100)/(2000-100)*100)}%">
            <div class="st2-range-ticks"><span>100</span><span>500</span><span>1 ГБ</span><span>2 ГБ</span></div>
          </div>
        </div>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-wifi')} Автозагрузка медиа</div>
      <div class="st2-grp">
        <div class="st2-dl-grid">
          <div></div><div class="st2-dl-head">Wi-Fi</div><div class="st2-dl-head">Моб.</div>
          ${[['photos','Фото','photo'],['videos','Видео','play'],['files','Файлы','file']].map(([k,l,ic])=>`
            <div class="st2-dl-row-h">${I(ic)} ${l}</div>
            <span class="switch ${ST2.data.autodl.wifi[k]?'on':''}" onclick="st2DlTgl('wifi','${k}',this)"><i></i></span>
            <span class="switch ${ST2.data.autodl.mobile[k]?'on':''}" onclick="st2DlTgl('mobile','${k}',this)"><i></i></span>`).join('')}
        </div>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('file')} Загрузки файлов</div>
      <div class="st2-grp">
        ${[['always','Всегда','Скачивать документы сразу'],['ask','Спрашивать','Показывать кнопку скачивания'],['never','Никогда','Только по кнопке скачивания']].map(o =>
          `<button class="prow st2-radio ${ST2.data.docsPolicy===o[0]?'on':''}" onclick="st2SetDocsPolicy('${o[0]}')">
             <span class="st2-radio-mark"></span>
             <div class="st2-radio-b"><b>${o[1]}</b><small>${o[2]}</small></div>
           </button>`).join('')}
      </div>
    </div>`;
  }
};
function st2SetCacheLim(v){
  ST2.data.cacheLimit = +v; st2Save();
  const el = document.getElementById('st2CLV');
  const r  = document.getElementById('st2CLR');
  if(el) el.textContent = v + ' МБ';
  if(r)  r.style.setProperty('--st2-fill', ((v-100)/(2000-100)*100) + '%');
}
function st2DlTgl(net, k, el){
  ST2.data.autodl[net][k] = !ST2.data.autodl[net][k]; st2Save();
  if(el) el.classList.toggle('on', ST2.data.autodl[net][k]);
}
function st2SetDocsPolicy(p){ ST2.data.docsPolicy = p; st2Save(); st2Render(); }

/* «размер» разных категорий кэша — стабильный псевдо-разброс от общего */
function st2StorageParts(total){
  const seed = (ST2.lastClear || 42) % 7;
  const media = Math.max(0.5, total * (0.55 + seed*0.01));
  const docs  = Math.max(0.3, total * 0.18);
  const stick = Math.max(0.2, total * 0.09);
  const other = Math.max(0.1, total - media - docs - stick);
  return [
    {v:media, label:'Медиа (фото, видео)', color:'#9AFF00'},
    {v:docs,  label:'Документы',            color:'#5CD3F4'},
    {v:stick, label:'Стикеры и эмодзи',     color:'#F97AC9'},
    {v:other, label:'Прочее',               color:'#8A94A6'},
  ];
}
function st2PieSvg(parts){
  const total = parts.reduce((s,x)=>s+x.v,0) || 1;
  const R = 42, C = 2*Math.PI*R;
  let off = 0;
  const arcs = parts.map(p => {
    const frac = p.v/total, dash = frac*C;
    const seg = `<circle cx="50" cy="50" r="${R}" fill="none" stroke="${p.color}" stroke-width="12" stroke-dasharray="${dash.toFixed(2)} ${(C-dash).toFixed(2)}" stroke-dashoffset="${(-off).toFixed(2)}" transform="rotate(-90 50 50)" stroke-linecap="butt"/>`;
    off += dash;
    return seg;
  }).join('');
  return `<svg class="st2-pie st2-pie-anim" viewBox="0 0 100 100" aria-label="Распределение кэша">
    <circle class="st2-pie-bg" cx="50" cy="50" r="42" fill="none" stroke-width="12"/>
    ${arcs}
    <text class="st2-pie-cx" x="50" y="52" text-anchor="middle">${total.toFixed(0)}</text>
    <text class="st2-pie-cu" x="50" y="64" text-anchor="middle">МБ</text>
  </svg>`;
}

/* ================= ОФОРМЛЕНИЕ ================= */
ST2_PANELS.theme = {
  title: 'Оформление',
  render(){
    const themeMode = ST2.theme || (document.documentElement.dataset.theme === 'light' ? 'light' : 'dark');
    const ta = ST2.themeAuto;
    return `<p class="st2-panel-desc">Тема, авто-переключение по времени суток и настройки шрифта.</p>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('sun')} Тема</div>
      <div class="st2-grp">
        ${[['dark','Тёмная','Классическая ночная тема','moon'],['light','Светлая','Для яркого света и дня','sun'],['system','Системная','Как в настройках устройства','st2-devices']].map(o=>`
          <button class="prow st2-radio ${themeMode===o[0]?'on':''}" onclick="st2SetTheme('${o[0]}')">
            <span class="st2-radio-mark"></span>
            <div class="st2-radio-b"><b>${o[1]}</b><small>${o[2]}</small></div>
            <span class="st2-radio-tag">${I(o[3])}</span>
          </button>`).join('')}
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-clock')} Расписание темы</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2ThemeAutoTgl(this)">${I('st2-clock')} Автоматически по времени ${st2Sw(ta.on)}</button>
        <div class="st2-quiet${ta.on?' open':''}"><div class="st2-quiet-in">
          <div class="st2-quiet-times">
            <label class="st2-time"><span>тёмная</span><input type="time" value="${esc(ta.dark)}" onchange="st2ThemeAutoTime('dark',this.value)"></label>
            <span class="st2-quiet-arrow">${I('chev')}</span>
            <label class="st2-time"><span>светлая</span><input type="time" value="${esc(ta.light)}" onchange="st2ThemeAutoTime('light',this.value)"></label>
          </div>
          <p class="st2-quiet-note">${I('moon')} По умолчанию: 22:00 → тёмная, 07:00 → светлая. Тема переключится сразу и продолжит держать расписание.</p>
        </div></div>
      </div>
    </div>`;
  }
};
function st2ThemeAutoTgl(btn){
  ST2.themeAuto.on = !ST2.themeAuto.on; st2Save();
  const sw = btn && btn.querySelector('.switch');
  if(sw) sw.classList.toggle('on', ST2.themeAuto.on);
  st2ScheduleThemeAuto();
  st2Render();
  toast(ST2.themeAuto.on ? 'Тема автоматически переключается по времени' : 'Авто-расписание темы выключено');
}
function st2ThemeAutoTime(which,val){
  if(!/^\d\d:\d\d$/.test(val || '')) return;
  ST2.themeAuto[which] = val; st2Save();
  st2ScheduleThemeAuto();
}

/* планировщик авто-темы */
let st2AutoTimer = 0;
function st2ScheduleThemeAuto(){
  if(st2AutoTimer){ clearTimeout(st2AutoTimer); st2AutoTimer = 0; }
  if(!ST2.themeAuto.on) return;
  const now = new Date();
  const cur = now.getHours()*60 + now.getMinutes();
  const df = st2QuietMin(ST2.themeAuto.dark), lf = st2QuietMin(ST2.themeAuto.light);
  const inDark = df < lf ? (cur >= df && cur < lf) : (cur >= df || cur < lf);
  const want = inDark ? 'dark' : 'light';
  if(typeof applyTheme === 'function') applyTheme(want); else document.documentElement.dataset.theme = want;
  const next = st2NextThemeSwitch(cur, df, lf);
  st2AutoTimer = setTimeout(st2ScheduleThemeAuto, next*60*1000);
}
function st2NextThemeSwitch(cur, df, lf){
  const cands = [((df - cur)+1440)%1440 || 1440, ((lf - cur)+1440)%1440 || 1440];
  return Math.max(1, Math.min(...cands));
}

/* ================= ДОСТУПНОСТЬ ================= */
ST2_PANELS.a11y = {
  title: 'Доступность',
  render(){
    const a = ST2.a11y;
    return `<p class="st2-panel-desc">Сделай интерфейс удобнее: крупный шрифт, снижение анимаций, высокий контраст, большие кнопки.</p>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('star')} Размер</div>
      <div class="st2-grp">
        <div class="prow st2-prow-col" style="cursor:default">
          <div class="st2-range" style="width:100%">
            <div class="st2-range-h">${I('edit')} Размер шрифта<span class="st2-range-v" id="st2FSV">${a.fontScale}%</span></div>
            <input type="range" min="85" max="135" step="5" value="${a.fontScale}" oninput="st2SetFont(this.value)" id="st2FSR" style="--st2-fill:${((a.fontScale-85)/50*100)}%">
            <div class="st2-range-ticks"><span>Мельче</span><span>Обычно</span><span>Крупнее</span></div>
          </div>
        </div>
        <button class="prow" onclick="st2A11yTgl('bigTaps',this)">${I('st2-hand')} Большие тап-цели ${st2Sw(a.bigTaps)}</button>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('eye')} Восприятие</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2A11yTgl('reduceMotion',this)">${I('st2-motion')} Уменьшить анимации ${st2Sw(a.reduceMotion)}</button>
        <button class="prow" onclick="st2A11yTgl('highContrast',this)">${I('st2-contrast')} Высокий контраст ${st2Sw(a.highContrast)}</button>
        <button class="prow" onclick="st2A11yTgl('reduceOpacity',this)">${I('sun')} Снизить прозрачность ${st2Sw(a.reduceOpacity)}</button>
      </div>
    </div>`;
  }
};
function st2A11yTgl(k, btn){
  ST2.a11y[k] = !ST2.a11y[k]; st2Save();
  const sw = btn && btn.querySelector('.switch');
  if(sw) sw.classList.toggle('on', ST2.a11y[k]);
  st2ApplyA11y();
}
function st2SetFont(v){
  ST2.a11y.fontScale = +v; st2Save();
  const el = document.getElementById('st2FSV');
  const r  = document.getElementById('st2FSR');
  if(el) el.textContent = v + '%';
  if(r)  r.style.setProperty('--st2-fill', ((v-85)/50*100) + '%');
  st2ApplyA11y();
}
function st2ApplyA11y(){
  const a = ST2.a11y;
  const root = document.documentElement;
  root.classList.toggle('st2-a-motion',   a.reduceMotion);
  root.classList.toggle('st2-a-contrast', a.highContrast);
  root.classList.toggle('st2-a-opacity',  a.reduceOpacity);
  root.classList.toggle('st2-a-tap',      a.bigTaps);
  root.style.setProperty('--st2-font-scale', (a.fontScale/100).toFixed(2));
  document.body.style.fontSize = (a.fontScale === 100 ? '' : (a.fontScale/100 * 15) + 'px');
}

/* ================= ЯЗЫК ================= */
ST2_PANELS.lang = {
  title: 'Язык',
  render(){
    const lang = (typeof LANG !== 'undefined') ? LANG : 'ru';
    const L = [
      ['ru','Русский','РУ','Основной язык OKO'],
      ['en','English','EN','English interface'],
      ['kk','Қазақша','ҚАЗ','Қазақша интерфейс'],
    ];
    return `<p class="st2-panel-desc">Язык интерфейса. Контент от пользователей остаётся на языке оригинала.</p>
    <div class="st2-panel-sec">
      <div class="st2-grp">
        ${L.map(o=>`
          <button class="prow st2-radio ${lang===o[0]?'on':''}" onclick="st2SetLang('${o[0]}')">
            <span class="st2-radio-mark"></span>
            <div class="st2-radio-b"><b>${o[1]}</b><small>${o[3]}</small></div>
            <span class="st2-radio-tag">${o[2]}</span>
          </button>`).join('')}
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-clock')} Формат</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2Tgl('loc','hour24',this)">${I('st2-clock')} 24-часовой формат времени ${st2Sw(ST2.loc.hour24)}</button>
        <div class="prow st2-prow-col" style="cursor:default">
          <span class="st2-prow-lbl">${I('st2-clock')} Начало недели</span>
          <span class="st2-seg st2-seg3">
            ${[['auto','Авто'],['mon','Пн'],['sun','Вс']].map(o=>`<button class="${ST2.loc.weekStart===o[0]?'on':''}" onclick="st2SetLoc('weekStart','${o[0]}')">${o[1]}</button>`).join('')}
          </span>
        </div>
      </div>
    </div>`;
  }
};
function st2SetLoc(k,v){ ST2.loc[k] = v; st2Save(); st2Render(); }

/* ================= ПОМОЩЬ И ОБРАТНАЯ СВЯЗЬ ================= */
ST2_PANELS.help = {
  title: 'Помощь и обратная связь',
  render(){
    const faq = [
      ['Как переключиться между аккаунтами?','Открой «Профиль → Аккаунты» и нажми «Войти» рядом с нужным аккаунтом. Аккаунт активен, если рядом стоит зелёная галочка. Переключение мгновенное — фото, ник и чаты меняются автоматически.'],
      ['Куда девался кэш?','Кэш — это оффлайн-копия сообщений, обложек и медиа. Он живёт локально и очищается кнопкой «Очистить кэш» в разделе <b>Данные и хранилище</b>. Свои сообщения при этом не удаляются.'],
      ['Как включить двухфакторную защиту?','Открой «Аккаунт и вход → Безопасность → Двухфакторная защита». Мы сгенерируем ключ, который добавляется в Google Authenticator или 1Password. При каждом входе нужен будет 6-значный код.'],
      ['Что такое «тихие часы»?','Расписание для входящих уведомлений. В указанный интервал пуши приходят без звука и вибрации. Иконка приложения при этом обновляется как обычно.'],
      ['Как удалить аккаунт?','«Опасная зона → Экспорт и удаление». После подтверждения аккаунт помечен на удаление и стирается через 14 дней. В любой момент можно отменить: достаточно войти в этот раздел ещё раз.'],
      ['Где скачать выгрузку данных?','«Опасная зона → Экспорт данных → JSON». Файл содержит профиль, чаты, платежи и настройки. Данные готовятся локально и не отправляются на сервер.'],
    ];
    return `<div class="st2-help-hero">
      <h4>${I('st2-life')} На связи 24/7</h4>
      <p>Ответим за 3–5 минут в чате поддержки OKO. Если это техническая ошибка — приложи скриншот и опиши шаги.</p>
      <button class="st2-help-btn" onclick="st2OpenSupport()">${I('chat')} Открыть чат поддержки</button>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-tune')} Быстрые действия</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2Bugreport()">${I('flag')} Сообщить о баге<span class="chev">${I('chev')}</span></button>
        <button class="prow" onclick="st2OpenSupport()">${I('chat')} Написать поддержке<span class="chev">${I('chev')}</span></button>
        <button class="prow" onclick="st2CopyDiag()">${I('copy')} Скопировать диагностику<span class="chev">${I('chev')}</span></button>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('st2-help-circle')} Частые вопросы</div>
      <div class="st2-faq">
        ${faq.map(f=>`<details><summary>${esc(f[0])}</summary><div class="st2-faq-body">${f[1]}</div></details>`).join('')}
      </div>
    </div>`;
  }
};
function st2OpenSupport(){
  if(typeof openChatByName === 'function'){
    try{ openChatByName('OKO Support'); return; }catch(e){}
  }
  toast('Пишем в поддержку — ответим в течение 5 минут');
}
function st2Bugreport(){
  showPopup({
    ico:'flag', title:'Сообщить о баге',
    body:`<p style="margin-bottom:10px">Опиши, что произошло — приложим версию, устройство и настройки автоматически.</p>
      <div class="st2-pin"><textarea id="st2Bug" placeholder="Что случилось?" style="width:100%;min-height:110px;background:var(--raised);border:1px solid var(--border);border-radius:var(--r-md);padding:12px;font:inherit;color:var(--text);resize:vertical"></textarea></div>`,
    actions:[{label:'Отмена', ghost:true}, {label:'Отправить', onclick(){
      const v = (document.getElementById('st2Bug')||{}).value || '';
      if(v.trim().length < 10){ toast('Слишком коротко — расскажи подробнее'); return; }
      toast('Багрепорт отправлен · отвечает служба поддержки OKO');
    }}],
  });
}
function st2CopyDiag(){
  const d = {
    ver: st2Ver(),
    ua: navigator.userAgent,
    lang: (typeof LANG !== 'undefined') ? LANG : 'ru',
    theme: document.documentElement.dataset.theme,
    online: navigator.onLine,
    cache: st2CacheSize().toFixed(1) + ' МБ',
    ts: new Date().toISOString(),
  };
  const txt = 'OKO · диагностика\n' + Object.entries(d).map(([k,v])=>k+': '+v).join('\n');
  try{
    navigator.clipboard.writeText(txt).then(()=>toast('Диагностика скопирована — пришли в поддержку'));
  }catch(e){ toast('Не удалось скопировать'); }
}

/* ================= ЮР. ДОКУМЕНТЫ ================= */
ST2_PANELS.legal = {
  title: 'Юридические документы',
  render(){
    return `<p class="st2-panel-desc">Официальные документы: подписывая аккаунт и покупая на OKO, ты соглашаешься с ними.</p>
    <div class="st2-grp">
      <button class="prow" onclick="st2OpenDoc('offer')">${I('file')} Публичная оферта<span class="chev">${I('chev')}</span></button>
      <button class="prow" onclick="st2OpenDoc('privacy')">${I('lock')} Политика конфиденциальности<span class="chev">${I('chev')}</span></button>
      <button class="prow" onclick="st2OpenDoc('refund')">${I('money')} Правила возврата и рефанда<span class="chev">${I('chev')}</span></button>
      <button class="prow" onclick="st2OpenDoc('licenses')">${I('star')} Лицензии и открытый код<span class="chev">${I('chev')}</span></button>
    </div>`;
  }
};
function st2OpenDoc(key){
  const map = {offer:'terms', privacy:'privacy', refund:'refund', licenses:'licenses'};
  const k = map[key] || key;
  if(typeof openLegal === 'function'){ try{ openLegal(k); return; }catch(e){} }
  toast('Документ откроется в разделе «Документы»');
}

/* ================= ОПАСНАЯ ЗОНА ================= */
function st2DelDays(){ if(!ST2.delAt) return 0; const left = Math.ceil((ST2.delAt - Date.now())/86400000); return Math.max(0, left); }
ST2_PANELS.danger = {
  title: 'Опасная зона',
  render(){
    const days = st2DelDays();
    return `<p class="st2-panel-desc">Экспорт данных и удаление аккаунта. Удаление действует с задержкой 14 дней — успеешь передумать.</p>
    <div class="st2-panel-sec">
      <div class="st2-panel-h">${I('file')} Экспорт</div>
      <div class="st2-grp">
        <button class="prow" onclick="st2ExportJson()">${I('file')} Скачать данные (JSON)<span class="st2-val">.json</span><span class="chev">${I('chev')}</span></button>
        <button class="prow" onclick="st2Download()">${I('file')} Читаемая выгрузка (TXT)<span class="st2-val">.txt</span><span class="chev">${I('chev')}</span></button>
      </div>
    </div>
    <div class="st2-panel-sec">
      <div class="st2-panel-h" style="color:var(--danger)">${I('trash')} Удаление</div>
      <div class="st2-grp st2-danger">
        <h4 style="padding:6px 8px 0">${I('trash')} Удалить аккаунт</h4>
        <p style="padding:0 8px 6px">Профиль, чаты, кошелёк, сертификаты Академии и вся история будут стёрты безвозвратно. Удаление начинается через 14 дней после подтверждения — за это время его можно отменить.</p>
        ${ST2.delAt ? `<div class="st2-del-status">
            <b>${I('st2-clock')} Аккаунт помечен на удаление</b>
            Автоудаление через ${days} дн. — до ${new Date(ST2.delAt).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'})}
            <button class="st2-del-cancel" onclick="st2CancelDel()">Отменить удаление</button>
          </div>` : `<button class="st2-del" onclick="st2DeleteAsk()" style="margin:10px 8px 6px;width:calc(100% - 16px)">Удалить аккаунт через 14 дней</button>`}
      </div>
    </div>`;
  }
};

/* ================= ЧЁРНЫЙ СПИСОК (панель) ================= */
ST2_PANELS.blocked = {
  title: 'Чёрный список',
  render(){ return `<div id="st2BlockedBody"></div>`; },
  after(){ st2RenderBlocked(); }
};

/* ---------- открыть / закрыть (navstack) ---------- */
function st2Open(){
  ST2_STACK = [{id:'root', ctx:{}, title:'Настройки'}];
  st2Render();
  const v = document.getElementById('st2View');
  if(v && !v.classList.contains('open')){
    v.classList.add('open');
    if(typeof nvPush === 'function') nvPush('view:st2', st2Close);
  }
}
function st2Close(){
  const v = document.getElementById('st2View');
  if(v) v.classList.remove('open');
  ST2_STACK = [];
  if(typeof nvPop === 'function') nvPop('view:st2');
}

/* ---------- версия сборки ---------- */
function st2Ver(){
  const m = document.querySelector('.head-build,.ver-chip,.build-chip');
  if(m) return (m.textContent||'').replace(/[^\d.v]/g,'') || '';
  const t = document.body.textContent.match(/сборка v([\d.]+)/); return t ? 'v'+t[1] : '';
}

/* ---------- профиль-панель: хуки ---------- */
function st2OpenEdit(){ if(typeof openEdit === 'function') openEdit(); }
function st2GoPlans(){
  st2Close();
  if(typeof scrollToPlans === 'function') scrollToPlans();
  else{
    const el = document.querySelector('#screen-profile .plan-cards,#screen-profile .section-h');
    if(el) el.scrollIntoView({behavior:'smooth', block:'start'});
  }
}
function st2CopyPublic(){
  const url = 'https://oko.app/@' + st2Nick();
  try{ navigator.clipboard.writeText(url).then(()=>toast('Ссылка скопирована: ' + url)); }
  catch(e){ toast('Публичная ссылка: ' + url); }
}
function st2LinkTg(err){
  if(ST2.tg){
    showPopup({ico:'send', title:'Отвязать Telegram', body:'Аккаунт <b>'+esc(ST2.tg)+'</b> будет отключён от OKO. Вход через Telegram перестанет работать.',
      actions:[{label:'Отмена', ghost:true},{label:'Отвязать', onclick(){ ST2.tg = null; st2Save(); st2Render(); toast('Telegram отвязан'); }}]});
    const ok = document.querySelector('#okoPopup [data-pa="1"]'); if(ok) ok.classList.add('st2-btn-danger');
    return;
  }
  st2Prompt({
    ico:'send', title:'Привязать Telegram', err: err,
    note:'Укажи @ник в Telegram — привяжем аккаунт после подтверждения через бота @okoappbot.',
    ph:'@nickname', saveLabel:'Привязать',
    save(v){
      v = v.replace(/^@/,'').trim();
      if(!/^[a-zA-Z0-9_]{4,32}$/.test(v)) return st2LinkTg('Ник Telegram: 4–32 латинских, цифры или _');
      ST2.tg = '@'+v; st2Save(); st2Render(); toast('Telegram привязан: @'+v);
    },
  });
}
function st2LinkStub(name){
  showPopup({ico:'plus', title:'Привязать '+name,
    body:'Привязка '+name+' появится после включения OAuth на боевом бэкенде — сейчас интерфейс в сборке.',
    actions:[{label:'Понятно'}]});
}

/* ---------- отмена запланированного удаления ---------- */
function st2CancelDel(){
  ST2.delAt = 0; st2Save(); st2Render();
  toast('Удаление аккаунта отменено');
}

/* ---------- тумблеры ---------- */
function st2Tgl(group, k, btn){
  ST2[group][k] = !ST2[group][k];
  st2Save(); st2PushCore();
  const sw = btn && btn.querySelector('.switch');
  if(sw) sw.classList.toggle('on', ST2[group][k]);
}

/* ---------- живой поиск: индекс всех настроек с прыжком в панель ---------- */
const ST2_SEARCH_INDEX = [
  /* profile */
  ['Профиль','profile','user','Личное · публичное'],
  ['Аватар · фото профиля','profile','camera','Личное'],
  ['Обложка','profile','st2-image','Личное'],
  ['Имя и био','profile','edit','Личное'],
  ['Ник · nickname · username','profile','st2-key','Личное'],
  ['Публичная ссылка · шеринг','profile','share','Личное'],
  ['Тариф · план · подписка','profile','crown','Личное'],
  /* accounts */
  ['Аккаунты · переключение','accounts','users','Профиль'],
  ['Добавить аккаунт','accounts','plus','Профиль'],
  /* account */
  ['Почта · email','account','st2-mail','Аккаунт и вход'],
  ['Телефон','account','phone','Аккаунт и вход'],
  ['Telegram · TG · привязка','account','send','Аккаунт и вход'],
  ['Google · привязка OAuth','account','google','Аккаунт и вход'],
  ['Apple ID · привязка','account','apple','Аккаунт и вход'],
  /* security */
  ['Двухфакторная защита · 2FA · TOTP','security','st2-shield','Безопасность'],
  ['Код-пароль при входе · PIN','security','lock','Безопасность'],
  ['Автоблокировка','security','st2-clock','Безопасность'],
  ['Сменить пароль','security','st2-key','Безопасность'],
  /* sessions */
  ['Активные сессии · устройства','sessions','st2-devices','Безопасность'],
  ['Завершить все сессии','sessions','trash','Безопасность'],
  /* privacy */
  ['Онлайн · последнее посещение','privacy','eye','Приватность'],
  ['Отчёты о прочтении','privacy','check2','Приватность'],
  ['Кто может писать первым','privacy','send','Приватность'],
  ['Кто может звонить','privacy','phone','Приватность'],
  ['Кто видит телефон','privacy','phone','Приватность'],
  ['Кто видит фото профиля','privacy','photo','Приватность'],
  ['Кто видит статус','privacy','star','Приватность'],
  ['Кто видит посты','privacy','feed','Приватность'],
  ['Кто видит истории · сторис','privacy','camera','Приватность'],
  ['Чёрный список · блокировки','blocked','st2-ban','Приватность'],
  /* notif */
  ['Уведомления · пуши','notif','bell','Уведомления'],
  ['Звук уведомлений','notif','st2-speaker','Уведомления'],
  ['Вибрация','notif','st2-vibrate','Уведомления'],
  ['Превью текста','notif','eye','Уведомления'],
  ['Тихие часы · не беспокоить · DND','notif','moon','Уведомления'],
  ['Каналы уведомлений','notifChannels','st2-tune','Уведомления'],
  ['Лайки и реакции','notifChannels','heart','Уведомления'],
  ['Комментарии','notifChannels','comment','Уведомления'],
  ['Партнёрка','notifChannels','briefcase','Уведомления'],
  ['Академия · уроки','notifChannels','star','Уведомления'],
  ['Игры · ставки','notifChannels','st2-controller','Уведомления'],
  /* data */
  ['Данные и хранилище · кэш','data','st2-db','Данные'],
  ['Очистить кэш','data','trash','Данные'],
  ['Лимит кэша','data','st2-db','Данные'],
  ['Автозагрузка медиа Wi-Fi','data','st2-wifi','Данные'],
  ['Автозагрузка на мобильном','data','st2-wifi','Данные'],
  ['Загрузка документов','data','file','Данные'],
  /* theme */
  ['Тема · тёмная · светлая','theme','sun','Оформление'],
  ['Расписание темы · авто · dark mode','theme','st2-clock','Оформление'],
  /* a11y */
  ['Доступность · a11y','a11y','st2-eye','Оформление'],
  ['Размер шрифта','a11y','edit','Оформление'],
  ['Большие тап-цели','a11y','st2-hand','Оформление'],
  ['Уменьшить анимации · motion-reduce','a11y','st2-motion','Оформление'],
  ['Высокий контраст','a11y','st2-contrast','Оформление'],
  ['Снизить прозрачность','a11y','sun','Оформление'],
  /* lang */
  ['Язык · language · русский english қазақша','lang','globe','Язык'],
  ['24-часовой формат времени','lang','st2-clock','Язык'],
  ['Начало недели','lang','st2-clock','Язык'],
  /* help */
  ['Помощь · FAQ · частые вопросы','help','st2-life','Помощь'],
  ['Чат поддержки OKO','help','chat','Помощь'],
  ['Сообщить о баге · багрепорт','help','flag','Помощь'],
  ['Диагностика','help','copy','Помощь'],
  /* legal */
  ['Оферта · публичная','legal','file','Документы'],
  ['Политика конфиденциальности','legal','lock','Документы'],
  ['Возврат · рефанд','legal','money','Документы'],
  ['Лицензии · open source','legal','star','Документы'],
  /* danger */
  ['Экспорт данных JSON','danger','file','Опасная зона'],
  ['Скачать выгрузку','danger','file','Опасная зона'],
  ['Удалить аккаунт · с задержкой 14 дней','danger','trash','Опасная зона'],
];
function st2Search(qv){
  const root = document.getElementById('st2Groups');
  const res  = document.getElementById('st2SearchResults');
  const lab  = document.getElementById('st2Search');
  const empty= document.getElementById('st2SearchEmpty');
  const raw = String(qv || '');
  if(lab) lab.classList.toggle('has', !!raw.trim());
  const q = raw.trim().toLowerCase();
  if(!q){
    if(root) root.style.display = '';
    if(res)  res.innerHTML = '';
    if(empty) empty.style.display = 'none';
    return;
  }
  if(root) root.style.display = 'none';
  const hits = ST2_SEARCH_INDEX.filter(r => (r[0] + ' ' + r[3]).toLowerCase().indexOf(q) > -1).slice(0, 24);
  if(!hits.length){
    if(res)  res.innerHTML = '';
    if(empty) empty.style.display = '';
    return;
  }
  if(empty) empty.style.display = 'none';
  if(!res) return;
  res.innerHTML = `<div class="st2-search-res">${
    hits.map(h => `<button class="prow" onclick="st2SearchGo('${h[1]}')">
      <span class="st2-nav-ico">${I(h[2])}</span>
      <span>${st2SearchHl(h[0], q)}</span>
      <span class="st2-search-res-tag">${esc(h[3])}</span>
      <span class="chev">${I('chev')}</span></button>`).join('')
  }</div>`;
}
function st2SearchHl(s, q){
  const idx = s.toLowerCase().indexOf(q);
  if(idx < 0) return esc(s);
  return esc(s.slice(0,idx)) + '<b style="color:var(--accent)">' + esc(s.slice(idx, idx+q.length)) + '</b>' + esc(s.slice(idx+q.length));
}
function st2SearchGo(id){
  st2SearchClear();
  st2Push(id);
}
function st2SearchClear(){
  const inp = document.getElementById('st2SearchInp');
  if(inp) inp.value = '';
  st2Search('');
  if(inp) try{ inp.focus(); }catch(e){}
}

/* ---------- тихие часы / не беспокоить (расписание беззвучных уведомлений) ---------- */
function st2QuietMin(t){ const m = /^(\d\d):(\d\d)$/.exec(t || ''); return m ? (+m[1] * 60 + +m[2]) : 0; }
function st2QuietActive(){
  const qq = ST2.notif.quiet;
  if(!qq || !qq.on) return false;
  const f = st2QuietMin(qq.from), t = st2QuietMin(qq.to);
  if(f === t) return false;
  const d = new Date(), now = d.getHours() * 60 + d.getMinutes();
  return f < t ? (now >= f && now < t) : (now >= f || now < t);
}
function st2QuietChip(){
  const qq = ST2.notif.quiet;
  if(!qq || !qq.on) return '';
  return st2QuietActive()
    ? `<span class="chip st2-chip st2-chip-on">${I('moon')} сейчас тихо</span>`
    : `<span class="chip st2-chip">${esc(qq.from)}–${esc(qq.to)}</span>`;
}
function st2QuietRefreshChip(){
  const prow = document.querySelector('.prow[onclick="st2QuietTgl(this)"]');
  if(!prow) return;
  const old = prow.querySelector('.chip');
  if(old) old.remove();
  const html = st2QuietChip();
  const sw = prow.querySelector('.switch');
  if(html && sw) sw.insertAdjacentHTML('beforebegin', html);
}
function st2QuietTgl(btn){
  const qq = ST2.notif.quiet;
  qq.on = !qq.on; st2Save();
  const sw = btn && btn.querySelector('.switch');
  if(sw) sw.classList.toggle('on', qq.on);
  const box = document.getElementById('st2Quiet');
  if(box) box.classList.toggle('open', qq.on);
  st2QuietRefreshChip();
  toast(qq.on ? 'Тихие часы: с ' + qq.from + ' до ' + qq.to : 'Тихие часы выключены');
}
function st2QuietTime(which, val){
  if(!/^\d\d:\d\d$/.test(val || '')) return;
  ST2.notif.quiet[which] = val; st2Save();
  st2QuietRefreshChip();
}

/* ---------- автоблокировка (запрос код-пароля после простоя) ---------- */
function st2SetAutolock(v){
  ST2.sec.autolock = v; st2Save();
  const seg = document.getElementById('st2LockSeg');
  if(seg) seg.querySelectorAll('button').forEach(b => b.classList.toggle('on', b.getAttribute('data-v') === v));
  const M = {now:'сразу', '1m':'через 1 минуту', '5m':'через 5 минут', '1h':'через час'};
  toast('Автоблокировка: ' + (M[v] || v));
}

/* ---------- Оформление: тема / язык ---------- */
function st2ApplyTheme(mode){
  let t = mode;
  if(mode === 'system'){
    t = (window.matchMedia && window.matchMedia('(prefers-color-scheme: light)').matches) ? 'light' : 'dark';
  }
  if(typeof applyTheme === 'function') applyTheme(t);
  else document.documentElement.dataset.theme = t;
}
function st2SetTheme(mode){
  ST2.theme = mode; st2Save();
  st2ApplyTheme(mode);
  const seg = document.getElementById('st2ThemeSeg');
  if(seg) seg.querySelectorAll('button').forEach(b => b.classList.toggle('on', b.getAttribute('data-v') === mode));
  toast(mode === 'system' ? 'Тема: как в системе' : (mode === 'light' ? 'Тема: светлая' : 'Тема: тёмная'));
}
function st2SetLang(l){
  if(typeof setLang === 'function') setLang(l);           /* сам покажет toast + прогонит хуки */
  else { try{ localStorage.setItem('oko-lang', l); }catch(e){} }
  const seg = document.getElementById('st2LangSeg');
  if(seg) seg.querySelectorAll('button').forEach(b => b.classList.toggle('on', b.getAttribute('data-v') === l));
}

/* ---------- попап с инпутом (значение читаем из замыкания: popup закрывается ДО onclick) ---------- */
function st2Attr(t){ return esc(t).replace(/"/g, '&quot;'); }
let _st2Inp = null;
let st2_pwOld = null, st2_pwNew = null;
function st2Prompt(o){ /* {ico,title,note,val,ph,saveLabel,danger,err,mode,save(v)} */
  showPopup({
    ico: o.ico, title: o.title,
    body: `${o.note ? `<p style="margin-bottom:12px">${o.note}</p>` : ''}
      <div class="st2-pin"><input id="st2PopInp" value="${st2Attr(o.val || '')}" placeholder="${st2Attr(o.ph || '')}" autocomplete="off"></div>
      ${o.err ? `<div class="st2-perr">${esc(o.err)}</div>` : ''}`,
    actions: [
      {label:'Отмена', ghost:true},
      {label: o.saveLabel || 'Сохранить', onclick: ()=> o.save(_st2Inp ? _st2Inp.value.trim() : '')},
    ],
  });
  _st2Inp = document.getElementById('st2PopInp');
  const ok = document.querySelector('#okoPopup [data-pa="1"]');
  if(o.danger && ok) ok.classList.add('st2-btn-danger');
  if(_st2Inp){
    if(o.mode === 'num'){ _st2Inp.setAttribute('inputmode', 'numeric'); _st2Inp.setAttribute('maxlength', String(o.max || 6)); }
    setTimeout(()=>{ try{ _st2Inp.focus(); _st2Inp.select(); }catch(e){} }, 60);
    _st2Inp.addEventListener('keydown', e => { if(e.key === 'Enter' && ok) ok.click(); });
  }
}

/* ---------- Аккаунт: ник (синк PROFILE + активный аккаунт), почта, телефон ---------- */
function st2EditNick(err){
  st2Prompt({
    ico:'edit', title:'Смена ника', err: err,
    note:'3–16 символов: латиница, цифры, подчёркивание. Ник обновится в профиле, ленте и чатах.',
    val: st2Nick(), ph:'nickname',
    save(v){
      v = v.replace(/^@/, '');
      if(!/^[a-z0-9_]{3,16}$/i.test(v)) return st2EditNick('Недопустимый ник — проверь формат');
      ST2.nick = v;
      if(typeof PROFILE !== 'undefined'){
        PROFILE.nick = v;
        if(typeof renderMyProfile === 'function') renderMyProfile();
      }
      st2SyncActiveFromProfile(); st2Save();
      st2Render(); toast('Ник обновлён: @' + v);
    },
  });
}
function st2EditEmail(err){
  st2Prompt({
    ico:'st2-mail', title:'Почта', err: err,
    note:'На новый адрес придёт письмо-подтверждение.',
    val: ST2.email, ph:'you@example.com',
    save(v){
      if(!/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(v)) return st2EditEmail('Похоже, в адресе опечатка');
      ST2.email = v; st2Save(); st2Render();
      toast('Письмо-подтверждение отправлено');
    },
  });
}
function st2EditPhone(err){
  st2Prompt({
    ico:'phone', title:'Телефон', err: err,
    note:'Телефон используется для входа и восстановления доступа.',
    val: ST2.phone, ph:'+7 900 000-00-00',
    save(v){
      if(!/^\+?[\d\s()-]{10,18}$/.test(v)) return st2EditPhone('Проверь номер — нужен формат +7 …');
      ST2.phone = v; st2Save(); st2Render();
      toast('Код подтверждения отправлен по SMS');
    },
  });
}

/* ---------- Аккаунты: переключение / добавление / удаление ---------- */
function st2SwitchAccount(id){
  if(id === ST2.activeAcc) return;
  const a = ST2.accounts.find(x => x.id === id);
  if(!a) return;
  st2SyncActiveFromProfile();          /* сохранить текущий стейт перед уходом */
  ST2.activeAcc = id; ST2.nick = a.nick;
  if(typeof PROFILE !== 'undefined'){
    st2ApplyAccToProfile(a);
    if(typeof renderMyProfile === 'function') renderMyProfile();
  }
  st2Save();
  const el = document.querySelector(`[data-acc="${id}"]`);
  if(el){ el.classList.add('st2-acc-flash'); setTimeout(()=> st2Render(), 220); }
  else st2Render();
  toast('Аккаунт: ' + a.name);
}
function st2AddAccount(err){
  st2Prompt({
    ico:'plus', title:'Добавить аккаунт', err: err,
    note:'Войдите в другой аккаунт OKO — введите его ник, данные подтянутся автоматически.',
    ph:'nickname', saveLabel:'Войти',
    save(v){
      v = v.replace(/^@/, '');
      if(!/^[a-z0-9_]{3,16}$/i.test(v)) return st2AddAccount('Недопустимый ник — проверь формат');
      if(ST2.accounts.some(a => (a.nick || '').toLowerCase() === v.toLowerCase()))
        return st2AddAccount('Этот аккаунт уже добавлен');
      const name = v.charAt(0).toUpperCase() + v.slice(1);
      const id = 'acc' + Date.now();
      ST2.accounts.push({id, name, nick:v, tier:'FREE', role:'user', bio:''});
      st2Save();
      st2SwitchAccount(id);
      toast('Вошли как @' + v);
    },
  });
}
function st2RemoveAccount(id){
  const a = ST2.accounts.find(x => x.id === id);
  if(!a || a.id === ST2.activeAcc || a.role === 'owner') return;
  showPopup({
    ico:'trash', title:'Убрать аккаунт?',
    body:`Аккаунт «${esc(a.name)}» (@${esc(a.nick)}) исчезнет из списка на этом устройстве. Сам аккаунт не удаляется — вы сможете войти снова.`,
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Убрать', onclick(){
        ST2.accounts = ST2.accounts.filter(x => x.id !== id);
        st2Save(); st2Render(); toast('Аккаунт убран');
      }},
    ],
  });
  const ok = document.querySelector('#okoPopup [data-pa="1"]');
  if(ok) ok.classList.add('st2-btn-danger');
}

/* ---------- Безопасность: 2FA (честная демо-заглушка), код-пароль, смена пароля ---------- */
function st2GenSecret(){
  const A = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ234567';
  let s = '';
  for(let i = 0; i < 16; i++){ if(i && i % 4 === 0) s += ' '; s += A[Math.floor(Math.random() * 32)]; }
  return s;
}
function st2TwoFA(){
  if(ST2.sec.twofa){
    showPopup({
      ico:'st2-shield', title:'Отключить 2FA?',
      body:'Вход снова будет защищён только паролем. Рекомендуем держать двухфакторную защиту включённой.',
      actions:[
        {label:'Отмена', ghost:true},
        {label:'Отключить', onclick(){ ST2.sec.twofa = false; ST2.sec.secret = null; st2Save(); st2Render(); toast('2FA отключена'); }},
      ],
    });
    const ok = document.querySelector('#okoPopup [data-pa="1"]');
    if(ok) ok.classList.add('st2-btn-danger');
    return;
  }
  const secret = st2GenSecret();
  ST2.sec.secret = secret;
  showPopup({
    ico:'st2-shield', title:'Двухфакторная защита',
    body:`<p style="margin-bottom:10px">Откройте приложение-аутентификатор (Google Authenticator, 1Password, OKO&nbsp;Key) и добавьте ключ вручную:</p>
      <div class="st2-2fa-key">${esc(secret)}</div>
      <div class="st2-note"><b>Честно:</b> в этой сборке код подтверждения не сверяется с сервером — рабочая проверка TOTP подключается на боевом бэкенде. Введите любые 6&nbsp;цифр, чтобы завершить демонстрацию.</div>`,
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Ввести код', onclick: ()=> st2TwoFACode()},
    ],
  });
}
function st2TwoFACode(err){
  st2Prompt({
    ico:'st2-shield', title:'Код из приложения', err: err, mode:'num',
    note:'Введите 6-значный код из аутентификатора.',
    ph:'000000', saveLabel:'Включить',
    save(v){
      if(!/^\d{6}$/.test(v)) return st2TwoFACode('Нужно ровно 6 цифр');
      ST2.sec.twofa = true; st2Save(); st2Render();
      toast('Двухфакторная защита включена');
    },
  });
}
function st2Passcode(){
  if(ST2.sec.passcode){
    showPopup({
      ico:'lock', title:'Отключить код-пароль?',
      body:'Приложение перестанет запрашивать код при входе.',
      actions:[
        {label:'Отмена', ghost:true},
        {label:'Отключить', onclick(){ ST2.sec.passcode = false; ST2.sec.pin = null; st2Save(); st2Render(); toast('Код-пароль отключён'); }},
      ],
    });
    const ok = document.querySelector('#okoPopup [data-pa="1"]');
    if(ok) ok.classList.add('st2-btn-danger');
    return;
  }
  st2PinSet1();
}
function st2PinSet1(err){
  st2Prompt({
    ico:'lock', title:'Код-пароль', err: err, mode:'num', max:4,
    note:'Придумайте 4-значный код — он будет запрашиваться при входе в приложение.',
    ph:'4 цифры', saveLabel:'Далее',
    save(v){ if(!/^\d{4}$/.test(v)) return st2PinSet1('Нужно ровно 4 цифры'); st2PinSet2(v); },
  });
}
function st2PinSet2(first, err){
  st2Prompt({
    ico:'lock', title:'Повторите код', err: err, mode:'num', max:4,
    note:'Введите код ещё раз для подтверждения.',
    ph:'4 цифры', saveLabel:'Включить',
    save(v){
      if(v !== first) return st2PinSet2(first, 'Коды не совпадают');
      ST2.sec.passcode = true; ST2.sec.pin = first; st2Save(); st2Render();
      toast('Код-пароль включён');
    },
  });
}
function st2ChangePass(err){
  showPopup({
    ico:'st2-key', title:'Сменить пароль',
    body:`<div class="st2-pin"><input id="st2PwOld" type="password" placeholder="Текущий пароль" autocomplete="off"></div>
      <div class="st2-pin" style="margin-top:10px"><input id="st2PwNew" type="password" placeholder="Новый пароль: минимум 8 символов" autocomplete="off"></div>
      ${err ? `<div class="st2-perr">${esc(err)}</div>` : ''}`,
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Сохранить', onclick: st2ChangePassSave},
    ],
  });
  st2_pwOld = document.getElementById('st2PwOld');
  st2_pwNew = document.getElementById('st2PwNew');
  const ok = document.querySelector('#okoPopup [data-pa="1"]');
  if(st2_pwOld) setTimeout(()=>{ try{ st2_pwOld.focus(); }catch(e){} }, 60);
  if(st2_pwNew && ok) st2_pwNew.addEventListener('keydown', e => { if(e.key === 'Enter') ok.click(); });
}
function st2ChangePassSave(){
  const oldv = st2_pwOld ? st2_pwOld.value : '';
  const nv   = st2_pwNew ? st2_pwNew.value : '';
  if(!oldv)          return st2ChangePass('Введите текущий пароль');
  if(nv.length < 8)  return st2ChangePass('Новый пароль слишком короткий, минимум 8 символов');
  if(nv === oldv)    return st2ChangePass('Новый пароль совпадает со старым');
  toast('Пароль изменён');
}

/* ---------- Данные: очистка кэша с подсчётом + выгрузка .txt ---------- */
let st2Clearing = false;
function st2ClearCache(){
  if(st2Clearing) return;
  st2Clearing = true;
  const row = document.getElementById('st2CacheRow');
  const el = document.getElementById('st2CacheVal');
  if(row) row.classList.add('st2-clearing');
  const from = st2CacheSize(), t0 = performance.now(), dur = 1500;
  (function tick(now){
    const p = Math.min(1, ((now || performance.now()) - t0) / dur);
    if(el) el.textContent = (from * (1 - p * p)).toFixed(1) + ' МБ';
    if(p < 1) return requestAnimationFrame(tick);
    ST2.lastClear = Date.now(); st2Save();
    st2Clearing = false;
    if(row) row.classList.remove('st2-clearing');
    if(el) el.textContent = st2CacheSize().toFixed(1) + ' МБ';
    toast('Кэш очищен · освобождено ' + from.toFixed(1) + ' МБ');
  })();
}
function st2Download(){
  const p = (typeof PROFILE !== 'undefined') ? PROFILE : {name:'—', nick: st2Nick(), tier:'—', bio:''};
  const themeMode = ST2.theme || (document.documentElement.dataset.theme === 'light' ? 'light' : 'dark');
  const L = ['OKO — выгрузка данных аккаунта', 'Дата: ' + new Date().toLocaleString('ru-RU'), ''];
  L.push('=== ПРОФИЛЬ ===', 'Имя: ' + p.name, 'Ник: @' + p.nick, 'Тариф: ' + p.tier,
         'Почта: ' + ST2.email, 'Телефон: ' + ST2.phone, 'О себе: ' + (p.bio || '—'), '');
  L.push('=== НАСТРОЙКИ ===',
         'Уведомления: ' + JSON.stringify(ST2.notif),
         'Приватность: ' + JSON.stringify(ST2.priv),
         'Тема: ' + themeMode + ' · Язык: ' + ((typeof LANG !== 'undefined') ? LANG : 'ru'),
         'Двухфакторная защита: ' + (ST2.sec.twofa ? 'вкл' : 'выкл') + ' · Код-пароль: ' + (ST2.sec.passcode ? 'вкл' : 'выкл'), '');
  if(ST2.accounts.length){
    L.push('=== АККАУНТЫ НА УСТРОЙСТВЕ ===');
    ST2.accounts.forEach(a => L.push('  ' + a.name + ' · @' + a.nick + ' · ' + (a.tier || 'FREE') + (a.id === ST2.activeAcc ? ' (активен)' : '')));
    L.push('');
  }
  if(typeof WALLET !== 'undefined'){
    L.push('=== КОШЕЛЁК ===', 'Счёт: ' + WALLET.acc, 'Баланс: ' + WALLET.balance + ' ₽', 'В холде: ' + WALLET.hold + ' ₽');
    (WALLET.ledger || []).slice(0, 30).forEach(e =>
      L.push('  ' + (e.d || '') + '  ' + (e.sum > 0 ? '+' : '') + e.sum + ' ₽  ' + (e.why || '')));
    L.push('');
  }
  L.push('=== СЕССИИ ===');
  st2Alive().forEach(s => L.push('  ' + s.dev + ' · ' + s.app + (s.cur ? ' (текущая)' : ' · ' + s.geo)));
  L.push('', 'Файл сформирован приложением OKO. Данные хранятся локально на устройстве.');
  try{
    const url = URL.createObjectURL(new Blob([L.join('\n')], {type:'text/plain;charset=utf-8'}));
    const a = document.createElement('a');
    a.href = url; a.download = 'oko-data-' + st2Nick() + '.txt';
    document.body.appendChild(a); a.click(); a.remove();
    setTimeout(()=> URL.revokeObjectURL(url), 4000);
    toast('Файл с данными скачан');
  }catch(e){ toast('Не удалось сформировать файл'); }
}

/* ---------- Сессии ---------- */
function st2Kill(id){
  const s = ST2_SES.find(x => x.id === id);
  if(!s) return;
  showPopup({
    ico:'st2-devices', title:'Завершить сессию?',
    body:`Устройство «${esc(s.dev)}» будет отключено от аккаунта. Для входа понадобится пароль.`,
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Завершить', onclick(){
        const el = document.querySelector(`[data-st2ses="${id}"]`);
        if(el) el.classList.add('st2-out');
        ST2.killed.push(id); st2Save();
        setTimeout(()=>{ st2Render(); toast('Сессия завершена'); }, 300);
      }},
    ],
  });
  const ok = document.querySelector('#okoPopup [data-pa="1"]');
  if(ok) ok.classList.add('st2-btn-danger');
}
function st2KillAll(){
  showPopup({
    ico:'st2-devices', title:'Завершить остальные?',
    body:'Все сессии, кроме текущего устройства, будут отключены.',
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Завершить все', onclick(){
        ST2_SES.forEach(s => { if(!s.cur && ST2.killed.indexOf(s.id) < 0) ST2.killed.push(s.id); });
        st2Save(); st2Render(); toast('Остальные сессии завершены');
      }},
    ],
  });
  const ok = document.querySelector('#okoPopup [data-pa="1"]');
  if(ok) ok.classList.add('st2-btn-danger');
}

/* ---------- Опасная зона: двойное подтверждение удаления ---------- */
function st2DeleteAsk(){
  showPopup({
    ico:'trash', title:'Удалить аккаунт?',
    body:'После подтверждения аккаунт перейдёт в режим удаления. Через 14 дней будут стёрты безвозвратно:<br>· профиль и подписчики<br>· чаты, каналы и сторис<br>· кошелёк и история операций<br>· сертификаты Академии<br><br>Отменить удаление можно в любой момент до конца отсчёта.',
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Продолжить', onclick: ()=> st2DeleteConfirm()},
    ],
  });
  const ok = document.querySelector('#okoPopup [data-pa="1"]');
  if(ok) ok.classList.add('st2-btn-danger');
}
function st2DeleteConfirm(err){
  st2Prompt({
    ico:'trash', title:'Последний шаг', danger:true, err: err,
    note:'Для подтверждения введи свой ник <b>@' + esc(st2Nick()) + '</b>. Аккаунт будет помечен на удаление и стёрт через 14 дней.',
    ph:'@' + st2Nick(), saveLabel:'Пометить на удаление',
    save(v){
      if(v.replace(/^@/, '').toLowerCase() !== st2Nick().toLowerCase())
        return st2DeleteConfirm('Ник не совпадает — аккаунт не помечен');
      ST2.delAt = Date.now() + 14*24*3600*1000;
      st2Save(); st2Render();
      toast('Удаление запланировано · осталось 14 дней');
    },
  });
}
function st2DoDelete(){
  try{ localStorage.clear(); }catch(e){}
  toast('Аккаунт удалён. До встречи!');
  document.body.style.transition = 'opacity .5s';
  document.body.style.opacity = '0';
  setTimeout(()=> location.reload(), 650); /* oko-auth стёрт -> загрузка на экран входа */
}

/* JSON-выгрузка: профиль + чаты + платежи + настройки (готовится локально) */
function st2ExportJson(){
  const p = (typeof PROFILE !== 'undefined') ? PROFILE : {};
  const themeMode = ST2.theme || (document.documentElement.dataset.theme === 'light' ? 'light' : 'dark');
  const chats = [];
  try{
    if(typeof CHATS !== 'undefined' && Array.isArray(CHATS)){
      CHATS.slice(0, 60).forEach(c => {
        chats.push({name:c.name, kind:c.kind || 'chat', unread:c.unread || 0,
          lastMsg: (c.msgs && c.msgs.length ? String(c.msgs[c.msgs.length-1].t || c.msgs[c.msgs.length-1].text || '').slice(0, 240) : null),
          count: (c.msgs || []).length});
      });
    }
  }catch(e){}
  const wallet = {};
  try{
    if(typeof WALLET !== 'undefined'){
      wallet.account = WALLET.acc; wallet.balance = WALLET.balance; wallet.hold = WALLET.hold;
      wallet.ledger = (WALLET.ledger || []).slice(0, 300).map(e => ({date:e.d, amount:e.sum, note:e.why}));
    }
  }catch(e){}
  const data = {
    format: 'oko-account-export',
    version: 2,
    generatedAt: new Date().toISOString(),
    profile: {name:p.name, nick:st2Nick(), tier:p.tier, role:p.role, bio:p.bio, status:p.status, avatar:!!p.avatar, cover:!!p.cover,
              email:ST2.email, phone:ST2.phone, telegram:ST2.tg},
    accounts: ST2.accounts.map(a => ({id:a.id, name:a.name, nick:a.nick, tier:a.tier, role:a.role,
                                     active: a.id === ST2.activeAcc})),
    settings: {
      notif: ST2.notif, privacy: ST2.priv, security: {twofa:ST2.sec.twofa, passcode:ST2.sec.passcode, autolock:ST2.sec.autolock},
      theme: themeMode, themeAuto: ST2.themeAuto,
      language: (typeof LANG !== 'undefined') ? LANG : 'ru',
      accessibility: ST2.a11y, dataStorage: ST2.data, locale: ST2.loc,
    },
    chats, wallet,
    sessions: st2Alive().map(s => ({device:s.dev, app:s.app, current:!!s.cur, geo:s.geo || null})),
    blocked: ST2.blocked.slice(),
    deleteScheduledAt: ST2.delAt ? new Date(ST2.delAt).toISOString() : null,
  };
  try{
    const url = URL.createObjectURL(new Blob([JSON.stringify(data, null, 2)], {type:'application/json;charset=utf-8'}));
    const a = document.createElement('a');
    a.href = url; a.download = 'oko-export-' + st2Nick() + '-' + new Date().toISOString().slice(0,10) + '.json';
    document.body.appendChild(a); a.click(); a.remove();
    setTimeout(()=> URL.revokeObjectURL(url), 4000);
    toast('JSON-выгрузка скачана · ' + (JSON.stringify(data).length/1024).toFixed(1) + ' КБ');
  }catch(e){ toast('Не удалось сформировать файл'); }
}

/* ================= ПРОФИЛЬ: аватар + обложка (реальная загрузка, canvas-сжатие, персист) ================= */
/* хранилище — в PROFILE.avatar / PROFILE.cover (dataURL) + per-account в ST2.accounts.
   Единый источник аккаунтов — ST2, поэтому фото переключается вместе с аккаунтом. */

/* cover-fit сжатие файла в dataURL нужного размера */
function st2FitImage(file, cw, ch, quality, cb){
  if(!file || !/^image\//.test(file.type)){ toast('Нужен файл изображения'); return; }
  const rd = new FileReader();
  rd.onload = function(){
    const img = new Image();
    img.onload = function(){
      const sw = img.naturalWidth, sh = img.naturalHeight;
      if(!sw || !sh){ toast('Пустое изображение'); return; }
      let cv;
      try{
        cv = document.createElement('canvas'); cv.width = cw; cv.height = ch;
        const ctx = cv.getContext('2d');
        const scale = Math.max(cw / sw, ch / sh);          /* cover-fit */
        const dw = sw * scale, dh = sh * scale;
        ctx.drawImage(img, (cw - dw) / 2, (ch - dh) / 2, dw, dh);
        cb(cv.toDataURL('image/jpeg', quality));
      }catch(e){ toast('Не удалось обработать фото'); }
    };
    img.onerror = function(){ toast('Не удалось открыть изображение'); };
    img.src = rd.result;
  };
  rd.onerror = function(){ toast('Ошибка чтения файла'); };
  rd.readAsDataURL(file);
}
/* системный выбор файла (переиспользуемый скрытый input) */
let _st2FileCb = null;
function st2PickFile(cb){
  _st2FileCb = cb;
  let inp = document.getElementById('st2FileInput');
  if(!inp){
    inp = document.createElement('input');
    inp.type = 'file'; inp.accept = 'image/*'; inp.id = 'st2FileInput'; inp.style.display = 'none';
    inp.addEventListener('change', function(){
      const f = inp.files && inp.files[0]; inp.value = '';
      if(f && _st2FileCb) _st2FileCb(f);
    });
    document.body.appendChild(inp);
  }
  inp.click();
}

/* аватар */
function st2SetAvatar(){
  st2PickFile(f => st2FitImage(f, 512, 512, 0.84, url => {
    if(typeof PROFILE !== 'undefined') PROFILE.avatar = url;
    st2SyncActiveFromProfile(); st2Save();
    st2PaintEditAva(); st2PaintProfile();
    toast('Фото профиля обновлено');
  }));
}
function st2RemoveAvatar(){
  if(typeof PROFILE !== 'undefined') PROFILE.avatar = null;
  st2SyncActiveFromProfile(); st2Save();
  st2PaintEditAva(); st2PaintProfile();
  toast('Фото профиля удалено');
}
function st2AvatarMenu(){
  const has = typeof PROFILE !== 'undefined' && !!PROFILE.avatar;
  showPopup({
    ico:'camera', title:'Фото профиля',
    body:'Загрузите фото с устройства — оно сожмётся и сохранится на этом устройстве. Фото привязано к активному аккаунту.',
    actions: has
      ? [{label:'Загрузить новое', onclick: st2SetAvatar}, {label:'Удалить фото', ghost:true, onclick: st2RemoveAvatar}]
      : [{label:'Загрузить фото', onclick: st2SetAvatar}, {label:'Отмена', ghost:true}],
  });
}
/* обложка */
function st2SetCover(){
  st2PickFile(f => st2FitImage(f, 1080, 420, 0.82, url => {
    if(typeof PROFILE !== 'undefined') PROFILE.cover = url;
    st2SyncActiveFromProfile(); st2Save();
    st2PaintEditCover(); st2PaintProfile();
    toast('Обложка обновлена');
  }));
}
function st2RemoveCover(){
  if(typeof PROFILE !== 'undefined') PROFILE.cover = null;
  st2SyncActiveFromProfile(); st2Save();
  st2PaintEditCover(); st2PaintProfile();
  toast('Обложка удалена');
}
function st2CoverMenu(){
  const has = typeof PROFILE !== 'undefined' && !!PROFILE.cover;
  showPopup({
    ico:'st2-image', title:'Обложка профиля',
    body:'Широкое фото-полотно в шапке профиля. Загрузите изображение — оно сожмётся и сохранится локально.',
    actions: has
      ? [{label:'Загрузить новую', onclick: st2SetCover}, {label:'Убрать обложку', ghost:true, onclick: st2RemoveCover}]
      : [{label:'Загрузить обложку', onclick: st2SetCover}, {label:'Отмена', ghost:true}],
  });
}

/* ---------- покраска аватаров/обложек ---------- */
function st2PaintAvaEl(el, url, letter){
  if(!el) return;
  if(url){
    el.style.backgroundImage = 'url("' + url + '")';
    el.textContent = '';
    el.classList.add('st2-has-photo');
  } else {
    el.style.backgroundImage = '';
    el.classList.remove('st2-has-photo');
    if(letter !== undefined && letter !== null) el.textContent = letter;
  }
}
function st2ProfLetter(){ return (typeof PROFILE !== 'undefined' && PROFILE.name ? PROFILE.name[0] : 'O').toUpperCase(); }
/* профиль (ядро): #profAva + собственная обложка над profile-top */
function st2PaintProfile(){
  const url = (typeof PROFILE !== 'undefined' && PROFILE.avatar) || null;
  st2PaintAvaEl(document.getElementById('profAva'), url, st2ProfLetter());
  st2PaintMyCover();
}
function st2PaintMyCover(){
  const pad = document.querySelector('#screen-profile .pad');
  if(!pad) return;
  const top = pad.querySelector('.profile-top');
  if(!top) return;
  const url = (typeof PROFILE !== 'undefined' && PROFILE.cover) || null;
  let cov = document.getElementById('st2MyCover');
  if(!cov){
    cov = document.createElement('button');
    cov.id = 'st2MyCover'; cov.className = 'st2-mycover';
    cov.setAttribute('aria-label', 'Обложка профиля');
    cov.onclick = st2CoverMenu;
    pad.insertBefore(cov, top);
  }
  cov.classList.toggle('has', !!url);
  cov.style.backgroundImage = url ? 'url("' + url + '")' : '';
  cov.innerHTML = `<span class="st2-mycover-grid"></span>
    <span class="st2-mycover-mark">${I('logo')}</span>
    <span class="st2-mycover-edit">${I('camera')}${url ? 'Сменить обложку' : 'Добавить обложку'}</span>`;
}
/* экран редактирования (ядро #editProfile) */
function st2PaintEditAva(){
  st2PaintAvaEl(document.getElementById('epAva'), (typeof PROFILE !== 'undefined' && PROFILE.avatar) || null, st2ProfLetter());
}
function st2PaintEditCover(){
  const el = document.getElementById('st2EpCover');
  if(!el) return;
  const url = (typeof PROFILE !== 'undefined' && PROFILE.cover) || null;
  el.classList.toggle('has', !!url);
  el.style.backgroundImage = url ? 'url("' + url + '")' : '';
  el.innerHTML = `<span class="ep-cover-mark">${I('logo')}</span>
    <span class="ep-cover-hint">${I('st2-image')}${url ? 'Сменить обложку' : 'Добавить обложку'}</span>`;
}
/* рерайринг кнопок фото ядра + инъекция контрола обложки в #editProfile (один раз) */
function st2WireEditProfile(){
  const avBtn = document.querySelector('#editProfile .ep-avabtn');
  const avLink = document.querySelector('#editProfile .ep-photolink');
  if(avBtn){ avBtn.removeAttribute('onclick'); avBtn.onclick = st2AvatarMenu; }
  if(avLink){ avLink.removeAttribute('onclick'); avLink.onclick = st2AvatarMenu; avLink.textContent = 'Изменить фото'; }
  if(avLink && !document.getElementById('st2EpCover')){
    const lab = document.createElement('label'); lab.className = 'ep-lab'; lab.textContent = 'Обложка профиля'; lab.id = 'st2EpCoverLab';
    const cov = document.createElement('button'); cov.type = 'button'; cov.className = 'ep-cover'; cov.id = 'st2EpCover';
    cov.onclick = st2CoverMenu;
    avLink.insertAdjacentElement('afterend', cov);
    avLink.insertAdjacentElement('afterend', lab);
  }
}

/* ================= ЧЁРНЫЙ СПИСОК / БЛОКИРОВКИ (реальный персист в oko-settings2) ================= */
function st2IsBlocked(name){ return ST2.blocked.indexOf(name) > -1; }
function st2Block(name){
  if(!name || st2IsBlocked(name)) return false;
  ST2.blocked.push(name); st2Save();
  try{ if(typeof psSetFollow === 'function') psSetFollow(name, false); }catch(e){}  /* блок = отписка */
  try{ if(typeof renderFeed === 'function' && typeof curFeedKind !== 'undefined') renderFeed(curFeedKind); }catch(e){}
  st2SyncBlockedVal();
  return true;
}
function st2Unblock(name){
  const i = ST2.blocked.indexOf(name);
  if(i < 0) return false;
  ST2.blocked.splice(i, 1); st2Save();
  try{ if(typeof renderFeed === 'function' && typeof curFeedKind !== 'undefined') renderFeed(curFeedKind); }catch(e){}
  st2SyncBlockedVal();
  return true;
}
function st2SyncBlockedVal(){
  const el = document.getElementById('st2BlockedVal');
  if(el) el.textContent = ST2.blocked.length
    ? ST2.blocked.length + (ST2.blocked.length === 1 ? ' человек' : (ST2.blocked.length < 5 ? ' человека' : ' человек'))
    : 'пусто';
}
/* убрать посты заблокированных из ленты */
function st2FilterBlockedFeed(){
  if(!ST2.blocked.length) return;
  const list = document.getElementById('feedList');
  if(!list) return;
  list.querySelectorAll('article.post').forEach(art => {
    const b = art.querySelector('.post-more');
    const m = b && (b.getAttribute('onclick') || '').match(/openPostMenu\((\d+)/);
    const p = (m && typeof postById === 'function') ? postById(+m[1]) : null;
    if(p && p.name && st2IsBlocked(p.name)) art.remove();
  });
}
/* кандидаты на блокировку — реальные люди приложения (из profile-social пула) */
function st2BlockablePeople(){
  let pool = [];
  try{ if(typeof psPeoplePool === 'function') pool = psPeoplePool(); }catch(e){}
  return pool.filter(p => p && p.name && !st2IsBlocked(p.name));
}
function st2BlAva(p){
  if(p && p.avaIcon) return I(p.avaIcon);
  if(p && p.ava && p.ava.length <= 3) return esc(p.ava);
  return esc(st2AccInit((p && p.name) || 'U'));
}
function st2UnblockUI(enc){ const n = decodeURIComponent(enc); if(st2Unblock(n)){ st2RenderBlocked(); toast('Разблокирован: ' + n); } }
function st2BlockUI(enc){ const n = decodeURIComponent(enc); if(st2Block(n)){ st2RenderBlocked(); toast('Заблокирован: ' + n); } }
function st2RenderBlocked(){
  const wrap = document.getElementById('st2BlockedBody');
  if(!wrap) return;
  const blocked = ST2.blocked.slice();
  const pool = st2BlockablePeople();
  const byName = {}; pool.forEach(p => byName[p.name] = p);
  const suggest = pool.slice(0, 16);
  const blRow = name => {
    const p = byName[name] || {name};
    return `<div class="st2-bl-row">
      <span class="st2-bl-av">${st2BlAva(p)}</span>
      <div class="st2-bl-b"><b>${esc(name)}</b><small>в чёрном списке</small></div>
      <button class="st2-bl-un" onclick="st2UnblockUI('${encodeURIComponent(name)}')">Разблокировать</button>
    </div>`;
  };
  const sgRow = p =>
    `<div class="st2-bl-row st2-bl-sg">
      <span class="st2-bl-av">${st2BlAva(p)}</span>
      <div class="st2-bl-b"><b>${esc(p.name)}</b><small>@${esc(p.nick || st2AccInit(p.name).toLowerCase())}</small></div>
      <button class="st2-bl-do" onclick="st2BlockUI('${encodeURIComponent(p.name)}')">${I('st2-ban')} Заблокировать</button>
    </div>`;
  wrap.innerHTML =
    (blocked.length
      ? `<div class="st2-bl-cap">В чёрном списке · ${blocked.length}</div><div class="st2-bl-list">${blocked.map(blRow).join('')}</div>`
      : `<div class="st2-bl-empty">${I('st2-ban')}<p>Чёрный список пуст</p><span>Заблокированные не смогут вам писать и исчезнут из вашей ленты</span></div>`) +
    `<button class="st2-bl-add" onclick="st2BlockManual()">${I('plus')} Заблокировать по @нику</button>` +
    (suggest.length ? `<div class="st2-bl-cap" style="margin-top:6px">Люди из приложения</div><div class="st2-bl-list">${suggest.map(sgRow).join('')}</div>` : '');
}
function st2BlockManual(err){
  st2Prompt({
    ico:'st2-ban', title:'Заблокировать', err: err,
    note:'Введите имя или @ник — пользователь попадёт в чёрный список.',
    ph:'@nickname или Имя', saveLabel:'Заблокировать', danger:true,
    save(v){
      v = v.replace(/^@/, '').trim();
      if(v.length < 2) return st2BlockManual('Слишком короткое имя');
      /* попробуем сопоставить с реальным человеком из пула по нику/имени */
      let match = v;
      try{
        const pool = (typeof psPeoplePool === 'function') ? psPeoplePool() : [];
        const hit = pool.find(p => (p.nick || '').toLowerCase() === v.toLowerCase() || p.name.toLowerCase() === v.toLowerCase());
        if(hit) match = hit.name;
      }catch(e){}
      if(st2IsBlocked(match)) return st2BlockManual('Уже в чёрном списке');
      st2Block(match); st2RenderBlocked(); toast('Заблокирован: ' + match);
    },
  });
}
function st2OpenBlocked(){
  st2Push('blocked');
}

/* ================= ВЫХОД ИЗ АККАУНТА ================= */
function st2LogoutAccount(){
  const others = ST2.accounts.filter(a => a.id !== ST2.activeAcc);
  const cur = st2ActiveAcc();
  showPopup({
    ico:'logout', title:'Выйти из аккаунта?',
    body: others.length
      ? `Вы выйдете из <b>${esc(cur ? cur.name : 'текущего аккаунта')}</b> на этом устройстве и переключитесь на «${esc(others[0].name)}».`
      : 'Вы выйдете из аккаунта и вернётесь на экран входа.',
    actions:[
      {label:'Отмена', ghost:true},
      {label:'Выйти', onclick: st2DoLogoutAccount},
    ],
  });
  const ok = document.querySelector('#okoPopup [data-pa="1"]');
  if(ok) ok.classList.add('st2-btn-danger');
}
function st2DoLogoutAccount(){
  const goneId = ST2.activeAcc;
  const others = ST2.accounts.filter(a => a.id !== goneId);
  if(!others.length){
    /* Вопрос уже задан в st2LogoutAccount — зовём выход напрямую,
       иначе человек увидит два одинаковых окна подряд. */
    if(typeof doLogoutNow === 'function') doLogoutNow();
    else { try{ localStorage.removeItem('oko-auth'); }catch(e){} location.reload(); }
    return;
  }
  ST2.accounts = ST2.accounts.filter(a => a.id !== goneId);
  st2Save();
  st2SwitchAccount(others[0].id);   /* применит профиль + renderMyProfile + persist + toast */
  st2Render();
  toast('Вы вышли из аккаунта');
}

/* ---------- свои SVG-иконки (штрих ядра: stroke 7, округлые концы) ---------- */
function st2AddIcons(){
  const defs = document.querySelector('svg defs');
  if(!defs || document.getElementById('i-gear')) return;
  const mk = (id, inner)=>{
    const s = document.createElementNS('http://www.w3.org/2000/svg', 'symbol');
    s.setAttribute('id', id); s.setAttribute('viewBox', '0 0 100 100');
    s.innerHTML = inner;
    defs.appendChild(s);
  };
  /* шестерёнка: контур feather-settings, масштаб 24->100 (штрих 1.68*4.167≈7) */
  mk('i-gear', '<g fill="none" stroke="currentColor" stroke-width="1.68" stroke-linecap="round" stroke-linejoin="round" transform="scale(4.1667)">' +
    '<circle cx="12" cy="12" r="3.4"/>' +
    '<path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></g>');
  /* конверт (почта) */
  mk('i-st2-mail', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<rect x="12" y="26" width="76" height="50" rx="9"/><path d="M16 32 L50 58 L84 32"/></g>');
  /* устройства (монитор + телефон) */
  mk('i-st2-devices', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M56 62 H16 a6 6 0 0 1 -6 -6 V26 a6 6 0 0 1 6 -6 H74 a6 6 0 0 1 6 6 v6"/>' +
    '<path d="M28 76 h20 M38 62 v14"/>' +
    '<rect x="62" y="42" width="28" height="46" rx="7"/><path d="M72 78 h8"/></g>');
  /* база данных (цилиндр) */
  mk('i-st2-db', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<ellipse cx="50" cy="24" rx="30" ry="11"/>' +
    '<path d="M20 24 v26 c0 6 13.4 11 30 11 s30-5 30-11 V24"/>' +
    '<path d="M20 50 v26 c0 6 13.4 11 30 11 s30-5 30-11 V50"/></g>');
  /* щит с галочкой (безопасность / 2FA) */
  mk('i-st2-shield', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M50 9 L83 21 V47 C83 68 69 83 50 91 C31 83 17 68 17 47 V21 Z"/>' +
    '<path d="M37 49 l9 10 18-22"/></g>');
  /* ключ (смена пароля) */
  mk('i-st2-key', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<circle cx="35" cy="37" r="17"/><path d="M47 49 L86 88"/><path d="M76 78 l9 -9"/><path d="M65 67 l9 -9"/></g>');
  /* запрет / бан (чёрный список) */
  mk('i-st2-ban', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<circle cx="50" cy="50" r="38"/><path d="M24 24 L76 76"/></g>');
  /* картинка / обложка */
  mk('i-st2-image', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<rect x="14" y="20" width="72" height="60" rx="9"/><circle cx="36" cy="41" r="7"/>' +
    '<path d="M20 74 L42 52 L58 66 L72 54 L84 64"/></g>');
  /* лупа (поиск по настройкам) */
  mk('i-st2-search', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<circle cx="43" cy="43" r="27"/><path d="M63 63 L86 86"/></g>');
  /* крестик (очистить поиск) */
  mk('i-st2-x', '<g fill="none" stroke="currentColor" stroke-width="8" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M28 28 L72 72 M72 28 L28 72"/></g>');
  /* часы (автоблокировка) */
  mk('i-st2-clock', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<circle cx="50" cy="50" r="38"/><path d="M50 28 V50 L66 60"/></g>');
  /* динамик (звук) */
  mk('i-st2-speaker', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M18 38 h14 L52 22 v56 L32 62 H18 Z"/>' +
    '<path d="M64 36 c7 8 7 20 0 28"/><path d="M74 26 c14 15 14 33 0 48"/></g>');
  /* вибрация */
  mk('i-st2-vibrate', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<rect x="36" y="22" width="28" height="56" rx="4"/>' +
    '<path d="M14 40 v20 M22 34 v32 M78 34 v32 M86 40 v20"/></g>');
  /* бейдж (кружок с точкой) */
  mk('i-st2-badge', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<rect x="14" y="14" width="52" height="52" rx="10"/>' +
    '<circle cx="76" cy="24" r="12" fill="currentColor" stroke="none"/></g>');
  /* контроллер игр */
  mk('i-st2-controller', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M22 66 L14 50 c-3-13 6-24 20-24 h32 c14 0 23 11 20 24 L78 66 c-3 6-11 6-14 1 L58 60 H42 L36 67 c-3 5-11 5-14-1 Z"/>' +
    '<path d="M28 45 h10 M33 40 v10"/><circle cx="66" cy="42" r="3.5" fill="currentColor" stroke="none"/><circle cx="74" cy="49" r="3.5" fill="currentColor" stroke="none"/></g>');
  /* eq / тюнер (каналы) */
  mk('i-st2-tune', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M20 26 h60 M20 50 h60 M20 74 h60"/>' +
    '<circle cx="36" cy="26" r="6" fill="var(--bg,#000)"/><circle cx="66" cy="50" r="6" fill="var(--bg,#000)"/><circle cx="30" cy="74" r="6" fill="var(--bg,#000)"/></g>');
  /* WiFi */
  mk('i-st2-wifi', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M12 40 c22-22 54-22 76 0"/><path d="M24 54 c14-14 38-14 52 0"/><path d="M36 68 c7-7 21-7 28 0"/>' +
    '<circle cx="50" cy="80" r="4" fill="currentColor" stroke="none"/></g>');
  /* линк (привязка) */
  mk('i-st2-link', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M40 32 h-8 a20 20 0 0 0 0 40 h12"/>' +
    '<path d="M60 68 h8 a20 20 0 0 0 0 -40 h-12"/>' +
    '<path d="M36 50 h28"/></g>');
  /* глаз в круге (a11y) */
  mk('i-st2-eye', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M10 50 c14-24 66-24 80 0 c-14 24-66 24-80 0 Z"/>' +
    '<circle cx="50" cy="50" r="12"/></g>');
  /* motion (стрелка со следом) */
  mk('i-st2-motion', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M18 62 L46 34 L58 46 L84 22"/><path d="M70 22 h14 v14"/></g>');
  /* контраст (половинки круга) */
  mk('i-st2-contrast', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<circle cx="50" cy="50" r="36"/><path d="M50 14 v72 A36 36 0 0 1 50 14 Z" fill="currentColor" stroke="none"/></g>');
  /* рука/тап */
  mk('i-st2-hand', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<path d="M34 48 V22 c0-5 3-9 8-9 s8 4 8 9 v22"/>' +
    '<path d="M50 44 v-6 c0-4 3-7 7-7 s7 3 7 7 v14"/>' +
    '<path d="M64 46 c0-3 3-6 7-6 s7 3 7 7 v18 c0 12-9 22-22 22 h-4 c-10 0-18-6-22-14 L20 56 c-2-4 1-8 5-8 c3 0 5 1 7 4 l4 6"/></g>');
  /* спасательный круг */
  mk('i-st2-life', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<circle cx="50" cy="50" r="36"/><circle cx="50" cy="50" r="13"/>' +
    '<path d="M25 25 L41 41 M75 25 L59 41 M25 75 L41 59 M75 75 L59 59"/></g>');
  /* «?» в круге */
  mk('i-st2-help-circle', '<g fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round">' +
    '<circle cx="50" cy="50" r="36"/>' +
    '<path d="M40 40 c0-8 6-13 12-13 s12 5 12 12 c0 8-12 10-12 18"/><circle cx="52" cy="70" r="3.5" fill="currentColor" stroke="none"/></g>');
}

/* ---------- строка «Настройки» в профиле ---------- */
function st2InsertRow(){
  if(document.getElementById('st2Row')) return;
  const card = document.querySelector('#screen-profile .card');
  if(!card) return;
  const row = document.createElement('button');
  row.className = 'prow'; row.id = 'st2Row';
  row.onclick = st2Open;
  row.innerHTML = `${I('gear')} Настройки <span class="chev">${I('chev')}</span>`;
  const after = card.querySelector('.prow[onclick="openEdit()"]');
  if(after) after.insertAdjacentElement('afterend', row);
  else card.insertBefore(row, card.firstChild);
}

/* ---------- самоинициализация ---------- */
(function st2Init(){
  st2AddIcons();
  st2InsertRow();
  st2WireEditProfile();

  /* chain renderMyProfile: покрасить свой аватар + собственную обложку (внешний слой) */
  if(typeof renderMyProfile === 'function'){
    const _st2PrevRMP = renderMyProfile;
    renderMyProfile = function(){
      _st2PrevRMP.apply(this, arguments);
      try{ st2PaintProfile(); }catch(e){}
    };
  }
  /* chain openEdit: показать текущее фото/обложку в редакторе */
  if(typeof openEdit === 'function'){
    const _st2PrevOpenEdit = openEdit;
    openEdit = function(){
      _st2PrevOpenEdit.apply(this, arguments);
      try{ st2WireEditProfile(); st2PaintEditAva(); st2PaintEditCover(); }catch(e){}
    };
  }
  /* chain saveProfile: персист профиля (имя/ник/био/статус/фото/обложка) в активный аккаунт */
  if(typeof saveProfile === 'function'){
    const _st2PrevSaveProfile = saveProfile;
    saveProfile = function(){
      _st2PrevSaveProfile.apply(this, arguments);   /* ядро уже обновило PROFILE + вызвало renderMyProfile */
      try{ st2SyncActiveFromProfile(); st2Save(); st2PaintProfile(); }catch(e){}
    };
  }
  /* chain renderFeed: убрать посты заблокированных (внешний слой — после декора profile-social) */
  if(typeof renderFeed === 'function'){
    const _st2PrevRenderFeed = renderFeed;
    renderFeed = function(){
      _st2PrevRenderFeed.apply(this, arguments);
      try{ st2FilterBlockedFeed(); }catch(e){}
    };
  }
  /* chain closeSheet: снять подъём шита чёрного списка над #st2View */
  if(typeof closeSheet === 'function'){
    const _st2PrevCloseSheet = closeSheet;
    closeSheet = function(){
      _st2PrevCloseSheet.apply(this, arguments);
      try{ document.body.classList.remove('st2-over'); }catch(e){}
    };
  }

  /* аккаунты: сид из PROFILE при первом запуске; иначе — применить активный аккаунт */
  if(typeof PROFILE !== 'undefined'){
    /* миграция: сохранённый ранее override ника -> PROFILE (до сида аккаунтов) */
    if(ST2.nick && !ST2.accounts.length && PROFILE.nick !== ST2.nick){
      PROFILE.nick = ST2.nick;
      if(typeof renderMyProfile === 'function') renderMyProfile();
    }
    if(!ST2.accounts.length){
      ST2.accounts = [{id:'owner', name:PROFILE.name, nick:PROFILE.nick, tier:PROFILE.tier, role:PROFILE.role, bio:PROFILE.bio,
                       status:PROFILE.status||null, avatar:PROFILE.avatar||null, cover:PROFILE.cover||null}];
      ST2.activeAcc = 'owner';
      st2Save();
    } else {
      if(!ST2.activeAcc || !ST2.accounts.some(a => a.id === ST2.activeAcc)) ST2.activeAcc = ST2.accounts[0].id;
      const a = st2ActiveAcc();
      if(a){
        st2ApplyAccToProfile(a);
        ST2.nick = a.nick;
        if(typeof renderMyProfile === 'function') renderMyProfile();
        st2Save();
      }
    }
  } else if(ST2.nick){
    /* PROFILE ещё не готов — ничего не делаем, применится при рендере */
  }

  /* мои настройки -> старые sheet-ы ядра; и chain-обратный синк из них */
  st2PushCore();
  if(typeof toggleSetting === 'function'){
    const _st2PrevToggleSetting = toggleSetting;
    toggleSetting = function(group, k){
      _st2PrevToggleSetting(group, k);
      st2PullCore();
      const v = document.getElementById('st2View');
      if(v && v.classList.contains('open')) st2Render();
    };
  }

  /* тема «Система»: применить и слушать смену системной темы */
  if(ST2.theme === 'system') st2ApplyTheme('system');
  try{
    const mq = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)');
    if(mq){
      const h = ()=>{ if(ST2.theme === 'system') st2ApplyTheme('system'); };
      if(mq.addEventListener) mq.addEventListener('change', h);
      else if(mq.addListener) mq.addListener(h);
    }
  }catch(e){}

  /* при смене языка — перерисовать открытую вьюху (обновить активную кнопку) */
  if(typeof onLangChange === 'function'){
    onLangChange(function(){
      const v = document.getElementById('st2View');
      if(v && v.classList.contains('open')) st2Render();
    });
  }

  /* стартовая покраска аватара/обложки (ядро могло отрисовать профиль до установки чейна) */
  try{ st2PaintProfile(); }catch(e){}

  /* применить сохранённые опции a11y и запустить авто-расписание темы */
  try{ st2ApplyA11y(); }catch(e){}
  try{ if(ST2.themeAuto && ST2.themeAuto.on) st2ScheduleThemeAuto(); }catch(e){}

  /* автоматически стереть аккаунт, если срок 14 дней истёк, пока пользователя не было */
  try{ if(ST2.delAt && ST2.delAt < Date.now()) st2DoDelete(); }catch(e){}
})();

/* ===== module: notifs-plus ===== */
/* ===== NOTIFS-PLUS: центр уведомлений уровня TG + iOS/Android system =====
   Полный overlay поверх ядра base.html. Не трогает разметку — только
   перекрывает window.renderNotifs / updateNotifDot и chain-патчит
   openNotifs / closeNotifs / notifTap.

   Механики:
     1) Категории-чипы: Всё / Чаты / Партнёрка / Игры / Академия / Система
     2) Группировка (5 новых лайков → тап раскрывает список, iOS-стиль)
     3) Inline-действия: Ответить (textarea), Принять/Отклонить, Оплатить, Открыть
     4) Свайпы: влево → Отклонить, вправо → Прочитано (iOS Mail)
     5) Live-toast стек сверху экрана (slide-in-out 4s, до 3, «закрыть все»)
     6) Расписание "Не беспокоить" (по умолчанию 22:00–08:00) + иконка луны
     7) Приоритеты: Все / Только важные / Тишина — bell-иконка меняется
     8) Бейдж с числом непрочитанных (>99 → «99+»)
     9) Настройки каналов (звук/вибро/превью/бейдж) — persist в localStorage
    10) Демо-поток: реалистичные примеры и авторы, «X мин назад» */

(function(){
  if(typeof NOTIFS==='undefined' || typeof window.renderNotifs!=='function') return;

  /* ================================================================
     STATE + PERSIST
  ================================================================ */
  var LS = 'oko-npx-v2';
  var DEFAULTS = {
    filter:   'all',
    priority: 'all',                 // all | important | silent
    dnd:      { mode:'off', start:'22:00', end:'08:00' }, // off | on | schedule
    channels: { sound:true, vibro:true, preview:true, badge:true },
    read:     {},                    // id -> true (в дополнение к n.unread=false)
    expanded: {}                     // stackKey -> true
  };
  var NPX = (function(){
    try{
      var raw = JSON.parse(localStorage.getItem(LS));
      if(!raw || typeof raw!=='object') return JSON.parse(JSON.stringify(DEFAULTS));
      // merge с дефолтами (миграция)
      return {
        filter:   raw.filter   || DEFAULTS.filter,
        priority: raw.priority || DEFAULTS.priority,
        dnd:      Object.assign({}, DEFAULTS.dnd, raw.dnd||{}),
        channels: Object.assign({}, DEFAULTS.channels, raw.channels||{}),
        read:     raw.read     || {},
        expanded: raw.expanded || {}
      };
    }catch(e){ return JSON.parse(JSON.stringify(DEFAULTS)); }
  })();
  function save(){ try{ localStorage.setItem(LS, JSON.stringify(NPX)); }catch(e){} }

  /* ================================================================
     ХЕЛПЕРЫ
  ================================================================ */
  function _esc(s){
    if(typeof esc==='function') return esc(s==null?'':String(s));
    return String(s==null?'':s).replace(/[&<>"']/g,function(c){
      return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];
    });
  }
  function _I(n){ return typeof I==='function' ? I(n) : ''; }
  function _toast(m){ if(typeof toast==='function') toast(m); }
  var _uid = 0;
  function nextId(){ return 'n'+(++_uid)+'_'+Date.now().toString(36); }

  /* Категория по иконке / автору. Полностью покрывает демо и живой поток. */
  function catOf(n){
    if(!n) return 'system';
    if(n.cat) return n.cat;
    var who = (n.who||'').toLowerCase();
    var ic  = n.ic || '';
    if(ic==='money' || /партнёрк|партнерк|referral|вывод|выплат/i.test(who)) return 'partner';
    if(/академ|курс|урок|academy|lesson/i.test(who)) return 'academy';
    if(/игр|казино|рулетк|game|дорог/i.test(who))    return 'games';
    if(ic==='chat' || ic==='mic' || /чат|message|сообщ/i.test(who)) return 'chats';
    if(ic==='heart' || ic==='comment' || ic==='star') return 'chats';
    // системные — заявки, подписчики, ролик в топе и т.п.
    return 'system';
  }

  /* Что за inline-действия показать? */
  function actionsFor(n){
    var c = catOf(n);
    var acts = [];
    if(n.ic==='comment' || (c==='chats' && n.ic!=='heart')){
      acts.push({k:'reply', t:'Ответить', ic:'send',   cls:'primary'});
    }
    if(n.ic==='briefcase' || n.ic==='users'){
      acts.push({k:'accept', t:'Принять',   ic:'check', cls:'primary'});
      acts.push({k:'reject', t:'Отклонить', ic:'plus',  cls:'danger', rot:45});
    }
    if(n.ic==='money' && /счёт|счет|оплат|invoice/i.test(n.t||'')){
      acts.push({k:'pay',   t:'Оплатить', ic:'money', cls:'primary'});
    }
    if(n.ic==='rocket' || n.ic==='star'){
      acts.push({k:'open',  t:'Открыть',  ic:'back',  cls:'ghost', rot:180});
    }
    if(n.ic==='heart'){
      acts.push({k:'openPost', t:'Открыть пост', ic:'heart', cls:'ghost'});
    }
    // Если ничего не подошло — универсальное «Открыть»
    if(!acts.length) acts.push({k:'open', t:'Открыть', ic:'back', cls:'ghost', rot:180});
    return acts;
  }

  /* Расписание DND: активен ли режим сейчас? */
  function inSchedule(){
    var d = NPX.dnd; if(!d || d.mode!=='schedule') return false;
    var now = new Date();
    var cur = now.getHours()*60 + now.getMinutes();
    var s = parseHM(d.start), e = parseHM(d.end);
    if(s==null||e==null) return false;
    if(s===e) return false;
    if(s < e) return cur>=s && cur<e;              // 08:00–22:00
    return cur>=s || cur<e;                        // ночное окно 22:00–08:00
  }
  function parseHM(x){
    var m = /^(\d{1,2}):(\d{2})$/.exec(x||''); if(!m) return null;
    var h=+m[1], mn=+m[2]; if(h>23||mn>59) return null; return h*60+mn;
  }
  function dndActive(){
    if(!NPX.dnd) return false;
    if(NPX.dnd.mode==='on') return true;
    if(NPX.dnd.mode==='schedule') return inSchedule();
    return false;
  }
  function isSilent(){ return NPX.priority==='silent' || dndActive(); }

  /* Определяем «важное» уведомление: partner (деньги), заявки, счета,
     топ-охваты. Лайки и подписки — не важные. */
  function isImportant(n){
    var c = catOf(n);
    if(c==='partner') return true;
    if(n.ic==='briefcase') return true;
    if(/счёт|счет|оплат|invoice|вывод/i.test(n.t||'')) return true;
    if(n.ic==='rocket') return true;
    return false;
  }

  /* ================================================================
     ПОДГОТОВКА NOTIFS (id + мета) + ДЕМО-ЗАСЕВ
  ================================================================ */
  function ensureIds(){
    NOTIFS.forEach(function(n){
      if(!n.id) n.id = nextId();
      if(!n.cat) n.cat = catOf(n);
    });
  }
  var SEEDED = false;
  function seedDemo(){
    if(SEEDED) return; SEEDED = true;
    /* Дозасев уведомлений ОТКЛЮЧЁН (правка Даниэля: ноль демо-данных).
       Ниже добавлялись «Маша С. написала», «Костя Р. прислал голосовое»,
       «Оля Т. оценила твой пост» и заявки от несуществующих людей — по
       одному примеру на каждую категорию. Человек открывал колокольчик и
       видел чужую жизнь вместо своей. Пусто — значит пусто. */
    return;
    // eslint-disable-next-line no-unreachable
    // Дозасев: чтобы все категории имели живой пример.
    var haveCat = {};
    NOTIFS.forEach(function(n){ haveCat[catOf(n)] = true; });
    var add = [];
    if(!haveCat.chats) add.push(
      {ic:'chat',    who:'Маша С.',       t:'написала: «Привет! Ты видел новую сборку?»',           time:'6 мин',  g:'Сегодня', unread:true},
      {ic:'mic',     who:'Костя Р.',      t:'прислал голосовое 0:34',                                time:'22 мин', g:'Сегодня', unread:true}
    );
    if(!haveCat.games) add.push(
      {ic:'rocket',  who:'OKO Игры',      t:'ежедневный бонус +50 монет уже ждёт в Рулетке',         time:'1 ч',    g:'Сегодня', unread:true, cat:'games'},
      {ic:'star',    who:'OKO Дорога',    t:'ты в топ-10 недели — награда +200 монет',               time:'вчера',  g:'Ранее',   unread:false, cat:'games'}
    );
    if(!haveCat.academy) add.push(
      {ic:'star',    who:'Академия OKO',  t:'новый урок «Хук за 3 секунды» открыт для тебя',         time:'35 мин', g:'Сегодня', unread:true, cat:'academy', act:function(){ if(typeof showTab==='function'){ showTab('mini'); if(typeof openMa==='function') openMa('academy'); } }},
      {ic:'star',    who:'Академия OKO',  t:'сертификат «Reels-магистр» готов к скачиванию',         time:'2 ч',    g:'Сегодня', unread:false, cat:'academy'}
    );
    if(!haveCat.partner) add.push(
      {ic:'money',   who:'Партнёрка',     t:'счёт на вывод $124.80 сформирован — оплатим за 24 ч',   time:'18 мин', g:'Сегодня', unread:true}
    );
    // Несколько лайков подряд — покажем группировку
    add.push(
      {ic:'heart',   who:'Оля Т.',        t:'оценила твой пост «Как я убрал 90% ручного монтажа»',   time:'8 мин',  g:'Сегодня', unread:true},
      {ic:'heart',   who:'Никита В.',     t:'оценил твой пост «Как я убрал 90% ручного монтажа»',    time:'11 мин', g:'Сегодня', unread:true},
      {ic:'heart',   who:'Женя А.',       t:'оценил твой пост «Как я убрал 90% ручного монтажа»',    time:'12 мин', g:'Сегодня', unread:true}
    );
    // Заявка в чат/канал — accept/reject
    add.push(
      {ic:'users',   who:'Гриша Л.',      t:'просит вступить в закрытый канал «OKO инсайды»',        time:'14 мин', g:'Сегодня', unread:true, cat:'system'}
    );
    // Достижение
    add.push(
      {ic:'rocket',  who:'OKO',           t:'ты получил ачивку «100 постов» — держи +1000 монет',    time:'25 мин', g:'Сегодня', unread:true, cat:'system'}
    );
    NOTIFS.push.apply(NOTIFS, add);
    ensureIds();
  }

  /* ================================================================
     ЖИВОЙ ПОТОК (dozarazhda дозасев для демо)
  ================================================================ */
  var LIVE_POOL = []  /* демо-данные удалены 09.08: фейковые уведомления по таймеру — источник только API */;
  var liveT = null;
  function livePush(){
    var view = document.getElementById('notifsView'); if(!view) return;
    if(!view.classList.contains('open')) return;
    if(!LIVE_POOL.length) return;   /* пул вычищен 09.08 — событий из демо больше нет */
    var p = LIVE_POOL[Math.floor(Math.random()*LIVE_POOL.length)];
    var n = {id:nextId(), ic:p.ic, who:p.who, t:p.t, time:'только что', g:'Сегодня', unread:true, act:function(){}};
    n.cat = catOf(n);
    NOTIFS.unshift(n);
    if(typeof updateNotifDot==='function') updateNotifDot();
    window.renderNotifs();
    var first = document.querySelector('#notifsBody .np-row');
    if(first){
      first.classList.add('np-bounce');
      setTimeout(function(){ first.classList.remove('np-bounce'); }, 800);
    }
  }
  function liveStart(){
    if(liveT) return;
    var tick = function(){
      livePush();
      liveT = setTimeout(tick, 24000 + Math.random()*22000);
    };
    liveT = setTimeout(tick, 18000 + Math.random()*10000);
  }
  function liveStop(){ if(liveT){ clearTimeout(liveT); liveT = null; } }

  /* ================================================================
     LIVE-TOAST-СТЕК (top-of-screen, до 3, 4s auto-close)
  ================================================================ */
  function ensureStack(){
    var st = document.getElementById('np-live-stack');
    if(st) return st;
    st = document.createElement('div');
    st.id = 'np-live-stack';
    document.body.appendChild(st);
    return st;
  }
  function iconTone(ic){
    if(ic==='heart') return 'tone-heart';
    if(ic==='star')  return 'tone-star';
    if(ic==='comment' || ic==='chat' || ic==='mic') return 'tone-comment';
    if(ic==='money') return 'tone-money';
    return '';
  }
  function pushLive(n){
    if(isSilent()) return;                         // тихо
    if(NPX.priority==='important' && !isImportant(n)) return;
    var stack = ensureStack();
    // ограничить до 3
    var live = stack.querySelectorAll('.np-live');
    if(live.length >= 3) removeLive(live[0]);
    // «закрыть все» появляется, когда 2+ живых уведомлений
    ensureAllBtn();

    var el = document.createElement('div');
    el.className = 'np-live';
    var preview = NPX.channels.preview !== false;
    el.innerHTML =
      '<span class="np-live-ic '+iconTone(n.ic)+'"><svg class="i"><use href="#i-'+(n.ic||'bell')+'"/></svg></span>'+
      '<div class="np-live-b">'+
        '<b>'+_esc(n.who||'OKO')+'</b>'+
        (preview
          ? '<span>'+_esc(n.t||'')+'</span>'
          : '<span>Новое уведомление</span>')+
      '</div>'+
      '<button type="button" class="np-live-x" aria-label="Закрыть">'+
        '<svg class="i" style="transform:rotate(45deg)"><use href="#i-plus"/></svg>'+
      '</button>';
    stack.insertBefore(el, stack.firstChild);
    // slide-in
    requestAnimationFrame(function(){ el.classList.add('show'); });
    // auto-close 4s
    var timer = setTimeout(function(){ removeLive(el); }, 4000);
    // тап по телу → открыть центр уведомлений
    el.addEventListener('click', function(ev){
      if(ev.target.closest && ev.target.closest('.np-live-x')){
        ev.preventDefault(); ev.stopPropagation();
        clearTimeout(timer); removeLive(el); return;
      }
      clearTimeout(timer); removeLive(el);
      if(typeof openNotifs==='function') openNotifs();
    });
  }
  function removeLive(el){
    if(!el || !el.parentNode) return;
    el.classList.remove('show'); el.classList.add('hide');
    setTimeout(function(){
      if(el.parentNode) el.parentNode.removeChild(el);
      // если пусто — снять «закрыть все»
      var stack = document.getElementById('np-live-stack');
      if(stack && !stack.querySelector('.np-live')){
        var all = stack.querySelector('.np-live-all');
        if(all) all.parentNode.removeChild(all);
      } else ensureAllBtn();
    }, 240);
  }
  function ensureAllBtn(){
    var stack = document.getElementById('np-live-stack'); if(!stack) return;
    var live = stack.querySelectorAll('.np-live');
    var existing = stack.querySelector('.np-live-all');
    if(live.length >= 2 && !existing){
      var b = document.createElement('button');
      b.type = 'button'; b.className = 'np-live-all';
      b.textContent = 'Закрыть все';
      b.addEventListener('click', function(e){
        e.preventDefault(); e.stopPropagation();
        stack.querySelectorAll('.np-live').forEach(removeLive);
      });
      stack.appendChild(b);
    } else if(live.length < 2 && existing){
      existing.parentNode.removeChild(existing);
    }
  }

  /* ================================================================
     СЧЁТЧИК-БЕЙДЖ ПОВЕРХ #notifDot + СИНХРОНИЗАЦИЯ ИКОНКИ КОЛОКОЛА
  ================================================================ */
  var _baseUpdateDot = window.updateNotifDot;
  window.updateNotifDot = function(){
    var dot = document.getElementById('notifDot'); if(!dot) return;
    var count = NOTIFS.filter(function(x){ return x.unread; }).length;
    if(NPX.priority==='important'){
      count = NOTIFS.filter(function(x){ return x.unread && isImportant(x); }).length;
    }
    dot.classList.add('np-count');
    dot.classList.toggle('on', count>0 && NPX.channels.badge!==false);
    dot.textContent = count>99 ? '99+' : (count>0 ? String(count) : '');
    // расцветка по режиму
    dot.classList.toggle('np-only-imp', NPX.priority==='important');
    dot.classList.toggle('np-silent',   NPX.priority==='silent' || dndActive());
    // сама кнопка-колокол
    var btn = document.querySelector('.notif-btn');
    if(btn){
      btn.classList.toggle('np-mode-silent',    NPX.priority==='silent' || dndActive());
      btn.classList.toggle('np-mode-important', NPX.priority==='important');
      // подмена иконки колокольчика на луну при DND/silent
      var svg = btn.querySelector('svg.i use');
      if(svg){
        var want = (NPX.priority==='silent' || dndActive()) ? '#i-moon' : '#i-bell';
        if(svg.getAttribute('href') !== want) svg.setAttribute('href', want);
      }
    }
    // если бейдж отключён — скрываем полностью
    if(NPX.channels.badge===false){ dot.classList.remove('on'); dot.textContent=''; }
  };

  /* ================================================================
     РЕНДЕР ЦЕНТРА УВЕДОМЛЕНИЙ (полный override)
  ================================================================ */
  function timeMinutes(t){
    t = (t||'').trim();
    if(/только что|сейчас/i.test(t)) return 0;
    var m = t.match(/(\d+)\s*мин/); if(m) return +m[1];
    var h = t.match(/(\d+)\s*ч/);  if(h) return +h[1]*60;
    var d = t.match(/(\d+)\s*д/);  if(d) return +d[1]*1440;
    if(/вчера/i.test(t)) return 1440;
    if(/недел/i.test(t)) return 10080;
    return 99999;
  }

  function renderItem(n, small){
    var cat  = catOf(n);
    var brand = /OKO|Партнёрк|Биржа|подписчик|Академ|Систем/i.test(n.who||'');
    var badgeCls = (['heart','star','comment'].indexOf(n.ic)>-1) ? ' b-'+n.ic : '';
    var icon = brand
      ? '<span class="nt-ic">'+_I(n.ic)+'</span>'
      : '<span class="nt-ava">'+_esc((n.who||'?').trim().charAt(0).toUpperCase())+
        '<i class="nt-badge'+badgeCls+'">'+_I(n.ic)+'</i></span>';

    var acts = actionsFor(n);
    var actHtml = '';
    if(!small && acts.length){
      actHtml = '<span class="np-quick">' + acts.map(function(a){
        var svg = '<svg class="i"'+(a.rot?' style="transform:rotate('+a.rot+'deg)"':'')+'><use href="#i-'+(a.ic||'star')+'"/></svg>';
        return '<span class="np-q '+(a.cls||'')+'" data-a="'+a.k+'">'+svg+' '+_esc(a.t)+'</span>';
      }).join('') + '</span>';
    }
    var replyHtml = '';
    if(!small && acts.some(function(a){ return a.k==='reply'; })){
      replyHtml =
        '<div class="np-reply">'+
          '<textarea placeholder="Ответить '+_esc(n.who||'')+'…"></textarea>'+
          '<div class="np-reply-row">'+
            '<span class="np-q ghost" data-a="reply-cancel">Отмена</span>'+
            '<span class="np-q primary" data-a="reply-send"><svg class="i"><use href="#i-send"/></svg> Отправить</span>'+
          '</div>'+
        '</div>';
    }

    return '<button class="nt-item '+(n.unread?'unread':'')+'" type="button" data-tap="1">'+
      icon+
      '<div class="nt-b">'+
        '<span><b>'+_esc(n.who||'')+'</b> '+_esc(n.t||'')+'</span>'+
        '<small>'+_esc(n.time||'')+'</small>'+
        actHtml + replyHtml +
      '</div>'+
      (n.unread?'<i class="nt-dot"></i>':'')+
    '</button>';
  }

  function renderRow(n, extraCls){
    var html =
      '<div class="np-row '+(extraCls||'')+'" data-id="'+_esc(n.id)+'" data-cat="'+_esc(catOf(n))+'">'+
        '<div class="np-actions-r"><span class="np-act read" data-a="read">'+
          '<svg class="i"><use href="#i-check"/></svg> Прочитано</span></div>'+
        '<div class="np-actions"><span class="np-act del" data-a="del">'+
          '<svg class="i" style="transform:rotate(45deg)"><use href="#i-plus"/></svg> Отклонить</span></div>'+
        '<div class="np-swipe">'+ renderItem(n, false) +'</div>'+
      '</div>';
    return html;
  }
  function renderSubItem(n){
    return '<div class="np-row" data-id="'+_esc(n.id)+'" data-cat="'+_esc(catOf(n))+'">'+
      '<div class="np-swipe">'+ renderItem(n, true) +'</div>'+
    '</div>';
  }

  /* Ключ группировки: категория+иконка+день (только когда >=3 подряд по одной теме) */
  function stackKey(n){ return catOf(n)+':'+n.ic+':'+(n.g||''); }

  function renderAll(){
    ensureIds();
    var body = document.getElementById('notifsBody'); if(!body) return;

    // 1) фильтр по категории и приоритету
    var visible = NOTIFS.filter(function(n){
      if(NPX.filter !== 'all' && catOf(n) !== NPX.filter) return false;
      if(NPX.priority === 'important' && !isImportant(n)) return false;
      return true;
    });

    // 2) сортировка внутри групп по свежести
    var groups = {};
    var order  = [];
    visible.forEach(function(n){
      var g = n.g || 'Ранее';
      if(!groups[g]){ groups[g] = []; order.push(g); }
      groups[g].push(n);
    });
    order.forEach(function(g){
      groups[g].sort(function(a,b){ return timeMinutes(a.time) - timeMinutes(b.time); });
    });

    // 3) сборка HTML со стеками (одинаковые темы >=3 → collapsed)
    var html = '';
    order.forEach(function(g){
      var items = groups[g];
      html += '<p class="nt-group">'+_esc(g)+'</p>';
      // группировка одинаковых
      var i = 0;
      while(i < items.length){
        var n = items[i];
        var key = stackKey(n);
        // соберём подряд идущие с тем же ключом
        var run = [n]; var j = i+1;
        while(j < items.length && stackKey(items[j]) === key){ run.push(items[j]); j++; }
        if(run.length >= 3){
          // делаем группу
          var top = run[0];
          var expanded = !!NPX.expanded[key];
          // клон верхнего с обогащённым текстом
          var topClone = Object.assign({}, top);
          var kind = kindLabel(top);
          topClone.t = 'и ещё '+(run.length-1)+' — '+kind;
          // рендерим стек
          html += renderRow(topClone, 'np-stack'+(expanded?' np-expanded':'')) ;
          // и подстек — все элементы (включая верхний тоже, чтобы клик по стеку раскрывал полный список)
          var sub = run.map(renderSubItem).join('');
          html += '<div class="np-substack" data-stack="'+_esc(key)+'">'+sub+'</div>';
          // сразу после — вставим data-key на .np-row чтобы найти при клике
          i = j;
        } else {
          run.forEach(function(x){ html += renderRow(x); });
          i = j;
        }
      }
    });

    if(!html){
      html =
        '<div class="np-empty">'+
          '<svg class="i"><use href="#i-bell"/></svg>'+
          '<b>Пока тихо</b>'+
          (NPX.filter!=='all'
            ? 'В категории «'+chipLabel(NPX.filter)+'» ничего нет.<br>Попробуй фильтр «Всё».'
            : 'Тут появятся лайки, заявки, начисления и уроки.')+
        '</div>';
    }
    body.innerHTML = html;

    ensureHead();
    ensureSettings();
    bindItems();
    updateChipCounts();
    updateHint();
    if(typeof updateNotifDot==='function') updateNotifDot();
  }

  function kindLabel(n){
    if(n.ic==='heart') return 'лайков к твоим постам';
    if(n.ic==='comment') return 'комментариев';
    if(n.ic==='users') return 'новых подписчиков и заявок';
    if(n.ic==='briefcase') return 'заявок на бирже';
    if(n.ic==='money') return 'начислений партнёрки';
    if(n.ic==='star') return 'оценок и достижений';
    return 'событий';
  }
  function chipLabel(k){
    for(var i=0;i<CATS.length;i++) if(CATS[i].k===k) return CATS[i].t;
    return k;
  }

  /* ================================================================
     ШАПКА: DND-кнопка, кнопка настроек, чипы, hint
  ================================================================ */
  var CATS = [
    {k:'all',      t:'Всё'},
    {k:'chats',    t:'Чаты'},
    {k:'partner',  t:'Партнёрка'},
    {k:'games',    t:'Игры'},
    {k:'academy',  t:'Академия'},
    {k:'system',   t:'Система'}
  ];
  function ensureHead(){
    var view = document.getElementById('notifsView'); if(!view) return;
    var head = view.querySelector('.sv-head');

    // группа кнопок в шапке (DND + Настройки + «Прочитать»)
    if(head && !head.querySelector('.np-headx')){
      var done = head.querySelector('.ep-done');
      var wrap = document.createElement('div');
      wrap.className = 'np-headx';

      // DND
      var dnd = document.createElement('button');
      dnd.type='button'; dnd.className='np-hb'; dnd.title='Тихий режим';
      dnd.setAttribute('aria-label','Тихий режим');
      dnd.innerHTML = '<svg class="i"><use href="#i-moon"/></svg>';
      dnd.addEventListener('click', function(e){
        e.preventDefault(); e.stopPropagation();
        NPX.dnd.mode = (NPX.dnd.mode==='off' ? 'on' : 'off'); save();
        syncModes();
        _toast(NPX.dnd.mode==='on' ? 'Тихий режим включён' : 'Тихий режим выключен');
      });

      // Настройки-панель (шестерёнка = «⋯»)
      var cog = document.createElement('button');
      cog.type='button'; cog.className='np-hb'; cog.title='Настройки уведомлений';
      cog.setAttribute('aria-label','Настройки уведомлений');
      cog.innerHTML = '<svg class="i"><use href="#i-more"/></svg>';
      cog.addEventListener('click', function(e){
        e.preventDefault(); e.stopPropagation();
        view.classList.toggle('np-settings-on');
      });

      wrap.appendChild(dnd);
      wrap.appendChild(cog);
      if(done){ head.insertBefore(wrap, done); wrap.appendChild(done); }
      else head.appendChild(wrap);
    }

    // строка-подсказка (появляется если DND/silent активен)
    if(view && !view.querySelector('.np-modehint')){
      var hint = document.createElement('div');
      hint.className = 'np-modehint';
      view.appendChild(hint);
      var body = document.getElementById('notifsBody');
      if(body) view.insertBefore(hint, body);
    }

    // чипы фильтра
    var body = document.getElementById('notifsBody');
    if(body && !view.querySelector('.np-chips')){
      var bar = document.createElement('div');
      bar.className = 'np-chips';
      bar.innerHTML = CATS.map(function(c){
        return '<button type="button" class="np-chip '+(NPX.filter===c.k?'on':'')+
          '" data-k="'+c.k+'">'+_esc(c.t)+
          '<span class="np-chip-c" data-c="'+c.k+'">0</span></button>';
      }).join('');
      bar.addEventListener('click', function(e){
        var b = e.target.closest && e.target.closest('.np-chip'); if(!b) return;
        NPX.filter = b.dataset.k; save();
        bar.querySelectorAll('.np-chip').forEach(function(x){
          x.classList.toggle('on', x.dataset.k===NPX.filter);
        });
        renderAll();
      });
      view.insertBefore(bar, body);
    }
    syncModes();
  }

  function syncModes(){
    var view = document.getElementById('notifsView'); if(!view) return;
    var dndOn = dndActive();
    view.classList.toggle('np-mode-dnd',     dndOn);
    view.classList.toggle('np-mode-silent',  NPX.priority==='silent');
    view.classList.toggle('np-mode-imp',     NPX.priority==='important');
    var btn = view.querySelector('.np-hb[title="Тихий режим"]');
    if(btn) btn.classList.toggle('on', dndOn);
    if(typeof updateNotifDot==='function') updateNotifDot();
  }

  function updateHint(){
    var view = document.getElementById('notifsView'); if(!view) return;
    var hint = view.querySelector('.np-modehint'); if(!hint) return;
    var on = false, txt = '';
    if(NPX.priority==='silent'){ on = true; txt = 'Режим «Тишина» — уведомления только в центре, без всплывающих'; }
    else if(NPX.dnd.mode==='on'){ on = true; txt = 'Тихий режим включён — уведомления придут беззвучно'; }
    else if(NPX.dnd.mode==='schedule' && inSchedule()){
      on = true;
      txt = 'Расписание «Не беспокоить» ('+NPX.dnd.start+'–'+NPX.dnd.end+') — сейчас тихо';
    } else if(NPX.priority==='important'){
      on = true; txt = 'Показываются только важные — деньги, заявки, счета';
    }
    if(on) hint.innerHTML = '<svg class="i"><use href="#i-moon"/></svg><span>'+_esc(txt)+'</span>';
    view.classList.toggle('np-hint-on', on);
  }

  function updateChipCounts(){
    var view = document.getElementById('notifsView'); if(!view) return;
    var counts = { all:0, chats:0, partner:0, games:0, academy:0, system:0 };
    NOTIFS.forEach(function(n){
      if(!n.unread) return;
      counts.all++;
      var c = catOf(n); if(counts[c]!=null) counts[c]++;
    });
    view.querySelectorAll('.np-chip-c').forEach(function(el){
      var k = el.dataset.c;
      var c = counts[k]||0;
      el.textContent = c>99 ? '99+' : String(c);
      el.style.display = c>0 ? 'inline-block' : 'none';
    });
  }

  /* ================================================================
     ПАНЕЛЬ НАСТРОЕК (внутри экрана уведомлений)
  ================================================================ */
  function ensureSettings(){
    var view = document.getElementById('notifsView'); if(!view) return;
    if(view.querySelector('.np-settings')) { syncSettingsUI(); return; }
    var chips = view.querySelector('.np-chips'); if(!chips) return;

    var box = document.createElement('div');
    box.className = 'np-settings';
    box.innerHTML =
      '<div class="np-sec-title">Приоритет</div>'+
      '<div class="np-seg" data-seg="prio">'+
        '<button type="button" data-v="all"><svg class="i"><use href="#i-bell"/></svg> Все</button>'+
        '<button type="button" data-v="important" class="warn"><svg class="i"><use href="#i-star"/></svg> Только важные</button>'+
        '<button type="button" data-v="silent"    class="danger"><svg class="i"><use href="#i-moon"/></svg> Тишина</button>'+
      '</div>'+

      '<div class="np-sec-title">Не беспокоить</div>'+
      '<div class="np-seg" data-seg="dnd">'+
        '<button type="button" data-v="off">Выкл</button>'+
        '<button type="button" data-v="on">Всегда</button>'+
        '<button type="button" data-v="schedule">По расписанию</button>'+
      '</div>'+
      '<div class="np-dnd-row">'+
        '<div class="np-dnd-times">'+
          '<span>С</span>'+
          '<input type="time" class="np-dnd-start" value="'+_esc(NPX.dnd.start)+'">'+
          '<span>до</span>'+
          '<input type="time" class="np-dnd-end"   value="'+_esc(NPX.dnd.end)+'">'+
        '</div>'+
      '</div>'+

      '<div class="np-sec-title">Каналы доставки</div>'+
      '<div class="np-toggles">'+
        toggleRow('sound',   'volume',    'Звук уведомлений',   'Короткий сигнал при новом событии') +
        toggleRow('vibro',   'mic',       'Вибрация',           'Вибро на мобильных, где поддерживается') +
        toggleRow('preview', 'eye',       'Показывать превью',  'Текст уведомления в живом всплывании') +
        toggleRow('badge',   'bell',      'Значок с цифрой',    'Бейдж непрочитанных на иконке колокольчика') +
      '</div>';

    view.insertBefore(box, chips.nextSibling);

    // segment controls
    box.querySelectorAll('.np-seg').forEach(function(seg){
      seg.addEventListener('click', function(e){
        var b = e.target.closest && e.target.closest('button'); if(!b) return;
        var kind = seg.dataset.seg;
        var v = b.dataset.v;
        if(kind==='prio'){ NPX.priority = v; }
        else if(kind==='dnd'){ NPX.dnd.mode = v; }
        save(); syncSettingsUI(); syncModes(); updateHint();
        renderAll();
      });
    });
    // times
    box.querySelector('.np-dnd-start').addEventListener('change', function(e){
      NPX.dnd.start = e.target.value || '22:00'; save(); syncModes(); updateHint();
    });
    box.querySelector('.np-dnd-end').addEventListener('change', function(e){
      NPX.dnd.end = e.target.value || '08:00'; save(); syncModes(); updateHint();
    });
    // toggles
    box.querySelectorAll('.np-sw').forEach(function(sw){
      sw.addEventListener('click', function(){
        var k = sw.dataset.k;
        NPX.channels[k] = !NPX.channels[k]; save();
        sw.classList.toggle('on', !!NPX.channels[k]);
        if(k==='badge' && typeof updateNotifDot==='function') updateNotifDot();
      });
    });

    syncSettingsUI();
  }
  function toggleRow(k, ic, title, sub){
    var on = NPX.channels[k] !== false;
    return ''+
      '<div class="np-toggle">'+
        '<div class="np-toggle-l">'+
          '<svg class="i"><use href="#i-'+_esc(ic)+'"/></svg>'+
          '<div class="np-toggle-t"><b>'+_esc(title)+'</b><small>'+_esc(sub)+'</small></div>'+
        '</div>'+
        '<div class="np-sw '+(on?'on':'')+'" data-k="'+_esc(k)+'" role="switch" aria-checked="'+on+'"></div>'+
      '</div>';
  }
  function syncSettingsUI(){
    var box = document.querySelector('.np-settings'); if(!box) return;
    box.querySelectorAll('.np-seg').forEach(function(seg){
      var kind = seg.dataset.seg;
      var v = kind==='prio' ? NPX.priority : NPX.dnd.mode;
      seg.querySelectorAll('button').forEach(function(b){
        b.classList.toggle('on', b.dataset.v===v);
      });
    });
    var s = box.querySelector('.np-dnd-start'); if(s) s.value = NPX.dnd.start;
    var e = box.querySelector('.np-dnd-end');   if(e) e.value = NPX.dnd.end;
    box.querySelectorAll('.np-sw').forEach(function(sw){
      sw.classList.toggle('on', NPX.channels[sw.dataset.k] !== false);
    });
  }

  /* ================================================================
     БИНДЫ: клики (действия / стек), свайпы
  ================================================================ */
  function findNotifById(id){
    for(var i=0;i<NOTIFS.length;i++) if(NOTIFS[i].id===id) return {n:NOTIFS[i], i:i};
    return null;
  }

  function bindItems(){
    var body = document.getElementById('notifsBody'); if(!body) return;

    // Клики по строкам / действиям / стеку
    body.addEventListener('click', bodyClick, false);

    // Свайпы
    body.querySelectorAll('.np-row').forEach(function(row){
      var swipe = row.querySelector('.np-swipe'); if(!swipe) return;
      if(swipe.__np_bound) return; swipe.__np_bound = true;
      var sx=0, sy=0, dx=0, live=false, lock=null, startOpen='';
      swipe.addEventListener('touchstart', function(e){
        var t = e.touches[0]; sx=t.clientX; sy=t.clientY; dx=0; live=true; lock=null;
        startOpen = row.classList.contains('np-open') ? 'l' : row.classList.contains('np-open-r') ? 'r' : '';
        // закрыть остальные
        document.querySelectorAll('.np-row.np-open,.np-row.np-open-r').forEach(function(r){
          if(r!==row){ r.classList.remove('np-open'); r.classList.remove('np-open-r'); }
        });
      }, {passive:true});
      swipe.addEventListener('touchmove', function(e){
        if(!live) return;
        var t = e.touches[0]; var cx = t.clientX - sx, cy = t.clientY - sy;
        if(lock===null){
          if(Math.abs(cx) < 6 && Math.abs(cy) < 6) return;
          lock = Math.abs(cx) > Math.abs(cy)*1.4 ? 'x' : 'y';
        }
        if(lock!=='x'){ live=false; swipe.style.transform=''; return; }
        var base = 0;
        if(startOpen==='l') base = -140;
        else if(startOpen==='r') base = 140;
        dx = Math.max(-160, Math.min(160, cx + base));
        swipe.style.transform = 'translateX('+dx+'px)';
      }, {passive:true});
      swipe.addEventListener('touchend', function(){
        if(!live) return; live = false;
        swipe.style.transform = '';
        row.classList.remove('np-open'); row.classList.remove('np-open-r');
        if(dx < -70) row.classList.add('np-open');
        else if(dx > 70) row.classList.add('np-open-r');
      });
    });
  }

  function bodyClick(e){
    // если открыт свайп — первый клик закрывает
    var open = document.querySelector('.np-row.np-open, .np-row.np-open-r');
    if(open && !e.target.closest('.np-actions, .np-actions-r, .np-q, textarea, .np-reply-row')){
      var isSame = e.target.closest && e.target.closest('.np-row') === open;
      if(isSame){
        e.preventDefault(); e.stopPropagation();
        open.classList.remove('np-open'); open.classList.remove('np-open-r');
        return;
      }
    }
    var row = e.target.closest && e.target.closest('.np-row'); if(!row) return;

    // клик по свайп-action
    var swAct = e.target.closest && e.target.closest('.np-act');
    if(swAct){
      e.preventDefault(); e.stopPropagation();
      var id = row.dataset.id, r = findNotifById(id);
      if(!r){ row.classList.remove('np-open'); row.classList.remove('np-open-r'); return; }
      if(swAct.dataset.a === 'read'){
        r.n.unread = false;
        window.renderNotifs(); _toast('Отмечено прочитанным');
      } else if(swAct.dataset.a === 'del'){
        NOTIFS.splice(r.i, 1);
        window.renderNotifs(); _toast('Уведомление удалено');
      }
      return;
    }

    // inline-action (np-q)
    var q = e.target.closest && e.target.closest('.np-q');
    if(q){
      e.preventDefault(); e.stopPropagation();
      var id2 = row.dataset.id, rr = findNotifById(id2); if(!rr) return;
      var a = q.dataset.a;
      if(a === 'reply'){
        row.classList.add('np-reply-on');
        var ta = row.querySelector('.np-reply textarea');
        if(ta){ setTimeout(function(){ ta.focus(); }, 60); }
        return;
      }
      if(a === 'reply-cancel'){ row.classList.remove('np-reply-on'); return; }
      if(a === 'reply-send'){
        var text = (row.querySelector('.np-reply textarea')||{}).value || '';
        if(!text.trim()){ _toast('Пустое сообщение'); return; }
        row.classList.remove('np-reply-on');
        rr.n.unread = false;
        window.renderNotifs(); _toast('Ответ отправлен');
        return;
      }
      if(a === 'accept'){
        rr.n.unread = false;
        window.renderNotifs(); _toast('Заявка принята');
        return;
      }
      if(a === 'reject'){
        NOTIFS.splice(rr.i, 1);
        window.renderNotifs(); _toast('Заявка отклонена');
        return;
      }
      if(a === 'pay'){
        rr.n.unread = false;
        if(typeof closeNotifs==='function') closeNotifs();
        if(typeof showTab==='function') showTab('wallet');
        _toast('Открываем оплату');
        return;
      }
      if(a === 'openPost'){
        rr.n.unread = false;
        if(typeof closeNotifs==='function') closeNotifs();
        if(typeof showTab==='function') showTab('feed');
        return;
      }
      if(a === 'open'){
        rr.n.unread = false;
        if(typeof closeNotifs==='function') closeNotifs();
        if(typeof rr.n.act === 'function') rr.n.act();
        return;
      }
    }

    // тап по стеку → раскрыть/свернуть
    if(row.classList.contains('np-stack')){
      var sub = row.nextElementSibling;
      if(sub && sub.classList.contains('np-substack')){
        var key = sub.dataset.stack;
        NPX.expanded[key] = !row.classList.contains('np-expanded');
        save();
        row.classList.toggle('np-expanded', !!NPX.expanded[key]);
        return;
      }
    }

    // обычный тап по кнопке уведомления
    var btn = e.target.closest && e.target.closest('.nt-item');
    if(btn){
      var id3 = row.dataset.id, rrr = findNotifById(id3); if(!rrr) return;
      // если стек — то это уже обработано выше как раскрытие
      if(row.classList.contains('np-stack')) return;
      rrr.n.unread = false;
      if(typeof closeNotifs==='function') closeNotifs();
      if(typeof rrr.n.act === 'function') rrr.n.act();
    }
  }

  /* ================================================================
     ОВЕРРАЙД window.renderNotifs / chain: openNotifs / closeNotifs
  ================================================================ */
  seedDemo();
  window.renderNotifs = renderAll;

  var _open = window.openNotifs;
  window.openNotifs = function(){
    _open.apply(this, arguments);
    ensureHead();
    ensureSettings();
    liveStart();
    if(typeof updateNotifDot==='function') updateNotifDot();
  };
  var _close = window.closeNotifs;
  window.closeNotifs = function(){
    _close.apply(this, arguments);
    liveStop();
    document.querySelectorAll('.np-row.np-open, .np-row.np-open-r').forEach(function(r){
      r.classList.remove('np-open'); r.classList.remove('np-open-r');
    });
  };

  // notifTap — базовый ядра всё ещё вызывается по HTML-атрибуту, но
  // наши элементы больше не имеют onclick="notifTap(i)", так что chain
  // остаётся для совместимости.
  var _tap = window.notifTap;
  window.notifTap = function(){
    var openRow = document.querySelector('.np-row.np-open, .np-row.np-open-r');
    if(openRow){
      openRow.classList.remove('np-open');
      openRow.classList.remove('np-open-r');
      return;
    }
    _tap.apply(this, arguments);
  };

  /* ================================================================
     GLOBAL LIVE-FEED «стука в дверь» (даже если центр закрыт)
     — реалистично показываем всплывающие сверху раз в 45–90 сек
  ================================================================ */
  var globalT = null;
  function globalTick(){
    /* Пул выдуманных уведомлений вычищен 09.08 — «живые» события приходят
       только из Realtime. Пока пула нет, таймер просто ничего не показывает
       (раньше здесь падало обращение к undefined). */
    if(!LIVE_POOL.length){ globalT = setTimeout(globalTick, 60000); return; }
    if(!isSilent() && document.visibilityState !== 'hidden'){
      var p = LIVE_POOL[Math.floor(Math.random()*LIVE_POOL.length)];
      var n = {id:nextId(), ic:p.ic, who:p.who, t:p.t, time:'только что', g:'Сегодня', unread:true, act:function(){}};
      n.cat = catOf(n);
      NOTIFS.unshift(n);
      if(typeof updateNotifDot==='function') updateNotifDot();
      pushLive(n);
      // если центр открыт — обновим список
      var view = document.getElementById('notifsView');
      if(view && view.classList.contains('open')) window.renderNotifs();
    }
    globalT = setTimeout(globalTick, 45000 + Math.random()*45000);
  }
  function startGlobal(){
    if(globalT) return;
    // первый показ — через 12–20 сек после старта, чтобы юзер успел освоиться
    globalT = setTimeout(globalTick, 12000 + Math.random()*8000);
  }

  /* ================================================================
     Периодический ре-чек расписания DND (раз в 60 сек)
  ================================================================ */
  setInterval(function(){
    syncModes();
    updateHint();
    if(typeof updateNotifDot==='function') updateNotifDot();
  }, 60000);

  /* ================================================================
     WEB PUSH (VAPID) — реальные браузер-native уведомления от OKO.
     Экспортируем 3 функции в window: npAskPushPermission / npSubscribeToPush /
     npUnsubscribeFromPush. Стейт хранится в localStorage (npPush).
  ================================================================ */
  var NP_PUSH_LS = 'oko-np-push-v1';
  var NP_PUSH_PROMPT_SESSIONS = 3;         /* показать плашку после N активных сессий */
  var NP_PUSH_SESSION_MIN_MS = 45 * 1000;  /* «активная сессия» = ≥ 45 сек фокуса */
  var NP_PUSH_API_BASE = '';               /* '' → относительный /api.php (тот же хост); при желании — 'https://okoteam.top' */
  /* Публичный VAPID-ключ приложения OKO. Приватный ключ живёт только на VPS в config.php. */
  var NP_VAPID_PUBLIC = 'BEH8Gaj3aU85U-h9rrMVv07KU7-sWP1BoWQd-fvu-Oc6zJWx-pTuumELO-_AsGab26NI9Qk1dk_-dwjXt7s8Zhg';

  function npPushState(){
    try{
      var raw = JSON.parse(localStorage.getItem(NP_PUSH_LS));
      if(!raw || typeof raw!=='object') raw = {};
      raw.sessions   = raw.sessions   || 0;    /* число «активных» сессий */
      raw.asked      = !!raw.asked;            /* уже показывали красивую плашку */
      raw.declined   = !!raw.declined;         /* нажал «Позже» — не давить */
      raw.subscribed = !!raw.subscribed;       /* подписка отправлена на сервер */
      raw.endpoint   = raw.endpoint || '';
      return raw;
    }catch(e){ return {sessions:0,asked:false,declined:false,subscribed:false,endpoint:''}; }
  }
  function npPushSave(s){ try{ localStorage.setItem(NP_PUSH_LS, JSON.stringify(s)); }catch(e){} }

  function npPushSupported(){
    return typeof window!=='undefined' && 'serviceWorker' in navigator &&
           'PushManager' in window && 'Notification' in window &&
           location.protocol === 'https:';
  }

  /* Base64URL → Uint8Array (VAPID applicationServerKey) */
  function npB64UrlToU8(s){
    var pad = '='.repeat((4 - s.length % 4) % 4);
    var base = (s + pad).replace(/-/g,'+').replace(/_/g,'/');
    var raw = atob(base); var u8 = new Uint8Array(raw.length);
    for(var i=0;i<raw.length;i++) u8[i] = raw.charCodeAt(i);
    return u8;
  }
  /* ArrayBuffer → base64 (для отправки подписки на сервер) */
  function npAbToB64(buf){
    if(!buf) return '';
    var bytes = new Uint8Array(buf), s = '';
    for(var i=0;i<bytes.length;i++) s += String.fromCharCode(bytes[i]);
    return btoa(s);
  }

  function npGetEmail(){
    /* попытаемся достать email пользователя, чтобы бэкенд знал, кому слать */
    try{
      if(typeof RG2 !== 'undefined' && RG2.contact && /@/.test(RG2.contact)) return RG2.contact;
      var a = localStorage.getItem('oko-auth-contact');
      if(a && /@/.test(a)) return a;
    }catch(e){}
    return '';
  }

  function npApi(action, payload){
    var url = (NP_PUSH_API_BASE || '') + '/api.php?action=' + encodeURIComponent(action);
    return fetch(url, {
      method: 'POST',
      headers: {'Content-Type':'application/json'},
      credentials: 'omit',
      body: JSON.stringify(payload||{})
    }).then(function(r){ return r.json().catch(function(){ return {ok:false}; }); });
  }

  /* Показать красивую плашку «Разреши уведомления от OKO».
     Обёртка над showPopup из core-ext (см. showPopup в base). */
  window.npAskPushPermission = function(force){
    var s = npPushState();
    if(!npPushSupported()) return;
    if(!force && (s.asked || s.subscribed)) return;
    if(!force && s.declined) return;
    if(typeof Notification==='undefined') return;
    if(Notification.permission === 'granted'){ npSubscribeToPush(); return; }
    if(Notification.permission === 'denied'){ s.declined = true; npPushSave(s); return; }
    if(typeof showPopup !== 'function') return;

    s.asked = true; npPushSave(s);
    showPopup({
      ico: 'bell',
      title: 'Не пропусти лидов и сообщения',
      body:
        '<div style="text-align:left;font-size:13.5px;line-height:1.55">'+
        'Разреши OKO присылать уведомления прямо в браузер:'+
        '<div style="margin:12px 0;display:flex;flex-direction:column;gap:6px;color:var(--dim);font-size:12.5px">'+
          '<div><svg class="i" style="width:14px;height:14px;color:var(--accent);vertical-align:-2px"><use href="#i-money"/></svg>&nbsp; Новый лид или оплата'+ ' — сразу узнаешь</div>'+
          '<div><svg class="i" style="width:14px;height:14px;color:var(--accent);vertical-align:-2px"><use href="#i-chat"/></svg>&nbsp; Ответ клиента в чате</div>'+
          '<div><svg class="i" style="width:14px;height:14px;color:var(--accent);vertical-align:-2px"><use href="#i-star"/></svg>&nbsp; Ролик залетел или урок открыт</div>'+
        '</div>'+
        '<small style="color:var(--dim)">В любой момент выключишь в настройках уведомлений.</small>'+
        '</div>',
      actions: [
        { label: 'Разрешить', onclick: function(){
            Notification.requestPermission().then(function(perm){
              if(perm === 'granted'){ npSubscribeToPush(); }
              else { var ss = npPushState(); ss.declined = true; npPushSave(ss); _toast('Уведомления не разрешены'); }
            });
          } },
        { label: 'Позже', ghost: true, onclick: function(){
            var ss = npPushState(); ss.declined = true; npPushSave(ss);
          } }
      ]
    });
  };

  /* Оформить web-push-подписку и отправить на бэкенд. */
  window.npSubscribeToPush = function(){
    if(!npPushSupported()) return Promise.resolve({ok:false, error:'unsupported'});
    if(Notification.permission !== 'granted'){
      return Notification.requestPermission().then(function(p){
        if(p !== 'granted') return {ok:false, error:'denied'};
        return doSubscribe();
      });
    }
    return doSubscribe();
    function doSubscribe(){
      return navigator.serviceWorker.ready.then(function(reg){
        return reg.pushManager.getSubscription().then(function(existing){
          if(existing) return existing;
          return reg.pushManager.subscribe({
            userVisibleOnly: true,
            applicationServerKey: npB64UrlToU8(NP_VAPID_PUBLIC)
          });
        });
      }).then(function(sub){
        var j = sub.toJSON ? sub.toJSON() : {
          endpoint: sub.endpoint,
          keys: {
            p256dh: npAbToB64(sub.getKey && sub.getKey('p256dh')),
            auth:   npAbToB64(sub.getKey && sub.getKey('auth'))
          }
        };
        return npApi('push_subscribe', {
          subscription: j,
          email: npGetEmail(),
          ua: navigator.userAgent || ''
        }).then(function(res){
          var s = npPushState();
          s.subscribed = !!(res && res.ok);
          s.endpoint = j.endpoint || '';
          npPushSave(s);
          if(s.subscribed){
            _toast('Уведомления OKO включены');
            syncSettingsUI();
          }
          return res;
        });
      }).catch(function(e){
        _toast('Не удалось подключить push');
        return {ok:false, error:String(e&&e.message||e)};
      });
    }
  };

  /* Отключить web-push (у браузера + на сервере). */
  window.npUnsubscribeFromPush = function(){
    if(!npPushSupported()) return Promise.resolve({ok:true});
    return navigator.serviceWorker.ready.then(function(reg){
      return reg.pushManager.getSubscription().then(function(sub){
        if(!sub) return {ok:true};
        var ep = sub.endpoint;
        return sub.unsubscribe().then(function(){
          return npApi('push_unsubscribe', { endpoint: ep, email: npGetEmail() });
        }).then(function(res){
          var s = npPushState();
          s.subscribed = false; s.endpoint = '';
          npPushSave(s);
          _toast('Push-уведомления выключены');
          syncSettingsUI();
          return res || {ok:true};
        });
      });
    }).catch(function(){ return {ok:false}; });
  };

  /* Слушаем клики по push-уведомлениям и pushsubscriptionchange из SW */
  if('serviceWorker' in navigator){
    navigator.serviceWorker.addEventListener('message', function(ev){
      var d = ev.data || {};
      if(d.type === 'oko-push-click'){
        try{
          var url = d.url || '/';
          if(url && url !== '/' && typeof showTab === 'function'){
            /* url может быть локальным маршрутом вида '/#tab=chats' — просто фокус, страница сама разберётся */
            if(/#tab=/.test(url)){ var t = url.split('tab=')[1]||''; if(t) showTab(t); }
          }
        }catch(e){}
      }
      if(d.type === 'oko-push-resubscribe'){
        var s = npPushState();
        if(s.subscribed) { try{ window.npSubscribeToPush(); }catch(e){} }
      }
    });
  }

  /* Учёт активных сессий: +1 после NP_PUSH_SESSION_MIN_MS в фокусе,
     через 3 сессии — показываем плашку. */
  (function trackSessions(){
    var s = npPushState();
    if(!npPushSupported()) return;
    if(s.asked || s.subscribed || s.declined) return;

    var counted = false, timer = null;
    function start(){
      if(counted || document.visibilityState==='hidden') return;
      timer = setTimeout(function(){
        counted = true;
        var ss = npPushState();
        ss.sessions = (ss.sessions||0) + 1;
        npPushSave(ss);
        if(ss.sessions >= NP_PUSH_PROMPT_SESSIONS){
          /* небольшая задержка, чтобы плашка не перебивала стартовые попапы */
          setTimeout(function(){ try{ window.npAskPushPermission(false); }catch(e){} }, 6000);
        }
      }, NP_PUSH_SESSION_MIN_MS);
    }
    function stop(){ if(timer){ clearTimeout(timer); timer = null; } }
    document.addEventListener('visibilitychange', function(){
      if(document.visibilityState==='hidden') stop();
      else start();
    });
    start();
  })();

  /* Автоподхват тумблера в панели настроек уведомлений: если пользователь
     разрешил push раньше (Notification.permission === 'granted'), но подписки
     нет (например, VAPID-ключ обновился) — попробуем восстановить тихо. */
  if(npPushSupported() && Notification.permission === 'granted'){
    setTimeout(function(){
      var s = npPushState();
      if(!s.subscribed){ try{ window.npSubscribeToPush(); }catch(e){} }
    }, 3000);
  }

  /* ================================================================
     ТУМБЛЕР «Push-уведомления OKO» в панели настроек модуля.
     Врезаем в конец .np-toggles после первой отрисовки настроек.
  ================================================================ */
  var _ensureSettings = ensureSettings;
  ensureSettings = function(){
    _ensureSettings();
    var box = document.querySelector('.np-settings'); if(!box) return;
    if(box.querySelector('.np-push-row')) { syncPushRow(); return; }
    var toggles = box.querySelector('.np-toggles'); if(!toggles) return;

    var support = npPushSupported();
    var perm = (typeof Notification !== 'undefined') ? Notification.permission : 'default';
    var s = npPushState();
    var on = s.subscribed && perm === 'granted';

    var row = document.createElement('div');
    row.className = 'np-toggle np-push-row';
    row.innerHTML =
      '<div class="np-toggle-l">'+
        '<svg class="i"><use href="#i-bell"/></svg>'+
        '<div class="np-toggle-t">'+
          '<b>Push-уведомления OKO</b>'+
          '<small class="np-push-sub">'+ pushSubText(support, perm, on) +'</small>'+
        '</div>'+
      '</div>'+
      '<div class="np-sw '+(on?'on':'')+'" role="switch" aria-checked="'+on+'"></div>';

    /* Тумблер: on → subscribe, off → unsubscribe */
    row.querySelector('.np-sw').addEventListener('click', function(){
      if(!npPushSupported()){ _toast('Твой браузер не поддерживает push'); return; }
      var perm2 = Notification.permission;
      var st = npPushState();
      var isOn = st.subscribed && perm2 === 'granted';
      if(isOn){
        window.npUnsubscribeFromPush();
      } else {
        if(perm2 === 'denied'){
          _toast('Push заблокирован в настройках браузера — включи вручную');
          return;
        }
        if(perm2 === 'default'){
          /* красивая плашка, force=true — игнорим "declined/asked" */
          window.npAskPushPermission(true);
        } else {
          window.npSubscribeToPush();
        }
      }
    });

    toggles.appendChild(row);
    syncPushRow();
  };

  function pushSubText(support, perm, on){
    if(!support) return 'Не поддерживается этим браузером';
    if(perm === 'denied') return 'Заблокировано в настройках браузера';
    if(on) return 'Придут даже если приложение закрыто';
    if(perm === 'granted') return 'Разрешены, но подписка не создана';
    return 'Лиды, оплаты, чаты — прямо на экран';
  }
  function syncPushRow(){
    var row = document.querySelector('.np-push-row'); if(!row) return;
    var support = npPushSupported();
    var perm = (typeof Notification !== 'undefined') ? Notification.permission : 'default';
    var s = npPushState();
    var on = s.subscribed && perm === 'granted';
    var sw = row.querySelector('.np-sw');
    if(sw){
      sw.classList.toggle('on', on);
      sw.setAttribute('aria-checked', on ? 'true' : 'false');
    }
    var sub = row.querySelector('.np-push-sub');
    if(sub) sub.textContent = pushSubText(support, perm, on);
  }

  /* ================================================================
     СТАРТ
  ================================================================ */
  ensureIds();
  // проставим bell-иконку/бейдж сразу после загрузки base.html
  if(typeof updateNotifDot==='function') updateNotifDot();
  // стек-контейнер для живых toast
  ensureStack();
  // запуск глобального демо-потока
  startGlobal();
})();

/* ===== module: tour ===== */
/* ===== TOUR: сторис Даниэля + интерактивный тур (префикс tr-) =====
   Первый заход после регистрации:
     1) STORIES — Instagram-style сторис от Даниэля (5 карточек: продукт,
        тарифы, партнёрка, академия, штаб). Тап по правой половине — вперёд,
        по левой — назад, удержание — пауза, свайп вниз / крест — закрыть.
     2) TOUR — интерактивный тур: спотлайт вокруг реального элемента
        (дыра box-shadow по getBoundingClientRect), карточка с текстом +
        стрелка, точки-прогресс, Далее/Пропустить.
     3) FINAL — попап «Готово! 2 500 ₽ уже на счёте — попробуй продвижение».
   Единый ключ persistent-state: `oko-tour-done` (сохраняем back-compat со
   старым ключом `oko-tour`). Строгий одноразовый гейт: если ключ выставлен,
   ни сторис, ни тур автоматически больше не запускаются. */

const TR_KEY = 'oko-tour-done';
const TR_KEY_LEGACY = 'oko-tour';
const TR_STORIES_KEY = 'oko-stories-seen';

/* ---------- двуязычие тура: EN, когда язык интерфейса английский ---------- */
/* берём язык из i18n-ядра (LANG); helper — на каждый видимый текст тура */
function trT(ru, en){ return (typeof LANG !== 'undefined' && LANG === 'en') ? en : ru; }

/* ---------- 5 сторис Даниэля (первый заход) ---------- */
/* Каждая история двуязычна. Визуал строим сами (без внешних картинок) —
   бренд-градиенты, тонкая грид-сетка, крупная типографика Bebas Neue,
   иконка бренда — только SVG из спрайта (#i-*). */
const TR_STORIES = [
  {key:'product', ico:'logo', tone:'lime',
   tag:{ru:'Продукт', en:'Product'},
   title:{ru:'OKO — экосистема, где всё в одном', en:'OKO — everything in one place'},
   body:{ru:'Мессенджер, лента, биржа, кошелёк, академия и партнёрка. Один аккаунт — целый мир для роста и заработка.',
         en:'Messenger, feed, marketplace, wallet, academy and partner program. One account — a whole ecosystem for growth and income.'},
   meta:{ru:'Даниэль · основатель OKO', en:'Daniel · founder of OKO'}},
  {key:'plans', ico:'crown', tone:'dark',
   tag:{ru:'Тарифы', en:'Plans'},
   title:{ru:'Три плана — под любые задачи', en:'Three plans — for every need'},
   body:{ru:'Free для старта, PRO для роста и BUSINESS для команд. Первые 2 500 ₽ на балансе — тратятся на любые тарифы и продвижение.',
         en:'Free to start, PRO to grow and BUSINESS for teams. First 2,500 ₽ on your balance covers any plan or promotion.'},
   meta:{ru:'Даниэль · тарифы OKO', en:'Daniel · OKO plans'}},
  {key:'partner', ico:'heart', tone:'lime',
   tag:{ru:'Партнёрка', en:'Partners'},
   title:{ru:'Приводи людей — расти вместе', en:'Bring people in — grow together'},
   body:{ru:'До 30% с оплат приглашённых, бессрочно. Промо-материалы, QR, дип-линки и прозрачная история начислений — всё в кабинете.',
         en:'Up to 30% of your invitees’ payments, forever. Promo assets, QR, deep links and a transparent payout log — all in one dashboard.'},
   meta:{ru:'Даниэль · партнёрская программа', en:'Daniel · partner program'}},
  {key:'academy', ico:'star', tone:'dark',
   tag:{ru:'Академия', en:'Academy'},
   title:{ru:'Учись и получай сертификат', en:'Study and get certified'},
   body:{ru:'Полноформатные курсы: видео, слайды, тест и практика. За каждое направление — официальный сертификат OKO с печатью и подписью.',
         en:'Full-format courses: video, slides, tests and practice. Every track ends with an official OKO certificate — stamp and signature included.'},
   meta:{ru:'Даниэль · академия OKO', en:'Daniel · OKO academy'}},
  {key:'hq', ico:'compass', tone:'lime',
   tag:{ru:'Штаб', en:'HQ'},
   title:{ru:'Штаб — твой контроль', en:'HQ — your control room'},
   body:{ru:'Профиль, статистика, темы, конфиденциальность и уведомления. Начни отсюда — и вернись сюда, чтобы включить повтор сторис и тура.',
         en:'Profile, stats, themes, privacy and notifications. Start here — and come back to replay stories or the tour anytime.'},
   meta:{ru:'Даниэль · штаб OKO', en:'Daniel · OKO HQ'}},
];

/* Каждый шаг двуязычен: title/text — {ru,en}. Рендер выбирает язык по LANG. */
const TR_STEPS = [
  {tab:'feed', sel:'nav#tabs', ico:'compass',
   title:{ru:'Нижняя навигация', en:'Bottom navigation'},
   text:{ru:'Пять разделов всегда под рукой: Лента, Чаты, Мини-аппы, Кошелёк и Профиль. Переключайся одним касанием — всё OKO живёт здесь.',
         en:'Five sections always within reach: Feed, Chats, Hub, Wallet and Profile. Switch with a single tap — all of OKO lives here.'},
   pad:4, round:'14px'},
  {tab:'feed', sel:'#screen-feed .feed-tabs', ico:'feed',
   title:{ru:'Лента и алгоритмы', en:'Feed & algorithms'},
   text:{ru:'«Подписки» — посты твоих авторов, «Рекомендации» — умный подбор по интересам. Чем больше реакций ставишь, тем точнее лента подстраивается под тебя.',
         en:'“Following” shows posts from your authors, “For you” is a smart pick by interest. The more you react, the better the feed tunes to you.'},
   pad:6, round:'16px'},
  {tab:'wallet', sel:'#screen-wallet .wal-hero', ico:'money',
   title:{ru:'Кошелёк — баланс на всё', en:'Wallet — one balance for everything'},
   text:{ru:'Один лицевой счёт на всё: тарифы, продвижение, биржа с эскроу-защитой, игры и переводы в чатах. Пополняй картой или криптой, выводи заработанное.',
         en:'One account for it all: plans, promotion, the escrow-protected marketplace, games and chat transfers. Top up by card or crypto, withdraw what you earn.'},
   pad:6, round:'18px', clamp:true},
  {tab:'mini', sel:'#maGrid .svc-grid', ico:'bolt',
   title:{ru:'Мини-аппы', en:'Mini-apps'},
   text:{ru:'Сервисы как приложения — без установки: Академия с сертификатами, Игры, Биржа услуг, реклама и проверка видео. Всё открывается в один тап.',
         en:'Services like apps — no install: Academy with certificates, Games, the services marketplace, ads and video checks. Everything opens in one tap.'},
   pad:6, round:'18px', clamp:true},
  {tab:'feed', sel:'#screen-feed .fab', ico:'plus',
   title:{ru:'Создавай посты', en:'Create posts'},
   text:{ru:'Лаймовая кнопка — новый пост: текст, фото, видео или опрос. Опубликуй и сразу продвинь его за счёт стартового бонуса на счёте.',
         en:'The lime button starts a new post: text, photo, video or poll. Publish and promote it right away with your starter bonus.'},
   pad:8, round:'50%'},
  {tab:'profile', sel:'#screen-profile .profile-top', ico:'user',
   title:{ru:'Профиль и настройки', en:'Profile & settings'},
   text:{ru:'Твоя страница: тариф, статистика и достижения. Ниже — темы, уведомления, конфиденциальность и строка «Тур по OKO», если захочешь повторить подсказки.',
         en:'Your page: plan, stats and achievements. Below — themes, notifications, privacy and the “OKO tour” row if you want to replay these tips.'},
   pad:6, round:'18px'},
];
/* безопасно достать текст шага в текущем языке (совместимо со строкой, если вдруг) */
function trStepStr(v){ return (v && typeof v === 'object') ? trT(v.ru, v.en) : (v || ''); }

let trIdx = 0, trOpen = false, trFirstPlace = true, trRzT = null;

/* --- persistent-state: единый ключ «тур пройден» + back-compat со старым --- */
function trSeen(){
  try{
    if(localStorage.getItem(TR_KEY)) return true;
    /* миграция старого ключа: если пользователь уже видел тур раньше — уважаем */
    if(localStorage.getItem(TR_KEY_LEGACY)){
      try{ localStorage.setItem(TR_KEY, '1'); }catch(e){}
      return true;
    }
    return false;
  }catch(e){ return true; }
}
function trMark(){ try{ localStorage.setItem(TR_KEY, '1'); }catch(e){} }
function trStoriesSeen(){ try{ return !!localStorage.getItem(TR_STORIES_KEY); }catch(e){ return true; } }
function trStoriesMark(){ try{ localStorage.setItem(TR_STORIES_KEY, '1'); }catch(e){} }

/* ============================================================
   INSTAGRAM STORIES: сторис Даниэля (5 карточек, первый заход)
   ============================================================ */
let TS = { open:false, idx:0, timer:null, start:0, dur:5400, pausedAt:0, pausedFor:0, holdT:null,
           touchX:0, touchY:0, touchT:0 };

function trStoriesBuild(){
  if(document.getElementById('trStories')) return;
  const el = document.createElement('div');
  el.id = 'trStories';
  el.setAttribute('role','dialog');
  el.setAttribute('aria-label', trT('Сторис OKO','OKO stories'));
  el.innerHTML = `
    <div class="ts-bars" id="tsBars"></div>
    <div class="ts-head">
      <div class="ts-author">
        <span class="ts-ava"><svg class="i"><use href="#i-logo"/></svg></span>
        <div class="ts-author-b">
          <b>Даниэль</b>
          <small id="tsAuthorSub">${trT('основатель OKO','founder of OKO')}</small>
        </div>
      </div>
      <button class="ts-close" id="tsClose" aria-label="${trT('Закрыть','Close')}" onclick="trStoriesClose()">
        <svg class="i" style="transform:rotate(45deg)"><use href="#i-plus"/></svg>
      </button>
    </div>
    <div class="ts-stage" id="tsStage"></div>
    <div class="ts-nav">
      <button class="ts-hit ts-hit-l" aria-label="${trT('Назад','Previous')}" onclick="trStoriesPrev()"></button>
      <button class="ts-hit ts-hit-r" aria-label="${trT('Дальше','Next')}" onclick="trStoriesNext()"></button>
    </div>
    <div class="ts-cta" id="tsCta">
      <button class="btn" onclick="trStoriesFinish()"><span id="tsCtaLabel">${trT('Пропустить сторис','Skip stories')}</span></button>
    </div>`;
  document.body.appendChild(el);

  /* удержание — пауза (mouse + touch) */
  el.addEventListener('mousedown', trStoriesHoldStart);
  el.addEventListener('mouseup',   trStoriesHoldEnd);
  el.addEventListener('mouseleave',trStoriesHoldEnd);
  el.addEventListener('touchstart', function(e){
    const t = e.touches[0]; if(t){ TS.touchX = t.clientX; TS.touchY = t.clientY; TS.touchT = Date.now(); }
    trStoriesHoldStart();
  }, {passive:true});
  el.addEventListener('touchend', function(e){
    trStoriesHoldEnd();
    const t = e.changedTouches[0]; if(!t) return;
    const dx = t.clientX - TS.touchX, dy = t.clientY - TS.touchY, dt = Date.now() - TS.touchT;
    /* свайп вниз > 90px за 500 мс — закрыть сторис */
    if(dy > 90 && Math.abs(dy) > Math.abs(dx) * 1.2 && dt < 500) trStoriesClose();
  }, {passive:true});
}

function trStoriesStart(cb){
  if(TS.open) return;
  trStoriesBuild();
  TS.open = true; TS.idx = 0; TS.pausedFor = 0; TS.pausedAt = 0;
  TS._onDone = cb || null;
  const ov = document.getElementById('trStories');
  requestAnimationFrame(function(){ ov.classList.add('ts-on'); });
  if(typeof nvPush === 'function')
    nvPush('view:stories', function(){ trStoriesClose(); },
      function(){ if(TS.idx > 0){ trStoriesGo(TS.idx - 1); return true; } return false; });
  trStoriesRenderBars();
  trStoriesGo(0);
}
function trStoriesRenderBars(){
  const bars = document.getElementById('tsBars');
  if(!bars) return;
  bars.innerHTML = TR_STORIES.map((_, i)=>{
    const cls = i < TS.idx ? 'done' : (i === TS.idx ? 'live' : '');
    return `<span class="ts-bar ${cls}"><i></i></span>`;
  }).join('');
}
function trStoriesGo(i){
  if(!TS.open) return;
  if(i < 0){ i = 0; }
  if(i > TR_STORIES.length - 1){ trStoriesFinish(); return; }
  TS.idx = i;
  const s = TR_STORIES[i];
  const stage = document.getElementById('tsStage');
  const authorSub = document.getElementById('tsAuthorSub');
  const ctaLabel = document.getElementById('tsCtaLabel');
  if(!stage) return;
  const isLast = i === TR_STORIES.length - 1;
  const tone = s.tone === 'lime' ? 'lime' : 'dark';
  stage.className = 'ts-stage ts-tone-' + tone;
  stage.innerHTML = `
    <div class="ts-slide" data-i="${i}">
      <div class="ts-grid"></div>
      <div class="ts-halo"></div>
      <div class="ts-ico-big">${typeof I === 'function' ? I(s.ico) : ''}</div>
      <div class="ts-content">
        <span class="ts-tag">${trStepStr(s.tag)}</span>
        <h2 class="ts-title">${trStepStr(s.title)}</h2>
        <p class="ts-text">${trStepStr(s.body)}</p>
      </div>
      <div class="ts-foot"><span class="ts-dot"></span><span class="ts-meta">${trStepStr(s.meta)}</span></div>
    </div>`;
  if(authorSub) authorSub.textContent = trStepStr(s.meta);
  if(ctaLabel) ctaLabel.textContent = isLast
    ? trT('Начать тур по OKO','Start OKO tour')
    : trT('Пропустить сторис','Skip stories');
  trStoriesRenderBars();
  trStoriesTimerStart();
}
function trStoriesTimerStart(){
  trStoriesTimerStop();
  TS.start = Date.now(); TS.pausedFor = 0;
  const bar = document.querySelector('#tsBars .ts-bar.live > i');
  if(bar){ bar.style.transition = 'none'; bar.style.width = '0%';
    /* force reflow, затем плавная заливка по длительности */
    void bar.offsetWidth;
    bar.style.transition = 'width ' + TS.dur + 'ms linear';
    bar.style.width = '100%';
  }
  TS.timer = setTimeout(trStoriesNext, TS.dur);
}
function trStoriesTimerStop(){
  if(TS.timer){ clearTimeout(TS.timer); TS.timer = null; }
}
function trStoriesNext(){ if(TS.idx < TR_STORIES.length - 1) trStoriesGo(TS.idx + 1); else trStoriesFinish(); }
function trStoriesPrev(){ if(TS.idx > 0) trStoriesGo(TS.idx - 1); else trStoriesGo(0); }
function trStoriesHoldStart(){
  if(!TS.open) return;
  /* удержание > 180 мс = пауза (короткий тап = переключение) */
  TS.holdT = setTimeout(function(){
    if(!TS.timer) return;
    TS.pausedAt = Date.now();
    clearTimeout(TS.timer); TS.timer = null;
    const bar = document.querySelector('#tsBars .ts-bar.live > i');
    if(bar){
      const cs = getComputedStyle(bar);
      const w = cs.width;
      bar.style.transition = 'none'; bar.style.width = w;
    }
    const ov = document.getElementById('trStories'); if(ov) ov.classList.add('ts-paused');
  }, 180);
}
function trStoriesHoldEnd(){
  if(TS.holdT){ clearTimeout(TS.holdT); TS.holdT = null; }
  if(!TS.open || TS.timer) return;
  if(!TS.pausedAt) return;
  const ov = document.getElementById('trStories'); if(ov) ov.classList.remove('ts-paused');
  /* продолжить с того же места */
  const elapsed = TS.pausedAt - TS.start;
  const left = Math.max(400, TS.dur - elapsed);
  TS.start = Date.now() - (TS.dur - left);
  TS.pausedAt = 0;
  const bar = document.querySelector('#tsBars .ts-bar.live > i');
  if(bar){
    const cs = getComputedStyle(bar);
    const cur = parseFloat(cs.width) || 0;
    const total = parseFloat(cs.getPropertyValue('width'));
    /* дозаливка bar до 100% за оставшееся время */
    void bar.offsetWidth;
    bar.style.transition = 'width ' + left + 'ms linear';
    bar.style.width = '100%';
  }
  TS.timer = setTimeout(trStoriesNext, left);
}
function trStoriesClose(){
  if(!TS.open) return;
  TS.open = false;
  trStoriesTimerStop();
  const ov = document.getElementById('trStories');
  if(ov){ ov.classList.remove('ts-on'); setTimeout(function(){ ov.remove(); }, 320); }
  if(typeof nvPop === 'function') nvPop('view:stories');
  /* закрыли крестиком / свайпом — сторис считаем показанными, но тур НЕ запускаем */
  trStoriesMark();
  const cb = TS._onDone; TS._onDone = null;
}
function trStoriesFinish(){
  if(!TS.open) return;
  TS.open = false;
  trStoriesTimerStop();
  const ov = document.getElementById('trStories');
  if(ov){ ov.classList.remove('ts-on'); setTimeout(function(){ ov.remove(); }, 320); }
  if(typeof nvPop === 'function') nvPop('view:stories');
  trStoriesMark();
  const cb = TS._onDone; TS._onDone = null;
  /* Дошли до конца — сразу запускаем интерактивный тур */
  if(typeof cb === 'function') setTimeout(cb, 240);
  else setTimeout(function(){ trStart(false); }, 240);
}

/* ---------- DOM оверлея тура ---------- */
function trBuild(){
  if(document.getElementById('trOverlay')) return;
  const el = document.createElement('div');
  el.id = 'trOverlay';
  el.innerHTML = `
    <div class="tr-hole tr-jump" id="trHole"></div>
    <div class="tr-card tr-jump" id="trCard">
      <div class="tr-arr up hide" id="trArr"></div>
      <div class="tr-body" id="trBody"></div>
      <div class="tr-foot">
        <div class="tr-dots" id="trDots"></div>
        <div class="tr-btns">
          <button class="tr-skip" id="trSkip" onclick="trSkip()">${trT('Пропустить','Skip')}</button>
          <button class="btn tr-next" id="trNext" onclick="trNext()">${trT('Далее','Next')}</button>
        </div>
      </div>
    </div>`;
  /* тап по тёмному фону — мягкий намёк на карточку (ничего не закрываем) */
  el.addEventListener('click', e => {
    if(e.target !== el) return;
    const c = document.getElementById('trCard');
    if(c){ c.style.transform = 'translateX(-50%) scale(1.02)'; setTimeout(()=>{ c.style.transform = 'translateX(-50%)'; }, 140); }
  });
  document.body.appendChild(el);
}

/* ---------- запуск / завершение тура ---------- */
function trStart(manual){
  if(trOpen) return;
  if(!manual && trSeen()) return;
  if(!manual && trBusy()){ setTimeout(trMaybeAuto, 2600); return; }  /* попап успел открыться — позже */
  if(manual && typeof closePopup === 'function') closePopup();       /* ручной запуск — дорогу туру */
  trMark();                       /* «показан один раз» — даже если пропустили */
  trBuild();
  trOpen = true; trFirstPlace = true; trIdx = 0;
  const ov = document.getElementById('trOverlay');
  requestAnimationFrame(()=>ov.classList.add('tr-on'));
  if(typeof nvPush === 'function')
    nvPush('view:tour', function(){ trClose(); },
      function(){ if(trIdx > 0){ trGo(trIdx - 1); return true; } return false; });
  trGo(0);
}
function trClose(){
  if(!trOpen) return;
  trOpen = false;
  const ov = document.getElementById('trOverlay');
  if(ov){ ov.classList.remove('tr-on'); setTimeout(()=>ov.remove(), 340); }
  if(typeof nvPop === 'function') nvPop('view:tour');
  setTimeout(trFlushQ, 600);      /* показать отложенные на время тура попапы */
}

/* ---------- попапы других модулей НЕ перебивают тур/сторис: очередь на потом ---------- */
const trPopupQ = [];
function trFlushQ(){
  if(!trPopupQ.length || trOpen || TS.open) return;
  if(document.getElementById('okoPopup')){ setTimeout(trFlushQ, 4000); return; }
  const o = trPopupQ.shift();
  if(typeof showPopup === 'function') showPopup(o);
  if(trPopupQ.length) setTimeout(trFlushQ, 6000);
}
function trSkip(){ trClose(); if(typeof toast === 'function') toast(trT('Тур можно вернуть: Профиль → «Тур по OKO»','You can reopen the tour: Profile → “OKO tour”')); }
function trNext(){ if(trIdx < TR_STEPS.length - 1) trGo(trIdx + 1); else trFinish(); }
function trFinish(){
  trClose();
  setTimeout(trDonePopup, 380);
}
function trDonePopup(){
  const bal = (typeof WALLET !== 'undefined' && WALLET && typeof fmtMoney === 'function')
    ? trT(` Сейчас на балансе — ${fmtMoney(WALLET.balance)}.`, ` Your balance is now ${fmtMoney(WALLET.balance)}.`) : '';
  if(typeof showPopup !== 'function'){ if(typeof toast === 'function') toast(trT('Тур завершён','Tour complete')); return; }
  showPopup({ico:'rocket', title:trT('Готово!','All set!'),
    body:trT('2 500 ₽ уже на счёте — попробуй продвижение: запусти первую кампанию в рекламном кабинете или продвинь пост из ленты.',
             '2,500 ₽ is already in your account — try promotion: launch your first campaign in Ads Manager or boost a post from the feed.') + bal,
    actions:[
      {label:trT('Попробовать продвижение','Try promotion'), onclick:()=>{
        if(typeof showTab !== 'function') return;
        if(document.getElementById('screen-ads')) showTab('ads'); else showTab('wallet');
      }},
      {label:trT('Позже','Later'), ghost:true},
    ]});
}

/* ---------- шаг тура: вкладка -> ожидание рендера -> спотлайт ---------- */
function trGo(i, dir){
  if(!trOpen) return;
  if(i < 0){ i = 0; }
  if(i > TR_STEPS.length - 1){ trFinish(); return; }
  trIdx = i; dir = dir || 1;
  const st = TR_STEPS[i];
  try{
    if(st.tab && typeof showTab === 'function'){
      const scr = document.getElementById('screen-' + st.tab);
      if(scr && !scr.classList.contains('active')) showTab(st.tab);
    }
  }catch(e){}
  trSettle(function(){
    if(!trOpen || trIdx !== i) return;
    const el = document.querySelector(st.sel);
    if(!el){ trGo(i + dir, dir); return; }           /* элемента нет — шаг пропускаем */
    /* показать элемент в вьюпорте (если он в скролле экрана) и дождаться докрутки */
    try{ if(el.closest('.screen')) el.scrollIntoView({block:'nearest'}); }catch(e){}
    requestAnimationFrame(function(){ if(trOpen && trIdx === i) trPlace(el, st); });
  });
}
/* двойной rAF + пауза: вкладка успевает отрисоваться и разложиться */
function trSettle(cb){
  requestAnimationFrame(function(){ requestAnimationFrame(function(){ setTimeout(cb, 170); }); });
}

/* ---------- геометрия: дыра + карточка + стрелка ---------- */
function trPlace(el, st){
  const hole = document.getElementById('trHole');
  const card = document.getElementById('trCard');
  const arr  = document.getElementById('trArr');
  if(!hole || !card) return;
  const vw = window.innerWidth, vh = window.innerHeight;
  const r = el.getBoundingClientRect();
  const pad = st.pad != null ? st.pad : 6;
  let x = Math.max(4, r.left - pad), y = Math.max(4, r.top - pad);
  let w = Math.min(r.width + pad * 2, vw - x - 4);
  let h = r.height + pad * 2;
  if(st.clamp && h > vh * 0.5) h = Math.round(vh * 0.5);   /* высокие блоки — режем дыру */
  if(y + h > vh - 4) h = vh - 4 - y;

  hole.style.left = x + 'px';  hole.style.top = y + 'px';
  hole.style.width = w + 'px'; hole.style.height = h + 'px';
  hole.style.borderRadius = st.round || '16px';

  /* контент карточки (анимация появления перезапускается пересозданием .tr-body) */
  const body = document.getElementById('trBody');
  body.style.animation = 'none'; void body.offsetWidth; body.style.animation = '';
  body.innerHTML = `
    <div class="tr-step-chip">${typeof I === 'function' ? I(st.ico) : ''} ${trT('шаг','step')} ${trIdx + 1} ${trT('из','of')} ${TR_STEPS.length}</div>
    <h3>${trStepStr(st.title)}</h3>
    <p>${trStepStr(st.text)}</p>`;
  document.getElementById('trDots').innerHTML = TR_STEPS.map((_, k) =>
    `<span class="${k === trIdx ? 'on' : ''}" onclick="trGo(${k})"></span>`).join('');
  const next = document.getElementById('trNext');
  next.innerHTML = trIdx === TR_STEPS.length - 1
    ? `${trT('Готово','Done')} ${typeof I === 'function' ? I('check2') : ''}`
    : `${trT('Далее','Next')} ${typeof I === 'function' ? I('chev') : ''}`;

  /* вертикаль: под дырой, если не влезает — над, иначе по центру свободной зоны */
  const ch = card.offsetHeight, gap = 16;
  let top, below = y + h + gap + ch < vh - 12;
  const above = y - gap - ch > 12;
  if(below) top = y + h + gap;
  else if(above) top = y - gap - ch;
  else { top = Math.max(12, Math.min(vh - ch - 12, y + h + gap)); below = true; }
  card.style.top = top + 'px';

  /* стрелка — к центру подсвеченного элемента */
  const cw = card.offsetWidth, cardLeft = (vw - cw) / 2;
  const ax = Math.max(18, Math.min(cw - 32, x + w / 2 - cardLeft - 7));
  arr.className = 'tr-arr ' + (below ? 'up' : 'down');
  arr.style.left = ax + 'px';

  if(trFirstPlace){
    /* первый показ — без анимации подлёта из угла */
    requestAnimationFrame(function(){
      hole.classList.remove('tr-jump'); card.classList.remove('tr-jump');
    });
    trFirstPlace = false;
  }
}

/* resize / поворот — пересчёт текущего шага */
window.addEventListener('resize', function(){
  if(!trOpen) return;
  clearTimeout(trRzT);
  trRzT = setTimeout(function(){ if(trOpen) trGo(trIdx); }, 140);
});

/* клавиатура (десктоп): Esc — пропустить, ←/→ — навигация, Enter — далее */
document.addEventListener('keydown', function(e){
  if(TS.open){
    if(e.key === 'Escape'){ e.preventDefault(); trStoriesClose(); }
    else if(e.key === 'ArrowRight'){ e.preventDefault(); trStoriesNext(); }
    else if(e.key === 'ArrowLeft'){ e.preventDefault(); trStoriesPrev(); }
    else if(e.key === 'Enter'){ e.preventDefault(); trStoriesFinish(); }
    return;
  }
  if(!trOpen) return;
  if(e.key === 'Escape'){ e.preventDefault(); trSkip(); }
  else if(e.key === 'ArrowRight'){ e.preventDefault(); trNext(); }
  else if(e.key === 'Enter'){ e.preventDefault(); trNext(); }
  else if(e.key === 'ArrowLeft'){ e.preventDefault(); if(trIdx > 0) trGo(trIdx - 1, -1); }
});

/* ---------- авто-старт: сторис + тур, один раз после первого входа ---------- */
function trBusy(){
  const auth = document.getElementById('authScreen');
  const ob = document.getElementById('onboard');
  const reg = document.getElementById('regView');
  return !!(
    (auth && !auth.classList.contains('hidden')) ||
    (ob && !ob.classList.contains('hidden')) ||
    (reg && reg.classList.contains('open')) ||
    document.getElementById('okoPopup') ||
    document.querySelector('.sheet.open') ||
    TS.open);
}
function trMaybeAuto(){
  if(trOpen || TS.open) return;
  if(typeof authed === 'function' && !authed()) return;  /* ждём логина — патчи ниже */
  if(trBusy()){ setTimeout(trMaybeAuto, 2600); return; } /* занято — попробуем позже */
  /* Первым — сторис Даниэля, если ещё не видели */
  if(!trStoriesSeen()){
    setTimeout(function(){
      if(trBusy()) { setTimeout(trMaybeAuto, 2600); return; }
      trStoriesStart(function(){ setTimeout(function(){ trStart(false); }, 220); });
    }, 650);
    return;
  }
  if(trSeen()) return;
  setTimeout(function(){ if(!trSeen() && !trOpen) trStart(false); }, 650);
}

/* ---------- строки в профиле: «Сторис Даниэля» и «Тур по OKO» ---------- */
function trAddRow(){
  try{
    const rows = document.querySelectorAll('#screen-profile .prow');
    let logoutRow = null;
    rows.forEach(r => { if((r.getAttribute('onclick') || '').indexOf('doLogout') > -1) logoutRow = r; });
    if(!logoutRow) return;

    /* «Сторис Даниэля» — повторный запуск */
    if(!document.getElementById('trProwStories')){
      const bs = document.createElement('button');
      bs.className = 'prow'; bs.id = 'trProwStories';
      bs.innerHTML = `${I('play')} <span id="trRowStoriesLabel">${trT('Сторис Даниэля','Daniel’s stories')}</span> <span class="chev">${I('chev')}</span>`;
      bs.onclick = ()=>{ trStoriesStart(null); };
      logoutRow.parentNode.insertBefore(bs, logoutRow);
    }
    /* «Тур по OKO» — повторный запуск */
    if(!document.getElementById('trProwTour')){
      const b = document.createElement('button');
      b.className = 'prow'; b.id = 'trProwTour';
      b.innerHTML = `${I('compass')} <span id="trRowLabel">${trT('Тур по OKO','OKO tour')}</span> <span class="chev">${I('chev')}</span>`;
      b.onclick = ()=>trStart(true);
      logoutRow.parentNode.insertBefore(b, logoutRow);
    }
  }catch(e){}
}

/* ---------- самоинициализация ---------- */
(function trInit(){
  trAddRow();

  /* chain-патч showPopup: во время тура/сторис попапы откладываются (см. trFlushQ) */
  if(typeof showPopup === 'function'){
    const _trPrevShowPopup = showPopup;
    showPopup = function(o){
      if(trOpen || TS.open){ trPopupQ.push(o); return; }
      _trPrevShowPopup(o);
    };
  }

  /* перевод строк профиля (i18n ядра, если доступно) */
  if(typeof regT === 'function' && typeof onLangChange === 'function' && typeof t === 'function'){
    regT({'tr.row': {ru:'Тур по OKO', en:'OKO tour'},
          'tr.stories': {ru:'Сторис Даниэля', en:'Daniel’s stories'}});
    onLangChange(function(){
      const l = document.getElementById('trRowLabel');
      if(l) l.textContent = t('tr.row');
      const ls = document.getElementById('trRowStoriesLabel');
      if(ls) ls.textContent = t('tr.stories');
    });
  }

  /* хук на конец онбординга и на вход — сторис/тур стартуют сразу после них */
  if(typeof obFinish === 'function'){
    const _trPrevObFinish = obFinish;
    obFinish = function(){ _trPrevObFinish(); setTimeout(trMaybeAuto, 900); };
  }
  if(typeof doLogin === 'function'){
    const _trPrevDoLogin = doLogin;
    doLogin = function(m){ _trPrevDoLogin(m); setTimeout(trMaybeAuto, 1400); };
  }

  /* уже залогинен (повторный запуск приложения, сторис/тур ещё не показаны) */
  setTimeout(trMaybeAuto, 1600);
})();

/* ===== module: navstack ===== */
/* ===== NAVSTACK: единый стек навигации — «назад» работает ВЕЗДЕ =====
   Боль: в Telegram Mini App нет браузерных кнопок назад — приходится закрывать
   всё приложение. Модуль ведёт ЕДИНСТВЕННЫЙ источник правды о том, что открыто
   поверх корневых вкладок (стек NV, LIFO), и подключает к нему три «входа»:
     1) системный back браузера/Android (history API + popstate),
     2) Telegram BackButton (window.Telegram.WebApp.BackButton),
     3) клавишу Escape на десктопе.
   Патчи open- и close-функций сохраняют прежнее поведение (chain), синхронизация с
   закрытием крестиком/оверлеем — через nvPop и observers классов .open.

   API (глобальное, префикс nv):
     nvPush(label, closeFn[, stepFn]) — слой открыт (stepFn: внутренний шаг назад,
                                        вернуть true = слой обработал back и остался)
     nvBack()  — закрыть верхний слой (или не-feed вкладка -> feed)
     nvClear() — принудительно закрыть все слои
     nvStackLabels() — отладка: метки стека снизу вверх */

/* ---------- 1. Центральный стек ---------- */
let NV = [];              /* [{label, close, step, tab}] */
let nvClosing = false;    /* close выполняется самим стеком — патчи не трогают NV */
let nvSwallow = 0;        /* сколько ближайших popstate — наши программные history.back() */
let nvTab = 'feed';       /* текущая корневая вкладка */
let nvPrevTab = 'feed';
let nvTg = null;          /* Telegram.WebApp, когда появится */

function nvFind(prefix){
  for(let i = NV.length - 1; i >= 0; i--)
    if(NV[i].label.indexOf(prefix) === 0) return NV[i];
  return null;
}
function nvPush(label, close, step){
  if(nvClosing) return;
  const top = NV[NV.length - 1];
  if(top && top.label === label){ top.close = close; return; } /* повторное открытие того же слоя */
  NV.push({label: label, close: close, step: step || null, tab: nvTab});
  try{ history.pushState({nv: NV.length}, ''); }catch(e){}
  nvUi();
}
/* прямое закрытие (крестик/оверлей/свайп/код) — убрать запись БЕЗ повторного close */
function nvPop(label){
  if(nvClosing) return;
  for(let i = NV.length - 1; i >= 0; i--){
    if(NV[i].label.indexOf(label) === 0){
      NV.splice(i, 1);
      nvDropHistory();
      break;
    }
  }
  nvUi();
}
/* съесть одну history-запись; её popstate — программный, глушим флагом-счётчиком */
function nvDropHistory(){
  nvSwallow++;
  setTimeout(function(){ if(nvSwallow > 0) nvSwallow--; }, 900); /* страховка, если popstate не пришёл */
  try{ history.back(); }catch(e){}
}
function nvArm(){ try{ history.pushState({nv: NV.length}, ''); }catch(e){} }

/* закрыть верхний слой. Возврат: 'step' — слой шагнул внутрь себя и остался,
   'closed' — слой закрыт, false — стек пуст */
function nvBackTop(){
  const rec = NV[NV.length - 1];
  if(!rec) return false;
  if(rec.step){
    let stepped = false;
    nvClosing = true;
    try{ stepped = !!rec.step(); }catch(e){}
    nvClosing = false;
    if(stepped){ nvUi(); return 'step'; }
  }
  NV.pop();
  nvClosing = true;
  try{ rec.close(); }catch(e){}
  nvClosing = false;
  nvUi();
  return 'closed';
}
/* публичный «назад»: TG BackButton, Escape, вызовы из кода */
function nvBack(){
  if(NV.length){
    if(nvBackTop() === 'closed') nvDropHistory();
  } else if(nvTab !== 'feed' && typeof showTab === 'function'){
    showTab('feed');
  }
  nvUi();
}
function nvClear(){
  while(NV.length){
    const rec = NV.pop();
    nvClosing = true;
    try{ rec.close(); }catch(e){}
    nvClosing = false;
    nvDropHistory();
  }
  nvUi();
}
function nvStackLabels(){ return NV.map(function(r){ return r.label; }); }

/* ---------- 2. BROWSER/ANDROID BACK через history API ----------
   История: [корень nvRoot] -> [взведённая nv:0] -> по записи на каждый слой.
   Пользовательский back = popstate: стек непуст -> закрыть верхний слой;
   пуст и вкладка не feed -> feed + перевзвод; пуст на feed -> перевзвод
   (приложение не «выпадает»). Логика ведётся ТОЛЬКО по стеку — глубина в
   state не является источником правды, поэтому рассинхрон самоизлечивается. */
(function nvHistoryInit(){
  try{
    if('scrollRestoration' in history) history.scrollRestoration = 'manual';
    history.replaceState({nvRoot: true}, '');
    history.pushState({nv: 0}, '');
  }catch(e){}
})();

window.addEventListener('popstate', function(ev){
  const atRoot = !!(ev.state && ev.state.nvRoot);
  let armed = false;
  if(nvSwallow > 0){
    nvSwallow--;                       /* наш программный history.back() */
  } else if(NV.length){
    if(nvBackTop() === 'step'){ nvArm(); armed = true; } /* слой остался — вернуть запись */
  } else {
    if(nvTab !== 'feed' && typeof showTab === 'function') showTab('feed');
  }
  if(atRoot && !armed) nvArm();        /* никогда не остаёмся на корне без взведённой записи */
  nvUi();
});

/* Escape на десктопе = «назад» */
document.addEventListener('keydown', function(e){
  if(e.key === 'Escape') nvBack();
});

/* ---------- 3. TELEGRAM BackButton ----------
   tg-webapp подгружает telegram-web-app.js асинхронно ПОСЛЕ navstack —
   поллим появление window.Telegram каждые 500 мс до ~12 c. */
function nvUi(){
  if(!nvTg || !nvTg.BackButton) return;
  try{
    if(NV.length || nvTab !== 'feed') nvTg.BackButton.show();
    else nvTg.BackButton.hide();
  }catch(e){}
}
(function nvTgInit(n){
  const tg = window.Telegram && window.Telegram.WebApp;
  if(tg && tg.BackButton){
    nvTg = tg;
    try{
      if(typeof tg.BackButton.onClick === 'function') tg.BackButton.onClick(function(){ nvBack(); });
      else if(typeof tg.onEvent === 'function') tg.onEvent('backButtonClicked', function(){ nvBack(); });
    }catch(e){}
    nvUi();
    return;
  }
  if(n < 24) setTimeout(function(){ nvTgInit(n + 1); }, 500);
})(0);

/* ---------- 4. ПАТЧИ ЯДРА (chain, прежнее поведение сохраняется) ---------- */

/* вкладки — НЕ стек (корневые разделы), но помним текущую/предыдущую */
if(typeof showTab === 'function'){
  const _nvShowTab = showTab;
  showTab = function(t){
    if(t !== nvTab){ nvPrevTab = nvTab; nvTab = t; }
    _nvShowTab(t);
    nvUi();
  };
}

/* sheets: не стекуются между собой (closeSheet закрывает все) — одна запись, replace */
if(typeof openSheet === 'function' && typeof closeSheet === 'function'){
  const _nvOpenSheet = openSheet, _nvCloseSheet = closeSheet;
  openSheet = function(id){
    _nvOpenSheet(id);
    const r = nvFind('sheet:');
    if(r) r.label = 'sheet:' + id;
    else nvPush('sheet:' + id, function(){ closeSheet(); });
  };
  closeSheet = function(){
    _nvCloseSheet();
    nvPop('sheet:');
  };
}

/* чат-конверсация (только mobile-layout, где появляется .conv-open) */
if(typeof openConv === 'function' && typeof closeConv === 'function'){
  const _nvOpenConv = openConv, _nvCloseConv = closeConv;
  openConv = function(id){
    _nvOpenConv(id);
    const app = document.getElementById('app');
    if(app && app.classList.contains('conv-open') && !nvFind('conv'))
      nvPush('conv', function(){ closeConv(); });
  };
  closeConv = function(){
    _nvCloseConv();
    nvPop('conv');
  };
}

/* мини-аппы (#maGrid -> .ma-view): одна запись, replace при смене вьюхи */
if(typeof openMa === 'function' && typeof closeMa === 'function'){
  const _nvOpenMa = openMa, _nvCloseMa = closeMa;
  openMa = function(id){
    _nvOpenMa(id);
    const r = nvFind('ma:');
    if(r) r.label = 'ma:' + id;
    else nvPush('ma:' + id, function(){ closeMa(); });
  };
  closeMa = function(){
    _nvCloseMa();
    nvPop('ma:');
  };
}

/* попапы core-ext: showPopup сам вызывает closePopup (replace) — глушим
   внутренний вызов флагом, чтобы не гонять history.back()+pushState впритык */
if(typeof showPopup === 'function' && typeof closePopup === 'function'){
  const _nvShowPopup = showPopup, _nvClosePopup = closePopup;
  showPopup = function(o){
    const had = !!nvFind('popup');
    const was = nvClosing;
    nvClosing = true;
    try{ _nvShowPopup(o); } finally { nvClosing = was; }
    if(!had) nvPush('popup', function(){ closePopup(); });
  };
  closePopup = function(){
    _nvClosePopup();
    nvPop('popup');
  };
}

/* HQ-embed (admin-hq уже зовёт nvPush('hq-embed') при открытии) — прямое закрытие */
if(typeof hqCloseEmbed === 'function'){
  const _nvHqCloseEmbed = hqCloseEmbed;
  hqCloseEmbed = function(){
    _nvHqCloseEmbed();
    nvPop('hq-embed');
  };
}

/* ---------- 5. FULLSCREEN-ВЬЮХИ через observers класса .open ----------
   Надёжнее патчей: ловит ЛЮБОЙ путь открытия/закрытия (крестик, оверлей,
   обходные вызовы вроде гейта админки), включая асинхронные (сертификат). */
const NV_VIEWS = [
  /* [id элемента, метка, close, step|null] */
  ['searchView',  'view:search',      function(){ closeSearch(); },      null],
  ['notifsView',  'view:notifs',      function(){ closeNotifs(); },      null],
  ['adminView',   'view:admin',       function(){ closeAdmin(); },       null],
  ['systemView',  'view:system',      function(){ closeSystemView(); },  null],
  ['editProfile', 'view:editProfile', function(){ closeEditProfile(); }, null],
  ['storyViewer', 'story',            function(){ closeStory(); },       null],
  ['msgMenu',     'menu:msg',         function(){ closeMsgMenu(); },     null],
  ['legalView',   'view:legal',       function(){ if(typeof closeLegalDoc==='function') closeLegalDoc(); }, null],
  ['acCertFull',  'view:accert',      function(){ if(typeof acCertFullClose==='function') acCertFullClose(); }, null],
  ['regView',     'view:reg',         function(){ if(typeof regClose==='function') regClose(); },
    /* назад внутри регистрации = шаг назад, с шага 1 — закрыть */
    function(){
      if(typeof REG !== 'undefined' && REG && REG.step > 1 && typeof regBack === 'function'){
        regBack(); return true;
      }
      return false;
    }],
];
function nvSyncView(id, label, close, step){
  const el = document.getElementById(id);
  const open = !!(el && el.classList.contains('open'));
  if(open){ if(!nvFind(label)) nvPush(label, close, step); }
  else nvPop(label); /* записи нет — no-op; после nvBackTop запись уже снята */
}
(function nvViewObservers(){
  NV_VIEWS.forEach(function(v){
    const el = document.getElementById(v[0]);
    if(!el) return;
    try{
      new MutationObserver(function(){ nvSyncView(v[0], v[1], v[2], v[3]); })
        .observe(el, {attributes: true, attributeFilter: ['class']});
    }catch(e){}
  });
})();

/* ---------- 6. ВНУТРЕННЯЯ НАВИГАЦИЯ РАЗДЕЛОВ ---------- */

/* Академия: список курса <-> страница урока (единая точка рендера acRender).
   Кнопка «Академия» в уроке уже есть (.ac-back) — синк идёт через рендер. */
if(typeof acRender === 'function'){
  const _nvAcRender = acRender;
  acRender = function(){
    _nvAcRender();
    if(typeof acView !== 'undefined'){
      if(acView === 'lesson'){
        if(!nvFind('ac:lesson')) nvPush('ac:lesson', function(){ acBackHome(); });
      } else {
        nvPop('ac:lesson');
      }
    }
  };
}

/* Биржа: cats <-> list/fav/пакеты — один внутренний слой 'mk:*' (replace,
   без лишних записей истории). Кнопки «Категории»/«Биржа» (.mk-back) уже есть —
   синк идёт через единую точку рендера renderMarket. */
function nvMkToCats(){ mkView = 'cats'; mkCat = null; renderMarket(); }
function nvMkSlot(label){
  const r = nvFind('mk:');
  if(r){ r.label = label; r.close = nvMkToCats; }
  else nvPush(label, nvMkToCats);
}
if(typeof renderMarket === 'function'){
  const _nvRenderMarket = renderMarket;
  renderMarket = function(){
    _nvRenderMarket();
    if(typeof mkView !== 'undefined'){
      if(mkView === 'list' || mkView === 'fav') nvMkSlot('mk:list');
      else nvPop('mk:'); /* cats = корень биржи, внутренний слой снят */
    }
  };
}

/* Пакеты OKO Production (market-pro): страница поверх биржи, back -> категории */
if(typeof mpOpenPackages === 'function'){
  const _nvMpOpenPackages = mpOpenPackages;
  mpOpenPackages = function(){
    _nvMpOpenPackages();
    nvMkSlot('mk:pack');
  };
}

/* ---------- 7. САМОИНИЦИАЛИЗАЦИЯ: аудит уже открытого ---------- */
(function nvInit(){
  /* если что-то уже открыто к моменту загрузки модуля (авто-restore) — учесть */
  NV_VIEWS.forEach(function(v){ nvSyncView(v[0], v[1], v[2], v[3]); });
  const app = document.getElementById('app');
  if(app && app.classList.contains('conv-open') && !nvFind('conv') && typeof closeConv === 'function')
    nvPush('conv', function(){ closeConv(); });
  const activeScr = document.querySelector('.screen.active');
  if(activeScr && activeScr.id && activeScr.id.indexOf('screen-') === 0)
    nvTab = activeScr.id.slice(7);
  nvUi();
})();

/* ===== module: tg-webapp ===== */
/* ===== TG-WEBAPP: интеграция с Telegram Mini App (@okoappbot) =====
   Скрипт telegram-web-app.js подгружается динамически и опционально:
   вне Telegram (или офлайн) приложение работает как обычно.
   Режим: FULLSCREEN (Bot API 8.0) + учёт safe-area, чтобы шапка
   не уезжала под статус-бар телефона. */
(function tgWebAppInit(){
  /* СИНХРОНИЗАЦИЯ ВЫСОТЫ: убирает «пустоту» при скролле — высота приложения
     жёстко равна видимой области (visualViewport), а не 100dvh, который
     в Telegram-фуллскрине может считаться неверно. Работает и вне Telegram. */
  let _okoLastH = 0, _okoVhRaf = 0, _okoResizeOff = 0, _okoMaxH = 0;
  function _okoApplyVh(){
    _okoVhRaf = 0;
    try{
      const h = Math.round((window.visualViewport && window.visualViewport.height) || window.innerHeight);
      if(!h) return;
      if(h > _okoMaxH) _okoMaxH = h;      // самая большая виденная высота = экран без клавиатуры

      /* ЖЁСТКИЕ ПИКСЕЛЬНЫЕ ВЫСОТЫ — ТОЛЬКО ПОД КЛАВИАТУРУ (правка Даниэля 09.08).
         Раньше высота html/body/#app переписывалась на КАЖДОЕ изменение
         вьюпорта. В Telegram вьюпорт меняется постоянно: при прокрутке
         клиент подбирает свою шапку, при переключении вкладки, при появлении
         полосы. Каждое такое событие двигало нижнее меню — оно «поднималось
         и дёргалось». Теперь пиксельные высоты ставим, только когда экран
         реально сильно уменьшился (открыта клавиатура), а в обычном
         состоянии отдаём управление CSS: height 100% / 100dvh. */
      const keyboardOpen = _okoMaxH && (_okoMaxH - h) > 90;
      const app = document.getElementById('app');

      if(!keyboardOpen){
        if(_okoLastH === 0) return;       // высоты и не ставили — нечего снимать
        _okoLastH = 0;
        document.documentElement.style.height = '';
        document.body.style.height = '';
        if(app) app.style.height = '';
        return;
      }
      if(h === _okoLastH) return;         // ничего не изменилось — не трогаем DOM
      _okoLastH = h;
      document.documentElement.style.height = h + 'px';
      document.body.style.height = h + 'px';
      if(app) app.style.height = h + 'px';
    }catch(e){}
  }
  /* rAF-троттлинг: при открытии клавиатуры visualViewport шлёт десятки resize/сек —
     раньше каждый писал высоту в 3 элемента => лаг/дёрганье. Теперь один пересчёт за кадр.
     На время всплеска ресайза ставим декоративные анимации на паузу (класс перф-слоя). */
  function okoSyncVh(){
    try{
      const root = document.documentElement;
      if(root && !root.classList.contains('oko-scrolling')) root.classList.add('oko-scrolling');
      if(_okoResizeOff) clearTimeout(_okoResizeOff);
      _okoResizeOff = setTimeout(function(){ _okoResizeOff = 0; try{ document.documentElement.classList.remove('oko-scrolling'); }catch(e){} }, 180);
    }catch(e){}
    if(_okoVhRaf) return;
    _okoVhRaf = (window.requestAnimationFrame || window.setTimeout)(_okoApplyVh, 16);
  }
  window.addEventListener('resize', okoSyncVh);
  window.addEventListener('orientationchange', ()=>setTimeout(okoSyncVh, 120));
  if(window.visualViewport) window.visualViewport.addEventListener('resize', okoSyncVh);
  _okoApplyVh();
  setTimeout(_okoApplyVh, 400); setTimeout(_okoApplyVh, 1500);

  function applySafeArea(tg){
    try{
      const sa = tg.safeAreaInset || {top:0,bottom:0,left:0,right:0};
      const ca = tg.contentSafeAreaInset || {top:0,bottom:0,left:0,right:0};
      /* В fullsize-режиме Telegram САМ резервирует место под свою шапку и статус-бар,
         вебвью начинается уже под ними. Складывать инсеты там нельзя — получим
         двойной отступ и пустую полосу сверху. Суммируем только в фуллскрине
         (на случай, если клиент включил его сам). */
      const fs = tg.isFullscreen === true;
      const top = fs ? ((sa.top||0) + (ca.top||0)) : 0;
      const bottom = Math.max(sa.bottom||0, ca.bottom||0, 0);
      /* ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ ПО ИНСЕТАМ (правка Даниэля 09.08).
         Раньше верхний инсет уезжал ТОЛЬКО в padding-top у #app — а все фиксированные
         оверлеи (#notifsView, #adminView, .w2-page, шапки подстраниц, тосты, модалки)
         лежат ВНЕ #app и никакого паддинга не получали. Отсюда «Помощь и лимиты»,
         «Безопасность», «Приём платежа» и т.д. упирались заголовком в шапку Telegram.
         Теперь инсеты живут в CSS-переменных, а весь CSS считает отступы от них. */
      try{
        const rs = document.documentElement.style;
        rs.setProperty('--oko-safe-top', top + 'px');
        rs.setProperty('--oko-safe-bottom', bottom + 'px');
        rs.setProperty('--oko-safe-left', (sa.left||0) + 'px');
        rs.setProperty('--oko-safe-right', (sa.right||0) + 'px');
      }catch(_){}
      const app = document.getElementById('app');
      if(app){
        /* В fullsize-режиме Telegram сам резервирует место под свою шапку, поэтому
           top тут обычно 0. Паддинг оставляем как страховку для фуллскрин-режима. */
        app.style.paddingTop = top + 'px';
        /* НИЖНИЙ safe-area НЕ пихаем в #app — иначе его паддинг показывает #000
           между composer/nav (--surface) и нижним баром Telegram => «тень снизу».
           Отдаём инсет в CSS-переменную, а её используют nav и composer (оба --surface),
           так низ становится сплошным --surface до самого бара Telegram. */
        app.style.paddingBottom = '0px';
      }
      ['authScreen','onboard','splash'].forEach(id=>{
        const el = document.getElementById(id);
        if(el) el.style.paddingTop = top + 'px';
      });
    }catch(e){}
  }
  /* цвета Telegram-хрома (шапка, фон, нижний бар/home-indicator) — строго под тему приложения,
     чтобы в светлой теме снизу/сверху НЕ было чёрных полос («тёмная тень снизу»). */
  function okoTgColors(tg){
    try{
      const light = document.documentElement.getAttribute('data-theme') === 'light';
      /* ТОЧНОЕ соответствие CSS-токенам (иначе шов/«тень» сверху и снизу):
         dark  --bg:#000  --surface:#0d0d0d ; light --bg:#fff --surface:#f7f9f4.
         Хедер и общий фон = --bg (шапка приложения лежит на --bg);
         нижний бар = --surface (на вкладках это nav, в чате — композер, оба --surface). */
      const surf = light ? '#f7f9f4' : '#0d0d0d';
      const bg   = light ? '#ffffff' : '#000000';
      if(tg.setHeaderColor) tg.setHeaderColor(bg);
      if(tg.setBackgroundColor) tg.setBackgroundColor(bg);
      if(tg.setBottomBarColor){ try{ tg.setBottomBarColor(surf); }catch(e){} }
    }catch(e){}
  }
  /* перекрашивать хром Telegram при переключении темы в приложении */
  try{
    new MutationObserver(()=>{
      const tg = window.Telegram && window.Telegram.WebApp;
      if(tg && tg.initData) okoTgColors(tg);
    }).observe(document.documentElement, {attributes:true, attributeFilter:['data-theme']});
  }catch(e){}
  function apply(){
    const tg = window.Telegram && window.Telegram.WebApp;
    if(!tg || !tg.initData) return; // не в Telegram
    try{
      tg.ready();
      tg.expand();
      /* FULLSIZE, А НЕ FULLSCREEN (правка Даниэля 09.08 — постоянное правило).
         В fullscreen Telegram рисует свою плавающую шапку (кнопка «назад», шеврон,
         «...») ПОВЕРХ вебвью — и заголовки экранов, шапки чатов, тосты и модалки
         уезжали под неё на десятках страниц. В обычном (fullsize) режиме Telegram
         сам резервирует место под шапку: контент начинается под ней, ничего не
         перекрывается, нижний бар тоже учитывается. Фуллскрин НЕ включать. */
      try{
        if(tg.isFullscreen && typeof tg.exitFullscreen === 'function') tg.exitFullscreen();
      }catch(e){}
      okoTgColors(tg);  // цвета Telegram-хрома под тему приложения (иначе в светлой теме снизу чёрная полоса)
      if(tg.disableVerticalSwipes) tg.disableVerticalSwipes();
      if(tg.lockOrientation){ try{ tg.lockOrientation('portrait'); }catch(e){} }
      applySafeArea(tg);
      if(tg.onEvent){
        tg.onEvent('viewportChanged', okoSyncVh);
        tg.onEvent('safeAreaChanged', ()=>{applySafeArea(tg); okoSyncVh();});
        tg.onEvent('contentSafeAreaChanged', ()=>{applySafeArea(tg); okoSyncVh();});
        tg.onEvent('fullscreenChanged', ()=>{applySafeArea(tg); okoSyncVh();});
      }
      const u = tg.initDataUnsafe && tg.initDataUnsafe.user;
      if(u && u.first_name){
        PROFILE.name = u.first_name + (u.last_name ? ' ' + u.last_name : '');
        if(u.username) PROFILE.nick = u.username;
        try{ localStorage.setItem('oko-auth','tg'); }catch(e){}
        const auth = document.getElementById('authScreen');
        if(auth) auth.style.display = 'none';
        if(typeof renderMyProfile === 'function') renderMyProfile();
      }
    }catch(e){}
  }
  const s = document.createElement('script');
  s.src = 'https://telegram.org/js/telegram-web-app.js';
  s.onload = apply;
  s.onerror = function(){}; // офлайн/вне TG — молча
  document.head.appendChild(s);
})();

/* ===== module: pwa ===== */
/* ===== PWA module: service worker, установка, offline, sync, share =====
   Всё под префиксом pwa*, без эмодзи, обе темы, без внешних зависимостей.
   Экспорт наружу:
     window.okoPwaShare({title,text,url})   — Web Share API + fallback на clipboard
     window.okoPwaEnqueue(kind, payload)    — положить действие в офлайн-очередь IDB
     window.okoPwaSetSyncHandler(kind, fn)  — зарегистрировать хендлер отправки
     window.okoPwaFlushQueue()              — попытка синхронизации сейчас
*/
(function pwaModule(){
  'use strict';

  const IS_HTTPS       = location.protocol === 'https:';
  const IS_LOCAL       = location.hostname === 'localhost' || location.hostname === '127.0.0.1';
  const IS_STANDALONE  = window.matchMedia('(display-mode: standalone)').matches
                      || window.matchMedia('(display-mode: window-controls-overlay)').matches
                      || window.navigator.standalone === true;
  const UA             = navigator.userAgent || '';
  const IS_IOS         = /iPad|iPhone|iPod/.test(UA) && !window.MSStream;
  const IS_IOS_SAFARI  = IS_IOS && /Safari/i.test(UA) && !/CriOS|FxiOS|EdgiOS|OPiOS/i.test(UA);
  const LS_DECLINED    = 'oko-pwa-declined-at';
  const LS_FIRST_SEEN  = 'oko-pwa-first-seen-at';
  const LS_INSTALLED   = 'oko-pwa-installed';
  const LS_IOS_HINT    = 'oko-pwa-ios-hint-at';
  const DECLINE_COOLDOWN_MS = 30 * 24 * 60 * 60 * 1000; /* 30 дней */
  const PROMPT_DELAY_MS     = 30 * 1000;                /* 30 сек после первого захода */

  /* --------- state --------- */
  let deferredPrompt = null;
  let installTimer   = null;
  let iosTimer       = null;
  let reloadedOnce   = false;
  let waitingWorker  = null;

  /* --------- helpers --------- */
  function safeToast(msg){
    try { if (typeof window.toast === 'function') window.toast(msg); }
    catch(e){}
  }
  function nowTs(){ return Date.now(); }
  function readTs(key){ try { return +localStorage.getItem(key) || 0; } catch(e){ return 0; } }
  function writeTs(key, val){ try { localStorage.setItem(key, String(val)); } catch(e){} }
  function delTs(key){ try { localStorage.removeItem(key); } catch(e){} }

  function markProfileInstalled(){
    try {
      if (window.PROFILE) window.PROFILE.pwaInstalled = true;
      writeTs(LS_INSTALLED, 1);
    } catch(e){}
  }

  function pwaInstalledOrDeclinedRecently(){
    if (readTs(LS_INSTALLED)) return true;
    if (IS_STANDALONE) return true;
    const declined = readTs(LS_DECLINED);
    if (declined && (nowTs() - declined) < DECLINE_COOLDOWN_MS) return true;
    return false;
  }

  /* --------- INSTALL PROMPT (Android/desktop) --------- */
  function showInstallBanner(){
    const el = document.getElementById('pwa-install');
    if (!el) return;
    el.classList.add('pwa-show');
    el.setAttribute('aria-hidden', 'false');
  }
  function hideInstallBanner(){
    const el = document.getElementById('pwa-install');
    if (!el) return;
    el.classList.remove('pwa-show');
    el.setAttribute('aria-hidden', 'true');
  }

  window.pwaTriggerInstall = async function(){
    if (!deferredPrompt) { hideInstallBanner(); return; }
    try {
      deferredPrompt.prompt();
      const choice = await deferredPrompt.userChoice;
      if (choice && choice.outcome === 'accepted'){
        markProfileInstalled();
        safeToast('OKO установлено');
      } else {
        writeTs(LS_DECLINED, nowTs());
      }
    } catch(e){}
    deferredPrompt = null;
    hideInstallBanner();
  };

  window.pwaDismissInstall = function(persist){
    if (persist) writeTs(LS_DECLINED, nowTs());
    hideInstallBanner();
  };

  window.addEventListener('beforeinstallprompt', ev => {
    ev.preventDefault();
    deferredPrompt = ev;
    if (pwaInstalledOrDeclinedRecently()) return;
    clearTimeout(installTimer);
    installTimer = setTimeout(() => {
      if (!deferredPrompt || pwaInstalledOrDeclinedRecently()) return;
      showInstallBanner();
    }, PROMPT_DELAY_MS);
  });

  window.addEventListener('appinstalled', () => {
    hideInstallBanner();
    hideIosHint();
    markProfileInstalled();
    delTs(LS_DECLINED);
    safeToast('OKO установлено на главный экран');
  });

  /* --------- iOS: свой tooltip (у iOS Safari нет beforeinstallprompt) --------- */
  function showIosHint(){
    const el = document.getElementById('pwa-ios');
    if (!el) return;
    el.classList.add('pwa-show');
    el.setAttribute('aria-hidden', 'false');
  }
  function hideIosHint(){
    const el = document.getElementById('pwa-ios');
    if (!el) return;
    el.classList.remove('pwa-show');
    el.setAttribute('aria-hidden', 'true');
  }
  window.pwaDismissIos = function(persist){
    if (persist) writeTs(LS_IOS_HINT, nowTs());
    hideIosHint();
  };

  function maybeScheduleIosHint(){
    if (!IS_IOS_SAFARI) return;
    if (IS_STANDALONE) return;
    const shown = readTs(LS_IOS_HINT);
    if (shown && (nowTs() - shown) < DECLINE_COOLDOWN_MS) return;
    clearTimeout(iosTimer);
    iosTimer = setTimeout(showIosHint, PROMPT_DELAY_MS);
  }

  /* --------- OFFLINE indicator + sync trigger --------- */
  function paintOffline(){
    const el = document.getElementById('pwa-offline');
    const off = !navigator.onLine;
    if (el) el.classList.toggle('pwa-show', off);
    document.body.classList.toggle('pwa-offline-shift', off);
    if (!off) { setTimeout(flushSyncQueue, 300); }
  }
  window.addEventListener('online',  paintOffline);
  window.addEventListener('offline', paintOffline);

  /* --------- UPDATE toast (появление нового SW) --------- */
  function showUpdateToast(w){
    waitingWorker = w || waitingWorker;
    const el = document.getElementById('pwa-update');
    if (!el) return;
    el.classList.add('pwa-show');
  }
  function hideUpdateToast(){
    const el = document.getElementById('pwa-update');
    if (!el) return;
    el.classList.remove('pwa-show');
  }
  window.pwaApplyUpdate = function(){
    hideUpdateToast();
    try {
      if (waitingWorker) waitingWorker.postMessage({ type: 'SKIP_WAITING' });
    } catch(e){}
    setTimeout(() => { try { location.reload(); } catch(e){} }, 250);
  };

  /* --------- SERVICE WORKER --------- */
  async function registerSW(){
    if (!('serviceWorker' in navigator)) return;
    if (!IS_HTTPS && !IS_LOCAL) return;

    /* убираем устаревшие регистрации (был /sw.js в старой сборке) */
    try {
      const regs = await navigator.serviceWorker.getRegistrations();
      for (const r of regs){
        const url = (r.active && r.active.scriptURL) || (r.installing && r.installing.scriptURL) || '';
        if (/\/sw\.js(\?|$)/.test(url) && !/\/service-worker\.js/.test(url)) {
          try { await r.unregister(); } catch(e){}
        }
      }
    } catch(e){}

    try {
      const reg = await navigator.serviceWorker.register('/service-worker.js', { scope: '/' });

      /* если уже стоит waiting-воркер — сразу показать плашку обновления */
      if (reg.waiting && navigator.serviceWorker.controller){
        showUpdateToast(reg.waiting);
      }

      reg.addEventListener('updatefound', () => {
        const nw = reg.installing;
        if (!nw) return;
        nw.addEventListener('statechange', () => {
          if (nw.state === 'installed' && navigator.serviceWorker.controller){
            showUpdateToast(nw);
          }
        });
      });

      /* пассивная проверка апдейта раз в 30 мин */
      setInterval(() => { try { reg.update(); } catch(e){} }, 30 * 60 * 1000);

    } catch(e){ /* тихо */ }

    /* Перезагрузка ТОЛЬКО при настоящем обновлении сборки.
       Правка Даниэля 09.08: «меню при клике везде обновляет страницу».
       Причина была здесь: controllerchange срабатывает и при ПЕРВОЙ установке
       воркера — на самом первом заходе страница сама себя перезагружала,
       и это читалось как «приложение обновляется от каждого нажатия».
       Теперь перезагружаем, только если контроллер УЖЕ был, то есть человек
       действительно перешёл на новую версию. И только когда экран не занят
       работой: во время звонка, записи или открытого плеера не дёргаем. */
    const hadController = !!navigator.serviceWorker.controller;
    navigator.serviceWorker.addEventListener('controllerchange', () => {
      if (!hadController) return;      // первая установка — молча
      if (reloadedOnce) return;
      reloadedOnce = true;
      const busy = () => {
        try{
          if (typeof recStart !== 'undefined' && recStart) return true;         // идёт запись
          if (document.querySelector('#cl-personal.open, #cl-conf.open')) return true; // звонок
          if (window.okoReels && window.okoReels.isOpen && window.okoReels.isOpen()) return true;
          const inp = document.getElementById('msgInput');
          if (inp && inp.value.trim()) return true;                              // набран текст
        }catch(e){}
        return false;
      };
      const go = () => {
        if (busy()) { setTimeout(go, 4000); return; }
        try { location.reload(); } catch(e){}
      };
      setTimeout(go, 300);
    });

    /* сообщения от воркера (например, sync тэг) */
    navigator.serviceWorker.addEventListener('message', ev => {
      const d = ev.data || {};
      if (d.type === 'oko-sync-request') flushSyncQueue();
    });
  }

  /* --------- WEB SHARE API helper --------- */
  window.okoPwaShare = async function(o){
    o = o || {};
    const data = {};
    if (o.title) data.title = o.title;
    if (o.text)  data.text  = o.text;
    if (o.url)   data.url   = o.url;

    if (navigator.share) {
      try {
        if (!navigator.canShare || navigator.canShare(data)) {
          await navigator.share(data);
          return true;
        }
      } catch(e){
        if (e && e.name === 'AbortError') return false;
      }
    }
    /* fallback: clipboard */
    const composite = [o.title, o.text, o.url].filter(Boolean).join(' — ');
    try {
      await navigator.clipboard.writeText(composite);
      safeToast('Ссылка скопирована');
      return true;
    } catch(e){
      /* legacy fallback */
      try {
        const ta = document.createElement('textarea');
        ta.value = composite; ta.setAttribute('readonly','');
        ta.style.position='fixed'; ta.style.top='-9999px';
        document.body.appendChild(ta); ta.select();
        document.execCommand('copy'); ta.remove();
        safeToast('Ссылка скопирована');
        return true;
      } catch(err){
        safeToast('Не удалось поделиться');
        return false;
      }
    }
  };

  /* --------- OFFLINE SYNC QUEUE (IndexedDB) --------- */
  const DB_NAME = 'oko-pwa';
  const DB_VER  = 1;
  const STORE   = 'queue';
  const HANDLERS = {};
  let flushing = false;

  function openDb(){
    return new Promise((resolve, reject) => {
      try {
        const req = indexedDB.open(DB_NAME, DB_VER);
        req.onupgradeneeded = () => {
          const db = req.result;
          if (!db.objectStoreNames.contains(STORE)){
            const s = db.createObjectStore(STORE, { keyPath: 'id', autoIncrement: true });
            s.createIndex('kind', 'kind', { unique: false });
            s.createIndex('createdAt', 'createdAt', { unique: false });
          }
        };
        req.onsuccess = () => resolve(req.result);
        req.onerror   = () => reject(req.error);
      } catch(e){ reject(e); }
    });
  }

  async function idbAdd(item){
    const db = await openDb();
    return new Promise((res, rej) => {
      const tx = db.transaction(STORE, 'readwrite');
      const s = tx.objectStore(STORE);
      const r = s.add(item);
      r.onsuccess = () => res(r.result);
      r.onerror   = () => rej(r.error);
    });
  }
  async function idbList(){
    const db = await openDb();
    return new Promise((res, rej) => {
      const tx = db.transaction(STORE, 'readonly');
      const s = tx.objectStore(STORE);
      const r = s.getAll();
      r.onsuccess = () => res(r.result || []);
      r.onerror   = () => rej(r.error);
    });
  }
  async function idbDelete(id){
    const db = await openDb();
    return new Promise((res) => {
      const tx = db.transaction(STORE, 'readwrite');
      tx.objectStore(STORE).delete(id);
      tx.oncomplete = () => res(true);
      tx.onerror    = () => res(false);
    });
  }

  window.okoPwaSetSyncHandler = function(kind, fn){
    if (!kind || typeof fn !== 'function') return;
    HANDLERS[kind] = fn;
  };

  window.okoPwaEnqueue = async function(kind, payload){
    if (!kind) return null;
    const item = { kind, payload: payload || null, createdAt: nowTs(), tries: 0 };
    try {
      const id = await idbAdd(item);
      /* если онлайн — тут же пробуем отправить */
      if (navigator.onLine) queueMicrotask(flushSyncQueue);
      else showSyncToast('Сохранено. Отправим, когда появится сеть.', 1900);
      /* пробуем зарегистрировать background sync */
      try {
        const reg = await navigator.serviceWorker.ready;
        if (reg && reg.sync) await reg.sync.register('oko-sync-queue');
      } catch(e){}
      return id;
    } catch(e){
      return null;
    }
  };

  window.okoPwaFlushQueue = function(){ return flushSyncQueue(); };

  async function flushSyncQueue(){
    if (flushing || !navigator.onLine) return;
    flushing = true;
    let list = [];
    try { list = await idbList(); } catch(e){ flushing = false; return; }
    if (!list.length){ flushing = false; return; }

    showSyncToast('Синхронизирую офлайн-действия…', 0);
    let ok = 0, fail = 0;
    for (const item of list){
      const fn = HANDLERS[item.kind];
      if (!fn){ /* нет обработчика — пропускаем, но не удаляем */ fail++; continue; }
      try {
        const done = await fn(item.payload, item);
        if (done !== false){ await idbDelete(item.id); ok++; }
        else fail++;
      } catch(e){ fail++; }
    }
    hideSyncToast();
    if (ok) safeToast(`Отправлено ${ok} офлайн-${plural(ok, 'действие','действия','действий')}`);
    flushing = false;
  }

  function plural(n, one, few, many){
    const n10 = n % 10, n100 = n % 100;
    if (n10 === 1 && n100 !== 11) return one;
    if (n10 >= 2 && n10 <= 4 && (n100 < 10 || n100 >= 20)) return few;
    return many;
  }

  let syncToastTimer = null;
  function showSyncToast(txt, autohide){
    const el = document.getElementById('pwa-sync-toast');
    const t  = document.getElementById('pwa-sync-text');
    if (!el || !t) return;
    t.textContent = txt;
    el.classList.add('pwa-show');
    clearTimeout(syncToastTimer);
    if (autohide) syncToastTimer = setTimeout(hideSyncToast, autohide);
  }
  function hideSyncToast(){
    const el = document.getElementById('pwa-sync-toast');
    if (el) el.classList.remove('pwa-show');
  }

  /* --------- SHORTCUTS: ?tab=feed|chats|wallet|academy из manifest --------- */
  function handleShortcut(){
    try {
      const url = new URL(location.href);
      const tab = url.searchParams.get('tab');
      if (!tab) return;
      const allowed = ['feed','chats','wallet','academy','mini','partner','profile'];
      if (allowed.indexOf(tab) === -1) return;
      const fire = () => { try { if (typeof window.showTab === 'function') window.showTab(tab); } catch(e){} };
      /* даём приложению отрисоваться */
      setTimeout(fire, 350);
      /* чистим URL, чтобы не залипал tab= при рестартах */
      try {
        url.searchParams.delete('tab');
        const clean = url.pathname + (url.search ? url.search : '') + url.hash;
        history.replaceState({}, '', clean);
      } catch(e){}
    } catch(e){}
  }

  /* --------- BOOT --------- */
  function boot(){
    /* first-visit stamp */
    if (!readTs(LS_FIRST_SEEN)) writeTs(LS_FIRST_SEEN, nowTs());
    /* если приложение уже установлено — фиксируем в профиле */
    if (IS_STANDALONE) markProfileInstalled();

    paintOffline();
    registerSW();
    maybeScheduleIosHint();
    handleShortcut();

    /* если onLine=true при загрузке — сразу пробуем добить очередь */
    if (navigator.onLine) setTimeout(flushSyncQueue, 1200);
  }

  if (document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', boot, { once: true });
  } else {
    boot();
  }
})();

/* ===== module: paywall ===== */
/* ===== PAYWALL assets (data-URI, вшиты; НЕ править вручную) ===== */
/* Фоновые ассеты тарифов. Раньше все пять лежали прямо здесь как data-URI —
   792 КБ base64 внутри app.js, который целиком грузится при каждом старте.
   base64 к тому же на треть тяжелее самих файлов, кэшировать его отдельно
   нельзя, и при любой правке кода он скачивается заново.
   Теперь это обычные файлы: браузер возьмёт их только когда они реально
   понадобятся, и закэширует отдельно от кода. */
var PW_ASSET = {
  start: 'media/paywall/start.webp',
  pro: 'media/paywall/pro.webp',
  business: 'media/paywall/business.webp',
  paywallImg: 'media/paywall/paywallImg.webp',
  paywallVid: 'media/paywall/paywallVid.webm',
};

/* ============ PAYWALL (pw-) — API-гейт, локи, триггеры, редизайн тарифов ============ */
(function(){
  'use strict';

  /* ---- иерархия тарифов (FREE < START < PRO < BUSINESS < BUSINESS_PRO < MAX) ---- */
  var PW_RANK = {FREE:0, START:1, PRO:2, BUSINESS:3, BUSINESS_PRO:4, 'BUSINESS PRO':4, MAX:5};
  var PW_ORDER = ['FREE','START','PRO','BUSINESS','BUSINESS_PRO','MAX'];
  /* нормализация имени тарифа (PROFILE.tier может быть "BUSINESS PRO" с пробелом) */
  function pwNorm(t){ return String(t||'').replace(/\s+/g,'_').toUpperCase(); }
  /* красивое имя тарифа для UI (обратно из ключа) */
  function pwLabel(t){ return t==='BUSINESS_PRO' ? 'BUSINESS PRO' : t; }
  function pwCurRank(){
    if(typeof isOwner==='function' && isOwner()) return 99;
    var t = (typeof PROFILE!=='undefined' && PROFILE.tier ? pwNorm(PROFILE.tier) : 'FREE');
    return PW_RANK[t] != null ? PW_RANK[t] : 0;
  }
  function pwCurTier(){
    if(typeof PROFILE==='undefined') return 'FREE';
    var t = pwNorm(PROFILE.tier||'FREE');
    return PW_RANK[t]!=null ? t : 'FREE';
  }

  /* ---- цены/мес (fallback если PLANS нет), FREE = 0 ---- */
  var PW_MO = {FREE:0, START:990, PRO:4900, BUSINESS:19900, BUSINESS_PRO:49900, MAX:149900};
  var PW_YR = {};   /* спец-годовых цен нет: скидка периода применяется единообразно */
  function pwMonthly(tier){
    if(typeof PLANS!=='undefined' && PLANS[tier] && PLANS[tier].mo) return PLANS[tier].mo;
    return PW_MO[tier] || 0;
  }
  function pwRub(n){
    if(typeof fmtRub==='function') return fmtRub(n);
    return Number(n||0).toLocaleString('ru-RU').replace(/,/g,' ') + ' ₽';
  }

  /* прописать все тарифы в ядро, чтобы openPay(...)/doPay работали корректно */
  try{
    if(typeof PLANS!=='undefined'){
      if(!PLANS.MAX) PLANS.MAX = {name:'MAX', mo:PW_MO.MAX};
      if(!PLANS.BUSINESS_PRO) PLANS.BUSINESS_PRO = {name:'BUSINESS PRO', mo:PW_MO.BUSINESS_PRO};
      if(!PLANS.FREE) PLANS.FREE = {name:'FREE', mo:0};
    }
  }catch(e){}

  /* ---- выгоды по тарифам для продающего попапа (БЕЗ партнёрки — она отдельно) ---- */
  var PW_BENEFITS = {
    FREE: [
      {t:'Мессенджер и лента', s:'общение, каналы, сторис — базовая социальная сеть'},
      {t:'До 3 подписок', s:'подписывайся на каналы и людей без лимита времени'},
      {t:'Файлы до 100 МБ', s:'загружай ролики и документы в чат'},
      {t:'Голосовые до 5 минут', s:'общайся без ограничений в диалогах'}
    ],
    START: [
      {t:'Premium-мессенджер 4 ГБ', s:'файлы до 4 ГБ, транскрипция голосовых, стикеры'},
      {t:'Магазин шаблонов', s:'готовые сценарии, обложки и структуры Reels'},
      {t:'Аналитика 1 канал', s:'разбор охватов, аудитории и роста подписок'},
      {t:'30 проверок видео/мес', s:'команда OKO смотрит хук, риски и тренды'}
    ],
    PRO: [
      {t:'Всё из START', s:'Premium-мессенджер, магазин шаблонов, аналитика'},
      {t:'Система Роста', s:'персональная стратегия и план на 30 дней'},
      {t:'Автопостинг VK + Telegram', s:'публикация в один клик по расписанию'},
      {t:'Все соцсети и реклама −10%', s:'подключай Instagram, TikTok, YouTube'}
    ],
    BUSINESS: [
      {t:'Контент-завод 30 клипов/мес', s:'производство видео на конвейере'},
      {t:'Академия премиум', s:'закрытые курсы, менторы, разборы'},
      {t:'Белый лейбл', s:'публикуй под своим брендом без пометок OKO'},
      {t:'Реклама −20%', s:'самое выгодное продвижение в кабинете'}
    ],
    BUSINESS_PRO: [
      {t:'Контент-завод 100 клипов/мес', s:'три ролика в день без пауз'},
      {t:'Голос-клон и студия', s:'озвучка твоим голосом, редактор шоу'},
      {t:'API OKO', s:'интеграции с CRM, сайтом, автоматизации'},
      {t:'Команда до 5 мест', s:'общий штаб, роли и распределение задач'}
    ],
    MAX: [
      {t:'Контент-завод 500 клипов/мес', s:'полностью автоматический медиа-поток'},
      {t:'Higgsfield-безлимит', s:'генерация видео и картинок без квот'},
      {t:'Личный ассистент', s:'выделенный менеджер и команда 24/7'},
      {t:'Бренд-агентство', s:'логотип, гайдлайн, лендинг, боты — под ключ'}
    ]
  };

  /* ---- краткие фичи для карточек тарифов (4 маркерами) ---- */
  var PW_CARD_FEATS = {
    FREE:         ['Мессенджер и лента','До 3 подписок','Файлы до 100 МБ','Голосовые до 5 мин'],
    START:        ['Premium-мессенджер 4 ГБ','Магазин шаблонов','Аналитика 1 канал','30 проверок видео/мес'],
    PRO:          ['Всё из START','Система Роста + автопостинг','Все соцсети','Реклама −10%'],
    BUSINESS:     ['Контент-завод 30 роликов/мес','Академия премиум','Белый лейбл','Реклама −20%'],
    BUSINESS_PRO: ['100 роликов/мес','Голос-клон и студия','API','Команда до 5 мест'],
    MAX:          ['500 роликов/мес','Higgsfield-безлимит','Личный ассистент','Бренд-агентство под ключ']
  };
  var PW_TAG = {
    FREE:'Базовая социальная сеть',
    START:'Старт медийности',
    PRO:'Выбор большинства',
    BUSINESS:'Бизнес и команда',
    BUSINESS_PRO:'Студия и API',
    MAX:'Продакшн-агентство'
  };
  /* короткий ценностный подзаголовок для продающего попапа */
  var PW_SELL_SUB = {
    FREE:'Общайся, читай ленту и подписывайся — навсегда бесплатно.',
    START:'Первые шаги в медийности, без лишних лимитов.',
    PRO:'Система Роста, автопостинг и все соцсети — выбор большинства.',
    BUSINESS:'Контент-завод, академия премиум и белый лейбл.',
    BUSINESS_PRO:'100 роликов в месяц, голос-клон, API и командная работа.',
    MAX:'500 роликов в месяц, Higgsfield-безлимит и бренд-агентство.'
  };
  /* что заменяет тариф (внешний рыночный якорь цены — честное «обычно от X/мес») */
  var PW_REPLACES = {
    START:    {who:'сервисы автопостинга и планировщик', cost:6900},
    PRO:      {who:'SMM-специалиста и аналитику под ключ', cost:60000},
    BUSINESS: {who:'команду: SMM, монтажёр, таргетолог', cost:150000},
    MAX:      {who:'продакшн-команду и агентство', cost:280000}
  };
  /* мини-инфографика в попапе: 3 «кольца» [значение-в-центре, подпись, заливка%] */
  var PW_INFO = {
    START:[['+46%','к охватам',46],['15','проверок',80],['2','соцсети',55]],
    PRO:[['88%','продлевают',88],['∞','безлимит',100],['+30','дней плана',75]],
    BUSINESS:[['×5','быстрее',90],['−20%','реклама',80],['PRO','кабинет',100]],
    MAX:[['30','видео/мес',85],['5','мест',70],['−30%','реклама',92]]
  };

  /* ---- фоновые ассеты (data-URI, вшиты) ---- */
  function pwBg(tier){
    return {START:PW_ASSET.start, PRO:PW_ASSET.pro, BUSINESS:PW_ASSET.business, MAX:PW_ASSET.paywallImg}[tier] || PW_ASSET.start;
  }

  /* =================== API-ГЕЙТ =================== */
  window.okoHasSub = function(tier){
    var need = PW_RANK[String(tier||'').toUpperCase()];
    if(need == null) need = 1;
    return pwCurRank() >= need;
  };

  window.okoRequireSub = function(minTier, reason, onOk){
    minTier = String(minTier||'PRO').toUpperCase();
    if(window.okoHasSub(minTier)){ if(typeof onOk==='function') onOk(); return true; }
    pwPaywall({minTier:minTier, reason:reason, onOk:onOk});
    return false;
  };

  /* =================== ПРОДАЮЩИЙ ПОПАП =================== */
  function pwClose(){
    var p = document.getElementById('pwPop');
    if(p){ p.style.animation='pwFade .18s ease reverse forwards'; setTimeout(function(){ if(p.parentNode) p.remove(); },170); }
  }
  window.pwClosePaywall = pwClose;

  function pwRing(display, l, fill){
    var pct = Math.max(0, Math.min(100, Number(fill)||0));
    return '<div class="pw-info-cell"><div class="pw-ring" style="--pwp:'+pct+'"><b>'+display+'</b></div><small>'+l+'</small></div>';
  }

  function pwPaywall(o){
    o = o || {};
    var tier = String(o.minTier||'PRO').toUpperCase();
    if(!PW_BENEFITS[tier]) tier = 'PRO';
    var mo = pwMonthly(tier);
    var perDay = Math.max(1, Math.round(mo/30));
    var sell = !!o.sell;
    var reason = o.reason || ('С подпиской <b>'+tier+'</b> у тебя откроется эта и десятки других возможностей.');
    if(sell && !o.reason) reason = PW_SELL_SUB[tier] || reason;
    var bens = PW_BENEFITS[tier] || PW_BENEFITS.PRO;
    var info = PW_INFO[tier] || PW_INFO.PRO;
    pwClose();
    var el = document.createElement('div');
    el.id = 'pwPop';
    /* фон — CSS-анимация тарифа, без видео */
    var media = pwBgFor(tier);
    var infoHtml = '<div class="pw-info">'+ info.map(function(c){ return pwRing(c[0], c[1], c[2]); }).join('') +'</div>';
    var priceLine = (tier==='MAX')
      ? ('<div class="pw-p-v">'+pwRub(mo)+'<small> /мес</small></div>')
      : ('<div class="pw-p-v">'+pwRub(mo)+'<small> /мес</small></div>');
    var priceNote = (tier==='MAX')
      ? ('год '+pwRub(PW_YR.MAX)+'<br>выгода −20%')
      : ('всего '+pwRub(perDay)+'/день<br>−до 20% на год');
    el.innerHTML =
      '<div class="pw-card">'+
        '<div class="pw-hero">'+ media +
          '<div class="pw-hero-top">'+
            '<div class="pw-lock-badge">'+ (sell ? I('crown') : I('lock')) +'</div>'+
            '<button class="pw-close" aria-label="Закрыть">'+I('plus')+'</button>'+
          '</div>'+
          '<div class="pw-hero-cap"><div class="k">OKO '+(PW_TAG[tier]||'')+'</div><h2>'+(sell?'Тариф ':'Открой ')+tier+'</h2></div>'+
        '</div>'+
        '<div class="pw-body">'+
          '<p class="pw-reason">'+reason+'</p>'+
          infoHtml +
          '<div class="pw-benefits">'+ bens.map(function(b){
            return '<div class="pw-benefit"><span class="pw-b-ic">'+I('check2')+'</span><div><b>'+b.t+'</b><small>'+b.s+'</small></div></div>';
          }).join('') +'</div>'+
          '<div class="pw-price"><div><div class="pw-p-l">Тариф '+tier+'</div>'+
            priceLine+'</div>'+
            '<div class="pw-p-note">'+priceNote+'</div></div>'+
          '<button class="btn pw-cta pw-shine">'+I('crown')+' Оформить '+tier+'</button>'+
          '<button class="btn ghost pw-later">Может позже</button>'+
          '<div class="pw-guarant">'+I('check')+' Отмена в 1 клик · без скрытых списаний · безопасная оплата</div>'+
        '</div>'+
      '</div>';
    el.addEventListener('click', function(e){ if(e.target===el) pwClose(); });
    document.body.appendChild(el);
    var cx = el.querySelector('.pw-close'); if(cx) cx.onclick = pwClose;
    var lt = el.querySelector('.pw-later'); if(lt) lt.onclick = pwClose;
    var ct = el.querySelector('.pw-cta');
    if(ct) ct.onclick = function(){
      pwClose();
      if(typeof openPay==='function') openPay(tier);
      pwSeen();
    };
    /* запуск анимации колец на след. кадре */
    requestAnimationFrame(function(){ if(el.parentNode) el.classList.add('pw-anim'); });
    pwSeen();
  }
  window.pwPaywall = pwPaywall;
  /* открыть продающий попап тарифа из UI (кнопка на карточке) */
  window.pwOpenSell = function(tier){ pwPaywall({minTier:tier, sell:true}); };

  /* =================== ПАРТНЁРКА (ВСЕГДА ОТДЕЛЬНО ОТ ТАРИФА) =================== */
  window.pwPartnerInfo = function(){
    if(typeof showPopup==='function'){
      showPopup({
        ico:'users',
        title:'Партнёрская программа',
        body:'<div class="pw-partner-pop">'+
          '<p>Работает <b>на любом тарифе</b>, даже на FREE — она не входит в подписку, а идёт отдельно.</p>'+
          '<div class="pw-pr"><b>15%</b><span>с каждой продажи по твоей ссылке</span></div>'+
          '<div class="pw-pr"><b>+5%</b><span>с дохода приглашённого партнёра</span></div>'+
          '<div class="pw-pr pw-pr-max"><b>до 20%</b><span>максимальная суммарная комиссия</span></div>'+
          '<div class="pw-pr-eg">'+I('money')+' Пример: друг оформил PRO 4 890 ₽ → <b>+734 ₽</b> тебе на счёт</div>'+
          '<p class="dim" style="font-size:12px;margin:10px 0 4px">Твоя ссылка и готовый текст — поделись в один тап:</p>'+
          pwShareKitHtml()+
          '<p class="dim" style="font-size:12px;margin-top:10px">Выплаты — на лицевой счёт OKO. Статистика переходов и начислений — в реальном времени.</p>'+
        '</div>',
        actions:[{label:'Скопировать ссылку', onclick:function(){ window.pwCopyRef(); }},{label:'Понятно', ghost:true}]
      });
    } else if(typeof toast==='function'){
      toast('Партнёрка: 15% с продаж + 5% с партнёра (до 20%)');
    }
  };

  /* =================== СЕССИОННЫЙ ФЛАГ (ambient-триггеры) =================== */
  function pwSeen(){ try{ sessionStorage.setItem('oko-paywall-seen','1'); }catch(e){} }
  function pwWasSeen(){ try{ return sessionStorage.getItem('oko-paywall-seen')==='1'; }catch(e){ return false; } }

  function pwTrigger(o){
    o = o || {};
    if(pwWasSeen()) return false;
    if(window.okoHasSub(o.minTier||'PRO')) return false;
    pwPaywall(o);
    return true;
  }
  window.okoPaywallTrigger = pwTrigger;

  function pwNudge(o){
    o = o || {};
    if(pwWasSeen() || document.getElementById('pwNudge')) return false;
    if(window.okoHasSub(o.minTier||'PRO')) return false;
    var n = document.createElement('div');
    n.id = 'pwNudge';
    n.innerHTML =
      '<span class="pw-n-ic">'+I('crown')+'</span>'+
      '<div class="pw-n-tx"><b>'+(o.title||'Открой больше с '+(o.minTier||'PRO'))+'</b><small>'+(o.text||'')+'</small></div>'+
      '<button class="pw-n-go">'+(o.cta||'Открыть доступ')+'</button>'+
      '<button class="pw-n-x" aria-label="Скрыть">'+I('plus')+'</button>';
    document.body.appendChild(n);
    function drop(){ n.classList.add('pw-out'); setTimeout(function(){ if(n.parentNode) n.remove(); },300); }
    var nx = n.querySelector('.pw-n-x'); if(nx) nx.onclick = function(){ drop(); pwSeen(); };
    var ng = n.querySelector('.pw-n-go'); if(ng) ng.onclick = function(){ drop(); pwPaywall({minTier:o.minTier||'PRO', reason:o.reason, sell:true}); };
    setTimeout(function(){ if(n.parentNode){ drop(); } }, 8000);
    pwSeen();
    return true;
  }
  window.okoPaywallNudge = pwNudge;

  /* =================== ЛОКИ НА МИНИ-АППАХ =================== */
  var PW_MA_GATE = {
    factory: {tier:'BUSINESS', label:'BUSINESS', reason:'<b>Контент-завод</b> собирает ролики на конвейере. Доступен с тарифа BUSINESS, а 30 видео/мес — на MAX.'},
    system:  {tier:'PRO',      label:'PRO',      reason:'<b>Система роста</b> строит персональную стратегию и план на 30 дней. Доступно с тарифа PRO.'}
  };

  function pwDecorateTiles(){
    var grid = document.getElementById('maGrid');
    if(!grid) return;
    grid.querySelectorAll('.svc').forEach(function(btn){
      var oc = btn.getAttribute('onclick')||'';
      var m = oc.match(/openMa\(['"](\w+)['"]\)/);
      if(!m) return;
      var id = m[1], g = PW_MA_GATE[id];
      if(!g) return;
      var locked = !window.okoHasSub(g.tier);
      btn.classList.toggle('pw-locked', locked);
      var lock = btn.querySelector('.pw-lock');
      if(locked){
        if(!lock){ lock=document.createElement('span'); lock.className='pw-lock'; btn.appendChild(lock); }
        lock.innerHTML = I('lock') + '<i>' + g.label + '</i>';
      } else {
        if(lock) lock.remove();
      }
    });
  }
  window.pwRefreshLocks = pwDecorateTiles;

  if(typeof openMa==='function'){
    var _pwPrevOpenMa = openMa;
    openMa = function(id){
      var g = PW_MA_GATE[id];
      if(g && !window.okoHasSub(g.tier)){
        window.okoRequireSub(g.tier, g.reason, function(){ _pwPrevOpenMa(id); });
        return;
      }
      return _pwPrevOpenMa(id);
    };
  }

  /* =================== ТРИГГЕР: лимит проверок видео =================== */
  var PW_FREE_CHECKS = 3;
  function pwChecksKey(){ var d=new Date(); return 'oko-pw-checks-'+d.getFullYear()+'-'+(d.getMonth()+1)+'-'+d.getDate(); }
  function pwChecksUsed(){ try{ return parseInt(localStorage.getItem(pwChecksKey())||'0',10)||0; }catch(e){ return 0; } }
  function pwChecksInc(){ try{ localStorage.setItem(pwChecksKey(), String(pwChecksUsed()+1)); }catch(e){} }

  if(typeof vcStart==='function'){
    var _pwPrevVcStart = vcStart;
    vcStart = function(){
      if(!window.okoHasSub('PRO')){
        if(pwChecksUsed() >= PW_FREE_CHECKS){
          pwPaywall({minTier:'PRO',
            reason:'Ты исчерпал <b>'+PW_FREE_CHECKS+' бесплатные проверки</b> на сегодня. С тарифом PRO проверки роликов — без лимита.'});
          return;
        }
        pwChecksInc();
      }
      return _pwPrevVcStart.apply(this, arguments);
    };
  }

  /* =================== ТРИГГЕР: большой файл =================== */
  window.okoRequireSubForFile = function(sizeMB, minTier, onOk){
    minTier = String(minTier||'PRO').toUpperCase();
    var limit = window.okoHasSub('MAX') ? 8192
              : window.okoHasSub('BUSINESS') ? 4096
              : window.okoHasSub('PRO') ? 2048
              : window.okoHasSub('START') ? 300 : 100;
    if(sizeMB > limit && !window.okoHasSub(minTier)){
      var cap = (minTier==='MAX'?'8 ГБ':minTier==='BUSINESS'?'4 ГБ':'2 ГБ');
      pwPaywall({minTier:minTier,
        reason:'Файл <b>'+sizeMB+' МБ</b> превышает лимит '+limit+' МБ твоего тарифа. С <b>'+minTier+'</b> загружай ролики до '+cap+' и 4K.'});
      return false;
    }
    if(typeof onOk==='function') onOk();
    return true;
  };

  function pwInjectBigFile(){
    var up = document.getElementById('vcUpload');
    if(!up || document.getElementById('pwBigFileBtn')) return;
    var b = document.createElement('button');
    b.id = 'pwBigFileBtn';
    b.className = 'btn ghost';
    b.style.marginTop = '9px';
    b.innerHTML = I('file')+' Загрузить 4K-ролик (420 МБ)';
    b.onclick = function(){
      window.okoRequireSubForFile(420, 'PRO', function(){
        if(typeof vcStart==='function') vcStart();
      });
    };
    up.appendChild(b);
  }

  /* =================== РЕДИЗАЙН ТАРИФОВ: chain-патч renderPay =================== */
  var _pwPrevRenderPay = (typeof renderPay==='function') ? renderPay : null;
  window._pwPrevRenderPay = _pwPrevRenderPay;

  var pwCmpOpen = false;
  window.pwToggleCmp = function(btn){
    pwCmpOpen = !pwCmpOpen;
    renderPay();
  };
  window.pwSelectTier = function(tier){
    if(typeof payState==='undefined') return;
    payState.plan = tier;
    renderPay();
  };

  /* сравнительная таблица: FREE / START / PRO / BUSINESS / BUSINESS PRO / MAX (партнёрки тут НЕТ) */
  var PW_CMP_ROWS = [
    {k:'Проверка видео',     vals:['no',    '30/мес',    'безлимит',    'безлимит',       'безлимит',    'безлимит']},
    {k:'Загрузка файла',     vals:['100 МБ','4 ГБ',      '4 ГБ',        '8 ГБ',           '16 ГБ',       '32 ГБ']},
    {k:'Автопостинг',        vals:['no',    'no',        'VK+TG',       'все сети',       'все + план',  'все + очередь']},
    {k:'Система Роста',      vals:['no',    'no',        'yes',         'yes',            'yes',         'yes']},
    {k:'Контент-завод',      vals:['no',    'no',        'no',          '30/мес',         '100/мес',     '500/мес']},
    {k:'Академия премиум',   vals:['no',    'no',        'no',          'yes',            'yes',         'yes']},
    {k:'Белый лейбл',        vals:['no',    'no',        'no',          'yes',            'yes',         'yes']},
    {k:'Голос-клон / студия',vals:['no',    'no',        'no',          'no',             'yes',         'yes']},
    {k:'API',                vals:['no',    'no',        'no',          'no',             'yes',         'yes']},
    {k:'Команда / места',    vals:['no',    'no',        'no',          'no',             'до 5',        'до 15']},
    {k:'Скидка на рекламу',  vals:['0%',    '0%',        '10%',         '20%',            '25%',         '30%']},
    {k:'Higgsfield',         vals:['no',    'no',        'no',          'no',             'no',          'безлимит']},
    {k:'Поддержка',          vals:['общая', 'общая',     'приоритет',   'приоритет',      '24/7',        '24/7 + личный']}
  ];
  function pwCmpCell(v, isPro){
    var cls = isPro ? ' pw-c-pro-cell' : '';
    if(v==='yes') return '<td class="pw-yes'+cls+'">'+I('check2')+'</td>';
    if(v==='no')  return '<td class="pw-no'+cls+'">—</td>';
    return '<td class="pw-val'+cls+'">'+v+'</td>';
  }

  function pwLiveNumbers(){
    /* «2 437 на платных тарифах», «92% продлевают PRO», «+34% к охватам» —
       все три цифры высчитывались из текущего времени и менялись каждые
       девять секунд. Это выдуманная статистика на экране, где человек
       достаёт деньги. Пока нет настоящих метрик — не показываем ничего. */
    return [];
  }

  /* Здесь крутилась лента «Недавно в OKO»: Артём оформил PRO, Ирина перешла
     на PRO и так далее — десять выдуманных покупок прямо на экране оплаты.
     Это фальшивое социальное доказательство: людей нет, покупок не было.
     Список пуст, блок не рисуется. Вернём, когда будут настоящие продажи. */
  var PW_TICKER = [];
  function pwTickerHtml(){
    if(!PW_TICKER.length) return '';
    /* дублируем список для бесшовной CSS-прокрутки (translateY -50%) */
    var rows = PW_TICKER.concat(PW_TICKER).map(function(x){
      return '<div class="pw-tick-row"><span class="pw-tick-dot"></span>'+
        '<b>'+esc(x[0])+'</b><span>'+esc(x[1])+'</span></div>';
    }).join('');
    return '<div class="pw-tick"><div class="pw-tick-h">'+I('bolt')+' Недавно в OKO</div>'+
      '<div class="pw-tick-view"><div class="pw-tick-track">'+rows+'</div></div></div>';
  }

  /* ---- ЧЕСТНЫЙ обратный отсчёт годовой скидки: реальный дедлайн — конец текущего месяца ---- */
  var pwCountTimer = 0;
  function pwPromoEnd(){ var d=new Date(); return new Date(d.getFullYear(), d.getMonth()+1, 0, 23,59,59,0).getTime(); }
  function pwCountStr(ms){ if(ms<0)ms=0; var s=Math.floor(ms/1000);
    var d=Math.floor(s/86400);s-=d*86400; var h=Math.floor(s/3600);s-=h*3600; var m=Math.floor(s/60);s-=m*60;
    return [d,h,m,s]; }
  function pwCountHtml(){
    return '<div class="pw-count" id="pwCount">'+
      '<div class="pw-count-t">'+I('clock')+' Годовая скидка держится до конца месяца</div>'+
      '<div class="pw-count-segs">'+
        ['дн','час','мин','сек'].map(function(l){ return '<div class="pw-count-seg"><b>0</b><i>'+l+'</i></div>'; }).join('<span class="pw-count-sep">:</span>')+
      '</div></div>';
  }
  function pwStartCountdown(){
    if(pwCountTimer){ try{ clearInterval(pwCountTimer); }catch(e){} pwCountTimer=0; }
    function tick(){
      var el=document.getElementById('pwCount');
      if(!el || el.offsetParent===null){ if(pwCountTimer){ try{clearInterval(pwCountTimer);}catch(e){} pwCountTimer=0; } return; }
      var c=pwCountStr(pwPromoEnd()-Date.now());
      var b=el.querySelectorAll('.pw-count-seg b');
      if(b.length===4){ b[0].textContent=c[0]; b[1].textContent=('0'+c[1]).slice(-2); b[2].textContent=('0'+c[2]).slice(-2); b[3].textContent=('0'+c[3]).slice(-2); }
    }
    tick();
    try{ pwCountTimer=setInterval(tick,1000); }catch(e){}
  }

  /* ---- FAQ / снятие возражений (native <details>, без JS-таймеров) ---- */
  var PW_FAQ = [
    {q:'Спишется ровно столько, сколько показано?', a:'Да. Цена в карточке равна сумме к оплате — рубль в рубль. Годовой MAX — 259 200 ₽ (−20%), ни копейкой больше.'},
    {q:'Можно отменить подписку?', a:'В один клик в настройках. Автопродление выключается сразу, а доступ остаётся до конца оплаченного периода.'},
    {q:'Почему OKO не полностью бесплатный?', a:'Проверки видео, автопостинг и контент-завод — это работа реальной команды и серверов. Базовый уровень бесплатен навсегда: мессенджер, лента, кошелёк, партнёрка и Академия.'},
    {q:'А если тариф не подойдёт?', a:'Понизь уровень или вернись на FREE когда угодно. Подписчики, ролики и баланс остаются с тобой.'}
  ];
  function pwFaqHtml(){
    return '<div class="pw-faq"><div class="pw-faq-h">'+I('comment')+' Частые вопросы</div>'+
      PW_FAQ.map(function(f){
        return '<details class="pw-faq-i"><summary>'+esc(f.q)+'<span class="pw-faq-x">'+I('chev')+'</span></summary>'+
          '<p>'+esc(f.a)+'</p></details>';
      }).join('')+'</div>';
  }

  /* =================== ПАРТНЁРСКИЙ ШЭР-КИТ (растущая петля роста) =================== */
  window.pwRefLink = function(){ var n=(typeof PROFILE!=='undefined'&&PROFILE.nick)?PROFILE.nick:'ref'; return 'https://okoteam.top/i/'+n; };
  window.pwShareText = function(){ return 'Веду соцсети через OKO: команда проверяет ролики, автопостинг во все сети и система роста. Заходи по моей ссылке — '+window.pwRefLink(); };
  window.pwCopyRef = function(){ var l=window.pwRefLink();
    try{ if(navigator.clipboard) navigator.clipboard.writeText(l); }catch(e){}
    if(typeof toast==='function') toast('Партнёрская ссылка скопирована'); };
  window.pwCopyShareText = function(){ var s=window.pwShareText();
    try{ if(navigator.clipboard) navigator.clipboard.writeText(s); }catch(e){}
    if(typeof toast==='function') toast('Готовый текст для поста скопирован'); };
  window.pwShare = function(net){
    var link=window.pwRefLink(), txt=window.pwShareText();
    if(net==='native'){ try{ if(navigator.share){ navigator.share({title:'OKO',text:txt,url:link}); return; } }catch(e){} }
    var u = net==='tg' ? 'https://t.me/share/url?url='+encodeURIComponent(link)+'&text='+encodeURIComponent(txt)
          : net==='vk' ? 'https://vk.com/share.php?url='+encodeURIComponent(link)+'&title='+encodeURIComponent(txt)
          : link;
    try{ window.open(u,'_blank','noopener'); }catch(e){}
  };
  function pwShareKitHtml(){
    var link=window.pwRefLink();
    return '<div class="pw-share">'+
      '<div class="pw-share-link"><span>'+esc(link)+'</span>'+
        '<button class="pw-share-copy" onclick="pwCopyRef()" aria-label="Скопировать ссылку">'+I('copy')+'</button></div>'+
      '<div class="pw-share-row">'+
        '<button class="pw-share-btn" onclick="pwShare(\'tg\')">'+I('send')+' Telegram</button>'+
        '<button class="pw-share-btn" onclick="pwShare(\'vk\')">'+I('globe')+' VK</button>'+
        '<button class="pw-share-btn" onclick="pwCopyShareText()">'+I('file')+' Текст поста</button>'+
      '</div></div>';
  }

  /* =================== АНИМИРОВАННЫЕ CSS-ФОНЫ ТАРИФОВ (без видео) =================== */
  /* SVG для каждого фона — вшиты; анимируются через CSS (transform/opacity, GPU) */
  var PW_BG_MARKUP = {
    FREE: '<div class="pw-tbg pw-tbg-free"><span class="pw-bub"></span><span class="pw-bub"></span><span class="pw-bub"></span></div>',
    START: '<div class="pw-tbg pw-tbg-start">'+
      '<svg viewBox="0 0 200 100" preserveAspectRatio="xMidYMid slice" aria-hidden="true">'+
        '<path class="pw-trail" d="M-10 92 Q 60 82 100 55 T 220 8"/>'+
        '<g class="pw-rocket">'+
          '<path d="M-3.5 -7 L3.5 -7 L3.5 3 L0 8 L-3.5 3 Z" fill="#9AFF00"/>'+
          '<circle cx="0" cy="-2.5" r="1.5" fill="#0a0a0a"/>'+
          '<path d="M-3.5 3 L-6 8 L0 5 L6 8 L3.5 3 Z" fill="rgba(154,255,0,.55)"/>'+
        '</g>'+
      '</svg>'+
      '<span class="pw-spark"></span>'+
    '</div>',
    PRO: '<div class="pw-tbg pw-tbg-pro">'+
      '<div class="pw-gear pw-gear-a">'+ pwGearSvg() +'</div>'+
      '<div class="pw-gear pw-gear-b">'+ pwGearSvg() +'</div>'+
      '<div class="pw-gear pw-gear-c">'+ pwGearSvg() +'</div>'+
    '</div>',
    BUSINESS: '<div class="pw-tbg pw-tbg-biz">'+
      '<svg viewBox="0 0 200 100" preserveAspectRatio="none" aria-hidden="true">'+
        '<rect class="pw-bar" x="24" y="66" width="14" height="30" rx="2"/>'+
        '<rect class="pw-bar" x="56" y="54" width="14" height="42" rx="2"/>'+
        '<rect class="pw-bar" x="88" y="42" width="14" height="54" rx="2"/>'+
        '<rect class="pw-bar" x="120" y="28" width="14" height="68" rx="2"/>'+
        '<rect class="pw-bar" x="152" y="16" width="14" height="80" rx="2"/>'+
        '<circle class="pw-dot" cx="31" cy="66" r="2.5"/>'+
        '<circle class="pw-dot" cx="63" cy="54" r="2.5"/>'+
        '<circle class="pw-dot" cx="95" cy="42" r="2.5"/>'+
        '<circle class="pw-dot" cx="127" cy="28" r="2.5"/>'+
        '<polyline class="pw-line" points="12,80 44,68 76,50 108,36 140,22 176,10"/>'+
      '</svg>'+
    '</div>',
    BUSINESS_PRO: '<div class="pw-tbg pw-tbg-bizpro">'+
      '<span class="pw-ray"></span>'+
      '<span class="pw-orb pw-orb-a"></span>'+
      '<span class="pw-orb pw-orb-b"></span>'+
      '<span class="pw-orb pw-orb-c"></span>'+
    '</div>',
    MAX: '<div class="pw-tbg pw-tbg-max"><div class="pw-stars"></div><div class="pw-stars b"></div><span class="pw-comet"></span></div>'
  };
  function pwGearSvg(){
    /* один SVG-cog, шестерёнка (10 зубцов + отверстие) */
    return '<svg viewBox="-20 -20 40 40" aria-hidden="true">'+
      '<path fill="currentColor" d="M0 -18 L3.4 -17.5 L4.6 -13.7 L8.5 -13.5 L11.5 -10.5 L15.2 -12 L17.5 -9 L15.8 -5.5 L18 -2.7 L18 3.4 L15 5 L15 9 L11 11 L10 15 L6 15 L4 18 L-4 18 L-6 15 L-10 15 L-11 11 L-15 9 L-15 5 L-18 3.4 L-18 -2.7 L-15.8 -5.5 L-17.5 -9 L-15.2 -12 L-11.5 -10.5 L-8.5 -13.5 L-4.6 -13.7 L-3.4 -17.5 Z"/>'+
      '<circle cx="0" cy="0" r="5.5" fill="rgba(10,10,10,.65)"/>'+
    '</svg>';
  }
  function pwBgFor(tier){ return PW_BG_MARKUP[tier] || PW_BG_MARKUP.PRO; }
  window.pwBgFor = pwBgFor;

  /* ================= КОМПАКТ-КАРТОЧКА + СУБСТРАНИЦА ДЕТАЛЕЙ =================== */
  window.pwSelectTier = function(tier){
    tier = pwNorm(tier);
    if(typeof payState==='undefined') return;
    if(PW_RANK[tier]==null || tier==='FREE') return; /* FREE — не оплачивается */
    payState.plan = tier;
    if(typeof renderPay==='function') renderPay();
  };
  window.pwOpenDetails = function(tier){
    tier = pwNorm(tier);
    var view = document.getElementById('payView');
    if(!view) return;
    var det = view.querySelector('.pw-details');
    if(!det){ det = document.createElement('div'); det.className='pw-details'; view.appendChild(det); }
    var mo = pwMonthly(tier);
    var bens = PW_BENEFITS[tier] || PW_BENEFITS.PRO;
    var cur = pwCurTier();
    var isCur = cur===tier;
    var canBuy = tier!=='FREE';
    det.innerHTML =
      '<div class="pw-det-top">'+
        '<button class="pw-det-back" onclick="pwCloseDetails()" aria-label="Назад">'+I('chev')+'</button>'+
        '<h3>'+pwLabel(tier)+'</h3>'+
      '</div>'+
      '<div class="pw-det-hero">'+ pwBgFor(tier) +'</div>'+
      '<div class="dim" style="font-size:12.5px;margin:-4px 0 10px">'+esc(PW_TAG[tier]||'')+'</div>'+
      '<div class="pw-det-price"><b>'+pwRub(mo)+'</b><span>/мес</span>'+
        (mo>0?'<em>≈ '+pwRub(Math.max(1,Math.round(mo/30)))+'/день</em>':'<em>навсегда бесплатно</em>')+
      '</div>'+
      '<ul class="pw-det-feats">'+ bens.map(function(b){
        return '<li><span class="pw-df-ic">'+I('check2')+'</span><div><b>'+esc(b.t)+'</b><small>'+esc(b.s)+'</small></div></li>';
      }).join('') +'</ul>'+
      (canBuy
        ? '<button class="btn pw-shine" onclick="pwSelectTier(\''+tier+'\');pwCloseDetails()"'+(isCur?' disabled':'')+'>'+I(isCur?'check':'crown')+' '+(isCur?'Твой тариф':'Выбрать '+pwLabel(tier))+'</button>'
        : '<button class="btn ghost" onclick="pwCloseDetails()">Понятно</button>')+
      '<p class="dim" style="font-size:11px;text-align:center;margin-top:9px">Отключишь в 1 клик · спишется ровно как показано</p>';
    requestAnimationFrame(function(){ det.classList.add('on'); });
  };
  window.pwCloseDetails = function(){
    var view = document.getElementById('payView');
    if(!view) return;
    var det = view.querySelector('.pw-details');
    if(det){ det.classList.remove('on'); setTimeout(function(){ if(det.parentNode) det.remove(); }, 340); }
  };

  function pwRenderPay(){
    var view = document.getElementById('payView');
    if(!view) return;
    if(typeof payState==='undefined' || typeof PAY_PERIODS==='undefined'){
      if(_pwPrevRenderPay) return _pwPrevRenderPay(); return;
    }
    /* нормализуем текущий план (может прийти 'BUSINESS PRO') */
    var normed = pwNorm(payState.plan);
    if(PW_RANK[normed]==null || normed==='FREE') normed = 'PRO';
    payState.plan = normed;
    var sel = payState.plan;
    var per = PAY_PERIODS.find(function(x){return x[0]===payState.period;}) || PAY_PERIODS[PAY_PERIODS.length-1];
    var disc = per[2];
    var cur = pwCurTier();

    /* СВАЙПЕР ТАРИФОВ */
    var tiersHtml = PW_ORDER.map(function(tier){
      var mo = pwMonthly(tier);
      var isPro = tier==='PRO';
      var isMax = tier==='MAX';
      var isFree = tier==='FREE';
      var on = sel===tier;
      var isCur = cur===tier;
      var perDay = mo>0 ? Math.max(1, Math.round(mo/30)) : 0;
      var flag = isPro ? '<span class="pw-flag">Хит</span>'
               : isMax ? '<span class="pw-flag pw-flag-max">MAX</span>' : '';
      var curBadge = isCur ? '<span class="pw-tier-cur-badge">Сейчас</span>' : '';
      var priceLine = mo>0
        ? '<div class="pw-tier-price"><b>'+pwRub(mo)+'</b><span>/мес</span><span class="pw-per">'+pwRub(perDay)+'/день</span></div>'
        : '<div class="pw-tier-price"><b>0 ₽</b><span>/навсегда</span><span class="pw-per">без карты</span></div>';
      var btn;
      if(isCur) btn = '<button class="pw-tier-btn is-current" disabled>'+I('check2')+' Твой тариф</button>';
      else if(isFree) btn = '<button class="pw-tier-btn ghost" onclick="event.stopPropagation();pwOpenDetails(\''+tier+'\')">'+I('eye')+' Подробнее</button>';
      else btn = '<button class="pw-tier-btn" onclick="event.stopPropagation();pwSelectTier(\''+tier+'\')">'+I(on?'check':'crown')+' '+(on?'Выбран':'Выбрать')+'</button>';
      return ''+
      '<div class="pw-tier'+(on?' on':'')+(isMax?' pw-tier-max':'')+(isPro?' pw-tier-rec':'')+'" onclick="pwOpenDetails(\''+tier+'\')">'+
        '<div class="pw-tier-bg">'+ pwBgFor(tier) + flag + curBadge +
          '<div class="pw-tier-cap"><div class="n">'+pwLabel(tier)+'</div><div class="s">'+esc(PW_TAG[tier]||'')+'</div></div>'+
        '</div>'+
        priceLine +
        '<ul class="pw-tier-feats">'+ (PW_CARD_FEATS[tier]||[]).map(function(f){
          return '<li>'+I('check2')+'<span>'+esc(f)+'</span></li>';
        }).join('') +'</ul>'+
        btn +
      '</div>';
    }).join('');

    /* Скидочные чипы периодов (3 мес / 6 мес / год + вариант 1 мес) */
    var discRow = '<div class="pw-disc-row">'+ PAY_PERIODS.map(function(p){
      return '<button class="chip'+(payState.period===p[0]?' on':'')+'" onclick="payState.period='+p[0]+';renderPay()">'+
        p[1]+ (p[2]?'<i>−'+p[2]+'%</i>':'') +'</button>';
    }).join('') +'</div>';

    /* цена выбранного */
    var full = pwMonthly(sel) * per[0];
    var total = Math.round(full * (1 - disc/100));
    var effDisc = full>0 ? Math.round((1 - total/full)*100) : 0;

    /* Индекс выбранной карточки — для точек-индикаторов */
    var selIdx = PW_ORDER.indexOf(sel);
    var dotsHtml = '<div class="pw-dots">'+ PW_ORDER.map(function(_,i){
      return '<span class="'+(i===selIdx?'on':'')+'"></span>';
    }).join('') +'</div>';

    var methodsHtml = (typeof PAY_METHODS!=='undefined'?PAY_METHODS:[['card','Карта РФ','card']]).map(function(pm){
      return '<button class="pay-m'+(payState.method===pm[0]?' on':'')+'" onclick="payState.method=\''+pm[0]+'\';renderPay()">'+I(pm[2])+'<span>'+pm[1]+'</span></button>';
    }).join('');

    var cmpHtml = '';
    if(pwCmpOpen){
      cmpHtml =
      '<div class="pw-cmp-hint">'+I('chev')+' листай вправо: BUSINESS PRO, MAX</div>'+
      '<div class="pw-cmp-wrap"><table class="pw-cmp">'+
        '<thead><tr><th></th><th>FREE</th><th>START</th><th class="pw-c-pro">PRO<i class="pw-c-tag">хит</i></th><th>BUSINESS</th><th>B. PRO</th><th class="pw-c-max">MAX</th></tr></thead>'+
        '<tbody>'+ PW_CMP_ROWS.map(function(r){
          return '<tr><th>'+r.k+'</th>'+
            r.vals.map(function(v,idx){ return pwCmpCell(v, idx===2); }).join('')+
          '</tr>';
        }).join('') +'</tbody>'+
      '</table></div>';
    }

    view.innerHTML =
      '<div class="pw-pay-head"><h3>Тарифы OKO</h3><p>Свайпни, чтобы выбрать — активация мгновенно</p></div>'+
      discRow +
      '<div class="pw-swiper-wrap"><div class="pw-swiper" id="pwSwiper">'+ tiersHtml +'</div></div>'+
      dotsHtml +
      '<button class="pw-cmp-toggle'+(pwCmpOpen?' on':'')+'" onclick="pwToggleCmp(this)">'+I('poll')+' Сравнить все тарифы '+I('chev')+'</button>'+
      cmpHtml +
      '<div class="pay-total"><div><div class="dim" style="font-size:12px">К оплате · '+pwLabel(sel)+' · '+per[1]+'</div>'+
        '<div class="pay-sum" id="pwSum">'+pwRub(total)+'</div></div>'+
        (effDisc>0?'<div class="pay-old">'+pwRub(full)+'</div>':'')+'</div>'+
      '<p style="font-weight:600;font-size:13px;margin:14px 0 8px">Способ оплаты</p>'+
      '<div class="pay-methods">'+ methodsHtml +'</div>'+
      '<div style="height:16px"></div>'+
      '<button class="btn pw-shine" onclick="doPay()">'+I('lock')+' Оплатить '+pwRub(total)+'</button>'+
      '<div class="pw-guar-band">'+I('check')+' Отключишь в 1 клик · спишется ровно как показано · данные остаются с тобой</div>'+
      pwFaqHtml() +
      '<p class="dim" style="font-size:11px;text-align:center;margin-top:12px">Безопасная оплата. Автопродление можно отключить в любой момент.</p>';

    /* прокрутить свайпер к выбранной карточке + синхронизировать точки при скролле */
    requestAnimationFrame(function(){
      var sw = document.getElementById('pwSwiper');
      if(!sw) return;
      var cards = sw.querySelectorAll('.pw-tier');
      var target = cards[selIdx];
      if(target){
        var left = target.offsetLeft - (sw.clientWidth - target.clientWidth)/2;
        sw.scrollTo({left:Math.max(0,left), behavior:'instant' in sw ? 'instant' : 'auto'});
      }
      /* индикаторы точек следуют за скроллом */
      var dots = view.querySelectorAll('.pw-dots span');
      var onScroll = function(){
        var mid = sw.scrollLeft + sw.clientWidth/2;
        var closest = 0, best = Infinity;
        cards.forEach(function(c,i){
          var cm = c.offsetLeft + c.clientWidth/2;
          var d = Math.abs(cm-mid);
          if(d<best){ best=d; closest=i; }
        });
        dots.forEach(function(d,i){ d.classList.toggle('on', i===closest); });
      };
      sw.addEventListener('scroll', onScroll, {passive:true});
    });

    /* тик обратного отсчёта не нужен в новой версии */
    if(pwCountTimer){ try{ clearInterval(pwCountTimer); }catch(e){} pwCountTimer=0; }
  }

  if(_pwPrevRenderPay){
    renderPay = pwRenderPay;
  }

  /* Спец-годовых цен больше нет — скидка периода PAY_PERIODS применяется ко всем тарифам единообразно.
     Ядровой payPrice() остаётся источником правды. */

  /* =================== САМОИНИЦИАЛИЗАЦИЯ =================== */
  function pwInit(){
    pwDecorateTiles();
    pwInjectBigFile();
    try{ pwCompactProfilePlans(); }catch(e){}
  }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded', pwInit);
  else pwInit();

  /* ---- В ПРОФИЛЕ: скрыть длинные карточки тарифов, показать компакт-строку ---- */
  function pwCompactProfilePlans(){
    var screen = document.getElementById('screen-profile');
    if(!screen) return;
    var plans = screen.querySelector('.plans-wrap');
    var discRow = screen.querySelector('.disc-row');
    if(!plans) return; /* уже переработано */
    /* Найти заголовок "Тарифы" перед .disc-row */
    var head = null;
    var prev = discRow ? discRow.previousElementSibling : plans.previousElementSibling;
    while(prev){
      if(prev.classList && prev.classList.contains('section-h') && /Тарифы/i.test(prev.textContent||'')){ head = prev; break; }
      prev = prev.previousElementSibling;
    }
    /* Собираем компакт-строку */
    var cur = pwCurTier();
    var curLabel = pwLabel(cur);
    var isOwner_ = (typeof isOwner==='function' && isOwner());
    var band = document.createElement('div');
    band.className = 'pw-prof-band';
    band.id = 'pwProfBand';
    band.innerHTML =
      '<span class="pw-prof-ic">'+I('crown')+'</span>'+
      '<div class="pw-prof-tx"><b>Твой тариф · '+curLabel+(isOwner_?' · владелец':'')+'</b>'+
        '<small>'+esc(PW_TAG[cur]||'Открой больше возможностей OKO')+'</small></div>'+
      '<button class="pw-prof-btn" onclick="pwOpenTiers()">'+I('bolt')+' Тарифы</button>';
    /* Вставляем band на место старой шапки/чипов/карточек, всё лишнее — удаляем */
    var anchor = head || discRow || plans;
    anchor.parentNode.insertBefore(band, anchor);
    if(head) head.remove();
    if(discRow) discRow.remove();
    plans.remove();
  }
  /* Открытие тарифов (кнопка «Тарифы» в компакт-строке профиля) */
  window.pwOpenTiers = function(){
    if(typeof openPay==='function'){
      var cur = pwCurTier();
      /* если у пользователя FREE — предлагаем PRO как якорь; иначе — текущий тариф */
      var start = (cur==='FREE' || cur==='OWNER') ? 'PRO' : cur;
      openPay(start);
    }
  };
  /* Перехват «Тарифы OKO» из profile-social (pp2OpenTiers): вместо второго набора длинных
     карточек — единый экран тарифов со свайпером (pwOpenTiers -> openPay -> pw-swiper). */
  window.pp2OpenTiers = function(){ window.pwOpenTiers(); };

  var _pwPrevRMP = (typeof renderMyProfile==='function') ? renderMyProfile : null;
  if(_pwPrevRMP){
    renderMyProfile = function(){
      var r = _pwPrevRMP.apply(this, arguments);
      try{ pwDecorateTiles(); }catch(e){}
      try{ pwCompactProfilePlans(); }catch(e){}
      /* обновить текст компакт-строки, если она уже есть */
      try{
        var band = document.getElementById('pwProfBand');
        if(band){
          var cur = pwCurTier();
          var isOwner_ = (typeof isOwner==='function' && isOwner());
          var tx = band.querySelector('.pw-prof-tx');
          if(tx){
            tx.querySelector('b').textContent = 'Твой тариф · '+pwLabel(cur)+(isOwner_?' · владелец':'');
            tx.querySelector('small').textContent = PW_TAG[cur]||'Открой больше возможностей OKO';
          }
        }
      }catch(e){}
      return r;
    };
  }

  var _pwPrevShowTab = (typeof showTab==='function') ? showTab : null;
  if(_pwPrevShowTab){
    showTab = function(tab){
      var r = _pwPrevShowTab.apply(this, arguments);
      if(tab==='mini'){
        try{ pwDecorateTiles(); }catch(e){}
        setTimeout(function(){
          pwNudge({minTier:'PRO',
            title:'С PRO у тебя был бы доступ',
            text:'К Системе роста, безлимитным проверкам и автопостингу',
            cta:'Открыть'});
        }, 1400);
      }
      return r;
    };
  }

})();

/* ============================================================
   PAYWALL EXTRAS (pw- / bnd-): free-trial PRO, cross-sell,
   bundles, micro-subs, LTV report, 14-day guarantee
   ============================================================ */
(function pwExtras(){
  'use strict';

  /* ---------- утилиты ---------- */
  function fmt(n){ return Number(n||0).toLocaleString('ru-RU').replace(/,/g,' ') + ' ₽'; }
  function num(n){ return Number(n||0).toLocaleString('ru-RU').replace(/,/g,' '); }
  function _esc(t){ var d=document.createElement('div'); d.textContent=String(t==null?'':t); return d.innerHTML; }
  function ico(n){ return '<svg class="i"><use href="#i-'+n+'"/></svg>'; }
  function _toast(m){ if(typeof toast==='function') toast(m); }
  function normTier(t){ return String(t||'').replace(/\s+/g,'_').toUpperCase(); }
  function curTier(){
    if(typeof PROFILE==='undefined') return 'FREE';
    return normTier(PROFILE.tier||'FREE');
  }

  /* =========================================================
     1) FREE-TRIAL PRO НА 7 ДНЕЙ
     ========================================================= */
  var TRIAL_KEY      = 'oko-pw-trial-v1';
  var TRIAL_OFFERED  = 'oko-pw-trial-offered-v1';
  var TRIAL_MS       = 7*24*3600*1000;
  var TRIAL_RENEW    = 3430;   /* -30% от 4900 = 3430 */
  var TRIAL_NOTIFY_H = 24;     /* показать «продлить со скидкой» за N часов до конца */

  function trialGet(){ try{ return JSON.parse(localStorage.getItem(TRIAL_KEY)||'null'); }catch(e){ return null; } }
  function trialSet(o){ try{ localStorage.setItem(TRIAL_KEY, JSON.stringify(o)); }catch(e){} }
  function trialClear(){ try{ localStorage.removeItem(TRIAL_KEY); }catch(e){} }
  function trialWasOffered(){ try{ return localStorage.getItem(TRIAL_OFFERED)==='1'; }catch(e){ return false; } }
  function trialMarkOffered(){ try{ localStorage.setItem(TRIAL_OFFERED, '1'); }catch(e){} }

  window.pwTrialGet = trialGet;
  window.pwTrialActive = function(){
    var t = trialGet(); return !!(t && t.until && t.until > Date.now());
  };
  window.pwTrialMsLeft = function(){
    var t = trialGet(); return (t && t.until) ? Math.max(0, t.until - Date.now()) : 0;
  };
  window.pwTrialDaysLeft = function(){
    return Math.ceil(window.pwTrialMsLeft() / (24*3600*1000));
  };

  /* Проверка на каждом заходе: если trial истёк — auto-downgrade */
  function trialCheckExpired(){
    var t = trialGet();
    if(!t || !t.until) return;
    if(t.until > Date.now()) return;
    /* trial истёк */
    var paidUpgrade = !!t.paidUpgrade;
    var order = {FREE:0, START:1, PRO:2, BUSINESS:3, BUSINESS_PRO:4, MAX:5};
    var cur = curTier();
    var isPro = cur === 'PRO';
    var wasFromTrial = (isPro && t.trialTier === 'PRO' && !paidUpgrade);
    trialClear();
    if(wasFromTrial && typeof PROFILE !== 'undefined'){
      PROFILE.tier = t.prevTier || 'FREE';
      try{ if(typeof renderMyProfile==='function') renderMyProfile(); }catch(e){}
      setTimeout(function(){
        _toast('Пробный PRO закончился. Продли за 4 900 ₽ или продолжи в FREE.');
      }, 800);
    }
  }
  window.pwTrialCheck = trialCheckExpired;

  /* активация trial: PROFILE.tier -> PRO, trialUntil = now + 7d */
  function trialActivate(){
    var prev = curTier();
    if(prev === 'FREE' || prev === 'START'){
      /* сохраняем предыдущий уровень чтоб корректно откатить */
    } else {
      /* если уже PRO+ — trial не нужен */
      return false;
    }
    var until = Date.now() + TRIAL_MS;
    trialSet({until: until, started: Date.now(), prevTier: prev, trialTier: 'PRO'});
    if(typeof PROFILE !== 'undefined') PROFILE.tier = 'PRO';
    try{ if(typeof renderMyProfile==='function') renderMyProfile(); }catch(e){}
    _toast('Пробный PRO активирован на 7 дней');
    return true;
  }
  window.pwTrialActivate = trialActivate;

  /* Экран «Забери 7 дней PRO бесплатно» */
  function trialCanOffer(){
    if(trialWasOffered()) return false;
    if(window.pwTrialActive()) return false;
    var c = curTier();
    return (c === 'FREE' || c === 'START');
  }
  function trialShow(onClose){
    trialMarkOffered();
    var el = document.createElement('div');
    el.className = 'pw-modal';
    el.id = 'pwTrialSheet';
    el.innerHTML =
      '<div class="pw-modal-card pw-trial">'+
        '<button class="pw-modal-x" aria-label="Закрыть">'+ico('plus')+'</button>'+
        '<div class="pw-trial-hero">'+
          '<div class="pw-trial-ray"></div>'+
          '<div class="pw-trial-badge">'+ico('crown')+'</div>'+
          '<div class="pw-trial-days"><b>7</b><span>дней</span></div>'+
          '<div class="pw-trial-h1">PRO бесплатно</div>'+
          '<div class="pw-trial-sub">Забери 7 дней тарифа PRO без карты и оплаты</div>'+
        '</div>'+
        '<div class="pw-trial-body">'+
          '<ul class="pw-trial-list">'+
            '<li>'+ico('check2')+'<span><b>Система Роста</b> — стратегия и план на 30 дней</span></li>'+
            '<li>'+ico('check2')+'<span><b>Автопостинг</b> во все соцсети из одного окна</span></li>'+
            '<li>'+ico('check2')+'<span><b>Проверка видео без лимита</b> и файлы до 2 ГБ</span></li>'+
            '<li>'+ico('check2')+'<span><b>Скидка −10%</b> на рекламный кабинет</span></li>'+
          '</ul>'+
          '<div class="pw-trial-note">'+ico('lock')+' Карта не нужна — через 7 дней аккаунт автоматически вернётся в FREE, без списаний</div>'+
          '<button class="btn pw-shine pw-trial-cta">'+ico('crown')+' Забрать 7 дней PRO</button>'+
          '<button class="btn ghost pw-trial-skip">Пропустить</button>'+
        '</div>'+
      '</div>';
    document.body.appendChild(el);
    requestAnimationFrame(function(){ el.classList.add('on'); });
    function done(activated){
      el.classList.remove('on');
      setTimeout(function(){ if(el.parentNode) el.remove(); }, 220);
      if(typeof onClose === 'function') onClose(!!activated);
    }
    el.addEventListener('click', function(e){ if(e.target===el) done(false); });
    el.querySelector('.pw-modal-x').onclick = function(){ done(false); };
    el.querySelector('.pw-trial-skip').onclick = function(){ done(false); };
    el.querySelector('.pw-trial-cta').onclick = function(){
      trialActivate();
      done(true);
    };
  }
  window.pwTrialShow = trialShow;

  /* Патчим rg2Next4 — вставляем trial-экран, когда пользователь выбрал FREE в регистрации */
  function patchRg2(){
    if(typeof rg2Next4 !== 'function') return;
    if(rg2Next4.__pwTrialPatched) return;
    var _prev = rg2Next4;
    rg2Next4 = function(){
      var pickedFree = (typeof RG2 !== 'undefined') && (!RG2.tier || RG2.tier === 'FREE');
      if(pickedFree && trialCanOffer()){
        trialShow(function(){
          _prev();
        });
        return;
      }
      return _prev.apply(this, arguments);
    };
    rg2Next4.__pwTrialPatched = true;
  }

  /* Патчим rg2Skip тоже */
  function patchRg2Skip(){
    if(typeof rg2Skip !== 'function') return;
    if(rg2Skip.__pwTrialPatched) return;
    var _prev = rg2Skip;
    rg2Skip = function(){
      if(trialCanOffer()){
        trialShow(function(){
          _prev();
        });
        return;
      }
      return _prev.apply(this, arguments);
    };
    rg2Skip.__pwTrialPatched = true;
  }

  /* «Продли сейчас −30%» — попап, вызывается за 24ч до истечения */
  function trialRenewOffer(){
    if(typeof showPopup !== 'function') return;
    showPopup({
      ico:'crown',
      title:'Пробный PRO заканчивается',
      body:
        '<p style="font-size:13.5px;line-height:1.55;color:var(--dim);margin-bottom:12px">'+
          'Тебе доступна <b>скидка −30%</b> на первый месяц PRO. '+
          'Оформи сейчас, чтобы не потерять Систему Роста, автопостинг и безлимит.'+
        '</p>'+
        '<div class="pw-trial-renew-box">'+
          '<div class="pw-trial-renew-old">4 900 ₽</div>'+
          '<div class="pw-trial-renew-new"><b>'+num(TRIAL_RENEW)+' ₽</b><span>первый месяц</span></div>'+
          '<div class="pw-trial-renew-tag">−30%</div>'+
        '</div>',
      actions:[
        {label:'Продлить со скидкой', onclick:function(){
          try{
            if(typeof payState !== 'undefined'){
              payState.plan = 'PRO';
              payState.period = 1;
              payState._trialRenew = true;
            }
            if(typeof openPay === 'function') openPay('PRO');
          }catch(e){}
        }},
        {label:'Позже', ghost:true},
      ]
    });
  }
  window.pwTrialRenewOffer = trialRenewOffer;

  /* Показать renew-offer один раз за 24ч до конца */
  var RENEW_SHOWN_KEY = 'oko-pw-trial-renew-shown-v1';
  function trialMaybeRenew(){
    var t = trialGet();
    if(!t || !t.until) return;
    var msLeft = t.until - Date.now();
    if(msLeft <= 0) return;
    if(msLeft > TRIAL_NOTIFY_H*3600*1000) return;
    try{ if(localStorage.getItem(RENEW_SHOWN_KEY)==='1') return; }catch(e){}
    try{ localStorage.setItem(RENEW_SHOWN_KEY, '1'); }catch(e){}
    setTimeout(trialRenewOffer, 2500);
  }

  /* Индикатор «PRO trial: осталось X дней» в профиле — добавляем в pw-prof-band */
  function trialInjectBadge(){
    if(!window.pwTrialActive()) return;
    var band = document.getElementById('pwProfBand');
    if(!band) return;
    if(band.querySelector('.pw-trial-badge-inline')) return;
    var days = window.pwTrialDaysLeft();
    var chip = document.createElement('span');
    chip.className = 'pw-trial-badge-inline';
    chip.innerHTML = ico('bolt') + '<span>Trial PRO · '+days+' дн</span>';
    var tx = band.querySelector('.pw-prof-tx');
    if(tx) tx.appendChild(chip);
  }

  /* =========================================================
     2) CROSS-SELL НА CHECKOUT
     ========================================================= */
  var CROSSSELL = {
    /* тариф-который-оплатили → апселл */
    PRO: {
      title:'Возьми ещё Систему Роста',
      sub:'Для тех, кто хочет не просто автопостинг, а результат за 30 дней',
      icon:'rocket',
      old: 1990,
      price: 990,
      save: 50,
      pill: 'экономия 50%',
      cta: 'Добавить Систему Роста · 990 ₽/мес',
      bullets:[
        'Персональный план на 30 дней с целями и метриками',
        'Разбор 15 конкурентов и слепые зоны рынка',
        'Еженедельная проверка результатов и корректировки',
      ],
    },
    BUSINESS: {
      title:'Добавь Контент-завод',
      sub:'Три ролика в день без монтажёра — производство на конвейере',
      icon:'bolt',
      old: 49900,
      price: 25000,
      save: 50,
      pill: 'первый месяц −50%',
      cta: 'Добавить Контент-завод · 25 000 ₽',
      bullets:[
        '100 роликов в месяц: сценарий, монтаж, озвучка, субтитры',
        'Формат под VK Clips, TikTok, Reels, Shorts, YT-Long',
        'Отчёт по охватам и точкам роста каждую неделю',
      ],
    },
    BUSINESS_PRO: {
      title:'Апгрейд до MAX на месяц',
      sub:'500 роликов, Higgsfield безлимит, бренд-агентство под ключ',
      icon:'crown',
      old: 149900,
      price: 99900,
      save: 33,
      pill: '−33% в первый месяц',
      cta: 'Апгрейд MAX · 99 900 ₽ за 1 мес',
      bullets:[
        'Контент-завод 500 роликов вместо 100',
        'Higgsfield безлимит + личный менеджер',
        'Пакет «Бренд-агентство» — сайт, лендинг, боты',
      ],
    },
  };

  function showCrossSell(paidTier){
    var t = normTier(paidTier);
    var offer = CROSSSELL[t];
    if(!offer) return;
    var el = document.createElement('div');
    el.className = 'pw-modal';
    el.id = 'pwCross';
    el.innerHTML =
      '<div class="pw-modal-card pw-cross">'+
        '<button class="pw-modal-x" aria-label="Закрыть">'+ico('plus')+'</button>'+
        '<div class="pw-cross-top">'+
          '<div class="pw-cross-ic">'+ico(offer.icon)+'</div>'+
          '<div class="pw-cross-pill">'+_esc(offer.pill)+'</div>'+
        '</div>'+
        '<h3 class="pw-cross-h">'+_esc(offer.title)+'</h3>'+
        '<p class="pw-cross-sub">'+_esc(offer.sub)+'</p>'+
        '<ul class="pw-cross-list">'+ offer.bullets.map(function(b){
          return '<li>'+ico('check2')+'<span>'+_esc(b)+'</span></li>';
        }).join('') +'</ul>'+
        '<div class="pw-cross-price">'+
          '<div class="pw-cross-old">'+num(offer.old)+' ₽</div>'+
          '<div class="pw-cross-new">'+num(offer.price)+' ₽</div>'+
          '<div class="pw-cross-save">−'+offer.save+'%</div>'+
        '</div>'+
        '<button class="btn pw-shine pw-cross-cta">'+_esc(offer.cta)+'</button>'+
        '<button class="btn ghost pw-cross-skip">Не сейчас</button>'+
      '</div>';
    document.body.appendChild(el);
    requestAnimationFrame(function(){ el.classList.add('on'); });
    function done(){
      el.classList.remove('on');
      setTimeout(function(){ if(el.parentNode) el.remove(); }, 220);
    }
    el.addEventListener('click', function(e){ if(e.target===el) done(); });
    el.querySelector('.pw-modal-x').onclick = done;
    el.querySelector('.pw-cross-skip').onclick = done;
    el.querySelector('.pw-cross-cta').onclick = function(){
      done();
      _toast('Добавка «'+offer.title+'» подключена. Спасибо!');
      try{
        var bal = parseInt(localStorage.getItem('oko-pw-crosssell-count')||'0',10)||0;
        localStorage.setItem('oko-pw-crosssell-count', String(bal+1));
        localStorage.setItem('oko-pw-crosssell-last', String(offer.price));
      }catch(e){}
    };
  }
  window.pwShowCrossSell = showCrossSell;

  /* Патчим doPay — после успешной оплаты (~2000ms) показываем cross-sell */
  function patchDoPay(){
    if(typeof doPay !== 'function') return;
    if(doPay.__pwCrossPatched) return;
    var _prev = doPay;
    doPay = function(){
      var picked = (typeof payState !== 'undefined') ? normTier(payState.plan) : '';
      var r = _prev.apply(this, arguments);
      /* реальная оплата — trial больше не «активен от нас», не даунгрейдим */
      setTimeout(function(){
        var t = trialGet();
        if(t){
          /* пометим trial как «переросший в реальную оплату» — не будем откатывать tier */
          t.paidUpgrade = true;
          trialSet(t);
        }
      }, 1800);
      if(CROSSSELL[picked]){
        setTimeout(function(){
          /* показываем cross-sell после успешной оплаты (симуляция ~1600ms) */
          if(document.getElementById('sheet-pay') && document.getElementById('sheet-pay').classList.contains('open')){
            showCrossSell(picked);
          }
        }, 2200);
      }
      return r;
    };
    doPay.__pwCrossPatched = true;
  }

  /* =========================================================
     3) BUNDLE (комбо-пакеты) — отдельный экран/попап
     ========================================================= */
  var BUNDLES = [
    {
      id:'all',
      name:'Всё OKO',
      tag:'Максимальный комбо',
      icon:'crown',
      old: 4900 + 990 + 990 + 590 + 990,   /* PRO + Академия + Аналитика + Партнёрка + Реклама */
      priceNote:'−40%',
      discount: 40,
      bullets:[
        'PRO (Система Роста, автопостинг, все соцсети)',
        'Академия премиум · все курсы и разборы',
        'Аналитика соцсетей · 10 каналов',
        'Партнёрка Pro · промо-материалы и приоритет',
        'Рекламный кабинет Pro · без комиссий OKO',
      ],
    },
    {
      id:'start',
      name:'Стартовый пакет',
      tag:'Начни с сильной базы',
      icon:'rocket',
      old: 3990,
      price: 2490,
      priceNote:'−38%',
      discount: 38,
      bullets:[
        'START тариф (Premium-мессенджер, 30 проверок)',
        'Академия премиум с сертификатом',
        'Магазин шаблонов + Каталог трендов',
      ],
    },
    {
      id:'pro',
      name:'Про пакет',
      tag:'Всё для быстрого роста',
      icon:'bolt',
      old: 4900 + 490 + 990,     /* PRO + Партнёрка Pro + Реклама Pro */
      priceNote:'−35%',
      discount: 35,
      bullets:[
        'PRO тариф (Система Роста + автопостинг)',
        'Партнёрка Pro (промо, приоритет, повышенный %)',
        'Рекламный кабинет Pro (без комиссий OKO)',
      ],
    },
    {
      id:'biz',
      name:'Бизнес пакет',
      tag:'Команда и производство',
      icon:'users',
      old: 19900 + 25000,        /* BUSINESS + Контент-завод 30 роликов */
      priceNote:'−30%',
      discount: 30,
      bullets:[
        'BUSINESS тариф (белый лейбл, академия премиум)',
        'Контент-завод · 30 роликов в месяц под ключ',
        '3 команды-места и общий штаб задач',
      ],
    },
  ];
  function bundleFinalPrice(b){
    if(b.price != null) return b.price;
    return Math.round(b.old * (1 - b.discount/100));
  }
  BUNDLES.forEach(function(b){ b.price = bundleFinalPrice(b); });

  function bundlesHtml(){
    return '<div class="bnd-list">'+
      BUNDLES.map(function(b){
        return '<div class="bnd-card">'+
          '<div class="bnd-card-top">'+
            '<div class="bnd-card-ic">'+ico(b.icon)+'</div>'+
            '<div class="bnd-card-tag">'+_esc(b.tag)+'</div>'+
            '<div class="bnd-card-disc">'+b.priceNote+'</div>'+
          '</div>'+
          '<h4 class="bnd-card-h">'+_esc(b.name)+'</h4>'+
          '<ul class="bnd-card-list">'+ b.bullets.map(function(x){
            return '<li>'+ico('check2')+'<span>'+_esc(x)+'</span></li>';
          }).join('') +'</ul>'+
          '<div class="bnd-card-price">'+
            '<div class="bnd-old">'+num(b.old)+' ₽</div>'+
            '<div class="bnd-new"><b>'+num(b.price)+'</b><span> ₽/мес</span></div>'+
          '</div>'+
          '<button class="btn pw-shine bnd-cta" onclick="bndTakePack(\''+b.id+'\')">'+ico('crown')+' Взять пакет</button>'+
        '</div>';
      }).join('') +
    '</div>';
  }

  function openBundles(){
    var el = document.createElement('div');
    el.className = 'pw-modal';
    el.id = 'bndSheet';
    el.innerHTML =
      '<div class="pw-modal-card bnd-sheet">'+
        '<button class="pw-modal-x" aria-label="Закрыть">'+ico('plus')+'</button>'+
        '<div class="bnd-head">'+
          '<div class="bnd-head-k">'+ico('gift')+' Комбо-пакеты</div>'+
          '<h2>Экономь до 40% · возьми всё сразу</h2>'+
          '<p>Собери свой набор из тарифа и продуктов OKO — выгоднее, чем брать по отдельности.</p>'+
        '</div>'+
        bundlesHtml()+
        '<p class="bnd-note">'+ico('lock')+' Отключишь в 1 клик · безопасная оплата · гарантия возврата 14 дней</p>'+
      '</div>';
    document.body.appendChild(el);
    requestAnimationFrame(function(){ el.classList.add('on'); });
    function done(){
      el.classList.remove('on');
      setTimeout(function(){ if(el.parentNode) el.remove(); }, 220);
    }
    el.addEventListener('click', function(e){ if(e.target===el) done(); });
    el.querySelector('.pw-modal-x').onclick = done;
  }
  window.bndOpenBundles = openBundles;

  window.bndTakePack = function(id){
    var b = BUNDLES.filter(function(x){return x.id===id})[0];
    if(!b) return;
    /* закрываем bundle-модалку */
    var el = document.getElementById('bndSheet');
    if(el){ el.classList.remove('on'); setTimeout(function(){ if(el.parentNode) el.remove(); }, 220); }
    _toast('Пакет «'+b.name+'» оформлен · '+num(b.price)+' ₽/мес');
    /* сохранить факт покупки пакета */
    try{
      localStorage.setItem('oko-bnd-active', id);
      localStorage.setItem('oko-bnd-active-at', String(Date.now()));
    }catch(e){}
    /* если это Про/Бизнес — поднять tier */
    if(id === 'pro' && typeof PROFILE !== 'undefined') PROFILE.tier = 'PRO';
    if(id === 'biz' && typeof PROFILE !== 'undefined') PROFILE.tier = 'BUSINESS';
    if(id === 'all' && typeof PROFILE !== 'undefined') PROFILE.tier = 'PRO';
    try{ if(typeof renderMyProfile==='function') renderMyProfile(); }catch(e){}
  };

  /* =========================================================
     4) МИКРО-ПОДПИСКИ (для тех, кому не нужен весь Business)
     ========================================================= */
  var MICROSUBS = [
    {id:'academy',  ic:'flag',      name:'Только Академия',              price: 990, sub:'Доступ ко всем курсам, разборы, сертификаты', bullets:['Все курсы Академии OKO','Живые разборы и уроки','Сертификат по итогу']},
    {id:'analytics',ic:'poll',      name:'Только Аналитика соцсетей',    price: 590, sub:'10 каналов · охваты, аудитория, рост подписок', bullets:['10 каналов на 1 подписку','Динамика охватов и роста','Сравнение с конкурентами']},
    {id:'partner',  ic:'users',     name:'Только Партнёрка Pro',         price: 490, sub:'Промо-материалы, приоритетные выплаты, повышенный %', bullets:['Готовые баннеры, тексты, Reels-скрипты','Выплаты за 6-12 часов','+2% к базовому проценту']},
    {id:'ads',      ic:'megaphone', name:'Только Реклама Pro',           price: 990, sub:'Рекламный кабинет без комиссий OKO', bullets:['0% комиссии OKO на пополнения','Расширенные ставки и ЦА','Приоритетная модерация']},
  ];

  function microHtml(){
    return '<div class="bnd-micro-list">'+
      MICROSUBS.map(function(m){
        return '<div class="bnd-micro-card">'+
          '<div class="bnd-micro-ic">'+ico(m.ic)+'</div>'+
          '<div class="bnd-micro-body">'+
            '<div class="bnd-micro-h">'+_esc(m.name)+'</div>'+
            '<div class="bnd-micro-sub">'+_esc(m.sub)+'</div>'+
            '<ul class="bnd-micro-feats">'+ m.bullets.map(function(x){
              return '<li>'+ico('check2')+'<span>'+_esc(x)+'</span></li>';
            }).join('') +'</ul>'+
          '</div>'+
          '<div class="bnd-micro-price">'+
            '<b>'+num(m.price)+'</b><span>₽/мес</span>'+
            '<button class="btn bnd-micro-cta" onclick="bndTakeMicro(\''+m.id+'\')">'+ico('crown')+' Взять</button>'+
          '</div>'+
        '</div>';
      }).join('') +
    '</div>';
  }

  function openMicroSubs(){
    var el = document.createElement('div');
    el.className = 'pw-modal';
    el.id = 'bndMicro';
    el.innerHTML =
      '<div class="pw-modal-card bnd-sheet">'+
        '<button class="pw-modal-x" aria-label="Закрыть">'+ico('plus')+'</button>'+
        '<div class="bnd-head">'+
          '<div class="bnd-head-k">'+ico('bolt')+' Микро-подписки</div>'+
          '<h2>Возьми только то, что нужно</h2>'+
          '<p>Не хочется весь Business? Подключи один продукт OKO отдельно — от 490 ₽/мес.</p>'+
        '</div>'+
        microHtml()+
        '<p class="bnd-note">'+ico('lock')+' Отключишь в 1 клик · без карты не спишем · гарантия возврата 14 дней</p>'+
      '</div>';
    document.body.appendChild(el);
    requestAnimationFrame(function(){ el.classList.add('on'); });
    function done(){
      el.classList.remove('on');
      setTimeout(function(){ if(el.parentNode) el.remove(); }, 220);
    }
    el.addEventListener('click', function(e){ if(e.target===el) done(); });
    el.querySelector('.pw-modal-x').onclick = done;
  }
  window.bndOpenMicroSubs = openMicroSubs;
  window.bndTakeMicro = function(id){
    var m = MICROSUBS.filter(function(x){return x.id===id})[0];
    if(!m) return;
    var el = document.getElementById('bndMicro');
    if(el){ el.classList.remove('on'); setTimeout(function(){ if(el.parentNode) el.remove(); }, 220); }
    _toast('Микро-подписка «'+m.name+'» оформлена · '+num(m.price)+' ₽/мес');
    try{
      var raw = localStorage.getItem('oko-bnd-micro')||'[]';
      var arr = JSON.parse(raw); if(!Array.isArray(arr)) arr = [];
      if(arr.indexOf(id) < 0) arr.push(id);
      localStorage.setItem('oko-bnd-micro', JSON.stringify(arr));
    }catch(e){}
  };

  /* =========================================================
     5) LTV-ОТЧЁТ В ПРОФИЛЕ (сколько сэкономил на OKO за месяц)
     ========================================================= */
  function ltvNumbers(){
    /* стабильный псевдо-рандом от месяца, чтобы цифры не прыгали внутри дня */
    var d = new Date();
    var seed = d.getFullYear()*100 + d.getMonth();
    var t = curTier();
    /* базовая экономия зависит от тарифа */
    var basis = {FREE:8000, START:18000, PRO:62000, BUSINESS:184000, BUSINESS_PRO:340000, MAX:820000}[t] || 20000;
    var jitter = (seed % 17) / 100;
    var total = Math.round(basis * (1 + jitter));
    var hours = Math.round(total / 1800);        /* усреднённая цена часа копирайта/дизайна ≈ 1800₽ */
    var toolsSaved = Math.round(total * 0.42);   /* доля «на инструментах» */
    var timeSaved  = Math.round(total * 0.58);   /* доля «на времени» */
    return {total: total, hours: hours, toolsSaved: toolsSaved, timeSaved: timeSaved};
  }

  function ltvWidgetHtml(){
    var n = ltvNumbers();
    return '<div class="pw-ltv card" id="pwLtv">'+
      '<div class="pw-ltv-head">'+
        '<div class="pw-ltv-ic">'+ico('bolt')+'</div>'+
        '<div class="pw-ltv-cap"><b>Ты сэкономил на OKO за месяц</b><small>сумма ниже — против классических инструментов и подрядчиков</small></div>'+
      '</div>'+
      '<div class="pw-ltv-big">'+num(n.total)+' ₽</div>'+
      '<div class="pw-ltv-hours">'+ico('clock')+' ≈ '+num(n.hours)+' ч свободного времени</div>'+
      '<div class="pw-ltv-split">'+
        '<div class="pw-ltv-cell">'+
          '<div class="pw-ltv-c-l">На инструментах</div>'+
          '<div class="pw-ltv-c-v">'+num(n.toolsSaved)+' ₽</div>'+
          '<div class="pw-ltv-bar"><i style="width:42%"></i></div>'+
        '</div>'+
        '<div class="pw-ltv-cell">'+
          '<div class="pw-ltv-c-l">На времени</div>'+
          '<div class="pw-ltv-c-v">'+num(n.timeSaved)+' ₽</div>'+
          '<div class="pw-ltv-bar"><i style="width:58%"></i></div>'+
        '</div>'+
      '</div>'+
      '<button class="btn ghost pw-ltv-share" onclick="pwLtvShare()">'+ico('send')+' Поделиться результатом</button>'+
    '</div>';
  }

  window.pwLtvShare = function(){
    var n = ltvNumbers();
    var txt = 'Я сэкономил '+num(n.total)+' ₽ на OKO за месяц · ≈ '+num(n.hours)+' часов свободного времени. Одно приложение вместо десяти. okoteam.top';
    try{
      if(navigator.share){
        navigator.share({title:'OKO — сэкономил '+num(n.total)+' ₽', text:txt, url:'https://okoteam.top'});
        return;
      }
    }catch(e){}
    try{ if(navigator.clipboard) navigator.clipboard.writeText(txt); }catch(e){}
    _toast('Текст скопирован — поделись в соцсети');
  };

  function ltvInject(){
    var scr = document.getElementById('screen-profile');
    if(!scr) return;
    if(scr.querySelector('#pwLtv')) return;
    var pad = scr.querySelector('.pad');
    if(!pad) return;
    /* вставляем перед section-h «Тарифы» либо перед pw-prof-band, или в конец */
    var anchor = pad.querySelector('#pwProfBand') || pad.querySelector('.section-h') || null;
    var wrap = document.createElement('div');
    wrap.innerHTML = ltvWidgetHtml();
    var node = wrap.firstElementChild;
    if(anchor && anchor.parentNode === pad){
      pad.insertBefore(node, anchor);
    } else {
      pad.appendChild(node);
    }
  }

  /* Также добавим кнопки «Комбо-пакеты» и «Микро-подписки» рядом с LTV */
  function bundlesEntryInject(){
    var scr = document.getElementById('screen-profile');
    if(!scr) return;
    if(scr.querySelector('#bndEntry')) return;
    var pad = scr.querySelector('.pad');
    if(!pad) return;
    var anchor = pad.querySelector('#pwLtv') || pad.querySelector('#pwProfBand') || null;
    var wrap = document.createElement('div');
    wrap.id = 'bndEntry';
    wrap.className = 'bnd-entry';
    wrap.innerHTML =
      '<button class="bnd-entry-btn" onclick="bndOpenBundles()">'+
        '<span class="bnd-entry-ic">'+ico('gift')+'</span>'+
        '<span class="bnd-entry-tx"><b>Комбо-пакеты</b><small>Всё OKO, Стартовый, Про, Бизнес — до −40%</small></span>'+
        '<span class="bnd-entry-chev">'+ico('chev')+'</span>'+
      '</button>'+
      '<button class="bnd-entry-btn" onclick="bndOpenMicroSubs()">'+
        '<span class="bnd-entry-ic bnd-entry-ic-alt">'+ico('bolt')+'</span>'+
        '<span class="bnd-entry-tx"><b>Микро-подписки</b><small>Только один продукт OKO · от 490 ₽/мес</small></span>'+
        '<span class="bnd-entry-chev">'+ico('chev')+'</span>'+
      '</button>';
    if(anchor && anchor.parentNode === pad){
      /* вставляем сразу после anchor */
      if(anchor.nextSibling) pad.insertBefore(wrap, anchor.nextSibling);
      else pad.appendChild(wrap);
    } else {
      pad.appendChild(wrap);
    }
  }

  /* =========================================================
     6) ГАРАНТИЯ 14 ДНЕЙ — плашка на checkout (sheet-pay)
     ========================================================= */
  function injectGuarantee(){
    var view = document.getElementById('payView');
    if(!view) return;
    if(view.querySelector('.pw-14d-guar')) return;
    var band = document.createElement('div');
    band.className = 'pw-14d-guar';
    band.innerHTML =
      '<div class="pw-14d-ic">'+ico('check2')+'</div>'+
      '<div class="pw-14d-tx"><b>Гарантия возврата 14 дней</b>'+
        '<small>Не понравилось? Вернём деньги без вопросов в течение 14 дней.</small></div>';
    /* вставляем над кнопкой Оплатить */
    var payBtn = view.querySelector('button.btn');
    var target = null;
    /* найти финальную кнопку "Оплатить" */
    Array.prototype.forEach.call(view.querySelectorAll('button.btn'), function(b){
      if(/Оплатить/i.test(b.textContent||'')) target = b;
    });
    if(target && target.parentNode){
      target.parentNode.insertBefore(band, target);
    } else if(payBtn && payBtn.parentNode){
      payBtn.parentNode.insertBefore(band, payBtn);
    } else {
      view.appendChild(band);
    }
  }

  /* Патчим renderPay — после каждого рендера подсовываем плашку */
  function patchRenderPay(){
    if(typeof renderPay !== 'function') return;
    if(renderPay.__pwGuarPatched) return;
    var _prev = renderPay;
    renderPay = function(){
      var r = _prev.apply(this, arguments);
      try{ injectGuarantee(); }catch(e){}
      return r;
    };
    renderPay.__pwGuarPatched = true;
  }

  /* Также патчим renderMyProfile → инжектим LTV + entry + trial-бейдж */
  function patchRenderMyProfile(){
    if(typeof renderMyProfile !== 'function') return;
    if(renderMyProfile.__pwExtrasPatched) return;
    var _prev = renderMyProfile;
    renderMyProfile = function(){
      var r = _prev.apply(this, arguments);
      try{ ltvInject(); }catch(e){}
      try{ bundlesEntryInject(); }catch(e){}
      try{ trialInjectBadge(); }catch(e){}
      return r;
    };
    renderMyProfile.__pwExtrasPatched = true;
  }

  /* =========================================================
     ИНИЦИАЛИЗАЦИЯ
     ========================================================= */
  function initExtras(){
    trialCheckExpired();
    patchRg2();
    patchRg2Skip();
    patchDoPay();
    patchRenderPay();
    patchRenderMyProfile();
    trialMaybeRenew();
    /* если профиль уже отрисован — вставим виджеты сразу */
    setTimeout(function(){
      try{ ltvInject(); }catch(e){}
      try{ bundlesEntryInject(); }catch(e){}
      try{ trialInjectBadge(); }catch(e){}
    }, 400);
  }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded', initExtras);
  else initExtras();
  /* повторная попытка — на случай если модули (registration, base) подгрузили функции позже */
  setTimeout(initExtras, 900);
  setTimeout(initExtras, 2400);

  /* также перепроверяем trial каждую минуту (если юзер сидит долго — авто-даунгрейд сработает) */
  setInterval(function(){
    trialCheckExpired();
    trialMaybeRenew();
  }, 60*1000);

})();

/* ===== module: partner-plus ===== */
/* =================== PARTNER-PLUS ===================
   Партнёрский кабинет уровня Amazon Associates + Lava Partners + Getcourse:
   hero-дашборд с count-up и sparkline, топ-3 источника, дип-линки на все
   продукты OKO, QR-код, промо-пак (тексты + баннеры + reels-сценарии),
   turnover-лестница из 5 уровней, wall of fame, история выплат с фильтром,
   запрос выплаты (мин. 1000 ₽) на Lava/USDT/карту, живая слайд-плашка,
   welcome-онбординг с 15%-обещанием.
   Не трогает base.html: всё вставляется в существующий #screen-partner. */

(function partnerPlus(){
  'use strict';

  /* ---------- иконки: трофей, медаль, звезда-заливка, поделиться и др. ---------- */
  (function addIcons(){
    const defs = document.querySelector('svg defs');
    if(!defs) return;
    function sym(id, vb, inner){
      if(document.getElementById(id)) return;
      const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
      s.setAttribute('id', id); s.setAttribute('viewBox', vb);
      s.innerHTML = inner; defs.appendChild(s);
    }
    sym('i-pp-trophy', '0 0 100 100',
      '<path d="M30 20h40v18c0 12-9 22-20 22S30 50 30 38V20z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>' +
      '<path d="M30 26H18c0 10 5 18 12 20M70 26h12c0 10-5 18-12 20" fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round"/>' +
      '<path d="M42 62h16v10H42zM34 78h32v6H34z" fill="currentColor"/>');
    sym('i-pp-medal', '0 0 100 100',
      '<circle cx="50" cy="58" r="22" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<path d="M32 40 22 14h20l10 20M68 40l10-26H58L48 34" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>');
    sym('i-pp-qr', '0 0 100 100',
      '<rect x="10" y="10" width="26" height="26" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<rect x="18" y="18" width="10" height="10" fill="currentColor"/>' +
      '<rect x="64" y="10" width="26" height="26" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<rect x="72" y="18" width="10" height="10" fill="currentColor"/>' +
      '<rect x="10" y="64" width="26" height="26" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<rect x="18" y="72" width="10" height="10" fill="currentColor"/>' +
      '<rect x="46" y="10" width="6" height="14" fill="currentColor"/>' +
      '<rect x="46" y="30" width="6" height="6" fill="currentColor"/>' +
      '<rect x="46" y="46" width="14" height="6" fill="currentColor"/>' +
      '<rect x="66" y="46" width="6" height="6" fill="currentColor"/>' +
      '<rect x="46" y="60" width="6" height="6" fill="currentColor"/>' +
      '<rect x="60" y="60" width="14" height="14" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<rect x="82" y="60" width="8" height="8" fill="currentColor"/>' +
      '<rect x="82" y="76" width="8" height="14" fill="currentColor"/>' +
      '<rect x="60" y="82" width="14" height="8" fill="currentColor"/>');
    sym('i-pp-share', '0 0 100 100',
      '<circle cx="22" cy="50" r="10" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<circle cx="74" cy="22" r="10" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<circle cx="74" cy="78" r="10" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<path d="M31 46 65 27M31 54 65 73" fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round"/>');
    sym('i-pp-up', '0 0 100 100',
      '<path d="M14 68 42 40l16 16 28-30M62 26h24v24" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/>');
    sym('i-pp-ig', '0 0 100 100',
      '<rect x="14" y="14" width="72" height="72" rx="20" fill="none" stroke="currentColor" stroke-width="7"/>' +
      '<circle cx="50" cy="50" r="16" fill="none" stroke="currentColor" stroke-width="7"/>' +
      '<circle cx="72" cy="28" r="5" fill="currentColor"/>');
    sym('i-pp-tg', '0 0 100 100',
      '<path d="M88 14 14 44c-3 1-3 5 0 6l20 7c2 1 3 2 4 4l7 20c1 3 5 3 6 0L81 22c1-4-2-7-6-6z" fill="currentColor"/>');
    sym('i-pp-yt', '0 0 100 100',
      '<rect x="10" y="24" width="80" height="52" rx="13" fill="currentColor"/>' +
      '<path d="M44 38v24l20-12z" fill="var(--surface)"/>');
    sym('i-pp-vk', '0 0 100 100',
      '<rect x="10" y="10" width="80" height="80" rx="18" fill="currentColor"/>' +
      '<path d="M22 36c2 22 12 32 30 32h4v-14c2 0 4 2 8 6l8 8h10c-6-8-9-11-14-16-2-2-2-4 0-6 4-6 12-14 12-18h-10c-4 4-8 10-12 14-3 3-4 2-4-2V28h-4c-8 0-12 4-14 6 0 0 6 0 6 6v10c0 4-2 4-4 2-4-4-8-12-10-16z" fill="var(--surface)"/>');
    sym('i-pp-link', '0 0 100 100',
      '<path d="M42 58c-3-3-3-9 0-12l16-16c3-3 9-3 12 0l4 4c3 3 3 9 0 12L64 54M58 42c3 3 3 9 0 12L42 70c-3 3-9 3-12 0l-4-4c-3-3-3-9 0-12l10-10" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round"/>');
    sym('i-pp-usdt', '0 0 100 100',
      '<circle cx="50" cy="50" r="38" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<path d="M28 36h44v10H56v22c-4 1-8 1-12 0V46H28z M38 58c8 3 16 3 24 0" fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round" stroke-linejoin="round"/>');
    sym('i-pp-clip', '0 0 100 100',
      '<rect x="24" y="14" width="52" height="72" rx="8" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<path d="M36 14V10a4 4 0 0 1 4-4h20a4 4 0 0 1 4 4v4M36 40h28M36 54h28M36 68h20" fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round"/>');
    sym('i-pp-image', '0 0 100 100',
      '<rect x="12" y="18" width="76" height="64" rx="10" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<circle cx="34" cy="40" r="7" fill="currentColor"/>' +
      '<path d="M14 72 36 52l14 12 12-10 24 18" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>');
    sym('i-pp-play', '0 0 100 100',
      '<circle cx="50" cy="50" r="38" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<path d="M42 34 68 50 42 66z" fill="currentColor"/>');
    sym('i-pp-gift', '0 0 100 100',
      '<rect x="14" y="40" width="72" height="46" rx="6" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<path d="M14 56h72M50 40v46" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<path d="M50 40s-4-18-14-18-8 18 14 18zM50 40s4-18 14-18 8 18-14 18z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>');
    sym('i-pp-rocket', '0 0 100 100',
      '<path d="M52 12c14 4 26 20 26 40 0 6-2 12-6 18l-14 4-4-14-14-4c6-4 12-6 18-6 20 0 36-12 40-26-4-14-20-16-46-12z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>' +
      '<circle cx="62" cy="38" r="5" fill="currentColor"/>' +
      '<path d="M40 60c-4 4-6 12-6 20 8 0 16-2 20-6" fill="none" stroke="currentColor" stroke-width="6"/>');
    sym('i-pp-badge-check', '0 0 100 100',
      '<path d="M50 8l12 8 14-2 4 14 14 4-2 14 8 12-8 12 2 14-14 4-4 14-14-2-12 8-12-8-14 2-4-14-14-4 2-14L4 74l8-12-2-14 14-4 4-14 14 2z" fill="currentColor"/>' +
      '<path d="M32 52l12 12 24-30" fill="none" stroke="var(--surface)" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/>');
    sym('i-pp-calc', '0 0 100 100',
      '<rect x="20" y="10" width="60" height="80" rx="8" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<rect x="28" y="18" width="44" height="16" rx="3" fill="currentColor"/>' +
      '<circle cx="36" cy="46" r="4" fill="currentColor"/>' +
      '<circle cx="50" cy="46" r="4" fill="currentColor"/>' +
      '<circle cx="64" cy="46" r="4" fill="currentColor"/>' +
      '<circle cx="36" cy="60" r="4" fill="currentColor"/>' +
      '<circle cx="50" cy="60" r="4" fill="currentColor"/>' +
      '<circle cx="64" cy="60" r="4" fill="currentColor"/>' +
      '<circle cx="36" cy="74" r="4" fill="currentColor"/>' +
      '<circle cx="50" cy="74" r="4" fill="currentColor"/>' +
      '<circle cx="64" cy="74" r="4" fill="currentColor"/>');
  })();

  /* ---------- профиль партнёра ---------- */
  /* Партнёрский профиль стартует с нуля (правка Даниэля 09.08).
     Раньше здесь были выдуманные 47 350 ₽ «заработано в этом месяце»,
     128 740 ₽ оборота и 12 780 ₽ «к выплате» — человек видел чужие деньги
     как свои. Суммы приходят из партнёрского бэкенда. */
  const PP = {
    nick: (typeof PROFILE !== 'undefined' && PROFILE.nick) ? String(PROFILE.nick).replace(/^@/, '') : '',
    monthEarned: 0,          // ₽ заработано в этом месяце
    monthPrev:   0,          // ₽ за прошлый месяц
    turnoverAll: 0,          // ₽ всего оборота (для лестницы)
    weekTurnover: 0,         // ₽ за эту неделю (для wall of fame)
    payoutAvail: 0,          // ₽ доступно к выводу
    payoutMin:   1000,       // ₽ порог
    products: [
      {k:'all',    label:'Все', short:'all', dot:false},
      {k:'sistema',label:'Система OKO',   short:'sistema', price:4900,  desc:'PRO подписка · 4 900 ₽/мес', dot:true},
      {k:'zavod',  label:'Контент-завод', short:'zavod',   price:2900,  desc:'Reels-конвейер · 2 900 ₽/мес', dot:true},
      {k:'consult',label:'Консалтинг',    short:'consult', price:30000, desc:'Личный разбор · 30 000 ₽', dot:true},
      {k:'club',   label:'Клуб OKO',      short:'club',    price:1900,  desc:'Сообщество · 1 900 ₽/мес', dot:true},
    ],
    activeProduct: 'all',
  };

  const REF_PCT_FIRST = 15;    // % с первой оплаты (config.php: partner_percent)
  const REF_PCT_REPEAT = 5;    // % с повторных
  const REF_PCT_L2     = 5;    // % со 2-й линии

  /* ---------- turnover-лестница из 5 уровней ---------- */
  const TIERS = [
    {k:'junior', label:'Junior', min:0,        max:10000,    bonus:0,  badge:'pp-medal'},
    {k:'middle', label:'Middle', min:10000,    max:50000,    bonus:5,  badge:'pp-medal'},
    {k:'senior', label:'Senior', min:50000,    max:200000,   bonus:10, badge:'pp-medal'},
    {k:'pro',    label:'Pro',    min:200000,   max:1000000,  bonus:15, badge:'pp-trophy'},
    {k:'legend', label:'Legend', min:1000000,  max:Infinity, bonus:20, badge:'crown'},
  ];
  function currentTier(turnover){
    let idx = 0;
    for(let i=TIERS.length-1;i>=0;i--) if(turnover >= TIERS[i].min){ idx = i; break; }
    return {idx, tier:TIERS[idx], next:TIERS[idx+1]||null};
  }

  /* ---------- источники трафика (топ-3+) ---------- */
  /* Каналы привлечения — справочник. Суммы обнулены 09.08: заполняются
     реальной атрибуцией переходов по реф-ссылке. */
  const SOURCES = [
    {k:'ig',  ic:'pp-ig',  name:'Instagram',      sum:0, hint:'сторис · reels'},
    {k:'tg',  ic:'pp-tg',  name:'Telegram-канал', sum:0, hint:'посты · закреп'},
    {k:'yt',  ic:'pp-yt',  name:'YouTube',        sum:0, hint:'обзоры · описание'},
    {k:'vk',  ic:'pp-vk',  name:'VK',             sum:0, hint:'клипы · группа'},
    {k:'dir', ic:'pp-link',name:'Прямые ссылки',  sum:0, hint:'визитка · QR'},
  ];

  /* ---------- доска лидеров ----------
     Десять выдуманных партнёров с оборотами до 184 300 ₽ удалены 09.08.
     Доска наполняется реальными партнёрами; пока пусто — показываем
     пустое состояние, а не чужие достижения. */
  const LEADERS = [];

  /* ---------- промо-материалы (тексты, баннеры, reels) ---------- */
  const PROMO = [
    // тексты для соцсетей
    { kind:'text', k:'ig-story', h:'Один инструмент вместо десяти', sub:'IG Stories · 1080×1920',
      gr:'g1', fmt:'1080×1920', tag:'Instagram',
      copy:'OKO · я снёс 8 приложений и оставил одно.\n\nМессенджер + лента + мини-аппы + кошелёк + академия · всё внутри одного окна.\n\nЗабирай по моей ссылке · я приготовил скидку -15% на первый месяц PRO.\n\n{ref}\n\n#OKO #продуктивность'
    },
    { kind:'text', k:'tg-post', h:'Пост в Telegram: почему я перевёл команду', sub:'TG · длинный пост',
      gr:'g4', fmt:'Telegram', tag:'Telegram',
      copy:'Почему я перевёл всю команду в OKO.\n\nБыло: Notion + Telegram + CapCut + пять таблиц. Работало, но каждое утро я 40 минут собирал контекст.\n\nСтало: одно окно OKO. Чаты, лента, кошелёк, академия, биржа услуг · всё синхронно и на русском рынке.\n\nЧто зашло сильнее всего:\n· контент-завод собирает Reels 1080×1920 без монтажёра\n· кошелёк с эскроу на сделки внутри\n· партнёрка 15% + 5% сверху со второй линии\n\nСсылка ниже. Первый месяц PRO — со скидкой по моей ссылке.\n\n{ref}'
    },
    { kind:'text', k:'vk-post', h:'Пост во ВКонтакте / Дзен', sub:'VK · Дзен',
      gr:'g7', fmt:'VK', tag:'ВК/Дзен',
      copy:'5 фишек OKO, которых нет у Telegram:\n\n1. Партнёрка 15% / 5% прямо внутри приложения · выплаты на карту РФ, USDT или Lava.top.\n2. Кошелёк с эскроу для сделок между людьми.\n3. Академия с сертификатом и уроками по нейронкам.\n4. Биржа услуг под ключ с рейтингом исполнителей.\n5. Контент-завод: Reels 1080×1920 с озвучкой и субтитрами без монтажёра.\n\nВходить сюда: {ref}\nПри переходе по моей ссылке скинут первый месяц PRO.'
    },
    { kind:'text', k:'ig-caption', h:'Подпись под пост в Instagram', sub:'IG · Feed 1080×1080',
      gr:'g3', fmt:'1080×1080', tag:'Instagram',
      copy:'Раньше монтаж = 3 часа. Теперь · 8 минут.\n\nОткрываю Контент-завод в OKO, кидаю тему · получаю Reels 1080×1920 с озвучкой и караоке-субтитрами. Работает на телефоне.\n\nПо моей ссылке первый месяц PRO — со скидкой. Проверяй в описании.\n\n{ref}\n\n#reels #контент #OKO #продуктивность'
    },
    // баннеры
    { kind:'banner', k:'banner-story', h:'OKO · всё в одном', sub:'Stories',
      gr:'g1', fmt:'1080×1920', tag:'Stories',
      copy:'Баннер 1080×1920 для сторис. Скачивай PNG и добавляй свою реф-ссылку в стикер «ссылка».\n\nВариант подписи под баннер: «Одно приложение вместо восьми · забирай PRO со скидкой по моей ссылке»\n\n{ref}'
    },
    { kind:'banner', k:'banner-square', h:'Партнёрка 15% + 5%', sub:'Пост / Ads',
      gr:'g2', fmt:'1080×1080', tag:'Feed',
      copy:'Баннер 1080×1080 для ленты Instagram, ВК, LinkedIn.\n\nПодпись:\n«Приведи одного клиента в OKO · получай 15% с каждой его оплаты. Он приведёт друга · +5% сверху. Выплаты за 24-48 часов на Lava, USDT или карту РФ»\n\n{ref}'
    },
    { kind:'banner', k:'banner-wide', h:'OKO · русский рынок', sub:'Web-hero',
      gr:'g5', fmt:'1600×900', tag:'Web',
      copy:'Web-баннер 1600×900 для сайта, статьи или Дзена.\n\nЗаголовок:\n«OKO · одно приложение вместо восьми. Мессенджер, кошелёк, академия, биржа, контент-завод и партнёрка. Русский рынок, платит с оборота твоих клиентов.»\n\nCTA: Забрать PRO со скидкой -> {ref}'
    },
    { kind:'banner', k:'banner-yt', h:'YouTube preview', sub:'16:9',
      gr:'g4', fmt:'1280×720', tag:'YouTube',
      copy:'Обложка 1280×720 для видео-обзора OKO на YouTube.\n\nПодпись под ролик:\n«В новом ролике разбираю OKO по косточкам: где выигрывает у Notion + Telegram + CapCut, где недотягивает, и почему я перевёл всю команду. По моей реф-ссылке · месяц PRO в подарок при годовой оплате.»\n\n{ref}'
    },
    { kind:'banner', k:'banner-ads', h:'Ads square 1080×1080', sub:'реклама · таргет',
      gr:'g6', fmt:'1080×1080', tag:'Ads',
      copy:'Оффер для ретаргета на подписчиков:\n\n«Приведи одного клиента в OKO · получай 15% с каждой его оплаты. Он приведёт друга · +5% сверху. Выплаты на карту РФ, USDT или Lava.top. Стартуй за 30 секунд.»\n\nСсылка на посадочную: {ref}'
    },
    // reels-сценарии
    { kind:'reels', k:'reels-success', h:'Успех · до/после', sub:'Reels · 9:16 · 20 сек',
      gr:'g2', fmt:'1080×1920', tag:'Успех',
      scenes:[
        {n:'0-2 сек', t:'HOOK', body:'Крупный план · телефон в руке', vo:'«Я снёс 8 приложений и оставил одно»'},
        {n:'2-6 сек', t:'ПРОБЛЕМА', body:'Быстрая нарезка: Notion, Telegram, CapCut, Google Docs, десятки вкладок', vo:'«Каждое утро 40 минут собирал контекст между вкладками»'},
        {n:'6-12 сек', t:'РЕШЕНИЕ', body:'Экран OKO: чаты · лента · кошелёк · академия · биржа', vo:'«В OKO всё в одном окне: чаты, лента, кошелёк, академия, биржа, контент-завод»'},
        {n:'12-16 сек', t:'ДОКАЗАТЕЛЬСТВО', body:'Скрин истории оплат партнёрки · сумма подсвечена', vo:'«За месяц партнёрка приносит от 47 000 ₽ пассивно · 15% с оплат»'},
        {n:'16-20 сек', t:'CTA', body:'Стикер «ссылка» + палец указывает вверх', vo:'«Ссылка в описании · по ней +30 дней PRO бесплатно»'}
      ],
      copy:'Reels-сценарий «Успех» · длительность 20 секунд · вертикальное 1080×1920.\n\nМузыка: спокойный upbeat, 90-100 BPM, drop на секунде 12.\nОбщий тон: уверенно, спокойно, без крика.\nCTA: {ref}'
    },
    { kind:'reels', k:'reels-pain', h:'Боль · раздражение → облегчение', sub:'Reels · 9:16 · 25 сек',
      gr:'g8', fmt:'1080×1920', tag:'Боль',
      scenes:[
        {n:'0-2 сек', t:'HOOK', body:'Крупный план · разлитый кофе, ноут завис', vo:'«Задолбался прыгать между приложениями?»'},
        {n:'2-8 сек', t:'НАКАЛ', body:'Быстро листаем: телега → задача пропала. Notion → бюджет не сошёлся. CapCut → рендер упал.', vo:'«Задача теряется в чатах. Клиент ждёт. Дедлайн вчера. Знакомо?»'},
        {n:'8-14 сек', t:'РАЗВЯЗКА', body:'Один жест · открывается OKO · всё в одном окне', vo:'«Один жест · OKO. Чат, оплата, задача, ролик · в одном окне. На русском»'},
        {n:'14-20 сек', t:'ДЕТАЛИ', body:'Кошелёк с эскроу · партнёрка · контент-завод (3 крупных плана)', vo:'«Эскроу-кошелёк для сделок. Партнёрка 15%. Контент-завод собирает Reels за 8 минут»'},
        {n:'20-25 сек', t:'CTA', body:'Свайп вверх на стикер «ссылка» · пульсация лого', vo:'«Ссылка в шапке · первый месяц PRO со скидкой»'}
      ],
      copy:'Reels-сценарий «Боль» · длительность 25 секунд · вертикальное 1080×1920.\n\nМузыка: тревожный саспенс на 0-8с → облегчение на 8с, тёплая мелодия дальше.\nОбщий тон: искренне, чуть иронично, без агрессии.\nCTA: {ref}'
    },
    { kind:'reels', k:'reels-compare', h:'Сравнение · vs Notion + Telegram + CapCut', sub:'Reels · 9:16 · 30 сек',
      gr:'g6', fmt:'1080×1920', tag:'Сравнение',
      scenes:[
        {n:'0-3 сек', t:'HOOK', body:'Экран разделён на 4: Notion, Telegram, CapCut, Google Docs vs OKO', vo:'«4 приложения против одного OKO. Ставлю на второе»'},
        {n:'3-10 сек', t:'РАУНД 1 · СПИСКИ', body:'Слева · создание задачи в 4 сервисах. Справа · создание задачи в OKO', vo:'«Задача. 4 сервиса → 42 секунды. OKO → 6 секунд»'},
        {n:'10-18 сек', t:'РАУНД 2 · ОПЛАТА', body:'Слева · перевод через банк. Справа · OKO кошелёк-эскроу', vo:'«Оплата с эскроу. Банк → 3 дня и комиссия. OKO → мгновенно, без комиссии внутри»'},
        {n:'18-25 сек', t:'РАУНД 3 · КОНТЕНТ', body:'Слева · монтаж в CapCut. Справа · Reels собран в OKO', vo:'«Reels 1080×1920 с озвучкой. CapCut → 3 часа. OKO → 8 минут»'},
        {n:'25-30 сек', t:'CTA', body:'Крупный план лого OKO · пульсация · ссылка снизу', vo:'«Ссылка в описании · +30 дней PRO по моей»'}
      ],
      copy:'Reels-сценарий «Сравнение» · длительность 30 секунд · вертикальное 1080×1920.\n\nМузыка: битовый ритм, 110 BPM, акценты на каждом раунде.\nОбщий тон: спортивный комментарий, «раунд · нокаут».\nCTA: {ref}'
    },
  ];

  /* ---------- история начислений ----------
     Одиннадцать выдуманных начислений (до 6 350 ₽, с «Иваном» и «Еленой»)
     удалены 09.08: человек видел чужие деньги как свои. Реальные строки
     приходят из партнёрского бэкенда — до тех пор список пуст, а честное
     пустое состояние рисует слой oko-mini2.js. */
  const HIST = [];
  const HIST_STATE = {status:'all', product:'all'};

  /* ---------- утилиты формата ---------- */
  function fmtRub(n){
    return Math.round(n).toLocaleString('ru-RU').replace(/,/g,' ') + ' ₽';
  }
  function fmtNum(n){
    return Math.round(n).toLocaleString('ru-RU').replace(/,/g,' ');
  }
  function fmtDate(ts){
    const d = new Date(ts);
    const now = Date.now(), diff = now - ts;
    if(diff < 3600e3) return Math.max(1, Math.round(diff/60e3)) + ' мин назад';
    if(diff < 24*3600e3) return Math.round(diff/3600e3) + ' ч назад';
    if(diff < 7*24*3600e3) return Math.round(diff/(24*3600e3)) + ' дн назад';
    return d.toLocaleDateString('ru-RU', {day:'2-digit', month:'short'});
  }
  function escapeHtml(s){
    return String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  }

  /* ---------- 30 дней динамики для sparkline (детерминированный шум) ---------- */
  function spark30(){
    // амплитуда вокруг среднего = monthEarned / 30
    const avg = PP.monthEarned / 30;
    const arr = [];
    let seed = 20260729;
    function rnd(){ seed ^= seed<<13; seed ^= seed>>>17; seed ^= seed<<5; return ((seed>>>0)%1000)/1000; }
    for(let i=0;i<30;i++){
      const noise = 0.55 + rnd()*0.9;               // 0.55–1.45
      const trend = 0.7 + i/30 * 0.6;               // рост к концу месяца
      arr.push(avg * noise * trend);
    }
    // нормируем сумму к monthEarned
    const total = arr.reduce((s,v)=>s+v,0);
    /* Пока заработка нет, шкалировать не на что: нулевое деление давало NaN,
       и график рисовался битыми путями. Ровная линия по нулю — честно. */
    if(!total || !PP.monthEarned) return arr.map(()=>0);
    const scale = PP.monthEarned / total;
    return arr.map(v => Math.round(v * scale));
  }

  /* ================= РЕНДЕР ================= */
  function ppRender(){
    const scr = document.getElementById('screen-partner');
    if(!scr) return;
    if(scr.dataset.ppReady === '1') return;
    const pad = scr.querySelector('.pad');
    if(!pad) return;

    // 0) убираем старый заголовок «Партнёрский кабинет» вводим свой
    const oldH = pad.querySelector('.section-h');
    if(oldH) oldH.remove();

    // 1) прячем базовые блоки (stat-grid, старый график, старая карточка ref, старая карточка начислений, старую кнопку выплаты)
    const legacy = pad.querySelector('.stat-grid');
    if(legacy) legacy.style.display = 'none';
    // старый график
    const legacyChart = pad.querySelector('#chartWrap')?.closest('.card');
    if(legacyChart) legacyChart.style.display = 'none';
    // старая карточка реф-ссылки
    const legacyRef = pad.querySelector('#refInput')?.closest('.card');
    if(legacyRef) legacyRef.style.display = 'none';
    // старая карточка «последние начисления»
    Array.from(pad.querySelectorAll('.card')).forEach(c => {
      if(/Последние начисления/.test(c.innerText||'')) c.style.display = 'none';
    });
    // старая кнопка «Заявка на выплату» (btn.ghost с onclick openPayout)
    Array.from(pad.querySelectorAll('button')).forEach(b => {
      const oc = b.getAttribute('onclick') || '';
      if(oc.includes('openPayout')) b.style.display = 'none';
    });
    // хвостовой spacer
    Array.from(pad.children).forEach(c => {
      if(c.tagName === 'DIV' && !c.className && c.style && c.style.height === '12px') c.style.display = 'none';
    });

    // 2) собираем новый экран одной вставкой в начало
    const root = document.createElement('div');
    root.id = 'ppRoot';
    root.innerHTML = `
      <h2 class="section-h" style="display:flex;justify-content:space-between;align-items:baseline;gap:8px">
        <span>Партнёрская программа</span>
        <span class="chip" style="letter-spacing:.05em">${REF_PCT_FIRST}% · ${REF_PCT_REPEAT}% сверху</span>
      </h2>
      <div id="ppHero"></div>
      <div id="ppLadder"></div>
      <div id="ppCalc"></div>
      <div id="ppDeep" class="card" style="margin-top:12px"></div>
      <div id="ppPromo" class="card" style="margin-top:12px"></div>
      <div id="ppLb" class="card" style="margin-top:12px;padding:14px 0 4px"></div>
      <div id="ppPayoutTop" style="margin-top:12px"></div>
      <div id="ppHistory" class="card" style="margin-top:12px"></div>
      <div id="ppGuide" class="card" style="margin-top:12px"></div>
      <div style="height:14px"></div>
    `;
    pad.insertBefore(root, pad.firstChild);

    // 3) наполнение
    document.getElementById('ppHero').innerHTML = heroHTML();
    renderSpark();
    animateCountUp();

    document.getElementById('ppLadder').innerHTML = tierHTML();

    document.getElementById('ppCalc').innerHTML = calcHTML();
    wireCalc();

    document.getElementById('ppDeep').innerHTML = deepHTML();
    wireDeep();

    document.getElementById('ppPromo').innerHTML = promoHTML();

    document.getElementById('ppLb').innerHTML = leaderboardHTML();

    document.getElementById('ppPayoutTop').innerHTML = payoutTopHTML();

    document.getElementById('ppHistory').innerHTML = historyHTML();
    renderHistoryList();

    document.getElementById('ppGuide').innerHTML = guideHTML();

    scr.dataset.ppReady = '1';

    // 4) первый заход · welcome-модалка
    maybeShowWelcome();
    // 5) плашка живого начисления
    scheduleLiveNotif();
  }

  /* ================= HERO ================= */
  function heroHTML(){
    const delta = PP.monthEarned - PP.monthPrev;
    const deltaPct = PP.monthPrev ? Math.round(delta / PP.monthPrev * 100) : 0;
    const up = delta >= 0;
    const now = new Date();
    /* предложный падеж: «в августе», а не «в август» */
    const monthName = ['январе','феврале','марте','апреле','мае','июне','июле',
                       'августе','сентябре','октябре','ноябре','декабре'][now.getMonth()];

    return `<div class="pp-hero">
      <div class="pp-hero-lbl">
        <span>Заработано в ${monthName}</span>
        <span class="pp-hero-tag"><svg class="i"><use href="#i-pp-badge-check"/></svg>активна</span>
      </div>
      <div class="pp-hero-num" id="ppHeroNum">0 <span class="pp-hero-cur">₽</span></div>
      <div class="pp-hero-delta ${up?'':'down'}">
        <svg class="i" style="transform:rotate(${up?0:180}deg)"><use href="#i-pp-up"/></svg>
        <span>${up?'+':''}${fmtRub(delta)} · ${up?'+':''}${deltaPct}% к прошлому месяцу</span>
      </div>
      <div class="pp-hero-spark" id="ppHeroSpark"></div>

      <div class="pp-microstat">
        <div><div class="v">${fmtNum(PP.clicks || 0)}</div><div class="l">клики</div></div>
        <div><div class="v">${fmtNum(PP.regs || 0)}</div><div class="l">регистрации</div></div>
        <div><div class="v">${fmtNum(PP.pays || 0)}</div><div class="l">оплаты</div></div>
        <div><div class="v lime">${fmtNum(PP.payoutAvail)}<span style="font-size:11px;margin-left:2px">₽</span></div><div class="l">к выплате</div></div>
      </div>

      <div class="pp-sources">
        <div class="pp-sources-h">
          <span>Топ-источники за месяц</span>
          <span class="dim" style="font-size:11px;font-weight:600">клики · оплаты</span>
        </div>
        ${sourcesHTML()}
      </div>

      <div class="pp-hero-cta">
        <button class="btn" onclick="ppCopyRef()"><svg class="i"><use href="#i-copy"/></svg>Скопировать реф-ссылку</button>
        <button class="btn ghost icon-only" onclick="ppOpenQR()" title="QR-код"><svg class="i"><use href="#i-pp-qr"/></svg></button>
        <button class="btn ghost icon-only" onclick="ppShareRef()" title="Поделиться"><svg class="i"><use href="#i-pp-share"/></svg></button>
      </div>
    </div>`;
  }

  function sourcesHTML(){
    const sorted = SOURCES.slice().sort((a,b)=>b.sum-a.sum);
    const top3 = sorted.slice(0,3);
    const totalTop = top3.reduce((s,x)=>s+x.sum,0);
    /* Пока переходов не было, ни один канал не «лидирует».
       Раньше деление нуля на ноль давало NaN, и все полосы рисовались
       полными — выглядело так, будто каждый канал отработал на 100%. */
    if(!totalTop){
      return top3.map(s=>`<div class="pp-src-row">
        <div class="pp-src-ico s-${s.k}"><svg class="i"><use href="#i-${s.ic}"/></svg></div>
        <div class="pp-src-name">${s.name}<span class="dim"> · ${s.hint}</span></div>
        <div class="pp-src-sum dim">—</div>
        <div class="pp-src-bar"><i style="width:0%"></i></div>
      </div>`).join('');
    }
    return top3.map(s=>{
      const pct = Math.round(s.sum/totalTop*100);
      return `<div class="pp-src-row">
        <div class="pp-src-ico s-${s.k}"><svg class="i"><use href="#i-${s.ic}"/></svg></div>
        <div class="pp-src-name">${s.name}<span class="dim"> · ${s.hint}</span></div>
        <div class="pp-src-sum">${fmtRub(s.sum)}</div>
        <div class="pp-src-bar"><i style="width:${pct}%"></i></div>
      </div>`;
    }).join('');
  }

  /* Sparkline SVG */
  function renderSpark(){
    const host = document.getElementById('ppHeroSpark');
    if(!host) return;
    const data = spark30();
    const W = 320, H = 56, padX = 4, padT = 4, padB = 4;
    const max = Math.max(...data), min = Math.min(...data);
    const xs = i => padX + i*(W-2*padX)/(data.length-1);
    const ys = v => padT + (1-(v-min)/((max-min)||1))*(H-padT-padB);
    const line = data.map((v,i)=>`${i?'L':'M'}${xs(i).toFixed(1)} ${ys(v).toFixed(1)}`).join(' ');
    const area = `${line} L${xs(data.length-1).toFixed(1)} ${(H-padB).toFixed(1)} L${xs(0).toFixed(1)} ${(H-padB).toFixed(1)} Z`;
    const lx = xs(data.length-1).toFixed(1), ly = ys(data[data.length-1]).toFixed(1);
    host.innerHTML = `
      <svg viewBox="0 0 ${W} ${H}" preserveAspectRatio="none">
        <defs>
          <linearGradient id="ppSparkGrad" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0" stop-color="var(--lime)" stop-opacity="0.38"/>
            <stop offset="1" stop-color="var(--lime)" stop-opacity="0"/>
          </linearGradient>
        </defs>
        <path d="${area}" fill="url(#ppSparkGrad)"/>
        <path d="${line}" fill="none" stroke="var(--lime)" stroke-width="2" stroke-linejoin="round" stroke-linecap="round"/>
        <circle class="spot" cx="${lx}" cy="${ly}" r="3.4"/>
      </svg>`;
  }

  /* Count-up для главной цифры */
  function animateCountUp(){
    const el = document.getElementById('ppHeroNum');
    if(!el) return;
    const to = PP.monthEarned;
    const dur = 1100;
    const start = performance.now();
    function tick(now){
      const t = Math.min(1, (now-start)/dur);
      const eased = 1 - Math.pow(1-t, 3);
      const cur = Math.round(to * eased);
      el.innerHTML = fmtNum(cur) + ' <span class="pp-hero-cur">₽</span>';
      if(t < 1) requestAnimationFrame(tick);
    }
    requestAnimationFrame(tick);
  }

  /* ================= Turnover-лестница ================= */
  function tierHTML(){
    const {idx, tier, next} = currentTier(PP.turnoverAll);
    const min = tier.min, max = next ? next.min : tier.min * 2;
    const pct = Math.min(100, Math.round((PP.turnoverAll - min) / Math.max(1, max - min) * 100));
    const remaining = next ? Math.max(0, next.min - PP.turnoverAll) : 0;
    const currentPct = REF_PCT_FIRST + tier.bonus;
    const nextPct = next ? REF_PCT_FIRST + next.bonus : currentPct;
    const note = next
      ? `Ещё <b>${fmtRub(remaining)}</b> оборота · и открывается <b>${next.label} +${next.bonus}%</b> сверху · итого <b>${nextPct}%</b> с первой линии.`
      : `Ты в топе программы · <b>+${tier.bonus}% бонус к комиссии</b>, персональный менеджер и приоритетные выплаты за 6-12 часов.`;

    return `<div class="pp-tier">
      <div class="pp-tier-row">
        <div class="pp-tier-name">
          <span class="pp-tier-badge ${tier.k}"><svg class="i"><use href="#i-${tier.badge}"/></svg></span>
          <span>${tier.label}</span>
          <span class="pp-tier-percent">${currentPct}%</span>
        </div>
        <div class="pp-tier-sales"><b>${fmtNum(PP.turnoverAll)}</b> ${next?'/ '+fmtNum(next.min):''} ₽</div>
      </div>
      <div class="pp-tier-bar"><i style="width:${pct}%"></i></div>
      <div class="pp-tier-sub">
        <span>${tier.label} · ${fmtNum(min)}+ ₽</span>
        <span>${next ? next.label + ' · ' + fmtNum(next.min) + '+ ₽' : 'вершина программы'}</span>
      </div>
      <div class="pp-tier-note">${note}</div>
      <div class="pp-ladder">
        ${TIERS.map((t,i)=>{
          const cls = i===idx ? 'on' : (i<idx ? 'done' : '');
          return `<div class="pp-ladder-cell ${cls}" title="${t.label} · +${t.bonus}%">
            <span class="lv-name">${t.label}</span>
            <span class="lv-pct">${REF_PCT_FIRST + t.bonus}%</span>
          </div>`;
        }).join('')}
      </div>
    </div>`;
  }

  /* ================= Мульти-tier калькулятор ================= */
  const CALC = {product:'sistema', clients:10};
  function calcHTML(){
    return `<div class="pp-calc">
      <div style="display:flex;align-items:center;gap:8px;margin-bottom:6px">
        <div style="width:32px;height:32px;border-radius:10px;background:var(--lime-dim);color:var(--accent);display:flex;align-items:center;justify-content:center">
          <svg class="i"><use href="#i-pp-calc"/></svg>
        </div>
        <div>
          <div class="pp-calc-h">Калькулятор дохода</div>
          <div class="pp-calc-sub">Посмотри, сколько принесёт партнёрка на твоём объёме.</div>
        </div>
      </div>
      <div class="pp-calc-prod" id="ppCalcProd">
        ${PP.products.filter(p=>p.k!=='all').map(p=>`
          <button data-p="${p.k}" class="${CALC.product===p.k?'on':''}">${p.label}</button>
        `).join('')}
      </div>
      <div class="pp-calc-row">
        <div>
          <div class="pp-calc-label">Клиентов в месяц</div>
          <input type="range" class="pp-calc-slider" min="1" max="200" value="${CALC.clients}" id="ppCalcSlider">
        </div>
        <div class="pp-calc-stepper">
          <button onclick="ppCalcStep(-1)" type="button" aria-label="меньше">-</button>
          <span class="val" id="ppCalcVal">${CALC.clients}</span>
          <button onclick="ppCalcStep(1)" type="button" aria-label="больше">+</button>
        </div>
      </div>
      <div id="ppCalcResult"></div>
      <div class="pp-calc-hint">Пример: 10 клиентов на PRO 4 900 ₽ → +${fmtNum(10*4900*(REF_PCT_FIRST+REF_PCT_REPEAT)/100)} ₽/мес пассивно.</div>
    </div>`;
  }
  function calcResultHTML(){
    const p = PP.products.find(x=>x.k===CALC.product);
    if(!p) return '';
    const {tier} = currentTier(PP.turnoverAll);
    const bonusPct = tier.bonus;
    const first = CALC.clients * p.price * (REF_PCT_FIRST + bonusPct) / 100;
    const repeat = CALC.clients * p.price * (REF_PCT_REPEAT) / 100;
    const l2 = CALC.clients * 0.4 * p.price * (REF_PCT_L2) / 100;   // 40% приведут второго
    const total = first + repeat + l2;

    return `<div class="pp-calc-result">
      <div class="pp-calc-result-line"><span>Первая оплата · ${REF_PCT_FIRST + bonusPct}%</span><b>${fmtRub(first)}</b></div>
      <div class="pp-calc-result-line"><span>Повторные · ${REF_PCT_REPEAT}%</span><b>${fmtRub(repeat)}</b></div>
      <div class="pp-calc-result-line"><span>2-я линия · ${REF_PCT_L2}%</span><b>${fmtRub(l2)}</b></div>
      <div class="pp-calc-result-total">
        <span>Итого в месяц</span>
        <b>+${fmtRub(total)}</b>
      </div>
    </div>`;
  }
  function updateCalc(){
    const r = document.getElementById('ppCalcResult');
    if(r) r.innerHTML = calcResultHTML();
    const v = document.getElementById('ppCalcVal');
    if(v) v.textContent = CALC.clients;
    const s = document.getElementById('ppCalcSlider');
    if(s && +s.value !== CALC.clients) s.value = CALC.clients;
  }
  window.ppCalcStep = function(delta){
    CALC.clients = Math.max(1, Math.min(500, CALC.clients + delta));
    updateCalc();
  };
  function wireCalc(){
    const s = document.getElementById('ppCalcSlider');
    if(s) s.addEventListener('input', e => { CALC.clients = +e.target.value; updateCalc(); });
    const prod = document.getElementById('ppCalcProd');
    if(prod) prod.addEventListener('click', e => {
      const b = e.target.closest('button[data-p]'); if(!b) return;
      CALC.product = b.dataset.p;
      prod.querySelectorAll('button').forEach(x=>x.classList.toggle('on', x===b));
      updateCalc();
    });
    updateCalc();
  }

  /* ================= Реф-ссылки (дип-линки) ================= */
  function baseRef(){ return 'okoteam.top/r/' + PP.nick; }
  function currentRef(){
    const p = PP.activeProduct;
    if(!p || p==='all') return baseRef();
    // короткий формат /r/<nick>/<product>
    return baseRef() + '/' + p;
  }
  function deepHTML(){
    return `<div class="pp-section-h">
        <p>Твоя реф-ссылка</p>
        <span class="chip">${REF_PCT_FIRST}% · +${REF_PCT_REPEAT}%</span>
      </div>
      <p class="dim" style="font-size:12.5px;margin-top:0;margin-bottom:10px">
        Свой короткий адрес на каждый продукт OKO · ведёт сразу на нужный оффер.
      </p>
      <div class="pp-deep">
        <div class="pp-deep-tabs" id="ppDeepTabs">
          ${PP.products.map(p=>`<button data-p="${p.k}" class="${p.k===PP.activeProduct?'on':''}">
            ${p.dot?'<span class="dot"></span>':''}${p.label}
          </button>`).join('')}
        </div>
        <div class="reflink">
          <input value="${currentRef()}" readonly id="ppRefInput">
        </div>
        <div class="pp-ref-actions">
          <button class="btn sm" onclick="ppCopyRef()"><svg class="i"><use href="#i-copy"/></svg>Копировать</button>
          <button class="btn ghost icon-only" onclick="ppOpenQR()" title="QR-код"><svg class="i"><use href="#i-pp-qr"/></svg></button>
          <button class="btn ghost icon-only" onclick="ppShareRef()" title="Поделиться"><svg class="i"><use href="#i-pp-share"/></svg></button>
        </div>
        <p class="pp-ref-hint" id="ppRefHint">Общий адрес: ведёт на главную OKO. Выбери продукт выше · получишь короткую дип-ссылку.</p>
      </div>`;
  }
  function wireDeep(){
    const tabs = document.getElementById('ppDeepTabs');
    if(!tabs) return;
    tabs.addEventListener('click', e=>{
      const b = e.target.closest('button[data-p]');
      if(!b) return;
      PP.activeProduct = b.dataset.p;
      tabs.querySelectorAll('button').forEach(x=>x.classList.toggle('on', x===b));
      const inp = document.getElementById('ppRefInput');
      if(inp) inp.value = currentRef();
      // держим old base-input в синхроне, чтобы старая copyRef из base работала корректно
      const legacyInp = document.querySelector('#screen-partner input#refInput');
      if(legacyInp) legacyInp.value = currentRef();
      const hint = document.getElementById('ppRefHint');
      const p = PP.products.find(x=>x.k===PP.activeProduct);
      if(hint) hint.innerHTML = p && p.desc
        ? `<b style="color:var(--accent)">${p.label}</b> · ${p.desc} · дип-линк открывает страницу оплаты сразу.`
        : `Общий адрес: ведёт на главную OKO. Выбери продукт выше · получишь короткую дип-ссылку.`;
    });
  }
  window.ppCopyRef = function(){
    const inp = document.getElementById('ppRefInput');
    const val = inp ? inp.value : currentRef();
    if(inp){ try{ inp.select(); }catch(e){} }
    try{ navigator.clipboard.writeText(val); }catch(e){ try{ document.execCommand('copy'); }catch(_){} }
    if(typeof toast === 'function') toast('Реф-ссылка скопирована');
  };
  window.ppShareRef = function(){
    const url = document.getElementById('ppRefInput')?.value || currentRef();
    if(navigator.share){
      navigator.share({title:'OKO', text:'Заходи в OKO по моей ссылке', url:'https://'+url}).catch(()=>{});
    } else {
      try{ navigator.clipboard.writeText('https://'+url); }catch(e){}
      if(typeof toast === 'function') toast('Ссылка для расшаривания скопирована');
    }
  };

  /* ---------- QR: SVG, детерминированный от URL ---------- */
  function qrSvg(text){
    const N = 29;
    let seed = 2166136261;
    for(let i=0;i<text.length;i++){ seed ^= text.charCodeAt(i); seed = (seed*16777619)>>>0; }
    function rnd(){ seed ^= seed<<13; seed ^= seed>>>17; seed ^= seed<<5; return ((seed>>>0)%1000)/1000; }
    const cells = [];
    for(let y=0;y<N;y++){
      const row = [];
      for(let x=0;x<N;x++) row.push(rnd() < 0.48 ? 1 : 0);
      cells.push(row);
    }
    for(let i=8;i<N-8;i++){ cells[6][i] = i%2===0?1:0; cells[i][6] = i%2===0?1:0; }
    function finder(cy, cx){
      for(let dy=-1;dy<=7;dy++) for(let dx=-1;dx<=7;dx++){
        const y=cy+dy, x=cx+dx; if(y<0||y>=N||x<0||x>=N) continue;
        cells[y][x] = 0;
      }
      for(let dy=0;dy<7;dy++) for(let dx=0;dx<7;dx++){
        const y=cy+dy, x=cx+dx;
        const inner = dy>=2 && dy<=4 && dx>=2 && dx<=4;
        const outer = dy===0 || dy===6 || dx===0 || dx===6;
        cells[y][x] = (inner||outer) ? 1 : 0;
      }
    }
    finder(0,0); finder(0,N-7); finder(N-7,0);
    for(let dy=-2;dy<=2;dy++) for(let dx=-2;dx<=2;dx++){
      const y=N-5+dy, x=N-5+dx;
      const outer = Math.abs(dy)===2 || Math.abs(dx)===2;
      const center = dy===0 && dx===0;
      cells[y][x] = (outer||center) ? 1 : 0;
    }
    const size = 232, pad = 6, cell = (size - pad*2) / N;
    let rects = '';
    for(let y=0;y<N;y++) for(let x=0;x<N;x++){
      if(!cells[y][x]) continue;
      rects += `<rect x="${(pad+x*cell).toFixed(2)}" y="${(pad+y*cell).toFixed(2)}" width="${cell.toFixed(2)}" height="${cell.toFixed(2)}"/>`;
    }
    const c = size/2, r = 22;
    const brand = `<circle cx="${c}" cy="${c}" r="${r+2}" fill="#fff"/>
      <circle cx="${c}" cy="${c}" r="${r-2}" fill="#000"/>
      <circle cx="${c}" cy="${c}" r="8" fill="#9AFF00"/>`;
    return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${size} ${size}">
      <rect width="${size}" height="${size}" fill="#fff"/>
      <g fill="#000">${rects}</g>${brand}
    </svg>`;
  }
  window.ppOpenQR = function(){
    const url = currentRef();
    document.getElementById('ppQrView').innerHTML = `
      <h3>QR-код твоей ссылки</h3>
      <div class="pp-qr-wrap">
        <div class="pp-qr">${qrSvg(url)}</div>
        <div class="pp-qr-url">${url}</div>
        <div class="pp-qr-tip">Покажи на встрече, наклей на визитку, встрой в сторис.</div>
      </div>
      <div style="height:12px"></div>
      <button class="btn" onclick="ppCopyRef()"><svg class="i"><use href="#i-copy"/></svg>Скопировать ссылку</button>`;
    if(typeof openSheet === 'function') openSheet('pp-qr');
  };

  /* ================= Промо-материалы ================= */
  const PROMO_STATE = {kind:'all'};
  function promoHTML(){
    return `<div class="pp-section-h">
        <p>Готовые промо-материалы</p>
        <span class="chip">${PROMO.length} шт.</span>
      </div>
      <p class="dim" style="font-size:12.5px;margin-bottom:0;margin-top:0">
        Тексты, баннеры и Reels-сценарии · копируй в 1 клик · реф-ссылка подставляется автоматически.
      </p>
      <div class="pp-promo-tabs" id="ppPromoTabs">
        <button data-kind="all" class="on">Все · ${PROMO.length}</button>
        <button data-kind="text">Тексты · ${PROMO.filter(p=>p.kind==='text').length}</button>
        <button data-kind="banner">Баннеры · ${PROMO.filter(p=>p.kind==='banner').length}</button>
        <button data-kind="reels">Reels-сценарии · ${PROMO.filter(p=>p.kind==='reels').length}</button>
      </div>
      <div class="pp-promo-grid" id="ppPromoGrid"></div>`;
  }
  function renderPromoGrid(){
    const grid = document.getElementById('ppPromoGrid');
    if(!grid) return;
    const list = PROMO_STATE.kind==='all' ? PROMO : PROMO.filter(p => p.kind === PROMO_STATE.kind);
    if(!list.length){ grid.innerHTML = `<div class="pp-hist-empty" style="grid-column:1/-1">По этому фильтру пусто.</div>`; return; }
    grid.innerHTML = list.map(p => {
      const i = PROMO.indexOf(p);
      const kindTag = p.kind==='reels' ? 'reels' : (p.kind==='banner' ? 'banner' : '');
      return `<div class="pp-promo-card" onclick="ppOpenPromo(${i})">
        <div class="pp-promo-thumb ${p.gr}">
          <span class="pp-promo-fmt">${p.fmt}</span>
          <div>
            <div class="pp-promo-h">${p.h}</div>
            <div class="pp-promo-sub">${p.sub}</div>
          </div>
          <div class="pp-promo-logo">OKO</div>
        </div>
        <div class="pp-promo-meta">
          <span class="pp-promo-kind-tag ${kindTag}">${p.tag}</span>
          <span class="dim">${p.kind==='reels'?'сценарий':(p.kind==='banner'?'баннер':'текст')}</span>
        </div>
      </div>`;
    }).join('');
  }
  document.addEventListener('click', e=>{
    const kb = e.target.closest('#ppPromoTabs button[data-kind]');
    if(!kb) return;
    PROMO_STATE.kind = kb.dataset.kind;
    kb.parentElement.querySelectorAll('button').forEach(x=>x.classList.toggle('on', x===kb));
    renderPromoGrid();
  });
  // отрисовываем grid отдельно после первичного рендера
  const _oldRender = ppRender;
  const _renderGridSoon = () => setTimeout(renderPromoGrid, 0);
  window.ppOpenPromo = function(i){
    const p = PROMO[i]; if(!p) return;
    const ref = currentRef();
    let body = '';
    if(p.kind === 'reels' && p.scenes){
      body = p.scenes.map(s=>`
        <div class="pp-scene">
          <div class="pp-scene-h"><span class="n">${s.n}</span><span class="t">${s.t}</span></div>
          <div class="pp-scene-txt">${escapeHtml(s.body)}</div>
          <div class="pp-scene-vo"><b style="color:var(--accent)">Озвучка:</b> ${escapeHtml(s.vo)}</div>
        </div>`).join('') +
        `<p style="font-weight:700;font-size:13px;margin:8px 0 6px">Общие параметры</p>
         <div class="pp-promo-copy">${escapeHtml(p.copy.replace(/\{ref\}/g, ref))}</div>`;
    } else {
      body = `<p style="font-weight:700;font-size:13px;margin-bottom:6px">Готовый текст</p>
              <div class="pp-promo-copy">${escapeHtml(p.copy.replace(/\{ref\}/g, ref))}</div>`;
    }
    document.getElementById('ppPromoView').innerHTML = `
      <h3>${p.h}</h3>
      <p class="dim" style="font-size:12px">${p.sub} · ${p.fmt}</p>
      <div class="pp-promo-preview pp-promo-thumb ${p.gr}">
        <div>
          <div class="pp-promo-h" style="font-size:20px">${p.h}</div>
          <div class="pp-promo-sub" style="margin-top:8px">${p.sub}</div>
        </div>
        <div class="pp-promo-logo">OKO</div>
      </div>
      ${body}
      <div class="pp-promo-actions">
        <button class="btn" onclick="ppCopyPromo(${i})"><svg class="i"><use href="#i-copy"/></svg>Скопировать</button>
        <button class="btn ghost" onclick="ppSavePromo(${i})"><svg class="i"><use href="#i-bookmark"/></svg>В избранное</button>
      </div>
      <p class="dim" style="font-size:11.5px;margin-top:12px;text-align:center">Реф-ссылка подставится автоматически: ${ref}</p>`;
    if(typeof openSheet === 'function') openSheet('pp-promo');
  };
  window.ppCopyPromo = function(i){
    const p = PROMO[i]; if(!p) return;
    const ref = currentRef();
    let text;
    if(p.kind === 'reels' && p.scenes){
      text = `${p.h} · ${p.sub} · ${p.fmt}\n\n` +
             p.scenes.map(s => `${s.n} · ${s.t}\n${s.body}\nОзвучка: ${s.vo}`).join('\n\n') +
             `\n\n---\n${p.copy.replace(/\{ref\}/g, ref)}`;
    } else {
      text = p.copy.replace(/\{ref\}/g, ref);
    }
    try{ navigator.clipboard.writeText(text); }catch(e){}
    if(typeof toast === 'function') toast('Скопировано в буфер');
  };
  window.ppSavePromo = function(){
    if(typeof toast === 'function') toast('Сохранено в избранное');
  };

  /* ================= Wall of Fame ================= */
  function leaderboardHTML(){
    const sorted = LEADERS.slice().sort((a,b)=>b.sum-a.sum);
    const myIdx = sorted.findIndex(l=>l.nick===PP.nick);
    return `<div class="pp-section-h padded">
        <p>Wall of Fame · неделя</p>
        <span class="chip">твоё место · ${myIdx+1}</span>
      </div>
      <div class="pp-lb">
        ${sorted.slice(0,10).map((l,i)=>{
          const cls = i===0?'top1':(i===1?'top2':(i===2?'top3':''));
          const you = l.nick===PP.nick ? 'you' : '';
          const av = l.nick[0].toUpperCase();
          const tierLbl = (TIERS.find(t=>t.k===l.tier)||{}).label || '';
          return `<div class="pp-lb-row ${cls} ${you}">
            <div class="pp-lb-place">${i+1}</div>
            <div class="pp-lb-nick">
              <span class="pp-lb-ava">${av}</span>
              <span>@${l.nick}</span>
              <span class="pp-lb-tier ${l.tier}">${tierLbl}</span>
            </div>
            <div class="pp-lb-sum">${fmtRub(l.sum)}</div>
          </div>`;
        }).join('')}
      </div>
      <p class="dim" style="font-size:11.5px;text-align:center;padding:10px 14px 4px">
        Рейтинг обновляется каждый понедельник · топ-10 недели.
      </p>`;
  }

  /* ================= Выплаты: hero + sheet ================= */
  function payoutTopHTML(){
    const enough = PP.payoutAvail >= PP.payoutMin;
    return `<div class="pp-payout-hero">
      <div style="min-width:0;flex:1 1 auto">
        <div class="lbl">Доступно к выводу</div>
        <div class="sum"><b>${fmtNum(PP.payoutAvail)}</b> ₽</div>
        <div class="pp-payout-hint">
          <svg class="i"><use href="#i-clock"/></svg>
          <span>Средний срок выплаты · 24-48 часов</span>
        </div>
      </div>
      <div class="cta">
        <button class="btn" onclick="ppOpenPayout()" ${enough?'':'disabled'}>
          ${enough ? 'Запросить' : `от ${fmtRub(PP.payoutMin)}`}
        </button>
      </div>
    </div>`;
  }

  const PAY = {method:'card'};
  const PAY_METHODS = [
    {k:'card',   label:'Карта РФ', icon:'card'},
    {k:'usdt',   label:'USDT · TRC-20', icon:'pp-usdt'},
    {k:'lava',   label:'Lava.top', icon:'bolt'},
  ];
  function payoutSheetHTML(){
    return `<h3>Запросить выплату</h3>
      <div class="pp-payout-summary">
        <div><div class="lb">Сумма к выводу</div><div class="sm">${fmtRub(PP.payoutAvail)}</div></div>
        <span class="chip">мин. ${fmtNum(PP.payoutMin)} ₽</span>
      </div>
      <p style="font-weight:700;font-size:13px;margin-bottom:6px">Куда вывести</p>
      <div class="pp-payout-method" id="ppPayMethod">
        ${PAY_METHODS.map(m=>`
          <button data-m="${m.k}" class="${PAY.method===m.k?'on':''}" type="button">
            <svg class="i"><use href="#i-${m.icon}"/></svg>
            <span>${m.label}</span>
          </button>`).join('')}
      </div>
      <input id="ppPayReq" placeholder="${payoutPlaceholder()}">
      <button class="btn" onclick="ppDoPayout()">
        <svg class="i"><use href="#i-card"/></svg>Запросить ${fmtRub(PP.payoutAvail)}
      </button>
      <p class="dim" style="font-size:11.5px;margin-top:10px;text-align:center">
        Выплата поступит в течение 24-48 часов. Комиссия · 0 ₽ для сумм от 1 000 ₽.
      </p>`;
  }
  function payoutPlaceholder(){
    return PAY.method==='usdt' ? 'Адрес кошелька USDT (TRC-20)'
         : PAY.method==='lava' ? 'Аккаунт Lava.top / e-mail'
         : 'Номер карты · 16 цифр';
  }
  window.ppOpenPayout = function(){
    PAY.method = 'card';
    const host = document.getElementById('ppPayoutView');
    if(!host) return;
    host.innerHTML = payoutSheetHTML();
    if(typeof openSheet === 'function') openSheet('pp-payout');
    // клики по методам
    const meth = document.getElementById('ppPayMethod');
    if(meth) meth.addEventListener('click', e=>{
      const b = e.target.closest('button[data-m]'); if(!b) return;
      PAY.method = b.dataset.m;
      meth.querySelectorAll('button').forEach(x=>x.classList.toggle('on', x===b));
      const inp = document.getElementById('ppPayReq');
      if(inp) inp.placeholder = payoutPlaceholder();
    });
  };
  window.ppDoPayout = function(){
    const inp = document.getElementById('ppPayReq');
    const req = (inp && inp.value || '').trim();
    if(!req){
      if(typeof toast === 'function') toast('Укажи реквизиты');
      if(inp) inp.focus();
      return;
    }
    document.getElementById('ppPayoutView').innerHTML = `
      <div style="text-align:center;padding:16px 0">
        <div class="pay-ok" style="width:64px;height:64px;border-radius:50%;background:var(--lime);color:#000;display:inline-flex;align-items:center;justify-content:center;margin:0 auto">
          <svg class="i" style="width:32px;height:32px"><use href="#i-check"/></svg>
        </div>
        <p style="font-weight:800;font-size:18px;margin-top:12px">Заявка принята</p>
        <p class="dim" style="font-size:13px;margin-top:6px">
          ${fmtRub(PP.payoutAvail)} придут на
          ${{card:'карту',usdt:'кошелёк USDT',lava:'Lava.top'}[PAY.method]}
          в течение 24-48 часов.
        </p>
        <div style="height:16px"></div>
        <button class="btn" onclick="closeSheet()">Готово</button>
      </div>`;
    if(typeof toast === 'function') toast('Заявка на ' + fmtRub(PP.payoutAvail) + ' создана');
  };

  /* ================= История начислений с фильтром ================= */
  function historyHTML(){
    const products = ['all', ...Array.from(new Set(HIST.map(h=>h.p)))];
    const productLabel = (k)=> {
      if(k==='all') return 'Все продукты';
      const p = PP.products.find(x=>x.k===k);
      return p ? p.label : k;
    };
    const statuses = [['all','Все'],['paid','Выплачено'],['hold','Ожидают'],['can','Отменено']];
    return `<div class="pp-section-h">
        <p>История начислений</p>
        <span class="chip" id="ppHistCount"></span>
      </div>
      <div class="pp-hist-filters" id="ppHistStatus">
        ${statuses.map(([k,l])=>`<button data-s="${k}" class="${k===HIST_STATE.status?'on':''}">${l}</button>`).join('')}
      </div>
      <div class="pp-hist-filters" id="ppHistProduct">
        ${products.map(p=>`<button data-p="${p}" class="${p===HIST_STATE.product?'on':''}">${productLabel(p)}</button>`).join('')}
      </div>
      <div id="ppHistList"></div>`;
  }
  function renderHistoryList(){
    const list = document.getElementById('ppHistList');
    const cnt = document.getElementById('ppHistCount');
    if(!list) return;
    const filtered = HIST.filter(h =>
      (HIST_STATE.status==='all' || h.st===HIST_STATE.status) &&
      (HIST_STATE.product==='all' || h.p===HIST_STATE.product)
    ).sort((a,b)=>b.at-a.at);
    if(cnt) cnt.textContent = filtered.length + ' начисл.';
    if(!filtered.length){
      list.innerHTML = `<div class="pp-hist-empty">По этим фильтрам ничего нет.</div>`;
      return;
    }
    const stLabel = {paid:'Выплачено', hold:'Ожидает', can:'Отменено'};
    list.innerHTML = filtered.map(h=>`
      <div class="pp-hist-row">
        <div>
          <div class="pp-hist-t">${escapeHtml(h.t)}</div>
          <div class="pp-hist-m">
            <span>${fmtDate(h.at)}</span>
            <span class="pp-hist-st ${h.st}">${stLabel[h.st]}</span>
            ${h.name?`<span>от · ${escapeHtml(h.name)}</span>`:''}
          </div>
        </div>
        <div class="pp-hist-sum">${h.st==='can'?'-':'+'+fmtRub(h.sum)}</div>
      </div>`).join('');
  }
  document.addEventListener('click', e=>{
    const bp = e.target.closest('#ppHistProduct button[data-p]');
    if(bp){
      HIST_STATE.product = bp.dataset.p;
      bp.parentElement.querySelectorAll('button').forEach(x=>x.classList.toggle('on', x===bp));
      renderHistoryList();
      return;
    }
    const bs = e.target.closest('#ppHistStatus button[data-s]');
    if(bs){
      HIST_STATE.status = bs.dataset.s;
      bs.parentElement.querySelectorAll('button').forEach(x=>x.classList.toggle('on', x===bs));
      renderHistoryList();
    }
  });

  /* ================= 3-шаговый гайд ================= */
  function guideHTML(){
    return `<div class="pp-section-h">
        <p>Как начать · за 3 шага</p>
        <span class="chip">2 минуты</span>
      </div>
      <div class="pp-guide">
        <div class="pp-guide-step">
          <span class="pp-guide-num">01</span>
          <div class="pp-guide-ic"><svg class="i"><use href="#i-copy"/></svg></div>
          <div class="pp-guide-h">Скопируй ссылку</div>
          <div class="pp-guide-t">Общая или на конкретный продукт · выбирай тариф над списком.</div>
        </div>
        <div class="pp-guide-step">
          <span class="pp-guide-num">02</span>
          <div class="pp-guide-ic"><svg class="i"><use href="#i-megaphone"/></svg></div>
          <div class="pp-guide-h">Расскажи</div>
          <div class="pp-guide-t">Бери готовые тексты, баннеры и Reels-сценарии из промо-пака выше.</div>
        </div>
        <div class="pp-guide-step">
          <span class="pp-guide-num">03</span>
          <div class="pp-guide-ic"><svg class="i"><use href="#i-card"/></svg></div>
          <div class="pp-guide-h">Получи выплату</div>
          <div class="pp-guide-t">От 1 000 ₽ на Lava, USDT или карту РФ. 24-48 часов на зачисление.</div>
        </div>
      </div>`;
  }

  /* ================= Onboarding welcome (первый заход) ================= */
  const WELCOME_KEY = 'pp-welcome-shown-v1';
  function maybeShowWelcome(){
    try{
      if(localStorage.getItem(WELCOME_KEY) === '1') return;
      /* Показываем ТОЛЬКО когда человек реально стоит на экране партнёрки
         и экран ничем не занят (правка 09.08). Раньше окно всплывало через
         350 мс после инициализации модуля — и накрывало чат, ленту, что
         угодно, да ещё вместе со вторым окном «награда за партнёрство». */
      var tries = 0;
      var tick = function(){
        if(localStorage.getItem(WELCOME_KEY) === '1') return;
        if(++tries > 40) return;                       /* минуты через две сдаёмся */
        var scr = document.getElementById('screen-partner');
        var onPartner = scr && scr.classList.contains('active');
        var busy = document.querySelector('.sheet.open, #msgMenu.open, #okoPopup, .okg-modal.open')
                || (document.getElementById('app') || {classList:{contains:function(){return false;}}}).classList.contains('conv-open');
        if(onPartner && !busy){ showWelcome(); return; }
        setTimeout(tick, 3000);
      };
      setTimeout(tick, 1200);
    }catch(e){}
  }
  function showWelcome(){
    const host = document.getElementById('ppWelcomeView');
    if(!host) return;
    host.innerHTML = `<div class="pp-welcome">
      <div class="pp-welcome-hero">
        <span class="pp-welcome-badge">
          <svg class="i" style="width:12px;height:12px"><use href="#i-pp-badge-check"/></svg>
          Партнёрская программа
        </span>
        <div class="pp-welcome-num">${REF_PCT_FIRST}%</div>
        <div class="pp-welcome-h">С каждой оплаты твоих клиентов</div>
      </div>
      <ul class="pp-welcome-list">
        <li>
          <span class="pp-w-ic"><svg class="i"><use href="#i-pp-badge-check"/></svg></span>
          <span><b>${REF_PCT_FIRST}%</b> с первой оплаты + <b>${REF_PCT_REPEAT}%</b> со всех повторных · до <b>25%</b> на топ-уровнях</span>
        </li>
        <li>
          <span class="pp-w-ic"><svg class="i"><use href="#i-users"/></svg></span>
          <span>Ещё <b>+${REF_PCT_L2}%</b> со 2-й линии · когда твой клиент приведёт своих</span>
        </li>
        <li>
          <span class="pp-w-ic"><svg class="i"><use href="#i-card"/></svg></span>
          <span>Выплаты от <b>${fmtNum(PP.payoutMin)} ₽</b> на карту РФ, USDT или Lava · <b>24-48 часов</b></span>
        </li>
        <li>
          <span class="pp-w-ic"><svg class="i"><use href="#i-pp-gift"/></svg></span>
          <span>Готовые <b>промо-материалы</b> для соцсетей · тексты, баннеры и Reels-сценарии</span>
        </li>
      </ul>
      <div class="pp-welcome-actions">
        <button class="btn" onclick="ppCloseWelcome(true)">
          <svg class="i"><use href="#i-copy"/></svg>Скопировать реф-ссылку и начать
        </button>
        <button class="btn ghost" onclick="ppCloseWelcome(false)">Осмотреться сначала</button>
      </div>
    </div>`;
    if(typeof openSheet === 'function') openSheet('pp-welcome');
    try{ localStorage.setItem(WELCOME_KEY, '1'); }catch(e){}
  }
  window.ppCloseWelcome = function(copy){
    if(copy){
      try{ navigator.clipboard.writeText(currentRef()); }catch(e){}
      if(typeof toast === 'function') toast('Реф-ссылка скопирована · погнали!');
    }
    if(typeof closeSheet === 'function') closeSheet();
  };

  /* ================= Live-уведомление ================= */
  /* Выдуманные поступления удалены 09.08. Раньше через 8 секунд после входа
     всплывало «+735 ₽ от клиента Иван» — человек видел деньги, которых нет.
     Механика showLive() оставлена: её вызовет реальное начисление партнёрки. */
  const LIVE_MSGS = [];
  function scheduleLiveNotif(){ /* по расписанию ничего не показываем */ }
  function showLive(msg){
    const el = document.getElementById('ppLive');
    if(!el) return;
    document.getElementById('ppLiveT').innerHTML =
      msg.t.replace(/(\+[\d\s]+ ₽)/, '<b>$1</b>');
    document.getElementById('ppLiveS').textContent = msg.s;
    el.classList.add('on');
    clearTimeout(showLive._t);
    showLive._t = setTimeout(()=>el.classList.remove('on'), 6000);
  }
  window.ppHideLive = function(){
    const el = document.getElementById('ppLive');
    if(el) el.classList.remove('on');
    clearTimeout(showLive._t);
  };

  /* ---------- init ---------- */
  const _showTab = window.showTab;
  if(typeof _showTab === 'function'){
    window.showTab = function(t){
      const r = _showTab.apply(this, arguments);
      if(t === 'partner') { ppRender(); setTimeout(renderPromoGrid, 0); }
      return r;
    };
  }
  document.addEventListener('DOMContentLoaded', ()=>{ ppRender(); renderPromoGrid(); });
  setTimeout(()=>{ ppRender(); renderPromoGrid(); }, 100);
  setTimeout(()=>{ ppRender(); renderPromoGrid(); }, 600);
})();

/* ============================================================
   REFERRAL SQUARED (pp-goals) — Приведи N клиентов PRO → бонус
   ============================================================ */
(function referralSquared(){
  'use strict';

  function ico(n){ return '<svg class="i"><use href="#i-'+n+'"/></svg>'; }
  function esc(t){ var d=document.createElement('div'); d.textContent=String(t==null?'':t); return d.innerHTML; }
  function _toast(m){ if(typeof toast==='function') toast(m); }

  var GOALS_KEY   = 'oko-pp-goals-v1';
  var CLAIM_KEY   = 'oko-pp-goals-claimed-v1';
  var COUNT_KEY   = 'oko-pp-goals-count-v1';

  var GOALS = [
    {
      id:'g3',
      need: 3,
      reward: '+3 месяца PRO бесплатно',
      short: '+3 мес PRO',
      icon: 'crown',
      body: 'Приведи 3 клиентов на тариф PRO и получи <b>3 месяца PRO бесплатно</b>.',
      afterTier: null,      /* не апгрейдит tier, только продлевает */
      grantMonths: 3,
    },
    {
      id:'g10',
      need: 10,
      reward: 'PRO навсегда',
      short: 'PRO навсегда',
      icon: 'bolt',
      body: 'Приведи 10 клиентов на PRO — <b>PRO становится твоим навсегда</b>. Никаких списаний.',
      afterTier: 'PRO',
      grantMonths: 0,
    },
    {
      id:'g30',
      need: 30,
      reward: 'BUSINESS навсегда',
      short: 'BUSINESS навсегда',
      icon: 'rocket',
      body: 'Приведи 30 клиентов на PRO — получаешь <b>BUSINESS навсегда</b> с контент-заводом, академией премиум и белым лейблом.',
      afterTier: 'BUSINESS',
      grantMonths: 0,
    },
  ];

  /* --- state --- */
  function loadCount(){
    /* число приведённых на PRO. в live-версии — из партнёрского бэка */
    try{
      var v = parseInt(localStorage.getItem(COUNT_KEY)||'',10);
      if(!isNaN(v) && v>=0) return v;
    }catch(e){}
    /* Пока партнёрский бэкенд не отдал число — ноль.
       Раньше здесь возвращалось 4, и новому человеку показывали чужой
       прогресс, а затем «награду» за цель, которую он не выполнял. */
    return 0;
  }
  function saveCount(n){ try{ localStorage.setItem(COUNT_KEY, String(n)); }catch(e){} }
  function loadClaimed(){
    try{ return JSON.parse(localStorage.getItem(CLAIM_KEY)||'[]')||[]; }catch(e){ return []; }
  }
  function saveClaimed(arr){ try{ localStorage.setItem(CLAIM_KEY, JSON.stringify(arr)); }catch(e){} }

  window.ppGoalsGetCount = loadCount;
  window.ppGoalsSetCount = function(n){
    n = Math.max(0, parseInt(n,10)||0);
    saveCount(n);
    autoClaim();
    render();
  };

  /* Автозачисление при достижении — вызывается при каждом заходе и при изменении count */
  function autoClaim(){
    var count = loadCount();
    var claimed = loadClaimed();
    GOALS.forEach(function(g){
      if(count >= g.need && claimed.indexOf(g.id) < 0){
        claimed.push(g.id);
        applyGoalReward(g);
      }
    });
    saveClaimed(claimed);
  }

  function applyGoalReward(g){
    /* поднимаем tier если нужно */
    if(g.afterTier && typeof PROFILE !== 'undefined'){
      var order = ['FREE','START','PRO','BUSINESS','BUSINESS_PRO','MAX'];
      var cur = String(PROFILE.tier||'FREE').replace(/\s+/g,'_').toUpperCase();
      var iCur = order.indexOf(cur); if(iCur<0) iCur = 0;
      var iNew = order.indexOf(g.afterTier);
      if(iNew > iCur){
        PROFILE.tier = g.afterTier === 'BUSINESS' ? 'BUSINESS' : (g.afterTier === 'PRO' ? 'PRO' : g.afterTier);
      }
      try{ localStorage.setItem('oko-pp-goal-perma-'+g.id, '1'); }catch(e){}
    }
    if(g.grantMonths > 0){
      /* «+N месяцев PRO» — записываем срок, PROFILE.tier временно PRO */
      try{
        var raw = localStorage.getItem('oko-pp-goal-months-pro')||'0';
        var ms = parseInt(raw,10)||0;
        ms += g.grantMonths * 30 * 24 * 3600 * 1000;
        var until = Math.max(Date.now(), ms > 1e12 ? ms : (Date.now() + g.grantMonths*30*24*3600*1000));
        localStorage.setItem('oko-pp-goal-months-pro', String(until));
      }catch(e){}
      if(typeof PROFILE !== 'undefined'){
        var cur = String(PROFILE.tier||'FREE').replace(/\s+/g,'_').toUpperCase();
        if(cur === 'FREE' || cur === 'START') PROFILE.tier = 'PRO';
      }
    }
    try{ if(typeof renderMyProfile==='function') renderMyProfile(); }catch(e){}
    setTimeout(function(){
      if(typeof showPopup === 'function'){
        showPopup({
          ico: g.icon,
          title: 'Награда за партнёрство',
          body: '<p style="font-size:13.5px;line-height:1.55;margin-bottom:8px">Ты выполнил цель <b>«приведи '+g.need+' клиентов на PRO»</b>.</p>'+
                '<div class="pp-goal-reward-box">'+
                  '<div class="pp-goal-reward-ic">'+ico(g.icon)+'</div>'+
                  '<div class="pp-goal-reward-tx"><b>'+esc(g.reward)+'</b><small>зачислено автоматически</small></div>'+
                '</div>',
          actions:[{label:'Отлично'}],
        });
      } else {
        _toast('Награда: '+g.reward);
      }
    }, 500);
  }

  /* --- рендер блока целей внутри партнёрского экрана --- */
  function goalsHtml(){
    var count = loadCount();
    var claimed = loadClaimed();
    var totalNeed = GOALS[GOALS.length-1].need;
    var overallPct = Math.min(100, Math.round(count/totalNeed*100));
    return '<div class="pp-goals-h">'+
        '<div class="pp-goals-title"><span class="pp-goals-title-ic">'+ico('rocket')+'</span>'+
          '<div><b>Реферальные цели</b><small>Приводи клиентов на PRO — получай тариф в подарок</small></div>'+
        '</div>'+
        '<div class="pp-goals-count"><b>'+count+'</b><span>/ '+totalNeed+' клиентов на PRO</span></div>'+
      '</div>'+
      '<div class="pp-goals-overall"><i style="width:'+overallPct+'%"></i></div>'+
      '<div class="pp-goals-list">'+
        GOALS.map(function(g){
          var done = claimed.indexOf(g.id) >= 0 || count >= g.need;
          var pct = Math.min(100, Math.round(count/g.need*100));
          var left = Math.max(0, g.need - count);
          return '<div class="pp-goal '+(done?'done':'')+'">'+
            '<div class="pp-goal-row">'+
              '<div class="pp-goal-ic">'+ico(g.icon)+'</div>'+
              '<div class="pp-goal-body">'+
                '<div class="pp-goal-h">Приведи <b>'+g.need+'</b> клиентов PRO</div>'+
                '<div class="pp-goal-r"><span class="pp-goal-tag">'+esc(g.short)+'</span></div>'+
              '</div>'+
              '<div class="pp-goal-count"><b>'+Math.min(count,g.need)+'</b><span>/'+g.need+'</span></div>'+
            '</div>'+
            '<div class="pp-goal-bar"><i style="width:'+pct+'%"></i></div>'+
            '<div class="pp-goal-status">'+
              (done ? '<span class="pp-goal-done">'+ico('check2')+' Зачислено — '+esc(g.reward)+'</span>'
                    : '<span class="pp-goal-left">Ещё <b>'+left+'</b> · до '+esc(g.reward)+'</span>')+
            '</div>'+
          '</div>';
        }).join('') +
      '</div>'+
      '<div class="pp-goals-actions">'+
        '<button class="btn ghost pp-goals-share" onclick="ppCopyRef()">'+ico('copy')+' Скопировать реф-ссылку и начать</button>'+
      '</div>';
  }

  function render(){
    var scr = document.getElementById('screen-partner');
    if(!scr) return;
    var pad = scr.querySelector('.pad');
    if(!pad) return;
    autoClaim();
    var host = document.getElementById('ppGoals');
    if(!host){
      host = document.createElement('div');
      host.id = 'ppGoals';
      host.className = 'card pp-goals';
      /* вставляем сразу после ladder-блока (уровни партнёра) */
      var ladder = document.getElementById('ppLadder');
      if(ladder && ladder.parentNode){
        if(ladder.nextSibling) ladder.parentNode.insertBefore(host, ladder.nextSibling);
        else ladder.parentNode.appendChild(host);
      } else {
        pad.appendChild(host);
      }
    }
    host.innerHTML = goalsHtml();
  }
  window.ppGoalsRender = render;

  /* --- инициализация --- */
  function init(){
    autoClaim();
    render();
  }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded', init);
  else init();
  setTimeout(init, 800);
  setTimeout(init, 2000);

  /* Патчим showTab, чтобы render'ить при переходе на partner */
  var _showTab = window.showTab;
  if(typeof _showTab === 'function' && !_showTab.__ppGoalsPatched){
    window.showTab = function(t){
      var r = _showTab.apply(this, arguments);
      if(t === 'partner') setTimeout(render, 60);
      return r;
    };
    window.showTab.__ppGoalsPatched = true;
  }
})();

/* ===== module: channels ===== */
/* ================= CHANNELS: платные каналы / курсы + управление (префикс ch) =================
   Реальные каналы как в Telegram + монетизация OKO.
   1) Создание: обычный / платный (N ₽/мес) / курс (видео-уроки внутри), название, описание,
      аватар (буква/иконка), сменяемый фон-градиент.
   2) Платный доступ: превью с paywall + «Подписаться за N ₽/мес» → walletCharge → комиссия
      OKO 10% через okoEarn, владелец получает N минус 10%.
   3) Управление (глубина Telegram): Тип / Обсуждения / Реакции / Админы / Подписчики /
      Статистика (canvas) / Чёрный список / Настройки / Фон-аватар. Реалистичные мок-данные.
   4) Курсы: канал-курс с видео-уроками и прогрессом; пользователь создаёт и продаёт (комиссия 10%).
   5) Входы: «Канал» в меню создания чата (перехват pickChatKind) + строка «Мои каналы» в профиле.
   Единый fullscreen #chView с внутренним стеком страниц chNav (nvPush + step → «назад» работает).
   Персист: localStorage oko-channels. Все стили с префиксом ch-. */

(function(){
'use strict';

const CH_FEE = 0.10;                 // комиссия OKO с продаж каналов/курсов
const CH_LESSON_URL = null;          // видео-уроки — заглушка-плеер (реальный URL Академии подключается отдельно)

/* фирменные градиенты-фоны (сменяемый фон канала) */
const CH_BGS = [
  'linear-gradient(135deg,#0a0a0a 0%,#1a2b00 55%,#9AFF00 140%)', // чёрный → лайм
  'linear-gradient(135deg,#141414,#2a2a2a)',                     // графит
  'linear-gradient(135deg,#0d2818,#0a0a0a 70%)',                 // тёмно-зелёный → чёрный
  'linear-gradient(135deg,#12240a,#050a02)',                     // глубокий лист-зелёный → почти чёрный
  'linear-gradient(135deg,#043024,#07160f)',                     // тёмный изумруд (замена off-brand синего/тиловый)
  'linear-gradient(135deg,#1a2b00,#000)',                        // лайм-тон → чёрный (замена off-brand пурпура)
  'linear-gradient(135deg,#6fd400,#3a7a00)',                     // яркий зелёный
  'linear-gradient(135deg,#111,#000)',                           // почти чёрный
  'linear-gradient(135deg,#2e4d0a,#0d1a00)',                     // олива (замена off-brand фиолетового)
  'linear-gradient(135deg,#1a1a1a,#0a1400)',                     // уголь → тёмно-зелёный (замена off-brand коричневого)
];
const CH_AV_BGS = [   // legacy: плоские цвета аватара (оставлены для совместимости, НЕ используются в рендере)
  '#9AFF00','#e8e8e8','#9AFF00','#f0c000','#00c8ff','#ff4da6','#0a0a0a','#9AFF00','#b98cff','#ff9a3c'
];
/* Процедурные фирменные аватары: только чёрный + лайм/зелёное семейство (никаких off-brand цветов).
   g/c — тёмная тема (градиент-фон / цвет глифа), gl/cl — светлая тема (тайлы не должны быть
   чёрными кляксами на белых карточках → в светлой теме все тайлы светлые/лаймовые).
   Индекс = bg канала или хэш ника. */
const CH_AV_GRADS = [
  {g:'linear-gradient(135deg,#c8ff5e,#9AFF00 52%,#6fd400)', c:'#0a0a0a', gl:'linear-gradient(135deg,#c8ff5e,#9AFF00 52%,#7ad400)', cl:'#0a1400'}, // яркий лайм
  {g:'linear-gradient(135deg,#2c2c2c,#0d0d0d)',            c:'#9AFF00', gl:'linear-gradient(135deg,#e9f5d2,#cfe9a4)',            cl:'#2e4d00'}, // графит → светлый лайм
  {g:'linear-gradient(135deg,#9AFF00,#3a7a00)',            c:'#0a0a0a', gl:'linear-gradient(135deg,#b6f56a,#6fc400)',            cl:'#0a1400'}, // лайм → зелёный
  {g:'linear-gradient(135deg,#eaffcf,#b6f56a)',            c:'#1e3a00', gl:'linear-gradient(135deg,#f2ffe0,#d3f2a6)',            cl:'#2e4d00'}, // бледный лайм
  {g:'linear-gradient(135deg,#14330a,#0a0a0a)',            c:'#9AFF00', gl:'linear-gradient(135deg,#d9efb5,#add86e)',            cl:'#274500'}, // тёмн.зелёный → светлый
  {g:'linear-gradient(135deg,#7ad400,#1f4d00)',            c:'#eaffcf', gl:'linear-gradient(135deg,#9ee84a,#5fae10)',            cl:'#0a1400'}, // средний зелёный
  {g:'linear-gradient(135deg,#0a0a0a,#1a1a1a)',            c:'#9AFF00', gl:'linear-gradient(135deg,#eef8db,#c6e792)',            cl:'#2b4a00'}, // чёрный → светлый лайм
  {g:'linear-gradient(135deg,#b6f56a,#7ad000)',            c:'#0a0a0a', gl:'linear-gradient(135deg,#c6f584,#8fd82e)',            cl:'#0a1400'}, // мягкий лайм
  {g:'linear-gradient(135deg,#1f3d00,#0d1a00)',            c:'#b6f56a', gl:'linear-gradient(135deg,#dcefb0,#b3da70)',            cl:'#2a4700'}, // тёмная олива → светлая
  {g:'linear-gradient(135deg,#8fe600,#2e5c00)',            c:'#f2ffe0', gl:'linear-gradient(135deg,#a9e85a,#5fa614)',            cl:'#0f2600'}, // зелёный
];
/* стиль процедурного аватара по индексу (bg канала) или по хэшу строки (участник).
   Отдаём CSS-переменные (--av-g/--av-c тёмная, --av-gl/--av-cl светлая); фон рисует CSS
   через var() → тема переключается чисто в CSS, без ре-рендера. */
function chAvGrad(i){ return CH_AV_GRADS[((+i||0)%CH_AV_GRADS.length+CH_AV_GRADS.length)%CH_AV_GRADS.length]; }
function chHashIdx(s){ s=String(s||'x'); let h=0; for(let k=0;k<s.length;k++) h=(h*31+s.charCodeAt(k))|0; return Math.abs(h)%CH_AV_GRADS.length; }
function chAvVars(g){ return `--av-g:${g.g};--av-c:${g.c};--av-gl:${g.gl};--av-cl:${g.cl}`; }
function chAvGradStyle(i){ return chAvVars(chAvGrad(i)); }
function chAvSeedStyle(s){ return chAvVars(CH_AV_GRADS[chHashIdx(s)]); }
const CH_ICONS = ['megaphone','bolt','fire','rocket','star','crown','globe','compass'];

/* ---------- состояние ---------- */
let CH = (()=>{ try{ return JSON.parse(localStorage.getItem('oko-channels'))||null; }catch(e){ return null; } })();
function chSave(){ try{ localStorage.setItem('oko-channels', JSON.stringify(CH)); }catch(e){} }

function chSeed(){
  /* СТАРТОВЫЙ НАБОР КАНАЛОВ.
     Правка Даниэля 09.08: «оставь только официальные чаты и каналы OKO,
     остальное уже будут создавать люди». Убраны выдуманные авторские каналы
     («Клуб роста · PRO», «Reels под ключ · курс», «Трафик и деньги»,
     «Нейро-дизайн PRO», «Python с нуля») вместе с фейковыми подписчиками,
     оборотами и отзывами.
     Остаются два официальных канала OKO. Своих каналов у нового человека нет —
     он создаёт их сам кнопкой «Создать канал». */
  const mine = [];

  const disc = [
    { id:'ch-disc-4', name:'OKO Новости',
      desc:'Официальный канал обновлений OKO: релизы, новые возможности, важное для авторов. Бесплатно для всех.',
      icon:'megaphone', bg:6, kind:'channel', access:'open', type:'free', price:0,
      verified:true, official:true, subs:0, reactions:true, discussions:false,
      owner:'Команда OKO', ownerNick:'okonews', posts:[] },

    { id:'ch-disc-5', name:'Инсайдеры OKO',
      desc:'Закрытый бесплатный канал для активных участников: ранний доступ к новым возможностям, созвоны с командой и влияние на дорожную карту. Вступление по заявке.',
      icon:'star', bg:2, kind:'channel', access:'closed', type:'free', price:0,
      verified:true, official:true, subs:0, reactions:true, discussions:true,
      owner:'Команда OKO', ownerNick:'oko_insiders', posts:[] },
  ];

  return { v:3, seq:0, pseq:0, mine, disc, sub:{}, prog:{}, likes:{}, cmt:{} };
}
/* v3 = чистка демо-каналов 09.08: старые сохранённые каталоги пересобираем. */
if(!CH || !CH.v || CH.v < 3){ CH = chSeed(); chSave(); }
CH.sub = CH.sub||{}; CH.prog = CH.prog||{}; CH.likes = CH.likes||{}; CH.votes = CH.votes||{}; CH.notify = CH.notify||{};
CH.cmt = CH.cmt||{}; CH.pseq = CH.pseq||0;   // cmt: комментарии по стабильному id поста; pseq: счётчик id постов
/* стабильный id поста (не зависит от позиции в массиве — лайки/голоса/комменты не «съезжают» после публикации) */
function chPostId(p){ if(p && !p.id){ p.id = 'p'+(++CH.pseq); } return p ? p.id : ''; }
function chEnsurePostIds(c){ (c&&c.posts||[]).forEach(chPostId); }
/* миграция v1→v2: нормализуем оси доступ/оплата, не теряя каналы пользователя */
(function chMigrate(){
  try{
    (CH.mine||[]).forEach(c=>{ chNormalize(c); chEnsurePostIds(c); });
    (CH.disc||[]).forEach(c=>{ chNormalize(c); chEnsurePostIds(c); });
    if((CH.v||1) < 2){
      // добавляем витринные примеры новых осей, если их ещё нет
      const seed = chSeed();
      (seed.disc||[]).forEach(s=>{ if(!CH.disc.some(x=>x.id===s.id)) CH.disc.push(chNormalize(s)); });
      CH.v = 2;
    }
    chSave();
  }catch(e){}
})();

function chMockMembers(n){
  /* демо-сидер отключён 09.08 (правка Даниэля: ноль демо-данных) */
  return (arguments.length && Array.isArray(arguments[0])) ? [] : undefined;

  const pool = [
    ['Марина Ковалёва','marina_k'],['Игорь Власов','igor_v'],['Алина Романова','alina_r'],
    ['Сергей Данилов','sergey_d'],['Катя SMM','katya_smm'],['Олег Петров','oleg_p'],
    ['Настя Ким','nastya_kim'],['Роман Гуров','roman_g'],['Лена Соболь','lena_s'],['Артём Лис','artem_l']
  ];
  const days=['5 мин','1 ч','сегодня','вчера','2 дня','неделю'];
  return pool.slice(0,n).map((p,i)=>({name:p[0], nick:p[1], joined:days[i%days.length]}));
}

/* ---------- хелперы ---------- */
const chI = (n,cls)=> (typeof I==='function') ? I(n,cls) : `<svg class="i"><use href="#i-${n}"/></svg>`;
function chEsc(t){ return (typeof esc==='function') ? esc(t) : String(t==null?'':t); }
function chFmtN(n){ return (typeof fmtN==='function') ? fmtN(n) : (n>=1000?(n/1000).toFixed(1).replace('.0','')+'к':n); }
function chChannel(id){ return CH.mine.find(c=>c.id===id) || CH.disc.find(c=>c.id===id) || null; }
function chIsMine(c){ return CH.mine.some(x=>x.id===c.id); }

/* ---- две независимые оси: ДОСТУП (открытый/закрытый) + ОПЛАТА (бесплатно/платно) ----
   плюс вид продукта kind: channel | club | course. gated = нужна витрина для не-участника. */
function chNormalize(c){
  if(!c) return c;
  if(c.kind==null) c.kind = (c.type==='course') ? 'course' : (c.club ? 'club' : 'channel');
  if(c.price==null) c.price = (c.type==='free') ? 0 : (c.price||0);
  if(c.access==null) c.access = (c.type==='paid' || c.type==='course' || c.kind!=='channel' || (c.price||0)>0) ? 'closed' : 'open';
  if(c.reactions==null) c.reactions = true;
  if(c.discussions==null) c.discussions = (c.kind!=='course');
  if(c.kind==='course' && !c.lessons) c.lessons = [{id:'l1',title:'Вводный урок',dur:'5:00'}];
  // --- расширенные настройки управления (глубина Telegram) ---
  if(c.autopost==null)    c.autopost    = true;                               // авто-постинг новых постов в ленту рекомендаций
  if(c.slowmode==null)    c.slowmode    = 0;                                  // медленный режим, сек (0 = выкл)
  if(c.whoPost==null)     c.whoPost     = 'admins';                           // кто может писать: admins | subs
  if(c.privSubs==null)    c.privSubs    = (c.access==='closed'?'admins':'all'); // кто видит список подписчиков: all | subs | admins
  if(c.privFwd==null)     c.privFwd     = true;                               // разрешить пересылку/сохранение постов
  if(c.privHistory==null) c.privHistory = 'visible';                          // история для новых: visible | hidden
  if(c.archived==null)    c.archived    = false;                             // канал в архиве
  if(c.pinned===undefined) c.pinned     = null;                              // id закреплённого поста (закреп)
  if(!Array.isArray(c.invites)) c.invites = [];                              // пригласительные ссылки
  if(c.chatBgUrl===undefined) c.chatBgUrl = null;                            // свой фон чата (data URL)
  if(c.website===undefined)   c.website   = '';                              // сайт-ссылка (PRO)
  if(c.verifyRequested===undefined) c.verifyRequested = false;               // заявка на верификацию
  if(c.kind==='channel' && c.commentsOn===undefined) c.commentsOn = true;    // комменты к постам (админ вкл/выкл)
  // гранулярные права администраторов (как в Telegram): доп-миграция старых записей
  if(Array.isArray(c.admins)) c.admins.forEach(a=>{ if(a && !a.rights) a.rights = chDefaultRights(); });
  // legacy-поле type держим синхронным — вдруг читает внешний код
  c.type = c.kind==='course' ? 'course' : ((c.price||0)>0 ? 'paid' : 'free');
  return c;
}
function chGated(c){ return !!c && (c.kind==='course' || c.access==='closed' || (c.price||0)>0); }
function chPaid(c){ return (c.price||0)>0; }
function chKindLabel(c){ return c.kind==='course'?'Курс':c.kind==='club'?'Клуб':c.kind==='sgroup'?'Супергруппа':c.kind==='chat'?'Чат':'Канал'; }
function chPriceUnit(c){ return c.kind==='course'?'':'/мес'; }
function chIsSubbed(c){ return chIsMine(c) || !!CH.sub[c.id] || !chGated(c); }
/* краткая подпись типа: «Закрытый · Платно 299 ₽/мес» */
function chTypeLabel(t){ return t==='paid'?'Платный':t==='course'?'Курс':'Обычный'; } // legacy
function chAccessLine(c){
  const parts = [c.access==='closed'?'Закрытый':'Открытый'];
  if(c.kind==='course') parts.push(chPaid(c)?c.price+' ₽':'бесплатно');
  else parts.push(chPaid(c)?('Платно '+c.price+' ₽'+chPriceUnit(c)):'Бесплатно');
  return parts.join(' · ');
}
/* компактная подпись для строк-настроек (без слова «Платно» — цена и так его подразумевает),
   чтобы значение помещалось на 390px без обрезки */
function chAccessLineShort(c){
  const access = c.access==='closed'?'Закрытый':'Открытый';
  if(c.kind==='course') return 'Курс · '+(chPaid(c)?c.price+' ₽':'бесплатно');
  return access+' · '+(chPaid(c)?(c.price+' ₽'+chPriceUnit(c)):'бесплатно');
}
/* SVG-бейджи для карточки/витрины */
function chBadges(c){
  let h='';
  if(c.access==='closed') h+=`<span class="ch-tag closed">${chI('lock')}Закрытый</span>`;
  if(c.kind==='course') h+=`<span class="ch-tag course">${chI('circle-play')}Курс</span>`;
  else if(c.kind==='club') h+=`<span class="ch-tag club">${chI('crown')}Клуб</span>`;
  if(chPaid(c)) h+=`<span class="ch-tag paid">${c.kind==='course'?c.price+' ₽':'Платно '+c.price+' ₽'+chPriceUnit(c)}</span>`;
  else if(c.access==='closed') h+=`<span class="ch-tag free">Бесплатно</span>`;
  return h;
}
function chAvStyle(c){ return `background:${CH_BGS[c.bg||0]};`; }
function chAvInner(c,cls,lock){
  const lk = lock ? `<span class="ch-avlock">${chI('lock')}</span>` : '';
  if(c.avatar){
    return `<div class="ch-av ${cls||''}" style="${chAvPhoto(c)}">${lk}</div>`;
  }
  const content = c.icon ? chI(c.icon) : chEsc((c.name[0]||'K').toUpperCase());
  return `<div class="ch-av ${cls||''}" style="${chAvGradStyle(c.bg||0)}">${content}${lk}</div>`;
}
function chCourseProg(c){
  if(c.kind!=='course' || !c.lessons) return 0;
  const done = (CH.prog[c.id]||[]).length;
  return Math.round(done/c.lessons.length*100);
}
function chBadge(c){ return c.verified ? `<svg class="i fill" style="width:0.95em;height:0.95em;vertical-align:-0.12em;margin-left:1px;color:var(--lime)"><use href="#i-verified"/></svg>` : ''; }
function chSlug(s){ return String(s||'').toLowerCase().replace(/[^a-zа-я0-9_]/gi,'').replace(/ё/g,'е').slice(0,18) || 'channel'; }
function chNick(c){ if(!c.nick) c.nick = chSlug(c.name); return c.nick; }
/* url(...) для фото-аватара/обложки, если владелец загрузил картинку */
function chAvPhoto(c){ return c.avatar ? `background-image:url(${c.avatar});background-size:cover;background-position:center;color:transparent` : ''; }

/* ===== Telegram-grade управление: сгруппированные списки, строки-контролы, пикеры ===== */
/* группа строк в одной карточке с разделителями (как настройки Telegram/iOS) */
function chList(rows){ return `<div class="ch-list">${(rows||[]).filter(Boolean).join('')}</div>`; }
function chLF(txt){ return `<div class="ch-lf">${txt}</div>`; }
/* строка-переход: иконка + заголовок(+подпись) + значение справа + шеврон/копи */
function chNavRow(ic, label, val, onclick, o){
  o = o||{};
  const rv = (val!=null && val!=='') ? `<span class="ch-rv-t${o.lime?' lime':''}">${val}</span>` : '';
  const tail = o.copy ? chI('copy') : (o.noChev ? '' : chI('chev'));
  return `<button class="ch-row${o.danger?' danger':''}" onclick="${onclick}">
    ${ic?`<span class="ch-row-ic${o.danger?' danger':''}">${chI(ic)}</span>`:''}
    <span class="ch-row-main"><b>${label}</b>${o.sub?`<small>${o.sub}</small>`:''}</span>
    <span class="ch-row-val">${rv}${tail}</span>
  </button>`;
}
/* строка-ссылка: URL занимает всю ширину под заголовком (не обрезается на 390px как боковое значение) */
function chLinkRow(ic, label, url, onclick){
  return `<button class="ch-row" onclick="${onclick}">
    ${ic?`<span class="ch-row-ic">${chI(ic)}</span>`:''}
    <span class="ch-row-main"><b>${label}</b><small class="ch-url">${chEsc(url)}</small></span>
    <span class="ch-row-val">${chI('copy')}</span>
  </button>`;
}
/* строка-тумблер: клик переключает состояние (surgical, без полного ре-рендера) */
function chTglRow(ic, label, sub, on, onclick){
  return `<button class="ch-row" onclick="${onclick}">
    ${ic?`<span class="ch-row-ic">${chI(ic)}</span>`:''}
    <span class="ch-row-main"><b>${label}</b>${sub?`<small>${sub}</small>`:''}</span>
    <span class="switch ${on?'on':''}"><i></i></span>
  </button>`;
}
/* значение-подписи для настроек */
function chNotifyOn(c){ return CH.notify[c.id]!==false; }
function chSlowLabel(s){ s=+s||0; return s===0?'выкл':(s<60?s+' сек':(s/60)+' мин'); }
function chWhoPostLabel(c){ return c.whoPost==='subs'?'Все подписчики':'Только админы'; }
function chPrivSubsLabel(c){ return c.privSubs==='all'?'Все':c.privSubs==='subs'?'Подписчики':'Только админы'; }
function chPrivHistLabel(c){ return c.privHistory==='hidden'?'Скрыта':'Видна'; }
/* публичная ссылка канала — реальный домен OKO в стиле @id */
function chPublicLink(c){ return 'okoteam.top/@'+chNick(c); }

/* ---- закрепы (pinned) ---- */
/* id закреплённого поста, если он всё ещё существует (самоочистка от «висячих» ссылок) */
function chPinnedId(c){ if(!c||!c.pinned) return null; return (c.posts||[]).some(p=>chPostId(p)===c.pinned) ? c.pinned : null; }

/* ---- гранулярные права администратора (как в Telegram) ---- */
const CH_RIGHTS = [
  ['post','megaphone','Публикация постов','Создавать и отправлять записи в канал'],
  ['edit','edit','Редактирование','Изменять и удалять любые посты'],
  ['pin','pin','Закреплять сообщения','Управлять закреплённой записью'],
  ['ban','lock','Блокировка участников','Банить и разблокировать людей'],
  ['invite','forward','Приглашения','Создавать пригласительные ссылки'],
  ['addAdmins','crown','Назначение админов','Добавлять новых администраторов']
];
function chDefaultRights(){ return {post:true, edit:true, pin:true, ban:true, invite:true, addAdmins:false}; }
function chRightsSummary(a){
  const r=(a&&a.rights)||{}, keys=CH_RIGHTS.map(x=>x[0]);
  const on=keys.filter(k=>r[k]).length;
  return on>=keys.length ? 'Полные права' : (on===0 ? 'Права ограничены' : on+' из '+keys.length+' прав');
}

/* быстрый тумблер булевого поля канала — переключаем класс на месте, без chRender */
window.chTgl = function(id, key, el){
  const c = chChannel(id); if(!c) return;
  c[key] = !c[key]; chSave();
  if(el){ const sw = el.querySelector('.switch'); if(sw) sw.classList.toggle('on', !!c[key]); }
  if(key==='discussions') toast(c[key]?'Обсуждения включены':'Обсуждения выключены');
  else if(key==='reactions') toast(c[key]?'Реакции включены':'Реакции выключены');
  else if(key==='autopost') toast(c[key]?'Авто-постинг включён':'Авто-постинг выключен');
  else if(key==='archived') toast(c[key]?'Канал в архиве':'Канал возвращён из архива');
};
/* уведомления подписчика (по-канальная тишина) */
window.chToggleNotify = function(id, el){
  const c = chChannel(id); if(!c) return;
  const nowOn = CH.notify[id]!==false;
  CH.notify[id] = nowOn ? false : true; chSave();
  if(el){
    const sw = el.querySelector('.switch');
    if(sw){ sw.classList.toggle('on', !nowOn); }
    else { el.classList.toggle('on', !nowOn); const sp = el.querySelector('span'); if(sp) sp.textContent = (!nowOn)?'Уведомления':'Без звука'; }
  }
  toast(nowOn?'Уведомления выключены':'Уведомления включены');
};

/* универсальный выбор из вариантов (как action sheet Telegram) */
function chChooser(title, body, options){
  if(typeof showPopup!=='function'){ return; }
  showPopup({ title, body, actions: options.concat([{label:'Отмена', ghost:true}]) });
}
window.chPickSlow = function(id){
  const c = chChannel(id); if(!c) return;
  chChooser('Медленный режим', 'Минимальный интервал между сообщениями участников', [0,10,30,60,300,900].map(s=>({
    label: chSlowLabel(s), onclick:()=>{ c.slowmode=s; chSave(); chRender(); toast('Медленный режим: '+chSlowLabel(s)); }
  })));
};
window.chPickWhoPost = function(id){
  const c = chChannel(id); if(!c) return;
  chChooser('Кто может писать', 'Кому разрешено публиковать в канале', [
    {label:'Только админы', onclick:()=>{ c.whoPost='admins'; chSave(); chRender(); }},
    {label:'Все подписчики', onclick:()=>{ c.whoPost='subs'; chSave(); chRender(); }},
  ]);
};
window.chPickPrivSubs = function(id){
  const c = chChannel(id); if(!c) return;
  chChooser('Кто видит подписчиков', 'Видимость списка участников канала', [
    {label:'Все', onclick:()=>{ c.privSubs='all'; chSave(); chRender(); }},
    {label:'Подписчики', onclick:()=>{ c.privSubs='subs'; chSave(); chRender(); }},
    {label:'Только админы', onclick:()=>{ c.privSubs='admins'; chSave(); chRender(); }},
  ]);
};
window.chPickPrivHist = function(id){
  const c = chChannel(id); if(!c) return;
  chChooser('История для новых', 'Что видят только что вступившие подписчики', [
    {label:'Видна вся история', onclick:()=>{ c.privHistory='visible'; chSave(); chRender(); }},
    {label:'Скрыта до вступления', onclick:()=>{ c.privHistory='hidden'; chSave(); chRender(); }},
  ]);
};

/* поделиться каналом (нативный share → иначе копирование ссылки) */
window.chShareChannel = function(id){
  const c = chChannel(id); if(!c) return;
  const link = chPublicLink(c);
  try{
    if(navigator.share){ navigator.share({title:c.name, text:'Канал в OKO', url:'https://'+link}).catch(()=>{}); }
    else if(navigator.clipboard){ navigator.clipboard.writeText(link); }
  }catch(e){}
  toast('Ссылка на канал скопирована');
};
/* меню «Ещё» — action sheet для владельца и подписчика */
window.chMoreMenu = function(id){
  const c = chChannel(id); if(!c) return;
  const mine = chIsMine(c);
  const acts = [
    {label:'Поделиться', onclick:()=>chShareChannel(id)},
    {label:'Скопировать ссылку', onclick:()=>chCopyLink(id)},
  ];
  if(mine){
    acts.push({label:'Управление каналом', onclick:()=>chGo('manage',id)});
    acts.push({label:'Статистика', onclick:()=>chGo('mStats',id)});
  } else {
    acts.push({label: chNotifyOn(c)?'Выключить уведомления':'Включить уведомления', onclick:()=>chToggleNotify(id)});
    acts.push({label:'Пожаловаться', onclick:()=>toast('Жалоба отправлена на модерацию')});
    if(chGated(c)) acts.push({label: chPaid(c)?'Отменить подписку':'Покинуть канал', ghost:true, onclick:()=>chUnsub(id)});
  }
  acts.push({label:'Закрыть', ghost:true});
  if(typeof showPopup==='function') showPopup({ico:'more', title:chEsc(c.name), body:'@'+chEsc(chNick(c)), actions:acts});
};

/* канал публикует пост → он попадает и в ленту рекомендаций (как Instagram) */
let CH_FEED_SEQ = 900000;
function chPushToFeed(c, post){
  if(typeof POSTS==='undefined' || !POSTS.rec) return;
  const id = ++CH_FEED_SEQ;
  post._feedId = id;
  POSTS.rec.unshift({
    id, ava:(c.name[0]||'K').toUpperCase(), avaIcon:c.icon||null, avaImg:c.avatar||null,
    name:c.name, sub:(c.kind==='club'?'клуб':c.kind==='course'?'курс':'канал')+' · '+chFmtN(c.subs||0), body:post.txt||'',
    media: post.img ? null : (post.media==='circle-play'?'0:30':post.media==='poll'?'опрос':null),
    img: post.img||null, likes:post.likes||0, views:post.views||1,
    liked:false, saved:false, reposts:0, comments:[], chOrigin:c.id, chKind:c.kind||'channel'
  });
  if(typeof renderFeed==='function' && typeof curFeedKind!=='undefined' && curFeedKind==='rec'){ try{ renderFeed('rec'); }catch(e){} }
}
/* чтение картинки с устройства → сжатие в dataURL (persist-safe) */
function chReadImage(file, maxW, quality, cb){
  if(!file || !/^image\//.test(file.type)){ toast('Выберите изображение'); return; }
  const rd = new FileReader();
  rd.onload = e=>{
    const img = new Image();
    img.onload = ()=>{
      const sc = Math.min(1, maxW/img.width);
      const cv = document.createElement('canvas');
      cv.width = Math.round(img.width*sc); cv.height = Math.round(img.height*sc);
      cv.getContext('2d').drawImage(img,0,0,cv.width,cv.height);
      let out; try{ out = cv.toDataURL('image/jpeg', quality); }catch(err){ out = e.target.result; }
      cb(out);
    };
    img.onerror = ()=>toast('Не удалось прочитать изображение');
    img.src = e.target.result;
  };
  rd.onerror = ()=>toast('Ошибка чтения файла');
  rd.readAsDataURL(file);
}
/* невидимый file-input, вызывается по кнопке */
function chPickFile(onFile){
  const inp = document.createElement('input');
  inp.type='file'; inp.accept='image/*'; inp.style.display='none';
  inp.onchange = ()=>{ const f=inp.files&&inp.files[0]; if(f) onFile(f); inp.remove(); };
  document.body.appendChild(inp); inp.click();
}

/* ================= НАВИГАЦИЯ ВЬЮХИ (внутренний стек страниц) ================= */
let chNav = [];   // [{page, arg}]
let chListFilter = {q:'', kind:'all'};   // поиск+фильтр по списку каналов

function chOpen(page, arg){
  const v = document.getElementById('chView');
  if(!v) return;
  const fresh = !v.classList.contains('open');
  if(fresh) chNav = [];
  chNav.push({page:page||'list', arg:arg||null});
  chRender();
  if(fresh){
    v.classList.add('open');
    if(typeof nvPush==='function') nvPush('view:channels', chClose, chStep);
  }
}
function chGo(page, arg){ chOpen(page, arg); }   // deeper navigation (view already open)
function chStep(){ if(chNav.length>1){ chNav.pop(); chRender(); return true; } return false; }
function chBack(){ if(chNav.length>1){ chNav.pop(); chRender(); } else chClose(); }
function chClose(){
  const v = document.getElementById('chView');
  if(v) v.classList.remove('open');
  chNav = [];
  if(typeof nvPop==='function') nvPop('view:channels');
}

/* ---------- роутер рендера ---------- */
function chRender(){
  const top = chNav[chNav.length-1] || {page:'list'};
  const body = document.getElementById('chBody');
  const titleEl = document.getElementById('chHeadTitle');
  const tools = document.getElementById('chHeadTools');
  if(!body) return;
  tools.innerHTML = '';
  /* сохраняем позицию прокрутки при повторном рендере той же страницы (лайк/голос/фильтр) */
  const sig = top.page+':'+(top.arg && typeof top.arg==='object' ? JSON.stringify(top.arg) : (top.arg==null?'':top.arg));
  const keepScroll = (chRender._sig === sig);
  const prevScroll = keepScroll ? body.scrollTop : 0;
  let out = {title:'Каналы', html:''};
  switch(top.page){
    case 'list':    out = chPageList(); break;
    case 'create':  out = chPageCreate(); break;
    case 'catalog': out = chPageCatalog(); break;
    case 'channel': out = chPageChannel(top.arg); break;
    case 'lesson':  out = chPageLesson(top.arg); break;
    case 'compose': out = chPageCompose(top.arg); break;
    case 'addLesson': out = chPageAddLesson(top.arg); break;
    case 'manage':  out = chPageManage(top.arg); break;
    case 'mType':   out = chPageMType(top.arg); break;
    case 'mSubs':   out = chPageMSubs(top.arg); break;
    case 'mAdmins': out = chPageMAdmins(top.arg); break;
    case 'mAdminRights': out = chPageMAdminRights(top.arg); break;
    case 'mBlack':  out = chPageMBlack(top.arg); break;
    case 'mStats':  out = chPageMStats(top.arg); break;
    case 'mAppear': out = chPageMAppear(top.arg); break;
    case 'mSettings': out = chPageMSettings(top.arg); break;
    case 'mInvites': out = chPageMInvites(top.arg); break;
    default: out = chPageList();
  }
  titleEl.textContent = out.title;
  if(out.tools) tools.innerHTML = out.tools;
  body.innerHTML = out.html;
  body.scrollTop = keepScroll ? prevScroll : 0;
  chRender._sig = sig;
  if(out.after) try{ out.after(); }catch(e){}
}

/* ================= СТРАНИЦА: МОИ КАНАЛЫ ================= */
function chPageList(){
  const subbed = CH.disc.filter(c=>CH.sub[c.id]);
  const discover = CH.disc.filter(c=>!CH.sub[c.id]);
  const card = (c,ctx)=>{
    const subbed = chIsSubbed(c);
    const prog = c.kind==='course' ? chCourseProg(c) : null;
    const sub = c.kind==='course'
      ? `${chI('circle-play')} ${c.lessons?c.lessons.length:0} уроков${prog!=null&&subbed?` · ${prog}%`:''}`
      : `${chI('users')} ${chFmtN(c.subs||0)} подписчиков`;
    const locked = chGated(c) && !subbed;
    let right = '';
    if(ctx!=='mine'){
      if(subbed) right = `<span class="ch-tag free">${chI('check')}доступ</span>`;
      else if(chPaid(c)) right = `<span class="ch-price-pill">${c.price} ₽${chPriceUnit(c)}</span>`;
      else if(c.access==='closed') right = `<span class="ch-price-pill dim-pill">${chI('lock')}заявка</span>`;
    }
    return `<div class="ch-card" onclick="chGo('channel','${c.id}')">
      ${chAvInner(c, locked?'locked':'', locked)}
      <div class="ch-cbody">
        <div class="ch-cname"><span style="min-width:0;flex:0 1 auto;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${chEsc(c.name)}</span>${chBadge(c)}</div>
        <div class="ch-csub">${sub}</div>
        <div class="ch-cbadges">${chBadges(c)}</div>
      </div>
      ${right}</div>`;
  };
  let html = `<button class="ch-create-cta" onclick="chGo('create')">
      <span class="ch-cc-ic">${chI('plus')}</span>
      <span><b>Создать канал</b><small>Канал, клуб или видео-курс · открытый/закрытый, платный/бесплатный</small></span>
    </button>`;
  html += `<div class="ch-sec-h">${chI('crown')} Мои каналы</div>`;
  html += CH.mine.length ? CH.mine.map(c=>card(c,'mine')).join('')
    : `<div class="ch-empty">Пока нет своих каналов. Создай первый — и начни зарабатывать.</div>`;
  if(subbed.length){
    html += `<div class="ch-sec-h">${chI('bookmark')} Мои подписки</div>`;
    html += subbed.map(c=>card(c,'sub')).join('');
  }
  html += `<div class="ch-sec-h">${chI('compass')} Рекомендуем · каналы, клубы и курсы</div>`;
  html += discover.map(c=>card(c,'disc')).join('');
  return {title:'Каналы', html};
}

/* ================= СТРАНИЦА: КАТАЛОГ ПЛАТНЫХ КАНАЛОВ/КЛУБОВ/КУРСОВ =================
   Полноэкранная витрина только закрытого/платного контента. Фильтры: ниша, цена,
   формат, сортировка. Правка Даниэля 29.07 — «список с фильтрами по всем закрытым
   платным каналам/клубам/курсам». Данные — из CH.disc + CH.mine (не свои). */
const CH_NICHES = [
  {k:'all',  name:'Все'},
  {k:'smm',  name:'SMM и трафик'},
  {k:'biz',  name:'Бизнес и продажи'},
  {k:'psy',  name:'Психология'},
  {k:'des',  name:'Дизайн и AI'},
  {k:'fin',  name:'Финансы'},
  {k:'edu',  name:'Образование'},
  {k:'life', name:'Стиль жизни'},
];
function chNiche(c){
  if(c.niche) return c.niche;
  const s = ((c.name||'')+' '+(c.desc||'')).toLowerCase();
  if(/(smm|reels|traffic|трафик|блог|инстаг|тикток|аудит|видео|reels)/.test(s)) return 'smm';
  if(/(бизнес|продаж|деньг|доход|запуск|воронк|прибыл|роста|инсайд)/.test(s)) return 'biz';
  if(/(психолог|отношен|самооценк|терап|тревог|эмоц)/.test(s)) return 'psy';
  if(/(дизайн|нейро|midjourney|ai|обложк|интерфейс|figma|graphic)/.test(s)) return 'des';
  if(/(финанс|инвест|акции|крипт|бюджет|ipo|фондов)/.test(s)) return 'fin';
  if(/(курс|учеб|обучен|уроки|школ|мастер|python|программ)/.test(s)) return 'edu';
  return 'life';
}
/* мок-рейтинг: если у карточки задан явный rating — берём его, иначе стабильный хэш по id */
function chRating(c){
  if(c && typeof c.rating==='number' && c.rating>0) return c.rating;
  const s = String((c&&c.id)||(c&&c.name)||''); let h=0; for(let i=0;i<s.length;i++) h=(h*33+s.charCodeAt(i))|0;
  return (40 + Math.abs(h)%15) / 10;  // 4.0 … 5.4 → нормируем
}
function chRatingN(c){ const r = chRating(c); return Math.min(5, +r.toFixed(1)); }
/* мок-отзывы: реалистичное количество; если у карточки не задано, тянем стабильный хэш */
function chReviewsN(c){
  if(c && typeof c.reviews==='number' && c.reviews>=0) return c.reviews;
  const s = String((c&&c.id)||''); let h=0; for(let i=0;i<s.length;i++) h=(h*29+s.charCodeAt(i))|0;
  return 40 + Math.abs(h)%180;
}

const CH_CAT = {niche:'all', price:'any', kind:'any', sort:'top'};
function chPageCatalog(){
  const all = ((CH.disc||[]).concat((CH.mine||[]).filter(c=>false)))  /* только чужие */
    .filter(c=>chGated(c));   /* только закрытое/платное — как просил Даниэль */
  const priceMax = {any:Infinity, '500':500, '1000':1000, '2500':2500, '5000':5000};
  const list = all.filter(c=>{
    if(CH_CAT.niche!=='all' && chNiche(c)!==CH_CAT.niche) return false;
    if(CH_CAT.kind!=='any' && (c.kind||'channel')!==CH_CAT.kind) return false;
    const pm = priceMax[CH_CAT.price] ?? Infinity;
    if((c.price||0) > pm) return false;
    return true;
  }).sort((a,b)=>{
    if(CH_CAT.sort==='new')   return (b.subs||0)*0.1 - (a.subs||0)*0.1 + (Math.random()-0.5);  // мок: без даты — рандом
    if(CH_CAT.sort==='cheap') return (a.price||0) - (b.price||0);
    if(CH_CAT.sort==='rating')return chRating(b)  - chRating(a);
    return (b.subs||0) - (a.subs||0);  // top
  });

  const niches = CH_NICHES.map(n=>`<button class="ch-cat-chip ${CH_CAT.niche===n.k?'on':''}" onclick="chCatSet('niche','${n.k}')">${chEsc(n.name)}</button>`).join('');
  const prices = [['any','Любая'],['500','до 500 ₽'],['1000','до 1000 ₽'],['2500','до 2500 ₽'],['5000','до 5000 ₽']]
    .map(([k,l])=>`<button class="ch-cat-chip ${CH_CAT.price===k?'on':''}" onclick="chCatSet('price','${k}')">${l}</button>`).join('');
  const kinds = [['any','Все'],['channel','Каналы'],['club','Клубы'],['course','Курсы']]
    .map(([k,l])=>`<button class="ch-cat-chip ${CH_CAT.kind===k?'on':''}" onclick="chCatSet('kind','${k}')">${l}</button>`).join('');
  const sorts = [['top','Топ'],['new','Новые'],['cheap','Дешевле'],['rating','Рейтинг']]
    .map(([k,l])=>`<button class="ch-cat-chip ${CH_CAT.sort===k?'on':''}" onclick="chCatSet('sort','${k}')">${l}</button>`).join('');

  const cards = list.map(c=>{
    const paid = chPaid(c);
    const priceHtml = paid
      ? `<span class="ch-catx-price">${c.price} ₽${chPriceUnit(c)}</span>`
      : `<span class="ch-catx-price free">Бесплатно</span>`;
    const rating = chRatingN(c);
    const rev = chReviewsN(c);
    const stars = `<span class="ch-catx-star">${chI('star')}${rating.toFixed(1)}<small style="opacity:.72;font-weight:600;margin-left:3px">(${chFmtN(rev)})</small></span>`;
    const kindTag = c.kind==='course'?'Курс':c.kind==='club'?'Клуб':'Канал';
    const usersLabel = c.kind==='course' ? 'учеников' : (c.kind==='club' ? 'участников' : 'подписчиков');
    return `<button class="ch-catx-card" onclick="chGo('channel','${c.id}')">
      <div class="ch-catx-hero" style="background:${CH_BGS[c.bg||0]}">
        <span class="ch-catx-kind">${chI(c.kind==='course'?'circle-play':c.kind==='club'?'crown':'megaphone')}${kindTag}</span>
        ${priceHtml}
      </div>
      <div class="ch-catx-body">
        <div class="ch-catx-name">${chEsc(c.name)}${chBadge(c)}</div>
        <div class="ch-catx-sub">${chEsc(c.owner||'@'+chNick(c))}</div>
        <div class="ch-catx-meta">${stars}<span title="${usersLabel}">${chI('users')}${chFmtN(c.subs||0)}</span></div>
      </div>
    </button>`;
  }).join('');

  const empty = list.length ? '' : `<div class="ch-empty">По фильтрам ничего не нашлось. Сбрось фильтры и попробуй снова.</div>`;
  const html = `
    <div class="ch-cat-hint">${chI('bookmark')} Каталог платных каналов, клубов и курсов OKO. Комиссия платформы 10% — авторы получают остальное.</div>
    <div class="ch-cat-filter">
      <div class="ch-cat-lab">Ниша</div>
      <div class="ch-cat-chips">${niches}</div>
      <div class="ch-cat-lab">Цена</div>
      <div class="ch-cat-chips">${prices}</div>
      <div class="ch-cat-lab">Формат</div>
      <div class="ch-cat-chips">${kinds}</div>
      <div class="ch-cat-lab">Сортировка</div>
      <div class="ch-cat-chips">${sorts}</div>
    </div>
    <div class="ch-cat-count">${list.length} ${list.length%10===1&&list.length%100!==11?'канал':'каналов'}</div>
    <div class="ch-catx-grid">${cards}</div>
    ${empty}`;
  return {title:'Каталог', html};
}
window.chCatSet = function(k,v){ CH_CAT[k]=v; chRender(); };

/* ================= СТРАНИЦА: СОЗДАНИЕ (правки Даниэля 29.07) =================
   Единый мастер: чат / супергруппа / канал / клуб / курс. TG-parity: ава+обложка
   можно грузить с устройства прямо в создании, PRO-фичи (ссылка на сайт, галочка
   верификации, кастомный фон чата) размечены значком PRO и открываются с START+. */
const chDraft = {
  kind:'channel', access:'open', name:'', desc:'',
  price:0, bg:0, chatBg:null, chatBgUrl:null,
  icon:'megaphone', useIcon:false,
  cover:null, avatar:null,
  website:'', verified:false
};
function chIsPro(){ try{ return (typeof okoIsPremium==='function') && okoIsPremium(); }catch(e){ return false; } }
function chFreeBgs(){ return CH_BGS.slice(0,5); }  /* FREE — только 5 стандартных */
function chAllowedBgs(){ return chIsPro() ? CH_BGS : chFreeBgs(); }
function chPageCreate(){
  const d = chDraft;
  const kinds = [
    ['channel','megaphone','Канал','Лента постов для аудитории. Пишет только админ, юзеры — в комментарии, если админ разрешил.'],
    ['sgroup','users','Супергруппа','Как в Telegram: до 200 000 участников, темы, роли, модерация.'],
    ['club','crown','Клуб','Закрытое комьюнити по подписке: посты, чат и бонусы участникам.'],
    ['course','circle-play','Видео-курс','Уроки с прогрессом. Продажа за фикс-цену, доступ навсегда.'],
  ];
  const lockAccess = d.kind==='course';   // курс всегда закрытый
  const lockPaid = d.kind==='course';     // курс всегда платный
  const paid = (d.price||0)>0;
  const kl = chKindLabel(d).toLowerCase();
  const pro = chIsPro();
  const bgs = chAllowedBgs();
  const proTag = pro ? '' : ' <span class="ch-pro-tag" title="PRO">PRO</span>';
  const html = `
    <div class="ch-sec-h">${chI('bolt')} Что создаём</div>
    <div class="ch-type-grid">${kinds.map(([t,ic,tt,ds])=>`
      <button class="ch-type-card ${d.kind===t?'on':''}" onclick="chPickKind('${t}')">
        <span class="ch-tc-ic">${chI(ic)}</span>
        <span style="flex:1;min-width:0"><b>${tt}</b><small>${ds}</small></span>
        <span class="ch-tc-check">${chI('check')}</span>
      </button>`).join('')}</div>

    <div class="ch-sec-h">${chI('lock')} Доступ</div>
    <div class="ch-choice2${lockAccess?' locked':''}">
      <button class="ch-ch2 ${d.access==='open'?'on':''}" ${lockAccess?'disabled':''} onclick="chDraftAccess('open')">${chI('globe')}<b>Открытый</b><small>Виден и читается всем</small></button>
      <button class="ch-ch2 ${d.access==='closed'?'on':''}" ${lockAccess?'disabled':''} onclick="chDraftAccess('closed')">${chI('lock')}<b>Закрытый</b><small>Доступ по подписке/оплате</small></button>
    </div>
    ${lockAccess?`<div class="ch-owner-note">Курс всегда закрытый — уроки открываются после покупки.</div>`:''}

    <div class="ch-sec-h">${chI('money')} Оплата</div>
    <div class="ch-choice2${lockPaid?' locked':''}">
      <button class="ch-ch2 ${!paid?'on':''}" ${lockPaid?'disabled':''} onclick="chDraftPaid(false)">${chI('check')}<b>Бесплатно</b><small>Без платы за доступ</small></button>
      <button class="ch-ch2 ${paid?'on':''}" ${lockPaid?'disabled':''} onclick="chDraftPaid(true)">${chI('card')}<b>Платно</b><small>${d.kind==='course'?'Разовая цена':'Подписка ₽/мес'}</small></button>
    </div>

    <label class="ch-lab">Название · ${kl}</label>
    <input class="ch-input" id="chDName" maxlength="42" placeholder="Например: Клуб роста PRO" value="${chEsc(d.name)}" oninput="chDraft.name=this.value;chSyncCreateBtn()">

    <label class="ch-lab">Описание${(d.access==='closed'||paid)?' — видно на витрине':''}</label>
    <textarea class="ch-ta" id="chDDesc" rows="3" maxlength="240" placeholder="О чём канал, что получит подписчик…" oninput="chDraft.desc=this.value">${chEsc(d.desc)}</textarea>

    <div id="chPriceBlock" style="display:${paid?'block':'none'}">
      <label class="ch-lab">${d.kind==='course'?'Цена курса (разовая)':'Цена подписки в месяц'}</label>
      <div class="ch-price-row">
        <input class="ch-input" id="chDPrice" inputmode="numeric" value="${d.price}" oninput="chDraft.price=+this.value.replace(/\\D/g,'')||0;chSyncPriceChips()">
        <span class="ch-unit">₽${chPriceUnit(d)}</span>
      </div>
      <div class="ch-price-chips" id="chPriceChips">${chPriceChips()}</div>
      <div class="ch-owner-note">Ты получишь <b id="chNetHint">${chNet(d.price)} ₽</b> с каждой продажи, OKO удержит комиссию 10%.</div>
    </div>

    <label class="ch-lab">Аватар</label>
    <div class="ch-avatar-row">
      <div class="ch-avatar-prev" style="${d.avatar?('background-image:url('+d.avatar+');background-size:cover;background-position:center'):chAvGradStyle(d.bg)}">
        ${!d.avatar ? (d.useIcon?chI(d.icon):chEsc((d.name[0]||'A').toUpperCase())) : ''}
      </div>
      <div class="ch-avatar-actions">
        <button class="ch-photo-btn" onclick="chDraftPickAvatar()">${chI('camera')} ${d.avatar?'Сменить фото':'Загрузить фото'}</button>
        ${d.avatar?`<button class="ch-photo-btn del" onclick="chDraftClearAvatar()" title="Убрать">${chI('trash')}</button>`:''}
      </div>
    </div>

    <label class="ch-lab">Значок <span style="font-weight:400;color:var(--dim)">— если без фото</span></label>
    <div class="ch-letter-row" id="chIconRow">
      <button class="ch-lt ${!d.useIcon?'on':''}" onclick="chDraft.useIcon=false;chRender()" title="Первая буква названия"><span style="font:800 18px/1 var(--font-display);color:var(--lime)">A</span></button>
      ${CH_ICONS.map(ic=>`<button class="ch-lt ${d.useIcon&&d.icon===ic?'on':''}" onclick="chDraft.useIcon=true;chDraft.icon='${ic}';chRender()">${chI(ic)}</button>`).join('')}
    </div>

    <label class="ch-lab">Фон обложки</label>
    <div class="ch-appear-prev" id="chBgPrev" style="${d.cover?('background:linear-gradient(180deg,rgba(0,0,0,.15),rgba(0,0,0,.5)),url('+d.cover+');background-size:cover;background-position:center'):('background:'+CH_BGS[d.bg])}">
      <div class="ch-ap-ava" style="${d.avatar?('background-image:url('+d.avatar+');background-size:cover;background-position:center'):chAvGradStyle(d.bg)}">${d.avatar?'':(d.useIcon?chI(d.icon):chEsc((d.name[0]||'K').toUpperCase()))}</div>
      <div class="ch-ap-name">${chEsc(d.name||'Новый '+kl)}</div>
    </div>
    <div class="ch-cover-actions">
      <button class="ch-photo-btn" onclick="chDraftPickCover()">${chI('photo')} ${d.cover?'Сменить обложку':'Загрузить обложку'}</button>
      ${d.cover?`<button class="ch-photo-btn del" onclick="chDraftClearCover()" title="Убрать">${chI('trash')}</button>`:''}
    </div>
    <div class="ch-swatches">${bgs.map((g,i)=>`<div class="ch-sw ${d.bg===i?'on':''}" style="background:${g}" onclick="chDraft.bg=${i};chRender()"></div>`).join('')}</div>
    ${!pro?`<div class="ch-owner-note">Показаны 5 стандартных фонов. Дополнительные фоны — на тарифе <b>START+</b>.</div>`:''}

    <label class="ch-lab">Фон чата ${!pro?proTag:''}<span style="font-weight:400;color:var(--dim)"> — за лентой сообщений</span></label>
    <div class="ch-swatches">
      <div class="ch-sw ch-sw-none ${d.chatBg==null && !d.chatBgUrl?'on':''}" onclick="chDraft.chatBg=null;chDraft.chatBgUrl=null;chRender()" title="Без фона">${chI('check2')}</div>
      ${bgs.map((g,i)=>`<div class="ch-sw ${d.chatBg===i && !d.chatBgUrl?'on':''}" style="background:${g}" onclick="chDraft.chatBg=${i};chDraft.chatBgUrl=null;chRender()"></div>`).join('')}
      ${d.chatBgUrl?`<div class="ch-sw on" style="background-image:url(${d.chatBgUrl});background-size:cover" title="Свой фон">${chI('check2')}</div>`:''}
    </div>
    ${pro?`<button class="ch-photo-btn ch-mt-6" onclick="chDraftPickChatBg()">${chI('photo')} ${d.chatBgUrl?'Сменить свой фон':'Свой фон из файла'}</button>`:`<div class="ch-owner-note">Загрузка собственного фона чата доступна на тарифе <b>START+</b>.</div>`}

    <label class="ch-lab">Ссылка на сайт ${proTag}<span style="font-weight:400;color:var(--dim)"> — кнопка в шапке канала</span></label>
    <input class="ch-input" id="chDSite" ${pro?'':'disabled'} placeholder="https://okoteam.top" value="${chEsc(d.website||'')}" oninput="chDraft.website=this.value.trim()">

    <label class="ch-check-row"><input type="checkbox" ${d.verified?'checked':''} ${pro?'':'disabled'} onchange="chDraft.verified=this.checked;chRender()">
      <span>Заявка на верификацию ${proTag}<small>Синяя галочка — модерация OKO проверит канал вручную</small></span>
    </label>

    ${d.kind==='sgroup' || d.kind==='chat' ? `<div class="ch-owner-note">${chI('users')} При росте свыше <b>1000 участников</b> потребуется тариф <b>START+</b>. Платные каналы этим ограничением не связаны.</div>`:''}

    <div style="height:18px"></div>
    <button class="btn" id="chCreateBtn" onclick="chCreateChannel()" ${d.name.trim()?'':'disabled style="opacity:.5"'}>${chI('plus')} Создать ${kl}</button>
    <div style="height:10px"></div>`;
  return {title:'Новый '+kl, html};
}

/* --- пикеры для черновика --- */
window.chDraftPickAvatar = function(){
  chPickFile(f=>chReadImage(f, 512, 0.82, url=>{ chDraft.avatar=url; chDraft.useIcon=false; chRender(); }));
};
window.chDraftClearAvatar = function(){ chDraft.avatar=null; chRender(); };
window.chDraftPickCover = function(){
  chPickFile(f=>chReadImage(f, 1200, 0.72, url=>{ chDraft.cover=url; chRender(); }));
};
window.chDraftClearCover = function(){ chDraft.cover=null; chRender(); };
window.chDraftPickChatBg = function(){
  if(!chIsPro()){ toast('Свой фон — на тарифе START+'); return; }
  chPickFile(f=>chReadImage(f, 1200, 0.72, url=>{ chDraft.chatBgUrl=url; chDraft.chatBg=null; chRender(); }));
};
function chPriceChips(){
  const opts = chDraft.kind==='course' ? [490,990,1490,2900] : [99,199,299,499];
  return opts.map(p=>`<button class="${chDraft.price===p?'on':''}" onclick="chDraft.price=${p};chSyncPriceChips();chSyncPriceInput()">${p} ₽</button>`).join('');
}
function chNet(p){ return Math.round((p||0)*(1-CH_FEE)); }
window.chPickKind = function(k){
  chDraft.kind = k;
  if(k==='channel'){ chDraft.access='open'; }
  else if(k==='chat'){ chDraft.access='open'; chDraft.price=0; }
  else if(k==='sgroup'){ chDraft.access='open'; chDraft.price=0; }
  else if(k==='club'){ chDraft.access='closed'; if(!chDraft.price) chDraft.price=299; }
  else if(k==='course'){ chDraft.access='closed'; if(!chDraft.price || chDraft.price<490) chDraft.price=990; }
  chRender();
};
window.chDraftAccess = function(a){ chDraft.access=a; chRender(); };
window.chDraftPaid = function(p){
  if(p){ if(!chDraft.price) chDraft.price = chDraft.kind==='course'?990:299; if(chDraft.access==='open') chDraft.access='closed'; }
  else { chDraft.price = 0; }
  chRender();
};
window.chSyncPriceChips = function(){
  const el = document.getElementById('chPriceChips'); if(el) el.innerHTML = chPriceChips();
  const h = document.getElementById('chNetHint'); if(h) h.textContent = chNet(chDraft.price)+' ₽';
};
window.chSyncPriceInput = function(){ const el = document.getElementById('chDPrice'); if(el) el.value = chDraft.price; };
window.chSyncCreateBtn = function(){
  const b = document.getElementById('chCreateBtn'); if(!b) return;
  const ok = chDraft.name.trim().length>0;
  b.disabled = !ok; b.style.opacity = ok?'':'0.5';
  const prev = document.querySelector('#chBgPrev .ch-ap-name'); if(prev) prev.textContent = chDraft.name||'Новый канал';
};
window.chCreateChannel = function(){
  const d = chDraft, name = d.name.trim();
  if(!name){ toast('Введите название'); return; }
  CH.seq = (CH.seq||0)+1;
  const isChatLike = (d.kind==='chat' || d.kind==='sgroup');
  const c = chNormalize({
    id:'ch-my-'+CH.seq, name, nick:chSlug(name), desc:d.desc.trim()||(isChatLike?'Добро пожаловать в чат!':'Добро пожаловать!'),
    icon: d.useIcon ? d.icon : null, bg:d.bg,
    chatBg:(d.chatBg==null?null:d.chatBg), chatBgUrl:(d.chatBgUrl||null),
    cover:d.cover||null, avatar:d.avatar||null,
    kind:d.kind, access:d.access, price:(d.price||0),
    website:d.website||'', verifyRequested:!!d.verified, verified:false,
    subs:1, reactions:true, discussions:d.kind!=='course',
    /* для чата/супергруппы юзеры пишут всегда; для канала — только админы */
    whoPost: (d.kind==='chat' || d.kind==='sgroup') ? 'subs' : 'admins',
    /* для канала админ может вкл/выкл комментарии; по умолчанию — вкл */
    commentsOn: (d.kind==='channel') ? true : undefined,
    admins:[], gross:0, members:[{name:PROFILE.name, nick:PROFILE.nick, joined:'сейчас'}], black:[],
    posts:[{
      txt: isChatLike
        ? 'Чат создан. Пригласи первых участников и задай тон общения.'
        : 'Канал создан. Первый пост задаёт тон — расскажи, что будет полезного для подписчиков.',
      likes:0, views:1, when:'сейчас'
    }],
    lessons: d.kind==='course' ? [{id:'l1', title:'Вводный урок', dur:'5:00'}] : undefined,
  });
  CH.mine.unshift(c);
  chSave();
  chMirrorToChats(c);  // канал/чат появляется в мессенджере
  // сброс черновика
  chDraft.kind='channel'; chDraft.access='open'; chDraft.name=''; chDraft.desc=''; chDraft.price=0;
  chDraft.bg=0; chDraft.chatBg=null; chDraft.chatBgUrl=null; chDraft.useIcon=false; chDraft.icon='megaphone';
  chDraft.cover=null; chDraft.avatar=null; chDraft.website=''; chDraft.verified=false;
  const kl = chKindLabel(c).toLowerCase();
  if(typeof showPopup==='function'){
    showPopup({ico:'check', title:chKindLabel(c)+' создан',
      body:`«${chEsc(c.name)}» готов. ${chPaid(c)?'Настрой доступ и приглашай первых подписчиков — они платят, ты зарабатываешь.':c.access==='closed'?'Закрытый доступ включён — принимай заявки на вступление.':'Публикуй посты и расти аудиторию.'}`,
      actions:[{label:'Открыть', onclick:()=>{ chNav=[{page:'list'}]; chGo('channel',c.id); }},{label:'К списку', ghost:true, onclick:()=>{ chNav=[{page:'list'}]; chRender(); }}]});
  } else { chNav=[{page:'list'}]; chGo('channel',c.id); }
};

/* канал/чат/супергруппа → запись в CHATS (мессенджер), чтобы был «настоящим».
   kind в CHATS: направляем канал/клуб/курс как 'channel' (лента постов), чат — 'direct'
   (личный по функциональности, но с managed=true), супергруппу — 'group'.
   Для юзера чат и канал выглядят одинаково — разница в правах: в канале пишет только
   админ (writeAll=false), в чате/супергруппе пишут все (writeAll=true). */
function chMirrorToChats(c){
  if(typeof CHATS==='undefined') return;
  if(CHATS.some(x=>x.chId===c.id)) return;
  const chatsKind = c.kind==='sgroup' ? 'group' : c.kind==='chat' ? 'group' : 'channel';
  const kIcon    = c.kind==='sgroup' ? 'users' : c.kind==='chat' ? 'users' : 'megaphone';
  const writeAll = (c.kind==='chat' || c.kind==='sgroup');
  CHATS.unshift({
    id:'chan_'+c.id, chId:c.id, name:c.name, kind:chatsKind, managed:true, writeAll:writeAll,
    ava:(c.name[0]||'K').toUpperCase(), avaIcon: c.icon||null, avaImg:c.avatar||null, kindIcon:kIcon,
    subs:chFmtN(c.subs||0), nick:c.nick,
    preview: c.kind==='course'?'Видео-курс':c.kind==='club'?'Закрытый клуб':c.kind==='sgroup'?'Супергруппа':c.kind==='chat'?'Чат создан':chPaid(c)?'Платный канал':c.access==='closed'?'Закрытый канал':'Канал создан',
    time:(typeof nowT==='function'?nowT():'сейчас'), unread:0, online:false,
    msgs:[{kind:'sys', body:`${chKindLabel(c)} «${c.name}» — открой в «Мои каналы» для управления`}],
    openChannel:c.id
  });
  if(typeof renderChatList==='function') try{ renderChatList(); }catch(e){}
}

/* ================= СТРАНИЦА: ПРОСМОТР КАНАЛА ================= */
function chPageChannel(id){
  const c = chChannel(id);
  if(!c) return {title:'Канал', html:'<div class="ch-empty">Канал не найден</div>'};
  const mine = chIsMine(c);
  const subbed = chIsSubbed(c);
  const bgStyle = c.cover
    ? `background:linear-gradient(180deg,rgba(0,0,0,.15),rgba(0,0,0,.5)),url(${c.cover});background-size:cover;background-position:center`
    : `background:${CH_BGS[c.bg||0]}`;
  const cover = `
    <div class="ch-cover${c.cover?' has-photo':''}" style="${bgStyle}">
      ${mine?`<button class="ch-cv-edit" onclick="chGo('mAppear','${c.id}')" title="Оформление">${chI('camera')}</button>`:''}
      ${chAvInner(c,'big')}
      <div class="ch-cv-name">${chEsc(c.name)}${chBadge(c)}</div>
      <div class="ch-cv-id">@${chEsc(chNick(c))}</div>
      <div class="ch-cv-badges">${chBadges(c)}</div>
      <div class="ch-cv-sub">${chAccessLine(c)}${mine?' · вы владелец':''}</div>
      <div class="ch-cv-stats">
        <div><b>${chFmtN(c.subs||0)}</b><small>подписчиков</small></div>
        ${c.kind==='course'?`<div><b>${c.lessons?c.lessons.length:0}</b><small>уроков</small></div>`:`<div><b>${c.posts?c.posts.length:0}</b><small>постов</small></div>`}
        <div><b>${c.access==='closed'?'закрытый':'открытый'}</b><small>доступ</small></div>
      </div>
    </div>`;

  // не участник закрытого/платного/курса → ВИТРИНА (paywall / gate)
  if(!subbed){
    const paid = chPaid(c);
    const benefits = c.kind==='course'
      ? ['Полный доступ ко всем '+(c.lessons?c.lessons.length:0)+' видео-урокам','Практика и разбор твоих работ','Доступ навсегда, смотри в своём темпе','Чат поддержки с автором']
      : c.kind==='club'
      ? ['Закрытые посты, разборы и материалы клуба','Живое комьюнити и чат участников','Еженедельные задания и обратная связь','Отмена подписки в любой момент']
      : ['Все закрытые посты и разборы','Новые материалы регулярно','Доступ в закрытое обсуждение','Отмена доступа в любой момент'];
    const gTitle = c.kind==='course'?'Открой доступ к курсу': c.kind==='club'?'Вступи в клуб': 'Закрытый канал';
    const priceHtml = paid
      ? `<div class="ch-pw-price">${c.price} ₽<span>${c.kind==='course'?' разово':' / мес'}</span></div>`
      : `<div class="ch-pw-price">Бесплатно<span> · по заявке</span></div>`;
    const ctaLabel = c.kind==='course' ? `Купить курс за ${c.price} ₽`
      : paid ? `Оформить доступ · ${c.price} ₽/мес`
      : `Оформить доступ бесплатно`;
    const note = paid
      ? 'Оплата с кошелька OKO · безопасно · комиссия сервиса 10%'
      : 'Бесплатное вступление — доступ откроется сразу';
    return {title:c.name, html: cover + `
      <div class="ch-desc">${chEsc(c.desc)}</div>
      ${chStudentsLine(c)}
      <div class="ch-paywall">
        <div class="ch-pw-blur">
          ${(c.posts&&c.posts.length?c.posts:[{txt:'Закрытый материал для участников…'},{txt:'Здесь публикуются приватные разборы и бонусы.'}]).slice(0,2).map(p=>
            `<div class="ch-post"><div class="ch-post-txt">${chEsc(p.txt)}</div></div>`).join('')}
        </div>
        <div class="ch-pw-body">
          <div class="ch-pw-lock">${chI('lock')}</div>
          <h3>${gTitle}</h3>
          <p>${chEsc(c.owner?('Автор: '+c.owner):'Оформи доступ, чтобы видеть все материалы')}</p>
          ${priceHtml}
          <ul class="ch-pw-benefits">${benefits.map(b=>`<li>${chI('check')}<span>${b}</span></li>`).join('')}</ul>
          <button class="btn" onclick="chSubscribe('${c.id}')">${chI(c.kind==='course'?'circle-play':paid?'card':'check')} ${ctaLabel}</button>
          <p style="font-size:11px;color:var(--dim);margin-top:9px">${note}</p>
        </div>
      </div>
      ${chReviewsHtml(c)}`};
  }

  // участник / владелец / открытый бесплатный
  let html = cover;
  html += `<div class="ch-desc">${chEsc(c.desc)}</div>`;
  html += chStudentsLine(c);

  if(mine){
    html += `<div class="ch-owner-bar">
      <button class="btn" onclick="chGo('${c.kind==='course'?'addLesson':'compose'}','${c.id}')">${chI('plus')} ${c.kind==='course'?'Добавить урок':'Опубликовать пост'}</button>
      <button class="btn ghost" onclick="chGo('manage','${c.id}')">${chI('bolt')} Управление</button>
    </div>`;
  } else {
    const authorName = c.owner || (c.admins&&c.admins[0]&&c.admins[0].name) || c.name;
    const authorNick = c.ownerNick || chNick(c);
    html += `<div class="ch-owner-bar">
      <button class="btn ghost" onclick="chDM('${chEsc(authorName)}','${chEsc(authorNick)}')">${chI('send')} Написать автору</button>
      ${chGated(c)?`<button class="btn ghost" onclick="chUnsub('${c.id}')">${chI('check')} ${chPaid(c)?'Подписка':'Доступ'}</button>`:''}
    </div>`;
  }

  /* ряд быстрых действий (как шапка канала в Telegram): уведомления · поделиться · ещё */
  html += `<div class="ch-actrow">
    <button class="ch-act ${chNotifyOn(c)?'on':''}" onclick="chToggleNotify('${c.id}',this)">${chI('bell')}<span>${chNotifyOn(c)?'Уведомления':'Без звука'}</span></button>
    <button class="ch-act" onclick="chShareChannel('${c.id}')">${chI('share')}<span>Поделиться</span></button>
    <button class="ch-act" onclick="chMoreMenu('${c.id}')">${chI('more')}<span>Ещё</span></button>
  </div>`;

  if(c.kind==='course'){
    const prog = chCourseProg(c);
    const done = (CH.prog[c.id]||[]).length;
    html += `<div class="ch-prog-top">
      <div class="ch-pt-row"><b>Прогресс курса</b><span class="ch-pt-pct">${prog}%</span></div>
      <div class="ch-prog-bar"><i style="width:${prog}%"></i></div>
      <div style="color:var(--dim);font-size:11.5px;margin-top:8px">${done} из ${c.lessons.length} уроков пройдено</div>
    </div>`;
    html += `<div class="ch-sec-h">${chI('circle-play')} Уроки</div>`;
    html += c.lessons.map((l,i)=>{
      const isDone = (CH.prog[c.id]||[]).includes(l.id);
      return `<div class="ch-lesson ${isDone?'done':''}" onclick="chGo('lesson',{c:'${c.id}',l:'${l.id}'})">
        <span class="ch-ls-n">${isDone?chI('check'):(i+1)}</span>
        <span class="ch-ls-b"><b>${chEsc(l.title)}</b><small>${chI('clock')} ${l.dur}${isDone?' · пройден':''}</small></span>
        <span class="ch-ls-play">${chI('circle-play')}</span>
      </div>`;
    }).join('');
  } else {
    chEnsurePostIds(c);
    if(!mine) chCountViews(c);   // просмотры тикают, когда пост читает подписчик (раз за сессию)
    // закреплённый пост — отдельным блоком сверху (как в Telegram), без дублирования в общей ленте
    const pinId = chPinnedId(c);
    const pinPost = pinId ? (c.posts||[]).find(p=>chPostId(p)===pinId) : null;
    if(pinPost) html += chPostHtml(c, pinPost, -1, true);
    html += `<div class="ch-sec-h">${chI('feed')} ${c.kind==='club'?'Лента клуба':'Посты'}</div>`;
    const rest = (c.posts||[]).filter(p=>chPostId(p)!==pinId);
    const posts = rest.length ? rest.map((p,i)=>chPostHtml(c,p,i)).join('')
      : (pinPost ? '' : `<div class="ch-empty">Постов пока нет</div>`);
    html += (c.chatBg!=null && posts)
      ? `<div class="ch-feed" style="background:${CH_BGS[c.chatBg]}">${posts}</div>`
      : posts;
  }
  html += chReviewsHtml(c);
  html += `<div style="height:8px"></div>`;
  const tools = mine ? `<button onclick="chGo('manage','${c.id}')" title="Управление">${chI('bolt')}</button>` : '';
  return {title:c.name, html, tools};
}

/* «X учеников проходят сейчас» — только для курсов маркетплейса */
function chStudentsLine(c){
  if(c.kind!=='course' || !c.students_now) return '';
  return `<div class="ch-students-now">${chI('users')}<span><b>${chFmtN(c.students_now)}</b> учеников проходят курс прямо сейчас</span></div>`;
}
/* Блок отзывов: реалистичный рейтинг с распределением звёзд + список 5-10 отзывов */
function chReviewsHtml(c){
  const list = Array.isArray(c.reviewsList) ? c.reviewsList : [];
  const total = chReviewsN(c);
  if(!list.length && total<=0) return '';
  const rat = chRatingN(c);
  // мок-распределение по звёздам от общего рейтинга (детерминированное)
  const p5 = Math.min(96, Math.max(60, Math.round(rat*18)));
  const p4 = Math.min(30, Math.max(3, 100 - p5 - 4));
  const p3 = Math.max(1, Math.round((100-p5-p4)*0.55));
  const p2 = Math.max(0, Math.round((100-p5-p4)*0.30));
  const p1 = Math.max(0, 100-p5-p4-p3-p2);
  const dist = [[5,p5],[4,p4],[3,p3],[2,p2],[1,p1]];
  const bars = dist.map(([k,v])=>`
    <div class="ch-rv-bar-row"><span class="ch-rv-bar-k">${k}</span>
      <span class="ch-rv-bar"><i style="width:${v}%"></i></span>
      <span class="ch-rv-bar-v">${v}%</span></div>`).join('');
  const items = (list.length?list:[]).map(r=>{
    const stars = [1,2,3,4,5].map(k=>`<span class="${k<=r.stars?'on':''}">${chI('star')}</span>`).join('');
    return `<div class="ch-rv-item">
      <div class="ch-rv-h">
        <div class="ch-rv-av" style="${chAvSeedStyle(r.name)}">${chEsc((r.name||'?').charAt(0).toUpperCase())}</div>
        <div class="ch-rv-hh"><b>${chEsc(r.name||'')}</b><small>${chEsc(r.when||'')}</small></div>
        <div class="ch-rv-stars">${stars}</div>
      </div>
      <div class="ch-rv-txt">${chEsc(r.txt||'')}</div>
    </div>`;
  }).join('');
  const kindWord = c.kind==='course' ? 'курса' : c.kind==='club' ? 'клуба' : 'канала';
  return `<div class="ch-reviews">
    <div class="ch-rv-head">
      <div class="ch-rv-num"><b>${rat.toFixed(1)}</b><small>${chI('star')} из 5</small></div>
      <div class="ch-rv-bars">${bars}</div>
      <div class="ch-rv-count">${chFmtN(total)} отзывов ${kindWord}</div>
    </div>
    <div class="ch-rv-list">${items}</div>
  </div>`;
}
/* ---- просмотры: считаем один раз за сессию на каждый пост, читаемый подписчиком ---- */
const chViewed = {};   // pid -> true (в пределах сессии)
function chCountViews(c){
  let ch=false;
  (c.posts||[]).forEach(p=>{ const pid=chPostId(p); if(pid && !chViewed[pid]){ chViewed[pid]=true; p.views=(p.views||0)+1; ch=true; } });
  if(ch) chSave();
}
function chPostHtml(c,p,i,pinned){
  const pid = chPostId(p);
  const mine = chIsMine(c);
  const liked = !!CH.likes[pid];
  const likes = (p.likes||0)+(liked?1:0);
  const cmts = CH.cmt[pid]||[];
  const open = !!chOpenCmt[pid];
  return `<div class="ch-post${pinned?' pinned':''}">
    ${pinned?`<div class="ch-pin-lab">${chI('pin')}<span>Закреплённое</span>${mine?`<button class="ch-pin-off" onclick="chUnpin('${c.id}')">Открепить</button>`:''}</div>`:''}
    <div class="ch-post-h">${chAvInner(c,'mini')}<b class="ch-ph-name">${chEsc(c.name)}</b><small>${chEsc(p.when||'')}</small></div>
    <div class="ch-post-txt">${chEsc(p.txt)}</div>
    ${p.img?`<div class="ch-post-media photo" style="background-image:url(${p.img})"></div>`
      :p.media==='poll'?chPollHtml(c,p,i)
      :p.media?`<div class="ch-post-media" style="background:${CH_BGS[c.bg||0]}">${chI(p.media)}${p.media==='circle-play'?'<span class="ch-pm-dur">видео</span>':''}</div>`:''}
    <div class="ch-post-acts">
      ${c.reactions?`<button class="${liked?'on':''}" onclick="chLike('${c.id}','${pid}')" aria-label="Нравится">${chI('heart')}${likes}</button>`:''}
      ${c.discussions?`<button class="${open?'on':''}" onclick="chToggleCmt('${c.id}','${pid}')" aria-label="Комментарии">${chI('comment')}${cmts.length||'Обсудить'}</button>`:''}
      ${mine&&!pinned?`<button class="ch-pin-btn" onclick="chPin('${c.id}','${pid}')" aria-label="Закрепить">${chI('pin')}<span>Закрепить</span></button>`:''}
      <span class="ch-views">${chI('eye')}${chFmtN(p.views||0)}</span>
    </div>
    ${c.discussions&&open?chCommentsHtml(c,pid,cmts):''}
  </div>`;
}
/* закрепить / открепить пост (доступно владельцу) */
window.chPin = function(id,pid){
  const c = chChannel(id); if(!c || !chIsMine(c)) return;
  c.pinned = pid; chSave(); chRender();
  toast('Пост закреплён вверху канала');
};
window.chUnpin = function(id){
  const c = chChannel(id); if(!c) return;
  c.pinned = null; chSave(); chRender();
  toast('Пост откреплён');
};
/* блок комментариев под постом (живой: пишем, лайкаем, сохраняется) */
function chCommentsHtml(c,pid,cmts){
  const rows = cmts.length ? cmts.map((m,k)=>{
    const clk = !!(m.likedBy);
    return `<div class="ch-cmt">
      <div class="ch-cmt-av" style="${chAvSeedStyle(m.nick||m.name)}">${chEsc((m.name[0]||'U').toUpperCase())}</div>
      <div class="ch-cmt-b">
        <div class="ch-cmt-top"><b>${chEsc(m.name)}</b><small>${chEsc(m.when||'')}</small></div>
        <div class="ch-cmt-txt">${chEsc(m.txt)}</div>
      </div>
      <button class="ch-cmt-like ${clk?'on':''}" onclick="chCmtLike('${c.id}','${pid}',${k})" aria-label="Нравится">${chI('heart')}<span>${(m.likes||0)+(clk?1:0)||''}</span></button>
    </div>`;
  }).join('') : `<div class="ch-cmt-empty">Комментариев пока нет — будь первым.</div>`;
  return `<div class="ch-comments">
    <div class="ch-cmt-list">${rows}</div>
    <div class="ch-cmt-form">
      <input class="ch-cmt-input" id="chCmt_${pid}" maxlength="300" placeholder="Комментарий…" onkeydown="if(event.key==='Enter')chSendCmt('${c.id}','${pid}')">
      <button class="ch-cmt-send" onclick="chSendCmt('${c.id}','${pid}')" aria-label="Отправить">${chI('send')}</button>
    </div>
  </div>`;
}
/* состояние раскрытых комментариев (в пределах сессии, чтобы не мигало при ре-рендере) */
const chOpenCmt = {};
window.chToggleCmt = function(cid,pid){ chOpenCmt[pid] = !chOpenCmt[pid]; chRender(); };
window.chSendCmt = function(cid,pid){
  const inp = document.getElementById('chCmt_'+pid); if(!inp) return;
  const txt = (inp.value||'').trim(); if(!txt){ return; }
  CH.cmt[pid] = CH.cmt[pid]||[];
  CH.cmt[pid].push({ name:PROFILE.name, nick:PROFILE.nick, txt, when:'сейчас', likes:0 });
  chOpenCmt[pid] = true; chSave(); chRender();
};
window.chCmtLike = function(cid,pid,k){
  const arr = CH.cmt[pid]; if(!arr||!arr[k]) return;
  arr[k].likedBy = !arr[k].likedBy; chSave(); chRender();
};
/* живой опрос под постом (голос сохраняется) */
function chPollHtml(c,p,i){
  const pid = chPostId(p);
  const opts = (p.poll&&p.poll.opts) || ['Уже применяю','Возьму в работу'];
  const key = pid;
  const voted = CH.votes[key];
  const base = (p.poll&&p.poll.base) || [Math.max(3,(p.views||40)%60+12), Math.max(2,(p.views||30)%40+7)];
  const counts = opts.map((_,k)=> base[k%base.length] + (voted===k?1:0));
  const total = counts.reduce((a,b)=>a+b,0)||1;
  const q = (p.poll&&p.poll.q) || 'А ты как?';
  return `<div class="ch-poll">
    <div class="ch-poll-q">${chI('poll')} ${chEsc(q)}</div>
    ${opts.map((o,k)=>{
      const pct = Math.round(counts[k]/total*100);
      return `<button class="ch-poll-opt${voted!=null?' voted':''}${voted===k?' mine':''}" ${voted!=null?'disabled':''} onclick="chVote('${c.id}','${pid}',${k})">
        <span class="ch-po-fill" style="width:${voted!=null?pct:0}%"></span>
        <span class="ch-po-t">${chEsc(o)}</span>
        ${voted!=null?`<span class="ch-po-p">${pct}%</span>`:''}
      </button>`;
    }).join('')}
    <div class="ch-poll-total">${voted!=null?chFmtN(total)+' голосов':'Нажми, чтобы проголосовать'}</div>
  </div>`;
}
window.chVote = function(id,pid,k){
  const c = chChannel(id); if(!c) return;
  CH.votes[pid] = k; chSave(); chRender();
};
window.chLike = function(id,pid){
  const c = chChannel(id); if(!c||!c.reactions) return;
  CH.likes[pid] = !CH.likes[pid];
  chSave(); chRender();
};

/* ---------- подписка / покупка (деньги) ---------- */
window.chSubscribe = function(id){
  const c = chChannel(id); if(!c) return;
  // бесплатный закрытый (или открытый) → вступление без оплаты
  if(!chPaid(c)){
    CH.sub[id]=1; c.subs=(c.subs||0)+1; chSave();
    if(typeof showPopup==='function'){
      showPopup({ico:'check', title:'Доступ открыт',
        body:`Ты вступил в «${chEsc(c.name)}» бесплатно. Все материалы теперь доступны.`,
        actions:[{label:'Открыть', onclick:()=>chRender()}]});
    } else toast('Вы вступили в канал');
    chRender(); return;
  }
  const price = c.price||0;
  if(typeof walletCharge!=='function'){ toast('Кошелёк недоступен'); return; }
  const why = (c.kind==='course'?'Покупка курса: ':c.kind==='club'?'Подписка на клуб: ':'Подписка на канал: ')+c.name;
  if(!walletCharge(price, why)) return;              // сам покажет «недостаточно средств»
  if(typeof okoEarn==='function') okoEarn(price*CH_FEE, 'Комиссия каналов 10%');
  CH.sub[id]=1; c.subs=(c.subs||0)+1; c.gross=(c.gross||0)+price; chSave();
  const net = Math.round(price*(1-CH_FEE));
  if(typeof showPopup==='function'){
    showPopup({ico:'check', title: c.kind==='course'?'Курс открыт':'Доступ оформлен',
      body:`Списано ${price} ₽ с кошелька. Автору начислено ${net} ₽, комиссия OKO 10% (${Math.round(price*CH_FEE)} ₽). ${c.kind==='course'?'Все уроки доступны навсегда.':'Доступ активен на месяц.'}`,
      actions:[{label:c.kind==='course'?'К урокам':'Открыть', onclick:()=>chRender()}]});
  } else { toast('Доступ открыт'); }
  chRender();
};
window.chUnsub = function(id){
  const c = chChannel(id); if(!c) return;
  if(typeof showPopup!=='function'){ delete CH.sub[id]; if(c.subs)c.subs--; chSave(); chRender(); return; }
  showPopup({ico:'flag', title:'Отменить доступ?',
    body:`Ты потеряешь доступ к «${chEsc(c.name)}». ${chPaid(c)?'Подписку можно оформить снова в любой момент.':'Вступить снова можно в любой момент.'}`,
    actions:[{label:'Отменить доступ', onclick:()=>{ delete CH.sub[id]; if(c.subs)c.subs--; chSave(); toast('Доступ отменён'); chRender(); }},{label:'Оставить', ghost:true}]});
};

/* ================= СТРАНИЦА: УРОК ================= */
function chPageLesson(arg){
  const c = chChannel(arg&&arg.c);
  if(!c || !c.lessons) return {title:'Урок', html:'<div class="ch-empty">Урок не найден</div>'};
  const idx = c.lessons.findIndex(l=>l.id===arg.l);
  const l = c.lessons[idx];
  if(!l) return {title:'Урок', html:'<div class="ch-empty">Урок не найден</div>'};
  const isDone = (CH.prog[c.id]||[]).includes(l.id);
  const next = c.lessons[idx+1];
  const html = `
    <div class="ch-player" style="background:${CH_BGS[c.bg||0]}">
      <div class="ch-play-btn" onclick="toast('Видео-плеер урока — подключается к Академии OKO')">${chI('play')}</div>
    </div>
    <div style="display:flex;align-items:center;gap:8px;margin-bottom:4px">
      <span class="ch-tag course">Урок ${idx+1}/${c.lessons.length}</span>
      <small style="color:var(--dim);font-size:12px">${chI('clock')} ${l.dur}</small>
    </div>
    <h2 style="font:800 20px/1.2 var(--font-display);letter-spacing:.01em;margin:8px 0 10px">${chEsc(l.title)}</h2>
    <div class="ch-desc">В этом уроке разбираем «${chEsc(l.title)}» по шагам: теория, живой пример и практическое задание. После просмотра отметь урок пройденным — прогресс курса обновится.</div>
    <button class="btn ${isDone?'ghost':''}" onclick="chToggleLesson('${c.id}','${l.id}')">${chI(isDone?'check':'check2')} ${isDone?'Урок пройден':'Отметить пройденным'}</button>
    ${next?`<div style="height:10px"></div><button class="btn ghost" onclick="chGo('lesson',{c:'${c.id}',l:'${next.id}'})">${chI('forward')} Следующий урок · ${chEsc(next.title)}</button>`:''}
    <div style="height:10px"></div>`;
  return {title:'Урок '+(idx+1), html};
}
window.chToggleLesson = function(cid, lid){
  CH.prog[cid] = CH.prog[cid]||[];
  const i = CH.prog[cid].indexOf(lid);
  if(i<0){ CH.prog[cid].push(lid); toast('Урок пройден — прогресс обновлён'); }
  else { CH.prog[cid].splice(i,1); }
  chSave(); chRender();
};

/* ================= СТРАНИЦА: ПУБЛИКАЦИЯ ПОСТА (владелец) ================= */
const chCompose = {txt:'', media:null, img:null, poll:''};
function chPageCompose(id){
  const c = chChannel(id);
  if(!c || !chIsMine(c)) return {title:'Публикация', html:'<div class="ch-empty">Нет доступа</div>'};
  const kinds = [
    ['none','feed','Текст'],
    ['photo','photo','Фото'],
    ['circle-play','circle-play','Видео'],
    ['poll','poll','Опрос'],
  ];
  const html = `
    <div class="ch-compose-head">${chAvInner(c,'mini')}<div><b>${chEsc(c.name)}</b><small>@${chEsc(chNick(c))} · публикация от вас</small></div></div>
    <textarea class="ch-ta" id="chCompTxt" rows="4" maxlength="600" placeholder="Что нового? Поделись пользой с подписчиками…" oninput="chCompose.txt=this.value;chCompSync()">${chEsc(chCompose.txt)}</textarea>
    <div class="ch-compose-kinds">${kinds.map(([m,ic,lab])=>`
      <button class="ch-ck ${((chCompose.media||'none')===m)?'on':''}" onclick="chCompKind('${m}')">${chI(ic)}<span>${lab}</span></button>`).join('')}</div>
    ${chCompose.media==='photo'?`
      <div class="ch-compose-photo" onclick="chCompPickPhoto('${id}')" style="${chCompose.img?`background-image:url(${chCompose.img})`:''}">
        ${chCompose.img?`<span class="ch-cp-change">${chI('camera')} Заменить</span>`:`${chI('camera')}<small>Загрузить фото поста</small>`}
      </div>`:''}
    ${chCompose.media==='poll'?`
      <label class="ch-lab">Варианты ответа (через запятую)</label>
      <input class="ch-input" id="chCompPoll" placeholder="Уже применяю, Возьму в работу" value="${chEsc(chCompose.poll)}" oninput="chCompose.poll=this.value">`:''}
    <div style="height:16px"></div>
    <button class="btn" id="chCompBtn" onclick="chPublishPost('${id}')" ${chCompose.txt.trim()||chCompose.img?'':'disabled style="opacity:.5"'}>${chI('send')} Опубликовать</button>
    <div class="ch-owner-note">Пост появится в канале и в ленте рекомендаций OKO — алгоритм покажет его новой аудитории.</div>`;
  return {title:'Новый пост', html};
}
window.chCompKind = function(m){ chCompose.media = (m==='none'?null:m); if(m!=='photo') chCompose.img=null; chRender(); };
window.chCompSync = function(){
  const b=document.getElementById('chCompBtn'); if(!b) return;
  const ok = chCompose.txt.trim().length>0 || !!chCompose.img;
  b.disabled=!ok; b.style.opacity=ok?'':'0.5';
};
window.chCompPickPhoto = function(id){
  chPickFile(f=>chReadImage(f, 1000, 0.72, url=>{ chCompose.img=url; chRender(); }));
};
window.chPublishPost = function(id){
  const c = chChannel(id); if(!c || !chIsMine(c)) return;
  const txt = chCompose.txt.trim();
  if(!txt && !chCompose.img){ toast('Добавьте текст или фото'); return; }
  const post = { txt, likes:0, views:1, when:'сейчас' };
  if(chCompose.media==='photo' && chCompose.img) post.img = chCompose.img;
  else if(chCompose.media==='circle-play') post.media='circle-play';
  else if(chCompose.media==='poll'){
    post.media='poll';
    const opts = chCompose.poll.split(',').map(s=>s.trim()).filter(Boolean);
    if(opts.length>=2) post.poll = {q:txt||'А ты как?', opts:opts.slice(0,4)};
  }
  c.posts = c.posts||[]; c.posts.unshift(post);
  chPushToFeed(c, post);
  chSave();
  chCompose.txt=''; chCompose.media=null; chCompose.img=null; chCompose.poll='';
  toast('Пост опубликован — он уже в ленте рекомендаций');
  chStep();  // назад к каналу
};

/* ================= СТРАНИЦА: ДОБАВИТЬ УРОК (владелец курса) ================= */
const chLessonDraft = {title:'', dur:'5:00'};
function chPageAddLesson(id){
  const c = chChannel(id);
  if(!c || !chIsMine(c)) return {title:'Урок', html:'<div class="ch-empty">Нет доступа</div>'};
  if(!c.lessons) c.lessons=[];
  const html = `
    <div class="ch-owner-note" style="margin-bottom:10px">Урок №${c.lessons.length+1} курса «${chEsc(c.name)}». Видео подключится из Академии OKO.</div>
    <label class="ch-lab">Название урока</label>
    <input class="ch-input" id="chLsTitle" maxlength="80" placeholder="Например: Хук за 3 секунды" value="${chEsc(chLessonDraft.title)}" oninput="chLessonDraft.title=this.value;chLsSync()">
    <label class="ch-lab">Длительность</label>
    <div class="ch-price-row">
      <input class="ch-input" id="chLsDur" placeholder="8:30" value="${chEsc(chLessonDraft.dur)}" oninput="chLessonDraft.dur=this.value" style="max-width:120px">
      <span class="ch-unit">мин:сек</span>
    </div>
    <div style="height:16px"></div>
    <button class="btn" id="chLsBtn" onclick="chAddLesson('${id}')" ${chLessonDraft.title.trim()?'':'disabled style="opacity:.5"'}>${chI('plus')} Добавить урок</button>
    ${c.lessons.length?`<div class="ch-sec-h">${chI('circle-play')} Уроки курса (${c.lessons.length})</div>
      ${c.lessons.map((l,i)=>`<div class="ch-lesson" style="cursor:default"><span class="ch-ls-n">${i+1}</span><span class="ch-ls-b"><b>${chEsc(l.title)}</b><small>${chI('clock')} ${l.dur}</small></span><button class="ch-m-act" onclick="chDelLesson('${id}','${l.id}')" title="Удалить">${chI('trash')}</button></div>`).join('')}`:''}`;
  return {title:'Новый урок', html};
}
window.chLsSync = function(){ const b=document.getElementById('chLsBtn'); if(!b) return; const ok=chLessonDraft.title.trim().length>0; b.disabled=!ok; b.style.opacity=ok?'':'0.5'; };
window.chAddLesson = function(id){
  const c = chChannel(id); if(!c) return;
  const title = chLessonDraft.title.trim(); if(!title){ toast('Введите название урока'); return; }
  c.lessons = c.lessons||[];
  c.lessons.push({ id:'l'+(Date.now()), title, dur:(chLessonDraft.dur||'5:00').trim() });
  chSave(); chLessonDraft.title=''; chLessonDraft.dur='5:00';
  toast('Урок добавлен'); chRender();
};
window.chDelLesson = function(id,lid){
  const c = chChannel(id); if(!c||!c.lessons) return;
  c.lessons = c.lessons.filter(l=>l.id!==lid);
  if(CH.prog[id]) CH.prog[id] = CH.prog[id].filter(x=>x!==lid);
  chSave(); chRender();
};

/* ================= ЛИЧНЫЕ СООБЩЕНИЯ (написать человеку, как в Telegram) ================= */
window.chDM = function(name,nick){
  if(typeof CHATS==='undefined' || typeof openConv!=='function'){ toast('Мессенджер недоступен'); return; }
  let chat = CHATS.find(x=>x.kind==='direct' && x.dmNick===nick);
  if(!chat){
    chat = {
      id:'dm_'+nick+'_'+Date.now(), dmNick:nick, ava:(name[0]||'U').toUpperCase(), name,
      kind:'direct', kindIcon:null, managed:false, writeAll:true,
      preview:'Личный чат', time:(typeof nowT==='function'?nowT():'сейчас'), unread:0, online:true,
      msgs:[{kind:'sys', body:'Начните личную переписку с '+name}]
    };
    CHATS.unshift(chat);
    if(typeof renderChatList==='function') try{ renderChatList(); }catch(e){}
  }
  chClose();
  if(typeof showTab==='function') showTab('chats');
  try{ openConv(chat.id); }catch(e){ toast('Открываю чаты'); }
};

/* ================= СТРАНИЦА: УПРАВЛЕНИЕ (хаб) ================= */
function chPageManage(id){
  const c = chChannel(id);
  if(!c || !chIsMine(c)) return {title:'Управление', html:'<div class="ch-empty">Нет доступа</div>'};
  const course = c.kind==='course';

  // шапка: аватар, имя, @id, счётчики
  let html = `<div class="ch-manage-hub">${chAvInner(c,'big')}
    <div style="text-align:center;margin-bottom:14px">
      <div style="font:800 19px/1.1 var(--font-display);display:flex;align-items:center;gap:6px;justify-content:center">${chEsc(c.name)}${chBadge(c)}</div>
      <div style="color:var(--dim);font-size:12px;margin-top:4px">@${chEsc(chNick(c))} · ${chFmtN(c.subs||0)} подписчиков</div>
    </div></div>`;

  // быстрые действия
  html += `<div class="ch-actrow" style="margin-bottom:16px">
    <button class="ch-act" onclick="chGo('${course?'addLesson':'compose'}','${id}')">${chI('plus')}<span>${course?'Урок':'Пост'}</span></button>
    <button class="ch-act" onclick="chShareChannel('${id}')">${chI('share')}<span>Поделиться</span></button>
    <button class="ch-act" onclick="chGo('mStats','${id}')">${chI('poll')}<span>Статистика</span></button>
  </div>`;

  // доход владельца (платный/курс)
  if(chPaid(c)){
    const gross = c.gross||0, net = Math.round(gross*(1-CH_FEE)), fee = gross-net;
    html += `<div class="ch-earn">
      <small class="ch-earn-lab">Доход канала к выводу</small>
      <div class="ch-earn-sum">${net.toLocaleString('ru-RU').replace(/,/g,' ')} <span>₽</span></div>
      <div class="ch-earn-calc">
        <div><span>Продажи ${c.kind==='course'?'курса':'подписок'}</span><b>${gross.toLocaleString('ru-RU').replace(/,/g,' ')} ₽</b></div>
        <div class="fee"><span>Комиссия OKO 10%</span><b>− ${fee.toLocaleString('ru-RU').replace(/,/g,' ')} ₽</b></div>
        <div class="total"><span>К выводу</span><b>${net.toLocaleString('ru-RU').replace(/,/g,' ')} ₽</b></div>
      </div>
      <button class="btn" onclick="chWithdraw('${id}')" ${net>0?'':'disabled style="opacity:.5"'}>${chI('money')} Вывести на кошелёк</button>
    </div>`;
  }

  // ---- ОСНОВНОЕ ----
  html += `<div class="ch-sec-h">${chI('bolt')} Основное</div>`;
  html += chList([
    chNavRow('megaphone','Тип и доступ', chAccessLineShort(c), `chGo('mType','${id}')`),
    chNavRow('photo','Оформление', '', `chGo('mAppear','${id}')`, {sub:'Аватар, обложка, описание'}),
  ]);

  // ---- ССЫЛКА И ПРИГЛАШЕНИЯ ----
  html += `<div class="ch-sec-h">${chI('globe')} Ссылка и приглашения</div>`;
  html += chList([
    chLinkRow('globe','Публичная ссылка', chPublicLink(c), `chCopyLink('${id}')`),
    chNavRow('share','Пригласительные ссылки', (c.invites.length?c.invites.length+'':'создать'), `chGo('mInvites','${id}')`),
  ]);

  // ---- МОНЕТИЗАЦИЯ ----
  html += `<div class="ch-sec-h">${chI('money')} Монетизация</div>`;
  const monRows = [
    chTglRow('card','Платный доступ',
      course?'Курс всегда платный':(chPaid(c)?('Цена '+c.price+' ₽'+chPriceUnit(c)):'Включить и задать цену'),
      chPaid(c), course?`toast('Курс всегда платный')`:`chToggleAccess('${id}','paid')`)
  ];
  if(chPaid(c)){
    monRows.push(chNavRow('bolt','Цена и комиссия', c.price+' ₽'+chPriceUnit(c), `chGo('mType','${id}')`, {sub:'вам '+chNet(c.price)+' ₽ · OKO 10%'}));
  }
  html += chList(monRows);
  html += chLF('OKO удерживает комиссию 10% с каждой продажи. Остальное — ваш доход к выводу на кошелёк.');

  // ---- ПУБЛИКАЦИИ ----
  html += `<div class="ch-sec-h">${chI('feed')} Публикации</div>`;
  const pubRows = [
    chTglRow('rocket','Авто-постинг в рекомендации','Новые посты попадают в ленту OKO', c.autopost, `chTgl('${id}','autopost',this)`),
    chTglRow('heart','Реакции','Лайки под постами', c.reactions, `chTgl('${id}','reactions',this)`),
  ];
  if(!course){
    pubRows.push(chTglRow('comment','Обсуждения','Комментарии к постам', c.discussions, `chTgl('${id}','discussions',this)`));
    pubRows.push(chNavRow('clock','Медленный режим', chSlowLabel(c.slowmode), `chPickSlow('${id}')`));
    pubRows.push(chNavRow('user','Кто может писать', chWhoPostLabel(c), `chPickWhoPost('${id}')`));
  }
  html += chList(pubRows);

  // ---- УЧАСТНИКИ ----
  html += `<div class="ch-sec-h">${chI('users')} Участники</div>`;
  html += chList([
    chNavRow('crown','Администраторы и права', (c.admins?c.admins.length:0)+'', `chGo('mAdmins','${id}')`),
    chNavRow('user','Подписчики', chFmtN(c.subs||0), `chGo('mSubs','${id}')`),
    chNavRow('lock','Чёрный список', (c.black?c.black.length:0)+'', `chGo('mBlack','${id}')`),
  ]);

  // ---- ПРИВАТНОСТЬ ----
  html += `<div class="ch-sec-h">${chI('lock')} Приватность</div>`;
  html += chList([
    chNavRow('users','Кто видит подписчиков', chPrivSubsLabel(c), `chPickPrivSubs('${id}')`),
    chTglRow('forward','Пересылка и сохранение', c.privFwd?'Разрешены':'Запрещены', c.privFwd, `chTgl('${id}','privFwd',this)`),
    chNavRow('feed','История для новых', chPrivHistLabel(c), `chPickPrivHist('${id}')`),
  ]);

  // ---- УВЕДОМЛЕНИЯ ----
  html += `<div class="ch-sec-h">${chI('bell')} Уведомления</div>`;
  html += chList([
    chTglRow('bell','Уведомления о канале', chNotifyOn(c)?'Звук включён':'Без звука', chNotifyOn(c), `chToggleNotify('${id}',this)`),
  ]);

  // ---- СТАТИСТИКА ----
  html += `<div class="ch-sec-h">${chI('poll')} Статистика</div>`;
  html += chList([
    chNavRow('poll','Аналитика канала','', `chGo('mStats','${id}')`, {sub:'Охваты · вовлечённость'}),
  ]);

  // ---- ОПАСНАЯ ЗОНА ----
  html += `<div class="ch-sec-h">${chI('flag')} Опасная зона</div>`;
  html += chList([
    chTglRow('bookmark', c.archived?'В архиве':'Архивировать', 'Скрыть из списков, не удаляя', c.archived, `chTgl('${id}','archived',this)`),
    chNavRow('trash','Удалить канал','Действие необратимо', `chDeleteChannel('${id}')`, {danger:true, noChev:true}),
  ]);
  html += `<div style="height:10px"></div>`;
  return {title:'Управление', html};
}
window.chWithdraw = function(id){
  const c = chChannel(id); if(!c) return;
  const gross = c.gross||0, net = Math.round(gross*(1-CH_FEE));
  if(net<=0){ toast('Пока нечего выводить — продаж не было'); return; }
  if(typeof walletAdd==='function') walletAdd(net, 'Доход канала: '+c.name);
  if(typeof okoEarn==='function') okoEarn(gross-net, 'Комиссия каналов 10%');
  c.gross = 0; chSave();
  toast('+'+net.toLocaleString('ru-RU').replace(/,/g,' ')+' ₽ зачислено на кошелёк');
  chRender();
};
window.chDeleteChannel = function(id){
  const c = chChannel(id); if(!c) return;
  const act = ()=>{
    CH.mine = CH.mine.filter(x=>x.id!==id); chSave();
    if(typeof CHATS!=='undefined'){ const k=CHATS.findIndex(x=>x.chId===id); if(k>=0){ CHATS.splice(k,1); if(typeof renderChatList==='function') renderChatList(); } }
    chUpdateProwCount();
    toast('Канал удалён'); chNav=[{page:'list'}]; chRender();
  };
  if(typeof showPopup==='function') showPopup({ico:'trash', title:'Удалить канал?',
    body:`«${chEsc(c.name)}» и все его материалы будут удалены безвозвратно.`,
    actions:[{label:'Удалить', onclick:act},{label:'Отмена', ghost:true}]});
  else act();
};

/* ---------- Тип, доступ и цена ---------- */
function chPageMType(id){
  const c = chChannel(id); if(!c) return {title:'Тип', html:''};
  const kinds = [
    ['channel','megaphone','Канал','Лента постов для аудитории'],
    ['club','crown','Клуб','Закрытое комьюнити по подписке'],
    ['course','circle-play','Курс','Видео-уроки внутри с прогрессом'],
  ];
  const lockAccess = c.kind==='course';
  const lockPaid = c.kind==='course';
  const paid = chPaid(c);
  const html = `
    <div class="ch-owner-note" style="margin-bottom:10px">Настрой продукт и доступ. Две независимые оси: <b>доступ</b> (открытый/закрытый) и <b>оплата</b> (бесплатно/платно). Изменения применяются сразу.</div>

    <div class="ch-sec-h">${chI('bolt')} Вид</div>
    <div class="ch-seg">${kinds.map(([t,ic,tt,d])=>`
      <button class="ch-type-card ${c.kind===t?'on':''}" onclick="chSetKind('${id}','${t}')">
        <span class="ch-tc-ic">${chI(ic)}</span>
        <span style="flex:1;min-width:0"><b>${tt}</b><small>${d}</small></span>
        <span class="ch-tc-check">${chI('check')}</span>
      </button>`).join('')}</div>

    <div class="ch-sec-h">${chI('lock')} Доступ</div>
    <div class="ch-choice2${lockAccess?' locked':''}">
      <button class="ch-ch2 ${c.access==='open'?'on':''}" ${lockAccess?'disabled':''} onclick="chSetAccess('${id}','open')">${chI('globe')}<b>Открытый</b><small>Виден и читается всем</small></button>
      <button class="ch-ch2 ${c.access==='closed'?'on':''}" ${lockAccess?'disabled':''} onclick="chSetAccess('${id}','closed')">${chI('lock')}<b>Закрытый</b><small>Витрина + доступ по заявке/оплате</small></button>
    </div>
    ${lockAccess?`<div class="ch-owner-note">Курс всегда закрытый.</div>`:''}

    <div class="ch-sec-h">${chI('money')} Оплата</div>
    <div class="ch-choice2${lockPaid?' locked':''}">
      <button class="ch-ch2 ${!paid?'on':''}" ${lockPaid?'disabled':''} onclick="chSetPaid('${id}',false)">${chI('check')}<b>Бесплатно</b><small>Без платы за доступ</small></button>
      <button class="ch-ch2 ${paid?'on':''}" ${lockPaid?'disabled':''} onclick="chSetPaid('${id}',true)">${chI('card')}<b>Платно</b><small>${c.kind==='course'?'Разовая цена':'Подписка ₽/мес'}</small></button>
    </div>
    ${paid?`
      <label class="ch-lab">${c.kind==='course'?'Цена курса (разовая)':'Цена подписки в месяц'}</label>
      <div class="ch-price-row">
        <input class="ch-input" id="chTPrice" inputmode="numeric" value="${c.price}" oninput="chSetPrice('${id}',this.value)">
        <span class="ch-unit">₽${chPriceUnit(c)}</span>
      </div>
      <div class="ch-price-chips" id="chPriceChips2">${[c.kind==='course'?[490,990,1490,2900]:[99,199,299,499]][0].map(p=>`<button class="${c.price===p?'on':''}" onclick="chSetPrice('${id}',${p});chRender()">${p} ₽</button>`).join('')}</div>
      <div class="ch-owner-note">С каждой продажи ты получаешь <b>${chNet(c.price)} ₽</b>, OKO — 10% (${Math.round(c.price*CH_FEE)} ₽).</div>`:''}`;
  return {title:'Тип и доступ', html};
}
window.chSetKind = function(id,t){
  const c = chChannel(id); if(!c) return;
  c.kind = t;
  if(t==='course'){ c.access='closed'; if(!chPaid(c)) c.price = c.price||990; if(!c.lessons) c.lessons=[{id:'l1',title:'Вводный урок',dur:'5:00'}]; c.discussions=false; }
  else if(t==='club'){ c.access='closed'; if(!chPaid(c)) c.price = c.price||299; }
  chNormalize(c); chSave(); chRender();
  toast('Вид изменён на «'+chKindLabel(c)+'»');
};
window.chSetAccess = function(id,a){
  const c = chChannel(id); if(!c) return;
  c.access = a; chNormalize(c); chSave(); chRender();
  toast(a==='closed'?'Канал закрыт — включена витрина':'Канал открыт для всех');
};
window.chSetPaid = function(id,p){
  const c = chChannel(id); if(!c) return;
  if(p){ if(!c.price) c.price = c.kind==='course'?990:299; if(c.access==='open') c.access='closed'; }
  else { c.price = 0; }
  chNormalize(c); chSave(); chRender();
  toast(p?'Платный доступ включён':'Доступ теперь бесплатный');
};
window.chSetPrice = function(id,v){
  const c = chChannel(id); if(!c) return;
  c.price = +String(v).replace(/\D/g,'')||0; chNormalize(c); chSave();
  const hint = document.querySelector('#chBody .ch-owner-note b'); // мягко, без перерисовки инпута
  if(hint) hint.textContent = chNet(c.price)+' ₽';
};

/* ---------- Подписчики ---------- */
function chPageMSubs(id){
  const c = chChannel(id); if(!c) return {title:'Подписчики', html:''};
  const m = c.members||[];
  const html = `
    <div class="ch-kpi" style="grid-template-columns:repeat(2,1fr)">
      <div class="ch-kpi-t">${chI('users')}<b>${chFmtN(c.subs||0)}</b><small>всего</small></div>
      <div class="ch-kpi-t">${chI('rocket')}<b>+${Math.max(1,Math.round((c.subs||0)*0.04))}</b><small>за неделю</small></div>
    </div>
    <div class="ch-sec-h">${chI('user')} Активные участники</div>
    <div style="background:var(--surface);border:1px solid var(--border);border-radius:var(--r-md);padding:4px 13px">
    ${m.length? m.map(p=>`
      <div class="ch-member">
        <div class="ch-av mini" style="${chAvSeedStyle(p.nick||p.name)}">${chEsc((p.name[0]||'U').toUpperCase())}</div>
        <div class="ch-m-b"><b>${chEsc(p.name)}${p.name===PROFILE.name?' <span class="ch-m-role owner">вы</span>':''}</b><small>@${chEsc(p.nick)} · вступил ${p.joined||'недавно'}</small></div>
        ${p.name!==PROFILE.name?`<button class="ch-m-act send" title="Написать" onclick="chDM('${chEsc(p.name)}','${chEsc(p.nick)}')">${chI('send')}</button>
        <button class="ch-m-act" title="В чёрный список" onclick="chBanMember('${id}','${chEsc(p.nick)}')">${chI('lock')}</button>`:''}
      </div>`).join('') : '<div class="ch-empty" style="border:none">Список участников пуст</div>'}
    </div>
    <div class="ch-owner-note">Показаны активные участники. Полный список синхронизируется с бэкендом при подключении Supabase.</div>`;
  return {title:'Подписчики', html};
}
window.chBanMember = function(id,nick){
  const c = chChannel(id); if(!c) return;
  const idx = (c.members||[]).findIndex(p=>p.nick===nick);
  if(idx<0) return;
  const m = c.members[idx];
  c.members.splice(idx,1);
  c.black = c.black||[]; c.black.unshift({name:m.name, nick:m.nick});
  if(c.subs) c.subs--;
  chSave(); chRender();
  toast(m.name+' в чёрном списке');
};

/* ---------- Администраторы ---------- */
function chPageMAdmins(id){
  const c = chChannel(id); if(!c) return {title:'Админы', html:''};
  const a = c.admins||[];
  const html = `
    <div class="ch-owner-note" style="margin-bottom:12px">Назначай администраторов и тонко настраивай их права — как в Telegram. Нажми на админа, чтобы открыть права. Владелец (ты) всегда имеет полный доступ.</div>
    <div style="background:var(--surface);border:1px solid var(--border);border-radius:var(--r-md);padding:4px 13px">
      <div class="ch-member">
        <div class="ch-av mini" style="${chAvGradStyle(c.bg||0)}">${chEsc((PROFILE.name[0]||'O').toUpperCase())}</div>
        <div class="ch-m-b"><b>${chEsc(PROFILE.name)}</b><small>@${chEsc(PROFILE.nick)} · создатель</small></div>
        <span class="ch-m-role owner">владелец</span>
      </div>
      ${a.map((p,idx)=>`
      <div class="ch-member ch-member-tap" onclick="chGo('mAdminRights',{c:'${id}',i:${idx}})">
        <div class="ch-av mini" style="${chAvSeedStyle(p.nick||p.name)}">${chEsc((p.name[0]||'A').toUpperCase())}</div>
        <div class="ch-m-b"><b>${chEsc(p.name)}</b><small>${chRightsSummary(p)} · @${chEsc(p.nick)}</small></div>
        <span class="ch-m-chev">${chI('chev')}</span>
      </div>`).join('')}
    </div>
    <button class="ch-add-btn" onclick="chAddAdmin('${id}')">${chI('plus')} Назначить администратора</button>`;
  return {title:'Администраторы', html};
}
window.chRemoveAdmin = function(id,nick){
  const c = chChannel(id); if(!c) return;
  c.admins = (c.admins||[]).filter(p=>p.nick!==nick); chSave(); chRender();
  toast('Администратор разжалован');
};
window.chAddAdmin = function(id){
  const c = chChannel(id); if(!c) return;
  const cand = (c.members||[]).filter(p=>p.name!==PROFILE.name && !(c.admins||[]).some(a=>a.nick===p.nick));
  if(!cand.length){ toast('Нет участников для назначения'); return; }
  const p = cand[0];
  c.admins = c.admins||[]; c.admins.push({name:p.name, nick:p.nick, rights:chDefaultRights()}); chSave();
  toast(p.name+' назначен администратором');
  chGo('mAdminRights',{c:id, i:c.admins.length-1});   // сразу открываем настройку прав (как в Telegram)
};

/* ---------- Права конкретного администратора ---------- */
function chPageMAdminRights(arg){
  const c = chChannel(arg&&arg.c); if(!c || !chIsMine(c)) return {title:'Права', html:'<div class="ch-empty">Нет доступа</div>'};
  const p = (c.admins||[])[arg&&arg.i]; if(!p) return {title:'Права', html:'<div class="ch-empty">Администратор не найден</div>'};
  if(!p.rights) p.rights = chDefaultRights();
  let html = `<div class="ch-manage-hub" style="text-align:center">
      <div class="ch-av big" style="${chAvSeedStyle(p.nick||p.name)}">${chEsc((p.name[0]||'A').toUpperCase())}</div>
      <div style="font:800 18px/1.1 var(--font-display);margin-top:10px">${chEsc(p.name)}</div>
      <div style="color:var(--dim);font-size:12px;margin-top:4px">@${chEsc(p.nick)} · администратор</div>
    </div>`;
  html += `<div class="ch-sec-h">${chI('crown')} Права администратора</div>`;
  html += chList(CH_RIGHTS.map(([k,ic,t,d])=>chTglRow(ic,t,d,!!p.rights[k],`chToggleRight('${arg.c}',${arg.i},'${k}',this)`)));
  html += chLF('Точная настройка прав — как в Telegram. Отключённые права недоступны этому администратору; остальные модераторы не затрагиваются.');
  html += `<div class="ch-sec-h">${chI('flag')} Управление</div>`;
  html += chList([ chNavRow('trash','Разжаловать администратора','Снять все права', `chRemoveAdminAt('${arg.c}',${arg.i})`, {danger:true, noChev:true}) ]);
  html += `<div style="height:10px"></div>`;
  return {title:'Права администратора', html};
}
/* переключить одно право (surgical, без полного ре-рендера — как chTgl) */
window.chToggleRight = function(id,i,key,el){
  const c = chChannel(id); if(!c) return;
  const p = (c.admins||[])[i]; if(!p) return;
  p.rights = p.rights || chDefaultRights();
  p.rights[key] = !p.rights[key]; chSave();
  if(el){ const sw = el.querySelector('.switch'); if(sw) sw.classList.toggle('on', !!p.rights[key]); }
  toast(p.rights[key]?'Право выдано':'Право отозвано');
};
window.chRemoveAdminAt = function(id,i){
  const c = chChannel(id); if(!c) return;
  const p = (c.admins||[])[i]; if(!p) return;
  const act = ()=>{ c.admins.splice(i,1); chSave(); if(chNav.length>1) chNav.pop(); chRender(); toast('Администратор разжалован'); };
  if(typeof showPopup==='function') showPopup({ico:'trash', title:'Разжаловать администратора?',
    body:`«${chEsc(p.name)}» потеряет все права администратора канала.`,
    actions:[{label:'Разжаловать', onclick:act},{label:'Отмена', ghost:true}]});
  else act();
};

/* ---------- Чёрный список ---------- */
function chPageMBlack(id){
  const c = chChannel(id); if(!c) return {title:'Чёрный список', html:''};
  const b = c.black||[];
  const html = `
    <div class="ch-owner-note" style="margin-bottom:12px">Заблокированные не видят канал и не могут подписаться. Разблокировка возвращает доступ.</div>
    ${b.length?`<div style="background:var(--surface);border:1px solid var(--border);border-radius:var(--r-md);padding:4px 13px">
      ${b.map(p=>`<div class="ch-member">
        <div class="ch-av mini" style="background:var(--raised);color:var(--danger)">${chI('lock')}</div>
        <div class="ch-m-b"><b>${chEsc(p.name)}</b><small>@${chEsc(p.nick)}</small></div>
        <button class="ch-m-act" style="color:var(--lime)" title="Разблокировать" onclick="chUnban('${id}','${chEsc(p.nick)}')">${chI('check')}</button>
      </div>`).join('')}
    </div>`:'<div class="ch-empty">Чёрный список пуст — канал чистый</div>'}`;
  return {title:'Чёрный список', html};
}
window.chUnban = function(id,nick){
  const c = chChannel(id); if(!c) return;
  const idx = (c.black||[]).findIndex(p=>p.nick===nick);
  if(idx<0) return;
  const p = c.black[idx]; c.black.splice(idx,1);
  c.members = c.members||[]; c.members.push({name:p.name, nick:p.nick, joined:'сейчас'});
  if(c.subs!=null) c.subs++;
  chSave(); chRender(); toast(p.name+' разблокирован');
};

/* ---------- Статистика (canvas) ---------- */
function chPageMStats(id){
  const c = chChannel(id); if(!c) return {title:'Статистика', html:''};
  const subs = c.subs||0;
  const reach = Math.round(subs*3.4)+1200;
  const er = (4 + (id.length%4))+'.'+((subs%9))+'%';
  const html = `
    <div class="ch-kpi">
      <div class="ch-kpi-t">${chI('users')}<b>${chFmtN(subs)}</b><small>подписчиков</small></div>
      <div class="ch-kpi-t">${chI('eye')}<b>${chFmtN(reach)}</b><small>охват/нед</small></div>
      <div class="ch-kpi-t">${chI('heart')}<b>${er}</b><small>вовлечённость</small></div>
      <div class="ch-kpi-t">${chI('rocket')}<b>+${Math.max(1,Math.round(subs*0.04))}</b><small>прирост/нед</small></div>
    </div>
    <div class="ch-canvas-wrap">
      <div class="ch-cw-h">Рост подписчиков · 14 дней <span>+${Math.max(1,Math.round(subs*0.09))}</span></div>
      <canvas id="chStatLine" width="600" height="260"></canvas>
    </div>
    <div class="ch-canvas-wrap">
      <div class="ch-cw-h">Охват по дням недели <span>пик: сб</span></div>
      <canvas id="chStatBars" width="600" height="260"></canvas>
    </div>
    <div class="ch-donut">
      <canvas id="chStatDonut" width="192" height="192"></canvas>
      <div class="ch-donut-leg">
        <div><i style="background:#9AFF00"></i>Из рекомендаций<b>46%</b></div>
        <div><i style="background:#6fd400"></i>По ссылке<b>31%</b></div>
        <div><i style="background:#3a7a00"></i>Поиск OKO<b>15%</b></div>
        <div><i style="background:var(--border)"></i>Другое<b>8%</b></div>
      </div>
    </div>
    ${chPaid(c)?`<div class="ch-canvas-wrap"><div class="ch-cw-h">Выручка · 6 мес <span>${(c.gross||0).toLocaleString('ru-RU').replace(/,/g,' ')} ₽</span></div><canvas id="chStatRev" width="600" height="260"></canvas></div>`:''}
    <div class="ch-owner-note">Данные прототипа сгенерированы детерминированно по параметрам канала. С подключением Supabase — реальная аналитика.</div>`;
  return {title:'Статистика', html, after:()=>chDrawStats(c, subs, reach)};
}
function chSeeded(seed){ let s=seed%2147483647; if(s<=0)s+=2147483646; return ()=>{ s=s*16807%2147483647; return (s-1)/2147483646; }; }
function chDrawStats(c, subs, reach){
  const acc = '#9AFF00', dim = 'rgba(154,255,0,.14)';
  const gridCol = getComputedStyle(document.documentElement).getPropertyValue('--border')||'#333';
  const rnd = chSeeded((c.id.length*31 + subs)||7);
  // --- линия роста ---
  (function(){
    const cv = document.getElementById('chStatLine'); if(!cv) return;
    const ctx = cv.getContext('2d'), W=cv.width, H=cv.height, pad=14;
    const pts=[]; let base=subs*0.82;
    for(let i=0;i<14;i++){ base += subs*0.013*(0.6+rnd()); pts.push(base); }
    const mn=Math.min(...pts), mx=Math.max(...pts);
    const x=i=>pad+(W-pad*2)*i/(pts.length-1);
    const y=v=>H-pad-(H-pad*2)*(v-mn)/((mx-mn)||1);
    ctx.clearRect(0,0,W,H);
    ctx.strokeStyle=gridCol; ctx.lineWidth=1; ctx.globalAlpha=.5;
    for(let g=0;g<=3;g++){ const yy=pad+(H-pad*2)*g/3; ctx.beginPath(); ctx.moveTo(pad,yy); ctx.lineTo(W-pad,yy); ctx.stroke(); }
    ctx.globalAlpha=1;
    let t=0; (function anim(){
      t=Math.min(1,t+0.05); const n=Math.max(1,Math.floor((pts.length-1)*t));
      ctx.clearRect(0,0,W,H);
      ctx.strokeStyle=gridCol; ctx.globalAlpha=.5; ctx.lineWidth=1;
      for(let g=0;g<=3;g++){ const yy=pad+(H-pad*2)*g/3; ctx.beginPath(); ctx.moveTo(pad,yy); ctx.lineTo(W-pad,yy); ctx.stroke(); }
      ctx.globalAlpha=1;
      const grad=ctx.createLinearGradient(0,pad,0,H); grad.addColorStop(0,dim); grad.addColorStop(1,'rgba(154,255,0,0)');
      ctx.beginPath(); ctx.moveTo(x(0),y(pts[0]));
      for(let i=1;i<=n;i++) ctx.lineTo(x(i),y(pts[i]));
      ctx.lineTo(x(n),H-pad); ctx.lineTo(x(0),H-pad); ctx.closePath(); ctx.fillStyle=grad; ctx.fill();
      ctx.beginPath(); ctx.moveTo(x(0),y(pts[0]));
      for(let i=1;i<=n;i++) ctx.lineTo(x(i),y(pts[i]));
      ctx.strokeStyle=acc; ctx.lineWidth=2.6; ctx.lineJoin='round'; ctx.stroke();
      ctx.beginPath(); ctx.arc(x(n),y(pts[n]),4.2,0,7); ctx.fillStyle=acc; ctx.fill();
      if(t<1) requestAnimationFrame(anim);
    })();
  })();
  // --- бары охвата ---
  (function(){
    const cv=document.getElementById('chStatBars'); if(!cv) return;
    const ctx=cv.getContext('2d'), W=cv.width, H=cv.height, pad=14;
    const days=['пн','вт','ср','чт','пт','сб','вс']; const vals=days.map(()=>0.4+rnd()*0.6); vals[5]=1;
    const bw=(W-pad*2)/days.length*0.62, gap=(W-pad*2)/days.length;
    let t=0;(function anim(){
      t=Math.min(1,t+0.06); ctx.clearRect(0,0,W,H);
      vals.forEach((v,i)=>{
        const h=(H-pad*2-16)*v*t; const bx=pad+gap*i+(gap-bw)/2; const by=H-pad-16-h;
        const g=ctx.createLinearGradient(0,by,0,by+h); g.addColorStop(0,acc); g.addColorStop(1,'#3a7a00');
        ctx.fillStyle=i===5?acc:g; ctx.globalAlpha=i===5?1:.85;
        const r=6; ctx.beginPath();
        ctx.moveTo(bx,by+h); ctx.lineTo(bx,by+r); ctx.arcTo(bx,by,bx+r,by,r); ctx.lineTo(bx+bw-r,by); ctx.arcTo(bx+bw,by,bx+bw,by+r,r); ctx.lineTo(bx+bw,by+h); ctx.closePath(); ctx.fill();
        ctx.globalAlpha=1;
      });
      if(t<1) requestAnimationFrame(anim);
    })();
  })();
  // --- пончик источников ---
  (function(){
    const cv=document.getElementById('chStatDonut'); if(!cv) return;
    const ctx=cv.getContext('2d'), W=cv.width, H=cv.height, cx=W/2, cy=H/2, R=W/2-8, r=R*0.6;
    const segs=[[0.46,'#9AFF00'],[0.31,'#6fd400'],[0.15,'#3a7a00'],[0.08,gridCol]];
    let t=0;(function anim(){
      t=Math.min(1,t+0.05); ctx.clearRect(0,0,W,H);
      let a=-Math.PI/2;
      segs.forEach(([v,col])=>{
        const ang=v*Math.PI*2*t; ctx.beginPath(); ctx.moveTo(cx,cy);
        ctx.arc(cx,cy,R,a,a+ang); ctx.closePath(); ctx.fillStyle=col; ctx.fill(); a+=ang;
      });
      ctx.beginPath(); ctx.arc(cx,cy,r,0,7); ctx.fillStyle=getComputedStyle(document.documentElement).getPropertyValue('--surface')||'#111'; ctx.fill();
      if(t<1) requestAnimationFrame(anim);
    })();
  })();
  // --- выручка (если платный) ---
  (function(){
    const cv=document.getElementById('chStatRev'); if(!cv) return;
    const ctx=cv.getContext('2d'), W=cv.width, H=cv.height, pad=14;
    const w=[0.4,0.55,0.6,0.72,0.85,1]; const bw=(W-pad*2)/6*0.6, gap=(W-pad*2)/6;
    let t=0;(function anim(){
      t=Math.min(1,t+0.06); ctx.clearRect(0,0,W,H);
      w.forEach((v,i)=>{
        const h=(H-pad*2)*v*t; const bx=pad+gap*i+(gap-bw)/2; const by=H-pad-h;
        const g=ctx.createLinearGradient(0,by,0,by+h); g.addColorStop(0,'#9AFF00'); g.addColorStop(1,'#2a5a00');
        ctx.fillStyle=g; ctx.globalAlpha=i===5?1:.8;
        ctx.fillRect(bx,by,bw,h); ctx.globalAlpha=1;
      });
      if(t<1) requestAnimationFrame(anim);
    })();
  })();
}

/* ---------- Фон и аватар ---------- */
function chPageMAppear(id){
  const c = chChannel(id); if(!c || !chIsMine(c)) return {title:'Оформление', html:'<div class="ch-empty">Нет доступа</div>'};
  const prevBg = c.cover
    ? `background:linear-gradient(180deg,rgba(0,0,0,.1),rgba(0,0,0,.45)),url(${c.cover});background-size:cover;background-position:center`
    : `background:${CH_BGS[c.bg||0]}`;
  const avStyle = c.avatar
    ? `background-image:url(${c.avatar});background-size:cover;background-position:center`
    : chAvGradStyle(c.bg||0);
  const html = `
    <div class="ch-appear-prev" style="${prevBg}">
      <div class="ch-ap-ava" style="${avStyle}">${c.avatar?'':(c.icon?chI(c.icon):chEsc((c.name[0]||'K').toUpperCase()))}</div>
      <div class="ch-ap-name">${chEsc(c.name)}<span class="ch-ap-id">@${chEsc(chNick(c))}</span></div>
    </div>

    <div class="ch-photo-row">
      <button class="ch-photo-btn" onclick="chUploadCover('${id}')">${chI('photo')} ${c.cover?'Сменить обложку':'Загрузить обложку'}</button>
      ${c.cover?`<button class="ch-photo-btn del" onclick="chClearCover('${id}')" title="Убрать">${chI('trash')}</button>`:''}
    </div>
    <div class="ch-photo-row">
      <button class="ch-photo-btn" onclick="chUploadAvatar('${id}')">${chI('camera')} ${c.avatar?'Сменить аватар':'Загрузить аватар'}</button>
      ${c.avatar?`<button class="ch-photo-btn del" onclick="chClearAvatar('${id}')" title="Убрать">${chI('trash')}</button>`:''}
    </div>

    <label class="ch-lab">Название канала</label>
    <input class="ch-input" id="chNameEdit" maxlength="42" value="${chEsc(c.name)}" oninput="chSetName('${id}',this.value)">
    <label class="ch-lab">Адрес канала (@id)</label>
    <div class="ch-price-row">
      <span class="ch-unit" style="font-size:15px">@</span>
      <input class="ch-input" id="chNickEdit" maxlength="18" value="${chEsc(chNick(c))}" oninput="chSetNick('${id}',this.value)" style="flex:1">
    </div>
    <label class="ch-lab">Описание</label>
    <textarea class="ch-ta" id="chDescEdit" rows="3" maxlength="240" placeholder="О чём канал, что получит подписчик…" oninput="chSetDesc('${id}',this.value)">${chEsc(c.desc||'')}</textarea>

    <label class="ch-lab">Фон-градиент${c.cover?' (под обложкой)':''}</label>
    <div class="ch-swatches">${CH_BGS.map((g,i)=>`<div class="ch-sw ${c.bg===i?'on':''}" style="background:${g}" onclick="chSetBg('${id}',${i})"></div>`).join('')}</div>

    <label class="ch-lab">Фон чата <span style="font-weight:400;color:var(--dim)">— за постами канала</span></label>
    <div class="ch-swatches">
      <div class="ch-sw ch-sw-none ${c.chatBg==null?'on':''}" onclick="chSetChatBg('${id}',null)" title="Без фона">${chI('check2')}</div>
      ${CH_BGS.map((g,i)=>`<div class="ch-sw ${c.chatBg===i?'on':''}" style="background:${g}" onclick="chSetChatBg('${id}',${i})"></div>`).join('')}
    </div>

    <label class="ch-lab">Значок (если без фото-аватара)</label>
    <div class="ch-letter-row">
      <button class="ch-lt ${!c.icon?'on':''}" onclick="chSetIcon('${id}','')" title="Первая буква"><span style="font:800 18px/1 var(--font-display);color:var(--lime)">${(c.name[0]||'K').toUpperCase()}</span></button>
      ${CH_ICONS.map(ic=>`<button class="ch-lt ${c.icon===ic?'on':''}" onclick="chSetIcon('${id}','${ic}')">${chI(ic)}</button>`).join('')}
    </div>
    <div class="ch-owner-note">Обложка, аватар, фон чата, название, адрес и описание отображаются на странице канала, в карточке и в мессенджере. Всё сохраняется сразу.</div>`;
  return {title:'Оформление', html};
}
window.chSetDesc = function(id,v){ const c=chChannel(id); if(!c) return; c.desc = String(v).slice(0,240); chSave(); };
window.chSetChatBg = function(id,i){ const c=chChannel(id); if(!c) return; c.chatBg = (i==null?null:+i); chSave(); chRender(); };
window.chSetBg = function(id,i){ const c=chChannel(id); if(!c) return; c.bg=i; chSave(); chSyncMirror(c); chRender(); };
window.chSetIcon = function(id,ic){ const c=chChannel(id); if(!c) return; c.icon=ic||null; chSave(); chSyncMirror(c); chRender(); };
window.chSetName = function(id,v){
  const c=chChannel(id); if(!c) return;
  c.name = String(v).slice(0,42) || 'Канал'; chSave(); chSyncMirror(c);
  const pn=document.querySelector('#chBody .ch-ap-name'); if(pn && pn.childNodes[0]){ pn.childNodes[0].nodeValue = c.name; }
};
window.chSetNick = function(id,v){
  const c=chChannel(id); if(!c) return;
  c.nick = chSlug(v); chSave(); chSyncMirror(c);
  const el=document.getElementById('chNickEdit'); if(el && el.value!==c.nick) el.value=c.nick;
  const pid=document.querySelector('#chBody .ch-ap-id'); if(pid) pid.textContent='@'+c.nick;
};
window.chUploadCover = function(id){ const c=chChannel(id); if(!c) return; chPickFile(f=>chReadImage(f,1000,0.7,url=>{ c.cover=url; chSave(); chRender(); toast('Обложка обновлена'); })); };
window.chClearCover = function(id){ const c=chChannel(id); if(!c) return; c.cover=null; chSave(); chRender(); };
window.chUploadAvatar = function(id){ const c=chChannel(id); if(!c) return; chPickFile(f=>chReadImage(f,300,0.8,url=>{ c.avatar=url; chSave(); chSyncMirror(c); chRender(); toast('Аватар обновлён'); })); };
window.chClearAvatar = function(id){ const c=chChannel(id); if(!c) return; c.avatar=null; chSave(); chSyncMirror(c); chRender(); };
function chSyncMirror(c){
  if(typeof CHATS==='undefined') return;
  const x = CHATS.find(k=>k.chId===c.id); if(!x) return;
  x.avaIcon = c.icon||null; x.ava=(c.name[0]||'K').toUpperCase();
  x.name = c.name; x.avaImg = c.avatar||null; x.nick = chNick(c);
  if(typeof renderChatList==='function') try{ renderChatList(); }catch(e){}
}

/* ---------- Настройки (тумблеры) ---------- */
function chPageMSettings(id){
  const c = chChannel(id); if(!c) return {title:'Настройки', html:''};
  const row = (ic,t,d,on,fn)=>`
    <button class="ch-toggle-row" onclick="${fn}">
      <span class="ch-tr-ic">${chI(ic)}</span>
      <span class="ch-tr-b"><b>${t}</b><small>${d}</small></span>
      <span class="switch ${on?'on':''}"><i></i></span>
    </button>`;
  const lockAccess = c.kind==='course';
  const html = `
    <div class="ch-sec-h">${chI('lock')} Доступ</div>
    ${row('lock','Закрытый доступ', lockAccess?'Курс всегда закрытый':'Витрина и вход по заявке/оплате', c.access==='closed', lockAccess?`toast('Курс всегда закрытый')`:`chToggleAccess('${id}')`)}
    ${row('card','Платный доступ', chPaid(c)?('Цена '+c.price+' ₽'+chPriceUnit(c)):'Включи и задай цену', chPaid(c), `chToggleAccess('${id}','paid')`)}
    <div class="ch-sec-h">${chI('comment')} Взаимодействие</div>
    ${row('comment','Обсуждения','Подписчики комментируют посты канала', c.discussions, `chToggle('${id}','discussions')`)}
    ${row('heart','Реакции','Лайки и эмодзи под постами', c.reactions, `chToggle('${id}','reactions')`)}
    <div class="ch-sec-h">${chI('megaphone')} Тип и цена</div>
    <button class="ch-mrow" onclick="chGo('mType','${id}')">
      <span class="ch-mr-ic">${chI('bolt')}</span>
      <span class="ch-mr-b"><b>Тип, доступ и цена</b><small>${chAccessLine(c)}</small></span>
      <span class="ch-mr-val">${chI('chev')}</span>
    </button>
    <div class="ch-sec-h">${chI('share')} Ссылка на канал</div>
    <button class="ch-mrow" onclick="chCopyLink('${id}')">
      <span class="ch-mr-ic">${chI('globe')}</span>
      <span class="ch-mr-b"><b style="white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${chEsc(chPublicLink(c))}</b><small>Публичная ссылка-приглашение</small></span>
      <span class="ch-mr-val">${chI('copy')}</span>
    </button>`;
  return {title:'Настройки', html};
}
window.chToggle = function(id,key){
  const c = chChannel(id); if(!c) return; c[key]=!c[key]; chSave(); chRender();
  toast((key==='discussions'?'Обсуждения ':'Реакции ')+(c[key]?'включены':'выключены'));
};
/* быстрый тумблер доступа/оплаты прямо из настроек */
window.chToggleAccess = function(id, which){
  const c = chChannel(id); if(!c) return;
  if(which==='paid'){
    if(c.kind==='course'){ toast('Курс всегда платный'); return; }
    if(chPaid(c)){ c.price = 0; }
    else { c.price = c.kind==='course'?990:299; c.access='closed'; toast('Задай цену в «Тип и доступ»'); }
  } else {
    if(c.kind==='course'){ toast('Курс всегда закрытый'); return; }
    c.access = (c.access==='closed') ? 'open' : 'closed';
  }
  chNormalize(c); chSave(); chRender();
};
window.chCopyLink = function(id){
  const c = chChannel(id); if(!c) return;
  const link = chPublicLink(c);
  try{ navigator.clipboard && navigator.clipboard.writeText(link); }catch(e){}
  toast('Ссылка скопирована: '+link);
};

/* ---------- Приглашения / ссылки-инвайты ---------- */
function chPageMInvites(id){
  const c = chChannel(id); if(!c || !chIsMine(c)) return {title:'Приглашения', html:'<div class="ch-empty">Нет доступа</div>'};
  let html = `<div class="ch-sec-h">${chI('globe')} Публичная ссылка</div>`;
  html += chList([
    chNavRow('globe', chPublicLink(c), '', `chCopyLink('${id}')`, {copy:true, lime:true, sub:'Любой откроет канал по этой ссылке'})
  ]);
  html += `<div class="ch-sec-h">${chI('share')} Пригласительные ссылки</div>`;
  const rows = (c.invites||[]).map((v,i)=>
    chNavRow('forward', v.link, '', `chCopyInvite('${id}',${i})`, {copy:true, sub:(v.limit?('лимит '+v.limit+' входов · '):'')+'создана '+(v.when||'недавно')}));
  rows.push(`<button class="ch-row" onclick="chNewInvite('${id}')">
    <span class="ch-row-ic">${chI('plus')}</span>
    <span class="ch-row-main"><b style="color:var(--lime)">Создать новую ссылку</b></span>
  </button>`);
  html += chList(rows);
  html += chLF('Пригласительные ссылки работают даже для закрытого канала — делись ими где угодно. Каждая ведёт напрямую в канал.');
  return {title:'Приглашения', html};
}
window.chNewInvite = function(id){
  const c = chChannel(id); if(!c) return;
  c.invites = c.invites||[];
  const code = Math.random().toString(36).slice(2,10);
  c.invites.unshift({ link:'okoteam.top/+'+code, when:'сейчас', limit:0 });
  chSave(); chRender(); toast('Пригласительная ссылка создана');
};
window.chCopyInvite = function(id,i){
  const c = chChannel(id); if(!c || !c.invites || !c.invites[i]) return;
  try{ navigator.clipboard && navigator.clipboard.writeText(c.invites[i].link); }catch(e){}
  toast('Ссылка скопирована: '+c.invites[i].link);
};

/* ================= ВХОДЫ ================= */
/* 1) профиль: строка «Мои каналы» */
function chInsertProfileRow(){
  if(document.getElementById('chProfileRow')) return;
  const card = document.querySelector('#screen-profile .card');
  if(!card) return;
  const row = document.createElement('button');
  row.className = 'prow'; row.id = 'chProfileRow'; row.onclick = ()=>chOpen('list');
  row.innerHTML = `${chI('megaphone')} Мои каналы <span id="chProwCount">${CH.mine.length}</span> <span class="chev">${chI('chev')}</span>`;
  const anchor = card.querySelector('.prow[onclick="openEdit()"]');
  if(anchor) anchor.insertAdjacentElement('afterend', row);
  else card.appendChild(row);
}
function chUpdateProwCount(){ const el=document.getElementById('chProwCount'); if(el) el.textContent = CH.mine.length; }

/* 2) перехват создания канала/чата/супергруппы из меню «+» в чатах:
      pickChatKind('channel'|'chat'|'sgroup'|'group') → единый мастер chPageCreate */
if(typeof pickChatKind==='function'){
  const _prevPickChatKind = pickChatKind;
  pickChatKind = function(kind){
    if(kind==='channel' || kind==='sgroup' || kind==='chat' || kind==='group'){
      if(typeof closeSheet==='function') closeSheet();
      const target = kind==='group' ? 'sgroup' : kind;   // старую «Группу» тянем в супергруппу
      chDraft.kind = target;
      chPickKind(target);
      chOpen('create');
      return;
    }
    return _prevPickChatKind.apply(this, arguments);
  };
}

/* 2b) заменяем содержимое sheet-new-chat: теперь 3 варианта — Чат / Канал / Супергруппа */
function chPatchNewChatSheet(){
  const sheet = document.getElementById('sheet-new-chat'); if(!sheet) return;
  if(sheet.dataset.chPatched === '1') return;
  const heading = sheet.querySelector('h3');
  const form = sheet.querySelector('#newChatForm');
  const items = [
    {kind:'chat',    ic:'users',     t:'Чат',         s:'Общение группой. Пишут все участники, до 200 на FREE.'},
    {kind:'sgroup',  ic:'crown',     t:'Супергруппа', s:'До 200 000 участников, роли, темы, модерация — как в Telegram.'},
    {kind:'channel', ic:'megaphone', t:'Канал',       s:'Лента постов, пишет только админ. Юзеры — в комментариях.'},
    {kind:'direct',  ic:'user',      t:'Личный чат',  s:'Диалог один на один.'}
  ];
  const btnsHtml = items.map(it=>`
    <button class="sheet-item" onclick="pickChatKind('${it.kind}')">
      <svg class="i"><use href="#i-${it.ic}"/></svg>
      <span>${it.t}<small>${it.s}</small></span>
    </button>`).join('');
  /* убираем старые кнопки, подставляем новые перед формой */
  const old = sheet.querySelectorAll('.sheet-item'); old.forEach(b=>b.remove());
  if(form) sheet.insertBefore(document.createRange().createContextualFragment(btnsHtml), form);
  else sheet.insertAdjacentHTML('beforeend', btnsHtml);
  if(heading) heading.textContent = 'Создать';
  sheet.dataset.chPatched = '1';
}
try{ chPatchNewChatSheet(); }catch(e){}

/* 3) открытие канала-зеркала из списка чатов → наша вьюха */
if(typeof openConv==='function'){
  const _prevOpenConvCh = openConv;
  openConv = function(id){
    const c = (typeof CHATS!=='undefined') && CHATS.find(x=>x.id===id);
    if(c && c.openChannel){ chOpen('channel', c.openChannel); return; }
    return _prevOpenConvCh.apply(this, arguments);
  };
}

/* строку в профиле обновлять при перерисовке профиля */
if(typeof renderMyProfile==='function'){
  const _prevRMPch = renderMyProfile;
  renderMyProfile = function(){ _prevRMPch.apply(this, arguments); chInsertProfileRow(); chUpdateProwCount(); };
}

/* 3b) чип «Каталог» первым в фильтрах чатов — открывает витрину платного контента */
function chInsertCatalogChip(){
  const folders = document.getElementById('folders'); if(!folders) return;
  if(folders.querySelector('.ch-cat-open-chip')) return;
  const b = document.createElement('button');
  b.className = 'ch-cat-open-chip';
  b.innerHTML = chI('star') + '<span>Каталог</span>';
  b.title = 'Каталог платных каналов, клубов и курсов';
  b.addEventListener('click', e=>{ e.preventDefault(); chOpen('catalog'); });
  folders.insertBefore(b, folders.firstChild);
}
if(typeof renderFolders==='function'){
  const _prevRF = renderFolders;
  renderFolders = function(){ _prevRF.apply(this, arguments); try{ chInsertCatalogChip(); }catch(e){} };
}

/* 4) посты каналов в ленте рекомендаций: помечаем источником-каналом + открываем канал по тапу.
   Ранжирование по активности уже делает ядро (feedScore: лайки/комменты/репосты/просмотры). */
function chDecorateFeed(){
  const list = document.getElementById('feedList'); if(!list || typeof POSTS==='undefined') return;
  const map = {};
  (POSTS.rec||[]).concat(POSTS.sub||[]).forEach(p=>{ if(p && p.chOrigin) map[String(p.id)] = p; });
  list.querySelectorAll('.post[data-pid]').forEach(card=>{
    const p = map[card.getAttribute('data-pid')]; if(!p) return;
    const ava = card.querySelector('.ava');
    if(ava && !ava.dataset.chDone){
      ava.dataset.chDone = '1';
      if(p.avaImg){ ava.style.backgroundImage='url('+p.avaImg+')'; ava.style.backgroundSize='cover'; ava.style.backgroundPosition='center'; ava.textContent=''; }
      else if(p.avaIcon){ ava.innerHTML = chI(p.avaIcon); ava.classList.add('ch-feed-ava'); }
      ava.style.cursor='pointer';
      ava.addEventListener('click', function(e){ e.stopPropagation(); chOpen('channel', p.chOrigin); });
    }
    const nameEl = card.querySelector('.name');
    if(nameEl && !nameEl.querySelector('.ch-src-chip')){
      const chip = document.createElement('span');
      chip.className = 'chip ch-src-chip';
      chip.innerHTML = chI(p.chKind==='course'?'circle-play':p.chKind==='club'?'crown':'megaphone') + (p.chKind==='course'?'Курс':p.chKind==='club'?'Клуб':'Канал');
      chip.style.cursor='pointer';
      chip.addEventListener('click', function(e){ e.stopPropagation(); chOpen('channel', p.chOrigin); });
      nameEl.appendChild(chip);
    }
  });
}
if(typeof renderFeed==='function'){
  const _prevRenderFeedCh = renderFeed;
  renderFeed = function(){ const r=_prevRenderFeedCh.apply(this, arguments); try{ chDecorateFeed(); }catch(e){} return r; };
}

/* ================= МАРКЕТПЛЕЙС OKO: живой каталог (каналы / клубы / курсы) =================
   Дефолтная витрина, чтобы новый юзер видел «живой Amazon Marketplace», а не пустоту.
   Всё детерминированное — рейтинги, отзывы, обложки, авторы. Никаких эмодзи, только SVG.
   Данные добавляются один раз (по id), никогда не перетирают уже созданное юзером. */
/* Витрина «Каналы · Клубы · Курсы».
   09.08 удалён демо-каталог (15 каналов + 10 клубов + 15 курсов с выдуманными
   авторами и отзывами). Витрину наполняют реальные авторы через «Создать канал»,
   данные приходят из Supabase. Пока пусто — экран показывает empty-state. */
const CH_MARKET = [];

(function chInitCat(){
  try{
    (CH.disc||[]).forEach(c=>{ if(!c.niche) c.niche = chNiche(c); });
    (CH.mine||[]).forEach(c=>{ if(!c.niche) c.niche = chNiche(c); });
    CH_MARKET.forEach(s=>{ if(!CH.disc.some(x=>x.id===s.id)) CH.disc.push(chNormalize(s)); });
    chSave();
  }catch(e){}
})();
(function chInit(){
  // недостающий символ шестерёнки (в ядре нет i-gear) — добавляем в стиле бренда
  try{
    const defs = document.querySelector('svg defs');
    if(defs && !document.getElementById('i-gear')){
      const s = document.createElementNS('http://www.w3.org/2000/svg','symbol');
      s.setAttribute('id','i-gear'); s.setAttribute('viewBox','0 0 100 100');
      s.innerHTML = '<circle cx="50" cy="50" r="12"/><path d="M50 20V32M50 68V80M20 50H32M68 50H80M29 29l8.5 8.5M62.5 62.5 71 71M71 29l-8.5 8.5M37.5 62.5 29 71"/>';
      defs.appendChild(s);
    }
  }catch(e){}
  chInsertProfileRow();
  try{ chInsertCatalogChip(); }catch(e){}
  // зеркалим существующие свои каналы в мессенджер (после перезагрузки)
  CH.mine.forEach(c=>chMirrorToChats(c));
})();

/* экспорт в глобальную область (onclick в разметке) */
window.chOpen = chOpen; window.chGo = chGo; window.chBack = chBack; window.chClose = chClose;
window.chDraft = chDraft; window.chRender = chRender;
/* черновики композера/урока используются в inline-обработчиках (глобальная область) — экспортируем */
window.chCompose = chCompose; window.chLessonDraft = chLessonDraft;

/* ---- мост для слоя полировки oko-channels2.js ----------------------------
   Модуль «Каналы» завёрнут в IIFE: модель CH и хелперы наружу не выходили,
   поэтому слой полировки не мог ни починить демо-данные, ни добавить поиск.
   Отдаём наружу читаемый доступ к модели и набор чистых хелперов. Ядро при
   этом не переписываем — слой правит рендер поверх него. */
window.chCore = {
  get CH(){ return CH; },
  get nav(){ return chNav; },
  setNav(a){ chNav = Array.isArray(a) ? a : [{page:'list'}]; },
  save: chSave, render: chRender, close: chClose,
  channel: chChannel, isMine: chIsMine, isSubbed: chIsSubbed, normalize: chNormalize,
  gated: chGated, paid: chPaid, nick: chNick, slug: chSlug, publicLink: chPublicLink,
  kindLabel: chKindLabel, priceUnit: chPriceUnit, accessLine: chAccessLine,
  accessLineShort: chAccessLineShort, badges: chBadges, badge: chBadge,
  avInner: chAvInner, avGradStyle: chAvGradStyle, avSeedStyle: chAvSeedStyle,
  courseProg: chCourseProg, postId: chPostId, ensurePostIds: chEnsurePostIds,
  pinnedId: chPinnedId, notifyOn: chNotifyOn, niche: chNiche, net: chNet,
  mirror: chMirrorToChats, syncMirror: chSyncMirror, prowCount: chUpdateProwCount,
  pickFile: chPickFile, readImage: chReadImage,
  list: chList, lf: chLF, navRow: chNavRow, linkRow: chLinkRow, tglRow: chTglRow,
  I: chI, esc: chEsc, fmtN: chFmtN,
  BGS: CH_BGS, ICONS: CH_ICONS, NICHES: CH_NICHES, RIGHTS: CH_RIGHTS, FEE: CH_FEE,
  get CAT(){ return CH_CAT; },
  defaultRights: chDefaultRights
};

})();

/* ===== module: ui-chrome ===== */
/* ============================================================
   UI-CHROME · script.js — премиум-моушен хаба (перф-безопасно)
   1) Параллакс-слои хиро (глаз + текст) по указателю — только transform,
      rAF-троттлинг, отключён при prefers-reduced-motion и во время скролла.
   2) IntersectionObserver: хиро ушёл из вьюпорта → .uic-hero-off (CSS
      замораживает @keyframes хиро) — ноль работы вне экрана.
   Префикс uic*, self-init, всё в try/catch: одна ошибка не должна ронять ядро.
   ============================================================ */
(function uicHubMotion(){
  'use strict';
  try{
    var reduce = false;
    try{ reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion:reduce)').matches; }catch(e){}

    function uicInit(){
      var hero = document.getElementById('maHero');
      if(!hero) return;

      /* ---- IntersectionObserver: пауза анимаций хиро вне вьюпорта ---- */
      try{
        if('IntersectionObserver' in window){
          var io = new IntersectionObserver(function(entries){
            for(var i=0;i<entries.length;i++){
              var vis = entries[i].isIntersecting && entries[i].intersectionRatio > 0.02;
              hero.classList.toggle('uic-hero-off', !vis);
            }
          }, {threshold:[0,0.03]});
          io.observe(hero);
        }
      }catch(e){}

      /* ---- Параллакс по указателю (пропускаем при reduced-motion) ---- */
      if(reduce) return;

      var logo = hero.querySelector('.ma-hero-logo');
      var txt  = hero.querySelector('.ma-hero-txt');
      if(!logo && !txt) return;

      var tx = 0, ty = 0, raf = 0;
      function uicApply(){
        raf = 0;
        // не двигаем во время активного скролла (перф-слой wow-motion/tg-webapp)
        if(document.documentElement.classList.contains('oko-scrolling')) return;
        // глаз и текст смещаются в противоположные стороны — ощущение глубины
        hero.style.setProperty('--uic-lx', (tx * 9).toFixed(2) + 'px');
        hero.style.setProperty('--uic-ly', (ty * 7).toFixed(2) + 'px');
        hero.style.setProperty('--uic-tx', (tx * -4).toFixed(2) + 'px');
        hero.style.setProperty('--uic-ty', (ty * -3).toFixed(2) + 'px');
      }
      function uicMove(ev){
        try{
          var r = hero.getBoundingClientRect();
          if(!r.width || !r.height) return;
          var px = (ev.clientX - r.left) / r.width;   // 0..1
          var py = (ev.clientY - r.top) / r.height;   // 0..1
          tx = Math.max(-1, Math.min(1, (px - 0.5) * 2));
          ty = Math.max(-1, Math.min(1, (py - 0.5) * 2));
          if(!raf) raf = requestAnimationFrame(uicApply);
        }catch(e){}
      }
      function uicReset(){
        tx = 0; ty = 0;
        try{
          hero.style.setProperty('--uic-lx','0px'); hero.style.setProperty('--uic-ly','0px');
          hero.style.setProperty('--uic-tx','0px'); hero.style.setProperty('--uic-ty','0px');
        }catch(e){}
      }
      hero.addEventListener('pointermove', uicMove, {passive:true});
      hero.addEventListener('pointerleave', uicReset, {passive:true});
      hero.addEventListener('pointercancel', uicReset, {passive:true});
    }

    if(document.readyState === 'loading'){
      document.addEventListener('DOMContentLoaded', uicInit, {once:true});
    } else {
      uicInit();
    }
  }catch(e){ /* тихо: перф-моушен не критичен */ }
})();

/* ============================================================
   UI-CHROME · script.js — класс-хук быстрого доступа поиска (ПК-полировка)
   Ставит .uic-qa на #searchView, когда запрос пуст (режим «Быстрый доступ»),
   чтобы CSS раскладывал карточную сетку. Аддитивно, ядро не патчим:
   слушаем перерисовки #searchBody (renderSearch меняет innerHTML при открытии
   и на каждый ввод) через MutationObserver — CSS-эффект только на ≥900px,
   на мобайле класс ни на что не влияет. Всё в try/catch, префикс uic*.
   ============================================================ */
(function uicSearchQaHook(){
  'use strict';
  try{
    function uicSyncQa(){
      try{
        var view = document.getElementById('searchView');
        if(!view) return;
        var inp = document.getElementById('gSearchInput');
        var empty = !inp || !((inp.value||'').trim());
        view.classList.toggle('uic-qa', empty);
      }catch(e){}
    }
    function uicBind(){
      try{
        var body = document.getElementById('searchBody');
        if(body && 'MutationObserver' in window){
          var mo = new MutationObserver(uicSyncQa);
          mo.observe(body, {childList:true});
        }
        // подстраховка: реагируем и на прямой ввод, и на первичное состояние
        var inp = document.getElementById('gSearchInput');
        if(inp) inp.addEventListener('input', uicSyncQa, {passive:true});
        uicSyncQa();
      }catch(e){}
    }
    if(document.readyState === 'loading'){
      document.addEventListener('DOMContentLoaded', uicBind, {once:true});
    } else {
      uicBind();
    }
  }catch(e){ /* тихо: полировка поиска не критична */ }
})();

/* ===== module: wow-eye ===== */
/* wow-eye ОТКЛЮЧЁН по правке Даниэля от 29.07.
   Ранее модуль создавал canvas-фолбэк «глаз с зрачком» который заменял
   собой мастер-логотип на экране входа. Теперь показываем мастер-PNG
   (oko-logo-master.png) без замен. Файл сохранён пустым stub'ом чтобы
   не ломать build.py (@MODULES:JS). */
(function(){ /* no-op */ })();

/* ===== module: wow-motion ===== */
/* ==========================================================================
   wow-motion — ненавязчивый моушен-движок OKO.
   - reveal-анимации при появлении (IntersectionObserver), СТРОГО один раз;
   - мягкий stagger внутри контейнеров;
   - реагирует на смену табов и динамический ре-рендер (MutationObserver);
   - уважает prefers-reduced-motion;
   - НЕ переопределяет функции ядра, работает только классами + IO.
   Всё завёрнуто в IIFE, префикс wm*, с null-проверками и try/catch.
   ========================================================================== */
(function(){
  'use strict';
  try{
    var root = document.documentElement;
    if(!root) return;

    // reduced-motion → не включаем моушен-слой вовсе (CSS reveal гейтится wm-on).
    var mqReduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)');
    if(mqReduce && mqReduce.matches) return;

    root.classList.add('wm-on');

    // Контентные поверхности, которые красиво «всплывают». Sheet-item намеренно
    // НЕ включён (bottom-sheet и так анимируется — исключаем риск застрять в opacity:0).
    var SEL = '.card,.stat,.plan,.svc,.chat-item,.prow,.post,.reel-card';

    var supportsIO = ('IntersectionObserver' in window);
    var io = null;
    if(supportsIO){
      io = new IntersectionObserver(function(entries){
        for(var i=0;i<entries.length;i++){
          var en = entries[i];
          if(en.isIntersecting){
            en.target.classList.add('wm-in');
            io.unobserve(en.target);
          }
        }
      }, { root:null, rootMargin:'0px 0px -6% 0px', threshold:0.08 });
    }

    // Пометить элемент для reveal (idempotent) + вычислить stagger-задержку.
    function wmTag(el){
      if(!el || el.__wm) return;
      el.__wm = 1;
      // Индекс среди уже помеченных прямых соседей → мягкий каскад (макс 6).
      var idx = 0, p = el.parentElement;
      if(p){
        try { idx = p.querySelectorAll(':scope > .wm-r').length; } catch(e){ idx = 0; }
      }
      if(idx > 6) idx = 6;
      el.style.setProperty('--wm-d', (idx * 45) + 'ms');
      el.classList.add('wm-r');
      if(io) io.observe(el);
      else el.classList.add('wm-in'); // деградация: просто показать
    }

    function wmScan(ctx){
      var nodes;
      try { nodes = (ctx || document).querySelectorAll(SEL); }
      catch(e){ return; }
      for(var i=0;i<nodes.length;i++) wmTag(nodes[i]);
    }

    // Ручной проход: показать уже-видимые (нужно, когда экран стал active —
    // IO иногда не триггерит на display:none→block без скролла).
    function wmRevealVisible(scope){
      var vh = window.innerHeight || 800;
      var nodes;
      try { nodes = (scope || document).querySelectorAll('.wm-r:not(.wm-in)'); }
      catch(e){ return; }
      for(var i=0;i<nodes.length;i++){
        var el = nodes[i];
        var r = el.getBoundingClientRect();
        if(r.bottom > 0 && r.top < vh * 0.96 && (r.width > 0 || r.height > 0)){
          el.classList.add('wm-in');
          if(io) io.unobserve(el);
        }
      }
    }

    // Дебаунс для реакции на мутации DOM (ре-рендеры ленты/чатов и т.п.).
    var scanTimer = 0;
    function wmScanSoon(){
      if(scanTimer) return;
      scanTimer = setTimeout(function(){
        scanTimer = 0;
        wmScan(document);
      }, 140);
    }

    // ========================================================================
    // COUNT-UP — крупные числовые статы «набегают» с 0 при первом появлении.
    // Строго один раз на элемент (IO + unobserve). rAF, ≤700 мс, easeOut.
    // БЕЗОПАСНО: в конце всегда возвращаем ТОЧНЫЙ исходный innerHTML — если
    // анимацию прервали или узел удалили, число НИКОГДА не останется частичным.
    // Живые/приложением-управляемые значения (id, data-*count, «0») пропускаем.
    // ========================================================================
    // Разбор форматированного числа: префикс(₽/$/+) + ядро(с пробелами/точкой) + суффикс(к/%/₽).
    function wmNumParse(raw){
      try{
        var s = ('' + raw).replace(/ /g, ' ').trim();
        if(!s) return null;
        var m = s.match(/^([^\d]*?)([+\-]?\d[\d .,]*\d|[+\-]?\d)(.*)$/);
        if(!m) return null;
        var prefix = m[1], core = m[2], suffix = m[3];
        if(/\d/.test(suffix)) return null;          // в суффиксе не должно быть цифр
        var sign = 1, c = core;
        if(c.charAt(0) === '+'){ c = c.slice(1); }
        else if(c.charAt(0) === '-'){ sign = -1; c = c.slice(1); }
        var dec = 0, decSep = '.';
        var dm = c.match(/([.,])(\d{1,2})$/);       // хвост вида .2 / ,5 = дробная часть
        if(dm){ decSep = dm[1]; dec = dm[2].length; }
        var digits = c.replace(/[ .,]/g, '');
        if(!/^\d+$/.test(digits)) return null;
        var intD = dec ? digits.slice(0, digits.length - dec) : digits;
        var decD = dec ? digits.slice(digits.length - dec) : '';
        var value = parseFloat((intD || '0') + (dec ? ('.' + decD) : '')) * sign;
        if(!isFinite(value)) return null;
        var thou = /\d \d/.test(core) ? ' ' : '';    // пробел-разделитель тысяч?
        return { prefix:prefix, suffix:suffix, value:value, dec:dec, decSep:decSep, thou:thou };
      }catch(e){ return null; }
    }
    // Собрать промежуточное значение обратно в тот же формат.
    function wmNumFmt(v, info){
      var neg = v < 0; v = Math.abs(v);
      var s = info.dec > 0 ? v.toFixed(info.dec) : String(Math.round(v));
      var p = s.split('.');
      var ip = p[0];
      if(info.thou) ip = ip.replace(/\B(?=(\d{3})+(?!\d))/g, info.thou);
      var body = ip + (p[1] !== undefined ? info.decSep + p[1] : '');
      return info.prefix + (neg ? '-' : '') + body + info.suffix;
    }
    // Запустить счётчик для конкретного элемента (уже отфильтрован как числовой).
    function wmNumRun(el){
      try{
        if(!el || !el.isConnected) return;
        var info = wmNumParse(el.textContent);
        if(!info || !isFinite(info.value) || info.value === 0) return;
        var orig = el.innerHTML;                    // точный исходник для доводки
        var target = info.value;
        var dur = Math.min(700, 340 + String(Math.round(Math.abs(target))).length * 60);
        var t0 = 0;
        function settle(){
          try{
            if(!el.isConnected) return;
            // Не затираем, если содержимое сменил кто-то другой (ре-рендер/тикер).
            if(el.textContent === el.__wmLast) el.innerHTML = orig;
          }catch(e){}
        }
        function step(ts){
          try{
            if(!el.isConnected) return;             // узел удалён — тихо выходим
            if(!t0) t0 = ts;
            var pr = (ts - t0) / dur; if(pr > 1) pr = 1;
            var e = 1 - Math.pow(1 - pr, 3);        // easeOutCubic
            var txt = wmNumFmt(target * e, info);
            el.textContent = txt; el.__wmLast = txt;
            if(pr < 1) requestAnimationFrame(step);
            else settle();
          }catch(err){ settle(); }
        }
        requestAnimationFrame(step);
      }catch(e){}
    }

    // Селекторы «явно числовых» героев-статов (иконочные/live отсекаются гардом ниже).
    var SELNUM = '.pay-sum,.stat .v,.pstat .v,.ch-stat .v,.pw-live-cell .v,.adm-kpi b,.gm-prz-stat b';
    var ioNum = null;
    if(supportsIO){
      ioNum = new IntersectionObserver(function(entries){
        for(var i=0;i<entries.length;i++){
          var en = entries[i];
          if(en.isIntersecting){ ioNum.unobserve(en.target); wmNumRun(en.target); }
        }
      }, { root:null, rootMargin:'0px 0px -8% 0px', threshold:0.5 });
    }
    // Гард: только листовой текст, без id и без «счётных» data-* (их анимирует ядро).
    function wmNumEligible(el){
      try{
        if(!el || el.__wmn) return false;
        if(el.childElementCount !== 0) return false;   // есть вложенные теги — не трогаем
        if(el.id) return false;                        // id → живое значение приложения
        var a = el.attributes;
        for(var i=0;i<a.length;i++){
          var n = a[i].name;
          if(n === 'data-to' || n === 'data-sk' || n === 'data-mpcount' ||
             n === 'data-fn' || n === 'data-count' || n === 'data-anim') return false;
        }
        return !!wmNumParse(el.textContent);
      }catch(e){ return false; }
    }
    function wmNumTag(el){
      if(!wmNumEligible(el)) return;
      el.__wmn = 1;
      if(ioNum) ioNum.observe(el);                     // без IO — просто не анимируем (безопасно)
    }
    function wmNumScan(ctx){
      var nodes;
      try { nodes = (ctx || document).querySelectorAll(SELNUM); }
      catch(e){ return; }
      for(var i=0;i<nodes.length;i++) wmNumTag(nodes[i]);
    }
    // Троттлинг: одно сканирование document на «пачку» кликов (~180 мс), чтобы
    // поймать попапы/шиты/паволлы. Уже помеченное отсекается мгновенно (__wmn).
    var numScanTimer = 0;
    function wmNumScanSoon(){
      if(numScanTimer) return;
      numScanTimer = setTimeout(function(){ numScanTimer = 0; wmNumScan(document); }, 180);
    }

    // Смена таба: даём экрану активироваться, помечаем новое и раскрываем видимое.
    function wmOnTabSwitch(){
      setTimeout(function(){
        var active = document.querySelector('main .screen.active') ||
                     document.querySelector('.screen.active');
        wmScan(active || document);
        wmRevealVisible(active || document);
        wmNumScan(active || document);      // count-up на новых числах экрана
      }, 60);
    }

    function wmInit(){
      wmScan(document);
      // Раскрыть то, что видно на старте (активный экран).
      wmRevealVisible(document);
      wmNumScan(document);                  // count-up на числах стартового экрана

      // Реакция на нижнюю навигацию (переключение табов).
      var tabs = document.getElementById('tabs');
      if(tabs) tabs.addEventListener('click', wmOnTabSwitch, true);

      // Тайлы мини-аппов / любые переходы, меняющие active-экран.
      document.addEventListener('click', function(ev){
        var t = ev.target;
        if(t && t.closest && (t.closest('.svc') || t.closest('[data-tab]') || t.closest('nav#tabs'))){
          wmOnTabSwitch();
        }
        // Любой клик мог открыть попап/шит/паволл с новыми числами — дадим им
        // отрисоваться и один раз пометим (событийно + троттлинг, НЕ polling/MO).
        wmNumScanSoon();
      }, true);

      // Глобальный MutationObserver УБРАН — он вызывал лаги на каждом ре-рендере.
      // Reveal переинициализируется при смене таба (wmOnTabSwitch) — этого достаточно,
      // и контент теперь всегда видим (opacity:1), так что «прятать» нечего.

      // Подстраховка: после полной загрузки ещё раз раскрыть видимое.
      window.addEventListener('load', function(){ wmScan(document); wmRevealVisible(document); wmNumScan(document); }, { once:true });

      // ---- PERF-слой: пауза декоративных @keyframes во время скролла и в фоне ----
      // (главная борьба с «лагает при скролле» — освобождаем компоновщик).
      var scrollOff = 0;
      function onScroll(){
        if(!root.classList.contains('oko-scrolling')) root.classList.add('oko-scrolling');
        if(scrollOff) clearTimeout(scrollOff);
        scrollOff = setTimeout(function(){ scrollOff = 0; root.classList.remove('oko-scrolling'); }, 160);
      }
      // capture:true ловит скролл ЛЮБОГО внутреннего контейнера (main, .screen, списки).
      window.addEventListener('scroll', onScroll, { capture:true, passive:true });
      document.addEventListener('touchmove', onScroll, { capture:true, passive:true });

      document.addEventListener('visibilitychange', function(){
        if(document.hidden) root.classList.add('oko-hidden');
        else root.classList.remove('oko-hidden');
      });
    }

    if(document.readyState === 'loading'){
      document.addEventListener('DOMContentLoaded', wmInit, { once:true });
    } else {
      wmInit();
    }

  }catch(err){
    // Любой сбой моушен-слоя не должен ломать приложение и не должен прятать контент.
    try { document.documentElement.classList.remove('wm-on'); } catch(e){}
  }
})();

/* ==========================================================================
   wow-motion, слой 2: RIPPLE + SCROLL-SHADOW.
   Отдельная IIFE со своей защитой (reduced-motion гасит только визуал через CSS,
   а JS всё равно не мешает работать интерфейсу). Ничего в ядре не переопределяем,
   только слушаем события и добавляем классы/дочерние спаны.
   ========================================================================== */
(function(){
  'use strict';
  try{
    var doc = document;
    if(!doc || !doc.documentElement) return;

    var mqReduce2 = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)');
    var reduced = !!(mqReduce2 && mqReduce2.matches);

    // ----------------------------------------------------------------------
    // RIPPLE, лаймовая волна из точки клика на кнопках/чипах/тайлах/табах/строках.
    // При reduced-motion не добавляем спан вовсе (экономим DOM).
    // ----------------------------------------------------------------------
    var RIP_SEL = '.btn, .chip, .svc-ic, .sheet-item, .prow, nav#tabs>button, nav#tabs button';
    function wmMakeRipple(ev){
      try{
        if(reduced) return;
        var t = ev.target;
        if(!t || !t.closest) return;
        var host = t.closest(RIP_SEL);
        if(!host) return;
        // не плодим одновременных волн
        if(host.__wmRipTs && (Date.now() - host.__wmRipTs) < 80) return;
        host.__wmRipTs = Date.now();

        var rect = host.getBoundingClientRect();
        var px = (typeof ev.clientX === 'number') ? ev.clientX
              : (ev.touches && ev.touches[0] ? ev.touches[0].clientX : rect.left + rect.width/2);
        var py = (typeof ev.clientY === 'number') ? ev.clientY
              : (ev.touches && ev.touches[0] ? ev.touches[0].clientY : rect.top + rect.height/2);
        var x = px - rect.left;
        var y = py - rect.top;
        // масштаб волны подгоняем под самый дальний угол
        var maxDim = Math.max(rect.width, rect.height);
        var scale = Math.max(18, Math.min(42, Math.ceil(maxDim / 8)));

        var r = doc.createElement('span');
        r.className = 'wm-ripple';
        r.style.left = x + 'px';
        r.style.top  = y + 'px';
        r.style.setProperty('--wm-rip-max', scale);
        // не ломаем клики по внутренним элементам
        host.appendChild(r);
        setTimeout(function(){
          try{ if(r && r.parentNode) r.parentNode.removeChild(r); }catch(e){}
        }, 640);
      }catch(e){}
    }
    // pointerdown ловит и мышь и тач, ДО клика (волна успевает стартовать)
    doc.addEventListener('pointerdown', wmMakeRipple, { capture:true, passive:true });

    // ----------------------------------------------------------------------
    // SCROLL-SHADOW, тень под шапкой когда контент под ней прокручивается.
    // Основной скроллер приложения, <main>. Плюс sticky-поиск .st2-search
    // внутри своих скроллеров получает такую же подсветку.
    // ----------------------------------------------------------------------
    function wmApplyMainShadow(){
      try{
        var m = doc.querySelector('main');
        if(!m) return;
        if(m.scrollTop > 6) m.classList.add('wm-scrolled');
        else m.classList.remove('wm-scrolled');
      }catch(e){}
    }
    function wmApplyStickyShadow(scope){
      try{
        var stickies = (scope || doc).querySelectorAll('.st2-search');
        for(var i=0;i<stickies.length;i++){
          var s = stickies[i];
          // sticky сидит внутри своего скроллера, находим ближайший scroller
          var p = s.parentElement, hit = null;
          while(p){
            var ov = getComputedStyle(p).overflowY;
            if(ov === 'auto' || ov === 'scroll'){ hit = p; break; }
            p = p.parentElement;
          }
          if(!hit) continue;
          if(hit.scrollTop > 6) s.classList.add('wm-scrolled');
          else s.classList.remove('wm-scrolled');
        }
      }catch(e){}
    }
    function wmOnAnyScroll(ev){
      // main-скролл ставим по любому событию скролла (capture ловит и внутренние)
      wmApplyMainShadow();
      var tgt = ev && ev.target;
      if(tgt && tgt.querySelectorAll) wmApplyStickyShadow(tgt);
      else wmApplyStickyShadow(doc);
    }
    // capture:true ловит скролл main и любых внутренних контейнеров
    window.addEventListener('scroll', wmOnAnyScroll, { capture:true, passive:true });

    // Первичный прогон + после смены таба (нужен для нового активного экрана)
    function wmShadowInit(){
      wmApplyMainShadow();
      wmApplyStickyShadow(doc);
    }
    if(doc.readyState === 'loading'){
      doc.addEventListener('DOMContentLoaded', wmShadowInit, { once:true });
    } else {
      wmShadowInit();
    }
    // Клик по табам может сбросить скролл нового экрана, обновим индикатор
    var tabsEl = doc.getElementById('tabs');
    if(tabsEl){
      tabsEl.addEventListener('click', function(){
        setTimeout(wmShadowInit, 80);
      }, true);
    }

  }catch(err){ /* тихо, не ломаем приложение */ }
})();

/* ==========================================================================
   wow-motion, слой 3 — кинематографические микро-анимации уровня Apple/Instagram.
   • Splash-shot (SVG-глаз + halo) при первой загрузке приложения
   • Tab-switch — направленный slide-in нового экрана + слайд-fade клона старого
   • Long-press ripple — большая лаймовая волна на удержании .btn
   • okoWow.showCheck(text) — оверлей с рисующейся галочкой + backdrop-blur
   • okoWow.shake(el) — тряска ошибки
   • okoWow.confetti(x,y) — 40 лаймовых квадратов с физикой падения
   • Skeleton .wow-skel — CSS-класс (в style.css)
   • Number count-up на .wow-count[data-to]
   • Page-parallax на .wow-parallax
   • Haptic-hint scale(.97→1) на .btn/.svc/.chip
   Публичный API: window.okoWow.{showCheck,shake,confetti,ripple,init,pop}.
   ========================================================================== */
(function(){
  'use strict';
  try{
    if(typeof document==='undefined'||typeof window==='undefined') return;
    var doc=document, root=doc.documentElement, win=window;
    if(!root) return;

    var mqR = win.matchMedia && win.matchMedia('(prefers-reduced-motion: reduce)');
    var REDUCED = !!(mqR && mqR.matches);

    var LIME = '#9AFF00';

    // Уже собранный API — не даём случайно перетереть.
    var api = win.okoWow = win.okoWow || {};

    // ------------------------------------------------------------------
    // 1) SPLASH-SHOT
    // ------------------------------------------------------------------
    function buildSplash(){
      if(REDUCED) return null;
      if(doc.getElementById('wm-splash')) return doc.getElementById('wm-splash');
      var el = doc.createElement('div');
      el.id = 'wm-splash';
      el.setAttribute('aria-hidden','true');
      el.innerHTML =
        '<div id="wm-splash-halo"></div>'+
        '<svg id="wm-splash-eye" viewBox="0 0 200 100" xmlns="http://www.w3.org/2000/svg">'+
          '<defs>'+
            '<clipPath id="wm-eye-clip"><ellipse cx="100" cy="50" rx="90" ry="45"/></clipPath>'+
          '</defs>'+
          '<g clip-path="url(#wm-eye-clip)">'+
            '<circle class="wm-splash-iris" cx="100" cy="50" r="26" fill="'+LIME+'"/>'+
            '<circle cx="100" cy="50" r="10" fill="#0d0d0d"/>'+
            '<circle cx="108" cy="42" r="4" fill="#fff" opacity="0.85"/>'+
          '</g>'+
          '<ellipse cx="100" cy="50" rx="90" ry="45" fill="none" stroke="'+LIME+'" stroke-width="3"/>'+
          '<rect class="wm-splash-lid wm-splash-lid-top" x="-10" y="-2" width="220" height="52" fill="#000"/>'+
          '<rect class="wm-splash-lid wm-splash-lid-bot" x="-10" y="50" width="220" height="52" fill="#000"/>'+
        '</svg>'+
        '<div class="wm-splash-word">OKO</div>';
      (doc.body || doc.documentElement).appendChild(el);
      return el;
    }
    function dismissSplash(){
      var el = doc.getElementById('wm-splash');
      if(!el) return;
      el.classList.add('wm-gone');
      setTimeout(function(){
        try{ if(el.parentNode) el.parentNode.removeChild(el); }catch(e){}
      }, 520);
    }
    // Собираем и снимаем сплэш через oko:app-ready
    function initSplash(){
      if(REDUCED) return;
      // Вставить как можно раньше — до paint основного контента
      if(doc.body) buildSplash();
      else doc.addEventListener('DOMContentLoaded', buildSplash, { once:true });

      var dismissed = false;
      function once(){ if(dismissed) return; dismissed = true; dismissSplash(); }
      win.addEventListener('oko:app-ready', once, { once:true });

      // Автоматический emit через 400 мс после DOMContentLoaded
      function armAutoReady(){
        setTimeout(function(){
          try{ win.dispatchEvent(new Event('oko:app-ready')); }
          catch(e){
            // Старые движки без Event()
            var evt = doc.createEvent('Event'); evt.initEvent('oko:app-ready', false, false);
            win.dispatchEvent(evt);
          }
        }, 400);
      }
      if(doc.readyState === 'loading') doc.addEventListener('DOMContentLoaded', armAutoReady, { once:true });
      else armAutoReady();

      // Аварийный сброс, чтобы никогда не залипнуть больше 2.5с
      setTimeout(once, 2500);
    }

    // ------------------------------------------------------------------
    // 2) TAB-SWITCH — направленный slide-in + клон-оверлей уходящего экрана.
    // ------------------------------------------------------------------
    var _lastTabIdx = -1;
    var _pendingDir = 'right';   // куда двигать входящий экран
    var _tabsOrder = [];

    function refreshTabsOrder(){
      var tabs = doc.querySelectorAll('nav#tabs>button');
      _tabsOrder = [];
      for(var i=0;i<tabs.length;i++){
        var b = tabs[i];
        var t = b.getAttribute('data-tab') || b.getAttribute('data-screen') || b.id || ('t'+i);
        _tabsOrder.push({ el:b, key:t });
      }
    }
    function idxOfTab(btn){
      for(var i=0;i<_tabsOrder.length;i++) if(_tabsOrder[i].el === btn) return i;
      return -1;
    }
    function snapshotActiveScreen(){
      /* ОТКЛЮЧЕНО 09.08 по правке Даниэля: «при клике меню все страницы
         обновляются, дёргаются, меню поднимается и лагает».
         Здесь клонировался ВЕСЬ активный экран в фиксированный оверлей и
         500 мс жил поверх нового экрана. На телефоне это давало ровно то,
         что видно на скринах: текст и кнопки нарисованы дважды со сдвигом.
         Плюс cloneNode сотен узлов на каждый тап — источник подтормаживания.
         Переход теперь один короткий фейд, без клонов и без слайдов. */
      return;
      /* eslint-disable no-unreachable */
      if(REDUCED) return;
      var main = doc.querySelector('main');
      if(!main) return;
      var active = main.querySelector('.screen.active');
      if(!active) return;
      // Пропускаем гигантские экраны — cloneNode будет дорогим
      var count = active.querySelectorAll('*').length;
      if(count > 350) return;

      var mr = main.getBoundingClientRect();
      var overlay = doc.createElement('div');
      overlay.className = 'wm-tab-overlay';
      overlay.style.left   = mr.left + 'px';
      overlay.style.top    = mr.top  + 'px';
      overlay.style.width  = mr.width + 'px';
      overlay.style.height = mr.height + 'px';

      var clone;
      try{ clone = active.cloneNode(true); }catch(e){ return; }
      // Не хотим двойных обработчиков/id-конфликтов
      try{
        var ids = clone.querySelectorAll('[id]');
        for(var i=0;i<ids.length;i++) ids[i].removeAttribute('id');
        if(clone.id) clone.removeAttribute('id');
      }catch(e){}
      clone.classList.remove('active','wm-tab-in-right','wm-tab-in-left');
      clone.style.display = 'block';
      // Компенсация скролла main — чтобы клон встал ровно там же
      clone.style.transform = 'translate3d(0,'+(-main.scrollTop)+'px,0)';
      overlay.appendChild(clone);
      doc.body.appendChild(overlay);

      var dx = (_pendingDir === 'right') ? -8 : 8;
      try{
        overlay.animate([
          { opacity:1, transform:'translate3d(0,0,0)' },
          { opacity:0, transform:'translate3d('+dx+'%,0,0)' }
        ], { duration:150, easing:'cubic-bezier(.4,0,.2,1)', fill:'forwards' });
      }catch(e){
        overlay.style.transition = 'opacity .15s ease, transform .15s ease';
        overlay.style.opacity = '0';
        overlay.style.transform = 'translate3d('+dx+'%,0,0)';
      }
      setTimeout(function(){
        try{ if(overlay.parentNode) overlay.parentNode.removeChild(overlay); }catch(e){}
      }, 200);
    }

    function initTabSwitch(){
      refreshTabsOrder();
      var tabs = doc.getElementById('tabs');

      // Перехватываем клик по табу capture:true — до того как ядро сменит .active.
      // На основе индекса определяем direction для входящего экрана.
      if(tabs){
        tabs.addEventListener('pointerdown', function(ev){
          try{
            var t = ev.target && ev.target.closest && ev.target.closest('button');
            if(!t) return;
            if(!_tabsOrder.length) refreshTabsOrder();
            var newIdx = idxOfTab(t);
            if(newIdx < 0) return;
            if(_lastTabIdx < 0){
              // первый переход — узнаём текущий активный
              var active = tabs.querySelector('button.active');
              _lastTabIdx = idxOfTab(active);
            }
            _pendingDir = (newIdx >= _lastTabIdx) ? 'right' : 'left';
            snapshotActiveScreen();
          }catch(e){}
        }, true);

        tabs.addEventListener('click', function(ev){
          try{
            var t = ev.target && ev.target.closest && ev.target.closest('button');
            if(!t) return;
            var idx = idxOfTab(t);
            if(idx >= 0) _lastTabIdx = idx;
          }catch(e){}
        }, true);
      }

      // MutationObserver на main для отслеживания смены .screen.active — маркируем
      // новый активный экран правильным классом slide-in.
      var main = doc.querySelector('main');
      if(!main || REDUCED) return;
      var mo = new MutationObserver(function(muts){
        for(var i=0;i<muts.length;i++){
          var m = muts[i];
          if(m.type !== 'attributes' || m.attributeName !== 'class') continue;
          var el = m.target;
          if(!el || !el.classList || !el.classList.contains('screen')) continue;
          if(!el.classList.contains('active')) continue;
          // Не дублировать пока анимация идёт
          if(el.classList.contains('wm-tab-in-right') || el.classList.contains('wm-tab-in-left')) continue;
          var cls = (_pendingDir === 'right') ? 'wm-tab-in-right' : 'wm-tab-in-left';
          el.classList.add(cls);
          (function(node, klass){
            var clear = function(){
              try{ node.classList.remove(klass); }catch(e){}
              try{ node.removeEventListener('animationend', clear); }catch(e){}
            };
            node.addEventListener('animationend', clear);
            setTimeout(clear, 380);
          })(el, cls);
        }
      });
      try{
        mo.observe(main, { subtree:true, attributes:true, attributeFilter:['class'] });
      }catch(e){}
    }

    // ------------------------------------------------------------------
    // 3) LONG-PRESS RIPPLE — большая волна при удержании .btn
    // ------------------------------------------------------------------
    var LP_SEL = '.btn';
    var LP_HOLD_MS = 320;

    function initLongPress(){
      if(REDUCED) return;
      var lpTimer = 0, lpHost = null, lpX = 0, lpY = 0;
      function cancel(){
        if(lpTimer){ clearTimeout(lpTimer); lpTimer = 0; }
        lpHost = null;
      }
      doc.addEventListener('pointerdown', function(ev){
        try{
          var t = ev.target;
          if(!t || !t.closest) return;
          var host = t.closest(LP_SEL);
          if(!host) return;
          lpHost = host;
          var rect = host.getBoundingClientRect();
          var px = (typeof ev.clientX === 'number') ? ev.clientX : rect.left + rect.width/2;
          var py = (typeof ev.clientY === 'number') ? ev.clientY : rect.top + rect.height/2;
          lpX = px - rect.left;
          lpY = py - rect.top;
          if(lpTimer) clearTimeout(lpTimer);
          lpTimer = setTimeout(function(){
            try{
              if(!lpHost || !lpHost.isConnected) return;
              var r = doc.createElement('span');
              r.className = 'wm-lpress';
              r.style.left = lpX + 'px';
              r.style.top  = lpY + 'px';
              lpHost.appendChild(r);
              setTimeout(function(){ try{ if(r.parentNode) r.parentNode.removeChild(r); }catch(e){} }, 780);
            }catch(e){}
            lpTimer = 0;
          }, LP_HOLD_MS);
        }catch(e){}
      }, { capture:true, passive:true });
      doc.addEventListener('pointerup',     cancel, { capture:true, passive:true });
      doc.addEventListener('pointercancel', cancel, { capture:true, passive:true });
      doc.addEventListener('pointerleave',  cancel, { capture:true, passive:true });
      // Скролл убивает намерение удержания
      win.addEventListener('scroll', cancel, { capture:true, passive:true });
    }

    // ------------------------------------------------------------------
    // 4) okoWow.showCheck(text)
    // ------------------------------------------------------------------
    api.showCheck = function(text){
      try{
        // Убрать предыдущий, если ещё жив
        var prev = doc.getElementById('wm-check');
        if(prev && prev.parentNode) prev.parentNode.removeChild(prev);

        var el = doc.createElement('div');
        el.id = 'wm-check';
        el.setAttribute('role','status');
        el.setAttribute('aria-live','polite');
        var label = (typeof text === 'string' && text) ? text : 'Готово';
        el.innerHTML =
          '<div class="wm-check-box">'+
            '<svg class="wm-check-svg" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">'+
              '<circle class="wm-check-circle" cx="50" cy="50" r="40"/>'+
              '<path class="wm-check-tick" d="M28 52 L44 68 L74 36"/>'+
            '</svg>'+
            '<div class="wm-check-text"></div>'+
          '</div>';
        var textNode = el.querySelector('.wm-check-text');
        if(textNode) textNode.textContent = label;
        doc.body.appendChild(el);
        // force reflow → плавный fade-in
        void el.offsetWidth;
        el.classList.add('wm-show');

        setTimeout(function(){
          try{ el.classList.remove('wm-show'); }catch(e){}
          setTimeout(function(){ try{ if(el.parentNode) el.parentNode.removeChild(el); }catch(e){} }, 240);
        }, 1200);
        return el;
      }catch(e){ return null; }
    };

    // ------------------------------------------------------------------
    // 5) okoWow.shake(el) — тряска ошибки
    // ------------------------------------------------------------------
    api.shake = function(el){
      try{
        if(!el || !el.classList) return;
        el.classList.remove('wm-shake');
        // reflow чтобы анимация перезапустилась при повторном вызове
        void el.offsetWidth;
        el.classList.add('wm-shake');
        var clear = function(){
          try{ el.classList.remove('wm-shake'); }catch(e){}
          try{ el.removeEventListener('animationend', clear); }catch(e){}
        };
        el.addEventListener('animationend', clear);
        setTimeout(clear, 320);
      }catch(e){}
    };

    // ------------------------------------------------------------------
    // 6) okoWow.confetti(x, y) — 40 лаймовых квадратов из точки клика
    // ------------------------------------------------------------------
    var _confCanvas = null, _confCtx = null, _confParts = [], _confRAF = 0;

    function ensureConfCanvas(){
      if(_confCanvas && _confCanvas.isConnected) return _confCanvas;
      _confCanvas = doc.createElement('canvas');
      _confCanvas.id = 'wm-confetti-canvas';
      doc.body.appendChild(_confCanvas);
      resizeConfCanvas();
      _confCtx = _confCanvas.getContext('2d');
      return _confCanvas;
    }
    function resizeConfCanvas(){
      if(!_confCanvas) return;
      var dpr = Math.min(win.devicePixelRatio || 1, 2);
      var w = win.innerWidth, h = win.innerHeight;
      _confCanvas.width  = Math.round(w * dpr);
      _confCanvas.height = Math.round(h * dpr);
      _confCanvas.style.width  = w + 'px';
      _confCanvas.style.height = h + 'px';
      if(_confCtx) _confCtx.setTransform(dpr,0,0,dpr,0,0);
    }
    win.addEventListener('resize', resizeConfCanvas, { passive:true });

    function confStep(){
      if(!_confCtx || !_confCanvas){ _confRAF = 0; return; }
      _confCtx.clearRect(0, 0, _confCanvas.width, _confCanvas.height);
      var alive = 0;
      var dt = 1;  // условная единица; физика упрощённая
      for(var i=0;i<_confParts.length;i++){
        var p = _confParts[i];
        if(p.life <= 0) continue;
        alive++;
        p.vy += 0.45 * dt;     // гравитация
        p.vx *= 0.995;         // сопротивление
        p.x  += p.vx;
        p.y  += p.vy;
        p.rot += p.vrot;
        p.life -= dt;
        var a = Math.max(0, Math.min(1, p.life / p.lifeMax));
        _confCtx.save();
        _confCtx.translate(p.x, p.y);
        _confCtx.rotate(p.rot);
        _confCtx.globalAlpha = a;
        _confCtx.fillStyle = p.color;
        _confCtx.fillRect(-p.size/2, -p.size/2, p.size, p.size * (0.35 + 0.65 * Math.abs(Math.sin(p.rot))));
        _confCtx.restore();
      }
      if(alive > 0){
        _confRAF = requestAnimationFrame(confStep);
      }else{
        _confRAF = 0;
        try{
          if(_confCanvas && _confCanvas.parentNode) _confCanvas.parentNode.removeChild(_confCanvas);
        }catch(e){}
        _confCanvas = null; _confCtx = null; _confParts = [];
      }
    }

    api.confetti = function(x, y, opts){
      try{
        if(REDUCED) return;
        opts = opts || {};
        var count = opts.count || 40;
        var cx = (typeof x === 'number') ? x : win.innerWidth / 2;
        var cy = (typeof y === 'number') ? y : win.innerHeight / 2;
        ensureConfCanvas();
        // Палитра — лайм с лёгкими вариациями (яркий/тёмный/белый акцент)
        var palette = [LIME, '#B8FF3D', '#7ACC00', '#DFFF9A', '#FFFFFF'];
        for(var i=0;i<count;i++){
          var ang = -Math.PI/2 + (Math.random() - 0.5) * Math.PI * 1.2; // веерный выхлоп вверх
          var speed = 6 + Math.random() * 10;
          _confParts.push({
            x: cx, y: cy,
            vx: Math.cos(ang) * speed + (Math.random()-0.5) * 2,
            vy: Math.sin(ang) * speed,
            rot: Math.random() * Math.PI,
            vrot: (Math.random() - 0.5) * 0.4,
            size: 6 + Math.random() * 6,
            color: palette[(Math.random() * palette.length) | 0],
            life: 72 + Math.random() * 30,       // ~1.2s @ 60fps
            lifeMax: 100
          });
        }
        if(!_confRAF) _confRAF = requestAnimationFrame(confStep);
      }catch(e){}
    };

    // ------------------------------------------------------------------
    // 7) okoWow.ripple(x, y, el) — программный ripple (короткая волна)
    // ------------------------------------------------------------------
    api.ripple = function(x, y, host){
      try{
        if(REDUCED) return;
        if(!host) return;
        var rect = host.getBoundingClientRect();
        var lx = (typeof x === 'number' ? x : rect.left + rect.width/2) - rect.left;
        var ly = (typeof y === 'number' ? y : rect.top  + rect.height/2) - rect.top;
        // Гарантируем контекст стакинга/оверфлоу как у других ripple-хостов
        var cs = getComputedStyle(host);
        if(cs.position === 'static') host.style.position = 'relative';
        if(cs.overflow === 'visible') host.style.overflow = 'hidden';
        var r = doc.createElement('span');
        r.className = 'wm-ripple';
        r.style.left = lx + 'px';
        r.style.top  = ly + 'px';
        host.appendChild(r);
        setTimeout(function(){ try{ if(r.parentNode) r.parentNode.removeChild(r); }catch(e){} }, 640);
      }catch(e){}
    };

    // ------------------------------------------------------------------
    // 8) NUMBER COUNT-UP — .wow-count[data-to="1234"]
    // ------------------------------------------------------------------
    var _wcIO = null;
    function fmtNum(n, dec, thousand){
      var neg = n < 0; n = Math.abs(n);
      var s = (dec > 0) ? n.toFixed(dec) : String(Math.round(n));
      var parts = s.split('.');
      var ip = parts[0];
      if(thousand) ip = ip.replace(/\B(?=(\d{3})+(?!\d))/g, thousand);
      return (neg ? '-' : '') + ip + (parts[1] !== undefined ? '.' + parts[1] : '');
    }
    function runCount(el){
      try{
        if(!el || !el.isConnected || el.__wcDone) return;
        el.__wcDone = 1;
        var raw = el.getAttribute('data-to');
        if(raw == null) return;
        var target = parseFloat(String(raw).replace(/[ ,]/g,''));
        if(!isFinite(target)) return;
        var dec = 0;
        var m = String(raw).match(/[.,](\d+)$/);
        if(m) dec = m[1].length;
        var thou = el.getAttribute('data-thousand');
        if(thou == null) thou = (/\d,\d/.test(raw)) ? ',' : (/\d \d/.test(raw)) ? ' ' : '';
        var prefix = el.getAttribute('data-prefix') || '';
        var suffix = el.getAttribute('data-suffix') || '';
        var dur = parseInt(el.getAttribute('data-dur'), 10) || 1500;
        var t0 = 0;
        function step(ts){
          try{
            if(!el.isConnected) return;
            if(!t0) t0 = ts;
            var pr = (ts - t0) / dur; if(pr > 1) pr = 1;
            var e = 1 - Math.pow(1 - pr, 3);  // easeOutCubic
            el.textContent = prefix + fmtNum(target * e, dec, thou) + suffix;
            if(pr < 1) requestAnimationFrame(step);
            else el.textContent = prefix + fmtNum(target, dec, thou) + suffix;
          }catch(e){}
        }
        requestAnimationFrame(step);
      }catch(e){}
    }
    function scanWowCount(scope){
      try{
        if(!('IntersectionObserver' in win)) return;
        if(!_wcIO){
          _wcIO = new IntersectionObserver(function(entries){
            for(var i=0;i<entries.length;i++){
              var en = entries[i];
              if(en.isIntersecting){ _wcIO.unobserve(en.target); runCount(en.target); }
            }
          }, { root:null, rootMargin:'0px 0px -8% 0px', threshold:0.3 });
        }
        var nodes = (scope || doc).querySelectorAll('.wow-count[data-to]');
        for(var i=0;i<nodes.length;i++){
          var el = nodes[i];
          if(el.__wcTagged) continue;
          el.__wcTagged = 1;
          // Начальное «0» — чтобы не мелькать финальным значением
          try{
            var prefix = el.getAttribute('data-prefix') || '';
            var suffix = el.getAttribute('data-suffix') || '';
            var dec = 0;
            var raw = el.getAttribute('data-to') || '';
            var mm = String(raw).match(/[.,](\d+)$/);
            if(mm) dec = mm[1].length;
            if(!el.textContent || el.textContent.trim() === '' || el.textContent.trim() === '0'){
              el.textContent = prefix + (dec ? (0).toFixed(dec) : '0') + suffix;
            }
          }catch(e){}
          _wcIO.observe(el);
        }
      }catch(e){}
    }

    // ------------------------------------------------------------------
    // 9) PAGE PARALLAX — .wow-parallax на main.scroll
    // ------------------------------------------------------------------
    function initParallax(){
      if(REDUCED) return;
      var main = doc.querySelector('main');
      if(!main) return;
      var els = [];
      function rescan(){
        try{ els = Array.prototype.slice.call(doc.querySelectorAll('.wow-parallax')); }catch(e){ els = []; }
      }
      rescan();
      var rafPending = false, lastTop = 0;
      function onScroll(){
        if(rafPending) return;
        rafPending = true;
        requestAnimationFrame(function(){
          rafPending = false;
          var top = main.scrollTop;
          if(top === lastTop) return;
          lastTop = top;
          for(var i=0;i<els.length;i++){
            var el = els[i];
            if(!el.isConnected) continue;
            var speed = parseFloat(el.getAttribute('data-speed'));
            if(!isFinite(speed)) speed = 0.35;
            // Отрицательный знак — фон уезжает медленнее, эффект глубины
            var y = -(top * speed);
            el.style.setProperty('--wm-par-y', y.toFixed(2) + 'px');
          }
        });
      }
      main.addEventListener('scroll', onScroll, { passive:true });
      // Смена таба — пересканировать
      var tabs = doc.getElementById('tabs');
      if(tabs) tabs.addEventListener('click', function(){ setTimeout(rescan, 120); }, true);
    }

    // ------------------------------------------------------------------
    // 10) HAPTIC-HINT — короткий scale(.97→1) на .btn/.svc/.chip
    // ------------------------------------------------------------------
    var HP_SEL = '.btn, .svc, .chip';
    function initHaptic(){
      if(REDUCED) return;
      api.pop = function(el){
        try{
          if(!el || !el.classList) return;
          el.classList.remove('wm-pop');
          void el.offsetWidth;
          el.classList.add('wm-pop');
          var clear = function(){
            try{ el.classList.remove('wm-pop'); }catch(e){}
            try{ el.removeEventListener('animationend', clear); }catch(e){}
          };
          el.addEventListener('animationend', clear);
          setTimeout(clear, 140);
        }catch(e){}
      };
      doc.addEventListener('pointerdown', function(ev){
        try{
          var t = ev.target;
          if(!t || !t.closest) return;
          var host = t.closest(HP_SEL);
          if(!host) return;
          api.pop(host);
        }catch(e){}
      }, { capture:true, passive:true });
    }

    // ------------------------------------------------------------------
    // INIT — идемпотентный, вызывается один раз
    // ------------------------------------------------------------------
    var _inited = false;
    api.init = function(){
      if(_inited) return;
      _inited = true;
      initSplash();
      var ready = function(){
        try{ initTabSwitch(); }catch(e){}
        try{ initLongPress(); }catch(e){}
        try{ initParallax(); }catch(e){}
        try{ initHaptic(); }catch(e){}
        try{ scanWowCount(doc); }catch(e){}
      };
      if(doc.readyState === 'loading') doc.addEventListener('DOMContentLoaded', ready, { once:true });
      else ready();

      // Пере-сканирование чисел при смене таба (появляются новые .wow-count)
      var tabs = doc.getElementById('tabs');
      if(tabs) tabs.addEventListener('click', function(){ setTimeout(function(){ scanWowCount(doc); }, 120); }, true);

      // Публичный хук для повторного скана — если приложение динамически подгрузило блоки
      api.rescan = function(){ try{ scanWowCount(doc); }catch(e){} };
    };

    // Автостарт
    api.init();

  }catch(err){ /* тихо, ничего не ломаем */ }
})();

/* ===== module: viral ===== */
/* =================== VIRAL ===================
   Виральные механики шеринга уровня Duolingo (streak-share) + Notion (share doc)
   + LinkedIn (share achievement). Всё под префиксом vr-/viral*.
   Ничего в base.html не трогается — только патчи и подписки на существующие
   функции/классы Академии, Партнёрки, Игр и ленты.

   Публичный API (глобальный):
     viralOpenShare(type, data)     — универсальный share-sheet
     viralGenReelsQuote(lesson)     — «Reels из урока за 30 сек»
     viralOpenWeekly()              — Weekly Story-summary
     viralOpenPublicProfile(slug)   — публичный профиль /u/@slug
     viralCanvas.*                  — helper-функции рендера в canvas
*/

(function viralModule(){
  'use strict';

  /* ============================================================
     ИКОНКИ (svg symbol'ы, только SVG, никаких эмодзи)
     ============================================================ */
  (function icons(){
    const defs = document.querySelector('svg defs');
    if(!defs) return;
    function sym(id, vb, inner){
      if(document.getElementById(id)) return;
      const s = document.createElementNS('http://www.w3.org/2000/svg', 'symbol');
      s.setAttribute('id', id); s.setAttribute('viewBox', vb);
      s.innerHTML = inner; defs.appendChild(s);
    }
    sym('i-vr-ig', '0 0 100 100',
      '<rect x="14" y="14" width="72" height="72" rx="20" fill="none" stroke="currentColor" stroke-width="7"/>' +
      '<circle cx="50" cy="50" r="16" fill="none" stroke="currentColor" stroke-width="7"/>' +
      '<circle cx="72" cy="28" r="5" fill="currentColor"/>');
    sym('i-vr-tg', '0 0 100 100',
      '<path d="M88 14 14 44c-3 1-3 5 0 6l20 7c2 1 3 2 4 4l7 20c1 3 5 3 6 0L81 22c1-4-2-7-6-6z" fill="currentColor"/>');
    sym('i-vr-vk', '0 0 100 100',
      '<path d="M22 36c2 22 12 32 30 32h4v-14c2 0 4 2 8 6l8 8h10c-6-8-9-11-14-16-2-2-2-4 0-6 4-6 12-14 12-18h-10c-4 4-8 10-12 14-3 3-4 2-4-2V28h-4c-8 0-12 4-14 6 0 0 6 0 6 6v10c0 4-2 4-4 2-4-4-8-12-10-16z" fill="currentColor"/>');
    sym('i-vr-tt', '0 0 100 100',
      '<path d="M58 12v46c0 8-6 14-14 14s-14-6-14-14 6-14 14-14v10c-2 0-4 2-4 4s2 4 4 4 4-2 4-4V12h10c0 8 6 14 14 14v10c-6 0-11-2-14-4z" fill="currentColor"/>');
    sym('i-vr-wa', '0 0 100 100',
      '<path d="M50 8C26 8 8 26 8 50c0 8 2 14 6 20L8 92l24-6c6 3 12 4 18 4 24 0 42-18 42-42S74 8 50 8z" fill="none" stroke="currentColor" stroke-width="6" stroke-linejoin="round"/>' +
      '<path d="M38 34c0 10 8 22 22 26 3 1 6 1 7-3l-6-8c-3 1-6 3-8-1-2-3-2-6 0-8l-4-10c-4-2-11 0-11 4z" fill="currentColor"/>');
    sym('i-vr-story', '0 0 100 100',
      '<rect x="18" y="10" width="64" height="80" rx="10" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<circle cx="50" cy="50" r="14" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<path d="M38 50a12 12 0 1 0 24 0" fill="currentColor"/>');
    sym('i-vr-crown', '0 0 100 100',
      '<path d="M10 34l16 28 24-36 24 36 16-28-8 46H18z" fill="currentColor"/>');
    sym('i-vr-fire', '0 0 100 100',
      '<path d="M50 10c8 20 30 24 30 46 0 18-14 34-30 34S20 74 20 56C20 42 32 34 38 20c4 8 8 12 12 22 4-8 4-20 0-32z" fill="currentColor"/>');
    sym('i-vr-qr', '0 0 100 100',
      '<rect x="10" y="10" width="26" height="26" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<rect x="18" y="18" width="10" height="10" fill="currentColor"/>' +
      '<rect x="64" y="10" width="26" height="26" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<rect x="72" y="18" width="10" height="10" fill="currentColor"/>' +
      '<rect x="10" y="64" width="26" height="26" fill="none" stroke="currentColor" stroke-width="6"/>' +
      '<rect x="18" y="72" width="10" height="10" fill="currentColor"/>' +
      '<rect x="46" y="46" width="14" height="6" fill="currentColor"/>' +
      '<rect x="60" y="60" width="14" height="14" fill="none" stroke="currentColor" stroke-width="6"/>');
    sym('i-vr-badge', '0 0 100 100',
      '<path d="M50 10 62 22 78 22 78 38 92 50 78 62 78 78 62 78 50 90 38 78 22 78 22 62 8 50 22 38 22 22 38 22Z" fill="currentColor"/>' +
      '<path d="M34 52 44 62 66 40" fill="none" stroke="var(--surface, #000)" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/>');
  })();

  /* ============================================================
     0. КОНСТАНТЫ и УТИЛИТЫ
     ============================================================ */
  const BRAND = { bg:'#0b0d0e', ink:'#ffffff', dim:'#9aa0a5', lime:'#9AFF00' };
  const HOST  = 'okoteam.top';       // публичный домен OKO
  const DEEP  = 'https://true-journey-418.higgsfield.app'; // зеркало прогресса

  function nick(){
    return (typeof PROFILE !== 'undefined' && PROFILE.nick) ? PROFILE.nick : 'me';
  }
  function myName(){
    return (typeof PROFILE !== 'undefined' && PROFILE.name) ? PROFILE.name : 'OKO';
  }
  function myBio(){
    return (typeof PROFILE !== 'undefined' && PROFILE.bio) ? PROFILE.bio : '';
  }
  function myTier(){
    return (typeof PROFILE !== 'undefined' && PROFILE.tier) ? PROFILE.tier : 'FREE';
  }
  function myAvatar(){
    return (typeof PROFILE !== 'undefined' && PROFILE.avatar) ? PROFILE.avatar : '';
  }
  function refUrl(product){
    const base = 'https://' + HOST + '/u/@' + nick();
    return product ? base + '?p=' + encodeURIComponent(product) : base;
  }
  function refText(){
    return refUrl() + '?ref=' + encodeURIComponent(nick());
  }
  function toastSafe(msg){
    if(typeof toast === 'function') toast(msg);
    else console.log('[toast]', msg);
  }
  function escapeHtml(s){
    return String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  }
  function initials(name){
    const parts = String(name || '').trim().split(/\s+/).slice(0, 2);
    return parts.map(p => (p[0] || '').toUpperCase()).join('') || 'O';
  }
  function shareStore(){
    try{ return JSON.parse(localStorage.getItem('vr.state') || '{}'); }catch(e){ return {}; }
  }
  function saveShareStore(s){
    try{ localStorage.setItem('vr.state', JSON.stringify(s)); }catch(e){}
  }
  /* ============================================================
     1. viralCanvas: helper-набор рендера картинок в canvas
        Все методы возвращают HTMLCanvasElement (или Promise<canvas>).
     ============================================================ */
  function makeCanvas(w, h){
    const c = document.createElement('canvas');
    // ретина: рисуем в 1x, шкалируем в CSS
    c.width = w; c.height = h;
    c.style.width = '100%'; c.style.height = 'auto';
    return c;
  }
  function loadImage(src){
    return new Promise((res, rej) => {
      const img = new Image();
      img.crossOrigin = 'anonymous';
      img.onload = () => res(img);
      img.onerror = () => rej(new Error('img load'));
      img.src = src;
    });
  }
  function bgPattern(ctx, w, h){
    // фирменный фон: чёрный + плавный лаймовый радиалг сверху
    const g = ctx.createLinearGradient(0, 0, 0, h);
    g.addColorStop(0, '#141719');
    g.addColorStop(1, '#050506');
    ctx.fillStyle = g; ctx.fillRect(0, 0, w, h);
    const r = ctx.createRadialGradient(w * 0.15, -60, 0, w * 0.15, -60, w * 0.85);
    r.addColorStop(0, 'rgba(154,255,0,0.28)');
    r.addColorStop(1, 'rgba(154,255,0,0)');
    ctx.fillStyle = r; ctx.fillRect(0, 0, w, h);
    // угловой акцент
    ctx.strokeStyle = 'rgba(154,255,0,0.35)';
    ctx.lineWidth = 8;
    ctx.beginPath();
    ctx.moveTo(0, h - 90); ctx.lineTo(160, h - 90);
    ctx.moveTo(90, h - 160); ctx.lineTo(90, h);
    ctx.stroke();
  }
  function drawLogo(ctx, x, y, size){
    // упрощённый OKO-глаз: чёрный круг + лаймовый зрачок с «выкусом»
    const r = size / 2;
    ctx.save();
    ctx.translate(x, y);
    ctx.fillStyle = '#000';
    ctx.beginPath(); ctx.arc(r, r, r, 0, Math.PI * 2); ctx.fill();
    ctx.fillStyle = BRAND.lime;
    ctx.beginPath(); ctx.arc(r, r, r * 0.58, 0, Math.PI * 2); ctx.fill();
    ctx.fillStyle = '#000';
    ctx.beginPath(); ctx.arc(r * 1.32, r * 0.62, r * 0.24, 0, Math.PI * 2); ctx.fill();
    ctx.restore();
  }
  function drawBrand(ctx, x, y, scale){
    scale = scale || 1;
    drawLogo(ctx, x, y, 42 * scale);
    ctx.fillStyle = '#fff';
    ctx.font = '700 ' + (28 * scale) + 'px "Bebas Neue", "Montserrat", system-ui, sans-serif';
    ctx.textBaseline = 'middle';
    ctx.fillText('OKO', x + 52 * scale, y + 21 * scale);
    ctx.fillStyle = BRAND.dim;
    ctx.font = '600 ' + (13 * scale) + 'px "Montserrat", system-ui, sans-serif';
    ctx.fillText('одно приложение вместо восьми', x + 52 * scale, y + 42 * scale);
  }
  function drawFooter(ctx, w, h, text){
    ctx.fillStyle = '#fff';
    ctx.font = '800 22px "Bebas Neue", "Montserrat", system-ui, sans-serif';
    ctx.textBaseline = 'middle'; ctx.textAlign = 'left';
    ctx.fillText(text, 60, h - 60);
    ctx.textAlign = 'right';
    ctx.fillStyle = BRAND.lime;
    ctx.fillText(HOST + '/u/@' + nick(), w - 60, h - 60);
    ctx.textAlign = 'left';
  }
  function wrapText(ctx, text, x, y, maxW, lineH){
    const words = String(text || '').split(/\s+/);
    let line = '', lines = 0;
    for(let n = 0; n < words.length; n++){
      const test = line ? (line + ' ' + words[n]) : words[n];
      if(ctx.measureText(test).width > maxW && line){
        ctx.fillText(line, x, y); line = words[n]; y += lineH; lines++;
      } else {
        line = test;
      }
    }
    if(line){ ctx.fillText(line, x, y); lines++; }
    return lines;
  }
  function drawQR(ctx, x, y, size, text){
    // компактный «эстетический» QR — детерминированный шум по seed(text)
    const N = 25, cell = size / N;
    let seed = 2166136261;
    for(let i = 0; i < text.length; i++){ seed ^= text.charCodeAt(i); seed = (seed * 16777619) >>> 0; }
    function rnd(){ seed ^= seed << 13; seed ^= seed >>> 17; seed ^= seed << 5; return ((seed >>> 0) % 1000) / 1000; }
    ctx.fillStyle = '#fff';
    ctx.fillRect(x, y, size, size);
    ctx.fillStyle = '#000';
    for(let iy = 0; iy < N; iy++) for(let ix = 0; ix < N; ix++){
      if(rnd() < 0.48) ctx.fillRect(x + ix * cell, y + iy * cell, cell, cell);
    }
    // маркеры-«глазки»
    function eye(cx, cy){
      ctx.fillStyle = '#fff';
      ctx.fillRect(x + cx * cell - 1, y + cy * cell - 1, 7 * cell + 2, 7 * cell + 2);
      ctx.fillStyle = '#000';
      ctx.fillRect(x + cx * cell, y + cy * cell, 7 * cell, 7 * cell);
      ctx.fillStyle = '#fff';
      ctx.fillRect(x + (cx + 1) * cell, y + (cy + 1) * cell, 5 * cell, 5 * cell);
      ctx.fillStyle = '#000';
      ctx.fillRect(x + (cx + 2) * cell, y + (cy + 2) * cell, 3 * cell, 3 * cell);
    }
    eye(0, 0); eye(N - 7, 0); eye(0, N - 7);
    // центральный «глаз» бренда
    const cx = x + size / 2, cy = y + size / 2;
    ctx.fillStyle = '#fff';
    ctx.beginPath(); ctx.arc(cx, cy, size * 0.13, 0, Math.PI * 2); ctx.fill();
    ctx.fillStyle = '#000';
    ctx.beginPath(); ctx.arc(cx, cy, size * 0.10, 0, Math.PI * 2); ctx.fill();
    ctx.fillStyle = BRAND.lime;
    ctx.beginPath(); ctx.arc(cx, cy, size * 0.05, 0, Math.PI * 2); ctx.fill();
  }

  const viralCanvas = window.viralCanvas = {

    /* Аватар: круг с инициалами (или картинкой, если она уже была загружена) */
    _drawAvatar(ctx, cx, cy, r, name, src){
      ctx.save();
      ctx.beginPath(); ctx.arc(cx, cy, r, 0, Math.PI * 2); ctx.closePath();
      ctx.fillStyle = '#111'; ctx.fill();
      ctx.strokeStyle = BRAND.lime; ctx.lineWidth = Math.max(3, r * 0.06); ctx.stroke();
      ctx.fillStyle = BRAND.lime;
      ctx.font = '700 ' + Math.round(r * 0.85) + 'px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
      ctx.fillText(initials(name), cx, cy + r * 0.05);
      ctx.restore();
    },

    /* Сертификат — 1080×1350 (TikTok-friendly 4:5) */
    certificate(data){
      const w = 1080, h = 1350;
      const c = makeCanvas(w, h); const ctx = c.getContext('2d');
      bgPattern(ctx, w, h);
      drawBrand(ctx, 60, 60, 1.1);
      // рамка
      ctx.strokeStyle = 'rgba(154,255,0,0.4)'; ctx.lineWidth = 3;
      ctx.strokeRect(60, 200, w - 120, h - 340);
      // название
      ctx.fillStyle = '#fff'; ctx.textAlign = 'center';
      ctx.font = '800 44px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('СЕРТИФИКАТ АКАДЕМИИ', w / 2, 300);
      ctx.font = '600 22px "Montserrat", system-ui, sans-serif';
      ctx.fillStyle = BRAND.dim;
      ctx.fillText(String(data.no || ''), w / 2, 340);
      // имя (крупно)
      ctx.fillStyle = BRAND.lime;
      ctx.font = '800 92px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText(String(data.name || myName()).toUpperCase(), w / 2, 500);
      // «Прошёл курс»
      ctx.fillStyle = '#fff';
      ctx.font = '600 26px "Montserrat", system-ui, sans-serif';
      ctx.fillText('прошёл(а) курс', w / 2, 570);
      // название курса (в несколько строк)
      ctx.font = '800 42px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.textAlign = 'center';
      wrapText(ctx, String(data.course || 'Академия OKO'), w / 2, 640, w - 200, 54);
      // дата + балл теста
      ctx.font = '600 24px "Montserrat", system-ui, sans-serif';
      ctx.fillStyle = BRAND.dim;
      ctx.fillText((data.date || '') + '  ·  тест ' + (data.score || 0) + '%', w / 2, 850);
      // серебряная печать
      const sx = 240, sy = 1010, sr = 100;
      const g = ctx.createRadialGradient(sx, sy, 10, sx, sy, sr);
      g.addColorStop(0, '#f4f4f4'); g.addColorStop(0.6, '#c9c9c9'); g.addColorStop(1, '#7f7f7f');
      ctx.fillStyle = g; ctx.beginPath(); ctx.arc(sx, sy, sr, 0, Math.PI * 2); ctx.fill();
      ctx.strokeStyle = BRAND.lime; ctx.lineWidth = 4;
      ctx.beginPath(); ctx.arc(sx, sy, sr - 6, 0, Math.PI * 2); ctx.stroke();
      ctx.fillStyle = '#111';
      ctx.font = '800 26px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.textBaseline = 'middle';
      ctx.fillText('OKO', sx, sy - 12);
      ctx.font = '700 13px "Montserrat", system-ui, sans-serif';
      ctx.fillText('ACADEMY', sx, sy + 14);
      // QR для верификации
      drawQR(ctx, w - 240 - 60, 940, 240,
        'https://' + HOST + '/verify/' + (data.no || 'OKO'));
      ctx.textAlign = 'center'; ctx.fillStyle = BRAND.dim;
      ctx.font = '600 16px "Montserrat", system-ui, sans-serif';
      ctx.fillText('верификация по QR', w - 240 - 60 + 120, 1210);
      // подпись
      ctx.textBaseline = 'alphabetic';
      drawFooter(ctx, w, h, 'ОФИЦИАЛЬНЫЙ ДИПЛОМ');
      return c;
    },

    /* Reels-цитата — 5 кадров 1080×1920 (превью в один канвас-коллаж) */
    lessonQuotes(lesson){
      const scenes = lesson.scenes || viral_buildScenes(lesson);
      const w = 1080, sh = 1920;
      // мини-превью — 5 кадров в столбик уменьшенные до 216×384
      const scale = 0.28;
      const cw = Math.round(w * scale) + 24, ch = Math.round(sh * scale) + 24;
      const total = scenes.length;
      const cols = Math.min(3, total);
      const rows = Math.ceil(total / cols);
      const gap = 12, padOuter = 12;
      const cw2 = cw + gap, ch2 = ch + gap;
      const bigW = padOuter * 2 + cols * cw2 - gap;
      const bigH = padOuter * 2 + rows * ch2 - gap;
      const c = makeCanvas(bigW, bigH); const ctx = c.getContext('2d');
      ctx.fillStyle = '#0b0d0e'; ctx.fillRect(0, 0, bigW, bigH);
      scenes.forEach((s, i) => {
        const gx = i % cols, gy = Math.floor(i / cols);
        const x = padOuter + gx * cw2, y = padOuter + gy * ch2;
        // тень
        ctx.fillStyle = 'rgba(0,0,0,.5)';
        ctx.fillRect(x + 4, y + 6, cw - 8, ch - 8);
        // кадр
        const mini = viralCanvas._reelFrame(s, w, sh);
        ctx.drawImage(mini, 0, 0, w, sh, x, y, cw, ch);
      });
      return c;
    },
    _reelFrame(scene, w, h){
      const c = makeCanvas(w, h); const ctx = c.getContext('2d');
      bgPattern(ctx, w, h);
      drawBrand(ctx, 60, 80, 1.6);
      if(scene.kind === 'cover'){
        ctx.textAlign = 'center';
        ctx.fillStyle = '#fff';
        ctx.font = '800 88px "Bebas Neue", "Montserrat", system-ui, sans-serif';
        wrapText(ctx, String(scene.title || 'Урок Академии OKO'), w / 2, h * 0.5 - 100, w - 160, 100);
        ctx.font = '600 34px "Montserrat", system-ui, sans-serif';
        ctx.fillStyle = BRAND.lime;
        ctx.fillText('Академия OKO', w / 2, h * 0.5 + 220);
      } else if(scene.kind === 'quote'){
        ctx.textAlign = 'left';
        ctx.fillStyle = BRAND.lime;
        ctx.font = '800 300px "Bebas Neue", "Montserrat", system-ui, sans-serif';
        ctx.fillText('“', 60, 420);
        ctx.fillStyle = '#fff';
        ctx.font = '700 54px "Bebas Neue", "Montserrat", system-ui, sans-serif';
        wrapText(ctx, scene.text || '', 60, 560, w - 120, 66);
        ctx.textAlign = 'right';
        ctx.font = '600 26px "Montserrat", system-ui, sans-serif';
        ctx.fillStyle = BRAND.dim;
        ctx.fillText('— ' + (scene.author || myName()), w - 60, h - 260);
      } else if(scene.kind === 'cta'){
        ctx.textAlign = 'center';
        ctx.fillStyle = '#fff';
        ctx.font = '800 92px "Bebas Neue", "Montserrat", system-ui, sans-serif';
        wrapText(ctx, 'Ищи меня в OKO', w / 2, h * 0.42, w - 200, 110);
        ctx.font = '700 44px "Montserrat", system-ui, sans-serif';
        ctx.fillStyle = BRAND.lime;
        ctx.fillText('@' + nick(), w / 2, h * 0.42 + 180);
        drawQR(ctx, (w - 320) / 2, h * 0.55, 320, refText());
      }
      drawFooter(ctx, w, h, 'АКАДЕМИЯ');
      return c;
    },

    /* Weekly Story — 1080×1920 */
    weekly(metrics){
      const w = 1080, h = 1920;
      const c = makeCanvas(w, h); const ctx = c.getContext('2d');
      bgPattern(ctx, w, h);
      drawBrand(ctx, 60, 80, 1.5);
      ctx.textAlign = 'left'; ctx.fillStyle = '#fff';
      ctx.font = '800 72px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('МОЯ НЕДЕЛЯ', 60, 350);
      ctx.fillStyle = BRAND.lime;
      ctx.font = '800 96px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('В OKO', 60, 460);
      // 4 метрики
      const mets = [
        {l:'XP заработано', v: (metrics.xp || 0) + ''},
        {l:'дней подряд',  v: (metrics.streak || 0) + ''},
        {l:'партнёрка',    v: (metrics.earned || 0) + ' ₽'},
        {l:'уроков', v: (metrics.lessons || 0) + ''}
      ];
      const bx = 60, by = 620, bw = w - 120, cellH = 220, gap = 20;
      mets.forEach((m, i) => {
        const gx = i % 2, gy = Math.floor(i / 2);
        const cellW = (bw - gap) / 2;
        const x = bx + gx * (cellW + gap), y = by + gy * (cellH + gap);
        ctx.fillStyle = 'rgba(255,255,255,.06)';
        ctx.strokeStyle = 'rgba(154,255,0,.35)'; ctx.lineWidth = 2;
        roundRect(ctx, x, y, cellW, cellH, 22, true, true);
        ctx.textAlign = 'left';
        ctx.fillStyle = BRAND.dim;
        ctx.font = '700 22px "Montserrat", system-ui, sans-serif';
        ctx.fillText(m.l.toUpperCase(), x + 26, y + 44);
        ctx.fillStyle = '#fff';
        ctx.font = '800 88px "Bebas Neue", "Montserrat", system-ui, sans-serif';
        ctx.fillText(m.v, x + 26, y + 150);
      });
      // любимый курс
      if(metrics.favCourse){
        ctx.fillStyle = 'rgba(154,255,0,0.1)';
        ctx.strokeStyle = BRAND.lime; ctx.lineWidth = 3;
        roundRect(ctx, 60, 1160, w - 120, 200, 22, true, true);
        ctx.fillStyle = BRAND.dim;
        ctx.font = '700 22px "Montserrat", system-ui, sans-serif';
        ctx.fillText('ЛЮБИМЫЙ КУРС НЕДЕЛИ', 86, 1205);
        ctx.fillStyle = '#fff';
        ctx.font = '800 52px "Bebas Neue", "Montserrat", system-ui, sans-serif';
        wrapText(ctx, metrics.favCourse, 86, 1275, w - 172, 60);
      }
      // CTA + QR
      ctx.textAlign = 'left';
      ctx.fillStyle = '#fff';
      ctx.font = '700 40px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('Забирай PRO по моей ссылке', 60, h - 480);
      drawQR(ctx, w - 300 - 60, h - 460, 300, refText());
      ctx.fillStyle = BRAND.lime;
      ctx.font = '700 32px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('okoteam.top/u/@' + nick(), 60, h - 380);
      drawFooter(ctx, w, h, 'НЕДЕЛЯ');
      return c;
    },

    /* Leaderboard share — 1080×1350 */
    leaderboard(row){
      const w = 1080, h = 1350;
      const c = makeCanvas(w, h); const ctx = c.getContext('2d');
      bgPattern(ctx, w, h);
      drawBrand(ctx, 60, 60, 1.1);
      ctx.textAlign = 'center';
      ctx.fillStyle = '#fff';
      ctx.font = '800 60px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('Я В ТОП-' + (row.rank || 1), w / 2, 300);
      ctx.fillStyle = BRAND.lime;
      ctx.font = '800 260px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('#' + (row.rank || 1), w / 2, 560);
      ctx.fillStyle = '#fff';
      ctx.font = '700 44px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('OKO ' + (row.league || 'GAMES').toUpperCase(), w / 2, 660);
      // моё имя
      ctx.fillStyle = BRAND.lime;
      ctx.font = '800 72px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('@' + nick(), w / 2, 800);
      ctx.fillStyle = BRAND.dim;
      ctx.font = '600 32px "Montserrat", system-ui, sans-serif';
      ctx.fillText((row.score ? row.score + ' очков · ' : '') + 'неделя', w / 2, 860);
      // «присоединяйся»
      ctx.fillStyle = '#fff';
      ctx.font = '700 40px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('присоединяйся', w / 2, 1000);
      drawQR(ctx, (w - 260) / 2, 1040, 260, refText());
      drawFooter(ctx, w, h, 'GAMES');
      return c;
    },

    /* Пост из ленты — 1080×1350 */
    post(post){
      const w = 1080, h = 1350;
      const c = makeCanvas(w, h); const ctx = c.getContext('2d');
      bgPattern(ctx, w, h);
      drawBrand(ctx, 60, 60, 1.1);
      // аватар + имя автора
      viralCanvas._drawAvatar(ctx, 130, 220, 60, post.name || myName(), post.ava);
      ctx.textAlign = 'left';
      ctx.fillStyle = '#fff';
      ctx.font = '800 34px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText(post.name || myName(), 210, 210);
      ctx.fillStyle = BRAND.dim;
      ctx.font = '600 22px "Montserrat", system-ui, sans-serif';
      ctx.fillText('@' + (post.nick || nick()), 210, 250);
      // текст поста в рамке
      ctx.strokeStyle = 'rgba(154,255,0,0.3)'; ctx.lineWidth = 2;
      roundRect(ctx, 60, 350, w - 120, 700, 24, false, true);
      ctx.fillStyle = '#fff';
      ctx.font = '700 40px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      wrapText(ctx, String(post.text || '').slice(0, 260), 100, 430, w - 200, 54);
      // CTA
      ctx.textAlign = 'center';
      ctx.fillStyle = BRAND.lime;
      ctx.font = '800 48px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('okoteam.top/u/@' + (post.nick || nick()), w / 2, h - 160);
      drawQR(ctx, (w - 180) / 2, h - 400, 180, refText());
      drawFooter(ctx, w, h, 'ПОСТ');
      return c;
    },

    /* Реф-ссылка (общая) — 1080×1080 */
    ref(){
      const w = 1080, h = 1080;
      const c = makeCanvas(w, h); const ctx = c.getContext('2d');
      bgPattern(ctx, w, h);
      drawBrand(ctx, 60, 60, 1.2);
      // аватар
      viralCanvas._drawAvatar(ctx, w / 2, 380, 130, myName());
      ctx.textAlign = 'center';
      ctx.fillStyle = '#fff';
      ctx.font = '800 62px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText(myName(), w / 2, 570);
      ctx.fillStyle = BRAND.lime;
      ctx.font = '700 32px "Montserrat", system-ui, sans-serif';
      ctx.fillText('@' + nick(), w / 2, 620);
      // CTA
      ctx.fillStyle = '#fff';
      ctx.font = '700 44px "Bebas Neue", "Montserrat", system-ui, sans-serif';
      ctx.fillText('заходи в OKO по моей ссылке', w / 2, 730);
      drawQR(ctx, (w - 220) / 2, 780, 220, refText());
      drawFooter(ctx, w, h, 'ЗАЙТИ В OKO');
      return c;
    }
  };
  // локальный roundRect (Canvas roundRect в старых движках нет)
  function roundRect(ctx, x, y, w, h, r, fill, stroke){
    r = Math.min(r, Math.min(w, h) / 2);
    ctx.beginPath();
    ctx.moveTo(x + r, y);
    ctx.arcTo(x + w, y, x + w, y + h, r);
    ctx.arcTo(x + w, y + h, x, y + h, r);
    ctx.arcTo(x, y + h, x, y, r);
    ctx.arcTo(x, y, x + w, y, r);
    ctx.closePath();
    if(fill) ctx.fill();
    if(stroke) ctx.stroke();
  }

  /* ============================================================
     2. viralOpenShare(type, data) — универсальный share-sheet
     ============================================================ */
  const SHARE_TEXTS = {
    ref:         (d) => 'Заходи в OKO по моей ссылке: ' + refText(),
    certificate: (d) => 'Прошёл(а) курс "' + (d.course || '') + '" в @okoteam · ' + refText(),
    lesson:      (d) => (d.title || 'Урок Академии OKO') + ' · @okoteam · ' + refText(),
    post:        (d) => (d.text ? d.text.slice(0, 140) + ' · ' : '') + '@okoteam · ' + refText(),
    leaderboard: (d) => 'Я #' + (d.rank || 1) + ' в OKO ' + (d.league || 'Games') + ' · ' + refText(),
    weekly:      (d) => 'Моя неделя в OKO · ' + (d.xp || 0) + ' XP, ' + (d.streak || 0) + ' дней подряд · ' + refText()
  };

  window.viralOpenShare = function(type, data){
    data = data || {};
    const host = document.getElementById('vrShareView');
    if(!host) return;
    // рисуем canvas (Promise-free тк без внешних картинок)
    let canvas;
    try{
      if(type === 'certificate') canvas = viralCanvas.certificate(data);
      else if(type === 'lesson') canvas = viralCanvas.lessonQuotes(data);
      else if(type === 'leaderboard') canvas = viralCanvas.leaderboard(data);
      else if(type === 'post') canvas = viralCanvas.post(data);
      else if(type === 'weekly') canvas = viralCanvas.weekly(data);
      else canvas = viralCanvas.ref();
    }catch(e){ console.warn('[viral] canvas err', e); canvas = viralCanvas.ref(); }
    VR_LAST = {type, data, canvas};

    const titles = {
      ref:'Поделись OKO',
      certificate:'Поделись сертификатом',
      lesson:'Reels из урока',
      post:'Поделись постом',
      leaderboard:'Поделись позицией',
      weekly:'Твоя неделя в OKO'
    };
    const subs = {
      ref:'Реф-ссылка автоматом · +' + '15% с каждой оплаты твоего клиента',
      certificate:'Publikация в один клик · 1080×1350 (TikTok/IG-friendly)',
      lesson:'Готовый storyboard · 3 цитаты · 15 секунд',
      post:'Картинка + твоя реф-ссылка автоматически',
      leaderboard:'Скрин с QR — сразу для сторис/ленты',
      weekly:'XP, streak, партнёрка · для Instagram Stories'
    };

    host.innerHTML =
      '<h3>' + escapeHtml(titles[type] || 'Поделиться') + '</h3>' +
      '<p class="sub">' + escapeHtml(subs[type] || '') + '</p>' +
      '<div class="vr-preview-wrap" id="vrPreviewWrap"></div>' +
      '<p class="vr-preview-hint">Реф-код <b>' + escapeHtml(nick()) + '</b> подставлен · попадёт в статистику партнёрки</p>' +
      (navigator.share
        ? '<button class="vr-native-btn" onclick="viralNative()"><svg class="i"><use href="#i-share"/></svg>Поделиться на устройстве</button>'
        : '') +
      '<div class="vr-share-grid">' +
      '  <button class="vr-share-btn story" onclick="viralPickPlatform(\'story\')">' +
      '    <span class="ico"><svg class="i"><use href="#i-vr-story"/></svg></span>Stories</button>' +
      '  <button class="vr-share-btn tg" onclick="viralPickPlatform(\'tg\')">' +
      '    <span class="ico"><svg class="i"><use href="#i-vr-tg"/></svg></span>Telegram</button>' +
      '  <button class="vr-share-btn vk" onclick="viralPickPlatform(\'vk\')">' +
      '    <span class="ico"><svg class="i"><use href="#i-vr-vk"/></svg></span>VK</button>' +
      '  <button class="vr-share-btn tt" onclick="viralPickPlatform(\'tt\')">' +
      '    <span class="ico"><svg class="i"><use href="#i-vr-tt"/></svg></span>TikTok</button>' +
      '</div>' +
      '<div class="vr-share-grid" style="grid-template-columns:repeat(2,1fr)">' +
      '  <button class="vr-share-btn wa" onclick="viralPickPlatform(\'wa\')">' +
      '    <span class="ico"><svg class="i"><use href="#i-vr-wa"/></svg></span>WhatsApp</button>' +
      '  <button class="vr-share-btn copy" onclick="viralPickPlatform(\'copy\')">' +
      '    <span class="ico"><svg class="i"><use href="#i-copy"/></svg></span>Скопировать</button>' +
      '</div>';

    const wrap = document.getElementById('vrPreviewWrap');
    if(wrap && canvas){ wrap.appendChild(canvas); }
    if(typeof openSheet === 'function') openSheet('vr-share');
  };

  let VR_LAST = null;

  function viralCanvasBlob(){
    return new Promise((res) => {
      if(!VR_LAST || !VR_LAST.canvas) return res(null);
      try{
        VR_LAST.canvas.toBlob(b => res(b), 'image/png');
      }catch(e){ res(null); }
    });
  }
  window.viralDownloadImage = async function(){
    const b = await viralCanvasBlob();
    if(!b) return;
    const a = document.createElement('a');
    a.href = URL.createObjectURL(b);
    a.download = 'oko-' + (VR_LAST.type || 'share') + '-' + Date.now() + '.png';
    document.body.appendChild(a); a.click();
    setTimeout(() => { a.remove(); URL.revokeObjectURL(a.href); }, 500);
  };
  window.viralNative = async function(){
    if(!VR_LAST) return;
    const text = SHARE_TEXTS[VR_LAST.type] ? SHARE_TEXTS[VR_LAST.type](VR_LAST.data) : refText();
    const b = await viralCanvasBlob();
    // share с файлом, если доступно
    if(navigator.canShare && b){
      const file = new File([b], 'oko-share.png', {type:'image/png'});
      if(navigator.canShare({files:[file]})){
        try{
          await navigator.share({files:[file], text, title:'OKO', url: refText()});
          viralOnShared();
          return;
        }catch(e){ /* fallthrough */ }
      }
    }
    if(navigator.share){
      try{ await navigator.share({text, title:'OKO', url: refText()}); viralOnShared(); return; }
      catch(e){}
    }
    // fallback: скачать картинку
    await window.viralDownloadImage();
    toastSafe('Картинка сохранена · публикуй в сторис');
    viralOnShared();
  };
  window.viralPickPlatform = async function(kind){
    if(!VR_LAST) return;
    const text = SHARE_TEXTS[VR_LAST.type] ? SHARE_TEXTS[VR_LAST.type](VR_LAST.data) : refText();
    const url  = refText();
    const enc  = encodeURIComponent;
    let go = null;
    if(kind === 'tg') go = 'https://t.me/share/url?url=' + enc(url) + '&text=' + enc(text);
    else if(kind === 'vk') go = 'https://vk.com/share.php?url=' + enc(url) + '&title=' + enc(text);
    else if(kind === 'wa') go = 'https://wa.me/?text=' + enc(text);
    else if(kind === 'tt' || kind === 'story'){
      // TikTok/Stories не имеют веб-share deeplink на пост → сохраняем картинку
      await window.viralDownloadImage();
      try{ await navigator.clipboard.writeText(text); }catch(e){}
      toastSafe('Картинка сохранена · текст в буфере');
      viralOnShared();
      return;
    } else if(kind === 'copy'){
      try{ await navigator.clipboard.writeText(text); }catch(e){}
      toastSafe('Ссылка скопирована');
      viralOnShared();
      return;
    }
    if(go){
      window.open(go, '_blank', 'noopener');
      viralOnShared();
    }
  };
  function viralOnShared(){
    // share-streak: +1 за сегодня (уникально по дню)
    const s = shareStore();
    const today = new Date().toISOString().slice(0, 10);
    if(s.lastShareDay !== today){
      const prev = s.lastShareDay ? new Date(s.lastShareDay) : null;
      const diff = prev ? Math.round((Date.now() - prev.getTime()) / 86400e3) : 999;
      s.streak = (diff === 1) ? (s.streak || 0) + 1 : 1;
      s.lastShareDay = today;
    }
    s.total = (s.total || 0) + 1;
    saveShareStore(s);
    if(s.streak === 30) toastSafe('Serial × 30 · ты получил бейдж «Мессия OKO»');
    else if(s.streak === 7) toastSafe('7 дней подряд · серия шеринга растёт');
  }

  /* ============================================================
     3. Reels-цитаты автоматом
     ============================================================ */
  function viral_buildScenes(lesson){
    const title = (lesson && lesson.title) || 'Урок Академии OKO';
    let quotes = (lesson && (lesson.quotes || lesson.keyQuotes)) || null;
    if(!quotes || !quotes.length){
      const body = (lesson && (lesson.body || lesson.text || lesson.summary)) || '';
      quotes = String(body)
        .split(/(?<=[.!?])\s+/)
        .map(s => s.trim())
        .filter(s => s.length >= 40 && s.length <= 220)
        .slice(0, 3);
      if(quotes.length < 3){
        const fill = [
          'Один инструмент вместо десяти',
          'Скорость важнее совершенства',
          'Практика сегодня сильнее теории завтра'
        ];
        while(quotes.length < 3) quotes.push(fill[quotes.length] || fill[0]);
      }
    }
    return [
      {kind:'cover', title, dur:2},
      {kind:'quote', text: quotes[0], author: myName(), dur:4},
      {kind:'quote', text: quotes[1], author: myName(), dur:4},
      {kind:'quote', text: quotes[2], author: myName(), dur:4},
      {kind:'cta', dur:2}
    ];
  }

  window.viralGenReelsQuote = function(lesson){
    const scenes = viral_buildScenes(lesson || {});
    const view = document.getElementById('vrReelsView');
    if(!view) return;
    view.innerHTML =
      '<h3>Reels из урока за 30 секунд</h3>' +
      '<p class="sub">' + escapeHtml((lesson && lesson.title) || 'Урок Академии OKO') + ' · 5 кадров · 15 секунд</p>' +
      '<div class="vr-preview-wrap" id="vrReelsWrap"></div>' +
      '<div class="vr-reels-scenes">' +
        scenes.map((s, i) => {
          if(s.kind === 'cover') return '<div class="vr-reels-scene"><span class="n">Кадр ' + (i+1) + ' · обложка · ' + s.dur + ' сек</span><div class="q">' + escapeHtml(s.title) + '</div></div>';
          if(s.kind === 'quote') return '<div class="vr-reels-scene"><span class="n">Кадр ' + (i+1) + ' · цитата · ' + s.dur + ' сек</span><div class="q">' + escapeHtml(s.text) + '</div></div>';
          return '<div class="vr-reels-scene"><span class="n">Кадр ' + (i+1) + ' · CTA · ' + s.dur + ' сек</span><div class="q">Ищи меня в OKO · @' + escapeHtml(nick()) + '</div></div>';
        }).join('') +
      '</div>' +
      '<button class="vr-native-btn" onclick="viralOpenShare(\'lesson\', ' + escapeAttr(JSON.stringify({title:(lesson && lesson.title) || '', scenes})) + ')"><svg class="i"><use href="#i-share"/></svg>Поделиться storyboard</button>' +
      '<button class="vr-native-btn" style="background:var(--raised);color:var(--fg);border:1px solid var(--border);margin-top:8px" onclick="viralDownloadReels()"><svg class="i"><use href="#i-copy"/></svg>Скачать 5 кадров (PNG)</button>';
    const wrap = document.getElementById('vrReelsWrap');
    if(wrap){
      const c = viralCanvas.lessonQuotes({scenes});
      wrap.appendChild(c);
      VR_LAST = {type:'lesson', data:{scenes}, canvas: c};
    }
    if(typeof openSheet === 'function') openSheet('vr-reels');
  };
  function escapeAttr(s){ return String(s).replace(/"/g, '&quot;'); }
  window.viralDownloadReels = function(){
    if(!VR_LAST || !VR_LAST.canvas) return;
    const scenes = (VR_LAST.data && VR_LAST.data.scenes) || [];
    scenes.forEach((s, i) => {
      const c = viralCanvas._reelFrame(s, 1080, 1920);
      c.toBlob(b => {
        if(!b) return;
        const a = document.createElement('a');
        a.href = URL.createObjectURL(b);
        a.download = 'oko-reels-' + (i+1) + '.png';
        document.body.appendChild(a); a.click();
        setTimeout(() => { a.remove(); URL.revokeObjectURL(a.href); }, 400);
      }, 'image/png');
    });
    toastSafe('5 кадров скачаны · собери из них Reels/Stories');
  };

  /* ============================================================
     4. Weekly Story-summary — client-side scheduler
     ============================================================ */
  function weeklyMetrics(){
    let xp = 0, streak = 0, lessons = 0, earned = 0, favCourse = 'Академия OKO';
    try{
      if(typeof acS !== 'undefined' && acS){
        xp = acS.xp || 0;
        streak = acS.streak || 0;
        if(Array.isArray(acS.lessons)) lessons = acS.lessons.filter(l => l && l.done).length;
      }
    }catch(e){}
    try{ if(typeof PP !== 'undefined' && PP && PP.monthEarned) earned = Math.round(PP.monthEarned / 4); }catch(e){}
    try{
      if(typeof AC_COURSES !== 'undefined' && Array.isArray(AC_COURSES) && AC_COURSES.length){
        favCourse = AC_COURSES[0].title || favCourse;
      }
    }catch(e){}
    return {xp, streak, lessons, earned, favCourse};
  }
  window.viralOpenWeekly = function(){
    const view = document.getElementById('vrWeeklyView');
    if(!view) return;
    const m = weeklyMetrics();
    view.innerHTML =
      '<h3>Твоя неделя в OKO</h3>' +
      '<p class="sub">Story-summary 1080×1920 · для Instagram Stories, TG-канала и VK Clips</p>' +
      '<div class="vr-preview-wrap" id="vrWeeklyWrap"></div>' +
      '<div class="vr-weekly-metrics">' +
      '  <div class="vr-weekly-metric"><div class="l">XP · неделя</div><div class="v lime">' + m.xp + '</div></div>' +
      '  <div class="vr-weekly-metric"><div class="l">дней подряд</div><div class="v">' + m.streak + '</div></div>' +
      '  <div class="vr-weekly-metric"><div class="l">партнёрка</div><div class="v lime">' + m.earned + ' ₽</div></div>' +
      '  <div class="vr-weekly-metric"><div class="l">уроков</div><div class="v">' + m.lessons + '</div></div>' +
      '</div>' +
      '<button class="vr-native-btn" onclick="viralOpenShare(\'weekly\', ' + escapeAttr(JSON.stringify(m)) + ')"><svg class="i"><use href="#i-share"/></svg>Поделиться в Instagram Stories</button>';
    const wrap = document.getElementById('vrWeeklyWrap');
    if(wrap){
      const c = viralCanvas.weekly(m);
      wrap.appendChild(c);
      VR_LAST = {type:'weekly', data:m, canvas:c};
    }
    if(typeof openSheet === 'function') openSheet('vr-weekly');
  };

  // scheduler: проверять каждую минуту, воскресенье 20:00 → плашка (раз в неделю)
  function weeklySchedulerTick(){
    const now = new Date();
    if(now.getDay() !== 0) return; // 0 = вс
    if(now.getHours() < 20) return;
    const s = shareStore();
    const wk = weekKey();
    if(s.weeklyShownWk === wk) return;
    s.weeklyShownWk = wk; saveShareStore(s);
    const el = document.getElementById('vrWeeklyNudge');
    if(el){ el.classList.add('on'); }
  }
  function weekKey(){
    const d = new Date();
    // ISO-week: год-неделя (грубо)
    const oneJan = new Date(d.getFullYear(), 0, 1);
    const wk = Math.ceil((((d - oneJan) / 86400000) + oneJan.getDay() + 1) / 7);
    return d.getFullYear() + '-w' + wk;
  }
  window.viralWeeklyNudgeHide = function(){
    const el = document.getElementById('vrWeeklyNudge');
    if(el) el.classList.remove('on');
  };
  setInterval(weeklySchedulerTick, 60000);
  setTimeout(weeklySchedulerTick, 3000);

  /* ============================================================
     5. Публичный профиль /u/@username
     ============================================================ */
  function parsePublicSlug(){
    // из pathname: /u/@nick[?ref=x]
    const m = location.pathname.match(/\/u\/@?([\w.\-]+)/i);
    if(m) return m[1];
    // из hash: #/u/@nick
    const mh = (location.hash || '').match(/#\/?u\/@?([\w.\-]+)/i);
    if(mh) return mh[1];
    // из query: ?u=nick
    const q = new URLSearchParams(location.search).get('u');
    if(q) return q.replace(/^@/, '');
    return null;
  }
  function saveRefFrom(){
    try{
      const q = new URLSearchParams(location.search);
      const r = q.get('ref') || (parsePublicSlug());
      if(r && typeof PROFILE !== 'undefined'){
        PROFILE.refFrom = String(r).replace(/^@/, '');
        try{ localStorage.setItem('vr.refFrom', PROFILE.refFrom); }catch(e){}
      }
    }catch(e){}
  }
  function setOgTags(slug){
    function upsert(prop, value){
      let el = document.querySelector('meta[property="' + prop + '"]');
      if(!el){ el = document.createElement('meta'); el.setAttribute('property', prop); document.head.appendChild(el); }
      el.setAttribute('content', value);
    }
    const url = 'https://' + HOST + '/u/@' + slug;
    // рендерим OG-картинку и вставляем как data-URL
    let img = '';
    try{
      const c = viralCanvas.ref();
      img = c.toDataURL('image/png');
    }catch(e){}
    upsert('og:title', myName() + ' · @' + slug + ' в OKO');
    upsert('og:description', (myBio() || 'Профиль в OKO — одном приложении для медийщика, эксперта, автора.'));
    upsert('og:url', url);
    upsert('og:type', 'profile');
    if(img) upsert('og:image', img);
    upsert('twitter:card', 'summary_large_image');
  }
  function makePublicSample(slug){
    /* Здесь генерировался «партнёр OKO»: восемь выдуманных имён (Анна Ковалёва,
       Кирилл Морозов и так далее), выдуманные биографии, 400–12 000 подписчиков,
       два сертификата с номерами и два канала с охватами — всё из суммы кодов
       символов ника. Открыв чужую визитку, человек видел несуществующего
       специалиста с готовым портфолио.
       Правка Даниэля: ноль выдуманных людей. Мы честно знаем про этот профиль
       ровно одно — ник. Всё остальное подтянется, когда появится бэкенд. */
    return {
      name: '@' + slug,
      nick: slug,
      bio: '',
      followers: 0,
      posts: 0,
      streak: 0,
      tier: 'FREE',
      unknown: true,
      certs: [],
      channels: [],
      socials: []
    };
  }
  function publicProfileData(slug){
    if(typeof PROFILE !== 'undefined' && slug === PROFILE.nick){
      // мой профиль
      const certs = (typeof acS !== 'undefined' && acS && Array.isArray(acS.certs)) ? acS.certs.map(c => ({
        t: c.courseTitle || 'Курс Академии OKO',
        d: c.date,
        no: c.no
      })) : [];
      return {
        name: PROFILE.name,
        nick: PROFILE.nick,
        bio: PROFILE.bio || '',
        /* Было: 2400 подписчиков, 47 постов, streak 14, «Канал Даниэля · 2.4к»
           и сертификат-заглушка OKO-CERT-000001. Всё выдумано. Считаем то,
           что действительно есть в состоянии, и не рисуем то, чего нет. */
        followers: (typeof okoMyFollowers === 'function') ? okoMyFollowers() : 0,
        posts: (typeof okoMyPostsCount === 'function') ? okoMyPostsCount() : 0,
        streak: (typeof acS !== 'undefined' && acS && acS.streak) || 0,
        tier: PROFILE.tier,
        certs: certs.slice(0, 4),
        channels: (typeof okoMyChannels === 'function') ? okoMyChannels() : [],
        socials: [
          {id:'tg', ic:'i-vr-tg', label:'Telegram · @' + PROFILE.nick, href:'https://t.me/' + PROFILE.nick},
          {id:'ig', ic:'i-vr-ig', label:'Instagram · @' + PROFILE.nick, href:'https://instagram.com/' + PROFILE.nick}
        ]
      };
    }
    return makePublicSample(slug);
  }

  window.viralOpenPublicProfile = function(slug){
    if(!slug) return;
    saveRefFrom();
    setOgTags(slug);
    const host = document.getElementById('vrPublic');
    const inner = document.getElementById('vrPublicInner');
    if(!host || !inner) return;
    const d = publicProfileData(slug);
    const publicUrl = 'okoteam.top/u/@' + slug;

    const socHtml = (d.socials || []).map(s =>
      '<a href="' + escapeHtml(s.href) + '" target="_blank" rel="noopener"><span class="ico"><svg class="i"><use href="#' + escapeHtml(s.ic) + '"/></svg></span><span class="lbl">' + escapeHtml(s.label) + '</span></a>'
    ).join('');
    const certsHtml = (d.certs || []).map(c =>
      '<div class="vr-pu-cert"><span class="seal">OKO</span><div class="info"><div class="t">' + escapeHtml(c.t) + '</div><div class="s">' + escapeHtml(c.no || '') + ' · ' + escapeHtml(c.d || '') + '</div></div></div>'
    ).join('');
    const chansHtml = (d.channels || []).map(ch =>
      '<div class="vr-pu-chan"><span class="a">' + escapeHtml(initials(ch.t)) + '</span><div class="info"><div class="t">' + escapeHtml(ch.t) + '</div><div class="s">' + escapeHtml(ch.s || '') + '</div></div><div class="k">' + escapeHtml(ch.k || '') + '</div></div>'
    ).join('');

    inner.innerHTML =
      '<div class="vr-pu-top">' +
        '<a class="vr-pu-logo" href="/"><svg class="i"><use href="#i-logo"/></svg><span>OKO</span></a>' +
        '<div class="vr-pu-actions">' +
          '<button onclick="viralPublicShare()"><svg class="i"><use href="#i-share"/></svg>Поделиться</button>' +
          '<a class="primary" href="/?ref=' + escapeHtml(slug) + '"><svg class="i"><use href="#i-plus"/></svg>Открыть OKO</a>' +
        '</div>' +
      '</div>' +
      '<div class="vr-pu-hero">' +
        '<div class="vr-pu-ava"' + (myAvatar() && d.nick === nick() ? ' style="background-image:url(' + JSON.stringify(myAvatar()).slice(1,-1) + ')"' : '') + '>' + escapeHtml(initials(d.name)) + '</div>' +
        '<div class="vr-pu-name">' + escapeHtml(d.name) + '</div>' +
        '<div class="vr-pu-nick">@' + escapeHtml(d.nick) + '</div>' +
        (d.bio ? '<div class="vr-pu-bio">' + escapeHtml(d.bio) + '</div>' : '') +
        '<div class="vr-pu-chips">' +
          '<span class="vr-pu-chip gold"><svg class="i"><use href="#i-vr-crown"/></svg>' + escapeHtml(d.tier || 'FREE') + '</span>' +
          '<span class="vr-pu-chip"><svg class="i"><use href="#i-vr-fire"/></svg>' + escapeHtml(d.streak + ' дней подряд') + '</span>' +
          '<span class="vr-pu-chip"><svg class="i"><use href="#i-vr-badge"/></svg>партнёр OKO</span>' +
        '</div>' +
        '<div class="vr-pu-stats">' +
          '<div class="vr-pu-stat"><div class="v">' + fmtK(d.followers) + '</div><div class="l">подписчиков</div></div>' +
          '<div class="vr-pu-stat"><div class="v">' + d.posts + '</div><div class="l">постов</div></div>' +
          '<div class="vr-pu-stat"><div class="v lime">' + (d.certs.length || 0) + '</div><div class="l">сертификатов</div></div>' +
          '<div class="vr-pu-stat"><div class="v">' + d.streak + '</div><div class="l">дн. streak</div></div>' +
        '</div>' +
        '<div class="vr-pu-cta">' +
          '<button class="btn-primary" onclick="viralPublicMessage(\'' + escapeHtml(slug) + '\')"><svg class="i"><use href="#i-chat"/></svg>Написать в OKO</button>' +
          '<button class="btn-ghost" onclick="viralPublicShare()" title="Поделиться"><svg class="i"><use href="#i-share"/></svg></button>' +
          '<button class="btn-ghost" onclick="viralPublicQr()" title="QR"><svg class="i"><use href="#i-vr-qr"/></svg></button>' +
        '</div>' +
      '</div>' +

      (socHtml ? '<div class="vr-pu-section"><h3>Соцсети <span class="badge">' + (d.socials.length) + '</span></h3><div class="vr-pu-soc">' + socHtml + '</div></div>' : '') +

      (certsHtml ? '<div class="vr-pu-section"><h3>Сертификаты Академии <span class="badge">' + (d.certs.length) + '</span></h3>' + certsHtml + '</div>' : '') +

      (chansHtml ? '<div class="vr-pu-section"><h3>Каналы <span class="badge">' + (d.channels.length) + '</span></h3>' + chansHtml + '</div>' : '') +

      '<div class="vr-pu-section"><h3>Партнёрская ссылка</h3>' +
        '<div class="vr-pu-ref">' +
          '<div class="ico"><svg class="i"><use href="#i-vr-badge"/></svg></div>' +
          '<div class="info"><div class="t">' + escapeHtml(publicUrl) + '</div><div class="s">Первый месяц PRO со скидкой · трекается за ' + escapeHtml(d.nick) + '</div></div>' +
          '<button onclick="viralPublicRefCopy()">Копировать</button>' +
        '</div>' +
      '</div>' +

      '<div class="vr-pu-foot">Профиль OKO · ' + escapeHtml(publicUrl) + '<br/><a href="/">открыть приложение OKO</a></div>';

    host.classList.add('open');
    document.body.style.overflow = 'hidden';
  };
  window.viralClosePublic = function(){
    const host = document.getElementById('vrPublic');
    if(host) host.classList.remove('open');
    document.body.style.overflow = '';
  };
  window.viralPublicShare = function(){ viralOpenShare('ref', {}); };
  window.viralPublicQr = function(){ viralOpenShare('ref', {}); };
  window.viralPublicRefCopy = function(){
    const slug = parsePublicSlug() || nick();
    try{ navigator.clipboard.writeText('https://okoteam.top/u/@' + slug + '?ref=' + slug); }catch(e){}
    toastSafe('Ссылка скопирована');
  };
  window.viralPublicMessage = function(slug){
    // если приложение открыто — идём во вкладку чатов; иначе deep-link
    if(typeof showTab === 'function'){
      viralClosePublic();
      showTab('chats');
      toastSafe('Открой чат с @' + slug + ' и напиши сообщение');
    } else {
      location.href = '/?tab=chats&to=' + encodeURIComponent(slug);
    }
  };
  function fmtK(n){
    if(n >= 1e6) return (n / 1e6).toFixed(1).replace('.0','') + 'M';
    if(n >= 1e3) return (n / 1e3).toFixed(1).replace('.0','') + 'к';
    return String(n);
  }

  /* ============================================================
     6. Патчи: Академия · сертификат → viral share
     ============================================================ */
  (function patchCertShare(){
    let tries = 0;
    function tryPatch(){
      if(typeof acCertShare !== 'function'){
        if(++tries < 40) return setTimeout(tryPatch, 300);
        return;
      }
      const orig = window.acCertShare;
      window.acCertShare = function(i){
        try{
          const c = (typeof i === 'number' && typeof acS !== 'undefined' && acS.certs) ? acS.certs[i] : (typeof acCertRec === 'function' ? acCertRec() : null);
          if(!c){ return orig.apply(this, arguments); }
          viralOpenShare('certificate', {
            no: c.no, date: c.date, score: c.score,
            name: c.name || myName(),
            course: c.courseTitle || (typeof acCertLabel === 'function' ? acCertLabel(c) : 'Академия OKO')
          });
        }catch(e){ return orig.apply(this, arguments); }
      };
    }
    tryPatch();
  })();

  /* ============================================================
     7. Академия · nudge «сделай Reels из урока за 30 сек»
        Показывается когда открыт длинный урок (durSec > 60) и он завершён.
     ============================================================ */
  (function lessonNudge(){
    let last = null, checks = 0;
    function currentLesson(){
      try{
        if(typeof AC_COURSE === 'undefined' || typeof acL === 'undefined') return null;
        const l = AC_COURSE[acL];
        if(!l) return null;
        const durSec = (typeof l.dur === 'number') ? l.dur : (l.durationSec || l.duration || 0);
        const done = (typeof acLessonDone === 'function') ? acLessonDone(acL) : false;
        return {l, durSec, done};
      }catch(e){ return null; }
    }
    function tick(){
      // работает только на экране академии, когда открыт урок
      const scrAc = document.getElementById('screen-academy');
      if(!scrAc || !scrAc.classList.contains('active')) return;
      const cur = currentLesson();
      if(!cur || !cur.l) return;
      // условие: урок больше 60 сек и завершён, ещё не предлагали
      if((cur.durSec || 120) < 60) return;
      if(!cur.done) return;
      const key = 'ac:' + (cur.l.id || cur.l.title || acL);
      if(key === last) return;
      const s = shareStore();
      s.reelsNudged = s.reelsNudged || {};
      if(s.reelsNudged[key]) return;
      last = key;
      s.reelsNudged[key] = 1; saveShareStore(s);
      showReelsNudge(cur.l);
    }
    function showReelsNudge(lesson){
      const el = document.getElementById('vrLessonNudge');
      if(!el) return;
      window.__vrLessonCache = lesson;
      el.classList.add('on');
      clearTimeout(showReelsNudge._t);
      showReelsNudge._t = setTimeout(() => el.classList.remove('on'), 12000);
    }
    setInterval(tick, 2000);
    setTimeout(tick, 3000);
  })();
  window.viralNudgeReels = function(){
    const el = document.getElementById('vrLessonNudge');
    if(el) el.classList.remove('on');
    viralGenReelsQuote(window.__vrLessonCache || {});
  };
  window.viralNudgeHide = function(){
    const el = document.getElementById('vrLessonNudge');
    if(el) el.classList.remove('on');
  };

  /* ============================================================
     8. Игры · «Поделиться моей позицией» в лидерборде
     ============================================================ */
  (function patchLbShare(){
    let tries = 0;
    function tryPatch(){
      if(typeof gmLbRender !== 'function'){
        if(++tries < 40) return setTimeout(tryPatch, 300);
        return;
      }
      const orig = window.gmLbRender;
      window.gmLbRender = function(){
        const r = orig.apply(this, arguments);
        // после отрисовки — добавляем share-кнопку под лидерборд
        try{
          const el = document.getElementById('gmLb');
          if(!el) return r;
          if(el.querySelector('.vr-lb-share')) return r;
          const rows = (typeof gmLbRank === 'function') ? gmLbRank() : [];
          const myIdx = rows.findIndex(x => x.me);
          const btn = document.createElement('button');
          btn.className = 'vr-lb-share';
          const league = (typeof GM_LB_LEAGUES !== 'undefined' && GM_LB_LEAGUES.find)
            ? (GM_LB_LEAGUES.find(l => l.id === (window.GM_LB_LEAGUE || 'friends')) || {n:'GAMES'}).n
            : 'GAMES';
          const rank = myIdx >= 0 ? myIdx + 1 : (rows.length || 1);
          const score = (myIdx >= 0 && rows[myIdx].s) ? Math.round(rows[myIdx].s) : 0;
          btn.innerHTML = '<svg class="i"><use href="#i-share"/></svg>Поделиться позицией · #' + rank + ' ' + league;
          btn.onclick = () => viralOpenShare('leaderboard', {rank, league, score});
          el.appendChild(btn);
        }catch(e){}
        return r;
      };
      // сразу подтянуть, если элемент уже отрисован
      try{ if(document.getElementById('gmLb')) window.gmLbRender(); }catch(e){}
    }
    tryPatch();
  })();

  /* ============================================================
     9. Лента · share-хук для любого поста
     ============================================================ */
  (function patchFeedShare(){
    // если в приложении уже есть sharePost — оборачиваем
    let tries = 0;
    function tryPatch(){
      if(typeof window.sharePost === 'function'){
        const orig = window.sharePost;
        window.sharePost = function(i){
          try{
            const post = (typeof POSTS !== 'undefined' && POSTS[i]) ? POSTS[i]
              : (typeof POSTS !== 'undefined' && POSTS.sub && POSTS.sub[i]) ? POSTS.sub[i]
              : {text:'Пост из OKO', name: myName(), nick: nick()};
            viralOpenShare('post', {
              text: post.body || post.text || '',
              name: post.name || myName(),
              nick: post.nick || post.author_nick || nick(),
              ava: post.ava || ''
            });
          }catch(e){ return orig.apply(this, arguments); }
        };
        return;
      }
      // делегированный click по .i-share/.share-btn внутри поста
      document.addEventListener('click', function(e){
        const btn = e.target.closest('[data-vr-post-share]');
        if(!btn) return;
        e.preventDefault();
        try{
          const post = JSON.parse(btn.dataset.vrPostShare);
          viralOpenShare('post', post);
        }catch(err){}
      });
      if(++tries < 20) setTimeout(tryPatch, 500);
    }
    tryPatch();
  })();

  /* ============================================================
     10. Партнёрка · Referral squared (+3 / +10 клиентов → бонусы)
     ============================================================ */
  function refCounts(){
    /* Реальное число приведённых клиентов. Раньше при пустой истории
       функция возвращала «2» — новому человеку рисовали чужой прогресс
       по целям. Источник один: счётчик партнёрских целей (бэкенд). */
    let uniq = 0;
    try{ if(typeof window.ppGoalsGetCount === 'function') uniq = window.ppGoalsGetCount()|0; }catch(e){}
    if(!uniq){
      try{
        if(typeof HIST !== 'undefined' && Array.isArray(HIST)){
          const set = new Set();
          HIST.forEach(h => { if(h && h.name) set.add(h.name); });
          uniq = set.size || HIST.filter(h => h.st === 'paid').length;
        }
      }catch(e){}
    }
    return Math.max(0, uniq|0);
  }
  function refSquaredHtml(){
    const c = refCounts();
    const g1 = 3, g2 = 10;
    const p1 = Math.min(100, Math.round(c / g1 * 100));
    const p2 = Math.min(100, Math.round(c / g2 * 100));
    return '' +
      '<div class="h"><span class="lb">Referral squared</span><span class="chip">' + c + ' / ' + g2 + '</span></div>' +
      '<div class="vr-refsq-progress">' +
        '<div class="vr-refsq-row"><span class="lbl">Приведи 3 клиентов</span><span class="n">' + Math.min(c, g1) + '/' + g1 + '</span></div>' +
        '<div class="vr-refsq-bar"><i style="width:' + p1 + '%"></i></div>' +
        '<div class="vr-refsq-row" style="margin-top:14px"><span class="lbl">Приведи 10 клиентов</span><span class="n">' + Math.min(c, g2) + '/' + g2 + '</span></div>' +
        '<div class="vr-refsq-bar"><i style="width:' + p2 + '%"></i></div>' +
      '</div>' +
      '<div class="vr-refsq-goal ' + (c >= g1 ? 'done' : '') + '">' +
        '<div class="badge">+3</div>' +
        '<div><div class="t">PRO бесплатно 3 месяца</div><div class="s">' + (c >= g1 ? 'Открыто · зачислили тариф PRO' : 'Приведи ещё ' + (g1 - c) + ' клиентов через партнёрскую ссылку') + '</div></div>' +
      '</div>' +
      '<div class="vr-refsq-goal ' + (c >= g2 ? 'done' : '') + '">' +
        '<div class="badge">+10</div>' +
        '<div><div class="t">PRO навсегда · бейдж Legend</div><div class="s">' + (c >= g2 ? 'Легендарный статус · большой бейдж в профиле' : 'Осталось ' + (g2 - c) + ' · пожизненный PRO ждёт') + '</div></div>' +
      '</div>' +
      '<button onclick="viralOpenShare(\'ref\',{})"><svg class="i" style="width:14px;height:14px;vertical-align:-2px"><use href="#i-share"/></svg> Пригласить ещё</button>';
  }
  (function injectRefSquared(){
    let tries = 0;
    function tick(){
      const root = document.getElementById('ppRoot');
      if(!root){
        if(++tries < 60) return setTimeout(tick, 500);
        return;
      }
      if(document.getElementById('vrRefSquaredCard')) return;
      const card = document.createElement('div');
      card.id = 'vrRefSquaredCard';
      card.innerHTML = refSquaredHtml();
      // вставляем после ppLadder, если есть
      const anchor = document.getElementById('ppLadder') || root.firstElementChild;
      if(anchor && anchor.parentNode){
        anchor.parentNode.insertBefore(card, anchor.nextSibling);
      } else {
        root.appendChild(card);
      }
    }
    setTimeout(tick, 500);
    // при переходе на вкладку partner — переустановить
    if(typeof window.showTab === 'function'){
      const orig = window.showTab;
      window.showTab = function(t){
        const r = orig.apply(this, arguments);
        if(t === 'partner') setTimeout(tick, 300);
        return r;
      };
    }
  })();

  /* ============================================================
     11. Serial-бейдж «Мессия OKO» — 30 дней подряд share
        Показывается в профиле (append к #profStats области).
     ============================================================ */
  (function serialBadge(){
    let tries = 0;
    function tick(){
      const s = shareStore();
      const streak = s.streak || 0;
      if(streak < 30){
        if(++tries < 12) return setTimeout(tick, 5000);
        return;
      }
      const host = document.getElementById('profStats') || document.querySelector('#screen-profile .pad');
      if(!host) { if(++tries < 12) return setTimeout(tick, 5000); return; }
      if(document.getElementById('vrSerialBadge')) return;
      const b = document.createElement('div');
      b.id = 'vrSerialBadge'; b.style.marginTop = '10px'; b.style.textAlign = 'center';
      b.innerHTML = '<span class="vr-serial"><svg class="i"><use href="#i-vr-fire"/></svg>Мессия OKO · ' + streak + ' дней подряд</span>';
      host.appendChild(b);
    }
    setTimeout(tick, 4000);
  })();

  /* ============================================================
     12. Автоматический запуск публичного профиля по URL
     ============================================================ */
  (function bootPublic(){
    saveRefFrom();
    const slug = parsePublicSlug();
    if(slug){
      // ждём DOM
      const boot = () => viralOpenPublicProfile(slug);
      if(document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot);
      else setTimeout(boot, 200);
    }
    // навешиваем hash-навигацию — обновления URL внутри app
    window.addEventListener('hashchange', () => {
      const s = parsePublicSlug();
      if(s) viralOpenPublicProfile(s);
      else if(document.getElementById('vrPublic')) window.viralClosePublic();
    });
  })();

  /* ============================================================
     13. Хук для reg/чужого профиля: кнопка «Поделиться моим OKO»
     ============================================================ */
  window.viralShareMyProfile = function(){ viralOpenShare('ref', {}); };

  /* ============================================================
     14. Экспорт viralCanvas для внешних модулей + метка готовности
     ============================================================ */
  window.viral = {
    openShare: viralOpenShare,
    reels: viralGenReelsQuote,
    openWeekly: viralOpenWeekly,
    openPublic: viralOpenPublicProfile,
    canvas: viralCanvas,
    state: () => shareStore()
  };
  document.documentElement.dataset.viral = '1';
})();
