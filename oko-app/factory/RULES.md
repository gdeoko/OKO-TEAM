# DIESEL CARGO — ЖЁСТКИЕ ПРАВИЛА КОНТЕНТ-ЗАВОДА (источник правды)

Даниэль, 21.07: каждая буква — жёсткое правило, НЕ шаблон-который-можно-пропустить.
Всё делается ЗАНОВО на КАЖДЫЙ ролик, с нуля, без лени и отговорок. За 450 роликов —
НИ ОДНОГО повтора ни по одной оси (тема, хук, формат, стоки, анимации, наложения,
инфографика, музыка, нарезка, анализ, активность). Цель — каждый ролик на 1 000 000+.

## КЛИЕНТ
Импорт мото/квадро/гидро из Китая. dieselcompany.pro. Бренд чёрный+амбер #EA5920,
Bebas Neue + Montserrat. Голос: **Piper TTS (студийный, бесплатный, БЕЗЛИМИТ, MIT/коммерч., локально быстро) — `pipeline/tts_piper.py`, голоса ru_RU-ruslan/dmitri/denis (env VOICE_PIPER)**. Резерв: **Silero v4_ru (`pipeline/tts_silero.py`)** — мужской (eugene/aidar), ударения+ё авто (put_accent/put_yo) → БЕЗ ошибок в словах. Локально/стабильно. Заменил edge-tts И Qwen3-TTS (Даниэль 22.07: и edge, и Qwen-голоса — плохие/с ошибками). Финальный спикер — по выбору Даниэля. Снегоходы НЕ делаем.
Контент-микс: **ОСНОВА квадро+мото, изредка гидро, реже всего спецтехника (1 из ~8).**

## ОБЪЁМ, ТЕМП, РАМП
- **2 пачки × 3 сети**, каждая пачка — РАЗНЫЕ ролики и РАЗНЫЕ аккаунты (ноль пересечений).
- Пачка A: YouTube main · TikTok **2365299 (@diesel_kitay «DIESEL CARGO»)** · Instagram @diesel_cargo.
- Пачка B: YouTube ytnew_b · TikTok 2363201 (@diesel_official11) · Instagram @diesel_kitay.
- 22.07: старый TikTok пачки A @diesel.cargo (page 2350915) УДАЛЁН Даниэлем («туда больше не платим»),
  заменён на новый @diesel_kitay (page 2365299). Hooppy: page_id стабилен, account_id меняется при
  переподключении — брать page_id из `GET api.hooppy.ru/api/accounts/pages`, НЕ account_id.
- ⛔ НЕ-DIESEL Hooppy-страницы (НИКОГДА не постить): 2356602 @daniel.oko.app, 2352065 @mama.s.bogom,
  2350868 @tappio.app. Наши только: 2365299 (A) и 2363201 (B).
- В `meta.json` каждого ролика — `"batch":"A"` или `"batch":"B"`. `publish_next.py`
  постит ролик ТОЛЬКО в аккаунты своей пачки.
- **Старт 5 роликов/день на пачку (=10/день).** Через 3 дня ровного выпуска — плавно
  наращивать до **15/пачку**. Если на ЛЮБОМ аккаунте просмотры проседают или 0 —
  **откат к 5/пачку** и снова плавно вверх. Состояние: `cfg/ramp_state.json`.
- **График постинга: интервал ≥ 1.5 часа между роликами (правило Даниэля 22.07).**
  Cron-слоты каждые 1.5ч: 06:00·07:30·09:00·10:30·12:00·13:30·15:00·16:30·18:00·19:30·21:00 UTC
  (`diesel_publish_slot.sh` через flock, post_direct, ramp-cap = 2×target/день, пачки чередуются).
  ⚠️ При ≥1.5ч максимум ~16 роликов/сутки суммарно (≈8/пачку) — цель 15/пачку с этим интервалом
  недостижима физически; при рампе выше 8/пачку интервал придётся сократить (обсудить с Даниэлем).
- Осталось (с 21.07): **450 роликов**. Цель 20k подписчиков, 100M просмотров.

## КОНТЕНТ-МИКС ПО ЦЕЛИ (пропорция)
**50% виральное · 30% реально полезное (называем инструменты/факты/цифры) · 20% продажи.**
В описаниях — воронка продаж + кодовые слова, CTA веди на dieselcompany.pro, в канал
или в директ; либо продавай напрямую.

## ПОЛНЫЙ ЦИКЛ — ВЫПОЛНЯТЬ ПЕРЕД КАЖДЫМ РОЛИКОМ (с нуля, без шаблонов)

### 1. АНАЛИЗ КОНКУРЕНТОВ (всегда НОВЫЕ, без повторов)
- 10 самых популярных конкурентов ниши на момент сборки (каждый раз другие).
- По 5 роликов от 1 000 000 просмотров в Instagram и в YouTube (новые, дедуп) →
  скачать → раскадровка + полностью смотрю кадры/озвучку/сценарий → метрики
  (лайки, комментарии, просмотры, репосты, сохранения).
- Разбор: хук, сценарий, активность, воронка продаж, описание, продукт.
- **Красивый документ-аналитика со всеми цифрами → в Telegram-бот.**
- Во время анализа: лайк + комментарий + сохранение каждому; подписка ТОЛЬКО на 3
  с наибольшим числом подписчиков; ролики смотрю до конца.

### 2. СЦЕНАРИЙ
- На основе анализа 10 конкурентов + связка с брендом DIESEL.
- Прогон через маркетинг/продажи/медийность/виральность/хук, воронку, кодовые слова,
  описание и Humanizer — заточка на гарантированный миллион.

### 3. СБОРКА РОЛИКА
- 4K-стоки под тему, смена каждые ~3с (дедуп по USED.md).
- **Анимации/наложения/инфографика ПИШУТСЯ КОДОМ С НУЛЯ под ЭТОТ ролик — НОВАЯ визуальная
  метафора/геометрия/моушен под конкретные цифры сегмента. ЗАПРЕЩЕНО звать пресеты из готовой
  библиотеки build_accents.py «поменяв текст/стиль» — это шаблон и БРАК (претензия Даниэля 22.07).
  Обязательный порядок и реестр занятых форм — `USED_ANIM.md`: прочитать → придумать 8–12 новых
  форм, которых там ещё нет → написать кодом → отрендерить → дописать в журнал USED_ANIM.md.**
- Проверка разнообразия: музыка / анимации / инфографика / наложения / стоки / нарезка —
  разные и ВНУТРИ ролика, и относительно прошлых.
- Обложка первым кадром через FLUX/Gemini (фолбэк при лимите).
- **МУЗЫКА — СВОЯ на каждый ролик, без повторов.** Выбор под тему/сценарий/бакет:
  `MUSIC=$(python3 pipeline/pick_music.py <viral|useful|sales> <theme-slug>)` → выставить
  `MUSIC_TRACK`+`MUSIC_SEED` в env перед `audio.py`. Дедуп `music/used_music.json`, реестр `USED_MUSIC.md`.
  `audio.py` доп. варьирует секцию/темп/EQ по MUSIC_SEED. Один общий бэд на все ролики = БРАК (претензия 22.07).
  SFX-палитру тоже тасовать под сегменты (не повторять один и тот же план).
- Проверка озвучки, пауз, ударений.

### 4. ПУБЛИКАЦИЯ
- Финальная проверка: красота, разнообразие, динамика, число кадров и наложений,
  ноль повторов, оценка залёта на миллион.
- Публикация в 3 сети СВОЕЙ пачки + отчёт в бота. Описание = польза + воронка +
  15–30 ключевых поисковых фраз внизу без хештегов + dieselcompany.pro.

### 5. ОБЩЕНИЕ И ОТЧЁТ
- Отвечаю на комментарии в YouTube и Instagram и в ЛС Instagram обеих пачек —
  веду на сайт/канал или продаю (кодовые слова, воронка).
- **Каждое утро 10:00 МСК (07:00 UTC)** — полная аналитика в бота по каждой соцсети
  и пачке: просмотры, комментарии, лайки, репосты, подписчики, переходы на сайт,
  жалобы, анализ.

## АКТИВНОСТЬ (квота на ДЕНЬ, не на ролик)
10 лайков · 5 комментариев · 3 подписки в день (безопасные лимиты от бана).
Разное на разных аккаунтах/пачках. Остальная активность — перед каждым роликом (см. этап 1).

## ⛔ АКТИВНОСТЬ IG — НА ПАУЗЕ (21.07, важно!)
Ночные авто-прогоны активности с серверного IP привели к human-check на **diesel_cargo**
(`/accounts/suspended/` → «подтвердите, что вы человек», не бан). ПРАВИЛА:
- **НЕ запускать `diesel_engage.mjs`, пока есть файл `cfg/ENGAGE_PAUSED.txt`.** Проверять перед Этапом 1.
- `ig_alive.mjs` теперь различает ALIVE / DEAD / **CHECKPOINT**. Если CHECKPOINT — НЕ автоматизировать
  прохождение (капча ужесточится), а коротко написать Даниэлю: пройти human-check в приложении IG
  с ТЕЛЕФОНА (реальный мобильный IP), кнопка «Продолжить» (~30 сек).
- Активность включать ТОЛЬКО после: (1) Даниэль прошёл верификацию, (2) настроен мобильный/резид. прокси
  для IG-браузера, (3) лимиты снижены (≤3 лайка/день на аккаунт, паузы 30–90с). До этого — только постинг.
- Постинг с checkpoint-аккаунта временно не пройдёт (публикатор не блокирует очередь: YT+TikTok выходят,
  IG ретраит и пропускает). Пачка B (diesel_kitay) не затронута.

## ОБЛОЖКИ = ПРЕВЬЮ (обязательно на каждый ролик)
Наша дизайн-обложка — ПЕРВЫЙ кадр ролика и ОНА ЖЕ ставится превью на площадках:
- В очередь класть `queue/NNN/cover.jpg`. `publish_next.py` передаёт её: YouTube — `yt_upload.py … cover.jpg`
  (ставит thumbnail через API), Instagram — `IG_COVER` (обложка из первого кадра). TikTok — авто.
- **YouTube-блокер:** кастомные превью доступны ТОЛЬКО после верификации канала (youtube.com/verify,
  подтверждение по телефону). Пока каналы не подтверждены — YT отдаёт 403 на thumbnail (код готов,
  сработает сам после верификации). Даниэлю: подтвердить оба канала.

## ЭТАП 1 ПЕРЕД КАЖДОЙ СБОРКОЙ — ОБЯЗАТЕЛЬНЫЙ ШЛЮЗ (не пропускать!)
ПЕРЕД каждым роликом ОБЯЗАТЕЛЬНО сгенерировать свежий документ-анализ и СТРОИТЬ ролик ИЗ НЕГО:
`python3 competitor_recon.py <out.txt> "<тема ролика>" <ниша:квадроцикл|мотоцикл|эндуро|гидроцикл>`
(запускать ЛОКАЛЬНО в сессии — VPS деградирован зависшими root-процессами, там yt-dlp тормозит).
Модуль: широкие нишевые запросы → префильтр по view_count из flat-поиска → ТОЛЬКО ролики 1M+ (до 8),
реальные заголовки+просмотры+каналы+ссылки, формат, «почему залетел», транскрипт-хук топ-3, сводный
разбор частых слов/ER, воронка, и ПРИЁМЫ под наш ролик. Дедуп recon_seen.json (каждый раз НОВЫЕ ролики).
Документ → в diesel-бот (sendDocument). Сценарий/хук/формат ролика брать ИЗ приёмов этого документа.
ЧЕСТНЫЕ ЛИМИТЫ (не выдумывать): YouTube ратолимитит глубокие запросы → лайки/комменты часто «н/д»
(просмотры+ссылки всегда реальные); IG-метрики и полная раскадровка/скачивание требуют куки/прокси/ребута VPS.
Активность (лайки/комменты/подписки) — на ПАУЗЕ до прокси (см. блок выше), не запускать.
СТАРЫЙ модуль (тонкий, 2 ролика) заменён — теперь 8 роликов 1M+ с разбором.


## ИНФРАСТРУКТУРА АВТОНОМНОСТИ
- **Почасовой супервайзер-мост** (`trig_011d9aX3dyTpqZVVNCkrkEKH`) фигачит в этот чат
  каждый час: проверяет ВСЁ (диск VPS/ENOSPC, IG-сессии обеих пачек, рамп, очереди,
  публикацию, комменты/ЛС, аналитику 10:00), чинит и двигает план. Ноль проёбов.
- Публикатор `publish_next.py` — batch-aware, retry-safe, IG не блокирует очередь.
- Реестры разнообразия: `USED.md`. Состояние рампа: `cfg/ramp_state.json`.
- Ветка разработки: `claude/new-session-xxozd5`.

## ⚠️ ПУБЛИКАЦИЯ — ТОЛЬКО ОДИН publish_next.py ЗА РАЗ
Не запускать несколько publish_next.py параллельно (гонка за очередь → риск дубля на YT/TikTok).
Перед запуском: `pkill -9 -f publish_next.py; sleep 2`, затем один прогон. Публикатор берёт ОДИН элемент очереди за вызов — для следующего запускать снова после того, как предыдущий ушёл в published.

## ⚠️ publish_next.py ЗАВИСАЕТ — ИСПОЛЬЗУЙ post_direct.py
22.07: publish_next.py стабильно виснет ДО первого лога (среда попорчена застрявшими root-процессами `playwright install` ~14ч, диск тесный). Надёжный путь публикации: `python3 post_direct.py NNN` — читает meta.batch, постит YT+TikTok+IG нужной пачки НАПРЯМУЮ (yt_upload/hooppy/ig_reel_post), помечает _done, двигает в published. IG best-effort (checkpoint пропускает). Дубль-защита: проверяет _done.
ПРИМЕЧАНИЕ: 2 root-процесса `playwright install` висят с ENOSPC-инцидента — okoposter их не убьёт, нужен root/ребут VPS (Даниэлю). Пока не мешают (idle), постинг идёт через post_direct.
