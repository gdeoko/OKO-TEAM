/* Rocket VPN. Глыба льда со знаком внутри и приборной выноской.

   ЗАЧЕМ ОНА ЕСТЬ. Сценарий владельца дословно: «можно и не карточки,
   можно придумать что-то поинтереснее, как у igloo сделать глыбы льда,
   которые влетают в стену, разбивая часть кирпичей, и втыкаются в неё, а
   текст как будто внутри глыбы... взять именно такие же формы льда, как
   у них, сделать разные формы, внутри льда у нас крутые 3D иконки... от
   самой глыбы посмотри, как сделано у igloo: у них там какие-то линии,
   стрелочки и текст, типа описание, сделано очень круто».

   Его же замечания и что по каждому сделано:
     · «сделай чтобы эти глыбы льда были более реалистичные» - тело
       строится теперь режущими плоскостями и получает настоящие плоские
       грани, рёбра и полосу скола между ними; свет берётся из панорамы окружения, снятой
       у них, вместо одного забитого в код направления;
     · «глыбы льда по текстурам не такие, как нужно, должны быть такие
       же как igloo» - у них фактуру даёт КАРТА НОРМАЛЕЙ 2048 и карта
       шероховатости 1024 с двумя резко разными зонами. Обе считаются
       процедурно по снятым числам (см. Ф_ЛЁД): направленная штриховка
       на полированной грани, крупное зерно на изломе, слои и трещины в
       толще;
     · «формы разнообразнее сделать как у igloo» - три РАЗНЫХ закона
       построения вместо одного с тремя растягами (см. ФОРМЫ ниже);
     · «иконки внутри глыб льда вообще не видно» - разобрано числом
       (см. ВТОРОЙ ПРОХОД ЗНАКА в функции иконка): при тёмной кладке
       акта знак под слоем льда не может стать темнее его больше чем на
       семь пунктов, сколько его ни крась. Знак печатается дважды, до
       льда и после, и садится на тридцать три пункта ниже - Вебер 0.25
       при их 0.38;
     · «посмотри вот эти линии, полоски, стрелки и надписи, которые идут
       от этих льдышек» - выноска перебрана в их приборный вид: узел,
       веер волосяных лучей, белая ЗАЛИВНАЯ плашка с тёмным текстом.

   ПОКАЗАТЕЛЬ ПРЕЛОМЛЕНИЯ: ПРАВИЛЬНОЕ ЧИСЛО 1.2727. В разборе их сборки
   стоит ior=1.18, у нас стояло табличное для льда 1.31, и оба неверны.
   В их коде ior=1.18 присваивается ПЕРВЫМ, а следом идёт
   reflectivity=0.3; reflectivity у MeshPhysicalMaterial это сеттер: он
   пересчитывает показатель по формуле
   (1+0.4*r)/(1-0.4*r), то есть ЗАТИРАЕТ 1.18 на 1.2727. Отражение в лоб
   выходит F0=1.44 процента (у настоящего льда 1.79 при 1.309). Разбег
   каналов дисперсии у них 0.1, у нас стоял 0.035 - это и есть то
   расхождение втрое, из-за которого радугу приходилось вытягивать силой
   до бензиновой плёнки.

   ЧТО ЗАМЕРЕНО У НИХ (кадр /tmp/игло/плёнка-ПК-009.png, габариты из
   /tmp/игло/геометрия.json, разбор сеток в /tmp/глыбы-дамп/форма2.json,
   разбор карт и кадров в /tmp/глыбы-текстуры):

     cube1.drc  2.025 x 2.404 x 1.567, 6494 треугольника
     cube2.drc  2.141 x 2.130 x 1.727, 6186 треугольников
     cube3.drc  1.576 x 2.456 x 1.427, 8038 треугольников

   ТРИ РАЗНЫХ ЗАКОНА, А НЕ ОДИН С ТРЕМЯ РАСТЯГАМИ. Это и есть то, что
   владелец назвал «формы разнообразнее»:
     cube1 - ПРАВИЛЬНАЯ ШЕСТИГРАННАЯ ПРИЗМА со сломанными торцами. Шесть
       боковых плоскостей стоят к оси под 89.9-90.0 градуса, азимуты идут
       шагом 59.3..61.0 градуса, плоскости отстоят от центра на
       0.7235 плюс-минус 0.024. Ось наклонена от вертикали на 14.0
       градуса. Боковые грани держат 56.1 процента площади.
     cube2 - ПРЯМОУГОЛЬНЫЙ БРУСОК. Шесть граней попарно под 89.8-90.3
       градуса, все шесть на 0.830 плюс-минус 0.013 от центра, коробка
       развёрнута на 20.0 градуса вокруг Z. Грани держат 53.9 процента.
     cube3 - НЕПРАВИЛЬНЫЙ ОБЛОМОК. Восемь-десять плоскостей под
       произвольными углами (45.6, 59.8, 66.9, 72.7, 91.8, 110.0, 131.0,
       155.7 между шестью главными), расстояния гуляют 0.412..1.007.
       Выпуклость 82 процента, шарообразность 0.661.

   КРОМКИ У НИХ НЕ ОСТРЫЕ И НЕ СКРУГЛЁННЫЕ. Прямых рёбер грань-в-грань
   на всю сетку 1..11 штук из десяти тысяч: между двумя гранями лежит
   ПОЛОСА сколотых треугольников шириной 0.22 (cube2), 0.45 (cube1) и
   0.68 (cube3) мировых единиц. Поэтому здесь плоскости режут заготовку,
   а стык двух плоскостей размывается рваной полосой.

   МЕЛКАЯ ФАКТУРА У НИХ В КАРТАХ, А НЕ В ВЕРШИНАХ. Отклонение их сетки
   от местной плоскости растёт быстрее радиуса (показатель 1.3-1.55),
   значит геометрия на мелком масштабе ГЛАДКАЯ, а слоистость, штриховка
   и зерно лежат в карте нормалей 2048 и карте шероховатости 1024. Их
   файлы в репозиторий класть нельзя, поэтому обе карты считаются
   процедурно в шейдере по снятым числам (см. Ф_ЛЁД).

   API (не менять, зовут снаружи - assets/rv-снаряды.js):
     RV_ГЛЫБА.собрать(мир, о) -> { корень, лёд, иконка, выноска, мат, кадр(...) }
     RV_ГЛЫБА.формы() -> имена форм */
(function (g) {
  "use strict";

  var T = null;

  function зажать(v, н, в) { return v < н ? н : (v > в ? в : v); }

  /* ── Шейдер льда ───────────────────────────────────────────────
     Собран по их материалу WL (App3D-f554a111.js, смещение 1249074) и
     по замерам их карт. Что откуда:

     ОКРУЖЕНИЕ. Сцена кубов у igloo освещена ТОЛЬКО картой окружения:
     ни одного источника света в ней нет вовсе. Отсюда и берётся живой
     широкий блик, который у нас раньше подменялся одним забитым в код
     направлением света и оттого читался пластиком. Их cubes_env.exr
     разобран по поясам: небо 0.428/0.487/0.618, пояс горизонта 2.21
     нейтральный, низ 0.22/0.23/0.27, одно пятно солнца на высоте 28.8
     градуса и азимуте минус 11. Средняя по всей панораме 0.919. Файл их,
     класть его нельзя, поэтому те же пояса считает функция env().

     ПРЕЛОМЛЕНИЕ И ЕГО ПОКАЗАТЕЛЬ. У них в коде стоит ior=1.18, а СЛЕДОМ
     reflectivity=0.3, и это присвоение через сеттер перезаписывает
     показатель: ior=(1+0.4*0.3)/(1-0.4*0.3)=1.2727. Значит правильное
     число не 1.18 и не наши прежние 1.31, а 1.2727, и отражение в лоб
     выходит F0=1.44 процента. Разбег каналов uChromaticAberration у них
     0.1, у нас стояло 0.035 - втрое меньше нужного, оттого дисперсия и
     не читалась без задранной силы uRainbow.

     ШЕРОХОВАТОСТЬ ДВУМЯ ЗОНАМИ. Их карта шероховатости множится на
     базовые 0.65 и даёт на полированной грани 0.21-0.30, на матовом
     изломе 0.37-0.38. Разница ровно вдвое, и она правит и ширину блика,
     и силу сбоя нормали при преломлении (у них distortionNormal идёт
     как roughness в квадрате). Зона приходит из геометрии атрибутом
     aFace: единица на плоской грани, ноль в полосе скола.

     КАРТА НОРМАЛЕЙ. На полированных гранях у них тонкая НАПРАВЛЕННАЯ
     штриховка вдоль длинной оси грани: период 0.06-0.15 мировых единиц,
     наклон ско 2.1 градуса, размах от минус 6.9 до плюс 8.1. На изломе
     наклон совсем другой: медиана 25 градусов, p90 45, p99 70, пятна
     размером 16-64 текселя при 456 текселях на единицу, то есть
     0.035-0.14 мировых единиц. Обе зоны считаются здесь: штриховка
     синусами по местной координате vHatch, излом двумя октавами шума.

     ЦВЕТ. Их лёд на экране ТЕМНЕЕ фона на 23-28 процентов (тело 108/116/132
     при фоне 150/155/168), а наш прежний был СВЕТЛЕЕ фона на 3-5 - оттого
     глыбы и читались целлофаном, а знак внутри пропадал. Тело здесь
     тёмное, светлое только отражение по кромке. */
  var В_ЛЁД = [
    "attribute float aFace;",
    "attribute float aRnd;",
    "varying vec3 vPos;",
    "varying vec3 vWPos;",
    "varying vec3 vWNor;",
    "varying vec3 vDirW;",
    "varying float vHatch;",
    "varying float vFace;",
    "varying float vRnd;",
    "varying float vMvz;",
    "void main(){",
    "  vPos = position;",
    "  vFace = aFace;",
    "  vRnd = aRnd;",
    "  vec3 nL = normalize(normal);",
    /* Направление штриховки одно на грань: aRnd постоянен внутри грани,
       значит и направление стоит на месте, пока грань не сменится. У них
       штриховка тоже идёт одним направлением на грань, вдоль её длинной
       оси, а не случайным полем. */
    "  float ang = aRnd * 6.2831853;",
    "  vec3 dirL = normalize(vec3(cos(ang), sin(ang) * 0.62 + 0.22, sin(ang * 2.13 + 1.7)));",
    "  dirL = dirL - nL * dot(nL, dirL);",
    "  float dl = length(dirL);",
    "  dirL = dl > 0.02 ? dirL / dl : normalize(cross(nL, vec3(0.0, 1.0, 0.0)) + vec3(1e-4));",
    /* Координата поперёк штриха считается в вершине: на плоской грани
       она линейна, и интерполяция даёт её точно, без единого лишнего
       умножения во фрагменте. */
    "  vHatch = dot(position, dirL);",
    "  mat3 M = mat3(modelMatrix);",
    "  vWNor = normalize(M * nL);",
    "  vDirW = normalize(M * dirL);",
    "  vec4 wp = modelMatrix * vec4(position, 1.0);",
    "  vWPos = wp.xyz;",
    "  vec4 mv = modelViewMatrix * vec4(position, 1.0);",
    "  vMvz = mv.z;",
    "  gl_Position = projectionMatrix * mv;",
    "}"
  ].join("\n");

  var Ф_ЛЁД = [
    "uniform vec3 uIce;",
    "uniform vec3 uDeep;",
    "uniform vec3 uEdge;",
    "uniform float uTime;",
    "uniform float uOpacity;",
    "uniform float uHot;",
    "uniform float uFly;",
    "uniform float uIor;",
    "uniform float uCa;",
    "uniform float uRainbow;",
    "uniform float uCore;",
    "uniform float uEnv;",
    "uniform float uCap;",
    "uniform float uFine;",
    "varying vec3 vPos;",
    "varying vec3 vWPos;",
    "varying vec3 vWNor;",
    "varying vec3 vDirW;",
    "varying float vHatch;",
    "varying float vFace;",
    "varying float vRnd;",
    "varying float vMvz;",
    "float hash13(vec3 p){",
    "  p = fract(p * 0.1031);",
    "  p += dot(p, p.yzx + 33.33);",
    "  return fract((p.x + p.y) * p.z);",
    "}",
    "float noise3(vec3 p){",
    "  vec3 i = floor(p), f = fract(p);",
    "  f = f * f * (3.0 - 2.0 * f);",
    "  float n000 = hash13(i), n100 = hash13(i + vec3(1,0,0));",
    "  float n010 = hash13(i + vec3(0,1,0)), n110 = hash13(i + vec3(1,1,0));",
    "  float n001 = hash13(i + vec3(0,0,1)), n101 = hash13(i + vec3(1,0,1));",
    "  float n011 = hash13(i + vec3(0,1,1)), n111 = hash13(i + vec3(1,1,1));",
    "  return mix(mix(mix(n000,n100,f.x), mix(n010,n110,f.x), f.y),",
    "             mix(mix(n001,n101,f.x), mix(n011,n111,f.x), f.y), f.z);",
    "}",
    /* ── ОКРУЖЕНИЕ ВМЕСТО ЗАБИТОГО СВЕТА ───────────────────────
       Панорама igloo cubes_env.exr, снятая по поясам и пересказанная
       числами: низ 0.22/0.23/0.27, небо 0.428/0.487/0.618, между ними
       яркий пояс горизонта до 2.21 нейтрального, плюс одно пятно солнца
       на высоте 28.8 градуса и азимуте минус 11. Средняя по панораме у
       них 0.919, у этой функции 0.95 - на неё и делим тело ниже.

       Шероховатость размывает пояс и солнце: гладкая грань ловит узкую
       яркую полосу, матовый излом - широкое мягкое пятно. Это дешёвая
       замена мип-уровням их карты, и на глаз она работает так же. */
    "vec3 env(vec3 d, float rough){",
    "  float h = clamp(d.y, -1.0, 1.0);",
    "  float sg = 0.165 + rough * 0.62;",
    "  float band = exp(-(h * h) / (2.0 * sg * sg));",
    "  vec3 baza = mix(vec3(0.22, 0.23, 0.27), vec3(0.428, 0.487, 0.618),",
    "                  smoothstep(-0.28, 0.34, h));",
    "  vec3 c = baza + band * 1.60 * vec3(0.985, 0.995, 1.0);",
    "  vec3 sun = vec3(-0.1673, 0.4818, 0.8608);",
    "  float sd = max(dot(d, sun), 0.0);",
    "  c += vec3(1.0, 0.97, 0.90) * pow(sd, mix(160.0, 10.0, rough)) * mix(7.0, 1.2, rough);",
    "  return c;",
    "}",
    "void main(){",
    "  vec3 nW = normalize(vWNor);",
    "  vec3 V = normalize(cameraPosition - vWPos);",
    "  float face = clamp(vFace, 0.0, 1.0);",
    /* ── ШЕРОХОВАТОСТЬ ДВУМЯ ЗОНАМИ ───────────────────────────
       Их карта после множителя материала 0.65 даёт 0.21-0.30 на
       полированной грани и 0.373-0.384 на изломе. Разброс по граням
       плюс-минус 0.025 берётся из aRnd, чтобы соседние грани не
       блестели одинаково. */
    "  float rough = mix(0.378, 0.215, face) + (vRnd - 0.5) * 0.05;",
    "  rough = clamp(rough, 0.16, 0.46);",
    /* Касательный базис строится от направления штриховки: оно уже
       лежит в плоскости грани и постоянно внутри неё. */
    "  vec3 t1 = vDirW - nW * dot(nW, vDirW);",
    "  float lt = length(t1);",
    "  t1 = lt > 1e-4 ? t1 / lt : normalize(cross(nW, vec3(0.0, 1.0, 0.0)) + vec3(1e-4));",
    "  vec3 t2 = cross(nW, t1);",
    /* ── ШТРИХОВКА ПОЛИРОВАННОЙ ГРАНИ ──────────────────────────
       Период 0.052..0.125 местной единицы: у них 0.06-0.15 при высоте
       модели 2.4, у нас высота 2.1, то есть те же доли тела. Наклон
       ско 2.1 градуса это tan 0.037, размах до 8 градусов это tan 0.14.
       Сумма двух синусов ходит примерно в единицу, множитель 0.13
       ставит её ровно в этот размах. */
    "  float per = mix(0.052, 0.125, fract(vRnd * 7.31));",
    "  float ph = vHatch / per;",
    "  float sh = sin(ph * 6.2831853) * 0.55 + sin(ph * 2.61 + vRnd * 9.0) * 0.45;",
    /* ── ЗЕРНО ИЗЛОМА ──────────────────────────────────────────
       Пятна 16-64 текселя при 456 текселях на единицу это 0.035-0.14
       мировых единиц, то есть частоты 7..29 на единицу их модели и
       17..41 на единицу нашей. Наклон на изломе: медиана 25 градусов
       (tan 0.47), p90 45 (tan 1.0), p99 70 (tan 2.7). Сила 1.05 при
       шуме с размахом около единицы даёт медиану около 25 градусов и
       хвост за 45. Дословные 1.45 пробовались первыми: на кадре полоса
       скола вспыхивала белым кружевом, потому что случайная нормаль
       ловила яркий пояс горизонта на каждой второй точке. */
    "  float gx = noise3(vPos * 17.0) - 0.5;",
    "  float gy = noise3(vPos * 17.0 + vec3(19.3, 7.7, 31.1)) - 0.5;",
    "  if (uFine > 0.5) {",
    "    gx += (noise3(vPos * 41.0) - 0.5) * 0.58;",
    "    gy += (noise3(vPos * 41.0 + vec3(5.1, 23.9, 11.3)) - 0.5) * 0.58;",
    "  }",
    "  float sila = mix(1.05, 0.14, face);",
    "  vec3 n = normalize(nW + t1 * (sh * 0.13 * face + gx * sila) + t2 * (gy * sila));",
    "  float NdV = max(dot(n, V), 0.0);",
    /* Френель по Шлику с настоящим F0 льда их сборки: ior 1.2727 даёт
       1.44 процента в лоб. Прежние pow(1-NdV, 6) с весом 0.30 были
       выдумкой и давали белую кайму там, где у них её нет. */
    "  float shl = pow(1.0 - NdV, 5.0);",
    "  float F0 = 0.0144;",
    "  float F = (F0 + (1.0 - F0) * shl) * mix(1.0, 0.55, rough);",
    "  vec3 refl = env(reflect(-V, n), rough) * uEnv;",
    /* ── ДИСПЕРСИЯ ПО ИХ СХЕМЕ ─────────────────────────────────
       Красный идёт с показателем ior, зелёный с ior*(1+uCa), синий с
       ior*(1+2*uCa) - те же множители, что в их шейдере, и тот же
       разбег 0.1. Преломлённый луч читает окружение, а не выдуманную
       полосу: только так разница каналов остаётся пастельной. Толщу
       луч видит размытее поверхности, поэтому шероховатость под
       преломление берётся в полтора раза больше. */
    "  vec3 I = -V;",
    "  float eta = 1.0 / max(uIor, 1.001);",
    "  float rt = min(0.75, rough * 1.55);",
    "  vec3 tr = vec3(env(refract(I, n, eta), rt).r,",
    "                 env(refract(I, n, eta / (1.0 + uCa)), rt).g,",
    "                 env(refract(I, n, eta / (1.0 + 2.0 * uCa)), rt).b);",
    "  float trL = (tr.r + tr.g + tr.b) * 0.33333;",
    /* ── ТОЛЩА: СЛОИ, ТРЕЩИНЫ, ЗЕРНО ──────────────────────────
       Три вещи, которые владелец назвал словом «текстуры».

       СЛОИ. Лёд намерзает слоями, и на их кадре 009 по телу глыбы идёт
       пологая слоистость. Синус по высоте с частотой 11 на местную
       единицу (то есть около двадцати слоёв на всю высоту) плюс сбивка
       шумом, чтобы слои не были линейкой.

       ТРЕЩИНЫ. Трещина во льду это ПЛОСКОСТЬ, а не клякса: первый заход
       брал гребень объёмного шума и давал жёлтых червей по всему телу.
       Здесь два семейства параллельных плоскостей разного наклона,
       сбитых тем же шумом, что уже посчитан на муть, - лишней выборки
       не стоит. Ширина 0.045 и 0.035 от шага семейства даёт волосяную
       нить, как в настоящем куске льда.

       ЗЕРНО. Мелкий шум, он же кормит муть.

       Поглощения у них нет вовсе (volumeAttenuation первой строкой
       отдаёт единицу), поэтому толща только красит, а не гасит. */
    "  float mut = noise3(vPos * 2.6 + vec3(0.0, uTime * 0.05, 0.0)) * 0.62",
    "            + noise3(vPos * 7.4) * 0.38;",
    "  float sloi = noise3(vPos * vec3(2.1, 11.0, 2.1)) - 0.5;",
    "  float sl1 = fract(dot(vPos, vec3(0.31, 0.86, -0.41)) * 6.0 + (mut - 0.5) * 1.6);",
    "  float sl2 = fract(dot(vPos, vec3(-0.74, 0.22, 0.63)) * 4.3 + sloi * 2.2);",
    "  float tresh = (1.0 - smoothstep(0.0, 0.045, abs(sl1 - 0.5)))",
    "              + (1.0 - smoothstep(0.0, 0.035, abs(sl2 - 0.5))) * 0.7;",
    "  vec3 baza = mix(uDeep, uIce, clamp(mut * 0.90 + sloi * 0.22 + 0.28, 0.0, 1.0));",
    /* Тело следует за яркостью преломлённого окружения, но НЕ ЛИНЕЙНО:
       пояс горизонта у панорамы доходит до 2.2, и на прямой зависимости
       тело уезжало до линейного 0.40 вместо 0.24 - ровно та молочная
       муть, из-за которой знак внутри и переставал читаться. Здесь
       вклад зажат, но не срезан: без углового хода все грани выходят
       одной яркости, и тело читается матовым пластиком. */
    "  vec3 color = baza * (0.52 + 0.62 * clamp(trL / 1.90, 0.0, 1.0));",
    /* Трещина светится сама: она отражает свет обеими своими стенками,
       и потому в настоящем льду видна светлой нитью, а не тёмной. Цвет
       холодный и свой, не от uIce: на добавке цветом тела нить уходила
       в тёплое и читалась жёлтым червём. */
    "  color += vec3(0.80, 0.88, 1.0) * tresh * 0.085;",
    /* В кадр идёт только РАЗНИЦА каналов преломления. Сама картинка
       окружения уже учтена яркостью выше, а размах каналов у них не
       больше 50 из 255 на точку - при вкладе всей выборки он выходил
       за полторы сотни. */
    "  color += (tr - vec3(trL)) * uRainbow * (0.10 + shl * 0.35);",
    "  color = color * (1.0 - F) + refl * F;",
    /* Дальше от камеры лёд глуше: воздух подземелья не пустой. */
    "  float dal = 1.0 - smoothstep(26.0, 110.0, -vMvz);",
    "  color *= mix(0.80, 1.0, dal);",
    /* Потолок. У них по всем двенадцати кадрам с глыбами НЕТ НИ ОДНОЙ
       точки ярче 200 из 255: лёд целиком лежит в поясе 60..185. Через
       плёнку сборки экранным 185 отвечает линейное 0.44, экранным 200 -
       линейное 0.586, поэтому потолок стоит между ними.

       Потолок стоит ДО вспышки удара: их число про лёд в покое, а удар
       о стену обязан пробить его и уйти в белое, иначе попадание не
       читается вовсе. */
    "  color = min(color, vec3(uCap));",
    /* Вспышка при ударе о стену и при наведении. */
    "  color += uEdge * uHot * 0.7;",
    /* В полёте лёд светится по кромке сильнее: у летящего предмета есть
       свой ход, и свет по краю читается его следом. */
    "  color += uEdge * shl * uFly * 0.5;",
    /* ── ПЛОТНОСТЬ ────────────────────────────────────────────
       У igloo лёд НЕПРОЗРАЧНЫЙ, а знак внутри виден потому, что
       попадает в снимок сцены и читается через преломление. Отдельного
       прохода со снимком у нас нет, поэтому знак виден смешиванием, и
       плотность подобрана под их же контраст: тело темнее фона на
       27 процентов, знак темнее тела по Веберу на 0.38. Середина
       держится на uCore, кромка добирает Френелем. */
    "  float a = clamp(uCore + shl * (1.0 - uCore) * 0.92, 0.0, 1.0) * uOpacity;",
    "  gl_FragColor = vec4(color, a);",
    "}"
  ].join("\n");

  /* ── Шейдер знака внутри ───────────────────────────────────────
     У igloo знак это отдельная модель с ЗАПЕЧЁННОЙ картой цвета и
     БЕЗ освещения (MeshBasicMaterial): он не зависит ни от света сцены,
     ни от ступени качества и всегда выглядит одинаково. Замер их карт:
     abstractlogo средняя 68/72/83, overpass_logo 78/81/94, pudgy
     46/48/54, все три синеватые (синий выше красного на 12-16), сотый
     процентиль доходит до 200-212, то есть в запечённой картинке есть и
     полутени, и редкие блики.

     Здесь то же самое: тёмное тело с мягкой полутенью и редким узким
     бликом, никакой кромки и никакого свечения. Прежняя сборка светила
     знаку холодной каймой uRim и давала контраст к льду по Веберу 0.70
     при их 0.38 - знак читался вырезанным силуэтом, а не предметом
     внутри толщи. */
  var В_ЗНАК = [
    "varying vec3 vN;",
    "varying vec3 vV;",
    "void main(){",
    "  vN = normalize(normalMatrix * normal);",
    "  vec4 mv = modelViewMatrix * vec4(position, 1.0);",
    "  vV = mv.xyz;",
    "  gl_Position = projectionMatrix * mv;",
    "}"
  ].join("\n");

  var Ф_ЗНАК = [
    "uniform vec3 uBody;",
    "uniform vec3 uRim;",
    "uniform float uLift;",
    "uniform float uAlpha;",
    "varying vec3 vN;",
    "varying vec3 vV;",
    "void main(){",
    "  vec3 n = normalize(vN);",
    "  vec3 V = normalize(-vV);",
    "  if (!gl_FrontFacing) n = -n;",
    "  vec3 L = normalize(vec3(-0.38, 0.80, 0.56));",
    "  float d = max(dot(n, L), 0.0);",
    "  vec3 H = normalize(L + V);",
    /* Полутень мягкая: множитель гуляет 0.72..1.27 вокруг единицы,
       поэтому средняя яркости знака остаётся ровно uBody. У них
       медиана карты 59/68/88 при средней 68/72/83, то есть разброс
       примерно такой же. */
    /* Блик сильный намеренно. Знак внутри тёмной толщи читается прежде
       всего отсветами по граням: у igloo в запечённой карте сотый
       процентиль доходит до 200-212 при средней 68-78, то есть блики
       там ярче тела втрое. Ими и держится форма предмета. */
    "  float spec = pow(max(dot(n, H), 0.0), 24.0);",
    "  vec3 col = uBody * (0.72 + 0.55 * d) + uRim * spec * 0.55 + uBody * uLift;",
    /* ── КРОМКА ЗНАКА РАЗМЫВАЕТСЯ ──────────────────────────────
       У igloo знак виден через преломление, и его край размывается на
       пять процентов ширины глыбы: замер кадра 009 дал переход от 20 до
       80 процентов на 22 точки при глыбе в 431. У нас знак печатается
       вторым проходом поверх льда, и без этого он выходил вырезанным по
       живому - резкая кромка в одну точку читалась наклейкой на грани.
       На силуэте знака нормаль уходит от глаза, и второй проход там
       слабеет: фактура льда над краем проступает, и знак садится в
       толщу. На первом проходе прозрачности нет вовсе, и множитель туда
       не доходит. */
    "  float kr = max(dot(n, V), 0.0);",
    /* Светлый кант по силуэту знака. Замер боевого кадра показал, что
       ореол плёнки съедает половину контраста: в стенде без него знак
       сидел на 29 пунктов ниже льда, в акте осталось 15-27. Тонкий
       светлый обвод восстанавливает читаемость не яркостью тела, а
       формой - у igloo знак тоже узнаётся силуэтом, а не заливкой. */
    "  col += uRim * pow(1.0 - kr, 3.0) * 0.35;",
    "  gl_FragColor = vec4(col, uAlpha * mix(0.72, 1.0, kr));",
    "}"
  ].join("\n");

  /* ── Формы ─────────────────────────────────────────────────────
     Имена оставлены прежними: их передают из разметки
     (frame.html, data-форма) и менять их нельзя. У каждой теперь СВОЙ
     ЗАКОН ПОСТРОЕНИЯ, снятый со своей их модели, и свои пропорции,
     нормированные по полувысоте 1.05:

       кристалл <- cube1  шестигранная призма   0.885 : 1.05 : 0.685
       брусок   <- cube2  прямоугольный брусок  1.055 : 1.05 : 0.851
       плита    <- cube3  неправильный обломок  0.674 : 1.05 : 0.610
       обломок  - второй обломок, свои плоскости, между двумя первыми

     Полувысота 1.05 у всех: слот под глыбу в rv-снаряды.js посчитан от
     неё (кладка 4.73 делится на три слота по 1.58), и глыба выше сломала
     бы расстановку подписей. */
  var ФОРМЫ = ["кристалл", "брусок", "плита", "обломок"];
  var ЗАКОН = {
    "кристалл": "призма",
    "брусок":   "коробка",
    "плита":    "обломок",
    "обломок":  "обломок"
  };
  var РАСТЯГ = {
    "кристалл": [0.885, 1.05, 0.685],
    "брусок":   [1.055, 1.05, 0.851],
    "плита":    [0.674, 1.05, 0.610],
    "обломок":  [0.820, 1.05, 0.720]
  };
  /* Ширина полосы скола между двумя гранями. У них она замерена по
     сеткам: 0.45 мировых единиц у призмы, 0.22 у бруска, 0.68 у
     обломка. Здесь число в единицах заготовки (радиус около 0.8), и
     подобрано так, чтобы доля ПЛОСКОЙ зоны легла в их же числа по
     картам шероховатости: 19.7 процента у призмы, 32.3 у бруска,
     17.0 у обломка. */
  var ПОЛОСА = { "призма": 0.44, "коробка": 0.52, "обломок": 0.30 };

  function шумСемя(семя) {
    var s = семя * 9301 + 49297;
    return function () { s = (s * 9301 + 49297) % 233280; return s / 233280; };
  }

  /* Тот же шум, что в шейдере: значения в узлах целочисленной решётки,
     между узлами сглаженная ступенька. Нужен здесь, чтобы полоса скола
     была рваной, а не ровной канавкой. */
  function хеш3(x, y, z) {
    var h = Math.sin(x * 127.1 + y * 311.7 + z * 74.7) * 43758.5453;
    return h - Math.floor(h);
  }
  function шум3(x, y, z) {
    var ix = Math.floor(x), iy = Math.floor(y), iz = Math.floor(z);
    var fx = x - ix, fy = y - iy, fz = z - iz;
    fx = fx * fx * (3 - 2 * fx); fy = fy * fy * (3 - 2 * fy); fz = fz * fz * (3 - 2 * fz);
    function л(a, b, t) { return a + (b - a) * t; }
    function у(a, b, c) { return хеш3(ix + a, iy + b, iz + c); }
    return л(л(л(у(0, 0, 0), у(1, 0, 0), fx), л(у(0, 1, 0), у(1, 1, 0), fx), fy),
             л(л(у(0, 0, 1), у(1, 0, 1), fx), л(у(0, 1, 1), у(1, 1, 1), fx), fy), fz);
  }

  function норм3(v) {
    var d = Math.sqrt(v[0] * v[0] + v[1] * v[1] + v[2] * v[2]) || 1;
    return [v[0] / d, v[1] / d, v[2] / d];
  }

  /* ── Набор режущих плоскостей ──────────────────────────────────
     У каждой плоскости есть признак «гладь»: единица значит
     полированную грань (у igloo такие держат 54-56 процентов площади и
     имеют шероховатость 0.21-0.30), ноль значит излом (0.37-0.38).
     Признак уходит в атрибут aFace и правит в шейдере и блеск, и
     штриховку, и силу зерна. */
  function плоскости(закон, сл) {
    var п = [], k, s, а, n, б;
    if (закон === "призма") {
      /* Шестигранник cube1. Ось наклонена от вертикали на 14.0 градуса
         (их нормаль оси -0.242, -0.970, 0.000), шесть боковых плоскостей
         стоят к ней под прямым углом, азимуты идут шагом 59.3..61.0
         градуса, расстояния 0.7235 плюс-минус 0.024. */
      var ось = норм3([0.242, 0.970, 0.0]);
      var u = норм3([ось[1] * 1 - ось[2] * 0, ось[2] * 0 - ось[0] * 1, ось[0] * 0 - ось[1] * 0]);
      u = норм3([ось[1], -ось[0], 0]);
      var w = [ось[1] * u[2] - ось[2] * u[1], ось[2] * u[0] - ось[0] * u[2], ось[0] * u[1] - ось[1] * u[0]];
      for (k = 0; k < 6; k++) {
        а = k * Math.PI / 3 + (сл() - 0.5) * 0.030;
        var ко = Math.cos(а), си = Math.sin(а);
        n = норм3([u[0] * ко + w[0] * си, u[1] * ко + w[1] * си, u[2] * ко + w[2] * си]);
        п.push({ n: n, d: 0.7235 + (сл() - 0.5) * 0.048, гладь: 1 });
      }
      /* Торцы у них СЛОМАНЫ: боковые грани держат 56.1 процента
         площади, остальные 43.9 приходятся на неровные концы. Три
         плоскости сверху и три снизу с разбросом до сорока градусов от
         оси дают такой конец, оставаясь плоскостями. */
      for (s = -1; s <= 1; s += 2) {
        for (k = 0; k < 3; k++) {
          б = [(сл() - 0.5) * 1.3, (сл() - 0.5) * 1.3, (сл() - 0.5) * 1.3];
          n = норм3([ось[0] * s + б[0], ось[1] * s + б[1], ось[2] * s + б[2]]);
          п.push({ n: n, d: 0.96 + сл() * 0.14, гладь: 0 });
        }
      }
    } else if (закон === "коробка") {
      /* Брусок cube2: шесть граней попарно под прямым углом, все на
         0.830 плюс-минус 0.013 от центра, коробка развёрнута на 20.0
         градуса вокруг Z (их нормали 0.341/0.940 и -0.940/0.340). */
      а = 20 * Math.PI / 180;
      var базис = [[Math.cos(а), Math.sin(а), 0], [-Math.sin(а), Math.cos(а), 0], [0, 0, 1]];
      for (k = 0; k < 3; k++) {
        for (s = -1; s <= 1; s += 2) {
          п.push({ n: [базис[k][0] * s, базис[k][1] * s, базис[k][2] * s],
                   d: 0.830 + (сл() - 0.5) * 0.026, гладь: 1 });
        }
      }
      /* Девять срезов по углам. Без них у коробки остались бы восемь
         острых углов, а у них выпуклость 91.9 процента и угол сколот
         на всех восьми. */
      for (k = 0; k < 9; k++) {
        n = норм3([сл() - 0.5, сл() - 0.5, сл() - 0.5]);
        п.push({ n: n, d: 0.94 + сл() * 0.22, гладь: 0 });
      }
    } else {
      /* Обломок cube3: десять плоскостей под произвольными углами
         (между шестью главными у них 45.6, 59.8, 66.9, 72.7, 91.8,
         110.0, 131.0, 155.7 градуса), расстояния гуляют 0.412..1.007.
         Направления кладутся по спирали с дрожанием, иначе случайный
         набор может не замкнуть тело. Полированных из десяти три:
         у них гладкая зона занимает 17 процентов карты. */
      var N = 10;
      for (k = 0; k < N; k++) {
        var y = 1 - (k + 0.5) * 2 / N;
        var r = Math.sqrt(Math.max(0, 1 - y * y));
        var ф = k * 2.399963 + сл() * 0.9;
        n = норм3([Math.cos(ф) * r + (сл() - 0.5) * 0.25, y + (сл() - 0.5) * 0.25,
                   Math.sin(ф) * r + (сл() - 0.5) * 0.25]);
        /* Расстояния 0.64..1.01, а не их дословные 0.412..1.007. Их
           разброс снят с готовой модели вместе с формой куска, а у нас
           направления кладутся спиралью, и при таком же разбросе тело
           выходит однобоким: вписанная часть садится втрое против
           габарита, и знак внутри приходится ужимать до трети. Две
           плоскости из десяти всё же режут глубоко (0.56..0.63) - без
           них обломок теряет характер. */
        var глуб = (k === 2 || k === 7);
        п.push({ n: n, d: глуб ? (0.56 + сл() * 0.07) : (0.64 + Math.pow(сл(), 0.8) * 0.37),
                 гладь: k < 3 ? 1 : 0 });
      }
    }
    return п;
  }

  /* ── Геометрия глыбы ───────────────────────────────────────────
     Заготовка это шар, и взят он ради РАВНОМЕРНОЙ СЕТКИ:
     каждая её вершина лежит на своём луче из центра, и вся форма
     задаётся одним числом - длиной этого луча.

     Длина луча берётся как расстояние до БЛИЖАЙШЕЙ режущей плоскости:
     r(луч) = min по плоскостям d / (луч, n). Это точное построение
     выпуклого многогранника, у которого грани плоские, а рёбра резкие.
     Прежняя сборка вместо этого мяла шар суперэллипсом и синусами и
     давала обмылок с четырьмя мягкими боками - ни одной настоящей
     плоскости в ней не было.

     Дальше стык двух плоскостей размывается. У igloo прямых рёбер
     грань-в-грань на всю сетку одна-одиннадцать штук из десяти тысяч:
     между гранями лежит полоса сколотых треугольников шириной
     0.22-0.68 мировых единиц. Здесь та же полоса: где вторая по
     близости плоскость подходит ближе чем на ширину полосы, луч
     сдвигается тремя октавами шума на 0.34 доли - в обе стороны,
     поэтому тело местами уходит внутрь, и выпуклость садится с 100 до
     их 82-92 процентов.

     На плоской грани остаётся только очень мелкая рябь в полтора
     процента: у них отклонение сетки от плоскости 0.006-0.013 при
     стороне грани около единицы, то есть геометрия там ГЛАДКАЯ, а вся
     фактура лежит в карте нормалей (её считает шейдер).

     Шум детерминированный (семя от номера глыбы), иначе три глыбы на
     стене менялись бы при каждой перезагрузке и снимок проверки
     никогда бы не совпал с прошлым. */
  function геометрия(форма, семя, ступень) {
    /* Их сетки: 6494, 6186 и 8038 треугольников при медианной стороне
       0.054. Шар 96 на 62 даёт 11712 треугольников со стороной 0.065 -
       тот же порядок, и полоса скола шириной 0.3 укладывается в четыре
       ряда треугольников, как у них. */
    var сегА = ступень === 0 ? 48 : (ступень === 1 ? 72 : 96);
    var сегВ = ступень === 0 ? 30 : (ступень === 1 ? 46 : 62);
    var гео = new T.SphereGeometry(1, сегА, сегВ);
    var растяг = РАСТЯГ[форма] || РАСТЯГ["обломок"];
    var закон = ЗАКОН[форма] || "обломок";
    var сл = шумСемя(семя);
    var пл = плоскости(закон, сл);
    var полоса = ПОЛОСА[закон] || 0.30;

    var п = гео.attributes.position;
    var N = п.count;
    var грань = new Float32Array(N);
    var семечко = new Float32Array(N);
    /* Своё число на каждую плоскость: в шейдере оно задаёт направление
       штриховки и разброс шероховатости, поэтому обязано быть
       постоянным внутри грани. */
    var числоПлана = [];
    for (var q = 0; q < пл.length; q++) числоПлана.push(сл());

    var сдвиг = семя * 0.37;
    for (var i = 0; i < N; i++) {
      var dx = п.getX(i), dy = п.getY(i), dz = п.getZ(i);
      var дл = Math.sqrt(dx * dx + dy * dy + dz * dz) || 1;
      dx /= дл; dy /= дл; dz /= дл;

      var t1 = 1e9, t2 = 1e9, i1 = -1;
      for (var k = 0; k < пл.length; k++) {
        var n = пл[k].n;
        var c = dx * n[0] + dy * n[1] + dz * n[2];
        if (c > 1e-4) {
          var t = пл[k].d / c;
          if (t < t1) { t2 = t1; t1 = t; i1 = k; } else if (t < t2) { t2 = t; }
        }
      }
      if (i1 < 0) { t1 = 1.0; i1 = 0; t2 = 1e9; }

      /* Насколько близко второй план: ноль в глубине грани, единица
         ровно на ребре. */
      var край = 1 - Math.min(1, (t2 - t1) / полоса);
      /* Три октавы с упором на мелкие: на первом снимке при упоре на
         крупную октаву и размахе 0.34 полоса читалась пилой по силуэту,
         а у них скол это ЗЕРНО в полосе, а не зубцы наружу. */
      var скол = (шум3(dx * 13 + сдвиг, dy * 13, dz * 13) - 0.5) * 0.40
               + (шум3(dx * 31, dy * 31, dz * 31 + сдвиг) - 0.5) * 0.34
               + (шум3(dx * 74, dy * 74, dz * 74) - 0.5) * 0.26;
      var мелк = шум3(dx * 46, dy * 46, dz * 46) - 0.5;
      var r = t1 * (1 + край * край * скол * 0.24 + (1 - край) * мелк * 0.014);

      грань[i] = (1 - край) * пл[i1].гладь;
      семечко[i] = числоПлана[i1];
      п.setXYZ(i, dx * r, dy * r, dz * r);
    }

    /* ── ПРИВЕДЕНИЕ К ИХ ГАБАРИТУ ─────────────────────────────
       Закон построения даёт форму, но не точные пропорции: у призмы
       наклон оси и сколотые торцы уводят высоту, у обломка расстояния
       плоскостей случайны. Здесь тело приводится к габариту их модели
       по каждой оси. Аффинное сжатие оставляет плоскости плоскостями и
       рёбра рёбрами, меняются только углы между гранями, и у призмы с
       бруском поправка не выходит за одиннадцать процентов. */
    var мин = [1e9, 1e9, 1e9], макс = [-1e9, -1e9, -1e9];
    for (i = 0; i < N; i++) {
      var в = [п.getX(i), п.getY(i), п.getZ(i)];
      for (k = 0; k < 3; k++) {
        if (в[k] < мин[k]) мин[k] = в[k];
        if (в[k] > макс[k]) макс[k] = в[k];
      }
    }
    var коэф = [1, 1, 1];
    for (k = 0; k < 3; k++) {
      var габ = макс[k] - мин[k];
      коэф[k] = габ > 1e-4 ? (2 * растяг[k]) / габ : 1;
    }
    /* Середина габарита сводится к нулю: снаряд в rv-снаряды.js садится
       на стену по своему началу координат, и тело со смещённой
       серединой висело бы мимо слота. */
    var центр = [(макс[0] + мин[0]) * 0.5, (макс[1] + мин[1]) * 0.5, (макс[2] + мин[2]) * 0.5];
    for (i = 0; i < N; i++) {
      п.setXYZ(i,
        (п.getX(i) - центр[0]) * коэф[0],
        (п.getY(i) - центр[1]) * коэф[1],
        (п.getZ(i) - центр[2]) * коэф[2]);
    }
    п.needsUpdate = true;

    гео.setAttribute("aFace", new T.BufferAttribute(грань, 1));
    гео.setAttribute("aRnd", new T.BufferAttribute(семечко, 1));
    /* ── ПРОВЕРКА «ТОЧКА ВНУТРИ ТЕЛА» ─────────────────────────
       Знак внутри льда вписывается по этой функции, а не по габаритной
       коробке: тело это многогранник, и знак в 58 процентов КОРОБКИ
       свободно вылезает наружу через косую грань. На первом снимке так
       и вышло - молния торчала из плиты чёрным клином прямо по фону.
       Обратный ход аффинного приведения возвращает точку в пространство
       заготовки, дальше её длина сравнивается с длиной луча до
       ближайшей плоскости. */
    гео.userData["внутри"] = function (x, y, z) {
      var lx = x / коэф[0] + центр[0];
      var ly = y / коэф[1] + центр[1];
      var lz = z / коэф[2] + центр[2];
      var д = Math.sqrt(lx * lx + ly * ly + lz * lz);
      if (д < 1e-6) return true;
      var ux = lx / д, uy = ly / д, uz = lz / д;
      var рм = 1e9;
      for (var s = 0; s < пл.length; s++) {
        var нн = пл[s].n;
        var сс = ux * нн[0] + uy * нн[1] + uz * нн[2];
        if (сс > 1e-4) {
          var тт = пл[s].d / сс;
          if (тт < рм) рм = тт;
        }
      }
      /* Запас пять сотых: у самой поверхности лёд тонкий, и знак,
         подошедший вплотную, проступал бы через неё яркой заплатой. */
      return д <= рм * 0.95;
    };
    /* Нормали считаются ПОСЛЕ сжатия: иначе на приведённом теле они
       смотрели бы мимо поверхности и блик поехал бы вбок. Сглаженные,
       а не плоские: мелкую фактуру рисует карта нормалей в шейдере, и
       на плоских нормалях она превратилась бы в шум из блёсток. */
    гео.computeVertexNormals();
    гео.computeBoundingBox();
    гео.computeBoundingSphere();
    return гео;
  }

  /* ── Знаки внутри льда ─────────────────────────────────────────
     Владелец: «внутри льда находится иконка какая-то или логотип, на
     всех трёх разный... нужно подумать, какие будут иконки, связанные с
     VPN».

     Три знака про то, что делает сервис, собранные из примитивов - без
     файлов, чтобы не тянуть модели по сети ради предмета размером с
     ноготь:
       щит    - трафик закрыт;
       шар    - выход в любой стране;
       молния - канал не проседает по скорости.
     Четвёртый, ключ, остаётся про доступ туда, куда не пускают. */
  function иконка(вид, цвет, кромка) {
    var гр = new T.Group();
    гр.name = "иконка: " + вид;
    var общие = {
      uBody: { value: new T.Color(цвет) },
      uRim: { value: new T.Color(кромка) },
      uLift: { value: 0 }
    };
    var мат = new T.ShaderMaterial({
      uniforms: {
        uBody: общие.uBody, uRim: общие.uRim, uLift: общие.uLift,
        uAlpha: { value: 1 }
      },
      vertexShader: В_ЗНАК, fragmentShader: Ф_ЗНАК, fog: false
    });
    /* ── ВТОРОЙ ПРОХОД ЗНАКА ПОВЕРХ ЛЬДА ───────────────────────
       У igloo знак виден НЕ сквозь толщу: он попадает в снимок сцены на
       первом проходе и читается через преломление, то есть доходит до
       кадра неослабленным. Отсюда их контраст знака ко льду по Веберу
       0.38 при том, что лёд у них непрозрачный.

       Своего прохода со снимком сцены у нас нет. Считаем, что даёт
       смешивание: над знаком лежит лёд плотностью uCore, и тело льда
       вносит в эту точку 0.58 своего цвета - больше, чем вся разница
       между знаком и кладкой за глыбой. При тёмной кладке акта
       ПЕРИМЕТР (68/78/96) знак математически НЕ МОЖЕТ стать темнее льда
       больше чем на семь пунктов, сколько его ни крась. Это и есть
       причина, по которой владелец дважды не увидел иконку.

       Поэтому знак печатается ДВАЖДЫ: первый раз до льда (даёт верную
       перекрытость и то, что он ВНУТРИ), второй раз после льда с
       прозрачностью 0.55 (возвращает контраст). Смесь выходит
       0.16 льда плюс 0.84 знака вместо 0.58 на 0.42, и знак садится на
       тридцать три пункта ниже льда. Фактура льда над
       знаком при этом остаётся видна, поэтому он читается предметом в
       толще, а не наклейкой на грани. */
    var матПоверх = new T.ShaderMaterial({
      uniforms: {
        uBody: общие.uBody, uRim: общие.uRim, uLift: общие.uLift,
        uAlpha: { value: 0.85 }
      },
      vertexShader: В_ЗНАК, fragmentShader: Ф_ЗНАК, fog: false,
      transparent: true, depthWrite: false, depthTest: true
    });
    function часть(гео, x, y, z, rx, ry, rz, s) {
      var м = new T.Mesh(гео, мат);
      м.position.set(x || 0, y || 0, z || 0);
      м.rotation.set(rx || 0, ry || 0, rz || 0);
      if (s) м.scale.setScalar(s);
      гр.add(м);
      var в = new T.Mesh(гео, матПоверх);
      в.position.copy(м.position);
      в.rotation.copy(м.rotation);
      в.scale.copy(м.scale);
      в.userData["поверх"] = true;
      гр.add(в);
      return м;
    }
    if (вид === "щит") {
      /* ── Щит собирается контуром, а не двумя цилиндрами ─────────
         Прежде он складывался из пятигранного конуса и пятигранной
         шайбы сверху. На проверочном снимке это читалось молотком:
         шайба вставала поперёк перекладиной, а конус висел под ней
         отдельной сосулькой.

         Настоящий силуэт щита это плоский верх, отвесные плечи и мыс
         внизу. Такое рисуется контуром с двумя дугами и выдавливается.

         ТОЛЩИНА ПОДНЯТА С 0.22 ДО 0.72. Замер их моделей: знак внутри
         глыбы занимает 48-65 процентов габарита ПО КАЖДОЙ ОСИ, включая
         глубину (overpass_logo 1.186 x 1.165 x 0.915 внутри cube1
         2.03 x 2.40 x 1.57). Наш щит был 0.84 в ширину при 0.30 в
         глубину, то есть плоской выдавленной фигурой, и заметную часть
         оборота стоял к камере ребром - тёмной плитой без рисунка.
         При 0.72 глубина сравнялась с шириной, и предмет читается с
         любого угла. */
      var кон = new T.Shape();
      кон.moveTo(-0.42, 0.52);
      кон.lineTo(0.42, 0.52);
      кон.lineTo(0.42, 0.04);
      кон.quadraticCurveTo(0.42, -0.36, 0, -0.60);
      кон.quadraticCurveTo(-0.42, -0.36, -0.42, 0.04);
      кон.closePath();
      var гщ = new T.ExtrudeGeometry(кон, {
        depth: 0.72, bevelEnabled: true, bevelSize: 0.05,
        bevelThickness: 0.05, bevelSegments: 2, curveSegments: 10
      });
      гщ.translate(0, 0, -0.36);
      часть(гщ, 0, 0.02, 0);
    } else if (вид === "шар") {
      часть(new T.SphereGeometry(0.44, 18, 12));
      /* Меридианы и экватор: без них шар это просто шар, а нужен глобус.
         Сечение обруча поднято с 0.028 до 0.045 - на тёмном знаке без
         кромки тонкий обруч не читался вовсе. */
      for (var м = 0; м < 3; м++) {
        часть(new T.TorusGeometry(0.46, 0.045, 8, 32), 0, 0, 0, Math.PI / 2, м * Math.PI / 3, 0);
      }
      часть(new T.TorusGeometry(0.46, 0.045, 8, 32), 0, 0, 0, 0, 0, 0);
    } else if (вид === "молния") {
      /* ── Молния рисуется контуром, а не тремя брусками ──────────
         Три повёрнутых бруска на проверочном снимке читались двумя
         тёмными коробками: знак молнии узнаётся по СВОЕМУ силуэту с
         острыми концами, а сложить его из прямоугольников нельзя.
         Настоящий контур выдавливается на 0.55, то есть глубина
         сравнима с шириной 0.58 - объёмный предмет, а не пластина. */
      var кмол = new T.Shape();
      кмол.moveTo(0.12, 0.62);
      кмол.lineTo(-0.28, 0.04);
      кмол.lineTo(-0.01, 0.04);
      кмол.lineTo(-0.15, -0.62);
      кмол.lineTo(0.29, 0.07);
      кмол.lineTo(0.02, 0.07);
      кмол.closePath();
      var гмол = new T.ExtrudeGeometry(кмол, {
        depth: 0.55, bevelEnabled: true, bevelSize: 0.035,
        bevelThickness: 0.035, bevelSegments: 2, curveSegments: 4
      });
      гмол.translate(0, 0, -0.275);
      часть(гмол, 0, 0, 0);
    } else {
      /* Ключ: кольцо и бородка. */
      часть(new T.TorusGeometry(0.26, 0.10, 10, 24), 0, 0.28, 0, 0, 0, 0);
      часть(new T.BoxGeometry(0.13, 0.60, 0.13), 0, -0.16, 0);
      часть(new T.BoxGeometry(0.26, 0.12, 0.12), 0.10, -0.34, 0);
      часть(new T.BoxGeometry(0.20, 0.12, 0.12), 0.07, -0.50, 0);
    }
    /* Первый проход раньше льда (порядок 4) и пишет глубину: знак
       оказывается ВНУТРИ, лёд ложится поверх. Второй проход после льда
       (порядок 8), глубину не пишет и не проверяет её у льда, потому
       что лёд глубину не пишет тоже. */
    гр.traverse(function (о) {
      if (!о.isMesh) return;
      о.renderOrder = о.userData["поверх"] ? 8 : 4;
      о.frustumCulled = false;
    });
    гр.userData["мат"] = мат;
    гр.userData["матПоверх"] = матПоверх;
    return гр;
  }

  /* ── Линии выноски ─────────────────────────────────────────────
     Все отрезки складываются в один буфер. У линии в WebGL нет
     толщины, зато каждый отдельный T.Line это отдельный вызов
     отрисовки: веер из восьми лучей плюс дюжина крестиков на трёх
     глыбах дали бы под сотню вызовов на кадр. Один T.LineSegments на
     всю пачку решает это разом. */
  function пачка(отр, цвет, прозр, порядок) {
    var поз = new Float32Array(отр.length * 6);
    for (var i = 0; i < отр.length; i++) {
      var с = отр[i];
      поз[i * 6] = с[0]; поз[i * 6 + 1] = с[1]; поз[i * 6 + 2] = с[2];
      поз[i * 6 + 3] = с[3]; поз[i * 6 + 4] = с[4]; поз[i * 6 + 5] = с[5];
    }
    var гео = new T.BufferGeometry();
    гео.setAttribute("position", new T.BufferAttribute(поз, 3));
    var мат = new T.LineBasicMaterial({
      color: цвет, transparent: true, opacity: прозр, depthWrite: false
    });
    var м = new T.LineSegments(гео, мат);
    м.renderOrder = порядок;
    м.frustumCulled = false;
    return м;
  }

  function крест(отр, x, y, z, r) {
    отр.push([x - r, y, z, x + r, y, z]);
    отр.push([x, y - r, z, x, y + r, z]);
  }

  /* ── Почему лучи и маркеры вынесены вперёд по глубине ──────────
     Знак внутри льда НЕПРОЗРАЧНЫЙ и пишет глубину (иначе его срезала бы
     собственная задняя грань). Волосяная линия, проложенная по середине
     глыбы, попадает знаку за спину и пропадает кусками - на снимке это
     выглядит как рваный пунктир без всякой причины.

     Поэтому веер садится на ПЕРЕДНЮЮ грань: 0.72 размера в системе
     выноски, то есть 0.92 размера от середины глыбы. Замер готовых
     сеток дал самой толстой из форм (брусок) полутолщину 0.851, так что
     линия ложится перед льдом у любой из четырёх. У igloo на кадре 009 линии тоже идут
     поверх тела, а не тонут в нём. */
  var ПЕРЕД = 0.72;

  /* ── Выноска ───────────────────────────────────────────────────
     Разобрано по кадру /tmp/игло/плёнка-ПК-009.png с увеличением.
     Устройство у них такое, по порядку от текста к предмету:

       белая ЗАЛИВНАЯ плашка с мелким тёмным моноширинным текстом
         -> короткий отвод с квадратным хвостиком за дальним краем
         -> УЗЕЛ (точка с крестиком)
         -> ВЕЕР из восьми-десяти волосяных лучей разной длины,
            сходящихся в этот один узел и уходящих на тело глыбы.

     Плюс с десяток голых крестиков-маркеров по поверхности и вокруг,
     без подписей вовсе.

     Прежняя сборка давала полку, наклонный поводок и три луча - то
     есть скелет был верный, но без узла, без веера и, главное, без
     плашки: белый текст на светлом тумане просто пропадал, что
     владелец и увидел.

     Текст ставит тот, кто зовёт (ему виднее, что писать). Плашка под
     текст растёт здесь, когда текст появится: шрифт грузится отдельным
     файлом, и в момент сборки выноски его геометрии ещё нет. */
  function выноска(сторона, длина, спуск, кЦентру, масштаб, ступень, семя) {
    var гр = new T.Group();
    гр.name = "выноска";
    var сл = шумСемя(семя);
    var основа = [];   /* отвод и узел: видны всегда */
    var волос = [];    /* веер: тоньше, значит бледнее */
    var лучей = ступень === 0 ? 5 : 8;

    function веер(ux, uy) {
      for (var i = 0; i < лучей; i++) {
        /* Луч садится где-то на теле глыбы, до середины он не
           доходит: доля хода 0.42..1.04 при разбросе вбок в треть размера.
           На их кадре лучи кончаются на разной глубине предмета, отчего
           веер и выглядит замером, а не звездой. */
        var д = 0.42 + сл() * 0.62;
        var бx = (сл() - 0.5) * масштаб * 0.80;
        var бy = (сл() - 0.5) * масштаб * 0.80;
        волос.push([ux, uy, 0,
                    ux + (кЦентру[0] - ux) * д + бx,
                    uy + (кЦентру[1] - uy) * д + бy, масштаб * ПЕРЕД]);
      }
    }

    if (!сторона) {
      /* ── ВЫНОСКА СВЕРХУ ────────────────────────────────────────
         На стоячем кадре сбоку места нет: кадр в плоскости стены три с
         небольшим единицы шириной, а полка с подписью просит пять. На
         снимке телефона подпись «Выход в любой стране» ложилась прямо
         на соседнюю глыбу. Поэтому здесь тот же приборный набор,
         развёрнутый вверх: стояк от макушки, полка под подпись,
         засечки по краям, узел на стояке и веер от него вниз. */
      основа.push([0, спуск * 0.34, 0, 0, спуск, 0]);
      основа.push([-длина * 0.5, спуск, 0, длина * 0.5, спуск, 0]);
      основа.push([-длина * 0.5, спуск, 0, -длина * 0.5, спуск - спуск * 0.28, 0]);
      основа.push([длина * 0.5, спуск, 0, длина * 0.5, спуск - спуск * 0.28, 0]);
      крест(основа, 0, спуск * 0.34, 0, масштаб * 0.055);
      веер(0, спуск * 0.34);
      гр.userData["конец"] = [0, спуск, 0];
      гр.userData["сверху"] = true;
    } else {
      var зн = сторона < 0 ? -1 : 1;
      /* Полка с изломом: горизонталь под подписью, потом наклон к узлу.
         Узел стоит на дальнем конце - оттуда и бьёт веер. */
      основа.push([0, 0, 0, зн * длина * 0.62, 0, 0]);
      основа.push([зн * длина * 0.62, 0, 0, зн * длина, -спуск, 0]);
      крест(основа, зн * длина, -спуск, 0, масштаб * 0.06);
      веер(зн * длина, -спуск);
      гр.userData["конец"] = [зн * длина, -спуск, 0];
    }

    /* ── Голые маркеры ─────────────────────────────────────────
       На их кадре крестиков без подписей больше, чем с подписями:
       десяток по телу глыбы и вокруг неё. Они и создают ощущение, что
       предмет обмерян целиком, а подписаны только три точки. */
    var маркеров = ступень === 0 ? 6 : 11;
    for (var м = 0; м < маркеров; м++) {
      var а = сл() * 6.283;
      var р = масштаб * (0.35 + сл() * 1.05);
      крест(волос,
            кЦентру[0] + Math.cos(а) * р,
            кЦентру[1] + Math.sin(а) * р * 1.15,
            масштаб * ПЕРЕД, масштаб * (0.030 + сл() * 0.030));
    }

    var мОсн = пачка(основа, 0xF4F8FF, 0.92, 11);
    var мВол = пачка(волос, 0xEAF2FF, 0.52, 11);
    гр.add(мОсн); гр.add(мВол);
    гр.userData["маты"] = [мОсн.material, мВол.material];
    гр.userData["прозр"] = [0.92, 0.52];
    гр.userData["плашка"] = null;
    гр.userData["текст"] = null;
    return гр;
  }

  /* ── Плашка под подпись ────────────────────────────────────────
     Замер по кадру: плашка у них примерно 48 на 21 точку при кегле в
     девять, то есть поля сбоку около трети высоты строки, сверху и
     снизу около пятой. Заливка чистая белая, текст тёмный. Справа за
     краем висит отдельный квадратный хвостик со щелью - мелкая
     приборная деталь, которая и делает подпись похожей на их разметку.

     Строится не сразу: подпись приходит из rv-msdf.js, а тот держит
     пустую геометрию, пока не догрузится шрифт. Поэтому попытка идёт
     каждый кадр, пока не получится, и после этого больше не идёт. */
  function растиПлашку(гр) {
    if (гр.userData["плашка"]) return;
    var текст = гр.userData["текст"];
    if (!текст) {
      var д = гр.children;
      for (var i = 0; i < д.length; i++) {
        if (д[i].isMesh && д[i].userData && д[i].userData["текст"] != null) {
          текст = д[i]; гр.userData["текст"] = текст; break;
        }
      }
    }
    if (!текст || !текст.geometry) return;
    var поз = текст.geometry.attributes && текст.geometry.attributes.position;
    if (!поз || !поз.count) return;

    текст.geometry.computeBoundingBox();
    var б = текст.geometry.boundingBox;
    if (!б) return;
    var s = текст.scale.x || 1;
    var стр = Math.max(1, текст.userData["строк"] || 1);
    var шТ = (б.max.x - б.min.x) * s;
    var вТ = (б.max.y - б.min.y) * s;
    if (шТ <= 0 || вТ <= 0) return;
    var вСтроки = вТ / стр;
    var полеX = вСтроки * 0.34;
    var полеY = вСтроки * 0.20;
    var шП = шТ + полеX * 2;
    var вП = вТ + полеY * 2;
    var цx = (б.max.x + б.min.x) * 0.5 * s + текст.position.x;
    var цy = (б.max.y + б.min.y) * 0.5 * s + текст.position.y;
    var цz = текст.position.z - вСтроки * 0.05;

    var мат = new T.MeshBasicMaterial({
      color: 0xFFFFFF, transparent: true, opacity: 0,
      depthWrite: false, side: T.DoubleSide, fog: false
    });
    var пл = new T.Group();
    пл.name = "плашка";
    var осн = new T.Mesh(new T.PlaneGeometry(шП, вП), мат);
    осн.position.set(цx, цy, цz);
    осн.renderOrder = 12;
    осн.frustumCulled = false;
    пл.add(осн);
    /* Хвостик за дальним краем, со щелью в половину его ширины. Сторона
       берётся по знаку смещения текста: подпись всегда уходит от глыбы
       наружу, туда же смотрит и хвостик. */
    var зн = цx >= 0 ? 1 : -1;
    var хв = new T.Mesh(new T.PlaneGeometry(вП * 0.42, вП * 0.46), мат);
    хв.position.set(цx + зн * (шП * 0.5 + вП * 0.42), цy, цz);
    хв.renderOrder = 12;
    хв.frustumCulled = false;
    пл.add(хв);
    гр.add(пл);
    гр.userData["плашка"] = пл;
    гр.userData["матПлашки"] = мат;

    /* ── Текст перекрашивается в тёмный ────────────────────────
       Плашка белая заливная, значит белая подпись на ней исчезнет.
       Цвет подписи ставит вызывающий (rv-снаряды.js передаёт белый в
       RV_MSDF), и переписать его там нельзя - файл чужой. Поэтому
       правится униформа уже готового меша: тёмный набор на белом это
       то, что на их кадре, и без этого плашка была бы во вред. */
    if (текст.material && текст.material.uniforms) {
      var ун = текст.material.uniforms;
      if (ун.uColor) ун.uColor.value.setHex(0x1B2430);
      /* ── Набор ещё и утолщается ────────────────────────────────
         Одного тёмного цвета мало. Порог поля расстояний в rv-msdf.js
         считается как 0.5 - uThickness, то есть МИНУС делает букву
         тоньше, а подпись у выноски приходит оттуда с толщиной -0.16.
         На плашке строка занимает по высоте около семи точек экрана,
         тонкий штрих до полной непрозрачности не дотягивает вовсе, и
         замер живого кадра дал по строке пятый процентиль 207 против
         девяносто пятого 226 при белой плашке 230. Контраст 1.09,
         читать там нечего, и это ровно та беда, на которую владелец
         жаловался словами «надписи не видно».

         Порог правится у готового меша, как и цвет: значение ставит
         чужой файл, трогать его нельзя. */
      if (ун.uThickness && ун.uThickness.value < 0.20) ун.uThickness.value = 0.20;
    }

    /* ── Подпись печатается ДВАЖДЫ ─────────────────────────────
       Порога мало. Строка на плашке высотой в семь точек экрана, поле
       расстояний сглаживается производной fwidth, и на таком кегле
       производная шире самого штриха: буква не добирает до полной
       непрозрачности физически. Замер по живому кадру: самая тёмная
       точка набора 165,175,182 при плашке 229,230,235, то есть буква
       легла долей около трети.

       Второй проход тем же мешем складывает долю с самой собой: треть
       и треть дают больше половины, и набор становится читаемым, не
       теряя формы букв. Геометрия и материал общие, лишний вызов
       отрисовки на глыбу один. Настоящее лекарство это кегль подписи,
       он ставится в rv-снаряды.js и оттуда же приходит толщина. */
    var копия = new T.Mesh(текст.geometry, текст.material);
    копия.position.copy(текст.position);
    копия.rotation.copy(текст.rotation);
    копия.scale.copy(текст.scale);
    копия.renderOrder = текст.renderOrder;
    копия.frustumCulled = false;
    гр.add(копия);
    гр.userData["копияПодписи"] = копия;
  }

  /* Появление и гашение выноски идут ЗА ПОДПИСЬЮ. rv-msdf.js ведёт
     показ строки через uShow1 от нуля до единицы, и если линии с
     плашкой живут своей жизнью, на кадре сначала повисает пустая белая
     табличка, а текст в ней проступает секундой позже. */
  function ходВыноски(гр) {
    if (!гр) return;
    растиПлашку(гр);
    var текст = гр.userData["текст"];
    var доля = 1;
    if (текст && текст.material && текст.material.uniforms &&
        текст.material.uniforms.uShow1) {
      доля = зажать(текст.material.uniforms.uShow1.value, 0, 1);
    }
    /* Шрифт доезжает файлом, и rv-msdf.js подменяет геометрию строки
       уже после сборки. Второй проход обязан ехать за той же сеткой,
       иначе на кадре останется старый набор поверх нового. */
    var коп = гр.userData["копияПодписи"];
    if (коп && текст && коп.geometry !== текст.geometry) коп.geometry = текст.geometry;
    var маты = гр.userData["маты"], база = гр.userData["прозр"];
    for (var i = 0; i < маты.length; i++) маты[i].opacity = база[i] * доля;
    if (гр.userData["матПлашки"]) гр.userData["матПлашки"].opacity = доля;
  }

  function собрать(мир, о) {
    if (!мир || !мир.T) return null;
    T = мир.T;
    о = о || {};
    var ступень = мир.ступень == null ? 2 : мир.ступень;
    var форма = о["форма"] || ФОРМЫ[0];
    var семя = о["семя"] == null ? 17 : о["семя"];
    var размер = о["размер"] || 1;

    var корень = new T.Group();
    корень.name = "глыба: " + форма;

    var мат = new T.ShaderMaterial({
      uniforms: {
        /* ── ПАЛИТРА ЛЬДА ────────────────────────────────────────
           Прежние #CFE0F2 и #53627D давали на кадре тело яркостью
           133-186 при кладке за ним 78-102, то есть лёд был СВЕТЛЕЕ
           окружения на 55-85 пунктов и читался целлофаном. У igloo
           наоборот: тело 108/116/132 при окружении 150/155/168, лёд
           ТЕМНЕЕ на 27 процентов. Наша сцена темнее их (кладка стоит на
           68-102), поэтому дословно их числа брать нельзя: лёд ушёл бы
           в черноту. Взято их ОТНОШЕНИЕ - тело между кладкой и полом.
           Замер боевого кадра доли 0.45 после правки: тело 125-133 при
           кладке 77 и поле 150, синий выше красного на 22-25, точек
           ярче 200 ноль. Было 133-186 при том же фоне. */
        uIce: { value: new T.Color(о["цвет"] || 0x8A91A0) },
        uDeep: { value: new T.Color(о["цветТолщи"] || 0x4C525F) },
        /* uEdge остался только под вспышку удара и след полёта: белую
           кайму теперь даёт отражение окружения, а не отдельное
           слагаемое. */
        uEdge: { value: new T.Color(о["цветРёбер"] || 0xBFD2EA) },
        uTime: { value: 0 },
        uOpacity: { value: 1 },
        uHot: { value: 0 },
        /* uFly держит след полёта. Имя то же, что было у плиты-снаряда:
           ход снаряда (rv-снаряды.js) правит его напрямую и знать про
           смену предмета не обязан. */
        uFly: { value: 0 },
        /* ПОКАЗАТЕЛЬ ПРЕЛОМЛЕНИЯ 1.2727, А НЕ 1.18 И НЕ 1.31. В их коде
           стоит ior=1.18, но следом reflectivity=0.3, и это присвоение
           через сеттер пересчитывает показатель по формуле
           (1+0.4*r)/(1-0.4*r) = 1.2727. Разбег каналов у них 0.1, у нас
           стояло 0.035 - втрое меньше, оттого дисперсию и приходилось
           вытягивать силой uRainbow до бензиновой плёнки. */
        uIor: { value: о["преломление"] || 1.273 },
        uCa: { value: о["дисперсия"] == null ? 0.10 : о["дисперсия"] },
        /* Сила радуги упала с 0.62 до 0.18: замер наших кадров дал
           размах каналов на точку до 168 при их 40-56. В кадр теперь
           идёт только разница каналов преломления, сама картинка
           окружения учтена яркостью тела. */
        uRainbow: { value: о["радуга"] == null ? 0.18 : о["радуга"] },
        /* Плотность середины. Знак внутри читается тем сильнее, чем
           тоньше лёд над ним, а тело тем плотнее, чем выше это число.
           0.32 это найденное равновесие: тело держит свою яркость, знак
           проступает контрастом около трети по Веберу (у них 0.38). */
        uCore: { value: о["плотность"] == null ? 0.58 : о["плотность"] },
        /* Сила отражения окружения. У них envMapIntensity 0.91. */
        uEnv: { value: о["окружение"] == null ? 0.91 : о["окружение"] },
        /* Потолок яркости. У igloo на всех двенадцати кадрах с глыбами
           нет ни одной точки ярче 200 из 255, весь лёд лежит в 60..185.
           Линейное 0.50 через плёнку сборки даёт около 193. */
        uCap: { value: о["потолок"] == null ? 0.42 : о["потолок"] },
        /* Вторая октава зерна излома: на слабой ступени она снимается,
           там и экран мельче, и разницы не видно. */
        uFine: { value: ступень === 0 ? 0 : 1 }
      },
      vertexShader: В_ЛЁД, fragmentShader: Ф_ЛЁД,
      /* FrontSide, как у них в основном проходе. Прежний DoubleSide на
         замкнутом теле рисовал заднюю половину поверх передней в
         произвольном порядке (внутри одного меша треугольники идут по
         номеру, а не по глубине) - отсюда и бралась заметная часть той
         молочной мути. */
      transparent: true, depthWrite: false, side: T.FrontSide,
      fog: false
    });

    var лёд = new T.Mesh(геометрия(форма, семя, ступень), мат);
    лёд.name = "лёд: " + форма;
    лёд.renderOrder = 6;
    лёд.frustumCulled = false;
    лёд.scale.setScalar(размер);
    корень.add(лёд);

    var знак = null;
    if (о["иконка"]) {
      /* Тело тёмное, отсвет холодный. Форму знака внутри льда держат
         именно блики: у них в запечённой карте средняя 68/72/83, а
         сотый процентиль доходит до 200-212. */
      знак = иконка(о["иконка"], о["цветЗнака"] || 0x1A2130,
                    о["кромкаЗнака"] || 0xE6EEFB);
      /* ── РАЗМЕР ЗНАКА СЧИТАЕТСЯ ОТ ТЕЛА, А НЕ НАЗНАЧАЕТСЯ ЧИСЛОМ
         У них знак занимает 48-65 процентов габарита глыбы ПО КАЖДОЙ
         ОСИ, включая глубину: overpass_logo 1.186 x 1.165 x 0.915
         внутри cube1 2.03 x 2.40 x 1.57 это 58.6 / 48.4 / 58.4
         процента. Прежнее число 1.15 было общим на все формы, и на
         кристалле знак выходил в 93 процента ширины тела, то есть
         торчал наружу.

         Здесь берётся габарит готовой сетки льда и габарит собранного
         знака, и знак вписывается в долю по САМОЙ ТЕСНОЙ оси. Если
         после этого он оказался тоньше половины тела по глубине -
         догоняется по Z: плоский знак в объёмном льду читается плитой,
         а не предметом. */
      var доля = о["размерЗнака"] == null ? 0.60 : о["размерЗнака"];
      var гЛ = лёд.geometry.boundingBox;
      var кор = new T.Box3().setFromObject(знак);
      var тЛ = [гЛ.max.x - гЛ.min.x, гЛ.max.y - гЛ.min.y, гЛ.max.z - гЛ.min.z];
      var тЗ = [кор.max.x - кор.min.x, кор.max.y - кор.min.y, кор.max.z - кор.min.z];
      var к = 1e9;
      for (var ос = 0; ос < 3; ос++) {
        if (тЗ[ос] > 1e-4) к = Math.min(к, доля * тЛ[ос] / тЗ[ос]);
      }
      if (!isFinite(к) || к <= 0) к = доля;
      /* Тоньше половины тела по глубине знак быть не должен: у них он
         занимает 48-65 процентов габарита ПО КАЖДОЙ оси, включая
         глубину, и плоский знак в объёмном льду читается плитой. */
      var кZ = к;
      if (тЗ[2] > 1e-4 && тЗ[2] * к < 0.50 * тЛ[2]) кZ = 0.50 * тЛ[2] / тЗ[2];
      /* ── ЗНАК ВПИСЫВАЕТСЯ В ТЕЛО, А НЕ В ЕГО КОРОБКУ ──────────
         Тело это многогранник, и доля от габаритной коробки ничего не
         обещает: на первом снимке молния при 58 процентах коробки
         вылезла из плиты наружу чёрным клином и легла прямо на кладку.

         Проверяются САМИ ВЕРШИНЫ знака, а не углы его коробки: у
         коробки углы пустые, и по ним знак ужимался вчетверо. Точки
         берутся с прореживанием (до 240 штук: этого хватает, потому что
         решает крайняя, а крайних у примитивов немного) и проверяются в
         четырёх крайних положениях качания. */
      var точки = [];
      знак.updateMatrixWorld(true);
      знак.traverse(function (о) {
        if (!о.isMesh || о.userData["поверх"]) return;
        var поз = о.geometry.attributes.position;
        var шг = Math.max(1, Math.ceil(поз.count / 80));
        var м4 = о.matrix;
        for (var v = 0; v < поз.count; v += шг) {
          var т = new T.Vector3(поз.getX(v), поз.getY(v), поз.getZ(v)).applyMatrix4(м4);
          точки.push(т);
        }
      });
      var повороты = [[0.42, 0.11], [-0.42, 0.11], [0.42, -0.11], [-0.42, -0.11], [0, 0]];
      var внутри = лёд.geometry.userData["внутри"];
      if (внутри && точки.length) {
        var шаг = 0;
        while (шаг < 20) {
          var влез = true;
          for (var пв = 0; пв < повороты.length && влез; пв++) {
            var сY = Math.sin(повороты[пв][0]), пY = Math.cos(повороты[пв][0]);
            var сX = Math.sin(повороты[пв][1]), пX = Math.cos(повороты[пв][1]);
            for (var ти = 0; ти < точки.length; ти++) {
              var тx = точки[ти].x * к, тy = точки[ти].y * к, тz = точки[ти].z * кZ;
              var ax = тx * пY + тz * сY, az = -тx * сY + тz * пY;
              var ay = тy * пX - az * сX, bz = тy * сX + az * пX;
              if (!внутри(ax, ay, bz)) { влез = false; break; }
            }
          }
          if (влез) break;
          к *= 0.955; кZ *= 0.955; шаг++;
        }
      }
      знак.scale.set(размер * к, размер * к, размер * кZ);
      корень.add(знак);
    }

    var вын = null;
    if (о["выноска"]) {
      var верх = о["верхВыноски"] || 0.75;
      var сторона = о["сторона"] == null ? -1 : о["сторона"];
      if (!сторона) верх = 0.95;
      вын = выноска(сторона,
                    размер * (о["длинаВыноски"] || 1.6),
                    размер * (о["спускВыноски"] || 0.55),
                    /* Центр глыбы в координатах группы выноски: сама
                       группа поднята над ним, и веер обязан целить
                       вниз, в тело, а не в пустоту. */
                    [0, -размер * верх],
                    размер, ступень, семя + 101);
      вын.position.set(0, размер * верх, размер * (сторона ? 0.2 : 0.25));
      корень.add(вын);
    }

    var М = {
      "корень": корень, "лёд": лёд, "иконка": знак, "выноска": вын,
      "мат": мат, "форма": форма,
      "сторона": о["сторона"] == null ? -1 : о["сторона"],
      "размер": размер,
      /* ── ЗНАК КАЧАЕТСЯ, А НЕ КРУТИТСЯ ─────────────────────────
         Стояло знак.rotation.y += dt * 0.55, то есть полный оборот за
         одиннадцать с половиной секунд. Щит при этом плоская фигура, и
         примерно треть оборота он стоял к камере ребром - тёмной плитой
         без рисунка. Ровно это видно на проверочном снимке доли 0.45:
         левая глыба показывает тёмную полосу вместо щита.

         Теперь качание в плюс-минус 24 градуса: знак ни на одном кадре
         не уходит из своего силуэта. У igloo та же механика - их глыбы
         не крутятся сами, у них собственное качание амплитудой 0.1
         радиана на частоте 0.3, а полный разворот даёт только
         прокрутка страницы. Плюс сама фигура теперь объёмная (глубина
         щита поднята с 0.22 до 0.72), так что даже боком он читается
         предметом.

         Фаза разведена по семени: три знака на стене не должны качаться
         в такт. */
      "кадр": function (dt, часы, горячо) {
        мат.uniforms.uTime.value = часы || 0;
        var ц = зажать(горячо == null ? 0 : горячо, 0, 1);
        var у = мат.uniforms.uHot;
        у.value += (ц - у.value) * зажать((dt || 0.016) * 6, 0, 1);
        if (знак) {
          var т = (часы || 0) + семя * 0.41;
          знак.rotation.y = Math.sin(т * 0.42) * 0.42;
          знак.rotation.x = Math.sin(т * 0.31 + 1.1) * 0.11;
          /* Знак подсвечивается вместе со вспышкой льда: при ударе о
             стену он проступает сквозь толщу сильнее, и удар читается
             не только по кромке. */
          if (знак.userData["мат"]) знак.userData["мат"].uniforms.uLift.value = у.value * 0.55;
        }
        if (вын) ходВыноски(вын);
      },
      "прозрачность": function (v) { мат.uniforms.uOpacity.value = зажать(v, 0, 1); },
      "замер": function () {
        /* Доля плоской зоны и габарит нужны проверке: по ним видно, что
           тело построено плоскостями, а не смято из шара. У igloo по
           картам шероховатости гладкая зона занимает 19.7 / 32.3 / 17.0
           процента, габариты 2.03 x 2.40 x 1.57, 2.14 x 2.13 x 1.73 и
           1.58 x 2.46 x 1.43 при нашей полувысоте 1.05. */
        var ф = лёд.geometry.attributes.aFace;
        var гл = 0;
        if (ф) { for (var i = 0; i < ф.count; i++) if (ф.getX(i) > 0.5) гл++; }
        var бк = лёд.geometry.boundingBox;
        return {
          "форма": форма, "размер": размер,
          "иконка": о["иконка"] || null,
          "закон": ЗАКОН[форма] || "обломок",
          "гладь%": ф ? +(гл / ф.count * 100).toFixed(1) : null,
          "габарит": бк ? [+(бк.max.x - бк.min.x).toFixed(3),
                           +(бк.max.y - бк.min.y).toFixed(3),
                           +(бк.max.z - бк.min.z).toFixed(3)] : null,
          "вершин": лёд.geometry.attributes.position.count,
          "плашка": !!(вын && вын.userData["плашка"]),
          "видна": !!корень.visible
        };
      }
    };
    return М;
  }

  g.RV_ГЛЫБА = {
    "собрать": собрать,
    "формы": function () { return ФОРМЫ.slice(); }
  };
})(window);
