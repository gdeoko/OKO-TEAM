/* Rocket VPN. Луна: грунт, небо, Земля, дымка свечения, лучи, пыль.

   ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ. Станция (rv-act-станция.js) это предмет, Луна
   это место, где он стоит. У места свои законы: небо и туман красятся
   ОДНИМ экранным градиентом, грунт растворяет свою кромку, тень под
   предметом синяя, пыль идёт понизу. Всё это нужно и куполу, и любому
   другому предмету, который когда-нибудь встанет на этот грунт, поэтому
   Луна отдаёт фабрику и общие униформы, а не рисует сама по себе.

   РЕЦЕПТ ЧУЖОЙ, ЧИСЛА СВОИ. Устройство снято с igloo.inc разбором их
   боевого кода (docs/РАЗБОР-IGLOO.md): в их сцене ноль живых источников
   света, небо это pow((x+y)/2, 2) по экрану, туман в каждом материале
   считается тем же градиентом, контактная тень нарисована формулой и
   она синяя, дым это три перемноженные выборки одного тайлового шума,
   кромка любой плоскости уходит в smoothstep. Их моделей и текстур
   здесь нет: грунт с кратерами считается при сборке, шум рисуется в
   холст, Земля это сфера с формулой терминатора.

   ПОЧЕМУ БЕЗ ИСТОЧНИКОВ СВЕТА. У мира Rocket VPN четыре живых источника,
   и каждый пиксель каждой модели считает их заново. На Луне свет один,
   жёсткий, без рассеяния, и его дешевле и честнее записать формулой по
   нормали: верх светлый, низ синий отсвет Земли. Так же сделан весь
   igloo, и так же читается «запечённый» вид, который просил владелец. */
(function (g, d) {
  "use strict";

  var W = null, T = null;
  var М = {};
  var собрано = false;

  function зажать(v, н, в) { return v < н ? н : (v > в ? в : v); }

  /* ── Общие униформы ──────────────────────────────────────────
     Их читают ВСЕ материалы Луны и материал купола: небо и туман обязаны
     совпадать до бита, иначе предмет висит на фоне, а не стоит в
     воздухе. Наружу отдаются как RV_ЛУНА.градиент. */
  var ГРАД = {
    uColor1: { value: null },   /* зенит */
    uColor2: { value: null },   /* горизонт */
    uResolution: { value: null },
    uTime: { value: 0 },
    uDen: { value: 0 }           /* 0 ночь, 1 день */
  };

  /* Палитры. Ночью зенит чуть синий, горизонт почти чёрный: свет идёт
     СНИЗУ, от грунта, поэтому градиент перевёрнут относительно igloo.
     Днём числа сняты с их кадра (разбор ниже, у самой дневной
     палитры). Ни одна точка неба не белая ни в одной теме - это
     правило igloo, и оно снимает пересвет на корню. */
  /* Ночной реголит темнее, чем казалось по числам igloo: у них снег, у
     нас камень. Первый кадр с грунтом #7C7F87 дал светло-серую заливку
     на всю нижнюю половину, читавшуюся туманом, а не поверхностью.
     Средняя яркость кадра была 90; цель 55-70 при тех же нулях
     выбитых и чёрных точек. */
  /* НЕБО ЛУНЫ ЧЁРНОЕ, И ЭТО НЕ СТИЛИЗАЦИЯ.

     Синим небо делает рассеяние Рэлея в воздухе. На Луне воздуха нет
     вовсе, рассеивать нечем, и небо там чёрное ДАЖЕ ДНЁМ, при солнце
     над головой. Ровно это видно на каждом кадре Аполлона: слепящий
     грунт и абсолютно чёрный купол над ним, без единого перехода.

     Стоял зенит 0x202A48, то есть земные сумерки. От него весь кадр
     читался «ночь на Земле», а не Луна, и никакая отделка грунта этого
     не перебивала. Теперь зенит почти нулевой, у горизонта оставлен
     чуть более светлый тон: это не воздух, а отсвет самого реголита,
     который у поверхности действительно подсвечивает пыль. */
  var НОЧЬ = { зенит: 0x02030A, горизонт: 0x07080F, грунт: 0x60636C, склон: 0x716E67,
               верх: 0xB2B9C8, низ: 0x262C40, дымка: 0x7E9CFF, дымкаСила: 0.18,
               пыль: 0xB9C6FF, пыльСила: 0.16 };
  /* ── ДНЕВНАЯ ПАЛИТРА СЧИТАНА С ЖИВОГО igloo ───────────────────
     Владелец: «светлую тему вообще всю доработать нужно, чтобы цвет
     фона, текста, текстур, объектов мира был как у igloo.inc 1:1,
     сейчас всё пересвечено».

     Замер полос кадра 1440x900 (docs/РАЗБОР-IGLOO-ЖИВОЙ.md) у них:
       верх       #9EA4AF … #B3B8C2
       середина   #858C99 … #ABB0B9
       низ        #6D7380 … #9CA1AC
       яркость кадра 143-171, самое тёмное место кадра #6D7380.
     Их кадр ТЕМНЕЕТ книзу: небо самое светлое, земля под ногами самая
     тёмная.

     Наш голый кадр в дневной теме до правки: верх #B5BAC3, середина
     #929AA9, низ #99999B, яркость 163.5. То есть небо уже совпадало
     до пары единиц, а низ шёл ВВЕРХ вместо вниз: земля под ногами
     выходила светлее середины кадра и была ровно того же серого, что
     и середина. Пересвечен у нас именно грунт. Небо своё место
     держало и до правки.

     Отсюда правка трёх чисел грунта; неба она коснулась одним числом,
     и по отдельной причине - разбор горизонта ниже.

     · верх 0xF1F3F8 -> 0xCFD6DE. Это верхняя половина полусферного
       света, множитель на ВЕСЬ грунт. Прежний почти белый, а у igloo
       белого нет нигде ни на одном из 71 снятого кадра, и самое
       светлое их место это небо #B3B8C2. Свет ярче собственного неба
       физически неоткуда взять: солнце тут одно и оно же красит небо.
     · склон 0xA8A49E -> 0x9EA2AC. Прежний тёплый, песочный (красного
       больше синего на десять единиц). Оттого низ кадра выходил
       нейтрально-серым, а у них он холодный: в #6D7380 синего больше
       красного на девятнадцать.
     · грунт 0x9A9DA6 -> 0x848A9C. Это цвет НЕосвещённой стороны, то
       есть тени по рельефу. Тени у igloo синие и заметные, у нас были
       почти той же светлоты, что и свет, и рельеф не читался вовсе.

     Дымка и пыль тоже дневные и тоже правятся: разбор.

     Ночную палитру выше не трогаем, это отдельная сцена. */
  /* ── ГОРИЗОНТ ДНЁМ ОПУЩЕН: У НИХ НЕБО С РАЗМАХОМ, У НАС БЫЛА ПЛИТА
     Их небо это pow((x+y)/2, 2) от горизонта к зениту, и на кадре это
     видно ДИАГОНАЛЬЮ: замер трёх точек верхней кромки кадра 002 дал
     слева #9499A4, посередине #B3B7C1, справа #CDD0D9, то есть от 153
     до 208 по яркости. Наши те же три точки шли 178, 196, 192: размах
     двадцать пять там, где у них пятьдесят пять.

     Ровное светлое небо без хода и читается «пересветом»: зацепиться
     глазу не за что, и весь верх кадра идёт молочной заливкой.
     Зенит уже совпадал (наш угол 208 против их 208), поэтому опускаем
     ТОЛЬКО горизонт, 0xAFB6C7 -> 0x959CB0.

     Заодно это чинит середину кадра: tuman() растворяет даль в тот же
     nebo(), значит дальний грунт уходит теперь в более тёмный тон, и
     средняя полоса идёт к их #858C99 вместо нашей #9199A9.

     Эти два числа читает не только Луна: тем же uColor1/uColor2 красит
     туман купол в rv-act-станция.js. Правка здесь держит их вместе
     сама собой, ради этого униформы и общие. */
  var ДЕНЬ = { зенит: 0xD1D6E3, горизонт: 0x959CB0, грунт: 0x848A9C, склон: 0x9EA2AC,
               верх: 0xCFD6DE, низ: 0x6A7290,
               /* Дымка днём была 0x5F7BD9, насыщенный синий, и клалась
                  СЛОЖЕНИЕМ поверх и без того светлого кадра: чистая
                  добавка яркости синим. У igloo пятно свечения под домом
                  белое: на светлом фоне свечение работает светлотой.
                  Цвета для этого хватает холодного светлого, и силу
                  режем почти вдвое. */
               дымка: 0x93A9DA, дымкаСила: 0.06,
               /* Пыль днём была 0x2B3560 при силе 0.35 и обычном
                  смешивании, то есть ТЁМНО-СИНИЕ крапины по светлому
                  грунту. На снимке они читались грязью. У igloo
                  приземная взвесь всегда СВЕТЛЕЕ земли: пылинка ловит
                  свет и потому видна. */
               пыль: 0xB4BCCB, пыльСила: 0.20 };
  var ПАЛ = НОЧЬ;

  /* ── Куски GLSL, общие для всех материалов ─────────────────── */
  var ОБЩЕЕ_GLSL = [
    /* ── ПЛОТНОСТЬ ТУМАНА ИДЁТ ЗА РАССТОЯНИЕМ ДО ПРЕДМЕТА ────────
       Владелец: «на мобильной версии весь сайт затемнённый, сделать
       как на ПК по текстурам и освещению».

       Замер средней яркости кадра: монитор 56.9, телефон 42.2. Свет и
       материалы одни и те же, разное только одно - РАССТОЯНИЕ. Кадр
       телефона уже, и чтобы дом занял свою долю, камера стоит в 31.7
       единицы против 20.2 на мониторе. При туманe, заданном числом на
       единицу пути, дальний план телефона съедало мглой вдвое сильнее,
       и вся нижняя половина кадра уходила в темень.

       Плотность теперь задаётся снаружи и считается от рабочей
       дистанции: полная мгла на четырёх её длинах. Тогда на обоих
       устройствах туман съедает ОДНУ И ТУ ЖЕ долю глубины сцены. */
    "uniform float uTumanK;",
    "uniform vec3 uColor1;",
    "uniform vec3 uColor2;",
    "uniform vec2 uResolution;",
    "uniform float uTime;",
    "uniform float uDen;",
    /* Экранный градиент неба. Их формула слово в слово. */
    "vec3 nebo(){",
    "  vec2 screenUv = gl_FragCoord.xy / uResolution;",
    "  float grad = (screenUv.x + screenUv.y) * 0.5;",
    "  grad = pow(grad, 2.0);",
    "  return mix(uColor2, uColor1, grad);",
    "}",
    /* Туман тем же градиентом: по глубине и по высоте. */
    "vec3 tuman(vec3 color, float mvz, float wy){",
    /* 0.011, а не 0.005. Полная мгла на девяноста единицах вместо двухсот.
       Два повода. Первый: диск грунта конечен (радиус 45), и его кромка
       на семидесяти семи единицах при прежнем тумане была видна прямой
       чертой поперёк кадра. Второй - владелец: «нету тумана свечений,
       как у igloo». У них горизонт растворяется в мгле полностью, и
       именно она делает пейзаж бесконечным. */
    "  float distanceFog = clamp(-mvz * uTumanK, 0.0, 1.0);",
    "  float heightFog = clamp(1.0 - wy * 0.35, 0.0, 1.0) * 0.25;",
    "  return mix(color, nebo(), clamp(distanceFog + heightFog, 0.0, 1.0));",
    "}",
    "float hash12(vec2 p){",
    "  vec3 p3 = fract(vec3(p.xyx) * 0.1031);",
    "  p3 += dot(p3, p3.yzx + 33.33);",
    "  return fract((p3.x + p3.y) * p3.z);",
    "}",
    /* Дизеринг против полос: их приём, 1/255 с покадровым сдвигом. */
    "vec3 dizer(vec3 c){ return c + (hash12(gl_FragCoord.xy + uTime) - 0.5) / 255.0; }",
    /* Шум без текстуры: шесть синусов, их функция дословно. */
    "#define sinlayer(fx, fy, fz) val += sin(dot(p, vec3(fx, fy, fz)));",
    "float sinenoise1(vec3 p){ float val = 0.0;",
    "  sinlayer(1.5, 3.4598, 1.234); sinlayer(3.12, -3.234, 4.221);",
    "  sinlayer(0.355, 2.3, -1.375); sinlayer(-0.156, -3.34, -0.4566);",
    "  sinlayer(-4.1235, -0.485, -1.45); sinlayer(2.54, -0.879, -2.123);",
    "  return val / 6.0; }"
  ].join("\n");

  /* ── Небо ────────────────────────────────────────────────────
     Их класс g3: сфера 800 с 12 сегментами, изнанкой наружу, чуть
     повёрнутая, чтобы шов полюса не стоял в зените. У нас радиус 300:
     сфера живёт только в акте станции, а на 800 она накрывала бы
     соседние акты и подменяла бы им небо мира. Звёзды хешем по
     направлению, без мерцания: на Луне нет воздуха, мерцать нечему. */
  var В_НЕБО = [
    "varying vec3 vDir;",
    "void main(){",
    "  vDir = normalize(position);",
    "  gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);",
    "}"
  ].join("\n");
  /* НЕБО ЛУНЫ: ЧЁРНОЕ, СО ЗВЁЗДАМИ РАЗНОЙ ВЕЛИЧИНЫ И ЦВЕТА.

     Прежде звезда была одна на всех: step(0.9985) по клетке давал
     россыпь одинаковых квадратных точек одного размера, одного цвета и
     одной яркости. Настоящее небо устроено иначе, и глаз это знает:

     · ВЕЛИЧИН МНОГО, и слабых кратно больше ярких. Три решётки разной
       частоты дают три класса: редкие яркие, средние и мелкая пыль.
     · ЗВЕЗДА КРУГЛАЯ, а не квадратная. Точка ставится в случайное место
       ВНУТРИ клетки, и яркость считается от расстояния до неё, поэтому
       от размера клетки форма больше не зависит.
     · У ЗВЁЗД ЕСТЬ ЦВЕТ. Он идёт от температуры: горячие голубовато-
       белые, холодные оранжевые. Чистого белого почти нет.
     · ЗВЁЗДЫ НЕ МЕРЦАЮТ. Мерцание это дрожь воздуха, а воздуха здесь
       нет вовсе. Небо стоит неподвижно, и это сразу читается вакуумом.
     · ЕСТЬ МЛЕЧНЫЙ ПУТЬ. Без него звёздное поле выглядит рассыпанной
       солью: настоящее небо всегда с полосой неразрешимых звёзд. */
  var Ф_НЕБО = [
    ОБЩЕЕ_GLSL,
    "uniform float uAlpha;",
    "varying vec3 vDir;",
    /* Одна решётка звёзд. Возвращает яркость, тон отдаёт наружу. */
    "float zvezda(vec2 uv, float porog, out vec3 ton){",
    "  vec2 kl = floor(uv);",
    "  ton = vec3(1.0);",
    "  float h = hash12(kl);",
    "  if (h < porog) return 0.0;",
    /* Место внутри клетки: без него звёзды стоят решёткой. */
    "  vec2 p = vec2(hash12(kl + 11.3), hash12(kl + 27.7));",
    "  float dd = length(fract(uv) - p);",
    /* Величина. Квадрат равномерной случайной даёт много слабых и мало
       ярких, то есть примерно ту же картину, что и настоящий счёт
       звёзд по величинам. */
    "  float m = hash12(kl + 5.9); m *= m;",
    "  float r = mix(0.075, 0.22, m);",
    "  float ya = smoothstep(r, 0.0, dd);",
    "  ya *= ya;",
    /* Цвет по температуре: от голубовато-белой к оранжевой. Подмес
       белого не даёт небу стать цветной крупой. */
    "  float t = hash12(kl + 19.1);",
    "  ton = mix(vec3(0.74, 0.82, 1.0), vec3(1.0, 0.84, 0.66), t * t);",
    "  ton = mix(ton, vec3(1.0), 0.34);",
    "  return ya * mix(0.28, 1.0, m);",
    "}",
    /* Ценностный шум для пятнистости млечной полосы. */
    "float pyatna(vec2 p){",
    "  vec2 i = floor(p), f = fract(p);",
    "  f = f * f * (3.0 - 2.0 * f);",
    "  float a = hash12(i), b = hash12(i + vec2(1.0, 0.0));",
    "  float c2 = hash12(i + vec2(0.0, 1.0)), d2 = hash12(i + vec2(1.0, 1.0));",
    "  return mix(mix(a, b, f.x), mix(c2, d2, f.x), f.y);",
    "}",
    "void main(){",
    "  vec3 c = nebo();",
    "  vec3 dn = normalize(vDir);",
    /* Равноугольные координаты: долгота и широта. По ним клетки почти
       не тянет у полюсов, в отличие от прежних atan и y напрямую. */
    "  vec2 sf = vec2(atan(dn.z, dn.x) * 0.15915494, asin(clamp(dn.y, -1.0, 1.0)) * 0.31830989);",
    "  vec3 t1, t2, t3;",
    "  float s1 = zvezda(sf * 330.0, 0.720, t1);",
    "  float s2 = zvezda(sf * 152.0, 0.885, t2);",
    "  float s3 = zvezda(sf * 62.0,  0.962, t3);",
    "  vec3 st = t1 * s1 * 0.80 + t2 * s2 * 1.75 + t3 * s3 * 3.10;",
    /* МЛЕЧНЫЙ ПУТЬ. Большой круг с наклоном; яркость падает по
       расстоянию до его плоскости. Пятнистость двумя масштабами шума,
       тёмная прожилка посередине это пылевые облака в плоскости
       Галактики, они у настоящей полосы всегда есть. */
    "  vec3 os = normalize(vec3(0.38, 0.60, -0.70));",
    "  float pp = abs(dot(dn, os));",
    "  float ml = exp(-pp * pp * 13.0);",
    "  float sh = pyatna(sf * 34.0) * 0.6 + pyatna(sf * 88.0) * 0.4;",
    "  ml *= 0.42 + 0.72 * sh;",
    "  ml *= 1.0 - 0.55 * smoothstep(0.0, 0.16, 0.16 - pp * 4.2) * pyatna(sf * 21.0 + 4.0);",
    "  st += vec3(0.66, 0.72, 0.92) * ml * 0.38;",
    /* Ниже горизонта звёзд нет: там грунт. Полоса перехода узкая,
       воздуха, который размывал бы её, здесь нет. */
    "  st *= smoothstep(-0.02, 0.06, dn.y);",
    "  c += st * (1.0 - uDen);",
    "  gl_FragColor = vec4(dizer(c), uAlpha);",
    "}"
  ].join("\n");

  /* ── Грунт ───────────────────────────────────────────────────
     Их классы y3 и hF. Смещение вершин посчитано ОДИН раз при сборке в
     JS (шесть синусов плюс кратеры), нормали пересчитаны честно. В
     шейдере остаётся свет по нормали, крупа, пульс свечения, четыре
     контактные тени и растворение кромки. */
  var В_ГРУНТ = [
    "varying vec3 vPos;",
    "varying vec3 vNor;",
    "varying vec3 vWorld;",
    "varying float vMvz;",
    "void main(){",
    "  vPos = position;",
    "  vNor = normalize(normalMatrix * normal);",
    "  vec4 w = modelMatrix * vec4(position, 1.0);",
    "  vWorld = w.xyz;",
    "  vec4 mv = viewMatrix * w;",
    "  vMvz = mv.z;",
    "  gl_Position = projectionMatrix * mv;",
    "}"
  ].join("\n");
  var Ф_ГРУНТ = [
    ОБЩЕЕ_GLSL,
    "uniform vec3 uGrunt;",
    "uniform vec3 uSklon;",
    "uniform vec3 uVerh;",
    "uniform vec3 uNiz;",
    "uniform vec3 uDymka;",
    "uniform vec3 uShadows[4];",
    "uniform float uRadius;",
    "uniform sampler2D tAlb;",
    "uniform sampler2D tNor;",
    "uniform sampler2D tFoto;",
    "uniform float uFoto;",
    "varying vec3 vPos;",
    "varying vec3 vNor;",
    "varying vec3 vWorld;",
    "varying float vMvz;",
    "void main(){",
    "  vec3 n = normalize(vNor);",
    /* Фактура: карта нормалей на двух масштабах (крупный и мелкий), в
       мировых координатах, грунт почти горизонтален, поэтому нормаль
       карты кладётся на xz напрямую. Альбедо теми же выборками. */
    "  vec2 uv1 = vWorld.xz * 0.09, uv2 = vWorld.xz * 0.41;",
    "  vec3 nm = (texture2D(tNor, uv1).xyz * 2.0 - 1.0) * 0.8 + (texture2D(tNor, uv2).xyz * 2.0 - 1.0) * 0.45;",
    "  n = normalize(n + vec3(nm.x, 0.0, nm.y));",
    "  float alb = texture2D(tAlb, uv1).r * 0.6 + texture2D(tAlb, uv2).r * 0.4;",
    /* Фото реголита на двух масштабах, зеркальный повтор прячет шов.
       Средняя яркость фото около 0.5, поэтому делим на неё. */
    /* Два масштаба под разным поворотом: одинаковые следы колёс через
       каждые восемь единиц читались повтором плитки. */
    "  vec2 pv = vWorld.xz * 0.055;",
    "  vec2 pr = vec2(pv.x * 0.6 - pv.y * 0.8, pv.x * 0.8 + pv.y * 0.6) * 4.2 + 0.37;",
    "  vec3 foto = texture2D(tFoto, pv).rgb * 0.5 + texture2D(tFoto, pr).rgb * 0.5;",
    "  alb = mix(alb, (foto.r + foto.g + foto.b) / 3.0 / 0.5 * 0.5, uFoto);",
    /* Свет по нормали: верх и низ, без источников. Солнце жёсткое,
       сверху-слева: его добавка ступенькой по нормали, как на Луне без
       воздуха - тень резкая. */
    "  vec3 sun = normalize(vec3(-0.55, 0.78, 0.3));",
    "  float hemi = n.y * 0.5 + 0.5;",
    "  vec3 svet = mix(uNiz, uVerh, hemi);",
    /* ── ОКНО СОЛНЦА ШИРЕ ДНЁМ ────────────────────────────────
       Грунт почти горизонтален, солнце стоит высоко (0.78 по y), и на
       окне 0.1..0.35 косинус угла проходит верхнюю границу на КАЖДОЙ
       точке ближнего плана. Значит sol там ровно единица везде, mix
       грунта со склоном всегда даёт склон, множитель (0.5+0.65*sol)
       всегда даёт 1.15 - рельефа в свете нет вовсе, и ближняя земля
       выходит ровной плитой одного тона. Это и видно на нашем кадре:
       низ #99999B без единого перепада.

       На их кадре 004 ближняя земля лепится светом: борта складок
       светлее, обратные скаты заметно темнее и синее. Раздвигаем окно
       так, чтобы карта нормалей начала работать светом. До правки её
       было видно одной мелкой крупой.

       Ночь оставлена как была ДО ЕДИНИЦЫ: там верхняя граница по-
       прежнему 0.35, ночная сцена отдельная и её никто не отменял. */
    "  float sunEdge = mix(0.35, 0.74, uDen);",
    "  float sol = smoothstep(0.1, sunEdge, dot(n, sun));",
    "  vec3 base = mix(uGrunt, uSklon, sol) * (0.6 + 0.8 * alb);",
    "  vec3 color = base * svet * (0.5 + 0.65 * sol);",
    /* Крупа реголита: их hash12 по мировым координатам. */
    "  color *= 1.0 + (hash12(vWorld.xz * 40.0) - 0.5) * 0.125;",
    /* Пульс свечения от станции, их формула. */
    "  float glowStrength = sin(vPos.x - uTime * 1.0 + 3.2) * 0.5 + 0.5;",
    "  float near = 1.0 - smoothstep(0.0, 9.0, length(vPos.xz));",
    "  color += uDymka * 0.06 * glowStrength * near;",
    /* Контактные тени. Синие, не чёрные: это их приём целиком. */
    "  for (int i = 0; i < 4; i++) {",
    "    vec3 s = uShadows[i];",
    "    if (s.z <= 0.0) continue;",
    "    float shadow = min(1.0, length((vPos.xz - s.xy) / s.z * 1.5));",
    "    shadow = pow(shadow, 2.0);",
    "    shadow += sin(uTime * 3.3 + vPos.z * 5.0) * 0.1 + 0.1;",
    "    shadow += sin(uTime * 3.1 + vPos.x * 4.0) * 0.1 + 0.1;",
    "    shadow = mix(0.5, 1.0, clamp(shadow, 0.0, 1.0));",
    "    color *= mix(vec3(0.5, 0.7, 1.0) * 0.55, vec3(1.0), shadow);",
    "  }",
    "  color = tuman(color, vMvz, vWorld.y);",
    /* Растворение кромки: их smoothstep, множитель под свой радиус. */
    "  float alpha = 1.0 - smoothstep(0.8, 1.0, length(vPos.xz) / uRadius);",
    "  gl_FragColor = vec4(dizer(color), alpha);",
    "}"
  ].join("\n");

  /* ── Земля ────────────────────────────────────────────────────
     Сфера с терминатором по тому же солнцу, облака шестью синусами,
     ободок атмосферы через Френеля. */
  var В_ЗЕМЛЯ = [
    "varying vec3 vN;",
    "varying vec3 vV;",
    "varying vec3 vP;",
    "varying vec2 vUv;",
    "varying float vMvz;",
    "void main(){",
    "  vUv = uv;",
    "  vN = normalize(mat3(modelMatrix) * normal);",
    "  vec4 w = modelMatrix * vec4(position, 1.0);",
    "  vP = position;",
    "  vV = normalize(cameraPosition - w.xyz);",
    "  vec4 mv = viewMatrix * w;",
    "  vMvz = mv.z;",
    "  gl_Position = projectionMatrix * mv;",
    "}"
  ].join("\n");
  var Ф_ЗЕМЛЯ = [
    ОБЩЕЕ_GLSL,
    "uniform sampler2D tEarth;",
    "uniform float uEst;",
    "varying vec3 vN;",
    "varying vec3 vV;",
    "varying vec3 vP;",
    "varying vec2 vUv;",
    "varying float vMvz;",
    "void main(){",
    "  vec3 n = normalize(vN);",
    "  vec3 sun = normalize(vec3(-0.55, 0.78, 0.3));",
    /* Терминатор. Стояло окно 0.40 по косинусу, это очень мягкая
       растушёвка, от неё граница дня и ночи расползалась на четверть
       диска. У настоящей Земли её размывает только атмосфера, и с Луны
       это узкая полоса. */
    "  float day = smoothstep(-0.06, 0.16, dot(n, sun));",
    /* Настоящая Земля: карта NASA Blue Marble (общественное достояние).
       Пока она не доехала, суша рисуется шумом, как раньше. */
    "  float land = smoothstep(0.15, 0.35, sinenoise1(vP * 0.9 + 3.0));",
    "  vec3 more = vec3(0.16, 0.36, 0.72);",
    "  vec3 susha = vec3(0.34, 0.42, 0.30);",
    "  vec3 c = mix(more, susha, land);",
    /* Снимок NASA идёт БЕЗ множителя. Стояло 1.15, и океан уходил в
       ярко-синее пятно: снимок уже снят по свету, поднимать его нечем. */
    "  vec3 tex = texture2D(tEarth, vec2(vUv.x + uTime * 0.004, vUv.y)).rgb;",
    "  c = mix(c, tex, uEst);",
    /* ПРОЦЕДУРНЫХ ОБЛАКОВ БОЛЬШЕ НЕТ. Поверх настоящего снимка они
       клали белые кляксы синусного шума, и Земля читалась заляпанной.
       Облака есть на самом снимке, рисовать их второй раз незачем. */
    /* Ночная сторона. Абсолютный ноль здесь неверен: ночную Землю
       подсвечивает Луна, и край диска виден всегда. */
    "  c *= 0.045 + 0.955 * day;",
    /* АТМОСФЕРА. Тонкий ободок по краю диска, и только на освещённой
       стороне: ночной лимб подсвечивать нечем. Степень поднята с 3 до 6,
       иначе свечение расползалось на треть диска и Земля выходила
       размытым шаром вместо планеты с чёткой кромкой. */
    "  float fres = pow(1.0 - clamp(dot(n, normalize(vV)), 0.0, 1.0), 6.0);",
    "  c += vec3(0.42, 0.66, 1.0) * fres * (0.10 + 0.95 * day);",
    /* ТУМАНА НА ЗЕМЛЕ НЕТ. Между Луной и Землёй вакуум, размывать
       нечем. Прежний tuman подмешивал в планету цвет неба и сажал
       контраст диска, отчего она висела блёклым пятном. */
    /* ── ДНЁМ ДИСК ВСЁ ЖЕ САДИТСЯ В НЕБО, НО ЧУТЬ-ЧУТЬ ─────────
       Ночью Земля светит на чёрном и вакуум читается сам. Днём небо
       у нас #B5BAC3, а ночная половина диска идёт от 0.045 яркости,
       то есть почти нулём. На светлом кадре она читается ДЫРОЙ,
       вырезанной в небе, и глаз принимает её за брак вывода.

       Владелец просил Землю видимой, поэтому не гасим её и не
       осветляем весь диск: подмешиваем одну восьмую неба. Диск
       остаётся самым тёмным и самым читаемым предметом кадра, но
       перестаёт быть провалом в ноль. Ночью uDen нулевой и слагаемое
       исчезает целиком. */
    "  c = mix(c, nebo(), 0.13 * uDen);",
    "  gl_FragColor = vec4(dizer(c), 1.0);",
    "}"
  ].join("\n");

  /* ── Дымка свечения и лучи ────────────────────────────────────
     Их дым: три выборки ОДНОЙ тайловой текстуры на масштабах 1, 2, 3 с
     одним ходом, ПЕРЕМНОЖЕННЫЕ, поэтому в результате нет ни повтора
     плитки, ни ровной каши. Лучи: их lightshaft, две выборки и
     круговой спад. */
  var В_ПЛОСК = [
    "varying vec2 vUv;",
    "varying vec3 vPos;",
    "varying float vMvz;",
    "void main(){",
    "  vUv = uv; vPos = position;",
    "  vec4 mv = modelViewMatrix * vec4(position, 1.0);",
    "  vMvz = mv.z;",
    "  gl_Position = projectionMatrix * mv;",
    "}"
  ].join("\n");
  var Ф_ДЫМКА = [
    ОБЩЕЕ_GLSL,
    "uniform sampler2D tWind;",
    "uniform vec3 uCol;",
    "uniform float uSila;",
    "uniform float uPoryv;",
    "varying vec2 vUv;",
    "varying vec3 vPos;",
    "void main(){",
    "  float t = -uTime * 0.075 * (1.0 + uPoryv);",
    "  vec2 uv = vUv * 2.0;",
    "  float wind = texture2D(tWind, uv * 1.0 + vec2(-t, t * 0.7)).r;",
    "  wind *= texture2D(tWind, uv * 2.0 + vec2(-t, t * 0.7)).r;",
    "  wind *= texture2D(tWind, uv * 3.0 + vec2(-t, t * 0.7)).r;",
    "  float r = length(vUv - 0.5) * 2.0;",
    "  float ring = smoothstep(0.25, 0.5, r) * (1.0 - smoothstep(0.7, 1.0, r));",
    "  float a = wind * 2.6 * ring * uSila;",
    "  gl_FragColor = vec4(uCol * a, a);",
    "}"
  ].join("\n");
  var Ф_ЛУЧ = [
    ОБЩЕЕ_GLSL,
    "uniform sampler2D tMap;",
    "uniform vec3 uCol;",
    "uniform float uAlpha;",
    "varying vec2 vUv;",
    "void main(){",
    "  float t = uTime * 0.12;",
    "  float noise = texture2D(tMap, vUv * vec2(1.0, 0.46) + vec2(t, t * 0.323)).r;",
    "  noise += texture2D(tMap, vUv * vec2(0.5, 0.25) + vec2(-t * 0.77, -t * 0.414)).r;",
    "  float circularGradient = pow(1.0 - clamp(length(vUv - 0.5) * 2.0, 0.0, 1.0), 2.0);",
    "  float a = noise * circularGradient * uAlpha;",
    "  gl_FragColor = vec4(uCol * a, a);",
    "}"
  ].join("\n");

  /* ── Пыль ─────────────────────────────────────────────────────
     Низовая, по ветру вдоль x, с завихрением шестью синусами. Порыв
     раз в 6-9 секунд удваивает ветер на полторы секунды - это и есть
     «метель», какой она бывает без воздуха: пыль, поднятая и опавшая. */
  /* Вершинному шейдеру общий кусок не годится: в нём nebo() читает
     gl_FragCoord, а его в вершинах нет, и программа не собиралась
     (поймано первым же снимком: «Vertex shader is not compiled»).
     Пыли из общего нужен только шум. */
  var В_ПЫЛЬ = [
    "uniform float uTime;",
    "#define sinlayer(fx, fy, fz) val += sin(dot(p, vec3(fx, fy, fz)));",
    "float sinenoise1(vec3 p){ float val = 0.0;",
    "  sinlayer(1.5, 3.4598, 1.234); sinlayer(3.12, -3.234, 4.221);",
    "  sinlayer(0.355, 2.3, -1.375); sinlayer(-0.156, -3.34, -0.4566);",
    "  sinlayer(-4.1235, -0.485, -1.45); sinlayer(2.54, -0.879, -2.123);",
    "  return val / 6.0; }",
    "attribute float aSeed;",
    "uniform float uH;",
    "uniform float uPoryv;",
    "uniform float uRadius;",
    "varying float vA;",
    "void main(){",
    "  vec3 p = position;",
    "  float t = uTime * (0.35 + aSeed * 0.4) * (1.0 + uPoryv * 1.2);",
    "  p.x += t * 1.6;",
    "  p.x = mod(p.x + uRadius, uRadius * 2.0) - uRadius;",
    "  p.y += sin(uTime * (0.7 + aSeed) + aSeed * 40.0) * 0.12 + uPoryv * aSeed * 0.5;",
    "  p.z += sinenoise1(p * 0.35 + uTime * 0.08) * 0.35;",
    "  vec4 mv = modelViewMatrix * vec4(p, 1.0);",
    "  gl_PointSize = clamp(0.32 / max(0.5, -mv.z) * uH * 0.5, 1.0, 9.0);",
    "  vA = (1.0 - smoothstep(0.6, 1.0, length(p.xz) / uRadius)) * (0.4 + 0.6 * aSeed);",
    "  gl_Position = projectionMatrix * mv;",
    "}"
  ].join("\n");
  var Ф_ПЫЛЬ = [
    "uniform vec3 uCol;",
    "uniform float uSila;",
    "varying float vA;",
    "void main(){",
    "  vec2 c = gl_PointCoord - 0.5;",
    "  float r = dot(c, c) * 4.0;",
    "  if (r > 1.0) discard;",
    "  float a = pow(1.0 - r, 1.8) * vA * uSila;",
    "  gl_FragColor = vec4(uCol, a);",
    "}"
  ].join("\n");

  /* ── Шумовая текстура ─────────────────────────────────────────
     Рисуется в холст при сборке: сумма синусов с ЦЕЛЫМИ частотами по
     обеим осям тайлится по краям сама собой, и шов не виден. В файл
     не кладём: 256x256 считается быстрее, чем качается. */
  function шумТекстура() {
    var Р = 256;
    var х = d.createElement("canvas"); х.width = Р; х.height = Р;
    var к = х.getContext("2d");
    var img = к.createImageData(Р, Р);
    var i, j, v;
    for (j = 0; j < Р; j++) {
      for (i = 0; i < Р; i++) {
        var u = i / Р * Math.PI * 2, w = j / Р * Math.PI * 2;
        v = 0;
        v += Math.sin(u * 1 + Math.cos(w * 2)) * 0.5;
        v += Math.sin(w * 3 + Math.sin(u * 2) * 1.3) * 0.35;
        v += Math.sin(u * 5 + w * 4) * 0.2;
        v += Math.sin(u * 9 - w * 7) * 0.1;
        v = v * 0.5 + 0.5;
        v = Math.pow(зажать(v, 0, 1), 1.4);
        var б = Math.round(v * 255), o = (j * Р + i) * 4;
        img.data[o] = б; img.data[o + 1] = б; img.data[o + 2] = б; img.data[o + 3] = 255;
      }
    }
    к.putImageData(img, 0, 0);
    var тек = new T.CanvasTexture(х);
    тек.wrapS = tek(); тек.wrapT = tek();
    function tek() { return T.RepeatWrapping; }
    return тек;
  }

  /* ── Фактура реголита ─────────────────────────────────────────
     Две карты, посчитанные при сборке: альбедо и нормали. Без них грунт
     вблизи это ровная плита с крупой в один пиксель, и это было видно на
     первом кадре. Высота это пять октав ценностного шума на решётке 64
     плюс мелкие крапины камней; нормаль из разностей высот. Файлов нет,
     512x512 считается быстрее, чем качается. */
  function реголит() {
    var Р = W.ступень === 2 ? 512 : 384, С = 64;
    var семя = 17;
    function сл() { семя = (семя * 9301 + 49297) % 233280; return семя / 233280; }
    var сет = new Float32Array(С * С);
    for (var i0 = 0; i0 < сет.length; i0++) сет[i0] = сл();
    function узел(x, y) { return сет[((y % С + С) % С) * С + ((x % С + С) % С)]; }
    function шум(u, v) {
      var x0 = Math.floor(u), y0 = Math.floor(v), fx = u - x0, fy = v - y0;
      fx = fx * fx * (3 - 2 * fx); fy = fy * fy * (3 - 2 * fy);
      var a = узел(x0, y0), b = узел(x0 + 1, y0), c = узел(x0, y0 + 1), d = узел(x0 + 1, y0 + 1);
      return (a + (b - a) * fx) * (1 - fy) + (c + (d - c) * fx) * fy;
    }
    var выс = new Float32Array(Р * Р);
    var k, j, i;
    for (j = 0; j < Р; j++) {
      for (i = 0; i < Р; i++) {
        var h = 0, амп = 1, ч = 4, сум = 0;
        for (k = 0; k < 5; k++) { h += шум(i / Р * ч, j / Р * ч) * амп; сум += амп; амп *= 0.5; ч *= 2; }
        h /= сум;
        выс[j * Р + i] = h;
      }
    }
    /* Крапины камней: полторы сотни мелких бугорков. */
    for (k = 0; k < 160; k++) {
      var cx = сл() * Р, cy = сл() * Р, r = 2 + сл() * 5, g = 0.15 + сл() * 0.25;
      for (j = -r; j <= r; j++) for (i = -r; i <= r; i++) {
        var dd = Math.hypot(i, j) / r; if (dd > 1) continue;
        var xx = ((Math.round(cx + i) % Р) + Р) % Р, yy = ((Math.round(cy + j) % Р) + Р) % Р;
        выс[yy * Р + xx] += g * (1 - dd * dd);
      }
    }
    var альб = d.createElement("canvas"); альб.width = Р; альб.height = Р;
    var норм = d.createElement("canvas"); норм.width = Р; норм.height = Р;
    var ка = альб.getContext("2d"), кн = норм.getContext("2d");
    var ia = ка.createImageData(Р, Р), inn = кн.createImageData(Р, Р);
    for (j = 0; j < Р; j++) {
      for (i = 0; i < Р; i++) {
        var o = (j * Р + i) * 4;
        var hh = выс[j * Р + i];
        var hx = выс[j * Р + (i + 1) % Р] - выс[j * Р + (i - 1 + Р) % Р];
        var hy = выс[((j + 1) % Р) * Р + i] - выс[((j - 1 + Р) % Р) * Р + i];
        var сила = 6.0;
        var nx = -hx * сила, ny = -hy * сила, nz = 1;
        var L = Math.hypot(nx, ny, nz);
        inn.data[o] = Math.round((nx / L * 0.5 + 0.5) * 255);
        inn.data[o + 1] = Math.round((ny / L * 0.5 + 0.5) * 255);
        inn.data[o + 2] = Math.round((nz / L * 0.5 + 0.5) * 255);
        inn.data[o + 3] = 255;
        var a = зажать(0.55 + (hh - 0.5) * 0.9, 0, 1) * 255;
        ia.data[o] = Math.round(a); ia.data[o + 1] = Math.round(a); ia.data[o + 2] = Math.round(a * 1.03); ia.data[o + 3] = 255;
      }
    }
    ка.putImageData(ia, 0, 0); кн.putImageData(inn, 0, 0);
    var tA = new T.CanvasTexture(альб), tN = new T.CanvasTexture(норм);
    tA.wrapS = tA.wrapT = T.RepeatWrapping; tN.wrapS = tN.wrapT = T.RepeatWrapping;
    return { альбедо: tA, нормали: tN };
  }

  /* ── Грунт с кратерами ────────────────────────────────────────
     Смещение считается в JS, потому что нормали нужны честные: шейдер
     мог бы сместить вершину, но не знал бы соседей и дал бы плоский
     свет. Кратер это воронка smoothstep с валом на 0.85..1.0 радиуса.
     Ни один не под станцией: центр держим пустым на 5 единиц. */
  function грунт(радиус) {
    var сег = W.ступень === 0 ? 90 : 160;
    var гео = new T.PlaneGeometry(радиус * 2, радиус * 2, сег, сег);
    гео.rotateX(-Math.PI / 2);
    var поз = гео.attributes.position;
    var кратеры = [];
    var семя = 7;
    function сл() { семя = (семя * 9301 + 49297) % 233280; return семя / 233280; }
    for (var k = 0; k < 9; k++) {
      var уг = сл() * Math.PI * 2, дал = 7 + сл() * (радиус * 0.8 - 7);
      кратеры.push({ x: Math.cos(уг) * дал, z: Math.sin(уг) * дал, r: 1 + сл() * 5, г: 0.25 + сл() * 0.6 });
    }
    /* Мелкие кратеры ближе к камере: крупные стоят за семь единиц и в
       кадр с двенадцати попадают краем, а без рельефа вблизи грунт
       читался плоской плитой. */
    for (var k1 = 0; k1 < 6; k1++) {
      var уг1 = сл() * Math.PI * 2, дал1 = 4.2 + сл() * 3.5;
      кратеры.push({ x: Math.cos(уг1) * дал1, z: Math.sin(уг1) * дал1, r: 0.5 + сл() * 1.1, г: 0.08 + сл() * 0.18 });
    }
    function шум(x, z) {
      var p = [x * 0.13, z * 0.13, 0.4];
      var v = 0;
      var L = [[1.5, 3.4598, 1.234], [3.12, -3.234, 4.221], [0.355, 2.3, -1.375],
               [-0.156, -3.34, -0.4566], [-4.1235, -0.485, -1.45], [2.54, -0.879, -2.123]];
      for (var i = 0; i < 6; i++) v += Math.sin(p[0] * L[i][0] + p[1] * L[i][1] + p[2] * L[i][2]);
      return v / 6;
    }
    for (var i = 0; i < поз.count; i++) {
      var x = поз.getX(i), z = поз.getZ(i);
      var y = шум(x, z) * 0.55 + шум(x * 2.7 + 11, z * 2.7) * 0.18;
      /* Площадка под станцией ровная: купол стоит, а не висит. */
      var центр = Math.hypot(x, z);
      y *= зажать((центр - 3.2) / 3.0, 0, 1);
      for (var k2 = 0; k2 < кратеры.length; k2++) {
        var к = кратеры[k2];
        var р = Math.hypot(x - к.x, z - к.z) / к.r;
        if (р < 1.15) {
          var чаша = -к.г * (1 - зажать(р / 0.85, 0, 1) * зажать(р / 0.85, 0, 1));
          var вал = к.г * 0.35 * Math.max(0, 1 - Math.abs(р - 0.95) / 0.2);
          y += чаша + вал;
        }
      }
      поз.setY(i, y);
    }
    поз.needsUpdate = true;
    гео.computeVertexNormals();
    return гео;
  }

  function цвет(h) { return new T.Color(h); }

  function собрать(мир) {
    if (собрано) return М.корень;
    W = мир; T = W.T;
    if (!T) return null;
    собрано = true;

    ГРАД.uColor1.value = цвет(ПАЛ.зенит);
    ГРАД.uColor2.value = цвет(ПАЛ.горизонт);
    ГРАД.uResolution.value = new T.Vector2(1, 1);
    /* Плотность тумана общая для всех материалов Луны: она приходит
       снаружи и обязана быть одной, иначе грунт, небо и дымка разойдутся
       по глубине. */
    if (!ГРАД.uTumanK) ГРАД.uTumanK = { value: 0.011 };

    М.корень = new T.Group();
    М.корень.name = "луна";
    М.радиус = 45;

    var шум = шумТекстура();
    М.шум = шум;

    /* Небо. */
    М.небоМат = new T.ShaderMaterial({
      uniforms: { uTumanK: ГРАД.uTumanK, uColor1: ГРАД.uColor1, uColor2: ГРАД.uColor2, uResolution: ГРАД.uResolution,
                  uTime: ГРАД.uTime, uDen: ГРАД.uDen, uAlpha: { value: 1 } },
      vertexShader: В_НЕБО, fragmentShader: Ф_НЕБО,
      side: T.BackSide, depthWrite: false, transparent: true, fog: false
    });
    М.небо = new T.Mesh(new T.SphereGeometry(300, 12, 12), М.небоМат);
    М.небо.scale.x = -1;
    М.небо.rotation.x = 16 * Math.PI / 180;
    М.небо.rotation.z = -16 * Math.PI / 180;
    М.небо.renderOrder = -50;
    М.небо.frustumCulled = false;
    М.корень.add(М.небо);

    /* Грунт. */
    М.тени = [];
    var тениU = [];
    for (var i = 0; i < 4; i++) тениU.push(new T.Vector3(0, 0, 0));
    var фактура = реголит();
    М.грунтМат = new T.ShaderMaterial({
      uniforms: {
        uTumanK: ГРАД.uTumanK,
        uColor1: ГРАД.uColor1, uColor2: ГРАД.uColor2, uResolution: ГРАД.uResolution,
        uTime: ГРАД.uTime, uDen: ГРАД.uDen,
        uGrunt: { value: цвет(ПАЛ.грунт) }, uSklon: { value: цвет(ПАЛ.склон) },
        uVerh: { value: цвет(ПАЛ.верх) }, uNiz: { value: цвет(ПАЛ.низ) },
        uDymka: { value: цвет(ПАЛ.дымка) },
        uShadows: { value: тениU }, uRadius: { value: М.радиус },
        tAlb: { value: фактура.альбедо }, tNor: { value: фактура.нормали },
        tFoto: { value: фактура.альбедо }, uFoto: { value: 0 }
      },
      vertexShader: В_ГРУНТ, fragmentShader: Ф_ГРУНТ,
      transparent: true, fog: false
    });
    М.грунт = new T.Mesh(грунт(М.радиус), М.грунтМат);
    М.грунт.renderOrder = -10;
    М.корень.add(М.грунт);

    /* Земля. */
    М.земляМат = new T.ShaderMaterial({
      uniforms: { uTumanK: ГРАД.uTumanK, uColor1: ГРАД.uColor1, uColor2: ГРАД.uColor2, uResolution: ГРАД.uResolution,
                  uTime: ГРАД.uTime, uDen: ГРАД.uDen,
                  tEarth: { value: null }, uEst: { value: 0 } },
      vertexShader: В_ЗЕМЛЯ, fragmentShader: Ф_ЗЕМЛЯ, fog: false
    });
    /* Карты с фотографий NASA (общественное достояние): Земля Blue
       Marble 2048x1024 и реголит с кадра Apollo 17, выровненный по
       освещению и сложенный зеркально в квадрат. Едут лениво, до
       приезда работают формулы. */
    var загр = new T.TextureLoader();
    загр.load("assets/gen/земля-2k.jpg", function (тек) {
      тек.colorSpace = T.SRGBColorSpace || тек.colorSpace;
      тек.wrapS = T.RepeatWrapping;
      М.земляМат.uniforms.tEarth.value = тек;
      М.земляМат.uniforms.uEst.value = 1;
    });
    загр.load("assets/gen/реголит.jpg", function (тек) {
      тек.colorSpace = T.SRGBColorSpace || тек.colorSpace;
      тек.wrapS = tek(); тек.wrapT = tek();
      function tek() { return T.MirroredRepeatWrapping; }
      М.грунтМат.uniforms.tFoto.value = тек;
      М.грунтМат.uniforms.uFoto.value = 1;
    });

    /* ФОТОСКАН ВМЕСТО ПОСЧИТАННЫХ ХОЛСТОВ.

       Владелец сказал про грунт то же, что про весь сайт: не должен
       отличаться от реальной жизни. Холсты выше (функция реголит) дают
       правильную статистику и неправильную частность: пять октав шума
       ложатся слишком ровно, соседние миллиметры отражают одинаково, и
       глаз читает поверхность как рисунок. Здесь в те же два сэмплера
       кладётся фотограмметрия: настоящая поверхность, снятая в поле и
       разложенная на альбедо и нормаль (ambientCG, CC0).

       Шейдер не трогаем ни строкой. Он и так берёт tAlb каналом r и tNor
       как карту нормалей на двух масштабах, а холсты остаются рабочим
       запасом на те секунды, пока файлы едут, и на случай отказа сети.

       Анизотропия обязательна: грунт уходит к горизонту под очень острым
       углом, и без неё дальняя половина кадра идёт мылом и муаром. */
    var анизоГрунт = 4;
    try {
      var рис = W && W.r;
      if (рис && рис.capabilities) анизоГрунт = Math.max(1, Math.min(8, рис.capabilities.getMaxAnisotropy()));
    } catch (e) {}
    загр.load("assets/gen/pbr/реголит-цвет.webp", function (тек) {
      тек.colorSpace = T.SRGBColorSpace || тек.colorSpace;
      тек.wrapS = тек.wrapT = T.RepeatWrapping;
      тек.anisotropy = анизоГрунт;
      М.грунтМат.uniforms.tAlb.value = тек;
    });
    загр.load("assets/gen/pbr/реголит-норм.webp", function (тек) {
      /* Рельеф это ЧИСЛА, а не цвет: гамма-кривая sRGB исказила бы сам
         вектор нормали, и свет по грунту поехал бы. */
      тек.colorSpace = T.NoColorSpace || T.LinearSRGBColorSpace || тек.colorSpace;
      тек.wrapS = тек.wrapT = T.RepeatWrapping;
      тек.anisotropy = анизоГрунт;
      М.грунтМат.uniforms.tNor.value = тек;
    });
    /* Земля дальше и меньше: на первом кадре с радиусом 9 на 62 единицах
       она занимала угол и резалась кромкой. */
    М.земля = new T.Mesh(new T.SphereGeometry(8.2, 64, 64), М.земляМат);
    /* Ниже: камера стоит на 2.2 над грунтом и смотрит вниз на десять
       градусов, и на 21 по высоте Земля резалась шапкой. */
    /* ── ЗЕМЛЯ СТОИТ ТАМ, ГДЕ ЕЁ ВИДНО ─────────────────────────
       Владелец: «земля на фоне не видна». Так и было: она стояла в 34
       вправо от начала координат Луны, а после того как Луну подвели
       под дом, это оказалось шестнадцать градусов вправо от курса - за
       краем стоячего кадра, у которого весь полукадр одиннадцать
       градусов. Плюс её закрывал сам дом, он тоже справа.

       Считано от кадра: цель - левее оси на 0.55 полуширины и выше на
       0.35 полувысоты. На телефоне это шесть градусов влево и восемь
       вверх при удалении в сто десять единиц. */
    М.земля.position.set(-11.4, 18.8, -79);
    М.земля.renderOrder = -40;
    М.корень.add(М.земля);

    /* Дымка свечения: кольцо над грунтом вокруг станции. */
    М.дымкаМат = new T.ShaderMaterial({
      uniforms: { uTumanK: ГРАД.uTumanK, uColor1: ГРАД.uColor1, uColor2: ГРАД.uColor2, uResolution: ГРАД.uResolution,
                  uTime: ГРАД.uTime, uDen: ГРАД.uDen, tWind: { value: шум },
                  uCol: { value: цвет(ПАЛ.дымка) }, uSila: { value: ПАЛ.дымкаСила * 1.6 },
                  uPoryv: { value: 0 } },
      vertexShader: В_ПЛОСК, fragmentShader: Ф_ДЫМКА,
      transparent: true, depthWrite: false, blending: T.AdditiveBlending, fog: false
    });
    /* Плоскость свечения у грунта была 15 на 15 - пятно вокруг начала
       координат, которое после переезда Луны под дом накрывало ровно
       его подножие и обрывалось. Сорок на сорок закрывает всю видимую
       площадку и уходит в туман, не показывая своего края. */
    М.дымка = new T.Mesh(new T.PlaneGeometry(40, 40), М.дымкаМат);
    М.дымка.rotation.x = -Math.PI / 2;
    М.дымка.position.y = 0.18;
    М.дымка.renderOrder = 5;
    М.корень.add(М.дымка);

    /* Лучей здесь больше нет: они переехали в купол, к арке. Два квада у
       грунта, снятые с igloo как есть, у нас ложились плоской лентой
       поперёк нижней части кадра и читались полосой света без источника
       (поймано пробой: без них низ кадра гаснет с 75 до 35). У igloo
       камера смотрит на стену, у нас на грунт под собой, и один и тот же
       квад ведёт себя по-разному. */
    М.лучи = [];

    /* ПЫЛЬ. Счёт срезан втрое с лишним, и это правка по физике, а не по
       вкусу. Висящей пыли на Луне не бывает: воздуха нет, держать
       частицу в воздухе нечем, и поднятое падает по баллистике сразу.
       Две с лишним тысячи точек над грунтом читались снегопадом, то
       есть ровно земной погодой, которой здесь быть не может. Оставлена
       редкая искра поднятого реголита у самой поверхности. */
    var число = W.ступень === 0 ? 0 : (W.ступень === 1 ? 200 : 420);
    if (число) {
      var гео = new T.BufferGeometry();
      var поз = new Float32Array(число * 3), семя = new Float32Array(число);
      var Rп = 18;
      for (var p = 0; p < число; p++) {
        поз[p * 3] = (Math.random() * 2 - 1) * Rп;
        поз[p * 3 + 1] = Math.random() * Math.random() * 0.9;
        поз[p * 3 + 2] = (Math.random() * 2 - 1) * Rп;
        семя[p] = Math.random();
      }
      гео.setAttribute("position", new T.BufferAttribute(поз, 3));
      гео.setAttribute("aSeed", new T.BufferAttribute(семя, 1));
      М.пыльМат = new T.ShaderMaterial({
        uniforms: { uTumanK: ГРАД.uTumanK, uColor1: ГРАД.uColor1, uColor2: ГРАД.uColor2, uResolution: ГРАД.uResolution,
                    uTime: ГРАД.uTime, uDen: ГРАД.uDen, uH: { value: 800 },
                    uPoryv: { value: 0 }, uRadius: { value: Rп },
                    uCol: { value: цвет(ПАЛ.пыль) }, uSila: { value: ПАЛ.пыльСила } },
        vertexShader: В_ПЫЛЬ, fragmentShader: Ф_ПЫЛЬ,
        transparent: true, depthWrite: false, blending: T.AdditiveBlending, fog: false
      });
      М.пыль = new T.Points(гео, М.пыльМат);
      М.пыль.frustumCulled = false;
      М.пыль.renderOrder = 7;
      М.корень.add(М.пыль);
    }

    М.порыв = { сила: 0, до: 6 + Math.random() * 3, длит: 0 };
    М.разм = new T.Vector2();
    g.addEventListener("rv-тема", function (е) { тема(е && е.detail); });
    тема(d.documentElement.getAttribute("data-тема") === "светлая" ? "светлая" : "тёмная");
    return М.корень;
  }

  function тема(имя) {
    ПАЛ = имя === "светлая" ? ДЕНЬ : НОЧЬ;
    if (!собрано) return;
    ГРАД.uColor1.value.setHex(ПАЛ.зенит);
    ГРАД.uColor2.value.setHex(ПАЛ.горизонт);
    ГРАД.uDen.value = имя === "светлая" ? 1 : 0;
    var у = М.грунтМат.uniforms;
    у.uGrunt.value.setHex(ПАЛ.грунт); у.uSklon.value.setHex(ПАЛ.склон);
    у.uVerh.value.setHex(ПАЛ.верх); у.uNiz.value.setHex(ПАЛ.низ);
    у.uDymka.value.setHex(ПАЛ.дымка);
    М.дымкаМат.uniforms.uCol.value.setHex(ПАЛ.дымка);
    М.дымкаМат.uniforms.uSila.value = ПАЛ.дымкаСила;
    for (var i = 0; i < М.лучи.length; i++) М.лучи[i].material.uniforms.uCol.value.setHex(ПАЛ.дымка);
    if (М.пыльМат) {
      М.пыльМат.uniforms.uCol.value.setHex(ПАЛ.пыль);
      М.пыльМат.uniforms.uSila.value = ПАЛ.пыльСила;
      /* Днём пыль чернилами, ночью светом: тот же переключатель, что у
         роя, иначе светлая пыль на светлом небе исчезает. */
      М.пыльМат.blending = имя === "светлая" ? T.NormalBlending : T.AdditiveBlending;
      М.пыльМат.needsUpdate = true;
    }
  }

  /* Контактная тень под предметом. До четырёх пятен: больше на одной
     площадке не бывает, а цикл в шейдере на четыре шага дешевле
     текстуры теней в сотни раз. */
  function тень(x, z, радиус) {
    if (!собрано) return null;
    var сп = М.грунтМат.uniforms.uShadows.value;
    for (var i = 0; i < сп.length; i++) {
      if (сп[i].z <= 0) { сп[i].set(x, z, радиус); return сп[i]; }
    }
    сп[0].set(x, z, радиус);
    /* Отдаём сам вектор: предмет, который переставили (купол уходит
       вправо на широком кадре), двигает тень за собой без новой записи. */
    return сп[0];
  }

  function кадр(доля, dt, часы, камера) {
    if (!собрано) return;
    ГРАД.uTime.value = часы;
    if (W.r && W.r.getDrawingBufferSize) {
      W.r.getDrawingBufferSize(М.разм);
      ГРАД.uResolution.value.copy(М.разм);
      if (М.пыльМат) М.пыльМат.uniforms.uH.value = М.разм.y || 800;
    }
    /* Порыв: раз в 6-9 секунд ветер вдвое сильнее на полторы секунды. */
    var п = М.порыв;
    п.до -= dt;
    if (п.до <= 0) { п.длит = 1.5; п.до = 6 + Math.random() * 3; }
    var цель = п.длит > 0 ? 1 : 0;
    if (п.длит > 0) п.длит -= dt;
    п.сила += (цель - п.сила) * (1 - Math.exp(-dt / 0.35));
    М.дымкаМат.uniforms.uPoryv.value = п.сила;
    if (М.пыльМат) М.пыльМат.uniforms.uPoryv.value = п.сила;
    /* Небо станции гаснет к концу акта: дальше идёт небо мира, и без
       спада на границе актов был бы щелчок цвета. */
    М.небоМат.uniforms.uAlpha.value = 1 - зажать((доля - 0.7) / 0.3, 0, 1);
  }

  /* ── КОМУ НУЖНА ЛУНА ──────────────────────────────────────────
     Луна это МИР, а не реквизит одного акта. Владелец сказал прямо:
     «на том же фоне поверхности Луны, как и в начале, камера никуда не
     уезжает и фон не меняется между первой и второй сценой».

     До этой правки грунт, небо, Земля и дымка лежали внутри корня акта
     станции и гасли вместе с ним. На периметре стена оказывалась в
     пустоте: фон менялся ровно там, где меняться было нельзя, и кладка
     висела в воздухе вместо того, чтобы стоять на грунте.

     Теперь спрашивают все, кому она нужна: станция, периметр, прокол.
     Гаснет Луна только когда её отпустили ВСЕ - иначе акт, уходящий из
     кадра, утаскивал бы мир из-под соседа, который ещё виден. */
  var просят = {};
  var подЗемлёй = false;
  function пересчитать() {
    if (!М.корень) return;
    if (подЗемлёй) { М.корень.visible = false; return; }
    var хоть = false;
    for (var к in просят) if (просят[к]) { хоть = true; break; }
    М.корень.visible = хоть;
  }
  function нужна(кто, да) { просят[кто] = !!да; пересчитать(); }

  /* ── ЛУНА ВСТАЁТ ПОД ТОТ АКТ, КОТОРОМУ ОНА НУЖНА ─────────────
     И это главная правка всей сцены. Луна собиралась в НАЧАЛЕ КООРДИНАТ
     мира, а станция стоит в (0, 14.2, 80). Замер это показал числом:
     диск грунта занимает z от -45 до +45 при y около нуля, дом - z 80
     при y 14.2. То есть дом висел в пустоте в тридцати пяти единицах ЗА
     краем грунта и в четырнадцати над ним, а то, что читалось на
     снимках поверхностью под ним, было далёким куском диска у нижней
     кромки кадра.

     Отсюда разом всё, на что жаловался владелец: «Луны и космоса почти
     нету», «земля на фоне не видна», «появились какие-то тёмные тени»,
     «стена как будто наклеена поверх фона, а не воткнута в поверхность».
     Ничего из этого не чинится ни светом, ни туманом: дом надо было
     поставить НА грунт.

     Ставит тот акт, который знает своё место в мире. */
  /* Плотность тумана ставит тот, кто знает рабочую дистанцию сцены. */
  function туман(k) {
    if (!ГРАД || !ГРАД.uTumanK || !(k > 0)) return false;
    ГРАД.uTumanK.value = k;
    return true;
  }

  function поставить(x, y, z) {
    if (!М.корень) return false;
    М.корень.position.set(x || 0, y || 0, z || 0);
    М.корень.updateMatrixWorld();
    return true;
  }

  /* ── ПОД ЗЕМЛЁЙ ЛУНЫ НЕТ ───────────────────────────────────────
     Спрашивают Луну сразу три акта, и пока хоть один в кадре, она
     горит. На падении это давало вот что: камера уже в двадцати
     единицах под грунтом, вокруг шахта, а сквозь неё светят звёзды и
     Земля - соседний акт всё ещё числился видимым и держал небо.

     Просьба и запрет это разные вещи. Кто под землёй, тот не просит
     Луну погасить, он СООБЩАЕТ, что её здесь быть не может, и это
     сильнее любой просьбы. */
  function подземка(да) { подЗемлёй = !!да; пересчитать(); }

  g.RV_ЛУНА = {
    "собрать": собрать,
    "нужна": нужна,
    "поставить": поставить,
    "туман": туман,
    "подземка": подземка,
    "кадр": кадр,
    "тень": тень,
    "тема": тема,
    "градиент": ГРАД,
    /* Шумовая текстура наружу: купол строит свой луч из арки той же
       текстурой, а вторая такая же в видеопамяти была бы лишней. */
    "шум": function () { return М.шум || null; },
    /* ЗАГЛУШИТЬ ПРИЗЕМНЫЕ ДОБАВКИ. Дымка это горизонтальная плоскость в
       пятнадцать единиц, лежащая на грунте и рисуемая сложением. Пока
       камера стоит низко и смотрит на купол, она честно даёт свечение
       вокруг станции. Когда камера поднимается и перед ней встаёт стена,
       та же плоскость проходит СКВОЗЬ стену: половина её остаётся позади
       и закрывается, а половина торчит перед стеной и с малого угла
       читается светлой полосой поперёк всего кадра.

       Глушится долей: ноль это как было, единица это без дымки. Пыль
       глушится вместе с ней, она живёт на той же высоте. */
    "приземное": function (гашение) {
      var к = 1 - Math.max(0, Math.min(1, гашение || 0));
      if (М.дымкаМат) М.дымкаМат.uniforms.uSila.value = ПАЛ.дымкаСила * к;
      if (М.пыльМат) М.пыльМат.uniforms.uSila.value = ПАЛ.пыльСила * к;
    }
  };
})(window, document);
