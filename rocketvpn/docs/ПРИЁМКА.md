# Rocket VPN: приёмка проекта

Документ для сдачи. Отвечает на семь вопросов сразу: что сделано, из
чего это собрано, что человек может нажать, совпадает ли фильм со
сценарием владельца, как оно ведёт себя на телефоне и на мониторе, что
слышно, и насколько близко к igloo.inc.

Всё, что здесь написано числами, ЗАМЕРЕНО инструментами из `tools/`, а
не оценено на глаз. У каждой таблицы стоит имя инструмента, которым её
можно пересчитать.

Ветка разработки: `claude/rocket-cdn-website-admin-x5482k`.
Боевой адрес: `rocketvpn.top`. Стенд: `php -S 127.0.0.1:8170 tools/стенд.php`.

## Второй заход по списку владельца (сентябрь 2026)

После первой приёмки владелец прошёл сайт с телефона и прислал тридцать с
лишним замечаний с тридцатью двумя снимками. Набор из шестнадцати проверок к
тому моменту докладывал «чисто». Значит проверки мерили не то, что видит
человек, и первое, что здесь надо записать - ЧЕМ они мерили не то.

**Все шестнадцать ставили страницу на долю через `RV_MOTION.кПунктy` и ждали,
пока всё уляжется.** Человек так не делает: он крутит колесо, и беды живут
ровно в переходах. Отсюда `tools/лента-глазами.mjs` - листает колесом шаг за
шагом и снимает каждый шаг, - и `tools/что-мигает.mjs`, который при стоящей
камере считает разницу соседних кадров по точкам и называет предмет,
закрывающий самую мерцающую точку.

Что эти два инструмента и точечные замеры нашли числом:

| беда, как её видит человек | что было на самом деле |
|---|---|
| «стены нету, только глыбы прилетают» | признак «стену держит дом» ставится в кадре акта станции и ЗАСТЫВАЕТ при уходе акта из кадра. На всём периметре `кирпичная стена` visible=false и `снаряды` visible=false |
| «обломки вместо звёзд, глянцевые угли» | первый экран звал `RV_КОРИДОР.подземка(false)`, а этот признак включает 822 гранёных камня и 120 синих колец-маяков |
| «синий круг моргает на всём сайте» | те же маяки коридора плюс железный обод входа в тоннель |
| «стена перекрывает весь экран, слишком близко» | лунный кадр делил дальность на «зум», которого мир камере не даёт: объектив стоял в 9.2 единицы от стены вместо 12.3 |
| «кнопки уезжают наверх поверх сцены» | видимость слоя брала МАКСИМУМ из двух причин гашения, и признак по доле держал единицу всю дорогу наверх. Замер: верх кнопок 636, 522, 443, 182, 77, -322, -721 при непрозрачности ровно 1 |
| «тоннель появляется снизу, с наклоном» | высота оси тоннеля прибита числом 16.7, а глаз стоит на высоте лунного кадра, и она зависит от формы экрана |
| «камера ушла куда-то влево» | шаг вбок в обходе имел обратный знак: дуга шла от сорок восьмого градуса ко второму, а камера выходит из тоннеля на нулевом |
| «лёд слишком прозрачный» | числа льда подбирались при кладке на 77 из 255; кладка теперь стоит на 132-140, лёд упёрся в свой потолок на тех же 175 |
| «текст перебирается символами» | приём igloo с подменой глифа: на слабом телефоне потолок кадра растягивает 0.75 секунды на восемь и больше |
| «снизу появляется чёрная полоса» | липкий кадр мерился в `100svh` - высоте окна с ПОКАЗАННОЙ адресной строкой |
| «резкий свайп проглатывает анимацию» | `scroll-snap-stop: always` на акте высотой три-семь экранов обязывает браузер довести прокрутку до следующей сцены |
| «частые вопросы обрезаны» | у экранов пульта `overflow: hidden` при выравнивании по середине: срезало сразу с двух сторон |

Вывод тот же, что и в первую приёмку, только жёстче: **замер, который меряет
не то, что нарисовано, хуже отсутствия замера.** Все двенадцать бед выше нашли
не проверки, а два новых инструмента и точечные замеры по координате.

И самое неприятное нашлось в самих проверках. У проверки СВЕТА в списке актов
стояли «оболочка», «выход» и «стыковка», у проверки КОНТРАСТА ещё и «видно».
Таких актов в разметке нет ни одного уже давно: лента идёт станция, периметр,
прокол, рубка, пуск, финал. Ядро на незнакомое имя не падает, поэтому обе
честно печатали проценты - и ни разу не смотрели на первый экран, на стену и
на финал. То есть ровно на те три акта, про которые владелец написал больше
всего. Имена сверены с `frame.html`; держать их надо оттуда, а не из памяти.

Заодно телефон в проверке контраста переведён с плотности 2 на 3. У телефона
владельца она около трёх, и мыло, на которое он жалуется, на плотности 2
физически не воспроизводится.

Третья того же рода: проверка ЗВУКА включала его вызовом из `page.evaluate`,
то есть МИМО жеста. Звуковой контекст от такого поднимается, а элемент `audio`
- не всегда, у него своя политика автоигры. Поэтому она докладывала «музыка не
играет» и на здоровом сайте, и на больном одинаково. Отдельный инструмент
`tools/музыка-по-жесту.mjs` снимает поблажку Playwright, делает настоящий жест
и ждёт, пока тронется головка дорожки:

| жест | состояние | шина | музыка |
|---|---|---|---|
| клик | running | 0.876 | играет, 0.4 из 304.4 с |
| колесо | running | 0.881 | играет, 0.1 |
| касание | running | 0.895 | играет, 0.5 |

## Итог приёмки

`bash tools/проверить-всё.sh` - **чистых 16, грязных 0**:

    витрина · глсл · движение · звук · контраст · переносимость
    полёт · посадка · приёмка · речь · свет · связка
    скорость · створ · тишина · ход-камеры

Числа, за которыми стоит следить между заходами:

| что | ПК | телефон | порог |
|---|---|---|---|
| вес страницы | 5.75 МБ | - | 6 МБ |
| ход камеры за фильм | 165 ед. | 178 ед. | не мертва при 60 |
| худший кусок хода (шов) | 46 ед. | 39 ед. | 120 |
| 95я доля кадра | 1017 мс | 967 мс | 1474 / 1518 (по эталону машины) |
| эталон растра | 203 мс | 209 мс | слепок 179 |
| зазор обратимости хода | 0.33 ед. | - | 1 |

**Про чистоту этого списка честно.** Настоящих бед в сайте за последний
заход нашлось пять, и все пять нашли снимки владельца и проверка
движения. Дефектов В САМИХ ЗАМЕРАХ нашлось восемь, и каждый краснел на
здоровом сайте. Разбор всех восьми - в разделе 7, «Урок первый». Список
из шестнадцати ЧИСТО стоит читать вместе с ним.

---

## 1. Сценарий: что просил владелец и что стоит в фильме

Владелец описал сайт пятью экранами. Ниже дословный разбор: строка
сценария, акт разметки, где это лежит в коде и что показал замер.

| экран владельца | акт | высота | модули |
|---|---|---|---|
| 1. Дом на Луне, кирпичи улетают в стену | `станция` | 320svh | `rv-act-станция.js`, `rv-луна.js`, `rv-кирпичи.js` |
| 2. Стена, глыбы льда с иконками, трещины | `периметр` | 420svh | `rv-глыба.js`, `rv-снаряды.js`, `rv-сетка.js` |
| 3. Тоннель один в один с igloo | `прокол` | 300svh | `rv-act-fold.js`, `rv-труба.js` |
| 4. Подземелье, три кольца, ракета | `прокол` (хвост) | 430svh | `rv-комната.js`, `rv-ракета-кирпичи.js` |
| 5. Три раздела, вход в ракету, пульт | `финал` + подвал | 800svh | `rv-act-финал.js`, `rv-финал.js`, `rv-игра.js`, `rv-меню.js` |

### Актов стало четыре: «рубка» и «пуск» сняты с ленты (11.09.2026)

Владелец: «раздел с ракетой из частиц вообще можно удалить и собирать
заново, т.к. там каша», «частицы может вообще не нужны, смотри сама».

Смотрю по снимкам и по пробам.

`пуск` занимал 330svh, то есть три с лишним экрана прокрутки, и не
говорил ни слова: в разметке у него пустой кадр, в сцене всё тот же рой,
что и в рубке до него. Подход к кораблю целиком делает финал, и делает
его с бортом, люком и створками. Второй подход, немой и без корабля, не
был нужен никому.

`рубка` показывала четыре доли подряд одно облако точек, потому что её
предмет НЕ РИСОВАЛСЯ ВОВСЕ. Проверено прямой пробой: красный куб шесть
на шесть, поставленный на место корабля с выключенным отсечением и
наибольшим порядком отрисовки, выходил в кадре ЧЁРНОЙ ДЫРОЙ в рое, а
луч из камеры не находил ни одного треугольника корпуса.

Три карточки причала переехали в финал и читаются на подходе к тому
кораблю, в который человек заходит. Одно непрерывное движение вместо
двух разделов с пересадкой.

### Правило, которое стоило этого раздела: прозрачное не пишет глубину

Складывающаяся на кадр поверхность (`transparent: true`) у движка
ПО-ПРЕЖНЕМУ пишет буфер глубины, если не сказать обратного. Невидимая
при альфе ноль, она честно кладёт туда свой силуэт, и всё, что стоит за
ней, вычищается из кадра.

Поймано трижды на одном сайте:

| где | что съедало |
|---|---|
| плоскости шахты (`rv-act-fold.js`) | всё, что позади, на входе в тоннель |
| рубашка фигуры (`rv-комната.js`) | корабль в зале |
| сам рой (`rv-частицы.js`, снят) | корабль в зале, второй слой поверх первого |

Правило: складывающаяся на кадр плоскость глубину не пишет. Каменной
кладке ракеты она нужна, пока та в кадре ОДНА, поэтому зал переключает
её по ходу превращения.

### Ракета складывается из кирпичей тоннеля, роя частиц больше нет (11.09.2026)

Владелец: «ракета из частиц - ты же говорила, уберёшь её вообще, удалишь
и сделаешь, что как будто она из туннелей, из того же материала, из тех
же кирпичей собирается», и про весь кусок до финала - «полная каша».

`rv-частицы.js` удалён из репозитория. На его месте
`rv-ракета-кирпичи.js` (`RV_СБОРКА`): сто с лишним камней слетаются из
темноты вокруг и встают в корпус рядами снизу вверх - юбка сопла, тело,
носовой конус, четыре киля. Ход ведёт ПРОКРУТКА (доля прокола
0.78..0.99), а не таймер: человек выходит из тоннеля, и кладка растёт у
него на глазах. Окно выбрано по снимкам - до 0.8 камера ещё внутри
трубы и дна не видит.

Камень берётся у `RV_КИРПИЧИ.камень()` и брусок у `RV_КИРПИЧИ.брусок()`.
Не копия шейдера и не «те же числа»: тот же материал из той же фабрики,
те же три карты фотоскана, та же смена темы. Копию тут писали трижды, и
трижды она отставала.

Передача металлу идёт ОДНИМ числом (`превращение`), а не двумя: камень
целый до 0.10, тает к 0.55, обшивка трогается с 0.40. Раньше камень гас
по доле прокрутки, а металл рос по превращению, и при быстрой прокрутке
в кадре на миг не оставалось корабля вовсе.

Каменная оболочка ШИРЕ металлического корпуса на восемнадцать процентов:
при равных радиусах во время передачи сквозь кладку торчали нос, кили и
раструб, то есть два корпуса разом.

### Подножие вместо сдвига: ракета стоит, а числа кадра не тронуты

Замер по самим экземплярам кладки (у `InstancedMesh` `Box3.setFromObject`
отдаёт габарит ОДНОГО бруска, и по нему выходило, что ракета стоит на
месте при любой доле сборки): низ кладки 18.05 мира, верх площадки
16.30, между ними 1.75 пустоты. Пока рядом стоял металлический корабль,
зазор закрывали его опоры и раструб; корабль ушёл - ракета повисла.

Первый заход опускал саму фигуру. Она села, но за ней потянулись камера
обхода и высота подъёма тоннеля, и на проколе 0.99 ракета ушла под
нижнюю кромку кадра. Место фигуры остаётся тем, под которое настроен
кадр; до площадки её доводит ПОДНОЖИЕ - три ряда камня под соплом,
нижний шире верхнего. В габарит ракеты оно не входит: по габариту
встаёт объёмный корабль.

Высоту фигуры прокол теперь спрашивает у зала, а не помнит числом.

### Залом распоряжается тот акт, чей кадр идёт

Секции ленты высокие и липкие, поэтому кадры финала приходят ещё на
хвосте прокола - с долей ровно ноль, до своего начала. Зал он звал
безусловно и долей единица, и числа показали цену: на проколе 0.90
сборка стояла на единице вместо 0.57, превращение ушло на 0.087, и на
дне шахты стоял готовый металлический корабль, пока человек ещё летел по
тоннелю. Пока у финала нет своей доли, зал остаётся за проколом.

### Подмена стены: зерно камня считается от его размера в мире (11.09.2026)

Владелец в третий раз: «подмена стены осталась». Прежняя проверка
(`tools/checks/кирпич.mjs`) печатала «чисто», и печатала честно - она
мерила не то место. Она сверяла СТЫК актов, станцию на 0.92 против
периметра на 0.08, а ровно там обе кладки уже сведены к одной сетке,
камень в камень. Замер живой сцены показал, где расхождение есть:

| точка ленты | блок дома | кирпич стены |
|---|---|---|
| станция 0.10 | 2.075 x 1.503 | 1.194 x 0.673 |
| станция 0.50 | 1.877 x 1.473 | 1.194 x 0.673 |
| станция 0.92 | 1.194 x 0.673 | 1.194 x 0.673 |

Дом сложен крупным камнем нарочно - так у igloo, и владелец сам просил
крупный блок. Беда была в фотоскане: он ложился ФИКСИРОВАННЫМ числом
плиток на грань любого размера (1.35 у дома, 1.2 у стены). На блоке
вдвое крупнее порода выходила вдвое крупнее, и менялась прямо в полёте,
пока блок ужимался до кирпича. Одна порода не может иметь два зерна;
глаз читает это как два материала.

Теперь `uvT = vUv * vRazmer`, где `vRazmer` это настоящий размер грани в
мире, снятый с `modelMatrix * instanceMatrix`. Одна плитка скана на
единицу мира - у любой кладки, любого размера камня.

Второе: обе кладки стояли в кадре РАЗОМ. Правило «в кадре ровно одна
кладка» держали периметр и прокол, каждый прятал свои кирпичи. Но кадр
идёт только у того акта, что на экране, и пока человек в станции,
прятать кирпичи периметра было некому. Теперь станция гасит их сама.

Проверка переписана под это: мерит иглу против стены, а не стык, и
читает в самом шейдере, привязано ли зерно к размеру камня.

### Экран 1. Луна и дом

- Дом стоит справа на мониторе и по центру на телефоне, заголовок в
  верхней трети. Отход дома на мониторе поправлен: рамка занимает 55
  процентов высоты кадра, как у igloo, а не 70.
- Кирпичи улетают верхним рядом в нижний и складываются в плоскую стену,
  **фон, камера и ракурс не двигаются**. Это условие владельца, и оно
  держится позой акта: узел подхода и узел станции стоят в одной точке.

### Экран 2. Стена и глыбы

- Три глыбы с формами igloo влетают в стену со звуком, втыкаются, от
  каждого удара по стене расходятся трещины со свечением.
- **Обвала кирпичей нет.** Владелец: «обвал не очень смотрится... как
  будто они в воздухе». Выброс убран целиком, вместо него удлинён радиус
  трещины (`predel = 6.6 * sila + 1.4`).
- Внутри каждой глыбы объёмный знак. Цвет знака светлый (`0x8FA6C4`,
  кромка `0xE6EEFB`): их собственный тёмный `0x1A2130` верен для светлого
  мира igloo и не виден на нашей лунной ночи.
- Три глыбы это три функции VPN: шифрование трафика, подмена IP-адреса,
  создание защищённого туннеля. Четвёртая, без надписи, ломает стену.
- Кнопка канала `https://t.me/rocketvpnnews` стоит в акте.

### Экран 3. Тоннель

Разбор и все числа - в `docs/ЦВЕТ-ТОННЕЛЯ-IGLOO.md`. Сводка сверки ниже,
в разделе 7.

ТОННЕЛЬ НЕ КАМЕННЫЙ, И ЭТО ПО СЦЕНАРИЮ. Владелец просил третий экран
«один в один с igloo», а у них труба это ДЫМ: открытый цилиндр,
вывернутый внутрь, цвет vec3(0.85, 0.9, 1.0), альфа по трём выборкам
карты ветра с потолком 0.068. Светлое поле кадра рисует не она, а
огромная светящаяся сфера. Кладка кончается там, где начинается
тоннель: сквозь неё камера проходит пробоем, дальше стен из камня нет.

ЗАТО КИРПИЧ ОДИН НА ВЕСЬ ФИЛЬМ. Летящий кирпич и кладка дома держат
одни четыре краски и переключаются одним событием темы. Раньше кладка
за темой ходила, а кирпич стоял на месте: в тёмной теме камень стены
0xCBD2DE против кирпичного 0x9DA3B0, и стена выходила заметно светлее
того, из чего собралась.

Камера идёт ТОЛЬКО ВПЕРЁД: ни падения, ни поворота, ни крена. Держится
позой (`крен` нулевой во всех трёх узлах) и прицелом вдоль оси трубы:
`уклонОси = ПОДЪЁМ_К_РОЮ / (УСТЬЕ_Z - КОНЕЦ_Z)`.

### Экран 4. Подземелье

- Три кольца собираются, ракета из частиц видна сверху, набита частицами
  плотно, стоит по центру, заголовок в верхней четверти.
- Камера обходит ровно по окружности радиусом двадцать шесть.

### Экран 5. Финал

Владелец прислал восемь снимков финала Rocket CDN со словами «вот как на
сайте rocketcdn.ru 1:1 должен быть финал, кроме разворота на 360».
Разбор по кадрам и что стоит у нас:

| их кадр | что на нём | у нас |
|---|---|---|
| 1 | ракета целиком на площадке, звёзды | их модель, их площадка; свет уходит ещё в хвосте пуска, на доле 0.00 финала оболочка уже 0.12 |
| 2 | подход, корпус во весь кадр | тот же ход, темп акта [0.30, 0.18] |
| 3-4 | открытый люк, внутри карточки текста | рамка «люк», два блока разметки в проёме; глаз проходит проём на доле 0.59 |
| 5-6 | рубка, в окне Земля | даль с планетой по их `planetTex` |
| 7-8 | панель с вопросами и кнопками, ряд клавиш | снимок рамы, дека с клавишами, карточки со скобками; рама с 0.64, текст с 0.68, держатся до последнего кадра |

Дорога акта разложена по трём узлам: подход снаружи, ПРОЁМ ЛЮКА,
пульт. Половина доли на каждое плечо, и оба плеча несут настоящий ход -
подлёт к ракете и вход внутрь. Пока средним узлом стоял сам пульт, а
уход был тринадцать сотых мировой единицы, вторая половина акта не
двигалась вовсе: проверка движения показывала 1.5 при пороге 2.

    доля   0.00   0.30   0.45   0.59   0.75   0.90   1.00
    где    старт  ползёт подлёт ПРОЁМ  зал    пульт  пульт

- Корабль СНАРУЖИ перенесён из `rc-rocket.js` целиком: обшивка с
  заклёпками и швами, иллюминатор, стабилизаторы, три опоры с рабочей
  гидравликой, люк с двумя створками и салоном за ними, площадка с
  разметкой, бортик, огни по ободу. Файл `rv-корабль.js`.
- Панель перенесена снимком рубки с прозрачной дырой на месте окна
  (`rv-кабина.js` плюс три кадра в `assets/gen/каб`). Ниши в снимке
  пустые, клавиши рисует дека (`rv-настил.js`, подписи `rv-клавиши.js`).
- Оборота на 360 нет нигде: вся дорога это одна прямая внутрь.
- Победа в мини-игре превращает рой в цельную ракету (`rv-комната.js`,
  `превращение`, полтора цикла на переход).

---

## 2. Техника

| мерка | значение |
|---|---|
| движок | three.js r152, без сборщика, обычные `<script defer>` |
| файлов слоя | 42 в `assets/*.js` |
| строк кода | 30 690 |
| вес папки `assets` | 37 МБ (из них звук и шрифты) |
| холст | один на весь сайт, акты меняют содержимое, а не полотно |
| живых проверок | 18 в `tools/checks/`, запуск `bash tools/проверить-всё.sh` |
| инструментов замера | 22 в `tools/` |

### Ступени железа

`RV_WORLD.ступень` считается по памяти и числу ядер, **не по ширине
экрана**: телефон с восемью ядрами получает ту же картинку, что ноутбук.

| ступень | условие | что теряет |
|---|---|---|
| 0 | память ≤2 ГБ или ядер ≤2 | точки вместо стриков, без сглаживания, 260 частиц |
| 1 | память ≤4 ГБ или ядер ≤4 | реже сетки, меньше карта неба, 620 частиц |
| 2 | остальные | всё, 1100 частиц, сглаживание |

### Правила, которые держат код

- **Идентификаторы в шейдерах только латиницей.** Кириллица ломает
  сборку программы молча, без единой строки в консоли. Стережёт
  `tools/checks/глсл.mjs`.
- **Постобработка живёт в `rv-real.js` под именем «плёнка»**, а свечение
  с нулевым порогом по числам igloo - в `rv-ореол.js`. В `rv-world.js`
  их нет, и искать их там бесполезно.
- **Вступление считает своё время кадрами с потолком**
  (`вступТ += Math.min(0.1, dt)`). На песочнице без видеокарты пять с
  половиной секунд растягиваются в десять минут, поэтому каждый замер
  обязан ждать `RV_WORLD.вступлениеИдёт()`, а не секундомер.

---

## 3. Что человек может нажать

| место | действие | куда ведёт |
|---|---|---|
| шапка, «Войти» | ссылка | `rocketweb.top` |
| шапка, почта | окно связи | форма, уходит на `rocketcdn.ru/api.php` |
| шапка, тема | светлая и тёмная | переключение мгновенное, память в браузере |
| шапка, звук | включить и выключить | память в браузере |
| экран 2 | кнопка канала | `t.me/rocketvpnnews` |
| экраны 2-5 | тарифы и кнопки | `t.me/RocketCompanyVPN_bot` |
| подвал | форма связи | те же поля, что в окне шапки, один источник |
| финал | «Запустить VPN» | бот |
| финал | «Запустить игру» | возврат к воротам в ангаре |
| ангар | мини-игра | победа даёт три дня и превращает рой в корабль |

Поля формы собирает `rv-меню.js` одной функцией `формаВГнездо` в три
гнезда: окно шапки, лента подвала, пульт финала. Три копии полей
разошлись бы на первой правке.

---

## 4. Адаптация

- Раскладка выбирается **по отношению сторон**, а не по ширине: на узком
  кадре слова уходят наверх, на широком встают сбоку от предмета. Порог
  1.05.
- Перенос строк считается от ширины колонки в кеглях, поэтому от высоты
  кадра не зависит вовсе. Пересборка идёт только при смене отношения
  сторон больше чем на три сотых - выезд адресной строки телефона её не
  вызывает.
- Поворот телефона слушается отдельным `orientationchange` там, где от
  отношения сторон зависит место камеры (финал): на части телефонов он
  приходит раньше `resize`, и один кадр панель успевала встать по старым
  числам.
- Плотность пикселей до 2.25 (было 1.35 и 1.8): на телефоне с
  `devicePixelRatio` 2.6-3.0 сцена рисовалась вдвое мельче экрана и
  растягивалась. Это и было «на телефоне мыло».

---

## 5. Звук

Синтеза на лету нет: девять подложек и тринадцать разовых лежат
готовыми файлами в `assets/snd`. Спецификация синтеза, по которой они
сняты, - `docs/ЗВУК-СПЕК.md`.

| акт | подложка | разовые |
|---|---|---|
| станция | `gul` растёт по акту | нет намеренно: тишина перед первым ударом |
| периметр | `bed-net` уходит, `bed-wall` приходит | `stena` один раз, `sweep` четыре прохода |
| прокол | `bed-shell` → **тишина 0.33-0.44** → `tonnel` | `pull`, потом `prokol` + `impact` |
| рубка | `bed-open` → `bed-bridge` | `dock` три причала, громкость растёт |
| пуск | `bed-shell`, `bed-open` на выходе | `dock` и `pick` по трём окнам, `pusk`, `shell-seal` |
| финал | `bed-open` → `bed-shell` → `bed-bridge` | `shell-seal` на притворе; люк и пульт озвучивает сама сцена |

Тишина перед ударом прокола закрыта для всех: в окне 0.33-0.44 не
проходят и разовые со стороны (отклик роя на прокрутку, смена текста).
Без этого замок ловил «перед броском нет тишины, разовых 1».

---

## 6. Что было найдено и починено на этой приёмке

| беда | как поймана | что сделано |
|---|---|---|
| текст пульта стоял ДВАЖДЫ: холст экрана и разметка поверх него | снимок ПК 1440x900 | слова только в разметке, холст держит прибор |
| у финала не было звука вовсе | чтение партитуры | своя сцена на три окна |
| камера входила СКВОЗЬ ЗАКРЫТЫЕ створки | `tools/дорога-финала.mjs` | темп акта плюс пересчёт порогов люка |
| финал не заявлял вокруг себя ангар | замер яркости по актам | акт спрашивает фон, подземку и порог ореола |
| `tools/проверить-всё.sh` не запускал НИ ОДНОЙ проверки | запуск | имена переменных в оболочке латиницей |
| проверки красили ГРЯЗНО из-за чужого сертификата | `requestfailed` в песочнице | отказ по сертификату не считается бедой сайта |
| кладок в мире было ДВЕ, разного цвета | чтение шейдеров плюс снимок телефона | цвет камня один, в кадре всегда одна кладка |
| камера всегда сносило вбок | чтение ядра камеры | постоянный снос по X убран, дыхание только вверх |
| на обходе взгляд уходил к касательной | то же | упреждение и крен на обходе выключены |
| камере не доставалось сглаживание ленты | сверка с `rv-motion.js` | те же две подтяжки igloo 0.075 и 0.15 |
| ракет в мире было ДВЕ, в разных местах | `tools/две-ракеты.mjs` | корабль финала на месте роя, рост сходится до сотых |
| люк смотрел не туда, где кончается облёт | геометрия поз | корабль повёрнут на сто двадцать градусов |
| отход камеры считался в местных единицах | разбор после ужатия узла | умножается на настоящий масштаб узла |
| панель финала была набрана плитами | слово владельца | снимок рубки соседнего сайта, окно прозрачно |
| страница весила 7.03 МБ при пределе 6 | `tools/checks/приёмка.mjs` | перепаковка фотоскановых карт по замеру угла нормали, 5.75 МБ |
| финал: между долями 0.57 и 0.71 кадр стоял | `tools/checks/движение.mjs` | средний узел акта переехал в проём люка, оба плеча несут дорогу |
| первый кадр финала: площадка с ногами, корпуса нет | снимок доли 0.00 | спад светлой оболочки перенесён в хвост пуска, на стыке оба акта просят 0.12 |
| периметр: последняя четверть акта не менялась | `tools/checks/движение.mjs` | окна четырёх карточек считаются из конца и длины, последняя втыкается на 0.86 |
| «круглый купол вокруг ракеты» | снимок владельца + `tools/что-в-кадре.mjs` | это «силовое поле фигуры» 265x546 точек, снято |
| заголовок стоял как «Когда ложилкъя о 5Ф5?`т» | снимок владельца | волна от прокрутки гоняла перебор глифов; теперь она светит, перебор один раз при появлении |
| у утолщения буквы был перевёрнут знак: телефон получал самый тонкий набор | чтение шейдера | знак исправлен (см. ниже про ложный повод) |
| проверка контраста краснела полусотней промахов по невидимому тексту | чтение проверки | дети обрезанной коробки докладывали свой размер; теперь предки проверяются на обрезку |
| `backdrop-filter` без `-webkit-` в четырёх местах | `tools/checks/переносимость.mjs` | префиксы проставлены |
| проверка переносимости краснела на рабочем коде | чтение | она не знала страховки вида `if (g.rIC) ... else ...` |

---

## 6а. Бесшовность: где стыки и чем они держатся

Владелец: «швов очень много». Шов это любое место, где кадр меняется
рывком, а не ходом. Их четыре рода, и держатся они по-разному.

### Стыки актов

Секции ленты перекрываются: пока один акт уходит, другой уже в кадре, и
оба считают свой кадр. Значит на перекрытии они обязаны просить у мира
ОДНО И ТО ЖЕ, иначе кадр мигает через кадр.

| стык | что могло разойтись | чем держится |
|---|---|---|
| станция → периметр | две кладки в одной плоскости | цвет камня один, признак `RV_СТЕНА_У_ДОМА` отдаёт стену на доле 0.94 |
| периметр → прокол | кладка и труба | один модуль `rv-act-fold.js`, кирпичи те же |
| прокол → рубка | Луна, подземка, оболочка | прокол переключает всё на доле 0.22, рубка застаёт готовое |
| рубка → пуск | одна окружность обхода на два акта | узлы обоих лежат на радиусе 26, между ними перегон в 20 градусов |
| пуск → финал | оболочка и зал | ступеньки финала начинаются с ЕДИНИЦЫ на доле ноль, то есть с того же числа, что просит пуск |
| финал → подвал | рама рубки во весь экран | гаснет переходом в стилях, а не выключателем |

### Ход камеры

Проверка `tools/checks/ход-камеры.mjs` идёт по всей ленте шагом четыре
пикселя и меряет семь величин: угловую скорость взгляда, полёт боком и
длину таких кусков, расстояние до точки взгляда, линейную скорость и её
размах, шаг за один кадр быстрой прокрутки, расхождение перспектив
вёрстки и мира. Щелчок взгляда длится двадцать четыре пикселя прокрутки
и в редкую сетку не попадает никогда, поэтому шаг именно такой.

### Взгляд на обходе

Упреждение взгляда (поворот к касательной на перегоне) выключено там,
где узел объявил обзор от девяноста градусов. Иначе на двадцати
градусах перегона между ангаром и пуском ракета уезжала вбок ровно на
ту величину, на какую камера прошла по дуге.

### Прокрутка

Камера читает СГЛАЖЕННУЮ прокрутку, теми же двумя подтяжками igloo
(0.075 и 0.15, постоянные времени 0.214 и 0.103 секунды). До правки она
читала `scrollY` напрямую и дёргалась за пальцем, пока вся отделка шла
их плавным ходом.

---

## 7. Сверка с igloo.inc

Зеркало их сайта разобрано в `docs/РАЗБОР-IGLOO*.md` (шесть файлов, 10
тысяч строк). Ниже итог по тоннелю - месту, про которое владелец сказал
«один в один, чтобы невозможно было отличить на глаз».

| мерка | igloo | наш | было |
|---|---|---|---|
| пояс губы кольца | 193-243 | 200 | 159 |
| средняя яркость кадра | 152-189 | 172 | 203 |
| чёрного в кадре | 0.00% | 0.00% | 52-60% |
| размах каналов | 19-24 | 27 | 10.4 |
| выбито в белое | 0.7-19.5% | 0.2% | 34.8% |
| кольцо в кадре | по центру | центр (−0.01) | +0.57 |
| два кольца сразу | есть | есть | нет |

Радиальный профиль яркости повторяет их форму: светлая середина, провал,
пик на полосах 10-12, спад к кромке.

### Свечение

Числа их собственные: `levels 6, порог 0, сила 1, радиус 0.85`, половина
разрешения, 13 отсчётов вниз и 3x3 вверх, смешивание SCREEN. Радиус
проверен разлётом точки: R50=32, R80=105, R90=142, R99=219.

### Чего у нас НЕТ и не будет

- Их файлов моделей `.drc` и текстур `.ktx2` в репозитории нет и не
  будет: геометрия воспроизведена по замерам, а не скопирована.
- Первый экран у нас лунная ночь, у них ровный серый. Это выбор
  владельца, и он объясняет остаточное тёмное на подлёте.

### Урок первый: замер, который меряет не то, что нарисовано

Три случая этой приёмки стоит держать рядом: все три об одном - **такой
замер хуже отсутствия замера**, потому что отправляет искать поломку
туда, где её нет.

1. **Ход финала.** `дорога-финала.mjs` брал место корабля как
   `узел.position` (местное, ноль) и радиус обшивки как записанное 3.45
   (местное при масштабе 0.377). Печатал правдоподобные числа про чужое
   место, и по ним уже делались выводы. Плюс ждал камеру семьсот
   миллисекунд вместо того, чтобы дождаться остановки: на крупных шагах
   доли он показывал плато там, где камера шла. Оба чинятся мировыми
   матрицами и ожиданием по условию.
2. **Купол вокруг ракеты.** Я снял цилиндр текста по подозрению, и
   следующий снимок показал купол на месте. Виновника назвал только
   замер кадра (`что-в-кадре.mjs`), который проецирует коробку КАЖДОГО
   видимого предмета: «силовое поле фигуры», 265 на 546 точек. Цилиндр
   пришлось вернуть.
3. **Гарь в заголовке.** «Когда ложилкъя о 5Ф5?`т» я сперва принял за
   поломку атласа и полез сверять прямоугольники глифов с картинкой.
   Атлас оказался цел до точки. Поломка была в поводе: прокрутка пускает
   волну раз в 0.42 секунды, а волна гоняла перебор чужих глифов.
4. **Контраст текста в зале.** Проверка выдала полсотни промахов -
   1.7-2.9 при пороге 4.5, - и я пошёл чинить сайт. Сайт был здоров.
   Когда слова подняты в объём, разметка не удаляется (её читают
   поисковик и голосовой помощник), а прячется приёмом «для
   скринридера»: предку ставится размер в одну точку с обрезкой. Свой
   размер такого предка проверка отсеивала, а его ДЕТЕЙ нет -
   `getBoundingClientRect` отдаёт разметочный прямоугольник, и `span`
   внутри обрезанной коробки честно докладывал свои двести точек.
   Меряли текст, которого на экране нет.

   Правка знака утолщения при этом ВЕРНА и остаётся: она держится не на
   этом замере, а на арифметике шейдера (полоса перехода стоит на
   0.5-T, при отрицательном T она уезжает вверх и штрих тоньше) и на
   соседнем модуле, где то же поле с плюсом даёт читаемые подписи
   карточек. Верный вывод по неверному поводу - всё равно везение.
5. **Обратимость хода.** Проверка полёта: «те же доли, пройденные вниз
   и вверх, дают разные места камеры, до 96 единиц». Звучит как беда:
   человек листает назад и попадает не туда. Разбор занял три захода, и
   каждый заход ловил СВОЙ дефект замера:

   · ждал тишины камеры - не умеет отличить «доехала и встала» от «ещё
     не тронулась», выдал три доли подряд с одной точкой;
   · сравнивал два прохода между собой - разойтись они могут и потому,
     что не доехал один, а какой именно, по разнице не видно;
   · спрашивал эталон `проба(y)` про ту прокрутку, которую ЗАКАЗАЛ, а
     камеру мерил там, где она стоит. Браузер заказ дословно выполнять
     не обязан: пока липкие кадры не встали, лента короче, и scrollTo
     упирается в конец документа. Отсюда постоянный зазор 5.60,
     ОДИНАКОВЫЙ с обеих сторон, не уходивший и за сто пятьдесят секунд.

   Итог замера по фактической прокрутке (`tools/обратимость.mjs`): ход
   ОБРАТИМ, худший зазор до эталона 0.33 единицы, между проходами 0.54.
   Правила проверки полёта переписаны: рывок вместо пробега, зазор до
   позы вместо разницы проходов.

### Урок второй: чужое число верно для чужой сцены

Числа igloo верны ДЛЯ СЦЕНЫ IGLOO. Дважды пересадка их числа вслепую
ломала ровно то, ради чего пересаживалась:

1. Радиус кольца. Две по отдельности разумные поправки (1.35 за
   растяжение трубы вдоль и поперёк, 1.30 за наше более широкое поле)
   вместе дали ровно двойной промах: кольцо вышло 1.81 полувысоты против
   их 1.09, и яркий пояс диска ушёл за кромку кадра. Вернули их сырой
   0.4006.
2. Цвет знака в глыбе. Их тёмный `0x1A2130` верен для светло-серого мира
   igloo и невидим на нашей тёмной лунной стене.
3. Рубашка силового поля. У них фигура приземистая, рубашка садится на
   неё чулком и читается дымкой в стакане. Наша фигура это ракета,
   вчетверо выше при том же радиусе, и та же рубашка стала стеклянной
   колбой в полный рост - тем самым «куполом», который увидел владелец.

### Урок третий: проверка, которую убил не сайт

Проверка контраста трижды подряд отдала пустой файл и код 137. По коду
сайта искать было нечего: её убивал сборщик памяти. Каждый Chromium под
программным отрисовщиком берёт около двух гигабайт, и три брошенных
прогона держали шесть - на четвёртый места не осталось.

Отсюда правило прогона: **тяжёлые проверки гонять по одной**, а перед
прогоном смотреть `ps aux --sort=-rss | head`. Пустой вывод проверки
это не «сайт молчит», это «проверку сняли».

---

## 8. Что остаётся открытым

1. **Лунное небо первого экрана** оставляет 26.9 процента кадра ниже
   яркости 40. У igloo такого нет вовсе, потому что у них нет ночи. Три
   пути: оставить как есть; поднять небо до тёмно-серого 40-50 как
   засветку объектива; сделать первый экран серым, как у них, и убрать
   Луну. Решение за владельцем.
2. **`rocketweb.top`** (ссылка «Войти») отвечает 468 от щита SafeLine.
   Из песочницы не проверить, нужен обычный браузер.
3. **Даль за остеклением** финала показывает звёзды, а корабль стоит в
   подземном ангаре. Расхождение видно только когда глаз уже внутри и
   смотрит в окно.

## Рой частиц меряется по aHome, а не по position (правило, стоило полдня)

Рой (`assets/rv-частицы.js`) рисуется из атрибута `aHome`: в вершинном шейдере
стоит `vec3 p = aHome;`, а `geometry.position` при этом вырожден. Значит:

* `Box3.setFromObject(рой)` показывает НЕ ТО МЕСТО. Инструмент
  `tools/что-в-кадре.mjs` считает коробки по `position` и по рою врёт.
* Подмена материала на обычный `PointsMaterial` тоже ничего не покажет: он
  рисует по `position`, то есть по вырожденной геометрии.

Настоящее место точки на экране:

```js
v.set(a.getX(i), a.getY(i), a.getZ(i))   // a = geometry.getAttribute("aHome")
 .applyMatrix4(рой.matrixWorld)
 .project(камера);
```

Готовый замер лежит в `/tmp/гдерой.mjs` (перенести в tools при случае).

## Программный отрисовщик врёт пустыми кадрами

На SwiftShader один и тот же кадр при одних и тех же числах (uAlpha 1,
uVisible 1, все родители visible, проекция в середине экрана) снимается то с
роем яркостью p95 = 224, то без него вовсе, p95 = 72. Это артефакт съёмки, а не
сайта. Прежде чем чинить «пропавший предмет», сними его ТРИЖДЫ и померь
яркость: если хоть раз он яркий, чинить нечего.

## Кладка станции сидит ВНУТРИ группы купола (правило, стоило дня)

Блоки стены раскладывает `стенаРазметка()` в `assets/rv-act-станция.js`, и
кладёт она их в местные координаты `М.купол`. Группа купола на широком
экране была отодвинута вбок на `М.сдвиг.x`, поэтому местная координата
центра стены обязана этот отъезд ВЫЧЕСТЬ:

```js
var цX = -М.сдвиг.x / s;      // s = масштаб группы
```

Я один раз перевернула здесь знак, пробуя увести кладку из-под колонки
слов, и получила ровно УДВОЕНИЕ отъезда: на снимке монитора от стены
остался огрызок у правой кромки кадра. Знак трогать нельзя.

И сдвигать эту стену вбок нельзя вообще. Ту же стену следом строит акт
периметра (`assets/rv-act-fold.js`, `размерыСтены`) - и строит её В ГНЕЗДЕ,
то есть ровно по оси взгляда. Сдвинутая станционная кладка означает, что на
стыке актов стена прыгает вбок.

Место словам покупается высотой кладки (`верхС` в `лунномКадре`) или
раскладкой слов, но не сдвигом стены.

## Боковой раскладки слов на этом сайте больше нет

Акт станции был последним, где слова стояли колонкой слева, а предмет
уходил вправо (`сдвигX`). Владелец про неё: «половины сайта пустые поля»,
«всё всегда центрированно». У периметра, рубки и пуска слова стояли сверху
уже давно; теперь и у станции - `data-слова="сверху"` в разметке, `сдвигX`
ноль.

Проверять после любой правки раскладки надо ТРИ зазора, все в долях кадра
(NDC, -1 низ, +1 верх):

* низ зоны слов (`НИЗ_ШИРОКО` 0.145, то есть NDC 0.29) выше верха кладки
  (`верхС` 0.22);
* низ зоны слов выше верха купола (`верхД` 0.26 на лежачем кадре);
* правый край колонки слов (0.52 ширины по центру, то есть 76 процентов
  ширины экрана) левее манифеста (`.rv-манифест`, `max-width` 280).

Все три меряет `node tools/замер-луны.mjs пк`: строка «НАЛЁТ ШxВ» и есть
пересечение прямоугольников слов и предмета.

## Замеры после переезда слов первого акта (сентябрь 2026)

Все три инструмента гонялись по одному, на пустой машине - иначе программный
отрисовщик встаёт колом и точки идут по четыре минуты вместо одной.

`node tools/замер-луны.mjs пк` (1440x900). Доли в NDC, -1 низ, +1 верх.

| точка | предмет | слова | итог |
|---|---|---|---|
| станция 0.00 | дом -0.30..0.26 | 0.43..0.76 | чисто |
| станция 0.30 | облако -0.29..0.43 | 0.43..0.76 | чисто |
| станция 0.70 | облако -0.53..0.20 | 0.43..0.76 | чисто |
| станция 0.99 | кладка -0.60..0.15 | 0.43..0.76 | чисто |
| периметр 0.05 | кладка -0.58..0.18 | нет | стык держит |
| периметр 0.50 | кладка -0.60..0.15 | 0.47..0.76 | чисто |

Разброс камеры: x 0, y 0.17, z 0.09. Камера на первых двух актах стоит.

`node tools/замер-луны.mjs тел` (390x844).

| точка | предмет | слова | итог |
|---|---|---|---|
| станция 0.00 | дом -0.19..0.22 | 0.39..0.80 | чисто |
| станция 0.30 | облако -0.19..0.34 | 0.39..0.80 | чисто |
| станция 0.70 | облако -0.47..0.16 | 0.39..0.80 | чисто |
| станция 0.99 | кладка -0.61..0.18 | 0.39..0.80 | чисто |
| периметр 0.05 | кладка -0.59..0.20 | нет | стык держит |
| периметр 0.50 | кладка -0.61..0.18 | 0.36..0.80 | чисто |

Разброс камеры: x 0, y 0.17, z 0.10.

`node tools/центр-ракеты.mjs тел 7` - уход роя от середины экрана в точках
при ширине 390: рубка 0.5, 0.8, 3.1, 3.1, -2.0, 0.7, 3.2; пуск -0.3, 4.3, 2.9.
Предел ухода 4.3 точки, это 1.1 процента ширины. Ракета прибита к середине.

## Съёмка светлой темы: что тут правда, а что было моей ошибкой

ЭТОТ РАЗДЕЛ Я ПЕРЕПИСАЛА. В первой редакции он валил на программный
отрисовщик всё подряд, и это было неправдой: половину бед делала моя
собственная поломка в `rv-слово3d.js` (см. следующий раздел про невидимые
исключения). Оставляю обе части разделёнными, потому что смешивать «среда
медленная» и «код сломан» это ровно та ошибка, на которую я потратила
вечер.

**Что было моей поломкой, а не средой.** Пока в перекраске жил
ReferenceError, сборка ленты умирала на первом акте, класс
`рв-слова-в-сцене` не вставал НИКОГДА, и любая проба ждала его до своего
таймаута. Отсюда «прогон встал на час», «проба не дошла за пятнадцать
минут» и «занижение железа сделало хуже» - все три наблюдения были про
мой баг, а не про SwiftShader. После починки лента собирается за ДЕСЯТЬ
СЕКУНД на том же стенде.

**Что правда про среду.** Кадры действительно дорогие: рой на старшей
ступени это девятьсот тысяч точек, вершины SwiftShader считает
процессором, и один снимок идёт минуты. Прогон `tools/светлая-лента.mjs`
на шесть актов это десятки минут - но кадры ПРИХОДЯТ, папка не остаётся
пустой. Пустая папка это признак поломки, а не медленности.

Что помогает:

* Тема кладётся в `localStorage` ДО первого байта страницы, через
  `addInitScript` (`tools/светлая-лента.mjs`). Ставить её после загрузки
  дороже: второй проход стоит столько же, сколько первый.
* Ждать надо класс `рв-слова-в-сцене` на корне, а не `RV_MOTION`: до
  переезда слов в объём на снимке стоит РАЗМЕТОЧНЫЙ текст акта, другой и
  другими стилями. Я один раз сняла его и чуть не приняла за беду темы.
* Тяжёлые пробы гонять СТРОГО по одной и смотреть `ps` перед пуском. Две
  пробы на одной машине растягивают обе вчетверо. Это правда независимо
  от багов.
* Стенд поднимать с `PHP_CLI_SERVER_WORKERS=8`: одиночный `php -S`
  однопоточный, а страница просит восемь десятков файлов.

Числовая замена кадру: `RV_СЛОВО3D.замер()` говорит светлоту набора по
каждому акту (выше 0.5 светлый, ниже тёмный) и признак `data-текст`,
который акт просит. `tools/цвет-по-теме.mjs` её печатает,
`tools/переключить-тему.mjs` показывает обе темы разом через переключение.
Секунды вместо десятков минут - для темы кадр не нужен вовсе.

Чего НЕ делать: подменять `navigator.deviceMemory` и
`hardwareConcurrency` ради младшей ступени. Я пробовала, стало хуже, и
теперь понятно почему - дело было не в ступени. Подмену из инструмента
убрала.

## Исключения на этом сайте бывают НЕВИДИМЫМИ (правило, стоило вечера)

Я переименовала переменную в `перекрасить()` (`свой` -> `тьма`) и одну
строку пропустила: ореол по-прежнему читал `свой`. В строгом режиме это
ReferenceError на каждый вызов, а перекраска зовётся в конце сборки
КАЖДОГО акта - исключение выбивало цикл после первого, и слова не
переезжали в объём вовсе. Человек видел плоскую разметочную версию всей
ленты.

**В консоли страницы при этом было ЧИСТО.** Сборка идёт внутри
обработчика готовности `RV_MSDF["готов"]`, и он глушит исключения:
`pageerror` не приходит, `console.error` пуст. Проверка «беды: 0» ничего
не доказывает.

Признак жизни ленты один и надёжный - класс `рв-слова-в-сцене` на корне:

```js
document.documentElement.classList.contains("рв-слова-в-сцене")
```

Встал - лента собралась целиком. Не встал за полминуты на живой машине -
сборка умерла молча, и искать надо исключение, а не медленный отрисовщик.
Я потратила вечер на второе, пока не подменила файл на предыдущую версию:
с ней лента собиралась за десять секунд, с моей стояла на первом акте
пять минут подряд. Подмена версии и есть самый быстрый способ отделить
«медленно» от «сломано».

Готовая проверка лежит в скретче сессии (`чек-слов.mjs`): печатает
состояние каждые десять секунд и замер слов. Перенести в tools при случае.

## Правило темы проверено переключением (сентябрь 2026)

`node tools/переключить-тему.mjs` грузит тёмную тему (в облаке она
собирается, светлая нет), дожидается переезда слов и переключает тему тем
же событием, которым её переключает шапка. Светлота набора:

| акт | просит | тёмная тема | светлая тема |
|---|---|---|---|
| станция | - | 0.792 светлый | 0.018 тёмный |
| периметр | - | 0.784 светлый | 0.018 тёмный |
| рубка | светлый | 0.784 светлый | 0.784 светлый |

Ровно то, что задумано: наземные акты переворачиваются вслед за фоном,
подземные держат свой набор в обеих темах.

## Аудит кода в семь рук (сентябрь 2026): что он дал

Прогон по семи направлениям, у каждого свой скептик на перепроверку.
Сорок четыре находки пережили сверку. Направления и главное из каждого:

**Хвосты переименований.** Ровно тот класс ошибки, что убил ленту (см.
раздел про невидимые исключения). Нашлись ещё шесть мест, все мелкие:
подписки на события, которых никто не шлёт (`rv:наполнено`,
`rv:стыковка`), гнездо `data-заполнить="призыв"` без разметки, модуль
`rv-анкета.js`, который грузится и молча выходит.

**Тема при сборке.** Кроме четырёх уже починенных модулей: пересборка
слов по смене отношения сторон не спрашивала тему (поворот телефона
возвращал ночной набор), линии выноски у глыб стояли прибитыми светлыми,
карта окружения собирается один раз из ночного павильона.

**Проглоченные исключения.** Семь опасных мест. Два починены: обработчики
готовности атласа и очередь отложенных работ мира. Оба теперь пишут в
консоль и считают срывы, `RV_WORLD.срывы()` отдаёт число одним вопросом.
`tools/кадр.mjs` печатает его при каждом снимке.

**Финал против CDN.** Три настоящие поломки, все починены:
  · переходник спрашивал у мира поля `рисовальщик`/`сцена`, которых нет
    (объект мира несёт `r`/`scene`) - соседский пульт собирался без
    предела анизотропии и без карты отражений, то есть матовым;
  · в `RC_CABIN.build` не передавался `dПан`, проём под пульт резался по
    чужому числу 0.86;
  · поле зрения салона стояло числом 72 при камере на 84 (стоячий кадр).

**Обещания против досье.** Сняты: «чинят в тот же вечер» как правило,
роутеры Keenetic из строки «работает на», «это 350 ₽ в месяц» (досье
метит как догадку), незаявленные свойства Hysteria 2.

**Речь.** Два противопоставления «не X, а Y» в видимом тексте.

**Мёртвая проводка.** Кнопка «Оставить заявку» на первом экране вела на
якорь `#форма`, которого нет; теперь открывает окно формы. Гнездо игры не
имело НИ ОДНОГО правила стилей и садилось по запасным числам поверх ряда
клавиш; теперь идёт вторым именем во всех правилах карточки вопросов.

## Предел читаемости светлой темы (замерено, не догадка)

Тёмный набор на дневном небе упирается в контраст 2.7 при пороге 3.0 для
крупного текста. Это НЕ вопрос выбора цвета, и вот доказательство: я
довела краску до чистого чёрного, и снимок дал ту же яркость 124.
Проверено и снято с подозрения: положение каймы (любой цвет и ширина),
вес набора, направление свечения показа, наложение чужого слоя (слова
рисовались поверх всего - тот же результат), недоснятое проявление
(девяносто кадров ожидания и конец вступления - тот же результат).

Причина в сумме двух вещей: холст рисуется с плотностью 0.85 и
растягивается, а смешивание идёт в линейном пространстве, где тёмная
краска на светлом фоне поднимается гораздо сильнее, чем ждёт глаз. Тонкий
моноширинный штрих в 1-2 точки этого не переживает.

Числа для сверки: тёмная тема - небо 31, буква 221 при краске 242
(доходит до своей краски). Светлая - небо 197, буква 113 при краске 12.

Что с этим делать - решение владельца, и оно про вид, а не про код:
  1. оставить как есть (светлая тема вторична, тёмная основная);
  2. увести дневное небо в сумерки (на Луне атмосферы нет, небо там
     чёрное в любой час - физически это честнее нынешнего белого);
  3. поднять кегль набора в светлой теме.

## Мёртвая проводка: что убрано и что теперь считается (сентябрь 2026)

Второй заход по находкам аудита. Всё, что тут перечислено, проверено
поиском по репозиторию, а не на глаз: скрипт `хвосты.mjs` сводит вместе
объявленные и зовомые имена `RV_*`, посланные и слушаемые события, гнёзда
`data-заполнить` в разметке против гнёзд в коде, `id` из `getElementById`
против `id` в разметке, подключённые скрипты против файлов на диске.

**Событие `rv:стыковка` не слал никто.** Его ждали двое: ореол в
`rv-ореол.js` и плёнка в `rv-real.js`, оба чтобы вздрогнуть на входе в
ракету. Настоящее имя события входа - `rv:внутрь`, и приходит оно дважды,
на входе и на выходе. Обе подписки переведены на него с отбором по
`detail.что === "вход"`: на выходе камера уже внутри салона, толкать
нечего.

**Лента технологий вырезана целиком.** В `rv-слово.js` жил замер
горизонтального хода: ширина ряда карточек минус ширина окна, число в
`--лента-ход`, вёрстка катит ряд боком. От этого не осталось ничего:
гнезда `rvТех` в разметке нет, правил под `--лента-ход` и `[data-лента]`
в `rv.css` нет ни одного, технологии переехали в карточку пульта и
прокручиваются там вертикально. Замер выходил из первой строки по
отсутствию гнезда - на каждом изменении размера, на готовности шрифтов и
дважды по таймеру. Вместе с ним ушли подписки, событие `rv:наполнено`,
которого они ждали, и экспорт `RV_СЛОВО`: в нём было одно имя, и к нему
не обращался ни один модуль и ни одна проверка.

**Блок `призыв` в `rv-fill.js` снят.** Он клал кнопку «Забрать пробу», а
ровно такая же кнопка стоит первой в контактах: два одинаковых главных
действия в одном подвале спорят. Гнезда под него в разметке не было ни
разу, так что вживую он не рисовался никогда. Строка со статистикой из
него переехала под контакты - она единственное, чего больше нигде нет.

**`rv-анкета.js` удалён вместе со строкой подключения.** Он искал форму
`#rvАнкета`, которой нет с тех пор, как анкету признали дублем: все три
копии формы (окно шапки, плита подвала, пульт финала) кладёт `rv-меню.js`
одними полями и одним обработчиком. Пять килобайт на каждой загрузке за
то, чтобы ничего не сделать.

**Ложные тревоги, снятые проверкой.** `RV_КАБИНА`, `RV_КАБ_ПЛОСКАЯ`,
`RV_КАБ_НАСТИЛ`, `RV_ЛУННЫЙ_КАДР` объявляются скобочной записью или в
`assets/gen/каб/`, `RV_BOX` и `RV_VAR` стоят с запасным значением через
`||`. `rvМирГнездо` в `rv-опознаватели.js` имеет запасное `d.body`.
Остальные `id`, которых нет в разметке, создаёт сам код строками
`innerHTML`.

## Падения актов больше не невидимы

Три места в `rv-world.js` звали акт в перехвате, который не делал ничего:
`показать`, `кадр` и `размер`. Акт мог падать на каждом кадре, консоль
оставалась чистой, кадр пустым. Та же беда, что стоила вечера на ленте.

Писать в консоль каждый кадр нельзя: шестьдесят строк в секунду смоют
всё остальное. Теперь пишется ПЕРВОЕ падение каждой пары акт+место, а
дальше идёт счёт. `RV_WORLD.срывы()` отдаёт четыре числа одним вопросом:
`работы`, `перваяРабота`, `акты`, `первыйАкт` (плюс `атлас` от
`RV_MSDF`). Ноль по каждому - единственное правильное значение.

Там же снят перехват вокруг сборки карты окружения: он глотал срыв
целиком, корпус ракеты и щит рубки оставались матовыми, а в журнале было
чисто. Работа отдана очереди мира, которая падение уже ловит и считает.
Прямой путь (когда очереди нет) перехват держит, но теперь пишет в
консоль: там вызывающий ждёт текстуру, и ронять ему подъём мира из-за
отражений нельзя.

## Павильон отражений бывает дневным

Карта окружения снимается с мини-сцены: сфера с градиентом и четыре
софтбокса. Сфера стояла ночной ВСЕГДА, при любой теме, и на дневном небе
корпус ракеты отражал синие сумерки, которых на экране нет.

Софтбоксы остались те же: свет стана не меняется от темы сайта. Меняется
только заполнение сферы, и меняется скромно - дневной набор светлее
ночного втрое и всё равно на порядок ниже пиков ламп, а рисуют блик
именно пики. Двинуть сильнее нельзя: щит рубки и модели держат
`envMapIntensity` 1.8 и 1.25, а сфера бьёт по всей поверхности разом.

Смена темы пересобирает карту. Прежняя цель сносится ПОСЛЕ сборки новой,
а не до: сорвётся сборка - у сцены останется старая карта, а не пустота.

## Мёртвая прокрутка: три из четырёх закрыты, четвёртая за владельцем

Проверка движения после правок дала четыре места, где кадр почти не
меняется. Разобраны все четыре, и они оказались разной природы.

**Периметр, доли 0.00-0.14 (было 1.4, стало 2.9).** Настоящая находка,
и снимок её подтвердил: кладка стоит, первый снаряд ещё далеко и мелок в
кадре, меняется одно звёздное поле. Камера в этом акте не проходит ни
единицы намеренно, всё движение отдано снарядам и трещинам - значит
начинаться оно обязано с первого оборота колеса. Начало первого окна
снаряда сдвинуто с 0.04 на ноль. Конец последнего остался на 0.86: шаг
считается из трёх чисел, и сдвиг начала растягивает шаг, а не съезжает
хвост.

**Рубка, доли 0.86-1.00 (было 1.9, стало 2.3).** Тот же сдвиг окон
подобрал и её: разница держалась на волосок ниже порога.

**Финал, доли 0.86-1.00 (1.7).** Проверка была неправа, а сайт прав.
Владелец дословно: «панель управления это финал каждого из сайтов дальше
нее ничего не скроллится вниз вообще». Хвост финала это и есть та панель:
кадр держится, человек читает вопросы и жмёт кнопки. В проверке заведён
список `СТОИТ_ПО_ЗАМЫСЛУ` с одной строкой, актом, границами долей и
причиной. Исключение узкое: голова того же финала меряется как прежде.
Список лежит в одном месте, чтобы каждое послабление было видно взглядом.

**Станция, доли 0.00-0.14 (1.6). ОСТАЁТСЯ ОТКРЫТЫМ, и это выбор вида, а
не код.** Здесь два слова владельца тянут в разные стороны:
  · «когда я только начинаю скроллить, я долго вниз иду, но ничего на
    экране не меняется» - ради этого проверка и написана;
  · «я не понимаю, почему домик никогда не собран целым» - ради этого
    купол стоит целым больше половины акта, а камера не проходит ни
    единицы.

Снимки долей 0.00 и 0.14 совпадают почти полностью: тот же дом, та же
камера, тот же текст, разница только в звёздном поле и дыхании блоков.

Что пробовалось и почему не годится:
  · раньше начать разлёт купола - `плавно` в начале плоская, сдвиг с
    0.20 на 0.10 даёт на доле 0.14 раскрытие в один процент, кадр от
    этого не меняется;
  · привязать проявление снизу вверх (uIntro) к доле вместо времени - на
    первом кадре сайта дом окажется непроявленным, то есть ровно то, на
    что владелец жаловался;
  · усилить дыхание - оно идёт по времени, а не по прокрутке: число
    вырастет, а прокрутка по-прежнему не будет покупать ничего. Это
    подгонка под проверку, а не починка.

Остаётся то, что меняет замысел, и решать это владельцу:
  1. оставить как есть: первый экран держится, купол целый;
  2. дать камере лёгкий ПОДХОД вперёд в первой трети акта (вбок она не
     уходит, дом остаётся по центру, слово про «вправо-влево» не
     нарушено);
  3. начать разлёт купола с самого нуля - тогда первый экран снова
     встретит человека грудой блоков.

## Аудит трёх: VPN, CDN и игра (11.09.2026)

Владелец прислал восемь снимков с телефона и потребовал полный аудит:
визуал, текст, техника, адаптация, объём, движение камеры, замеры, и
список «что просил, что сделано, что нет». Ниже ровно это. Каждая строка
держится инструментом или снимком, а не впечатлением; где числа не
сходятся с просьбой, так и написано.

### VPN: просьбы владельца по этому заходу

| просьба | состояние | чем держится |
|---|---|---|
| ракета собирается из кирпичей тоннеля, рой частиц удалить | сделано | `rv-ракета-кирпичи.js`, `rv-частицы.js` удалён, кладка по прокрутке 0.78..0.99 |
| подмена стены: тот же камень на всём фильме | сделано | `кирпич` ЧИСТО: дом, периметр, прокол 1.194x0.673 |
| зал без каши: один корпус в кадре, ракета стоит на площадке | сделано | подножие в три ряда, `корпус` гасится по доле, снимки з3 |
| голограмма не залазит на пульт | сделано | проём рубки из `окноКадра` минус зазор 0.115 |
| рубка ближе, кадр как в CDN и игре | сделано частично | развёртка лица ЗУМ 1.28: рубка до кромок на мониторе и телефоне. Числа против CDN ниже |
| кнопки пульта ведут куда-то | сделано | «Перезвоните мне» и «Отправить заявку» уходят в Телеграм |
| без наложений объектов на текст | сделано | `контраст`, `витрина`, `речь` ЧИСТО |
| резкость холста один к одному | сделано ранее | раздел выше |

### VPN против CDN: где кадр финала ещё расходится (замер)

Телефон 390x844. У CDN в конце подъезда (доля ленты 1.0) рама рубки на
экране 150..605 по высоте, клавиши 620..720, под ними тёмная палуба. У
VPN (доля финала 0.99) проём рубки 108..564, клавиши 585..650, ниже -
палуба, освещённая лампами зала в бирюзу (луч из камеры в каждой точке
низа отвечает «салон»: это не пол зала, а палуба той же рубки под нашим
светом). То есть VPN стоит на сорок точек выше и на четверть дальше от
пульта, а палуба подсвечена иначе. Монитор: у обоих рубка до кромок,
разница в положении глаза 0.62 (VPN) против `C.p0` игры.

Что это значит: геометрия одна (рубку рисует `rc-cabin.js` с CDN),
расходятся место глаза и свет. Место глаза в `местоУПульта` уже
придвинуто с 0.95 до 0.62; дальше пульт лезет в кадр снизу. Свет: три
лампы зала (`rv-финал.js:1345-1357`) светят на палубу рубки, у CDN палубу
светят только лампы салона. Это следующий шаг, если владелец подтвердит,
что бирюзовая палуба мешает.

Стык внутри CDN тоже не идеален: в конце подъезда рама 150..605, а
первым кадром полёта 95..690 (поле 84 против 100 у полёта). Замер
`кдн-старт.mjs`: 0.5 с после нажатия кадр уже игровой. Скачок виден,
владелец о нём не писал, записываю честно.

### CDN: финал на телефоне «умер, тёмный экран»

На стенде с телефонным экраном финал живой на 0.88, 0.96 и 1.0 доли
(`кдн-финал.mjs`: салон, Земля, голограмма, клавиши; исключений ноль),
и старт полёта живой через 0.5, 2, 5 и 10 секунд (`кдн-старт.mjs`).
Значит на телефоне владельца гаснет не код сцены, а контекст WebGL:
браузер отбирает его за память, и это единственный путь к чёрному холсту
без единой ошибки в журнале. На что уходила память:

  · Земля: две карты 4096x2048 (день и ночь), по 32 МБ видеопамяти
    каждая плюс мип-уровни, анизотропия 16;
  · при потере контекста игра делала `close()`: слой снимался, мир
    забывался, а человек оставался на тёмном экране с угасающей
    карточкой брифинга - ровно снимок владельца.

Сделано в `rc-flight.js`:

  1. на телефоне Земля идёт с карт 2048x1024 (`earth-day-2k.webp`,
     `earth-night-2k.webp`), анизотропия восемь;
  2. потеря контекста ставит цикл на паузу, возврат продолжает тем же
     миром в той же точке; нет возврата три секунды - мир пересобирается
     на свежем холсте в том же режиме (сцена финала остаётся сценой,
     полёт полётом, без карточки брифинга поверх);
  3. размер звезды следует за плотностью кадра.

Проверить на устройстве владельца отсюда нельзя; проверено то, что можно:
стенд, оба перехода, синтаксис, проверка встроенных скриптов CDN ЧИСТО.

### Космос игры: «неестественные пятна»

Снимки стенда на телефоне показали мягкие кляксы по 20-40 пикселей. Что
они НЕ такое, проверено кадром, а не рассуждением:

  · не плёнка: кадр без постобработки (`_пост(false)`) держит те же
    кляксы, `кдн-игра-пост.mjs`;
  · не слои точек: все десять стоят в пикселях, крупнее семи нет, и с
    погашенными точками кляксы на месте, `кдн-кто-пятна.mjs`;
  · не спрайты, не прозрачные меши, не инстансы: те же три кадра.

Нашёл их луч, пущенный в саму кляксу (`кдн-кто-пятна-4.mjs`: кадр холста
читается в странице, пятна находятся по яркости, в каждое летит луч).
Все восемь лучей упёрлись в одно и то же: сфера радиусом 4200 с
панорамой неба (`sky`, `skyTexture`, `rc-flight.js:3678`). На панораме
самые яркие звёзды рисовались ореолом радиусом 1.7..6.3 текселя, а
тексель панорамы 2048 на телефоне занимает полтора-два экранных пикселя:
итого мягкие круги по 20-40 пикселей, плюс тридцать мелких цветных
мазков на каждую из четырёх туманностей.

Сделано: ореол 1.0..2.8 текселя и считается от размера карты (на 4096
вдвое больше в текселях, на экране одно и то же); мазков туманности
двенадцать вместо тридцати, шире и бледнее, прозрачность держится выше
ступени яркости, чтобы не вернуть решётку дизеринга. Та же беда была в
дали за стеклом VPN (`rv-финал.js`, `дальЗаОкном`): шестьдесят четыре
звезды с ореолом в пять с половиной радиусов, на мониторе круги по 20-40
пикселей; поджато до двух с половиной радиусов и ядра 1.4..3.

Замер после правки тем же инструментом на той же точке: пятен по
порогу (мягкие, от 120 точек площади) было восемь, стало пять, и все
пять сидят в одном месте - у балджа Млечного Пути, где панорама по
замыслу густая и яркая; по размеру это уже не круги по 20-40, а
скопление точек по 6-10 пикселей. Круги слева и внизу кадра ушли
совсем. Снимки: `/tmp/пятна4-тел.png` до, `/tmp/пятна4б-тел.png` после.

Что в космосе игры остаётся по замыслу и владельцу стоит знать: ближние
малые тела (спутники, малые планеты) на телефоне выходят серыми шариками
по 10-25 пикселей без резкой кромки, потому что плотность кадра там
0.72 и сглаживания у буфера нет; кольца Сатурна на такой плотности
лесенкой. Лечится плотностью, а плотность на телефоне владельца снимает
регулятор по тяжести кадра.

### Проверки этого захода и что осталось открытым

Набор VPN (`tools/проверить-всё.sh`) шёл параллельно с семью браузерами
замеров CDN при нагрузке 18, и два падения в нём (`обратно`, `полёт`) это
таймаут снимка в тридцать секунд, а не сайт: обе рухнули на
`page.screenshot: Timeout`. Их надо перегнать одних. Из прошедших:
витрина, глсл, движение, звук, контраст, переносимость, речь - ЧИСТО;
кирпич - ЧИСТО после выдержки в сорок кадров (до неё меряла купол на
середине морфа); приёмка - одна цель на телефоне 81x51 «А если связь
пропадёт?»: строка справочника, тычок на девятнадцать точек вверх и вниз
от середины попадает в соседа. Мелочь, но настоящая. CDN: встроенные
скрипты ЧИСТО, `панель-одна` и `маршрут` идут.

Открытое, по убыванию веса:

1. **Финал VPN против CDN на телефоне**: проём выше на сорок точек,
   глаз дальше от пульта на четверть, палуба подсвечена лампами зала.
   Числа в разделе выше. Следующий шаг: место глаза `C.p0` из игры и
   отсечь лампы зала от рубки слоями света.
2. **Стык сцены и полёта в CDN**: рама 150..605 -> 95..690 на первом кадре
   полёта, поле 84 -> 100. Владелец не называл, замер показал.
3. **`ход-камеры`** держит прежнее красное: 0.27..0.30 единицы до точки
   взгляда против порога 0.35 на y=20608 (монитор) и 19324 (телефон), это
   доля 0.87..0.9 финала, где глаз стоит вплотную к лицу секции. Порог
   писался под зал, а не под кресло пилота: либо порог по акту, либо
   точка взгляда дальше стекла. Решение за владельцем, менять кадр ради
   числа нельзя.
4. **Малые тела игры на телефоне** - серые шарики без кромки на плотности
   0.72, кольца Сатурна лесенкой без плёнки. Плотность снимает регулятор
   по тяжести кадра; на сильном телефоне этого нет.
5. **Цель 81x51** в справочнике голограммы на телефоне.
6. Прежние три пункта раздела 8 (лунное небо, `rocketweb.top`, даль за
   стеклом в ангаре) остаются за владельцем.

Выложено на боевые: VPN - `assets/rv-финал.js` (маркер правки отдаётся с
rocketvpn.top); CDN - `assets/rc-flight.js`, `earth-day-2k.webp`,
`earth-night-2k.webp` (маркер отдаётся с rocketcdn.ru, метки версий
поднял `bump.php`).


## Концовка обоих сайтов сведена в один кадр (11.09.2026)

Владелец снял вопрос о «задумках» прямо: «нету никаких задумок; концовка
начиная от открытия дверей ракеты, стоящей перед нами, точно так же, как
на CDN, сделать и на VPN; на CDN и на VPN убрать 360 оборот и сделать
1:1 весь фон, визуал, сцену, ракурсы, размер, движение камеры; на CDN
тоже 360 внутри ракеты убери, и сделай так, чтобы после входа в ракету
не было шва и вспышки, после чего просто чёрный экран; всё должно быть
бесшовно: вошли в ракету, подошли к панели и застыли, дальше вопросы,
кнопки и всё, и так одинаково 1:1 на двух сайтах».

### Что было не так на самом деле

**Тёмный экран CDN это не «сложно», это падение кода.** Инструмент
`tools/лента-яркости.mjs` листает ленту шагом полтора процента и печатает
среднюю яркость кадра. Профиль телефона показал ровную картину до доли
0.895 и ноль начиная с 0.910, а рядом исключение «i is not defined»:
сборка узлов салона падала, и в кадре не оставалось ни одного холста.
Снимок стенда совпал с тем, что владелец присылал с телефона. Причина
была моя же: правка расписания оборота унесла объявление переменной
цикла, а строгий режим на это отвечает ReferenceError.

Правило отсюда простое и не новое: **кадр проверяется профилем, а не
взглядом на одну долю.** Один снимок «вроде живой» ничего не говорит о
соседних пяти процентах ленты.

### Оборот снят на обоих сайтах

На VPN его не было с самого начала (`rv-act-финал.js`, «ПОЧЕМУ ОДНА
ПРЯМАЯ»). На CDN расписание оборота (`rc-interior.js`, KNOT) вело камеру
от входа на 0.35 радиана до полного круга через семь остановок. Теперь
это прямая из двух узлов: угол держится нулём, вход смотрит в
остекление, подъезд к панели считается ОТ ВХОДА, а не от конца оборота.

Вместе с оборотом снялись две подпорки, которые без него ломали кадр:
· ворота подъезда в `stageCam` (при нулевом угле они закрывали подъезд
  навсегда, и камера осталась бы у порога);
· доводка угла к полному кругу там же.

И одна, которую без оборота пришлось бы искать глазами: углы разделов
внутри рубки. `rc-cinema.js` ставит карточки по `PLAN.rel` и `PLAN.faq` и
поворачивает комнату на тот же yaw. Пока шёл оборот, справочник стоял на
двухстах градусах и приходил в кадр вместе с камерой; без оборота он
остался бы за спиной. Все углы сведены к азимуту панели.

### Ракурс и фон: формулы вместо подобранных чисел

Поле кадра и место глаза в финале VPN были подобраны по снимкам (72 и 84
градуса, глаз в 0.62 от обшивки). У соседей те же две величины
считаются: поле - `baseViewFov`, глаз - `панельD`. Обе формулы перенесены
в `rv-финал.js`. На телефоне это сразу дало 99 градусов вместо 84 - вот
отчего у них рубка обнимала кадр, а у нас стояла коробочкой в тёмной
рамке.

Вместе с полем ушло поджатие лица секции (`зумЛица` 1.28): оно вводилось
ради узкого поля, а при соседском поле пульт вылезал за кадр и подписи
клавиш обрезались по краям.

Свет: у соседей общий свет помещения к концу подъезда уходит на девять
десятых, а разгорается только пульт. У нас лампы держались в полную
силу, и замер это поймал числом - яркость нижней полосы кадра 184.9
против 110.2. Светилась палуба рубки, которой у них в кадре нет.

Земля за стеклом собрана теми же картами и тем же способом: дневная и
ночная, огни городов, шероховатость по океану, облачный слой, ободок
атмосферы и направленный свет для терминатора. Размер и место сверены по
снимкам: было 24 процента ширины кадра и центр ниже, стало 35 процентов
и центр там же, где у соседей.

### Копии модулей рубки сведены

`rc-cabin.js` расходился между репозиториями на 181 строку: на двух
сайтах стояли два разных корабля. Файлы сведены побайтно, различия ушли
в параметры вызова (зум лица, отрисовщик). Вместе с этим в игру приехали
правки соседа, которых у неё не было:

| что | было в игре | стало |
|---|---|---|
| текстуры экранов рубки | RepeatWrapping с мипами: на первой версии WebGL холст 784x704 ужимался до 512x512, текст мылился | ClampToEdge и линейная выборка без мипов |
| экраны | полупрозрачные, сквозь буквы шли блики обшивки | непрозрачные |
| окружение пульта | в `envMap` ложилась сама функция `RC_REAL.env` | ставится только текстурой |

### Чем это держится дальше

· `tools/два-финала.mjs` - снимает оба сайта и сравнивает числами: низ
  проёма, верх клавиш, яркость низа кадра и яркость стекла. Порог полтора
  процента высоты.
· `tools/лента-яркости.mjs` - профиль яркости по ленте: провал, вспышка,
  ступенька.
· Оба читают холст ВНУТРИ его кадра. Буфер WebGL не сохраняется, и чтение
  следующей задачей возвращает ноль на живом кадре - первый заход этих
  инструментов так и печатал «VPN 0», то есть мерил не то, что нарисовано.

## Стена разлетается целиком, а не «дыркой» (11.09.2026)

Владелец, глядя на живой сайт: «сейчас я на сайте вижу что просто дырка
появляется и мы сквозь дырку пролетаем. А должна вся стена разлететься
также по кирпичикам как и собралась только эти кирпичики должны за
пределы за рамки сайта во все стороны вылететь чтобы их видно не было
вообще и потом обратно прилететь и собраться в тоннель».

Дырка объяснилась числом. Камера проходит плоскость кладки на доле 0.073
прокола, а окно пробоя тянулось до 0.26: на 0.073 радиус дыры был 0.97
при том, что углу кладки нужен 1.83. То есть в секунду пролёта снята
была ровно вписанная в стену эллиптическая область, а рама стояла целой.
Дыра, сквозь которую пролетаем, это она и есть, буквально.

**Разлёт перенесён туда, где его видно.** На периметре камера СТОИТ и
кладка в кадре целиком с шестнадцати единиц. Последняя пятая акта вместо
последней восьмой, раскрытие до 0.72 вместо 0.45. Прокол добирает углы
за первые 0.055, то есть до плоскости стены.

**Скорость стала линейной по срыву.** Квадрат отнимал ход у кирпичей,
снятых последними: угловой доходит до полного срыва к концу окна, а до
этого квадрат резал ему скорость вчетверо, и он оставался плитой в
кадре. Боковой отход поднят до 5.4 минимум - это посчитанный порог
выхода за кромку при худшем сочетании поля и дальности (5.15).

**Возврат идёт дугой снаружи, а не прямой сквозь объектив.** В середине
сбора кирпич уводится на 3.4 радиуса кольца вбок и на 30 единиц глубже
кольца, поэтому к своему месту подходит спереди и в поле зрения. Поворот
доворачивается slerp'ом, а не встаёт конечным с первого кадра. Волна по
рядам в 0.12 хода: кольцо выкладывается на глазах.

### Сборку тоннеля не было видно ВООБЩЕ, и это выяснилось числом

Первый заход переносил на периметр только разлёт, а возврат оставлял
проколу. Замер положения объектива показал, что так его не увидеть
никогда:

| где | камера | кладка |
|---|---|---|
| периметр, любая доля | стоит на месте | в 16.9 единицы ВПЕРЕДИ |
| прокол 0.00 | z 121.5 | в 9.3 единицы впереди |
| прокол 0.10 | z 108.6 | в 3.7 единицы ПОЗАДИ |
| прокол 0.60 | z 54.0 | в 51 единице позади |

Кольцо, в которое складываются кирпичи, стоит на месте стены. Значит в
проколе оно оказывается за спиной к десятой доле акта, а сбор идёт
дальше и весь целиком случается у человека за плечом. Замер кирпичей это
и печатал: в акте прокола НОЛЬ в кадре от начала до конца, и не потому,
что они улетели за рамку, а потому, что все они позади объектива.

Поэтому на периметр перенесён весь показ, все три его части. Прокол
получает тоннель сложенным и остаётся тем, чем задуман, полётом внутрь.

| доля периметра | что происходит |
|---|---|
| 0.58 .. 0.70 | летит пятый снаряд и бьёт в середину стены |
| 0.70 .. 0.86 | стена разлетается и уходит за рамку кадра целиком |
| 0.86 .. 0.89 | пустая доля, кирпичей не видно вовсе |
| 0.89 .. 1.00 | они возвращаются и выкладывают кольца тоннеля |

Окно пятого снаряда сдвинуто с [0.86, 0.98] на [0.58, 0.70]: иначе стена
разваливалась бы раньше, чем прилетает то, что её ломает.

Замер после переноса (`tools/разлёт-кладки.mjs`, телефон 390x844):

| доля | пробой | в кадре | у стены | удалённость от середины |
|---|---|---|---|---|
| 0.60 .. 0.72 | 0 | 84 из 84 | 84 | 0.49 |
| 0.75 | 0.231 | 74 | 70 | 0.66 |
| 0.78 | 0.499 | 21 | 16 | 2.39 |
| 0.81 .. 0.90 | 0.77 .. 1 | **0** | 0 | за кадром |
| 0.93 .. 0.99 | 1 | **84** | 0 | 0.27 |

Читается так: вся стена стоит, разлетается, пропадает целиком, потом
ВСЕ восемьдесят четыре кирпича возвращаются и встают кольцом. Удалённость
в долях полуэкрана, единица это рамка.

### Замер считает кирпичи в кадре, а не нетронутые

Прежний `замер` кладки возвращал только «целых» (разлёт меньше 0.02). У
углового кирпича разлёт упирался в 0.80 при любом пробое, и замер
объявлял стену снятой, пока она стояла плитой у кромки. Добавлено поле
«неснятых»: оно и есть числовая приёмка правила «на этой доле кирпичей в
кадре нет».

## Один камень на весь фильм: последнее расхождение (11.09.2026)

Камень дома, стены, тоннеля и ракеты сводили трижды, и трижды оставалось
место, где он выходил разным. Разбор перед сдачей закрыл четыре:

| где | было | стало |
|---|---|---|
| швы дома в стене | гасли на 0.7, шов светлее тела на 40 % при том, что у кладки он темнее на 40 % | гаснут до нуля, купол свечение сохраняет |
| разброс по камням у дома | по клетке мира 2.1, размах 1.22 | по семени камня, размах 2.10, как у кладки |
| тоновая кривая дома | 0.60 + 0.86 на sol: тень ВТРОЕ светлее тени кладки | 0.21 + 1.34, расхождение меньше двух процентов |
| створки ангара в игре | свой камень 0x424B60, темнее общего в 2.5 раза | 0xA8B0C2, кривая общая |

Тоновая кривая и была тем, что владелец видел как «вначале одного цвета
кирпичи, потом другого, как будто одно на другое накладывается»: камень
не менялся, менялась глубина его тени.

Палитра камня живёт теперь в ЧЕТЫРЁХ местах (`rv-кирпичи.js`,
`rv-act-станция.js`, смена темы у обоих, `rv-игра.js`). Менять только все
вместе.

## Панель мылилась не там, где искали (11.09.2026)

Владелец: «и почему панель управления в салоне и до старта игры такая не
качественная?»

Плотность печати лица секции бралась у `devicePixelRatio`, а кадр
рисуется со СВОЕЙ плотностью: подбор поднимает её до 1.8 на мониторе,
где dpr равен единице. Текстуру пекли в 1.8 раза грубее кадра, в который
она встанет, и никакая правка плотности кадра этого не чинила - мылило
уже на текстуре. Спрашиваем сам отрисовщик (`r.getPixelRatio()`), dpr
остаётся нижней границей и запасом, пока мира ещё нет.

## Что нашёл разбор перед сдачей (11.09.2026)

Шесть направлений чтением, каждая находка проверена вторым агентом,
которому поставлена задача её ОПРОВЕРГНУТЬ. Четыре находки опровергнуты:
правка уже стояла в коде, а разбор читал комментарий с историей и
принимал колонку «было» за живой код. Подтверждённое и починенное:

**Главная кнопка финала VPN не была видна на телефоне.** Кадр намеренно
взят в полную высоту (`100lvh`), низ уходит под адресную строку, и там
по уговору нет ни строки текста, ни кнопки. Финал уговор нарушал: блок
кнопок стоял на 42 точки от НИЗА КАДРА. Счёт на 390 на 844: разница
единиц 99 точек, блок высотой 112 занимал полосу от 690 до 802, и
«Запустить VPN» в колонке последняя. Её не было видно ни одной точкой.

**Заголовки рубки CDN набирались системным шрифтом.** Шесть правил
спрашивали `var(--f-head, ...)`, а переменной в `rocketcdn` не объявлено
нигде; запасная ветка называла Unbounded и Bebas Neue, которых в папке
шрифтов не было. Всё падало на `sans-serif`, на Android это Roboto. Два
сайта стоят в одном мире, и шрифт у мира один.

**Цели нажатия ниже собственного порога проекта.** Условие
`max-height:650` ловит ВЕСЬ лежачий телефон, и выбор цели в игре получал
34 точки на 844x390, 926x428, 800x360. Крестик досье держал 34 на всех
устройствах при комментарии «на телефоне она ещё и крупнее». Кнопки
шапки VPN ужимались до 34 уже с 400 точек, хотя строка сходится с 334:
её считали на шесть кнопок, а меню собирает четыре.

**VPN умирал от отбора контекста навсегда.** Обработчик звал
`preventDefault` (просьба вернуть контекст) и следом уходил в запасной
режим и сносил отрисовщик, а слушателя `webglcontextrestored` не было
вовсе. Телефон отбирает контекст за память и почти всегда возвращает
через секунду. Ровно эту беду CDN уже починил.

**Тупик в погружении запирал ленту насмерть.** При потере цели
сбрасывалась фаза, но признак «рв-внутри» оставался, а он держит
`overflow:hidden` со знаком важности. Цели пересобираются на повороте
телефона, то есть случай обычный.

**Мир CDN замирал и прыгал на границе салона.** Кадр выходил из цикла
раньше записи трансформа, наезд до 1.14 замораживался, а выход возвращал
его одним кадром.

**Ожидание модуля рубки было бессрочным и безусловным.** Таймер без
счётчика попыток, и по приходу модуля игра открывалась в любом случае -
в том числе поверх раздела, который человек читает вместо неё.

### Что найдено и сознательно НЕ тронуто

`html.рв-кабина-есть` в `rv.css` несёт сорок правил и не ставится
никогда: плоскую раму рубки сняли с маршрута в пользу объёмной
(`rv-салон-cdn.js`), а `RV_КАБИНА.сила()` зовут ровно один раз и только
нулём. Правила мёртвые, но инертные, и живую раскладку финала держат
двенадцать других правил на `html.рв-живая-рубка`. Переименовать мёртвые
в живые значит положить снятую раскладку поверх принятой. Удалять их
перед самой сдачей рискованнее, чем оставить.

## Второе объявление имени в шейдере (правило от 11.09.2026)

Блок разброса цвета по камню переписывался в дом из кладки, чтобы камень
у них был один. Вместе с кодом приехало имя `zerno`, а в этом же `main()`
оно уже занято под трёхкомпонентный шум нормали. GLSL второго объявления
одного имени не прощает: программа не собирается, материал молча
перестаёт рисоваться, и в консоли при этом пусто. Дом не нарисовался бы
ни одним кадром, и поймать это глазами можно было только открыв сайт.

Поймал скептик, которому поставлена задача ОПРОВЕРГНУТЬ чужую находку:
он читал место сам и увидел столкновение, которого не искал.

Обе проверки шейдеров (`tools/checks/глсл.mjs` у VPN и
`tools/shadercheck.mjs` у CDN) теперь ловят такое сами. Они искали только
кириллицу, то есть ровно один из двух способов не собрать шейдер.

Проверка доведена двумя ложными тревогами, и обе стоит помнить:

1. **Разные ветки это разные шейдеры.** На `rc-planets.js` `float rcNdl`
   объявлен дважды, но в разных ветках обычного JS, и в собранную
   программу попадает ровно одна. Договорённость: подряд идущие строковые
   литералы это один кусок шейдера, а живая строка кода между ними
   (закрытая скобка, `else`, присваивание) кусок обрывает.
2. **Комментарий кусок НЕ обрывает.** После первой правки проверка
   перестала ловить ту самую поломку: комментарием считалась только
   строка, начинающаяся со знака открытия, а между двумя объявлениями в
   станции лежат семьдесят строк разбора, у которого середина начинается
   обычным словом. Состояние блока теперь ведётся, а не угадывается.

Проверено живьём на обоих сайтах: возвращаю `zerno` - краснеет и называет
обе строки; убираю - чисто, и `rc-planets` при этом молчит.

## Долги, которые не наши (записать при сдаче)

· **Реквизиты Исполнителя в оферте CDN не заполнены.** В тексте стоит
  «компания, оказывающая услуги под маркой Rocket CDN», в политике
  конфиденциальности оператор назван так же обобщённо. Без названия,
  ИНН и банковских данных оба документа юридической силы не имеют.
  Внутренняя пометка о черновике с публичной страницы снята: она лежала
  в `sitemap.xml` и читалась покупателем первой. Заполнить реквизиты это
  дело клиента и его юриста, а не вёрстки.

· **`html.рв-кабина-есть` в `rv.css` несёт сорок мёртвых правил.**
  Плоскую раму рубки сняли с маршрута в пользу объёмной, и признак
  теперь не ставится никогда. Живую раскладку финала держат двенадцать
  других правил на `html.рв-живая-рубка`. Правила мёртвые, но инертные;
  переименовать их значит положить снятую раскладку поверх принятой, а
  удалять сорок правил перед самой сдачей рискованнее, чем оставить.
  Снести следующим заходом, проверив финал замером до и после.

· **Мёртвый блок плоской панели салона в `rc-flight.js`** (stageBake,
  stagePanel, панельПрочь, заглушкаОкна, stageWall) помечен шапкой, но
  не снесён: четыреста строк в трёх местах плюс правила в
  `rc-flight.css`. Браузер их только разбирает, но не исполняет.

## Расписание периметра одним списком (11.09.2026)

Числа лежат в двух файлах и держатся ПАРОЙ. Разъедутся - и либо стена
разваливается раньше того, что её ломает, либо карточка втыкается в
пустоту, либо тоннель собирается за спиной. Всё это уже случалось.

| доля акта | что происходит | где число |
|---|---|---|
| 0.00 .. 0.22 | летит и втыкается первая карточка боли | `rv-снаряды.js`, `О_НАЧАЛО` и `О_ДЛИНА` |
| 0.17 .. 0.39 | вторая | шаг считается из `О_КОНЕЦ` |
| 0.34 .. 0.56 | третья, последняя | `О_КОНЕЦ = 0.56` |
| 0.56 .. 0.72 | все три стоят в стене, их читают | |
| 0.58 .. 0.72 | летит пятый снаряд, без текста, в середину | `rv-снаряды.js`, `уОкно` |
| 0.72 .. 0.86 | стена и карточки разлетаются за рамку кадра | `rv-act-fold.js` `пробойСтены`, `rv-снаряды.js` `разлС` |
| 0.86 .. 0.89 | пустая доля, кирпичей не видно вовсе | |
| 0.89 .. 1.00 | кирпичи возвращаются и выкладывают кольца | `rv-act-fold.js`, `кладкуНаТрубу` |

Прокол после этого получает и раскрытие, и сбор единицей: собирать
тоннель там нечем и незачем, объектив проходит плоскость стены к седьмой
сотой доли акта и дальше только удаляется.

Проверяется это `tools/разлёт-кладки.mjs тел периметр 0.60 1.0 0.03`: он
печатает по долям, сколько кирпичей в кадре и как далеко они от
середины, и краснеет, если стена ни разу не уходит целиком.

## Круг в середине кадра и лишние наложения (11.09.2026)

Владелец на снимке телефона: «ещё какой-то круг везде появляется, убери
лишние круги, очертания, невидимые наложения и тд». Нашлось три
источника, и все три разные.

**Четыре тела «пакета человека» в `rv-act-fold.js`.** Пакет втягивался В
КОЛЬЦО прокола, кольцо из мира убрано совсем (строка «КОЛЬЦА НЕТ
ВОВСЕ»), а сам пакет остался. Корень акта сидит в гнезде станции, гнездо
мир ставит в точку прицела позы, значит локальный ноль корня это ТОЧНЫЙ
ЦЕНТР КАДРА. Гало собиралось там с `uBright` 0.30 и без `visible=false`:
светящийся синий диск 0x8A9CFF на сложении, шириной 1.7 при высоте кадра
14.5 в этой точке, то есть двенадцать сотых высоты экрана, и стоял он
весь акт. Ни `кадрСтены`, ни `видимость` его не трогали ВООБЩЕ.

Рядом с гало жил шарик `М.свой` - непрозрачный материал с `depthWrite`
по умолчанию. Глазом его почти нет, а глубину в центре кадра он писал и
резал собой всё, что за ним. Это и есть «невидимое наложение».

Сняты все четыре: шарик, гало, хвост из двенадцати призраков и дальний
зрачок выключенного коридора (`М.зрачок` живёт вместе с `М.тоннель`, а
тот выключен). Перепись видимых тел `tools/что-в-кадре.mjs тел периметр
0.66` печатает «подозрительное: ничего».

**Вторая копия каждой подписи.** Кайму рисовал отдельный меш: та же
геометрия, то же место, тот же материал с другим цветом и весом, порядок
13 под набором на 14. Два меша на подпись, оба без записи глубины. В
самом `rv-msdf.js` кайма есть своя, одним проходом через `uHalo`, и
включена она вместо копии.

**Полярность подписей шла за темой.** Это верно для строки, которая
висит на небе. Подписям глыб фоном служит освещённая солнцем кладка, и
она светлая в обеих темах: замер снимка даёт 138-144 из 255. Белый набор
давал контраст 3.0, тёмный даёт 5.7. Вместе с цветом развёрнут
`uGlowDir`: гребень показа прибавляет к краске постоянную, и для тёмного
набора она тянула букву к фону - ровно та беда, что описана в
`rv-msdf.js` над самой строкой.

## Кегль подписей и отступ держатся парой (11.09.2026)

Кегль стоял на 0.074 полувысоты стены, и держала его правая кромка
кадра: слово «открывается» упиралось в неё на 380 точке. Лунный кадр с
тех пор пересчитали, и замер даёт той же подписи рамку 161 .. 287 при
ширине экрана 390, то есть запас в 103 точки. Ограничение осталось от
прежней раскладки, а цена его видна глазом: высота прописной была восемь
точек экрана.

Стало 0.105 на телефоне и 0.088 на мониторе. Отступ подписи над глыбой
теперь считается ОТ КЕГЛЯ (`0.74 + 1.01 * (строк - 1)` от него), а не
живёт своим числом: на прежних 0.074 формула даёт ровно прежние 0.055 и
0.075, то есть выверенная раскладка не сдвинулась, но разойтись они
больше не могут. Первый заход этого не сделал, и подпись съехала на
глыбу - замер показал 36 процентов строки под камнем.

**Подписи гаснут вместе со стеной.** Глыба на разлёте намеренно гаснет
только до 0.65 и остаётся в кадре осколком: уводить её совсем значит
оставить экран пустым. Разметке такое послабление не годится - снимок
доли 0.88 показал три читаемые строки, косо летящие по пустому кадру.
У выноски теперь своё гашение (`глыба["выноскаГаснет"]`), полное и вдвое
быстрее разлёта. Набор гасится через `uOpacity` своего материала:
у msdf-строки `opacity` материала не читается вовсе.

## Стык дома и стены сведён по тональной кривой (11.09.2026)

Дом стоял на 120, периметр на 137 при допуске 12, и владелец видел это
глазом: «вначале одного цвета кирпичи, потом другого, как будто одно на
другое накладывается».

Прежний расчёт ждал 137 и промахнулся. Ошибка была в самом ожидании:
между цветом шейдера и точкой на экране стоит тональная кривая, и она
жмёт верх. По двум замерам (множитель 0.69 даёт 93, 1.02 даёт 120)
медиана растёт примерно как множитель в степени 0.65, а не линейно.
Сведение «умножь во столько же раз» на такой кривой не работает вовсе.

И сводить надо НАВСТРЕЧУ. Догнать периметр одним домом значит поднять
множитель до 1.25, а вместе со стеной поднимется и собственная сцена
дома: на доле 0.1 медиана уже ушла с 61 на 82. Шаг делают обе стороны -
дом 1.02 -> 1.14 (`rv-act-станция.js`), кладка 0.62 -> 0.57
(`rv-кирпичи.js`). Замер: 129 и 131.

## Две проверки переписаны на то, что они обещают мерить (11.09.2026)

Обе давали красное там, где глазом всё в порядке, и это опаснее
зелёного вранья: по такому числу либо поднимают порог, то есть глушат
проверку, либо чинят беду, которой нет.

**`tools/подписи-и-глыбы.mjs` считал пересечение габаритных коробок.**
Глыба стоит вполоборота и наклонена, её коробка на экране это квадрат
вокруг косого осколка, и добрая половина квадрата это пустой камень.
Замер давал треть строки «под камнем» там, где текст лежит на чистой
кладке. Теперь из каждой точки строки идёт луч от камеры и смотрит, что
человек увидит первым - букву или лёд. Живой кадр: 6 и 2 процента.

Проверка носит с собой САМОПРОБУ (`проба` третьим словом): она ставит
лёд перед подписями и обязана покраснеть. Первый заход самопробы сажал
подпись на глыбу и получал ноль - и получал верно, потому что выноска
вынесена вперёд и текст оказывался ближе льда к камере. Проба проверяла
не ту беду. Сейчас: живой кадр 6 и 2, проба 53 и 98.

**`tools/очаг-трещин.mjs` искал светлые точки вокруг глыбы.** В квадрат
вокруг глыбы попадает сама глыба, а у льда яркая кромка, и она
перетягивала центр тяжести: замер писал «центр свечения ушёл на 0.83»
при звёздах разломов ровно под камнем. У нижней плиты порог не брал
ничего вовсе.

Теперь трещины выделяются РАЗНОСТЬЮ ДВУХ КАДРОВ: снимок как есть, потом
`uHitPow` в ноль и снимок ещё раз. Что поменялось между кадрами, то и
есть трещины - ни камень, ни лёд, ни небо за это время не двигались.
Мера: доля разности, попавшая в круг радиусом `predel` вокруг
какой-нибудь точки удара. Радиус берётся тот же, по которому рисует
шейдер, и переводится в точки экрана проекцией.

Сами `uHits` при этом проверены отдельно и сходятся с `worldToLocal`
самих глыб до трёх знаков: (-0.574, 0.931, 0.45), (0.574, -0.222, 0.45),
(-0.574, -1.375, 0.45).

## Круглое в кадре теперь ищется инструментом (12.09.2026)

Владелец сказал «какой-то круг ВЕЗДЕ появляется», то есть беда не в
одном акте. Обходить шесть актов и две темы глазами долго, а круг в
сцене узнаётся по признакам: кольцо и диск видны своей геометрией,
плоскость с радиальным спадом - куском `length(vUv - 0.5)` прямо в
исходнике материала.

`tools/что-в-кадре.mjs` печатает теперь отдельный раздел «круглое в
кадре»: что это, где его середина и стоит ли она в середине экрана.
Последнее важно само по себе - корни актов мир ставит в точку прицела
позы, поэтому забытое тело садится ровно в центр кадра, и заметнее его
нет ничего.

**Одного шейдера мало.** Первый прогон записал в круги дымку горизонта
Луны: радиальный спад у неё тот же, но плоскость растянута 9.74 на
0.22, сорок четыре к одному, и на кадре это полоса у земли. Проверяется
отношение сторон НА ЭКРАНЕ, от двух пятых до двух с половиной.

## Вес страницы: сжатым, а не сырьём (12.09.2026)

Приёмка поймала настоящий рост - порог 6 МБ ставился от тогдашних трёх
с половиной, страница дошла до 6.55. Но мерилось не там. Стенд это
`php -S`, он отдаёт файлы как есть; боевой сервер отдаёт сжатыми
(`curl -I https://rocketvpn.top/assets/rv-act-fold.js` → `gzip`). Сайт
это 2.7 МБ исходников на JavaScript с подробными комментариями: 4.15 МБ
текста сжимаются до 1.19, на семьдесят один процент.

Проверка считает оба числа. Сырое ловит возврат тяжёлого файла и
отвечает за память и разбор; сжатое отвечает, сколько уедет к человеку,
и по нему теперь вердикт. Текст жмётся настоящим gzip из `node:zlib`, а
не оценкой по доле; картинкам, шрифтам и моделям gzip не даёт ничего,
они идут как есть. Пороги 4 МБ сжатым и 8 сырьём - тот же запас в
треть, с каким ставился прежний.

Сейчас: **3.09 МБ сжатым, 6.06 сырьём, 82 запроса**.

**Снимки Земли отложены до финала.** Сцена финала собирается сразу при
готовности мира, и по делу: её поза задаёт дорогу камеры, а без сцены
мир повёл бы объектив не туда. Карты не обязаны ехать так рано - три
снимка (день 217, ночь 71, облака 215 КБ) уезжали с первым экраном,
хотя видны в последнем акте. Держим место заглушкой в одну точку и
подменяем снимок внутри той же текстуры по первому показу акта.

На таймер это не вешаем: отложить на шесть секунд значило бы обмануть
замер, а не разгрузить человека.

**`dispose` перед подменой обязателен.** Заглушка одна точка, снимок
2048 на 1024, и три.js на `needsUpdate` докладывает картинку в уже
выделенную на видеокарте текстуру. `glTexSubImage2D` с чужим размером
отвечает `GL_INVALID_VALUE: Offset overflows texture dimensions`, и
Земля остаётся заглушкой: за окном рубки ровная бежевая заливка. Ошибка
тихая - исключения нет, загрузка отчитывается успехом, в журнале одна
строка GL. Поймано снимком финала 0.70, а не журналом.

## Скорость на мониторе: не регрессия, а старое состояние (12.09.2026)

`tools/checks/скорость.mjs` краснеет на ПК: девяносто пятая доля кадра
2117 и 2150 мс при пороге около 1900. Телефон при этом вдвое под
порогом - 967 и 1033.

**Это состояние старше нынешней работы, и работа его чуть улучшила.**
Замер поставлен прямо: коммит 56b8f54 (до всей этой сессии) поднят
отдельным стендом на порт 8175 и прогнан тем же инструментом - 2183 мс.
Нынешний код даёт 2117 и 2150. То есть снятые тела и убранная вторая
копия каждой подписи дали около полусотни миллисекунд, а не отняли.

**Мерить это тут можно только с оговоркой.** Стенд рисует SwiftShader,
то есть процессором, и он упирается в закраску: монитор 1440x900 это в
3.5 раза больше точек, чем телефон 390x844, отсюда и вся разница между
двумя строками замера. На живой видеокарте отношение обратное. Поправка
проверки считается по растровому эталону и выравнивает СКОРОСТЬ МАШИНЫ,
а не площадь кадра.

Порог не трогаем: подогнать его под нынешнее число значит потерять
единственный сторож, который заметит настоящее утяжеление. Красное
здесь честно означает «на софтверной отрисовке монитор тяжёлый», и это
записано, чтобы следующая сессия не искала регрессию там, где её нет.

## Финал VPN против финала CDN: две мерки тянут в разные стороны (12.09.2026)

Владелец: «концовка начиная от открытия дверей ракеты точно так же, как
на CDN», «пульт внизу, палуба за кадром». Проверяет это
`tools/два-финала.mjs` четырьмя числами. На начало работы:

    низ проёма    VPN 0.720   CDN 0.747   (порог 0.015)
    верх клавиш   VPN 0.869   CDN 0.798   (порог 0.02)
    яркость низа  VPN 155.7   CDN 7.1

Третье число и есть то, что видно глазом: под пультом оставался
просвет, и в него светила бирюзовая подсветка палубы, а у соседей в том
же месте чернота.

**Зал гасит своё свечение, когда человек вошёл.** Финал зовёт комнату
кадром с долей единица и снаружи, и внутри, поэтому сама она различить
этого не может, а обруч, воздух и дым у неё складывающие. Признак
ставит финал тем же числом, которым уводит обшивку
(`RV_КОМНАТА.вСалоне`). Дало немного, 155.7 -> 148.7: главным
источником оказался не зал, а собственная палуба рубки.

**Глаз стоял дальше пульта, чем у соседей.** Замер ширины пульта на
мониторе 1440x900: у нас 85.1 процента кадра, у них 91.0, отношение
1.069. У соседей глаз стоит в `панельD` от СТЕНЫ (`back = cabin.R -
панельD`), и пульт висит на ней; у нас та же D отмеряется от щита, а щит
на четверть метра ближе стены. Полную четверть отдавать нельзя - это
шестнадцать процентов при измеренных шести с половиной. Берём ровно
измеренное: `L = D * 0.935`.

**Прицел считается по наклону, а не подбирается.** Два прежних круга
дали пару точек: прицел -0.31 -> низ проёма 0.663, прицел -0.17 ->
0.718, то есть 0.393 кадра на единицу прицела. Отсюда -0.096.

**И четвёртый круг опроверг старую запись в коде.** После сближения
глаза проём уехал на 0.764; расчёт требовал опустить прицел до -0.139, и
по проёму это сошлось идеально (0.746). Но палуба вернулась: яркость
низа прыгнула с 41.6 на 133.3. Комментарий в `rv-финал.js` обещал
ровно обратное - «опускаем взгляд, пульт поднимается в кадр и сам
закрывает палубу», - и живой замер его опроверг. Опущенный взгляд
открывает просвет под пультом, а не закрывает.

**Выбор по цене ошибки.** Семнадцать тысячных кадра по низу проёма
глазом не различить. Девяносто два пункта яркости на нижней пятой части
экрана видно сразу, и владелец назвал это словами. Остаёмся на -0.096.

Стало:

    низ проёма    VPN 0.766   CDN 0.747   перелёт на 0.004 сверх порога
    яркость низа  VPN 35.9    CDN 7.1     было 155.7

**Что осталось и какой меркой это мерить.** Просвет между пультом и
нижней кромкой у нас ещё есть, у соседей его нет: их пульт занимает те
же строки кадра целиком. Значит остаток закрывается РАЗМЕРОМ пульта, а
не наклоном камеры - наклон уже уперся в размен между двумя мерками.
Следующий круг: замерить вертикальный габарит пульта на обоих кадрах
так же, как замерена ширина, и считать поправку из него.

«Верх клавиш» (0.934 против 0.798) читать пока нельзя: проверка ищет
первую снизу строку яркой полосы, и у нас она находит остаток палубы, а
не клавиши. Число станет осмысленным, когда палуба уйдёт совсем.

### Телефон: та же беда, но мерить её этим инструментом нельзя (12.09.2026)

`tools/два-финала.mjs тел` на VPN не находит ни низ проёма, ни верх
клавиш (оба `null`), а яркость низа даёт 41.8 против 110.2 у CDN - то
есть у нас ТЕМНЕЕ, а не светлее. Читать это как «на телефоне лучше»
нельзя: числа меряют не то.

На стоячем кадре панель вопросов закрывает остекление целиком, и искать
под ней кромку стекла нечего - отсюда `null`. Нижние три процента кадра
у соседей это сам пульт (он доходит до кромки), у нас - тёмная полоса
ПОД палубой, потому что пульт стоит выше. Число получилось меньше от
той же беды, от которой на мониторе оно было больше.

Глазом на снимках видно одно и то же: у CDN пульт упирается в нижнюю
кромку, у нас между ним и кромкой сначала тёмный провал, потом
бирюзовая дуга палубы. Значит остаток один и тот же на обоих экранах, и
закрывается он одним и тем же - вертикальным габаритом пульта.

Для телефона проверке нужны свои мерки: верх пульта вместо низа проёма
(стекло закрыто панелью) и доля кадра между пультом и кромкой вместо
яркости последних трёх процентов.

## Рубки сверяются одной меркой с двух сторон (12.09.2026)

Владелец: «почему не доделать то, что есть в CDN, и потом просто ctrl+c
ctrl+v? В чём сложность?»

**Салон уже скопирован целиком.** `rc-cabin.js` лежит в обоих сайтах
одним файлом, без единой правки. Не скопирована КАМЕРА, и не могла
быть: у соседа рубка ставится ПОД камеру полёта (`back = cabin.R -
панельD`, rc-flight.js), а у нас камеру ведёт лента актов, рубка же
стоит в мире. Один и тот же кадр приходится получать с двух сторон.

**Сложность была не в этом, а в том, что сверять было нечем.** Наш код
повторял их формулы пересказом в комментариях, а все проверки рубки -
и наши, и соседские - смотрят на КАДР, внутрь сцены не лезет ни одна.
Пересказ разошёлся, и разошёлся тихо. Отсюда четыре круга подгонки по
снимкам, записанные выше у прицела.

Теперь обе стороны отвечают одними и теми же тремя числами:
`RV_ФИНАЛ.замерРубки` и `RC_FLIGHT.замерРубки`, сверяет
`tools/рубки-рядом.mjs`. Числа местные, в единицах салона: узел финала
у нас ужат, мировые единицы разные, а местные те же.

Первый же прогон назвал обе беды сразу:

    глаз над настилом      1.6204 против 1.62     сошлось
    глаз до стены панели   1.662  против 1.55     мы дальше
    наклон взгляда        -1.28   против -3.05    мы выше

**И объяснил, почему четыре круга не сходились.** У соседа глаз ближе,
поэтому пульт крупнее и закрывает палубу СОБОЙ - и это позволяет ему
смотреть НИЖЕ нас, а не выше. Меняя по одному, каждый раз ломаешь
второе: придвинешь глаз - уедет проём, опустишь взгляд - вылезет
палуба. «Две мерки тянут в разные стороны» только поодиночке.

Правки, обе формулой:

· **дистанция.** D отмерялась от щита, а щит стоит не на обшивке
  (обшивка -3.05, щит -2.80). Вычитаем разницу честно, плюс измеренный
  недоезд мира: камера идёт по позе с затуханием и последние сотые не
  выбирает.
· **высота и наклон разом.** Мир ведёт камеру по прямой от «от» к «на»,
  значит эти две точки задают сразу обе величины. Считаются из двух
  чисел соседа: глаз 1.62, наклон 3.0529 градуса. Модель проверена ДО
  правки - предсказала нынешний наклон 1.681 при замере 1.6808.

Стало: 1.6176 / 1.5506 / -3.0526 против 1.62 / 1.55 / -3.0529.

### Две гипотезы, которые замер отверг

**`pitch` в сборку салона.** Сосед передаёт в `RC_CABIN.build` наклон
взгляда, мы нет, и rc-cabin правда крутит им развёртку снимка
(«Своя развёртка», строка 1277). Сдвиг пульта в кадре на 0.04 в
точности отвечал нашим трём градусам. Гипотеза стройная и неверная:
проба на обе стороны знака дала низ проёма 0.803 и 0.699 при 0.750 без
наклона, а палуба не убралась вовсе. Похоже, у нас наклон уже учтён
камерой позы, и в развёртке он накладывается вторым разом.

**`dПан` со сломанным знаком.** Выражение считало
`-R_СТЕНА - (ЭКР_ПЛОСКОСТЬ + даль)`, то есть минус 1.61, переходник
берёт только положительное - и число молча отбрасывалось, работала
запасная формула. Комментарий рядом при этом обещал обратное. Чинить
знак было НЕЛЬЗЯ: с верным знаком выходит 1.61, а сосед режет проём по
`панельD`, то есть по 1.55. Запасная формула переходника И ЕСТЬ
панельD, перенесённая оттуда без правок. Верное действие - не
передавать ничего; мёртвое выражение убрано.

### Что осталось

Палуба в кадре: яркость низа 64.1 против 7.1 у соседа (было 155.7).
Низ проёма при этом сошёлся - 0.751 против 0.747. Глаз стоит в рубке
идентично, салон один файл, а пульт у нас занимает 98.7 процента
ширины кадра против 91.1 у соседа, то есть ВЫШЕ и КРУПНЕЕ его. При
одинаковом глазе и одинаковом салоне так быть не должно, значит
расходится что-то в параметрах сборки секции - и искать это надо
сравнением того, что каждая сторона кладёт в `RC_CABIN.build`, а не
снимками.

## Скорость ленты: мерить экранами, а не единицами мира

Владелец 11.09.2026: «скорость движения камеры по всему сайту точно
такая же, как на igloo; все анимации, действия сценария должны быть в
одной скорости и движении, привязанные к скроллу».

### Разрыв темпа внутри акта был и закрыт

`темпАкта` в `rv-world.js` держит ступеньку: первую часть ленты акт
идёт ползком, дальше в полный ход. Второй отрезок был сглажен
(`smoothstep`), а первый оставался ЛИНЕЙНЫМ, и на стыке скорость
обрывалась. Правка - тот же smoothstep на оба отрезка.

Доказательство, что это был именно обрыв, а не кривизна: сетку замера
сгустили вчетверо. Кривизна при сгущении падает вчетверо, разрыв
производной остаётся на месте. Он остался.

Мерит это `tools/скорость-ленты.mjs`, и мерит ОБРЫВ между соседними
кусками, а не размах по акту. Размах велик у любого движения, которое
начинается с покоя, - он такой же и у igloo. После правки: станция 9%,
периметр 0%, прокол 7%, финал 13% при пороге 40.

Порог проходят ОБА соседних куска, а не больший из них. Иначе в мерку
попадают трогание с места и остановка: переход с нуля на ход всегда
«сто процентов», и станция краснела на y=24, то есть на первых двух
десятках пикселей ленты.

### Единицы мира не отвечают на вопрос владельца

По мировой мерке акты расходятся вчетверо: прокол 20.1 единицы на 1000
пикселей прокрутки, финал 4.5. Отсюда шёл вывод «лента идёт с разной
скоростью» и задание порезать акты по длине прокрутки.

Вывод был неверный, и правка сломала бы финал. Единиц мира человек не
видит. Он видит, как быстро картинка ползёт по ЭКРАНУ, а это зависит
от расстояния до того, на что он смотрит: в проколе камера смотрит
вперёд на 58 единиц, в финале на 9 - внутри рубки стена в двух метрах
от глаза. Двадцать единиц в просторе выглядят медленнее пяти в рубке.

Паспорт igloo (`docs/РАЗБОР-IGLOO-ДВИЖЕНИЕ.md`) замерен ровно так же:
поводок ±0.5625 ЭКРАНА, ограничение 4.5 ЭКРАНА в секунду. Сверяться с
igloo по мерке, которой у igloo нет, значит сверяться ни с чем.

### Экранная мерка и её вердикт

`tools/экранная-скорость.mjs`. Глубину берёт у самой пробы мира
(`доВзгляда` - расстояние до точки, на которую камера смотрит по
сценарию), ставит на этой глубине крест из пяти точек в долях
полукадра, проецирует его позой в точке `y` и позой в `y + шаг` и
меряет сдвиг в долях высоты экрана. Ни отрисовки, ни лучей.

Одной точки мало: поворот вокруг оси взгляда центр не двигает вовсе, а
картинка при этом крутится. Отсюда крест, и медиана по пяти точкам.

Замер на мониторе 1440x900, шаг 30 пикселей:

    акт        лента,px  экранов/1000px  на ходу  доля хода  глубина
    станция    3240      0               0        0%         24.83
    периметр   5400      0               0        0%         24.83
    прокол     2970      0.089           0.089    100%       58.41
    финал      6300      0.08            0.088    94%        9.05

**Разброс ходовой скорости 1.0x.** Там, где камера едет, лента УЖЕ идёт
одной скоростью, и резать акты по длине не нужно. Четырёхкратная
разница в мировых единицах была обманом глубины целиком.

Сверяем ХОДОВУЮ скорость, медиану по кускам от 0.02 экрана на 1000
пикселей. Медиана по всему акту занижена там, где камера по замыслу
долго стоит: финал последнюю треть ленты держит рубку, и по средней он
дал бы 0.08 против 0.088 на ходу. Держать в мерке стоянку значит
мерить длину стоянки, а не скорость хода.

### Первые два акта стоят намеренно, и это остаётся вопросом к владельцу

Станция и периметр дают ноль: камера за оба акта не проходит ни
единицы. Это не поломка, это прямая прежняя просьба владельца («камера
стоит на месте, дом разбирается по кирпичикам»), и записана она в
`rv-act-станция.js` словом в слово. Картинка при этом не стоит: дом
разбирается, кирпичи летят, стена собирается - мерка считает камеру, а
не сцену.

Вместе это половина ленты (8640 пикселей из 17910) с неподвижным
объективом. Если «одна скорость везде» понимать буквально, двум первым
актам нужен снос вбок - по расчёту около шести единиц за акт, чтобы
выйти на те же 0.088 экрана. Это правка СЦЕНАРИЯ, а не починка бага, и
она отменяет названную просьбу. Сделано не будет до слова владельца;
числа посчитаны, работа на один вечер.

## Кольца тоннеля: своими, а не из кирпича (владелец, 12.09.2026)

Владелец дословно: «помнишь я говорил что кольца нужно сделать из того
же кирпича? я передумал. из твоих колец был лучше, давай просто сделаем
чтобы стена разлетелась как сейчас, а дальше просто те кольца как раньше
будут».

Это отмена ЕГО ЖЕ прежней просьбы («из этих же кирпичей формируется
туннель»), и написано это здесь затем, чтобы кирпичные кольца не
вернулись через месяц как «задуманное по сценарию». Задумано было так,
потом решение сменилось. Действует последнее.

### Что осталось и что ушло

Разлёт стены не тронут ни одним числом: пятый снаряд на 0.58..0.72
периметра, разлёт 0.72..0.86, дальше кадр принадлежит трубе. Владельцу
нравится именно этот разлёт, и трогать его было нельзя.

Ушёл сбор: кирпичи больше не возвращаются на кольцо. Тоннель рисует
`rv-труба.js` своими тремя разбитыми кольцами по кадрам igloo - каменные
дуги с зазорами и белой каймой по внутренней кромке.

### Флаг вместо вырезанного кода

Сбор выключен одним читаемым флагом `СБОР_В_КОЛЬЦА` в `rv-act-fold.js`,
а не удалён. Причина не в жалости к коду: решение по этому месту
менялось дважды, механизм стоил трёх кругов подгонки (квантование по
глубине на три кольца, по углу на дуги с зазорами, горб возврата снаружи
мимо объектива), и третье возвращение теперь стоит одного слова.

Флаг ЧИТАЕТСЯ кодом, поэтому мёртвым выражением он не станет - на таких
мы здесь уже обжигались дважды (`dПан` со сломанным знаком и `pitch`,
который не доезжал до сборки). С `false` сбор просто никогда не
запускается, и `М.сбор` остаётся нулём весь фильм.

### Кладка уходит со сцены, но её время идёт

Периметр гасит кладку на 0.89 - ровно там, где прежде начинался возврат.
Без гашения три тысячи разлетевшихся кусков висели бы в кадре последнюю
десятую акта, где человек уже должен видеть кольца.

Работу кадра при этом НЕ обрываем, и ранний возврат отсюда был бы
ошибкой: внутри того же блока висят снаряды (`RV_СНАРЯДЫ.кадрСтены`
ведёт пятый и убирает следы четырёх предыдущих), а невидимой кладке
`пробой` обязан дойти до единицы - иначе на обратной прокрутке стена
вернётся недорванной. Проверка `обратно` это и держит: одна доля
прокрутки даёт один и тот же кадр, замер прошёл чисто.

В проколе кладки нет вовсе. Замер положения объектива записан там же в
коде: плоскость стены камера проходит к 0.07 доли и дальше только
удаляется, то есть все кирпичи позади объектива с начала акта. Показывать
их там было бы возвратом разлёта, который человек уже отсмотрел.

### Проверка переписана с ДВУХ сторон

`tools/checks/кирпич.mjs` держала отменённое правило и требовала кладку в
кадре прокола: кирпичи БЫЛИ стенкой тоннеля, и пустой кадр означал
развалившуюся трубу. Теперь наоборот.

Мало убрать ожидание кладки. Тогда пустой кадр без колец тоже прошёл бы
как «чисто», а это и есть та беда, которой боялись - тоннеля в кадре нет
вовсе. Поэтому кольца считаются отдельно (`кольца()` ищет в сцене
видимые сетки «кольцо 0..2») и их отсутствие красное наравне с кладкой.

Место замера сдвинуто с 0.30 на 0.35: ход шахты отсчитывается с 0.30, и
на самой этой доле кольца только начинают расти.

Живьём: `прокол 0.35: кладки нет, кольца кольцо 1,кольцо 2`.

### Чем проверено, что пустой тянучки не появилось

Опасение было такое: кирпичи закрывали собой окно между разлётом и
тоннелем, и без них остался бы кусок ленты, где ничего не меняется -
ровно то, что владелец называл «долго вниз иду, но ничего на экране не
меняется».

Проверка `движение` его снимает. Разницы соседних кадров по долям
периметра: 6.1 · 8.7 · 8.9 · 8.8 · 4.5 · 27.2 · 8.1 - кадр меняется и в
хвосте акта, где прежде шёл сбор. Состав кадра на 0.94 (`что-в-кадре`):
кладки нет, подозрительного нет, тёмное по краям это обшивка самой трубы
на подлёте.

## ОТКРЫТЫЙ ПУНКТ: контраст камня на стыке станции и периметра

Состояние на конец захода по списку владельца от 12.09.2026.

Владелец: «материал снова начал меняться до этого так небыло». Пункт
состоял из ЧЕТЫРЁХ разных вещей, три закрыты, четвёртая закрыта
частично.

**Закрыто.**

1. Ракета в финале таяла из камня в металл на долях 0.00..0.22, ровно
   там, где камера по новому темпу стоит. Ставится собранной с первого
   кадра акта (`победа(true, true)` в rv-act-финал.js).
2. Обводка кладки рисуется тёмной краской сквозь туман: на близком
   куполе это контур в стиле igloo, на отошедшей плоской стене туман
   поднимает её до синей пелены. Гаснет с куполом (альфа на `1-uStena`).
3. Свечение шва: формула `1.0 - uStena * vLet` давала ноль только когда
   блок И в стене, И в полёте, то есть никогда, и на собранной стене шов
   горел в полную силу. Абзац над строкой требовал обратного. Стало
   `1.0 - uStena * (1.0 - vLet)`.

**Закрыто частично: разрыв света между двумя модулями на границе 0.94.**

Одну стену держат два модуля: до 0.94 rv-act-станция, дальше
rv-кирпичи. Замер куска кладки по ряду долей с покоем на каждой
(`tools/checks/один-камень.mjs`):

    до правок        станция 0.90  153.5 / контраст 28.4 / синий 175.7
                     станция 0.96  128.0 / контраст 24.3 / синий 143.9
                     шаг           27.8  / 4.1            / 33.7

    после множителя  станция 0.90  128.3 / контраст 36.5 / синий 149.4
    0.527            шаг            3.7  / 11.9           / 8.7

Яркость и синий сведены и лежат в допусках (8 и 9). КОНТРАСТ НЕТ: 11.9
при допуске 4, то есть зерно кладки дома примерно вдвое жёстче
кирпичного.

**Что уже пробовано и не сработало, чтобы не пробовать снова.**

| попытка | результат |
|---|---|
| подставить формулу света кирпичей `mix(0.80,1.10,hemi)*0.57` | шаг яркости ВЫРОС с 25.6 до 43.5 |
| ровный множитель 0.834 (отношение уровней на экране) | на экране вышло 0.95, шаг 27.8 -> 21.0 |
| множитель 0.527, решённый через отклик плёнки | яркость и синий сошлись, контраст не тронут |
| погасить запечённое затенение `mix(0.3,1.0,vAO)` со сводом | контраст 11.9 -> 10.9, яркость и синий ухудшились |

**Два вывода для следующего захода.**

Первый: множитель в шейдере и уровень на экране это РАЗНЫЕ числа.
Между ними свечение с нулевым порогом и тональная кривая (rv-ореол.js).
Отклик замерен степенным законом по двум точкам, показатель k = 0.2835:
множитель 0.834 даёт на экране 0.95. Любое число, подобранное «по
яркости на снимке», обязано проходить через этот пересчёт.

Второй: источник контраста надо искать ЗАМЕРОМ, а не чтением шейдера.
Четыре попытки по чтению закрыли середину и не тронули разброс. Мерить
надо вклад каждого множителя base по отдельности: у дома это kamen,
kamen2, podtek, фотоскан и щель шва, у кирпичей zerno, zerno2, podtek и
фотоскан. Имена в двух файлах разные, формулы списаны друг у друга, и
где они разошлись, чтением уже трижды не нашлось.

## ОТКРЫТЫЙ ПУНКТ: рубка меньше корпуса в 5.57 раза (каша в дверях)

Владелец: «в открытых дверях видно кашу в салоне салон не тот что
внутри. Захожу и меняется что-то и снова панель вначале была близко
панель потом снова далеко странно». Это ОДНА причина на две жалобы, и
она измерена.

**Как нашлось.** Перебор тел в рамке проёма люка
(`tools/каша-в-проёме.mjs`, финал 0.50):

    без «пол комнаты»      меняется  7.5% точек проёма
    без «воздух комнаты»             13.9%
    без «комната» целиком            24.0%
    без «чужой корабль»              82.6%

Снимок без «чужого корабля» показал, что под этим узлом лежит ВЕСЬ
корпус, и без него в кадре остаётся коробка сто шестьдесят на сто
двадцать точек экрана. Это и есть рубка.

Прежние подозреваемые (пол зала, воздух, дымы) дают от семи до двадцати
четырёх процентов. Я собиралась гасить их до открытия створок - беду бы
это не вылечило.

**Числа** (`tools/салон-против-корпуса.mjs`, финал 0.50):

    корпус «снаружи»        12.806 x 11.326 x 12.806  центр 0, 21.22, -4
    рубка, радиус обшивки   3.05 местных x 0.377 = 1.150 мировых
    поперечник рубки        2.300
    корпус шире рубки в     5.57 раза

**Габаритная коробка рубки не мера, и она меня обманула.** Первый заход
делил коробки и напечатал «ок  салон заполняет корпус изнутри» при
соотношении 235%: коробку салона раздувает `даль`, плоскость с Землёй за
остеклением, она же уводит центр коробки на 5.58 назад (z -9.58 против
-4). Мера берётся у модуля: `замерРубки().радиусОбшивки` в местных
единицах, на масштаб узла.

**В кадре ДВА корпуса разного размера, и оба включены всегда.**
`RV_ФИНАЛ.корпус()` показывает и обшивку из `RV_КОРАБЛЬ` (12.81 в мире),
и свой борт с проёмом и створками (R_БОРТ 3.45 местных, то есть 2.60 в
мире). Комментарий называет второй «запасным, если чужой модуль не
доехал», но условия там нет - он горит всегда. Створки, щель и проём
принадлежат МАЛОМУ корпусу, поэтому люк в кадре маленький, а обшивка за
ним большая.

**Куда править.** Растить малую группу (борт, створки, щель, салон, даль,
пульт) до большого корпуса, а не наоборот: размер большого прибит к росту
роя из зала множителем 0.377 (`МАСШТАБ` в rv-act-финал.js, посчитан как
10.64/28.21 по ВЫСОТЕ), и трогать его нельзя - это правка владельца про
шов между двумя ракетами. Местные отношения рубки при равномерном
масштабе не меняются, значит сходимость с соседним сайтом сохранится
(`замерРубки`: настил у стены 0.4992/1.1335 против 0.5/1.1465 у соседа),
а поза считается по мировым матрицам и поедет сама.

**Что надо замерить ПЕРЕД правкой.** Множитель нельзя брать как
12.806/2.6013 = 4.923: 12.806 это коробка, и в неё входят стабилизаторы,
которые торчат далеко за обшивку. Нужен радиус ТЕЛА корпуса из
`RV_КОРАБЛЬ`, а не габарит с оперением. Отдельная проба на это ещё не
написана.
