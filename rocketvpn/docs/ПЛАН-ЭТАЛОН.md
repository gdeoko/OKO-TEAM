# Rocket VPN до уровня эталонных WebGL-сайтов

Разбор по коду: igloo.inc, wearebrand.io, lusion.co, unseen.co, basement.studio,
activetheory.net. Взяты ПРИЁМЫ (архитектура сцены, математика шейдеров, ход
камеры, как текст живёт в 3D), не ассеты и не контент. Дополняет docs/РАЗБОР-IGLOO.md.

{"summary": "Разбор эталонных WebGL-сайтов по коду и план, как поднять Rocket VPN до их уровня", "agentCount": 6, "logs": ["сайтов разобрано 5 из 5"], "result": "Прочитал ядро (rv-world.js, rv-real.js, rv-motion.js, rv-слово.js/.css, rv-титры.js, rv-луна.js, rv-act-станция.js), сценарий, frame.html и разбор igloo. Мир уже несёт половину рецептов референсов (экранный туман, синяя контактная тень, edge-dissolve, sinenoise1, кадронезависимая подтяжка, своя плёнка с bloom-пирамидой). Ниже план, как добрать остальное.\n\n---\n\n# ПЛАН: Rocket VPN до уровня эталонных WebGL-сайтов\n\n## 1. ЧТО У НИХ ОБЩЕГО (пять приёмов, которые делают картину дорогой)\n\n**baked_light_not_realtime.** Свет нигде не считается вживую, он лежит текстурой. Basement: 17 файлов bake-XX-lightmap-*.ktx2 + bake-XX-ao-*.jpg. Igloo: ноль включённых источников three.js, весь свет это карты 2048x2048. Activetheory: ___CyclesBake_COMBINED, PBR упакован в один RGB (metal/rough/ao по каналам). Lusion/unseen: matcap (58 штук у lusion), развёртка света зашита в сферу. Общее правило: живого света минимум, форму даёт запечённая карта.\n\n**final_post_pass.** Поверх сцены один финишный проход с низкими интенсивностями. Lusion: filmicToneMapping `(c*(6.2c+.5))/(c*(6.2c+1.7)+0.06)`, дизер `hash13/255.0` (ровно 1 бит), виньетка from=2 to=5. Unseen: бочка amt -0.15*0.4, хроматика 5 проб, зерно 0.07, виньетка pow 0.05, FXAA. Igloo: SMAA HIGH + LUT 32x32x32 + Bloom 6 уровней порог 0.2. Общее: тонмап + дизер + слабая виньетка + слабый bloom, всё тихо, а не «эффект ради эффекта».\n\n**everything_lerp_frameindependent.** Ничто не встаёт в позицию мгновенно, всё через `lerp(a,b,1-exp(-rate*dt))` с мелким коэффициентом. Lusion: `damp`, wheelEaseCoeff=12. Activetheory: доминируют lerp 0.05 и 0.1, камера двойной лерп, ориентация slerp. Igloo: `1-exp2(log2(1-t)*dt)`, курсор земли lerp 0.03. Общее: коэффициент 0.05-0.1 на кадр, одинаково на 60/120/144 Гц.\n\n**text_one_way_synced.** Текст решён ОДНИМ способом, синхронным с камерой. Unseen: troika MSDF-меши в сцене + туман по глубине `smoothstep(fogNear,fogFar,depth)`. Basement: резкий DOM кастомным шрифтом, привязанный к проекции. Activetheory: SDF+halo в сцене. Общее: текст либо честная геометрия в тумане и bloom мира, либо резкий DOM по проекции камеры. Наклейки-рандома нет.\n\n**one_world_narrow_fov.** Один непрерывный мир, разделы это перелёты камеры, угол узкий. Basement: fov 8.5 из z=2, весь сайт одна диорама, routingElements.glb задаёт путь. Igloo: одна сцена. Общее: собранный макет вместо рыбьего глаза, полёт без разрезов.\n\n---\n\n## 2. ГДЕ RV СЕЙЧАС ОТСТАЁТ (честно, по файлам)\n\n- **Пост есть, но короткий.** `rv-real.js:734` пирамида ореола в 3 уровня (СДВИГ [1,2,4], ВЕС [1.0,0.55,0.30]), порог `0.62` (`:757`). У igloo 6 уровней и порог 0.2: у нас светится только вспышка, а не обшивка под ключом. Дизер белым хешем от координаты (`:243`), синего шума с покадровым сдвигом нет.\n- **Грейда как ручки нет.** Вид вшит в аналитическую кривую в коде плёнки, одна на все акты и обе темы. LUT-файла (Texture3D) нет нигде (`grep LUT` пусто). Любая правка тона это правка кода, и цвет между `rv-act-*` не приведён к одной палитре.\n- **Свет живой и тяжёлый.** `rv-real.js:304-337` четыре источника (Directional 3.2/2.1/2.4 + Hemisphere 2.5) считаются на каждом пикселе NASA-моделей (станция под 80к вершин). aoMap/lightMap не подключены, matcap не используется. Это источник и пересвета, и нагрузки на телефоне.\n- **Сглаживание разнописано.** `rv-motion.js:44` подтяжка через `1-pow(1-k,раз)`, `rv-world.js:887/908` через `1-exp(-dt/tau)`, `rv-рой.js:69` своя. Разнобой записи это разнобой поведения. Второго сглаживающего прохода на позицию камеры и пружины второго порядка нет (slerp по кватерниону уже есть, `rv-world.js:805`).\n- **Текст живёт в ДВУХ мирах разом.** `rv-слово.css` держит абзацы/карточки/заголовки DOM в CSS-перспективе (`perspective`, `preserve-3d`, точка схода, крен из `--сл-*`, `:47-51`), а `rv-титры.js` кладёт в сцену только ИМЯ акта плоскостью. Заголовки разделов и карточки всё ещё наклейка поверх холста. Ровно то, на что жаловался владелец.\n- **Плотность высокая.** `rv-world.js:91` плотностьПо: ступень 2 = 1.8 против потолка 1.15-1.5 у igloo. Адаптивного множителя по частоте кадров для точек нет.\n- **Актов девять.** frame.html: станция, видно, периметр, оболочка, прокол, выход, рубка, пуск, стыковка. Между ними бывают чёрные провалы (об этом прямо сказано в `rv-motion.js:100-106`). Владелец хочет 4-6.\n\n---\n\n## 3. ПЛАН НА 6 УЗЛОВ (глагол «прокол», один мир, камера по узлам)\n\nСжать 8 содержательных актов до 6, продающая лестница проблема -> шифрование -> скорость -> продукт -> цена -> связка сохраняется. Каждый узел это поза камеры на одной кривой (`rv-world.js` уже слерпит по `кватУзлов`).\n\n1. **hero_станция_видно** (склейка станция+видно). Идея: лунная станция из блоков + «тебя видно». 3D: готовое из `rv-луна.js`/`rv-act-станция.js` (разлёт-сборка купола, экранный туман `nebo()`, синяя контактная тень `vec3(0.5,0.7,1.0)*0.55`, edge-dissolve `:210`), поверх решётка открытой сети `rv-net.js` уходит в глубину, пакет человека вскрывается линией досмотра. Текст: H1 «Канал начинается здесь» честной плоскостью в сцене (расширить `rv-титры.js`), туман по глубине.\n\n2. **периметр** (оставить). Идея: блокпост, блокировки, троттлинг, белые списки. 3D: `блокпост.webp` железом + `rvПериметр` холст, отбитые пакеты с физикой отскока, янтарные лучи досмотра (единственное тёплое пятно). Приём: `mesh_godrays` конусами с additive вместо волюметрики. Текст: имя акта в сцене, лид плоскостью.\n\n3. **оболочка** (оставить, момент бренда). Идея: пакет одевается, шифрование, первый индиго. 3D: оболочка-стекло по рецепту igloo #5 (`MeshPhysicalMaterial` transmission/ior 1.18 + хроматика по каналам onBeforeCompile, фон статичной картинкой неба вместо второго прохода сцены), луч досмотра соскальзывает. Текст: марка Rocket VPN плоскостью в сцене.\n\n4. **прокол** (оставить, подпись). Идея: пространство складывается, скорость. 3D: линза сворачивает стену в кольцо (полноэкранный шейдер искривления), тоннель `rv-коридор.js` строго вглубь, каустики по стенкам (igloo #9: `min` двух ползущих выборок 64x64, множитель на `color.b`), вспышка на входе, тишина. Текст: слово НАСКВОЗЬ плоскостью, приходит прямо на объектив (правило 3 из `rv-слово.js`).\n\n5. **выход_рубка** (склейка выход+рубка+пуск, разведены по фазам `data-лист`). Идея: локации + тарифы + устройства + рефералка + три шага. 3D: орбитальные станции = страны выхода (`rv-act-exit/dock/bridge.js`), выбор станции доворотом камеры, доки «Орбита-3/5», звено-рефералка орбитами. Механика железом, не таблицей. Текст: карточки станций и тарифов честными панелями, привязанными к проекции камеры (см. раздел 4).\n\n6. **стыковка** (оставить, связка). Идея: вторые ворота ROCKET CDN, тот же прокол в бирюзе. 3D: линза прокола повторно, цвет `#30A8D8` вместо индиго. Текст: марка CDN плоскостью.\n\nИтог: 6 узлов на одной кривой, все переходы это перелёты камеры без разрезов, чёрные провалы убираются плотной посадкой узлов.\n\n---\n\n## 4. ТЕКСТ В МИРЕ (конкретный приём, читаемый на телефоне)\n\nГибрид, где заголовок это событие в сцене, а рабочий текст резкий DOM по проекции.\n\n**title_plane_fog** (заголовки -> сцена). `rv-титры.js` уже печёт строку в canvas-текстуру (`текст()`, 3 прохода нимб/тело/ядро, `:101-107`) и вешает `PlaneGeometry` за предметами с additive и iней-reveal. Расширить его с «только имя акта» до H1/H2 акта. Добавить в фрагмент те же три строки тумана по глубине, что у unseen:\n```\nfloat depth = gl_FragCoord.z / gl_FragCoord.w;\nfloat fog = smoothstep(fogNear, fogFar, depth);\ngl_FragColor.rgb = mix(gl_FragColor.rgb, fogColor, fog);\n```\nfogColor берётся из темы (тот же цвет, что `nebo()` в `rv-луна.js`). Заголовок садится в дымку мира и попадает в общую плёнку, читается частью кадра. MSDF не нужен: canvas-текстура 900-веса даёт кириллицу за копейки (сам `rv-титры.js:22-27` это отмечает).\n\n**dom_by_true_projection** (абзацы/карточки -> резкий DOM). Тело абзаца, сноски и карточки станций/тарифов держать DOM (резче любой геометрии, как basement), но привязать к ТОЧНОЙ проекции камеры, а не к приблизительной CSS-перспективе. `rv-слово.js:124-131` уже считает перспективу формулой `P=(H/2)*zoom/tan(fov/2)`. Довести до полной формулы unseen/basement `fov=2*atan(innerHeight/2/camZ)`: держать невидимый каркас-rect, класть DOM-панель по его rect + смещению прокрутки. Тогда карточки едут синхронно с миром, правятся в CSS, и наклейкой не читаются.\n\nЧитаемость на 360px: заголовок крупный, стоит по оси кадра (на телефоне сдвигать некуда, `rv-титры.js:67-70`), кегль от высоты окна; em-слово индиго остаётся событием; рабочий текст всегда резкий DOM, никогда не уходит в геометрию.\n\n---\n\n## 5. ПОРЯДОК РАБОТ (разница видна рано)\n\n1. **post_boost** (полдня, заметно сразу на всех актах). В `rv-real.js` плёнке: порог bloom `0.62 -> 0.30`, уровни `3 -> 5` (каждый вчетверо меньше по площади, почти даром), белый hash-дизер заменить на синий шум 128x128 (void-and-cluster, свой скрипт) с покадровым сдвигом offset. Обшивка под ключом начинает светиться, полосы на небе уходят.\n2. **lut_grade** (день). Свой `.cube` LUT на тёмную и светлую тему, отдельным `ShaderPass` после bloom через Texture3D, тетраэдральная выборка (igloo #11). 14 КБ на тему, приводит станцию, тоннель и текст к одной палитре, снимает разнобой цвета между `rv-act-*`. Вид правится в DaVinci без сборки.\n3. **unify_damp** (полдня). Свести `rv-motion.js`, `rv-world.js`, `rv-рой.js` к одной `damp(a,b,rate,dt)=lerp(a,b,1-exp(-rate*dt))` + `damp3` для векторов, добавить второй сглаживающий проход на позицию камеры (двойной лерп activetheory) поверх готового slerp. Остаток щелчков взгляда уходит.\n4. **text_to_world** (день, прямой ответ владельцу). `title_plane_fog` + `dom_by_true_projection` из раздела 4.\n5. **collapse_acts** (полдня). Склейки 8 -> 6 в frame.html и узлах `rv-world.js`, плотная посадка узлов, чёрные провалы между актами убрать (`подход`/`уход` в `rv-motion.js` уже есть, довести перекрытие).\n6. **bake_light** (больше дня, дороже, потому последним). Запечь GI станции/корабля/зонда в Blender Cycles в lightmap, подключить как `aoMap`+`lightMap`, живые источники срезать до ключ+небо. Пересвет и нагрузка на телефоне уходят вместе с суммой четырёх источников.\n\n---\n\n## 6. ЧЕГО НЕ БРАТЬ (с причиной, бюджет кадра телефона)\n\n- **gpu_fluid_navier_stokes** (unseen fluidSim 128px, activetheory PRESSURE_ITERATIONS=20, CURL=30). Пинг-понг FBO плюс 20 итераций давления на кадр съедают мобильный GPU. Причина: бюджет кадра. Максимум один акт с даунскейлом SIM 64-128, и то опционально.\n- **offscreen_worker_canvas** (basement, transferControlToOffscreen). Крупный рефактор всего цикла `rv-world.js` (рендер в главном потоке, `:2153`). Причина: цена и риск переписать ядро не окупают выигрыш плавности, который дешевле берётся единым damp и постом.\n- **troika_msdf_runtime** (unseen). +1.5 МБ разбора шрифта в браузере на кириллице (сам `rv-титры.js:22-27` это фиксирует). Причина: вес загрузки на мобиле. canvas-текстура даёт тот же кадр даром.\n- **smaa_three_pass** (igloo/lusion, area/search текстуры, 3 прохода). Три полных прохода поверх плёнки дороги на слабом GPU и с постом бесполезен встроенный MSAA. Причина: бюджет кадра. FXAA один проход дешевле и работает после плёнки.\n- **rect_area_light_ltc** (activetheory, площадные источники). Дороже matcap при том же результате. Причина: бюджет кадра, matcap или запечённая карта дешевле.\n- **screen_space_mouse_paint** (lusion, экранный FBO под курсор). Приятно, но лишний RT на кадр ради ряби под курсором на телефоне не окупается. Причина: бюджет кадра, необязательный флориш.\n\n---\n\nКлючевые файлы: `/home/user/OKO-TEAM/rocketvpn/assets/rv-real.js` (плёнка/свет), `/home/user/OKO-TEAM/rocketvpn/assets/rv-титры.js` (текст в сцене), `/home/user/OKO-TEAM/rocketvpn/assets/rv-слово.css` (DOM-перспектива), `/home/user/OKO-TEAM/rocketvpn/assets/rv-world.js` (рендер/камера/узлы), `/home/user/OKO-TEAM/rocketvpn/assets/rv-motion.js` (доли актов), `/home/user/OKO-TEAM/rocketvpn/assets/rv-луна.js` (готовый hero-рецепт igloo), `/home/user/OKO-TEAM/rocketvpn/frame.html` (9 актов сейчас).", "workflowProgress": [{"type": "workflow_phase", "index": 1, "title": "Разбор"}, {"type": "workflow_phase", "index": 2, "title": "План"}, {"type": "workflow_agent", "index": 1, "label": "разбор:wearebrand", "phaseIndex": 1, "phaseTitle": "Разбор", "agentId": "a896c147168b7fa77", "model": "claude-opus-4-8", "state": "done", "startedAt": 1788451142143, "queuedAt": 1788451142089, "attempt": 1, "lastToolName": "StructuredOutput", "lastToolSummary": "WordPress + тема hello-elementor + Elementor Pro 3.30 (fron…", "promptPreview": "СТРОГО ЧТЕНИЕ И РАЗБОР. Ничего не публикуй, ничего не меняй в файлах проекта.\nТы разбираешь ЧУЖОЙ сайт, чтобы понять ПРИЁМЫ (архитектура сцены, математика\nшейдеров, механика прокрутки и камеры, как текст живёт в 3D). Их ассеты, шрифты\nи контент мы НЕ копируем: берём способ, а не файлы.\n\nКак смотреть код сайта (сеть идёт через прокси, часть чужих CDN может не\nпускать - это нормально, бери что досту…", "lastProgressAt": 1788451405155, "tokens": 95006, "toolCalls": 14, "durationMs": 262980, "resultPreview": "{\"findings\":[{\"name\":\"scrollProc_parallax_math\",\"how\":\"Ядро моушена - Slider Revolution 7 (sr7.js, 343 КБ). Каждый слой сцены двигается по единой доле прокрутки scrollProc (диапазон -1..1 внутри модуля). Формула сдвига слоя из sr7.js дословно: sx = round(scrollProc * (-scroll.x/100 * module.h) * 10)/10, sy аналогично по scroll.y. То есть смещение слоя = доля прокрутки умножить на его личную скорос…"}, {"type": "workflow_agent", "index": 2, "label": "разбор:lusion", "phaseIndex": 1, "phaseTitle": "Разбор", "agentId": "a7917e63679ae00e8", "model": "claude-opus-4-8", "state": "done", "startedAt": 1788451142173, "queuedAt": 1788451142089, "attempt": 1, "lastToolName": "StructuredOutput", "lastToolSummary": "Astro (статический сайт, класс no-js на html, base href /, …", "promptPreview": "СТРОГО ЧТЕНИЕ И РАЗБОР. Ничего не публикуй, ничего не меняй в файлах проекта.\nТы разбираешь ЧУЖОЙ сайт, чтобы понять ПРИЁМЫ (архитектура сцены, математика\nшейдеров, механика прокрутки и камеры, как текст живёт в 3D). Их ассеты, шрифты\nи контент мы НЕ копируем: берём способ, а не файлы.\n\nКак смотреть код сайта (сеть идёт через прокси, часть чужих CDN может не\nпускать - это нормально, бери что досту…", "lastProgressAt": 1788451555658, "tokens": 101641, "toolCalls": 23, "durationMs": 408484, "resultPreview": "{\"findings\":[{\"name\":\"frame_independent_damp\",\"how\":\"Вся плавность держится на одной функции: damp(o,e,t,r){return lerp(o,e,1-Math.exp(-t*r))}, где lerp(o,e,t)=(1-t)*o+t*e, t это скорость сближения, r это дельта кадра в секундах. Из-за 1-exp(-rate*dt) движение одинаковое на 60, 120 и 144 Гц. Скролл сглаживается так же: a = f*(1-Math.exp(-wheelEaseCoeff*e)), где f=targetScrollPixel-scrollPixel, whe…"}, {"type": "workflow_agent", "index": 3, "label": "разбор:unseen", "phaseIndex": 1, "phaseTitle": "Разбор", "agentId": "a8a9d78e4ed41f5f1", "model": "claude-opus-4-8", "state": "done", "startedAt": 1788451405205, "queuedAt": 1788451142090, "attempt": 1, "lastToolName": "StructuredOutput", "lastToolSummary": "WordPress-тема unseen. Три бандла: manifest.js, vendor.js (…", "promptPreview": "СТРОГО ЧТЕНИЕ И РАЗБОР. Ничего не публикуй, ничего не меняй в файлах проекта.\nТы разбираешь ЧУЖОЙ сайт, чтобы понять ПРИЁМЫ (архитектура сцены, математика\nшейдеров, механика прокрутки и камеры, как текст живёт в 3D). Их ассеты, шрифты\nи контент мы НЕ копируем: берём способ, а не файлы.\n\nКак смотреть код сайта (сеть идёт через прокси, часть чужих CDN может не\nпускать - это нормально, бери что досту…", "lastProgressAt": 1788451663735, "tokens": 95100, "toolCalls": 18, "durationMs": 258527, "resultPreview": "{\"findings\":[{\"name\":\"Troika MSDF-текст прямо в 3D-сцене с туманом по глубине\",\"how\":\"Текст не HTML и не canvas-текстура, а troika-three-text: каждый заголовок/абзац - настоящий mesh из MSDF-глифов в мировых координатах. Шрифты грузятся как webglFonts 'Neue Montreal' и 'Saol Display', sdfGlyphSize:128, anchorX:'center', anchorY:'middle', letterSpacing:-.03. В фрагментный шейдер troika вручную допи…"}, {"type": "workflow_agent", "index": 4, "label": "разбор:basement", "phaseIndex": 1, "phaseTitle": "Разбор", "agentId": "a8e93f58b32353d7e", "model": "claude-opus-4-8", "state": "done", "startedAt": 1788451555689, "queuedAt": 1788451142090, "attempt": 1, "lastToolName": "StructuredOutput", "lastToolSummary": "three.js r180 + @react-three/fiber (без drei и без postproc…", "promptPreview": "СТРОГО ЧТЕНИЕ И РАЗБОР. Ничего не публикуй, ничего не меняй в файлах проекта.\nТы разбираешь ЧУЖОЙ сайт, чтобы понять ПРИЁМЫ (архитектура сцены, математика\nшейдеров, механика прокрутки и камеры, как текст живёт в 3D). Их ассеты, шрифты\nи контент мы НЕ копируем: берём способ, а не файлы.\n\nКак смотреть код сайта (сеть идёт через прокси, часть чужих CDN может не\nпускать - это нормально, бери что досту…", "lastProgressAt": 1788452029083, "tokens": 115868, "toolCalls": 27, "durationMs": 473394, "resultPreview": "{\"findings\":[{\"name\":\"baked_lightmaps_ao\",\"how\":\"Свет у basement не считается в реальном времени. Он запечён в текстуры: 17 файлов bake-XX-lightmap-*.ktx2 (lightMap) плюс bake-XX-ao-*.jpg (aoMap) на каждую комнату дигорамы (office, showcase, library, roof, chairs, people). Рантайм просто читает готовую карту света. Отсюда ровная дорогая картинка без пересвета и без шумных теней: тени, отражения и …"}, {"type": "workflow_agent", "index": 5, "label": "разбор:activetheory", "phaseIndex": 1, "phaseTitle": "Разбор", "agentId": "a7d78c992de8896ae", "model": "claude-opus-4-8", "state": "done", "startedAt": 1788451663774, "queuedAt": 1788451142090, "attempt": 1, "lastToolName": "StructuredOutput", "lastToolSummary": "Собственный движок Active Theory (Hydra/Aura), НЕ three.js:…", "promptPreview": "СТРОГО ЧТЕНИЕ И РАЗБОР. Ничего не публикуй, ничего не меняй в файлах проекта.\nТы разбираешь ЧУЖОЙ сайт, чтобы понять ПРИЁМЫ (архитектура сцены, математика\nшейдеров, механика прокрутки и камеры, как текст живёт в 3D). Их ассеты, шрифты\nи контент мы НЕ копируем: берём способ, а не файлы.\n\nКак смотреть код сайта (сеть идёт через прокси, часть чужих CDN может не\nпускать - это нормально, бери что досту…", "lastProgressAt": 1788452001723, "tokens": 109900, "toolCalls": 19, "durationMs": 337948, "resultPreview": "{\"findings\":[{\"name\":\"sdf_text_in_scene\",\"how\":\"Текст живёт настоящей WebGL-геометрией в сцене, не HTML поверх. Классы GLTextGeometry/GLUIText/Text3D: шрифт грузится как JSON с метриками глифов (GLTextGeometry.chars, kerning, whitespaces) плюс SDF-текстура (ASSETS.SDF, одноканальная, не MSDF - median нет). Раскладка своя: строки, justify с раздачей остатка по пробелам (totalToAdd/whitespaces). Кра…"}, {"type": "workflow_agent", "index": 6, "label": "план", "phaseIndex": 2, "phaseTitle": "План", "agentId": "aaed413d533cd9416", "model": "claude-opus-4-8", "state": "done", "startedAt": 1788452029113, "queuedAt": 1788452029087, "attempt": 1, "lastToolName": "Bash", "lastToolSummary": "cd /home/user/OKO-TEAM/rocketvpn/assets && echo \"===== rv-л…", "promptPreview": "СТРОГО ЧТЕНИЕ И РАЗБОР. Ничего не публикуй, ничего не меняй в файлах проекта.\nТы разбираешь ЧУЖОЙ сайт, чтобы понять ПРИЁМЫ (архитектура сцены, математика\nшейдеров, механика прокрутки и камеры, как текст живёт в 3D). Их ассеты, шрифты\nи контент мы НЕ копируем: берём способ, а не файлы.\n\nКак смотреть код сайта (сеть идёт через прокси, часть чужих CDN может не\nпускать - это нормально, бери что досту…", "lastProgressAt": 1788452328246, "tokens": 169149, "toolCalls": 17, "durationMs": 299131, "resultPreview": "Прочитал ядро (rv-world.js, rv-real.js, rv-motion.js, rv-слово.js/.css, rv-титры.js, rv-луна.js, rv-act-станция.js), сценарий, frame.html и разбор igloo. Мир уже несёт половину рецептов референсов (экранный туман, синяя контактная тень, edge-dissolve, sinenoise1, кадронезависимая подтяжка, своя плёнка с bloom-пирамидой). Ниже план, как добрать остальное.\n\n---\n\n# ПЛАН: Rocket VPN до уровня эталонны…"}], "totalTokens": 686664, "totalToolCalls": 118}
