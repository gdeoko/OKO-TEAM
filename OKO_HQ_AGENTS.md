# OKO HQ — Агенты (полная спецификация ролей)

Дополнение к `OKO_HQ_SPEC.md`. Здесь — каждая роль детально: внешность, атрибут, поведение,
что показывает экран ноутбука, какие команды берёт, откуда данные.

## Общий дизайн робота (напоминание)
Округлый низкополигональный робот, чёрный матовый корпус, **лицо = глаз-логотип OKO** (лаймовая
дуга-веко + светящийся зрачок #9AFF00 + вырез-полумесяц как в лого). Антенна с лаймовым огоньком.
Короткие ручки-капсулы + сферы-ладони. У каждой роли — **свой акцент-цвет** (шарф/полоса/наушники)
+ **свой атрибут-предмет** на столе/в руках. Глаз у всех единый лаймовый (все — агенты OKO).

Глаз-состояния (общие): idle — медленно моргает, смотрит по сторонам; working — зрачок ярче,
сфокусирован; blocked — краснеет, дрожит, «?» над головой; success — прищур + искра; thinking —
зрачок бегает, точки «...».

---

## РОЛИ (v1 — 10 агентов)

### 1. Гендиректор — «Даниэль»
- Цвет: **лайм #9AFF00** (главный, брендовый). Атрибут: угловой стол с панорамой на весь офис,
  кресло-босс, чашка кофе, табличка «CEO».
- Поведение: обход офиса, «раздаёт задачи» (жест), смотрит дашборд-стену. Idle — стоит у окна.
- Экран ноутбука: сводка (выручка, воронка, что делают все агенты) — вид «CEO overview».
- Команды: любые (роутит на других), стратегические решения, аппрув КП/договоров.
- Данные: агрегат со всех (`/api/funnel` + `/api/agents`).

### 2. Отдел продаж — «Sales» (можно 2-3: acc1/acc2/acc3 ТГ + ВК + YouTube)
- Цвет: **синий #4aa0ff**. Атрибут: 2 монитора (диалоги + воронка), гарнитура, стикеры лидов.
- Поведение: активно «печатает», иногда вскакивает (горячий лид → war-room). Idle редко.
- Экран: живой диалог с клиентом (последние сообщения + стадия воронки + следующий шаг).
- Команды: `дожать <лид>`, `назвать цену <лид>`, `отправить КП/договор <клиент>`, `прогреть запас`.
- Данные: `client_memory`, `dialogs.jsonl`, `taskqueue` (отправка). Природа: **живой 24/7 на VPS**.

### 3. Монтажёр — «Editor»
- Цвет: **фиолет #a855f7**. Атрибут: таймлайн на экране, наушники, «плёнка»/клапперборд на столе.
- Поведение: покачивает головой «в ритм», рендерит (прогресс-бар над столом).
- Экран: таймлайн монтажа / прогресс рендера / превью ролика.
- Команды: `смонтируй ролик <сценарий>`, `собери Reels/Shorts`, `сделай тест-ролик <клиент>`.
- Данные: скилл `reels-machine`. Природа: **роль-Claude** (я через мост; экран = скрин процесса).

### 4. Дизайнер — «Designer»
- Цвет: **оранж #ff7a3c**. Атрибут: графический планшет, палитра, макеты на стене.
- Поведение: «рисует» рукой, разглядывает макет, показывает большой палец при готовом.
- Экран: Figma/макет/рендер обложки (как обложки MedCraft).
- Команды: `сделай обложку/карточку/макет <бриф>`, `тест-дизайн <клиент>`, `баннер <тема>`.
- Данные: Higgsfield / HTML-композит + Chromium-рендер. Природа: **роль-Claude**.

### 5. Маркетолог — «Marketer»
- Цвет: **бирюза #22d3ee**. Атрибут: графики/дашборд аналитики, доска с воронкой.
- Поведение: тыкает в графики, «считает» (цифры над головой).
- Экран: аналитика (охваты, конверсии, ROI), сегменты ЦА, план кампании.
- Команды: `придумай воронку/оффер`, `分 разбери нишу <тема>`, `контент-план <клиент>`.
- Данные: база знаний продаж + аналитика соцсетей. Природа: **роль-Claude**.

### 6. Юрист — «Legal»
- Цвет: **золото #d4af37**. Атрибут: «галстук», стопка документов, печать, весы Фемиды на столе.
- Поведение: листает документы, поправляет «очки», кивает при проверке.
- Экран: договор/КП по эталону, чек 161-ФЗ, реквизиты.
- Команды: `составь договор <клиент>`, `проверь на 161-ФЗ`, `оферта/условия`.
- Данные: эталон-договор, `core/contract.py`, реквизиты из vault. Природа: **роль-Claude**.
- ВАЖНО: оплата ТОЛЬКО lava/офиц.реквизиты/крипта — юрист следит за 161-ФЗ.

### 7. Копирайтер — «Copy»
- Цвет: **розовый #ff6bad**. Атрибут: печатная машинка/клавиатура, блокнот, кофе.
- Поведение: быстро печатает, черкает, «озаряется» (лампочка над головой).
- Экран: тексты — офферы, посты, скрипты, заголовки (варианты A/B).
- Команды: `напиши оффер/пост/скрипт <тема>`, `заголовки <продукт>`, `прогрев <клиент>`.
- Данные: база продаж (приёмы/хуки/скрипты). Природа: **роль-Claude**.

### 8. Саппорт OKO App — «Support»
- Цвет: **зелёный #34d399**. Атрибут: гарнитура, много чат-окон, значок «24/7».
- Поведение: быстро отвечает в несколько окон, дружелюбный.
- Экран: тикеты/чаты поддержки приложения OKO App, статусы обращений.
- Команды: `ответь в поддержку`, `разбери тикет <id>`, FAQ/онбординг.
- Данные: чаты OKO App. Природа: **роль-Claude / бот приложения**.

### 9. Контент-завод — «Factory»
- Цвет: **жёлтый #facc15**. Атрибут: конвейер с роликами, много экранов, робо-рука.
- Поведение: «штампует» контент, конвейер движется, ролики «улетают» в постинг.
- Экран: очередь роликов/постов, календарь автопостинга (Hooppy), статус выкладки.
- Команды: `запусти контент под <ниша>`, `автопостинг <клиент>`, `продвижение OKO App`.
- Данные: `reels-machine`, Hooppy, соцсети клиентов. Природа: **роль-Claude + автопостинг**.

### 10. Помощники — «Assistants» (1-2 универсала)
- Цвет: **серо-лайм #9AFF00/#8892a0**. Атрибут: швейцарский нож, разные инструменты.
- Поведение: подхватывают любую задачу, бегают между столами, помогают другим.
- Экран: текущая разнородная задача (парсинг, поиск, разбор файла).
- Команды: всё, что не попало в узкую роль; ресёрч, парсинг, разбор загрузок в мозг.
- Данные: браузер (Chromium), поиск, OCR/whisper. Природа: **роль-Claude**.

---

## Логика «двух природ» (важно для сборки)
- **Живые 24/7 на VPS**: Отдел продаж (ТГ acc1/2/3, ВК, YouTube). Статус/действия/экран берём с
  `CONTROL_URL` (`dialogs.jsonl`, `client_memory`). Они работают сами, дашборд их ОТОБРАЖАЕТ и
  позволяет вмешаться/командовать через `taskqueue`-мост.
- **Роли-Claude** (монтажёр/дизайнер/маркетолог/юрист/копирайтер/саппорт/завод/помощники): это
  Claude Code (через мост) в разных «шляпах». Когда даёшь задачу такой роли — она ставится в
  `taskqueue`, я выполняю, пушу на «экран» этого агента скрин процесса (браузер/код/рендер) и
  отчитываюсь в ленту. В офисе обе природы = сотрудники за столами, визуально одинаково.

## Модель данных агента (для дашборда)
```json
{
  "id": "designer",
  "role": "Дизайнер",
  "nature": "role-claude",        // или "live-vps"
  "accent": "#ff7a3c",
  "attr": "tablet",
  "status": "working",            // idle|working|blocked|thinking
  "task": "обложка Цинк для MedCraft",
  "progress": 0.6,
  "screen": { "type": "render", "url": "/screens/designer_live.png" },
  "stats": { "done": 12, "revenue": 0 },
  "desk": [ -3.2, 0, 1.8 ],
  "eye": "focused"                // состояние глаза
}
```

## Распределение задач (диспетчер)
Правила роутинга по ключевым словам (пример):
- «обложк|карточк|макет|баннер|дизайн» → Дизайнер
- «ролик|reels|shorts|монтаж|видео» → Монтажёр / Контент-завод
- «договор|оферта|161|юрид» → Юрист
- «оффер|пост|текст|скрипт|заголов» → Копирайтер
- «дожать|цена|КП|клиент|лид|продаж» → Отдел продаж
- «воронк|аналитик|ниша|стратег» → Маркетолог
- «поддержк|тикет|FAQ» → Саппорт
- иначе / сложное / много ролей → дробим и раздаём + Помощники
Общий чат без адресата → диспетчер сам решает и раздаёт (может нескольким сразу).
