# 🌀 DIESEL — контент-завод (доставка техники из Китая)

Объединённая ветка по клиенту **DIESEL / Cargo Panda Go** и всему, что касается
**доставки техники из Китая**. Здесь собраны все материалы завода:
- `oko-app/factory/` — пайплайн DIESEL-роликов, анализ конкурентов, реестры (USED_*), vps_mirror
  (`diesel_analytics.py`, `diesel_engage.mjs`, `diesel_publish_slot.sh`).
- `pandago-order/` — сайт/материалы PandaGo (заказ доставки).
- `brain/Claude/Projects/` и `brain/Claude/Sessions/` — память проекта (diesel/pandago сессии).

Аккаунты: IG `diesel_cargo` / `diesel_kitay`, TikTok DIESEL (Hooppy page 2350915),
YouTube `CLIENT_DIESEL_YT_*` (на VPS), сайт `dieselcompany.pro`. Доступы — в мастер-vault (§8).

Порядок сборки каждого ролика — `.claude/skills/oko-content-factory/FACTORY_PROTOCOL.md`
(⛔ закон: внутри проекта DIESEL ничего не повторяется — новые конкуренты/кадры/музыка/
наложения/переходы/субтитры на каждый ролик).
