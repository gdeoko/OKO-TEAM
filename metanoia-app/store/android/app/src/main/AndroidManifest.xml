<?xml version="1.0" encoding="utf-8"?>
<!-- Обёртка приложения: открывает школу в доверенном окне браузера.
     Ни куска содержимого здесь нет, всё живёт на сайте школы. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <!-- Голосовые сообщения и видеокружочки в чатах школы -->
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />

  <application
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher"
      android:allowBackup="true"
      android:supportsRtl="false"
      android:theme="@style/Theme.Metanoya">

    <meta-data android:name="asset_statements" android:resource="@string/asset_statements" />

    <activity
        android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
        android:exported="true"
        android:screenOrientation="portrait">

      <meta-data android:name="android.support.customtabs.trusted.DEFAULT_URL"
                 android:value="${defaultUrl}" />
      <meta-data android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                 android:resource="@color/cream" />
      <meta-data android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                 android:resource="@color/cream" />
      <meta-data android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
                 android:resource="@drawable/splash" />
      <meta-data android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                 android:resource="@color/cream" />
      <meta-data android:name="androidx.browser.trusted.FILE_PROVIDER_AUTHORITY"
                 android:value="ru.metanoya.app.fileprovider" />
      <!-- Если на телефоне нет браузера, умеющего доверенное окно (частый
           случай на устройствах без сервисов Google, а это как раз RuStore),
           школа открывается во встроенном окне, а не в браузере с адресной
           строкой. Для магазина это разница между «приложение» и «ярлык». -->
      <meta-data android:name="android.support.customtabs.trusted.FALLBACK_STRATEGY"
                 android:value="webview" />

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <!-- Ссылка на школу открывается прямо в приложении -->
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="${hostName}" />
      </intent-filter>
    </activity>

    <!-- Само встроенное окно для запасного случая -->
    <activity
        android:name="com.google.androidbrowserhelper.trusted.WebViewFallbackActivity"
        android:exported="false"
        android:screenOrientation="portrait"
        android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout" />

    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="ru.metanoya.app.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/filepaths" />
    </provider>
  </application>
</manifest>
